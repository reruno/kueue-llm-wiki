# PR #2363: Deactivate Workload if any of its AdmissionCheck get Rejected

**Summary**: Deactivate Workload if any of its AdmissionCheck get Rejected

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2363

**Last updated**: 2024-07-31T11:43:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `deactivate-workload` → `main`
- **Created**: 2024-06-05T11:44:17Z
- **Updated**: 2024-07-31T11:43:40Z
- **Merged**: 2024-06-12T10:26:25Z
- **Closed**: 2024-06-12T10:26:25Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 8   **+336 / -153**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
To unify behavior of Workloads which got an AdmissionCheck Rejected

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:
Please wait until #2196 is merged

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
AdmissionChecks: A workload with a Rejected AdmissionCheck gets deactivated
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-05T11:44:35Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2fe10164ff69815ea3845278f6e54d569f97ca31 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6668b993b950840008255517 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-05T11:44:38Z

/assign @alculquicondor 
cc @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-05T14:25:40Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T13:09:51Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-07T14:34:12Z

LGTM label has been added.  <details>Git tree hash: db1fdc4844e37e44c027b85ce99efc810f13251b</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-10T14:03:22Z

cc @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-10T15:56:15Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-10T15:56:21Z

LGTM label has been added.  <details>Git tree hash: a784746a766b900baa9db47eb0939a94dd9e42d1</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-10T16:00:13Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-12T10:01:22Z

/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-12T10:09:01Z

LGTM label has been added.  <details>Git tree hash: 5116a9954c3a080d71b804d68fc64aa395904f13</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-12T10:09:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2363#" title="Author self-approved">PBundyra</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2363#pullrequestreview-2112567147" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-12T15:06:54Z

Please update the documentation for this.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T20:38:41Z

/release-note-edit
```release-note
AdmissionChecks: A workload with a Rejected AdmissionCheck gets deactivated
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T10:25:21Z

- **pkg/controller/core/workload_controller.go:335** by [@mimowo](https://github.com/mimowo) — 2024-06-07T10:25:21Z
```diff
@@ -313,17 +313,32 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
-	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+	if workload.HasRetryChecks(wl) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, _ := r.queues.ClusterQueueForWorkload(wl)
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+	// if Workload has Rejected checks deactivate it
+	wl.Spec.Active = ptr.To(false)
+	if err := r.client.Update(ctx, wl); err != nil {
+		return false, err
+	}
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, "At least one admission check is Rejected")
```

  Could we delegate setting the Evicted condition to the workload_controller, see[here](https://github.com/kubernetes-sigs/kueue/blob/d728cb87dc3998592e1fcb27a672b15c46df8b6c/pkg/controller/core/workload_controller.go#L183-L205), and here just deactive (set active=false)? This would make the code consistent with PodsReady handling. Otherwise I feel there might be a race condition, where the workload_controller may be trying to do the same.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T10:48:17Z

- **pkg/controller/core/workload_controller.go:335** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T10:48:17Z
```diff
@@ -313,17 +313,32 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
-	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+	if workload.HasRetryChecks(wl) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, _ := r.queues.ClusterQueueForWorkload(wl)
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+	// if Workload has Rejected checks deactivate it
+	wl.Spec.Active = ptr.To(false)
+	if err := r.client.Update(ctx, wl); err != nil {
+		return false, err
+	}
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, "At least one admission check is Rejected")
```

  Thanks for catching this, fixed!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T12:53:21Z

- **pkg/controller/core/workload_controller.go:335** by [@mimowo](https://github.com/mimowo) — 2024-06-07T12:53:21Z
```diff
@@ -313,18 +313,27 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
-	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
-	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
-	if err == nil {
-		cqName, _ := r.queues.ClusterQueueForWorkload(wl)
-		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+	if workload.HasRetryChecks(wl) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, _ := r.queues.ClusterQueueForWorkload(wl)
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+	// if Workload has Rejected checks deactivate it
+	wl.Spec.Active = ptr.To(false)
+	if err := r.client.Update(ctx, wl); err != nil {
+		return false, err
 	}
-	return true, client.IgnoreNotFound(err)
+	r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v has got Rejected", workload.GetRejectedChecks(wl)[0])
```

  Can you explain where in code we make sure that `workload.GetRejectedChecks(wl)` is not empty to prevent panics?
  
  Also, do we have an analogous event for deactivating due to PodsReady timeout?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T12:59:16Z

- **pkg/controller/core/workload_controller.go:335** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T12:59:16Z
```diff
@@ -313,18 +313,27 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
-	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
-	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
-	if err == nil {
-		cqName, _ := r.queues.ClusterQueueForWorkload(wl)
-		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+	if workload.HasRetryChecks(wl) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, _ := r.queues.ClusterQueueForWorkload(wl)
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+	// if Workload has Rejected checks deactivate it
+	wl.Spec.Active = ptr.To(false)
+	if err := r.client.Update(ctx, wl); err != nil {
+		return false, err
 	}
-	return true, client.IgnoreNotFound(err)
+	r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v has got Rejected", workload.GetRejectedChecks(wl)[0])
```

  [At this point](https://github.com/kubernetes-sigs/kueue/pull/2363/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR330)
  We know a Workload has at least one Rejected AdmissionCheck because of previous if statements. I've added a comment clarifying that.
  
   >> Also, do we have an analogous event for deactivating due to PodsReady timeout?
   
  Afaik we don't. I can add it in a follow-up PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T13:25:53Z

- **pkg/controller/core/workload_controller.go:335** by [@mimowo](https://github.com/mimowo) — 2024-06-07T13:25:52Z
```diff
@@ -313,18 +313,27 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
-	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
-	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
-	if err == nil {
-		cqName, _ := r.queues.ClusterQueueForWorkload(wl)
-		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+	if workload.HasRetryChecks(wl) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, _ := r.queues.ClusterQueueForWorkload(wl)
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+	// if Workload has Rejected checks deactivate it
+	wl.Spec.Active = ptr.To(false)
+	if err := r.client.Update(ctx, wl); err != nil {
+		return false, err
 	}
-	return true, client.IgnoreNotFound(err)
+	r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v has got Rejected", workload.GetRejectedChecks(wl)[0])
```

  > [At this point](https://github.com/kubernetes-sigs/kueue/pull/2363/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR330)
  > We know a Workload has at least one Rejected AdmissionCheck because of previous if statements. I've added a comment clarifying that.
  
  I see, thanks for clarifying, but it leads me to another question.
  
  In the title you say "Deactivate Workload if any of its AdmissionCheck get Rejected". However, IIUC, if the workload has Retry check and Rejected check, then we would not deactivate as we first enter the `if workload.HasRetryChecks(wl) {` if. It is probably a corner case, so not sure if it matters, but I would like to make sure that the impl and title are aligned.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T13:37:57Z

- **pkg/controller/core/workload_controller.go:335** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T13:37:57Z
```diff
@@ -313,18 +313,27 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
-	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
-	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
-	if err == nil {
-		cqName, _ := r.queues.ClusterQueueForWorkload(wl)
-		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+	if workload.HasRetryChecks(wl) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, _ := r.queues.ClusterQueueForWorkload(wl)
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+	// if Workload has Rejected checks deactivate it
+	wl.Spec.Active = ptr.To(false)
+	if err := r.client.Update(ctx, wl); err != nil {
+		return false, err
 	}
-	return true, client.IgnoreNotFound(err)
+	r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v has got Rejected", workload.GetRejectedChecks(wl)[0])
```

  Good point, fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T13:45:28Z

- **pkg/controller/core/workload_controller.go:335** by [@mimowo](https://github.com/mimowo) — 2024-06-07T13:45:28Z
```diff
@@ -313,18 +313,27 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
-	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
-	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
-	if err == nil {
-		cqName, _ := r.queues.ClusterQueueForWorkload(wl)
-		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+	if workload.HasRetryChecks(wl) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, _ := r.queues.ClusterQueueForWorkload(wl)
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+	// if Workload has Rejected checks deactivate it
+	wl.Spec.Active = ptr.To(false)
+	if err := r.client.Update(ctx, wl); err != nil {
+		return false, err
 	}
-	return true, client.IgnoreNotFound(err)
+	r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v has got Rejected", workload.GetRejectedChecks(wl)[0])
```

  I like the change, as it is now also easy to see that `[0]` is safe.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T14:07:52Z

The implementation lgtm, rather minor comments to tests.

- **test/integration/controller/core/workload_controller_test.go:360** by [@mimowo](https://github.com/mimowo) — 2024-06-07T13:50:39Z
```diff
@@ -357,16 +357,16 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 					return k8sClient.Status().Update(ctx, &createdWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 
-				util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByAdmissionCheck, 1)
+				util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByDeactivation, 1)
```

  I would suggest to rename the step, maybe: "setting a check as rejected, the workload should be  deactivated and evicted". Please also check that `active=false`.

- **pkg/controller/core/workload_controller_test.go:497** by [@mimowo](https://github.com/mimowo) — 2024-06-07T13:58:06Z
```diff
@@ -491,18 +491,32 @@ func TestReconcile(t *testing.T) {
 			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
 				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
 				Admitted(true).
+				Active(false).
```

  I'm wondering if we should have a dedicated case for "admitted workload with retry checks", because in the past the handling was the same, we didn't need to have two cases.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:483** by [@mimowo](https://github.com/mimowo) — 2024-06-07T14:00:55Z
```diff
@@ -469,9 +470,107 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
+
+			ginkgo.By("Checking if workload is deactivated, an event is emitted and a metric is increased", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
+
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
```

  nit: This is a lot checks for a single Eventually. I think it is better not have multiple Eventually blocks, even if there is slightly more code. If things are inside a single Eventually, it is not clear for which we need to wait, and which updates are synchronous.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T14:27:45Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:483** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T14:27:45Z
```diff
@@ -469,9 +470,107 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
+
+			ginkgo.By("Checking if workload is deactivated, an event is emitted and a metric is increased", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
+
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T14:27:52Z

- **test/integration/controller/core/workload_controller_test.go:360** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T14:27:52Z
```diff
@@ -357,16 +357,16 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 					return k8sClient.Status().Update(ctx, &createdWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 
-				util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByAdmissionCheck, 1)
+				util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByDeactivation, 1)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T14:28:06Z

- **pkg/controller/core/workload_controller_test.go:497** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T14:28:06Z
```diff
@@ -491,18 +491,32 @@ func TestReconcile(t *testing.T) {
 			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
 				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
 				Admitted(true).
+				Active(false).
```

  Good point, fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T14:34:07Z

/lgtm
@tenzen-y anything to add?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-07T15:33:56Z

- **pkg/controller/core/workload_controller.go:325** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-07T15:29:42Z
```diff
@@ -313,11 +313,20 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  Please add a comment to indicate what the bool means.

- **pkg/controller/core/workload_controller.go:316** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-07T15:30:20Z
```diff
@@ -313,11 +313,20 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
```

  ```suggestion
  	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || (!workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl)) {
  ```
  
  for readability

- **pkg/controller/core/workload_controller.go:326** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-07T15:32:54Z
```diff
@@ -313,11 +313,20 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryOrRejectedChecks(wl) {
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) || !workload.HasRetryChecks(wl) && !workload.HasRejectedChecks(wl) {
 		return false, nil
 	}
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
+	if workload.HasRejectedChecks(wl) {
+		wl.Spec.Active = ptr.To(false)
+		if err := r.client.Update(ctx, wl); err != nil {
+			return false, err
+		}
+		r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v has got Rejected", workload.GetRejectedChecks(wl)[0])
```

  ```suggestion
  		r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v was Rejected: %s", workload.GetRejectedChecks(wl)[0], <message>)
  ```
  
  You'll have to modify `GetRejectedChecks` to return `AdmissionCheckState`. Rename to `RejectedChecks` while at it.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-10T10:29:07Z

- **pkg/controller/core/workload_controller.go:325** by [@PBundyra](https://github.com/PBundyra) — 2024-06-10T10:29:07Z
```diff
@@ -313,11 +313,20 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 }
 
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T11:52:16Z

LGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-10T16:01:09Z

- **site/static/examples/admin/minimal-cq.yaml:8** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-10T16:00:45Z
```diff
@@ -4,8 +4,11 @@ metadata:
   name: "cluster-queue-2"
 spec:
   namespaceSelector: {} # match all.
-  admissionChecksStrategy:
-    admissionChecks:
-    - name: "sample-prov"           # Name of the AdmissionCheck to be run
-      onFlavors: ["default-flavor"] # This AdmissionCheck will only run for Workloads that use default-flavor
-    - name: "sample-prov-2" # This AdmissionCheck will run for all Workloads regardless of a used ResourceFlavor
+  flavorFallbackStrategy:
+    rules:
```

  @PBundyra What is this API?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-10T16:02:26Z

- **site/static/examples/admin/minimal-cq.yaml:8** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-10T16:02:25Z
```diff
@@ -4,8 +4,11 @@ metadata:
   name: "cluster-queue-2"
 spec:
   namespaceSelector: {} # match all.
-  admissionChecksStrategy:
-    admissionChecks:
-    - name: "sample-prov"           # Name of the AdmissionCheck to be run
-      onFlavors: ["default-flavor"] # This AdmissionCheck will only run for Workloads that use default-flavor
-    - name: "sample-prov-2" # This AdmissionCheck will run for all Workloads regardless of a used ResourceFlavor
+  flavorFallbackStrategy:
+    rules:
```

  As I can see in this PR, there is not this API.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-10T16:52:05Z

- **site/static/examples/admin/minimal-cq.yaml:8** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-10T16:52:05Z
```diff
@@ -4,8 +4,11 @@ metadata:
   name: "cluster-queue-2"
 spec:
   namespaceSelector: {} # match all.
-  admissionChecksStrategy:
-    admissionChecks:
-    - name: "sample-prov"           # Name of the AdmissionCheck to be run
-      onFlavors: ["default-flavor"] # This AdmissionCheck will only run for Workloads that use default-flavor
-    - name: "sample-prov-2" # This AdmissionCheck will run for all Workloads regardless of a used ResourceFlavor
+  flavorFallbackStrategy:
+    rules:
```

  Oops, this looks like a change from a different effort.
  
  /approve cancel

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-10T17:28:44Z

- **site/static/examples/admin/minimal-cq.yaml:8** by [@PBundyra](https://github.com/PBundyra) — 2024-06-10T17:28:44Z
```diff
@@ -4,8 +4,11 @@ metadata:
   name: "cluster-queue-2"
 spec:
   namespaceSelector: {} # match all.
-  admissionChecksStrategy:
-    admissionChecks:
-    - name: "sample-prov"           # Name of the AdmissionCheck to be run
-      onFlavors: ["default-flavor"] # This AdmissionCheck will only run for Workloads that use default-flavor
-    - name: "sample-prov-2" # This AdmissionCheck will run for all Workloads regardless of a used ResourceFlavor
+  flavorFallbackStrategy:
+    rules:
```

  I've added it by mistake, sorry. Thanks for catching that

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-12T10:08:56Z

/lgtm
/approve

Thank you!
