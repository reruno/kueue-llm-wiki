# PR #237: Add metric for pending workloads, broken down by queue and cluster_queue

**Summary**: Add metric for pending workloads, broken down by queue and cluster_queue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/237

**Last updated**: 2022-05-03T20:29:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `queue-metrics` → `main`
- **Created**: 2022-04-29T16:09:43Z
- **Updated**: 2022-05-03T20:29:12Z
- **Merged**: 2022-05-03T20:29:12Z
- **Closed**: 2022-05-03T20:29:12Z
- **Labels**: `kind/feature`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 9   **+83 / -6**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Add metric pending_workloads. We only track it when there are changes in a queue. The total number of pending workloads for a cluster_queue can be obtained by aggregation.

#### Which issue(s) this PR fixes:

Part of #199 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-29T16:09:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/237#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T19:52:05Z

/hold
to prevent premature merge

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T19:55:07Z

/assign @kerthcet @ahg-g

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-29T19:55:09Z

@alculquicondor: GitHub didn't allow me to assign the following users: kerthcet.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people), repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/237#issuecomment-1113670501):

>/assign @kerthcet @ahg-g 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T21:49:36Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T22:00:28Z

/hold cancel
fixed the integration test framework and squashed.

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-30T15:04:49Z

/retest-required

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-01T11:04:29Z

Another solution for reference, counting the pending workloads in `ClusterQueueImpl`, it seems more stable. When added, `metrics.GaugeMetric.Inc()`,  when deleted, `metrics.GaugeMetric.Dec()`. E.g.
https://github.com/kerthcet/kueue/blob/9d1d1da21c139a9b6cf188f9ebe58506a12f8a67/pkg/queue/cluster_queue_impl.go#L80-L103

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-03T20:25:55Z

/lgtm

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-29T21:15:37Z

- **pkg/metrics/metrics.go:57** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T20:50:02Z
```diff
@@ -48,16 +48,24 @@ var (
 			Help:      "Latency of an admission attempt, broken down by result.",
 		}, []string{"result"},
 	)
+
+	PendingWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: subsystemName,
+			Name:      "pending_workloads",
+			Help:      "Number of pending workloads, per queue and cluster_queue.",
+		}, []string{"queue", "cluster_queue"})
```

  nit: place cluster_queue first because the relation is `cq -- 1:n -- q`

- **pkg/queue/manager.go:181** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T21:07:20Z
```diff
@@ -178,6 +178,7 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 	if cq != nil && cq.AddFromQueue(qImpl) {
 		m.cond.Broadcast()
 	}
+	qImpl.reportPendingWorkloads()
```

  we will eventually get events for the workloads, doesn't look like this is necessary.

- **pkg/queue/manager.go:331** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T21:14:14Z
```diff
@@ -325,6 +331,7 @@ func (m *Manager) deleteWorkloadFromQueueAndClusterQueue(w *kueue.Workload, qKey
 		return
```

  unrelated to the PR, but can you unroll this function inside `DeleteWorkload`, seems unnecessary.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-29T21:43:04Z

- **pkg/metrics/metrics.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T21:39:00Z
```diff
@@ -48,16 +48,24 @@ var (
 			Help:      "Latency of an admission attempt, broken down by result.",
 		}, []string{"result"},
 	)
+
+	PendingWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: subsystemName,
+			Name:      "pending_workloads",
+			Help:      "Number of pending workloads, per queue and cluster_queue.",
+		}, []string{"queue", "cluster_queue"})
```

  Done

- **pkg/queue/manager.go:181** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T21:39:32Z
```diff
@@ -178,6 +178,7 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 	if cq != nil && cq.AddFromQueue(qImpl) {
 		m.cond.Broadcast()
 	}
+	qImpl.reportPendingWorkloads()
```

  what if the events for all the workloads came in before the queue?

- **pkg/queue/manager.go:331** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T21:41:24Z
```diff
@@ -325,6 +331,7 @@ func (m *Manager) deleteWorkloadFromQueueAndClusterQueue(w *kueue.Workload, qKey
 		return
```

  It's also used in UpdateWorkload

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-29T21:48:59Z

- **pkg/queue/manager.go:181** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T21:48:58Z
```diff
@@ -178,6 +178,7 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 	if cq != nil && cq.AddFromQueue(qImpl) {
 		m.cond.Broadcast()
 	}
+	qImpl.reportPendingWorkloads()
```

  sg

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-30T12:36:15Z

- **pkg/queue/queue.go:79** by [@kerthcet](https://github.com/kerthcet) — 2022-04-30T12:23:27Z
```diff
@@ -71,3 +74,11 @@ func (q *Queue) AddIfNotPresent(w *workload.Info) bool {
 	}
 	return false
 }
+
+func (q *Queue) reportPendingWorkloads() {
+	metrics.PendingWorkloads.WithLabelValues(q.ClusterQueue, q.Key).Set(float64(len(q.items)))
```

  why not using `Key(q)` directly but introduced a new field `Key` here?

- **pkg/queue/manager.go:204** by [@kerthcet](https://github.com/kerthcet) — 2022-04-30T12:27:25Z
```diff
@@ -199,6 +201,7 @@ func (m *Manager) UpdateQueue(q *kueue.Queue) error {
 		}
 	}
 	qImpl.update(q)
+	qImpl.reportPendingWorkloads()
```

  How about wrap with `defer`, difference here is when return `errQueueDoesNotExist` error, we will still run `reportPendingWorkloads()`. (Similar to other places)

- **pkg/metrics/metrics.go:69** by [@kerthcet](https://github.com/kerthcet) — 2022-04-30T12:35:44Z
```diff
@@ -48,16 +48,24 @@ var (
 			Help:      "Latency of an admission attempt, broken down by result.",
 		}, []string{"result"},
 	)
+
+	PendingWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: subsystemName,
+			Name:      "pending_workloads",
+			Help:      "Number of pending workloads, per queue and cluster_queue.",
+		}, []string{"cluster_queue", "queue"})
 )
 
 func AdmissionAttempt(result AdmissionResult, duration time.Duration) {
 	admissionAttempts.WithLabelValues(string(result)).Inc()
 	admissionAttemptLatency.WithLabelValues(string(result)).Observe(duration.Seconds())
 }
 
-func init() {
+func Register() {
 	metrics.Registry.MustRegister(
 		admissionAttempts,
 		admissionAttemptLatency,
+		PendingWorkloads,
```

  What about lowcase `PendingWorkloads` to `pendingWorkloads`  and move  `reportPendingWorkloads` to this package and pass in the necessary parameters.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-30T15:04:13Z

- **pkg/queue/manager.go:325** by [@denkensk](https://github.com/denkensk) — 2022-04-30T14:55:44Z
```diff
@@ -315,6 +321,7 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 
 func (m *Manager) DeleteWorkload(w *kueue.Workload) {
 	m.Lock()
+	defer m.Unlock()
 	m.deleteWorkloadFromQueueAndClusterQueue(w, queueKeyForWorkload(w))
 	m.Unlock()
```

  rm m.Unlock in L326

- **pkg/queue/queue.go:79** by [@denkensk](https://github.com/denkensk) — 2022-04-30T15:01:31Z
```diff
@@ -71,3 +74,11 @@ func (q *Queue) AddIfNotPresent(w *workload.Info) bool {
 	}
 	return false
 }
+
+func (q *Queue) reportPendingWorkloads() {
+	metrics.PendingWorkloads.WithLabelValues(q.ClusterQueue, q.Key).Set(float64(len(q.items)))
```

  Maybe can't. 
  We don't have the object `*kueue.Queue` in the struct `Queue`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T18:51:40Z

Rebased

> Another solution for reference, counting the pending workloads in ClusterQueueImpl, it seems more stable

What makes you say that it would be more stable?

The source-of-truth for how many elements are in the queue is the queue implementation. Are you thinking of race conditions where 2 routines try to add/remove elements at the same time? That's not a problem because the operations take a lock.

- **pkg/metrics/metrics.go:69** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T17:46:03Z
```diff
@@ -48,16 +48,24 @@ var (
 			Help:      "Latency of an admission attempt, broken down by result.",
 		}, []string{"result"},
 	)
+
+	PendingWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: subsystemName,
+			Name:      "pending_workloads",
+			Help:      "Number of pending workloads, per queue and cluster_queue.",
+		}, []string{"cluster_queue", "queue"})
 )
 
 func AdmissionAttempt(result AdmissionResult, duration time.Duration) {
 	admissionAttempts.WithLabelValues(string(result)).Inc()
 	admissionAttemptLatency.WithLabelValues(string(result)).Observe(duration.Seconds())
 }
 
-func init() {
+func Register() {
 	metrics.Registry.MustRegister(
 		admissionAttempts,
 		admissionAttemptLatency,
+		PendingWorkloads,
```

  The necessary parameters are the cluster_queue and queue keys, which are the same parameters that `WithValues` requires. Then we still need a `reportPendingWorkloads` to get a key for the queue. So we would need to functions. I think this is clean enough.

- **pkg/queue/manager.go:204** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T17:48:09Z
```diff
@@ -199,6 +201,7 @@ func (m *Manager) UpdateQueue(q *kueue.Queue) error {
 		}
 	}
 	qImpl.update(q)
+	qImpl.reportPendingWorkloads()
```

  We don't want to report when the queue doesn't exist.

- **pkg/queue/manager.go:325** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T17:49:09Z
```diff
@@ -315,6 +321,7 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 
 func (m *Manager) DeleteWorkload(w *kueue.Workload) {
 	m.Lock()
+	defer m.Unlock()
 	m.deleteWorkloadFromQueueAndClusterQueue(w, queueKeyForWorkload(w))
 	m.Unlock()
```

  Oops, I'll just revert this.

- **pkg/queue/queue.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T17:49:40Z
```diff
@@ -71,3 +74,11 @@ func (q *Queue) AddIfNotPresent(w *workload.Info) bool {
 	}
 	return false
 }
+
+func (q *Queue) reportPendingWorkloads() {
+	metrics.PendingWorkloads.WithLabelValues(q.ClusterQueue, q.Key).Set(float64(len(q.items)))
```

  Exactly, we don't store the object.
