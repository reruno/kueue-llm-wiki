# PR #2376: Add event for eviction on the workload.

**Summary**: Add event for eviction on the workload.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2376

**Last updated**: 2024-06-11T14:52:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/event-for-eviction-on-the-workload` → `main`
- **Created**: 2024-06-07T07:09:34Z
- **Updated**: 2024-06-11T14:52:11Z
- **Merged**: 2024-06-11T14:41:14Z
- **Closed**: 2024-06-11T14:41:15Z
- **Labels**: `kind/bug`, `kind/feature`, `size/L`, `lgtm`, `release-note-action-required`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 3   **+84 / -18**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add event for eviction on the workload.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2012

#### Special notes for your reviewer:
```
Events:
  Type    Reason                        Age                  From                       Message
  ----    ------                        ----                 ----                       -------
  Normal  Pending                       3m5s (x5 over 3m5s)  kueue-admission            couldn't assign flavors to pod set main: flavor spot doesn't match node affinity, insufficient unused quota for cpu in flavor on-demand, 1 more needed. Pending the preemption of 1 workload(s)
  Normal  QuotaReserved                 3m5s                 kueue-admission            Quota reserved in ClusterQueue cluster-queue, wait time since queued was 1s
  Normal  EvictedDueToPodsReadyTimeout  2m5s                 kueue-workload-controller  Exceeded the PodsReady timeout e2e-gsj4m/job-high-67543
  Normal  Admitted                      65s (x2 over 3m5s)   kueue-admission            Admitted by ClusterQueue cluster-queue, wait time since reservation was 0s
  Normal  QuotaReserved                 65s                  kueue-admission            Quota reserved in ClusterQueue cluster-queue, wait time since queued was 0s
  Normal  EvictedDueToInactiveWorkload  5s                   kueue-workload-controller  The workload is deactivated due to exceeding the maximum number of re-queuing retries
```

Preempted event is not renamed to keep backwards compatibility.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add the following events for eviction on the workload indicating the reason for eviction:
- "EvictedDueToPodsReadyTimeout"
- "EvictedDueToAdmissionCheck"
- "EvictedDueToClusterQueueStopped"
- "EvictedDueToInactiveWorkload" (renamed from InactiveWorkload)

ACTION REQUIRED: If you were watching for the typed Normal event with `InactiveWorkload` reason, use `EvictedDueToInactiveWorkload` reason one instead.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-07T07:09:37Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-07T07:09:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | fc0ebb10636a785a7b44184643c8a957987281a6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66685e5221478200086750fc |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-10T08:49:06Z

LGTM label has been added.  <details>Git tree hash: fae18dbc5cfc8812047b3cf43e09d8fe1e13ba40</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T18:06:36Z

/test pull-kueue-test-e2e-main-1-27

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-11T07:46:29Z

/release-note-edit
```release-note
Add the following events for eviction on the workload indicating the reason for eviction:
- "EvictedDueToPodsReadyTimeout"
- "EvictedDueToAdmissionCheck"
- "EvictedDueToClusterQueueStopped"
- "EvictedDueToInactiveWorkload" (renamed from InactiveWorkload)
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-11T07:48:09Z

@mbobrovskyi please also indicate in the "Special notes for your reviewer:" indicate that Preempted is not renamed to keep backwards compatibility.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-11T07:48:54Z

/lgtm
/assign @tenzen-y 
For another pair of eyes.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T07:48:59Z

LGTM label has been added.  <details>Git tree hash: 012c258a649aa6b8a9d4d7eee40b5514439c3189</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T12:52:54Z

> /lgtm /assign @tenzen-y For another pair of eyes.

ACK

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T14:05:02Z

/release-note-edit
```release-note
Add the following events for eviction on the workload indicating the reason for eviction:
- "EvictedDueToPodsReadyTimeout"
- "EvictedDueToAdmissionCheck"
- "EvictedDueToClusterQueueStopped"
- "EvictedDueToInactiveWorkload" (renamed from InactiveWorkload)

ACTION REQUIRED: If you were watching for the typed Normal event with `InactiveWorkload` reason, use `EvictedDueToInactiveWorkload` reason one instead.
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T14:28:18Z

LGTM label has been added.  <details>Git tree hash: 4070abc9f8b7cfbc81f1e4ef20c92d807c74d785</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T14:28:22Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2376#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2376#pullrequestreview-2107121268" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2376#pullrequestreview-2110642854" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T14:28:22Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T14:35:23Z

@mimowo I'm wondering if this is not a bug and this is a new feature.
WDYT?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-11T14:51:15Z

> @mimowo I'm wondering if this is not a bug and this is a new feature.
> WDYT?

Yeah, I think it is on the edge, but probably closer to a feature.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-11T14:52:08Z

/kind feature

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T07:39:50Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mimowo](https://github.com/mimowo) — 2024-06-07T07:39:50Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  I one-liner fixes :). 
  
  It would be great to cover it with some unit or integration tests. IIRC we already have some unit tests for events, maybe we can employ the same way.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T07:48:46Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mimowo](https://github.com/mimowo) — 2024-06-07T07:48:46Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  The issue description https://github.com/kubernetes-sigs/kueue/issues/2012 indicates we are already getting an event Preempted for preemptions. So, I guess we will not have: Evicted and Preempted, which is a little redundant, but maybe ok. 
  
  @mbobrovskyi please check what happens in case of preemptions and post in the PR description as you did for the PodsReady timeouts.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T08:15:06Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mimowo](https://github.com/mimowo) — 2024-06-07T08:15:06Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  After further analysis it seems to be the case, I can see the options:
  1. Introduce generic Evicted event (as currently), along with the existing Preempted event
  2. Send the scenario-specific event (as currently for preemptions)
  3. do 1 and 2
  
  Maybe for now we can start with 2, just to make the handling symmetric. We could wrap sending the event and bumping the metric into one function `ReportEvictedWorkload`: https://github.com/kubernetes-sigs/kueue/blob/baefcc433a1f9fd844f91a6c12327f163838c7f6/pkg/scheduler/preemption/preemption.go#L190-L191. 
  
  Any view @alculquicondor @tenzen-y ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-07T08:18:39Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-07T08:18:39Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  Thanks

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-07T10:34:55Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-07T10:34:55Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  > @mbobrovskyi please check what happens in case of preemptions and post in the PR description as you did for the PodsReady timeouts.
  
  With current implementation we have:
  ```bash
  Events:
    Type    Reason         Age                From                        Message
    ----    ------         ----               ----                        -------
    Normal  QuotaReserved  41s                kueue-admission             Quota reserved in ClusterQueue cluster-queue, wait time since queued was 1s
    Normal  Admitted       40s (x2 over 41s)  kueue-admission             Admitted by ClusterQueue cluster-queue, wait time since reservation was 0s
    Normal  Preempted      40s                kueue-admission             Preempted to accommodate a workload (UID: b33cd52f-3973-404a-ad4e-7395dd4aa06a) in the ClusterQueue
    Normal  Evicted        40s                batch/job-kueue-controller  Preempted to accommodate a workload (UID: b33cd52f-3973-404a-ad4e-7395dd4aa06a) in the ClusterQueue
    Normal  QuotaReserved  40s                kueue-admission             Quota reserved in ClusterQueue cluster-queue, wait time since queued was 0s
  ```
  
  Also added to description.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-07T14:28:54Z

- **pkg/controller/jobframework/reconciler.go:440** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-07T14:28:54Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  Next to all calls to `metrics.ReportEvictedWorkloads` makes sense, so that we can be more specific.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-07T14:30:52Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mimowo](https://github.com/mimowo) — 2024-06-07T14:30:52Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  Thanks for the update.
  
  While I think both generic "Evicted" event, and scenario-specific might be useful, since we already have the scenario-specific Preempted, I think adding analogous events for other scenarios is enough, and would satisfy  the original motivation for the issue.
  
  Maybe we can add the generic one later, if we have an ask from a user. OTOH, adding the generic one is one-liner so I'm fine to keep it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-07T14:55:17Z

- **pkg/controller/jobframework/reconciler.go:440** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-07T14:55:17Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  TBH, both approaches ( 1 and 2 ) should work fine. But if we introduce the generic Eviction events, we need to take care of the order of events (Preemppted -> Evicted vs Evicted -> Preempted ).
  
  Anyway, events existing would be worth it rather than no events.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T07:12:09Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mimowo](https://github.com/mimowo) — 2024-06-10T07:12:09Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  So, I'm thinking maybe we could reuse the naming pattern `EvictedDueTo...`, as we already do [here](https://github.com/kubernetes-sigs/kueue/blob/a7dcb6da5572bcbb403485051095f1ee8ace56ec/pkg/controller/jobframework/reconciler.go#L837). It would be both indicating that the workload is evicted, and be specific. 
  
  Renaming `Preempted` to `EvictedDueToPreempted` might be a downside, but seems acceptable. WDYT @alculquicondor @tenzen-y ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T08:34:30Z

- **pkg/workload/workload.go:693** by [@mimowo](https://github.com/mimowo) — 2024-06-10T08:34:30Z
```diff
@@ -687,3 +689,12 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 	}
 	return acNames
 }
+
+func EvictedEventReason(reason string) string {
```

  Exporting this function just for tests seems unnecessary. I would suggest to inline this function.
  
  Actually, seeing "EvictedDueTo...` will make the tests more readable.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-10T08:45:00Z

- **pkg/workload/workload.go:693** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T08:45:00Z
```diff
@@ -687,3 +689,12 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 	}
 	return acNames
 }
+
+func EvictedEventReason(reason string) string {
```

  Thanks. Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T08:49:00Z

/lgtm
/approve
/hold
for the input from @alculquicondor and @tenzen-y as this change renames the existing event.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-10T14:31:20Z

- **pkg/controller/jobframework/reconciler.go:440** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-10T14:31:20Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  because the `Preempted` event has been out there for a while, I would be worried about changing the constant.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T15:06:18Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mimowo](https://github.com/mimowo) — 2024-06-10T15:06:18Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  That's right, since 0.3.0. In that case, we could keep `Preempted` for the preemption scenario, and
  1. add `EvictedDueTo` events for all scenarios
  2. add `EvictedDueTo` events for all other scenarios
  
  Maybe with (1.) we could gradually deprecate and remove "Preempted", for example, when moving to v1?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-10T16:01:59Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T16:01:59Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  > add EvictedDueTo events for all other scenarios
  
  To do not duplicate events, I think it's enough.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-10T16:02:34Z

- **pkg/controller/jobframework/reconciler.go:440** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T16:02:34Z
```diff
@@ -437,6 +437,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
 				}
 				_ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
+				r.record.Event(wl, corev1.EventTypeNormal, kueue.WorkloadEvicted, evCond.Message)
```

  @alculquicondor WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T16:19:40Z

- **pkg/controller/core/workload_controller.go:537** by [@mimowo](https://github.com/mimowo) — 2024-06-10T16:19:40Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  Maybe we don't delete this event. It's been already released. It still has some information for marking `active=false`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T16:22:03Z

- **pkg/controller/core/workload_controller_test.go:516** by [@mimowo](https://github.com/mimowo) — 2024-06-10T16:22:02Z
```diff
@@ -509,6 +509,14 @@ func TestReconcile(t *testing.T) {
 					Message: "At least one admission check is false",
 				}).
 				Obj(),
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
+					EventType: corev1.EventTypeNormal,
+					Reason:    kueue.WorkloadEvicted + "DueTo" + kueue.WorkloadEvictedByAdmissionCheck,
```

  non-blocking nit, I think the asserts by string, as here: https://github.com/kubernetes-sigs/kueue/blob/fd82aad2350ad5e0f29e906441d9410860945899/pkg/controller/jobs/pod/pod_controller_test.go#L319 are more readable, when reading tests.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-10T16:25:39Z

- **pkg/controller/core/workload_controller.go:537** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T16:25:39Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  Yes, but we have it on [here](https://github.com/kubernetes-sigs/kueue/blob/1e874b28b3cefc517c9f7dd799c56863c9c79687/pkg/controller/core/workload_controller.go#L210).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T16:30:24Z

- **pkg/controller/core/workload_controller.go:537** by [@mimowo](https://github.com/mimowo) — 2024-06-10T16:30:24Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  Yeah, but there are two differences: 1. it is renamed, 2. one is added after `active:false`, the other after `Evicted: true`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-10T16:36:32Z

- **pkg/controller/core/workload_controller.go:537** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T16:36:32Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  So you propose to move [this](https://github.com/kubernetes-sigs/kueue/blob/1e874b28b3cefc517c9f7dd799c56863c9c79687/pkg/controller/core/workload_controller.go#L210-L211) logic to L540?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-10T16:46:11Z

- **pkg/controller/core/workload_controller.go:537** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T16:46:10Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  I'm not sure, why we separated [event](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/workload_controller.go#L537) and [metric](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/workload_controller.go#L211) updates.?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-10T16:47:19Z

- **pkg/controller/core/workload_controller_test.go:516** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-10T16:47:19Z
```diff
@@ -509,6 +509,14 @@ func TestReconcile(t *testing.T) {
 					Message: "At least one admission check is false",
 				}).
 				Obj(),
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
+					EventType: corev1.EventTypeNormal,
+					Reason:    kueue.WorkloadEvicted + "DueTo" + kueue.WorkloadEvictedByAdmissionCheck,
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-10T17:03:27Z

- **pkg/controller/core/workload_controller.go:537** by [@mimowo](https://github.com/mimowo) — 2024-06-10T17:03:27Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  > So you propose to move [this](https://github.com/kubernetes-sigs/kueue/blob/1e874b28b3cefc517c9f7dd799c56863c9c79687/pkg/controller/core/workload_controller.go#L210-L211) logic to L540?
  
  I was proposing to keep it as is (just for backwards-compatibility with 0.7). I'm also ok to drop it (as you did). WDYT @alculquicondor ?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-10T17:55:13Z

- **pkg/controller/core/workload_controller.go:537** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-10T17:55:13Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  I like this implementation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-11T07:37:00Z

- **pkg/controller/core/workload_controller.go:537** by [@mimowo](https://github.com/mimowo) — 2024-06-11T07:37:00Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  ack

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-11T13:57:23Z

Basically lgtm

- **pkg/workload/workload.go:693** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T13:49:18Z
```diff
@@ -687,3 +689,8 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 	}
 	return acNames
 }
+
+func ReportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cqName string, reason string, message string) {
```

  ```suggestion
  func ReportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cqName, reason, message string) {
  ```
  
  Nit. All are typed string.

- **pkg/controller/core/workload_controller_test.go:598** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T13:56:49Z
```diff
@@ -590,12 +606,6 @@ func TestReconcile(t *testing.T) {
 				}).
 				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(1*time.Second).Truncate(time.Second)))).
 				Obj(),
-			wantEvents: []utiltesting.EventRecord{{
-				Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
-				EventType: v1.EventTypeNormal,
-				Reason:    kueue.WorkloadEvictedByDeactivation,
-				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
-			}},
```

  What is the reason that the workload doesn't have this event?
  This workload should have the "EvictedDueToInactiveWorkload", right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-11T14:19:05Z

- **pkg/controller/core/workload_controller_test.go:598** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T14:19:05Z
```diff
@@ -590,12 +606,6 @@ func TestReconcile(t *testing.T) {
 				}).
 				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(1*time.Second).Truncate(time.Second)))).
 				Obj(),
-			wantEvents: []utiltesting.EventRecord{{
-				Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
-				EventType: v1.EventTypeNormal,
-				Reason:    kueue.WorkloadEvictedByDeactivation,
-				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
-			}},
```

  Yes, we moved it to https://github.com/kubernetes-sigs/kueue/blob/4a5c0612ff80dc0595364dcfadddb1b0e8a85637/pkg/controller/core/workload_controller.go#L210-L211. So on this test case we can't get this event.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-11T14:24:22Z

- **pkg/controller/core/workload_controller_test.go:598** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T14:24:21Z
```diff
@@ -590,12 +606,6 @@ func TestReconcile(t *testing.T) {
 				}).
 				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(1*time.Second).Truncate(time.Second)))).
 				Obj(),
-			wantEvents: []utiltesting.EventRecord{{
-				Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
-				EventType: v1.EventTypeNormal,
-				Reason:    kueue.WorkloadEvictedByDeactivation,
-				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
-			}},
```

  Sorry? I couldn't understand the reason why this workload doesn't have the event something like:
  
  ```YAML
  {
  	Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
  	EventType: corev1.EventTypeNormal,
  	Reason:    "EvictedDueToInactiveWorkload",
  	Message:   "The workload is deactivated",
  },
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-11T14:26:15Z

- **pkg/workload/workload.go:693** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T14:26:15Z
```diff
@@ -687,3 +689,8 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 	}
 	return acNames
 }
+
+func ReportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cqName string, reason string, message string) {
```

  Fixed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-11T14:26:53Z

- **pkg/controller/core/workload_controller.go:537** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T14:26:43Z
```diff
@@ -534,8 +537,6 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		if err := r.client.Update(ctx, wl); err != nil {
 			return false, err
 		}
-		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  Ah, I see the reason why the workload doesn't have the event.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-11T14:27:09Z

- **pkg/controller/core/workload_controller_test.go:598** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T14:27:09Z
```diff
@@ -590,12 +606,6 @@ func TestReconcile(t *testing.T) {
 				}).
 				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(1*time.Second).Truncate(time.Second)))).
 				Obj(),
-			wantEvents: []utiltesting.EventRecord{{
-				Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
-				EventType: v1.EventTypeNormal,
-				Reason:    kueue.WorkloadEvictedByDeactivation,
-				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
-			}},
```

  NVM
  https://github.com/kubernetes-sigs/kueue/pull/2376#discussion_r1634990476

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-11T14:28:14Z

Thank you!
/lgtm
/approve

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-11T14:31:21Z

- **pkg/controller/core/workload_controller_test.go:598** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T14:31:21Z
```diff
@@ -590,12 +606,6 @@ func TestReconcile(t *testing.T) {
 				}).
 				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(1*time.Second).Truncate(time.Second)))).
 				Obj(),
-			wantEvents: []utiltesting.EventRecord{{
-				Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
-				EventType: v1.EventTypeNormal,
-				Reason:    kueue.WorkloadEvictedByDeactivation,
-				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
-			}},
```

  This workload should have event on the next reconcile.
