# PR #2395: [kueuectl] Fix "should print workloads list with paging" test

**Summary**: [kueuectl] Fix "should print workloads list with paging" test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2395

**Last updated**: 2024-06-12T03:58:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/should-print-workloads-list-with-paging-test` → `main`
- **Created**: 2024-06-11T06:31:51Z
- **Updated**: 2024-06-12T03:58:38Z
- **Merged**: 2024-06-11T15:28:16Z
- **Closed**: 2024-06-11T15:28:16Z
- **Labels**: `lgtm`, `kind/flake`, `release-note-none`, `approved`, `ok-to-test`, `size/XS`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 1   **+1 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind flake

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
To fix "Should print workloads list with paging" test.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2353

#### Special notes for your reviewer:
The order of workloads in the table is wl3, wl1, wl2, which is different that the order of workloads in the expectations. Since the creation timestamps are generated by API server they may differ, so we need to use the same order.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T06:31:53Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-11T06:32:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ccfadf0a87dbb2043afb0dc6db4c862b8fe77cec |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6668595da183210007508874 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T06:34:07Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T06:54:21Z

Integration passed 1/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T07:05:44Z

Integration passed 2/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T07:27:13Z

Integration passed 3/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T07:41:51Z

Integration passed 4/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T08:00:56Z

Integration passed 5/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T08:12:17Z

/assign @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T14:07:41Z

LGTM label has been added.  <details>Git tree hash: 4bbd4949f53750f49030649c5ababa3ad4f4f3b4</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-11T14:21:36Z

@mbobrovskyi could you please explain why the change fixes the issue, and what is the problem?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-11T15:07:27Z

/lgtm
/approve
Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T15:07:36Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2395#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2395#issuecomment-2161004159" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-11T08:24:14Z

- **test/integration/kueuectl/list_test.go:72** by [@mimowo](https://github.com/mimowo) — 2024-06-11T08:24:14Z
```diff
@@ -69,48 +69,50 @@ var _ = ginkgo.Describe("Kueuectl List", ginkgo.Ordered, ginkgo.ContinueOnFailur
 
 		// Simple client set that are using on unit tests not allow to filter by field selector.
 		ginkgo.It("Should print local queues list filtered by field selector", func() {
-			streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
-			configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
-			executeTime := time.Now()
-			kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
-
-			kueuectl.SetArgs([]string{"list", "localqueue", "--field-selector",
-				fmt.Sprintf("metadata.name=%s", lq1.Name), "--namespace", ns.Name})
+			gomega.Eventually(func(g gomega.Gomega) {
```

  Why is Eventually needed since we use a fake clock? Is it that some places in the code still use real clock?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-11T08:37:24Z

- **test/integration/kueuectl/list_test.go:224** by [@mimowo](https://github.com/mimowo) — 2024-06-11T08:37:23Z
```diff
@@ -194,45 +200,49 @@ very-long-cluster-queue-name            0                   0
 
 			// Simple client set that are using on unit tests not allow to filter by field selector.
 			ginkgo.It("Should print workloads list filtered by field selector", func() {
-				streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
-				configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
-				executeTime := time.Now()
-				kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
-
-				kueuectl.SetArgs([]string{"list", "workload", "--field-selector",
-					fmt.Sprintf("metadata.name=%s", wl1.Name), "--namespace", ns.Name})
-				err := kueuectl.Execute()
-
-				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-				gomega.Expect(errOutput.String()).Should(gomega.BeEmpty())
-				gomega.Expect(output.String()).Should(gomega.Equal(fmt.Sprintf(`NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+				gomega.Eventually(func(g gomega.Gomega) {
+					streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
+					configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
+					executeTime := time.Now()
+					kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
+
+					kueuectl.SetArgs([]string{"list", "workload", "--field-selector",
+						fmt.Sprintf("metadata.name=%s", wl1.Name), "--namespace", ns.Name})
+					err := kueuectl.Execute()
+
+					g.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+					g.Expect(errOutput.String()).Should(gomega.BeEmpty())
+					g.Expect(output.String()).Should(gomega.Equal(fmt.Sprintf(`NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
 wl1                          lq1                         PENDING                       %s
 `,
-					duration.HumanDuration(executeTime.Sub(wl1.CreationTimestamp.Time)))))
+						duration.HumanDuration(executeTime.Sub(wl1.CreationTimestamp.Time)))))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			// Simple client set that are using on unit tests not allow paging.
 			ginkgo.It("Should print workloads list with paging", func() {
-				streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
-				configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
-				executeTime := time.Now()
-				kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
-
-				os.Setenv(list.KueuectlListRequestLimitEnvName, "1")
-				kueuectl.SetArgs([]string{"list", "workload", "--namespace", ns.Name})
-				err := kueuectl.Execute()
-
-				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-				gomega.Expect(errOutput.String()).Should(gomega.BeEmpty())
-				gomega.Expect(output.String()).Should(gomega.Equal(fmt.Sprintf(`NAME                      JOB TYPE   JOB NAME   LOCALQUEUE                   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+				gomega.Eventually(func(g gomega.Gomega) {
```

  This test failed here: https://github.com/kubernetes-sigs/kueue/issues/2353#issuecomment-2158668997 due to time shift, which suggests still some parts of the code use the real clock. 
  
  Could you explain why the test failed, then we can think if wrapping with Eventually is the solution we want? 
  
  Maybe we could make sure all parts of the code use the same fake clock consistently instead.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-11T09:00:29Z

- **test/integration/kueuectl/list_test.go:72** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T09:00:29Z
```diff
@@ -69,48 +69,50 @@ var _ = ginkgo.Describe("Kueuectl List", ginkgo.Ordered, ginkgo.ContinueOnFailur
 
 		// Simple client set that are using on unit tests not allow to filter by field selector.
 		ginkgo.It("Should print local queues list filtered by field selector", func() {
-			streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
-			configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
-			executeTime := time.Now()
-			kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
-
-			kueuectl.SetArgs([]string{"list", "localqueue", "--field-selector",
-				fmt.Sprintf("metadata.name=%s", lq1.Name), "--namespace", ns.Name})
+			gomega.Eventually(func(g gomega.Gomega) {
```

  Added eventually to fix [this](https://github.com/kubernetes-sigs/kueue/issues/2353#issuecomment-2159897688) problem. In another cases you right it's not need. So reverted this changes.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-11T14:05:21Z

- **test/integration/kueuectl/list_test.go:72** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T14:05:21Z
```diff
@@ -69,48 +69,50 @@ var _ = ginkgo.Describe("Kueuectl List", ginkgo.Ordered, ginkgo.ContinueOnFailur
 
 		// Simple client set that are using on unit tests not allow to filter by field selector.
 		ginkgo.It("Should print local queues list filtered by field selector", func() {
-			streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
-			configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
-			executeTime := time.Now()
-			kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
-
-			kueuectl.SetArgs([]string{"list", "localqueue", "--field-selector",
-				fmt.Sprintf("metadata.name=%s", lq1.Name), "--namespace", ns.Name})
+			gomega.Eventually(func(g gomega.Gomega) {
```

  Moved this fix to separate PR #2399

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-11T14:06:14Z

- **test/integration/kueuectl/list_test.go:224** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T14:06:14Z
```diff
@@ -194,45 +200,49 @@ very-long-cluster-queue-name            0                   0
 
 			// Simple client set that are using on unit tests not allow to filter by field selector.
 			ginkgo.It("Should print workloads list filtered by field selector", func() {
-				streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
-				configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
-				executeTime := time.Now()
-				kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
-
-				kueuectl.SetArgs([]string{"list", "workload", "--field-selector",
-					fmt.Sprintf("metadata.name=%s", wl1.Name), "--namespace", ns.Name})
-				err := kueuectl.Execute()
-
-				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-				gomega.Expect(errOutput.String()).Should(gomega.BeEmpty())
-				gomega.Expect(output.String()).Should(gomega.Equal(fmt.Sprintf(`NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+				gomega.Eventually(func(g gomega.Gomega) {
+					streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
+					configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
+					executeTime := time.Now()
+					kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
+
+					kueuectl.SetArgs([]string{"list", "workload", "--field-selector",
+						fmt.Sprintf("metadata.name=%s", wl1.Name), "--namespace", ns.Name})
+					err := kueuectl.Execute()
+
+					g.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+					g.Expect(errOutput.String()).Should(gomega.BeEmpty())
+					g.Expect(output.String()).Should(gomega.Equal(fmt.Sprintf(`NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
 wl1                          lq1                         PENDING                       %s
 `,
-					duration.HumanDuration(executeTime.Sub(wl1.CreationTimestamp.Time)))))
+						duration.HumanDuration(executeTime.Sub(wl1.CreationTimestamp.Time)))))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			// Simple client set that are using on unit tests not allow paging.
 			ginkgo.It("Should print workloads list with paging", func() {
-				streams, _, output, errOutput := genericiooptions.NewTestIOStreams()
-				configFlags := CreateConfigFlagsWithRestConfig(cfg, streams)
-				executeTime := time.Now()
-				kueuectl := app.NewKueuectlCmd(app.KueuectlOptions{ConfigFlags: configFlags, IOStreams: streams, Clock: testingclock.NewFakeClock(executeTime)})
-
-				os.Setenv(list.KueuectlListRequestLimitEnvName, "1")
-				kueuectl.SetArgs([]string{"list", "workload", "--namespace", ns.Name})
-				err := kueuectl.Execute()
-
-				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-				gomega.Expect(errOutput.String()).Should(gomega.BeEmpty())
-				gomega.Expect(output.String()).Should(gomega.Equal(fmt.Sprintf(`NAME                      JOB TYPE   JOB NAME   LOCALQUEUE                   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+				gomega.Eventually(func(g gomega.Gomega) {
```

  Looks like it just mixed workload creation times.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-11T14:07:36Z

/lgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-11T14:12:22Z

- **test/integration/kueuectl/list_test.go:232** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T14:12:19Z
```diff
@@ -229,9 +229,9 @@ very-long-workload-name                         lq1
 wl1                                             lq1                                         PENDING                       %s
 wl2                                             very-long-local-queue-name                  PENDING                       %s
 `,
+					duration.HumanDuration(executeTime.Sub(wl3.CreationTimestamp.Time)),
```

  I don't think that this should resolve the root cause since the metav1 stores the only second level time, right?
  So, could you add a sleep for every workload creation?
  
  ```shell
   lq1 = testing.MakeLocalQueue("lq1", ns.Name).ClusterQueue("cq1").Obj() 
   gomega.Expect(k8sClient.Create(ctx, lq1)).To(gomega.Succeed()) 
   time.Sleep(time.Second)
  ...
  ```
  
  https://github.com/kubernetes-sigs/kueue/blob/ccfadf0a87dbb2043afb0dc6db4c862b8fe77cec/test/integration/kueuectl/list_test.go#L60-L67

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-11T14:58:43Z

- **test/integration/kueuectl/list_test.go:232** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-11T14:58:43Z
```diff
@@ -229,9 +229,9 @@ very-long-workload-name                         lq1
 wl1                                             lq1                                         PENDING                       %s
 wl2                                             very-long-local-queue-name                  PENDING                       %s
 `,
+					duration.HumanDuration(executeTime.Sub(wl3.CreationTimestamp.Time)),
```

  It should resolve. The problem that It was mixed time durations and generated invalid expectation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-11T15:05:47Z

- **test/integration/kueuectl/list_test.go:232** by [@mimowo](https://github.com/mimowo) — 2024-06-11T15:05:46Z
```diff
@@ -229,9 +229,9 @@ very-long-workload-name                         lq1
 wl1                                             lq1                                         PENDING                       %s
 wl2                                             very-long-local-queue-name                  PENDING                       %s
 `,
+					duration.HumanDuration(executeTime.Sub(wl3.CreationTimestamp.Time)),
```

  Aha, I see it , the order of workloads in the table is `wl3, wl1, wl2`, which is different that the order of workloads in the expectations. Since the creation timestamps are generated by API server they may differ, so we need to use the same order.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-11T15:50:03Z

- **test/integration/kueuectl/list_test.go:232** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T15:50:03Z
```diff
@@ -229,9 +229,9 @@ very-long-workload-name                         lq1
 wl1                                             lq1                                         PENDING                       %s
 wl2                                             very-long-local-queue-name                  PENDING                       %s
 `,
+					duration.HumanDuration(executeTime.Sub(wl3.CreationTimestamp.Time)),
```

  Ah got it. Thanks.
