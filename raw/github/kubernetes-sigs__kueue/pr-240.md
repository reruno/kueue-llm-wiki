# PR #240: Add docs on how to change the configuration

**Summary**: Add docs on how to change the configuration

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/240

**Last updated**: 2022-05-03T20:33:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-selector` → `main`
- **Created**: 2022-04-30T22:11:44Z
- **Updated**: 2022-05-03T20:33:13Z
- **Merged**: 2022-05-03T20:33:12Z
- **Closed**: 2022-05-03T20:33:12Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 1   **+42 / -0**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Documents how to change the default configuration of a released version

#### Which issue(s) this PR fixes:
Part of https://github.com/kubernetes-sigs/kueue/issues/169

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-30T22:11:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/240#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-30T22:11:56Z

/hold

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-30T22:16:03Z


cc @cortespao

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-03T20:21:41Z

/label tide/merge-method-squash
/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-03T20:26:53Z

@alculquicondor: The label(s) `/label merge-method/squash` cannot be applied. These labels are supported: `api-review, tide/merge-method-merge, tide/merge-method-rebase, tide/merge-method-squash, team/katacoda, refactor`

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/240#pullrequestreview-961018188):

>/label merge-method/squash


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T20:27:15Z

/label tide/merge-method=squash

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-03T20:27:16Z

@alculquicondor: The label(s) `/label tide/merge-method=squash
` cannot be applied. These labels are supported: `api-review, tide/merge-method-merge, tide/merge-method-rebase, tide/merge-method-squash, team/katacoda, refactor`

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/240#issuecomment-1116565485):

>/label tide/merge-method=squash
>
>/lgtm


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T20:27:29Z

/label tide/merge-method-squash

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T13:43:10Z

- **docs/setup/install.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T13:41:28Z
```diff
@@ -17,6 +17,27 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+First, download the release's `manifests.yaml` file:
```

  ```suggestion
  1. Download the release's `manifests.yaml` file using the following command:
  ```

- **docs/setup/install.md:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T13:42:56Z
```diff
@@ -17,6 +17,27 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+First, download the release's `manifests.yaml` file:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+
+Second, with your favourite editor open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
```

  Probably worth an example?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-02T19:56:36Z

- **docs/setup/install.md:32** by [@ahg-g](https://github.com/ahg-g) — 2022-05-02T19:56:36Z
```diff
@@ -17,6 +17,27 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+First, download the release's `manifests.yaml` file:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+
+Second, with your favourite editor open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
```

  should we add that in a separate config.md file?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-02T19:57:08Z

- **docs/setup/install.md:32** by [@ahg-g](https://github.com/ahg-g) — 2022-05-02T19:57:08Z
```diff
@@ -17,6 +17,27 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+First, download the release's `manifests.yaml` file:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+
+Second, with your favourite editor open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
```

  similar to https://kubernetes.io/docs/reference/scheduling/config/

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T20:16:00Z

- **docs/setup/install.md:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T20:12:23Z
```diff
@@ -17,6 +17,27 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+First, download the release's `manifests.yaml` file:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+
+Second, with your favourite editor open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
```

  That would make sense as we add more configuration complexity in the future. But for now, I'm just not sure the way you are referring to the data entry is clear enough. So an example would clarify what users should look for.
  
  Maybe there could be a phrase such as:
  
  ```
  The contents of the ConfigMap are similar to the following:
  ```

- **docs/setup/install.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T20:13:10Z
```diff
@@ -17,6 +17,26 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
+the default Kueue Configuration struct ([v1alpha1@v0.1.0](https://pkg.go.dev/sigs.k8s.io/kueue@v0.1.0/apis/config/v1alpha1#Configuration)).
+
+3. Apply the customized manifests to the cluster:
```

  ```suggestion
  3. To apply the customized manifests to the cluster, run the following command:
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-02T20:35:00Z

- **docs/setup/install.md:32** by [@ahg-g](https://github.com/ahg-g) — 2022-05-02T20:35:00Z
```diff
@@ -17,6 +17,27 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+First, download the release's `manifests.yaml` file:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+
+Second, with your favourite editor open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
```

  > That would make sense as we add more configuration complexity in the future
  
  My thinking is that we need to emphasize the importance of the `manageJobsWithoutQueueName` flag. May be we should just leave it to the API docs?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T21:23:22Z

- **docs/setup/install.md:36** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T21:23:22Z
```diff
@@ -18,6 +18,38 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
+the default Kueue Configuration 
+struct ([v1alpha1@v0.1.0](https://pkg.go.dev/sigs.k8s.io/kueue@v0.1.0/apis/config/v1alpha1#Configuration)).
+The contents of the ConfigMap are similar to the following:
```

  These are the contents of a field of the ConfigMap, not the ConfigMap :)
  
  I think we can just document the ConfigMap here, and leave the contents of the Configuration to a separate config doc.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-02T22:26:42Z

- **docs/setup/install.md:36** by [@ahg-g](https://github.com/ahg-g) — 2022-05-02T22:26:41Z
```diff
@@ -18,6 +18,38 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
+the default Kueue Configuration 
+struct ([v1alpha1@v0.1.0](https://pkg.go.dev/sigs.k8s.io/kueue@v0.1.0/apis/config/v1alpha1#Configuration)).
+The contents of the ConfigMap are similar to the following:
```

  ok, but we still need to put a sample configuration, see how it looks now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T22:29:25Z

Looks good. I'll leave the rest to @cortespao

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-05-03T02:05:28Z

Small suggestions

- **docs/setup/install.md:21** by [@cortespao](https://github.com/cortespao) — 2022-05-03T01:56:21Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
```

  Could it be "costum released version"?

- **docs/setup/install.md:25** by [@cortespao](https://github.com/cortespao) — 2022-05-03T01:57:08Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
```

  ```suggestion
  1. Download the release's `manifests.yaml` file:
  ```

- **docs/setup/install.md:31** by [@cortespao](https://github.com/cortespao) — 2022-05-03T02:00:14Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
```

  ```suggestion
  2. With an editor of your preference, open `manifests.yaml`.
  3. In the `kueue-manager-config` 
  ```

- **docs/setup/install.md:56** by [@cortespao](https://github.com/cortespao) — 2022-05-03T02:02:41Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
+the default Kueue Configuration 
+struct ([v1alpha1@v0.1.0](https://pkg.go.dev/sigs.k8s.io/kueue@v0.1.0/apis/config/v1alpha1#Configuration)).
+The contents of the ConfigMap are similar to the following:
+
+```yaml
+apiVersion: v1
+kind: ConfigMap
+metadata:
+  name: kueue-manager-config
+  namespace: kueue-system
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1alpha1
+    kind: Configuration
+    health:
+      healthProbeBindAddress: :8081
+    metrics:
+      bindAddress: :8080
+    webhook:
+      port: 9443
+    manageJobsWithoutQueueName: true
+```
+
+3. To apply the customized manifests to the cluster, run the following command:
```

  ```suggestion
  3. Apply the customized manifests to the cluster:
  ```

- **docs/setup/install.md:32** by [@cortespao](https://github.com/cortespao) — 2022-05-03T02:04:31Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
```

  ```suggestion
  ConfigMap manifest, edit the `controller_manager_config.yaml` data entry. This entry represents
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-03T02:39:33Z

- **docs/setup/install.md:21** by [@ahg-g](https://github.com/ahg-g) — 2022-05-03T02:34:21Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
```

  No really, it is custom "configured", the configuration is the custom, not the release itself.

- **docs/setup/install.md:25** by [@ahg-g](https://github.com/ahg-g) — 2022-05-03T02:35:51Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
```

  done.

- **docs/setup/install.md:32** by [@ahg-g](https://github.com/ahg-g) — 2022-05-03T02:38:17Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
```

  done.

- **docs/setup/install.md:56** by [@ahg-g](https://github.com/ahg-g) — 2022-05-03T02:38:52Z
```diff
@@ -18,6 +18,47 @@ VERSION=v0.1.0
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+## Install a custom-configured released version
+
+To install a custom-configured released version of Kueue in your cluster, execute the following steps: 
+
+1. Download the release's `manifests.yaml` file using the following command:
+
+```shell
+VERSION=v0.1.0
+wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+2. Using your favourite editor, open `manifests.yaml` and edit the `kueue-manager-config` 
+ConfigMap manifest, specifically edit the `controller_manager_config.yaml` data entry which represents
+the default Kueue Configuration 
+struct ([v1alpha1@v0.1.0](https://pkg.go.dev/sigs.k8s.io/kueue@v0.1.0/apis/config/v1alpha1#Configuration)).
+The contents of the ConfigMap are similar to the following:
+
+```yaml
+apiVersion: v1
+kind: ConfigMap
+metadata:
+  name: kueue-manager-config
+  namespace: kueue-system
+data:
+  controller_manager_config.yaml: |
+    apiVersion: config.kueue.x-k8s.io/v1alpha1
+    kind: Configuration
+    health:
+      healthProbeBindAddress: :8081
+    metrics:
+      bindAddress: :8080
+    webhook:
+      port: 9443
+    manageJobsWithoutQueueName: true
+```
+
+3. To apply the customized manifests to the cluster, run the following command:
```

  done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-03T20:26:52Z

/label merge-method/squash
