# PR #2400: [kjobctl] Add volume API

**Summary**: [kjobctl] Add volume API

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2400

**Last updated**: 2024-07-02T08:13:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/kjobctl-volume-api` → `main`
- **Created**: 2024-06-11T18:26:58Z
- **Updated**: 2024-07-02T08:13:38Z
- **Merged**: 2024-06-20T09:32:17Z
- **Closed**: 2024-06-20T09:32:17Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 4   **+2013 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature
#### What this PR does / why we need it:
Adds Volume API for kjobctl.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T18:27:01Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-11T18:27:16Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a586b8007ed3d0b18ec83d1bc598b6ab4fb77b5e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6673ed8e6081190008c91a5a |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2400--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2400--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI0MDAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.ZhdkkmhioWjlZvIMT6xKhFx4QTjLS4evWj05bH6ErnA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-13T09:28:50Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-14T13:01:26Z

LGTM

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-19T11:14:47Z

/test pull-kueue-build-image-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-19T15:51:38Z

LGTM label has been added.  <details>Git tree hash: 31db8cd63cad5cab14d0fccd8af0b13c7f881b91</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-19T18:49:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2400#pullrequestreview-2128896967" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2400#" title="Author self-approved">IrvingMg</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@trasc](https://github.com/trasc) — 2024-06-20T08:58:03Z

/retest 

(test infra issues)

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-20T09:14:24Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2024-06-20T09:31:12Z

/lgtm
/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-20T09:31:17Z

LGTM label has been added.  <details>Git tree hash: eaa44cd00010005787a987fcc132fd0c4f8e2ffc</details>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-12T06:21:43Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:37** by [@trasc](https://github.com/trasc) — 2024-06-12T06:16:44Z
```diff
@@ -17,11 +17,37 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +listType=map
+	// +listMapKey=name
+	// +optional
+	Env []corev1.EnvVar `json:"env,omitempty"`
+}
+
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.volumes.size() == self.mountPoints.size()", message="volumes and mountPoints must have the same amount of items"
```

  Not necessary, you could mount a volume (or pats of it) in multiple points. Ideally we should make sure that  `MountPoints[x].Name` is found in `Volumes[...].Name`

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:47** by [@trasc](https://github.com/trasc) — 2024-06-12T06:17:31Z
```diff
@@ -17,11 +17,37 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +listType=map
+	// +listMapKey=name
+	// +optional
+	Env []corev1.EnvVar `json:"env,omitempty"`
+}
+
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.volumes.size() == self.mountPoints.size()", message="volumes and mountPoints must have the same amount of items"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all containers.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// MountPoints is a list of locations where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=name
```

  ```suggestion
  	// +listType=map
  	// +listMapKey=mountPath
  ```

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:33** by [@trasc](https://github.com/trasc) — 2024-06-12T06:21:32Z
```diff
@@ -17,11 +17,37 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +listType=map
+	// +listMapKey=name
+	// +optional
+	Env []corev1.EnvVar `json:"env,omitempty"`
```

  `cirev1.EnvVal` might be too much, since it's likely we are only going to use the `name` a set of strings should be sufficient.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-13T16:04:52Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-13T16:04:52Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Env []string `json:"env,omitempty"`
```

  @trasc Shouldn't it be just a one env? Or I something missed?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-13T16:05:28Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-13T16:05:28Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Env []string `json:"env,omitempty"`
```

  But if it's supposed to be an array, maybe rename it to Envs?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-14T10:10:34Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-14T10:10:34Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Env []string `json:"env,omitempty"`
```

  @mwielgus WDYT?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-06-14T10:32:27Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@mwielgus](https://github.com/mwielgus) — 2024-06-14T10:32:26Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Env []string `json:"env,omitempty"`
```

  It is supposed to be an array as there might be multiple important locations on the volume. Please make it Envs.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-14T12:56:28Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-14T12:56:28Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Env []string `json:"env,omitempty"`
```

  Renamed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-14T13:01:00Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-14T13:01:00Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Env []string `json:"env,omitempty"`
```

  Thanks

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-14T14:07:55Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@trasc](https://github.com/trasc) — 2024-06-14T14:07:54Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Env []string `json:"env,omitempty"`
```

  An environment it's a collection of environment variables. :), I think the intent is to have multiple env-variables that will be placed in a single environment.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-14T18:51:35Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@trasc](https://github.com/trasc) — 2024-06-14T18:51:35Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Envs []string `json:"envs,omitempty"`
```

  should match "[A-Za-z_][A-Za-z0-9_]*"
  
  "EnvVars" should be a better name

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-06-14T20:29:09Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@mwielgus](https://github.com/mwielgus) — 2024-06-14T20:29:09Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Envs []string `json:"envs,omitempty"`
```

  The type is problematic here. If we go with multiple environment variables then they need to be pairs - variable name & value/path

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-17T09:41:59Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-17T09:41:59Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Envs []string `json:"envs,omitempty"`
```

  @mwielgus  could you please provide an example of a use case for this property so that we can see what is the expected behavior?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-19T10:59:23Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:31** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-19T10:59:22Z
```diff
@@ -17,11 +17,35 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
+	// +optional
+	Envs []string `json:"envs,omitempty"`
```

  Moved envVars to VolumeBundleSpec as discussed in the design document.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T13:42:13Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:28** by [@trasc](https://github.com/trasc) — 2024-06-19T13:39:42Z
```diff
@@ -17,11 +17,36 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+}
+
```

  we no longer need this

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:42** by [@trasc](https://github.com/trasc) — 2024-06-19T13:40:14Z
```diff
@@ -17,11 +17,36 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MountPoint struct {
+	// Inlines Container’s volume mounts.
+	corev1.VolumeMount `json:",inline"`
+}
+
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all containers.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// MountPoints is a list of locations where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=mountPath
+	// +kubebuilder:validation:MinItems=1
+	MountPoints []MountPoint `json:"mountPoints"`
```

  ```suggestion
  	MountPoints []corev1.VolumeMount `json:"mountPoints"`
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-19T15:05:24Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T15:00:24Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all containers.
```

  ```suggestion
  	// Volumes is a set of volumes that will be added to all pods of the job.
  ```

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:33** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T15:00:40Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all containers.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// MountPoints is a list of locations where the volumes will be mounted.
```

  ```suggestion
  	// MountPoints is a list of locations in each container of a pod where the volumes will be mounted.
  ```

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T15:01:09Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all containers.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// MountPoints is a list of locations where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=mountPath
+	// +kubebuilder:validation:MinItems=1
+	MountPoints []corev1.VolumeMount `json:"mountPoints"`
```

  ```suggestion
  	containerVolumeMounts []corev1.VolumeMount `json:"mountPoints"`
  ```
  
  or just volumeMounts?

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T15:01:51Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all containers.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// MountPoints is a list of locations where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=mountPath
+	// +kubebuilder:validation:MinItems=1
+	MountPoints []corev1.VolumeMount `json:"mountPoints"`
+
+	// Environment variables pointing to locations on the mount.
+	// These values will be copied to pods/containers environment variables.
```

  ```suggestion
  	// These key/value pairs will be available in containers as environment variables.
  ```

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T15:03:54Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all containers.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// MountPoints is a list of locations where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=mountPath
+	// +kubebuilder:validation:MinItems=1
+	MountPoints []corev1.VolumeMount `json:"mountPoints"`
+
+	// Environment variables pointing to locations on the mount.
```

  ```suggestion
  	// Environment variables that refer to absolute paths in the container filesystem.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T15:51:33Z

just one nit,  LGTM otherwise

/lgtm

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:42** by [@trasc](https://github.com/trasc) — 2024-06-19T15:49:48Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all pods of the job.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// ContainerVolumeMounts is a list of locations in each container of a pod where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=mountPath
+	// +kubebuilder:validation:MinItems=1
+	ContainerVolumeMounts []corev1.VolumeMount `json:"containerVolumeMounts"`
+
+	// Environment variables that refer to absolute paths in the container filesystem.
+	// These key/value pairs will be available in containers as environment variables.
+	// +optional
+	// +kubebuilder:validation:XValidation:rule="self.all(x, x.name.matches('^[A-Za-z_][A-Za-z0-9_]*$') )", message="invalid environment variable name"
```

  nit:
  
  This may no longer be needed.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-19T16:01:38Z

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:42** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-19T16:01:37Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all pods of the job.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// ContainerVolumeMounts is a list of locations in each container of a pod where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=mountPath
+	// +kubebuilder:validation:MinItems=1
+	ContainerVolumeMounts []corev1.VolumeMount `json:"containerVolumeMounts"`
+
+	// Environment variables that refer to absolute paths in the container filesystem.
+	// These key/value pairs will be available in containers as environment variables.
+	// +optional
+	// +kubebuilder:validation:XValidation:rule="self.all(x, x.name.matches('^[A-Za-z_][A-Za-z0-9_]*$') )", message="invalid environment variable name"
```

  I think we can leave the validation as it seems the EnvVar type doesn't do any validation for names.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-19T18:49:52Z

/approve
/hold for nit

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T18:48:09Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all pods of the job.
```

  Almost forgot. We use the json names in API comments. We probably slipped some violations at some point. Feel free to open separate PRs if you find any.
  
  ```suggestion
  	// volumes is a set of volumes that will be added to all pods of the job.
  ```

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:33** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T18:48:15Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all pods of the job.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// ContainerVolumeMounts is a list of locations in each container of a pod where the volumes will be mounted.
```

  ```suggestion
  	// containerVolumeMounts is a list of locations in each container of a pod where the volumes will be mounted.
  ```

- **cmd/experimental/kjobctl/apis/v1alpha1/volumebundle_types.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T18:48:33Z
```diff
@@ -17,11 +17,31 @@ limitations under the License.
 package v1alpha1
 
 import (
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
 // VolumeBundleSpec defines the desired state of VolumeBundle
-type VolumeBundleSpec struct{}
+// +kubebuilder:validation:XValidation:rule="self.mountPoints.map(x, x.name in self.volumes.map(y, y.name))", message="mountPoint name must match a volume name"
+type VolumeBundleSpec struct {
+	// Volumes is a set of volumes that will be added to all pods of the job.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MinItems=1
+	Volumes []corev1.Volume `json:"volumes"`
+
+	// ContainerVolumeMounts is a list of locations in each container of a pod where the volumes will be mounted.
+	// +listType=map
+	// +listMapKey=mountPath
+	// +kubebuilder:validation:MinItems=1
+	ContainerVolumeMounts []corev1.VolumeMount `json:"containerVolumeMounts"`
+
+	// Environment variables that refer to absolute paths in the container filesystem.
```

  ```suggestion
  	// envVars are environment variables that refer to absolute paths in the container filesystem.
  ```
