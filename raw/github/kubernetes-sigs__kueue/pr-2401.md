# PR #2401: [MultiKueue] Default the managedBy field for batch Jobs managed by MultiKueue

**Summary**: [MultiKueue] Default the managedBy field for batch Jobs managed by MultiKueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2401

**Last updated**: 2024-07-18T20:40:53Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `job-default-managed-by` → `main`
- **Created**: 2024-06-11T22:25:32Z
- **Updated**: 2024-07-18T20:40:53Z
- **Merged**: 2024-06-17T16:31:29Z
- **Closed**: 2024-06-17T16:31:30Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 3   **+168 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Defaults `Spec.ManagedBy` of the job for k8s 1.30+ and if it's managed by multi-kueue 
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to https://github.com/kubernetes-sigs/kueue/issues/693

#### Special notes for your reviewer:
Follow-up of https://github.com/kubernetes-sigs/kueue/pull/2331#pullrequestreview-2098780499
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: Allow for defaulting of the spec.managedBy field for Jobs managed by MultiKueue.
The defaulting is enabled by the MultiKueueBatchJobWithManagedBy feature-gate.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T22:25:35Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-11T22:25:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 027761825a5d187782369595596d876c79343d0a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66706100bb98a40007a658c5 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2401--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2401--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI0MDEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Cpv8LEALOSudeVWuvkfpifYBIALr-cFbfsDg_UQYXAk)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-11T22:26:11Z

/assign @vladikkuzn @trasc

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-11T22:26:20Z

/cc @trasc @mimowo

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-14T01:29:44Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2024-06-14T05:58:37Z

/test all

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-14T10:08:51Z

Behaviour before that PR: when job is admitted, but cluster queue of local queue doesn't exist, job is suspended

Possible behaviours after this PR:
1. when job is admitted, but cluster queue of local queue doesn't exist, webhook rejects the admission
2. when job is admitted, but cluster queue of local queue doesn't exist, job is suspended, but managed by is set to "job controlled" and will never be changed because of immutability

WDYT what's expected? @mimowo @alculquicondor

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-14T10:30:50Z

> Possible behaviours after this PR:
> 
> 1. when job is admitted, but cluster queue of local queue doesn't exist, webhook rejects the admission
> 2. when job is admitted, but cluster queue of local queue doesn't exist, job is suspended, but managed by is set to "job controlled" and will never be changed because of immutability
> 
> WDYT what's expected? @mimowo @alculquicondor

IIUC your question is outside of the MultiKueue context, because if the ClusterQueue does not exist, then we don't know it is MultiKueue? Changing the behavior for non-MultiKueue workloads is out-of-scope for this PR, so (2.) is my preference in this PR. Please make sure this is also consistent with our webhook for JobSet.

Indeed, webhooks seeing the cache could potentially allow for such fail-fast rejection. We can open a separate issue to discuss and see what is expected by the users (reject, suspend, or configurable?).

### Comment by [@trasc](https://github.com/trasc) — 2024-06-14T10:43:09Z

The problem is that being consistent with JobSet approach and not changing the non-MultiKueue scenario behavior are mutually excursive.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-14T10:53:35Z

So in the JobSet webhook we started to fail fast?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-14T11:03:30Z

I see we reject it [here](https://github.com/kubernetes-sigs/kueue/blob/8b217c67c6cfb2a588a90107a63d3cf19182b191/pkg/controller/jobs/jobset/jobset_webhook.go#L78). This is inside the MultiKueue feature gate.

I think the decision to reject or admit should be independent of the MultiKueue feature gate. Thus, I would suggest to not reject in this PR, and also prepare a fix for JobSet to do not reject. Then we have the behavior consistent, and we can discuss separately what should happen, but don't couple the decision to the MultiKueue feature gate, since in this case we cannot inspect the ClusterQueue (as it doesn't exist).

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-14T12:45:05Z

> not changing the non-MultiKueue scenario behavior are mutually excursive.

It would be strange if it's rejected in MultiKueue context and accepted in non-MultiKueue

> So in the JobSet webhook we started to fail fast?

Yes
Probably also need to check how jobset works: if it also suspends all child jobs if there's no matching cluster queue

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-14T12:48:50Z

> It would be strange if it's rejected in MultiKueue context and accepted in non-MultiKueue

Exactly, but not only that - it could be a breaking change for some users who don't use MultiKueue, just because the feature gate graduated from Alpha to Beta. Let's fix the JobSet if it rejects.

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-17T00:00:01Z

/test all

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-17T00:14:20Z

/test all

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-17T16:22:42Z

/lgtm
/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-17T16:22:49Z

LGTM label has been added.  <details>Git tree hash: 096a56cee24531166a89c35fb59e80183c95fffc</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-17T16:22:49Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2401#issuecomment-2173834912" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2401#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [mimowo]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-17T16:27:27Z

/retitle "[MultiKueue] Default the managedBy field for batch Jobs managed by MultiKueue"
/release-note-edit
```release-note
Allow for defaulting of the spec.managedBy field for Jobs managed by MultiKueue.
The defaulting is enabled by the MultiKueueBatchJobWithManagedBy feature-gate.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-17T16:28:21Z

/release-note-edit
```release-note
Allow for defaulting of the spec.managedBy field for Jobs managed by MultiKueue.
The defaulting is enabled by the MultiKueueBatchJobWithManagedBy feature-gate.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-18T07:40:08Z

/retitle [MultiKueue] Default the managedBy field for batch Jobs managed by MultiKueue

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T13:23:56Z

/release-note-edit:

```release-note
MultiKueue: Allow for defaulting of the spec.managedBy field for Jobs managed by MultiKueue.
The defaulting is enabled by the MultiKueueBatchJobWithManagedBy feature-gate.
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T20:40:51Z

/release-note-edit
```release-note
MultiKueue: Allow for defaulting of the spec.managedBy field for Jobs managed by MultiKueue.
The defaulting is enabled by the MultiKueueBatchJobWithManagedBy feature-gate.
```

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-12T05:02:11Z

add some unit tests as well

- **pkg/controller/jobs/job/job_webhook.go:106** by [@trasc](https://github.com/trasc) — 2024-06-12T04:51:46Z
```diff
@@ -70,9 +80,36 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(job.Spec.ManagedBy) {
+		localQueueName, found := job.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(job.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Defaulting ManagedBy", "job", klog.KObj(job), "oldManagedBy", job.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
+				job.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				return nil
+			}
+		}
+	}
 	return nil
 }
 
+func canDefaultManagedBy(jobSpecManagedBy *string) bool {
+	// Only from k8s 1.30
+	// https://kubernetes.io/docs/concepts/workloads/controllers/job/#delegation-of-managing-a-job-object-to-external-controller
+	// TODO: check for JobManagedBy k8s feature gate?
```

  We should not bother, the admin should check this before enabling `MultiKueueBatchJobWithManagedBy` .

- **pkg/controller/jobs/job/job_webhook.go:110** by [@trasc](https://github.com/trasc) — 2024-06-12T05:01:50Z
```diff
@@ -70,9 +80,36 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(job.Spec.ManagedBy) {
+		localQueueName, found := job.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(job.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Defaulting ManagedBy", "job", klog.KObj(job), "oldManagedBy", job.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
+				job.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				return nil
+			}
+		}
+	}
 	return nil
 }
 
+func canDefaultManagedBy(jobSpecManagedBy *string) bool {
+	// Only from k8s 1.30
+	// https://kubernetes.io/docs/concepts/workloads/controllers/job/#delegation-of-managing-a-job-object-to-external-controller
+	// TODO: check for JobManagedBy k8s feature gate?
+	return features.Enabled(features.MultiKueueBatchJobWithManagedBy) &&
+		features.Enabled(features.MultiKueue) &&
+		(jobSpecManagedBy == nil || *jobSpecManagedBy == jobsetapi.JobSetControllerName)
```

  ```suggestion
  		(jobSpecManagedBy == nil || *jobSpecManagedBy == batchv1.JobControllerName)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-12T09:20:09Z

- **pkg/controller/jobs/job/job_webhook.go:83** by [@mimowo](https://github.com/mimowo) — 2024-06-12T09:20:09Z
```diff
@@ -70,9 +79,36 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(job.Spec.ManagedBy) {
```

  Please cover with unit tests as for JobSet. IIRC we should now be also able to test this e2e by not setting `managedBy` in the test code.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-12T22:36:50Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:33** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-12T22:36:50Z
```diff
@@ -30,14 +30,22 @@ import (
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	kueuejob "sigs.k8s.io/kueue/pkg/controller/jobs/job"
```

  Had to remove it because of circular import

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-13T05:45:48Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:33** by [@trasc](https://github.com/trasc) — 2024-06-13T05:45:48Z
```diff
@@ -30,14 +30,22 @@ import (
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	kueuejob "sigs.k8s.io/kueue/pkg/controller/jobs/job"
```

  If it's caused by ControllerName being used  in job , just move it to API, that is what I'm doing in  #2373.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-13T08:58:01Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:33** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-13T08:58:01Z
```diff
@@ -30,14 +30,22 @@ import (
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	kueuejob "sigs.k8s.io/kueue/pkg/controller/jobs/job"
```

  No, it's because of legacyJobNameLabel, legacyControllerUIDLabel (not prefixed versions of job-name and controller-uid) (which k8s still support, but they are deprecated overall)

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-13T09:33:11Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:33** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-13T09:33:11Z
```diff
@@ -30,14 +30,22 @@ import (
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	kueuejob "sigs.k8s.io/kueue/pkg/controller/jobs/job"
```

  Will get controller name from API

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-14T02:05:31Z

- **test/e2e/singlecluster/e2e_test.go:68** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-14T02:05:31Z
```diff
@@ -63,6 +63,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 	})
 
+	// TODO: how it should behave?
 	ginkgo.When("Creating a Job without a matching LocalQueue", func() {
 		ginkgo.It("Should stay in suspended", func() {
 			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
```

  Should it stay suspended or not be admitted at all?
  WDYT?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-14T02:05:46Z

- **pkg/controller/jobs/job/job_webhook.go:92** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-14T02:05:45Z
```diff
@@ -70,9 +79,34 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(job.Spec.ManagedBy) {
+		localQueueName, found := job.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(job.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			// Or return error, in case of error job will be rejected if there's no matching local queue
+			return nil
+		}
```

  WDYT?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-14T06:03:07Z

- **test/e2e/singlecluster/e2e_test.go:68** by [@trasc](https://github.com/trasc) — 2024-06-14T06:03:07Z
```diff
@@ -63,6 +63,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 	})
 
+	// TODO: how it should behave?
 	ginkgo.When("Creating a Job without a matching LocalQueue", func() {
 		ginkgo.It("Should stay in suspended", func() {
 			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
```

  Nothing should change here, only in multikueue.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-14T06:06:17Z

- **pkg/controller/jobs/job/job_webhook.go:92** by [@trasc](https://github.com/trasc) — 2024-06-14T06:06:16Z
```diff
@@ -70,9 +79,34 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(job.Spec.ManagedBy) {
+		localQueueName, found := job.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(job.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			// Or return error, in case of error job will be rejected if there's no matching local queue
+			return nil
+		}
```

  return the err, it's the way we are doing it in 
  
  https://github.com/kubernetes-sigs/kueue/blame/8b217c67c6cfb2a588a90107a63d3cf19182b191/pkg/controller/jobs/jobset/jobset_webhook.go#L76-L79

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-17T09:10:59Z

Lgtm overall.

- **test/e2e/multikueue/e2e_test.go:276** by [@mimowo](https://github.com/mimowo) — 2024-06-17T09:04:32Z
```diff
@@ -270,7 +273,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			// Since it requires 2 CPU in total, this jobset can only be admitted in worker 1.
 			jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
 				Queue(managerLq.Name).
-				ManagedBy(multikueue.ControllerName).
+				ManagedBy(kueuealpha.MultiKueueControllerName).
```

  Can we drop this? Looks like leftover from the previous work

- **test/e2e/multikueue/e2e_test.go:205** by [@mimowo](https://github.com/mimowo) — 2024-06-17T09:07:28Z
```diff
@@ -201,6 +199,11 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 
 			ginkgo.By("Creating the job", func() {
 				gomega.Expect(k8sManagerClient.Create(ctx, job)).Should(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdJob := &batchv1.Job{}
+					g.Expect(k8sManagerClient.Get(ctx, client.ObjectKeyFromObject(job), createdJob)).To(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.ManagedBy, "")).To(gomega.BeEquivalentTo(kueuealpha.MultiKueueControllerName))
```

  ```suggestion
  					g.Expect(createdJob.Spec.ManagedBy).To(gomega.BeEquivalentTo(ptr.To(kueuealpha.MultiKueueControllerName)))
  ```
  I think this would read better.
  ```

- **pkg/controller/jobs/job/job_webhook.go:90** by [@mimowo](https://github.com/mimowo) — 2024-06-17T09:10:04Z
```diff
@@ -70,9 +79,34 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(job.Spec.ManagedBy) {
+		localQueueName, found := job.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(job.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			log.V(5).Info("Failed to get cluster queue name", "job", klog.KObj(job), "error", err)
```

  I don't particularly like handling errors like info. IIUC "does not exist" is the only scenario in which the error is thrown. Could we instead return a bool "found" from `ClusterQueueFromLocalQueue`? Then we could ignore it similarly as we do above for LocalQueue.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-17T09:24:32Z

- **test/e2e/multikueue/e2e_test.go:276** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-17T09:24:32Z
```diff
@@ -270,7 +273,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			// Since it requires 2 CPU in total, this jobset can only be admitted in worker 1.
 			jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
 				Queue(managerLq.Name).
-				ManagedBy(multikueue.ControllerName).
+				ManagedBy(kueuealpha.MultiKueueControllerName).
```

  No, it's needed because of circular import
  TLDR controller name should be defined on api level to prevent such issue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-17T09:32:32Z

- **test/e2e/multikueue/e2e_test.go:276** by [@mimowo](https://github.com/mimowo) — 2024-06-17T09:32:32Z
```diff
@@ -270,7 +273,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			// Since it requires 2 CPU in total, this jobset can only be admitted in worker 1.
 			jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
 				Queue(managerLq.Name).
-				ManagedBy(multikueue.ControllerName).
+				ManagedBy(kueuealpha.MultiKueueControllerName).
```

  I meant can we delete the entire line: `ManagedBy(kueuealpha.MultiKueueControllerName).`? Seems not needed since we default it now in the webhook

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-17T11:09:33Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:82** by [@mimowo](https://github.com/mimowo) — 2024-06-17T11:09:33Z
```diff
@@ -75,12 +75,13 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 		}
 		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
 		if err != nil {
-			return err
+			log.V(5).Info("Failed to get cluster queue name", "jobset", klog.KObj(jobSet), "error", err)
+			return nil
 		}
 		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
-			if admissionCheck.Controller == multikueue.ControllerName {
-				log.V(5).Info("Defaulting ManagedBy", "jobset", klog.KObj(jobSet), "oldManagedBy", jobSet.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
-				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+			if admissionCheck.Controller == v1alpha1.MultiKueueControllerName {
```

  Please de-couple the JobSet fix to a separate PR. We may want to cherry-pick it onto 0.7 (not confirmed yet, but let's keep the decisions about cherry-picking independent).

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-17T13:20:58Z

- **test/e2e/multikueue/e2e_test.go:205** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-17T13:20:58Z
```diff
@@ -201,6 +199,11 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 
 			ginkgo.By("Creating the job", func() {
 				gomega.Expect(k8sManagerClient.Create(ctx, job)).Should(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdJob := &batchv1.Job{}
+					g.Expect(k8sManagerClient.Get(ctx, client.ObjectKeyFromObject(job), createdJob)).To(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.ManagedBy, "")).To(gomega.BeEquivalentTo(kueuealpha.MultiKueueControllerName))
```

  Maybe, but it's better to compare values then pointers

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-17T13:24:01Z

- **test/e2e/multikueue/e2e_test.go:205** by [@mimowo](https://github.com/mimowo) — 2024-06-17T13:24:01Z
```diff
@@ -201,6 +199,11 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 
 			ginkgo.By("Creating the job", func() {
 				gomega.Expect(k8sManagerClient.Create(ctx, job)).Should(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdJob := &batchv1.Job{}
+					g.Expect(k8sManagerClient.Get(ctx, client.ObjectKeyFromObject(job), createdJob)).To(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.ManagedBy, "")).To(gomega.BeEquivalentTo(kueuealpha.MultiKueueControllerName))
```

  For pointers Equals will not work, but BeEquivalentTo, or BeComparableTo would work. 
  
  Anyway, it is as nit, I'm ok to keep as is, just pointing out that the additional defaulting makes it tricky to read.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-06-17T13:39:49Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:82** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-06-17T13:39:49Z
```diff
@@ -75,12 +75,13 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 		}
 		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
 		if err != nil {
-			return err
+			log.V(5).Info("Failed to get cluster queue name", "jobset", klog.KObj(jobSet), "error", err)
+			return nil
 		}
 		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
-			if admissionCheck.Controller == multikueue.ControllerName {
-				log.V(5).Info("Defaulting ManagedBy", "jobset", klog.KObj(jobSet), "oldManagedBy", jobSet.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
-				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+			if admissionCheck.Controller == v1alpha1.MultiKueueControllerName {
```

  https://github.com/kubernetes-sigs/kueue/pull/2425
  ^ I think this one should be merged first, it would be easier to make this PR smaller then

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-17T13:50:17Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:82** by [@mimowo](https://github.com/mimowo) — 2024-06-17T13:50:17Z
```diff
@@ -75,12 +75,13 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 		}
 		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
 		if err != nil {
-			return err
+			log.V(5).Info("Failed to get cluster queue name", "jobset", klog.KObj(jobSet), "error", err)
+			return nil
 		}
 		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
-			if admissionCheck.Controller == multikueue.ControllerName {
-				log.V(5).Info("Defaulting ManagedBy", "jobset", klog.KObj(jobSet), "oldManagedBy", jobSet.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
-				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+			if admissionCheck.Controller == v1alpha1.MultiKueueControllerName {
```

  +1
