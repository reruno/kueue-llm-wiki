# PR #2402: Bump Kubernetes dependencies to v0.31.x

**Summary**: Bump Kubernetes dependencies to v0.31.x

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2402

**Last updated**: 2024-09-12T14:26:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/update-kube-codegen-version` → `main`
- **Created**: 2024-06-12T06:35:53Z
- **Updated**: 2024-09-12T14:26:55Z
- **Merged**: 2024-09-12T14:25:20Z
- **Closed**: 2024-09-12T14:25:20Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2653   **+137476 / -72653**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Bump Kubernetes dependencies to v0.31.x.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2948

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support for Kubernetes 1.31
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-12T06:35:55Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-12T06:36:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4f0badd5d935459e39c64b54e8de80c13aae69c7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e2dab56b0e0f0008038434 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-12T06:41:09Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-12T07:15:25Z

/test pull-kueue-verify-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-14T21:41:44Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-14T22:08:00Z

/cc @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-15T07:06:46Z

/hold
We keep parking until the first patch release so that we can avoid the early bugs on Kubernetes and we can release a patch version easily.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-15T13:32:21Z

/retitle Bump Kubernetes dependencies to v0.31.x

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-15T17:59:40Z

LGTM label has been added.  <details>Git tree hash: 4d003af3fcfe81c85032665f6507555838caf069</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2024-08-20T19:54:04Z

Looking forward to this change. COuld you please update the PR to fix conflicts?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-21T15:28:03Z

> Looking forward to this change. Could you please update the PR to fix conflicts?

This PR on hold and we are waiting for the first release patch of k8s.

### Comment by [@kannon92](https://github.com/kannon92) — 2024-09-03T18:08:22Z

Can you add 1.31 to the E2E_K8S_VERSION in Makefile-test.mk?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-03T18:24:24Z

> Can you add 1.31 to the E2E_K8S_VERSION in Makefile-test.mk?

Good point, thanks!

### Comment by [@kannon92](https://github.com/kannon92) — 2024-09-12T11:52:25Z

1.31.1 was released. Can you rebase this and get this in?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-12T12:13:28Z

Is this just a cleanup though? I think it deserves a release note, as it has user-facing consequences - it fixes https://github.com/kubernetes-sigs/kueue/issues/2948, and allows support for JobSet 0.6.0 . 

Tentatively updating, but feel free to change / discuss if I'm missing something. We can mention the support for JobSet 0.6 in the follow up PR updating JobSet.

/kind feature
/release-note-edit
```release-note
Support for Kubernetes 1.31
```

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T12:22:25Z

Yeah, it started as a small cleanup for the code-generator and ended up as a major feature to support Kubernetes 1.31 :)

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T12:31:57Z

/hold cancel

Due to first patch for k8s dependencies released.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-12T13:59:12Z

Please also add  "Fixes #2948" in the description. I know this issue is closed already, but I think it will make the relationship easier to discover for future readers who find the PR in the release-notes.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T14:23:30Z

/remove-kind cleanup

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T14:23:35Z

/approve
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-12T14:23:41Z

LGTM label has been added.  <details>Git tree hash: e14448fd7c8aaf840769d0c52e96ca6330f37f3e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-12T14:23:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2402#issuecomment-2346449373" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2402#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-15T15:25:20Z

- **cmd/kueuectl/app/create/create_resourceflavor_test.go:252** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T13:18:43Z
```diff
@@ -249,6 +249,7 @@ func TestResourceFlavorCmd(t *testing.T) {
 		"shouldn't create resource flavor with dry-run client": {
 			rfName:  "rf",
 			args:    []string{"--dry-run", "client"},
+			wantRf:  &v1beta1.ResourceFlavor{},
```

  why this change?

- **hack/update-codegen.sh:25** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T14:11:52Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
```

  do we need to call go mod download here?

- **hack/update-codegen.sh:54** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T14:24:26Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
+
+cd "$CURRENT_DIR/.."
 
 # shellcheck source=/dev/null
 source "${CODEGEN_PKG}/kube_codegen.sh"
 
-# TODO: remove the workaround when the issue is solved in the code-generator
-# (https://github.com/kubernetes/code-generator/issues/165).
-# Here, we create the soft link named "sigs.k8s.io" to the parent directory of
-# Kueue to ensure the layout required by the kube_codegen.sh script.
-ln -s .. sigs.k8s.io
-trap "rm sigs.k8s.io" EXIT
-
 # Generating conversion and defaults functions
 kube::codegen::gen_helpers \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-base "${KUEUE_ROOT}" \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  "${KUEUE_ROOT}/apis"
 
 # Generating OpenAPI for Kueue API extensions
 kube::codegen::gen_openapi \
-  --input-pkg-root sigs.k8s.io/kueue/apis/visibility \
-  --output-pkg-root sigs.k8s.io/kueue/apis/visibility/v1alpha1 \
-  --output-base "${KUEUE_ROOT}" \
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/apis/visibility/v1alpha1/openapi" \
+  --output-pkg "${KUEUE_PKG}/apis/visibility/v1alpha1/openapi" \
   --update-report \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  "${KUEUE_ROOT}/apis/visibility"
 
 kube::codegen::gen_client \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-pkg-root sigs.k8s.io/kueue/client-go \
-  --output-base "${KUEUE_ROOT}" \
   --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/client-go" \
+  --output-pkg "${KUEUE_PKG}/client-go" \
   --with-watch \
-  --with-applyconfig
+  --with-applyconfig \
+  "${KUEUE_ROOT}/apis"
 
 # We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+"${GO_CMD}" mod tidy
```

  I don't think we need this because we manage the libraries in go.mod

- **pkg/controller/core/clusterqueue_controller.go:749** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T14:46:56Z
```diff
@@ -739,19 +739,18 @@ func (r *ClusterQueueReconciler) takeSnapshot(ctx context.Context) {
 func (r *ClusterQueueReconciler) processNextSnapshot(ctx context.Context) bool {
 	log := ctrl.LoggerFrom(ctx).WithName("processNextSnapshot")
 
-	key, quit := r.snapshotsQueue.Get()
+	cqName, quit := r.snapshotsQueue.Get()
 	if quit {
 		return false
 	}
 
 	startTime := time.Now()
 	defer func() {
-		log.V(5).Info("Finished snapshot job", "key", key, "elapsed", time.Since(startTime))
+		log.V(5).Info("Finished snapshot job", "key", cqName, "elapsed", time.Since(startTime))
```

  Let's use `clusterQueue` with `KRef` for consistency.

- **test/integration/framework/framework.go:138** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T14:51:39Z
```diff
@@ -134,6 +136,9 @@ func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerS
 				Port:    webhookInstallOptions.LocalServingPort,
 				CertDir: webhookInstallOptions.LocalServingCertDir,
 			}),
+		Controller: crconfig.Controller{
+			SkipNameValidation: ptr.To(true),
```

  I noticed that you added some names to controllers. Can you make sure we have different names, instead of adding this skip?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-15T15:26:49Z

- **hack/update-codegen.sh:25** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T15:26:49Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
```

  maybe use a dependency in the Makefile rule for codegen to download the tools

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T15:28:03Z

- **cmd/kueuectl/app/create/create_resourceflavor_test.go:252** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T15:28:03Z
```diff
@@ -249,6 +249,7 @@ func TestResourceFlavorCmd(t *testing.T) {
 		"shouldn't create resource flavor with dry-run client": {
 			rfName:  "rf",
 			args:    []string{"--dry-run", "client"},
+			wantRf:  &v1beta1.ResourceFlavor{},
```

  Generated client was updated and now on error it's returning empty object.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T15:38:56Z

- **hack/update-codegen.sh:54** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T15:38:56Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
+
+cd "$CURRENT_DIR/.."
 
 # shellcheck source=/dev/null
 source "${CODEGEN_PKG}/kube_codegen.sh"
 
-# TODO: remove the workaround when the issue is solved in the code-generator
-# (https://github.com/kubernetes/code-generator/issues/165).
-# Here, we create the soft link named "sigs.k8s.io" to the parent directory of
-# Kueue to ensure the layout required by the kube_codegen.sh script.
-ln -s .. sigs.k8s.io
-trap "rm sigs.k8s.io" EXIT
-
 # Generating conversion and defaults functions
 kube::codegen::gen_helpers \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-base "${KUEUE_ROOT}" \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  "${KUEUE_ROOT}/apis"
 
 # Generating OpenAPI for Kueue API extensions
 kube::codegen::gen_openapi \
-  --input-pkg-root sigs.k8s.io/kueue/apis/visibility \
-  --output-pkg-root sigs.k8s.io/kueue/apis/visibility/v1alpha1 \
-  --output-base "${KUEUE_ROOT}" \
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/apis/visibility/v1alpha1/openapi" \
+  --output-pkg "${KUEUE_PKG}/apis/visibility/v1alpha1/openapi" \
   --update-report \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  "${KUEUE_ROOT}/apis/visibility"
 
 kube::codegen::gen_client \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-pkg-root sigs.k8s.io/kueue/client-go \
-  --output-base "${KUEUE_ROOT}" \
   --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/client-go" \
+  --output-pkg "${KUEUE_PKG}/client-go" \
   --with-watch \
-  --with-applyconfig
+  --with-applyconfig \
+  "${KUEUE_ROOT}/apis"
 
 # We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+"${GO_CMD}" mod tidy
```

  Maybe it's not correct comment for this case, but sometimes we need to update go.mod and go.sum after client generation. Most often this happens after updating the code-generator library.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T15:56:07Z

- **test/integration/framework/framework.go:138** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T15:56:07Z
```diff
@@ -134,6 +136,9 @@ func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerS
 				Port:    webhookInstallOptions.LocalServingPort,
 				CertDir: webhookInstallOptions.LocalServingCertDir,
 			}),
+		Controller: crconfig.Controller{
+			SkipNameValidation: ptr.To(true),
```

  Ginkgo running suits concurrently and it's happening that managerSetup initialise manager and controllers a few times. So in this case we have an duplicate names error. I think for integration tests we can skip it.  And we also can be sure that we don't have duplicates on e2e tests.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-15T16:08:40Z

- **hack/update-codegen.sh:54** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T16:08:40Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
+
+cd "$CURRENT_DIR/.."
 
 # shellcheck source=/dev/null
 source "${CODEGEN_PKG}/kube_codegen.sh"
 
-# TODO: remove the workaround when the issue is solved in the code-generator
-# (https://github.com/kubernetes/code-generator/issues/165).
-# Here, we create the soft link named "sigs.k8s.io" to the parent directory of
-# Kueue to ensure the layout required by the kube_codegen.sh script.
-ln -s .. sigs.k8s.io
-trap "rm sigs.k8s.io" EXIT
-
 # Generating conversion and defaults functions
 kube::codegen::gen_helpers \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-base "${KUEUE_ROOT}" \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  "${KUEUE_ROOT}/apis"
 
 # Generating OpenAPI for Kueue API extensions
 kube::codegen::gen_openapi \
-  --input-pkg-root sigs.k8s.io/kueue/apis/visibility \
-  --output-pkg-root sigs.k8s.io/kueue/apis/visibility/v1alpha1 \
-  --output-base "${KUEUE_ROOT}" \
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/apis/visibility/v1alpha1/openapi" \
+  --output-pkg "${KUEUE_PKG}/apis/visibility/v1alpha1/openapi" \
   --update-report \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  "${KUEUE_ROOT}/apis/visibility"
 
 kube::codegen::gen_client \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-pkg-root sigs.k8s.io/kueue/client-go \
-  --output-base "${KUEUE_ROOT}" \
   --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/client-go" \
+  --output-pkg "${KUEUE_PKG}/client-go" \
   --with-watch \
-  --with-applyconfig
+  --with-applyconfig \
+  "${KUEUE_ROOT}/apis"
 
 # We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+"${GO_CMD}" mod tidy
```

  But updating the code-generator library should be separate from running `update-codegen`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-15T16:10:18Z

- **test/integration/framework/framework.go:138** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T16:10:18Z
```diff
@@ -134,6 +136,9 @@ func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerS
 				Port:    webhookInstallOptions.LocalServingPort,
 				CertDir: webhookInstallOptions.LocalServingCertDir,
 			}),
+		Controller: crconfig.Controller{
+			SkipNameValidation: ptr.To(true),
```

  Uhm... that sounds like a sign of a different problem. When running concurrently, we should probably have different managers. But we can investigate in another PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T16:14:44Z

- **test/integration/framework/framework.go:138** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T16:14:44Z
```diff
@@ -134,6 +136,9 @@ func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerS
 				Port:    webhookInstallOptions.LocalServingPort,
 				CertDir: webhookInstallOptions.LocalServingCertDir,
 			}),
+		Controller: crconfig.Controller{
+			SkipNameValidation: ptr.To(true),
```

  > we should probably have different managers
  
  Yes, we have. But [here](https://github.com/kubernetes-sigs/controller-runtime/blob/b901db121e1f53c47ec9f9683fad90a546688c3e/pkg/controller/name.go#L26-L27) using global variable for all managers.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T16:26:10Z

- **hack/update-codegen.sh:54** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T16:26:10Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
+
+cd "$CURRENT_DIR/.."
 
 # shellcheck source=/dev/null
 source "${CODEGEN_PKG}/kube_codegen.sh"
 
-# TODO: remove the workaround when the issue is solved in the code-generator
-# (https://github.com/kubernetes/code-generator/issues/165).
-# Here, we create the soft link named "sigs.k8s.io" to the parent directory of
-# Kueue to ensure the layout required by the kube_codegen.sh script.
-ln -s .. sigs.k8s.io
-trap "rm sigs.k8s.io" EXIT
-
 # Generating conversion and defaults functions
 kube::codegen::gen_helpers \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-base "${KUEUE_ROOT}" \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  "${KUEUE_ROOT}/apis"
 
 # Generating OpenAPI for Kueue API extensions
 kube::codegen::gen_openapi \
-  --input-pkg-root sigs.k8s.io/kueue/apis/visibility \
-  --output-pkg-root sigs.k8s.io/kueue/apis/visibility/v1alpha1 \
-  --output-base "${KUEUE_ROOT}" \
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/apis/visibility/v1alpha1/openapi" \
+  --output-pkg "${KUEUE_PKG}/apis/visibility/v1alpha1/openapi" \
   --update-report \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  "${KUEUE_ROOT}/apis/visibility"
 
 kube::codegen::gen_client \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-pkg-root sigs.k8s.io/kueue/client-go \
-  --output-base "${KUEUE_ROOT}" \
   --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/client-go" \
+  --output-pkg "${KUEUE_PKG}/client-go" \
   --with-watch \
-  --with-applyconfig
+  --with-applyconfig \
+  "${KUEUE_ROOT}/apis"
 
 # We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+"${GO_CMD}" mod tidy
```

  Yes you're right. But generated code can import some additional libraries then we need to update go.mod and go.sum. For example:
  
  https://github.com/kubernetes-sigs/kueue/blob/869ab97424fe5355b8f1b76150210d9e040c1126/client-go/applyconfiguration/utils.go#L21

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T16:28:21Z

- **hack/update-codegen.sh:25** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T16:28:21Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
```

  You're right. It's not necessary. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T16:29:20Z

- **pkg/controller/core/clusterqueue_controller.go:749** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T16:29:20Z
```diff
@@ -739,19 +739,18 @@ func (r *ClusterQueueReconciler) takeSnapshot(ctx context.Context) {
 func (r *ClusterQueueReconciler) processNextSnapshot(ctx context.Context) bool {
 	log := ctrl.LoggerFrom(ctx).WithName("processNextSnapshot")
 
-	key, quit := r.snapshotsQueue.Get()
+	cqName, quit := r.snapshotsQueue.Get()
 	if quit {
 		return false
 	}
 
 	startTime := time.Now()
 	defer func() {
-		log.V(5).Info("Finished snapshot job", "key", key, "elapsed", time.Since(startTime))
+		log.V(5).Info("Finished snapshot job", "key", cqName, "elapsed", time.Since(startTime))
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-15T17:27:21Z

- **hack/update-codegen.sh:54** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-15T17:27:21Z
```diff
@@ -21,40 +21,34 @@ set -o pipefail
 GO_CMD=${1:-go}
 CURRENT_DIR=$(dirname "${BASH_SOURCE[0]}")
 KUEUE_ROOT=$(realpath "${CURRENT_DIR}/..")
-CODEGEN_PKG=$($GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
-cd "${CURRENT_DIR}/.."
+KUEUE_PKG="sigs.k8s.io/kueue"
+CODEGEN_PKG=$(cd "${TOOLS_DIR}"; go mod download; $GO_CMD list -m -mod=readonly -f "{{.Dir}}" k8s.io/code-generator)
+
+cd "$CURRENT_DIR/.."
 
 # shellcheck source=/dev/null
 source "${CODEGEN_PKG}/kube_codegen.sh"
 
-# TODO: remove the workaround when the issue is solved in the code-generator
-# (https://github.com/kubernetes/code-generator/issues/165).
-# Here, we create the soft link named "sigs.k8s.io" to the parent directory of
-# Kueue to ensure the layout required by the kube_codegen.sh script.
-ln -s .. sigs.k8s.io
-trap "rm sigs.k8s.io" EXIT
-
 # Generating conversion and defaults functions
 kube::codegen::gen_helpers \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-base "${KUEUE_ROOT}" \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  "${KUEUE_ROOT}/apis"
 
 # Generating OpenAPI for Kueue API extensions
 kube::codegen::gen_openapi \
-  --input-pkg-root sigs.k8s.io/kueue/apis/visibility \
-  --output-pkg-root sigs.k8s.io/kueue/apis/visibility/v1alpha1 \
-  --output-base "${KUEUE_ROOT}" \
+  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/apis/visibility/v1alpha1/openapi" \
+  --output-pkg "${KUEUE_PKG}/apis/visibility/v1alpha1/openapi" \
   --update-report \
-  --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt"
+  "${KUEUE_ROOT}/apis/visibility"
 
 kube::codegen::gen_client \
-  --input-pkg-root sigs.k8s.io/kueue/apis \
-  --output-pkg-root sigs.k8s.io/kueue/client-go \
-  --output-base "${KUEUE_ROOT}" \
   --boilerplate "${KUEUE_ROOT}/hack/boilerplate.go.txt" \
+  --output-dir "${KUEUE_ROOT}/client-go" \
+  --output-pkg "${KUEUE_PKG}/client-go" \
   --with-watch \
-  --with-applyconfig
+  --with-applyconfig \
+  "${KUEUE_ROOT}/apis"
 
 # We need to clean up the go.mod file since code-generator adds temporary library to the go.mod file.
-"${GO_CMD}" mod tidy
+"${GO_CMD}" mod tidy
```

  I think you right, we should update it only one time on updating library. Removed it. Thanks.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-15T17:59:34Z

/lgtm
/approve
