# PR #2408: Improve AdmissionCheck documentation

**Summary**: Improve AdmissionCheck documentation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2408

**Last updated**: 2024-07-31T11:42:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `update-doc-deactivate-workload` → `main`
- **Created**: 2024-06-13T14:06:41Z
- **Updated**: 2024-07-31T11:42:14Z
- **Merged**: 2024-06-19T18:44:41Z
- **Closed**: 2024-06-19T18:44:41Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: _none_
- **Changed files**: 5   **+68 / -64**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
AdmissionCheck documentation needed an update as it was outdated. Main changes are:
- `retryDelayMinutes` field is deprecated
- AdmissionChecks can be configured per ResourceFlavor
- If AdmissionChecks gets `Rejected` a Workload is deactivated

Additionally it was lacking documentation on AdmissionCheck states (`Pending`,`Ready`, `Retry`, `Rejected`).
I've also fixed some typos.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-13T14:06:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4d8497e43f2dabeef3d29409ff4fc34a72dd236c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6672c23954e1920008a1ff5a |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-13T14:09:14Z

/assign @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-17T11:48:09Z

/assign @alculquicondor

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-17T11:48:21Z

cc @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-17T12:10:42Z

@PBundyra  please also update the PR description section "What this PR does / why we need it:" to list the main changes, because they are mixed with typo / rephrase improvements. Please also add that the main changes are motivated by recent changes in the semantic.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-19T12:01:59Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-19T18:28:40Z

LGTM label has been added.  <details>Git tree hash: e030d06154c0ce1cee495a48e9aa287bc549720c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-19T18:28:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2408#pullrequestreview-2128875437" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2408#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-17T11:52:00Z

otherwise LGTM

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@trasc](https://github.com/trasc) — 2024-06-17T11:51:50Z
```diff
@@ -31,8 +31,7 @@ const (
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// A workload having at least one check in this state will be evicted if admitted and
-	// will be marked as Finished.
+	// A workload having at least one check in this state will be evicted if admitted and deactivated
```

  ```suggestion
  	// A workload having at least one check in this state will be evicted if admitted and deactivated.
  ```
  
  NIT

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-17T12:08:48Z

- **site/content/en/docs/concepts/admission_check.md:18** by [@mimowo](https://github.com/mimowo) — 2024-06-17T11:54:52Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
+All of the AdmissionChecks have to provide a positive signal to the Workload before it can be [Admitted](/docs/concepts#admission).
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- **controllerName** - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes (deprecated)g** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to “Unknown”. The default is 15 min.
```

  ```suggestion
  - **retryDelayMinutes (deprecated)** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to “Unknown”. The default is 15 min.
  ```

- **site/content/en/docs/concepts/admission_check.md:19** by [@mimowo](https://github.com/mimowo) — 2024-06-17T11:55:47Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
+All of the AdmissionChecks have to provide a positive signal to the Workload before it can be [Admitted](/docs/concepts#admission).
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- **controllerName** - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes (deprecated)g** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to “Unknown”. The default is 15 min.
+- **parameters** - identifies additional resources providing additional parameters for the check, e.g. [`ProvisioningRequestConfig`](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration)
```

  ```suggestion
  - **parameters** - identifies a configuration with additional parameters for the check, e.g. [`ProvisioningRequestConfig`](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration)
  ```

- **site/content/en/docs/concepts/admission_check.md:17** by [@mimowo](https://github.com/mimowo) — 2024-06-17T12:02:25Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
+All of the AdmissionChecks have to provide a positive signal to the Workload before it can be [Admitted](/docs/concepts#admission).
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- **controllerName** - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
```

  Please also align the API comments, as they are user to generate the API reference: https://kueue.sigs.k8s.io/docs/reference/

- **site/content/en/docs/concepts/admission_check.md:117** by [@mimowo](https://github.com/mimowo) — 2024-06-17T12:06:21Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` th Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
```

  ```suggestion
    - If `Admitted` the Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
  ```

- **site/content/en/docs/concepts/admission_check.md:119** by [@mimowo](https://github.com/mimowo) — 2024-06-17T12:07:05Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` th Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
+  - Event is emitted
```

  ```suggestion
    - Event <event name> is emitted
  ```

- **site/content/en/docs/concepts/admission_check.md:118** by [@mimowo](https://github.com/mimowo) — 2024-06-17T12:07:37Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` th Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
```

  ```suggestion
    - If the Workload has `QuotaReservation` it will be released.
  ```

- **site/content/en/docs/concepts/admission_check.md:125** by [@mimowo](https://github.com/mimowo) — 2024-06-17T12:08:13Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` th Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
+  - Event is emitted
 
-Is a component that monitors Workloads maintaining the content of its specific `admissionCheckStates` and the `Active` condition of the AdmissionChecks it's  controlling.
-The logic for how an AdmissionCheck changes states is not part of Kueue.
+If any of the Workload's AdmissionCheck is in the `Rejected` state:
+  - Workload is deactivated - `workload.Spec.Active` is set to `False`
+  - If `Admitted` the Workload is evicted - Workload has an `Evicted` condition in `workload.Status.Condition` with `InactiveWorkload` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be released.
+  - Event is emitted
```

  ```suggestion
    - Event <event name> is emitted
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-17T13:52:50Z

- **site/content/en/docs/concepts/admission_check.md:125** by [@PBundyra](https://github.com/PBundyra) — 2024-06-17T13:52:50Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` th Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
+  - Event is emitted
 
-Is a component that monitors Workloads maintaining the content of its specific `admissionCheckStates` and the `Active` condition of the AdmissionChecks it's  controlling.
-The logic for how an AdmissionCheck changes states is not part of Kueue.
+If any of the Workload's AdmissionCheck is in the `Rejected` state:
+  - Workload is deactivated - `workload.Spec.Active` is set to `False`
+  - If `Admitted` the Workload is evicted - Workload has an `Evicted` condition in `workload.Status.Condition` with `InactiveWorkload` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be released.
+  - Event is emitted
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-17T13:52:57Z

- **site/content/en/docs/concepts/admission_check.md:119** by [@PBundyra](https://github.com/PBundyra) — 2024-06-17T13:52:57Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` th Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
+  - Event is emitted
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-17T14:09:09Z

- **site/content/en/docs/concepts/admission_check.md:17** by [@PBundyra](https://github.com/PBundyra) — 2024-06-17T14:09:08Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
+All of the AdmissionChecks have to provide a positive signal to the Workload before it can be [Admitted](/docs/concepts#admission).
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- **controllerName** - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
```

  Fixed

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-17T14:27:34Z

- **apis/kueue/v1beta1/admissioncheck_types.go:64** by [@trasc](https://github.com/trasc) — 2024-06-17T14:27:34Z
```diff
@@ -47,22 +46,22 @@ const (
 
 // AdmissionCheckSpec defines the desired state of AdmissionCheck
 type AdmissionCheckSpec struct {
-	// controllerName is name of the controller which will actually perform
-	// the checks. This is the name with which controller identifies with,
-	// not necessarily a K8S Pod or Deployment name. Cannot be empty.
+	// controllerName identifies the controller that processes the AdmissionCheck,
+	// not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ControllerName string `json:"controllerName"`
 
-	// RetryDelayMinutes specifies how long to keep the workload suspended
-	// after a failed check (after it transitioned to False).
-	// After that the check state goes to "Unknown".
+	// RetryDelayMinutes **deprecated** specifies how long to keep the workload suspended after
+	// a failed check (after it transitioned to False). After that the check
+	// state goes to “Unknown”. The default is 15 min.
 	// The default is 15 min.
 	// +optional
 	// +kubebuilder:default=15
 	RetryDelayMinutes *int64 `json:"retryDelayMinutes,omitempty"`
 
-	// Parameters identifies the resource providing additional check parameters.
+	// Parameters identifies a configuration with additional parameters for the
+	// check, e.g. [`ProvisioningRequestConfig`](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration)
```

  I don't think hard-coding a link to some generated site content will age very well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-17T15:15:33Z

- **apis/kueue/v1beta1/admissioncheck_types.go:64** by [@mimowo](https://github.com/mimowo) — 2024-06-17T15:15:33Z
```diff
@@ -47,22 +46,22 @@ const (
 
 // AdmissionCheckSpec defines the desired state of AdmissionCheck
 type AdmissionCheckSpec struct {
-	// controllerName is name of the controller which will actually perform
-	// the checks. This is the name with which controller identifies with,
-	// not necessarily a K8S Pod or Deployment name. Cannot be empty.
+	// controllerName identifies the controller that processes the AdmissionCheck,
+	// not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ControllerName string `json:"controllerName"`
 
-	// RetryDelayMinutes specifies how long to keep the workload suspended
-	// after a failed check (after it transitioned to False).
-	// After that the check state goes to "Unknown".
+	// RetryDelayMinutes **deprecated** specifies how long to keep the workload suspended after
+	// a failed check (after it transitioned to False). After that the check
+	// state goes to “Unknown”. The default is 15 min.
 	// The default is 15 min.
 	// +optional
 	// +kubebuilder:default=15
 	RetryDelayMinutes *int64 `json:"retryDelayMinutes,omitempty"`
 
-	// Parameters identifies the resource providing additional check parameters.
+	// Parameters identifies a configuration with additional parameters for the
+	// check, e.g. [`ProvisioningRequestConfig`](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration)
```

  Yeah, I don't see a need to link this. Just this seems enough: "Parameters identifies a configuration with additional parameters for the check". If you want more details, then maybe "Parameters identifies a configuration, represented as ProvisioningRequestConfig, with additional parameters for the check"

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-17T15:22:27Z

- **apis/kueue/v1beta1/admissioncheck_types.go:64** by [@PBundyra](https://github.com/PBundyra) — 2024-06-17T15:22:27Z
```diff
@@ -47,22 +46,22 @@ const (
 
 // AdmissionCheckSpec defines the desired state of AdmissionCheck
 type AdmissionCheckSpec struct {
-	// controllerName is name of the controller which will actually perform
-	// the checks. This is the name with which controller identifies with,
-	// not necessarily a K8S Pod or Deployment name. Cannot be empty.
+	// controllerName identifies the controller that processes the AdmissionCheck,
+	// not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ControllerName string `json:"controllerName"`
 
-	// RetryDelayMinutes specifies how long to keep the workload suspended
-	// after a failed check (after it transitioned to False).
-	// After that the check state goes to "Unknown".
+	// RetryDelayMinutes **deprecated** specifies how long to keep the workload suspended after
+	// a failed check (after it transitioned to False). After that the check
+	// state goes to “Unknown”. The default is 15 min.
 	// The default is 15 min.
 	// +optional
 	// +kubebuilder:default=15
 	RetryDelayMinutes *int64 `json:"retryDelayMinutes,omitempty"`
 
-	// Parameters identifies the resource providing additional check parameters.
+	// Parameters identifies a configuration with additional parameters for the
+	// check, e.g. [`ProvisioningRequestConfig`](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration)
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-17T18:34:41Z

- **apis/kueue/v1beta1/admissioncheck_types.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:22:06Z
```diff
@@ -47,22 +46,22 @@ const (
 
 // AdmissionCheckSpec defines the desired state of AdmissionCheck
 type AdmissionCheckSpec struct {
-	// controllerName is name of the controller which will actually perform
-	// the checks. This is the name with which controller identifies with,
-	// not necessarily a K8S Pod or Deployment name. Cannot be empty.
+	// controllerName identifies the controller that processes the AdmissionCheck,
+	// not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ControllerName string `json:"controllerName"`
 
-	// RetryDelayMinutes specifies how long to keep the workload suspended
-	// after a failed check (after it transitioned to False).
-	// After that the check state goes to "Unknown".
+	// RetryDelayMinutes specifies how long to keep the workload suspended after
+	// a failed check (after it transitioned to False). After that the check
+	// state goes to “Unknown”. The default is 15 min.
```

  ```suggestion
  	// state goes to "Unknown". The default is 15 min.
  ```
  
  It looks like your keyboard or IDE is configured with some form of special character.

- **apis/kueue/v1beta1/admissioncheck_types.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:23:47Z
```diff
@@ -47,22 +46,22 @@ const (
 
 // AdmissionCheckSpec defines the desired state of AdmissionCheck
 type AdmissionCheckSpec struct {
-	// controllerName is name of the controller which will actually perform
-	// the checks. This is the name with which controller identifies with,
-	// not necessarily a K8S Pod or Deployment name. Cannot be empty.
+	// controllerName identifies the controller that processes the AdmissionCheck,
+	// not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ControllerName string `json:"controllerName"`
 
-	// RetryDelayMinutes specifies how long to keep the workload suspended
-	// after a failed check (after it transitioned to False).
-	// After that the check state goes to "Unknown".
+	// RetryDelayMinutes specifies how long to keep the workload suspended after
+	// a failed check (after it transitioned to False). After that the check
```

  ```suggestion
  	// a failed check (after it transitioned to False). When the delay period has passed, the check
  ```

- **site/content/en/docs/concepts/admission_check.md:10** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:26:33Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
```

  ```suggestion
  After Kueue has reserved quota for a Workload, Kueue runs all the admission checks configured
  in the ClusterQueue concurrently.
  ```

- **site/content/en/docs/concepts/admission_check.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:27:08Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
+All of the AdmissionChecks have to provide a positive signal to the Workload before it can be [Admitted](/docs/concepts#admission).
```

  Kueue can only admit a Workload when all of the AdmissionChecks have provided a positive signal for the Workload.

- **site/content/en/docs/concepts/admission_check.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:28:39Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
+All of the AdmissionChecks have to provide a positive signal to the Workload before it can be [Admitted](/docs/concepts#admission).
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- **controllerName** - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes (deprecated)** - specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to “Unknown”. The default is 15 min.
```

  Sorry, can you remind me where/when this was deprecated?
  
  If it's deprecated, it's worth mentioning in the API comment too.

- **site/content/en/docs/concepts/admission_check.md:82** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:29:55Z
```diff
@@ -79,13 +77,18 @@ spec:
 ```
 
 
-### AdmissionCheckState
-
-AdmissionCheckState is the way the state of an AdmissionCheck for a specific Workload is tracked.
+### AdmissionCheckStates
 
+AdmissionCheckStates is representation of the AdmissionCheck's state for a specific Workload.
```

  ```suggestion
  An AdmissionCheckState is the representation of the AdmissionCheck's state for a specific Workload.
  ```
  ?

- **site/content/en/docs/concepts/admission_check.md:118** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:33:44Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` th Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
```

  still pending

- **site/content/en/docs/concepts/admission_check.md:122** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-17T18:34:39Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` the Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
+  - Event `EvictedDueToAdmissionCheck` is emitted
 
-Is a component that monitors Workloads maintaining the content of its specific `admissionCheckStates` and the `Active` condition of the AdmissionChecks it's  controlling.
-The logic for how an AdmissionCheck changes states is not part of Kueue.
+If any of the Workload's AdmissionCheck is in the `Rejected` state:
+  - Workload is deactivated - `workload.Spec.Active` is set to `False`
```

  Maybe we should have a paragraph in the Workload documentation to explain what active means and how to "reactivate" a Workload.
  And link to it from here.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-17T21:58:17Z

- **site/content/en/docs/concepts/admission_check.md:11** by [@PBundyra](https://github.com/PBundyra) — 2024-06-17T21:58:17Z
```diff
@@ -1,23 +1,22 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+Kueue runs all AdmissionChecks at the same time, after a Workload has reserved the quota.
+All of the AdmissionChecks have to provide a positive signal to the Workload before it can be [Admitted](/docs/concepts#admission).
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-18T07:08:35Z

- **site/content/en/docs/concepts/admission_check.md:122** by [@PBundyra](https://github.com/PBundyra) — 2024-06-18T07:08:35Z
```diff
@@ -98,27 +101,28 @@ status:
     - annotations:
         cluster-autoscaler.kubernetes.io/consume-provisioning-request: job-prov-job-9815b-sample-prov
       name: main
-    state: Ready
+    state: Pending
   <...>
 ```
 
-A list of states being maintained in the Status of all the monitored Workloads.
+Kueue ensures that the list of the Workload's AdmissionCheckStates is in sync with the list of the Workload's ClusterQueue.
+When a user adds a new AdmissionCheck, Kueue adds it to the Workload's AdmissionCheckStates with the `Pending` state.
+If a Workload is admitted, adding a new AdmissionCheck does not evict the Workload.
 
-Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+### Admitting Workload with AdmissionChecks
 
-- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
-- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
-  - If `Admitted` the workload is evicted.
-  - If the workload has `QuotaReservation` it will be release released.
-  - The workload is marked as 'Finished' with a relevant failure message.
+Once a Workload has `QuotaReservation` condition set to `True`, and all of its AdmissionChecks are in `Ready` state the Workload will become `Admitted`.
 
-### Admission Check Controller
+If any of the Workload's AdmissionCheck is in the `Retry` state:
+  - If `Admitted` the Workload is evicted - Workload will have an `Evicted` condition in `workload.Status.Condition` with `AdmissionCheck` as a `Reason`
+  - If the Workload has `QuotaReservation` it will be release released.
+  - Event `EvictedDueToAdmissionCheck` is emitted
 
-Is a component that monitors Workloads maintaining the content of its specific `admissionCheckStates` and the `Active` condition of the AdmissionChecks it's  controlling.
-The logic for how an AdmissionCheck changes states is not part of Kueue.
+If any of the Workload's AdmissionCheck is in the `Rejected` state:
+  - Workload is deactivated - `workload.Spec.Active` is set to `False`
```

  We already have a paragraph for that. I linked it

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-18T13:14:16Z

- **apis/kueue/v1beta1/admissioncheck_types.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T13:14:15Z
```diff
@@ -47,22 +46,22 @@ const (
 
 // AdmissionCheckSpec defines the desired state of AdmissionCheck
 type AdmissionCheckSpec struct {
-	// controllerName is name of the controller which will actually perform
-	// the checks. This is the name with which controller identifies with,
-	// not necessarily a K8S Pod or Deployment name. Cannot be empty.
+	// controllerName identifies the controller that processes the AdmissionCheck,
+	// not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ControllerName string `json:"controllerName"`
 
-	// RetryDelayMinutes specifies how long to keep the workload suspended
-	// after a failed check (after it transitioned to False).
-	// After that the check state goes to "Unknown".
+	// RetryDelayMinutes specifies how long to keep the workload suspended after
+	// a failed check (after it transitioned to False). When the delay period has passed, the check
+	// state goes to "Unknown". The default is 15 min.
```

  Add here that this is deprecated

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-18T13:14:31Z

- **site/content/en/docs/concepts/admission_check.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T13:14:31Z
```diff
@@ -1,23 +1,23 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+After Kueue has reserved quota for a Workload, Kueue runs all the admission checks configured
+in the ClusterQueue concurrently.
+Kueue can only admit a Workload when all of the AdmissionChecks have provided a positive signal for the Workload.
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- **controllerName** - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
```

  ```suggestion
  - `controllerName` - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-18T16:52:52Z

- **site/content/en/docs/concepts/admission_check.md:19** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T16:52:38Z
```diff
@@ -1,23 +1,23 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+After Kueue has reserved quota for a Workload, Kueue runs all the admission checks configured
+in the ClusterQueue concurrently.
+Kueue can only admit a Workload when all of the AdmissionChecks have provided a positive signal for the Workload.
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- `controllerName` - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes (deprecated)** - specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
```

  ```suggestion
  - `retryDelayMinutes` (deprecated) - specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
  ```

- **site/content/en/docs/concepts/admission_check.md:20** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T16:52:46Z
```diff
@@ -1,23 +1,23 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
 weight: 6
 description: >
-  A mechanism allowing internal or external components to influence the timing of
-  workloads admission.
+  Mechanism allowing internal or external components to influence the workload's admission.
 ---
 
 AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
-When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
-positive signal to the Workload before it can be [Admitted](https://kueue.sigs.k8s.io/docs/concepts#admission).
+After Kueue has reserved quota for a Workload, Kueue runs all the admission checks configured
+in the ClusterQueue concurrently.
+Kueue can only admit a Workload when all of the AdmissionChecks have provided a positive signal for the Workload.
 
-### AdmissionCheck
+### API
 
-AdmissionCheck is a non-namespaced API object used to define details about an admission check like:
+AdmissionCheck is a non-namespaced API object used to define details about an admission check:
 
-- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
-- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
-- **parameters** - Identifies an additional resource providing additional parameters for the check.
+- `controllerName` - identifies the controller that processes the AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes (deprecated)** - specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+- **parameters** - identifies a configuration with additional parameters for the check.
```

  ```suggestion
  - `parameters` - identifies a configuration with additional parameters for the check.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-19T09:20:45Z

- **apis/kueue/v1beta1/admissioncheck_types.go:57** by [@PBundyra](https://github.com/PBundyra) — 2024-06-19T09:20:45Z
```diff
@@ -47,22 +46,22 @@ const (
 
 // AdmissionCheckSpec defines the desired state of AdmissionCheck
 type AdmissionCheckSpec struct {
-	// controllerName is name of the controller which will actually perform
-	// the checks. This is the name with which controller identifies with,
-	// not necessarily a K8S Pod or Deployment name. Cannot be empty.
+	// controllerName identifies the controller that processes the AdmissionCheck,
+	// not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
 	// +kubebuilder:validation:Required
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ControllerName string `json:"controllerName"`
 
-	// RetryDelayMinutes specifies how long to keep the workload suspended
-	// after a failed check (after it transitioned to False).
-	// After that the check state goes to "Unknown".
+	// RetryDelayMinutes specifies how long to keep the workload suspended after
+	// a failed check (after it transitioned to False). When the delay period has passed, the check
+	// state goes to "Unknown". The default is 15 min.
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-19T18:28:36Z

/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-25T15:50:14Z

- **site/content/en/docs/concepts/admission_check.md:3** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-25T15:50:07Z
```diff
@@ -1,23 +1,23 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
```

  @PBundyra  🧐

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-25T15:58:12Z

- **site/content/en/docs/concepts/admission_check.md:3** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T15:58:12Z
```diff
@@ -1,23 +1,23 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
```

  oh hehe, it should be `2024-06-13`
  
  As it is right now, it might prevent this page from rendering.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-25T16:00:06Z

- **site/content/en/docs/concepts/admission_check.md:3** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-25T16:00:06Z
```diff
@@ -1,23 +1,23 @@
 ---
 title: "Admission Check"
-date: 2023-10-05
+date: 2024-13-06
```

  yeah, that is literally my curious about this date :)
