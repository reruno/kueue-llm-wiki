# PR #2409: Add a new DeactivationTarget workload condition.

**Summary**: Add a new DeactivationTarget workload condition.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2409

**Last updated**: 2024-06-26T16:31:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/add-deactivation-triggered-workload-condition` → `main`
- **Created**: 2024-06-13T15:39:21Z
- **Updated**: 2024-06-26T16:31:01Z
- **Merged**: 2024-06-26T16:30:21Z
- **Closed**: 2024-06-26T16:30:21Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 7   **+90 / -32**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Added a new Workload condition called DeactivationTarget that would signalise that a Workload which has this condition should be deactivated. This condition is temporary, so it should be removed after deactivation.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2388 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-13T15:39:24Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-13T15:39:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d5ac04d124ad83a2cfe02cdab30b138995ea8e9d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/667ba1b6a886b10008ffe86f |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-13T15:39:52Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-13T16:18:16Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-19T08:30:15Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-19T08:47:40Z

/assign @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-19T09:37:09Z

>> Initial pass, I think we should also use it in case of deactivation due to Rejected admission check, cc @PBundyra

Yes, this would be another use case of the Condition.

However, I would prefer to wait for the consensus in #2388 before merging anything

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-19T10:04:10Z

> However, I would prefer to wait for the consensus in #2388 before merging anything

+1, agree, let's align on the approach before detailed review. I just did an initial pass to make sure the scope of the new condition is clarified.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-26T16:14:21Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-26T16:14:26Z

LGTM label has been added.  <details>Git tree hash: 17717c3bdae3d7a3d458b7adb5493d5eaf251aaa</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-26T16:14:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2409#issuecomment-2192092492" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2409#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T09:01:29Z

Initial pass, I think we should also use it in case of deactivation due to Rejected admission check, cc @PBundyra

- **pkg/controller/core/workload_controller.go:192** by [@mimowo](https://github.com/mimowo) — 2024-06-19T09:00:32Z
```diff
@@ -180,22 +186,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	} else {
 		var updated, evicted bool
 		message := "The workload is deactivated"
+		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
-			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
-			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The workload already has been re-queued previously, which means it doesn't have the requeueAt field.
-			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
-			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
-				((wl.Status.RequeueState == nil && ptr.Equal(r.waitForPodsReady.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt == nil &&
-						ptr.Equal(wl.Status.RequeueState.Count, r.waitForPodsReady.requeuingBackoffLimitCount))) {
+			if dtCond != nil && dtCond.Reason == kueue.WorkloadRequeuingBackoffLimitExceeded {
 				message = fmt.Sprintf("%s due to exceeding the maximum number of re-queuing retries", message)
```

  Can we take the message from the condition, and build when adding the condition?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-19T09:17:31Z

- **pkg/controller/core/workload_controller.go:192** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-19T09:17:31Z
```diff
@@ -180,22 +186,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	} else {
 		var updated, evicted bool
 		message := "The workload is deactivated"
+		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
-			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
-			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The workload already has been re-queued previously, which means it doesn't have the requeueAt field.
-			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
-			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
-				((wl.Status.RequeueState == nil && ptr.Equal(r.waitForPodsReady.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt == nil &&
-						ptr.Equal(wl.Status.RequeueState.Count, r.waitForPodsReady.requeuingBackoffLimitCount))) {
+			if dtCond != nil && dtCond.Reason == kueue.WorkloadRequeuingBackoffLimitExceeded {
 				message = fmt.Sprintf("%s due to exceeding the maximum number of re-queuing retries", message)
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-25T20:28:04Z

- **apis/kueue/v1beta1/workload_types.go:384** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T20:15:13Z
```diff
@@ -374,6 +378,10 @@ const (
 	// WorkloadLocalQueueRestarted indicates that the workload was requeued because
 	// local queue was restarted after being stopped.
 	WorkloadLocalQueueRestarted = "LocalQueueRestarted"
+
+	// WorkloadRequeuingBackoffLimitExceeded indicates that the workload exceeded max number
+	// of re-queuing retries.
+	WorkloadRequeuingBackoffLimitExceeded = "RequeuingBackoffLimitExceeded"
```

  ```suggestion
  	WorkloadRequeuingLimitExceeded = "RequeuingLimitExceeded"
  ```
  and the comment above

- **pkg/controller/core/workload_controller.go:194** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T20:19:47Z
```diff
@@ -180,22 +186,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	} else {
 		var updated, evicted bool
 		message := "The workload is deactivated"
+		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
-			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
-			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The workload already has been re-queued previously, which means it doesn't have the requeueAt field.
-			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
-			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
-				((wl.Status.RequeueState == nil && ptr.Equal(r.waitForPodsReady.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt == nil &&
-						ptr.Equal(wl.Status.RequeueState.Count, r.waitForPodsReady.requeuingBackoffLimitCount))) {
-				message = fmt.Sprintf("%s due to exceeding the maximum number of re-queuing retries", message)
+			if dtCond != nil {
+				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
```

  we should also copy or concatenate the reason.

- **pkg/controller/core/workload_controller.go:517** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T20:22:25Z
```diff
@@ -509,19 +511,20 @@ func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req c
 	return ctrl.Result{}, client.IgnoreNotFound(err)
 }
 
-// triggerDeactivationOrBackoffRequeue deactivates a workload (".spec.active"="false")
+// triggerDeactivationOrBackoffRequeue trigger deactivation of workload
 // if a re-queued number has already exceeded the limit of re-queuing backoff.
 // Otherwise, it increments a re-queueing count and update a time to be re-queued.
-// It returns true as a first value if a workload is deactivated.
+// It returns true as a first value if a workload is triggered deactivation.
```

  ```suggestion
  // It returns true as a first value if a workload triggered deactivation.
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:2112** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T20:26:57Z
```diff
@@ -2109,6 +2109,8 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
```

  comment is unnecessary here.

- **test/integration/controller/jobs/job/job_controller_test.go:2112** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T20:27:08Z
```diff
@@ -2109,6 +2109,8 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
+				g.Expect(apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)).Should(gomega.BeNil())
```

  Use `g.Expect(wl.Status.Conditions).NotTo(gomega.ContainElements`

- **test/integration/controller/jobs/pod/pod_controller_test.go:1832** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T20:28:02Z
```diff
@@ -1828,6 +1829,8 @@ var _ = ginkgo.Describe("Pod controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
```

  Same suggestions here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:43:13Z

- **test/integration/controller/jobs/job/job_controller_test.go:2112** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:43:13Z
```diff
@@ -2109,6 +2109,8 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
+				g.Expect(apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)).Should(gomega.BeNil())
```

  In this case it is enough if the message is changed and the test will pass even if the condition stays.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:51:47Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:1832** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:51:47Z
```diff
@@ -1828,6 +1829,8 @@ var _ = ginkgo.Describe("Pod controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
```

  Removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:52:01Z

- **test/integration/controller/jobs/job/job_controller_test.go:2112** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:52:00Z
```diff
@@ -2109,6 +2109,8 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
```

  Removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:52:26Z

- **pkg/controller/core/workload_controller.go:517** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:52:26Z
```diff
@@ -509,19 +511,20 @@ func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req c
 	return ctrl.Result{}, client.IgnoreNotFound(err)
 }
 
-// triggerDeactivationOrBackoffRequeue deactivates a workload (".spec.active"="false")
+// triggerDeactivationOrBackoffRequeue trigger deactivation of workload
 // if a re-queued number has already exceeded the limit of re-queuing backoff.
 // Otherwise, it increments a re-queueing count and update a time to be re-queued.
-// It returns true as a first value if a workload is deactivated.
+// It returns true as a first value if a workload is triggered deactivation.
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:52:55Z

- **apis/kueue/v1beta1/workload_types.go:384** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:52:55Z
```diff
@@ -374,6 +378,10 @@ const (
 	// WorkloadLocalQueueRestarted indicates that the workload was requeued because
 	// local queue was restarted after being stopped.
 	WorkloadLocalQueueRestarted = "LocalQueueRestarted"
+
+	// WorkloadRequeuingBackoffLimitExceeded indicates that the workload exceeded max number
+	// of re-queuing retries.
+	WorkloadRequeuingBackoffLimitExceeded = "RequeuingBackoffLimitExceeded"
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:53:58Z

- **pkg/controller/core/workload_controller.go:194** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:53:58Z
```diff
@@ -180,22 +186,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	} else {
 		var updated, evicted bool
 		message := "The workload is deactivated"
+		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
-			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
-			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The workload already has been re-queued previously, which means it doesn't have the requeueAt field.
-			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
-			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
-				((wl.Status.RequeueState == nil && ptr.Equal(r.waitForPodsReady.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt == nil &&
-						ptr.Equal(wl.Status.RequeueState.Count, r.waitForPodsReady.requeuingBackoffLimitCount))) {
-				message = fmt.Sprintf("%s due to exceeding the maximum number of re-queuing retries", message)
+			if dtCond != nil {
+				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
```

  Is it ok? 
  
  ```
  The workload is deactivated due to exceeding the maximum number of re-queuing retries (RequeuingLimitExceeded)
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-25T21:34:36Z

- **pkg/controller/core/workload_controller.go:194** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T21:34:36Z
```diff
@@ -180,22 +186,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	} else {
 		var updated, evicted bool
 		message := "The workload is deactivated"
+		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
-			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
-			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The workload already has been re-queued previously, which means it doesn't have the requeueAt field.
-			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
-			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
-				((wl.Status.RequeueState == nil && ptr.Equal(r.waitForPodsReady.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt == nil &&
-						ptr.Equal(wl.Status.RequeueState.Count, r.waitForPodsReady.requeuingBackoffLimitCount))) {
-				message = fmt.Sprintf("%s due to exceeding the maximum number of re-queuing retries", message)
+			if dtCond != nil {
+				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
```

  No, not in the message. It should be in the reason field, which is machine-friendly.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-25T21:35:19Z

- **test/integration/controller/jobs/job/job_controller_test.go:2112** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T21:35:19Z
```diff
@@ -2109,6 +2109,8 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
+				g.Expect(apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)).Should(gomega.BeNil())
```

  you can add `cmpopts.IgnoreFields`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-25T21:37:18Z

- **test/integration/controller/jobs/job/job_controller_test.go:2112** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T21:37:18Z
```diff
@@ -2109,6 +2109,8 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 						Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
 					}, util.IgnoreConditionTimestampsAndObservedGeneration),
 				))
+				// Fake client that are using on unit tests doesn't allow us to check it.
+				g.Expect(apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)).Should(gomega.BeNil())
```

  you know, maybe this is fine. It's more problematic if you use booleans. But since FindStatusCondition returns the full condition, it's obvious what the problem is if this check fails.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-26T05:07:49Z

- **pkg/controller/core/workload_controller.go:194** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-26T05:07:49Z
```diff
@@ -180,22 +186,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	} else {
 		var updated, evicted bool
 		message := "The workload is deactivated"
+		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
-			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
-			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The workload already has been re-queued previously, which means it doesn't have the requeueAt field.
-			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
-			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
-				((wl.Status.RequeueState == nil && ptr.Equal(r.waitForPodsReady.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt == nil &&
-						ptr.Equal(wl.Status.RequeueState.Count, r.waitForPodsReady.requeuingBackoffLimitCount))) {
-				message = fmt.Sprintf("%s due to exceeding the maximum number of re-queuing retries", message)
+			if dtCond != nil {
+				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
```

  Ah, got it. So in result we have for eviction condition `InactiveWorkloadRequeuingLimitExceeded` and for event `EvictedDueToInactiveWorkloadRequeuingLimitExceeded`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-26T16:09:59Z

- **pkg/controller/core/workload_controller.go:194** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-26T16:09:59Z
```diff
@@ -180,22 +186,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	} else {
 		var updated, evicted bool
 		message := "The workload is deactivated"
+		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
 		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
-			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
-			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The workload already has been re-queued previously, which means it doesn't have the requeueAt field.
-			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
-			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
-				((wl.Status.RequeueState == nil && ptr.Equal(r.waitForPodsReady.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt == nil &&
-						ptr.Equal(wl.Status.RequeueState.Count, r.waitForPodsReady.requeuingBackoffLimitCount))) {
-				message = fmt.Sprintf("%s due to exceeding the maximum number of re-queuing retries", message)
+			if dtCond != nil {
+				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
```

  That event reason is long, but if the API validation allows it, go for it :)
