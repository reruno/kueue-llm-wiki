# PR #2436: [multikueue] KEP add subcomponents and integration details.

**Summary**: [multikueue] KEP add subcomponents and integration details.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2436

**Last updated**: 2024-06-25T15:33:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-kep-components-details` → `main`
- **Created**: 2024-06-18T11:06:05Z
- **Updated**: 2024-06-25T15:33:49Z
- **Merged**: 2024-06-19T18:58:42Z
- **Closed**: 2024-06-19T18:58:42Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 2   **+109 / -14**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add subcomponents and integration details in MultiKueue's KEP. 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@trasc](https://github.com/trasc) — 2024-06-18T11:06:23Z

/hold 
for #2373

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-18T11:06:24Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 089a092e32e668a9a547897b962709d937146c13 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6672fa359ac5900008e23698 |

### Comment by [@trasc](https://github.com/trasc) — 2024-06-19T10:42:52Z

/unhold

/cc @tenzen-y

### Comment by [@trasc](https://github.com/trasc) — 2024-06-19T12:30:17Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-19T18:43:28Z

/lgtm
/approve

Many thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-19T18:43:33Z

LGTM label has been added.  <details>Git tree hash: 90213e222cd230c89e1497cddf5b5b02c8cac9d5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-19T18:43:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2436#issuecomment-2179304777" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2436#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-06-18T12:19:05Z

- **keps/693-multikueue/README.md:247** by [@mszadkow](https://github.com/mszadkow) — 2024-06-18T12:19:04Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but ware not deleted by the Workload Controller 
```

  `ware` -> `were`

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-06-18T12:24:57Z

- **keps/693-multikueue/README.md:285** by [@mszadkow](https://github.com/mszadkow) — 2024-06-18T12:24:56Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but ware not deleted by the Workload Controller 
+in the normal flow, due to a temporary connectivity outage to the remote cluster.
+
+### Jobs abstraction
+
+In order to work with different types of jobs the MultiKueue Controller will use two abstraction
+interfaces:
+
+```go
+// MultiKueueAdapter interface needed for MultiKueue job delegation.
+type MultiKueueAdapter interface {
+	// Creates the Job object in the worker cluster using remote client, if not already created.
+	// Copy the status from the remote job if already exists.
+	SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error
+	// Deletes the Job in the worker cluster.
+	DeleteRemoteObject(ctx context.Context, remoteClient client.Client, key types.NamespacedName) error
+	// IsJobManagedByKueue returns:
+	// - a bool indicating if the job object identified by key is managed by kueue and can be delegated.
+	// - a reason indicating why the job is not managed by Kueue
+	// - any API error encountered during the check
+	IsJobManagedByKueue(ctx context.Context, localClient client.Client, key types.NamespacedName, controllerName string) (bool, string, error)
+	// KeepAdmissionCheckPending returns true if the state of the multikueue admission check should be
+	// kept Pending while the job runs in a worker. This might be needed to keep the managers job
+	// suspended and not start the execution locally.
+	KeepAdmissionCheckPending() bool
+	// GVK returns GVK (Group Version Kind) for the job.
+	GVK() schema.GroupVersionKind
+}
+```
+Used by the [Workload Controller](#workload-controller) to interact with the owner of the reconciled workloads.
+
+
+```
+
+// MultiKueueWatcher optional interface that can be implemented by a MultiKueueAdapter
+// to receive job related watch events from the worker cluster.
+// If not implemented, MultiKueue will only receive events related to the job's workload.
+type MultiKueueWatcher interface {
+	// returns an empty list of objects
```

  what is the purpose of receiving empty list?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-18T12:31:58Z

- **keps/693-multikueue/README.md:285** by [@trasc](https://github.com/trasc) — 2024-06-18T12:31:58Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but ware not deleted by the Workload Controller 
+in the normal flow, due to a temporary connectivity outage to the remote cluster.
+
+### Jobs abstraction
+
+In order to work with different types of jobs the MultiKueue Controller will use two abstraction
+interfaces:
+
+```go
+// MultiKueueAdapter interface needed for MultiKueue job delegation.
+type MultiKueueAdapter interface {
+	// Creates the Job object in the worker cluster using remote client, if not already created.
+	// Copy the status from the remote job if already exists.
+	SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error
+	// Deletes the Job in the worker cluster.
+	DeleteRemoteObject(ctx context.Context, remoteClient client.Client, key types.NamespacedName) error
+	// IsJobManagedByKueue returns:
+	// - a bool indicating if the job object identified by key is managed by kueue and can be delegated.
+	// - a reason indicating why the job is not managed by Kueue
+	// - any API error encountered during the check
+	IsJobManagedByKueue(ctx context.Context, localClient client.Client, key types.NamespacedName, controllerName string) (bool, string, error)
+	// KeepAdmissionCheckPending returns true if the state of the multikueue admission check should be
+	// kept Pending while the job runs in a worker. This might be needed to keep the managers job
+	// suspended and not start the execution locally.
+	KeepAdmissionCheckPending() bool
+	// GVK returns GVK (Group Version Kind) for the job.
+	GVK() schema.GroupVersionKind
+}
+```
+Used by the [Workload Controller](#workload-controller) to interact with the owner of the reconciled workloads.
+
+
+```
+
+// MultiKueueWatcher optional interface that can be implemented by a MultiKueueAdapter
+// to receive job related watch events from the worker cluster.
+// If not implemented, MultiKueue will only receive events related to the job's workload.
+type MultiKueueWatcher interface {
+	// returns an empty list of objects
```

  it's used when starting the watch for the objects, https://github.com/kubernetes-sigs/kueue/blob/e26d2fd418eb8b7bebb3c578d36e5bc18b5440e2/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L194

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-06-18T12:38:16Z

- **keps/693-multikueue/README.md:293** by [@mszadkow](https://github.com/mszadkow) — 2024-06-18T12:38:16Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but were not deleted by the Workload Controller 
+in the normal flow, due to a temporary connectivity outage to the remote cluster.
+
+### Jobs abstraction
+
+In order to work with different types of jobs the MultiKueue Controller will use two abstraction
+interfaces:
+
+```go
+// MultiKueueAdapter interface needed for MultiKueue job delegation.
+type MultiKueueAdapter interface {
+	// Creates the Job object in the worker cluster using remote client, if not already created.
+	// Copy the status from the remote job if already exists.
+	SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error
+	// Deletes the Job in the worker cluster.
+	DeleteRemoteObject(ctx context.Context, remoteClient client.Client, key types.NamespacedName) error
+	// IsJobManagedByKueue returns:
+	// - a bool indicating if the job object identified by key is managed by kueue and can be delegated.
+	// - a reason indicating why the job is not managed by Kueue
+	// - any API error encountered during the check
+	IsJobManagedByKueue(ctx context.Context, localClient client.Client, key types.NamespacedName, controllerName string) (bool, string, error)
+	// KeepAdmissionCheckPending returns true if the state of the multikueue admission check should be
+	// kept Pending while the job runs in a worker. This might be needed to keep the managers job
+	// suspended and not start the execution locally.
+	KeepAdmissionCheckPending() bool
+	// GVK returns GVK (Group Version Kind) for the job.
+	GVK() schema.GroupVersionKind
+}
+```
+Used by the [Workload Controller](#workload-controller) to interact with the owner of the reconciled workloads.
+
+
+```
+
+// MultiKueueWatcher optional interface that can be implemented by a MultiKueueAdapter
+// to receive job related watch events from the worker cluster.
+// If not implemented, MultiKueue will only receive events related to the job's workload.
+type MultiKueueWatcher interface {
+	// returns an empty list of objects
+	GetEmptyList() client.ObjectList
+	// returns the key of the workload of interest
+	// - the object name for workloads
+	// - the prebuilt workload for job types
+	GetWorkloadKey(runtime.Object) (types.NamespacedName, error)
+}
+```
+Used by the [MultiKueueCluster Controller](#multikueuecluster-controller) to start watching job updates in the remote clusters and convert them
```

  `convert them` or `convert their`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-18T22:16:54Z

And also please add yourself as one of the authors of this KEP. Credit were credit is due :)

- **keps/693-multikueue/README.md:215** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T22:04:04Z
```diff
@@ -188,16 +194,28 @@ type MultiKueueClusterStatus {
    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 }
 ```
+### Subcomponents
 
-MultiKueue controller will monitor all cluster definitions and maintain 
+#### MultiKueueCluster Controller
+
+Will monitor all cluster definitions and maintain 
 the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
+MultiKueueCluster status and Events. MultiKueue controller 
 will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
+clients will also be recreated and attempt to reconnect when the connection to the
+target cluster is lost.
 
 Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
 provider/environment dependant.
 
+#### AdmissionCheck Controller
+
+Will monitor the AdmissionChecks associated with MultiKueue (by ControllerName) and maintain
+their `Active` status condition based on the validity of their MultiKueueConfig and `Active`
+state of the MultiKueueClusters in use.
```

  Let's add when a check is considered active: I suppose as long as there is one MKCluster that is active?

- **keps/693-multikueue/README.md:229** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T22:10:00Z
```diff
@@ -188,16 +194,28 @@ type MultiKueueClusterStatus {
    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 }
 ```
+### Subcomponents
 
-MultiKueue controller will monitor all cluster definitions and maintain 
+#### MultiKueueCluster Controller
+
+Will monitor all cluster definitions and maintain 
 the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
+MultiKueueCluster status and Events. MultiKueue controller 
 will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
+clients will also be recreated and attempt to reconnect when the connection to the
+target cluster is lost.
 
 Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
 provider/environment dependant.
 
+#### AdmissionCheck Controller
+
+Will monitor the AdmissionChecks associated with MultiKueue (by ControllerName) and maintain
+their `Active` status condition based on the validity of their MultiKueueConfig and `Active`
+state of the MultiKueueClusters in use.
+
+#### Workload Controller
```

  Add a sentence or two to summarize the overall responsibility of this subcomponent.

- **keps/693-multikueue/README.md:221** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T22:11:50Z
```diff
@@ -188,16 +194,28 @@ type MultiKueueClusterStatus {
    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 }
 ```
+### Subcomponents
 
-MultiKueue controller will monitor all cluster definitions and maintain 
+#### MultiKueueCluster Controller
+
+Will monitor all cluster definitions and maintain 
 the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
+MultiKueueCluster status and Events. MultiKueue controller 
 will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
+clients will also be recreated and attempt to reconnect when the connection to the
+target cluster is lost.
 
 Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
 provider/environment dependant.
 
+#### AdmissionCheck Controller
+
+Will monitor the AdmissionChecks associated with MultiKueue (by ControllerName) and maintain
+their `Active` status condition based on the validity of their MultiKueueConfig and `Active`
+state of the MultiKueueClusters in use.
+
+#### Workload Controller
+
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
```

  These sentences describe the overall behavior and semantics of MK. Please extract them out and put it before the "subcomponents" section.

- **keps/693-multikueue/README.md:247** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T22:13:43Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but were not deleted by the Workload Controller 
```

  were

- **keps/693-multikueue/README.md:252** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T22:14:34Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
```

  Does this run periodically or react to some events?

- **keps/693-multikueue/README.md:267** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-18T22:16:01Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but were not deleted by the Workload Controller 
+in the normal flow, due to a temporary connectivity outage to the remote cluster.
+
+### Jobs abstraction
+
+In order to work with different types of jobs the MultiKueue Controller will use two abstraction
+interfaces:
+
+```go
+// MultiKueueAdapter interface needed for MultiKueue job delegation.
+type MultiKueueAdapter interface {
```

  Let's not put the whole interface here, just the overall intent and list the important operations (sync, delete)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T06:57:48Z

- **keps/693-multikueue/README.md:215** by [@trasc](https://github.com/trasc) — 2024-06-19T06:03:26Z
```diff
@@ -188,16 +194,28 @@ type MultiKueueClusterStatus {
    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 }
 ```
+### Subcomponents
 
-MultiKueue controller will monitor all cluster definitions and maintain 
+#### MultiKueueCluster Controller
+
+Will monitor all cluster definitions and maintain 
 the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
+MultiKueueCluster status and Events. MultiKueue controller 
 will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
+clients will also be recreated and attempt to reconnect when the connection to the
+target cluster is lost.
 
 Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
 provider/environment dependant.
 
+#### AdmissionCheck Controller
+
+Will monitor the AdmissionChecks associated with MultiKueue (by ControllerName) and maintain
+their `Active` status condition based on the validity of their MultiKueueConfig and `Active`
+state of the MultiKueueClusters in use.
```

  done

- **keps/693-multikueue/README.md:252** by [@trasc](https://github.com/trasc) — 2024-06-19T06:04:11Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
```

  periodically, mentioned.

- **keps/693-multikueue/README.md:221** by [@trasc](https://github.com/trasc) — 2024-06-19T06:04:52Z
```diff
@@ -188,16 +194,28 @@ type MultiKueueClusterStatus {
    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 }
 ```
+### Subcomponents
 
-MultiKueue controller will monitor all cluster definitions and maintain 
+#### MultiKueueCluster Controller
+
+Will monitor all cluster definitions and maintain 
 the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
+MultiKueueCluster status and Events. MultiKueue controller 
 will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
+clients will also be recreated and attempt to reconnect when the connection to the
+target cluster is lost.
 
 Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
 provider/environment dependant.
 
+#### AdmissionCheck Controller
+
+Will monitor the AdmissionChecks associated with MultiKueue (by ControllerName) and maintain
+their `Active` status condition based on the validity of their MultiKueueConfig and `Active`
+state of the MultiKueueClusters in use.
+
+#### Workload Controller
+
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
```

  Done.

- **keps/693-multikueue/README.md:229** by [@trasc](https://github.com/trasc) — 2024-06-19T06:04:59Z
```diff
@@ -188,16 +194,28 @@ type MultiKueueClusterStatus {
    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 }
 ```
+### Subcomponents
 
-MultiKueue controller will monitor all cluster definitions and maintain 
+#### MultiKueueCluster Controller
+
+Will monitor all cluster definitions and maintain 
 the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
+MultiKueueCluster status and Events. MultiKueue controller 
 will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
+clients will also be recreated and attempt to reconnect when the connection to the
+target cluster is lost.
 
 Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
 provider/environment dependant.
 
+#### AdmissionCheck Controller
+
+Will monitor the AdmissionChecks associated with MultiKueue (by ControllerName) and maintain
+their `Active` status condition based on the validity of their MultiKueueConfig and `Active`
+state of the MultiKueueClusters in use.
+
+#### Workload Controller
```

  done

- **keps/693-multikueue/README.md:247** by [@trasc](https://github.com/trasc) — 2024-06-19T06:06:11Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but were not deleted by the Workload Controller 
```

  done

- **keps/693-multikueue/README.md:267** by [@trasc](https://github.com/trasc) — 2024-06-19T06:45:51Z
```diff
@@ -223,7 +241,59 @@ If there is enough of global quota, the unknown admitted workloads would be re-a
 the management cluster. If not, some workloads will be preempted to meet the global quota.
 In case of duplicates, all but one of them will be removed.
 
-#### Follow ups ideas
+#### Garbage Collector
+
+Will monitor the Workloads in the remote clusters to detect and remove
+those that ware created by MultiKueue but were not deleted by the Workload Controller 
+in the normal flow, due to a temporary connectivity outage to the remote cluster.
+
+### Jobs abstraction
+
+In order to work with different types of jobs the MultiKueue Controller will use two abstraction
+interfaces:
+
+```go
+// MultiKueueAdapter interface needed for MultiKueue job delegation.
+type MultiKueueAdapter interface {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T10:33:21Z

- **keps/693-multikueue/kep.yaml:25** by [@mimowo](https://github.com/mimowo) — 2024-06-19T10:33:21Z
```diff
@@ -20,7 +21,7 @@ latest-milestone: "v0.6"
 
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
-  beta: "v0.7"
+  beta: "v0.8"
```

  nit: I think this should actually be 0.6, as this is the first release which satisfies the "beta" criteria. The latest release with significant updates is indicated with `latest-milestone` so it should be "0.8".

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T10:40:57Z

- **keps/693-multikueue/kep.yaml:25** by [@trasc](https://github.com/trasc) — 2024-06-19T10:40:57Z
```diff
@@ -20,7 +21,7 @@ latest-milestone: "v0.6"
 
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
-  beta: "v0.7"
+  beta: "v0.8"
```

  We can update the `latest-milestone` as well , but it think the beta target should be updated as well the comment says:
  
  "The milestone at which this feature was, **or is targeted to be**, at each stage."

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T10:46:19Z

- **keps/693-multikueue/README.md:208** by [@mimowo](https://github.com/mimowo) — 2024-06-19T10:46:19Z
```diff
@@ -189,23 +197,40 @@ type MultiKueueClusterStatus {
 }
 ```
 
-MultiKueue controller will monitor all cluster definitions and maintain 
-the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
-will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
-
-Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
-provider/environment dependant.
-
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
 Worker ClusterQueue definitions may be different than in the management cluster. For example,
 quota settings may be specific to the given location. And/or cluster queue may have different
 admission checks, use ProvisioningRequest, etc.
 
-When distributing the workloads across clusters MultiKueue controller will first create
+### Subcomponents
```

  In the API section of "Design Details" could you please include the global API configuration for the MultiKueue AC?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T10:47:10Z

Great! I think this update will be a very valuable resource when designing new features on top of the MultiKueue. 

Left two recommendations.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T10:53:11Z

- **keps/693-multikueue/kep.yaml:25** by [@mimowo](https://github.com/mimowo) — 2024-06-19T10:53:11Z
```diff
@@ -20,7 +21,7 @@ latest-milestone: "v0.6"
 
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
-  beta: "v0.7"
+  beta: "v0.8"
```

  Yeah, so MultiKueue was targeted to be at Beta in 0.6. I had the discussion recently in the core k8s and the sig-apps lead supported that interpretation.  Otherwise we lose the information what was the first release at a given stage, which might be relevant. However, I don't find it blocking, rather a nit.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T11:00:04Z

- **keps/693-multikueue/README.md:208** by [@trasc](https://github.com/trasc) — 2024-06-19T11:00:03Z
```diff
@@ -189,23 +197,40 @@ type MultiKueueClusterStatus {
 }
 ```
 
-MultiKueue controller will monitor all cluster definitions and maintain 
-the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
-will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
-
-Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
-provider/environment dependant.
-
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
 Worker ClusterQueue definitions may be different than in the management cluster. For example,
 quota settings may be specific to the given location. And/or cluster queue may have different
 admission checks, use ProvisioningRequest, etc.
 
-When distributing the workloads across clusters MultiKueue controller will first create
+### Subcomponents
```

  As in yaml objects to be to be created in the cluster? , Like we have here https://kueue.sigs.k8s.io/docs/tasks/manage/setup_multikueue/#create-a-sample-setup ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T11:04:19Z

- **keps/693-multikueue/README.md:208** by [@mimowo](https://github.com/mimowo) — 2024-06-19T11:04:19Z
```diff
@@ -189,23 +197,40 @@ type MultiKueueClusterStatus {
 }
 ```
 
-MultiKueue controller will monitor all cluster definitions and maintain 
-the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
-will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
-
-Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
-provider/environment dependant.
-
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
 Worker ClusterQueue definitions may be different than in the management cluster. For example,
 quota settings may be specific to the given location. And/or cluster queue may have different
 admission checks, use ProvisioningRequest, etc.
 
-When distributing the workloads across clusters MultiKueue controller will first create
+### Subcomponents
```

  I meant global config API as here: https://github.com/kubernetes-sigs/kueue/blob/3f46a781dd43b74ec64514f6cee7cb7c32ffbd3b/apis/config/v1beta1/configuration_types.go#L211C6-L231

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T11:26:45Z

- **keps/693-multikueue/kep.yaml:25** by [@trasc](https://github.com/trasc) — 2024-06-19T11:26:45Z
```diff
@@ -20,7 +21,7 @@ latest-milestone: "v0.6"
 
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
-  beta: "v0.7"
+  beta: "v0.8"
```

  updated

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T11:27:04Z

- **keps/693-multikueue/README.md:208** by [@trasc](https://github.com/trasc) — 2024-06-19T11:27:04Z
```diff
@@ -189,23 +197,40 @@ type MultiKueueClusterStatus {
 }
 ```
 
-MultiKueue controller will monitor all cluster definitions and maintain 
-the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
-will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
-
-Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
-provider/environment dependant.
-
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
 Worker ClusterQueue definitions may be different than in the management cluster. For example,
 quota settings may be specific to the given location. And/or cluster queue may have different
 admission checks, use ProvisioningRequest, etc.
 
-When distributing the workloads across clusters MultiKueue controller will first create
+### Subcomponents
```

  I don't think this KEP is the place for that, that only describes how kueue (as in `cmd/kueue`) configures some parameters for the AC Controller.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T12:02:55Z

- **keps/693-multikueue/kep.yaml:25** by [@mimowo](https://github.com/mimowo) — 2024-06-19T12:02:55Z
```diff
@@ -20,7 +21,7 @@ latest-milestone: "v0.6"
 
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
-  beta: "v0.7"
+  beta: "v0.8"
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T13:06:11Z

- **keps/693-multikueue/README.md:208** by [@mimowo](https://github.com/mimowo) — 2024-06-19T13:06:11Z
```diff
@@ -189,23 +197,40 @@ type MultiKueueClusterStatus {
 }
 ```
 
-MultiKueue controller will monitor all cluster definitions and maintain 
-the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
-will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
-
-Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
-provider/environment dependant.
-
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
 Worker ClusterQueue definitions may be different than in the management cluster. For example,
 quota settings may be specific to the given location. And/or cluster queue may have different
 admission checks, use ProvisioningRequest, etc.
 
-When distributing the workloads across clusters MultiKueue controller will first create
+### Subcomponents
```

  Having the global API for MultiKueue discussed in KEP will help us to make informed decisions answering questions like: where to place some user-facing APIs? Is it global or per cluster, per admission check? 
  
  I think we will have such considerations when designing the extension point for the custom jobs in MultiKueue, when thinking if this should it be global, or per admission check.
  
  We added the global configuration to KEPs it in the past, for example for [wait for pods ready](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kueue-configuration-api).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-19T13:15:50Z

- **keps/693-multikueue/README.md:208** by [@mimowo](https://github.com/mimowo) — 2024-06-19T13:15:50Z
```diff
@@ -189,23 +197,40 @@ type MultiKueueClusterStatus {
 }
 ```
 
-MultiKueue controller will monitor all cluster definitions and maintain 
-the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
-will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
-
-Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
-provider/environment dependant.
-
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
 Worker ClusterQueue definitions may be different than in the management cluster. For example,
 quota settings may be specific to the given location. And/or cluster queue may have different
 admission checks, use ProvisioningRequest, etc.
 
-When distributing the workloads across clusters MultiKueue controller will first create
+### Subcomponents
```

  In other words - both global API and scoped APIs are admin facing APIs for MultiKueue (and as such part of the API design for MK). I think the distinction if they are global, or per some scope is secondary.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-19T15:34:44Z

- **keps/693-multikueue/README.md:208** by [@trasc](https://github.com/trasc) — 2024-06-19T15:34:44Z
```diff
@@ -189,23 +197,40 @@ type MultiKueueClusterStatus {
 }
 ```
 
-MultiKueue controller will monitor all cluster definitions and maintain 
-the Kube clients for all of them. Any connectivity problems will be reported both in
-MultiKueueCluster status as well as AdmissionCheckStatus and Events. MultiKueue controller 
-will make sure that whenever the kubeconfig is refreshed, the appropriate 
-clients will also be recreated.
-
-Creation of kubeconfig files is outside of the MultiKueue scope, and is cloud
-provider/environment dependant.
-
 MultiKueue controller, when pushing workloads to the worker clusters, will use the same 
 namespace and local queue names as were used in the management cluster. It is user's 
 responsibility to set up the appropriate namespaces and local queues.
 Worker ClusterQueue definitions may be different than in the management cluster. For example,
 quota settings may be specific to the given location. And/or cluster queue may have different
 admission checks, use ProvisioningRequest, etc.
 
-When distributing the workloads across clusters MultiKueue controller will first create
+### Subcomponents
```

  added

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-25T15:33:49Z

LGTM
Thank you for this updating.
