# PR #245: fix: update failure in requeue

**Summary**: fix: update failure in requeue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/245

**Last updated**: 2022-05-06T16:17:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `bug/udpate-failure-in-requeue` → `main`
- **Created**: 2022-05-03T08:52:01Z
- **Updated**: 2022-05-06T16:17:21Z
- **Merged**: 2022-05-06T16:17:07Z
- **Closed**: 2022-05-06T16:17:07Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+129 / -27**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of https://github.com/kubernetes-sigs/kueue/issues/241

#### Special notes for your reviewer:

## Discussion

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T08:52:38Z

/test pull-kueue-test-integration-main

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T08:55:21Z

/test pull-kueue-test-integration-main

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T08:59:16Z

/test pull-kueue-test-integration-main

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T09:02:05Z

/test pull-kueue-test-integration-main

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-05T16:19:09Z

address the comments. @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-06T13:54:01Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-06T13:54:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/245#issuecomment-1119648379" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/245#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T14:06:39Z

The https://github.com/kubernetes-sigs/kueue/pull/248 bug occurs, I'll take a spike.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-06T14:48:41Z

/hold

could it be related to this PR? Have you seen the same error for other PRs?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T15:03:26Z

> 

I think it's not related. I used to met this error for multi times in my local dev env. I think we can ignore the error in the pr.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T16:06:09Z

/retest

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-03T13:13:50Z

- **pkg/queue/manager.go:303** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T13:13:47Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
```

  Can you explain why you think this solves the problem?
  
  If there was a change in the object, the event would update the object, and `AddIfNotPresent` would prevent it to be added again.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-03T15:17:14Z

- **pkg/queue/manager.go:303** by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T15:17:14Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
```

  Refer to the log, we are always failing with update operation, this must be the reason that we stored the old version workload. So we should ensure to store the latest workload. The reason might be that the failed api update with the old version  requeue earlier than the newest one, then we stored the old ones. We can also find some clues from the log:
  <img width="1910" alt="image" src="https://user-images.githubusercontent.com/18364341/166482500-5182d59c-fd98-4385-b5c6-519a945a5168.png">

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-03T15:25:48Z

- **pkg/queue/manager.go:303** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T15:25:48Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
```

  But that screenshot suggests that it wasn't actually added. So I'm not sure if this solves the issue or maybe it obscures a different one.
  
  What's the link for this failure? I want to look at the entire logs.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-03T15:26:47Z

- **pkg/queue/manager.go:303** by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T15:26:47Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
```

  Here's the link https://storage.googleapis.com/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/227/pull-kueue-test-integration-main/1520594730851766272/build-log.txt

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-03T15:39:52Z

- **pkg/queue/manager.go:303** by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T15:39:52Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
```

  <img width="1919" alt="image" src="https://user-images.githubusercontent.com/18364341/166486355-6ae48b0b-483a-4260-9617-eee1e35021e2.png">
  From the context, we know that the `assume-1` is return from the old version, it requeued successfully, the `assume-2` is from the new version, it requeued failed for old workload has already requeued.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-03T18:10:28Z

Can you add unit tests?

- **pkg/queue/manager.go:303** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T17:56:53Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
```

  Right, we are actually rejected the newer version. Thanks for the extra explanation.

- **pkg/queue/manager.go:301** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T18:00:19Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
```

  actually, a new version might be on a different queue (because the user might have changed it).
  
  let's do the get before obtaining the queue from `m.queues`.

- **pkg/queue/manager.go:310** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T18:03:27Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
+	q.AddIfNotPresent(newInfo)
 	cq := m.clusterQueues[q.ClusterQueue]
 	if cq == nil {
 		return false
 	}
 
-	added := cq.RequeueIfNotPresent(info, immediate)
+	added := cq.RequeueIfNotPresent(newInfo, immediate)
```

  Should we change this for just `Requeue` instead of `RequeueIfNotPresent`?
  
  It might not be necessary, as one of the routines trying to requeue will have the chance to push the newest version.

- **pkg/queue/manager_test.go:716** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T18:10:05Z
```diff
@@ -710,6 +710,10 @@ func TestHeadsAsync(t *testing.T) {
 	now := time.Now().Truncate(time.Second)
 	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
```

  why is this necessary?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-04T23:33:25Z

- **pkg/queue/manager.go:301** by [@kerthcet](https://github.com/kerthcet) — 2022-05-04T23:33:24Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
```

  good catch.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-05T07:24:21Z

- **pkg/queue/manager_test.go:716** by [@kerthcet](https://github.com/kerthcet) — 2022-05-05T07:24:21Z
```diff
@@ -710,6 +710,10 @@ func TestHeadsAsync(t *testing.T) {
 	now := time.Now().Truncate(time.Second)
 	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
```

  We get workloads from api server now which contains these necessary fields.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-05T07:51:13Z

- **pkg/queue/manager.go:310** by [@kerthcet](https://github.com/kerthcet) — 2022-05-05T07:51:13Z
```diff
@@ -300,13 +300,14 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	newInfo := workload.NewInfo(&w)
+	q.AddIfNotPresent(newInfo)
 	cq := m.clusterQueues[q.ClusterQueue]
 	if cq == nil {
 		return false
 	}
 
-	added := cq.RequeueIfNotPresent(info, immediate)
+	added := cq.RequeueIfNotPresent(newInfo, immediate)
```

  sg.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T13:54:01Z

- **pkg/queue/manager_test.go:716** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T13:54:00Z
```diff
@@ -710,6 +710,10 @@ func TestHeadsAsync(t *testing.T) {
 	now := time.Now().Truncate(time.Second)
 	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
```

  Maybe use `cmpopts.IgnoreTypes` instead.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T13:57:59Z

- **pkg/queue/manager.go:297** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T13:55:10Z
```diff
@@ -293,25 +293,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 	m.Lock()
 	defer m.Unlock()
 
-	q := m.queues[queueKeyForWorkload(info.Obj)]
-	if q == nil {
-		return false
-	}
-
 	var w kueue.Workload
+	// Always to get the newest workload in case of requeuing the out of date obj.
```

  ```suggestion
  	// Always get the newest workload to avoid requeuing an out-of-date obj.
  ```

- **pkg/queue/manager_test.go:741** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T13:56:02Z
```diff
@@ -708,21 +708,37 @@ func TestHeadsAsync(t *testing.T) {
 		t.Fatalf("Failed adding kueue scheme: %s", err)
 	}
 	now := time.Now().Truncate(time.Second)
-	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("fooCq").Obj(),
+		utiltesting.MakeClusterQueue("barCq").Obj(),
+	}
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Workload",
+			APIVersion: "kueue.x-k8s.io/v1alpha1",
+		},
 		ObjectMeta: metav1.ObjectMeta{
 			Name:              "a",
 			CreationTimestamp: metav1.NewTime(now),
 		},
 		Spec: kueue.WorkloadSpec{QueueName: "foo"},
 	}
-	q := kueue.Queue{
-		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
-		Spec: kueue.QueueSpec{
-			ClusterQueue: "fooCq",
+	var newWl kueue.Workload
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "fooCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "barCq",
+			},
 		},
 	}
-	wantHeads := []workload.Info{
+	defaultHeads := []workload.Info{
```

  get rid of this variable and put it in the test table when necessary.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-05T14:16:39Z

- **pkg/queue/manager_test.go:741** by [@kerthcet](https://github.com/kerthcet) — 2022-05-05T14:16:34Z
```diff
@@ -708,21 +708,37 @@ func TestHeadsAsync(t *testing.T) {
 		t.Fatalf("Failed adding kueue scheme: %s", err)
 	}
 	now := time.Now().Truncate(time.Second)
-	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("fooCq").Obj(),
+		utiltesting.MakeClusterQueue("barCq").Obj(),
+	}
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Workload",
+			APIVersion: "kueue.x-k8s.io/v1alpha1",
+		},
 		ObjectMeta: metav1.ObjectMeta{
 			Name:              "a",
 			CreationTimestamp: metav1.NewTime(now),
 		},
 		Spec: kueue.WorkloadSpec{QueueName: "foo"},
 	}
-	q := kueue.Queue{
-		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
-		Spec: kueue.QueueSpec{
-			ClusterQueue: "fooCq",
+	var newWl kueue.Workload
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "fooCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "barCq",
+			},
 		},
 	}
-	wantHeads := []workload.Info{
+	defaultHeads := []workload.Info{
```

  Sorry, can't get the point. You mean define this in the `t.Run()` and only add customized workload.Infos in the test table?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-05T14:16:54Z

- **pkg/queue/manager_test.go:716** by [@kerthcet](https://github.com/kerthcet) — 2022-05-05T14:16:54Z
```diff
@@ -710,6 +710,10 @@ func TestHeadsAsync(t *testing.T) {
 	now := time.Now().Truncate(time.Second)
 	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
```

  sg.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T14:33:45Z

- **pkg/queue/manager_test.go:741** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T14:33:45Z
```diff
@@ -708,21 +708,37 @@ func TestHeadsAsync(t *testing.T) {
 		t.Fatalf("Failed adding kueue scheme: %s", err)
 	}
 	now := time.Now().Truncate(time.Second)
-	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("fooCq").Obj(),
+		utiltesting.MakeClusterQueue("barCq").Obj(),
+	}
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Workload",
+			APIVersion: "kueue.x-k8s.io/v1alpha1",
+		},
 		ObjectMeta: metav1.ObjectMeta{
 			Name:              "a",
 			CreationTimestamp: metav1.NewTime(now),
 		},
 		Spec: kueue.WorkloadSpec{QueueName: "foo"},
 	}
-	q := kueue.Queue{
-		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
-		Spec: kueue.QueueSpec{
-			ClusterQueue: "fooCq",
+	var newWl kueue.Workload
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "fooCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "barCq",
+			},
 		},
 	}
-	wantHeads := []workload.Info{
+	defaultHeads := []workload.Info{
```

  No, I just mean to get rid of this variable and copy paste the list of workloads into the test table.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T14:35:02Z

- **pkg/queue/manager_test.go:741** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T14:35:02Z
```diff
@@ -708,21 +708,37 @@ func TestHeadsAsync(t *testing.T) {
 		t.Fatalf("Failed adding kueue scheme: %s", err)
 	}
 	now := time.Now().Truncate(time.Second)
-	cq := utiltesting.MakeClusterQueue("fooCq").Obj()
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("fooCq").Obj(),
+		utiltesting.MakeClusterQueue("barCq").Obj(),
+	}
 	wl := kueue.Workload{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Workload",
+			APIVersion: "kueue.x-k8s.io/v1alpha1",
+		},
 		ObjectMeta: metav1.ObjectMeta{
 			Name:              "a",
 			CreationTimestamp: metav1.NewTime(now),
 		},
 		Spec: kueue.WorkloadSpec{QueueName: "foo"},
 	}
-	q := kueue.Queue{
-		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
-		Spec: kueue.QueueSpec{
-			ClusterQueue: "fooCq",
+	var newWl kueue.Workload
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "fooCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "barCq",
+			},
 		},
 	}
-	wantHeads := []workload.Info{
+	defaultHeads := []workload.Info{
```

  the point is that `defaultHeads` is just confusing. It's clearer to have the wantHeads in the test table.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T18:14:14Z

- **pkg/queue/manager.go:316** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T18:13:16Z
```diff
@@ -293,25 +293,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 	m.Lock()
 	defer m.Unlock()
 
-	q := m.queues[queueKeyForWorkload(info.Obj)]
-	if q == nil {
-		return false
-	}
-
 	var w kueue.Workload
+	// Always get the newest workload to avoid requeuing the out-of-date obj.
 	err := m.client.Get(ctx, client.ObjectKeyFromObject(info.Obj), &w)
 	// Since the client is cached, the only possible error is NotFound
 	if apierrors.IsNotFound(err) || w.Spec.Admission != nil {
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	q := m.queues[queueKeyForWorkload(&w)]
+	if q == nil {
+		return false
+	}
+	q.AddOrUpdate(&w)
 	q.reportPendingWorkloads()
 	cq := m.clusterQueues[q.ClusterQueue]
 	if cq == nil {
 		return false
 	}
 
+	info = workload.NewInfo(&w)
```

  Once we do #162, we probably want to make this `info.Update(&w)` or similar.
  
  Can you leave a `TODO(#162)`?

- **pkg/queue/manager_test.go:704** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T18:13:36Z
```diff
@@ -700,6 +701,8 @@ func TestHeads(t *testing.T) {
 	}
 }
 
+var ignoreTypeMeta = cmpopts.IgnoreFields(metav1.TypeMeta{}, "Kind", "APIVersion")
```

  ```suggestion
  var ignoreTypeMeta = cmpopts.IgnoreType(metav1.TypeMeta{})
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-06T00:12:07Z

- **pkg/queue/manager.go:316** by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T00:12:07Z
```diff
@@ -293,25 +293,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 	m.Lock()
 	defer m.Unlock()
 
-	q := m.queues[queueKeyForWorkload(info.Obj)]
-	if q == nil {
-		return false
-	}
-
 	var w kueue.Workload
+	// Always get the newest workload to avoid requeuing the out-of-date obj.
 	err := m.client.Get(ctx, client.ObjectKeyFromObject(info.Obj), &w)
 	// Since the client is cached, the only possible error is NotFound
 	if apierrors.IsNotFound(err) || w.Spec.Admission != nil {
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	q := m.queues[queueKeyForWorkload(&w)]
+	if q == nil {
+		return false
+	}
+	q.AddOrUpdate(&w)
 	q.reportPendingWorkloads()
 	cq := m.clusterQueues[q.ClusterQueue]
 	if cq == nil {
 		return false
 	}
 
+	info = workload.NewInfo(&w)
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-06T13:53:47Z

Please squash.

- **pkg/queue/manager.go:316** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-06T13:53:20Z
```diff
@@ -293,25 +293,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 	m.Lock()
 	defer m.Unlock()
 
-	q := m.queues[queueKeyForWorkload(info.Obj)]
-	if q == nil {
-		return false
-	}
-
 	var w kueue.Workload
+	// Always get the newest workload to avoid requeuing the out-of-date obj.
 	err := m.client.Get(ctx, client.ObjectKeyFromObject(info.Obj), &w)
 	// Since the client is cached, the only possible error is NotFound
 	if apierrors.IsNotFound(err) || w.Spec.Admission != nil {
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	q := m.queues[queueKeyForWorkload(&w)]
+	if q == nil {
+		return false
+	}
+	q.AddOrUpdate(&w)
 	q.reportPendingWorkloads()
 	cq := m.clusterQueues[q.ClusterQueue]
 	if cq == nil {
 		return false
 	}
 
+	info = workload.NewInfo(&w)
```

  You have to explain what is there "to do".
  
  For example:
  
  ```
  // TODO(#162): Update the info object instead of recreating.
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-06T14:01:13Z

- **pkg/queue/manager.go:316** by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T14:01:13Z
```diff
@@ -293,25 +293,26 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, imme
 	m.Lock()
 	defer m.Unlock()
 
-	q := m.queues[queueKeyForWorkload(info.Obj)]
-	if q == nil {
-		return false
-	}
-
 	var w kueue.Workload
+	// Always get the newest workload to avoid requeuing the out-of-date obj.
 	err := m.client.Get(ctx, client.ObjectKeyFromObject(info.Obj), &w)
 	// Since the client is cached, the only possible error is NotFound
 	if apierrors.IsNotFound(err) || w.Spec.Admission != nil {
 		return false
 	}
 
-	q.AddIfNotPresent(info)
+	q := m.queues[queueKeyForWorkload(&w)]
+	if q == nil {
+		return false
+	}
+	q.AddOrUpdate(&w)
 	q.reportPendingWorkloads()
 	cq := m.clusterQueues[q.ClusterQueue]
 	if cq == nil {
 		return false
 	}
 
+	info = workload.NewInfo(&w)
```

  I'll keep in mind then. Thanks for pointing out.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-06T15:53:37Z

/lgtm
/hold cancel
