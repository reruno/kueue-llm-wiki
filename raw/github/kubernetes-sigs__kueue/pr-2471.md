# PR #2471: Fix race condition when setting MaxRetries on provisioning controller.

**Summary**: Fix race condition when setting MaxRetries on provisioning controller.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2471

**Last updated**: 2024-06-25T21:32:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/race-condition-when-setting-max-retries` → `main`
- **Created**: 2024-06-24T05:23:56Z
- **Updated**: 2024-06-25T21:32:56Z
- **Merged**: 2024-06-25T21:32:38Z
- **Closed**: 2024-06-25T21:32:38Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Changed files**: 5   **+120 / -79**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
To fix race condition when setting MaxRetries on provisioning controller.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2470

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-24T05:24:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4f78c6ca63b887837c0400d7181d30b8eb20b6c6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/667b251813d10800084f51e3 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-25T08:31:13Z

LGTM label has been added.  <details>Git tree hash: c3892bcb6d0a8cfe017cdf7d5f0b1099c89a52a0</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T16:35:05Z

/test pull-kueue-test-multikueue-e2e-main

### Comment by [@trasc](https://github.com/trasc) — 2024-06-25T17:53:49Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-25T18:16:51Z

@mbobrovskyi could you rebase this PR?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T18:42:08Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T18:51:05Z

@alculquicondor @tenzen-y 

> There are no nodes that your pod can schedule to - check your requests, tolerations, and node selectors (0/46 nodes are available: 3 node(s) had untolerated taint {node-group: stable}, 43 Insufficient cpu. preemption: 0/46 nodes are available: 3 Preemption is not helpful for scheduling, 43 No preemption victims found for incoming pod..)

Is it possible some how to increase limitation?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T18:54:48Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T19:05:14Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T19:34:15Z

> Is it possible some how to increase limitation?

No, that's the k8s-infra cluster. The only thing we can do is reduce the size of our test pod, but we usually need all those resources to avoid timeouts.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-25T21:28:55Z

LGTM label has been added.  <details>Git tree hash: b9de35c1f98684547bb5dc96a47aaf3934e4cbcb</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-25T21:28:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2471#pullrequestreview-2139901239" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2471#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2471#pullrequestreview-2134760385" title="Approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (approved) — 2024-06-24T06:21:50Z

lgtm

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-25T08:31:06Z

just one nit, otherwise 

/lgtm

- **pkg/controller/admissionchecks/provisioning/controller.go:100** by [@trasc](https://github.com/trasc) — 2024-06-25T08:19:36Z
```diff
@@ -60,25 +63,54 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
+	maxRetries        int32
+	minBackoffSeconds int32
+	maxBackoffSeconds int32
+}
+
 type Controller struct {
-	client client.Client
-	helper *provisioningConfigHelper
-	record record.EventRecorder
+	client                          client.Client
+	record                          record.EventRecorder
+	helper                          *provisioningConfigHelper
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
 }
 
 var _ reconcile.Reconciler = (*Controller)(nil)
 
+// Option configures the provisioning controller.
+type Option func(*Controller)
+
+type WaitForProvisioningRequestReadyConfig struct {
+	MaxRetries        *int32
+	MinBackoffSeconds int32
+	MaxBackoffSeconds int32
+}
+
+// WithWaitForProvisioningRequestReady indicates the configuration for the waitForProvisioningRequestReady.
+func WithWaitForProvisioningRequestReady(cfg *WaitForProvisioningRequestReadyConfig) Option {
+	return func(controller *Controller) {
+		if cfg == nil {
+			return
+		}
+		if cfg.MaxRetries != nil {
+			controller.waitForProvisioningRequestReady.maxRetries = *cfg.MaxRetries
+		}
+		if cfg.MinBackoffSeconds > 0 {
+			controller.waitForProvisioningRequestReady.minBackoffSeconds = cfg.MinBackoffSeconds
+		}
+		if cfg.MaxBackoffSeconds > 0 {
+			controller.waitForProvisioningRequestReady.maxBackoffSeconds = cfg.MaxBackoffSeconds
+		}
+	}
+}
```

  ```suggestion
  func WithWaitForProvisioningMaxRetries(v int32) Option {
  	return func(controller *Controller) {
  		controller.waitForProvisioningRequestReady.maxRetries =  &v 
  	}
  }
  
  func WithWaitForProvisioningMinBackoffSeconds(v int32) Option {
  	return func(controller *Controller) {
  		controller.waitForProvisioningRequestReady.minBackoffSeconds = v
  	}
  }
  
  func WithWaitForProvisioningMaxBackoffSeconds(v int32) Option {
  	return func(controller *Controller) {
  		controller.waitForProvisioningRequestReady.maxBackoffSeconds = v
  	}
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T16:20:13Z

- **pkg/controller/admissionchecks/provisioning/controller.go:100** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T16:20:13Z
```diff
@@ -60,25 +63,54 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
+	maxRetries        int32
+	minBackoffSeconds int32
+	maxBackoffSeconds int32
+}
+
 type Controller struct {
-	client client.Client
-	helper *provisioningConfigHelper
-	record record.EventRecorder
+	client                          client.Client
+	record                          record.EventRecorder
+	helper                          *provisioningConfigHelper
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
 }
 
 var _ reconcile.Reconciler = (*Controller)(nil)
 
+// Option configures the provisioning controller.
+type Option func(*Controller)
+
+type WaitForProvisioningRequestReadyConfig struct {
+	MaxRetries        *int32
+	MinBackoffSeconds int32
+	MaxBackoffSeconds int32
+}
+
+// WithWaitForProvisioningRequestReady indicates the configuration for the waitForProvisioningRequestReady.
+func WithWaitForProvisioningRequestReady(cfg *WaitForProvisioningRequestReadyConfig) Option {
+	return func(controller *Controller) {
+		if cfg == nil {
+			return
+		}
+		if cfg.MaxRetries != nil {
+			controller.waitForProvisioningRequestReady.maxRetries = *cfg.MaxRetries
+		}
+		if cfg.MinBackoffSeconds > 0 {
+			controller.waitForProvisioningRequestReady.minBackoffSeconds = cfg.MinBackoffSeconds
+		}
+		if cfg.MaxBackoffSeconds > 0 {
+			controller.waitForProvisioningRequestReady.maxBackoffSeconds = cfg.MaxBackoffSeconds
+		}
+	}
+}
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-25T16:54:46Z

- **pkg/controller/admissionchecks/provisioning/controller.go:95** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-25T16:54:40Z
```diff
@@ -60,25 +63,41 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
+	maxRetries        int32
+	minBackoffSeconds int32
+	maxBackoffSeconds int32
+}
+
 type Controller struct {
-	client client.Client
-	helper *provisioningConfigHelper
-	record record.EventRecorder
+	client                          client.Client
+	record                          record.EventRecorder
+	helper                          *provisioningConfigHelper
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
 }
 
 var _ reconcile.Reconciler = (*Controller)(nil)
 
+// Option configures the provisioning controller.
+type Option func(*Controller)
+
+func WithWaitForProvisioningMaxRetries(maxRetries int32) Option {
+	return func(controller *Controller) {
+		controller.waitForProvisioningRequestReady.maxRetries = maxRetries
+	}
+}
```

  ```suggestion
  type Option func(*options)
  
  type options struct {
  	waitForProvisioningRequestReady *waitForProvisioningRequestReady
  }
  
  func WithWaitForProvisioningMaxRetries(maxRetries int32) Option {
  	return func(o *options) {
  		o.waitForProvisioningRequestReady = maxRetries
  	}
  }
  ```
  
  Could you avoid exposing unnecessary fields in the Controller?
  Additionally, we can avoid null pointers by such dedicated typed `options`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T18:42:43Z

- **pkg/controller/admissionchecks/provisioning/controller.go:95** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T18:42:42Z
```diff
@@ -60,25 +63,41 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
+	maxRetries        int32
+	minBackoffSeconds int32
+	maxBackoffSeconds int32
+}
+
 type Controller struct {
-	client client.Client
-	helper *provisioningConfigHelper
-	record record.EventRecorder
+	client                          client.Client
+	record                          record.EventRecorder
+	helper                          *provisioningConfigHelper
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
 }
 
 var _ reconcile.Reconciler = (*Controller)(nil)
 
+// Option configures the provisioning controller.
+type Option func(*Controller)
+
+func WithWaitForProvisioningMaxRetries(maxRetries int32) Option {
+	return func(controller *Controller) {
+		controller.waitForProvisioningRequestReady.maxRetries = maxRetries
+	}
+}
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-25T19:48:03Z

- **pkg/controller/admissionchecks/provisioning/controller.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T19:39:20Z
```diff
@@ -60,25 +63,45 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
```

  A bit too much indirection. Just put these values inside the `options` struct.

- **pkg/controller/admissionchecks/provisioning/controller.go:94** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T19:39:39Z
```diff
@@ -60,25 +63,45 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
+	maxRetries        int32
+	minBackoffSeconds int32
+	maxBackoffSeconds int32
+}
+
 type Controller struct {
-	client client.Client
-	helper *provisioningConfigHelper
-	record record.EventRecorder
+	client                          client.Client
+	record                          record.EventRecorder
+	helper                          *provisioningConfigHelper
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
 }
 
 var _ reconcile.Reconciler = (*Controller)(nil)
 
+type options struct {
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
+}
+
+// Option configures the provisioning controller.
+type Option func(*options)
+
+func WithWaitForProvisioningMaxRetries(maxRetries int32) Option {
```

  ```suggestion
  func WithMaxRetries(maxRetries int32) Option {
  ```
  
  same below

- **pkg/controller/admissionchecks/provisioning/controller.go:55** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T19:41:43Z
```diff
@@ -50,7 +50,10 @@ import (
 )
 
 const (
-	objNameHashLength = 5
+	DefaultMaxRetries        int32 = 3
+	DefaultMinBackoffSeconds int32 = 60   // 1 min
+	DefaultMaxBackoffSeconds int32 = 1800 // 30 min
```

  I think this is possible now
  
  ```suggestion
  	defaultMaxRetries        int32 = 3
  	defaultMinBackoffSeconds int32 = 60   // 1 min
  	defaultMaxBackoffSeconds int32 = 1800 // 30 min
  ```

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:67** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T19:46:23Z
```diff
@@ -47,24 +46,25 @@ const (
 
 var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 	var (
-		defaultMaxRetries                            = provisioning.MaxRetries
-		defaultMinBackoffSeconds                     = provisioning.MinBackoffSeconds
-		resourceGPU              corev1.ResourceName = "example.com/gpu"
-		flavorOnDemand                               = "on-demand"
+		maxRetries                            = provisioning.DefaultMaxRetries
+		minBackoffSeconds                     = provisioning.DefaultMinBackoffSeconds
+		resourceGPU       corev1.ResourceName = "example.com/gpu"
+		flavorOnDemand                        = "on-demand"
 	)
 
-	ginkgo.BeforeAll(func() {
-		fwk = &framework.Framework{
-			CRDPath:     filepath.Join("..", "..", "..", "..", "..", "config", "components", "crd", "bases"),
-			DepCRDPaths: []string{filepath.Join("..", "..", "..", "..", "..", "dep-crds", "cluster-autoscaler")},
-			WebhookPath: filepath.Join("..", "..", "..", "..", "..", "config", "components", "webhook"),
-		}
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{CRDPath: crdPath, DepCRDPaths: depCRDPaths, WebhookPath: webhookPath}
 		cfg = fwk.Init()
-		ctx, k8sClient = fwk.RunManager(cfg, managerSetup)
+		ctx, k8sClient = fwk.RunManager(cfg, managerSetup(
+			provisioning.WithWaitForProvisioningMaxRetries(maxRetries),
+			provisioning.WithWaitForProvisioningMinBackoffSeconds(minBackoffSeconds),
+		))
 	})
 
-	ginkgo.AfterAll(func() {
+	ginkgo.AfterEach(func() {
 		fwk.Teardown()
+		maxRetries = provisioning.DefaultMaxRetries
+		minBackoffSeconds = provisioning.DefaultMinBackoffSeconds
```

  we don't need to reset these anymore. That was necessary when we were using global variables.

- **test/integration/controller/admissionchecks/provisioning/suite_test.go:61** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-25T19:48:00Z
```diff
@@ -53,33 +57,39 @@ func TestProvisioning(t *testing.T) {
 	)
 }
 
-func managerSetup(mgr manager.Manager, ctx context.Context) {
-	err := indexer.Setup(ctx, mgr.GetFieldIndexer())
-	gomega.Expect(err).NotTo(gomega.HaveOccurred())
+func managerSetup(opts ...provisioning.Option) framework.ManagerSetup {
+	return func(mgr manager.Manager, ctx context.Context) {
```

  Maybe just this?
  ```suggestion
  func managerSetup(mgr manager.Manager, ctx context.Context, opts ...provisioning.Option) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:02:44Z

- **test/integration/controller/admissionchecks/provisioning/suite_test.go:61** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:02:44Z
```diff
@@ -53,33 +57,39 @@ func TestProvisioning(t *testing.T) {
 	)
 }
 
-func managerSetup(mgr manager.Manager, ctx context.Context) {
-	err := indexer.Setup(ctx, mgr.GetFieldIndexer())
-	gomega.Expect(err).NotTo(gomega.HaveOccurred())
+func managerSetup(opts ...provisioning.Option) framework.ManagerSetup {
+	return func(mgr manager.Manager, ctx context.Context) {
```

  Unfortunately no. The `ManagerSetup` that using on `fwk.RunManager` required only this two fields.
  
  ```go
  type ManagerSetup func(manager.Manager, context.Context)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:14:39Z

- **pkg/controller/admissionchecks/provisioning/controller.go:72** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:14:38Z
```diff
@@ -60,25 +63,45 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
```

  Thanks. Done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:14:49Z

- **pkg/controller/admissionchecks/provisioning/controller.go:94** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:14:49Z
```diff
@@ -60,25 +63,45 @@ var (
 	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
 )
 
-var (
-	MaxRetries        int32 = 3
-	MinBackoffSeconds int32 = 60
-)
-
 type provisioningConfigHelper = admissioncheck.ConfigHelper[*kueue.ProvisioningRequestConfig, kueue.ProvisioningRequestConfig]
 
 func newProvisioningConfigHelper(c client.Client) (*provisioningConfigHelper, error) {
 	return admissioncheck.NewConfigHelper[*kueue.ProvisioningRequestConfig](c)
 }
 
+type waitForProvisioningRequestReadyConfig struct {
+	maxRetries        int32
+	minBackoffSeconds int32
+	maxBackoffSeconds int32
+}
+
 type Controller struct {
-	client client.Client
-	helper *provisioningConfigHelper
-	record record.EventRecorder
+	client                          client.Client
+	record                          record.EventRecorder
+	helper                          *provisioningConfigHelper
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
 }
 
 var _ reconcile.Reconciler = (*Controller)(nil)
 
+type options struct {
+	waitForProvisioningRequestReady *waitForProvisioningRequestReadyConfig
+}
+
+// Option configures the provisioning controller.
+type Option func(*options)
+
+func WithWaitForProvisioningMaxRetries(maxRetries int32) Option {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:14:58Z

- **pkg/controller/admissionchecks/provisioning/controller.go:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:14:58Z
```diff
@@ -50,7 +50,10 @@ import (
 )
 
 const (
-	objNameHashLength = 5
+	DefaultMaxRetries        int32 = 3
+	DefaultMinBackoffSeconds int32 = 60   // 1 min
+	DefaultMaxBackoffSeconds int32 = 1800 // 30 min
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-25T20:15:21Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:67** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-25T20:15:20Z
```diff
@@ -47,24 +46,25 @@ const (
 
 var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 	var (
-		defaultMaxRetries                            = provisioning.MaxRetries
-		defaultMinBackoffSeconds                     = provisioning.MinBackoffSeconds
-		resourceGPU              corev1.ResourceName = "example.com/gpu"
-		flavorOnDemand                               = "on-demand"
+		maxRetries                            = provisioning.DefaultMaxRetries
+		minBackoffSeconds                     = provisioning.DefaultMinBackoffSeconds
+		resourceGPU       corev1.ResourceName = "example.com/gpu"
+		flavorOnDemand                        = "on-demand"
 	)
 
-	ginkgo.BeforeAll(func() {
-		fwk = &framework.Framework{
-			CRDPath:     filepath.Join("..", "..", "..", "..", "..", "config", "components", "crd", "bases"),
-			DepCRDPaths: []string{filepath.Join("..", "..", "..", "..", "..", "dep-crds", "cluster-autoscaler")},
-			WebhookPath: filepath.Join("..", "..", "..", "..", "..", "config", "components", "webhook"),
-		}
+	ginkgo.JustBeforeEach(func() {
+		fwk = &framework.Framework{CRDPath: crdPath, DepCRDPaths: depCRDPaths, WebhookPath: webhookPath}
 		cfg = fwk.Init()
-		ctx, k8sClient = fwk.RunManager(cfg, managerSetup)
+		ctx, k8sClient = fwk.RunManager(cfg, managerSetup(
+			provisioning.WithWaitForProvisioningMaxRetries(maxRetries),
+			provisioning.WithWaitForProvisioningMinBackoffSeconds(minBackoffSeconds),
+		))
 	})
 
-	ginkgo.AfterAll(func() {
+	ginkgo.AfterEach(func() {
 		fwk.Teardown()
+		maxRetries = provisioning.DefaultMaxRetries
+		minBackoffSeconds = provisioning.DefaultMinBackoffSeconds
```

  You right. Thanks. Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-25T21:28:50Z

/lgtm
/approve
