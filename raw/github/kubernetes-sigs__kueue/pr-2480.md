# PR #2480: [kjobctl] Add interactive builder

**Summary**: [kjobctl] Add interactive builder

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2480

**Last updated**: 2024-07-03T15:53:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/kjobctl-interactive-builder` → `main`
- **Created**: 2024-06-26T08:31:27Z
- **Updated**: 2024-07-03T15:53:11Z
- **Merged**: 2024-07-02T13:27:59Z
- **Closed**: 2024-07-02T13:27:59Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mwielgus](https://github.com/mwielgus), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 9   **+527 / -78**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:

Add interactive builder for kjobctl.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-26T08:31:29Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-26T08:31:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a004f0542620e9a940b6ee20da8d47920fff9040 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6682bb09b68d4100082b446b |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2480--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2480--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI0ODAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.byZNryAu9W7trHQGFOtgROWAqEFwgz9Tt4axyF1K_wo)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-27T17:11:31Z

LGTM label has been added.  <details>Git tree hash: 778d60c88c4c8d2867baf4d7987635d0fed67772</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-27T17:57:26Z

/assign @mwielgus

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-28T04:01:24Z

lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-02T13:20:16Z

LGTM label has been added.  <details>Git tree hash: 3085d3c097256be861bae1619676286a7719d801</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-02T13:20:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2480#pullrequestreview-2153787664" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2480#" title="Author self-approved">IrvingMg</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-26T14:08:44Z

- **cmd/experimental/kjobctl/pkg/builder/interactive_builder.go:67** by [@trasc](https://github.com/trasc) — 2024-06-26T13:59:56Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type interactiveBuilder struct {
+	*Builder
+}
+
+var _ builder = (*interactiveBuilder)(nil)
+
+func (b *interactiveBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.k8sClientset.CoreV1().PodTemplates(b.profile.Namespace).Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	pod := &corev1.Pod{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Pod",
+			APIVersion: "v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		pod.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	// TODO: Move to common function to generate a Pod Spec
+	for _, vb := range b.volumeBundles {
+		for _, volume := range vb.Spec.Volumes {
+			index := slices.IndexFunc(pod.Spec.Volumes, func(v corev1.Volume) bool {
+				return v.Name == volume.Name
+			})
+			if index != -1 {
+				pod.Spec.Volumes[index] = volume
```

  in my opinion this can only happen if two bundles contain volumes with the same name, case in which just accepting this and keep the last one we see is a strange approach.
  
  This scenario most likely should be checked by the `validate` command however until then we can:
  
  1. error out from here
  OR
  3. just concatenate the volume slices and let the API server generate the error upon object creation
  
  cc: @mbobrovskyi

- **cmd/experimental/kjobctl/pkg/builder/interactive_builder.go:106** by [@trasc](https://github.com/trasc) — 2024-06-26T14:03:57Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type interactiveBuilder struct {
+	*Builder
+}
+
+var _ builder = (*interactiveBuilder)(nil)
+
+func (b *interactiveBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.k8sClientset.CoreV1().PodTemplates(b.profile.Namespace).Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	pod := &corev1.Pod{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Pod",
+			APIVersion: "v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		pod.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	// TODO: Move to common function to generate a Pod Spec
+	for _, vb := range b.volumeBundles {
+		for _, volume := range vb.Spec.Volumes {
+			index := slices.IndexFunc(pod.Spec.Volumes, func(v corev1.Volume) bool {
+				return v.Name == volume.Name
+			})
+			if index != -1 {
+				pod.Spec.Volumes[index] = volume
+			} else {
+				pod.Spec.Volumes = append(pod.Spec.Volumes, volume)
+			}
+		}
+	}
+
+	for i := range pod.Spec.Containers {
+		container := &pod.Spec.Containers[i]
+
+		if len(b.command) > 0 {
+			container.Command = b.command
+		}
+
+		if len(b.requests) > 0 {
+			container.Resources.Requests = b.requests
+		}
+
+		for _, vb := range b.volumeBundles {
+			for _, volumeMount := range vb.Spec.ContainerVolumeMounts {
+				index := slices.IndexFunc(container.VolumeMounts, func(vm corev1.VolumeMount) bool {
+					return vm.Name == volumeMount.Name
+				})
+				if index != -1 {
+					container.VolumeMounts[index] = volumeMount
+				} else {
+					container.VolumeMounts = append(container.VolumeMounts, volumeMount)
+				}
+			}
+
+			for _, envVar := range vb.Spec.EnvVars {
+				index := slices.IndexFunc(container.Env, func(ev corev1.EnvVar) bool {
+					return ev.Name == envVar.Name
+				})
+				if index != -1 {
+					container.Env[index] = envVar
+				} else {
+					container.Env = append(container.Env, envVar)
+				}
+			}
```

  The comment above applies here also. Additionally we should do this only once keep the resulting slices in some local vars and set them to all the containers.

- **cmd/experimental/kjobctl/pkg/builder/interactive_builder.go:60** by [@trasc](https://github.com/trasc) — 2024-06-26T14:06:45Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type interactiveBuilder struct {
+	*Builder
+}
+
+var _ builder = (*interactiveBuilder)(nil)
+
+func (b *interactiveBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.k8sClientset.CoreV1().PodTemplates(b.profile.Namespace).Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	pod := &corev1.Pod{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Pod",
+			APIVersion: "v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		pod.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	// TODO: Move to common function to generate a Pod Spec
```

  it could be something like
  
  ```go
  func mergeBundles(s []VolumeBundle) VolumeBundle
  ```
   the result containing all the volumes, volume mounts and env vars.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-26T14:15:40Z

- **cmd/experimental/kjobctl/pkg/builder/interactive_builder.go:67** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-26T14:15:40Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type interactiveBuilder struct {
+	*Builder
+}
+
+var _ builder = (*interactiveBuilder)(nil)
+
+func (b *interactiveBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.k8sClientset.CoreV1().PodTemplates(b.profile.Namespace).Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	pod := &corev1.Pod{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Pod",
+			APIVersion: "v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		pod.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	// TODO: Move to common function to generate a Pod Spec
+	for _, vb := range b.volumeBundles {
+		for _, volume := range vb.Spec.Volumes {
+			index := slices.IndexFunc(pod.Spec.Volumes, func(v corev1.Volume) bool {
+				return v.Name == volume.Name
+			})
+			if index != -1 {
+				pod.Spec.Volumes[index] = volume
```

  What if we have `PodTemplate` with already exists `Volume` that have the same name that we have on `VolumeBundle`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-26T15:15:12Z

- **cmd/experimental/kjobctl/pkg/builder/interactive_builder.go:67** by [@trasc](https://github.com/trasc) — 2024-06-26T15:15:12Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type interactiveBuilder struct {
+	*Builder
+}
+
+var _ builder = (*interactiveBuilder)(nil)
+
+func (b *interactiveBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.k8sClientset.CoreV1().PodTemplates(b.profile.Namespace).Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	pod := &corev1.Pod{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Pod",
+			APIVersion: "v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		pod.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	// TODO: Move to common function to generate a Pod Spec
+	for _, vb := range b.volumeBundles {
+		for _, volume := range vb.Spec.Volumes {
+			index := slices.IndexFunc(pod.Spec.Volumes, func(v corev1.Volume) bool {
+				return v.Name == volume.Name
+			})
+			if index != -1 {
+				pod.Spec.Volumes[index] = volume
```

  We can generate an error or let the API server do it. We should not just silently overwrite, in my opinion.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-27T08:34:26Z

- **cmd/experimental/kjobctl/pkg/builder/interactive_builder.go:60** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-27T08:34:26Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type interactiveBuilder struct {
+	*Builder
+}
+
+var _ builder = (*interactiveBuilder)(nil)
+
+func (b *interactiveBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.k8sClientset.CoreV1().PodTemplates(b.profile.Namespace).Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	pod := &corev1.Pod{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Pod",
+			APIVersion: "v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		pod.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	// TODO: Move to common function to generate a Pod Spec
```

  Done

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-27T08:38:36Z

- **cmd/experimental/kjobctl/pkg/builder/interactive_builder.go:67** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-27T08:38:36Z
```diff
@@ -0,0 +1,119 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type interactiveBuilder struct {
+	*Builder
+}
+
+var _ builder = (*interactiveBuilder)(nil)
+
+func (b *interactiveBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.k8sClientset.CoreV1().PodTemplates(b.profile.Namespace).Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	pod := &corev1.Pod{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "Pod",
+			APIVersion: "v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		pod.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	// TODO: Move to common function to generate a Pod Spec
+	for _, vb := range b.volumeBundles {
+		for _, volume := range vb.Spec.Volumes {
+			index := slices.IndexFunc(pod.Spec.Volumes, func(v corev1.Volume) bool {
+				return v.Name == volume.Name
+			})
+			if index != -1 {
+				pod.Spec.Volumes[index] = volume
```

  Changed. Option 2 was implemented, concatenating slices and delegating the duplicate name detection to the `validate` command.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-27T09:33:19Z

- **cmd/experimental/kjobctl/pkg/builder/helpers.go:34** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-27T09:33:19Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package builder
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/apis/v1alpha1"
+)
+
+func mergeBundles(bundles []v1alpha1.VolumeBundle) v1alpha1.VolumeBundle {
+	var volumeBundle v1alpha1.VolumeBundle
+	for _, b := range bundles {
+		volumeBundle.Spec.Volumes = append(volumeBundle.Spec.Volumes, b.Spec.Volumes...)
+		volumeBundle.Spec.ContainerVolumeMounts = append(volumeBundle.Spec.ContainerVolumeMounts, b.Spec.ContainerVolumeMounts...)
+		volumeBundle.Spec.EnvVars = append(volumeBundle.Spec.EnvVars, b.Spec.EnvVars...)
+	}
+
+	return volumeBundle
+}
+
+func buildPodSpec(templateSpec corev1.PodSpec, command []string, requests corev1.ResourceList, bundle v1alpha1.VolumeBundle) corev1.PodSpec {
```

  Maybe it's a good idea to make it as a method of `Builder`? WDYT?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-06-27T10:20:52Z

- **cmd/experimental/kjobctl/pkg/builder/helpers.go:34** by [@IrvingMg](https://github.com/IrvingMg) — 2024-06-27T10:20:52Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package builder
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/apis/v1alpha1"
+)
+
+func mergeBundles(bundles []v1alpha1.VolumeBundle) v1alpha1.VolumeBundle {
+	var volumeBundle v1alpha1.VolumeBundle
+	for _, b := range bundles {
+		volumeBundle.Spec.Volumes = append(volumeBundle.Spec.Volumes, b.Spec.Volumes...)
+		volumeBundle.Spec.ContainerVolumeMounts = append(volumeBundle.Spec.ContainerVolumeMounts, b.Spec.ContainerVolumeMounts...)
+		volumeBundle.Spec.EnvVars = append(volumeBundle.Spec.EnvVars, b.Spec.EnvVars...)
+	}
+
+	return volumeBundle
+}
+
+func buildPodSpec(templateSpec corev1.PodSpec, command []string, requests corev1.ResourceList, bundle v1alpha1.VolumeBundle) corev1.PodSpec {
```

  Done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-27T17:11:26Z

/lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-29T13:12:49Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:226** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-29T13:10:53Z
```diff
@@ -215,3 +218,36 @@ func (b *Builder) Do(ctx context.Context) (runtime.Object, error) {
 
 	return bImpl.build(ctx)
 }
+
+func (b *Builder) buildPodSpec(templateSpec corev1.PodSpec) corev1.PodSpec {
+	bundle := mergeBundles(b.volumeBundles)
+
+	templateSpec.Volumes = append(templateSpec.Volumes, bundle.Spec.Volumes...)
+	for i := range templateSpec.Containers {
```

  volume mounts should also be added to initContainers.
  
  But the command should only be added to the first primary container. Requests too.
  
  And this has to be documented somewhere, probably as part of the APIs.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-07-01T10:16:15Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:226** by [@IrvingMg](https://github.com/IrvingMg) — 2024-07-01T10:16:15Z
```diff
@@ -215,3 +218,36 @@ func (b *Builder) Do(ctx context.Context) (runtime.Object, error) {
 
 	return bImpl.build(ctx)
 }
+
+func (b *Builder) buildPodSpec(templateSpec corev1.PodSpec) corev1.PodSpec {
+	bundle := mergeBundles(b.volumeBundles)
+
+	templateSpec.Volumes = append(templateSpec.Volumes, bundle.Spec.Volumes...)
+	for i := range templateSpec.Containers {
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-07-01T11:57:35Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:72** by [@mwielgus](https://github.com/mwielgus) — 2024-07-01T11:57:35Z
```diff
@@ -69,6 +69,8 @@ type SupportedMode struct {
 	// requiredFlags point which cli flags are required to be passed in order to fill the gaps in the templates.
 	// Possible values are cmd, parallelism, completions, request, localqueue.
 	//
+	// cmd and requests values are going to be added only to the first primary container.
```

  The flags should be specific to the used profile. For example, parallelism doesn't make sense for interactive. While it doesn't have to be checked at the CRD level, we probably want to check when running the profile, so that all flags passed are actually used.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-01T13:15:58Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:72** by [@trasc](https://github.com/trasc) — 2024-07-01T13:15:58Z
```diff
@@ -69,6 +69,8 @@ type SupportedMode struct {
 	// requiredFlags point which cli flags are required to be passed in order to fill the gaps in the templates.
 	// Possible values are cmd, parallelism, completions, request, localqueue.
 	//
+	// cmd and requests values are going to be added only to the first primary container.
```

  We are checking that the required flags are provided , here: https://github.com/kubernetes-sigs/kueue/blob/398545f971a9d5ecb83d69f410f606525aaa8814/cmd/experimental/kjobctl/pkg/builder/builder.go#L169

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-02T13:20:10Z

/approve
/lgtm
