# PR #2486: Use Kueue API types in FlavorAssignerTest

**Summary**: Use Kueue API types in FlavorAssignerTest

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2486

**Last updated**: 2024-07-09T15:49:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `refactor_flavor_resource_test` → `main`
- **Created**: 2024-06-27T10:31:05Z
- **Updated**: 2024-07-09T15:49:27Z
- **Merged**: 2024-07-02T12:13:59Z
- **Closed**: 2024-07-02T12:13:59Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 2   **+843 / -1093**

## Description

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
We update the FlavorAssignerTest to use Kueue API types, rather than construct CQ/Cohort snapshot manually. This will allow us to change CQ/Cohort Snapshot logic without updating the tests each time.

Preparation for main changes in #79

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-27T10:31:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 005c37f5734d9a27bf21718d6d2f7c95307eb8a0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6683e70109167b00070f0c83 |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-06-27T11:23:26Z

@macsko

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-06-27T15:13:12Z

> Looks good, two small comments

thanks for the review

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-28T16:15:46Z

/lgtm
/approve
/hold
in case @alculquicondor wants to give a pass

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-28T16:15:52Z

LGTM label has been added.  <details>Git tree hash: d4dd3ceb413f316b130f7d4714b22633bd9853a0</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-28T16:15:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2486#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2486#issuecomment-2197255163" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-02T11:02:18Z

LGTM, just one nit to rename the test

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-07-02T11:39:59Z

> LGTM, just one nit to rename the test

Done. Thanks for the review!

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-02T12:10:17Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-02T12:10:22Z

LGTM label has been added.  <details>Git tree hash: cc59312abc3a5ed7512193dcc10f8fc4bbc5ca0e</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-02T12:12:31Z

/hold cancel
I think we are good to merge as all comments are addressed. We can follow up in case @alculquicondor has another comment.

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-27T11:41:23Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1816** by [@gabesaba](https://github.com/gabesaba) — 2024-06-27T11:41:23Z
```diff
@@ -2172,30 +1797,25 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
-				},
-				FlavorFungibility: defaultFlavorFungibility,
-				ResourceGroups: []cache.ResourceGroup{{
-					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
-					Flavors: []cache.FlavorQuotas{{
-						Name: "one",
-						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourcePods: {Nominal: 10},
-							corev1.ResourceCPU:  {Nominal: 10_000, LendingLimit: ptr.To[int64](0)},
-						},
-					}},
-				}},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				FlavorFungibility(defaultFlavorFungibility).
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						Resource(corev1.ResourceCPU, "10000m", "10000m", "0m").
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
 					{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-				}.Unflatten(),
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2000,
```

  note to reviewers: This test started failing once borrowing limit was added (limitation of the Resource constructor - which requires you to specify borrowing limit to set lending limit)
  
  I think the original test didn't match its intention: since previously there was no borrowing limit, the cq wasn't looking for capacity in its cohort, only looking in cq. I changed this value to 2000m, and specified 10000m borrowing limit.

### Review by [@macsko](https://github.com/macsko) (commented) — 2024-06-27T14:49:21Z

Looks good, two small comments

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:2002** by [@macsko](https://github.com/macsko) — 2024-06-27T14:05:12Z
```diff
@@ -2373,6 +1993,19 @@ func TestAssignFlavors(t *testing.T) {
 	}
 }
 
+func updateResources(dest resources.FlavorResourceQuantities, src resources.FlavorResourceQuantitiesFlat) {
+	if dest == nil {
+		dest = make(resources.FlavorResourceQuantities)
+	}
+
+	for flavorResource, usage := range src {
+		if _, ok := (dest)[flavorResource.Flavor]; !ok {
```

  I think `dest` here doesn't need to be wrapped in parenthesis

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1975** by [@macsko](https://github.com/macsko) — 2024-06-27T14:45:28Z
```diff
@@ -2352,15 +1948,39 @@ func TestAssignFlavors(t *testing.T) {
 					ReclaimablePods: tc.wlReclaimablePods,
 				},
 			})
-			if tc.clusterQueue.FlavorFungibility.WhenCanBorrow == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow = defaultFlavorFungibility.WhenCanBorrow
 			}
-			if tc.clusterQueue.FlavorFungibility.WhenCanPreempt == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
+
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt = defaultFlavorFungibility.WhenCanPreempt
+			}
+
+			cache := cache.New(utiltesting.NewFakeClient())
+			if err := cache.AddClusterQueue(context.Background(), &tc.clusterQueue); err != nil {
+				t.Fatalf("failed to add CQ to cache")
 			}
-			tc.clusterQueue.UpdateWithFlavors(resourceFlavors)
-			tc.clusterQueue.UpdateRGByResource()
-			flvAssigner := New(wlInfo, &tc.clusterQueue, resourceFlavors, tc.enableFairSharing)
+			for _, rf := range resourceFlavors {
+				cache.AddOrUpdateResourceFlavor(rf)
+			}
+
+			clusterQueue := cache.Snapshot().ClusterQueues[tc.clusterQueue.Name]
+
+			if clusterQueue == nil {
+				t.Fatalf("failed to create CQ snapshot")
+			}
+
+			if tc.cohortResources != nil {
+				if clusterQueue.Cohort == nil {
+					t.Fatalf("test case has cohort resources, but cluster queue doesn't have cohort")
```

  I would begin the failure messages with capital letter as in the other places

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T13:10:00Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1975** by [@mimowo](https://github.com/mimowo) — 2024-06-28T13:10:00Z
```diff
@@ -2352,15 +1948,39 @@ func TestAssignFlavors(t *testing.T) {
 					ReclaimablePods: tc.wlReclaimablePods,
 				},
 			})
-			if tc.clusterQueue.FlavorFungibility.WhenCanBorrow == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow = defaultFlavorFungibility.WhenCanBorrow
 			}
-			if tc.clusterQueue.FlavorFungibility.WhenCanPreempt == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
+
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt = defaultFlavorFungibility.WhenCanPreempt
+			}
+
+			cache := cache.New(utiltesting.NewFakeClient())
+			if err := cache.AddClusterQueue(context.Background(), &tc.clusterQueue); err != nil {
+				t.Fatalf("failed to add CQ to cache")
 			}
-			tc.clusterQueue.UpdateWithFlavors(resourceFlavors)
-			tc.clusterQueue.UpdateRGByResource()
-			flvAssigner := New(wlInfo, &tc.clusterQueue, resourceFlavors, tc.enableFairSharing)
+			for _, rf := range resourceFlavors {
+				cache.AddOrUpdateResourceFlavor(rf)
+			}
+
+			clusterQueue := cache.Snapshot().ClusterQueues[tc.clusterQueue.Name]
+
+			if clusterQueue == nil {
+				t.Fatalf("failed to create CQ snapshot")
+			}
+
+			if tc.cohortResources != nil {
+				if clusterQueue.Cohort == nil {
+					t.Fatalf("test case has cohort resources, but cluster queue doesn't have cohort")
```

  FYI we have https://google.github.io/styleguide/go/decisions.html#error-strings, otoh being consistent is good, I'm ok either way.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T13:11:24Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1975** by [@mimowo](https://github.com/mimowo) — 2024-06-28T13:11:24Z
```diff
@@ -2352,15 +1948,39 @@ func TestAssignFlavors(t *testing.T) {
 					ReclaimablePods: tc.wlReclaimablePods,
 				},
 			})
-			if tc.clusterQueue.FlavorFungibility.WhenCanBorrow == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow = defaultFlavorFungibility.WhenCanBorrow
 			}
-			if tc.clusterQueue.FlavorFungibility.WhenCanPreempt == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanPreempt = kueue.TryNextFlavor
+
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt = defaultFlavorFungibility.WhenCanPreempt
+			}
+
+			cache := cache.New(utiltesting.NewFakeClient())
+			if err := cache.AddClusterQueue(context.Background(), &tc.clusterQueue); err != nil {
+				t.Fatalf("failed to add CQ to cache")
 			}
-			tc.clusterQueue.UpdateWithFlavors(resourceFlavors)
-			tc.clusterQueue.UpdateRGByResource()
-			flvAssigner := New(wlInfo, &tc.clusterQueue, resourceFlavors, tc.enableFairSharing)
+			for _, rf := range resourceFlavors {
+				cache.AddOrUpdateResourceFlavor(rf)
+			}
+
+			clusterQueue := cache.Snapshot().ClusterQueues[tc.clusterQueue.Name]
+
+			if clusterQueue == nil {
+				t.Fatalf("failed to create CQ snapshot")
+			}
+
+			if tc.cohortResources != nil {
+				if clusterQueue.Cohort == nil {
+					t.Fatalf("test case has cohort resources, but cluster queue doesn't have cohort")
```

  "On the other hand, the style for the full displayed message (logging, **test failure**, API response, or other UI) depends, but should typically be capitalized."
  
  So seems that capital is preferred in this case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T13:29:37Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1982** by [@mimowo](https://github.com/mimowo) — 2024-06-28T13:29:37Z
```diff
@@ -2373,6 +1993,19 @@ func TestAssignFlavors(t *testing.T) {
 	}
 }
 
+func updateResources(dest resources.FlavorResourceQuantities, src resources.FlavorResourceQuantitiesFlat) {
```

  Can we use the pre-existing Unflatten function for this, like `clusterQueue.Usage = tc.clusterQueueUsage.Unflatten()`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T13:36:07Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:554** by [@mimowo](https://github.com/mimowo) — 2024-06-28T13:36:07Z
```diff
@@ -513,71 +427,15 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "3").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{
-					{
-						CoveredResources: sets.New(corev1.ResourceCPU),
-						Flavors: []cache.FlavorQuotas{
-							{
-								Name: "tainted",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU: {Nominal: 4000},
-								},
-							},
-							{
-								Name: "two",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU: {Nominal: 4000},
-								},
-							},
-						},
-					},
-				},
-			},
-			wantRepMode: Fit,
-			wantAssignment: Assignment{
-				PodSets: []PodSetAssignment{{
-					Name: "main",
-					Flavors: ResourceAssignment{
-						corev1.ResourceCPU: {Name: "two", Mode: Fit, TriedFlavorIdx: -1},
-					},
-					Requests: corev1.ResourceList{
-						corev1.ResourceCPU: resource.MustParse("3000m"),
-					},
-					Count: 1,
-				}},
-				Usage: resources.FlavorResourceQuantitiesFlat{
-					{Flavor: "two", Resource: corev1.ResourceCPU}: 3000,
-				}.Unflatten(),
-			},
-		},
-		"multiple flavors, skip missing ResourceFlavor": {
```

  I'm wondering if there could be some race conditions if scheduler tries to use them before they are marked inactive? Maybe it is safer to keep the test in some form?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T13:41:23Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1367** by [@mimowo](https://github.com/mimowo) — 2024-06-28T13:41:23Z
```diff
@@ -1364,55 +1114,20 @@ func TestAssignFlavors(t *testing.T) {
 				Usage: resources.FlavorResourceQuantities{},
 			},
 		},
-		"flavor not found": {
```

  Similar question as above - wondering if there could be race conditions before such a CQ is marked inactive so that it is worth covering with a test

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T13:51:04Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1688** by [@mimowo](https://github.com/mimowo) — 2024-06-28T13:51:04Z
```diff
@@ -2037,45 +1680,36 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  11000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-						{Flavor: "two", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "two", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor, WhenCanPreempt: kueue.TryNextFlavor}).
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						Resource(corev1.ResourceCPU, "10000m", "1000", "1000m").
```

  This borrowing limit "1000" looks suspicious, IIUC it is unbound in the deleted code, which is slightly different semantically, Would there be a way to express "unbound" now? Otherwise maybe Max int 32, but this may mean that we no longer cover cases when borrowingLimit==nil. (same question for some test cases below)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T14:05:57Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1816** by [@mimowo](https://github.com/mimowo) — 2024-06-28T14:05:57Z
```diff
@@ -2172,30 +1797,25 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
-				},
-				FlavorFungibility: defaultFlavorFungibility,
-				ResourceGroups: []cache.ResourceGroup{{
-					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
-					Flavors: []cache.FlavorQuotas{{
-						Name: "one",
-						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourcePods: {Nominal: 10},
-							corev1.ResourceCPU:  {Nominal: 10_000, LendingLimit: ptr.To[int64](0)},
-						},
-					}},
-				}},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				FlavorFungibility(defaultFlavorFungibility).
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						Resource(corev1.ResourceCPU, "10000m", "10000m", "0m").
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
 					{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-				}.Unflatten(),
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2000,
```

  I'm not convinced, if there is no borrowing limit, then the cq is free to borrow within the entire cq.
  Would it be feasible to have a new dedicated constructor to let using borrowingLimit =0, so that we are sure we don't drop some coverage?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-28T14:07:18Z

Thanks, nice preparatory pr which will make the following PRs easier to review!

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T15:36:47Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1688** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T15:36:47Z
```diff
@@ -2037,45 +1680,36 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  11000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-						{Flavor: "two", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "two", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				FlavorFungibility(kueue.FlavorFungibility{WhenCanBorrow: kueue.TryNextFlavor, WhenCanPreempt: kueue.TryNextFlavor}).
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						Resource(corev1.ResourceCPU, "10000m", "1000", "1000m").
```

  updated the tests to match original semantics

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T15:37:01Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:554** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T15:37:01Z
```diff
@@ -513,71 +427,15 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "3").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{
-					{
-						CoveredResources: sets.New(corev1.ResourceCPU),
-						Flavors: []cache.FlavorQuotas{
-							{
-								Name: "tainted",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU: {Nominal: 4000},
-								},
-							},
-							{
-								Name: "two",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU: {Nominal: 4000},
-								},
-							},
-						},
-					},
-				},
-			},
-			wantRepMode: Fit,
-			wantAssignment: Assignment{
-				PodSets: []PodSetAssignment{{
-					Name: "main",
-					Flavors: ResourceAssignment{
-						corev1.ResourceCPU: {Name: "two", Mode: Fit, TriedFlavorIdx: -1},
-					},
-					Requests: corev1.ResourceList{
-						corev1.ResourceCPU: resource.MustParse("3000m"),
-					},
-					Count: 1,
-				}},
-				Usage: resources.FlavorResourceQuantitiesFlat{
-					{Flavor: "two", Resource: corev1.ResourceCPU}: 3000,
-				}.Unflatten(),
-			},
-		},
-		"multiple flavors, skip missing ResourceFlavor": {
```

  added back these test cases in a new suite

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T15:52:44Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1982** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T15:52:44Z
```diff
@@ -2373,6 +1993,19 @@ func TestAssignFlavors(t *testing.T) {
 	}
 }
 
+func updateResources(dest resources.FlavorResourceQuantities, src resources.FlavorResourceQuantitiesFlat) {
```

  I could do this in all but one case - a test breaks unfortunately. But I don't like this approach either. We should clean this up in a future PR - by calling some public method of cache or snapshot to get the values we want, rather than manipulating the internal accounting of the CQs/Cohorts
  
  Created https://github.com/kubernetes-sigs/kueue/issues/2502

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-28T16:18:34Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T16:00:20Z
```diff
@@ -74,18 +81,13 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceMemory, "1Mi").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{{
-					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourceMemory),
-					Flavors: []cache.FlavorQuotas{{
-						Name: "default",
-						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourceCPU:    {Nominal: 1000},
-							corev1.ResourceMemory: {Nominal: 2 * utiltesting.Mi},
-						},
-					}},
-				}},
-			},
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("default").
+						Resource(corev1.ResourceCPU, "1000m").
```

  ```suggestion
  						Resource(corev1.ResourceCPU, "1").
  ```
  
  Same for others

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:347** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T16:04:59Z
```diff
@@ -381,52 +323,36 @@ func TestAssignFlavors(t *testing.T) {
 					Request("example.com/gpu", "3").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{
-					{
-						CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourceMemory),
-						Flavors: []cache.FlavorQuotas{
-							{
-								Name: "one",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 2000},
-									corev1.ResourceMemory: {Nominal: utiltesting.Gi},
-								},
-							},
-							{
-								Name: "two",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 4000},
-									corev1.ResourceMemory: {Nominal: 15 * utiltesting.Mi},
-								},
-							},
-						},
-					},
-					{
-						CoveredResources: sets.New[corev1.ResourceName]("example.com/gpu"),
-						Flavors: []cache.FlavorQuotas{{
-							Name: "b_one",
-							Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-								"example.com/gpu": {Nominal: 4},
-							},
-						}},
-					},
-				},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourceCPU, "2000m").
+						Resource(corev1.ResourceMemory, "1Gi").
+						FlavorQuotas,
+					utiltesting.MakeFlavorQuotas("two").
+						Resource(corev1.ResourceCPU, "4000m").
+						Resource(corev1.ResourceMemory, "15Mi").
+						FlavorQuotas,
+				).ResourceGroup(
+				utiltesting.MakeFlavorQuotas("b_one").
+					Resource("example.com/gpu", "4").
+					FlavorQuotas,
+			).Cohort("test-cohort").
+				ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "two", Resource: corev1.ResourceMemory}: 10 * utiltesting.Mi,
+			},
+			cohortResources: &cohortResources{
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:    2000,
```

  While at it, to improve readability:
  
  ```suggestion
  					{Flavor: "one", Resource: corev1.ResourceCPU}:    2_000,
  ```

- **pkg/util/testing/wrappers.go:809** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T16:07:02Z
```diff
@@ -758,25 +758,57 @@ func (f *FlavorQuotasWrapper) Obj() *kueue.FlavorQuotas {
 	return &f.FlavorQuotas
 }
 
+// Resource takes ResourceName, followed by the optional NominalQuota, BorrowingLimit, LendingLimit.
 func (f *FlavorQuotasWrapper) Resource(name corev1.ResourceName, qs ...string) *FlavorQuotasWrapper {
-	rq := kueue.ResourceQuota{
-		Name: name,
-	}
+	resourceWrapper := f.ResourceQuotaWrapper(name)
 	if len(qs) > 0 {
-		rq.NominalQuota = resource.MustParse(qs[0])
+		resourceWrapper.NominalQuota(qs[0])
 	}
 	if len(qs) > 1 && len(qs[1]) > 0 {
-		rq.BorrowingLimit = ptr.To(resource.MustParse(qs[1]))
+		resourceWrapper.BorrowingLimit(qs[1])
 	}
 	if len(qs) > 2 && len(qs[2]) > 0 {
-		rq.LendingLimit = ptr.To(resource.MustParse(qs[2]))
+		resourceWrapper.LendingLimit(qs[2])
 	}
 	if len(qs) > 3 {
 		panic("Must have at most 3 quantities for nominalQuota, borrowingLimit and lendingLimit")
 	}
+	return resourceWrapper.Build()
+}
+
+// ResourceQuotaWrapper allows creation the creation of a Resource in a type-safe manner.
+func (f *FlavorQuotasWrapper) ResourceQuotaWrapper(name corev1.ResourceName) *resourceQuotaWrapper {
+	rq := kueue.ResourceQuota{
+		Name: name,
+	}
+	return &resourceQuotaWrapper{parent: f, ResourceQuota: rq}
+}
+
+// resourceQuotaWrapper wraps a ResourceQuota object.
+type resourceQuotaWrapper struct {
+	parent *FlavorQuotasWrapper
+	kueue.ResourceQuota
+}
+
+func (rq *resourceQuotaWrapper) NominalQuota(quantity string) *resourceQuotaWrapper {
+	rq.ResourceQuota.NominalQuota = resource.MustParse(quantity)
+	return rq
+}
+
+func (rq *resourceQuotaWrapper) BorrowingLimit(quantity string) *resourceQuotaWrapper {
+	rq.ResourceQuota.BorrowingLimit = ptr.To(resource.MustParse(quantity))
+	return rq
+}
+
+func (rq *resourceQuotaWrapper) LendingLimit(quantity string) *resourceQuotaWrapper {
+	rq.ResourceQuota.LendingLimit = ptr.To(resource.MustParse(quantity))
+	return rq
+}
 
-	f.Resources = append(f.Resources, rq)
-	return f
+// Adds the ResourceQuotaWrapper to its parent
+func (rq *resourceQuotaWrapper) Build() *FlavorQuotasWrapper {
```

  ```suggestion
  func (rq *resourceQuotaWrapper) Append() *FlavorQuotasWrapper {
  ```

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1816** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T16:10:07Z
```diff
@@ -2172,30 +1797,25 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
-				},
-				FlavorFungibility: defaultFlavorFungibility,
-				ResourceGroups: []cache.ResourceGroup{{
-					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
-					Flavors: []cache.FlavorQuotas{{
-						Name: "one",
-						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourcePods: {Nominal: 10},
-							corev1.ResourceCPU:  {Nominal: 10_000, LendingLimit: ptr.To[int64](0)},
-						},
-					}},
-				}},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				FlavorFungibility(defaultFlavorFungibility).
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						Resource(corev1.ResourceCPU, "10000m", "10000m", "0m").
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
 					{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-				}.Unflatten(),
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2000,
```

  +1. nil borrowingLimit means "borrow any amount". Please revert numbers.

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1957** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T16:13:10Z
```diff
@@ -2352,15 +1948,174 @@ func TestAssignFlavors(t *testing.T) {
 					ReclaimablePods: tc.wlReclaimablePods,
 				},
 			})
-			if tc.clusterQueue.FlavorFungibility.WhenCanBorrow == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow = defaultFlavorFungibility.WhenCanBorrow
+			}
+
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt = defaultFlavorFungibility.WhenCanPreempt
+			}
```

  do we still need this? I think this is handled by the cache logic now.

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:66** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T16:17:08Z
```diff
@@ -59,13 +63,16 @@ func TestAssignFlavors(t *testing.T) {
 	}
 
 	cases := map[string]struct {
-		wlPods             []kueue.PodSet
-		wlReclaimablePods  []kueue.ReclaimablePod
-		clusterQueue       cache.ClusterQueue
-		wantRepMode        FlavorAssignmentMode
-		wantAssignment     Assignment
-		enableLendingLimit bool
-		enableFairSharing  bool
+		wlPods                      []kueue.PodSet
+		wlReclaimablePods           []kueue.ReclaimablePod
+		clusterQueue                kueue.ClusterQueue
+		clusterQueueUsage           resources.FlavorResourceQuantitiesFlat
+		clusterQueueGuaranteedQuota resources.FlavorResourceQuantitiesFlat
```

  this should not be necessary. It should be calculated by the cache logic based on the lendingLimit

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T16:26:21Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1957** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T16:26:21Z
```diff
@@ -2352,15 +1948,174 @@ func TestAssignFlavors(t *testing.T) {
 					ReclaimablePods: tc.wlReclaimablePods,
 				},
 			})
-			if tc.clusterQueue.FlavorFungibility.WhenCanBorrow == "" {
-				tc.clusterQueue.FlavorFungibility.WhenCanBorrow = kueue.Borrow
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanBorrow = defaultFlavorFungibility.WhenCanBorrow
+			}
+
+			if tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt == "" {
+				tc.clusterQueue.Spec.FlavorFungibility.WhenCanPreempt = defaultFlavorFungibility.WhenCanPreempt
+			}
```

  removed

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T16:29:18Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:66** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T16:29:17Z
```diff
@@ -59,13 +63,16 @@ func TestAssignFlavors(t *testing.T) {
 	}
 
 	cases := map[string]struct {
-		wlPods             []kueue.PodSet
-		wlReclaimablePods  []kueue.ReclaimablePod
-		clusterQueue       cache.ClusterQueue
-		wantRepMode        FlavorAssignmentMode
-		wantAssignment     Assignment
-		enableLendingLimit bool
-		enableFairSharing  bool
+		wlPods                      []kueue.PodSet
+		wlReclaimablePods           []kueue.ReclaimablePod
+		clusterQueue                kueue.ClusterQueue
+		clusterQueueUsage           resources.FlavorResourceQuantitiesFlat
+		clusterQueueGuaranteedQuota resources.FlavorResourceQuantitiesFlat
```

  after removal, a bunch of tests fail. we can clean up in https://github.com/kubernetes-sigs/kueue/issues/2502

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T16:38:52Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:87** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T16:38:52Z
```diff
@@ -74,18 +81,13 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceMemory, "1Mi").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{{
-					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourceMemory),
-					Flavors: []cache.FlavorQuotas{{
-						Name: "default",
-						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourceCPU:    {Nominal: 1000},
-							corev1.ResourceMemory: {Nominal: 2 * utiltesting.Mi},
-						},
-					}},
-				}},
-			},
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("default").
+						Resource(corev1.ResourceCPU, "1000m").
```

  s/000m//g

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T16:39:05Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:347** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T16:39:05Z
```diff
@@ -381,52 +323,36 @@ func TestAssignFlavors(t *testing.T) {
 					Request("example.com/gpu", "3").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{
-					{
-						CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourceMemory),
-						Flavors: []cache.FlavorQuotas{
-							{
-								Name: "one",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 2000},
-									corev1.ResourceMemory: {Nominal: utiltesting.Gi},
-								},
-							},
-							{
-								Name: "two",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 4000},
-									corev1.ResourceMemory: {Nominal: 15 * utiltesting.Mi},
-								},
-							},
-						},
-					},
-					{
-						CoveredResources: sets.New[corev1.ResourceName]("example.com/gpu"),
-						Flavors: []cache.FlavorQuotas{{
-							Name: "b_one",
-							Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-								"example.com/gpu": {Nominal: 4},
-							},
-						}},
-					},
-				},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourceCPU, "2000m").
+						Resource(corev1.ResourceMemory, "1Gi").
+						FlavorQuotas,
+					utiltesting.MakeFlavorQuotas("two").
+						Resource(corev1.ResourceCPU, "4000m").
+						Resource(corev1.ResourceMemory, "15Mi").
+						FlavorQuotas,
+				).ResourceGroup(
+				utiltesting.MakeFlavorQuotas("b_one").
+					Resource("example.com/gpu", "4").
+					FlavorQuotas,
+			).Cohort("test-cohort").
+				ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "two", Resource: corev1.ResourceMemory}: 10 * utiltesting.Mi,
+			},
+			cohortResources: &cohortResources{
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:    2000,
```

  s/([0-9])([0-9]{3}),/\1_\2,/g

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-28T16:53:56Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:66** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T16:53:56Z
```diff
@@ -59,13 +63,16 @@ func TestAssignFlavors(t *testing.T) {
 	}
 
 	cases := map[string]struct {
-		wlPods             []kueue.PodSet
-		wlReclaimablePods  []kueue.ReclaimablePod
-		clusterQueue       cache.ClusterQueue
-		wantRepMode        FlavorAssignmentMode
-		wantAssignment     Assignment
-		enableLendingLimit bool
-		enableFairSharing  bool
+		wlPods                      []kueue.PodSet
+		wlReclaimablePods           []kueue.ReclaimablePod
+		clusterQueue                kueue.ClusterQueue
+		clusterQueueUsage           resources.FlavorResourceQuantitiesFlat
+		clusterQueueGuaranteedQuota resources.FlavorResourceQuantitiesFlat
```

  Maybe some tests didn't put the right LendingLimit.
  Ok.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-06-28T16:56:03Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:347** by [@gabesaba](https://github.com/gabesaba) — 2024-06-28T16:56:03Z
```diff
@@ -381,52 +323,36 @@ func TestAssignFlavors(t *testing.T) {
 					Request("example.com/gpu", "3").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{
-					{
-						CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourceMemory),
-						Flavors: []cache.FlavorQuotas{
-							{
-								Name: "one",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 2000},
-									corev1.ResourceMemory: {Nominal: utiltesting.Gi},
-								},
-							},
-							{
-								Name: "two",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 4000},
-									corev1.ResourceMemory: {Nominal: 15 * utiltesting.Mi},
-								},
-							},
-						},
-					},
-					{
-						CoveredResources: sets.New[corev1.ResourceName]("example.com/gpu"),
-						Flavors: []cache.FlavorQuotas{{
-							Name: "b_one",
-							Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-								"example.com/gpu": {Nominal: 4},
-							},
-						}},
-					},
-				},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourceCPU, "2000m").
+						Resource(corev1.ResourceMemory, "1Gi").
+						FlavorQuotas,
+					utiltesting.MakeFlavorQuotas("two").
+						Resource(corev1.ResourceCPU, "4000m").
+						Resource(corev1.ResourceMemory, "15Mi").
+						FlavorQuotas,
+				).ResourceGroup(
+				utiltesting.MakeFlavorQuotas("b_one").
+					Resource("example.com/gpu", "4").
+					FlavorQuotas,
+			).Cohort("test-cohort").
+				ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "two", Resource: corev1.ResourceMemory}: 10 * utiltesting.Mi,
+			},
+			cohortResources: &cohortResources{
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:    2000,
```

  this missed a few of the 1024s. I reverted those anyway, as 1024 looks better than 1_024

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-28T17:05:19Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:347** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T17:05:19Z
```diff
@@ -381,52 +323,36 @@ func TestAssignFlavors(t *testing.T) {
 					Request("example.com/gpu", "3").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				ResourceGroups: []cache.ResourceGroup{
-					{
-						CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourceMemory),
-						Flavors: []cache.FlavorQuotas{
-							{
-								Name: "one",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 2000},
-									corev1.ResourceMemory: {Nominal: utiltesting.Gi},
-								},
-							},
-							{
-								Name: "two",
-								Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-									corev1.ResourceCPU:    {Nominal: 4000},
-									corev1.ResourceMemory: {Nominal: 15 * utiltesting.Mi},
-								},
-							},
-						},
-					},
-					{
-						CoveredResources: sets.New[corev1.ResourceName]("example.com/gpu"),
-						Flavors: []cache.FlavorQuotas{{
-							Name: "b_one",
-							Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-								"example.com/gpu": {Nominal: 4},
-							},
-						}},
-					},
-				},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourceCPU, "2000m").
+						Resource(corev1.ResourceMemory, "1Gi").
+						FlavorQuotas,
+					utiltesting.MakeFlavorQuotas("two").
+						Resource(corev1.ResourceCPU, "4000m").
+						Resource(corev1.ResourceMemory, "15Mi").
+						FlavorQuotas,
+				).ResourceGroup(
+				utiltesting.MakeFlavorQuotas("b_one").
+					Resource("example.com/gpu", "4").
+					FlavorQuotas,
+			).Cohort("test-cohort").
+				ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "two", Resource: corev1.ResourceMemory}: 10 * utiltesting.Mi,
+			},
+			cohortResources: &cohortResources{
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:    2000,
```

  that's interesting... is that for memory? can we use `util.Ki` instead?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-28T17:10:56Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1809** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T17:10:56Z
```diff
@@ -2172,30 +1791,24 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("10").LendingLimit("0").Append().
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2_000,
```

  It looks like this was `10_000` before

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-28T17:17:27Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:105** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-28T17:17:27Z
```diff
@@ -96,14 +94,14 @@ func TestAssignFlavors(t *testing.T) {
 							corev1.ResourceMemory: {Name: "default", Mode: Fit, TriedFlavorIdx: -1},
 						},
 						Requests: corev1.ResourceList{
-							corev1.ResourceCPU:    resource.MustParse("1000m"),
+							corev1.ResourceCPU:    resource.MustParse("1"),
 							corev1.ResourceMemory: resource.MustParse("1Mi"),
 						},
 						Count: 1,
 					},
 				},
 				Usage: resources.FlavorResourceQuantitiesFlat{
-					{Flavor: "default", Resource: corev1.ResourceCPU}:    1000,
+					{Flavor: "default", Resource: corev1.ResourceCPU}:    1_000,
 					{Flavor: "default", Resource: corev1.ResourceMemory}: 1 * 1024 * 1024,
```

  ```suggestion
  					{Flavor: "default", Resource: corev1.ResourceMemory}: utiltesting.Mi,
  ```

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-02T10:23:00Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1809** by [@gabesaba](https://github.com/gabesaba) — 2024-07-02T09:51:41Z
```diff
@@ -2172,30 +1791,24 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("10").LendingLimit("0").Append().
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2_000,
```

  This test was previously broken/didn't make sense, as it set up the internal state incorrectly. With a lending limit of 0, it should have also set guaranteed quota to 10. I made the minimal update to make it work again. After my change to use the cache, it was failing as the workload actually fit.
  
  I am going to rename, by s/lendinglimit/quota//

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1982** by [@gabesaba](https://github.com/gabesaba) — 2024-07-02T09:51:51Z
```diff
@@ -2373,6 +1993,19 @@ func TestAssignFlavors(t *testing.T) {
 	}
 }
 
+func updateResources(dest resources.FlavorResourceQuantities, src resources.FlavorResourceQuantitiesFlat) {
```

  Actually, was able to get rid of guaranteedQuota, as this value is now updated in cache logic.

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:66** by [@gabesaba](https://github.com/gabesaba) — 2024-07-02T10:22:54Z
```diff
@@ -59,13 +63,16 @@ func TestAssignFlavors(t *testing.T) {
 	}
 
 	cases := map[string]struct {
-		wlPods             []kueue.PodSet
-		wlReclaimablePods  []kueue.ReclaimablePod
-		clusterQueue       cache.ClusterQueue
-		wantRepMode        FlavorAssignmentMode
-		wantAssignment     Assignment
-		enableLendingLimit bool
-		enableFairSharing  bool
+		wlPods                      []kueue.PodSet
+		wlReclaimablePods           []kueue.ReclaimablePod
+		clusterQueue                kueue.ClusterQueue
+		clusterQueueUsage           resources.FlavorResourceQuantitiesFlat
+		clusterQueueGuaranteedQuota resources.FlavorResourceQuantitiesFlat
```

  I was able to remove guaranteedQuota; I'm not sure what was happening last week (unless I thought you were referring to a different field?)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-02T10:26:46Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1816** by [@gabesaba](https://github.com/gabesaba) — 2024-07-02T10:26:46Z
```diff
@@ -2172,30 +1797,25 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
-				},
-				FlavorFungibility: defaultFlavorFungibility,
-				ResourceGroups: []cache.ResourceGroup{{
-					CoveredResources: sets.New(corev1.ResourceCPU, corev1.ResourcePods),
-					Flavors: []cache.FlavorQuotas{{
-						Name: "one",
-						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourcePods: {Nominal: 10},
-							corev1.ResourceCPU:  {Nominal: 10_000, LendingLimit: ptr.To[int64](0)},
-						},
-					}},
-				}},
-				Usage: resources.FlavorResourceQuantitiesFlat{
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				FlavorFungibility(defaultFlavorFungibility).
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						Resource(corev1.ResourceCPU, "10000m", "10000m", "0m").
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
 					{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-				}.Unflatten(),
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2000,
```

  Sorry, my explaination was wrong, but the test was still broken. See explaination here https://github.com/kubernetes-sigs/kueue/pull/2486#discussion_r1662224680

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-02T11:01:37Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1779** by [@mimowo](https://github.com/mimowo) — 2024-07-02T11:01:36Z
```diff
@@ -2153,49 +1763,43 @@ func TestAssignFlavors(t *testing.T) {
 						corev1.ResourcePods: {Name: "one", Mode: Fit, TriedFlavorIdx: -1},
 					},
 					Requests: corev1.ResourceList{
-						corev1.ResourceCPU:  resource.MustParse("9000m"),
+						corev1.ResourceCPU:  resource.MustParse("9"),
 						corev1.ResourcePods: resource.MustParse("1"),
 					},
 					Count: 1,
 				}},
 				Borrowing: true,
 				Usage: resources.FlavorResourceQuantitiesFlat{
-					{Flavor: "one", Resource: corev1.ResourceCPU}:  9000,
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  9_000,
 					{Flavor: "one", Resource: corev1.ResourcePods}: 1,
 				}.Unflatten(),
 			},
 			enableLendingLimit: true,
 		},
-		"lendingLimit exceeded, but can preempt in cohort and ClusterQueue": {
+		"quota exceeded, but can preempt in cohort and ClusterQueue": {
```

  ```suggestion
  		"quota exhausted, but can preempt in cohort and ClusterQueue": {
  ```
  ?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-02T12:26:53Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1809** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-02T12:26:53Z
```diff
@@ -2172,30 +1791,24 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("10").LendingLimit("0").Append().
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2_000,
```

  I'm pretty sure this test was added when the lendingLimit feature was added. So we are probably missing some coverage on it now. @kerthcet do you recall what was the intention of the test and can you send a PR to fix it?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2024-07-03T11:44:42Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1809** by [@kerthcet](https://github.com/kerthcet) — 2024-07-03T11:44:42Z
```diff
@@ -2172,30 +1791,24 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("10").LendingLimit("0").Append().
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2_000,
```

  I'll take a look later. Thanks for reminding, almost missed it.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2024-07-08T07:11:33Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1809** by [@kerthcet](https://github.com/kerthcet) — 2024-07-08T07:11:33Z
```diff
@@ -2172,30 +1791,24 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("10").LendingLimit("0").Append().
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2_000,
```

  I couldn't find the case, can anyone point that out？

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-08T18:48:19Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1809** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-08T18:48:18Z
```diff
@@ -2172,30 +1791,24 @@ func TestAssignFlavors(t *testing.T) {
 					Request(corev1.ResourceCPU, "9").
 					Obj(),
 			},
-			clusterQueue: cache.ClusterQueue{
-				Cohort: &cache.Cohort{
-					Usage: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}: 2000,
-					}.Unflatten(),
-					RequestableResources: resources.FlavorResourceQuantitiesFlat{
-						{Flavor: "one", Resource: corev1.ResourceCPU}:  10000,
-						{Flavor: "one", Resource: corev1.ResourcePods}: 10,
-					}.Unflatten(),
+			clusterQueue: utiltesting.MakeClusterQueue("test-clusterqueue").
+				ResourceGroup(
+					utiltesting.MakeFlavorQuotas("one").
+						Resource(corev1.ResourcePods, "10").
+						ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("10").LendingLimit("0").Append().
+						FlavorQuotas,
+				).Cohort("test-cohort").ClusterQueue,
+			clusterQueueUsage: resources.FlavorResourceQuantitiesFlat{
+				{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+			},
+			cohortResources: &cohortResources{
+				usage: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}: 2_000,
+				},
+				requestableResources: resources.FlavorResourceQuantitiesFlat{
+					{Flavor: "one", Resource: corev1.ResourceCPU}:  2_000,
```

  ![image](https://github.com/kubernetes-sigs/kueue/assets/1299064/ced2d2aa-9993-4e56-b5f2-de10ffe77044)
