# PR #2493: [jobframework] Only check if the owner is managed for enabled integrations.

**Summary**: [jobframework] Only check if the owner is managed for enabled integrations.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2493

**Last updated**: 2024-07-12T16:35:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `owner-integration-disabled` → `main`
- **Created**: 2024-06-27T17:54:47Z
- **Updated**: 2024-07-12T16:35:17Z
- **Merged**: 2024-07-12T15:32:43Z
- **Closed**: 2024-07-12T15:32:43Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 12   **+151 / -9**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Only check if the owner of a job is managed against the list of  jobs integrations currently enabled .
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2481

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Allow usage of the pod integration for pods belonging to jobs that Kueue supports, if the support for the job type is explicitly disabled
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-27T17:54:49Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-27T17:55:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a31adaeaf2ec2bff2908c48c9e0e39b46e80a789 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6691417fe61ac500089b1c99 |

### Comment by [@trasc](https://github.com/trasc) — 2024-06-27T17:55:10Z

/test all

### Comment by [@trasc](https://github.com/trasc) — 2024-06-27T19:36:08Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-08T14:10:47Z

/assign

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-10T13:40:42Z

/lgtm
/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-10T13:40:48Z

LGTM label has been added.  <details>Git tree hash: 5b8b87cd12688d45b899edfe7a71db8c79d94b35</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T19:50:40Z

/release-note-edit

```release-note
Allow usage of the pod integration for pods belonging to jobs that Kueue supports, if the support for the job type is explicitly disabled
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-12T15:30:28Z

LGTM label has been added.  <details>Git tree hash: ab20814953f829dba2144f872197f1081f81772c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-12T15:30:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2493#pullrequestreview-2175194296" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2493#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor,trasc]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T07:44:28Z

Implementation lgtm. Question related to improving reliability by an integration level test, which would cover the change in `SetupControllers`.

- **pkg/controller/jobframework/setup.go:86** by [@mimowo](https://github.com/mimowo) — 2024-07-09T07:36:26Z
```diff
@@ -83,6 +83,7 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 				if err = cb.SetupWebhook(mgr, opts...); err != nil {
 					return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
 				}
+				EnableIntegration(name)
```

  If we forgot to add this line all tests would pass, but the code would not work from the user perspective. 
  Would it be feasible to call `SetupControllers` from the integration tests to cover this (potentially in a follow up if requires refactoring)?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-09T10:38:29Z

- **pkg/controller/jobframework/setup.go:86** by [@trasc](https://github.com/trasc) — 2024-07-09T10:38:29Z
```diff
@@ -83,6 +83,7 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 				if err = cb.SetupWebhook(mgr, opts...); err != nil {
 					return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
 				}
+				EnableIntegration(name)
```

  > If we forgot to add this line all tests would pass, but the code would not work from the user perspective.
  
  I don't think I follow.
  
  > Would it be feasible to call `SetupControllers` from the integration tests to cover this (potentially in a follow up if requires refactoring)?
  
  Not really, in the integration test we need more flexibility (start only the controllers and not the web-hook server, only consider `rayjob` integration enabled but run the `raycluster`).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T10:53:56Z

- **pkg/controller/jobframework/setup.go:86** by [@mimowo](https://github.com/mimowo) — 2024-07-09T10:53:56Z
```diff
@@ -83,6 +83,7 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 				if err = cb.SetupWebhook(mgr, opts...); err != nil {
 					return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
 				}
+				EnableIntegration(name)
```

  Re: if you remove this line, will the build with all tests pass?
  
   Sure if there are tests that require more flexibility we could keep them. One which calls this function would be enough to get started.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-09T14:20:25Z

- **pkg/controller/jobframework/setup.go:86** by [@trasc](https://github.com/trasc) — 2024-07-09T14:20:25Z
```diff
@@ -83,6 +83,7 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 				if err = cb.SetupWebhook(mgr, opts...); err != nil {
 					return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
 				}
+				EnableIntegration(name)
```

  Added a check in the setup unit test.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-07-09T23:17:08Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-09T23:09:11Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  I think that we should not put any function only for testing.
  Could you move the testing utils package?
  
  I guess that implementing the `GetEnabledIntegrations` function in this package allows us to move this function to another package, right?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-10T06:49:43Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@trasc](https://github.com/trasc) — 2024-07-10T06:49:43Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  Not relay we need to be able to revert the changes done. we will need to add two extra public funcs  one to get the integrations previously enabled and another one to restore the configuration, the new funcs will also only be used in testing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T06:57:05Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@mimowo](https://github.com/mimowo) — 2024-07-10T06:57:05Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  Another possibility would be to create a new instance of integrationManager per test case.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-10T07:00:04Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@trasc](https://github.com/trasc) — 2024-07-10T06:55:13Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  I guess an alternative can be to return the revert function from `EnableIntegration` and ignore the returned function when not in a test. (But I don't like this idea too much)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-10T07:02:22Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@trasc](https://github.com/trasc) — 2024-07-10T07:02:22Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  The code impact will be too much with a close to 0 added value.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T07:22:39Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@mimowo](https://github.com/mimowo) — 2024-07-10T07:22:39Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  Adding functions, into production code, just for testing should be avoided (if feasible, and it seems that with the new instance it could work well). 
  
  Code should generally separate concerns. In particular, separate business responsibilities and testing to make the production code focused, making it easier to understand and thus maintain.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-07-10T09:15:51Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-10T09:15:51Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  > Not relay we need to be able to revert the changes done. we will need to add two extra public funcs one to get the integrations previously enabled and another one to restore the configuration, the new funcs will also only be used in testing.
  
  If so, shouldn't we receive the `integration manager` as an EnableIntegrationsForTesting argument?
  I believe that `SetFeatureGateDuringTest` function would be a good example.
  
  > Adding functions, into production code, just for testing should be avoided (if feasible, and it seems that with the new instance it could work well).
  > 
  > Code should generally separate concerns. In particular, separate business responsibilities and testing to make the production code focused, making it easier to understand and thus maintain.
  
  I fully agree with you. We never want to mix the production codes and testing code.
  Additionally, this function is exposed, which means the third party providers could import.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-10T12:28:02Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@trasc](https://github.com/trasc) — 2024-07-10T12:28:02Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  > > Not relay we need to be able to revert the changes done. we will need to add two extra public funcs one to get the integrations previously enabled and another one to restore the configuration, the new funcs will also only be used in testing.
  > 
  > If so, shouldn't we receive the `integration manager` as an EnableIntegrationsForTesting argument? I believe that `SetFeatureGateDuringTest` function would be a good example.
  > 
  
  There is no knowledge of `integrationManager` type existence outside the jobframework package and in my opinion it should stay that way. For the rest of the integration manager is just a global state that being the reason why we need a revert mechanism.
  
  
  > > Adding functions, into production code, just for testing should be avoided (if feasible, and it seems that with the new instance it could work well).
  > > Code should generally separate concerns. In particular, separate business responsibilities and testing to make the production code focused, making it easier to understand and thus maintain.
  > 
  > I fully agree with you. We never want to mix the production codes and testing code. Additionally, this function is exposed, which means the third party providers could import.
  
  Go being able to support build time tests only funcs out of the box will be ideal however we can only try to:
  - Put the test specific code that needs to be accessed from outside the package in conditional built file `//go:build ....`, and only use tat build tag when running the unit test
  - Panic in the test specific code if `!testing.Testing()`
  
  However both are overkill in my opinion.
  
  Also this in not the only `test dedicated`  implementation in kueue, https://github.com/kubernetes-sigs/kueue/blob/a3688c4a1bd30743414c3e1bef71369dfde13521/pkg/scheduler/scheduler.go#L76 is just one other example.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T12:56:44Z

- **pkg/controller/jobframework/setup_test.go:46** by [@mimowo](https://github.com/mimowo) — 2024-07-10T12:56:44Z
```diff
@@ -43,10 +43,38 @@ import (
 )
 
 func TestSetupControllers(t *testing.T) {
+	aviableIntegrations := map[string]IntegrationCallbacks{
```

  ```suggestion
  	availableIntegrations := map[string]IntegrationCallbacks{
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T13:05:06Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@mimowo](https://github.com/mimowo) — 2024-07-10T13:05:06Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  I don't have a clearly advantageous approach, so I'm willing to accept it, unless maybe @alculquicondor can recommend something? In fact, this issue would show up for external frameworks in https://github.com/kubernetes-sigs/kueue/pull/2059/ if we wanted to modify the state of the integrationManager from unit tests in other packages.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-10T13:38:51Z

- **pkg/controller/jobframework/setup_test.go:46** by [@trasc](https://github.com/trasc) — 2024-07-10T13:38:51Z
```diff
@@ -43,10 +43,38 @@ import (
 )
 
 func TestSetupControllers(t *testing.T) {
+	aviableIntegrations := map[string]IntegrationCallbacks{
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-07-10T19:18:09Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-10T19:18:09Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  > Also this in not the only test dedicated implementation in kueue,
  > 
  > [kueue/pkg/scheduler/scheduler.go](https://github.com/kubernetes-sigs/kueue/blob/a3688c4a1bd30743414c3e1bef71369dfde13521/pkg/scheduler/scheduler.go#L76)
  > 
  > Line 76 in [a3688c4](https://github.com/kubernetes-sigs/kueue/commit/a3688c4a1bd30743414c3e1bef71369dfde13521)
  > 
  >  applyAdmission func(context.Context, *kueue.Workload) error 
  > is just one other example.
  
  Significantly differences between this one and the `applyAdmission` is if the knob is exposed to other packages.
  In the case of `applyAdmission`, we can not modify the field from other packages.
  
  So, at least, it would be more better to add a testing object as a `EnableIntegrationsForTest` arguments like this. This is generic approach to prevent accidently used by production codes. After we find better approach, we can revisit here.
  
  ```suggestion
  // EnableIntegrationsForTest - should be used only in tests
  // Mark the frameworks identified by names and return a revert function.
  func EnableIntegrationsForTest(tb testing.TB, names ...string) {
  	tb.Helper()
  	[...]
  	tb.Cleanup(func(
  		manager.enabledIntegrations = old
  	))
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-11T04:30:14Z

- **pkg/controller/jobframework/integrationmanager.go:207** by [@trasc](https://github.com/trasc) — 2024-07-11T04:30:14Z
```diff
@@ -186,6 +197,23 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(names ...string) func() {
```

  
  > Significantly differences between this one and the `applyAdmission` is if the knob is exposed to other packages. In the case of `applyAdmission`, we can not modify the field from other packages.
  
  That is irrelevant if we talk about having **only for testing** code (`I think that we should not put any function only for testing.`) and `applyAdmission` is not the only one.
  
  > So, at least, it would be more better to add a testing object as a `EnableIntegrationsForTest` arguments like this. This is generic approach to prevent accidently used by production codes. After we find better approach, we can revisit here.
  
  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-11T20:13:26Z

- **pkg/controller/jobframework/integrationmanager.go:208** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T19:53:52Z
```diff
@@ -186,6 +198,24 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(tb testing.TB, names ...string) func() {
```

  Maybe for a follow up, but I would prefer that, instead, we get rid of the manager as a global variable and pass it to the functions or store it in the structs that need it.

- **pkg/controller/jobframework/integrationmanager_test.go:380** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T20:03:23Z
```diff
@@ -363,18 +377,28 @@ func TestGetJobTypeForOwner(t *testing.T) {
 	externalK3 := func() runtime.Object {
 		return &metav1.PartialObjectMetadata{TypeMeta: metav1.TypeMeta{Kind: "K3"}}
 	}()
+	manageK4 := func() IntegrationCallbacks {
```

  ```suggestion
  	disabledK4 := func() IntegrationCallbacks {
  ```

- **pkg/controller/jobs/pod/pod_controller_test.go:4882** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T20:10:05Z
```diff
@@ -4879,6 +4879,7 @@ func TestReconciler_ErrorFinalizingPod(t *testing.T) {
 }
 
 func TestIsPodOwnerManagedByQueue(t *testing.T) {
+	defer jobframework.EnableIntegrationsForTest(t, "batch/job", "ray.io/raycluster")()
```

  ```suggestion
  	t.CleanUp(jobframework.EnableIntegrationsForTest(t, "batch/job", "ray.io/raycluster"))
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-12T05:54:43Z

- **pkg/controller/jobframework/integrationmanager.go:208** by [@trasc](https://github.com/trasc) — 2024-07-12T05:54:43Z
```diff
@@ -186,6 +198,24 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(tb testing.TB, names ...string) func() {
```

  I'm not a fan of global states, however in this case when we are doing init bases framework registration this is our best option.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-12T06:34:50Z

- **pkg/controller/jobframework/integrationmanager_test.go:380** by [@trasc](https://github.com/trasc) — 2024-07-12T06:23:43Z
```diff
@@ -363,18 +377,28 @@ func TestGetJobTypeForOwner(t *testing.T) {
 	externalK3 := func() runtime.Object {
 		return &metav1.PartialObjectMetadata{TypeMeta: metav1.TypeMeta{Kind: "K3"}}
 	}()
+	manageK4 := func() IntegrationCallbacks {
```

  done

- **pkg/controller/jobs/pod/pod_controller_test.go:4882** by [@trasc](https://github.com/trasc) — 2024-07-12T06:25:18Z
```diff
@@ -4879,6 +4879,7 @@ func TestReconciler_ErrorFinalizingPod(t *testing.T) {
 }
 
 func TestIsPodOwnerManagedByQueue(t *testing.T) {
+	defer jobframework.EnableIntegrationsForTest(t, "batch/job", "ray.io/raycluster")()
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-12T14:29:30Z

- **pkg/controller/jobframework/integrationmanager.go:208** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-12T14:29:30Z
```diff
@@ -186,6 +198,24 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(tb testing.TB, names ...string) func() {
```

  It can still be done differently. There could be one `DefaultIntegrationManager` that the implementations can register to.
  
  But, you still can pass a different manager to the objects that need it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-12T14:29:50Z

- **pkg/controller/jobs/pod/pod_controller_test.go:4882** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-12T14:29:50Z
```diff
@@ -4879,6 +4879,7 @@ func TestReconciler_ErrorFinalizingPod(t *testing.T) {
 }
 
 func TestIsPodOwnerManagedByQueue(t *testing.T) {
+	defer jobframework.EnableIntegrationsForTest(t, "batch/job", "ray.io/raycluster")()
```

  Please do the same in all the usages of this function.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-12T14:39:40Z

- **pkg/controller/jobframework/integrationmanager.go:208** by [@trasc](https://github.com/trasc) — 2024-07-12T14:39:40Z
```diff
@@ -186,6 +198,24 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(tb testing.TB, names ...string) func() {
```

  Sure it could have been done in a different way, but it was done like this a long time ago and reviewed at that time.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-12T14:45:41Z

- **pkg/controller/jobs/pod/pod_controller_test.go:4882** by [@trasc](https://github.com/trasc) — 2024-07-12T14:45:40Z
```diff
@@ -4879,6 +4879,7 @@ func TestReconciler_ErrorFinalizingPod(t *testing.T) {
 }
 
 func TestIsPodOwnerManagedByQueue(t *testing.T) {
+	defer jobframework.EnableIntegrationsForTest(t, "batch/job", "ray.io/raycluster")()
```

  It makes no difference in our usecase, however done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-12T15:30:02Z

- **pkg/controller/jobs/pod/pod_controller_test.go:4882** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-12T15:30:02Z
```diff
@@ -4879,6 +4879,7 @@ func TestReconciler_ErrorFinalizingPod(t *testing.T) {
 }
 
 func TestIsPodOwnerManagedByQueue(t *testing.T) {
+	defer jobframework.EnableIntegrationsForTest(t, "batch/job", "ray.io/raycluster")()
```

  It's just a best practice. We should get into the habit of using the appropriate tools. In this case it doesn't make a difference, but in other cases it could. Just being consistent can direct people writing tests in the future to follow the same pattern.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-12T15:30:24Z

/lgtm
/approve

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-12T15:30:50Z

- **pkg/controller/jobframework/integrationmanager.go:208** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-12T15:30:50Z
```diff
@@ -186,6 +198,24 @@ func ForEachIntegration(f func(name string, cb IntegrationCallbacks) error) erro
 	return manager.forEach(f)
 }
 
+// EnableIntegration marks the integration identified by name as enabled.
+func EnableIntegration(name string) {
+	manager.enableIntegration(name)
+}
+
+// EnableIntegrationsForTest - should be used only in tests
+// Mark the frameworks identified by names and return a revert function.
+func EnableIntegrationsForTest(tb testing.TB, names ...string) func() {
```

  Yes, but, at that time, we were not modifying it in tests.
