# PR #2507: Add documentation for restricting job creation without queue name

**Summary**: Add documentation for restricting job creation without queue name

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2507

**Last updated**: 2024-07-15T14:56:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `documentation/restrict-jobs-creation` → `main`
- **Created**: 2024-06-30T16:01:51Z
- **Updated**: 2024-07-15T14:56:06Z
- **Merged**: 2024-07-03T14:31:09Z
- **Closed**: 2024-07-03T14:31:10Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 3   **+86 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind documentation

#### What this PR does / why we need it:
Documents how to restrict the creation of jobs without queue name using a ValidatingAdmissionPolicy.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #2119

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-06-30T16:02:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 75d90bd3bd48a5d02d1631f60f950d13ccb3bcc9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66855d58be908a0008ad8d9d |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2507--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2507--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI1MDctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.TGstuwep0rlmvLPAJoMdCUgQkqYxgs46_P63wZmYtMs)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-01T06:20:35Z

LGTM label has been added.  <details>Git tree hash: 29df682d9ccb6412ef25d4f5f51abbd916b31819</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-07-02T06:08:36Z

/cc @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-03T14:22:09Z

/lgtm
/approve

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-03T14:22:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2507#issuecomment-2206266740" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2507#" title="Author self-approved">IrvingMg</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-03T14:22:15Z

LGTM label has been added.  <details>Git tree hash: 83cc2c1172948c3ca78d1986e6f0115f0a89f046</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-03T16:18:33Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-07-03T16:19:12Z

@alculquicondor: new pull request created: #2527

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2507#issuecomment-2206735491):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-01T06:20:30Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-02T07:57:01Z

- **site/content/en/docs/tasks/manage/restricting_job_creation.md:39** by [@mimowo](https://github.com/mimowo) — 2024-07-02T07:41:06Z
```diff
@@ -0,0 +1,71 @@
+---
+title: "Restricting job creation with Validating Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Creating a Validating Admission Policy
+
+To verify a job contains a queue name, check that the `kueue.x-k8s.io/queue-name` label exists and has a value set using the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+Here is an example of a ValidatingAdmissionPolicy that rejects job creation without a queue name:
+
+```yaml
+# sample-validating-policy.yaml
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicy
+metadata:
+  name: sample-validating-admission-policy
+spec:
+  failurePolicy: Fail
+  matchConstraints:
+    resourceRules:
+    - apiGroups:   ["batch"]
+      apiVersions: ["v1"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobs"]
+  validations:
+    - expression: "object.metadata.labels.exists_one(key, key == 'kueue.x-k8s.io/queue-name') && object.metadata.labels['kueue.x-k8s.io/queue-name'] != ''"
```

  ```suggestion
      - expression: "'kueue.x-k8s.io/queue-name' in object.metadata.labels && object.metadata.labels['kueue.x-k8s.io/queue-name'] != ''"
  ```
  Shorter, and looks equivalent given that an object cannot have more than one value for a key.

- **site/content/en/docs/tasks/manage/restricting_job_creation.md:2** by [@mimowo](https://github.com/mimowo) — 2024-07-02T07:44:43Z
```diff
@@ -0,0 +1,71 @@
+---
+title: "Restricting job creation with Validating Admission Policy"
```

  ```suggestion
  title: "Setup a Job Admission Policy"
  ```

- **site/content/en/docs/tasks/manage/restricting_job_creation.md:26** by [@mimowo](https://github.com/mimowo) — 2024-07-02T07:48:49Z
```diff
@@ -0,0 +1,71 @@
+---
+title: "Restricting job creation with Validating Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Creating a Validating Admission Policy
+
+To verify a job contains a queue name, check that the `kueue.x-k8s.io/queue-name` label exists and has a value set using the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+Here is an example of a ValidatingAdmissionPolicy that rejects job creation without a queue name:
+
+```yaml
+# sample-validating-policy.yaml
+apiVersion: admissionregistration.k8s.io/v1
```

  Can we include the example rather than embed (to avoid duplication)?

- **site/static/examples/sample-validating-policy.yaml:14** by [@mimowo](https://github.com/mimowo) — 2024-07-02T07:49:27Z
```diff
@@ -0,0 +1,24 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicy
+metadata:
+  name: sample-validating-admission-policy
+spec:
+  failurePolicy: Fail
+  matchConstraints:
+    resourceRules:
+    - apiGroups:   ["batch"]
+      apiVersions: ["v1"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobs"]
+  validations:
+    - expression: "object.metadata.labels.exists_one(key, key == 'kueue.x-k8s.io/queue-name') && object.metadata.labels['kueue.x-k8s.io/queue-name'] != ''"
```

  ```suggestion
      - expression: "'kueue.x-k8s.io/queue-name' in object.metadata.labels && object.metadata.labels['kueue.x-k8s.io/queue-name'] != ''"
  ```
  Shorter, and looks equivalent given that an object cannot have more than one value for a key.

- **site/content/en/docs/tasks/manage/restricting_job_creation.md:22** by [@mimowo](https://github.com/mimowo) — 2024-07-02T07:56:10Z
```diff
@@ -0,0 +1,71 @@
+---
+title: "Restricting job creation with Validating Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Creating a Validating Admission Policy
+
+To verify a job contains a queue name, check that the `kueue.x-k8s.io/queue-name` label exists and has a value set using the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+Here is an example of a ValidatingAdmissionPolicy that rejects job creation without a queue name:
```

  I think users/admins are likely to have two use-cases for which examples would be handy:
  1. how to support more Job types - should we duplicate ValidatingAdmissionPolicy & ValidatingAdmissionPolicyBinding, or there is a way to deduplicate?
  2. how to keep some namespace for administrative purposes (not reject without QueueName).
  
  I'm ok either with extending the example (say for Job and JobSet), or adding a new example (say for excluded administrative namespace)

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-07-02T15:44:59Z

- **site/content/en/docs/tasks/manage/restricting_job_creation.md:22** by [@IrvingMg](https://github.com/IrvingMg) — 2024-07-02T15:44:59Z
```diff
@@ -0,0 +1,71 @@
+---
+title: "Restricting job creation with Validating Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Creating a Validating Admission Policy
+
+To verify a job contains a queue name, check that the `kueue.x-k8s.io/queue-name` label exists and has a value set using the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+Here is an example of a ValidatingAdmissionPolicy that rejects job creation without a queue name:
```

  I've updated the example to make the policy namespace-scoped and restrict JobSet creation as well.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-02T17:07:02Z

- **site/static/examples/sample-validating-policy-binding.yaml:7** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-02T17:03:53Z
```diff
@@ -0,0 +1,7 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicyBinding
+metadata:
+  name: sample-validating-admission-policy-binding
+spec:
+  policyName: sample-validating-admission-policy
+  validationActions: [Deny]
```

  Does this policy match every namespace? If so, I suggest making the example match a specific namespace or a namespace label.

- **site/static/examples/sample-validating-policy.yaml:19** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-02T17:05:32Z
```diff
@@ -0,0 +1,22 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicy
+metadata:
+  name: sample-validating-admission-policy
+spec:
+  failurePolicy: Fail
+  matchConstraints:
+    resourceRules:
+    - apiGroups:   ["batch"]
+      apiVersions: ["v1"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobs"]
+    - apiGroups:   ["jobset.x-k8s.io"]
+      apiVersions: ["v1alpha2"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobsets"]
+  matchConditions:
+    - name: "namespace-scoped-policy"
+      expression: "object.metadata.namespace == 'my-namespace'"
```

  This doesn't look like the most efficient way to match a specific namespace. Better do so in the Binding.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-02T17:09:21Z

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:9** by [@mimowo](https://github.com/mimowo) — 2024-07-02T16:56:50Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
```

  Maybe we could provide a bit more context why one would do it, maybe something along the lines:
  
  This page shows how you can set up for Kueue a Job Admission Policy using the Kubernetes [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy), based on the [Common Expression Language (CEL)](https://github.com/google/cel-spec).

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:18** by [@mimowo](https://github.com/mimowo) — 2024-07-02T16:57:15Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Creating a Validating Admission Policy
```

  ```suggestion
  ## Example
  ```

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:20** by [@mimowo](https://github.com/mimowo) — 2024-07-02T17:04:14Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Creating a Validating Admission Policy
+
+To verify a job contains a queue name, check that the `kueue.x-k8s.io/queue-name` label exists and has a value set using the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
```

  The example below shows you how to set up the Job Admission Policy to reject early all Job or JobSets
  without the queue-name if sent to the user namespaces. At the same time, it allows you to create and
  execute all Jobs in the namespace dedicated to administrative tasks.
  
  In this example we assume that `manageJobsWithoutQueueName` is disabled, letting the admin
  to execute Jobs in the `admin-namespace`. Jobs sent to this namespace aren't rejected, or managed
  by Kueue.

- **site/static/examples/sample-validating-policy.yaml:19** by [@mimowo](https://github.com/mimowo) — 2024-07-02T17:04:58Z
```diff
@@ -0,0 +1,22 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicy
+metadata:
+  name: sample-validating-admission-policy
+spec:
+  failurePolicy: Fail
+  matchConstraints:
+    resourceRules:
+    - apiGroups:   ["batch"]
+      apiVersions: ["v1"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobs"]
+    - apiGroups:   ["jobset.x-k8s.io"]
+      apiVersions: ["v1alpha2"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobsets"]
+  matchConditions:
+    - name: "namespace-scoped-policy"
+      expression: "object.metadata.namespace == 'my-namespace'"
```

  ```suggestion
        expression: "object.metadata.namespace != 'admin-namespace'"
  ```
  To make it a bit more useful as per https://github.com/kubernetes-sigs/kueue/issues/2119

- **site/content/en/docs/tasks/manage/restricting_job_creation.md:22** by [@mimowo](https://github.com/mimowo) — 2024-07-02T17:08:52Z
```diff
@@ -0,0 +1,71 @@
+---
+title: "Restricting job creation with Validating Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Creating a Validating Admission Policy
+
+To verify a job contains a queue name, check that the `kueue.x-k8s.io/queue-name` label exists and has a value set using the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+Here is an example of a ValidatingAdmissionPolicy that rejects job creation without a queue name:
```

  Thanks, I think it looks better, I leave a couple of more comments to make the example less abstract.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-07-03T09:40:45Z

- **site/static/examples/sample-validating-policy-binding.yaml:7** by [@IrvingMg](https://github.com/IrvingMg) — 2024-07-03T09:40:45Z
```diff
@@ -0,0 +1,7 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicyBinding
+metadata:
+  name: sample-validating-admission-policy-binding
+spec:
+  policyName: sample-validating-admission-policy
+  validationActions: [Deny]
```

  The example has been updated to exclude only a specific namespace from validation.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-07-03T09:40:52Z

- **site/static/examples/sample-validating-policy.yaml:19** by [@IrvingMg](https://github.com/IrvingMg) — 2024-07-03T09:40:52Z
```diff
@@ -0,0 +1,22 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicy
+metadata:
+  name: sample-validating-admission-policy
+spec:
+  failurePolicy: Fail
+  matchConstraints:
+    resourceRules:
+    - apiGroups:   ["batch"]
+      apiVersions: ["v1"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobs"]
+    - apiGroups:   ["jobset.x-k8s.io"]
+      apiVersions: ["v1alpha2"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobsets"]
+  matchConditions:
+    - name: "namespace-scoped-policy"
+      expression: "object.metadata.namespace == 'my-namespace'"
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-07-03T09:41:08Z

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:9** by [@IrvingMg](https://github.com/IrvingMg) — 2024-07-03T09:41:08Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows you how to set up a [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy) to prevent the creation of jobs without a queue name within a namespace.
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-07-03T09:42:08Z

- **site/static/examples/sample-validating-policy.yaml:19** by [@IrvingMg](https://github.com/IrvingMg) — 2024-07-03T09:42:07Z
```diff
@@ -0,0 +1,22 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicy
+metadata:
+  name: sample-validating-admission-policy
+spec:
+  failurePolicy: Fail
+  matchConstraints:
+    resourceRules:
+    - apiGroups:   ["batch"]
+      apiVersions: ["v1"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobs"]
+    - apiGroups:   ["jobset.x-k8s.io"]
+      apiVersions: ["v1alpha2"]
+      operations:  ["CREATE", "UPDATE"]
+      resources:   ["jobsets"]
+  matchConditions:
+    - name: "namespace-scoped-policy"
+      expression: "object.metadata.namespace == 'my-namespace'"
```

  Done and moved validation to the Binding.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-03T11:58:36Z

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-03T11:53:58Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows how you can set up for Kueue a Job Admission Policy using the Kubernetes [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy), based on the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Example
+
+The example below shows you how to set up the Job Admission Policy to reject early all Job or JobSets
+without the queue-name if sent to the user namespaces. At the same time, it allows you to create and
+execute all Jobs in the namespace dedicated to administrative tasks.
+
+In this example we assume that `manageJobsWithoutQueueName` is disabled, letting the admin
```

  ```suggestion
  You should set `manageJobsWithoutQueueName` to `false` in the Kueue Configuration to let an admin
  ```

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:16** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-03T11:54:27Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows how you can set up for Kueue a Job Admission Policy using the Kubernetes [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy), based on the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
```

  What about installing Kueue? :)

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:22** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-03T11:55:58Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows how you can set up for Kueue a Job Admission Policy using the Kubernetes [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy), based on the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Example
+
+The example below shows you how to set up the Job Admission Policy to reject early all Job or JobSets
+without the queue-name if sent to the user namespaces. At the same time, it allows you to create and
+execute all Jobs in the namespace dedicated to administrative tasks.
```

  ```suggestion
  without the queue-name if sent to a namespace other than `admin-namespace`.
  ```

- **site/content/en/docs/tasks/manage/setup_job_admission_policy.md:48** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-03T11:57:19Z
```diff
@@ -0,0 +1,53 @@
+---
+title: "Setup a Job Admission Policy"
+date: 2024-06-28
+weight: 10
+description: >
+  Implementing Validating Admission Policy to prevent job creation without queue name.
+---
+
+This page shows how you can set up for Kueue a Job Admission Policy using the Kubernetes [Validating Admission Policy](https://kubernetes.io/docs/reference/access-authn-authz/validating-admission-policy), based on the [Common Expression Language (CEL)](https://github.com/google/cel-spec).
+
+## Before you begin
+
+Ensure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool can communicate with your cluster.
+
+## Example
+
+The example below shows you how to set up the Job Admission Policy to reject early all Job or JobSets
+without the queue-name if sent to the user namespaces. At the same time, it allows you to create and
+execute all Jobs in the namespace dedicated to administrative tasks.
+
+In this example we assume that `manageJobsWithoutQueueName` is disabled, letting the admin
+to execute Jobs in the `admin-namespace`. Jobs sent to this namespace aren't rejected, or managed
+by Kueue.
+
+{{< include "examples/sample-validating-policy.yaml" "yaml" >}}
+
+To create the policy, download the above file and run the following command:
+
+```shell
+kubectl create -f sample-validating-policy.yaml
+```
+
+Then, apply the validating admission policy to the namespace by creating a `ValidatingAdmissionPolicyBinding`. The policy binding links the namespaces to the defined admission policy and it instructs Kubernetes how to respond to the validation outcome.
+
+The following is an example of a policy binding:
+
+{{< include "examples/sample-validating-policy-binding.yaml" "yaml" >}}
+
+To create the binding, download the above file and run the following command:
+
+```shell
+kubectl create -f sample-validating-policy-binding.yaml
+```
+
+Now, when you try to create a `Job` or a `JobSet` without the `kueue.x-k8s.io/queue-name` label or value in any namespace other than `admin-namespace`, an error message will return:
```

  ```suggestion
  Now, when you try to create a `Job` or a `JobSet` without the `kueue.x-k8s.io/queue-name` label or value in any namespace other than `admin-namespace`, the error message will be similar to the following:
  ```

- **site/static/examples/sample-validating-policy-binding.yaml:13** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-03T11:58:33Z
```diff
@@ -0,0 +1,13 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicyBinding
+metadata:
+  name: sample-validating-admission-policy-binding
+spec:
+  policyName: sample-validating-admission-policy
+  validationActions: [Deny]
+  matchResources:
+    namespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: ["admin-namespace"]
```

  @dgrove-oss Is the namespace in openshift kueue-system?
  If so, I think we can include it as part of the values here.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-07-03T15:59:34Z

- **site/static/examples/sample-validating-policy-binding.yaml:13** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-07-03T15:59:34Z
```diff
@@ -0,0 +1,13 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicyBinding
+metadata:
+  name: sample-validating-admission-policy-binding
+spec:
+  policyName: sample-validating-admission-policy
+  validationActions: [Deny]
+  matchResources:
+    namespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: ["admin-namespace"]
```

  When Kueue gets deployed as part of OpenShift AI, it is put in the `redhat-ods-applications` namespace.  I think probably best to just leave as the generic admin-namespace.
  
  @IrvingMg -- thanks for writing this up; this is great!

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-07-12T21:30:19Z

- **site/static/examples/sample-validating-policy-binding.yaml:13** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-07-12T21:30:19Z
```diff
@@ -0,0 +1,13 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicyBinding
+metadata:
+  name: sample-validating-admission-policy-binding
+spec:
+  policyName: sample-validating-admission-policy
+  validationActions: [Deny]
+  matchResources:
+    namespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: ["admin-namespace"]
```

  btw, I tested using validating admission policies today in our [setup](https://github.com/project-codeflare/mlbatch/blob/84610a12ba61cc1d05711c2da1f97b685f98ad61/setup.k8s-v1.30/admission-policy.yaml) (now open sourced).  It worked very nicely.
  We use a label on "user" namespaces to identify where we want the policy to be enforced.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-15T14:56:06Z

- **site/static/examples/sample-validating-policy-binding.yaml:13** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-15T14:56:06Z
```diff
@@ -0,0 +1,13 @@
+apiVersion: admissionregistration.k8s.io/v1
+kind: ValidatingAdmissionPolicyBinding
+metadata:
+  name: sample-validating-admission-policy-binding
+spec:
+  policyName: sample-validating-admission-policy
+  validationActions: [Deny]
+  matchResources:
+    namespaceSelector:
+      matchExpressions:
+        - key: kubernetes.io/metadata.name
+          operator: NotIn
+          values: ["admin-namespace"]
```

  Great to hear!
  
  Btw, have you considered adding yourselves to https://kueue.sigs.k8s.io/docs/adopters/?
