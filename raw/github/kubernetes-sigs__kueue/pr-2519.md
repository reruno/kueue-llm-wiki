# PR #2519: Define ClusterQueueSnapshot and CohortSnapshot types

**Summary**: Define ClusterQueueSnapshot and CohortSnapshot types

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2519

**Last updated**: 2024-07-09T15:50:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `cache_snapshot` → `main`
- **Created**: 2024-07-02T15:07:22Z
- **Updated**: 2024-07-09T15:50:29Z
- **Merged**: 2024-07-08T07:22:55Z
- **Closed**: 2024-07-08T07:22:55Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 15   **+336 / -185**

## Description

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
Preparation for #79. Create Snapshot types for CQ and Cohort, to better differentiate between Cache types. We were currently relying on fields properly being set/copied, and some fields not being set.

This change will make the code easier to understand and less error prone.

#### Special notes for your reviewer:
Unexporting Cohort/ClusterQueue works trivially. I will send this out in a subsequent PR.

Additionally, I didn't move methods that operate on ClusterQueueSnapshot to the new file, to keep the diff smaller. These can be moved in a subsequent PR if we wish.

Also folding https://github.com/kubernetes-sigs/kueue/pull/2489 into this change.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-02T15:07:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b7ecfe7b40381eb8e3500988bf0d6ce0192577ec |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66880d311c7f2c00080792b1 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-05T18:21:01Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-05T18:21:06Z

LGTM label has been added.  <details>Git tree hash: bd1d46818c682fcf6721ec4be56300a0d8cbd825</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-05T18:21:10Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2519#issuecomment-2211255645" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2519#" title="Author self-approved">gabesaba</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-08T07:13:03Z

/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-30
Looks like overloaded infra: "There are no nodes that your pod can schedule to - check your requests, tolerations, and node selectors" [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/2519/pull-kueue-test-e2e-main-1-30/1809291714087620608)

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-02T16:15:24Z

- **pkg/cache/clusterqueue_snapshot.go:15** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-02T16:15:24Z
```diff
@@ -0,0 +1,62 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type ClusterQueueSnapshot struct {
```

  Did you consider creating a new package `snapshot`? So we could have `snapshot.ClusterQueue`

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-04T09:16:32Z

- **pkg/cache/clusterqueue_snapshot.go:15** by [@gabesaba](https://github.com/gabesaba) — 2024-07-04T09:16:32Z
```diff
@@ -0,0 +1,62 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type ClusterQueueSnapshot struct {
```

  I did it this way mainly to keep the diff small. If I create a new Snapshot package, I will have to move all the Snapshot functions/tests to the new files/package. Additionally, I'll have to move some additional structures (such as ResourceGroup) to a third package, to avoid any circular dependencies.
  
  If you don't mind the much larger diff, I am happy to do it this way too. If yes, do you prefer `pkg/snapshot` or `pkg/cache/snapshot` as the directory structure for this new snapshot package?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-04T16:17:26Z

- **pkg/cache/cache_test.go:1265** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:00:04Z
```diff
@@ -1261,7 +1261,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 				for i := range cq.ResourceGroups {
 					rg := &cq.ResourceGroups[i]
 					for rName := range rg.CoveredResources {
-						if cq.RGByResource[rName] != rg {
+						if resourceGroupForResource(cq, rName) != rg {
 							t.Errorf("RGByResource[%s] does not point to its resource group", rName)
```

  update the error message here

- **pkg/cache/clusterqueue.go:142** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:02:19Z
```diff
@@ -150,7 +139,7 @@ func (c *Cohort) CalculateLendable() map[corev1.ResourceName]int64 {
 	return lendable
 }
 
-func (c *ClusterQueue) FitInCohort(q resources.FlavorResourceQuantities) bool {
+func (c ClusterQueueSnapshot) FitInCohort(q resources.FlavorResourceQuantities) bool {
```

  ```suggestion
  func (c *ClusterQueueSnapshot) FitInCohort(q resources.FlavorResourceQuantities) bool {
  ```

- **pkg/cache/clusterqueue.go:607** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:51:29Z
```diff
@@ -626,7 +604,7 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 // LendingLimit will also be counted here if feature LendingLimit enabled.
 // Please note that for different clusterQueues, the requestable quota is different,
 // they should be calculated dynamically.
-func (c *ClusterQueue) RequestableCohortQuota(fName kueue.ResourceFlavorReference, rName corev1.ResourceName) (val int64) {
+func (c ClusterQueueSnapshot) RequestableCohortQuota(fName kueue.ResourceFlavorReference, rName corev1.ResourceName) (val int64) {
```

  ```suggestion
  func (c *ClusterQueueSnapshot) RequestableCohortQuota(fName kueue.ResourceFlavorReference, rName corev1.ResourceName) (val int64) {
  ```
  same for others

- **pkg/resources/resource.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:56:39Z
```diff
@@ -40,3 +40,11 @@ func (f FlavorResourceQuantitiesFlat) Unflatten() FlavorResourceQuantities {
 	}
 	return out
 }
+
+// SafeGet attempts to access nested value, returning 0 if absent.
+func (f FlavorResourceQuantities) SafeGet(fr FlavorResource) int64 {
```

  ```suggestion
  func (f FlavorResourceQuantities) For(fr FlavorResource) int64 {
  ```

- **pkg/cache/clusterqueue.go:655** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:58:40Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
```

  Add a comment that these methods just implement the interface.
  
  Otherwise they look unnecessary.

- **pkg/cache/clusterqueue.go:662** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:58:52Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
+	return c.Cohort != nil
+}
+
+func (c ClusterQueue) getFairWeight() *resource.Quantity {
+	return &c.FairWeight
+}
```

  ```suggestion
  func (c ClusterQueue) fairWeight() resource.Quantity {
  	return c.FairWeight
  }
  ```

- **pkg/cache/clusterqueue.go:663** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:59:13Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
+	return c.Cohort != nil
+}
+
+func (c ClusterQueue) getFairWeight() *resource.Quantity {
+	return &c.FairWeight
+}
+
+func (c ClusterQueue) getLendable() map[corev1.ResourceName]int64 {
```

  ```suggestion
  func (c ClusterQueue) lendableResourcesInCohort() map[corev1.ResourceName]int64 {
  ```

- **pkg/cache/clusterqueue.go:667** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T15:59:26Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
+	return c.Cohort != nil
+}
+
+func (c ClusterQueue) getFairWeight() *resource.Quantity {
+	return &c.FairWeight
+}
+
+func (c ClusterQueue) getLendable() map[corev1.ResourceName]int64 {
+	return c.Cohort.CalculateLendable()
+}
+
+func (c ClusterQueue) usage(fr resources.FlavorResource) int64 {
```

  ```suggestion
  func (c ClusterQueue) usageFor(fr resources.FlavorResource) int64 {
  ```

- **pkg/cache/clusterqueue.go:671** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:00:45Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
+	return c.Cohort != nil
+}
+
+func (c ClusterQueue) getFairWeight() *resource.Quantity {
+	return &c.FairWeight
+}
+
+func (c ClusterQueue) getLendable() map[corev1.ResourceName]int64 {
+	return c.Cohort.CalculateLendable()
+}
+
+func (c ClusterQueue) usage(fr resources.FlavorResource) int64 {
+	return c.Usage.SafeGet(fr)
+}
+
+func (c ClusterQueue) getResourceGroups() *[]ResourceGroup {
```

  ```suggestion
  func (c ClusterQueue) resourceGroups() []ResourceGroup {
  ```

- **pkg/cache/clusterqueue.go:709** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:05:07Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
+	return c.Cohort != nil
+}
+
+func (c ClusterQueue) getFairWeight() *resource.Quantity {
+	return &c.FairWeight
+}
+
+func (c ClusterQueue) getLendable() map[corev1.ResourceName]int64 {
+	return c.Cohort.CalculateLendable()
+}
+
+func (c ClusterQueue) usage(fr resources.FlavorResource) int64 {
+	return c.Usage.SafeGet(fr)
+}
+
+func (c ClusterQueue) getResourceGroups() *[]ResourceGroup {
+	return &c.ResourceGroups
+}
+
 // DominantResourceShare returns a value from 0 to 1,000,000 representing the maximum of the ratios
 // of usage above nominal quota to the lendable resources in the cohort, among all the resources
 // provided by the ClusterQueue, and divided by the weight.
 // If zero, it means that the usage of the ClusterQueue is below the nominal quota.
 // The function also returns the resource name that yielded this value.
 // Also for a weight of zero, this will return 9223372036854775807.
-func (c *ClusterQueue) DominantResourceShare() (int, corev1.ResourceName) {
-	return c.dominantResourceShare(nil, 0)
+func (c ClusterQueueSnapshot) DominantResourceShare() (int, corev1.ResourceName) {
+	return dominantResourceShare(c, nil, 0)
 }
 
-func (c *ClusterQueue) DominantResourceShareWith(wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
-	return c.dominantResourceShare(wlReq, 1)
+func (c ClusterQueueSnapshot) DominantResourceShareWith(wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
+	return dominantResourceShare(c, wlReq, 1)
 }
 
-func (c *ClusterQueue) DominantResourceShareWithout(w *workload.Info) (int, corev1.ResourceName) {
-	return c.dominantResourceShare(w.FlavorResourceUsage(), -1)
+func (c ClusterQueueSnapshot) DominantResourceShareWithout(w *workload.Info) (int, corev1.ResourceName) {
+	return dominantResourceShare(c, w.FlavorResourceUsage(), -1)
 }
 
-func (c *ClusterQueue) dominantResourceShare(wlReq resources.FlavorResourceQuantities, m int64) (int, corev1.ResourceName) {
-	if c.Cohort == nil {
+type dominantResourceShareNode interface {
+	hasCohort() bool
+	getFairWeight() *resource.Quantity
+	getLendable() map[corev1.ResourceName]int64
+
+	netQuotaNode
+}
+
+func dominantResourceShare(node dominantResourceShareNode, wlReq resources.FlavorResourceQuantities, m int64) (int, corev1.ResourceName) {
+	if !node.hasCohort() {
 		return 0, ""
 	}
-	if c.FairWeight.IsZero() {
+	if node.getFairWeight().IsZero() {
 		return math.MaxInt, ""
 	}
 
+	netQuota := getNetQuota(node)
```

  remainingQuota or unusedQuota perhaps?

- **pkg/cache/clusterqueue.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:07:15Z
```diff
@@ -91,16 +90,6 @@ type ClusterQueue struct {
 type Cohort struct {
```

  The comment in line 75 can be removed

- **pkg/cache/clusterqueue_test.go:1065** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:09:47Z
```diff
@@ -1052,7 +1062,7 @@ func TestDominantResourceShare(t *testing.T) {
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			drValue, drName := tc.cq.DominantResourceShareWith(tc.flvResQ)
+			drValue, drName := dominantResourceShare(&tc.cq, tc.flvResQ, 1)
```

  In general, try to test exported functions or methods

- **pkg/cache/resource.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:12:50Z
```diff
@@ -0,0 +1,78 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type resourceGroupNode interface {
+	getResourceGroups() *[]ResourceGroup
+}
+
+// resourceGroupForResource returns the ResourceGroup which contains capacity
+// for the resource, or nil if the CQ doesn't provide this resource.
+func resourceGroupForResource(node resourceGroupNode, resource corev1.ResourceName) *ResourceGroup {
+	resourceGroups := node.getResourceGroups()
+	for i := range *resourceGroups {
+		if (*resourceGroups)[i].CoveredResources.Has(resource) {
+			return &(*resourceGroups)[i]
+		}
+	}
+	return nil
+}
+
+// flavorResources returns all of the FlavorResource(s) defined in the given node.
+// note: this is copy heavy, but we don't care - the structure of ResourceGroup will change
+// soon, which will have these values precomputed (or cheap to access).
+func flavorResources(node resourceGroupNode) []resources.FlavorResource {
+	flavorResources := make([]resources.FlavorResource, 0)
```

  ```suggestion
  	var flavorResources []resources.FlavorResource
  ```
  
  `append` can allocate as necessary.

- **pkg/cache/resource.go:74** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:15:46Z
```diff
@@ -0,0 +1,78 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type resourceGroupNode interface {
+	getResourceGroups() *[]ResourceGroup
+}
+
+// resourceGroupForResource returns the ResourceGroup which contains capacity
+// for the resource, or nil if the CQ doesn't provide this resource.
+func resourceGroupForResource(node resourceGroupNode, resource corev1.ResourceName) *ResourceGroup {
+	resourceGroups := node.getResourceGroups()
+	for i := range *resourceGroups {
+		if (*resourceGroups)[i].CoveredResources.Has(resource) {
+			return &(*resourceGroups)[i]
+		}
+	}
+	return nil
+}
+
+// flavorResources returns all of the FlavorResource(s) defined in the given node.
+// note: this is copy heavy, but we don't care - the structure of ResourceGroup will change
+// soon, which will have these values precomputed (or cheap to access).
+func flavorResources(node resourceGroupNode) []resources.FlavorResource {
+	flavorResources := make([]resources.FlavorResource, 0)
+	for _, rg := range *node.getResourceGroups() {
+		for _, flavor := range rg.Flavors {
+			for resource := range flavor.Resources {
+				flavorResources = append(flavorResources, resources.FlavorResource{Flavor: flavor.Name, Resource: resource})
+			}
+		}
+	}
+	return flavorResources
+}
+
+// resourceQuota returns the ResourceQuota for the given FlavorResource, or nil
+// if the CQ doesn't provide the FlavorResource.
+func resourceQuota(node resourceGroupNode, fr resources.FlavorResource) *ResourceQuota {
+	resourceGroup := resourceGroupForResource(node, fr.Resource)
+	if resourceGroup == nil {
+		return nil
+	}
+	for _, flavor := range resourceGroup.Flavors {
+		if flavor.Name == fr.Flavor {
+			return flavor.Resources[fr.Resource]
+		}
+	}
+	return nil
+}
+
+// nominalCapacity returns the nominal capacity for the given (CQ, FlavorResource).
+// if the FlavorResource isn't provided by CQ, 0 is returned.
+func nominalCapacity(node resourceGroupNode, fr resources.FlavorResource) int64 {
+	quota := resourceQuota(node, fr)
+	if quota == nil {
+		return 0
+	}
+	return quota.Nominal
+}
+
+type netQuotaNode interface {
+	usage(resources.FlavorResource) int64
+	getResourceGroups() *[]ResourceGroup
+}
+
+// getNetQuota computes the remaining quota for each FlavorResource. A
+// negative value implies that the node is borrowing.
+func getNetQuota(node netQuotaNode) resources.FlavorResourceQuantitiesFlat {
+	netQuota := make(resources.FlavorResourceQuantitiesFlat)
+	for _, fr := range flavorResources(node) {
```

  It looks like creating the intermediate `flavorResources` slice should not be necessary.
  
  the `resourceQuota` function also does a linear search again throughout the resource groups and the flavors within them.
  
  I think we can iterate once over the resourceGroups to calculate the net quotas.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-04T17:50:59Z

- **pkg/cache/snapshot.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:50:36Z
```diff
@@ -111,7 +111,7 @@ func (c *Cache) Snapshot() Snapshot {
 		snap.ResourceFlavors[name] = rf
 	}
 	for _, cohort := range c.cohorts {
-		cohortCopy := newCohort(cohort.Name, cohort.Members.Len())
+		cohortCopy := cohort.snapshot()
 		cohortCopy.AllocatableResourceGeneration = 0
```

  why not put all of this inside `cohort.snapshot`? Otherwise the logic is split between two places.

- **pkg/cache/snapshot_test.go:635** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:54:18Z
```diff
@@ -633,7 +631,7 @@ func TestSnapshot(t *testing.T) {
 				for i := range cq.ResourceGroups {
 					rg := &cq.ResourceGroups[i]
 					for rName := range rg.CoveredResources {
-						if cq.RGByResource[rName] != rg {
+						if resourceGroupForResource(cq, rName) != rg {
 							t.Errorf("RGByResource[%s] does not point to its resource group", rName)
```

  update error message

- **pkg/cache/clusterqueue_snapshot.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T16:58:35Z
```diff
@@ -0,0 +1,62 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type ClusterQueueSnapshot struct {
+	Name              string
+	Cohort            *CohortSnapshot
+	ResourceGroups    []ResourceGroup
+	Usage             resources.FlavorResourceQuantities
+	Workloads         map[string]*workload.Info
+	WorkloadsNotReady sets.Set[string]
+	NamespaceSelector labels.Selector
+	Preemption        kueue.ClusterQueuePreemption
+	FairWeight        resource.Quantity
+	FlavorFungibility kueue.FlavorFungibility
+	// Aggregates AdmissionChecks from both .spec.AdmissionChecks and .spec.AdmissionCheckStrategy
+	// Sets hold ResourceFlavors to which an AdmissionCheck should apply.
+	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
+	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
+	Status          metrics.ClusterQueueStatus
+	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
+	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
+	GuaranteedQuota resources.FlavorResourceQuantities
+	// AllocatableResourceGeneration will be increased when some admitted workloads are
+	// deleted, or the resource groups are changed.
+	AllocatableResourceGeneration int64
+
+	// Lendable holds the total lendable quota for the resources of the ClusterQueue, independent of the flavor.
+	Lendable map[corev1.ResourceName]int64
+}
+
+func (c ClusterQueueSnapshot) RGByResource(resource corev1.ResourceName) *ResourceGroup {
+	return resourceGroupForResource(c, resource)
```

  Do we need this function to also work for non-snapshot CQ?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-05T11:38:55Z

thanks for the review, Aldo. I first rebased (4d590a7), then sent out new changes in second push (323fbc8) so the diff is more clear.

- **pkg/cache/clusterqueue.go:142** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T06:48:22Z
```diff
@@ -150,7 +139,7 @@ func (c *Cohort) CalculateLendable() map[corev1.ResourceName]int64 {
 	return lendable
 }
 
-func (c *ClusterQueue) FitInCohort(q resources.FlavorResourceQuantities) bool {
+func (c ClusterQueueSnapshot) FitInCohort(q resources.FlavorResourceQuantities) bool {
```

  Is this exclusively for efficiency reasons, as ClusterQueueSnapshot is rather large?

- **pkg/cache/clusterqueue.go:709** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T06:58:33Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
+	return c.Cohort != nil
+}
+
+func (c ClusterQueue) getFairWeight() *resource.Quantity {
+	return &c.FairWeight
+}
+
+func (c ClusterQueue) getLendable() map[corev1.ResourceName]int64 {
+	return c.Cohort.CalculateLendable()
+}
+
+func (c ClusterQueue) usage(fr resources.FlavorResource) int64 {
+	return c.Usage.SafeGet(fr)
+}
+
+func (c ClusterQueue) getResourceGroups() *[]ResourceGroup {
+	return &c.ResourceGroups
+}
+
 // DominantResourceShare returns a value from 0 to 1,000,000 representing the maximum of the ratios
 // of usage above nominal quota to the lendable resources in the cohort, among all the resources
 // provided by the ClusterQueue, and divided by the weight.
 // If zero, it means that the usage of the ClusterQueue is below the nominal quota.
 // The function also returns the resource name that yielded this value.
 // Also for a weight of zero, this will return 9223372036854775807.
-func (c *ClusterQueue) DominantResourceShare() (int, corev1.ResourceName) {
-	return c.dominantResourceShare(nil, 0)
+func (c ClusterQueueSnapshot) DominantResourceShare() (int, corev1.ResourceName) {
+	return dominantResourceShare(c, nil, 0)
 }
 
-func (c *ClusterQueue) DominantResourceShareWith(wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
-	return c.dominantResourceShare(wlReq, 1)
+func (c ClusterQueueSnapshot) DominantResourceShareWith(wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
+	return dominantResourceShare(c, wlReq, 1)
 }
 
-func (c *ClusterQueue) DominantResourceShareWithout(w *workload.Info) (int, corev1.ResourceName) {
-	return c.dominantResourceShare(w.FlavorResourceUsage(), -1)
+func (c ClusterQueueSnapshot) DominantResourceShareWithout(w *workload.Info) (int, corev1.ResourceName) {
+	return dominantResourceShare(c, w.FlavorResourceUsage(), -1)
 }
 
-func (c *ClusterQueue) dominantResourceShare(wlReq resources.FlavorResourceQuantities, m int64) (int, corev1.ResourceName) {
-	if c.Cohort == nil {
+type dominantResourceShareNode interface {
+	hasCohort() bool
+	getFairWeight() *resource.Quantity
+	getLendable() map[corev1.ResourceName]int64
+
+	netQuotaNode
+}
+
+func dominantResourceShare(node dominantResourceShareNode, wlReq resources.FlavorResourceQuantities, m int64) (int, corev1.ResourceName) {
+	if !node.hasCohort() {
 		return 0, ""
 	}
-	if c.FairWeight.IsZero() {
+	if node.getFairWeight().IsZero() {
 		return math.MaxInt, ""
 	}
 
+	netQuota := getNetQuota(node)
```

  changed to remaining (as this sounds better when it may be negative)

- **pkg/cache/clusterqueue_snapshot.go:43** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T07:02:16Z
```diff
@@ -0,0 +1,62 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type ClusterQueueSnapshot struct {
+	Name              string
+	Cohort            *CohortSnapshot
+	ResourceGroups    []ResourceGroup
+	Usage             resources.FlavorResourceQuantities
+	Workloads         map[string]*workload.Info
+	WorkloadsNotReady sets.Set[string]
+	NamespaceSelector labels.Selector
+	Preemption        kueue.ClusterQueuePreemption
+	FairWeight        resource.Quantity
+	FlavorFungibility kueue.FlavorFungibility
+	// Aggregates AdmissionChecks from both .spec.AdmissionChecks and .spec.AdmissionCheckStrategy
+	// Sets hold ResourceFlavors to which an AdmissionCheck should apply.
+	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
+	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
+	Status          metrics.ClusterQueueStatus
+	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
+	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
+	GuaranteedQuota resources.FlavorResourceQuantities
+	// AllocatableResourceGeneration will be increased when some admitted workloads are
+	// deleted, or the resource groups are changed.
+	AllocatableResourceGeneration int64
+
+	// Lendable holds the total lendable quota for the resources of the ClusterQueue, independent of the flavor.
+	Lendable map[corev1.ResourceName]int64
+}
+
+func (c ClusterQueueSnapshot) RGByResource(resource corev1.ResourceName) *ResourceGroup {
+	return resourceGroupForResource(c, resource)
```

  No, it is only used by flavorassigner and scheduler

- **pkg/cache/resource.go:29** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T07:03:42Z
```diff
@@ -0,0 +1,78 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type resourceGroupNode interface {
+	getResourceGroups() *[]ResourceGroup
+}
+
+// resourceGroupForResource returns the ResourceGroup which contains capacity
+// for the resource, or nil if the CQ doesn't provide this resource.
+func resourceGroupForResource(node resourceGroupNode, resource corev1.ResourceName) *ResourceGroup {
+	resourceGroups := node.getResourceGroups()
+	for i := range *resourceGroups {
+		if (*resourceGroups)[i].CoveredResources.Has(resource) {
+			return &(*resourceGroups)[i]
+		}
+	}
+	return nil
+}
+
+// flavorResources returns all of the FlavorResource(s) defined in the given node.
+// note: this is copy heavy, but we don't care - the structure of ResourceGroup will change
+// soon, which will have these values precomputed (or cheap to access).
+func flavorResources(node resourceGroupNode) []resources.FlavorResource {
+	flavorResources := make([]resources.FlavorResource, 0)
```

  Turned flavorResources into a named return value

- **pkg/cache/resource.go:74** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T07:53:32Z
```diff
@@ -0,0 +1,78 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type resourceGroupNode interface {
+	getResourceGroups() *[]ResourceGroup
+}
+
+// resourceGroupForResource returns the ResourceGroup which contains capacity
+// for the resource, or nil if the CQ doesn't provide this resource.
+func resourceGroupForResource(node resourceGroupNode, resource corev1.ResourceName) *ResourceGroup {
+	resourceGroups := node.getResourceGroups()
+	for i := range *resourceGroups {
+		if (*resourceGroups)[i].CoveredResources.Has(resource) {
+			return &(*resourceGroups)[i]
+		}
+	}
+	return nil
+}
+
+// flavorResources returns all of the FlavorResource(s) defined in the given node.
+// note: this is copy heavy, but we don't care - the structure of ResourceGroup will change
+// soon, which will have these values precomputed (or cheap to access).
+func flavorResources(node resourceGroupNode) []resources.FlavorResource {
+	flavorResources := make([]resources.FlavorResource, 0)
+	for _, rg := range *node.getResourceGroups() {
+		for _, flavor := range rg.Flavors {
+			for resource := range flavor.Resources {
+				flavorResources = append(flavorResources, resources.FlavorResource{Flavor: flavor.Name, Resource: resource})
+			}
+		}
+	}
+	return flavorResources
+}
+
+// resourceQuota returns the ResourceQuota for the given FlavorResource, or nil
+// if the CQ doesn't provide the FlavorResource.
+func resourceQuota(node resourceGroupNode, fr resources.FlavorResource) *ResourceQuota {
+	resourceGroup := resourceGroupForResource(node, fr.Resource)
+	if resourceGroup == nil {
+		return nil
+	}
+	for _, flavor := range resourceGroup.Flavors {
+		if flavor.Name == fr.Flavor {
+			return flavor.Resources[fr.Resource]
+		}
+	}
+	return nil
+}
+
+// nominalCapacity returns the nominal capacity for the given (CQ, FlavorResource).
+// if the FlavorResource isn't provided by CQ, 0 is returned.
+func nominalCapacity(node resourceGroupNode, fr resources.FlavorResource) int64 {
+	quota := resourceQuota(node, fr)
+	if quota == nil {
+		return 0
+	}
+	return quota.Nominal
+}
+
+type netQuotaNode interface {
+	usage(resources.FlavorResource) int64
+	getResourceGroups() *[]ResourceGroup
+}
+
+// getNetQuota computes the remaining quota for each FlavorResource. A
+// negative value implies that the node is borrowing.
+func getNetQuota(node netQuotaNode) resources.FlavorResourceQuantitiesFlat {
+	netQuota := make(resources.FlavorResourceQuantitiesFlat)
+	for _, fr := range flavorResources(node) {
```

  My intention was to allow this to be easily substituted once I move capacity/limits out from ResourceGroup (into some flat datastructures, maybe FlavorResourceQuantitiesFlat), without concern for the complexity as (the inefficient part of) this code is expected to be short lived
  
  I updated so that it is now O(n), and should still be easily updated with my upcoming changes.

- **pkg/cache/snapshot.go:115** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T09:02:55Z
```diff
@@ -111,7 +111,7 @@ func (c *Cache) Snapshot() Snapshot {
 		snap.ResourceFlavors[name] = rf
 	}
 	for _, cohort := range c.cohorts {
-		cohortCopy := newCohort(cohort.Name, cohort.Members.Len())
+		cohortCopy := cohort.snapshot()
 		cohortCopy.AllocatableResourceGeneration = 0
```

  Done. Thank you.
  
  I don't love the signature I created (as it has no return value, and cohort is only accessible via CQ), but once Cohorts become first class, we'll likely update it to return CohortSnapshot.

- **pkg/cache/snapshot_test.go:635** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T09:04:44Z
```diff
@@ -633,7 +631,7 @@ func TestSnapshot(t *testing.T) {
 				for i := range cq.ResourceGroups {
 					rg := &cq.ResourceGroups[i]
 					for rName := range rg.CoveredResources {
-						if cq.RGByResource[rName] != rg {
+						if resourceGroupForResource(cq, rName) != rg {
 							t.Errorf("RGByResource[%s] does not point to its resource group", rName)
```

  Done. And updated test to use the exported method.

- **pkg/cache/clusterqueue.go:655** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T09:11:59Z
```diff
@@ -674,41 +652,67 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 	return cohortUsage
 }
 
+func (c ClusterQueue) hasCohort() bool {
```

  Done, also in clusterqueue_snapshot

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-05T13:52:52Z

- **pkg/cache/clusterqueue_snapshot.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-05T13:52:52Z
```diff
@@ -0,0 +1,62 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type ClusterQueueSnapshot struct {
+	Name              string
+	Cohort            *CohortSnapshot
+	ResourceGroups    []ResourceGroup
+	Usage             resources.FlavorResourceQuantities
+	Workloads         map[string]*workload.Info
+	WorkloadsNotReady sets.Set[string]
+	NamespaceSelector labels.Selector
+	Preemption        kueue.ClusterQueuePreemption
+	FairWeight        resource.Quantity
+	FlavorFungibility kueue.FlavorFungibility
+	// Aggregates AdmissionChecks from both .spec.AdmissionChecks and .spec.AdmissionCheckStrategy
+	// Sets hold ResourceFlavors to which an AdmissionCheck should apply.
+	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
+	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
+	Status          metrics.ClusterQueueStatus
+	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
+	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
+	GuaranteedQuota resources.FlavorResourceQuantities
+	// AllocatableResourceGeneration will be increased when some admitted workloads are
+	// deleted, or the resource groups are changed.
+	AllocatableResourceGeneration int64
+
+	// Lendable holds the total lendable quota for the resources of the ClusterQueue, independent of the flavor.
+	Lendable map[corev1.ResourceName]int64
+}
+
+func (c ClusterQueueSnapshot) RGByResource(resource corev1.ResourceName) *ResourceGroup {
+	return resourceGroupForResource(c, resource)
```

  Then implement it here, instead of using this secondary function.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-05T14:06:14Z

- **pkg/cache/cache_test.go:1264** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-05T13:55:50Z
```diff
@@ -1261,8 +1261,8 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 				for i := range cq.ResourceGroups {
 					rg := &cq.ResourceGroups[i]
 					for rName := range rg.CoveredResources {
-						if cq.RGByResource[rName] != rg {
-							t.Errorf("RGByResource[%s] does not point to its resource group", rName)
+						if resourceGroupForResource(cq, rName) != rg {
```

  I think this check is only relevant for the snapshot, so maybe worth removing it here.

- **pkg/cache/snapshot.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-05T14:05:54Z
```diff
@@ -156,7 +144,25 @@ func (c *ClusterQueue) snapshot() *ClusterQueue {
 	return cc
 }
 
-func (c *ClusterQueue) accumulateResources(cohort *Cohort) {
+func (c *Cohort) snapshot(cqs map[string]*ClusterQueueSnapshot) {
```

  ```suggestion
  func (c *Cohort) snapshotInto(cqs map[string]*ClusterQueueSnapshot) {
  ```

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-05T15:06:58Z

- **pkg/cache/clusterqueue_snapshot.go:43** by [@gabesaba](https://github.com/gabesaba) — 2024-07-05T15:06:58Z
```diff
@@ -0,0 +1,62 @@
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/labels"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type ClusterQueueSnapshot struct {
+	Name              string
+	Cohort            *CohortSnapshot
+	ResourceGroups    []ResourceGroup
+	Usage             resources.FlavorResourceQuantities
+	Workloads         map[string]*workload.Info
+	WorkloadsNotReady sets.Set[string]
+	NamespaceSelector labels.Selector
+	Preemption        kueue.ClusterQueuePreemption
+	FairWeight        resource.Quantity
+	FlavorFungibility kueue.FlavorFungibility
+	// Aggregates AdmissionChecks from both .spec.AdmissionChecks and .spec.AdmissionCheckStrategy
+	// Sets hold ResourceFlavors to which an AdmissionCheck should apply.
+	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
+	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
+	Status          metrics.ClusterQueueStatus
+	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
+	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
+	GuaranteedQuota resources.FlavorResourceQuantities
+	// AllocatableResourceGeneration will be increased when some admitted workloads are
+	// deleted, or the resource groups are changed.
+	AllocatableResourceGeneration int64
+
+	// Lendable holds the total lendable quota for the resources of the ClusterQueue, independent of the flavor.
+	Lendable map[corev1.ResourceName]int64
+}
+
+func (c ClusterQueueSnapshot) RGByResource(resource corev1.ResourceName) *ResourceGroup {
+	return resourceGroupForResource(c, resource)
```

  Ahh, I misunderstood your question. It was being also used in cache test. But after your comment below, i deleted that test.
  
  So I'll move it in here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-05T15:44:15Z

- **pkg/cache/clusterqueue.go:74** by [@mimowo](https://github.com/mimowo) — 2024-07-05T15:44:15Z
```diff
@@ -72,8 +71,6 @@ type ClusterQueue struct {
 	// Lendable holds the total lendable quota for the resources of the ClusterQueue, independent of the flavor.
 	Lendable map[corev1.ResourceName]int64
 
-	// The following fields are not populated in a snapshot.
-
 	AdmittedUsage resources.FlavorResourceQuantities
```

  Are all of the fields still needed in this struct?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-05T15:49:07Z

- **pkg/cache/clusterqueue_test.go:732** by [@mimowo](https://github.com/mimowo) — 2024-07-05T15:49:07Z
```diff
@@ -715,20 +715,21 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 
 func TestDominantResourceShare(t *testing.T) {
 	cases := map[string]struct {
-		cq          ClusterQueue
+		cq          ClusterQueueSnapshot
 		flvResQ     resources.FlavorResourceQuantities
 		wantDRValue int
 		wantDRName  corev1.ResourceName
 	}{
 		"no cohort": {
-			cq: ClusterQueue{
+			cq: ClusterQueueSnapshot{
 				FairWeight: oneQuantity,
 				Usage: resources.FlavorResourceQuantitiesFlat{
 					{Flavor: "default", Resource: corev1.ResourceCPU}: 1_000,
 					{Flavor: "default", Resource: "example.com/gpu"}:  2,
 				}.Unflatten(),
 				ResourceGroups: []ResourceGroup{
 					{
+						CoveredResources: sets.New(corev1.ResourceCPU, "example.com/gpu"),
```

  The setup for snapshot seems to require extra field(s) which seem redundant. 
  
  Wouldn't it be better to still use `ClusterQueue` in test cases setup and generate the snapshots based on the ClusterQueues inside `t.Run`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-05T15:50:31Z

I like the direction of the changes, thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-05T18:19:57Z

- **pkg/cache/clusterqueue_test.go:732** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-05T18:19:57Z
```diff
@@ -715,20 +715,21 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 
 func TestDominantResourceShare(t *testing.T) {
 	cases := map[string]struct {
-		cq          ClusterQueue
+		cq          ClusterQueueSnapshot
 		flvResQ     resources.FlavorResourceQuantities
 		wantDRValue int
 		wantDRName  corev1.ResourceName
 	}{
 		"no cohort": {
-			cq: ClusterQueue{
+			cq: ClusterQueueSnapshot{
 				FairWeight: oneQuantity,
 				Usage: resources.FlavorResourceQuantitiesFlat{
 					{Flavor: "default", Resource: corev1.ResourceCPU}: 1_000,
 					{Flavor: "default", Resource: "example.com/gpu"}:  2,
 				}.Unflatten(),
 				ResourceGroups: []ResourceGroup{
 					{
+						CoveredResources: sets.New(corev1.ResourceCPU, "example.com/gpu"),
```

  yes, but that can be done in a follow up.
