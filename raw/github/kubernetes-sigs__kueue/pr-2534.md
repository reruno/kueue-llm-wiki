# PR #2534: [kjobctl][builder] Detect surplus flags

**Summary**: [kjobctl][builder] Detect surplus flags

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2534

**Last updated**: 2024-07-24T07:59:15Z

---

## Metadata

- **State**: closed
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `kjobctl-extra-flags` → `main`
- **Created**: 2024-07-04T10:18:36Z
- **Updated**: 2024-07-24T07:59:15Z
- **Merged**: —
- **Closed**: 2024-07-24T07:45:36Z
- **Labels**: `kind/feature`, `size/L`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mwielgus](https://github.com/mwielgus)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mwielgus](https://github.com/mwielgus), [@IrvingMg](https://github.com/IrvingMg)
- **Changed files**: 8   **+101 / -88**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Detect unnecessary profile flags provided.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-04T10:18:45Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2534#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [trasc]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-04T10:18:54Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ae02e62dbb4539e9075df7eefb89a8300a3e7690 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66a08de83803ec0008391004 |

### Comment by [@trasc](https://github.com/trasc) — 2024-07-04T10:19:40Z

/cc @mwielgus @IrvingMg @mbobrovskyi

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-04T14:03:49Z

/assign @mwielgus

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-12T13:20:53Z

cc @pajakd

### Comment by [@trasc](https://github.com/trasc) — 2024-07-24T07:45:32Z

Since we are using different subcommands  with different flags set (after #2573) this PR is no longer needed .

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-24T07:45:37Z

@trasc: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2534#issuecomment-2247130663):

>Since we are using different subcommands  with different flags set (after #2573) this PR is no longer needed .
>
>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-04T10:49:41Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-04T10:49:41Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  Not sure if this is correct. We should allow all flags to be applied, but only if it's possible to add to this profile. So we need to know all possible flags on each builder or template.
  
  For example, `PodTemplate` don't have  parallelism. So we can't use this flag.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-04T10:55:24Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:118** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-04T10:55:24Z
```diff
@@ -109,9 +115,15 @@ func NewCreateCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStr
 		Example:               createExample,
 		Args:                  cobra.MatchAll(cobra.ExactArgs(1), cobra.OnlyValidArgs),
 		ValidArgs:             []string{"job", "interactive"},
-		Run: func(cmd *cobra.Command, args []string) {
-			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
-			cobra.CheckErr(o.Run(cmd.Context(), clientGetter))
+		RunE: func(cmd *cobra.Command, args []string) error {
```

  On this case it will show help on error.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-04T11:14:23Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@trasc](https://github.com/trasc) — 2024-07-04T11:14:23Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  Since the profile is the place where the admin will configure the needed flags we should use that list.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-04T11:15:54Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:118** by [@trasc](https://github.com/trasc) — 2024-07-04T11:15:54Z
```diff
@@ -109,9 +115,15 @@ func NewCreateCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStr
 		Example:               createExample,
 		Args:                  cobra.MatchAll(cobra.ExactArgs(1), cobra.OnlyValidArgs),
 		ValidArgs:             []string{"job", "interactive"},
-		Run: func(cmd *cobra.Command, args []string) {
-			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
-			cobra.CheckErr(o.Run(cmd.Context(), clientGetter))
+		RunE: func(cmd *cobra.Command, args []string) error {
```

  not if you flip the value of `cmd.SilenceUsage`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-04T11:16:07Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-04T11:16:07Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  Yes, it's correct for required fields. But what about optional?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-04T11:22:13Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:124** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-04T11:22:13Z
```diff
@@ -109,9 +115,15 @@ func NewCreateCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStr
 		Example:               createExample,
 		Args:                  cobra.MatchAll(cobra.ExactArgs(1), cobra.OnlyValidArgs),
 		ValidArgs:             []string{"job", "interactive"},
-		Run: func(cmd *cobra.Command, args []string) {
-			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
-			cobra.CheckErr(o.Run(cmd.Context(), clientGetter))
+		RunE: func(cmd *cobra.Command, args []string) error {
+			err := o.Complete(clientGetter, cmd, args)
+			if err != nil {
+				return err
+			}
+			// stop the usage if we've got this far
+			cmd.SilenceUsage = true
```

  So on this case:
  
  ```suggestion
  			// stop the usage if we've got this far
  			cmd.SilenceUsage = true
  			
  			err := o.Complete(clientGetter, cmd, args)
  			if err != nil {
  				return err
  			}
  		
  ```
  
  WDYT?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-04T11:47:20Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@trasc](https://github.com/trasc) — 2024-07-04T11:47:20Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  Do we have the concept of "optional flag"?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-04T11:49:46Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-04T11:49:46Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  @mwielgus

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-04T11:50:24Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:124** by [@trasc](https://github.com/trasc) — 2024-07-04T11:50:24Z
```diff
@@ -109,9 +115,15 @@ func NewCreateCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStr
 		Example:               createExample,
 		Args:                  cobra.MatchAll(cobra.ExactArgs(1), cobra.OnlyValidArgs),
 		ValidArgs:             []string{"job", "interactive"},
-		Run: func(cmd *cobra.Command, args []string) {
-			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
-			cobra.CheckErr(o.Run(cmd.Context(), clientGetter))
+		RunE: func(cmd *cobra.Command, args []string) error {
+			err := o.Complete(clientGetter, cmd, args)
+			if err != nil {
+				return err
+			}
+			// stop the usage if we've got this far
+			cmd.SilenceUsage = true
```

  done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-11T10:46:44Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T10:46:44Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  @alculquicondor @mwielgus WDYT?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-07-12T10:26:13Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@mwielgus](https://github.com/mwielgus) — 2024-07-12T10:26:12Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  Yes, we might. Some values might be prepopulated  in the template. While currently most of the flags are required, we may soon add additional ones like image name.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-12T11:47:33Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@trasc](https://github.com/trasc) — 2024-07-12T11:47:33Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  When there is the case we can think of adding the API support for that kind of flag, for now I think that hard-coding a list of flags that are usable or not for `interactive` and `job` is not the way to go.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-07-18T10:18:21Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@mwielgus](https://github.com/mwielgus) — 2024-07-18T10:18:21Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  Honestly speaking, hard coding the flags would be the simplest thing to detect which flags are needed. There can be some smart methods to detect whether the flag plays a role or not, but they will be much more complex. Couple hardcoded lists should not hurt us much.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-22T08:02:07Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@trasc](https://github.com/trasc) — 2024-07-22T08:02:07Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  Sure hardcoding a list is not hard, but it's a bad approach in my opinion.
  
  Such list could be added in a follow-up when needed and the list is properly defined.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-24T05:31:17Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:183** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-24T05:31:17Z
```diff
@@ -167,26 +172,19 @@ func (b *Builder) complete(ctx context.Context) error {
 }
 
 func (b *Builder) validateFlags() error {
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CmdFlag) && len(b.command) == 0 {
-		return noCommandSpecifiedErr
-	}
+	requiredSet := set.New(b.mode.RequiredFlags...)
+	providedSet := set.New(b.providedFlags...)
 
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.ParallelismFlag) && b.parallelism == nil {
-		return noParallelismSpecifiedErr
-	}
-
-	if slices.Contains(b.mode.RequiredFlags, v1alpha1.CompletionsFlag) && b.completions == nil {
-		return noCompletionsSpecifiedErr
-	}
+	missing := requiredSet.Difference(providedSet)
+	extra := providedSet.Difference(requiredSet)
```

  I still didn't get why we need to check here `extra` flags? Cobra should validate it for each mode.
