# PR #2538: Add a metric that tracks the number of preemptions issued by a ClusterQueue

**Summary**: Add a metric that tracks the number of preemptions issued by a ClusterQueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2538

**Last updated**: 2024-07-18T21:01:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `preemptions_metric` → `main`
- **Created**: 2024-07-05T09:05:52Z
- **Updated**: 2024-07-18T21:01:13Z
- **Merged**: 2024-07-18T14:16:54Z
- **Closed**: 2024-07-18T14:16:54Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@mszadkow](https://github.com/mszadkow), [@denkensk](https://github.com/denkensk), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba), [@kerthcet](https://github.com/kerthcet), [@IrvingMg](https://github.com/IrvingMg), [@trasc](https://github.com/trasc)
- **Changed files**: 5   **+84 / -37**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Adds a metric that tracks the number of preemptions issued by a ClusterQueue
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2491

#### Special notes for your reviewer:
NONE
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add preempted_workloads_total metric that tracks the number of preemptions issued by a ClusterQueue)
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-05T09:05:54Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-05T09:06:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1948fed36a04a4cac680e0ae1ab44618622bde15 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66990f387fa37b0008168758 |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-05T09:20:34Z

/assign

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-08T15:13:01Z

/test all

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-09T09:26:46Z

/test all

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-09T10:05:08Z

/cc @trasc @mbobrovskyi @IrvingMg @mszadkow

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T15:12:19Z

/cc

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T20:10:35Z

/release-note-edit
```release-note
Add preempted_workloads_total metric that tracks the number of preemptions issued by a ClusterQueue)
```

Use the name of the user facing metric, as opposed to the variable name.

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-11T12:21:35Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T14:56:55Z

Please rebase

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-18T13:56:15Z

LGTM label has been added.  <details>Git tree hash: 340b99ae81b7cd714303bfc87dbc6b2c4d03cfb1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-18T13:56:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2538#pullrequestreview-2185909697" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2538#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T21:01:10Z

/kind feature

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-10T09:49:53Z

a few nits, looks good overall

- **test/util/util.go:513** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T08:45:18Z
```diff
@@ -504,6 +504,15 @@ func ExpectEvictedWorkloadsTotalMetric(cqName string, reason string, v int) {
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func ExpectPreemptedWorkloadsTotalMetric(preemptorCqName, reason string, v int) {
+	metric := metrics.PreemptedWorkloadsTotal.WithLabelValues(preemptorCqName, reason)
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		count, err := testutil.GetCounterMetricValue(metric)
+		g.Expect(err).ToNot(gomega.HaveOccurred())
+		g.Expect(int(count)).Should(gomega.Equal(v))
+	}, Timeout, Interval).Should(gomega.Succeed())
```

  nit: this code is repeated 3 times already.
  
  refactor this to a helper function (perhaps `expectCounterMetric(metric prometheus.Counter, v int)`), and change the other (3?) usages.

- **pkg/metrics/metrics_test.go:164** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T08:47:16Z
```diff
@@ -158,3 +158,15 @@ func TestReportAndCleanupClusterQueueEvictedNumber(t *testing.T) {
 	ClearQueueSystemMetrics("cluster_queue1")
 	expectFilteredMetricsCount(t, EvictedWorkloadsTotal, 0, "cluster_queue", "cluster_queue1")
 }
+
+func TestReportAndCleanupClusterQueuePreemptedNumber(t *testing.T) {
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortReclamation")
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortFairSharing")
```

  add coverage for other two cases please

- **pkg/metrics/metrics.go:155** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T08:50:01Z
```diff
@@ -148,6 +148,19 @@ The label 'reason' can have the following values:
 		}, []string{"cluster_queue", "reason"},
 	)
 
+	PreemptedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "preempted_workloads_total",
+			Help: `The number of preempted workloads per 'preemptor_cluster_queue',
```

  Don't change this yet, but I think that `preempting_cluster_queue` might be a slightly better label.
  
  I'll comment here again once I think about this more.

- **test/integration/scheduler/preemption_test.go:141** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T08:53:55Z
```diff
@@ -137,6 +137,7 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 			util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl1, lowWl2)
 			util.ExpectEvictedWorkloadsTotalMetric(cq.Name, kueue.WorkloadEvictedByPreemption, 2)
+			util.ExpectPreemptedWorkloadsTotalMetric(cq.Name, "InClusterQueue", 2)
```

  are there integ tests covering the other 3 cases? can we add assertions there too?

- **pkg/metrics/metrics_test.go:169** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T08:54:29Z
```diff
@@ -158,3 +158,15 @@ func TestReportAndCleanupClusterQueueEvictedNumber(t *testing.T) {
 	ClearQueueSystemMetrics("cluster_queue1")
 	expectFilteredMetricsCount(t, EvictedWorkloadsTotal, 0, "cluster_queue", "cluster_queue1")
 }
+
+func TestReportAndCleanupClusterQueuePreemptedNumber(t *testing.T) {
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortReclamation")
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortFairSharing")
+
+	expectFilteredMetricsCount(t, PreemptedWorkloadsTotal, 2, "preemptor_cluster_queue", "cluster_queue1")
+	expectFilteredMetricsCount(t, PreemptedWorkloadsTotal, 1, "preemptor_cluster_queue", "cluster_queue1", "reason", "InCohortReclamation")
+	expectFilteredMetricsCount(t, PreemptedWorkloadsTotal, 1, "preemptor_cluster_queue", "cluster_queue1", "reason", "InCohortFairSharing")
+	// clear
```

  nit: replace comment with blank line - it is clear what we are doing below by name of `ClearQueueSystemMetrics`

- **pkg/scheduler/preemption/preemption.go:191** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T08:58:41Z
```diff
@@ -187,6 +187,8 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, preemptor *workload.In
 			}
 
 			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj), "reason", reason, "message", message)
+			metrics.ReportPreemptedWorkloads(preemptor.ClusterQueue, reason)
+
```

  can we combine the metrics (ReportPreemptedWorkloads, ReportEvictedWorkloads) into one function call? `ReportPreemption(preemptingCq, preemptingReason, targetCq)`?

- **pkg/metrics/metrics.go:282** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T09:43:24Z
```diff
@@ -262,6 +275,10 @@ func ReportEvictedWorkloads(cqName, reason string) {
 	EvictedWorkloadsTotal.WithLabelValues(cqName, reason).Inc()
 }
 
+func ReportPreemptedWorkloads(preemptorCqName, reason string) {
+	PreemptedWorkloadsTotal.WithLabelValues(preemptorCqName, reason).Inc()
+}
```

  complement to my comment in `preemption.go`. this is what API could look like
  ```suggestion
  func ReportPreemption(preemptingCqName, preemptingReason, targetCqName string) {
  	PreemptedWorkloadsTotal.WithLabelValues(preemptingCqName, preemptingReason).Inc()
  	ReportEvictedWorkloads(targetCqName, kueue.WorkloadEvictedByPreemption)
  }
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-10T16:16:58Z

- **pkg/metrics/metrics.go:282** by [@trasc](https://github.com/trasc) — 2024-07-10T16:15:34Z
```diff
@@ -262,6 +275,10 @@ func ReportEvictedWorkloads(cqName, reason string) {
 	EvictedWorkloadsTotal.WithLabelValues(cqName, reason).Inc()
 }
 
+func ReportPreemptedWorkloads(preemptorCqName, reason string) {
+	PreemptedWorkloadsTotal.WithLabelValues(preemptorCqName, reason).Inc()
+}
```

  in my opinion `func ReportPreemptedWorkloads(preemptorCqName, reason string) {` is easier to follow.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-10T20:12:37Z

- **pkg/metrics/metrics.go:155** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T20:12:37Z
```diff
@@ -148,6 +148,19 @@ The label 'reason' can have the following values:
 		}, []string{"cluster_queue", "reason"},
 	)
 
+	PreemptedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "preempted_workloads_total",
+			Help: `The number of preempted workloads per 'preemptor_cluster_queue',
```

  or we can change the name of the metric to something like `triggered_workload_preemptions_total`.
  
  Then the label can simply be `cluster_queue`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-10T20:15:59Z

- **pkg/metrics/metrics.go:282** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T20:14:15Z
```diff
@@ -262,6 +275,10 @@ func ReportEvictedWorkloads(cqName, reason string) {
 	EvictedWorkloadsTotal.WithLabelValues(cqName, reason).Inc()
 }
 
+func ReportPreemptedWorkloads(preemptorCqName, reason string) {
+	PreemptedWorkloadsTotal.WithLabelValues(preemptorCqName, reason).Inc()
+}
```

  I don't usually like one line functions that could have been inlined. @gabesaba's suggestion adds more value.

- **pkg/metrics/metrics_test.go:164** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T20:15:19Z
```diff
@@ -158,3 +158,15 @@ func TestReportAndCleanupClusterQueueEvictedNumber(t *testing.T) {
 	ClearQueueSystemMetrics("cluster_queue1")
 	expectFilteredMetricsCount(t, EvictedWorkloadsTotal, 0, "cluster_queue", "cluster_queue1")
 }
+
+func TestReportAndCleanupClusterQueuePreemptedNumber(t *testing.T) {
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortReclamation")
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortFairSharing")
```

  Nothing is validating the label values, so I think this test is enough.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-07-10T23:17:46Z

- **test/integration/scheduler/preemption_test.go:141** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-10T23:17:46Z
```diff
@@ -137,6 +137,7 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 			util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl1, lowWl2)
 			util.ExpectEvictedWorkloadsTotalMetric(cq.Name, kueue.WorkloadEvictedByPreemption, 2)
+			util.ExpectPreemptedWorkloadsTotalMetric(cq.Name, "InClusterQueue", 2)
```

  Can you please clarify which cases do you mean?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-11T07:14:13Z

- **test/integration/scheduler/preemption_test.go:141** by [@gabesaba](https://github.com/gabesaba) — 2024-07-11T07:14:13Z
```diff
@@ -137,6 +137,7 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 			util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl1, lowWl2)
 			util.ExpectEvictedWorkloadsTotalMetric(cq.Name, kueue.WorkloadEvictedByPreemption, 2)
+			util.ExpectPreemptedWorkloadsTotalMetric(cq.Name, "InClusterQueue", 2)
```

  the other 3 reasons - `InCohortReclamation` etc

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-07-11T09:22:01Z

- **pkg/metrics/metrics_test.go:164** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-11T09:22:01Z
```diff
@@ -158,3 +158,15 @@ func TestReportAndCleanupClusterQueueEvictedNumber(t *testing.T) {
 	ClearQueueSystemMetrics("cluster_queue1")
 	expectFilteredMetricsCount(t, EvictedWorkloadsTotal, 0, "cluster_queue", "cluster_queue1")
 }
+
+func TestReportAndCleanupClusterQueuePreemptedNumber(t *testing.T) {
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortReclamation")
+	ReportPreemptedWorkloads("cluster_queue1", "InCohortFairSharing")
```

  I added for other reasons as well, NP

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-11T12:21:40Z

- **pkg/scheduler/preemption/preemption.go:189** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T12:21:39Z
```diff
@@ -186,9 +186,9 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, preemptor *workload.In
 				return
 			}
 
-			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj), "reason", reason, "message", message)
+			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj), "reason", reason, "message", message, "targetClusterQueue", target.ClusterQueue)
```

  ```suggestion
  			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj), "reason", reason, "message", message, "targetClusterQueue", klog.KRef("", target.ClusterQueue.Name))
  ```

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-12T15:03:15Z

- **pkg/metrics/metrics.go:155** by [@gabesaba](https://github.com/gabesaba) — 2024-07-12T15:03:15Z
```diff
@@ -148,6 +148,19 @@ The label 'reason' can have the following values:
 		}, []string{"cluster_queue", "reason"},
 	)
 
+	PreemptedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "preempted_workloads_total",
+			Help: `The number of preempted workloads per 'preemptor_cluster_queue',
```

  Both options work, but I prefer the first. `triggered_workload_preemptions_total` implies the cq the subject of the metric (and is more verbose), while `preempted_workloads_total` better captures that the preemption is the thing we care about, with the reason/cluster_queue being dimensions.
  
  Please update to `preempting_cluster_queue` @vladikkuzn

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-12T15:04:26Z

- **pkg/metrics/metrics.go:155** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-12T15:04:01Z
```diff
@@ -148,6 +148,19 @@ The label 'reason' can have the following values:
 		}, []string{"cluster_queue", "reason"},
 	)
 
+	PreemptedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "preempted_workloads_total",
+			Help: `The number of preempted workloads per 'preemptor_cluster_queue',
```

  Let's proceed with `preempting_cluster_queue`, as it matches our documentation https://kueue.sigs.k8s.io/docs/concepts/preemption/

- **test/integration/scheduler/preemption_test.go:141** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-12T15:04:24Z
```diff
@@ -137,6 +137,7 @@ var _ = ginkgo.Describe("Preemption", func() {
 
 			util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl1, lowWl2)
 			util.ExpectEvictedWorkloadsTotalMetric(cq.Name, kueue.WorkloadEvictedByPreemption, 2)
+			util.ExpectPreemptedWorkloadsTotalMetric(cq.Name, "InClusterQueue", 2)
```

  Please look into this

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-16T15:42:42Z

These changes look good, but looking at the test updates, I realized that there is a bug in https://github.com/kubernetes-sigs/kueue/pull/2411#discussion_r1679655530

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-17T16:23:59Z

I think this needs a rebase now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-18T13:56:09Z

/lgtm
/approve
