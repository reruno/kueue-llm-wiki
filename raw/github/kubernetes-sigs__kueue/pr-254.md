# PR #254: feat: filter inactive clusterQueues in scheduling

**Summary**: feat: filter inactive clusterQueues in scheduling

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/254

**Last updated**: 2022-05-13T16:04:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feature/filter-inactive-clusterqueue` → `main`
- **Created**: 2022-05-09T10:19:41Z
- **Updated**: 2022-05-13T16:04:39Z
- **Merged**: 2022-05-13T16:03:49Z
- **Closed**: 2022-05-13T16:03:50Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 8   **+127 / -86**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/251

#### Special notes for your reviewer:

## Discussion

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-09T10:20:07Z

cc @ahg-g

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-09T16:37:05Z

PTAL @ahg-g when you have time.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-11T01:03:07Z

Updated as advised.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-13T15:36:45Z

Thanks Kante, looks great, can you please squash?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-13T15:42:25Z

squashed.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-13T16:03:03Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-13T16:03:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/254#issuecomment-1126214437" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/254#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-10T00:09:30Z

- **pkg/controller/core/resourceflavor_controller.go:66** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T00:06:12Z
```diff
@@ -63,7 +63,7 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	// As long as one clusterQueue becomes active,
 	// we should inform clusterQueue controller to broadcast the event.
 	if cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); len(cqNames) > 0 {
-		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		r.qManager.QueueInadmissibleWorkloads(cqNames, true)
```

  why do we need to force broadcast here?

- **pkg/scheduler/scheduler_test.go:1734** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T00:08:41Z
```diff
@@ -1723,3 +1730,167 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestHeads(t *testing.T) {
```

  can we keep this where it was and just use a fake cache implementation? The unit test should be in the pkg where the function lives.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-10T02:09:45Z

- **pkg/controller/core/resourceflavor_controller.go:66** by [@kerthcet](https://github.com/kerthcet) — 2022-05-10T02:09:45Z
```diff
@@ -63,7 +63,7 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	// As long as one clusterQueue becomes active,
 	// we should inform clusterQueue controller to broadcast the event.
 	if cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); len(cqNames) > 0 {
-		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		r.qManager.QueueInadmissibleWorkloads(cqNames, true)
```

  E.g. we have an inactive clusterQueue, workloads submitted to this clusterQueue will stay in heap and won't be popped out. So when we create a flavor, `QueueInadmissibleWorkloads` will always return false and `Broadcast()` won't be called. As a result, if we find an clusterQueue turns active from pending, we should broadcast in any cases.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-10T02:19:09Z

- **pkg/scheduler/scheduler_test.go:1864** by [@kerthcet](https://github.com/kerthcet) — 2022-05-10T02:19:04Z
```diff
@@ -1723,3 +1730,167 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestHeads(t *testing.T) {
+	ctx, cancel := context.WithTimeout(context.Background(), headsTimeout)
+	defer cancel()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+
+	client := fake.NewClientBuilder().WithScheme(scheme).Build()
+	now := time.Now().Truncate(time.Second)
+
+	clusterQueues := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("fooCq").Obj(),
+		*utiltesting.MakeClusterQueue("barCq").Obj(),
+		*utiltesting.MakeClusterQueue("bazCq").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("on-demand", "5").Obj()).
+				Obj()).
+			Obj(),
+	}
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "fooCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "barCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "baz"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "bazCq",
+			},
+		},
+	}
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
+		{
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
+		},
+		{
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+			},
+			wantWorkloads: sets.NewString("a", "b"),
+		},
+		{
+			name: "scheduler with clusterQueues containing multi workloads",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a1",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a2",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+			},
+			wantWorkloads: sets.NewString("a1", "b"),
+		},
+		{
+			name: "scheduler with inactive clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "c",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "baz"},
+				},
+			},
+			wantWorkloads: sets.NewString("a", "b"),
+		},
+	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			c := cache.New(client)
+			manager := queue.NewManager(fake.NewClientBuilder().WithScheme(scheme).Build(), c)
+			for _, cq := range clusterQueues {
+				if err := c.AddClusterQueue(ctx, &cq); err != nil {
```

  If we move the tests back to where it comes, we'll have to mock the `AddClusterQueue`, it's quite complexity and we will have to export `AddClusterQueue` method, but I think it's unnecessary for we will never add clusterQueue to cache in manager.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-10T12:14:09Z

- **pkg/scheduler/scheduler_test.go:1864** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T12:14:09Z
```diff
@@ -1723,3 +1730,167 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestHeads(t *testing.T) {
+	ctx, cancel := context.WithTimeout(context.Background(), headsTimeout)
+	defer cancel()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+
+	client := fake.NewClientBuilder().WithScheme(scheme).Build()
+	now := time.Now().Truncate(time.Second)
+
+	clusterQueues := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("fooCq").Obj(),
+		*utiltesting.MakeClusterQueue("barCq").Obj(),
+		*utiltesting.MakeClusterQueue("bazCq").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("on-demand", "5").Obj()).
+				Obj()).
+			Obj(),
+	}
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "fooCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "barCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "baz"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "bazCq",
+			},
+		},
+	}
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
+		{
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
+		},
+		{
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+			},
+			wantWorkloads: sets.NewString("a", "b"),
+		},
+		{
+			name: "scheduler with clusterQueues containing multi workloads",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a1",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a2",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+			},
+			wantWorkloads: sets.NewString("a1", "b"),
+		},
+		{
+			name: "scheduler with inactive clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "c",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "baz"},
+				},
+			},
+			wantWorkloads: sets.NewString("a", "b"),
+		},
+	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			c := cache.New(client)
+			manager := queue.NewManager(fake.NewClientBuilder().WithScheme(scheme).Build(), c)
+			for _, cq := range clusterQueues {
+				if err := c.AddClusterQueue(ctx, &cq); err != nil {
```

  why do you need to mock this? you only need to mock `ClusterQueueActive` function, right?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-10T12:33:16Z

- **pkg/controller/core/resourceflavor_controller.go:66** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T12:33:15Z
```diff
@@ -63,7 +63,7 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	// As long as one clusterQueue becomes active,
 	// we should inform clusterQueue controller to broadcast the event.
 	if cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); len(cqNames) > 0 {
-		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		r.qManager.QueueInadmissibleWorkloads(cqNames, true)
```

  sounds good, lets then just call `r.qManager.Broadcast()` here instead changing the `QueueInadmissibleWorkloads` func and of passing this flag. 
  
  I think we need to invoke `Broadcast` on update as well below.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-10T15:15:16Z

- **pkg/controller/core/resourceflavor_controller.go:66** by [@kerthcet](https://github.com/kerthcet) — 2022-05-10T13:02:51Z
```diff
@@ -63,7 +63,7 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	// As long as one clusterQueue becomes active,
 	// we should inform clusterQueue controller to broadcast the event.
 	if cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); len(cqNames) > 0 {
-		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		r.qManager.QueueInadmissibleWorkloads(cqNames, true)
```

  > sounds good, lets then just call r.qManager.Broadcast() here instead changing the QueueInadmissibleWorkloads func and of passing this flag.
  
  We already have `Broadcast()` in `QueueInadmissibleWorkloads`, if we call another `Broadcast` here, it's a little confusing.
  
  > I think we need to invoke Broadcast on update as well below.
  
  Actually update resourceFlavor will not lead clusterQueue to active as the flavor name will not change.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-10T15:15:29Z

- **pkg/scheduler/scheduler_test.go:1864** by [@kerthcet](https://github.com/kerthcet) — 2022-05-10T15:15:29Z
```diff
@@ -1723,3 +1730,167 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestHeads(t *testing.T) {
+	ctx, cancel := context.WithTimeout(context.Background(), headsTimeout)
+	defer cancel()
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+
+	client := fake.NewClientBuilder().WithScheme(scheme).Build()
+	now := time.Now().Truncate(time.Second)
+
+	clusterQueues := []kueue.ClusterQueue{
+		*utiltesting.MakeClusterQueue("fooCq").Obj(),
+		*utiltesting.MakeClusterQueue("barCq").Obj(),
+		*utiltesting.MakeClusterQueue("bazCq").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("on-demand", "5").Obj()).
+				Obj()).
+			Obj(),
+	}
+	queues := []kueue.Queue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "fooCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "bar"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "barCq",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "baz"},
+			Spec: kueue.QueueSpec{
+				ClusterQueue: "bazCq",
+			},
+		},
+	}
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
+		{
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
+		},
+		{
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+			},
+			wantWorkloads: sets.NewString("a", "b"),
+		},
+		{
+			name: "scheduler with clusterQueues containing multi workloads",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a1",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a2",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+			},
+			wantWorkloads: sets.NewString("a1", "b"),
+		},
+		{
+			name: "scheduler with inactive clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "c",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "baz"},
+				},
+			},
+			wantWorkloads: sets.NewString("a", "b"),
+		},
+	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			c := cache.New(client)
+			manager := queue.NewManager(fake.NewClientBuilder().WithScheme(scheme).Build(), c)
+			for _, cq := range clusterQueues {
+				if err := c.AddClusterQueue(ctx, &cq); err != nil {
```

  sg.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-10T17:04:16Z

- **pkg/controller/core/resourceflavor_controller.go:66** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T16:29:10Z
```diff
@@ -63,7 +63,7 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	// As long as one clusterQueue becomes active,
 	// we should inform clusterQueue controller to broadcast the event.
 	if cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); len(cqNames) > 0 {
-		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		r.qManager.QueueInadmissibleWorkloads(cqNames, true)
```

  Calling `Broadcast` again doesn't hurt, we anyways need to add a comment explaining why broadcast is always invoked here.
  
  Adding the broadcast flag to `QueueInadmissibleWorkloads` results in a less concise API that mixes two things.

- **pkg/queue/manager_test.go:624** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T16:54:13Z
```diff
@@ -619,20 +619,21 @@ func TestHeads(t *testing.T) {
 	now := time.Now().Truncate(time.Second)
 
 	clusterQueues := []kueue.ClusterQueue{
-		*utiltesting.MakeClusterQueue("fooCq").Obj(),
-		*utiltesting.MakeClusterQueue("barCq").Obj(),
+		*utiltesting.MakeClusterQueue("active-fooCq").Obj(),
+		*utiltesting.MakeClusterQueue("active-barCq").Obj(),
+		*utiltesting.MakeClusterQueue("bazCq").Obj(),
```

  ```suggestion
  		*utiltesting.MakeClusterQueue("pending-bazCq").Obj(),
  ```

- **pkg/queue/manager_test.go:652** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T16:55:47Z
```diff
@@ -642,62 +643,121 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "scheduler with empty clusterQueues",
```

  ```suggestion
  			name:          "empty clusterQueues",
  ```

- **pkg/queue/manager_test.go:657** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T16:56:27Z
```diff
@@ -642,62 +643,121 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "b",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with active clusterQueues",
```

  ```suggestion
  			name: "active clusterQueues",
  ```

- **pkg/queue/manager_test.go:677** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T16:57:10Z
```diff
@@ -642,62 +643,121 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "b",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "bar"},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "c",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with clusterQueues containing multi workloads",
```

  ```suggestion
  			name: "active clusterQueues with multiple workloads",
  ```

- **pkg/queue/manager_test.go:679** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T16:58:42Z
```diff
@@ -642,62 +643,121 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "b",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "bar"},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "c",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with clusterQueues containing multi workloads",
+			workloads: []kueue.Workload{
+				{
```

  can we replace those with [`MakeWorkload`](https://github.com/kubernetes-sigs/kueue/blob/b2b2efcc6d0cfa2cee3fdbb56f49e554b1555758/pkg/util/testing/wrappers.go#L142)

- **pkg/queue/manager_test.go:704** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T16:59:36Z
```diff
@@ -642,62 +643,121 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "b",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "bar"},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "c",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with clusterQueues containing multi workloads",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a1",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a2",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
-		},
-	}
-	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
-	for _, cq := range clusterQueues {
-		if err := manager.AddClusterQueue(ctx, &cq); err != nil {
-			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
-		}
-	}
-	for _, q := range queues {
-		if err := manager.AddQueue(ctx, &q); err != nil {
-			t.Fatalf("Failed adding queue %s: %s", q.Name, err)
-		}
-	}
-	go manager.CleanUpOnContext(ctx)
-	for _, wl := range workloads {
-		wl := wl
-		manager.AddOrUpdateWorkload(&wl)
-	}
-	wantHeads := []workload.Info{
-		{
-			Obj:          &workloads[1],
-			ClusterQueue: "barCq",
+			wantWorkloads: sets.NewString("a1", "b"),
 		},
 		{
-			Obj:          &workloads[2],
-			ClusterQueue: "fooCq",
+			name: "scheduler with inactive clusterQueues",
```

  ```suggestion
  			name: "inactive clusterQueues",
  ```

- **pkg/queue/manager_test.go:733** by [@ahg-g](https://github.com/ahg-g) — 2022-05-10T17:03:36Z
```diff
@@ -642,62 +643,121 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "b",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "bar"},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "c",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with clusterQueues containing multi workloads",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a1",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a2",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
-		},
-	}
-	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
-	for _, cq := range clusterQueues {
-		if err := manager.AddClusterQueue(ctx, &cq); err != nil {
-			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
-		}
-	}
-	for _, q := range queues {
-		if err := manager.AddQueue(ctx, &q); err != nil {
-			t.Fatalf("Failed adding queue %s: %s", q.Name, err)
-		}
-	}
-	go manager.CleanUpOnContext(ctx)
-	for _, wl := range workloads {
-		wl := wl
-		manager.AddOrUpdateWorkload(&wl)
-	}
-	wantHeads := []workload.Info{
-		{
-			Obj:          &workloads[1],
-			ClusterQueue: "barCq",
+			wantWorkloads: sets.NewString("a1", "b"),
 		},
 		{
-			Obj:          &workloads[2],
-			ClusterQueue: "fooCq",
+			name: "scheduler with inactive clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "c",
+						CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "baz"},
+				},
+			},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			fakeC := &fakeCache{}
```

  Creating the context at line 613 should be moved here inside the run

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-11T01:02:49Z

- **pkg/queue/manager_test.go:679** by [@kerthcet](https://github.com/kerthcet) — 2022-05-11T01:02:49Z
```diff
@@ -642,62 +643,121 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "scheduler with empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "b",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with active clusterQueues",
+			workloads: []kueue.Workload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "a",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "foo"},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:              "b",
+						CreationTimestamp: metav1.NewTime(now),
+					},
+					Spec: kueue.WorkloadSpec{QueueName: "bar"},
+				},
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "bar"},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "c",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "scheduler with clusterQueues containing multi workloads",
+			workloads: []kueue.Workload{
+				{
```

  done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-11T14:01:56Z

- **pkg/queue/cache.go:17** by [@ahg-g](https://github.com/ahg-g) — 2022-05-11T12:27:36Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  oh, just noticed this; the interface should be in the cache package. Any reason you placed it here?

- **pkg/queue/cache.go:19** by [@ahg-g](https://github.com/ahg-g) — 2022-05-11T12:30:06Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+// Cache stores objects which help to manage the capacity of kueue.
```

  ```suggestion
  // Cache stores objects and aggregated information related to queues and workloads.
  ```

- **pkg/controller/core/resourceflavor_controller.go:70** by [@ahg-g](https://github.com/ahg-g) — 2022-05-11T13:51:58Z
```diff
@@ -64,6 +64,11 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	// we should inform clusterQueue controller to broadcast the event.
 	if cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); len(cqNames) > 0 {
 		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		// Always broadcast here in case the potential blocking in scheduling.
+		// E.g. we have an inactive bestEffort clusterQueue and it's inadmissibleWorkloads
+		// is empty, calling `QueueInadmissibleWorkloads` will not queue any InadmissibleWorkloads,
+		// as a result, broadcast is not called in that function.
```

  ```suggestion
  		// If at least one CQ becomes active, then those CQs should now get evaluated by the scheduler; 
  		// note that the workloads in those CQs are not necessarily "inadmissible", and hence we trigger a 
  		// broadcast here in all cases.
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-11T14:45:38Z

- **pkg/queue/cache.go:17** by [@kerthcet](https://github.com/kerthcet) — 2022-05-11T14:45:38Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  Somehow behaves as DI. Queue should depend on the cache interface, not the real cache. If we move this to cache package, we will depend on the package then. Is this make sense?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-11T15:52:26Z

- **pkg/queue/cache.go:17** by [@ahg-g](https://github.com/ahg-g) — 2022-05-11T15:52:26Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  It is still strange to have an interface named Cache defined here. I would rename the interface to something different then, like `StatusChecker` perhaps?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-12T07:15:14Z

- **pkg/queue/cache.go:17** by [@kerthcet](https://github.com/kerthcet) — 2022-05-12T07:15:14Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  Or maybe `accessor`, more generic?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-12T13:25:31Z

- **pkg/queue/cache.go:17** by [@ahg-g](https://github.com/ahg-g) — 2022-05-12T13:25:31Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  I don't think the interface will be that generic. I would stick with `StatusChecker` for now.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-12T14:06:06Z

- **pkg/queue/cache.go:17** by [@kerthcet](https://github.com/kerthcet) — 2022-05-12T14:06:06Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  updated.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-12T14:56:22Z

- **pkg/scheduler/scheduler_test.go:1707** by [@denkensk](https://github.com/denkensk) — 2022-05-12T14:43:48Z
```diff
@@ -1691,15 +1691,21 @@ func TestRequeueAndUpdate(t *testing.T) {
 			cl := clientBuilder.Build()
 			broadcaster := record.NewBroadcaster()
 			recorder := broadcaster.NewRecorder(scheme, corev1.EventSource{Component: constants.ManagerName})
-			qManager := queue.NewManager(cl)
 			cqCache := cache.New(cl)
+			qManager := queue.NewManager(cl, cqCache)
 			scheduler := New(qManager, cqCache, cl, recorder)
 			if err := qManager.AddQueue(ctx, q1); err != nil {
 				t.Fatalf("Inserting queue %s/%s in manager: %v", q1.Namespace, q1.Name, err)
 			}
 			if err := qManager.AddClusterQueue(ctx, cq); err != nil {
 				t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
 			}
+			if err := cqCache.AddClusterQueue(ctx, cq); err != nil {
+				t.Fatalf("Inserting clusterQueue %s to cache: %v", cq.Name, err)
+			}
+			if !cqCache.ClusterQueueActive(cq.Name) {
+				t.Fatal("ClusterQueue status not right")
```

  ```suggestion
  				t.Fatal("Status of clusterQueue %s isn't active", cq.Name)
  ```

- **pkg/queue/manager_test.go:700** by [@denkensk](https://github.com/denkensk) — 2022-05-12T14:52:57Z
```diff
@@ -642,62 +641,75 @@ func TestHeads(t *testing.T) {
 			},
 		},
 	}
-	workloads := []kueue.Workload{
+	tests := []struct {
+		name          string
+		workloads     []kueue.Workload
+		wantWorkloads sets.String
+	}{
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "a",
-				CreationTimestamp: metav1.NewTime(now.Add(time.Hour)),
-			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
+			name:          "empty clusterQueues",
+			workloads:     []kueue.Workload{},
+			wantWorkloads: sets.String{},
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "b",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "active clusterQueues",
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "").Creation(now).Queue("foo").Obj(),
+				*utiltesting.MakeWorkload("b", "").Creation(now).Queue("bar").Obj(),
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "bar"},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 		{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:              "c",
-				CreationTimestamp: metav1.NewTime(now),
+			name: "active clusterQueues with multiple workloads",
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "").Creation(now).Queue("foo").Obj(),
+				*utiltesting.MakeWorkload("a2", "").Creation(now.Add(time.Hour)).Queue("foo").Obj(),
+				*utiltesting.MakeWorkload("b", "").Creation(now).Queue("bar").Obj(),
 			},
-			Spec: kueue.WorkloadSpec{QueueName: "foo"},
-		},
-	}
-	manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build())
-	for _, cq := range clusterQueues {
-		if err := manager.AddClusterQueue(ctx, &cq); err != nil {
-			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
-		}
-	}
-	for _, q := range queues {
-		if err := manager.AddQueue(ctx, &q); err != nil {
-			t.Fatalf("Failed adding queue %s: %s", q.Name, err)
-		}
-	}
-	go manager.CleanUpOnContext(ctx)
-	for _, wl := range workloads {
-		wl := wl
-		manager.AddOrUpdateWorkload(&wl)
-	}
-	wantHeads := []workload.Info{
-		{
-			Obj:          &workloads[1],
-			ClusterQueue: "barCq",
+			wantWorkloads: sets.NewString("a1", "b"),
 		},
 		{
-			Obj:          &workloads[2],
-			ClusterQueue: "fooCq",
+			name: "inactive clusterQueues",
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "").Creation(now).Queue("foo").Obj(),
+				*utiltesting.MakeWorkload("b", "").Creation(now).Queue("bar").Obj(),
+				*utiltesting.MakeWorkload("c", "").Creation(now.Add(time.Hour)).Queue("baz").Obj(),
+			},
+			wantWorkloads: sets.NewString("a", "b"),
 		},
 	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, cancel := context.WithTimeout(context.Background(), headsTimeout)
+			defer cancel()
+			fakeC := &fakeCache{}
+			manager := NewManager(fake.NewClientBuilder().WithScheme(scheme).Build(), fakeC)
+			for _, cq := range clusterQueues {
+				if err := manager.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Failed adding clusterQueue %s to manager: %v", cq.Name, err)
+				}
+			}
+			for _, q := range queues {
+				if err := manager.AddQueue(ctx, &q); err != nil {
+					t.Fatalf("Failed adding queue %s: %s", q.Name, err)
+				}
+			}
 
-	heads := manager.Heads(ctx)
-	sort.Slice(heads, func(i, j int) bool {
-		return heads[i].Obj.Name < heads[j].Obj.Name
-	})
-	if diff := cmp.Diff(wantHeads, heads); diff != "" {
-		t.Errorf("GetHeads returned wrong heads (-want,+got):\n%s", diff)
+			go manager.CleanUpOnContext(ctx)
+			for _, wl := range tc.workloads {
+				wl := wl
```

  If we define `tc.workloads` as `[]*kueue.Workload`, do we need this？

- **pkg/queue/manager_test.go:628** by [@denkensk](https://github.com/denkensk) — 2022-05-12T14:53:46Z
```diff
@@ -610,29 +610,28 @@ func TestUpdateWorkload(t *testing.T) {
 }
 
 func TestHeads(t *testing.T) {
-	ctx, cancel := context.WithTimeout(context.Background(), headsTimeout)
-	defer cancel()
 	scheme := runtime.NewScheme()
 	if err := kueue.AddToScheme(scheme); err != nil {
 		t.Fatalf("Failed adding kueue scheme: %s", err)
 	}
 	now := time.Now().Truncate(time.Second)
 
 	clusterQueues := []kueue.ClusterQueue{
-		*utiltesting.MakeClusterQueue("fooCq").Obj(),
-		*utiltesting.MakeClusterQueue("barCq").Obj(),
+		*utiltesting.MakeClusterQueue("active-fooCq").Obj(),
+		*utiltesting.MakeClusterQueue("active-barCq").Obj(),
+		*utiltesting.MakeClusterQueue("pending-bazCq").Obj(),
 	}
 	queues := []kueue.Queue{
 		{
 			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
 			Spec: kueue.QueueSpec{
-				ClusterQueue: "fooCq",
+				ClusterQueue: "active-fooCq",
```

  change to use `utiltesting.MakeQueue`?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-13T03:25:33Z

- **pkg/queue/manager_test.go:628** by [@kerthcet](https://github.com/kerthcet) — 2022-05-13T03:25:33Z
```diff
@@ -610,29 +610,28 @@ func TestUpdateWorkload(t *testing.T) {
 }
 
 func TestHeads(t *testing.T) {
-	ctx, cancel := context.WithTimeout(context.Background(), headsTimeout)
-	defer cancel()
 	scheme := runtime.NewScheme()
 	if err := kueue.AddToScheme(scheme); err != nil {
 		t.Fatalf("Failed adding kueue scheme: %s", err)
 	}
 	now := time.Now().Truncate(time.Second)
 
 	clusterQueues := []kueue.ClusterQueue{
-		*utiltesting.MakeClusterQueue("fooCq").Obj(),
-		*utiltesting.MakeClusterQueue("barCq").Obj(),
+		*utiltesting.MakeClusterQueue("active-fooCq").Obj(),
+		*utiltesting.MakeClusterQueue("active-barCq").Obj(),
+		*utiltesting.MakeClusterQueue("pending-bazCq").Obj(),
 	}
 	queues := []kueue.Queue{
 		{
 			ObjectMeta: metav1.ObjectMeta{Name: "foo"},
 			Spec: kueue.QueueSpec{
-				ClusterQueue: "fooCq",
+				ClusterQueue: "active-fooCq",
```

  done.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-13T14:19:45Z

- **pkg/queue/cache.go:19** by [@ahg-g](https://github.com/ahg-g) — 2022-05-13T14:19:45Z
```diff
@@ -0,0 +1,23 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+// StatusChecker checks status of clusterQueue.
```

  just place it in manager.go or change the file name.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-13T14:20:58Z

- **pkg/queue/manager_test.go:954** by [@ahg-g](https://github.com/ahg-g) — 2022-05-13T14:20:57Z
```diff
@@ -952,3 +948,9 @@ func workloadNamesFromQ(q *Queue) sets.String {
 	}
 	return names
 }
+
+type fakeCache struct{}
+
+func (c *fakeCache) ClusterQueueActive(name string) bool {
```

  ```suggestion
  type fakeStatusChecker struct{}
  
  func (c *fakeStatusChecker) ClusterQueueActive(name string) bool {
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-13T15:36:52Z

- **pkg/queue/manager_test.go:954** by [@kerthcet](https://github.com/kerthcet) — 2022-05-13T15:36:52Z
```diff
@@ -952,3 +948,9 @@ func workloadNamesFromQ(q *Queue) sets.String {
 	}
 	return names
 }
+
+type fakeCache struct{}
+
+func (c *fakeCache) ClusterQueueActive(name string) bool {
```

  sorry for missed that. done.
