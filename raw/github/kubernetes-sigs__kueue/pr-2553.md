# PR #2553: Use patch instead of update on jobframework.

**Summary**: Use patch instead of update on jobframework.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2553

**Last updated**: 2024-07-11T20:57:09Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/optimize-patch-and-patch-status` → `main`
- **Created**: 2024-07-08T15:41:39Z
- **Updated**: 2024-07-11T20:57:09Z
- **Merged**: 2024-07-11T20:30:36Z
- **Closed**: 2024-07-11T20:30:36Z
- **Labels**: `release-note`, `size/L`, `lgtm`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 7   **+129 / -76**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use `Patch` instead of `Update` on jobframework to prevent the risk of dropping fields.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2269

#### Special notes for your reviewer:
Follow up for https://github.com/kubernetes-sigs/kueue/pull/2501#discussion_r1668119175.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Use Patch instead of Update on jobframework to prevent the risk of dropping fields.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-08T15:41:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 430a9bf0f386c33c2fcc7cf560552a6bb58c29d3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/669039457f64fc000873709f |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-08T16:17:04Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T10:47:10Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T15:26:22Z

I want to review the whole thing from the previous version.
Please revert https://github.com/kubernetes-sigs/kueue/pull/2501 and incorporate in this PR.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T15:27:25Z

/hold

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T19:45:35Z

/hold cancel

Due to #2566 already fixed.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T19:55:17Z

/retest

Due to insufficient computing.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T20:46:47Z

/retest

Due to insufficient computing.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T04:46:53Z

/test pull-kueue-test-e2e-main-1-27

Due to #2581

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-11T20:17:16Z

LGTM label has been added.  <details>Git tree hash: 099ca25c315a841889c2fe48567b9820b2169f37</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-11T20:17:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2553#pullrequestreview-2172950313" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2553#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T20:17:49Z

/retest

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-08T15:55:31Z

LGTM, just a nit. cc @alculquicondor

- **pkg/controller/jobs/pod/pod_controller.go:884** by [@mimowo](https://github.com/mimowo) — 2024-07-08T15:47:05Z
```diff
@@ -879,15 +886,17 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
-		podOriginal := pod.DeepCopy()
-		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
-			log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
```

  It seems this log got dropped?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-08T16:02:17Z

- **pkg/controller/jobs/pod/pod_controller.go:884** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-08T16:02:17Z
```diff
@@ -879,15 +886,17 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
 		pod := extraPods[i]
-		podOriginal := pod.DeepCopy()
-		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
-			log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
```

  Good catch, thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-08T19:34:34Z

- **pkg/util/client/client.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-08T19:34:34Z
```diff
@@ -31,23 +31,33 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+func Patch(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
```

  actually, can you also do:
  
  ```golang
  objOriginal.SetResourceVersion("")
  ```
  
  This will give us the same behavior as the original code (to fail the patch if there was any other update that we are not aware of).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T11:20:18Z

- **pkg/util/client/client.go:38** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T11:20:18Z
```diff
@@ -31,23 +31,33 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+func Patch(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T11:30:07Z

- **pkg/util/client/client.go:64** by [@mimowo](https://github.com/mimowo) — 2024-07-09T11:30:06Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
```

  Will this make the patch strict? Can you provide some reference or test to confirm? 
  I know that in other places we achieve strict differently, like [here](https://github.com/kubernetes-sigs/kueue/blob/b0f6dd7951323dce577ba3699bec7915f0dce2e9/pkg/workload/workload.go#L512-L514).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T11:30:26Z

- **pkg/util/client/client.go:60** by [@mimowo](https://github.com/mimowo) — 2024-07-09T11:30:26Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  Are we ever using strict=false?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T11:46:48Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T11:46:48Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  Almost in all cases we are using `strict=true`, except
  
  https://github.com/kubernetes-sigs/kueue/blob/4f3689bc803da641f761f978142b92dbb8b92ff7/pkg/controller/jobs/job/job_controller.go#L166
  
  After research an [error](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/2553/pull-kueue-test-integration-main/1810412244677169152) I found that `Get` sometimes return old version of `Job`.
  
  ```go
  	if !j.IsSuspended() {
  		if err := clientutil.Patch(ctx, c, object, func() (bool, error) {
  			j.Suspend()
  			return true, nil
  		}); err != nil {
  			return false, fmt.Errorf("suspend: %w", err)
  		}
  		if err := c.Get(ctx, client.ObjectKeyFromObject(object), object); err != nil {
  			return false, fmt.Errorf("get after suspend: %w", err)
  		}
  		stoppedNow = true
  	}
  ```
  
  And in the next updates we will have a conflict error.
  
  So in this case we can use `strict=false` to avoid this error.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T11:51:54Z

- **pkg/util/client/client.go:60** by [@mimowo](https://github.com/mimowo) — 2024-07-09T11:51:54Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  This is worrisome, I see that `Get` could occasionally return the old object, but in this case it should succeed on retry. Wasn't this retried in the test, or was retried, but fail consistently every time?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T11:57:14Z

- **pkg/util/client/client.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T11:57:13Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
```

  > Will this make the patch strict?
  
  Yes sure. When we are setting `resourceVersion=""` and after that creating a patch we are comparing empty string `""` of `objOriginal` and some version `"243"` of `obj`. In result we will have `{"metadata":{"resourceVersion":"243"}}`
  
  > I know that in other places we achieve strict differently, like [here](https://github.com/kubernetes-sigs/kueue/blob/b0f6dd7951323dce577ba3699bec7915f0dce2e9/pkg/workload/workload.go#L512-L514).
  
  On this case the same. `BaseSSAWorkload` creates an workload with `resourceVersion=""`. Thats mean that `resourceVersion` will not add to the patch. But when we add version it will.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T11:58:20Z

- **pkg/util/client/client.go:64** by [@mimowo](https://github.com/mimowo) — 2024-07-09T11:58:20Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
```

  Thanks for explaining, sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T12:00:22Z

- **pkg/util/client/client.go:64** by [@mimowo](https://github.com/mimowo) — 2024-07-09T12:00:22Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
```

  Maybe just add a comment like "clearing Resource version from the original object to make sure it is included in the generated patch"

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T12:38:45Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T12:38:45Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  Yes, almost everytime I got this error.  It's depends of result of `c.Get`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-09T12:58:52Z

- **pkg/util/client/client.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T12:58:52Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
```

  Since we were using Update before, it was strict, so I think we should keep it the same to avoid race conditions.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-09T13:01:40Z

- **pkg/util/client/client.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T13:01:35Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  The only case where it makes sense to use strict=false is to remove finalizers. Everything else should be strict.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-09T13:02:51Z

- **pkg/controller/jobs/pod/pod_controller.go:542** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T13:02:51Z
```diff
@@ -530,11 +539,9 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 
 	return parallelize.Until(ctx, len(podsInGroup.Items), func(i int) error {
 		pod := &podsInGroup.Items[i]
-		podOriginal := pod.DeepCopy()
-		if controllerutil.RemoveFinalizer(pod, PodFinalizer) {
-			return clientutil.Patch(ctx, c, podOriginal, pod)
-		}
-		return nil
+		return clientutil.Patch(ctx, c, pod, true, func() (bool, error) {
```

  this can be non strict

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T13:35:07Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T13:35:07Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  The biggest problem that we can't execute `job.Stop()` second time if we will have an error after suspend.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T13:40:59Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T13:40:59Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  https://github.com/kubernetes-sigs/kueue/blob/4f3689bc803da641f761f978142b92dbb8b92ff7/pkg/controller/jobframework/reconciler.go#L602-L623

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T13:53:25Z

- **pkg/util/client/client.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T13:53:25Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
```

  You mean NOT strict? Because Update not return this error. 
  
  ```go
  func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, eventMsg string) (bool, error) {
  	stoppedNow := false
  	if !j.IsSuspended() {
  		j.Suspend()
  		if err := c.Update(ctx, j.Object()); err != nil {
  			return false, fmt.Errorf("suspend: %w", err)
  		}
  		stoppedNow = true
  	}
  
  	// Reset start time if necessary, so we can update the scheduling directives.
  	if j.Status.StartTime != nil {
  		j.Status.StartTime = nil
  		if err := c.Status().Update(ctx, j.Object()); err != nil {
  			return stoppedNow, fmt.Errorf("reset status: %w", err)
  		}
  	}
  
  	if changed := j.RestorePodSetsInfo(podSetsInfo); !changed {
  		return stoppedNow, nil
  	}
  	if err := c.Update(ctx, j.Object()); err != nil {
  		return false, fmt.Errorf("restore info: %w", err)
  	}
  	return stoppedNow, nil
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T13:58:06Z

- **pkg/util/client/client.go:60** by [@mimowo](https://github.com/mimowo) — 2024-07-09T13:58:06Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  > The biggest problem that we can't execute job.Stop() second time if we will have an error after suspend.
  
  Do you mean that re-try will not be attempted?
  
  Does it mean we also had an issue before - that if Update to reset start time fails, then we would also skip retry, and never fully reset the start time?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T14:01:51Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T14:01:51Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  Looks like yes :(

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T14:08:08Z

- **pkg/util/client/client.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T14:08:08Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
```

  Ah, got your point. It updating job after successful update.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T14:14:48Z

- **pkg/util/client/client.go:60** by [@mimowo](https://github.com/mimowo) — 2024-07-09T14:14:48Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  I think to retry we could probably just remove this check: `!job.IsSuspended()` and leave `if match == nil`. Can you check if this helps?
  
  If this helps, then this is something we should also do for Updates. What is a bit worrisome in case of patches, is that the get often gets the old version, and so results in common errors.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-09T15:01:10Z

- **pkg/util/client/client.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T15:01:10Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  I don't believe we had a bug in this logic before. We used to have a lot of bug reports on this topic, but we haven't had one in a couple of months.
  Let's not change the behavior, just swap every Update with a Patch with resourceVersion, and the outcome should be exactly the same.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T15:11:04Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T15:11:04Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  But we have an [issue](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/2553/pull-kueue-test-integration-main/1810412244677169152) that approve it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-09T15:13:03Z

- **pkg/util/client/client.go:60** by [@mimowo](https://github.com/mimowo) — 2024-07-09T15:13:03Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  IIRC we solved the other scenarios by removing the check `!job.IsSuspended()` to re-enter `stopJob` on the Reconcile retry. For example here: 
  - [here](https://github.com/kubernetes-sigs/kueue/blob/952301289948ffb1c1095958e9628835252000ae/pkg/controller/jobframework/reconciler.go#L343) we re-enter whenever there is a deletion timestamp
  - [here](https://github.com/kubernetes-sigs/kueue/blob/952301289948ffb1c1095958e9628835252000ae/pkg/controller/jobframework/reconciler.go#L489) we re-enter when not admitted
  
  The scenario for no matching workload might be a remaining one. Still, even if so, we shouldn't couple the fix in this PR. I will double check and open a dedicated issue potentially.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T15:18:56Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T15:18:56Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  @mimowo I agree with you to open another PR. But without fix this issue I can't use `strict=true` on `job.Stop()` :(

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-09T16:44:54Z

- **pkg/util/client/client.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-09T16:44:54Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  Created an issue #2566

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-09T20:31:42Z

- **pkg/util/client/client.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-09T20:31:42Z
```diff
@@ -31,23 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
 		return err
 	}
 	return nil
 }
 
-func PatchStatus(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  I see, thanks! We should be able to fix that independently.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-07-10T20:20:52Z

- **pkg/util/client/client.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-10T20:20:45Z
```diff
@@ -31,12 +31,47 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  ```suggestion
  func Patch(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
  ```
  
  Is there any reasons that we still need to provide the `strict` option?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T20:22:29Z

- **pkg/util/client/client.go:37** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T20:22:29Z
```diff
@@ -31,12 +31,47 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  Yes, due to https://github.com/kubernetes-sigs/kueue/pull/2553#discussion_r1670493486.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-07-10T20:24:33Z

- **pkg/util/client/client.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-10T20:24:33Z
```diff
@@ -31,12 +31,47 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  I see. So, we can remove the `strict` option from the `PatchStatus`, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T20:47:36Z

- **pkg/util/client/client.go:37** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T20:47:35Z
```diff
@@ -31,12 +31,47 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
```

  Removed. Thanks.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-11T19:10:28Z

- **pkg/controller/jobs/job/job_controller.go:185** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T18:40:26Z
```diff
@@ -163,34 +164,49 @@ func (j *Job) Suspend() {
 	j.Spec.Suspend = ptr.To(true)
 }
 
-func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, eventMsg string) (bool, error) {
+func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, _ string) (bool, error) {
+	object := j.Object()
 	stoppedNow := false
+
 	if !j.IsSuspended() {
-		j.Suspend()
-		if j.ObjectMeta.Annotations == nil {
-			j.ObjectMeta.Annotations = map[string]string{}
-		}
-		// We are using annotation to be sure that all updates finished successfully.
-		j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
-		if err := c.Update(ctx, j.Object()); err != nil {
+		if err := clientutil.Patch(ctx, c, object, true, func() (bool, error) {
+			j.Suspend()
+			if j.ObjectMeta.Annotations == nil {
+				j.ObjectMeta.Annotations = map[string]string{}
+			}
+			// We are using annotation to be sure that all updates finished successfully.
+			j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
+			return true, nil
+		}); err != nil {
 			return false, fmt.Errorf("suspend: %w", err)
 		}
+		if err := c.Get(ctx, client.ObjectKeyFromObject(object), object); err != nil {
+			return false, fmt.Errorf("get after suspend: %w", err)
+		}
```

  This shouldn't be necessary. `Patch` updates the object with the results from the server, so it should have the latest resource version https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client#Writer
  
  See my suggestion in client.go

- **pkg/util/client/client.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T18:42:18Z
```diff
@@ -31,12 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
+	if err != nil {
+		return err
+	}
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
+		return err
+	}
+	return nil
+}
+
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// The resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
+	objOriginal.SetResourceVersion("")
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Status().Patch(ctx, objOriginal, patch); err != nil {
```

  ```suggestion
  	if err = c.Status().Patch(ctx, obj, patch); err != nil {
  ```

- **pkg/controller/jobs/job/job_controller.go:197** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T18:43:21Z
```diff
@@ -163,34 +164,49 @@ func (j *Job) Suspend() {
 	j.Spec.Suspend = ptr.To(true)
 }
 
-func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, eventMsg string) (bool, error) {
+func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, _ string) (bool, error) {
+	object := j.Object()
 	stoppedNow := false
+
 	if !j.IsSuspended() {
-		j.Suspend()
-		if j.ObjectMeta.Annotations == nil {
-			j.ObjectMeta.Annotations = map[string]string{}
-		}
-		// We are using annotation to be sure that all updates finished successfully.
-		j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
-		if err := c.Update(ctx, j.Object()); err != nil {
+		if err := clientutil.Patch(ctx, c, object, true, func() (bool, error) {
+			j.Suspend()
+			if j.ObjectMeta.Annotations == nil {
+				j.ObjectMeta.Annotations = map[string]string{}
+			}
+			// We are using annotation to be sure that all updates finished successfully.
+			j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
+			return true, nil
+		}); err != nil {
 			return false, fmt.Errorf("suspend: %w", err)
 		}
+		if err := c.Get(ctx, client.ObjectKeyFromObject(object), object); err != nil {
+			return false, fmt.Errorf("get after suspend: %w", err)
+		}
 		stoppedNow = true
 	}
 
 	// Reset start time if necessary, so we can update the scheduling directives.
 	if j.Status.StartTime != nil {
-		j.Status.StartTime = nil
-		if err := c.Status().Update(ctx, j.Object()); err != nil {
+		if err := clientutil.PatchStatus(ctx, c, object, func() (bool, error) {
+			j.Status.StartTime = nil
+			return true, nil
+		}); err != nil {
 			return stoppedNow, fmt.Errorf("reset status: %w", err)
 		}
+		if err := c.Get(ctx, client.ObjectKeyFromObject(object), object); err != nil {
```

  Same here.

- **pkg/util/client/client.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T18:46:44Z
```diff
@@ -31,12 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
```

  This is not SSA. SSA uses an SSA patch. This is a merge patch.

- **pkg/controller/jobs/job/job_multikueue_adapter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T18:50:13Z
```diff
@@ -35,30 +35,33 @@ import (
 	"sigs.k8s.io/kueue/pkg/controller/jobframework"
 	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
 )
 
 type multikueueAdapter struct{}
 
 var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
 
 func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
-	localJob := batchv1.Job{}
-	err := localClient.Get(ctx, key, &localJob)
+	localJob := &batchv1.Job{}
```

  I'm not sure if we should be using strict patches here... But I would prefer we put MultiKueue changes in another PR, so they can easily be reverted independently, in case of emergencies.

- **pkg/controller/jobs/pod/pod_controller.go:280** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-11T18:54:23Z
```diff
@@ -253,54 +253,63 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 			return fmt.Errorf("%w: expecting 1 pod set got %d", podset.ErrInvalidPodsetInfo, len(podSetsInfo))
 		}
 
-		podOriginal := p.pod.DeepCopy()
-
-		if ungated := ungatePod(&p.pod); !ungated {
-			return nil
-		}
+		var ungated bool
+		if err := clientutil.Patch(ctx, c, &p.pod, true, func() (bool, error) {
+			if ungated = ungatePod(&p.pod); !ungated {
+				return false, nil
+			}
 
-		if err := podset.Merge(&p.pod.ObjectMeta, &p.pod.Spec, podSetsInfo[0]); err != nil {
+			return true, podset.Merge(&p.pod.ObjectMeta, &p.pod.Spec, podSetsInfo[0])
+		}); err != nil {
 			return err
 		}
-
-		if err := clientutil.Patch(ctx, c, podOriginal, &p.pod); err != nil {
-			return err
+		if !ungated {
+			return nil
 		}
+
 		if recorder != nil {
 			recorder.Event(&p.pod, corev1.EventTypeNormal, jobframework.ReasonStarted, msg)
 		}
+
 		return nil
 	}
 
 	return parallelize.Until(ctx, len(p.list.Items), func(i int) error {
 		pod := &p.list.Items[i]
-		podOriginal := pod.DeepCopy()
 
-		if ungated := ungatePod(pod); !ungated {
-			return nil
-		}
+		var ungated bool
```

  for readability, it might be better to do this at the beginning.
  
  ```golang
  if gateIndex(pod) == gateNotFound {
    return nil
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-11T20:07:21Z

- **pkg/util/client/client.go:72** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T20:07:20Z
```diff
@@ -31,12 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
+// If strict is true, the resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	if strict {
+		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
+		objOriginal.SetResourceVersion("")
+	}
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
+	if err != nil {
+		return err
+	}
+	if err = c.Patch(ctx, objOriginal, patch); err != nil {
+		return err
+	}
+	return nil
+}
+
+// PatchStatus applies the merge patch of client.Object status with SSA.
+// The resourceVersion will be part of the patch, make this call fail if
+// client.Object was changed.
+func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	objOriginal := obj.DeepCopyObject().(client.Object)
+	// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
+	objOriginal.SetResourceVersion("")
+	updated, err := update()
+	if err != nil || !updated {
+		return err
+	}
+	patch, err := CreatePatch(objOriginal, obj)
 	if err != nil {
 		return err
 	}
-	if err = c.Patch(ctx, before, patch); err != nil {
+	if err = c.Status().Patch(ctx, objOriginal, patch); err != nil {
```

  Yes, it works. Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-11T20:07:31Z

- **pkg/controller/jobs/job/job_controller.go:185** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T20:07:31Z
```diff
@@ -163,34 +164,49 @@ func (j *Job) Suspend() {
 	j.Spec.Suspend = ptr.To(true)
 }
 
-func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, eventMsg string) (bool, error) {
+func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, _ string) (bool, error) {
+	object := j.Object()
 	stoppedNow := false
+
 	if !j.IsSuspended() {
-		j.Suspend()
-		if j.ObjectMeta.Annotations == nil {
-			j.ObjectMeta.Annotations = map[string]string{}
-		}
-		// We are using annotation to be sure that all updates finished successfully.
-		j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
-		if err := c.Update(ctx, j.Object()); err != nil {
+		if err := clientutil.Patch(ctx, c, object, true, func() (bool, error) {
+			j.Suspend()
+			if j.ObjectMeta.Annotations == nil {
+				j.ObjectMeta.Annotations = map[string]string{}
+			}
+			// We are using annotation to be sure that all updates finished successfully.
+			j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
+			return true, nil
+		}); err != nil {
 			return false, fmt.Errorf("suspend: %w", err)
 		}
+		if err := c.Get(ctx, client.ObjectKeyFromObject(object), object); err != nil {
+			return false, fmt.Errorf("get after suspend: %w", err)
+		}
```

  Removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-11T20:07:41Z

- **pkg/controller/jobs/job/job_controller.go:197** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T20:07:41Z
```diff
@@ -163,34 +164,49 @@ func (j *Job) Suspend() {
 	j.Spec.Suspend = ptr.To(true)
 }
 
-func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, eventMsg string) (bool, error) {
+func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, _ string) (bool, error) {
+	object := j.Object()
 	stoppedNow := false
+
 	if !j.IsSuspended() {
-		j.Suspend()
-		if j.ObjectMeta.Annotations == nil {
-			j.ObjectMeta.Annotations = map[string]string{}
-		}
-		// We are using annotation to be sure that all updates finished successfully.
-		j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
-		if err := c.Update(ctx, j.Object()); err != nil {
+		if err := clientutil.Patch(ctx, c, object, true, func() (bool, error) {
+			j.Suspend()
+			if j.ObjectMeta.Annotations == nil {
+				j.ObjectMeta.Annotations = map[string]string{}
+			}
+			// We are using annotation to be sure that all updates finished successfully.
+			j.ObjectMeta.Annotations[StoppingAnnotation] = "true"
+			return true, nil
+		}); err != nil {
 			return false, fmt.Errorf("suspend: %w", err)
 		}
+		if err := c.Get(ctx, client.ObjectKeyFromObject(object), object); err != nil {
+			return false, fmt.Errorf("get after suspend: %w", err)
+		}
 		stoppedNow = true
 	}
 
 	// Reset start time if necessary, so we can update the scheduling directives.
 	if j.Status.StartTime != nil {
-		j.Status.StartTime = nil
-		if err := c.Status().Update(ctx, j.Object()); err != nil {
+		if err := clientutil.PatchStatus(ctx, c, object, func() (bool, error) {
+			j.Status.StartTime = nil
+			return true, nil
+		}); err != nil {
 			return stoppedNow, fmt.Errorf("reset status: %w", err)
 		}
+		if err := c.Get(ctx, client.ObjectKeyFromObject(object), object); err != nil {
```

  Removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-11T20:08:18Z

- **pkg/util/client/client.go:34** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T20:08:18Z
```diff
@@ -31,12 +31,45 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 	return client.RawPatch(patchBase.Type(), patchBytes), nil
 }
 
-func Patch(ctx context.Context, c client.Client, before, after client.Object) error {
-	patch, err := CreatePatch(before, after)
+// Patch applies the merge patch of client.Object with SSA.
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-11T20:08:43Z

- **pkg/controller/jobs/pod/pod_controller.go:280** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T20:08:43Z
```diff
@@ -253,54 +253,63 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 			return fmt.Errorf("%w: expecting 1 pod set got %d", podset.ErrInvalidPodsetInfo, len(podSetsInfo))
 		}
 
-		podOriginal := p.pod.DeepCopy()
-
-		if ungated := ungatePod(&p.pod); !ungated {
-			return nil
-		}
+		var ungated bool
+		if err := clientutil.Patch(ctx, c, &p.pod, true, func() (bool, error) {
+			if ungated = ungatePod(&p.pod); !ungated {
+				return false, nil
+			}
 
-		if err := podset.Merge(&p.pod.ObjectMeta, &p.pod.Spec, podSetsInfo[0]); err != nil {
+			return true, podset.Merge(&p.pod.ObjectMeta, &p.pod.Spec, podSetsInfo[0])
+		}); err != nil {
 			return err
 		}
-
-		if err := clientutil.Patch(ctx, c, podOriginal, &p.pod); err != nil {
-			return err
+		if !ungated {
+			return nil
 		}
+
 		if recorder != nil {
 			recorder.Event(&p.pod, corev1.EventTypeNormal, jobframework.ReasonStarted, msg)
 		}
+
 		return nil
 	}
 
 	return parallelize.Until(ctx, len(p.list.Items), func(i int) error {
 		pod := &p.list.Items[i]
-		podOriginal := pod.DeepCopy()
 
-		if ungated := ungatePod(pod); !ungated {
-			return nil
-		}
+		var ungated bool
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-11T20:17:11Z

/lgtm
/approve

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-11T20:57:09Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:46** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T20:57:09Z
```diff
@@ -35,30 +35,33 @@ import (
 	"sigs.k8s.io/kueue/pkg/controller/jobframework"
 	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
 )
 
 type multikueueAdapter struct{}
 
 var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
 
 func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
-	localJob := batchv1.Job{}
-	err := localClient.Get(ctx, key, &localJob)
+	localJob := &batchv1.Job{}
```

  Created new PR for MultiKueue changes #2590.
