# PR #2567: Retry to reset start time and restore info after error on stop job.

**Summary**: Retry to reset start time and restore info after error on stop job.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2567

**Last updated**: 2024-07-10T19:22:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/job-reconciler-does-not-retry-to-stop-job-after-error` → `main`
- **Created**: 2024-07-09T19:56:41Z
- **Updated**: 2024-07-10T19:22:01Z
- **Merged**: 2024-07-10T19:11:23Z
- **Closed**: 2024-07-10T19:11:23Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 2   **+10 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
To fix an issue that didn't allow to retry reset start time and restore info after error on stop job.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2566

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fixed issue that prevented restoring the startTime and pod template when evicting a batch/v1 Job, if any API errors happened in the process
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-09T19:56:43Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-09T19:56:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0ccedeaa4864e42ac676ec7502a8d0f9791b9524 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/668ecbe74f960e0008b9086e |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-10T00:18:53Z

/retest
due to insufficient computing.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-10T18:03:39Z

LGTM label has been added.  <details>Git tree hash: 004dfc32f4a71005499e9a3914c22118dc841148</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-10T18:03:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2567#pullrequestreview-2169888762" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2567#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T18:05:21Z

/release-note-edit
```release-note
Fixed issue that prevented restoring the startTime and pod template when evicting a batch/v1 Job, if any API errors happened in the process
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T18:05:31Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T18:12:39Z

/retest
due to insufficient computing.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T18:23:00Z

/retest
due to insufficient computing.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T18:33:14Z

/retest
due to insufficient computing.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T18:44:18Z

/retest
due to insufficient computing.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T18:54:31Z

/retest
due to insufficient computing.

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-07-10T00:46:04Z

- **pkg/controller/jobframework/reconciler.go:600** by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-10T00:45:59Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  What happens in the case where there is a suspended Job without restored nodeSelector which means the reconciler failed to restore nodeSelector in the previous reconciliation.
  
  I'm suspecting that the Worlload is accidentally removed, right?
  Could you add a unit test in the batch/job controller for the assumed situations?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T08:07:27Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T08:07:27Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  You mean this case?
  
  https://github.com/kubernetes-sigs/kueue/blob/dfa182907281dea39fd227aea58abf1374727bef/pkg/controller/jobs/job/job_controller_test.go#L2552-L2601

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T08:21:09Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T08:21:09Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  Looks like removing workloads happening after stop, so it's nothing change. 
  
  https://github.com/kubernetes-sigs/kueue/blob/dfa182907281dea39fd227aea58abf1374727bef/pkg/controller/jobframework/reconciler.go#L600-L641

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-10T14:33:51Z

- **pkg/controller/jobframework/reconciler.go:600** by [@trasc](https://github.com/trasc) — 2024-07-10T14:33:33Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  The main problem is that a "half stopped" will have the "running"  pod set info making it not equivalent to its workload , therefore the problematic part of this condition if  is `match==nil` not `!job.IsSuspended()`.

- **pkg/controller/jobs/job/job_controller_test.go:2561** by [@trasc](https://github.com/trasc) — 2024-07-10T14:33:44Z
```diff
@@ -2558,7 +2558,7 @@ func TestReconciler(t *testing.T) {
 				Suspend(true).
 				Obj(),
 			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
-				Key:      "tolerationkey2",
+				Key:      "tolerationkey1",
```

  why?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T17:07:13Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mimowo](https://github.com/mimowo) — 2024-07-10T17:07:13Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  > I'm suspecting that the Worlload is accidentally removed, right?
  
  I suspect the workload continues to exist, but no longer matches the job, @mbobrovskyi can you check what happens on a running system? I think the code is rather complex already, so it would be good to do an actual e2e check, than just rely on static code analysis

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T17:10:23Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T17:10:22Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  Looks like this solution will not work on another cases. 
  
  For example here:
  
  https://github.com/kubernetes-sigs/kueue/blob/dfa182907281dea39fd227aea58abf1374727bef/pkg/controller/jobframework/reconciler.go#L484-L492

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T17:13:59Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mimowo](https://github.com/mimowo) — 2024-07-10T17:13:59Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  > Looks like this solution will not work on another cases.
  
  How does the code change affect the code you reference here? Still, I think we should really test this e2e (by e2e here I mean on a running system, not necessarily in our testing e2e testing framework).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T17:14:54Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T17:14:54Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  Maybe it's a good idea to create something like transactions using labels. We can add some label when we start suspending job and delete this label on the last update.
  
  ```go
  func (j *Job) Stop(ctx context.Context, c client.Client, podSetsInfo []podset.PodSetInfo, _ jobframework.StopReason, eventMsg string) (bool, error) {
  	stoppedNow := false
  	if !j.IsSuspended() {
  		j.Suspend()
  		j.ObjectMeta.Labels[StoppingLabel] = "true"
  		if err := c.Update(ctx, j.Object()); err != nil {
  			return false, fmt.Errorf("suspend: %w", err)
  		}
  		stoppedNow = true
  	}
  
  	// Reset start time if necessary, so we can update the scheduling directives.
  	if j.Status.StartTime != nil {
  		j.Status.StartTime = nil
  		if err := c.Status().Update(ctx, j.Object()); err != nil {
  			return stoppedNow, fmt.Errorf("reset status: %w", err)
  		}
  	}
  
  	j.RestorePodSetsInfo(podSetsInfo)
  	delete(j.ObjectMeta.Labels, StoppingLabel)
  	if err := c.Update(ctx, j.Object()); err != nil {
  		return false, fmt.Errorf("restore info: %w", err)
  	}
  	return stoppedNow, nil
  }
  
  ...
  
  func (j *Job) IsSuspended() bool {
      return j.Spec.Suspend != nil && *j.Spec.Suspend && j.Labels[StoppingLabel] != "true"
  }
  
  ```
   
  WDYT @alculquicondor @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T17:17:12Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mimowo](https://github.com/mimowo) — 2024-07-10T17:17:12Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  Using labels seems awkward, if anything we could use conditions for this purpose, but I'm not clear if this is needed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T17:17:36Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T17:17:36Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  > > Looks like this solution will not work on another cases.
  > 
  > How does the code change affect the code you reference here? Still, I think we should really test this e2e.
  
  I mean that it will not "re-stop" job because we have:
  
  https://github.com/kubernetes-sigs/kueue/blob/dfa182907281dea39fd227aea58abf1374727bef/pkg/controller/jobframework/reconciler.go#L445-L446

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-10T17:20:43Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-10T17:20:43Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  > Using labels seems awkward, if anything we could use conditions for this purpose, but I'm not clear if this is needed.
  
  Agree, but on this case we will know for sure that it stopped.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T17:21:47Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mimowo](https://github.com/mimowo) — 2024-07-10T17:21:47Z
```diff
@@ -596,8 +596,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		toDelete = toDelete[1:]
 	}
 
-	// If there is no matching workload and the job is running, suspend it.
-	if match == nil && !job.IsSuspended() {
+	// If there is no matching workload suspend job.
+	if match == nil {
```

  I see. In that case it seems trickier than initially thought.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-10T18:03:35Z

/lgtm
/approve
