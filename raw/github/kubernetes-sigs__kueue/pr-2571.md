# PR #2571: Improve ClusterQueueSnapshot.ResourceGroups Testing

**Summary**: Improve ClusterQueueSnapshot.ResourceGroups Testing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2571

**Last updated**: 2024-08-09T13:04:40Z

---

## Metadata

- **State**: closed
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `test_grouping_and_capacity` → `main`
- **Created**: 2024-07-10T12:56:55Z
- **Updated**: 2024-08-09T13:04:40Z
- **Merged**: —
- **Closed**: 2024-08-09T13:04:39Z
- **Labels**: `size/L`, `release-note-none`, `needs-rebase`, `kind/cleanup`, `do-not-merge/hold`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 2   **+171 / -141**

## Description

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
Preparing for refactor of ResourceGroups, by testing its properties without directly comparing to the snapshot.

We ensure that label keys are correctly assigned to ResourceGroups, that flavors/resources are properly grouped together, and that capacities/limits are stored in ClusterQueueSnapshot.

Additionally, we delete more usages of ClusterQueueSnapshot.ResourceGroups in tests

#2502

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-10T12:57:00Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2571#" title="Author self-approved">gabesaba</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [alculquicondor](https://github.com/alculquicondor) for approval. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["alculquicondor"]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-10T12:57:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c71fc9ed5df63456c9dfe3f113370db22a38c7b9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/668e96f38d6f1b0008647f8d |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T13:01:02Z

cc @PBundyra

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T13:01:31Z

/assign @PBundyra

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T13:02:23Z

/retest

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T13:59:42Z

Thanks for the review, @PBundyra

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T14:18:46Z

/retest

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T14:19:05Z

/assign @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T14:32:02Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T18:34:41Z

/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-12T20:14:14Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T13:09:55Z

- **pkg/cache/snapshot_test.go:1216** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T13:09:55Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				FlavorQuotas,
```

  Can we use Obj() here?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T13:13:21Z

- **pkg/cache/snapshot_test.go:1260** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T13:13:21Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				FlavorQuotas,
+			utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				FlavorQuotas,
+		).
+		ResourceGroup(
+			utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				FlavorQuotas,
+			utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				FlavorQuotas,
+		).
+		ClusterQueue
+	if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+		t.Fatal("Failed to add CQ to cache", err)
+	}
+
+	// Flavors (and Label) used by ResourceGroup1
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").
+		NodeLabel("instance", "demand").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").
+		NodeLabel("instance", "spot").
+		NodeLabel("cost", "cheap").
+		Obj())
+
+	// Flavors (and Label) used by ResourceGroup2
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").
+		NodeLabel("accelerator_type", "type1").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").
+		NodeLabel("instance", "demand").
+		NodeLabel("cost", "dear").
+		Obj())
+
+	t.Run("TestLabelKeys", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New("instance", "cost")
+		group2Expected := sets.New("instance", "accelerator_type", "cost")
+
+		if labelKeys := clusterQueueSnapshot.RGByResource("cpu").LabelKeys; !labelKeys.Equal(group1Expected) {
```

  ```suggestion
  		if labelKeys := clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).LabelKeys; !labelKeys.Equal(group1Expected) {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T13:13:39Z

- **pkg/cache/snapshot_test.go:1263** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T13:13:39Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				FlavorQuotas,
+			utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				FlavorQuotas,
+		).
+		ResourceGroup(
+			utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				FlavorQuotas,
+			utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				FlavorQuotas,
+		).
+		ClusterQueue
+	if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+		t.Fatal("Failed to add CQ to cache", err)
+	}
+
+	// Flavors (and Label) used by ResourceGroup1
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").
+		NodeLabel("instance", "demand").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").
+		NodeLabel("instance", "spot").
+		NodeLabel("cost", "cheap").
+		Obj())
+
+	// Flavors (and Label) used by ResourceGroup2
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").
+		NodeLabel("accelerator_type", "type1").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").
+		NodeLabel("instance", "demand").
+		NodeLabel("cost", "dear").
+		Obj())
+
+	t.Run("TestLabelKeys", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New("instance", "cost")
+		group2Expected := sets.New("instance", "accelerator_type", "cost")
+
+		if labelKeys := clusterQueueSnapshot.RGByResource("cpu").LabelKeys; !labelKeys.Equal(group1Expected) {
+			t.Errorf("unexpected LabelKeys %s", labelKeys.SymmetricDifference(group1Expected))
+		}
+		if labelKeys := clusterQueueSnapshot.RGByResource("memory").LabelKeys; !labelKeys.Equal(group1Expected) {
```

  ```suggestion
  		if labelKeys := clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).LabelKeys; !labelKeys.Equal(group1Expected) {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T13:26:54Z

- **pkg/cache/snapshot_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T13:26:54Z
  Could we use `cmp.Diff()` instead of `Equal()` to compare structs?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-10T13:58:31Z

- **pkg/cache/snapshot_test.go:1216** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T13:58:31Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				FlavorQuotas,
```

  Done. In general, I wonder if it may be more ergonomic to replace all usages of Obj() with FlavorQuotas (and also replace .Obj() all wrapper types), as I think most of the time we actually need the underlying type, and not a pointer.
  
  Out of the scope of this PR obviously.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-10T13:59:32Z

- **pkg/cache/snapshot_test.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-07-10T13:59:32Z
  Done. For my future reference, what is the advantage of using `cmp.Diff` rather than `Equal` (or whatever equality some data structure defines)?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T14:06:09Z

- **pkg/cache/snapshot_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T14:06:08Z
  Could we make new tests more expandable in the future, and divide them into two parts: cases and running them. E.g. like https://github.com/PBundyra/kueue/blob/0dbcaf96ed784f0fe23d8e458e177421b6347e5f/pkg/cache/snapshot_test.go#L45
  
  This way we could add more scenarios in the future

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T14:09:08Z

- **pkg/cache/snapshot_test.go:1358** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T14:09:08Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+		).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+		).
+		ClusterQueue
+	if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+		t.Fatal("Failed to add CQ to cache", err)
+	}
+
+	// Flavors (and Label) used by ResourceGroup1
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").
+		NodeLabel("instance", "demand").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").
+		NodeLabel("instance", "spot").
+		NodeLabel("cost", "cheap").
+		Obj())
+
+	// Flavors (and Label) used by ResourceGroup2
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").
+		NodeLabel("accelerator_type", "type1").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").
+		NodeLabel("instance", "demand").
+		NodeLabel("cost", "dear").
+		Obj())
+
+	t.Run("TestLabelKeys", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New("instance", "cost")
+		group2Expected := sets.New("instance", "accelerator_type", "cost")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").LabelKeys, group2Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+	})
+
+	t.Run("TestFlavorGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		getFlavors := func(r *ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+			flavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, f := range r.Flavors {
+				flavors.Insert(f.Name)
+			}
+			return flavors
+		}
+
+		group1Expected := sets.New[kueue.ResourceFlavorReference]("on-demand", "spot")
+		group2Expected := sets.New[kueue.ResourceFlavorReference]("gpu1", "gpu2")
+
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource("example.com/gpu")), group2Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+	})
+
+	t.Run("TestResourceGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New[corev1.ResourceName]("cpu", "memory")
+		group2Expected := sets.New[corev1.ResourceName]("example.com/gpu")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").CoveredResources, group2Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+	})
+}
+
+func TestQuotaAndLimits(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").Obj())
+
+	t.Run("TestQuotasAndLimits", func(t *testing.T) {
+		clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("5").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").BorrowingLimit("50").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").LendingLimit("100").Append().
+					Obj(),
+			).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("gpu1").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").BorrowingLimit("5").LendingLimit("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("gpu2").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("3").BorrowingLimit("1").LendingLimit("4").Append().
+					Obj(),
+			).
+			ClusterQueue
+
+		if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+			t.Fatal("Failed to add CQ to cache", err)
+		}
+
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		actualCapacitiesAndLimits := make(map[resources.FlavorResource]*ResourceQuota, 0)
+		for _, frq := range flavorResourceQuotas(clusterQueueSnapshot) {
+			actualCapacitiesAndLimits[frq.fr] = frq.quota
+		}
+
+		expectedCapacityAndLimits := map[resources.FlavorResource]*ResourceQuota{
+			{Flavor: "on-demand", Resource: "cpu"}:        {Nominal: 5_000},
```

  ```suggestion
  			{Flavor: "on-demand", Resource: corev1.ResourceCPU}:        {Nominal: 5_000},
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T14:09:20Z

- **pkg/cache/snapshot_test.go:1359** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T14:09:19Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+		).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+		).
+		ClusterQueue
+	if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+		t.Fatal("Failed to add CQ to cache", err)
+	}
+
+	// Flavors (and Label) used by ResourceGroup1
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").
+		NodeLabel("instance", "demand").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").
+		NodeLabel("instance", "spot").
+		NodeLabel("cost", "cheap").
+		Obj())
+
+	// Flavors (and Label) used by ResourceGroup2
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").
+		NodeLabel("accelerator_type", "type1").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").
+		NodeLabel("instance", "demand").
+		NodeLabel("cost", "dear").
+		Obj())
+
+	t.Run("TestLabelKeys", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New("instance", "cost")
+		group2Expected := sets.New("instance", "accelerator_type", "cost")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").LabelKeys, group2Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+	})
+
+	t.Run("TestFlavorGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		getFlavors := func(r *ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+			flavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, f := range r.Flavors {
+				flavors.Insert(f.Name)
+			}
+			return flavors
+		}
+
+		group1Expected := sets.New[kueue.ResourceFlavorReference]("on-demand", "spot")
+		group2Expected := sets.New[kueue.ResourceFlavorReference]("gpu1", "gpu2")
+
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource("example.com/gpu")), group2Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+	})
+
+	t.Run("TestResourceGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New[corev1.ResourceName]("cpu", "memory")
+		group2Expected := sets.New[corev1.ResourceName]("example.com/gpu")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").CoveredResources, group2Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+	})
+}
+
+func TestQuotaAndLimits(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").Obj())
+
+	t.Run("TestQuotasAndLimits", func(t *testing.T) {
+		clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("5").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").BorrowingLimit("50").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").LendingLimit("100").Append().
+					Obj(),
+			).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("gpu1").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").BorrowingLimit("5").LendingLimit("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("gpu2").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("3").BorrowingLimit("1").LendingLimit("4").Append().
+					Obj(),
+			).
+			ClusterQueue
+
+		if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+			t.Fatal("Failed to add CQ to cache", err)
+		}
+
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		actualCapacitiesAndLimits := make(map[resources.FlavorResource]*ResourceQuota, 0)
+		for _, frq := range flavorResourceQuotas(clusterQueueSnapshot) {
+			actualCapacitiesAndLimits[frq.fr] = frq.quota
+		}
+
+		expectedCapacityAndLimits := map[resources.FlavorResource]*ResourceQuota{
+			{Flavor: "on-demand", Resource: "cpu"}:        {Nominal: 5_000},
+			{Flavor: "on-demand", Resource: "memory"}:     {Nominal: 10},
```

  ```suggestion
  			{Flavor: "on-demand", Resource: corev1.ResourceMemory}:     {Nominal: 10},
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-10T14:11:32Z

- **pkg/cache/snapshot_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T14:11:32Z
  `cmp.Diff` will output a readable diff of what's different, whereas if you use `Equal`, you just get true or false.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T14:13:12Z

- **pkg/cache/snapshot_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T14:13:12Z
  It's generally recommended as general guidelines e.g. in [sig-scheduling contributing guide](https://github.com/kubernetes/community/blob/master/sig-scheduling/CONTRIBUTING.md#technical-and-style-guidelines)
  
  From my perspective it's also useful to be able to pass some comparison options such as `IgnoreFields()` etc.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T14:22:10Z

- **pkg/cache/snapshot_test.go:1228** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T14:22:10Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+		).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
```

  Is `NominalQuota()` necessary here?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-10T14:49:00Z

- **pkg/cache/snapshot_test.go:1343** by [@PBundyra](https://github.com/PBundyra) — 2024-07-10T14:48:59Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+		).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+		).
+		ClusterQueue
+	if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+		t.Fatal("Failed to add CQ to cache", err)
+	}
+
+	// Flavors (and Label) used by ResourceGroup1
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").
+		NodeLabel("instance", "demand").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").
+		NodeLabel("instance", "spot").
+		NodeLabel("cost", "cheap").
+		Obj())
+
+	// Flavors (and Label) used by ResourceGroup2
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").
+		NodeLabel("accelerator_type", "type1").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").
+		NodeLabel("instance", "demand").
+		NodeLabel("cost", "dear").
+		Obj())
+
+	t.Run("TestLabelKeys", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New("instance", "cost")
+		group2Expected := sets.New("instance", "accelerator_type", "cost")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").LabelKeys, group2Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+	})
+
+	t.Run("TestFlavorGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		getFlavors := func(r *ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+			flavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, f := range r.Flavors {
+				flavors.Insert(f.Name)
+			}
+			return flavors
+		}
+
+		group1Expected := sets.New[kueue.ResourceFlavorReference]("on-demand", "spot")
+		group2Expected := sets.New[kueue.ResourceFlavorReference]("gpu1", "gpu2")
+
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource("example.com/gpu")), group2Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+	})
+
+	t.Run("TestResourceGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New[corev1.ResourceName](corev1.ResourceCPU, corev1.ResourceMemory)
+		group2Expected := sets.New[corev1.ResourceName]("example.com/gpu")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").CoveredResources, group2Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+	})
+}
+
+func TestQuotaAndLimits(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").Obj())
+
+	t.Run("TestQuotasAndLimits", func(t *testing.T) {
+		clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("5").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").BorrowingLimit("50").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").LendingLimit("100").Append().
+					Obj(),
+			).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("gpu1").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").BorrowingLimit("5").LendingLimit("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("gpu2").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("3").BorrowingLimit("1").LendingLimit("4").Append().
+					Obj(),
+			).
+			ClusterQueue
```

  ```suggestion
  			Obj()
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-10T16:25:10Z

- **pkg/cache/snapshot_test.go:1352** by [@mimowo](https://github.com/mimowo) — 2024-07-10T16:25:10Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+		).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+		).
+		ClusterQueue
+	if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+		t.Fatal("Failed to add CQ to cache", err)
+	}
+
+	// Flavors (and Label) used by ResourceGroup1
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").
+		NodeLabel("instance", "demand").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").
+		NodeLabel("instance", "spot").
+		NodeLabel("cost", "cheap").
+		Obj())
+
+	// Flavors (and Label) used by ResourceGroup2
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").
+		NodeLabel("accelerator_type", "type1").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").
+		NodeLabel("instance", "demand").
+		NodeLabel("cost", "dear").
+		Obj())
+
+	t.Run("TestLabelKeys", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New("instance", "cost")
+		group2Expected := sets.New("instance", "accelerator_type", "cost")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).LabelKeys, group1Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").LabelKeys, group2Expected); diff != "" {
+			t.Errorf("unexpected LabelKeys %s", diff)
+		}
+	})
+
+	t.Run("TestFlavorGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		getFlavors := func(r *ResourceGroup) sets.Set[kueue.ResourceFlavorReference] {
+			flavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, f := range r.Flavors {
+				flavors.Insert(f.Name)
+			}
+			return flavors
+		}
+
+		group1Expected := sets.New[kueue.ResourceFlavorReference]("on-demand", "spot")
+		group2Expected := sets.New[kueue.ResourceFlavorReference]("gpu1", "gpu2")
+
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory)), group1Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+		if diff := cmp.Diff(getFlavors(clusterQueueSnapshot.RGByResource("example.com/gpu")), group2Expected); diff != "" {
+			t.Errorf("unexpected flavors %s", diff)
+		}
+	})
+
+	t.Run("TestResourceGrouping", func(t *testing.T) {
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		group1Expected := sets.New[corev1.ResourceName](corev1.ResourceCPU, corev1.ResourceMemory)
+		group2Expected := sets.New[corev1.ResourceName]("example.com/gpu")
+
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceCPU).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource(corev1.ResourceMemory).CoveredResources, group1Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+		if diff := cmp.Diff(clusterQueueSnapshot.RGByResource("example.com/gpu").CoveredResources, group2Expected); diff != "" {
+			t.Errorf("unexpected resources %s", diff)
+		}
+	})
+}
+
+func TestQuotaAndLimits(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").Obj())
+
+	t.Run("TestQuotasAndLimits", func(t *testing.T) {
+		clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("on-demand").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("5").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("spot").
+					ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").BorrowingLimit("50").Append().
+					ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").LendingLimit("100").Append().
+					Obj(),
+			).
+			ResourceGroup(
+				*utiltesting.MakeFlavorQuotas("gpu1").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").BorrowingLimit("5").LendingLimit("10").Append().
+					Obj(),
+				*utiltesting.MakeFlavorQuotas("gpu2").
+					ResourceQuotaWrapper("example.com/gpu").NominalQuota("3").BorrowingLimit("1").LendingLimit("4").Append().
+					Obj(),
+			).
+			ClusterQueue
+
+		if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+			t.Fatal("Failed to add CQ to cache", err)
+		}
+
+		snapshot := cache.Snapshot()
+		clusterQueueSnapshot := snapshot.ClusterQueues["test-clusterqueue"]
+
+		actualCapacitiesAndLimits := make(map[resources.FlavorResource]*ResourceQuota, 0)
```

  ```suggestion
  		gotCapacitiesAndLimits := make(map[resources.FlavorResource]*ResourceQuota, 0)
  ```
  Excuse me a bit of annoyance, it is a non-blocking nit, and we are not consistent about it, but go generally tries to name variables short (see at the bottom [here](https://go.dev/wiki/CodeReviewComments)). 
  
  So, want / got are more popular than expected / actual. This naming is also suggested in the style guides: https://google.github.io/styleguide/go/best-practices.html, or https://www.arp242.net/go-testing-style.html

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-10T18:34:31Z

- **pkg/cache/snapshot_test.go:1253** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T18:30:35Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
+	cache := New(utiltesting.NewFakeClient())
+
+	clusterQueue := utiltesting.MakeClusterQueue("test-clusterqueue").
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("on-demand").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("spot").
+				ResourceQuotaWrapper(corev1.ResourceCPU).NominalQuota("0").Append().
+				ResourceQuotaWrapper(corev1.ResourceMemory).NominalQuota("0").Append().
+				Obj(),
+		).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("gpu1").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+			*utiltesting.MakeFlavorQuotas("gpu2").
+				ResourceQuotaWrapper("example.com/gpu").NominalQuota("0").Append().
+				Obj(),
+		).
+		ClusterQueue
+	if err := cache.AddClusterQueue(context.Background(), &clusterQueue); err != nil {
+		t.Fatal("Failed to add CQ to cache", err)
+	}
+
+	// Flavors (and Label) used by ResourceGroup1
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("on-demand").
+		NodeLabel("instance", "demand").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("spot").
+		NodeLabel("instance", "spot").
+		NodeLabel("cost", "cheap").
+		Obj())
+
+	// Flavors (and Label) used by ResourceGroup2
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu1").
+		NodeLabel("accelerator_type", "type1").
+		Obj())
+	cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("gpu2").
+		NodeLabel("instance", "demand").
+		NodeLabel("cost", "dear").
+		Obj())
+
+	t.Run("TestLabelKeys", func(t *testing.T) {
```

  ```suggestion
  	t.Run("LabelKeys", func(t *testing.T) {
  ```

- **pkg/cache/snapshot_test.go:1208** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-10T18:34:29Z
```diff
@@ -1331,3 +1204,166 @@ func TestSnapshotAddRemoveWorkloadWithLendingLimit(t *testing.T) {
 		})
 	}
 }
+
+func TestResourceGroups(t *testing.T) {
```

  I disagree with these changes... We are replacing a simple diff with more test code.
  
  It's ok if the test code is repetitive (https://stackoverflow.com/questions/6453235/what-does-damp-not-dry-mean-when-talking-about-unit-tests)
  
  If ResourceGroups are not relevant for a certain test, we can simply remove them from the input data.
