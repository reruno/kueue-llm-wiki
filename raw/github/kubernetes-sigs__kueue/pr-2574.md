# PR #2574: Start JobFramework controller and webhook on new CRDs availability

**Summary**: Start JobFramework controller and webhook on new CRDs availability

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2574

**Last updated**: 2024-09-10T09:40:16Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ChristianZaccaria](https://github.com/ChristianZaccaria)
- **Branch**: `restmapper-crd-start` → `main`
- **Created**: 2024-07-11T08:33:48Z
- **Updated**: 2024-09-10T09:40:16Z
- **Merged**: 2024-08-23T19:44:20Z
- **Closed**: 2024-08-23T19:44:20Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@varshaprasad96](https://github.com/varshaprasad96)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@KPostOffice](https://github.com/KPostOffice), [@kerthcet](https://github.com/kerthcet), [@varshaprasad96](https://github.com/varshaprasad96)
- **Changed files**: 5   **+160 / -30**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add function to wait for API availability using the RESTMapper in controller manager.

This PR checks for availability of JobFramework integrations in the Kubernetes cluster before executing the action().

The function utilises the RESTMapper from the controller-runtime manager to verify the existence of the CRD by its GroupVersionKind (GVK). If one of the CRDs become available, the respective controller and webhook will start.

The user/dev will no longer be required to restart the Kueue pod to allow for integration on new CRDs available.

No restarts on the Kueue pod is performed.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2414 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Detect and enable support for job CRDs installed after Kueue starts.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-11T08:33:58Z

Hi @ChristianZaccaria. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-11T08:34:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 86e480d4b66e838635cc97e1734d29de6b32af66 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66c8e26c405f47000744dd52 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2574--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2574--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI1NzQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.zNEqxvLzyKP7ZDJBaR0iDUt35H4MqjnL1u8FP5t58EI)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-11T08:35:28Z

cc: @trasc thank you for your previous input on this.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-11T13:15:39Z

/ok-to-test

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-12T16:02:28Z

Rebased

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-15T15:57:17Z

Hi @trasc, wondering what are the next steps for this PR to be successful and merged? Thank you.

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-17T15:34:10Z

Hi @alculquicondor, thanks for your previous input on issue https://github.com/kubernetes-sigs/kueue/issues/2414. This PR resolves the issue and includes a test case. Manual tests have been made and work as expected. Let me know if there's anything else I could do for this PR to be successfully merged. Thank you

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-17T18:39:06Z

I'm unfortunately overwhelmed with reviews. I'll let @trasc take a pass, but note that he's currently on vacation.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T21:35:08Z

/release-note-edit

```release-note
Added logic to wait until the job framework API is available.
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T12:58:20Z

/release-note-edit
```
Detect and enable support for job CRDs installed after Kueue starts.
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-18T12:58:22Z

@alculquicondor: /release-note-edit must be used with a release note block.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2574#issuecomment-2236432249):

>/release-note-edit
>```
>Detect and enable support for job CRDs installed after Kueue starts.
>```


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T12:58:35Z

/release-note-edit
```release-note
Detect and enable support for job CRDs installed after Kueue starts.
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-19T11:44:49Z

LGTM label has been added.  <details>Git tree hash: 2bc2aaf2c5ea6f9b5a25fa7d2e8cf264e3c82efc</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-25T06:50:03Z

It seems that this PR is working in progress based on https://github.com/kubernetes-sigs/kueue/pull/2574#discussion_r1687575212 and https://github.com/kubernetes-sigs/kueue/pull/2574#discussion_r1687586765. Once these discussion results are applied to the code, I will come back here.

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-08T11:02:15Z

Hi @mimowo , I just got back to this recently. I've created 2 PRs in my fork repository with some alternatives. Each PR has a description with additional details:
- [Using Mutex on enabledIntegrations as there is a real case of race conditions](https://github.com/ChristianZaccaria/kueue/pull/6) - I've added the different use cases in which a race could happen.
- [Alternative without using Mutex: cancelling the context to restart the Kueue pod on newly available Frameworks.](https://github.com/ChristianZaccaria/kueue/pull/8)

Please let me know which alternative would be best, and your thoughts. Thank you !

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-08T11:31:05Z

Hi @ChristianZaccaria, very nice to see you are back on this. 

Thanks for the investigation. IIUC the non-mutex solution requires Kueue pod restart, I think this is a significant con. So, I'm leaning towards the mutex-based solution. 

I don't expect the mutex to have any significant performance impact, but we may consider R/W locks to allow multiple readers for the `getJobTypeForOwner` function.

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-12T07:35:54Z

@mimowo thanks! :) . I understand, the 'restarting Kueue pod' would interfere with any running processes.
> we may consider R/W locks to allow multiple readers for the getJobTypeForOwner function.

Completely agree on this. will make changes now, thanks!

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-12T10:16:13Z

Correct me if I'm wrong, [`getEnabledIntegrations()`](https://github.com/kubernetes-sigs/kueue/pull/2574/files#diff-fde4763cb4c71b0994c0012fc717450c2c2d90a182e3fa30f3d5a21417b42686R152-R154) currently uses R Locks allowing for multiple reads for the `getJobTypeForOwner()`.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-12T10:23:34Z

Yes, it is ok. I somehow missed that, maybe I confused it with the other function.

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-12T13:13:28Z

Perfect, please let me know on the next steps for this PR to be successfully merged. I'll create a follow-up issue for further discussion on [this thread](https://github.com/kubernetes-sigs/kueue/pull/2574#discussion_r1687586765).

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-13T06:29:58Z

> Perfect, please let me know on the next steps for this PR to be successfully merged.

Sgtm, we can track both things mentioned in my previous comment in follow up issues. 
/cc @tenzen-y 
Would you also like to give it a pass?

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-13T10:22:27Z

> Just some nits. Ideally, we would have these covered before release:
> 
> * integration or e2e test
> * only start `waitForAPI` with exponential backoff once the CRD is registered (wait either by controller or watcher on CRD type)

Thank you, I'll track these items in the follow-up issue.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-13T10:35:39Z

> Thank you, I'll track these items in the follow-up issue.

Please also address this nit: https://github.com/kubernetes-sigs/kueue/pull/2574/files#r1687576889.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-13T11:02:24Z

I would like to consider this PR for cherry-picking as a fix. WDYT @tenzen-y @alculquicondor ? It does not entail API changes and I've seen users hitting this.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-13T12:52:52Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-13T12:52:59Z

LGTM label has been added.  <details>Git tree hash: c207059aeb489d1a089ef6ce69b01b1f456eb9b2</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-15T07:11:05Z

> > Perfect, please let me know on the next steps for this PR to be successfully merged.
> 
> Sgtm, we can track both things mentioned in my previous comment in follow up issues. /cc @tenzen-y Would you also like to give it a pass?

I came back here.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T17:49:42Z

That's a good point.

I don't recall at which point we switched from looking at CRDs to querying the RESTMapper. But certainly using a reconciler has the potential of reducing the registration time to zero. And we could even remove integrations as CRDs are removed.

I don't want to hold this PR for much longer, since it's not extremely complex, but you are right, a reconciler would have similar complexity with a few extra benefits.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T17:54:04Z

> I don't want to hold this PR for much longer, since it's not extremely complex, but you are right, a reconciler would have similar complexity with a few extra benefits.

I agree with you. So I recommended this one: 

> Even if we decided to switch to reconciler mechanism, I think we can work on the switching at the follow-up PRs.

So, let's track and investigate the possibility of the reconciler pattern for this mechanism by another issue.

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-23T19:26:49Z

Rebased

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-23T19:37:52Z

LGTM label has been added.  <details>Git tree hash: a08aaa26c555df954eb00e2fbe84214100690f0a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-23T19:37:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2574#pullrequestreview-2257980713" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2574#" title="Author self-approved">ChristianZaccaria</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2574#pullrequestreview-2177422079" title="Approved">Ygnas</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [alculquicondor]
- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-04T17:22:08Z

@tenzen-y @alculquicondor as we are about to prepare the 0.8.1 release - how do you find cherry-picking this PR?

I feel this issue is on a boundary between a feature and a bug. It could be considered a feature, because we didn't plan for it up front. OTOH, I participated in some debugging sessions for users, and I think the users' perception was always that this is a bug.

Finally, the change does not introduce any API changes, so I would suggest considering it for cherry-picking.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-04T18:34:58Z

> OTOH, I participated in some debugging sessions for users, and I think the users' perception was always that this is a bug.

Thank you for sharing this experience. In that case, I agree with cherry-picking.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-05T08:44:57Z

/cherry-pick release-0.8

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-09-05T08:45:33Z

@mimowo: #2574 failed to apply on top of branch "release-0.8":
```
Applying: Start Controller and Webhook on new CRDs availability
Applying: Log errors from JobFramework controller and webhook
Applying: Add test case for delayed JobFramework API becoming available
Applying: Wait for API integration to be enabled
Applying: Implement synchronization for safe concurrent access
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/integrationmanager.go
M	pkg/controller/jobframework/setup.go
M	pkg/controller/jobframework/setup_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobframework/setup_test.go
Auto-merging pkg/controller/jobframework/setup.go
Auto-merging pkg/controller/jobframework/integrationmanager.go
Applying: Implement exponential backoff for waitForAPI() and add function type to streamline REST mapping checks
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/setup.go
M	pkg/controller/jobframework/setup_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobframework/setup_test.go
Auto-merging pkg/controller/jobframework/setup.go
Applying: Add Job Framework API to mapper directly
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/integrationmanager.go
M	pkg/controller/jobframework/integrationmanager_test.go
M	pkg/controller/jobframework/setup.go
M	pkg/controller/jobframework/setup_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobframework/setup_test.go
Auto-merging pkg/controller/jobframework/setup.go
Auto-merging pkg/controller/jobframework/integrationmanager_test.go
CONFLICT (content): Merge conflict in pkg/controller/jobframework/integrationmanager_test.go
Auto-merging pkg/controller/jobframework/integrationmanager.go
CONFLICT (content): Merge conflict in pkg/controller/jobframework/integrationmanager.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0007 Add Job Framework API to mapper directly
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2574#issuecomment-2330953057):

>/cherry-pick release-0.8


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-05T08:46:36Z

There is a conflict in `integrationmanager`. @ChristianZaccaria would you like to prepare the cherry-pick manually? You can use the https://github.com/kubernetes-sigs/kueue/blob/main/hack/cherry_pick_pull.sh script

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-09-05T10:29:40Z

Thank you, on it now.

### Comment by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-09-05T12:43:46Z

For reference, here is the generated PR: https://github.com/kubernetes-sigs/kueue/pull/2991 Thanks!

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-10T09:40:16Z

To x-reference the follow up issues:
- https://github.com/kubernetes-sigs/kueue/issues/2837
- https://github.com/kubernetes-sigs/kueue/issues/3015

## Reviews

### Review by [@Ygnas](https://github.com/Ygnas) (commented) — 2024-07-11T13:17:54Z

- **pkg/controller/jobframework/setup.go:114** by [@Ygnas](https://github.com/Ygnas) — 2024-07-11T11:47:52Z
```diff
@@ -94,6 +89,43 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 	})
 }
 
+func setupControllerAndWebhook(mgr ctrl.Manager, gvk schema.GroupVersionKind, fwkNamePrefix string, cb IntegrationCallbacks, opts ...Option) error {
+    if err := cb.NewReconciler(
+        mgr.GetClient(),
+        mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", gvk.Kind, "managerName")), // Ensure managerName is defined or fetched
+        opts...,
+    ).SetupWithManager(mgr); err != nil {
+        return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+    }
+
+    if err := cb.SetupWebhook(mgr, opts...); err != nil {
+        return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+    }
+
+    return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	getRestMapping := func() (*meta.RESTMapping, error) {
+		return mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+	}
+	var err error
+	for {
+		_, err = getRestMapping()
```

  Is it worth doing this and not minimising `var err error` scope?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-11T13:56:37Z

- **pkg/controller/jobframework/setup.go:120** by [@trasc](https://github.com/trasc) — 2024-07-11T13:45:44Z
```diff
@@ -94,6 +89,43 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 	})
 }
 
+func setupControllerAndWebhook(mgr ctrl.Manager, gvk schema.GroupVersionKind, fwkNamePrefix string, cb IntegrationCallbacks, opts ...Option) error {
+    if err := cb.NewReconciler(
+        mgr.GetClient(),
+        mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", gvk.Kind, "managerName")), // Ensure managerName is defined or fetched
+        opts...,
+    ).SetupWithManager(mgr); err != nil {
+        return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+    }
+
+    if err := cb.SetupWebhook(mgr, opts...); err != nil {
+        return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+    }
+
+    return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	getRestMapping := func() (*meta.RESTMapping, error) {
+		return mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+	}
+	var err error
+	for {
+		_, err = getRestMapping()
+		if err != nil {
+			select {
+			case <-ctx.Done():
+				log.Info(fmt.Sprint("Context cancelled!", "gvk", gvk))
+				return
+			case <-time.After(time.Second * 5):
```

  let's make this a package constant. When you do that add a comment describing how it is used. 
  
  For me the value looks small, so maybe let't bring it up to 20s at least.

- **pkg/controller/jobframework/setup.go:154** by [@trasc](https://github.com/trasc) — 2024-07-11T13:46:54Z
```diff
@@ -94,6 +89,43 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 	})
 }
 
+func setupControllerAndWebhook(mgr ctrl.Manager, gvk schema.GroupVersionKind, fwkNamePrefix string, cb IntegrationCallbacks, opts ...Option) error {
+    if err := cb.NewReconciler(
+        mgr.GetClient(),
+        mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", gvk.Kind, "managerName")), // Ensure managerName is defined or fetched
+        opts...,
+    ).SetupWithManager(mgr); err != nil {
+        return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+    }
+
+    if err := cb.SetupWebhook(mgr, opts...); err != nil {
+        return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+    }
+
+    return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	getRestMapping := func() (*meta.RESTMapping, error) {
+		return mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+	}
```

  I don't think we need this , It's a one line function used once.

- **pkg/controller/jobframework/setup.go:114** by [@trasc](https://github.com/trasc) — 2024-07-11T13:47:27Z
```diff
@@ -94,6 +89,43 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 	})
 }
 
+func setupControllerAndWebhook(mgr ctrl.Manager, gvk schema.GroupVersionKind, fwkNamePrefix string, cb IntegrationCallbacks, opts ...Option) error {
+    if err := cb.NewReconciler(
+        mgr.GetClient(),
+        mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", gvk.Kind, "managerName")), // Ensure managerName is defined or fetched
+        opts...,
+    ).SetupWithManager(mgr); err != nil {
+        return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+    }
+
+    if err := cb.SetupWebhook(mgr, opts...); err != nil {
+        return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+    }
+
+    return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	getRestMapping := func() (*meta.RESTMapping, error) {
+		return mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+	}
+	var err error
+	for {
+		_, err = getRestMapping()
```

  ```suggestion
  	for {
  		_, err := ......
  ```

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2024-07-11T16:39:25Z

Nice, I tested this out on a cluster and it worked as expected for me

- **pkg/controller/jobframework/setup.go:95** by [@KPostOffice](https://github.com/KPostOffice) — 2024-07-11T16:30:24Z
```diff
@@ -94,6 +89,43 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 	})
 }
 
+func setupControllerAndWebhook(mgr ctrl.Manager, gvk schema.GroupVersionKind, fwkNamePrefix string, cb IntegrationCallbacks, opts ...Option) error {
+    if err := cb.NewReconciler(
+        mgr.GetClient(),
+        mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", gvk.Kind, "managerName")), // Ensure managerName is defined or fetched
```

  I think "managerName" should still come from options
  ```suggestion
          options := ProcessOptions(opts...)
          mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", gvk.Kind, options.ManagerName)), // Ensure managerName is defined or fetched
  ```

- **pkg/controller/jobframework/setup.go:79** by [@KPostOffice](https://github.com/KPostOffice) — 2024-07-11T16:38:23Z
```diff
@@ -67,24 +69,17 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(context.Background(), mgr, logger, gvk, func() {
+					log.Info(fmt.Sprintf("API now available, starting controller and webhook for %v", gvk))
+					setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
```

  One question here is what happens if a controller/webhook errors on startup? Should kueue kueue restart or should it log the error and continue on?

- **pkg/controller/jobframework/setup.go:82** by [@KPostOffice](https://github.com/KPostOffice) — 2024-07-11T16:38:58Z
```diff
@@ -67,24 +69,17 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(context.Background(), mgr, logger, gvk, func() {
+					log.Info(fmt.Sprintf("API now available, starting controller and webhook for %v", gvk))
+					setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
+				})
 			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
-				}
-				logger.Info("Set up controller and webhook for job framework")
-				return nil
+				setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
```

  Here as well, the previous behavior was to return an error

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-12T10:25:21Z

- **pkg/controller/jobframework/setup.go:79** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-12T10:25:21Z
```diff
@@ -67,24 +69,17 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(context.Background(), mgr, logger, gvk, func() {
+					log.Info(fmt.Sprintf("API now available, starting controller and webhook for %v", gvk))
+					setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
```

  Originally, the error would be logged and we would continue on. - I wouldn't like to restart Kueue based on controller/webhook startup errors as in the event of consistent/repetitive errors on one framework would cause Kueue to never start. With that said, the waitForAPI() takes care of ensuring the API exists before starting the controller/webhook.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-12T10:27:33Z

- **pkg/controller/jobframework/setup.go:82** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-12T10:27:33Z
```diff
@@ -67,24 +69,17 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(context.Background(), mgr, logger, gvk, func() {
+					log.Info(fmt.Sprintf("API now available, starting controller and webhook for %v", gvk))
+					setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
+				})
 			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
-				}
-				logger.Info("Set up controller and webhook for job framework")
-				return nil
+				setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
```

  Correct me if I'm wrong, the current implementation will still return an error if encountered on startup of the controller/webhook.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-12T10:34:08Z

- **pkg/controller/jobframework/setup.go:82** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-12T10:34:08Z
```diff
@@ -67,24 +69,17 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(context.Background(), mgr, logger, gvk, func() {
+					log.Info(fmt.Sprintf("API now available, starting controller and webhook for %v", gvk))
+					setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
+				})
 			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
-				}
-				logger.Info("Set up controller and webhook for job framework")
-				return nil
+				setupControllerAndWebhook(mgr, gvk, fwkNamePrefix, cb, opts...)
```

  Oh I understand now, you're right! - Will adjust. Thanks

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-12T10:49:48Z

- **pkg/controller/jobframework/setup.go:120** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-12T10:49:48Z
```diff
@@ -94,6 +89,43 @@ func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
 	})
 }
 
+func setupControllerAndWebhook(mgr ctrl.Manager, gvk schema.GroupVersionKind, fwkNamePrefix string, cb IntegrationCallbacks, opts ...Option) error {
+    if err := cb.NewReconciler(
+        mgr.GetClient(),
+        mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", gvk.Kind, "managerName")), // Ensure managerName is defined or fetched
+        opts...,
+    ).SetupWithManager(mgr); err != nil {
+        return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+    }
+
+    if err := cb.SetupWebhook(mgr, opts...); err != nil {
+        return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+    }
+
+    return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	getRestMapping := func() (*meta.RESTMapping, error) {
+		return mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+	}
+	var err error
+	for {
+		_, err = getRestMapping()
+		if err != nil {
+			select {
+			case <-ctx.Done():
+				log.Info(fmt.Sprint("Context cancelled!", "gvk", gvk))
+				return
+			case <-time.After(time.Second * 5):
```

  You're right, the `5s` is too short, it was mainly for testing rapidly. Great idea on the package constant, will do, thank you!

### Review by [@Ygnas](https://github.com/Ygnas) (approved) — 2024-07-15T11:03:53Z

Works as expected.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-07-15T12:36:37Z

- **pkg/controller/jobframework/setup.go:40** by [@kannon92](https://github.com/kannon92) — 2024-07-15T12:36:37Z
```diff
@@ -35,6 +37,9 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+// APIRetryInterval is the duration to wait before retrying the API request.
```

  Should this be added as a config option for Kueue?

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-15T12:52:35Z

- **pkg/controller/jobframework/setup.go:40** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-15T12:52:35Z
```diff
@@ -35,6 +37,9 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+// APIRetryInterval is the duration to wait before retrying the API request.
```

  Hi Kevin, good question. I might be wrong, but I don't see great benefit on allowing this value to be configurable through Kueue config options.
  
  Dev/Users will want the controller and webhook to start in a "reasonable" amount of time after the respective CRD becomes available.  - I'm not saying 20s is the right amount, it's something we could discuss, but I think making this value configurable for the dev/user to update to 1s or 5min could lead to inconsistent behaviour and harder to troubleshoot.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-07-16T13:57:10Z

- **pkg/controller/jobframework/setup.go:40** by [@kannon92](https://github.com/kannon92) — 2024-07-16T13:57:09Z
```diff
@@ -35,6 +37,9 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+// APIRetryInterval is the duration to wait before retrying the API request.
```

  I’m not sure either. I just usually see constants and wonder if someone would want to tune this..
  
  @tenzen-y wdyt?

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-16T14:24:27Z

- **pkg/controller/jobframework/setup.go:40** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-16T14:24:26Z
```diff
@@ -35,6 +37,9 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+// APIRetryInterval is the duration to wait before retrying the API request.
```

  I'm actually going to change this to a `var` as I wish to add a test case for this. - Will have a new commit promptly.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-17T23:13:01Z

- **pkg/controller/jobframework/setup.go:44** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T21:53:28Z
```diff
@@ -35,6 +38,13 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+var (
+	// APIRetryInterval is the duration to wait before retrying the API request.
+	APIRetryInterval = 20 * time.Second
+	// APIRetryMutex is the mutex used to safely access the APIRetryInterval.
+	APIRetryMutex sync.Mutex
+)
```

  Maybe like this?
  
  ```go
  func SetupControllers(mgr ctrl.Manager, log logr.Logger, retryInterval time.Duration, opts ...Option) error
  ```
  
  In this case we don't need to use mutex and getter-setter.

- **pkg/controller/jobframework/setup.go:125** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T22:11:43Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
```

  Can we avoid this lock? Or maybe create another mutex? It is just for test, correct?

- **pkg/controller/jobframework/integrationmanager.go:80** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T22:44:58Z
```diff
@@ -76,6 +77,7 @@ type integrationManager struct {
 	integrations         map[string]IntegrationCallbacks
 	enabledIntegrations  set.Set[string]
 	externalIntegrations map[string]runtime.Object
+	mu                   sync.Mutex
```

  ```suggestion
  	mu                   sync.RWMutex
  ```

- **pkg/controller/jobframework/integrationmanager.go:171** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T22:46:03Z
```diff
@@ -148,6 +150,8 @@ func (m *integrationManager) getExternal(kindArg string) (runtime.Object, bool)
 }
 
 func (m *integrationManager) enableIntegration(name string) {
+	m.mu.Lock()
+	defer m.mu.Unlock()
```

  Also need to lock `enabledIntegrations` for read in another places.

- **pkg/controller/jobframework/setup.go:89** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T22:47:35Z
```diff
@@ -71,25 +81,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go m.waitForAPI(context.Background(), mgr, logger, gvk, func() {
```

  Can we use `ctx` from main function?

- **pkg/controller/jobframework/setup_test.go:177** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T22:58:59Z
```diff
@@ -140,7 +161,26 @@ func TestSetupControllers(t *testing.T) {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if name == "mapper doesn't have ray.io/raycluster when Controllers have been setup, but eventually does" {
+				rayGVK := schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayCluster"}
+				manager.mu.Lock()
+				mgr.GetRESTMapper().(*apimeta.DefaultRESTMapper).Add(rayGVK, apimeta.RESTScopeNamespace)
+				manager.mu.Unlock()
+				// Wait for setup to complete
+				for {
+					manager.mu.Lock()
+					_, ok := manager.enabledIntegrations["ray.io/raycluster"]
+					manager.mu.Unlock()
+					if ok {
+						break // Exit loop if RayCluster is enabled
+					}
+					time.Sleep(10 * time.Millisecond)
```

  Can we move it to test case as a function? And here just execute it if exists.

- **pkg/controller/jobframework/setup.go:131** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-17T23:03:24Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
+		_, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+		m.mu.Unlock()
+		if err != nil {
+			select {
+			case <-ctx.Done():
+				log.Info(fmt.Sprint("Context cancelled!", "gvk", gvk))
```

  I don't think this log will be useful.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-18T06:32:03Z

- **pkg/controller/jobframework/setup.go:136** by [@mimowo](https://github.com/mimowo) — 2024-07-18T06:32:03Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
+		_, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+		m.mu.Unlock()
+		if err != nil {
```

  Make sure the error is logged. Can you paste here an example log line?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-18T06:34:44Z

- **pkg/controller/jobframework/setup.go:45** by [@mimowo](https://github.com/mimowo) — 2024-07-18T06:34:25Z
```diff
@@ -35,6 +38,13 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+var (
+	// APIRetryInterval is the duration to wait before retrying the API request.
+	APIRetryInterval = 20 * time.Second
+	// APIRetryMutex is the mutex used to safely access the APIRetryInterval.
+	APIRetryMutex sync.Mutex
```

  This mutex does not seem needed as the interval cannot be changed at runtime, just in tests.

- **pkg/controller/jobframework/setup.go:43** by [@mimowo](https://github.com/mimowo) — 2024-07-18T06:34:37Z
```diff
@@ -35,6 +38,13 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+var (
+	// APIRetryInterval is the duration to wait before retrying the API request.
+	APIRetryInterval = 20 * time.Second
```

  Not need to export

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-18T14:48:29Z

- **pkg/controller/jobframework/setup.go:44** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-18T14:48:29Z
```diff
@@ -35,6 +38,13 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+var (
+	// APIRetryInterval is the duration to wait before retrying the API request.
+	APIRetryInterval = 20 * time.Second
+	// APIRetryMutex is the mutex used to safely access the APIRetryInterval.
+	APIRetryMutex sync.Mutex
+)
```

  That is excellent! - You're right, thanks.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-18T14:48:33Z

- **pkg/controller/jobframework/setup.go:125** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-18T14:48:33Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
```

  I've been finding this lock hard to avoid as a `write` is performed in the tests [[here] ](https://github.com/kubernetes-sigs/kueue/pull/2574/files#diff-0e0e2357162d6c7d6172e1f1e2f0fed3d27946005ee221718652d972f6a61879R167)
  
  However, I enhanced this by using `RLock()` instead which can be held by an arbitrary number of readers.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-18T14:48:37Z

- **pkg/controller/jobframework/integrationmanager.go:80** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-18T14:48:37Z
```diff
@@ -76,6 +77,7 @@ type integrationManager struct {
 	integrations         map[string]IntegrationCallbacks
 	enabledIntegrations  set.Set[string]
 	externalIntegrations map[string]runtime.Object
+	mu                   sync.Mutex
```

  I just had an interesting read on RWMutex vs Mutex. - That is a really nice change, thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-18T15:13:55Z

- **pkg/controller/jobframework/setup.go:125** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T15:13:55Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
```

  Actually, I don't like that we are using the same mutex for `mgr.GetRESTMapper()` and `integrationManager`. It's not related for integrationManager and need just for test. But we will unnecessary lock this code on update `integrationManager`. In my opinion it's better to create another one just for test.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-18T15:31:37Z

- **pkg/controller/jobframework/setup_test.go:170** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T15:25:56Z
```diff
@@ -135,18 +156,42 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(context.Background(), mgr, logger, time.Millisecond * 20, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if slices.Contains(tc.wantEnabledIntegrations, "ray.io/raycluster") {
+				testDelayedIntegration(mgr, manager)
+			}
+
+			manager.mu.RLock()
+			diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList())
+			manager.mu.RUnlock()
```

  I think it's better to create a method `getEnabledIntegrations()` that will lock `enabledIntegrations` and return copy of value. You can use it where it's need.

- **pkg/controller/jobframework/integrationmanager.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T15:29:09Z
  I think you missed to lock `enabledIntegrations` on `getJobTypeForOwner(ownerRef *metav1.OwnerReference)` and EnableIntegrationsForTest(tb testing.TB, names ...string).

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-18T15:33:42Z

- **pkg/controller/jobframework/setup.go:136** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-18T15:33:42Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
+		_, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+		m.mu.Unlock()
+		if err != nil {
```

  I hesitated on adding a log here as it would log for each API that is not yet available, and will retry after the specified interval. - This could cause log flooding. The error serves here as an indication to retry.
  
  I've refactored the `waitForAPI()` to reflect the expected behaviour explicitly. - Let me know what you think, thank you.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-18T15:36:42Z

- **pkg/controller/jobframework/setup.go:136** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T15:36:42Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
+		_, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+		m.mu.Unlock()
+		if err != nil {
```

  Can we log only unexpected errors?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-18T15:52:39Z

- **pkg/controller/jobframework/setup_test.go:177** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T15:52:39Z
```diff
@@ -135,18 +156,42 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(context.Background(), mgr, logger, time.Millisecond*20, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if slices.Contains(tc.wantEnabledIntegrations, "ray.io/raycluster") {
+				testDelayedIntegration(mgr, manager)
+			}
```

  ```suggestion
  			if tc.afterSetup != nil {
  				tc.afterSetup(mgr, manager)
  			}
  ```
  
  Maybe like this? And just move `testDelayedIntegration` to test case struct. It will allow to create another test cases if we will need and be independent of "ray.io/raycluster".

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-18T16:17:41Z

- **pkg/controller/jobframework/integrationmanager.go:1** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-18T16:17:41Z
  I didn't lock any direct calls to `enabledIntegrations` as if I'm not mistaken the function is already locking/unlocking from within the function. I ensured though to add a lock to the `Clone` action being made on `EnableIntegrationsForTest(b testing.TB, names ...string) func()`:
  
  `old := manager.enabledIntegrations.Clone()`
  
  Let me know what you think and if I should lock the other `enableIntegrations` calls too, thanks.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-18T16:26:56Z

- **pkg/controller/jobframework/setup_test.go:177** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-18T16:26:56Z
```diff
@@ -135,18 +156,42 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(context.Background(), mgr, logger, time.Millisecond*20, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if slices.Contains(tc.wantEnabledIntegrations, "ray.io/raycluster") {
+				testDelayedIntegration(mgr, manager)
+			}
```

  That's a great idea! - Thanks

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-18T16:30:46Z

- **pkg/controller/jobframework/integrationmanager.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T16:30:46Z
  Yes, we need to lock all writes and reads of `enabledIntegrations` because it is potential race condition.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-18T16:41:09Z

- **pkg/controller/jobframework/setup_test.go:193** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T16:40:03Z
```diff
@@ -135,18 +158,44 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(context.Background(), mgr, logger, time.Millisecond*20, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if tc.afterSetup != nil {
+				tc.afterSetup(mgr, manager)
+			}
+
+			diff := cmp.Diff(tc.wantEnabledIntegrations, getEnabledIntegrations(manager).SortedList())
+			if len(diff) != 0 {
 				t.Errorf("Unexpected enabled integrations (-want,+got):\n%s", diff)
 			}
 		})
 	}
 }
 
+func testDelayedIntegration(mgr ctrlmgr.Manager, manager *integrationManager) {
+	rayGVK := schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayCluster"}
+	manager.mu.Lock()
+	mgr.GetRESTMapper().(*apimeta.DefaultRESTMapper).Add(rayGVK, apimeta.RESTScopeNamespace)
+	manager.mu.Unlock()
+	// Wait for setup to complete
+	for {
+		_, ok := getEnabledIntegrations(manager)["ray.io/raycluster"]
+		if ok {
+			break // Exit loop if RayCluster is enabled
+		}
+		time.Sleep(10 * time.Millisecond)
+	}
+}
+
+func getEnabledIntegrations(manager *integrationManager) set.Set[string] {
```

  No, I meant a method of `integrationManager`. You can use it inside `integrationManager` too to do not duplicate every time lock/unlock.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-18T16:46:14Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-18T16:42:45Z
```diff
@@ -207,12 +211,16 @@ func EnableIntegration(name string) {
 // Mark the frameworks identified by names and return a revert function.
 func EnableIntegrationsForTest(tb testing.TB, names ...string) func() {
 	tb.Helper()
+	manager.mu.Lock()
 	old := manager.enabledIntegrations.Clone()
+	manager.mu.Unlock()
```

  ```suggestion
  	old := manager.getEnabledIntegrations()
  ```

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-19T10:03:13Z

- **pkg/controller/jobframework/setup.go:125** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-19T10:03:13Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
```

  That makes sense, I've added a new mutex for use with the RESTMapper. Let me know what you think, thank you.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-19T10:03:58Z

- **pkg/controller/jobframework/setup.go:136** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-19T10:03:58Z
```diff
@@ -99,6 +105,40 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func (m *integrationManager) waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	for {
+		m.mu.Lock()
+		_, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+		m.mu.Unlock()
+		if err != nil {
```

  Absolutely, I've added an error log for unexpected errors, such as to log any error that is not a `IsNoMatchError`

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-19T10:05:14Z

- **pkg/controller/jobframework/setup_test.go:193** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-19T10:05:14Z
```diff
@@ -135,18 +158,44 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(context.Background(), mgr, logger, time.Millisecond*20, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if tc.afterSetup != nil {
+				tc.afterSetup(mgr, manager)
+			}
+
+			diff := cmp.Diff(tc.wantEnabledIntegrations, getEnabledIntegrations(manager).SortedList())
+			if len(diff) != 0 {
 				t.Errorf("Unexpected enabled integrations (-want,+got):\n%s", diff)
 			}
 		})
 	}
 }
 
+func testDelayedIntegration(mgr ctrlmgr.Manager, manager *integrationManager) {
+	rayGVK := schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayCluster"}
+	manager.mu.Lock()
+	mgr.GetRESTMapper().(*apimeta.DefaultRESTMapper).Add(rayGVK, apimeta.RESTScopeNamespace)
+	manager.mu.Unlock()
+	// Wait for setup to complete
+	for {
+		_, ok := getEnabledIntegrations(manager)["ray.io/raycluster"]
+		if ok {
+			break // Exit loop if RayCluster is enabled
+		}
+		time.Sleep(10 * time.Millisecond)
+	}
+}
+
+func getEnabledIntegrations(manager *integrationManager) set.Set[string] {
```

  Ohh I understand now, please see latest changes if it meets with expectations. Thanks for the help

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-19T10:27:45Z

Overall LGTM

- **pkg/controller/jobframework/setup.go:40** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-19T10:06:56Z
```diff
@@ -33,6 +36,7 @@ import (
 
 var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+	restMapperMutex          sync.RWMutex
```

  To avoid confusion other members, please, add a comment why we add this mutex (that it is using only for unit test and we need to lock RESTMapper on update).

### Review by [@varshaprasad96](https://github.com/varshaprasad96) (commented) — 2024-07-19T11:44:44Z

Nice work @ChristianZaccaria! The current changes look good, and my suggestions are completely nits (not a blocker at all)!

/lgtm

- **cmd/kueue/main.go:286** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-07-19T08:48:27Z
```diff
@@ -286,7 +287,7 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		jobframework.WithCache(cCache),
 		jobframework.WithQueues(queues),
 	}
-	if err := jobframework.SetupControllers(mgr, setupLog, opts...); err != nil {
+	if err := jobframework.SetupControllers(ctx, mgr, setupLog, time.Second*20, opts...); err != nil {
```

  Nit: Let's define this as a constant on the top, instead of passing it in here directly.

- **pkg/controller/jobframework/setup.go:131** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-07-19T11:43:10Z
```diff
@@ -99,6 +100,50 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, retryInterval time.Duration, action func()) {
+	for {
+		_, err := getRESTMapping(mgr, gvk)
+		if err == nil {
+			action()
+			return
+		}
+		if !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(retryInterval):
```

  Nit in terms of optimisation (can be done in follow up, not a blocker at all) - Given we are re-trying fetching the rest mapper in every cycle, better to use golang's [ticker](https://gobyexample.com/tickers):
  
  ```
  ticker := time.NewTicker(retryInterval)
  defer ticker.Stop()
  ```
  
  Reason for this being - in this case a new timer is created every iteration, and it remains till the garbage collector does its job.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-22T07:13:32Z

- **pkg/controller/jobframework/setup.go:131** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-22T07:13:32Z
```diff
@@ -99,6 +100,50 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, retryInterval time.Duration, action func()) {
+	for {
+		_, err := getRESTMapping(mgr, gvk)
+		if err == nil {
+			action()
+			return
+		}
+		if !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(retryInterval):
```

  That's an excellent idea, thanks!

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-22T08:32:51Z

- **cmd/kueue/main.go:76** by [@trasc](https://github.com/trasc) — 2024-07-22T08:12:25Z
```diff
@@ -72,6 +73,8 @@ import (
 	// +kubebuilder:scaffold:imports
 )
 
+const retryInterval = time.Second * 20
```

  let's make the variable name specific CRD detection.
  
  In the context of kueue's main `retryInterval` can be confusing.
  
  However, making the retry an option with a default defined in the jobframework might be better.

- **pkg/controller/jobframework/setup_test.go:42** by [@trasc](https://github.com/trasc) — 2024-07-22T08:16:15Z
```diff
@@ -38,7 +39,7 @@ import (
 	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
-	"sigs.k8s.io/kueue/pkg/util/slices"
+	kslices "sigs.k8s.io/kueue/pkg/util/slices"
```

  I don'y think this change is necessary.

- **pkg/controller/jobframework/setup.go:52** by [@trasc](https://github.com/trasc) — 2024-07-22T08:30:28Z
```diff
@@ -44,11 +49,11 @@ var (
 // this function needs to be called after the certs get ready because the controllers won't work
 // until the webhooks are operating, and the webhook won't work until the
 // certs are all in place.
-func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
-	return manager.setupControllers(mgr, log, opts...)
+func SetupControllers(ctx context.Context, mgr ctrl.Manager, log logr.Logger, retryInterval time.Duration, opts ...Option) error {
```

  How about making `retryInterval` an option, then we can setup a default in https://github.com/kubernetes-sigs/kueue/blob/86691f1265974b9b58634773c928804952787bdb/pkg/controller/jobframework/reconciler.go#L187 , and avoid doing any changes to main until necessary (we have a config key for it).

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-22T17:54:37Z

- **pkg/controller/jobframework/setup.go:40** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-22T17:54:37Z
```diff
@@ -35,6 +37,9 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+// APIRetryInterval is the duration to wait before retrying the API request.
```

  @kannon92 I have added `IntegrationRetryInterval` as a config option for Kueue. This option can now be tuned through the configmap. Thanks for the great suggestion.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-07-22T18:08:16Z

- **pkg/controller/jobframework/setup.go:40** by [@kannon92](https://github.com/kannon92) — 2024-07-22T18:08:16Z
```diff
@@ -35,6 +37,9 @@ var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
 )
 
+// APIRetryInterval is the duration to wait before retrying the API request.
```

  Looks good!

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-23T05:34:43Z

- **apis/config/v1beta1/configuration_types.go:322** by [@trasc](https://github.com/trasc) — 2024-07-23T05:34:43Z
```diff
@@ -319,6 +319,10 @@ type Integrations struct {
 	//  - "kubeflow.org/xgboostjob"
 	//  - "pod"
 	Frameworks []string `json:"frameworks,omitempty"`
+	// IntegrationRetryInterval refers to the duration to wait before retrying the API request.
```

  This needs to be more specific

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-23T05:42:01Z

- **pkg/controller/jobframework/setup.go:52** by [@trasc](https://github.com/trasc) — 2024-07-23T05:42:01Z
```diff
@@ -44,11 +49,11 @@ var (
 // this function needs to be called after the certs get ready because the controllers won't work
 // until the webhooks are operating, and the webhook won't work until the
 // certs are all in place.
-func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
-	return manager.setupControllers(mgr, log, opts...)
+func SetupControllers(ctx context.Context, mgr ctrl.Manager, log logr.Logger, retryInterval time.Duration, opts ...Option) error {
```

  Please set up a default value as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-23T07:48:47Z

Generally lgtm, but I have an ask about extending the API surface for this approach, which may not be optimal in the long-run. Maybe we could consider / follow up with a CRD watch.

- **apis/config/v1beta1/configuration_types.go:325** by [@mimowo](https://github.com/mimowo) — 2024-07-23T07:36:10Z
```diff
@@ -319,6 +319,10 @@ type Integrations struct {
 	//  - "kubeflow.org/xgboostjob"
 	//  - "pod"
 	Frameworks []string `json:"frameworks,omitempty"`
+	// IntegrationRetryInterval refers to the duration to wait before retrying the API request.
+	// Defaults to 20 seconds if not set.
+	// +optional
+	IntegrationRetryInterval *metav1.Duration `json:"integrationRetryInterval,omitempty"`
```

  Actually, I'm not sure we want to expose this configuration at the surface of the API. @ChristianZaccaria do you have a use case for changing the value, or we could come up with a satisfying default which we set as a const? I would prefer not to expose it if we don't have a clear need, and we do research about other approaches.
  
  The reason is - I'm not sure this is the optimal approach (see another comment about a CRDs watch), and exposing this at the API level would sort of commit us to it (deprecation of API is hard).

- **pkg/controller/jobframework/integrationmanager.go:163** by [@mimowo](https://github.com/mimowo) — 2024-07-23T07:37:33Z
```diff
@@ -147,7 +149,15 @@ func (m *integrationManager) getExternal(kindArg string) (runtime.Object, bool)
 	return jt, f
 }
 
+func (m *integrationManager) getEnabledIntegrations() set.Set[string] {
```

  ```suggestion
  func (m *integrationManager) enabledIntegrations() set.Set[string] {
  ```

- **pkg/controller/jobframework/setup.go:40** by [@mimowo](https://github.com/mimowo) — 2024-07-23T07:38:12Z
```diff
@@ -33,6 +36,8 @@ import (
 
 var (
 	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+	// restMapperMutex is required for unit tests to lock the RESTMapper on update.
+	restMapperMutex sync.RWMutex
```

  I would prefer not to introduce code just for test. See related comment.

- **pkg/controller/jobframework/setup.go:140** by [@mimowo](https://github.com/mimowo) — 2024-07-23T07:40:27Z
```diff
@@ -99,6 +100,52 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, retryInterval time.Duration, action func()) {
+	ticker := time.NewTicker(retryInterval)
+	defer ticker.Stop()
+	for {
+		_, err := getRESTMapping(mgr, gvk)
+		if err == nil {
+			action()
+			return
+		}
+		if !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-ticker.C:
+			continue
+		}
+	}
+}
+
+func getRESTMapping(mgr ctrl.Manager, gvk schema.GroupVersionKind) (*meta.RESTMapping, error) {
+	restMapperMutex.RLock()
```

  Would the `get` fail occasionally if there is no lock? Note that there is no such locking in production code, so such errors (race conditions) could happen in prod code. Seeing the errors which may be throws is desired, we can retry them in the test code, as the prod code would retry.

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-23T07:45:03Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  I'm ok with this as a quick fix, but I'm wondering if it would be better / feasible to have a watch on the CRDs (an analog of `kubectl get crd -w --output-watch-events`), and install the Controller and webhook when we see the ADDED event for the CRD in question. Unless I'm missing something, this could work without any user-facing API.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-23T14:42:24Z

- **pkg/controller/jobframework/setup.go:52** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-23T14:42:24Z
```diff
@@ -44,11 +49,11 @@ var (
 // this function needs to be called after the certs get ready because the controllers won't work
 // until the webhooks are operating, and the webhook won't work until the
 // certs are all in place.
-func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
-	return manager.setupControllers(mgr, log, opts...)
+func SetupControllers(ctx context.Context, mgr ctrl.Manager, log logr.Logger, retryInterval time.Duration, opts ...Option) error {
```

  The default value has been set [here](https://github.com/kubernetes-sigs/kueue/pull/2574/files#diff-dec60c4ac9a26c19509573fd87068ecb6bb784c0af528d38b48f5dc526eb4241R52). Let me know if this is okay.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-23T15:06:48Z

- **pkg/controller/jobframework/setup.go:84** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-23T15:06:48Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  Previously, that was the original idea. I was going to use [`apiextensions`](https://pkg.go.dev/k8s.io/apiextensions-apiserver/pkg/apis/apiextensions) to watch CRDs directly, however I was suggested to use the restmapper. I suppose it is more straightforward and it's also how we've been checking for their availability. This is from [a draft PR](https://github.com/opendatahub-io/kueue/pull/33/commits/5bec43389f43dc7a71d644d4efa9d6f456f77fc4#diff-9b0c3819f5a2c007ac714ec151937ce08cc2b768527cc28d20580a57f47ca5a7R155) from some time ago (might need more work). A follow-up can be made on this.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-23T15:09:25Z

- **apis/config/v1beta1/configuration_types.go:325** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-23T15:09:25Z
```diff
@@ -319,6 +319,10 @@ type Integrations struct {
 	//  - "kubeflow.org/xgboostjob"
 	//  - "pod"
 	Frameworks []string `json:"frameworks,omitempty"`
+	// IntegrationRetryInterval refers to the duration to wait before retrying the API request.
+	// Defaults to 20 seconds if not set.
+	// +optional
+	IntegrationRetryInterval *metav1.Duration `json:"integrationRetryInterval,omitempty"`
```

  I agree, please see the other comment for details. Should I remove the latest commit and remove this as a config option, and just have minimal changes to include the default value?

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-23T15:12:23Z

- **pkg/controller/jobframework/integrationmanager.go:163** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-23T15:12:23Z
```diff
@@ -147,7 +149,15 @@ func (m *integrationManager) getExternal(kindArg string) (runtime.Object, bool)
 	return jt, f
 }
 
+func (m *integrationManager) getEnabledIntegrations() set.Set[string] {
```

  This might not be possible, as that already exists [here](https://github.com/kubernetes-sigs/kueue/pull/2574/files#diff-fde4763cb4c71b0994c0012fc717450c2c2d90a182e3fa30f3d5a21417b42686L150-L152). WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-23T15:22:27Z

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-23T15:22:27Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  > I was going to use [apiextensions](https://pkg.go.dev/k8s.io/apiextensions-apiserver/pkg/apis/apiextensions) to watch CRDs directly, however I was suggested to use the restmapper.
  
  Thank you for the context. I still think the current approach is suboptimal, whatever is the interval, because it means periodic polling, which takes goroutines and results in spammy logging. 
  
  So, I think we would like to use a watch for CRDs  to guide the RestMapper check. So only once we discover the new CRD, then we start waiting for the rest mapper to be filled. Then, the waiting intervals could be much smaller (and don't need to be configurable).
  
  One way to the watcher would be to have a controller-runtime reconciler for the CRD type, similar to the one for [ResourceFlavor](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/resourceflavor_controller.go). I imagine we would use `WithEventFilter` to only listen for the ADD events. Then trigger reconciler. The Reconcile function would actually call the rest mapper. If succeeds, then done, if fails, then it will retry with exponential backoff, but this is already handled by controller-runtime.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-07-23T15:22:44Z

- **pkg/controller/jobframework/setup.go:140** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-07-23T15:22:44Z
```diff
@@ -99,6 +100,52 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, retryInterval time.Duration, action func()) {
+	ticker := time.NewTicker(retryInterval)
+	defer ticker.Stop()
+	for {
+		_, err := getRESTMapping(mgr, gvk)
+		if err == nil {
+			action()
+			return
+		}
+		if !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-ticker.C:
+			continue
+		}
+	}
+}
+
+func getRESTMapping(mgr ctrl.Manager, gvk schema.GroupVersionKind) (*meta.RESTMapping, error) {
+	restMapperMutex.RLock()
```

  The `get` never failed without the lock. In fact, all tests passed without the `-race` flag. 
  
  This lock is added for the tests to pass with the `-race` flag. In the tests, a [`write` is performed](https://github.com/kubernetes-sigs/kueue/pull/2574/files#diff-0e0e2357162d6c7d6172e1f1e2f0fed3d27946005ee221718652d972f6a61879R185), causing a race condition here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-23T15:23:59Z

- **apis/config/v1beta1/configuration_types.go:325** by [@mimowo](https://github.com/mimowo) — 2024-07-23T15:23:59Z
```diff
@@ -319,6 +319,10 @@ type Integrations struct {
 	//  - "kubeflow.org/xgboostjob"
 	//  - "pod"
 	Frameworks []string `json:"frameworks,omitempty"`
+	// IntegrationRetryInterval refers to the duration to wait before retrying the API request.
+	// Defaults to 20 seconds if not set.
+	// +optional
+	IntegrationRetryInterval *metav1.Duration `json:"integrationRetryInterval,omitempty"`
```

  Please remove it from API (which is largely the last commit). I'm ok to keep it as an iinput option to some function that is used for test purposes to make the tests run fast.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-23T15:25:33Z

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-23T15:25:32Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  Then we could also investigate if we can do the symmetric operation - when the CRD is removed (DELETE event), can we unregister the controller and the webhook.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T09:15:38Z

- **pkg/controller/jobframework/setup.go:140** by [@mimowo](https://github.com/mimowo) — 2024-07-24T09:15:38Z
```diff
@@ -99,6 +100,52 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, retryInterval time.Duration, action func()) {
+	ticker := time.NewTicker(retryInterval)
+	defer ticker.Stop()
+	for {
+		_, err := getRESTMapping(mgr, gvk)
+		if err == nil {
+			action()
+			return
+		}
+		if !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-ticker.C:
+			continue
+		}
+	}
+}
+
+func getRESTMapping(mgr ctrl.Manager, gvk schema.GroupVersionKind) (*meta.RESTMapping, error) {
+	restMapperMutex.RLock()
```

  I see, in that case we could retry the write, but then there is a question if the write would be retried by the production code. I think, ideally we would have an integration test for that. At the unit test level I prefer to retry the write.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-24T11:44:06Z

- **pkg/controller/jobframework/setup.go:52** by [@trasc](https://github.com/trasc) — 2024-07-24T11:44:06Z
```diff
@@ -44,11 +49,11 @@ var (
 // this function needs to be called after the certs get ready because the controllers won't work
 // until the webhooks are operating, and the webhook won't work until the
 // certs are all in place.
-func SetupControllers(mgr ctrl.Manager, log logr.Logger, opts ...Option) error {
-	return manager.setupControllers(mgr, log, opts...)
+func SetupControllers(ctx context.Context, mgr ctrl.Manager, log logr.Logger, retryInterval time.Duration, opts ...Option) error {
```

  It may not be sufficient, if at some point we and up calling `SetupControllers` without the option set , we'll end up running loops `0s` sleep.

### Review by [@varshaprasad96](https://github.com/varshaprasad96) (commented) — 2024-07-24T11:51:22Z

- **pkg/controller/jobframework/setup.go:84** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-07-24T11:51:22Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  @mimowo having a separate controller watch for a CRD, and then send in the event through a channel to trigger the start of the main controller does follow the event driven principle and reduces the delay that comes by polling at the specified interval. But the reason for having it this way, was to introduce as minimum complexity as possible upstream, as this would be an occurrence only once before the start of the main controller. 
  
  > One way to the watcher would be to have a controller-runtime reconciler for the CRD type, similar to the one for [ResourceFlavor](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/resourceflavor_controller.go).
  
  Looking at the code (pl correct me if I'm wrong), each of the watchers specified in the ResourceFlavour, receive events through channels created in their respective reconcilers (AdmissionCheck, LocalClusterQueue, ClusterQueue) - https://github.com/kubernetes-sigs/kueue/blob/58ef31e6ef674db0aa04cb587d41862570ad1d12/pkg/controller/core/clusterqueue_controller.go#L89. But in this case, the list of CRDs is configurable, so having a reconciler for each type would not be scalable. One option here, would be to have a single reconciler, and have a dynamic restMapper (with a dynamic Client) to watch for all the expected CRD types and send in a signal to start the core controller. This is going to be very similar (without the creation of separate reconciler) to @ChristianZaccaria option (1) as mentioned in the issue.
  
  >  when the CRD is removed (DELETE event), can we unregister the controller and the webhook.
  
  This touches the part of dynamically starting/stopping the controllers, which unfortunately is not natively supported by controller-runtime yet (https://github.com/kubernetes-sigs/controller-runtime/pull/2159). There are ways to do workarounds on this - like cancelling the context, stopping the informers (which may not be trivial), registering a stopChn with `manager` to signal a shutdown. That being said - is this the behaviour we would expect? Since the option to provide integrations is provided by the user through config API at the start of the Kueue controller, and iiuc there is no way to signal that the integration is no longer required - I would expect the Kueue controller to fail and error if the CRD is removed, rather than us unregistering the respective controller?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T13:09:09Z

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-24T13:09:08Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  > But the reason for having it this way, was to introduce as minimum complexity as possible upstream, as this would be an occurrence only once before the start of the main controller.
  
  I agree with reducing the complexity, but being uncertain about the long-term solution does not make me comfortable extending the API surface, because likely we will need to deprecate it one day. Having hard-coded 20s for the current solution is fine for me.
  
  > But in this case, the list of CRDs is configurable, so having a reconciler for each type would not be scalable.
  
  I was thinking we could have the just a single reconciler with a single watch for the `CustomResourceDefinition` kind. Then, based on the object, we could read the details of the new CRD installed: https://github.com/kubernetes/kubernetes/blob/ceb58a4dbc671b9d0a2de6d73a1616bc0c299863/staging/src/k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/types.go#L36-L44, and lookup the RESTMapper.
  
  >  I would expect the Kueue controller to fail and error if the CRD is removed, rather than us unregistering the respective controller?
  
  I'm not sure, maybe it should be configurable. Anyway, I think we can park this particular decision for now. 
  
  In summary, I would suggest to research a little more the reconciler based approach, if there are obstacles, we can go with the current one, but I wouldn't extend the API surface for it.

### Review by [@varshaprasad96](https://github.com/varshaprasad96) (commented) — 2024-07-24T14:32:10Z

- **pkg/controller/jobframework/setup.go:84** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-07-24T14:32:10Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  > I agree with reducing the complexity, but being uncertain about the long-term solution does not make me comfortable extending the API surface, because likely we will need to deprecate it one day. Having hard-coded 20s for the current solution is fine for me.
  
  Agreed, seems fair enough to not extend an API surface especially given that it's not going to impact the Kueue functionality largely and is used only during setup. 
  
  > I was thinking we could have the just a single reconciler with a single watch for the CustomResourceDefinition kind. 
  
  I'm a bit skeptical with controller approach because - Controllers are useful when there is a need to manage the state of the resource through continuous reconciliation, in addition to being able to integrate with other controllers (which is the use-case here). The design you had mentioned above with ResourceFlavours complies perfectly with the current Watcher approach, wherein say the `ClusterQueueReconciler` is managing the state of `ClusterQueue` objects apart from being able to send a signal to another controller. In this case, we are not intending to manage the lifecycle of any of the CRDs (there are respective operators for each one of them), nor are we going to manage the CRs created for them throughout the existence of Kueue's core controller. 
  
  Having a separate controller, apart from its benefits - is also going to give us the overhead of caching these CRDs, spinning up informers for them (which currently cannot be stopped as of c-r design) consuming memory and CPU resources, while we are going to use it only during setup. Imo having a controller for this seems to be an overkill. Given we are not going to deal with managing the state of any of the resources - a simple Watch makes more sense, given its going to be lightweight,  where we can just close the channel once we have received the event we want and have started the core controller and also follows the event driven approach rather than polling.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T14:41:45Z

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-24T14:41:45Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  The CPU consumption should not be an issue - it would be mostly dormant. Memory, a little bit yes, but you don't expect to have more than 1000 CRDs, so it would  probably not be a memory bottleneck either 
  
  That being said, the Reconciler indeed might be overkill compared to the simple "watch" approach for `CustomResourceDefinition` . It's just that when using simple watches one need to be careful to reconnect them as they get disconnected by the API server once every 30min or so, and the controller-reconciler already does this for us. Other than that, I'm totally for a simple watch based approach. Whenever we observe the ADDED event, just trigger the waitForAPI function.

### Review by [@varshaprasad96](https://github.com/varshaprasad96) (commented) — 2024-07-24T14:41:57Z

- **pkg/controller/jobframework/setup.go:84** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-07-24T14:41:57Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  @mimowo would you be comfortable with making the `retryInterval` constant in the current design, and having follow up PRs further on the watch based approach as suggested by @ChristianZaccaria in [here](https://github.com/opendatahub-io/kueue/pull/33/commits/5bec43389f43dc7a71d644d4efa9d6f456f77fc4#diff-9b0c3819f5a2c007ac714ec151937ce08cc2b768527cc28d20580a57f47ca5a7R155)?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T14:46:06Z

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-24T14:46:06Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  Yes, I would also like you folks to consider adding an integration test for it on the way.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T14:51:35Z

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-24T14:51:35Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  One more thing worth considering also is an exponential backoff instead of fixed intervals for for calling RESTmapper (say from 1s to 1min). I believe generally the RESTMapper would be updated very fast, but maybe for an overloaded system it makes sense to throttle the attempts. WDYT?

### Review by [@varshaprasad96](https://github.com/varshaprasad96) (commented) — 2024-07-24T15:02:29Z

- **pkg/controller/jobframework/setup.go:84** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-07-24T15:02:29Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  > I believe generally the RESTMapper would be updated very fast, but maybe for an overloaded system it makes sense to throttle the attempts.
  
  Yeah, polling it quickly might indeed overload the apiserver. My only concern here would be the responsiveness of the overall system with us trying exponential backoff. If we can have a reasonable limit on the maximum backoff time, then it should be good.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T15:20:12Z

- **pkg/controller/jobframework/setup.go:84** by [@mimowo](https://github.com/mimowo) — 2024-07-24T15:20:12Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  Reconcilers use exponential rate limiters: https://github.com/kubernetes/client-go/blob/fe54892e0cc44385de5b5300985bd27218dd2c23/util/workqueue/default_rate_limiters.go#L112C6-L115, so from 1ms to 1000s by default. We can try that and see how it works in practice, I'm ok for adjust the lower end to say 100ms (or 1s) if it turns out there are too many failed attempts.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-08T10:54:07Z

- **pkg/controller/jobframework/setup.go:84** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-08T10:54:07Z
```diff
@@ -71,25 +76,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if _, err := getRESTMapping(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, options.IntegrationRetryInterval.Duration, func() {
```

  Exponential rate limiters sound like a great idea, will be implemented in this PR. Thanks! We could re-discuss to agree on a lower and upper end values.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-12T10:19:54Z

- **pkg/controller/jobframework/integrationmanager.go:163** by [@mimowo](https://github.com/mimowo) — 2024-08-12T10:19:54Z
```diff
@@ -147,7 +149,15 @@ func (m *integrationManager) getExternal(kindArg string) (runtime.Object, bool)
 	return jt, f
 }
 
+func (m *integrationManager) getEnabledIntegrations() set.Set[string] {
```

  The name `enableIntegration` is slightly different so will not conflict with `enabledIntegrations`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-12T15:37:31Z

Just some nits. Ideally, we would have these covered before release:
- integration or e2e test
- only start `waitForAPI` with exponential backoff once the CRD is registered (wait either by controller or watcher on CRD type)

- **pkg/controller/jobframework/setup_test.go:168** by [@mimowo](https://github.com/mimowo) — 2024-08-12T15:23:04Z
```diff
@@ -135,18 +165,33 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(context.Background(), mgr, logger, tc.opts...)
```

  nit, can we use context from `utiltesting.ContextWithLog(t)`?

- **pkg/controller/jobframework/setup_test.go:187** by [@mimowo](https://github.com/mimowo) — 2024-08-12T15:30:39Z
```diff
@@ -135,18 +165,33 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(context.Background(), mgr, logger, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if tc.afterSetup != nil {
+				tc.afterSetup(mgr, &manager)
+			}
+
+			diff := cmp.Diff(tc.wantEnabledIntegrations, manager.getEnabledIntegrations().SortedList())
+			if len(diff) != 0 {
 				t.Errorf("Unexpected enabled integrations (-want,+got):\n%s", diff)
 			}
 		})
 	}
 }
 
+func testDelayedIntegration(mgr ctrlmgr.Manager, manager *integrationManager) {
+	for {
+		_, ok := manager.getEnabledIntegrations()["ray.io/raycluster"]
```

  nit, make the CRD name as a param to this function

- **pkg/controller/jobframework/setup.go:119** by [@mimowo](https://github.com/mimowo) — 2024-08-12T15:32:24Z
```diff
@@ -99,6 +100,60 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, checkAPIAvailable fetchRestMapperFunc, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(100*time.Millisecond, 100*time.Second)
```

  nit, move to constants, and until we can await for the CRD being installed, I would suggest:
  `minBackoffWaitForIntegration =1s`, `maxBackoffWaitForIntegration=2min`.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-13T10:19:46Z

- **pkg/controller/jobframework/setup.go:119** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-13T10:19:46Z
```diff
@@ -99,6 +100,60 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, checkAPIAvailable fetchRestMapperFunc, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(100*time.Millisecond, 100*time.Second)
```

  Thanks, those values sound reasonable to me.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-13T11:23:18Z

- **pkg/controller/jobframework/integrationmanager.go:163** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-13T11:23:18Z
```diff
@@ -147,7 +149,15 @@ func (m *integrationManager) getExternal(kindArg string) (runtime.Object, bool)
 	return jt, f
 }
 
+func (m *integrationManager) getEnabledIntegrations() set.Set[string] {
```

  Sorry, I meant the name `enabledIntegrations` would conflict with this [`enabledIntegrations`](https://github.com/kubernetes-sigs/kueue/pull/2574/files#diff-fde4763cb4c71b0994c0012fc717450c2c2d90a182e3fa30f3d5a21417b42686R85)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-13T12:51:55Z

- **pkg/controller/jobframework/integrationmanager.go:163** by [@mimowo](https://github.com/mimowo) — 2024-08-13T12:51:55Z
```diff
@@ -147,7 +149,15 @@ func (m *integrationManager) getExternal(kindArg string) (runtime.Object, bool)
 	return jt, f
 }
 
+func (m *integrationManager) getEnabledIntegrations() set.Set[string] {
```

  ah, got it, thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-15T19:47:22Z

- **pkg/controller/jobframework/integrationmanager.go:251** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T19:23:56Z
```diff
@@ -231,12 +241,14 @@ func EnableIntegration(name string) {
 // Mark the frameworks identified by names and return a revert function.
 func EnableIntegrationsForTest(tb testing.TB, names ...string) func() {
 	tb.Helper()
-	old := manager.enabledIntegrations.Clone()
+	old := manager.getEnabledIntegrations()
 	for _, name := range names {
 		manager.enableIntegration(name)
 	}
```

  It would be more accurate to obtain the lock once for this entire operation. But because this is just for tests, it's fine.

- **pkg/controller/jobframework/setup.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T19:34:45Z
```diff
@@ -21,16 +21,26 @@ import (
 	"errors"
 	"fmt"
 	"os"
+	"time"
 
 	"github.com/go-logr/logr"
 	"k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/util/workqueue"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
 
 	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
 )
 
+type fetchRestMapperFunc func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error)
```

  ```suggestion
  type fetchRestMapperFunc func(mgr ctrl.Manager, gvk schema.GroupVersionKind) error
  ```
  
  It doesn't look like the boolean will be true when there is an error, so no need for 2 outputs.

- **pkg/controller/jobframework/setup.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T19:35:39Z
```diff
@@ -103,6 +109,60 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, checkAPIAvailable fetchRestMapperFunc, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
```

  what's the rationale for using an exponential backoff instead of just checking every minute?

- **pkg/controller/jobframework/setup.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T19:37:33Z
```diff
@@ -103,6 +109,60 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, checkAPIAvailable fetchRestMapperFunc, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+
+	for {
+		isAvailable, err := checkAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func getRESTMapping(mgr ctrl.Manager, gvk schema.GroupVersionKind) (*meta.RESTMapping, error) {
```

  are we ever using the `*meta.RESTMapping`?
  
  If not, please simplify.

- **pkg/controller/jobframework/setup.go:158** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T19:39:15Z
```diff
@@ -103,6 +109,60 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, checkAPIAvailable fetchRestMapperFunc, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+
+	for {
+		isAvailable, err := checkAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func getRESTMapping(mgr ctrl.Manager, gvk schema.GroupVersionKind) (*meta.RESTMapping, error) {
+	restMapping, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+	if err != nil {
+		return nil, fmt.Errorf("failed to get REST mapping for %v: %w", gvk, err)
+	}
+	return restMapping, nil
+}
+
+var defaultCheckAPIAvailable fetchRestMapperFunc = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
```

  global variables for the purpose of testing are cumbersome (as you should be restoring them after the test). Prefer passing the function to `SetupControllers` as an `Option`.
  Even better, make use of `MapperProvider`

- **pkg/controller/jobframework/setup_test.go:121** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T19:45:07Z
```diff
@@ -99,6 +117,18 @@ func TestSetupControllers(t *testing.T) {
 			},
 			wantEnabledIntegrations: []string{"batch/job"},
 		},
+		"mapper doesn't have ray.io/raycluster when Controllers have been setup, but eventually does": {
+			opts: []Option{
+				WithEnabledFrameworks([]string{"batch/job", "kubeflow.org/mpijob", "ray.io/raycluster"}),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+				// Not including RayCluster
```

  what adds it?

- **pkg/controller/jobframework/setup_test.go:173** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-15T19:47:20Z
```diff
@@ -134,18 +164,33 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(ctx, mgr, logger, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if tc.afterSetup != nil {
+				tc.afterSetup(mgr, &manager, "ray.io/raycluster")
```

  `afterSetup` sounds too generic and we are only implementing it once. Try to be more explicit with the naming.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T09:18:21Z

- **pkg/controller/jobframework/setup.go:37** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T09:18:21Z
```diff
@@ -21,16 +21,26 @@ import (
 	"errors"
 	"fmt"
 	"os"
+	"time"
 
 	"github.com/go-logr/logr"
 	"k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/util/workqueue"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
 
 	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
 )
 
+type fetchRestMapperFunc func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error)
```

  This is true, but there is a scenario where it can return false with no errors i.e., if it is an `IsNoMatchError`, it is fine and expected to have missing job frameworks. It could also return false with errors if it's a different error such as issues with fetching the RESTMapper itself. I've modified the function variable to reflect this explicitly.
  ```
  	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
  		err := restMappingExists(mgr, gvk)
  		if meta.IsNoMatchError(err) {
  			return false, nil
  		}
  		if err != nil {
  			return false, err
  		}
  		return true, nil
  	}

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T09:24:19Z

- **pkg/controller/jobframework/setup.go:126** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T09:24:19Z
```diff
@@ -103,6 +109,60 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, checkAPIAvailable fetchRestMapperFunc, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
```

  Good question, there could be a scenario where we deploy 2 or more components at the same time, where Kueue was the first one to be deployed. The low base backoff helps in integrating with the new job frameworks quickly, while allowing for a higher-end backoff for any newly added frameworks at any later stage. I'm open to changing it to check on every minute if desired.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T09:34:53Z

- **pkg/controller/jobframework/setup_test.go:121** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T09:34:53Z
```diff
@@ -99,6 +117,18 @@ func TestSetupControllers(t *testing.T) {
 			},
 			wantEnabledIntegrations: []string{"batch/job"},
 		},
+		"mapper doesn't have ray.io/raycluster when Controllers have been setup, but eventually does": {
+			opts: []Option{
+				WithEnabledFrameworks([]string{"batch/job", "kubeflow.org/mpijob", "ray.io/raycluster"}),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+				// Not including RayCluster
```

  I've updated the test to add the job framework to the mapper directly. 
  
  However, previously, with the `defaultCheckAPIAvailable` function variable we simulate the RayCluster framework becoming available. For context, first we check if the restMapping exists for any given framework, if not, we start the go routine on `waitForAPI()` with the function variable which will return if the restMapping was found. In the test, we overwrite the function variable to eventually return true, meaning that the restMapping for the RayCluster framework was found.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T09:42:04Z

- **pkg/controller/jobframework/setup.go:158** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T09:42:03Z
```diff
@@ -103,6 +109,60 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, checkAPIAvailable fetchRestMapperFunc, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+
+	for {
+		isAvailable, err := checkAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func getRESTMapping(mgr ctrl.Manager, gvk schema.GroupVersionKind) (*meta.RESTMapping, error) {
+	restMapping, err := mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version)
+	if err != nil {
+		return nil, fmt.Errorf("failed to get REST mapping for %v: %w", gvk, err)
+	}
+	return restMapping, nil
+}
+
+var defaultCheckAPIAvailable fetchRestMapperFunc = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
```

  In the new commit, I'm using the MapperProvider directly and added to the mapper the delayed job framework such as 'RayCluster'. The only issue I encounter is that for the test I'm required to use a [Read Lock on checking the RestMapping](https://github.com/kubernetes-sigs/kueue/pull/2574/commits/e4f1ad26087d1af41356be62b74131a0e9a0d374#diff-9b0c3819f5a2c007ac714ec151937ce08cc2b768527cc28d20580a57f47ca5a7R161-R164), and a [Lock in the test](https://github.com/kubernetes-sigs/kueue/pull/2574/commits/e4f1ad26087d1af41356be62b74131a0e9a0d374#diff-0e0e2357162d6c7d6172e1f1e2f0fed3d27946005ee221718652d972f6a61879R189) to add to the existing mapper.
  
  In the application code, the go routine will continuously check for `RESTMapping` while in the test it will add to the mapper to simulate the availability of the newly added framework. It's a race condition, but of eventual consistency as the go routine reads again from the mapper and verifies the framework's availability.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-22T14:50:46Z

- **pkg/controller/jobframework/setup.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T13:59:44Z
```diff
@@ -75,25 +86,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if err := restMappingExists(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, func() {
+					log.Info(fmt.Sprintf("API now available, starting controller and webhook for %v", gvk))
```

  Use contextual logging:
  
  ```suggestion
  					log.Info("API now available, starting controller and webhook", "gvk", gvk)
  ```

- **pkg/controller/jobframework/setup.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T14:04:29Z
```diff
@@ -103,6 +110,64 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
+		err := restMappingExists(mgr, gvk)
+		if meta.IsNoMatchError(err) {
+			return false, nil
+		}
+		if err != nil {
+			return false, err
+		}
+		return true, nil
+	}
+
+	for {
+		isAvailable, err := defaultCheckAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func restMappingExists(mgr ctrl.Manager, gvk schema.GroupVersionKind) error {
+	mu.RLock()
```

  It looks like this is just for tests, so create the lock somewhere in the `MapperProvider` function, probably wrapping the Wrapper into another struct that has the lock.

- **pkg/controller/jobframework/setup.go:149** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T14:06:14Z
```diff
@@ -103,6 +110,64 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
+		err := restMappingExists(mgr, gvk)
+		if meta.IsNoMatchError(err) {
+			return false, nil
+		}
+		if err != nil {
+			return false, err
+		}
+		return true, nil
+	}
+
+	for {
+		isAvailable, err := defaultCheckAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
```

  ```suggestion
  		err := restMappingExists(mgr, gvk)
  		if err != nil {
  			rateLimiter.Forget(item)
  			action()
  			return
  		} else if !meta.IsNoMatchError(err) {
  ```

- **pkg/controller/jobframework/setup_test.go:168** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T14:49:52Z
```diff
@@ -134,18 +160,46 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(ctx, mgr, logger, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if tc.delayedIntegrationTest {
```

  ```suggestion
  			if len(tc.delayedGVKs) > 0 {
  ```

- **pkg/controller/jobframework/setup_test.go:171** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T14:50:43Z
```diff
@@ -134,18 +160,46 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(ctx, mgr, logger, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if tc.delayedIntegrationTest {
+				// Simulate the delayed addition of RayCluster
+				simulateDelayedIntegration(mgr, tc.delayedGVKs)
+				testDelayedIntegration(&manager, "ray.io/raycluster")
```

  use a for loop on `tc.delayedGVKs` or something along those lines, instead of the hardcoded string.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-22T17:37:28Z

Overall not bad.
IIUC, we decided to introduce this mechanism if the mechanism is not complex.
However, this PR tries to implement a lightweight mechanism similar to reconciler by hand.

Although I know there were discussions about whether we should watch CRD by reconcilers, and then we put the decision to introduce the reconciler for CRD on hold.

But, I'm wondering if we should use the dedicated reconciler instead of hand made watcher so that we can avoid implementing the similar to mechanism as reconciler by hand.

@alculquicondor @mimowo Please let me know what you think.

Even if we decided to switch to reconciler mechanism, I think we can work on the switching at the follow-up PRs.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T18:25:59Z

- **pkg/controller/jobframework/setup_test.go:171** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T18:25:59Z
```diff
@@ -134,18 +160,46 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(ctx, mgr, logger, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if tc.delayedIntegrationTest {
+				// Simulate the delayed addition of RayCluster
+				simulateDelayedIntegration(mgr, tc.delayedGVKs)
+				testDelayedIntegration(&manager, "ray.io/raycluster")
```

  Ohh great idea thanks!

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T18:26:48Z

- **pkg/controller/jobframework/setup.go:95** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T18:26:48Z
```diff
@@ -75,25 +86,21 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 			if err != nil {
 				return fmt.Errorf("%s: %w: %w", fwkNamePrefix, errFailedMappingResource, err)
 			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
+			if err := restMappingExists(mgr, gvk); err != nil {
 				if !meta.IsNoMatchError(err) {
 					return fmt.Errorf("%s: %w", fwkNamePrefix, err)
 				}
 				logger.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
+				go waitForAPI(ctx, mgr, log, gvk, func() {
+					log.Info(fmt.Sprintf("API now available, starting controller and webhook for %v", gvk))
```

  Thanks!!

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T18:33:03Z

- **pkg/controller/jobframework/setup.go:150** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T18:33:03Z
```diff
@@ -103,6 +110,64 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
+		err := restMappingExists(mgr, gvk)
+		if meta.IsNoMatchError(err) {
+			return false, nil
+		}
+		if err != nil {
+			return false, err
+		}
+		return true, nil
+	}
+
+	for {
+		isAvailable, err := defaultCheckAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func restMappingExists(mgr ctrl.Manager, gvk schema.GroupVersionKind) error {
+	mu.RLock()
```

  I've attempted to create a wrapper into another struct with the mapper and the lock, but the result is very similar.
  
  The issue is this: In the application code, the go routine will continuously read the RESTMapping while in the test it will add to the mapper to simulate the availability of the newly added framework. It's a race condition, but of eventual consistency as the go routine reads again from the mapper and verifies the framework's availability.
  
  I tried to lock the mapper in the test on its own, but the application code will continue to read from the mapper, causing this race condition.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-22T18:54:22Z

- **pkg/controller/jobframework/setup.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T18:54:22Z
```diff
@@ -103,6 +110,64 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
+		err := restMappingExists(mgr, gvk)
+		if meta.IsNoMatchError(err) {
+			return false, nil
+		}
+		if err != nil {
+			return false, err
+		}
+		return true, nil
+	}
+
+	for {
+		isAvailable, err := defaultCheckAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func restMappingExists(mgr ctrl.Manager, gvk schema.GroupVersionKind) error {
+	mu.RLock()
```

  But in the application code, the mapper is updated without taking this lock (because it's defined somewhere else).
  
  So the race condition will still exist.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T20:23:04Z

- **pkg/controller/jobframework/setup.go:150** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T20:23:04Z
```diff
@@ -103,6 +110,64 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
+		err := restMappingExists(mgr, gvk)
+		if meta.IsNoMatchError(err) {
+			return false, nil
+		}
+		if err != nil {
+			return false, err
+		}
+		return true, nil
+	}
+
+	for {
+		isAvailable, err := defaultCheckAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func restMappingExists(mgr ctrl.Manager, gvk schema.GroupVersionKind) error {
+	mu.RLock()
```

  I think I understand your point, correct me please. If we were to add a new `CRD`, this would update the RESTMapper, we then at the same time will be requesting it to check/read for specific Mappings. If the RESTMapper has not yet been updated with the new CRD at the time we read from it, the mechanism is to re-read of which, is a race condition but of eventual synchronization. How do you think we could proceed?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-22T20:49:37Z

- **pkg/controller/jobframework/setup.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T20:49:37Z
```diff
@@ -103,6 +110,64 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
+		err := restMappingExists(mgr, gvk)
+		if meta.IsNoMatchError(err) {
+			return false, nil
+		}
+		if err != nil {
+			return false, err
+		}
+		return true, nil
+	}
+
+	for {
+		isAvailable, err := defaultCheckAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func restMappingExists(mgr ctrl.Manager, gvk schema.GroupVersionKind) error {
+	mu.RLock()
```

  I don't know if the application RESTMapper itself has some internal lock already, it might be worth checking.
  
  My point is that adding a lock here doesn't prevent a race condition in the application code, but maybe that's fine, eventually it will succeed.

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-22T21:36:00Z

- **pkg/controller/jobframework/setup.go:150** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-22T21:36:00Z
```diff
@@ -103,6 +110,64 @@ func (m *integrationManager) setupControllers(mgr ctrl.Manager, log logr.Logger,
 	})
 }
 
+func (m *integrationManager) setupControllerAndWebhook(mgr ctrl.Manager, name string, fwkNamePrefix string, cb IntegrationCallbacks, options Options, opts ...Option) error {
+	if err := cb.NewReconciler(
+		mgr.GetClient(),
+		mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, options.ManagerName)),
+		opts...,
+	).SetupWithManager(mgr); err != nil {
+		return fmt.Errorf("%s: %w", fwkNamePrefix, err)
+	}
+	if err := cb.SetupWebhook(mgr, opts...); err != nil {
+		return fmt.Errorf("%s: unable to create webhook: %w", fwkNamePrefix, err)
+	}
+	m.enableIntegration(name)
+	return nil
+}
+
+func waitForAPI(ctx context.Context, mgr ctrl.Manager, log logr.Logger, gvk schema.GroupVersionKind, action func()) {
+	rateLimiter := workqueue.NewItemExponentialFailureRateLimiter(baseBackoffWaitForIntegration, maxBackoffWaitForIntegration)
+	item := gvk.String()
+	var defaultCheckAPIAvailable = func(mgr ctrl.Manager, gvk schema.GroupVersionKind) (bool, error) {
+		err := restMappingExists(mgr, gvk)
+		if meta.IsNoMatchError(err) {
+			return false, nil
+		}
+		if err != nil {
+			return false, err
+		}
+		return true, nil
+	}
+
+	for {
+		isAvailable, err := defaultCheckAPIAvailable(mgr, gvk)
+		if isAvailable {
+			rateLimiter.Forget(item)
+			action()
+			return
+		}
+		if err != nil && !meta.IsNoMatchError(err) {
+			log.Error(err, "Failed to get REST mapping for gvk", "gvk", gvk)
+		}
+		select {
+		case <-ctx.Done():
+			return
+		case <-time.After(rateLimiter.When(item)):
+			continue
+		}
+	}
+}
+
+func restMappingExists(mgr ctrl.Manager, gvk schema.GroupVersionKind) error {
+	mu.RLock()
```

  > My point is that adding a lock here doesn't prevent a race condition in the application code, but maybe that's fine, eventually it will succeed.
  
  That's correct. You're right, the lock here is for the test to pass, given it's ran with the `-race` flag. Ideally, it's best to not introduce code here for the tests, but the `setup_test.go` (test file) essentially runs the `setup_controllers()` function which executes almost the entirety of the `setup.go` file, meaning it basically runs as a hybrid test or closely as it would run in production, hence requiring this lock here.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-23T12:34:09Z

- **pkg/controller/jobframework/setup_test.go:185** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T12:34:09Z
```diff
@@ -134,18 +159,48 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(ctx, mgr, logger, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if len(tc.delayedGVKs) > 0 {
+				// Simulate the delayed addition of RayCluster
+				simulateDelayedIntegration(mgr, tc.delayedGVKs)
+				for _, gvk := range tc.delayedGVKs {
+					testDelayedIntegration(&manager, gvk.Group+"/"+strings.ToLower(gvk.Kind))
+				}
+			}
+
+			diff := cmp.Diff(tc.wantEnabledIntegrations, manager.getEnabledIntegrations().SortedList())
+			if len(diff) != 0 {
 				t.Errorf("Unexpected enabled integrations (-want,+got):\n%s", diff)
 			}
 		})
 	}
 }
```

  You can do something like this:
  
  ```golang
  func TestRESTMapper struct {
    *DefaultRESTMapper
    lock sync.RWMutex
  }
  
  func (m *TestRESTMapper) RESTMapping(...) {
    m.lock.Lock()
    defer m.lock.Unlock()
    return m.DefaultRESTMapper.RESTMapping(...)
  }
  ```

### Review by [@ChristianZaccaria](https://github.com/ChristianZaccaria) (commented) — 2024-08-23T19:25:39Z

- **pkg/controller/jobframework/setup_test.go:185** by [@ChristianZaccaria](https://github.com/ChristianZaccaria) — 2024-08-23T19:25:38Z
```diff
@@ -134,18 +159,48 @@ func TestSetupControllers(t *testing.T) {
 				t.Fatalf("Failed to setup manager: %v", err)
 			}
 
-			gotError := manager.setupControllers(mgr, logger, tc.opts...)
+			gotError := manager.setupControllers(ctx, mgr, logger, tc.opts...)
 			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
 				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
 			}
 
-			if diff := cmp.Diff(tc.wantEnabledIntegrations, manager.enabledIntegrations.SortedList()); len(diff) != 0 {
+			if len(tc.delayedGVKs) > 0 {
+				// Simulate the delayed addition of RayCluster
+				simulateDelayedIntegration(mgr, tc.delayedGVKs)
+				for _, gvk := range tc.delayedGVKs {
+					testDelayedIntegration(&manager, gvk.Group+"/"+strings.ToLower(gvk.Kind))
+				}
+			}
+
+			diff := cmp.Diff(tc.wantEnabledIntegrations, manager.getEnabledIntegrations().SortedList())
+			if len(diff) != 0 {
 				t.Errorf("Unexpected enabled integrations (-want,+got):\n%s", diff)
 			}
 		})
 	}
 }
```

  That is genius, please see latest changes if it meets with expectations. Thanks for the help and guidance Aldo.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-23T19:37:47Z

/lgtm
/approve

- **pkg/controller/jobframework/setup_test.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:36:11Z
```diff
@@ -123,29 +149,73 @@ func TestSetupControllers(t *testing.T) {
 						return gvk.GroupVersion()
 					})
 					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					testMapper := &TestRESTMapper{
+						DefaultRESTMapper: mapper,
+						lock:              sync.RWMutex{},
```

  nit: this line is not needed
