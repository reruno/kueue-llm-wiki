# PR #2606: Fixed "Should return an appropriate error" e2e test.

**Summary**: Fixed "Should return an appropriate error" e2e test.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2606

**Last updated**: 2024-07-17T09:34:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/visibility-server-role-binding-e2e-test` → `main`
- **Created**: 2024-07-15T04:45:17Z
- **Updated**: 2024-07-17T09:34:02Z
- **Merged**: 2024-07-16T16:03:28Z
- **Closed**: 2024-07-16T16:03:28Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 1   **+16 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fix "Kueue visibility server when A subject is bound to kueue-batch-admin-role Should return an appropriate error" e2e test.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2586

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-15T04:45:19Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-15T04:45:35Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8d917ba96a9307570755717197e2cb5bb052a79e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6696959e2e0c7e0008e23d39 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-15T04:45:55Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-15T05:16:56Z

/retest 

Due to #2581

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-15T06:03:51Z

/kind bug

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-16T16:01:25Z

/lgtm
/approve
Let's make sure the test is not flaky, we can always follow up with a better approach if find it

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-16T16:01:31Z

LGTM label has been added.  <details>Git tree hash: 4bfd1a84e0343de787a5db2530146ed92302ee65</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-16T16:01:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2606#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2606#issuecomment-2231303051" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-15T07:33:44Z

- **test/e2e/singlecluster/visibility_test.go:503** by [@mimowo](https://github.com/mimowo) — 2024-07-15T07:33:44Z
```diff
@@ -500,16 +500,20 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 
 		ginkgo.It("Should return an appropriate error", func() {
 			ginkgo.By("Returning a ResourceNotFound error for a nonexistent ClusterQueue", func() {
-				_, err := impersonatedVisibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, "non-existent", metav1.GetOptions{})
-				statusErr, ok := err.(*errors.StatusError)
-				gomega.Expect(ok).To(gomega.BeTrue())
-				gomega.Expect(statusErr.ErrStatus.Reason).To(gomega.Equal(metav1.StatusReasonNotFound))
+				gomega.Eventually(func(g gomega.Gomega) {
```

  Can you explain why wrapping the code in Eventually helps here? If the CQ is non-existent then we should expect `StatusReasonNotFound` consistently.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-15T07:38:47Z

- **test/e2e/singlecluster/visibility_test.go:503** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-15T07:38:47Z
```diff
@@ -500,16 +500,20 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 
 		ginkgo.It("Should return an appropriate error", func() {
 			ginkgo.By("Returning a ResourceNotFound error for a nonexistent ClusterQueue", func() {
-				_, err := impersonatedVisibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, "non-existent", metav1.GetOptions{})
-				statusErr, ok := err.(*errors.StatusError)
-				gomega.Expect(ok).To(gomega.BeTrue())
-				gomega.Expect(statusErr.ErrStatus.Reason).To(gomega.Equal(metav1.StatusReasonNotFound))
+				gomega.Eventually(func(g gomega.Gomega) {
```

  Yes, you right, but if `ClusterRoleBinding` not applied we will get `StatusReasonForbidden`. IIRC it can't apply immediately. Correct me if I'm wrong.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-15T07:42:43Z

- **test/e2e/singlecluster/visibility_test.go:503** by [@mimowo](https://github.com/mimowo) — 2024-07-15T07:42:43Z
```diff
@@ -500,16 +500,20 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 
 		ginkgo.It("Should return an appropriate error", func() {
 			ginkgo.By("Returning a ResourceNotFound error for a nonexistent ClusterQueue", func() {
-				_, err := impersonatedVisibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, "non-existent", metav1.GetOptions{})
-				statusErr, ok := err.(*errors.StatusError)
-				gomega.Expect(ok).To(gomega.BeTrue())
-				gomega.Expect(statusErr.ErrStatus.Reason).To(gomega.Equal(metav1.StatusReasonNotFound))
+				gomega.Eventually(func(g gomega.Gomega) {
```

  I don't know this bit, please investigate if you can, but if this is the case, I think the preferred solution is to wait for that to happen inside `BeforeEach` rather than inside individual `It`s.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-16T14:45:42Z

- **test/e2e/singlecluster/visibility_test.go:503** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-16T14:45:36Z
```diff
@@ -500,16 +500,20 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 
 		ginkgo.It("Should return an appropriate error", func() {
 			ginkgo.By("Returning a ResourceNotFound error for a nonexistent ClusterQueue", func() {
-				_, err := impersonatedVisibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, "non-existent", metav1.GetOptions{})
-				statusErr, ok := err.(*errors.StatusError)
-				gomega.Expect(ok).To(gomega.BeTrue())
-				gomega.Expect(statusErr.ErrStatus.Reason).To(gomega.Equal(metav1.StatusReasonNotFound))
+				gomega.Eventually(func(g gomega.Gomega) {
```

  I'm ok with this approach.
  
  I think the delay makes sense. IIUC, it is Kueue that is verifying the RBAC, and the role objects can be delayed due to the nature of the informers.
  
  @PBundyra does this match your knowledge of the apiserver extensions?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-16T14:50:54Z

- **test/e2e/singlecluster/visibility_test.go:503** by [@mimowo](https://github.com/mimowo) — 2024-07-16T14:50:54Z
```diff
@@ -500,16 +500,20 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 
 		ginkgo.It("Should return an appropriate error", func() {
 			ginkgo.By("Returning a ResourceNotFound error for a nonexistent ClusterQueue", func() {
-				_, err := impersonatedVisibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, "non-existent", metav1.GetOptions{})
-				statusErr, ok := err.(*errors.StatusError)
-				gomega.Expect(ok).To(gomega.BeTrue())
-				gomega.Expect(statusErr.ErrStatus.Reason).To(gomega.Equal(metav1.StatusReasonNotFound))
+				gomega.Eventually(func(g gomega.Gomega) {
```

  I think we can do the same - use eventually inside BeforeEach. Then the active waiting will not need to be spread across `It`s, and easier to replace if we even find out another approach to wait.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-16T15:40:10Z

- **test/e2e/singlecluster/visibility_test.go:535** by [@mimowo](https://github.com/mimowo) — 2024-07-16T15:40:09Z
```diff
@@ -526,6 +532,14 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, roleBinding)).Should(gomega.Succeed())
+			ginkgo.By("Returning a ResourceNotFound error for a nonexistent LocalQueue", func() {
```

  ```suggestion
  			ginkgo.By("Wait for ResourceNotFound error instead of Forbidden to make sure the role bindings work", func() {
  ```
  nit, and use the same for the other block

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-16T15:41:48Z

- **test/e2e/singlecluster/visibility_test.go:535** by [@mimowo](https://github.com/mimowo) — 2024-07-16T15:41:47Z
```diff
@@ -526,6 +532,14 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, roleBinding)).Should(gomega.Succeed())
+			ginkgo.By("Returning a ResourceNotFound error for a nonexistent LocalQueue", func() {
```

  Or even better, wrap in a function `awaitForInitialization`, and call from both `BeforeEach`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-16T15:47:42Z

- **test/e2e/singlecluster/visibility_test.go:535** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-16T15:47:42Z
```diff
@@ -526,6 +532,14 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, roleBinding)).Should(gomega.Succeed())
+			ginkgo.By("Returning a ResourceNotFound error for a nonexistent LocalQueue", func() {
```

  > Or even better, wrap in a function `awaitForInitialization`, and call from both `BeforeEach`
  
  Unfortunately they are different.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-16T16:00:37Z

- **test/e2e/singlecluster/visibility_test.go:535** by [@mimowo](https://github.com/mimowo) — 2024-07-16T16:00:37Z
```diff
@@ -526,6 +532,14 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, roleBinding)).Should(gomega.Succeed())
+			ginkgo.By("Returning a ResourceNotFound error for a nonexistent LocalQueue", func() {
```

  ah, I see, thanks, this binding in the second `BeforeEach` is for nsA

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-07-17T09:33:02Z

- **test/e2e/singlecluster/visibility_test.go:503** by [@PBundyra](https://github.com/PBundyra) — 2024-07-17T09:33:02Z
```diff
@@ -500,16 +500,20 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 
 		ginkgo.It("Should return an appropriate error", func() {
 			ginkgo.By("Returning a ResourceNotFound error for a nonexistent ClusterQueue", func() {
-				_, err := impersonatedVisibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, "non-existent", metav1.GetOptions{})
-				statusErr, ok := err.(*errors.StatusError)
-				gomega.Expect(ok).To(gomega.BeTrue())
-				gomega.Expect(statusErr.ErrStatus.Reason).To(gomega.Equal(metav1.StatusReasonNotFound))
+				gomega.Eventually(func(g gomega.Gomega) {
```

  >> I think the delay makes sense. IIUC, it is Kueue that is verifying the RBAC, and the role objects can be delayed due to the nature of the informers.
  
  IIRC it's apiserver's responsibility to authorize (based on RBAC) requests from users. Refering to [Authentication Flow documentation](https://kubernetes.io/docs/tasks/extend-kubernetes/configure-aggregation-layer/#authentication-flow)
  
  "_Kubernetes apiserver: authenticate the requesting user and authorize their rights to the requested API path._"
  
  Even at the stage of authorizing request by extension apiserver (Kueue in this case) is still asks apiserver:
  
  "_The extension apiserver now can validate that the user/group retrieved from the headers are authorized to execute the given request. It does so by sending a standard [SubjectAccessReview](https://kubernetes.io/docs/reference/access-authn-authz/authorization/) request to the Kubernetes apiserver._"
