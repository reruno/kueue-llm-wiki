# PR #262: Refactor TestCacheWorkloadOperations

**Summary**: Refactor TestCacheWorkloadOperations

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/262

**Last updated**: 2022-05-26T13:23:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@shuheiktgw](https://github.com/shuheiktgw)
- **Branch**: `refactor_cache_workload_tests` → `main`
- **Created**: 2022-05-14T02:38:07Z
- **Updated**: 2022-05-26T13:23:24Z
- **Merged**: 2022-05-26T13:23:24Z
- **Closed**: 2022-05-26T13:23:24Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@denkensk](https://github.com/denkensk)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 1   **+247 / -199**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind cleanup

#### What this PR does / why we need it:
Refactored TestCacheWorkloadOperations so that each test case is independent of the others. I also added some missing test cases as well. Thanks for your review! 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/72

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-14T02:38:15Z

Hi @shuheiktgw. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@denkensk](https://github.com/denkensk) — 2022-05-14T02:58:05Z

/ok-to-test

### Comment by [@shuheiktgw](https://github.com/shuheiktgw) — 2022-05-19T23:55:20Z

@denkensk Thank you for your review! I've updated the PR so would you review it again? 🙏

### Comment by [@denkensk](https://github.com/denkensk) — 2022-05-25T14:36:05Z

Thanks. lgtm .
Can you squash it?

### Comment by [@shuheiktgw](https://github.com/shuheiktgw) — 2022-05-26T00:03:57Z

@denkensk Thank you. Squashed!

### Comment by [@denkensk](https://github.com/denkensk) — 2022-05-26T13:22:10Z

/lgtm
/approve

Thanks. @shuheiktgw

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-26T13:22:22Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/262#issuecomment-1138572384" title="Approved">denkensk</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/262#" title="Author self-approved">shuheiktgw</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [denkensk]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-17T13:46:22Z

Thanks @shuheiktgw 
Add some comments.

- **pkg/cache/cache_test.go:562** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:25:36Z
```diff
@@ -559,25 +559,18 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 		},
 		&kueue.Workload{
-			ObjectMeta: metav1.ObjectMeta{Name: "c"},
+			ObjectMeta: metav1.ObjectMeta{Name: "b"},
```

  You can use `utiltesting.MakeWorkload` to construct workload.

- **pkg/cache/cache_test.go:684** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:34:51Z
```diff
@@ -704,68 +697,126 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("c"),
+					Workloads:     sets.NewString("b"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
 				"two": {
-					Workloads:     sets.NewString("a", "b", "d"),
+					Workloads:     sets.NewString("a", "c"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 		},
 		{
-			name: "update old doesn't exist",
-			operation: func() error {
+			name: "update error old clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				old := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "e"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
-						Admission: &kueue.Admission{ClusterQueue: "one"},
+						Admission: &kueue.Admission{ClusterQueue: "three"},
 					},
 				}
 				latest := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "e"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
-						Admission: &kueue.Admission{ClusterQueue: "two"},
+						Admission: &kueue.Admission{ClusterQueue: "one"},
 					},
 				}
 				return cache.UpdateWorkload(&old, &latest)
 			},
+			wantError: "old ClusterQueue doesn't exist",
```

  ?

- **pkg/cache/cache_test.go:768** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:36:35Z
```diff
@@ -704,68 +697,126 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("c"),
+					Workloads:     sets.NewString("b"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
 				"two": {
-					Workloads:     sets.NewString("a", "b", "d"),
+					Workloads:     sets.NewString("a", "c"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 		},
 		{
-			name: "update old doesn't exist",
-			operation: func() error {
+			name: "update error old clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				old := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "e"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
-						Admission: &kueue.Admission{ClusterQueue: "one"},
+						Admission: &kueue.Admission{ClusterQueue: "three"},
 					},
 				}
 				latest := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "e"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
-						Admission: &kueue.Admission{ClusterQueue: "two"},
+						Admission: &kueue.Admission{ClusterQueue: "one"},
 					},
 				}
 				return cache.UpdateWorkload(&old, &latest)
 			},
+			wantError: "old ClusterQueue doesn't exist",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
+		{
+			name: "update error new clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
+				old := kueue.Workload{
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
+					Spec: kueue.WorkloadSpec{
+						Admission: &kueue.Admission{ClusterQueue: "one"},
+					},
+				}
+				latest := kueue.Workload{
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
+					Spec: kueue.WorkloadSpec{
+						Admission: &kueue.Admission{ClusterQueue: "three"},
+					},
+				}
+				return cache.UpdateWorkload(&old, &latest)
+			},
+			wantError: "new ClusterQueue doesn't exist",
+			wantResults: map[string]result{
+				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
 				"two": {
-					Workloads:     sets.NewString("a", "b", "d", "e"),
+					Workloads:     sets.NewString("c"),
+					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
+				},
+			},
+		},
+		{
+			name: "update old doesn't exist",
```

  ```suggestion
  			name: "update workload which doesn't exist.",
  ```

- **pkg/cache/cache_test.go:841** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:37:47Z
```diff
@@ -777,20 +828,20 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			wantError: "cluster queue not found",
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("c"),
-					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e"),
+					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
 			},
 		},
 		{
 			name: "delete workload doesn't exist",
```

  ```suggestion
  			name: "delete workload which doesn't exist",
  ```

- **pkg/cache/cache_test.go:859** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:41:41Z
```diff
@@ -842,81 +893,185 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("a", "c"),
-					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+					Workloads:     sets.NewString("a", "b", "d"),
+					UsedResources: Resources{"cpu": {"on-demand": 20, "spot": 30}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/a": "one",
-				"/f": "two",
+				"/d": "one",
+				"/e": "two",
 			},
 		},
 		{
-			name: "forget",
-			operation: func() error {
+			name: "assume error clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				w := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "a"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
 						PodSets: podSets,
 						Admission: &kueue.Admission{
-							ClusterQueue:  "one",
-							PodSetFlavors: podSetFlavors,
+							ClusterQueue: "three",
 						},
 					},
 				}
-				return cache.ForgetWorkload(&w)
+				if err := cache.AssumeWorkload(&w); err != nil {
+					return err
+				}
+				return nil
 			},
+			wantError: "cluster queue not found",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
+		{
+			name: "forget",
```

  Can we simplify this test case？Just forget the exist one like `a`?

- **pkg/cache/cache_test.go:918** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:42:41Z
```diff
@@ -842,81 +893,185 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("a", "c"),
-					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+					Workloads:     sets.NewString("a", "b", "d"),
+					UsedResources: Resources{"cpu": {"on-demand": 20, "spot": 30}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/a": "one",
-				"/f": "two",
+				"/d": "one",
+				"/e": "two",
 			},
 		},
 		{
-			name: "forget",
-			operation: func() error {
+			name: "assume error clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				w := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "a"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
 						PodSets: podSets,
 						Admission: &kueue.Admission{
-							ClusterQueue:  "one",
-							PodSetFlavors: podSetFlavors,
+							ClusterQueue: "three",
 						},
 					},
 				}
-				return cache.ForgetWorkload(&w)
+				if err := cache.AssumeWorkload(&w); err != nil {
+					return err
+				}
+				return nil
 			},
+			wantError: "cluster queue not found",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
+		{
+			name: "forget",
+			operation: func(cache *Cache) error {
+				workloads := []kueue.Workload{
+					{
+						ObjectMeta: metav1.ObjectMeta{Name: "d"},
+						Spec: kueue.WorkloadSpec{
+							PodSets: podSets,
+							Admission: &kueue.Admission{
+								ClusterQueue:  "one",
+								PodSetFlavors: podSetFlavors,
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{Name: "e"},
+						Spec: kueue.WorkloadSpec{
+							PodSets: podSets,
+							Admission: &kueue.Admission{
+								ClusterQueue:  "two",
+								PodSetFlavors: podSetFlavors,
+							},
+						},
+					},
+				}
+				for i := range workloads {
+					if err := cache.AssumeWorkload(&workloads[i]); err != nil {
+						return err
+					}
+				}
+
+				w := workloads[0]
+				return cache.ForgetWorkload(&w)
+			},
+			wantResults: map[string]result{
+				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/f": "two",
+				"/e": "two",
 			},
 		},
 		{
-			name: "add assumed workload",
-			operation: func() error {
+			name: "forget error workload is not assumed",
+			operation: func(cache *Cache) error {
 				w := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "f"},
+					ObjectMeta: metav1.ObjectMeta{Name: "b"},
 					Spec: kueue.WorkloadSpec{
 						PodSets: podSets,
-						Admission: &kueue.Admission{
-							ClusterQueue:  "two",
-							PodSetFlavors: podSetFlavors,
-						},
 					},
 				}
-				if !cache.AddOrUpdateWorkload(&w) {
-					return fmt.Errorf("failed to add workload that was assumed")
+				if err := cache.ForgetWorkload(&w); err != nil {
+					return err
 				}
 				return nil
 			},
+			wantError: "the workload is not assumed",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
+		{
+			name: "add assumed workload",
```

  Can we simplify this test case？Just add the exist one like a?

- **pkg/cache/cache_test.go:830** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:44:45Z
```diff
@@ -842,81 +893,185 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("a", "c"),
-					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+					Workloads:     sets.NewString("a", "b", "d"),
+					UsedResources: Resources{"cpu": {"on-demand": 20, "spot": 30}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/a": "one",
-				"/f": "two",
+				"/d": "one",
```

  Why `a` is not in the `wantAssumedWorkloads`?

- **pkg/cache/cache_test.go:857** by [@denkensk](https://github.com/denkensk) — 2022-05-17T13:45:17Z
```diff
@@ -842,81 +893,185 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("a", "c"),
-					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+					Workloads:     sets.NewString("a", "b", "d"),
+					UsedResources: Resources{"cpu": {"on-demand": 20, "spot": 30}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/a": "one",
-				"/f": "two",
+				"/d": "one",
+				"/e": "two",
 			},
 		},
 		{
-			name: "forget",
-			operation: func() error {
+			name: "assume error clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				w := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "a"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
 						PodSets: podSets,
 						Admission: &kueue.Admission{
-							ClusterQueue:  "one",
-							PodSetFlavors: podSetFlavors,
+							ClusterQueue: "three",
 						},
 					},
 				}
-				return cache.ForgetWorkload(&w)
+				if err := cache.AssumeWorkload(&w); err != nil {
+					return err
+				}
+				return nil
 			},
+			wantError: "cluster queue not found",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
```

  Also check the `wantAssumedWorkloads` in this case.

### Review by [@shuheiktgw](https://github.com/shuheiktgw) (commented) — 2022-05-19T23:20:14Z

- **pkg/cache/cache_test.go:684** by [@shuheiktgw](https://github.com/shuheiktgw) — 2022-05-19T23:20:14Z
```diff
@@ -704,68 +697,126 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("c"),
+					Workloads:     sets.NewString("b"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
 				"two": {
-					Workloads:     sets.NewString("a", "b", "d"),
+					Workloads:     sets.NewString("a", "c"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 		},
 		{
-			name: "update old doesn't exist",
-			operation: func() error {
+			name: "update error old clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				old := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "e"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
-						Admission: &kueue.Admission{ClusterQueue: "one"},
+						Admission: &kueue.Admission{ClusterQueue: "three"},
 					},
 				}
 				latest := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "e"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
-						Admission: &kueue.Admission{ClusterQueue: "two"},
+						Admission: &kueue.Admission{ClusterQueue: "one"},
 					},
 				}
 				return cache.UpdateWorkload(&old, &latest)
 			},
+			wantError: "old ClusterQueue doesn't exist",
```

  It comes from [here](https://github.com/kubernetes-sigs/kueue/blob/b5e7322df70e1fe3b69b1005ce993693f0d1c289/pkg/cache/cache.go#L374). I agree that the message is kind of ambiguous so I'm happy to update the original error message if that's what you meant 🙂

### Review by [@shuheiktgw](https://github.com/shuheiktgw) (commented) — 2022-05-19T23:37:57Z

- **pkg/cache/cache_test.go:859** by [@shuheiktgw](https://github.com/shuheiktgw) — 2022-05-19T23:37:57Z
```diff
@@ -842,81 +893,185 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("a", "c"),
-					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+					Workloads:     sets.NewString("a", "b", "d"),
+					UsedResources: Resources{"cpu": {"on-demand": 20, "spot": 30}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/a": "one",
-				"/f": "two",
+				"/d": "one",
+				"/e": "two",
 			},
 		},
 		{
-			name: "forget",
-			operation: func() error {
+			name: "assume error clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				w := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "a"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
 						PodSets: podSets,
 						Admission: &kueue.Admission{
-							ClusterQueue:  "one",
-							PodSetFlavors: podSetFlavors,
+							ClusterQueue: "three",
 						},
 					},
 				}
-				return cache.ForgetWorkload(&w)
+				if err := cache.AssumeWorkload(&w); err != nil {
+					return err
+				}
+				return nil
 			},
+			wantError: "cluster queue not found",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
+		{
+			name: "forget",
```

  The workload should be assumed first so I believe we cannot use the workload `a` for this case
  https://github.com/kubernetes-sigs/kueue/blob/3ed4ded71520eb702a898cf1bd4fd5d0653ab68d/pkg/cache/cache_test.go#L987

### Review by [@shuheiktgw](https://github.com/shuheiktgw) (commented) — 2022-05-19T23:48:25Z

- **pkg/cache/cache_test.go:918** by [@shuheiktgw](https://github.com/shuheiktgw) — 2022-05-19T23:48:25Z
```diff
@@ -842,81 +893,185 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("a", "c"),
-					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+					Workloads:     sets.NewString("a", "b", "d"),
+					UsedResources: Resources{"cpu": {"on-demand": 20, "spot": 30}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/a": "one",
-				"/f": "two",
+				"/d": "one",
+				"/e": "two",
 			},
 		},
 		{
-			name: "forget",
-			operation: func() error {
+			name: "assume error clusterQueue doesn't exist",
+			operation: func(cache *Cache) error {
 				w := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "a"},
+					ObjectMeta: metav1.ObjectMeta{Name: "d"},
 					Spec: kueue.WorkloadSpec{
 						PodSets: podSets,
 						Admission: &kueue.Admission{
-							ClusterQueue:  "one",
-							PodSetFlavors: podSetFlavors,
+							ClusterQueue: "three",
 						},
 					},
 				}
-				return cache.ForgetWorkload(&w)
+				if err := cache.AssumeWorkload(&w); err != nil {
+					return err
+				}
+				return nil
 			},
+			wantError: "cluster queue not found",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
+		{
+			name: "forget",
+			operation: func(cache *Cache) error {
+				workloads := []kueue.Workload{
+					{
+						ObjectMeta: metav1.ObjectMeta{Name: "d"},
+						Spec: kueue.WorkloadSpec{
+							PodSets: podSets,
+							Admission: &kueue.Admission{
+								ClusterQueue:  "one",
+								PodSetFlavors: podSetFlavors,
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{Name: "e"},
+						Spec: kueue.WorkloadSpec{
+							PodSets: podSets,
+							Admission: &kueue.Admission{
+								ClusterQueue:  "two",
+								PodSetFlavors: podSetFlavors,
+							},
+						},
+					},
+				}
+				for i := range workloads {
+					if err := cache.AssumeWorkload(&workloads[i]); err != nil {
+						return err
+					}
+				}
+
+				w := workloads[0]
+				return cache.ForgetWorkload(&w)
+			},
+			wantResults: map[string]result{
+				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/f": "two",
+				"/e": "two",
 			},
 		},
 		{
-			name: "add assumed workload",
-			operation: func() error {
+			name: "forget error workload is not assumed",
+			operation: func(cache *Cache) error {
 				w := kueue.Workload{
-					ObjectMeta: metav1.ObjectMeta{Name: "f"},
+					ObjectMeta: metav1.ObjectMeta{Name: "b"},
 					Spec: kueue.WorkloadSpec{
 						PodSets: podSets,
-						Admission: &kueue.Admission{
-							ClusterQueue:  "two",
-							PodSetFlavors: podSetFlavors,
-						},
 					},
 				}
-				if !cache.AddOrUpdateWorkload(&w) {
-					return fmt.Errorf("failed to add workload that was assumed")
+				if err := cache.ForgetWorkload(&w); err != nil {
+					return err
 				}
 				return nil
 			},
+			wantError: "the workload is not assumed",
 			wantResults: map[string]result{
 				"one": {
+					Workloads:     sets.NewString("a", "b"),
+					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+				},
+				"two": {
 					Workloads:     sets.NewString("c"),
 					UsedResources: Resources{"cpu": {"on-demand": 0, "spot": 0}},
 				},
+			},
+		},
+		{
+			name: "add assumed workload",
```

  I believe the point of this test case is to add an assumed workload so we cannot simply use `a`, which is not assumed? 🤔

### Review by [@shuheiktgw](https://github.com/shuheiktgw) (commented) — 2022-05-19T23:51:47Z

- **pkg/cache/cache_test.go:830** by [@shuheiktgw](https://github.com/shuheiktgw) — 2022-05-19T23:51:46Z
```diff
@@ -842,81 +893,185 @@ func TestCacheWorkloadOperations(t *testing.T) {
 			},
 			wantResults: map[string]result{
 				"one": {
-					Workloads:     sets.NewString("a", "c"),
-					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
+					Workloads:     sets.NewString("a", "b", "d"),
+					UsedResources: Resources{"cpu": {"on-demand": 20, "spot": 30}},
 				},
 				"two": {
-					Workloads:     sets.NewString("b", "d", "e", "f"),
+					Workloads:     sets.NewString("c", "e"),
 					UsedResources: Resources{"cpu": {"on-demand": 10, "spot": 15}},
 				},
 			},
 			wantAssumedWorkloads: map[string]string{
-				"/a": "one",
-				"/f": "two",
+				"/d": "one",
```

  I don't know if this answers your question but I think that is because `d` and `e` are the only workloads we assume in this test case.
  
  https://github.com/kubernetes-sigs/kueue/pull/262/files/3ed4ded71520eb702a898cf1bd4fd5d0653ab68d#diff-3e99be25ef623d0bc22234a878bef63a1fef2029a18d5b35f9079537e9f59ebeR867-R886
