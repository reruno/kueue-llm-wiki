# PR #2641: Enable Multiple Preemptions within Cohort in a single Scheduling Cycle

**Summary**: Enable Multiple Preemptions within Cohort in a single Scheduling Cycle

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2641

**Last updated**: 2024-07-19T14:52:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `multiple_preemptions` → `main`
- **Created**: 2024-07-18T16:01:46Z
- **Updated**: 2024-07-19T14:52:51Z
- **Merged**: 2024-07-19T14:31:31Z
- **Closed**: 2024-07-19T14:31:31Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 5   **+671 / -16**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
When scheduling a `Preempt` workload, if conflicting workload (same cohort, overlapping resource flavors) was processed before, we would skip this preemption as our calculations [were invalidated](https://github.com/kubernetes-sigs/kueue/blob/1b8ec34eca4d07af9e91c22f163e90567226c5c7/pkg/scheduler/scheduler.go#L220-L222).

In this PR, we introduce less conservative calculations, to allow multiple preemptions within a cohort within one cycle, as long as the preemption targets do not overlap, and the workload still fits.

Additionally, we allow a `Preempt` fit workload to proceed, even if a `Fit` mode workload was previously processed, as long as the workload still fits.

Finally we improve logging in the old logic, to differentiate "No Longer Fits" from "Preemptions Invalidated"

#### Which issue(s) this PR fixes:
Fixes #2596
Contributes to #1867

#### Special notes for your reviewer:
See initial round of comments [here](https://github.com/gabesaba/kueue/commit/c3277c97cadc5653505b6a86add07d0c1f1b5dc6)

#### Does this PR introduce a user-facing change?
```release-note
Introduce the MultiplePreemptions flag, which allows more than one
preemption to occur in the same scheduling cycle, even with overlapping
FlavorResources
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-18T16:02:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b0c29f0a23e4442c52bdd1af222df87c6e36a83b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/669a6f2313e13900082bcd14 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-19T13:57:59Z

second commit https://github.com/kubernetes-sigs/kueue/pull/2641/commits/b0c29f0a23e4442c52bdd1af222df87c6e36a83b lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-19T14:04:47Z

LGTM label has been added.  <details>Git tree hash: 2fb9e195d49be81b6cb1e1d6c3bae301f679e08d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-19T14:04:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2641#pullrequestreview-2188325058" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2641#" title="Author self-approved">gabesaba</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-19T14:27:50Z

/hold cancel

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-18T19:47:13Z

Just some nits and clarifications

/approve
/hold

- **pkg/scheduler/scheduler.go:363** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:15:49Z
```diff
@@ -322,6 +357,26 @@ type entry struct {
 	preemptionTargets     []*preemption.Target
 }
 
+// NetUsage returns how much capacity this entry will require from the ClusterQueue/Cohort.
+// When a workload is preempting, it subtracts the preempted resources from the resources
+// required, as the remaining quota is all we need from the CQ/Cohort.
+func (e *entry) NetUsage() resources.FlavorResourceQuantitiesFlat {
```

  ```suggestion
  func (e *entry) netUsage() resources.FlavorResourceQuantitiesFlat {
  ```

- **pkg/scheduler/scheduler.go:371** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:19:02Z
```diff
@@ -322,6 +357,26 @@ type entry struct {
 	preemptionTargets     []*preemption.Target
 }
 
+// NetUsage returns how much capacity this entry will require from the ClusterQueue/Cohort.
+// When a workload is preempting, it subtracts the preempted resources from the resources
+// required, as the remaining quota is all we need from the CQ/Cohort.
+func (e *entry) NetUsage() resources.FlavorResourceQuantitiesFlat {
+	if e.assignment.RepresentativeMode() == flavorassigner.Fit {
+		return e.assignment.Usage
+	}
+
+	usage := maps.Clone(e.assignment.Usage)
+	for target := range e.preemptionTargets {
+		for fr, v := range e.preemptionTargets[target].WorkloadInfo.FlavorResourceUsage() {
+			if _, hasFlavor := usage[fr]; !hasFlavor {
```

  ```suggestion
  			if _, uses := usage[fr]; !uses {
  ```
  
  It cannot be has**Flavor**, because it's the flavor and resource.

- **pkg/scheduler/scheduler_test.go:83** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:20:40Z
```diff
@@ -60,6 +60,29 @@ var cmpDump = []cmp.Option{
 	cmpopts.SortSlices(func(a, b string) bool { return a < b }),
 }
 
+// Indicates under which configuration of the MultiplePreemptions flag the test passes.
+type multiplePreemptionsCompatibility int
+
+func (mode multiplePreemptionsCompatibility) runTest(baseName string, t *testing.T, f func(t *testing.T)) {
+	if mode == Both || mode == Legacy {
+		defer features.SetFeatureGateDuringTest(t, features.MultiplePreemptions, false)()
+		t.Run(baseName+" LegacyMode", f)
+	}
+	if mode == Both || mode == MultiplePremptions {
+		defer features.SetFeatureGateDuringTest(t, features.MultiplePreemptions, true)()
+		t.Run(baseName+" MultiplePreemptionsMode", f)
+	}
+}
+
+const (
+	// Default. Run test with both Legacy and MultiplePreemptions code.
+	Both multiplePreemptionsCompatibility = iota
+	// Run test only with Legacy code.
+	Legacy
+	// Run test only with MultiplePreemptions code.
+	MultiplePremptions
```

  ```suggestion
  	// Run test only with Legacy code.
  	Legacy = 1 << iota
  	// Run test only with MultiplePreemptions code.
  	MultiplePreemptions
  	Both multiplePreemptionsCompatibility = Legacy | MultiplePreemptions
  ```

- **pkg/scheduler/scheduler_test.go:927** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:28:39Z
```diff
@@ -896,6 +921,11 @@ func TestSchedule(t *testing.T) {
 			},
 		},
 		"no overadmission while borrowing": {
+			// we disable this test for MultiplePreemption
+			// logic, as the new logic considers this
+			// unschedulable, while the old logic considers
+			// it skipped.
```

  so what changes is that eng-gamma would not be in `wantLeft`? That's fine, but I wonder if we care about duplicating the test case for it.
  Maybe not worth, but instead we can update this test when we remove the Legacy mode.

- **pkg/scheduler/scheduler_test.go:1725** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:29:59Z
```diff
@@ -1651,6 +1681,563 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			//
+			// Note: this test case passes for legacy logic when
+			// the CQ's usage is equal to nominal capacity.
+			// Therefore, we set the usage of preempted workloads
+			// to less than nominal capacity, so this test fails
+			// in legacy logic.
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("other-beta").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("other", "eng-alpha").ClusterQueue("other-alpha").Obj(),
+				*utiltesting.MakeLocalQueue("other", "eng-beta").ClusterQueue("other-beta").Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "eng-alpha").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1500m").
```

  ```suggestion
  					Request(corev1.ResourceCPU, "1.5").
  ```

- **pkg/scheduler/scheduler_test.go:1726** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:30:43Z
```diff
@@ -1651,6 +1681,563 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			//
+			// Note: this test case passes for legacy logic when
+			// the CQ's usage is equal to nominal capacity.
+			// Therefore, we set the usage of preempted workloads
+			// to less than nominal capacity, so this test fails
+			// in legacy logic.
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("other-beta").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("other", "eng-alpha").ClusterQueue("other-alpha").Obj(),
+				*utiltesting.MakeLocalQueue("other", "eng-beta").ClusterQueue("other-beta").Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "eng-alpha").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1500m").
+					ReserveQuota(utiltesting.MakeAdmission("other-alpha").Assignment(corev1.ResourceCPU, "default", "1500m").Obj()).
```

  You can also use `SimpleReserveQuota` for less verbosity.

- **pkg/scheduler/scheduler_test.go:1689** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:37:44Z
```diff
@@ -1651,6 +1681,563 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			//
+			// Note: this test case passes for legacy logic when
```

  Interesting... how does it overcome this check? `(mode == flavorassigner.Preempt && cycleCohortsSkipPreemption.Has(cq.Cohort.Name))`?

- **pkg/scheduler/scheduler_test.go:1909** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:42:35Z
```diff
@@ -1651,6 +1681,563 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			//
+			// Note: this test case passes for legacy logic when
+			// the CQ's usage is equal to nominal capacity.
+			// Therefore, we set the usage of preempted workloads
+			// to less than nominal capacity, so this test fails
+			// in legacy logic.
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("other-beta").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("other", "eng-alpha").ClusterQueue("other-alpha").Obj(),
+				*utiltesting.MakeLocalQueue("other", "eng-beta").ClusterQueue("other-beta").Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "eng-alpha").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1500m").
+					ReserveQuota(utiltesting.MakeAdmission("other-alpha").Assignment(corev1.ResourceCPU, "default", "1500m").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("b1", "eng-beta").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1500m").
+					ReserveQuota(utiltesting.MakeAdmission("other-beta").Assignment(corev1.ResourceCPU, "default", "1500m").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("preemptor", "eng-alpha").
+					Priority(100).
+					Queue("other").
+					Request(corev1.ResourceCPU, "2").
+					Obj(),
+				*utiltesting.MakeWorkload("preemptor", "eng-beta").
+					Priority(100).
+					Queue("other").
+					Request(corev1.ResourceCPU, "2").
+					Obj(),
+			},
+			wantPreempted: sets.New("eng-alpha/a1", "eng-beta/b1"),
+			wantLeft: map[string][]string{
+				"other-alpha": {"eng-alpha/preemptor"},
+				"other-beta":  {"eng-beta/preemptor"},
+			},
+			wantAssignments: map[string]kueue.Admission{
+				"eng-alpha/a1": *utiltesting.MakeAdmission("other-alpha").Assignment(corev1.ResourceCPU, "default", "1500m").Obj(),
+				"eng-beta/b1":  *utiltesting.MakeAdmission("other-beta").Assignment(corev1.ResourceCPU, "default", "1500m").Obj(),
+			},
+		},
+		"multiple preemptions preemption possible after earlier workload fits": {
+			// When one workload is assigned Fit,
+			// and another Preempt, the Fit workload doesn't block
+			// the preempting workload in the same cycle.
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "1").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("other-beta").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("other", "eng-alpha").ClusterQueue("other-alpha").Obj(),
+				*utiltesting.MakeLocalQueue("other", "eng-beta").ClusterQueue("other-beta").Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "eng-beta").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1500m").
+					ReserveQuota(utiltesting.MakeAdmission("other-beta").Assignment(corev1.ResourceCPU, "default", "1500m").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("fit", "eng-alpha").
+					Priority(100).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1").
+					Obj(),
+				*utiltesting.MakeWorkload("preemptor", "eng-beta").
+					Priority(99).
+					Queue("other").
+					Request(corev1.ResourceCPU, "2").
+					Obj(),
+			},
+			wantPreempted: sets.New("eng-beta/b1"),
+			wantLeft: map[string][]string{
+				"other-beta": {"eng-beta/preemptor"},
+			},
+			wantAssignments: map[string]kueue.Admission{
+				"eng-alpha/fit": *utiltesting.MakeAdmission("other-alpha").Assignment(corev1.ResourceCPU, "default", "1").Obj(),
+				"eng-beta/b1":   *utiltesting.MakeAdmission("other-beta").Assignment(corev1.ResourceCPU, "default", "1500m").Obj(),
+			},
+			wantScheduled: []string{"eng-alpha/fit"},
+		},
+		"multiple preemptions skip preemption when shared limited resource": {
+			// The two preempting workloads, each requesting 3 CPU,
+			// require capacity in the Cohort in addition to preemption. We make sure
+			// that we don't do a wasteful preemption, as only one of the
+			// workloads can fit even after two preemptions.
+			//
+			// Evicted workloads: request 4
+			// Preempting workloads: request 6
+			// Cohort has capacity 5
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+						BorrowWithinCohort: &kueue.BorrowWithinCohort{
+							Policy: kueue.BorrowWithinCohortPolicyLowerPriority,
+						},
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("other-beta").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+						BorrowWithinCohort: &kueue.BorrowWithinCohort{
+							Policy: kueue.BorrowWithinCohortPolicyLowerPriority,
+						},
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("resource-bank").
+					Cohort("other").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "1").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("other", "eng-alpha").ClusterQueue("other-alpha").Obj(),
+				*utiltesting.MakeLocalQueue("other", "eng-beta").ClusterQueue("other-beta").Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "eng-alpha").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "2").
+					ReserveQuota(utiltesting.MakeAdmission("other-alpha").Assignment(corev1.ResourceCPU, "default", "2").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("b1", "eng-beta").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "2").
+					ReserveQuota(utiltesting.MakeAdmission("other-beta").Assignment(corev1.ResourceCPU, "default", "2").Obj()).
+					Obj(),
+				*utiltesting.MakeWorkload("preemptor", "eng-alpha").
+					Priority(100).
+					Queue("other").
+					Request(corev1.ResourceCPU, "3").
+					Obj(),
+				*utiltesting.MakeWorkload("pretending-preemptor", "eng-beta").
+					Priority(99).
+					Queue("other").
+					Request(corev1.ResourceCPU, "3").
+					Obj(),
+			},
+			wantPreempted: sets.New("eng-alpha/a1"),
+			wantLeft: map[string][]string{
+				"other-alpha": {"eng-alpha/preemptor"},
+				"other-beta":  {"eng-beta/pretending-preemptor"},
+			},
+			wantAssignments: map[string]kueue.Admission{
+				"eng-alpha/a1": *utiltesting.MakeAdmission("other-alpha").Assignment(corev1.ResourceCPU, "default", "2").Obj(),
+				"eng-beta/b1":  *utiltesting.MakeAdmission("other-beta").Assignment(corev1.ResourceCPU, "default", "2").Obj(),
+			},
+		},
+		"multiple preemptions possible when borrowing": {
+			// While requiring the same FlavorResource (Default, CPU),
+			// 3 workloads can issue preemptions.
+
+			// We assign workloads from each queue a unique resource flavor,
+			// to make the preemptions deterministic.
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+						BorrowWithinCohort: &kueue.BorrowWithinCohort{
```

  I would remove this test case, as it feels a bit artificial, and I would like to keep the flexibility of changing the algorithm without having to worry about breaking this case.

- **pkg/scheduler/scheduler_test.go:2826** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T19:46:50Z
```diff
@@ -2762,7 +3349,7 @@ func TestResourcesToReserve(t *testing.T) {
 		},
 	}
 	for _, tc := range cases {
-		t.Run(tc.name, func(t *testing.T) {
```

  It doesn't depend on the gate, so undo this to avoid increase tests' running time. I know it might be negligible, but we might forget about the possibility of optimizing this one if we ever need to optimize.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-19T10:31:36Z

- **pkg/scheduler/scheduler_test.go:83** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T07:22:46Z
```diff
@@ -60,6 +60,29 @@ var cmpDump = []cmp.Option{
 	cmpopts.SortSlices(func(a, b string) bool { return a < b }),
 }
 
+// Indicates under which configuration of the MultiplePreemptions flag the test passes.
+type multiplePreemptionsCompatibility int
+
+func (mode multiplePreemptionsCompatibility) runTest(baseName string, t *testing.T, f func(t *testing.T)) {
+	if mode == Both || mode == Legacy {
+		defer features.SetFeatureGateDuringTest(t, features.MultiplePreemptions, false)()
+		t.Run(baseName+" LegacyMode", f)
+	}
+	if mode == Both || mode == MultiplePremptions {
+		defer features.SetFeatureGateDuringTest(t, features.MultiplePreemptions, true)()
+		t.Run(baseName+" MultiplePreemptionsMode", f)
+	}
+}
+
+const (
+	// Default. Run test with both Legacy and MultiplePreemptions code.
+	Both multiplePreemptionsCompatibility = iota
+	// Run test only with Legacy code.
+	Legacy
+	// Run test only with MultiplePreemptions code.
+	MultiplePremptions
```

  I intended `Both` to be default value, so that in the `TestSchedule` suite it runs both tests if we don't explicitly set to one or the other

- **pkg/scheduler/scheduler.go:371** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T07:22:53Z
```diff
@@ -322,6 +357,26 @@ type entry struct {
 	preemptionTargets     []*preemption.Target
 }
 
+// NetUsage returns how much capacity this entry will require from the ClusterQueue/Cohort.
+// When a workload is preempting, it subtracts the preempted resources from the resources
+// required, as the remaining quota is all we need from the CQ/Cohort.
+func (e *entry) NetUsage() resources.FlavorResourceQuantitiesFlat {
+	if e.assignment.RepresentativeMode() == flavorassigner.Fit {
+		return e.assignment.Usage
+	}
+
+	usage := maps.Clone(e.assignment.Usage)
+	for target := range e.preemptionTargets {
+		for fr, v := range e.preemptionTargets[target].WorkloadInfo.FlavorResourceUsage() {
+			if _, hasFlavor := usage[fr]; !hasFlavor {
```

  Done.

- **pkg/scheduler/scheduler.go:363** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T07:22:57Z
```diff
@@ -322,6 +357,26 @@ type entry struct {
 	preemptionTargets     []*preemption.Target
 }
 
+// NetUsage returns how much capacity this entry will require from the ClusterQueue/Cohort.
+// When a workload is preempting, it subtracts the preempted resources from the resources
+// required, as the remaining quota is all we need from the CQ/Cohort.
+func (e *entry) NetUsage() resources.FlavorResourceQuantitiesFlat {
```

  Done.

- **pkg/scheduler/scheduler_test.go:927** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T10:09:03Z
```diff
@@ -896,6 +921,11 @@ func TestSchedule(t *testing.T) {
 			},
 		},
 		"no overadmission while borrowing": {
+			// we disable this test for MultiplePreemption
+			// logic, as the new logic considers this
+			// unschedulable, while the old logic considers
+			// it skipped.
```

  exactly, but rather in `wantInadmissibleLeft`. I added a duplicate test case, with that minor change, to cover the new logic.
  
  as a follow up PR, we could update the legacy logic to treat this case as `inadmissible` as well.

- **pkg/scheduler/scheduler_test.go:1689** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T10:31:15Z
```diff
@@ -1651,6 +1681,563 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			//
+			// Note: this test case passes for legacy logic when
```

  [this line](https://github.com/kubernetes-sigs/kueue/blob/a34134049e49c98cd17ed63a336d2a0305194da7/pkg/scheduler/scheduler.go#L381) - when usage is equal to nominal, we set our cycleCohortsUsage to 0. We are checking that cycleCohortsUsage is > 0, rather than a membership check - a subtle change in https://github.com/kubernetes-sigs/kueue/pull/2622/files#r1684180659 which I'm reverting in #2646.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-19T12:00:00Z

- **pkg/scheduler/scheduler_test.go:1726** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T12:00:00Z
```diff
@@ -1651,6 +1681,563 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			//
+			// Note: this test case passes for legacy logic when
+			// the CQ's usage is equal to nominal capacity.
+			// Therefore, we set the usage of preempted workloads
+			// to less than nominal capacity, so this test fails
+			// in legacy logic.
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("other-beta").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("other", "eng-alpha").ClusterQueue("other-alpha").Obj(),
+				*utiltesting.MakeLocalQueue("other", "eng-beta").ClusterQueue("other-beta").Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "eng-alpha").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1500m").
+					ReserveQuota(utiltesting.MakeAdmission("other-alpha").Assignment(corev1.ResourceCPU, "default", "1500m").Obj()).
```

  ack for the future
  
  i will not update existing test cases (if we do, we can do this all at once, when trying to reduce verbosity)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-19T12:47:10Z

- **pkg/scheduler/scheduler_test.go:1725** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T12:47:10Z
```diff
@@ -1651,6 +1681,563 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			//
+			// Note: this test case passes for legacy logic when
+			// the CQ's usage is equal to nominal capacity.
+			// Therefore, we set the usage of preempted workloads
+			// to less than nominal capacity, so this test fails
+			// in legacy logic.
+			multiplePreemptions: MultiplePremptions,
+			additionalClusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("other-alpha").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+				*utiltesting.MakeClusterQueue("other-beta").
+					Cohort("other").
+					Preemption(kueue.ClusterQueuePreemption{
+						WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+					}).
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("default").
+							Resource(corev1.ResourceCPU, "2").Obj(),
+					).
+					Obj(),
+			},
+			additionalLocalQueues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("other", "eng-alpha").ClusterQueue("other-alpha").Obj(),
+				*utiltesting.MakeLocalQueue("other", "eng-beta").ClusterQueue("other-beta").Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "eng-alpha").
+					Priority(0).
+					Queue("other").
+					Request(corev1.ResourceCPU, "1500m").
```

  changed to `2` and removed the comment, as now the test case will fail in the old code after https://github.com/kubernetes-sigs/kueue/pull/2646

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-19T13:32:37Z

- **pkg/scheduler/scheduler_test.go:1809** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-19T13:32:36Z
```diff
@@ -1651,6 +1802,428 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			multiplePreemptions: MultiplePremptions,
```

  can't this be left for both?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-19T13:34:57Z

- **pkg/scheduler/scheduler_test.go:1809** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T13:34:57Z
```diff
@@ -1651,6 +1802,428 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			multiplePreemptions: MultiplePremptions,
```

  Nope, the previous logic would trigger the skip preemption logic, as it counted overlapping FlavorResource.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-19T13:36:58Z

- **pkg/scheduler/scheduler_test.go:1809** by [@gabesaba](https://github.com/gabesaba) — 2024-07-19T13:36:58Z
```diff
@@ -1651,6 +1802,428 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			multiplePreemptions: MultiplePremptions,
```

  we added a test case to cover this for the previous logic in https://github.com/kubernetes-sigs/kueue/pull/2646

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-19T14:04:13Z

- **pkg/scheduler/scheduler_test.go:1809** by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-19T14:04:12Z
```diff
@@ -1651,6 +1802,428 @@ func TestSchedule(t *testing.T) {
 				"eng-gamma/gamma4":   *utiltesting.MakeAdmission("eng-gamma").Assignment(corev1.ResourceCPU, "on-demand", "20").Obj(),
 			},
 		},
+		"multiple preemptions without borrowing": {
+			// While requiring the same shared FlavorResource (Default, cpu),
+			// multiple workloads are able to issue preemptions on workloads within
+			// their own CQs in a single scheduling cycle.
+			multiplePreemptions: MultiplePremptions,
```

  oh, true, true, this is what we are trying to fix lol

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-07-19T14:04:41Z

/lgtm
/approve
