# PR #265: Feat: add internal cert to replace cert manager

**Summary**: Feat: add internal cert to replace cert manager

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/265

**Last updated**: 2022-06-08T16:13:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feat/add-internal-cert` → `main`
- **Created**: 2022-05-17T03:18:44Z
- **Updated**: 2022-06-08T16:13:40Z
- **Merged**: 2022-06-08T16:12:23Z
- **Closed**: 2022-06-08T16:12:23Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 12   **+216 / -64**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/232#event-6620500497

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-08T14:20:31Z

lgtm, can you squash?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-06-08T15:47:23Z

> lgtm, can you squash?

squashed.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-08T16:10:58Z

/approve
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-06-08T16:11:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/265#issuecomment-1150118247" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/265#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-06T19:19:37Z

- **apis/kueue/v1alpha1/webhook.go:17** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T18:48:28Z
```diff
@@ -0,0 +1,65 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha1
```

  This is not the appropriate package for this code.
  
  Maybe `pkg/util/cert` or `pkg/util/webhook`?

- **config/default/kustomization.yaml:44** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T18:53:43Z
```diff
@@ -38,37 +39,37 @@ patchesStrategicMerge:
 # crd/kustomization.yaml
 - manager_webhook_patch.yaml
 
-# [CERTMANAGER] To enable cert-manager, uncomment all sections with 'CERTMANAGER'.
-# Uncomment 'CERTMANAGER' sections in crd/kustomization.yaml to enable the CA injection in the admission webhooks.
-# 'CERTMANAGER' needs to be enabled to use ca injection
-- webhookcainjection_patch.yaml
+# # [CERTMANAGER] To enable cert-manager, uncomment all sections with 'CERTMANAGER'.
+# # Uncomment 'CERTMANAGER' sections in crd/kustomization.yaml to enable the CA injection in the admission webhooks.
+# # 'CERTMANAGER' needs to be enabled to use ca injection
```

  why another `#`?

- **config/internalcert/kustomization.yaml:2** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T18:54:49Z
```diff
@@ -0,0 +1,2 @@
+resources:
+- manifests.yaml
```

  ```suggestion
  - secret.yaml
  ```

- **apis/kueue/v1alpha1/webhook.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T19:00:11Z
```diff
@@ -0,0 +1,65 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha1
+
+import (
+	"fmt"
+
+	cert "github.com/open-policy-agent/cert-controller/pkg/rotator"
+	"k8s.io/apimachinery/pkg/types"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+const (
+	serviceName     = "kueue-webhook-service"
+	secretName      = "kueue-webhook-server-cert"
```

  you should be passing these through the component config that is populated by kustomize https://github.com/kubernetes-sigs/kueue/blob/main/config/manager/controller_manager_config.yaml
  Although we can leave it for a follow up.

- **main.go:183** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T19:12:46Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
+
+	ctx := ctrl.SetupSignalHandler()
+	go func() {
+		queues.CleanUpOnContext(ctx)
+	}()
+
+	startScheduler(ctx, mgr, cCache, queues, certsReady)
+
+	setupLog.Info("starting manager")
+	if err := mgr.Start(ctx); err != nil {
+		setupLog.Error(err, "problem running manager")
+		os.Exit(1)
+	}
+}
+
+func setupIndexes(mgr ctrl.Manager) error {
 	if err := queue.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
-		setupLog.Error(err, "Unable to setup queue indexes")
+		return err
 	}
 	if err := cache.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
-		setupLog.Error(err, "Unable to setup cache indexes")
+		return err
+	}
+	if err := job.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
+		return err
 	}
+	return nil
+}
+
+func startControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manager, certsReady chan struct{}, manageJobsWithoutQueueName bool) {
+	// The controllers won't work until the webhooks are operating, and those won't work until the
+	// certs are all in place.
+	setupLog.Info("Waiting for certificate generation to complete")
+	<-certsReady
+	setupLog.Info("Certs ready")
 
-	cCache := cache.New(mgr.GetClient())
-	queues := queue.NewManager(mgr.GetClient(), cCache)
 	if failedCtrl, err := core.SetupControllers(mgr, queues, cCache); err != nil {
 		setupLog.Error(err, "Unable to create controller", "controller", failedCtrl)
+		os.Exit(1)
 	}
-	if err = job.NewReconciler(mgr.GetScheme(),
+	if err := job.NewReconciler(mgr.GetScheme(),
 		mgr.GetClient(),
 		mgr.GetEventRecorderFor(constants.JobControllerName),
-		job.WithManageJobsWithoutQueueName(config.ManageJobsWithoutQueueName),
+		job.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
 	).SetupWithManager(mgr); err != nil {
 		setupLog.Error(err, "unable to create controller", "controller", "Job")
 		os.Exit(1)
 	}
-	if err = (&kueuev1alpha1.Workload{}).SetupWebhookWithManager(mgr); err != nil {
+	if err := (&kueuev1alpha1.Workload{}).SetupWebhookWithManager(mgr); err != nil {
 		setupLog.Error(err, "unable to create webhook", "webhook", "Workload")
 		os.Exit(1)
 	}
 	//+kubebuilder:scaffold:builder
+}
+
+// setupProbeEndpoints registers the health endpoints
+func setupProbeEndpoints(mgr ctrl.Manager, certsReady chan struct{}) {
```

  why pass `certsReady`?

- **main.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T19:14:32Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
+
+	ctx := ctrl.SetupSignalHandler()
+	go func() {
+		queues.CleanUpOnContext(ctx)
+	}()
+
+	startScheduler(ctx, mgr, cCache, queues, certsReady)
+
+	setupLog.Info("starting manager")
+	if err := mgr.Start(ctx); err != nil {
+		setupLog.Error(err, "problem running manager")
+		os.Exit(1)
+	}
+}
+
+func setupIndexes(mgr ctrl.Manager) error {
 	if err := queue.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
-		setupLog.Error(err, "Unable to setup queue indexes")
+		return err
```

  log here instead of returning, so that we can at least configure the other indexes.

- **main.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T19:18:12Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
+
+	ctx := ctrl.SetupSignalHandler()
+	go func() {
+		queues.CleanUpOnContext(ctx)
+	}()
+
+	startScheduler(ctx, mgr, cCache, queues, certsReady)
+
+	setupLog.Info("starting manager")
+	if err := mgr.Start(ctx); err != nil {
+		setupLog.Error(err, "problem running manager")
+		os.Exit(1)
+	}
+}
+
+func setupIndexes(mgr ctrl.Manager) error {
 	if err := queue.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
-		setupLog.Error(err, "Unable to setup queue indexes")
+		return err
 	}
 	if err := cache.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
-		setupLog.Error(err, "Unable to setup cache indexes")
+		return err
+	}
+	if err := job.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
+		return err
 	}
+	return nil
+}
+
+func startControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manager, certsReady chan struct{}, manageJobsWithoutQueueName bool) {
```

  `setupControllers`

- **main.go:157** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T19:18:22Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
+
+	ctx := ctrl.SetupSignalHandler()
+	go func() {
+		queues.CleanUpOnContext(ctx)
+	}()
+
+	startScheduler(ctx, mgr, cCache, queues, certsReady)
+
+	setupLog.Info("starting manager")
+	if err := mgr.Start(ctx); err != nil {
+		setupLog.Error(err, "problem running manager")
+		os.Exit(1)
+	}
+}
+
+func setupIndexes(mgr ctrl.Manager) error {
 	if err := queue.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
-		setupLog.Error(err, "Unable to setup queue indexes")
+		return err
 	}
 	if err := cache.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
-		setupLog.Error(err, "Unable to setup cache indexes")
+		return err
+	}
+	if err := job.SetupIndexes(mgr.GetFieldIndexer()); err != nil {
+		return err
 	}
+	return nil
+}
+
+func startControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manager, certsReady chan struct{}, manageJobsWithoutQueueName bool) {
+	// The controllers won't work until the webhooks are operating, and those won't work until the
```

  ```suggestion
  	// The controllers won't work until the webhooks are operating, and the webhooks won't work until the
  ```

- **main.go:127** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T19:18:51Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  why in a routine?

- **main.go:134** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-06T19:19:32Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
+
+	ctx := ctrl.SetupSignalHandler()
+	go func() {
+		queues.CleanUpOnContext(ctx)
+	}()
+
+	startScheduler(ctx, mgr, cCache, queues, certsReady)
```

  it's just two lines, why a function?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-06-07T07:05:42Z

- **config/default/kustomization.yaml:44** by [@kerthcet](https://github.com/kerthcet) — 2022-06-07T05:52:29Z
```diff
@@ -38,37 +39,37 @@ patchesStrategicMerge:
 # crd/kustomization.yaml
 - manager_webhook_patch.yaml
 
-# [CERTMANAGER] To enable cert-manager, uncomment all sections with 'CERTMANAGER'.
-# Uncomment 'CERTMANAGER' sections in crd/kustomization.yaml to enable the CA injection in the admission webhooks.
-# 'CERTMANAGER' needs to be enabled to use ca injection
-- webhookcainjection_patch.yaml
+# # [CERTMANAGER] To enable cert-manager, uncomment all sections with 'CERTMANAGER'.
+# # Uncomment 'CERTMANAGER' sections in crd/kustomization.yaml to enable the CA injection in the admission webhooks.
+# # 'CERTMANAGER' needs to be enabled to use ca injection
```

  Removed.

- **main.go:127** by [@kerthcet](https://github.com/kerthcet) — 2022-06-07T05:59:24Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  controllers are blocked by certsReady https://github.com/kubernetes-sigs/kueue/blob/6bf71a423947f6e86105d8b923f70f9133359d3e/main.go#L157-L161

- **main.go:134** by [@kerthcet](https://github.com/kerthcet) — 2022-06-07T06:05:16Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
+
+	ctx := ctrl.SetupSignalHandler()
+	go func() {
+		queues.CleanUpOnContext(ctx)
+	}()
+
+	startScheduler(ctx, mgr, cCache, queues, certsReady)
```

  Just like `startControllers`, hiding the details about how to start scheduler, although we have only two lines.

- **apis/kueue/v1alpha1/webhook.go:29** by [@kerthcet](https://github.com/kerthcet) — 2022-06-07T06:58:17Z
```diff
@@ -0,0 +1,65 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha1
+
+import (
+	"fmt"
+
+	cert "github.com/open-policy-agent/cert-controller/pkg/rotator"
+	"k8s.io/apimachinery/pkg/types"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+const (
+	serviceName     = "kueue-webhook-service"
+	secretName      = "kueue-webhook-server-cert"
```

  issue https://github.com/kubernetes-sigs/kueue/issues/270

- **apis/kueue/v1alpha1/webhook.go:17** by [@kerthcet](https://github.com/kerthcet) — 2022-06-07T07:05:39Z
```diff
@@ -0,0 +1,65 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha1
```

  Moved to `pkg/util/cert`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-07T14:56:13Z

- **main.go:127** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-07T14:56:13Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  why not do that in the main routine? Otherwise, `mgr.Start` in the main routine could be called before all the controllers are registered on it.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-06-08T07:15:50Z

- **main.go:127** by [@kerthcet](https://github.com/kerthcet) — 2022-06-08T07:15:49Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  Cert won't be ready until manager starts, add a comment here. 
  Controllers can be added after manager starts and will started immediately. See here: 
  https://github.com/kubernetes-sigs/controller-runtime/blob/0c2effbc7eabc502eb79472b6c6ba8fbb6ec8b76/pkg/manager/runnable_group.go#L49-L69

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-08T13:14:52Z

- **main.go:127** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-08T13:14:52Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  What's the point of starting an empty manager?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-06-08T14:06:51Z

- **main.go:127** by [@kerthcet](https://github.com/kerthcet) — 2022-06-08T14:06:51Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  We register a cert controller to the manager by `cert.ManageCerts(mgr)`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-08T14:18:19Z

- **main.go:127** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-08T14:18:19Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  I see. Does the cert controller need to run before the certs can be ready?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-08T14:18:52Z

- **main.go:127** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-08T14:18:52Z
```diff
@@ -108,31 +109,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = kueuev1alpha1.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	if err := setupIndexes(mgr); err != nil {
+		setupLog.Error(err, "Unable to setup indexes")
+	}
+
+	setupProbeEndpoints(mgr, certsReady)
+	go startControllers(mgr, cCache, queues, certsReady, config.ManageJobsWithoutQueueName)
```

  ah ok, that's the comment you added. Thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-08T14:20:20Z

- **main.go:128** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-08T14:19:40Z
```diff
@@ -108,31 +110,79 @@ func main() {
 		setupLog.Error(err, "unable to start manager")
 		os.Exit(1)
 	}
+
+	certsReady := make(chan struct{})
+	if err = cert.ManageCerts(mgr, certsReady); err != nil {
+		setupLog.Error(err, "unable to set up cert rotation")
+		os.Exit(1)
+	}
+
+	cCache := cache.New(mgr.GetClient())
+	queues := queue.NewManager(mgr.GetClient(), cCache)
+
+	setupIndexes(mgr)
+
+	setupProbeEndpoints(mgr)
+	// Cert won't be ready until manager starts, so start a goroutine here which
+	// will block until the cert is ready before setting up the controllers.
+	// Controllers register after manager starts will start directly.
```

  ```suggestion
  	// Controllers register after manager starts and will start directly.
  ```
