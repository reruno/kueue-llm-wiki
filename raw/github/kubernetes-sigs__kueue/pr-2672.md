# PR #2672: [kjobctl] Adds RayJob mode.

**Summary**: [kjobctl] Adds RayJob mode.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2672

**Last updated**: 2024-08-05T15:37:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/kjobctl-add-rayjob-mode` → `main`
- **Created**: 2024-07-22T15:52:26Z
- **Updated**: 2024-08-05T15:37:39Z
- **Merged**: 2024-08-05T15:37:03Z
- **Closed**: 2024-08-05T15:37:03Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mwielgus](https://github.com/mwielgus), [@trasc](https://github.com/trasc)
- **Changed files**: 34   **+27855 / -62**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Adds RayJob mode on kjobctl.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-22T15:52:29Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-22T15:52:43Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e5ba786f5d03e06a5a252187c4b85c7716072bb6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66b0ed793370dc00080693c7 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-25T07:25:15Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-26T06:35:14Z

/assign @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-30T07:23:18Z

LGTM label has been added.  <details>Git tree hash: c43ed93da2a5320724a2ddc3b3ae358fb0af624b</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-31T09:44:04Z

/cc @mwielgus @mwysokin

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-31T09:44:07Z

@mbobrovskyi: GitHub didn't allow me to request PR reviews from the following users: mwysokin.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2672#issuecomment-2260105546):

>/cc @mwielgus @mwysokin 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mwysokin](https://github.com/mwysokin) — 2024-08-05T11:52:14Z

Just your friendly neighborhood shadow reviewer here. 

Overall, the code looks good, but I'm a bit concerned about some of the modules being used. They're a few years old and don't seem to be actively maintained anymore.  Not sure why we're going with these specific choices, but I'm wondering if there's any risk in using outdated dependencies for a brand new component.

Also, the examples seem to be copied directly from KubeRay and are referencing some pretty old Python packages (which can sometimes lead to issues with unsupported/outdated TLS certificates because ). I get that these examples might be pulled from another project, but I'm not entirely sure it's a good idea to just copy and paste them into our own without careful consideration.

One last thing: should we maybe mention somewhere that these examples are straight from KubeRay (https://raw.githubusercontent.com/ray-project/kuberay/v1.1.1/ray-operator/config/samples/ray-job.sample.yaml)? Just to be transparent.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-05T12:08:31Z

> Overall, the code looks good, but I'm a bit concerned about some of the modules being used. They're a few years old and don't seem to be actively maintained anymore. Not sure why we're going with these specific choices, but I'm wondering if there's any risk in using outdated dependencies for a brand new component.

Sorry, what dependencies do you mean exactly?

### Comment by [@mwysokin](https://github.com/mwysokin) — 2024-08-05T12:36:22Z

> > Overall, the code looks good, but I'm a bit concerned about some of the modules being used. They're a few years old and don't seem to be actively maintained anymore. Not sure why we're going with these specific choices, but I'm wondering if there's any risk in using outdated dependencies for a brand new component.
> 
> Sorry, what dependencies do you mean exactly?

@mbobrovskyi The ones I made comments for so: `groupcache`, `beorn7/perks`, `matttproud/golang_protobuf_extensions`.

Also please take my comments with a grain of salt. I'm new to the project and learning what's good or wrong as I go 🙇

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-05T13:21:58Z

> @mbobrovskyi The ones I made comments for so: groupcache, beorn7/perks, matttproud/golang_protobuf_extensions.

I think I got what you mean. There is a list of dependencies that are automatically added by `go mod tidy` and used by other dependencies. So it's okay that there're old and we don't need to change it to avoid conflicts.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-05T15:35:29Z

> @mbobrovskyi The ones I made comments for so: `groupcache`, `beorn7/perks`, `matttproud/golang_protobuf_extensions`.

I think we are ok with these dependencies since they already are part of the main [go.mod](https://github.com/kubernetes-sigs/kueue/blob/main/go.mod).

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-05T15:35:36Z

/lgtm
/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-05T15:35:42Z

LGTM label has been added.  <details>Git tree hash: f4653fb36b49d57877f2dc2128750cfc09a37ebe</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-05T15:35:45Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2672#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2672#issuecomment-2269361894" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-26T12:29:36Z

1st pass

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:71** by [@trasc](https://github.com/trasc) — 2024-07-26T11:34:38Z
```diff
@@ -60,10 +60,21 @@ const (
 	--profile my-application-profile  \
 	--pod-running-timeout 30s \
 	--rm`
+	createRayJobExample = `  # Create job 
+  kjobctl create rayjob \ 
+	--profile my-application-profile  \
+	--request cpu=500m,ram=4Gi \
```

  I don't think this is used

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:185** by [@trasc](https://github.com/trasc) — 2024-07-26T12:14:02Z
```diff
@@ -138,15 +154,31 @@ var createModeSubcommands = map[string]modeSubcommand{
 	"interactive": {
 		ModeName: v1alpha1.InteractiveMode,
 		Setup: func(subcmd *cobra.Command, o *CreateOptions) {
-			subcmd.Use += " [--pod-running-timeout DURATION] [--rm]"
+			subcmd.Use += " [--cmd COMMAND] [--pod-running-timeout DURATION] [--rm]"
 			subcmd.Short = "Create an interactive shell"
 			subcmd.Example = createInteractiveExample
+			subcmd.Flags().StringVar(&o.UserSpecifiedCommand, commandFlagName, "",
+				"Command which is associated with the resource.")
 			subcmd.Flags().DurationVar(&o.PodRunningTimeout, podRunningTimeout, podRunningTimeoutDefault,
 				"The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running.")
 			subcmd.Flags().BoolVar(&o.RemoveInteractivePod, "rm", false,
 				"Remove pod when interactive session exits.")
 		},
 	},
+	"rayjob": {
+		ModeName: v1alpha1.RayJobMode,
+		Setup: func(subcmd *cobra.Command, o *CreateOptions) {
+			subcmd.Use += " [--replicas [WORKER_GROUP]=REPLICAS] [--min-replicas [WORKER_GROUP]=MIN_REPLICAS] [--max-replicas [WORKER_GROUP]=MAX_REPLICAS]"
+			subcmd.Short = "Create a rayjob"
+			subcmd.Example = createRayJobExample
+			subcmd.Flags().StringToIntVar(&o.Replicas, replicasFlagName, nil,
+				"Replicas is the number of desired Pods for this worker group.")
+			subcmd.Flags().StringToIntVar(&o.MinReplicas, minReplicasFlagName, nil,
+				"MinReplicas denotes the minimum number of desired Pods for this worker group.")
+			subcmd.Flags().StringToIntVar(&o.MaxReplicas, maxReplicasFlagName, nil,
+				"MaxReplicas denotes the maximum number of desired Pods for this worker group, and the default value is maxInt32.")
+		},
```

  We should keep the `cmd` flag for ray as well, a job user ca chose to run a different file from a volume mount provided by the profile.
  
  Without this, just being able run the profile with different replicas is a very limiting UX.

- **cmd/experimental/kjobctl/pkg/builder/ray_job_builder.go:88** by [@trasc](https://github.com/trasc) — 2024-07-26T12:17:35Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/utils/ptr"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type rayJobBuilder struct {
+	*Builder
+}
+
+var _ builder = (*rayJobBuilder)(nil)
+
+func (b *rayJobBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.kjobctlClientset.KjobctlV1alpha1().RayJobTemplates(b.profile.Namespace).
+		Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	rayJob := &rayv1.RayJob{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "RayJob",
+			APIVersion: "ray.io/v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		rayJob.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	if len(b.localQueue) > 0 {
+		rayJob.ObjectMeta.Labels[kueueconstants.QueueLabel] = b.localQueue
+	}
+
+	if rayJob.Spec.RayClusterSpec != nil {
+		for index := range rayJob.Spec.RayClusterSpec.WorkerGroupSpecs {
+			workerGroupSpec := &rayJob.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+			if replicas, ok := b.replicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.Replicas = ptr.To(int32(replicas))
+			}
+			if minReplicas, ok := b.minReplicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.MinReplicas = ptr.To(int32(minReplicas))
+			}
+			if maxReplicas, ok := b.maxReplicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.MaxReplicas = ptr.To(int32(maxReplicas))
+			}
+
+			workerGroupSpec.Template.Spec = b.buildPodSpec(workerGroupSpec.Template.Spec)
+		}
+	}
+
```

  We should propagate the volumes, mounts and env-vars.

- **cmd/experimental/kjobctl/config/samples/application_profile.yaml:129** by [@trasc](https://github.com/trasc) — 2024-07-26T12:24:01Z
```diff
@@ -45,3 +48,143 @@ template:
                 cpu: "1"
                 memory: "200Mi"
         restartPolicy: OnFailure
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
+kind: RayJobTemplate
+metadata:
+  name: sample-ray-job-template
+  namespace: default
+template:
+  spec:
+    # submissionMode specifies how RayJob submits the Ray job to the RayCluster.
+    # The default value is "K8sJobMode", meaning RayJob will submit the Ray job via a submitter Kubernetes Job.
+    # The alternative value is "HTTPMode", indicating that KubeRay will submit the Ray job by sending an HTTP request to the RayCluster.
+    # submissionMode: "K8sJobMode"
+    entrypoint: python /home/ray/samples/sample_code.py
+    # shutdownAfterJobFinishes specifies whether the RayCluster should be deleted after the RayJob finishes. Default is false.
+    # shutdownAfterJobFinishes: false
+
+    # ttlSecondsAfterFinished specifies the number of seconds after which the RayCluster will be deleted after the RayJob finishes.
+    # ttlSecondsAfterFinished: 10
+
+    # activeDeadlineSeconds is the duration in seconds that the RayJob may be active before
+    # KubeRay actively tries to terminate the RayJob; value must be positive integer.
+    # activeDeadlineSeconds: 120
+
+    # RuntimeEnvYAML represents the runtime environment configuration provided as a multi-line YAML string.
+    # See https://docs.ray.io/en/latest/ray-core/handling-dependencies.html for details.
+    # (New in KubeRay version 1.0.)
+    runtimeEnvYAML: |
+      pip:
+        - requests==2.26.0
+        - pendulum==2.1.2
+      env_vars:
+        counter_name: "test_counter"
+
+    # Suspend specifies whether the RayJob controller should create a RayCluster instance.
+    # If a job is applied with the suspend field set to true, the RayCluster will not be created and we will wait for the transition to false.
+    # If the RayCluster is already created, it will be deleted. In the case of transition to false, a new RayCluster will be created.
+    # suspend: false
+
+    # rayClusterSpec specifies the RayCluster instance to be created by the RayJob controller.
+    rayClusterSpec:
+      rayVersion: '2.9.0' # should match the Ray version in the image of the containers
+      # Ray head pod template
+      headGroupSpec:
+        # The `rayStartParams` are used to configure the `ray start` command.
+        # See https://github.com/ray-project/kuberay/blob/master/docs/guidance/rayStartParams.md for the default settings of `rayStartParams` in KubeRay.
+        # See https://docs.ray.io/en/latest/cluster/cli.html#ray-start for all available options in `rayStartParams`.
+        rayStartParams:
+          dashboard-host: '0.0.0.0'
+        #pod template
+        template:
+          spec:
+            containers:
+              - name: ray-head
+                image: rayproject/ray:2.9.0
+                ports:
+                  - containerPort: 6379
+                    name: gcs-server
+                  - containerPort: 8265 # Ray dashboard
+                    name: dashboard
+                  - containerPort: 10001
+                    name: client
+                resources:
+                  limits:
+                    cpu: "1"
+                  requests:
+                    cpu: "200m"
+                volumeMounts:
+                  - mountPath: /home/ray/samples
+                    name: code-sample
+            volumes:
+              # You set volumes at the Pod level, then mount them into containers inside that Pod
+              - name: code-sample
+                configMap:
+                  # Provide the name of the ConfigMap you want to mount.
+                  name: ray-job-code-sample
+                  # An array of keys from the ConfigMap to create as files
+                  items:
+                    - key: sample_code.py
+                      path: sample_code.py
```

  let's make this mount come from a VolumeBundle.

- **cmd/experimental/kjobctl/config/samples/application_profile.yaml:63** by [@trasc](https://github.com/trasc) — 2024-07-26T12:25:52Z
```diff
@@ -45,3 +48,143 @@ template:
                 cpu: "1"
                 memory: "200Mi"
         restartPolicy: OnFailure
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
+kind: RayJobTemplate
+metadata:
+  name: sample-ray-job-template
+  namespace: default
+template:
+  spec:
+    # submissionMode specifies how RayJob submits the Ray job to the RayCluster.
+    # The default value is "K8sJobMode", meaning RayJob will submit the Ray job via a submitter Kubernetes Job.
+    # The alternative value is "HTTPMode", indicating that KubeRay will submit the Ray job by sending an HTTP request to the RayCluster.
+    # submissionMode: "K8sJobMode"
+    entrypoint: python /home/ray/samples/sample_code.py
```

  Use a VolumeBundle env var as a base.

- **cmd/experimental/kjobctl/config/samples/application_profile.yaml:160** by [@trasc](https://github.com/trasc) — 2024-07-26T12:26:39Z
```diff
@@ -45,3 +48,143 @@ template:
                 cpu: "1"
                 memory: "200Mi"
         restartPolicy: OnFailure
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
+kind: RayJobTemplate
+metadata:
+  name: sample-ray-job-template
+  namespace: default
+template:
+  spec:
+    # submissionMode specifies how RayJob submits the Ray job to the RayCluster.
+    # The default value is "K8sJobMode", meaning RayJob will submit the Ray job via a submitter Kubernetes Job.
+    # The alternative value is "HTTPMode", indicating that KubeRay will submit the Ray job by sending an HTTP request to the RayCluster.
+    # submissionMode: "K8sJobMode"
+    entrypoint: python /home/ray/samples/sample_code.py
+    # shutdownAfterJobFinishes specifies whether the RayCluster should be deleted after the RayJob finishes. Default is false.
+    # shutdownAfterJobFinishes: false
+
+    # ttlSecondsAfterFinished specifies the number of seconds after which the RayCluster will be deleted after the RayJob finishes.
+    # ttlSecondsAfterFinished: 10
+
+    # activeDeadlineSeconds is the duration in seconds that the RayJob may be active before
+    # KubeRay actively tries to terminate the RayJob; value must be positive integer.
+    # activeDeadlineSeconds: 120
+
+    # RuntimeEnvYAML represents the runtime environment configuration provided as a multi-line YAML string.
+    # See https://docs.ray.io/en/latest/ray-core/handling-dependencies.html for details.
+    # (New in KubeRay version 1.0.)
+    runtimeEnvYAML: |
+      pip:
+        - requests==2.26.0
+        - pendulum==2.1.2
+      env_vars:
+        counter_name: "test_counter"
+
+    # Suspend specifies whether the RayJob controller should create a RayCluster instance.
+    # If a job is applied with the suspend field set to true, the RayCluster will not be created and we will wait for the transition to false.
+    # If the RayCluster is already created, it will be deleted. In the case of transition to false, a new RayCluster will be created.
+    # suspend: false
+
+    # rayClusterSpec specifies the RayCluster instance to be created by the RayJob controller.
+    rayClusterSpec:
+      rayVersion: '2.9.0' # should match the Ray version in the image of the containers
+      # Ray head pod template
+      headGroupSpec:
+        # The `rayStartParams` are used to configure the `ray start` command.
+        # See https://github.com/ray-project/kuberay/blob/master/docs/guidance/rayStartParams.md for the default settings of `rayStartParams` in KubeRay.
+        # See https://docs.ray.io/en/latest/cluster/cli.html#ray-start for all available options in `rayStartParams`.
+        rayStartParams:
+          dashboard-host: '0.0.0.0'
+        #pod template
+        template:
+          spec:
+            containers:
+              - name: ray-head
+                image: rayproject/ray:2.9.0
+                ports:
+                  - containerPort: 6379
+                    name: gcs-server
+                  - containerPort: 8265 # Ray dashboard
+                    name: dashboard
+                  - containerPort: 10001
+                    name: client
+                resources:
+                  limits:
+                    cpu: "1"
+                  requests:
+                    cpu: "200m"
+                volumeMounts:
+                  - mountPath: /home/ray/samples
+                    name: code-sample
+            volumes:
+              # You set volumes at the Pod level, then mount them into containers inside that Pod
+              - name: code-sample
+                configMap:
+                  # Provide the name of the ConfigMap you want to mount.
+                  name: ray-job-code-sample
+                  # An array of keys from the ConfigMap to create as files
+                  items:
+                    - key: sample_code.py
+                      path: sample_code.py
+      workerGroupSpecs:
+        # the pod replicas in this group typed worker
+        - replicas: 1
+          minReplicas: 1
+          maxReplicas: 5
+          # logical group name, for this called small-group, also can be functional
+          groupName: small-group
+          # The `rayStartParams` are used to configure the `ray start` command.
+          # See https://github.com/ray-project/kuberay/blob/master/docs/guidance/rayStartParams.md for the default settings of `rayStartParams` in KubeRay.
+          # See https://docs.ray.io/en/latest/cluster/cli.html#ray-start for all available options in `rayStartParams`.
+          rayStartParams: {}
+          #pod template
+          template:
+            spec:
+              containers:
+                - name: ray-worker # must consist of lower case alphanumeric characters or '-', and must start and end with an alphanumeric character (e.g. 'my-name',  or '123-abc'
+                  image: rayproject/ray:2.9.0
+                  lifecycle:
+                    preStop:
+                      exec:
+                        command: [ "/bin/sh","-c","ray stop" ]
+                  resources:
+                    limits:
+                      cpu: "1"
+                    requests:
+                      cpu: "200m"
+---
+apiVersion: v1
+kind: ConfigMap
+metadata:
+  name: ray-job-code-sample
```

  Create a VolumeBundle that will mount this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-26T12:34:19Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:185** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-26T12:34:18Z
```diff
@@ -138,15 +154,31 @@ var createModeSubcommands = map[string]modeSubcommand{
 	"interactive": {
 		ModeName: v1alpha1.InteractiveMode,
 		Setup: func(subcmd *cobra.Command, o *CreateOptions) {
-			subcmd.Use += " [--pod-running-timeout DURATION] [--rm]"
+			subcmd.Use += " [--cmd COMMAND] [--pod-running-timeout DURATION] [--rm]"
 			subcmd.Short = "Create an interactive shell"
 			subcmd.Example = createInteractiveExample
+			subcmd.Flags().StringVar(&o.UserSpecifiedCommand, commandFlagName, "",
+				"Command which is associated with the resource.")
 			subcmd.Flags().DurationVar(&o.PodRunningTimeout, podRunningTimeout, podRunningTimeoutDefault,
 				"The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running.")
 			subcmd.Flags().BoolVar(&o.RemoveInteractivePod, "rm", false,
 				"Remove pod when interactive session exits.")
 		},
 	},
+	"rayjob": {
+		ModeName: v1alpha1.RayJobMode,
+		Setup: func(subcmd *cobra.Command, o *CreateOptions) {
+			subcmd.Use += " [--replicas [WORKER_GROUP]=REPLICAS] [--min-replicas [WORKER_GROUP]=MIN_REPLICAS] [--max-replicas [WORKER_GROUP]=MAX_REPLICAS]"
+			subcmd.Short = "Create a rayjob"
+			subcmd.Example = createRayJobExample
+			subcmd.Flags().StringToIntVar(&o.Replicas, replicasFlagName, nil,
+				"Replicas is the number of desired Pods for this worker group.")
+			subcmd.Flags().StringToIntVar(&o.MinReplicas, minReplicasFlagName, nil,
+				"MinReplicas denotes the minimum number of desired Pods for this worker group.")
+			subcmd.Flags().StringToIntVar(&o.MaxReplicas, maxReplicasFlagName, nil,
+				"MaxReplicas denotes the maximum number of desired Pods for this worker group, and the default value is maxInt32.")
+		},
```

  Do you mean entrypoint? 
  
  https://github.com/kubernetes-sigs/kueue/blob/2d971cff71a8d4ac3bcddf7c0f955b24c6ceb704/cmd/experimental/kjobctl/config/samples/application_profile.yaml#L63

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-26T12:36:18Z

- **cmd/experimental/kjobctl/pkg/builder/ray_job_builder.go:88** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-26T12:36:18Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/utils/ptr"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type rayJobBuilder struct {
+	*Builder
+}
+
+var _ builder = (*rayJobBuilder)(nil)
+
+func (b *rayJobBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.kjobctlClientset.KjobctlV1alpha1().RayJobTemplates(b.profile.Namespace).
+		Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	rayJob := &rayv1.RayJob{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "RayJob",
+			APIVersion: "ray.io/v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		rayJob.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	if len(b.localQueue) > 0 {
+		rayJob.ObjectMeta.Labels[kueueconstants.QueueLabel] = b.localQueue
+	}
+
+	if rayJob.Spec.RayClusterSpec != nil {
+		for index := range rayJob.Spec.RayClusterSpec.WorkerGroupSpecs {
+			workerGroupSpec := &rayJob.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+			if replicas, ok := b.replicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.Replicas = ptr.To(int32(replicas))
+			}
+			if minReplicas, ok := b.minReplicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.MinReplicas = ptr.To(int32(minReplicas))
+			}
+			if maxReplicas, ok := b.maxReplicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.MaxReplicas = ptr.To(int32(maxReplicas))
+			}
+
+			workerGroupSpec.Template.Spec = b.buildPodSpec(workerGroupSpec.Template.Spec)
+		}
+	}
+
```

  We already have it on buildPodSpec. Or you mean to add to headGroup too? 
  
  https://github.com/kubernetes-sigs/kueue/blob/2d971cff71a8d4ac3bcddf7c0f955b24c6ceb704/cmd/experimental/kjobctl/config/samples/application_profile.yaml#L120-L129

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-26T12:44:12Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:185** by [@trasc](https://github.com/trasc) — 2024-07-26T12:44:12Z
```diff
@@ -138,15 +154,31 @@ var createModeSubcommands = map[string]modeSubcommand{
 	"interactive": {
 		ModeName: v1alpha1.InteractiveMode,
 		Setup: func(subcmd *cobra.Command, o *CreateOptions) {
-			subcmd.Use += " [--pod-running-timeout DURATION] [--rm]"
+			subcmd.Use += " [--cmd COMMAND] [--pod-running-timeout DURATION] [--rm]"
 			subcmd.Short = "Create an interactive shell"
 			subcmd.Example = createInteractiveExample
+			subcmd.Flags().StringVar(&o.UserSpecifiedCommand, commandFlagName, "",
+				"Command which is associated with the resource.")
 			subcmd.Flags().DurationVar(&o.PodRunningTimeout, podRunningTimeout, podRunningTimeoutDefault,
 				"The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running.")
 			subcmd.Flags().BoolVar(&o.RemoveInteractivePod, "rm", false,
 				"Remove pod when interactive session exits.")
 		},
 	},
+	"rayjob": {
+		ModeName: v1alpha1.RayJobMode,
+		Setup: func(subcmd *cobra.Command, o *CreateOptions) {
+			subcmd.Use += " [--replicas [WORKER_GROUP]=REPLICAS] [--min-replicas [WORKER_GROUP]=MIN_REPLICAS] [--max-replicas [WORKER_GROUP]=MAX_REPLICAS]"
+			subcmd.Short = "Create a rayjob"
+			subcmd.Example = createRayJobExample
+			subcmd.Flags().StringToIntVar(&o.Replicas, replicasFlagName, nil,
+				"Replicas is the number of desired Pods for this worker group.")
+			subcmd.Flags().StringToIntVar(&o.MinReplicas, minReplicasFlagName, nil,
+				"MinReplicas denotes the minimum number of desired Pods for this worker group.")
+			subcmd.Flags().StringToIntVar(&o.MaxReplicas, maxReplicasFlagName, nil,
+				"MaxReplicas denotes the maximum number of desired Pods for this worker group, and the default value is maxInt32.")
+		},
```

  Yes, but for the create command uniformity let's keep naming it cmd

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-26T12:47:35Z

- **cmd/experimental/kjobctl/pkg/builder/ray_job_builder.go:88** by [@trasc](https://github.com/trasc) — 2024-07-26T12:47:35Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package builder
+
+import (
+	"context"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/utils/ptr"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+	kueueconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+)
+
+type rayJobBuilder struct {
+	*Builder
+}
+
+var _ builder = (*rayJobBuilder)(nil)
+
+func (b *rayJobBuilder) build(ctx context.Context) (runtime.Object, error) {
+	template, err := b.kjobctlClientset.KjobctlV1alpha1().RayJobTemplates(b.profile.Namespace).
+		Get(ctx, string(b.mode.Template), metav1.GetOptions{})
+	if err != nil {
+		return nil, err
+	}
+
+	rayJob := &rayv1.RayJob{
+		TypeMeta: metav1.TypeMeta{
+			Kind:       "RayJob",
+			APIVersion: "ray.io/v1",
+		},
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace:    b.profile.Namespace,
+			GenerateName: b.profile.Name + "-",
+			Labels:       map[string]string{},
+		},
+		Spec: template.Template.Spec,
+	}
+
+	if b.profile != nil {
+		rayJob.Labels[constants.ProfileLabel] = b.profile.Name
+	}
+
+	if len(b.localQueue) > 0 {
+		rayJob.ObjectMeta.Labels[kueueconstants.QueueLabel] = b.localQueue
+	}
+
+	if rayJob.Spec.RayClusterSpec != nil {
+		for index := range rayJob.Spec.RayClusterSpec.WorkerGroupSpecs {
+			workerGroupSpec := &rayJob.Spec.RayClusterSpec.WorkerGroupSpecs[index]
+			if replicas, ok := b.replicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.Replicas = ptr.To(int32(replicas))
+			}
+			if minReplicas, ok := b.minReplicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.MinReplicas = ptr.To(int32(minReplicas))
+			}
+			if maxReplicas, ok := b.maxReplicas[workerGroupSpec.GroupName]; ok {
+				workerGroupSpec.MaxReplicas = ptr.To(int32(maxReplicas))
+			}
+
+			workerGroupSpec.Template.Spec = b.buildPodSpec(workerGroupSpec.Template.Spec)
+		}
+	}
+
```

  Yes, the way I see it mostly the `head` needs the volume.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-30T06:06:25Z

- **cmd/experimental/kjobctl/config/samples/ray_sample_code.py:1** by [@trasc](https://github.com/trasc) — 2024-07-30T05:34:51Z
  put this in a config map

- **cmd/experimental/kjobctl/config/samples/volume_bundle.yaml:10** by [@trasc](https://github.com/trasc) — 2024-07-30T05:35:46Z
```diff
@@ -0,0 +1,16 @@
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
+kind: VolumeBundle
+metadata:
+  name: sample-volume-bundle
+spec:
+  volumes:
+    - name: foo
+      hostPath:
+        path: /foo
```

  use the config map holding `ray_sample_code.py`

- **cmd/experimental/kjobctl/config/samples/application_profile.yaml:129** by [@trasc](https://github.com/trasc) — 2024-07-30T05:37:28Z
```diff
@@ -45,3 +48,108 @@ template:
                 cpu: "1"
                 memory: "200Mi"
         restartPolicy: OnFailure
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
+kind: RayJobTemplate
+metadata:
+  name: sample-ray-job-template
+  namespace: default
+template:
+  spec:
+    # submissionMode specifies how RayJob submits the Ray job to the RayCluster.
+    # The default value is "K8sJobMode", meaning RayJob will submit the Ray job via a submitter Kubernetes Job.
+    # The alternative value is "HTTPMode", indicating that KubeRay will submit the Ray job by sending an HTTP request to the RayCluster.
+    # submissionMode: "K8sJobMode"
+    entrypoint: python ${ENTRYPOINT_PATH}/ray_sample_code.py
+    # shutdownAfterJobFinishes specifies whether the RayCluster should be deleted after the RayJob finishes. Default is false.
+    # shutdownAfterJobFinishes: false
+
+    # ttlSecondsAfterFinished specifies the number of seconds after which the RayCluster will be deleted after the RayJob finishes.
+    # ttlSecondsAfterFinished: 10
+
+    # activeDeadlineSeconds is the duration in seconds that the RayJob may be active before
+    # KubeRay actively tries to terminate the RayJob; value must be positive integer.
+    # activeDeadlineSeconds: 120
+
+    # RuntimeEnvYAML represents the runtime environment configuration provided as a multi-line YAML string.
+    # See https://docs.ray.io/en/latest/ray-core/handling-dependencies.html for details.
+    # (New in KubeRay version 1.0.)
+    runtimeEnvYAML: |
+      pip:
+        - requests==2.26.0
+        - pendulum==2.1.2
+      env_vars:
+        counter_name: "test_counter"
+
+    # Suspend specifies whether the RayJob controller should create a RayCluster instance.
+    # If a job is applied with the suspend field set to true, the RayCluster will not be created and we will wait for the transition to false.
+    # If the RayCluster is already created, it will be deleted. In the case of transition to false, a new RayCluster will be created.
+    # suspend: false
+
+    # rayClusterSpec specifies the RayCluster instance to be created by the RayJob controller.
+    rayClusterSpec:
+      rayVersion: '2.9.0' # should match the Ray version in the image of the containers
+      # Ray head pod template
+      headGroupSpec:
+        # The `rayStartParams` are used to configure the `ray start` command.
+        # See https://github.com/ray-project/kuberay/blob/master/docs/guidance/rayStartParams.md for the default settings of `rayStartParams` in KubeRay.
+        # See https://docs.ray.io/en/latest/cluster/cli.html#ray-start for all available options in `rayStartParams`.
+        rayStartParams:
+          dashboard-host: '0.0.0.0'
+        #pod template
+        template:
+          spec:
+            containers:
+              - name: ray-head
+                image: rayproject/ray:2.9.0
+                ports:
+                  - containerPort: 6379
+                    name: gcs-server
+                  - containerPort: 8265 # Ray dashboard
+                    name: dashboard
+                  - containerPort: 10001
+                    name: client
+                resources:
+                  limits:
+                    cpu: "1"
+                  requests:
+                    cpu: "200m"
+                volumeMounts:
+                  - mountPath: /home/ray/samples
+                    name: code-sample
+            volumes:
+              # You set volumes at the Pod level, then mount them into containers inside that Pod
+              - name: code-sample
+                configMap:
+                  # Provide the name of the ConfigMap you want to mount.
+                  name: ray-job-code-sample
+                  # An array of keys from the ConfigMap to create as files
+                  items:
+                    - key: ray_sample_code.py
+                      path: ray_sample_code.py
```

  drop this , it should come from the VolumeBundle

- **cmd/experimental/kjobctl/docs/commands/kjobctl_create/_index.md:36** by [@trasc](https://github.com/trasc) — 2024-07-30T05:39:12Z
```diff
@@ -29,6 +29,16 @@ Create a task
 	--profile my-application-profile  \
 	--pod-running-timeout 30s \
 	--rm
+
+  # Create job 
+  kjobctl create rayjob \ 
+	--profile my-application-profile  \
+	--cmd &#34;sleep 5&#34; \
```

  sleep may not be very usefull , let's make it "python <ray-entrypoint>" or something

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:71** by [@trasc](https://github.com/trasc) — 2024-07-30T05:58:54Z
```diff
@@ -60,10 +60,21 @@ const (
 	--profile my-application-profile  \
 	--pod-running-timeout 30s \
 	--rm`
+	createRayJobExample = `  # Create job 
+  kjobctl create rayjob \ 
+	--profile my-application-profile  \
+	--request cpu=500m,ram=4Gi \
```

  Is it used?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-30T06:43:19Z

- **cmd/experimental/kjobctl/docs/commands/kjobctl_create/_index.md:36** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-30T06:43:19Z
```diff
@@ -29,6 +29,16 @@ Create a task
 	--profile my-application-profile  \
 	--pod-running-timeout 30s \
 	--rm
+
+  # Create job 
+  kjobctl create rayjob \ 
+	--profile my-application-profile  \
+	--cmd &#34;sleep 5&#34; \
```

  Good point. Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-30T06:43:30Z

- **cmd/experimental/kjobctl/config/samples/application_profile.yaml:129** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-30T06:43:30Z
```diff
@@ -45,3 +48,108 @@ template:
                 cpu: "1"
                 memory: "200Mi"
         restartPolicy: OnFailure
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
+kind: RayJobTemplate
+metadata:
+  name: sample-ray-job-template
+  namespace: default
+template:
+  spec:
+    # submissionMode specifies how RayJob submits the Ray job to the RayCluster.
+    # The default value is "K8sJobMode", meaning RayJob will submit the Ray job via a submitter Kubernetes Job.
+    # The alternative value is "HTTPMode", indicating that KubeRay will submit the Ray job by sending an HTTP request to the RayCluster.
+    # submissionMode: "K8sJobMode"
+    entrypoint: python ${ENTRYPOINT_PATH}/ray_sample_code.py
+    # shutdownAfterJobFinishes specifies whether the RayCluster should be deleted after the RayJob finishes. Default is false.
+    # shutdownAfterJobFinishes: false
+
+    # ttlSecondsAfterFinished specifies the number of seconds after which the RayCluster will be deleted after the RayJob finishes.
+    # ttlSecondsAfterFinished: 10
+
+    # activeDeadlineSeconds is the duration in seconds that the RayJob may be active before
+    # KubeRay actively tries to terminate the RayJob; value must be positive integer.
+    # activeDeadlineSeconds: 120
+
+    # RuntimeEnvYAML represents the runtime environment configuration provided as a multi-line YAML string.
+    # See https://docs.ray.io/en/latest/ray-core/handling-dependencies.html for details.
+    # (New in KubeRay version 1.0.)
+    runtimeEnvYAML: |
+      pip:
+        - requests==2.26.0
+        - pendulum==2.1.2
+      env_vars:
+        counter_name: "test_counter"
+
+    # Suspend specifies whether the RayJob controller should create a RayCluster instance.
+    # If a job is applied with the suspend field set to true, the RayCluster will not be created and we will wait for the transition to false.
+    # If the RayCluster is already created, it will be deleted. In the case of transition to false, a new RayCluster will be created.
+    # suspend: false
+
+    # rayClusterSpec specifies the RayCluster instance to be created by the RayJob controller.
+    rayClusterSpec:
+      rayVersion: '2.9.0' # should match the Ray version in the image of the containers
+      # Ray head pod template
+      headGroupSpec:
+        # The `rayStartParams` are used to configure the `ray start` command.
+        # See https://github.com/ray-project/kuberay/blob/master/docs/guidance/rayStartParams.md for the default settings of `rayStartParams` in KubeRay.
+        # See https://docs.ray.io/en/latest/cluster/cli.html#ray-start for all available options in `rayStartParams`.
+        rayStartParams:
+          dashboard-host: '0.0.0.0'
+        #pod template
+        template:
+          spec:
+            containers:
+              - name: ray-head
+                image: rayproject/ray:2.9.0
+                ports:
+                  - containerPort: 6379
+                    name: gcs-server
+                  - containerPort: 8265 # Ray dashboard
+                    name: dashboard
+                  - containerPort: 10001
+                    name: client
+                resources:
+                  limits:
+                    cpu: "1"
+                  requests:
+                    cpu: "200m"
+                volumeMounts:
+                  - mountPath: /home/ray/samples
+                    name: code-sample
+            volumes:
+              # You set volumes at the Pod level, then mount them into containers inside that Pod
+              - name: code-sample
+                configMap:
+                  # Provide the name of the ConfigMap you want to mount.
+                  name: ray-job-code-sample
+                  # An array of keys from the ConfigMap to create as files
+                  items:
+                    - key: ray_sample_code.py
+                      path: ray_sample_code.py
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-30T06:43:44Z

- **cmd/experimental/kjobctl/config/samples/ray_sample_code.py:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-30T06:43:44Z
  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-30T07:17:25Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:71** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-30T07:17:25Z
```diff
@@ -60,10 +60,21 @@ const (
 	--profile my-application-profile  \
 	--pod-running-timeout 30s \
 	--rm`
+	createRayJobExample = `  # Create job 
+  kjobctl create rayjob \ 
+	--profile my-application-profile  \
+	--request cpu=500m,ram=4Gi \
```

  Removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-07-30T07:18:07Z

- **cmd/experimental/kjobctl/config/samples/volume_bundle.yaml:10** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-07-30T07:18:07Z
```diff
@@ -0,0 +1,16 @@
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
+kind: VolumeBundle
+metadata:
+  name: sample-volume-bundle
+spec:
+  volumes:
+    - name: foo
+      hostPath:
+        path: /foo
```

  Done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-07-30T07:23:13Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-05T14:22:21Z

/approve
Just some small remarks. Thanks!

- **cmd/experimental/kjobctl/config/samples/volume_bundle.yaml:2** by [@mimowo](https://github.com/mimowo) — 2024-08-05T14:17:47Z
```diff
@@ -0,0 +1,54 @@
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
```

  How is this file loaded? Does it require a manual non-documened step? If documented, please point me where is the command which applies this file.
  Also, can we can name it "ray_volume_bundle.yaml` to reflect this is only for the Ray mode?

- **cmd/experimental/kjobctl/config/samples/application_profile.yaml:53** by [@mimowo](https://github.com/mimowo) — 2024-08-05T14:19:31Z
```diff
@@ -45,3 +48,95 @@ template:
                 cpu: "1"
                 memory: "200Mi"
         restartPolicy: OnFailure
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
```

  Please add a small README.md to link where is the source of the example. Addressing the bottom comment raised in https://github.com/kubernetes-sigs/kueue/pull/2672#issuecomment-2268889432.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-05T15:19:29Z

- **cmd/experimental/kjobctl/config/samples/application_profile.yaml:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-05T15:19:29Z
```diff
@@ -45,3 +48,95 @@ template:
                 cpu: "1"
                 memory: "200Mi"
         restartPolicy: OnFailure
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-05T15:21:33Z

- **cmd/experimental/kjobctl/config/samples/volume_bundle.yaml:2** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-05T15:21:32Z
```diff
@@ -0,0 +1,54 @@
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
```

  > How is this file loaded?
  
  It should be included on volumeBundles in ApplicationProfile. I added it on `application_profile.yaml`. Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-05T15:21:46Z

- **cmd/experimental/kjobctl/config/samples/volume_bundle.yaml:2** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-05T15:21:46Z
```diff
@@ -0,0 +1,54 @@
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
```

  > Also, can we can name it "ray_volume_bundle.yaml` to reflect this is only for the Ray mode?
  
  Renamed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-05T15:25:46Z

- **cmd/experimental/kjobctl/config/samples/volume_bundle.yaml:2** by [@mimowo](https://github.com/mimowo) — 2024-08-05T15:25:46Z
```diff
@@ -0,0 +1,54 @@
+---
+apiVersion: kjobctl.x-k8s.io/v1alpha1
```

  Thanks!
