# PR #2686: Optional garbage collection of finished workloads

**Summary**: Optional garbage collection of finished workloads

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2686

**Last updated**: 2025-05-14T12:32:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mwysokin](https://github.com/mwysokin)
- **Branch**: `optional-garbage-collection-of-finished-workloads` → `main`
- **Created**: 2024-07-24T12:12:52Z
- **Updated**: 2025-05-14T12:32:38Z
- **Merged**: 2025-05-14T12:07:21Z
- **Closed**: 2025-05-14T12:07:21Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 16   **+498 / -37**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind feature

#### What this PR does / why we need it:

This PR adds an optional and configurable garbage collection of finished Workloads. If the object retention policy is configured for Workloads a completed workload will be either re-queued for later deletion (if the policy is configured but its retention period hasn't elapsed) or deleted (both for existing Workloads that might've been created by the previous version or instance of kueue but now are expired according to the retention policy or for newly created Workloads when their retention period elapses).

It decreases both kueue's memory footprint as well as etcd's storage.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1618 

#### Special notes for your reviewer:

This PR comes with a couple of configuration scenarios:
- if the object retention policy is not configured for Workloads (default) or the policy is explicitly set to null all Workloads are kept indefinitely like in the previous versions of kueue,
- if the object retention policy is configured to 0s it means that there's effectively no retention and all completed Workloads are going to be immediately delated,
- if the object retention policy is set to any other value than 0s kueue will delete Workloads when their retention period elapses (both for existing and newly created Workloads).

##### Known issue
The Workload deletion triggers a Workload delete event to be emitted. It's needed because in response to that event kueue performs its internal cleanup (e.g. caches and queues). However it seems that emitting that event automatically causes a Job related to the Workload to be suspended. In this scenario it's confusing and unnecessary because the job is already completed. This is how it chronologically looks:

```py
{'level': 'Level(-2)', 'caller': 'core/workload_controller.go:154', 'msg': 'Reconciling Workload'}
{'level': 'Level(-2)', 'caller': 'core/workload_controller.go:169', 'msg': 'Deleting workload because it has finished and the retention period has elapsed'}
{'level': 'debug, 'caller': 'recorder/recorder.go:104', 'msg': 'Deleted workload job-stress-job-1-pqsxf-dd783'}
{'level': 'Level(-2)', 'caller': 'core/workload_controller.go:627', 'msg': 'Workload delete event'}
{'level': 'Level(-2)', 'caller': 'core/clusterqueue_controller.go:330', 'msg': 'Got generic event'}
{'level': 'Level(-2)', 'caller': 'jobframework/reconciler.go:313', 'msg': 'Reconciling Job'}
{'level': 'Level(-2)', 'caller': 'jobframework/reconciler.go:606', 'msg': 'job with no matching workload, suspending'}
```

The author of this PR commits to creating a fix/improvement for the showcased scenario. It's up to the reviewers to decide whether the fix should be either: part of this PR or its own subsequent issue/PR and whether this PR should be blocked or not until the fix is done.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Introduce ObjectRetentionPolicies.FinishedWorkloadRetention flag, which allows to configure a retention policy for Workloads, Workloads will be deleted when their retention period elapses, by default this new behavior is disabled
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-24T12:13:01Z

Hi @mwysokin. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-24T12:13:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f514a5c875ac04230c678d880ea8efbfd9725752 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/682480bc48767d0008cc0878 |

### Comment by [@trasc](https://github.com/trasc) — 2024-07-24T13:37:51Z

> I think the issue with suspending a Job when deleting a workload is pre-existing, and can be handled separately from this feature (feel free to open a separate one).

In my opinion it's better to be done with this PR, it should just be a matter of moving  https://github.com/kubernetes-sigs/kueue/blob/014d4ec600cc063606c3ae7ab9c2cc77f4683681/pkg/controller/jobframework/reconciler.go#L606 within https://github.com/kubernetes-sigs/kueue/blob/014d4ec600cc063606c3ae7ab9c2cc77f4683681/pkg/controller/jobframework/reconciler.go#L615-L625.

(I don't think the job is really suspended the logs are lying :) )

### Comment by [@mwysokin](https://github.com/mwysokin) — 2024-07-25T12:41:46Z

Thank you @mimowo @tenzen-y @trasc for your comments 🙏 
The plan with this PR is following:

- [x] create a small KEP for it (https://github.com/kubernetes-sigs/kueue/pull/2742),
- [x] add logic/a test case for an expired Workload with a finalizer once I understand that case correctly (https://github.com/kubernetes-sigs/kueue/pull/2686/commits/26d083e01a0b507f97ad72445ed25b1e5fbc6e53),
- [x] either fix the suspension or remove the confusing message about removing a Job that was completed if it's a small enough change so that it doesn't pollute the PR.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-25T12:43:36Z

> Thank you @mimowo @tenzen-y @trasc for your comments 🙏 The plan with this PR is following:
> 
> * [ ]  create a small KEP for it,
> * [ ]  add logic/a test case for an expired Workload with a finalizer once I understand that case correctly,
> * [ ]  either fix the suspension or remove the confusing message about removing a Job that was completed if it's a small enough change so that it doesn't pollute the PR.

It would better to open a dedicated PR for the KEP :)

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-25T12:47:56Z

Sounds like a great plan.

### Comment by [@mwysokin](https://github.com/mwysokin) — 2024-08-08T12:56:22Z

It took me a while but I applied all requested updates. Thank you so much for your time spent on the initial review 🙏 May I please ask for a little bit more of your time for a second round? 🙏

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-14T16:46:14Z

/ok-to-test
/cc

### Comment by [@mwysokin](https://github.com/mwysokin) — 2024-08-14T18:12:40Z

I'll take a look at the failing test on Friday. I'm pretty sure I run them last week and they worked fine. Maybe one of the main merges broke something. I'll investigate.

### Comment by [@mwysokin](https://github.com/mwysokin) — 2024-08-26T05:33:27Z

FYI I just wanted to let everyone know that I didn't abandon the work. I've been on vacation last week and I'll be back in September. Sorry for the delay 🙇

### Comment by [@mwysokin](https://github.com/mwysokin) — 2024-09-27T08:59:14Z

I'm really sorry it's been dragging. I'm coming back to it starting Monday.

### Comment by [@trasc](https://github.com/trasc) — 2025-01-14T11:57:28Z

/uncc

### Comment by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:35:03Z

This revision should address majority of the comments and I will spend more time with tests, but the handling of the new API and main logic is here so I believe this should be reviewable.
@mbobrovskyi PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-06T14:40:08Z

@mwysokin: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-multikueue-e2e-main | 0b9933ff6c2f27b749a96b893c05587a2fb48a38 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/2686/pull-kueue-test-multikueue-e2e-main/1864018038399963136) | true | `/test pull-kueue-test-multikueue-e2e-main`
pull-kueue-test-integration-main | 0b9933ff6c2f27b749a96b893c05587a2fb48a38 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/2686/pull-kueue-test-integration-main/1864018038156693504) | true | `/test pull-kueue-test-integration-main`
pull-kueue-test-e2e-main-1-29 | 0b9933ff6c2f27b749a96b893c05587a2fb48a38 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/2686/pull-kueue-test-e2e-main-1-29/1864018038219608064) | true | `/test pull-kueue-test-e2e-main-1-29`
pull-kueue-test-tas-e2e-main | 0b9933ff6c2f27b749a96b893c05587a2fb48a38 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/2686/pull-kueue-test-tas-e2e-main/1864018038454489088) | true | `/test pull-kueue-test-tas-e2e-main`
pull-kueue-test-multikueue-integration-main | 0b9933ff6c2f27b749a96b893c05587a2fb48a38 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/2686/pull-kueue-test-multikueue-integration-main/1883921350901370880) | true | `/test pull-kueue-test-multikueue-integration-main`
pull-kueue-test-customconfigs-e2e-main | 0b9933ff6c2f27b749a96b893c05587a2fb48a38 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/2686/pull-kueue-test-customconfigs-e2e-main/1889321500255719424) | true | `/test pull-kueue-test-customconfigs-e2e-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=2686). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Amwysokin). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mykysha](https://github.com/mykysha) — 2025-05-09T11:54:51Z

This iteration includes feature gate for controlling the object retention functionality.

Will be addressing the remaining comment (and any future ones) and add some e2e tests in the meantime

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-14T12:06:20Z

LGTM label has been added.  <details>Git tree hash: 14e802e5d1b531df996ce3da3bee87359a8bd6b1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-14T12:06:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2686#pullrequestreview-2839924187" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2686#" title="Author self-approved">mwysokin</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-14T12:28:38Z

As a follow up @mykysha could you please:
- address the remarks for the sibling issue: https://github.com/kubernetes-sigs/kueue/issues/4471#issuecomment-2873279028
- update documentation. I think it is fair to update it for both cleanup of finished and deactivated workloads. I would propose a new page "Setup object retention policy" under Tasks -> Manage Kueue https://kueue.sigs.k8s.io/docs/tasks/manage/.

cc @PBundyra @tenzen-y

### Comment by [@mykysha](https://github.com/mykysha) — 2025-05-14T12:32:36Z

Surely, regarding the comments for the #4471 in https://github.com/kubernetes-sigs/kueue/pull/5177 I'm already in progress, and will also take a look at the docs

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T13:00:11Z

Overall lgtm. I'm not certain about is the finalizer - it might be worth adding an integration test for it. 

I think the issue with suspending a Job when deleting a workload is pre-existing, and can be handled separately from this feature (feel free to open a separate one).

- **apis/config/v1beta1/configuration_types.go:417** by [@mimowo](https://github.com/mimowo) — 2024-07-24T12:27:14Z
```diff
@@ -400,3 +404,15 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type ObjectRetentionPolicies struct {
+	// FinishedWorkloadRetention is the duration to retain finished Workloads.
+	// A duration of 0 will delete finished Workloads immediately.
+	// A nil value will disable automatic deletion.
+	// The value is represented using the metav1.Duration format, allowing for flexible
+	// specification of time units (e.g., "24h", "1h30m", "30s").
+	//
+	// Defaults to null.
+	// +optional
+	FinishedWorkloadRetention *metav1.Duration `json:"finishedWorkloadRetention"`
```

  This seems like a relatively small and self-explanatory API, so I think we could go without KEP. WDYT @tenzen-y ?

- **pkg/controller/core/workload_controller.go:171** by [@mimowo](https://github.com/mimowo) — 2024-07-24T12:34:05Z
```diff
@@ -144,11 +154,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			err := r.client.Delete(ctx, &wl)
+			if err != nil {
```

  ```suggestion
  			if err := r.client.Delete(ctx, &wl); err != nil {
  ```
  nit

- **pkg/controller/core/workload_controller.go:175** by [@mimowo](https://github.com/mimowo) — 2024-07-24T12:38:21Z
```diff
@@ -144,11 +154,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			err := r.client.Delete(ctx, &wl)
+			if err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted workload %v", wl.Name)
```

  ```suggestion
  			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention:  %v", workload.Key(wl))
  ```
  By analogy to https://github.com/kubernetes-sigs/kueue/blob/014d4ec600cc063606c3ae7ab9c2cc77f4683681/pkg/controller/jobframework/reconciler.go#L643

- **pkg/controller/core/workload_controller_test.go:1317** by [@mimowo](https://github.com/mimowo) — 2024-07-24T12:38:39Z
```diff
@@ -1314,6 +1314,95 @@ func TestReconcile(t *testing.T) {
 				}).
 				Obj(),
 		},
+		"shouldn't delete the workflow because, object retention not configured": {
```

  ```suggestion
  		"shouldn't delete the workload because, object retention not configured": {
  ```
  nit :)

- **pkg/controller/core/workload_controller.go:170** by [@mimowo](https://github.com/mimowo) — 2024-07-24T12:49:11Z
```diff
@@ -144,11 +154,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
```

  This will remove the workload finalizer when there is no Job owner. What removes the finalizer if the Job object still exists?

- **pkg/controller/core/workload_controller_test.go:1773** by [@mimowo](https://github.com/mimowo) — 2024-07-24T12:50:57Z
```diff
@@ -1314,6 +1314,95 @@ func TestReconcile(t *testing.T) {
 				}).
 				Obj(),
 		},
+		"shouldn't delete the workflow because, object retention not configured": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+			reconcilerOpts: []Option{
+				WithWorkloadObjectRetention(nil),
+			},
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+			wantError: nil,
+		},
+		"shouldn't try to delete the workload (no event emitted) because it is already being deleted by kubernetes, object retention configured": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+					LastTransitionTime: metav1.Time{
+						Time: testStartTime,
+					},
+				}).
+				DeletionTimestamp(testStartTime).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Obj(),
+			reconcilerOpts: []Option{
+				WithWorkloadObjectRetention(&metav1.Duration{
+					Duration: 24 * time.Hour,
+				}),
+			},
+			wantWorkload: nil,
+			wantError: nil,
+		},
+		"shouldn't try to delete the workload because the retention period hasn't elapsed yet, object retention configured": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type: kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+					LastTransitionTime: metav1.NewTime(testStartTime.Add(-10 * time.Second)),
+				}).
+				Obj(),
+			reconcilerOpts: []Option{
+				WithWorkloadObjectRetention(
+					&metav1.Duration{
+						Duration: 30 * time.Second,
+				}),
+			},
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type: kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+					LastTransitionTime: metav1.NewTime(testStartTime.Add(-10 * time.Second)),
+				}).
+				Obj(),
+			wantError: nil,
+		},
+		"should delete the workload because the retention period has elapsed, object retention configured": {
```

  Can we add a test case for a workload with finalizer? I would like to also consider an integration test for it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-24T15:40:30Z

- **apis/config/v1beta1/configuration_types.go:417** by [@mimowo](https://github.com/mimowo) — 2024-07-24T15:40:30Z
```diff
@@ -400,3 +404,15 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type ObjectRetentionPolicies struct {
+	// FinishedWorkloadRetention is the duration to retain finished Workloads.
+	// A duration of 0 will delete finished Workloads immediately.
+	// A nil value will disable automatic deletion.
+	// The value is represented using the metav1.Duration format, allowing for flexible
+	// specification of time units (e.g., "24h", "1h30m", "30s").
+	//
+	// Defaults to null.
+	// +optional
+	FinishedWorkloadRetention *metav1.Duration `json:"finishedWorkloadRetention"`
```

  Yuki, do you have some history of adding small APIs without a KEP or extension of existing one?
  
  On the second thought I think the new API can grow with new fields, and having some place for discussing the new extensions, before implementing, would be useful. 
  
  For that reason it could be helpful to already start with a KEP https://github.com/kubernetes/enhancements/blob/master/keps%2FREADME.md . Michal would you mind adding one based on the Kueue's template?
  
  It could be the same PR imo.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-07-25T03:42:04Z

- **apis/config/v1beta1/configuration_types.go:417** by [@tenzen-y](https://github.com/tenzen-y) — 2024-07-25T03:42:04Z
```diff
@@ -400,3 +404,15 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type ObjectRetentionPolicies struct {
+	// FinishedWorkloadRetention is the duration to retain finished Workloads.
+	// A duration of 0 will delete finished Workloads immediately.
+	// A nil value will disable automatic deletion.
+	// The value is represented using the metav1.Duration format, allowing for flexible
+	// specification of time units (e.g., "24h", "1h30m", "30s").
+	//
+	// Defaults to null.
+	// +optional
+	FinishedWorkloadRetention *metav1.Duration `json:"finishedWorkloadRetention"`
```

  > Yuki, do you have some history of adding small APIs without a KEP or extension of existing one?
  
  Actually, we added the `active` field to the Workload without any KEP: https://github.com/kubernetes-sigs/kueue/issues/1091
  
  But, as you know, the deactivation mechanism brings us implied specifications and bugs around the release 0.7, and we struggled to control and fix the bugs and implied specifications.
  So, I regret that we did not prepare KEP for the deactivated mechanism.
  
  Hence, I would like to prepare the dedicated KEP for this feature so that we can evaluate if this approach is better way.
  @mimowo WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-25T07:17:28Z

- **apis/config/v1beta1/configuration_types.go:417** by [@mimowo](https://github.com/mimowo) — 2024-07-25T07:17:28Z
```diff
@@ -400,3 +404,15 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type ObjectRetentionPolicies struct {
+	// FinishedWorkloadRetention is the duration to retain finished Workloads.
+	// A duration of 0 will delete finished Workloads immediately.
+	// A nil value will disable automatic deletion.
+	// The value is represented using the metav1.Duration format, allowing for flexible
+	// specification of time units (e.g., "24h", "1h30m", "30s").
+	//
+	// Defaults to null.
+	// +optional
+	FinishedWorkloadRetention *metav1.Duration `json:"finishedWorkloadRetention"`
```

  Yeah, the KEP could be relatively short, but it would serve also as a basis for future extensions of the retention API if needed.

### Review by [@mwysokin](https://github.com/mwysokin) (commented) — 2024-07-25T12:09:01Z

- **apis/config/v1beta1/configuration_types.go:417** by [@mwysokin](https://github.com/mwysokin) — 2024-07-25T12:09:01Z
```diff
@@ -400,3 +404,15 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type ObjectRetentionPolicies struct {
+	// FinishedWorkloadRetention is the duration to retain finished Workloads.
+	// A duration of 0 will delete finished Workloads immediately.
+	// A nil value will disable automatic deletion.
+	// The value is represented using the metav1.Duration format, allowing for flexible
+	// specification of time units (e.g., "24h", "1h30m", "30s").
+	//
+	// Defaults to null.
+	// +optional
+	FinishedWorkloadRetention *metav1.Duration `json:"finishedWorkloadRetention"`
```

  @mimowo @tenzen-y Sure, I'm happy to prepare a KEP for it. I'm new to the project and learning its ways, so I'm sorry I didn't start with the KEP in the first place 🙇

### Review by [@mwysokin](https://github.com/mwysokin) (commented) — 2024-07-25T12:15:35Z

- **pkg/controller/core/workload_controller.go:175** by [@mwysokin](https://github.com/mwysokin) — 2024-07-25T12:15:35Z
```diff
@@ -144,11 +154,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			err := r.client.Delete(ctx, &wl)
+			if err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted workload %v", wl.Name)
```

  Thank you for spotting that 🙏 I'll fix the corresponding test in the subsequent commit.

### Review by [@mwysokin](https://github.com/mwysokin) (commented) — 2024-07-25T12:35:29Z

- **pkg/controller/core/workload_controller.go:170** by [@mwysokin](https://github.com/mwysokin) — 2024-07-25T12:35:29Z
```diff
@@ -144,11 +154,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
```

  I might be lucking some understanding here so please excuse my ignorance 🙏 
  
  How I understand the problem is that the primary function of this code branch (which was not created by me, I just added a comment here to be able to immediately differentiate it from the rest of the deletion logic) is to act when a job was manually deleted by a user and we need to cleanup workload resources because kueue controls them and they won't be deleted by K8s itself and so that we could also control the cleanup of the kueue internal state [cache, internal queues). That's why there are 2 conditions here no owner (parent Job) and there's a deletion timestamp set so the workload itself is supposed to be deleted too.
  
  Today if no one touches the Job, its Workload will stay. The primary function of the code in this PR is to cleanup K8s and kueue's knowledge about completed workloads to free up memory and storage, so the Workload needed to finish first and only then it could be deleted. My assumption is that when a Workload is completed it shouldn't have any finalizers (and resources) in place so that's why I didn't do anything with the finalizers. 
  
  @mimowo If that is not the case and a completed Workload can indeed still have finalizers I think I'd need to some guidance with how to achieve this state and how it should be handled as I was not able to see it with my own basic tests.

### Review by [@mwysokin](https://github.com/mwysokin) (commented) — 2024-07-25T12:37:01Z

- **pkg/controller/core/workload_controller_test.go:1773** by [@mwysokin](https://github.com/mwysokin) — 2024-07-25T12:37:01Z
```diff
@@ -1314,6 +1314,95 @@ func TestReconcile(t *testing.T) {
 				}).
 				Obj(),
 		},
+		"shouldn't delete the workflow because, object retention not configured": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+			reconcilerOpts: []Option{
+				WithWorkloadObjectRetention(nil),
+			},
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+			wantError: nil,
+		},
+		"shouldn't try to delete the workload (no event emitted) because it is already being deleted by kubernetes, object retention configured": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+					LastTransitionTime: metav1.Time{
+						Time: testStartTime,
+					},
+				}).
+				DeletionTimestamp(testStartTime).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Obj(),
+			reconcilerOpts: []Option{
+				WithWorkloadObjectRetention(&metav1.Duration{
+					Duration: 24 * time.Hour,
+				}),
+			},
+			wantWorkload: nil,
+			wantError: nil,
+		},
+		"shouldn't try to delete the workload because the retention period hasn't elapsed yet, object retention configured": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type: kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+					LastTransitionTime: metav1.NewTime(testStartTime.Add(-10 * time.Second)),
+				}).
+				Obj(),
+			reconcilerOpts: []Option{
+				WithWorkloadObjectRetention(
+					&metav1.Duration{
+						Duration: 30 * time.Second,
+				}),
+			},
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type: kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+					LastTransitionTime: metav1.NewTime(testStartTime.Add(-10 * time.Second)),
+				}).
+				Obj(),
+			wantError: nil,
+		},
+		"should delete the workload because the retention period has elapsed, object retention configured": {
```

  Sure thing 👍  It'd greatly help me to understand the case if you could respond to my comment above first about cleaning up Workloads with a finalizer 🙏

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-30T09:26:12Z

- **pkg/controller/core/workload_controller.go:170** by [@mimowo](https://github.com/mimowo) — 2024-07-30T09:26:12Z
```diff
@@ -144,11 +154,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
```

  > My assumption is that when a Workload is completed it shouldn't have any finalizers (and resources) in place so that's why I didn't do anything with the finalizers.
  
  Yes, this is correct, we should make sure the finalizer for a finished workload is deleted. I think this is the code: which already does it: https://github.com/kubernetes-sigs/kueue/blob/a128ae3354a56670ffa58695fef1eca270fe3a47/pkg/controller/jobframework/reconciler.go#L332-L340.  In that case calling `Delete` as you do is enough.
  
  However, given that the code change depends on the logic in another part of the codebase I would prefer to have an integration test to show the workloads get deleted from the API server.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-26T16:07:24Z

- **apis/config/v1beta1/configuration_types.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T15:55:16Z
  I'm leaving the API discussion to #2742

- **apis/config/v1beta1/defaults.go:197** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T15:56:30Z
```diff
@@ -193,4 +193,7 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.PreemptionStrategies) == 0 {
 		fs.PreemptionStrategies = []PreemptionStrategy{LessThanOrEqualToFinalShare, LessThanInitialShare}
 	}
+	if cfg.ObjectRetentionPolicies == nil {
+		cfg.ObjectRetentionPolicies = &ObjectRetentionPolicies{}
```

  If we are not adding anything relevant, let's not add any code to "defaults"

- **pkg/config/validation.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T15:57:20Z
  add a test

- **pkg/controller/core/workload_controller.go:174** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T16:00:03Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention:  %v", workload.Key(&wl))
```

  ```suggestion
  			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention")
  ```
  Because the event is already targeting the workload, we don't need to add it's name again in the message.

- **pkg/controller/core/workload_controller.go:174** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T16:00:51Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention:  %v", workload.Key(&wl))
```

  we are missing a `return` for this happy path.

- **pkg/controller/core/workload_controller.go:177** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T16:01:12Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention:  %v", workload.Key(&wl))
+		} else {
+			remainingTime := expirationTime.Sub(now)
+			log.V(2).Info("Requeueing workload for deletion after retention period", "remainingTime", remainingTime)
```

  ```suggestion
  			log.V(3).Info("Requeueing workload for deletion after retention period", "remainingTime", remainingTime)
  ```
  
  Or even 4.

- **pkg/controller/core/workload_controller.go:182** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T16:02:48Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
```

  we should probably ignore a NotFound error.

- **pkg/controller/core/workload_controller_test.go:1722** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T16:04:26Z
```diff
@@ -1314,6 +1314,95 @@ func TestReconcile(t *testing.T) {
 				}).
 				Obj(),
 		},
+		"shouldn't delete the workload because, object retention not configured": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+			reconcilerOpts: []Option{
+				WithWorkloadObjectRetention(nil),
+			},
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+			wantError: nil,
+		},
+		"shouldn't try to delete the workload (no event emitted) because it is already being deleted by kubernetes, object retention configured": {
```

  what if it has an owner reference?

- **test/integration/controller/core/workload_controller_test.go:517** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T16:06:28Z
```diff
@@ -429,3 +430,91 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns              *corev1.Namespace
+		createdWorkload kueue.Workload
+		localQueue      *kueue.LocalQueue
+		clusterQueue    *kueue.ClusterQueue
+		flavor          *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{CRDPath: crdPath, WebhookPath: webhookPath}
+		cfg = fwk.Init()
+		ctx, k8sClient = fwk.RunManager(cfg, managerSetupWithWorkloadRetentionEnabled)
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-workload-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		createdWorkload = kueue.Workload{}
+		clusterQueue = nil
+		localQueue = nil
+		flavor = nil
+	})
+
+	ginkgo.When("a workload with retention period", func() {
+		ginkgo.BeforeEach(func() {
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("q", ns.Name).ClusterQueue("cq").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("should delete the workload after retention period elapses", func() {
+			// Create workload
+			wl := testing.MakeWorkload("wl-to-expire", ns.Name).Queue("q").Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			// Simulate admission
+			admission := testing.MakeAdmission("cq").Obj()
+			gomega.Expect(k8sClient.Get(ctx, wlKey, &createdWorkload)).To(gomega.Succeed())
+			gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, &createdWorkload, admission)).Should(gomega.Succeed())
+			util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, &createdWorkload)
+
+			// Mark as Finished
+			gomega.Expect(k8sClient.Get(ctx, wlKey, &createdWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload.Status.Conditions = append(createdWorkload.Status.Conditions, metav1.Condition{
+					Type:               kueue.WorkloadFinished,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: metav1.Now(),
+					Reason:             "FinishedByTest",
+					Message:            "Finished for testing purposes",
+				})
+				g.Expect(k8sClient.Status().Update(ctx, &createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("workload should not be deleted before retention period")
+			gomega.Consistently(func() error {
+				return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
+			}, 2*time.Second, time.Second).Should(gomega.Succeed())
+
+			ginkgo.By("workload should be deleted after the retention period")
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
+			}, 2*time.Second, time.Second).ShouldNot(gomega.Succeed())
```

  use the existing timeout constants.

- **test/integration/controller/core/suite_test.go:86** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T16:07:23Z
```diff
@@ -78,3 +80,32 @@ func managerSetup(ctx context.Context, mgr manager.Manager) {
 	failedCtrl, err := core.SetupControllers(mgr, queues, cCache, controllersCfg)
 	gomega.Expect(err).ToNot(gomega.HaveOccurred(), "controller", failedCtrl)
 }
+
+func managerSetupWithWorkloadRetentionEnabled(ctx context.Context, mgr manager.Manager) {
+	objectRetention := &config.ObjectRetentionPolicies{FinishedWorkloadRetention: &v1.Duration{
+		Duration: 3 * time.Second,
```

  a bit too long, given that this influences how long the test runs for.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-06T14:35:44Z

- **test/integration/controller/core/suite_test.go:86** by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:35:44Z
```diff
@@ -78,3 +80,32 @@ func managerSetup(ctx context.Context, mgr manager.Manager) {
 	failedCtrl, err := core.SetupControllers(mgr, queues, cCache, controllersCfg)
 	gomega.Expect(err).ToNot(gomega.HaveOccurred(), "controller", failedCtrl)
 }
+
+func managerSetupWithWorkloadRetentionEnabled(ctx context.Context, mgr manager.Manager) {
+	objectRetention := &config.ObjectRetentionPolicies{FinishedWorkloadRetention: &v1.Duration{
+		Duration: 3 * time.Second,
```

  Have replaced with a ShortTimeout

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-06T14:35:53Z

- **test/integration/controller/core/workload_controller_test.go:517** by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:35:53Z
```diff
@@ -429,3 +430,91 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns              *corev1.Namespace
+		createdWorkload kueue.Workload
+		localQueue      *kueue.LocalQueue
+		clusterQueue    *kueue.ClusterQueue
+		flavor          *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{CRDPath: crdPath, WebhookPath: webhookPath}
+		cfg = fwk.Init()
+		ctx, k8sClient = fwk.RunManager(cfg, managerSetupWithWorkloadRetentionEnabled)
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-workload-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		createdWorkload = kueue.Workload{}
+		clusterQueue = nil
+		localQueue = nil
+		flavor = nil
+	})
+
+	ginkgo.When("a workload with retention period", func() {
+		ginkgo.BeforeEach(func() {
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("q", ns.Name).ClusterQueue("cq").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("should delete the workload after retention period elapses", func() {
+			// Create workload
+			wl := testing.MakeWorkload("wl-to-expire", ns.Name).Queue("q").Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			// Simulate admission
+			admission := testing.MakeAdmission("cq").Obj()
+			gomega.Expect(k8sClient.Get(ctx, wlKey, &createdWorkload)).To(gomega.Succeed())
+			gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, &createdWorkload, admission)).Should(gomega.Succeed())
+			util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, &createdWorkload)
+
+			// Mark as Finished
+			gomega.Expect(k8sClient.Get(ctx, wlKey, &createdWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload.Status.Conditions = append(createdWorkload.Status.Conditions, metav1.Condition{
+					Type:               kueue.WorkloadFinished,
+					Status:             metav1.ConditionTrue,
+					LastTransitionTime: metav1.Now(),
+					Reason:             "FinishedByTest",
+					Message:            "Finished for testing purposes",
+				})
+				g.Expect(k8sClient.Status().Update(ctx, &createdWorkload)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("workload should not be deleted before retention period")
+			gomega.Consistently(func() error {
+				return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
+			}, 2*time.Second, time.Second).Should(gomega.Succeed())
+
+			ginkgo.By("workload should be deleted after the retention period")
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWorkload)
+			}, 2*time.Second, time.Second).ShouldNot(gomega.Succeed())
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-06T14:36:16Z

- **pkg/controller/core/workload_controller.go:182** by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:36:16Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-06T14:37:06Z

- **apis/config/v1beta1/defaults.go:197** by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:37:05Z
```diff
@@ -193,4 +193,7 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.PreemptionStrategies) == 0 {
 		fs.PreemptionStrategies = []PreemptionStrategy{LessThanOrEqualToFinalShare, LessThanInitialShare}
 	}
+	if cfg.ObjectRetentionPolicies == nil {
+		cfg.ObjectRetentionPolicies = &ObjectRetentionPolicies{}
```

  Have removed the code from the defaults

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-06T14:37:34Z

- **pkg/controller/core/workload_controller.go:174** by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:37:34Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention:  %v", workload.Key(&wl))
```

  Removed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-06T14:38:03Z

- **pkg/controller/core/workload_controller.go:174** by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:38:03Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention:  %v", workload.Key(&wl))
```

  Added, should be working as expected now

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-06T14:38:08Z

- **pkg/controller/core/workload_controller.go:177** by [@mykysha](https://github.com/mykysha) — 2025-05-06T14:38:08Z
```diff
@@ -144,11 +154,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconciling Workload")
 
 	if len(wl.ObjectMeta.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.objectRetention == nil  {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(r.objectRetention.Duration)
+		if now.After(expirationTime) {
+			log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", r.objectRetention.Duration)
+			if err := r.client.Delete(ctx, &wl); err != nil {
+				log.Error(err, "Failed to delete workload from the API server")
+				return ctrl.Result{}, fmt.Errorf("deleting workflow from the API server: %w", err)
+			}
+			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Deleted", "Deleted finished workload due to elapsed retention:  %v", workload.Key(&wl))
+		} else {
+			remainingTime := expirationTime.Sub(now)
+			log.V(2).Info("Requeueing workload for deletion after retention period", "remainingTime", remainingTime)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-07T11:35:08Z

- **pkg/config/validation.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-06T15:19:42Z
```diff
@@ -61,6 +61,7 @@ var (
 	internalCertManagementPath        = field.NewPath("internalCertManagement")
 	queueVisibilityPath               = field.NewPath("queueVisibility")
 	resourceTransformationPath        = field.NewPath("resources", "transformations")
+	objectRetentionPoliciesPath       = field.NewPath("objectRetentionPolicies")
```

  ```suggestion
  	objectRetentionPoliciesPath       = field.NewPath("objectRetentionPolicies")
  	objectRetentionPoliciesWorkloadsPath = objectRetentionPoliciesPath.Child("workloads")
  ```
  We should add workloads to path.

- **pkg/config/config_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-06T15:44:06Z
  Could you please add validation tests?

- **config/components/manager/controller_manager_config.yaml:76** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-07T11:15:15Z
```diff
@@ -72,3 +72,5 @@ integrations:
 #    outputs:
 #      example.com/accelerator-memory: 5Gi
 #      example.com/accelerator-gpc: 4
+#objectRetentionPolicies:
+#  finishedWorkloadRetention: null # null indicates infinite retention, 0s means no retention at all
```

  Please update it as well.

- **pkg/controller/core/workload_controller.go:180** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-07T11:18:30Z
```diff
@@ -152,11 +166,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconcile Workload")
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.workloadRetention == nil || r.workloadRetention.afterFinished == nil {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(*r.workloadRetention.afterFinished)
+		if now.After(expirationTime) {
```

  Shouldn't we check for equal? For case if afterFinished=0.

- **test/integration/singlecluster/controller/core/suite_test.go:92** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-07T11:30:22Z
```diff
@@ -87,3 +88,36 @@ func managerSetup(ctx context.Context, mgr manager.Manager) {
 	failedCtrl, err := core.SetupControllers(mgr, queues, cCache, controllersCfg)
 	gomega.Expect(err).ToNot(gomega.HaveOccurred(), "controller", failedCtrl)
 }
+
+func managerSetupWithWorkloadRetentionEnabled(ctx context.Context, mgr manager.Manager) {
```

  Can we propagate Configuration and use managerSetup? Like we did in Jobs.
  
  https://github.com/kubernetes-sigs/kueue/blob/ba42340c6467669a454d0a267140e4fc1a4f3e4d/test/integration/singlecluster/controller/jobs/job/suite_test.go#L89
  
  It would also be helpful to use different configurations for different tests.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-09T11:51:52Z

- **pkg/config/validation.go:64** by [@mykysha](https://github.com/mykysha) — 2025-05-09T11:51:52Z
```diff
@@ -61,6 +61,7 @@ var (
 	internalCertManagementPath        = field.NewPath("internalCertManagement")
 	queueVisibilityPath               = field.NewPath("queueVisibility")
 	resourceTransformationPath        = field.NewPath("resources", "transformations")
+	objectRetentionPoliciesPath       = field.NewPath("objectRetentionPolicies")
```

  Thanks, done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-09T11:52:02Z

- **pkg/config/config_test.go:1** by [@mykysha](https://github.com/mykysha) — 2025-05-09T11:52:02Z
  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-09T11:52:20Z

- **config/components/manager/controller_manager_config.yaml:76** by [@mykysha](https://github.com/mykysha) — 2025-05-09T11:52:20Z
```diff
@@ -72,3 +72,5 @@ integrations:
 #    outputs:
 #      example.com/accelerator-memory: 5Gi
 #      example.com/accelerator-gpc: 4
+#objectRetentionPolicies:
+#  finishedWorkloadRetention: null # null indicates infinite retention, 0s means no retention at all
```

  Thanks for noticing, have updated here

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-09T11:52:37Z

- **pkg/controller/core/workload_controller.go:180** by [@mykysha](https://github.com/mykysha) — 2025-05-09T11:52:36Z
```diff
@@ -152,11 +166,30 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconcile Workload")
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if r.workloadRetention == nil || r.workloadRetention.afterFinished == nil {
+			return ctrl.Result{}, nil
+		}
+		now := r.clock.Now()
+		expirationTime := finishedCond.LastTransitionTime.Add(*r.workloadRetention.afterFinished)
+		if now.After(expirationTime) {
```

  You are correct, reflected this in a new revision

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-09T16:01:30Z

- **pkg/controller/core/workload_controller.go:83** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-09T16:01:29Z
```diff
@@ -73,9 +73,14 @@ type waitForPodsReadyConfig struct {
 	requeuingBackoffJitter      float64
 }
 
+type workloadRetentionConfig struct {
+	afterFinished *time.Duration
+}
+
 type options struct {
 	watchers               []WorkloadUpdateWatcher
 	waitForPodsReadyConfig *waitForPodsReadyConfig
+	objectRetention        *workloadRetentionConfig
```

  ```suggestion
  	workloadRetentionConfig        workloadRetentionConfig
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-13T09:03:07Z

- **test/integration/singlecluster/controller/core/suite_test.go:92** by [@mykysha](https://github.com/mykysha) — 2025-05-13T09:03:07Z
```diff
@@ -87,3 +88,36 @@ func managerSetup(ctx context.Context, mgr manager.Manager) {
 	failedCtrl, err := core.SetupControllers(mgr, queues, cCache, controllersCfg)
 	gomega.Expect(err).ToNot(gomega.HaveOccurred(), "controller", failedCtrl)
 }
+
+func managerSetupWithWorkloadRetentionEnabled(ctx context.Context, mgr manager.Manager) {
```

  Great idea, done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-13T09:03:29Z

- **pkg/controller/core/workload_controller.go:83** by [@mykysha](https://github.com/mykysha) — 2025-05-13T09:03:29Z
```diff
@@ -73,9 +73,14 @@ type waitForPodsReadyConfig struct {
 	requeuingBackoffJitter      float64
 }
 
+type workloadRetentionConfig struct {
+	afterFinished *time.Duration
+}
+
 type options struct {
 	watchers               []WorkloadUpdateWatcher
 	waitForPodsReadyConfig *waitForPodsReadyConfig
+	objectRetention        *workloadRetentionConfig
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T07:48:28Z

- **pkg/controller/core/core.go:127** by [@mimowo](https://github.com/mimowo) — 2025-05-14T07:27:37Z
```diff
@@ -112,6 +113,20 @@ func waitForPodsReady(cfg *configapi.WaitForPodsReady) *waitForPodsReadyConfig {
 	return &result
 }
 
+func workloadRetention(cfg *configapi.ObjectRetentionPolicies) *workloadRetentionConfig {
+	if cfg == nil || cfg.Workloads == nil {
+		return nil
+	}
+
+	result := &workloadRetentionConfig{}
+
+	if cfg.Workloads.AfterFinished != nil {
+		result.afterFinished = &cfg.Workloads.AfterFinished.Duration
+	}
+
+	return result
```

  ```suggestion
  	if cfg == nil || cfg.Workloads == nil || cfg.Workloads.AfterFinished {
  		return nil
  	}
          return &workloadRetentionConfig{
              afterFinished: cfg.Workloads.AfterFinished.Duration
          }
  ```
  to shorten it

- **pkg/controller/core/workload_controller.go:178** by [@mimowo](https://github.com/mimowo) — 2025-05-14T07:34:45Z
```diff
@@ -152,10 +168,35 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconcile Workload")
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if !features.Enabled(features.ObjectRetentionPolicies) || r.workloadRetention.afterFinished == nil {
+			return ctrl.Result{}, nil
+		}
+
```

  nit, I would suggest to drop the empty lines in this block. I don't think they really improve readability, and it is not so consistent with the style in the codebase. Yes, sometimes we use empty lines to emphasize different stages, but in this case there is an empty line almost after every line of code. This is too much.

- **test/integration/singlecluster/controller/core/workload_controller_test.go:590** by [@mimowo](https://github.com/mimowo) — 2025-05-14T07:40:56Z
```diff
@@ -544,3 +546,97 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns              *corev1.Namespace
+		createdWorkload kueue.Workload
+		localQueue      *kueue.LocalQueue
+		clusterQueue    *kueue.ClusterQueue
+		flavor          *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(
+			ctx, cfg,
+			managerAndControllerSetup(
+				&config.Configuration{
+					ObjectRetentionPolicies: &config.ObjectRetentionPolicies{
+						Workloads: &config.WorkloadRetentionPolicy{
+							AfterFinished: &metav1.Duration{
+								Duration: util.ShortTimeout,
+							},
+						},
+					},
+				},
+			),
+		)
+
+		gomega.Expect(features.SetEnable(features.ObjectRetentionPolicies, true)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "core-workload-")
+	})
+
+	ginkgo.AfterEach(func() {
+		createdWorkload = kueue.Workload{}
+		clusterQueue = nil
+		localQueue = nil
+		flavor = nil
```

  This should not be needed. If the variables are scoped to When, let's place them there

- **test/integration/singlecluster/controller/core/workload_controller_test.go:567** by [@mimowo](https://github.com/mimowo) — 2025-05-14T07:46:48Z
```diff
@@ -544,3 +546,97 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns              *corev1.Namespace
+		createdWorkload kueue.Workload
+		localQueue      *kueue.LocalQueue
+		clusterQueue    *kueue.ClusterQueue
+		flavor          *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(
+			ctx, cfg,
+			managerAndControllerSetup(
+				&config.Configuration{
+					ObjectRetentionPolicies: &config.ObjectRetentionPolicies{
+						Workloads: &config.WorkloadRetentionPolicy{
+							AfterFinished: &metav1.Duration{
+								Duration: util.ShortTimeout,
```

  I don't think ShortTimeout is a good choice here. It means 1s spent on every test without any coverage benefit (it is like 1, but with long timeout). 
  
  The possible variants I see:
  1. just use TinyTimeout and keep the asserts as they are
  2. use 5s timeout, verify it is not deleted immediately by Consistently, then verify it is deleted eventually
  3. add two different "When" section, one with TinyTimeout, one with LongTimeout, verify it gets deleted only if TinyTimeout is used
  
  All of them could be justified by having some merit. I would normally probably go with (1.) to delegate the time-based testing to unit tests where we can control the clock. However, in this case, given the nature of the feature, which could be very disruptive, I'm ok to be conservative and use (2.) or (3.).

- **test/integration/singlecluster/controller/core/admissioncheck_controller_test.go:35** by [@mimowo](https://github.com/mimowo) — 2025-05-14T07:48:02Z
```diff
@@ -32,7 +32,7 @@ var _ = ginkgo.Describe("AdmissionCheck controller", ginkgo.Ordered, ginkgo.Cont
 	var ns *corev1.Namespace
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil))
```

  Avoid these changes. I would suggest keeping `managerSetup` but just make it a wrapper for calling  managerAndControllerSetup(nil)

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-14T09:20:54Z

- **pkg/controller/core/core.go:127** by [@mykysha](https://github.com/mykysha) — 2025-05-14T09:20:53Z
```diff
@@ -112,6 +113,20 @@ func waitForPodsReady(cfg *configapi.WaitForPodsReady) *waitForPodsReadyConfig {
 	return &result
 }
 
+func workloadRetention(cfg *configapi.ObjectRetentionPolicies) *workloadRetentionConfig {
+	if cfg == nil || cfg.Workloads == nil {
+		return nil
+	}
+
+	result := &workloadRetentionConfig{}
+
+	if cfg.Workloads.AfterFinished != nil {
+		result.afterFinished = &cfg.Workloads.AfterFinished.Duration
+	}
+
+	return result
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-14T09:21:23Z

- **pkg/controller/core/workload_controller.go:178** by [@mykysha](https://github.com/mykysha) — 2025-05-14T09:21:23Z
```diff
@@ -152,10 +168,35 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconcile Workload")
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if !features.Enabled(features.ObjectRetentionPolicies) || r.workloadRetention.afterFinished == nil {
+			return ctrl.Result{}, nil
+		}
+
```

  Surely, have left only divisions between three main blocks here

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-14T09:22:05Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:590** by [@mykysha](https://github.com/mykysha) — 2025-05-14T09:22:05Z
```diff
@@ -544,3 +546,97 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns              *corev1.Namespace
+		createdWorkload kueue.Workload
+		localQueue      *kueue.LocalQueue
+		clusterQueue    *kueue.ClusterQueue
+		flavor          *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(
+			ctx, cfg,
+			managerAndControllerSetup(
+				&config.Configuration{
+					ObjectRetentionPolicies: &config.ObjectRetentionPolicies{
+						Workloads: &config.WorkloadRetentionPolicy{
+							AfterFinished: &metav1.Duration{
+								Duration: util.ShortTimeout,
+							},
+						},
+					},
+				},
+			),
+		)
+
+		gomega.Expect(features.SetEnable(features.ObjectRetentionPolicies, true)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "core-workload-")
+	})
+
+	ginkgo.AfterEach(func() {
+		createdWorkload = kueue.Workload{}
+		clusterQueue = nil
+		localQueue = nil
+		flavor = nil
```

  Moved to the respective When cases

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-14T09:22:57Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:567** by [@mykysha](https://github.com/mykysha) — 2025-05-14T09:22:57Z
```diff
@@ -544,3 +546,97 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns              *corev1.Namespace
+		createdWorkload kueue.Workload
+		localQueue      *kueue.LocalQueue
+		clusterQueue    *kueue.ClusterQueue
+		flavor          *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(
+			ctx, cfg,
+			managerAndControllerSetup(
+				&config.Configuration{
+					ObjectRetentionPolicies: &config.ObjectRetentionPolicies{
+						Workloads: &config.WorkloadRetentionPolicy{
+							AfterFinished: &metav1.Duration{
+								Duration: util.ShortTimeout,
```

  Went with (3.) here as I think that having 5s or something similar in (2.) would be unnecessarily long for the case

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-14T09:23:07Z

- **test/integration/singlecluster/controller/core/admissioncheck_controller_test.go:35** by [@mykysha](https://github.com/mykysha) — 2025-05-14T09:23:07Z
```diff
@@ -32,7 +32,7 @@ var _ = ginkgo.Describe("AdmissionCheck controller", ginkgo.Ordered, ginkgo.Cont
 	var ns *corev1.Namespace
 
 	ginkgo.BeforeAll(func() {
-		fwk.StartManager(ctx, cfg, managerSetup)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil))
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T09:30:54Z

- **pkg/controller/core/workload_controller.go:178** by [@mimowo](https://github.com/mimowo) — 2025-05-14T09:30:54Z
```diff
@@ -152,10 +168,35 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconcile Workload")
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if !features.Enabled(features.ObjectRetentionPolicies) || r.workloadRetention.afterFinished == nil {
+			return ctrl.Result{}, nil
+		}
+
```

  Cool, drop also the line just before the last `return ctrl.Result{}, nil`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T09:33:20Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:602** by [@mimowo](https://github.com/mimowo) — 2025-05-14T09:33:19Z
```diff
@@ -544,3 +546,169 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.When("manager is setup with tiny retention period", func() {
+		var (
+			ns              *corev1.Namespace
+			createdWorkload kueue.Workload
+			localQueue      *kueue.LocalQueue
+			clusterQueue    *kueue.ClusterQueue
+			flavor          *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(
+				ctx, cfg,
+				managerAndControllerSetup(
+					&config.Configuration{
+						ObjectRetentionPolicies: &config.ObjectRetentionPolicies{
+							Workloads: &config.WorkloadRetentionPolicy{
+								AfterFinished: &metav1.Duration{
+									Duration: util.TinyTimeout,
+								},
+							},
+						},
+					},
+				),
+			)
+
+			gomega.Expect(features.SetEnable(features.ObjectRetentionPolicies, true)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "core-workload-")
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			localQueue = testing.MakeLocalQueue("q", ns.Name).ClusterQueue("cq").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, flavor, true)
+		})
+
+		ginkgo.It("should delete the workload after retention period elapses", func() {
+			// Create workload
```

  Instead of adding comments use `ginkgo.By`. It will make the failure logs more readable when an issue happens (same for all new test cases)

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-14T10:22:13Z

- **test/integration/singlecluster/controller/core/workload_controller_test.go:602** by [@mykysha](https://github.com/mykysha) — 2025-05-14T10:22:13Z
```diff
@@ -544,3 +546,169 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload controller with resource retention", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.When("manager is setup with tiny retention period", func() {
+		var (
+			ns              *corev1.Namespace
+			createdWorkload kueue.Workload
+			localQueue      *kueue.LocalQueue
+			clusterQueue    *kueue.ClusterQueue
+			flavor          *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(
+				ctx, cfg,
+				managerAndControllerSetup(
+					&config.Configuration{
+						ObjectRetentionPolicies: &config.ObjectRetentionPolicies{
+							Workloads: &config.WorkloadRetentionPolicy{
+								AfterFinished: &metav1.Duration{
+									Duration: util.TinyTimeout,
+								},
+							},
+						},
+					},
+				),
+			)
+
+			gomega.Expect(features.SetEnable(features.ObjectRetentionPolicies, true)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "core-workload-")
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			localQueue = testing.MakeLocalQueue("q", ns.Name).ClusterQueue("cq").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, flavor, true)
+		})
+
+		ginkgo.It("should delete the workload after retention period elapses", func() {
+			// Create workload
```

  Replaced

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-14T10:22:32Z

- **pkg/controller/core/workload_controller.go:178** by [@mykysha](https://github.com/mykysha) — 2025-05-14T10:22:32Z
```diff
@@ -152,10 +168,35 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	log.V(2).Info("Reconcile Workload")
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
+		// manual deletion triggered by the user
 		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
 	}
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+	finishedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadFinished)
+	if finishedCond != nil && finishedCond.Status == metav1.ConditionTrue {
+		if !features.Enabled(features.ObjectRetentionPolicies) || r.workloadRetention.afterFinished == nil {
+			return ctrl.Result{}, nil
+		}
+
```

  Sure, done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T10:34:06Z

- **pkg/controller/core/workload_controller.go:108** by [@mimowo](https://github.com/mimowo) — 2025-05-14T10:34:06Z
```diff
@@ -95,6 +100,15 @@ func WithWorkloadUpdateWatchers(value ...WorkloadUpdateWatcher) Option {
 	}
 }
 
+// WithWorkloadRetention allows to specify retention for workload resources
+func WithWorkloadRetention(value *workloadRetentionConfig) Option {
+	return func(o *options) {
+		if value != nil && value.afterFinished != nil {
+			o.workloadRetentionConfig.afterFinished = value.afterFinished
+		}
```

  Actually, can we just say `o.workloadRetentionConfig= value`? It would simplify the code. Currently there is a mismatch: function name is generic, but we only copy the `afterFinished`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T12:06:13Z

/lgtm
/approve
Thanks a lot!
