# PR #2698: Document how to use Kueue for Deployments

**Summary**: Document how to use Kueue for Deployments

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2698

**Last updated**: 2024-08-05T16:32:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `doc-deployment` → `main`
- **Created**: 2024-07-25T14:43:27Z
- **Updated**: 2024-08-05T16:32:46Z
- **Merged**: 2024-08-05T14:29:00Z
- **Closed**: 2024-08-05T14:29:00Z
- **Labels**: `release-note`, `size/L`, `lgtm`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 3   **+116 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Documents how to use Kueue for Deployments
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2677

#### Special notes for your reviewer:
https://deploy-preview-2698--kubernetes-sigs-kueue.netlify.app/docs/tasks/run/deployment/

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Documentation on how to use Kueue for Deployments is added
```

## Discussion

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-25T14:43:36Z

/assign

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-25T14:43:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6451609503366a5b296d01fb26d2eb355ec7a8da |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66b0d4822ebedb0008293686 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2698--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2698--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI2OTgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.GmFyZv7f0-saMEdqjq1Fj3YYvIVa7PptEJyBWd2haWM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-29T09:27:23Z

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:35:15Z

cc @tenzen-y @dgrove-oss 
for other pairs of eyes, I believe this is just the beginning of the "serving" support in Kueue, so some rough edges might be polished in follow up PRs.

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-29T13:32:38Z

/cc @tenzen-y @dgrove-oss

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-29T13:32:41Z

@vladikkuzn: GitHub didn't allow me to request PR reviews from the following users: dgrove-oss.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2698#issuecomment-2255963095):

>/cc @tenzen-y @dgrove-oss


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-05T13:18:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2698#pullrequestreview-2210730607" title="Approved">dgrove-oss</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2698#pullrequestreview-2218983712" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2698#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-05T14:10:44Z

I think we can merge as are comments are addressed. We can follow up with improvements later.
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-05T14:10:51Z

LGTM label has been added.  <details>Git tree hash: 84261de1fcff235cd11d21b0b30af076462bce61</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-05T16:32:02Z

Thanks!
/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-08-05T16:32:45Z

@tenzen-y: new pull request created: #2774

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2698#issuecomment-2269468807):

>Thanks!
>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-29T10:35:30Z

- **site/content/en/docs/tasks/run/deployment.md:13** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:05:36Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
```

  I would suggest to introduce "Serving user", with the description. As a description maybe we can have 
  "A serving user runs serving workloads, for example, to expose a trained AI/ML model for inference"

- **site/content/en/docs/tasks/run/deployment.md:19** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:06:35Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
```

  ```suggestion
  1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
  2. Enable the Pod integration as described in [Run Plain Pods](...)
  ```
  I think this should be enough for now here.

- **site/content/en/docs/tasks/run/deployment.md:45** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:10:19Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
```

  Similarly, this seems like a repetition, I would skip it.

- **site/content/en/docs/tasks/run/deployment.md:60** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:11:07Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
```

  I don't think this is relevant either.

- **site/content/en/docs/tasks/run/deployment.md:93** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:12:24Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. The "managed" label
+
+Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
```

  I don't think this is important for users.

- **site/content/en/docs/tasks/run/deployment.md:97** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:13:29Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. The "managed" label
+
+Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
+
+### d. Limitations
+
+- A Kueue managed Deployment cannot be created in `kube-system` or `kueue-system` namespaces.
```

  I don't think this is an important limitation to mention here. Also, it can be altered by the `podOptions` so it is not really a strong limitation.

- **site/content/en/docs/tasks/run/deployment.md:98** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:14:05Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. The "managed" label
+
+Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
+
+### d. Limitations
+
+- A Kueue managed Deployment cannot be created in `kube-system` or `kueue-system` namespaces.
+- In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Deployment will
```

  I don't think this is accurate - the Deployement would not be deleted, because all of its pods are independent units of preemption.

- **site/content/en/docs/tasks/run/deployment.md:109** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:14:57Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. The "managed" label
+
+Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
+
+### d. Limitations
+
+- A Kueue managed Deployment cannot be created in `kube-system` or `kueue-system` namespaces.
+- In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Deployment will
+  be terminated and deleted.
+
+## Example Deployment
+
+Here is a sample Deployment:
+
+{{< include "examples/deployment-kueue/kueue-deployment.yaml" "yaml" >}}
+
+You can create the Deployment using the following command:
+```sh
+# Create the pod
```

  drop the comment

- **site/content/en/docs/tasks/run/deployment.md:31** by [@mimowo](https://github.com/mimowo) — 2024-07-29T10:25:07Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
```

  I would suggest to have a short sub-section of "Scale up or down", and mention: 
  
  "You may perform scale up or scale down operations on Deployments. 
  On scale down the exceess Pods are deleted and the quota is freed. 
  On scale up new Pods are created and remain suspended until their corresponding workloads get admitted.
  If there is not enough quota in your cluster, then the Deployment might be running only a subset of Pods."

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-07-30T14:48:40Z

Made a few suggestions.  Overall it looks like a good start.

- **site/content/en/docs/tasks/run/deployment.md:10** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-07-30T14:42:04Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
```

  I'd suggest providing a little more context.  For example,
  
  Although Kueue does not yet support managing a Deployment as a single workload, it is still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.

- **site/content/en/docs/tasks/run/deployment.md:71** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-07-30T14:45:04Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration.
```

  Maybe worth emphasizing that this is different than all the other "natively" supported kinds where you put the local queue in the metadata.labels.

- **site/content/en/docs/tasks/run/deployment.md:97** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-07-30T14:46:25Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. The "managed" label
+
+Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
+
+### d. Limitations
+
+- A Kueue managed Deployment cannot be created in `kube-system` or `kueue-system` namespaces.
```

  Maybe the limitation to mention is that the scope for Deployments  is implied by the pod integration's namespace selector.  No independent control for deployments.

- **site/content/en/docs/tasks/run/deployment.md:98** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-07-30T14:48:08Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+3. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration. 
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. The "managed" label
+
+Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
+
+### d. Limitations
+
+- A Kueue managed Deployment cannot be created in `kube-system` or `kueue-system` namespaces.
+- In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Deployment will
```

  I agree with @mimowo -- I don't see how this would be the case since Kueue has no notion of the Deployment as a schedulable unit.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-07-31T15:13:19Z

- **site/content/en/docs/tasks/run/deployment.md:45** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-07-31T15:13:19Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
```

  I intentionally repeated it from pod integration to "localize"
  Are you sure about referencing plain pod integration instead of duplicating the content with minor adjustements?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-31T15:40:33Z

- **site/content/en/docs/tasks/run/deployment.md:45** by [@mimowo](https://github.com/mimowo) — 2024-07-31T15:40:33Z
```diff
@@ -0,0 +1,111 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   A configuration for Kueue with enabled pod integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+2. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
```

  Yeah, I understand the intention, but I think this is a bit too much detail copy-pasted, imo. 
  
  Maybe in the introduction we could add some intro like:
  ```
  In this section we demonstrate how to support scheduling Deployments in Kueue 
  based on the Plain Pod integration, where every Pod from a Deployment is represented
  as a single independent Plain Pod. This approach allows independent resource
  management for the Pods, and thus scale up and down of the Deployment.
  ```
  Then, in the "Before you begin" section something like: 
  ```
  Follow steps in <link> to learn how to enable the ``v1/pod` integration and how to 
  configure it using the `podOptions` field.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-31T16:03:03Z

- **site/content/en/docs/tasks/run/deployment.md:29** by [@mimowo](https://github.com/mimowo) — 2024-07-31T16:03:03Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
```

  I think this can be dropped either. It is also part of the doc for Pod integration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-31T16:04:37Z

- **site/content/en/docs/tasks/run/deployment.md:15** by [@mimowo](https://github.com/mimowo) — 2024-07-31T16:04:37Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
```

  ```suggestion
  We demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
  ```
  since we already mention above "This page shows" setting the context.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-07-31T16:05:02Z

- **site/content/en/docs/tasks/run/deployment.md:85** by [@mimowo](https://github.com/mimowo) — 2024-07-31T16:05:02Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
+On scale up new Pods are created and remain suspended until their corresponding workloads get admitted.
+If there is not enough quota in your cluster, then the Deployment might be running only a subset of Pods.
+
+### d. Limitations
+
+- The scope for Deployments is implied by the pod integration's namespace selector. There's no independent control for deployments.
+
+## Example Deployment
```

  ```suggestion
  ## Example
  ```

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (approved) — 2024-07-31T17:17:42Z

lgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-01T06:40:45Z

- **site/content/en/docs/tasks/_index.md:49** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T05:58:10Z
```diff
@@ -44,6 +44,14 @@ As a batch user, you can learn how to:
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 
+### Serving user
+
+A _serving user_ runs [serving workloads](/docs/concepts/workload). 
```

  ```suggestion
  A _serving user_ runs [workloads](/docs/concepts/workload). 
  ```
  
  I think `workloads` should be enough since the serving users obviously use the serving workloads and we call the batch workloads as workloads.

- **site/content/en/docs/tasks/_index.md:53** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T05:59:38Z
```diff
@@ -44,6 +44,14 @@ As a batch user, you can learn how to:
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 
+### Serving user
+
+A _serving user_ runs [serving workloads](/docs/concepts/workload). 
+A serving user runs serving workloads, for example, to expose a trained AI/ML model for inference.
+
+As a serving user, you can learn how to:
+- [Run a Kueue managed Deployment](run/deployments).
```

  ```suggestion
  - [Run a Kueue managed Deployment](run/deployment).
  ```
  
  Does `run/deployments` work fine? I guess that we should add `run/deployment`.

- **site/content/en/docs/tasks/run/deployment.md:7** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T06:01:04Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
```

  ```suggestion
    Run a Deployment as a Kueue-managed workload.
  ```

- **site/content/en/docs/tasks/run/deployment.md:12** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T06:08:03Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
```

  ```suggestion
  Although Kueue does not yet support managing a Deployment as a single Workload, 
  ```
  
  I think that this `Workload` indicates the Kueue's Workload, right?

- **site/content/en/docs/tasks/run/deployment.md:29** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T06:11:24Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
```

  +1

- **site/content/en/docs/tasks/run/deployment.md:77** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T06:25:50Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
```

  ```suggestion
  On scale-in, the excess Pods are deleted, and the quota is freed.
  ```
  
  The `scale down` indicates vertical, and the `scale in` indicates the horizontal.

- **site/content/en/docs/tasks/run/deployment.md:78** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T06:27:20Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
+On scale up new Pods are created and remain suspended until their corresponding workloads get admitted.
```

  ```suggestion
  On scale-out, new Pods are created and remain suspended until their corresponding workloads get admitted.
  ```
  
  The `scale up` indicates vertical, and the `scale out` indicates the horizontal.

- **site/content/en/docs/tasks/run/deployment.md:80** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T06:40:41Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
+On scale up new Pods are created and remain suspended until their corresponding workloads get admitted.
+If there is not enough quota in your cluster, then the Deployment might be running only a subset of Pods.
+
```

  ```suggestion
  If there is not enough quota in your cluster, the Deployment might run only a subset of Pods. So, if your workloads are business-critical, you can consider reserving the quota only for the serving workloads by the ClusterQueue `lendingLimit`. The `lendingLimit` allows you to rapidly scale out the critical serving workload.
  
  For more `lendingLimit` details, please see the [ClusterQueue page](docs/concepts/cluster_queue#lendinglimit).
  ```
  
  I would recommend using the `lendingLimit` for the critical serving workloads: https://kueue.sigs.k8s.io/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ResourceQuota

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T06:57:09Z

- **site/content/en/docs/tasks/run/deployment.md:80** by [@mimowo](https://github.com/mimowo) — 2024-08-01T06:57:09Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
+On scale up new Pods are created and remain suspended until their corresponding workloads get admitted.
+If there is not enough quota in your cluster, then the Deployment might be running only a subset of Pods.
+
```

  +1

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T07:02:54Z

- **site/content/en/docs/tasks/run/deployment.md:77** by [@mimowo](https://github.com/mimowo) — 2024-08-01T07:02:54Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
```

  Do you have some reference for that? I'm looking at the k8s docs https://kubernetes.io/docs/concepts/workloads/controllers/deployment/, and I uses of scale up / down.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T07:03:41Z

- **site/content/en/docs/tasks/_index.md:49** by [@mimowo](https://github.com/mimowo) — 2024-08-01T07:03:41Z
```diff
@@ -44,6 +44,14 @@ As a batch user, you can learn how to:
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 
+### Serving user
+
+A _serving user_ runs [serving workloads](/docs/concepts/workload). 
```

  +1

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-01T07:08:35Z

- **site/content/en/docs/tasks/run/deployment.md:77** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T07:08:35Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
```

  Oh, I didn't know that.
  But, in general, we use the scale-up for the vertical like this: https://azure.microsoft.com/en-us/resources/cloud-computing-dictionary/scaling-out-vs-scaling-up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T07:16:40Z

- **site/content/en/docs/tasks/run/deployment.md:77** by [@mimowo](https://github.com/mimowo) — 2024-08-01T07:16:40Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed job.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+In this section we demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Kueue will run webhooks for all created pods if the pod integration is enabled. The webhook namespaceSelector could be 
+   used to filter the pods to reconcile. The default webhook namespaceSelector is:
+   ```yaml
+   matchExpressions:
+   - key: kubernetes.io/metadata.name
+     operator: NotIn
+     values: [ kube-system, kueue-system ]
+   ```
+   
+   When you [install Kueue via Helm](/docs/installation/#install-via-helm), the webhook namespace selector 
+   will match the `integrations.podOptions.namespaceSelector` in the `values.yaml`.
+
+   Make sure that namespaceSelector never matches the kueue namespace, otherwise the 
+   Kueue deployment won't be able to create Pods.
+
+4. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a Deployment admitted by Kueue
+
+When running Deployment on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `spec.template.metadata.labels` section of the Deployment configuration. 
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the Deployment,
+the queue name should be specified at the Pod level.
+
+```yaml
+spec:
+   template:
+      metadata:
+         labels:
+            kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+    - resources:
+        requests:
+          cpu: 3
+```
+
+### c. Scaling
+
+You may perform scale up or scale down operations on Deployments.
+On scale down the excess Pods are deleted and the quota is freed.
```

  I see, I'm ok with the proposed naming, it makes sense to discriminate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-05T13:16:03Z

- **site/content/en/docs/tasks/run/deployment.md:17** by [@mimowo](https://github.com/mimowo) — 2024-08-05T13:16:03Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Run Deployment"
+linkTitle: "Deployment"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a Deployment as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running Deployments.
+Although Kueue does not yet support managing a Deployment as a single Workload, 
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the Deployment.
+
+We demonstrate how to support scheduling Deployments in Kueue based on the Plain Pod integration,
+where every Pod from a Deployment is represented as a single independent Plain Pod.
+This approach allows independent resource management for the Pods, and thus scale up and down of the Deployment.
```

  ```suggestion
  This approach allows independent resource management for the Pods, and thus scale-out and scale-in of the Deployment.
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-05T13:18:22Z

/approve
Just one nit from me. @tenzen-y anything to add?
