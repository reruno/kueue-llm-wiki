# PR #2704: [site] Update classic preemption candidates selection description.

**Summary**: [site] Update classic preemption candidates selection description.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2704

**Last updated**: 2024-08-08T14:09:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `borrow-fix-doc` → `main`
- **Created**: 2024-07-26T07:40:51Z
- **Updated**: 2024-08-08T14:09:40Z
- **Merged**: 2024-08-08T10:09:25Z
- **Closed**: 2024-08-08T10:09:25Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 1   **+13 / -8**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Update classic preemption candidates selection description.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2681

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-07-26T07:41:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 64c84b571786be55fd6ecde29ae169a1961536da |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66b49569e16b330008f9c338 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2704--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2704--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI3MDQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.FvpluL9NzM_huBNsicxhhyydjUth4botT_rFyqgN3Fc)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@trasc](https://github.com/trasc) — 2024-07-26T07:41:11Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-07-26T08:26:31Z

/assign @gabesaba 
for the first pass

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-07T09:00:40Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-07T09:00:44Z

LGTM label has been added.  <details>Git tree hash: 7a6fab62dc8e86cf82f5c5324cf72a332049a0bb</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-08T09:58:57Z

LGTM label has been added.  <details>Git tree hash: 2d6a94e80c9c4fe19e76eb77448508f9628929fe</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-08T09:59:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2704#pullrequestreview-2227413673" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2704#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-08T10:01:06Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-08-08T10:01:09Z

@mimowo: once the present PR merges, I will cherry-pick it on top of website in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2704#issuecomment-2275433142):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-08-08T10:10:06Z

@mimowo: new pull request created: #2798

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2704#issuecomment-2275433142):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-07-30T09:39:34Z

- **site/content/en/docs/concepts/preemption.md:60** by [@gabesaba](https://github.com/gabesaba) — 2024-07-30T09:39:34Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
```

  Does usage have to be below nominal? If we have borrowWithinCohort, I think we can preempt while borrowing
  
  https://github.com/kubernetes-sigs/kueue/blob/a128ae3354a56670ffa58695fef1eca270fe3a47/pkg/scheduler/flavorassigner/flavorassigner.go#L576-L583
  https://github.com/kubernetes-sigs/kueue/blob/a128ae3354a56670ffa58695fef1eca270fe3a47/pkg/scheduler/flavorassigner/flavorassigner.go#L612-L613

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-01T12:45:50Z

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-01T12:45:45Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
```

  this refers to the quota reclaim scenario.  the ` if a.canPreemptWhileBorrowing() ..` is described above.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-06T12:34:56Z

- **site/content/en/docs/concepts/preemption.md:60** by [@gabesaba](https://github.com/gabesaba) — 2024-08-06T12:13:58Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
```

  You mean the list item on line 58? Got it. I think my confusion came from thinking the list items were an `AND`, rather than an `OR`

- **site/content/en/docs/concepts/preemption.md:57** by [@gabesaba](https://github.com/gabesaba) — 2024-08-06T12:30:06Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
```

  ```suggestion
  - **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
  ```

- **site/content/en/docs/concepts/preemption.md:60** by [@gabesaba](https://github.com/gabesaba) — 2024-08-06T12:33:22Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
```

  nit: change order (non-borrowing first) to match `Classical Preemption` section below 
  
  ... or only go into detail in one place - let's see what we conclude in [this comment thread](https://github.com/kubernetes-sigs/kueue/pull/2704#discussion_r1705455929)

- **site/content/en/docs/concepts/preemption.md:57** by [@gabesaba](https://github.com/gabesaba) — 2024-08-06T12:33:48Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
```

  at a higher level (not caused by this PR, but a comment on the previous state of the documentation):
  
  We have a whole section for `Classical Preemption` and for `Fair Sharing`. Does it make sense to have the description very high-level here, and go into detail in those respective sections? Or to make those sections subsections of `Preemption algorithms`, rather than top-level sections on their own?
  
  also curious to hear @mimowo's thoughts here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-06T12:56:40Z

- **site/content/en/docs/concepts/preemption.md:57** by [@mimowo](https://github.com/mimowo) — 2024-08-06T12:56:40Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
```

  I think the main objective is to make docs clear for readers, and the current structure seems to serve the purpose.  Putting them under the `Preemption algorithms` seems more accurate, but they have some subsections, so I'm not sure how this would impact formatting. I'm leaning to keep the structure as is for this PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-06T18:20:09Z

- **site/content/en/docs/concepts/preemption.md:57** by [@trasc](https://github.com/trasc) — 2024-08-06T18:13:27Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
```

  done

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-06T18:15:33Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
```

  done

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-06T18:15:35Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
```

  done

- **site/content/en/docs/concepts/preemption.md:57** by [@trasc](https://github.com/trasc) — 2024-08-06T18:15:59Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when:
```

  Let's keep it as is for now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-07T11:52:46Z

Lgtm, just some tweaks to make it more readable.

- **site/content/en/docs/concepts/preemption.md:60** by [@mimowo](https://github.com/mimowo) — 2024-08-07T09:26:38Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  ```suggestion
   - other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
  ```

- **site/content/en/docs/concepts/preemption.md:81** by [@mimowo](https://github.com/mimowo) — 2024-08-07T09:41:11Z
```diff
@@ -73,9 +76,9 @@ to issue preemptions when one of the following is true:
 
 ### Candidates
 
-The list of preemption candidates is compiled from Workloads within the Cluster
-Queue satisfying the `withinClusterQueue` policy, and Workloads within the
-cohort which satisfy the `reclaimWithinCohort` policy.
+The list of preemption candidates is compiled from Workloads within the Cluster Queue satisfying 
+the `withinClusterQueue` policy, and Workloads within the cohort which satisfy the `reclaimWithinCohort`
+policy of other ClusterQueues that are actively borrowing.
```

  ```suggestion
  The list of preemption candidates is compiled from Workloads:
  - belonging to the same Cluster Queue as the preemptor Workload, and satisfing the `withinClusterQueue` policy of the preemptor's Cluster Queue
  - belonging to other Cluster Queues in the cohort, which are actively borrowing, and satisfying the `reclaimWithinCohort` and `borrowWithinCohort` policies of the preemptor's Cluster Queue.
  ```
  I think this will read better as this sentence is rather long

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-07T13:17:57Z

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-07T13:17:57Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  the "or" refers to the cluster queue a candidate belongs to.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-07T13:25:40Z

- **site/content/en/docs/concepts/preemption.md:60** by [@mimowo](https://github.com/mimowo) — 2024-08-07T13:25:40Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  TBH I don't understand this then. Do you think the changes in the overview of the algorithms are required?
  In particular mentioning ``borrowWithinCohort` here does not feel coherent with the rest of the paragraph,
  
  I would suggest to retract the changes from the paragraph, I think the previous wording, while not very detailed gives enough overview. We can explain in more detail in the dedicated section. wdyt @gabesaba ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-07T16:07:53Z

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-07T16:07:53Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  For me, this covers the source of the confusion for which the issue was opened.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-07T16:14:11Z

- **site/content/en/docs/concepts/preemption.md:60** by [@mimowo](https://github.com/mimowo) — 2024-08-07T16:14:11Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  I know, but think it is better to explain it in one place, in the section dedicated to the algorithm details. We have all the necessary context there. I think mentioning `borrowWithinCohort` here seems too low level, and detached from the rest of the high-level paragraph. TBH I still don't understand this sentence.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-07T16:52:55Z

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-07T16:52:55Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  The problem is that the two sections will be out of sync.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-07T16:57:10Z

- **site/content/en/docs/concepts/preemption.md:60** by [@mimowo](https://github.com/mimowo) — 2024-08-07T16:57:10Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  It is ok, they serve different purposes. This is just an abstract, it does not need to cover the algorithm in every detail. I don't want to go the path of repeating the same content twice.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-07T17:04:38Z

- **site/content/en/docs/concepts/preemption.md:81** by [@trasc](https://github.com/trasc) — 2024-08-07T17:04:38Z
```diff
@@ -73,9 +76,9 @@ to issue preemptions when one of the following is true:
 
 ### Candidates
 
-The list of preemption candidates is compiled from Workloads within the Cluster
-Queue satisfying the `withinClusterQueue` policy, and Workloads within the
-cohort which satisfy the `reclaimWithinCohort` policy.
+The list of preemption candidates is compiled from Workloads within the Cluster Queue satisfying 
+the `withinClusterQueue` policy, and Workloads within the cohort which satisfy the `reclaimWithinCohort`
+policy of other ClusterQueues that are actively borrowing.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-07T17:05:48Z

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-07T17:05:48Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  It's not ok, they describe the same thing.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-08T09:53:34Z

- **site/content/en/docs/concepts/preemption.md:60** by [@trasc](https://github.com/trasc) — 2024-08-08T09:53:34Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  rephrased

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-08T09:58:53Z

/lgtm
/approve
Thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-08T10:00:48Z

- **site/content/en/docs/concepts/preemption.md:60** by [@mimowo](https://github.com/mimowo) — 2024-08-08T10:00:48Z
```diff
@@ -54,11 +54,14 @@ Kueue offers two preemption algorithms. The main difference between them is the
 preemptions from a ClusterQueue to others in the Cohort, when the usage of the preempting ClusterQueue is
 already above the nominal quota. The algorithms are:
 
-- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when the usage of the preempting ClusterQueue
-  will be under the nominal quota after the ongoing admission process, or when all the candidates for preemption belong to
-  the same ClusterQueue as the preempting Workload. In other words, ClusterQueues
-  can only borrow quota from others in the cohort if they do not preempt admitted Workloads from
-  other ClusterQueues. ClusterQueues in a cohort borrow resources in a first-come first-served fashion.
+- **[Classic Preemption](#classic-preemption)**: Preemption in the cohort only happens when either of the following occurs:
+  - the usage of the preempting ClusterQueue will be under the nominal quota after the ongoing admission process
+  - all the candidates for preemption belong to the same ClusterQueue as the preempting Workload 
+    or other borrowing ClusterQueues within the same cohort matching the preemptor's queue `borrowWithinCohort` policy.
```

  I think the final version is better than the initial proposals. Thanks for the update!
