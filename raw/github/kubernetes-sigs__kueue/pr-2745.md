# PR #2745: [e2e] Ignore cluster delete failures

**Summary**: [e2e] Ignore cluster delete failures

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2745

**Last updated**: 2025-01-13T10:46:23Z

---

## Metadata

- **State**: closed
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `flaky-e2e-delete-cluster` → `main`
- **Created**: 2024-08-01T12:34:35Z
- **Updated**: 2025-01-13T10:46:23Z
- **Merged**: —
- **Closed**: 2024-12-12T10:00:11Z
- **Labels**: `kind/bug`, `kind/flake`, `release-note-none`, `size/XS`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+1 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
/kind flake

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Ignore the failure of cluster deletion in the e2e test's cleanup.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2738 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-01T12:34:42Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2745#" title="Author self-approved">trasc</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [mimowo](https://github.com/mimowo) for approval. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-01T12:34:53Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8fcd637646ad060a38566c468d818c4b7c5543ba |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66b340b7b47864000890d532 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2745--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2745--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI3NDUtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.K38vXdnZRsqYHAW_Cs9O6cbsPvVZeqp8zl6052NRSEA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@trasc](https://github.com/trasc) — 2024-08-01T13:40:27Z

/cc @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-06T12:35:25Z

/close 
I don't think we have enough evidence to justify ignoring cluster deletion errors in Kueue based on a single observed failure (see [comment](https://github.com/kubernetes-sigs/kueue/pull/2745#discussion_r1703552975)). I'm concerned that doing so could sweep issues under the carpet, leading to unnoticed cluster leaks by the team. A similar sentiment was expressed in [this comment](https://github.com/kubernetes-sigs/kueue/pull/2745#discussion_r1700649043).

For now, I'm open to exploring solutions on another layer, possibly as suggested in [this comment](https://github.com/kubernetes-sigs/kueue/pull/2745#discussion_r1703567857), but I'll leave the decision to @BenTheElder.

I'm also open to revisiting the idea of ignoring errors in Kueue if this issue becomes more frequent, we rule out that the errors are related to Kueue, and we've exhausted options for resolving it at other layers.

We can keep the issue open for further discussion.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-06T12:35:29Z

@mimowo: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2745#issuecomment-2271185125):

>/close 
>I don't think we have enough evidence to justify ignoring cluster deletion errors in Kueue based on a single observed failure (see [comment](https://github.com/kubernetes-sigs/kueue/pull/2745#discussion_r1703552975)). I'm concerned that doing so could sweep issues under the carpet, leading to unnoticed cluster leaks by the team. A similar sentiment was expressed in [this comment](https://github.com/kubernetes-sigs/kueue/pull/2745#discussion_r1700649043).
>
>For now, I'm open to exploring solutions on another layer, possibly as suggested in [this comment](https://github.com/kubernetes-sigs/kueue/pull/2745#discussion_r1703567857), but I'll leave the decision to @BenTheElder.
>
>I'm also open to revisiting the idea of ignoring errors in Kueue if this issue becomes more frequent, we rule out that the errors are related to Kueue, and we've exhausted options for resolving it at other layers.
>
>We can keep the issue open for further discussion.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-07T09:05:29Z

/reopen
I synced in @trasc who suggests we should continue the PR open for discussion and possibly merging. 
I would still be leaning to a fix in Kind, but let's keep both options open.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-07T09:05:33Z

@mimowo: Failed to re-open PR: state cannot be changed. The flaky-e2e-delete-cluster branch was force-pushed or recreated.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2745#issuecomment-2272986575):

>/reopen
>I synced in @trasc who suggests we should continue the PR open for discussion and possibly merging. 
>I would still be leaning to a fix in Kind, but let's keep both options open.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-07T09:35:43Z

/reopen
retrying

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-07T09:35:47Z

@mimowo: Failed to re-open PR: state cannot be changed. The flaky-e2e-delete-cluster branch was force-pushed or recreated.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2745#issuecomment-2273044755):

>/reopen
>retrying


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-08-07T09:38:21Z

/reopen

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-07T09:38:25Z

@trasc: Reopened this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2745#issuecomment-2273049543):

>/reopen


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-12T10:00:06Z

/close 
we haven't seen the issue in the last couple of months. We can re-evaluate options when this repeats, as also suggested here: https://github.com/kubernetes-sigs/kueue/issues/2738#issuecomment-2538418306

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-12T10:00:12Z

@mimowo: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2745#issuecomment-2538421406):

>/close 
>we haven't seen the issue in the last couple of months. We can re-evaluate options when this repeats, as also suggested here: https://github.com/kubernetes-sigs/kueue/issues/2738#issuecomment-2538418306


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T12:52:37Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-01T12:52:37Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  Can you point out another example where we ignore such a failure?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-01T12:59:46Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-01T12:59:46Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  the three lines above

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T13:30:51Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-01T13:30:51Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  No strong view, but maybe it makes sense to only merge the `-v3` so that we can get more input if this repeats?
  Otherwise we will not know if this happened.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-01T13:39:52Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-01T13:39:52Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  Unfortunately I doubt that  `-v=3` will make much of a difference (based on how I see the implementation in kind).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T15:16:12Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-01T15:16:11Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  So I see two paths forward:
  1. only increase log verbosity now (the level TBD, maybe v5 is better?), and wait for repeating to see if we can understand it
  2. ignore the errors as in the current version of the PR
  
  I would be slightly leaning towards 1, as we are not under any pressure (the flake is rare), and it would be nice to understand what is going on.
  
  OTOH I'm ok to accept as is, if this is @tenzen-y preference.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T15:18:18Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-01T15:18:18Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  Also, a related question, how long does the `kind delete cluster` normally take? 
  
  If the time is normally within 10-20s, then it is likely that occasionally it just takes 30s. If this is normally 5s then I suspect there might be some resource leak, or a bug in kind we could report.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-01T16:33:44Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-01T16:33:44Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  The timeout is for killing the docker process, is not part of our setup, it's part of the image used to run the test in the CI, it's failure is already ignored.
  
  Kind is just dropping any output of the `docker delete ` command and only prints the exit code.
  
  In kind,  I was not able to find any `log.V(n)` where n > 3.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-01T16:41:47Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-01T16:41:47Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  I see, how long does it normally take to delete the kind cluster? 
  
  Could it be that a bug in kind makes it exceptionally long sometimes? If so, maybe we can open a bug in kind and see if other folks are are of the problem.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-01T17:58:37Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-01T17:58:37Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  Docker fails to delete a container, kind only calls docker.
  
  Why is deleting the cluster, in this context, a lot more relevant then exporting the logs?
  (None of them are relvant to the kueue implementation)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-01T18:36:55Z

- **hack/e2e-common.sh:37** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-01T18:36:55Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  I would prefer to select opt 1 since once we omit errors, we never find errors in the phase of the deletion cluster.
  I understand that the current deletion error is irrelevant to Kueue, but there is no guarantee for the future.
  
  > only increase log verbosity now (the level TBD, maybe v5 is better?), and wait for repeating to see if we can understand it
  
  My only concern for the increasing log level is huge log files.
  IIRC, previously, we faced flaky tests due to the huge size of the log file.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-01T19:16:42Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-01T19:16:42Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  We are talking about `kind delete` v option which, based on what I can see in the kind implementation will not do too much.
  
  And once again, there are no logs in kind done with a level higher than 3.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-02T06:35:26Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-02T06:35:26Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  > Why is deleting the cluster, in this context, a lot more relevant then exporting the logs?
  
  If there is a bug to archive logs we only impact Kueue, with a bug to delete a cluster we may impact the infra.  I'm concerned with the risk of leaving clusters.
  
  For example, if we silence the errors, and there is even a typo in the command to delete a cluster we would be  leaving orphan clusters, no? Maybe we could consider retry of the delete, but I'm not sure if the gain / complication ratio makes worth it.
  
  I searched for similar issues and found https://github.com/helm/kind-action/issues/54, which leads to https://github.com/kubernetes-sigs/kind/issues/708. So, it seems this might be some issue with docker, where restart helps.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-02T07:19:33Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-02T07:19:33Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  > If there is a bug to archive logs we only impact Kueue,
  
  How, it could be some kind or docker issue for all that we know. 
  
  >  with a bug to delete a cluster we may impact the infra. I'm concerned with the risk of leaving clusters.
  
  The test image also try to kill the docker daemon at this end of the test but if that fails it is just ignored.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-02T07:32:59Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-02T07:32:59Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  > The test image also try to kill the docker daemon at this end of the test but if that fails it is just ignored.
  
  could you point out the code to support this?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-02T07:44:24Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-02T07:44:24Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  
  https://github.com/kubernetes/test-infra/blob/34cbc7e7fbf0036e615ec70031aabe43b03d8501/images/bootstrap/runner.sh#L18-L28
  
  (Updated)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-02T07:58:17Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-02T07:58:17Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  Thanks, this is actually dims's fork, but similar code is [here](https://github.com/kubernetes/test-infra/blob/master/images/bootstrap/runner.sh). 
  
  @BenTheElder as the author of some of the code around there, would you say that it is safe to ignore the cluster failure delete in Kueue?

### Review by [@BenTheElder](https://github.com/BenTheElder) (commented) — 2024-08-02T17:15:01Z

- **hack/e2e-common.sh:37** by [@BenTheElder](https://github.com/BenTheElder) — 2024-08-02T17:15:01Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  it's not the end of the world to ignore but ...
  
  Cluster delete failure should not be happening. Cluster deletion is reentrant and will succeed if there is nothing to do.

### Review by [@BenTheElder](https://github.com/BenTheElder) (commented) — 2024-08-02T17:15:35Z

- **hack/e2e-common.sh:37** by [@BenTheElder](https://github.com/BenTheElder) — 2024-08-02T17:15:35Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  The linked kind issue is really old, do we have current examples?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-02T17:21:30Z

- **hack/e2e-common.sh:37** by [@mimowo](https://github.com/mimowo) — 2024-08-02T17:21:30Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  >Cluster delete failure should not be happening. 
  
  Yeah, but they do, as shown in the example. I'm not sure about the frequency, 
  
  > Cluster deletion is reentrant and will succeed if there is nothing to do.
  
  So, we may expect there is a good chance a retry would help? This might be safer, but it is a code complication, so I'm not convinced either.
  
  Maybe we just park it and only proceed with ignoring if this repeats, so that we see the frequency? Did you see this before @trasc or @tenzen-y ?

### Review by [@BenTheElder](https://github.com/BenTheElder) (commented) — 2024-08-02T17:23:14Z

- **hack/e2e-common.sh:37** by [@BenTheElder](https://github.com/BenTheElder) — 2024-08-02T17:23:14Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  > Kind is just dropping any output of the docker delete  command and only prints the exit code.
  
  that's not accurate. in kind exec errors contain the command output (which is always captured) and it is being printed in the error message ...

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-05T05:29:07Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-05T05:29:07Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  > > Kind is just dropping any output of the docker delete  command and only prints the exit code.
  > 
  > that's not accurate. in kind exec errors contain the command output (which is always captured) and it is being printed in the error message ...
  
  Indeed, I missed that.
  
  > Cluster delete failure should not be happening. Cluster deletion is reentrant and will succeed if there is nothing to do.
  
  It is, more or less, happening in our case as `cleanup_dind()` is able to delete all the containers.
  
  ```
  Waiting 30 seconds for pods stopped with terminationGracePeriod:30
  Cleaning up after docker
  ed68da3fb667
  6beb571d417e
  bf442dfcffc1
  ...
  ```
  
  > Did you see this before @trasc or @tenzen-y ?
  
  No

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-05T05:51:36Z

- **hack/e2e-common.sh:37** by [@trasc](https://github.com/trasc) — 2024-08-05T05:51:36Z
```diff
@@ -34,7 +34,7 @@ function cluster_cleanup {
         $KIND export logs $ARTIFACTS --name $1 || true
         kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
         kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
-        $KIND delete cluster --name $1
+        $KIND delete cluster -v=3 --name $1 || echo "Ignoring cluster delete failure."
```

  @BenTheElder I think we could make the `DeleteNodes` a two step op, `docker stop` (this is able to send two signals with a timeout between them) and `docker rm ` after that. 
  
  I can open a PR in kind for this, just let me know.
