# PR #278: Feat: add support for replacing internal cert management

**Summary**: Feat: add support for replacing internal cert management

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/278

**Last updated**: 2022-07-15T17:06:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feat/add-enable-internal-cert-flag` → `main`
- **Created**: 2022-06-10T06:01:32Z
- **Updated**: 2022-07-15T17:06:40Z
- **Merged**: 2022-07-15T16:56:19Z
- **Closed**: 2022-07-15T16:56:19Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 8   **+211 / -30**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add DisableInternalCertManagement to Component Config as an option for end-users to choose whether to use the internal cert management or a third-party one like cert manager.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/266

#### Special notes for your reviewer:
None

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-07-14T18:30:16Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/278#pullrequestreview-1038866126" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/278#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-07-15T03:13:23Z

squashed

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-07-15T15:14:19Z

It's still hold now.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-15T16:54:43Z

/hold cancel

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-13T13:57:22Z

- **apis/config/v1alpha1/configuration_types.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T13:54:22Z
```diff
@@ -38,6 +38,11 @@ type Configuration struct {
 	// Defaults to false; therefore, those jobs are not managed and if they are created
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
+
+	// DisableInternalCertManagement controls whether to disable internal cert management or not,
+	// default to false. If you want to use a third-party management, e.g. cert-manager,
+	// set it to true. See the user guide for more information.
+	DisableInternalCertManagement bool `json:"disableInternalCertManagement"`
```

  let's call it `EnableInternalCertManagement` and make it a pointer so that the default can be true.

- **docs/setup/install.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T13:55:09Z
```diff
@@ -8,6 +8,12 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+Webhook Server in kueue uses a internal cert management for provisioning certificates, if you want to use
```

  ```suggestion
  The webhook server in kueue uses a internal cert management for provisioning certificates. If you want to use
  ```

- **docs/setup/install.md:12** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T13:55:25Z
```diff
@@ -8,6 +8,12 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+Webhook Server in kueue uses a internal cert management for provisioning certificates, if you want to use
+  a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), please
```

  ```suggestion
    a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
  ```

- **docs/setup/install.md:13** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T13:56:08Z
```diff
@@ -8,6 +8,12 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+Webhook Server in kueue uses a internal cert management for provisioning certificates, if you want to use
+  a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), please
+  1. Set `disableInternalCertManagement` to true in kueue Configuration.
```

  link to `#install-a-custom-configured-released-version`

- **docs/setup/install.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T13:56:57Z
```diff
@@ -8,6 +8,12 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+Webhook Server in kueue uses a internal cert management for provisioning certificates, if you want to use
+  a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), please
+  1. Set `disableInternalCertManagement` to true in kueue Configuration.
+  2. Disable the `internalcert` in `config/default/kustomization.yaml`.
```

  ```suggestion
    2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-06-13T16:32:23Z

- **apis/config/v1alpha1/configuration_types.go:45** by [@kerthcet](https://github.com/kerthcet) — 2022-06-13T16:32:23Z
```diff
@@ -38,6 +38,11 @@ type Configuration struct {
 	// Defaults to false; therefore, those jobs are not managed and if they are created
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
+
+	// DisableInternalCertManagement controls whether to disable internal cert management or not,
+	// default to false. If you want to use a third-party management, e.g. cert-manager,
+	// set it to true. See the user guide for more information.
+	DisableInternalCertManagement bool `json:"disableInternalCertManagement"`
```

  Done :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-13T17:47:20Z

- **apis/config/v1alpha1/configuration_types.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T17:45:38Z
```diff
@@ -38,6 +38,11 @@ type Configuration struct {
 	// Defaults to false; therefore, those jobs are not managed and if they are created
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
+
+	// EnableInternalCertManagement controls whether to enable internal cert management or not,
+	// default to true. If you want to use a third-party management, e.g. cert-manager,
```

  ```suggestion
  	// EnableInternalCertManagement controls whether to enable internal cert management or not.
  	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
  ```

- **main.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T17:46:55Z
```diff
@@ -65,6 +65,13 @@ func init() {
 	//+kubebuilder:scaffold:scheme
 }
 
+func defaultConfiguration() configv1alpha1.Configuration {
+	cfg := configv1alpha1.Configuration{}
+	defaultEnabled := true
+	cfg.EnableInternalCertManagement = &defaultEnabled
```

  you can use `pointer.Bool`

- **main.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-13T17:46:56Z
```diff
@@ -65,6 +65,13 @@ func init() {
 	//+kubebuilder:scaffold:scheme
 }
 
+func defaultConfiguration() configv1alpha1.Configuration {
```

  it should be in the api's package

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-06-14T02:39:07Z

- **main.go:68** by [@kerthcet](https://github.com/kerthcet) — 2022-06-14T02:39:07Z
```diff
@@ -65,6 +65,13 @@ func init() {
 	//+kubebuilder:scaffold:scheme
 }
 
+func defaultConfiguration() configv1alpha1.Configuration {
```

  Good catch.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-14T14:52:08Z

- **main.go:93** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-14T14:52:02Z
```diff
@@ -95,7 +88,7 @@ func main() {
 		LeaderElectionID:       "c1f6bfd2.kueue.x-k8s.io",
 	}
 	var err error
-	config := defaultConfiguration()
+	config := configv1alpha1.DefaultConfiguration()
 	if configFile != "" {
 		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
```

  Does this actually preserve the values that were already set?
  
  Maybe we should have a function `SetDefaults` that applies after the file was loaded.
  
  And unit test that.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-06-14T17:23:52Z

- **main.go:93** by [@kerthcet](https://github.com/kerthcet) — 2022-06-14T17:23:52Z
```diff
@@ -95,7 +88,7 @@ func main() {
 		LeaderElectionID:       "c1f6bfd2.kueue.x-k8s.io",
 	}
 	var err error
-	config := defaultConfiguration()
+	config := configv1alpha1.DefaultConfiguration()
 	if configFile != "" {
 		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
```

  > Does this actually preserve the values that were already set?
  > 
  
  Yes, values configured in yaml will overwrite the default ones.
  
  > Maybe we should have a function `SetDefaults` that applies after the file was loaded.
  
  I think `Defaults` should be called firstly, then we can overwrite them consciously.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-14T17:29:24Z

- **main.go:93** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-14T17:29:24Z
```diff
@@ -95,7 +88,7 @@ func main() {
 		LeaderElectionID:       "c1f6bfd2.kueue.x-k8s.io",
 	}
 	var err error
-	config := defaultConfiguration()
+	config := configv1alpha1.DefaultConfiguration()
 	if configFile != "" {
 		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
```

  I would prefer we do the same we do in upstream, which sets defaults after loading the file.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-06-15T03:05:29Z

- **main.go:93** by [@kerthcet](https://github.com/kerthcet) — 2022-06-15T03:05:28Z
```diff
@@ -95,7 +88,7 @@ func main() {
 		LeaderElectionID:       "c1f6bfd2.kueue.x-k8s.io",
 	}
 	var err error
-	config := defaultConfiguration()
+	config := configv1alpha1.DefaultConfiguration()
 	if configFile != "" {
 		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
```

  Could you please provide some examples in upstream? In kube-scheduler, we also call `Default()` first then we complete the other configurations.

### Review by [@haosdent](https://github.com/haosdent) (commented) — 2022-06-26T15:43:52Z

- **docs/setup/install.md:11** by [@haosdent](https://github.com/haosdent) — 2022-06-26T15:43:52Z
```diff
@@ -8,6 +8,12 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+The webhook server in kueue uses a internal cert management for provisioning certificates. If you want to use
```

  s/a internal/an internal/g

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-06-27T18:51:50Z

- **main.go:93** by [@alculquicondor](https://github.com/alculquicondor) — 2022-06-27T18:49:50Z
```diff
@@ -95,7 +88,7 @@ func main() {
 		LeaderElectionID:       "c1f6bfd2.kueue.x-k8s.io",
 	}
 	var err error
-	config := defaultConfiguration()
+	config := configv1alpha1.DefaultConfiguration()
 	if configFile != "" {
 		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
```

  It's hard to see, but when we load a config file, we pass it through the decoder https://github.com/kubernetes/kubernetes/blob/c6b0652b89ad64e8d86d47fc2e4089e281732ede/cmd/kube-scheduler/app/options/configfile.go#L44
  
  the decoder applies the default for the scheme, setup here: https://github.com/kubernetes/kubernetes/blob/c6b0652b89ad64e8d86d47fc2e4089e281732ede/pkg/scheduler/apis/config/v1beta3/zz_generated.defaults.go#L32
  
  I don't know if we can do exactly the same here, but just applying the defaults after loading the file should be similar enough.
  
  OTOH, if this is the suggested pattern by controller-runtime, that should be fine too. But it should be unit tested.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-12T14:55:56Z

- **main.go:93** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-12T14:55:56Z
```diff
@@ -95,7 +88,7 @@ func main() {
 		LeaderElectionID:       "c1f6bfd2.kueue.x-k8s.io",
 	}
 	var err error
-	config := defaultConfiguration()
+	config := configv1alpha1.DefaultConfiguration()
 	if configFile != "" {
 		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
```

  Can you add unit tests?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-13T05:03:09Z

- **docs/setup/install.md:11** by [@kerthcet](https://github.com/kerthcet) — 2022-07-13T05:03:09Z
```diff
@@ -8,6 +8,12 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+The webhook server in kueue uses a internal cert management for provisioning certificates. If you want to use
```

  Thanks.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-13T12:34:47Z

- **main.go:210** by [@kerthcet](https://github.com/kerthcet) — 2022-07-13T12:34:42Z
```diff
@@ -219,3 +203,44 @@ func encodeConfig(cfg *configv1alpha1.Configuration) (string, error) {
 	}
 	return buf.String(), nil
 }
+
+func apply(configFile string) (ctrl.Options, configv1alpha1.Configuration) {
+	var err error
+	options := ctrl.Options{
+		Scheme: scheme,
```

  If we set values here, it will not be overwrite by ComponentConfig. So I moved to setOptionDefaults.
  https://github.com/kubernetes-sigs/controller-runtime/blob/15154aaa67679df320008ed45534f83ff3d6922d/pkg/manager/manager.go#L456-L482

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-13T12:35:08Z

- **main.go:93** by [@kerthcet](https://github.com/kerthcet) — 2022-07-13T12:35:08Z
```diff
@@ -95,7 +88,7 @@ func main() {
 		LeaderElectionID:       "c1f6bfd2.kueue.x-k8s.io",
 	}
 	var err error
-	config := defaultConfiguration()
+	config := configv1alpha1.DefaultConfiguration()
 	if configFile != "" {
 		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-13T13:59:48Z

- **apis/config/v1alpha1/configuration.go:22** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-13T13:54:41Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha1
+
+import "sigs.k8s.io/kueue/pkg/util/pointer"
+
+// SetConfigurationDefaults sets default values for ComponentConfig.
+func SetConfigurationDefaults(cfg *Configuration) {
```

  ```suggestion
  func SetDefaults_Configuration(cfg *Configuration) {
  ```
  
  to match https://github.com/kubernetes/kubernetes/blob/09e8339ae4e575ec40e8ed0fc2829fcb678c0f19/pkg/scheduler/apis/config/v1beta3/defaults.go#L103
  
  Unless the linter complains? Then feel free to remove the underscore.
  
  Also, let's name the file `defaults.go`

- **docs/setup/install.md:13** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-13T13:55:16Z
```diff
@@ -8,6 +8,12 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+The webhook server in kueue uses an internal cert management for provisioning certificates. If you want to use
+  a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
+  1. Set `enableInternalCertManagement` to true in [config file](#install-a-custom-configured-released-version).
```

  ```suggestion
    1. Set `enableInternalCertManagement` to `false` in [config file](#install-a-custom-configured-released-version).
  ```

- **main.go:232** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-13T13:58:00Z
```diff
@@ -219,3 +203,44 @@ func encodeConfig(cfg *configv1alpha1.Configuration) (string, error) {
 	}
 	return buf.String(), nil
 }
+
+func apply(configFile string) (ctrl.Options, configv1alpha1.Configuration) {
+	var err error
+	options := ctrl.Options{
+		Scheme: scheme,
+	}
+	config := configv1alpha1.Configuration{}
+	if configFile != "" {
+		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
+		if err != nil {
+			setupLog.Error(err, "unable to load the config file")
+			os.Exit(1)
+		}
+
+		cfgStr, err := encodeConfig(&config)
+		if err != nil {
+			setupLog.Error(err, "unable to encode config file")
+			os.Exit(1)
+		}
+		setupLog.Info("Successfully loaded config file", "config", cfgStr)
+	}
+
+	setOptionDefaults(&options)
```

  are you sure this can't be done before calling `AndFrom`?
  Or are you just trying to be consistent with the function below?

- **main.go:210** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-13T13:59:46Z
```diff
@@ -219,3 +203,44 @@ func encodeConfig(cfg *configv1alpha1.Configuration) (string, error) {
 	}
 	return buf.String(), nil
 }
+
+func apply(configFile string) (ctrl.Options, configv1alpha1.Configuration) {
+	var err error
+	options := ctrl.Options{
+		Scheme: scheme,
```

  Also, what happens if you register the defaulting function in the scheme?
  https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/apis/config/v1beta3/zz_generated.defaults.go#L35-L37
  
  Would that allow you to skip calling the defaulting function explicitly?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-14T08:31:53Z

- **main.go:232** by [@kerthcet](https://github.com/kerthcet) — 2022-07-14T08:31:53Z
```diff
@@ -219,3 +203,44 @@ func encodeConfig(cfg *configv1alpha1.Configuration) (string, error) {
 	}
 	return buf.String(), nil
 }
+
+func apply(configFile string) (ctrl.Options, configv1alpha1.Configuration) {
+	var err error
+	options := ctrl.Options{
+		Scheme: scheme,
+	}
+	config := configv1alpha1.Configuration{}
+	if configFile != "" {
+		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
+		if err != nil {
+			setupLog.Error(err, "unable to load the config file")
+			os.Exit(1)
+		}
+
+		cfgStr, err := encodeConfig(&config)
+		if err != nil {
+			setupLog.Error(err, "unable to encode config file")
+			os.Exit(1)
+		}
+		setupLog.Info("Successfully loaded config file", "config", cfgStr)
+	}
+
+	setOptionDefaults(&options)
```

  If we set default values in options before calling `AndFrom`, we can not overwrite them in `Configuration`, e.g. currently our default port is 9443, even if we set this to 9444 in `Configuration`, we'll still get port 9443. This is because in `AndFrom`, we'll check where port is zero value, if not, we'll not update it. see https://github.com/kubernetes-sigs/controller-runtime/blob/15154aaa67679df320008ed45534f83ff3d6922d/pkg/manager/manager.go#L480-L482

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-14T15:48:01Z

- **main.go:210** by [@kerthcet](https://github.com/kerthcet) — 2022-07-14T15:48:01Z
```diff
@@ -219,3 +203,44 @@ func encodeConfig(cfg *configv1alpha1.Configuration) (string, error) {
 	}
 	return buf.String(), nil
 }
+
+func apply(configFile string) (ctrl.Options, configv1alpha1.Configuration) {
+	var err error
+	options := ctrl.Options{
+		Scheme: scheme,
```

  Yes, only when the `configFile != ""` , if not, we still have to call `scheme.Defaults()`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-14T18:30:01Z

/approve
you can squash

- **apis/config/v1alpha1/configuration.go:22** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-14T13:26:09Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha1
+
+import "sigs.k8s.io/kueue/pkg/util/pointer"
+
+// SetConfigurationDefaults sets default values for ComponentConfig.
+func SetConfigurationDefaults(cfg *Configuration) {
```

  Can you rename the file to `defaults.go`?

- **apis/config/v1alpha1/groupversion_info.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-14T18:17:01Z
```diff
@@ -20,17 +20,32 @@ limitations under the License.
 package v1alpha1
 
 import (
+	runtime "k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/runtime/schema"
-	"sigs.k8s.io/controller-runtime/pkg/scheme"
 )
 
 var (
 	// GroupVersion is group version used to register these objects
 	GroupVersion = schema.GroupVersion{Group: "config.kueue.x-k8s.io", Version: "v1alpha1"}
 
-	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
-	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
+	// localSchemeBuilder is used to add go types to the GroupVersionKind scheme
+	localSchemeBuilder = runtime.NewSchemeBuilder(addKnownTypes)
```

  ```suggestion
  	schemeBuilder = runtime.NewSchemeBuilder(addKnownTypes)
  ```

- **apis/config/v1alpha1/groupversion_info.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-14T18:22:14Z
```diff
@@ -20,17 +20,32 @@ limitations under the License.
 package v1alpha1
 
 import (
+	runtime "k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/runtime/schema"
-	"sigs.k8s.io/controller-runtime/pkg/scheme"
 )
 
 var (
 	// GroupVersion is group version used to register these objects
 	GroupVersion = schema.GroupVersion{Group: "config.kueue.x-k8s.io", Version: "v1alpha1"}
 
-	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
-	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
+	// localSchemeBuilder is used to add go types to the GroupVersionKind scheme
+	localSchemeBuilder = runtime.NewSchemeBuilder(addKnownTypes)
 
 	// AddToScheme adds the types in this group-version to the given scheme.
-	AddToScheme = SchemeBuilder.AddToScheme
+	AddToScheme = localSchemeBuilder.AddToScheme
 )
+
+func init() {
+	// We only register manually written functions here. The registration of the
+	// generated functions takes place in the generated files. The separation
+	// makes the code compile even when the generated files are missing.
+	localSchemeBuilder.Register(addDefaultingFuncs)
```

  oh the controller-runtime builder doesn't support defaults? :smiling_face_with_tear:

- **main_test.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-14T18:29:38Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2021 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"os"
+	"path/filepath"
+	"testing"
+)
+
+func TestApply(t *testing.T) {
+	// temp dir
+	tmpDir, err := os.MkdirTemp("", "temp")
+	if err != nil {
+		t.Fatal(err)
+	}
+	defer os.RemoveAll(tmpDir)
+
+	portOverWriteConfig := filepath.Join(tmpDir, "port-overwrite.yaml")
+	if err := os.WriteFile(portOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha1
+kind: Configuration
+leaderElection:
+  leaderElect: true
+webhook:
+  port: 9444
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
+	if err := os.WriteFile(certOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha1
+kind: Configuration
+leaderElection:
+  leaderElect: true
+enableInternalCertManagement: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	testcases := []struct {
+		name                             string
+		configFile                       string
+		wantPort                         int
+		wantEnableInternalCertManagement bool
+	}{
+		{
+			name:                             "default config",
+			configFile:                       "",
+			wantPort:                         9443,
```

  good for now... but we probably want this to be `wantOptions` and `wantConfiguration` and use cmp.Diff.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-15T03:05:41Z

- **main_test.go:65** by [@kerthcet](https://github.com/kerthcet) — 2022-07-15T00:38:07Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2021 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package main
+
+import (
+	"os"
+	"path/filepath"
+	"testing"
+)
+
+func TestApply(t *testing.T) {
+	// temp dir
+	tmpDir, err := os.MkdirTemp("", "temp")
+	if err != nil {
+		t.Fatal(err)
+	}
+	defer os.RemoveAll(tmpDir)
+
+	portOverWriteConfig := filepath.Join(tmpDir, "port-overwrite.yaml")
+	if err := os.WriteFile(portOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha1
+kind: Configuration
+leaderElection:
+  leaderElect: true
+webhook:
+  port: 9444
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
+	if err := os.WriteFile(certOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha1
+kind: Configuration
+leaderElection:
+  leaderElect: true
+enableInternalCertManagement: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	testcases := []struct {
+		name                             string
+		configFile                       string
+		wantPort                         int
+		wantEnableInternalCertManagement bool
+	}{
+		{
+			name:                             "default config",
+			configFile:                       "",
+			wantPort:                         9443,
```

  Yes, but I didn't think out any elegant ways, I'll follow this later.

- **apis/config/v1alpha1/groupversion_info.go:42** by [@kerthcet](https://github.com/kerthcet) — 2022-07-15T02:59:23Z
```diff
@@ -20,17 +20,32 @@ limitations under the License.
 package v1alpha1
 
 import (
+	runtime "k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/runtime/schema"
-	"sigs.k8s.io/controller-runtime/pkg/scheme"
 )
 
 var (
 	// GroupVersion is group version used to register these objects
 	GroupVersion = schema.GroupVersion{Group: "config.kueue.x-k8s.io", Version: "v1alpha1"}
 
-	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
-	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
+	// localSchemeBuilder is used to add go types to the GroupVersionKind scheme
+	localSchemeBuilder = runtime.NewSchemeBuilder(addKnownTypes)
 
 	// AddToScheme adds the types in this group-version to the given scheme.
-	AddToScheme = SchemeBuilder.AddToScheme
+	AddToScheme = localSchemeBuilder.AddToScheme
 )
+
+func init() {
+	// We only register manually written functions here. The registration of the
+	// generated functions takes place in the generated files. The separation
+	// makes the code compile even when the generated files are missing.
+	localSchemeBuilder.Register(addDefaultingFuncs)
```

  We can register like `schemeBuilder.SchemeBuilder.Register(addDefaultingFuncs)`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-15T14:31:54Z

- **apis/config/v1alpha1/groupversion_info.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-15T14:31:54Z
```diff
@@ -20,17 +20,32 @@ limitations under the License.
 package v1alpha1
 
 import (
+	runtime "k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/runtime/schema"
-	"sigs.k8s.io/controller-runtime/pkg/scheme"
 )
 
 var (
 	// GroupVersion is group version used to register these objects
 	GroupVersion = schema.GroupVersion{Group: "config.kueue.x-k8s.io", Version: "v1alpha1"}
 
-	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
-	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
+	// localSchemeBuilder is used to add go types to the GroupVersionKind scheme
+	localSchemeBuilder = runtime.NewSchemeBuilder(addKnownTypes)
 
 	// AddToScheme adds the types in this group-version to the given scheme.
-	AddToScheme = SchemeBuilder.AddToScheme
+	AddToScheme = localSchemeBuilder.AddToScheme
 )
+
+func init() {
+	// We only register manually written functions here. The registration of the
+	// generated functions takes place in the generated files. The separation
+	// makes the code compile even when the generated files are missing.
+	localSchemeBuilder.Register(addDefaultingFuncs)
```

  Since we are using controller-runtime scheme builder in the other package, I prefer we keep them consistent.
  
  /hold
  in case you have time to do this.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-15T14:32:57Z

/lgtm

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-15T14:55:11Z

- **apis/config/v1alpha1/groupversion_info.go:42** by [@kerthcet](https://github.com/kerthcet) — 2022-07-15T14:55:10Z
```diff
@@ -20,17 +20,32 @@ limitations under the License.
 package v1alpha1
 
 import (
+	runtime "k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/runtime/schema"
-	"sigs.k8s.io/controller-runtime/pkg/scheme"
 )
 
 var (
 	// GroupVersion is group version used to register these objects
 	GroupVersion = schema.GroupVersion{Group: "config.kueue.x-k8s.io", Version: "v1alpha1"}
 
-	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
-	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
+	// localSchemeBuilder is used to add go types to the GroupVersionKind scheme
+	localSchemeBuilder = runtime.NewSchemeBuilder(addKnownTypes)
 
 	// AddToScheme adds the types in this group-version to the given scheme.
-	AddToScheme = SchemeBuilder.AddToScheme
+	AddToScheme = localSchemeBuilder.AddToScheme
 )
+
+func init() {
+	// We only register manually written functions here. The registration of the
+	// generated functions takes place in the generated files. The separation
+	// makes the code compile even when the generated files are missing.
+	localSchemeBuilder.Register(addDefaultingFuncs)
```

  Done, PTAL

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-15T15:08:08Z

Awesome!

/lgtm
