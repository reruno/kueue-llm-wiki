# PR #2795: attempt to commonize Kubeflow jobs Multikueue support methods

**Summary**: attempt to commonize Kubeflow jobs Multikueue support methods

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2795

**Last updated**: 2024-08-19T17:06:45Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/support-kubeflow-multikueue-common-interface` → `main`
- **Created**: 2024-08-07T15:27:10Z
- **Updated**: 2024-08-19T17:06:45Z
- **Merged**: 2024-08-19T16:21:29Z
- **Closed**: 2024-08-19T16:21:29Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Changed files**: 13   **+214 / -356**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->
/kind cleanup
#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Attempt to unify code so it's not repeated for each kubeflow job multikueue adapter
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates #2552 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-07T15:27:12Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-07T15:27:26Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5ed0f3bd0dc5cf750745c05b6bcb5438814ae40d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66c36a3de9eb690008605005 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-07T15:31:34Z

/ok-to-test

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-16T11:41:57Z

@alculquicondor you may want to have a look here too ;)

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-16T18:03:56Z

/release-note-edit

```release-note
NONE
```

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-19T13:32:45Z

> Overall, looks good to me. This is a comment for a followup.
> 
> Could you work on consolidating / commonizing the kubeflow MK integration tests (TFJob/PaddleJob/XGBoostJob/PyTorchJob)
> 
> https://github.com/kubernetes-sigs/kueue/blob/5b9ff8e74f19296174cf20d69b485f20f8d7c7b0/test/integration/multikueue/multikueue_test.go#L781
> 
> ?
> I'm wondering if we can reduce the kubeflow MK integration tests like kubeflowjob reconcilers.

I actually work on this one while doing MPIJob multikueue adapter, but I can add it here.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-19T13:40:13Z

> > Overall, looks good to me. This is a comment for a followup.
> > Could you work on consolidating / commonizing the kubeflow MK integration tests (TFJob/PaddleJob/XGBoostJob/PyTorchJob)
> > https://github.com/kubernetes-sigs/kueue/blob/5b9ff8e74f19296174cf20d69b485f20f8d7c7b0/test/integration/multikueue/multikueue_test.go#L781
> > 
> > ?
> > I'm wondering if we can reduce the kubeflow MK integration tests like kubeflowjob reconcilers.
> 
> I actually work on this one while doing MPIJob multikueue adapter, but I can add it here.

It seems that this PR will be finalized soon. So, it may be helpful to work on test refactoring at another PR so that we do not block merging this PR.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-19T16:17:14Z

LGTM label has been added.  <details>Git tree hash: 282c50d2fe8cd61ed799f6f3196028fd327700e9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-19T16:17:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2795#pullrequestreview-2245966127" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2795#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-08T13:57:24Z

- **pkg/controller/jobframework/interface.go:176** by [@trasc](https://github.com/trasc) — 2024-08-08T13:45:08Z
```diff
@@ -172,6 +172,11 @@ type MultiKueueAdapter interface {
 	GVK() schema.GroupVersionKind
 }
 
+type UpdateRemoteJob interface {
+	UpdateRemoteJobStatus(localJob, remoteJob interface{}) 
```

  we can make them client.Object

- **pkg/controller/jobs/kubeflow/common/common.go:1** by [@trasc](https://github.com/trasc) — 2024-08-08T13:48:30Z
  why not put this in the kubeflow package?

- **pkg/controller/jobframework/interface.go:175** by [@trasc](https://github.com/trasc) — 2024-08-08T13:50:13Z
```diff
@@ -172,6 +172,11 @@ type MultiKueueAdapter interface {
 	GVK() schema.GroupVersionKind
 }
 
+type UpdateRemoteJob interface {
```

  it's place should be somewhere in pkg/controllers/jobs/kubeflow

- **pkg/controller/jobs/kubeflow/common/common.go:25** by [@trasc](https://github.com/trasc) — 2024-08-08T13:55:19Z
```diff
@@ -0,0 +1,89 @@
+package common
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+func SyncJob[PtrT objAsPtr[T], T any](
```

  why not put this in a generic struct that implements multikueue adapter and has two members holding the specialized "Update..." functions 
  
  then the job will just need to define the two functions and call a `MakeAdaptor(f1, f2)`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-09T20:27:49Z

That doesn't seem to be my expectations.
Could we commonize kubeflow adapters like kubeflow job reconcilers?

https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/kubeflow/kubeflowjob

- **pkg/controller/jobframework/interface.go:177** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-09T20:27:05Z
```diff
@@ -172,6 +172,11 @@ type MultiKueueAdapter interface {
 	GVK() schema.GroupVersionKind
 }
 
+type UpdateRemoteJob interface {
+	UpdateRemoteJobStatus(localJob, remoteJob interface{}) 
+	UpdateRemoteJobSpec(localJob, remoteJob interface{})
```

  We should not expose these interfaces just for kubeflow jobs.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-13T16:14:48Z

- **test/integration/multikueue/multikueue_test.go:900** by [@trasc](https://github.com/trasc) — 2024-08-13T16:12:54Z
```diff
@@ -897,7 +897,7 @@ var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 							Succeeded: 1,
 						},
 					}))
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  do we still need this? or it's just some debug attempt leftover?

- **pkg/controller/jobs/kubeflow/common/common.go:1** by [@trasc](https://github.com/trasc) — 2024-08-13T16:14:45Z
  I meant `pkg/controller/jobs/kubeflow/kubeflowjob`

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-14T11:30:01Z

- **pkg/controller/jobs/kubeflow/common/common.go:1** by [@mszadkow](https://github.com/mszadkow) — 2024-08-14T11:30:01Z
  Yeah, that makes sense

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-16T18:03:37Z

looks good overall :)

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-16T17:38:40Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+type adapter[PtrT objAsPtr[T], T any] struct {
+	copySpec   func(dst, src PtrT)
+	copyStatus func(dst, src PtrT)
+	emptyList  func() client.ObjectList
+	gwk        schema.GroupVersionKind
+	typeName   string
+}
+
+type fullInterface interface {
+	jobframework.MultiKueueAdapter
+	jobframework.MultiKueueWatcher
+}
+
+func NewAdapter[PtrT objAsPtr[T], T any](
```

  ```suggestion
  func NewMKAdapter[PtrT objAsPtr[T], T any](
  ```

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go:47** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-16T17:48:14Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+type adapter[PtrT objAsPtr[T], T any] struct {
+	copySpec   func(dst, src PtrT)
+	copyStatus func(dst, src PtrT)
+	emptyList  func() client.ObjectList
+	gwk        schema.GroupVersionKind
```

  ```suggestion
  	gvk        schema.GroupVersionKind
  ```

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go:141** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-16T17:48:26Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+type adapter[PtrT objAsPtr[T], T any] struct {
+	copySpec   func(dst, src PtrT)
+	copyStatus func(dst, src PtrT)
+	emptyList  func() client.ObjectList
+	gwk        schema.GroupVersionKind
+	typeName   string
+}
+
+type fullInterface interface {
+	jobframework.MultiKueueAdapter
+	jobframework.MultiKueueWatcher
+}
+
+func NewAdapter[PtrT objAsPtr[T], T any](
+	copySpec func(dst, src PtrT),
+	copyStatus func(dst, src PtrT),
+	emptyList func() client.ObjectList,
+	gwk schema.GroupVersionKind,
+	typeName string,
+) fullInterface {
+	return &adapter[PtrT, T]{
+		copySpec:   copySpec,
+		copyStatus: copyStatus,
+		emptyList:  emptyList,
+		gwk:        gwk,
+		typeName:   typeName,
+	}
+}
+
+func (a adapter[PtrT, T]) GVK() schema.GroupVersionKind {
+	return a.gwk
+}
+
+func (a adapter[PtrT, T]) KeepAdmissionCheckPending() bool {
+	return false
+}
+
+func (a adapter[PtrT, T]) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
+	return true, "", nil
+}
+
+func (a adapter[PtrT, T]) SyncJob(
+	ctx context.Context,
+	localClient client.Client,
+	remoteClient client.Client,
+	key types.NamespacedName,
+	workloadName, origin string) error {
+	localJob := PtrT(new(T))
+	err := localClient.Get(ctx, key, localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := PtrT(new(T))
+	err = remoteClient.Get(ctx, key, remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, localJob, func() (bool, error) {
+			// if the remote exists, just copy the status
+			a.copyStatus(localJob, remoteJob)
+			return true, nil
+		})
+	}
+
+	remoteJob = PtrT(new(T))
+	a.copySpec(remoteJob, localJob)
+
+	// add the prebuilt workload
+	labels := remoteJob.GetLabels()
+	if remoteJob.GetLabels() == nil {
+		labels = make(map[string]string, 2)
+	}
+	labels[constants.PrebuiltWorkloadLabel] = workloadName
+	labels[kueuealpha.MultiKueueOriginLabel] = origin
+	remoteJob.SetLabels(labels)
+
+	return remoteClient.Create(ctx, remoteJob)
+}
+
+func (a adapter[PtrT, T]) DeleteRemoteObject(ctx context.Context, remoteClient client.Client, key types.NamespacedName) error {
+	job := PtrT(new(T))
+	err := remoteClient.Get(ctx, key, job)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+	return client.IgnoreNotFound(remoteClient.Delete(ctx, job))
+}
+
+func (a adapter[PtrT, T]) GetEmptyList() client.ObjectList {
+	return a.emptyList()
+}
+
+func (a adapter[PtrT, T]) WorkloadKeyFor(o runtime.Object) (types.NamespacedName, error) {
+	job, isTheJob := o.(PtrT)
+	if !isTheJob {
+		return types.NamespacedName{}, fmt.Errorf("not a %s", a.typeName)
```

  ```suggestion
  		return types.NamespacedName{}, fmt.Errorf("not a %s", a.gvk.Kind)
  ```
  
  so that we can get rid of `a.typeName`

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go:130** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-16T17:55:34Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+type adapter[PtrT objAsPtr[T], T any] struct {
+	copySpec   func(dst, src PtrT)
+	copyStatus func(dst, src PtrT)
+	emptyList  func() client.ObjectList
+	gwk        schema.GroupVersionKind
+	typeName   string
+}
+
+type fullInterface interface {
+	jobframework.MultiKueueAdapter
+	jobframework.MultiKueueWatcher
+}
+
+func NewAdapter[PtrT objAsPtr[T], T any](
+	copySpec func(dst, src PtrT),
+	copyStatus func(dst, src PtrT),
+	emptyList func() client.ObjectList,
+	gwk schema.GroupVersionKind,
+	typeName string,
+) fullInterface {
+	return &adapter[PtrT, T]{
+		copySpec:   copySpec,
+		copyStatus: copyStatus,
+		emptyList:  emptyList,
+		gwk:        gwk,
+		typeName:   typeName,
+	}
+}
+
+func (a adapter[PtrT, T]) GVK() schema.GroupVersionKind {
+	return a.gwk
+}
+
+func (a adapter[PtrT, T]) KeepAdmissionCheckPending() bool {
+	return false
+}
+
+func (a adapter[PtrT, T]) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
+	return true, "", nil
+}
+
+func (a adapter[PtrT, T]) SyncJob(
+	ctx context.Context,
+	localClient client.Client,
+	remoteClient client.Client,
+	key types.NamespacedName,
+	workloadName, origin string) error {
+	localJob := PtrT(new(T))
+	err := localClient.Get(ctx, key, localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := PtrT(new(T))
+	err = remoteClient.Get(ctx, key, remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, localJob, func() (bool, error) {
+			// if the remote exists, just copy the status
+			a.copyStatus(localJob, remoteJob)
+			return true, nil
+		})
+	}
+
+	remoteJob = PtrT(new(T))
+	a.copySpec(remoteJob, localJob)
+
+	// add the prebuilt workload
+	labels := remoteJob.GetLabels()
+	if remoteJob.GetLabels() == nil {
+		labels = make(map[string]string, 2)
+	}
+	labels[constants.PrebuiltWorkloadLabel] = workloadName
+	labels[kueuealpha.MultiKueueOriginLabel] = origin
+	remoteJob.SetLabels(labels)
+
+	return remoteClient.Create(ctx, remoteJob)
+}
+
+func (a adapter[PtrT, T]) DeleteRemoteObject(ctx context.Context, remoteClient client.Client, key types.NamespacedName) error {
+	job := PtrT(new(T))
+	err := remoteClient.Get(ctx, key, job)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
```

  Doing a `Get` for a delete sounds wasteful, we can do this:
  
  ```suggestion
  	job.SetName(key.Name)
  	job.SetNamespace(key.Namespace)
  ```
  
  And we should do something similar for other non-kubeflow types, but that's for a separate PR.

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go:118** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-16T18:02:51Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+type adapter[PtrT objAsPtr[T], T any] struct {
+	copySpec   func(dst, src PtrT)
+	copyStatus func(dst, src PtrT)
+	emptyList  func() client.ObjectList
+	gwk        schema.GroupVersionKind
+	typeName   string
+}
+
+type fullInterface interface {
+	jobframework.MultiKueueAdapter
+	jobframework.MultiKueueWatcher
+}
+
+func NewAdapter[PtrT objAsPtr[T], T any](
+	copySpec func(dst, src PtrT),
+	copyStatus func(dst, src PtrT),
+	emptyList func() client.ObjectList,
+	gwk schema.GroupVersionKind,
+	typeName string,
+) fullInterface {
+	return &adapter[PtrT, T]{
+		copySpec:   copySpec,
+		copyStatus: copyStatus,
+		emptyList:  emptyList,
+		gwk:        gwk,
+		typeName:   typeName,
+	}
+}
+
+func (a adapter[PtrT, T]) GVK() schema.GroupVersionKind {
+	return a.gwk
+}
+
+func (a adapter[PtrT, T]) KeepAdmissionCheckPending() bool {
+	return false
+}
+
+func (a adapter[PtrT, T]) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
+	return true, "", nil
+}
+
+func (a adapter[PtrT, T]) SyncJob(
+	ctx context.Context,
+	localClient client.Client,
+	remoteClient client.Client,
+	key types.NamespacedName,
+	workloadName, origin string) error {
+	localJob := PtrT(new(T))
+	err := localClient.Get(ctx, key, localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := PtrT(new(T))
+	err = remoteClient.Get(ctx, key, remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, localJob, func() (bool, error) {
+			// if the remote exists, just copy the status
+			a.copyStatus(localJob, remoteJob)
+			return true, nil
+		})
+	}
+
+	remoteJob = PtrT(new(T))
+	a.copySpec(remoteJob, localJob)
+
+	// add the prebuilt workload
+	labels := remoteJob.GetLabels()
+	if remoteJob.GetLabels() == nil {
+		labels = make(map[string]string, 2)
+	}
+	labels[constants.PrebuiltWorkloadLabel] = workloadName
+	labels[kueuealpha.MultiKueueOriginLabel] = origin
+	remoteJob.SetLabels(labels)
+
```

  I think this code should even be part of the MK workload controller, but that can be left for a follow up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-16T18:34:55Z

Overall, looks good to me.
This is a comment for a followup.

Could you work on consolidating / commonizing the kubeflow MK integration tests (TFJob/PaddleJob/XGBoostJob/PyTorchJob) https://github.com/kubernetes-sigs/kueue/blob/5b9ff8e74f19296174cf20d69b485f20f8d7c7b0/test/integration/multikueue/multikueue_test.go#L781?

I'm wondering if we can reduce the kubeflow MK integration tests like kubeflowjob reconcilers.

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go:80** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-16T18:27:52Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+type adapter[PtrT objAsPtr[T], T any] struct {
+	copySpec   func(dst, src PtrT)
+	copyStatus func(dst, src PtrT)
+	emptyList  func() client.ObjectList
+	gwk        schema.GroupVersionKind
+	typeName   string
+}
+
+type fullInterface interface {
+	jobframework.MultiKueueAdapter
+	jobframework.MultiKueueWatcher
+}
+
+func NewAdapter[PtrT objAsPtr[T], T any](
+	copySpec func(dst, src PtrT),
+	copyStatus func(dst, src PtrT),
+	emptyList func() client.ObjectList,
+	gwk schema.GroupVersionKind,
+	typeName string,
+) fullInterface {
+	return &adapter[PtrT, T]{
+		copySpec:   copySpec,
+		copyStatus: copyStatus,
+		emptyList:  emptyList,
+		gwk:        gwk,
+		typeName:   typeName,
+	}
+}
+
+func (a adapter[PtrT, T]) GVK() schema.GroupVersionKind {
+	return a.gwk
+}
+
+func (a adapter[PtrT, T]) KeepAdmissionCheckPending() bool {
+	return false
+}
+
+func (a adapter[PtrT, T]) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
```

  ```suggestion
  func (a adapter[PtrT, T]) IsJobManagedByKueue(context.Context, client.Client, types.NamespacedName) (bool, string, error) {
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-19T15:39:44Z

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go:118** by [@mszadkow](https://github.com/mszadkow) — 2024-08-19T15:39:44Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type objAsPtr[T any] interface {
+	metav1.Object
+	client.Object
+	*T
+}
+
+type adapter[PtrT objAsPtr[T], T any] struct {
+	copySpec   func(dst, src PtrT)
+	copyStatus func(dst, src PtrT)
+	emptyList  func() client.ObjectList
+	gwk        schema.GroupVersionKind
+	typeName   string
+}
+
+type fullInterface interface {
+	jobframework.MultiKueueAdapter
+	jobframework.MultiKueueWatcher
+}
+
+func NewAdapter[PtrT objAsPtr[T], T any](
+	copySpec func(dst, src PtrT),
+	copyStatus func(dst, src PtrT),
+	emptyList func() client.ObjectList,
+	gwk schema.GroupVersionKind,
+	typeName string,
+) fullInterface {
+	return &adapter[PtrT, T]{
+		copySpec:   copySpec,
+		copyStatus: copyStatus,
+		emptyList:  emptyList,
+		gwk:        gwk,
+		typeName:   typeName,
+	}
+}
+
+func (a adapter[PtrT, T]) GVK() schema.GroupVersionKind {
+	return a.gwk
+}
+
+func (a adapter[PtrT, T]) KeepAdmissionCheckPending() bool {
+	return false
+}
+
+func (a adapter[PtrT, T]) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
+	return true, "", nil
+}
+
+func (a adapter[PtrT, T]) SyncJob(
+	ctx context.Context,
+	localClient client.Client,
+	remoteClient client.Client,
+	key types.NamespacedName,
+	workloadName, origin string) error {
+	localJob := PtrT(new(T))
+	err := localClient.Get(ctx, key, localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := PtrT(new(T))
+	err = remoteClient.Get(ctx, key, remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, localJob, func() (bool, error) {
+			// if the remote exists, just copy the status
+			a.copyStatus(localJob, remoteJob)
+			return true, nil
+		})
+	}
+
+	remoteJob = PtrT(new(T))
+	a.copySpec(remoteJob, localJob)
+
+	// add the prebuilt workload
+	labels := remoteJob.GetLabels()
+	if remoteJob.GetLabels() == nil {
+		labels = make(map[string]string, 2)
+	}
+	labels[constants.PrebuiltWorkloadLabel] = workloadName
+	labels[kueuealpha.MultiKueueOriginLabel] = origin
+	remoteJob.SetLabels(labels)
+
```

  ok, noted

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-19T16:17:09Z

/lgtm
/approve
