# PR #2824: Define hierarchy.Manager

**Summary**: Define hierarchy.Manager

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2824

**Last updated**: 2024-08-26T19:50:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `hierarchy_manager` → `main`
- **Created**: 2024-08-12T13:25:43Z
- **Updated**: 2024-08-26T19:50:08Z
- **Merged**: 2024-08-13T16:28:11Z
- **Closed**: 2024-08-13T16:28:11Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 16   **+625 / -270**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
We define `hierarchy.Manager`, which manages `ClusterQueues`, `Cohorts`, and their edges. Currently, we duplicate logic which creates and updates edges, and creates/deletes implicit Cohorts (those without backing API objects). This change moves all of the relevant logic into a new package, `hierarchy`.

This change is important for the implementation of #79. With Hierarchical Cohorts, we have to account for both implicit and explicit Cohorts, in addition to new edges (Cohorts as parents and children of other Cohorts).

Additionally, the `hierarchy` package does not export the edges' fields, preventing unintentional manipulation outside of the `hierarchy.Manager` interface.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-12T13:26:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b93042fb2d9eb9ea4811ccc57e01964ff269ea7c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66bb8503e7c72d000832b591 |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-13T07:50:01Z

/assign @PBundyra

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-13T16:18:24Z

/lgtm
/approve
I like the encapsulation of the logic to manage the hierarchy edges, especially as we are going to support deeper structures with HC.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-13T16:18:31Z

LGTM label has been added.  <details>Git tree hash: ed25ac6fe87d6b49143e367b91e417ec67eea4e5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-13T16:18:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2824#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2824#issuecomment-2286637072" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-12T15:57:01Z

- **pkg/hierarchy/manager.go:5** by [@mimowo](https://github.com/mimowo) — 2024-08-12T15:57:01Z
```diff
@@ -0,0 +1,80 @@
+package hierarchy
+
+// Manager stores Cohorts and ClusterQueues, and maintains the edges
+// between them.
+type Manager[CQ cqNode[CQ, C], C cohortNode[CQ, C]] struct {
```

  Are the generic types to simplify the implementation, or they are in anticipation of future needs? 
  If in anticipation of future needs I would suggest to defer them for later so that the PR just two fold responsibility : encapsulating the edge management, and deduplicate the code for edge management.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-12T15:59:18Z

- **pkg/hierarchy/manager.go:5** by [@gabesaba](https://github.com/gabesaba) — 2024-08-12T15:59:18Z
```diff
@@ -0,0 +1,80 @@
+package hierarchy
+
+// Manager stores Cohorts and ClusterQueues, and maintains the edges
+// between them.
+type Manager[CQ cqNode[CQ, C], C cohortNode[CQ, C]] struct {
```

  They are needed because use this in both `cache` and `queue` packages.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-12T16:14:26Z

- **pkg/hierarchy/manager.go:5** by [@mimowo](https://github.com/mimowo) — 2024-08-12T16:14:25Z
```diff
@@ -0,0 +1,80 @@
+package hierarchy
+
+// Manager stores Cohorts and ClusterQueues, and maintains the edges
+// between them.
+type Manager[CQ cqNode[CQ, C], C cohortNode[CQ, C]] struct {
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-13T15:30:05Z

The implementation LGTM, just minor suggestions to the way we are testing the structure

- **pkg/hierarchy/manager_test.go:169** by [@mimowo](https://github.com/mimowo) — 2024-08-13T15:25:43Z
```diff
@@ -0,0 +1,232 @@
+package hierarchy
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+func TestManager(t *testing.T) {
+	type M = Manager[*testClusterQueue, *testCohort]
+	type cqEdge struct {
+		cq     string
+		cohort string
+	}
+	tests := map[string]struct {
+		operations  func(M)
+		wantCqs     sets.Set[string]
+		wantCohorts sets.Set[string]
+		wantCqEdge  []cqEdge
+	}{
+		"create queues": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+			},
+			wantCqs: sets.New("queue1", "queue2"),
+		},
+		"delete queue": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.DeleteClusterQueue("queue2")
+			},
+			wantCqs: sets.New("queue1"),
+		},
+		"create queues with cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.AddClusterQueue(newCq("queue3"))
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-a")
+				m.UpdateClusterQueueEdge("queue3", "cohort-b")
+			},
+			wantCqs: sets.New(
+				"queue1",
+				"queue2",
+				"queue3",
+			),
+			wantCohorts: sets.New(
+				"cohort-a",
+				"cohort-b",
+			),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-a"},
+				{"queue2", "cohort-a"},
+				{"queue3", "cohort-b"},
+			},
+		},
+		"update cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+			},
+			wantCqs:     sets.New("queue1", "queue2"),
+			wantCohorts: sets.New("cohort-b", "cohort-c"),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-b"},
+				{"queue2", "cohort-c"},
+			},
+		},
+		"updating idempotent": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+			},
+			wantCqs:     sets.New("queue1", "queue2"),
+			wantCohorts: sets.New("cohort-b", "cohort-c"),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-b"},
+				{"queue2", "cohort-c"},
+			},
+		},
+		"delete cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				// Delete cohort-a by deleting edge.
+				// Delete cohort-b by deleting cq.
+				m.UpdateClusterQueueEdge("queue1", "")
+				m.DeleteClusterQueue("queue2")
+			},
+			wantCqs: sets.New("queue1"),
+		},
+		"deletion idempotent": {
+			operations: func(m M) {
+				m.DeleteClusterQueue("doesnt-exist")
+
+				m.AddClusterQueue(newCq("deleted-once"))
+				m.AddClusterQueue(newCq("deleted-twice"))
+
+				m.DeleteClusterQueue("doesnt-exist2")
+				m.DeleteClusterQueue("deleted-once")
+				m.DeleteClusterQueue("deleted-twice")
+				m.DeleteClusterQueue("deleted-twice")
+			},
+			wantCqs: sets.New[string](),
+		},
+		"cohort remains as long as one of its children remains": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.AddClusterQueue(newCq("queue3"))
+				m.UpdateClusterQueueEdge("queue1", "cohort")
+				m.UpdateClusterQueueEdge("queue2", "cohort")
+				m.UpdateClusterQueueEdge("queue3", "to-be-deleted-cohort")
+
+				m.DeleteClusterQueue("queue2")
+				m.DeleteClusterQueue("queue3")
+			},
+			wantCqs:     sets.New("queue1"),
+			wantCohorts: sets.New("cohort"),
+			wantCqEdge:  []cqEdge{{"queue1", "cohort"}},
+		},
+	}
+
+	for name, tc := range tests {
+		t.Run(name, func(t *testing.T) {
+			mgr := NewManager[*testClusterQueue, *testCohort](cohortFactory)
+			tc.operations(mgr)
+			t.Run("clusterqueues match", func(t *testing.T) {
+				gotCqs := sets.New[string]()
+				for _, cq := range mgr.ClusterQueues {
+					gotCqs.Insert(cq.GetName())
+				}
+				if diff := cmp.Diff(tc.wantCqs, gotCqs); diff != "" {
+					t.Fatalf("Unexpected cqs -want +got %s", diff)
+				}
+			})
+			t.Run("cohorts match", func(t *testing.T) {
+				gotCohorts := sets.New[string]()
+				for _, cohort := range mgr.Cohorts {
+					gotCohorts.Insert(cohort.GetName())
+				}
+				if diff := cmp.Diff(tc.wantCohorts, gotCohorts); diff != "" {
+					t.Fatalf("Unexpected cohorts -want +got %s", diff)
+				}
+			})
+			t.Run("verify cq<->cohort edge count", func(t *testing.T) {
```

  I have a feeling these two tests "verify cq<->cohort edge count" and "verify cq<->cohort edges" are quite verbose. Would it be simpler to create a set of pairs edges by iterating over the graph, then use cmp.Diff to compare it with `wantCqEdge`?

- **pkg/cache/cache_test.go:1066** by [@mimowo](https://github.com/mimowo) — 2024-08-13T15:29:33Z
```diff
@@ -985,25 +1044,37 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			if err := tc.operation(cache); err != nil {
 				t.Errorf("Unexpected error during test operation: %s", err)
 			}
-			if diff := cmp.Diff(tc.wantClusterQueues, cache.clusterQueues,
-				cmpopts.IgnoreFields(clusterQueue{}, "Cohort", "ResourceGroups"),
+			if diff := cmp.Diff(tc.wantClusterQueues, cache.hm.ClusterQueues,
+				cmpopts.IgnoreFields(clusterQueue{}, "ResourceGroups"),
 				cmpopts.IgnoreFields(workload.Info{}, "Obj", "LastAssignment"),
-				cmpopts.IgnoreUnexported(clusterQueue{}),
+				cmpopts.IgnoreUnexported(clusterQueue{}, hierarchy.WiredClusterQueue[*clusterQueue, *cohort]{}),
 				cmpopts.EquateEmpty()); diff != "" {
 				t.Errorf("Unexpected clusterQueues (-want,+got):\n%s", diff)
 			}
 
-			gotCohorts := make(map[string]sets.Set[string], len(cache.cohorts))
-			for name, cohort := range cache.cohorts {
+			gotCohorts := make(map[string]sets.Set[string], len(cache.hm.Cohorts))
+			for name, cohort := range cache.hm.Cohorts {
 				gotCohort := sets.New[string]()
-				for cq := range cohort.Members {
+				for _, cq := range cohort.Members() {
 					gotCohort.Insert(cq.Name)
 				}
 				gotCohorts[name] = gotCohort
 			}
 			if diff := cmp.Diff(tc.wantCohorts, gotCohorts, cmpopts.EquateEmpty()); diff != "" {
 				t.Errorf("Unexpected cohorts (-want,+got):\n%s", diff)
 			}
+			for _, cq := range cache.hm.ClusterQueues {
```

  I think these two-way checks are a bit hard to read. Would it be cleaner to have `wantCqEdges` and assert similarly as suggested in the comment to "verify cq<->cohort edge count"?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-13T15:46:35Z

- **pkg/hierarchy/manager_test.go:169** by [@gabesaba](https://github.com/gabesaba) — 2024-08-13T15:46:35Z
```diff
@@ -0,0 +1,232 @@
+package hierarchy
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+func TestManager(t *testing.T) {
+	type M = Manager[*testClusterQueue, *testCohort]
+	type cqEdge struct {
+		cq     string
+		cohort string
+	}
+	tests := map[string]struct {
+		operations  func(M)
+		wantCqs     sets.Set[string]
+		wantCohorts sets.Set[string]
+		wantCqEdge  []cqEdge
+	}{
+		"create queues": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+			},
+			wantCqs: sets.New("queue1", "queue2"),
+		},
+		"delete queue": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.DeleteClusterQueue("queue2")
+			},
+			wantCqs: sets.New("queue1"),
+		},
+		"create queues with cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.AddClusterQueue(newCq("queue3"))
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-a")
+				m.UpdateClusterQueueEdge("queue3", "cohort-b")
+			},
+			wantCqs: sets.New(
+				"queue1",
+				"queue2",
+				"queue3",
+			),
+			wantCohorts: sets.New(
+				"cohort-a",
+				"cohort-b",
+			),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-a"},
+				{"queue2", "cohort-a"},
+				{"queue3", "cohort-b"},
+			},
+		},
+		"update cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+			},
+			wantCqs:     sets.New("queue1", "queue2"),
+			wantCohorts: sets.New("cohort-b", "cohort-c"),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-b"},
+				{"queue2", "cohort-c"},
+			},
+		},
+		"updating idempotent": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+			},
+			wantCqs:     sets.New("queue1", "queue2"),
+			wantCohorts: sets.New("cohort-b", "cohort-c"),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-b"},
+				{"queue2", "cohort-c"},
+			},
+		},
+		"delete cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				// Delete cohort-a by deleting edge.
+				// Delete cohort-b by deleting cq.
+				m.UpdateClusterQueueEdge("queue1", "")
+				m.DeleteClusterQueue("queue2")
+			},
+			wantCqs: sets.New("queue1"),
+		},
+		"deletion idempotent": {
+			operations: func(m M) {
+				m.DeleteClusterQueue("doesnt-exist")
+
+				m.AddClusterQueue(newCq("deleted-once"))
+				m.AddClusterQueue(newCq("deleted-twice"))
+
+				m.DeleteClusterQueue("doesnt-exist2")
+				m.DeleteClusterQueue("deleted-once")
+				m.DeleteClusterQueue("deleted-twice")
+				m.DeleteClusterQueue("deleted-twice")
+			},
+			wantCqs: sets.New[string](),
+		},
+		"cohort remains as long as one of its children remains": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.AddClusterQueue(newCq("queue3"))
+				m.UpdateClusterQueueEdge("queue1", "cohort")
+				m.UpdateClusterQueueEdge("queue2", "cohort")
+				m.UpdateClusterQueueEdge("queue3", "to-be-deleted-cohort")
+
+				m.DeleteClusterQueue("queue2")
+				m.DeleteClusterQueue("queue3")
+			},
+			wantCqs:     sets.New("queue1"),
+			wantCohorts: sets.New("cohort"),
+			wantCqEdge:  []cqEdge{{"queue1", "cohort"}},
+		},
+	}
+
+	for name, tc := range tests {
+		t.Run(name, func(t *testing.T) {
+			mgr := NewManager[*testClusterQueue, *testCohort](cohortFactory)
+			tc.operations(mgr)
+			t.Run("clusterqueues match", func(t *testing.T) {
+				gotCqs := sets.New[string]()
+				for _, cq := range mgr.ClusterQueues {
+					gotCqs.Insert(cq.GetName())
+				}
+				if diff := cmp.Diff(tc.wantCqs, gotCqs); diff != "" {
+					t.Fatalf("Unexpected cqs -want +got %s", diff)
+				}
+			})
+			t.Run("cohorts match", func(t *testing.T) {
+				gotCohorts := sets.New[string]()
+				for _, cohort := range mgr.Cohorts {
+					gotCohorts.Insert(cohort.GetName())
+				}
+				if diff := cmp.Diff(tc.wantCohorts, gotCohorts); diff != "" {
+					t.Fatalf("Unexpected cohorts -want +got %s", diff)
+				}
+			})
+			t.Run("verify cq<->cohort edge count", func(t *testing.T) {
```

  Yes, I quite like this suggestion. It is much simpler now :)
  
   Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-13T16:00:42Z

- **pkg/hierarchy/manager_test.go:169** by [@mimowo](https://github.com/mimowo) — 2024-08-13T16:00:42Z
```diff
@@ -0,0 +1,232 @@
+package hierarchy
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+func TestManager(t *testing.T) {
+	type M = Manager[*testClusterQueue, *testCohort]
+	type cqEdge struct {
+		cq     string
+		cohort string
+	}
+	tests := map[string]struct {
+		operations  func(M)
+		wantCqs     sets.Set[string]
+		wantCohorts sets.Set[string]
+		wantCqEdge  []cqEdge
+	}{
+		"create queues": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+			},
+			wantCqs: sets.New("queue1", "queue2"),
+		},
+		"delete queue": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.DeleteClusterQueue("queue2")
+			},
+			wantCqs: sets.New("queue1"),
+		},
+		"create queues with cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.AddClusterQueue(newCq("queue3"))
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-a")
+				m.UpdateClusterQueueEdge("queue3", "cohort-b")
+			},
+			wantCqs: sets.New(
+				"queue1",
+				"queue2",
+				"queue3",
+			),
+			wantCohorts: sets.New(
+				"cohort-a",
+				"cohort-b",
+			),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-a"},
+				{"queue2", "cohort-a"},
+				{"queue3", "cohort-b"},
+			},
+		},
+		"update cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+			},
+			wantCqs:     sets.New("queue1", "queue2"),
+			wantCohorts: sets.New("cohort-b", "cohort-c"),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-b"},
+				{"queue2", "cohort-c"},
+			},
+		},
+		"updating idempotent": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue2", "cohort-c")
+				m.UpdateClusterQueueEdge("queue1", "cohort-b")
+			},
+			wantCqs:     sets.New("queue1", "queue2"),
+			wantCohorts: sets.New("cohort-b", "cohort-c"),
+			wantCqEdge: []cqEdge{
+				{"queue1", "cohort-b"},
+				{"queue2", "cohort-c"},
+			},
+		},
+		"delete cohorts": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+
+				m.UpdateClusterQueueEdge("queue1", "cohort-a")
+				m.UpdateClusterQueueEdge("queue2", "cohort-b")
+
+				// Delete cohort-a by deleting edge.
+				// Delete cohort-b by deleting cq.
+				m.UpdateClusterQueueEdge("queue1", "")
+				m.DeleteClusterQueue("queue2")
+			},
+			wantCqs: sets.New("queue1"),
+		},
+		"deletion idempotent": {
+			operations: func(m M) {
+				m.DeleteClusterQueue("doesnt-exist")
+
+				m.AddClusterQueue(newCq("deleted-once"))
+				m.AddClusterQueue(newCq("deleted-twice"))
+
+				m.DeleteClusterQueue("doesnt-exist2")
+				m.DeleteClusterQueue("deleted-once")
+				m.DeleteClusterQueue("deleted-twice")
+				m.DeleteClusterQueue("deleted-twice")
+			},
+			wantCqs: sets.New[string](),
+		},
+		"cohort remains as long as one of its children remains": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue1"))
+				m.AddClusterQueue(newCq("queue2"))
+				m.AddClusterQueue(newCq("queue3"))
+				m.UpdateClusterQueueEdge("queue1", "cohort")
+				m.UpdateClusterQueueEdge("queue2", "cohort")
+				m.UpdateClusterQueueEdge("queue3", "to-be-deleted-cohort")
+
+				m.DeleteClusterQueue("queue2")
+				m.DeleteClusterQueue("queue3")
+			},
+			wantCqs:     sets.New("queue1"),
+			wantCohorts: sets.New("cohort"),
+			wantCqEdge:  []cqEdge{{"queue1", "cohort"}},
+		},
+	}
+
+	for name, tc := range tests {
+		t.Run(name, func(t *testing.T) {
+			mgr := NewManager[*testClusterQueue, *testCohort](cohortFactory)
+			tc.operations(mgr)
+			t.Run("clusterqueues match", func(t *testing.T) {
+				gotCqs := sets.New[string]()
+				for _, cq := range mgr.ClusterQueues {
+					gotCqs.Insert(cq.GetName())
+				}
+				if diff := cmp.Diff(tc.wantCqs, gotCqs); diff != "" {
+					t.Fatalf("Unexpected cqs -want +got %s", diff)
+				}
+			})
+			t.Run("cohorts match", func(t *testing.T) {
+				gotCohorts := sets.New[string]()
+				for _, cohort := range mgr.Cohorts {
+					gotCohorts.Insert(cohort.GetName())
+				}
+				if diff := cmp.Diff(tc.wantCohorts, gotCohorts); diff != "" {
+					t.Fatalf("Unexpected cohorts -want +got %s", diff)
+				}
+			})
+			t.Run("verify cq<->cohort edge count", func(t *testing.T) {
```

  lgtm, thanks

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-13T16:13:35Z

- **pkg/cache/cache_test.go:1066** by [@gabesaba](https://github.com/gabesaba) — 2024-08-13T16:13:35Z
```diff
@@ -985,25 +1044,37 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			if err := tc.operation(cache); err != nil {
 				t.Errorf("Unexpected error during test operation: %s", err)
 			}
-			if diff := cmp.Diff(tc.wantClusterQueues, cache.clusterQueues,
-				cmpopts.IgnoreFields(clusterQueue{}, "Cohort", "ResourceGroups"),
+			if diff := cmp.Diff(tc.wantClusterQueues, cache.hm.ClusterQueues,
+				cmpopts.IgnoreFields(clusterQueue{}, "ResourceGroups"),
 				cmpopts.IgnoreFields(workload.Info{}, "Obj", "LastAssignment"),
-				cmpopts.IgnoreUnexported(clusterQueue{}),
+				cmpopts.IgnoreUnexported(clusterQueue{}, hierarchy.WiredClusterQueue[*clusterQueue, *cohort]{}),
 				cmpopts.EquateEmpty()); diff != "" {
 				t.Errorf("Unexpected clusterQueues (-want,+got):\n%s", diff)
 			}
 
-			gotCohorts := make(map[string]sets.Set[string], len(cache.cohorts))
-			for name, cohort := range cache.cohorts {
+			gotCohorts := make(map[string]sets.Set[string], len(cache.hm.Cohorts))
+			for name, cohort := range cache.hm.Cohorts {
 				gotCohort := sets.New[string]()
-				for cq := range cohort.Members {
+				for _, cq := range cohort.Members() {
 					gotCohort.Insert(cq.Name)
 				}
 				gotCohorts[name] = gotCohort
 			}
 			if diff := cmp.Diff(tc.wantCohorts, gotCohorts, cmpopts.EquateEmpty()); diff != "" {
 				t.Errorf("Unexpected cohorts (-want,+got):\n%s", diff)
 			}
+			for _, cq := range cache.hm.ClusterQueues {
```

  Discussed offline with @mimowo: decided to delete these tests, as it is `hierarchy.Manager`'s job to maintain consistency of edges, so these tests are duplicates. We are already verify CQs in Cohorts a few lines above

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-23T19:51:39Z

- **pkg/hierarchy/manager.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:30:44Z
```diff
@@ -0,0 +1,80 @@
+package hierarchy
```

  Please add the copyright notice. Same in other files.

- **pkg/cache/clusterqueue.go:125** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:39:46Z
```diff
@@ -112,16 +122,16 @@ type queue struct {
 	admittedUsage resources.FlavorResourceQuantities
 }
 
-func newCohort(name string, size int) *cohort {
+func cohortFactory(name string) *cohort {
```

  factories usually return a function that creates something...
  this is just creating a cohort, so the appropriate name is `newCohort` still.

- **pkg/hierarchy/wired_clusterqueue.go:3** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:46:11Z
```diff
@@ -0,0 +1,19 @@
+package hierarchy
+
+type WiredClusterQueue[CQ, Cohort nodeBase] struct {
```

  What does "wired" mean?
  
  Add a comment or find a better word.

- **pkg/hierarchy/manager.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:51:29Z
```diff
@@ -0,0 +1,80 @@
+package hierarchy
+
+// Manager stores Cohorts and ClusterQueues, and maintains the edges
+// between them.
+type Manager[CQ cqNode[CQ, C], C cohortNode[CQ, C]] struct {
+	Cohorts       map[string]C
+	ClusterQueues map[string]CQ
+	cohortFactory func(string) C
+}
+
+// NewManager creates a new Manager.  A Cohort factory
+// function must be provided to instantiate Cohorts in the case that a
+// ClusterQueue references a Cohort not backed by an API object.
+func NewManager[CQ cqNode[CQ, C], C cohortNode[CQ, C]](cohortFactory func(string) C) Manager[CQ, C] {
+	return Manager[CQ, C]{
+		make(map[string]C),
+		make(map[string]CQ),
+		cohortFactory,
+	}
+}
+
+func (c *Manager[CQ, C]) AddClusterQueue(cq CQ) {
+	c.ClusterQueues[cq.GetName()] = cq
+}
+
+func (c *Manager[CQ, C]) UpdateClusterQueueEdge(name, parentName string) {
+	cq := c.ClusterQueues[name]
+	c.unwireClusterQueue(cq)
+	if parentName != "" {
+		cohort := c.getCohort(parentName)
+		cohort.Wired().members.Insert(cq)
+		cq.Wired().cohort = cohort
+	}
+}
+
+func (c *Manager[CQ, C]) DeleteClusterQueue(name string) {
+	if cq, ok := c.ClusterQueues[name]; ok {
+		c.unwireClusterQueue(cq)
+		delete(c.ClusterQueues, name)
+		return
+	}
+}
+
+func (c *Manager[CQ, C]) unwireClusterQueue(cq CQ) {
+	if cq.Wired().HasCohort() {
+		cohort := cq.Wired().Cohort()
+		cohort.Wired().members.Delete(cq)
+		c.cleanupCohort(cohort)
+		var zero C
+		cq.Wired().cohort = zero
+	}
+}
+
+func (c *Manager[CQ, C]) getCohort(cohortName string) C {
+	if _, ok := c.Cohorts[cohortName]; !ok {
+		c.Cohorts[cohortName] = c.cohortFactory(cohortName)
+	}
+	return c.Cohorts[cohortName]
+}
+
+func (c *Manager[CQ, C]) cleanupCohort(cohort C) {
+	if cohort.Wired().members.Len() == 0 {
+		delete(c.Cohorts, cohort.GetName())
+	}
+}
+
+type nodeBase interface {
+	GetName() string
+	comparable
+}
+
+type cqNode[CQ, C nodeBase] interface {
+	Wired() *WiredClusterQueue[CQ, C]
```

  why not have in the interface something like:
  
  ```golang
  Cohort() C
  HasCohort() bool
  ```

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-26T10:11:30Z

- **pkg/hierarchy/manager.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T10:11:30Z
```diff
@@ -0,0 +1,80 @@
+package hierarchy
```

  #2900

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-26T19:50:08Z

- **pkg/hierarchy/wired_clusterqueue.go:3** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T19:50:08Z
```diff
@@ -0,0 +1,19 @@
+package hierarchy
+
+type WiredClusterQueue[CQ, Cohort nodeBase] struct {
```

  TBH, I would just name this `ClusterQueue`.
  
  The differentiator from the other structs in the project comes from the package name.
