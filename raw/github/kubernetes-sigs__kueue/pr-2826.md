# PR #2826: [PartialAdmission] Fix preemption while partially admitting.

**Summary**: [PartialAdmission] Fix preemption while partially admitting.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2826

**Last updated**: 2024-10-09T13:51:53Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `partial-admission-preempt` → `main`
- **Created**: 2024-08-12T16:10:07Z
- **Updated**: 2024-10-09T13:51:53Z
- **Merged**: 2024-09-23T17:18:00Z
- **Closed**: 2024-09-23T17:18:00Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk)
- **Changed files**: 4   **+56 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
- Fix the flow that allows a workload to be partially admitted while preempting lower priority workloads. 
- Add an unit test for preemption while partially admitting.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2799 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix some scenarios for partial admission which are affected by wrong calculation of resources
used by the incoming workload which is partially admitted and preempting.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-12T16:10:24Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6fa2dcaf1ed6477e5c2ac3b1f1cef3c394a0c17d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e9896ab4da4400089409d6 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2826--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2826--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI4MjYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.tFQEFzsohe4NBvz7rjXmawQkSB6MThcK3hDNxwAgJbk)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@trasc](https://github.com/trasc) — 2024-08-12T16:10:26Z

/cc @gabesaba

### Comment by [@mimowo](https://github.com/mimowo) — 2024-08-13T06:27:29Z

Please add a release note describing what's changed from the user's perspective.

### Comment by [@trasc](https://github.com/trasc) — 2024-08-13T06:57:55Z

> Please add a release note describing what's changed from the user's perspective.

done

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-13T09:31:15Z

/assign

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T05:11:37Z

@gabesaba @mimowo is there anything else we should do on this?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-17T07:31:03Z

I support the ask in [comment](https://github.com/kubernetes-sigs/kueue/pull/2826#discussion_r1719937771).

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-17T08:02:11Z

@trasc  Could you describe what the scenario is fixed here, and what is the consequence from the user perspective when it happens? 

Please update the release note with a short description of the problematic scenario. 

It will also help us to guide the decision about cherry-picking.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-17T08:03:13Z

@gabesaba is it fair to close the original issue https://github.com/kubernetes-sigs/kueue/issues/2799 when this PR is merged?

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T13:31:23Z

> @trasc Could you describe what the scenario is fixed here, and what is the consequence from the user perspective when it happens?
> 
> Please update the release note with a short description of the problematic scenario.
> 
> It will also help us to guide the decision about cherry-picking.

The scenario is `preemption during partial admission`.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-17T13:35:11Z

> The scenario is `preemption during partial admission`.

Can you provide a little bit more details? In particular: is the preemption not working at all / erroring / panicing / or working sub-optimally.
Also: where the issues deterministic on every preemption, or occasional?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-17T13:35:25Z

/reopen
misclicked by mistake

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-17T13:35:32Z

@mimowo: Reopened this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2826#issuecomment-2355827271):

>/reopen
>misclicked by mistake


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-19T12:24:37Z

/cc

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-20T12:13:51Z

> The scenario is preemption during partial admission.

I'm testing locally the impact of the bug and it seems to go beyond preemption. 

For example:
1. I have a CQ allowing for 9 pods (preemption disabled completely)
2. I created 2 small jobs, each consuming 3 pods, so 6 pods total. 
3. a big Job comes in requesting 9 pods, but using partial admission to say min 3 pods
4. 3 pods are created (correctly), but the CQ misreports usage as 15 units (6 + 9).
5. When, the small jobs are deleted (or finished), the big Job continues to use 3 pods, but reports consuming 9 resources
6. I create new small jobs requesting 3 units which should fit in, but they are suspended

I'm yet going to determine the scope of the problem and fix, but it seems serious enough to justify cherry-picking.

### Comment by [@trasc](https://github.com/trasc) — 2024-09-20T13:07:36Z

> > The scenario is preemption during partial admission.
> 
> I'm testing locally the impact of the bug and it seems to go beyond preemption.
> 
> For example:
> 
> 1. I have a CQ allowing for 9 pods (preemption disabled completely)
> 2. I created 2 small jobs, each consuming 3 pods, so 6 pods total.
> 3. a big Job comes in requesting 9 pods, but using partial admission to say min 3 pods
> 4. 3 pods are created (correctly), but the CQ misreports usage as 15 units (6 + 9).
> 5. When, the small jobs are deleted (or finished), the big Job continues to use 3 pods, but reports consuming 9 resources
> 6. I create new small jobs requesting 3 units which should fit in, but they are suspended
> 
> I'm yet going to determine the scope of the problem and fix, but it seems serious enough to justify cherry-picking.

I think this is a different issue. Just open the issue in GH and feel free to assign it to me.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-20T17:36:33Z

Ok, there you go: https://github.com/kubernetes-sigs/kueue/issues/3108

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-20T17:59:34Z

Regarding the fix here I played a bit more, and observed, that the feature generally worked before (no panics, no errors), but there are differences in some scenarios, and they are really hard for me to briefly (also the other issue is interfering to test properly, because the other issue is also present with preemption enabled). 

Still, IIUC the culprit for the issue is wrong calculation of resources requested by the incoming workload. I would like to capture this in the release note as below.
```
/release-note-edit
```release-note
Fix some scenarios for partial admission which are affected by wrong calculation of resources
used by the incoming workload which is partially admitted and preempting.
```

Going forward it would be super helpful to have an integration test(s) for this feature, otherwise it is hard to judge how the unit tests translate into the e2e behavior, imo.

Regarding the cherry-pick, no strong view here, but I think we don't need to, given that we don't have customers / users affected by the issue, or demanding the fix. Additionally, the other issue opened above still interferes to fully evaluate the fix. We can re-evaluate cherry-picking them together, or when we have users demanding the fix.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-20T18:04:06Z

LGTM, except for the point raised [here](https://github.com/kubernetes-sigs/kueue/pull/2826#discussion_r1766411786), but I can accept as is, just waiting a bit for @alculquicondor in case he has something to add or other ideas to improve.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-23T16:56:55Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-23T16:57:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2826#issuecomment-2368846622" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2826#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-23T16:57:02Z

LGTM label has been added.  <details>Git tree hash: bfea979511531e090af34379b6573419f2bde216</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-10-09T13:51:14Z

/cherrypick release-0.8

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-10-09T13:51:52Z

@trasc: new pull request created: #3205

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2826#issuecomment-2402410415):

>/cherrypick release-0.8


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-08-13T07:17:36Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:117** by [@mimowo](https://github.com/mimowo) — 2024-08-13T07:16:07Z
```diff
@@ -110,7 +110,12 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
 		for res, q := range ps.Requests {
-			flv := a.PodSets[i].Flavors[res].Name
+			aps := a.PodSets[i]
+			flv := aps.Flavors[res].Name
+			// in case of partial admission scale down the quantity
+			if aps.Count != ps.Count {
+				q = q * int64(aps.Count) / int64(ps.Count)
```

  IIRC we support `ps.Count=0` (https://github.com/kubernetes-sigs/kueue/issues/2227). Could it break in a scenario like this?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-13T08:00:04Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:117** by [@trasc](https://github.com/trasc) — 2024-08-13T07:59:11Z
```diff
@@ -110,7 +110,12 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
 		for res, q := range ps.Requests {
-			flv := a.PodSets[i].Flavors[res].Name
+			aps := a.PodSets[i]
+			flv := aps.Flavors[res].Name
+			// in case of partial admission scale down the quantity
+			if aps.Count != ps.Count {
+				q = q * int64(aps.Count) / int64(ps.Count)
```

  I guess it is possible, reworked.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-14T14:33:49Z

- **pkg/scheduler/scheduler.go:497** by [@gabesaba](https://github.com/gabesaba) — 2024-08-14T14:33:49Z
```diff
@@ -471,9 +471,12 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	if wl.CanBePartiallyAdmitted() {
 		reducer := flavorassigner.NewPodSetReducer(wl.Obj.Spec.PodSets, func(nextCounts []int32) (*partialAssignment, bool) {
 			assignment := flvAssigner.Assign(log, nextCounts)
-			if assignment.RepresentativeMode() == flavorassigner.Fit {
+			mode := assignment.RepresentativeMode()
+			if mode == flavorassigner.Fit {
 				return &partialAssignment{assignment: assignment}, true
-			} else if assignment.RepresentativeMode() == flavorassigner.Preempt {
+			}
+
+			if mode == flavorassigner.Preempt {
```

  > [This test](https://github.com/kubernetes-sigs/kueue/blob/00111d9aada59e1a6d8d90b9847cf7451bc5be76/pkg/scheduler/scheduler_test.go#L1194) exits [here](https://github.com/kubernetes-sigs/kueue/blob/00111d9aada59e1a6d8d90b9847cf7451bc5be76/pkg/scheduler/scheduler.go#L467-L469), as we find a preemption target above. The PartialAdmission preemption path is not tested.
  
  after deletion of this preemption block, the tests noted in #2799 still passes:
  ```
  go test ./pkg/scheduler -run "TestSchedule/partial_admission_single_variable_pod_set,_preempt_first"
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-08-14T16:03:09Z

- **pkg/scheduler/scheduler.go:497** by [@trasc](https://github.com/trasc) — 2024-08-14T16:03:09Z
```diff
@@ -471,9 +471,12 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	if wl.CanBePartiallyAdmitted() {
 		reducer := flavorassigner.NewPodSetReducer(wl.Obj.Spec.PodSets, func(nextCounts []int32) (*partialAssignment, bool) {
 			assignment := flvAssigner.Assign(log, nextCounts)
-			if assignment.RepresentativeMode() == flavorassigner.Fit {
+			mode := assignment.RepresentativeMode()
+			if mode == flavorassigner.Fit {
 				return &partialAssignment{assignment: assignment}, true
-			} else if assignment.RepresentativeMode() == flavorassigner.Preempt {
+			}
+
+			if mode == flavorassigner.Preempt {
```

  That test checks that preemption is done and wl admitted in full (without checking if it fits partially and decreasing the count) if that preemption is an option.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-16T14:53:59Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:119** by [@gabesaba](https://github.com/gabesaba) — 2024-08-16T14:37:58Z
```diff
@@ -109,8 +109,13 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
+			ps = *ps.ScaledTo(aps.Count)
+		}
 		for res, q := range ps.Requests {
```

  ```suggestion
  		aps := a.PodSets[i]
  		for res, q := range ps.ScaledTo(aps.Count).Requests {
  ```
  
  nit: check whether there is a change of `pod count` in `ScaledTo`, and call ScaledTo in the `range` line

- **pkg/workload/workload.go:173** by [@gabesaba](https://github.com/gabesaba) — 2024-08-16T14:39:39Z
```diff
@@ -170,9 +170,11 @@ func (psr *PodSetResources) ScaledTo(newCount int32) *PodSetResources {
 		Flavors:  maps.Clone(psr.Flavors),
 	}
 
-	scaleDown(ret.Requests, int64(ret.Count))
-	scaleUp(ret.Requests, int64(newCount))
-	ret.Count = newCount
+	if psr.Count != 0 {
```

  ```suggestion
  	if psr.Count == 0 || psr.Count == newCount {
  	  return psr
  	}
  ```
  is it safe to do this guard clause (which would be placed at the top of the function, before the copy)? i.e. do any callers modify this object, outside of ScaledTo?

- **pkg/scheduler/flavorassigner/flavorassigner.go:111** by [@gabesaba](https://github.com/gabesaba) — 2024-08-16T14:50:28Z
```diff
@@ -109,8 +109,13 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
```

  Please add a comment indicating that in the case of `PartialAdmission`, we scale the returned `Requests`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-02T07:20:56Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:111** by [@trasc](https://github.com/trasc) — 2024-09-02T07:12:47Z
```diff
@@ -109,8 +109,13 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
```

  done

- **pkg/scheduler/flavorassigner/flavorassigner.go:119** by [@trasc](https://github.com/trasc) — 2024-09-02T07:16:31Z
```diff
@@ -109,8 +109,13 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
+			ps = *ps.ScaledTo(aps.Count)
+		}
 		for res, q := range ps.Requests {
```

  I prefer to keep it as is, in my opinion the caller should decide if it needs to do scale and this way is more visible.

- **pkg/workload/workload.go:173** by [@trasc](https://github.com/trasc) — 2024-09-02T07:19:02Z
```diff
@@ -170,9 +170,11 @@ func (psr *PodSetResources) ScaledTo(newCount int32) *PodSetResources {
 		Flavors:  maps.Clone(psr.Flavors),
 	}
 
-	scaleDown(ret.Requests, int64(ret.Count))
-	scaleUp(ret.Requests, int64(newCount))
-	ret.Count = newCount
+	if psr.Count != 0 {
```

  let's just skip the scale operations if the count is the same.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-17T07:30:06Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:119** by [@mimowo](https://github.com/mimowo) — 2024-09-17T07:30:06Z
```diff
@@ -109,8 +109,13 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
+			ps = *ps.ScaledTo(aps.Count)
+		}
 		for res, q := range ps.Requests {
```

  I agree that moving the check into `ScaledTo` is preferred. It matches my expectations on how the function should behave (safe and performant).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-17T07:55:41Z

- **pkg/scheduler/scheduler.go:492** by [@mimowo](https://github.com/mimowo) — 2024-09-17T07:55:41Z
```diff
@@ -489,9 +489,12 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	if wl.CanBePartiallyAdmitted() {
 		reducer := flavorassigner.NewPodSetReducer(wl.Obj.Spec.PodSets, func(nextCounts []int32) (*partialAssignment, bool) {
 			assignment := flvAssigner.Assign(log, nextCounts)
-			if assignment.RepresentativeMode() == flavorassigner.Fit {
```

  Please reverted formatting changes if not needed (to miminize distractions).

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-17T13:29:20Z

- **pkg/scheduler/scheduler.go:492** by [@trasc](https://github.com/trasc) — 2024-09-17T13:23:37Z
```diff
@@ -489,9 +489,12 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	if wl.CanBePartiallyAdmitted() {
 		reducer := flavorassigner.NewPodSetReducer(wl.Obj.Spec.PodSets, func(nextCounts []int32) (*partialAssignment, bool) {
 			assignment := flvAssigner.Assign(log, nextCounts)
-			if assignment.RepresentativeMode() == flavorassigner.Fit {
```

  there is no reason the call `RepresentativeMode` twice

- **pkg/scheduler/flavorassigner/flavorassigner.go:119** by [@trasc](https://github.com/trasc) — 2024-09-17T13:29:15Z
```diff
@@ -109,8 +109,13 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
+			ps = *ps.ScaledTo(aps.Count)
+		}
 		for res, q := range ps.Requests {
```

  `ScaleTo` is now checking if the count is different but for it's own purpose (to skip the actual scaling), having it also skip the cloning it's odd for me.  As well as cloning when it's not needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-17T13:35:52Z

- **pkg/scheduler/scheduler.go:492** by [@mimowo](https://github.com/mimowo) — 2024-09-17T13:35:52Z
```diff
@@ -489,9 +489,12 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	if wl.CanBePartiallyAdmitted() {
 		reducer := flavorassigner.NewPodSetReducer(wl.Obj.Spec.PodSets, func(nextCounts []int32) (*partialAssignment, bool) {
 			assignment := flvAssigner.Assign(log, nextCounts)
-			if assignment.RepresentativeMode() == flavorassigner.Fit {
```

  I would suggest opening a dedicated cleanup PR, doesn't see to have anything to do with the fix.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-17T14:00:58Z

- **pkg/scheduler/scheduler.go:492** by [@trasc](https://github.com/trasc) — 2024-09-17T14:00:58Z
```diff
@@ -489,9 +489,12 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	if wl.CanBePartiallyAdmitted() {
 		reducer := flavorassigner.NewPodSetReducer(wl.Obj.Spec.PodSets, func(nextCounts []int32) (*partialAssignment, bool) {
 			assignment := flvAssigner.Assign(log, nextCounts)
-			if assignment.RepresentativeMode() == flavorassigner.Fit {
```

  done #3078

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T08:32:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:116** by [@mimowo](https://github.com/mimowo) — 2024-09-19T08:32:17Z
```diff
@@ -106,11 +106,18 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 	return psFlavors
 }
 
+// TotalRequestsFor - returns the total quota needs of the wl, taking into account the potential
+// scaling needed in case of partial admission.
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
```

  Please drop this check, as suggested in the [comment](https://github.com/kubernetes-sigs/kueue/pull/2826/files#r1719937771) it is not necessary as `ScaledTo` already takes care of it.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-19T09:09:46Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:116** by [@trasc](https://github.com/trasc) — 2024-09-19T09:09:46Z
```diff
@@ -106,11 +106,18 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 	return psFlavors
 }
 
+// TotalRequestsFor - returns the total quota needs of the wl, taking into account the potential
+// scaling needed in case of partial admission.
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
```

  - `ScaledTo` need to do that after cloning the input , otherwise it will need to return a clone or the original depending on some opaque logic. 
  
  - At least for now `aps.Count != ps.Count` it's very unlikely to happen , the feature is supported by batch/Job only, additionally potential new frameworks supporting this will only allow one podSet to have a variable count, therefore by keeping this we avoid unnecessary allocations.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T09:18:27Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:116** by [@mimowo](https://github.com/mimowo) — 2024-09-19T09:18:27Z
```diff
@@ -106,11 +106,18 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 	return psFlavors
 }
 
+// TotalRequestsFor - returns the total quota needs of the wl, taking into account the potential
+// scaling needed in case of partial admission.
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
```

  I think it is a premature optimization. Clean code is more important in this case that the allocation.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-19T09:46:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:116** by [@trasc](https://github.com/trasc) — 2024-09-19T09:46:05Z
```diff
@@ -106,11 +106,18 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 	return psFlavors
 }
 
+// TotalRequestsFor - returns the total quota needs of the wl, taking into account the potential
+// scaling needed in case of partial admission.
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
```

  Separation of concerns should be taken into account also, `TotalRequestsFor` can and should decide if scaling is needed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-19T16:02:58Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:116** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-19T15:57:34Z
```diff
@@ -106,11 +106,18 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 	return psFlavors
 }
 
+// TotalRequestsFor - returns the total quota needs of the wl, taking into account the potential
+// scaling needed in case of partial admission.
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
```

  I think `TotalRequestsFor` is called in a couple of somewhat hot places, so it might be worth avoiding the allocation (plus 2 map clones).
  
  A middle ground solution could be to have an additional `ScaleTo` function that overrides the original object, which `ScaledTo` can call to avoid code repetition.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-20T08:45:18Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:116** by [@trasc](https://github.com/trasc) — 2024-09-20T08:45:18Z
```diff
@@ -106,11 +106,18 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 	return psFlavors
 }
 
+// TotalRequestsFor - returns the total quota needs of the wl, taking into account the potential
+// scaling needed in case of partial admission.
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
```

  Unfortunately we need a clone every time we are scaling, otherwise we end up mutating the `Info.TotalRequests` that is used to re-queue the workload.
  
  - We could ignore this aspect but we it could have surprising and very hard to debug side effects in the future.
  - We could attempt to restore the scaling before re-queuing.
  
  But any of this options are more complex or harder to follow the the current approach.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-23T16:52:00Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:116** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-23T16:51:59Z
```diff
@@ -106,11 +106,18 @@ func (a *Assignment) ToAPI() []kueue.PodSetAssignment {
 	return psFlavors
 }
 
+// TotalRequestsFor - returns the total quota needs of the wl, taking into account the potential
+// scaling needed in case of partial admission.
 func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourceQuantities {
 	usage := make(resources.FlavorResourceQuantities)
 	for i, ps := range wl.TotalRequests {
+		// in case of partial admission scale down the quantity
+		aps := a.PodSets[i]
+		if aps.Count != ps.Count {
```

  Right.
  I'm ok with keeping the optimization.
