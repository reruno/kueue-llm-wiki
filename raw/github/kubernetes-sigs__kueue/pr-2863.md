# PR #2863: [kjobctl] Add delete slurm command.

**Summary**: [kjobctl] Add delete slurm command.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2863

**Last updated**: 2024-09-12T11:13:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/kjobctl-add-delete-slurm-command` → `main`
- **Created**: 2024-08-20T12:45:14Z
- **Updated**: 2024-09-12T11:13:47Z
- **Merged**: 2024-09-12T11:13:12Z
- **Closed**: 2024-09-12T11:13:12Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus)
- **Changed files**: 7   **+810 / -20**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add delete slurm command on kjobctl.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-20T12:45:17Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-20T12:45:32Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 84aa4a25fb5147369bcbb380ddd8058210987879 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e2c9e94804960008d7ca84 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2863--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2863--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI4NjMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.uvb6l2zW0Eru64k_fJZBfTcfZFKq4skpeNwOYMoEIpU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-20T12:50:00Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-20T12:50:30Z

/cc @IrvingMg

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-27T18:01:57Z

/cc @mwielgus

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T10:23:25Z

/cc @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-12T11:11:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2863#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2863#pullrequestreview-2299983600" title="Approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [trasc]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-12T11:11:28Z

LGTM label has been added.  <details>Git tree hash: 24735cf1e959a52cede315f852910717a3d200fb</details>

## Reviews

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-09-04T08:06:06Z

- **cmd/experimental/kjobctl/pkg/cmd/delete/delete_slurm.go:160** by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-04T08:05:22Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delete
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	k8s "k8s.io/client-go/kubernetes"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/apis/v1alpha1"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/completion"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/util"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+)
+
+const (
+	slurmExample = `  # Delete Slurm 
+  kjobctl delete slurm my-application-profile-slurm-k2wzd`
+)
+
+type SlurmOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	JobNames  []string
+	Namespace string
+
+	CascadeStrategy metav1.DeletionPropagation
+	DryRunStrategy  util.DryRunStrategy
+
+	Clientset k8s.Interface
+
+	PrintObj printers.ResourcePrinterFunc
+
+	genericiooptions.IOStreams
+}
+
+func NewSlurmOptions(streams genericiooptions.IOStreams) *SlurmOptions {
+	return &SlurmOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("deleted").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewSlurmCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewSlurmOptions(streams)
+
+	cmd := &cobra.Command{
+		Use:                   "slurm NAME [--cascade STRATEGY] [--dry-run STRATEGY]",
+		DisableFlagsInUseLine: true,
+		Short:                 "Delete Slurm",
+		Example:               slurmExample,
+		Args:                  cobra.MinimumNArgs(1),
+		ValidArgsFunction:     completion.JobNameFunc(clientGetter, v1alpha1.SlurmMode),
+		RunE: func(cmd *cobra.Command, args []string) error {
+			cmd.SilenceUsage = true
+
+			err := o.Complete(clientGetter, cmd, args)
+			if err != nil {
+				return err
+			}
+
+			return o.Run(cmd.Context())
+		},
+	}
+
+	addCascadingFlag(cmd)
+	util.AddDryRunFlag(cmd)
+
+	o.PrintFlags.AddFlags(cmd)
+
+	return cmd
+}
+
+func (o *SlurmOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	o.JobNames = args
+
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.DryRunStrategy, err = util.GetDryRunStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	err = util.PrintFlagsWithDryRunStrategy(o.PrintFlags, o.DryRunStrategy)
+	if err != nil {
+		return err
+	}
+
+	o.CascadeStrategy, err = getCascadingStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.PrintFlags.ToPrinter()
+	if err != nil {
+		return err
+	}
+
+	o.PrintObj = printer.PrintObj
+
+	o.Clientset, err = clientGetter.K8sClientset()
+	if err != nil {
+		return err
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) Run(ctx context.Context) error {
+	for _, jobName := range o.JobNames {
+		if err := o.deleteJob(ctx, jobName); err != nil {
+			return err
+		}
+		if err := o.deleteConfigMap(ctx, jobName); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) deleteJob(ctx context.Context, jobName string) error {
+	job, err := o.Clientset.BatchV1().Jobs(o.Namespace).Get(ctx, jobName, metav1.GetOptions{})
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err != nil {
+		fmt.Fprintln(o.ErrOut, err)
+		return nil
+	}
+	if _, ok := job.Labels[constants.ProfileLabel]; !ok {
+		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not created via kjob\n", job.Name)
```

  
  
  ```suggestion
  		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not owned by kjobctl\n", job.Name)
  ```
  Nit: Maybe like this?

- **cmd/experimental/kjobctl/pkg/cmd/delete/delete_slurm.go:164** by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-04T08:05:36Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delete
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	k8s "k8s.io/client-go/kubernetes"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/apis/v1alpha1"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/completion"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/util"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+)
+
+const (
+	slurmExample = `  # Delete Slurm 
+  kjobctl delete slurm my-application-profile-slurm-k2wzd`
+)
+
+type SlurmOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	JobNames  []string
+	Namespace string
+
+	CascadeStrategy metav1.DeletionPropagation
+	DryRunStrategy  util.DryRunStrategy
+
+	Clientset k8s.Interface
+
+	PrintObj printers.ResourcePrinterFunc
+
+	genericiooptions.IOStreams
+}
+
+func NewSlurmOptions(streams genericiooptions.IOStreams) *SlurmOptions {
+	return &SlurmOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("deleted").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewSlurmCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewSlurmOptions(streams)
+
+	cmd := &cobra.Command{
+		Use:                   "slurm NAME [--cascade STRATEGY] [--dry-run STRATEGY]",
+		DisableFlagsInUseLine: true,
+		Short:                 "Delete Slurm",
+		Example:               slurmExample,
+		Args:                  cobra.MinimumNArgs(1),
+		ValidArgsFunction:     completion.JobNameFunc(clientGetter, v1alpha1.SlurmMode),
+		RunE: func(cmd *cobra.Command, args []string) error {
+			cmd.SilenceUsage = true
+
+			err := o.Complete(clientGetter, cmd, args)
+			if err != nil {
+				return err
+			}
+
+			return o.Run(cmd.Context())
+		},
+	}
+
+	addCascadingFlag(cmd)
+	util.AddDryRunFlag(cmd)
+
+	o.PrintFlags.AddFlags(cmd)
+
+	return cmd
+}
+
+func (o *SlurmOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	o.JobNames = args
+
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.DryRunStrategy, err = util.GetDryRunStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	err = util.PrintFlagsWithDryRunStrategy(o.PrintFlags, o.DryRunStrategy)
+	if err != nil {
+		return err
+	}
+
+	o.CascadeStrategy, err = getCascadingStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.PrintFlags.ToPrinter()
+	if err != nil {
+		return err
+	}
+
+	o.PrintObj = printer.PrintObj
+
+	o.Clientset, err = clientGetter.K8sClientset()
+	if err != nil {
+		return err
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) Run(ctx context.Context) error {
+	for _, jobName := range o.JobNames {
+		if err := o.deleteJob(ctx, jobName); err != nil {
+			return err
+		}
+		if err := o.deleteConfigMap(ctx, jobName); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) deleteJob(ctx context.Context, jobName string) error {
+	job, err := o.Clientset.BatchV1().Jobs(o.Namespace).Get(ctx, jobName, metav1.GetOptions{})
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err != nil {
+		fmt.Fprintln(o.ErrOut, err)
+		return nil
+	}
+	if _, ok := job.Labels[constants.ProfileLabel]; !ok {
+		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not created via kjob\n", job.Name)
+		return nil
+	}
+	if job.Labels[constants.ModeLabel] != string(v1alpha1.SlurmMode) {
+		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not used for Slurm mode\n", job.Name)
```

  ```suggestion
  		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" created in \"%s\" mode. Switch to the correct mode to delete it\n", job.Name, job.Labels[constants.ModeLabel])
  ```
  Nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-04T08:16:42Z

- **cmd/experimental/kjobctl/pkg/cmd/delete/delete_slurm.go:160** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-04T08:16:42Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delete
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	k8s "k8s.io/client-go/kubernetes"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/apis/v1alpha1"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/completion"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/util"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+)
+
+const (
+	slurmExample = `  # Delete Slurm 
+  kjobctl delete slurm my-application-profile-slurm-k2wzd`
+)
+
+type SlurmOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	JobNames  []string
+	Namespace string
+
+	CascadeStrategy metav1.DeletionPropagation
+	DryRunStrategy  util.DryRunStrategy
+
+	Clientset k8s.Interface
+
+	PrintObj printers.ResourcePrinterFunc
+
+	genericiooptions.IOStreams
+}
+
+func NewSlurmOptions(streams genericiooptions.IOStreams) *SlurmOptions {
+	return &SlurmOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("deleted").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewSlurmCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewSlurmOptions(streams)
+
+	cmd := &cobra.Command{
+		Use:                   "slurm NAME [--cascade STRATEGY] [--dry-run STRATEGY]",
+		DisableFlagsInUseLine: true,
+		Short:                 "Delete Slurm",
+		Example:               slurmExample,
+		Args:                  cobra.MinimumNArgs(1),
+		ValidArgsFunction:     completion.JobNameFunc(clientGetter, v1alpha1.SlurmMode),
+		RunE: func(cmd *cobra.Command, args []string) error {
+			cmd.SilenceUsage = true
+
+			err := o.Complete(clientGetter, cmd, args)
+			if err != nil {
+				return err
+			}
+
+			return o.Run(cmd.Context())
+		},
+	}
+
+	addCascadingFlag(cmd)
+	util.AddDryRunFlag(cmd)
+
+	o.PrintFlags.AddFlags(cmd)
+
+	return cmd
+}
+
+func (o *SlurmOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	o.JobNames = args
+
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.DryRunStrategy, err = util.GetDryRunStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	err = util.PrintFlagsWithDryRunStrategy(o.PrintFlags, o.DryRunStrategy)
+	if err != nil {
+		return err
+	}
+
+	o.CascadeStrategy, err = getCascadingStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.PrintFlags.ToPrinter()
+	if err != nil {
+		return err
+	}
+
+	o.PrintObj = printer.PrintObj
+
+	o.Clientset, err = clientGetter.K8sClientset()
+	if err != nil {
+		return err
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) Run(ctx context.Context) error {
+	for _, jobName := range o.JobNames {
+		if err := o.deleteJob(ctx, jobName); err != nil {
+			return err
+		}
+		if err := o.deleteConfigMap(ctx, jobName); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) deleteJob(ctx context.Context, jobName string) error {
+	job, err := o.Clientset.BatchV1().Jobs(o.Namespace).Get(ctx, jobName, metav1.GetOptions{})
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err != nil {
+		fmt.Fprintln(o.ErrOut, err)
+		return nil
+	}
+	if _, ok := job.Labels[constants.ProfileLabel]; !ok {
+		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not created via kjob\n", job.Name)
```

  We already discussed it [here](https://github.com/kubernetes-sigs/kueue/pull/2839#discussion_r1722277918). I would like it to be as in other places. Or we can create follow-up for this case? WDYT?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-09-04T08:22:01Z

- **cmd/experimental/kjobctl/pkg/cmd/delete/delete_slurm.go:160** by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-04T08:22:01Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delete
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	k8s "k8s.io/client-go/kubernetes"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/apis/v1alpha1"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/completion"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/util"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+)
+
+const (
+	slurmExample = `  # Delete Slurm 
+  kjobctl delete slurm my-application-profile-slurm-k2wzd`
+)
+
+type SlurmOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	JobNames  []string
+	Namespace string
+
+	CascadeStrategy metav1.DeletionPropagation
+	DryRunStrategy  util.DryRunStrategy
+
+	Clientset k8s.Interface
+
+	PrintObj printers.ResourcePrinterFunc
+
+	genericiooptions.IOStreams
+}
+
+func NewSlurmOptions(streams genericiooptions.IOStreams) *SlurmOptions {
+	return &SlurmOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("deleted").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewSlurmCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewSlurmOptions(streams)
+
+	cmd := &cobra.Command{
+		Use:                   "slurm NAME [--cascade STRATEGY] [--dry-run STRATEGY]",
+		DisableFlagsInUseLine: true,
+		Short:                 "Delete Slurm",
+		Example:               slurmExample,
+		Args:                  cobra.MinimumNArgs(1),
+		ValidArgsFunction:     completion.JobNameFunc(clientGetter, v1alpha1.SlurmMode),
+		RunE: func(cmd *cobra.Command, args []string) error {
+			cmd.SilenceUsage = true
+
+			err := o.Complete(clientGetter, cmd, args)
+			if err != nil {
+				return err
+			}
+
+			return o.Run(cmd.Context())
+		},
+	}
+
+	addCascadingFlag(cmd)
+	util.AddDryRunFlag(cmd)
+
+	o.PrintFlags.AddFlags(cmd)
+
+	return cmd
+}
+
+func (o *SlurmOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	o.JobNames = args
+
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.DryRunStrategy, err = util.GetDryRunStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	err = util.PrintFlagsWithDryRunStrategy(o.PrintFlags, o.DryRunStrategy)
+	if err != nil {
+		return err
+	}
+
+	o.CascadeStrategy, err = getCascadingStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.PrintFlags.ToPrinter()
+	if err != nil {
+		return err
+	}
+
+	o.PrintObj = printer.PrintObj
+
+	o.Clientset, err = clientGetter.K8sClientset()
+	if err != nil {
+		return err
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) Run(ctx context.Context) error {
+	for _, jobName := range o.JobNames {
+		if err := o.deleteJob(ctx, jobName); err != nil {
+			return err
+		}
+		if err := o.deleteConfigMap(ctx, jobName); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) deleteJob(ctx context.Context, jobName string) error {
+	job, err := o.Clientset.BatchV1().Jobs(o.Namespace).Get(ctx, jobName, metav1.GetOptions{})
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err != nil {
+		fmt.Fprintln(o.ErrOut, err)
+		return nil
+	}
+	if _, ok := job.Labels[constants.ProfileLabel]; !ok {
+		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not created via kjob\n", job.Name)
```

  I see. Then it's fine, let's keep it as is :)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-04T08:47:03Z

- **cmd/experimental/kjobctl/pkg/cmd/delete/delete_slurm.go:164** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-04T08:47:02Z
```diff
@@ -0,0 +1,218 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package delete
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	k8s "k8s.io/client-go/kubernetes"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/apis/v1alpha1"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/completion"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/cmd/util"
+	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
+)
+
+const (
+	slurmExample = `  # Delete Slurm 
+  kjobctl delete slurm my-application-profile-slurm-k2wzd`
+)
+
+type SlurmOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	JobNames  []string
+	Namespace string
+
+	CascadeStrategy metav1.DeletionPropagation
+	DryRunStrategy  util.DryRunStrategy
+
+	Clientset k8s.Interface
+
+	PrintObj printers.ResourcePrinterFunc
+
+	genericiooptions.IOStreams
+}
+
+func NewSlurmOptions(streams genericiooptions.IOStreams) *SlurmOptions {
+	return &SlurmOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("deleted").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewSlurmCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewSlurmOptions(streams)
+
+	cmd := &cobra.Command{
+		Use:                   "slurm NAME [--cascade STRATEGY] [--dry-run STRATEGY]",
+		DisableFlagsInUseLine: true,
+		Short:                 "Delete Slurm",
+		Example:               slurmExample,
+		Args:                  cobra.MinimumNArgs(1),
+		ValidArgsFunction:     completion.JobNameFunc(clientGetter, v1alpha1.SlurmMode),
+		RunE: func(cmd *cobra.Command, args []string) error {
+			cmd.SilenceUsage = true
+
+			err := o.Complete(clientGetter, cmd, args)
+			if err != nil {
+				return err
+			}
+
+			return o.Run(cmd.Context())
+		},
+	}
+
+	addCascadingFlag(cmd)
+	util.AddDryRunFlag(cmd)
+
+	o.PrintFlags.AddFlags(cmd)
+
+	return cmd
+}
+
+func (o *SlurmOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	o.JobNames = args
+
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.DryRunStrategy, err = util.GetDryRunStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	err = util.PrintFlagsWithDryRunStrategy(o.PrintFlags, o.DryRunStrategy)
+	if err != nil {
+		return err
+	}
+
+	o.CascadeStrategy, err = getCascadingStrategy(cmd)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.PrintFlags.ToPrinter()
+	if err != nil {
+		return err
+	}
+
+	o.PrintObj = printer.PrintObj
+
+	o.Clientset, err = clientGetter.K8sClientset()
+	if err != nil {
+		return err
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) Run(ctx context.Context) error {
+	for _, jobName := range o.JobNames {
+		if err := o.deleteJob(ctx, jobName); err != nil {
+			return err
+		}
+		if err := o.deleteConfigMap(ctx, jobName); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (o *SlurmOptions) deleteJob(ctx context.Context, jobName string) error {
+	job, err := o.Clientset.BatchV1().Jobs(o.Namespace).Get(ctx, jobName, metav1.GetOptions{})
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err != nil {
+		fmt.Fprintln(o.ErrOut, err)
+		return nil
+	}
+	if _, ok := job.Labels[constants.ProfileLabel]; !ok {
+		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not created via kjob\n", job.Name)
+		return nil
+	}
+	if job.Labels[constants.ModeLabel] != string(v1alpha1.SlurmMode) {
+		fmt.Fprintf(o.ErrOut, "jobs.batch \"%s\" not used for Slurm mode\n", job.Name)
```

  Fixed on 244bf4c.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-12T09:41:11Z

- **cmd/experimental/kjobctl/pkg/builder/ray_cluster_builder_test.go:201** by [@trasc](https://github.com/trasc) — 2024-09-12T09:37:11Z
```diff
@@ -196,7 +197,8 @@ func TestRayClusterBuilder(t *testing.T) {
 				wrappers.MakeRayCluster("", metav1.NamespaceDefault).GenerateName("profile-raycluster-").
 					Annotation("foo", "baz").
 					Label("foo", "bar").
-					Label(constants.ProfileLabel, "profile").
+					Profile("profile").
+					Mode(v1alpha1.RayClusterMode).
```

  Can we make set `v1alpha1.RayClusterMode` as default in `wrappers.MakeRayCluster`?, (the same goes for the other job types).

- **cmd/experimental/kjobctl/pkg/constants/constants.go:21** by [@trasc](https://github.com/trasc) — 2024-09-12T09:41:03Z
```diff
@@ -18,6 +18,7 @@ package constants
 
 const (
 	ProfileLabel = "kjobctl.x-k8s.io/profile"
+	ModeLabel    = "kjobctl.x-k8s.io/mode"
```

  Can we just have this for slurm related jobs and therefore reduce the impact on the other kjob modes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-12T09:48:02Z

- **cmd/experimental/kjobctl/pkg/constants/constants.go:21** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T09:48:02Z
```diff
@@ -18,6 +18,7 @@ package constants
 
 const (
 	ProfileLabel = "kjobctl.x-k8s.io/profile"
+	ModeLabel    = "kjobctl.x-k8s.io/mode"
```

  There are `Job` and `Slurm` modes using `batch.Job` object. We need to filter it somehow in both lists, describes and so on. So I think it's better to have the same logic for both.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-12T10:49:45Z

- **cmd/experimental/kjobctl/pkg/builder/ray_cluster_builder_test.go:201** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T10:49:44Z
```diff
@@ -196,7 +197,8 @@ func TestRayClusterBuilder(t *testing.T) {
 				wrappers.MakeRayCluster("", metav1.NamespaceDefault).GenerateName("profile-raycluster-").
 					Annotation("foo", "baz").
 					Label("foo", "bar").
-					Label(constants.ProfileLabel, "profile").
+					Profile("profile").
+					Mode(v1alpha1.RayClusterMode).
```

  We have some places where we are using object without `Mode` at all. And also for `Job` and `Slurm` we are using same `JobWrapper`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-12T10:50:02Z

- **cmd/experimental/kjobctl/pkg/constants/constants.go:21** by [@trasc](https://github.com/trasc) — 2024-09-12T10:50:02Z
```diff
@@ -18,6 +18,7 @@ package constants
 
 const (
 	ProfileLabel = "kjobctl.x-k8s.io/profile"
+	ModeLabel    = "kjobctl.x-k8s.io/mode"
```

  I'm thinking of setting the label only when it's a slurm job, otherwise we consider it a standalone job.
  
  This way we don't need to do anything for standalone jobs, Ray modes and so on.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-12T10:56:14Z

- **cmd/experimental/kjobctl/pkg/constants/constants.go:21** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T10:56:14Z
```diff
@@ -18,6 +18,7 @@ package constants
 
 const (
 	ProfileLabel = "kjobctl.x-k8s.io/profile"
+	ModeLabel    = "kjobctl.x-k8s.io/mode"
```

  But what if in the future we add more modes that will overlap with other types?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-12T11:00:18Z

- **cmd/experimental/kjobctl/pkg/constants/constants.go:21** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T11:00:18Z
```diff
@@ -18,6 +18,7 @@ package constants
 
 const (
 	ProfileLabel = "kjobctl.x-k8s.io/profile"
+	ModeLabel    = "kjobctl.x-k8s.io/mode"
```

  I also can see that this changes already merged https://github.com/kubernetes-sigs/kueue/blob/main/cmd/experimental/kjobctl/pkg/constants/constants.go#L21 :) I think I need to rebase it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-12T11:05:34Z

- **cmd/experimental/kjobctl/pkg/constants/constants.go:21** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T11:05:34Z
```diff
@@ -18,6 +18,7 @@ package constants
 
 const (
 	ProfileLabel = "kjobctl.x-k8s.io/profile"
+	ModeLabel    = "kjobctl.x-k8s.io/mode"
```

  It's very strange that git didn't show any conflicts. But maybe it's just because the changes are the same.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-12T11:11:22Z

/lgtm
/approve
