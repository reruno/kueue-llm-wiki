# PR #2873: Fix overadmission after deleting resource from borrowing CQ.

**Summary**: Fix overadmission after deleting resource from borrowing CQ.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2873

**Last updated**: 2024-08-22T19:59:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/overadmission-after-deleting-resource-from-borrowing-cq` → `main`
- **Created**: 2024-08-21T15:04:11Z
- **Updated**: 2024-08-22T19:59:27Z
- **Merged**: 2024-08-22T18:10:07Z
- **Closed**: 2024-08-22T18:10:07Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@trasc](https://github.com/trasc)
- **Changed files**: 6   **+495 / -363**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fix overadmission after deleting resource from borrowing CQ.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2678

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix over-admission after deleting resources from borrowing ClusterQueue.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-21T15:04:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | afa1c56680fc4269f2bb9948f23407bbeef751a1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66c77a6988f4e600087ffc39 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-21T15:06:13Z

/assign @gabesaba

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T00:14:00Z

/test all

Let me check if the prow job will be started here.

https://github.com/kubernetes-sigs/kueue/pull/2849#issuecomment-2303328536

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T00:16:01Z

As we can see here, no prow jobs were triggered.

https://prow.k8s.io/job-history/gs/kubernetes-jenkins/pr-logs/directory/pull-kueue-build-image-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T00:26:50Z

> As we can see here, no prow jobs were triggered.
> 
> https://prow.k8s.io/job-history/gs/kubernetes-jenkins/pr-logs/directory/pull-kueue-build-image-main

I guess that this problem happend by "Aug 21, 2024: Prow is being migrated on Wednesday, August 21! Expect CI disruption and avoid releases that week. ([details](https://groups.google.com/a/kubernetes.io/g/dev/c/qzNYpcN5la4))".

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-22T12:33:03Z

/lgtm

thanks for the fix :)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-22T12:33:08Z

LGTM label has been added.  <details>Git tree hash: b6c2b89e6ea5a8367acff7b3b3bdc8de54c09af9</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-22T15:11:59Z

/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-22T18:01:40Z

LGTM label has been added.  <details>Git tree hash: 46f2034fa81410daae4168fcfdb97bc6e6fcebf4</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-22T18:01:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2873#pullrequestreview-2255315985" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2873#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T19:56:49Z

/release-note-edit

```release-note
Fix over-admission after deleting resources from borrowing ClusterQueue. 
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T19:57:02Z

/cherry-pick release-0.8

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-08-22T19:57:41Z

@tenzen-y: #2873 failed to apply on top of branch "release-0.8":
```
Applying: Fixed overadmission after deleting resource from borrowing CQ.
Using index info to reconstruct a base tree...
M	pkg/cache/cache_test.go
M	pkg/cache/clusterqueue.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/clusterqueue.go
Auto-merging pkg/cache/cache_test.go
CONFLICT (content): Merge conflict in pkg/cache/cache_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0001 Fixed overadmission after deleting resource from borrowing CQ.
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2873#issuecomment-2305525264):

>/cherry-pick release-0.8


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T19:59:25Z

@mbobrovskyi, Could you take a look at the cherry-pick?

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-22T08:30:13Z

- **pkg/cache/clusterqueue.go:235** by [@gabesaba](https://github.com/gabesaba) — 2024-08-22T08:25:55Z
```diff
@@ -225,14 +225,24 @@ func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 
 func filterFlavorQuantities(orig resources.FlavorResourceQuantities, resourceGroups []kueue.ResourceGroup) resources.FlavorResourceQuantities {
 	ret := make(resources.FlavorResourceQuantities, len(orig))
+
+	for key, value := range orig {
+		if value > 0 {
+			ret[key] = value
+		}
+	}
+
 	for _, rg := range resourceGroups {
```

  We shouldn't need to differentiate between zero value and absent value for `usage`. Can we delete this logic, and rename the function to something like "filterZeros"? The only annoying part is this requires test updates...
  
  Furthermore, I'm tempted to delete this function altogether [1] - and be okay leaking memory for a few int64s. Thoughts? @mbobrovskyi @tenzen-y 
  
  [1] and also update newClusterQueue to allocate these maps, as we rely on this function for allocating the maps

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-22T08:56:59Z

- **pkg/cache/clusterqueue.go:235** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-22T08:56:59Z
```diff
@@ -225,14 +225,24 @@ func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 
 func filterFlavorQuantities(orig resources.FlavorResourceQuantities, resourceGroups []kueue.ResourceGroup) resources.FlavorResourceQuantities {
 	ret := make(resources.FlavorResourceQuantities, len(orig))
+
+	for key, value := range orig {
+		if value > 0 {
+			ret[key] = value
+		}
+	}
+
 	for _, rg := range resourceGroups {
```

  > Furthermore, I'm tempted to delete this function altogether [1] - and be okay leaking memory for a few int64s. 
  
  I thought about it too. Just don't know is it using keys somewhere. Will check.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-22T10:20:46Z

- **pkg/cache/clusterqueue.go:235** by [@gabesaba](https://github.com/gabesaba) — 2024-08-22T10:20:46Z
```diff
@@ -225,14 +225,24 @@ func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 
 func filterFlavorQuantities(orig resources.FlavorResourceQuantities, resourceGroups []kueue.ResourceGroup) resources.FlavorResourceQuantities {
 	ret := make(resources.FlavorResourceQuantities, len(orig))
+
+	for key, value := range orig {
+		if value > 0 {
+			ret[key] = value
+		}
+	}
+
 	for _, rg := range resourceGroups {
```

  I took a quick look before I left the comment above, and didn't see any

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-22T10:26:53Z

- **pkg/cache/clusterqueue.go:235** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-22T10:26:53Z
```diff
@@ -225,14 +225,24 @@ func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 
 func filterFlavorQuantities(orig resources.FlavorResourceQuantities, resourceGroups []kueue.ResourceGroup) resources.FlavorResourceQuantities {
 	ret := make(resources.FlavorResourceQuantities, len(orig))
+
+	for key, value := range orig {
+		if value > 0 {
+			ret[key] = value
+		}
+	}
+
 	for _, rg := range resourceGroups {
```

  I removed `filterLocalQueueUsage` and fixed tests. Looks like working fine. Thanks.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-22T15:57:05Z

- **pkg/cache/cache_test.go:114** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T15:53:57Z
```diff
@@ -111,30 +111,22 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 					AllocatableResourceGeneration: 1,
 					NamespaceSelector:             labels.Nothing(),
 					FlavorFungibility:             defaultFlavorFungibility,
-					Usage: resources.FlavorResourceQuantities{
-						{Flavor: "default", Resource: corev1.ResourceCPU}: 0,
-					},
-					AdmittedUsage: resources.FlavorResourceQuantities{
-						{Flavor: "default", Resource: corev1.ResourceCPU}: 0,
-					},
-					Status:     active,
-					Preemption: defaultPreemption,
-					FairWeight: oneQuantity,
+					Usage:                         resources.FlavorResourceQuantities{},
```

  you can use `cmpopts.EquateEmpty` to avoid having to define these empty maps.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-22T16:12:20Z

- **pkg/cache/clusterqueue.go:235** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T16:12:20Z
```diff
@@ -225,14 +225,24 @@ func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 
 func filterFlavorQuantities(orig resources.FlavorResourceQuantities, resourceGroups []kueue.ResourceGroup) resources.FlavorResourceQuantities {
 	ret := make(resources.FlavorResourceQuantities, len(orig))
+
+	for key, value := range orig {
+		if value > 0 {
+			ret[key] = value
+		}
+	}
+
 	for _, rg := range resourceGroups {
```

  Looking great approach without the `filterLocalQueueUsage`.
  Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-22T17:53:31Z

- **pkg/cache/cache_test.go:114** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-22T17:53:30Z
```diff
@@ -111,30 +111,22 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 					AllocatableResourceGeneration: 1,
 					NamespaceSelector:             labels.Nothing(),
 					FlavorFungibility:             defaultFlavorFungibility,
-					Usage: resources.FlavorResourceQuantities{
-						{Flavor: "default", Resource: corev1.ResourceCPU}: 0,
-					},
-					AdmittedUsage: resources.FlavorResourceQuantities{
-						{Flavor: "default", Resource: corev1.ResourceCPU}: 0,
-					},
-					Status:     active,
-					Preemption: defaultPreemption,
-					FairWeight: oneQuantity,
+					Usage:                         resources.FlavorResourceQuantities{},
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-22T18:01:35Z

/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-22T18:06:36Z

- **test/integration/scheduler/scheduler_test.go:2314** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T18:06:32Z
```diff
@@ -2207,4 +2209,137 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Deleting resources from borrowing clusterQueue", func() {
+		var (
+			cq1 *kueue.ClusterQueue
+			cq2 *kueue.ClusterQueue
+			lq1 *kueue.LocalQueue
+			lq2 *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).To(gomega.Succeed())
+
+			cq1 = testing.MakeClusterQueue("cq1").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq1)).Should(gomega.Succeed())
+
+			cq2 = testing.MakeClusterQueue("cq2").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq2)).Should(gomega.Succeed())
+
+			lq1 = testing.MakeLocalQueue("lq1", ns.Name).ClusterQueue(cq1.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq1)).Should(gomega.Succeed())
+
+			lq2 = testing.MakeLocalQueue("lq2", ns.Name).ClusterQueue(cq2.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq2)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq1, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq2, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
+		})
+
+		ginkgo.It("shouldn't admit second workload on over admission", func() {
+			ginkgo.By("creating first workload")
+			wl1 := testing.MakeWorkload("wl1", ns.Name).
+				Queue(lq1.Name).
+				Request(corev1.ResourceCPU, "2").
+				Request(corev1.ResourceMemory, "2Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+
+			ginkgo.By("creating second workload")
+			wl2 := testing.MakeWorkload("wl2", ns.Name).
+				Queue(lq2.Name).
+				Request(corev1.ResourceCPU, "1").
+				Request(corev1.ResourceMemory, "1Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			ginkgo.By("checking the first workload is admitted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).Should(gomega.Succeed())
+				g.Expect(wl1.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Quota reserved in ClusterQueue %s", cq1.Name),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadAdmitted,
+						Message: "The workload is admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).Should(gomega.Succeed())
+				g.Expect(wl2.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "couldn't assign flavors to pod set main: insufficient unused quota in cohort for memory in flavor on-demand, 1Gi more needed",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("removing memory resources from first cluster queue")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq1), cq1)).Should(gomega.Succeed())
+				cq1.Spec.ResourceGroups = nil
+				cq1Wrapper := &testing.ClusterQueueWrapper{ClusterQueue: *cq1}
+				cq1Wrapper.ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Obj(),
+				)
+				cq1 = cq1Wrapper.Obj()
+				g.Expect(k8sClient.Update(ctx, cq1)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
```

  @mbobrovskyi My question is what happens if the kueue-manager is restarted after the CQ resources are reduced?
  
  I guess that the overadmission will happen because the kueue-manager doesn't know the previous CQ resourceGroup parameters, right?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-22T18:16:16Z

- **test/integration/scheduler/scheduler_test.go:2314** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-22T18:16:16Z
```diff
@@ -2207,4 +2209,137 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Deleting resources from borrowing clusterQueue", func() {
+		var (
+			cq1 *kueue.ClusterQueue
+			cq2 *kueue.ClusterQueue
+			lq1 *kueue.LocalQueue
+			lq2 *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).To(gomega.Succeed())
+
+			cq1 = testing.MakeClusterQueue("cq1").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq1)).Should(gomega.Succeed())
+
+			cq2 = testing.MakeClusterQueue("cq2").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq2)).Should(gomega.Succeed())
+
+			lq1 = testing.MakeLocalQueue("lq1", ns.Name).ClusterQueue(cq1.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq1)).Should(gomega.Succeed())
+
+			lq2 = testing.MakeLocalQueue("lq2", ns.Name).ClusterQueue(cq2.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq2)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq1, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq2, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
+		})
+
+		ginkgo.It("shouldn't admit second workload on over admission", func() {
+			ginkgo.By("creating first workload")
+			wl1 := testing.MakeWorkload("wl1", ns.Name).
+				Queue(lq1.Name).
+				Request(corev1.ResourceCPU, "2").
+				Request(corev1.ResourceMemory, "2Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+
+			ginkgo.By("creating second workload")
+			wl2 := testing.MakeWorkload("wl2", ns.Name).
+				Queue(lq2.Name).
+				Request(corev1.ResourceCPU, "1").
+				Request(corev1.ResourceMemory, "1Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			ginkgo.By("checking the first workload is admitted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).Should(gomega.Succeed())
+				g.Expect(wl1.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Quota reserved in ClusterQueue %s", cq1.Name),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadAdmitted,
+						Message: "The workload is admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).Should(gomega.Succeed())
+				g.Expect(wl2.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "couldn't assign flavors to pod set main: insufficient unused quota in cohort for memory in flavor on-demand, 1Gi more needed",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("removing memory resources from first cluster queue")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq1), cq1)).Should(gomega.Succeed())
+				cq1.Spec.ResourceGroups = nil
+				cq1Wrapper := &testing.ClusterQueueWrapper{ClusterQueue: *cq1}
+				cq1Wrapper.ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Obj(),
+				)
+				cq1 = cq1Wrapper.Obj()
+				g.Expect(k8sClient.Update(ctx, cq1)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
```

  I don't think so, because we would still load the Workload objects into the cache and keep the usage for them

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-22T18:23:57Z

- **test/integration/scheduler/scheduler_test.go:2314** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T18:23:57Z
```diff
@@ -2207,4 +2209,137 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Deleting resources from borrowing clusterQueue", func() {
+		var (
+			cq1 *kueue.ClusterQueue
+			cq2 *kueue.ClusterQueue
+			lq1 *kueue.LocalQueue
+			lq2 *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).To(gomega.Succeed())
+
+			cq1 = testing.MakeClusterQueue("cq1").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq1)).Should(gomega.Succeed())
+
+			cq2 = testing.MakeClusterQueue("cq2").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq2)).Should(gomega.Succeed())
+
+			lq1 = testing.MakeLocalQueue("lq1", ns.Name).ClusterQueue(cq1.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq1)).Should(gomega.Succeed())
+
+			lq2 = testing.MakeLocalQueue("lq2", ns.Name).ClusterQueue(cq2.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq2)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq1, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq2, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
+		})
+
+		ginkgo.It("shouldn't admit second workload on over admission", func() {
+			ginkgo.By("creating first workload")
+			wl1 := testing.MakeWorkload("wl1", ns.Name).
+				Queue(lq1.Name).
+				Request(corev1.ResourceCPU, "2").
+				Request(corev1.ResourceMemory, "2Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+
+			ginkgo.By("creating second workload")
+			wl2 := testing.MakeWorkload("wl2", ns.Name).
+				Queue(lq2.Name).
+				Request(corev1.ResourceCPU, "1").
+				Request(corev1.ResourceMemory, "1Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			ginkgo.By("checking the first workload is admitted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).Should(gomega.Succeed())
+				g.Expect(wl1.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Quota reserved in ClusterQueue %s", cq1.Name),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadAdmitted,
+						Message: "The workload is admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).Should(gomega.Succeed())
+				g.Expect(wl2.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "couldn't assign flavors to pod set main: insufficient unused quota in cohort for memory in flavor on-demand, 1Gi more needed",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("removing memory resources from first cluster queue")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq1), cq1)).Should(gomega.Succeed())
+				cq1.Spec.ResourceGroups = nil
+				cq1Wrapper := &testing.ClusterQueueWrapper{ClusterQueue: *cq1}
+				cq1Wrapper.ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Obj(),
+				)
+				cq1 = cq1Wrapper.Obj()
+				g.Expect(k8sClient.Update(ctx, cq1)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
```

  Oh, that makes sense. Thank you for the clarifications!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-22T19:47:39Z

- **test/integration/scheduler/scheduler_test.go:2314** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-22T19:47:38Z
```diff
@@ -2207,4 +2209,137 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Deleting resources from borrowing clusterQueue", func() {
+		var (
+			cq1 *kueue.ClusterQueue
+			cq2 *kueue.ClusterQueue
+			lq1 *kueue.LocalQueue
+			lq2 *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).To(gomega.Succeed())
+
+			cq1 = testing.MakeClusterQueue("cq1").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq1)).Should(gomega.Succeed())
+
+			cq2 = testing.MakeClusterQueue("cq2").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq2)).Should(gomega.Succeed())
+
+			lq1 = testing.MakeLocalQueue("lq1", ns.Name).ClusterQueue(cq1.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq1)).Should(gomega.Succeed())
+
+			lq2 = testing.MakeLocalQueue("lq2", ns.Name).ClusterQueue(cq2.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq2)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq1, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq2, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
+		})
+
+		ginkgo.It("shouldn't admit second workload on over admission", func() {
+			ginkgo.By("creating first workload")
+			wl1 := testing.MakeWorkload("wl1", ns.Name).
+				Queue(lq1.Name).
+				Request(corev1.ResourceCPU, "2").
+				Request(corev1.ResourceMemory, "2Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+
+			ginkgo.By("creating second workload")
+			wl2 := testing.MakeWorkload("wl2", ns.Name).
+				Queue(lq2.Name).
+				Request(corev1.ResourceCPU, "1").
+				Request(corev1.ResourceMemory, "1Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			ginkgo.By("checking the first workload is admitted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).Should(gomega.Succeed())
+				g.Expect(wl1.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Quota reserved in ClusterQueue %s", cq1.Name),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadAdmitted,
+						Message: "The workload is admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).Should(gomega.Succeed())
+				g.Expect(wl2.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "couldn't assign flavors to pod set main: insufficient unused quota in cohort for memory in flavor on-demand, 1Gi more needed",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("removing memory resources from first cluster queue")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq1), cq1)).Should(gomega.Succeed())
+				cq1.Spec.ResourceGroups = nil
+				cq1Wrapper := &testing.ClusterQueueWrapper{ClusterQueue: *cq1}
+				cq1Wrapper.ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Obj(),
+				)
+				cq1 = cq1Wrapper.Obj()
+				g.Expect(k8sClient.Update(ctx, cq1)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
```

  I manually tested this case again to make sure it works fine. After restarting, the controller manager is still not admit the workload.
  
  ```
  couldn''t assign flavors to pod set main: insufficient unused quota
          in cohort for memory in flavor fl, 2Gi more needed
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-22T19:54:58Z

- **test/integration/scheduler/scheduler_test.go:2314** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-22T19:54:58Z
```diff
@@ -2207,4 +2209,137 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Deleting resources from borrowing clusterQueue", func() {
+		var (
+			cq1 *kueue.ClusterQueue
+			cq2 *kueue.ClusterQueue
+			lq1 *kueue.LocalQueue
+			lq2 *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).To(gomega.Succeed())
+
+			cq1 = testing.MakeClusterQueue("cq1").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq1)).Should(gomega.Succeed())
+
+			cq2 = testing.MakeClusterQueue("cq2").
+				Cohort("cohort").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "1Gi").
+					Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq2)).Should(gomega.Succeed())
+
+			lq1 = testing.MakeLocalQueue("lq1", ns.Name).ClusterQueue(cq1.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq1)).Should(gomega.Succeed())
+
+			lq2 = testing.MakeLocalQueue("lq2", ns.Name).ClusterQueue(cq2.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq2)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq1, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq2, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
+		})
+
+		ginkgo.It("shouldn't admit second workload on over admission", func() {
+			ginkgo.By("creating first workload")
+			wl1 := testing.MakeWorkload("wl1", ns.Name).
+				Queue(lq1.Name).
+				Request(corev1.ResourceCPU, "2").
+				Request(corev1.ResourceMemory, "2Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+
+			ginkgo.By("creating second workload")
+			wl2 := testing.MakeWorkload("wl2", ns.Name).
+				Queue(lq2.Name).
+				Request(corev1.ResourceCPU, "1").
+				Request(corev1.ResourceMemory, "1Gi").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			ginkgo.By("checking the first workload is admitted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).Should(gomega.Succeed())
+				g.Expect(wl1.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Quota reserved in ClusterQueue %s", cq1.Name),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadAdmitted,
+						Message: "The workload is admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).Should(gomega.Succeed())
+				g.Expect(wl2.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "couldn't assign flavors to pod set main: insufficient unused quota in cohort for memory in flavor on-demand, 1Gi more needed",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("removing memory resources from first cluster queue")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq1), cq1)).Should(gomega.Succeed())
+				cq1.Spec.ResourceGroups = nil
+				cq1Wrapper := &testing.ClusterQueueWrapper{ClusterQueue: *cq1}
+				cq1Wrapper.ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).
+					Resource(corev1.ResourceCPU, "1").
+					Obj(),
+				)
+				cq1 = cq1Wrapper.Obj()
+				g.Expect(k8sClient.Update(ctx, cq1)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
```

  That sounds great!
  Thank you!
