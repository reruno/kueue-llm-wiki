# PR #2880: [Feature] Support Kubeflow MPIJob in MultiKueue

**Summary**: [Feature] Support Kubeflow MPIJob in MultiKueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2880

**Last updated**: 2024-09-13T11:52:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/support-kubeflow-mpijob-in-mk` → `main`
- **Created**: 2024-08-23T11:34:49Z
- **Updated**: 2024-09-13T11:52:37Z
- **Merged**: 2024-09-12T02:03:12Z
- **Closed**: 2024-09-12T02:03:12Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 35   **+804 / -381**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
The PR introduces a new MultiKueue adapter to handle MPIJob (Kubeflow).
We want to extend MultiKueue capabilities to satisfy the needs of early adopters.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2552

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: Support for the Kubeflow MPIJob
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-23T11:34:52Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-23T11:35:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8daa5f7b18041b7c23d9d8d33d2457e10f52cef2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e1f6c187261800080eb51e |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-23T11:35:28Z

/ok-to-test

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-23T13:21:00Z

/retest-required

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-27T07:42:49Z

/retest-required

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-27T08:08:41Z

/test pull-kueue-test-integration-main

Due to https://github.com/kubernetes-sigs/kueue/issues/2901.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-27T09:33:47Z

/retest-required

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-27T11:01:27Z

/retest-required

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-27T11:12:27Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-27T12:25:25Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-27T13:04:13Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-27T15:26:12Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-08-28T07:38:32Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-09-03T11:29:58Z

@mbobrovskyi @alculquicondor @tenzen-y PTAL, I have addressed all comments

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T06:35:14Z

> first pass Please address @mbobrovskyi's comments

Yes, I am doing that but one of the comment suggestion is bit more complicated and I am checking how to approach it - the one with not repeating version in kustomize

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-09-06T14:39:14Z

@alculquicondor I think everything from @mbobrovskyi was addressed

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-09-11T20:19:42Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-11T20:24:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2880#pullrequestreview-2298547842" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2880#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T20:24:56Z

Leaving LGTM to @mbobrovskyi

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T02:02:26Z

/lgtm

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-12T02:02:31Z

LGTM label has been added.  <details>Git tree hash: e6c09ab9b855734fcb0824b61368ab7fe86456f5</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T02:39:16Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-09-12T02:39:54Z

@mbobrovskyi: #2880 failed to apply on top of branch "website":
```
Applying: Introduce MultiKueue adapter for MPIJob
Applying: Add MPIJobs integration tests
Using index info to reconstruct a base tree...
M	test/integration/multikueue/multikueue_test.go
M	test/integration/multikueue/suite_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/multikueue/suite_test.go
CONFLICT (content): Merge conflict in test/integration/multikueue/suite_test.go
Auto-merging test/integration/multikueue/multikueue_test.go
CONFLICT (content): Merge conflict in test/integration/multikueue/multikueue_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0002 Add MPIJobs integration tests
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2880#issuecomment-2345149858):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T07:06:19Z

@mszadkow IIUC we have changes on site. Could you please prepare cherry-pick to website branch?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-13T11:52:36Z

> @mszadkow IIUC we have changes on site. Could you please prepare cherry-pick to website branch?

We should not merge this PR to the website branch since this feature has not been released yet.

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-28T09:04:41Z

- **.gitignore:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T09:02:29Z
```diff
@@ -29,5 +29,6 @@ testbin/*
 
 # Directory with CRDs for dependencies
 dep-crds
+dep-manifests
```

  Do we need this?

- **Makefile-deps.mk:19** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T09:02:39Z
```diff
@@ -16,6 +16,7 @@ PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 TOOLS_DIR := $(PROJECT_DIR)/hack/internal/tools
 BIN_DIR ?= $(PROJECT_DIR)/bin
 EXTERNAL_CRDS_DIR ?= $(PROJECT_DIR)/dep-crds
+EXTERNAL_MANIFESTS_DIR ?= $(PROJECT_DIR)/dep-manifests
```

  And this?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-28T12:31:30Z

- **Makefile-deps.mk:19** by [@mszadkow](https://github.com/mszadkow) — 2024-08-28T12:31:30Z
```diff
@@ -16,6 +16,7 @@ PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 TOOLS_DIR := $(PROJECT_DIR)/hack/internal/tools
 BIN_DIR ?= $(PROJECT_DIR)/bin
 EXTERNAL_CRDS_DIR ?= $(PROJECT_DIR)/dep-crds
+EXTERNAL_MANIFESTS_DIR ?= $(PROJECT_DIR)/dep-manifests
```

  yep, you are right, artifacts from previous approach

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-28T20:35:31Z

- **Makefile-deps.mk:116** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T17:26:02Z
```diff
@@ -112,7 +113,10 @@ KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	# Remove `kubeflow.org_mpijobs.yaml` version v1 from the folder and from the kustomization.yaml, kueue uses v2beta1 CRD from mpi-operator
```

  ```suggestion
  	# Remove `kubeflow.org_mpijobs.yaml` version v1 from the kustomization.yaml, kueue uses v2beta1 CRD from mpi-operator
  ```

- **hack/e2e-common.sh:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T19:52:04Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
```

  What about `yq`? Is it useful here?

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:209** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T20:01:52Z
```diff
@@ -161,3 +205,15 @@ func (j *MPIJobWrapper) Generation(num int64) *MPIJobWrapper {
 	j.ObjectMeta.Generation = num
 	return j
 }
+
+// Condition adds a condition
```

  ```suggestion
  // StatusConditions adds a condition
  ```

- **Makefile-test.mk:63** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T20:09:17Z
```diff
@@ -59,6 +59,8 @@ IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
 # JobSet Version
 JOBSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/jobset)
 KUBEFLOW_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/training-operator)
+# Mismatch between manifest semver tag and docker release tag, latter has no leading 'v'
+KUBEFLOW_MPI_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/mpi-operator | sed 's/^v//')
```

  It's very easy to remove `v` from the string on bash. Can we move this logic to place where we really need it? 
  
  ```bash
  export KUBEFLOW_MPI_IMAGE=mpioperator/mpi-operator:${KUBEFLOW_MPI_VERSION/v}
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T06:48:54Z

- **hack/e2e-common.sh:102** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T06:48:54Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
```

  just a matter of preference it would be something like:
  `yq eval 'del(.resources[] | select(. == "kubeflow.org_mpijobs.yaml"))' $(EXTERNAL_CRDS_DIR)/training-operator/base/crds/kustomization.yaml`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T06:52:58Z

- **hack/e2e-common.sh:83** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T04:29:43Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
```

  This is already implemented in the Makefile-deps.mk, right?

- **hack/e2e-common.sh:30** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T05:18:07Z
```diff
@@ -23,10 +23,15 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
-export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator/
+export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator
+export KUBEFLOW_CRDS_BASE=${KUBEFLOW_CRDS}/base/crds
+export KUBEFLOW_MANIFEST=${KUBEFLOW_CRDS}/overlays/standalone
```

  If I correct understood, standalone overwrite tag to latest. Is it ok for us? Or we need to use specific version?
  
  https://github.com/kubeflow/training-operator/blob/e9766d171507ad3772cc04c740acb4d1ee4c544c/manifests/overlays/standalone/kustomization.yaml#L9

- **hack/e2e-common.sh:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T05:26:11Z
```diff
@@ -23,10 +23,15 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
-export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator/
+export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator
+export KUBEFLOW_CRDS_BASE=${KUBEFLOW_CRDS}/base/crds
+export KUBEFLOW_MANIFEST=${KUBEFLOW_CRDS}/overlays/standalone
+
+export KUBEFLOW_MPI_MANIFEST="https://github.com/kubeflow/mpi-operator/manifests/overlays/standalone?ref=v${KUBEFLOW_MPI_VERSION}"
```

  Shouldn't it be like this:
  
  ```suggestion
  export KUBEFLOW_MPI_MANIFEST="https://raw.githubusercontent.com/kubeflow/mpi-operator/${KUBEFLOW_MPI_VERSION}/deploy/v2beta1/mpi-operator.yaml"
  ```
  
  ?

- **hack/e2e-common.sh:118** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T06:43:54Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
+${ARGS_PATCH}
+EOF
+}
+
 #$1 - cluster name
 function install_kubeflow {
-    cluster_kind_load_image "${1}" ${KUBEFLOW_IMAGE}
+    cluster_kind_load_image "${1}" "${KUBEFLOW_IMAGE}"
     kubectl config use-context "kind-${1}"
     kubectl apply -k "${KUBEFLOW_MANIFEST}"
 }
 
+#$1 - cluster name
+function install_mpi {
+    cluster_kind_load_image "${1}" "${KUBEFLOW_MPI_IMAGE}"
+    kubectl config use-context "kind-${1}"
+    kubectl create -k "${KUBEFLOW_MPI_MANIFEST}"
```

  And also here:
  
  ```suggestion
      kubectl apply --server-side -f "${KUBEFLOW_MPI_MANIFEST}"
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T06:56:08Z

- **Makefile-test.mk:63** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T06:56:08Z
```diff
@@ -59,6 +59,8 @@ IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
 # JobSet Version
 JOBSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/jobset)
 KUBEFLOW_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/training-operator)
+# Mismatch between manifest semver tag and docker release tag, latter has no leading 'v'
+KUBEFLOW_MPI_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/mpi-operator | sed 's/^v//')
```

  Sure, we could

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T06:57:04Z

- **hack/e2e-common.sh:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T06:57:04Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
```

  It might be a good idea to create a custom `kustomization.yaml` file to replace arguments and update the image version without modifying the original files.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T07:00:56Z

- **hack/e2e-common.sh:30** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T07:00:56Z
```diff
@@ -23,10 +23,15 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
-export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator/
+export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator
+export KUBEFLOW_CRDS_BASE=${KUBEFLOW_CRDS}/base/crds
+export KUBEFLOW_MANIFEST=${KUBEFLOW_CRDS}/overlays/standalone
```

  That's correct if you use main branch, but we copy it from the version we have in the go.mod.
  That's the example I have locally:
  ```
  apiVersion: kustomize.config.k8s.io/v1beta1
  kind: Kustomization
  namespace: kubeflow
  resources:
    - ../../base
    - namespace.yaml
  images:
    - name: kubeflow/training-operator
      newTag: "v1-9e52eb7"
  secretGenerator:
    - name: training-operator-webhook-cert
      options:
        disableNameSuffixHash: true
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T07:02:21Z

- **hack/e2e-common.sh:30** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T07:02:21Z
```diff
@@ -23,10 +23,15 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
-export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator/
+export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator
+export KUBEFLOW_CRDS_BASE=${KUBEFLOW_CRDS}/base/crds
+export KUBEFLOW_MANIFEST=${KUBEFLOW_CRDS}/overlays/standalone
```

  Ah, I got. Thanks for explain.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T07:02:25Z

- **hack/e2e-common.sh:32** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T07:02:25Z
```diff
@@ -23,10 +23,15 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
-export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator/
+export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator
+export KUBEFLOW_CRDS_BASE=${KUBEFLOW_CRDS}/base/crds
+export KUBEFLOW_MANIFEST=${KUBEFLOW_CRDS}/overlays/standalone
+
+export KUBEFLOW_MPI_MANIFEST="https://github.com/kubeflow/mpi-operator/manifests/overlays/standalone?ref=v${KUBEFLOW_MPI_VERSION}"
```

  No, when we "install it" with this command:
  `kubectl create -k "${KUBEFLOW_MPI_MANIFEST}"`
  We need to point to the folder

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T07:05:11Z

- **hack/e2e-common.sh:118** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T07:05:11Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
+${ARGS_PATCH}
+EOF
+}
+
 #$1 - cluster name
 function install_kubeflow {
-    cluster_kind_load_image "${1}" ${KUBEFLOW_IMAGE}
+    cluster_kind_load_image "${1}" "${KUBEFLOW_IMAGE}"
     kubectl config use-context "kind-${1}"
     kubectl apply -k "${KUBEFLOW_MANIFEST}"
 }
 
+#$1 - cluster name
+function install_mpi {
+    cluster_kind_load_image "${1}" "${KUBEFLOW_MPI_IMAGE}"
+    kubectl config use-context "kind-${1}"
+    kubectl create -k "${KUBEFLOW_MPI_MANIFEST}"
```

  That would be smooth I agree, however somehow when we apply the annotation section is too big and we face this:
  https://github.com/argoproj/argo-cd/issues/820

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T07:10:27Z

- **hack/e2e-common.sh:118** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T07:10:27Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
+${ARGS_PATCH}
+EOF
+}
+
 #$1 - cluster name
 function install_kubeflow {
-    cluster_kind_load_image "${1}" ${KUBEFLOW_IMAGE}
+    cluster_kind_load_image "${1}" "${KUBEFLOW_IMAGE}"
     kubectl config use-context "kind-${1}"
     kubectl apply -k "${KUBEFLOW_MANIFEST}"
 }
 
+#$1 - cluster name
+function install_mpi {
+    cluster_kind_load_image "${1}" "${KUBEFLOW_MPI_IMAGE}"
+    kubectl config use-context "kind-${1}"
+    kubectl create -k "${KUBEFLOW_MPI_MANIFEST}"
```

  I think with `--server-side` it should work fine.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T08:20:12Z

- **Makefile-test.mk:62** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T07:29:25Z
```diff
@@ -59,6 +59,8 @@ IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
 # JobSet Version
 JOBSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/jobset)
 KUBEFLOW_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/training-operator)
+# Mismatch between manifest semver tag and docker release tag, latter has no leading 'v'
```

  I think now it's not need.

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:65** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T07:42:19Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = corev1.RestartPolicy(rs.RestartPolicy)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.Containers[0].Name = "mpijob"
```

  Why we rename it?

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:56** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T08:17:38Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
```

  ```suggestion
  	RestartPolicy corev1.RestartPolicy
  ```
  ?

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T08:18:00Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = corev1.RestartPolicy(rs.RestartPolicy)
```

  ```suggestion
  		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = rs.RestartPolicy
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T08:38:45Z

- **hack/e2e-common.sh:118** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T08:38:45Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
+${ARGS_PATCH}
+EOF
+}
+
 #$1 - cluster name
 function install_kubeflow {
-    cluster_kind_load_image "${1}" ${KUBEFLOW_IMAGE}
+    cluster_kind_load_image "${1}" "${KUBEFLOW_IMAGE}"
     kubectl config use-context "kind-${1}"
     kubectl apply -k "${KUBEFLOW_MANIFEST}"
 }
 
+#$1 - cluster name
+function install_mpi {
+    cluster_kind_load_image "${1}" "${KUBEFLOW_MPI_IMAGE}"
+    kubectl config use-context "kind-${1}"
+    kubectl create -k "${KUBEFLOW_MPI_MANIFEST}"
```

  I mean I had tried this approach

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T08:39:16Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:63** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T08:38:09Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
```

  Not sure that we need to set template.Name. Maybe like this:
  
  ```suggestion
  		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.Containers[0].Name = rs.Name
  ```
  
  ?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T08:42:19Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:63** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T08:42:19Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
```

  those are different names, one is for the job itself and other for containers, which should be equal certain name - 
  [The pod must include a container named tensorflow.](https://www.kubeflow.org/docs/components/training/user-guides/tensorflow/)

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T08:42:27Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:65** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T08:42:27Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = corev1.RestartPolicy(rs.RestartPolicy)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.Containers[0].Name = "mpijob"
```

  the container?
  I read on kubeflow training-operator page that each type should have the containers with specific name.
  I think I even experience the issue when they weren't named properly
  [The pod must include a container named tensorflow.](https://www.kubeflow.org/docs/components/training/user-guides/tensorflow/)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T08:47:09Z

- **hack/e2e-common.sh:118** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T08:47:09Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
+${ARGS_PATCH}
+EOF
+}
+
 #$1 - cluster name
 function install_kubeflow {
-    cluster_kind_load_image "${1}" ${KUBEFLOW_IMAGE}
+    cluster_kind_load_image "${1}" "${KUBEFLOW_IMAGE}"
     kubectl config use-context "kind-${1}"
     kubectl apply -k "${KUBEFLOW_MANIFEST}"
 }
 
+#$1 - cluster name
+function install_mpi {
+    cluster_kind_load_image "${1}" "${KUBEFLOW_MPI_IMAGE}"
+    kubectl config use-context "kind-${1}"
+    kubectl create -k "${KUBEFLOW_MPI_MANIFEST}"
```

  It's very strange. Let me check.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-29T08:54:34Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:65** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-29T08:54:33Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = corev1.RestartPolicy(rs.RestartPolicy)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.Containers[0].Name = "mpijob"
```

  > The pod must include a container named tensorflow.
  
  You mean this one?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T08:57:55Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:65** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T08:57:55Z
```diff
@@ -42,44 +42,88 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy kubeflow.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = corev1.RestartPolicy(rs.RestartPolicy)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.Containers[0].Name = "mpijob"
```

  yes

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-29T10:48:53Z

- **hack/e2e-common.sh:102** by [@mszadkow](https://github.com/mszadkow) — 2024-08-29T10:48:53Z
```diff
@@ -72,13 +77,47 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    sed -i '/kubeflow.org_mpijobs.yaml/d' "${KUBEFLOW_CRDS}/base/crds/kustomization.yaml"
+
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
+EOF
+)
+    # Apply the patch
+    sed -i -e "${INSERT_LINE}r /dev/stdin" "${KUBEFLOW_DEPLOYMENT}" << EOF
```

  it's already copied, I mean it's recommended to apply such patches

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-30T04:13:32Z

- **Makefile-deps.mk:115** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T03:46:35Z
```diff
@@ -112,7 +112,10 @@ KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	# Remove `kubeflow.org_mpijobs.yaml` version v1 from the kustomization.yaml, kueue uses v2beta1 CRD from mpi-operator
```

  Instead of this script, could you use the kustomize delete patch like this?
  
  https://github.com/kubernetes/community/blob/master/contributors/devel/sig-api-machinery/strategic-merge-patch.md#delete-directive
  
  ```yaml
  $patch: delete
  apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    name: mpijobs.kubeflow.org
  ```

- **Makefile-deps.mk:115** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T03:48:53Z
```diff
@@ -112,7 +112,10 @@ KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	# Remove `kubeflow.org_mpijobs.yaml` version v1 from the kustomization.yaml, kueue uses v2beta1 CRD from mpi-operator
```

  I guess that we can kustomization.yaml for TrainigOperator in https://github.com/kubernetes-sigs/kueue/tree/1fc8aa506e0f3e04916b99e4e340d01d51499179/test/e2e/config

- **hack/e2e-common.sh:97** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T03:49:50Z
```diff
@@ -72,13 +77,46 @@ function install_jobset {
     kubectl apply --server-side -f "${JOBSET_MANIFEST}"
 }
 
+function patch_kubeflow_manifest {
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # Done already in Makefile-deps.mk target `kf-training-operator-crd`
+    # 2. Training-operator deployment file is patched and manually enabled for all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+    KUBEFLOW_DEPLOYMENT="${KUBEFLOW_CRDS}/base/deployment.yaml"
+
+    # Find the line after which to insert the args
+    INSERT_LINE=$(grep -n "^ *- /manager" "${KUBEFLOW_DEPLOYMENT}" | head -n 1 | cut -d ':' -f 1)
+
+    # Prepare patch with the args after the specified line
+    # EOF must be indented most left - doesn't work otherwise
+    ARGS_PATCH=$(cat <<EOF
+          args:
+            - --enable-scheme=tfjob
+            - --enable-scheme=pytorchjob
+            - --enable-scheme=xgboostjob
+            - --enable-scheme=paddlejob
```

  As I mentioned above, could we add a kustomization for the training-operator?

- **pkg/controller/jobs/mpijob/mpijob_controller.go:76** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T03:51:18Z
```diff
@@ -72,7 +73,7 @@ func NewJob() jobframework.GenericJob {
 var NewReconciler = jobframework.NewGenericReconcilerFactory(NewJob)
 
 func isMPIJob(owner *metav1.OwnerReference) bool {
-	return owner.Kind == "MPIJob" && strings.HasPrefix(owner.APIVersion, "kubeflow.org/v2")
+	return owner.Kind == "MPIJob" && strings.HasPrefix(owner.APIVersion, kubeflow.SchemeGroupVersion.Group)
```

  ```suggestion
  	return owner.Kind == "MPIJob" && strings.HasPrefix(owner.APIVersion, "kubeflow.org/v2")
  ```
  
  This allows us to support MPIJob v1, but we do not want to support the deprecated version.

- **pkg/controller/jobs/mpijob/mpijob_multikueue_adapter.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T03:54:01Z
```diff
@@ -0,0 +1,117 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mpijob
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type multikueueAdapter struct{}
+
+var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
+
+func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
+	localJob := kubeflow.MPIJob{}
+	err := localClient.Get(ctx, key, &localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := kubeflow.MPIJob{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// if the remote exists, just copy the status
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, &localJob, func() (bool, error) {
+			localJob.Status = remoteJob.Status
+			return true, nil
+		})
+	}
+
+	remoteJob = kubeflow.MPIJob{
+		ObjectMeta: api.CloneObjectMetaForCreation(&localJob.ObjectMeta),
+		Spec:       *localJob.Spec.DeepCopy(),
+	}
+
+	// add the prebuilt workload
+	if remoteJob.Labels == nil {
+		remoteJob.Labels = map[string]string{}
```

  ```suggestion
  		remoteJob.Labels = make(map[string]string, 2)
  ```

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T04:05:06Z
```diff
@@ -42,44 +42,87 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy corev1.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
```

  Why do we need this name?

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:79** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T04:06:10Z
```diff
@@ -42,44 +42,87 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy corev1.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = rs.RestartPolicy
+
+		if rs.Annotations != nil {
+			j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.ObjectMeta.Annotations = rs.Annotations
+		}
+	}
+
+	return j
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecsDefault() *MPIJobWrapper {
+	j.Spec.MPIReplicaSpecs[kubeflow.MPIReplicaTypeLauncher] = &kubeflow.ReplicaSpec{
+		Replicas: ptr.To[int32](1),
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				RestartPolicy: "Never",
```

  ```suggestion
  				RestartPolicy: corev1.RestartPolicyNever,
  ```

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:97** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T04:06:49Z
```diff
@@ -42,44 +42,87 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy corev1.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = rs.RestartPolicy
+
+		if rs.Annotations != nil {
+			j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.ObjectMeta.Annotations = rs.Annotations
+		}
+	}
+
+	return j
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecsDefault() *MPIJobWrapper {
+	j.Spec.MPIReplicaSpecs[kubeflow.MPIReplicaTypeLauncher] = &kubeflow.ReplicaSpec{
+		Replicas: ptr.To[int32](1),
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				RestartPolicy: "Never",
+				Containers: []corev1.Container{
+					{
+						Name:      "mpijob",
+						Image:     "pause",
+						Command:   []string{},
+						Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
 					},
 				},
-				kubeflow.MPIReplicaTypeWorker: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+				NodeSelector: map[string]string{},
+			},
+		},
+	}
+
+	j.Spec.MPIReplicaSpecs[kubeflow.MPIReplicaTypeWorker] = &kubeflow.ReplicaSpec{
+		Replicas: ptr.To[int32](1),
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				RestartPolicy: "Never",
```

  ```suggestion
  				RestartPolicy: corev1.RestartPolicyNever,
  ```

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:122** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T04:07:08Z
```diff
@@ -42,44 +42,87 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy corev1.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = rs.RestartPolicy
+
+		if rs.Annotations != nil {
+			j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.ObjectMeta.Annotations = rs.Annotations
+		}
+	}
+
+	return j
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecsDefault() *MPIJobWrapper {
+	j.Spec.MPIReplicaSpecs[kubeflow.MPIReplicaTypeLauncher] = &kubeflow.ReplicaSpec{
+		Replicas: ptr.To[int32](1),
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				RestartPolicy: "Never",
+				Containers: []corev1.Container{
+					{
+						Name:      "mpijob",
+						Image:     "pause",
+						Command:   []string{},
+						Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
 					},
 				},
-				kubeflow.MPIReplicaTypeWorker: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+				NodeSelector: map[string]string{},
+			},
+		},
+	}
+
+	j.Spec.MPIReplicaSpecs[kubeflow.MPIReplicaTypeWorker] = &kubeflow.ReplicaSpec{
+		Replicas: ptr.To[int32](1),
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				RestartPolicy: "Never",
+				Containers: []corev1.Container{
+					{
+						Name:      "mpijob",
+						Image:     "pause",
+						Command:   []string{},
+						Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
 					},
 				},
+				NodeSelector: map[string]string{},
 			},
 		},
-	}}
+	}
+
+	return j
+}
+
+// Clone returns deep copy of the PaddleJobWrapper.
+func (j *MPIJobWrapper) Clone() *MPIJobWrapper {
+	return &MPIJobWrapper{MPIJob: *j.DeepCopy()}
+}
+
+// Label sets the label key and value
+func (j *MPIJobWrapper) Label(key, value string) *MPIJobWrapper {
+	if j.Labels == nil {
+		j.Labels = make(map[string]string)
```

  ```suggestion
  		j.Labels = make(map[string]string, 1)
  ```

- **test/e2e/multikueue/e2e_test.go:450** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T04:08:26Z
```diff
@@ -567,65 +435,63 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
-		ginkgo.It("Should run a kubeflow XGBoostJob on worker if admitted", func() {
-			// Skipped due to known bug - https://github.com/kubeflow/training-operator/issues/1711
-			ginkgo.Skip("Skipped due to state transitioning bug in training-operator")
+		ginkgo.It("Should run a MPIJob on worker if admitted", func() {
 			// Since it requires 1.5 CPU, this job can only be admitted in worker 1.
-			xgboostJob := testingxgboostjob.MakeXGBoostJob("xgboostjob1", managerNs.Name).
+			mpijob := testingmpijob.MakeMPIJob("mpijob1", managerNs.Name).
 				Queue(managerLq.Name).
-				XGBReplicaSpecs(
-					testingxgboostjob.XGBReplicaSpecRequirement{
-						ReplicaType:   kftraining.XGBoostJobReplicaTypeMaster,
+				MPIJobReplicaSpecs(
+					testingmpijob.MPIJobReplicaSpecRequirement{
+						ReplicaType:   kubeflow.MPIReplicaTypeLauncher,
 						ReplicaCount:  1,
 						RestartPolicy: "OnFailure",
 					},
-					testingxgboostjob.XGBReplicaSpecRequirement{
-						ReplicaType:   kftraining.XGBoostJobReplicaTypeWorker,
-						ReplicaCount:  1,
+					testingmpijob.MPIJobReplicaSpecRequirement{
+						ReplicaType:   kubeflow.MPIReplicaTypeWorker,
+						ReplicaCount:  2,
```

  ```suggestion
  						ReplicaCount:  1,
  ```
  
  Could we use the "1" replicas to reduce testing duration?

- **test/e2e/multikueue/e2e_test.go:482** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T04:11:17Z
```diff
@@ -567,65 +435,63 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 
-		ginkgo.It("Should run a kubeflow XGBoostJob on worker if admitted", func() {
-			// Skipped due to known bug - https://github.com/kubeflow/training-operator/issues/1711
-			ginkgo.Skip("Skipped due to state transitioning bug in training-operator")
+		ginkgo.It("Should run a MPIJob on worker if admitted", func() {
 			// Since it requires 1.5 CPU, this job can only be admitted in worker 1.
-			xgboostJob := testingxgboostjob.MakeXGBoostJob("xgboostjob1", managerNs.Name).
+			mpijob := testingmpijob.MakeMPIJob("mpijob1", managerNs.Name).
 				Queue(managerLq.Name).
-				XGBReplicaSpecs(
-					testingxgboostjob.XGBReplicaSpecRequirement{
-						ReplicaType:   kftraining.XGBoostJobReplicaTypeMaster,
+				MPIJobReplicaSpecs(
+					testingmpijob.MPIJobReplicaSpecRequirement{
+						ReplicaType:   kubeflow.MPIReplicaTypeLauncher,
 						ReplicaCount:  1,
 						RestartPolicy: "OnFailure",
 					},
-					testingxgboostjob.XGBReplicaSpecRequirement{
-						ReplicaType:   kftraining.XGBoostJobReplicaTypeWorker,
-						ReplicaCount:  1,
+					testingmpijob.MPIJobReplicaSpecRequirement{
+						ReplicaType:   kubeflow.MPIReplicaTypeWorker,
+						ReplicaCount:  2,
 						RestartPolicy: "OnFailure",
 					},
 				).
-				Request(kftraining.XGBoostJobReplicaTypeMaster, corev1.ResourceCPU, "1").
-				Request(kftraining.XGBoostJobReplicaTypeMaster, corev1.ResourceMemory, "200M").
-				Request(kftraining.XGBoostJobReplicaTypeWorker, corev1.ResourceCPU, "0.5").
-				Request(kftraining.XGBoostJobReplicaTypeWorker, corev1.ResourceMemory, "100M").
-				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.1.0").
-				Args([]string{"1ms"}).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "1").
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceMemory, "200M").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "0.5").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceMemory, "100M").
+				Image(kubeflow.MPIReplicaTypeLauncher, "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0", []string{"1ms"}).
+				Image(kubeflow.MPIReplicaTypeWorker, "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0", []string{"1ms"}).
 				Obj()
 
-			ginkgo.By("Creating the XGBoostJob", func() {
-				gomega.Expect(k8sManagerClient.Create(ctx, xgboostJob)).Should(gomega.Succeed())
+			ginkgo.By("Creating the MPIJob", func() {
+				gomega.Expect(k8sManagerClient.Create(ctx, mpijob)).Should(gomega.Succeed())
 			})
 
-			wlLookupKey := types.NamespacedName{Name: workloadxgboostjob.GetWorkloadNameForXGBoostJob(xgboostJob.Name, xgboostJob.UID), Namespace: managerNs.Name}
+			wlLookupKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(mpijob.Name, mpijob.UID), Namespace: managerNs.Name}
 
 			// the execution should be given to the worker1
 			waitForJobAdmitted(wlLookupKey, multiKueueAc.Name, "worker1")
 
-			ginkgo.By("Waiting for the XGBoostJob to finish", func() {
+			ginkgo.By("Waiting for the MPIJob to finish", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
-					createdXGBoostJob := &kftraining.XGBoostJob{}
-					g.Expect(k8sManagerClient.Get(ctx, client.ObjectKeyFromObject(xgboostJob), createdXGBoostJob)).To(gomega.Succeed())
-					g.Expect(createdXGBoostJob.Status.ReplicaStatuses[kftraining.XGBoostJobReplicaTypeMaster]).To(gomega.BeComparableTo(
-						&kftraining.ReplicaStatus{
+					createdMPIJob := &kubeflow.MPIJob{}
+					g.Expect(k8sManagerClient.Get(ctx, client.ObjectKeyFromObject(mpijob), createdMPIJob)).To(gomega.Succeed())
+					g.Expect(createdMPIJob.Status.ReplicaStatuses[kubeflow.MPIReplicaTypeLauncher]).To(gomega.BeComparableTo(
+						&kubeflow.ReplicaStatus{
 							Active:    0,
 							Succeeded: 1,
 						},
 						util.IgnoreConditionTimestampsAndObservedGeneration))
 
-					finishReasonMessage := fmt.Sprintf("XGBoostJob %s is successfully completed.", xgboostJob.Name)
+					finishReasonMessage := fmt.Sprintf("MPIJob %s/%s successfully completed.", mpijob.Namespace, mpijob.Name)
```

  ```suggestion
  					finishReasonMessage := fmt.Sprintf("MPIJob %s successfully completed.", client.ObjectKeyFromObject(mpijob).String())
  ```

- **test/integration/multikueue/multikueue_test.go:1065** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-30T04:12:37Z
```diff
@@ -1026,6 +1031,86 @@ var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 		})
 	})
 
+	ginkgo.It("Should run a MPIJob on worker if admitted", func() {
+		mpijob := testingmpijob.MakeMPIJob("mpijob1", managerNs.Name).
+			Queue(managerLq.Name).
+			MPIJobReplicaSpecs(
+				testingmpijob.MPIJobReplicaSpecRequirement{
+					ReplicaType:   kubeflow.MPIReplicaTypeLauncher,
+					ReplicaCount:  1,
+					Name:          "launcher",
+					RestartPolicy: corev1.RestartPolicyOnFailure,
+				},
+				testingmpijob.MPIJobReplicaSpecRequirement{
+					ReplicaType:   kubeflow.MPIReplicaTypeWorker,
+					ReplicaCount:  1,
+					Name:          "worker",
+					RestartPolicy: corev1.RestartPolicyNever,
+				},
+			).
+			Obj()
+		gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, mpijob)).Should(gomega.Succeed())
+
+		wlLookupKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(mpijob.Name, mpijob.UID), Namespace: managerNs.Name}
+		admission := utiltesting.MakeAdmission(managerCq.Name).PodSets(
+			kueue.PodSetAssignment{
+				Name: "launcher",
+			}, kueue.PodSetAssignment{
+				Name: "worker",
+			},
+		)
+
+		admitWorkloadAndCheckWorkerCopies(multikueueAC.Name, wlLookupKey, admission)
+
+		ginkgo.By("changing the status of the XGBoostJob in the worker, updates the manager's XGBoostJob status", func() {
```

  ```suggestion
  		ginkgo.By("changing the status of the MPIJob in the worker, updates the manager's MPIJob status", func() {
  ```
  
  😉

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-30T07:06:38Z

- **Makefile-deps.mk:115** by [@mszadkow](https://github.com/mszadkow) — 2024-08-30T07:06:38Z
```diff
@@ -112,7 +112,10 @@ KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	# Remove `kubeflow.org_mpijobs.yaml` version v1 from the kustomization.yaml, kueue uses v2beta1 CRD from mpi-operator
```

  you mean to copy the original one and store it in dedicated folder in e2e config alongside the delete patch?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-08-30T12:49:03Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:76** by [@mszadkow](https://github.com/mszadkow) — 2024-08-30T12:49:02Z
```diff
@@ -72,7 +73,7 @@ func NewJob() jobframework.GenericJob {
 var NewReconciler = jobframework.NewGenericReconcilerFactory(NewJob)
 
 func isMPIJob(owner *metav1.OwnerReference) bool {
-	return owner.Kind == "MPIJob" && strings.HasPrefix(owner.APIVersion, "kubeflow.org/v2")
+	return owner.Kind == "MPIJob" && strings.HasPrefix(owner.APIVersion, kubeflow.SchemeGroupVersion.Group)
```

  the one I specified was actually allowing for `v2beta1` support

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-02T07:10:49Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:63** by [@mszadkow](https://github.com/mszadkow) — 2024-09-02T07:10:49Z
```diff
@@ -42,44 +42,87 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	Name          string
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy corev1.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Name = rs.Name
```

  hmm, maybe that's indeed not needed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-03T22:25:56Z

- **hack/multikueue-e2e-test.sh:97** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-03T22:02:53Z
```diff
@@ -80,16 +80,25 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
     # MANAGER
     # Only install the CRDs and not the controller to be able to
     # have Kubeflow Jobs admitted without execution in the manager cluster.
     kubectl config use-context "kind-${MANAGER_KIND_CLUSTER_NAME}"
-    kubectl apply -k "${KUBEFLOW_CRDS}"
+    kubectl apply -k "${KUBEFLOW_MANIFEST_MANAGER}"
+    ## MPI
+    kubectl apply --server-side -f "${KUBEFLOW_MPI_CRD}"
 
     # WORKERS
     docker pull kubeflow/training-operator:v1-855e096
+    docker pull "mpioperator/mpi-operator:${KUBEFLOW_MPI_VERSION/#v}"
```

  ```suggestion
      docker pull "${KUBEFLOW_MPI_IMAGE}"
  ```
  nit

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:112** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-03T22:10:53Z
```diff
@@ -42,44 +42,85 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy corev1.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = rs.RestartPolicy
+
+		if rs.Annotations != nil {
+			j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.ObjectMeta.Annotations = rs.Annotations
+		}
+	}
+
+	return j
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecsDefault() *MPIJobWrapper {
+	j.Spec.MPIReplicaSpecs[kubeflow.MPIReplicaTypeLauncher] = &kubeflow.ReplicaSpec{
+		Replicas: ptr.To[int32](1),
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				RestartPolicy: corev1.RestartPolicyNever,
+				Containers: []corev1.Container{
+					{
+						Name:      "mpijob",
+						Image:     "pause",
+						Command:   []string{},
+						Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
 					},
 				},
-				kubeflow.MPIReplicaTypeWorker: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+				NodeSelector: map[string]string{},
+			},
+		},
+	}
+
+	j.Spec.MPIReplicaSpecs[kubeflow.MPIReplicaTypeWorker] = &kubeflow.ReplicaSpec{
+		Replicas: ptr.To[int32](1),
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				RestartPolicy: corev1.RestartPolicyNever,
+				Containers: []corev1.Container{
+					{
+						Name:      "mpijob",
+						Image:     "pause",
+						Command:   []string{},
+						Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
 					},
 				},
+				NodeSelector: map[string]string{},
 			},
 		},
-	}}
+	}
+
+	return j
+}
+
+// Clone returns deep copy of the PaddleJobWrapper.
```

  ```suggestion
  // Clone returns deep copy of the MPIJobWrapper.
  ```

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-03T22:20:16Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  Can we use the `manifests/overlays/standalone/kustomization.yaml` file as a resource?
  
  On this way we don't need to hardcode and replace `newTag`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-04T06:00:53Z

- **hack/e2e-common.sh:27** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-04T05:48:55Z
```diff
@@ -23,10 +23,14 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
```

  ```suggestion
  export KUBEFLOW_IMAGE=$($KUSTOMIZE build "$KUBEFLOW_MANIFEST" | $YQ e 'select(.kind == "Deployment") | .spec.template.spec.containers[0].image')
  ```

- **hack/multikueue-e2e-test.sh:96** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-04T05:50:38Z
```diff
@@ -80,16 +80,25 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
     # MANAGER
     # Only install the CRDs and not the controller to be able to
     # have Kubeflow Jobs admitted without execution in the manager cluster.
     kubectl config use-context "kind-${MANAGER_KIND_CLUSTER_NAME}"
-    kubectl apply -k "${KUBEFLOW_CRDS}"
+    kubectl apply -k "${KUBEFLOW_MANIFEST_MANAGER}"
+    ## MPI
+    kubectl apply --server-side -f "${KUBEFLOW_MPI_CRD}"
 
     # WORKERS
     docker pull kubeflow/training-operator:v1-855e096
```

  ```suggestion
      docker pull "${KUBEFLOW_IMAGE}"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-04T11:10:00Z

- **Makefile-deps.mk:116** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-04T11:04:37Z
```diff
@@ -111,8 +111,12 @@ mpi-operator-crd: ## Copy the CRDs from the mpi-operator to the dep-crds directo
 KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/kubeflow/training-operator)
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
+	## Full version of the manifest is required for e2e multikueue tests.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	cp -prf $(KF_TRAINING_ROOT)/manifests/* $(EXTERNAL_CRDS_DIR)/training-operator/
```

  I encountered a `Permission Denied` error from `cp` command when executing `make kf-training-operator-crd` multiple times.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-04T18:40:38Z

first pass
Please address @mbobrovskyi's comments

- **Makefile-deps.mk:113** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-04T18:32:18Z
```diff
@@ -111,8 +111,12 @@ mpi-operator-crd: ## Copy the CRDs from the mpi-operator to the dep-crds directo
 KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/kubeflow/training-operator)
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
```

  maybe we can have a different directive for `kf-training-operator` that contains all the manifests?

- **Makefile-deps.mk:114** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-04T18:32:58Z
```diff
@@ -111,8 +111,12 @@ mpi-operator-crd: ## Copy the CRDs from the mpi-operator to the dep-crds directo
 KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/kubeflow/training-operator)
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
+	## Full version of the manifest is required for e2e multikueue tests.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	cp -prf $(KF_TRAINING_ROOT)/manifests/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	## Removing kubeflow.org_mpijobs.yaml is required as the version of MPIJob is conflicting between training-operator and mpi-operator - in integration tests.
```

  why is it only a problem for integration and not E2E?

- **Makefile-test.mk:71** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-04T18:34:44Z
```diff
@@ -67,7 +68,7 @@ test: gotestsum ## Run tests.
 	$(GOTESTSUM) --junitfile $(ARTIFACTS)/junit.xml -- $(GO_TEST_FLAGS) $(shell $(GO_CMD) list ./... | grep -v '/test/') -coverpkg=./... -coverprofile $(ARTIFACTS)/cover.out
 
 .PHONY: test-integration
-test-integration: gomod-download envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd kf-training-operator-crd  cluster-autoscaler-crd kueuectl ## Run tests.
+test-integration: gomod-download envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd kf-training-operator-crd cluster-autoscaler-crd kueuectl ## Run tests.
```

  can we use `dep-crds` here?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T06:27:04Z

- **Makefile-test.mk:71** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T06:27:04Z
```diff
@@ -67,7 +68,7 @@ test: gotestsum ## Run tests.
 	$(GOTESTSUM) --junitfile $(ARTIFACTS)/junit.xml -- $(GO_TEST_FLAGS) $(shell $(GO_CMD) list ./... | grep -v '/test/') -coverpkg=./... -coverprofile $(ARTIFACTS)/cover.out
 
 .PHONY: test-integration
-test-integration: gomod-download envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd kf-training-operator-crd  cluster-autoscaler-crd kueuectl ## Run tests.
+test-integration: gomod-download envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd kf-training-operator-crd cluster-autoscaler-crd kueuectl ## Run tests.
```

  yes, that can be used

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T06:27:49Z

- **Makefile-deps.mk:113** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T06:27:49Z
```diff
@@ -111,8 +111,12 @@ mpi-operator-crd: ## Copy the CRDs from the mpi-operator to the dep-crds directo
 KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/kubeflow/training-operator)
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
```

  Yes, we can do that, we need it and I wi explain it below.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T06:33:31Z

- **Makefile-deps.mk:114** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T06:33:31Z
```diff
@@ -111,8 +111,12 @@ mpi-operator-crd: ## Copy the CRDs from the mpi-operator to the dep-crds directo
 KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/kubeflow/training-operator)
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
+	## Full version of the manifest is required for e2e multikueue tests.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	cp -prf $(KF_TRAINING_ROOT)/manifests/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	## Removing kubeflow.org_mpijobs.yaml is required as the version of MPIJob is conflicting between training-operator and mpi-operator - in integration tests.
```

  Let me explain.
  **Integration tests** suit requires to specify the CRD directory and it doesn't care about the customise.yaml there, only plain CRDs - https://github.com/kubernetes-sigs/kueue/blob/afb49468223ca81419b13e45923f752f803dd80e/test/integration/multikueue/suite_test.go#L94
  Thus we can't have MPIJob CRD from training-operator there, we need to physically remove the file from the folder.
  
  **E2E** doesn't care about the MPIJob file, because we have our own kustomize in test/e2e/config/multikueue, where we patch it so MPIJob CRD is deleted.
  
  As you can see we have different needs depending on the test, that can't be satisfied with the current approach.
  Previously I had combined the two by using sed to remove MPIJob file entry from original kustomize and also delete the fie from the folder.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T13:21:02Z

- **Makefile-deps.mk:116** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T13:21:02Z
```diff
@@ -111,8 +111,12 @@ mpi-operator-crd: ## Copy the CRDs from the mpi-operator to the dep-crds directo
 KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/kubeflow/training-operator)
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
+	## Full version of the manifest is required for e2e multikueue tests.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	cp -prf $(KF_TRAINING_ROOT)/manifests/* $(EXTERNAL_CRDS_DIR)/training-operator/
```

  will be fixed

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T13:21:08Z

- **hack/multikueue-e2e-test.sh:96** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T13:21:08Z
```diff
@@ -80,16 +80,25 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
     # MANAGER
     # Only install the CRDs and not the controller to be able to
     # have Kubeflow Jobs admitted without execution in the manager cluster.
     kubectl config use-context "kind-${MANAGER_KIND_CLUSTER_NAME}"
-    kubectl apply -k "${KUBEFLOW_CRDS}"
+    kubectl apply -k "${KUBEFLOW_MANIFEST_MANAGER}"
+    ## MPI
+    kubectl apply --server-side -f "${KUBEFLOW_MPI_CRD}"
 
     # WORKERS
     docker pull kubeflow/training-operator:v1-855e096
```

  good one

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T13:21:46Z

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T13:21:46Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  not possible, let it stay like this for now

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T14:17:49Z

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T14:17:49Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  Why?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T19:00:34Z

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T19:00:34Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  we want different behaviour in our kustomize, that excludes the possibility to use the one in original folder, thus we need to repeat whatever is there

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-05T19:19:46Z

- **hack/e2e-common.sh:27** by [@mszadkow](https://github.com/mszadkow) — 2024-09-05T19:19:46Z
```diff
@@ -23,10 +23,14 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
```

  TBH this approach is very problematic, bc for some unknown to me reason any operation on files in dep-crds in e2e scripts is failing.
  Looking for solution there to save a line or two of code is very burdensome, that involves many repeated GitHub workflow runs as locally all works perfectly.
  I suggest to only approach it in follow up as this PR is bit extended already.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T19:36:43Z

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:32:30Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  ```suggestion
  - ../../../../../dep-crds/training-operator/overlays/standalone
  ```
  
  I mean this one.

- **test/e2e/config/multikueue/manager/kustomization.yaml:14** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:33:03Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
+- namespace.yaml
+images:
+- name: kubeflow/training-operator
+  newTag: v1-9e52eb7
+secretGenerator:
+- name: training-operator-webhook-cert
+  options:
+    disableNameSuffixHash: true
+patchesStrategicMerge:
+- patch_crds.yaml
```

  ```suggestion
  apiVersion: kustomize.config.k8s.io/v1beta1
  kind: Kustomization
  namespace: kubeflow
  resources:
  - ../../../../../dep-crds/training-operator/overlays/standalone
  patchesStrategicMerge:
  - patch_crds.yaml
  ```

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:33:15Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  And this not need.

- **test/e2e/config/multikueue/worker/kustomization.yaml:8** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:34:12Z
```diff
@@ -0,0 +1,16 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base
+- namespace.yaml
+images:
+- name: kubeflow/training-operator
+  newTag: v1-9e52eb7
+secretGenerator:
+- name: training-operator-webhook-cert
+  options:
+    disableNameSuffixHash: true
+patchesStrategicMerge:
+- patch_crds.yaml
+- patch_deployment.yaml
```

  ```suggestion
  apiVersion: kustomize.config.k8s.io/v1beta1
  kind: Kustomization
  namespace: kubeflow
  resources:
    - ../../../../../dep-crds/training-operator/overlays/standalone
  patchesStrategicMerge:
  - patch_crds.yaml
  - patch_deployment.yaml
  ```
  The same situation here.

- **test/e2e/config/multikueue/worker/namespace.yaml:4** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:34:51Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  And this we can remove too. Because it is already added on standalone kustomization.yaml.

- **test/e2e/config/multikueue/worker/patch_crds.yaml:5** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:35:35Z
```diff
@@ -0,0 +1,5 @@
+$patch: delete
+apiVersion: apiextensions.k8s.io/v1
+kind: CustomResourceDefinition
+metadata:
+  name: mpijobs.kubeflow.org
```

  Here is the duplicate of `test/e2e/config/multikueue/manager/patch_crds.yaml`. We can move it to common directory.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T19:38:12Z

- **hack/e2e-common.sh:27** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:38:12Z
```diff
@@ -23,10 +23,14 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
```

  Agree. Let's make it on another PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T19:38:50Z

- **hack/e2e-common.sh:27** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:38:50Z
```diff
@@ -23,10 +23,14 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
 #no matching semver tag unfortunately
 export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
```

  Just maybe one thing that we have different versions on kustomization file and here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T19:41:06Z

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T19:41:06Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  I checked this solution locally. Looks like working fine. Maybe I something missed?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-06T07:55:23Z

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mszadkow](https://github.com/mszadkow) — 2024-09-06T07:55:23Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  then maybe I tried it wrong, I will check

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-06T07:56:15Z

- **test/e2e/config/multikueue/manager/kustomization.yaml:5** by [@mszadkow](https://github.com/mszadkow) — 2024-09-06T07:56:14Z
```diff
@@ -0,0 +1,15 @@
+apiVersion: kustomize.config.k8s.io/v1beta1
+kind: Kustomization
+namespace: kubeflow
+resources:
+- ../../../../../dep-crds/training-operator/base/crds
```

  but for the worker, for the manager we don't wan to install the operator
  thus we point to base/crds

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-06T13:18:53Z

- **test/e2e/config/multikueue/worker/patch_crds.yaml:5** by [@mszadkow](https://github.com/mszadkow) — 2024-09-06T13:18:53Z
```diff
@@ -0,0 +1,5 @@
+$patch: delete
+apiVersion: apiextensions.k8s.io/v1
+kind: CustomResourceDefinition
+metadata:
+  name: mpijobs.kubeflow.org
```

  I am sorry, we can't kustomize requires to have patches in the same folder or in the folder below

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-06T13:19:16Z

- **test/e2e/config/multikueue/worker/namespace.yaml:4** by [@mszadkow](https://github.com/mszadkow) — 2024-09-06T13:19:16Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  this one I can remove

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-06T13:20:07Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@mszadkow](https://github.com/mszadkow) — 2024-09-06T13:20:06Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  this one will be needed as we don't use kustomize from overlays/standalone like worker does

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-06T15:07:39Z

Overall LGTM. Only this warning left:

```
# Warning: 'patchesStrategicMerge' is deprecated. Please use 'patches' instead. Run 'kustomize edit fix' to update your Kustomization automatically.
```

But I think we can fix it on follow-up.

Thanks!

- **hack/multikueue-e2e-test.sh:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-06T15:05:30Z
```diff
@@ -57,7 +57,7 @@ function startup {
             MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
         fi
 
-	      echo "Using manager config: $MANAGER_KIND_CONFIG"
+	    echo "Using manager config: $MANAGER_KIND_CONFIG"
```

  ```suggestion
  	      echo "Using manager config: $MANAGER_KIND_CONFIG"
  ```
  
  nit

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-06T17:09:27Z

- **Makefile-deps.mk:114** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T16:09:14Z
```diff
@@ -111,8 +111,12 @@ mpi-operator-crd: ## Copy the CRDs from the mpi-operator to the dep-crds directo
 KF_TRAINING_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/kubeflow/training-operator)
 .PHONY: kf-training-operator-crd
 kf-training-operator-crd: ## Copy the CRDs from the training-operator to the dep-crds directory.
+	## Full version of the manifest is required for e2e multikueue tests.
 	mkdir -p $(EXTERNAL_CRDS_DIR)/training-operator/
-	cp -f $(KF_TRAINING_ROOT)/manifests/base/crds/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	cp -prf $(KF_TRAINING_ROOT)/manifests/* $(EXTERNAL_CRDS_DIR)/training-operator/
+	## Removing kubeflow.org_mpijobs.yaml is required as the version of MPIJob is conflicting between training-operator and mpi-operator - in integration tests.
```

  Thanks for the details!
  
  It makes me think that we need a note on this page https://kueue.sigs.k8s.io/docs/tasks/run/kubeflow/mpijobs/ explaining that you need to disable the MPI from training-operator if using both.

- **hack/e2e-common.sh:30** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T16:12:24Z
```diff
@@ -23,10 +23,15 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
-#no matching semver tag unfortunately
-export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
-export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator/
+export KUBEFLOW_MANIFEST_MANAGER=${ROOT_DIR}/test/e2e/config/multikueue/manager
+export KUBEFLOW_MANIFEST_WORKER=${ROOT_DIR}/test/e2e/config/multikueue/worker
+KUBEFLOW_IMAGE_VERSION=$($KUSTOMIZE build "$KUBEFLOW_MANIFEST_WORKER" | $YQ e 'select(.kind == "Deployment") | .spec.template.spec.containers[0].image | split(":") | .[1]')
+export KUBEFLOW_IMAGE_VERSION
+export KUBEFLOW_IMAGE=kubeflow/training-operator:${KUBEFLOW_IMAGE_VERSION}
```

  ```suggestion
  export KUBEFLOW_IMAGE=$($KUSTOMIZE build "$KUBEFLOW_MANIFEST_WORKER" | $YQ e 'select(.kind == "Deployment") | .spec.template.spec.containers[0].image')
  ```
  
  what about that?

- **hack/multikueue-e2e-test.sh:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T16:14:50Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
```

  why do we need to do this?

- **pkg/controller/jobframework/validation.go:23** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T16:54:04Z
```diff
@@ -20,6 +20,7 @@ import (
 	"fmt"
 	"strings"
 
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
```

  ```suggestion
  	kfmpi "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
  ```

- **pkg/controller/jobs/mpijob/mpijob_multikueue_adapter.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T16:59:12Z
```diff
@@ -0,0 +1,117 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mpijob
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type multikueueAdapter struct{}
+
+var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
+
+func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
+	localJob := kubeflow.MPIJob{}
+	err := localClient.Get(ctx, key, &localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := kubeflow.MPIJob{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// if the remote exists, just copy the status
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, &localJob, func() (bool, error) {
+			localJob.Status = remoteJob.Status
+			return true, nil
+		})
+	}
+
+	remoteJob = kubeflow.MPIJob{
+		ObjectMeta: api.CloneObjectMetaForCreation(&localJob.ObjectMeta),
+		Spec:       *localJob.Spec.DeepCopy(),
+	}
+
+	// add the prebuilt workload
+	if remoteJob.Labels == nil {
+		remoteJob.Labels = make(map[string]string, 2)
+	}
+	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
+
+	return remoteClient.Create(ctx, &remoteJob)
+}
+
+func (b *multikueueAdapter) DeleteRemoteObject(ctx context.Context, remoteClient client.Client, key types.NamespacedName) error {
+	job := kubeflow.MPIJob{}
```

  Just like we did for other kubeflow jobs, instead of doing a get, just create an object with Name and Namespace
  
  https://github.com/kubernetes-sigs/kueue/blob/7fed1235a645da375c332c470ecd4b9be6971322/pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_multikueue_adapter.go#L122

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T17:02:17Z
```diff
@@ -42,44 +42,85 @@ func MakeMPIJob(name, ns string) *MPIJobWrapper {
 			RunPolicy: kubeflow.RunPolicy{
 				Suspend: ptr.To(true),
 			},
-			MPIReplicaSpecs: map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec{
-				kubeflow.MPIReplicaTypeLauncher: {
-					Replicas: ptr.To[int32](1),
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							RestartPolicy: corev1.RestartPolicyNever,
-							Containers: []corev1.Container{
-								{
-									Name:      "c",
-									Image:     "pause",
-									Command:   []string{},
-									Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
-								},
-							},
-							NodeSelector: map[string]string{},
-						},
+			MPIReplicaSpecs: make(map[kubeflow.MPIReplicaType]*kubeflow.ReplicaSpec),
+		},
+	},
+	}
+}
+
+type MPIJobReplicaSpecRequirement struct {
+	ReplicaType   kubeflow.MPIReplicaType
+	ReplicaCount  int32
+	Annotations   map[string]string
+	RestartPolicy corev1.RestartPolicy
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecs(replicaSpecs ...MPIJobReplicaSpecRequirement) *MPIJobWrapper {
+	j = j.MPIJobReplicaSpecsDefault()
+	for _, rs := range replicaSpecs {
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Replicas = ptr.To[int32](rs.ReplicaCount)
+		j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.Spec.RestartPolicy = rs.RestartPolicy
+
+		if rs.Annotations != nil {
+			j.Spec.MPIReplicaSpecs[rs.ReplicaType].Template.ObjectMeta.Annotations = rs.Annotations
+		}
+	}
+
+	return j
+}
+
+func (j *MPIJobWrapper) MPIJobReplicaSpecsDefault() *MPIJobWrapper {
```

  ```suggestion
  func (j *MPIJobWrapper) GenericLauncherAndWorker() *MPIJobWrapper {
  ```

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T17:05:13Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  Why do we need this namespace? Are the tests creating any objects in it?

- **test/integration/multikueue/multikueue_test.go:26** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T17:08:50Z
```diff
@@ -23,6 +23,7 @@ import (
 	"time"
 
 	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
```

  ```suggestion
  	kfmpi "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-09T07:03:36Z

- **hack/multikueue-e2e-test.sh:60** by [@mszadkow](https://github.com/mszadkow) — 2024-09-09T07:03:36Z
```diff
@@ -57,7 +57,7 @@ function startup {
             MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
         fi
 
-	      echo "Using manager config: $MANAGER_KIND_CONFIG"
+	    echo "Using manager config: $MANAGER_KIND_CONFIG"
```

  just wonder why you guys think it should intended?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-09T07:16:13Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@mszadkow](https://github.com/mszadkow) — 2024-09-09T07:16:13Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  Yes, we first create a job in the manager cluster.
  Base kustomize does not contain namespace object.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-09T07:28:34Z

- **hack/e2e-common.sh:30** by [@mszadkow](https://github.com/mszadkow) — 2024-09-09T07:28:34Z
```diff
@@ -23,10 +23,15 @@ export JOBSET_MANIFEST="https://github.com/kubernetes-sigs/jobset/releases/downl
 export JOBSET_IMAGE=registry.k8s.io/jobset/jobset:${JOBSET_VERSION}
 export JOBSET_CRDS=${ROOT_DIR}/dep-crds/jobset-operator/
 
-export KUBEFLOW_MANIFEST="https://github.com/kubeflow/training-operator/manifests/overlays/standalone?ref=${KUBEFLOW_VERSION}"
-#no matching semver tag unfortunately
-export KUBEFLOW_IMAGE=kubeflow/training-operator:v1-855e096
-export KUBEFLOW_CRDS=${ROOT_DIR}/dep-crds/training-operator/
+export KUBEFLOW_MANIFEST_MANAGER=${ROOT_DIR}/test/e2e/config/multikueue/manager
+export KUBEFLOW_MANIFEST_WORKER=${ROOT_DIR}/test/e2e/config/multikueue/worker
+KUBEFLOW_IMAGE_VERSION=$($KUSTOMIZE build "$KUBEFLOW_MANIFEST_WORKER" | $YQ e 'select(.kind == "Deployment") | .spec.template.spec.containers[0].image | split(":") | .[1]')
+export KUBEFLOW_IMAGE_VERSION
+export KUBEFLOW_IMAGE=kubeflow/training-operator:${KUBEFLOW_IMAGE_VERSION}
```

  1. Keeping it in one line fail on verification
  ```shell
  export KUBEFLOW_IMAGE_VERSION=$($KUSTOMIZE build "$KUBEFLOW_MANIFEST_WORKER" | $YQ e 'select(.kind == "Deployment") | .spec.template.spec.containers[0].image | split(":") | .[1]')
         ^--------------------^ SC2155 (warning): Declare and assign separately to avoid masking return values.
  
  For more information:
    https://www.shellcheck.net/wiki/SC2155 -- Declare and assign separately to ...
  ```
  so there are 2 options either apply the suggestion or `# shellcheck disable=SC2155`
  
  2. I need the image version in another place to make version used for the manager cluster compatible with the version in the worker.
  We didn't want the hardcoded version in `test/e2e/config/multikueue/manager/kustomization.yaml`

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-09T07:29:00Z

- **hack/multikueue-e2e-test.sh:89** by [@mszadkow](https://github.com/mszadkow) — 2024-09-09T07:28:59Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
```

  Explained above in point 2 - https://github.com/kubernetes-sigs/kueue/pull/2880#discussion_r1749716578

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-09T07:43:53Z

- **hack/multikueue-e2e-test.sh:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-09T07:43:53Z
```diff
@@ -57,7 +57,7 @@ function startup {
             MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
         fi
 
-	      echo "Using manager config: $MANAGER_KIND_CONFIG"
+	    echo "Using manager config: $MANAGER_KIND_CONFIG"
```

  This is not a necessary change that breaks the alignment. Look [here](https://github.com/kubernetes-sigs/kueue/blob/467b4ee707286f1f86f4f94cdb681111152bee61/hack/multikueue-e2e-test.sh#L60).

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-09T08:20:07Z

- **hack/multikueue-e2e-test.sh:60** by [@mszadkow](https://github.com/mszadkow) — 2024-09-09T08:20:07Z
```diff
@@ -57,7 +57,7 @@ function startup {
             MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
         fi
 
-	      echo "Using manager config: $MANAGER_KIND_CONFIG"
+	    echo "Using manager config: $MANAGER_KIND_CONFIG"
```

  ok, I won't argue ;)

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-09T13:41:37Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@mszadkow](https://github.com/mszadkow) — 2024-09-09T13:41:37Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  https://github.com/kubernetes-sigs/kueue/pull/2880#discussion_r1747455294 - explained here
  cc @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-09T19:46:33Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-09T19:46:33Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  But why do we create a job in the `kubeflow` namespace, instead of creating it in a namespace specifically created for the test?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-09T19:47:32Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-09T19:47:32Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  Or is `kubeflow` the name of the test namespace? That could be confusing, because users don't typically put jobs in the same namespace as the operator.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-09T19:49:11Z

- **hack/multikueue-e2e-test.sh:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-09T19:49:11Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
```

  if you don't do this change, what image would it be using?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-11T06:07:33Z

- **hack/multikueue-e2e-test.sh:89** by [@mszadkow](https://github.com/mszadkow) — 2024-09-11T06:07:33Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
```

  the `latest` would be used

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-11T06:11:33Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@mszadkow](https://github.com/mszadkow) — 2024-09-11T06:11:33Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  No, this is the namespace of the CRD.
  We need this to install them in the right place.
  I have mentioned tests, because for e2e we install CRDs for the manager cluster.
  We don't use this namespace (kubeflow) for test objects

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-11T06:40:31Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-11T06:40:31Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  I agree with @alculquicondor. We don't need this namespace here. `kubeflow` namespace can be used for `controller-manager`. But we are not using Deployment on manager, right? Try to remove it. It should work.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-11T06:52:56Z

- **hack/multikueue-e2e-test.sh:90** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-11T06:52:56Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
+    $YQ eval '(.images[] | select(.name == "kubeflow/training-operator").newTag) = env(KUBEFLOW_IMAGE_VERSION)' -i "$KUBEFLOW_MANIFEST_MANAGER/kustomization.yaml"
```

  Are you reverting these changes after testing?
  
  Like here:
  
  https://github.com/kubernetes-sigs/kueue/blob/467b4ee707286f1f86f4f94cdb681111152bee61/hack/multikueue-e2e-test.sh#L109
  
  https://github.com/kubernetes-sigs/kueue/blob/467b4ee707286f1f86f4f94cdb681111152bee61/hack/e2e-common.sh#L97-L99
  
   If not, users may accidentally commit unnecessary changes to this file.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-09-11T08:20:25Z

- **hack/multikueue-e2e-test.sh:90** by [@mszadkow](https://github.com/mszadkow) — 2024-09-11T08:20:25Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
+    $YQ eval '(.images[] | select(.name == "kubeflow/training-operator").newTag) = env(KUBEFLOW_IMAGE_VERSION)' -i "$KUBEFLOW_MANIFEST_MANAGER/kustomization.yaml"
```

  I don't, good point I will add the restore

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T13:12:36Z

- **hack/multikueue-e2e-test.sh:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T13:12:35Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
```

  Ah, nvm.
  
  I forgot that in kustomize, there is a field called `newTag`, which is independent from the image name.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T13:14:38Z

- **test/e2e/config/multikueue/manager/namespace.yaml:4** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T13:14:38Z
```diff
@@ -0,0 +1,4 @@
+apiVersion: v1
+kind: Namespace
+metadata:
+  name: kubeflow
```

  CRDs (the definitions) are non-namespaced, so we shouldn't need this namespace to install the CRDs.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T20:24:10Z

- **hack/multikueue-e2e-test.sh:90** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T20:24:10Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
+    $YQ eval '(.images[] | select(.name == "kubeflow/training-operator").newTag) = env(KUBEFLOW_IMAGE_VERSION)' -i "$KUBEFLOW_MANIFEST_MANAGER/kustomization.yaml"
```

  isn't this file outside of the tracked files?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T20:24:42Z

/approve

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-12T02:00:11Z

- **hack/multikueue-e2e-test.sh:90** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-12T02:00:11Z
```diff
@@ -80,16 +80,27 @@ function kind_load {
     install_jobset "$WORKER2_KIND_CLUSTER_NAME"
 
     # KUBEFLOW SETUP
+    # In order for MPI-operator and Training-operator to work on the same cluster it is required that:
+    # 1. 'kubeflow.org_mpijobs.yaml' is removed from base/crds/kustomization.yaml - https://github.com/kubeflow/training-operator/issues/1930
+    # 2. Training-operator deployment is modified to enable all kubeflow jobs except for mpi -  https://github.com/kubeflow/training-operator/issues/1777
+   
+    # Modify the `newTag` for the `kubeflow/training-operator` to use the one training-operator version
+    $YQ eval '(.images[] | select(.name == "kubeflow/training-operator").newTag) = env(KUBEFLOW_IMAGE_VERSION)' -i "$KUBEFLOW_MANIFEST_MANAGER/kustomization.yaml"
```

  No. It's on the git and yq will edit this file.
