# PR #2881: Update ClusterQueue and Cohort Resource Accounting for Hierarchical Cohorts

**Summary**: Update ClusterQueue and Cohort Resource Accounting for Hierarchical Cohorts

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2881

**Last updated**: 2024-08-30T14:49:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `resource_node` → `main`
- **Created**: 2024-08-23T14:41:33Z
- **Updated**: 2024-08-30T14:49:02Z
- **Merged**: 2024-08-29T15:49:31Z
- **Closed**: 2024-08-29T15:49:31Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 16   **+677 / -524**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
We define `cache.ResourceNode`, which is the new way of accounting capacity
and usage that is compatible with Hierarchical Cohorts. See #79

We define `SubtreeQuota`, which is the capacity available to a certain
node in the tree, respecting `LendingLimits` of its children.

The notion of `Usage` remains the same: `Usage` past
`GuaranteedQuota` is bubbled up the parent.

We delete the field `GuaranteedQuota`, as it can be derived
from `SubtreeQuota` and `LendingLimit`.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-23T14:41:48Z

/assign @alculquicondor

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-23T14:41:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d45d318d34f59f03015ede506c07472c78cac37b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66d04be6efeca00008fbe28a |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2881--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2881--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI4ODEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.__BNhsve3-Cwyb4zMNAZnEYVfOpWVe7W8h2xBwwkiic)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-23T14:52:07Z

> @gabesaba: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:
> 
> Test name	Commit	Details	Required	Rerun command
> pull-kueue-verify-main	[e6ba7b4](https://github.com/kubernetes-sigs/kueue/commit/e6ba7b4deeb1c169b114e3a2e1be3a9d188d2d58)	[link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/2881/pull-kueue-verify-main/1826993194550693888)	true	`/test pull-kueue-verify-main`
> [Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=2881). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Agabesaba). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

The linter is mistaken. These methods are needed to to satisfy the `dominantResourceShareNode` interface. Can we override this @alculquicondor?

Alternatively, if there is a way to get the `dominantResourceShareNode` interface working without generics...

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-28T10:44:44Z

1bb1476 to fe523eb was rebase
fe523eb to ec57951 addresses comments

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-29T10:23:20Z

8574203 to d45d318: rebase and conflict resolution

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-29T14:49:21Z

LGTM label has been added.  <details>Git tree hash: 21ab898a443003426a91b4873f2cf8abfb350875</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-29T15:15:45Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-29T15:15:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2881#issuecomment-2318053146" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2881#" title="Author self-approved">gabesaba</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-23T16:11:58Z

- **pkg/cache/clusterqueue_snapshot.go:52** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-23T16:11:51Z
```diff
@@ -28,13 +27,11 @@ type ClusterQueueSnapshot struct {
 	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
 	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
 	Status          metrics.ClusterQueueStatus
-	Quotas          map[resources.FlavorResource]*ResourceQuota
-	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
-	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
-	GuaranteedQuota resources.FlavorResourceQuantities
 	// AllocatableResourceGeneration will be increased when some admitted workloads are
 	// deleted, or the resource groups are changed.
 	AllocatableResourceGeneration int64
+
+	ResourceNode ResourceNode
 }
 
```

  ```suggestion
  var _ dominantResourceShareNode = (*ClusterQueueSnapshot)(nil)
  ```
  
  @gabesaba 
  Regarding the lint error, how about this obvious interface implementation?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-08-23T16:17:48Z

- **pkg/cache/clusterqueue_snapshot.go:52** by [@tenzen-y](https://github.com/tenzen-y) — 2024-08-23T16:17:48Z
```diff
@@ -28,13 +27,11 @@ type ClusterQueueSnapshot struct {
 	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
 	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
 	Status          metrics.ClusterQueueStatus
-	Quotas          map[resources.FlavorResource]*ResourceQuota
-	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
-	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
-	GuaranteedQuota resources.FlavorResourceQuantities
 	// AllocatableResourceGeneration will be increased when some admitted workloads are
 	// deleted, or the resource groups are changed.
 	AllocatableResourceGeneration int64
+
+	ResourceNode ResourceNode
 }
 
```

  Additionally, it would be great to check if bumping golangci-lint version resolve this issue: https://github.com/kubernetes-sigs/kueue/pull/2857

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-23T20:31:03Z

First pass

- **pkg/cache/clusterqueue.go:134** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:53:32Z
```diff
@@ -113,6 +123,27 @@ type ResourceQuota struct {
 	LendingLimit   *int64
 }
 
+func (r *ResourceQuota) nominalSafe() int64 {
```

  why do we need these functions?

- **pkg/cache/clusterqueue.go:166** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:56:24Z
```diff
@@ -126,28 +157,17 @@ func cohortFactory(name string) *cohort {
 	return &cohort{
 		name,
 		hierarchy.NewWiredCohort[*clusterQueue, *cohort](),
+		NewResourceNode(),
 	}
 }
 
-func (c *cohort) CalculateLendable() map[corev1.ResourceName]int64 {
-	lendable := make(map[corev1.ResourceName]int64)
-	for _, member := range c.Members() {
-		for _, fr := range flavorResources(member) {
-			quota := member.QuotaFor(fr)
-			if features.Enabled(features.LendingLimit) && quota.LendingLimit != nil {
-				lendable[fr.Resource] += *quota.LendingLimit
-			} else {
-				lendable[fr.Resource] += quota.Nominal
-			}
-		}
-	}
-	return lendable
-}
-
+// FitInCohort is supports the legacy
+// features.MultiplePreemptions=false path. It doesn't take into
```

  are you saying that, to support HierarchicalCohorts, the MultiplePreemptions feature gate needs to be enabled?

- **pkg/cache/clusterqueue.go:189** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T19:57:02Z
```diff
@@ -165,8 +185,20 @@ var defaultPreemption = kueue.ClusterQueuePreemption{
 
 var defaultFlavorFungibility = kueue.FlavorFungibility{WhenCanBorrow: kueue.Borrow, WhenCanPreempt: kueue.TryNextFlavor}
 
-func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
-	c.updateResourceGroups(in.Spec.ResourceGroups)
+func (cache *Cache) updateClusterQueue(c *clusterQueue, in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
+	oldCohort := c.Parent()
```

  ```suggestion
  	oldParent := c.Parent()
  ```

- **pkg/cache/clusterqueue.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T20:00:18Z
```diff
@@ -165,8 +185,20 @@ var defaultPreemption = kueue.ClusterQueuePreemption{
 
 var defaultFlavorFungibility = kueue.FlavorFungibility{WhenCanBorrow: kueue.Borrow, WhenCanPreempt: kueue.TryNextFlavor}
 
-func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
-	c.updateResourceGroups(in.Spec.ResourceGroups)
+func (cache *Cache) updateClusterQueue(c *clusterQueue, in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
+	oldCohort := c.Parent()
+	cache.hm.UpdateClusterQueueEdge(in.Name, in.Spec.Cohort)
+	if c.updateResourceGroups(in.Spec.ResourceGroups) || oldCohort != c.Parent() {
+		updateClusterQueueResourceNode(c)
+		c.AllocatableResourceGeneration += 1
+		if oldCohort != nil && oldCohort != c.Parent() {
+			updateCohortResourceNode(oldCohort)
+		}
+		if c.HasParent() {
```

  ```suggestion
  		} else if c.HasParent() {
  ```
  ?

- **pkg/cache/clusterqueue.go:241** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T20:01:04Z
```diff
@@ -203,30 +235,15 @@ func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.
 		c.FairWeight = *fs.Weight
 	}
 
-	if features.Enabled(features.LendingLimit) {
-		c.GuaranteedQuota = make(resources.FlavorResourceQuantities)
-		for _, rg := range c.ResourceGroups {
-			for _, fName := range rg.Flavors {
-				for rName := range rg.CoveredResources {
-					fr := resources.FlavorResource{Flavor: fName, Resource: rName}
-					rQuota := c.QuotaFor(fr)
-					if rQuota.LendingLimit != nil {
-						c.GuaranteedQuota[fr] += rQuota.Nominal - *rQuota.LendingLimit
-					}
-				}
-			}
-		}
-	}
-
 	return nil
 }
 
-func (c *clusterQueue) updateResourceGroups(in []kueue.ResourceGroup) {
+func (c *clusterQueue) updateResourceGroups(in []kueue.ResourceGroup) bool {
```

  Add a comment for the return value

- **pkg/cache/clusterqueue.go:450** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T20:12:37Z
```diff
@@ -434,7 +449,14 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
```

  we should try to get rid of this function if the `m` value is used to fork, as opposed to be used as a multiplication factor.

- **pkg/cache/resource.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T20:19:11Z
```diff
@@ -44,3 +46,15 @@ func remainingQuota(node netQuotaNode) resources.FlavorResourceQuantities {
 	}
 	return remainingQuota
 }
+
+type lendableNode interface {
+	getResourceNode() ResourceNode
+}
+
+func calculateLendable(node lendableNode) map[corev1.ResourceName]int64 {
```

  maybe this could be a function of `FlavorResourceQuantities` to merge lendables for all flavors of a resource.

- **pkg/cache/resource_node.go:103** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-23T20:25:05Z
```diff
@@ -0,0 +1,141 @@
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
+	getResourceNode() ResourceNode
+
+	HasParent() bool
+	Parent() T
+}
+
+// available determines how much capacity remains for the current
+// node, taking into account usage and BorrowingLimits.
+//
+// This function may return a negative number in the case of
+// overadmission - e.g. capacity was removed or the node moved to
+// another Cohort.
+//
+// We add an option to ignore the borrowing limit, to support
+// features.MultiplePreemptions=false path. This will be cleaned up in
+// v0.10, when we delete legacy logic.
+func available[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, enforceBorrowLimit bool) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr] - r.Usage[fr]
+	}
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	parentAvailable := available(node.Parent(), fr, enforceBorrowLimit)
+
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); enforceBorrowLimit && borrowingLimit != nil {
+		storedInParent := r.SubtreeQuota[fr] - r.guaranteedQuota(fr)
+		usedInParent := max(0, r.Usage[fr]-r.guaranteedQuota(fr))
+		withMaxFromParent := storedInParent - usedInParent + *borrowingLimit
+		parentAvailable = min(withMaxFromParent, parentAvailable)
+	}
+	return localAvailable + parentAvailable
```

  doesn't this sum double count localAvailable twice?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-26T08:50:58Z

- **pkg/cache/clusterqueue.go:134** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T08:09:59Z
```diff
@@ -113,6 +123,27 @@ type ResourceQuota struct {
 	LendingLimit   *int64
 }
 
+func (r *ResourceQuota) nominalSafe() int64 {
```

  Since we do a root walk to determine capacity, a node in path to root may not define quota for the `FlavorResource` we're checking. These convenience functions allow us to omit the nil check

- **pkg/cache/clusterqueue.go:166** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T08:12:40Z
```diff
@@ -126,28 +157,17 @@ func cohortFactory(name string) *cohort {
 	return &cohort{
 		name,
 		hierarchy.NewWiredCohort[*clusterQueue, *cohort](),
+		NewResourceNode(),
 	}
 }
 
-func (c *cohort) CalculateLendable() map[corev1.ResourceName]int64 {
-	lendable := make(map[corev1.ResourceName]int64)
-	for _, member := range c.Members() {
-		for _, fr := range flavorResources(member) {
-			quota := member.QuotaFor(fr)
-			if features.Enabled(features.LendingLimit) && quota.LendingLimit != nil {
-				lendable[fr.Resource] += *quota.LendingLimit
-			} else {
-				lendable[fr.Resource] += quota.Nominal
-			}
-		}
-	}
-	return lendable
-}
-
+// FitInCohort is supports the legacy
+// features.MultiplePreemptions=false path. It doesn't take into
```

  No, `HierarchicalCohorts` will work with or without `MultiplePreemptions`.
  
  It is only that the math we're doing [here](https://github.com/kubernetes-sigs/kueue/blob/21bd4462f1ada39e8074836cf2957eccb774373f/pkg/scheduler/scheduler.go#L266) relies on not checking borrowing limits, so we need to add an option to ignore the borrowing limit to the new accounting.

- **pkg/cache/clusterqueue.go:196** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T08:15:44Z
```diff
@@ -165,8 +185,20 @@ var defaultPreemption = kueue.ClusterQueuePreemption{
 
 var defaultFlavorFungibility = kueue.FlavorFungibility{WhenCanBorrow: kueue.Borrow, WhenCanPreempt: kueue.TryNextFlavor}
 
-func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
-	c.updateResourceGroups(in.Spec.ResourceGroups)
+func (cache *Cache) updateClusterQueue(c *clusterQueue, in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
+	oldCohort := c.Parent()
+	cache.hm.UpdateClusterQueueEdge(in.Name, in.Spec.Cohort)
+	if c.updateResourceGroups(in.Spec.ResourceGroups) || oldCohort != c.Parent() {
+		updateClusterQueueResourceNode(c)
+		c.AllocatableResourceGeneration += 1
+		if oldCohort != nil && oldCohort != c.Parent() {
+			updateCohortResourceNode(oldCohort)
+		}
+		if c.HasParent() {
```

  This check is correct: we need to update both affected subtrees. Suppose CQ moves from Cohort-A to Cohort-B. We need to remove resources/usage from Cohort-A, and add those to Cohort-B.

- **pkg/cache/clusterqueue.go:450** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T08:28:52Z
```diff
@@ -434,7 +449,14 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
```

  I agree, but it is outside of the scope of this PR. Let's fix in a followup to keep this PR's size reasonable.

- **pkg/cache/resource.go:70** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T08:39:10Z
```diff
@@ -44,3 +46,15 @@ func remainingQuota(node netQuotaNode) resources.FlavorResourceQuantities {
 	}
 	return remainingQuota
 }
+
+type lendableNode interface {
+	getResourceNode() ResourceNode
+}
+
+func calculateLendable(node lendableNode) map[corev1.ResourceName]int64 {
```

  It feels a little easier to use when we  define this as a function of the node, as the caller won't have to dig inside to know that it is the SubtreeQuota.

- **pkg/cache/resource_node.go:103** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T08:39:55Z
```diff
@@ -0,0 +1,141 @@
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
+	getResourceNode() ResourceNode
+
+	HasParent() bool
+	Parent() T
+}
+
+// available determines how much capacity remains for the current
+// node, taking into account usage and BorrowingLimits.
+//
+// This function may return a negative number in the case of
+// overadmission - e.g. capacity was removed or the node moved to
+// another Cohort.
+//
+// We add an option to ignore the borrowing limit, to support
+// features.MultiplePreemptions=false path. This will be cleaned up in
+// v0.10, when we delete legacy logic.
+func available[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, enforceBorrowLimit bool) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr] - r.Usage[fr]
+	}
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	parentAvailable := available(node.Parent(), fr, enforceBorrowLimit)
+
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); enforceBorrowLimit && borrowingLimit != nil {
+		storedInParent := r.SubtreeQuota[fr] - r.guaranteedQuota(fr)
+		usedInParent := max(0, r.Usage[fr]-r.guaranteedQuota(fr))
+		withMaxFromParent := storedInParent - usedInParent + *borrowingLimit
+		parentAvailable = min(withMaxFromParent, parentAvailable)
+	}
+	return localAvailable + parentAvailable
```

  I don't think so. The localAvailable is just the remaining guaranteed quota. Then we need to do some additional math for the effective borrowing limit (or amount of resource that can flow through parent->child edge), given that the child stores quota in its parent

- **pkg/cache/clusterqueue_snapshot.go:52** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T08:42:23Z
```diff
@@ -28,13 +27,11 @@ type ClusterQueueSnapshot struct {
 	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
 	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
 	Status          metrics.ClusterQueueStatus
-	Quotas          map[resources.FlavorResource]*ResourceQuota
-	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
-	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
-	GuaranteedQuota resources.FlavorResourceQuantities
 	// AllocatableResourceGeneration will be increased when some admitted workloads are
 	// deleted, or the resource groups are changed.
 	AllocatableResourceGeneration int64
+
+	ResourceNode ResourceNode
 }
 
```

  This gives the same error: have Parent() *CohortSnapshot, want Parent() lendableNode
  
  I was able to get rid of the generics, and define methods on `clusterQueue` and `ClusterQueueSnapshot` to return the `lendableNode` type

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-26T09:59:14Z

- **pkg/cache/clusterqueue_snapshot.go:52** by [@gabesaba](https://github.com/gabesaba) — 2024-08-26T09:59:14Z
```diff
@@ -28,13 +27,11 @@ type ClusterQueueSnapshot struct {
 	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
 	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
 	Status          metrics.ClusterQueueStatus
-	Quotas          map[resources.FlavorResource]*ResourceQuota
-	// GuaranteedQuota records how much resource quota the ClusterQueue reserved
-	// when feature LendingLimit is enabled and flavor's lendingLimit is not nil.
-	GuaranteedQuota resources.FlavorResourceQuantities
 	// AllocatableResourceGeneration will be increased when some admitted workloads are
 	// deleted, or the resource groups are changed.
 	AllocatableResourceGeneration int64
+
+	ResourceNode ResourceNode
 }
 
```

  > Additionally, it would be great to check if bumping golangci-lint version resolve this issue: #2857
  
  I resolved the issue, but I will leave as it is, without generics, as I think this is easier to read

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-26T19:59:57Z

The fact that there are no changes to the preemption or scheduler tests is a good sign that this is not a breaking change.

However, I hope the type complexity can be reduced.

- **pkg/cache/cache_test.go:530** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T17:35:53Z
```diff
@@ -530,7 +529,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 				},
 				"c": {
 					Name:                          "c",
-					AllocatableResourceGeneration: 1,
+					AllocatableResourceGeneration: 2,
```

  why does this number go up?

- **pkg/cache/clusterqueue.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T17:40:04Z
```diff
@@ -165,8 +185,20 @@ var defaultPreemption = kueue.ClusterQueuePreemption{
 
 var defaultFlavorFungibility = kueue.FlavorFungibility{WhenCanBorrow: kueue.Borrow, WhenCanPreempt: kueue.TryNextFlavor}
 
-func (c *clusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
-	c.updateResourceGroups(in.Spec.ResourceGroups)
+func (cache *Cache) updateClusterQueue(c *clusterQueue, in *kueue.ClusterQueue, resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, admissionChecks map[string]AdmissionCheck) error {
+	oldCohort := c.Parent()
+	cache.hm.UpdateClusterQueueEdge(in.Name, in.Spec.Cohort)
+	if c.updateResourceGroups(in.Spec.ResourceGroups) || oldCohort != c.Parent() {
+		updateClusterQueueResourceNode(c)
+		c.AllocatableResourceGeneration += 1
+		if oldCohort != nil && oldCohort != c.Parent() {
+			updateCohortResourceNode(oldCohort)
+		}
+		if c.HasParent() {
```

  my bad, I was reading this PR too late in the day :)

- **pkg/cache/clusterqueue.go:450** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T17:46:56Z
```diff
@@ -434,7 +449,14 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
```

  I agree.

- **pkg/cache/clusterqueue.go:592** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T17:53:32Z
```diff
@@ -628,12 +581,20 @@ func (c *ClusterQueueSnapshot) DominantResourceShareWithout(wlReq resources.Flav
 
 type dominantResourceShareNode interface {
 	HasParent() bool
+	lendableParent() lendableNode
```

  how about this?
  
  ```suggestion
  	parentResources() ResourceNode
  ```
  
  I'm trying to cut the indirections to reduce mental load when reviewing or revisiting this code.

- **pkg/cache/resource.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T17:56:32Z
```diff
@@ -44,3 +46,15 @@ func remainingQuota(node netQuotaNode) resources.FlavorResourceQuantities {
 	}
 	return remainingQuota
 }
+
+type lendableNode interface {
+	getResourceNode() ResourceNode
+}
+
+func calculateLendable(node lendableNode) map[corev1.ResourceName]int64 {
```

  Maybe it could be a member function of `ResourceNode`.

- **pkg/cache/resource_node.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T18:02:58Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
```

  ```suggestion
  	// the child's guaranteedQuotas.
  ```
  ?

- **pkg/cache/resource_node.go:50** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T18:04:46Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
```

  Wouldn't this reuse the same pointers to `ResourceQuota`? `Clone` does a shallow copy.

- **pkg/cache/resource_node.go:90** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T18:14:52Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
+	getResourceNode() ResourceNode
+
+	HasParent() bool
+	Parent() T
+}
+
+// available determines how much capacity remains for the current
+// node, taking into account usage and BorrowingLimits.
+//
+// This function may return a negative number in the case of
+// overadmission - e.g. capacity was removed or the node moved to
+// another Cohort.
+//
+// We add an option to ignore the borrowing limit, to support
+// features.MultiplePreemptions=false path. This will be cleaned up in
+// v0.10, when we delete legacy logic.
+func available[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, enforceBorrowLimit bool) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr] - r.Usage[fr]
+	}
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	parentAvailable := available(node.Parent(), fr, enforceBorrowLimit)
+
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); enforceBorrowLimit && borrowingLimit != nil {
```

  Add a few comments to explain what is happening in this function.

- **pkg/hierarchy/wired_cohort.go:6** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T19:27:21Z
```diff
@@ -6,6 +6,15 @@ type WiredCohort[CQ, Cohort nodeBase] struct {
 	members sets.Set[CQ]
```

  are the members always only CQs?

- **pkg/cache/resource_node.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T19:32:30Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
```

  I think you might be able to add one of the hierarchy objects as a member of this object, so that the functions below are members of this struct, but first it might be worth applying some of the remarks I left in #2824.

- **pkg/cache/resource_node.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T19:34:58Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
+	getResourceNode() ResourceNode
+
+	HasParent() bool
+	Parent() T
+}
+
+// available determines how much capacity remains for the current
+// node, taking into account usage and BorrowingLimits.
+//
+// This function may return a negative number in the case of
+// overadmission - e.g. capacity was removed or the node moved to
+// another Cohort.
+//
+// We add an option to ignore the borrowing limit, to support
+// features.MultiplePreemptions=false path. This will be cleaned up in
+// v0.10, when we delete legacy logic.
+func available[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, enforceBorrowLimit bool) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr] - r.Usage[fr]
+	}
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	parentAvailable := available(node.Parent(), fr, enforceBorrowLimit)
+
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); enforceBorrowLimit && borrowingLimit != nil {
+		storedInParent := r.SubtreeQuota[fr] - r.guaranteedQuota(fr)
+		usedInParent := max(0, r.Usage[fr]-r.guaranteedQuota(fr))
+		withMaxFromParent := storedInParent - usedInParent + *borrowingLimit
+		parentAvailable = min(withMaxFromParent, parentAvailable)
+	}
+	return localAvailable + parentAvailable
+}
+
+// maxAvailable returns the maximum capacity available to this node,
+// assuming no usage, while respecting BorrowingLimits.
+func maxAvailable[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr]
+	}
+	available := r.guaranteedQuota(fr) + maxAvailable(node.Parent(), fr)
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); borrowingLimit != nil {
+		maxWithBorrowing := r.SubtreeQuota[fr] + *borrowingLimit
+		available = min(maxWithBorrowing, available)
+	}
+	return available
+}
+
+// addUsage adds usage to the current node, and bubbles up usage to
+// its Cohort when usage exceeds guaranteedQuota.
+func addUsage[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, val int64) {
+	r := node.getResourceNode()
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	r.Usage[fr] += val
+	if node.HasParent() && val > localAvailable {
+		deltaParentUsage := val - localAvailable
+		addUsage(node.Parent(), fr, deltaParentUsage)
+	}
+}
+
+// removeUsage removes usage from the current node, and removes usage
+// past guaranteedQuota that it was storing in its Cohort.
+func removeUsage[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, val int64) {
+	r := node.getResourceNode()
+	usageStoredInParent := r.Usage[fr] - r.guaranteedQuota(fr)
+	r.Usage[fr] -= val
+	if usageStoredInParent <= 0 || !node.HasParent() {
+		return
+	}
+	deltaParentUsage := min(val, usageStoredInParent)
+	removeUsage(node.Parent(), fr, deltaParentUsage)
+}
+
+func updateClusterQueueResourceNode(cq *clusterQueue) {
```

  it looks like this should live in clusterqueue.go, and probably a member function.

- **pkg/cache/resource_node.go:163** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T19:35:48Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
+	getResourceNode() ResourceNode
+
+	HasParent() bool
+	Parent() T
+}
+
+// available determines how much capacity remains for the current
+// node, taking into account usage and BorrowingLimits.
+//
+// This function may return a negative number in the case of
+// overadmission - e.g. capacity was removed or the node moved to
+// another Cohort.
+//
+// We add an option to ignore the borrowing limit, to support
+// features.MultiplePreemptions=false path. This will be cleaned up in
+// v0.10, when we delete legacy logic.
+func available[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, enforceBorrowLimit bool) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr] - r.Usage[fr]
+	}
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	parentAvailable := available(node.Parent(), fr, enforceBorrowLimit)
+
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); enforceBorrowLimit && borrowingLimit != nil {
+		storedInParent := r.SubtreeQuota[fr] - r.guaranteedQuota(fr)
+		usedInParent := max(0, r.Usage[fr]-r.guaranteedQuota(fr))
+		withMaxFromParent := storedInParent - usedInParent + *borrowingLimit
+		parentAvailable = min(withMaxFromParent, parentAvailable)
+	}
+	return localAvailable + parentAvailable
+}
+
+// maxAvailable returns the maximum capacity available to this node,
+// assuming no usage, while respecting BorrowingLimits.
+func maxAvailable[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr]
+	}
+	available := r.guaranteedQuota(fr) + maxAvailable(node.Parent(), fr)
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); borrowingLimit != nil {
+		maxWithBorrowing := r.SubtreeQuota[fr] + *borrowingLimit
+		available = min(maxWithBorrowing, available)
+	}
+	return available
+}
+
+// addUsage adds usage to the current node, and bubbles up usage to
+// its Cohort when usage exceeds guaranteedQuota.
+func addUsage[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, val int64) {
+	r := node.getResourceNode()
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	r.Usage[fr] += val
+	if node.HasParent() && val > localAvailable {
+		deltaParentUsage := val - localAvailable
+		addUsage(node.Parent(), fr, deltaParentUsage)
+	}
+}
+
+// removeUsage removes usage from the current node, and removes usage
+// past guaranteedQuota that it was storing in its Cohort.
+func removeUsage[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, val int64) {
+	r := node.getResourceNode()
+	usageStoredInParent := r.Usage[fr] - r.guaranteedQuota(fr)
+	r.Usage[fr] -= val
+	if usageStoredInParent <= 0 || !node.HasParent() {
+		return
+	}
+	deltaParentUsage := min(val, usageStoredInParent)
+	removeUsage(node.Parent(), fr, deltaParentUsage)
+}
+
+func updateClusterQueueResourceNode(cq *clusterQueue) {
+	cq.resourceNode.SubtreeQuota = make(resources.FlavorResourceQuantities, len(cq.resourceNode.Quotas))
+	for fr, quota := range cq.resourceNode.Quotas {
+		cq.resourceNode.SubtreeQuota[fr] = quota.Nominal
+	}
+}
+
+func updateCohortResourceNode(cohort *cohort) {
```

  And this wherever cohort is defined.

- **pkg/cache/resource_node.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T19:37:30Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
```

  if you can't apply any of my other remarks, then this should be renamed to `hierarchicalResourceNode`. But I hope we can get rid of this interface.

- **pkg/cache/clusterqueue_snapshot.go:107** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-26T19:54:41Z
```diff
@@ -62,43 +67,29 @@ func (c *ClusterQueueSnapshot) Fits(frq resources.FlavorResourceQuantities) bool
 }
 
 func (c *ClusterQueueSnapshot) QuotaFor(fr resources.FlavorResource) *ResourceQuota {
-	return c.Quotas[fr]
+	return c.ResourceNode.Quotas[fr]
 }
 
 func (c *ClusterQueueSnapshot) Borrowing(fr resources.FlavorResource) bool {
 	return c.BorrowingWith(fr, 0)
 }
 
 func (c *ClusterQueueSnapshot) BorrowingWith(fr resources.FlavorResource, val int64) bool {
-	return c.Usage[fr]+val > c.nominal(fr)
+	return c.usageFor(fr)+val > c.QuotaFor(fr).nominalSafe()
 }
 
+// Available returns the current capacity available, before preempting
+// any workloads. Includes local capacity and capacity borrowed from
+// Cohort.
 func (c *ClusterQueueSnapshot) Available(fr resources.FlavorResource) int64 {
-	if c.Cohort == nil {
-		return max(0, c.nominal(fr)-c.Usage[fr])
-	}
-	capacityAvailable := c.RequestableCohortQuota(fr) - c.UsedCohortQuota(fr)
-
-	// if the borrowing limit exists, we cap our available capacity by the borrowing limit.
-	if borrowingLimit := c.borrowingLimit(fr); borrowingLimit != nil {
-		withBorrowingRemaining := c.nominal(fr) + *borrowingLimit - c.Usage[fr]
-		capacityAvailable = min(capacityAvailable, withBorrowingRemaining)
-	}
-	return max(0, capacityAvailable)
-}
-
-func (c *ClusterQueueSnapshot) nominal(fr resources.FlavorResource) int64 {
-	if quota := c.QuotaFor(fr); quota != nil {
-		return quota.Nominal
-	}
-	return 0
+	return available(c, fr, true)
 }
 
-func (c *ClusterQueueSnapshot) borrowingLimit(fr resources.FlavorResource) *int64 {
-	if quota := c.QuotaFor(fr); quota != nil {
-		return quota.BorrowingLimit
-	}
-	return nil
+// MaxAvailable returns the largest workload this ClusterQueue could
+// possibly admit, accounting for its capacity and capacity borrowed
+// its from Cohort.
+func (c *ClusterQueueSnapshot) MaxAvailable(fr resources.FlavorResource) int64 {
```

  ```suggestion
  func (c *ClusterQueueSnapshot) PotentialAvailable(fr resources.FlavorResource) int64 {
  ```
  or `AvailableIfEmpty`?
  
  When reading the changes in `flavorassigner.go`, it's not obvious what the difference is between `Available` and `MaxAvailable`.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-27T14:16:03Z

- **pkg/cache/cache_test.go:530** by [@gabesaba](https://github.com/gabesaba) — 2024-08-27T14:16:03Z
```diff
@@ -530,7 +529,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 				},
 				"c": {
 					Name:                          "c",
-					AllocatableResourceGeneration: 1,
+					AllocatableResourceGeneration: 2,
```

  As now we update the generation when a CQ changes cohorts

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-27T14:40:06Z

- **pkg/cache/resource_node.go:50** by [@gabesaba](https://github.com/gabesaba) — 2024-08-27T14:40:05Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
```

  Removed the copy from immutable types, and added a comment.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-08-28T10:32:49Z

- **pkg/cache/resource_node.go:156** by [@gabesaba](https://github.com/gabesaba) — 2024-08-28T10:08:15Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
+	getResourceNode() ResourceNode
+
+	HasParent() bool
+	Parent() T
+}
+
+// available determines how much capacity remains for the current
+// node, taking into account usage and BorrowingLimits.
+//
+// This function may return a negative number in the case of
+// overadmission - e.g. capacity was removed or the node moved to
+// another Cohort.
+//
+// We add an option to ignore the borrowing limit, to support
+// features.MultiplePreemptions=false path. This will be cleaned up in
+// v0.10, when we delete legacy logic.
+func available[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, enforceBorrowLimit bool) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr] - r.Usage[fr]
+	}
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	parentAvailable := available(node.Parent(), fr, enforceBorrowLimit)
+
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); enforceBorrowLimit && borrowingLimit != nil {
+		storedInParent := r.SubtreeQuota[fr] - r.guaranteedQuota(fr)
+		usedInParent := max(0, r.Usage[fr]-r.guaranteedQuota(fr))
+		withMaxFromParent := storedInParent - usedInParent + *borrowingLimit
+		parentAvailable = min(withMaxFromParent, parentAvailable)
+	}
+	return localAvailable + parentAvailable
+}
+
+// maxAvailable returns the maximum capacity available to this node,
+// assuming no usage, while respecting BorrowingLimits.
+func maxAvailable[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource) int64 {
+	r := node.getResourceNode()
+	if !node.HasParent() {
+		return r.SubtreeQuota[fr]
+	}
+	available := r.guaranteedQuota(fr) + maxAvailable(node.Parent(), fr)
+	if borrowingLimit := r.Quotas[fr].borrowingLimitSafe(); borrowingLimit != nil {
+		maxWithBorrowing := r.SubtreeQuota[fr] + *borrowingLimit
+		available = min(maxWithBorrowing, available)
+	}
+	return available
+}
+
+// addUsage adds usage to the current node, and bubbles up usage to
+// its Cohort when usage exceeds guaranteedQuota.
+func addUsage[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, val int64) {
+	r := node.getResourceNode()
+	localAvailable := max(0, r.guaranteedQuota(fr)-r.Usage[fr])
+	r.Usage[fr] += val
+	if node.HasParent() && val > localAvailable {
+		deltaParentUsage := val - localAvailable
+		addUsage(node.Parent(), fr, deltaParentUsage)
+	}
+}
+
+// removeUsage removes usage from the current node, and removes usage
+// past guaranteedQuota that it was storing in its Cohort.
+func removeUsage[T wiredResourceNode[T]](node wiredResourceNode[T], fr resources.FlavorResource, val int64) {
+	r := node.getResourceNode()
+	usageStoredInParent := r.Usage[fr] - r.guaranteedQuota(fr)
+	r.Usage[fr] -= val
+	if usageStoredInParent <= 0 || !node.HasParent() {
+		return
+	}
+	deltaParentUsage := min(val, usageStoredInParent)
+	removeUsage(node.Parent(), fr, deltaParentUsage)
+}
+
+func updateClusterQueueResourceNode(cq *clusterQueue) {
```

  I'm conflicted here, as while it would be nice to keep all CQ/Cohort functions in the same place, I think it will make the code more understandable when the resource_node logic resides next to each other. Especially the update logic, as this becomes more involved in the proper hierarchical case (following PRs)
  
  Perhaps it is easier to justify leaving these here, given that these are functions of CQ/Cohort, rather than methods for those types?

- **pkg/cache/resource_node.go:65** by [@gabesaba](https://github.com/gabesaba) — 2024-08-28T10:22:03Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
+	// Quotas are the ResourceQuotas specified for the current
+	// node.
+	Quotas map[resources.FlavorResource]*ResourceQuota
+	// SubtreeQuota is the sum of the node's quota, as well as
+	// resources available from its children, constrained by
+	// LendingLimits.
+	SubtreeQuota resources.FlavorResourceQuantities
+	// Usage is the quantity which counts against this node's
+	// SubtreeQuota. For ClusterQueues, this is simply its
+	// usage. For Cohorts, this is the sum of child usage past
+	// guaranteedQuotas.
+	Usage resources.FlavorResourceQuantities
+}
+
+func NewResourceNode() ResourceNode {
+	return ResourceNode{
+		Quotas:       make(map[resources.FlavorResource]*ResourceQuota),
+		SubtreeQuota: make(resources.FlavorResourceQuantities),
+		Usage:        make(resources.FlavorResourceQuantities),
+	}
+}
+
+func (r ResourceNode) Clone() ResourceNode {
+	return ResourceNode{
+		Quotas:       maps.Clone(r.Quotas),
+		SubtreeQuota: maps.Clone(r.SubtreeQuota),
+		Usage:        maps.Clone(r.Usage),
+	}
+}
+
+// guaranteedQuota is the capacity which will not be lent the node's
+// Cohort.
+func (r ResourceNode) guaranteedQuota(fr resources.FlavorResource) int64 {
+	if lendingLimit := r.Quotas[fr].lendingLimitSafe(); lendingLimit != nil {
+		return max(0, r.SubtreeQuota[fr]-*lendingLimit)
+	}
+	return 0
+}
+
+type wiredResourceNode[T any] interface {
```

  Renamed to hierarchicalResourceNode. I was able to get rid of the generics, by defining method for each of the types to return its parent as a `hierarchicalResourceNode`

- **pkg/cache/resource_node.go:27** by [@gabesaba](https://github.com/gabesaba) — 2024-08-28T10:23:37Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
```

  This becomes tricky, as we have different hierarchy types for Cohorts and ClusterQueues. So we will have to use generics here. Additionally, I'd prefer to keep the hierarchy aspect and the resource aspect of the nodes orthogonal (at the price of a new interface, `hierarchicalResourceNode` for combining those aspects...)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-29T14:41:33Z

- **pkg/cache/resource_node.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-29T14:41:33Z
```diff
@@ -0,0 +1,157 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type ResourceNode struct {
```

  Yet, this file contains functions that assume both aspects.
  
  I think it could have been worth the tradeoff, for example, to avoid the function parentHRN and just use `Parent()`.
  
  But let's move forward with this.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-29T14:49:16Z

/lgtm
