# PR #291: Add metric for admitted active workloads

**Summary**: Add metric for admitted active workloads

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/291

**Last updated**: 2022-07-12T17:46:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `admitted_metrics` → `main`
- **Created**: 2022-07-07T18:24:59Z
- **Updated**: 2022-07-12T17:46:23Z
- **Merged**: 2022-07-12T17:46:23Z
- **Closed**: 2022-07-12T17:46:23Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 7   **+172 / -53**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Add a metric that keeps track of admitted workloads that are still active (running).

This requires keeping track of admitted workloads per queue in the cache, which can also be used to report queue status.

#### Which issue(s) this PR fixes:

Part of #199 

#### Special notes for your reviewer:

The metric will under-report active workloads if the queue is deleted, or the queue switches cluster queue, which should be disallowed with finalizers and webhooks #171

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-07-07T18:25:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/291#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-07T20:41:57Z

/hold
There is some very weird flakiness where a cluster queue name disappears. I'm investigating.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-07T21:04:45Z

/hold cancel
/test pull-kueue-test-integration-main

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-07-12T17:44:22Z

/lgtm

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-07T21:31:20Z

- **pkg/metrics/metrics.go:65** by [@ahg-g](https://github.com/ahg-g) — 2022-07-07T21:31:20Z
```diff
@@ -54,7 +54,16 @@ var (
 			Subsystem: subsystemName,
 			Name:      "pending_workloads",
 			Help:      "Number of pending workloads, per queue and cluster_queue.",
-		}, []string{"cluster_queue", "queue"})
+		}, []string{"cluster_queue", "queue"},
+	)
+
+	AdmittedActiveWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: subsystemName,
+			Name:      "admitted_active_workloads",
+			Help:      "Number of admitted workloads that are active (unsuspended and not finished)",
+		}, []string{"cluster_queue", "queue"},
```

  Since users can create as many queues as they wish, aren't we concerned about this dimension getting too big? do you remember what the limits where?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-08T16:22:25Z

- **pkg/metrics/metrics.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-08T16:22:24Z
```diff
@@ -54,7 +54,16 @@ var (
 			Subsystem: subsystemName,
 			Name:      "pending_workloads",
 			Help:      "Number of pending workloads, per queue and cluster_queue.",
-		}, []string{"cluster_queue", "queue"})
+		}, []string{"cluster_queue", "queue"},
+	)
+
+	AdmittedActiveWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: subsystemName,
+			Name:      "admitted_active_workloads",
+			Help:      "Number of admitted workloads that are active (unsuspended and not finished)",
+		}, []string{"cluster_queue", "queue"},
```

  There is no limit. The more queues there are, the more memory we use. Let me try to get more information on how bad this could be for a gauge.
  
  Note that when a queue or clusterQueue is removed, I'm deleting the label values .

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-11T18:33:04Z

- **pkg/metrics/metrics.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-11T18:33:04Z
```diff
@@ -54,7 +54,16 @@ var (
 			Subsystem: subsystemName,
 			Name:      "pending_workloads",
 			Help:      "Number of pending workloads, per queue and cluster_queue.",
-		}, []string{"cluster_queue", "queue"})
+		}, []string{"cluster_queue", "queue"},
+	)
+
+	AdmittedActiveWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: subsystemName,
+			Name:      "admitted_active_workloads",
+			Help:      "Number of admitted workloads that are active (unsuspended and not finished)",
+		}, []string{"cluster_queue", "queue"},
```

  We chatted offline and concluded that it's best to only keep track of active workloads per clusterqueue, similarly to #293

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-12T17:04:08Z

- **pkg/workload/workload.go:37** by [@ahg-g](https://github.com/ahg-g) — 2022-07-12T16:54:14Z
```diff
@@ -34,7 +34,8 @@ type Info struct {
 	Obj *kueue.Workload
 	// list of total resources requested by the podsets.
 	TotalRequests []PodSetResources
-	// Populated from queue.
+	// Populated from the queue during admission of from the admission field if
```

  typo

- **pkg/workload/workload.go:54** by [@ahg-g](https://github.com/ahg-g) — 2022-07-12T16:54:35Z
```diff
@@ -45,10 +46,14 @@ type PodSetResources struct {
 }
 
 func NewInfo(w *kueue.Workload) *Info {
-	return &Info{
+	info := &Info{
 		Obj:           w,
 		TotalRequests: totalRequests(&w.Spec),
 	}
+	if w.Spec.Admission != nil {
+		info.ClusterQueue = string(w.Spec.Admission.ClusterQueue)
```

  was this a bug?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-12T17:42:09Z

- **pkg/workload/workload.go:54** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-12T17:40:21Z
```diff
@@ -45,10 +46,14 @@ type PodSetResources struct {
 }
 
 func NewInfo(w *kueue.Workload) *Info {
-	return &Info{
+	info := &Info{
 		Obj:           w,
 		TotalRequests: totalRequests(&w.Spec),
 	}
+	if w.Spec.Admission != nil {
+		info.ClusterQueue = string(w.Spec.Admission.ClusterQueue)
```

  No, we were only using this field during scheduling, where the information comes from the queues, rather than the Workload.
  
  Now I'm populating the field when adding the workload to the cache, for consistency and convenience (and to avoid potential bugs in the future).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-12T17:42:22Z

- **pkg/workload/workload.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-12T17:42:22Z
```diff
@@ -34,7 +34,8 @@ type Info struct {
 	Obj *kueue.Workload
 	// list of total resources requested by the podsets.
 	TotalRequests []PodSetResources
-	// Populated from queue.
+	// Populated from the queue during admission of from the admission field if
```

  Done
