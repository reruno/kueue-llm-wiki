# PR #2921: Add a guide for deleting cluster queues.

**Summary**: Add a guide for deleting cluster queues.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2921

**Last updated**: 2025-03-05T06:47:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `docs/add-a-guide-for-deleting-cluster-queues` → `main`
- **Created**: 2024-08-28T06:05:37Z
- **Updated**: 2025-03-05T06:47:02Z
- **Merged**: 2024-08-28T16:14:57Z
- **Closed**: 2024-08-28T16:14:57Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 3   **+118 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a guide for deleting cluster queues.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2835

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-28T06:05:53Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3e92f2b5591cf499c62c84a4957de78fb445ee3b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66cf455558452e00073315af |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2921--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2921--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI5MjEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.9LuQdKyY7sAr_jjFEzry-OW_jSrRcFFe4V2x9tV7UGM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T06:25:10Z

/cc @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T15:46:22Z

/lgtm
/approve
/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-08-28T15:46:25Z

@alculquicondor: once the present PR merges, I will cherry-pick it on top of website in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2921#issuecomment-2315713743):

>/lgtm
>/approve
>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-28T15:46:29Z

LGTM label has been added.  <details>Git tree hash: e347d949a87ce519c63cd430c399d67178e9cb94</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-08-28T15:46:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2921#issuecomment-2315713743" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2921#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-08-28T16:15:36Z

@alculquicondor: new pull request created: #2927

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2921#issuecomment-2315713743):

>/lgtm
>/approve
>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-28T14:58:58Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:7** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:53:38Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
```

  ```suggestion
  Deleting a ClusterQueue might require additional steps when it has active Workloads. 
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:12** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:54:01Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
```

  ```suggestion
  For this example, assume your ClusterQueue is named `my-cq`.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:16** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:54:13Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run:
```

  ```suggestion
  When you run the following command:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:31** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:54:46Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run:
+
+```sh
+kubectl delete clusterqueue my-cq
+```
+
+you are initiating the deletion of the ClusterQueue object. 
+However, if there are Workloads still utilizing the ClusterQueue, the command may hang or take some time to complete.
+
+
+## Why does this happen?
+
+Kueue creates a [Workload](/docs/concepts/workload/) object for each Job to track its admission status. 
+If a Workload is admitted, it is associated with a specific ClusterQueue. 
+Kueue uses a [finalizer](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/) 
+called kueue.x-k8s.io/resource-in-use to prevent deletion of the ClusterQueue while resources are still in use. 
```

  ```suggestion
  called `kueue.x-k8s.io/resource-in-use` to prevent deletion of the ClusterQueue while resources are still in use. 
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:37** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:56:11Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run:
+
+```sh
+kubectl delete clusterqueue my-cq
+```
+
+you are initiating the deletion of the ClusterQueue object. 
+However, if there are Workloads still utilizing the ClusterQueue, the command may hang or take some time to complete.
+
+
+## Why does this happen?
+
+Kueue creates a [Workload](/docs/concepts/workload/) object for each Job to track its admission status. 
+If a Workload is admitted, it is associated with a specific ClusterQueue. 
+Kueue uses a [finalizer](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/) 
+called kueue.x-k8s.io/resource-in-use to prevent deletion of the ClusterQueue while resources are still in use. 
+Consequently, a ClusterQueue with this finalizer cannot be deleted until all resources are released.
+
+
+## How to resolve this issue?
+
+To delete a ClusterQueue, first stop it by applying a [StopPolicy](/docs/concepts/cluster_queue/#stoppolicy) with `HoldAndDrain`. 
```

  ```suggestion
  To delete a ClusterQueue, you can first stop all the admitted Workloads by applying the [StopPolicy](/docs/concepts/cluster_queue/#stoppolicy) `HoldAndDrain`. 
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:38** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:56:53Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run:
+
+```sh
+kubectl delete clusterqueue my-cq
+```
+
+you are initiating the deletion of the ClusterQueue object. 
+However, if there are Workloads still utilizing the ClusterQueue, the command may hang or take some time to complete.
+
+
+## Why does this happen?
+
+Kueue creates a [Workload](/docs/concepts/workload/) object for each Job to track its admission status. 
+If a Workload is admitted, it is associated with a specific ClusterQueue. 
+Kueue uses a [finalizer](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/) 
+called kueue.x-k8s.io/resource-in-use to prevent deletion of the ClusterQueue while resources are still in use. 
+Consequently, a ClusterQueue with this finalizer cannot be deleted until all resources are released.
+
+
+## How to resolve this issue?
+
+To delete a ClusterQueue, first stop it by applying a [StopPolicy](/docs/concepts/cluster_queue/#stoppolicy) with `HoldAndDrain`. 
+This will evict all associated Workloads and release the resources, allowing the ClusterQueue to be deleted without issues.
```

  ```suggestion
  This will evict all associated Workloads and release the resources, allowing the ClusterQueue to be deleted immediately.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:35** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:58:20Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run:
+
+```sh
+kubectl delete clusterqueue my-cq
+```
+
+you are initiating the deletion of the ClusterQueue object. 
+However, if there are Workloads still utilizing the ClusterQueue, the command may hang or take some time to complete.
+
+
+## Why does this happen?
+
+Kueue creates a [Workload](/docs/concepts/workload/) object for each Job to track its admission status. 
+If a Workload is admitted, it is associated with a specific ClusterQueue. 
+Kueue uses a [finalizer](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/) 
+called kueue.x-k8s.io/resource-in-use to prevent deletion of the ClusterQueue while resources are still in use. 
+Consequently, a ClusterQueue with this finalizer cannot be deleted until all resources are released.
+
+
+## How to resolve this issue?
```

  Maybe we can remove this section and just leave `How to stop the ClusterQueue?` below.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:88** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T14:58:46Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run:
+
+```sh
+kubectl delete clusterqueue my-cq
+```
+
+you are initiating the deletion of the ClusterQueue object. 
+However, if there are Workloads still utilizing the ClusterQueue, the command may hang or take some time to complete.
+
+
+## Why does this happen?
+
+Kueue creates a [Workload](/docs/concepts/workload/) object for each Job to track its admission status. 
+If a Workload is admitted, it is associated with a specific ClusterQueue. 
+Kueue uses a [finalizer](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/) 
+called kueue.x-k8s.io/resource-in-use to prevent deletion of the ClusterQueue while resources are still in use. 
+Consequently, a ClusterQueue with this finalizer cannot be deleted until all resources are released.
+
+
+## How to resolve this issue?
+
+To delete a ClusterQueue, first stop it by applying a [StopPolicy](/docs/concepts/cluster_queue/#stoppolicy) with `HoldAndDrain`. 
+This will evict all associated Workloads and release the resources, allowing the ClusterQueue to be deleted without issues.
+
+
+## How to identify the Workloads related to the ClusterQueue?
+
+To find Workloads linked to a specific ClusterQueue, you can use the following steps:
+
+### Using `kueuectl`
+
+{{% alert title="Note" color="primary" %}}
+If you don't have `kueuectl` installed, follow the [installation guide](/docs/reference/kubectl-kueue/installation/).
+{{% /alert %}}
+
+```shell
+kueuectl list workload --clusterqueue my-cq -A
+```
+
+Example output:
+
+```shell
+NAMESPACE   NAME                     JOB TYPE   JOB NAME       LOCALQUEUE   CLUSTERQUEUE    STATUS     POSITION IN QUEUE   AGE
+default     my-job-job-4gk8s-7b737   job        my-job-4gk8s   my-lq        my-cq           ADMITTED                       27m
+default     my-job-job-826hp-caa72   job        my-job-826hp   my-lq        my-cq           ADMITTED                       1h
+```
+
+### Using `kubectl` and `grep`
+
+Alternatively, you can use `kubectl` with `grep`:
+
+```shell
+kueuectl get workload -A | grep my-cq
+```
+
+Example output:
+
+```shell
+default     my-job-4gk8s-7b737   my-lq   my-cq   True                  30m
+default     my-job-826hp-caa72   my-lq   my-cq   True                  30m
+```
+
+{{% alert title="Note" color="primary" %}}
+The grep command only filters lines based on the specified pattern. 
+It might return additional Workloads if the Workload name or LocalQueue name includes the same name as the ClusterQueue.
+{{% /alert %}}
+
+## How to stop the ClusterQueue?
+
+{{% alert title="Note" color="primary" %}}
+If no Workloads are found for the specified ClusterQueue, you can skip the stopping step and proceed directly to deleting the ClusterQueue.
+{{% /alert %}}
+
+### Using `kueuectl`
+
+To stop the ClusterQueue, run:
+
+```shell
+kueuectl stop clusterqueue my-cq
```

  does this Drain by default?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-08-28T15:01:45Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:88** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-08-28T15:01:45Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue can be a bit complex when there are active Workloads. 
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, let's assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run:
+
+```sh
+kubectl delete clusterqueue my-cq
+```
+
+you are initiating the deletion of the ClusterQueue object. 
+However, if there are Workloads still utilizing the ClusterQueue, the command may hang or take some time to complete.
+
+
+## Why does this happen?
+
+Kueue creates a [Workload](/docs/concepts/workload/) object for each Job to track its admission status. 
+If a Workload is admitted, it is associated with a specific ClusterQueue. 
+Kueue uses a [finalizer](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/) 
+called kueue.x-k8s.io/resource-in-use to prevent deletion of the ClusterQueue while resources are still in use. 
+Consequently, a ClusterQueue with this finalizer cannot be deleted until all resources are released.
+
+
+## How to resolve this issue?
+
+To delete a ClusterQueue, first stop it by applying a [StopPolicy](/docs/concepts/cluster_queue/#stoppolicy) with `HoldAndDrain`. 
+This will evict all associated Workloads and release the resources, allowing the ClusterQueue to be deleted without issues.
+
+
+## How to identify the Workloads related to the ClusterQueue?
+
+To find Workloads linked to a specific ClusterQueue, you can use the following steps:
+
+### Using `kueuectl`
+
+{{% alert title="Note" color="primary" %}}
+If you don't have `kueuectl` installed, follow the [installation guide](/docs/reference/kubectl-kueue/installation/).
+{{% /alert %}}
+
+```shell
+kueuectl list workload --clusterqueue my-cq -A
+```
+
+Example output:
+
+```shell
+NAMESPACE   NAME                     JOB TYPE   JOB NAME       LOCALQUEUE   CLUSTERQUEUE    STATUS     POSITION IN QUEUE   AGE
+default     my-job-job-4gk8s-7b737   job        my-job-4gk8s   my-lq        my-cq           ADMITTED                       27m
+default     my-job-job-826hp-caa72   job        my-job-826hp   my-lq        my-cq           ADMITTED                       1h
+```
+
+### Using `kubectl` and `grep`
+
+Alternatively, you can use `kubectl` with `grep`:
+
+```shell
+kueuectl get workload -A | grep my-cq
+```
+
+Example output:
+
+```shell
+default     my-job-4gk8s-7b737   my-lq   my-cq   True                  30m
+default     my-job-826hp-caa72   my-lq   my-cq   True                  30m
+```
+
+{{% alert title="Note" color="primary" %}}
+The grep command only filters lines based on the specified pattern. 
+It might return additional Workloads if the Workload name or LocalQueue name includes the same name as the ClusterQueue.
+{{% /alert %}}
+
+## How to stop the ClusterQueue?
+
+{{% alert title="Note" color="primary" %}}
+If no Workloads are found for the specified ClusterQueue, you can skip the stopping step and proceed directly to deleting the ClusterQueue.
+{{% /alert %}}
+
+### Using `kueuectl`
+
+To stop the ClusterQueue, run:
+
+```shell
+kueuectl stop clusterqueue my-cq
```

  Yes, we have `--keep-already-running` flag (look [here](https://kueue.sigs.k8s.io/docs/reference/kubectl-kueue/commands/kueuectl_stop/kueuectl_stop_clusterqueue/#options.))

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-08-28T15:38:48Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_delete_clusterqueue.md:91** by [@alculquicondor](https://github.com/alculquicondor) — 2024-08-28T15:38:42Z
```diff
@@ -0,0 +1,122 @@
+---
+title: "Troubleshooting delete ClusterQueue"
+date: 2024-08-28
+weight: 5
+---
+
+Deleting a ClusterQueue might require additional steps when it has active Workloads.
+
+This guide provides a clear approach to safely deleting a ClusterQueue,
+explaining the process and offering alternative methods to handle specific scenarios.
+
+For this example, assume your ClusterQueue is named `my-cq`.
+
+## Understanding `kubectl delete clusterqueue`
+
+When you run the following command:
+
+```sh
+kubectl delete clusterqueue my-cq
+```
+
+you are initiating the deletion of the ClusterQueue object. 
+However, if there are Workloads still utilizing the ClusterQueue, the command may hang or take some time to complete.
+
+
+## Why does this happen?
+
+Kueue creates a [Workload](/docs/concepts/workload/) object for each Job to track its admission status. 
+If a Workload is admitted, it is associated with a specific ClusterQueue. 
+Kueue uses a [finalizer](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/) 
+called `kueue.x-k8s.io/resource-in-use` to prevent deletion of the ClusterQueue while resources are still in use. 
+Consequently, a ClusterQueue with this finalizer cannot be deleted until all resources are released.
+
+
+## How to resolve this issue?
+
+To delete a ClusterQueue, you can first stop all the admitted Workloads by applying the [StopPolicy](/docs/concepts/cluster_queue/#stoppolicy) `HoldAndDrain`.  
+This will evict all associated Workloads and release the resources, allowing the ClusterQueue to be deleted immediately.
+
+
+## How to identify the Workloads related to the ClusterQueue?
+
+To find Workloads linked to a specific ClusterQueue, you can use the following steps:
+
+### Using `kueuectl`
+
+{{% alert title="Note" color="primary" %}}
+If you don't have `kueuectl` installed, follow the [installation guide](/docs/reference/kubectl-kueue/installation/).
+{{% /alert %}}
+
+```shell
+kueuectl list workload --clusterqueue my-cq -A
+```
+
+Example output:
+
+```shell
+NAMESPACE   NAME                     JOB TYPE   JOB NAME       LOCALQUEUE   CLUSTERQUEUE    STATUS     POSITION IN QUEUE   AGE
+default     my-job-job-4gk8s-7b737   job        my-job-4gk8s   my-lq        my-cq           ADMITTED                       27m
+default     my-job-job-826hp-caa72   job        my-job-826hp   my-lq        my-cq           ADMITTED                       1h
+```
+
+### Using `kubectl` and `grep`
+
+Alternatively, you can use `kubectl` with `grep`:
+
+```shell
+kueuectl get workload -A | grep my-cq
+```
+
+Example output:
+
+```shell
+default     my-job-4gk8s-7b737   my-lq   my-cq   True                  30m
+default     my-job-826hp-caa72   my-lq   my-cq   True                  30m
+```
+
+{{% alert title="Note" color="primary" %}}
+The grep command only filters lines based on the specified pattern. 
+It might return additional Workloads if the Workload name or LocalQueue name includes the same name as the ClusterQueue.
+{{% /alert %}}
+
+## How to stop the ClusterQueue?
+
+{{% alert title="Note" color="primary" %}}
+If no Workloads are found for the specified ClusterQueue, you can skip the stopping step and proceed directly to deleting the ClusterQueue.
+{{% /alert %}}
+
+### Using `kueuectl`
+
+To stop the ClusterQueue, run:
```

  ```suggestion
  To stop all the jobs in a ClusterQueue and prevent new jobs from being admitted by it, run the following command:
  ```
