# PR #2939: Support Cohorts with Resources in cache/hierarchy packages

**Summary**: Support Cohorts with Resources in cache/hierarchy packages

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2939

**Last updated**: 2024-09-03T14:04:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `cache_resource_cohort` → `main`
- **Created**: 2024-08-30T12:01:47Z
- **Updated**: 2024-09-03T14:04:41Z
- **Merged**: 2024-09-03T09:37:16Z
- **Closed**: 2024-09-03T09:37:16Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@macsko](https://github.com/macsko)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 12   **+427 / -89**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
Update cache to support Cohorts with resources. Update hierarchy to support explicit Cohorts. Part of #79

#### Special notes for your reviewer:
commit 1 : move files and refactor parsing
commit 2: support Cohorts which provide resources in `cache`. support explicit Cohorts in `hierarchy`

Users cannot use this functionality until we wire up reconciler and webhook. In the interest of keeping PRs small, I will publish those in separate PRs.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-08-30T12:02:02Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bcdebab02af2db0c6436a104194975307d8d2432 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66d6c97d2c493400084cf660 |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-08-30T12:02:03Z

/assign @macsko

### Comment by [@macsko](https://github.com/macsko) — 2024-09-03T08:34:55Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-03T08:34:59Z

LGTM label has been added.  <details>Git tree hash: 5c6066569fcfec1b165141c8c4c496c8ae093556</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-03T08:38:43Z

/assign @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-03T09:18:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2939#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2939#pullrequestreview-2276877764" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@macsko](https://github.com/macsko) (commented) — 2024-09-02T10:42:20Z

- **pkg/hierarchy/manager_test.go:210** by [@macsko](https://github.com/macsko) — 2024-09-02T10:41:00Z
```diff
@@ -158,11 +166,51 @@ func TestManager(t *testing.T) {
 			wantCohorts: sets.New("cohort"),
 			wantCqEdge:  []cqEdge{{"queue1", "cohort"}},
 		},
+		"explicit cohort": {
+			operations: func(m M) {
+				m.AddCohort(newCohort("cohort"))
+			},
+			wantCohorts: sets.New("cohort"),
+		},
+		"delete explicit cohort": {
+			operations: func(m M) {
+				m.AddCohort(newCohort("cohort"))
+				m.DeleteCohort("cohort")
+			},
+		},
+		"delete explicit cohort idempotent": {
+			operations: func(m M) {
+				m.DeleteCohort("cohort")
+				m.AddCohort(newCohort("cohort"))
+				m.DeleteCohort("cohort")
+				m.DeleteCohort("cohort")
+			},
+		},
+		"explicit cohort persists after child deleted": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue"))
+				m.UpdateClusterQueueEdge("queue", "cohort")
+				m.AddCohort(newCohort("cohort"))
+				m.DeleteClusterQueue("queue")
+			},
+			wantCohorts: sets.New("cohort"),
+		},
+		"explicit cohort downgraded to implicit cohort": {
+			operations: func(m M) {
+				m.AddCohort(newCohort("cohort"))
+				m.AddClusterQueue(newCq("queue"))
+				m.UpdateClusterQueueEdge("queue", "cohort")
+				m.DeleteCohort("cohort")
+			},
+			wantCohorts: sets.New("cohort"),
+			wantCqs:     sets.New("queue"),
+			wantCqEdge:  []cqEdge{{"queue", "cohort"}},
+		},
```

  What about testing the `cleanupCohort` method? Like: 
  ```go
  m.AddCohort(newCohort("cohort"))
  m.AddClusterQueue(newCq("queue"))
  m.UpdateClusterQueueEdge("queue", "cohort")
  m.DeleteCohort("cohort")
  m.DeleteClusterQueue("queue")
  ```

- **pkg/hierarchy/manager.go:79** by [@macsko](https://github.com/macsko) — 2024-09-02T10:41:16Z
```diff
@@ -53,8 +53,35 @@ func (c *Manager[CQ, C]) DeleteClusterQueue(name string) {
 	if cq, ok := c.ClusterQueues[name]; ok {
 		c.unwireClusterQueue(cq)
 		delete(c.ClusterQueues, name)
+	}
+}
+
+func (c *Manager[CQ, C]) AddCohort(cohort C) {
+	cohort.markExplicit()
+	if oldCohort, ok := c.Cohorts[cohort.GetName()]; ok {
+		c.rewireChildren(oldCohort, cohort)
+	}
+	c.Cohorts[cohort.GetName()] = cohort
+}
+
+func (c *Manager[CQ, C]) DeleteCohort(name string) {
+	cohort, ok := c.Cohorts[name]
+	delete(c.Cohorts, name)
+	if !ok || cohort.childCount() == 0 {
 		return
 	}
+	implicitCohort := c.cohortFactory(name)
+	c.Cohorts[implicitCohort.GetName()] = implicitCohort
+	c.rewireChildren(cohort, implicitCohort)
+}
+
+// rewireChildren is used when we are changing a Cohort
+// from an implicit to an explcit Cohort, or vice-versa.
```

  nit: explcit -> explicit

- **pkg/cache/cache.go:414** by [@macsko](https://github.com/macsko) — 2024-09-02T10:41:46Z
```diff
@@ -397,6 +398,27 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	metrics.ClearCacheMetrics(cq.Name)
 }
 
+func (c *Cache) AddCohort(apiCohort *kueuealpha.Cohort) {
+	c.Lock()
+	defer c.Unlock()
+	cohort := newCohort(apiCohort.Name)
+	c.hm.AddCohort(cohort)
+	cohort.updateCohort(apiCohort)
+}
+
+func (c *Cache) DeleteCohort(apiCohort *kueuealpha.Cohort) {
+	c.Lock()
+	defer c.Unlock()
+	c.hm.DeleteCohort(apiCohort.Name)
+
+	// if the cohort still exists after deletion, it means
```

  nit: start with upper case

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-03T08:30:49Z

- **pkg/hierarchy/manager_test.go:210** by [@gabesaba](https://github.com/gabesaba) — 2024-09-03T08:30:49Z
```diff
@@ -158,11 +166,51 @@ func TestManager(t *testing.T) {
 			wantCohorts: sets.New("cohort"),
 			wantCqEdge:  []cqEdge{{"queue1", "cohort"}},
 		},
+		"explicit cohort": {
+			operations: func(m M) {
+				m.AddCohort(newCohort("cohort"))
+			},
+			wantCohorts: sets.New("cohort"),
+		},
+		"delete explicit cohort": {
+			operations: func(m M) {
+				m.AddCohort(newCohort("cohort"))
+				m.DeleteCohort("cohort")
+			},
+		},
+		"delete explicit cohort idempotent": {
+			operations: func(m M) {
+				m.DeleteCohort("cohort")
+				m.AddCohort(newCohort("cohort"))
+				m.DeleteCohort("cohort")
+				m.DeleteCohort("cohort")
+			},
+		},
+		"explicit cohort persists after child deleted": {
+			operations: func(m M) {
+				m.AddClusterQueue(newCq("queue"))
+				m.UpdateClusterQueueEdge("queue", "cohort")
+				m.AddCohort(newCohort("cohort"))
+				m.DeleteClusterQueue("queue")
+			},
+			wantCohorts: sets.New("cohort"),
+		},
+		"explicit cohort downgraded to implicit cohort": {
+			operations: func(m M) {
+				m.AddCohort(newCohort("cohort"))
+				m.AddClusterQueue(newCq("queue"))
+				m.UpdateClusterQueueEdge("queue", "cohort")
+				m.DeleteCohort("cohort")
+			},
+			wantCohorts: sets.New("cohort"),
+			wantCqs:     sets.New("queue"),
+			wantCqEdge:  []cqEdge{{"queue", "cohort"}},
+		},
```

  added

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-03T08:38:17Z

- **.golangci.yaml:60** by [@gabesaba](https://github.com/gabesaba) — 2024-09-03T08:38:16Z
```diff
@@ -57,6 +57,9 @@ issues:
       - staticcheck
       # TODO(#768): Drop when incrementing the API version.
       text: "SA1019: constants.QueueAnnotation is deprecated"
+    - linters:
```

  tracking here: #2965

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-03T09:18:34Z

/approve
