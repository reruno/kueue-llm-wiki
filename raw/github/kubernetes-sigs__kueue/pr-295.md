# PR #295: Track number of active running workloads per queue

**Summary**: Track number of active running workloads per queue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/295

**Last updated**: 2022-07-11T18:59:15Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `admitted_per_queu` → `main`
- **Created**: 2022-07-11T15:25:33Z
- **Updated**: 2022-07-11T18:59:15Z
- **Merged**: 2022-07-11T18:59:15Z
- **Closed**: 2022-07-11T18:59:15Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 11   **+386 / -31**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

This PR doesn't have any user visible changes. The information can be used to populate the information into a Queue status, to improve observability of kueue.

This is a spin off from #291

#### Which issue(s) this PR fixes:

Part of #259

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-07-11T15:25:45Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/295#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-11T15:26:39Z

Part of #259

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-07-11T18:57:57Z

/label tide/merge-method-squash

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-07-11T18:58:05Z

/lgtm

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-11T18:04:52Z

- **pkg/cache/cache.go:132** by [@ahg-g](https://github.com/ahg-g) — 2022-07-11T16:55:32Z
```diff
@@ -118,8 +127,9 @@ type FlavorLimits struct {
 
 func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	cqImpl := &ClusterQueue{
-		Name:      cq.Name,
-		Workloads: map[string]*workload.Info{},
+		Name:            cq.Name,
+		Workloads:       map[string]*workload.Info{},
+		wlCountPerQueue: make(map[string]int),
```

  can we unify the way we allocate the two maps, no preference on my side.

- **pkg/cache/cache.go:118** by [@ahg-g](https://github.com/ahg-g) — 2022-07-11T17:51:45Z
```diff
@@ -107,6 +111,11 @@ type ClusterQueue struct {
 	// that can be matched against the flavors.
 	LabelKeys map[corev1.ResourceName]sets.String
 	Status    ClusterQueueStatus
+
+	// The following fields are not populated in a snapshot.
+
+	// The number of admitted workloads per queue.
+	wlCountPerQueue map[string]int
```

  perhaps `admittedWorkloadsPerQueue`

- **pkg/cache/cache_test.go:1178** by [@ahg-g](https://github.com/ahg-g) — 2022-07-11T17:58:51Z
```diff
@@ -1161,6 +1162,254 @@ func TestClusterQueueUsage(t *testing.T) {
 	}
 }
 
+func TestCacheQueueOperations(t *testing.T) {
+	cqs := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("foo").Obj(),
+		utiltesting.MakeClusterQueue("bar").Obj(),
+	}
+	queues := []*kueue.Queue{
+		utiltesting.MakeQueue("alpha", "ns1").ClusterQueue("foo").Obj(),
+		utiltesting.MakeQueue("beta", "ns2").ClusterQueue("foo").Obj(),
+		utiltesting.MakeQueue("gamma", "ns1").ClusterQueue("bar").Obj(),
+	}
+	workloads := []*kueue.Workload{
+		utiltesting.MakeWorkload("job1", "ns1").Queue("alpha").Admit(utiltesting.MakeAdmission("foo").Obj()).Obj(),
+		utiltesting.MakeWorkload("job2", "ns2").Queue("beta").Admit(utiltesting.MakeAdmission("foo").Obj()).Obj(),
+		utiltesting.MakeWorkload("job3", "ns1").Queue("gamma").Admit(utiltesting.MakeAdmission("bar").Obj()).Obj(),
+		utiltesting.MakeWorkload("job4", "ns2").Queue("beta").Admit(utiltesting.MakeAdmission("foo").Obj()).Obj(),
```

  perhaps add an unadmitted workload? I know a workload that is not yet admitted shouldn't be in the cash, but perhaps worth adding one anyways just to make sure we don't crash if there is such a bug?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-11T18:45:38Z

- **pkg/cache/cache.go:118** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-11T18:40:43Z
```diff
@@ -107,6 +111,11 @@ type ClusterQueue struct {
 	// that can be matched against the flavors.
 	LabelKeys map[corev1.ResourceName]sets.String
 	Status    ClusterQueueStatus
+
+	// The following fields are not populated in a snapshot.
+
+	// The number of admitted workloads per queue.
+	wlCountPerQueue map[string]int
```

  Done

- **pkg/cache/cache_test.go:1178** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-11T18:44:33Z
```diff
@@ -1161,6 +1162,254 @@ func TestClusterQueueUsage(t *testing.T) {
 	}
 }
 
+func TestCacheQueueOperations(t *testing.T) {
+	cqs := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("foo").Obj(),
+		utiltesting.MakeClusterQueue("bar").Obj(),
+	}
+	queues := []*kueue.Queue{
+		utiltesting.MakeQueue("alpha", "ns1").ClusterQueue("foo").Obj(),
+		utiltesting.MakeQueue("beta", "ns2").ClusterQueue("foo").Obj(),
+		utiltesting.MakeQueue("gamma", "ns1").ClusterQueue("bar").Obj(),
+	}
+	workloads := []*kueue.Workload{
+		utiltesting.MakeWorkload("job1", "ns1").Queue("alpha").Admit(utiltesting.MakeAdmission("foo").Obj()).Obj(),
+		utiltesting.MakeWorkload("job2", "ns2").Queue("beta").Admit(utiltesting.MakeAdmission("foo").Obj()).Obj(),
+		utiltesting.MakeWorkload("job3", "ns1").Queue("gamma").Admit(utiltesting.MakeAdmission("bar").Obj()).Obj(),
+		utiltesting.MakeWorkload("job4", "ns2").Queue("beta").Admit(utiltesting.MakeAdmission("foo").Obj()).Obj(),
```

  Added in the tests for workloads instead.

- **pkg/cache/cache.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-11T18:45:28Z
```diff
@@ -118,8 +127,9 @@ type FlavorLimits struct {
 
 func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	cqImpl := &ClusterQueue{
-		Name:      cq.Name,
-		Workloads: map[string]*workload.Info{},
+		Name:            cq.Name,
+		Workloads:       map[string]*workload.Info{},
+		wlCountPerQueue: make(map[string]int),
```

  Done
