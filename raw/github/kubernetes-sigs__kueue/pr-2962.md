# PR #2962: [kjobctl] Add describe slurm

**Summary**: [kjobctl] Add describe slurm

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2962

**Last updated**: 2024-09-16T11:34:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/kjobctl-describe-slurm` → `main`
- **Created**: 2024-09-02T16:17:47Z
- **Updated**: 2024-09-16T11:34:43Z
- **Merged**: 2024-09-16T10:09:15Z
- **Closed**: 2024-09-16T10:09:15Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mwielgus](https://github.com/mwielgus), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mbobrovskyi](https://github.com/mbobrovskyi), [@trasc](https://github.com/trasc)
- **Changed files**: 4   **+394 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:

Add `describe` slurm command to kjobctl.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-02T16:17:50Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-02T16:18:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9455f5dd437b7c68c6dd8d1b8795536dd730f526 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e165083fdb500008cce4e9 |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-06T06:59:40Z

/test all

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-06T07:11:55Z

/cc @mbobrovskyi

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-06T12:03:48Z

Thanks!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-06T12:03:55Z

LGTM label has been added.  <details>Git tree hash: 8ccad0c9fdfe99dad8720ab2480cedb768b5a132</details>

### Comment by [@mwielgus](https://github.com/mwielgus) — 2024-09-11T08:31:11Z

Can you paste a sample command output?

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-11T09:52:37Z

@mwielgus 

Sample 1
---
Input: 
```
describe slurm sample-profile-slurm-m2lgd
```

Output:
```
Name:           sample-profile-slurm-m2lgd
Namespace:      default
Labels:         kjobctl.x-k8s.io/mode=Slurm
                kjobctl.x-k8s.io/profile=sample-profile
Parallelism:    2
Completions:    3
Start Time:     Tue, 10 Sep 2024 17:12:31 +0200
Pods Statuses:  2 Active (0 Ready) / 0 Succeeded / 0 Failed
Pod Template:
  Containers:
   sample-container:
    Port:       <none>
    Host Port:  <none>
    Command:
      bash
      /slurm/entrypoint.sh
    Environment:
      ENTRYPOINT_PATH:      /home/slurm/samples
      USER_ID:              Irving_Mondragon
      TASK_NAME:            default_sample-profile
      TASK_ID:              Irving_Mondragon_2024-09-10T17:12:31+02:00_default_sample-profile
      PROFILE:              default_sample-profile
      TIMESTAMP:            2024-09-10T17:12:31+02:00
      JOB_CONTAINER_INDEX:  0
    Mounts:
      /home/slurm/samples from slurm-code-sample (rw)
      /slurm from sample-profile-slurm-m2lgd (rw)
  Volumes:
   slurm-code-sample:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      slurm-code-sample
    Optional:  false
   sample-profile-slurm-m2lgd:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      sample-profile-slurm-m2lgd
    Optional:  false


Name:       sample-profile-slurm-m2lgd
Namespace:  default
Labels:     kjobctl.x-k8s.io/mode=Slurm
            kjobctl.x-k8s.io/profile=sample-profile

Data
====
entrypoint.sh:
----
#!/usr/bin/bash

set -o errexit
set -o nounset
set -o pipefail

# External variables
# JOB_COMPLETION_INDEX  - completion index of the job.
# JOB_CONTAINER_INDEX   - container index in the container template.

# ["COMPLETION_INDEX"]="CONTAINER_INDEX_1,CONTAINER_INDEX_2"
declare -A array_indexes=(["0"]="1" ["1"]="2" ["2"]="3")   # Requires bash v4+

container_indexes=${array_indexes[${JOB_COMPLETION_INDEX}]}
container_indexes=(${container_indexes//,/ })

if [[ ! -v container_indexes[${JOB_CONTAINER_INDEX}] ]];
then
  exit 0
fi

SBATCH_ARRAY_INX=1-3%2
SBATCH_GPUS_PER_TASK=
SBATCH_MEM_PER_CPU=
SBATCH_MEM_PER_GPU=
SBATCH_OUTPUT=
SBATCH_ERROR=
SBATCH_INPUT=
SBATCH_JOB_NAME=
SBATCH_PARTITION=

export SLURM_ARRAY_JOB_ID=1                  # Job array’s master job ID number.
export SLURM_ARRAY_TASK_COUNT=3              # Total number of tasks in a job array.
export SLURM_ARRAY_TASK_MAX=3                # Job array’s maximum ID (index) number.
export SLURM_ARRAY_TASK_MIN=1                # Job array’s minimum ID (index) number.
export SLURM_TASKS_PER_NODE=1                # Number of tasks to be initiated on each node.
export SLURM_CPUS_PER_TASK=                  # Number of CPUs per task.
export SLURM_CPUS_ON_NODE=                   # Number of CPUs on the allocated node (actually pod).
export SLURM_JOB_CPUS_PER_NODE=              # Count of processors available to the job on this node.
export SLURM_CPUS_PER_GPU=                   # Number of CPUs requested per allocated GPU.
export SLURM_MEM_PER_CPU=                  # Memory per CPU. Same as --mem-per-cpu .
export SLURM_MEM_PER_GPU=                  # Memory per GPU.
export SLURM_MEM_PER_NODE=                 # Memory per node. Same as --mem.
export SLURM_GPUS=0                        # Number of GPUs requested (in total).
export SLURM_NTASKS=1                      # Same as -n, –ntasks. The number of tasks.
export SLURM_NTASKS_PER_NODE=1               # Number of tasks requested per node.
export SLURM_NPROCS=$SLURM_NTASKS          # Same as -n, --ntasks. See $SLURM_NTASKS.
export SLURM_NNODES=2                        # Total number of nodes (actually pods) in the job’s resource allocation.
export SLURM_SUBMIT_DIR=/slurm               # The path of the job submission directory.
export SLURM_SUBMIT_HOST=$HOSTNAME         # The hostname of the node used for job submission.

export SLURM_JOB_ID=$(( JOB_COMPLETION_INDEX * SLURM_TASKS_PER_NODE + JOB_CONTAINER_INDEX + SLURM_ARRAY_JOB_ID ))   # The Job ID.
export SLURM_JOBID=$SLURM_JOB_ID                                                                                    # Deprecated. Same as $SLURM_JOB_ID
export SLURM_ARRAY_TASK_ID=${container_indexes[${JOB_CONTAINER_INDEX}]}                        # Task ID.

unmask_filename () {
  replaced="$1"

  if [[ "$replaced" == "\\"* ]]; then
      replaced="${replaced//\\/}"
      echo "${replaced}"
      return 0
  fi

  replaced=$(echo "$replaced" | sed -E "s/(%)(%A)/\1\n\2/g;:a s/(^|[^\n])%A/\1$SLURM_ARRAY_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%a)/\1\n\2/g;:a s/(^|[^\n])%a/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%j)/\1\n\2/g;:a s/(^|[^\n])%j/\1$SLURM_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%N)/\1\n\2/g;:a s/(^|[^\n])%N/\1$HOSTNAME/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%n)/\1\n\2/g;:a s/(^|[^\n])%n/\1$JOB_COMPLETION_INDEX/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%t)/\1\n\2/g;:a s/(^|[^\n])%t/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%u)/\1\n\2/g;:a s/(^|[^\n])%u/\1$USER_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%x)/\1\n\2/g;:a s/(^|[^\n])%x/\1$SBATCH_JOB_NAME/;ta;s/\n//g")

  replaced="${replaced//%%/%}"

  echo "$replaced"
}

input_file=$(unmask_filename "$SBATCH_INPUT")
output_file=$(unmask_filename "$SBATCH_OUTPUT")
error_path=$(unmask_filename "$SBATCH_ERROR")

/slurm/script


script:
----
#!/bin/bash

#SBATCH --array=1-3%2

echo "now processing task id:: ${SLURM_ARRAY_TASK_ID}"
python /home/slurm/samples/sample_code.py

exit 0



BinaryData
====
```

Sample 2
---
Input:
```
describe slurm
```

Output:
```
Name:           sample-profile-slurm-m2lgd
Namespace:      default
Labels:         kjobctl.x-k8s.io/mode=Slurm
                kjobctl.x-k8s.io/profile=sample-profile
Parallelism:    2
Completions:    3
Start Time:     Tue, 10 Sep 2024 17:12:31 +0200
Pods Statuses:  2 Active (0 Ready) / 0 Succeeded / 0 Failed
Pod Template:
  Containers:
   sample-container:
    Port:       <none>
    Host Port:  <none>
    Command:
      bash
      /slurm/entrypoint.sh
    Environment:
      ENTRYPOINT_PATH:      /home/slurm/samples
      USER_ID:              Irving_Mondragon
      TASK_NAME:            default_sample-profile
      TASK_ID:              Irving_Mondragon_2024-09-10T17:12:31+02:00_default_sample-profile
      PROFILE:              default_sample-profile
      TIMESTAMP:            2024-09-10T17:12:31+02:00
      JOB_CONTAINER_INDEX:  0
    Mounts:
      /home/slurm/samples from slurm-code-sample (rw)
      /slurm from sample-profile-slurm-m2lgd (rw)
  Volumes:
   slurm-code-sample:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      slurm-code-sample
    Optional:  false
   sample-profile-slurm-m2lgd:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      sample-profile-slurm-m2lgd
    Optional:  false


Name:       sample-profile-slurm-m2lgd
Namespace:  default
Labels:     kjobctl.x-k8s.io/mode=Slurm
            kjobctl.x-k8s.io/profile=sample-profile

Data
====
entrypoint.sh:
----
#!/usr/bin/bash

set -o errexit
set -o nounset
set -o pipefail

# External variables
# JOB_COMPLETION_INDEX  - completion index of the job.
# JOB_CONTAINER_INDEX   - container index in the container template.

# ["COMPLETION_INDEX"]="CONTAINER_INDEX_1,CONTAINER_INDEX_2"
declare -A array_indexes=(["0"]="1" ["1"]="2" ["2"]="3")   # Requires bash v4+

container_indexes=${array_indexes[${JOB_COMPLETION_INDEX}]}
container_indexes=(${container_indexes//,/ })

if [[ ! -v container_indexes[${JOB_CONTAINER_INDEX}] ]];
then
  exit 0
fi

SBATCH_ARRAY_INX=1-3%2
SBATCH_GPUS_PER_TASK=
SBATCH_MEM_PER_CPU=
SBATCH_MEM_PER_GPU=
SBATCH_OUTPUT=
SBATCH_ERROR=
SBATCH_INPUT=
SBATCH_JOB_NAME=
SBATCH_PARTITION=

export SLURM_ARRAY_JOB_ID=1                  # Job array’s master job ID number.
export SLURM_ARRAY_TASK_COUNT=3              # Total number of tasks in a job array.
export SLURM_ARRAY_TASK_MAX=3                # Job array’s maximum ID (index) number.
export SLURM_ARRAY_TASK_MIN=1                # Job array’s minimum ID (index) number.
export SLURM_TASKS_PER_NODE=1                # Number of tasks to be initiated on each node.
export SLURM_CPUS_PER_TASK=                  # Number of CPUs per task.
export SLURM_CPUS_ON_NODE=                   # Number of CPUs on the allocated node (actually pod).
export SLURM_JOB_CPUS_PER_NODE=              # Count of processors available to the job on this node.
export SLURM_CPUS_PER_GPU=                   # Number of CPUs requested per allocated GPU.
export SLURM_MEM_PER_CPU=                  # Memory per CPU. Same as --mem-per-cpu .
export SLURM_MEM_PER_GPU=                  # Memory per GPU.
export SLURM_MEM_PER_NODE=                 # Memory per node. Same as --mem.
export SLURM_GPUS=0                        # Number of GPUs requested (in total).
export SLURM_NTASKS=1                      # Same as -n, –ntasks. The number of tasks.
export SLURM_NTASKS_PER_NODE=1               # Number of tasks requested per node.
export SLURM_NPROCS=$SLURM_NTASKS          # Same as -n, --ntasks. See $SLURM_NTASKS.
export SLURM_NNODES=2                        # Total number of nodes (actually pods) in the job’s resource allocation.
export SLURM_SUBMIT_DIR=/slurm               # The path of the job submission directory.
export SLURM_SUBMIT_HOST=$HOSTNAME         # The hostname of the node used for job submission.

export SLURM_JOB_ID=$(( JOB_COMPLETION_INDEX * SLURM_TASKS_PER_NODE + JOB_CONTAINER_INDEX + SLURM_ARRAY_JOB_ID ))   # The Job ID.
export SLURM_JOBID=$SLURM_JOB_ID                                                                                    # Deprecated. Same as $SLURM_JOB_ID
export SLURM_ARRAY_TASK_ID=${container_indexes[${JOB_CONTAINER_INDEX}]}                        # Task ID.

unmask_filename () {
  replaced="$1"

  if [[ "$replaced" == "\\"* ]]; then
      replaced="${replaced//\\/}"
      echo "${replaced}"
      return 0
  fi

  replaced=$(echo "$replaced" | sed -E "s/(%)(%A)/\1\n\2/g;:a s/(^|[^\n])%A/\1$SLURM_ARRAY_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%a)/\1\n\2/g;:a s/(^|[^\n])%a/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%j)/\1\n\2/g;:a s/(^|[^\n])%j/\1$SLURM_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%N)/\1\n\2/g;:a s/(^|[^\n])%N/\1$HOSTNAME/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%n)/\1\n\2/g;:a s/(^|[^\n])%n/\1$JOB_COMPLETION_INDEX/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%t)/\1\n\2/g;:a s/(^|[^\n])%t/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%u)/\1\n\2/g;:a s/(^|[^\n])%u/\1$USER_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%x)/\1\n\2/g;:a s/(^|[^\n])%x/\1$SBATCH_JOB_NAME/;ta;s/\n//g")

  replaced="${replaced//%%/%}"

  echo "$replaced"
}

input_file=$(unmask_filename "$SBATCH_INPUT")
output_file=$(unmask_filename "$SBATCH_OUTPUT")
error_path=$(unmask_filename "$SBATCH_ERROR")

/slurm/script


script:
----
#!/bin/bash

#SBATCH --array=1-3%2

echo "now processing task id:: ${SLURM_ARRAY_TASK_ID}"
python /home/slurm/samples/sample_code.py

exit 0



BinaryData
====



Name:           sample-profile-slurm-qzcv4
Namespace:      default
Labels:         kjobctl.x-k8s.io/mode=Slurm
                kjobctl.x-k8s.io/profile=sample-profile
Parallelism:    2
Completions:    3
Start Time:     Tue, 10 Sep 2024 17:11:41 +0200
Pods Statuses:  2 Active (0 Ready) / 0 Succeeded / 0 Failed
Pod Template:
  Containers:
   sample-container:
    Port:       <none>
    Host Port:  <none>
    Command:
      bash
      /slurm/entrypoint.sh
    Environment:
      ENTRYPOINT_PATH:      /home/slurm/samples
      USER_ID:              Irving_Mondragon
      TASK_NAME:            default_sample-profile
      TASK_ID:              Irving_Mondragon_2024-09-10T17:11:41+02:00_default_sample-profile
      PROFILE:              default_sample-profile
      TIMESTAMP:            2024-09-10T17:11:41+02:00
      JOB_CONTAINER_INDEX:  0
    Mounts:
      /home/slurm/samples from slurm-code-sample (rw)
      /slurm from sample-profile-slurm-qzcv4 (rw)
  Volumes:
   slurm-code-sample:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      slurm-code-sample
    Optional:  false
   sample-profile-slurm-qzcv4:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      sample-profile-slurm-qzcv4
    Optional:  false


Name:       sample-profile-slurm-qzcv4
Namespace:  default
Labels:     kjobctl.x-k8s.io/mode=Slurm
            kjobctl.x-k8s.io/profile=sample-profile

Data
====
entrypoint.sh:
----
#!/usr/bin/bash

set -o errexit
set -o nounset
set -o pipefail

# External variables
# JOB_COMPLETION_INDEX  - completion index of the job.
# JOB_CONTAINER_INDEX   - container index in the container template.

# ["COMPLETION_INDEX"]="CONTAINER_INDEX_1,CONTAINER_INDEX_2"
declare -A array_indexes=(["0"]="1" ["1"]="2" ["2"]="3")   # Requires bash v4+

container_indexes=${array_indexes[${JOB_COMPLETION_INDEX}]}
container_indexes=(${container_indexes//,/ })

if [[ ! -v container_indexes[${JOB_CONTAINER_INDEX}] ]];
then
  exit 0
fi

SBATCH_ARRAY_INX=1-3%2
SBATCH_GPUS_PER_TASK=
SBATCH_MEM_PER_CPU=
SBATCH_MEM_PER_GPU=
SBATCH_OUTPUT=
SBATCH_ERROR=
SBATCH_INPUT=
SBATCH_JOB_NAME=
SBATCH_PARTITION=

export SLURM_ARRAY_JOB_ID=1                  # Job array’s master job ID number.
export SLURM_ARRAY_TASK_COUNT=3              # Total number of tasks in a job array.
export SLURM_ARRAY_TASK_MAX=3                # Job array’s maximum ID (index) number.
export SLURM_ARRAY_TASK_MIN=1                # Job array’s minimum ID (index) number.
export SLURM_TASKS_PER_NODE=1                # Number of tasks to be initiated on each node.
export SLURM_CPUS_PER_TASK=                  # Number of CPUs per task.
export SLURM_CPUS_ON_NODE=                   # Number of CPUs on the allocated node (actually pod).
export SLURM_JOB_CPUS_PER_NODE=              # Count of processors available to the job on this node.
export SLURM_CPUS_PER_GPU=                   # Number of CPUs requested per allocated GPU.
export SLURM_MEM_PER_CPU=                  # Memory per CPU. Same as --mem-per-cpu .
export SLURM_MEM_PER_GPU=                  # Memory per GPU.
export SLURM_MEM_PER_NODE=                 # Memory per node. Same as --mem.
export SLURM_GPUS=0                        # Number of GPUs requested (in total).
export SLURM_NTASKS=1                      # Same as -n, –ntasks. The number of tasks.
export SLURM_NTASKS_PER_NODE=1               # Number of tasks requested per node.
export SLURM_NPROCS=$SLURM_NTASKS          # Same as -n, --ntasks. See $SLURM_NTASKS.
export SLURM_NNODES=2                        # Total number of nodes (actually pods) in the job’s resource allocation.
export SLURM_SUBMIT_DIR=/slurm               # The path of the job submission directory.
export SLURM_SUBMIT_HOST=$HOSTNAME         # The hostname of the node used for job submission.

export SLURM_JOB_ID=$(( JOB_COMPLETION_INDEX * SLURM_TASKS_PER_NODE + JOB_CONTAINER_INDEX + SLURM_ARRAY_JOB_ID ))   # The Job ID.
export SLURM_JOBID=$SLURM_JOB_ID                                                                                    # Deprecated. Same as $SLURM_JOB_ID
export SLURM_ARRAY_TASK_ID=${container_indexes[${JOB_CONTAINER_INDEX}]}                        # Task ID.

unmask_filename () {
  replaced="$1"

  if [[ "$replaced" == "\\"* ]]; then
      replaced="${replaced//\\/}"
      echo "${replaced}"
      return 0
  fi

  replaced=$(echo "$replaced" | sed -E "s/(%)(%A)/\1\n\2/g;:a s/(^|[^\n])%A/\1$SLURM_ARRAY_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%a)/\1\n\2/g;:a s/(^|[^\n])%a/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%j)/\1\n\2/g;:a s/(^|[^\n])%j/\1$SLURM_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%N)/\1\n\2/g;:a s/(^|[^\n])%N/\1$HOSTNAME/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%n)/\1\n\2/g;:a s/(^|[^\n])%n/\1$JOB_COMPLETION_INDEX/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%t)/\1\n\2/g;:a s/(^|[^\n])%t/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%u)/\1\n\2/g;:a s/(^|[^\n])%u/\1$USER_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%x)/\1\n\2/g;:a s/(^|[^\n])%x/\1$SBATCH_JOB_NAME/;ta;s/\n//g")

  replaced="${replaced//%%/%}"

  echo "$replaced"
}

input_file=$(unmask_filename "$SBATCH_INPUT")
output_file=$(unmask_filename "$SBATCH_OUTPUT")
error_path=$(unmask_filename "$SBATCH_ERROR")

/slurm/script


script:
----
#!/bin/bash

#SBATCH --array=1-3%2

echo "now processing task id:: ${SLURM_ARRAY_TASK_ID}"
python /home/slurm/samples/sample_code.py

exit 0



BinaryData
====



Name:           sample-profile-slurm-vscv7
Namespace:      default
Labels:         kjobctl.x-k8s.io/mode=Slurm
                kjobctl.x-k8s.io/profile=sample-profile
Parallelism:    2
Completions:    3
Start Time:     Tue, 10 Sep 2024 17:12:41 +0200
Pods Statuses:  2 Active (0 Ready) / 0 Succeeded / 0 Failed
Pod Template:
  Containers:
   sample-container:
    Port:       <none>
    Host Port:  <none>
    Command:
      bash
      /slurm/entrypoint.sh
    Environment:
      ENTRYPOINT_PATH:      /home/slurm/samples
      USER_ID:              Irving_Mondragon
      TASK_NAME:            default_sample-profile
      TASK_ID:              Irving_Mondragon_2024-09-10T17:12:41+02:00_default_sample-profile
      PROFILE:              default_sample-profile
      TIMESTAMP:            2024-09-10T17:12:41+02:00
      JOB_CONTAINER_INDEX:  0
    Mounts:
      /home/slurm/samples from slurm-code-sample (rw)
      /slurm from sample-profile-slurm-vscv7 (rw)
  Volumes:
   slurm-code-sample:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      slurm-code-sample
    Optional:  false
   sample-profile-slurm-vscv7:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      sample-profile-slurm-vscv7
    Optional:  false


Name:       sample-profile-slurm-vscv7
Namespace:  default
Labels:     kjobctl.x-k8s.io/mode=Slurm
            kjobctl.x-k8s.io/profile=sample-profile

Data
====
entrypoint.sh:
----
#!/usr/bin/bash

set -o errexit
set -o nounset
set -o pipefail

# External variables
# JOB_COMPLETION_INDEX  - completion index of the job.
# JOB_CONTAINER_INDEX   - container index in the container template.

# ["COMPLETION_INDEX"]="CONTAINER_INDEX_1,CONTAINER_INDEX_2"
declare -A array_indexes=(["0"]="1" ["1"]="2" ["2"]="3")   # Requires bash v4+

container_indexes=${array_indexes[${JOB_COMPLETION_INDEX}]}
container_indexes=(${container_indexes//,/ })

if [[ ! -v container_indexes[${JOB_CONTAINER_INDEX}] ]];
then
  exit 0
fi

SBATCH_ARRAY_INX=1-3%2
SBATCH_GPUS_PER_TASK=
SBATCH_MEM_PER_CPU=
SBATCH_MEM_PER_GPU=
SBATCH_OUTPUT=
SBATCH_ERROR=
SBATCH_INPUT=
SBATCH_JOB_NAME=
SBATCH_PARTITION=

export SLURM_ARRAY_JOB_ID=1                  # Job array’s master job ID number.
export SLURM_ARRAY_TASK_COUNT=3              # Total number of tasks in a job array.
export SLURM_ARRAY_TASK_MAX=3                # Job array’s maximum ID (index) number.
export SLURM_ARRAY_TASK_MIN=1                # Job array’s minimum ID (index) number.
export SLURM_TASKS_PER_NODE=1                # Number of tasks to be initiated on each node.
export SLURM_CPUS_PER_TASK=                  # Number of CPUs per task.
export SLURM_CPUS_ON_NODE=                   # Number of CPUs on the allocated node (actually pod).
export SLURM_JOB_CPUS_PER_NODE=              # Count of processors available to the job on this node.
export SLURM_CPUS_PER_GPU=                   # Number of CPUs requested per allocated GPU.
export SLURM_MEM_PER_CPU=                  # Memory per CPU. Same as --mem-per-cpu .
export SLURM_MEM_PER_GPU=                  # Memory per GPU.
export SLURM_MEM_PER_NODE=                 # Memory per node. Same as --mem.
export SLURM_GPUS=0                        # Number of GPUs requested (in total).
export SLURM_NTASKS=1                      # Same as -n, –ntasks. The number of tasks.
export SLURM_NTASKS_PER_NODE=1               # Number of tasks requested per node.
export SLURM_NPROCS=$SLURM_NTASKS          # Same as -n, --ntasks. See $SLURM_NTASKS.
export SLURM_NNODES=2                        # Total number of nodes (actually pods) in the job’s resource allocation.
export SLURM_SUBMIT_DIR=/slurm               # The path of the job submission directory.
export SLURM_SUBMIT_HOST=$HOSTNAME         # The hostname of the node used for job submission.

export SLURM_JOB_ID=$(( JOB_COMPLETION_INDEX * SLURM_TASKS_PER_NODE + JOB_CONTAINER_INDEX + SLURM_ARRAY_JOB_ID ))   # The Job ID.
export SLURM_JOBID=$SLURM_JOB_ID                                                                                    # Deprecated. Same as $SLURM_JOB_ID
export SLURM_ARRAY_TASK_ID=${container_indexes[${JOB_CONTAINER_INDEX}]}                        # Task ID.

unmask_filename () {
  replaced="$1"

  if [[ "$replaced" == "\\"* ]]; then
      replaced="${replaced//\\/}"
      echo "${replaced}"
      return 0
  fi

  replaced=$(echo "$replaced" | sed -E "s/(%)(%A)/\1\n\2/g;:a s/(^|[^\n])%A/\1$SLURM_ARRAY_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%a)/\1\n\2/g;:a s/(^|[^\n])%a/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%j)/\1\n\2/g;:a s/(^|[^\n])%j/\1$SLURM_JOB_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%N)/\1\n\2/g;:a s/(^|[^\n])%N/\1$HOSTNAME/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%n)/\1\n\2/g;:a s/(^|[^\n])%n/\1$JOB_COMPLETION_INDEX/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%t)/\1\n\2/g;:a s/(^|[^\n])%t/\1$SLURM_ARRAY_TASK_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%u)/\1\n\2/g;:a s/(^|[^\n])%u/\1$USER_ID/;ta;s/\n//g")
  replaced=$(echo "$replaced" | sed -E "s/(%)(%x)/\1\n\2/g;:a s/(^|[^\n])%x/\1$SBATCH_JOB_NAME/;ta;s/\n//g")

  replaced="${replaced//%%/%}"

  echo "$replaced"
}

input_file=$(unmask_filename "$SBATCH_INPUT")
output_file=$(unmask_filename "$SBATCH_OUTPUT")
error_path=$(unmask_filename "$SBATCH_ERROR")

/slurm/script


script:
----
#!/bin/bash

#SBATCH --array=1-3%2

echo "now processing task id:: ${SLURM_ARRAY_TASK_ID}"
python /home/slurm/samples/sample_code.py

exit 0



BinaryData
====
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-11T16:54:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2962#pullrequestreview-2297946924" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2962#" title="Author self-approved">IrvingMg</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mwielgus](https://github.com/mwielgus) — 2024-09-16T09:50:50Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-16T09:50:57Z

LGTM label has been added.  <details>Git tree hash: 6381379660f88ac1797104378bd150c27a42bdd4</details>

### Comment by [@mwielgus](https://github.com/mwielgus) — 2024-09-16T09:51:00Z

/unhold

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-06T09:30:44Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe_printer.go:44** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-06T09:26:09Z
```diff
@@ -40,7 +41,7 @@ type ResourceDescriber interface {
 	Describe(object *unstructured.Unstructured) (output string, err error)
 }
 
-// NewResourceDescriber Describer returns a Describer for displaying the specified RESTMapping type or an error.
+// Describer returns a Describer for displaying the specified RESTMapping type or an error.
```

  ```suggestion
  // NewResourceDescriber returns a Describer for displaying the specified RESTMapping type or an error.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-06T13:56:04Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T13:56:04Z
```diff
@@ -261,3 +284,57 @@ func (o *DescribeOptions) customizeResourceBuilder() *resource.Builder {
 
 	return builder.Flatten()
 }
+
+func (o *DescribeOptions) getConfigMaps(ctx context.Context) ([]*resource.Info, error) {
+	infos := make([]*resource.Info, 0)
+
+	if o.argsFormat == modeTaskArgsFormat || o.argsFormat == modeSlashTaskArgsFormat {
+		cm, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).Get(ctx, o.TaskName, metav1.GetOptions{})
+		if err != nil {
+			return nil, err
+		}
+
+		info, err := configMapToInfo(cm)
+		if err != nil {
+			return nil, err
+		}
+
+		infos = append(infos, info)
+	} else {
+		cmList, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).List(ctx, metav1.ListOptions{
+			LabelSelector: constants.ProfileLabel,
```

  isn't this getting all the configmaps that have a profile label?
  
  How is this specific to one job?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-09-06T18:11:55Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:305** by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-06T18:11:55Z
```diff
@@ -261,3 +284,57 @@ func (o *DescribeOptions) customizeResourceBuilder() *resource.Builder {
 
 	return builder.Flatten()
 }
+
+func (o *DescribeOptions) getConfigMaps(ctx context.Context) ([]*resource.Info, error) {
+	infos := make([]*resource.Info, 0)
+
+	if o.argsFormat == modeTaskArgsFormat || o.argsFormat == modeSlashTaskArgsFormat {
+		cm, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).Get(ctx, o.TaskName, metav1.GetOptions{})
+		if err != nil {
+			return nil, err
+		}
+
+		info, err := configMapToInfo(cm)
+		if err != nil {
+			return nil, err
+		}
+
+		infos = append(infos, info)
+	} else {
+		cmList, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).List(ctx, metav1.ListOptions{
+			LabelSelector: constants.ProfileLabel,
```

  The `describe` command allows to see a specific job or all the jobs created for a specific mode. It's for that second case when we need to retrieve all the configmaps created for Slurm mode.
  
  However, I was missing filtering the configmaps by Mode label as well. I´ve updated the label selector to include it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-06T18:25:03Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T18:25:03Z
```diff
@@ -261,3 +284,57 @@ func (o *DescribeOptions) customizeResourceBuilder() *resource.Builder {
 
 	return builder.Flatten()
 }
+
+func (o *DescribeOptions) getConfigMaps(ctx context.Context) ([]*resource.Info, error) {
+	infos := make([]*resource.Info, 0)
+
+	if o.argsFormat == modeTaskArgsFormat || o.argsFormat == modeSlashTaskArgsFormat {
+		cm, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).Get(ctx, o.TaskName, metav1.GetOptions{})
+		if err != nil {
+			return nil, err
+		}
+
+		info, err := configMapToInfo(cm)
+		if err != nil {
+			return nil, err
+		}
+
+		infos = append(infos, info)
+	} else {
+		cmList, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).List(ctx, metav1.ListOptions{
+			LabelSelector: constants.ProfileLabel,
```

  Will the config maps be printed alongside their respective job?
  It looks like jobs will be printed first, then all the configmaps

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-09-10T08:45:12Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:305** by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-10T08:45:12Z
```diff
@@ -261,3 +284,57 @@ func (o *DescribeOptions) customizeResourceBuilder() *resource.Builder {
 
 	return builder.Flatten()
 }
+
+func (o *DescribeOptions) getConfigMaps(ctx context.Context) ([]*resource.Info, error) {
+	infos := make([]*resource.Info, 0)
+
+	if o.argsFormat == modeTaskArgsFormat || o.argsFormat == modeSlashTaskArgsFormat {
+		cm, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).Get(ctx, o.TaskName, metav1.GetOptions{})
+		if err != nil {
+			return nil, err
+		}
+
+		info, err := configMapToInfo(cm)
+		if err != nil {
+			return nil, err
+		}
+
+		infos = append(infos, info)
+	} else {
+		cmList, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).List(ctx, metav1.ListOptions{
+			LabelSelector: constants.ProfileLabel,
```

  Yes, currently jobs are printed first, and then the configmaps

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-10T14:42:04Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-10T14:42:04Z
```diff
@@ -261,3 +284,57 @@ func (o *DescribeOptions) customizeResourceBuilder() *resource.Builder {
 
 	return builder.Flatten()
 }
+
+func (o *DescribeOptions) getConfigMaps(ctx context.Context) ([]*resource.Info, error) {
+	infos := make([]*resource.Info, 0)
+
+	if o.argsFormat == modeTaskArgsFormat || o.argsFormat == modeSlashTaskArgsFormat {
+		cm, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).Get(ctx, o.TaskName, metav1.GetOptions{})
+		if err != nil {
+			return nil, err
+		}
+
+		info, err := configMapToInfo(cm)
+		if err != nil {
+			return nil, err
+		}
+
+		infos = append(infos, info)
+	} else {
+		cmList, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).List(ctx, metav1.ListOptions{
+			LabelSelector: constants.ProfileLabel,
```

  Can we interleave them so that jobs are always next to their configmaps?
  Otherwise the output is not very usable.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-09-10T15:21:03Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:305** by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-10T15:21:02Z
```diff
@@ -261,3 +284,57 @@ func (o *DescribeOptions) customizeResourceBuilder() *resource.Builder {
 
 	return builder.Flatten()
 }
+
+func (o *DescribeOptions) getConfigMaps(ctx context.Context) ([]*resource.Info, error) {
+	infos := make([]*resource.Info, 0)
+
+	if o.argsFormat == modeTaskArgsFormat || o.argsFormat == modeSlashTaskArgsFormat {
+		cm, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).Get(ctx, o.TaskName, metav1.GetOptions{})
+		if err != nil {
+			return nil, err
+		}
+
+		info, err := configMapToInfo(cm)
+		if err != nil {
+			return nil, err
+		}
+
+		infos = append(infos, info)
+	} else {
+		cmList, err := o.Clientset.CoreV1().ConfigMaps(o.Namespace).List(ctx, metav1.ListOptions{
+			LabelSelector: constants.ProfileLabel,
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-10T17:18:51Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-10T17:18:51Z
```diff
@@ -198,6 +199,10 @@ func (o *DescribeOptions) Run(ctx context.Context) error {
 		infos = append(infos, configMapsAsInfos...)
 	}
 
+	slices.SortFunc(infos, func(a, b *resource.Info) int {
+		return strings.Compare(a.Name, b.Name)
```

  is this enough guarantee? Are the names always supposed to match?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-10T17:53:15Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:206** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-10T17:53:15Z
```diff
@@ -198,6 +199,10 @@ func (o *DescribeOptions) Run(ctx context.Context) error {
 		infos = append(infos, configMapsAsInfos...)
 	}
 
+	slices.SortFunc(infos, func(a, b *resource.Info) int {
+		return strings.Compare(a.Name, b.Name)
```

  Yes, we are creating `Job` and `ConfigMap` with the same name.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-10T18:20:40Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-10T18:20:40Z
```diff
@@ -198,6 +199,10 @@ func (o *DescribeOptions) Run(ctx context.Context) error {
 		infos = append(infos, configMapsAsInfos...)
 	}
 
+	slices.SortFunc(infos, func(a, b *resource.Info) int {
+		return strings.Compare(a.Name, b.Name)
```

  Add a comment to the function indicating why we are sorting and why it works.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-09-11T09:39:32Z

- **cmd/experimental/kjobctl/pkg/cmd/describe/describe.go:206** by [@IrvingMg](https://github.com/IrvingMg) — 2024-09-11T09:39:32Z
```diff
@@ -198,6 +199,10 @@ func (o *DescribeOptions) Run(ctx context.Context) error {
 		infos = append(infos, configMapsAsInfos...)
 	}
 
+	slices.SortFunc(infos, func(a, b *resource.Info) int {
+		return strings.Compare(a.Name, b.Name)
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T16:54:02Z

/approve
/hold
in case @mwielgus has more questions.
