# PR #2969: [doc] Develop ACC task.

**Summary**: [doc] Develop ACC task.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2969

**Last updated**: 2024-10-09T06:50:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `create-AC-task` → `main`
- **Created**: 2024-09-04T07:44:09Z
- **Updated**: 2024-10-09T06:50:33Z
- **Merged**: 2024-09-13T16:17:13Z
- **Closed**: 2024-09-13T16:17:13Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 2   **+50 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add `Develop an AdmissionCheck Controller` task page.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2932 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-04T07:44:12Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-04T07:44:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0967a413092f7e61603298a6fc7336566b9e7cf8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e3e053b041e200083df6cb |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2969--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2969--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTI5NjktLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.fg_9EmI-gB3ETAY6FCShLo3RREo2AbQTlsKF4JfNilg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@trasc](https://github.com/trasc) — 2024-09-04T07:57:40Z

/cc @tenzen-y

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2024-09-12T09:01:40Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: trasc / name: Traian Schiau  (0967a413092f7e61603298a6fc7336566b9e7cf8, be5c3e4447d2c7739a0afb5c94f63ddf1e407d54, 81d21a38ed2760e9105752369ac1d73cbe4cdb2e)</li></ul><!-- Date Modified: 2024-09-13 06:48:52.399653 -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-13T09:22:25Z

LGTM
I think we could expand it to better describe the case of building an external ACC, similarly as we do for Job framework, but I think we can expand it later when we have such examples. I consider it a good starting point for future extensions.

Leaving tagging to @tenzen-y or @alculquicondor  in case they have more comments.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-13T10:54:34Z

I just noticed the original discussion motivating the effort: https://github.com/kubernetes-sigs/kueue/discussions/2699 which focuses on documenting the process of adding external controllers.

### Comment by [@trasc](https://github.com/trasc) — 2024-09-13T11:11:12Z

> I just noticed the original discussion motivating the effort: #2699 which focuses on documenting the process of adding external controllers.

We could reference some documentation that describes how e controller can be created an deployed and so on maybe something from kubebuilder book , do you know a good link for that? (In my opinion kueue's documentation should not be the place one learns how to create an operator )

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-13T16:02:34Z

LGTM label has been added.  <details>Git tree hash: 825d5636baefd08bc4f99c6de7b8726d15d0ecef</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-13T16:02:36Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2969#pullrequestreview-2303505717" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2969#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-13T17:50:37Z

> LGTM I think we could expand it to better describe the case of building an external ACC, similarly as we do for Job framework, but I think we can expand it later when we have such examples. I consider it a good starting point for future extensions.
> 
> Leaving tagging to @tenzen-y or @alculquicondor in case they have more comments.

I guess that we may be able to provide good examples and more informable documentation after we finalize this: https://github.com/kubernetes-sigs/kueue/pull/2458

Anyway, this document looks good to me.
/cherry-pick website

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-13T17:50:52Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-09-13T17:51:15Z

@tenzen-y: new pull request created: #3045

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2969#issuecomment-2349658589):

>> LGTM I think we could expand it to better describe the case of building an external ACC, similarly as we do for Job framework, but I think we can expand it later when we have such examples. I consider it a good starting point for future extensions.
>> 
>> Leaving tagging to @tenzen-y or @alculquicondor in case they have more comments.
>
>I guess that we may be able to provide good examples and more informable documentation after we finalize this: https://github.com/kubernetes-sigs/kueue/pull/2458
>
>Anyway, this document looks good to me.
>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-09-13T17:51:50Z

@tenzen-y: new pull request could not be created: failed to create pull request against kubernetes-sigs/kueue#website from head k8s-infra-cherrypick-robot:cherry-pick-2969-to-website: status code 422 not one of [201], body: {"message":"Validation Failed","errors":[{"resource":"PullRequest","code":"custom","message":"A pull request already exists for k8s-infra-cherrypick-robot:cherry-pick-2969-to-website."}],"documentation_url":"https://docs.github.com/rest/pulls/pulls#create-a-pull-request","status":"422"}

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2969#issuecomment-2349659545):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-11T16:22:36Z

Thank you for this.

Additionally, could you add explanations that they need to implement those components as a separate binary from the kueue-manager?

- **site/content/en/docs/tasks/dev/develop-acc.md:32** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-11T16:20:58Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+Is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads based on the ACC's custom logic being able to allow the admission of the workload, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
```

  AFAIK, additionally, the platform developer need to implement CustomResource and its controller used in the ACC parameters field.
  
  So, could you mention that?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T18:20:04Z

- **site/content/en/docs/tasks/dev/develop-acc.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T18:12:03Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
```

  ```suggestion
  An Admission Check Controller, referred to as **ACC** in the rest of the doc,
  is a component that manages the AdmissionChecks associated with it
  (given by the corresponding value of `spec.controllerName`)
  and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
  ```

- **site/content/en/docs/tasks/dev/develop-acc.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T18:13:02Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
```

  ```suggestion
  Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
  ```

- **site/content/en/docs/tasks/dev/develop-acc.md:20** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T18:15:04Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
```

  ```suggestion
  Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`).
  Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
  ```

- **site/content/en/docs/tasks/dev/develop-acc.md:26** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T18:18:26Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+Is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads based on the ACC's custom logic being able to allow the admission of the workload, requeue or fail the workload. 
```

  maybe something like this?
  
  ```suggestion
  It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
  based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
  ```
  
  But I'm open to other suggestions.

- **site/content/en/docs/tasks/dev/develop-acc.md:30** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T18:19:39Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+Is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads based on the ACC's custom logic being able to allow the admission of the workload, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+## Util
```

  ```suggestion
  ## Utility code
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-12T08:39:55Z

- **site/content/en/docs/tasks/dev/develop-acc.md:9** by [@trasc](https://github.com/trasc) — 2024-09-12T08:39:55Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-12T09:01:45Z

- **site/content/en/docs/tasks/dev/develop-acc.md:11** by [@trasc](https://github.com/trasc) — 2024-09-12T08:40:44Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
```

  done

- **site/content/en/docs/tasks/dev/develop-acc.md:20** by [@trasc](https://github.com/trasc) — 2024-09-12T08:43:41Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
```

  done

- **site/content/en/docs/tasks/dev/develop-acc.md:26** by [@trasc](https://github.com/trasc) — 2024-09-12T08:45:37Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+Is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads based on the ACC's custom logic being able to allow the admission of the workload, requeue or fail the workload. 
```

  done

- **site/content/en/docs/tasks/dev/develop-acc.md:30** by [@trasc](https://github.com/trasc) — 2024-09-12T08:46:13Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+Is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads based on the ACC's custom logic being able to allow the admission of the workload, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+## Util
```

  done

- **site/content/en/docs/tasks/dev/develop-acc.md:32** by [@trasc](https://github.com/trasc) — 2024-09-12T09:00:53Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, sometime referred as **ACC**, is a component that manages the AdmissionChecks associated with it , by `spec.controllerName` and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Check [Admission Check](/docs/concepts/admission_check/) section for the general description of the Admission Checks usage in Kueue.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintain the `Active` condition of those that are associated with it (by `spec.controllerName`). Optionally it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+Is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads based on the ACC's custom logic being able to allow the admission of the workload, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-12T13:28:15Z

- **site/content/en/docs/tasks/dev/develop-acc.md:35** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T13:26:45Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
```

  ```suggestion
  Optionally, you can define a cluster level object type to hold the Admission Check parameters specific to your implementation. 
  ```

- **site/content/en/docs/tasks/dev/develop-acc.md:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T13:27:50Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
+Instances of this object type can be referenced in the AdmissionCheck definition by [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) and provide implementation specific data to the ACC.
```

  ```suggestion
  Users can reference instances of this object type in the AdmissionCheck definition in [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference).
  ```

- **site/content/en/docs/tasks/dev/develop-acc.md:38** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T13:28:11Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
+Instances of this object type can be referenced in the AdmissionCheck definition by [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) and provide implementation specific data to the ACC.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) uses [ProvisioningRequestConfig](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ProvisioningRequestConfig) for this purpose.
```

  ```suggestion
  For example, the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) uses [ProvisioningRequestConfig](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ProvisioningRequestConfig) for this purpose.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-13T06:49:02Z

- **site/content/en/docs/tasks/dev/develop-acc.md:38** by [@trasc](https://github.com/trasc) — 2024-09-13T06:47:27Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
+Instances of this object type can be referenced in the AdmissionCheck definition by [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) and provide implementation specific data to the ACC.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) uses [ProvisioningRequestConfig](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ProvisioningRequestConfig) for this purpose.
```

  done

- **site/content/en/docs/tasks/dev/develop-acc.md:36** by [@trasc](https://github.com/trasc) — 2024-09-13T06:47:32Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
+Instances of this object type can be referenced in the AdmissionCheck definition by [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) and provide implementation specific data to the ACC.
```

  done

- **site/content/en/docs/tasks/dev/develop-acc.md:35** by [@trasc](https://github.com/trasc) — 2024-09-13T06:47:38Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-13T08:04:43Z

- **site/content/en/docs/tasks/dev/develop-acc.md:45** by [@mimowo](https://github.com/mimowo) — 2024-09-12T13:33:52Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
+Instances of this object type can be referenced in the AdmissionCheck definition by [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) and provide implementation specific data to the ACC.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) uses [ProvisioningRequestConfig](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ProvisioningRequestConfig) for this purpose.
+
+## Utility Code
+
+`pkg/util/admissioncheck` provides helper code that can be used to interact with AdmissionCheck's parameters and more.
+
+## Examples
+
+Currently there are two ACCs implemented into kueue:
```

  ```suggestion
  ## Implementation
  
  Your external ACC should be provided as an external binary.  `pkg/util/admissioncheck` provides helper code that can be used to interact with AdmissionCheck's parameters and more.
  
  You can learn more how to implement your own ACC based on those which are implemented into Kueue:
  ```
  I assume developing an external controller is the target audience.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-13T08:36:24Z

- **site/content/en/docs/tasks/dev/develop-acc.md:45** by [@trasc](https://github.com/trasc) — 2024-09-13T08:36:24Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
+Instances of this object type can be referenced in the AdmissionCheck definition by [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) and provide implementation specific data to the ACC.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) uses [ProvisioningRequestConfig](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ProvisioningRequestConfig) for this purpose.
+
+## Utility Code
+
+`pkg/util/admissioncheck` provides helper code that can be used to interact with AdmissionCheck's parameters and more.
+
+## Examples
+
+Currently there are two ACCs implemented into kueue:
```

  > I assume developing an external controller is the target audience.
  
  It is mentioned that it could be built-in or in a different controller L16.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-13T09:08:56Z

- **site/content/en/docs/tasks/dev/develop-acc.md:45** by [@mimowo](https://github.com/mimowo) — 2024-09-13T09:08:56Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "Develop an AdmissionCheck Controller"
+date: 2024-09-03
+weight: 9
+description: >
+    Develop an AdmissionCheck Controller (ACC).
+---
+
+An Admission Check Controller, referred to as **ACC** in the rest of the doc, is a component that manages the AdmissionChecks associated with it (given by the corresponding value of `spec.controllerName`) and the workloads queued against ClusterQueues configured to use those AdmissionChecks.
+
+Read [Admission Check](/docs/concepts/admission_check/) to learn more about the mechanism from a user perspective.
+
+
+## Subcomponents
+
+The ACC can be built-in into Kueue or run in a different kubernetes controller manager and should implement reconcilers for AdmissionChecks and Workloads.
+
+### AdmissionCheck Reconciler
+
+Monitors the AdmissionChecks in the cluster and maintains the `Active` condition of those that are associated with it (by `spec.controllerName`). 
+
+Optionally, it can watch custom [parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) objects.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go`
+
+### Workload Reconciler
+
+It is in charge of maintaining the [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) of individual workloads
+based on the ACC's custom logic. It can either allow the admission, requeue or fail the workload. 
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) implements this in `pkg/controller/admissionchecks/provisioning/controller.go`
+
+### Parameters Object Type
+
+Optionally, a custom cluster level object type holding the Admission Check specific parameters can be defined. 
+Instances of this object type can be referenced in the AdmissionCheck definition by [spec.parameters](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-AdmissionCheckParametersReference) and provide implementation specific data to the ACC.
+
+The [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/) uses [ProvisioningRequestConfig](/docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ProvisioningRequestConfig) for this purpose.
+
+## Utility Code
+
+`pkg/util/admissioncheck` provides helper code that can be used to interact with AdmissionCheck's parameters and more.
+
+## Examples
+
+Currently there are two ACCs implemented into kueue:
```

  Ok, I think though the audience of building external and built-in is different, so we may need to have a section dedicated to external ACCs similarly as we do for Job integrations, but maybe we can defer this until we have such examples. So for now, I find it acceptable as is.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-13T16:02:28Z

/lgtm
/approve
