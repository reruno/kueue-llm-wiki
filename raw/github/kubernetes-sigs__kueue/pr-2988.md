# PR #2988: import deployment package in jobs.go to register the deployment webhook

**Summary**: import deployment package in jobs.go to register the deployment webhook

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2988

**Last updated**: 2024-09-05T18:33:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alaypatel07](https://github.com/alaypatel07)
- **Branch**: `fix-deployment-webhook` → `main`
- **Created**: 2024-09-04T21:38:10Z
- **Updated**: 2024-09-05T18:33:01Z
- **Merged**: 2024-09-05T18:32:59Z
- **Closed**: 2024-09-05T18:32:59Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor)
- **Changed files**: 6   **+95 / -79**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR fixes deployment webhook to allow for create/update of deployments after kueue is installed

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2987

#### Special notes for your reviewer:
Testing done:
1. enable the pod controller with following configuration option and applied the patch
```
$ cat config/components/manager/controller_manager_config.yaml
...
  - "pod"
  - "deployment"
#  externalFrameworks:
#  - "Foo.v1.example.com"
  podOptions:
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values: [ kube-system, kueue-system ]
...
$  kubectl patch -n kueue-system deployment kueue-controller-manager --type='json' -p='[
  {
    "op": "add",
    "path": "/spec/template/spec/containers/0/args/-",
    "value": "--feature-gates=VisibilityOnDemand=true"
  }
]'
deployment.apps/kueue-controller-manager patched
```
2. disabled the pod controller and applied the patch
```
$ cat config/components/manager/controller_manager_config.yaml
...
#  - "pod"
#  externalFrameworks:
#  - "Foo.v1.example.com"
#  podOptions:
#    namespaceSelector:
#      matchExpressions:
#        - key: kubernetes.io/metadata.name
#          operator: NotIn
#          values: [ kube-system, kueue-system ]
...
$ kubectl patch -n kueue-system deployment kueue-controller-manager --type='json' -p='[
  {
    "op": "add",
    "path": "/spec/template/spec/containers/0/args/-",
    "value": "--feature-gates=VisibilityOnDemand=true"
  }
]'
deployment.apps/kueue-controller-manager patched
```

Both the scenarios worked

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-04T21:38:20Z

Welcome @alaypatel07! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-04T21:38:20Z

Hi @alaypatel07. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-04T21:38:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 66989def27d96dee975e75162dd0aa8cbe2d2cd0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66d9f4789f14b60008bce4f5 |

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-04T21:40:59Z

cc @mbobrovskyi since you were interested in the issue, PTAL

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T00:44:06Z

/ok-to-test

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T17:38:25Z

Considering all the input, I have reworked the PR, PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-05T17:47:46Z

LGTM label has been added.  <details>Git tree hash: 228130eaa93c664d00634c3689b3b68c0f3d217f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-05T17:47:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2988#" title="Author self-approved">alaypatel07</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2988#pullrequestreview-2283723525" title="Approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T18:19:02Z

Thanks!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-05T18:19:08Z

LGTM label has been added.  <details>Git tree hash: 7dcde12450ad8d54a31b6c1530756a862c4454bc</details>

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T18:27:28Z

@alculquicondor can you please take another look and give out the approve tag? TIA

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-05T09:06:15Z

- **config/components/webhook/manifests.yaml:7** by [@mimowo](https://github.com/mimowo) — 2024-09-05T09:06:14Z
```diff
@@ -4,6 +4,25 @@ kind: MutatingWebhookConfiguration
 metadata:
   name: mutating-webhook-configuration
 webhooks:
+- admissionReviewVersions:
```

  Is this change required, or just formatting? If formatting, then I would suggest to revert.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-05T09:08:02Z

- **pkg/controller/jobframework/integrationmanager.go:270** by [@mimowo](https://github.com/mimowo) — 2024-09-05T09:08:02Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
```

  Can you explain why this is needed? Also, this check could be done only when `ok` - so you could do `if ok && matchingGVK(integration, gvk)`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T09:10:02Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T09:10:02Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  Can we add `NewJob` callback on `deployment_controller` instead to create another one?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T09:11:17Z

- **config/components/webhook/manifests.yaml:7** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T09:11:16Z
```diff
@@ -4,6 +4,25 @@ kind: MutatingWebhookConfiguration
 metadata:
   name: mutating-webhook-configuration
 webhooks:
+- admissionReviewVersions:
```

  I believe it's automatically added on `make verify`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-05T09:13:46Z

- **config/components/webhook/manifests.yaml:7** by [@mimowo](https://github.com/mimowo) — 2024-09-05T09:13:46Z
```diff
@@ -4,6 +4,25 @@ kind: MutatingWebhookConfiguration
 metadata:
   name: mutating-webhook-configuration
 webhooks:
+- admissionReviewVersions:
```

  Hm, but it is only moved from another place. Why wasn't this then moved prior to this PR?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T09:18:14Z

- **config/components/webhook/manifests.yaml:7** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T09:18:14Z
```diff
@@ -4,6 +4,25 @@ kind: MutatingWebhookConfiguration
 metadata:
   name: mutating-webhook-configuration
 webhooks:
+- admissionReviewVersions:
```

  I think it's because `_ "sigs.k8s.io/kueue/pkg/controller/jobs/deployment"` added  on `pkg/controller/jobs/jobs.go` and sort list changed on code-generator. In any case, `make verify` should throw an error if something not correct.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-05T09:20:09Z

- **config/components/webhook/manifests.yaml:7** by [@mimowo](https://github.com/mimowo) — 2024-09-05T09:20:08Z
```diff
@@ -4,6 +4,25 @@ kind: MutatingWebhookConfiguration
 metadata:
   name: mutating-webhook-configuration
 webhooks:
+- admissionReviewVersions:
```

  sgtm, thanks!

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-09-05T14:49:25Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-09-05T14:49:25Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  NewJob and jobframework.GenericJob would require a lot of methods to be implemented, probably that's why

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-09-05T14:49:28Z

- **pkg/controller/jobframework/integrationmanager.go:270** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-09-05T14:49:28Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
```

  NewJob and jobframework.GenericJob would require a lot of methods to be implemented, probably that's why

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2024-09-05T15:37:21Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T15:37:21Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > NewJob and jobframework.GenericJob would require a lot of methods to be implemented, probably that's why
  
  This is correct, I started out implementing the interface for deployment_controller, but a lot of the methods there are not applicable because it being no-op controller.  For all no-op controllers, there is a need to find a way to avoid implementing GenericJob interface.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T15:38:44Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T15:38:44Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  Can we implement jobframework.GenericJob? If I correct understood it's required option https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/#job-framework no?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-09-05T15:39:10Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-09-05T15:39:10Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  WDYT about getting gvk from JobType?
  The only change requeired:
  ```
  if ok && gvk == integration.JobType.GetObjectKind().GroupVersionKind() {
  			return integration, true
  		}
  		```

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2024-09-05T15:40:55Z

- **pkg/controller/jobframework/integrationmanager.go:270** by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T15:40:55Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
```

  >  Also, this check could be done only when ok - so you could do if ok && matchingGVK(integration, gvk)
  
  The check on ok passes, but the ingetration.NewJob() is nil for deployment_controller. And like @vladikkuzn said setting NewJob to a non-nil value means implementing GenericJob interface for a deployment_controller which didn't make sense. The idea was that GenericJob is a big interface, decomposing it into smaller ones will help with other no-op controllers like deployment_controller in the future.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-09-05T15:43:38Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-09-05T15:43:38Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > Can we implement jobframework.GenericJob? If I correct understood it's required option https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/#job-framework no?
  
  The problem that it's not a job :) It's "serving workload"
  I don't think it can be suspended and so on

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-05T15:46:08Z

- **pkg/controller/jobframework/integrationmanager.go:270** by [@mimowo](https://github.com/mimowo) — 2024-09-05T15:46:08Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
```

  Thanks for explaining, sgtm. In the second part of the comment I meant just a small refactor:
  
  ```golang
  func matchingGVK(integration, gvk) bool {
    if integration.NewJob != nil {
        return gvk == integration.NewJob().GVK()
     } else {
      return gvk == integration.GVKer().GVK()
    }
  }
  ```
  then use it `if ok && gvk == matchingGVK(integration, gvk) {`, to avoid calling NewJob if `!ok`. Would this work?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2024-09-05T15:49:18Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T15:49:18Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > WDYT about getting gvk from JobType?
  
  This seems reasonable. Given that we can find GVK from JobType, I will go ahead and remove that method form the interface?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T15:49:39Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T15:49:39Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  The problematic part that we are using GenericJob on the kueuectl to filter pods by Job https://github.com/kubernetes-sigs/kueue/blob/ece18974690b4e59cfa179b4514da0cffcd53fda/cmd/kueuectl/app/list/list_pods.go#L231-L240.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T15:51:13Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T15:51:13Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > > Can we implement jobframework.GenericJob? If I correct understood it's required option https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/#job-framework no?
  > 
  > The problem that it's not a job :) It's "serving workload" I don't think it can be suspended and so on
  
  If it is not a Job should we move it to another directory?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-05T15:59:40Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mimowo](https://github.com/mimowo) — 2024-09-05T15:59:40Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > If it is not a Job should we move it to another directory?
  
  This might be a good idea, like "workload", but I would suggest to focus this PR on fixing the issue, and we could follow up with a structure cleanup.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T16:05:09Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T16:05:09Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > WDYT about getting gvk from JobType? The only change requeired:
  > 
  > ```
  > if ok && gvk == integration.JobType.GetObjectKind().GroupVersionKind() {
  > 			return integration, true
  > 		}
  > 		```
  > ```
  
  I think this solution is better. But we also need to check [here](https://github.com/kubernetes-sigs/kueue/blob/ece18974690b4e59cfa179b4514da0cffcd53fda/cmd/kueuectl/app/list/list_pods.go#L231) if `NewJob` callback is implemented. If not just skip it `return "", nil`.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2024-09-05T16:18:11Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T16:18:11Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  @vladikkuzn checking from jobtype is not working
  > WDYT about getting gvk from JobType?
  
  Reason being the JobType objects are empty, for example
  ```
  		JobType:        &appsv1.Deployment{},
  ``` 
  in deployment controller hence their gvks are empty, what we need is a filled GVK like 
  ```
      gvk = appsv1.SchemeGroupVersion.WithKind("Deployment")
  ```

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2024-09-05T16:20:30Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T16:20:30Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > The problematic part that we are using GenericJob on the kueuectl to filter pods by Job
  
  sorry I am not following what the problem is here? IIUC the deployment controller has to be in jobframework because of its dependencies on the pod controller which is an actual implementation of GenericJob

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-05T16:47:03Z

- **pkg/controller/jobframework/integrationmanager.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-05T16:47:03Z
```diff
@@ -56,6 +56,8 @@ type ReconcilerFactory func(client client.Client, record record.EventRecorder, o
 type IntegrationCallbacks struct {
 	// NewJob creates a new instance of job
 	NewJob func() GenericJob
+	// GVKer returns the GVK for job
+	GVKer func() GVKer
```

  ```suggestion
  	GVK schema.GroupVersionKind
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-05T16:47:35Z

- **pkg/controller/jobframework/integrationmanager.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-05T16:47:35Z
```diff
@@ -56,6 +56,8 @@ type ReconcilerFactory func(client client.Client, record record.EventRecorder, o
 type IntegrationCallbacks struct {
 	// NewJob creates a new instance of job
 	NewJob func() GenericJob
+	// GVKer returns the GVK for job
+	GVKer func() GVKer
```

  It doesn't look like we need the indirection.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T16:49:38Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T16:49:37Z
```diff
@@ -265,7 +267,13 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		var g GVKer
+		if integration.NewJob != nil {
+			g = integration.NewJob()
+		} else {
+			g = integration.GVKer()
+		}
```

  > > The problematic part that we are using GenericJob on the kueuectl to filter pods by Job
  > 
  > sorry I am not following what the problem is here? IIUC the deployment controller has to be in jobframework because of its dependencies on the pod controller which is an actual implementation of GenericJob
  
  We are using `NewJob` callback [here](https://github.com/kubernetes-sigs/kueue/blob/ece18974690b4e59cfa179b4514da0cffcd53fda/cmd/kueuectl/app/list/list_pods.go#L231). But if it's not implemented we will have a panic. So I propose just to check it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-05T17:47:40Z

/lgtm
/approve

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T17:49:52Z

- **cmd/kueuectl/app/list/list_pods.go:232** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T17:40:30Z
```diff
@@ -228,6 +228,9 @@ func (o *PodOptions) getPodLabelSelector() (string, error) {
 		return "", nil
 	}
 
+	if cbs.NewJob == nil {
+		return "", fmt.Errorf("NewJob is not defined for integration job")
```

  ```suggestion
  		return "", nil
  ```
  
  We already have an error [here](https://github.com/kubernetes-sigs/kueue/blob/c7b367ec851b862f831fa5df955598702f0116ee/cmd/kueuectl/app/list/list_pods.go#L107).

- **pkg/controller/jobframework/integrationmanager.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T17:44:03Z
```diff
@@ -56,6 +56,8 @@ type ReconcilerFactory func(client client.Client, record record.EventRecorder, o
 type IntegrationCallbacks struct {
 	// NewJob creates a new instance of job
 	NewJob func() GenericJob
+	// GVK holds the schema information for the job
```

  ```suggestion
  	// GVK holds the schema information for the job
  	// (this callback is optional)
  ```

- **pkg/controller/jobframework/integrationmanager.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T17:45:47Z
```diff
@@ -56,6 +56,8 @@ type ReconcilerFactory func(client client.Client, record record.EventRecorder, o
 type IntegrationCallbacks struct {
 	// NewJob creates a new instance of job
 	NewJob func() GenericJob
+	// GVK holds the schema information for the job
```

  Or maybe we can implement GVKs in all jobs. @alculquicondor WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-05T17:56:38Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-05T17:56:38Z
```diff
@@ -265,7 +267,10 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		if ok && integration.NewJob == nil && integration.GVK == gvk {
+			return integration, true
+		}
+		if ok && integration.NewJob != nil && integration.NewJob().GVK() == gvk {
 			return integration, true
 		}
```

  I would like to use @mimowo [suggestion](https://github.com/kubernetes-sigs/kueue/pull/2988#discussion_r1745789652).
  
  ```
  func matchingGVK(integration, gvk) bool {
    if integration.NewJob != nil {
        return gvk == integration.NewJob().GVK()
     } else {
      return gvk == integration.GVK
    }
  }
  ```
  
  WDYT?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2024-09-05T17:59:00Z

- **pkg/controller/jobframework/integrationmanager.go:59** by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T17:59:00Z
```diff
@@ -56,6 +56,8 @@ type ReconcilerFactory func(client client.Client, record record.EventRecorder, o
 type IntegrationCallbacks struct {
 	// NewJob creates a new instance of job
 	NewJob func() GenericJob
+	// GVK holds the schema information for the job
```

  Can we defer the implementation changes across the board until we find a better way? as noted here https://github.com/kubernetes-sigs/kueue/pull/2988#discussion_r1745809416 
  
  I will make the comment update. If someone can help me create an issue for the bigger refactor, I can start looking at it

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-05T18:09:18Z

- **pkg/controller/jobframework/integrationmanager.go:59** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-05T18:09:18Z
```diff
@@ -56,6 +56,8 @@ type ReconcilerFactory func(client client.Client, record record.EventRecorder, o
 type IntegrationCallbacks struct {
 	// NewJob creates a new instance of job
 	NewJob func() GenericJob
+	// GVK holds the schema information for the job
```

  We could remove the `GVK` method from the Job interface and just populate the IntegrationCallbacks struct. We have a gvk constant for every integration already, IIRC.
  
  But I'm ok leaving it for another PR.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2024-09-05T18:12:10Z

- **pkg/controller/jobframework/integrationmanager.go:273** by [@alaypatel07](https://github.com/alaypatel07) — 2024-09-05T18:12:09Z
```diff
@@ -265,7 +267,10 @@ func GetIntegration(name string) (IntegrationCallbacks, bool) {
 func GetIntegrationByGVK(gvk schema.GroupVersionKind) (IntegrationCallbacks, bool) {
 	for _, name := range manager.getList() {
 		integration, ok := GetIntegration(name)
-		if ok && gvk == integration.NewJob().GVK() {
+		if ok && integration.NewJob == nil && integration.GVK == gvk {
+			return integration, true
+		}
+		if ok && integration.NewJob != nil && integration.NewJob().GVK() == gvk {
 			return integration, true
 		}
```

  yes that is definitely preferred, missed that, updated now.
