# PR #300: Trigger re-queueing of workloads on namespace label update

**Summary**: Trigger re-queueing of workloads on namespace label update

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/300

**Last updated**: 2022-07-18T18:25:16Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-ns2` → `main`
- **Created**: 2022-07-15T17:24:54Z
- **Updated**: 2022-07-18T18:25:16Z
- **Merged**: 2022-07-18T18:25:16Z
- **Closed**: 2022-07-18T18:25:16Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 4   **+123 / -16**

## Description


#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #234 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-07-15T17:25:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/300#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-07-18T17:47:12Z

/label tide/merge-method-squash

I will just use auto squash

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-18T18:24:42Z

/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-15T17:45:25Z

- **test/integration/scheduler/scheduler_test.go:464** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-15T17:42:35Z
```diff
@@ -461,7 +461,6 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
 
 			cq = testing.MakeClusterQueue("cluster-queue-with-selector").
-				QueueingStrategy(kueue.StrictFIFO).
```

  This seems problematic (but beyond the scope of this PR): if a namespace doesn't match the CQ, it shouldn't block other workloads, even if StrictFIFO.

- **test/integration/scheduler/scheduler_test.go:499** by [@alculquicondor](https://github.com/alculquicondor) — 2022-07-15T17:45:20Z
```diff
@@ -485,7 +484,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			framework.ExpectedResourceFlavorToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
 		})
 
-		ginkgo.It("Should schedule jobs from the selected namespaces", func() {
+		ginkgo.It("Should schedule jobs from the selected namespaces after label update", func() {
```

  We need a better test case where workloads in another namespace stay in inadmissible state. But maybe we can do that in unit tests

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-15T19:03:26Z

- **test/integration/scheduler/scheduler_test.go:464** by [@ahg-g](https://github.com/ahg-g) — 2022-07-15T19:03:26Z
```diff
@@ -461,7 +461,6 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
 
 			cq = testing.MakeClusterQueue("cluster-queue-with-selector").
-				QueueingStrategy(kueue.StrictFIFO).
```

  Right, perhaps those workloads shouldn't be added to the CQ cache at all, we should add them only when the namespace matches. This means that those workloads wouldn't be counted in the CQ metrics, which I think is reasonable.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-15T19:05:09Z

- **test/integration/scheduler/scheduler_test.go:499** by [@ahg-g](https://github.com/ahg-g) — 2022-07-15T19:05:09Z
```diff
@@ -485,7 +484,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			framework.ExpectedResourceFlavorToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
 		})
 
-		ginkgo.It("Should schedule jobs from the selected namespaces", func() {
+		ginkgo.It("Should schedule jobs from the selected namespaces after label update", func() {
```

  added to this test

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-07-18T11:58:30Z

- **pkg/controller/core/clusterqueue_controller.go:266** by [@denkensk](https://github.com/denkensk) — 2022-07-18T11:54:21Z
```diff
@@ -241,13 +243,47 @@ func (h *cqWorkloadHandler) requestForWorkloadClusterQueue(w *kueue.Workload) *r
 	}
 }
 
+// cqNamespaceHandler handles namespace update events.
+type cqNamespaceHandler struct {
+	qManager *queue.Manager
+	cache    *cache.Cache
+}
+
+func (h *cqNamespaceHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+}
+
+func (h *cqNamespaceHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldNs := e.ObjectOld.(*corev1.Namespace)
+	oldMatchingCqs := h.cache.MatchingClusterQueues(oldNs.Labels)
+	newNs := e.ObjectNew.(*corev1.Namespace)
+	newMatchingCqs := h.cache.MatchingClusterQueues(newNs.Labels)
+	cqs := sets.NewString()
+	for cq := range newMatchingCqs {
+		if !oldMatchingCqs.Has(cq) {
+			cqs.Insert(cq)
+		}
+	}
+	h.qManager.QueueInadmissibleWorkloads(cqs)
```

  Shall we consider just moving the workloads in the updated NS in the future？not for this pr.

- **test/integration/scheduler/scheduler_test.go:464** by [@denkensk](https://github.com/denkensk) — 2022-07-18T11:58:25Z
```diff
@@ -461,7 +461,6 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
 
 			cq = testing.MakeClusterQueue("cluster-queue-with-selector").
-				QueueingStrategy(kueue.StrictFIFO).
```

  But I think even we don't put it into the CQ cache, we still need to tell the message(by event) to the User why this workload can't be scheduled successfully(not match).

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-18T13:02:01Z

- **pkg/controller/core/clusterqueue_controller.go:266** by [@ahg-g](https://github.com/ahg-g) — 2022-07-18T13:02:01Z
```diff
@@ -241,13 +243,47 @@ func (h *cqWorkloadHandler) requestForWorkloadClusterQueue(w *kueue.Workload) *r
 	}
 }
 
+// cqNamespaceHandler handles namespace update events.
+type cqNamespaceHandler struct {
+	qManager *queue.Manager
+	cache    *cache.Cache
+}
+
+func (h *cqNamespaceHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+}
+
+func (h *cqNamespaceHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldNs := e.ObjectOld.(*corev1.Namespace)
+	oldMatchingCqs := h.cache.MatchingClusterQueues(oldNs.Labels)
+	newNs := e.ObjectNew.(*corev1.Namespace)
+	newMatchingCqs := h.cache.MatchingClusterQueues(newNs.Labels)
+	cqs := sets.NewString()
+	for cq := range newMatchingCqs {
+		if !oldMatchingCqs.Has(cq) {
+			cqs.Insert(cq)
+		}
+	}
+	h.qManager.QueueInadmissibleWorkloads(cqs)
```

  we have a comment for that somewhere, to do finer grained requeueing, not just for this, but other cases as well I think. But yeah that is outside the scope of this PR

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-18T13:02:35Z

- **test/integration/scheduler/scheduler_test.go:464** by [@ahg-g](https://github.com/ahg-g) — 2022-07-18T13:02:35Z
```diff
@@ -461,7 +461,6 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
 
 			cq = testing.MakeClusterQueue("cluster-queue-with-selector").
-				QueueingStrategy(kueue.StrictFIFO).
```

  we can do that in the workload controller, just like for cases where the CQ is not existent.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-07-18T15:30:46Z

- **pkg/cache/cache_test.go:1550** by [@kerthcet](https://github.com/kerthcet) — 2022-07-18T13:06:16Z
```diff
@@ -1545,6 +1545,43 @@ func TestUpdateWithFlavors(t *testing.T) {
 	}
 }
 
+func TestMatchingClusterQueues(t *testing.T) {
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("matching1").
```

  Shall we add another clusterQueue specify `MatchLabels` like:
  ```
  		utiltesting.MakeClusterQueue("matching3").
  			NamespaceSelector(&metav1.LabelSelector{
  				MatchLabels: map[string]string{"dep": "eng"},
  			}).Obj()
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-18T17:33:18Z

- **test/integration/scheduler/scheduler_test.go:464** by [@ahg-g](https://github.com/ahg-g) — 2022-07-18T17:33:17Z
```diff
@@ -461,7 +461,6 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
 
 			cq = testing.MakeClusterQueue("cluster-queue-with-selector").
-				QueueingStrategy(kueue.StrictFIFO).
```

  opened https://github.com/kubernetes-sigs/kueue/issues/301 to track this issue

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-07-18T17:38:26Z

- **pkg/cache/cache_test.go:1550** by [@ahg-g](https://github.com/ahg-g) — 2022-07-18T17:38:26Z
```diff
@@ -1545,6 +1545,43 @@ func TestUpdateWithFlavors(t *testing.T) {
 	}
 }
 
+func TestMatchingClusterQueues(t *testing.T) {
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("matching1").
```

  I would avoid testing the selector itself, that is not in scope for the logic we are adding.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-07-18T17:43:54Z

good for squash
