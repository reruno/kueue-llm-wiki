# PR #3005: Create resource_test Suite

**Summary**: Create resource_test Suite

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3005

**Last updated**: 2024-09-12T17:05:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `resource_test` → `main`
- **Created**: 2024-09-06T12:40:01Z
- **Updated**: 2024-09-12T17:05:55Z
- **Merged**: 2024-09-12T16:25:20Z
- **Closed**: 2024-09-12T16:25:20Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@macsko](https://github.com/macsko)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc), [@macsko](https://github.com/macsko)
- **Changed files**: 3   **+299 / -3**

## Description

#### What type of PR is this?
/kind cleanup
/kind feature

#### What this PR does / why we need it:
Add test suite to give more confidence in the new resource accounting logic (#2881). Part of #79

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-06T12:40:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f99e096f6cf3d39ac6bab87e0d1191f78fb5b1bf |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e30ac48670d2000806b0d8 |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-06T12:45:07Z

/assign @macsko

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-11T20:28:24Z

LGTM label has been added.  <details>Git tree hash: f8e85521fc6fee05518331b49fc9ba659a7b8cfb</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-12T16:00:12Z

LGTM label has been added.  <details>Git tree hash: 6480fae1d58115bf463dbd6657ba264c0c29eabf</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-12T16:00:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3005#pullrequestreview-2300780221" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3005#" title="Author self-approved">gabesaba</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T16:03:29Z

/unhold

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-06T17:45:09Z

- **pkg/cache/resource_test.go:152** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T17:29:56Z
```diff
@@ -0,0 +1,275 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -1_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -11000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: -1000},
```

  interesting....
  What would happen if those 10 CPUs that CQ2 has are split into two CQs?
  What would the usage on each of them be, and why?

- **pkg/cache/resource_test.go:184** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T17:42:54Z
```diff
@@ -0,0 +1,275 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -1_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -11000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: -1000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+			},
+		},
+		"lending and borrowing limits respected": {
+			// cohort has 40 (red, cpu) total
+			// cq1 can access 33 = 40 - 7 reserved
+			// cq2 can access 12, due to borrowing limit
+			// cq3 can access 40, as there are no restrictions.
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "2").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq3").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "3").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 5_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 3_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 5_000},
```

  I think each of these values require a comment

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-11T08:31:30Z

- **pkg/cache/resource_test.go:152** by [@gabesaba](https://github.com/gabesaba) — 2024-09-11T08:31:30Z
```diff
@@ -0,0 +1,275 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -1_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -11000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: -1000},
```

  `cohort` SubtreeQuota is 20k (from `cohort`, and CQ1). Usage in cohort is 31k.  When checking usage, CQ2 and CQ3 would find 5k available locally and -11k in the parent. Adding those numbers would result in `available` -6k for each of those CQs.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-11T08:47:39Z

- **pkg/cache/resource_test.go:152** by [@gabesaba](https://github.com/gabesaba) — 2024-09-11T08:47:39Z
```diff
@@ -0,0 +1,275 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -1_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -11000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: -1000},
```

  When a negative number is returned, it indicates that some CQ/Cohort in the CohortTree is using too much resources - which can happen after a change of available resources, or switching parent Cohort.
  
  This ensures that, for any CohortTree, we won't admit a workload that would result in using more of a FlavorResource than is in the CohortTree. I think it is desirable that this also affects CQs which do not lend capacity to or borrow capacity from the CohortTree - once we are overadmited, we should prevent any admissions which worsen the debt.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-11T09:13:32Z

- **pkg/cache/resource_test.go:184** by [@gabesaba](https://github.com/gabesaba) — 2024-09-11T09:13:31Z
```diff
@@ -0,0 +1,275 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -1_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -11000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: -1000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+			},
+		},
+		"lending and borrowing limits respected": {
+			// cohort has 40 (red, cpu) total
+			// cq1 can access 33 = 40 - 7 reserved
+			// cq2 can access 12, due to borrowing limit
+			// cq3 can access 40, as there are no restrictions.
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "2").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq3").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "3").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 5_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 3_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 5_000},
```

  Done. Also modified the numbers slightly to make the math more simple (3->5)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T20:28:00Z

- **pkg/cache/resource_test.go:152** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-11T20:28:00Z
```diff
@@ -0,0 +1,275 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -1_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -11000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: -1000},
```

  As discussed offline, consider just returning zero when the CQ is oversubscribed.
  IIUC, it shouldn't have any effect on algorithms, and this number is not reported to the user.
  The advantage is just more maintainable unit tests.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-11T20:28:18Z

/approve
/lgtm
/hold
(in case my suggestion is easily achievable)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-12T12:32:14Z

- **pkg/cache/resource_test.go:152** by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T12:32:13Z
```diff
@@ -0,0 +1,275 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -1_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: -11000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: -1000},
```

  Done. We report the capacity lacked to the user during flavor assignment. Updated the overadmission integration test with a new number.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-12T14:25:54Z

- **pkg/cache/resource_test.go:223** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T14:25:54Z
```diff
@@ -0,0 +1,292 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11_000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				// even though cq2 doesn't borrow/lend
+				// resources from Cohort, the
+				// misbehaving cq1's usage spills into
+				// its available, to prevent
+				// overadmission for (red,cpu) within
+				// the CohortTree.
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 0},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+			},
+		},
+		"lending and borrowing limits respected": {
+			// cohort has 40 (red, cpu) total
+			// cq1 can access 35 = 40 - 5 reserved
+			// cq2 can access 12, due to borrowing limit
+			// cq3 can access 40, as there are no restrictions.
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "2").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq3").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "5").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 6_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				// cq1 uses 20k, cq2 uses 10k, and
+				// cq3 uses 1k (not counting first 5k
+				// in guaranteedQuota), resulting in
+				// 4k left of potential 35k.
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 4_000},
+				// cq2 has access to only 2k of the
+				// remaining 4k due to its borrowing
+				// limit.
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_000},
+				// 40k - (20k + 10k + 6k) = 4k
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 4_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 35_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 12_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 40_000},
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			cache := New(utiltesting.NewFakeClient())
+			cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("red").Obj())
+			cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("blue").Obj())
+			for _, cq := range tc.clusterQueues {
+				_ = cache.AddClusterQueue(ctx, &cq)
+			}
+			for _, cohort := range tc.cohorts {
+				cache.AddCohort(&cohort)
+			}
+
+			snapshot := cache.Snapshot()
+
+			t.Run("before adding usage", func(t *testing.T) {
```

  remove `t.Run` here, as the remaining subtests depend on it.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-12T15:38:17Z

- **pkg/cache/resource_test.go:223** by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T15:38:16Z
```diff
@@ -0,0 +1,292 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestAvailable(t *testing.T) {
+	cases := map[string]struct {
+		cohorts                  []kueuealpha.Cohort
+		clusterQueues            []kueue.ClusterQueue
+		usage                    map[string]resources.FlavorResourceQuantities
+		wantAvailable            map[string]resources.FlavorResourceQuantities
+		wantPotentiallyAvailable map[string]resources.FlavorResourceQuantities
+	}{
+		"base cqs": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "0").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "5").Obj(),
+						*utiltesting.MakeFlavorQuotas("blue").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {
+					{Flavor: "red", Resource: "cpu"}:  2_500,
+					{Flavor: "blue", Resource: "cpu"}: 1_000,
+				},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_500, {Flavor: "blue", Resource: "cpu"}: 9_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 5_000, {Flavor: "blue", Resource: "cpu"}: 10_000},
+			},
+		},
+		"cqs with cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "9").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 1000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 500},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 28_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 28_500},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 29_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 30_000},
+			},
+		},
+		"cq borrows from cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage:                    map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 11_000}},
+			wantAvailable:            map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 9_000}},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000}},
+		},
+		"cq oversubscription spills into cohort": {
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "0", "0").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 31_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 0},
+				// even though cq2 doesn't borrow/lend
+				// resources from Cohort, the
+				// misbehaving cq1's usage spills into
+				// its available, to prevent
+				// overadmission for (red,cpu) within
+				// the CohortTree.
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 0},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+			},
+		},
+		"lending and borrowing limits respected": {
+			// cohort has 40 (red, cpu) total
+			// cq1 can access 35 = 40 - 5 reserved
+			// cq2 can access 12, due to borrowing limit
+			// cq3 can access 40, as there are no restrictions.
+			clusterQueues: []kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue("cq1").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq2").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "2").Obj(),
+					).ClusterQueue,
+				utiltesting.MakeClusterQueue("cq3").
+					Cohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10", "", "5").Obj(),
+					).ClusterQueue,
+			},
+			cohorts: []kueuealpha.Cohort{
+				utiltesting.MakeCohort("cohort").
+					ResourceGroup(
+						*utiltesting.MakeFlavorQuotas("red").Resource("cpu", "10").Obj(),
+					).Cohort,
+			},
+			usage: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 20_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 10_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 6_000},
+			},
+			wantAvailable: map[string]resources.FlavorResourceQuantities{
+				// cq1 uses 20k, cq2 uses 10k, and
+				// cq3 uses 1k (not counting first 5k
+				// in guaranteedQuota), resulting in
+				// 4k left of potential 35k.
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 4_000},
+				// cq2 has access to only 2k of the
+				// remaining 4k due to its borrowing
+				// limit.
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 2_000},
+				// 40k - (20k + 10k + 6k) = 4k
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 4_000},
+			},
+			wantPotentiallyAvailable: map[string]resources.FlavorResourceQuantities{
+				"cq1": {{Flavor: "red", Resource: "cpu"}: 35_000},
+				"cq2": {{Flavor: "red", Resource: "cpu"}: 12_000},
+				"cq3": {{Flavor: "red", Resource: "cpu"}: 40_000},
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			cache := New(utiltesting.NewFakeClient())
+			cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("red").Obj())
+			cache.AddOrUpdateResourceFlavor(utiltesting.MakeResourceFlavor("blue").Obj())
+			for _, cq := range tc.clusterQueues {
+				_ = cache.AddClusterQueue(ctx, &cq)
+			}
+			for _, cohort := range tc.cohorts {
+				cache.AddCohort(&cohort)
+			}
+
+			snapshot := cache.Snapshot()
+
+			t.Run("before adding usage", func(t *testing.T) {
```

  Removed all 3, but left the blocks with a comment, so that the `got` variables are scoped.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-12T16:00:06Z

/lgtm
/approve
