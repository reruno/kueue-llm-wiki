# PR #3006: Support Hierarchical Cohorts in Cache/Snapshot

**Summary**: Support Hierarchical Cohorts in Cache/Snapshot

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3006

**Last updated**: 2024-09-20T13:37:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `hc_cache` → `main`
- **Created**: 2024-09-06T12:50:25Z
- **Updated**: 2024-09-20T13:37:43Z
- **Merged**: 2024-09-16T13:43:14Z
- **Closed**: 2024-09-16T13:43:14Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 10   **+665 / -27**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
Support HierarchicalCohorts (#79), with cycle-detection, in the cache and snapshot. 

#### Special notes for your reviewer:
I wrote tests which check resource usage. I will add them to this PR once #3005 merges.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-06T12:50:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9aae9eadc1126ec317c696969fe768c2385852f0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66e803a271318a0009a33e42 |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-06T12:52:01Z

/assign @mimowo

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T14:14:29Z

/cc

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-13T13:53:52Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-16T13:24:14Z

LGTM label has been added.  <details>Git tree hash: c3d67eb01af97818021dc8b9379cf9317d75678c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-16T13:24:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3006#pullrequestreview-2306636763" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3006#" title="Author self-approved">gabesaba</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-20T13:37:37Z

Only attaching release note to a single PR, where we define the API https://github.com/kubernetes-sigs/kueue/pull/2693

/release-note-edit

```release-note
NONE
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-06T18:18:33Z

- **pkg/cache/algorithm.go:22** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T17:57:16Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
+	checker := cycleChecker{cycles: make(map[string]bool, 8)}
```

  clarify in a comment that 8 is a reasonable upper-bound for hierarchy height

- **pkg/cache/algorithm.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T18:02:58Z
  `hierarchy.go`?

- **pkg/cache/algorithm.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T18:06:05Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
+	checker := cycleChecker{cycles: make(map[string]bool, 8)}
+	if checker.hasCycle(c) {
+		return nil, false
+	}
+	return getRootHelper(c), true
+}
+
+func getRootHelper(c *cohort) *cohort {
```

  ```suggestion
  func getRoot(c *cohort) *cohort {
  ```

- **pkg/cache/algorithm.go:21** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T18:06:27Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
```

  ```suggestion
  func getRootUnsafe(c *cohort) (*cohort, bool) {
  ```

- **pkg/cache/algorithm.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T18:07:46Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
+	checker := cycleChecker{cycles: make(map[string]bool, 8)}
+	if checker.hasCycle(c) {
+		return nil, false
+	}
+	return getRootHelper(c), true
+}
+
+func getRootHelper(c *cohort) *cohort {
+	if !c.HasParent() {
+		return c
+	}
+	return getRootHelper(c.Parent())
+}
+
+// cycleChecker checks for cycles in Cohorts, while memoizing the
+// result. This allows for O(n) cycle check within a scheduling cycle.
+type cycleChecker struct {
+	cycles map[string]bool
+}
+
+func (c *cycleChecker) hasCycle(cohort *cohort) bool {
+	if cohort == nil || !cohort.HasParent() {
+		return false
+	}
+	if c.cycles[cohort.GetName()] {
+		return true
+	}
```

  ```suggestion
  	if cycle, seen := c.cycles[cohort.GetName()]; seen {
  		return cycle
  	}
  ```
  ?

- **pkg/cache/algorithm.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T18:16:19Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
+	checker := cycleChecker{cycles: make(map[string]bool, 8)}
+	if checker.hasCycle(c) {
+		return nil, false
+	}
+	return getRootHelper(c), true
+}
+
+func getRootHelper(c *cohort) *cohort {
```

  I would expect the scheduling cycle to call this function directly, as opposed to check for cycles again.

- **pkg/cache/snapshot.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-06T18:18:28Z
```diff
@@ -84,12 +84,19 @@ func (c *Cache) Snapshot() Snapshot {
 		ResourceFlavors:          make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, len(c.resourceFlavors)),
 		InactiveClusterQueueSets: sets.New[string](),
 	}
+	cycleChecker := cycleChecker{cycles: make(map[string]bool, len(c.hm.Cohorts))}
```

  why do you need to check at this point?
  I think the information should be stored in the cache somehow. We need to communicate it to the CQ and cohort statuses anyways.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-12T14:21:17Z

- **pkg/cache/algorithm.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T12:36:23Z
  Deleted in this revision. We now have `cycle.go` in `hierarchy` package

- **pkg/cache/algorithm.go:21** by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T12:38:23Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
```

  shouldn't the other function be called unsafe, as it will enter an infinite loop if called with cycle?

- **pkg/cache/algorithm.go:22** by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T12:38:27Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
+	checker := cycleChecker{cycles: make(map[string]bool, 8)}
```

  Deleted in this revision.

- **pkg/cache/algorithm.go:48** by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T12:40:27Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+// getRoot returns the root of the Cohort tree IFF there was not a
+// cycle.
+func getRoot(c *cohort) (*cohort, bool) {
+	checker := cycleChecker{cycles: make(map[string]bool, 8)}
+	if checker.hasCycle(c) {
+		return nil, false
+	}
+	return getRootHelper(c), true
+}
+
+func getRootHelper(c *cohort) *cohort {
+	if !c.HasParent() {
+		return c
+	}
+	return getRootHelper(c.Parent())
+}
+
+// cycleChecker checks for cycles in Cohorts, while memoizing the
+// result. This allows for O(n) cycle check within a scheduling cycle.
+type cycleChecker struct {
+	cycles map[string]bool
+}
+
+func (c *cycleChecker) hasCycle(cohort *cohort) bool {
+	if cohort == nil || !cohort.HasParent() {
+		return false
+	}
+	if c.cycles[cohort.GetName()] {
+		return true
+	}
```

  This is better, thanks :)

- **pkg/cache/snapshot.go:87** by [@gabesaba](https://github.com/gabesaba) — 2024-09-12T14:17:17Z
```diff
@@ -84,12 +84,19 @@ func (c *Cache) Snapshot() Snapshot {
 		ResourceFlavors:          make(map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor, len(c.resourceFlavors)),
 		InactiveClusterQueueSets: sets.New[string](),
 	}
+	cycleChecker := cycleChecker{cycles: make(map[string]bool, len(c.hm.Cohorts))}
```

  I'm storing this structure in the hierarchy.Manager (and thus cache) now, but it will still be called again during snapshotting - using the memoized result.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-12T15:04:50Z

- **pkg/cache/cache_test.go:1059** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T14:51:10Z
```diff
@@ -1055,7 +1055,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			name: "create cohort",
 			operation: func(cache *Cache) error {
 				cohort := utiltesting.MakeCohort("cohort").Obj()
-				cache.AddCohort(cohort)
+				_ = cache.AddCohort(cohort)
 				return nil
```

  ```suggestion
  				return cache.AddCohort(cohort)
  ```

- **pkg/cache/cache_test.go:3562** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-12T15:04:49Z
```diff
@@ -3378,3 +3378,310 @@ func TestClusterQueueReadiness(t *testing.T) {
 		})
 	}
 }
+
+func TestCohortCycles(t *testing.T) {
+	t.Run("self cycle", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		cohort := utiltesting.MakeCohort("cohort").Parent("cohort").Obj()
+		if err := cache.AddCohort(cohort); err == nil {
+			t.Fatal("Expected failure when cycle")
+		}
+	})
+	t.Run("simple cycle", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		cohortA := utiltesting.MakeCohort("cohort-a").Parent("cohort-b").Obj()
+		cohortB := utiltesting.MakeCohort("cohort-b").Parent("cohort-c").Obj()
+		cohortC := utiltesting.MakeCohort("cohort-c").Parent("cohort-a").Obj()
+		if err := cache.AddCohort(cohortA); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		if err := cache.AddCohort(cohortB); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		if err := cache.AddCohort(cohortC); err == nil {
+			t.Fatal("Expected failure when cycle")
+		}
+	})
+	t.Run("clusterqueue add and update return error when cohort has cycle", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		ctx := context.Background()
+		cohortA := utiltesting.MakeCohort("cohort-a").Parent("cohort-b").Obj()
+		if err := cache.AddCohort(cohortA); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		cohortB := utiltesting.MakeCohort("cohort-b").Parent("cohort-c").Obj()
+		if err := cache.AddCohort(cohortB); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		cohortC := utiltesting.MakeCohort("cohort-c").Parent("cohort-a").Obj()
+		if err := cache.AddCohort(cohortC); err == nil {
+			t.Fatal("Expected failure when cycle")
+		}
+
+		// Error when creating CQ with parent Cohort-A
+		cq := utiltesting.MakeClusterQueue("cq").Cohort("cohort-a").Obj()
+		if err := cache.AddClusterQueue(ctx, cq); err == nil {
+			t.Fatal("Expected failure when adding cq to cohort with cycle")
+		}
+
+		// Error when updating CQ with parent Cohort-B
+		cq = utiltesting.MakeClusterQueue("cq").Cohort("cohort-b").Obj()
+		if err := cache.UpdateClusterQueue(cq); err == nil {
+			t.Fatal("Expected failure when updating cq to cohort with cycle")
+		}
+
+		// Delete Cohort C, breaking cycle
+		cache.DeleteCohort("cohort-c")
+
+		// Update succeeds
+		cq = utiltesting.MakeClusterQueue("cq").Cohort("cohort-b").Obj()
+		if err := cache.UpdateClusterQueue(cq); err != nil {
+			t.Fatal("Expected success")
+		}
+	})
+
+	t.Run("clusterqueue leaving cohort with cycle successfully updates new cohort", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		ctx := context.Background()
+		cycleCohort := utiltesting.MakeCohort("cycle").Parent("cycle").Obj()
+		if err := cache.AddCohort(cycleCohort); err == nil {
+			t.Fatal("Expected failure")
+		}
+
+		cohort := utiltesting.MakeCohort("cohort").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "10").FlavorQuotas).Obj()
+		if err := cache.AddCohort(cohort); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// Error when creating cq with parent that has cycle
+		cq := utiltesting.MakeClusterQueue("cq").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "5").FlavorQuotas).
+			Cohort("cycle").Obj()
+		if err := cache.AddClusterQueue(ctx, cq); err == nil {
+			t.Fatal("Expected failure")
+		}
+
+		// Successfully updated to cohort without cycle
+		cq.Spec.Cohort = "cohort"
+		if err := cache.UpdateClusterQueue(cq); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// cohort's SubtreeQuota contains resources from cq.
+		gotResource := cache.hm.Cohorts["cohort"].getResourceNode()
+		wantResource := ResourceNode{
+			Quotas: map[resources.FlavorResource]ResourceQuota{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: {Nominal: 10_000},
+			},
+			SubtreeQuota: resources.FlavorResourceQuantities{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: 15_000,
+			},
+			Usage: resources.FlavorResourceQuantities{},
+		}
+		if diff := cmp.Diff(wantResource, gotResource); diff != "" {
+			t.Errorf("Unexpected resource (-want,+got):\n%s", diff)
+		}
+	})
+
+	t.Run("clusterqueue joining cohort with cycle successfully updates old cohort", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		ctx := context.Background()
+		cycleCohort := utiltesting.MakeCohort("cycle").Parent("cycle").Obj()
+		if err := cache.AddCohort(cycleCohort); err == nil {
+			t.Fatal("Expected failure")
+		}
+		cohort := utiltesting.MakeCohort("cohort").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "10").FlavorQuotas).Obj()
+		if err := cache.AddCohort(cohort); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// Add CQ to cohort
+		cq := utiltesting.MakeClusterQueue("cq").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "5").FlavorQuotas).
+			Cohort("cohort").Obj()
+		if err := cache.AddClusterQueue(ctx, cq); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// cohort's SubtreeQuota contains resources from cq
+		gotResource := cache.hm.Cohorts["cohort"].getResourceNode()
+		wantResource := ResourceNode{
+			Quotas: map[resources.FlavorResource]ResourceQuota{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: {Nominal: 10_000},
+			},
+			SubtreeQuota: resources.FlavorResourceQuantities{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: 15_000,
+			},
+			Usage: resources.FlavorResourceQuantities{},
+		}
+		if diff := cmp.Diff(wantResource, gotResource); diff != "" {
+			t.Errorf("Unexpected resource (-want,+got):\n%s", diff)
+		}
+
+		// Updated to cycle
+		cq.Spec.Cohort = "cycle"
+		if err := cache.UpdateClusterQueue(cq); err == nil {
+			t.Fatal("Expected failure")
+		}
+
+		// Cohort's SubtreeQuota no longer contains resources from CQ.
+		gotResource = cache.hm.Cohorts["cohort"].getResourceNode()
+		wantResource = ResourceNode{
+			Quotas: map[resources.FlavorResource]ResourceQuota{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: {Nominal: 10_000},
+			},
+			SubtreeQuota: resources.FlavorResourceQuantities{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: 10_000,
+			},
+			Usage: resources.FlavorResourceQuantities{},
+		}
+		if diff := cmp.Diff(wantResource, gotResource); diff != "" {
+			t.Errorf("Unexpected resource (-want,+got):\n%s", diff)
+		}
+	})
+
+	t.Run("cohort switching cohorts updates both cohort trees", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		root1 := utiltesting.MakeCohort("root1").Obj()
+		root2 := utiltesting.MakeCohort("root2").Obj()
+		if err := cache.AddCohort(root1); err != nil {
+			t.Fatal("Expected success")
+		}
+		if err := cache.AddCohort(root2); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		cohort := utiltesting.MakeCohort("cohort").Parent("root1").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "10").FlavorQuotas).Obj()
+		if err := cache.AddCohort(cohort); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		t.Run("before move", func(t *testing.T) {
```

  do not use `t.Run` for non-independent tests cases

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-13T13:30:27Z

- **pkg/cache/cache_test.go:1059** by [@gabesaba](https://github.com/gabesaba) — 2024-09-13T13:30:27Z
```diff
@@ -1055,7 +1055,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 			name: "create cohort",
 			operation: func(cache *Cache) error {
 				cohort := utiltesting.MakeCohort("cohort").Obj()
-				cache.AddCohort(cohort)
+				_ = cache.AddCohort(cohort)
 				return nil
```

  Done.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-13T13:30:30Z

- **pkg/cache/cache_test.go:3562** by [@gabesaba](https://github.com/gabesaba) — 2024-09-13T13:30:30Z
```diff
@@ -3378,3 +3378,310 @@ func TestClusterQueueReadiness(t *testing.T) {
 		})
 	}
 }
+
+func TestCohortCycles(t *testing.T) {
+	t.Run("self cycle", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		cohort := utiltesting.MakeCohort("cohort").Parent("cohort").Obj()
+		if err := cache.AddCohort(cohort); err == nil {
+			t.Fatal("Expected failure when cycle")
+		}
+	})
+	t.Run("simple cycle", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		cohortA := utiltesting.MakeCohort("cohort-a").Parent("cohort-b").Obj()
+		cohortB := utiltesting.MakeCohort("cohort-b").Parent("cohort-c").Obj()
+		cohortC := utiltesting.MakeCohort("cohort-c").Parent("cohort-a").Obj()
+		if err := cache.AddCohort(cohortA); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		if err := cache.AddCohort(cohortB); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		if err := cache.AddCohort(cohortC); err == nil {
+			t.Fatal("Expected failure when cycle")
+		}
+	})
+	t.Run("clusterqueue add and update return error when cohort has cycle", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		ctx := context.Background()
+		cohortA := utiltesting.MakeCohort("cohort-a").Parent("cohort-b").Obj()
+		if err := cache.AddCohort(cohortA); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		cohortB := utiltesting.MakeCohort("cohort-b").Parent("cohort-c").Obj()
+		if err := cache.AddCohort(cohortB); err != nil {
+			t.Fatal("Expected success as no cycle yet")
+		}
+		cohortC := utiltesting.MakeCohort("cohort-c").Parent("cohort-a").Obj()
+		if err := cache.AddCohort(cohortC); err == nil {
+			t.Fatal("Expected failure when cycle")
+		}
+
+		// Error when creating CQ with parent Cohort-A
+		cq := utiltesting.MakeClusterQueue("cq").Cohort("cohort-a").Obj()
+		if err := cache.AddClusterQueue(ctx, cq); err == nil {
+			t.Fatal("Expected failure when adding cq to cohort with cycle")
+		}
+
+		// Error when updating CQ with parent Cohort-B
+		cq = utiltesting.MakeClusterQueue("cq").Cohort("cohort-b").Obj()
+		if err := cache.UpdateClusterQueue(cq); err == nil {
+			t.Fatal("Expected failure when updating cq to cohort with cycle")
+		}
+
+		// Delete Cohort C, breaking cycle
+		cache.DeleteCohort("cohort-c")
+
+		// Update succeeds
+		cq = utiltesting.MakeClusterQueue("cq").Cohort("cohort-b").Obj()
+		if err := cache.UpdateClusterQueue(cq); err != nil {
+			t.Fatal("Expected success")
+		}
+	})
+
+	t.Run("clusterqueue leaving cohort with cycle successfully updates new cohort", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		ctx := context.Background()
+		cycleCohort := utiltesting.MakeCohort("cycle").Parent("cycle").Obj()
+		if err := cache.AddCohort(cycleCohort); err == nil {
+			t.Fatal("Expected failure")
+		}
+
+		cohort := utiltesting.MakeCohort("cohort").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "10").FlavorQuotas).Obj()
+		if err := cache.AddCohort(cohort); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// Error when creating cq with parent that has cycle
+		cq := utiltesting.MakeClusterQueue("cq").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "5").FlavorQuotas).
+			Cohort("cycle").Obj()
+		if err := cache.AddClusterQueue(ctx, cq); err == nil {
+			t.Fatal("Expected failure")
+		}
+
+		// Successfully updated to cohort without cycle
+		cq.Spec.Cohort = "cohort"
+		if err := cache.UpdateClusterQueue(cq); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// cohort's SubtreeQuota contains resources from cq.
+		gotResource := cache.hm.Cohorts["cohort"].getResourceNode()
+		wantResource := ResourceNode{
+			Quotas: map[resources.FlavorResource]ResourceQuota{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: {Nominal: 10_000},
+			},
+			SubtreeQuota: resources.FlavorResourceQuantities{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: 15_000,
+			},
+			Usage: resources.FlavorResourceQuantities{},
+		}
+		if diff := cmp.Diff(wantResource, gotResource); diff != "" {
+			t.Errorf("Unexpected resource (-want,+got):\n%s", diff)
+		}
+	})
+
+	t.Run("clusterqueue joining cohort with cycle successfully updates old cohort", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		ctx := context.Background()
+		cycleCohort := utiltesting.MakeCohort("cycle").Parent("cycle").Obj()
+		if err := cache.AddCohort(cycleCohort); err == nil {
+			t.Fatal("Expected failure")
+		}
+		cohort := utiltesting.MakeCohort("cohort").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "10").FlavorQuotas).Obj()
+		if err := cache.AddCohort(cohort); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// Add CQ to cohort
+		cq := utiltesting.MakeClusterQueue("cq").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "5").FlavorQuotas).
+			Cohort("cohort").Obj()
+		if err := cache.AddClusterQueue(ctx, cq); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		// cohort's SubtreeQuota contains resources from cq
+		gotResource := cache.hm.Cohorts["cohort"].getResourceNode()
+		wantResource := ResourceNode{
+			Quotas: map[resources.FlavorResource]ResourceQuota{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: {Nominal: 10_000},
+			},
+			SubtreeQuota: resources.FlavorResourceQuantities{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: 15_000,
+			},
+			Usage: resources.FlavorResourceQuantities{},
+		}
+		if diff := cmp.Diff(wantResource, gotResource); diff != "" {
+			t.Errorf("Unexpected resource (-want,+got):\n%s", diff)
+		}
+
+		// Updated to cycle
+		cq.Spec.Cohort = "cycle"
+		if err := cache.UpdateClusterQueue(cq); err == nil {
+			t.Fatal("Expected failure")
+		}
+
+		// Cohort's SubtreeQuota no longer contains resources from CQ.
+		gotResource = cache.hm.Cohorts["cohort"].getResourceNode()
+		wantResource = ResourceNode{
+			Quotas: map[resources.FlavorResource]ResourceQuota{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: {Nominal: 10_000},
+			},
+			SubtreeQuota: resources.FlavorResourceQuantities{
+				{Flavor: "arm", Resource: corev1.ResourceCPU}: 10_000,
+			},
+			Usage: resources.FlavorResourceQuantities{},
+		}
+		if diff := cmp.Diff(wantResource, gotResource); diff != "" {
+			t.Errorf("Unexpected resource (-want,+got):\n%s", diff)
+		}
+	})
+
+	t.Run("cohort switching cohorts updates both cohort trees", func(t *testing.T) {
+		cache := New(utiltesting.NewFakeClient())
+		root1 := utiltesting.MakeCohort("root1").Obj()
+		root2 := utiltesting.MakeCohort("root2").Obj()
+		if err := cache.AddCohort(root1); err != nil {
+			t.Fatal("Expected success")
+		}
+		if err := cache.AddCohort(root2); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		cohort := utiltesting.MakeCohort("cohort").Parent("root1").
+			ResourceGroup(utiltesting.MakeFlavorQuotas("arm").Resource(corev1.ResourceCPU, "10").FlavorQuotas).Obj()
+		if err := cache.AddCohort(cohort); err != nil {
+			t.Fatal("Expected success")
+		}
+
+		t.Run("before move", func(t *testing.T) {
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-13T18:00:19Z

- **pkg/controller/core/cohort_controller.go:108** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-13T17:59:15Z
```diff
@@ -104,7 +104,9 @@ func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctr
 	}
 
 	log.V(2).Info("Cohort is being created or updated", "resources", cohort.Spec.ResourceGroups)
-	r.cache.AddOrUpdateCohort(&cohort)
+	if err := r.cache.AddOrUpdateCohort(&cohort); err != nil {
+		return ctrl.Result{}, err
```

  Doesn't this cause a requeue into the reconciler?
  
  Maybe we should log the issue, but then don't return the error.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-16T10:09:15Z

- **pkg/controller/core/cohort_controller.go:108** by [@gabesaba](https://github.com/gabesaba) — 2024-09-16T10:09:15Z
```diff
@@ -104,7 +104,9 @@ func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctr
 	}
 
 	log.V(2).Info("Cohort is being created or updated", "resources", cohort.Spec.ResourceGroups)
-	r.cache.AddOrUpdateCohort(&cohort)
+	if err := r.cache.AddOrUpdateCohort(&cohort); err != nil {
+		return ctrl.Result{}, err
```

  That makes sense, updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-16T13:24:09Z

/lgtm
/approve
