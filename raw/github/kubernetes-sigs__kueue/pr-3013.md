# PR #3013: KEP 2937: Configurable Resource Transformers

**Summary**: KEP 2937: Configurable Resource Transformers

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3013

**Last updated**: 2024-10-02T10:58:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@dgrove-oss](https://github.com/dgrove-oss)
- **Branch**: `kep-2937` → `main`
- **Created**: 2024-09-07T02:22:57Z
- **Updated**: 2024-10-02T10:58:11Z
- **Merged**: 2024-09-30T17:40:03Z
- **Closed**: 2024-09-30T17:40:04Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 2   **+649 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind feature
/kind documentation

#### What this PR does / why we need it:

KEP to propose a design to address issue #2937 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-07T02:23:06Z

Hi @dgrove-oss. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-07T02:23:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2a3c8cc6fb4ae9c8fcd1e3c6066c69315b4c738d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66f9a3ac4937e50007d712d9 |

### Comment by [@kannon92](https://github.com/kannon92) — 2024-09-09T13:36:42Z

/ok-to-test

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-10T21:25:48Z

I've speculatively updated the prototype from #2930 that I demoed two weeks ago to match the current KEP proposal and added unit tests.  Will look at integration tests next.  Doing the work on this branch in case anyone wants an early look: https://github.com/dgrove-oss/kueue/tree/2937-impl

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-11T21:41:35Z

Tentative implementation and tests are in #3026 (to be adjusted as this KEP evolves).

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-17T17:40:58Z

@mimowo -- what needs to be done to drive this KEP forward towards acceptance?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-17T17:52:36Z

Thank you for your patience.
I am reviewing KEPs submitted orders. I will review this KEP after the WorkflowSupport KEP.

- TAS (mostly finalized)
- OptinalGarbageCollection (reviewed)
- WaitForPodsReadyReplacementTimeout (reviewed and pending)

---

- WorkflowSupport (The head of my queue)
- This KEP (The second of my queue)
- DRA Support (The tail of my queue)

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-18T06:52:10Z

> @mimowo -- what needs to be done to drive this KEP forward towards acceptance?

Overall lgtm, left some questions that came to my mind about observability of the output (transformed) resources. I will try to review this in more detail on Friday.

### Comment by [@tardieu](https://github.com/tardieu) — 2024-09-19T18:37:33Z

Maybe the transformation matrix should be specific to and defined as part of the resource flavor. This would make it possible to not only define a picking order between, e.g., spot instances and on-demand VMs but also to reflect pricing differences between different types VMs, or xPU units for different xPUs. Concretely, we could specify that 1 CPU unit (or GPU unit) on node type 1 is worth 2 CPU units on node type 2.

Transformation matrices could also be attached to cluster queues for finer-grained control, but the maintenance overhead would be significantly higher for cluster admins, especially if these matrices are there to cope with node heterogeneity.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-19T21:20:41Z

> Maybe the transformation matrix should be specific to and defined as part of the resource flavor. 

I added putting the matrices on ClusterQueue or ResourceFlavors as options in the KEP.  Thanks for suggesting it.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-19T21:49:00Z

> Thank you for your patience. I am reviewing KEPs submitted orders. I will review this KEP after the WorkflowSupport KEP.
> 
> * TAS (mostly finalized)
> * OptinalGarbageCollection (reviewed)
> * WaitForPodsReadyReplacementTimeout (reviewed and pending)
> 
> * WorkflowSupport (The head of my queue)
> * This KEP (The second of my queue)
> * DRA Support (The tail of my queue)

I just landed here.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T21:28:37Z

Thank you @mimowo @alculquicondor and @tenzen-y for your detailed comments.  I've updated the KEP to reflect them and it is ready for you to review again when you have time.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-27T16:22:03Z

/lgtm
/approve
/hold 
In case @tenzen-y or @alculquicondor have more comments

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-27T16:22:09Z

LGTM label has been added.  <details>Git tree hash: bd871082b033cafe0f76dd783c9112f7d7f7c3f9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-27T16:22:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3013#" title="Author self-approved">dgrove-oss</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3013#issuecomment-2379645589" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-29T19:02:50Z

Made a small fix to the observability to show that `Status.ResourceRequests`  an [] with one entry per PodSet (similar to Admission.PodSetAssignments).  No other changes.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-30T17:25:30Z

/lgtm
/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-30T17:25:36Z

LGTM label has been added.  <details>Git tree hash: 5ccfe3f64bbe46b0bbc0c6cd174f593144e69d7b</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-09-09T13:46:02Z

- **keps/2937-resource-transformer/README.md:146** by [@kannon92](https://github.com/kannon92) — 2024-09-09T13:39:09Z
```diff
@@ -0,0 +1,291 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in the  `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single extended resource `kueue.x-k8s.io/accelerator-memory` and completely elide
+the MIG extended resource from the quota calculation.
+
+Concretely, the cluster admin would register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+The covered resources in a ClusterQueue would only need to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to list every MIG variant resource. 
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be `false` so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
+
+  // should the input resource be replaced (true) or retained (false)
```

  I usually see people discourage the use of bools. I would use an enum to represent replaced and retained. It is a bit easier to read than a bool. It also allows for extensions if there are other options that one wants to add.

- **keps/2937-resource-transformer/README.md:260** by [@kannon92](https://github.com/kannon92) — 2024-09-09T13:40:39Z
```diff
@@ -0,0 +1,291 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in the  `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single extended resource `kueue.x-k8s.io/accelerator-memory` and completely elide
+the MIG extended resource from the quota calculation.
+
+Concretely, the cluster admin would register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+The covered resources in a ClusterQueue would only need to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to list every MIG variant resource. 
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be `false` so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
+
+  // should the input resource be replaced (true) or retained (false)
+  Replace bool `json:"replace"`
+}
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the map key is the input resource name
+  Transformations map[corev1.ResourceName]ResourceTransformation `json:"transformations"`
+}
+```
+A key advantage of defining a single object is that it gives a cluster admin
+a single source of truth.  By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+### Option 2: Singleton Matrix Output Focused
+This is similar to Option 1, but makes the output resource the
+primary key.  
+```go
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the first map key is the output resource name
+  // and the inner map key is the input resource name
+  Transformations map[corev1.ResourceName]map[corev1.ResourceName]resource.Quantity `json:"transformations"`
+
+  // The map key is the input resource name. True means it should be replaced after it is transformed
+  ReplaceInput map[corev1.ResourceName]bool `json:"replaceInput"`
+}
+```
+
+Grouping by output makes it easier for the cluster admin to see/specify
+all the resources that map into a given output as a unit.  However it
+comes at the cost of a separate map to specify the replace boolean. 
+It may also be confusing that the `Transformations` map is keyed by
+output name as the primary key while `ReplaceInput` is keyed by input name.
+
+### Comparison
+
+Consider the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `true`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `false`)
+
+Option 1
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      replace: true
+      functions:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      replace: true
+      functions:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      replace: true
+      functions:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      replace: false
+      functions:
+        kueue.x-k8s.io/credits: 1
+```
+
+Option 2
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    kueue.x-k8s.io/accelerator-memory:
+      nvidia.com/mig-1g.5gb: 5G
+      nvidia.com/mig-2g.10gb: 10G
+      nvidia.com/mig-4g.20gb: 20G
+    kueue.x-k8s.io/credits:
+      nvidia.com/mig-1g.5gb: 10
+      nvidia.com/mig-2g.10gb: 15
+      nvidia.com/mig-4g.20gb: 30
+      cpu: 1
+  replaceInput:
+    nvidia.com/mig-1g.5gb: true
+    nvidia.com/mig-2g.10gb: true
+    nvidia.com/mig-4g.20gb: true
+```
+
+Option 2 is likely to be terser, but I find Option 1 slightly more intutive because
+I tend to think of functions in the "forward direction" (mapping inputs to outputs).
+Either is viable and has similar implementation complexity.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+None
+
+#### Unit Tests
+
+Unit tests of the helper function invoked from `workload.AdjustResources`
+will be added.
+
+- `pkg/workload`: `2024-09-07` - `2.8%`
+
+#### Integration tests
+
+Unclear what is necessary.  Please advise.
```

  Sometimes people will walk through what kind of test cases they will write.

- **keps/2937-resource-transformer/README.md:174** by [@kannon92](https://github.com/kannon92) — 2024-09-09T13:43:43Z
```diff
@@ -0,0 +1,291 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in the  `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single extended resource `kueue.x-k8s.io/accelerator-memory` and completely elide
+the MIG extended resource from the quota calculation.
+
+Concretely, the cluster admin would register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+
+A Job requesting the resources below:
```

  I am not sure that dropping the "mig" requests from the workload is a good idea.
  
  I think having the actual "scheduled" quantities on a workload could be useful and it makes it easier to understand what is being requests from the scheduler at the workload level.
  
  I understand that you make it configurable but maybe we should expand on the use of retain or remove and is it actually necessary to have this configurable?

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-09T18:27:27Z

- **keps/2937-resource-transformer/README.md:146** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-09T18:27:27Z
```diff
@@ -0,0 +1,291 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in the  `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single extended resource `kueue.x-k8s.io/accelerator-memory` and completely elide
+the MIG extended resource from the quota calculation.
+
+Concretely, the cluster admin would register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+The covered resources in a ClusterQueue would only need to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to list every MIG variant resource. 
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be `false` so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
+
+  // should the input resource be replaced (true) or retained (false)
```

  Good point.  Reworked to use an enum.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-09T18:31:43Z

- **keps/2937-resource-transformer/README.md:174** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-09T18:31:43Z
```diff
@@ -0,0 +1,291 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in the  `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single extended resource `kueue.x-k8s.io/accelerator-memory` and completely elide
+the MIG extended resource from the quota calculation.
+
+Concretely, the cluster admin would register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+
+A Job requesting the resources below:
```

  I expanded the MIG user story into two sub-parts and tried to motivate why replace might be desirable as an option (mainly to DRY up the ClusterQueue's  ResourceGroup).  I don't think the replace/retain configuration is absolutely essential, but it could be useful in some cases.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-09T18:32:14Z

- **keps/2937-resource-transformer/README.md:260** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-09T18:32:14Z
```diff
@@ -0,0 +1,291 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in the  `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single extended resource `kueue.x-k8s.io/accelerator-memory` and completely elide
+the MIG extended resource from the quota calculation.
+
+Concretely, the cluster admin would register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+The covered resources in a ClusterQueue would only need to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to list every MIG variant resource. 
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be `false` so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
+
+  // should the input resource be replaced (true) or retained (false)
+  Replace bool `json:"replace"`
+}
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the map key is the input resource name
+  Transformations map[corev1.ResourceName]ResourceTransformation `json:"transformations"`
+}
+```
+A key advantage of defining a single object is that it gives a cluster admin
+a single source of truth.  By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+### Option 2: Singleton Matrix Output Focused
+This is similar to Option 1, but makes the output resource the
+primary key.  
+```go
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the first map key is the output resource name
+  // and the inner map key is the input resource name
+  Transformations map[corev1.ResourceName]map[corev1.ResourceName]resource.Quantity `json:"transformations"`
+
+  // The map key is the input resource name. True means it should be replaced after it is transformed
+  ReplaceInput map[corev1.ResourceName]bool `json:"replaceInput"`
+}
+```
+
+Grouping by output makes it easier for the cluster admin to see/specify
+all the resources that map into a given output as a unit.  However it
+comes at the cost of a separate map to specify the replace boolean. 
+It may also be confusing that the `Transformations` map is keyed by
+output name as the primary key while `ReplaceInput` is keyed by input name.
+
+### Comparison
+
+Consider the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `true`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `false`)
+
+Option 1
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      replace: true
+      functions:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      replace: true
+      functions:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      replace: true
+      functions:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      replace: false
+      functions:
+        kueue.x-k8s.io/credits: 1
+```
+
+Option 2
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    kueue.x-k8s.io/accelerator-memory:
+      nvidia.com/mig-1g.5gb: 5G
+      nvidia.com/mig-2g.10gb: 10G
+      nvidia.com/mig-4g.20gb: 20G
+    kueue.x-k8s.io/credits:
+      nvidia.com/mig-1g.5gb: 10
+      nvidia.com/mig-2g.10gb: 15
+      nvidia.com/mig-4g.20gb: 30
+      cpu: 1
+  replaceInput:
+    nvidia.com/mig-1g.5gb: true
+    nvidia.com/mig-2g.10gb: true
+    nvidia.com/mig-4g.20gb: true
+```
+
+Option 2 is likely to be terser, but I find Option 1 slightly more intutive because
+I tend to think of functions in the "forward direction" (mapping inputs to outputs).
+Either is viable and has similar implementation complexity.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+None
+
+#### Unit Tests
+
+Unit tests of the helper function invoked from `workload.AdjustResources`
+will be added.
+
+- `pkg/workload`: `2024-09-07` - `2.8%`
+
+#### Integration tests
+
+Unclear what is necessary.  Please advise.
```

  ok.  put in an initial thought.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-10T08:33:17Z

- **keps/2937-resource-transformer/README.md:247** by [@mimowo](https://github.com/mimowo) — 2024-09-10T08:33:17Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  I find the `Functions` name confusing here. These are specifying the value of a single unit, which is then multiplied by the value of the input resource. Maybe it could be cleaner if called `UnitValues`?
  
  IIUC the name `Functions` would be appropriate if we want to support some sort of placeholder functions, such as `5G * $count`, but this is out-of-scope IIUC.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-10T08:35:37Z

Thanks for the proposal, looks cool, first pass.

- **keps/2937-resource-transformer/README.md:300** by [@mimowo](https://github.com/mimowo) — 2024-09-10T08:35:15Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the map key is the input resource name
+  Transformations map[corev1.ResourceName]ResourceTransformation `json:"transformations"`
+}
+```
+A key advantage of defining a single object is that it gives a cluster admin
+a single source of truth.  By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+### Option 2: Singleton Matrix Output Focused
+This is similar to Option 1, but makes the output resource the
+primary key.  
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the first map key is the output resource name
+  // and the inner map key is the input resource name
+  Transformations map[corev1.ResourceName]map[corev1.ResourceName]resource.Quantity `json:"transformations"`
+
+  // The map key is the input resource name.
+  Strategy map[corev1.ResourceName]ResourceTransformationStrategy `json:"strategy"`
+}
+```
+
+Grouping by output makes it easier for the cluster admin to see/specify
+all the resources that map into a given output as a unit.  However it
+comes at the cost of a separate map to specify strategy.
+It may be confusing that the `Transformations` map is keyed by
+output name as the primary key while `Strategy` is keyed by input name.
+
+### Comparison
+
+Consider the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+Option 1
```

  I'm leaning towards option 1 - yes, it is more verbose, but seems more intuitive, with less hidden assumptions.
  I think it will also be easier to evolve in the future (if ever) if we want to introduce more flexible transformation functions.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-10T13:42:31Z

- **keps/2937-resource-transformer/README.md:247** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-10T13:42:31Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  Agree `Functions` is fairly generic.  `UnitValues` could work.  `Conversions` is another possibility.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-10T19:58:57Z

- **keps/2937-resource-transformer/README.md:247** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-10T19:58:57Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  I changed to Conversions for now.  Can change again if another term is preferred.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-11T08:56:58Z

- **keps/2937-resource-transformer/README.md:247** by [@mimowo](https://github.com/mimowo) — 2024-09-11T08:56:58Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  `Conversions` sounds better than `Functions` for sure, and probably better than `UnitValues`. 
  
  Still, the field doesn't specify the conversion formula, as this is hardcoded: `$baseValue * $count`. So, maybe another possibility: `conversionBaseValues`. Longer, but I think it's more descriptive. I'm open to other proposals.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-11T15:47:28Z

- **keps/2937-resource-transformer/README.md:247** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-11T15:47:28Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  maybe `conversionFactors` Seems to fit how NIST [defines it](https://www.nist.gov/pml/owm/metric-si/unit-conversion#:~:text=A%20conversion%20factor%20is%20an,value%20expressed%20in%20different%20units.).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-11T18:00:35Z

- **keps/2937-resource-transformer/README.md:247** by [@mimowo](https://github.com/mimowo) — 2024-09-11T18:00:35Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  IIUC it is more of an expression, say `1m=100cm`. It is close enough for me to be ok with it, but I'm slightly leaning towards `conversionBaseValues`.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-11T18:16:34Z

- **keps/2937-resource-transformer/README.md:247** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-11T18:16:34Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  ok.  I'll go with `conversionBaseValues`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-12T12:37:00Z

- **keps/2937-resource-transformer/README.md:247** by [@mimowo](https://github.com/mimowo) — 2024-09-12T12:37:00Z
```diff
@@ -0,0 +1,403 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  Functions map[corev1.ResourceName ]resource.Quantity `json:"functions"`
```

  sgtm for the lack of a clearly better option for now

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-09-17T17:43:23Z

- **keps/2937-resource-transformer/README.md:174** by [@kannon92](https://github.com/kannon92) — 2024-09-17T17:43:23Z
```diff
@@ -0,0 +1,291 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in the  `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single extended resource `kueue.x-k8s.io/accelerator-memory` and completely elide
+the MIG extended resource from the quota calculation.
+
+Concretely, the cluster admin would register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `true`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `true`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `true`)
+
+A Job requesting the resources below:
```

  Looks great.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-18T06:48:06Z

- **keps/2937-resource-transformer/README.md:253** by [@mimowo](https://github.com/mimowo) — 2024-09-18T06:47:58Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
```

  Questions:
  - Have you considered exposing the results of the transformations at the API level (for debuggability)?
  - Will the used output resources be accounted for in the statuses of Workload (admission) and ClusterQueue?

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2024-09-18T20:44:07Z

- **keps/2937-resource-transformer/README.md:345** by [@KPostOffice](https://github.com/KPostOffice) — 2024-09-18T20:25:15Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the map key is the input resource name
+  Transformations map[corev1.ResourceName]ResourceTransformation `json:"transformations"`
+}
+```
+A key advantage of defining a single object is that it gives a cluster admin
+a single source of truth.  By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+### Option 2: Singleton Matrix Output Focused
+This is similar to Option 1, but makes the output resource the
+primary key.  
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the first map key is the output resource name
+  // and the inner map key is the input resource name
+  Transformations map[corev1.ResourceName]map[corev1.ResourceName]resource.Quantity `json:"transformations"`
+
+  // The map key is the input resource name.
+  Strategy map[corev1.ResourceName]ResourceTransformationStrategy `json:"strategy"`
+}
+```
+
+Grouping by output makes it easier for the cluster admin to see/specify
+all the resources that map into a given output as a unit.  However it
+comes at the cost of a separate map to specify strategy.
+It may be confusing that the `Transformations` map is keyed by
+output name as the primary key while `Strategy` is keyed by input name.
+
+### Comparison
+
+Consider the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+Option 1
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+Option 2
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    kueue.x-k8s.io/accelerator-memory:
+      nvidia.com/mig-1g.5gb: 5G
+      nvidia.com/mig-2g.10gb: 10G
+      nvidia.com/mig-4g.20gb: 20G
+    kueue.x-k8s.io/credits:
+      nvidia.com/mig-1g.5gb: 10
+      nvidia.com/mig-2g.10gb: 15
+      nvidia.com/mig-4g.20gb: 30
+      cpu: 1
+  strategy:
+    nvidia.com/mig-1g.5gb: Replace
+    nvidia.com/mig-2g.10gb: Replace
+    nvidia.com/mig-4g.20gb: Replace
+    cpu: Retain
+```
+
+Option 2 is likely to be terser, but I find Option 1 slightly more intutive because
```

  +1 to option 1

- **keps/2937-resource-transformer/README.md:550** by [@KPostOffice](https://github.com/KPostOffice) — 2024-09-18T20:44:02Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the map key is the input resource name
+  Transformations map[corev1.ResourceName]ResourceTransformation `json:"transformations"`
+}
+```
+A key advantage of defining a single object is that it gives a cluster admin
+a single source of truth.  By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+### Option 2: Singleton Matrix Output Focused
+This is similar to Option 1, but makes the output resource the
+primary key.  
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the first map key is the output resource name
+  // and the inner map key is the input resource name
+  Transformations map[corev1.ResourceName]map[corev1.ResourceName]resource.Quantity `json:"transformations"`
+
+  // The map key is the input resource name.
+  Strategy map[corev1.ResourceName]ResourceTransformationStrategy `json:"strategy"`
+}
+```
+
+Grouping by output makes it easier for the cluster admin to see/specify
+all the resources that map into a given output as a unit.  However it
+comes at the cost of a separate map to specify strategy.
+It may be confusing that the `Transformations` map is keyed by
+output name as the primary key while `Strategy` is keyed by input name.
+
+### Comparison
+
+Consider the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+Option 1
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+Option 2
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    kueue.x-k8s.io/accelerator-memory:
+      nvidia.com/mig-1g.5gb: 5G
+      nvidia.com/mig-2g.10gb: 10G
+      nvidia.com/mig-4g.20gb: 20G
+    kueue.x-k8s.io/credits:
+      nvidia.com/mig-1g.5gb: 10
+      nvidia.com/mig-2g.10gb: 15
+      nvidia.com/mig-4g.20gb: 30
+      cpu: 1
+  strategy:
+    nvidia.com/mig-1g.5gb: Replace
+    nvidia.com/mig-2g.10gb: Replace
+    nvidia.com/mig-4g.20gb: Replace
+    cpu: Retain
+```
+
+Option 2 is likely to be terser, but I find Option 1 slightly more intutive because
+I tend to think of functions in the "forward direction" (mapping inputs to outputs).
+Either is viable and has similar implementation complexity.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+None
+
+#### Unit Tests
+
+Unit tests of the helper function invoked from `workload.AdjustResources` will be added.
+
+Unit tests of loading the `ResourceTransformationMatrix` from a `Configuration` will be added.
+
+#### Integration tests
```

  How will Kueue handle an update to the transformation matrix followed by a controller restart? What will happen to existing Workloads?

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-19T01:37:45Z

- **keps/2937-resource-transformer/README.md:253** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-19T01:37:45Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
```

  Exposing the details of the transformation for debugging is an interesting idea.  I had not thought of that. One possibility would be that if workload's resources are actually transformed then we could emit an event that contained the workload, and the input and output resources.  
  
  I think for the second question the answer is that the output resources are treated no differently than any other (non-transformed) resource that is present in the Workload's Spec.PodSet[].  The rest of Kueue is not aware of whether a resource is in a PodSet because it was part of the "raw" PodSet that came from the result of GenericJob.PodSet() when the Workload was being constructed or because it was added by AdjustResources.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-19T01:50:41Z

- **keps/2937-resource-transformer/README.md:550** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-19T01:50:41Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the map key is the input resource name
+  Transformations map[corev1.ResourceName]ResourceTransformation `json:"transformations"`
+}
+```
+A key advantage of defining a single object is that it gives a cluster admin
+a single source of truth.  By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+### Option 2: Singleton Matrix Output Focused
+This is similar to Option 1, but makes the output resource the
+primary key.  
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the first map key is the output resource name
+  // and the inner map key is the input resource name
+  Transformations map[corev1.ResourceName]map[corev1.ResourceName]resource.Quantity `json:"transformations"`
+
+  // The map key is the input resource name.
+  Strategy map[corev1.ResourceName]ResourceTransformationStrategy `json:"strategy"`
+}
+```
+
+Grouping by output makes it easier for the cluster admin to see/specify
+all the resources that map into a given output as a unit.  However it
+comes at the cost of a separate map to specify strategy.
+It may be confusing that the `Transformations` map is keyed by
+output name as the primary key while `Strategy` is keyed by input name.
+
+### Comparison
+
+Consider the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+Option 1
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+Option 2
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    kueue.x-k8s.io/accelerator-memory:
+      nvidia.com/mig-1g.5gb: 5G
+      nvidia.com/mig-2g.10gb: 10G
+      nvidia.com/mig-4g.20gb: 20G
+    kueue.x-k8s.io/credits:
+      nvidia.com/mig-1g.5gb: 10
+      nvidia.com/mig-2g.10gb: 15
+      nvidia.com/mig-4g.20gb: 30
+      cpu: 1
+  strategy:
+    nvidia.com/mig-1g.5gb: Replace
+    nvidia.com/mig-2g.10gb: Replace
+    nvidia.com/mig-4g.20gb: Replace
+    cpu: Retain
+```
+
+Option 2 is likely to be terser, but I find Option 1 slightly more intutive because
+I tend to think of functions in the "forward direction" (mapping inputs to outputs).
+Either is viable and has similar implementation complexity.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+None
+
+#### Unit Tests
+
+Unit tests of the helper function invoked from `workload.AdjustResources` will be added.
+
+Unit tests of loading the `ResourceTransformationMatrix` from a `Configuration` will be added.
+
+#### Integration tests
```

  Existing workloads would be subject to the new matrix when `AdjustResources` is invoked on them (or more precisely a deepCopy of them).  This would mainly happen during the processing of an Update event (workload controller) or in the corner case of an existing workload whose local queue was created after it (queue.AddLocalQueue).
  
  This looks plausible to me, but I do not claim to fully understand this part of the system and may be missing something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T06:55:57Z

- **keps/2937-resource-transformer/README.md:253** by [@mimowo](https://github.com/mimowo) — 2024-09-19T06:55:57Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
```

  > Exposing the details of the transformation for debugging is an interesting idea.
  
  The more info we can expose without compromising the performance the better. If some requires significant work we could add points the the graduation criteria for second phase of Beta, or GA.
  
  > if workload's resources are actually transformed then we could emit an event that contained the workload, and the input and output resources. 
  
  That is a possibility, feel free to add, but I was thinking about exposing the information about the "effective" quotas after transformation. For example, from the story 1:
  
  ```yaml
    - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
      flavors:
      - name: default-flavor
        resources:
        - name: "cpu"
          nominalQuota: ...
        - name: "memory"
          nominalQuota: ...
        - name: kueue.x-k8s.io/accelerator-memory
          nominalQuota: 320G
  ```
  is transformed into 
  ```yaml
    - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
      flavors:
      - name: default-flavor
        resources:
        - name: "cpu"
          nominalQuota: ...
        - name: "memory"
          nominalQuota: ...
        - name: kueue.x-k8s.io/accelerator-memory
          nominalQuota: 200G
        - name: nvidia.com/mig-1g.5gb
          nominalQuota: 40  # 200G/5g
        - name: nvidia.com/mig-2g.10gb
          nominalQuota: 20  # 200G/10g
        - name: nvidia.com/mig-4g.20gb
          nominalQuota: 10  # 200G/20g
        - name: nvidia.com/mig-7g.40gb
          nominalQuota: 5  # 200G/40g
  ```
  But the lower yaml only is represented in memory, IIUC? So with this KEP the "spec" quotas are not actually the "effective" quotas. I would like to consider exposing the "effective" quotas. ClusterQueue status seems like a natural place. The transformations are not just additive so we would need to expose the whole effective structure. I'm wondering if we should do this unconditionally then.
  
  > I think for the second question the answer is that the output resources are treated no differently than any other (non-transformed) resource that is present in the Workload's Spec.PodSet[]. The rest of Kueue is not aware of whether a resource is in a PodSet because it was part of the "raw" PodSet that came from the result of GenericJob.PodSet() when the Workload was being constructed or because it was added by AdjustResources.
  
  I see, so in case there is a workload which consumes "nvidia.com/mig-1g.5gb" it will also consume 5 of `kueue.x-k8s.io/accelerator-memory`. And, we will see both resources also as "used" in the ClusterQueue status under "FlavorUsage](https://github.com/kubernetes-sigs/kueue/blob/baba452433312a0195bd9972c95a1c097d8de269/apis/kueue/v1beta1/clusterqueue_types.go#L320-L330)? Is my understanding correct?

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-19T13:59:38Z

- **keps/2937-resource-transformer/README.md:253** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-19T13:59:37Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
```

  Thanks for the detailed comment.   I can see where I need to clarify the KEP.  The transformations are only applied to the Workload, not to the ClusterQueue.
  
  I'm going to give a more detailed response in the KEP by expanding the MIG user stories with a concrete example of what a Workload and a ClusterQueue with that workload admitted would like under two different transformation configurations.  Will tag you when when I push updated version.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T14:03:12Z

- **keps/2937-resource-transformer/README.md:253** by [@mimowo](https://github.com/mimowo) — 2024-09-19T14:03:12Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
```

  awesome, thanks

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-19T16:03:14Z

- **keps/2937-resource-transformer/README.md:345** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-19T16:03:14Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the map key is the input resource name
+  Transformations map[corev1.ResourceName]ResourceTransformation `json:"transformations"`
+}
+```
+A key advantage of defining a single object is that it gives a cluster admin
+a single source of truth.  By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+### Option 2: Singleton Matrix Output Focused
+This is similar to Option 1, but makes the output resource the
+primary key.  
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformationMatrix struct {
+  // The transformations to apply where the first map key is the output resource name
+  // and the inner map key is the input resource name
+  Transformations map[corev1.ResourceName]map[corev1.ResourceName]resource.Quantity `json:"transformations"`
+
+  // The map key is the input resource name.
+  Strategy map[corev1.ResourceName]ResourceTransformationStrategy `json:"strategy"`
+}
+```
+
+Grouping by output makes it easier for the cluster admin to see/specify
+all the resources that map into a given output as a unit.  However it
+comes at the cost of a separate map to specify strategy.
+It may be confusing that the `Transformations` map is keyed by
+output name as the primary key while `Strategy` is keyed by input name.
+
+### Comparison
+
+Consider the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+Option 1
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+Option 2
+```yaml
+resourceTransformationMatrix:
+  transformations:
+    kueue.x-k8s.io/accelerator-memory:
+      nvidia.com/mig-1g.5gb: 5G
+      nvidia.com/mig-2g.10gb: 10G
+      nvidia.com/mig-4g.20gb: 20G
+    kueue.x-k8s.io/credits:
+      nvidia.com/mig-1g.5gb: 10
+      nvidia.com/mig-2g.10gb: 15
+      nvidia.com/mig-4g.20gb: 30
+      cpu: 1
+  strategy:
+    nvidia.com/mig-1g.5gb: Replace
+    nvidia.com/mig-2g.10gb: Replace
+    nvidia.com/mig-4g.20gb: Replace
+    cpu: Retain
+```
+
+Option 2 is likely to be terser, but I find Option 1 slightly more intutive because
```

  Since everyone who commented liked option 1 for specifying the configuration better than option 2, I'm going to simplify the KEP by just proposing option 1.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-19T21:17:17Z

- **keps/2937-resource-transformer/README.md:253** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-19T21:17:17Z
```diff
@@ -0,0 +1,405 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Option 1: Singleton Matrix Input Focused](#option-1-singleton-matrix-input-focused)
+  - [Option 2: Singleton Matrix Output Focused](#option-2-singleton-matrix-output-focused)
+  - [Comparison](#comparison)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend the `workload.AdjustResources` function so that it applies this
+matrix when adjusting the resource request for its input Workload object.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 320G
+```
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload requesting:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example resource group that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: ...
+      - name: "memory"
+        nominalQuota: ...
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+The only non-trivial design question is how best to specify the configuration.
+
+### Option 1: Singleton Matrix Input Focused
+The entire transformation matrix is defined in one object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName ]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+type ResourceTransformationMatrix struct {
```

  Pushed a major revision.  I'm going to be offline Friday-Sunday so apologies in advance for being slow in responding to any additional comments you might have.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-09-20T02:55:55Z

- **keps/2937-resource-transformer/README.md:35** by [@kannon92](https://github.com/kannon92) — 2024-09-20T02:55:55Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
```

  ```suggestion
  the actual resource requests/limits that the Pods created by the Workload 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T18:43:33Z

- **keps/2937-resource-transformer/kep.yaml:10** by [@mimowo](https://github.com/mimowo) — 2024-09-20T15:46:27Z
```diff
@@ -0,0 +1,26 @@
+title: Configurable Resource Transformers
+kep-number: 2937
+authors:
+  - "@dgrove-oss"
+status: implementable
+creation-date: 2024-09-06
+reviewers:
+  - TBD
+approvers:
+  - TBD
```

  Please add me here and as a reviewer & approver.

- **keps/2937-resource-transformer/README.md:319** by [@mimowo](https://github.com/mimowo) — 2024-09-20T18:24:11Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  Even with option (3)  we may have redundancy to repeat the transformations across resource flavors (which may be representing spot vs on-demand vs reservation machines).  I would like to avoid the repeated transformations, as they will be hard to maintain by admins to edit consistently in multiple places.
  
  Maybe another option is to use top-level API, like ResourceTransformationMatrix API. Then we could reference the objects from the RF or CQ instances. If done properly it would also not require Kueue restart to update.
  
  OTOH, I want to keep it simple and grounded to the use cases, and the global configuration seems to majority of them. Even if we choose global to implement the global rules we may still add API support later. If `resourcetransformationMatrixName` is supplied, then use the indicated API object, otherwise fallback to the global.
  
  WDYT @mwielgus @dgrove-oss ?

- **keps/2937-resource-transformer/README.md:526** by [@mimowo](https://github.com/mimowo) — 2024-09-20T18:41:01Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
+
+Options 1 and 2 are simplest to implement.  I have implemented 1 in a prototype already.  I'm confident 2 could be done almost
+identically (there is just a different logic to compute the `InfoOptions` for the ClusterQueue).
+
+Option 3 is interesting because it allows more flexibility than Option 1, but should avoid many redundant
+specifications of the same transformation matrix (the big drawback of Option 2).  There may be some additional
+implementation complexity because instead of applying the transformation in `workload.NewInfo()` we would
+instead need to do it during flavor assignment.  It seems like doing it in `findFlavorForPodSetResource`
+would work, but making the results observable in the Workload Status may be significantly harder, especially
+with fungible flavors that may define different transformation functions for the same input resource.
+
+### Observabilty
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+    nvidia.com/mig-7g.40gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        kueue.x-k8s.io/accelerator-memory:  default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        kueue.x-k8s.io/accelerator-memory:  20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      kueue.x-k8s.io/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             kueue.x-k8s.io/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
```

  Yes, some form of a field like this will be important, but if we have multiple PodSets we may need to keep the structure per PodSet.
  
  Alternatively, we can wait with adding the field, but start with "Alpha" phase, and adding some form of the field before Beta. We may actually prefer to go via Alpha regardless, WDYT?

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-23T15:53:01Z

- **keps/2937-resource-transformer/README.md:319** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-23T15:53:01Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  It seems reasonable to me to start with option 1 as the simplest and then extend later with API support if needed.  I agree that adding  a reference `resourcetransformationMatrixName` as you suggested is the best option for avoiding duplication.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-23T15:54:45Z

- **keps/2937-resource-transformer/README.md:35** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-23T15:54:45Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
```

  changed.  Thanks!

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-23T15:54:53Z

- **keps/2937-resource-transformer/kep.yaml:10** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-23T15:54:53Z
```diff
@@ -0,0 +1,26 @@
+title: Configurable Resource Transformers
+kep-number: 2937
+authors:
+  - "@dgrove-oss"
+status: implementable
+creation-date: 2024-09-06
+reviewers:
+  - TBD
+approvers:
+  - TBD
```

  done.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-23T15:57:16Z

- **keps/2937-resource-transformer/README.md:526** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-23T15:57:16Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
+
+Options 1 and 2 are simplest to implement.  I have implemented 1 in a prototype already.  I'm confident 2 could be done almost
+identically (there is just a different logic to compute the `InfoOptions` for the ClusterQueue).
+
+Option 3 is interesting because it allows more flexibility than Option 1, but should avoid many redundant
+specifications of the same transformation matrix (the big drawback of Option 2).  There may be some additional
+implementation complexity because instead of applying the transformation in `workload.NewInfo()` we would
+instead need to do it during flavor assignment.  It seems like doing it in `findFlavorForPodSetResource`
+would work, but making the results observable in the Workload Status may be significantly harder, especially
+with fungible flavors that may define different transformation functions for the same input resource.
+
+### Observabilty
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+    nvidia.com/mig-7g.40gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        kueue.x-k8s.io/accelerator-memory:  default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        kueue.x-k8s.io/accelerator-memory:  20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      kueue.x-k8s.io/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             kueue.x-k8s.io/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
```

  I have no strong opinion about going Alpha vs. Beta as the first target.  I guess if Alpha, then I should add a feature gate to the current prototype implementation PR?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-23T16:18:01Z

- **keps/2937-resource-transformer/README.md:319** by [@mimowo](https://github.com/mimowo) — 2024-09-23T16:18:01Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  I see, so I think we have two competitors:
  I. global now, dedicated ResourceTransformationMatrix API if needed (as currently in KEP)
  II. dedicated ResourceTransformationMatrix API from start with two variants - referenced from (a) CQ or  (b) RF
  
  I guess doing (II)  is slightly more code, but the pros are: 
  
  1. it allows us to go via Alpha API and so easily evolve the API
  2. natural way of supporting multiple transformations  - no need to create 2 APIs (global and API) to do the same thing
  
  It can be helpful to know how likely you see a need for multiple transformations in the future. @dgrove-oss @tardieu  do you have any insights here?
  
  I'm now slightly leaning towards IIb (API object attached to RF). Any opinion @alculquicondor @mwielgus ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-23T16:19:44Z

- **keps/2937-resource-transformer/README.md:526** by [@mimowo](https://github.com/mimowo) — 2024-09-23T16:19:44Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
+
+Options 1 and 2 are simplest to implement.  I have implemented 1 in a prototype already.  I'm confident 2 could be done almost
+identically (there is just a different logic to compute the `InfoOptions` for the ClusterQueue).
+
+Option 3 is interesting because it allows more flexibility than Option 1, but should avoid many redundant
+specifications of the same transformation matrix (the big drawback of Option 2).  There may be some additional
+implementation complexity because instead of applying the transformation in `workload.NewInfo()` we would
+instead need to do it during flavor assignment.  It seems like doing it in `findFlavorForPodSetResource`
+would work, but making the results observable in the Workload Status may be significantly harder, especially
+with fungible flavors that may define different transformation functions for the same input resource.
+
+### Observabilty
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+    nvidia.com/mig-7g.40gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        kueue.x-k8s.io/accelerator-memory:  default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        kueue.x-k8s.io/accelerator-memory:  20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      kueue.x-k8s.io/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             kueue.x-k8s.io/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
```

  Going via Alpha would be nice, so that we can easily adjust the API before Beta. Please add a feature gate, maybe `ConfigurableResourceTransformations` per KEP title.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-23T20:41:31Z

- **keps/2937-resource-transformer/README.md:319** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-23T20:41:31Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  A single global matrix works well for the first set of user stories (MIG devices).  We are fairly confident we understand this use case and that it will be useful in practice. 
  
  The second user story ("pseudo-currency" quotas) seems like it would work better if we could have different transformation matrixes for different resource flavors.  This user story is a bit more speculative, but it seems plausible and generated some interest when we discussed it in the batch wg call last month.
  
  So I would also lean to IIb as the end goal.  I don't think the additional implementation complexity will be too bad, with the possible exception of the combination of flavor fungibility and per-flavor transformation matrices making it more complex to explain exactly why a ClusterQueue didn't have sufficient resources to admit a workload. 
  
  We would need to add `resourcetransformationMatrixName` to ResourceFlavor.  Are there any issues in adding a field that is intended to hold the name of an alpha API instance to ResourceFlavor?

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-23T20:54:10Z

- **keps/2937-resource-transformer/README.md:526** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-23T20:54:10Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
+
+Options 1 and 2 are simplest to implement.  I have implemented 1 in a prototype already.  I'm confident 2 could be done almost
+identically (there is just a different logic to compute the `InfoOptions` for the ClusterQueue).
+
+Option 3 is interesting because it allows more flexibility than Option 1, but should avoid many redundant
+specifications of the same transformation matrix (the big drawback of Option 2).  There may be some additional
+implementation complexity because instead of applying the transformation in `workload.NewInfo()` we would
+instead need to do it during flavor assignment.  It seems like doing it in `findFlavorForPodSetResource`
+would work, but making the results observable in the Workload Status may be significantly harder, especially
+with fungible flavors that may define different transformation functions for the same input resource.
+
+### Observabilty
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+    nvidia.com/mig-7g.40gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        kueue.x-k8s.io/accelerator-memory:  default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        kueue.x-k8s.io/accelerator-memory:  20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      kueue.x-k8s.io/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             kueue.x-k8s.io/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
```

  Added feature gate to KEP.yaml

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-24T07:43:36Z

- **keps/2937-resource-transformer/README.md:319** by [@mimowo](https://github.com/mimowo) — 2024-09-24T07:43:36Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  > A single global matrix works well for the first set of user stories (MIG devices). We are fairly confident we understand this use case and that it will be useful in practice.
  
  +1
  
  > The second user story ("pseudo-currency" quotas) seems like it would work better if we could have different transformation matrixes for different resource flavors. This user story is a bit more speculative, but it seems plausible and generated some interest when we discussed it in the batch wg call last month.
  
  Yes, it is speculative, but seems likely to happen as we move forward. 
  
  >  I don't think the additional implementation complexity will be too bad, with the possible exception of the combination of flavor fungibility and per-flavor transformation matrices making it more complex to explain exactly why a ClusterQueue didn't have sufficient resources to admit a workload.
  
  I know you have a working prototype using the global config, but it would be good to try the APIs and weigh the complexity added. Long term I think it is the cleanest approach.
  
  > We would need to add resourcetransformationMatrixName to ResourceFlavor. Are there any issues in adding a field that is intended to hold the name of an alpha API instance to ResourceFlavor?
  
  This is not an issue I think. We reference by name Alpha API objects from Beta, for example Cohort API is alpha and addressed from CQ. We also planned a similar approach for [Topology Aware Scheduling](https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-schedling#admin-facing-api) - see the TopologyName field referencing the Alpha Topology object.
  
  I'm open to also consider alternative names for the API object, and the corresponding field, like: 
  1. `resourcetransformationMatrixName` (and `ResourcetransformationMatrix` object) (as currently discussed)
  2. `resourceMapperName` (and `ResourceMapper` object)
  3. `resourceConfigName` (and `ResourceConfig` object) (if we want other configurations to also keep there)

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-24T13:55:08Z

- **keps/2937-resource-transformer/README.md:319** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-24T13:55:08Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  I like `resourceMapperName` and `ResourceMapper`.  It's shorter and also avoids thinking about whether the plural of matrix should be matrices or matrixes :).
  
  In thinking about this some more, I think we might actually want `resourceMapperNames` on ResourceFlavor to be an array to allow maximal sharing of configuration.  Since ResourceFlavor currently doesn't have a Status, we'd defer the combining of the resourceMapperNames to the ClusterQueues that have ResourceGroups that use a ResourceFlavor with a non-empty resourceMapperNames. We could record the combined mapping in the ClusterQueue's status and if there were clashes when combining mappings we could make the ClusterQueue inactive until they were resolved and report the problem in the ClusterQueue's status. 
  
  I'll update the KEP to propose IIb as the preferred alternative with a ResourceMapper API object and move the other configuration variants we discussed to the alternatives section.  Then go ahead and update the prototype to see what it would look like.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-24T14:22:19Z

- **keps/2937-resource-transformer/README.md:319** by [@mimowo](https://github.com/mimowo) — 2024-09-24T14:22:19Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  > I like resourceMapperName and ResourceMapper. It's shorter and also avoids thinking about whether the plural of matrix should be matrices or matrixes :).
  
  +1 
  
  > I think we might actually want resourceMapperNames on ResourceFlavor to be an array to allow maximal sharing of configuration.
  
  This sounds reasonable, but we need to balance the complication with usefulness, so some motivation use-case for that would be useful.
  
  > Since ResourceFlavor currently doesn't have a Status
  
  Would the story be different if it had status? Maybe we could consider adding it, but not sure about the process here since we usually add fields by pointers.
  
  > I'll update the KEP to propose IIb as the preferred alternative with a ResourceMapper API object and move the other configuration variants we discussed to the alternatives section. Then go ahead and update the prototype to see what it would look like.
  
  +1

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-24T17:09:24Z

- **keps/2937-resource-transformer/README.md:92** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T15:56:51Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
```

  ```suggestion
  these into a single primary extended resource `example.com/accelerator-memory`.
  ```
  
  If it's configured by the user, it shouldn't be in the `kueue.x-k8s.io` domain.

- **keps/2937-resource-transformer/README.md:102** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T15:58:25Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
```

  ```suggestion
  * (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5Gi`, `replace`)
  ```
  ?
  `G` is not a unit in kubernetes resources, IIRC.

- **keps/2937-resource-transformer/README.md:223** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T16:56:49Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
```

  ```suggestion
  define quotas in terms of a fictional currency `example.com/credits`
  ```

- **keps/2937-resource-transformer/README.md:246** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T16:59:27Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
```

  The location of the API needs to be decided before we proceed to implementation.
  What is your suggestion and why?

- **keps/2937-resource-transformer/README.md:269** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T17:01:11Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
```

  Follow API conventions regarding the use of lists over maps https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#lists-of-named-subobjects-preferred-over-maps

- **keps/2937-resource-transformer/README.md:273** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T17:02:00Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
```

  This can be achieved by using `listType=map`

- **keps/2937-resource-transformer/README.md:319** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T17:05:09Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  I like the idea of starting with I and leaving II.b for a possible follow up based on user feedback.

- **keps/2937-resource-transformer/README.md:262** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T17:06:47Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
```

  ```suggestion
    baseValues map[corev1.ResourceName]resource.Quantity `json:"baseValues"`
  ```
  
  Because the field is already inside "transformations", I think there is enough context to understand what the field does.

- **keps/2937-resource-transformer/README.md:495** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T17:08:01Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
+
+Options 1 and 2 are simplest to implement.  I have implemented 1 in a prototype already.  I'm confident 2 could be done almost
+identically (there is just a different logic to compute the `InfoOptions` for the ClusterQueue).
+
+Option 3 is interesting because it allows more flexibility than Option 1, but should avoid many redundant
+specifications of the same transformation matrix (the big drawback of Option 2).  There may be some additional
+implementation complexity because instead of applying the transformation in `workload.NewInfo()` we would
+instead need to do it during flavor assignment.  It seems like doing it in `findFlavorForPodSetResource`
+would work, but making the results observable in the Workload Status may be significantly harder, especially
+with fungible flavors that may define different transformation functions for the same input resource.
+
+### Observabilty
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+    nvidia.com/mig-7g.40gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        kueue.x-k8s.io/accelerator-memory:  default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        kueue.x-k8s.io/accelerator-memory:  20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      kueue.x-k8s.io/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             kueue.x-k8s.io/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
```

  +1

- **keps/2937-resource-transformer/README.md:526** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T17:08:46Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
+
+Options 1 and 2 are simplest to implement.  I have implemented 1 in a prototype already.  I'm confident 2 could be done almost
+identically (there is just a different logic to compute the `InfoOptions` for the ClusterQueue).
+
+Option 3 is interesting because it allows more flexibility than Option 1, but should avoid many redundant
+specifications of the same transformation matrix (the big drawback of Option 2).  There may be some additional
+implementation complexity because instead of applying the transformation in `workload.NewInfo()` we would
+instead need to do it during flavor assignment.  It seems like doing it in `findFlavorForPodSetResource`
+would work, but making the results observable in the Workload Status may be significantly harder, especially
+with fungible flavors that may define different transformation functions for the same input resource.
+
+### Observabilty
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+    nvidia.com/mig-7g.40gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        kueue.x-k8s.io/accelerator-memory:  default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        kueue.x-k8s.io/accelerator-memory:  20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      kueue.x-k8s.io/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             kueue.x-k8s.io/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
```

  This should be per podset, as they can go to different flavors.

- **keps/2937-resource-transformer/README.md:576** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-24T17:09:21Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
+
+Options 1 and 2 are simplest to implement.  I have implemented 1 in a prototype already.  I'm confident 2 could be done almost
+identically (there is just a different logic to compute the `InfoOptions` for the ClusterQueue).
+
+Option 3 is interesting because it allows more flexibility than Option 1, but should avoid many redundant
+specifications of the same transformation matrix (the big drawback of Option 2).  There may be some additional
+implementation complexity because instead of applying the transformation in `workload.NewInfo()` we would
+instead need to do it during flavor assignment.  It seems like doing it in `findFlavorForPodSetResource`
+would work, but making the results observable in the Workload Status may be significantly harder, especially
+with fungible flavors that may define different transformation functions for the same input resource.
+
+### Observabilty
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+    nvidia.com/mig-7g.40gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        kueue.x-k8s.io/accelerator-memory:  default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        kueue.x-k8s.io/accelerator-memory:  20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      kueue.x-k8s.io/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             kueue.x-k8s.io/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for kueue.x-k8s.io/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
+    Cpu:                                1
+    kueue.x-k8s.io/accelerator-memory:  20G
+    Memory:                             97656250Ki
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+None
+
+#### Unit Tests
+
+Unit tests of the helper function invoked from `workload.NewInfo` will be added.
+
+Unit tests of loading the `ResourceTransformationMatrix` from a `Configuration` will be added.
+
+#### Integration tests
+
+Because resource transformations are configured during manager startup, we need to run
+an entire integration test suite with a non-empty resource transformation if we want to
+do any integration testing with resource transformations. I'd propose to extend
+`test/integration/controller/core` by adding a resource transformation to the manager that
+maps from a synthetic extended resource to both `cpu` and `credits`.  We'd then add a
+`test/integration/controller/core/resource_transform_test.go` that would define a cluster
+queue with a resource group that covers `cpu` and `credit` and run a few quota and queuing
+tests against it.
+
+### Graduation Criteria
+
+## Implementation History
+
+## Drawbacks
+
+Adds an additional dimension of configuration that needs to be documented
+and maintained.
+
+## Alternatives
```

  Move whatever is discarded here, with subtitles to distinguish the different decision points.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-24T22:47:19Z

- **keps/2937-resource-transformer/README.md:212** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:06:41Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
```

  What happens if we specify the borrowingLimit like this?
  
  ```yaml
        - name: nvidia.com/mig-7g.40gb
          nominalQuota: 0
          borrowingLimit: 10
  ```
  
  Is this borrowingLimit still effective the same way it is today?

- **keps/2937-resource-transformer/README.md:217** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:15:27Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
```

  This use case might bring us weird metrics and usage status since even though they use only 5 GPUs, the metrics and usage could say using 10 GPUs. 
  In large batch clusters, batch admins sometimes expose the cluster capacity and usage using Grafana or other monitoring tools to batch users. In that case, the batch users might be confusing the usage.
  
  So, we may want to allow admins to modify the configurable metrics and usage calculation in the future.
  But, in this PR, we can take the feature separately and postpone it to the future based on user feedback.

- **keps/2937-resource-transformer/README.md:246** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:19:07Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
```

  My recommendation is to start from Configuration API to be able to configure cluster scoped.
  Given we found the reasonable use cases and actual user feedback came to Kueue, we can consider adding a dedicated API to configure the resource Transformation more granular.
  
  WDYT?

- **keps/2937-resource-transformer/README.md:262** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:24:50Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
```

  ```suggestion
    ConversionBaseValues corev1.ResourceList `json:"conversionBaseValues"`
  ```
  
  Is there any motivation to select the typed "map[corev1.ResourceName]resource.Quantity" instead of the "corev1.ResourceList"?

- **keps/2937-resource-transformer/README.md:262** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:25:06Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
```

  +1

- **keps/2937-resource-transformer/README.md:265** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:26:04Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
```

  What is the default strategy?

- **keps/2937-resource-transformer/README.md:269** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:29:02Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
```

  The primary concern for the typed map is the typed map is dropped from CRD scheme.
  So, as Aldo mentioned above, let's use the slice. The dropping scheme will bring us some limitations to using the K8s functionality like the CEL computation budget.

- **keps/2937-resource-transformer/README.md:319** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:34:27Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  That sounds good to me.
  Actually, I mentioned a similar approach to the above.

- **keps/2937-resource-transformer/README.md:296** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:44:48Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        example.com/accelerator-memory: 5G
+        example.com/credits: 10
```

  What happens if both pre-transformed and post-transformed resource names are specified in the ClusterQueue in the following?
  Will the kueue just treat the "nvidia.com/mig-1g.5gb" as the same as today? Or will we introduce any validation rules?
  
  ```yaml
  # ClusterQueue
    - coveredResources: ["nvidia.com/mig-1g.5gb", "example.com/accelerator-memory"]
      flavors:
      - name: default-flavor
        resources:
        - name: "nvidia.com/mig-1g.5gb"
          nominalQuota: 10
        - name: example.com/accelerator-memory
          nominalQuota: 80G
  ---
  # Configuration
  resources:
    transformations:
      nvidia.com/mig-1g.5gb:
        strategy: Replace
        conversionBaseValues:
          example.com/accelerator-memory: 5G
  ```

- **keps/2937-resource-transformer/README.md:290** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-24T22:47:04Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
```

  What if the transformation resource name prefixs match this `excludedResourcePrefixes`?
  The transformation rules are just ignored? Or will we introduce any validation rules?

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T02:46:41Z

- **keps/2937-resource-transformer/README.md:319** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T02:46:41Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  I've rewritten to propose a global configuration (I) and moved other options to alternatives.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T02:48:38Z

- **keps/2937-resource-transformer/README.md:212** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T02:48:38Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
```

  yes.  no change to borrowingLimit or lendingLimit.  I added a `borrowingLimit: 0`  to the example to clarify.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T02:49:41Z

- **keps/2937-resource-transformer/README.md:262** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T02:49:40Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
```

  simplified as suggested.  Was the result of several steps of evolution.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T02:50:13Z

- **keps/2937-resource-transformer/README.md:265** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T02:50:13Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
```

  proposing `Retain`.  Now specified with annotation.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T02:52:06Z

- **keps/2937-resource-transformer/README.md:290** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T02:52:06Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
```

  As implemented, `excludedResourcePrefixes` is applied first.  We probably should add a validation rule to flag this however.   Leaving this unresolved for now to remind me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T07:21:18Z

- **keps/2937-resource-transformer/README.md:319** by [@mimowo](https://github.com/mimowo) — 2024-09-25T07:21:18Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Job 
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/credits`, `30`, `replace`)
+* (`cpu`, `kueue.x-k8s.io/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 5G
+        kueue.x-k8s.io/credits: 10
+    nvidia.com/mig-2g.10gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 10G
+        kueue.x-k8s.io/credits: 15
+    nvidia.com/mig-4g.20gb:
+      strategy: Replace
+      conversionBaseValues:
+        kueue.x-k8s.io/accelerator-memory: 20G
+        kueue.x-k8s.io/credits: 30
+    cpu:
+      strategy: Retain
+      conversionBaseValues:
+        kueue.x-k8s.io/credits: 1
+```
+
+### Granularity Options
+
+There are three natural options.
+
+1. Following how `excludedResourcePrefixes` works, define a single transformation matrix at the operator level.
+2. Allow per-ClusterQueue configuration by making the transformation matrix part of the ResourceGroup
+3. Define the transformation matrix as part of the ResourceFlavor definition.
```

  cool, thanks for the update

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T13:59:37Z

- **keps/2937-resource-transformer/README.md:246** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T13:59:36Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `kueue.x-k8s.io/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `kueue.x-k8s.io/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `kueue.x-k8s.io/accelerator-memory`, `5G`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `kueue.x-k8s.io/accelerator-memory`, `10G`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `kueue.x-k8s.io/accelerator-memory`, `20G`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `kueue.x-k8s.io/accelerator-memory`, `40G`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  kueue.x-k8s.io/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "kueue.x-k8s.io/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: kueue.x-k8s.io/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `kueue.x-k8s.io/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
```

  Updated to embed ResourceMapping in configuration's Resource struct and moved other options to alternatives.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T17:23:21Z

- **keps/2937-resource-transformer/README.md:296** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T17:23:21Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        example.com/accelerator-memory: 5G
+        example.com/credits: 10
```

  Only the post-transformed resources are used to assign flavors and reserve resources.  So the nominalQuota of `nvidia.com/mig-1g.5gb` given to the default-flavor by this cluster queue will never be requested.  In terms of the ClusterQueue logic, this is the same processing/logic that would happen when for example a workload that only requests `cpu` is admitted via using a flavor that covers both `cpu` and `memory`.
  
  It is certainly would be possible to detect that a `coveredResource` is also an input resource with a `Replace` strategy.  I'm not sure what the appropriate level of warning/action is if this happens.  It seems like a parallel situation with a `coveredResource` being matching by an `excludedResourcePrefixes` which is not currently checked or validated by Kueue.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T17:29:32Z

- **keps/2937-resource-transformer/README.md:290** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T17:29:31Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
```

  My suggestion is to leave semantic validation of the `Resources` struct as separate item if we think it is needed.
  
  There seem to be two main areas that could be checked:
  1.  A resource that shows up as an `input` in `mappings` that would match a string in `excludedResourcePrefixes`.  This seems like it could be flagged as configuration error.  Probably giving `excludedResourcePrefixes` priority is the least surprising semantics.
  2. Validation at the ClusterQueue level that nothing in a `coveredResources` list has a `Replace` resource mapping and that nothing in a `coveredResources` list matches an excludedResourcePrefixes.  Probably marking the ClusterQueue inactive and reporting the resason in the conditions message is the least surprising semantics.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T21:23:52Z

- **keps/2937-resource-transformer/README.md:217** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T21:23:52Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
```

  I expanded/clarified user story 2 (credits).  I think this might address this comment.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T21:25:48Z

- **keps/2937-resource-transformer/README.md:296** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T21:25:48Z
```diff
@@ -0,0 +1,560 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Granularity Options](#granularity-options)
+  - [Observabilty](#observabilty)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+a ResourceGroup whose CoveredResources include all resources requested by the Job.
+2. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations.
+
+Injecting a configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of 
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a 
+`ResourceTransformation` matrix.
+* Extend `workload.NewInfo` to apply the matrix when computing the resources needed to admit the workload
+ immediately after it applies `dropExcludedResources`.
+* Optionally extend the status information for non-admitted workloads to make the full set of
+transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` matrix which would be created by
+a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related 
+extended resources without creating overly complex ResourceGroups in its 
+ClusterQueues.  A motivating example is managing quotas for the various 
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource, 
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider defining a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` paritions and puts a tighter cap on `4g.20gb`
+partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the acelerator-memory quota
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 0  # not allowed -- inefficient use of GPU resources
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually 
+available in the Nodes of the cluster. For example, it could be used to 
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the actual monetary cost of a cloud resource (for example a larger or more powerful GPU
+could cost twice as many `credits` as a smaller or previous generation GPU).
+We'd expect in this usage pattern that `Replace` would be used so that
+resource list is just augmented with the computed `credits` resource.  We'd also
+expect that a ClusterQueue's `borrowingLimit` for `credits` would be set to `0`
+to enforce the intended fiscal constraint.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+A transformation matrix is defined as a single object.  Most likely
+this object would be embedded inside Kueue's `Configuration` API, but
+it would also be possible to make it a separate top-level config API or 
+use a config map containing a single yaml field that would be deserialized
+into the type defined below.  To support finer-grained configuration, instead
+of defining a single controller-wide transformation matrix, they could be embedded
+in ClusterQueue ResourceGroups (perhaps implicitly by instead embedding the
+transformation matrix in a ResourceFlavor).
+
+```go
+type ResourceTransformationStrategy string
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+  // key is the output resource name and value is the quantity of the output resource 
+  // that corresponds to 1 unit of the input resource
+  ConversionBaseValues map[corev1.ResourceName]resource.Quantity `json:"conversionBaseValues"`
+
+  // should the input resource be replaced or retained
+  Strategy ResourceTransformationStrategy `json:"strategy"`
+}
+
+// The map key is the input resource name
+type ResourceTransformationMatrix map[corev1.ResourceName]ResourceTransformation
+
+```
+By using maps instead of arrays, we avoid needing
+to define the semantics of multiple entries having the same input or output
+resource name.  Using the input resource name as the key to the outer map
+avoids conflicting definitions of `Replace` for an input resource.
+
+As an example, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  excludedResourcePrefixes: []
+  transformations:
+    nvidia.com/mig-1g.5gb:
+      strategy: Replace
+      conversionBaseValues:
+        example.com/accelerator-memory: 5G
+        example.com/credits: 10
```

  Added discussion in KEP about interactions between `resourceMappings`,  `excludedResourcePrefixes` and `coveredResources`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-27T11:53:41Z

LGTM, just a naming nit

- **keps/2937-resource-transformer/README.md:297** by [@mimowo](https://github.com/mimowo) — 2024-09-27T11:50:42Z
```diff
@@ -0,0 +1,617 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceMapping`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceMapping` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceMapping` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceMapping` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceMapping `json:"transformations,omitempty"`
```

  nit, but I would suggest to align the field name with the type name. Maybe call the type "ResourceTransformation" - I think it will play nicely. Rename ResourceMappingStrategy consistently in that case. Alternative is to call the field "ResourceMappings", I'm good with both.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-27T15:11:16Z

- **keps/2937-resource-transformer/README.md:297** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-27T15:11:16Z
```diff
@@ -0,0 +1,617 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceMapping`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceMapping` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceMapping` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceMapping` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceMapping `json:"transformations,omitempty"`
```

  Makes sense.   Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-30T17:57:38Z

Sorry for the late.

- **keps/2937-resource-transformer/README.md:528** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-30T17:57:14Z
```diff
@@ -0,0 +1,619 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceTransformation`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceTransformation` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceTransformation` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceTransformation `json:"transformations,omitempty"`
+}
+
+type ResourceTransformationStrategy string
+
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+	// Name of the input resource
+	Input corev1.ResourceName `json:"input"`
+
+	// Whether the input resource should be replaced or retained
+	// +kubebuilder:default="Retain"
+	Strategy ResourceTransformationStrategy `json:"strategy"`
+
+	// Output resources and quantities per unit of input resource
+	Outputs corev1.ResourceList `json:"outputs,omitempty"`
+}
+```
+Given these types, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+      example.com/credits: 10
+  - input: nvidia.com/mig-2g.10gb
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+      example.com/credits: 15
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+      example.com/credits: 30
+  - cpu:
+    strategy: Retain
+    outputs:
+      example.com/credits: 1
+```
+
+### Observability
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+  - input: nvidia.com/mig-2g.10gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+  - input: nvidia.com/mig-7g.40gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        example.com/accelerator-memory:     default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        example.com/accelerator-memory:     20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      example.com/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             example.com/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
+    Name: main
+    Resource Request:
```

  @dgrove-oss This status API still seems a bit weird to me since the ".resourceRequests[0].resourceRequest" looks like duplicated meaning and semantics. And, the "resourceRequests" field name does not indicate the semantics that this is transformed resources by Kueue recognization. So, could you expand this API to the following?
  
  Additionllly, I'm wondering if we should add "PodSet" prefix like the following since, in the future, we may want to add any resourceRequests information for other objects and concepts.
  
  ```suggestion
  transformedPodSetResources:
      - name: ${PODSET_NAME}
        requests:
            cpu: xxx
            memory: yyy
  ```
  
  @alculquicondor @mimowo Any thoughts?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-30T18:08:40Z

- **keps/2937-resource-transformer/README.md:528** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-30T18:08:40Z
```diff
@@ -0,0 +1,619 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceTransformation`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceTransformation` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceTransformation` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceTransformation `json:"transformations,omitempty"`
+}
+
+type ResourceTransformationStrategy string
+
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+	// Name of the input resource
+	Input corev1.ResourceName `json:"input"`
+
+	// Whether the input resource should be replaced or retained
+	// +kubebuilder:default="Retain"
+	Strategy ResourceTransformationStrategy `json:"strategy"`
+
+	// Output resources and quantities per unit of input resource
+	Outputs corev1.ResourceList `json:"outputs,omitempty"`
+}
+```
+Given these types, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+      example.com/credits: 10
+  - input: nvidia.com/mig-2g.10gb
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+      example.com/credits: 15
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+      example.com/credits: 30
+  - cpu:
+    strategy: Retain
+    outputs:
+      example.com/credits: 1
+```
+
+### Observability
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+  - input: nvidia.com/mig-2g.10gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+  - input: nvidia.com/mig-7g.40gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        example.com/accelerator-memory:     default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        example.com/accelerator-memory:     20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      example.com/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             example.com/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
+    Name: main
+    Resource Request:
```

  It crossed my mind that there might be duplicated information within the admission field.
  
  But if we go with the name `TransformedPodSetResources`, then we would only list the transformed resources, and not the original ones.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-30T18:15:36Z

- **keps/2937-resource-transformer/README.md:528** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-30T18:15:36Z
```diff
@@ -0,0 +1,619 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceTransformation`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceTransformation` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceTransformation` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceTransformation `json:"transformations,omitempty"`
+}
+
+type ResourceTransformationStrategy string
+
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+	// Name of the input resource
+	Input corev1.ResourceName `json:"input"`
+
+	// Whether the input resource should be replaced or retained
+	// +kubebuilder:default="Retain"
+	Strategy ResourceTransformationStrategy `json:"strategy"`
+
+	// Output resources and quantities per unit of input resource
+	Outputs corev1.ResourceList `json:"outputs,omitempty"`
+}
+```
+Given these types, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+      example.com/credits: 10
+  - input: nvidia.com/mig-2g.10gb
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+      example.com/credits: 15
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+      example.com/credits: 30
+  - cpu:
+    strategy: Retain
+    outputs:
+      example.com/credits: 1
+```
+
+### Observability
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+  - input: nvidia.com/mig-2g.10gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+  - input: nvidia.com/mig-7g.40gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        example.com/accelerator-memory:     default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        example.com/accelerator-memory:     20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      example.com/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             example.com/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
+    Name: main
+    Resource Request:
```

  The resources listed in `Status.Admission.PodSetAssignments[N].ResourceUsage` will always be the transformed resources (because the transformed resources are what is used for flavor assignment). 
  
  Not sure if this helps, but maybe it helps to look at the current implementation (#3026) and in particular the related question I raised here: https://github.com/kubernetes-sigs/kueue/pull/3026#discussion_r1780152714

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-30T18:16:23Z

- **keps/2937-resource-transformer/README.md:528** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-30T18:16:23Z
```diff
@@ -0,0 +1,619 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceTransformation`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceTransformation` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceTransformation` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceTransformation `json:"transformations,omitempty"`
+}
+
+type ResourceTransformationStrategy string
+
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+	// Name of the input resource
+	Input corev1.ResourceName `json:"input"`
+
+	// Whether the input resource should be replaced or retained
+	// +kubebuilder:default="Retain"
+	Strategy ResourceTransformationStrategy `json:"strategy"`
+
+	// Output resources and quantities per unit of input resource
+	Outputs corev1.ResourceList `json:"outputs,omitempty"`
+}
+```
+Given these types, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+      example.com/credits: 10
+  - input: nvidia.com/mig-2g.10gb
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+      example.com/credits: 15
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+      example.com/credits: 30
+  - cpu:
+    strategy: Retain
+    outputs:
+      example.com/credits: 1
+```
+
+### Observability
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+  - input: nvidia.com/mig-2g.10gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+  - input: nvidia.com/mig-7g.40gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        example.com/accelerator-memory:     default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        example.com/accelerator-memory:     20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      example.com/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             example.com/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
+    Name: main
+    Resource Request:
```

  I'm happy to adjust both the KEP (in a follow up PR) and the prototype implementation so we end up in a good place.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-30T18:19:47Z

- **keps/2937-resource-transformer/README.md:528** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-30T18:19:47Z
```diff
@@ -0,0 +1,619 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceTransformation`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceTransformation` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceTransformation` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceTransformation `json:"transformations,omitempty"`
+}
+
+type ResourceTransformationStrategy string
+
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+	// Name of the input resource
+	Input corev1.ResourceName `json:"input"`
+
+	// Whether the input resource should be replaced or retained
+	// +kubebuilder:default="Retain"
+	Strategy ResourceTransformationStrategy `json:"strategy"`
+
+	// Output resources and quantities per unit of input resource
+	Outputs corev1.ResourceList `json:"outputs,omitempty"`
+}
+```
+Given these types, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+      example.com/credits: 10
+  - input: nvidia.com/mig-2g.10gb
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+      example.com/credits: 15
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+      example.com/credits: 30
+  - cpu:
+    strategy: Retain
+    outputs:
+      example.com/credits: 1
+```
+
+### Observability
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+  - input: nvidia.com/mig-2g.10gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+  - input: nvidia.com/mig-7g.40gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        example.com/accelerator-memory:     default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        example.com/accelerator-memory:     20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      example.com/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             example.com/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
+    Name: main
+    Resource Request:
```

  > It crossed my mind that there might be duplicated information within the admission field.
  
  Yes, that's true, but considering the case of partial admission, dynamic reclaiming and dynamically sized Jobs (not yet implemented), a dedicated transformed resource request field might be worth it since the admission field has only admitted resource requests rather than a whole of input resource requests.
  
  Although, adding the transformed resource information to admission field might be better, I guess that we can consider it as a separate enhancement.
  
  > But if we go with the name TransformedPodSetResources, then we would only list the transformed resources, and not the original ones.
  
  Good point. That sounds reasonable. We should hold only modified information in the status field. The immutable field should not be added to the status field.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-02T10:58:11Z

- **keps/2937-resource-transformer/README.md:528** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-02T10:58:11Z
```diff
@@ -0,0 +1,619 @@
+# KEP-2937: Configurable Resource Transformers
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+      - [Story 1A - Using Replace with MIG](#story-1a---using-replace-with-mig)
+      - [Story 1B - Using Retain with MIG](#story-1b---using-retain-with-mig)
+    - [Story 2](#story-2)
+- [Design Details](#design-details)
+  - [Specifying the Transformation](#specifying-the-transformation)
+  - [Observability](#observability)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ClusterQueue Scoped](#clusterqueue-scoped)
+  - [ResourceFlavor Scoped](#resourceflavor-scoped)
+  - [Workload Scoped](#workload-scoped)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a mechanism that enables cluster admins to customize
+how the resource requests and limits of a Job are translated into
+the resource requirements of its corresponding Workload. This enables
+the admission and quota calculations done by ClusterQueues to be performed
+on an abstracted and/or transformed set of resources without impacting
+the actual resource requests/limits that the Pods created by the Workload
+will present to the Kubernetes Scheduler when the Workload is admitted by
+a ClusterQueue.
+
+## Motivation
+
+Kueue currently performs a direct and mostly non-configurable mapping of the resource
+requests/limits of a Job into the resource requests of the Job's Workload.
+The only supported configuration is the ability to ignore resources that match
+a global `execludedResourcePrefixes` list.
+This direct mapping results in two significant limitations that limit the expressivity
+of Kueue's resource and quota mechanisms:
+1. Only resources that are explicitly listed in a Jobs requests/limits can
+be used in Kueue's quota calculations and admission decisions.
+2. For a Job to be admissible by a ClusterQueue, the ClusterQueue must have
+ResourceGroups whose CoveredResources include all non-excluded resources requested by the Job.
+
+Injecting a more configurable transformation function into this mapping
+will enable Kueue to support simpler and more powerful definitions of resource quotas.
+
+### Goals
+
+* Establish a mechanism that enables limited transformations on resources
+  to be specified via external configuration.
+
+### Non-Goals
+
+* Supporting complex numerical transformations of resource quantities
+* Supporting transformations that take multiple input resources
+* Performing any mutation of the resource requests/limits of Jobs
+* Ensuring that a Workload's resource requests is updated if the set of
+configured transformations functions changes after its resource requests are computed.
+
+## Proposal
+
+* Add a configuration mechanism that enables a cluster admin to specify a `ResourceTransformation`.
+* Extend `workload.NewInfo` to apply the resource mapping when computing the resources
+needed to admit the workload immediately after it applies `dropExcludedResources`.
+* Extend the `Status` field of Workload to make the transformed resources directly observable.
+
+### User Stories (Optional)
+
+In the user stories below, we use a 4-tuple (InputResourceName, OutputResourceName, OutputQuantity, Replace/Retain)
+to express a resource transformation operation. In the actual implementation, this transformation would
+be encoded as an entry in a `ResourceTransformation` which would be created by a cluster admin.
+
+#### Story 1
+
+This KEP will improve Kueue's ability to manage families of closely related
+extended resources without creating overly complex ResourceGroups in its
+ClusterQueues.  A motivating example is managing quotas for the various
+MIG resources created by the NVIDIA GPU Operator using the mixed strategy
+(see [MIG Mixed Strategy][mig]).
+In the mixed strategy, instead of there being a single `nvidia.com/gpu` extended resource,
+there is a proliferation of extended resource of the form `nvidia.com/mig-1g.5gb`, `nvidia.com/mig-2g.10gb`,
+`nvidia.com/mig-4g.20gb`, and so on.  For quota management purposes, we would like to abstract
+these into a single primary extended resource `example.com/accelerator-memory`.
+
+##### Story 1A - Using Replace with MIG
+
+If an operator such as [InstaSlice][instaslice] is deployed
+that is capable of dynamically managing the MIG partitions available on the cluster,
+the cluster admin may desire to completely elide the details of different MIG variants
+from their quota management.
+
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `replace`)
+
+In this scenario, the covered resources in a ClusterQueue would only need
+to include `example.com/accelerator-memory`
+and would not need to assign redundant and potentially confusing quotas to every MIG variant.
+
+As a concrete example, consider a submitted Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+this would yield a Workload with an effective request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  memory: 100M
+  cpu: 1
+```
+and could be admitted to the `default-flavor` of a ClusterQueue with the ResourceGroup below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+To compare, to achieve the identical effective quotas without `replace` the covered
+resource stanza of each ClusterQueue would need to be:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory", "nvidia.com/mig-1g.5gb", "nvidia.com/mig-2g.10gb", "nvidia.com/mig-4g.20gb", "nvidia.com/mig-7g.40gb"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 10  # 200G/20g
+      - name: nvidia.com/mig-7g.40gb
+        nominalQuota: 5  # 200G/40g
+```
+
+##### Story 1B - Using Retain with MIG
+
+However, some cluster admins may desire to use `retain` for MIG resources to exercise
+finer grained control over the use of MIG partitions.
+To do this, the cluster admin could register the following transformations:
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `retain`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `retain`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `retain`)
+* (`nvidia.com/mig-7g.40gb`, `example.com/accelerator-memory`, `40Gi`, `retain`)
+
+A Job requesting the resources below:
+```yaml
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+would yield a Workload with an effective resource request of:
+```yaml
+  example.com/accelerator-memory: 20G
+  nvidia.com/mig-1g.5gb: 2
+  nvidia.com/mig-2g.10gb: 1
+  memory: 100M
+  cpu: 1
+```
+
+Below is an example ResourceGroup that the cluster admin could define that
+prevents the usage of `7g.40gb` partitions by not listing them in the covered-resources
+and puts a tighter quota on `4g.20gb` partitions than would be implied by the `accelerator-memory` quota:
+```yaml
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 200G
+      - name: nvidia.com/mig-1g.5gb
+        nominalQuota: 40  # 200G/5g
+      - name: nvidia.com/mig-2g.10gb
+        nominalQuota: 20  # 200G/10g
+      - name: nvidia.com/mig-4g.20gb
+        nominalQuota: 4  # less than the 10 that would be allowed by the accelerator-memory quota
+        borrowingLimit: 0
+```
+
+[mig]: https://docs.nvidia.com/datacenter/cloud-native/kubernetes/latest/index.html#the-mixed-strategy
+[instaslice]: https://github.com/openshift/instaslice-operator
+
+#### Story 2
+
+This KEP would allow Kueue to augment a Job's resource requests
+for quota purposes with additional resources that are not actually
+available in the Nodes of the cluster. For example, it could be used to
+define quotas in terms of a fictional currency `example.com/credits`
+to give "costs" to various resources. This could be used to approximate
+the monetary cost of different cloud resources and thus apply a "budget" to a team.
+We'd expect in this usage pattern that `retain` would be used so that the
+transformed resource list is just augmented with the computed `credits` resource.
+Most likely `credits` would be in a separate ResourceGroup than the "real" resources (similar to the
+[example of a ClusterQueue with software licenses](https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups))
+and the `borrowingLimit` and `lendingLimit` for
+`credits` would be set to `0` to enforce the intended fiscal constraint.
+
+For example:
+* (`foo.com/gpu`, `example.com/credits`, `10`, `retain`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+```yaml
+  - coveredResources: ["cpu", "memory", "foo.com/gpu"]
+    flavors:
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 10
+      - name: "memory"
+        nominalQuota: 36Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 40
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+      - name: "foo.com/gpu"
+        nominalQuota: 100
+  - coveredResources: ["example.com/credits"]
+    flavors:
+    - name: "team1-budget"
+      resources:
+      - name: "example.com/credits"
+        nominalQuota: 750
+        borrowingLimit: 0
+        lendingLimit: 0
+```
+
+With the single global `ResourceTransformation` proposed in this KEP, this use case may
+be of limited applicability.  It would benefit from finer-grained
+[alternatives](#alternatives) that would for example `cpu` allocated from
+the `spot` flavor to cost fewer credits than when allocated from the `on-demand` flavor.
+
+## Design Details
+
+Since Kueue's workload.Info struct is already a well-defined
+place where resource filtering is being applied to compute the effective resource
+request from a Workload's PodSpec, the core of the implementation is
+straightforward and follows from how `excludedResourcePrefixes` are already processed.
+
+The non-trivial design issues are how to configure the transformations,
+the possible granularity of that configuration, and how to maximize observability
+of the effective resources requested by a Workload.
+
+### Specifying the Transformation
+
+In this KEP we propose to support a global set of resource transformations that would
+be applied to all Workloads.  A `ResourceTransformation` instance would be added to Kueue's
+`Configuration` API as part of the existing `Resources` struct.
+More [flexible alternatives](#alternatives) were considered, but left as possible
+future extensions.
+
+The definitions below would be added to Kueue's `Configuration` types
+```go
+type Resources struct {
+	// ExcludedResourcePrefixes defines which resources should be ignored by Kueue
+	ExcludeResourcePrefixes []string `json:"excludeResourcePrefixes,omitempty"`
+
+	// Transformations defines how to transform PodSpec resources into Workload resource requests.
+	// +listType=map
+	// +listMapKey=input
+	Transformations []ResourceTransformation `json:"transformations,omitempty"`
+}
+
+type ResourceTransformationStrategy string
+
+const Retain ResourceTransformationStrategy = "Retain"
+const Replace ResourceTransformationStrategy = "Replace"
+
+type ResourceTransformation struct {
+	// Name of the input resource
+	Input corev1.ResourceName `json:"input"`
+
+	// Whether the input resource should be replaced or retained
+	// +kubebuilder:default="Retain"
+	Strategy ResourceTransformationStrategy `json:"strategy"`
+
+	// Output resources and quantities per unit of input resource
+	Outputs corev1.ResourceList `json:"outputs,omitempty"`
+}
+```
+Given these types, the following list of 4-tuples
+
+* (`nvidia.com/mig-1g.5gb`, `example.com/accelerator-memory`, `5Gi`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/accelerator-memory`, `10Gi`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/accelerator-memory`, `20Gi`, `replace`)
+* (`nvidia.com/mig-1g.5gb`, `example.com/credits`, `10`, `replace`)
+* (`nvidia.com/mig-2g.10gb`, `example.com/credits`, `15`, `replace`)
+* (`nvidia.com/mig-4g.20gb`, `example.com/credits`, `30`, `replace`)
+* (`cpu`, `example.com/credits`, `1`, `retain`)
+
+would be represented in yaml when embedded in the Kueue Configuration object as
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+      example.com/credits: 10
+  - input: nvidia.com/mig-2g.10gb
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+      example.com/credits: 15
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+      example.com/credits: 30
+  - cpu:
+    strategy: Retain
+    outputs:
+      example.com/credits: 1
+```
+
+### Observability
+
+Let's return to the first user story with MIG and look in more detail at how it works.
+The transformation matrix is:
+```yaml
+resources:
+  transformations:
+  - input: nvidia.com/mig-1g.5gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 5G
+  - input: nvidia.com/mig-2g.10gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 10G
+  - input: nvidia.com/mig-4g.20gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 20G
+  - input: nvidia.com/mig-7g.40gb:
+    strategy: Replace
+    outputs:
+      example.com/accelerator-memory: 40G
+```
+
+We define a ClusterQueue with the covered resources below:
+```yaml
+...
+  - coveredResources: ["cpu", "memory", "example.com/accelerator-memory"]
+    flavors:
+    - name: default-flavor
+      resources:
+      - name: "cpu"
+        nominalQuota: 8
+      - name: "memory"
+        nominalQuota: 500G
+      - name: example.com/accelerator-memory
+        nominalQuota: 80G
+```
+
+The user submits a single Pod Job to this ClusterQueue with the resource requests/limits:
+```yaml
+    resources:
+      requests:
+        cpu: 1
+        memory: 100G
+      limits:
+        nvidia.com/mig-1g.5gb: 2
+        nvidia.com/mig-2g.10gb: 1
+```
+
+When the Job is admitted, the transformed resources appear in the Workload's Status as shown below:
+```yaml
+Status:
+  Admission:
+    Cluster Queue:  cluster-queue
+    Pod Set Assignments:
+      Count:  1
+      Flavors:
+        Cpu:                                default-flavor
+        example.com/accelerator-memory:     default-flavor
+        Memory:                             default-flavor
+      Name:                                 main
+      Resource Usage:
+        Cpu:                                1
+        example.com/accelerator-memory:     20G
+        Memory:                             97656250Ki
+```
+and in the ClusterQueue Status we would see:
+```yaml
+Status:
+  Admitted Workloads:  1
+  Conditions:
+    Last Transition Time:  2024-09-19T14:20:13Z
+    Message:               Can admit new workloads
+    Observed Generation:   3
+    Reason:                Ready
+    Status:                True
+    Type:                  Active
+  Flavors Reservation:
+    Name:  default-flavor
+    Resources:
+      Borrowed:  0
+      Name:      cpu
+      Total:     1
+      Borrowed:  0
+      Name:      example.com/accelerator-memory
+      Total:     20G
+      Borrowed:  0
+      Name:      memory
+      Total:     97656250Ki
+  Flavors Usage:
+    Name:  default-flavor
+    Resources:
+      Borrowed:         0
+      Name:             cpu
+      Total:            1
+      Borrowed:         0
+      Name:             example.com/accelerator-memory
+      Total:            20G
+      Borrowed:         0
+      Name:             memory
+      Total:            97656250Ki
+  Pending Workloads:    0
+  Reserving Workloads:  1
+```
+
+After enough Workloads are admitted that there is queuing,
+then the detailed status message in the `QuotaReserved` condition
+of the Workload will indicate the resource(s) with insufficient quota, but the
+actual transformed resource request is not observable.
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+We could improve the observability by extending the Workload Status with a ResourceRequests
+field that would contain a copy of the `TotalRequests` field of the controller-internal `workload.Info`
+struct.  If we did this, we would instead see something like the below for a non-admitted Workload:
+```yaml
+Spec:
+  Active:  true
+  Pod Sets:
+    Count:  1
+    Name:   main
+    Template:
+      Metadata:
+      Spec:
+        Containers:
+          ...
+          Resources:
+            Limits:
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+            Requests:
+              Cpu:                     1
+              Memory:                  100G
+              nvidia.com/mig-1g.5gb:   2
+              nvidia.com/mig-2g.10gb:  1
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-09-19T14:59:01Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for example.com/accelerator-memory in flavor default-flavor, 20G more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+  ResourceRequests:
+    Name: main
+    Resource Request:
```

  Let's continue the discussion in the implementation PR.
  We can refine the design and API in the implementation PR.
