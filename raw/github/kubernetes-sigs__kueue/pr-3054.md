# PR #3054: Integration restart manager

**Summary**: Integration restart manager

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3054

**Last updated**: 2024-09-26T06:54:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `integration-restart-manager` → `main`
- **Created**: 2024-09-16T07:26:00Z
- **Updated**: 2024-09-26T06:54:24Z
- **Merged**: 2024-09-26T06:30:02Z
- **Closed**: 2024-09-26T06:30:02Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 52   **+780 / -865**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
- Make the integration test framework able to stop the manager running in an envtest instance.
- Reuse the envtest instances in the test suites.
- Reorganize some suites for better performance while running in parallel.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #2993 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-16T07:26:02Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-16T07:26:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2f58d0689fc6b2010fb1acd613025e69fde5c269 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66f438a879ba140008697b06 |

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T07:26:41Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T07:58:07Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T08:17:24Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T08:37:44Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T10:06:12Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T10:56:14Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T13:06:11Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T14:01:09Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T14:38:16Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T15:13:02Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T15:31:29Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-16T16:55:07Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T06:04:54Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T06:34:25Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T07:23:19Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T07:40:44Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T09:24:11Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T13:06:31Z

/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-17T13:39:56Z

/cc @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-17T15:00:02Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-18T16:05:24Z

> * Make the integration test framework able to stop the manager running in an envtest instance.
> * Reuse the envtest instances in the test suites.
> * Reorganize some suites for better performance while running in parallel.

Would you mind splitting this into dedicated PRs for the ease of reviewing?

### Comment by [@trasc](https://github.com/trasc) — 2024-09-18T16:29:47Z

> > * Make the integration test framework able to stop the manager running in an envtest instance.
> > * Reuse the envtest instances in the test suites.
> > * Reorganize some suites for better performance while running in parallel.
> 
> Would you mind splitting this into dedicated PRs for the ease of reviewing?

I'd prefer not to, the only file that contains "substantial" changes is [test/integration/framework/framework.go](https://github.com/kubernetes-sigs/kueue/pull/3054/files#diff-9be12d69f919437ea6abbe1fbcae80e1e97b5e28fdcbc515596187f25b3b6fe4), for the rest of them , it's just about changing the place where `fwk.Init()` is called (`BeforeSuite` instead of `BeforeAll` or similar) and for the webhook related tests moved in one package.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-19T08:10:02Z

> I'd prefer not to, the only file that contains "substantial" changes is [test/integration/framework/framework.go](https://github.com/kubernetes-sigs/kueue/pull/3054/files#diff-9be12d69f919437ea6abbe1fbcae80e1e97b5e28fdcbc515596187f25b3b6fe4), for the rest of them , it's just about changing the place where fwk.Init() is called (BeforeSuite instead of BeforeAll or similar) and for the webhook related tests moved in one package.

You can generally expect much faster reviews for PRs which are small and separate concerns, please read:
- https://www.kubernetes.dev/docs/guide/pull-requests/#smaller-is-better-small-commits-small-pull-requests
- https://google.github.io/eng-practices/review/reviewer/speed.html#large

In particular, moving the webhooks feels like separate PR, we would also maybe be able to see what is the performance benefit of various improvements separately. Otherwise it is hard to tell if one optimization helps and how much, if there is another optimization biasing the stats. 

Anyway, if you want I can try to review the PR in this form, but seeing that the process may not be as effective as it could.

### Comment by [@trasc](https://github.com/trasc) — 2024-09-19T08:25:32Z

> > I'd prefer not to, the only file that contains "substantial" changes is [test/integration/framework/framework.go](https://github.com/kubernetes-sigs/kueue/pull/3054/files#diff-9be12d69f919437ea6abbe1fbcae80e1e97b5e28fdcbc515596187f25b3b6fe4), for the rest of them , it's just about changing the place where fwk.Init() is called (BeforeSuite instead of BeforeAll or similar) and for the webhook related tests moved in one package.
> 
> You can generally expect much faster reviews for PRs which are small and separate concerns, please read:
> 
> * https://www.kubernetes.dev/docs/guide/pull-requests/#smaller-is-better-small-commits-small-pull-requests
> * https://google.github.io/eng-practices/review/reviewer/speed.html#large
> 
> In particular, moving the webhooks feels like separate PR, we would also maybe be able to see what is the performance benefit of various improvements separately. Otherwise it is hard to tell if one optimization helps and how much, if there is another optimization biasing the stats.
> 
> Anyway, if you want I can try to review the PR in this form, but seeing that the process may not be as effective as it could.

Most of the changes are trivial. Also the webhook moving needs to be in the same PR as the `Init` call changes, otherwise we'll have a broken intermediate state.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-25T14:45:51Z

I would prefer some of the drive-by fixes/cleanups to be in dedicated PRs so that they could cherry-pick if needed. Also, to reduce distractions and minimize the cognitive load. As mentioned in https://github.com/kubernetes-sigs/kueue/pull/3054#issuecomment-2360285542, I believe it makes the review process easier and faster overall.

In any case, I think I understand the changes now, and are ok to go with them.

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-25T14:45:57Z

LGTM label has been added.  <details>Git tree hash: d701e65d64d84e6011e2ef650acd6067d92cc368</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-25T14:45:58Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3054#issuecomment-2374308227" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3054#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo,trasc]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-25T14:55:53Z

/hold
Due to the failure in integration tests which might be likely due to the PR https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/3054/pull-kueue-test-integration-main/1838953606426923008. Let's verify that and check stability by at least 5 repeats before merging

### Comment by [@trasc](https://github.com/trasc) — 2024-09-25T16:17:57Z

> Due to the failure in integration tests which might be likely due to the PR https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/3054/pull-kueue-test-integration-main/1838953606426923008. Let's verify that and check stability by at least 5 repeats before merging

`autoscaling.AddToScheme` moved in the framework https://github.com/kubernetes-sigs/kueue/pull/3054/commits/2f58d0689fc6b2010fb1acd613025e69fde5c269

### Comment by [@trasc](https://github.com/trasc) — 2024-09-25T16:39:22Z

1/5
/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-25T16:59:03Z

2/5
/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-25T17:16:34Z

3/5
/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-25T17:33:07Z

4/5
/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-25T18:00:04Z

5/5
/test pull-kueue-test-integration-main

### Comment by [@trasc](https://github.com/trasc) — 2024-09-25T18:26:06Z

5 out of 5 ok, @mimowo please have another look

### Comment by [@trasc](https://github.com/trasc) — 2024-09-26T04:11:47Z

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-26T06:28:04Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-26T06:28:09Z

LGTM label has been added.  <details>Git tree hash: cf47c2fd0e44acbf59ff6688242d01553aeae0ec</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T10:14:50Z

- **test/integration/framework/framework.go:182** by [@mimowo](https://github.com/mimowo) — 2024-09-20T10:14:50Z
```diff
@@ -124,46 +127,71 @@ func (f *Framework) SetupClient(cfg *rest.Config) (context.Context, client.Clien
 }
 
 func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup) {
-	webhookInstallOptions := &f.testEnv.WebhookInstallOptions
-	mgrOpts := manager.Options{
-		Scheme: scheme.Scheme,
-		Metrics: metricsserver.Options{
-			BindAddress: "0", // disable metrics to avoid conflicts between packages.
-		},
-		WebhookServer: webhook.NewServer(
-			webhook.Options{
-				Host:    webhookInstallOptions.LocalServingHost,
-				Port:    webhookInstallOptions.LocalServingPort,
-				CertDir: webhookInstallOptions.LocalServingCertDir,
-			}),
-		Controller: crconfig.Controller{
-			SkipNameValidation: ptr.To(true),
-		},
-	}
-	mgr, err := ctrl.NewManager(cfg, mgrOpts)
-	gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to create manager")
-
-	managerSetup(ctx, mgr)
-
-	go func() {
-		defer ginkgo.GinkgoRecover()
-		err := mgr.Start(ctx)
-		gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to run manager")
-	}()
-
-	if len(f.WebhookPath) > 0 {
-		// wait for the webhook server to get ready
-		dialer := &net.Dialer{Timeout: time.Second}
-		addrPort := fmt.Sprintf("%s:%d", webhookInstallOptions.LocalServingHost, webhookInstallOptions.LocalServingPort)
-		gomega.Eventually(func() error {
-			conn, err := tls.DialWithDialer(dialer, "tcp", addrPort, &tls.Config{InsecureSkipVerify: true})
-			if err != nil {
-				return err
-			}
-			conn.Close()
-			return nil
-		}).Should(gomega.Succeed())
-	}
+	ginkgo.By("starting the manager", func() {
+		webhookInstallOptions := &f.testEnv.WebhookInstallOptions
+		mgrOpts := manager.Options{
+			Scheme: scheme.Scheme,
+			Metrics: metricsserver.Options{
+				BindAddress: "0", // disable metrics to avoid conflicts between packages.
+			},
+			WebhookServer: webhook.NewServer(
+				webhook.Options{
+					Host:    webhookInstallOptions.LocalServingHost,
+					Port:    webhookInstallOptions.LocalServingPort,
+					CertDir: webhookInstallOptions.LocalServingCertDir,
+				}),
+			Controller: crconfig.Controller{
+				SkipNameValidation: ptr.To(true),
+			},
+		}
+		mgr, err := ctrl.NewManager(cfg, mgrOpts)
+		gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to create manager")
+
+		managerCtx, managerCancel := context.WithCancel(ctx)
+		managerSetup(managerCtx, mgr)
+
+		done := make(chan struct{})
+
+		f.managerCancel = managerCancel
+		f.managerDone = done
+
+		go func() {
+			defer close(done)
+			defer ginkgo.GinkgoRecover()
+			err := mgr.Start(managerCtx)
+			gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to run manager")
+		}()
+
+		if len(f.WebhookPath) > 0 {
+			// wait for the webhook server to get ready
+			dialer := &net.Dialer{Timeout: time.Second}
+			addrPort := fmt.Sprintf("%s:%d", webhookInstallOptions.LocalServingHost, webhookInstallOptions.LocalServingPort)
+			gomega.Eventually(func() error {
+				conn, err := tls.DialWithDialer(dialer, "tcp", addrPort, &tls.Config{InsecureSkipVerify: true})
+				if err != nil {
+					return err
+				}
+				conn.Close()
+				return nil
+			}).Should(gomega.Succeed())
+		}
+	})
+}
+
+func (f *Framework) StopManager(ctx context.Context) {
+	ginkgo.By("stoping the manager", func() {
```

  ```suggestion
  	ginkgo.By("stopping the manager", func() {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T10:32:25Z

Left some comments. TBH I'm still not sure why we cannot move the webhooks in a preparatory PR , commonizing the setup for webhooks first, but let it be.

- **test/integration/multikueue/suite_test.go:101** by [@mimowo](https://github.com/mimowo) — 2024-09-20T10:17:24Z
```diff
@@ -96,7 +96,11 @@ func createCluster(setupFnc framework.ManagerSetup, apiFeatureGates ...string) c
 		APIServerFeatureGates: apiFeatureGates,
 	}
 	c.cfg = c.fwk.Init()
-	c.ctx, c.client = c.fwk.RunManager(c.cfg, setupFnc)
+	c.ctx, c.client = c.fwk.SetupClient(c.cfg)
+	// skip the manager setup if setup func is not provided
+	if setupFnc != nil {
```

  Is this needed for the PR?

- **test/integration/scheduler/podsready/scheduler_test.go:188** by [@mimowo](https://github.com/mimowo) — 2024-09-20T10:20:35Z
```diff
@@ -190,7 +185,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, testCQ)).Should(gomega.Succeed())
 			defer func() {
-				gomega.Expect(util.DeleteObject(ctx, k8sClient, testCQ)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, testCQ, true)
```

  I don't see how this change is related. Please move to a dedicated PR if not needed here.

- **test/integration/scheduler/podsready/scheduler_test.go:158** by [@mimowo](https://github.com/mimowo) — 2024-09-20T10:20:40Z
```diff
@@ -161,9 +155,10 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("checking the first prod workload gets admitted while the second is waiting")
 			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
 			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, prodWl)
```

  I don't see how this change is related. Please move to a dedicated PR if not needed here.
  .

- **test/integration/scheduler/podsready/scheduler_test.go:644** by [@mimowo](https://github.com/mimowo) — 2024-09-20T10:21:29Z
```diff
@@ -650,7 +641,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, testCQ)).Should(gomega.Succeed())
 			defer func() {
-				gomega.Expect(util.DeleteObject(ctx, k8sClient, testCQ)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, testCQ, true)
```

  I don't see how this change is related. Please move to a dedicated PR if not needed here.

- **test/integration/multikueue/suite_test.go:237** by [@mimowo](https://github.com/mimowo) — 2024-09-20T10:24:18Z
```diff
@@ -222,27 +226,26 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 }
 
-func multiclusterSetup(gcInterval time.Duration) {
+var _ = ginkgo.BeforeSuite(func() {
 	var managerFeatureGates []string
 	version, err := versionutil.ParseGeneric(os.Getenv("ENVTEST_K8S_VERSION"))
 	if err != nil || !version.LessThan(versionutil.MustParseSemantic("1.30.0")) {
 		managerFeatureGates = []string{"JobManagedBy=true"}
 	}
 
-	managerTestCluster = createCluster(func(ctx context.Context, mgr manager.Manager) {
-		managerAndMultiKueueSetup(ctx, mgr, gcInterval)
-	}, managerFeatureGates...)
+	// pass nil setup since the manager for the manage cluster is different in some specs.
+	managerTestCluster = createCluster(nil, managerFeatureGates...)
```

  I don't think I follow, why the kueue manager setup is moved from here, but the kueue manager for workers stays here? I think it will be easier to understand if both types of manager where setup in the same place.

- **test/integration/webhook/jobs/mpijob_webhook_test.go:31** by [@mimowo](https://github.com/mimowo) — 2024-09-20T10:26:59Z
```diff
@@ -23,24 +23,21 @@ import (
 	apierrors "k8s.io/apimachinery/pkg/api/errors"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 
+	"sigs.k8s.io/kueue/pkg/controller/jobs/mpijob"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
-	"sigs.k8s.io/kueue/test/integration/framework"
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("MPIJob Webhook", func() {
+var _ = ginkgo.Describe("MPIJob Webhook", ginkgo.Ordered, func() {
```

  Whys it the structure for the Describe and When changed (the Ordered label moved from When to Describe)? Also, I don't see ContinueOnFailure anymore. Is it needed in this PR?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-20T13:48:15Z

- **test/integration/multikueue/suite_test.go:101** by [@trasc](https://github.com/trasc) — 2024-09-20T13:12:09Z
```diff
@@ -96,7 +96,11 @@ func createCluster(setupFnc framework.ManagerSetup, apiFeatureGates ...string) c
 		APIServerFeatureGates: apiFeatureGates,
 	}
 	c.cfg = c.fwk.Init()
-	c.ctx, c.client = c.fwk.RunManager(c.cfg, setupFnc)
+	c.ctx, c.client = c.fwk.SetupClient(c.cfg)
+	// skip the manager setup if setup func is not provided
+	if setupFnc != nil {
```

  Yes , the worker clusters are fully initialized (including the managers) in before suite, the manager's manager is started/stopped per spec.

- **test/integration/multikueue/suite_test.go:237** by [@trasc](https://github.com/trasc) — 2024-09-20T13:13:15Z
```diff
@@ -222,27 +226,26 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 }
 
-func multiclusterSetup(gcInterval time.Duration) {
+var _ = ginkgo.BeforeSuite(func() {
 	var managerFeatureGates []string
 	version, err := versionutil.ParseGeneric(os.Getenv("ENVTEST_K8S_VERSION"))
 	if err != nil || !version.LessThan(versionutil.MustParseSemantic("1.30.0")) {
 		managerFeatureGates = []string{"JobManagedBy=true"}
 	}
 
-	managerTestCluster = createCluster(func(ctx context.Context, mgr manager.Manager) {
-		managerAndMultiKueueSetup(ctx, mgr, gcInterval)
-	}, managerFeatureGates...)
+	// pass nil setup since the manager for the manage cluster is different in some specs.
+	managerTestCluster = createCluster(nil, managerFeatureGates...)
```

  There is no point in restarting the managers in the workers as long no configuration change is needed.

- **test/integration/scheduler/podsready/scheduler_test.go:158** by [@trasc](https://github.com/trasc) — 2024-09-20T13:35:14Z
```diff
@@ -161,9 +155,10 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("checking the first prod workload gets admitted while the second is waiting")
 			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
 			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, prodWl)
```

  This and a couple of other small fixes ware added to solve some flakes observed after switching to cluster reuse.  
  
  For this case, historically the test was passing due to a higher load at the beginning due to envtest initialization.
  Now any of the two can get admitted first and by moving this line we make prodWl is admitted first.

- **test/integration/scheduler/podsready/scheduler_test.go:188** by [@trasc](https://github.com/trasc) — 2024-09-20T13:36:45Z
```diff
@@ -190,7 +185,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, testCQ)).Should(gomega.Succeed())
 			defer func() {
-				gomega.Expect(util.DeleteObject(ctx, k8sClient, testCQ)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, testCQ, true)
```

  This fixes an improper cleanup which used to be unnoticed because the envtest cluster used to be replaced.

- **test/integration/scheduler/podsready/scheduler_test.go:644** by [@trasc](https://github.com/trasc) — 2024-09-20T13:36:55Z
```diff
@@ -650,7 +641,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, testCQ)).Should(gomega.Succeed())
 			defer func() {
-				gomega.Expect(util.DeleteObject(ctx, k8sClient, testCQ)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, testCQ, true)
```

  same

- **test/integration/webhook/jobs/mpijob_webhook_test.go:31** by [@trasc](https://github.com/trasc) — 2024-09-20T13:44:26Z
```diff
@@ -23,24 +23,21 @@ import (
 	apierrors "k8s.io/apimachinery/pkg/api/errors"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 
+	"sigs.k8s.io/kueue/pkg/controller/jobs/mpijob"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
-	"sigs.k8s.io/kueue/test/integration/framework"
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("MPIJob Webhook", func() {
+var _ = ginkgo.Describe("MPIJob Webhook", ginkgo.Ordered, func() {
```

  In our test `ginkgo.Ordered` is label to decorate a container in which we need to be able to call `ginkgo.BeforeAll` (especially in this Spec which has a single `It`, it has no other value),  The `BeforeAll` was moved just for to me this look similar to the other webhook specs.

- **test/integration/framework/framework.go:182** by [@trasc](https://github.com/trasc) — 2024-09-20T13:46:23Z
```diff
@@ -124,46 +127,71 @@ func (f *Framework) SetupClient(cfg *rest.Config) (context.Context, client.Clien
 }
 
 func (f *Framework) StartManager(ctx context.Context, cfg *rest.Config, managerSetup ManagerSetup) {
-	webhookInstallOptions := &f.testEnv.WebhookInstallOptions
-	mgrOpts := manager.Options{
-		Scheme: scheme.Scheme,
-		Metrics: metricsserver.Options{
-			BindAddress: "0", // disable metrics to avoid conflicts between packages.
-		},
-		WebhookServer: webhook.NewServer(
-			webhook.Options{
-				Host:    webhookInstallOptions.LocalServingHost,
-				Port:    webhookInstallOptions.LocalServingPort,
-				CertDir: webhookInstallOptions.LocalServingCertDir,
-			}),
-		Controller: crconfig.Controller{
-			SkipNameValidation: ptr.To(true),
-		},
-	}
-	mgr, err := ctrl.NewManager(cfg, mgrOpts)
-	gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to create manager")
-
-	managerSetup(ctx, mgr)
-
-	go func() {
-		defer ginkgo.GinkgoRecover()
-		err := mgr.Start(ctx)
-		gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to run manager")
-	}()
-
-	if len(f.WebhookPath) > 0 {
-		// wait for the webhook server to get ready
-		dialer := &net.Dialer{Timeout: time.Second}
-		addrPort := fmt.Sprintf("%s:%d", webhookInstallOptions.LocalServingHost, webhookInstallOptions.LocalServingPort)
-		gomega.Eventually(func() error {
-			conn, err := tls.DialWithDialer(dialer, "tcp", addrPort, &tls.Config{InsecureSkipVerify: true})
-			if err != nil {
-				return err
-			}
-			conn.Close()
-			return nil
-		}).Should(gomega.Succeed())
-	}
+	ginkgo.By("starting the manager", func() {
+		webhookInstallOptions := &f.testEnv.WebhookInstallOptions
+		mgrOpts := manager.Options{
+			Scheme: scheme.Scheme,
+			Metrics: metricsserver.Options{
+				BindAddress: "0", // disable metrics to avoid conflicts between packages.
+			},
+			WebhookServer: webhook.NewServer(
+				webhook.Options{
+					Host:    webhookInstallOptions.LocalServingHost,
+					Port:    webhookInstallOptions.LocalServingPort,
+					CertDir: webhookInstallOptions.LocalServingCertDir,
+				}),
+			Controller: crconfig.Controller{
+				SkipNameValidation: ptr.To(true),
+			},
+		}
+		mgr, err := ctrl.NewManager(cfg, mgrOpts)
+		gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to create manager")
+
+		managerCtx, managerCancel := context.WithCancel(ctx)
+		managerSetup(managerCtx, mgr)
+
+		done := make(chan struct{})
+
+		f.managerCancel = managerCancel
+		f.managerDone = done
+
+		go func() {
+			defer close(done)
+			defer ginkgo.GinkgoRecover()
+			err := mgr.Start(managerCtx)
+			gomega.ExpectWithOffset(1, err).NotTo(gomega.HaveOccurred(), "failed to run manager")
+		}()
+
+		if len(f.WebhookPath) > 0 {
+			// wait for the webhook server to get ready
+			dialer := &net.Dialer{Timeout: time.Second}
+			addrPort := fmt.Sprintf("%s:%d", webhookInstallOptions.LocalServingHost, webhookInstallOptions.LocalServingPort)
+			gomega.Eventually(func() error {
+				conn, err := tls.DialWithDialer(dialer, "tcp", addrPort, &tls.Config{InsecureSkipVerify: true})
+				if err != nil {
+					return err
+				}
+				conn.Close()
+				return nil
+			}).Should(gomega.Succeed())
+		}
+	})
+}
+
+func (f *Framework) StopManager(ctx context.Context) {
+	ginkgo.By("stoping the manager", func() {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T14:33:16Z

- **test/integration/scheduler/podsready/scheduler_test.go:158** by [@mimowo](https://github.com/mimowo) — 2024-09-25T14:33:16Z
```diff
@@ -161,9 +155,10 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			ginkgo.By("checking the first prod workload gets admitted while the second is waiting")
 			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
 			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, prodWl)
```

  if these are fixes to flakes, then it makes sense to merge them separately so that we can have the release branch more stable? I see that it didn't fail before (almost never), but the point holds, IIUC, that the code was buggy.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T14:33:39Z

- **test/integration/multikueue/suite_test.go:237** by [@mimowo](https://github.com/mimowo) — 2024-09-25T14:33:39Z
```diff
@@ -222,27 +226,26 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 }
 
-func multiclusterSetup(gcInterval time.Duration) {
+var _ = ginkgo.BeforeSuite(func() {
 	var managerFeatureGates []string
 	version, err := versionutil.ParseGeneric(os.Getenv("ENVTEST_K8S_VERSION"))
 	if err != nil || !version.LessThan(versionutil.MustParseSemantic("1.30.0")) {
 		managerFeatureGates = []string{"JobManagedBy=true"}
 	}
 
-	managerTestCluster = createCluster(func(ctx context.Context, mgr manager.Manager) {
-		managerAndMultiKueueSetup(ctx, mgr, gcInterval)
-	}, managerFeatureGates...)
+	// pass nil setup since the manager for the manage cluster is different in some specs.
+	managerTestCluster = createCluster(nil, managerFeatureGates...)
```

  I see, ok

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T14:41:46Z

- **test/integration/webhook/jobs/mpijob_webhook_test.go:31** by [@mimowo](https://github.com/mimowo) — 2024-09-25T14:41:46Z
```diff
@@ -23,24 +23,21 @@ import (
 	apierrors "k8s.io/apimachinery/pkg/api/errors"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 
+	"sigs.k8s.io/kueue/pkg/controller/jobs/mpijob"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
-	"sigs.k8s.io/kueue/test/integration/framework"
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("MPIJob Webhook", func() {
+var _ = ginkgo.Describe("MPIJob Webhook", ginkgo.Ordered, func() {
```

  sgtm, though I prefer to see drive-by cleanups in a separate PR to reduce distractions. Anyway, I can accept it after I now understand.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T14:42:32Z

- **test/integration/scheduler/podsready/scheduler_test.go:188** by [@mimowo](https://github.com/mimowo) — 2024-09-25T14:42:32Z
```diff
@@ -190,7 +185,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, testCQ)).Should(gomega.Succeed())
 			defer func() {
-				gomega.Expect(util.DeleteObject(ctx, k8sClient, testCQ)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, testCQ, true)
```

  ok

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T14:42:40Z

- **test/integration/multikueue/suite_test.go:101** by [@mimowo](https://github.com/mimowo) — 2024-09-25T14:42:39Z
```diff
@@ -96,7 +96,11 @@ func createCluster(setupFnc framework.ManagerSetup, apiFeatureGates ...string) c
 		APIServerFeatureGates: apiFeatureGates,
 	}
 	c.cfg = c.fwk.Init()
-	c.ctx, c.client = c.fwk.RunManager(c.cfg, setupFnc)
+	c.ctx, c.client = c.fwk.SetupClient(c.cfg)
+	// skip the manager setup if setup func is not provided
+	if setupFnc != nil {
```

  ok
