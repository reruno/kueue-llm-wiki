# PR #3070: Requeue all inadmissible workloads in Cohort Tree

**Summary**: Requeue all inadmissible workloads in Cohort Tree

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3070

**Last updated**: 2024-09-19T13:52:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `hc_qmanager` → `main`
- **Created**: 2024-09-16T14:20:47Z
- **Updated**: 2024-09-19T13:52:12Z
- **Merged**: 2024-09-19T13:52:02Z
- **Closed**: 2024-09-19T13:52:02Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@macsko](https://github.com/macsko)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc), [@macsko](https://github.com/macsko)
- **Changed files**: 4   **+132 / -10**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
Part of #79. We support hierarchical cohorts in the queue package, by requeueing all inadmissible workloads in the Cohort tree upon any events which may allow inadmissible workloads to be scheduled.

#### Special notes for your reviewer:
Once #3066 is resolved, we can add a unit test to make sure that all workloads in tree are moved. Until then, we have coverage via an integration test, and a unit test to cover the cycle-path in the `queue` package

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-16T14:21:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1de85dd16de0a51c413d5f7982193e9ad4f1427d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66ec22e8077ee00008da5002 |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-17T08:02:38Z

/assign @macsko

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T09:55:03Z

> /assign @macsko

thanks for the review

### Comment by [@macsko](https://github.com/macsko) — 2024-09-19T10:19:05Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-19T10:19:10Z

LGTM label has been added.  <details>Git tree hash: aabae6d26b95658adaf85f0c772acc5dcfb32d10</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T11:16:39Z

/assign @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-19T13:10:12Z

LGTM label has been added.  <details>Git tree hash: 5ca7c01550fb630e8accfb5746eefab416e18cc1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-19T13:10:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3070#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3070#pullrequestreview-2315497351" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T13:12:29Z

> /lgtm /approve Thanks!

Thanks for review! Sorry, updated after you lgtm'd, to make the test name consistent...

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-19T13:16:38Z

> Thanks for review! Sorry, updated after you lgtm'd, to make the test name consistent...

Cool, still
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-19T13:16:44Z

LGTM label has been added.  <details>Git tree hash: c5e969606b5eb7da760325e041d3eed126317f0a</details>

## Reviews

### Review by [@macsko](https://github.com/macsko) (commented) — 2024-09-19T07:45:37Z

- **pkg/queue/manager.go:476** by [@macsko](https://github.com/macsko) — 2024-09-19T06:57:26Z
```diff
@@ -452,18 +453,42 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
+	log := ctrl.LoggerFrom(ctx)
+
+	if m.hm.CycleChecker.HasCycle(cohort) {
+		log.V(2).Info("attempted to move workloads from Cohort which has cycle", "cohort", cohort.GetName())
```

  Can you start the messages with a capital letter?

- **pkg/queue/manager.go:480** by [@macsko](https://github.com/macsko) — 2024-09-19T06:58:03Z
```diff
@@ -452,18 +453,42 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
+	log := ctrl.LoggerFrom(ctx)
+
+	if m.hm.CycleChecker.HasCycle(cohort) {
+		log.V(2).Info("attempted to move workloads from Cohort which has cycle", "cohort", cohort.GetName())
+		return false
+	}
+	root := cohort.getRootUnsafe()
+	log.V(2).Info("attempting to move workloads", "cohort", cohort.Name, "cohort", root.Name)
```

  "cohort" key is duplicated in the log.

- **pkg/queue/manager_test.go:232** by [@macsko](https://github.com/macsko) — 2024-09-19T07:45:17Z
```diff
@@ -195,6 +196,40 @@ func TestUpdateClusterQueue(t *testing.T) {
 	}
 }
 
+func TestCohortCycle(t *testing.T) {
+	cohorts := []*kueuealpha.Cohort{
+		utiltesting.MakeCohort("cohort-a").Parent("cohort-b").Obj(),
+		utiltesting.MakeCohort("cohort-b").Parent("cohort-c").Obj(),
+		utiltesting.MakeCohort("cohort-c").Parent("cohort-a").Obj(),
+	}
+	cq := utiltesting.MakeClusterQueue("cq1").Cohort("cohort-a").Obj()
+	lq := utiltesting.MakeLocalQueue("foo", defaultNamespace).ClusterQueue("cq1").Obj()
+	wl := utiltesting.MakeWorkload("a", defaultNamespace).Queue("foo").Creation(time.Now()).Obj()
+	// Setup.
+	ctx := context.Background()
+	cl := utiltesting.NewFakeClient(
+		&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: defaultNamespace}},
+	)
+	manager := NewManager(cl, nil)
+	for _, cohort := range cohorts {
+		manager.AddOrUpdateCohort(ctx, cohort)
+	}
+	if err := manager.AddClusterQueue(ctx, cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
+	}
+	if err := manager.AddLocalQueue(ctx, lq); err != nil {
+		t.Fatalf("Failed adding queue %s: %v", lq.Name, err)
+	}
+	if err := cl.Create(ctx, wl); err != nil {
+		t.Fatalf("Failed adding workload to client: %v", err)
+	}
+	manager.RequeueWorkload(ctx, workload.NewInfo(wl), RequeueReasonGeneric)
+
+	if manager.moveWorkloadsCohort(ctx, manager.hm.Cohorts["cohort-a"]) {
```

  Can you add some context to this part of the test to emphasize this method checks for cycle?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T09:54:17Z

- **pkg/queue/manager.go:476** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T09:54:17Z
```diff
@@ -452,18 +453,42 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
+	log := ctrl.LoggerFrom(ctx)
+
+	if m.hm.CycleChecker.HasCycle(cohort) {
+		log.V(2).Info("attempted to move workloads from Cohort which has cycle", "cohort", cohort.GetName())
```

  Done.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T09:54:36Z

- **pkg/queue/manager.go:480** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T09:54:36Z
```diff
@@ -452,18 +453,42 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise a Cohort updating may result in an infinite loop.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
+	log := ctrl.LoggerFrom(ctx)
+
+	if m.hm.CycleChecker.HasCycle(cohort) {
+		log.V(2).Info("attempted to move workloads from Cohort which has cycle", "cohort", cohort.GetName())
+		return false
+	}
+	root := cohort.getRootUnsafe()
+	log.V(2).Info("attempting to move workloads", "cohort", cohort.Name, "cohort", root.Name)
```

  Good catch. This was supposed to be "root".

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T09:54:52Z

- **pkg/queue/manager_test.go:232** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T09:54:52Z
```diff
@@ -195,6 +196,40 @@ func TestUpdateClusterQueue(t *testing.T) {
 	}
 }
 
+func TestCohortCycle(t *testing.T) {
+	cohorts := []*kueuealpha.Cohort{
+		utiltesting.MakeCohort("cohort-a").Parent("cohort-b").Obj(),
+		utiltesting.MakeCohort("cohort-b").Parent("cohort-c").Obj(),
+		utiltesting.MakeCohort("cohort-c").Parent("cohort-a").Obj(),
+	}
+	cq := utiltesting.MakeClusterQueue("cq1").Cohort("cohort-a").Obj()
+	lq := utiltesting.MakeLocalQueue("foo", defaultNamespace).ClusterQueue("cq1").Obj()
+	wl := utiltesting.MakeWorkload("a", defaultNamespace).Queue("foo").Creation(time.Now()).Obj()
+	// Setup.
+	ctx := context.Background()
+	cl := utiltesting.NewFakeClient(
+		&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: defaultNamespace}},
+	)
+	manager := NewManager(cl, nil)
+	for _, cohort := range cohorts {
+		manager.AddOrUpdateCohort(ctx, cohort)
+	}
+	if err := manager.AddClusterQueue(ctx, cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
+	}
+	if err := manager.AddLocalQueue(ctx, lq); err != nil {
+		t.Fatalf("Failed adding queue %s: %v", lq.Name, err)
+	}
+	if err := cl.Create(ctx, wl); err != nil {
+		t.Fatalf("Failed adding workload to client: %v", err)
+	}
+	manager.RequeueWorkload(ctx, workload.NewInfo(wl), RequeueReasonGeneric)
+
+	if manager.moveWorkloadsCohort(ctx, manager.hm.Cohorts["cohort-a"]) {
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T11:59:16Z

LGTM, just naming nits

- **pkg/queue/manager.go:474** by [@mimowo](https://github.com/mimowo) — 2024-09-19T11:46:29Z
```diff
@@ -452,18 +453,44 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
```

  ```suggestion
  func (m *Manager) queueInadmissibleWorkloadsForCohort(ctx context.Context, cohort *cohort) bool {
  ```
  non-blocking nit (as this is pre-existing name), but I think "moveWorkloadsCohort" is too ambigous

- **pkg/queue/manager.go:486** by [@mimowo](https://github.com/mimowo) — 2024-09-19T11:48:46Z
```diff
@@ -452,18 +453,44 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
+	log := ctrl.LoggerFrom(ctx)
+
+	if m.hm.CycleChecker.HasCycle(cohort) {
+		log.V(2).Info("Attempted to move workloads from Cohort which has cycle", "cohort", cohort.GetName())
+		return false
+	}
+	root := cohort.getRootUnsafe()
+	log.V(2).Info("Attempting to move workloads", "cohort", cohort.Name, "root", root.Name)
+	return moveWorkloadsCohortHelper(ctx, m, root)
+}
+
+func moveWorkloadsCohortHelper(ctx context.Context, m *Manager, cohort *cohort) bool {
```

  ```suggestion
  func queueInadmissibleWorkloadsInCohortSubtree(ctx context.Context, m *Manager, cohort *cohort) bool {
  ```

- **pkg/queue/manager_test.go:199** by [@mimowo](https://github.com/mimowo) — 2024-09-19T11:54:15Z
```diff
@@ -195,6 +196,44 @@ func TestUpdateClusterQueue(t *testing.T) {
 	}
 }
 
+func TestCohortCycle(t *testing.T) {
```

  ```suggestion
  func TestMoveWorkloadsCohort(t *testing.T) {
  ```
  nit, or `TestQueueInadmissibleWorkloadsForCohort`

- **pkg/queue/cohort.go:40** by [@mimowo](https://github.com/mimowo) — 2024-09-19T11:57:15Z
```diff
@@ -35,3 +35,14 @@ func newCohort(name string) *cohort {
 func (c *cohort) GetName() string {
 	return c.Name
 }
+
+func (c *cohort) CCParent() hierarchy.CycleCheckable {
```

  nit: Is this used? Don't see it used on the diff.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T12:56:37Z

- **pkg/queue/manager.go:474** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T12:56:36Z
```diff
@@ -452,18 +453,44 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
```

  Decided in another PR to keep name short - it was hard to keep name short and unambiguous, as the function for CQs does almost the same thing, but starting at the CQ
  
  See https://github.com/kubernetes-sigs/kueue/pull/2971#discussion_r1745396750

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T13:02:17Z

- **pkg/queue/manager_test.go:199** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T13:02:17Z
```diff
@@ -195,6 +196,44 @@ func TestUpdateClusterQueue(t *testing.T) {
 	}
 }
 
+func TestCohortCycle(t *testing.T) {
```

  Renamed to TestMoveWorkloadsCohortCycle, since the important part of this test is that this function doesn't misbehave when there's a cycle.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T13:02:19Z

- **pkg/queue/cohort.go:40** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T13:02:19Z
```diff
@@ -35,3 +35,14 @@ func newCohort(name string) *cohort {
 func (c *cohort) GetName() string {
 	return c.Name
 }
+
+func (c *cohort) CCParent() hierarchy.CycleCheckable {
```

  Yes, it satisfies the interface so we can use the cycle-checker. Added a comment.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T13:06:51Z

- **pkg/queue/manager.go:474** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T13:06:51Z
```diff
@@ -452,18 +453,44 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
```

  Renamed slightly - `s/move/requeue/`

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-09-19T13:07:16Z

- **pkg/queue/manager.go:486** by [@gabesaba](https://github.com/gabesaba) — 2024-09-19T13:07:16Z
```diff
@@ -452,18 +453,44 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
+	log := ctrl.LoggerFrom(ctx)
+
+	if m.hm.CycleChecker.HasCycle(cohort) {
+		log.V(2).Info("Attempted to move workloads from Cohort which has cycle", "cohort", cohort.GetName())
+		return false
+	}
+	root := cohort.getRootUnsafe()
+	log.V(2).Info("Attempting to move workloads", "cohort", cohort.Name, "root", root.Name)
+	return moveWorkloadsCohortHelper(ctx, m, root)
+}
+
+func moveWorkloadsCohortHelper(ctx context.Context, m *Manager, cohort *cohort) bool {
```

  how about `requeueWorkloadsCohortSubtree`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T13:08:48Z

- **pkg/queue/manager.go:486** by [@mimowo](https://github.com/mimowo) — 2024-09-19T13:08:48Z
```diff
@@ -452,18 +453,44 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
+	log := ctrl.LoggerFrom(ctx)
+
+	if m.hm.CycleChecker.HasCycle(cohort) {
+		log.V(2).Info("Attempted to move workloads from Cohort which has cycle", "cohort", cohort.GetName())
+		return false
+	}
+	root := cohort.getRootUnsafe()
+	log.V(2).Info("Attempting to move workloads", "cohort", cohort.Name, "root", root.Name)
+	return moveWorkloadsCohortHelper(ctx, m, root)
+}
+
+func moveWorkloadsCohortHelper(ctx context.Context, m *Manager, cohort *cohort) bool {
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T13:09:03Z

- **pkg/queue/manager.go:474** by [@mimowo](https://github.com/mimowo) — 2024-09-19T13:09:03Z
```diff
@@ -452,18 +453,44 @@ func (m *Manager) QueueInadmissibleWorkloads(ctx context.Context, cqNames sets.S
 // 2. add events of any cluster queue in the cohort.
 // 3. update events of any cluster queue in the cohort.
 // 4. update of cohort.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCQ(ctx context.Context, cq *ClusterQueue) bool {
 	if cq.HasParent() {
 		return m.moveWorkloadsCohort(ctx, cq.Parent())
 	}
 	return cq.QueueInadmissibleWorkloads(ctx, m.client)
 }
 
+// moveWorkloadsCohorts checks for a cycle, the moves all inadmissible
+// workloads in the Cohort tree. If a cycle exists, or no workloads were
+// moved, it returns false.
+//
+// WARNING: must hold a read-lock on the manager when calling,
+// or otherwise risk encountering an infinite loop if a Cohort
+// cycle is introduced.
 func (m *Manager) moveWorkloadsCohort(ctx context.Context, cohort *cohort) bool {
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-19T13:10:07Z

/lgtm
/approve
Thanks!
