# PR #3101: Update KEP-2076: Kueuectl.

**Summary**: Update KEP-2076: Kueuectl.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3101

**Last updated**: 2024-09-23T11:33:26Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/update-KEP-2076-Kueuectl` → `main`
- **Created**: 2024-09-19T15:44:04Z
- **Updated**: 2024-09-23T11:33:26Z
- **Merged**: 2024-09-23T10:04:06Z
- **Closed**: 2024-09-23T10:04:06Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mwielgus](https://github.com/mwielgus)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mwielgus](https://github.com/mwielgus)
- **Changed files**: 1   **+41 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Update "KEP-2076: Kueuectl":
- Add separate delete workload command, 
- Remove delete workload command from pass-through.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #2975

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-19T15:44:21Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3e7128a1fb56739f83be0df61fb9cc9a10f20638 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66f1061f1f9206000886bbd9 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-19T15:44:28Z

/cc @alculquicondor @mwielgus

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-23T06:41:47Z

/approve
Leaving lgtm to @mwielgus

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-23T06:41:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3101#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3101#issuecomment-2367344290" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mwielgus](https://github.com/mwielgus) — 2024-09-23T09:45:15Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-23T09:45:21Z

LGTM label has been added.  <details>Git tree hash: 4fe6d23847b5c6e162c47b767db0a1b4062adfbe</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-19T19:19:03Z

- **keps/2076-kueuectl/README.md:334** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-19T19:18:55Z
```diff
@@ -330,6 +331,16 @@ kueuectl resume workload name --namespace=ns
 Output:
 None.
 
+### Delete Workload
```

  Adding context and motivations for dedicated commands would be helpful to understand this command objective.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T02:51:52Z

- **keps/2076-kueuectl/README.md:334** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T02:51:52Z
```diff
@@ -330,6 +331,16 @@ kueuectl resume workload name --namespace=ns
 Output:
 None.
 
+### Delete Workload
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T09:39:25Z

- **keps/2076-kueuectl/README.md:352** by [@mimowo](https://github.com/mimowo) — 2024-09-20T09:39:25Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  Does it need to delete the workload object? I think deleting the Job is enough - the workload object will be deleted by Kueue by this code:

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T09:42:14Z

- **keps/2076-kueuectl/README.md:363** by [@mimowo](https://github.com/mimowo) — 2024-09-20T09:42:13Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
+```
+
+Flags:
+```
+-y, --yes             Confirm the deletion of the Workload and its corresponding Job.
```

  ```suggestion
  -y, --yes             Automatic yes to the prompt for deleting the Workload.
  ```
  Inspired by the -y from apt-get help https://linux.die.net/man/8/apt-get

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T09:43:31Z

- **keps/2076-kueuectl/README.md:361** by [@mimowo](https://github.com/mimowo) — 2024-09-20T09:43:30Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
```

  how a user passes the namespace, `-n`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T09:43:51Z

- **keps/2076-kueuectl/README.md:371** by [@mimowo](https://github.com/mimowo) — 2024-09-20T09:43:51Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
+```
+
+Flags:
+```
+-y, --yes             Confirm the deletion of the Workload and its corresponding Job.
+    --all             Delete all Workloads, in the specified namespace.
+    --all-namespaces  Using with --all flag. Delete all Workloads, in all namespaces.
+```
+
+Approval Output:
+```
+This operation will also delete:
+  - jobs.batch/name associated with the name workload
```

  ```suggestion
    - jobs.batch/name associated with the namespace/name workload
  ```
  I guess we should also display the namespace to avoid confusion?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T13:11:31Z

- **keps/2076-kueuectl/README.md:352** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T13:11:31Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  Yes, we should delete the Workload if there is no Job associated with it. For example, this would be happens if a user created the Workload manually.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T13:17:50Z

- **keps/2076-kueuectl/README.md:361** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T13:17:50Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
```

  The `-n` and `--namespaces`  flags is exist by default for each command. But I will add it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T13:19:43Z

- **keps/2076-kueuectl/README.md:352** by [@mimowo](https://github.com/mimowo) — 2024-09-20T13:19:43Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  Ok, but this is a corner case. If the Job exists, no point deleting the workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T13:22:17Z

- **keps/2076-kueuectl/README.md:352** by [@mimowo](https://github.com/mimowo) — 2024-09-20T13:22:17Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  hm, or maybe there is a point - to make the operation synchronous - so that the workload is gone if the `kueuectl workload list` command is repeated very fast. If this is the motivation, then please state it in the KEP, otherwise we can just say in the help that the operation is async and may take a while.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-20T13:59:13Z

- **keps/2076-kueuectl/README.md:352** by [@trasc](https://github.com/trasc) — 2024-09-20T13:59:13Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  (prebuilt workloads are orphan in multikueue worker clusters until they get admitted)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-20T14:06:30Z

- **keps/2076-kueuectl/README.md:352** by [@trasc](https://github.com/trasc) — 2024-09-20T14:06:30Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

   I think we should better define what "corresponding Job(s)" are. 
  And maybe think about:
  - how we ca address what I mentioned in https://github.com/kubernetes-sigs/kueue/pull/2992#discussion_r1765051596
  - maybe mention the the effect of deleting parent object on workload sibling objects

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T14:11:37Z

- **keps/2076-kueuectl/README.md:377** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T14:11:37Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
+```
+
+Flags:
+```
+-y, --yes             Confirm the deletion of the Workload and its corresponding Job.
+    --all             Delete all Workloads, in the specified namespace.
+    --all-namespaces  Using with --all flag. Delete all Workloads, in all namespaces.
+```
+
+Approval Output:
+```
+This operation will also delete:
+  - jobs.batch/name associated with the name workload
+Do you want to proceed (y/n)?
+```
+
+Deletion Process Output:
+```
+workload.kueue.x-k8s.io/name deleted
```

  ```suggestion
  jobs.batch/name deleted
  ```
  
  @mimowo If we deleting Workloads asynchronously, should we display here jobs? WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T14:13:32Z

- **keps/2076-kueuectl/README.md:377** by [@mimowo](https://github.com/mimowo) — 2024-09-20T14:13:32Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
+```
+
+Flags:
+```
+-y, --yes             Confirm the deletion of the Workload and its corresponding Job.
+    --all             Delete all Workloads, in the specified namespace.
+    --all-namespaces  Using with --all flag. Delete all Workloads, in all namespaces.
+```
+
+Approval Output:
+```
+This operation will also delete:
+  - jobs.batch/name associated with the name workload
+Do you want to proceed (y/n)?
+```
+
+Deletion Process Output:
+```
+workload.kueue.x-k8s.io/name deleted
```

  I think listing Job object is more important. We may additionally display the workload object name too.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T14:26:28Z

- **keps/2076-kueuectl/README.md:352** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T14:26:27Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  > how we ca address what I mentioned in https://github.com/kubernetes-sigs/kueue/pull/2992#discussion_r1765051596
  
  We've just looked at this case with @mimowo and found that the Deployment recreates the Pod and it will have a different name and also the Workload name too.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-20T14:38:16Z

- **keps/2076-kueuectl/README.md:352** by [@trasc](https://github.com/trasc) — 2024-09-20T14:38:16Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  I don't understand. The Deployment is just an example.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T14:41:28Z

- **keps/2076-kueuectl/README.md:352** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T14:41:28Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  The Workload associated with Pod and we delete this Pod. So it's ok here. A user may want to delete the whole `Deployement`, but for that, we need to have a different workload. But I also need to check [FluxMiniclusters](https://kueue.sigs.k8s.io/docs/tasks/run/flux_miniclusters/).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T14:47:23Z

- **keps/2076-kueuectl/README.md:371** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T14:47:22Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
+```
+
+Flags:
+```
+-y, --yes             Confirm the deletion of the Workload and its corresponding Job.
+    --all             Delete all Workloads, in the specified namespace.
+    --all-namespaces  Using with --all flag. Delete all Workloads, in all namespaces.
+```
+
+Approval Output:
+```
+This operation will also delete:
+  - jobs.batch/name associated with the name workload
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T14:47:28Z

- **keps/2076-kueuectl/README.md:361** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T14:47:28Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T14:47:41Z

- **keps/2076-kueuectl/README.md:363** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T14:47:41Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
+owns any Jobs, the command will prompt for deletion approval, displaying the Jobs 
+that will be deleted along with the Workload.
+
+Format:
+```
+kueuectl delete workload name
+```
+
+Flags:
+```
+-y, --yes             Confirm the deletion of the Workload and its corresponding Job.
```

  Fixed

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-20T14:55:41Z

- **keps/2076-kueuectl/README.md:352** by [@trasc](https://github.com/trasc) — 2024-09-20T14:55:41Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  Even if the names are slightly different in case of replicasets/deployments, you end up deleting two objects and other two (having the same purpose) are created.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-20T15:09:22Z

- **keps/2076-kueuectl/README.md:352** by [@mimowo](https://github.com/mimowo) — 2024-09-20T15:09:22Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  I think this is ok for now, as the workload requested to be deleted by the user is gone, which was the main confusing factor in the original issue.
  
  The support for Deployments is in initial phase.
  
  Long term, we discuss introducing a concept of a workload fragment. Then workloads for Pods will be considered workload fragments, and we may display a parent workload which aggregates the fragments. Then deleting the parent workload would delete the Deployment instance, but I believe this work can be deferred for later, but cc @mwielgus in case we want to prioritize it more as part of the fix.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-20T15:21:38Z

- **keps/2076-kueuectl/README.md:352** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-20T15:21:38Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  > But I also need to check [FluxMiniclusters](https://kueue.sigs.k8s.io/docs/tasks/run/flux_miniclusters/).
  
  Just to confirm, the situation in `FluxMiniclusters` is the same. It removes the Job associated with a Workload but then recreates both a new Job and a new Workload.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-22T06:57:04Z

- **keps/2076-kueuectl/README.md:352** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-22T06:57:04Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  Done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-23T05:29:25Z

- **keps/2076-kueuectl/README.md:352** by [@trasc](https://github.com/trasc) — 2024-09-23T05:29:24Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  > Just to confirm, the situation in `FluxMiniclusters` is the same. It removes the Job associated with a Workload but then recreates both a new Job and a new Workload.
  
  With the same name ore a slightly different one?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-23T05:36:43Z

- **keps/2076-kueuectl/README.md:350** by [@trasc](https://github.com/trasc) — 2024-09-23T05:36:42Z
```diff
@@ -339,7 +340,45 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
```

  ```suggestion
  The pass-through delete workload command deletes the Workload API object, which is later
  recreated leading to its job to be re-queued. This can confuse users, as the recreated Workload
  has the same name giving the impression that the `delete workload` command did not succeed.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-23T06:06:12Z

- **keps/2076-kueuectl/README.md:352** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-23T06:06:12Z
```diff
@@ -339,7 +340,42 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
+
+This command will delete the Workload and its corresponding Job(s). If the Workload 
```

  With different one.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-23T06:10:03Z

- **keps/2076-kueuectl/README.md:350** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-23T06:10:03Z
```diff
@@ -339,7 +340,45 @@ so that the users won't have to remember to switch the command to kubectl.
 * `describe workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `edit workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
 * `patch workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
-* `delete workload|wl|clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+* `delete clusterqueue|cq|localqueue|lq|resourceflavor|rf`
+
+### Delete Workload
+
+Motivation:
+The pass-through delete workload command doesn't actually delete the Workload but 
+instead requeues it. This can confuse users, as the Workload remains listed even 
+after they attempt to delete it, due to its recreation.
```

  Fixed
