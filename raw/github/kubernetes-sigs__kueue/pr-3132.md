# PR #3132: Drop remove operations from webhook patches

**Summary**: Drop remove operations from webhook patches

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3132

**Last updated**: 2024-10-29T11:45:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `fix-webhooks` → `main`
- **Created**: 2024-09-24T18:00:16Z
- **Updated**: 2024-10-29T11:45:51Z
- **Merged**: 2024-09-27T17:34:01Z
- **Closed**: 2024-09-27T17:34:01Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 12   **+476 / -45**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

Prevent job webhooks from dropping fields for newer API fields when Kueue libraries are behind the latest released CRDs.

This was possible by reusing controller-runtime's handler for CustomDefaulter, by dropping the "remove" operations from the jsondiffs.

I had to duplicate controller-runtime's webhook builder, because there is no way to introduce a custom handler in it. I have an open PR to upstream this ability https://github.com/kubernetes-sigs/controller-runtime/pull/2932

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2878

#### Special notes for your reviewer:

Just to expand on why I decided to go with this approach of dropping the remove operations:
Another option I considered was for every job to return jsonpatches in the Suspend operation. But then it becomes quite hard to explain how to do your own, if you aren't already familiar with jsonpatches. It's much simpler to say "just edit the object". Then I was thinking how can we produce safe patches generically, starting from an edited object. One option would have been to do something similar to https://github.com/kubernetes-sigs/controller-runtime/pull/2931, but that approach might introduce other bugs, as explained in https://github.com/kubernetes-sigs/controller-runtime/pull/2931#discussion_r1728367254

So ultimately I decided to exploit the fact that our webhooks simply don't drop fields.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Prevent job webhooks from dropping fields for newer API fields when Kueue libraries are behind the latest released CRDs.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-24T18:00:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c2f6eca75229958e8dec02d8af8ef8e454afe1d6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66f4536bd1fdc300070ab33b |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T00:55:55Z

/cc

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-27T16:26:38Z

/lgtm
/approve
/hold
In case @tenzen-y has more comments. @alculquicondor please follow up with a note about the recommendation for registering webhooks for integrations.

Also, @tenzen-y  @alculquicondor  WDYT about cherry-picking to 0.8?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-27T16:26:44Z

LGTM label has been added.  <details>Git tree hash: a5685fbb976d32e79114b24e05592ed8622f3625</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-27T16:35:10Z

re cherry-picking:
This PR depends on https://github.com/kubernetes-sigs/kueue/pull/3132, so maybe a bit too much for a clean merge?

I think we are relatively safe given that we are using the latest libraries in 0.8 already. And people who might be thinking of upgrading k8s or kuberay are more likely to also stay up to date with Kueue.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T16:40:28Z

> re cherry-picking: This PR depends on #3132, so maybe a bit too much for a clean merge?
> 
> I think we are relatively safe given that we are using the latest libraries in 0.8 already. And people who might be thinking of upgrading k8s or kuberay are more likely to also stay up to date with Kueue.

Indeed, I had some discussions about cherry-picking there: https://github.com/kubernetes-sigs/kueue/pull/3132#discussion_r1775672986

So, I'm ok without cherry-picking.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-27T16:54:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3132#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3132#issuecomment-2379652453" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3132#pullrequestreview-2334215671" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T17:04:56Z

> go: github.com/mikefarah/yq/v4@v4.44.3: Get "https://proxy.golang.org/github.com/mikefarah/yq/v4/@v/v4.44.3.info": dial tcp 142.250.191.113:443: i/o timeout

https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/3132/pull-kueue-test-multikueue-e2e-main/1839710600670744576

This seems to be a networking connectivity error.

/test pull-kueue-test-multikueue-e2e-main

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-27T17:15:05Z

I think https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/3132/pull-kueue-test-integration-main/1839710600372948992 is a flake

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T17:15:40Z

Due to https://github.com/kubernetes-sigs/kueue/issues/3144

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T17:15:48Z

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-29T11:45:13Z

/cherry-pick release-0.8

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-10-29T11:45:50Z

@mbobrovskyi: #3132 failed to apply on top of branch "release-0.8":
```
Applying: Drop remove operations from webhook patches
Using index info to reconstruct a base tree...
M	go.mod
A	pkg/controller/jobframework/noop_webhook.go
A	pkg/controller/jobs/deployment/deployment_webhook.go
M	pkg/controller/jobs/job/job_webhook.go
M	pkg/controller/jobs/pod/pod_webhook.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobs/pod/pod_webhook.go
Auto-merging pkg/controller/jobs/noop/noop_webhook.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/noop/noop_webhook.go
Auto-merging pkg/controller/jobs/job/job_webhook.go
CONFLICT (modify/delete): pkg/controller/jobs/deployment/deployment_webhook.go deleted in HEAD and modified in Drop remove operations from webhook patches. Version Drop remove operations from webhook patches of pkg/controller/jobs/deployment/deployment_webhook.go left in tree.
Auto-merging go.mod
CONFLICT (content): Merge conflict in go.mod
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Drop remove operations from webhook patches

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3132#issuecomment-2443985789):

>/cherry-pick release-0.8


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T12:03:34Z

This is just confirmation. Have you ever seen any dropping schemes in the reconcilers/controllers?

For sure, I have seen your comment mentioning the investigations: https://github.com/kubernetes-sigs/kueue/issues/2878#issuecomment-2305438068

- **pkg/controller/jobframework/webhook/builder.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T11:42:34Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
```

  Does the exact file name is `/pkg/builder/webhook.go`?

- **pkg/controller/jobframework/webhook/builder.go:45** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T11:45:41Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
+// with some modifications to get full control of the construction of patches.
+
+// Builder builds a Webhook.
+type Builder struct {
+	apiType         runtime.Object
+	mutationHandler admission.Handler
```

  Is this alternative of CustomDefaulter?

- **pkg/controller/jobframework/webhook/builder.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T11:48:31Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
+// with some modifications to get full control of the construction of patches.
+
+// Builder builds a Webhook.
+type Builder struct {
+	apiType         runtime.Object
+	mutationHandler admission.Handler
+	customValidator admission.CustomValidator
+	gvk             schema.GroupVersionKind
+	mgr             manager.Manager
+	config          *rest.Config
+	recoverPanic    bool
+	logConstructor  func(base logr.Logger, req *admission.Request) logr.Logger
+}
+
+// ManagedBy returns a new webhook builder.
+func ManagedBy(m manager.Manager) *Builder {
+	return &Builder{mgr: m}
+}
+
+// For takes a runtime.Object which should be a CR.
+func (blder *Builder) For(apiType runtime.Object) *Builder {
+	blder.apiType = apiType
+	return blder
+}
+
+// WithMutationHandler takes an admission.Handler inteface, a MutationgWebhook will be wired for this type.
+func (blder *Builder) WithMutationHandler(handler admission.Handler) *Builder {
+	blder.mutationHandler = handler
+	return blder
+}
+
+// WithValidator takes a admission.CustomValidator interface, a ValidatingWebhook will be wired for this type.
+func (blder *Builder) WithValidator(validator admission.CustomValidator) *Builder {
+	blder.customValidator = validator
+	return blder
+}
+
+// WithLogConstructor overrides the webhook's LogConstructor.
+func (blder *Builder) WithLogConstructor(logConstructor func(base logr.Logger, req *admission.Request) logr.Logger) *Builder {
+	blder.logConstructor = logConstructor
+	return blder
+}
+
+// RecoverPanic indicates whether panics caused by the webhook should be recovered.
+func (blder *Builder) RecoverPanic() *Builder {
+	blder.recoverPanic = true
+	return blder
+}
+
+// Complete builds the webhook.
+func (blder *Builder) Complete() error {
+	// Set the Config
+	blder.loadRestConfig()
+
+	// Configure the default LogConstructor
+	blder.setLogConstructor()
+
+	// Set the Webhook if needed
+	return blder.registerWebhooks()
+}
+
+func (blder *Builder) loadRestConfig() {
+	if blder.config == nil {
+		blder.config = blder.mgr.GetConfig()
+	}
+}
+
+func (blder *Builder) setLogConstructor() {
+	if blder.logConstructor == nil {
+		blder.logConstructor = func(base logr.Logger, req *admission.Request) logr.Logger {
+			log := base.WithValues(
+				"webhookGroup", blder.gvk.Group,
+				"webhookKind", blder.gvk.Kind,
+			)
+			if req != nil {
+				return log.WithValues(
+					blder.gvk.Kind, klog.KRef(req.Namespace, req.Name),
+					"namespace", req.Namespace, "name", req.Name,
+					"resource", req.Resource, "user", req.UserInfo.Username,
+					"requestID", req.UID,
+				)
+			}
+			return log
+		}
+	}
+}
+
+func (blder *Builder) registerWebhooks() error {
+	typ, err := blder.getType()
+	if err != nil {
+		return err
+	}
+
+	blder.gvk, err = apiutil.GVKForObject(typ, blder.mgr.GetScheme())
+	if err != nil {
+		return err
+	}
+
+	// Register webhook(s) for type
+	blder.registerDefaultingWebhook()
+	blder.registerValidatingWebhook()
+
+	err = blder.registerConversionWebhook()
+	if err != nil {
+		return err
+	}
+	return nil
```

  ```suggestion
  	return blder.err
  ```
  
  What is motivation using the custom logic for error handling instead of contoller-runtime approach?
  I'm wondering if we can just copy and paste the controller-runtime codes as much as possible.
  WDYT?
  
  https://github.com/kubernetes-sigs/controller-runtime/blob/236e448db12c423c16c31485a8ef20313a6880da/pkg/builder/webhook.go#L150

- **pkg/controller/jobframework/webhook/builder.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T12:00:08Z
  Could we mention that we will remove this original webhook builders once https://github.com/kubernetes-sigs/controller-runtime/pull/2932 is released in the controller-runtime repository?
  
  The comment would be helpful for developers who implement custom Jobs queuing controllers.
  Additionally, the comment can encourage not too much depending on these custom builders.
  
  Thoughts?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T12:30:25Z

- **pkg/controller/jobframework/webhook/builder.go:37** by [@mimowo](https://github.com/mimowo) — 2024-09-25T12:28:34Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
```

  I would suggest to minimize the changes which aren't required, so that in the future we don't diverge too much. 
  Some changes that look extra: rename of WebhookBuilder -> Builder, making recoverPanic as bool from *bool.

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@mimowo](https://github.com/mimowo) — 2024-09-25T12:30:01Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  I'm slightly worried that this assumption may change and we may not notice. Maybe some warning to draw attention?
  I'm also wondering if this is safe in case of webhooks delivered by users for external Jobs?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T13:44:25Z

- **pkg/controller/jobframework/webhook/builder.go:144** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T13:44:25Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
+// with some modifications to get full control of the construction of patches.
+
+// Builder builds a Webhook.
+type Builder struct {
+	apiType         runtime.Object
+	mutationHandler admission.Handler
+	customValidator admission.CustomValidator
+	gvk             schema.GroupVersionKind
+	mgr             manager.Manager
+	config          *rest.Config
+	recoverPanic    bool
+	logConstructor  func(base logr.Logger, req *admission.Request) logr.Logger
+}
+
+// ManagedBy returns a new webhook builder.
+func ManagedBy(m manager.Manager) *Builder {
+	return &Builder{mgr: m}
+}
+
+// For takes a runtime.Object which should be a CR.
+func (blder *Builder) For(apiType runtime.Object) *Builder {
+	blder.apiType = apiType
+	return blder
+}
+
+// WithMutationHandler takes an admission.Handler inteface, a MutationgWebhook will be wired for this type.
+func (blder *Builder) WithMutationHandler(handler admission.Handler) *Builder {
+	blder.mutationHandler = handler
+	return blder
+}
+
+// WithValidator takes a admission.CustomValidator interface, a ValidatingWebhook will be wired for this type.
+func (blder *Builder) WithValidator(validator admission.CustomValidator) *Builder {
+	blder.customValidator = validator
+	return blder
+}
+
+// WithLogConstructor overrides the webhook's LogConstructor.
+func (blder *Builder) WithLogConstructor(logConstructor func(base logr.Logger, req *admission.Request) logr.Logger) *Builder {
+	blder.logConstructor = logConstructor
+	return blder
+}
+
+// RecoverPanic indicates whether panics caused by the webhook should be recovered.
+func (blder *Builder) RecoverPanic() *Builder {
+	blder.recoverPanic = true
+	return blder
+}
+
+// Complete builds the webhook.
+func (blder *Builder) Complete() error {
+	// Set the Config
+	blder.loadRestConfig()
+
+	// Configure the default LogConstructor
+	blder.setLogConstructor()
+
+	// Set the Webhook if needed
+	return blder.registerWebhooks()
+}
+
+func (blder *Builder) loadRestConfig() {
+	if blder.config == nil {
+		blder.config = blder.mgr.GetConfig()
+	}
+}
+
+func (blder *Builder) setLogConstructor() {
+	if blder.logConstructor == nil {
+		blder.logConstructor = func(base logr.Logger, req *admission.Request) logr.Logger {
+			log := base.WithValues(
+				"webhookGroup", blder.gvk.Group,
+				"webhookKind", blder.gvk.Kind,
+			)
+			if req != nil {
+				return log.WithValues(
+					blder.gvk.Kind, klog.KRef(req.Namespace, req.Name),
+					"namespace", req.Namespace, "name", req.Name,
+					"resource", req.Resource, "user", req.UserInfo.Username,
+					"requestID", req.UID,
+				)
+			}
+			return log
+		}
+	}
+}
+
+func (blder *Builder) registerWebhooks() error {
+	typ, err := blder.getType()
+	if err != nil {
+		return err
+	}
+
+	blder.gvk, err = apiutil.GVKForObject(typ, blder.mgr.GetScheme())
+	if err != nil {
+		return err
+	}
+
+	// Register webhook(s) for type
+	blder.registerDefaultingWebhook()
+	blder.registerValidatingWebhook()
+
+	err = blder.registerConversionWebhook()
+	if err != nil {
+		return err
+	}
+	return nil
```

  I think the code changed since the last time I copied it. I'll resync.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T15:47:01Z

- **pkg/controller/jobframework/webhook/builder.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T15:41:27Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
+// with some modifications to get full control of the construction of patches.
+
+// Builder builds a Webhook.
+type Builder struct {
+	apiType         runtime.Object
+	mutationHandler admission.Handler
```

  Yes

- **pkg/controller/jobframework/webhook/builder.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T15:42:11Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
```

  The file just changed since I copied the code. Except the rename, which I reverted now. And I synced with the latest version.

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T15:45:59Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  I highlighted the word "dropping" now.
  
  > I'm also wondering if this is safe in case of webhooks delivered by users for external Jobs?
  
  In most cases, custom integrations would use the baseWebhook, which doesn't remove fields.
  But implementors can choose not to use this handler or builder.
  And they should write integration tests, in which they would notice if a field is not removed.

- **pkg/controller/jobframework/webhook/builder.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T15:46:42Z
  Added to the comment below

- **pkg/controller/jobframework/webhook/builder.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T15:46:51Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
```

  Yes, added filename and hash.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T16:15:07Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@mimowo](https://github.com/mimowo) — 2024-09-25T16:15:07Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  > In most cases, custom integrations would use the baseWebhook, which doesn't remove fields.
  
  Yeah, but this may not be feasible for more some complex webhooks like [appwrapper](https://github.com/project-codeflare/appwrapper/blob/main/internal/webhook/appwrapper_webhook.go).
  
  > And they should write integration tests, in which they would notice if a field is not removed.
  
  I guess we need to resort to that. One potential preemptive action would be to note about this in the [docs](https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/#building-a-built-in-integration).
  
  I assume users still can register webhooks without this property if they really need to, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-25T16:21:02Z

- **pkg/controller/jobframework/webhook/builder.go:37** by [@mimowo](https://github.com/mimowo) — 2024-09-25T16:21:02Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
```

  I checked the diff: https://www.diffchecker.com/iSfsfBol/. Looks clean to me now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T17:05:35Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T17:05:35Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  > Yeah, but this may not be feasible for more some complex webhooks like [appwrapper](https://github.com/project-codeflare/appwrapper/blob/main/internal/webhook/appwrapper_webhook.go).
  
  Actually, for external integrations, they are more likely to happen in the same repo as to where the CRDs live, so they shouldn't be affected by version skew. As a result, they probably will be ok with the CustomDefaulter way of controller-runtime.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T17:06:23Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T17:06:22Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  > I assume users still can register webhooks without this property if they really need to, right?
  
  Yes, in https://github.com/kubernetes-sigs/controller-runtime/pull/2932 I'm leaving the two options for building webhooks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T17:12:57Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T17:12:57Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  TBH, I'm hoping to be fixed this regression in the controller-runtime side by the next Kueue minor release (~~v0.10~~ v0.9.0). 
  So, if this regression will not be fixed before we cut a ~~v0.10~~ v0.9.0, it might be better to add notes to the documents.
  
  Otherwise, we can keep the current documentation. WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T17:13:53Z

- **pkg/controller/jobframework/webhook/builder.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T17:13:53Z
  Thanks. Looks good to me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T17:14:40Z

- **pkg/controller/jobframework/webhook/builder.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T17:14:39Z
```diff
@@ -0,0 +1,246 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"errors"
+	"net/http"
+	"net/url"
+	"strings"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/client-go/rest"
+	"k8s.io/klog/v2"
+
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/conversion"
+)
+
+// This code is copied from https://github.com/kubernetes-sigs/controller-runtime
+// with some modifications to get full control of the construction of patches.
+
+// Builder builds a Webhook.
+type Builder struct {
+	apiType         runtime.Object
+	mutationHandler admission.Handler
+	customValidator admission.CustomValidator
+	gvk             schema.GroupVersionKind
+	mgr             manager.Manager
+	config          *rest.Config
+	recoverPanic    bool
+	logConstructor  func(base logr.Logger, req *admission.Request) logr.Logger
+}
+
+// ManagedBy returns a new webhook builder.
+func ManagedBy(m manager.Manager) *Builder {
+	return &Builder{mgr: m}
+}
+
+// For takes a runtime.Object which should be a CR.
+func (blder *Builder) For(apiType runtime.Object) *Builder {
+	blder.apiType = apiType
+	return blder
+}
+
+// WithMutationHandler takes an admission.Handler inteface, a MutationgWebhook will be wired for this type.
+func (blder *Builder) WithMutationHandler(handler admission.Handler) *Builder {
+	blder.mutationHandler = handler
+	return blder
+}
+
+// WithValidator takes a admission.CustomValidator interface, a ValidatingWebhook will be wired for this type.
+func (blder *Builder) WithValidator(validator admission.CustomValidator) *Builder {
+	blder.customValidator = validator
+	return blder
+}
+
+// WithLogConstructor overrides the webhook's LogConstructor.
+func (blder *Builder) WithLogConstructor(logConstructor func(base logr.Logger, req *admission.Request) logr.Logger) *Builder {
+	blder.logConstructor = logConstructor
+	return blder
+}
+
+// RecoverPanic indicates whether panics caused by the webhook should be recovered.
+func (blder *Builder) RecoverPanic() *Builder {
+	blder.recoverPanic = true
+	return blder
+}
+
+// Complete builds the webhook.
+func (blder *Builder) Complete() error {
+	// Set the Config
+	blder.loadRestConfig()
+
+	// Configure the default LogConstructor
+	blder.setLogConstructor()
+
+	// Set the Webhook if needed
+	return blder.registerWebhooks()
+}
+
+func (blder *Builder) loadRestConfig() {
+	if blder.config == nil {
+		blder.config = blder.mgr.GetConfig()
+	}
+}
+
+func (blder *Builder) setLogConstructor() {
+	if blder.logConstructor == nil {
+		blder.logConstructor = func(base logr.Logger, req *admission.Request) logr.Logger {
+			log := base.WithValues(
+				"webhookGroup", blder.gvk.Group,
+				"webhookKind", blder.gvk.Kind,
+			)
+			if req != nil {
+				return log.WithValues(
+					blder.gvk.Kind, klog.KRef(req.Namespace, req.Name),
+					"namespace", req.Namespace, "name", req.Name,
+					"resource", req.Resource, "user", req.UserInfo.Username,
+					"requestID", req.UID,
+				)
+			}
+			return log
+		}
+	}
+}
+
+func (blder *Builder) registerWebhooks() error {
+	typ, err := blder.getType()
+	if err != nil {
+		return err
+	}
+
+	blder.gvk, err = apiutil.GVKForObject(typ, blder.mgr.GetScheme())
+	if err != nil {
+		return err
+	}
+
+	// Register webhook(s) for type
+	blder.registerDefaultingWebhook()
+	blder.registerValidatingWebhook()
+
+	err = blder.registerConversionWebhook()
+	if err != nil {
+		return err
+	}
+	return nil
```

  Ah, I see. Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T17:20:30Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T17:20:30Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  In controller-runtime I'm only adding the option to provide a full handler. I'm not adding this handler, as this is somewhat specific to Kueue. So we will still need this handler code.
  
  To make things more clear, I renamed the Defaulter to LosslessDefaulter, so bring more attention to its effects.
  
  Just to expand on why I decided to go with this approach of dropping the remove operations:
  Another option I considered was for every job to return jsonpatches in the `Suspend` operation. But then it becomes quite hard to explain how to do your own, if you aren't already familiar with jsonpatches. It's much simpler to say "just edit the object". Then I was thinking how can we produce safe patches generically, starting from an edited object. One option would have been to do something similar to https://github.com/kubernetes-sigs/controller-runtime/pull/2931, but that approach might introduce other bugs, as explained in https://github.com/kubernetes-sigs/controller-runtime/pull/2931#discussion_r1728367254
  
  So ultimately I decided to exploit the fact that our webhooks simply don't drop fields.
  
  EDIT: I added the above to the PR description for future reference.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T17:33:38Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T17:33:38Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  I see. That makes sense.
  In that case, it would be worth it to add documentation in https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/ so that we can recommend this custom Handler for the external controller developments even after the controller-runtime PR are merged.
  
  But, I would like to update the documentation in a separate PR so that we can easily cherry-pick the commit to the website branch.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-09-25T17:35:41Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-09-25T17:35:40Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  > > Yeah, but this may not be feasible for more some complex webhooks like [appwrapper](https://github.com/project-codeflare/appwrapper/blob/main/internal/webhook/appwrapper_webhook.go).
  > 
  > Actually, for external integrations, they are more likely to happen in the same repo as to where the CRDs live, so they shouldn't be affected by version skew. As a result, they probably will be ok with the CustomDefaulter way of controller-runtime.
  
  I believe this is correct in general (and is definitely correct for appwrapper). We build/release the CRDs and the controller/webhook together so aren't subject to the same version skew problems.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T17:36:08Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T17:36:08Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	admissionv1 "k8s.io/api/admission/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"sigs.k8s.io/controller-runtime/pkg/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+var (
+	testResourceKind = "TestResource"
+	testResourceGVK  = schema.GroupVersionKind{Group: "foo.test.org", Version: "v1", Kind: testResourceKind}
+)
+
+type TestResource struct {
+	Foo string `json:"foo,omitempty"`
+}
+
+func (d *TestResource) GetObjectKind() schema.ObjectKind { return d }
+func (d *TestResource) DeepCopyObject() runtime.Object {
+	return &TestResource{
+		Foo: d.Foo,
+	}
+}
+
+func (d *TestResource) GroupVersionKind() schema.GroupVersionKind {
+	return testResourceGVK
+}
+
+func (d *TestResource) SetGroupVersionKind(gvk schema.GroupVersionKind) {}
+
+type TestCustomDefaulter struct{}
+
+func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
+	d := obj.(*TestResource)
+	if d.Foo == "" {
+		d.Foo = "bar"
+	}
+	return nil
+}
+
+func TestLossLessDefaulter(t *testing.T) {
+	sch := runtime.NewScheme()
+	builder := scheme.Builder{GroupVersion: testResourceGVK.GroupVersion()}
+	builder.Register(&TestResource{})
+	if err := builder.AddToScheme(sch); err != nil {
+		t.Fatalf("Couldn't add types to scheme: %v", err)
+	}
+
+	handler := WithLosslessDefaulter(sch, &TestResource{}, &TestCustomDefaulter{})
+
+	req := admission.Request{
+		AdmissionRequest: admissionv1.AdmissionRequest{
+			Kind: metav1.GroupVersionKind(testResourceGVK),
+			Object: runtime.RawExtension{
+				Raw: []byte(`{"baz": "qux"}`),
+			},
+		},
+	}
+	resp := handler.Handle(context.Background(), req)
+	if !resp.Allowed {
+		t.Errorf("Response not allowed")
+	}
+	wantPatches := []jsonpatch.Operation{
+		{
+			Operation: "add",
+			Path:      "/foo",
+			Value:     "bar",
+		},
```

  Could we add a remove operation so that we can verify if the remove operation is ignored?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T17:37:09Z

Otherwise lgtm.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-25T17:45:01Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-25T17:45:01Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  > I believe this is correct in general (and is definitely correct for appwrapper). We build/release the CRDs and the controller/webhook together so aren't subject to the same version skew problems.
  
  I guess that there are some situations where they develop the external Kueue controller to enqueue non upstream supported Job, internally.
  
  For example, I can imagine the VolcanoJob, TeckTon Pipeline, or something.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T18:16:11Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:94** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T18:16:11Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	admissionv1 "k8s.io/api/admission/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"sigs.k8s.io/controller-runtime/pkg/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+var (
+	testResourceKind = "TestResource"
+	testResourceGVK  = schema.GroupVersionKind{Group: "foo.test.org", Version: "v1", Kind: testResourceKind}
+)
+
+type TestResource struct {
+	Foo string `json:"foo,omitempty"`
+}
+
+func (d *TestResource) GetObjectKind() schema.ObjectKind { return d }
+func (d *TestResource) DeepCopyObject() runtime.Object {
+	return &TestResource{
+		Foo: d.Foo,
+	}
+}
+
+func (d *TestResource) GroupVersionKind() schema.GroupVersionKind {
+	return testResourceGVK
+}
+
+func (d *TestResource) SetGroupVersionKind(gvk schema.GroupVersionKind) {}
+
+type TestCustomDefaulter struct{}
+
+func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
+	d := obj.(*TestResource)
+	if d.Foo == "" {
+		d.Foo = "bar"
+	}
+	return nil
+}
+
+func TestLossLessDefaulter(t *testing.T) {
+	sch := runtime.NewScheme()
+	builder := scheme.Builder{GroupVersion: testResourceGVK.GroupVersion()}
+	builder.Register(&TestResource{})
+	if err := builder.AddToScheme(sch); err != nil {
+		t.Fatalf("Couldn't add types to scheme: %v", err)
+	}
+
+	handler := WithLosslessDefaulter(sch, &TestResource{}, &TestCustomDefaulter{})
+
+	req := admission.Request{
+		AdmissionRequest: admissionv1.AdmissionRequest{
+			Kind: metav1.GroupVersionKind(testResourceGVK),
+			Object: runtime.RawExtension{
+				Raw: []byte(`{"baz": "qux"}`),
+			},
+		},
+	}
+	resp := handler.Handle(context.Background(), req)
+	if !resp.Allowed {
+		t.Errorf("Response not allowed")
+	}
+	wantPatches := []jsonpatch.Operation{
+		{
+			Operation: "add",
+			Path:      "/foo",
+			Value:     "bar",
+		},
```

  Let me add a comment:
  
  The raw object has a field `"baz": "qux"`, which is not in the go type, and there is no remove operation for it.
  If I replace the LossLessDefaulter with controller-runtime's custom defaulter, then it generates a remove operation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T18:16:50Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T18:16:50Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  I'll submit a separate PR for the documentation, but I don't think I'll cherry-pick this change, so it can go with the next release.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T18:17:31Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T18:17:31Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  We don't need to cherry-pick because we are already in the latest versions of kube-ray and kubernetes, which were the known libraries to have new fields.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-25T19:27:31Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-25T19:27:31Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  I was chatting with @jpbetz to get his opinion on my former approach: doing a round-trip of the raw object. This proposal was rejected by the maintainers of controller-runtime because it might break backwards compatibility when a go type removes an omitempty clause that was set for a field before https://github.com/kubernetes-sigs/controller-runtime/pull/2931#discussion_r1728367254
  
  He agreed that both approaches (current behavior of controller-runtime and https://github.com/kubernetes-sigs/controller-runtime/pull/2931) have pros and cons. However, getting new fields dropped is worse than dealing with CRDs that did backward-incompatible changes related to omitempty.
  And, in the case of Kueue, we are only dealing with production ready CRDs and we only modify a few fields, so the likelihood of a problem is low.
  So, we could move in that direction instead of the approach in the current PR to drop "remove" operations, which can be less future proof.
  WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-26T07:16:14Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@mimowo](https://github.com/mimowo) — 2024-09-26T07:16:13Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  I support moving forward with https://github.com/kubernetes-sigs/controller-runtime/pull/2932 to at least enable us in Kueue to register the lossLesshandler. 
  
  However, for the long term solution LossLessHandler isn't ideal. Even if we don't the problem yet, it might happen with increased adoption, for example an adopter of Kueue wants to write an in-house webhook to drop a problematic field from Volcano CRD (for example to work-around some issue). 
  
  I see the concern in https://github.com/kubernetes-sigs/controller-runtime/pull/2931, but some solution like this could be handy, maybe it just needs additional care for the problematic cases mentioned?
  
  I'm also wondering if this problem is related to https://github.com/kubernetes/kubernetes/issues/126175 (might be red herring, but PTAL)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-26T13:01:20Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-26T13:01:20Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > but some solution like this could be handy, maybe it just needs additional care for the problematic cases mentioned?
  
  We wouldn't be able to know generically. It depends on the CRD. But again, the likelihood is very low.
  
  So, up to you. Would you prefer the implementation of LosslessHandler as suggested in https://github.com/kubernetes-sigs/controller-runtime/pull/2931 or are you ok with proceeding with the current PR?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-26T14:20:35Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@mimowo](https://github.com/mimowo) — 2024-09-26T14:20:34Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  I will need to learn more about the pros & cons of thttps://github.com/kubernetes-sigs/controller-runtime/pull/2931. 
  Would the change in omitempty be problematic then only for build-in types, or CRDs also? Also is it problematic removing "omitempty", or adding as well?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-26T15:30:30Z

- **pkg/controller/jobframework/webhook/defaulter.go:45** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-26T15:30:30Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithDefaulter creates a new Handler for a CustomDefaulter interface that drops remove operations.
+func WithDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &defaulterForType{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type defaulterForType struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, dropping remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
```

  > We don't need to cherry-pick because we are already in the latest versions of kube-ray and kubernetes, which were the known libraries to have new fields.
  
  That makes sense. Indeed, we do not observe the new field on our other supporting Jobs (Kubeflow, JobSet...).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-26T15:35:00Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-26T15:35:00Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	admissionv1 "k8s.io/api/admission/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"sigs.k8s.io/controller-runtime/pkg/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+var (
+	testResourceKind = "TestResource"
+	testResourceGVK  = schema.GroupVersionKind{Group: "foo.test.org", Version: "v1", Kind: testResourceKind}
+)
+
+type TestResource struct {
+	Foo string `json:"foo,omitempty"`
+}
+
+func (d *TestResource) GetObjectKind() schema.ObjectKind { return d }
+func (d *TestResource) DeepCopyObject() runtime.Object {
+	return &TestResource{
+		Foo: d.Foo,
+	}
+}
+
+func (d *TestResource) GroupVersionKind() schema.GroupVersionKind {
+	return testResourceGVK
+}
+
+func (d *TestResource) SetGroupVersionKind(gvk schema.GroupVersionKind) {}
+
+type TestCustomDefaulter struct{}
+
+func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
+	d := obj.(*TestResource)
+	if d.Foo == "" {
+		d.Foo = "bar"
+	}
+	return nil
+}
+
+func TestLossLessDefaulter(t *testing.T) {
+	sch := runtime.NewScheme()
+	builder := scheme.Builder{GroupVersion: testResourceGVK.GroupVersion()}
+	builder.Register(&TestResource{})
+	if err := builder.AddToScheme(sch); err != nil {
+		t.Fatalf("Couldn't add types to scheme: %v", err)
+	}
+
+	handler := WithLosslessDefaulter(sch, &TestResource{}, &TestCustomDefaulter{})
+
+	req := admission.Request{
+		AdmissionRequest: admissionv1.AdmissionRequest{
+			Kind: metav1.GroupVersionKind(testResourceGVK),
+			Object: runtime.RawExtension{
+				Raw: []byte(`{"baz": "qux"}`),
+			},
+		},
+	}
+	resp := handler.Handle(context.Background(), req)
+	if !resp.Allowed {
+		t.Errorf("Response not allowed")
+	}
+	wantPatches := []jsonpatch.Operation{
+		{
+			Operation: "add",
+			Path:      "/foo",
+			Value:     "bar",
+		},
```

  Oh, I got it. Thank you for the clarifications.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-26T17:33:35Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-26T17:33:35Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  The problematic scenario is when the go library in Kueue doesn't have omitempty, but the object stored in apiserver doesn't have the field. In this case, roundtripping in Kueue would add the fields, leading to possibly invalid patches. This can happen in two scenarios:
  - The go type author removes the omitempty from a field, and this is the library that Kueue uses. A user created the object when omitempty was in place.
  - Kueue is using an older library that doesn't have omitempty. The go type author adds omitempty to a field, and a user creates an object using this version.
  
  So yes, it can happen in either scenario. @jpbetz, please correct me if my interpretation is wrong.
  
  Another diminishing factor is that this only matters if we are modifying a field that is having these omitempty transitions. If the transition happens in an object that Kueue doesn't touch, then this wouldn't show up in the jsondiff. But the omitempty status of all the parents of the field could be relevant.
  
  I think https://github.com/kubernetes/kubernetes/issues/126175 is also related, but it rather happens with the current CustomDefaulter of controller-runtime, and it wouldn't happen with my version because it wouldn't show in the jsondiff.
  
  So the question comes down to:
  Do we want to prioritize supporting newly added fields that Kueue libraries don't know about? Or do we prioritize supporting buggy types (which, as we can see, could also be a problem in the current version)? I think it's only fair to prioritize the former and be in the watch to report issues as necessary in other libraries and upgrade when appropriate.
  
  OTOH, the LosslessDefaulter as currently implemented (just dropping every remove operation), doesn't have problems with adding or removing omitempty, as long as we know that we are not removing any fields in the webhook. Although it's still subject to a case like https://github.com/kubernetes/kubernetes/issues/126175

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-26T17:46:10Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-26T17:46:10Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  I think there is a risk in kubeflow because some of the `RunPolicy` fields don't have omitempty. Maybe we can make sure they get omitempty before we implement the RoundtripDefaulter in Kueue? cc @andreyvelich (sorry, it's a long thread to read, but you can only understand it if you follow all of it).

### Review by [@jpbetz](https://github.com/jpbetz) (commented) — 2024-09-26T19:03:57Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@jpbetz](https://github.com/jpbetz) — 2024-09-26T19:03:57Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  I'm strongly in favor of biasing toward approaches that work for well-behaved CRD authors.
  
  Adding fields to CRDs is very common, and it's essential to being able to safely and backward compatibly evolve a CRD over time.  If we have a webhook mechanism that removes fields that it doesn't recognize or need to care about purely because the webhook was developed before the field was introduced, that's a risk to all well behaved CRD authors. I personally wouldn't ever use a webhook that might start removing perfectly valid fields in the future.  That seems broken to me, and the failure mode is pretty bad.. it seems to work when you develop it, then later it mutates resources in a dangerous way, maybe in production.
  
  Am I mis-representing the problem?
  
  > Would the change in omitempty be problematic then only for build-in types, or CRDs also? Also is it problematic removing "omitempty", or adding as well?
  
  I don't think we could ever make this change to a built-in type. And even if you do make this change, isn't it only a problem if the server differentiates between absent and zero-valued? (which is backward incompatible and dangerous for the interpretation of any resources written to storage before the change?)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-26T19:16:49Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-26T19:16:49Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > Am I mis-representing the problem?
  
  No, you are on point :)
  
  > I don't think we could ever make this change to a built-in type. And even if you do make this change, isn't it only problem if the server differentiates between absent and zero-valued? (
  
  Right, in a built-in type, we would never remove an "omitempty" for an optional field, but we could still add one, just because we forgot before, like in https://github.com/kubernetes/kubernetes/pull/126046
  
  I think, in that case, if Kueue keeps the library with the buggy go type (without omitempty), then using the RoundTripperDefaulter is at risk of producing invalid jsonpatches.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-26T19:23:19Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-26T19:23:19Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  But in that case, it would be silly not to upgrade Kueue library to the latest patch version.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-26T20:32:15Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-26T20:32:15Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  My overall suggestion is the following:
  1. Merge LosslessDefaulter (this PR)
  2. Do an audit of all in-tree integrations and make sure they are using omitempty appropriately, at least for the fields we modify.
  3. Fix each external CRD as necessary and wait for their releases.
  4. Upgrade Kueue libraries and change the LosslessDefaulter for a RoundTripDefaulter.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-27T09:39:55Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@mimowo](https://github.com/mimowo) — 2024-09-27T09:39:55Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  Aldo, big thanks for explaining the issue in detail, and to all folks sharing their perspectives.
  
  I fully agree with the need to fix the issue, a webhook which removes fields is very bad. This is important to the extent that I would consider cherry-picking this fix to 0.8.2. Even if the current master webhook doesn’t drop fields, because it is compiled against the latest CRD (say Ray), it may start dropping fields in a couple on months just because the next version of Ray is released (outside of Kueue’s control). WDY @tenzen-y ?
  
  Regarding the fix of choice. 
  
  Re 1. I really like the LossLessDefaulter approach, and I don’t see it buggy at all. IIUC it ensures correctness (current and future) for all CRDs managed by Kueue, as long as our the webhooks don’t remove fields, but our webhooks are part of Kueue codebase for built-in integrations, so we are in control. The approach is opt-in, so external developers may or may not want to use LossLessDefaulter (we should warn about the potential risk in documentation). Finally, if a user developing external or internal integration using LossLessDefaulter has a need to remove a field, they could do so using a second webhook registered using regular Defaulter, and just drop the field (set to nil). It will be safe, because the dedicated webhook will be compiled against the CRD version new enough to contain the field.
  
  Re 2-4: I’m sceptical here. First, IIUC the mechanism as prototyped in https://github.com/kubernetes-sigs/controller-runtime/pull/2931 is not opt-in, and so if there is a field that changes omitempty, the correctness is out of Kueue - we basically need to accept the fact that Kueue’s webhooks misbehave from the end-user perspective.  
  
  I just did a quick heuristic check: searched in kubernetes by “omitempty” : https://github.com/kubernetes/kubernetes/pulls?q=is%3Apr+omitempty+label%3Akind%2Fapi-change+is%3Aclosed , and clicked through a couple of top results, finding:
  - https://github.com/kubernetes/kubernetes/pull/126046 added omitempty
  - https://github.com/kubernetes/kubernetes/pull/124859 removed omitempty
  - https://github.com/kubernetes/kubernetes/pull/124553 removed omitempty
  - https://github.com/kubernetes/kubernetes/pull/121078 added omitempty
  - https://github.com/kubernetes/kubernetes/pull/121000 added omitempty
  
  I also remember OnCalling an issue where the culprit was that a user changed omitempty for an in-house CRD.
  
  Given that such changes happen in the core k8s (often to fix bugs), and we cannot control in-house integrations, I think it isn't feasible to audit all CRDs (as suggested in 2-3 points), so I think any fix we choose needs to handle that scenario gracefully. 
  
  I would like to move forward with (1.), and maybe (4) provided that the approach can be adjusted to handle changes in omitempty gracefully. 
  
  I’m happy to re-evaluate if I missed something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-27T13:42:56Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@mimowo](https://github.com/mimowo) — 2024-09-27T13:42:56Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  One more thing to clarify: when I say I don't see (1.) buggy I mean it solves the problem for Kueue and as long as we are careful - the correctness is in hands of Kueue developers (or developers of integrations). **as long as we are careful** is still an important caveat here, so the solution is far from ideal. Another reason it is not ideal, is that other projects, like JobSet would also need to introduce something like LossLessDefaulter, and this approach will not scale.  It is just my preference out of the options on the table.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-27T15:55:09Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-27T15:55:09Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > First, IIUC the mechanism as prototyped in https://github.com/kubernetes-sigs/controller-runtime/pull/2931 is not opt-in
  
  Right, that original PR is not opt-in. However, the proposal would be to add a `RoundTripDefaulter` as an alternative to c-r's `CustomDefaulter`, not as a replacement. Then it is opt-in.
  
  But, in any case, if you are accepting `LosslessDefaulter` as is, then the only reason to change to `RoundTripDefaulter` would be because we suddenly have a need to remove fields in our webhooks or we just want to be future-proof or provide the alternative to external developers.
  
  A good thing about making it opt-in, is that we don't have to migrate all webhooks at once. We can do the audit for a specific type and then do the migration for just that webhook.
  
  > I just did a quick heuristic check: searched in kubernetes by “omitempty” 
  
  That's scary. We need to be more diligent about https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#optional-vs-required when doing API reviews. I'll send an email to api-reviewers :)
  
  > Another reason it is not ideal, is that other projects, like JobSet would also need to introduce something like LossLessDefaulter
  
  I don't think this is a problem for Jobset, at least not in the same way. Jobset's webhook would always be released alongside the new CRD version. Sure, jobset uses job specs underneath, which in theory could add fields, but unless they are in the latest version, the fields would be dropped even if there wasn't a webhook, because they wouldn't be part of the jobset CRD.
  So, this is more of a problem of components like Kueue that deal with libraries that it doesn't control.
  
  Anyways, it sounds like we should be proceeding with this PR for now. Any additional implementation comments?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-27T16:36:27Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@mimowo](https://github.com/mimowo) — 2024-09-27T16:36:27Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > the proposal would be to add a RoundTripDefaulter as an alternative to c-r's CustomDefaulter, not as a replacement. Then it is opt-in.
  
  Probably worth adding to controller-runtime as an option. However, even then I would be a bit cautious for using in Kueue (but open to the possibility). The reason is that it gets correctness, from the end-user perspective, out of Kueue. So, a user of Kueue may see there was an edit of CRD by Kueue's webhook, and attribute this as a bug of the Kueue webhook, which would be understandable, even though the actual change was in CRD.
  
  > I don't think this is a problem for Jobset, at least not in the same way. Jobset's webhook would always be released alongside the new CRD version.
  
  Ah, right. Though, to some extent it may still happen if omitempty is changed for the Pod API in Kube, because JobSet has [pod webook](https://github.com/kubernetes-sigs/jobset/blob/main/pkg/webhooks/pod_mutating_webhook.go) also.
  
  > Anyways, it sounds like we should be proceeding with this PR for now. Any additional implementation comments?
  
  Good to go from me, but let's see if @tenzen-y has some final comments.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-27T16:52:49Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T16:52:49Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > I think there is a risk in kubeflow because some of the RunPolicy fields don't have omitempty. Maybe we can make sure they get omitempty before we implement the RoundtripDefaulter in Kueue?
  
  On behalf of Kubeflow Training TL, I believe this request is reasonable and acceptable. But, I want to open an issue in the kubeflow side before we start implementations.
  
  Another concern is the older Kubeflow TrainingOperator users. IIUC, after the RoundTripDefaulter is implemented in the Kueue side, if the users use the older TrainingOperator (without omitempty RunPolicy), some field could be accidentally dropped during webhook, right?
  
  So, I want to keep using this LossLessDefaulter for a while. But, we can discuss it in another issue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-27T16:54:40Z

Thanks @alculquicondor @mimowo @jpbetz!
/lgtm
/approve
/hold cancel

@alculquicondor  Please open a PR to update docs and an issue to discuss about the RoudtripDefaulter as we discussed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-27T17:26:17Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-27T17:26:17Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  >  if the users use the older TrainingOperator (without omitempty RunPolicy), some field could be accidentally dropped during webhook, right?
  
  I don't think so. If Kueue libraries have omitempty when necessary, then the patches will be correct even if the objects were created with a different version.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-27T17:40:23Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T17:40:23Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > > if the users use the older TrainingOperator (without omitempty RunPolicy), some field could be accidentally dropped during webhook, right?
  > 
  > I don't think so. If Kueue libraries have omitempty when necessary, then the patches will be correct even if the objects were created with a different version.
  
  I see. I was misunderstanding your point a little bit. Now, all clear. So, we want to add the `omitempty` to RunPolicy as soon as possible. Let's open an issue and add omitempty to the TrainigOperator and MPIOperator.
  
  Thanks for the clarifications and for sharing your investigations.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-27T17:41:34Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@mimowo](https://github.com/mimowo) — 2024-09-27T17:41:33Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  I think understanding all nuances of setting "omitempty" properly has proved to be hard, and so it is hard for me to imagine we could maintain bug-free CRDs. Even if we put effort into auditing the CRDs for version X, I think with new fields added such bugs are likely to slip review in X+2, and will require changes (bugfixes). 
  
  I'm also a bit concerned that with widespread use of RoundTripDefauler we will have an incentive not to fix omitempty bugs, so that we don't run into issues on upgrades.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-27T17:46:33Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T17:46:33Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > I'm also a bit concerned that with widespread use of RoundTripDefauler we will have an incentive not to fix omitempty bugs, so that we don't run into issues on upgrades.
  
  I'm sure your concerns. But, I guess that we need to take a little bit longer time to fix the root cause in the contoller-runtime and core kube API. So, I would recommend to treat the RoutTripDefaulter as a temporary solution and put the function in the internal package so that the function is not widespread used.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-27T17:52:48Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-27T17:52:48Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  Again, the omitempty issue only affects Kueue webhooks if they are missing in the fields that Kueue modifies. If Kueue doesn't modify them, then they would simply not form part of the jsondiff. So, to ensure Kueue correctness, we only need to audit the fields that we touch.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-27T17:56:22Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T17:56:22Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  I guess that Michal's concern is for the third-party Kueue controllers. Isn't it?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-09-27T18:25:44Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-27T18:25:44Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  Yeah, but unfortunately we cannot do much better than document the problem.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-27T19:02:09Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T19:02:09Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  > Yeah, but unfortunately we cannot do much better than document the problem.
  
  I agree with you. The third-party controller is not controllable by us.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-30T10:12:35Z

- **pkg/controller/jobframework/webhook/defaulter.go:46** by [@mimowo](https://github.com/mimowo) — 2024-09-30T10:12:35Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
```

  My comment was more speculative, touching two potential concerns. Let me explain below in more detail.
  
  > So, to ensure Kueue correctness, we only need to audit the fields that we touch. 
  
  Yes, but even if we audit all fields modified by Kueue for TFJob at a given release of Kueue and TFJob, Kueue may want to use new fields in its future releases (for new features, like managedBy). Then, we need to audit the new field, and if we find an "omitempty" bug in TFJob operator in the field, then what do we do? Wait with the new feature of Kueue until the TFJob is fixed and released, or build the new feature based on the buggy field? None of the choices is ideal.
  
  > I guess that Michal's concern is for the third-party Kueue controllers. Isn't it?
  
  Yeah, this is another consideration, if RoundTrip defaulter is widespread in customer, and third-party webhooks, it would create an incentive not to fix the omitempty bugs (whatever small, but still).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-01T07:31:16Z

- **pkg/controller/jobframework/webhook/defaulter.go:51** by [@mimowo](https://github.com/mimowo) — 2024-10-01T07:31:16Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
+	response := h.Handler.Handle(ctx, req)
+	if response.Allowed {
+		var patches []jsonpatch.Operation
+		for _, p := range response.Patches {
+			if p.Operation != "remove" {
```

  @alculquicondor would it be feasible to make the LossLessDefaulter more selective - only drop remove operations which correspond to fields outside of schema? If possible, then we could allow webhooks to remove known fields, while still preventing dropping of unknown fields. I suppose the answer may not be straightforward - depending how easily we can check if a remove operation corresponds to a known field, also performance may play a role. 
  
  If possible, maybe it would increase chances for promoting LossLessDefaulter as part of opt-in defaulter inside controller-runtime?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-01T14:41:10Z

- **pkg/controller/jobframework/webhook/defaulter.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-01T14:41:10Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
+	response := h.Handler.Handle(ctx, req)
+	if response.Allowed {
+		var patches []jsonpatch.Operation
+		for _, p := range response.Patches {
+			if p.Operation != "remove" {
```

  I suppose there is a way using the `reflect` package. But I don't think I'll have time to explore it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-01T16:25:12Z

- **pkg/controller/jobframework/webhook/defaulter.go:51** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-01T16:25:12Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
+	response := h.Handler.Handle(ctx, req)
+	if response.Allowed {
+		var patches []jsonpatch.Operation
+		for _, p := range response.Patches {
+			if p.Operation != "remove" {
```

  @mimowo That sounds more safety. Could we open an issue on the Kueue side?
  If anyone is interested in this enhancement, they can take it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-01T17:15:23Z

- **pkg/controller/jobframework/webhook/defaulter.go:51** by [@mimowo](https://github.com/mimowo) — 2024-10-01T17:15:23Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package webhook
+
+import (
+	"context"
+
+	jsonpatch "gomodules.xyz/jsonpatch/v2"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/webhook/admission"
+)
+
+// WithLosslessDefaulter creates a new Handler for a CustomDefaulter interface that **drops** remove operations,
+// which are typically the result of new API fields not present in Kueue libraries.
+func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
+	return &losslessDefaulter{
+		Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+	}
+}
+
+type losslessDefaulter struct {
+	admission.Handler
+}
+
+// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
+// The controller-runtime handler works by creating a jsondiff from the raw object and the marshalled
+// version of the object modified by the defaulter. This generates "remove" operations for fields
+// that are not present in the go types, which can occur when Kueue libraries are behind the latest
+// released CRDs.
+// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
+func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
+	response := h.Handler.Handle(ctx, req)
+	if response.Allowed {
+		var patches []jsonpatch.Operation
+		for _, p := range response.Patches {
+			if p.Operation != "remove" {
```

  It would be wonderful, opened: https://github.com/kubernetes-sigs/kueue/issues/3174
