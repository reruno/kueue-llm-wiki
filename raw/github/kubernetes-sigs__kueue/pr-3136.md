# PR #3136: [kjobctl] Use init container to initialise env on Slurm mode.

**Summary**: [kjobctl] Use init container to initialise env on Slurm mode.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3136

**Last updated**: 2024-10-01T18:40:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/kjobctl-init-container-to-initialize-env` → `main`
- **Created**: 2024-09-25T15:27:20Z
- **Updated**: 2024-10-01T18:40:23Z
- **Merged**: 2024-10-01T18:39:49Z
- **Closed**: 2024-10-01T18:39:49Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@IrvingMg](https://github.com/IrvingMg), [@trasc](https://github.com/trasc)
- **Changed files**: 9   **+575 / -278**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use init container to initialise env on kjobctl Slurm mode.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Prepare for #3072

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-25T15:27:38Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ef2b47670e8b63f33154f83460172ba53321cd33 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66fc330ad52e890008c41330 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-25T15:28:05Z

/cc @trasc

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-25T15:28:19Z

/cc @IrvingMg

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T11:49:04Z

@trasc PTAL. Because it's blocker for https://github.com/kubernetes-sigs/kueue/pull/3072.

### Comment by [@trasc](https://github.com/trasc) — 2024-09-30T13:27:25Z

/assign

### Comment by [@trasc](https://github.com/trasc) — 2024-10-01T14:50:51Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-01T14:50:57Z

LGTM label has been added.  <details>Git tree hash: 67912572356b2d5e3d6eceb3c3d66e9e910c8668</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-01T18:27:45Z

LGTM label has been added.  <details>Git tree hash: b0f627dfe6bc201baee2ae0727ce6bb5116573e9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-01T18:27:45Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3136#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3136#pullrequestreview-2341104637" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-09-26T07:26:58Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:55** by [@mwielgus](https://github.com/mwielgus) — 2024-09-26T07:26:58Z
```diff
@@ -39,9 +39,20 @@ import (
 )
 
 const (
-	slurmScriptFilename     = "script"
-	slurmEntrypointFilename = "entrypoint.sh"
-	slurmPath               = "/slurm"
+	// Job array's master job ID number.
+	// Job IDs start at 1, so the master job ID will always be 1.
+	slurmArrayJobID = 1
+
+	slurmScriptsPath           = "/slurm/scripts"
+	slurmEnvEntrypointFilename = "init-entrypoint.sh"
+	slurmEntrypointFilename    = "entrypoint.sh"
+	slurmScriptFilename        = "script"
+
+	slurmEnvsPath          = "/slurm/env"
+	slurmSbatchEnvFilename = "sbatch.env"
+	slurmSlurmEnvFilename  = "slurm.env"
+
+	slurmInitImage = "bash:5-alpine3.20"
```

  Can this be a flag?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-26T07:52:34Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-26T07:52:34Z
```diff
@@ -39,9 +39,20 @@ import (
 )
 
 const (
-	slurmScriptFilename     = "script"
-	slurmEntrypointFilename = "entrypoint.sh"
-	slurmPath               = "/slurm"
+	// Job array's master job ID number.
+	// Job IDs start at 1, so the master job ID will always be 1.
+	slurmArrayJobID = 1
+
+	slurmScriptsPath           = "/slurm/scripts"
+	slurmEnvEntrypointFilename = "init-entrypoint.sh"
+	slurmEntrypointFilename    = "entrypoint.sh"
+	slurmScriptFilename        = "script"
+
+	slurmEnvsPath          = "/slurm/env"
+	slurmSbatchEnvFilename = "sbatch.env"
+	slurmSlurmEnvFilename  = "slurm.env"
+
+	slurmInitImage = "bash:5-alpine3.20"
```

  Added.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-30T14:21:21Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:45** by [@trasc](https://github.com/trasc) — 2024-09-30T13:26:42Z
```diff
@@ -39,9 +39,20 @@ import (
 )
 
 const (
-	slurmScriptFilename     = "script"
-	slurmEntrypointFilename = "entrypoint.sh"
-	slurmPath               = "/slurm"
+	// Job array's master job ID number.
+	// Job IDs start at 1, so the master job ID will always be 1.
```

  ```suggestion
  	// Job array's management job ID number.
  	// Job IDs start at 1, so the management job ID will always be 1.
  ```

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:442** by [@trasc](https://github.com/trasc) — 2024-09-30T13:35:09Z
```diff
@@ -368,34 +424,137 @@ func (b *slurmBuilder) buildEntrypointScript() string {
 
 	slices.Sort(keyValues)
 
-	return fmt.Sprintf(`#!/usr/bin/bash
+	var gpusPerTask, memPerCPU, memPerGPU string
+	if b.gpusPerTask != nil {
+		gpus := make([]string, 0)
+		for name, number := range b.gpusPerTask {
+			gpus = append(gpus, fmt.Sprintf("%s:%s", name, number))
+		}
+		gpusPerTask = strings.Join(gpus, ",")
+	}
+	if b.memPerCPU != nil {
+		memPerCPU = b.memPerCPU.String()
+	}
+	if b.memPerGPU != nil {
+		memPerGPU = b.memPerGPU.String()
+	}
+
+	return fmt.Sprintf(`#!/usr/local/bin/bash
```

  Let's use a go template instead (ideally put the body in a embedded file, not related to this PR but `unmaskFilenameFunction` can be an embedded file as well).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-30T16:56:13Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:45** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T16:56:13Z
```diff
@@ -39,9 +39,20 @@ import (
 )
 
 const (
-	slurmScriptFilename     = "script"
-	slurmEntrypointFilename = "entrypoint.sh"
-	slurmPath               = "/slurm"
+	// Job array's master job ID number.
+	// Job IDs start at 1, so the master job ID will always be 1.
```

  Not sure, because on document it's named as master https://slurm.schedmd.com/sbatch.html#OPT_SLURM_ARRAY_JOB_ID.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-30T16:56:30Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:442** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T16:56:30Z
```diff
@@ -368,34 +424,137 @@ func (b *slurmBuilder) buildEntrypointScript() string {
 
 	slices.Sort(keyValues)
 
-	return fmt.Sprintf(`#!/usr/bin/bash
+	var gpusPerTask, memPerCPU, memPerGPU string
+	if b.gpusPerTask != nil {
+		gpus := make([]string, 0)
+		for name, number := range b.gpusPerTask {
+			gpus = append(gpus, fmt.Sprintf("%s:%s", name, number))
+		}
+		gpusPerTask = strings.Join(gpus, ",")
+	}
+	if b.memPerCPU != nil {
+		memPerCPU = b.memPerCPU.String()
+	}
+	if b.memPerGPU != nil {
+		memPerGPU = b.memPerGPU.String()
+	}
+
+	return fmt.Sprintf(`#!/usr/local/bin/bash
```

  Done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-01T13:18:35Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:45** by [@trasc](https://github.com/trasc) — 2024-10-01T13:18:35Z
```diff
@@ -39,9 +39,20 @@ import (
 )
 
 const (
-	slurmScriptFilename     = "script"
-	slurmEntrypointFilename = "entrypoint.sh"
-	slurmPath               = "/slurm"
+	// Job array's master job ID number.
+	// Job IDs start at 1, so the master job ID will always be 1.
```

  How about avoiding it, can 
  ```suggestion
  	// Note that the first job ID will always be 1.
  ```
  express the same intent?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T13:22:28Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:45** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T13:22:28Z
```diff
@@ -39,9 +39,20 @@ import (
 )
 
 const (
-	slurmScriptFilename     = "script"
-	slurmEntrypointFilename = "entrypoint.sh"
-	slurmPath               = "/slurm"
+	// Job array's master job ID number.
+	// Job IDs start at 1, so the master job ID will always be 1.
```

  Done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-01T14:13:38Z

Overall LGTM just one nit

- **cmd/experimental/kjobctl/pkg/builder/builder.go:46** by [@trasc](https://github.com/trasc) — 2024-10-01T14:01:26Z
```diff
@@ -40,6 +41,9 @@ import (
 	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
 )
 
+//go:embed templates/*
+var templates embed.FS
+
```

  nit: should this be in the slurm_builder file?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T14:18:19Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:46** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T14:18:19Z
```diff
@@ -40,6 +41,9 @@ import (
 	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
 )
 
+//go:embed templates/*
+var templates embed.FS
+
```

  Yeah, I thought about it. But the name is `templates` and it can work with another builders. Maybe we will use it for another modes. WDYT?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-01T14:23:52Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:46** by [@trasc](https://github.com/trasc) — 2024-10-01T14:23:52Z
```diff
@@ -40,6 +41,9 @@ import (
 	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
 )
 
+//go:embed templates/*
+var templates embed.FS
+
```

  > But the name is templates 
  
  we can also change the name, I don't know about the future but for now it is only used in slurm. If/When it's needed in other builders we can think of moving it back along with the template parsing logic.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T14:28:06Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:46** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T14:28:06Z
```diff
@@ -40,6 +41,9 @@ import (
 	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
 )
 
+//go:embed templates/*
+var templates embed.FS
+
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T14:40:07Z

- **cmd/experimental/kjobctl/pkg/builder/builder.go:46** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T14:40:06Z
```diff
@@ -40,6 +41,9 @@ import (
 	"sigs.k8s.io/kueue/cmd/experimental/kjobctl/pkg/constants"
 )
 
+//go:embed templates/*
+var templates embed.FS
+
```

  > we can also change the name
  
  And also renamed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-01T16:31:48Z

- **cmd/experimental/kjobctl/pkg/builder/templates/slurm_init_entrypoint_script.sh.tmpl:5** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-01T16:28:52Z
```diff
@@ -0,0 +1,62 @@
+#!/usr/local/bin/bash
+
+set -o errexit
+set -o nounset
+set -o pipefail
```

  ```suggestion
  set -o pipefail
  set -x
  ```
  
  We may want to see debug logs. Is there any concerns to increase log level?

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder_test.go:273** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-01T16:30:25Z
```diff
@@ -233,57 +233,75 @@ func TestSlurmBuilderDo(t *testing.T) {
 				CompletionMode(batchv1.IndexedCompletion).
 				Profile("profile").
 				Mode(v1alpha1.SlurmMode).
+				WithInitContainer(*wrappers.MakeContainer("slurm-init-env", "bash:5-alpine3.20").
+					Command("bash", "/slurm/scripts/init-entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					Obj()).
 				WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
-					Command("bash", "/slurm/entrypoint.sh").
-					WithVolumeMount(corev1.VolumeMount{MountPath: "/slurm"}).
+					Command("bash", "/slurm/scripts/entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{
+						Name:  constants.EnvVarTaskID,
+						Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+					}).
+					WithEnvVar(corev1.EnvVar{Name: "PROFILE", Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{Name: "TIMESTAMP", Value: testStartTime.Format(time.RFC3339)}).
+					WithEnvVar(corev1.EnvVar{Name: "JOB_CONTAINER_INDEX", Value: "0"}).
 					Obj()).
 				WithVolume(corev1.Volume{
+					Name: "slurm-scripts",
 					VolumeSource: corev1.VolumeSource{
 						ConfigMap: &corev1.ConfigMapVolumeSource{
 							Items: []corev1.KeyToPath{
+								{Key: "init-entrypoint.sh", Path: "init-entrypoint.sh"},
 								{Key: "entrypoint.sh", Path: "entrypoint.sh"},
 								{Key: "script", Path: "script", Mode: ptr.To[int32](0755)},
 							},
 						},
 					},
 				}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
-				WithEnvVar(corev1.EnvVar{
-					Name:  constants.EnvVarTaskID,
-					Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+				WithVolume(corev1.Volume{
+					Name: "slurm-env",
+					VolumeSource: corev1.VolumeSource{
+						EmptyDir: &corev1.EmptyDirVolumeSource{},
+					},
```

  ```suggestion
  					VolumeSource: corev1.VolumeSource{},
  ```
  
  Couldn't we use the `cmpopts.EquateEnpty()` instead of such an empty struct?

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:340** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-01T16:31:43Z
```diff
@@ -335,6 +337,8 @@ var createModeSubcommands = map[string]modeSubcommand{
 
 			subcmd.Flags().BoolVar(&o.IgnoreUnknown, ignoreUnknownFlagName, false,
 				"Ignore all the unsupported flags in the bash script.")
+			subcmd.Flags().StringVar(&o.InitImage, initImageFlagName, "",
```

  ```suggestion
  			subcmd.Flags().StringVar(&o.InitImage, initImageFlagName, slurmDefaultInitImage,
  ```
  
  Shouldn't we set the default image to the flag, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T17:38:01Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:340** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T17:38:01Z
```diff
@@ -335,6 +337,8 @@ var createModeSubcommands = map[string]modeSubcommand{
 
 			subcmd.Flags().BoolVar(&o.IgnoreUnknown, ignoreUnknownFlagName, false,
 				"Ignore all the unsupported flags in the bash script.")
+			subcmd.Flags().StringVar(&o.InitImage, initImageFlagName, "",
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T17:38:16Z

- **cmd/experimental/kjobctl/pkg/builder/templates/slurm_init_entrypoint_script.sh.tmpl:5** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T17:38:16Z
```diff
@@ -0,0 +1,62 @@
+#!/usr/local/bin/bash
+
+set -o errexit
+set -o nounset
+set -o pipefail
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T17:46:55Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder_test.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T17:46:55Z
```diff
@@ -233,57 +233,75 @@ func TestSlurmBuilderDo(t *testing.T) {
 				CompletionMode(batchv1.IndexedCompletion).
 				Profile("profile").
 				Mode(v1alpha1.SlurmMode).
+				WithInitContainer(*wrappers.MakeContainer("slurm-init-env", "bash:5-alpine3.20").
+					Command("bash", "/slurm/scripts/init-entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					Obj()).
 				WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
-					Command("bash", "/slurm/entrypoint.sh").
-					WithVolumeMount(corev1.VolumeMount{MountPath: "/slurm"}).
+					Command("bash", "/slurm/scripts/entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{
+						Name:  constants.EnvVarTaskID,
+						Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+					}).
+					WithEnvVar(corev1.EnvVar{Name: "PROFILE", Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{Name: "TIMESTAMP", Value: testStartTime.Format(time.RFC3339)}).
+					WithEnvVar(corev1.EnvVar{Name: "JOB_CONTAINER_INDEX", Value: "0"}).
 					Obj()).
 				WithVolume(corev1.Volume{
+					Name: "slurm-scripts",
 					VolumeSource: corev1.VolumeSource{
 						ConfigMap: &corev1.ConfigMapVolumeSource{
 							Items: []corev1.KeyToPath{
+								{Key: "init-entrypoint.sh", Path: "init-entrypoint.sh"},
 								{Key: "entrypoint.sh", Path: "entrypoint.sh"},
 								{Key: "script", Path: "script", Mode: ptr.To[int32](0755)},
 							},
 						},
 					},
 				}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
-				WithEnvVar(corev1.EnvVar{
-					Name:  constants.EnvVarTaskID,
-					Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+				WithVolume(corev1.Volume{
+					Name: "slurm-env",
+					VolumeSource: corev1.VolumeSource{
+						EmptyDir: &corev1.EmptyDirVolumeSource{},
+					},
```

  Is it working with pointers in struct? I still have an error:
  
  ```
            						VolumeSource: v1.VolumeSource{
            							HostPath:             nil,
          - 							EmptyDir:             nil,
          + 							EmptyDir:             s"&EmptyDirVolumeSource{Medium:,SizeLimit:<nil>,}",
            							GCEPersistentDisk:    nil,
            							AWSElasticBlockStore: nil,
            							... // 26 identical fields
            						},
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T17:50:03Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder_test.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T17:50:03Z
```diff
@@ -233,57 +233,75 @@ func TestSlurmBuilderDo(t *testing.T) {
 				CompletionMode(batchv1.IndexedCompletion).
 				Profile("profile").
 				Mode(v1alpha1.SlurmMode).
+				WithInitContainer(*wrappers.MakeContainer("slurm-init-env", "bash:5-alpine3.20").
+					Command("bash", "/slurm/scripts/init-entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					Obj()).
 				WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
-					Command("bash", "/slurm/entrypoint.sh").
-					WithVolumeMount(corev1.VolumeMount{MountPath: "/slurm"}).
+					Command("bash", "/slurm/scripts/entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{
+						Name:  constants.EnvVarTaskID,
+						Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+					}).
+					WithEnvVar(corev1.EnvVar{Name: "PROFILE", Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{Name: "TIMESTAMP", Value: testStartTime.Format(time.RFC3339)}).
+					WithEnvVar(corev1.EnvVar{Name: "JOB_CONTAINER_INDEX", Value: "0"}).
 					Obj()).
 				WithVolume(corev1.Volume{
+					Name: "slurm-scripts",
 					VolumeSource: corev1.VolumeSource{
 						ConfigMap: &corev1.ConfigMapVolumeSource{
 							Items: []corev1.KeyToPath{
+								{Key: "init-entrypoint.sh", Path: "init-entrypoint.sh"},
 								{Key: "entrypoint.sh", Path: "entrypoint.sh"},
 								{Key: "script", Path: "script", Mode: ptr.To[int32](0755)},
 							},
 						},
 					},
 				}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
-				WithEnvVar(corev1.EnvVar{
-					Name:  constants.EnvVarTaskID,
-					Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+				WithVolume(corev1.Volume{
+					Name: "slurm-env",
+					VolumeSource: corev1.VolumeSource{
+						EmptyDir: &corev1.EmptyDirVolumeSource{},
+					},
```

  Looks like only only slices and maps: https://github.com/google/go-cmp/blob/c3ad8435e7bef96af35732bc0789e5a2278c6d5f/cmp/cmpopts/equate.go#L20-L26

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-01T18:27:39Z

/lgtm
/approve

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder_test.go:273** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-01T18:25:59Z
```diff
@@ -233,57 +233,75 @@ func TestSlurmBuilderDo(t *testing.T) {
 				CompletionMode(batchv1.IndexedCompletion).
 				Profile("profile").
 				Mode(v1alpha1.SlurmMode).
+				WithInitContainer(*wrappers.MakeContainer("slurm-init-env", "bash:5-alpine3.20").
+					Command("bash", "/slurm/scripts/init-entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					Obj()).
 				WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
-					Command("bash", "/slurm/entrypoint.sh").
-					WithVolumeMount(corev1.VolumeMount{MountPath: "/slurm"}).
+					Command("bash", "/slurm/scripts/entrypoint.sh").
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-scripts", MountPath: "/slurm/scripts"}).
+					WithVolumeMount(corev1.VolumeMount{Name: "slurm-env", MountPath: "/slurm/env"}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
+					WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{
+						Name:  constants.EnvVarTaskID,
+						Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+					}).
+					WithEnvVar(corev1.EnvVar{Name: "PROFILE", Value: "default_profile"}).
+					WithEnvVar(corev1.EnvVar{Name: "TIMESTAMP", Value: testStartTime.Format(time.RFC3339)}).
+					WithEnvVar(corev1.EnvVar{Name: "JOB_CONTAINER_INDEX", Value: "0"}).
 					Obj()).
 				WithVolume(corev1.Volume{
+					Name: "slurm-scripts",
 					VolumeSource: corev1.VolumeSource{
 						ConfigMap: &corev1.ConfigMapVolumeSource{
 							Items: []corev1.KeyToPath{
+								{Key: "init-entrypoint.sh", Path: "init-entrypoint.sh"},
 								{Key: "entrypoint.sh", Path: "entrypoint.sh"},
 								{Key: "script", Path: "script", Mode: ptr.To[int32](0755)},
 							},
 						},
 					},
 				}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarNameUserID, Value: userID}).
-				WithEnvVar(corev1.EnvVar{Name: constants.EnvVarTaskName, Value: "default_profile"}).
-				WithEnvVar(corev1.EnvVar{
-					Name:  constants.EnvVarTaskID,
-					Value: fmt.Sprintf("%s_%s_default_profile", userID, testStartTime.Format(time.RFC3339)),
+				WithVolume(corev1.Volume{
+					Name: "slurm-env",
+					VolumeSource: corev1.VolumeSource{
+						EmptyDir: &corev1.EmptyDirVolumeSource{},
+					},
```

  That makes sense. Thank you for investigating this.
