# PR #3143: Expose available ResourceFlavors from the ClusterQueue in the LocalQueue status.

**Summary**: Expose available ResourceFlavors from the ClusterQueue in the LocalQueue status.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3143

**Last updated**: 2024-10-19T02:57:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/expose-flavors-in-local-queue-status` → `main`
- **Created**: 2024-09-26T11:51:00Z
- **Updated**: 2024-10-19T02:57:58Z
- **Merged**: 2024-10-18T15:29:04Z
- **Closed**: 2024-10-18T15:29:04Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@KPostOffice](https://github.com/KPostOffice), [@varshaprasad96](https://github.com/varshaprasad96), [@trasc](https://github.com/trasc)
- **Changed files**: 14   **+861 / -156**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Expose available ResourceFlavors from the ClusterQueue in the LocalQueue status.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3122

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Exposed available ResourceFlavors from the ClusterQueue in the LocalQueue status.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-26T11:51:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 216aba17171e7c3333c144cbe5f1d40b09b28f08 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/671273cdf36750000885aa6c |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-26T11:53:50Z

/cc @alculquicondor

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-26T12:08:03Z

/retest pull-kueue-test-integration-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-26T12:08:07Z

@mbobrovskyi: The `/retest` command does not accept any targets.
The following commands are available to trigger required jobs:
* `/test pull-kueue-build-image-main`
* `/test pull-kueue-test-e2e-main-1-28`
* `/test pull-kueue-test-e2e-main-1-29`
* `/test pull-kueue-test-e2e-main-1-30`
* `/test pull-kueue-test-e2e-main-1-31`
* `/test pull-kueue-test-integration-main`
* `/test pull-kueue-test-kjobctl`
* `/test pull-kueue-test-multikueue-e2e-main`
* `/test pull-kueue-test-scheduling-perf-main`
* `/test pull-kueue-test-unit-main`
* `/test pull-kueue-verify-main`

Use `/test all` to run the following jobs that were automatically triggered:
* `pull-kueue-build-image-main`
* `pull-kueue-test-e2e-main-1-28`
* `pull-kueue-test-e2e-main-1-29`
* `pull-kueue-test-e2e-main-1-30`
* `pull-kueue-test-e2e-main-1-31`
* `pull-kueue-test-integration-main`
* `pull-kueue-test-multikueue-e2e-main`
* `pull-kueue-test-scheduling-perf-main`
* `pull-kueue-test-unit-main`
* `pull-kueue-verify-main`



<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3143#issuecomment-2376756827):

>/retest pull-kueue-test-integration-main


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-26T12:08:23Z

/test pull-kueue-test-integration-main

Due to https://github.com/kubernetes-sigs/kueue/issues/3144.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-26T14:20:15Z

Is there any decision to implement this API in the LocalQueue level?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-09-26T16:58:15Z

I don't see any high-level concerns. Do you have any?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-01T14:12:12Z

I don't have any high-level concerns about the approach. However, from the process perspective it might be better to include a small one-pager KEP for it? WDYT @alculquicondor @tenzen-y ?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-01T16:51:29Z

Yeah, it doesn't hurt

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-01T16:55:47Z

> I don't have any high-level concerns about the approach. However, from the process perspective it might be better to include a small one-pager KEP for it? WDYT @alculquicondor @tenzen-y ?

Basically, I agree with this enhancement.
But, I would like to seek the possibility of API since we may want to expose other information related to ResourceFlavor something like topology or nodeSelector.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-01T16:56:51Z

> > I don't have any high-level concerns about the approach. However, from the process perspective it might be better to include a small one-pager KEP for it? WDYT @alculquicondor @tenzen-y ?
> 
> Basically, I agree with this enhancement. But, I would like to seek the possibility of API since we may want to expose other information related to ResourceFlavor something like topology or nodeSelector.

I did not indicate that we should expose that information. But, evaluations would be worth it.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-02T16:08:19Z

> I don't have any high-level concerns about the approach. However, from the process perspective it might be better to include a small one-pager KEP for it? WDYT @alculquicondor @tenzen-y ?

Created https://github.com/kubernetes-sigs/kueue/pull/3181.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-06T15:34:21Z

/hold for #3181

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-15T09:09:19Z

/hold cancel
as the KEP is merged

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-15T09:15:12Z

@mimowo: GitHub didn't allow me to request PR reviews from the following users: for, another, pair, of, eyes.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3143#pullrequestreview-2368661867):

>LGTM, just a minor nit.
>/cc @tenzen-y for another pair of eyes


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-16T11:48:14Z

/hold for https://github.com/kubernetes-sigs/kueue/blob/d9db44460c0b61532b3ca03c7f98ef935dd848ff/keps/3122-expose-flavors-in-localqueue-status/README.md?plain=1#L179-L180

https://github.com/kubernetes-sigs/kueue/pull/3181#discussion_r1795232989

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-16T14:32:29Z

/unhold

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-16T14:41:52Z

/cc @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-17T07:20:33Z

LGTM label has been added.  <details>Git tree hash: 40d0a6fd0c55999a398b2f1b1e14e38d04b6e4fc</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-17T07:20:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3143#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3143#pullrequestreview-2374366205" title="LGTM">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-18T15:27:57Z

I believe all comments are addressed, so ready to go
/lgtm
/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-18T15:28:03Z

LGTM label has been added.  <details>Git tree hash: 13974c483c80f16db679b619367f153e80c9724b</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-26T12:07:47Z

- **pkg/cache/cache.go:691** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-26T12:07:47Z
```diff
@@ -683,11 +685,21 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	availableFlavors := set.New[kueue.ResourceFlavorReference]()
+	for _, rg := range cqImpl.ResourceGroups {
+		for _, fl := range rg.Flavors {
+			if _, ok := c.resourceFlavors[fl]; ok {
```

  The question, should we check that the ResourceFlavor exists? Or just enough the list of available on CQ? In the first case, we would also need to reconcile LocalQueues after updating the ResourceFlavors. 
  
  @mimowo @alculquicondor WDYT?

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2024-09-26T17:37:08Z

- **test/integration/controller/core/localqueue_controller_test.go:147** by [@KPostOffice](https://github.com/KPostOffice) — 2024-09-26T17:35:41Z
```diff
@@ -91,32 +91,60 @@ var _ = ginkgo.Describe("Queue controller", ginkgo.Ordered, ginkgo.ContinueOnFai
 	})
 
 	ginkgo.It("Should update conditions when clusterQueues that its localQueue references are updated", func() {
-		gomega.Eventually(func() []metav1.Condition {
+		gomega.Eventually(func() kueue.LocalQueueStatus {
 			var updatedQueue kueue.LocalQueue
 			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
-			return updatedQueue.Status.Conditions
-		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo([]metav1.Condition{
-			{
-				Type:    kueue.LocalQueueActive,
-				Status:  metav1.ConditionFalse,
-				Reason:  "ClusterQueueDoesNotExist",
-				Message: "Can't submit new workloads to clusterQueue",
+			return updatedQueue.Status
+		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(kueue.LocalQueueStatus{
+			Conditions: []metav1.Condition{
+				{
+					Type:    kueue.LocalQueueActive,
+					Status:  metav1.ConditionFalse,
+					Reason:  "ClusterQueueDoesNotExist",
+					Message: "Can't submit new workloads to clusterQueue",
+				},
 			},
 		}, util.IgnoreConditionTimestampsAndObservedGeneration))
 
+		emptyUsage := []kueue.LocalQueueFlavorUsage{
+			{
+				Name: flavorModelC,
+				Resources: []kueue.LocalQueueResourceUsage{
+					{
+						Name:  resourceGPU,
+						Total: resource.MustParse("0"),
+					},
+				},
+			},
+			{
+				Name: flavorModelD,
+				Resources: []kueue.LocalQueueResourceUsage{
+					{
+						Name:  resourceGPU,
+						Total: resource.MustParse("0"),
+					},
+				},
+			},
+		}
+
 		ginkgo.By("Creating a clusterQueue")
 		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
-		gomega.Eventually(func() []metav1.Condition {
+		gomega.Eventually(func() kueue.LocalQueueStatus {
 			var updatedQueue kueue.LocalQueue
 			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
-			return updatedQueue.Status.Conditions
-		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo([]metav1.Condition{
-			{
-				Type:    kueue.LocalQueueActive,
-				Status:  metav1.ConditionFalse,
-				Reason:  "ClusterQueueIsInactive",
-				Message: "Can't submit new workloads to clusterQueue",
+			return updatedQueue.Status
+		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(kueue.LocalQueueStatus{
+			Conditions: []metav1.Condition{
+				{
+					Type:    kueue.LocalQueueActive,
+					Status:  metav1.ConditionFalse,
+					Reason:  "ClusterQueueIsInactive",
+					Message: "Can't submit new workloads to clusterQueue",
+				},
 			},
+			FlavorsReservation: emptyUsage,
+			FlavorUsage:        emptyUsage,
+			AvailableFlavors:   []kueue.ResourceFlavorReference{"model-c", "model-d"},
```

  Any reason not to do:
  ```suggestion
  			AvailableFlavors:   []kueue.ResourceFlavorReference{flavorModelC, flavorModelD},
  ```
  here and below?

### Review by [@varshaprasad96](https://github.com/varshaprasad96) (commented) — 2024-09-27T02:58:38Z

- **pkg/cache/cache.go:691** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-09-27T02:58:38Z
```diff
@@ -683,11 +685,21 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	availableFlavors := set.New[kueue.ResourceFlavorReference]()
+	for _, rg := range cqImpl.ResourceGroups {
+		for _, fl := range rg.Flavors {
+			if _, ok := c.resourceFlavors[fl]; ok {
```

  Just adding a few thoughts: on running a quick example, where RF is not available, but CQ and LQ are created, the LQ status appears to be:
  ```
  Status:
    Admitted Workloads:  0
    Conditions:
      Last Transition Time:  2024-09-27T02:46:32Z
      Message:               Can't submit new workloads to clusterQueue
      Observed Generation:   1
      Reason:                ClusterQueueIsInactive
      Status:                False
      Type:                  Active
    Flavor Usage:
      Name:  default-flavor
      Resources:
        Name:   cpu
        Total:  0
        Name:   memory
        Total:  0
    Flavors Reservation:
      Name:  default-flavor
      Resources:
        Name:             cpu
        Total:            0
        Name:             memory
        Total:            0
    Pending Workloads:    0
    Reserving Workloads:  0
    ```
  
  The source of truth for populating the status of LQ is the CQ. And the LQ also explicitly bubbles up that the ClusterQueue is in inactive status. Considering the same approach here, it is probably enough to look at the CQ spec for the referenced flavours to populate the status, as we are anyway bubbling up the errors from CQ. One thing that we may change to be more explicit could be to rename the field to be - `availableFlavorsInClusterQueue` to be more explicit that it refers to the snapshot from the cluster queue's spec, rather than the resource flavour's status on cluster?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-27T06:21:04Z

- **test/integration/controller/core/localqueue_controller_test.go:147** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-27T06:21:03Z
```diff
@@ -91,32 +91,60 @@ var _ = ginkgo.Describe("Queue controller", ginkgo.Ordered, ginkgo.ContinueOnFai
 	})
 
 	ginkgo.It("Should update conditions when clusterQueues that its localQueue references are updated", func() {
-		gomega.Eventually(func() []metav1.Condition {
+		gomega.Eventually(func() kueue.LocalQueueStatus {
 			var updatedQueue kueue.LocalQueue
 			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
-			return updatedQueue.Status.Conditions
-		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo([]metav1.Condition{
-			{
-				Type:    kueue.LocalQueueActive,
-				Status:  metav1.ConditionFalse,
-				Reason:  "ClusterQueueDoesNotExist",
-				Message: "Can't submit new workloads to clusterQueue",
+			return updatedQueue.Status
+		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(kueue.LocalQueueStatus{
+			Conditions: []metav1.Condition{
+				{
+					Type:    kueue.LocalQueueActive,
+					Status:  metav1.ConditionFalse,
+					Reason:  "ClusterQueueDoesNotExist",
+					Message: "Can't submit new workloads to clusterQueue",
+				},
 			},
 		}, util.IgnoreConditionTimestampsAndObservedGeneration))
 
+		emptyUsage := []kueue.LocalQueueFlavorUsage{
+			{
+				Name: flavorModelC,
+				Resources: []kueue.LocalQueueResourceUsage{
+					{
+						Name:  resourceGPU,
+						Total: resource.MustParse("0"),
+					},
+				},
+			},
+			{
+				Name: flavorModelD,
+				Resources: []kueue.LocalQueueResourceUsage{
+					{
+						Name:  resourceGPU,
+						Total: resource.MustParse("0"),
+					},
+				},
+			},
+		}
+
 		ginkgo.By("Creating a clusterQueue")
 		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
-		gomega.Eventually(func() []metav1.Condition {
+		gomega.Eventually(func() kueue.LocalQueueStatus {
 			var updatedQueue kueue.LocalQueue
 			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
-			return updatedQueue.Status.Conditions
-		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo([]metav1.Condition{
-			{
-				Type:    kueue.LocalQueueActive,
-				Status:  metav1.ConditionFalse,
-				Reason:  "ClusterQueueIsInactive",
-				Message: "Can't submit new workloads to clusterQueue",
+			return updatedQueue.Status
+		}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(kueue.LocalQueueStatus{
+			Conditions: []metav1.Condition{
+				{
+					Type:    kueue.LocalQueueActive,
+					Status:  metav1.ConditionFalse,
+					Reason:  "ClusterQueueIsInactive",
+					Message: "Can't submit new workloads to clusterQueue",
+				},
 			},
+			FlavorsReservation: emptyUsage,
+			FlavorUsage:        emptyUsage,
+			AvailableFlavors:   []kueue.ResourceFlavorReference{"model-c", "model-d"},
```

  Yeah, that's a good point. Thanks for the review!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-27T06:33:50Z

- **pkg/cache/cache.go:691** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-27T06:33:50Z
```diff
@@ -683,11 +685,21 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	availableFlavors := set.New[kueue.ResourceFlavorReference]()
+	for _, rg := range cqImpl.ResourceGroups {
+		for _, fl := range rg.Flavors {
+			if _, ok := c.resourceFlavors[fl]; ok {
```

  > One thing that we may change to be more explicit could be to rename the field to be - availableFlavorsInClusterQueue to be more explicit that it refers to the snapshot from the cluster queue's spec, rather than the resource flavour's status on cluster?
  
  Yeah, `availableFlavorsInClusterQueue` sounds better. Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-03T07:01:11Z

- **pkg/cache/cache.go:691** by [@mimowo](https://github.com/mimowo) — 2024-10-03T07:01:11Z
```diff
@@ -683,11 +685,21 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	availableFlavors := set.New[kueue.ResourceFlavorReference]()
+	for _, rg := range cqImpl.ResourceGroups {
+		for _, fl := range rg.Flavors {
+			if _, ok := c.resourceFlavors[fl]; ok {
```

  @varshaprasad96 thank you for the input. 
  
  In the KEP I propose a slightly more flexible API as a list of extendable objects: (see related threads: [one](https://github.com/kubernetes-sigs/kueue/pull/3181/files#r1784862198) and [two](https://github.com/kubernetes-sigs/kueue/pull/3181/files#r1784869191)). 
  
  Then, we could in the future convey more information (say about the RF status). As the model is more flexible I think we don't need to necessarily indicate the source of the information in the field name. I think we could just describe the meaning in API comment, and shorten the name. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T09:15:09Z

LGTM, just a minor nit.
/cc @tenzen-y for another pair of eyes

- **apis/kueue/v1beta1/localqueue_types.go:51** by [@mimowo](https://github.com/mimowo) — 2024-10-15T09:12:49Z
```diff
@@ -48,6 +48,31 @@ type LocalQueueSpec struct {
 // +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
 type ClusterQueueReference string
 
+type Flavor struct {
```

  ```suggestion
  type LocalQueueFlavorStatus struct {
  ```
  nit, to indicate that this is used for LocalQueues only, name by analogy to LocalQueueFlavorUsage.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-15T09:42:23Z

- **apis/kueue/v1beta1/localqueue_types.go:51** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-15T09:42:23Z
```diff
@@ -48,6 +48,31 @@ type LocalQueueSpec struct {
 // +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
 type ClusterQueueReference string
 
+type Flavor struct {
```

  Done

### Review by [@varshaprasad96](https://github.com/varshaprasad96) (commented) — 2024-10-15T22:43:46Z

- **pkg/cache/cache.go:705** by [@varshaprasad96](https://github.com/varshaprasad96) — 2024-10-15T22:42:58Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; !ok {
```

  ```suggestion
  				if rif, ok := resourcesInFlavor[rgFlavor]; ok {
  ```
  Only if `rif` is not nil, we add it to `flavour.Resources`, right? If not it could be a nil pointer err.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-16T09:19:00Z

- **pkg/cache/cache.go:705** by [@mimowo](https://github.com/mimowo) — 2024-10-16T09:19:00Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; !ok {
```

  It is surprising to see this changed without impact on test. @mbobrovskyi please make sure this is covered by tests.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-16T11:20:06Z

- **pkg/cache/cache.go:705** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-16T11:20:06Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; !ok {
```

  > Only if rif is not nil, we add it to flavour.Resources, right? If not it could be a nil pointer err.
  
  You right. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-16T11:20:30Z

- **pkg/cache/cache.go:705** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-16T11:20:29Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; !ok {
```

  > It is surprising to see this changed without impact on test. @mbobrovskyi please make sure this is covered by tests.
  
  We have test. This is my mistake.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-17T06:59:30Z

LGTM, just nits

- **apis/kueue/v1beta1/localqueue_types.go:55** by [@mimowo](https://github.com/mimowo) — 2024-10-17T06:54:01Z
```diff
@@ -48,6 +48,31 @@ type LocalQueueSpec struct {
 // +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
 type ClusterQueueReference string
 
+type LocalQueueFlavorStatus struct {
+	// name of the flavor.
+	Name ResourceFlavorReference `json:"name"`
```

  It should probably be marked as required since it is a key in the `Flavors`.

- **pkg/features/kube_features.go:117** by [@mimowo](https://github.com/mimowo) — 2024-10-17T06:58:03Z
```diff
@@ -109,6 +109,13 @@ const (
 	// Enable Topology Aware Scheduling allowing to optimize placement of Pods
 	// to put them on closely located nodes (e.g. within the same rack or block).
 	TopologyAwareScheduling featuregate.Feature = "TopologyAwareScheduling"
+
+	// owner: @mbobrovskyi
+	// beta: v0.9
+	//
+	// Enable the Flavors status field in the LocalQueue, allowing users to view
+	// all currently available ResourceFlavors in the LocalQueue.
```

  ```suggestion
  	// Enable the Flavors status field in the LocalQueue, allowing users to view
  	// all currently available ResourceFlavors for the LocalQueue.
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-17T07:11:21Z

- **apis/kueue/v1beta1/localqueue_types.go:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-17T07:11:21Z
```diff
@@ -48,6 +48,31 @@ type LocalQueueSpec struct {
 // +kubebuilder:validation:Pattern="^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
 type ClusterQueueReference string
 
+type LocalQueueFlavorStatus struct {
+	// name of the flavor.
+	Name ResourceFlavorReference `json:"name"`
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-17T07:11:26Z

- **pkg/features/kube_features.go:117** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-17T07:11:26Z
```diff
@@ -109,6 +109,13 @@ const (
 	// Enable Topology Aware Scheduling allowing to optimize placement of Pods
 	// to put them on closely located nodes (e.g. within the same rack or block).
 	TopologyAwareScheduling featuregate.Feature = "TopologyAwareScheduling"
+
+	// owner: @mbobrovskyi
+	// beta: v0.9
+	//
+	// Enable the Flavors status field in the LocalQueue, allowing users to view
+	// all currently available ResourceFlavors in the LocalQueue.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-17T07:20:27Z

/approve
/lgtm
/hold
in case @tenzen-y has extra comments

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-17T20:05:05Z

- **pkg/cache/cache.go:700** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T19:44:14Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
```

  ```suggestion
  	var flavors map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus
  	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
  		flavors = make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
  ```
  
  Shouldn't we initialize the `flavors` only when we enable this feature gate? Because this initialization could create an empty flavors, which means the flavors has any empty flavor name (.status.flavors.name).
  But, the `name` field is required field, so I guess that the status updates operation could fail when we disable this feature gate.

- **pkg/cache/cache.go:703** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T19:48:22Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
```

  ```suggestion
  		resourcesInFlavor := make(map[kueue.ResourceFlavorReference][]corev1.ResourceName)
  ```
  
  Could you use the slice instead of map so that we can keep showing the same order in the localQueue status field. If not, the lq reconciler accidentally updates the localQueue status based on unstable flavors order.

- **pkg/cache/cache.go:707** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T19:51:04Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
```

  ```suggestion
  					resourcesInFlavor[rgFlavor] = make(corev1.ResourceName, len(rg.CoveredResources))
  ```

- **pkg/cache/cache.go:709** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T19:51:43Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
```

  ```suggestion
  				resourcesInFlavor[rgFlavor] = append(resourcesInFlavor[rgFlavor], sets.List(rg.CoveredResources))
  ```
  
  Same reason as the `resourcesInFlavor`.

- **pkg/cache/cache.go:706** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T19:52:49Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; ok {
+					flavor.Resources = rif.UnsortedList()
```

  ```suggestion
  					flavor.Resources = sets.List(rif)
  ```

- **pkg/cache/cache.go:722** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T19:54:48Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; ok {
+					flavor.Resources = rif.UnsortedList()
+				}
+				if rf, ok := c.resourceFlavors[rgFlavor]; ok {
+					flavor.NodeLabels = rf.Spec.NodeLabels
+					flavor.NodeTaints = rf.Spec.NodeTaints
+				}
+				flavors[rgFlavor] = flavor
+			}
+		}
+	}
+
 	return &LocalQueueUsageStats{
 		ReservedResources:  filterLocalQueueUsage(qImpl.usage, cqImpl.ResourceGroups),
 		ReservingWorkloads: qImpl.reservingWorkloads,
 		AdmittedResources:  filterLocalQueueUsage(qImpl.admittedUsage, cqImpl.ResourceGroups),
 		AdmittedWorkloads:  qImpl.admittedWorkloads,
+		Flavors:            maps.Values(flavors),
```

  Could you sort the flavors by the `kueue.ResourceFlavorReference` so that we can keep the same order in the LocalQueue status.

- **test/integration/controller/core/localqueue_controller_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T20:02:25Z
  Basically, we should propagate the `gomega` instance to the `Eventually`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T00:21:15Z

- **pkg/cache/cache.go:700** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T00:21:15Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
```

  Done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T00:22:16Z

- **pkg/cache/cache.go:706** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T00:22:16Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; ok {
+					flavor.Resources = rif.UnsortedList()
```

  Done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T00:22:23Z

- **pkg/cache/cache.go:722** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T00:22:23Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
+			}
+		}
+
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				flavor := kueue.LocalQueueFlavorStatus{Name: rgFlavor}
+				if rif, ok := resourcesInFlavor[rgFlavor]; ok {
+					flavor.Resources = rif.UnsortedList()
+				}
+				if rf, ok := c.resourceFlavors[rgFlavor]; ok {
+					flavor.NodeLabels = rf.Spec.NodeLabels
+					flavor.NodeTaints = rf.Spec.NodeTaints
+				}
+				flavors[rgFlavor] = flavor
+			}
+		}
+	}
+
 	return &LocalQueueUsageStats{
 		ReservedResources:  filterLocalQueueUsage(qImpl.usage, cqImpl.ResourceGroups),
 		ReservingWorkloads: qImpl.reservingWorkloads,
 		AdmittedResources:  filterLocalQueueUsage(qImpl.admittedUsage, cqImpl.ResourceGroups),
 		AdmittedWorkloads:  qImpl.admittedWorkloads,
+		Flavors:            maps.Values(flavors),
```

  Done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T00:22:28Z

- **test/integration/controller/core/localqueue_controller_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T00:22:28Z
  Done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T00:26:16Z

- **pkg/cache/cache.go:703** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T00:26:16Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
```

  In this case, we might have duplicate resources. However, I believe #3143 (comment) should prevent this issue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-18T09:07:57Z

- **pkg/cache/cache.go:703** by [@mimowo](https://github.com/mimowo) — 2024-10-18T09:07:57Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
```

  +1 for the remark. we should use the same order as in CQ. Don't sort by name as IIUC is done with the `sort.Slice(` below. The order of flavors is important as they are considered in order (based on preference, machine price etc.).
  
  Please also ensure there is a test that the order is an in CQ.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-18T09:09:05Z

Remaining [comment](https://github.com/kubernetes-sigs/kueue/pull/3143/files#r1805332105) would be good to address. Other than that LGTM

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T14:02:18Z

- **pkg/cache/cache.go:703** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T14:02:18Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T14:02:24Z

- **pkg/cache/cache.go:707** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T14:02:24Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T14:02:39Z

- **pkg/cache/cache.go:709** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T14:02:39Z
```diff
@@ -683,11 +687,39 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 		return nil, errQNotFound
 	}
 
+	flavors := make(map[kueue.ResourceFlavorReference]kueue.LocalQueueFlavorStatus)
+	if features.Enabled(features.ExposeFlavorsInLocalQueue) {
+		resourcesInFlavor := make(map[kueue.ResourceFlavorReference]sets.Set[corev1.ResourceName])
+		for _, rg := range cqImpl.ResourceGroups {
+			for _, rgFlavor := range rg.Flavors {
+				if _, ok := resourcesInFlavor[rgFlavor]; !ok {
+					resourcesInFlavor[rgFlavor] = sets.New[corev1.ResourceName]()
+				}
+				resourcesInFlavor[rgFlavor].Insert(rg.CoveredResources.UnsortedList()...)
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-18T19:07:39Z

Thanks for addressing my comments!
/lgtm
