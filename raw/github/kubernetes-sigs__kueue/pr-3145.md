# PR #3145: [test][integration] Relax `CheckLatestEvent` to `CheckEventRecordedFor`.

**Summary**: [test][integration] Relax `CheckLatestEvent` to `CheckEventRecordedFor`.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3145

**Last updated**: 2024-09-30T12:29:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `integration-dont-check-only-last-event` → `main`
- **Created**: 2024-09-26T12:54:52Z
- **Updated**: 2024-09-30T12:29:56Z
- **Merged**: 2024-09-30T07:32:03Z
- **Closed**: 2024-09-30T07:32:03Z
- **Labels**: `kind/bug`, `lgtm`, `kind/failing-test`, `kind/flake`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 8   **+31 / -37**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind failing-test
/kind flake

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
[test][integration] Relax `CheckLatestEvent` to `CheckEventRecordedFor`.

Instead of expecting that an event is recorded last, check that the event was recorded for a specific object.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3144

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
none
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-26T12:55:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 42454ba4713184e5b34ad0e88e17b08fd7ac4967 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66fa4ffd52bff3000862de2d |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-26T12:58:32Z

Is it fix for https://github.com/kubernetes-sigs/kueue/issues/3144?

### Comment by [@trasc](https://github.com/trasc) — 2024-09-26T13:00:27Z

> Is it fix for #3144?

It was not done for it specifically but it's fixing it. 
/cc @mbobrovskyi

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-26T13:02:16Z

> > Is it fix for #3144?
> 
> It was not done for it specifically but it's fixing it. /cc @mbobrovskyi

Awesome. Thanks!

### Comment by [@trasc](https://github.com/trasc) — 2024-09-26T13:09:56Z

/test pull-kueue-test-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-09-26T13:33:25Z

https://github.com/kubernetes-sigs/kueue/issues/3147 created for the e2e flake

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-27T06:35:17Z

@mbobrovskyi would be great if you could provide the first review

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-27T07:02:55Z

/lgtm 
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-27T07:03:02Z

LGTM label has been added.  <details>Git tree hash: 4416005ca2625921d28da81ef68d288fe4ff3ef9</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T07:13:03Z

/test pull-kueue-test-integration-main

Due to https://github.com/kubernetes-sigs/kueue/issues/3149.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-30T07:19:51Z

LGTM label has been added.  <details>Git tree hash: 2e31472a2e7e9320177a2fd9dabac0da9cb23300</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-30T07:19:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3145#pullrequestreview-2336594312" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3145#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [mimowo]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo,trasc]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-27T06:55:52Z

Overall LGTM.

- **pkg/util/testing/core.go:98** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-27T06:38:23Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
+	eventReason string,
+	eventType string, eventNote string, t time.Time) (bool, error) {
+	events := &eventsv1.EventList{}
+	if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+		return false, err
+	}
+
+	length := len(events.Items)
+	if length == 0 {
+		return false, errors.New("no events currently exist")
+	}
+
+	lastIndexAfterTime := 0
+	for i := length - 1; i >= 0; i-- {
+		item := events.Items[i]
+		if item.EventTime.Time.Before(t) {
+			lastIndexAfterTime = i + 1
+		}
+		if item.Reason == eventReason && item.Type == eventType && item.Note == eventNote {
+			return true, nil
+		}
+	}
+
+	return false, fmt.Errorf("event not found fater checking %d events", length-lastIndexAfterTime)
```

  ```suggestion
  	return false, fmt.Errorf("event not found after checking %d events", length-lastIndexAfterTime)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-27T07:59:51Z

- **pkg/util/testing/core.go:74** by [@mimowo](https://github.com/mimowo) — 2024-09-27T07:57:34Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  Instead of relying on time, I suggest to have `CountMachingEventRecorded`, then in the tests record the current count, and await for the count to be incremented.
  
  If we want to search for optimizations I would suggest to see if we could narrow down the List by namespace, or the target object.

- **pkg/util/testing/core.go:87** by [@mimowo](https://github.com/mimowo) — 2024-09-27T07:59:26Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
+	eventReason string,
+	eventType string, eventNote string, t time.Time) (bool, error) {
+	events := &eventsv1.EventList{}
+	if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+		return false, err
+	}
+
+	length := len(events.Items)
+	if length == 0 {
+		return false, errors.New("no events currently exist")
+	}
+
+	lastIndexAfterTime := 0
```

  I don't like the optimization for the iteration over the list by timestamp. It adds complexity (I'm not sure we are even guaranteed that the events are sorted). Also, it does not improve performance in any meaningful way, because the full list needs to be fetched from API server (so serialized there), and deserialized in the client. Investigating potential flakes will eat us much more time than the potential gain.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-30T06:28:44Z

- **pkg/util/testing/core.go:74** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T06:28:44Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  > Instead of relying on time, I suggest to have CountMachingEventRecorded, then in the tests record the current count, and await for the count to be incremented.
  
  Honestly, I'm not sure if this is better than finding by time. I also think we might still encounter flaky tests in this case.
  
  > If we want to search for optimizations I would suggest to see if we could narrow down the List by namespace, or the target object.
  
  Yeah, I think it's make sense.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-30T06:34:52Z

- **pkg/util/testing/core.go:87** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T06:34:52Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
+	eventReason string,
+	eventType string, eventNote string, t time.Time) (bool, error) {
+	events := &eventsv1.EventList{}
+	if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+		return false, err
+	}
+
+	length := len(events.Items)
+	if length == 0 {
+		return false, errors.New("no events currently exist")
+	}
+
+	lastIndexAfterTime := 0
```

  This should help, as all new events are added to the end of the list. In the previous implementation, we also found the last event in the list:
  
  https://github.com/kubernetes-sigs/kueue/blob/4cc9b1a919c16124f9c1632c33404856f1ee9fc4/pkg/util/testing/core.go#L56-L68

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-30T06:36:33Z

- **pkg/util/testing/core.go:74** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T06:36:33Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  Do we need the previous implementation? It doesn't seem to be used anywhere.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-30T06:44:42Z

- **pkg/util/testing/core.go:74** by [@mimowo](https://github.com/mimowo) — 2024-09-30T06:44:42Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  Checking the counter before and after is a common approach, I don't see why this would have a potential to flake, but let me know if I'm missing something.
  
  As for the time-based approach, the potential for flake I see is the optimization based on `lastIndexAfterTime`, whicb relies on the assumption that the list of events is sorted by time, I'm not sure this is always true. If we drop this check I think we can go ahead also with the approach.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-09-30T06:49:10Z

- **pkg/util/testing/core.go:74** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-09-30T06:49:09Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  > Checking the counter before and after is a common approach, I don't see why this would have a potential to flake, but let me know if I'm missing something.
  
  Ah, I got your point. Yeah, it's make sense.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-30T06:54:57Z

- **pkg/util/testing/core.go:74** by [@mimowo](https://github.com/mimowo) — 2024-09-30T06:54:57Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  +1

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-30T06:58:20Z

- **pkg/util/testing/core.go:74** by [@trasc](https://github.com/trasc) — 2024-09-30T06:58:20Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  reworked

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-30T06:58:33Z

- **pkg/util/testing/core.go:87** by [@trasc](https://github.com/trasc) — 2024-09-30T06:58:33Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
+	eventReason string,
+	eventType string, eventNote string, t time.Time) (bool, error) {
+	events := &eventsv1.EventList{}
+	if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+		return false, err
+	}
+
+	length := len(events.Items)
+	if length == 0 {
+		return false, errors.New("no events currently exist")
+	}
+
+	lastIndexAfterTime := 0
```

  reworked

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-30T06:58:45Z

- **pkg/util/testing/core.go:74** by [@trasc](https://github.com/trasc) — 2024-09-30T06:58:45Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  removed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-30T07:01:31Z

- **pkg/util/testing/core.go:65** by [@mimowo](https://github.com/mimowo) — 2024-09-30T07:01:30Z
```diff
@@ -47,26 +46,23 @@ func SingleContainerForRequest(request map[corev1.ResourceName]string) []corev1.
 	}
 }
 
-// CheckLatestEvent will return true if the latest event is as you want.
-func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
-	eventReason string,
-	eventType string, eventNote string) (bool, error) {
-	events := &eventsv1.EventList{}
-	if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+// CheckEventRecordedFor checks if an event identified by eventReason, eventType, eventNote
+// was recorded for the object indentified by ref.
+func CheckEventRecordedFor(ctx context.Context, k8sClient client.Client,
+	eventReason string, eventType string, eventMessage string,
+	ref types.NamespacedName) (bool, error) {
+	events := &corev1.EventList{}
+	if err := k8sClient.List(ctx, events, client.InNamespace(ref.Namespace)); err != nil {
 		return false, err
 	}
 
-	length := len(events.Items)
-	if length == 0 {
-		return false, errors.New("no events currently exist")
-	}
-
-	item := events.Items[length-1]
-	if item.Reason == eventReason && item.Type == eventType && item.Note == eventNote {
-		return true, nil
+	for i := range events.Items {
+		item := &events.Items[i]
+		if item.InvolvedObject.Name == ref.Name && item.Reason == eventReason && item.Type == eventType && item.Message == eventMessage {
+			return true, nil
+		}
 	}
-
-	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
+	return false, fmt.Errorf("event not found fater checking %d events", len(events.Items))
```

  Include the `eventReason`, `eventType`, `eventMessage` and namespace in the error message.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-30T07:02:15Z

- **pkg/util/testing/core.go:74** by [@mimowo](https://github.com/mimowo) — 2024-09-30T07:02:15Z
```diff
@@ -69,6 +70,34 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
 
+// CheckEventRecordedAfter checks if an event identified by eventReason, eventType, eventNote was recorded after the provided time t
+func CheckEventRecordedAfter(ctx context.Context, k8sClient client.Client,
```

  lgtm, left a nit about the error message in the other comment

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-30T07:15:15Z

- **pkg/util/testing/core.go:65** by [@trasc](https://github.com/trasc) — 2024-09-30T07:15:15Z
```diff
@@ -47,26 +46,23 @@ func SingleContainerForRequest(request map[corev1.ResourceName]string) []corev1.
 	}
 }
 
-// CheckLatestEvent will return true if the latest event is as you want.
-func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
-	eventReason string,
-	eventType string, eventNote string) (bool, error) {
-	events := &eventsv1.EventList{}
-	if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+// CheckEventRecordedFor checks if an event identified by eventReason, eventType, eventNote
+// was recorded for the object indentified by ref.
+func CheckEventRecordedFor(ctx context.Context, k8sClient client.Client,
+	eventReason string, eventType string, eventMessage string,
+	ref types.NamespacedName) (bool, error) {
+	events := &corev1.EventList{}
+	if err := k8sClient.List(ctx, events, client.InNamespace(ref.Namespace)); err != nil {
 		return false, err
 	}
 
-	length := len(events.Items)
-	if length == 0 {
-		return false, errors.New("no events currently exist")
-	}
-
-	item := events.Items[length-1]
-	if item.Reason == eventReason && item.Type == eventType && item.Note == eventNote {
-		return true, nil
+	for i := range events.Items {
+		item := &events.Items[i]
+		if item.InvolvedObject.Name == ref.Name && item.Reason == eventReason && item.Type == eventType && item.Message == eventMessage {
+			return true, nil
+		}
 	}
-
-	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
+	return false, fmt.Errorf("event not found fater checking %d events", len(events.Items))
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-30T07:19:45Z

/lgtm
/approve
