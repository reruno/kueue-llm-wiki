# PR #3146: [test][int][multikueue] Ignore error on config namespace multiple creation.

**Summary**: [test][int][multikueue] Ignore error on config namespace multiple creation.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3146

**Last updated**: 2024-09-27T11:05:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `integration-mk-config-namespace-recreate` → `main`
- **Created**: 2024-09-26T13:28:59Z
- **Updated**: 2024-09-27T11:05:25Z
- **Merged**: 2024-09-27T06:32:02Z
- **Closed**: 2024-09-27T06:32:02Z
- **Labels**: `kind/bug`, `lgtm`, `kind/failing-test`, `kind/flake`, `release-note-none`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Changed files**: 1   **+7 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind failing-test
/kind flake
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
[test][int][multikueue] Ignore error on config namespace multiple creation.

Ignore Already Exists error when creating the kueue config namespace.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
none
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-09-26T13:29:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 791d171bb91f0c9ea2bb421e79bd6f339e5f3b97 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66f6314014f648000895ad86 |

### Comment by [@trasc](https://github.com/trasc) — 2024-09-26T15:07:47Z

/retest

https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/3146/pull-kueue-test-integration-main/1839313891822145536 should be solved by #3145

### Comment by [@mimowo](https://github.com/mimowo) — 2024-09-27T06:31:17Z

/lgtm
/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-27T06:31:24Z

LGTM label has been added.  <details>Git tree hash: a3dcae289fc21721cde3519a40a185bedb9a7e96</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-09-27T06:31:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3146#issuecomment-2378500583" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3146#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo,trasc]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-26T14:14:48Z

- **test/integration/multikueue/suite_test.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-26T14:14:41Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  Could we just check if the kueue-system does not found in the cluster before we try to create the namespace?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-26T14:39:15Z

- **test/integration/multikueue/suite_test.go:216** by [@trasc](https://github.com/trasc) — 2024-09-26T14:39:15Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  We can , but we'll need to use the APIReader of the manager since at this point the cache is not yet started, it might decrease a bit the performance... anyway.
  
  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-26T14:40:49Z

- **test/integration/multikueue/suite_test.go:216** by [@mimowo](https://github.com/mimowo) — 2024-09-26T14:40:49Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  but could we then have a race condition - that we checked and it wasn't there, but it is created a couple of ms later?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-26T15:06:52Z

- **test/integration/multikueue/suite_test.go:216** by [@trasc](https://github.com/trasc) — 2024-09-26T15:06:52Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  In this particular test no, the create attempts are done once before the top level specs there is plenty of time in between.
  
  From a generic API POV it could be, and it's a good point for attempting to create it and ignoring the "AlreadyExists" errors.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-26T15:08:47Z

- **test/integration/multikueue/suite_test.go:216** by [@trasc](https://github.com/trasc) — 2024-09-26T15:08:47Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  @tenzen-y @mimowo let me know what you think it's the best way to go.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-26T15:12:21Z

- **test/integration/multikueue/suite_test.go:216** by [@mimowo](https://github.com/mimowo) — 2024-09-26T15:12:21Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  out of the two options I prefer ignoring "AlreadyExists", but is there another option: wait in eventually for async creation of kueue-system?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-26T15:17:29Z

- **test/integration/multikueue/suite_test.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-26T15:17:29Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  I think that the best solution depends on the root cause.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-26T15:24:29Z

- **test/integration/multikueue/suite_test.go:216** by [@trasc](https://github.com/trasc) — 2024-09-26T15:24:29Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  The cause it's very simple  `managerAndMultiKueueSetup` it's called twice (currently), depending on the ginkgo parallelism used in the same "cluster" (p=1) or in different "clusters" (p>1).
  
  In case of p=1 we attempt to create it twice in the same cluster.
  
  I personally also prefer the initial , `IgnoreAlreadyExists`, approach.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-26T15:29:14Z

- **test/integration/multikueue/suite_test.go:216** by [@mimowo](https://github.com/mimowo) — 2024-09-26T15:29:14Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  Thanks for explaining, I was also puzzled. In that case we couldn't wait as I suggested before.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-26T15:33:09Z

- **test/integration/multikueue/suite_test.go:216** by [@mimowo](https://github.com/mimowo) — 2024-09-26T15:33:09Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  But could we call managerAndMultiKueueSetup sequentially in this case. I suppose the time gain is negligible here, but it increases complexity. Is this changed by the recent manager integration reuse PR?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-26T15:43:09Z

- **test/integration/multikueue/suite_test.go:216** by [@trasc](https://github.com/trasc) — 2024-09-26T15:43:09Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  > But could we call managerAndMultiKueueSetup sequentially in this case. 
  
  The calls are far apart, One when the suite with GC is starting one when the one without GC starts. 
  (For ginkgo parallel runs it happens in different OS processes using different clusters ).
  
  > Is this changed by the recent manager integration reuse PR?
  
  Yes previously the envtest "clusters" wold have bean recreated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-09-26T15:46:43Z

- **test/integration/multikueue/suite_test.go:216** by [@mimowo](https://github.com/mimowo) — 2024-09-26T15:46:43Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  Ok another option. Could we create the namespace before calling the function? It is a core namespace for kueue so sounds reasonable to be present before the manager starts

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-09-26T15:49:37Z

- **test/integration/multikueue/suite_test.go:216** by [@trasc](https://github.com/trasc) — 2024-09-26T15:49:36Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  ~~That will be somewhere in the framework or extend the framework pass yet another init function.... it's kind of overkill since this is the only place we need it.~~
  
  EDIT: 
  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-09-27T11:05:25Z

- **test/integration/multikueue/suite_test.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2024-09-27T11:05:25Z
```diff
@@ -213,7 +213,7 @@ func managerAndMultiKueueSetup(ctx context.Context, mgr manager.Manager, gcInter
 			Name: "kueue-system",
 		},
 	}
-	gomega.Expect(mgr.GetClient().Create(ctx, managersConfigNamespace)).To(gomega.Succeed())
+	gomega.Expect(client.IgnoreAlreadyExists(mgr.GetClient().Create(ctx, managersConfigNamespace))).To(gomega.Succeed())
```

  @trasc @mimowo Thank you for moving this forward. That sounds good to me.
