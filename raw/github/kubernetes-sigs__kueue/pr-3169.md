# PR #3169: [kjobctl] Add --mem flag to Slurm mode

**Summary**: [kjobctl] Add --mem flag to Slurm mode

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3169

**Last updated**: 2024-10-03T09:24:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/kjobctl-add-mem-flag` → `main`
- **Created**: 2024-10-01T09:50:06Z
- **Updated**: 2024-10-03T09:24:41Z
- **Merged**: 2024-10-03T09:24:27Z
- **Closed**: 2024-10-03T09:24:27Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mwielgus](https://github.com/mwielgus), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 9   **+193 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:
Adds `--mem` flag to set memory per pod (slurm node).

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-01T09:50:21Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f91e0d8fc29ad64bbbe0393cf0b093bc68d0c9d8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66fe5d6075dc4a0008c9bde5 |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-10-01T10:16:40Z

/cc @mbobrovskyi

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-10-01T11:45:41Z

> Do we need to update `// +kubebuilder:validation:MaxItems=12` on `RequiredFlags`?

I think it's not needed for this flag as `--mem`, `--mem-per-task`, `--mem-per-cpu` and `--mem-per-gpu` are mutually exclusive.

However, I think it's needed for `--priority` flag. I'm going to update it in #3171.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T12:44:25Z

> > Do we need to update `// +kubebuilder:validation:MaxItems=12` on `RequiredFlags`?
> 
> I think it's not needed for this flag as `--mem`, `--mem-per-task`, `--mem-per-cpu` and `--mem-per-gpu` are mutually exclusive.
> 
> However, I think it's needed for `--priority` flag. I'm going to update it in #3171.

Makes sense. Thank you for the explanation.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T18:01:00Z

/lgtm 

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-01T18:01:07Z

LGTM label has been added.  <details>Git tree hash: c15c442a72318bf2451a4aa74a73f6f3aa09ecb0</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-01T18:10:08Z

/assign @mwielgus

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-02T08:26:36Z

LGTM label has been added.  <details>Git tree hash: 6635d3234f8339e5ec0e0fd290843d4345fe1a86</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-02T15:26:53Z

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-03T09:24:10Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-03T09:24:16Z

LGTM label has been added.  <details>Git tree hash: 65eff81fbf2a96af437db92cc39356638d6a25f9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-03T09:24:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3169#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3169#issuecomment-2390935800" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T10:28:55Z

Do we need to update `// +kubebuilder:validation:MaxItems=12` on `RequiredFlags`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T10:47:51Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:244** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T10:47:51Z
```diff
@@ -238,6 +239,9 @@ func (b *slurmBuilder) build(ctx context.Context) (runtime.Object, []runtime.Obj
 		gpusPerTask.Add(*number)
 	}
 
+	totalContainers := len(job.Spec.Template.Spec.Containers)
+	memPerNode := quantityToFloat(b.memPerNode)
+	memPerContainer := int64(math.Ceil(memPerNode / float64(totalContainers)))
```

  ```suggestion
  	memPerContainer := b.memPerNode.Mul(int64(1 / len(job.Spec.Template.Spec.Containers)))
  ```
  
  Maybe like this?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-10-01T11:47:36Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:244** by [@IrvingMg](https://github.com/IrvingMg) — 2024-10-01T11:47:36Z
```diff
@@ -238,6 +239,9 @@ func (b *slurmBuilder) build(ctx context.Context) (runtime.Object, []runtime.Obj
 		gpusPerTask.Add(*number)
 	}
 
+	totalContainers := len(job.Spec.Template.Spec.Containers)
+	memPerNode := quantityToFloat(b.memPerNode)
+	memPerContainer := int64(math.Ceil(memPerNode / float64(totalContainers)))
```

  Not sure that works, I think the result of `int64(1 / len(job.Spec.Template.Spec.Containers))` will always be `0`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-01T12:58:19Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:244** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-01T12:58:19Z
```diff
@@ -238,6 +239,9 @@ func (b *slurmBuilder) build(ctx context.Context) (runtime.Object, []runtime.Obj
 		gpusPerTask.Add(*number)
 	}
 
+	totalContainers := len(job.Spec.Template.Spec.Containers)
+	memPerNode := quantityToFloat(b.memPerNode)
+	memPerContainer := int64(math.Ceil(memPerNode / float64(totalContainers)))
```

  On this case maybe like this?
  
  ```
  limits[corev1.ResourceMemory] = *resource.NewMilliQuantity(b.memPerNode.MilliValue()/int64(len(job.Spec.Template.Spec.Containers)), b.memPerNode.Format)
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-10-01T17:51:40Z

- **cmd/experimental/kjobctl/pkg/builder/slurm_builder.go:244** by [@IrvingMg](https://github.com/IrvingMg) — 2024-10-01T17:51:39Z
```diff
@@ -238,6 +239,9 @@ func (b *slurmBuilder) build(ctx context.Context) (runtime.Object, []runtime.Obj
 		gpusPerTask.Add(*number)
 	}
 
+	totalContainers := len(job.Spec.Template.Spec.Containers)
+	memPerNode := quantityToFloat(b.memPerNode)
+	memPerContainer := int64(math.Ceil(memPerNode / float64(totalContainers)))
```

  Done. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-02T08:26:30Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-03T07:35:51Z

LGTM, asked for extra test when we need to use another unit after division.

- **cmd/experimental/kjobctl/pkg/cmd/create/create_test.go:1134** by [@mimowo](https://github.com/mimowo) — 2024-10-03T07:34:56Z
```diff
@@ -1131,6 +1131,72 @@ error_path=$(unmask_filename "$SBATCH_ERROR")
 			},
 			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
 		},
+		"should create slurm with --mem flag": {
```

  Could you add a test case for --mem=1G, to see how the memory will be formatted after dividing (as we need to use another unit)

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-10-03T08:47:29Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create_test.go:1134** by [@IrvingMg](https://github.com/IrvingMg) — 2024-10-03T08:47:29Z
```diff
@@ -1131,6 +1131,72 @@ error_path=$(unmask_filename "$SBATCH_ERROR")
 			},
 			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
 		},
+		"should create slurm with --mem flag": {
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-03T08:53:49Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create_test.go:1238** by [@mimowo](https://github.com/mimowo) — 2024-10-03T08:53:49Z
```diff
@@ -1131,6 +1131,138 @@ error_path=$(unmask_filename "$SBATCH_ERROR")
 			},
 			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
 		},
+		"should divide --mem exactly across containers": {
+			beforeTest: beforeSlurmTest,
+			afterTest:  afterSlurmTest,
+			args: func(tc *createCmdTestCase) []string {
+				return []string{
+					"slurm",
+					"--profile", "profile",
+					"--",
+					"--mem", "2G",
+					tc.tempFile,
+				}
+			},
+			kjobctlObjs: []runtime.Object{
+				wrappers.MakeJobTemplate("slurm-job-template", metav1.NamespaceDefault).
+					WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").Obj()).
+					WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").Obj()).
+					Obj(),
+				wrappers.MakeApplicationProfile("profile", metav1.NamespaceDefault).
+					WithSupportedMode(*wrappers.MakeSupportedMode(v1alpha1.SlurmMode, "slurm-job-template").Obj()).
+					Obj(),
+			},
+			gvks: []schema.GroupVersionKind{
+				{Group: "batch", Version: "v1", Kind: "Job"},
+				{Group: "", Version: "v1", Kind: "ConfigMap"},
+			},
+			wantLists: []runtime.Object{
+				&batchv1.JobList{
+					TypeMeta: metav1.TypeMeta{Kind: "JobList", APIVersion: "batch/v1"},
+					Items: []batchv1.Job{
+						*wrappers.MakeJob("", metav1.NamespaceDefault).
+							Completions(1).
+							CompletionMode(batchv1.IndexedCompletion).
+							Profile("profile").
+							Mode(v1alpha1.SlurmMode).
+							WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("1G"),
+									},
+								}).
+								Obj()).
+							WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("1G"),
+									},
+								}).
+								Obj()).
+							Obj(),
+					},
+				},
+				&corev1.ConfigMapList{},
+			},
+			cmpopts: []cmp.Option{
+				cmpopts.IgnoreFields(corev1.LocalObjectReference{}, "Name"),
+				cmpopts.IgnoreFields(metav1.OwnerReference{}, "Name"),
+				cmpopts.IgnoreFields(corev1.PodSpec{}, "InitContainers"),
+				cmpopts.IgnoreTypes([]corev1.EnvVar{}),
+				cmpopts.IgnoreTypes([]corev1.Volume{}),
+				cmpopts.IgnoreTypes([]corev1.VolumeMount{}),
+				cmpopts.IgnoreTypes(corev1.ConfigMapList{}),
+			},
+			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
+		},
+		"should handle non-exact --mem division across containers": {
+			beforeTest: beforeSlurmTest,
+			afterTest:  afterSlurmTest,
+			args: func(tc *createCmdTestCase) []string {
+				return []string{
+					"slurm",
+					"--profile", "profile",
+					"--",
+					"--mem", "1G",
+					tc.tempFile,
+				}
+			},
+			kjobctlObjs: []runtime.Object{
+				wrappers.MakeJobTemplate("slurm-job-template", metav1.NamespaceDefault).
+					WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").Obj()).
+					WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").Obj()).
+					Obj(),
+				wrappers.MakeApplicationProfile("profile", metav1.NamespaceDefault).
+					WithSupportedMode(*wrappers.MakeSupportedMode(v1alpha1.SlurmMode, "slurm-job-template").Obj()).
+					Obj(),
+			},
+			gvks: []schema.GroupVersionKind{
+				{Group: "batch", Version: "v1", Kind: "Job"},
+				{Group: "", Version: "v1", Kind: "ConfigMap"},
+			},
+			wantLists: []runtime.Object{
+				&batchv1.JobList{
+					TypeMeta: metav1.TypeMeta{Kind: "JobList", APIVersion: "batch/v1"},
+					Items: []batchv1.Job{
+						*wrappers.MakeJob("", metav1.NamespaceDefault).
+							Completions(1).
+							CompletionMode(batchv1.IndexedCompletion).
+							Profile("profile").
+							Mode(v1alpha1.SlurmMode).
+							WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("0.5G"),
```

  Why is this specified differently between containers? Can we align it? If we choose 0.5G test if the partial settings work ok e2e. I would suppose it is safer to use the lower unit as below: 500M

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-10-03T09:02:13Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create_test.go:1238** by [@IrvingMg](https://github.com/IrvingMg) — 2024-10-03T09:02:13Z
```diff
@@ -1131,6 +1131,138 @@ error_path=$(unmask_filename "$SBATCH_ERROR")
 			},
 			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
 		},
+		"should divide --mem exactly across containers": {
+			beforeTest: beforeSlurmTest,
+			afterTest:  afterSlurmTest,
+			args: func(tc *createCmdTestCase) []string {
+				return []string{
+					"slurm",
+					"--profile", "profile",
+					"--",
+					"--mem", "2G",
+					tc.tempFile,
+				}
+			},
+			kjobctlObjs: []runtime.Object{
+				wrappers.MakeJobTemplate("slurm-job-template", metav1.NamespaceDefault).
+					WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").Obj()).
+					WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").Obj()).
+					Obj(),
+				wrappers.MakeApplicationProfile("profile", metav1.NamespaceDefault).
+					WithSupportedMode(*wrappers.MakeSupportedMode(v1alpha1.SlurmMode, "slurm-job-template").Obj()).
+					Obj(),
+			},
+			gvks: []schema.GroupVersionKind{
+				{Group: "batch", Version: "v1", Kind: "Job"},
+				{Group: "", Version: "v1", Kind: "ConfigMap"},
+			},
+			wantLists: []runtime.Object{
+				&batchv1.JobList{
+					TypeMeta: metav1.TypeMeta{Kind: "JobList", APIVersion: "batch/v1"},
+					Items: []batchv1.Job{
+						*wrappers.MakeJob("", metav1.NamespaceDefault).
+							Completions(1).
+							CompletionMode(batchv1.IndexedCompletion).
+							Profile("profile").
+							Mode(v1alpha1.SlurmMode).
+							WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("1G"),
+									},
+								}).
+								Obj()).
+							WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("1G"),
+									},
+								}).
+								Obj()).
+							Obj(),
+					},
+				},
+				&corev1.ConfigMapList{},
+			},
+			cmpopts: []cmp.Option{
+				cmpopts.IgnoreFields(corev1.LocalObjectReference{}, "Name"),
+				cmpopts.IgnoreFields(metav1.OwnerReference{}, "Name"),
+				cmpopts.IgnoreFields(corev1.PodSpec{}, "InitContainers"),
+				cmpopts.IgnoreTypes([]corev1.EnvVar{}),
+				cmpopts.IgnoreTypes([]corev1.Volume{}),
+				cmpopts.IgnoreTypes([]corev1.VolumeMount{}),
+				cmpopts.IgnoreTypes(corev1.ConfigMapList{}),
+			},
+			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
+		},
+		"should handle non-exact --mem division across containers": {
+			beforeTest: beforeSlurmTest,
+			afterTest:  afterSlurmTest,
+			args: func(tc *createCmdTestCase) []string {
+				return []string{
+					"slurm",
+					"--profile", "profile",
+					"--",
+					"--mem", "1G",
+					tc.tempFile,
+				}
+			},
+			kjobctlObjs: []runtime.Object{
+				wrappers.MakeJobTemplate("slurm-job-template", metav1.NamespaceDefault).
+					WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").Obj()).
+					WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").Obj()).
+					Obj(),
+				wrappers.MakeApplicationProfile("profile", metav1.NamespaceDefault).
+					WithSupportedMode(*wrappers.MakeSupportedMode(v1alpha1.SlurmMode, "slurm-job-template").Obj()).
+					Obj(),
+			},
+			gvks: []schema.GroupVersionKind{
+				{Group: "batch", Version: "v1", Kind: "Job"},
+				{Group: "", Version: "v1", Kind: "ConfigMap"},
+			},
+			wantLists: []runtime.Object{
+				&batchv1.JobList{
+					TypeMeta: metav1.TypeMeta{Kind: "JobList", APIVersion: "batch/v1"},
+					Items: []batchv1.Job{
+						*wrappers.MakeJob("", metav1.NamespaceDefault).
+							Completions(1).
+							CompletionMode(batchv1.IndexedCompletion).
+							Profile("profile").
+							Mode(v1alpha1.SlurmMode).
+							WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("0.5G"),
```

  Yeah, It seems it returns the lower unit... Changed to M. Thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-03T09:23:51Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create_test.go:1238** by [@mimowo](https://github.com/mimowo) — 2024-10-03T09:23:51Z
```diff
@@ -1131,6 +1131,138 @@ error_path=$(unmask_filename "$SBATCH_ERROR")
 			},
 			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
 		},
+		"should divide --mem exactly across containers": {
+			beforeTest: beforeSlurmTest,
+			afterTest:  afterSlurmTest,
+			args: func(tc *createCmdTestCase) []string {
+				return []string{
+					"slurm",
+					"--profile", "profile",
+					"--",
+					"--mem", "2G",
+					tc.tempFile,
+				}
+			},
+			kjobctlObjs: []runtime.Object{
+				wrappers.MakeJobTemplate("slurm-job-template", metav1.NamespaceDefault).
+					WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").Obj()).
+					WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").Obj()).
+					Obj(),
+				wrappers.MakeApplicationProfile("profile", metav1.NamespaceDefault).
+					WithSupportedMode(*wrappers.MakeSupportedMode(v1alpha1.SlurmMode, "slurm-job-template").Obj()).
+					Obj(),
+			},
+			gvks: []schema.GroupVersionKind{
+				{Group: "batch", Version: "v1", Kind: "Job"},
+				{Group: "", Version: "v1", Kind: "ConfigMap"},
+			},
+			wantLists: []runtime.Object{
+				&batchv1.JobList{
+					TypeMeta: metav1.TypeMeta{Kind: "JobList", APIVersion: "batch/v1"},
+					Items: []batchv1.Job{
+						*wrappers.MakeJob("", metav1.NamespaceDefault).
+							Completions(1).
+							CompletionMode(batchv1.IndexedCompletion).
+							Profile("profile").
+							Mode(v1alpha1.SlurmMode).
+							WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("1G"),
+									},
+								}).
+								Obj()).
+							WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("1G"),
+									},
+								}).
+								Obj()).
+							Obj(),
+					},
+				},
+				&corev1.ConfigMapList{},
+			},
+			cmpopts: []cmp.Option{
+				cmpopts.IgnoreFields(corev1.LocalObjectReference{}, "Name"),
+				cmpopts.IgnoreFields(metav1.OwnerReference{}, "Name"),
+				cmpopts.IgnoreFields(corev1.PodSpec{}, "InitContainers"),
+				cmpopts.IgnoreTypes([]corev1.EnvVar{}),
+				cmpopts.IgnoreTypes([]corev1.Volume{}),
+				cmpopts.IgnoreTypes([]corev1.VolumeMount{}),
+				cmpopts.IgnoreTypes(corev1.ConfigMapList{}),
+			},
+			wantOutPattern: `job\.batch\/.+ created\\nconfigmap\/.+ created`,
+		},
+		"should handle non-exact --mem division across containers": {
+			beforeTest: beforeSlurmTest,
+			afterTest:  afterSlurmTest,
+			args: func(tc *createCmdTestCase) []string {
+				return []string{
+					"slurm",
+					"--profile", "profile",
+					"--",
+					"--mem", "1G",
+					tc.tempFile,
+				}
+			},
+			kjobctlObjs: []runtime.Object{
+				wrappers.MakeJobTemplate("slurm-job-template", metav1.NamespaceDefault).
+					WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").Obj()).
+					WithContainer(*wrappers.MakeContainer("c2", "bash:4.4").Obj()).
+					Obj(),
+				wrappers.MakeApplicationProfile("profile", metav1.NamespaceDefault).
+					WithSupportedMode(*wrappers.MakeSupportedMode(v1alpha1.SlurmMode, "slurm-job-template").Obj()).
+					Obj(),
+			},
+			gvks: []schema.GroupVersionKind{
+				{Group: "batch", Version: "v1", Kind: "Job"},
+				{Group: "", Version: "v1", Kind: "ConfigMap"},
+			},
+			wantLists: []runtime.Object{
+				&batchv1.JobList{
+					TypeMeta: metav1.TypeMeta{Kind: "JobList", APIVersion: "batch/v1"},
+					Items: []batchv1.Job{
+						*wrappers.MakeJob("", metav1.NamespaceDefault).
+							Completions(1).
+							CompletionMode(batchv1.IndexedCompletion).
+							Profile("profile").
+							Mode(v1alpha1.SlurmMode).
+							WithContainer(*wrappers.MakeContainer("c1", "bash:4.4").
+								Command("bash", "/slurm/scripts/entrypoint.sh").
+								WithResources(corev1.ResourceRequirements{
+									Limits: corev1.ResourceList{
+										corev1.ResourceMemory: resource.MustParse("0.5G"),
```

  Ah, ok so this was just test assert that was inconsistent, thanks for the update.
