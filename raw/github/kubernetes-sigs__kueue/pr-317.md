# PR #317: Default requests to limits in Workload

**Summary**: Default requests to limits in Workload

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/317

**Last updated**: 2022-08-10T16:13:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `requests` → `main`
- **Created**: 2022-08-09T18:31:06Z
- **Updated**: 2022-08-10T16:13:29Z
- **Merged**: 2022-08-10T02:44:30Z
- **Closed**: 2022-08-10T02:44:30Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+156 / -0**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Default requests to limits if requests are not set.
This matches the internal defaulting for k8s Pods.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #316

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-09T18:31:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/317#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-08-10T02:43:34Z

/lgtm

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-10T02:43:27Z

- **apis/kueue/webhooks/workload_webhook.go:70** by [@kerthcet](https://github.com/kerthcet) — 2022-08-10T02:43:23Z
```diff
@@ -57,10 +58,28 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		if len(podSet.Name) == 0 {
 			podSet.Name = kueue.DefaultPodSetName
 		}
+		setContainersDefaults(podSet.Spec.InitContainers)
+		setContainersDefaults(podSet.Spec.Containers)
 	}
 	return nil
 }
 
+func setContainersDefaults(containers []corev1.Container) {
+	for i := range containers {
+		c := &containers[i]
+		if c.Resources.Limits != nil {
```

  Not related to this PR, we currently leverage the requests in resource quota, should we also consider the limits.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-10T15:24:59Z

- **apis/kueue/webhooks/workload_webhook.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-10T15:24:59Z
```diff
@@ -57,10 +58,28 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		if len(podSet.Name) == 0 {
 			podSet.Name = kueue.DefaultPodSetName
 		}
+		setContainersDefaults(podSet.Spec.InitContainers)
+		setContainersDefaults(podSet.Spec.Containers)
 	}
 	return nil
 }
 
+func setContainersDefaults(containers []corev1.Container) {
+	for i := range containers {
+		c := &containers[i]
+		if c.Resources.Limits != nil {
```

  Are you talking about kubernetes' ResourceQuotas? If so, I would imagine they run after defaults are applied, but I don't have expertise to know for sure.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-10T16:04:53Z

- **apis/kueue/webhooks/workload_webhook.go:70** by [@kerthcet](https://github.com/kerthcet) — 2022-08-10T16:04:53Z
```diff
@@ -57,10 +58,28 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		if len(podSet.Name) == 0 {
 			podSet.Name = kueue.DefaultPodSetName
 		}
+		setContainersDefaults(podSet.Spec.InitContainers)
+		setContainersDefaults(podSet.Spec.Containers)
 	}
 	return nil
 }
 
+func setContainersDefaults(containers []corev1.Container) {
+	for i := range containers {
+		c := &containers[i]
+		if c.Resources.Limits != nil {
```

  I mean the resources.Limits in podSpec.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-10T16:13:29Z

- **apis/kueue/webhooks/workload_webhook.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-10T16:13:29Z
```diff
@@ -57,10 +58,28 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		if len(podSet.Name) == 0 {
 			podSet.Name = kueue.DefaultPodSetName
 		}
+		setContainersDefaults(podSet.Spec.InitContainers)
+		setContainersDefaults(podSet.Spec.Containers)
 	}
 	return nil
 }
 
+func setContainersDefaults(containers []corev1.Container) {
+	for i := range containers {
+		c := &containers[i]
+		if c.Resources.Limits != nil {
```

  It was the intention that we only look at requests, similar to the NodeResources Filter plugin in kube-scheduler.
