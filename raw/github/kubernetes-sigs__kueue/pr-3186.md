# PR #3186: Make LossLessDefaulter selective to only prevent dropping fields outside of schema.

**Summary**: Make LossLessDefaulter selective to only prevent dropping fields outside of schema.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3186

**Last updated**: 2025-03-05T06:48:45Z

---

## Metadata

- **State**: closed
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/make-loss-less-defaulter-selective-to-only-prevent-dropping-fields-outside-of-schema` → `main`
- **Created**: 2024-10-03T18:04:05Z
- **Updated**: 2025-03-05T06:48:45Z
- **Merged**: —
- **Closed**: 2024-10-25T09:58:04Z
- **Labels**: `kind/feature`, `release-note-none`, `size/XL`, `do-not-merge/work-in-progress`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor)
- **Changed files**: 2   **+634 / -31**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Make `LossLessDefaulter` selective to only prevent dropping fields outside of schema.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3174

#### Special notes for your reviewer:
For more information see:

- https://jsonpatch.com/
- https://datatracker.ietf.org/doc/html/rfc6902/
- https://datatracker.ietf.org/doc/html/rfc6901

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-03T18:04:21Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ac7821f3c4fc8e573540b1b4ea7b20b54019bc70 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67082c12c48c370008faf46c |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-03T18:50:09Z

/cc @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-04T12:06:36Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3186#" title="Author self-approved">mbobrovskyi</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [kerthcet](https://github.com/kerthcet) for approval. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["kerthcet"]} -->

### Comment by [@trasc](https://github.com/trasc) — 2024-10-04T13:36:39Z

Maybe I'm missing something but an alternative (slightly hacky) can be to overwrite the Raw object before the actual defaulter is called.


Something like:
```diff
import (
        "context"
+       "encoding/json"
+       "net/http"
-       jsonpatch "gomodules.xyz/jsonpatch/v2"
        "k8s.io/apimachinery/pkg/runtime"
        "sigs.k8s.io/controller-runtime/pkg/webhook/admission"
)
@@ -29,11 +30,15 @@ import (
func WithLosslessDefaulter(scheme *runtime.Scheme, obj runtime.Object, defaulter admission.CustomDefaulter) admission.Handler {
        return &losslessDefaulter{
                Handler: admission.WithCustomDefaulter(scheme, obj, defaulter).Handler,
+               obj:     obj.DeepCopyObject(),
+               decoder: admission.NewDecoder(scheme),
        }
}
type losslessDefaulter struct {
        admission.Handler
+       obj     runtime.Object
+       decoder admission.Decoder
}
// Handle handles admission requests, **dropping** remove operations from patches produced by controller-runtime.
@@ -43,18 +48,14 @@ type losslessDefaulter struct {
// released CRDs.
// Dropping the "remove" operations is safe because Kueue's job mutators never remove fields.
func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) admission.Response {
-       response := h.Handler.Handle(ctx, req)
-       if response.Allowed {
-               var patches []jsonpatch.Operation
-               for _, p := range response.Patches {
-                       if p.Operation != "remove" {
-                               patches = append(patches, p)
-                       }
-               }
-               if len(patches) == 0 {
-                       response.PatchType = nil
-               }
-               response.Patches = patches
+       o := h.obj.DeepCopyObject()
+       if err := h.decoder.Decode(req, o); err != nil {
+               return admission.Errored(http.StatusBadRequest, err)
        }
-       return response
+       marshalled, err := json.Marshal(o)
+       if err != nil {
+               return admission.Errored(http.StatusInternalServerError, err)
+       }
+       req.Object.Raw = marshalled
+       return h.Handler.Handle(ctx, req)
}
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-07T07:59:16Z

> Maybe I'm missing something but an alternative (slightly hacky) can be to overwrite the Raw object before the actual defaulter is called.

I'm not sure about that, maybe @alculquicondor can you think of some pros & cons of the alternative?

### Comment by [@trasc](https://github.com/trasc) — 2024-10-07T09:02:03Z

> > Maybe I'm missing something but an alternative (slightly hacky) can be to overwrite the Raw object before the actual defaulter is called.
> 
> I'm not sure about that, maybe @alculquicondor can you think of some pros & cons of the alternative?

I did find some discussions about that an the probability to generate invalid patches.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T11:56:51Z

> Maybe fieldExistsByJSONPath(object interface{}, path string) and friends can be moved in pkg/util/api.

We already discussed it here https://github.com/kubernetes-sigs/kueue/pull/3186#discussion_r1787524717 and take a decision to keep it on the same package.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T17:37:02Z

/close

Due to found better solution https://github.com/kubernetes-sigs/kueue/pull/3194.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-07T17:37:08Z

@mbobrovskyi: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3186#issuecomment-2397515688):

>/close
>
>Due to found better solution.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-10T15:54:02Z

/reopen

Due to https://github.com/kubernetes-sigs/kueue/pull/3194#discussion_r1795708267.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-10T15:54:09Z

@mbobrovskyi: Reopened this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3186#issuecomment-2405494094):

>/reopen


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T09:58:00Z

/close

Due to very hard to handle inline object and Unmarshal methods.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-25T09:58:05Z

@mbobrovskyi: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3186#issuecomment-2437379335):

>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T10:04:49Z

I think this extension is quite complex and we need much better test coverage, optimally 100%, including cases like removing from a slice (for example to drop a condition). 

It is essential piece of code for correctness of webhooks so as a bailout mechanism, in case of an issue, I would like to consider a construction parameter to control this selective behavior. Such as "AllowRemovingSchemaFields".

- **pkg/util/jsonpatch/jsonpatch.go:34** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:00:17Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
```

  Make sure this is 100% covered by unit tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T10:12:26Z

- **pkg/util/jsonpatch/jsonpatch.go:34** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:12:25Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
```

  Sorry, I missed TestFieldExistsByJSONPath, feel free to resolve the comment. I will read the tests and open another comment if I see a gap.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T10:15:51Z

- **pkg/util/jsonpatch/jsonpatch.go:34** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T10:15:51Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
```

  Yeah, it's covered 100%. But maybe I missed some specific Path.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T10:58:03Z

- **pkg/util/jsonpatch/jsonpatch.go:28** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:32:16Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
```

  Is this case `len(pathParts) > 0 && pathParts[0] != ""` covered by tests? It would be like `foo/something` or `foo/` right?

- **pkg/util/jsonpatch/jsonpatch.go:27** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:32:52Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
```

  Could you provide some reference for the syntax of a valid path?

- **pkg/util/jsonpatch/jsonpatch.go:2** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:38:15Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
```

  TBH I would prefer to keep this file in the same package as the defaulter. So that we don't export the functions. I also don't foresee its use outside of the package. Ideally, we also promote the package as is to controller-runtime one day.

- **pkg/util/jsonpatch/jsonpatch.go:58** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:46:26Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
```

  why `len(tagParts) > 1` what if  `json:"foo"`, then `len(tagParts)=1, and tag should be `foo`, no? 
  
  Is this covered by unit tests?

- **pkg/controller/jobframework/webhook/defaulter_test.go:40** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:54:36Z
```diff
@@ -37,6 +37,7 @@ var (
 
 type TestResource struct {
 	Foo string `json:"foo,omitempty"`
+	Bar string `json:"bar,omitempty"`
```

  Could you please also add higher level tests, to make sure the following scenarios work ok:
  - removal of list of strings (like finalizers), by assigning the list to nil
  - removal of a specific item on a list (like condition), say drop first condition
  - removal of a specific field in one of the conditions (say of Message)
  
  I think I would also like to test what happens when we remove a field without "omitempty":
  - fieldInt int32 `json:"fieldInt"`
  - fieldPtrInt *int32 `json:"fieldPtrInt"`

- **pkg/util/jsonpatch/jsonpatch_test.go:149** by [@mimowo](https://github.com/mimowo) — 2024-10-04T10:57:26Z
```diff
@@ -0,0 +1,191 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+)
+
+func TestFieldExistsByJSONPath(t *testing.T) {
+	testCases := map[string]struct {
+		obj        interface{}
+		path       string
+		wantExists bool
+	}{
+		"nil obj type": {
+			path:       "/foo",
+			wantExists: false,
+		},
+		"empty path": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "",
+			wantExists: false,
+		},
+		"invalid object type": {
+			obj:        "invalid",
+			path:       "/foo",
+			wantExists: false,
+		},
+		"invalid path": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "/",
+			wantExists: false,
+		},
+		"invalid path nest": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/",
+			wantExists: false,
+		},
+		"exists": {
+			obj: struct {
+				Foo string
+				Bar string `json:"bar,omitempty"`
+			}{},
+			path:       "/bar",
+			wantExists: true,
+		},
+		"exists with slice": {
+			obj: struct {
+				Foo []struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/0/bar",
+			wantExists: true,
+		},
+		"exists without tag": {
+			obj: struct {
+				Foo string
+			}{},
+			path:       "/Foo",
+			wantExists: true,
+		},
+		"exists number": {
+			obj: struct {
+				Foo string `json:"123,omitempty"`
+			}{},
+			path:       "/123",
+			wantExists: true,
+		},
+		"exists nest": {
+			obj: struct {
+				Foo struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar",
+			wantExists: true,
+		},
+		"exists nest with pointer": {
+			obj: struct {
+				Foo *struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{
+				&struct {
+					Bar string `json:"bar,omitempty"`
+				}{
+					Bar: "bar",
+				},
+			},
+			path:       "/foo/bar",
+			wantExists: true,
+		},
+		"exists nest with nil pointer": {
+			obj: struct {
+				Foo *struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar",
+			wantExists: true,
+		},
+		"not exists": {
+			obj:        struct{}{},
+			path:       "/foo",
+			wantExists: false,
+		},
+		"invalid type": {
+			obj: struct {
+				Foo func() `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar",
+			wantExists: false,
+		},
+		"not exists nest": {
+			obj:        struct{}{},
+			path:       "/foo/bar/quz",
+			wantExists: false,
+		},
+		"not exists nest with object": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar/quz",
+			wantExists: false,
+		},
+		"not exists with slice": {
```

  Add a positive test case for slice

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T12:03:04Z

- **pkg/util/jsonpatch/jsonpatch_test.go:149** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T12:03:03Z
```diff
@@ -0,0 +1,191 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+)
+
+func TestFieldExistsByJSONPath(t *testing.T) {
+	testCases := map[string]struct {
+		obj        interface{}
+		path       string
+		wantExists bool
+	}{
+		"nil obj type": {
+			path:       "/foo",
+			wantExists: false,
+		},
+		"empty path": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "",
+			wantExists: false,
+		},
+		"invalid object type": {
+			obj:        "invalid",
+			path:       "/foo",
+			wantExists: false,
+		},
+		"invalid path": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "/",
+			wantExists: false,
+		},
+		"invalid path nest": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/",
+			wantExists: false,
+		},
+		"exists": {
+			obj: struct {
+				Foo string
+				Bar string `json:"bar,omitempty"`
+			}{},
+			path:       "/bar",
+			wantExists: true,
+		},
+		"exists with slice": {
+			obj: struct {
+				Foo []struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/0/bar",
+			wantExists: true,
+		},
+		"exists without tag": {
+			obj: struct {
+				Foo string
+			}{},
+			path:       "/Foo",
+			wantExists: true,
+		},
+		"exists number": {
+			obj: struct {
+				Foo string `json:"123,omitempty"`
+			}{},
+			path:       "/123",
+			wantExists: true,
+		},
+		"exists nest": {
+			obj: struct {
+				Foo struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar",
+			wantExists: true,
+		},
+		"exists nest with pointer": {
+			obj: struct {
+				Foo *struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{
+				&struct {
+					Bar string `json:"bar,omitempty"`
+				}{
+					Bar: "bar",
+				},
+			},
+			path:       "/foo/bar",
+			wantExists: true,
+		},
+		"exists nest with nil pointer": {
+			obj: struct {
+				Foo *struct {
+					Bar string `json:"bar,omitempty"`
+				} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar",
+			wantExists: true,
+		},
+		"not exists": {
+			obj:        struct{}{},
+			path:       "/foo",
+			wantExists: false,
+		},
+		"invalid type": {
+			obj: struct {
+				Foo func() `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar",
+			wantExists: false,
+		},
+		"not exists nest": {
+			obj:        struct{}{},
+			path:       "/foo/bar/quz",
+			wantExists: false,
+		},
+		"not exists nest with object": {
+			obj: struct {
+				Foo struct{} `json:"foo,omitempty"`
+			}{},
+			path:       "/foo/bar/quz",
+			wantExists: false,
+		},
+		"not exists with slice": {
```

  Yeah, we already have "exists with slice".

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T12:08:21Z

- **pkg/util/jsonpatch/jsonpatch.go:28** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T12:08:21Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
```

  Exactly. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T12:08:49Z

- **pkg/util/jsonpatch/jsonpatch.go:27** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T12:08:49Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
```

  I think maybe this one https://jsonpatch.com/?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T12:09:13Z

- **pkg/util/jsonpatch/jsonpatch.go:2** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T12:09:13Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T12:17:43Z

- **pkg/util/jsonpatch/jsonpatch.go:58** by [@mimowo](https://github.com/mimowo) — 2024-10-04T12:17:43Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
```

  but I guess in reality fields without omitempty are legal, so we need to make sure this works well, take a look at some declarations here: https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/api/core/v1/types.go as a source of inspiration for additional test cases. For example: https://github.com/kubernetes/kubernetes/blob/b8e57a1c201914e8c5164a165f446480c7ff0bba/staging/src/k8s.io/api/core/v1/types.go#L965

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T12:18:59Z

- **pkg/util/jsonpatch/jsonpatch.go:27** by [@mimowo](https://github.com/mimowo) — 2024-10-04T12:18:59Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
```

  ok, does it require that the path starts with "/"?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T12:19:59Z

- **pkg/util/jsonpatch/jsonpatch.go:58** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T12:19:59Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
```

  > why len(tagParts) > 1
  
  User can use `json:"bar,omitempty"`. But we just need `bar` value.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T12:21:00Z

- **pkg/util/jsonpatch/jsonpatch.go:58** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T12:21:00Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
```

  But if we have `json:"bar` we  need to do nothing because we already get tag on `tag := ft.Tag.Get("json")`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T12:22:01Z

- **pkg/controller/jobframework/webhook/defaulter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-10-04T12:22:01Z
```diff
@@ -58,3 +63,70 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func fieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// Invalid path. For more information, see https://jsonpatch.com/.
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
```

  Is the intention of `len(pathParts) > 0 && pathParts[0] != ""` to say "needs to start with /"?
  
  If this is the intention, then check explicitly: `!strings.HasPrefix(path, "/"){return false}`. We can check this prior to the split. Unless the intention is different.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T12:25:21Z

- **pkg/controller/jobframework/webhook/defaulter.go:101** by [@mimowo](https://github.com/mimowo) — 2024-10-04T12:25:21Z
```diff
@@ -58,3 +63,70 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func fieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// Invalid path. For more information, see https://jsonpatch.com/.
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
+			tag = tagParts[0]
```

  Can you check if it is legal to have `json:"bar , omitempty"` - with spaces. If legal, then we should trim but I'm not sure.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T12:30:23Z

- **pkg/util/jsonpatch/jsonpatch.go:58** by [@mimowo](https://github.com/mimowo) — 2024-10-04T12:30:23Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
```

  ah, got it, I got tricked by assigning `tag := ft.Tag.Get("json")` while this is not necessarily the tag. Maybe it is cleaner to read if  `jsonTag := ft.Tag.Get("json")`, then instead calling `tag` call name it `fieldName`, or `fName` for short. It could be inside a helper function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T15:48:18Z

- **pkg/controller/jobframework/webhook/defaulter.go:101** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T15:48:18Z
```diff
@@ -58,3 +63,70 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func fieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// Invalid path. For more information, see https://jsonpatch.com/.
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
+			tag = tagParts[0]
```

  Added separate function to get field name.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-04T15:49:33Z

- **pkg/util/jsonpatch/jsonpatch.go:58** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-04T15:49:33Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		tag := ft.Tag.Get("json")
+		if tag == "" {
+			tag = ft.Name
+		} else if tagParts := strings.Split(tag, ","); len(tagParts) > 1 {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-04T15:55:00Z

- **pkg/controller/jobframework/webhook/defaulter.go:131** by [@mimowo](https://github.com/mimowo) — 2024-10-04T15:55:00Z
```diff
@@ -126,6 +119,18 @@ func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
 	return false
 }
 
+func getFieldName(fieldType reflect.StructField) string {
+	fieldName := fieldType.Tag.Get("json")
+
+	if fieldName == "" {
+		fieldName = fieldType.Name
+	} else if tagParts := strings.Split(fieldName, ","); len(tagParts) > 1 {
+		fieldName = strings.Trim(tagParts[0], " ")
+	}
+
+	return fieldName
```

  ```suggestion
  	jsonTag := fieldType.Tag.Get("json")
  	if jsonTag == "" {
  		return fieldType.Name
  	} else if tagParts := strings.Split(jsonTag, ","); len(tagParts) > 1 {
  		return strings.Trim(tagParts[0], " ")
  	}
  	return jsonTag
  ```
  nit to avoid assigning ` fieldType.Tag.Get("json")` as fieldName, which is confusing

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-06T15:02:04Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:40** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-06T15:02:04Z
```diff
@@ -37,6 +37,7 @@ var (
 
 type TestResource struct {
 	Foo string `json:"foo,omitempty"`
+	Bar string `json:"bar,omitempty"`
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-06T15:02:13Z

- **pkg/controller/jobframework/webhook/defaulter.go:131** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-06T15:02:13Z
```diff
@@ -126,6 +119,18 @@ func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
 	return false
 }
 
+func getFieldName(fieldType reflect.StructField) string {
+	fieldName := fieldType.Tag.Get("json")
+
+	if fieldName == "" {
+		fieldName = fieldType.Name
+	} else if tagParts := strings.Split(fieldName, ","); len(tagParts) > 1 {
+		fieldName = strings.Trim(tagParts[0], " ")
+	}
+
+	return fieldName
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-06T15:06:19Z

- **pkg/util/jsonpatch/jsonpatch.go:27** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-06T15:06:19Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jsonpatch
+
+import (
+	"reflect"
+	"strconv"
+	"strings"
+)
+
+func FieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// invalid path
```

  Looks like yes https://datatracker.ietf.org/doc/html/rfc6901#section-3. Each token should be prefixed with `/`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-06T15:21:04Z

- **pkg/controller/jobframework/webhook/defaulter.go:70** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-06T15:21:04Z
```diff
@@ -58,3 +63,70 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func fieldExistsByJSONPath(object interface{}, path string) bool {
+	pathParts := strings.Split(path, "/")
+	// Invalid path. For more information, see https://jsonpatch.com/.
+	if len(pathParts) < 2 || len(pathParts) > 0 && pathParts[0] != "" {
```

  Done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-07T07:53:00Z

Maybe ` fieldExistsByJSONPath(object interface{}, path string) ` and friends can be moved in `pkg/util/api`.

- **pkg/controller/jobframework/webhook/defaulter.go:75** by [@trasc](https://github.com/trasc) — 2024-10-07T07:10:30Z
```diff
@@ -58,3 +63,80 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func fieldExistsByJSONPath(object interface{}, path string) bool {
+	// Invalid path. For more information, see https://datatracker.ietf.org/doc/html/rfc6901#section-3.
+	if !strings.HasPrefix(path, "/") {
+		return false
+	}
+	pathParts := strings.Split(path, "/")
+	if len(pathParts) < 2 {
+		return false
+	}
```

  nit: maybe add a comment explaining why < 2

- **pkg/controller/jobframework/webhook/defaulter.go:108** by [@trasc](https://github.com/trasc) — 2024-10-07T07:47:44Z
```diff
@@ -58,3 +63,80 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func fieldExistsByJSONPath(object interface{}, path string) bool {
+	// Invalid path. For more information, see https://datatracker.ietf.org/doc/html/rfc6901#section-3.
+	if !strings.HasPrefix(path, "/") {
+		return false
+	}
+	pathParts := strings.Split(path, "/")
+	if len(pathParts) < 2 {
+		return false
+	}
+	return fieldExistsByJSONPathHelper(object, pathParts[1:])
+}
+
+func fieldExistsByJSONPathHelper(object interface{}, pathParts []string) bool {
+	if object == nil {
+		return false
+	}
+
+	t := reflect.TypeOf(object)
+	v := reflect.ValueOf(object)
+
+	if v.Kind() == reflect.Pointer {
+		t = t.Elem()
+		v = v.Elem()
+	}
+
+	if t.Kind() != reflect.Struct {
+		return false
+	}
+
+	for i := 0; i < t.NumField(); i++ {
+		ft := t.Field(i)
+		fv := v.Field(i)
+
+		if getFieldName(ft) != pathParts[0] {
+			continue
+		}
+
+		if len(pathParts) == 1 {
+			return true
+		}
+
+		switch fv.Kind() {
```

  I think we should also check for maps
