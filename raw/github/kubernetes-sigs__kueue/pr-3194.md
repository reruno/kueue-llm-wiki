# PR #3194: Make LossLessDefaulter selective to only prevent dropping fields outside of schema (unmarshal strict solution).

**Summary**: Make LossLessDefaulter selective to only prevent dropping fields outside of schema (unmarshal strict solution).

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3194

**Last updated**: 2025-03-05T06:48:46Z

---

## Metadata

- **State**: closed
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/make-loss-less-defaulter-selective-to-only-prevent-dropping-fields-outside-of-schema-alternative` → `main`
- **Created**: 2024-10-07T13:07:11Z
- **Updated**: 2025-03-05T06:48:46Z
- **Merged**: —
- **Closed**: 2024-10-25T10:13:27Z
- **Labels**: `kind/feature`, `size/L`, `release-note-none`, `do-not-merge/hold`, `do-not-merge/work-in-progress`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 3   **+209 / -34**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Make `LossLessDefaulter` selective to only prevent dropping fields outside of schema (unmarshal strict solution).  

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3174

#### Special notes for your reviewer:
The reflection-based solution for prevent dropping fields outside of schema is quite complex. This is an alternative solution for #3186 using `json.UnmarshalStrict()`. Thanks to @trasc for the idea.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-07T13:07:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 06c36b76bca9b424bacc322c03487ec66307cddf |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6705516c24850d0008ff9db1 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T13:07:58Z

/cc @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-07T13:10:09Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3194#" title="Author self-approved">mbobrovskyi</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-09T07:00:52Z

I'm still hesitant (no strong opinion at this point) that this path-mapping approach is simpler than https://github.com/kubernetes-sigs/kueue/pull/3186. 

It is certainly simpler on the surface, but the path mapping code makes me feel uneasy about productising it. First, I have a feeling we are building on top of API which is meant to surface errors for human readers, not to build functionality around it. Second, the mapping is done with regexes which are intrinsically meant for unstructured data. Finally, there are many nuances about syntax and escaping. I suppose the code to support all cases properly, like `[][]string` will be even more complex. And I'm not even sure how to methodically approach verifying correctness. 

I consider the previous structured-traversal approach straighforward conceptually. Yes, there are many cases to take care about corresponding to different field types (slice, Map, Pointer, Struct, primitives), but there is only a finite number of those, and we can get help from code analysis tools to see what are the uncovered types.

Let me know your thoughts.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-09T14:58:55Z

> I consider the previous structured-traversal approach straighforward conceptually. Yes, there are many cases to take care about corresponding to different field types (slice, Map, Pointer, Struct, primitives),

I would add the `inline` structs.

But yes, it seems like a more limited set. It overall sounds more reliable than the parsing via regex. Right now, the code in #3186 doesn't actually look that long.

### Comment by [@trasc](https://github.com/trasc) — 2024-10-10T13:58:37Z

In my opinion, in a longer run we can try to modify the json error to be able to return a reliable jsonpatch path and we'll be able to get rid of the string manipulation we are doing now.

 In a even longer run maybe we can get `controller-runtime/pkg/webhook/admission/defaulter_custom.go` to use a strict decoder and do the patch sanitation in there.

### Comment by [@trasc](https://github.com/trasc) — 2024-10-11T06:10:43Z

Yet another possible approach: https://github.com/kubernetes-sigs/kueue/compare/main...epam:kubernetes-kueue:dont-drop-on-muatation

In short, we crate a patch that shows what is removed just by an Unmarshal/Marshal cycle with the local scheme, and we drop the paths in question from the final patch.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T08:23:04Z

> Yet another possible approach: [main...epam:kubernetes-kueue:dont-drop-on-muatation](https://github.com/kubernetes-sigs/kueue/compare/main...epam:kubernetes-kueue:dont-drop-on-muatation)
> 
> In short, we crate a patch that shows what is removed just by an Unmarshal/Marshal cycle with the local scheme, and we drop the paths in question from the final patch.

I like this idea. Could you please create a separate PR to continue the discussion?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T08:25:17Z

> > Yet another possible approach: [main...epam:kubernetes-kueue:dont-drop-on-muatation](https://github.com/kubernetes-sigs/kueue/compare/main...epam:kubernetes-kueue:dont-drop-on-muatation)
> > In short, we crate a patch that shows what is removed just by an Unmarshal/Marshal cycle with the local scheme, and we drop the paths in question from the final patch.
> 
> I like this idea. Could you please create a separate PR to continue the discussion?

It would also be great to add more test cases from the current PR and check how it works with them. But I think it should work fine.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-11T15:28:19Z

Let me try to compare the options:

### @trasc's

- Con: It adds three passes to the structure (to unmarshal, then marshal, then diff)
- Pro: It delegates any nuances of json marshaling to well tested libraries.
- Pro: It handles structs that implement MarshalJSON, which is not uncommon. We have `resource.Quantity` upstream.

### @mbobrovskyi's

- Pro: It adds one pass to the structure
- Con: More maintenance on our side
- Con: It can't handle MarshalJSON

So maybe @trasc's is the only viable option? Maybe we can optimize a little if we can reuse the unmarshalled object from CustomDefaulter, once the change goes to controller-runtime.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-11T15:51:18Z

I think the extra in-memory pass is only a minimal con. The whole object is anyway serialized / deserialized multiple times during webhook admission and transferred over the wire between kube-apiserver and webhooks.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-14T06:50:51Z

> Con: More maintenance on our side

That is some con, but ideally we can transfer the solution to controller-runtime in the long run. In order to make the transfer possible I think we need to make sure it is correct in all cases.

So, for now I'm mostly focused on correctness, and running all tests we have against https://github.com/kubernetes-sigs/kueue/compare/main...epam:kubernetes-kueue:dont-drop-on-muatation would be great to build confidence in this approach.

Regarding MarshalJSON - I'm not familiar with this in the context of webhooks and wondering how this is working. IIUC kube-apiserver needs to serialize the object to send it over the wire to webhooks. I would suppose it doesn't use custom MarshalJSON because it would risk running arbitrary code on kube-apiserver. So, if the webhook uses it to deserialize it, we can create a mismatch in the serialize / deserialize functions. Maybe it warrants an experiment how/if this is supported, WDYT @alculquicondor ?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-15T15:51:40Z

This is how a Quantity file is described in the CRD:

https://github.com/kubernetes-sigs/kueue/blob/391a00004bded1819307c847cb2e0f84aa48c0bc/config/components/crd/bases/kueue.x-k8s.io_clusterqueues.yaml#L382-L397

I think, overall, we can say that the values that use MarshalJSON will be pretty much stored as strings in etcd.
To clarify, apiserver doesn't run marshal/unmarshal code. It is the client (like kueue itself) that runs that code, taken from the imported types.

Maybe LossLessDefaulter will need to just treat the structs that define MarshalJSON as leaves in the structure? Not sure if that's generic enough.

### Comment by [@trasc](https://github.com/trasc) — 2024-10-15T17:21:51Z

I think that a field with custom Marshal/Unmarshal can also declare itself as object with `// +kubebuilder:validation:Type=object`.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T10:13:22Z

/close 

It's very hard to compare UnmarshalStrict and json patch paths.

Alternative solution https://github.com/kubernetes-sigs/kueue/pull/3315.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-25T10:13:28Z

@mbobrovskyi: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3194#issuecomment-2437409299):

>/close 
>
>It's very hard to compare UnmarshalStrict and json patch paths.
>
>Alternative solution https://github.com/kubernetes-sigs/kueue/pull/3315.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-07T13:15:13Z

- **pkg/controller/jobframework/webhook/defaulter.go:90** by [@trasc](https://github.com/trasc) — 2024-10-07T13:15:12Z
```diff
@@ -58,3 +70,27 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func (h *losslessDefaulter) fieldErrors(raw []byte) ([]json.FieldError, error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	fieldErrors := make([]json.FieldError, len(strictErrors))
+	for i, err := range strictErrors {
+		fieldErrors[i] = err.(json.FieldError)
+	}
+	return fieldErrors, nil
+}
+
+func checkIsUnknownField(patchOperation jsonpatch.JsonPatchOperation, fieldErrors []json.FieldError) bool {
+	for _, fieldError := range fieldErrors {
+		fieldPath := fieldError.FieldPath()
+		fieldPath = strings.ReplaceAll(fieldPath, ".", "/")
```

  It's likely we need to chang thing like `a.b[c].d` to `a.b.c.d`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-07T13:16:29Z

- **pkg/controller/jobframework/webhook/defaulter.go:90** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T13:16:29Z
```diff
@@ -58,3 +70,27 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func (h *losslessDefaulter) fieldErrors(raw []byte) ([]json.FieldError, error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	fieldErrors := make([]json.FieldError, len(strictErrors))
+	for i, err := range strictErrors {
+		fieldErrors[i] = err.(json.FieldError)
+	}
+	return fieldErrors, nil
+}
+
+func checkIsUnknownField(patchOperation jsonpatch.JsonPatchOperation, fieldErrors []json.FieldError) bool {
+	for _, fieldError := range fieldErrors {
+		fieldPath := fieldError.FieldPath()
+		fieldPath = strings.ReplaceAll(fieldPath, ".", "/")
```

  Yeah, I still thinking how to optimize it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-07T13:27:38Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:47** by [@mimowo](https://github.com/mimowo) — 2024-10-07T13:27:38Z
```diff
@@ -36,7 +38,18 @@ var (
 )
 
 type TestResource struct {
-	Foo string `json:"foo,omitempty"`
+	Foo  string   `json:"foo,omitempty"`
+	Bar  string   `json:"bar,omitempty"`
+	Qux  []string `json:"qux,omitempty"`
+	Quux []string `json:"quux,omitempty"`
+
+	SubResource TestSubResource    `json:"subresource"`
```

  Please also add test cases for handling removal of labels. In particular those with "." or "/" in their name.
  For example removing label: `example.com/pi: 3.14`. I'm thinking it could hit potential issues with `strings.ReplaceAll(fieldPath, ".", "/")`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-07T17:35:02Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:47** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T17:35:02Z
```diff
@@ -36,7 +38,18 @@ var (
 )
 
 type TestResource struct {
-	Foo string `json:"foo,omitempty"`
+	Foo  string   `json:"foo,omitempty"`
+	Bar  string   `json:"bar,omitempty"`
+	Qux  []string `json:"qux,omitempty"`
+	Quux []string `json:"quux,omitempty"`
+
+	SubResource TestSubResource    `json:"subresource"`
```

  Fixed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-07T17:35:08Z

- **pkg/controller/jobframework/webhook/defaulter.go:90** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T17:35:08Z
```diff
@@ -58,3 +70,27 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+func (h *losslessDefaulter) fieldErrors(raw []byte) ([]json.FieldError, error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	fieldErrors := make([]json.FieldError, len(strictErrors))
+	for i, err := range strictErrors {
+		fieldErrors[i] = err.(json.FieldError)
+	}
+	return fieldErrors, nil
+}
+
+func checkIsUnknownField(patchOperation jsonpatch.JsonPatchOperation, fieldErrors []json.FieldError) bool {
+	for _, fieldError := range fieldErrors {
+		fieldPath := fieldError.FieldPath()
+		fieldPath = strings.ReplaceAll(fieldPath, ".", "/")
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-07T18:16:57Z

- **pkg/controller/jobframework/webhook/defaulter.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-07T18:16:48Z
```diff
@@ -58,3 +69,42 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getFieldErrors returns a list of unknown field errors.
+func (h *losslessDefaulter) getFieldErrors(raw []byte) ([]json.FieldError, error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	fieldErrors := make([]json.FieldError, len(strictErrors))
+	for i, err := range strictErrors {
+		fieldErrors[i] = err.(json.FieldError)
```

  It's worth storing the set of paths, instead of full errors

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-07T18:31:29Z

- **pkg/controller/jobframework/webhook/defaulter.go:82** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T18:31:29Z
```diff
@@ -58,3 +69,42 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getFieldErrors returns a list of unknown field errors.
+func (h *losslessDefaulter) getFieldErrors(raw []byte) ([]json.FieldError, error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	fieldErrors := make([]json.FieldError, len(strictErrors))
+	for i, err := range strictErrors {
+		fieldErrors[i] = err.(json.FieldError)
```

  Yeap, and also we can use `Set` to find element. Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-07T19:06:13Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-07T19:06:13Z
```diff
@@ -36,7 +38,20 @@ var (
 )
 
 type TestResource struct {
```

  Let's also add an `inline` field, just to make sure we are being comprehensive enough.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-07T19:07:08Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:135** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-07T19:07:08Z
```diff
@@ -78,7 +130,19 @@ func TestLossLessDefaulter(t *testing.T) {
 			Object: runtime.RawExtension{
 				// This raw object has a field not defined in the go type.
 				// controller-runtime CustomDefaulter would have added a remove operation for it.
-				Raw: []byte(`{"baz": "qux"}`),
+				Raw: []byte(`{
+	"invalid1": "invalid",
+	"invalid2": ["invalid"],
```

  maybe not `invalid` but `unknown`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-07T19:18:39Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:135** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T19:18:38Z
```diff
@@ -78,7 +130,19 @@ func TestLossLessDefaulter(t *testing.T) {
 			Object: runtime.RawExtension{
 				// This raw object has a field not defined in the go type.
 				// controller-runtime CustomDefaulter would have added a remove operation for it.
-				Raw: []byte(`{"baz": "qux"}`),
+				Raw: []byte(`{
+	"invalid1": "invalid",
+	"invalid2": ["invalid"],
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-07T19:18:45Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:40** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-07T19:18:45Z
```diff
@@ -36,7 +38,20 @@ var (
 )
 
 type TestResource struct {
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-07T19:27:03Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:173** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-07T19:24:08Z
```diff
@@ -87,13 +151,69 @@ func TestLossLessDefaulter(t *testing.T) {
 		t.Errorf("Response not allowed")
 	}
 	wantPatches := []jsonpatch.Operation{
-		{
-			Operation: "add",
-			Path:      "/foo",
-			Value:     "bar",
-		},
+		{Operation: "add", Path: "/creationTimestamp"},
+		{Operation: "add", Path: "/foo", Value: "foo"},
+		{Operation: "remove", Path: "/bar"},
+		{Operation: "remove", Path: "/baz"},
+		{Operation: "replace", Path: "/finalizers/0", Value: "bar"},
+		{Operation: "remove", Path: "/finalizers/1"},
+		{Operation: "remove", Path: "/labels/foo"},
+		{Operation: "replace", Path: "/subresource/foo", Value: ""},
+		{Operation: "replace", Path: "/subresource/bar"},
+		{Operation: "remove", Path: "/conditions/0/observedGeneration"},
+		{Operation: "remove", Path: "/conditions/1"},
 	}
-	if diff := cmp.Diff(wantPatches, resp.Patches); diff != "" {
+	if diff := cmp.Diff(wantPatches, resp.Patches, cmpopts.SortSlices(func(a, b jsonpatch.Operation) bool {
+		return a.Path < b.Path
+	})); diff != "" {
 		t.Errorf("Unexpected patches (-want, +got): %s", diff)
 	}
 }
+
+func TestConvertToFieldErrorPath(t *testing.T) {
```

  IMO, it's better to have more coverage in the test cases of the higher level (exported) functions.
  
  That allows us to change the implementation (or even get rid of the internal functions) without having to change the tests.

- **pkg/controller/jobframework/webhook/defaulter.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-07T19:26:57Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
+	// Replace foo/0 to foo[0]
+	re := regexp.MustCompile(`\/(\d+)$`)
+	path = re.ReplaceAllString(path, `[$1]`)
+	// Replace foo/1/bar to foo[1].bar
+	re = regexp.MustCompile(`\/(\d+)\/`)
```

  I believe this works:
  ```suggestion
  	re :=  regexp.MustCompile(`\/(\d+)(\/|$)`)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T06:43:19Z

- **pkg/controller/jobframework/webhook/defaulter.go:96** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T06:43:19Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
+	// Replace foo/0 to foo[0]
+	re := regexp.MustCompile(`\/(\d+)$`)
+	path = re.ReplaceAllString(path, `[$1]`)
+	// Replace foo/1/bar to foo[1].bar
+	re = regexp.MustCompile(`\/(\d+)\/`)
```

  Actually, no. We need to handle two cases: `foo/1` should be replaced with `foo[1]`, and `foo/1/bar` should be replaced with `foo[1].bar`. Only in the second case we need to add a `.` after `]`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-08T06:45:46Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:173** by [@mimowo](https://github.com/mimowo) — 2024-10-08T06:45:46Z
```diff
@@ -87,13 +151,69 @@ func TestLossLessDefaulter(t *testing.T) {
 		t.Errorf("Response not allowed")
 	}
 	wantPatches := []jsonpatch.Operation{
-		{
-			Operation: "add",
-			Path:      "/foo",
-			Value:     "bar",
-		},
+		{Operation: "add", Path: "/creationTimestamp"},
+		{Operation: "add", Path: "/foo", Value: "foo"},
+		{Operation: "remove", Path: "/bar"},
+		{Operation: "remove", Path: "/baz"},
+		{Operation: "replace", Path: "/finalizers/0", Value: "bar"},
+		{Operation: "remove", Path: "/finalizers/1"},
+		{Operation: "remove", Path: "/labels/foo"},
+		{Operation: "replace", Path: "/subresource/foo", Value: ""},
+		{Operation: "replace", Path: "/subresource/bar"},
+		{Operation: "remove", Path: "/conditions/0/observedGeneration"},
+		{Operation: "remove", Path: "/conditions/1"},
 	}
-	if diff := cmp.Diff(wantPatches, resp.Patches); diff != "" {
+	if diff := cmp.Diff(wantPatches, resp.Patches, cmpopts.SortSlices(func(a, b jsonpatch.Operation) bool {
+		return a.Path < b.Path
+	})); diff != "" {
 		t.Errorf("Unexpected patches (-want, +got): %s", diff)
 	}
 }
+
+func TestConvertToFieldErrorPath(t *testing.T) {
```

  +1

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-08T07:34:59Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mimowo](https://github.com/mimowo) — 2024-10-08T06:47:05Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  Please add a test case for removing label by key `example.com/foo`.

- **pkg/controller/jobframework/webhook/defaulter.go:93** by [@mimowo](https://github.com/mimowo) — 2024-10-08T07:22:15Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
+	// Replace foo/0 to foo[0]
+	re := regexp.MustCompile(`\/(\d+)$`)
```

  Name the regexes in a suggestive way. Maybe `reLastArrayIndex` and `reInnerArrayIndex`.

- **pkg/controller/jobframework/webhook/defaulter.go:91** by [@mimowo](https://github.com/mimowo) — 2024-10-08T07:23:57Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
```

  Maybe use `TrimLeft` so that you only have one value returned.

- **pkg/controller/jobframework/webhook/defaulter.go:93** by [@mimowo](https://github.com/mimowo) — 2024-10-08T07:26:09Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
+	// Replace foo/0 to foo[0]
+	re := regexp.MustCompile(`\/(\d+)$`)
```

  Would this recompile the regex on every invocation? Could we move the regex compilation to the `var` section to compile them only once?

- **pkg/controller/jobframework/webhook/defaulter.go:83** by [@mimowo](https://github.com/mimowo) — 2024-10-08T07:31:07Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
```

  I'm wondering if it is safe to cast to `json.FieldError` unconditionally. Please check if other error types be present.
  
  Still, even if this is the only error type possible currently it probably could change in the future, so I would suggest to check it first.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T08:11:01Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:173** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T08:11:01Z
```diff
@@ -87,13 +151,69 @@ func TestLossLessDefaulter(t *testing.T) {
 		t.Errorf("Response not allowed")
 	}
 	wantPatches := []jsonpatch.Operation{
-		{
-			Operation: "add",
-			Path:      "/foo",
-			Value:     "bar",
-		},
+		{Operation: "add", Path: "/creationTimestamp"},
+		{Operation: "add", Path: "/foo", Value: "foo"},
+		{Operation: "remove", Path: "/bar"},
+		{Operation: "remove", Path: "/baz"},
+		{Operation: "replace", Path: "/finalizers/0", Value: "bar"},
+		{Operation: "remove", Path: "/finalizers/1"},
+		{Operation: "remove", Path: "/labels/foo"},
+		{Operation: "replace", Path: "/subresource/foo", Value: ""},
+		{Operation: "replace", Path: "/subresource/bar"},
+		{Operation: "remove", Path: "/conditions/0/observedGeneration"},
+		{Operation: "remove", Path: "/conditions/1"},
 	}
-	if diff := cmp.Diff(wantPatches, resp.Patches); diff != "" {
+	if diff := cmp.Diff(wantPatches, resp.Patches, cmpopts.SortSlices(func(a, b jsonpatch.Operation) bool {
+		return a.Path < b.Path
+	})); diff != "" {
 		t.Errorf("Unexpected patches (-want, +got): %s", diff)
 	}
 }
+
+func TestConvertToFieldErrorPath(t *testing.T) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T08:27:49Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T08:27:49Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  Yeah, it's a good case. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T08:27:56Z

- **pkg/controller/jobframework/webhook/defaulter.go:93** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T08:27:55Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
+	// Replace foo/0 to foo[0]
+	re := regexp.MustCompile(`\/(\d+)$`)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T08:28:12Z

- **pkg/controller/jobframework/webhook/defaulter.go:93** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T08:28:12Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
+	// Replace foo/0 to foo[0]
+	re := regexp.MustCompile(`\/(\d+)$`)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T08:28:31Z

- **pkg/controller/jobframework/webhook/defaulter.go:83** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T08:28:31Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T08:29:10Z

- **pkg/controller/jobframework/webhook/defaulter.go:91** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T08:29:09Z
```diff
@@ -58,3 +70,32 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		paths.Insert(err.(json.FieldError).FieldPath())
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path, _ = strings.CutPrefix(path, "/")
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-08T08:39:41Z

- **pkg/controller/jobframework/webhook/defaulter.go:88** by [@mimowo](https://github.com/mimowo) — 2024-10-08T08:39:41Z
```diff
@@ -58,3 +75,34 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		if fieldError, ok := err.(json.FieldError); ok {
```

  Could we get other errors as well? Maybe we could trigger them to test that scenario? If we cannot trigger them easily this is fine.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T08:42:23Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T08:42:23Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  I also thinking about `[][]string` case. I will add this case as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-08T08:45:15Z

LGTM, but we need more eyes on this. 
cc @alculquicondor @tenzen-y @trasc

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-08T09:14:26Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mimowo](https://github.com/mimowo) — 2024-10-08T09:14:26Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  /hold 
  Thanks for bringing this up, let's hold for fixing this case

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T14:31:56Z

- **pkg/controller/jobframework/webhook/defaulter.go:88** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T14:31:56Z
```diff
@@ -58,3 +75,34 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		if fieldError, ok := err.(json.FieldError); ok {
```

  As I can see [here](https://github.com/kubernetes-sigs/json/blob/bc3834ca7abd3a90f03ef00a27ad80cb892f9c21/json.go#L87) it should implement `FieldError` interface.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-08T14:57:32Z

- **pkg/controller/jobframework/webhook/defaulter.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-08T14:57:32Z
```diff
@@ -58,3 +75,34 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		if fieldError, ok := err.(json.FieldError); ok {
```

  what happens if the go type is defined with an integer field, but the input object has a string?
  Would that fail here or a different place?
  I guess, in general, in that case we should fail the whole request.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-08T15:00:20Z

- **pkg/controller/jobframework/webhook/defaulter.go:106** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-08T15:00:20Z
```diff
@@ -58,3 +75,34 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		if fieldError, ok := err.(json.FieldError); ok {
+			paths.Insert(fieldError.FieldPath())
+		}
+	}
+	return paths, nil
+}
+
+// convertToFieldErrorPath converts the given path into the FieldError path.
+func convertToFieldErrorPath(path string) string {
+	// Replace /foo/bar to foo/bar
+	path = strings.TrimLeft(path, "/")
+	// Replace foo/0 to foo[0]
+	path = reLastArrayIndex.ReplaceAllString(path, `[$1]`)
+	// Replace foo/1/bar to foo[1].bar
+	path = reInnerArrayIndex.ReplaceAllString(path, `[$1].`)
+	// Replace foo/bar to foo.bar
+	path = strings.ReplaceAll(path, "/", ".")
+	// Replace example.com~1foo to example.com/foo
+	path = strings.ReplaceAll(path, "~1", "/")
```

  Uhm... I wonder if there are other escape characters we should be aware of. Can you check the jsonpath documentation?
  
  Or is there a different library or setting for strict json verification that we can use?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T15:01:14Z

- **pkg/controller/jobframework/webhook/defaulter.go:88** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T15:01:14Z
```diff
@@ -58,3 +75,34 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		if fieldError, ok := err.(json.FieldError); ok {
```

  It should fail on `response := h.Handler.Handle(ctx, req)`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T15:34:24Z

- **pkg/controller/jobframework/webhook/defaulter.go:88** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T15:34:24Z
```diff
@@ -58,3 +75,34 @@ func (h *losslessDefaulter) Handle(ctx context.Context, req admission.Request) a
 	}
 	return response
 }
+
+// getUnknownPaths returns a list of unknown paths.
+func (h *losslessDefaulter) getUnknownPaths(raw []byte) (sets.Set[string], error) {
+	obj := h.object.DeepCopyObject()
+	strictErrors, err := json.UnmarshalStrict(raw, obj, json.DisallowUnknownFields)
+	if err != nil {
+		return nil, err
+	}
+	paths := sets.New[string]()
+	for _, err := range strictErrors {
+		if fieldError, ok := err.(json.FieldError); ok {
```

  > what happens if the go type is defined with an integer field, but the input object has a string?
  Would that fail here or a different place?
  I guess, in general, in that case we should fail the whole request.
  
  Added separate test case to check it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T17:17:14Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T17:17:13Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  > I also thinking about [][]string case. I will add this case as well.
  
  OK. On this case we can have two cases `/qux/0/1 => qux[0][1]` and `/quux/0/0/unknown => quux[0][0].unknown`.
  
  The problem here is that we don't know exactly is it fieldName `0` or index of array :confused:.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-08T17:27:16Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-08T17:27:16Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  maybe it's better to do the conversion the other way?
  From `qux[0][1]` to `/qux/0/1`
  
  The reason is that the latter is part of the jsonpatch standard, as it is what the jsondiff is creating. I don't know where the other one is coming from. Or are both standards?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T17:33:35Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T17:33:35Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  Yeap, I checked this case as well. But problematic part that this variant is not clear too. It's not escaping  dot's on the field name (e.g. `{"unknown.unknown": "unknown"}`  => "unknown.unknown" ). So on this case we have another problem that we don't know what is `.` exactly fieldName or a part of another object.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T17:35:20Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T17:35:19Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  Just one way to check it is using original object. But on this case we coming back to https://github.com/kubernetes-sigs/kueue/pull/3186 solution.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-08T17:46:34Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-08T17:46:34Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  > The reason is that the latter is part of the jsonpatch standard, as it is what the jsondiff is creating. I don't know where the other one is coming from. Or are both standards?
  
  /a/b/c - Is [RFC 6901](https://datatracker.ietf.org/doc/html/rfc6901) standard.
  
  For about FieldError Path I'm not sure. IIUC it's just custom representation on the json library (https://github.com/kubernetes-sigs/json/blob/main/internal/golang/encoding/json/kubernetes_patch.go#L75-L78).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-08T17:49:01Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-08T17:49:01Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  cc @liggitt as owner of https://github.com/kubernetes-sigs/json

### Review by [@liggitt](https://github.com/liggitt) (commented) — 2024-10-08T18:03:28Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@liggitt](https://github.com/liggitt) — 2024-10-08T18:03:28Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  field paths returned from strict decoding errors in the json library are only used to surface errors, and mimic the serialization of Kubernetes API validation field paths (which also don't escape `.` characters in field names - https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/apimachinery/pkg/util/validation/field/path.go#L92)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-09T07:01:39Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mimowo](https://github.com/mimowo) — 2024-10-09T07:01:39Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  Thanks for sharing your thoughts, I have also summarized my doubts about productising this approach: https://github.com/kubernetes-sigs/kueue/pull/3194#issuecomment-2401494759.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-09T11:27:12Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-09T11:27:12Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  In this code https://github.com/kubernetes-sigs/json/blob/main/internal/golang/encoding/json/kubernetes_patch.go#L77 we are generating and building the FieldError path. We can add the full stack as a []string, which will show us something like ["quux", "[", "0", "]", "[", "0", "]", ".", "unknown"] and extend FieldError interface.
  
  It will be much easier to convert it to `/quux/0/0/unknown`. And we will not lost dots on the field names.
  
  @liggitt WDYT?

### Review by [@liggitt](https://github.com/liggitt) (commented) — 2024-10-09T14:48:45Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@liggitt](https://github.com/liggitt) — 2024-10-09T14:48:45Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  that's a very performance sensitive code path... the slice stack holding the field path is reused as we traverse through the object ... duplicating it in addition to creating strings to make the field paths reversible for this purpose isn't something I'd be in favor of at first glance

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-09T14:56:41Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-09T14:56:40Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  Other approaches I can see are:
  - Change Path to Stack and generate Path on demand in the function.
  - At least escape dots in the field name. However, I doubt that it will meet all our requirements.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-09T15:08:28Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-09T15:08:28Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  > Change Path to Stack and generate Path on demand in the function.
  
  Since the struct is private, it sounds like the change would be backwards-compatible.
  I guess there could potentially be performance regressions if a user calls `FieldPath()` repeatedly on the same object.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-09T18:51:01Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-09T18:51:01Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  > At least escape dots in the field name. However, I doubt that it will meet all our requirements.
  
  So... We only have one way?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-10T15:21:27Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-10T15:21:27Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  No, I think we can go the way of `Change Path to Stack and generate Path on demand in the function`, if @liggitt  agrees to it.

### Review by [@liggitt](https://github.com/liggitt) (commented) — 2024-10-10T15:26:13Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@liggitt](https://github.com/liggitt) — 2024-10-10T15:26:13Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  to do that we have to clone / alloc the stack for every error we return - that's what I wasn't in favor of in https://github.com/kubernetes-sigs/kueue/pull/3194#discussion_r1793669460

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-10T15:47:23Z

- **pkg/controller/jobframework/webhook/defaulter_test.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-10T15:47:23Z
```diff
@@ -57,8 +72,45 @@ type TestCustomDefaulter struct{}
 func (*TestCustomDefaulter) Default(ctx context.Context, obj runtime.Object) error {
 	d := obj.(*TestResource)
 	if d.Foo == "" {
-		d.Foo = "bar"
+		d.Foo = "foo"
+	}
+	if d.Bar == "bar" {
+		d.Bar = ""
+	}
+	if len(d.Baz) > 0 {
+		d.Baz = nil
+	}
+
+	if d.Labels != nil {
+		delete(d.Labels, "foo")
```

  Oh, I see what you mean. Ok, then let's proceed the way of https://github.com/kubernetes-sigs/kueue/pull/3186
