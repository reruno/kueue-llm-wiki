# PR #3209: Add integration tests for detecting CRD installed after Kueue started.

**Summary**: Add integration tests for detecting CRD installed after Kueue started.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3209

**Last updated**: 2024-10-11T18:48:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/add-integration-tests-for-detecting-crd-installed-after-kueue-started` → `main`
- **Created**: 2024-10-09T18:24:47Z
- **Updated**: 2024-10-11T18:48:40Z
- **Merged**: 2024-10-11T18:48:24Z
- **Closed**: 2024-10-11T18:48:24Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Changed files**: 2   **+191 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add integration tests for detecting CRD installed after Kueue started.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3015

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-09T18:25:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b179bc2e1e5ddeb67887d84b576c8b760df8a59f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6709666c59ce9a0008da3aa0 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-10T11:54:56Z

/cc @tenzen-y

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-10T11:57:51Z

/cc @trasc

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-10T15:02:05Z

> Overall LGTM.
> 
> One nit, can we note in a comment that the integration manager is a shared state and that after enabled a framework will remain enameled until the end of the test suite.
> 
> Alternatively we can try to modify
> 
> https://github.com/kubernetes-sigs/kueue/blob/a7681277ea932aef0b233e635447c0e16b31e116/pkg/controller/jobframework/setup.go#L53-L55
> 
> to be able to use a custom `manager` so we are able to start a potential different test from scratch. (but this might be too much)

Yeah, it's a good point. If we add very simple test case it will not work well because `jobset` integration already exist. 
We already have `EnableIntegration()` method, maybe we can add just `DisableIntegration()`? It should be enough to reset integration manager.

WDYT?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-11T09:50:15Z

I think it is ok to add a comment that the JobSet remains enabled for the whole suite since we don't fully support disabling integrations when CRDs are uninstalled at runtime.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-11T16:36:56Z

/lgtm
/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-11T16:37:02Z

LGTM label has been added.  <details>Git tree hash: 95c9e890997dd8073cf5a6b707d2baf349349cb1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-11T18:47:24Z

LGTM label has been added.  <details>Git tree hash: 1afdc600b296ce057d7e008d344933d4f190d3a7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-11T18:47:25Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3209#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3209#issuecomment-2407765683" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3209#pullrequestreview-2363392628" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-10T12:47:02Z

Overall LGTM.

One nit, can we  note in a comment that the integration manager is a shared state and that after enabled a framework will remain enameled until the end of the test suite. 

Alternatively we can try to modify 
https://github.com/kubernetes-sigs/kueue/blob/a7681277ea932aef0b233e635447c0e16b31e116/pkg/controller/jobframework/setup.go#L53-L55

to be able to use a custom `manager`  so we are able to start a potential different test from scratch. (but this might be too much)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-11T09:44:46Z

- **test/integration/controller/jobframework/setup/setup_controllers_test.go:131** by [@mimowo](https://github.com/mimowo) — 2024-10-11T09:44:46Z
```diff
@@ -0,0 +1,66 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"slices"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"sigs.k8s.io/controller-runtime/pkg/envtest"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/jobset"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Setup Controllers", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{CRDPath: crdPath}
+		cfg = fwk.Init()
+		ctx, _ = fwk.SetupClient(cfg)
+		fwk.StartManager(ctx, cfg, managerSetup(jobframework.WithEnabledFrameworks([]string{jobset.FrameworkName})))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should setup controller and webhook after CRD installation", framework.SlowSpec, func() {
+		ginkgo.By("Check that integration is not enabled", func() {
+			gomega.Expect(slices.Contains(jobframework.GetEnabledIntegrationsList(), jobset.FrameworkName)).To(gomega.BeFalse())
+		})
+
+		ginkgo.By("Install CRDs", func() {
+			options := envtest.CRDInstallOptions{
+				Paths:              []string{jobsetCrdPath},
+				ErrorIfPathMissing: true,
+				CleanUpAfterUse:    true,
+			}
+			_, err := envtest.InstallCRDs(cfg, options)
+			gomega.Expect(err).NotTo(gomega.HaveOccurred())
+		})
+
+		ginkgo.By("Check that integration is enabled", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(slices.Contains(jobframework.GetEnabledIntegrationsList(), jobset.FrameworkName)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
```

  Could you additionally create an instance of JobSet (with suspend=false)? Then we could check that the corresponding Workload object is created, and the JobSet has suspend set to true. For that we will need a LQ which can be inactive to make the workload suspended.
  
  This will prove the installation of CRDs works from the user-observable perspective.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-11T09:45:24Z

LGTM overall, I think it would be nice to extend the test slightly to see the installed CRD works from user-observable perspective

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-11T13:44:25Z

- **pkg/controller/jobframework/integrationmanager.go:295** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-11T13:44:19Z
```diff
@@ -288,6 +288,11 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsList returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsList() []string {
+	return manager.getEnabledIntegrations().UnsortedList()
+}
```

  ```suggestion
  // GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
  func GetEnabledIntegrationsList(tb *testing.T) []string {
  	tb.Helper()
  	return manager.getEnabledIntegrations().UnsortedList()
  }
  ```
  
  Isn't this only for testing? In that case, can you add "Test" suffix and receive testing object similar to "EnableIntegrationsForTest".
  
  Additionally, could we avoid implementing such only testing functions? Is there any alternative solution?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T15:38:23Z

- **test/integration/controller/jobframework/setup/setup_controllers_test.go:131** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T15:38:23Z
```diff
@@ -0,0 +1,66 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"slices"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"sigs.k8s.io/controller-runtime/pkg/envtest"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/jobset"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Setup Controllers", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{CRDPath: crdPath}
+		cfg = fwk.Init()
+		ctx, _ = fwk.SetupClient(cfg)
+		fwk.StartManager(ctx, cfg, managerSetup(jobframework.WithEnabledFrameworks([]string{jobset.FrameworkName})))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should setup controller and webhook after CRD installation", framework.SlowSpec, func() {
+		ginkgo.By("Check that integration is not enabled", func() {
+			gomega.Expect(slices.Contains(jobframework.GetEnabledIntegrationsList(), jobset.FrameworkName)).To(gomega.BeFalse())
+		})
+
+		ginkgo.By("Install CRDs", func() {
+			options := envtest.CRDInstallOptions{
+				Paths:              []string{jobsetCrdPath},
+				ErrorIfPathMissing: true,
+				CleanUpAfterUse:    true,
+			}
+			_, err := envtest.InstallCRDs(cfg, options)
+			gomega.Expect(err).NotTo(gomega.HaveOccurred())
+		})
+
+		ginkgo.By("Check that integration is enabled", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(slices.Contains(jobframework.GetEnabledIntegrationsList(), jobset.FrameworkName)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T15:39:45Z

- **pkg/controller/jobframework/integrationmanager.go:295** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T15:39:44Z
```diff
@@ -288,6 +288,11 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsList returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsList() []string {
+	return manager.getEnabledIntegrations().UnsortedList()
+}
```

  Just now it's using only for tests. But it can be useful in another places like `GetIntegrationsList`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-11T15:39:56Z

LGTM

- **test/integration/controller/jobframework/setup/setup_controllers_test.go:119** by [@mimowo](https://github.com/mimowo) — 2024-10-11T15:39:21Z
```diff
@@ -62,5 +101,38 @@ var _ = ginkgo.Describe("Setup Controllers", ginkgo.Ordered, ginkgo.ContinueOnFa
 				g.Expect(slices.Contains(jobframework.GetEnabledIntegrationsList(), jobset.FrameworkName)).To(gomega.BeTrue())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		jobSet := testingjobset.MakeJobSet("jobset", ns.Name).
+			ReplicatedJobs(testingjobset.ReplicatedJobRequirements{
+				Name:        "replicated-job-1",
+				Replicas:    1,
+				Parallelism: 1,
+				Completions: 1,
+			}).
+			Suspend(false).
+			Queue(localQueue.Name).
+			Obj()
+		ginkgo.By("Create a JobSet", func() {
+			gomega.Expect(k8sClient.Create(ctx, jobSet)).To(gomega.Succeed())
+		})
+
+		ginkgo.By("Check that the JobSet was created", func() {
```

  ```suggestion
  		ginkgo.By("Check that the JobSet was created and got suspended", func() {
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T15:41:24Z

- **test/integration/controller/jobframework/setup/setup_controllers_test.go:119** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T15:41:24Z
```diff
@@ -62,5 +101,38 @@ var _ = ginkgo.Describe("Setup Controllers", ginkgo.Ordered, ginkgo.ContinueOnFa
 				g.Expect(slices.Contains(jobframework.GetEnabledIntegrationsList(), jobset.FrameworkName)).To(gomega.BeTrue())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		jobSet := testingjobset.MakeJobSet("jobset", ns.Name).
+			ReplicatedJobs(testingjobset.ReplicatedJobRequirements{
+				Name:        "replicated-job-1",
+				Replicas:    1,
+				Parallelism: 1,
+				Completions: 1,
+			}).
+			Suspend(false).
+			Queue(localQueue.Name).
+			Obj()
+		ginkgo.By("Create a JobSet", func() {
+			gomega.Expect(k8sClient.Create(ctx, jobSet)).To(gomega.Succeed())
+		})
+
+		ginkgo.By("Check that the JobSet was created", func() {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T15:41:42Z

- **pkg/controller/jobframework/integrationmanager.go:295** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T15:41:42Z
```diff
@@ -288,6 +288,11 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsList returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsList() []string {
+	return manager.getEnabledIntegrations().UnsortedList()
+}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-11T15:54:20Z

- **pkg/controller/jobframework/integrationmanager.go:292** by [@mimowo](https://github.com/mimowo) — 2024-10-11T15:54:20Z
```diff
@@ -288,6 +288,12 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsListForTest(tb testing.TB) []string {
```

  Maybe we don't need this function check at all. Can we instead verify the JobSet CRD is not installed, and then installed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T15:56:15Z

- **pkg/controller/jobframework/integrationmanager.go:292** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T15:56:15Z
```diff
@@ -288,6 +288,12 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsListForTest(tb testing.TB) []string {
```

  But how it can help? We need to understand is it enabled before and after. CRDs exists or not doesn't help in this case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-11T16:07:19Z

- **pkg/controller/jobframework/integrationmanager.go:292** by [@mimowo](https://github.com/mimowo) — 2024-10-11T16:07:19Z
```diff
@@ -288,6 +288,12 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsListForTest(tb testing.TB) []string {
```

  Good question. Ideally integration tests should assert on user-observable features of the system, rather than some internal variables. Unit tests are meant for internal state.
  
  We already assert it is enabled after install, because we check the effect - workload gets created and webhook sets suspend=true.
  For checking prior to install. IIUC the check that there is no JobSet CRD would indicate the integration is disabled.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-11T16:14:45Z

- **pkg/controller/jobframework/integrationmanager.go:292** by [@mimowo](https://github.com/mimowo) — 2024-10-11T16:14:45Z
```diff
@@ -288,6 +288,12 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsListForTest(tb testing.TB) []string {
```

  What we want to cover is that after installing CRD Kueue picks it up. 
  
  Testing what happens prior to installation of the CRD is just a sanity check to make sure it wasn't already installed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T16:23:34Z

- **pkg/controller/jobframework/integrationmanager.go:292** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T16:23:34Z
```diff
@@ -288,6 +288,12 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsListForTest(tb testing.TB) []string {
```

  Make sense. Only one thing that user can also remove CRDs after enable. Maybe we should think how to disable integration on this case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-11T16:35:00Z

- **pkg/controller/jobframework/integrationmanager.go:292** by [@mimowo](https://github.com/mimowo) — 2024-10-11T16:35:00Z
```diff
@@ -288,6 +288,12 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsListForTest(tb testing.TB) []string {
```

  Yeah, this is not supported currently. I think we could do it as part of https://github.com/kubernetes-sigs/kueue/issues/2837. If we track the CRDs with a reconciler, we could react to the event of the CRD being remvoved and unload the controller and the webhook.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T16:40:33Z

- **pkg/controller/jobframework/integrationmanager.go:292** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T16:40:33Z
```diff
@@ -288,6 +288,12 @@ func GetIntegrationsList() []string {
 	return manager.getList()
 }
 
+// GetEnabledIntegrationsListForTest returns the list of currently enabled frameworks.
+func GetEnabledIntegrationsListForTest(tb testing.TB) []string {
```

  Ah I see. I missed this issue. Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-11T17:43:05Z

- **test/integration/controller/jobframework/setup/setup_controllers_test.go:98** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-11T17:43:01Z
```diff
@@ -0,0 +1,134 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"errors"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/envtest"
+	jobsetapi "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/jobset"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingjobset "sigs.k8s.io/kueue/pkg/util/testingjobs/jobset"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Setup Controllers", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns           *corev1.Namespace
+		flavor       *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{CRDPath: crdPath}
+		cfg = fwk.Init()
+		ctx, k8sClient = fwk.SetupClient(cfg)
+		fwk.StartManager(ctx, cfg, managerSetup(jobframework.WithEnabledFrameworks([]string{jobset.FrameworkName})))
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "jobset-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		flavor = testing.MakeResourceFlavor("on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(flavor.Name).
+					Resource(corev1.ResourceCPU, "5").
+					Obj(),
+			).Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, flavor, true)
+
+		fwk.StopManager(ctx)
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should setup controller and webhook after CRD installation", framework.SlowSpec, func() {
+		jobSet := testingjobset.MakeJobSet("jobset", ns.Name).
+			ReplicatedJobs(testingjobset.ReplicatedJobRequirements{
+				Name:        "replicated-job-1",
+				Replicas:    1,
+				Parallelism: 1,
+				Completions: 1,
+			}).
+			Suspend(false).
+			Queue(localQueue.Name).
+			Obj()
+
+		ginkgo.By("Check that the JobSet CRDs are not installed", func() {
+			gomega.Expect(errors.Is(k8sClient.Create(ctx, jobSet), &meta.NoKindMatchError{})).To(gomega.BeTrue())
```

  Instead of `To`, could we use the `gomega.BeComparableTo` and `cmpopts.EquoteErrors`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T17:55:09Z

- **test/integration/controller/jobframework/setup/setup_controllers_test.go:98** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T17:55:09Z
```diff
@@ -0,0 +1,134 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"errors"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/envtest"
+	jobsetapi "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/jobset"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	testingjobset "sigs.k8s.io/kueue/pkg/util/testingjobs/jobset"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Setup Controllers", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns           *corev1.Namespace
+		flavor       *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{CRDPath: crdPath}
+		cfg = fwk.Init()
+		ctx, k8sClient = fwk.SetupClient(cfg)
+		fwk.StartManager(ctx, cfg, managerSetup(jobframework.WithEnabledFrameworks([]string{jobset.FrameworkName})))
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "jobset-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		flavor = testing.MakeResourceFlavor("on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(flavor.Name).
+					Resource(corev1.ResourceCPU, "5").
+					Obj(),
+			).Obj()
+
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, flavor, true)
+
+		fwk.StopManager(ctx)
+		fwk.Teardown()
+	})
+
+	ginkgo.It("Should setup controller and webhook after CRD installation", framework.SlowSpec, func() {
+		jobSet := testingjobset.MakeJobSet("jobset", ns.Name).
+			ReplicatedJobs(testingjobset.ReplicatedJobRequirements{
+				Name:        "replicated-job-1",
+				Replicas:    1,
+				Parallelism: 1,
+				Completions: 1,
+			}).
+			Suspend(false).
+			Queue(localQueue.Name).
+			Obj()
+
+		ginkgo.By("Check that the JobSet CRDs are not installed", func() {
+			gomega.Expect(errors.Is(k8sClient.Create(ctx, jobSet), &meta.NoKindMatchError{})).To(gomega.BeTrue())
```

  Done in [b179bc2](https://github.com/kubernetes-sigs/kueue/pull/3209/commits/b179bc2e1e5ddeb67887d84b576c8b760df8a59f).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-11T18:47:18Z

Thanks!
/lgtm
/approve
