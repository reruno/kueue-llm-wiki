# PR #3213: [Kjobctl] Extend priority flag to other modes

**Summary**: [Kjobctl] Extend priority flag to other modes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3213

**Last updated**: 2024-10-20T05:19:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/kjobctl-priority-flags-other-builders` → `main`
- **Created**: 2024-10-10T11:02:52Z
- **Updated**: 2024-10-20T05:19:55Z
- **Merged**: 2024-10-19T16:15:05Z
- **Closed**: 2024-10-19T16:15:05Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@IrvingMg](https://github.com/IrvingMg)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@IrvingMg](https://github.com/IrvingMg)
- **Changed files**: 14   **+319 / -35**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Extend priority flag to other modes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-10T11:02:54Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-10T11:03:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c59e4be8c6b2fffb25835cbd4bb1c95dd5ccda89 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6710dbc2a0567300086a048c |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3213--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3213--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTMyMTMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.yTFnLz3NNlLDgdcqCDhSrOdSisDg4YpRw-TDfSdD-HM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3213--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-10-10T11:10:10Z

/assign @mbobrovskyi @IrvingMg

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-10-10T11:14:09Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-14T13:58:01Z

/lgtm

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-14T13:58:07Z

LGTM label has been added.  <details>Git tree hash: aeef0e03bf486491fd232e6b848a7f8e0d240b31</details>

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-10-14T14:05:31Z

ready for review @mwielgus @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-17T08:47:26Z

@mszadkow please rebase

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-17T09:10:08Z

/cc @mwysokin

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-17T09:10:12Z

@mimowo: GitHub didn't allow me to request PR reviews from the following users: mwysokin.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3213#issuecomment-2418987465):

>/cc @mwysokin 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-17T12:59:52Z

LGTM label has been added.  <details>Git tree hash: 490ee2f4588e9ba1eb73179c3bf1dd164d5ad85b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-19T16:04:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3213#" title="Author self-approved">mszadkow</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3213#pullrequestreview-2379581191" title="Approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[cmd/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/OWNERS)~~ [trasc]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-10T11:44:09Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:97** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-10T11:16:33Z
```diff
@@ -93,7 +93,8 @@ type TemplateReference string
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('ntasks' in self.requiredFlags) || self.name == 'Slurm'", message="ntasks flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('output' in self.requiredFlags) || self.name == 'Slurm'", message="output flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('partition' in self.requiredFlags) || self.name == 'Slurm'", message="partition flag can be used only on Slurm mode"
-// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name == 'Slurm'", message="priority flag can be used only on Slurm mode"
+// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name != 'Interactive'", message="priority flag can't be used only on Interactive mode"
+// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('skip-priority-validation' in self.requiredFlags) || self.name != 'Interactive'", message="skip priority validation flag can't be used only on Interactive mode"
```

  ```suggestion
  ```
  
  `--skip-priority-validation` is not required flag. So we don't need to add here.

- **cmd/experimental/kjobctl/pkg/builder/builder.go:317** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-10T11:22:17Z
```diff
@@ -313,6 +313,17 @@ func (b *Builder) validateGeneral(ctx context.Context) error {
 		}
 	}
 
+	// priority class is checked excluding interactive mode
+	if b.modeName != v1alpha1.InteractiveMode {
```

  I think it's not necessary. Because we are already checked it on the command and will skip if we don't need.

- **cmd/experimental/kjobctl/docs/commands/kjobctl_create/kjobctl_create_job.md:15** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-10T11:34:01Z
```diff
@@ -12,7 +12,7 @@ The file is auto-generated from the Go source code of the component using the
 Create a job
 
 ```
-kjobctl create job --profile APPLICATION_PROFILE_NAME [--localqueue LOCAL_QUEUE_NAME] [--skip-localqueue-validation] [--cmd COMMAND] [--request RESOURCE_NAME=QUANTITY] [--parallelism PARALLELISM] [--completions COMPLETIONS]
+kjobctl create job --profile APPLICATION_PROFILE_NAME [--localqueue LOCAL_QUEUE_NAME] [--skip-localqueue-validation] [--skip-priority-validation] [--cmd COMMAND] [--request RESOURCE_NAME=QUANTITY] [--parallelism PARALLELISM] [--completions COMPLETIONS] [--priority NAME]
```

  ```suggestion
  kjobctl create job --profile APPLICATION_PROFILE_NAME [--localqueue LOCAL_QUEUE_NAME] [--skip-localqueue-validation] [--cmd COMMAND] [--request RESOURCE_NAME=QUANTITY] [--parallelism PARALLELISM] [--completions COMPLETIONS] [--priority NAME] [--skip-priority-validation]
  ```
  
  nit

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-10-10T19:15:27Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:96** by [@IrvingMg](https://github.com/IrvingMg) — 2024-10-10T18:34:17Z
```diff
@@ -93,7 +93,8 @@ type TemplateReference string
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('ntasks' in self.requiredFlags) || self.name == 'Slurm'", message="ntasks flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('output' in self.requiredFlags) || self.name == 'Slurm'", message="output flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('partition' in self.requiredFlags) || self.name == 'Slurm'", message="partition flag can be used only on Slurm mode"
-// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name == 'Slurm'", message="priority flag can be used only on Slurm mode"
+// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name != 'Interactive'", message="priority flag can't be used only on Interactive mode"
```

  ```suggestion
  // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name != 'Interactive'", message="priority flag can't be used on Interactive mode"
  ```
  
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T07:26:32Z

- **cmd/experimental/kjobctl/docs/commands/kjobctl_create/kjobctl_create_job.md:15** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T07:26:31Z
```diff
@@ -12,7 +12,7 @@ The file is auto-generated from the Go source code of the component using the
 Create a job
 
 ```
-kjobctl create job --profile APPLICATION_PROFILE_NAME [--localqueue LOCAL_QUEUE_NAME] [--skip-localqueue-validation] [--cmd COMMAND] [--request RESOURCE_NAME=QUANTITY] [--parallelism PARALLELISM] [--completions COMPLETIONS]
+kjobctl create job --profile APPLICATION_PROFILE_NAME [--localqueue LOCAL_QUEUE_NAME] [--skip-localqueue-validation] [--skip-priority-validation] [--cmd COMMAND] [--request RESOURCE_NAME=QUANTITY] [--parallelism PARALLELISM] [--completions COMPLETIONS] [--priority NAME]
```

  ```suggestion
  kjobctl create job --profile APPLICATION_PROFILE_NAME [--localqueue LOCAL_QUEUE_NAME] [--priority NAME] [--skip-localqueue-validation] [--skip-priority-validation] [--cmd COMMAND] [--request RESOURCE_NAME=QUANTITY] [--parallelism PARALLELISM] [--completions COMPLETIONS]
  ```
  Or like this, to keep it nearby.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T07:29:34Z

- **cmd/experimental/kjobctl/pkg/cmd/create/create.go:329** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T07:29:34Z
```diff
@@ -320,6 +334,10 @@ var createModeSubcommands = map[string]modeSubcommand{
 				"MinReplicas denotes the minimum number of desired Pods for this worker group.")
 			subcmd.Flags().StringToIntVar(&o.MaxReplicas, maxReplicasFlagName, nil,
 				"MaxReplicas denotes the maximum number of desired Pods for this worker group, and the default value is maxInt32.")
+			subcmd.Flags().StringVar(&o.Priority, priorityFlagName, "",
+				"Apply priority for the entire workload.")
+			subcmd.Flags().BoolVar(&o.SkipPriorityValidation, skipPriorityValidationFlagName, false,
+				"Skip workload priority class validation. Add priority class label even if the class does not exist.")
 		},
 	},
 	"slurm": {
```

  Also for about Slurm. `[--priority NAME]` is not a Slurm flag we should move it to flags before dashes. It's not current changes, so maybe it would be greate to prepare a small follow-up PR to fix it.
  
  cc: @IrvingMg

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T10:44:22Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:133** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T10:42:12Z
```diff
@@ -121,15 +121,16 @@ type SupportedMode struct {
 	// The raycluster flag used only for the RayJob mode.
 	// The request flag used only for Interactive and Job modes.
 	// The cmd flag used only for Interactive, Job, and RayJob.
+	// The skip-priority-workload and priority flags can be used in all modes except for Interactive mode.
 	// If the raycluster flag are set, none of localqueue, replicas, min-replicas, or max-replicas can be set.
 	// For the Slurm mode, the possible values are: array, cpus-per-task, error, gpus-per-task, input, job-name, mem, mem-per-cpu,
-	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue, priority.
+	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue.
 	//
 	// cmd and requests values are going to be added only to the first primary container.
 	//
 	// +optional
 	// +listType=set
-	// +kubebuilder:validation:MaxItems=13
+	// +kubebuilder:validation:MaxItems=14
```

  Do we still need to increase it?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-11T10:45:16Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:133** by [@mszadkow](https://github.com/mszadkow) — 2024-10-11T10:45:16Z
```diff
@@ -121,15 +121,16 @@ type SupportedMode struct {
 	// The raycluster flag used only for the RayJob mode.
 	// The request flag used only for Interactive and Job modes.
 	// The cmd flag used only for Interactive, Job, and RayJob.
+	// The skip-priority-workload and priority flags can be used in all modes except for Interactive mode.
 	// If the raycluster flag are set, none of localqueue, replicas, min-replicas, or max-replicas can be set.
 	// For the Slurm mode, the possible values are: array, cpus-per-task, error, gpus-per-task, input, job-name, mem, mem-per-cpu,
-	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue, priority.
+	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue.
 	//
 	// cmd and requests values are going to be added only to the first primary container.
 	//
 	// +optional
 	// +listType=set
-	// +kubebuilder:validation:MaxItems=13
+	// +kubebuilder:validation:MaxItems=14
```

  no we don't my bad

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-10-11T12:32:49Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:96** by [@mwielgus](https://github.com/mwielgus) — 2024-10-11T12:32:48Z
```diff
@@ -93,7 +93,7 @@ type TemplateReference string
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('ntasks' in self.requiredFlags) || self.name == 'Slurm'", message="ntasks flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('output' in self.requiredFlags) || self.name == 'Slurm'", message="output flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('partition' in self.requiredFlags) || self.name == 'Slurm'", message="partition flag can be used only on Slurm mode"
-// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name == 'Slurm'", message="priority flag can be used only on Slurm mode"
+// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name != 'Interactive'", message="priority flag can't be used on Interactive mode"
```

  Why interactive is not allowed?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-11T13:54:35Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:96** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-11T13:54:34Z
```diff
@@ -93,7 +93,7 @@ type TemplateReference string
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('ntasks' in self.requiredFlags) || self.name == 'Slurm'", message="ntasks flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('output' in self.requiredFlags) || self.name == 'Slurm'", message="output flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('partition' in self.requiredFlags) || self.name == 'Slurm'", message="partition flag can be used only on Slurm mode"
-// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name == 'Slurm'", message="priority flag can be used only on Slurm mode"
+// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name != 'Interactive'", message="priority flag can't be used on Interactive mode"
```

  In interactive mode we are using plain pod to run interactive shell. As I can see on this table https://kueue.sigs.k8s.io/docs/overview/#job-integrated-features it's still not supported. 
  
  Please tell me if I'm wrong.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-11T14:46:32Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:96** by [@mszadkow](https://github.com/mszadkow) — 2024-10-11T14:46:32Z
```diff
@@ -93,7 +93,7 @@ type TemplateReference string
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('ntasks' in self.requiredFlags) || self.name == 'Slurm'", message="ntasks flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('output' in self.requiredFlags) || self.name == 'Slurm'", message="output flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('partition' in self.requiredFlags) || self.name == 'Slurm'", message="partition flag can be used only on Slurm mode"
-// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name == 'Slurm'", message="priority flag can be used only on Slurm mode"
+// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name != 'Interactive'", message="priority flag can't be used on Interactive mode"
```

  That was false assumption that pods don't allow to use
  WorkloadPriorityClass.
  We double checked with the team and it is not the case -  it will be fixed.
  Thanks for spotting that!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-14T07:54:47Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:96** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-14T07:54:47Z
```diff
@@ -93,7 +93,7 @@ type TemplateReference string
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('ntasks' in self.requiredFlags) || self.name == 'Slurm'", message="ntasks flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('output' in self.requiredFlags) || self.name == 'Slurm'", message="output flag can be used only on Slurm mode"
 // +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('partition' in self.requiredFlags) || self.name == 'Slurm'", message="partition flag can be used only on Slurm mode"
-// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name == 'Slurm'", message="priority flag can be used only on Slurm mode"
+// +kubebuilder:validation:XValidation:rule="!has(self.requiredFlags) || !('priority' in self.requiredFlags) || self.name != 'Interactive'", message="priority flag can't be used on Interactive mode"
```

  Fixed on docs https://github.com/kubernetes-sigs/kueue/pull/3216.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-14T08:02:05Z

- **cmd/experimental/kjobctl/pkg/builder/job_builder.go:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-14T07:59:25Z
```diff
@@ -48,6 +49,10 @@ func (b *jobBuilder) build(ctx context.Context) (runtime.Object, []runtime.Objec
 
 	job.Spec.Template.Spec = b.buildPodSpec(job.Spec.Template.Spec)
 
+	if len(b.priority) != 0 {
+		job.Labels[kueueconstants.WorkloadPriorityClassLabel] = b.priority
+	}
+
```

  Can we move it to `builder` as well? I think `b.buildObjectMeta()` would be a good place.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-14T11:37:00Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:132** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-14T11:37:00Z
```diff
@@ -121,15 +120,16 @@ type SupportedMode struct {
 	// The raycluster flag used only for the RayJob mode.
 	// The request flag used only for Interactive and Job modes.
 	// The cmd flag used only for Interactive, Job, and RayJob.
+	// The skip-priority-workload and priority flags can be used in all modes.
 	// If the raycluster flag are set, none of localqueue, replicas, min-replicas, or max-replicas can be set.
 	// For the Slurm mode, the possible values are: array, cpus-per-task, error, gpus-per-task, input, job-name, mem, mem-per-cpu,
-	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue, priority.
+	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue.
 	//
 	// cmd and requests values are going to be added only to the first primary container.
 	//
 	// +optional
 	// +listType=set
-	// +kubebuilder:validation:MaxItems=13
+	// +kubebuilder:validation:MaxItems=12
```

  Not sure that we should decrease it. We are not removing this flag from Slurm. So I think it still should be 13.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-14T12:25:41Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:132** by [@mszadkow](https://github.com/mszadkow) — 2024-10-14T12:25:41Z
```diff
@@ -121,15 +120,16 @@ type SupportedMode struct {
 	// The raycluster flag used only for the RayJob mode.
 	// The request flag used only for Interactive and Job modes.
 	// The cmd flag used only for Interactive, Job, and RayJob.
+	// The skip-priority-workload and priority flags can be used in all modes.
 	// If the raycluster flag are set, none of localqueue, replicas, min-replicas, or max-replicas can be set.
 	// For the Slurm mode, the possible values are: array, cpus-per-task, error, gpus-per-task, input, job-name, mem, mem-per-cpu,
-	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue, priority.
+	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue.
 	//
 	// cmd and requests values are going to be added only to the first primary container.
 	//
 	// +optional
 	// +listType=set
-	// +kubebuilder:validation:MaxItems=13
+	// +kubebuilder:validation:MaxItems=12
```

  We remove here only validation rules, which indeed was removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-14T12:27:59Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:132** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-14T12:27:59Z
```diff
@@ -121,15 +120,16 @@ type SupportedMode struct {
 	// The raycluster flag used only for the RayJob mode.
 	// The request flag used only for Interactive and Job modes.
 	// The cmd flag used only for Interactive, Job, and RayJob.
+	// The skip-priority-workload and priority flags can be used in all modes.
 	// If the raycluster flag are set, none of localqueue, replicas, min-replicas, or max-replicas can be set.
 	// For the Slurm mode, the possible values are: array, cpus-per-task, error, gpus-per-task, input, job-name, mem, mem-per-cpu,
-	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue, priority.
+	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue.
 	//
 	// cmd and requests values are going to be added only to the first primary container.
 	//
 	// +optional
 	// +listType=set
-	// +kubebuilder:validation:MaxItems=13
+	// +kubebuilder:validation:MaxItems=12
```

  Yeah, but count of Items shouldn't be changed. No?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-14T13:51:37Z

- **cmd/experimental/kjobctl/apis/v1alpha1/application_profile_types.go:132** by [@mszadkow](https://github.com/mszadkow) — 2024-10-14T13:51:37Z
```diff
@@ -121,15 +120,16 @@ type SupportedMode struct {
 	// The raycluster flag used only for the RayJob mode.
 	// The request flag used only for Interactive and Job modes.
 	// The cmd flag used only for Interactive, Job, and RayJob.
+	// The skip-priority-workload and priority flags can be used in all modes.
 	// If the raycluster flag are set, none of localqueue, replicas, min-replicas, or max-replicas can be set.
 	// For the Slurm mode, the possible values are: array, cpus-per-task, error, gpus-per-task, input, job-name, mem, mem-per-cpu,
-	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue, priority.
+	// mem-per-gpu, mem-per-task, nodes, ntasks, output, partition, localqueue.
 	//
 	// cmd and requests values are going to be added only to the first primary container.
 	//
 	// +optional
 	// +listType=set
-	// +kubebuilder:validation:MaxItems=13
+	// +kubebuilder:validation:MaxItems=12
```

  agree

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-17T12:59:47Z

/lgtm

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-19T16:04:11Z

/approve
