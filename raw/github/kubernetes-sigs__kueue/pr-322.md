# PR #322: Avoid queueing workloads that don't match CQ namespaceSelector

**Summary**: Avoid queueing workloads that don't match CQ namespaceSelector

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/322

**Last updated**: 2022-08-12T18:48:34Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-ns4` → `main`
- **Created**: 2022-08-10T20:15:47Z
- **Updated**: 2022-08-12T18:48:34Z
- **Merged**: 2022-08-12T18:48:34Z
- **Closed**: 2022-08-12T18:48:34Z
- **Labels**: `kind/bug`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 17   **+490 / -326**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

This PR updates the scheduler to avoid re-queueing workloads that don't match the cq NamespaceSelector.

The initial idea was to not add those workloads when first observed, but this is not enough to address this issue since a namespace label or CQ namespaceSelector could change by the time the workload that was initially accepted into the queue. Those changes could make the workload inadmissible due to not matching namespaceSelector, and so we still need a code path that handles the case during re-queueing.

The consequence is that such workloads will get evaluated, but at most once. To optimize away this wasted cycle, we will need to avoid adding the workload from the beginning, but this can be done as a followup because this PR is too long (I already made the changes to the workload controller to update the workload status for this case on another branch).

#### Which issue(s) this PR fixes:

Fixes https://github.com/kubernetes-sigs/kueue/issues/301

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-10T20:16:00Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/322#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-10T23:18:14Z

/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-11T19:11:15Z

/hold
to prevent premature merge

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T19:22:43Z

> I'm thinking if it's worth simply not adding a Workload to the queue system at all if the namespace doesn't match. Then, when there is a namespace update, we could use the goclient to list all the workloads in the informer's cache. Then these workloads wouldn't show up in the "pending" metric. Although that might be undesired?

I thought about this approach, but my conclusion was that it might be better to unify how we deal with inadmissible workloads. While the PR is large, most of it is a refactor that is agnostic to this specific issue, and I think can help us long term when deciding to be selective on re-queueing.

We can still do the optimization related to not adding the workload on add, but I think we still need to track those workloads and report them via a metric somewhere. May be we can change inadmissibleWorkloads into a map to to breakdown by requeue reason, and use that in the pending metric as well.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T17:34:53Z

All comments should now be addressed now.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T18:36:47Z

> /lgtm with a nit

thanks, fixed and squashed.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-12T18:47:44Z

/lgtm
/hold cancel

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-11T10:46:59Z

- **pkg/queue/cluster_queue_impl.go:96** by [@kerthcet](https://github.com/kerthcet) — 2022-08-11T08:38:01Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
```

  We can also add `namespaceSelector` filter logic here. When the namespaceSelector not match, we should put it into the inadmissibleWorkloads.

- **pkg/queue/cluster_queue_impl.go:90** by [@kerthcet](https://github.com/kerthcet) — 2022-08-11T09:29:11Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
```

  What if we changed the workload.Obj.Namespace?

- **pkg/queue/cluster_queue_impl.go:44** by [@kerthcet](https://github.com/kerthcet) — 2022-08-11T09:34:42Z
```diff
@@ -31,21 +39,30 @@ type ClusterQueueImpl struct {
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
 
-	heap   heap.Heap
-	cohort string
+	heap              heap.Heap
+	cohort            string
+	namespaceSelector labels.Selector
+
+	// inadmissibleWorkloads are workloads that have been tried at least once and couldn't be admitted.
+	inadmissibleWorkloads map[string]*workload.Info
 }
 
 func newClusterQueueImpl(keyFunc func(obj interface{}) string, lessFunc func(a, b interface{}) bool) *ClusterQueueImpl {
 	return &ClusterQueueImpl{
-		heap: heap.New(keyFunc, lessFunc),
+		heap:                  heap.New(keyFunc, lessFunc),
+		inadmissibleWorkloads: make(map[string]*workload.Info),
 	}
 }
 
-var _ ClusterQueue = &ClusterQueueImpl{}
```

  If we don't implement ClusterQueue, we should change the struct name.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-11T11:10:56Z

- **pkg/queue/cluster_queue_impl.go:163** by [@kerthcet](https://github.com/kerthcet) — 2022-08-11T11:07:14Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
-}
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
+	if len(c.inadmissibleWorkloads) == 0 {
+		return false
+	}
+
+	inadmissibleWorkloads := make(map[string]*workload.Info)
+	for key, wInfo := range c.inadmissibleWorkloads {
+		ns := corev1.Namespace{}
+		err := client.Get(context.Background(), types.NamespacedName{Name: wInfo.Obj.Namespace}, &ns)
+		if err != nil || !c.namespaceSelector.Matches(labels.Set(ns.Labels)) {
+			inadmissibleWorkloads[key] = wInfo
+		} else {
+			c.heap.PushIfNotPresent(wInfo)
+		}
+	}
+
+	c.inadmissibleWorkloads = inadmissibleWorkloads
+	return true
```

  This might be false if all workloads not match the namespaceSelector or `PushIfNotPresent` always returns false.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-11T11:21:34Z

- **pkg/queue/cluster_queue_impl.go:119** by [@kerthcet](https://github.com/kerthcet) — 2022-08-11T11:21:15Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
-}
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
```

  In StrictFIFO, we should only check 1)If the namespaceSelector match, push into heap 2)If not match, push into inadmissibleWorkloads. So parameter `immediate` is not necessary. So, we should implement this logic in `ClusterQueueImpl`, StrictFIFO should just call the function, For BestEffortFIFO, it should depend on the `immediate` then.  Also, we can remove the `RequeueReason`.
  
  Correct me if I misunderstand the codes.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T15:41:55Z

- **pkg/queue/cluster_queue_impl.go:90** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T15:41:55Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
```

  namespace is an immutable field.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T15:44:49Z

- **pkg/queue/cluster_queue_impl.go:96** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T15:44:49Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
```

  We can, I wanted to do that initially (and already made the changes to the workload controller to update the workload status), but left it out of this PR to simplify things since this PR is already long.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T16:30:47Z

- **pkg/queue/cluster_queue_impl.go:44** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T16:30:47Z
```diff
@@ -31,21 +39,30 @@ type ClusterQueueImpl struct {
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
 
-	heap   heap.Heap
-	cohort string
+	heap              heap.Heap
+	cohort            string
+	namespaceSelector labels.Selector
+
+	// inadmissibleWorkloads are workloads that have been tried at least once and couldn't be admitted.
+	inadmissibleWorkloads map[string]*workload.Info
 }
 
 func newClusterQueueImpl(keyFunc func(obj interface{}) string, lessFunc func(a, b interface{}) bool) *ClusterQueueImpl {
 	return &ClusterQueueImpl{
-		heap: heap.New(keyFunc, lessFunc),
+		heap:                  heap.New(keyFunc, lessFunc),
+		inadmissibleWorkloads: make(map[string]*workload.Info),
 	}
 }
 
-var _ ClusterQueue = &ClusterQueueImpl{}
```

  True, I will update the PR to actually have a default implementation for RequeueIfNotPresent that defaults to the semantics of StrictFIFO

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T16:41:48Z

- **pkg/queue/cluster_queue_impl.go:163** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T16:41:48Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
-}
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
+	if len(c.inadmissibleWorkloads) == 0 {
+		return false
+	}
+
+	inadmissibleWorkloads := make(map[string]*workload.Info)
+	for key, wInfo := range c.inadmissibleWorkloads {
+		ns := corev1.Namespace{}
+		err := client.Get(context.Background(), types.NamespacedName{Name: wInfo.Obj.Namespace}, &ns)
+		if err != nil || !c.namespaceSelector.Matches(labels.Set(ns.Labels)) {
+			inadmissibleWorkloads[key] = wInfo
+		} else {
+			c.heap.PushIfNotPresent(wInfo)
+		}
+	}
+
+	c.inadmissibleWorkloads = inadmissibleWorkloads
+	return true
```

  true, updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-11T19:11:01Z

I'm thinking if it's worth simply not adding a Workload to the queue system at all if the namespace doesn't match.
Then, when there is a namespace update, we could use the goclient to list all the workloads in the informer's cache.
Then these workloads wouldn't show up in the "pending" metric. Although that might be undesired?

- **pkg/queue/cluster_queue_impl.go:101** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-11T18:23:52Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
```

  nit: call `workload.Key` once

- **pkg/scheduler/scheduler.go:525** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-11T18:38:20Z
```diff
@@ -520,15 +522,19 @@ func (e entryOrdering) Less(i, j int) bool {
 }
 
 func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
-	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != "")
+	if e.status != "" && e.requeueReason == queue.RequeueReasonGeneric {
```

  for readability, we probably need a constant for empty status.

- **pkg/queue/cluster_queue_best_effort_fifo.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-11T18:49:42Z
```diff
@@ -39,90 +34,13 @@ const BestEffortFIFO = kueue.BestEffortFIFO
 func newClusterQueueBestEffortFIFO(cq *kueue.ClusterQueue) (ClusterQueue, error) {
 	cqImpl := newClusterQueueImpl(keyFunc, byCreationTime)
 	cqBE := &ClusterQueueBestEffortFIFO{
-		ClusterQueueImpl:      cqImpl,
-		inadmissibleWorkloads: make(map[string]*workload.Info),
-	}
-
-	cqBE.Update(cq)
-	return cqBE, nil
-}
-
-func (cq *ClusterQueueBestEffortFIFO) PushOrUpdate(wInfo *workload.Info) {
-	key := workload.Key(wInfo.Obj)
-	oldInfo := cq.inadmissibleWorkloads[key]
-	if oldInfo != nil {
-		// update in place if the workload was inadmissible and didn't change
-		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
-			cq.inadmissibleWorkloads[key] = wInfo
-			return
-		}
-		// otherwise move or update in place in the queue.
-		delete(cq.inadmissibleWorkloads, key)
-	}
-
-	cq.ClusterQueueImpl.PushOrUpdate(wInfo)
-}
-
-func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.Workload) {
-	delete(cq.inadmissibleWorkloads, workload.Key(w))
-	cq.ClusterQueueImpl.Delete(w)
-}
-
-func (cq *ClusterQueueBestEffortFIFO) DeleteFromQueue(q *Queue) {
-	for _, w := range q.items {
-		key := workload.Key(w.Obj)
-		if wl := cq.inadmissibleWorkloads[key]; wl != nil {
-			delete(cq.inadmissibleWorkloads, key)
-		}
-	}
-	cq.ClusterQueueImpl.DeleteFromQueue(q)
-}
-
-// RequeueIfNotPresent inserts a workload that cannot be admitted into
-// ClusterQueue, unless it is already in the queue. If immediate is true,
-// the workload will be pushed back to heap directly. If not,
-// the workload will be put into the inadmissibleWorkloads.
-func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
-	key := workload.Key(wInfo.Obj)
-	if immediate {
-		// If the workload was inadmissible, move it back into the queue.
-		inadmissibleWl := cq.inadmissibleWorkloads[key]
-		if inadmissibleWl != nil {
-			wInfo = inadmissibleWl
-			delete(cq.inadmissibleWorkloads, key)
-		}
-		return cq.ClusterQueueImpl.pushIfNotPresent(wInfo)
-	}
-
-	if cq.inadmissibleWorkloads[key] != nil {
-		return false
-	}
-
-	if data := cq.heap.GetByKey(key); data != nil {
-		return false
-	}
-
-	cq.inadmissibleWorkloads[key] = wInfo
-
-	return true
-}
-
-// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
-// If at least one workload is moved, returns true. Otherwise returns false.
-func (cq *ClusterQueueBestEffortFIFO) QueueInadmissibleWorkloads() bool {
-	if len(cq.inadmissibleWorkloads) == 0 {
-		return false
-	}
-
-	for _, wInfo := range cq.inadmissibleWorkloads {
-		cq.ClusterQueueImpl.pushIfNotPresent(wInfo)
+		ClusterQueueImpl: cqImpl,
 	}
 
-	cq.inadmissibleWorkloads = make(map[string]*workload.Info)
-	return true
+	err := cqBE.Update(cq)
+	return cqBE, err
 }
 
-func (cq *ClusterQueueBestEffortFIFO) Pending() int32 {
-	return cq.ClusterQueueImpl.Pending() + int32(len(cq.inadmissibleWorkloads))
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	return cq.ClusterQueueImpl.requeueIfNotPresent(wInfo, reason == RequeueReasonFailedAfterNomination)
```

  we still need a unit test for this check.

- **pkg/queue/cluster_queue_impl.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-11T18:59:38Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
+	// namespace selector.
+	return c.requeueIfNotPresent(wInfo, reason != RequeueReasonNamespaceMismatch)
 }
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+// requeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) requeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
+
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
```

  pass a context.
  
  Although... (could be in a follow up), we already know which namespace was updated in `cqNamespaceHandler`. So we could pass the name to this function.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T20:56:19Z

- **pkg/queue/cluster_queue_impl.go:154** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T20:56:19Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
+	// namespace selector.
+	return c.requeueIfNotPresent(wInfo, reason != RequeueReasonNamespaceMismatch)
 }
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+// requeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) requeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
+
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
```

  not the only place where this is called though.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T20:56:24Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:45** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T20:56:24Z
```diff
@@ -39,90 +34,13 @@ const BestEffortFIFO = kueue.BestEffortFIFO
 func newClusterQueueBestEffortFIFO(cq *kueue.ClusterQueue) (ClusterQueue, error) {
 	cqImpl := newClusterQueueImpl(keyFunc, byCreationTime)
 	cqBE := &ClusterQueueBestEffortFIFO{
-		ClusterQueueImpl:      cqImpl,
-		inadmissibleWorkloads: make(map[string]*workload.Info),
-	}
-
-	cqBE.Update(cq)
-	return cqBE, nil
-}
-
-func (cq *ClusterQueueBestEffortFIFO) PushOrUpdate(wInfo *workload.Info) {
-	key := workload.Key(wInfo.Obj)
-	oldInfo := cq.inadmissibleWorkloads[key]
-	if oldInfo != nil {
-		// update in place if the workload was inadmissible and didn't change
-		// to potentially become admissible.
-		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
-			cq.inadmissibleWorkloads[key] = wInfo
-			return
-		}
-		// otherwise move or update in place in the queue.
-		delete(cq.inadmissibleWorkloads, key)
-	}
-
-	cq.ClusterQueueImpl.PushOrUpdate(wInfo)
-}
-
-func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.Workload) {
-	delete(cq.inadmissibleWorkloads, workload.Key(w))
-	cq.ClusterQueueImpl.Delete(w)
-}
-
-func (cq *ClusterQueueBestEffortFIFO) DeleteFromQueue(q *Queue) {
-	for _, w := range q.items {
-		key := workload.Key(w.Obj)
-		if wl := cq.inadmissibleWorkloads[key]; wl != nil {
-			delete(cq.inadmissibleWorkloads, key)
-		}
-	}
-	cq.ClusterQueueImpl.DeleteFromQueue(q)
-}
-
-// RequeueIfNotPresent inserts a workload that cannot be admitted into
-// ClusterQueue, unless it is already in the queue. If immediate is true,
-// the workload will be pushed back to heap directly. If not,
-// the workload will be put into the inadmissibleWorkloads.
-func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
-	key := workload.Key(wInfo.Obj)
-	if immediate {
-		// If the workload was inadmissible, move it back into the queue.
-		inadmissibleWl := cq.inadmissibleWorkloads[key]
-		if inadmissibleWl != nil {
-			wInfo = inadmissibleWl
-			delete(cq.inadmissibleWorkloads, key)
-		}
-		return cq.ClusterQueueImpl.pushIfNotPresent(wInfo)
-	}
-
-	if cq.inadmissibleWorkloads[key] != nil {
-		return false
-	}
-
-	if data := cq.heap.GetByKey(key); data != nil {
-		return false
-	}
-
-	cq.inadmissibleWorkloads[key] = wInfo
-
-	return true
-}
-
-// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
-// If at least one workload is moved, returns true. Otherwise returns false.
-func (cq *ClusterQueueBestEffortFIFO) QueueInadmissibleWorkloads() bool {
-	if len(cq.inadmissibleWorkloads) == 0 {
-		return false
-	}
-
-	for _, wInfo := range cq.inadmissibleWorkloads {
-		cq.ClusterQueueImpl.pushIfNotPresent(wInfo)
+		ClusterQueueImpl: cqImpl,
 	}
 
-	cq.inadmissibleWorkloads = make(map[string]*workload.Info)
-	return true
+	err := cqBE.Update(cq)
+	return cqBE, err
 }
 
-func (cq *ClusterQueueBestEffortFIFO) Pending() int32 {
-	return cq.ClusterQueueImpl.Pending() + int32(len(cq.inadmissibleWorkloads))
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	return cq.ClusterQueueImpl.requeueIfNotPresent(wInfo, reason == RequeueReasonFailedAfterNomination)
```

  done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T20:56:31Z

- **pkg/queue/cluster_queue_impl.go:101** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T20:56:31Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
```

  done.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T21:03:05Z

- **pkg/scheduler/scheduler.go:525** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T21:03:05Z
```diff
@@ -520,15 +522,19 @@ func (e entryOrdering) Less(i, j int) bool {
 }
 
 func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
-	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != "")
+	if e.status != "" && e.requeueReason == queue.RequeueReasonGeneric {
```

  done.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-11T21:44:53Z

- **pkg/queue/cluster_queue_impl.go:119** by [@ahg-g](https://github.com/ahg-g) — 2022-08-11T21:44:53Z
```diff
@@ -55,39 +72,99 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
-}
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
```

  the check on namespaceSelector happens outside the RequeueIfNotPresent function during the scheduling loop, so we need to pass that down, and the way we did it is via the `RequeueReason`; immediate is now an implementation detail of the interface

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-12T08:49:14Z

- **pkg/scheduler/scheduler.go:534** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T06:56:41Z
```diff
@@ -529,7 +531,7 @@ func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e ent
 	added := s.queues.RequeueWorkload(ctx, &e.Info, e.requeueReason)
 	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "clusterQueue", e.ClusterQueue, "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
 
-	if e.status == "" {
+	if e.status == notNominated {
```

  Also in L527.

- **pkg/queue/cluster_queue_impl.go:118** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T07:17:53Z
```diff
@@ -112,11 +114,17 @@ func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
 	}
 }
 
-// RequeueIfNotPresent inserts a workload that cannot be admitted into
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
```

  s/we don't requeue/we don't requeue immediately/ ?

- **pkg/scheduler/scheduler.go:161** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T07:33:03Z
```diff
@@ -157,6 +157,8 @@ const (
 	skipped entryStatus = "skipped"
 	// indicates if the workload was assumed to have been admitted.
 	assumed entryStatus = "assumed"
+	// indicates that the workload was never nominated for admission.
+	notNominated entryStatus = ""
```

  maybe `defaultNotNominated`, notNominated sounds like it was set on purpose, but actually it's the default value.

- **pkg/scheduler/scheduler.go:195** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T07:55:44Z
```diff
@@ -182,15 +185,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
 		} else if cq == nil {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleMsg = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleMsg = "Workload namespace doesn't match ClusterQueue selector"
+			e.requeueReason = queue.RequeueReasonNamespaceMismatch
```

  I think we should also update Workload Controller

- **pkg/queue/cluster_queue_interface.go:33** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T08:05:23Z
```diff
@@ -17,19 +17,29 @@ limitations under the License.
 package queue
 
 import (
+	"context"
 	"fmt"
 
 	"k8s.io/apimachinery/pkg/util/sets"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+type RequeueReason string
+
+const (
+	RequeueReasonFailedAfterNomination RequeueReason = "FailedAfterNomination"
```

  Prefer `FailedAfterNomination`, I think it's enough.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-12T14:27:26Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-12T14:19:42Z
```diff
@@ -20,160 +20,44 @@ import (
 	"testing"
 
 	"github.com/google/go-cmp/cmp"
-	"k8s.io/apimachinery/pkg/util/sets"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func TestClusterQueueBestEffortFIFO(t *testing.T) {
-	clusterQueue := utiltesting.MakeClusterQueue("cq").Obj()
-	var workloads = []*kueue.Workload{
-		utiltesting.MakeWorkload("w1", "ns1").Queue("q1").Obj(),
-		utiltesting.MakeWorkload("w2", "ns2").Queue("q2").Obj(),
-	}
-	var updatedWorkloads = make([]*kueue.Workload, len(workloads))
-
-	updatedWorkloads[0] = workloads[0].DeepCopy()
-	updatedWorkloads[0].Spec.QueueName = "q2"
-	updatedWorkloads[1] = workloads[1].DeepCopy()
-	updatedWorkloads[1].Spec.QueueName = "q1"
-
-	tests := map[string]struct {
-		workloadsToAdd                 []*kueue.Workload
-		inadmissibleWorkloadsToRequeue []*workload.Info
-		admissibleWorkloadsToRequeue   []*workload.Info
-		workloadsToUpdate              []*kueue.Workload
-		workloadsToDelete              []*kueue.Workload
-		queueInadmissibleWorkloads     bool
-		wantActiveWorkloads            sets.String
-		wantPending                    int32
+func TestBestEffortFIFORequeueIfNotPresent(t *testing.T) {
+	tests := map[RequeueReason]struct {
+		wantInadmissible bool
 	}{
-		"add, update, delete workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0], workloads[1]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[0]},
-			workloadsToDelete:              []*kueue.Workload{workloads[0]},
-			wantActiveWorkloads:            sets.NewString(workloads[1].Name),
-			wantPending:                    1,
-		},
-		"re-queue inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    2,
+		RequeueReasonFailedAfterNomination: {
+			wantInadmissible: false,
 		},
-		"re-queue admissible workload that was inadmissible": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			admissibleWorkloadsToRequeue:   []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
+		RequeueReasonNamespaceMismatch: {
+			wantInadmissible: true,
 		},
-		"re-queue inadmissible workload and flush": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"update inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[1]},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"delete inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToDelete:              []*kueue.Workload{workloads[1]},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    1,
+		RequeueReasonGeneric: {
+			wantInadmissible: true,
 		},
 	}
 
-	for name, test := range tests {
-		t.Run(name, func(t *testing.T) {
-			cq, err := newClusterQueueBestEffortFIFO(clusterQueue)
-			if err != nil {
-				t.Fatalf("Failed creating ClusterQueue %v", err)
-			}
-
-			for _, w := range test.workloadsToAdd {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.inadmissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, false)
-			}
-			for _, w := range test.admissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, true)
-			}
-
-			for _, w := range test.workloadsToUpdate {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.workloadsToDelete {
-				cq.Delete(w)
+	for reason, test := range tests {
+		t.Run(string(reason), func(t *testing.T) {
+			cq, _ := newClusterQueueBestEffortFIFO(&kueue.ClusterQueue{
+				Spec: kueue.ClusterQueueSpec{
+					QueueingStrategy: kueue.StrictFIFO,
+				},
+			})
+			wl := utiltesting.MakeWorkload("workload-1", defaultNamespace).Obj()
+			if ok := cq.RequeueIfNotPresent(workload.NewInfo(wl), reason); !ok {
+				t.Error("failed to requeue nonexistent workload")
 			}
 
-			if test.queueInadmissibleWorkloads {
-				cq.QueueInadmissibleWorkloads()
+			_, gotInadmissible := cq.(*ClusterQueueBestEffortFIFO).inadmissibleWorkloads[workload.Key(wl)]
+			if diff := cmp.Diff(test.wantInadmissible, gotInadmissible); diff != "" {
```

  nit: don't use Diff for booleans

- **pkg/queue/cluster_queue_impl.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-12T14:27:26Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
+	// namespace selector.
+	return c.requeueIfNotPresent(wInfo, reason != RequeueReasonNamespaceMismatch)
 }
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+// requeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) requeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
+
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
```

  you can pass `metav1.NamespaceAll` when the namespace is not important (it's the empty string)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-12T14:29:52Z

- **pkg/scheduler/scheduler.go:525** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-12T14:29:52Z
```diff
@@ -520,15 +522,19 @@ func (e entryOrdering) Less(i, j int) bool {
 }
 
 func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
-	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != "")
+	if e.status != "" && e.requeueReason == queue.RequeueReasonGeneric {
```

  use `notNominated` here

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T15:50:50Z

- **pkg/scheduler/scheduler.go:161** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T15:50:50Z
```diff
@@ -157,6 +157,8 @@ const (
 	skipped entryStatus = "skipped"
 	// indicates if the workload was assumed to have been admitted.
 	assumed entryStatus = "assumed"
+	// indicates that the workload was never nominated for admission.
+	notNominated entryStatus = ""
```

  I don't feel it is necessary, what is important is to describe the semantics of the value; based on the current logic it means the entry is not nominated.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T15:52:59Z

- **pkg/scheduler/scheduler.go:195** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T15:52:59Z
```diff
@@ -182,15 +185,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
 		} else if cq == nil {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleMsg = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleMsg = "Workload namespace doesn't match ClusterQueue selector"
+			e.requeueReason = queue.RequeueReasonNamespaceMismatch
```

  We can't update in the workload controller without having the logic to prevent adding the workload to the queue on Add. As we discussed before, we can do that as followup since this is a long PR.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T15:54:23Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:57** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T15:54:23Z
```diff
@@ -20,160 +20,44 @@ import (
 	"testing"
 
 	"github.com/google/go-cmp/cmp"
-	"k8s.io/apimachinery/pkg/util/sets"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func TestClusterQueueBestEffortFIFO(t *testing.T) {
-	clusterQueue := utiltesting.MakeClusterQueue("cq").Obj()
-	var workloads = []*kueue.Workload{
-		utiltesting.MakeWorkload("w1", "ns1").Queue("q1").Obj(),
-		utiltesting.MakeWorkload("w2", "ns2").Queue("q2").Obj(),
-	}
-	var updatedWorkloads = make([]*kueue.Workload, len(workloads))
-
-	updatedWorkloads[0] = workloads[0].DeepCopy()
-	updatedWorkloads[0].Spec.QueueName = "q2"
-	updatedWorkloads[1] = workloads[1].DeepCopy()
-	updatedWorkloads[1].Spec.QueueName = "q1"
-
-	tests := map[string]struct {
-		workloadsToAdd                 []*kueue.Workload
-		inadmissibleWorkloadsToRequeue []*workload.Info
-		admissibleWorkloadsToRequeue   []*workload.Info
-		workloadsToUpdate              []*kueue.Workload
-		workloadsToDelete              []*kueue.Workload
-		queueInadmissibleWorkloads     bool
-		wantActiveWorkloads            sets.String
-		wantPending                    int32
+func TestBestEffortFIFORequeueIfNotPresent(t *testing.T) {
+	tests := map[RequeueReason]struct {
+		wantInadmissible bool
 	}{
-		"add, update, delete workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0], workloads[1]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[0]},
-			workloadsToDelete:              []*kueue.Workload{workloads[0]},
-			wantActiveWorkloads:            sets.NewString(workloads[1].Name),
-			wantPending:                    1,
-		},
-		"re-queue inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    2,
+		RequeueReasonFailedAfterNomination: {
+			wantInadmissible: false,
 		},
-		"re-queue admissible workload that was inadmissible": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			admissibleWorkloadsToRequeue:   []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
+		RequeueReasonNamespaceMismatch: {
+			wantInadmissible: true,
 		},
-		"re-queue inadmissible workload and flush": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"update inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[1]},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"delete inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToDelete:              []*kueue.Workload{workloads[1]},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    1,
+		RequeueReasonGeneric: {
+			wantInadmissible: true,
 		},
 	}
 
-	for name, test := range tests {
-		t.Run(name, func(t *testing.T) {
-			cq, err := newClusterQueueBestEffortFIFO(clusterQueue)
-			if err != nil {
-				t.Fatalf("Failed creating ClusterQueue %v", err)
-			}
-
-			for _, w := range test.workloadsToAdd {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.inadmissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, false)
-			}
-			for _, w := range test.admissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, true)
-			}
-
-			for _, w := range test.workloadsToUpdate {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.workloadsToDelete {
-				cq.Delete(w)
+	for reason, test := range tests {
+		t.Run(string(reason), func(t *testing.T) {
+			cq, _ := newClusterQueueBestEffortFIFO(&kueue.ClusterQueue{
+				Spec: kueue.ClusterQueueSpec{
+					QueueingStrategy: kueue.StrictFIFO,
+				},
+			})
+			wl := utiltesting.MakeWorkload("workload-1", defaultNamespace).Obj()
+			if ok := cq.RequeueIfNotPresent(workload.NewInfo(wl), reason); !ok {
+				t.Error("failed to requeue nonexistent workload")
 			}
 
-			if test.queueInadmissibleWorkloads {
-				cq.QueueInadmissibleWorkloads()
+			_, gotInadmissible := cq.(*ClusterQueueBestEffortFIFO).inadmissibleWorkloads[workload.Key(wl)]
+			if diff := cmp.Diff(test.wantInadmissible, gotInadmissible); diff != "" {
```

  why not?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T15:55:15Z

- **pkg/queue/cluster_queue_interface.go:33** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T15:55:15Z
```diff
@@ -17,19 +17,29 @@ limitations under the License.
 package queue
 
 import (
+	"context"
 	"fmt"
 
 	"k8s.io/apimachinery/pkg/util/sets"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+type RequeueReason string
+
+const (
+	RequeueReasonFailedAfterNomination RequeueReason = "FailedAfterNomination"
```

  I am not sure because the `RequeueReasonGeneric` would look strange without the prefix (just `Generic`)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T16:02:54Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:57** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T16:02:54Z
```diff
@@ -20,160 +20,44 @@ import (
 	"testing"
 
 	"github.com/google/go-cmp/cmp"
-	"k8s.io/apimachinery/pkg/util/sets"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func TestClusterQueueBestEffortFIFO(t *testing.T) {
-	clusterQueue := utiltesting.MakeClusterQueue("cq").Obj()
-	var workloads = []*kueue.Workload{
-		utiltesting.MakeWorkload("w1", "ns1").Queue("q1").Obj(),
-		utiltesting.MakeWorkload("w2", "ns2").Queue("q2").Obj(),
-	}
-	var updatedWorkloads = make([]*kueue.Workload, len(workloads))
-
-	updatedWorkloads[0] = workloads[0].DeepCopy()
-	updatedWorkloads[0].Spec.QueueName = "q2"
-	updatedWorkloads[1] = workloads[1].DeepCopy()
-	updatedWorkloads[1].Spec.QueueName = "q1"
-
-	tests := map[string]struct {
-		workloadsToAdd                 []*kueue.Workload
-		inadmissibleWorkloadsToRequeue []*workload.Info
-		admissibleWorkloadsToRequeue   []*workload.Info
-		workloadsToUpdate              []*kueue.Workload
-		workloadsToDelete              []*kueue.Workload
-		queueInadmissibleWorkloads     bool
-		wantActiveWorkloads            sets.String
-		wantPending                    int32
+func TestBestEffortFIFORequeueIfNotPresent(t *testing.T) {
+	tests := map[RequeueReason]struct {
+		wantInadmissible bool
 	}{
-		"add, update, delete workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0], workloads[1]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[0]},
-			workloadsToDelete:              []*kueue.Workload{workloads[0]},
-			wantActiveWorkloads:            sets.NewString(workloads[1].Name),
-			wantPending:                    1,
-		},
-		"re-queue inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    2,
+		RequeueReasonFailedAfterNomination: {
+			wantInadmissible: false,
 		},
-		"re-queue admissible workload that was inadmissible": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			admissibleWorkloadsToRequeue:   []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
+		RequeueReasonNamespaceMismatch: {
+			wantInadmissible: true,
 		},
-		"re-queue inadmissible workload and flush": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"update inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[1]},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"delete inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToDelete:              []*kueue.Workload{workloads[1]},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    1,
+		RequeueReasonGeneric: {
+			wantInadmissible: true,
 		},
 	}
 
-	for name, test := range tests {
-		t.Run(name, func(t *testing.T) {
-			cq, err := newClusterQueueBestEffortFIFO(clusterQueue)
-			if err != nil {
-				t.Fatalf("Failed creating ClusterQueue %v", err)
-			}
-
-			for _, w := range test.workloadsToAdd {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.inadmissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, false)
-			}
-			for _, w := range test.admissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, true)
-			}
-
-			for _, w := range test.workloadsToUpdate {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.workloadsToDelete {
-				cq.Delete(w)
+	for reason, test := range tests {
+		t.Run(string(reason), func(t *testing.T) {
+			cq, _ := newClusterQueueBestEffortFIFO(&kueue.ClusterQueue{
+				Spec: kueue.ClusterQueueSpec{
+					QueueingStrategy: kueue.StrictFIFO,
+				},
+			})
+			wl := utiltesting.MakeWorkload("workload-1", defaultNamespace).Obj()
+			if ok := cq.RequeueIfNotPresent(workload.NewInfo(wl), reason); !ok {
+				t.Error("failed to requeue nonexistent workload")
 			}
 
-			if test.queueInadmissibleWorkloads {
-				cq.QueueInadmissibleWorkloads()
+			_, gotInadmissible := cq.(*ClusterQueueBestEffortFIFO).inadmissibleWorkloads[workload.Key(wl)]
+			if diff := cmp.Diff(test.wantInadmissible, gotInadmissible); diff != "" {
```

  the reason I sometimes like it is because it makes the error message look standard just like any other comparison we do.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-12T16:12:44Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-12T16:12:44Z
```diff
@@ -20,160 +20,44 @@ import (
 	"testing"
 
 	"github.com/google/go-cmp/cmp"
-	"k8s.io/apimachinery/pkg/util/sets"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func TestClusterQueueBestEffortFIFO(t *testing.T) {
-	clusterQueue := utiltesting.MakeClusterQueue("cq").Obj()
-	var workloads = []*kueue.Workload{
-		utiltesting.MakeWorkload("w1", "ns1").Queue("q1").Obj(),
-		utiltesting.MakeWorkload("w2", "ns2").Queue("q2").Obj(),
-	}
-	var updatedWorkloads = make([]*kueue.Workload, len(workloads))
-
-	updatedWorkloads[0] = workloads[0].DeepCopy()
-	updatedWorkloads[0].Spec.QueueName = "q2"
-	updatedWorkloads[1] = workloads[1].DeepCopy()
-	updatedWorkloads[1].Spec.QueueName = "q1"
-
-	tests := map[string]struct {
-		workloadsToAdd                 []*kueue.Workload
-		inadmissibleWorkloadsToRequeue []*workload.Info
-		admissibleWorkloadsToRequeue   []*workload.Info
-		workloadsToUpdate              []*kueue.Workload
-		workloadsToDelete              []*kueue.Workload
-		queueInadmissibleWorkloads     bool
-		wantActiveWorkloads            sets.String
-		wantPending                    int32
+func TestBestEffortFIFORequeueIfNotPresent(t *testing.T) {
+	tests := map[RequeueReason]struct {
+		wantInadmissible bool
 	}{
-		"add, update, delete workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0], workloads[1]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[0]},
-			workloadsToDelete:              []*kueue.Workload{workloads[0]},
-			wantActiveWorkloads:            sets.NewString(workloads[1].Name),
-			wantPending:                    1,
-		},
-		"re-queue inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    2,
+		RequeueReasonFailedAfterNomination: {
+			wantInadmissible: false,
 		},
-		"re-queue admissible workload that was inadmissible": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			admissibleWorkloadsToRequeue:   []*workload.Info{workload.NewInfo(workloads[1])},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
+		RequeueReasonNamespaceMismatch: {
+			wantInadmissible: true,
 		},
-		"re-queue inadmissible workload and flush": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"update inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToUpdate:              []*kueue.Workload{updatedWorkloads[1]},
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name, workloads[1].Name),
-			wantPending:                    2,
-		},
-		"delete inadmissible workload": {
-			workloadsToAdd:                 []*kueue.Workload{workloads[0]},
-			inadmissibleWorkloadsToRequeue: []*workload.Info{workload.NewInfo(workloads[1])},
-			workloadsToDelete:              []*kueue.Workload{workloads[1]},
-			queueInadmissibleWorkloads:     true,
-			wantActiveWorkloads:            sets.NewString(workloads[0].Name),
-			wantPending:                    1,
+		RequeueReasonGeneric: {
+			wantInadmissible: true,
 		},
 	}
 
-	for name, test := range tests {
-		t.Run(name, func(t *testing.T) {
-			cq, err := newClusterQueueBestEffortFIFO(clusterQueue)
-			if err != nil {
-				t.Fatalf("Failed creating ClusterQueue %v", err)
-			}
-
-			for _, w := range test.workloadsToAdd {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.inadmissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, false)
-			}
-			for _, w := range test.admissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, true)
-			}
-
-			for _, w := range test.workloadsToUpdate {
-				cq.PushOrUpdate(workload.NewInfo(w))
-			}
-
-			for _, w := range test.workloadsToDelete {
-				cq.Delete(w)
+	for reason, test := range tests {
+		t.Run(string(reason), func(t *testing.T) {
+			cq, _ := newClusterQueueBestEffortFIFO(&kueue.ClusterQueue{
+				Spec: kueue.ClusterQueueSpec{
+					QueueingStrategy: kueue.StrictFIFO,
+				},
+			})
+			wl := utiltesting.MakeWorkload("workload-1", defaultNamespace).Obj()
+			if ok := cq.RequeueIfNotPresent(workload.NewInfo(wl), reason); !ok {
+				t.Error("failed to requeue nonexistent workload")
 			}
 
-			if test.queueInadmissibleWorkloads {
-				cq.QueueInadmissibleWorkloads()
+			_, gotInadmissible := cq.(*ClusterQueueBestEffortFIFO).inadmissibleWorkloads[workload.Key(wl)]
+			if diff := cmp.Diff(test.wantInadmissible, gotInadmissible); diff != "" {
```

  It makes the test output longer... But your argument makes sense too.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-12T16:13:47Z

- **pkg/scheduler/scheduler.go:195** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T16:13:47Z
```diff
@@ -182,15 +185,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
 		} else if cq == nil {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleMsg = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleMsg = "Workload namespace doesn't match ClusterQueue selector"
+			e.requeueReason = queue.RequeueReasonNamespaceMismatch
```

  I mean we should update the workload status to indicate that it was inadmissible for `namespaceSelector` not match.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T16:17:16Z

- **pkg/queue/cluster_queue_impl.go:154** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T16:17:16Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
+	// namespace selector.
+	return c.requeueIfNotPresent(wInfo, reason != RequeueReasonNamespaceMismatch)
 }
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+// requeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) requeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
+
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
```

  ok, I will make the change in this PR

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-12T16:18:31Z

- **pkg/scheduler/scheduler.go:161** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T16:18:23Z
```diff
@@ -157,6 +157,8 @@ const (
 	skipped entryStatus = "skipped"
 	// indicates if the workload was assumed to have been admitted.
 	assumed entryStatus = "assumed"
+	// indicates that the workload was never nominated for admission.
+	notNominated entryStatus = ""
```

  Also make sense.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T16:20:15Z

- **pkg/scheduler/scheduler.go:195** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T16:20:14Z
```diff
@@ -182,15 +185,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
 		} else if cq == nil {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleMsg = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleMsg = "Workload namespace doesn't match ClusterQueue selector"
+			e.requeueReason = queue.RequeueReasonNamespaceMismatch
```

  We are doing that, the inadmissibleMsg will show up in the workload status.
  
  https://github.com/kubernetes-sigs/kueue/blob/73659ad78c0c935048914d94f94c756522c72133/pkg/scheduler/scheduler.go#L527

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-12T16:32:03Z

- **pkg/scheduler/scheduler.go:195** by [@kerthcet](https://github.com/kerthcet) — 2022-08-12T16:32:03Z
```diff
@@ -182,15 +185,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
 		} else if cq == nil {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleMsg = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleMsg = "Workload namespace doesn't match ClusterQueue selector"
+			e.requeueReason = queue.RequeueReasonNamespaceMismatch
```

  Then it works well now, but if we put `namespaceSelector not match` workload to inadmissibleWorkloads in `PushOrUpdate`, then we'll not update the status at that case. Just FYI.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T16:37:40Z

- **pkg/scheduler/scheduler.go:195** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T16:37:40Z
```diff
@@ -182,15 +185,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s is inactive", w.ClusterQueue)
 		} else if cq == nil {
-			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+			e.inadmissibleMsg = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleMsg = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleMsg = "Workload namespace doesn't match ClusterQueue selector"
+			e.requeueReason = queue.RequeueReasonNamespaceMismatch
```

  As I mentioned in the comment above, if we do that change (the check on namespaceSelector when the workload is first added), we need to update the workload controller as well to update the workload status.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T17:33:00Z

- **pkg/queue/cluster_queue_impl.go:154** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T17:33:00Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
+	// namespace selector.
+	return c.requeueIfNotPresent(wInfo, reason != RequeueReasonNamespaceMismatch)
 }
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+// requeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) requeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
+
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
```

  Started to do it, but halfway through I felt the change not really worth it, at least not now. 
  
  Unless we use an options pattern with a default of `metav1.NamespaceAll`, I am afraid we may make a mistake somewhere calling this function incorrectly while restricting it to a namespace, also the only place that it currently makes sense to use it is when a namespace changes its labels, which is rather infrequent.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T17:34:20Z

- **pkg/scheduler/scheduler.go:525** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T17:34:19Z
```diff
@@ -520,15 +522,19 @@ func (e entryOrdering) Less(i, j int) bool {
 }
 
 func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
-	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != "")
+	if e.status != "" && e.requeueReason == queue.RequeueReasonGeneric {
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-12T18:18:14Z

- **pkg/queue/cluster_queue_impl.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-12T18:18:14Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
+	// namespace selector.
+	return c.requeueIfNotPresent(wInfo, reason != RequeueReasonNamespaceMismatch)
 }
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+// requeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) requeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
+
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
```

  I just don't like that we call into the client so much. Although it's cached. But sure, it can be a follow up.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-12T18:19:41Z

/lgtm
with a nit

- **pkg/controller/core/resourceflavor_controller.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-12T18:18:40Z
```diff
@@ -128,7 +129,8 @@ func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	log.V(2).Info("ResourceFlavor delete event")
 
 	if cqNames := r.cache.DeleteResourceFlavor(flv); len(cqNames) > 0 {
-		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		ctx := ctrl.LoggerInto(context.Background(), log)
```

  in clusterqueue_controller.go you are just passing the context without a log, which I think it's fine as we are not logging anything in the function below.
  
  For consistency, we can keep it simple and pass the bare context.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T18:24:53Z

- **pkg/queue/cluster_queue_impl.go:154** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T18:24:53Z
```diff
@@ -55,39 +74,106 @@ func (c *ClusterQueueImpl) Cohort() string {
 func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
 	added := false
 	for _, info := range q.items {
-		if c.pushIfNotPresent(info) {
+		if c.heap.PushIfNotPresent(info) {
 			added = true
 		}
 	}
 	return added
 }
 
+func (c *ClusterQueueImpl) PushOrUpdate(wInfo *workload.Info) {
+	key := workload.Key(wInfo.Obj)
+	oldInfo := c.inadmissibleWorkloads[key]
+	if oldInfo != nil {
+		// update in place if the workload was inadmissible and didn't change
+		// to potentially become admissible.
+		if equality.Semantic.DeepEqual(oldInfo.Obj.Spec, wInfo.Obj.Spec) {
+			c.inadmissibleWorkloads[key] = wInfo
+			return
+		}
+		// otherwise move or update in place in the queue.
+		delete(c.inadmissibleWorkloads, key)
+	}
+	c.heap.PushOrUpdate(wInfo)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
+	delete(c.inadmissibleWorkloads, workload.Key(w))
+	c.heap.Delete(workload.Key(w))
+}
+
 func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		key := workload.Key(w.Obj)
+		if wl := c.inadmissibleWorkloads[key]; wl != nil {
+			delete(c.inadmissibleWorkloads, key)
+		}
+	}
 	for _, w := range q.items {
 		c.Delete(w.Obj)
 	}
 }
 
-// pushIfNotPresent pushes the workload to ClusterQueue.
-// If the workload is already present, returns false. Otherwise returns true.
-func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	return c.heap.PushIfNotPresent(info)
+func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueReason) bool {
+	// By default, the only reason we don't requeue is if the workload doesn't match the CQ's
+	// namespace selector.
+	return c.requeueIfNotPresent(wInfo, reason != RequeueReasonNamespaceMismatch)
 }
 
-func (c *ClusterQueueImpl) PushOrUpdate(info *workload.Info) {
-	c.heap.PushOrUpdate(info)
-}
+// requeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If immediate is true,
+// the workload will be pushed back to heap directly. If not,
+// the workload will be put into the inadmissibleWorkloads.
+func (c *ClusterQueueImpl) requeueIfNotPresent(wInfo *workload.Info, immediate bool) bool {
+	key := workload.Key(wInfo.Obj)
+	if immediate {
+		// If the workload was inadmissible, move it back into the queue.
+		inadmissibleWl := c.inadmissibleWorkloads[key]
+		if inadmissibleWl != nil {
+			wInfo = inadmissibleWl
+			delete(c.inadmissibleWorkloads, key)
+		}
+		return c.heap.PushIfNotPresent(wInfo)
+	}
 
-func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	c.heap.Delete(workload.Key(w))
+	if c.inadmissibleWorkloads[key] != nil {
+		return false
+	}
+
+	if data := c.heap.GetByKey(key); data != nil {
+		return false
+	}
+
+	c.inadmissibleWorkloads[key] = wInfo
+
+	return true
 }
 
-func (c *ClusterQueueImpl) RequeueIfNotPresent(wInfo *workload.Info, _ bool) bool {
-	return c.pushIfNotPresent(wInfo)
+// QueueInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+// If at least one workload is moved, returns true. Otherwise returns false.
+func (c *ClusterQueueImpl) QueueInadmissibleWorkloads(client client.Client) bool {
```

  Yes, but this optimization wouldn't save us much because in the vast majority of the cases we are calling the function with NamespaceAll.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-12T18:34:27Z

- **pkg/controller/core/resourceflavor_controller.go:132** by [@ahg-g](https://github.com/ahg-g) — 2022-08-12T18:29:08Z
```diff
@@ -128,7 +129,8 @@ func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	log.V(2).Info("ResourceFlavor delete event")
 
 	if cqNames := r.cache.DeleteResourceFlavor(flv); len(cqNames) > 0 {
-		r.qManager.QueueInadmissibleWorkloads(cqNames)
+		ctx := ctrl.LoggerInto(context.Background(), log)
```

  ah, there are actually two instances in clusterqueue controller, one with a log and one without. I will change them all to use a bare context.
