# PR #3228: TAS: PodSet label and Workload annotation for PodTemplates

**Summary**: TAS: PodSet label and Workload annotation for PodTemplates

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3228

**Last updated**: 2025-01-21T11:25:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `podset-new-labels` → `main`
- **Created**: 2024-10-14T12:33:56Z
- **Updated**: 2025-01-21T11:25:41Z
- **Merged**: 2024-10-15T09:24:22Z
- **Closed**: 2024-10-15T09:24:22Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 5   **+78 / -1**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #2724

#### Special notes for your reviewer:

The annotation and label are used only for TAS now, but the names are generic since they can be used in other contexts too. For TAS they are used by TopologyUngater to lookup The admitted Workload from a Pod.

#### Does this PR introduce a user-facing change?

```release-note
Introduce the new PodTemplate annotation kueue.x-k8s.io/workload, and label kueue.x-k8s.io/podset.
The annotation and label are alpha-level and gated by the new TopologyAwareScheduling feature gate.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-14T12:33:59Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-14T12:34:02Z

/test all

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-14T12:34:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9ce743bf21467f5901f2564c08a44573f222692b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/670e2dccd94b730008e5cc77 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3228--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3228--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTMyMjgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.sLi8YYVNkj4SbBJZqX02fsh6kR15B2V8IzMaUf2Q8qU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3228--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-14T13:37:54Z

cc @PBundyra

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T14:28:14Z

/cc

### Comment by [@kannon92](https://github.com/kannon92) — 2024-10-14T14:34:38Z

/hold

Since author has approval rights, we should unhold when it is ready to merge.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-15T07:31:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3228#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-15T07:37:41Z

FYI following the discussion [here](https://github.com/kubernetes-sigs/kueue/pull/3228#discussion_r1799629864). I have updated the PR to introduce the TopologyAwareScheduling gate which I will also reuse in the follow up PRs for the feature. PTAL.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-15T09:23:17Z

/lgtm
/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-15T09:23:24Z

LGTM label has been added.  <details>Git tree hash: 413450b80b29c498e21873c3a408249d0ce8c058</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-10-14T14:32:04Z

- **pkg/controller/constants/constants.go:43** by [@kannon92](https://github.com/kannon92) — 2024-10-14T14:32:04Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
```

  So reading this I think its limiting to a K8s job. I think you mean a general kueue supported job but not sure what term we pick to reference this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-10-14T14:33:35Z

- **pkg/controller/jobs/pod/pod_controller_test.go:777** by [@kannon92](https://github.com/kannon92) — 2024-10-14T14:33:35Z
```diff
@@ -772,6 +774,8 @@ func TestReconciler(t *testing.T) {
 				*basePodWrapper.
 					Clone().
 					Label(constants.ManagedByKueueLabel, "true").
+					Label(controllerconsts.PodSetLabel, "dc85db45").
```

  Can you define these as constants and reuse that here?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-14T14:35:57Z

- **pkg/controller/constants/constants.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T14:34:09Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
+	// indicate the name of the admitted Workload corresponding to the Job. The
+	// annotation is set when starting the Job, and removed on stopping the Job.
+	WorkloadAnnotation = "kueue.x-k8s.io/workload"
```

  What is the difference between this annotation and the Workload name hash?
  
  https://github.com/kubernetes-sigs/kueue/blob/b6222fa0ec020bad0d93be9acdd0305348c4935a/pkg/controller/jobframework/workload_names.go#L35

- **pkg/controller/jobframework/reconciler.go:977** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T14:34:56Z
```diff
@@ -973,6 +973,8 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		if err != nil {
 			return nil, err
 		}
+		info.Labels[controllerconsts.PodSetLabel] = podSetFlavor.Name
+		info.Annotations[controllerconsts.WorkloadAnnotation] = w.Name
```

  Could we guard these labels and annotations insertion by the feature gates?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-14T14:36:57Z

- **pkg/controller/constants/constants.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T14:36:57Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
+	// indicate the name of the admitted Workload corresponding to the Job. The
+	// annotation is set when starting the Job, and removed on stopping the Job.
+	WorkloadAnnotation = "kueue.x-k8s.io/workload"
```

  Additionally, is there any case where this annotation and pre-build workload annotation?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T14:37:36Z

- **pkg/controller/constants/constants.go:43** by [@mimowo](https://github.com/mimowo) — 2024-10-14T14:37:35Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
```

  Yes, this is supported for any Job (can be MPIJob, JobSet, etc.). In Kueue we say Job in many contexts meaning any of the CRDs, see the user-facing docs: https://kueue.sigs.k8s.io/docs/overview/.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T14:45:03Z

- **pkg/controller/jobs/pod/pod_controller_test.go:777** by [@mimowo](https://github.com/mimowo) — 2024-10-14T14:45:03Z
```diff
@@ -772,6 +774,8 @@ func TestReconciler(t *testing.T) {
 				*basePodWrapper.
 					Clone().
 					Label(constants.ManagedByKueueLabel, "true").
+					Label(controllerconsts.PodSetLabel, "dc85db45").
```

  I'm not sure this is the best course of action, because:
  1. this string is used 105 times in the file already: https://github.com/kubernetes-sigs/kueue/blob/b6222fa0ec020bad0d93be9acdd0305348c4935a/pkg/controller/jobs/pod/pod_controller_test.go#L601, and so it would mix a feature with extended cleanup
  2. if I introduce a const like `podSetNameForTest` at the top of the file it could suggest it should be used in all tests, but it is derived from the Pods actually, it was just copy-pasted many times. 
  
  So, I don't see this would be very clean, but let me know your thoughts.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-10-14T14:49:13Z

- **pkg/controller/jobs/pod/pod_controller_test.go:777** by [@kannon92](https://github.com/kannon92) — 2024-10-14T14:49:13Z
```diff
@@ -772,6 +774,8 @@ func TestReconciler(t *testing.T) {
 				*basePodWrapper.
 					Clone().
 					Label(constants.ManagedByKueueLabel, "true").
+					Label(controllerconsts.PodSetLabel, "dc85db45").
```

  Would you mind if I do a cleanup?
  
  I agree with you that this should not be in scope for this feature then. I didn't see how many occurences this has.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T14:57:54Z

- **pkg/controller/jobs/pod/pod_controller_test.go:777** by [@mimowo](https://github.com/mimowo) — 2024-10-14T14:57:54Z
```diff
@@ -772,6 +774,8 @@ func TestReconciler(t *testing.T) {
 				*basePodWrapper.
 					Clone().
 					Label(constants.ManagedByKueueLabel, "true").
+					Label(controllerconsts.PodSetLabel, "dc85db45").
```

  Sure, feel free to follow up! maybe instead of hard-coding a const we could compute the hash in tests, but not sure at the moment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T15:02:15Z

- **pkg/controller/constants/constants.go:46** by [@mimowo](https://github.com/mimowo) — 2024-10-14T15:02:15Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
+	// indicate the name of the admitted Workload corresponding to the Job. The
+	// annotation is set when starting the Job, and removed on stopping the Job.
+	WorkloadAnnotation = "kueue.x-k8s.io/workload"
```

  > What is the difference between this annotation and the Workload name hash?
  
  This new annotation contains the full workload name, for example `job-sample-job-12345`, where 12345 is the hash. The annotation is set on PodTemplate so that newly created Pods carry the information to lookup the Workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T15:04:36Z

- **pkg/controller/constants/constants.go:46** by [@mimowo](https://github.com/mimowo) — 2024-10-14T15:04:36Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
+	// indicate the name of the admitted Workload corresponding to the Job. The
+	// annotation is set when starting the Job, and removed on stopping the Job.
+	WorkloadAnnotation = "kueue.x-k8s.io/workload"
```

  > Additionally, is there any case where this annotation and pre-build workload annotation?
  
  First, note that the two annotations are set at different levels. Prebuilt-workload is the Job-level annotations, whilst the new annotation is set at the PodTemplate level, so that the new Pods "know" their workload.
  
  This might happen on MultiKueue worker cluster. The MK would set the prebuit-workload annotation, and the admission process on the worker will set the new "workload" annotation. However, the new annotation is set on the PodTemplate rather than at the Job level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T15:08:52Z

- **pkg/controller/jobframework/reconciler.go:977** by [@mimowo](https://github.com/mimowo) — 2024-10-14T15:08:52Z
```diff
@@ -973,6 +973,8 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		if err != nil {
 			return nil, err
 		}
+		info.Labels[controllerconsts.PodSetLabel] = podSetFlavor.Name
+		info.Annotations[controllerconsts.WorkloadAnnotation] = w.Name
```

  Sure, let's summarize the options:
  1. no feature gate (as currently)
  2. use the TopologyAwareScheduling feature-gate
  3. use another dedicated feature gate like PodTemplatePodSetLabelAndWorkloadAnnotation
  
  TBH, I would be leaning towards 1. as the code is simple and low risk. 
  
  My second choice would be (2.) as it is introduced for the needs for TopologyAwareScheduling. if we do (2.) we can always enable it by or with other feature gates. For example `TopologyAwareScheduling || AnotherFeatureNeedingTheLabels`. WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-14T16:38:31Z

- **pkg/controller/constants/constants.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T16:38:31Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
+	// indicate the name of the admitted Workload corresponding to the Job. The
+	// annotation is set when starting the Job, and removed on stopping the Job.
+	WorkloadAnnotation = "kueue.x-k8s.io/workload"
```

  > This new annotation contains the full workload name, for example job-sample-job-12345, where 12345 is the hash. The annotation is set on PodTemplate so that newly created Pods carry the information to lookup the Workload.
  
  In that case, we can remove the hash mechanism after we graduate this feature to GA?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-14T16:45:18Z

- **pkg/controller/constants/constants.go:46** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T16:45:17Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
+	// indicate the name of the admitted Workload corresponding to the Job. The
+	// annotation is set when starting the Job, and removed on stopping the Job.
+	WorkloadAnnotation = "kueue.x-k8s.io/workload"
```

  > This might happen on MultiKueue worker cluster. The MK would set the prebuit-workload annotation, and the admission process on the worker will set the new "workload" annotation. However, the new annotation is set on the PodTemplate rather than at the Job level.
  
  I see, so the Pod has only new "workload" annotation. In that case, there seems no to be conflict.
  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-14T16:47:12Z

- **pkg/controller/jobframework/reconciler.go:977** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T16:47:12Z
```diff
@@ -973,6 +973,8 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		if err != nil {
 			return nil, err
 		}
+		info.Labels[controllerconsts.PodSetLabel] = podSetFlavor.Name
+		info.Annotations[controllerconsts.WorkloadAnnotation] = w.Name
```

  I discussed this with @mimowo offline.
  We decided to add the TAS gate here so that we can break this labels and annotations specification during the beta / GA graduation. For example, we may want to move the "Workload" annotation to labels.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T07:33:39Z

- **pkg/controller/jobframework/reconciler.go:977** by [@mimowo](https://github.com/mimowo) — 2024-10-15T07:33:39Z
```diff
@@ -973,6 +973,8 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		if err != nil {
 			return nil, err
 		}
+		info.Labels[controllerconsts.PodSetLabel] = podSetFlavor.Name
+		info.Annotations[controllerconsts.WorkloadAnnotation] = w.Name
```

  That's right. After a second thought I think it is safer. For the purpose of fast lookup of the workload from Pod it would be better to have a Workload label rather than annotation. For now it is an annotation as the workload names can be longer that 63 characters, so it requires some preparatory work.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T07:35:53Z

- **pkg/controller/constants/constants.go:46** by [@mimowo](https://github.com/mimowo) — 2024-10-15T07:35:53Z
```diff
@@ -39,4 +39,14 @@ const (
 
 	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
 	ProvReqAnnotationPrefix = "provreq.kueue.x-k8s.io/"
+
+	// WorkloadAnnotation is an annotation set on the Job's PodTemplate to
+	// indicate the name of the admitted Workload corresponding to the Job. The
+	// annotation is set when starting the Job, and removed on stopping the Job.
+	WorkloadAnnotation = "kueue.x-k8s.io/workload"
```

  > In that case, we can remove the hash mechanism after we graduate this feature to GA?
  
  I think the hash mechanism is here to stay, and was introduced prior to TAS. The hash in the workload name is to ensure Workload names don't exceed the max length (currently defined as 253 chars). This is needed because workload names add the prefix to Job names. For example for an MPIJob "test-job", we have "mpijob-test-job-<hash>".

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-15T08:48:08Z

- **pkg/features/kube_features.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-10-15T08:48:08Z
  Could you please reflect that change in [the website documentation](https://kueue.sigs.k8s.io/docs/installation/#change-the-feature-gates-configuration)?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T08:55:19Z

- **pkg/features/kube_features.go:1** by [@mimowo](https://github.com/mimowo) — 2024-10-15T08:55:19Z
  thx, pushed the update

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-10-15T20:34:25Z

- **pkg/controller/jobframework/reconciler.go:977** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-10-15T20:34:25Z
```diff
@@ -973,6 +973,8 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		if err != nil {
 			return nil, err
 		}
+		info.Labels[controllerconsts.PodSetLabel] = podSetFlavor.Name
+		info.Annotations[controllerconsts.WorkloadAnnotation] = w.Name
```

  Makes sense to gate it for 0.9, but this has the potential to be useful beyond TAS.  An efficient mapping from Pods back to their Workloads is a very useful building block for general fault tolerance.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-15T21:00:36Z

- **pkg/controller/jobframework/reconciler.go:977** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T21:00:36Z
```diff
@@ -973,6 +973,8 @@ func getPodSetsInfoFromStatus(ctx context.Context, c client.Client, w *kueue.Wor
 		if err != nil {
 			return nil, err
 		}
+		info.Labels[controllerconsts.PodSetLabel] = podSetFlavor.Name
+		info.Annotations[controllerconsts.WorkloadAnnotation] = w.Name
```

  I agree with you.
  So, the during beta graduation, we will consider if we move all to labels or change key name. After beta graduation, these labels are added by default.
