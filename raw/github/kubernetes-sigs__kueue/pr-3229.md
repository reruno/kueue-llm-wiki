# PR #3229: [KEP] Extend `WaitForPodsReady` config by `.waitForAdmissionChecks` field

**Summary**: [KEP] Extend `WaitForPodsReady` config by `.waitForAdmissionChecks` field

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3229

**Last updated**: 2024-11-05T13:14:38Z

---

## Metadata

- **State**: closed
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `KEP-AC-timeout` → `main`
- **Created**: 2024-10-14T13:12:14Z
- **Updated**: 2024-11-05T13:14:38Z
- **Merged**: —
- **Closed**: 2024-11-05T13:14:37Z
- **Labels**: `kind/feature`, `release-note-none`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+56 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Describes API extension for `WaitForPodsReady` which expands it's functionality to AdmissionChecks, to prevent wasteful allocation of resources and starvation.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #3231 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-14T13:12:27Z

/assign @mimowo 
cc @mwielgus

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-14T13:12:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c27dc8a510d7154f5ffbe2e98828ddf9963852d0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/670d352a7b66de00082528ed |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-14T13:23:42Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3229#" title="Author self-approved">PBundyra</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from [mimowo](https://github.com/mimowo). For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T14:41:39Z

/cc

@mimowo @PBundyra Will this be marked as a must-have for the next release?
I need to prioritize features for the next releases on my side.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-14T15:57:08Z

> /cc
> 
> @mimowo @PBundyra Will this be marked as a must-have for the next release? I need to prioritize features for the next releases on my side.

It shouldn't block the release, but it's a strong nice-to-have

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-14T16:48:13Z

> It shouldn't block the release, but it's a strong nice-to-have

I see. It seems that there are a few strong nice-to-have...

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T13:19:10Z

- **keps/349-all-or-nothing/README.md:254** by [@mimowo](https://github.com/mimowo) — 2024-10-14T13:18:06Z
```diff
@@ -225,6 +246,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks *WaitForAdmissionChecks
```

  I think this needs to be a list so that we can pose the timeout for multiple admission checks.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-14T13:23:47Z

- **keps/349-all-or-nothing/README.md:254** by [@PBundyra](https://github.com/PBundyra) — 2024-10-14T13:23:47Z
```diff
@@ -225,6 +246,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks *WaitForAdmissionChecks
```

  Indeed, that's what I meant. Thanks for point this out, done!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-14T14:32:25Z

- **keps/349-all-or-nothing/README.md:83** by [@mimowo](https://github.com/mimowo) — 2024-10-14T14:23:15Z
```diff
@@ -76,6 +78,10 @@ large jobs may deadlock if there are issues with resource provisioning to
 match the configured cluster quota. The same pair of jobs could run to
 completion if their pods were scheduled sequentially.
 
+Another reason a pods could not schedule, even if Kueue had a free quota would be resource
+fragmentation. We would like to prevent allocating quota for an extended period of time
+if it cannot be consumed.
```

  Maybe we can rephrase it a bit to emphasize the admission checks. The connection between resource fragmentation and admission checks is not clear. I think we can mention: 
  
  ```
  Another reason pods could not schedule is that there are some AdmissionChecks configured which are
  not satisfied for a long time for a Workload which already reserves the quota. 
  
  For example, some users may use AdmissionCheck ProvisioningRequest with check-capacity to gate
  admission of Workload. If the quota is fragmented the Workload may stay waiting for admission for
  an extended period of time, and we would like to help users to avoid that situation.
  ```

- **keps/349-all-or-nothing/README.md:128** by [@mimowo](https://github.com/mimowo) — 2024-10-14T14:25:07Z
```diff
@@ -114,7 +121,13 @@ and make progress.
 We introduce a mechanism to ensure jobs get their physical resources
 assigned by avoiding concurrent scheduling of their pods. More precisely, we
 block admission of new workloads until the first batch of pods for the
-unsuspended job is scheduled. This behavior can be opted-in at the level of
+unsuspended job is scheduled. We also specify the timeout for the Workload to schedule,
+which prevents starvation of new workloads.
+
+Additionally, we introduce a second mechanism to timeout Kueue's AdmissionChecks.
+AdmissionChecks (especially ProvisioningRequest), face the same risk of fragmentation as described above.
```

  ```suggestion
  AdmissionChecks (especially ProvisioningRequest), face the same risk of getting stuck for 
  an extended period of time, for example, in case of quota fragmentation as described above.
  ```

- **keps/349-all-or-nothing/README.md:166** by [@mimowo](https://github.com/mimowo) — 2024-10-14T14:26:04Z
```diff
@@ -146,6 +159,12 @@ the configured cluster queue quota and when the Jobs don't specify priorities
 My use case can be supported by enabling `waitForPodsReady` in the Kueue
 configuration.
 
+#### Story 2
+
+As a Kueue administrator I want to ensure that a Job doesn't allocate resources
+infinitely if there is capacity in Kueue, but there's no capacity on the cloud
+provider side.
```

  ```suggestion
  infinitely for Workloads awaiting for AdmissionChecks.
  ```

- **keps/349-all-or-nothing/README.md:262** by [@mimowo](https://github.com/mimowo) — 2024-10-14T14:32:11Z
```diff
@@ -225,6 +246,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks []WaitForAdmissionChecks
+}
+
+type WaitForAdmissionChecks struct {
+	// Name of the AdmissionCheck the configuration refers to
+	AdmissionCheck string
+
+	// Timeout defines the time for workload with reserved quota to reach the
+	// Admitted=true condition. When the timeout is exceeded, the workload
```

  Is this timeout really for the `Admitted=true`, or individually per AdmissionCheck? 
  
  If we consider the timeouts individually we should rather check on the LastTransitionTime of the AdmissionChecksState (Pending -> Ready).
  
  If we put timeout on all Admission Checks, then we could look for LastTransitionTime of QuotaReserved and Admitted.
  
  I think both approaches are valid, but we need to decide on one, and make it explicit.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-14T15:08:34Z

- **keps/349-all-or-nothing/README.md:262** by [@PBundyra](https://github.com/PBundyra) — 2024-10-14T15:08:34Z
```diff
@@ -225,6 +246,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks []WaitForAdmissionChecks
+}
+
+type WaitForAdmissionChecks struct {
+	// Name of the AdmissionCheck the configuration refers to
+	AdmissionCheck string
+
+	// Timeout defines the time for workload with reserved quota to reach the
+	// Admitted=true condition. When the timeout is exceeded, the workload
```

  Good point, my idea was to have a configuration per AdmissionCheck, hence the timeout should be based on the AdmissionCheckStates.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-14T15:18:07Z

- **keps/349-all-or-nothing/README.md:262** by [@PBundyra](https://github.com/PBundyra) — 2024-10-14T15:18:06Z
```diff
@@ -225,6 +246,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks []WaitForAdmissionChecks
+}
+
+type WaitForAdmissionChecks struct {
+	// Name of the AdmissionCheck the configuration refers to
+	AdmissionCheck string
+
+	// Timeout defines the time for workload with reserved quota to reach the
+	// Admitted=true condition. When the timeout is exceeded, the workload
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-14T15:18:18Z

- **keps/349-all-or-nothing/README.md:83** by [@PBundyra](https://github.com/PBundyra) — 2024-10-14T15:18:18Z
```diff
@@ -76,6 +78,10 @@ large jobs may deadlock if there are issues with resource provisioning to
 match the configured cluster quota. The same pair of jobs could run to
 completion if their pods were scheduled sequentially.
 
+Another reason a pods could not schedule, even if Kueue had a free quota would be resource
+fragmentation. We would like to prevent allocating quota for an extended period of time
+if it cannot be consumed.
```

  Done

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-10-14T18:48:29Z

- **keps/349-all-or-nothing/README.md:258** by [@mwielgus](https://github.com/mwielgus) — 2024-10-14T18:48:29Z
```diff
@@ -225,6 +250,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks []WaitForAdmissionChecks
```

  It looks a bit weird here, especially given that there is RequeuingStrategy is present above and in WaitForAdmissionChecks.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-10-14T18:54:48Z

- **keps/349-all-or-nothing/README.md:263** by [@mwielgus](https://github.com/mwielgus) — 2024-10-14T18:54:48Z
```diff
@@ -225,6 +250,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks []WaitForAdmissionChecks
+}
+
+type WaitForAdmissionChecks struct {
+	// Name of the AdmissionCheck the configuration refers to
+	AdmissionCheck string
```

  I know that we discussed it offline and I had suggestion to add it here, but when I look at it now it feels wrong. We should not reference AdmissionChecks that are dynamic and present in ApiServer from a static config file.  
  What about doing it some other way and adding the timeout to AdmissionCheckSpec or specifically in ProvisioningRequestConfigSpec. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T06:36:19Z

- **keps/349-all-or-nothing/README.md:263** by [@mimowo](https://github.com/mimowo) — 2024-10-15T06:36:19Z
```diff
@@ -225,6 +250,28 @@ type WaitForPodsReady struct {
 	// RequeuingStrategy defines the strategy for requeuing a Workload.
 	// +optional
 	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+
+	// WaitForAdmissionChecks defines configuration for the WaitForAdmissionChecks feature,
+	// which is used to ensure that specific AdmissionCheck is ready within the specified time.
+	// If there is no specified configuration for an AdmissionCheck, there is no timeout for it.
+	// +listType=map
+	WaitForAdmissionChecks []WaitForAdmissionChecks
+}
+
+type WaitForAdmissionChecks struct {
+	// Name of the AdmissionCheck the configuration refers to
+	AdmissionCheck string
```

  +1
  
  However, I think if we do it in `AdmissionCheckSpec` or `ProvisioningRequestConfigSpec` then it rather belongs to a dedicated KEP. 
  
  Given the enhancement is time sensitive maybe a global timeout for admission checks in the waitForPodsReady is good enough for 0.9, and we could make a new class-specific KEP for 0.10, wdyt @mwielgus? The new user story added doesn't indicate that specific timeouts are needed.
