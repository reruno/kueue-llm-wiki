# PR #3235: TAS: Introduce API

**Summary**: TAS: Introduce API

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3235

**Last updated**: 2025-01-21T11:25:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-alpha-api` → `main`
- **Created**: 2024-10-15T11:19:00Z
- **Updated**: 2025-01-21T11:25:38Z
- **Merged**: 2024-10-16T17:33:03Z
- **Closed**: 2024-10-16T17:33:03Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 35   **+2009 / -26**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #2724 

#### Special notes for your reviewer:

Some of the field API comments are improved compared to KEP. I will update the KEP accordingly to make them in-sync.

#### Does this PR introduce a user-facing change?

```release-note
Support Topology Aware Scheduling (TAS) in Kueue in the Alpha version, along with the new Topology API
to specify the ordered list of node labels corresponding to the different levels of hierarchy in data-centers
(like racks or blocks).

Additionally, we introduce the pair of Job-level annotations: `http://kueue.x-k8s.io/podset-required-topology`
and `kueue.x-k8s.io/podset-preferred-topology` which users can use to indicate their preference for the
Jobs to run all their Pods within a topology domain at the indicated level.
```

## Discussion

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-15T11:19:20Z

/cc @PBundyra @tenzen-y

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-15T11:19:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5d3b49711162ceba6b31c7c2d397cfbc934f1385 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/670ff42369bedc0008996654 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-15T11:40:25Z

> It seems that the Workload API is quite different from the KEP, right?
> https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-schedling#internal-apis

Yes, it is changed a bit, I will send the Workload API level in a separate PR, and will send a PR to update the KEP.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:43:37Z

> > It seems that the Workload API is quite different from the KEP, right?
> > https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-schedling#internal-apis
> 
> Yes, it is changed a bit, I will send the Workload API level in a separate PR, and will send a PR to update the KEP.

I would like to merge this PR after we check how this Workload API changes give to the TAS scheduler in the updated KEP.
/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-15T14:19:56Z

> I would like to merge this PR after we check how this Workload API changes give to the TAS scheduler in the updated KEP.

Makes sense, I prepared the update: https://github.com/kubernetes-sigs/kueue/pull/3237

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-16T13:32:31Z

FYI, the content here is in-sync with the KEP update PR (after the sync today).

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-16T14:12:22Z

nit: Could we start comments with lowercase letters?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-16T15:33:34Z

> nit: Could we start comments with lowercase letters?

I use lowercase for fields which follows https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#spec-and-status. It makes sense so tat in the generated user-facing docs it looks consistent with the json fields.

As for labels and annotations I don't think there is such a convention. We also don't generate docs based on the declarations. Finally all consts [here](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/constants/constants.go#L20-L41) are commented with upper-case.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-16T17:20:25Z

LGTM label has been added.  <details>Git tree hash: cd5fe89295a9751d5580fc9733fea98140fa9755</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-16T17:20:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3235#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3235#pullrequestreview-2373185390" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-16T17:32:47Z

> I would like to merge this PR after we check how this Workload API changes give to the TAS scheduler in the updated KEP.
> /hold

As the API update in KEP is merged
/hold cancel

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-28T08:52:30Z

/release-note-edit
```release-note
Support Topology Aware Scheduling (TAS) in Kueue in the Alpha version, along with the new Topology API
to specify the ordered list of node labels corresponding to the different levels of hierarchy in data-centers
(like racks or blocks).

Additionally, we introduce the pair of Job-level annotations: `http://kueue.x-k8s.io/podset-required-topology`
and `kueue.x-k8s.io/podset-preferred-topology` which users can use to indicate their preference for the
Jobs to run all their Pods within a topology domain at the indicated level.
```

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-15T11:34:51Z

It seems that the Workload API is quite different from the KEP, right?
https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-schedling#internal-apis

- **apis/kueue/v1alpha1/tas_types.go:91** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:22:47Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
+	// level.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinLength=1
+	// +kubebuilder:validation:MaxLength=316
+	// +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+	NodeLabel string `json:"nodeLabel,omitempty"`
+}
+
+// TopologyStatus defines the observed state of Topology
+type TopologyStatus struct {
+}
+
+// +kubebuilder:object:root=true
```

  ```suggestion
  // +genclient
  // +genclient:nonNamespaced
  // +kubebuilder:object:root=true
  ```
  
  Let's generate client as well.

- **apis/kueue/v1alpha1/tas_types.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:24:58Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
```

  ```suggestion
  	// levels define the levels of topology.
  ```
  
  Let's use the YAML field name same as other APIs since this is API comment and these are referenced by users.

- **apis/kueue/v1alpha1/tas_types.go:69** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:25:14Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
```

  ```suggestion
  	// nodeLabel indicates the name of the node label for a specific topology
  ```

- **apis/kueue/v1alpha1/tas_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:25:57Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
```

  ```suggestion
  	// +kubebuilder:validation:MaxItems=5
  	// +optional
  ```
  
  Is this optional field?

- **apis/kueue/v1alpha1/tas_types.go:82** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:27:13Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
+	// level.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinLength=1
+	// +kubebuilder:validation:MaxLength=316
+	// +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+	NodeLabel string `json:"nodeLabel,omitempty"`
```

  ```suggestion
  	NodeLabel string `json:"nodeLabel"`
  ```
  
  I guess that the `omitempty` marker makes this field an optional field, right?

- **apis/kueue/v1beta1/resourceflavor_types.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:27:25Z
```diff
@@ -85,6 +85,13 @@ type ResourceFlavorSpec struct {
 	// +kubebuilder:validation:XValidation:rule="self.all(x, has(x.operator) && x.operator == 'Exists' ? !has(x.value) : true)", message="a value must be empty when 'operator' is 'Exists'"
 	// +kubebuilder:validation:XValidation:rule="self.all(x, !has(x.effect) || x.effect in ['NoSchedule', 'PreferNoSchedule', 'NoExecute'])", message="supported taint effect values: 'NoSchedule', 'PreferNoSchedule', 'NoExecute'"
 	Tolerations []corev1.Toleration `json:"tolerations,omitempty"`
+
+	// TopologyName indicates topology for the TAS ResourceFlavor.
```

  ```suggestion
  	// topologyName indicates topology for the TAS ResourceFlavor.
  ```

- **apis/kueue/v1beta1/workload_types.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:28:12Z
```diff
@@ -77,6 +77,18 @@ type WorkloadSpec struct {
 	Active *bool `json:"active,omitempty"`
 }
 
+type PodSetTopologyRequest struct {
+	// Required is the level label indicated by the `kueue.x-k8s.io/podset-required-topology` annotation
```

  ```suggestion
  	// required is the level label indicated by the `kueue.x-k8s.io/podset-required-topology` annotation
  	// in jobs.
  ```

- **apis/kueue/v1beta1/workload_types.go:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:28:24Z
```diff
@@ -77,6 +77,18 @@ type WorkloadSpec struct {
 	Active *bool `json:"active,omitempty"`
 }
 
+type PodSetTopologyRequest struct {
+	// Required is the level label indicated by the `kueue.x-k8s.io/podset-required-topology` annotation
+	//
+	// +optional
+	Required *string `json:"required,omitempty"`
+
+	// Preferred is the level label indicated by the `kueue.x-k8s.io/podset-preferred-topology` annotation
```

  ```suggestion
  	// preferred is the level label indicated by the `kueue.x-k8s.io/podset-preferred-topology` annotation
  	// in jobs.
  ```

- **apis/kueue/v1beta1/workload_types.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:30:47Z
```diff
@@ -77,6 +77,18 @@ type WorkloadSpec struct {
 	Active *bool `json:"active,omitempty"`
 }
 
+type PodSetTopologyRequest struct {
+	// Required is the level label indicated by the `kueue.x-k8s.io/podset-required-topology` annotation
```

  Additionally, this annotation is added to Jobs, right?

- **apis/kueue/v1beta1/workload_types.go:129** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:33:24Z
```diff
@@ -113,6 +125,27 @@ type PodSetAssignment struct {
 	// +optional
 	// +kubebuilder:validation:Minimum=0
 	Count *int32 `json:"count,omitempty"`
+
+	// TopologyAssignment indicates the resources assigned per topology level
```

  ```suggestion
  	// topologyAssignment indicates the resources assigned per topology level
  ```

- **apis/kueue/v1beta1/workload_types.go:135** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-15T11:33:36Z
```diff
@@ -113,6 +125,27 @@ type PodSetAssignment struct {
 	// +optional
 	// +kubebuilder:validation:Minimum=0
 	Count *int32 `json:"count,omitempty"`
+
+	// TopologyAssignment indicates the resources assigned per topology level
+	// +optional
+	TopologyAssignment *TopologyAssignment `json:"topologyAssignment,omitempty"`
+}
+
+type TopologyAssignment struct {
+	// Domains contains the list of assignments split into lowest-level groups
```

  ```suggestion
  	// domains contains the list of assignments split into lowest-level groups
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T11:47:29Z

- **apis/kueue/v1alpha1/tas_types.go:63** by [@mimowo](https://github.com/mimowo) — 2024-10-15T11:47:29Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
```

  I don't think so, it only makes sense if you have at least one topology level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T11:48:44Z

- **apis/kueue/v1alpha1/tas_types.go:82** by [@mimowo](https://github.com/mimowo) — 2024-10-15T11:48:44Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
+	// level.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinLength=1
+	// +kubebuilder:validation:MaxLength=316
+	// +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+	NodeLabel string `json:"nodeLabel,omitempty"`
```

  yeah, it omits the field on serialization, but the field is non-empty so `omitempty` it can be skipped.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T12:06:50Z

- **apis/kueue/v1alpha1/tas_types.go:91** by [@mimowo](https://github.com/mimowo) — 2024-10-15T12:06:50Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
+	// level.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinLength=1
+	// +kubebuilder:validation:MaxLength=316
+	// +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+	NodeLabel string `json:"nodeLabel,omitempty"`
+}
+
+// TopologyStatus defines the observed state of Topology
+type TopologyStatus struct {
+}
+
+// +kubebuilder:object:root=true
```

  Added

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T12:06:58Z

- **apis/kueue/v1alpha1/tas_types.go:59** by [@mimowo](https://github.com/mimowo) — 2024-10-15T12:06:58Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T12:07:03Z

- **apis/kueue/v1alpha1/tas_types.go:69** by [@mimowo](https://github.com/mimowo) — 2024-10-15T12:07:03Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T12:07:17Z

- **apis/kueue/v1beta1/resourceflavor_types.go:89** by [@mimowo](https://github.com/mimowo) — 2024-10-15T12:07:16Z
```diff
@@ -85,6 +85,13 @@ type ResourceFlavorSpec struct {
 	// +kubebuilder:validation:XValidation:rule="self.all(x, has(x.operator) && x.operator == 'Exists' ? !has(x.value) : true)", message="a value must be empty when 'operator' is 'Exists'"
 	// +kubebuilder:validation:XValidation:rule="self.all(x, !has(x.effect) || x.effect in ['NoSchedule', 'PreferNoSchedule', 'NoExecute'])", message="supported taint effect values: 'NoSchedule', 'PreferNoSchedule', 'NoExecute'"
 	Tolerations []corev1.Toleration `json:"tolerations,omitempty"`
+
+	// TopologyName indicates topology for the TAS ResourceFlavor.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T12:07:53Z

- **apis/kueue/v1beta1/workload_types.go:129** by [@mimowo](https://github.com/mimowo) — 2024-10-15T12:07:53Z
```diff
@@ -113,6 +125,27 @@ type PodSetAssignment struct {
 	// +optional
 	// +kubebuilder:validation:Minimum=0
 	Count *int32 `json:"count,omitempty"`
+
+	// TopologyAssignment indicates the resources assigned per topology level
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-15T12:07:59Z

- **apis/kueue/v1beta1/workload_types.go:135** by [@mimowo](https://github.com/mimowo) — 2024-10-15T12:07:59Z
```diff
@@ -113,6 +125,27 @@ type PodSetAssignment struct {
 	// +optional
 	// +kubebuilder:validation:Minimum=0
 	Count *int32 `json:"count,omitempty"`
+
+	// TopologyAssignment indicates the resources assigned per topology level
+	// +optional
+	TopologyAssignment *TopologyAssignment `json:"topologyAssignment,omitempty"`
+}
+
+type TopologyAssignment struct {
+	// Domains contains the list of assignments split into lowest-level groups
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-16T13:31:39Z

- **apis/kueue/v1beta1/workload_types.go:86** by [@mimowo](https://github.com/mimowo) — 2024-10-16T13:31:39Z
```diff
@@ -77,6 +77,18 @@ type WorkloadSpec struct {
 	Active *bool `json:"active,omitempty"`
 }
 
+type PodSetTopologyRequest struct {
+	// Required is the level label indicated by the `kueue.x-k8s.io/podset-required-topology` annotation
+	//
+	// +optional
+	Required *string `json:"required,omitempty"`
+
+	// Preferred is the level label indicated by the `kueue.x-k8s.io/podset-preferred-topology` annotation
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-16T13:32:02Z

- **apis/kueue/v1beta1/workload_types.go:81** by [@mimowo](https://github.com/mimowo) — 2024-10-16T13:32:02Z
```diff
@@ -77,6 +77,18 @@ type WorkloadSpec struct {
 	Active *bool `json:"active,omitempty"`
 }
 
+type PodSetTopologyRequest struct {
+	// Required is the level label indicated by the `kueue.x-k8s.io/podset-required-topology` annotation
```

  The annotation is added to PodSets, adjuted the comment to make it clear.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-16T13:41:47Z

- **apis/kueue/v1alpha1/tas_types.go:28** by [@PBundyra](https://github.com/PBundyra) — 2024-10-16T13:41:47Z
```diff
@@ -16,7 +16,33 @@ limitations under the License.
 
 package v1alpha1
 
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
 const (
+	// PodSetTopologyRequiredAnnotation indicates that a PodSet requires
+	// Topology Aware Scheduling, and requires scheduling all pods on nodes
+	// within the same topology domain corresponding to the topology level
+	// indicated by the annotation value (e.g. within a rack or within a block).
+	PodSetTopologyRequiredAnnotation = "kueue.x-k8s.io/podset-required-topology"
```

  nit: Can we keep the naming consistent between `PodSetTopologyRequiredAnnotation` and `PodSetPreferredTopologyAnnotation`? Should the Topology be before or after Required/Preferred? I suggest renaming it to `PodSetRequiredTopologyAnnotation`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-16T15:28:36Z

- **apis/kueue/v1alpha1/tas_types.go:28** by [@mimowo](https://github.com/mimowo) — 2024-10-16T15:28:36Z
```diff
@@ -16,7 +16,33 @@ limitations under the License.
 
 package v1alpha1
 
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
 const (
+	// PodSetTopologyRequiredAnnotation indicates that a PodSet requires
+	// Topology Aware Scheduling, and requires scheduling all pods on nodes
+	// within the same topology domain corresponding to the topology level
+	// indicated by the annotation value (e.g. within a rack or within a block).
+	PodSetTopologyRequiredAnnotation = "kueue.x-k8s.io/podset-required-topology"
```

  Sure, done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-16T17:14:48Z

- **apis/kueue/v1alpha1/tas_types.go:82** by [@mimowo](https://github.com/mimowo) — 2024-10-16T17:14:47Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
+	// level.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinLength=1
+	// +kubebuilder:validation:MaxLength=316
+	// +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+	NodeLabel string `json:"nodeLabel,omitempty"`
```

  Ah, got it. Since the field is required the omitempty does not matter. I removed it for clarity.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-16T17:19:20Z

- **apis/kueue/v1alpha1/tas_types.go:82** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-16T17:19:20Z
```diff
@@ -27,3 +53,57 @@ const (
 	// The label is set when starting the Job, and removed on stopping the Job.
 	PodSetLabel = "kueue.x-k8s.io/podset"
 )
+
+// TopologySpec defines the desired state of Topology
+type TopologySpec struct {
+	// TopologyLevels define the levels of topology.
+	//
+	// +listType=atomic
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=5
+	Levels []TopologyLevel `json:"levels,omitempty"`
+}
+
+// TopologyLevel defines the desired state of TopologyLevel
+type TopologyLevel struct {
+	// NodeLabel indicates the name of the node label for a specific topology
+	// level.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinLength=1
+	// +kubebuilder:validation:MaxLength=316
+	// +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+	NodeLabel string `json:"nodeLabel,omitempty"`
```

  My concerns was related to https://github.com/kubernetes-sigs/controller-tools/issues/943.
  So, before controller-tools v0.16.0, the `+required` marked is not respected when the field has `omitempty`.
  But, the latest controller-tools allow us to make the omitempty field as required.
  
  However, we decided to remove `omitempty` since we should not add `omitempty` to the required field as the API definition principal.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-16T17:20:19Z

Thank you!

/lgtm
/approve
