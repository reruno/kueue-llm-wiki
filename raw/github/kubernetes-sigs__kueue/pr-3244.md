# PR #3244: [test][MK][e2e] Change the way JobManagedBy feature gate is enabled.

**Summary**: [test][MK][e2e] Change the way JobManagedBy feature gate is enabled.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3244

**Last updated**: 2024-12-05T13:56:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `mk-e2e-change-feature-enable-logic` → `main`
- **Created**: 2024-10-16T13:56:02Z
- **Updated**: 2024-12-05T13:56:47Z
- **Merged**: 2024-10-17T15:03:13Z
- **Closed**: 2024-10-17T15:03:14Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 2   **+9 / -25**

## Description



<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Change the way `JobManagedBy` feature gate is enabled in the manager's kind config to cover all the versions having their minor >= 30.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-16T13:56:05Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@trasc](https://github.com/trasc) — 2024-10-16T13:56:18Z

/test all

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-16T13:56:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0752f030227e6e34ab1473a529b0e1a0839633c4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6710b328b61cc80008f553c4 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-16T17:22:57Z

/assign @mbobrovskyi

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-17T06:50:13Z

/lgtm

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-17T06:50:19Z

LGTM label has been added.  <details>Git tree hash: 6ca07f5f545b2c6cbb46d0c7a6c14e9644e014ac</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-17T14:43:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3244#pullrequestreview-2375472595" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3244#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-05T13:56:08Z

/cherry-pick release-0.8

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-12-05T13:56:45Z

@mbobrovskyi: #3244 failed to apply on top of branch "release-0.8":
```
Applying: Change the way JobManagedBy feature gate is enabled.
.git/rebase-apply/patch:20: trailing whitespace.
        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
.git/rebase-apply/patch:22: trailing whitespace.
        minor=$(( varr[1] ))        
warning: 2 lines add whitespace errors.
Using index info to reconstruct a base tree...
M	hack/multikueue-e2e-test.sh
Falling back to patching base and 3-way merge...
Removing hack/multikueue/manager-cluster.kind-1.30.0.yaml
Auto-merging hack/multikueue-e2e-test.sh
CONFLICT (content): Merge conflict in hack/multikueue-e2e-test.sh
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Change the way JobManagedBy feature gate is enabled.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3244#issuecomment-2520396653):

>/cherry-pick release-0.8


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-17T05:14:26Z

- **hack/multikueue-e2e-test.sh:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-17T05:04:03Z
```diff
@@ -52,13 +52,19 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        ifs_bk="$IFS"
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        IFS="$ifs_bk"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  ```suggestion
          if [ "$(echo "$KIND_VERSION" | awk -F '.' '{print $2}')" -ge 30 ]; then
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-17T05:43:56Z

- **hack/multikueue-e2e-test.sh:60** by [@trasc](https://github.com/trasc) — 2024-10-17T05:43:56Z
```diff
@@ -52,13 +52,19 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        ifs_bk="$IFS"
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        IFS="$ifs_bk"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  Sure there are alternatives including `sed -E 's/([0-9]+).([0-9]+).([0-9]+)/\2/'`, But I prefer to use only bash if possible.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-17T06:11:33Z

- **hack/multikueue-e2e-test.sh:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-17T06:06:37Z
```diff
@@ -52,13 +52,19 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        ifs_bk="$IFS"
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        IFS="$ifs_bk"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  Make sense.

- **hack/multikueue-e2e-test.sh:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-17T06:09:40Z
```diff
@@ -52,13 +52,19 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        ifs_bk="$IFS"
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        IFS="$ifs_bk"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  On that case I would propose:
  
  ```suggestion
          IFS=. read -r -a varr <<< "$KIND_VERSION"
          minor=$(( varr[1] ))        
          if [ "$minor" -ge 30 ]; then
  ```
  
  We directly use `IFS` for the `read` command.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-17T06:58:13Z

- **hack/multikueue-e2e-test.sh:60** by [@trasc](https://github.com/trasc) — 2024-10-17T06:58:13Z
```diff
@@ -52,13 +52,19 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        ifs_bk="$IFS"
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        IFS="$ifs_bk"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-17T14:29:20Z

- **hack/multikueue-e2e-test.sh:60** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T14:29:13Z
```diff
@@ -52,13 +52,17 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  Can we directly add the feature gate to https://github.com/kubernetes-sigs/kueue/blob/4199c9dd9ce89636eb4e72f5cebb3c9adfba3f0c/hack/multikueue/manager-cluster.kind.yaml instead of this script change?
  
  Because, the featureGate can be specified even if the featureGate has already been graduated to the Beta or GA.
  Additionally, AFAIK, the k/k featureGate will be kept even after GA while 2 versions.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-17T14:37:58Z

- **hack/multikueue-e2e-test.sh:60** by [@trasc](https://github.com/trasc) — 2024-10-17T14:37:58Z
```diff
@@ -52,13 +52,17 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  No, the feature gate is added in 1.30.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-17T14:43:30Z

- **hack/multikueue-e2e-test.sh:60** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-17T14:43:30Z
```diff
@@ -52,13 +52,17 @@ function startup {
             mkdir -p "$ARTIFACTS"
         fi
 
-        MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind-${KIND_VERSION}.yaml"
-        if [ !  -f "$MANAGER_KIND_CONFIG" ]; then
-            MANAGER_KIND_CONFIG="${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml"
+	cp "${SOURCE_DIR}/multikueue/manager-cluster.kind.yaml" "$ARTIFACTS"
+
+        #Enable the JobManagedBy feature gates for k8s 1.30+ versions 
+        IFS=. read -r -a varr <<< "$KIND_VERSION"
+        minor=$(( varr[1] ))        
+        if [ "$minor" -ge 30 ]; then
```

  Oh, that's true. I recognized the `ge` as `le`, missingly.
  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-17T14:43:38Z

/approve
