# PR #3254: [MultiKueue] Introduce Admission Check Validation feature gate

**Summary**: [MultiKueue] Introduce Admission Check Validation feature gate

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3254

**Last updated**: 2024-10-22T16:07:00Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/disable-confusing-ac-conditions` → `main`
- **Created**: 2024-10-17T09:36:31Z
- **Updated**: 2024-10-22T16:07:00Z
- **Merged**: 2024-10-22T15:54:52Z
- **Closed**: 2024-10-22T15:54:52Z
- **Labels**: `size/L`, `lgtm`, `release-note-action-required`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor)
- **Changed files**: 10   **+368 / -77**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
`SingleInstanceInClusterQueue` and `FlavorIndependent` are confusing to the multi-cluster users.
We need to wrap them with feature gate, but preserve the logic for MultiKueue Admission Checks constraints without those status conditions.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3094 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Deprecated SingleInstanceInClusterQueue and FlavorIndependent status conditions.

ACTION REQUIRED: the Admission check status conditions “FlavorIndependent” and “SingleInstanceInClusterQueue” are no longer supported by default.
If you were using any of these conditions for your external AdmissionCheck you need to enable the `AdmissionCheckValidationRules` feature gate. 
For the future releases you will need to provide validation by an external controller.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-17T09:36:33Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-17T09:36:47Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f4fc1e0ede2081660d9307709e6f462c592b0ae3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6717c4389d141d00084bac5f |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-10-17T09:37:00Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-17T09:50:57Z

/cc

### Comment by [@trasc](https://github.com/trasc) — 2024-10-18T13:25:32Z

/uncc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-22T15:33:41Z

LGTM label has been added.  <details>Git tree hash: ac4ad9aee48df600a046633e5b172c443bb9d05c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-22T15:52:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3254#pullrequestreview-2385644687" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3254#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-17T09:50:35Z

- **pkg/features/kube_features.go:115** by [@mimowo](https://github.com/mimowo) — 2024-10-17T09:50:35Z
```diff
@@ -109,6 +109,13 @@ const (
 	// Enable Topology Aware Scheduling allowing to optimize placement of Pods
 	// to put them on closely located nodes (e.g. within the same rack or block).
 	TopologyAwareScheduling featuregate.Feature = "TopologyAwareScheduling"
+
+	// owner: @mszadkow
+	// alpha: v0.9
+	// Deprecated: v0.10
```

  ```suggestion
  	// alpha: v0.9
  	// deprecated: v0.9
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-18T11:27:55Z

Overall this looks good.

I'm leaning to use dedicated fields in the cache for the checks so that there is a clear separation of the new and old mechanism. It will make it easier to drop the old mechanism in 0.10. Maybe it would also give us more control over the reasons, it will be better to have dedicated reasons.

- **pkg/cache/cache.go:248** by [@mimowo](https://github.com/mimowo) — 2024-10-18T11:15:08Z
```diff
@@ -240,11 +241,19 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
+
+	// TBD: This might not be needed, but we could keep it for readability
+	// because SingleInstanceInClusterQueue and FlavorIndependent will be false if AdmissionCheckValidationRules is off
+	singleInstanceInClusterQueue, flavorIndependent := false, false
+	if features.Enabled(features.AdmissionCheckValidationRules) {
```

  Gating this code makes sense, but I think for reasability we can do:
  ```
  newAC :=  AdmissionCheck{
    Active
    Controller 
    // by default SingleInstanceInClusterQueue and FlavorIndependent to false
  }
  if features.Enabled(features.AdmissionCheckValidationRules) {
    newAC. SingleInstanceInClusterQueue =apimeta.IsStatusConditionTrue(ac.Status.Conditions, kueue.AdmissionChecksSingleInstanceInClusterQueue)
    newAC.FlavorIndependent = apimeta.IsStatusConditionTrue(ac.Status.Conditions, kueue.FlavorIndependentAdmissionCheck)
  }
  c.admissionChecks[ac.Name] = newAC
  ```

- **pkg/cache/clusterqueue.go:269** by [@mimowo](https://github.com/mimowo) — 2024-10-18T11:17:04Z
```diff
@@ -265,16 +266,28 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			reasons = append(reasons, kueue.ClusterQueueActiveReasonAdmissionCheckInactive)
 			messages = append(messages, fmt.Sprintf("references inactive AdmissionCheck(s): %v", c.inactiveAdmissionChecks))
 		}
-		if len(c.multipleSingleInstanceControllersChecks) > 0 {
-			reasons = append(reasons, kueue.ClusterQueueActiveReasonMultipleSingleInstanceControllerAdmissionChecks)
-			for _, controller := range utilmaps.SortedKeys(c.multipleSingleInstanceControllersChecks) {
-				messages = append(messages, fmt.Sprintf("only one AdmissionCheck of %v can be referenced for controller %q", c.multipleSingleInstanceControllersChecks[controller], controller))
+		if features.Enabled(features.AdmissionCheckValidationRules) {
```

  IIUC we don't need any diff here if we used a dedicated cache field.
  Then we can also easier drop the code in 0.10. For the hard-coded validation we can use 
  `multipleMultiKueueChecks` and `perFlavorMultiKueueChecks`

- **pkg/cache/clusterqueue.go:350** by [@mimowo](https://github.com/mimowo) — 2024-10-18T11:17:35Z
```diff
@@ -333,11 +346,22 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 				inactive = append(inactive, acName)
 			}
 			checksPerController[ac.Controller] = append(checksPerController[ac.Controller], acName)
-			if ac.SingleInstanceInClusterQueue {
+
+			if features.Enabled(features.AdmissionCheckValidationRules) {
```

  Similarly here, avoid the diff by using dedicated cache fields for the new checks

- **pkg/cache/clusterqueue_test.go:800** by [@mimowo](https://github.com/mimowo) — 2024-10-18T11:20:59Z
```diff
@@ -771,10 +775,52 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantReason:  "Terminating",
 			wantMessage: "Can't admit new workloads; clusterQueue is terminating",
 		},
+		{
+			name:     "Active clusterQueue with an AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                       true,
+					Controller:                   "controller1",
+					SingleInstanceInClusterQueue: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                       true,
+					Controller:                   "controller2",
+					SingleInstanceInClusterQueue: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage: "Can't admit new workloads: Cannot use multiple MultiKueue AdmissionChecks on the same ClusterQueue.",
```

  When the feature is disabled set reason as MultipleMultiKueueAdmissionChecks

- **pkg/cache/clusterqueue_test.go:815** by [@mimowo](https://github.com/mimowo) — 2024-10-18T11:21:55Z
```diff
@@ -771,10 +775,52 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantReason:  "Terminating",
 			wantMessage: "Can't admit new workloads; clusterQueue is terminating",
 		},
+		{
+			name:     "Active clusterQueue with an AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                       true,
+					Controller:                   "controller1",
+					SingleInstanceInClusterQueue: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                       true,
+					Controller:                   "controller2",
+					SingleInstanceInClusterQueue: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage: "Can't admit new workloads: Cannot use multiple MultiKueue AdmissionChecks on the same ClusterQueue.",
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        "controller1",
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
+			wantMessage: "Can't admit new workloads: MultiKueue AdmissionCheck cannot be specified per flavor.",
```

  When the gate is disabled use reason `MutliKueueAdmissionCheckAppliedPerFlavor`

- **test/integration/controller/core/clusterqueue_controller_test.go:790** by [@mimowo](https://github.com/mimowo) — 2024-10-18T11:22:58Z
```diff
@@ -758,6 +758,91 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Ordered, ginkgo.Contin
 				},
 			}, util.IgnoreConditionTimestampsAndObservedGeneration))
 		})
+
+		ginkgo.It("Should prevent workload admission due to multikueue contraints", func() {
+			cpuArchAFlavor = testing.MakeResourceFlavor(flavorCPUArchA).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cpuArchAFlavor)).To(gomega.Succeed())
+
+			cpuArchBFlavor = testing.MakeResourceFlavor(flavorCPUArchB).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cpuArchBFlavor)).To(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName(kueue.MultiKueueControllerName).Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).To(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName(kueue.MultiKueueControllerName).Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).To(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			ginkgo.By("Multiple MultiKueue admission checks for the same cluster queue")
+
+			gomega.Eventually(func() []metav1.Condition {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status.Conditions
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo([]metav1.Condition{
+				{
+					Type:    kueue.ClusterQueueActive,
+					Status:  metav1.ConditionFalse,
+					Reason:  "MultipleSingleInstanceControllerAdmissionChecks",
```

  Make this specific as in the other comment.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-18T15:34:20Z

- **test/integration/multikueue/multikueue_test.go:193** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T15:05:13Z
```diff
@@ -187,6 +187,10 @@ var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 		gomega.Expect(worker2TestCluster.client.Create(worker2TestCluster.ctx, worker2Cq)).Should(gomega.Succeed())
 		worker2Lq = utiltesting.MakeLocalQueue(worker2Cq.Name, worker2Ns.Name).ClusterQueue(worker2Cq.Name).Obj()
 		gomega.Expect(worker2TestCluster.client.Create(worker2TestCluster.ctx, worker2Lq)).Should(gomega.Succeed())
+
+		ginkgo.By("Disabling AdmissionCheckValidationRules feature", func() {
+			gomega.Expect(features.SetEnable(features.AdmissionCheckValidationRules, false)).To(gomega.Succeed())
+		})
```

  I think it should be disabled by default, no?

- **test/integration/multikueue/multikueue_test.go:341** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T15:06:55Z
```diff
@@ -348,6 +340,9 @@ var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 	})
 
 	ginkgo.It("Should properly manage the active condition of AdmissionChecks and MultiKueueClusters, kubeconfig provided by file", func() {
+		ginkgo.By("Enabling AdmissionCheckValidationRules feature", func() {
+			gomega.Expect(features.SetEnable(features.AdmissionCheckValidationRules, true)).To(gomega.Succeed())
+		})
```

  ```suggestion
  		ginkgo.By("Enabling AdmissionCheckValidationRules feature", func() {
  			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConfigurableResourceTransformations, true)
  		})
  ```

- **site/content/en/docs/installation/_index.md:258** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-18T15:26:46Z
```diff
@@ -255,6 +255,7 @@ The currently supported features are:
 | `TopologyAwareScheduling`             | `false` | Alpha      | 0.9   |       |
 | `ConfigurableResourceTransformations` | `false` | Alpha      | 0.9   |       |
 | `WorkloadResourceRequestsSummary`     | `false` | Alpha      | 0.9   |       |
+| `AdmissionCheckValidationRules`       | `false` | Alpha      | 0.9   | 0.9   |
```

  ```suggestion
  | `AdmissionCheckValidationRules`       | `false` | Deprecated | 0.9   | 0.9   |
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-21T07:39:56Z

- **test/integration/multikueue/multikueue_test.go:193** by [@mszadkow](https://github.com/mszadkow) — 2024-10-21T07:39:56Z
```diff
@@ -187,6 +187,10 @@ var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 		gomega.Expect(worker2TestCluster.client.Create(worker2TestCluster.ctx, worker2Cq)).Should(gomega.Succeed())
 		worker2Lq = utiltesting.MakeLocalQueue(worker2Cq.Name, worker2Ns.Name).ClusterQueue(worker2Cq.Name).Obj()
 		gomega.Expect(worker2TestCluster.client.Create(worker2TestCluster.ctx, worker2Lq)).Should(gomega.Succeed())
+
+		ginkgo.By("Disabling AdmissionCheckValidationRules feature", func() {
+			gomega.Expect(features.SetEnable(features.AdmissionCheckValidationRules, false)).To(gomega.Succeed())
+		})
```

  True, but I think it persists between tests, so I would rather keep it disabled before each test

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-21T07:59:40Z

- **test/integration/multikueue/multikueue_test.go:193** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-21T07:59:40Z
```diff
@@ -187,6 +187,10 @@ var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure,
 		gomega.Expect(worker2TestCluster.client.Create(worker2TestCluster.ctx, worker2Cq)).Should(gomega.Succeed())
 		worker2Lq = utiltesting.MakeLocalQueue(worker2Cq.Name, worker2Ns.Name).ClusterQueue(worker2Cq.Name).Obj()
 		gomega.Expect(worker2TestCluster.client.Create(worker2TestCluster.ctx, worker2Lq)).Should(gomega.Succeed())
+
+		ginkgo.By("Disabling AdmissionCheckValidationRules feature", func() {
+			gomega.Expect(features.SetEnable(features.AdmissionCheckValidationRules, false)).To(gomega.Succeed())
+		})
```

  Use `features.SetFeatureGateDuringTest` it should revert after test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T12:22:55Z

- **pkg/cache/clusterqueue.go:80** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:15:51Z
```diff
@@ -77,9 +77,12 @@ type clusterQueue struct {
 	inactiveAdmissionChecks                         []string
 	multipleSingleInstanceControllersChecks         map[string][]string // key = controllerName
 	flavorIndependentAdmissionCheckAppliedPerFlavor []string
-	admittedWorkloadsCount                          int
-	isStopped                                       bool
-	workloadInfoOptions                             []workload.InfoOption
+	// TBD: Are we interested in knowing those AC names?
```

  If it does not add too much complexity, then it is better to know them

- **pkg/cache/clusterqueue.go:236** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:16:12Z
```diff
@@ -230,7 +233,9 @@ func (c *clusterQueue) updateQueueStatus() {
 		len(c.missingAdmissionChecks) > 0 ||
 		len(c.inactiveAdmissionChecks) > 0 ||
 		len(c.multipleSingleInstanceControllersChecks) > 0 ||
-		len(c.flavorIndependentAdmissionCheckAppliedPerFlavor) > 0 {
+		len(c.flavorIndependentAdmissionCheckAppliedPerFlavor) > 0 ||
```

  was this check for `flavorIndependentAdmissionCheckAppliedPerFlavor` missing before?

- **pkg/cache/clusterqueue.go:274** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:17:28Z
```diff
@@ -265,18 +270,30 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			reasons = append(reasons, kueue.ClusterQueueActiveReasonAdmissionCheckInactive)
 			messages = append(messages, fmt.Sprintf("references inactive AdmissionCheck(s): %v", c.inactiveAdmissionChecks))
 		}
+
+		// TBD: This shouldn't be feature gated, as the messages and reasons are valid
```

  I guess we don't need to feature gate it, because if the gate is disabled thee value `multipleSingleInstanceControllersChecks` would be empty anyway. Right? If this is the case, maybe just comment: "This doesn't need to be gated behind <gate name>, because it is empty when the gate is disabled"

- **pkg/cache/clusterqueue.go:281** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:18:28Z
```diff
@@ -265,18 +270,30 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			reasons = append(reasons, kueue.ClusterQueueActiveReasonAdmissionCheckInactive)
 			messages = append(messages, fmt.Sprintf("references inactive AdmissionCheck(s): %v", c.inactiveAdmissionChecks))
 		}
+
+		// TBD: This shouldn't be feature gated, as the messages and reasons are valid
 		if len(c.multipleSingleInstanceControllersChecks) > 0 {
 			reasons = append(reasons, kueue.ClusterQueueActiveReasonMultipleSingleInstanceControllerAdmissionChecks)
 			for _, controller := range utilmaps.SortedKeys(c.multipleSingleInstanceControllersChecks) {
 				messages = append(messages, fmt.Sprintf("only one AdmissionCheck of %v can be referenced for controller %q", c.multipleSingleInstanceControllersChecks[controller], controller))
 			}
 		}
-
+		// TBD: This shouldn't be feature gated, as the messages and reasons are valid
```

  as above

- **pkg/cache/clusterqueue.go:355** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:19:48Z
```diff
@@ -333,19 +352,33 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 				inactive = append(inactive, acName)
 			}
 			checksPerController[ac.Controller] = append(checksPerController[ac.Controller], acName)
+
```

  nit, revert formatting changes

- **pkg/cache/clusterqueue_test.go:851** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:21:27Z
```diff
@@ -771,10 +797,69 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantReason:  "Terminating",
 			wantMessage: "Can't admit new workloads; clusterQueue is terminating",
 		},
+		{
+			name:     "Active clusterQueue with an AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                       true,
+					Controller:                   "controller1",
+					SingleInstanceInClusterQueue: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                       true,
+					Controller:                   "controller2",
+					SingleInstanceInClusterQueue: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage: `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        "controller1",
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
+			wantMessage: "Can't admit new workloads: AdmissionCheck(s): [check1] cannot be set at flavor level.",
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent MultiKueue AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        kueue.MultiKueueControllerName,
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
```

  IIUC this reason should be also specific for MultiKueue as the feature gate is disbaled

- **test/integration/controller/core/clusterqueue_controller_test.go:765** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:22:49Z
```diff
@@ -758,6 +761,100 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Ordered, ginkgo.Contin
 				},
 			}, util.IgnoreConditionTimestampsAndObservedGeneration))
 		})
+
+		ginkgo.It("Should prevent workload admission due to multikueue contraints", func() {
```

  We want to have also an integration test when the feature gate is not enabled, and we see the reasons are specific for MultiKueue.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-21T12:36:44Z

- **pkg/cache/clusterqueue.go:236** by [@mszadkow](https://github.com/mszadkow) — 2024-10-21T12:36:44Z
```diff
@@ -230,7 +233,9 @@ func (c *clusterQueue) updateQueueStatus() {
 		len(c.missingAdmissionChecks) > 0 ||
 		len(c.inactiveAdmissionChecks) > 0 ||
 		len(c.multipleSingleInstanceControllersChecks) > 0 ||
-		len(c.flavorIndependentAdmissionCheckAppliedPerFlavor) > 0 {
+		len(c.flavorIndependentAdmissionCheckAppliedPerFlavor) > 0 ||
```

  no, it was there what changed is just `||`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T12:40:00Z

- **pkg/cache/clusterqueue.go:236** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:40:00Z
```diff
@@ -230,7 +233,9 @@ func (c *clusterQueue) updateQueueStatus() {
 		len(c.missingAdmissionChecks) > 0 ||
 		len(c.inactiveAdmissionChecks) > 0 ||
 		len(c.multipleSingleInstanceControllersChecks) > 0 ||
-		len(c.flavorIndependentAdmissionCheckAppliedPerFlavor) > 0 {
+		len(c.flavorIndependentAdmissionCheckAppliedPerFlavor) > 0 ||
```

  ah, I missed that

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-21T13:08:04Z

- **pkg/cache/clusterqueue_test.go:862** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-21T13:06:51Z
```diff
@@ -771,10 +797,69 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantReason:  "Terminating",
 			wantMessage: "Can't admit new workloads; clusterQueue is terminating",
 		},
+		{
+			name:     "Active clusterQueue with an AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                       true,
+					Controller:                   "controller1",
+					SingleInstanceInClusterQueue: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                       true,
+					Controller:                   "controller2",
+					SingleInstanceInClusterQueue: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage: `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        "controller1",
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
+			wantMessage: "Can't admit new workloads: AdmissionCheck(s): [check1] cannot be set at flavor level.",
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent MultiKueue AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        kueue.MultiKueueControllerName,
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
+			wantMessage: "Can't admit new workloads: AdmissionCheck(s): [check1] cannot be set at flavor level, MultiKueue AdmissionCheck cannot be specified per flavor.",
+		},
 	}
 
 	for _, tc := range testcases {
 		t.Run(tc.name, func(t *testing.T) {
+			if tc.acValidationRulesEnabled {
+				features.SetFeatureGateDuringTest(t, features.AdmissionCheckValidationRules, true)
+			} else {
+				features.SetFeatureGateDuringTest(t, features.AdmissionCheckValidationRules, false)
+			}
```

  ```suggestion
  			}
  ```

- **pkg/cache/clusterqueue_test.go:683** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-21T13:07:40Z
```diff
@@ -672,12 +674,35 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 					SingleInstanceInClusterQueue: true,
 				},
 			},
+			wantStatus:               pending,
+			wantReason:               "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage:              `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+			acValidationRulesEnabled: true,
+		},
+		{
+			name:     "Active clusterQueue with an MultiKueue AC strategy updated with duplicate single instance AC Controller - MultiKueue AdmissionCheckValidationRules enabled",
```

  ```suggestion
  			name:     "Active clusterQueue with an MultiKueue AC strategy updated with duplicate single instance AC Controller - MultiKueue AdmissionCheckValidationRules disabled",
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-21T13:24:30Z

- **pkg/cache/clusterqueue.go:381** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-21T13:19:44Z
```diff
@@ -333,19 +352,33 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 				inactive = append(inactive, acName)
 			}
 			checksPerController[ac.Controller] = append(checksPerController[ac.Controller], acName)
+
 			if ac.SingleInstanceInClusterQueue {
 				singleInstanceControllers.Insert(ac.Controller)
 			}
 			if ac.FlavorIndependent && flavors.Len() != 0 {
 				flavorIndependentCheckOnFlavors = append(flavorIndependentCheckOnFlavors, acName)
 			}
+
+			if ac.Controller == kueue.MultiKueueControllerName {
+				// MultiKueue Admission Checks have extra constraints
+				// disallow to use multiple MultiKueue AdmissionChecks on the same ClusterQueue
+				// MultiKueue AdmissionCheck cannot be specified per flavor
+				multipleMultiKueueControllers.Insert(acName)
+				if flavors.Len() != 0 {
+					perFlavorMultiKueueChecks = append(perFlavorMultiKueueChecks, acName)
+				}
+			}
 		}
 	}
 
 	// sort the lists since c.AdmissionChecks is a map
 	slices.Sort(missing)
 	slices.Sort(inactive)
 	slices.Sort(flavorIndependentCheckOnFlavors)
+	slices.Sort(perFlavorMultiKueueChecks)
+	multipleMultiKueueControllersList := multipleMultiKueueControllers.UnsortedList()
+	slices.Sort(multipleMultiKueueControllersList)
```

  ```suggestion
  	multipleMultiKueueControllersList := sets.List(multipleMultiKueueControllers)
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-21T13:24:53Z

- **pkg/cache/clusterqueue_test.go:851** by [@mszadkow](https://github.com/mszadkow) — 2024-10-21T13:24:53Z
```diff
@@ -771,10 +797,69 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantReason:  "Terminating",
 			wantMessage: "Can't admit new workloads; clusterQueue is terminating",
 		},
+		{
+			name:     "Active clusterQueue with an AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                       true,
+					Controller:                   "controller1",
+					SingleInstanceInClusterQueue: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                       true,
+					Controller:                   "controller2",
+					SingleInstanceInClusterQueue: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage: `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        "controller1",
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
+			wantMessage: "Can't admit new workloads: AdmissionCheck(s): [check1] cannot be set at flavor level.",
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent MultiKueue AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        kueue.MultiKueueControllerName,
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
```

  problematic, bc who this stuff is designed, first reason to appear is actually returned
  ```go
  		return reasons[0], api.TruncateConditionMessage(strings.Join([]string{"Can't admit new workloads: ", strings.Join(messages, ", "), "."}, ""))
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-21T13:36:20Z

- **test/integration/controller/core/clusterqueue_controller_test.go:765** by [@mszadkow](https://github.com/mszadkow) — 2024-10-21T13:36:19Z
```diff
@@ -758,6 +761,100 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Ordered, ginkgo.Contin
 				},
 			}, util.IgnoreConditionTimestampsAndObservedGeneration))
 		})
+
+		ginkgo.It("Should prevent workload admission due to multikueue contraints", func() {
```

  this is actually the test without the feature gate,
  we don't check status there just messages

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T17:40:56Z

- **pkg/cache/clusterqueue_test.go:851** by [@mimowo](https://github.com/mimowo) — 2024-10-21T17:40:56Z
```diff
@@ -771,10 +797,69 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantReason:  "Terminating",
 			wantMessage: "Can't admit new workloads; clusterQueue is terminating",
 		},
+		{
+			name:     "Active clusterQueue with an AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                       true,
+					Controller:                   "controller1",
+					SingleInstanceInClusterQueue: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                       true,
+					Controller:                   "controller2",
+					SingleInstanceInClusterQueue: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage: `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        "controller1",
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
+			wantMessage: "Can't admit new workloads: AdmissionCheck(s): [check1] cannot be set at flavor level.",
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent MultiKueue AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        kueue.MultiKueueControllerName,
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
```

  I would think we don't need to generate this reason then, only the dedicated one
  
  EDIT: maybe in the worst case scenario re-ordering the reasons would work, but ideally we don't generate the old reasons at all

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T17:42:18Z

- **test/integration/controller/core/clusterqueue_controller_test.go:765** by [@mimowo](https://github.com/mimowo) — 2024-10-21T17:42:18Z
```diff
@@ -758,6 +761,100 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Ordered, ginkgo.Contin
 				},
 			}, util.IgnoreConditionTimestampsAndObservedGeneration))
 		})
+
+		ginkgo.It("Should prevent workload admission due to multikueue contraints", func() {
```

  I would say the reason is more important than the message.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-22T06:58:38Z

- **pkg/cache/clusterqueue_test.go:851** by [@mszadkow](https://github.com/mszadkow) — 2024-10-22T06:58:38Z
```diff
@@ -771,10 +797,69 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantReason:  "Terminating",
 			wantMessage: "Can't admit new workloads; clusterQueue is terminating",
 		},
+		{
+			name:     "Active clusterQueue with an AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                       true,
+					Controller:                   "controller1",
+					SingleInstanceInClusterQueue: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                       true,
+					Controller:                   "controller2",
+					SingleInstanceInClusterQueue: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage: `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        "controller1",
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
+			wantMessage: "Can't admit new workloads: AdmissionCheck(s): [check1] cannot be set at flavor level.",
+		},
+		{
+			name:     "Active clusterQueue with a FlavorIndependent MultiKueue AC applied per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:            true,
+					Controller:        kueue.MultiKueueControllerName,
+					FlavorIndependent: true,
+				},
+			},
+			wantStatus:  pending,
+			wantReason:  "FlavorIndependentAdmissionCheckAppliedPerFlavor",
```

  I will put the new multikueue reason before those old
  but if we want to change the overall approach it should be another PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T11:33:00Z

LGTM overall, left some minor remarks
cc @mbobrovskyi as another pair of eyes

- **pkg/cache/clusterqueue.go:277** by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:27:21Z
```diff
@@ -265,13 +271,25 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			reasons = append(reasons, kueue.ClusterQueueActiveReasonAdmissionCheckInactive)
 			messages = append(messages, fmt.Sprintf("references inactive AdmissionCheck(s): %v", c.inactiveAdmissionChecks))
 		}
+
+		if len(c.multipleMultiKueueAdmissionChecks) > 1 {
+			reasons = append(reasons, kueue.ClusterQueueActiveReasonMultipleMultiKueueAdmissionChecks)
+			messages = append(messages, "Cannot use multiple MultiKueue AdmissionChecks on the same ClusterQueue")
```

  ```suggestion
  			messages = append(messages, "Cannot use multiple MultiKueue AdmissionChecks on the same ClusterQueue, found: %v", strings.Join(multipleMultiKueueAdmissionChecks, ","))
  ```

- **pkg/cache/clusterqueue.go:282** by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:28:28Z
```diff
@@ -265,13 +271,25 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			reasons = append(reasons, kueue.ClusterQueueActiveReasonAdmissionCheckInactive)
 			messages = append(messages, fmt.Sprintf("references inactive AdmissionCheck(s): %v", c.inactiveAdmissionChecks))
 		}
+
+		if len(c.multipleMultiKueueAdmissionChecks) > 1 {
+			reasons = append(reasons, kueue.ClusterQueueActiveReasonMultipleMultiKueueAdmissionChecks)
+			messages = append(messages, "Cannot use multiple MultiKueue AdmissionChecks on the same ClusterQueue")
+		}
+
+		if len(c.perFlavorMultiKueueAdmissionChecks) > 0 {
+			reasons = append(reasons, kueue.ClusterQueueActiveReasonMutliKueueAdmissionCheckAppliedPerFlavor)
+			messages = append(messages, "MultiKueue AdmissionCheck cannot be specified per flavor")
```

  ```suggestion
  			messages = append(messages, "Cannot specify MultiKueue AdmissionCheck per flavor, found: %s", strings.Join(perFlavorMultiKueueAdmissionChecks, ",") )
  ```
  or along the lines

- **pkg/cache/clusterqueue.go:366** by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:29:52Z
```diff
@@ -339,13 +359,25 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 			if ac.FlavorIndependent && flavors.Len() != 0 {
 				flavorIndependentCheckOnFlavors = append(flavorIndependentCheckOnFlavors, acName)
 			}
+
+			if ac.Controller == kueue.MultiKueueControllerName {
+				// MultiKueue Admission Checks have extra constraints
+				// disallow to use multiple MultiKueue AdmissionChecks on the same ClusterQueue
+				// MultiKueue AdmissionCheck cannot be specified per flavor
```

  ```suggestion
  				// MultiKueue Admission Checks has extra constraints:
  				// - cannot use multiple MultiKueue AdmissionChecks on the same ClusterQueue
  				// - cannot use specify MultiKueue AdmissionCheck per flavor
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-22T12:31:30Z

- **pkg/features/kube_features.go:169** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-22T12:29:43Z
```diff
@@ -159,6 +166,7 @@ var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
 	ConfigurableResourceTransformations: {Default: false, PreRelease: featuregate.Alpha},
 	WorkloadResourceRequestsSummary:     {Default: false, PreRelease: featuregate.Alpha},
 	ExposeFlavorsInLocalQueue:           {Default: true, PreRelease: featuregate.Beta},
+	AdmissionCheckValidationRules:       {Default: false, PreRelease: featuregate.Alpha},
```

  ```suggestion
  	AdmissionCheckValidationRules:       {Default: false, PreRelease: featuregate.Deprecated},
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-22T12:44:50Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck_test.go:71** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-22T12:44:50Z
```diff
@@ -66,6 +68,7 @@ func TestReconcile(t *testing.T) {
 					}).
 					Obj(),
 			},
+			acValidationRulesEnabled: true,
```

  ```suggestion
  			acValidationRulesEnabled: false,
  ```
  
  If this feature is deprecated and disabled by default shouldn't we change previous tests to false? And create new one where it is enabled?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-10-22T13:17:43Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck_test.go:71** by [@mszadkow](https://github.com/mszadkow) — 2024-10-22T13:17:43Z
```diff
@@ -66,6 +68,7 @@ func TestReconcile(t *testing.T) {
 					}).
 					Obj(),
 			},
+			acValidationRulesEnabled: true,
```

  we can reverse this sure

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-22T13:53:13Z

- **pkg/cache/clusterqueue_test.go:728** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-22T13:53:13Z
```diff
@@ -672,12 +697,35 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 					SingleInstanceInClusterQueue: true,
 				},
 			},
+			wantStatus:               pending,
+			wantReason:               "MultipleSingleInstanceControllerAdmissionChecks",
+			wantMessage:              `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+			acValidationRulesEnabled: true,
+		},
+		{
+			name:     "Active clusterQueue with an MultiKueue AC strategy updated with duplicate single instance AC Controller",
+			cq:       cqWithACStrategy,
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: kueue.MultiKueueControllerName,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: kueue.MultiKueueControllerName,
+				},
+			},
 			wantStatus:  pending,
-			wantReason:  "MultipleSingleInstanceControllerAdmissionChecks",
-			wantMessage: `Can't admit new workloads: only one AdmissionCheck of [check2 check3] can be referenced for controller "controller2".`,
+			wantReason:  kueue.ClusterQueueActiveReasonMultipleMultiKueueAdmissionChecks,
+			wantMessage: `Can't admit new workloads: Cannot use multiple MultiKueue AdmissionChecks on the same ClusterQueue, found: check1,check3.`,
 		},
 		{
-			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
+			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor - AdmissionCheckValidationRules enabled",
```

  Shouldn't it be disabled? Because I found the same test case on L848.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-22T15:33:34Z

/lgtm

Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T15:52:53Z

/approve
Thanks!
