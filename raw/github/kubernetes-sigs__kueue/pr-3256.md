# PR #3256: TAS: Compute the topology assignments

**Summary**: TAS: Compute the topology assignments

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3256

**Last updated**: 2025-01-21T11:25:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-compute-assignment` → `main`
- **Created**: 2024-10-17T14:01:48Z
- **Updated**: 2025-01-21T11:25:31Z
- **Merged**: 2024-10-22T13:38:51Z
- **Closed**: 2024-10-22T13:38:51Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 34   **+2679 / -54**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #2724 

#### Special notes for your reviewer:

I'm not setting any release note here as it is already set on the API PR.

This PR will be followed up with (based on the prototype in https://github.com/kubernetes-sigs/kueue/pull/3218)
- Adding scheduling gates for the Pods (DONE)
- TopologyUngater to ungate the pods (DONE)
- e2e tests (prototyped)
- validation PRs
- bugfixes when tested e2e at bigger scale

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-17T14:02:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 915d1de6af9a0dbef384e54831dd4f676cef4181 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67178aa926fe560008fb8c32 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-17T14:44:34Z

/assign @PBundyra @gabesaba

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-17T15:59:14Z

/cc @tenzen-y

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-18T08:38:15Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-18T14:45:26Z

/cc @alculquicondor

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:40:38Z

/test pull-kueue-test-multikueue-e2e-main
infra flake unralted to the PR: 
`go: sigs.k8s.io/kind@v0.24.0: Get "https://proxy.golang.org/sigs.k8s.io/kind/@v/v0.24.0.info": net/http: TLS handshake timeout`

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:41:44Z

FYI as mentioned under the [issue](https://github.com/kubernetes-sigs/kueue/issues/2724#issuecomment-2428874072) I opened a [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU/edit?resourcekey=0-gr1ML2A1Axi8s6Lxr-Zhlw&gid=0#gid=0) to keep track of the follow ups.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-22T13:21:25Z

LGTM label has been added.  <details>Git tree hash: d1b5d6eebf128345d06fa257eb856a90e592f1e6</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-22T13:21:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3256#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3256#pullrequestreview-2385181276" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:23:25Z

/hold cancel

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-23T11:40:40Z

LGTM, great work!
Thank you

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-10-18T19:24:32Z

Some initial comments.

- **pkg/cache/cache.go:230** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:09:00Z
```diff
@@ -223,6 +227,22 @@ func (c *Cache) updateClusterQueues() sets.Set[string] {
 	return cqs
 }
 
+func (c *Cache) GetActiveClusterQueues() sets.Set[string] {
```

  ```suggestion
  func (c *Cache) ActiveClusterQueues() sets.Set[string] {
  ```

- **pkg/cache/cache.go:242** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:09:09Z
```diff
@@ -223,6 +227,22 @@ func (c *Cache) updateClusterQueues() sets.Set[string] {
 	return cqs
 }
 
+func (c *Cache) GetActiveClusterQueues() sets.Set[string] {
+	c.Lock()
+	defer c.Unlock()
+	cqs := sets.New[string]()
+	for _, cq := range c.hm.ClusterQueues {
+		if cq.Status == active {
+			cqs.Insert(cq.Name)
+		}
+	}
+	return cqs
+}
+
+func (c *Cache) GetTASCache() *TASCache {
```

  ```suggestion
  func (c *Cache) TASCache() *TASCache {
  ```

- **pkg/cache/clusterqueue.go:300** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:12:17Z
```diff
@@ -291,6 +295,15 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 func (c *clusterQueue) UpdateWithFlavors(flavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor) {
 	c.updateLabelKeys(flavors)
 	c.updateQueueStatus()
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for flavorName, rf := range flavors {
+			if rf.Spec.TopologyName != nil {
```

  What if a flavor is modified to remove the topology name?

- **pkg/cache/clusterqueue.go:469** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:23:16Z
```diff
@@ -449,6 +470,19 @@ func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
+func (c *clusterQueue) tasFlavorCache(flvName kueue.ResourceFlavorReference) *TASFlavorCache {
+	if !features.Enabled(features.TopologyAwareScheduling) {
+		return nil
+	}
+	if c.tasFlavors == nil || c.tasCache == nil {
+		return nil
+	}
+	if !slices.Contains(c.tasFlavors, flvName) {
+		return nil
+	}
```

  wouldn't `tasCache.Get` be able to address this?
  Then, do you really need `c.tasFlavors`?

- **pkg/cache/snapshot.go:80** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:27:30Z
```diff
@@ -76,6 +78,10 @@ func (s *Snapshot) Log(log logr.Logger) {
 }
 
 func (c *Cache) Snapshot() Snapshot {
```

  I suppose you plan to remove this function in a follow up?

- **pkg/cache/tas_cache.go:35** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:30:17Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client client.Client
+	Map    map[kueue.ResourceFlavorReference]*TASFlavorCache
```

  does this need to be exported?

- **pkg/cache/tas_cache.go:52** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:33:33Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client client.Client
+	Map    map[kueue.ResourceFlavorReference]*TASFlavorCache
+}
+
+func NewTASCache(client client.Client) TASCache {
+	return TASCache{
+		client: client,
+		Map:    make(map[kueue.ResourceFlavorReference]*TASFlavorCache),
+	}
+}
+
+func (t *TASCache) NewFlavorCache(labels []string, nodeLabels map[string]string) *TASFlavorCache {
+	return &TASFlavorCache{
+		client:     t.client,
+		Levels:     slices.Clone(labels),
+		NodeLabels: maps.Clone(nodeLabels),
+		usageMap:   make(map[utiltas.TopologyDomainID]resources.Requests),
+	}
+}
+
+func (t *TASCache) Get(name kueue.ResourceFlavorReference) *TASFlavorCache {
+	t.RLock()
+	defer t.RUnlock()
+	return t.Map[name]
+}
+
+func (t *TASCache) GetKeys() []kueue.ResourceFlavorReference {
+	t.RLock()
+	defer t.RUnlock()
+	return utilmaps.Keys(t.Map)
+}
```

  Maybe you can use `maps.All` instead? https://pkg.go.dev/maps#All
  
  Although, I presume it's not thread safe.

- **pkg/cache/clusterqueue_snapshot.go:53** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:34:39Z
```diff
@@ -49,6 +49,8 @@ type ClusterQueueSnapshot struct {
 
 	ResourceNode ResourceNode
 	hierarchy.ClusterQueue[*CohortSnapshot]
+
+	TASFlavorSnapshots map[kueue.ResourceFlavorReference]*TASFlavorSnapshot
```

  ```suggestion
  	TASFlavors map[kueue.ResourceFlavorReference]*TASFlavorSnapshot
  ```

- **pkg/cache/snapshot.go:116** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:35:21Z
```diff
@@ -99,10 +105,24 @@ func (c *Cache) Snapshot() Snapshot {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
-		snap.AddClusterQueue(snapshotClusterQueue(cq))
+		cqSnapshot := snapshotClusterQueue(cq)
+		snap.AddClusterQueue(cqSnapshot)
 		if cq.HasParent() {
 			snap.UpdateClusterQueueEdge(cq.Name, cq.Parent().Name)
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			tasSnapshotsMap := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
+			for _, tasFlv := range c.tasCache.GetKeys() {
+				if tasCacheRF := c.tasCache.Get(tasFlv); tasCacheRF != nil {
```

  why check this, if you passed a key that should be there?

- **pkg/cache/snapshot.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:40:32Z
```diff
@@ -99,10 +105,24 @@ func (c *Cache) Snapshot() Snapshot {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
-		snap.AddClusterQueue(snapshotClusterQueue(cq))
+		cqSnapshot := snapshotClusterQueue(cq)
+		snap.AddClusterQueue(cqSnapshot)
 		if cq.HasParent() {
 			snap.UpdateClusterQueueEdge(cq.Name, cq.Parent().Name)
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			tasSnapshotsMap := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
+			for _, tasFlv := range c.tasCache.GetKeys() {
```

  this doesn't seem to depend on the CQ, why not put it outside the loop?

- **pkg/cache/tas_flavor.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T18:57:35Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client     client.Client
+	NodeLabels map[string]string
+	Levels     []string
```

  Add some comments explaining what these are for

- **pkg/cache/tas_flavor_snapshot.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T19:01:35Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
```

  info of what? :)

- **pkg/cache/tas_flavor_snapshot.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T19:06:49Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+	levelValuesPerDomain  map[utiltas.TopologyDomainID][]string
+	infoPerLevel          []infoPerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	labelsCopy := slices.Clone(levels)
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             labelsCopy,
```

  ```suggestion
  		levelKeys:             slices.Clone(levels),
  ```

- **pkg/cache/tas_flavor_snapshot.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T19:15:06Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
```

  which domains will be here? every levels'?

- **pkg/cache/tas_flavor_snapshot.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T19:16:43Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+	levelValuesPerDomain  map[utiltas.TopologyDomainID][]string
+	infoPerLevel          []infoPerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	labelsCopy := slices.Clone(levels)
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             labelsCopy,
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		infoPerLevel:          make([]infoPerDomain, len(levels)),
+	}
+	return snapshot
+}
+
+// initialize prepares the infoPerLevel tree structure which is used during the
+// algorithm for multiple workloads.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.infoPerLevel[levelIdx] = make(map[utiltas.TopologyDomainID]*info)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childInfo := &info{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+			childIDs: make([]utiltas.TopologyDomainID, 0),
```

  couldn't this be left nil?

- **pkg/cache/tas_flavor_snapshot.go:76** by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-18T19:18:33Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+	levelValuesPerDomain  map[utiltas.TopologyDomainID][]string
+	infoPerLevel          []infoPerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	labelsCopy := slices.Clone(levels)
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             labelsCopy,
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		infoPerLevel:          make([]infoPerDomain, len(levels)),
+	}
+	return snapshot
+}
+
+// initialize prepares the infoPerLevel tree structure which is used during the
```

  please expand

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:28:07Z

- **pkg/cache/cache.go:230** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:28:07Z
```diff
@@ -223,6 +227,22 @@ func (c *Cache) updateClusterQueues() sets.Set[string] {
 	return cqs
 }
 
+func (c *Cache) GetActiveClusterQueues() sets.Set[string] {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:28:13Z

- **pkg/cache/cache.go:242** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:28:13Z
```diff
@@ -223,6 +227,22 @@ func (c *Cache) updateClusterQueues() sets.Set[string] {
 	return cqs
 }
 
+func (c *Cache) GetActiveClusterQueues() sets.Set[string] {
+	c.Lock()
+	defer c.Unlock()
+	cqs := sets.New[string]()
+	for _, cq := range c.hm.ClusterQueues {
+		if cq.Status == active {
+			cqs.Insert(cq.Name)
+		}
+	}
+	return cqs
+}
+
+func (c *Cache) GetTASCache() *TASCache {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:29:30Z

- **pkg/cache/clusterqueue.go:300** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:29:30Z
```diff
@@ -291,6 +295,15 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 func (c *clusterQueue) UpdateWithFlavors(flavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor) {
 	c.updateLabelKeys(flavors)
 	c.updateQueueStatus()
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for flavorName, rf := range flavors {
+			if rf.Spec.TopologyName != nil {
```

  The initial implementation would leak items in the `c.tasFlavors`. Now I drop this field as redundant, along with the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:29:52Z

- **pkg/cache/clusterqueue.go:469** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:29:52Z
```diff
@@ -449,6 +470,19 @@ func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
+func (c *clusterQueue) tasFlavorCache(flvName kueue.ResourceFlavorReference) *TASFlavorCache {
+	if !features.Enabled(features.TopologyAwareScheduling) {
+		return nil
+	}
+	if c.tasFlavors == nil || c.tasCache == nil {
+		return nil
+	}
+	if !slices.Contains(c.tasFlavors, flvName) {
+		return nil
+	}
```

  Indeed, the `c.tasFlavors` was redundant. Updated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:30:46Z

- **pkg/cache/snapshot.go:80** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:30:46Z
```diff
@@ -76,6 +78,10 @@ func (s *Snapshot) Log(log logr.Logger) {
 }
 
 func (c *Cache) Snapshot() Snapshot {
```

  Yes, I intended to follow up. I could do it in this PR too, but it is used in a bunch of tests, so I didn't want to increase the size of the diff which is already large.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:32:21Z

- **pkg/cache/tas_cache.go:35** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:32:21Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client client.Client
+	Map    map[kueue.ResourceFlavorReference]*TASFlavorCache
```

  node, renamed to flvsMap

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:33:36Z

- **pkg/cache/tas_cache.go:52** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:33:36Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client client.Client
+	Map    map[kueue.ResourceFlavorReference]*TASFlavorCache
+}
+
+func NewTASCache(client client.Client) TASCache {
+	return TASCache{
+		client: client,
+		Map:    make(map[kueue.ResourceFlavorReference]*TASFlavorCache),
+	}
+}
+
+func (t *TASCache) NewFlavorCache(labels []string, nodeLabels map[string]string) *TASFlavorCache {
+	return &TASFlavorCache{
+		client:     t.client,
+		Levels:     slices.Clone(labels),
+		NodeLabels: maps.Clone(nodeLabels),
+		usageMap:   make(map[utiltas.TopologyDomainID]resources.Requests),
+	}
+}
+
+func (t *TASCache) Get(name kueue.ResourceFlavorReference) *TASFlavorCache {
+	t.RLock()
+	defer t.RUnlock()
+	return t.Map[name]
+}
+
+func (t *TASCache) GetKeys() []kueue.ResourceFlavorReference {
+	t.RLock()
+	defer t.RUnlock()
+	return utilmaps.Keys(t.Map)
+}
```

  Yes, it needs to be thread safe as new items might be added concurrently via tas-resource-flavor controller.
  However, instead of getting keys I now have a Clone function which performs a shallow copy, so I don't need extra checks later on.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:33:52Z

- **pkg/cache/clusterqueue_snapshot.go:53** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:33:52Z
```diff
@@ -49,6 +49,8 @@ type ClusterQueueSnapshot struct {
 
 	ResourceNode ResourceNode
 	hierarchy.ClusterQueue[*CohortSnapshot]
+
+	TASFlavorSnapshots map[kueue.ResourceFlavorReference]*TASFlavorSnapshot
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:34:21Z

- **pkg/cache/snapshot.go:115** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:34:21Z
```diff
@@ -99,10 +105,24 @@ func (c *Cache) Snapshot() Snapshot {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
-		snap.AddClusterQueue(snapshotClusterQueue(cq))
+		cqSnapshot := snapshotClusterQueue(cq)
+		snap.AddClusterQueue(cqSnapshot)
 		if cq.HasParent() {
 			snap.UpdateClusterQueueEdge(cq.Name, cq.Parent().Name)
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			tasSnapshotsMap := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
+			for _, tasFlv := range c.tasCache.GetKeys() {
```

  Yeah, this block is independent of CQ, move it up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:34:45Z

- **pkg/cache/tas_flavor.go:37** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:34:44Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client     client.Client
+	NodeLabels map[string]string
+	Levels     []string
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:37:22Z

- **pkg/cache/snapshot.go:116** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:37:22Z
```diff
@@ -99,10 +105,24 @@ func (c *Cache) Snapshot() Snapshot {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
-		snap.AddClusterQueue(snapshotClusterQueue(cq))
+		cqSnapshot := snapshotClusterQueue(cq)
+		snap.AddClusterQueue(cqSnapshot)
 		if cq.HasParent() {
 			snap.UpdateClusterQueueEdge(cq.Name, cq.Parent().Name)
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			tasSnapshotsMap := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
+			for _, tasFlv := range c.tasCache.GetKeys() {
+				if tasCacheRF := c.tasCache.Get(tasFlv); tasCacheRF != nil {
```

  This was to avoid race conditions, when I get a key, but the value is removed concurrently. 
  I now avoid this issue altogether by the new Clone function which returns a shallow copy.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:41:25Z

- **pkg/cache/tas_flavor_snapshot.go:29** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:41:25Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
```

  I refactored the info to split into two parts of information:
  - static (hold in the node type, renamed from info), comment added 
  - dynamic with the pod counts, kept in `state statePerDomain`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:41:33Z

- **pkg/cache/tas_flavor_snapshot.go:68** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:41:33Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+	levelValuesPerDomain  map[utiltas.TopologyDomainID][]string
+	infoPerLevel          []infoPerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	labelsCopy := slices.Clone(levels)
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             labelsCopy,
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:42:31Z

- **pkg/cache/tas_flavor_snapshot.go:64** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:42:31Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
```

  Here only the lowest level, added a comment. The only information that needs propagation up is the count of pods which can fit. No need to propagate up info about capacity.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:42:43Z

- **pkg/cache/tas_flavor_snapshot.go:88** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:42:42Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+	levelValuesPerDomain  map[utiltas.TopologyDomainID][]string
+	infoPerLevel          []infoPerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	labelsCopy := slices.Clone(levels)
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             labelsCopy,
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		infoPerLevel:          make([]infoPerDomain, len(levels)),
+	}
+	return snapshot
+}
+
+// initialize prepares the infoPerLevel tree structure which is used during the
+// algorithm for multiple workloads.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.infoPerLevel[levelIdx] = make(map[utiltas.TopologyDomainID]*info)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childInfo := &info{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+			childIDs: make([]utiltas.TopologyDomainID, 0),
```

  It can, upated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:43:06Z

- **pkg/cache/tas_flavor_snapshot.go:76** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:43:06Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type info struct {
+	// count is a temporary state of the topology domain during the topology
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	count int32
+
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type infoPerDomain map[utiltas.TopologyDomainID]*info
+
+type TASFlavorSnapshot struct {
+	levelKeys             []string
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+	levelValuesPerDomain  map[utiltas.TopologyDomainID][]string
+	infoPerLevel          []infoPerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	labelsCopy := slices.Clone(levels)
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             labelsCopy,
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		infoPerLevel:          make([]infoPerDomain, len(levels)),
+	}
+	return snapshot
+}
+
+// initialize prepares the infoPerLevel tree structure which is used during the
```

  done

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-10-21T10:22:02Z

I haven't finished the full pass yet, but want to share the nitpicky comments I have so far

- **test/integration/tas/tas_job_test.go:2** by [@gabesaba](https://github.com/gabesaba) — 2024-10-18T10:53:21Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  nit: 2024, here and in test/integration/tas/suite_test.go

- **pkg/resources/requests.go:63** by [@gabesaba](https://github.com/gabesaba) — 2024-10-21T09:18:51Z
```diff
@@ -37,6 +39,28 @@ func NewRequests(rl corev1.ResourceList) Requests {
 	return r
 }
 
+func (r Requests) Clone() Requests {
+	return maps.Clone(r)
+}
+
+func (r Requests) Divide(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
+
+func (r Requests) Add(newRequests Requests) {
+	for k, v := range newRequests {
+		r[k] += v
+	}
+}
+
+func (r Requests) Sub(subRequests Requests) {
+	for k, v := range subRequests {
+		r[k] -= v
+	}
+}
+
```

  nit: more consistent naming
  
  s/name/k/ in Divide
  s/newRequests/addRequests/ in Add

- **pkg/controller/tas/resource_flavor.go:163** by [@gabesaba](https://github.com/gabesaba) — 2024-10-21T09:23:47Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+			r.queues.Broadcast()
```

  we already (conditionally) call Broadcast in QueueInadmissibleWorkloads
  
  https://github.com/kubernetes-sigs/kueue/blob/4a5155ac1b39e82b26ff8e64bab09b31dc724995/pkg/queue/manager.go#L447-L449

- **pkg/cache/cache.go:233** by [@gabesaba](https://github.com/gabesaba) — 2024-10-21T09:25:53Z
```diff
@@ -225,6 +229,22 @@ func (c *Cache) updateClusterQueues() sets.Set[string] {
 	return cqs
 }
 
+func (c *Cache) ActiveClusterQueues() sets.Set[string] {
+	c.Lock()
```

  RLock?

- **pkg/cache/clusterqueue.go:437** by [@gabesaba](https://github.com/gabesaba) — 2024-10-21T09:32:59Z
```diff
@@ -426,12 +429,20 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
 	frUsage := wi.FlavorResourceUsage()
+	tasUsage := wi.TASUsage()
 	for fr, q := range frUsage {
+		tasFlvCache := c.tasFlavorCache(fr.Flavor)
 		if m == 1 {
 			addUsage(c, fr, q)
+			if tasFlvCache != nil {
```

  nit here and below: consider moving the nil check inside of addUsage/removeUsage. Less verbosity when calling

- **pkg/cache/tas_flavor.go:127** by [@gabesaba](https://github.com/gabesaba) — 2024-10-21T09:49:11Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
+}
+
+func (c *TASFlavorCache) snapshot(ctx context.Context) *TASFlavorSnapshot {
+	log := ctrl.LoggerFrom(ctx)
+	nodeList := &corev1.NodeList{}
+	matcher := client.MatchingLabels{}
+	for k, v := range c.NodeLabels {
+		matcher[k] = v
+	}
+	err := c.client.List(ctx, nodeList, matcher)
+	if err != nil {
+		log.Error(err, "failed to list nodes for TAS", "nodeLabels", c.NodeLabels)
+	}
+	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.NodeLabels,
+		"levels", c.Levels, "nodeCount", len(nodeList.Items))
+	return c.snapshotForNodes(nodeList.Items)
+}
+
+func (c *TASFlavorCache) snapshotForNodes(nodes []corev1.Node) *TASFlavorSnapshot {
+	c.RLock()
+	defer c.RUnlock()
+	snapshot := newTASFlavorSnapshot(c.Levels)
+	for _, node := range nodes {
+		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
+		capacity := resources.NewRequests(node.Status.Allocatable)
+		domainID := utiltas.DomainID(levelValues)
+		snapshot.levelValuesPerDomain[domainID] = levelValues
+		snapshot.addCapacity(domainID, capacity)
+	}
+	snapshot.initialize()
+	for domainID, usage := range c.usageMap {
+		snapshot.addUsage(domainID, usage)
+	}
+	return snapshot
+}
+
+func (c *TASFlavorCache) addUsage(topologyRequests []workload.TopologyDomainRequests) {
+	c.updateUsage(topologyRequests, 1)
+}
+
+func (c *TASFlavorCache) removeUsage(topologyRequests []workload.TopologyDomainRequests) {
+	c.updateUsage(topologyRequests, -1)
+}
+
+func (c *TASFlavorCache) updateUsage(topologyRequests []workload.TopologyDomainRequests, m int64) {
+	c.Lock()
+	defer c.Unlock()
+	for _, tr := range topologyRequests {
+		domainID := utiltas.DomainID(tr.Values)
+		_, found := c.usageMap[domainID]
+		if !found {
+			c.usageMap[domainID] = resources.Requests{}
+		}
+		if m < 0 {
+			c.usageMap[domainID].Sub(tr.Requests)
+		} else {
+			c.usageMap[domainID].Add(tr.Requests)
+		}
```

  can we get rid of these -1/1 magic numbers? I'd even prefer to inline the entire function and duplicate the code. Or define an enum for Add/Subtract.

- **pkg/cache/tas_flavor_snapshot.go:31** by [@gabesaba](https://github.com/gabesaba) — 2024-10-21T09:59:25Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// node holds the static information about placement of a topology domain in
+// the hierarchy of topology domains.
+type node struct {
```

  To avoid confusion with K8 Node, consider a different name. topologyDomainNode?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T11:53:41Z

- **test/integration/tas/tas_job_test.go:2** by [@mimowo](https://github.com/mimowo) — 2024-10-21T11:53:41Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  thx

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T11:53:49Z

- **pkg/resources/requests.go:63** by [@mimowo](https://github.com/mimowo) — 2024-10-21T11:53:49Z
```diff
@@ -37,6 +39,28 @@ func NewRequests(rl corev1.ResourceList) Requests {
 	return r
 }
 
+func (r Requests) Clone() Requests {
+	return maps.Clone(r)
+}
+
+func (r Requests) Divide(f int64) {
+	for name := range r {
+		r[name] /= f
+	}
+}
+
+func (r Requests) Add(newRequests Requests) {
+	for k, v := range newRequests {
+		r[k] += v
+	}
+}
+
+func (r Requests) Sub(subRequests Requests) {
+	for k, v := range subRequests {
+		r[k] -= v
+	}
+}
+
```

  thx. done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T11:55:17Z

- **pkg/controller/tas/resource_flavor.go:163** by [@mimowo](https://github.com/mimowo) — 2024-10-21T11:55:17Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+			r.queues.Broadcast()
```

  good point

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T11:57:49Z

- **pkg/cache/cache.go:233** by [@mimowo](https://github.com/mimowo) — 2024-10-21T11:57:49Z
```diff
@@ -225,6 +229,22 @@ func (c *Cache) updateClusterQueues() sets.Set[string] {
 	return cqs
 }
 
+func (c *Cache) ActiveClusterQueues() sets.Set[string] {
+	c.Lock()
```

  yes, done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T11:59:26Z

- **pkg/controller/tas/resource_flavor.go:163** by [@mimowo](https://github.com/mimowo) — 2024-10-21T11:59:26Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+			r.queues.Broadcast()
```

  Actually, I copy pasted this from here: https://github.com/kubernetes-sigs/kueue/blob/b430724096a56df9a678b6c5d97eb4c5f5f1845b/pkg/controller/core/resourceflavor_controller.go#L137-L140
  
  not sure if it is really needed there. My code is added for scenario when a new node is added increasing the capacity, and I have an integration test for it, so let's see if this passes.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T12:10:17Z

- **pkg/cache/clusterqueue.go:437** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:10:17Z
```diff
@@ -426,12 +429,20 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
 	frUsage := wi.FlavorResourceUsage()
+	tasUsage := wi.TASUsage()
 	for fr, q := range frUsage {
+		tasFlvCache := c.tasFlavorCache(fr.Flavor)
 		if m == 1 {
 			addUsage(c, fr, q)
+			if tasFlvCache != nil {
```

  I see where you are coming from. I wanted to do so, but I hit issues with dependency structure. 
  In particular I would need to pass `tasUsage` and `tasFlvCache` down to `addUsage`, which is too more extra params. Maybe if you are thinking about some refactoring we could follow up?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T12:34:04Z

- **pkg/cache/tas_flavor_snapshot.go:31** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:34:04Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// node holds the static information about placement of a topology domain in
+// the hierarchy of topology domains.
+type node struct {
```

  sgtm, done. I only renamed the type. I think keeping the local vars as node makes sense for brevity still

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-10-21T12:48:21Z

- **pkg/cache/clusterqueue.go:437** by [@gabesaba](https://github.com/gabesaba) — 2024-10-21T12:48:21Z
```diff
@@ -426,12 +429,20 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
 	frUsage := wi.FlavorResourceUsage()
+	tasUsage := wi.TASUsage()
 	for fr, q := range frUsage {
+		tasFlvCache := c.tasFlavorCache(fr.Flavor)
 		if m == 1 {
 			addUsage(c, fr, q)
+			if tasFlvCache != nil {
```

  I'm fine leaving it as it is, then

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T12:50:42Z

- **pkg/cache/clusterqueue.go:437** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:50:42Z
```diff
@@ -426,12 +429,20 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
 	frUsage := wi.FlavorResourceUsage()
+	tasUsage := wi.TASUsage()
 	for fr, q := range frUsage {
+		tasFlvCache := c.tasFlavorCache(fr.Flavor)
 		if m == 1 {
 			addUsage(c, fr, q)
+			if tasFlvCache != nil {
```

  thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T12:57:29Z

- **pkg/cache/tas_flavor.go:127** by [@mimowo](https://github.com/mimowo) — 2024-10-21T12:57:29Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
+}
+
+func (c *TASFlavorCache) snapshot(ctx context.Context) *TASFlavorSnapshot {
+	log := ctrl.LoggerFrom(ctx)
+	nodeList := &corev1.NodeList{}
+	matcher := client.MatchingLabels{}
+	for k, v := range c.NodeLabels {
+		matcher[k] = v
+	}
+	err := c.client.List(ctx, nodeList, matcher)
+	if err != nil {
+		log.Error(err, "failed to list nodes for TAS", "nodeLabels", c.NodeLabels)
+	}
+	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.NodeLabels,
+		"levels", c.Levels, "nodeCount", len(nodeList.Items))
+	return c.snapshotForNodes(nodeList.Items)
+}
+
+func (c *TASFlavorCache) snapshotForNodes(nodes []corev1.Node) *TASFlavorSnapshot {
+	c.RLock()
+	defer c.RUnlock()
+	snapshot := newTASFlavorSnapshot(c.Levels)
+	for _, node := range nodes {
+		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
+		capacity := resources.NewRequests(node.Status.Allocatable)
+		domainID := utiltas.DomainID(levelValues)
+		snapshot.levelValuesPerDomain[domainID] = levelValues
+		snapshot.addCapacity(domainID, capacity)
+	}
+	snapshot.initialize()
+	for domainID, usage := range c.usageMap {
+		snapshot.addUsage(domainID, usage)
+	}
+	return snapshot
+}
+
+func (c *TASFlavorCache) addUsage(topologyRequests []workload.TopologyDomainRequests) {
+	c.updateUsage(topologyRequests, 1)
+}
+
+func (c *TASFlavorCache) removeUsage(topologyRequests []workload.TopologyDomainRequests) {
+	c.updateUsage(topologyRequests, -1)
+}
+
+func (c *TASFlavorCache) updateUsage(topologyRequests []workload.TopologyDomainRequests, m int64) {
+	c.Lock()
+	defer c.Unlock()
+	for _, tr := range topologyRequests {
+		domainID := utiltas.DomainID(tr.Values)
+		_, found := c.usageMap[domainID]
+		if !found {
+			c.usageMap[domainID] = resources.Requests{}
+		}
+		if m < 0 {
+			c.usageMap[domainID].Sub(tr.Requests)
+		} else {
+			c.usageMap[domainID].Add(tr.Requests)
+		}
```

  done, for now I introduce the new type locally here, but I'm +1 to extend it in the follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T14:17:11Z

- **pkg/controller/tas/resource_flavor.go:163** by [@mimowo](https://github.com/mimowo) — 2024-10-21T14:17:11Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+			r.queues.Broadcast()
```

  the tests passed, resolving the comment

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T04:29:20Z

The whole of mechanism looks great to me.

- **pkg/cache/snapshot.go:80** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:04:28Z
```diff
@@ -76,6 +78,10 @@ func (s *Snapshot) Log(log logr.Logger) {
 }
 
 func (c *Cache) Snapshot() Snapshot {
```

  Let's keep opening this thread so that we can avoid cleanup this function in a follow-up.

- **pkg/cache/snapshot.go:103** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:09:11Z
```diff
@@ -94,15 +100,29 @@ func (c *Cache) Snapshot() Snapshot {
 			snap.UpdateCohortEdge(cohort.Name, cohort.Parent().Name)
 		}
 	}
+	tasSnapshotsMap := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
```

  ```suggestion
  	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
  ```
  
  It would be better to avoid including the typed name in the variable name.
  
  
  > For a slice, users is a better name than userSlice.
  
  https://google.github.io/styleguide/go/decisions#variable-names

- **pkg/cache/tas_cache.go:34** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:13:00Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client  client.Client
+	flvsMap map[kueue.ResourceFlavorReference]*TASFlavorCache
```

  ```suggestion
  	flvs map[kueue.ResourceFlavorReference]*TASFlavorCache
  ```

- **pkg/cache/tas_cache.go:49** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:13:11Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client  client.Client
+	flvsMap map[kueue.ResourceFlavorReference]*TASFlavorCache
+}
+
+func NewTASCache(client client.Client) TASCache {
+	return TASCache{
+		client:  client,
+		flvsMap: make(map[kueue.ResourceFlavorReference]*TASFlavorCache),
+	}
+}
+
+func (t *TASCache) NewFlavorCache(labels []string, nodeLabels map[string]string) *TASFlavorCache {
+	return &TASFlavorCache{
+		client:     t.client,
+		Levels:     slices.Clone(labels),
+		NodeLabels: maps.Clone(nodeLabels),
+		usageMap:   make(map[utiltas.TopologyDomainID]resources.Requests),
```

  ```suggestion
  		usages:   make(map[utiltas.TopologyDomainID]resources.Requests),
  ```

- **pkg/cache/tas_cache_test.go:698** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:17:15Z
```diff
@@ -0,0 +1,702 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x3",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x3",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x4",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x4",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r1-x5",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x5",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r2-x6",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x6",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("2"),
+					corev1.ResourceMemory: resource.MustParse("4Gi"),
+				},
+			},
+		},
+	}
+	defaultOneLevel := []string{
+		tasHostLabel,
+	}
+	defaultTwoLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+	defaultThreeLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+		tasHostLabel,
+	}
+
+	cases := map[string]struct {
+		request        kueue.PodSetTopologyRequest
+		levels         []string
+		nodeLabels     map[string]string
+		nodes          []corev1.Node
+		requests       resources.Requests
+		count          int32
+		wantAssignment *kueue.TopologyAssignment
+	}{
+		"minimize the number of used racks before optimizing the number of nodes": {
+			// Solution by optimizing the number of racks then nodes: [r3]: [x3,x4,x5,x6]
+			// Solution by optimizing the number of nodes: [r1,r2]: [x1,x2]
+			//
+			//       b1
+			//   /   |    \
+			//  r1   r2   r3
+			//  |     |    |   \   \     \
+			// x1:2,x2:2,x3:1,x4:1,x5:1,x6:1
+			//
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r1",
+							tasHostLabel:  "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r2-x2",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r2",
+							tasHostLabel:  "x2",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x3",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x3",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x4",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x4",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x5",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x5",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x6",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x6",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x3",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x4",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x5",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"host required; single Pod fits in the host": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b2",
+							"r2",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"rack required; single Pod fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; multiple Pods fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 3,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; too many pods to fit in any rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          4,
+			wantAssignment: nil,
+		},
+		"block required; single Pod fits in a block": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: []string{
+					tasBlockLabel,
+					tasRackLabel,
+				},
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"block required; Pods fit in a block spread across two racks": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block required; single Pod which cannot be split": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 4000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"block required; too many Pods to fit requested": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          5,
+			wantAssignment: nil,
+		},
+		"rack required; single Pod requiring memory": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceMemory: 1024,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 4,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only block can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but the workload cannot be accommodate in entire topology": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          10,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; no matching node": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-b",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; matching node is found": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-a",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultOneLevel,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"x1",
+						},
+					},
+				},
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+
+			initialObjects := make([]client.Object, 0)
+			for i := range tc.nodes {
+				initialObjects = append(initialObjects, &tc.nodes[i])
+			}
+			client := utiltesting.NewFakeClient(initialObjects...)
+			tasCache := NewTASCache(client)
+			tasFlavorCache := tasCache.NewFlavorCache(tc.levels, tc.nodeLabels)
+			snapshot := tasFlavorCache.snapshot(ctx)
+			gotAssignment := snapshot.FindTopologyAssignment(&tc.request, tc.requests, tc.count)
+			if diff := cmp.Diff(tc.wantAssignment, gotAssignment); diff != "" {
+				t.Errorf("unexpected topology assignment: %s", diff)
```

  ```suggestion
  				t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
  ```

- **pkg/cache/tas_cache_test.go:712** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:24:50Z
```diff
@@ -0,0 +1,702 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x3",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x3",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x4",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x4",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r1-x5",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x5",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r2-x6",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x6",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("2"),
+					corev1.ResourceMemory: resource.MustParse("4Gi"),
+				},
+			},
+		},
+	}
+	defaultOneLevel := []string{
+		tasHostLabel,
+	}
+	defaultTwoLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+	defaultThreeLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+		tasHostLabel,
+	}
+
+	cases := map[string]struct {
+		request        kueue.PodSetTopologyRequest
+		levels         []string
+		nodeLabels     map[string]string
+		nodes          []corev1.Node
+		requests       resources.Requests
+		count          int32
+		wantAssignment *kueue.TopologyAssignment
+	}{
+		"minimize the number of used racks before optimizing the number of nodes": {
+			// Solution by optimizing the number of racks then nodes: [r3]: [x3,x4,x5,x6]
+			// Solution by optimizing the number of nodes: [r1,r2]: [x1,x2]
+			//
+			//       b1
+			//   /   |    \
+			//  r1   r2   r3
+			//  |     |    |   \   \     \
+			// x1:2,x2:2,x3:1,x4:1,x5:1,x6:1
+			//
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r1",
+							tasHostLabel:  "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r2-x2",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r2",
+							tasHostLabel:  "x2",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x3",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x3",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x4",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x4",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x5",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x5",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x6",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x6",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x3",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x4",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x5",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"host required; single Pod fits in the host": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b2",
+							"r2",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"rack required; single Pod fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; multiple Pods fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 3,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; too many pods to fit in any rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          4,
+			wantAssignment: nil,
+		},
+		"block required; single Pod fits in a block": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: []string{
+					tasBlockLabel,
+					tasRackLabel,
+				},
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"block required; Pods fit in a block spread across two racks": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block required; single Pod which cannot be split": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 4000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"block required; too many Pods to fit requested": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          5,
+			wantAssignment: nil,
+		},
+		"rack required; single Pod requiring memory": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceMemory: 1024,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 4,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only block can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but the workload cannot be accommodate in entire topology": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          10,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; no matching node": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-b",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; matching node is found": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-a",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultOneLevel,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"x1",
+						},
+					},
+				},
+			},
+		},
+	}
```

  Can we specify preferred and required mixed requests?
  If so, let's add a case here.
  
  ```golang
  request: kueue.PodSetTopologyRequest{
  	Required: ptr.To(tasHostLabel),
  	Preferred: ptr.To(tasRaskLabel),
  },
  ```

- **pkg/cache/tas_flavor.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:25:04Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
```

  ```suggestion
  	usages map[utiltas.TopologyDomainID]resources.Requests
  ```

- **pkg/cache/tas_flavor.go:69** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:30:50Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
+}
+
+func (c *TASFlavorCache) snapshot(ctx context.Context) *TASFlavorSnapshot {
+	log := ctrl.LoggerFrom(ctx)
+	nodeList := &corev1.NodeList{}
+	matcher := client.MatchingLabels{}
+	for k, v := range c.NodeLabels {
+		matcher[k] = v
+	}
+	err := c.client.List(ctx, nodeList, matcher)
+	if err != nil {
+		log.Error(err, "failed to list nodes for TAS", "nodeLabels", c.NodeLabels)
+	}
+	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.NodeLabels,
+		"levels", c.Levels, "nodeCount", len(nodeList.Items))
```

  Could we move this log to the `snapshotForNodes` function since this logging accesses the cache without a Read lock?

- **pkg/cache/tas_flavor_snapshot.go:128** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:40:51Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
```

  As we discussed in the TAS controller implementation, could you use logging or just return error?

- **pkg/cache/tas_flavor_snapshot.go:146** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:41:38Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
```

  ```suggestion
  func (s *TASFlavorSnapshot) initializefreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
  ```
  
  Isn't this initialize the freeCapacityPerDomain instead of ensuring?

- **pkg/cache/tas_flavor_snapshot.go:202** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:52:19Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
```

  ```suggestion
  	if levelIdx == -1 {
  ```
  
  Let's specify more strictly.

- **pkg/cache/tas_flavor_snapshot.go:236** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:53:31Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
```

  ```suggestion
  		}
  		if levelIdx > 0 {
  			return s.findLevelWithFitNodes(levelIdx-1, required, count)
  		}
  ```
  
  This `else` does not have any value since we will return if the required is true.

- **pkg/cache/tas_flavor_snapshot.go:264** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:54:48Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedNodes[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedNodes)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedNodes[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedNodes[:lastIdx+1]
+	}
+	return levelIdx, []*topologyDomainNode{topNode}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(nodes []*topologyDomainNode, count int32) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0)
+	remainingCount := count
+	for i := 0; i < len(nodes); i++ {
+		node := nodes[i]
+		if s.state[node.id] >= remainingCount {
+			s.state[node.id] = remainingCount
+			result = append(result, node)
+			return result
+		} else if s.state[node.id] > 0 {
+			remainingCount -= s.state[node.id]
+			result = append(result, node)
+		}
```

  ```suggestion
  		}
  		if s.state[node.id] > 0 {
  			remainingCount -= s.state[node.id]
  			result = append(result, node)
  		}
  ```

- **pkg/cache/tas_flavor_snapshot.go:251** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:55:09Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedNodes[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedNodes)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedNodes[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedNodes[:lastIdx+1]
+	}
+	return levelIdx, []*topologyDomainNode{topNode}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(nodes []*topologyDomainNode, count int32) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0)
+	remainingCount := count
+	for i := 0; i < len(nodes); i++ {
+		node := nodes[i]
+		if s.state[node.id] >= remainingCount {
+			s.state[node.id] = remainingCount
+			result = append(result, node)
+			return result
+		} else if s.state[node.id] > 0 {
+			remainingCount -= s.state[node.id]
+			result = append(result, node)
+		}
+	}
+	panic("unexpected remaining count")
```

  Let's use logging or returning error instead of panic.

- **pkg/cache/tas_flavor_snapshot.go:287** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T03:56:09Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedNodes[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedNodes)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedNodes[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedNodes[:lastIdx+1]
+	}
+	return levelIdx, []*topologyDomainNode{topNode}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(nodes []*topologyDomainNode, count int32) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0)
+	remainingCount := count
+	for i := 0; i < len(nodes); i++ {
+		node := nodes[i]
+		if s.state[node.id] >= remainingCount {
+			s.state[node.id] = remainingCount
+			result = append(result, node)
+			return result
+		} else if s.state[node.id] > 0 {
+			remainingCount -= s.state[node.id]
+			result = append(result, node)
+		}
+	}
+	panic("unexpected remaining count")
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(nodes []*topologyDomainNode) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(nodes); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(nodes[i].id),
+			Count:  s.state[nodes[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelNodes(levelIdx int, infos []*topologyDomainNode) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0, len(infos))
+	for _, info := range infos {
+		for _, childDomainID := range info.childIDs {
+			if childDomain := s.nodePerLevel[levelIdx+1][childDomainID]; childDomain != nil {
+				result = append(result, childDomain)
+			}
+		}
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) nodesForLevel(levelIdx int) []*topologyDomainNode {
+	infosMap := s.nodePerLevel[levelIdx]
```

  ```suggestion
  	infos := s.nodePerLevel[levelIdx]
  ```
  
  It would be better to avoid including the typed name in the variable name.
  
  > For a slice, users is a better name than userSlice.
  
  https://google.github.io/styleguide/go/decisions#variable-names

- **pkg/controller/jobframework/tas.go:39** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:01:58Z
```diff
@@ -0,0 +1,40 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/utils/ptr"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+func PodSetTopologyRequest(template *corev1.PodTemplateSpec) *kueue.PodSetTopologyRequest {
+	requiredValue, requiredFound := template.Annotations[kueuealpha.PodSetRequiredTopologyAnnotation]
+	preferredValue, preferredFound := template.Annotations[kueuealpha.PodSetPreferredTopologyAnnotation]
+	if requiredFound {
+		return &kueue.PodSetTopologyRequest{
+			Required: ptr.To(requiredValue),
+		}
+	} else if preferredFound {
+		return &kueue.PodSetTopologyRequest{
+			Preferred: ptr.To(preferredValue),
+		}
+	}
```

  ```suggestion
  	} 
  	if preferredFound {
  		return &kueue.PodSetTopologyRequest{
  			Preferred: ptr.To(preferredValue),
  		}
  	}
  ```
  
  This `else` is redundant since if requiredFound is true, this function early return the result.

- **pkg/controller/tas/resource_flavor.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:02:53Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
```

  ```suggestion
  var _ predicate.Predicate = (*rfReconciler)(nil)
  
  ```

- **pkg/controller/tas/resource_flavor.go:208** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:12:26Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *rfReconciler) Create(event event.CreateEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		return rf.Spec.TopologyName != nil
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Delete(event event.DeleteEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		if rf.Spec.TopologyName != nil {
+			r.tasCache.Delete(kueue.ResourceFlavorReference(rf.Name))
+		}
+		return false
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Update(event event.UpdateEvent) bool {
+	oldRf, isOldRf := event.ObjectOld.(*kueue.ResourceFlavor)
+	newRf, isNewRf := event.ObjectNew.(*kueue.ResourceFlavor)
+	if isOldRf && isNewRf {
+		switch {
+		case ptr.Equal(oldRf.Spec.TopologyName, newRf.Spec.TopologyName):
+			return false
+		case oldRf.Spec.TopologyName == nil:
+			return true
+		default:
+			// topologyName was set so is changed or removed
+			r.tasCache.Delete(kueue.ResourceFlavorReference(*oldRf.Spec.TopologyName))
+			return newRf.Spec.TopologyName != nil
+		}
+	}
+	_, isOldNode := event.ObjectOld.(*corev1.Node)
+	_, isNewNode := event.ObjectNew.(*corev1.Node)
+	return isOldNode && isNewNode
```

  These look weird. Is there any way to enqueue the ResourceFlavor based on Node event in the eventHandler?

- **pkg/controller/jobs/jobset/jobset_controller.go:124** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:13:25Z
```diff
@@ -118,9 +118,10 @@ func (j *JobSet) PodSets() []kueue.PodSet {
 	podSets := make([]kueue.PodSet, len(j.Spec.ReplicatedJobs))
 	for index, replicatedJob := range j.Spec.ReplicatedJobs {
 		podSets[index] = kueue.PodSet{
-			Name:     replicatedJob.Name,
-			Template: *replicatedJob.Template.Spec.Template.DeepCopy(),
-			Count:    podsCount(&replicatedJob),
+			Name:            replicatedJob.Name,
+			Template:        *replicatedJob.Template.Spec.Template.DeepCopy(),
+			Count:           podsCount(&replicatedJob),
+			TopologyRequest: jobframework.PodSetTopologyRequest(&replicatedJob.Template.Spec.Template),
```

  What if JobSet has the ReplicatedJobs with multiple replica?
  
  https://github.com/kubernetes-sigs/kueue/tree/45018b42c062868249b37f3c049bdbbf2efdb022/keps/2724-topology-aware-schedling#story-2

- **pkg/resources/requests.go:50** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:15:47Z
```diff
@@ -37,6 +39,28 @@ func NewRequests(rl corev1.ResourceList) Requests {
 	return r
 }
 
+func (r Requests) Clone() Requests {
+	return maps.Clone(r)
+}
+
+func (r Requests) Divide(f int64) {
+	for k := range r {
+		r[k] /= f
+	}
+}
```

  Uhm, could we avoid the resource divide since I think that this operation has a risk based on the precision?

- **pkg/resources/requests.go:102** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:18:42Z
```diff
@@ -67,3 +91,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 		return *resource.NewQuantity(v, resource.DecimalSI)
 	}
 }
+
+func (req Requests) CountIn(capacity Requests) int32 {
+	var result *int32
+	for rName, rValue := range req {
+		capacity, found := capacity[rName]
+		if !found {
+			return 0
+		}
+		count := int32(capacity / rValue)
```

  What if those are not divisible?

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:56** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:20:37Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package flavorassigner
+
+import (
+	"errors"
+	"fmt"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func assignTopology(log logr.Logger,
+	psAssignment *PodSetAssignment,
+	cq *cache.ClusterQueueSnapshot,
+	psResources workload.PodSetResources,
+	podSet *kueue.PodSet) {
+	switch {
+	case psAssignment.Status.IsError():
+		log.Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
+	case len(cq.TASFlavors) == 0:
+		if psAssignment.Status == nil {
+			psAssignment.Status = &Status{}
+		}
+		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
+		psAssignment.Flavors = nil
+	default:
+		singlePodRequests := psResources.Requests.Clone()
+		singlePodRequests.Divide(int64(psResources.Count))
+		podCount := psAssignment.Count
+		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
+		if err != nil {
+			if psAssignment.Status == nil {
+				psAssignment.Status = &Status{}
+			}
+			psAssignment.Status.err = err
+			psAssignment.Flavors = nil
+			return
+		} else {
```

  ```suggestion
  ```
  
  I think this `else` can be removed.

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:23:06Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package flavorassigner
+
+import (
+	"errors"
+	"fmt"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func assignTopology(log logr.Logger,
+	psAssignment *PodSetAssignment,
+	cq *cache.ClusterQueueSnapshot,
+	psResources workload.PodSetResources,
+	podSet *kueue.PodSet) {
+	switch {
+	case psAssignment.Status.IsError():
+		log.Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
```

  ```suggestion
  		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
  ```

- **test/integration/tas/tas_job_test.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T04:27:28Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					gomega.Expect(util.DeleteObject(ctx, k8sClient, &node)).Should(gomega.Succeed())
```

  Why we can not use the "ExpectObjectToBeDeleted"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:19:54Z

- **pkg/cache/tas_cache_test.go:712** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:19:54Z
```diff
@@ -0,0 +1,702 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x3",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x3",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x4",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x4",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r1-x5",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x5",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r2-x6",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x6",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("2"),
+					corev1.ResourceMemory: resource.MustParse("4Gi"),
+				},
+			},
+		},
+	}
+	defaultOneLevel := []string{
+		tasHostLabel,
+	}
+	defaultTwoLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+	defaultThreeLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+		tasHostLabel,
+	}
+
+	cases := map[string]struct {
+		request        kueue.PodSetTopologyRequest
+		levels         []string
+		nodeLabels     map[string]string
+		nodes          []corev1.Node
+		requests       resources.Requests
+		count          int32
+		wantAssignment *kueue.TopologyAssignment
+	}{
+		"minimize the number of used racks before optimizing the number of nodes": {
+			// Solution by optimizing the number of racks then nodes: [r3]: [x3,x4,x5,x6]
+			// Solution by optimizing the number of nodes: [r1,r2]: [x1,x2]
+			//
+			//       b1
+			//   /   |    \
+			//  r1   r2   r3
+			//  |     |    |   \   \     \
+			// x1:2,x2:2,x3:1,x4:1,x5:1,x6:1
+			//
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r1",
+							tasHostLabel:  "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r2-x2",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r2",
+							tasHostLabel:  "x2",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x3",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x3",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x4",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x4",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x5",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x5",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x6",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x6",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x3",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x4",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x5",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"host required; single Pod fits in the host": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b2",
+							"r2",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"rack required; single Pod fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; multiple Pods fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 3,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; too many pods to fit in any rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          4,
+			wantAssignment: nil,
+		},
+		"block required; single Pod fits in a block": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: []string{
+					tasBlockLabel,
+					tasRackLabel,
+				},
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"block required; Pods fit in a block spread across two racks": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block required; single Pod which cannot be split": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 4000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"block required; too many Pods to fit requested": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          5,
+			wantAssignment: nil,
+		},
+		"rack required; single Pod requiring memory": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceMemory: 1024,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 4,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only block can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but the workload cannot be accommodate in entire topology": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          10,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; no matching node": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-b",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; matching node is found": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-a",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultOneLevel,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"x1",
+						},
+					},
+				},
+			},
+		},
+	}
```

  it is not supported in this PR. We should either validate against it, or support (and then add the test).
  In any case I suggest a follow up. WDYT?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T08:37:03Z

Some initial thoughts, more incoming

- **pkg/cache/tas_cache.go:44** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T14:10:36Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client  client.Client
+	flvsMap map[kueue.ResourceFlavorReference]*TASFlavorCache
+}
+
+func NewTASCache(client client.Client) TASCache {
+	return TASCache{
+		client:  client,
+		flvsMap: make(map[kueue.ResourceFlavorReference]*TASFlavorCache),
+	}
+}
+
+func (t *TASCache) NewFlavorCache(labels []string, nodeLabels map[string]string) *TASFlavorCache {
```

  Can we move this function to `tas_flavor.go` where the type is actually defined, and change the name to `NewTASFlavorCache`

- **pkg/cache/tas_flavor.go:76** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T14:35:19Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
+}
+
+func (c *TASFlavorCache) snapshot(ctx context.Context) *TASFlavorSnapshot {
+	log := ctrl.LoggerFrom(ctx)
+	nodeList := &corev1.NodeList{}
+	matcher := client.MatchingLabels{}
+	for k, v := range c.NodeLabels {
+		matcher[k] = v
+	}
+	err := c.client.List(ctx, nodeList, matcher)
```

  Is it possible to list only nodes that has every nodeLabel from `c.Levels`?

- **pkg/controller/tas/resource_flavor.go:213** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T14:40:34Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *rfReconciler) Create(event event.CreateEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		return rf.Spec.TopologyName != nil
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Delete(event event.DeleteEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		if rf.Spec.TopologyName != nil {
+			r.tasCache.Delete(kueue.ResourceFlavorReference(rf.Name))
+		}
+		return false
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Update(event event.UpdateEvent) bool {
+	oldRf, isOldRf := event.ObjectOld.(*kueue.ResourceFlavor)
+	newRf, isNewRf := event.ObjectNew.(*kueue.ResourceFlavor)
+	if isOldRf && isNewRf {
+		switch {
+		case ptr.Equal(oldRf.Spec.TopologyName, newRf.Spec.TopologyName):
+			return false
+		case oldRf.Spec.TopologyName == nil:
+			return true
+		default:
+			// topologyName was set so is changed or removed
+			r.tasCache.Delete(kueue.ResourceFlavorReference(*oldRf.Spec.TopologyName))
+			return newRf.Spec.TopologyName != nil
+		}
+	}
+	_, isOldNode := event.ObjectOld.(*corev1.Node)
+	_, isNewNode := event.ObjectNew.(*corev1.Node)
+	return isOldNode && isNewNode
+}
+
+func (r *rfReconciler) Generic(event event.GenericEvent) bool {
+	return false
+}
+
+func isFlavorAffectedByNode(node *corev1.Node, nodeLabels map[string]string, levels []string) bool {
+	for k, v := range nodeLabels {
+		if node.Labels[k] != v {
```

  Can we assume that the label will always exists?

- **pkg/controller/tas/resource_flavor.go:215** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T14:42:51Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *rfReconciler) Create(event event.CreateEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		return rf.Spec.TopologyName != nil
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Delete(event event.DeleteEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		if rf.Spec.TopologyName != nil {
+			r.tasCache.Delete(kueue.ResourceFlavorReference(rf.Name))
+		}
+		return false
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Update(event event.UpdateEvent) bool {
+	oldRf, isOldRf := event.ObjectOld.(*kueue.ResourceFlavor)
+	newRf, isNewRf := event.ObjectNew.(*kueue.ResourceFlavor)
+	if isOldRf && isNewRf {
+		switch {
+		case ptr.Equal(oldRf.Spec.TopologyName, newRf.Spec.TopologyName):
+			return false
+		case oldRf.Spec.TopologyName == nil:
+			return true
+		default:
+			// topologyName was set so is changed or removed
+			r.tasCache.Delete(kueue.ResourceFlavorReference(*oldRf.Spec.TopologyName))
+			return newRf.Spec.TopologyName != nil
+		}
+	}
+	_, isOldNode := event.ObjectOld.(*corev1.Node)
+	_, isNewNode := event.ObjectNew.(*corev1.Node)
+	return isOldNode && isNewNode
+}
+
+func (r *rfReconciler) Generic(event event.GenericEvent) bool {
+	return false
+}
+
+func isFlavorAffectedByNode(node *corev1.Node, nodeLabels map[string]string, levels []string) bool {
```

  Could we rename it something along the lines: `nodeBelongsToFlavor` or something  hat better express the functionality of this function?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:43:04Z

- **pkg/cache/snapshot.go:103** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:43:04Z
```diff
@@ -94,15 +100,29 @@ func (c *Cache) Snapshot() Snapshot {
 			snap.UpdateCohortEdge(cohort.Name, cohort.Parent().Name)
 		}
 	}
+	tasSnapshotsMap := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:43:11Z

- **pkg/cache/tas_cache.go:34** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:43:11Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client  client.Client
+	flvsMap map[kueue.ResourceFlavorReference]*TASFlavorCache
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:43:16Z

- **pkg/cache/tas_cache.go:49** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:43:16Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client  client.Client
+	flvsMap map[kueue.ResourceFlavorReference]*TASFlavorCache
+}
+
+func NewTASCache(client client.Client) TASCache {
+	return TASCache{
+		client:  client,
+		flvsMap: make(map[kueue.ResourceFlavorReference]*TASFlavorCache),
+	}
+}
+
+func (t *TASCache) NewFlavorCache(labels []string, nodeLabels map[string]string) *TASFlavorCache {
+	return &TASFlavorCache{
+		client:     t.client,
+		Levels:     slices.Clone(labels),
+		NodeLabels: maps.Clone(nodeLabels),
+		usageMap:   make(map[utiltas.TopologyDomainID]resources.Requests),
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:43:24Z

- **pkg/cache/tas_cache_test.go:698** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:43:24Z
```diff
@@ -0,0 +1,702 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x3",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x3",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x4",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x4",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r1-x5",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x5",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r2-x6",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x6",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("2"),
+					corev1.ResourceMemory: resource.MustParse("4Gi"),
+				},
+			},
+		},
+	}
+	defaultOneLevel := []string{
+		tasHostLabel,
+	}
+	defaultTwoLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+	defaultThreeLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+		tasHostLabel,
+	}
+
+	cases := map[string]struct {
+		request        kueue.PodSetTopologyRequest
+		levels         []string
+		nodeLabels     map[string]string
+		nodes          []corev1.Node
+		requests       resources.Requests
+		count          int32
+		wantAssignment *kueue.TopologyAssignment
+	}{
+		"minimize the number of used racks before optimizing the number of nodes": {
+			// Solution by optimizing the number of racks then nodes: [r3]: [x3,x4,x5,x6]
+			// Solution by optimizing the number of nodes: [r1,r2]: [x1,x2]
+			//
+			//       b1
+			//   /   |    \
+			//  r1   r2   r3
+			//  |     |    |   \   \     \
+			// x1:2,x2:2,x3:1,x4:1,x5:1,x6:1
+			//
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r1",
+							tasHostLabel:  "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r2-x2",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r2",
+							tasHostLabel:  "x2",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x3",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x3",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x4",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x4",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x5",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x5",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x6",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x6",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x3",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x4",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x5",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"host required; single Pod fits in the host": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b2",
+							"r2",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"rack required; single Pod fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; multiple Pods fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 3,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; too many pods to fit in any rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          4,
+			wantAssignment: nil,
+		},
+		"block required; single Pod fits in a block": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: []string{
+					tasBlockLabel,
+					tasRackLabel,
+				},
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"block required; Pods fit in a block spread across two racks": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block required; single Pod which cannot be split": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 4000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"block required; too many Pods to fit requested": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          5,
+			wantAssignment: nil,
+		},
+		"rack required; single Pod requiring memory": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceMemory: 1024,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 4,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only block can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but the workload cannot be accommodate in entire topology": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          10,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; no matching node": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-b",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; matching node is found": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-a",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultOneLevel,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"x1",
+						},
+					},
+				},
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+
+			initialObjects := make([]client.Object, 0)
+			for i := range tc.nodes {
+				initialObjects = append(initialObjects, &tc.nodes[i])
+			}
+			client := utiltesting.NewFakeClient(initialObjects...)
+			tasCache := NewTASCache(client)
+			tasFlavorCache := tasCache.NewFlavorCache(tc.levels, tc.nodeLabels)
+			snapshot := tasFlavorCache.snapshot(ctx)
+			gotAssignment := snapshot.FindTopologyAssignment(&tc.request, tc.requests, tc.count)
+			if diff := cmp.Diff(tc.wantAssignment, gotAssignment); diff != "" {
+				t.Errorf("unexpected topology assignment: %s", diff)
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:43:50Z

- **pkg/cache/tas_flavor.go:54** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:43:49Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:52:38Z

- **pkg/cache/tas_flavor.go:69** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:52:38Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
+}
+
+func (c *TASFlavorCache) snapshot(ctx context.Context) *TASFlavorSnapshot {
+	log := ctrl.LoggerFrom(ctx)
+	nodeList := &corev1.NodeList{}
+	matcher := client.MatchingLabels{}
+	for k, v := range c.NodeLabels {
+		matcher[k] = v
+	}
+	err := c.client.List(ctx, nodeList, matcher)
+	if err != nil {
+		log.Error(err, "failed to list nodes for TAS", "nodeLabels", c.NodeLabels)
+	}
+	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.NodeLabels,
+		"levels", c.Levels, "nodeCount", len(nodeList.Items))
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:54:29Z

- **pkg/cache/tas_flavor_snapshot.go:146** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:54:28Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:54:38Z

- **pkg/cache/tas_flavor_snapshot.go:202** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:54:38Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:54:49Z

- **pkg/cache/tas_flavor_snapshot.go:236** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:54:49Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:54:58Z

- **pkg/cache/tas_flavor_snapshot.go:264** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:54:57Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedNodes[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedNodes)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedNodes[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedNodes[:lastIdx+1]
+	}
+	return levelIdx, []*topologyDomainNode{topNode}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(nodes []*topologyDomainNode, count int32) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0)
+	remainingCount := count
+	for i := 0; i < len(nodes); i++ {
+		node := nodes[i]
+		if s.state[node.id] >= remainingCount {
+			s.state[node.id] = remainingCount
+			result = append(result, node)
+			return result
+		} else if s.state[node.id] > 0 {
+			remainingCount -= s.state[node.id]
+			result = append(result, node)
+		}
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:55:12Z

- **pkg/cache/tas_flavor_snapshot.go:287** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:55:12Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedNodes[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedNodes)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedNodes[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedNodes[:lastIdx+1]
+	}
+	return levelIdx, []*topologyDomainNode{topNode}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(nodes []*topologyDomainNode, count int32) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0)
+	remainingCount := count
+	for i := 0; i < len(nodes); i++ {
+		node := nodes[i]
+		if s.state[node.id] >= remainingCount {
+			s.state[node.id] = remainingCount
+			result = append(result, node)
+			return result
+		} else if s.state[node.id] > 0 {
+			remainingCount -= s.state[node.id]
+			result = append(result, node)
+		}
+	}
+	panic("unexpected remaining count")
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(nodes []*topologyDomainNode) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(nodes); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(nodes[i].id),
+			Count:  s.state[nodes[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelNodes(levelIdx int, infos []*topologyDomainNode) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0, len(infos))
+	for _, info := range infos {
+		for _, childDomainID := range info.childIDs {
+			if childDomain := s.nodePerLevel[levelIdx+1][childDomainID]; childDomain != nil {
+				result = append(result, childDomain)
+			}
+		}
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) nodesForLevel(levelIdx int) []*topologyDomainNode {
+	infosMap := s.nodePerLevel[levelIdx]
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:55:19Z

- **pkg/controller/jobframework/tas.go:39** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:55:19Z
```diff
@@ -0,0 +1,40 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/utils/ptr"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+func PodSetTopologyRequest(template *corev1.PodTemplateSpec) *kueue.PodSetTopologyRequest {
+	requiredValue, requiredFound := template.Annotations[kueuealpha.PodSetRequiredTopologyAnnotation]
+	preferredValue, preferredFound := template.Annotations[kueuealpha.PodSetPreferredTopologyAnnotation]
+	if requiredFound {
+		return &kueue.PodSetTopologyRequest{
+			Required: ptr.To(requiredValue),
+		}
+	} else if preferredFound {
+		return &kueue.PodSetTopologyRequest{
+			Preferred: ptr.To(preferredValue),
+		}
+	}
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T08:56:17Z

- **pkg/controller/tas/resource_flavor.go:58** by [@mimowo](https://github.com/mimowo) — 2024-10-22T08:56:17Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:00:02Z

- **pkg/controller/jobs/jobset/jobset_controller.go:124** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:00:02Z
```diff
@@ -118,9 +118,10 @@ func (j *JobSet) PodSets() []kueue.PodSet {
 	podSets := make([]kueue.PodSet, len(j.Spec.ReplicatedJobs))
 	for index, replicatedJob := range j.Spec.ReplicatedJobs {
 		podSets[index] = kueue.PodSet{
-			Name:     replicatedJob.Name,
-			Template: *replicatedJob.Template.Spec.Template.DeepCopy(),
-			Count:    podsCount(&replicatedJob),
+			Name:            replicatedJob.Name,
+			Template:        *replicatedJob.Template.Spec.Template.DeepCopy(),
+			Count:           podsCount(&replicatedJob),
+			TopologyRequest: jobframework.PodSetTopologyRequest(&replicatedJob.Template.Spec.Template),
```

  Then the entire PodSet (all instances of repliacted Jobs will be placed as a unit). So, assume you have a JobSet with ReplicatedJob, replicas=10, each Job has 5 pods. Then, the PodSet has 50 pods. They will all be scheduled by TAS as a single unit. So, for example if the user specifies required: block. All of them will be in one block, but some Jobs may have pods scattered across racks. 
  
  This can be improved, but it is not planned for the first Alpha, maybe the second iteration of Alpha or Beta. FYI: Some initial support was designed here: https://github.com/kubernetes-sigs/kueue/tree/45018b42c062868249b37f3c049bdbbf2efdb022/keps/2724-topology-aware-schedling#support-for-replicatedjobs-in-jobset.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:01:47Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:56** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:01:47Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package flavorassigner
+
+import (
+	"errors"
+	"fmt"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func assignTopology(log logr.Logger,
+	psAssignment *PodSetAssignment,
+	cq *cache.ClusterQueueSnapshot,
+	psResources workload.PodSetResources,
+	podSet *kueue.PodSet) {
+	switch {
+	case psAssignment.Status.IsError():
+		log.Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
+	case len(cq.TASFlavors) == 0:
+		if psAssignment.Status == nil {
+			psAssignment.Status = &Status{}
+		}
+		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
+		psAssignment.Flavors = nil
+	default:
+		singlePodRequests := psResources.Requests.Clone()
+		singlePodRequests.Divide(int64(psResources.Count))
+		podCount := psAssignment.Count
+		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
+		if err != nil {
+			if psAssignment.Status == nil {
+				psAssignment.Status = &Status{}
+			}
+			psAssignment.Status.err = err
+			psAssignment.Flavors = nil
+			return
+		} else {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:01:56Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:37** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:01:56Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package flavorassigner
+
+import (
+	"errors"
+	"fmt"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func assignTopology(log logr.Logger,
+	psAssignment *PodSetAssignment,
+	cq *cache.ClusterQueueSnapshot,
+	psResources workload.PodSetResources,
+	podSet *kueue.PodSet) {
+	switch {
+	case psAssignment.Status.IsError():
+		log.Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:02:03Z

- **test/integration/tas/tas_job_test.go:176** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:02:03Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					gomega.Expect(util.DeleteObject(ctx, k8sClient, &node)).Should(gomega.Succeed())
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:05:16Z

- **pkg/cache/tas_cache.go:44** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:05:16Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"maps"
+	"slices"
+	"sync"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+type TASCache struct {
+	sync.RWMutex
+	client  client.Client
+	flvsMap map[kueue.ResourceFlavorReference]*TASFlavorCache
+}
+
+func NewTASCache(client client.Client) TASCache {
+	return TASCache{
+		client:  client,
+		flvsMap: make(map[kueue.ResourceFlavorReference]*TASFlavorCache),
+	}
+}
+
+func (t *TASCache) NewFlavorCache(labels []string, nodeLabels map[string]string) *TASFlavorCache {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:06:44Z

- **pkg/controller/tas/resource_flavor.go:215** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:06:43Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *rfReconciler) Create(event event.CreateEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		return rf.Spec.TopologyName != nil
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Delete(event event.DeleteEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		if rf.Spec.TopologyName != nil {
+			r.tasCache.Delete(kueue.ResourceFlavorReference(rf.Name))
+		}
+		return false
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Update(event event.UpdateEvent) bool {
+	oldRf, isOldRf := event.ObjectOld.(*kueue.ResourceFlavor)
+	newRf, isNewRf := event.ObjectNew.(*kueue.ResourceFlavor)
+	if isOldRf && isNewRf {
+		switch {
+		case ptr.Equal(oldRf.Spec.TopologyName, newRf.Spec.TopologyName):
+			return false
+		case oldRf.Spec.TopologyName == nil:
+			return true
+		default:
+			// topologyName was set so is changed or removed
+			r.tasCache.Delete(kueue.ResourceFlavorReference(*oldRf.Spec.TopologyName))
+			return newRf.Spec.TopologyName != nil
+		}
+	}
+	_, isOldNode := event.ObjectOld.(*corev1.Node)
+	_, isNewNode := event.ObjectNew.(*corev1.Node)
+	return isOldNode && isNewNode
+}
+
+func (r *rfReconciler) Generic(event event.GenericEvent) bool {
+	return false
+}
+
+func isFlavorAffectedByNode(node *corev1.Node, nodeLabels map[string]string, levels []string) bool {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:10:26Z

- **pkg/resources/requests.go:50** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:10:26Z
```diff
@@ -37,6 +39,28 @@ func NewRequests(rl corev1.ResourceList) Requests {
 	return r
 }
 
+func (r Requests) Clone() Requests {
+	return maps.Clone(r)
+}
+
+func (r Requests) Divide(f int64) {
+	for k := range r {
+		r[k] /= f
+	}
+}
```

  They should be divisible because we use it in the context where the psResources were computed by multiplying before (to scale single pod requests to total requests). I think we could avoid division all together if we kept the psResources [here](https://github.com/kubernetes-sigs/kueue/blob/9b45a1f202371949887f8a06cbd53cc0dae4cf67/pkg/workload/workload.go#L161-L170) per pod rather than total. However, this is a bigger change, I'm happy to try to clean it up in follow up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:15:50Z

- **pkg/resources/requests.go:102** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:15:50Z
```diff
@@ -67,3 +91,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 		return *resource.NewQuantity(v, resource.DecimalSI)
 	}
 }
+
+func (req Requests) CountIn(capacity Requests) int32 {
+	var result *int32
+	for rName, rValue := range req {
+		capacity, found := capacity[rName]
+		if !found {
+			return 0
+		}
+		count := int32(capacity / rValue)
```

  Then we will not be able to fit another full pod, which is what we wont - this function is used to count the number of pods which can fully fit into the free capacity. 
  
  Added a test case for it: https://github.com/kubernetes-sigs/kueue/pull/3256/files#diff-f07b44933bee01621ef7cc05b4a8c30a8962963044b725b600a2dee63846b727R74-R82

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:19:25Z

- **pkg/controller/tas/resource_flavor.go:208** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:19:25Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *rfReconciler) Create(event event.CreateEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		return rf.Spec.TopologyName != nil
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Delete(event event.DeleteEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		if rf.Spec.TopologyName != nil {
+			r.tasCache.Delete(kueue.ResourceFlavorReference(rf.Name))
+		}
+		return false
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Update(event event.UpdateEvent) bool {
+	oldRf, isOldRf := event.ObjectOld.(*kueue.ResourceFlavor)
+	newRf, isNewRf := event.ObjectNew.(*kueue.ResourceFlavor)
+	if isOldRf && isNewRf {
+		switch {
+		case ptr.Equal(oldRf.Spec.TopologyName, newRf.Spec.TopologyName):
+			return false
+		case oldRf.Spec.TopologyName == nil:
+			return true
+		default:
+			// topologyName was set so is changed or removed
+			r.tasCache.Delete(kueue.ResourceFlavorReference(*oldRf.Spec.TopologyName))
+			return newRf.Spec.TopologyName != nil
+		}
+	}
+	_, isOldNode := event.ObjectOld.(*corev1.Node)
+	_, isNewNode := event.ObjectNew.(*corev1.Node)
+	return isOldNode && isNewNode
```

  Yes, we only watch RF and Nodes, we check RFs in the if, so it is enough to just respond true otherwise

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:30:09Z

- **pkg/cache/tas_flavor_snapshot.go:251** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:30:09Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
+			len(levelValues), levelIdx, domainID, s.levelValuesPerDomain, s.freeCapacityPerDomain))
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.ensureDomainExists(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) ensureDomainExists(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the nodes which can fit the
+	fitLevelIdx, currFitNodes := s.findLevelWithFitNodes(levelIdx, required, count)
+	if len(currFitNodes) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitNodes = s.updateCountsToMinimum(currFitNodes, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.nodePerLevel); levelIdx++ {
+		lowerFitNodes := s.lowerLevelNodes(levelIdx, currFitNodes)
+		sortedLowerNodes := s.sortedNodes(lowerFitNodes)
+		currFitNodes = s.updateCountsToMinimum(sortedLowerNodes, count)
+	}
+	return s.buildAssignment(currFitNodes)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx < 0 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitNodes(levelIdx int, required bool, count int32) (int, []*topologyDomainNode) {
+	levelNodes := s.nodesForLevel(levelIdx)
+	if len(levelNodes) == 0 {
+		return 0, nil
+	}
+	sortedNodes := s.sortedNodes(levelNodes)
+	topNode := sortedNodes[0]
+	if s.state[topNode.id] < count {
+		if required {
+			return 0, nil
+		} else if levelIdx > 0 {
+			return s.findLevelWithFitNodes(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedNodes[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedNodes)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedNodes[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedNodes[:lastIdx+1]
+	}
+	return levelIdx, []*topologyDomainNode{topNode}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(nodes []*topologyDomainNode, count int32) []*topologyDomainNode {
+	result := make([]*topologyDomainNode, 0)
+	remainingCount := count
+	for i := 0; i < len(nodes); i++ {
+		node := nodes[i]
+		if s.state[node.id] >= remainingCount {
+			s.state[node.id] = remainingCount
+			result = append(result, node)
+			return result
+		} else if s.state[node.id] > 0 {
+			remainingCount -= s.state[node.id]
+			result = append(result, node)
+		}
+	}
+	panic("unexpected remaining count")
```

  done, I chose logging because this error should never happen, unless there is a bug

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T09:30:51Z

- **pkg/cache/tas_flavor_snapshot.go:128** by [@mimowo](https://github.com/mimowo) — 2024-10-22T09:30:51Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
```

  I'm choosing to log error because this error shouldn't happen unless there is a bug, so no need to handle the error

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T10:12:43Z

- **pkg/cache/tas_cache_test.go:712** by [@mimowo](https://github.com/mimowo) — 2024-10-22T10:12:43Z
```diff
@@ -0,0 +1,702 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x3",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x3",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x4",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x4",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r1-x5",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x5",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r2-x6",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x6",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("2"),
+					corev1.ResourceMemory: resource.MustParse("4Gi"),
+				},
+			},
+		},
+	}
+	defaultOneLevel := []string{
+		tasHostLabel,
+	}
+	defaultTwoLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+	defaultThreeLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+		tasHostLabel,
+	}
+
+	cases := map[string]struct {
+		request        kueue.PodSetTopologyRequest
+		levels         []string
+		nodeLabels     map[string]string
+		nodes          []corev1.Node
+		requests       resources.Requests
+		count          int32
+		wantAssignment *kueue.TopologyAssignment
+	}{
+		"minimize the number of used racks before optimizing the number of nodes": {
+			// Solution by optimizing the number of racks then nodes: [r3]: [x3,x4,x5,x6]
+			// Solution by optimizing the number of nodes: [r1,r2]: [x1,x2]
+			//
+			//       b1
+			//   /   |    \
+			//  r1   r2   r3
+			//  |     |    |   \   \     \
+			// x1:2,x2:2,x3:1,x4:1,x5:1,x6:1
+			//
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r1",
+							tasHostLabel:  "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r2-x2",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r2",
+							tasHostLabel:  "x2",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x3",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x3",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x4",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x4",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x5",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x5",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x6",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x6",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x3",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x4",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x5",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"host required; single Pod fits in the host": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b2",
+							"r2",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"rack required; single Pod fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; multiple Pods fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 3,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; too many pods to fit in any rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          4,
+			wantAssignment: nil,
+		},
+		"block required; single Pod fits in a block": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: []string{
+					tasBlockLabel,
+					tasRackLabel,
+				},
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"block required; Pods fit in a block spread across two racks": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block required; single Pod which cannot be split": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 4000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"block required; too many Pods to fit requested": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          5,
+			wantAssignment: nil,
+		},
+		"rack required; single Pod requiring memory": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceMemory: 1024,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 4,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only block can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but the workload cannot be accommodate in entire topology": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          10,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; no matching node": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-b",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; matching node is found": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-a",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultOneLevel,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"x1",
+						},
+					},
+				},
+			},
+		},
+	}
```

  I have added the point to the [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU/edit?resourcekey=0-gr1ML2A1Axi8s6Lxr-Zhlw&gid=0#gid=0)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T11:04:42Z

- **pkg/cache/tas_flavor.go:76** by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:04:42Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
+)
+
+type TASFlavorCache struct {
+	sync.RWMutex
+
+	client client.Client
+
+	// nodeLabels is a map of nodeLabels defined in the ResourceFlavor object.
+	NodeLabels map[string]string
+	// levels is a list of levels defined in the Topology object referenced
+	// by the flavor corresponding to the cache.
+	Levels []string
+
+	// usageMap maintains the usage per topology domain
+	usageMap map[utiltas.TopologyDomainID]resources.Requests
+}
+
+func (c *TASFlavorCache) snapshot(ctx context.Context) *TASFlavorSnapshot {
+	log := ctrl.LoggerFrom(ctx)
+	nodeList := &corev1.NodeList{}
+	matcher := client.MatchingLabels{}
+	for k, v := range c.NodeLabels {
+		matcher[k] = v
+	}
+	err := c.client.List(ctx, nodeList, matcher)
```

  You found a bug :) We need to additionally filter out the nodes which don't have label keys in `c.levels`.
  
  Fixed, and added a test case which fails without the change.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T11:09:14Z

- **pkg/controller/tas/resource_flavor.go:213** by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:09:14Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+const (
+	nodeBatchPeriod = time.Second
+)
+
+type rfReconciler struct {
+	queues   *queue.Manager
+	cache    *cache.Cache
+	tasCache *cache.TASCache
+	client   client.Client
+	recorder record.EventRecorder
+}
+
+var _ reconcile.Reconciler = (*rfReconciler)(nil)
+
+// +kubebuilder:rbac:groups="",resources=nodes,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=topologies,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+func newRfReconciler(c client.Client, queues *queue.Manager, cache *cache.Cache, recorder record.EventRecorder) *rfReconciler {
+	return &rfReconciler{
+		client:   c,
+		queues:   queues,
+		cache:    cache,
+		tasCache: cache.TASCache(),
+		recorder: recorder,
+	}
+}
+
+func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *cache.Cache, cfg *configapi.Configuration) (string, error) {
+	nodeHandler := nodeHandler{
+		tasCache: cache.TASCache(),
+	}
+	return TASResourceFlavorController, ctrl.NewControllerManagedBy(mgr).
+		Named(TASResourceFlavorController).
+		For(&kueue.ResourceFlavor{}).
+		Watches(&corev1.Node{}, &nodeHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*nodeHandler)(nil)
+
+// nodeHandler handles node update events.
+type nodeHandler struct {
+	tasCache *cache.TASCache
+}
+
+func (h *nodeHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldNode, isOldNode := e.ObjectOld.(*corev1.Node)
+	newNode, isNewNode := e.ObjectNew.(*corev1.Node)
+	if !isOldNode || !isNewNode {
+		return
+	}
+	h.queueReconcileForNode(oldNode, q)
+	h.queueReconcileForNode(newNode, q)
+}
+
+func (h *nodeHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	node, isNode := e.Object.(*corev1.Node)
+	if !isNode {
+		return
+	}
+	h.queueReconcileForNode(node, q)
+}
+
+func (h *nodeHandler) queueReconcileForNode(node *corev1.Node, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if node == nil {
+		return
+	}
+	// trigger reconcile for TAS flavors affected by the node being created or updated
+	for name, flavor := range h.tasCache.Clone() {
+		if isFlavorAffectedByNode(node, flavor.NodeLabels, flavor.Levels) {
+			q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+				Name: string(name),
+			}}, nodeBatchPeriod)
+		}
+	}
+}
+
+func (h *nodeHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("name", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile TAS Resource Flavor")
+
+	flv := &kueue.ResourceFlavor{}
+	if err := r.client.Get(ctx, req.NamespacedName, flv); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
+	}
+	if flv.Spec.TopologyName != nil {
+		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
+			topology := kueuealpha.Topology{}
+			if err := r.client.Get(ctx, types.NamespacedName{
+				Name: *flv.Spec.TopologyName,
+			}, &topology); err != nil {
+				return reconcile.Result{}, err
+			}
+			levels := r.levels(&topology)
+			tasInfo := r.tasCache.NewFlavorCache(levels, flv.Spec.NodeLabels)
+			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		}
+
+		// requeue inadmissible workloads as a change to the resource flavor
+		// or the set of nodes can allow admitting a workload which was
+		// previously inadmissible.
+		if cqNames := r.cache.ActiveClusterQueues(); len(cqNames) > 0 {
+			r.queues.QueueInadmissibleWorkloads(ctx, cqNames)
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *rfReconciler) Create(event event.CreateEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		return rf.Spec.TopologyName != nil
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Delete(event event.DeleteEvent) bool {
+	rf, isRf := event.Object.(*kueue.ResourceFlavor)
+	if isRf {
+		if rf.Spec.TopologyName != nil {
+			r.tasCache.Delete(kueue.ResourceFlavorReference(rf.Name))
+		}
+		return false
+	}
+	_, isNode := event.Object.(*corev1.Node)
+	return isNode
+}
+
+func (r *rfReconciler) Update(event event.UpdateEvent) bool {
+	oldRf, isOldRf := event.ObjectOld.(*kueue.ResourceFlavor)
+	newRf, isNewRf := event.ObjectNew.(*kueue.ResourceFlavor)
+	if isOldRf && isNewRf {
+		switch {
+		case ptr.Equal(oldRf.Spec.TopologyName, newRf.Spec.TopologyName):
+			return false
+		case oldRf.Spec.TopologyName == nil:
+			return true
+		default:
+			// topologyName was set so is changed or removed
+			r.tasCache.Delete(kueue.ResourceFlavorReference(*oldRf.Spec.TopologyName))
+			return newRf.Spec.TopologyName != nil
+		}
+	}
+	_, isOldNode := event.ObjectOld.(*corev1.Node)
+	_, isNewNode := event.ObjectNew.(*corev1.Node)
+	return isOldNode && isNewNode
+}
+
+func (r *rfReconciler) Generic(event event.GenericEvent) bool {
+	return false
+}
+
+func isFlavorAffectedByNode(node *corev1.Node, nodeLabels map[string]string, levels []string) bool {
+	for k, v := range nodeLabels {
+		if node.Labels[k] != v {
```

  No, this function `isFlavorAffectedByNode` (`nodeBelongsToFlavor` after rename) is invoked for any node in the system. We need to take into account that some nodes don't have the necessary labeles (for example the cloud provider does not support them on node-pools which co-exist with new node pools), or the nodes are labeled by a script in an on-premise environment.
  
  However, the code in question does not assume that, if the label is absent then `node.Labels[k]` returns "", and so it is different than "v" - and so we return false as expected.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T11:18:56Z

- **pkg/cache/snapshot.go:80** by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:18:56Z
```diff
@@ -76,6 +78,10 @@ func (s *Snapshot) Log(log logr.Logger) {
 }
 
 func (c *Cache) Snapshot() Snapshot {
```

  I cleaned up the Snapshot function in this PR, last commit: https://github.com/kubernetes-sigs/kueue/pull/3256/commits/cd977aa71fe9063052f5cf646e342dcc64025a19

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T11:24:48Z

- **pkg/cache/tas_flavor_snapshot.go:37** by [@mimowo](https://github.com/mimowo) — 2024-10-22T11:24:48Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
```

  FYI: I have renamed it once again. I like just `domain` because it is specific in this context (contrary to previous: `info` or `node`), yet short (compared to previous `topologyDomainNode`)
  cc @gabesaba

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T13:01:02Z

- **pkg/cache/tas_cache_test.go:712** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:01:02Z
```diff
@@ -0,0 +1,702 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x3",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x3",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x4",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x4",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r1-x5",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x5",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r2-x6",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x6",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("2"),
+					corev1.ResourceMemory: resource.MustParse("4Gi"),
+				},
+			},
+		},
+	}
+	defaultOneLevel := []string{
+		tasHostLabel,
+	}
+	defaultTwoLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+	defaultThreeLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+		tasHostLabel,
+	}
+
+	cases := map[string]struct {
+		request        kueue.PodSetTopologyRequest
+		levels         []string
+		nodeLabels     map[string]string
+		nodes          []corev1.Node
+		requests       resources.Requests
+		count          int32
+		wantAssignment *kueue.TopologyAssignment
+	}{
+		"minimize the number of used racks before optimizing the number of nodes": {
+			// Solution by optimizing the number of racks then nodes: [r3]: [x3,x4,x5,x6]
+			// Solution by optimizing the number of nodes: [r1,r2]: [x1,x2]
+			//
+			//       b1
+			//   /   |    \
+			//  r1   r2   r3
+			//  |     |    |   \   \     \
+			// x1:2,x2:2,x3:1,x4:1,x5:1,x6:1
+			//
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r1",
+							tasHostLabel:  "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r2-x2",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r2",
+							tasHostLabel:  "x2",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x3",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x3",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x4",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x4",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x5",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x5",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x6",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x6",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x3",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x4",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x5",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"host required; single Pod fits in the host": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b2",
+							"r2",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"rack required; single Pod fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; multiple Pods fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 3,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; too many pods to fit in any rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          4,
+			wantAssignment: nil,
+		},
+		"block required; single Pod fits in a block": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: []string{
+					tasBlockLabel,
+					tasRackLabel,
+				},
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"block required; Pods fit in a block spread across two racks": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block required; single Pod which cannot be split": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 4000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"block required; too many Pods to fit requested": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          5,
+			wantAssignment: nil,
+		},
+		"rack required; single Pod requiring memory": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceMemory: 1024,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 4,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only block can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but the workload cannot be accommodate in entire topology": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          10,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; no matching node": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-b",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; matching node is found": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-a",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultOneLevel,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"x1",
+						},
+					},
+				},
+			},
+		},
+	}
```

  Thanks, I do not offer you to support both required and preferred field for now.
  But, at least, we should verify in the validation if both fields are not specified together before 0.9 so that we can avoid internal errors.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T13:01:39Z

- **pkg/cache/tas_flavor_snapshot.go:128** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:01:39Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"fmt"
+	"slices"
+	"strings"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+// topologyDomainNode holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type topologyDomainNode struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type nodePerDomain map[utiltas.TopologyDomainID]*topologyDomainNode
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// nodePerLevel stores the static tree information
+	nodePerLevel []nodePerDomain
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		nodePerLevel:          make([]nodePerDomain, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the nodePerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.nodePerLevel[levelIdx] = make(nodePerDomain)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childNode := &topologyDomainNode{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.nodePerLevel[lastLevelIdx][childID] = childNode
+		parentFound := false
+		var parent *topologyDomainNode
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.nodePerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &topologyDomainNode{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.nodePerLevel[levelIdx][parentID] = parent
+			}
+			childNode.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		panic(fmt.Sprintf("levelValues count=%d, levelIdx=%d, domainID=%v, levelValuesPerDomain=%v, freeCapacityPerDomain=%v",
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T13:08:30Z

- **pkg/controller/jobs/jobset/jobset_controller.go:124** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:08:30Z
```diff
@@ -118,9 +118,10 @@ func (j *JobSet) PodSets() []kueue.PodSet {
 	podSets := make([]kueue.PodSet, len(j.Spec.ReplicatedJobs))
 	for index, replicatedJob := range j.Spec.ReplicatedJobs {
 		podSets[index] = kueue.PodSet{
-			Name:     replicatedJob.Name,
-			Template: *replicatedJob.Template.Spec.Template.DeepCopy(),
-			Count:    podsCount(&replicatedJob),
+			Name:            replicatedJob.Name,
+			Template:        *replicatedJob.Template.Spec.Template.DeepCopy(),
+			Count:           podsCount(&replicatedJob),
+			TopologyRequest: jobframework.PodSetTopologyRequest(&replicatedJob.Template.Spec.Template),
```

  Thank you for clarifying this behavior! 
  So IIUC, in the second iteration (second Alpha or Beta), we allow users to specify more fine-grained topology assignments in the JobSet integration, right?
  
  In the first iteration, the topology assignments are not slightly flexible for the JobSet ReplicatedJobs with multiple replicas.
  
  But, I agree with leaving this improvement on the next iteration.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T13:11:15Z

- **pkg/resources/requests.go:50** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:11:14Z
```diff
@@ -37,6 +39,28 @@ func NewRequests(rl corev1.ResourceList) Requests {
 	return r
 }
 
+func (r Requests) Clone() Requests {
+	return maps.Clone(r)
+}
+
+func (r Requests) Divide(f int64) {
+	for k := range r {
+		r[k] /= f
+	}
+}
```

  Thank you for describing this behavior. Alright, let us tackle in a follow-up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T13:13:25Z

- **pkg/resources/requests.go:102** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:13:25Z
```diff
@@ -67,3 +91,18 @@ func ResourceQuantity(name corev1.ResourceName, v int64) resource.Quantity {
 		return *resource.NewQuantity(v, resource.DecimalSI)
 	}
 }
+
+func (req Requests) CountIn(capacity Requests) int32 {
+	var result *int32
+	for rName, rValue := range req {
+		capacity, found := capacity[rName]
+		if !found {
+			return 0
+		}
+		count := int32(capacity / rValue)
```

  That sounds reasonable. Thanks for pointing out the unit testing case!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T13:21:18Z

Thank you for this great implementation!
This is an awesome feature! Thank you!

Note that I leave some trivial comments. Let's address those in a follow-up.

/lgtm
/approve

- **pkg/cache/tas_flavor.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:17:57Z
```diff
@@ -0,0 +1,129 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"maps"
+	"slices"
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
```

  I'm wondering if we can turn this into an iota.

- **test/integration/tas/tas_job_test.go:439** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:19:28Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			})
+
+			ginkgo.It("should admit workload when nodes become available", func() {
+				var (
+					nodes []corev1.Node
+					wl1   *kueue.Workload
+				)
+
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+
+				ginkgo.By("Create nodes to allow scheduling", func() {
+					nodes = []corev1.Node{
+						{
+							ObjectMeta: metav1.ObjectMeta{
+								Name: "b1-r1",
+								Labels: map[string]string{
+									tasBlockLabel: "b1",
+									tasRackLabel:  "r1",
+								},
+							},
+							Status: corev1.NodeStatus{
+								Allocatable: corev1.ResourceList{
+									corev1.ResourceCPU:    resource.MustParse("1"),
+									corev1.ResourceMemory: resource.MustParse("1Gi"),
+								},
+							},
+						},
+					}
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+				})
+
+				ginkgo.By("Delete the node for cleanup", func() {
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Delete(ctx, &node)).Should(gomega.Succeed())
```

  Introducing "util.ExpectObjectToBeDeleted".

- **test/integration/tas/tas_job_test.go:536** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:19:45Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			})
+
+			ginkgo.It("should admit workload when nodes become available", func() {
+				var (
+					nodes []corev1.Node
+					wl1   *kueue.Workload
+				)
+
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+
+				ginkgo.By("Create nodes to allow scheduling", func() {
+					nodes = []corev1.Node{
+						{
+							ObjectMeta: metav1.ObjectMeta{
+								Name: "b1-r1",
+								Labels: map[string]string{
+									tasBlockLabel: "b1",
+									tasRackLabel:  "r1",
+								},
+							},
+							Status: corev1.NodeStatus{
+								Allocatable: corev1.ResourceList{
+									corev1.ResourceCPU:    resource.MustParse("1"),
+									corev1.ResourceMemory: resource.MustParse("1Gi"),
+								},
+							},
+						},
+					}
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+				})
+
+				ginkgo.By("Delete the node for cleanup", func() {
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Delete(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+			})
+		})
+	})
+
+	ginkgo.When("Multiple TAS Resource Flavors in queue", func() {
+		var (
+			tasGPUFlavor *kueue.ResourceFlavor
+			tasCPUFlavor *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			const (
+				gpuResName = "example.com/gpu"
+			)
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "cpu-node",
+							Labels: map[string]string{
+								"node.kubernetes.io/instance-type": "cpu-node",
+								tasRackLabel:                       "cpu-rack",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("5"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "gpu-node",
+							Labels: map[string]string{
+								"node.kubernetes.io/instance-type": "gpu-node",
+								tasRackLabel:                       "gpu-rack",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								gpuResName: resource.MustParse("4"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasGPUFlavor = testing.MakeResourceFlavor("tas-gpu-flavor").
+					NodeLabel("node.kubernetes.io/instance-type", "gpu-node").
+					TopologyName("default").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasGPUFlavor)).Should(gomega.Succeed())
+
+				tasCPUFlavor = testing.MakeResourceFlavor("tas-cpu-flavor").
+					NodeLabel("node.kubernetes.io/instance-type", "cpu-node").
+					TopologyName("default").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasCPUFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*testing.MakeFlavorQuotas(tasGPUFlavor.Name).Resource(corev1.ResourceCPU, "1").Resource(gpuResName, "5").Obj(),
+						*testing.MakeFlavorQuotas(tasCPUFlavor.Name).Resource(corev1.ResourceCPU, "5").Resource(gpuResName, "0").Obj(),
+					).Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasCPUFlavor, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasGPUFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
```

  ditto.
  
  "util.ExpectObjectToBeDeleted"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-22T13:25:22Z

- **pkg/cache/tas_cache_test.go:712** by [@mimowo](https://github.com/mimowo) — 2024-10-22T13:25:21Z
```diff
@@ -0,0 +1,702 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x3",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x3",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x4",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x4",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r1-x5",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x5",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b2-r2-x6",
+				Labels: map[string]string{
+					tasBlockLabel: "b2",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x6",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("2"),
+					corev1.ResourceMemory: resource.MustParse("4Gi"),
+				},
+			},
+		},
+	}
+	defaultOneLevel := []string{
+		tasHostLabel,
+	}
+	defaultTwoLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+	defaultThreeLevels := []string{
+		tasBlockLabel,
+		tasRackLabel,
+		tasHostLabel,
+	}
+
+	cases := map[string]struct {
+		request        kueue.PodSetTopologyRequest
+		levels         []string
+		nodeLabels     map[string]string
+		nodes          []corev1.Node
+		requests       resources.Requests
+		count          int32
+		wantAssignment *kueue.TopologyAssignment
+	}{
+		"minimize the number of used racks before optimizing the number of nodes": {
+			// Solution by optimizing the number of racks then nodes: [r3]: [x3,x4,x5,x6]
+			// Solution by optimizing the number of nodes: [r1,r2]: [x1,x2]
+			//
+			//       b1
+			//   /   |    \
+			//  r1   r2   r3
+			//  |     |    |   \   \     \
+			// x1:2,x2:2,x3:1,x4:1,x5:1,x6:1
+			//
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r1",
+							tasHostLabel:  "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r2-x2",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r2",
+							tasHostLabel:  "x2",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x3",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x3",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x4",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x4",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x5",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x5",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r3-x6",
+						Labels: map[string]string{
+							tasBlockLabel: "b1",
+							tasRackLabel:  "r3",
+							tasHostLabel:  "x6",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x3",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x4",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x5",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r3",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"host required; single Pod fits in the host": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			levels: defaultThreeLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultThreeLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b2",
+							"r2",
+							"x6",
+						},
+					},
+				},
+			},
+		},
+		"rack required; single Pod fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; multiple Pods fits in a rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 3,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack required; too many pods to fit in any rack": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          4,
+			wantAssignment: nil,
+		},
+		"block required; single Pod fits in a block": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: []string{
+					tasBlockLabel,
+					tasRackLabel,
+				},
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"block required; Pods fit in a block spread across two racks": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block required; single Pod which cannot be split": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 4000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"block required; too many Pods to fit requested": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          5,
+			wantAssignment: nil,
+		},
+		"rack required; single Pod requiring memory": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceMemory: 1024,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 4,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only block can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 4,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"rack preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasRackLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but only multiple blocks can accommodate the workload": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 6,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultTwoLevels,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 3,
+						Values: []string{
+							"b1",
+							"r2",
+						},
+					},
+					{
+						Count: 2,
+						Values: []string{
+							"b2",
+							"r2",
+						},
+					},
+					{
+						Count: 1,
+						Values: []string{
+							"b1",
+							"r1",
+						},
+					},
+				},
+			},
+		},
+		"block preferred; but the workload cannot be accommodate in entire topology": {
+			nodes: defaultNodes,
+			request: kueue.PodSetTopologyRequest{
+				Preferred: ptr.To(tasBlockLabel),
+			},
+			levels: defaultTwoLevels,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          10,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; no matching node": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-b",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count:          1,
+			wantAssignment: nil,
+		},
+		"only nodes with matching labels are considered; matching node is found": {
+			nodes: []corev1.Node{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "b1-r1-x1",
+						Labels: map[string]string{
+							"zone":       "zone-a",
+							tasHostLabel: "x1",
+						},
+					},
+					Status: corev1.NodeStatus{
+						Allocatable: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+						},
+					},
+				},
+			},
+			request: kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasHostLabel),
+			},
+			nodeLabels: map[string]string{
+				"zone": "zone-a",
+			},
+			levels: defaultOneLevel,
+			requests: resources.Requests{
+				corev1.ResourceCPU: 1000,
+			},
+			count: 1,
+			wantAssignment: &kueue.TopologyAssignment{
+				Levels: defaultOneLevel,
+				Domains: []kueue.TopologyDomainAssignment{
+					{
+						Count: 1,
+						Values: []string{
+							"x1",
+						},
+					},
+				},
+			},
+		},
+	}
```

  For sure, it is part of the API surface so I marked it as P0 in the spreadsheet.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T13:42:05Z

- **pkg/cache/tas_flavor_snapshot.go:106** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T13:42:05Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
```

  nit: 
  ```suggestion
  	for levelIdx := range s.levelKeys {
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T13:45:29Z

- **pkg/cache/tas_flavor_snapshot.go:106** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T13:45:29Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
```

  +1 on this.
  This could avoid to compute the levelKeys size (`len(s.levelkeys)`) everytime, then improve performance.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T14:58:42Z

- **pkg/cache/tas_flavor_snapshot.go:39** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T13:52:38Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
```

  Could you please add a comment on how the exemplary `sortName` looks like so there is clear difference between it and `id`?

- **pkg/cache/tas_flavor_snapshot.go:64** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T13:59:18Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
```

  Can we improve the naming so it reflects better that this is only for the lowest level? Maybe `freeCapacityPerLeaf`, although `leaf` is a new term so it may cause confusion

- **pkg/cache/tas_flavor_snapshot.go:255** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T14:58:42Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
```

  ```suggestion
  	for _, domain := range domains {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:04:13Z

- **pkg/cache/tas_flavor_snapshot.go:261** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:04:13Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
```

  Why is this if needed?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:06:53Z

- **pkg/cache/tas_flavor_snapshot.go:296** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:06:53Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelDomains(levelIdx int, infos []*domain) []*domain {
```

  Can we rename `infos` to `domains`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:09:19Z

- **pkg/cache/tas_flavor_snapshot.go:300** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:09:18Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelDomains(levelIdx int, infos []*domain) []*domain {
+	result := make([]*domain, 0, len(infos))
+	for _, info := range infos {
+		for _, childDomainID := range info.childIDs {
+			if childDomain := s.domainsPerLevel[levelIdx+1][childDomainID]; childDomain != nil {
```

  Can the `childDomain` be nil?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:11:23Z

- **pkg/cache/tas_flavor_snapshot.go:279** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:11:22Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
```

  ```suggestion
  	for _, domain := range domains {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:12:22Z

- **pkg/cache/tas_flavor_snapshot.go:274** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:12:22Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
```

  Do I understand correctly that here domains are the lowest level domains? Can add a comment about it?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:15:05Z

- **pkg/cache/tas_flavor_snapshot.go:288** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:15:05Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
```

  Why can't we just return/use `s.levelValuesPerDomain[domainID]`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:18:06Z

- **pkg/cache/tas_flavor_snapshot.go:308** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:18:06Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelDomains(levelIdx int, infos []*domain) []*domain {
+	result := make([]*domain, 0, len(infos))
+	for _, info := range infos {
+		for _, childDomainID := range info.childIDs {
+			if childDomain := s.domainsPerLevel[levelIdx+1][childDomainID]; childDomain != nil {
+				result = append(result, childDomain)
+			}
+		}
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) domainsForLevel(levelIdx int) []*domain {
```

  Can we use s.domainsPerLevel instead of this function?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:20:27Z

- **pkg/cache/tas_flavor_snapshot.go:239** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:20:27Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
```

  Can we check if the `s.state[sortedDomain[lastIdx].id] > 0` to reduce adding domains that can't fit any pod?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-22T15:20:48Z

- **pkg/cache/tas_flavor_snapshot.go:261** by [@PBundyra](https://github.com/PBundyra) — 2024-10-22T15:20:48Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
```

  Please see the comment above: https://github.com/kubernetes-sigs/kueue/pull/3256/files#r1810936539

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:55:41Z

- **pkg/cache/tas_flavor_snapshot.go:106** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:55:41Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:55:46Z

- **pkg/cache/tas_flavor_snapshot.go:39** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:55:46Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:01Z

- **pkg/cache/tas_flavor_snapshot.go:64** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:01Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291
  As freeCapacityPerLeafDomain

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:09Z

- **pkg/cache/tas_flavor_snapshot.go:255** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:09Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:14Z

- **pkg/cache/tas_flavor_snapshot.go:261** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:14Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:21Z

- **pkg/cache/tas_flavor_snapshot.go:296** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:21Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelDomains(levelIdx int, infos []*domain) []*domain {
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:29Z

- **pkg/cache/tas_flavor_snapshot.go:279** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:29Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:41Z

- **pkg/cache/tas_flavor_snapshot.go:288** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:41Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
```

  yes, Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:48Z

- **pkg/cache/tas_flavor_snapshot.go:308** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:48Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelDomains(levelIdx int, infos []*domain) []*domain {
+	result := make([]*domain, 0, len(infos))
+	for _, info := range infos {
+		for _, childDomainID := range info.childIDs {
+			if childDomain := s.domainsPerLevel[levelIdx+1][childDomainID]; childDomain != nil {
+				result = append(result, childDomain)
+			}
+		}
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) domainsForLevel(levelIdx int) []*domain {
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:56:54Z

- **pkg/cache/tas_flavor_snapshot.go:239** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:56:54Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:57:01Z

- **test/integration/tas/tas_job_test.go:536** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:57:01Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			})
+
+			ginkgo.It("should admit workload when nodes become available", func() {
+				var (
+					nodes []corev1.Node
+					wl1   *kueue.Workload
+				)
+
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+
+				ginkgo.By("Create nodes to allow scheduling", func() {
+					nodes = []corev1.Node{
+						{
+							ObjectMeta: metav1.ObjectMeta{
+								Name: "b1-r1",
+								Labels: map[string]string{
+									tasBlockLabel: "b1",
+									tasRackLabel:  "r1",
+								},
+							},
+							Status: corev1.NodeStatus{
+								Allocatable: corev1.ResourceList{
+									corev1.ResourceCPU:    resource.MustParse("1"),
+									corev1.ResourceMemory: resource.MustParse("1Gi"),
+								},
+							},
+						},
+					}
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+				})
+
+				ginkgo.By("Delete the node for cleanup", func() {
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Delete(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+			})
+		})
+	})
+
+	ginkgo.When("Multiple TAS Resource Flavors in queue", func() {
+		var (
+			tasGPUFlavor *kueue.ResourceFlavor
+			tasCPUFlavor *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			const (
+				gpuResName = "example.com/gpu"
+			)
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "cpu-node",
+							Labels: map[string]string{
+								"node.kubernetes.io/instance-type": "cpu-node",
+								tasRackLabel:                       "cpu-rack",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("5"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "gpu-node",
+							Labels: map[string]string{
+								"node.kubernetes.io/instance-type": "gpu-node",
+								tasRackLabel:                       "gpu-rack",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								gpuResName: resource.MustParse("4"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasGPUFlavor = testing.MakeResourceFlavor("tas-gpu-flavor").
+					NodeLabel("node.kubernetes.io/instance-type", "gpu-node").
+					TopologyName("default").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasGPUFlavor)).Should(gomega.Succeed())
+
+				tasCPUFlavor = testing.MakeResourceFlavor("tas-cpu-flavor").
+					NodeLabel("node.kubernetes.io/instance-type", "cpu-node").
+					TopologyName("default").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasCPUFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*testing.MakeFlavorQuotas(tasGPUFlavor.Name).Resource(corev1.ResourceCPU, "1").Resource(gpuResName, "5").Obj(),
+						*testing.MakeFlavorQuotas(tasCPUFlavor.Name).Resource(corev1.ResourceCPU, "5").Resource(gpuResName, "0").Obj(),
+					).Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasCPUFlavor, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasGPUFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:57:05Z

- **test/integration/tas/tas_job_test.go:439** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:57:04Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			})
+
+			ginkgo.It("should admit workload when nodes become available", func() {
+				var (
+					nodes []corev1.Node
+					wl1   *kueue.Workload
+				)
+
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+
+				ginkgo.By("Create nodes to allow scheduling", func() {
+					nodes = []corev1.Node{
+						{
+							ObjectMeta: metav1.ObjectMeta{
+								Name: "b1-r1",
+								Labels: map[string]string{
+									tasBlockLabel: "b1",
+									tasRackLabel:  "r1",
+								},
+							},
+							Status: corev1.NodeStatus{
+								Allocatable: corev1.ResourceList{
+									corev1.ResourceCPU:    resource.MustParse("1"),
+									corev1.ResourceMemory: resource.MustParse("1Gi"),
+								},
+							},
+						},
+					}
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+				})
+
+				ginkgo.By("Delete the node for cleanup", func() {
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Delete(ctx, &node)).Should(gomega.Succeed())
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:57:10Z

- **pkg/cache/tas_flavor.go:42** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:57:10Z
```diff
@@ -0,0 +1,129 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"maps"
+	"slices"
+	"sync"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// usageOp indicates whether we should add or subtract the usage.
+type usageOp bool
+
+const (
+	// add usage to the cache
+	add usageOp = true
+	// subtract usage from the cache
+	subtract usageOp = false
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:58:53Z

- **pkg/cache/tas_flavor_snapshot.go:274** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:58:53Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
```

  Yes, but the function itself is agnostic about that, so I added it to the description of the algorithm.
  done in the follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T08:13:48Z

- **pkg/cache/tas_flavor_snapshot.go:300** by [@mimowo](https://github.com/mimowo) — 2024-10-23T08:13:48Z
```diff
@@ -0,0 +1,349 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"errors"
+	"slices"
+	"strings"
+
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+var (
+	errCodeAssumptionsViolated = errors.New("code assumptions violated")
+)
+
+// domain holds the static information about placement of a topology
+// domain in the hierarchy of topology domains.
+type domain struct {
+	// sortName indicates name used for sorting when two domains can fit
+	// the same number of pods
+	sortName string
+
+	// id is the globally unique id of the domain
+	id utiltas.TopologyDomainID
+
+	// parentID is the global ID of the parent domain
+	parentID utiltas.TopologyDomainID
+
+	// childIDs at the global IDs of the child domains
+	childIDs []utiltas.TopologyDomainID
+}
+
+type domainByID map[utiltas.TopologyDomainID]*domain
+type statePerDomain map[utiltas.TopologyDomainID]int32
+
+type TASFlavorSnapshot struct {
+	log logr.Logger
+
+	// levelKeys denotes the ordered list of topology keys set as label keys
+	// on the Topology object
+	levelKeys []string
+
+	// freeCapacityPerDomain stores the free capacity per domain, only for the
+	// lowest level of topology
+	freeCapacityPerDomain map[utiltas.TopologyDomainID]resources.Requests
+
+	// levelValuesPerDomain stores the mapping from domain ID back to the
+	// ordered list of values. It stores the information for all levels.
+	levelValuesPerDomain map[utiltas.TopologyDomainID][]string
+
+	// domainsPerLevel stores the static tree information
+	domainsPerLevel []domainByID
+
+	// statePerLevel is a temporary state of the topology domains during the
+	// assignment algorithm.
+	//
+	// In the first phase of the algorithm (traversal to the top the topology to
+	// determine the level to fit the workload) it denotes the number of pods
+	// which can fit in a given domain.
+	//
+	// In the second phase of the algorithm (traversal to the bottom to
+	// determine the actual assignments) it denotes the number of pods actually
+	// assigned to the given domain.
+	state statePerDomain
+}
+
+func newTASFlavorSnapshot(log logr.Logger, levels []string) *TASFlavorSnapshot {
+	snapshot := &TASFlavorSnapshot{
+		log:                   log,
+		levelKeys:             slices.Clone(levels),
+		freeCapacityPerDomain: make(map[utiltas.TopologyDomainID]resources.Requests),
+		levelValuesPerDomain:  make(map[utiltas.TopologyDomainID][]string),
+		domainsPerLevel:       make([]domainByID, len(levels)),
+		state:                 make(statePerDomain),
+	}
+	return snapshot
+}
+
+// initialize prepares the domainsPerLevel tree structure. This structure holds
+// for a given the list of topology domains with additional static and dynamic
+// information. This function initializes the static information which
+// represents the edges to the parent and child domains. This structure is
+// reused for multiple workloads during a single scheduling cycle.
+func (s *TASFlavorSnapshot) initialize() {
+	levelCount := len(s.levelKeys)
+	lastLevelIdx := levelCount - 1
+	for levelIdx := 0; levelIdx < len(s.levelKeys); levelIdx++ {
+		s.domainsPerLevel[levelIdx] = make(domainByID)
+	}
+	for childID := range s.freeCapacityPerDomain {
+		childDomain := &domain{
+			sortName: s.sortName(lastLevelIdx, childID),
+			id:       childID,
+		}
+		s.domainsPerLevel[lastLevelIdx][childID] = childDomain
+		parentFound := false
+		var parent *domain
+		for levelIdx := lastLevelIdx - 1; levelIdx >= 0 && !parentFound; levelIdx-- {
+			parentValues := s.levelValuesPerDomain[childID][:levelIdx+1]
+			parentID := utiltas.DomainID(parentValues)
+			s.levelValuesPerDomain[parentID] = parentValues
+			parent, parentFound = s.domainsPerLevel[levelIdx][parentID]
+			if !parentFound {
+				parent = &domain{
+					sortName: s.sortName(levelIdx, parentID),
+					id:       parentID,
+				}
+				s.domainsPerLevel[levelIdx][parentID] = parent
+			}
+			childDomain.parentID = parentID
+			parent.childIDs = append(parent.childIDs, childID)
+			childID = parentID
+		}
+	}
+}
+
+func (s *TASFlavorSnapshot) sortName(levelIdx int, domainID utiltas.TopologyDomainID) string {
+	levelValues := s.levelValuesPerDomain[domainID]
+	if len(levelValues) <= levelIdx {
+		s.log.Error(errCodeAssumptionsViolated, "invalid invocation of sortName",
+			"count", len(levelValues),
+			"levelIdx", levelIdx,
+			"domainID", domainID,
+			"levelValuesPerDomain", s.levelValuesPerDomain,
+			"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	}
+	// we prefix with the node label value to make it ordered naturally, but
+	// append also domain ID as the node label value may not be globally unique.
+	return s.levelValuesPerDomain[domainID][levelIdx] + " " + string(domainID)
+}
+
+func (s *TASFlavorSnapshot) addCapacity(domainID utiltas.TopologyDomainID, capacity resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Add(capacity)
+}
+
+func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage resources.Requests) {
+	s.initializeFreeCapacityPerDomain(domainID)
+	s.freeCapacityPerDomain[domainID].Sub(usage)
+}
+
+func (s *TASFlavorSnapshot) initializeFreeCapacityPerDomain(domainID utiltas.TopologyDomainID) {
+	if _, found := s.freeCapacityPerDomain[domainID]; !found {
+		s.freeCapacityPerDomain[domainID] = resources.Requests{}
+	}
+}
+
+// Algorithm overview:
+// Phase 1:
+//
+//	determine pod counts for each topology domain. Start at the lowest level
+//	and bubble up the numbers to the top level
+//
+// Phase 2:
+//
+//	a) select the domain at requested level with count >= requestedCount
+//	b) traverse the structure down level-by-level optimizing the number of used
+//	  domains at each level
+func (s *TASFlavorSnapshot) FindTopologyAssignment(
+	topologyRequest *kueue.PodSetTopologyRequest,
+	requests resources.Requests,
+	count int32) *kueue.TopologyAssignment {
+	required := topologyRequest.Required != nil
+	levelIdx, found := s.resolveLevelIdx(topologyRequest)
+	if !found {
+		return nil
+	}
+	// phase 1 - determine the number of pods which can fit in each topology domain
+	s.fillInCounts(requests)
+
+	// phase 2a: determine the level at which the assignment is done along with
+	// the domains which can accommodate all pods
+	fitLevelIdx, currFitDomain := s.findLevelWithFitDomains(levelIdx, required, count)
+	if len(currFitDomain) == 0 {
+		return nil
+	}
+
+	// phase 2b: traverse the tree down level-by-level optimizing the number of
+	// topology domains at each level
+	currFitDomain = s.updateCountsToMinimum(currFitDomain, count)
+	for levelIdx := fitLevelIdx; levelIdx+1 < len(s.domainsPerLevel); levelIdx++ {
+		lowerFitDomains := s.lowerLevelDomains(levelIdx, currFitDomain)
+		sortedLowerDomains := s.sortedDomains(lowerFitDomains)
+		currFitDomain = s.updateCountsToMinimum(sortedLowerDomains, count)
+	}
+	return s.buildAssignment(currFitDomain)
+}
+
+func (s *TASFlavorSnapshot) resolveLevelIdx(
+	topologyRequest *kueue.PodSetTopologyRequest) (int, bool) {
+	var levelKey string
+	if topologyRequest.Required != nil {
+		levelKey = *topologyRequest.Required
+	} else if topologyRequest.Preferred != nil {
+		levelKey = *topologyRequest.Preferred
+	}
+	levelIdx := slices.Index(s.levelKeys, levelKey)
+	if levelIdx == -1 {
+		return levelIdx, false
+	}
+	return levelIdx, true
+}
+
+func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool, count int32) (int, []*domain) {
+	levelDomains := s.domainsForLevel(levelIdx)
+	if len(levelDomains) == 0 {
+		return 0, nil
+	}
+	sortedDomain := s.sortedDomains(levelDomains)
+	topDomain := sortedDomain[0]
+	if s.state[topDomain.id] < count {
+		if required {
+			return 0, nil
+		}
+		if levelIdx > 0 {
+			return s.findLevelWithFitDomains(levelIdx-1, required, count)
+		}
+		lastIdx := 0
+		remainingCount := count - s.state[sortedDomain[lastIdx].id]
+		for remainingCount > 0 && lastIdx < len(sortedDomain)-1 {
+			lastIdx++
+			remainingCount -= s.state[sortedDomain[lastIdx].id]
+		}
+		if remainingCount > 0 {
+			return 0, nil
+		}
+		return 0, sortedDomain[:lastIdx+1]
+	}
+	return levelIdx, []*domain{topDomain}
+}
+
+func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
+	result := make([]*domain, 0)
+	remainingCount := count
+	for i := 0; i < len(domains); i++ {
+		domain := domains[i]
+		if s.state[domain.id] >= remainingCount {
+			s.state[domain.id] = remainingCount
+			result = append(result, domain)
+			return result
+		}
+		if s.state[domain.id] > 0 {
+			remainingCount -= s.state[domain.id]
+			result = append(result, domain)
+		}
+	}
+	s.log.Error(errCodeAssumptionsViolated, "unexpected remainingCount",
+		"remainingCount", remainingCount,
+		"count", count,
+		"levelValuesPerDomain", s.levelValuesPerDomain,
+		"freeCapacityPerDomain", s.freeCapacityPerDomain)
+	return nil
+}
+
+func (s *TASFlavorSnapshot) buildAssignment(domains []*domain) *kueue.TopologyAssignment {
+	assignment := kueue.TopologyAssignment{
+		Levels:  s.levelKeys,
+		Domains: make([]kueue.TopologyDomainAssignment, 0),
+	}
+	for i := 0; i < len(domains); i++ {
+		assignment.Domains = append(assignment.Domains, kueue.TopologyDomainAssignment{
+			Values: s.asLevelValues(domains[i].id),
+			Count:  s.state[domains[i].id],
+		})
+	}
+	return &assignment
+}
+
+func (s *TASFlavorSnapshot) asLevelValues(domainID utiltas.TopologyDomainID) []string {
+	result := make([]string, len(s.levelKeys))
+	for i := range s.levelKeys {
+		result[i] = s.levelValuesPerDomain[domainID][i]
+	}
+	return result
+}
+
+func (s *TASFlavorSnapshot) lowerLevelDomains(levelIdx int, infos []*domain) []*domain {
+	result := make([]*domain, 0, len(infos))
+	for _, info := range infos {
+		for _, childDomainID := range info.childIDs {
+			if childDomain := s.domainsPerLevel[levelIdx+1][childDomainID]; childDomain != nil {
```

  If the code is correct then not, it was just a "safety" check, but let's avoid them, as they could swipe serious issues under the carpet. 
  done in the follow up.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-23T08:44:43Z

Nits

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-23T11:35:47Z

These are my final nits, thank you!

- **pkg/cache/tas_cache_test.go:65** by [@PBundyra](https://github.com/PBundyra) — 2024-10-23T09:17:13Z
```diff
@@ -0,0 +1,731 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
```

  typo: tasHostLabel should be `x2`

- **test/integration/tas/tas_job_test.go:318** by [@PBundyra](https://github.com/PBundyra) — 2024-10-23T11:17:29Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
```

  Can we check if wl4 is not admitted before wl3 is finished?

- **test/integration/tas/tas_job_test.go:352** by [@PBundyra](https://github.com/PBundyra) — 2024-10-23T11:17:56Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
```

  Duplicate in the description

- **test/integration/tas/tas_job_test.go:437** by [@PBundyra](https://github.com/PBundyra) — 2024-10-23T11:18:54Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			})
+
+			ginkgo.It("should admit workload when nodes become available", func() {
+				var (
+					nodes []corev1.Node
+					wl1   *kueue.Workload
+				)
+
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+
+				ginkgo.By("Create nodes to allow scheduling", func() {
+					nodes = []corev1.Node{
+						{
+							ObjectMeta: metav1.ObjectMeta{
+								Name: "b1-r1",
+								Labels: map[string]string{
+									tasBlockLabel: "b1",
+									tasRackLabel:  "r1",
+								},
+							},
+							Status: corev1.NodeStatus{
+								Allocatable: corev1.ResourceList{
+									corev1.ResourceCPU:    resource.MustParse("1"),
+									corev1.ResourceMemory: resource.MustParse("1Gi"),
+								},
+							},
+						},
+					}
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+				})
+
+				ginkgo.By("Delete the node for cleanup", func() {
```

  Should we do cleanup here instead of `AfterEach` block?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T11:51:08Z

- **pkg/cache/tas_cache_test.go:65** by [@mimowo](https://github.com/mimowo) — 2024-10-23T11:51:08Z
```diff
@@ -0,0 +1,731 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestFindTopologyAssignment(t *testing.T) {
+	const (
+		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
+		tasHostLabel  = "kubernetes.io/hostname"
+	)
+
+	defaultNodes := []corev1.Node{
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r1-x1",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r1",
+					tasHostLabel:  "x1",
+				},
+			},
+			Status: corev1.NodeStatus{
+				Allocatable: corev1.ResourceList{
+					corev1.ResourceCPU:    resource.MustParse("1"),
+					corev1.ResourceMemory: resource.MustParse("1Gi"),
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: "b1-r2-x2",
+				Labels: map[string]string{
+					tasBlockLabel: "b1",
+					tasRackLabel:  "r2",
+					tasHostLabel:  "x1",
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T11:51:58Z

- **test/integration/tas/tas_job_test.go:318** by [@mimowo](https://github.com/mimowo) — 2024-10-23T11:51:58Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
```

  sure, added a check: util.ExpectWorkloadsToBePending(ctx, k8sClient, wl4)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T11:52:07Z

- **test/integration/tas/tas_job_test.go:352** by [@mimowo](https://github.com/mimowo) — 2024-10-23T11:52:07Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
```

  cleaned up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T11:52:22Z

- **test/integration/tas/tas_job_test.go:437** by [@mimowo](https://github.com/mimowo) — 2024-10-23T11:52:22Z
```diff
@@ -0,0 +1,602 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup)
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, true)
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "tas-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		_ = features.SetEnable(features.TopologyAwareScheduling, false)
+	})
+
+	ginkgo.When("Single TAS Resource Flavor", func() {
+		var (
+			tasFlavor    *kueue.ResourceFlavor
+			topology     *kueuealpha.Topology
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+
+		ginkgo.When("Nodes are created before test", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b1-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b1",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r1",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r1",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Name: "b2-r2",
+							Labels: map[string]string{
+								tasBlockLabel: "b2",
+								tasRackLabel:  "r2",
+							},
+						},
+						Status: corev1.NodeStatus{
+							Allocatable: corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+							},
+						},
+					},
+				}
+				for _, node := range nodes {
+					gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+					gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+				}
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should not admit workload which does not fit to the required topology domain", func() {
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 := testing.MakeWorkload("wl1-inadmissible", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+			})
+
+			ginkgo.It("should admit workload which fits in a required topology domain", func() {
+				var wl1, wl2, wl3, wl4 *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].Count = 2
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+					gomega.Expect(wl1.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r1",
+									},
+								},
+								{
+									Count: 1,
+									Values: []string{
+										"b1",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the second workload to see it lands on another rack", func() {
+					wl2 = testing.MakeWorkload("wl2", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl2.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 2)
+				})
+
+				ginkgo.By("verify admission for the workload2", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), wl2)).To(gomega.Succeed())
+					gomega.Expect(wl2.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r1",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating the wl3", func() {
+					wl3 = testing.MakeWorkload("wl3", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl3.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the wl3 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl3)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl3", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl3), wl3)).To(gomega.Succeed())
+					gomega.Expect(wl3.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+
+				ginkgo.By("creating wl4", func() {
+					wl4 = testing.MakeWorkload("wl4", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl4.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("finish wl3", func() {
+					util.FinishWorkloads(ctx, k8sClient, wl3)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)
+				})
+
+				ginkgo.By("verify the wl4 is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 3)
+				})
+
+				ginkgo.By("verify admission for the wl4", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl4), wl4)).To(gomega.Succeed())
+					gomega.Expect(wl4.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{
+								tasBlockLabel,
+								tasRackLabel,
+							},
+							Domains: []kueue.TopologyDomainAssignment{
+								{
+									Count: 1,
+									Values: []string{
+										"b2",
+										"r2",
+									},
+								},
+							},
+						},
+					))
+				})
+			})
+		})
+
+		ginkgo.When("Nodes node structure is mutated during test cases", func() {
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").TopologyName("default").Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+			})
+
+			ginkgo.It("should admit workload when nodes become available", func() {
+				var (
+					nodes []corev1.Node
+					wl1   *kueue.Workload
+				)
+
+				ginkgo.By("creating a workload which requires rack, but does not fit in any", func() {
+					wl1 = testing.MakeWorkload("wl1", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl1.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					}
+					gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify the workload is inadmissible", func() {
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				})
+
+				ginkgo.By("Create nodes to allow scheduling", func() {
+					nodes = []corev1.Node{
+						{
+							ObjectMeta: metav1.ObjectMeta{
+								Name: "b1-r1",
+								Labels: map[string]string{
+									tasBlockLabel: "b1",
+									tasRackLabel:  "r1",
+								},
+							},
+							Status: corev1.NodeStatus{
+								Allocatable: corev1.ResourceList{
+									corev1.ResourceCPU:    resource.MustParse("1"),
+									corev1.ResourceMemory: resource.MustParse("1Gi"),
+								},
+							},
+						},
+					}
+					for _, node := range nodes {
+						gomega.Expect(k8sClient.Create(ctx, &node)).Should(gomega.Succeed())
+						gomega.Expect(k8sClient.Status().Update(ctx, &node)).Should(gomega.Succeed())
+					}
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+				})
+
+				ginkgo.By("Delete the node for cleanup", func() {
```

  moved to AfterEach
