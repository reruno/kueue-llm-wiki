# PR #3261: [KEP] Update KEP #993 - Add configuration to retry mechanism for ProvisioningRequests

**Summary**: [KEP] Update KEP #993 - Add configuration to retry mechanism for ProvisioningRequests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3261

**Last updated**: 2024-11-05T13:14:33Z

---

## Metadata

- **State**: closed
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `KEP-AC-retry-strategy` → `main`
- **Created**: 2024-10-18T08:40:33Z
- **Updated**: 2024-11-05T13:14:33Z
- **Merged**: —
- **Closed**: 2024-10-21T11:31:29Z
- **Labels**: `release-note-none`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+39 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Propose changes to introduce configuration to retry mechanism for ProvisioningRequests

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #1353 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-18T08:40:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a76c47c656f19c1b71f4ae84686edd247d52a66e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67161f3704d21100080b7b0b |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-18T08:40:50Z

/assign @mimowo 
cc @mwielgus @yaroslava-serdiuk

### Comment by [@trasc](https://github.com/trasc) — 2024-10-18T14:09:26Z

In my opinion we should let individual admission check controllers implement their own retry policy, not all ACCs need this and having another actor managing its ACStates makes room for conflicts or needs additional logic to avoid them.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-21T08:26:30Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3261#" title="Author self-approved">PBundyra</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from [mimowo](https://github.com/mimowo). For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T08:37:25Z

> In my opinion we should let individual admission check controllers implement their own retry policy, not all ACCs need this and having another actor managing its ACStates makes room for conflicts or needs additional logic to avoid them.

Thanks for the review Traian. I considered it, and change the proposal to cover only ProvisioningRequests

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-21T08:59:57Z

/cc

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T11:31:26Z

I took it a step further and moved the API to the ProvisioningRequestConfig API in #3278, so I'm closing this one.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-18T09:02:57Z

- **keps/993-two-phase-admission/README.md:210** by [@mimowo](https://github.com/mimowo) — 2024-10-18T09:01:12Z
```diff
@@ -187,6 +188,29 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// admissionCheckRetryStrategy defines strategy for retrying AdmissionChecks
+	// +optional
+	AdmissionCheckRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type AdmissionCheckRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for AdmissionChecks
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 5
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 600
+	//
+	// +listType=map
+	DefaultRequeuingStrategy *RequeuingStrategy `json:defaultRequeuingStrategy`
```

  It this configuration going to be used to configure the mechanism based on this hard-coded values: https://github.com/kubernetes-sigs/kueue/blob/9332a5a3d911f191de262e41fe0508e9bccc2d77/pkg/controller/admissionchecks/provisioning/controller.go#L54-L56, or is it going to be yet another mechanism? 
  
  Please clarify in the KEP, I would prefer to make the existing mechanism configurable and we already have an issue for it https://github.com/kubernetes-sigs/kueue/issues/1353. If we reuse the mechanism, can we reuse the same defaults, or state clearly that we are changing the defaults.

- **keps/993-two-phase-admission/README.md:196** by [@mimowo](https://github.com/mimowo) — 2024-10-18T09:01:45Z
```diff
@@ -187,6 +188,29 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// admissionCheckRetryStrategy defines strategy for retrying AdmissionChecks
+	// +optional
```

  No need for the tags in the global configuration as this is not CRD.

- **keps/993-two-phase-admission/README.md:211** by [@mimowo](https://github.com/mimowo) — 2024-10-18T09:01:55Z
```diff
@@ -187,6 +188,29 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// admissionCheckRetryStrategy defines strategy for retrying AdmissionChecks
+	// +optional
+	AdmissionCheckRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type AdmissionCheckRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for AdmissionChecks
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 5
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 600
+	//
+	// +listType=map
```

  you can drop it.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-21T08:35:34Z

- **keps/993-two-phase-admission/README.md:211** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T08:35:34Z
```diff
@@ -187,6 +188,29 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// admissionCheckRetryStrategy defines strategy for retrying AdmissionChecks
+	// +optional
+	AdmissionCheckRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type AdmissionCheckRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for AdmissionChecks
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 5
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 600
+	//
+	// +listType=map
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-21T08:35:46Z

- **keps/993-two-phase-admission/README.md:196** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T08:35:46Z
```diff
@@ -187,6 +188,29 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// admissionCheckRetryStrategy defines strategy for retrying AdmissionChecks
+	// +optional
```

  Done, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-21T08:36:40Z

- **keps/993-two-phase-admission/README.md:210** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T08:36:40Z
```diff
@@ -187,6 +188,29 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// admissionCheckRetryStrategy defines strategy for retrying AdmissionChecks
+	// +optional
+	AdmissionCheckRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type AdmissionCheckRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for AdmissionChecks
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 5
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 600
+	//
+	// +listType=map
+	DefaultRequeuingStrategy *RequeuingStrategy `json:defaultRequeuingStrategy`
```

  Yes, and it introduces a field `releaseQuota` that allows users to release quota if the ProvisioningRequest fails

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T08:49:48Z

Overall lgtm, but we need to clarify what happens if the strategy is not specified. Do we fallback to the previous one for backwards compatibility, or disable it?

- **keps/993-two-phase-admission/README.md:96** by [@mimowo](https://github.com/mimowo) — 2024-10-21T08:39:23Z
```diff
@@ -93,6 +93,7 @@ TL;DR;
 * extend ClusterQueueSpec with the list of checks.
 * add extra conditions to Workload status.
 * introduce new CRD, AdmissionCheck.
+* introduce new field to Kueue's Configuration, AdmissionCheckRetryStrategy`
```

  ```suggestion
  * introduce new field to Kueue's Configuration, ProvisioningRequestRetryStrategy`
  ```

- **keps/993-two-phase-admission/README.md:196** by [@mimowo](https://github.com/mimowo) — 2024-10-21T08:39:35Z
```diff
@@ -187,6 +188,34 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// provisioningRequestRetryStrategy defines strategy for retrying ProvisioningRequest
+	ProvisioningRequestRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
```

  ```suggestion
  	ProvisioningRequestRetryStrategy *ProvisioningRequestRetryStrategy `json:provisioningRequestRetryStrategy, omitempty`
  ```

- **keps/993-two-phase-admission/README.md:210** by [@mimowo](https://github.com/mimowo) — 2024-10-21T08:42:11Z
```diff
@@ -187,6 +188,29 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// admissionCheckRetryStrategy defines strategy for retrying AdmissionChecks
+	// +optional
+	AdmissionCheckRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type AdmissionCheckRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for AdmissionChecks
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 5
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 600
+	//
+	// +listType=map
+	DefaultRequeuingStrategy *RequeuingStrategy `json:defaultRequeuingStrategy`
```

  sgtm, I will ask follow up questions in other comments

- **keps/993-two-phase-admission/README.md:196** by [@mimowo](https://github.com/mimowo) — 2024-10-21T08:46:49Z
```diff
@@ -187,6 +188,34 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// provisioningRequestRetryStrategy defines strategy for retrying ProvisioningRequest
+	ProvisioningRequestRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
```

  What if not specified? Will default to one? I guess it makes sense to default for backwards compatibility. Otherwise we may disable the existing mechanism, IIUC.

- **keps/993-two-phase-admission/README.md:217** by [@mimowo](https://github.com/mimowo) — 2024-10-21T08:48:31Z
```diff
@@ -187,6 +188,34 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// provisioningRequestRetryStrategy defines strategy for retrying ProvisioningRequest
+	ProvisioningRequestRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type ProvisioningRequestRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for ProvisioningRequests.
+	// The strategy is applied to all classes of ProvisioningRequest.
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 3
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 1800
+	DefaultRequeuingStrategy *RequeuingStrategy `json:defaultRequeuingStrategy`
+
+	// releaseQuota defines whether the reserved quota should be released during retry.
+	// If the releaseQuota is set not false, controller will delete old ProvisioningRequest
+	// and create a new one without releasing quota.
+	//
+	// Defaults to true.
+	ReleaseQuota *bool `json:releaseQuota`
```

  I don't think it needs to be a pointer as nil does not have any extra semantics here.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-21T09:30:32Z

- **keps/993-two-phase-admission/README.md:217** by [@PBundyra](https://github.com/PBundyra) — 2024-10-21T09:30:32Z
```diff
@@ -187,6 +188,34 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// provisioningRequestRetryStrategy defines strategy for retrying ProvisioningRequest
+	ProvisioningRequestRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type ProvisioningRequestRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for ProvisioningRequests.
+	// The strategy is applied to all classes of ProvisioningRequest.
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 3
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 1800
+	DefaultRequeuingStrategy *RequeuingStrategy `json:defaultRequeuingStrategy`
+
+	// releaseQuota defines whether the reserved quota should be released during retry.
+	// If the releaseQuota is set not false, controller will delete old ProvisioningRequest
+	// and create a new one without releasing quota.
+	//
+	// Defaults to true.
+	ReleaseQuota *bool `json:releaseQuota`
```

  Fixed

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-10-21T11:29:16Z

- **keps/993-two-phase-admission/README.md:267** by [@trasc](https://github.com/trasc) — 2024-10-21T11:19:26Z
```diff
@@ -232,8 +261,15 @@ are still valid (check quota/preemptions/reclamation) and admit the workload (do
 newly identified preemptions, if needed), setting the `Admitted`
 condition to True.
 
-If any check is switched to "False", with reason "Reject" or "Retry", the workload is either
-completely rejected or switched back to suspend state (with retry delay).
+If a ProvisioningRequest check is switched to `Retry` state, the Workload will be requeued
+or deactivated, with respect to the `ProvisioningRequestRetryStrategy.defaultRequeuingStrategy` defined in
+Kueue's global Configuration. If the Workload is requeued, controller should update Workload's `.status.requeueState`
+accordingly. If the limit of number of retries has been surpassed, the controller will mark the check as `Rejected`,
```

  ```suggestion
  accordingly. If the limit of number of retries has been surpassed, the provisioning admission check controller will mark the check as `Rejected`,
  ```

- **keps/993-two-phase-admission/README.md:214** by [@trasc](https://github.com/trasc) — 2024-10-21T11:24:44Z
```diff
@@ -187,6 +188,34 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// provisioningRequestRetryStrategy defines strategy for retrying ProvisioningRequest
+	ProvisioningRequestRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type ProvisioningRequestRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for ProvisioningRequests.
+	// The strategy is applied to all classes of ProvisioningRequest.
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 3
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 1800
+	DefaultRequeuingStrategy *RequeuingStrategy `json:defaultRequeuingStrategy`
+
+	// releaseQuota defines whether the reserved quota should be released during retry.
+	// If the releaseQuota is set not false, controller will delete old ProvisioningRequest
+	// and create a new one without releasing quota.
```

  ```suggestion
  	// releaseQuota defines whether the reserved quota should be released during when its ProvisioningRequest  fails.
  	// If the releaseQuota is set not false, controller will delete old ProvisioningRequest
  	// and create a new one without setting the workload's adission check state to `Retry`.
  ```
  
  If an admission check state is set to `Retry` the workload controller will release the quota.

- **keps/993-two-phase-admission/README.md:266** by [@trasc](https://github.com/trasc) — 2024-10-21T11:29:12Z
```diff
@@ -232,8 +261,15 @@ are still valid (check quota/preemptions/reclamation) and admit the workload (do
 newly identified preemptions, if needed), setting the `Admitted`
 condition to True.
 
-If any check is switched to "False", with reason "Reject" or "Retry", the workload is either
-completely rejected or switched back to suspend state (with retry delay).
+If a ProvisioningRequest check is switched to `Retry` state, the Workload will be requeued
+or deactivated, with respect to the `ProvisioningRequestRetryStrategy.defaultRequeuingStrategy` defined in
+Kueue's global Configuration. If the Workload is requeued, controller should update Workload's `.status.requeueState`
```

  >  If the Workload is requeued, controller ...
  
  Which Controller should do this? the workload controller or the admission check controller.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-10-21T11:30:48Z

- **keps/993-two-phase-admission/README.md:210** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-10-21T11:30:47Z
```diff
@@ -187,6 +188,34 @@ type AdmissionCheckParametersReference struct {
 	// Name is the name of the resource being referenced.
 	Name string `json:"name"`
 }
+
+// Configuration is the Schema for the kueueconfigurations API
+type Configuration struct {
+	...
+	// provisioningRequestRetryStrategy defines strategy for retrying ProvisioningRequest
+	ProvisioningRequestRetryStrategy *AdmissionCheckRetryStrategy `json:admissionCheckRetryStrategy, omitempty`
+}
+
+type ProvisioningRequestRetryStrategy struct {
+	// defaultRequeuingStrategy defines default requeueing strategy for ProvisioningRequests.
+	// The strategy is applied to all classes of ProvisioningRequest.
+	//
+	// Default values are:
+	//
+	// defaultRequeuingStrategyAC:
+	//	timestamp: Eviction
+	//	backoffLimitCount: 3
+	//	backoffBaseSeconds: 60
+	//	backoffMaxSeconds: 1800
+	DefaultRequeuingStrategy *RequeuingStrategy `json:defaultRequeuingStrategy`
```

  nit: Do we need default suffix in the name?
