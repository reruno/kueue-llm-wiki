# PR #3266: TAS: TopologyUngater

**Summary**: TAS: TopologyUngater

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3266

**Last updated**: 2025-01-21T11:25:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-topology-ungater` → `main`
- **Created**: 2024-10-18T16:49:42Z
- **Updated**: 2025-01-21T11:25:33Z
- **Merged**: 2024-10-22T02:52:51Z
- **Closed**: 2024-10-22T02:52:51Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 10   **+1440 / -5**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #2724 

#### Special notes for your reviewer:

Done since the initial version:
- use the TAS dedicated label to filiter out non-TAS pods (DONE)
- expectations mechanism to prevent race conditions (DONE)

I have also tested the handlers already in the e2e prototype PR: https://github.com/kubernetes-sigs/kueue/pull/3218

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-18T16:49:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bff95831779c4080f350f7699636cbb00f451102 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67167713329ee20008829e00 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-18T16:50:10Z

/cc @PBundyra @gabesaba @alculquicondor @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-22T02:51:41Z

LGTM label has been added.  <details>Git tree hash: 2fe42177c76b465861ca7a645f7b794f28f8d6c5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-22T02:51:41Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3266#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3266#pullrequestreview-2383623883" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-22T05:26:04Z

> @mimowo Could you open a follow-up PR?

Thanks, sure.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-22T06:59:11Z

Though thinking about it I think the refactoring to split TopologyUngater will not be that trivial imo. 

Please also note that we already were spawning new goroutines in the pod group integration, and goroutines are also spawned by the k8s code job controller when creating new pods.

I agree that ideally we can avoid it, but  I would prefer to prioritize the functional follow ups first and e2e tests.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T16:54:14Z

> Please also note that we already were spawning new goroutines in the pod group integration, and goroutines are also spawned by the k8s code job controller when creating new pods.
> 
> I agree that ideally we can avoid it, but I would prefer to prioritize the functional follow ups first and e2e tests.

I agree. I do not claim decoupling should be done before 0.9.

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-18T20:13:52Z

- **pkg/controller/tas/indexer.go:34** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:15:33Z
```diff
@@ -0,0 +1,41 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+)
+
+const (
+	workloadNameKey = "metadata.workload"
+)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return indexer.IndexField(ctx, &corev1.Pod{}, workloadNameKey, func(o client.Object) []string {
+		pod := o.(*corev1.Pod)
```

  ```suggestion
  		pod, ok := o.(*corev1.Pod)
  		if !ok {
  			return nil
  		}
  ```

- **pkg/controller/tas/topology_ungater.go:114** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:18:26Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
```

  ```suggestion
  	if pod == nil || !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
  ```

- **pkg/controller/tas/topology_ungater.go:117** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:18:34Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
```

  ```suggestion
  ```

- **pkg/controller/tas/topology_ungater.go:124** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:20:19Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
```

  What is the reason to use the batch enqueue?
  Will this delay cause the ungating delay?

- **pkg/controller/tas/topology_ungater.go:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:23:53Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
```

  ```suggestion
  	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.String())
  ```

- **pkg/controller/tas/topology_ungater.go:140** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:24:29Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
```

  ```suggestion
  		log.V(5).Info("workload not found")
  ```
  
  Isn't this debug log?

- **pkg/controller/tas/topology_ungater.go:156** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:27:13Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
```

  ```suggestion
  				log.V(2).Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
  ```

- **pkg/controller/tas/topology_ungater.go:178** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:39:46Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
+				allToUngate = append(allToUngate, toUngate...)
+			}
+		}
+	}
+	var err error
+	if len(allToUngate) > 0 {
```

  In the future, we may want to decouple the controller and topology ungator. 
  But now, I'm ok with combining both into the controller so that we mitigate the implementation costs.

- **pkg/controller/tas/topology_ungater_test.go:333** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:42:52Z
```diff
@@ -0,0 +1,363 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"maps"
+	"testing"
+
+	gocmp "github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	jsoniter "github.com/json-iterator/go"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/job"
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/raycluster"
+)
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var (
+	podCmpOpts = []gocmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(corev1.Pod{}, "TypeMeta", "ObjectMeta.ResourceVersion",
+			"ObjectMeta.DeletionTimestamp"),
+		cmpopts.IgnoreFields(corev1.PodCondition{}, "LastTransitionTime"),
+	}
+	defaultTestLevels = []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	// this code is meant to deal with the fact that the pod assignment to
+	// topology domains is non-deterministic (i.e. sometimes pod1 gets the
+	// assigned to domain1, but sometimes to domain2). We only care about the
+	// number of pods ungated to a given domain.
+	type counts struct {
+		NodeSelector map[string]string
+		Count        int32
+	}
+
+	mapToJSON := func(t *testing.T, m map[string]string) string {
+		json := jsoniter.Config{
+			SortMapKeys: true,
+		}.Froze()
+		bytes, err := json.Marshal(m)
+		if err != nil {
+			t.Fatalf("failed to serialize map: %v, error=%s", m, err)
+		}
+		return string(bytes)
+	}
+
+	extractCountsMapFromPods := func(pods []corev1.Pod) map[string]*counts {
+		result := make(map[string]*counts, len(pods))
+		for i := range pods {
+			pod := pods[i]
+			key := mapToJSON(t, pod.Spec.NodeSelector)
+			if _, found := result[key]; !found {
+				result[key] = &counts{
+					NodeSelector: maps.Clone(pod.Spec.NodeSelector),
+					Count:        0,
+				}
+			}
+			result[key].Count++
+		}
+		return result
+	}
+
+	testCases := map[string]struct {
+		workloads  []kueue.Workload
+		pods       []corev1.Pod
+		wantPods   []corev1.Pod
+		wantCounts []counts
+		wantErr    error
+	}{
+		"ungate single pod": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 1).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(1).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+			},
+		},
+		"ungate multiple pods in a single domain": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 3).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(3).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 3,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 2,
+				},
+			},
+		},
+		"ungate multiple pods across multiple domains": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 2).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(2).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r2",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r2",
+					},
+					Count: 1,
+				},
+			},
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			clientBuilder := utiltesting.NewClientBuilder().WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
+			if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+				t.Fatalf("Could not setup indexes: %v", err)
+			}
+
+			kcBuilder := clientBuilder.WithObjects()
+			for i := range tc.pods {
+				kcBuilder = kcBuilder.WithObjects(&tc.pods[i])
+			}
+
+			for i := range tc.workloads {
+				kcBuilder = kcBuilder.WithStatusSubresource(&tc.workloads[i])
+			}
+
+			kClient := kcBuilder.Build()
+			for i := range tc.workloads {
+				if err := kClient.Create(ctx, &tc.workloads[i]); err != nil {
+					t.Fatalf("Could not create workload: %v", err)
+				}
+			}
+			topologyUngater := newTopologyUngater(kClient)
+
+			reconcileRequest := reconcile.Request{
+				NamespacedName: client.ObjectKeyFromObject(&tc.workloads[0]),
+			}
+
+			_, err := topologyUngater.Reconcile(ctx, reconcileRequest)
+
+			if diff := gocmp.Diff(tc.wantErr, err, cmpopts.EquateErrors(), cmpopts.EquateEmpty()); diff != "" {
```

  ```suggestion
  			if diff := gocmp.Diff(tc.wantErr, err, cmpopts.EquateErrors()); diff != "" {
  ```
  
  I don't think that this `EquateEmpty` is needed.

- **pkg/controller/tas/topology_ungater_test.go:286** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:54:00Z
```diff
@@ -0,0 +1,363 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"maps"
+	"testing"
+
+	gocmp "github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	jsoniter "github.com/json-iterator/go"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/job"
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/raycluster"
+)
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var (
+	podCmpOpts = []gocmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(corev1.Pod{}, "TypeMeta", "ObjectMeta.ResourceVersion",
+			"ObjectMeta.DeletionTimestamp"),
+		cmpopts.IgnoreFields(corev1.PodCondition{}, "LastTransitionTime"),
+	}
+	defaultTestLevels = []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	// this code is meant to deal with the fact that the pod assignment to
+	// topology domains is non-deterministic (i.e. sometimes pod1 gets the
+	// assigned to domain1, but sometimes to domain2). We only care about the
+	// number of pods ungated to a given domain.
+	type counts struct {
+		NodeSelector map[string]string
+		Count        int32
+	}
+
+	mapToJSON := func(t *testing.T, m map[string]string) string {
+		json := jsoniter.Config{
+			SortMapKeys: true,
+		}.Froze()
+		bytes, err := json.Marshal(m)
+		if err != nil {
+			t.Fatalf("failed to serialize map: %v, error=%s", m, err)
+		}
+		return string(bytes)
+	}
+
+	extractCountsMapFromPods := func(pods []corev1.Pod) map[string]*counts {
+		result := make(map[string]*counts, len(pods))
+		for i := range pods {
+			pod := pods[i]
+			key := mapToJSON(t, pod.Spec.NodeSelector)
+			if _, found := result[key]; !found {
+				result[key] = &counts{
+					NodeSelector: maps.Clone(pod.Spec.NodeSelector),
+					Count:        0,
+				}
+			}
+			result[key].Count++
+		}
+		return result
+	}
+
+	testCases := map[string]struct {
+		workloads  []kueue.Workload
+		pods       []corev1.Pod
+		wantPods   []corev1.Pod
+		wantCounts []counts
+		wantErr    error
+	}{
+		"ungate single pod": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 1).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(1).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+			},
+		},
+		"ungate multiple pods in a single domain": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 3).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(3).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 3,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 2,
+				},
+			},
+		},
+		"ungate multiple pods across multiple domains": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 2).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(2).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r2",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r2",
+					},
+					Count: 1,
+				},
+			},
+		},
```

  Could you add cases in the following:
  
  1. workload without admission
  2. workload without topologyAssignment
  3. pod without "kueue.x-k8s.io/workload" annotation
  4. pod with unrelated gate
  5. pod with unrelated gate and "kueue.x-k8s.io/topology" gate

- **pkg/controller/tas/topology_ungater.go:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:57:21Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
```

  ```suggestion
  ```
  
  Duplicated check with the predicates.

- **pkg/controller/tas/topology_ungater.go:162** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T19:58:38Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
```

  ```suggestion
  ```
  
  Duplicated check with the predicates.

- **pkg/controller/tas/topology_ungater.go:215** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T20:01:27Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
+				allToUngate = append(allToUngate, toUngate...)
+			}
+		}
+	}
+	var err error
+	if len(allToUngate) > 0 {
+		log.V(2).Info("identified pods to ungate", "count", len(allToUngate))
+		err = parallelize.Until(ctx, len(allToUngate), func(i int) error {
+			podWithUngateInfo := &allToUngate[i]
+			e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, true, func() (bool, error) {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+				utilpod.Ungate(podWithUngateInfo.pod, kueuealpha.TopologySchedulingGate)
+				if podWithUngateInfo.pod.Spec.NodeSelector == nil {
+					podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
+				}
+				for labelKey, labelValue := range podWithUngateInfo.nodeLabels {
+					podWithUngateInfo.pod.Spec.NodeSelector[labelKey] = labelValue
+				}
+				return true, nil
+			})
+			if e != nil {
+				log.Error(e, "failed ungating pod", "pod", klog.KObj(podWithUngateInfo.pod))
+			}
+			return e
+		})
+		if err != nil {
+			return reconcile.Result{}, err
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *topologyUngater) Create(event event.CreateEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Delete(event event.DeleteEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Update(event event.UpdateEvent) bool {
+	_, isOldWl := event.ObjectOld.(*kueue.Workload)
+	newWl, isNewWl := event.ObjectNew.(*kueue.Workload)
+	if isOldWl && isNewWl {
+		return isTASWorkload(newWl)
+	}
+	return true
+}
+
+func isTASWorkload(wl *kueue.Workload) bool {
+	if wl.Status.Admission == nil {
```

  ```suggestion
  	if wl.Status.Admission == nil || slices.ContainsFunc(wl.Status.Admission.PodSetAssignments, func(psa kueue.PodSetAssignment) bool { return psa.TopologyAssignment != nil } ) {
  ```

- **pkg/controller/tas/topology_ungater.go:222** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T20:01:38Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
+				allToUngate = append(allToUngate, toUngate...)
+			}
+		}
+	}
+	var err error
+	if len(allToUngate) > 0 {
+		log.V(2).Info("identified pods to ungate", "count", len(allToUngate))
+		err = parallelize.Until(ctx, len(allToUngate), func(i int) error {
+			podWithUngateInfo := &allToUngate[i]
+			e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, true, func() (bool, error) {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+				utilpod.Ungate(podWithUngateInfo.pod, kueuealpha.TopologySchedulingGate)
+				if podWithUngateInfo.pod.Spec.NodeSelector == nil {
+					podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
+				}
+				for labelKey, labelValue := range podWithUngateInfo.nodeLabels {
+					podWithUngateInfo.pod.Spec.NodeSelector[labelKey] = labelValue
+				}
+				return true, nil
+			})
+			if e != nil {
+				log.Error(e, "failed ungating pod", "pod", klog.KObj(podWithUngateInfo.pod))
+			}
+			return e
+		})
+		if err != nil {
+			return reconcile.Result{}, err
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *topologyUngater) Create(event event.CreateEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Delete(event event.DeleteEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Update(event event.UpdateEvent) bool {
+	_, isOldWl := event.ObjectOld.(*kueue.Workload)
+	newWl, isNewWl := event.ObjectNew.(*kueue.Workload)
+	if isOldWl && isNewWl {
+		return isTASWorkload(newWl)
+	}
+	return true
+}
+
+func isTASWorkload(wl *kueue.Workload) bool {
+	if wl.Status.Admission == nil {
+		return false
+	}
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			return true
+		}
+	}
```

  ```suggestion
  ```

- **pkg/util/tas/tas.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T20:12:41Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  ```suggestion
  func DomainID(levelValues []string) (TopologyDomainID, error) {
  ```
  
  Handling error would be better.

- **pkg/util/testingjobs/pod/wrappers.go:142** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-18T20:13:42Z
```diff
@@ -135,6 +137,15 @@ func (p *PodWrapper) KueueSchedulingGate() *PodWrapper {
 	return p
 }
 
+// TopologySchedulingGate adds kueue scheduling gate to the Pod
+func (p *PodWrapper) TopologySchedulingGate() *PodWrapper {
+	if p.Spec.SchedulingGates == nil {
```

  ```suggestion
  	if len(p.Spec.SchedulingGates) == 0 {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T06:48:59Z

- **pkg/controller/tas/indexer.go:34** by [@mimowo](https://github.com/mimowo) — 2024-10-21T06:48:58Z
```diff
@@ -0,0 +1,41 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+
+	corev1 "k8s.io/api/core/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+)
+
+const (
+	workloadNameKey = "metadata.workload"
+)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return indexer.IndexField(ctx, &corev1.Pod{}, workloadNameKey, func(o client.Object) []string {
+		pod := o.(*corev1.Pod)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:06:22Z

- **pkg/controller/tas/topology_ungater.go:114** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:06:22Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
```

  I have refactored this a bit:
  - passing object instead of pod to the function to commonize between event handlers
  - I don't need to check the scheduling gate (it was just a workaround to check "is it a TAS pod")
  - proposed a new Label to use here: https://github.com/kubernetes-sigs/kueue/pull/3271

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:06:53Z

- **pkg/controller/tas/topology_ungater.go:117** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:06:53Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
```

  addressed in the comment above

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:08:05Z

- **pkg/controller/tas/topology_ungater.go:124** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:08:05Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
```

  We batch to avoid thousands of reconciles when a new job creates its pods. 
  I added a comment to ungateBatchPeriod.
  The analogous approach is used in the job controller in k8s: https://github.com/kubernetes/kubernetes/blob/e39571591d2917ca642f47ebca11253a91793c4b/pkg/controller/job/job_controller.go#L64C2-L64C20

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:08:12Z

- **pkg/controller/tas/topology_ungater.go:132** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:08:12Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
```

  thx, done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:08:54Z

- **pkg/controller/tas/topology_ungater.go:140** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:08:54Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
```

  it is debug

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:09:02Z

- **pkg/controller/tas/topology_ungater.go:156** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:09:02Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:12:06Z

- **pkg/controller/tas/topology_ungater.go:178** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:12:06Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
+				allToUngate = append(allToUngate, toUngate...)
+			}
+		}
+	}
+	var err error
+	if len(allToUngate) > 0 {
```

  How do you imagine the decoupling? One idea I have is to move out the code which for a list of pods ungates them all. It would be very similar to the code we have in pod_controller which for a list of pods cleans up finalizers: https://github.com/kubernetes-sigs/kueue/blob/4a5155ac1b39e82b26ff8e64bab09b31dc724995/pkg/controller/jobs/pod/pod_controller.go#L885-L919. In any case, I would prefer to move the refactoring to a follow up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:12:16Z

- **pkg/controller/tas/topology_ungater_test.go:333** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:12:16Z
```diff
@@ -0,0 +1,363 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"maps"
+	"testing"
+
+	gocmp "github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	jsoniter "github.com/json-iterator/go"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/job"
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/raycluster"
+)
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var (
+	podCmpOpts = []gocmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(corev1.Pod{}, "TypeMeta", "ObjectMeta.ResourceVersion",
+			"ObjectMeta.DeletionTimestamp"),
+		cmpopts.IgnoreFields(corev1.PodCondition{}, "LastTransitionTime"),
+	}
+	defaultTestLevels = []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	// this code is meant to deal with the fact that the pod assignment to
+	// topology domains is non-deterministic (i.e. sometimes pod1 gets the
+	// assigned to domain1, but sometimes to domain2). We only care about the
+	// number of pods ungated to a given domain.
+	type counts struct {
+		NodeSelector map[string]string
+		Count        int32
+	}
+
+	mapToJSON := func(t *testing.T, m map[string]string) string {
+		json := jsoniter.Config{
+			SortMapKeys: true,
+		}.Froze()
+		bytes, err := json.Marshal(m)
+		if err != nil {
+			t.Fatalf("failed to serialize map: %v, error=%s", m, err)
+		}
+		return string(bytes)
+	}
+
+	extractCountsMapFromPods := func(pods []corev1.Pod) map[string]*counts {
+		result := make(map[string]*counts, len(pods))
+		for i := range pods {
+			pod := pods[i]
+			key := mapToJSON(t, pod.Spec.NodeSelector)
+			if _, found := result[key]; !found {
+				result[key] = &counts{
+					NodeSelector: maps.Clone(pod.Spec.NodeSelector),
+					Count:        0,
+				}
+			}
+			result[key].Count++
+		}
+		return result
+	}
+
+	testCases := map[string]struct {
+		workloads  []kueue.Workload
+		pods       []corev1.Pod
+		wantPods   []corev1.Pod
+		wantCounts []counts
+		wantErr    error
+	}{
+		"ungate single pod": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 1).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(1).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+			},
+		},
+		"ungate multiple pods in a single domain": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 3).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(3).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 3,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 2,
+				},
+			},
+		},
+		"ungate multiple pods across multiple domains": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 2).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(2).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r2",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r2",
+					},
+					Count: 1,
+				},
+			},
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			clientBuilder := utiltesting.NewClientBuilder().WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
+			if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+				t.Fatalf("Could not setup indexes: %v", err)
+			}
+
+			kcBuilder := clientBuilder.WithObjects()
+			for i := range tc.pods {
+				kcBuilder = kcBuilder.WithObjects(&tc.pods[i])
+			}
+
+			for i := range tc.workloads {
+				kcBuilder = kcBuilder.WithStatusSubresource(&tc.workloads[i])
+			}
+
+			kClient := kcBuilder.Build()
+			for i := range tc.workloads {
+				if err := kClient.Create(ctx, &tc.workloads[i]); err != nil {
+					t.Fatalf("Could not create workload: %v", err)
+				}
+			}
+			topologyUngater := newTopologyUngater(kClient)
+
+			reconcileRequest := reconcile.Request{
+				NamespacedName: client.ObjectKeyFromObject(&tc.workloads[0]),
+			}
+
+			_, err := topologyUngater.Reconcile(ctx, reconcileRequest)
+
+			if diff := gocmp.Diff(tc.wantErr, err, cmpopts.EquateErrors(), cmpopts.EquateEmpty()); diff != "" {
```

  indeed, dropped

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:12:29Z

- **pkg/controller/tas/topology_ungater_test.go:286** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:12:29Z
```diff
@@ -0,0 +1,363 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"maps"
+	"testing"
+
+	gocmp "github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	jsoniter "github.com/json-iterator/go"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/job"
+	_ "sigs.k8s.io/kueue/pkg/controller/jobs/raycluster"
+)
+
+const (
+	tasBlockLabel = "cloud.com/topology-block"
+	tasRackLabel  = "cloud.com/topology-rack"
+)
+
+var (
+	podCmpOpts = []gocmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(corev1.Pod{}, "TypeMeta", "ObjectMeta.ResourceVersion",
+			"ObjectMeta.DeletionTimestamp"),
+		cmpopts.IgnoreFields(corev1.PodCondition{}, "LastTransitionTime"),
+	}
+	defaultTestLevels = []string{
+		tasBlockLabel,
+		tasRackLabel,
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	// this code is meant to deal with the fact that the pod assignment to
+	// topology domains is non-deterministic (i.e. sometimes pod1 gets the
+	// assigned to domain1, but sometimes to domain2). We only care about the
+	// number of pods ungated to a given domain.
+	type counts struct {
+		NodeSelector map[string]string
+		Count        int32
+	}
+
+	mapToJSON := func(t *testing.T, m map[string]string) string {
+		json := jsoniter.Config{
+			SortMapKeys: true,
+		}.Froze()
+		bytes, err := json.Marshal(m)
+		if err != nil {
+			t.Fatalf("failed to serialize map: %v, error=%s", m, err)
+		}
+		return string(bytes)
+	}
+
+	extractCountsMapFromPods := func(pods []corev1.Pod) map[string]*counts {
+		result := make(map[string]*counts, len(pods))
+		for i := range pods {
+			pod := pods[i]
+			key := mapToJSON(t, pod.Spec.NodeSelector)
+			if _, found := result[key]; !found {
+				result[key] = &counts{
+					NodeSelector: maps.Clone(pod.Spec.NodeSelector),
+					Count:        0,
+				}
+			}
+			result[key].Count++
+		}
+		return result
+	}
+
+	testCases := map[string]struct {
+		workloads  []kueue.Workload
+		pods       []corev1.Pod
+		wantPods   []corev1.Pod
+		wantCounts []counts
+		wantErr    error
+	}{
+		"ungate single pod": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 1).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(1).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+			},
+		},
+		"ungate multiple pods in a single domain": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 3).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(3).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 3,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 2,
+				},
+			},
+		},
+		"ungate multiple pods across multiple domains": {
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 2).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+							AssignmentPodCount(2).
+							TopologyAssignment(&kueue.TopologyAssignment{
+								Levels: defaultTestLevels,
+								Domains: []kueue.TopologyDomainAssignment{
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r1",
+										},
+									},
+									{
+										Count: 1,
+										Values: []string{
+											"b1",
+											"r2",
+										},
+									},
+								},
+							}).
+							Obj(),
+					).
+					Admitted(true).
+					Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					TopologySchedulingGate().
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*testingpod.MakePod("pod1", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+				*testingpod.MakePod("pod2", "ns").
+					Annotation(kueuealpha.WorkloadAnnotation, "unit-test").
+					Label(constants.ManagedByKueueLabel, "true").
+					Label(kueuealpha.PodSetLabel, kueue.DefaultPodSetName).
+					KueueFinalizer().
+					Obj(),
+			},
+			wantCounts: []counts{
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r1",
+					},
+					Count: 1,
+				},
+				{
+					NodeSelector: map[string]string{
+						tasBlockLabel: "b1",
+						tasRackLabel:  "r2",
+					},
+					Count: 1,
+				},
+			},
+		},
```

  Added all the test cases.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:15:22Z

- **pkg/controller/tas/topology_ungater.go:166** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:15:22Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
```

  A workload may have multiple PodSets, and I think it makes sense to allow only subset of them to use TAS.
  Also, it is to avoid race conditions when predicates schedule a reconcile, but a second later the workload is re-admitted without TAS.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:16:48Z

- **pkg/controller/tas/topology_ungater.go:162** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:16:47Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
```

  I prefer to keep the duplicate checks to avoid panicing on race conditions:
  - the predicates schedule reconcile by workload key , but then the workload is evicted
  - the workload is recreated with the same key, but admitted without TAS

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:17:22Z

- **pkg/controller/tas/topology_ungater.go:215** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:17:21Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
+				allToUngate = append(allToUngate, toUngate...)
+			}
+		}
+	}
+	var err error
+	if len(allToUngate) > 0 {
+		log.V(2).Info("identified pods to ungate", "count", len(allToUngate))
+		err = parallelize.Until(ctx, len(allToUngate), func(i int) error {
+			podWithUngateInfo := &allToUngate[i]
+			e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, true, func() (bool, error) {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+				utilpod.Ungate(podWithUngateInfo.pod, kueuealpha.TopologySchedulingGate)
+				if podWithUngateInfo.pod.Spec.NodeSelector == nil {
+					podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
+				}
+				for labelKey, labelValue := range podWithUngateInfo.nodeLabels {
+					podWithUngateInfo.pod.Spec.NodeSelector[labelKey] = labelValue
+				}
+				return true, nil
+			})
+			if e != nil {
+				log.Error(e, "failed ungating pod", "pod", klog.KObj(podWithUngateInfo.pod))
+			}
+			return e
+		})
+		if err != nil {
+			return reconcile.Result{}, err
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *topologyUngater) Create(event event.CreateEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Delete(event event.DeleteEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Update(event event.UpdateEvent) bool {
+	_, isOldWl := event.ObjectOld.(*kueue.Workload)
+	newWl, isNewWl := event.ObjectNew.(*kueue.Workload)
+	if isOldWl && isNewWl {
+		return isTASWorkload(newWl)
+	}
+	return true
+}
+
+func isTASWorkload(wl *kueue.Workload) bool {
+	if wl.Status.Admission == nil {
```

  cleaned up along the lines in a helper function `isAdmittedByTAS`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:17:58Z

- **pkg/controller/tas/topology_ungater.go:222** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:17:58Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
+				allToUngate = append(allToUngate, toUngate...)
+			}
+		}
+	}
+	var err error
+	if len(allToUngate) > 0 {
+		log.V(2).Info("identified pods to ungate", "count", len(allToUngate))
+		err = parallelize.Until(ctx, len(allToUngate), func(i int) error {
+			podWithUngateInfo := &allToUngate[i]
+			e := utilclient.Patch(ctx, r.client, podWithUngateInfo.pod, true, func() (bool, error) {
+				log.V(3).Info("ungating pod", "pod", klog.KObj(podWithUngateInfo.pod), "nodeLabels", podWithUngateInfo.nodeLabels)
+				utilpod.Ungate(podWithUngateInfo.pod, kueuealpha.TopologySchedulingGate)
+				if podWithUngateInfo.pod.Spec.NodeSelector == nil {
+					podWithUngateInfo.pod.Spec.NodeSelector = make(map[string]string)
+				}
+				for labelKey, labelValue := range podWithUngateInfo.nodeLabels {
+					podWithUngateInfo.pod.Spec.NodeSelector[labelKey] = labelValue
+				}
+				return true, nil
+			})
+			if e != nil {
+				log.Error(e, "failed ungating pod", "pod", klog.KObj(podWithUngateInfo.pod))
+			}
+			return e
+		})
+		if err != nil {
+			return reconcile.Result{}, err
+		}
+	}
+	return reconcile.Result{}, nil
+}
+
+func (r *topologyUngater) Create(event event.CreateEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Delete(event event.DeleteEvent) bool {
+	wl, isWl := event.Object.(*kueue.Workload)
+	if isWl {
+		return isTASWorkload(wl)
+	}
+	return true
+}
+
+func (r *topologyUngater) Update(event event.UpdateEvent) bool {
+	_, isOldWl := event.ObjectOld.(*kueue.Workload)
+	newWl, isNewWl := event.ObjectNew.(*kueue.Workload)
+	if isOldWl && isNewWl {
+		return isTASWorkload(newWl)
+	}
+	return true
+}
+
+func isTASWorkload(wl *kueue.Workload) bool {
+	if wl.Status.Admission == nil {
+		return false
+	}
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			return true
+		}
+	}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:19:39Z

- **pkg/util/testingjobs/pod/wrappers.go:142** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:19:39Z
```diff
@@ -135,6 +137,15 @@ func (p *PodWrapper) KueueSchedulingGate() *PodWrapper {
 	return p
 }
 
+// TopologySchedulingGate adds kueue scheduling gate to the Pod
+func (p *PodWrapper) TopologySchedulingGate() *PodWrapper {
+	if p.Spec.SchedulingGates == nil {
```

  Actually, the `if` is not needed at all. Dropped, and used the `Gate` function instead.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T07:22:31Z

- **pkg/util/tas/tas.go:25** by [@mimowo](https://github.com/mimowo) — 2024-10-21T07:22:31Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  I think handling error in all places calling the function would pollute the code unnecessarily.
  This function can only panic if the input is an empty list. So, I think it is analogous to panic when one accesses out-of-bounds index. 
  WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-21T09:13:55Z

- **pkg/util/tas/tas.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-21T09:13:55Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  I agree that every error returning could introduce the redundantly to a whole of codes.
  But that is Go specific characteristic. Go does not provide the try-and-catch. Go enforces us with error handling.
  
  https://go.dev/wiki/CodeReviewComments#dont-panic

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T09:40:25Z

- **pkg/util/tas/tas.go:25** by [@mimowo](https://github.com/mimowo) — 2024-10-21T09:40:25Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  ok, in that case, how about just logging an error and returning domain wrapping an empty string?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-21T12:02:12Z

- **pkg/controller/tas/topology_ungater.go:114** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-21T12:02:12Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
```

  As I mentioned in https://github.com/kubernetes-sigs/kueue/pull/3271#issuecomment-2426461284, we will go with the TAS label solution during the Alpha phase.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-21T12:19:11Z

- **pkg/controller/tas/topology_ungater.go:124** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-21T12:19:11Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
```

  That sounds good to me.
  Thank you for the clarification.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-21T13:02:08Z

- **pkg/util/tas/tas.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-21T13:02:08Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  In that case, will we check if the returned value is not empty at the top level, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T13:09:17Z

- **pkg/util/tas/tas.go:25** by [@mimowo](https://github.com/mimowo) — 2024-10-21T13:09:17Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  I was going to propose we don't check at all, just rely on the fact we never call it with empty values. 
  If we are to check if the result is empty or not, then I prefer to return error.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-21T13:24:33Z

- **pkg/util/tas/tas.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-21T13:24:33Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  +1 on logging only since the empty returned value will not affect on the TopologyUngator behavior: 
  
  ```
  domainID := utiltas.DomainID(levelValues)
  domainIDToUngatedCnt[domainID]++
  ```
  
  https://go.dev/play/p/_cfMRLA8q6m

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-21T15:45:36Z

- **pkg/util/tas/tas.go:25** by [@mimowo](https://github.com/mimowo) — 2024-10-21T15:45:35Z
```diff
@@ -0,0 +1,46 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"strings"
+)
+
+type TopologyDomainID string
+
+func DomainID(levelValues []string) TopologyDomainID {
```

  dropped the check

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-22T02:51:36Z

The whole mechanism looks excellent. But I left trivial comments.
@mimowo Could you open a follow-up PR?
/lgtm
/approve

- **pkg/controller/tas/topology_ungater.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T02:21:47Z
```diff
@@ -0,0 +1,326 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"errors"
+	"slices"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+var (
+	errPendingUngateOps = errors.New("pending ungate operations")
+)
+
+type topologyUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
```

  ```suggestion
  var _ predicate.Predicate = (*topologyUngator)(nil)
  
  ```

- **pkg/controller/tas/topology_ungater.go:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T02:23:41Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
```

  That makes sense. After refactoring, podHandler has never been checked if the workload has topologyAssignment.

- **pkg/controller/tas/topology_ungater.go:178** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T02:44:01Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
+
+	allToUngate := make([]podWithUngateInfo, 0)
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment != nil {
+			toUngate, err := r.podsetPodsToUngate(ctx, log, wl, &psa)
+			if err != nil {
+				log.Error(err, "failed to identify pods to ungate", "podset", psa.Name, "count", psa.Count)
+				return reconcile.Result{}, err
+			} else {
+				log.Info("identified pods to ungate for podset", "podset", psa.Name, "count", len(toUngate))
+				allToUngate = append(allToUngate, toUngate...)
+			}
+		}
+	}
+	var err error
+	if len(allToUngate) > 0 {
```

  I'm wondering if we can consolidate expectations and podWithUngateInfo into the TAS dedicated cache.
  And then, we can decouple TASController including reconciler, eventHandler, and Predicator, and TASUngator including this ungating processes.
  Becuase ideally, in general, we should not spawn the separate go routine in the reconciler. The separate Go routine could delay the next Reconcile and, the object information in reconcile queue could older.

- **pkg/controller/tas/topology_ungater.go:162** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T02:48:33Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+type topologyUngater struct {
+	client client.Client
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get
+
+func newTopologyUngater(c client.Client) *topologyUngater {
+	return &topologyUngater{
+		client: c,
+	}
+}
+
+func (r *topologyUngater) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	podHandler := podHandler{}
+	return TASTopologyUngater, ctrl.NewControllerManagedBy(mgr).
+		Named(TASTopologyUngater).
+		For(&kueue.Workload{}).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
+		WithEventFilter(r).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.ClusterQueue{}, cfg))
+}
+
+var _ handler.EventHandler = (*podHandler)(nil)
+
+type podHandler struct {
+}
+
+func (h *podHandler) Create(_ context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldPod, isOldPod := e.ObjectOld.(*corev1.Pod)
+	newPod, isNewPod := e.ObjectNew.(*corev1.Pod)
+	if !isOldPod || !isNewPod {
+		return
+	}
+	h.queueReconcileForPod(oldPod, q)
+	h.queueReconcileForPod(newPod, q)
+}
+
+func (h *podHandler) Delete(_ context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := e.Object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	h.queueReconcileForPod(pod, q)
+}
+
+func (h *podHandler) queueReconcileForPod(pod *corev1.Pod, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	if pod == nil {
+		return
+	}
+	if !utilpod.HasGate(pod, kueuealpha.TopologySchedulingGate) {
+		return
+	}
+	if wlName, found := pod.Annotations[kueuealpha.WorkloadAnnotation]; found {
+		q.AddAfter(reconcile.Request{NamespacedName: types.NamespacedName{
+			Name:      wlName,
+			Namespace: pod.Namespace,
+		}}, ungateBatchPeriod)
+	}
+}
+
+func (h *podHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx).WithValues("workload", req.NamespacedName.Name)
+	log.V(2).Info("Reconcile Topology Ungater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		log.Info("workload not found")
+		return reconcile.Result{}, nil
+	}
+	if wl.Status.Admission == nil {
+		log.Info("workload is not admitted")
+		return reconcile.Result{}, nil
+	}
```

  As I commented above, after refactoring, these verifications seem to be needed.

- **pkg/util/tas/tas.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-22T02:50:16Z
  Implementing unit testing for the exposed function should be better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-23T07:57:37Z

- **pkg/controller/tas/topology_ungater.go:70** by [@mimowo](https://github.com/mimowo) — 2024-10-23T07:57:37Z
```diff
@@ -0,0 +1,326 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"context"
+	"errors"
+	"slices"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ungateBatchPeriod = time.Second
+)
+
+var (
+	errPendingUngateOps = errors.New("pending ungate operations")
+)
+
+type topologyUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+}
+
+type podWithUngateInfo struct {
+	pod        *corev1.Pod
+	nodeLabels map[string]string
+}
+
+var _ reconcile.Reconciler = (*topologyUngater)(nil)
+
```

  Done in the follow up https://github.com/kubernetes-sigs/kueue/pull/3291
