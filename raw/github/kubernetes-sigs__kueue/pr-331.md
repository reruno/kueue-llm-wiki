# PR #331: KEP-78: Dynamically reclaiming resources

**Summary**: KEP-78: Dynamically reclaiming resources

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/331

**Last updated**: 2023-05-09T10:19:18Z

---

## Metadata

- **State**: closed
- **Author**: [@thisisprasad](https://github.com/thisisprasad)
- **Branch**: `dynamically-reclaim-resources` → `main`
- **Created**: 2022-08-15T15:22:57Z
- **Updated**: 2023-05-09T10:19:18Z
- **Merged**: —
- **Closed**: 2023-05-09T10:19:16Z
- **Labels**: `size/L`, `ok-to-test`, `do-not-merge/work-in-progress`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+152 / -0**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
One-line PR description: This KEP proposes an approach to reclaim unused resources of an unfinished job of cluster-queue in an incremental manner, so that all the resources of the cluster-queue can used efficiently.

Issue link: Dynamically reclaiming resources #78 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-15T15:23:05Z

Hi @thisisprasad. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@thisisprasad](https://github.com/thisisprasad) — 2022-08-15T16:16:29Z

/assign @alculquicondor

### Comment by [@thisisprasad](https://github.com/thisisprasad) — 2022-08-15T17:57:06Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-15T17:57:19Z

@thisisprasad: Cannot trigger testing until a trusted user reviews the PR and leaves an `/ok-to-test` message.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/331#issuecomment-1215514253):

>/ok-to-test


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-08-16T06:40:46Z

/ok-to-test

### Comment by [@denkensk](https://github.com/denkensk) — 2022-08-16T07:07:46Z

/cc

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-17T17:38:13Z

I'm currently focusing 100% on the 0.2.0 release. I'll get back to this hopefully next week.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-19T15:07:57Z

@thisisprasad any progress on the review?

### Comment by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-19T15:47:30Z

Yes, worked on changes as per review comments. Minor changes such as test-plans and implementation related changes are remaining, working on that. Will update the PR.

### Comment by [@thisisprasad](https://github.com/thisisprasad) — 2022-10-29T17:16:55Z

@alculquicondor @kerthcet  I have an approach for job with multi parallel and completion type suggested [here](https://github.com/kubernetes-sigs/kueue/pull/331#discussion_r1004824607). Would like to know your comments on it.

I have one doubt on updating/reconciling the workload `.status` based on the jobs `.succeeded` field. How should the workload reconcile when job's `.succeeded` field is updated. Should the workload controller catch events of job update or should the workload controller watch the kueue's job reconciler. Or is there a better way to handle this. Can you please guide me on this issue. Finding it difficult to figure out the best way out here.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-31T13:23:23Z

In general, kueue's job controller should observe the job and update something in the workload.

Then the workload reconciler would catch those updates.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T20:07:25Z

> Should the workload controller catch events of job update or should the workload controller watch the kueue's job reconciler. 

The job reconciler should understand Job and put that information into the Workload. From there, the Workload event handler can update the usage of the Workload in the cache.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-11-18T19:06:21Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/331#" title="Author self-approved">thisisprasad</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from alculquicondor by writing `/assign @alculquicondor` in a comment. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["alculquicondor"]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T13:26:15Z

@thisisprasad, are you still working on this? or do you need someone to assist?

### Comment by [@thisisprasad](https://github.com/thisisprasad) — 2022-12-14T18:16:42Z

@alculquicondor I'm currently not able to give enough time to work on this KEP. I will start working on this from next week.
Assistance from someone would be very much helpful.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T18:34:32Z

@mwielgus is interested in taking this over. Would you mind if we reassign?

### Comment by [@thisisprasad](https://github.com/thisisprasad) — 2022-12-14T18:56:18Z

I think I have taken enough time to work on this topic/proposal. 
I wouldn't mind anybody who is interested to work on this topic taking this over. Would be really happy to see this feature in-action in Kueue!

### Comment by [@thisisprasad](https://github.com/thisisprasad) — 2022-12-16T17:24:58Z

@alculquicondor Should I close this PR?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:54:09Z

It's fine. We can close it when there is a replacement PR up.

### Comment by [@k8s-triage-robot](https://github.com/k8s-triage-robot) — 2023-03-19T15:04:51Z

The Kubernetes project currently lacks enough contributors to adequately respond to all PRs.

This bot triages PRs according to the following rules:
- After 90d of inactivity, `lifecycle/stale` is applied
- After 30d of inactivity since `lifecycle/stale` was applied, `lifecycle/rotten` is applied
- After 30d of inactivity since `lifecycle/rotten` was applied, the PR is closed

You can:
- Mark this PR as fresh with `/remove-lifecycle stale`
- Close this PR with `/close`
- Offer to help out with [Issue Triage][1]

Please send feedback to sig-contributor-experience at [kubernetes/community](https://github.com/kubernetes/community).

/lifecycle stale

[1]: https://www.kubernetes.dev/docs/guide/issue-triage/

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-20T02:03:23Z

/remove-lifecycle stale

If this is on the plan of v0.4.0, I can work on this.
/assign

### Comment by [@trasc](https://github.com/trasc) — 2023-05-09T10:19:13Z

/close 
In favor of #742

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-05-09T10:19:18Z

@trasc: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/331#issuecomment-1539874417):

>/close 
>In favor of #742


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-05T07:53:53Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:45** by [@kerthcet](https://github.com/kerthcet) — 2022-09-02T06:53:38Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
```

  Seems like they are the requirements not the goals.

- **enhancements/78-dynamically-reclaiming-resources/README.md:82** by [@kerthcet](https://github.com/kerthcet) — 2022-09-05T05:57:50Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
```

  Different pods belong to different podSets request different resources. Only recording the pods number seems not enough. Maybe we can reuse `PodSets` here, but considering not every field of podSpec is needed, or we can define a simplified struct.

- **enhancements/78-dynamically-reclaiming-resources/README.md:50** by [@kerthcet](https://github.com/kerthcet) — 2022-09-05T07:53:33Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
```

  Some cases I didn't met but maybe worth considering: when job failed, all the pods should be restarted. If so, reclaiming the resources of succeed pods is not expected. Then we should provide an optional field for end-users to choose, like define a `reclaimPolicy` in workload. Default to `reclaim`

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-06T16:06:09Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:82** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-06T16:06:09Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
```

  PodSet consist of name, podSpec and count. What fields can be reused here?
  
  Is there a way to list pods belonging to a particular `PodSet`? If yes, then we can have counts of completed pods w.r.t PodSet. If there is no way, we can think of adding one.
  
  We can have a struct field which can count the:
  1. resources owned by a job
  2. count of completed pods segregated according to PodSets.
  
  What do you think?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-07T15:41:55Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:23** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-29T20:03:33Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
```

  ```suggestion
  This proposal allows for the resources of pods of a Workload to be reclaimed after successful completion of each of the Pods of the Workload.
  ```

- **enhancements/78-dynamically-reclaiming-resources/README.md:26** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-29T20:04:39Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
```

  This wording is a bit confusing. I would simplify and say something like:
  
  "Freeing resources earlier may allow more workloads to be admitted".

- **enhancements/78-dynamically-reclaiming-resources/README.md:29** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-29T20:20:44Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
```

  This should be enough.
  
  ```suggestion
  In the remaining of this document, *pods of a Workload* means the same as *pods of a Job* and vice-versa. 
  ```

- **enhancements/78-dynamically-reclaiming-resources/README.md:55** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-29T20:28:09Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
```

  ```suggestion
  the status of the Workload based on the Job status.
  ```

- **enhancements/78-dynamically-reclaiming-resources/README.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-29T20:28:44Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
```

  Remove this line. This should be standard process, it's not part of the proposal.

- **enhancements/78-dynamically-reclaiming-resources/README.md:45** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:22:28Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
```

  Non-goals are related features that this proposal does not intend to cover.
  
  For example, do you plan to support partial job preemption (a subset of the job pods preempted)

- **enhancements/78-dynamically-reclaiming-resources/README.md:50** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:25:01Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
```

  I think this depends a lot on the job implementation. For example, the job controller would continue creating pods until the backoffLimit is exceeded. And this is not something that kueue can control.
  
  But I agree that pod failure scenarios should be better clarified.

- **enhancements/78-dynamically-reclaiming-resources/README.md:54** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:25:53Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
```

  ```suggestion
  successfully completed its execution. The kueue job controller is responsible for updating
  ```
  
  or did you mean the kubernetes job controller?

- **enhancements/78-dynamically-reclaiming-resources/README.md:66** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:29:21Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
```

  You are basically saying that we should not have bugs :)
  
  That is right, but that's not a risk on the design.
  
  Example of risks are: dramatically increasing the reconciliation time, or using too much memory, or increasing the size of the CRD.
  
  A risk can also be about a corner case that is not well understood. But you should be more specific about which case that is and how we can handle the problem.
  
  It could also be the case that the design is simple enough that there shouldn't be any risks :)

- **enhancements/78-dynamically-reclaiming-resources/README.md:82** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:38:19Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
```

  at the very least, this should be a list map that has the podset name and the number of pods.
  
  Also, as already stated above, we can't treat failed and succeeded pods the same way, as failed pods are generally recreated.
  
  Maybe kueue only needs to know about succeeded pods for v1.Job

- **enhancements/78-dynamically-reclaiming-resources/README.md:102** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:39:44Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
+  // +optional
+  CompletedPods int32 `json:"completedPods"`
+}
+```
+
+#### CompletedPods
+
+`CompletedPods` stores the count of pods which have successfully completed its execution.
+
+High level execution flow:
+1. The completed pods in a Job is the count of Succeeded pods => sum(succeeded).
+2. Update the Workload status object if job.sum(Succeeded) > wl.Status.CompletedPods
+3. Handle the update event of Workload and update Workload status in cache.
+4. Update clusterQueue quota for resource flavor for requests of Pod of a Workload in cache.
+
+## Graduation Criteria
+
+* The features have been stable and reliable in the past several releases.
+* Adequate documentation exists for the features.
+* Test coverage of the features is acceptable.
```

  We are not doing feature gates, so I think we can remove this section.

- **enhancements/78-dynamically-reclaiming-resources/README.md:115** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:40:15Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
+  // +optional
+  CompletedPods int32 `json:"completedPods"`
+}
+```
+
+#### CompletedPods
+
+`CompletedPods` stores the count of pods which have successfully completed its execution.
+
+High level execution flow:
+1. The completed pods in a Job is the count of Succeeded pods => sum(succeeded).
+2. Update the Workload status object if job.sum(Succeeded) > wl.Status.CompletedPods
+3. Handle the update event of Workload and update Workload status in cache.
+4. Update clusterQueue quota for resource flavor for requests of Pod of a Workload in cache.
+
+## Graduation Criteria
+
+* The features have been stable and reliable in the past several releases.
+* Adequate documentation exists for the features.
+* Test coverage of the features is acceptable.
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of kueue's prow CI/CD pipeline.
+
+### Unit Tests
+Here is a list of unit tests for various modules of the feature:
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Integration tests for Job controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go)
```

  What would the new integration tests look like? what will they test?

- **enhancements/78-dynamically-reclaiming-resources/kep.yaml:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T15:41:52Z
```diff
@@ -0,0 +1,18 @@
+title: Dynamically Reclaiming Resources
+kep-number: 78
+authors:
+  - "@thisisprasad"
+owning-sig: sig-scheduling
+reviewers:
+  - "@alculquicondor"
+  - "@kerthcet"
+approvers:
+  - "@ahg-g"
+  - "@alculquicondor"
+editor: Prasad Saraf
+creation-date: 2022-08-15
+last-updated: 2022-08-15
```

  We don't have these field in kubernetes/enhancements

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:13:35Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:45** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:13:34Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
```

  Done.
  
  Added two non-goals, 1. partial job preemption. 2. Partial workload admission

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:16:00Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:23** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:16:00Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
```

  Done

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:16:31Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:26** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:16:30Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
```

  Made the wording simpler.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:16:39Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:29** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:16:39Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
```

  Done

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:16:45Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:55** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:16:44Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
```

  Done

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:16:54Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:57** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:16:53Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
```

  Removed

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:17:23Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:54** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:17:22Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
```

  I meant Kueue's Job controller only. Made the appropriate change.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:20:18Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:66** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:20:18Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
```

  As per the changes proposed, I don't see any risk that might crash any component of Kueue or suddenly increases consumption underlying system's resources.
  Hence, removed the section..

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:20:29Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:102** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:20:28Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
+  // +optional
+  CompletedPods int32 `json:"completedPods"`
+}
+```
+
+#### CompletedPods
+
+`CompletedPods` stores the count of pods which have successfully completed its execution.
+
+High level execution flow:
+1. The completed pods in a Job is the count of Succeeded pods => sum(succeeded).
+2. Update the Workload status object if job.sum(Succeeded) > wl.Status.CompletedPods
+3. Handle the update event of Workload and update Workload status in cache.
+4. Update clusterQueue quota for resource flavor for requests of Pod of a Workload in cache.
+
+## Graduation Criteria
+
+* The features have been stable and reliable in the past several releases.
+* Adequate documentation exists for the features.
+* Test coverage of the features is acceptable.
```

  Removed

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:20:56Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:115** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:20:56Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
+  // +optional
+  CompletedPods int32 `json:"completedPods"`
+}
+```
+
+#### CompletedPods
+
+`CompletedPods` stores the count of pods which have successfully completed its execution.
+
+High level execution flow:
+1. The completed pods in a Job is the count of Succeeded pods => sum(succeeded).
+2. Update the Workload status object if job.sum(Succeeded) > wl.Status.CompletedPods
+3. Handle the update event of Workload and update Workload status in cache.
+4. Update clusterQueue quota for resource flavor for requests of Pod of a Workload in cache.
+
+## Graduation Criteria
+
+* The features have been stable and reliable in the past several releases.
+* Adequate documentation exists for the features.
+* Test coverage of the features is acceptable.
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of kueue's prow CI/CD pipeline.
+
+### Unit Tests
+Here is a list of unit tests for various modules of the feature:
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Integration tests for Job controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go)
```

  Added the required test scenarios.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:21:58Z

- **enhancements/78-dynamically-reclaiming-resources/kep.yaml:14** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:21:58Z
```diff
@@ -0,0 +1,18 @@
+title: Dynamically Reclaiming Resources
+kep-number: 78
+authors:
+  - "@thisisprasad"
+owning-sig: sig-scheduling
+reviewers:
+  - "@alculquicondor"
+  - "@kerthcet"
+approvers:
+  - "@ahg-g"
+  - "@alculquicondor"
+editor: Prasad Saraf
+creation-date: 2022-08-15
+last-updated: 2022-08-15
```

  Removed

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:27:13Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:50** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:27:13Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
```

  Explained pod failure scenarios w.r.t fields restartPolicy, backoffLimit and activeDeadlineSeconds. But, as proposed, reclaiming of failed resources will not be done unless the job gets completed as either Failed or Succeeded.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-22T15:29:05Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:82** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-22T15:29:05Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
+
+Add a new field to the status of the Workload to count successful completion of 
+pods of the Workload. The Job controller monitors the count of pods which have
+successfully completed its execution. The Job controller is responsible for updating
+the status of the Workload object appropriately.
+
+Update our documentation to reflect the new enhancement.
+
+### Risks and Mitigations
+
+There is a change in the functionality of reclaiming the cluster-queue resources.
+The resources of a Job are reclaimed by kueue in an incremental manner(resources of each
+Pod of a Job are reclaimed separately), as opposed to reclaiming the whole resources 
+of the Job after its completion.
+
+The update logic to reclaim resources after a Job completion must be handled correctly.
+
+### API
+
+A new field `CompletedPods` is added to the `WorkloadStatus` struct.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  // conditions hold the latest available observations of the Workload
+  // current state.
+  // +optional
+  // +listType=map
+  // +listMapKey=type
+  Conditions []WorkloadCondition `json:"conditions,omitempty"`
+  
+  // The number of pods which reached phase Succeeded or Failed.
```

  Yes, kueue should only know about succeed pods to reclaim resources. Failed pod resources will stay with the job until the job is finished.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-23T19:32:26Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-23T19:25:09Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
```

  We added a template now https://github.com/kubernetes-sigs/kueue/tree/main/keps/NNNN-template
  
  Please move the file to keps/

- **enhancements/78-dynamically-reclaiming-resources/README.md:43** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-23T19:26:07Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
```

  ```suggestion
  - Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
  ```

- **enhancements/78-dynamically-reclaiming-resources/README.md:30** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-23T19:26:52Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
```

  Just try to be consistent

- **enhancements/78-dynamically-reclaiming-resources/README.md:48** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-23T19:27:17Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
```

  ```suggestion
  Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
  ```

- **enhancements/78-dynamically-reclaiming-resources/README.md:101** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-23T19:30:42Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
```

  why do we need the pod names? That could be a lot of data.

- **enhancements/78-dynamically-reclaiming-resources/README.md:99** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-23T19:31:17Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
```

  Prefer an array to a map, like we are doing for conditions

- **enhancements/78-dynamically-reclaiming-resources/README.md:110** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-23T19:32:21Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` holds the mapping between PodSet name to the count of Pods and Pod names that have successfully completed its execution and their resources have been reclaimed by Kueue. The names of Pods are recorded to identify the Pods whose resources have been reclaimed by Kueue. If a name of a Pod already exists in the list then the resources associated with the succeeded Pod have already been reclaimed by Kueue.
+
+## Implementation
+
+The Workload reconciler will keep a watch(or `Watches()`) on the Pods of the Job. The Workload reconciler will process the events of the Pods and update `.status.reclaimedPodSetPods` field accordingly.
```

  In the case of Job, why not just watch the Job status and observe the `.succeeded` field?

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-26T16:48:18Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:99** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-26T16:48:17Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
```

  Any reason to prefer array over map. Like what are cons of using a map here?
  
  We want to keep mapping between PodSet name and its completed pods. Can you tell how do you want to organize the data using array?

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-09-26T17:13:04Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:101** by [@thisisprasad](https://github.com/thisisprasad) — 2022-09-26T17:13:03Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
```

  Pod names was kept to record the pods whose resources are reclaimed and avoid reclaiming again. But I agree, that could be lot pods.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-29T13:24:28Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:101** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-29T13:24:28Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
```

  The job controller API already counts the number of finished pods. Why not use that? https://kubernetes.io/docs/concepts/workloads/controllers/job/#job-tracking-with-finalizers

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-29T13:35:40Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:99** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-29T13:35:40Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
```

  https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#lists-of-named-subobjects-preferred-over-maps
  
  You can use `PodSet` as an example: https://github.com/kubernetes-sigs/kueue/blob/main/apis/kueue/v1alpha2/workload_types.go#L26-L33

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-10-03T18:00:53Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:99** by [@thisisprasad](https://github.com/thisisprasad) — 2022-10-03T18:00:53Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
```

  thanks for the material. This clears my doubt.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-10-10T20:28:08Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:1** by [@thisisprasad](https://github.com/thisisprasad) — 2022-10-10T20:28:08Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
```

  Moved to keps/ directory.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-10-10T20:29:48Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:110** by [@thisisprasad](https://github.com/thisisprasad) — 2022-10-10T20:29:48Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` holds the mapping between PodSet name to the count of Pods and Pod names that have successfully completed its execution and their resources have been reclaimed by Kueue. The names of Pods are recorded to identify the Pods whose resources have been reclaimed by Kueue. If a name of a Pod already exists in the list then the resources associated with the succeeded Pod have already been reclaimed by Kueue.
+
+## Implementation
+
+The Workload reconciler will keep a watch(or `Watches()`) on the Pods of the Job. The Workload reconciler will process the events of the Pods and update `.status.reclaimedPodSetPods` field accordingly.
```

  Yes, changed the approach and watching the `.succeeded`field of the job status.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-11T03:37:29Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:50** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T03:37:28Z
```diff
@@ -0,0 +1,122 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Risks and Mitigations](#risks-and-mitigations)
+- [API](#api)
+- [Graduation Criteria](#graduation-criteria)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+The resources of pods of a Workload are reclaimed by kueue after successful completion of the Pod. 
+This is beneficial for workloads which cannot proceed due to the unavailability 
+of the little resources for its corresponding pods and Workload does not get 
+admitted.
+
+Here after, *pods of a Workload* are synonymously used to refer *pods of a Job* and vice-versa because, 
+Job manages the Workload and the pods associated with Job are associated with the Workload. 
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes.
+For jobs with multiple pods, the resources will be reclaimed only after the last Pod
+of the Job finishes. This is not efficient as the pods of parallel Job may have laggards
+consuming the unused resources of the jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the Job which running in kueue.
+
+### Non-Goals
+
+- A running Job on queue should not be preempted.
+- The resources that are being used by a Job should not be allocated to any other Job.
+
+## Proposal
+
+Reclaim the resources of the succeeded pods of a Job as soon as it completes its execution.
```

  Let me explain this clearly: when running some kinds of workloads(e.g. HPC), we can't tolerate pod failure, then we'll restart all the pods, at that moment, if some pods got succeed and we reclaimed their resources, with some probabilities, these pods may failed in creation for insufficient resources. 
  
  But I'm ok with current implementations for a startup until we come up with more user cases.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-11T06:09:30Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:43** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T04:01:19Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
```

  I think this hasn't been addressed.

- **enhancements/78-dynamically-reclaiming-resources/README.md:43** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T04:13:23Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
```

  Oh, you have another file. Then remove this before merge. Friendly reminding.

- **keps/78-dynamically-reclaiming-resources/README.md:1** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T04:13:59Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
```

  It might be a little too strict, but better to keep the format consistent like `# KEP-NNNN: Your short, descriptive title`

- **keps/78-dynamically-reclaiming-resources/README.md:27** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T04:15:43Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
```

  s/a successfully completed Pod/successfully completed Pods/

- **keps/78-dynamically-reclaiming-resources/README.md:43** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T04:17:35Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
```

  Maybe also add `Supporting variety reclaiming policies`.

- **keps/78-dynamically-reclaiming-resources/README.md:100** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T05:40:23Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
```

  Or `ReclaimedPodSet`? And rename `ReclaimedPodSetPods` to `ReclaimedPosSets` in WorkloadStatus.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-11T06:59:29Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:110** by [@kerthcet](https://github.com/kerthcet) — 2022-10-11T06:59:29Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` holds the mapping between PodSet name to the count of Pods and Pod names that have successfully completed its execution and their resources have been reclaimed by Kueue. The names of Pods are recorded to identify the Pods whose resources have been reclaimed by Kueue. If a name of a Pod already exists in the list then the resources associated with the succeeded Pod have already been reclaimed by Kueue.
+
+## Implementation
+
+The Workload reconciler will keep a watch(or `Watches()`) on the Pods of the Job. The Workload reconciler will process the events of the Pods and update `.status.reclaimedPodSetPods` field accordingly.
```

  If we want to support other Jobs, we should watch them all, right? Why not achieve this through job reconciling. Today when job completed, we'll update workloads in Job reconciler, and workload controller will get update events. I think it's similar.
  
  Actually, I prefer the original design, by watching the Pods of Job. This is self closed-loop with kueue. And if other Jobs don't support `.status.succeeded` field, they have to implement it when integrating with kueue.  
  
  But the disadvantage is we don't know whether this is as expected. For batch Job, we know that the succeeded pods can be reclaimed, but for other Jobs, we don't know.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-11T19:21:25Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T18:31:00Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
```

  Remove this file

- **enhancements/78-dynamically-reclaiming-resources/kep.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T18:31:16Z
```diff
@@ -0,0 +1,16 @@
+title: Dynamically Reclaiming Resources
```

  Remove this file too

- **keps/78-dynamically-reclaiming-resources/README.md:43** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T18:58:47Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
```

  Do you mean excluding support for APIs that are not `batch/v1.Job`? If so, I'm ok with that.

- **keps/78-dynamically-reclaiming-resources/README.md:94** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:13:33Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
```

  ```suggestion
      ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
  ```
  
  This name might be enough

- **keps/78-dynamically-reclaiming-resources/README.md:62** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:14:48Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
```

  Note that parallelism is not the same as completions.
  
  Did you analyze the case where parallelism=N and completions=M, where N<M?
  
  If a pod finishes successfully, it doesn't necessarily mean that the job controller won't create another one for it.

- **keps/78-dynamically-reclaiming-resources/README.md:98** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:16:07Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
```

  ```suggestion
  // belonging to the PodSet whose resources can be reclaimed.
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:106** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:16:44Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
```

  This is already explained above, so no need to repeat it.

- **keps/78-dynamically-reclaiming-resources/README.md:110** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:17:49Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
+
+## Implementation
+
+The workload reconciler will keep a watch(or `Watches()`) on the Job's `.status.succeeded` field. The Workload reconciler will calculate the difference between the Job's `.status.succeeded` field and Workload's `.status.reclaimedPodSetPods[i].count` field for every PodSet of the workload. With the former value being greater than the later, Kueue will reclaim the resources of the excess succeeded Pods.
```

  The workload reconciler doesn't watch Job.
  
  Did you mean the job reconciler?
  
  Shouldn't `count` be updated based on `succeeded`? Although, it's unclear what to do when `parallelism != completions`

- **keps/78-dynamically-reclaiming-resources/README.md:130** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:20:21Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
+
+## Implementation
+
+The workload reconciler will keep a watch(or `Watches()`) on the Job's `.status.succeeded` field. The Workload reconciler will calculate the difference between the Job's `.status.succeeded` field and Workload's `.status.reclaimedPodSetPods[i].count` field for every PodSet of the workload. With the former value being greater than the later, Kueue will reclaim the resources of the excess succeeded Pods.
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of Kueue's prow CI/CD pipeline.
+
+### Unit Tests
+
+All the Kueue's core components must be covered by unit tests.  Here is a list of unit tests required for the modules of the feature:
+
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Kueue Job Controller
+  - checking the resources owned by a Job are released to the cache and clusterQueue when a Pod of the Job succeed.
+  - Integration tests for Job controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go).
+
+* Workload Controller
+  - A pending Workload should be admitted when enough resources are available after release of resources by the succeeded Pods of the parallel Jobs.
```

  these tests don't have the scheduler running, so you couldn't verify this here.

- **keps/78-dynamically-reclaiming-resources/README.md:142** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:21:19Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
+
+## Implementation
+
+The workload reconciler will keep a watch(or `Watches()`) on the Job's `.status.succeeded` field. The Workload reconciler will calculate the difference between the Job's `.status.succeeded` field and Workload's `.status.reclaimedPodSetPods[i].count` field for every PodSet of the workload. With the former value being greater than the later, Kueue will reclaim the resources of the excess succeeded Pods.
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of Kueue's prow CI/CD pipeline.
+
+### Unit Tests
+
+All the Kueue's core components must be covered by unit tests.  Here is a list of unit tests required for the modules of the feature:
+
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Kueue Job Controller
+  - checking the resources owned by a Job are released to the cache and clusterQueue when a Pod of the Job succeed.
+  - Integration tests for Job controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go).
+
+* Workload Controller
+  - A pending Workload should be admitted when enough resources are available after release of resources by the succeeded Pods of the parallel Jobs.
+  - Should update the `.spec.reclaimedPodSetPods` of a Workload when a Pod of a Job succeeds.
+  - Integration tests for Workload Controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/core/workload_controller_test.go).
+
+* Scheduler
+  - Checking if a Workload gets admitted when an active parallel Job releases resources of completed Pods.
+  - Integration tests for Scheduler are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/scheduler/scheduler_test.go).
+
+## Implementation History
+
+Dynamically Reclaiming Resources are tracked as part of [enhancement#78](https://github.com/kubernetes-sigs/kueue/issues/78).
+
+**TODO** - Add proposal link
```

  this line can be removed now, right?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-11T19:24:02Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:110** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T19:24:01Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` holds the mapping between PodSet name to the count of Pods and Pod names that have successfully completed its execution and their resources have been reclaimed by Kueue. The names of Pods are recorded to identify the Pods whose resources have been reclaimed by Kueue. If a name of a Pod already exists in the list then the resources associated with the succeeded Pod have already been reclaimed by Kueue.
+
+## Implementation
+
+The Workload reconciler will keep a watch(or `Watches()`) on the Pods of the Job. The Workload reconciler will process the events of the Pods and update `.status.reclaimedPodSetPods` field accordingly.
```

  We don't even know that for Job, because parallelism can be different from completions. So understanding the specific API is necessary.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-10-12T04:33:50Z

- **keps/78-dynamically-reclaiming-resources/README.md:110** by [@thisisprasad](https://github.com/thisisprasad) — 2022-10-12T04:33:49Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
+
+## Implementation
+
+The workload reconciler will keep a watch(or `Watches()`) on the Job's `.status.succeeded` field. The Workload reconciler will calculate the difference between the Job's `.status.succeeded` field and Workload's `.status.reclaimedPodSetPods[i].count` field for every PodSet of the workload. With the former value being greater than the later, Kueue will reclaim the resources of the excess succeeded Pods.
```

  I meant Job CR only.
  
  Yes, will rethink for the case where `parallelism != completions`

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-12T06:31:39Z

- **enhancements/78-dynamically-reclaiming-resources/README.md:110** by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T06:31:39Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows for the resources of Pods of a Workload to be reclaimed after successful completion of each of the Pods of a Workload. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Job or Pods of a Job to allocate resource to a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as it completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the workload's Pods.
+
+`.status.ReclaimedPodSetPods` will keep the mapping between the PodSet name to the count and names of the successfully completed Pods belonging to the Workload. If a name of a Pod of a Workload is present in the field then it means that the resources associated with the completed Pods have been reclaimed. The name of the Pod is recorded to avoid reclaiming the resources of the Pod more than once.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // ReclaimedPodSetPods are the pods (by PodSet name) which have
+    //completed their execution successfully and resources of the pods
+	//are reclaimed.
+    // +optional
+    ReclaimedPodSetPods ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+type ReclaimedPodSetPods map[string]struct{
+    Count int
+    Pods []string
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` holds the mapping between PodSet name to the count of Pods and Pod names that have successfully completed its execution and their resources have been reclaimed by Kueue. The names of Pods are recorded to identify the Pods whose resources have been reclaimed by Kueue. If a name of a Pod already exists in the list then the resources associated with the succeeded Pod have already been reclaimed by Kueue.
+
+## Implementation
+
+The Workload reconciler will keep a watch(or `Watches()`) on the Pods of the Job. The Workload reconciler will process the events of the Pods and update `.status.reclaimedPodSetPods` field accordingly.
```

  Yes, if we want to support more jobs, kueue should understand the APIs then make decisions.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-12T06:34:47Z

- **keps/78-dynamically-reclaiming-resources/README.md:43** by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T06:34:47Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
```

  No, I mean https://github.com/kubernetes-sigs/kueue/pull/331#discussion_r991784771
  
  But what you said can also be added.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-12T06:38:40Z

- **keps/78-dynamically-reclaiming-resources/README.md:110** by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T06:38:39Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
+
+## Implementation
+
+The workload reconciler will keep a watch(or `Watches()`) on the Job's `.status.succeeded` field. The Workload reconciler will calculate the difference between the Job's `.status.succeeded` field and Workload's `.status.reclaimedPodSetPods[i].count` field for every PodSet of the workload. With the former value being greater than the later, Kueue will reclaim the resources of the excess succeeded Pods.
```

  > The workload reconciler doesn't watch Job.
  
  +1, or it will be too heavy for workload controller and coupled with Jobs.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-10-25T18:15:08Z

- **keps/78-dynamically-reclaiming-resources/README.md:62** by [@thisisprasad](https://github.com/thisisprasad) — 2022-10-25T18:15:08Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
```

  The remaining completions of a job will reduce by one when its pod will finish successfully. I think, we should reclaim the resources of a successful pod when the job's parallelism is greater or equal to the remaining job completions value. In this way, we can also generalize this approach to applicable in any scenario for reclaiming the resources of a successful pod of a job .
  
  @alculquicondor @kerthcet  let me know your comments on this. Just don't want to directly jump on writing the approach.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-16T20:05:24Z

- **keps/78-dynamically-reclaiming-resources/README.md:62** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T20:05:23Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
```

  let's say parallelism=3 and completions=5.
  When 1 pod finishes (let's sayindex 0), there should still be 3 running pods (indexes 1, 2, 3)
  When 3 pods finish, then there would only be 2 running pods (indexes 3, 4).
  
  Is this what you mean?

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-11-19T09:25:58Z

- **keps/78-dynamically-reclaiming-resources/README.md:62** by [@thisisprasad](https://github.com/thisisprasad) — 2022-11-19T09:25:57Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
```

  What you have explained is correct. That's what I meant. 
  
  I shouldn't have said Job's parallelism greater than it's completions. Rather, as we see from the above scenario you have explained, when indexes 3 & 4 remain(rather 2, 3 & 4 remain, consider 0 &1 completed for simplicity), now remaining completions of Job is not greater than the possible Job's parallelism. After this point in the Job's lifetime, Kueue can start to reclaim the resources of the Pod which completes successfully.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-11-19T09:31:43Z

- **keps/78-dynamically-reclaiming-resources/README.md:43** by [@thisisprasad](https://github.com/thisisprasad) — 2022-11-19T09:31:43Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
```

  > Do you mean excluding support for APIs that are not batch/v1.Job? If so, I'm ok with that
  
  Yes, currently resource reclaim feature is limited to kubernetes native `Job` API. For jobs of other types, reclaiming of resources would be dependent on the design of those particular jobs.
  
  > Maybe also add `Supporting variety reclaiming policies`.
  
  I suppose, we can think about when we add support for other Job types such `MPIJob`, `Argo/Tekton workflows`, etc

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-11-19T14:18:32Z

- **keps/78-dynamically-reclaiming-resources/README.md:142** by [@thisisprasad](https://github.com/thisisprasad) — 2022-11-19T14:18:32Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
+
+## Implementation
+
+The workload reconciler will keep a watch(or `Watches()`) on the Job's `.status.succeeded` field. The Workload reconciler will calculate the difference between the Job's `.status.succeeded` field and Workload's `.status.reclaimedPodSetPods[i].count` field for every PodSet of the workload. With the former value being greater than the later, Kueue will reclaim the resources of the excess succeeded Pods.
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of Kueue's prow CI/CD pipeline.
+
+### Unit Tests
+
+All the Kueue's core components must be covered by unit tests.  Here is a list of unit tests required for the modules of the feature:
+
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Kueue Job Controller
+  - checking the resources owned by a Job are released to the cache and clusterQueue when a Pod of the Job succeed.
+  - Integration tests for Job controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go).
+
+* Workload Controller
+  - A pending Workload should be admitted when enough resources are available after release of resources by the succeeded Pods of the parallel Jobs.
+  - Should update the `.spec.reclaimedPodSetPods` of a Workload when a Pod of a Job succeeds.
+  - Integration tests for Workload Controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/core/workload_controller_test.go).
+
+* Scheduler
+  - Checking if a Workload gets admitted when an active parallel Job releases resources of completed Pods.
+  - Integration tests for Scheduler are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/scheduler/scheduler_test.go).
+
+## Implementation History
+
+Dynamically Reclaiming Resources are tracked as part of [enhancement#78](https://github.com/kubernetes-sigs/kueue/issues/78).
+
+**TODO** - Add proposal link
```

  Removed

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-11-19T14:18:44Z

- **keps/78-dynamically-reclaiming-resources/README.md:130** by [@thisisprasad](https://github.com/thisisprasad) — 2022-11-19T14:18:44Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
+
+## Implementation
+
+The workload reconciler will keep a watch(or `Watches()`) on the Job's `.status.succeeded` field. The Workload reconciler will calculate the difference between the Job's `.status.succeeded` field and Workload's `.status.reclaimedPodSetPods[i].count` field for every PodSet of the workload. With the former value being greater than the later, Kueue will reclaim the resources of the excess succeeded Pods.
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of Kueue's prow CI/CD pipeline.
+
+### Unit Tests
+
+All the Kueue's core components must be covered by unit tests.  Here is a list of unit tests required for the modules of the feature:
+
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Kueue Job Controller
+  - checking the resources owned by a Job are released to the cache and clusterQueue when a Pod of the Job succeed.
+  - Integration tests for Job controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go).
+
+* Workload Controller
+  - A pending Workload should be admitted when enough resources are available after release of resources by the succeeded Pods of the parallel Jobs.
```

  Removed

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-11-19T14:18:54Z

- **keps/78-dynamically-reclaiming-resources/README.md:106** by [@thisisprasad](https://github.com/thisisprasad) — 2022-11-19T14:18:54Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+    - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
+    Name string
+    Count int
+}
+```
+
+#### ReclaimedPodSetPods
+`.status.reclaimedPodSetPods` is a list where each element of the list denotes the count of Pods of the Workload w.r.t PodSet whose resources have been reclaimed by Kueue. The structure consists of two fields - `Name` denotes the name of PodSet and `Count` denotes the count of Pods belonging to the `Name` PodSet.
```

  Removed

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-11-19T14:26:56Z

- **keps/78-dynamically-reclaiming-resources/README.md:100** by [@thisisprasad](https://github.com/thisisprasad) — 2022-11-19T14:26:55Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSetPods` to the Workload API. Workload controller will be responsible for updating the field `.status.ReclaimedPodSetPods` according to the status of the Workload's Pods.
+
+`.status.ReclaimedPodSetPods` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod failure cases and how the reclaiming of resources by Kueue will happen.
+
+### Pod Successful Completion Cases
+
+#### Job Parallelism Equal To 1
+
+By default, the `.spec.parallelism` of Job is equal to `1`.  In this case, if the Pod completes with successful execution, the whole Job execution can be considered as a success. Hence, the resource associated with the Job will be reclaimed by Kueue after the Job completion. This functionality is present today as well and no change will be required.
+
+#### Job Parallelism Greater Than 1
+
+Whenever a Pod of a Job successfully completes its execution, Kueue will reclaim the resources that were associated with successful Pod. The Job might be still in `Running` state as there could be other Pods of the Job that are executing. Thus, Kueue will reclaim the resources of the succeeded Pods of the Job in an incremental manner until the Job completes in either `Succeeded` or `Failed` state. Whenever the Job completes, the only remaining owned resources of the Job will be reclaimed by Kueue.
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSetPods` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSetPods []ReclaimedPodSetPods `json:"reclaimedPodSetPods"`
+}
+
+// ReclaimedPodSetPods defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources have been reclaimed by Kueue
+type ReclaimedPodSetPods struct{
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-21T16:45:28Z

- **keps/78-dynamically-reclaiming-resources/README.md:43** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:29:49Z
```diff
@@ -0,0 +1,142 @@
+# Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+    - [Goals](#goals)
+    - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+    - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+        - [Job Parallelism Equal To 1](#job-parallelism-equal-to-1)
+        - [Job Parallelism Greater Than 1](#job-parallelism-greater-than-1)
+    - [Pod Failure Cases](#pod-failure-cases)
+        - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+        - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+    - [ReclaimedPodSetPods](#reclaimedpodsetpods)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
```

  Then please add to non-goals

- **keps/78-dynamically-reclaiming-resources/README.md:49** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:30:54Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
```

  ```suggestion
  We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of Pods belonging to a PodSet whose resources can be reclaimed by Kueue.
  ```
  
  as we have seen, completed pods might not directly affect this number.

- **keps/78-dynamically-reclaiming-resources/README.md:51** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:32:04Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
```

  Maybe add a subtitle saying that this is specific to v1.Job

- **keps/78-dynamically-reclaiming-resources/README.md:77** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:37:09Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
```

  Maybe put this paragraph under `Pod Failure Cases`, as it resumes both scenarios.

- **keps/78-dynamically-reclaiming-resources/README.md:95** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:37:55Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
```

  what about `podSetsReclaims`?
  
  "reclaimed pod sets" sounds like we are reclaiming the entire pod set.

- **keps/78-dynamically-reclaiming-resources/README.md:79** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:39:53Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
```

  ```suggestion
  ### API
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:106** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:39:59Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimedPodSet struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
```

  ```suggestion
  ### Implementation
  ```

- **keps/78-dynamically-reclaiming-resources/README.md:108** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:40:14Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimedPodSet struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+Kueue's job reconciler will compare the Job's `.status.Succeeded` field and the Workload's `.status.reclaimedPodSets[i].count` field value. If the former value is greater than the later then, Kueue's Job reconciler will update the Workload object's `.status`. Workload reconciler will catch the update event of Kueue Job reconciler and release the resources of the newly succeeded Pods to the ClusterQueue depending upon the case the Job satisfies discussed in the section of [successful Pod completion](#pod-successful-completion-cases).
```

  we don't need to repeat the formula, but the first sentence of this paragraph seems to contradict what you said in the analysis above.
  
  Or maybe I'm not understanding your proposal. I think all the calculations will have to be in the job controller. The workload controller already has the curated number of reclaimable pods to update the ClusterQueues usage.

- **keps/78-dynamically-reclaiming-resources/README.md:117** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:43:49Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimedPodSet struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+Kueue's job reconciler will compare the Job's `.status.Succeeded` field and the Workload's `.status.reclaimedPodSets[i].count` field value. If the former value is greater than the later then, Kueue's Job reconciler will update the Workload object's `.status`. Workload reconciler will catch the update event of Kueue Job reconciler and release the resources of the newly succeeded Pods to the ClusterQueue depending upon the case the Job satisfies discussed in the section of [successful Pod completion](#pod-successful-completion-cases).
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of Kueue's prow CI/CD pipeline.
+
+### Unit Tests
+
+All the Kueue's core components must be covered by unit tests.  Here is a list of unit tests required for the modules of the feature:
```

  We might also need unit tests in the job controller. But we can decide that later, given that all tests are currently at integration level.

- **keps/78-dynamically-reclaiming-resources/README.md:128** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:44:48Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimedPodSet struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+Kueue's job reconciler will compare the Job's `.status.Succeeded` field and the Workload's `.status.reclaimedPodSets[i].count` field value. If the former value is greater than the later then, Kueue's Job reconciler will update the Workload object's `.status`. Workload reconciler will catch the update event of Kueue Job reconciler and release the resources of the newly succeeded Pods to the ClusterQueue depending upon the case the Job satisfies discussed in the section of [successful Pod completion](#pod-successful-completion-cases).
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of Kueue's prow CI/CD pipeline.
+
+### Unit Tests
+
+All the Kueue's core components must be covered by unit tests.  Here is a list of unit tests required for the modules of the feature:
+
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Kueue Job Controller
+  - Checking the resources owned by a Job are released to the cache and clusterQueue when a Pod of the Job succeed.
+  - Integration tests for Job controller are [found here](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go).
+
+* Workload Controller
+  - Should update the `.spec.reclaimedPodSets` of a Workload when a Pod of a Job succeeds.
```

  this sounds like a test for kueue job controller

- **keps/78-dynamically-reclaiming-resources/README.md:124** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T16:45:24Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimedPodSet struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+Kueue's job reconciler will compare the Job's `.status.Succeeded` field and the Workload's `.status.reclaimedPodSets[i].count` field value. If the former value is greater than the later then, Kueue's Job reconciler will update the Workload object's `.status`. Workload reconciler will catch the update event of Kueue Job reconciler and release the resources of the newly succeeded Pods to the ClusterQueue depending upon the case the Job satisfies discussed in the section of [successful Pod completion](#pod-successful-completion-cases).
+
+## Testing Plan
+
+Dynamically reclaiming resources enhancement has unit and integration tests. These tests
+are run regularly as a part of Kueue's prow CI/CD pipeline.
+
+### Unit Tests
+
+All the Kueue's core components must be covered by unit tests.  Here is a list of unit tests required for the modules of the feature:
+
+* [Cluster-Queue cache tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/cache_test.go)
+* [Workload tests](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload_test.go)
+
+### Integration tests
+* Kueue Job Controller
+  - Checking the resources owned by a Job are released to the cache and clusterQueue when a Pod of the Job succeed.
```

  this sounds like a test for the workload controller.

### Review by [@thisisprasad](https://github.com/thisisprasad) (commented) — 2022-11-21T18:40:32Z

- **keps/78-dynamically-reclaiming-resources/README.md:108** by [@thisisprasad](https://github.com/thisisprasad) — 2022-11-21T18:40:32Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimedPodSet struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+Kueue's job reconciler will compare the Job's `.status.Succeeded` field and the Workload's `.status.reclaimedPodSets[i].count` field value. If the former value is greater than the later then, Kueue's Job reconciler will update the Workload object's `.status`. Workload reconciler will catch the update event of Kueue Job reconciler and release the resources of the newly succeeded Pods to the ClusterQueue depending upon the case the Job satisfies discussed in the section of [successful Pod completion](#pod-successful-completion-cases).
```

  > we don't need to repeat the formula, but the first sentence of this paragraph seems to contradict what you said in the analysis above.
  
  In the analysis above, comparison of values of Job's `.spec.parallelism` and it's remaining completion count (**M**) is explained, and at what condition in future the resources will reclaimed by Kueue. Here, in the first sentence, I'm trying to say that, if the mentioned values differ, then workload object will be updated. And the actual cases where the decision to reclaim or not will be taken by workload reconciler when it recieves an update event from job reconciler.
  
  > I think all the calculations will have to be in the job controller. The workload controller already has the curated number of reclaimable pods to update the ClusterQueues usage.
  
  What I have assumed is, during the handling of update event of workload by its reconciler, old and new workloads objects are available. 
  
  1. Calculate the difference in the count of `.status.podSetReclaims` of  old and new workload object.
  2. Check the job parallelism and remaining completion values (Here a GET call to fetch Job will be required).
  3. Calculate the effective count of pods to reclaim and update cache.
  
  I see the issue with my implementation approach here. There could be inconsistency in Job's remaining completion value.
  
  What you have said on having all calculations in job controller seems the correct to me now. But, I see one challenge while releasing the resources from workload reconciler. How should the Job reconciler convey the effective count of Pods to be reclaimed to workload reconciler. As the workload reconciler has a cache object and the Job reconciler does not.
  Can having cache object as a part of job reconciler correct in this case?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-21T19:58:43Z

- **keps/78-dynamically-reclaiming-resources/README.md:108** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T19:58:43Z
```diff
@@ -0,0 +1,137 @@
+# KEP-78:  Dynamically reclaim resources of Pods of a Workload
+
+## Table of Contents
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Pod Successful Completion Cases](#pod-successful-completion-cases)
+    - [Case N &gt;= M](#case-n--m)
+    - [Case M &gt; N](#case-m--n)
+  - [Pod Failure Cases](#pod-failure-cases)
+    - [RestartPolicy of Pods as Never](#restartpolicy-of-pods-as-never)
+    - [RestartPolicy of Pods as OnFailure](#restartpolicy-of-pods-as-onfailure)
+- [API](#api)
+- [Implementation](#implementation)
+- [Testing Plan](#testing-plan)
+  - [Unit Tests](#unit-tests)
+  - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+<!-- /toc -->
+
+## Summary
+
+This proposal allows Kueue to reclaim the resources of a successfully completed Pod of a Workload even before the whole Workload completes its execution. Freeing the unused resources of a Workload earlier may allow more pending Workloads to be admitted. This will allow Kueue to utilize the existing resources of the cluster much more efficiently.
+
+In the remaining of this document, *Pods of a Workload* means the same as *Pods of a Job* and vice-versa.
+
+
+## Motivation
+
+Currently, the resources owned by a Job are reclaimed by Kueue only when the whole Job finishes. For Jobs with multiple Pods, the resources will be reclaimed only after the last Pod of the Job finishes. This is not efficient as the Pods of parallel Job may have laggards consuming the unused resources of the Jobs those are currently executing.
+
+### Goals
+
+- Utilize the unused resources of the successfully completed Pods of a running Job.
+
+### Non-Goals
+
+- Preempting a Workload or Pods of a Workload to free resources for a pending Workload.
+- Partially admitting a Workload.
+
+## Proposal
+
+Reclaiming the resources of the succeeded Pods of a running Job as soon as the Pod completes its execution.
+
+We propose to add a new field `.status.ReclaimedPodSets` to the Workload API. `.status.ReclaimedPodSets` is a list that holds the count of successfully completed Pods belonging to a PodSet whose resources have been reclaimed by Kueue.
+
+Now we will look at the Pod successful completion and failure cases and how Kueue will reclaim the resources in each of the cases.
+
+### Pod Successful Completion Cases
+
+Reclaiming the resources of a successful Pod of a Job depends on two parameters - remaining completions of a Job(M) and Job's parallelism(N). Depending on the values of **M** and **N**, we will look at the different cases how the resources will be reclaimed by Kueue.
+
+Please note here that **M** here refers to the remaining successful completions of a Job and not the Job's `.spec.completions` field. **M** is subject to change during the lifetime of the Job. One way to derive the value of **M** would be to calculate the difference of `.spec.completions` and `.status.succeeded` values of a Job.
+
+#### Case N >= M
+If a Job's parallelism is greater or equal to its remaining completions, then for every successful completion of Pod of a Job, Kueue will reclaim the resources associated with the successful Pod.
+
+#### Case M > N
+When the remaining completions of a Job are greater than the Job's parallelism, then for every successfully completed Pod of a Job, the resources associated with the Pod won't be reclaimed by Kueue. This is because, the resource requirement of the Job still remains the same. The Job has to create a new Pod as a replacement against the successfully completed Pod.
+
+The Job will be able to reclaim the resources of Pods when it satisfies the case `N >= M`. A Job which proceeds further in its execution with case `M > N` will get converted to a problem of case `N >= M` because, the value **M** will decrease with successful Pod completions. Hence, the process to reclaim resources of a Pod of a Job will be same as mentioned for the case `N >= M`
+
+### Pod Failure Cases
+
+#### RestartPolicy of Pods as Never
+
+Whenever a Pod of a Job fails, the Job will recreate a Pod as replacement against the failed Pod. The Job will continue this process of creating new Pods until the Job reaches its `backoffLimit`(by default the backoffLimit is `6`). The newly created Pod against the failed Pod will reuse the resources of the failed Pod. Once the Job reaches its `backoffLimit` and does not have the required successful `.spec.completions` count, the Job is termed as a `Failed` Job. When the Job is marked as failed, no more Pods would be created by the Job. So, the remaining owned resources of the Job will be reclaimed by queue. 
+
+#### RestartPolicy of Pods as OnFailure
+
+When the Pods of the Job have `.spec.template.spec.restartPolicy = "OnFailure"`, the failed Pod stays on the node and the failed container within the Pod is restarted. The Pod might run indefinitely or get marked as `Failed` after the `.spec.activeDeadlineSeconds` is completed by Job if specified. In this case also, Kueue won't reclaim the resources of the Job until the Job gets completed as either `Failed` or `Succeeded`.
+
+Hence, as seen from the above discussed Pod failure cases, we conclude that the Pods of a Job which fail during its execution, its resources are not immediately reclaimed by Kueue. Only when the Job gets marks as `Failed`, the resources of failed Pods will be reclaimed by Kueue.
+
+## API
+
+A new field `ReclaimedPodSets` is added to the `.status` of Workload API.
+
+```go
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    // conditions hold the latest available observations of the Workload
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    Conditions []WorkloadCondition `json:"conditions,omitempty"`
+
+    // list of count of Pods of a PodSet with resources reclaimed
+    // +optional
+    ReclaimedPodSets []ReclaimedPodSetPods `json:"reclaimedPodSets"`
+}
+
+// ReclaimedPodSetPod defines the PodSet name and count of successfully completed Pods 
+// belonging to the PodSet whose resources can be reclaimed.
+type ReclaimedPodSet struct{
+    Name string
+    Count int
+}
+```
+
+## Implementation
+
+Kueue's job reconciler will compare the Job's `.status.Succeeded` field and the Workload's `.status.reclaimedPodSets[i].count` field value. If the former value is greater than the later then, Kueue's Job reconciler will update the Workload object's `.status`. Workload reconciler will catch the update event of Kueue Job reconciler and release the resources of the newly succeeded Pods to the ClusterQueue depending upon the case the Job satisfies discussed in the section of [successful Pod completion](#pod-successful-completion-cases).
```

  The job controller doesn't need access to the cache (and it shouldn't have).
  
  The job controller can simply write into the workload status how many pods can be reclaimed.
  
  The workload controller observes that change and updates the cache.
