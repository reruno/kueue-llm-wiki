# PR #3312: Stateful set integration docs

**Summary**: Stateful set integration docs

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3312

**Last updated**: 2024-10-30T10:29:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `statefulset-docs` → `main`
- **Created**: 2024-10-24T21:35:57Z
- **Updated**: 2024-10-30T10:29:06Z
- **Merged**: 2024-10-30T06:27:27Z
- **Closed**: 2024-10-30T06:27:27Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+82 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Adds documentation for StatefulSet integration
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2717

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-24T21:35:59Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-24T21:36:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c80c517a9b119c842b4dbf49c31127b7e7ebdd10 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67215a1aab52c60008d6fbaf |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3312--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3312--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTMzMTItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.efctTVshyIikp18B9mIC5c4mlRP-YH7_nvPgOhwYeRk)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3312--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-10-24T21:39:04Z

/assign
/cc @mbobrovskyi @tenzen-y

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:18:17Z

/release-note-none

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-25T06:18:19Z

@mbobrovskyi: you can only set the release note label to release-note-none if the release-note block in the PR body text is empty or "none".

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3312#issuecomment-2436959544):

>/release-note-none


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:19:20Z

/release-note-edit 

```release-note
NONE
```

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-29T22:55:22Z

/lgtm

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-29T22:55:28Z

LGTM label has been added.  <details>Git tree hash: 2bae1b42db8e3b404df39e46cca1ca79540dfaa8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-30T06:12:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3312#pullrequestreview-2403721914" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3312#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-25T06:16:00Z

- **site/content/en/docs/tasks/run/statefulset.md:42** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T05:51:38Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration,
+where every Pod from a StatefulSet is represented as a single independent Plain Pod.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a StatefulSet admitted by Kueue
+
+When running a StatefulSet on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `labels` section of the StatefulSet configuration.
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the StatefulSet,
+the queue name should be specified at the Pod level.
+
+```yaml
+labels:
+   kueue.x-k8s.io/queue-name: user-queue
```

  ```suggestion
  metadata:
    labels:
      kueue.x-k8s.io/queue-name: user-queue
  ```

- **site/content/en/docs/tasks/run/statefulset.md:17** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T05:59:36Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration,
+where every Pod from a StatefulSet is represented as a single independent Plain Pod.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
```

  Please also add StatefulSet link here:
  
  https://github.com/kubernetes-sigs/kueue/blob/d52514301570c819766b94f93ad817f3656b0459/site/content/en/docs/tasks/_index.md?plain=1#L53-L54

- **site/content/en/docs/tasks/run/statefulset.md:12** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:02:18Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
```

  Should we also mention that scaling is not yet supported as well?

- **site/content/en/docs/tasks/run/statefulset.md:24** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:11:45Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration,
+where every Pod from a StatefulSet is represented as a single independent Plain Pod.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
```

  I think it would be great to mention that we need to include "statefulset" in the "frameworks" section too.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-25T06:25:23Z

- **site/content/en/docs/tasks/run/statefulset.md:36** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:24:44Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration,
+where every Pod from a StatefulSet is represented as a single independent Plain Pod.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a StatefulSet admitted by Kueue
+
+When running a StatefulSet on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `labels` section of the StatefulSet configuration.
```

  ```suggestion
  The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the StatefulSet configuration.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-25T06:40:56Z

- **site/content/en/docs/tasks/run/statefulset.md:46** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:33:47Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration,
+where every Pod from a StatefulSet is represented as a single independent Plain Pod.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a StatefulSet admitted by Kueue
+
+When running a StatefulSet on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `labels` section of the StatefulSet configuration.
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the StatefulSet,
+the queue name should be specified at the Pod level.
+
+```yaml
+labels:
+   kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+The resource needs of the workload can be configured in the spec.template.spec.containers.
```

  ```suggestion
  The resource needs of the workload can be configured in the `spec.template.spec.containers`.
  ```

- **site/content/en/docs/tasks/run/statefulset.md:16** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:39:52Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration,
+where every Pod from a StatefulSet is represented as a single independent Plain Pod.
```

  ```suggestion
  where every Pod from a StatefulSet is represented as a single independent Plain Pod.
  ```
  ```suggestion
  where Pods from a StatefulSet are represented as a Pod group.
  ```
  ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-25T06:46:02Z

- **site/content/en/docs/tasks/run/statefulset.md:13** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:46:01Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
```

  ```suggestion
  it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
  ```
  Not sure about it, because we are using Pod group.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-25T06:47:32Z

- **site/content/en/docs/tasks/run/statefulset.md:38** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-25T06:47:32Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+Although Kueue does not yet support managing a StatefulSet as a single Workload,
+it's still possible to leverage Kueue's scheduling and resource management capabilities for the individual Pods of the StatefulSet.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration,
+where every Pod from a StatefulSet is represented as a single independent Plain Pod.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+
+3. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a StatefulSet admitted by Kueue
+
+When running a StatefulSet on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `labels` section of the StatefulSet configuration.
+Since Kueue's scheduling and resource management will be applied to the individual Pods of the StatefulSet,
+the queue name should be specified at the Pod level.
```

  ```suggestion
  ```
  
  I think it's not need.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-28T10:34:59Z

- **site/content/en/docs/tasks/run/statefulset.md:31** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-28T10:29:40Z
```diff
@@ -0,0 +1,74 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration with the help of PodGroup,
+where StatefulSet is represented as a single independent workload.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+   Also, ensure that you have the `v1/statefulset` integration enabled.
+
+3. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a StatefulSet admitted by Kueue
+
+> Ensure that you have the `v1/statefulset` integration enabled.
```

  ```suggestion
  ```
  
  We already have on L25.

- **site/content/en/docs/tasks/run/statefulset.md:25** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-28T10:32:50Z
```diff
@@ -0,0 +1,74 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration with the help of PodGroup,
+where StatefulSet is represented as a single independent workload.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+   Also, ensure that you have the `v1/statefulset` integration enabled.
```

  I would move it to a separate step and add an example, similar to what we have on the Pods page.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T18:11:32Z

LGTM overall, thanks!

- **site/content/en/docs/tasks/run/statefulset.md:13** by [@mimowo](https://github.com/mimowo) — 2024-10-28T18:03:42Z
```diff
@@ -0,0 +1,97 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration with the help of PodGroup,
```

  ```suggestion
  We demonstrate how to support scheduling StatefulSets in Kueue based on the [Plain Pod Group](https://kueue.sigs.k8s.io/docs/tasks/run/plain_pods/) integration,
  ```

- **site/content/en/docs/tasks/run/statefulset.md:49** by [@mimowo](https://github.com/mimowo) — 2024-10-28T18:09:54Z
```diff
@@ -0,0 +1,97 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration with the help of PodGroup,
+where StatefulSet is represented as a single independent workload.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+   Also, ensure that you have the `v1/statefulset` integration enabled.
+
+   A configuration for Kueue with enabled statefulset integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod" # required by statefulset
+      - "statefulset"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
```

  I think this is not necessary here as this is repeating the content on the Plain pod page. 
  
  Or if you want we could make it minimal: 
  
  2. Ensure that you have the `v1/statefulset` integration enabled, for example:
  ```yaml
     apiVersion: config.kueue.x-k8s.io/v1beta1
     kind: Configuration
     integrations:
       frameworks:
        - "pod" # required by statefulset
        - "statefulset"
  ```
  Also, follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
     to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.

- **site/content/en/docs/tasks/run/statefulset.md:85** by [@mimowo](https://github.com/mimowo) — 2024-10-28T18:11:18Z
```diff
@@ -0,0 +1,97 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration with the help of PodGroup,
+where StatefulSet is represented as a single independent workload.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+   Also, ensure that you have the `v1/statefulset` integration enabled.
+
+   A configuration for Kueue with enabled statefulset integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod" # required by statefulset
+      - "statefulset"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
+   ```
+
+3. Check [Administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Running a StatefulSet admitted by Kueue
+
+When running a StatefulSet on Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the StatefulSet configuration.
+
+```yaml
+metadata:
+   labels:
+      kueue.x-k8s.io/queue-name: user-queue
+```
+
+### b. Configure the resource needs
+The resource needs of the workload can be configured in the `spec.template.spec.containers`.
+
+```yaml
+spec:
+  template:
+     spec:
+      containers:
+       - resources:
+           requests:
+             cpu: 3
+```
+
+### c. Scaling
+
+Currently, scaling operations on StatefulSets are not supported.
+This means you cannot perform scale up or scale down operations directly through Kueue.
+Ensure that your StatefulSet configuration accounts for the fixed number of Pods required for your workload.
```

  I would drop this sentence (from Ensure...)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-28T18:20:54Z

- **site/content/en/docs/tasks/run/statefulset.md:49** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-10-28T18:20:54Z
```diff
@@ -0,0 +1,97 @@
+---
+title: "Run StatefulSet"
+linkTitle: "StatefulSet"
+date: 2024-07-25
+weight: 6
+description: >
+  Run a StatefulSet as a Kueue-managed workload.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management
+capabilities when running StatefulSets.
+
+We demonstrate how to support scheduling StatefulSets in Kueue based on the Plain Pod integration with the help of PodGroup,
+where StatefulSet is represented as a single independent workload.
+
+This guide is for [serving users](/docs/tasks#serving-user) that have a basic understanding of Kueue.
+For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
+
+2. Follow steps in [Run Plain Pods](/docs/tasks/run/plain_pods/#before-you-begin)
+   to learn how to enable the `v1/pod` integration and how to configure it using the `podOptions` field.
+   Also, ensure that you have the `v1/statefulset` integration enabled.
+
+   A configuration for Kueue with enabled statefulset integration would look like follows:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod" # required by statefulset
+      - "statefulset"
+     podOptions:
+       # You can change namespaceSelector to define in which 
+       # namespaces kueue will manage the pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Kueue uses podSelector to manage pods with particular 
+       # labels. The default podSelector will match all the pods. 
+       podSelector:
+         matchExpressions:
+         - key: kueue-job
+           operator: In
+           values: [ "true", "True", "yes" ]
```

  I would prefer second one. It's more understandable I think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T06:12:35Z

/approve
Thanks!
