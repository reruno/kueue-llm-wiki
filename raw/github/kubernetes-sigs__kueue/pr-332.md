# PR #332: Use metav1.Condition and validate

**Summary**: Use metav1.Condition and validate

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/332

**Last updated**: 2022-08-17T20:24:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `conditions` → `main`
- **Created**: 2022-08-15T16:07:02Z
- **Updated**: 2022-08-17T20:24:47Z
- **Merged**: 2022-08-17T20:24:47Z
- **Closed**: 2022-08-17T20:24:47Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 17   **+178 / -183**

## Description

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:

Reuse existing metav1.Condition which comes with a validation function.

Side-effects:
- Reason and message are mandatory. Added on tests and controller.
- There is a limit for how big the message can be, so we need to truncate

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-15T16:07:14Z

/assign @ahg-g

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-15T16:07:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/332#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-08-17T03:11:26Z

Looks good to me, leave lgtm to @ahg-g in case he would like to review for another round.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-17T20:18:23Z

/lgtm

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-15T19:30:12Z

- **pkg/controller/core/workload_controller.go:106** by [@ahg-g](https://github.com/ahg-g) — 2022-08-15T19:19:16Z
```diff
@@ -85,25 +85,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	switch status {
 	case pending:
 		if !r.queues.QueueForWorkloadExists(&wl) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("Queue %s doesn't exist", wl.Spec.QueueName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 		if !cqOk {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if !r.cache.ClusterQueueActive(cqName) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 	case admitted:
-		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionTrue, "", "")
+		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionTrue, "ByClusterQueue", "Admitted by ClusterQueue")
```

  add the cluster queue in the message? also, for reason, may be we should have a name for the controller and use that instead of "ByClusterQueue"?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-15T20:35:29Z

- **pkg/controller/core/workload_controller.go:106** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-15T20:35:29Z
```diff
@@ -85,25 +85,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	switch status {
 	case pending:
 		if !r.queues.QueueForWorkloadExists(&wl) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("Queue %s doesn't exist", wl.Spec.QueueName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 		if !cqOk {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if !r.cache.ClusterQueueActive(cqName) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 	case admitted:
-		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionTrue, "", "")
+		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionTrue, "ByClusterQueue", "Admitted by ClusterQueue")
```

  I thought about adding the name of the ClusterQueue, but I found it repetitive since it's already present in the admission object. Still, I could add it.
  
  For reason, are you thinking along the lines of `ByKueueAdmission`?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-15T20:42:13Z

- **pkg/controller/core/workload_controller.go:106** by [@ahg-g](https://github.com/ahg-g) — 2022-08-15T20:42:13Z
```diff
@@ -85,25 +85,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	switch status {
 	case pending:
 		if !r.queues.QueueForWorkloadExists(&wl) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("Queue %s doesn't exist", wl.Spec.QueueName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 		if !cqOk {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if !r.cache.ClusterQueueActive(cqName) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 	case admitted:
-		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionTrue, "", "")
+		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionTrue, "ByClusterQueue", "Admitted by ClusterQueue")
```

  > I thought about adding the name of the ClusterQueue, but I found it repetitive since it's already present in the admission object. Still, I could add it.
  
  I would add it unless you are concerned about the message size.
  
  > For reason, are you thinking along the lines of ByKueueAdmission?
  
  Whatever string we put here I guess doesn't matter much, but the bigger question is if we will ever have different values for this field? for example if the admission was done manually (the case where admins force start a job)? or in this case the condition will not exist and be empty?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-15T21:58:15Z

- **pkg/controller/core/workload_controller.go:106** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-15T21:58:14Z
```diff
@@ -85,25 +85,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	switch status {
 	case pending:
 		if !r.queues.QueueForWorkloadExists(&wl) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("Queue %s doesn't exist", wl.Spec.QueueName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 		if !cqOk {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if !r.cache.ClusterQueueActive(cqName) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 	case admitted:
-		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionTrue, "", "")
+		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionTrue, "ByClusterQueue", "Admitted by ClusterQueue")
```

  I think it would be nice to mark it as admitted by the admin. Although the current code would override it.
  
  In any case, `ByKueueAdmission` would fit well if we ever did that.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-15T22:41:15Z

- **pkg/controller/core/workload_controller.go:106** by [@ahg-g](https://github.com/ahg-g) — 2022-08-15T22:41:15Z
```diff
@@ -85,25 +85,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	switch status {
 	case pending:
 		if !r.queues.QueueForWorkloadExists(&wl) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("Queue %s doesn't exist", wl.Spec.QueueName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 		if !cqOk {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if !r.cache.ClusterQueueActive(cqName) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 	case admitted:
-		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionTrue, "", "")
+		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionTrue, "ByClusterQueue", "Admitted by ClusterQueue")
```

  ok, lets change it then.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-16T03:09:01Z

- **pkg/util/api/api.go:20** by [@kerthcet](https://github.com/kerthcet) — 2022-08-16T02:48:56Z
```diff
@@ -0,0 +1,40 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package api
+
+const (
+	maxEventMessage     = 1024
```

  nit: maxEventMessageSize or eventLengthLimit?

- **pkg/controller/core/workload_controller.go:106** by [@kerthcet](https://github.com/kerthcet) — 2022-08-16T03:09:01Z
```diff
@@ -85,25 +85,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	switch status {
 	case pending:
 		if !r.queues.QueueForWorkloadExists(&wl) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("Queue %s doesn't exist", wl.Spec.QueueName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 		if !cqOk {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if !r.cache.ClusterQueueActive(cqName) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 	case admitted:
-		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionTrue, "", "")
+		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionTrue, "ByClusterQueue", "Admitted by ClusterQueue")
```

  I'm not a native speaker, but `KueueAdmitted` or `SchedulerAdmitted` seems better to me than `ByKueueAdmission` or `ByXxx`. Forget it if not.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-16T15:21:48Z

- **pkg/controller/core/workload_controller.go:106** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-16T15:21:48Z
```diff
@@ -85,25 +85,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	switch status {
 	case pending:
 		if !r.queues.QueueForWorkloadExists(&wl) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("Queue %s doesn't exist", wl.Spec.QueueName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 		if !cqOk {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s doesn't exist", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if !r.cache.ClusterQueueActive(cqName) {
-			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionFalse,
+			err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionFalse,
 				"Inadmissible", fmt.Sprintf("ClusterQueue %s is inactive", cqName))
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 	case admitted:
-		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, corev1.ConditionTrue, "", "")
+		err := workload.UpdateStatusIfChanged(ctx, r.client, &wl, kueue.WorkloadAdmitted, metav1.ConditionTrue, "ByClusterQueue", "Admitted by ClusterQueue")
```

  We can use this as a reference: https://github.com/kubernetes/website/pull/36007/files
  It has the action and an actor, so I'll go with `AdmissionByKueue`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-16T16:00:14Z

- **pkg/util/api/api.go:20** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-16T16:00:14Z
```diff
@@ -0,0 +1,40 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package api
+
+const (
+	maxEventMessage     = 1024
```

  Done
