# PR #3320: TAS: validation for CQs using TAS

**Summary**: TAS: validation for CQs using TAS

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3320

**Last updated**: 2025-01-21T10:54:09Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-validate-clusterqueue` → `main`
- **Created**: 2024-10-25T15:56:22Z
- **Updated**: 2025-01-21T10:54:09Z
- **Merged**: 2024-10-29T08:04:55Z
- **Closed**: 2024-10-29T08:04:55Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba), [@trasc](https://github.com/trasc), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 14   **+210 / -31**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #2724 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-25T15:56:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 17f5909c30f00670ef15130c40d1141fe5d327e4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/671fc5a73b68f70008e7e0ca |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-25T16:25:48Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-28T10:20:35Z

/cc @PBundyra @gabesaba @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-29T07:50:32Z

LGTM label has been added.  <details>Git tree hash: ed890b171c815ca60668df142b2ecba418287ef9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-29T07:50:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3320#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3320#pullrequestreview-2401022665" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-28T16:57:18Z

- **pkg/cache/clusterqueue.go:335** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T16:36:52Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
```

  Could we verify this during the validation?
  I'm wondering if we can follow the similar approach with the MK managedBy validations: https://github.com/kubernetes-sigs/kueue/blob/5203ac9eb69226a1ae3b6ba6d361eaed7ef3dc8c/pkg/controller/jobs/job/job_webhook.go#L90

- **pkg/cache/clusterqueue.go:423** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T16:39:06Z
```diff
@@ -380,6 +420,7 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 	slices.Sort(flavorIndependentCheckOnFlavors)
 	slices.Sort(perFlavorMultiKueueChecks)
 	multiKueueAdmissionChecksList := sets.List(multiKueueAdmissionChecks)
+	provisioningChecksList := sets.List(provisioningAdmissionChecks)
```

  ```suggestion
  	provisioningChecks := sets.List(provisioningAdmissionChecks)
  ```

- **pkg/cache/clusterqueue.go:339** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T16:42:03Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
+}
```

  Will we support these combinations between TAS and other features in the future (Beta or GA)?

- **pkg/cache/clusterqueue.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T16:44:23Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
```

  Does this mean that we support TAS collaboration with other third-party AdmissionCheck controllers?

- **pkg/cache/clusterqueue.go:379** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T16:53:25Z
```diff
@@ -343,6 +379,7 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 	checksPerController := make(map[string][]string, len(c.AdmissionChecks))
```

  Could you extend the unit testing?
  https://github.com/kubernetes-sigs/kueue/blob/5203ac9eb69226a1ae3b6ba6d361eaed7ef3dc8c/pkg/cache/clusterqueue_test.go#L400

- **pkg/cache/clusterqueue.go:348** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T16:54:17Z
```diff
@@ -315,6 +347,7 @@ func (c *clusterQueue) UpdateWithFlavors(flavors map[kueue.ResourceFlavorReferen
 
 func (c *clusterQueue) updateLabelKeys(flavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor) {
```

  Could you extend the unit testing?
  https://github.com/kubernetes-sigs/kueue/blob/5203ac9eb69226a1ae3b6ba6d361eaed7ef3dc8c/pkg/cache/clusterqueue_test.go#L38

- **pkg/cache/clusterqueue.go:321** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T16:57:10Z
```diff
@@ -297,6 +300,25 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			messages = append(messages, fmt.Sprintf("AdmissionCheck(s): %v cannot be set at flavor level", c.flavorIndependentAdmissionCheckAppliedPerFlavor))
 		}
 
+		if features.Enabled(features.TopologyAwareScheduling) && len(c.tasFlavors) > 0 {
+			if c.HasParent() {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for cohorts")
+			}
+			if c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for preemption within cluster queue")
+			}
+			if len(c.multiKueueAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with MultiKueue admission check")
+			}
+			if len(c.provisioningAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with ProvisioningRequest admission check")
+			}
+		}
+
```

  Could you expand this unit testing?
  https://github.com/kubernetes-sigs/kueue/blob/5203ac9eb69226a1ae3b6ba6d361eaed7ef3dc8c/pkg/cache/clusterqueue_test.go#L487
  
  Or, we may want to prepare the dedicated `TestClusterQueueUpdateWithTAS`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:05:13Z

- **pkg/cache/clusterqueue.go:335** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:05:13Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
```

  Well, I guess we could but there are downsides: 
  1. this mechanism would be per Job, so plenty of code which is going to be removed when we start to support the features
  2. it would be inconsistent with other CQ validation scenarios when we take the lazy validation approach
  3. it requires the webhooks to access the cache (yes, we did it for managedBy, but there it was last resort option because the managedBy field is immutable)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:11:40Z

- **pkg/cache/clusterqueue.go:423** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:11:40Z
```diff
@@ -380,6 +420,7 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 	slices.Sort(flavorIndependentCheckOnFlavors)
 	slices.Sort(perFlavorMultiKueueChecks)
 	multiKueueAdmissionChecksList := sets.List(multiKueueAdmissionChecks)
+	provisioningChecksList := sets.List(provisioningAdmissionChecks)
```

  done, also renamed `multiKueueAdmissionChecksList` -> `multiKueueChecks`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:12:27Z

- **pkg/cache/clusterqueue.go:339** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:12:26Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
+}
```

  It is in graduation criteria for GA, but if we get requests from users I think we can accelerate support for some of the features.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:13:51Z

- **pkg/cache/clusterqueue.go:338** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:13:51Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
```

  It means we don't support validation OOTB, if you use an external admission check with TAS you need to validate yourself. We could maybe make the mechanism under https://github.com/kubernetes-sigs/kueue/issues/3106 flexible enough to cover TAS.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:21:06Z

- **pkg/cache/clusterqueue.go:379** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:21:06Z
```diff
@@ -343,6 +379,7 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 	checksPerController := make(map[string][]string, len(c.AdmissionChecks))
```

  I would prefer in a follow up. 
  
  I actually tried to extend the unit tests but they seem very unintuitive, because they cover functions without any-user facing impact, so it is hard to interpret them, like: updateClusterQueue, UpdateWithFlavors. So, I thought the integration tests are more reliable here. 
  
  I would prefer unit tests here. My idea was to assert on `inactiveReason` function, and use `updateClusterQueue` for setup.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:21:52Z

- **pkg/cache/clusterqueue.go:348** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:21:52Z
```diff
@@ -315,6 +347,7 @@ func (c *clusterQueue) UpdateWithFlavors(flavors map[kueue.ResourceFlavorReferen
 
 func (c *clusterQueue) updateLabelKeys(flavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor) {
```

  I think the unit tests here aren't very useful as suggested in another comment. I would prefer to cover the `inactiveReason` function as in the other comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:23:56Z

- **pkg/cache/clusterqueue.go:321** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:23:56Z
```diff
@@ -297,6 +300,25 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			messages = append(messages, fmt.Sprintf("AdmissionCheck(s): %v cannot be set at flavor level", c.flavorIndependentAdmissionCheckAppliedPerFlavor))
 		}
 
+		if features.Enabled(features.TopologyAwareScheduling) && len(c.tasFlavors) > 0 {
+			if c.HasParent() {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for cohorts")
+			}
+			if c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for preemption within cluster queue")
+			}
+			if len(c.multiKueueAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with MultiKueue admission check")
+			}
+			if len(c.provisioningAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with ProvisioningRequest admission check")
+			}
+		}
+
```

  The selected code actually comes from the `inactiveReason` function. My idea is to write a unit test for this function, using `updateClusterQueue` as setup. However, I would prefer this as follow up to focus on completing the functional PRs, the integration tests we have cover all the cases here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-28T17:37:37Z

- **pkg/cache/clusterqueue.go:335** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T17:37:37Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
```

  I was wondering if we could support the validations in the CQ, RF, and Topology webhook. 
  Could you expand the reason why the validations should be done in the Kueue supported Jobs webhooks?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:41:02Z

- **pkg/cache/clusterqueue.go:335** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:41:02Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
```

  Oh, sorry, I was mistaken (was thinking about the annotations somehow we discussed in the morning). Indeed, it would be validation in the cq_webhook only for the scenarios in this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:43:16Z

- **pkg/cache/clusterqueue.go:335** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:43:16Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
```

  Still, we already have mechanics to validate CQ and deactivate, so the approach implemented here is more consistent with what we have.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-28T17:46:34Z

- **pkg/cache/clusterqueue.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T17:46:34Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
```

  > We could maybe make the mechanism under https://github.com/kubernetes-sigs/kueue/issues/3106 flexible enough to cover TAS.
  
  Let's investigate the possibility of this in the future.
  But, I guess that we can reject all CQs with any admissionCheck names (`.spec.admissionChecks` or `.spec.admissionChecksStrategy`), right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-28T17:48:45Z

- **pkg/cache/clusterqueue.go:321** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T17:48:44Z
```diff
@@ -297,6 +300,25 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			messages = append(messages, fmt.Sprintf("AdmissionCheck(s): %v cannot be set at flavor level", c.flavorIndependentAdmissionCheckAppliedPerFlavor))
 		}
 
+		if features.Enabled(features.TopologyAwareScheduling) && len(c.tasFlavors) > 0 {
+			if c.HasParent() {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for cohorts")
+			}
+			if c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for preemption within cluster queue")
+			}
+			if len(c.multiKueueAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with MultiKueue admission check")
+			}
+			if len(c.provisioningAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with ProvisioningRequest admission check")
+			}
+		}
+
```

  > However, I would prefer this as follow up to focus on completing the functional PRs, the integration tests we have cover all the cases here.
  
  That makes sense. Let's update the spread sheet.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:50:32Z

- **pkg/cache/clusterqueue.go:321** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:50:31Z
```diff
@@ -297,6 +300,25 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			messages = append(messages, fmt.Sprintf("AdmissionCheck(s): %v cannot be set at flavor level", c.flavorIndependentAdmissionCheckAppliedPerFlavor))
 		}
 
+		if features.Enabled(features.TopologyAwareScheduling) && len(c.tasFlavors) > 0 {
+			if c.HasParent() {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for cohorts")
+			}
+			if c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for preemption within cluster queue")
+			}
+			if len(c.multiKueueAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with MultiKueue admission check")
+			}
+			if len(c.provisioningAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with ProvisioningRequest admission check")
+			}
+		}
+
```

  Added as a new "Add unit test for CQ validation for unsupported configurations (the inactiveReason)" entry

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:52:15Z

- **pkg/cache/clusterqueue.go:338** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:52:15Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
```

  If we did that, then it would be stronger condition - meaning that TAS cannot be used with some external ACs, even if there is no issue. For example, the AC checks team's budget.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-28T17:54:32Z

- **pkg/cache/clusterqueue.go:379** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T17:54:32Z
```diff
@@ -343,6 +379,7 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 	checksPerController := make(map[string][]string, len(c.AdmissionChecks))
```

  I'm ok with a follow-up.
  As I mentioned here (https://github.com/kubernetes-sigs/kueue/pull/3320/files#r1819498096), let's update the spreadsheet.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T17:59:29Z

- **pkg/cache/clusterqueue.go:379** by [@mimowo](https://github.com/mimowo) — 2024-10-28T17:59:29Z
```diff
@@ -343,6 +379,7 @@ func (c *clusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 	checksPerController := make(map[string][]string, len(c.AdmissionChecks))
```

  I think this is already covered by the "Add unit test for CQ validation for unsupported configurations (the inactiveReason)" point. In this idea ClusterQueueUpdate will be used for setup, and inactiveReason will be actually asserted.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-28T20:28:04Z

- **pkg/cache/clusterqueue.go:321** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T20:28:04Z
```diff
@@ -297,6 +300,25 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 			messages = append(messages, fmt.Sprintf("AdmissionCheck(s): %v cannot be set at flavor level", c.flavorIndependentAdmissionCheckAppliedPerFlavor))
 		}
 
+		if features.Enabled(features.TopologyAwareScheduling) && len(c.tasFlavors) > 0 {
+			if c.HasParent() {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for cohorts")
+			}
+			if c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported for preemption within cluster queue")
+			}
+			if len(c.multiKueueAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with MultiKueue admission check")
+			}
+			if len(c.provisioningAdmissionChecks) > 0 {
+				reasons = append(reasons, kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling)
+				messages = append(messages, "TAS is not supported with ProvisioningRequest admission check")
+			}
+		}
+
```

  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-29T07:44:48Z

- **pkg/cache/clusterqueue.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-29T07:44:48Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
```

  > For example, the AC checks team's budget.
  
  That makes sense. In that case, we should reject the CQs with AC only related to the scheduling like ProvisioningRequest and MultiKueue. Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-29T07:45:24Z

- **pkg/cache/clusterqueue.go:339** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-29T07:45:24Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
+		c.Preemption.WithinClusterQueue != kueue.PreemptionPolicyNever ||
+		len(c.multiKueueAdmissionChecks) > 0 ||
+		len(c.provisioningAdmissionChecks) > 0
+}
```

  Oh, I missed the GA criteria. SGTM.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-29T07:50:12Z

- **pkg/cache/clusterqueue.go:335** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-29T07:50:12Z
```diff
@@ -306,6 +328,16 @@ func (c *clusterQueue) inactiveReason() (string, string) {
 	return kueue.ClusterQueueActiveReasonReady, "Can admit new workloads"
 }
 
+func (c *clusterQueue) isTASViolated() bool {
+	if !features.Enabled(features.TopologyAwareScheduling) || len(c.tasFlavors) == 0 {
+		return false
+	}
+	return c.HasParent() ||
```

  I discussed this with @mimowo offline.
  Technically, we can validate these points in the CQ, RF, and Topology validating webhooks.
  
  But, the webhook validation solutions have some issues like performance and complexity due to cross Object validations. Those fields are especially not immutable, and we can assume the validations will be performed many times. That could bring us cluster performance issues.
  
  So, we decided to select this non-webhook solutions.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-29T07:50:25Z

Thanks!
/lgtm
/approve
