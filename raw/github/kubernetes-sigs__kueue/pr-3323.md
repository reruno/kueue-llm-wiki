# PR #3323: Reset all AdmissionChecks on Eviction to Pending

**Summary**: Reset all AdmissionChecks on Eviction to Pending

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3323

**Last updated**: 2024-11-05T13:18:05Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `improve-ac-retry` → `main`
- **Created**: 2024-10-25T16:51:22Z
- **Updated**: 2024-11-05T13:18:05Z
- **Merged**: 2024-10-28T14:32:55Z
- **Closed**: 2024-10-28T14:32:55Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 8   **+57 / -20**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Currently, when AdmissionCheck is in `Retry` state, the Workloads gets evicted and there's no actual retry. The changes improves that flow, changing all AdmissionChecks to `Pending` if one of them is in `Retry` state (unless there's a `Rejected` AdmissionCheck).

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3322 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Workload is requeued with all AdmissionChecks set to Pending if there was an AdmissionCheck in Retry state.
```

## Discussion

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-25T16:51:32Z

/assign @mimowo

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-25T16:51:38Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1451b4ee1050dbc60866b2521c76142628c2fade |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/671f97c1d83c5100085ca0aa |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-28T12:46:45Z

/lgtm
/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-28T12:46:52Z

LGTM label has been added.  <details>Git tree hash: afcad61b453a0fec234011f5edbb2c5640012996</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-28T12:47:58Z

/retitle Reset all Ready and Ready AdmissionChecks on Eviction as Pending

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-28T12:50:17Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-28T12:50:56Z

I think we actually need to reset the checks regardless of the reason of eviction. For example, if the checks passed, but the workload is evicted due to WaitForPodsReady?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-28T14:31:02Z

When deactivating (evicting with deactivation reason) a workload we reset the `requeueState` so that it is "clean" or re-activation: https://github.com/kubernetes-sigs/kueue/blob/42c7b476ac13dbf2d494227ca831b91f262fa07e/pkg/controller/core/workload_controller.go#L203-L206

I think for consistency we should do the same with checks, but we can re-visit this as a follow up.

/lgtm
/approve
/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-28T14:31:10Z

LGTM label has been added.  <details>Git tree hash: 2c3dd1a32282d6a662b4c6c8192d1f63d446f7e9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-28T14:31:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3323#issuecomment-2441756535" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3323#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T17:27:01Z

It seems that this is obviously bug.
/cherry-pick release-0.8

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-10-28T17:27:38Z

@tenzen-y: #3323 failed to apply on top of branch "release-0.8":
```
Applying: Make AdmissionChecks Pending after one is in Retry state
Applying: Reduce number of patch requests
Using index info to reconstruct a base tree...
M	pkg/controller/core/workload_controller.go
M	pkg/scheduler/scheduler.go
M	pkg/workload/admissionchecks.go
M	pkg/workload/workload.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/workload/workload.go
CONFLICT (content): Merge conflict in pkg/workload/workload.go
Auto-merging pkg/workload/admissionchecks.go
Auto-merging pkg/scheduler/scheduler.go
CONFLICT (content): Merge conflict in pkg/scheduler/scheduler.go
Auto-merging pkg/controller/core/workload_controller.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0002 Reduce number of patch requests

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3323#issuecomment-2442200150):

>It seems that this is obviously bug.
>/cherry-pick release-0.8
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-29T12:47:25Z

> /cherry-pick release-0.8

I guess we could cherry-pick but then we also need to cherry-pick the forced-ownership PR which might be more that we want.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T08:19:22Z

- **pkg/controller/core/workload_controller.go:167** by [@mimowo](https://github.com/mimowo) — 2024-10-28T08:19:22Z
```diff
@@ -164,7 +164,7 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			case kueue.WorkloadEvictedByDeactivation:
 				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
 				updated = true
-			case kueue.WorkloadEvictedByPodsReadyTimeout:
+			case kueue.WorkloadEvictedByPodsReadyTimeout, kueue.WorkloadEvictedByAdmissionCheck:
```

  I'm wondering if this change belong conceptually to the same fix.
  IIUC the intention of this change (and the one below 
  ```
  // The requeued condition status set to true only on EvictedByPreemption
  setRequeued := evCond.Reason == kueue.WorkloadEvictedByPreemption
  ```
  IIUC they are aiming to "respect backoff delay when requeuing due to failed admission checks", and could be a separate bugfix. Let me know if I'm missing something.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-28T09:28:03Z

- **pkg/controller/core/workload_controller.go:167** by [@PBundyra](https://github.com/PBundyra) — 2024-10-28T09:28:03Z
```diff
@@ -164,7 +164,7 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			case kueue.WorkloadEvictedByDeactivation:
 				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
 				updated = true
-			case kueue.WorkloadEvictedByPodsReadyTimeout:
+			case kueue.WorkloadEvictedByPodsReadyTimeout, kueue.WorkloadEvictedByAdmissionCheck:
```

  That's right, fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T09:39:51Z

- **pkg/workload/workload.go:654** by [@mimowo](https://github.com/mimowo) — 2024-10-28T09:39:51Z
```diff
@@ -649,6 +649,11 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+// ApplyStatusPatch applies the patch of status fields of a workload with SSA.
+func ApplyStatusPatch(ctx context.Context, c client.Client, patch *kueue.Workload) error {
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.WorkloadControllerName))
```

  I'm wondering if we need another entity to make the patch. What is the entity managing status Workload fields like `status.requeueState`, or changing the readiness of checks when transitioning from Pending -> Ready or Rejected?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-28T11:15:07Z

- **pkg/workload/workload.go:654** by [@PBundyra](https://github.com/PBundyra) — 2024-10-28T11:15:06Z
```diff
@@ -649,6 +649,11 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+// ApplyStatusPatch applies the patch of status fields of a workload with SSA.
+func ApplyStatusPatch(ctx context.Context, c client.Client, patch *kueue.Workload) error {
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.WorkloadControllerName))
```

  Regarding `status.requeueState` the entity is `kueue-admission`, and for transitions e.g. Pending -> Ready it's `kueue.x-k8s.io/provisioning-request`. Hence, I decided to go with `kueue-workload-controller` since this is the controller that calls this function. We can come up with some more consitent semantics for the field owners, but I believe this is out of the scope of this PR. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T11:18:54Z

- **pkg/controller/core/workload_controller.go:349** by [@mimowo](https://github.com/mimowo) — 2024-10-28T11:18:54Z
```diff
@@ -346,6 +346,9 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true); err != nil {
 		return false, client.IgnoreNotFound(err)
 	}
+	if err := workload.SetAllChecksToPending(ctx, r.client, wl); err != nil {
```

  Actually, I think it is better to update the state for the checks just after `workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, message)`, and submit in the `ApplyAdmissionStatus`, because now it requires to Patches, and both update the status.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T11:19:38Z

- **pkg/workload/workload.go:654** by [@mimowo](https://github.com/mimowo) — 2024-10-28T11:19:38Z
```diff
@@ -649,6 +649,11 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+// ApplyStatusPatch applies the patch of status fields of a workload with SSA.
+func ApplyStatusPatch(ctx context.Context, c client.Client, patch *kueue.Workload) error {
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.WorkloadControllerName))
```

  I see, this is fine from that perspective, but maybe we don't need to think about it if we update the checks along with setting Evicted=true, as in the other comment: https://github.com/kubernetes-sigs/kueue/pull/3323/files#r1818884218?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-28T11:39:28Z

- **pkg/controller/core/workload_controller.go:349** by [@PBundyra](https://github.com/PBundyra) — 2024-10-28T11:39:28Z
```diff
@@ -346,6 +346,9 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true); err != nil {
 		return false, client.IgnoreNotFound(err)
 	}
+	if err := workload.SetAllChecksToPending(ctx, r.client, wl); err != nil {
```

  Agree, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-28T11:40:04Z

- **pkg/workload/workload.go:654** by [@PBundyra](https://github.com/PBundyra) — 2024-10-28T11:40:04Z
```diff
@@ -649,6 +649,11 @@ func ApplyAdmissionStatusPatch(ctx context.Context, c client.Client, patch *kueu
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+// ApplyStatusPatch applies the patch of status fields of a workload with SSA.
+func ApplyStatusPatch(ctx context.Context, c client.Client, patch *kueue.Workload) error {
+	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.WorkloadControllerName))
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T11:47:56Z

- **pkg/controller/core/workload_controller.go:383** by [@mimowo](https://github.com/mimowo) — 2024-10-28T11:47:56Z
```diff
@@ -380,6 +380,7 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, message)
+	workload.SetAllChecksToPending(wl, "AdmissionCheck pending after retry")
```

  It is not necessarily "after retry" as ready checks can also be flipped. I would move the message construction under the function as it potentially could differ.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T11:48:37Z

- **pkg/workload/admissionchecks.go:97** by [@mimowo](https://github.com/mimowo) — 2024-10-28T11:48:36Z
```diff
@@ -86,6 +86,19 @@ func FindAdmissionCheck(checks []kueue.AdmissionCheckState, checkName string) *k
 	return nil
 }
 
+// SetAllChecksToPending sets all AdmissionChecks to Pending
+func SetAllChecksToPending(w *kueue.Workload, message string) {
+	checks := w.Status.AdmissionChecks
+	for i := range checks {
+		checks[i] = kueue.AdmissionCheckState{
+			Name:               checks[i].Name,
+			State:              kueue.CheckStatePending,
+			LastTransitionTime: metav1.NewTime(time.Now()),
+			Message:            message,
```

  Maybe just say "Reset to Pending due to eviction. Previously: <reason>"

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-28T12:22:05Z

- **pkg/controller/core/workload_controller.go:383** by [@PBundyra](https://github.com/PBundyra) — 2024-10-28T12:22:05Z
```diff
@@ -380,6 +380,7 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, message)
+	workload.SetAllChecksToPending(wl, "AdmissionCheck pending after retry")
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-28T12:22:08Z

- **pkg/workload/admissionchecks.go:97** by [@PBundyra](https://github.com/PBundyra) — 2024-10-28T12:22:08Z
```diff
@@ -86,6 +86,19 @@ func FindAdmissionCheck(checks []kueue.AdmissionCheckState, checkName string) *k
 	return nil
 }
 
+// SetAllChecksToPending sets all AdmissionChecks to Pending
+func SetAllChecksToPending(w *kueue.Workload, message string) {
+	checks := w.Status.AdmissionChecks
+	for i := range checks {
+		checks[i] = kueue.AdmissionCheckState{
+			Name:               checks[i].Name,
+			State:              kueue.CheckStatePending,
+			LastTransitionTime: metav1.NewTime(time.Now()),
+			Message:            message,
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-28T12:29:59Z

- **pkg/controller/core/workload_controller.go:383** by [@mimowo](https://github.com/mimowo) — 2024-10-28T12:29:59Z
```diff
@@ -380,7 +380,7 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, message)
-	workload.SetAllChecksToPending(wl, "AdmissionCheck pending after retry")
+	workload.SetAllChecksToPending(wl, "Reset to Pending after eviction. Previously: ")
```

  ```suggestion
  	workload.ResetChecksOnEviction(wl)
  ```
  nit, I think constructing the message prefix makes it a bit weird to follow

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-28T12:44:56Z

- **pkg/controller/core/workload_controller.go:383** by [@PBundyra](https://github.com/PBundyra) — 2024-10-28T12:44:56Z
```diff
@@ -380,7 +380,7 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	// at this point we know a Workload has at least one Retry AdmissionCheck
 	message := "At least one admission check is false"
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, message)
-	workload.SetAllChecksToPending(wl, "AdmissionCheck pending after retry")
+	workload.SetAllChecksToPending(wl, "Reset to Pending after eviction. Previously: ")
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-28T17:25:24Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:450** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-28T17:23:15Z
```diff
@@ -447,6 +447,7 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					fmt.Printf("Workload %+v\n", updatedWl)
```

  @PBundyra Could you remove this debug log?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-29T13:06:12Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:450** by [@PBundyra](https://github.com/PBundyra) — 2024-10-29T13:06:12Z
```diff
@@ -447,6 +447,7 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					fmt.Printf("Workload %+v\n", updatedWl)
```

  Sure, will do in a follow-up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T08:55:41Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:450** by [@mimowo](https://github.com/mimowo) — 2024-10-31T08:55:41Z
```diff
@@ -447,6 +447,7 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					fmt.Printf("Workload %+v\n", updatedWl)
```

  This is needed for cheery picking to 0.8.2

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T09:05:28Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:450** by [@mimowo](https://github.com/mimowo) — 2024-10-31T09:05:28Z
```diff
@@ -447,6 +447,7 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					fmt.Printf("Workload %+v\n", updatedWl)
```

  taken that: https://github.com/kubernetes-sigs/kueue/pull/3389
