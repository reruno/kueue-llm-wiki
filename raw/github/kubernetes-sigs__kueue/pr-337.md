# PR #337: Use server-side-apply for admission

**Summary**: Use server-side-apply for admission

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/337

**Last updated**: 2022-08-19T16:35:52Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `ssa` → `main`
- **Created**: 2022-08-16T20:09:56Z
- **Updated**: 2022-08-19T16:35:52Z
- **Merged**: 2022-08-19T16:35:52Z
- **Closed**: 2022-08-19T16:35:52Z
- **Labels**: `kind/feature`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 8   **+46 / -35**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

This would reduce the chances of conflict updating a Workload, because a controller might have updated the workload's status.

There would still be conflicts in the case of Spec updates. This is achieved setting the .metadata.generation field.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #164

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-16T20:10:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/337#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-17T19:13:31Z

/assign @ahg-g

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-08-18T14:48:36Z

/cc

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T16:31:08Z

/lgtm

## Reviews

### Review by [@apelisse](https://github.com/apelisse) (commented) — 2022-08-16T20:36:09Z

- **config/crd/bases/kueue.x-k8s.io_workloads.yaml:7440** by [@apelisse](https://github.com/apelisse) — 2022-08-16T20:36:09Z
```diff
@@ -7437,8 +7437,6 @@ spec:
                 description: queueName is the name of the queue the Workload is associated
                   with. queueName cannot be changed once set.
                 type: string
-            required:
```

  Is this intended? :-)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-16T20:59:38Z

- **config/crd/bases/kueue.x-k8s.io_workloads.yaml:7440** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-16T20:59:38Z
```diff
@@ -7437,8 +7437,6 @@ spec:
                 description: queueName is the name of the queue the Workload is associated
                   with. queueName cannot be changed once set.
                 type: string
-            required:
```

  yes. Empty string is valid.

### Review by [@apelisse](https://github.com/apelisse) (commented) — 2022-08-16T21:13:11Z

- **config/crd/bases/kueue.x-k8s.io_workloads.yaml:7440** by [@apelisse](https://github.com/apelisse) — 2022-08-16T21:13:11Z
```diff
@@ -7437,8 +7437,6 @@ spec:
                 description: queueName is the name of the queue the Workload is associated
                   with. queueName cannot be changed once set.
                 type: string
-            required:
```

  But is empty string the same as unspecified string? Because that's what you have now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-17T12:59:52Z

- **config/crd/bases/kueue.x-k8s.io_workloads.yaml:7440** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-17T12:59:51Z
```diff
@@ -7437,8 +7437,6 @@ spec:
                 description: queueName is the name of the queue the Workload is associated
                   with. queueName cannot be changed once set.
                 type: string
-            required:
```

  yes, the default is empty and empty is valid

### Review by [@apelisse](https://github.com/apelisse) (commented) — 2022-08-17T19:49:39Z

- **config/crd/bases/kueue.x-k8s.io_workloads.yaml:7440** by [@apelisse](https://github.com/apelisse) — 2022-08-17T19:49:39Z
```diff
@@ -7437,8 +7437,6 @@ spec:
                 description: queueName is the name of the queue the Workload is associated
                   with. queueName cannot be changed once set.
                 type: string
-            required:
```

  it's not great then but I guess it works.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-18T18:17:26Z

- **pkg/constants/constants.go:29** by [@ahg-g](https://github.com/ahg-g) — 2022-08-18T17:54:05Z
```diff
@@ -25,8 +25,8 @@ const (
 	QueueAnnotation = "kueue.x-k8s.io/queue-name"
 
 	KueueName         = "kueue"
-	ManagerName       = KueueName + "-manager"
 	JobControllerName = KueueName + "-job-controller"
+	AdmissionName     = KueueName + "-admission"
```

  `admission` doesn't describe an entity, why are we changing this name?

- **pkg/scheduler/scheduler.go:372** by [@ahg-g](https://github.com/ahg-g) — 2022-08-18T18:16:57Z
```diff
@@ -351,6 +357,27 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 	return nil
 }
 
+func (s *Scheduler) applyAdmissionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return s.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// workloadAdmissionFrom returns only the fields necessary for admission using
+// ServerSideApply.
+func workloadAdmissionFrom(w *kueue.Workload) *kueue.Workload {
+	return &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			UID:        w.UID,
+			Name:       w.Name,
+			Namespace:  w.Namespace,
+			Generation: w.Generation, // Produce a conflict if there was a change in the spec.
```

  but then what conflicts does server-side apply help with vs Update? I assume changes in Status doesn't produce conflicts with the Update, or does it?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-18T20:47:08Z

- **pkg/constants/constants.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T20:47:08Z
```diff
@@ -25,8 +25,8 @@ const (
 	QueueAnnotation = "kueue.x-k8s.io/queue-name"
 
 	KueueName         = "kueue"
-	ManagerName       = KueueName + "-manager"
 	JobControllerName = KueueName + "-job-controller"
+	AdmissionName     = KueueName + "-admission"
```

  https://kubernetes.io/docs/reference/using-api/server-side-apply/#field-management
  
  Every field of the object has a manager, even if you didn't "Apply".
  If you don't specify the manager through CreateOptions or UpdateOptions, the manager is extracted from the UserAgent (hence #336).
  
  I wanted to distinguish things that are applied by the admission logic, rather than kueue as a whole.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-18T20:48:43Z

- **pkg/scheduler/scheduler.go:372** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T20:48:42Z
```diff
@@ -351,6 +357,27 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 	return nil
 }
 
+func (s *Scheduler) applyAdmissionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return s.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// workloadAdmissionFrom returns only the fields necessary for admission using
+// ServerSideApply.
+func workloadAdmissionFrom(w *kueue.Workload) *kueue.Workload {
+	return &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			UID:        w.UID,
+			Name:       w.Name,
+			Namespace:  w.Namespace,
+			Generation: w.Generation, // Produce a conflict if there was a change in the spec.
```

  Status changes do produce conflicts with Update (at least I have observed them in integration tests).

### Review by [@apelisse](https://github.com/apelisse) (commented) — 2022-08-18T22:00:34Z

- **pkg/scheduler/scheduler.go:372** by [@apelisse](https://github.com/apelisse) — 2022-08-18T22:00:33Z
```diff
@@ -351,6 +357,27 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 	return nil
 }
 
+func (s *Scheduler) applyAdmissionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return s.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// workloadAdmissionFrom returns only the fields necessary for admission using
+// ServerSideApply.
+func workloadAdmissionFrom(w *kueue.Workload) *kueue.Workload {
+	return &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			UID:        w.UID,
+			Name:       w.Name,
+			Namespace:  w.Namespace,
+			Generation: w.Generation, // Produce a conflict if there was a change in the spec.
```

  I'm not sure I understand this, but that sounds like something we might have to be careful about. Can someone explain that to me?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-19T13:14:05Z

- **pkg/scheduler/scheduler.go:372** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T13:14:05Z
```diff
@@ -351,6 +357,27 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 	return nil
 }
 
+func (s *Scheduler) applyAdmissionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return s.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+// workloadAdmissionFrom returns only the fields necessary for admission using
+// ServerSideApply.
+func workloadAdmissionFrom(w *kueue.Workload) *kueue.Workload {
+	return &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			UID:        w.UID,
+			Name:       w.Name,
+			Namespace:  w.Namespace,
+			Generation: w.Generation, // Produce a conflict if there was a change in the spec.
```

  We want the Apply to fail if there was any chance in the spec with respect to the version we took a decision on. However, we don't care if the status has changed.
  
  That was our motivation to use SSA. Daniel Smith suggested we use the generation for this purpose https://github.com/kubernetes-sigs/kueue/issues/164#issuecomment-1083394946
