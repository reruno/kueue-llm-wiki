# PR #3373: TAS: Skip RFs which don't match PodSet TAS requirements

**Summary**: TAS: Skip RFs which don't match PodSet TAS requirements

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3373

**Last updated**: 2025-01-21T10:54:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-podset-cq-mismatch` → `main`
- **Created**: 2024-10-30T08:34:33Z
- **Updated**: 2025-01-21T10:54:11Z
- **Merged**: 2024-10-30T15:59:26Z
- **Closed**: 2024-10-30T15:59:26Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+192 / -1**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #2724 

#### Special notes for your reviewer:

This is built on top of https://github.com/kubernetes-sigs/kueue/pull/3361.
WIP to merge first the scheduler-level tests.

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-30T08:34:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3373#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-30T08:34:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c94b9018e7548eef5a629f3a32cac5cebdb81e86 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6722541b68f2c0000869ec62 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-30T08:35:12Z

/cc @PBundyra @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-30T14:58:46Z

/cc @mbobrovskyi

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-10-30T15:57:14Z

LGTM label has been added.  <details>Git tree hash: 03e242bcdf38e6b3443a789350f66ea9d1a1fcb8</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-10-30T15:57:08Z

/lgtm

Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-30T16:22:06Z

I left a few comment.

- **pkg/scheduler/flavorassigner/flavorassigner.go:458** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-30T16:14:14Z
```diff
@@ -441,6 +442,20 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.append(fmt.Sprintf("flavor %s not found", fName))
 			continue
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			// For PodSets which require TAS skip resource flavors which don't support it
+			if ps.TopologyRequest != nil && flavor.Spec.TopologyName == nil {
+				log.Error(nil, "Flavor does not support TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s does not support TopologyAwareScheduling", fName))
+				continue
+			}
+			// For PodSets which don't use TAS skip resource flavors which are only for TAS
+			if ps.TopologyRequest == nil && flavor.Spec.TopologyName != nil {
+				log.Error(nil, "Flavor supports only TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s supports only TopologyAwareScheduling", fName))
+				continue
+			}
+		}
```

  Shouldn't we move these logic to the `pkg/scheduler/flavorassigner/tas_flavorassigner.go` same as the `func assignTopology()`?
  Let's separate the responsibility.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T16:31:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:458** by [@mimowo](https://github.com/mimowo) — 2024-10-30T16:31:33Z
```diff
@@ -441,6 +442,20 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.append(fmt.Sprintf("flavor %s not found", fName))
 			continue
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			// For PodSets which require TAS skip resource flavors which don't support it
+			if ps.TopologyRequest != nil && flavor.Spec.TopologyName == nil {
+				log.Error(nil, "Flavor does not support TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s does not support TopologyAwareScheduling", fName))
+				continue
+			}
+			// For PodSets which don't use TAS skip resource flavors which are only for TAS
+			if ps.TopologyRequest == nil && flavor.Spec.TopologyName != nil {
+				log.Error(nil, "Flavor supports only TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s supports only TopologyAwareScheduling", fName))
+				continue
+			}
+		}
```

  We could move this easily to a helper function Iin tas_flavorassigner,  checkPodSetAndFlavorCompatibleForTAS
  
  However, I do the logic here to prevent trying to compute the assignments.
  
  For example if PodSet is requesting TAS there is no point to compute assignment for non TAS flavors, so we can skip. Maybe we could compute the assignments and then drop, but I think it is more complicated.
  
  So I don't see a clearly better place to inject with the logic.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-10-31T08:40:46Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:458** by [@tenzen-y](https://github.com/tenzen-y) — 2024-10-31T08:40:45Z
```diff
@@ -441,6 +442,20 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.append(fmt.Sprintf("flavor %s not found", fName))
 			continue
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			// For PodSets which require TAS skip resource flavors which don't support it
+			if ps.TopologyRequest != nil && flavor.Spec.TopologyName == nil {
+				log.Error(nil, "Flavor does not support TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s does not support TopologyAwareScheduling", fName))
+				continue
+			}
+			// For PodSets which don't use TAS skip resource flavors which are only for TAS
+			if ps.TopologyRequest == nil && flavor.Spec.TopologyName != nil {
+				log.Error(nil, "Flavor supports only TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s supports only TopologyAwareScheduling", fName))
+				continue
+			}
+		}
```

  I just imagined the below code. My objective is to distribute the TAS-related codes to tas_flavorassigner.go.
  Note that the below code is pseudo-code.
  
  ```go
  func isFlavorAndAssignmentSupportTAS(ps, flavor, fName, status) bool { 
  		// For PodSets which require TAS skip resource flavors which don't support it
  		if ps.TopologyRequest != nil && flavor.Spec.TopologyName == nil {
  			log.Error(nil, "Flavor does not support TopologyAwareScheduling", "Flavor", fName)
  			status.append(fmt.Sprintf("flavor %s does not support TopologyAwareScheduling", fName))
  			return false
  		}
  		// For PodSets which don't use TAS skip resource flavors which are only for TAS
  		if ps.TopologyRequest == nil && flavor.Spec.TopologyName != nil {
  			log.Error(nil, "Flavor supports only TopologyAwareScheduling", "Flavor", fName)
  			status.append(fmt.Sprintf("flavor %s supports only TopologyAwareScheduling", fName))
  			return false
  		}
  		return true
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T09:39:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:458** by [@mimowo](https://github.com/mimowo) — 2024-10-31T09:39:34Z
```diff
@@ -441,6 +442,20 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.append(fmt.Sprintf("flavor %s not found", fName))
 			continue
 		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			// For PodSets which require TAS skip resource flavors which don't support it
+			if ps.TopologyRequest != nil && flavor.Spec.TopologyName == nil {
+				log.Error(nil, "Flavor does not support TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s does not support TopologyAwareScheduling", fName))
+				continue
+			}
+			// For PodSets which don't use TAS skip resource flavors which are only for TAS
+			if ps.TopologyRequest == nil && flavor.Spec.TopologyName != nil {
+				log.Error(nil, "Flavor supports only TopologyAwareScheduling", "Flavor", fName)
+				status.append(fmt.Sprintf("flavor %s supports only TopologyAwareScheduling", fName))
+				continue
+			}
+		}
```

  sure, opened: https://github.com/kubernetes-sigs/kueue/pull/3391
