# PR #3375: Introduce RetryStrategy API to ProvisioningRequestConfig

**Summary**: Introduce RetryStrategy API to ProvisioningRequestConfig

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3375

**Last updated**: 2025-08-12T10:15:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `ProvReq-config-retry` → `main`
- **Created**: 2024-10-30T09:04:54Z
- **Updated**: 2025-08-12T10:15:47Z
- **Merged**: 2024-11-05T13:47:29Z
- **Closed**: 2024-11-05T13:47:29Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 22   **+1155 / -264**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1353 

#### Special notes for your reviewer:

I am yet to add integration tests, but the rest of the code is ready for the first pass

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
ProvisioningRequestConfig API has now `RetryStrategy` field that allows users to configure retries per ProvisioningRequest class. By default retry releases allocated quota in Kueue. 
```

## Discussion

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T09:05:07Z

/assign @mimowo @tenzen-y

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-10-30T09:05:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d19a4ccc816cc7eecedf8e7db96fbe63657d4650 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6729f6a09798ed0008c41ed3 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:25:23Z

It looks like we are converging onto the solution, but left some comments to clarify doubts.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-04T15:51:50Z

/lgtm
/approve
Just one remark, but I think we can address in a follow up: https://github.com/kubernetes-sigs/kueue/pull/3375#discussion_r1827957053 after release
/hold 
just in case @tenzen-y has some more comments

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-04T15:51:58Z

LGTM label has been added.  <details>Git tree hash: b399f08207b36f7091fda86281ce35873d8f7e43</details>

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2024-11-05T10:18:09Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: PBundyra / name: Patryk Bundyra  (6002427da99a46fb8b32a1b22b368e44adb7a8b1, aaf08cde8d3e76ed688bd2f8803f5b7a4a91eb08, 55daaf8347b00741022cbe5f887e5a2debdd929e, 8b14c92a41b50c30a89a4a7cdd46bfdd4912e60b, 57dd724139246eb2d4913e24c4bdfc4b44c9752b, f5dbc7f5e69a0afbfadc2960206ca66cd347cef0, c8b93d2c47ebf6e736393ae9d2fba3baeeff44f1, c60a500094386b43f234dd9c86051d869599f22f, 9b6e1a6d344b0c5c61d1ba8983800d58bec8d4d5, 7829e64f0e1ac17f5ad30c6647a865d27b05ad41, 1df2fdcbbf961ff6cdb1cb568010c8759218ffc5, 5d6baf831c024a538b0fea5b55c499866b9bde88, f9fa2689c8d01f3bac81a48c4662de24011c9cfb, f1b250409e2f45bc76abeae3f7f2f837d5770c23, ee4b42c767b6ae286adfcd9bde7306f8050a27f5, 29cd2a6605ca380cf7cb0ff8ca3841e119d6fc7e, d5b5a7af71fc33aa60c536b0137cc25a900378eb, 03eb18296a477e21f13f72cc9a90b08ced858390, d19a4ccc816cc7eecedf8e7db96fbe63657d4650, 4206edd5b00e12e786305124dab2856eadbfe6ab, 3487594d73eeafeb69ae056076e5c4090e85634c, 442076e606e3a81db5918243924dedebd6432985, c35c7e8261f4e28767ca6aa90f1c9719dcc995f5, fc822b2a79c2851359686c1861d66f451d479519, 785d48f5fd9bffbbf70f9d6eb5ad876f0672fc6a)</li></ul><!-- Date Modified: 2024-11-05 10:42:43.525400 -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-05T10:46:09Z

> Additionally, is this still needed?
> 
> https://github.com/PBundyra/kueue/blob/29cd2a6605ca380cf7cb0ff8ca3841e119d6fc7e/pkg/controller/core/workload_controller.go#L370-L371
> 
> I guess that answer is yes since this is not only for ProvReq, this is used by other AdmissionChecks.

I believe so, even for ProvReq. Why do you think this is redundant? ProvReq check can also be in `Rejected` state, and then we want to deactivate the Workload

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-05T13:01:18Z

I believe the last remaining [comment](https://github.com/kubernetes-sigs/kueue/pull/3375#discussion_r1828359787) is more exploratory in nature, and I don't see any "quick fix" there. I summarized my understanding of the issue in the last message. Based on that understanding and the e2e tests performed by @PBundyra I believe it is safe to move the PR forward. I believe we can address remaining ideas in follow up PRs / issues, for example as I proposed to explore setting `Requeued: False` when reserving the quota.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-05T13:02:31Z

/lgtm
/approve
Thank you @PBundyra  for the code and @tenzen-y for careful review!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-05T13:02:36Z

LGTM label has been added.  <details>Git tree hash: 37c6cd40321ca21fb58caa4420010eb6715efc24</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-05T13:02:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3375#issuecomment-2457110990" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3375#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-05T13:10:27Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-05T16:20:42Z

> > Additionally, is this still needed?
> > https://github.com/PBundyra/kueue/blob/29cd2a6605ca380cf7cb0ff8ca3841e119d6fc7e/pkg/controller/core/workload_controller.go#L370-L371
> > I guess that answer is yes since this is not only for ProvReq, this is used by other AdmissionChecks.
> 
> I believe so, even for ProvReq. Why do you think this is redundant? ProvReq check can also be in `Rejected` state, and then we want to deactivate the Workload

That was just a question. So, I did not indicate the refining that part :)

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T09:50:56Z

initial review pass

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:64** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:14:37Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest
+	// if nil then default configuration will be applied
+	// to switch off retry mechanism completely, set retryStrategy.backoffLimitCount to 0.
```

  ```suggestion
  	// retryStrategy defines strategy for retrying ProvisioningRequest.
  	// If null, then the default configuration is applied. To to switch off retry mechanism
  	// set retryStrategy.backoffLimitCount to 0.
  ```
  nit

- **pkg/controller/admissionchecks/provisioning/controller.go:258** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:29:45Z
```diff
@@ -247,16 +216,18 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
+				if attempt <= ptr.Deref(retryStrategy.BackoffLimitCount, kueue.DefaultBackoffLimitCount) {
+					remainingTimeToRetry := c.remainingTimeToRetry(wlInfo, oldPr, attempt, prc)
+					if remainingTimeToRetry <= 0 {
 						shouldCreatePr = true
 						attempt += 1
-					} else if requeAfter == nil || remainingTime < *requeAfter {
```

  Why is this removed? I think the code makes sense to use minimum as the  effective `requeueAfter` rather than the last, but I agree we may not have tests for it

- **pkg/controller/admissionchecks/provisioning/controller.go:225** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:30:42Z
```diff
@@ -247,16 +216,18 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
+				if attempt <= ptr.Deref(retryStrategy.BackoffLimitCount, kueue.DefaultBackoffLimitCount) {
+					remainingTimeToRetry := c.remainingTimeToRetry(wlInfo, oldPr, attempt, prc)
```

  ```suggestion
  					remainingTime := c.remainingTimeToRetry(wlInfo, oldPr, attempt, prc)
  ```
  nit, your name makes sens, but this is a delicate PR, so I would prefer to minimize review distractions by renames. We can follow up with a cleanup PR. Same for requeueAfter.

- **pkg/controller/admissionchecks/provisioning/controller.go:296** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:33:18Z
```diff
@@ -317,12 +288,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 			return nil, err
 		}
 	}
-	return requeAfter, nil
+	return requeueAfter, nil
 }
 
-func (c *Controller) remainingTimeToRetry(pr *autoscaling.ProvisioningRequest, failuresCount int32) time.Duration {
-	backoffDuration := time.Duration(c.minBackoffSeconds) * time.Second
-	maxBackoffDuration := time.Duration(c.maxBackoffSeconds) * time.Second
+func (c *Controller) remainingTimeToRetry(wlInfo *workloadInfo, pr *autoscaling.ProvisioningRequest, failuresCount int32, prc *kueue.ProvisioningRequestConfig) time.Duration {
+	if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+		return wlInfo.requeueState.RequeueAt.Sub(time.Now())
```

  Hm is this what we want? IIUC RequeueAt should be in the future.
  If this is correct then add a comment to explain when / where the backoff is applied in this case.

- **pkg/controller/admissionchecks/provisioning/controller.go:466** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:39:38Z
```diff
@@ -487,8 +463,19 @@ func updateCheckState(checkState *kueue.AdmissionCheckState, state kueue.CheckSt
 	return true
 }
 
-func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) error {
+func updateWorkloadInfo(wl *kueue.Workload, wlInfo *workloadInfo) {
```

  nit: let's make this a function received by `workloadInfo` so we can use `workloadInfo.update(wl)`

- **pkg/controller/admissionchecks/provisioning/controller_test.go:1182** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:41:44Z
```diff
@@ -1122,25 +1153,58 @@ func TestReconcile(t *testing.T) {
 					Reason:    "ProvisioningRequestCreated",
 					Message:   `Created ProvisioningRequest: "wl-check1-2"`,
 				},
+			}},
+		"workload retries the admission check when is not admitted and receives the provisioning request's BookingExpired condition": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				Admitted(false).
+				Obj(),
+			checks:  []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
+			flavors: []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
+			configs: []kueue.ProvisioningRequestConfig{*provReqConfigWithRetryLimit(baseConfig, 1)},
+			requests: []autoscaling.ProvisioningRequest{
+				*requestWithConditions(baseRequest,
+					[]metav1.Condition{
+						{
+							Type:   autoscaling.Failed,
+							Status: metav1.ConditionFalse,
+						},
+						{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:   autoscaling.Accepted,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:               autoscaling.BookingExpired,
+							Status:             metav1.ConditionTrue,
+							LastTransitionTime: metav1.NewTime(time.Now().Add(time.Hour * (-1))),
```

  This seems like having a potential to flake if we are on the edge of an hour. Can we use fake clock for that?

- **pkg/controller/admissionchecks/provisioning/controller.go:525** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:45:40Z
```diff
@@ -519,19 +505,29 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if checkState.State == kueue.CheckStateRejected {
+					break
+				}
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= ptr.Deref(retryStrategy.BackoffLimitCount, kueue.DefaultBackoffLimitCount) {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
 					} else {
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch,
+							ptr.Deref(retryStrategy.BackoffBaseSeconds, kueue.DefaultBackoffBaseSeconds),
+							ptr.Deref(retryStrategy.BackoffMaxSeconds, kueue.DefaultBackoffMaxSeconds))
```

  fallback via DefaultRetryStrategy

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:77** by [@mimowo](https://github.com/mimowo) — 2024-10-30T09:50:32Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest
+	// if nil then default configuration will be applied
+	// to switch off retry mechanism completely, set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+const (
+	DefaultBackoffLimitCount  int32 = 3
+	DefaultBackoffBaseSeconds int32 = 60   // 1 min
+	DefaultBackoffMaxSeconds  int32 = 1800 // 30 min
+)
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
```

  Maybe this could have its own type, so that we don't use ptrs inside? Just
  ```
  type defualtRetryStrategy struct {
    int32 backoffLimitCount
    ...
  ```
  then we could refer to the defaults via the `DefaultRetryStrategy`. Cause currently we have two sources of truth when it comes to the defaults (the structure and the constants).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T12:17:19Z

- **pkg/controller/admissionchecks/provisioning/controller.go:296** by [@mimowo](https://github.com/mimowo) — 2024-10-30T12:17:19Z
```diff
@@ -317,12 +288,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 			return nil, err
 		}
 	}
-	return requeAfter, nil
+	return requeueAfter, nil
 }
 
-func (c *Controller) remainingTimeToRetry(pr *autoscaling.ProvisioningRequest, failuresCount int32) time.Duration {
-	backoffDuration := time.Duration(c.minBackoffSeconds) * time.Second
-	maxBackoffDuration := time.Duration(c.maxBackoffSeconds) * time.Second
+func (c *Controller) remainingTimeToRetry(wlInfo *workloadInfo, pr *autoscaling.ProvisioningRequest, failuresCount int32, prc *kueue.ProvisioningRequestConfig) time.Duration {
+	if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+		return wlInfo.requeueState.RequeueAt.Sub(time.Now())
```

  Oh, I misread it, actually you subtract Now which seems ok-ish. 
  
  Still, I'm wondering if we could simply return 0 in this scenario. My thinking is that we only get here if the workload has QuotaReserved=true, and this Requeued=True, and thus RequeueAt should be in the past. 
  
  Possibly I'm missing some scenario above, but it would be good to capture it in a comment in that case.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T12:40:43Z

- **pkg/controller/admissionchecks/provisioning/controller.go:296** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T12:40:43Z
```diff
@@ -317,12 +288,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 			return nil, err
 		}
 	}
-	return requeAfter, nil
+	return requeueAfter, nil
 }
 
-func (c *Controller) remainingTimeToRetry(pr *autoscaling.ProvisioningRequest, failuresCount int32) time.Duration {
-	backoffDuration := time.Duration(c.minBackoffSeconds) * time.Second
-	maxBackoffDuration := time.Duration(c.maxBackoffSeconds) * time.Second
+func (c *Controller) remainingTimeToRetry(wlInfo *workloadInfo, pr *autoscaling.ProvisioningRequest, failuresCount int32, prc *kueue.ProvisioningRequestConfig) time.Duration {
+	if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+		return wlInfo.requeueState.RequeueAt.Sub(time.Now())
```

  Indeed, thanks for catching that. Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T12:41:01Z

- **pkg/controller/admissionchecks/provisioning/controller.go:225** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T12:41:01Z
```diff
@@ -247,16 +216,18 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
+				if attempt <= ptr.Deref(retryStrategy.BackoffLimitCount, kueue.DefaultBackoffLimitCount) {
+					remainingTimeToRetry := c.remainingTimeToRetry(wlInfo, oldPr, attempt, prc)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T12:41:13Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:64** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T12:41:13Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest
+	// if nil then default configuration will be applied
+	// to switch off retry mechanism completely, set retryStrategy.backoffLimitCount to 0.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T12:41:26Z

- **pkg/controller/admissionchecks/provisioning/controller.go:466** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T12:41:26Z
```diff
@@ -487,8 +463,19 @@ func updateCheckState(checkState *kueue.AdmissionCheckState, state kueue.CheckSt
 	return true
 }
 
-func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) error {
+func updateWorkloadInfo(wl *kueue.Workload, wlInfo *workloadInfo) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T12:41:33Z

- **pkg/controller/admissionchecks/provisioning/controller.go:258** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T12:41:33Z
```diff
@@ -247,16 +216,18 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
+				if attempt <= ptr.Deref(retryStrategy.BackoffLimitCount, kueue.DefaultBackoffLimitCount) {
+					remainingTimeToRetry := c.remainingTimeToRetry(wlInfo, oldPr, attempt, prc)
+					if remainingTimeToRetry <= 0 {
 						shouldCreatePr = true
 						attempt += 1
-					} else if requeAfter == nil || remainingTime < *requeAfter {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T13:40:14Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:77** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T13:40:14Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest
+	// if nil then default configuration will be applied
+	// to switch off retry mechanism completely, set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+const (
+	DefaultBackoffLimitCount  int32 = 3
+	DefaultBackoffBaseSeconds int32 = 60   // 1 min
+	DefaultBackoffMaxSeconds  int32 = 1800 // 30 min
+)
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
```

  We discussed it offline and come to conclusion that it would be the best to get rid of some pointers in the API and default them with kubebuilder annotation. `BackOffLimitCount` should be a pointer thought, so when it's null, retries will happen endlessly

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T13:40:53Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:1182** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T13:40:52Z
```diff
@@ -1122,25 +1153,58 @@ func TestReconcile(t *testing.T) {
 					Reason:    "ProvisioningRequestCreated",
 					Message:   `Created ProvisioningRequest: "wl-check1-2"`,
 				},
+			}},
+		"workload retries the admission check when is not admitted and receives the provisioning request's BookingExpired condition": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				Admitted(false).
+				Obj(),
+			checks:  []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
+			flavors: []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
+			configs: []kueue.ProvisioningRequestConfig{*provReqConfigWithRetryLimit(baseConfig, 1)},
+			requests: []autoscaling.ProvisioningRequest{
+				*requestWithConditions(baseRequest,
+					[]metav1.Condition{
+						{
+							Type:   autoscaling.Failed,
+							Status: metav1.ConditionFalse,
+						},
+						{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:   autoscaling.Accepted,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:               autoscaling.BookingExpired,
+							Status:             metav1.ConditionTrue,
+							LastTransitionTime: metav1.NewTime(time.Now().Add(time.Hour * (-1))),
```

  Removed this as it seemed we could implement those test without it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:51:43Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:71** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:51:43Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
```

  The default here was 3 (defaultMaxRetries), so I would suggest to stay with that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:52:43Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:62** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:52:43Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
```

  ```suggestion
  	// If null, then the default configuration is applied with the following parameter values:
  	// describe here the default values
  
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:53:01Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:96** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:53:01Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
```

  ```suggestion
  	// +required
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:53:10Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:103** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:53:10Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds,omitempty"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +optional
```

  ```suggestion
  	// +required
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:54:42Z

- **pkg/controller/admissionchecks/provisioning/controller.go:212** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:54:41Z
```diff
@@ -224,20 +195,22 @@ func (c *Controller) deleteUnusedProvisioningRequests(ctx context.Context, owned
 	return nil
 }
 
-func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, wlInfo *workloadInfo, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
 	log := ctrl.LoggerFrom(ctx)
-	var requeAfter *time.Duration
+	var requeueAfter *time.Duration
 	for _, checkName := range relevantChecks {
 		// get the config
 		prc, err := c.helper.ConfigForAdmissionCheck(ctx, checkName)
 		if err != nil {
 			// the check is not active
 			continue
 		}
+		retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
 		if !c.reqIsNeeded(wl, prc) {
 			continue
 		}
-		ac := workload.FindAdmissionCheck(wl.Status.AdmissionChecks, checkName)
+		fmt.Printf("check states: %+v, checkName: %+v", wlInfo.checkStates, checkName)
```

  remove debug logs, but maybe replace them with the proper log at V(5) if you find this useful

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:57:00Z

- **pkg/controller/admissionchecks/provisioning/controller.go:229** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:56:59Z
```diff
@@ -247,16 +220,22 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
+				if retryStrategy.BackoffLimitCount == nil || attempt <= *retryStrategy.BackoffLimitCount {
+					if !features.Enabled(features.KeepQuotaForProvReqRetry) {
```

  Do you need this case? IIUC if you return 0 from r`emainingTimeToRetry` then `shouldCreatePr = true
  attempt += 1` will be exectuted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:59:24Z

- **pkg/workload/admissionchecks.go:130** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:59:24Z
```diff
@@ -128,8 +127,7 @@ func SetAdmissionCheckState(checks *[]kueue.AdmissionCheckState, newCheck kueue.
 // RejectedChecks returns the list of Rejected admission checks
 func RejectedChecks(wl *kueue.Workload) []kueue.AdmissionCheckState {
 	rejectedChecks := make([]kueue.AdmissionCheckState, 0, len(wl.Status.AdmissionChecks))
-	for i := range wl.Status.AdmissionChecks {
-		ac := wl.Status.AdmissionChecks[i]
+	for _, ac := range wl.Status.AdmissionChecks {
```

  nit: I prefer to revert to avoid distractions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:59:28Z

- **pkg/workload/admissionchecks.go:140** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:59:28Z
```diff
@@ -139,8 +137,8 @@ func RejectedChecks(wl *kueue.Workload) []kueue.AdmissionCheckState {
 
 // HasAllChecksReady returns true if all the checks of the workload are ready.
 func HasAllChecksReady(wl *kueue.Workload) bool {
-	for i := range wl.Status.AdmissionChecks {
-		if wl.Status.AdmissionChecks[i].State != kueue.CheckStateReady {
+	for _, ac := range wl.Status.AdmissionChecks {
```

  nit: I prefer to revert to avoid distractions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:59:35Z

- **pkg/workload/admissionchecks.go:167** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:59:35Z
```diff
@@ -166,9 +164,8 @@ func HasAllChecks(wl *kueue.Workload, mustHaveChecks sets.Set[string]) bool {
 
 // HasRetryChecks returns true if any of the workloads checks is Retry
 func HasRetryChecks(wl *kueue.Workload) bool {
-	for i := range wl.Status.AdmissionChecks {
-		state := wl.Status.AdmissionChecks[i].State
-		if state == kueue.CheckStateRetry {
+	for _, ac := range wl.Status.AdmissionChecks {
```

  nit: I prefer to revert to avoid distractions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T13:59:40Z

- **pkg/workload/admissionchecks.go:177** by [@mimowo](https://github.com/mimowo) — 2024-10-30T13:59:39Z
```diff
@@ -177,9 +174,8 @@ func HasRetryChecks(wl *kueue.Workload) bool {
 
 // HasRejectedChecks returns true if any of the workloads checks is Rejected
 func HasRejectedChecks(wl *kueue.Workload) bool {
-	for i := range wl.Status.AdmissionChecks {
-		state := wl.Status.AdmissionChecks[i].State
-		if state == kueue.CheckStateRejected {
+	for _, ac := range wl.Status.AdmissionChecks {
```

  nit: I prefer to revert to avoid distractions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T14:01:45Z

- **pkg/controller/admissionchecks/provisioning/controller.go:200** by [@mimowo](https://github.com/mimowo) — 2024-10-30T14:01:45Z
```diff
@@ -224,20 +195,22 @@ func (c *Controller) deleteUnusedProvisioningRequests(ctx context.Context, owned
 	return nil
 }
 
-func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, wlInfo *workloadInfo, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
 	log := ctrl.LoggerFrom(ctx)
-	var requeAfter *time.Duration
+	var requeueAfter *time.Duration
```

  Don't you mind sending a small cleanup PR to just rename it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T14:04:21Z

- **pkg/controller/admissionchecks/provisioning/controller.go:305** by [@mimowo](https://github.com/mimowo) — 2024-10-30T14:04:20Z
```diff
@@ -317,12 +296,16 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 			return nil, err
 		}
 	}
-	return requeAfter, nil
+	return requeueAfter, nil
 }
 
-func (c *Controller) remainingTimeToRetry(pr *autoscaling.ProvisioningRequest, failuresCount int32) time.Duration {
-	backoffDuration := time.Duration(c.minBackoffSeconds) * time.Second
-	maxBackoffDuration := time.Duration(c.maxBackoffSeconds) * time.Second
+func (c *Controller) remainingTimeToRetry(pr *autoscaling.ProvisioningRequest, failuresCount int32, prc *kueue.ProvisioningRequestConfig) time.Duration {
+	retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+	if prc.Spec.RetryStrategy != nil {
+
```

  This looks unfinished :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T14:05:30Z

- **pkg/controller/admissionchecks/provisioning/controller.go:516** by [@mimowo](https://github.com/mimowo) — 2024-10-30T14:05:29Z
```diff
@@ -519,19 +510,27 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if checkState.State == kueue.CheckStateRejected {
```

  Maybe include as part of the prep PR in the other comment: https://github.com/kubernetes-sigs/kueue/pull/3375/files#r1822708150

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T14:05:49Z

- **pkg/controller/admissionchecks/provisioning/controller.go:544** by [@mimowo](https://github.com/mimowo) — 2024-10-30T14:05:48Z
```diff
@@ -542,18 +541,24 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 					updated = updateCheckMessage(&checkState, apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.CapacityRevoked).Message) || updated
 				}
 			case isBookingExpired(pr):
-				if !workload.IsAdmitted(wl) {
-					attempt := getAttempt(log, pr, wl.Name, check)
-					if attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after booking expired: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.BookingExpired).Message)
+				if workload.IsAdmitted(wl) {
```

  Maybe include as part of the prep PR in the other comment: https://github.com/kubernetes-sigs/kueue/pull/3375/files#r1822708150

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-30T14:08:22Z

- **pkg/controller/admissionchecks/provisioning/controller.go:555** by [@mimowo](https://github.com/mimowo) — 2024-10-30T14:08:21Z
```diff
@@ -542,18 +541,24 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 					updated = updateCheckMessage(&checkState, apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.CapacityRevoked).Message) || updated
 				}
 			case isBookingExpired(pr):
-				if !workload.IsAdmitted(wl) {
-					attempt := getAttempt(log, pr, wl.Name, check)
-					if attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after booking expired: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.BookingExpired).Message)
+				if workload.IsAdmitted(wl) {
+					break
+				}
+				if attempt := getAttempt(log, pr, wl.Name, check); retryStrategy.BackoffLimitCount == nil || attempt <= *retryStrategy.BackoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after booking expired: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.BookingExpired).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
 					} else {
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.BookingExpired).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
```

  Note to myself and potentially other reviewers: the clue of the fix to trigger eviction :)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:29:18Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:96** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:29:18Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
```

  Should it be required if we default this value with kubebuilder annotation?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:29:22Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:103** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:29:22Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds,omitempty"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +optional
```

  Should it be required if we default this value with kubebuilder annotation?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:38:49Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:62** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:38:49Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:39:48Z

- **pkg/controller/admissionchecks/provisioning/controller.go:212** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:39:48Z
```diff
@@ -224,20 +195,22 @@ func (c *Controller) deleteUnusedProvisioningRequests(ctx context.Context, owned
 	return nil
 }
 
-func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, wlInfo *workloadInfo, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
 	log := ctrl.LoggerFrom(ctx)
-	var requeAfter *time.Duration
+	var requeueAfter *time.Duration
 	for _, checkName := range relevantChecks {
 		// get the config
 		prc, err := c.helper.ConfigForAdmissionCheck(ctx, checkName)
 		if err != nil {
 			// the check is not active
 			continue
 		}
+		retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
 		if !c.reqIsNeeded(wl, prc) {
 			continue
 		}
-		ac := workload.FindAdmissionCheck(wl.Status.AdmissionChecks, checkName)
+		fmt.Printf("check states: %+v, checkName: %+v", wlInfo.checkStates, checkName)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:40:02Z

- **pkg/controller/admissionchecks/provisioning/controller.go:229** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:40:02Z
```diff
@@ -247,16 +220,22 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
+				if retryStrategy.BackoffLimitCount == nil || attempt <= *retryStrategy.BackoffLimitCount {
+					if !features.Enabled(features.KeepQuotaForProvReqRetry) {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:40:14Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:71** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:40:13Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:40:28Z

- **pkg/workload/admissionchecks.go:130** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:40:28Z
```diff
@@ -128,8 +127,7 @@ func SetAdmissionCheckState(checks *[]kueue.AdmissionCheckState, newCheck kueue.
 // RejectedChecks returns the list of Rejected admission checks
 func RejectedChecks(wl *kueue.Workload) []kueue.AdmissionCheckState {
 	rejectedChecks := make([]kueue.AdmissionCheckState, 0, len(wl.Status.AdmissionChecks))
-	for i := range wl.Status.AdmissionChecks {
-		ac := wl.Status.AdmissionChecks[i]
+	for _, ac := range wl.Status.AdmissionChecks {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:40:32Z

- **pkg/workload/admissionchecks.go:140** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:40:32Z
```diff
@@ -139,8 +137,8 @@ func RejectedChecks(wl *kueue.Workload) []kueue.AdmissionCheckState {
 
 // HasAllChecksReady returns true if all the checks of the workload are ready.
 func HasAllChecksReady(wl *kueue.Workload) bool {
-	for i := range wl.Status.AdmissionChecks {
-		if wl.Status.AdmissionChecks[i].State != kueue.CheckStateReady {
+	for _, ac := range wl.Status.AdmissionChecks {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:40:40Z

- **pkg/workload/admissionchecks.go:177** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:40:40Z
```diff
@@ -177,9 +174,8 @@ func HasRetryChecks(wl *kueue.Workload) bool {
 
 // HasRejectedChecks returns true if any of the workloads checks is Rejected
 func HasRejectedChecks(wl *kueue.Workload) bool {
-	for i := range wl.Status.AdmissionChecks {
-		state := wl.Status.AdmissionChecks[i].State
-		if state == kueue.CheckStateRejected {
+	for _, ac := range wl.Status.AdmissionChecks {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:40:45Z

- **pkg/workload/admissionchecks.go:167** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:40:45Z
```diff
@@ -166,9 +164,8 @@ func HasAllChecks(wl *kueue.Workload, mustHaveChecks sets.Set[string]) bool {
 
 // HasRetryChecks returns true if any of the workloads checks is Retry
 func HasRetryChecks(wl *kueue.Workload) bool {
-	for i := range wl.Status.AdmissionChecks {
-		state := wl.Status.AdmissionChecks[i].State
-		if state == kueue.CheckStateRetry {
+	for _, ac := range wl.Status.AdmissionChecks {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:41:03Z

- **pkg/controller/admissionchecks/provisioning/controller.go:200** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:41:03Z
```diff
@@ -224,20 +195,22 @@ func (c *Controller) deleteUnusedProvisioningRequests(ctx context.Context, owned
 	return nil
 }
 
-func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, wlInfo *workloadInfo, relevantChecks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) (*time.Duration, error) {
 	log := ctrl.LoggerFrom(ctx)
-	var requeAfter *time.Duration
+	var requeueAfter *time.Duration
```

  I'll do a follow up PR and for now I revered the changes

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:41:11Z

- **pkg/controller/admissionchecks/provisioning/controller.go:305** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:41:11Z
```diff
@@ -317,12 +296,16 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 			return nil, err
 		}
 	}
-	return requeAfter, nil
+	return requeueAfter, nil
 }
 
-func (c *Controller) remainingTimeToRetry(pr *autoscaling.ProvisioningRequest, failuresCount int32) time.Duration {
-	backoffDuration := time.Duration(c.minBackoffSeconds) * time.Second
-	maxBackoffDuration := time.Duration(c.maxBackoffSeconds) * time.Second
+func (c *Controller) remainingTimeToRetry(pr *autoscaling.ProvisioningRequest, failuresCount int32, prc *kueue.ProvisioningRequestConfig) time.Duration {
+	retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+	if prc.Spec.RetryStrategy != nil {
+
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-30T14:41:23Z

- **pkg/controller/admissionchecks/provisioning/controller.go:516** by [@PBundyra](https://github.com/PBundyra) — 2024-10-30T14:41:22Z
```diff
@@ -519,19 +510,27 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if checkState.State == kueue.CheckStateRejected {
```

  Sure, I'll do that

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:09:37Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:103** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:09:37Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds,omitempty"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +optional
```

  I this is independent thing,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:10:38Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:96** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:10:38Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
```

  I think so, and I would remove omitempty

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:11:04Z

- **config/components/rbac/role.yaml:51** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:11:03Z
```diff
@@ -48,17 +48,6 @@ rules:
   verbs:
   - get
   - patch
-- apiGroups:
```

  Why is this removed?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:11:29Z

- **config/components/rbac/role.yaml:90** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:11:29Z
```diff
@@ -87,24 +76,6 @@ rules:
   - get
   - list
   - watch
-- apiGroups:
```

  Same here it is surprising to see this deleted

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:12:16Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:103** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:12:16Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds,omitempty"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +optional
```

  We should also drop omitempty for required fields

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:14:27Z

- **pkg/controller/admissionchecks/provisioning/controller.go:111** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:14:27Z
```diff
@@ -108,28 +89,15 @@ func WithMinBackoffSeconds(minBackoffSeconds int32) Option {
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
-
```

  Ah, this is likely why you lost the perimissions from manifests, I'm not sure why but kubebuilder requires this holy empty line:)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:16:54Z

- **pkg/controller/admissionchecks/provisioning/controller.go:247** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:16:53Z
```diff
@@ -247,20 +232,27 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
-						shouldCreatePr = true
-						attempt += 1
-					} else if requeAfter == nil || remainingTime < *requeAfter {
-						requeAfter = &remainingTime
-					}
+				if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+					shouldCreatePr = true
+					attempt += 1
+				}
+				remainingTime := c.remainingTimeToRetry(oldPr, attempt, prc)
+				if remainingTime <= 0 {
+					shouldCreatePr = true
+					attempt += 1
```

  This doesn't look right, if KeepQuotaForProvReqRetry disabled, and remainingTime=0, then we will bump `attempt` by 2.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:19:55Z

- **pkg/controller/admissionchecks/provisioning/controller.go:253** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:19:55Z
```diff
@@ -247,20 +232,27 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
-						shouldCreatePr = true
-						attempt += 1
-					} else if requeAfter == nil || remainingTime < *requeAfter {
-						requeAfter = &remainingTime
-					}
+				if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+					shouldCreatePr = true
+					attempt += 1
+				}
+				remainingTime := c.remainingTimeToRetry(oldPr, attempt, prc)
+				if remainingTime <= 0 {
+					shouldCreatePr = true
+					attempt += 1
+				} else if requeAfter == nil || remainingTime < *requeAfter {
+					requeAfter = &remainingTime
 				}
 			}
 		} else {
+			if oldPr, exists := latestStalePR[checkName]; exists {
```

  Can you explain / add a comment why this is needed? I'm a bit lost TBH at the moment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:20:29Z

- **pkg/controller/admissionchecks/provisioning/controller.go:488** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:20:29Z
```diff
@@ -487,8 +480,17 @@ func updateCheckState(checkState *kueue.AdmissionCheckState, state kueue.CheckSt
 	return true
 }
 
-func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) error {
+func (wlInfo *workloadInfo) update(wl *kueue.Workload) {
+	for _, check := range wl.Status.AdmissionChecks {
+		workload.SetAdmissionCheckState(&wlInfo.checkStates, check)
+	}
+	wlInfo.requeueState = wl.Status.RequeueState
+
```

  nit, drop empty line

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:22:25Z

- **pkg/controller/admissionchecks/provisioning/controller.go:554** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:22:25Z
```diff
@@ -542,18 +551,24 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 					updated = updateCheckMessage(&checkState, apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.CapacityRevoked).Message) || updated
 				}
 			case isBookingExpired(pr):
-				if !workload.IsAdmitted(wl) {
-					attempt := getAttempt(log, pr, wl.Name, check)
-					if attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after booking expired: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.BookingExpired).Message)
+				if workload.IsAdmitted(wl) || checkState.State == kueue.CheckStateRejected || checkState.State == kueue.CheckStateRetry {
```

  Why is this entry check changed? Also, it looks suspicuious, becuase if `checkState.State == kueue.CheckStateRejected || checkState.State == kueue.CheckStateRetry` then it should imply `!workload.IsAdmitted(wl)`.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-31T14:31:42Z

- **pkg/controller/admissionchecks/provisioning/controller.go:253** by [@PBundyra](https://github.com/PBundyra) — 2024-10-31T14:31:41Z
```diff
@@ -247,20 +232,27 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
-						shouldCreatePr = true
-						attempt += 1
-					} else if requeAfter == nil || remainingTime < *requeAfter {
-						requeAfter = &remainingTime
-					}
+				if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+					shouldCreatePr = true
+					attempt += 1
+				}
+				remainingTime := c.remainingTimeToRetry(oldPr, attempt, prc)
+				if remainingTime <= 0 {
+					shouldCreatePr = true
+					attempt += 1
+				} else if requeAfter == nil || remainingTime < *requeAfter {
+					requeAfter = &remainingTime
 				}
 			}
 		} else {
+			if oldPr, exists := latestStalePR[checkName]; exists {
```

  We need to keep stale ProvReqs so we know what should be the name (that contains retry number) of the new one. At the same time we don't want to process stale ProvReq that are failed and a workload already was retried with backoff - so I created a separate map for ProvReq that shouldn't be processed, but at the same time are need to create new ones

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-31T14:38:17Z

- **pkg/controller/admissionchecks/provisioning/controller.go:554** by [@PBundyra](https://github.com/PBundyra) — 2024-10-31T14:38:17Z
```diff
@@ -542,18 +551,24 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 					updated = updateCheckMessage(&checkState, apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.CapacityRevoked).Message) || updated
 				}
 			case isBookingExpired(pr):
-				if !workload.IsAdmitted(wl) {
-					attempt := getAttempt(log, pr, wl.Name, check)
-					if attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after booking expired: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.BookingExpired).Message)
+				if workload.IsAdmitted(wl) || checkState.State == kueue.CheckStateRejected || checkState.State == kueue.CheckStateRetry {
```

  `BookingExpired` is always set 10 minutes after `Provisioned=True` but it doesn't have any effect if the Workload is Admitted. It can be completely separated from Retry/Rejected states. Hence the first check.
  
  As for following checks - if we already set the Retry/Reject than we don't want to execute following code anymore as this would result in bumping the `requeueState`.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-31T14:44:57Z

- **pkg/controller/admissionchecks/provisioning/controller.go:111** by [@PBundyra](https://github.com/PBundyra) — 2024-10-31T14:44:57Z
```diff
@@ -108,28 +89,15 @@ func WithMinBackoffSeconds(minBackoffSeconds int32) Option {
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
-
```

  Wow, thank you

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-31T14:45:21Z

- **pkg/controller/admissionchecks/provisioning/controller.go:247** by [@PBundyra](https://github.com/PBundyra) — 2024-10-31T14:45:21Z
```diff
@@ -247,20 +232,27 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
-						shouldCreatePr = true
-						attempt += 1
-					} else if requeAfter == nil || remainingTime < *requeAfter {
-						requeAfter = &remainingTime
-					}
+				if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+					shouldCreatePr = true
+					attempt += 1
+				}
+				remainingTime := c.remainingTimeToRetry(oldPr, attempt, prc)
+				if remainingTime <= 0 {
+					shouldCreatePr = true
+					attempt += 1
```

  You're right, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-31T14:45:37Z

- **pkg/controller/admissionchecks/provisioning/controller.go:488** by [@PBundyra](https://github.com/PBundyra) — 2024-10-31T14:45:37Z
```diff
@@ -487,8 +480,17 @@ func updateCheckState(checkState *kueue.AdmissionCheckState, state kueue.CheckSt
 	return true
 }
 
-func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string, activeOrLastPRForChecks map[string]*autoscaling.ProvisioningRequest) error {
+func (wlInfo *workloadInfo) update(wl *kueue.Workload) {
+	for _, check := range wl.Status.AdmissionChecks {
+		workload.SetAdmissionCheckState(&wlInfo.checkStates, check)
+	}
+	wlInfo.requeueState = wl.Status.RequeueState
+
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-31T14:45:54Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:103** by [@PBundyra](https://github.com/PBundyra) — 2024-10-31T14:45:54Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds,omitempty"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +optional
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-10-31T14:46:03Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:96** by [@PBundyra](https://github.com/PBundyra) — 2024-10-31T14:46:02Z
```diff
@@ -57,6 +57,53 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied. To switch off retry mechanism
+	//  set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  nil,
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +optional
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:53:45Z

- **pkg/workload/workload.go:500** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:53:44Z
```diff
@@ -473,6 +474,33 @@ func UnsetQuotaReservationWithCondition(wl *kueue.Workload, reason, message stri
 	return changed
 }
 
+// UpdateRequeueState calculate requeueAt time and update requeuingCount
+func UpdateRequeueState(wl *kueue.Workload, backoffBaseSeconds int32, backoffMaxSeconds int32) {
+	if wl.Status.RequeueState == nil {
+		wl.Status.RequeueState = &kueue.RequeueState{}
+	}
+	requeuingCount := ptr.Deref(wl.Status.RequeueState.Count, 0) + 1
+
+	// Every backoff duration is about "60s*2^(n-1)+Rand" where:
+	// - "n" represents the "requeuingCount",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and other
+	// workloads will have a chance to be admitted.
+	backoff := &wait.Backoff{
+		Duration: time.Duration(backoffBaseSeconds) * time.Second,
+		Factor:   2,
+		Jitter:   0.0001,
+		Steps:    int(requeuingCount),
+	}
+	var waitDuration time.Duration
+	for backoff.Steps > 0 {
+		waitDuration = min(backoff.Step(), time.Duration(backoffMaxSeconds)*time.Second)
+	}
+
+	wl.Status.RequeueState.RequeueAt = ptr.To(metav1.NewTime(time.Now().Add(waitDuration)))
```

  use clock as the original code in workload_controller

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:54:19Z

- **test/integration/controller/core/workload_controller_test.go:43** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:54:19Z
```diff
@@ -38,7 +40,7 @@ import (
 
 // +kubebuilder:docs-gen:collapse=Imports
 
-var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+var _ = ginkgo.FDescribe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  revert

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:55:30Z

- **pkg/controller/core/workload_controller.go:564** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:55:30Z
```diff
@@ -562,33 +561,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		wl.Status.RequeueState = &kueue.RequeueState{}
 	}
 	// If requeuingBackoffLimitCount equals to null, the workloads is repeatedly and endless re-queued.
-	requeuingCount := ptr.Deref(wl.Status.RequeueState.Count, 0) + 1
-	if r.waitForPodsReady.requeuingBackoffLimitCount != nil && requeuingCount > *r.waitForPodsReady.requeuingBackoffLimitCount {
+	if r.waitForPodsReady.requeuingBackoffLimitCount != nil && ptr.Deref(wl.Status.RequeueState.Count, 0)+1 > *r.waitForPodsReady.requeuingBackoffLimitCount {
```

  please revert to avoid distractions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T14:59:00Z

- **pkg/controller/admissionchecks/provisioning/controller.go:253** by [@mimowo](https://github.com/mimowo) — 2024-10-31T14:59:00Z
```diff
@@ -247,20 +232,27 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
-					if remainingTime <= 0 {
-						shouldCreatePr = true
-						attempt += 1
-					} else if requeAfter == nil || remainingTime < *requeAfter {
-						requeAfter = &remainingTime
-					}
+				if !features.Enabled(features.KeepQuotaForProvReqRetry) {
+					shouldCreatePr = true
+					attempt += 1
+				}
+				remainingTime := c.remainingTimeToRetry(oldPr, attempt, prc)
+				if remainingTime <= 0 {
+					shouldCreatePr = true
+					attempt += 1
+				} else if requeAfter == nil || remainingTime < *requeAfter {
+					requeAfter = &remainingTime
 				}
 			}
 		} else {
+			if oldPr, exists := latestStalePR[checkName]; exists {
```

  but why can't we just reuse activeOrLastPRForChecks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-10-31T16:58:46Z

- **pkg/controller/admissionchecks/provisioning/controller.go:132** by [@mimowo](https://github.com/mimowo) — 2024-10-31T16:58:46Z
```diff
@@ -160,9 +129,12 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 		return reconcile.Result{}, err
 	}
 	ownedPrs := list.Items
-	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
+	activeOrLastPRForChecks, latestStalePR := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
```

  I would like to better understand if / why we need `latestStalePR`. We synced with @PBundyra who is yet looking into this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-03T15:59:46Z

- **pkg/features/kube_features.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-03T15:27:33Z
  Also please update [Currently supported features table](https://github.com/kubernetes-sigs/kueue/blob/c60a500094386b43f234dd9c86051d869599f22f/site/content/en/docs/installation/_index.md?plain=1#L236-L258).

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:104** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-03T15:42:20Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
```

  If we have a default for this field, shouldn't it be optional?

- **test/integration/controller/core/workload_controller_test.go:323** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-03T15:55:01Z
```diff
@@ -318,6 +320,50 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 			})
 		})
 
+		ginkgo.FIt("Should evict workload, reset checks to Pending, and wait for backoff time before requeuing, when a check is set to Retry", func() {
```

  ```suggestion
  		ginkgo.It("Should evict workload, reset checks to Pending, and wait for backoff time before requeuing, when a check is set to Retry", func() {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-03T18:19:17Z

- **pkg/controller/core/workload_controller.go:564** by [@PBundyra](https://github.com/PBundyra) — 2024-11-03T18:19:16Z
```diff
@@ -562,33 +561,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		wl.Status.RequeueState = &kueue.RequeueState{}
 	}
 	// If requeuingBackoffLimitCount equals to null, the workloads is repeatedly and endless re-queued.
-	requeuingCount := ptr.Deref(wl.Status.RequeueState.Count, 0) + 1
-	if r.waitForPodsReady.requeuingBackoffLimitCount != nil && requeuingCount > *r.waitForPodsReady.requeuingBackoffLimitCount {
+	if r.waitForPodsReady.requeuingBackoffLimitCount != nil && ptr.Deref(wl.Status.RequeueState.Count, 0)+1 > *r.waitForPodsReady.requeuingBackoffLimitCount {
```

  It's not just a clean-up change but we update `requeueState.Count` in the `UpdateRequeueState`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-03T22:58:14Z

- **pkg/features/kube_features.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-11-03T22:58:13Z
  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T08:12:29Z

I did not review all places. Just leave basic quesions.

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:77** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T07:44:43Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
```

  It seems that this default value conflicts with the beflow description:
  
  > When it is null, the workloads will repeatedly and endless re-queueing.
  
  Which specifications are correct?

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T07:46:14Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
```

  ```suggestion
  	// +optional
  	// +kubebuilder:default=60
  	BackoffBaseSeconds *int32 `json:"backoffBaseSeconds,omitempty"`
  ```
  
  > Default values can be specified on a field using the +default= tag. Primitives will have their values directly assigned while structs will go through the JSON unmarshalling process. Fields that do not have an omitempty json tag will default to the zero value of their corresponding type if no default is assigned.
  
  https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#static-defaults

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T07:47:14Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +required
+	// +kubebuilder:default=1800
+	BackoffMaxSeconds int32 `json:"backoffMaxSeconds"`
```

  ```suggestion
  	// +optional
  	// +kubebuilder:default=1800
  	BackoffMaxSeconds *int32 `json:"backoffMaxSeconds,omitempty"`
  ```

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T07:47:42Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  What is the reason to remove the timestamp field?

- **pkg/controller/admissionchecks/provisioning/controller.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T08:10:38Z
  I was not sure the reason why we need to handle the requeuing mechanism in this reconciler.
  I assumed that we can just rely on the workload reconciler (/pkg/controllers/core/workload_controller.go).

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T08:38:59Z

- **pkg/controller/admissionchecks/provisioning/controller.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T08:38:59Z
  Do you mean setting `workload.requeueState`? In this case, this controller provides retryStrategy configuration that the `requeueState` is based on

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T08:43:30Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T08:43:30Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  Historically, timestamp was introduced to the WaitForPodsReady configuration before backoff configuration. It was the first way of putting some overhead on workloads that have failed. Since we already do it by doing backoff, this seemed redundant

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T08:57:10Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:77** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T08:57:10Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
```

  Both are correct, the `DefaultRetryStrategy` is applied when a user doesn't specify `retryStrategy` field in ProvisioningRequestConfig. However, if they specify the `retryStrategy` field, but not `backoffLimitCount`, then it's null and workloads will endlessly requeue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T09:41:45Z

- **pkg/controller/admissionchecks/provisioning/controller.go:554** by [@mimowo](https://github.com/mimowo) — 2024-11-04T09:41:44Z
```diff
@@ -542,18 +551,24 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 					updated = updateCheckMessage(&checkState, apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.CapacityRevoked).Message) || updated
 				}
 			case isBookingExpired(pr):
-				if !workload.IsAdmitted(wl) {
-					attempt := getAttempt(log, pr, wl.Name, check)
-					if attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after booking expired: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.BookingExpired).Message)
+				if workload.IsAdmitted(wl) || checkState.State == kueue.CheckStateRejected || checkState.State == kueue.CheckStateRetry {
```

  I synced with @PBundyra and it seems we may drop the newly added checks. Patryk is yet to test if they are needed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T10:24:18Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:105** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T10:24:18Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T10:24:24Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:104** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T10:24:24Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T10:24:35Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:112** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T10:24:35Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +required
+	// +kubebuilder:default=1800
+	BackoffMaxSeconds int32 `json:"backoffMaxSeconds"`
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T11:14:11Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:105** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T11:14:11Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
```

  There is a tiny difference between the approaches. If we set omitempty then if a users sets `backoffBaseSeconds: 0` there will be no value under `backoffBaseSeconds`. Hence I'll rather stick with the previous approach. It doesn't enforce defining these values on user, but assures us that there will always be value under this field.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T11:16:03Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:104** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T11:16:02Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
```

  https://github.com/kubernetes-sigs/kueue/pull/3375#discussion_r1827567269

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T11:16:17Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:112** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T11:16:17Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +required
+	// +kubebuilder:default=1800
+	BackoffMaxSeconds int32 `json:"backoffMaxSeconds"`
```

  https://github.com/kubernetes-sigs/kueue/pull/3375#discussion_r1827567269

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T11:38:42Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T11:38:42Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
```

  > If we set omitempty then if a users sets backoffBaseSeconds: 0 there will be no value under backoffBaseSeconds.
  
  IIUC, in that case, the 0 will be represented in the field since this field is a pointer, and it can handle 0 as a separate one from the default integer value.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T11:53:50Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T11:53:49Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  > Historically, timestamp was introduced to the WaitForPodsReady configuration before backoff configuration.
  
  IIUC, this is slightly incorrect. Actually, both (timestamp and backoff) are discussed in the same KEP (https://github.com/kubernetes-sigs/kueue/pull/1311#issuecomment-1870531528), then the backoff was implemented a month later. So, both are needed for this enhancement.
  
  > Since we already do it by doing backoff, this seemed redundant
  
  As you can see the above, the timestamp and backoff is not same objective. The backoff is a mitigation solution for the timestamp. So, I still think both are needed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T12:09:00Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:77** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T12:08:59Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
```

  I see.
  In that case, I think that we should handle the bahavior in the defaulter webhook.
  Because the backoffLimitCount default value is dynamic. Such dynamic defaulting should be handled in the defaulter webhook instead of static (marker) and controller defaulting.
  
  But, for other fields of the retryStrategy like backoffBaseSeconds, we can still handle as a static defaulting (marker).

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T12:45:54Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:105** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T12:45:54Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T12:47:36Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:112** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T12:47:36Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "b*2^(n-1)+Rand" where:
+	// - "b" represents the base set by "BackoffBaseSeconds" parameter,
+	// - "n" represents the "workloadStatus.requeueState.count",
+	// - "Rand" represents the random jitter.
+	// During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// By default, the consecutive requeue delays are around: (60s, 120s, 240s, ...).
+	//
+	// +optional
+	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BackoffBaseSeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 60.
+	// +required
+	// +kubebuilder:default=60
+	BackoffBaseSeconds int32 `json:"backoffBaseSeconds"`
+
+	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
+	//
+	// Defaults to 1800.
+	// +required
+	// +kubebuilder:default=1800
+	BackoffMaxSeconds int32 `json:"backoffMaxSeconds"`
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T13:25:25Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T13:25:25Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  Discussed offline, we'll introduce `timestamp` field as a followup. For now Kueue relies on eviction timestamp in case of eviction by an admission check

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T13:30:42Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T13:30:42Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  I have some discussions with @PBundyra offline.
  In conclusion, we decided to use the Eviction time to requeue the Workload the same as the WaitForPodsReady so that we can mitigate the risk that the following Workloads are blocked by the evicted Workloads.
  
  For sure, we can mitigate the risk to set longer backoff time to retryStrategy. But, this is needed to tune the backoff parameters, then the tuning make the usability worse.
  
  OTOH, if we want to provide the knob to set the timestamp field, we need to design / implement the mechanism to specify the queuing timestamp every Workloads. But the effort is not easy.
  
  So, we postpone introducing the timestamp field to after the next release.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T13:41:17Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@mimowo](https://github.com/mimowo) — 2024-11-04T13:41:16Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  I also discussed this with Patryk and the implementation of `timestamp: Creation` seems rather complex, and the need for it is unclear. So, I would be in favor of keeping the implementation as simple as possible and to defer the new field until we have such feedback from users - either in this KEP (before graduation), or follow up KEP.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T13:41:42Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@mimowo](https://github.com/mimowo) — 2024-11-04T13:41:42Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  WDYT @tenzen-y ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T14:36:49Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T14:36:49Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: 60,   // 1 min
+		BackoffMaxSeconds:  1800, // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
```

  Yes, that is key point for the timestamp field. 
  
  Additionally, we need to design the timestamp field to mitigate quota violations by users since we do not want to allow batch users to turn timestamp to creation so that they can prioritize arbitrary workloads.
  
  So, I would propose handling this timestamp feature as a separate comprehensive feature, not only for ProvisioningRequest. I did not say that the timestamp should be implemented.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T15:33:27Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:77** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T15:33:27Z
```diff
@@ -57,6 +58,58 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
```

  Discussed offline, current behavior is to always set default to 3 so it's not dynamic anymore

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T15:42:38Z

- **keps/1136-provisioning-request-support/README.md:207** by [@mimowo](https://github.com/mimowo) — 2024-11-04T15:42:37Z
```diff
@@ -212,19 +204,22 @@ type ProvisioningRequestRetryStrategy struct {
 	//
 	// Defaults to 3.
 	// +optional
+	// +kubebuilder:default=60
```

  ```suggestion
  	// +kubebuilder:default=3
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T15:47:12Z

- **pkg/controller/admissionchecks/provisioning/controller.go:232** by [@mimowo](https://github.com/mimowo) — 2024-11-04T15:47:08Z
```diff
@@ -247,11 +224,16 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
+				if !features.Enabled(features.KeepQuotaForProvReqRetry) {
```

  nit. make the positive check first

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-04T15:47:54Z

- **pkg/controller/admissionchecks/provisioning/controller.go:232** by [@PBundyra](https://github.com/PBundyra) — 2024-11-04T15:47:53Z
```diff
@@ -247,11 +224,16 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			if isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl)) {
+			if (isFailed(oldPr) || (isBookingExpired(oldPr) && !workload.IsAdmitted(wl))) &&
 				// if the workload is Admitted we don't want to retry on BookingExpired
+				ac != nil && ac.State == kueue.CheckStatePending {
+				// if the workload is in Retry/Rejected state we don't create another ProvReq
 				attempt = getAttempt(log, oldPr, wl.Name, checkName)
-				if attempt <= c.maxRetries {
-					remainingTime := c.remainingTimeToRetry(oldPr, attempt)
+				if !features.Enabled(features.KeepQuotaForProvReqRetry) {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T15:48:54Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:92** by [@mimowo](https://github.com/mimowo) — 2024-11-04T15:48:53Z
```diff
@@ -87,6 +89,30 @@ func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType stri
 	return r
 }
 
+func provReqConfigWithRetryLimit(prc *kueue.ProvisioningRequestConfig, limit int32) *kueue.ProvisioningRequestConfig {
```

  I think it would be better to add a builder for testing of ProvisioningRequestConfig. but I'm ok to leave it as a follow up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T20:53:58Z

Additionally, is this still needed?

https://github.com/PBundyra/kueue/blob/29cd2a6605ca380cf7cb0ff8ca3841e119d6fc7e/pkg/controller/core/workload_controller.go#L370-L371

I guess that answer is yes since this is not only for ProvReq, this is used by other AdmissionChecks.

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T18:53:31Z
```diff
@@ -57,6 +58,60 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: ptr.To(int32(60)),   // 1 min
+		BackoffMaxSeconds:  ptr.To(int32(1800)), // 30 min
+	}
+)
+
+type ProvisioningRequestRetryStrategy struct {
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
```

  ```suggestion
  ```
  
  Currently, we do not support this behavior, right?

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T18:55:45Z
```diff
@@ -57,6 +58,60 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
+	RetryStrategy *ProvisioningRequestRetryStrategy `json:"retryStrategy,omitempty"`
+}
+
+var (
+	DefaultRetryStrategy = ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To(int32(3)),
+		BackoffBaseSeconds: ptr.To(int32(60)),   // 1 min
+		BackoffMaxSeconds:  ptr.To(int32(1800)), // 30 min
+	}
+)
```

  ```suggestion
  ```
  
  Instead of this one, could you use the marker defaulter like the below comment?

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T19:00:17Z
```diff
@@ -57,6 +58,60 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
+	//
+	// To switch off retry mechanism
+	// set retryStrategy.backoffLimitCount to 0.
+	//
+	// +optional
```

  ```suggestion
  	// +optional
  	// +kubebuilder:default={backoffLimitCount:3,backoffBaseSeconds:60,backoffMaxSeconds:1800}
  ```

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:66** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T19:01:11Z
```diff
@@ -57,6 +58,60 @@ type ProvisioningRequestConfigSpec struct {
 	// +listType=set
 	// +kubebuilder:validation:MaxItems=100
 	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+
+	// retryStrategy defines strategy for retrying ProvisioningRequest.
+	// If null, then the default configuration is applied with the following parameter values:
+	// BackoffLimitCount:  3
+	// BackoffBaseSeconds: 60 - 1 min
+	// BackoffMaxSeconds:  1800 - 30 mins
```

  ```suggestion
  	// backoffLimitCount:  3
  	// backoffBaseSeconds: 60 - 1 min
  	// backoffMaxSeconds:  1800 - 30 mins
  ```
  
  Could you use the user-facing field name?

- **pkg/controller/admissionchecks/provisioning/controller.go:515** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T20:22:37Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
```

  ```suggestion
  ```
  
  As I commented above, once we use the static marker defauler, we do not need this variables. We can just directly use those fields.

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T20:44:23Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  I assumed that this and BookingExpired case requeue mechanism will be implemented in the workload-controller.
  I guess that we should follow the steps:
  
  1. ProvReqController: Update Workload AdmissionCheckState with Retry
  2. WorkloadController: Do Requeue mechanism same as the WaitForPodsReady based on the Workload AdmissionCheckState.
  
  In the current implementation, the field owner conflict obviously occured between WorkloadController and ProvisioningRequestController.
  Then the requeueState update will fail.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:98** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T20:45:33Z
```diff
@@ -102,6 +94,9 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 						"p1": "v1",
 						"p2": "v2",
 					},
+					RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
+						BackoffLimitCount: ptr.To(int32(0)),
```

  ```suggestion
  						BackoffLimitCount: ptr.To[int32](0),
  ```

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:962** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T20:46:02Z
```diff
@@ -966,6 +958,9 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 						"p1": "v1",
 						"p2": "v2",
 					},
+					RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
+						BackoffLimitCount: ptr.To(int32(0)),
```

  ```suggestion
  						BackoffLimitCount: ptr.To[int32](0),
  ```

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:1207** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T20:46:58Z
```diff
@@ -1209,6 +1202,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 						"p1": "v1",
 						"p2": "v2",
 					},
+					RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
+						BackoffLimitCount:  ptr.To(int32(1)),
+						BackoffBaseSeconds: ptr.To(int32(1)),
```

  ```suggestion
  						BackoffLimitCount:  ptr.To[int32](1),
  						BackoffBaseSeconds: ptr.To[int32](1),
  ```
  
  There are the same comments in other places.

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T20:53:51Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  Especially, the WaitForPodsReady condition and ProvReqRequeue conditions are met at the requeue cycle (This often could happen), I guess that conflict often happen.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-05T08:58:06Z

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@PBundyra](https://github.com/PBundyra) — 2024-11-05T08:58:05Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  > I guess that we should follow the steps:
  >
  > 1. ProvReqController: Update Workload AdmissionCheckState with Retry
  > 2. WorkloadController: Do Requeue mechanism same as the WaitForPodsReady based on the Workload > AdmissionCheckState.
  
  The con of this solution is that Workload Controller doesn't have information about the ProvReqConfig. The controller would need to do two additional requests: 
  1) GET on AdmissionCheck that is in `Retry` state;
  2) LIST on all ProvReqConfigs and match it with the AdmissionCheck
  
  It's seems to me like we're just moving complexity from one controller to the another without any clear benefit.
  
  However, I am aware of this problem and we discussed it with @mimowo. The third important controller in this mechanism is jobframework reconciler which is responsible for setting `Requeue` condition, and unsetting Quota. So currently evicting workload and unsetting quota is not atomic which may also cause problems. We're on designing a solution to that but it seems like this need to be well-thought and is out of the scope for this PR.
  
  Additionally, I implemented the PR with those problems in mind so it's resilient to any interleavings.
  
  > In the current implementation, the field owner conflict obviously occured between WorkloadController and ProvisioningRequestController.
  > Then the requeueState update will fail.
  
  We send PATCH with FieldOwnership and during my e2e tests it worked properly

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-05T09:03:29Z

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-05T09:03:29Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  > The con of this solution is that Workload Controller doesn't have information about the ProvReqConfig. The controller would need to do two additional requests:
  > 
  > GET on AdmissionCheck that is in Retry state;
  > LIST on all ProvReqConfigs and match it with the AdmissionCheck
  
  Which ProvReq information do you need in the workload-controller?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-05T10:21:15Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:1207** by [@PBundyra](https://github.com/PBundyra) — 2024-11-05T10:21:15Z
```diff
@@ -1209,6 +1202,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 						"p1": "v1",
 						"p2": "v2",
 					},
+					RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
+						BackoffLimitCount:  ptr.To(int32(1)),
+						BackoffBaseSeconds: ptr.To(int32(1)),
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-05T10:21:26Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:962** by [@PBundyra](https://github.com/PBundyra) — 2024-11-05T10:21:26Z
```diff
@@ -966,6 +958,9 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 						"p1": "v1",
 						"p2": "v2",
 					},
+					RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
+						BackoffLimitCount: ptr.To(int32(0)),
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-05T10:43:10Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:92** by [@PBundyra](https://github.com/PBundyra) — 2024-11-05T10:43:09Z
```diff
@@ -87,6 +89,30 @@ func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType stri
 	return r
 }
 
+func provReqConfigWithRetryLimit(prc *kueue.ProvisioningRequestConfig, limit int32) *kueue.ProvisioningRequestConfig {
```

  Thanks, I'll do it in a follow up

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-05T10:44:34Z

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@PBundyra](https://github.com/PBundyra) — 2024-11-05T10:44:34Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  `backoffBaseSeconds` and `backoffMaxSeconds` from ``ProvReqConfig.RetryStrategy`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-05T10:44:42Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:98** by [@PBundyra](https://github.com/PBundyra) — 2024-11-05T10:44:42Z
```diff
@@ -102,6 +94,9 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 						"p1": "v1",
 						"p2": "v2",
 					},
+					RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
+						BackoffLimitCount: ptr.To(int32(0)),
```

  Done, thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-05T12:58:34Z

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@mimowo](https://github.com/mimowo) — 2024-11-05T12:58:34Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  @tenzen-y I know where you are coming from, for me this line also was "eyebrow raising" and we discussed this at length with Patryk.
  Let me summarize the more important aspects:
  1. actually conflict is unlikely because WaitForPodsReady owns the process when the workload is already admitted, while ProvReq owns the workload before admission (during admission checks phase), (I don't know how to trigger the conflict)
  2. there is no issue about field ownership since both workload_controller and ProvReq controller use ForceOwnership.
  3. the fact that this line sets `requeueAt` while still `Requeued: True` is a bit bothering me, but in fact the same happens for WaitForPodsReady, just the time window is a bit shorter. I think we could actually "fix" this by setting "Requeued: False" at the moment of reserving quota. I see no downsides, we could open a follow up issue to investigate.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-05T16:35:38Z

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-05T16:35:37Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  Thank you for summarizing that.
  My root motivation was centralizing managing the Requeue mechanism in the workload-controller to avoid logic fragmentation.
  
  In the current implementation, even if both features (WaitForPodsReady and ProvisioningRequest retryStrategy) relies on the same API, the mechanism are fragmented, then those fragmentations could bring us uncontrollable regresssions.
  Alternatively, we can consider preparing the dedicated field separate from the WaitForPodsReady, but I think that is not a good idea rather than the fragmented mechanism since that indicates we will push debt to API and end users.
  
  So, I'm wondering if we can store the ProvReq Retry parameters like `backoffBaseSeconds` in our cache.
  Indeed, we already have relationship between AdmissionCheck and ProvisioningRequest in our cache. We only extend the cache, a slightly.
  
  @mimowo Any objections?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-05T16:38:11Z

- **pkg/controller/admissionchecks/provisioning/controller.go:527** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-05T16:38:11Z
```diff
@@ -519,19 +509,28 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 				"accepted", isAccepted(pr),
 				"bookingExpired", isBookingExpired(pr),
 				"capacityRevoked", isCapacityRevoked(pr))
+			retryStrategy := ptr.Deref(prc.Spec.RetryStrategy, kueue.DefaultRetryStrategy)
+			backoffBaseSeconds := *retryStrategy.BackoffBaseSeconds
+			backoffMaxSeconds := *retryStrategy.BackoffMaxSeconds
+			backoffLimitCount := *retryStrategy.BackoffLimitCount
 			switch {
 			case isFailed(pr):
-				if checkState.State != kueue.CheckStateRejected {
-					if attempt := getAttempt(log, pr, wl.Name, check); attempt <= c.maxRetries {
-						// it is going to be retried
-						message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+				if attempt := getAttempt(log, pr, wl.Name, check); attempt <= backoffLimitCount {
+					// it is going to be retried
+					message := fmt.Sprintf("Retrying after failure: %s", apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message)
+					updated = updateCheckMessage(&checkState, message) || updated
+					if features.Enabled(features.KeepQuotaForProvReqRetry) {
 						updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-						updated = updateCheckMessage(&checkState, message) || updated
-					} else {
+					} else if wl.Status.RequeueState == nil || getAttempt(log, pr, wl.Name, check) > ptr.Deref(wl.Status.RequeueState.Count, 0) {
+						// We don't want to Retry on old ProvisioningRequests
 						updated = true
-						checkState.State = kueue.CheckStateRejected
-						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
+						updateCheckState(&checkState, kueue.CheckStateRetry)
+						workload.UpdateRequeueState(wlPatch, backoffBaseSeconds, backoffMaxSeconds, c.clock)
 					}
```

  > However, I am aware of this problem and we discussed it with @mimowo. The third important controller in this mechanism is jobframework reconciler which is responsible for setting Requeue condition, and unsetting Quota.
  
  Here key problem is even if they rely on the same API, both feature are managed by different mechanism.
