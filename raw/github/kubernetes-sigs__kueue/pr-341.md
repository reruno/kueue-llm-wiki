# PR #341: Renamed Queue to LocalQueue

**Summary**: Renamed Queue to LocalQueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/341

**Last updated**: 2022-08-19T17:43:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `local-queue` → `main`
- **Created**: 2022-08-18T21:15:26Z
- **Updated**: 2022-08-19T17:43:51Z
- **Merged**: 2022-08-19T17:43:51Z
- **Closed**: 2022-08-19T17:43:51Z
- **Labels**: `lgtm`, `size/XL`, `approved`, `kind/api-change`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 36   **+422 / -418**

## Description

#### What type of PR is this?

/kind cleanup
/kind api-change

#### What this PR does / why we need it:

Renames the namespaced resource Queue to LocalQueue.

#### Which issue(s) this PR fixes:
Part of #330

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-19T14:44:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/341#" title="Author self-approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/341#pullrequestreview-1078939023" title="Approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g,alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T14:54:26Z

> The localQueue name referenced by other components is still `queueName` or `kueue.x-k8s.io/queue-name`, right?

Yes, that will stay as is.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T16:45:50Z

squashed

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T17:41:44Z

/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-18T21:39:59Z

- **apis/kueue/v1alpha1/localqueue_types.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:21:31Z
```diff
@@ -42,24 +42,24 @@ type QueueStatus struct {
 //+kubebuilder:printcolumn:name="ClusterQueue",JSONPath=".spec.clusterQueue",type=string,description="Backing ClusterQueue"
 //+kubebuilder:printcolumn:name="Pending Workloads",JSONPath=".status.pendingWorkloads",type=integer,description="Number of pending workloads"
```

  Worth adding a short name:
  
  ```
  //+kubebuilder:resource:shortName={queue,queues}
  ```

- **apis/kueue/webhooks/localqueue_webhook.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:28:37Z
```diff
@@ -30,48 +30,48 @@ import (
 )
 
 // log is for logging in this package.
-var queueLog = ctrl.Log.WithName("queue-webhook")
+var localQueueLog = ctrl.Log.WithName("localqueue-webhook")
 
-type QueueWebhook struct{}
+type LocalQueueWebhook struct{}
 
-func setupWebhookForQueue(mgr ctrl.Manager) error {
+func setupWebhookForLocalQueue(mgr ctrl.Manager) error {
 	return ctrl.NewWebhookManagedBy(mgr).
-		For(&kueue.Queue{}).
-		WithValidator(&QueueWebhook{}).
+		For(&kueue.LocalQueue{}).
+		WithValidator(&LocalQueueWebhook{}).
 		Complete()
 }
 
-// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-queue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=queues,verbs=create;update,versions=v1alpha1,name=vqueue.kb.io,admissionReviewVersions=v1
+// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-localqueue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=localqueues,verbs=create;update,versions=v1alpha1,name=vlocalqueue.kb.io,admissionReviewVersions=v1
```

  :thinking: NB: `kb.io` refers to kubebuilder ¯\_(ツ)_/¯

- **config/manager/kustomization.yaml:17** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:33:41Z
```diff
@@ -14,4 +14,4 @@ kind: Kustomization
 images:
 - name: controller
   newName: gcr.io/k8s-staging-kueue/kueue
-  newTag: main
+  newTag: 0.2.0-devel-159-g59bf53b-dirty
```

  revert (maybe you got a failure when trying to deploy the image)

- **pkg/cache/cache.go:757** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:35:44Z
```diff
@@ -754,12 +754,12 @@ func SetupIndexes(indexer client.FieldIndexer) error {
 	})
 }
 
-func workloadBelongsToQueue(wl *kueue.Workload, q *kueue.Queue) bool {
+func workloadBelongsToQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
```

  ```suggestion
  func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
  ```

- **pkg/controller/core/localqueue_controller.go:67** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:36:27Z
```diff
@@ -60,12 +60,12 @@ func (r *QueueReconciler) NotifyWorkloadUpdate(w *kueue.Workload) {
 }
 
 //+kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues,verbs=get;list;watch;create;update;patch;delete
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/status,verbs=get;update;patch
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=queues/finalizers,verbs=update
+//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues,verbs=get;list;watch;create;update;patch;delete
+//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/status,verbs=get;update;patch
+//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *QueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
```

  LocalQueueReconciler

- **pkg/queue/manager.go:158** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:37:26Z
```diff
@@ -155,16 +155,16 @@ func (m *Manager) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	m.deleteCohort(cohort, cq.Name)
 }
 
-func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
+func (m *Manager) AddQueue(ctx context.Context, q *kueue.LocalQueue) error {
```

  ```suggestion
  func (m *Manager) AddLocalQueue(ctx context.Context, q *kueue.LocalQueue) error {
  ```

- **pkg/queue/manager.go:189** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:37:37Z
```diff
@@ -180,53 +180,53 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 		qImpl.AddOrUpdate(workload.NewInfo(&w))
 	}
 	cq := m.clusterQueues[qImpl.ClusterQueue]
-	if cq != nil && cq.AddFromQueue(qImpl) {
+	if cq != nil && cq.AddFromLocalQueue(qImpl) {
 		m.Broadcast()
 	}
 	return nil
 }
 
-func (m *Manager) UpdateQueue(q *kueue.Queue) error {
+func (m *Manager) UpdateQueue(q *kueue.LocalQueue) error {
```

  ```suggestion
  func (m *Manager) UpdateLocalQueue(q *kueue.LocalQueue) error {
  ```

- **pkg/queue/manager.go:210** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-18T21:37:45Z
```diff
@@ -180,53 +180,53 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 		qImpl.AddOrUpdate(workload.NewInfo(&w))
 	}
 	cq := m.clusterQueues[qImpl.ClusterQueue]
-	if cq != nil && cq.AddFromQueue(qImpl) {
+	if cq != nil && cq.AddFromLocalQueue(qImpl) {
 		m.Broadcast()
 	}
 	return nil
 }
 
-func (m *Manager) UpdateQueue(q *kueue.Queue) error {
+func (m *Manager) UpdateQueue(q *kueue.LocalQueue) error {
 	m.Lock()
 	defer m.Unlock()
-	qImpl, ok := m.queues[Key(q)]
+	qImpl, ok := m.localQueues[Key(q)]
 	if !ok {
 		return errQueueDoesNotExist
 	}
 	if qImpl.ClusterQueue != string(q.Spec.ClusterQueue) {
 		oldCQ := m.clusterQueues[qImpl.ClusterQueue]
 		if oldCQ != nil {
-			oldCQ.DeleteFromQueue(qImpl)
+			oldCQ.DeleteFromLocalQueue(qImpl)
 		}
 		newCQ := m.clusterQueues[string(q.Spec.ClusterQueue)]
-		if newCQ != nil && newCQ.AddFromQueue(qImpl) {
+		if newCQ != nil && newCQ.AddFromLocalQueue(qImpl) {
 			m.Broadcast()
 		}
 	}
 	qImpl.update(q)
 	return nil
 }
 
-func (m *Manager) DeleteQueue(q *kueue.Queue) {
+func (m *Manager) DeleteQueue(q *kueue.LocalQueue) {
```

  ```suggestion
  func (m *Manager) DeleteLocalQueue(q *kueue.LocalQueue) {
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-19T07:46:21Z

The localQueue name referenced by other components is still `queueName` or `kueue.x-k8s.io/queue-name`, right?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-19T14:25:15Z

- **apis/kueue/v1alpha1/localqueue_types.go:43** by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T14:25:14Z
```diff
@@ -42,24 +42,24 @@ type QueueStatus struct {
 //+kubebuilder:printcolumn:name="ClusterQueue",JSONPath=".spec.clusterQueue",type=string,description="Backing ClusterQueue"
 //+kubebuilder:printcolumn:name="Pending Workloads",JSONPath=".status.pendingWorkloads",type=integer,description="Number of pending workloads"
```

  should we use that for clusterQueue :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-19T14:28:57Z

- **apis/kueue/v1alpha1/localqueue_types.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T14:28:56Z
```diff
@@ -42,24 +42,24 @@ type QueueStatus struct {
 //+kubebuilder:printcolumn:name="ClusterQueue",JSONPath=".spec.clusterQueue",type=string,description="Backing ClusterQueue"
 //+kubebuilder:printcolumn:name="Pending Workloads",JSONPath=".status.pendingWorkloads",type=integer,description="Number of pending workloads"
```

  Uhm... I don't think so. An end user should be able to do `kubectl get queues` to get the names of the queues they can use in `Workload.spec.queueName`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-19T14:43:53Z

nit
/approve

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-19T14:51:17Z

more renames

- **config/manager/kustomization.yaml:17** by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T14:28:13Z
```diff
@@ -14,4 +14,4 @@ kind: Kustomization
 images:
 - name: controller
   newName: gcr.io/k8s-staging-kueue/kueue
-  newTag: main
+  newTag: 0.2.0-devel-159-g59bf53b-dirty
```

  changed by one of the scripts probably

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-19T14:54:04Z

- **apis/kueue/v1alpha1/localqueue_types.go:43** by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T14:54:04Z
```diff
@@ -42,24 +42,24 @@ type QueueStatus struct {
 //+kubebuilder:printcolumn:name="ClusterQueue",JSONPath=".spec.clusterQueue",type=string,description="Backing ClusterQueue"
 //+kubebuilder:printcolumn:name="Pending Workloads",JSONPath=".status.pendingWorkloads",type=integer,description="Number of pending workloads"
```

  added

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-19T14:55:26Z

- **apis/kueue/webhooks/localqueue_webhook.go:44** by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T14:55:25Z
```diff
@@ -30,48 +30,48 @@ import (
 )
 
 // log is for logging in this package.
-var queueLog = ctrl.Log.WithName("queue-webhook")
+var localQueueLog = ctrl.Log.WithName("localqueue-webhook")
 
-type QueueWebhook struct{}
+type LocalQueueWebhook struct{}
 
-func setupWebhookForQueue(mgr ctrl.Manager) error {
+func setupWebhookForLocalQueue(mgr ctrl.Manager) error {
 	return ctrl.NewWebhookManagedBy(mgr).
-		For(&kueue.Queue{}).
-		WithValidator(&QueueWebhook{}).
+		For(&kueue.LocalQueue{}).
+		WithValidator(&LocalQueueWebhook{}).
 		Complete()
 }
 
-// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-queue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=queues,verbs=create;update,versions=v1alpha1,name=vqueue.kb.io,admissionReviewVersions=v1
+// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-localqueue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=localqueues,verbs=create;update,versions=v1alpha1,name=vlocalqueue.kb.io,admissionReviewVersions=v1
```

  oh, what is this? should we remove it?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-19T14:56:43Z

- **pkg/controller/core/localqueue_controller.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T14:56:43Z
```diff
@@ -36,17 +36,17 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
-// QueueReconciler reconciles a Queue object
-type QueueReconciler struct {
+// LocalQueueReconciler reconciles a Queue object
```

  I thought I commented on this
  
  ```suggestion
  // LocalQueueReconciler reconciles a LocalQueue object
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-19T14:56:48Z

- **apis/kueue/webhooks/localqueue_webhook.go:44** by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T14:56:48Z
```diff
@@ -30,48 +30,48 @@ import (
 )
 
 // log is for logging in this package.
-var queueLog = ctrl.Log.WithName("queue-webhook")
+var localQueueLog = ctrl.Log.WithName("localqueue-webhook")
 
-type QueueWebhook struct{}
+type LocalQueueWebhook struct{}
 
-func setupWebhookForQueue(mgr ctrl.Manager) error {
+func setupWebhookForLocalQueue(mgr ctrl.Manager) error {
 	return ctrl.NewWebhookManagedBy(mgr).
-		For(&kueue.Queue{}).
-		WithValidator(&QueueWebhook{}).
+		For(&kueue.LocalQueue{}).
+		WithValidator(&LocalQueueWebhook{}).
 		Complete()
 }
 
-// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-queue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=queues,verbs=create;update,versions=v1alpha1,name=vqueue.kb.io,admissionReviewVersions=v1
+// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-localqueue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=localqueues,verbs=create;update,versions=v1alpha1,name=vlocalqueue.kb.io,admissionReviewVersions=v1
```

  there is also `name=mclusterqueue.kb.io` and `name=mresourceflavor.kb.io`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-19T14:57:55Z

- **apis/kueue/webhooks/localqueue_webhook.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T14:57:54Z
```diff
@@ -30,48 +30,48 @@ import (
 )
 
 // log is for logging in this package.
-var queueLog = ctrl.Log.WithName("queue-webhook")
+var localQueueLog = ctrl.Log.WithName("localqueue-webhook")
 
-type QueueWebhook struct{}
+type LocalQueueWebhook struct{}
 
-func setupWebhookForQueue(mgr ctrl.Manager) error {
+func setupWebhookForLocalQueue(mgr ctrl.Manager) error {
 	return ctrl.NewWebhookManagedBy(mgr).
-		For(&kueue.Queue{}).
-		WithValidator(&QueueWebhook{}).
+		For(&kueue.LocalQueue{}).
+		WithValidator(&LocalQueueWebhook{}).
 		Complete()
 }
 
-// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-queue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=queues,verbs=create;update,versions=v1alpha1,name=vqueue.kb.io,admissionReviewVersions=v1
+// +kubebuilder:webhook:path=/validate-kueue-x-k8s-io-v1alpha1-localqueue,mutating=false,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=localqueues,verbs=create;update,versions=v1alpha1,name=vlocalqueue.kb.io,admissionReviewVersions=v1
```

  Let's just keep it, as the code generator would use these patterns if we add more webhooks in the future.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-19T15:06:02Z

- **pkg/controller/core/localqueue_controller.go:39** by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T15:06:02Z
```diff
@@ -36,17 +36,17 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
-// QueueReconciler reconciles a Queue object
-type QueueReconciler struct {
+// LocalQueueReconciler reconciles a Queue object
```

  and lots more in this file related to logs

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-19T15:37:04Z

- **config/manager/kustomization.yaml:17** by [@ahg-g](https://github.com/ahg-g) — 2022-08-19T15:37:04Z
```diff
@@ -14,4 +14,4 @@ kind: Kustomization
 images:
 - name: controller
   newName: gcr.io/k8s-staging-kueue/kueue
-  newTag: main
+  newTag: 0.2.0-devel-160-gb92b9c2-dirty
```

  this keeps changing when running `make artifacts`
