# PR #3413: Exclude non-ready nodes from scheduling.

**Summary**: Exclude non-ready nodes from scheduling.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3413

**Last updated**: 2024-11-04T15:33:30Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/tas-exclude-non-ready` → `main`
- **Created**: 2024-11-01T12:10:37Z
- **Updated**: 2024-11-04T15:33:30Z
- **Merged**: 2024-11-04T14:45:29Z
- **Closed**: 2024-11-04T14:45:29Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 17   **+342 / -48**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Non-ready nodes are excluded by kube-scheduler so including them in TAS can be considered a bug - they will not be able to use in practice, and the pods will not schedule.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3401 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-11-01T12:10:46Z

/ok-to-test

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-01T12:10:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | dc4e2ef80122dda31b1221a653a5aa09f3981de0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6728d5092afe500008710587 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-11-01T12:14:21Z

/assign @mimowo

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-11-01T12:21:08Z

I am on the integration tests failure

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-11-01T12:29:30Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-11-01T12:53:38Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-11-04T13:23:59Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-04T14:09:20Z

/lgtm

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-04T14:09:25Z

LGTM label has been added.  <details>Git tree hash: 87f227e692d7b95f36db54f6c27aea0f5bea86c1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-04T14:43:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3413#pullrequestreview-2413262148" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3413#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (changes_requested) — 2024-11-03T07:33:53Z

- **pkg/cache/tas_flavor.go:114** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-03T06:52:46Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
```

  ```suggestion
  			if cond.Type == corev1.NodeReady {
  				// Only healthy and ready to accept pods nodes are considered for scheduling calculation
  				ready = cond.Status == corev1.ConditionTrue
  				break
  			}
  ```
  It won't work well if we have more than one condition.

- **pkg/cache/tas_flavor.go:114** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-03T07:07:52Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
```

  Also I think it would be great to move it to separate function. Like:
  
  ```
  func IsNodeStatusConditionTrue(conditions []corev1.NodeCondition, conditionType corev1.NodeConditionType, conditionStatus corev1.ConditionStatus) bool {
  	for _, cond := range conditions {
  		if cond.Type == conditionType {
  			return cond.Status == conditionStatus
  		}
  	}
  	return false
  }
  ```

- **pkg/cache/tas_flavor.go:124** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-03T07:12:23Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
+		}
+		if ready {
+			levelValues := utiltas.LevelValues(c.Levels, node.Labels)
+			capacity := resources.NewRequests(node.Status.Allocatable)
+			domainID := utiltas.DomainID(levelValues)
+			snapshot.levelValuesPerDomain[domainID] = levelValues
+			snapshot.addCapacity(domainID, capacity)
+			nodeToDomain[node.Name] = domainID
+		} else {
+			log.V(3).Info("Node was excluded from TAS Flavor snapshot", "nodeName", node.Name, "nodeStatusConditions", node.Status.Conditions)
```

  ```suggestion
  			log.V(3).Info("Node was excluded from TAS Flavor snapshot due to not ready", "nodeName", node.Name)
  ```
  
  The reason is still unclear.

- **pkg/cache/tas_flavor.go:122** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-03T07:27:11Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
+		}
+		if ready {
+			levelValues := utiltas.LevelValues(c.Levels, node.Labels)
+			capacity := resources.NewRequests(node.Status.Allocatable)
+			domainID := utiltas.DomainID(levelValues)
+			snapshot.levelValuesPerDomain[domainID] = levelValues
+			snapshot.addCapacity(domainID, capacity)
+			nodeToDomain[node.Name] = domainID
```

  It would be great to invert this condition with `continue` to don't touch current implementation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T07:58:56Z

- **pkg/cache/tas_flavor.go:124** by [@mimowo](https://github.com/mimowo) — 2024-11-04T07:58:56Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
+		}
+		if ready {
+			levelValues := utiltas.LevelValues(c.Levels, node.Labels)
+			capacity := resources.NewRequests(node.Status.Allocatable)
+			domainID := utiltas.DomainID(levelValues)
+			snapshot.levelValuesPerDomain[domainID] = levelValues
+			snapshot.addCapacity(domainID, capacity)
+			nodeToDomain[node.Name] = domainID
+		} else {
+			log.V(3).Info("Node was excluded from TAS Flavor snapshot", "nodeName", node.Name, "nodeStatusConditions", node.Status.Conditions)
```

  OTOH, maybe the checks will change in the future and we forget to rephrase, so I would just add `"ready", ready` to the params of the message. WDYT @mbobrovskyi ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T07:59:29Z

- **pkg/cache/tas_flavor.go:122** by [@mimowo](https://github.com/mimowo) — 2024-11-04T07:59:29Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
+		}
+		if ready {
+			levelValues := utiltas.LevelValues(c.Levels, node.Labels)
+			capacity := resources.NewRequests(node.Status.Allocatable)
+			domainID := utiltas.DomainID(levelValues)
+			snapshot.levelValuesPerDomain[domainID] = levelValues
+			snapshot.addCapacity(domainID, capacity)
+			nodeToDomain[node.Name] = domainID
```

  +1

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T08:00:34Z

- **pkg/cache/tas_flavor.go:114** by [@mimowo](https://github.com/mimowo) — 2024-11-04T08:00:34Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
```

  +1, then 
  ```golang
  if !IsNodeStatusConditionTrue() {
    // Only healthy and ready to accept pods nodes are considered for scheduling calculation
    continue
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T08:00:56Z

lgtm, just nits

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-04T08:02:43Z

- **pkg/cache/tas_flavor.go:124** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-04T08:02:43Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
+			// Only healthy and ready to accept pods nodes are considered for scheduling calculation
+			ready = (cond.Type == corev1.NodeReady && cond.Status == corev1.ConditionTrue)
+		}
+		if ready {
+			levelValues := utiltas.LevelValues(c.Levels, node.Labels)
+			capacity := resources.NewRequests(node.Status.Allocatable)
+			domainID := utiltas.DomainID(levelValues)
+			snapshot.levelValuesPerDomain[domainID] = levelValues
+			snapshot.addCapacity(domainID, capacity)
+			nodeToDomain[node.Name] = domainID
+		} else {
+			log.V(3).Info("Node was excluded from TAS Flavor snapshot", "nodeName", node.Name, "nodeStatusConditions", node.Status.Conditions)
```

  sgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T08:05:11Z

- **pkg/cache/tas_flavor.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T08:05:08Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  Is there any reason not to use the field indexer to list nodes?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T08:10:35Z

- **pkg/cache/tas_flavor.go:112** by [@mimowo](https://github.com/mimowo) — 2024-11-04T08:10:35Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  The nodes are already cached / indexed in controller-runtime. 
  
  We could have an index for virtual field like `metadata.ready` and use it to quickly filter only ready nodes, but this seems like overkill for now. I don't think a healthy system has more than 10% of non-ready nodes, so filtering out non-ready nodes should be very fast in code. 
  
  Doing this via indexer increases the code complexity while the performance benefit seems marginal, so I would categorize it as premature optimization for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T08:18:36Z

- **pkg/cache/tas_flavor.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T08:18:36Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  > Doing this via indexer increases the code complexity
  
  If we can introduce the indexer, we can get ride of this loop and the below if-else.
  We just add the index selector to line 83. For sure, we need to have indexer function, but the indexer function is separately from TAS codes.
  
  I think that the indexer solution is more simpler than the current impl based on removing the loop and if-else here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T08:29:10Z

- **pkg/cache/tas_flavor.go:112** by [@mimowo](https://github.com/mimowo) — 2024-11-04T08:29:10Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  I see, so we would move the complexity from here to a separate indexer function. I'm ok with either approach.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-11-04T08:37:39Z

- **pkg/cache/tas_flavor.go:112** by [@mszadkow](https://github.com/mszadkow) — 2024-11-04T08:37:39Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  so the indexer on `ready nodes` and just loop over ready nodes to gather info about usage, correct?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T10:24:19Z

- **pkg/cache/tas_flavor.go:112** by [@mimowo](https://github.com/mimowo) — 2024-11-04T10:24:19Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  use indexed to add a new "virtual" field to each Node ".metadata.ready=true/false". Then here use field selector to only list nodes with ".metadata.ready=true" (using client.MatchingFields). Then, we can keep the for loop as before.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T12:49:26Z

- **pkg/cache/tas_flavor.go:112** by [@mimowo](https://github.com/mimowo) — 2024-11-04T12:49:26Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  @tenzen-y given the PR is almost ready (not sure why the tests fail, PTAL @mszadkow ) would you be ok to merge it this way for 0.9.0 and cleanup for the future releases?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T13:24:06Z

- **pkg/cache/tas_flavor.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T13:24:06Z
```diff
@@ -108,12 +108,21 @@ func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node,
 	snapshot := newTASFlavorSnapshot(log, c.Levels)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		levelValues := utiltas.LevelValues(c.Levels, node.Labels)
-		capacity := resources.NewRequests(node.Status.Allocatable)
-		domainID := utiltas.DomainID(levelValues)
-		snapshot.levelValuesPerDomain[domainID] = levelValues
-		snapshot.addCapacity(domainID, capacity)
-		nodeToDomain[node.Name] = domainID
+		ready := false
+		for _, cond := range node.Status.Conditions {
```

  Yeah, we can. But, I think that we have still some time until 0.9
  So, if it possible, we want to contain it in 0.9.
  
  If no available based on timeline, I can be acceptable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T14:43:31Z

/lgtm
/approve
I believe all comments are addressed
