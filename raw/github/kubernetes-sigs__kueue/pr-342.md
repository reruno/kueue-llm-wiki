# PR #342: Validate Workload admission

**Summary**: Validate Workload admission

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/342

**Last updated**: 2022-08-19T22:22:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feat/add-validation-to-admission` → `main`
- **Created**: 2022-08-19T10:57:29Z
- **Updated**: 2022-08-19T22:22:55Z
- **Merged**: 2022-08-19T19:19:52Z
- **Closed**: 2022-08-19T19:19:52Z
- **Labels**: `kind/feature`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 3   **+79 / -6**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/328

#### Special notes for your reviewer:
Ignore the validation of `Each resourceName should be listed in the pod spec, although it might be too cumbersome to validate, so we could ignore it.`

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T13:37:09Z

/retitle Validate Workload admission

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T16:07:53Z

/approve

Can you squash?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-19T16:08:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/342#issuecomment-1220847013" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/342#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-08-19T16:10:48Z

Squashed

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T19:15:15Z

Great!
/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-19T13:47:30Z

- **apis/kueue/webhooks/workload_webhook.go:180** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T13:38:31Z
```diff
@@ -161,6 +166,30 @@ func validatePodSetName(name string, fldPath *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmission(obj *kueue.Workload, path *field.Path) field.ErrorList {
+	admission := obj.Spec.Admission
+	var allErrs field.ErrorList
+	allErrs = append(allErrs, validateNameReference(string(admission.ClusterQueue), path.Child("clusterQueue"))...)
+
+	flavorPath := path.Child("podSetFlavors")
+	for i, flavor := range admission.PodSetFlavors {
+		allErrs = append(allErrs, validateNameReference(flavor.Name, flavorPath.Index(i).Child("name"))...)
+	}
+
+	names := sets.NewString()
+	for _, flavor := range obj.Spec.PodSets {
```

  ```suggestion
  	for _, ps := range obj.Spec.PodSets {
  ```

- **apis/kueue/webhooks/workload_webhook.go:184** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T13:39:21Z
```diff
@@ -161,6 +166,30 @@ func validatePodSetName(name string, fldPath *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmission(obj *kueue.Workload, path *field.Path) field.ErrorList {
+	admission := obj.Spec.Admission
+	var allErrs field.ErrorList
+	allErrs = append(allErrs, validateNameReference(string(admission.ClusterQueue), path.Child("clusterQueue"))...)
+
+	flavorPath := path.Child("podSetFlavors")
+	for i, flavor := range admission.PodSetFlavors {
+		allErrs = append(allErrs, validateNameReference(flavor.Name, flavorPath.Index(i).Child("name"))...)
+	}
+
+	names := sets.NewString()
+	for _, flavor := range obj.Spec.PodSets {
+		names.Insert(flavor.Name)
+	}
+
+	for i, flavor := range obj.Spec.Admission.PodSetFlavors {
```

  ```suggestion
  	for i, ps := range obj.Spec.Admission.PodSetFlavors {
  ```

- **pkg/util/testing/wrappers.go:240** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T13:45:01Z
```diff
@@ -222,16 +222,30 @@ func (w *WorkloadWrapper) NodeSelector(kv map[string]string) *WorkloadWrapper {
 // AdmissionWrapper wraps an Admission
 type AdmissionWrapper struct{ kueue.Admission }
 
-func MakeAdmission(cq string) *AdmissionWrapper {
-	return &AdmissionWrapper{kueue.Admission{
+func MakeAdmission(cq string, podSetNames ...string) *AdmissionWrapper {
+	wrap := &AdmissionWrapper{kueue.Admission{
 		ClusterQueue: kueue.ClusterQueueReference(cq),
-		PodSetFlavors: []kueue.PodSetFlavors{
+	}}
+
+	if len(podSetNames) == 0 {
+		wrap.PodSetFlavors = []kueue.PodSetFlavors{
 			{
-				Name:    "main",
+				Name:    kueue.DefaultPodSetName,
 				Flavors: make(map[corev1.ResourceName]string),
 			},
-		},
-	}}
+		}
+		return wrap
+	}
+
+	var flavors []kueue.PodSetFlavors
```

  ```suggestion
  	var psFlavors []kueue.PodSetFlavors
  ```

- **apis/kueue/webhooks/workload_webhook.go:176** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-19T13:47:28Z
```diff
@@ -161,6 +166,30 @@ func validatePodSetName(name string, fldPath *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmission(obj *kueue.Workload, path *field.Path) field.ErrorList {
+	admission := obj.Spec.Admission
+	var allErrs field.ErrorList
+	allErrs = append(allErrs, validateNameReference(string(admission.ClusterQueue), path.Child("clusterQueue"))...)
+
+	flavorPath := path.Child("podSetFlavors")
+	for i, flavor := range admission.PodSetFlavors {
+		allErrs = append(allErrs, validateNameReference(flavor.Name, flavorPath.Index(i).Child("name"))...)
```

  ```suggestion
  		allErrs = append(allErrs, validatePodSetName(flavor.Name, flavorPath.Index(i).Child("name"))...)
  ```
  
  Although, maybe we don't need this validation, given than the value should be present in the podSets, which are already validated.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-19T15:12:01Z

- **apis/kueue/webhooks/workload_webhook.go:176** by [@kerthcet](https://github.com/kerthcet) — 2022-08-19T15:12:01Z
```diff
@@ -161,6 +166,30 @@ func validatePodSetName(name string, fldPath *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmission(obj *kueue.Workload, path *field.Path) field.ErrorList {
+	admission := obj.Spec.Admission
+	var allErrs field.ErrorList
+	allErrs = append(allErrs, validateNameReference(string(admission.ClusterQueue), path.Child("clusterQueue"))...)
+
+	flavorPath := path.Child("podSetFlavors")
+	for i, flavor := range admission.PodSetFlavors {
+		allErrs = append(allErrs, validateNameReference(flavor.Name, flavorPath.Index(i).Child("name"))...)
```

  Yes, updated.
