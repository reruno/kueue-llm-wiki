# PR #3432: TAS: Add documentation page for concepts

**Summary**: TAS: Add documentation page for concepts

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3432

**Last updated**: 2025-01-21T10:54:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-documentation` → `main`
- **Created**: 2024-11-04T13:11:22Z
- **Updated**: 2025-01-21T10:54:14Z
- **Merged**: 2024-11-05T06:55:29Z
- **Closed**: 2024-11-05T06:55:29Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@trasc](https://github.com/trasc), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 4   **+184 / -1**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #2724 

#### Special notes for your reviewer:

I think it would also be useful to have a tasks page, but I'm unlikely to be able to write it before 0.9.0.

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-04T13:11:34Z

/assign @tenzen-y @PBundyra

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-04T13:11:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5cfa7ead0d79833056609c2c04af06f00238bb56 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6729bd9c24069f0009fe3921 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3432--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3432--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTM0MzItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.7oGREIti38uH_UVMU-7sJY8peO1UVKWB18X4cdNgykA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3432--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T15:39:28Z

/cc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-05T06:38:08Z

LGTM label has been added.  <details>Git tree hash: 379ad559adc515606ff2ffb03ea06d731145aeca</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-05T06:38:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3432#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3432#pullrequestreview-2414738431" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-05T06:41:54Z

a race condition :) I have yet pushed a bit more comprehensive overview of the capacity calculation in  the last [update](https://github.com/kubernetes-sigs/kueue/compare/6f45058918d08bc8d87f734eba4251d18eb3b748..5cfa7ead0d79833056609c2c04af06f00238bb56). PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-05T06:54:15Z

LGTM label has been added.  <details>Git tree hash: 185463a9ea946edc7ac2f4cabbe84d0e3f1b4ae3</details>

## Reviews

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-11-04T14:58:21Z

A few minor suggestions.

- **site/content/en/docs/concepts/topology_aware_scheduling.md:12** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-11-04T14:53:26Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication, and thus the network bendwidth between the running Pods
```

  ```suggestion
  communication. Therefore the network bandwidth between the running Pods
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:14** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-11-04T14:54:20Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication, and thus the network bendwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. Then, the connectivity between the Pods depends on the placement
```

  ```suggestion
  such workloads. The available bandwidth between the Pods depends on the placement
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:28** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-11-04T14:54:53Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication, and thus the network bendwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. Then, the connectivity between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information in order
```

  ```suggestion
  of APIs for Kueue administrators and users to utilize the information
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:105** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-11-04T14:57:35Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication, and thus the network bendwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. Then, the connectivity between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information in order
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-required-topology` - indicates that a PodSet requires
```

  ```suggestion
  - `kueue.x-k8s.io/podset-preferred-topology` - indicates that a PodSet requires
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-04T15:09:25Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:105** by [@mimowo](https://github.com/mimowo) — 2024-11-04T15:09:25Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication, and thus the network bendwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. Then, the connectivity between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information in order
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-required-topology` - indicates that a PodSet requires
```

  thx

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-04T16:07:24Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:157** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T15:57:02Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-preferred-topology` - indicates that a PodSet requires
+	Topology Aware Scheduling, but scheduling all pods within pods on nodes
+	within the same topology domain is a preference rather than requirement.
+	The levels are evaluated one-by-one going up from the level indicated by
+	the annotation. If the PodSet cannot fit within a given topology domain
+	then the next topology level up is considered. If the PodSet cannot fit
+	at the highest topology level, then it gets admitted as distributed
+	among multiple topology domains.
+- `kueue.x-k8s.io/podset-required-topology` - indicates indicates that a PodSet
+  requires Topology Aware Scheduling, and requires scheduling all pods on nodes
+	within the same topology domain corresponding to the topology level
+	indicated by the annotation value (e.g. within a rack or within a block).
+
+#### Example
+
+Here is an example Job a user might submit to use TAS. It assumes there exists
+a LocalQueue named `tas-user-queue` which refernces the ClusterQueue pointing
+to a TAS ResourceFlavor.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: tas-sample-preferred
+  labels:
+    kueue.x-k8s.io/queue-name: tas-user-queue
+spec:
+  parallelism: 40
+  completions: 40
+  completionMode: Indexed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-preferred-topology: "cloud.provider.com/topology-block"
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.1.0
+        args: ["300s"]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+### Limitations
+
+Currently, there are multiple limitations for the compatibility of the feature
+with other features. In particular, a ClusterQueue referencing a TAS Resource
+Flavor (with the `.spec.topologyName` field) is marked as inactive in the
+following scenarios:
+- the CQ is in cohort
```

  Could we mention the field name like this? I think that we can follow the same approach in the following.
  
  ```suggestion
  - the CQ is in cohort (`.spec.cohort`)
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:159** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T16:00:49Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-preferred-topology` - indicates that a PodSet requires
+	Topology Aware Scheduling, but scheduling all pods within pods on nodes
+	within the same topology domain is a preference rather than requirement.
+	The levels are evaluated one-by-one going up from the level indicated by
+	the annotation. If the PodSet cannot fit within a given topology domain
+	then the next topology level up is considered. If the PodSet cannot fit
+	at the highest topology level, then it gets admitted as distributed
+	among multiple topology domains.
+- `kueue.x-k8s.io/podset-required-topology` - indicates indicates that a PodSet
+  requires Topology Aware Scheduling, and requires scheduling all pods on nodes
+	within the same topology domain corresponding to the topology level
+	indicated by the annotation value (e.g. within a rack or within a block).
+
+#### Example
+
+Here is an example Job a user might submit to use TAS. It assumes there exists
+a LocalQueue named `tas-user-queue` which refernces the ClusterQueue pointing
+to a TAS ResourceFlavor.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: tas-sample-preferred
+  labels:
+    kueue.x-k8s.io/queue-name: tas-user-queue
+spec:
+  parallelism: 40
+  completions: 40
+  completionMode: Indexed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-preferred-topology: "cloud.provider.com/topology-block"
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.1.0
+        args: ["300s"]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+### Limitations
+
+Currently, there are multiple limitations for the compatibility of the feature
+with other features. In particular, a ClusterQueue referencing a TAS Resource
+Flavor (with the `.spec.topologyName` field) is marked as inactive in the
+following scenarios:
+- the CQ is in cohort
+- the CQ is using preemption
+- the CQ is using MultiKueue or ProvisioningRequest admission checks
```

  ```suggestion
  - the CQ is using [MultiKueue](multikueue.md) or [ProvisioningRequest](../admission-check/controllers/provisioning.md) admission checks
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:64** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T16:01:28Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
```

  Could we move this to example folder?
  
  https://github.com/kubernetes-sigs/kueue/tree/main/site/static/examples

- **site/content/en/docs/concepts/topology_aware_scheduling.md:124** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T16:01:46Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-preferred-topology` - indicates that a PodSet requires
+	Topology Aware Scheduling, but scheduling all pods within pods on nodes
+	within the same topology domain is a preference rather than requirement.
+	The levels are evaluated one-by-one going up from the level indicated by
+	the annotation. If the PodSet cannot fit within a given topology domain
+	then the next topology level up is considered. If the PodSet cannot fit
+	at the highest topology level, then it gets admitted as distributed
+	among multiple topology domains.
+- `kueue.x-k8s.io/podset-required-topology` - indicates indicates that a PodSet
+  requires Topology Aware Scheduling, and requires scheduling all pods on nodes
+	within the same topology domain corresponding to the topology level
+	indicated by the annotation value (e.g. within a rack or within a block).
+
+#### Example
+
+Here is an example Job a user might submit to use TAS. It assumes there exists
+a LocalQueue named `tas-user-queue` which refernces the ClusterQueue pointing
+to a TAS ResourceFlavor.
+
+```yaml
```

  ditto

- **site/content/en/docs/concepts/topology_aware_scheduling.md:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T16:04:35Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
```

  ```suggestion
  For example, this is a representation of the data center hierarchy;
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:31** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-04T16:07:22Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
```

  Could we mention that we only care about ready nodes?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-05T06:26:14Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:157** by [@mimowo](https://github.com/mimowo) — 2024-11-05T06:26:14Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-preferred-topology` - indicates that a PodSet requires
+	Topology Aware Scheduling, but scheduling all pods within pods on nodes
+	within the same topology domain is a preference rather than requirement.
+	The levels are evaluated one-by-one going up from the level indicated by
+	the annotation. If the PodSet cannot fit within a given topology domain
+	then the next topology level up is considered. If the PodSet cannot fit
+	at the highest topology level, then it gets admitted as distributed
+	among multiple topology domains.
+- `kueue.x-k8s.io/podset-required-topology` - indicates indicates that a PodSet
+  requires Topology Aware Scheduling, and requires scheduling all pods on nodes
+	within the same topology domain corresponding to the topology level
+	indicated by the annotation value (e.g. within a rack or within a block).
+
+#### Example
+
+Here is an example Job a user might submit to use TAS. It assumes there exists
+a LocalQueue named `tas-user-queue` which refernces the ClusterQueue pointing
+to a TAS ResourceFlavor.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: tas-sample-preferred
+  labels:
+    kueue.x-k8s.io/queue-name: tas-user-queue
+spec:
+  parallelism: 40
+  completions: 40
+  completionMode: Indexed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-preferred-topology: "cloud.provider.com/topology-block"
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.1.0
+        args: ["300s"]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+### Limitations
+
+Currently, there are multiple limitations for the compatibility of the feature
+with other features. In particular, a ClusterQueue referencing a TAS Resource
+Flavor (with the `.spec.topologyName` field) is marked as inactive in the
+following scenarios:
+- the CQ is in cohort
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-05T06:26:21Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:159** by [@mimowo](https://github.com/mimowo) — 2024-11-05T06:26:21Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-preferred-topology` - indicates that a PodSet requires
+	Topology Aware Scheduling, but scheduling all pods within pods on nodes
+	within the same topology domain is a preference rather than requirement.
+	The levels are evaluated one-by-one going up from the level indicated by
+	the annotation. If the PodSet cannot fit within a given topology domain
+	then the next topology level up is considered. If the PodSet cannot fit
+	at the highest topology level, then it gets admitted as distributed
+	among multiple topology domains.
+- `kueue.x-k8s.io/podset-required-topology` - indicates indicates that a PodSet
+  requires Topology Aware Scheduling, and requires scheduling all pods on nodes
+	within the same topology domain corresponding to the topology level
+	indicated by the annotation value (e.g. within a rack or within a block).
+
+#### Example
+
+Here is an example Job a user might submit to use TAS. It assumes there exists
+a LocalQueue named `tas-user-queue` which refernces the ClusterQueue pointing
+to a TAS ResourceFlavor.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: tas-sample-preferred
+  labels:
+    kueue.x-k8s.io/queue-name: tas-user-queue
+spec:
+  parallelism: 40
+  completions: 40
+  completionMode: Indexed
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-preferred-topology: "cloud.provider.com/topology-block"
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.1.0
+        args: ["300s"]
+        resources:
+          requests:
+            cpu: "1"
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+### Limitations
+
+Currently, there are multiple limitations for the compatibility of the feature
+with other features. In particular, a ClusterQueue referencing a TAS Resource
+Flavor (with the `.spec.topologyName` field) is marked as inactive in the
+following scenarios:
+- the CQ is in cohort
+- the CQ is using preemption
+- the CQ is using MultiKueue or ProvisioningRequest admission checks
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-05T06:26:29Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:64** by [@mimowo](https://github.com/mimowo) — 2024-11-05T06:26:29Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-05T06:26:35Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:124** by [@mimowo](https://github.com/mimowo) — 2024-11-05T06:26:35Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
+
+|  node  |  cloud.provider.com/topology-block | cloud.provider.com/topology-rack |
+|:------:|:----------------------------------:|:--------------------------------:|
+| node-1 |               block-1              |              rack-1              |
+| node-2 |               block-1              |              rack-2              |
+| node-3 |               block-2              |              rack-1              |
+| node-4 |               block-2              |              rack-3              |
+
+Note that, there is a pair of nodes, node-1 and node-3, with the same value of
+the "cloud.provider.com/topology-rack" label, but in different blocks.
+
+### Admin-facing APIs
+
+As an admin, in order to enable the feature you need to:
+1. ensure the `TopologyAwareScheduling` feature gate is enabled
+2. create at least one instance of the `Topology` API
+3. reference the `Topology` API from a dedicated ResourceFlavor by the
+   `.spec.topologyName` field
+
+#### Example
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: Topology
+metadata:
+  name: "default"
+spec:
+  levels:
+  - nodeLabel: "cloud.provider.com/topology-block"
+  - nodeLabel: "cloud.provider.com/topology-rack"
+  - nodeLabel: "kubernetes.io/hostname"
+---
+kind: ResourceFlavor
+apiVersion: kueue.x-k8s.io/v1beta1
+metadata:
+  name: "tas-flavor"
+spec:
+  nodeLabels:
+    cloud.provider.com/: "tas-node-group"
+  topologyName: "default"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "tas-cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: "tas-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 100
+      - name: "memory"
+        nominalQuota: 100Gi
+```
+
+### User-facing APIs
+
+Once TAS is configured and ready to be used, you can create Jobs with the
+following annotations set at the PodTemplate level:
+- `kueue.x-k8s.io/podset-preferred-topology` - indicates that a PodSet requires
+	Topology Aware Scheduling, but scheduling all pods within pods on nodes
+	within the same topology domain is a preference rather than requirement.
+	The levels are evaluated one-by-one going up from the level indicated by
+	the annotation. If the PodSet cannot fit within a given topology domain
+	then the next topology level up is considered. If the PodSet cannot fit
+	at the highest topology level, then it gets admitted as distributed
+	among multiple topology domains.
+- `kueue.x-k8s.io/podset-required-topology` - indicates indicates that a PodSet
+  requires Topology Aware Scheduling, and requires scheduling all pods on nodes
+	within the same topology domain corresponding to the topology level
+	indicated by the annotation value (e.g. within a rack or within a block).
+
+#### Example
+
+Here is an example Job a user might submit to use TAS. It assumes there exists
+a LocalQueue named `tas-user-queue` which refernces the ClusterQueue pointing
+to a TAS ResourceFlavor.
+
+```yaml
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-05T06:26:55Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:31** by [@mimowo](https://github.com/mimowo) — 2024-11-05T06:26:55Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-05T06:27:03Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:42** by [@mimowo](https://github.com/mimowo) — 2024-11-05T06:27:02Z
```diff
@@ -0,0 +1,169 @@
+---
+title: "Topology Aware Scheduling"
+date: 2024-04-11
+weight: 6
+description: >
+  Allows scheduling of Pods based on the topology of nodes in a data center.
+---
+
+{{< feature-state state="alpha" for_version="v0.9" >}}
+
+It is common that AI/ML workloads require a significant amount of pod-to-pod
+communication. Therefore the network bandwidth between the running Pods
+translates into the workload execution time, and the cost of running
+such workloads. The available bandwidth between the Pods depends on the placement
+of the Nodes, running the Pods, in the data center.
+
+We observe that the data centers have a hierarchical structure of their
+organizational units, like racks and blocks, where there are multiple nodes
+within a rack, and there are multiple racks within a block. Pods running within
+the same organizational unit have better network bandwidth than Pods on
+different units. We say that nods placed in different racks are more distant
+than nodes placed within the same rack. Similarly, nodes placed in different
+blocks are more distant than two nodes within the same block.
+
+In this feature (called Topology Aware Scheduling, or TAS for short) we
+introduce a convention to represent the
+[hierarchical node topology information](#node-topology-information), and a set
+of APIs for Kueue administrators and users to utilize the information
+to optimize the Pod placement.
+
+### Node topology information
+
+We propose a lightweight model for representing the hierarchy of nodes within a
+data center by using node labels. In this model the node labels are set up by a
+cloud provider, or set up manually by administrators of on-premise clusters.
+
+Additionally, we assume that every node used for TAS has a set of the labels
+which identifies uniquely its location in the tree structure. We do not assume
+global uniqueness of labels on each level, i.e. there could be two nodes with
+the same "rack" label, but in different "blocks".
+
+For example, this is a representation of the dataset hierarchy;
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-05T06:38:03Z

Thank you!
/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-05T06:54:10Z

Thanks.
/lgtm
