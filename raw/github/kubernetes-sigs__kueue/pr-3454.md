# PR #3454: [MultiKueue] Document batch/Job managedBy field usage

**Summary**: [MultiKueue] Document batch/Job managedBy field usage

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3454

**Last updated**: 2024-11-08T07:00:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `docs/multi-cluster-job` → `main`
- **Created**: 2024-11-05T11:51:56Z
- **Updated**: 2024-11-08T07:00:48Z
- **Merged**: 2024-11-07T15:17:30Z
- **Closed**: 2024-11-07T15:17:30Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@trasc](https://github.com/trasc)
- **Changed files**: 5   **+51 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Document how to use managedBy provided by the Job Controller.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3357 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-05T11:52:00Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-05T11:52:13Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4d95d890f6342c210a4d9b917aad5e306ed81318 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/672cd6aeb89905000887956b |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3454--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3454--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTM0NTQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.4AV12SJhQecSSsiwNhfjiAz1do66_lfPoiL8d81vD6A)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3454--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-06T06:36:24Z

LGTM label has been added.  <details>Git tree hash: 308f54978be00caf7d53c13c0b1a1df845d427fe</details>

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-11-07T10:57:27Z

@tenzen-y @mimowo ptal

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T15:02:06Z

Otherwise lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-07T15:07:07Z

LGTM label has been added.  <details>Git tree hash: 7a2b8cd087fd65f27e0b4ed8ab2aafe7b6d0cf09</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-07T15:07:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3454#" title="Author self-approved">mszadkow</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3454#pullrequestreview-2421256512" title="LGTM">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T15:07:21Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-11-07T15:07:24Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3454#issuecomment-2462473103):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-11-07T15:18:10Z

@tenzen-y: new pull request created: #3485

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3454#issuecomment-2462473103):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-11-05T13:10:24Z

- **site/content/en/docs/tasks/run/multikueue/job.md:23** by [@trasc](https://github.com/trasc) — 2024-11-05T13:03:37Z
```diff
@@ -0,0 +1,35 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+
+## ManagedBy
+
+The batch/Job can work in two different ways depending on the state of `MultiKueueBatchJobWithManagedBy` feature gate.
```

  Add a link to feature getes setup, and mention the default state for `MultiKueueBatchJobWithManagedBy`, also mention the k8s versions that support `spec.managedBy`  and the fact that additional setup is necessary to enable the support. (enable `JobManagedBy ` feature gate)

- **site/content/en/docs/tasks/run/multikueue/job.md:26** by [@trasc](https://github.com/trasc) — 2024-11-05T13:07:18Z
```diff
@@ -0,0 +1,35 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+
+## ManagedBy
+
+The batch/Job can work in two different ways depending on the state of `MultiKueueBatchJobWithManagedBy` feature gate.
+
+When `MultiKueueBatchJobWithManagedBy` is disabled, the management cluster keeps the status of the Job `Pending` during the remote job execution.
+The sync happens only when the remote workload is marked as `Finished`.
```

  ```suggestion
  When `MultiKueueBatchJobWithManagedBy` is disabled, the management cluster keeps the multikueue AdmissionCheckState 'Pending'  and therefore the Job suspended,  during the remote job execution.
  The Job status sync happens only when the remote workload is marked as `Finished`.
  ```

- **site/content/en/docs/tasks/run/multikueue/job.md:33** by [@trasc](https://github.com/trasc) — 2024-11-05T13:10:02Z
```diff
@@ -0,0 +1,35 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+
+## ManagedBy
+
+The batch/Job can work in two different ways depending on the state of `MultiKueueBatchJobWithManagedBy` feature gate.
+
+When `MultiKueueBatchJobWithManagedBy` is disabled, the management cluster keeps the status of the Job `Pending` during the remote job execution.
+The sync happens only when the remote workload is marked as `Finished`.
+
+When `MultiKueueBatchJobWithManagedBy` is enabled, Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`.
+This allows the Jobs Controller to ignore the Jobs managed by MultiKueue on the management cluster, while 
+the status of the Job is synchronized during the remote job execution.
+
+{{% alert title="Note" color="primary" %}}
+Note: The MultiKueue admission check controller will `Reject` the workload that does not have the `spec.managedBy` field set to `kueue.x-k8s.io/multikueue`, causing it to be marked as `Finished` with an error indicating the cause.
```

  ```suggestion
  Note: The MultiKueue admission check controller will `Reject` the workloads owned by a batch job  that does not have the `spec.managedBy` field set to `kueue.x-k8s.io/multikueue`, causing them to be marked as `Finished` with an error indicating the cause.
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-11-05T14:04:34Z

- **site/content/en/docs/tasks/run/multikueue/job.md:23** by [@mszadkow](https://github.com/mszadkow) — 2024-11-05T14:04:34Z
```diff
@@ -0,0 +1,35 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+
+## ManagedBy
+
+The batch/Job can work in two different ways depending on the state of `MultiKueueBatchJobWithManagedBy` feature gate.
```

  thanks

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-11-06T06:36:18Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-06T09:39:21Z

- **site/content/en/docs/tasks/run/multikueue/job.md:10** by [@mimowo](https://github.com/mimowo) — 2024-11-06T09:39:20Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
```

  I would sugger to reorganize this. As intro mention that the Kueue setup will depend on the Kubernetes version used on the cluster. In particular if the `JobManagedBy` feature-gate is enabled on the cluster.
  
  Cluster with JobManagedBy enabled
  
  * Mention that enabling the feature gate is supported only since 1.30 (add a note that is going to be enabled by default in 1.32) 
  * In this setup we recommend enabling also `MultiKueueBatchJobWithManagedBy` so that MultiKueue sends life updates reflecting the status of the mirror Job on the worker.
  
  Cluster with JobManagedBy disabled
  
  * For this setup 1.30 isn't required
  * In this case `MultiKueueBatchJobWithManagedBy` should be disabled in Kueue. MultiKueue will not perform life updates of the mirror Job from a worker cluster.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-06T13:24:38Z

- **site/content/en/docs/tasks/run/multikueue/job.md:40** by [@mimowo](https://github.com/mimowo) — 2024-11-06T13:24:38Z
```diff
@@ -0,0 +1,46 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+## Cluster with JobManagedBy enabled
+
+You can only set the `spec.managedBy` field on Jobs if you enable the JobManagedBy [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates) (disabled by default).
+The `JobManagedBy` feature gate is available in Kubernetes version at least 1.30.0 and disabled by default, it will become enabled by default since Kubernetes version 1.32.0.
+
+Check the [Delegation of managing a Job object to external controller](https://kubernetes.io/docs/concepts/workloads/controllers/job/#delegation-of-managing-a-job-object-to-external-controller) for more details.
+
+The batch/Job can work in two different ways depending on the state of `MultiKueueBatchJobWithManagedBy` feature gate, disabled by default.
+Check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) for setup details.
+
+In this setup we recommend enabling also MultiKueueBatchJobWithManagedBy 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled, Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`.
+This is the recommended option, which allows MultiKueue to send life updates reflecting the status of the mirror Job on the worker.
+
+{{% alert title="Note" color="primary" %}}
+Note: The MultiKueue admission check controller will `Reject` the workloads owned by a batch job that does not have the `spec.managedBy` field set to `kueue.x-k8s.io/multikueue`, causing them to be marked as `Finished` with an error indicating the cause.
+{{% /alert %}}
+
+When `MultiKueueBatchJobWithManagedBy` is disabled, the management cluster keeps the multikueue AdmissionCheckState `Pending` and therefore the Job suspended, during the remote job execution.
+The Job status sync happens only when the remote workload is marked as `Finished`.
```

  I think this is too loaded with information. Even if correct, it is most likely out of interest for users.
  
  I would suggest to rephrase to make it lightweight, we can always add more details if requested by users:
  ```
  The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate  in your cluster. 
  
  NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
  
  ## Cluster with JobManagedBy enabled
  
  When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
  
  When `MultiKueueBatchJobWithManagedBy` is enabled Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. This allows to disable processing of the Job by the built-in controller, and allows MultiKueue
  to send life updates reflecting the status of the mirror Job on the worker.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-06T13:26:08Z

- **site/content/en/docs/tasks/run/multikueue/job.md:46** by [@mimowo](https://github.com/mimowo) — 2024-11-06T13:26:08Z
```diff
@@ -0,0 +1,46 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+## Cluster with JobManagedBy enabled
+
+You can only set the `spec.managedBy` field on Jobs if you enable the JobManagedBy [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates) (disabled by default).
+The `JobManagedBy` feature gate is available in Kubernetes version at least 1.30.0 and disabled by default, it will become enabled by default since Kubernetes version 1.32.0.
+
+Check the [Delegation of managing a Job object to external controller](https://kubernetes.io/docs/concepts/workloads/controllers/job/#delegation-of-managing-a-job-object-to-external-controller) for more details.
+
+The batch/Job can work in two different ways depending on the state of `MultiKueueBatchJobWithManagedBy` feature gate, disabled by default.
+Check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) for setup details.
+
+In this setup we recommend enabling also MultiKueueBatchJobWithManagedBy 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled, Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`.
+This is the recommended option, which allows MultiKueue to send life updates reflecting the status of the mirror Job on the worker.
+
+{{% alert title="Note" color="primary" %}}
+Note: The MultiKueue admission check controller will `Reject` the workloads owned by a batch job that does not have the `spec.managedBy` field set to `kueue.x-k8s.io/multikueue`, causing them to be marked as `Finished` with an error indicating the cause.
+{{% /alert %}}
+
+When `MultiKueueBatchJobWithManagedBy` is disabled, the management cluster keeps the multikueue AdmissionCheckState `Pending` and therefore the Job suspended, during the remote job execution.
+The Job status sync happens only when the remote workload is marked as `Finished`.
+
+## Cluster with JobManagedBy disabled
+
+There is no requirement on Kubernetes version other than required for [Kueue installation](https://kueue.sigs.k8s.io/docs/installation/#before-you-begin).
+
+In this case `MultiKueueBatchJobWithManagedBy` should be disabled in Kueue. MultiKueue will not perform life updates of the mirror Job from a worker cluster.
```

  ```suggestion
  When `JobManagedBy` is enabled in your cluster you should make sure  `MultiKueueBatchJobWithManagedBy` 
  is disabled in Kueue. This is important so that MultiKueue does not conflict with the build-in Job controller
  on the management cluster. Note that Kueue will not perform life updates of the mirror Job from a worker 
  cluster.
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-11-06T19:11:55Z

- **site/content/en/docs/tasks/run/multikueue/job.md:10** by [@mszadkow](https://github.com/mszadkow) — 2024-11-06T19:11:55Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-07T06:47:10Z

- **site/content/en/docs/tasks/run/multikueue/job.md:24** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T06:40:22Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
```

  Could we make this and `Cluster with JobManagedBy disabled` sections sub-section of the `Before you begin`?

- **site/content/en/docs/tasks/run/multikueue/job.md:29** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T06:42:36Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. This allows to disable processing of the Job by the built-in controller, and allows MultiKueue
+to send live updates reflecting the status of the mirror Job on the worker.
```

  It seems that this is an internal specification. What is the benefit for the batch users?
  I guess that the users can observe the actual Job state via Management cluster, right?
  
  Is there anything else?

- **site/content/en/docs/tasks/run/multikueue/job.md:40** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T06:45:10Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. This allows to disable processing of the Job by the built-in controller, and allows MultiKueue
+to send live updates reflecting the status of the mirror Job on the worker.
+
+## Cluster with JobManagedBy disabled
+
+When `JobManagedBy` is disabled in your cluster you should make sure `MultiKueueBatchJobWithManagedBy` 
+is also disabled in Kueue. 
+
+This is important so that MultiKueue does not conflict with the build-in Job controller
+on the management cluster. 
+
+Note that Kueue will not perform live updates of the mirror Job from a worker 
+cluster.
```

  As I mentioned above, this seems to be an internal specification, and it is not clear to the batch users what a mirror job is.
  
  I guess that we can say "As limitations of this deploy mode, you need to access worker clusters to obtain the actual Job state".

- **site/content/en/docs/tasks/run/multikueue/job.md:18** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T06:47:07Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
```

  Could you embed the YAML file similar to https://github.com/kubernetes-sigs/kueue/blob/337e8712fbb0c582819effe780f70ad0429b7ceb/site/content/en/docs/concepts/topology_aware_scheduling.md?plain=1#L74?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T07:18:41Z

- **site/content/en/docs/tasks/run/multikueue/job.md:24** by [@mimowo](https://github.com/mimowo) — 2024-11-07T07:18:41Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
```

  +1, I would also suggest to move the example "## Run Kubernetes Job example" below

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T07:24:14Z

- **site/content/en/docs/tasks/run/multikueue/job.md:29** by [@mimowo](https://github.com/mimowo) — 2024-11-07T07:24:14Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. This allows to disable processing of the Job by the built-in controller, and allows MultiKueue
+to send live updates reflecting the status of the mirror Job on the worker.
```

  > I guess that the users can observe the actual Job state via Management cluster, right?
  
  Yes, but this is very important. Basically we want to make MultiKueue transparent to the end-users. 
  Note that there might be many worker clusters. Without the feature, the user needs to check the current status a user needs to inspect the workload object to identify which worker is running the Job.
  Then, lookup the Job on the worker cluster, this is tedious for manual job. 
  
  Additionally, without `MultiKueueBatchJobWithManagedBy` the job `spec.suspend` field is kept as suspended on the management cluster, which can be misleading (we can mention that in the Cluster with JobManagedBy disabled) section.
  
  Also it is very important for automations. For example some companies may have in-house built UIs to track the Job progress. Without the feature they would need to complicate the code of the integrations and make 
  it MultiKueue-aware. With this feature the UI code does not need any adjustments.
  
  > Is there anything else?
  
  I think this is the only difference worth the doc. Another is that without `MultiKueueBatchJobWithManagedBy` MultiKueue needs to keep the admission check as Pending to avoid unsuspending the Job on the management cluster.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-07T07:28:15Z

- **site/content/en/docs/tasks/run/multikueue/job.md:29** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T07:28:15Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. This allows to disable processing of the Job by the built-in controller, and allows MultiKueue
+to send live updates reflecting the status of the mirror Job on the worker.
```

  Here, what I wanted to mean is that it needs to clarify that once they enable the JobManagedBy, they can get the Job state from the Management cluster.
  
  In the current documentation say only internal specifications.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-07T07:29:58Z

- **site/content/en/docs/tasks/run/multikueue/job.md:29** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T07:29:58Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. This allows to disable processing of the Job by the built-in controller, and allows MultiKueue
+to send live updates reflecting the status of the mirror Job on the worker.
```

  Especially, this one:
  
  > to send live updates reflecting the status of the mirror Job on the worker.
  
  They can not recognize what is live update? what is sending? what is mirror Job?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T07:31:25Z

- **site/content/en/docs/tasks/run/multikueue/job.md:40** by [@mimowo](https://github.com/mimowo) — 2024-11-07T07:31:25Z
```diff
@@ -0,0 +1,40 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1 and Kubernetes v1.30.0.
+
+## Run Kubernetes Job example
+
+Once the setup is complete you can test it by running the [`job-sample.yaml`](/docs/tasks/run/jobs/#1-define-the-job) Job. 
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+NOTE: The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+
+## Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled Kueue defaults the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. This allows to disable processing of the Job by the built-in controller, and allows MultiKueue
+to send live updates reflecting the status of the mirror Job on the worker.
+
+## Cluster with JobManagedBy disabled
+
+When `JobManagedBy` is disabled in your cluster you should make sure `MultiKueueBatchJobWithManagedBy` 
+is also disabled in Kueue. 
+
+This is important so that MultiKueue does not conflict with the build-in Job controller
+on the management cluster. 
+
+Note that Kueue will not perform live updates of the mirror Job from a worker 
+cluster.
```

  +1,  maybe just add the note to check the selected cluster:
  "As limitations of this deployment mode, you need to obtain the actual Job state you need to  access the worker cluster. You can identify the worker cluster running the Job by checking the AC state message of the Workload object on the management cluster"

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-11-07T10:58:13Z

- **site/content/en/docs/concepts/multikueue.md:63** by [@mszadkow](https://github.com/mszadkow) — 2024-11-07T10:58:13Z
```diff
@@ -60,7 +60,7 @@ Known Limitations:
 
 #### MultiKueueBatchJobWithManagedBy enabled
 
-When you want to submit Job to a ClusterQueue with a MultiKueue admission check, you should set the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`, otherwise the admission check controller will `Reject` the workload causing it to be marked as `Finished` with an error indicating the cause.
+When you want to submit Job to a ClusterQueue with a MultiKueue admission check, you should set the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`, otherwise the admission check controller will `Reject` the workload.
```

  this was also no longer the truth, I allowed myself to change it in this PR as we basically updates the same topic

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T11:45:08Z

- **site/content/en/docs/concepts/multikueue.md:63** by [@mimowo](https://github.com/mimowo) — 2024-11-07T11:45:08Z
```diff
@@ -60,7 +60,7 @@ Known Limitations:
 
 #### MultiKueueBatchJobWithManagedBy enabled
 
-When you want to submit Job to a ClusterQueue with a MultiKueue admission check, you should set the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`, otherwise the admission check controller will `Reject` the workload causing it to be marked as `Finished` with an error indicating the cause.
+When you want to submit Job to a ClusterQueue with a MultiKueue admission check, you should set the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`, otherwise the admission check controller will `Reject` the workload.
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T12:10:19Z

- **site/content/en/docs/tasks/run/multikueue/job.md:30** by [@mimowo](https://github.com/mimowo) — 2024-11-07T12:10:19Z
```diff
@@ -0,0 +1,48 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+{{% alert title="Note" color="primary" %}}
+The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+{{% /alert %}}
+
+### Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled the Job is processed by MultiKueue, which effectively reflects the job state in the management cluster.
+
+So regardless of the number of clusters, all states of all jobs are available in the management cluster.
+
+This in turn allows you to track the progress of a job as it occurs.
```

  ```suggestion
  When `MultiKueueBatchJobWithManagedBy` is enabled the current status of a Job being executed by MultiKueue on a worker cluster is live-updated on the management cluster.
  
  This gives the users and automation tools the ability to track the progress of such a Job without lookup to the
  worker cluster, making MultiKueue transparent from that perspective.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T12:10:49Z

- **site/content/en/docs/tasks/run/multikueue/job.md:30** by [@mimowo](https://github.com/mimowo) — 2024-11-07T12:10:49Z
```diff
@@ -0,0 +1,48 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+{{% alert title="Note" color="primary" %}}
+The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+{{% /alert %}}
+
+### Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled the Job is processed by MultiKueue, which effectively reflects the job state in the management cluster.
+
+So regardless of the number of clusters, all states of all jobs are available in the management cluster.
+
+This in turn allows you to track the progress of a job as it occurs.
```

  wdyt @tenzen-y ? We write about performing the lookup in the section below.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T13:04:18Z

- **site/content/en/docs/tasks/run/multikueue/job.md:43** by [@mimowo](https://github.com/mimowo) — 2024-11-07T13:04:18Z
```diff
@@ -0,0 +1,47 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+{{% alert title="Note" color="primary" %}}
+The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+{{% /alert %}}
+
+### Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled the current status of a Job being executed by MultiKueue on a worker cluster is live-updated on the management cluster.
+
+This gives the users and automation tools the ability to track the progress of such a Job without lookup to the
+worker cluster, making MultiKueue transparent from that perspective.
+
+### Cluster with JobManagedBy disabled
+
+When `JobManagedBy` is disabled in your cluster you should make sure `MultiKueueBatchJobWithManagedBy` is also disabled in Kueue. 
+
+This is important so that MultiKueue does not conflict with the build-in Job controller on the management cluster. 
+
+As a limitation of this deployment mode, to get the actual status of the job, you need to access the worker cluster.
+
+You can identify the worker cluster running the job by checking the AC status message of the workload object in the management cluster.
+
+Also, the job is suspended from the perspective of the management cluster until it is `Finished`.
+
+## Run Kubernetes Job example
```

  ```suggestion
  ## Example
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T13:05:06Z

LGTM basically one nit
leaving tagging to @tenzen-y

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-07T13:07:53Z

- **site/content/en/docs/tasks/run/multikueue/job.md:30** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T13:07:53Z
```diff
@@ -0,0 +1,48 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+{{% alert title="Note" color="primary" %}}
+The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+{{% /alert %}}
+
+### Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled the Job is processed by MultiKueue, which effectively reflects the job state in the management cluster.
+
+So regardless of the number of clusters, all states of all jobs are available in the management cluster.
+
+This in turn allows you to track the progress of a job as it occurs.
```

  > the progress of such a Job
  
  Could we make it more clarify? The current description looks like that the MultiKueue gives us another progress tracking mechanism. But we do not provide the mechanism, they need to watch Job status.
  
  > Job status (`.status`)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-07T14:58:05Z

Still LGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-07T15:01:39Z

Sorry for bothering you by grammatical comments.

- **site/content/en/docs/tasks/run/multikueue/job.md:26** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-07T15:01:01Z
```diff
@@ -0,0 +1,47 @@
+---
+title: "Run Kubernetes Job in Multi-Cluster"
+linkTitle: "Kubernetes Job"
+weight: 2
+date: 2024-11-05
+description: >
+  Run a MultiKueue scheduled Kubernetes Job.
+---
+
+## Before you begin
+
+Check the [MultiKueue installation guide](/docs/tasks/manage/setup_multikueue) on how to properly setup MultiKueue clusters.
+
+For the ease of setup and use we recommend using at least Kueue v0.8.1.
+
+The recommended way of running MultiKueue depends on the configuration of the `JobManagedBy` feature gate in your cluster. 
+
+{{% alert title="Note" color="primary" %}}
+The `JobManagedBy` feature gate is disabled in 1.30 and 1.31 by default, and will be enabled in 1.32 by default.
+{{% /alert %}}
+
+### Cluster with JobManagedBy enabled
+
+When `JobManagedBy` is enabled in your cluster we recommend configuring Kueue to enable the `MultiKueueBatchJobWithManagedBy` feature gate. 
+
+When `MultiKueueBatchJobWithManagedBy` is enabled the current status of a Job being executed by MultiKueue on a worker cluster is live-updated on the management cluster.
```

  ```suggestion
  When `MultiKueueBatchJobWithManagedBy` is enabled, the current status of a Job being executed by MultiKueue on a worker cluster is live-updated on the management cluster.
  ```
  nits.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-07T15:07:01Z

Thank you!
/approve
/lgtm
