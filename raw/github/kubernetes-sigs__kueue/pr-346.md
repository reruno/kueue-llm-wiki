# PR #346: Add docs to install and use metrics

**Summary**: Add docs to install and use metrics

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/346

**Last updated**: 2022-08-24T17:38:04Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `prometheus-install` → `main`
- **Created**: 2022-08-22T15:12:28Z
- **Updated**: 2022-08-24T17:38:04Z
- **Merged**: 2022-08-24T17:38:04Z
- **Closed**: 2022-08-24T17:38:04Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 7   **+79 / -11**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Explain how to install metrics scraping.
Show which metrics are available.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-22T15:12:41Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/346#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-22T15:15:14Z

/assign @cortespao

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-22T15:15:18Z

@alculquicondor: GitHub didn't allow me to assign the following users: cortespao.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people), repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/346#issuecomment-1222501392):

>/assign @cortespao 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-22T15:17:35Z

/hold for reviews

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T14:35:03Z

/label tide/merge-method-squash

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-24T17:23:38Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T17:37:15Z

/hold cancel

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-22T19:30:17Z

- **docs/reference/metrics.md:11** by [@ahg-g](https://github.com/ahg-g) — 2022-08-22T19:13:05Z
```diff
@@ -0,0 +1,29 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to admit
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
```

  ```suggestion
  - `kueue_admission_attempts_total` (Counter): Total number of attempts to admit
    workloads, broken down by `result` (`success` or `inadmissible`). Note that each attempt may try 
    to admit one or more workloads.
  ```

- **docs/reference/metrics.md:19** by [@ahg-g](https://github.com/ahg-g) — 2022-08-22T19:22:09Z
```diff
@@ -0,0 +1,29 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to admit
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
+- `kueue_admission_attempt_duration_seconds` (Histogram): Latency of an
+  admission attempt, broken down by `result` (`success` or `inadmissible`).
+
+## ClusterQueue status
+
+Use the following metrics to monitor the status of your ClusterQueues:
+
+- `kueue_pending_workloads` (Gauge): Number of pending workloads, per
```

  ```suggestion
  - `kueue_pending_workloads` (Gauge): The number of pending workloads, per
  ```

- **docs/reference/metrics.md:19** by [@ahg-g](https://github.com/ahg-g) — 2022-08-22T19:24:23Z
```diff
@@ -0,0 +1,29 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to admit
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
+- `kueue_admission_attempt_duration_seconds` (Histogram): Latency of an
+  admission attempt, broken down by `result` (`success` or `inadmissible`).
+
+## ClusterQueue status
+
+Use the following metrics to monitor the status of your ClusterQueues:
+
+- `kueue_pending_workloads` (Gauge): Number of pending workloads, per
```

  since we used "The wait time"; same below

- **docs/setup/install.md:11** by [@ahg-g](https://github.com/ahg-g) — 2022-08-22T19:28:11Z
```diff
@@ -8,6 +8,11 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+The kueue components publish [metrics](/docs/reference/metrics) to monitor its
```

  ```suggestion
  Kueue publishes [metrics](/docs/reference/metrics) to monitor its
  ```

- **docs/setup/install.md:12** by [@ahg-g](https://github.com/ahg-g) — 2022-08-22T19:28:29Z
```diff
@@ -8,6 +8,11 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+The kueue components publish [metrics](/docs/reference/metrics) to monitor its
+operators. You can choose to scrape these metrics with Prometheus.
```

  ```suggestion
  operators. You can scrape these metrics with Prometheus.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-22T20:40:55Z

- **docs/reference/metrics.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-22T20:38:19Z
```diff
@@ -0,0 +1,29 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to admit
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
```

  Done

- **docs/reference/metrics.md:19** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-22T20:39:15Z
```diff
@@ -0,0 +1,29 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to admit
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
+- `kueue_admission_attempt_duration_seconds` (Histogram): Latency of an
+  admission attempt, broken down by `result` (`success` or `inadmissible`).
+
+## ClusterQueue status
+
+Use the following metrics to monitor the status of your ClusterQueues:
+
+- `kueue_pending_workloads` (Gauge): Number of pending workloads, per
```

  Done

- **docs/setup/install.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-22T20:40:18Z
```diff
@@ -8,6 +8,11 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+The kueue components publish [metrics](/docs/reference/metrics) to monitor its
```

  Done

- **docs/setup/install.md:12** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-22T20:40:22Z
```diff
@@ -8,6 +8,11 @@ Make sure the following conditions are met:
 - The SuspendJob [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) is enabled. In Kubernetes 1.22 or newer, the feature gate is enabled by default.
 - The kubectl command-line tool has communication with your cluster.
 
+The kueue components publish [metrics](/docs/reference/metrics) to monitor its
+operators. You can choose to scrape these metrics with Prometheus.
```

  Done

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-08-23T18:12:52Z

- **docs/concepts/README.md:3** by [@cortespao](https://github.com/cortespao) — 2022-08-23T15:16:05Z
```diff
@@ -1,7 +1,7 @@
 # Concepts
 
-This section helps you learn about the components, APIs and abstractions
-that Kueue uses to represent your cluster and workloads.
+This section of the documentation helps you learn about the components, APIs and
```

  ```suggestion
  The following sections help you learn about the components, APIs, and
  ```

- **docs/reference/README.m:3** by [@cortespao](https://github.com/cortespao) — 2022-08-23T15:18:12Z
```diff
@@ -0,0 +1,5 @@
+# Reference
+
+This section of the documentation contains references.
```

  ```suggestion
  This section of the documentation contains the Kueue reference information.
  ```

- **docs/reference/metrics.md:11** by [@cortespao](https://github.com/cortespao) — 2022-08-23T15:20:44Z
```diff
@@ -0,0 +1,30 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to [admit](/docs/concepts/README.md#admission)
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
```

  ```suggestion
    one or more workloads, broken down by `result` (possible outcomes are `success` or `inadmissible`).
  ```

- **docs/reference/metrics.md:14** by [@cortespao](https://github.com/cortespao) — 2022-08-23T15:20:55Z
```diff
@@ -0,0 +1,30 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to [admit](/docs/concepts/README.md#admission)
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
+  Note that each admission attempt may try to admit more than one workload.
+- `kueue_admission_attempt_duration_seconds` (Histogram): Latency of an
+  admission attempt, broken down by `result` (`success` or `inadmissible`).
```

  ```suggestion
    admission attempt, broken down by `result` (possible outcomes are `success` or `inadmissible`).
  ```

- **docs/reference/metrics.md:17** by [@cortespao](https://github.com/cortespao) — 2022-08-23T15:23:30Z
```diff
@@ -0,0 +1,30 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to [admit](/docs/concepts/README.md#admission)
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
+  Note that each admission attempt may try to admit more than one workload.
+- `kueue_admission_attempt_duration_seconds` (Histogram): Latency of an
+  admission attempt, broken down by `result` (`success` or `inadmissible`).
+
+## ClusterQueue status
+
+Use the following metrics to monitor the status of your ClusterQueues:
```

  Suggestion:
  This could become a table with the following columns:
  - Name
  - Type
  - Description
  - Values

- **docs/setup/README.md:3** by [@cortespao](https://github.com/cortespao) — 2022-08-23T15:24:10Z
```diff
@@ -1,5 +1,6 @@
 # Setup
 
-This section provides step-by-step instructions on how to deploy Kueue in your cluster.
+This section of the documentation provides step-by-step instructions on how to
```

  ```suggestion
  This section shows you how to
  ```

- **docs/setup/install.md:33** by [@cortespao](https://github.com/cortespao) — 2022-08-23T15:56:23Z
```diff
@@ -23,6 +28,17 @@ VERSION=v0.1.1
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+### Add metrics scraping for prometheus-operator
+
+_Available in kueue v0.2.0 and newer_
```

  ```suggestion
  _Available in Kueue v0.2.0 and later_
  ```

- **pkg/metrics/metrics.go:96** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:11:26Z
```diff
@@ -83,15 +85,15 @@ var (
 		prometheus.CounterOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admitted_workloads_total",
-			Help:      "Total number of admitted workloads per cluster_queue",
+			Help:      "Total number of admitted workloads per 'cluster_queue'",
 		}, []string{"cluster_queue"},
 	)
 
 	admissionWaitTime = prometheus.NewHistogramVec(
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The wait time since a workload was created until it was admitted, per cluster_queue",
+			Help:      "The wait time since a Workload was created until it was admitted, per 'cluster_queue'",
```

  ```suggestion
  			Help:      "The time (seconds) between Workload `created` to `admitted` status, per 'cluster_queue'",
  ```
  I'm assuming is seconds, but feel free to change it if needed.

- **pkg/metrics/metrics.go:114** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:12:22Z
```diff
@@ -101,15 +103,16 @@ var (
 		prometheus.GaugeOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admitted_active_workloads",
-			Help:      "Number of admitted workloads that are active (unsuspended and not finished), per cluster_queue",
+			Help:      "Number of admitted Workloads that are active (unsuspended and not finished), per 'cluster_queue'",
 		}, []string{"cluster_queue"},
 	)
 
 	ClusterQueueByStatus = prometheus.NewGaugeVec(
 		prometheus.GaugeOpts{
 			Subsystem: constants.KueueName,
 			Name:      "cluster_queue_status",
-			Help:      "Status of a cluster_queue (pending, active or terminated)",
+			Help: `Reports 'cluster_queue' with 'status' ('pending', 'active' or 'terminated').
```

  ```suggestion
  			Help: `Reports 'cluster_queue' which 'status' value is either 'pending', 'active', or 'terminated'.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T19:57:00Z

- **docs/reference/README.m:3** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T19:13:33Z
```diff
@@ -0,0 +1,5 @@
+# Reference
+
+This section of the documentation contains references.
```

  Done.

- **docs/reference/metrics.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T19:14:25Z
```diff
@@ -0,0 +1,30 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to [admit](/docs/concepts/README.md#admission)
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
```

  used `values` instead of `outcomes`

- **docs/reference/metrics.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T19:14:53Z
```diff
@@ -0,0 +1,30 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to [admit](/docs/concepts/README.md#admission)
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
+  Note that each admission attempt may try to admit more than one workload.
+- `kueue_admission_attempt_duration_seconds` (Histogram): Latency of an
+  admission attempt, broken down by `result` (`success` or `inadmissible`).
```

  Done

- **docs/setup/README.md:3** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T19:27:26Z
```diff
@@ -1,5 +1,6 @@
 # Setup
 
-This section provides step-by-step instructions on how to deploy Kueue in your cluster.
+This section of the documentation provides step-by-step instructions on how to
```

  Done

- **docs/setup/install.md:33** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T19:27:31Z
```diff
@@ -23,6 +28,17 @@ VERSION=v0.1.1
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+### Add metrics scraping for prometheus-operator
+
+_Available in kueue v0.2.0 and newer_
```

  Done

- **pkg/metrics/metrics.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T19:31:16Z
```diff
@@ -83,15 +85,15 @@ var (
 		prometheus.CounterOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admitted_workloads_total",
-			Help:      "Total number of admitted workloads per cluster_queue",
+			Help:      "Total number of admitted workloads per 'cluster_queue'",
 		}, []string{"cluster_queue"},
 	)
 
 	admissionWaitTime = prometheus.NewHistogramVec(
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The wait time since a workload was created until it was admitted, per cluster_queue",
+			Help:      "The wait time since a Workload was created until it was admitted, per 'cluster_queue'",
```

  It's part of the metric name, so I'm not sure if we need to repeat that.
  
  Also, `created` and `admitted` are not values or fields anywhere, so I don't think putting them in quotes is appropriate.

- **docs/reference/metrics.md:17** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T19:56:54Z
```diff
@@ -0,0 +1,30 @@
+# Metrics
+
+Kueue exposes [prometheus](https://prometheus.io) metrics to monitor the health
+of the system and the status of [ClusterQueues](/docs/concepts/cluster_queue.md).
+
+## Kueue health
+
+Use the following metrics to monitor the health of the kueue controllers:
+
+- `kueue_admission_attempts_total` (Counter): Total number of attempts to [admit](/docs/concepts/README.md#admission)
+  one or more workloads, broken down by `result` (`success` or `inadmissible`).
+  Note that each admission attempt may try to admit more than one workload.
+- `kueue_admission_attempt_duration_seconds` (Histogram): Latency of an
+  admission attempt, broken down by `result` (`success` or `inadmissible`).
+
+## ClusterQueue status
+
+Use the following metrics to monitor the status of your ClusterQueues:
```

  Done
