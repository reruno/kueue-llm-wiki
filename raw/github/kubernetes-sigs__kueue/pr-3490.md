# PR #3490: TAS: fix accounting for usage from TAS workloads using multiple resources

**Summary**: TAS: fix accounting for usage from TAS workloads using multiple resources

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3490

**Last updated**: 2025-01-21T10:54:18Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-fix-workload-usage` → `main`
- **Created**: 2024-11-08T10:07:57Z
- **Updated**: 2025-01-21T10:54:18Z
- **Merged**: 2024-11-12T11:44:47Z
- **Closed**: 2024-11-12T11:44:47Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+103 / -14**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Fixes #3482

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
Fix accounting for usage coming from TAS workloads using multiple resources. The usage was multiplied
by the number of resources requested by a workload, which could result in under-utilization of the cluster.
It also manifested itself in the message in the workload status which could contain negative numbers.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-08T10:08:03Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3490#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-08T10:08:16Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b5d4b47ba232e1e86460bbd935788a8472f4dce4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67333c022def8c0007896493 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-08T10:10:46Z

/cc @tenzen-y @PBundyra

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-12T11:25:41Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-12T11:25:47Z

LGTM label has been added.  <details>Git tree hash: f19f095e9b65865b8182953c243daaa81e01dfec</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-12T11:38:10Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-12T11:38:16Z

LGTM label has been added.  <details>Git tree hash: b0c2b563bf5fc27e9ab9c0e4bb08e676f76f3d6e</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-12T12:01:20Z

/cherry-pick release-0.9

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-11-12T12:01:57Z

@mimowo: new pull request created: #3513

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3490#issuecomment-2470349046):

>/cherry-pick release-0.9


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@hy00nc](https://github.com/hy00nc) (commented) — 2024-11-09T09:02:17Z

- **pkg/workload/workload.go:284** by [@hy00nc](https://github.com/hy00nc) — 2024-11-09T09:02:17Z
```diff
@@ -261,15 +262,26 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
 	for _, ps := range i.TotalRequests {
 		if ps.TopologyRequest != nil {
-			result = append(result, ps.TopologyRequest.DomainRequests...)
+			psFlavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, psFlavor := range ps.Flavors {
+				psFlavors.Insert(psFlavor)
+			}
+			for psFlavor := range psFlavors {
+				result[psFlavor] = append(result[psFlavor], ps.TopologyRequest.DomainRequests...)
+			}
```

  Hi @mimowo thanks a lot for the quick fix. I'm trying to learn about TAS from your code and I have one question from a beginner's perspective (Maybe I'm asking some stupid question 😅 ):
  
  If we append the same `DomainRequest` for all the flavors in a podset, could that mean we're multiplying the requests by the number of flavors, which can also cause discrepancy compared to the actual request? And if there are multiple flavors, how to we know which of the `DomainRequest` is taken from a specific flavor?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-12T09:48:10Z

- **pkg/workload/workload.go:284** by [@mimowo](https://github.com/mimowo) — 2024-11-12T09:48:09Z
```diff
@@ -261,15 +262,26 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
 	for _, ps := range i.TotalRequests {
 		if ps.TopologyRequest != nil {
-			result = append(result, ps.TopologyRequest.DomainRequests...)
+			psFlavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, psFlavor := range ps.Flavors {
+				psFlavors.Insert(psFlavor)
+			}
+			for psFlavor := range psFlavors {
+				result[psFlavor] = append(result[psFlavor], ps.TopologyRequest.DomainRequests...)
+			}
```

  Actually, currently we only support one flavor per TopologyRequest for PodSet. This is validated early [here]( https://github.com/kubernetes-sigs/kueue/blob/a672a315eace3eb745848cf2b9cae44156f39664/pkg/scheduler/flavorassigner/tas_flavorassigner.go#L81). This code I have added a bit more generic to accomodate multiple flavors used.
  
  OTOH, it might be misleading, so I'm going to assume only one flavor is returned per topology request also here.
  
  We have it planned to revisit the scenario of multiple flavors, under the [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU/). See "Investigate if possible to have two flavors for the same PodSet".

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-12T09:53:14Z

- **pkg/workload/workload.go:281** by [@PBundyra](https://github.com/PBundyra) — 2024-11-12T09:53:14Z
```diff
@@ -261,15 +262,26 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
 	for _, ps := range i.TotalRequests {
 		if ps.TopologyRequest != nil {
-			result = append(result, ps.TopologyRequest.DomainRequests...)
+			psFlavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, psFlavor := range ps.Flavors {
+				psFlavors.Insert(psFlavor)
+			}
```

  ```suggestion
  			psFlavors := sets.New[kueue.ResourceFlavorReference](ps.Flavors)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-12T10:55:08Z

- **pkg/workload/workload.go:281** by [@mimowo](https://github.com/mimowo) — 2024-11-12T10:55:08Z
```diff
@@ -261,15 +262,26 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
 	for _, ps := range i.TotalRequests {
 		if ps.TopologyRequest != nil {
-			result = append(result, ps.TopologyRequest.DomainRequests...)
+			psFlavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, psFlavor := range ps.Flavors {
+				psFlavors.Insert(psFlavor)
+			}
```

  Done, using `utilmaps.Values`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-12T10:59:39Z

- **pkg/workload/workload.go:284** by [@mimowo](https://github.com/mimowo) — 2024-11-12T10:59:39Z
```diff
@@ -261,15 +262,26 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
 	for _, ps := range i.TotalRequests {
 		if ps.TopologyRequest != nil {
-			result = append(result, ps.TopologyRequest.DomainRequests...)
+			psFlavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, psFlavor := range ps.Flavors {
+				psFlavors.Insert(psFlavor)
+			}
+			for psFlavor := range psFlavors {
+				result[psFlavor] = append(result[psFlavor], ps.TopologyRequest.DomainRequests...)
+			}
```

  On the second thought I cannot do it, because the TASUsage function returns usage for multiple flavors. Even though there is one flavor per PodSet we still may have multiple flavors used for workloads with multiple PodSets.
  
  Still, I think in the current implementation the usage is accounted properly, the PodSets either:
  1. use the same flavor, in that case each case the `DomainRequest` will be appended
  2. use different flavors, in that case the usage will be accounted in the corresponding TAS flavors

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-12T11:01:25Z

- **pkg/cache/clusterqueue.go:529** by [@mimowo](https://github.com/mimowo) — 2024-11-12T11:01:25Z
```diff
@@ -518,19 +518,23 @@ func (c *clusterQueue) reportActiveWorkloads() {
 func (c *clusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
 	frUsage := wi.FlavorResourceUsage()
-	tasUsage := wi.TASUsage()
 	for fr, q := range frUsage {
-		tasFlvCache := c.tasFlavorCache(fr.Flavor)
 		if m == 1 {
 			addUsage(c, fr, q)
-			if tasFlvCache != nil {
-				tasFlvCache.addUsage(tasUsage)
-			}
 		}
 		if m == -1 {
 			removeUsage(c, fr, q)
-			if tasFlvCache != nil {
-				tasFlvCache.removeUsage(tasUsage)
+		}
+	}
+	if features.Enabled(features.TopologyAwareScheduling) && wi.IsUsingTAS() {
```

  `IsUsingTAS` is just a performance optimization to avoid allocation inside `TASUsage`. I could move it to a dedicated PR, OTOH it is straighforward so I'm leaving it to reviewers.
  
  cc @PBundyra

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-12T11:19:28Z

- **pkg/workload/workload.go:275** by [@PBundyra](https://github.com/PBundyra) — 2024-11-12T11:19:28Z
```diff
@@ -261,15 +262,22 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
```

  ```suggestion
  	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, len(ps.Flavors))
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-12T11:24:34Z

- **pkg/workload/workload.go:275** by [@mimowo](https://github.com/mimowo) — 2024-11-12T11:24:34Z
```diff
@@ -261,15 +262,22 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
```

  I don't have ps at this level

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-12T11:27:35Z

- **pkg/workload/workload.go:281** by [@mimowo](https://github.com/mimowo) — 2024-11-12T11:27:35Z
```diff
@@ -261,15 +262,26 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
 	for _, ps := range i.TotalRequests {
 		if ps.TopologyRequest != nil {
-			result = append(result, ps.TopologyRequest.DomainRequests...)
+			psFlavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, psFlavor := range ps.Flavors {
+				psFlavors.Insert(psFlavor)
+			}
```

  Ah, utilmaps.Values does not work as is does not deduplicate. We need "DistinctValues" (good that the new test picked it up), but not sure it warrants a new helper function. The suggestion from your comment does not compile. 
  For now returning to how it was. 
  Alternatively this would work, but is less efficient: `psFlavors := sets.New[kueue.ResourceFlavorReference](utilmaps.Values(ps.Flavors)...)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-12T11:29:29Z

- **pkg/workload/workload.go:281** by [@mimowo](https://github.com/mimowo) — 2024-11-12T11:29:29Z
```diff
@@ -261,15 +262,26 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+// IsUsingTAS returns information if the workload is using TAS
+func (i *Info) IsUsingTAS() bool {
+	return slices.ContainsFunc(i.TotalRequests,
+		func(ps PodSetResources) bool {
+			return ps.TopologyRequest != nil
+		})
+}
+
 // TASUsage returns topology usage requested by the Workload
-func (i *Info) TASUsage() []TopologyDomainRequests {
-	if !features.Enabled(features.TopologyAwareScheduling) {
-		return nil
-	}
-	result := make([]TopologyDomainRequests, 0)
+func (i *Info) TASUsage() map[kueue.ResourceFlavorReference][]TopologyDomainRequests {
+	result := make(map[kueue.ResourceFlavorReference][]TopologyDomainRequests, 0)
 	for _, ps := range i.TotalRequests {
 		if ps.TopologyRequest != nil {
-			result = append(result, ps.TopologyRequest.DomainRequests...)
+			psFlavors := sets.New[kueue.ResourceFlavorReference]()
+			for _, psFlavor := range ps.Flavors {
+				psFlavors.Insert(psFlavor)
+			}
```

  reverted
