# PR #351: Enhance and update documentation

**Summary**: Enhance and update documentation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/351

**Last updated**: 2023-08-03T16:55:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `docs-update` → `main`
- **Created**: 2022-08-23T18:26:43Z
- **Updated**: 2023-08-03T16:55:12Z
- **Merged**: 2022-08-24T21:15:50Z
- **Closed**: 2022-08-24T21:15:50Z
- **Labels**: `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 10   **+188 / -54**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Update documentation and changelog highlighting the main features and bugfixes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #222

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T18:27:32Z

/assign @ahg-g

cc @cortespao

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T17:49:55Z

/label tide/merge-method-squash
/hold for reviews

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-24T19:19:46Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-24T20:42:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/351#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/351#pullrequestreview-1084484619" title="Approved">cortespao</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T21:06:21Z

Updated and squashed

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T21:06:37Z

/hold cancel

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-08-24T21:14:05Z

/lgtm

## Reviews

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-08-23T19:35:29Z

- **CHANGELOG/CHANGELOG-0.2.md:6** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:36:59Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
```

  ```suggestion
  - Upgrade the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
  - Rename `Queue` to `LocalQueue`.
  ```

- **CHANGELOG/CHANGELOG-0.2.md:7** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:37:16Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
```

  ```suggestion
  - Add webhooks to validate, and add defaults to all kueue APIs.
  ```

- **CHANGELOG/CHANGELOG-0.2.md:20** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:45:31Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
```

  Not sure if this is a feature or a bug fix.
  If it's a bug fix, it would be good to reword this in the form of:
  "Fixed bug that prevented workloads..."

- **README.md:65** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:48:49Z
```diff
@@ -52,6 +58,18 @@ Learn more about:
 - Kueue [concepts](docs/concepts).
 - Common and advanced [tasks](docs/tasks).
 
+## Architecture
+
+<!-- TODO(#64) Remove links to google docs once the contents have been migrated to this repo -->
+
+Learn more about the architecture of Kueue in the design docs:
```

  ```suggestion
  Learn more about the architecture of Kueue with the following design docs:
  ```

- **README.md:18** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:52:02Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
+
+Kueue APIs allow you to express:
+- Quotas and policies for fair sharing among tenants.
+- Resource fungibility: if a [resource flavor](docs/concepts/cluster_queue.md#resourceflavor-object)
```

  ```suggestion
  - Resource fungibility: if a [resource flavor](docs/concepts/cluster_queue.md#resourceflavor-object) is fully utilized, you can using a different  flavor. For more information, see the [Workload objects](docs/concepts/workload.md).
  ```

- **README.md:18** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:52:27Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
+
+Kueue APIs allow you to express:
+- Quotas and policies for fair sharing among tenants.
+- Resource fungibility: if a [resource flavor](docs/concepts/cluster_queue.md#resourceflavor-object)
```

  Sorry for marking the changes on only one line 😞

- **README.md:69** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:57:43Z
```diff
@@ -52,6 +58,18 @@ Learn more about:
 - Kueue [concepts](docs/concepts).
 - Common and advanced [tasks](docs/tasks).
 
+## Architecture
+
+<!-- TODO(#64) Remove links to google docs once the contents have been migrated to this repo -->
+
+Learn more about the architecture of Kueue in the design docs:
+
+- [bit.ly/kueue-apis](https://bit.ly/kueue-apis) (please join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch)
+to get access) discusses the API proposal and a high-level description of how it
+operates.
```

  ```suggestion
  - [kueue-apis](https://bit.ly/kueue-apis) discusses the API proposal and a high-level description of how it
  operates. Join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch)
  to get document access.
  ```

- **README.md:70** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:57:54Z
```diff
@@ -52,6 +58,18 @@ Learn more about:
 - Kueue [concepts](docs/concepts).
 - Common and advanced [tasks](docs/tasks).
 
+## Architecture
+
+<!-- TODO(#64) Remove links to google docs once the contents have been migrated to this repo -->
+
+Learn more about the architecture of Kueue in the design docs:
+
+- [bit.ly/kueue-apis](https://bit.ly/kueue-apis) (please join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch)
+to get access) discusses the API proposal and a high-level description of how it
+operates.
+- [bit.ly/kueue-controller-design](https://bit.ly/kueue-controller-design)
```

  ```suggestion
  - [kueue-controller-design](https://bit.ly/kueue-controller-design)
  ```

- **docs/concepts/cluster_queue.md:4** by [@cortespao](https://github.com/cortespao) — 2022-08-23T18:59:52Z
```diff
@@ -1,8 +1,9 @@
 # Cluster Queue
 
-A `ClusterQueue` is a cluster-scoped object that governs a pool of resources
+A ClusterQueue is a cluster-scoped object that governs a pool of resources
 such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
```

  ```suggestion
  such as CPU, memory, and hardware accelerators. A `ClusterQueue` defines:
  ```

- **docs/concepts/cluster_queue.md:5** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:01:38Z
```diff
@@ -1,8 +1,9 @@
 # Cluster Queue
 
-A `ClusterQueue` is a cluster-scoped object that governs a pool of resources
+A ClusterQueue is a cluster-scoped object that governs a pool of resources
 such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
-- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- The [resource _flavors_](#resourceflavor-object) that it manages, with usage
```

  ```suggestion
  - The [resource _flavors_](#resourceflavor-object) that the cluster manages, with usage
  ```
  
  I'm assuming "it" refers to "the cluster" but please edit accordingly, but we must mention what "it" means.

- **docs/concepts/cluster_queue.md:42** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:02:03Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
```

  ```suggestion
  (CPU, memory, GPUs, etc.).
  ```

- **docs/concepts/cluster_queue.md:49** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:08:16Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
+
+For each resource, you can define quotas for multiple _flavors_. A
+flavor represents different variations of a resource. The variations can be
+defined in a [ResourceFlavor object](#resourceflavor-object).
+
+In a process called [admission](.#admission), Kueue assigns
+[Workload pod sets](workload.md#pod-sets) a flavor for each resource it requests.
```

  ```suggestion
  to the [Workload pod sets](workload.md#pod-sets) a flavor for each resource Kueue requests.
  ```
  Not sure if "it" refers to "Kueue" but we need to replace "it" with the object name.

- **docs/concepts/cluster_queue.md:58** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:10:39Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
+
+For each resource, you can define quotas for multiple _flavors_. A
+flavor represents different variations of a resource. The variations can be
+defined in a [ResourceFlavor object](#resourceflavor-object).
+
+In a process called [admission](.#admission), Kueue assigns
+[Workload pod sets](workload.md#pod-sets) a flavor for each resource it requests.
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
+list that has enough unused `min` quota in the ClusterQueue or the
+ClusterQueue's [cohort](#cohort).
+
+### Codepedent resources
+
+It is possible that multiple resources are tied to the same flavors. This is
+typical for `cpu` and `memory`, where the flavors are generally tied to a
+machine family or availability guarantees.
```

  ```suggestion
  machine family or availability policies. When two or more resources match their flavors,
  they are said to be codependent resources. 
  ```

- **docs/concepts/cluster_queue.md:64** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:20:35Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
+
+For each resource, you can define quotas for multiple _flavors_. A
+flavor represents different variations of a resource. The variations can be
+defined in a [ResourceFlavor object](#resourceflavor-object).
+
+In a process called [admission](.#admission), Kueue assigns
+[Workload pod sets](workload.md#pod-sets) a flavor for each resource it requests.
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
+list that has enough unused `min` quota in the ClusterQueue or the
+ClusterQueue's [cohort](#cohort).
+
+### Codepedent resources
+
+It is possible that multiple resources are tied to the same flavors. This is
+typical for `cpu` and `memory`, where the flavors are generally tied to a
+machine family or availability guarantees.
+
+If this is the case, the resources in the ClusterQueue must list the same
+flavors in the same order. When two or more resources match their flavors,
+they are said to be codependent. During admission, for each pod set in a
+Workload, Kueue assigns the same flavor to the codependent resources that the
+pod set requests.
```

  ```suggestion
  To manage codependent resources, list the resources in the ClusterQueue in the same
  flavors order. During admission, for each pod set in a
  Workload, Kueue assigns the same flavor to the codependent resources that the
  pod set requests.
  ```

- **docs/concepts/cluster_queue.md:153** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:22:44Z
```diff
@@ -81,7 +150,7 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 - architecture (ex: x86 vs ARM CPUs)
 - brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
 
-A `ResourceFlavor` is an object that represents these variations and allows you
+A ResourceFlavor is an object that represents these variations and allows you
```

  ```suggestion
  A ResourceFlavor is an object that represents these resource variations and allows you
  ```

- **docs/concepts/cluster_queue.md:211** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:25:00Z
```diff
@@ -143,8 +207,9 @@ with taints.
 
 Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
 For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
-workload should have a toleration for it. As opposed to ResourceFlavor labels,
-Kueue will not add tolerations for the flavor taints.
+workload should have a toleration for it. As opposed to the behavior for
+[ResourceFlavor labels](#resourceflavor-labels), Kueue will not add tolerations
```

  ```suggestion
  [ResourceFlavor labels](#resourceflavor-labels), Kueue does not add tolerations
  ```

- **docs/concepts/cluster_queue.md:248** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:31:41Z
```diff
@@ -173,18 +238,18 @@ ClusterQueue.
 
 ### Flavors and borrowing semantics
 
-When borrowing, Kueue satisfies the following semantics:
+When borrowing, Kueue satisfies the following admission semantics:
 
-- When assigning flavors, Kueue goes through the list of flavors in
-  `.spec.resources[*].flavors`. For each flavor, Kueue attempts to
-  fit the workload using the min quota of the ClusterQueue or the unused
-  min quota of other ClusterQueues in the cohort, up to the max quota of the
-  ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
+- When assigning flavors, Kueue goes through the list of flavors in the
+  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  to fit a Workload's pod set using the `min` quota of the ClusterQueue or the
+  unused `min` quota of other ClusterQueues in the cohort, up to the `max` quota
+  of the ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
   flavor in the list.
```

  ```suggestion
  - When assigning flavors, Kueue goes through the list of flavors in the
    ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
    to fit a Workload's pod set using the `min` quota of the ClusterQueue or the
    unused `min` quota of other ClusterQueues in the cohort. If the workload doesn't fit, Kueue proceeds evaluating the next flavor in the list. Kueue attempts to fit workloads until the `max` quota
    of the ClusterQueue is reached. 
  ```

- **docs/concepts/cluster_queue.md:250** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:32:56Z
```diff
@@ -173,18 +238,18 @@ ClusterQueue.
 
 ### Flavors and borrowing semantics
 
-When borrowing, Kueue satisfies the following semantics:
+When borrowing, Kueue satisfies the following admission semantics:
 
-- When assigning flavors, Kueue goes through the list of flavors in
-  `.spec.resources[*].flavors`. For each flavor, Kueue attempts to
-  fit the workload using the min quota of the ClusterQueue or the unused
-  min quota of other ClusterQueues in the cohort, up to the max quota of the
-  ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
+- When assigning flavors, Kueue goes through the list of flavors in the
+  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  to fit a Workload's pod set using the `min` quota of the ClusterQueue or the
+  unused `min` quota of other ClusterQueues in the cohort, up to the `max` quota
+  of the ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
   flavor in the list.
-- Borrowing happens per-flavor. A ClusterQueue can only borrow quota of flavors
-  it defines.
+- A ClusterQueue can only borrow quota of flavors it defines and it can only
+  borrow quota for one flavor.
```

  ```suggestion
  - A ClusterQueue can only borrow quota of flavors that ClusterQueue defines. ClusterQueue can only
    borrow quota for one flavor.
  ```

- **docs/concepts/local_queue.md:7** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:33:51Z
```diff
@@ -0,0 +1,17 @@
+# Local Queue
+
+A `LocalQueue` is a namespaced object that groups closely related workloads
+belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
+from which resources are allocated to run its workloads.
+
+Users submit jobs to a `LocalQueue`, instead of directly to a `ClusterQueue`.
```

  ```suggestion
  Users submit jobs to a `LocalQueue`, instead of to a `ClusterQueue` directly.
  ```

- **docs/concepts/local_queue.md:9** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:34:11Z
```diff
@@ -0,0 +1,17 @@
+# Local Queue
+
+A `LocalQueue` is a namespaced object that groups closely related workloads
+belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
+from which resources are allocated to run its workloads.
+
+Users submit jobs to a `LocalQueue`, instead of directly to a `ClusterQueue`.
+Tenants can discover which queues they can submit jobs to by listing the
+local queues in their namespace. The command looks similar to the following:
```

  ```suggestion
  local queues in their namespace. The command is similar to the following:
  ```

- **docs/setup/install.md:53** by [@cortespao](https://github.com/cortespao) — 2022-08-23T19:35:12Z
```diff
@@ -23,6 +23,20 @@ VERSION=v0.1.1
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+### Uninstall
+
+To uninstall a released version of Kueue from your cluster, run the following command:
+
+```shell
+VERSION=v0.1.1
+kubectl delete -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+
+### Upgrading from 0.1 to 0.2
+
+Upgrading from `0.1.x` to `0.2.y` is not supported due to breaking API changes.
```

  ```suggestion
  Upgrading from `0.1.x` to `0.2.y` is not supported because of breaking API changes.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T20:01:54Z

- **CHANGELOG/CHANGELOG-0.2.md:6** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:01:54Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
```

  Done, except we don't do ticks for API objects in k8s.io

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T20:02:55Z

- **CHANGELOG/CHANGELOG-0.2.md:7** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:02:54Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
```

  the webhooks validate and add defaults.
  
  How's the new wording?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T20:04:00Z

- **CHANGELOG/CHANGELOG-0.2.md:20** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:04:00Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T20:04:58Z

- **README.md:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:04:58Z
```diff
@@ -52,6 +58,18 @@ Learn more about:
 - Kueue [concepts](docs/concepts).
 - Common and advanced [tasks](docs/tasks).
 
+## Architecture
+
+<!-- TODO(#64) Remove links to google docs once the contents have been migrated to this repo -->
+
+Learn more about the architecture of Kueue in the design docs:
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T20:08:34Z

- **README.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:08:33Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
+
+Kueue APIs allow you to express:
+- Quotas and policies for fair sharing among tenants.
+- Resource fungibility: if a [resource flavor](docs/concepts/cluster_queue.md#resourceflavor-object)
```

  I added the subject. It's kueue that runs (or rather admit) the job in a different flavor. I think we don't need the link to workload again. It's in the first paragraph.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-23T20:58:01Z

- **CHANGELOG/CHANGELOG-0.2.md:21** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T18:30:52Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
+- Fixed number of pending workloads in a BestEffortFIFO ClusterQueue.
```

  ```suggestion
  - Fixed the number of pending workloads in a BestEffortFIFO ClusterQueue status.
  ```

- **CHANGELOG/CHANGELOG-0.2.md:22** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T18:31:38Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
+- Fixed number of pending workloads in a BestEffortFIFO ClusterQueue.
 - Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
```

  ```suggestion
  - Fixed a bug in BestEffortFIFO ClusterQueue where a workload might not be
  ```

- **CHANGELOG/CHANGELOG-0.2.md:25** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T18:32:01Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
+- Fixed number of pending workloads in a BestEffortFIFO ClusterQueue.
 - Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
   retried after a transient error.
-- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Fixed requeuing an out-of-date workload when failed to admit it.
+- Fixed bug in a BestEffortFIFO ClusterQueue where unadmissible workloads
```

  ```suggestion
  - Fixed a bug in BestEffortFIFO ClusterQueue where inadmissible workloads
  ```

- **docs/concepts/cluster_queue.md:46** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T20:06:06Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
+
+For each resource, you can define quotas for multiple _flavors_. A
+flavor represents different variations of a resource. The variations can be
+defined in a [ResourceFlavor object](#resourceflavor-object).
```

  ```suggestion
  For each resource, you can define quotas for multiple _flavors_. Flavors represent different variations of a resource (for example, different GPU models). A flavor can be defined using a [ResourceFlavor object](#resourceflavor-object).
  ```

- **README.md:14** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T20:33:06Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
```

  ```suggestion
  Kueue is a lean controller that you can install on top of a vanilla Kubernetes
  cluster. It does not replace any existing Kubernetes components. It is compatible with cloud
  environments where:
  - Compute resources are elastic and can be scaled up and down.
  - Compute resources are heterogeneous (in architecture, availability, price, etc.).
  ```

- **README.md:69** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T20:36:00Z
```diff
@@ -52,6 +58,18 @@ Learn more about:
 - Kueue [concepts](docs/concepts).
 - Common and advanced [tasks](docs/tasks).
 
+## Architecture
+
+<!-- TODO(#64) Remove links to google docs once the contents have been migrated to this repo -->
+
+Learn more about the architecture of Kueue in the design docs:
+
+- [bit.ly/kueue-apis](https://bit.ly/kueue-apis) (please join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch)
+to get access) discusses the API proposal and a high-level description of how it
+operates.
```

  I would keep the `bit.ly/` prefix, sometimes people highlight and want to copy the short link

- **docs/concepts/cluster_queue.md:58** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T20:42:50Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
+
+For each resource, you can define quotas for multiple _flavors_. A
+flavor represents different variations of a resource. The variations can be
+defined in a [ResourceFlavor object](#resourceflavor-object).
+
+In a process called [admission](.#admission), Kueue assigns
+[Workload pod sets](workload.md#pod-sets) a flavor for each resource it requests.
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
+list that has enough unused `min` quota in the ClusterQueue or the
+ClusterQueue's [cohort](#cohort).
+
+### Codepedent resources
+
+It is possible that multiple resources are tied to the same flavors. This is
+typical for `cpu` and `memory`, where the flavors are generally tied to a
+machine family or availability guarantees.
```

  ```suggestion
  machine family or provisioning model (e.g., spot vs standard).
  ```

- **docs/concepts/cluster_queue.md:154** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T20:46:35Z
```diff
@@ -81,7 +150,7 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 - architecture (ex: x86 vs ARM CPUs)
 - brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
 
-A `ResourceFlavor` is an object that represents these variations and allows you
+A ResourceFlavor is an object that represents these variations and allows you
 to associate them with node labels and taints.
```

  ```suggestion
  A ResourceFlavor is an object that represents these variations and allows you
  to associate them with the labels and taints of the nodes that offer those resource flavors.
  ```

- **docs/concepts/cluster_queue.md:201** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T20:52:15Z
```diff
@@ -132,9 +196,9 @@ steps:
    didn't specify them already.
 
    For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
-   Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
-   that the workload Pods run on the nodes associated to the flavor that Kueue
-   decided that the workload should use.
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the workload Pods run on the nodes associated to the flavor
+   that Kueue decided that the workload should use.
```

  ```suggestion
     Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
     guarantees that the workload Pods can only be scheduled on the nodes targeted by the flavor
     that Kueue assigned to the workload.
  ```

- **docs/concepts/cluster_queue.md:250** by [@ahg-g](https://github.com/ahg-g) — 2022-08-23T20:54:51Z
```diff
@@ -173,18 +238,18 @@ ClusterQueue.
 
 ### Flavors and borrowing semantics
 
-When borrowing, Kueue satisfies the following semantics:
+When borrowing, Kueue satisfies the following admission semantics:
 
-- When assigning flavors, Kueue goes through the list of flavors in
-  `.spec.resources[*].flavors`. For each flavor, Kueue attempts to
-  fit the workload using the min quota of the ClusterQueue or the unused
-  min quota of other ClusterQueues in the cohort, up to the max quota of the
-  ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
+- When assigning flavors, Kueue goes through the list of flavors in the
+  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  to fit a Workload's pod set using the `min` quota of the ClusterQueue or the
+  unused `min` quota of other ClusterQueues in the cohort, up to the `max` quota
+  of the ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
   flavor in the list.
-- Borrowing happens per-flavor. A ClusterQueue can only borrow quota of flavors
-  it defines.
+- A ClusterQueue can only borrow quota of flavors it defines and it can only
+  borrow quota for one flavor.
```

  I wouldn't mention "and it can only borrow quota for one flavor" because that also applies in the no borrowing case as well.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T21:09:04Z

- **docs/concepts/cluster_queue.md:4** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:14:19Z
```diff
@@ -1,8 +1,9 @@
 # Cluster Queue
 
-A `ClusterQueue` is a cluster-scoped object that governs a pool of resources
+A ClusterQueue is a cluster-scoped object that governs a pool of resources
 such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
```

  Done

- **docs/concepts/cluster_queue.md:5** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:14:56Z
```diff
@@ -1,8 +1,9 @@
 # Cluster Queue
 
-A `ClusterQueue` is a cluster-scoped object that governs a pool of resources
+A ClusterQueue is a cluster-scoped object that governs a pool of resources
 such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
-- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- The [resource _flavors_](#resourceflavor-object) that it manages, with usage
```

  the ClusterQueue. Done

- **docs/concepts/cluster_queue.md:49** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:16:32Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
+
+For each resource, you can define quotas for multiple _flavors_. A
+flavor represents different variations of a resource. The variations can be
+defined in a [ResourceFlavor object](#resourceflavor-object).
+
+In a process called [admission](.#admission), Kueue assigns
+[Workload pod sets](workload.md#pod-sets) a flavor for each resource it requests.
```

  the pod set. Done

- **docs/concepts/cluster_queue.md:64** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:28:49Z
```diff
@@ -35,6 +36,74 @@ This ClusterQueue admits [workloads](workload.md) if and only if:
 
 You can specify the quota as a [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/).
 
+## Resources
+
+In a ClusterQueue, you can define quotas for multiple [compute resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+(cpu, memory, GPUs, etc.).
+
+For each resource, you can define quotas for multiple _flavors_. A
+flavor represents different variations of a resource. The variations can be
+defined in a [ResourceFlavor object](#resourceflavor-object).
+
+In a process called [admission](.#admission), Kueue assigns
+[Workload pod sets](workload.md#pod-sets) a flavor for each resource it requests.
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
+list that has enough unused `min` quota in the ClusterQueue or the
+ClusterQueue's [cohort](#cohort).
+
+### Codepedent resources
+
+It is possible that multiple resources are tied to the same flavors. This is
+typical for `cpu` and `memory`, where the flavors are generally tied to a
+machine family or availability guarantees.
+
+If this is the case, the resources in the ClusterQueue must list the same
+flavors in the same order. When two or more resources match their flavors,
+they are said to be codependent. During admission, for each pod set in a
+Workload, Kueue assigns the same flavor to the codependent resources that the
+pod set requests.
```

  Done

- **docs/concepts/cluster_queue.md:153** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:29:39Z
```diff
@@ -81,7 +150,7 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 - architecture (ex: x86 vs ARM CPUs)
 - brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
 
-A `ResourceFlavor` is an object that represents these variations and allows you
+A ResourceFlavor is an object that represents these variations and allows you
```

  Done

- **docs/concepts/cluster_queue.md:211** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:29:59Z
```diff
@@ -143,8 +207,9 @@ with taints.
 
 Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
 For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
-workload should have a toleration for it. As opposed to ResourceFlavor labels,
-Kueue will not add tolerations for the flavor taints.
+workload should have a toleration for it. As opposed to the behavior for
+[ResourceFlavor labels](#resourceflavor-labels), Kueue will not add tolerations
```

  Done

- **docs/concepts/cluster_queue.md:248** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T20:54:42Z
```diff
@@ -173,18 +238,18 @@ ClusterQueue.
 
 ### Flavors and borrowing semantics
 
-When borrowing, Kueue satisfies the following semantics:
+When borrowing, Kueue satisfies the following admission semantics:
 
-- When assigning flavors, Kueue goes through the list of flavors in
-  `.spec.resources[*].flavors`. For each flavor, Kueue attempts to
-  fit the workload using the min quota of the ClusterQueue or the unused
-  min quota of other ClusterQueues in the cohort, up to the max quota of the
-  ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
+- When assigning flavors, Kueue goes through the list of flavors in the
+  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  to fit a Workload's pod set using the `min` quota of the ClusterQueue or the
+  unused `min` quota of other ClusterQueues in the cohort, up to the `max` quota
+  of the ClusterQueue. If the workload doesn't fit, Kueue proceeds evaluating the next
   flavor in the list.
```

  I completely reworked this section

- **docs/setup/install.md:53** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:08:58Z
```diff
@@ -23,6 +23,20 @@ VERSION=v0.1.1
 kubectl apply -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
 
+### Uninstall
+
+To uninstall a released version of Kueue from your cluster, run the following command:
+
+```shell
+VERSION=v0.1.1
+kubectl delete -f https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
+```
+
+### Upgrading from 0.1 to 0.2
+
+Upgrading from `0.1.x` to `0.2.y` is not supported due to breaking API changes.
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T21:12:23Z

- **CHANGELOG/CHANGELOG-0.2.md:21** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:12:23Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
+- Fixed number of pending workloads in a BestEffortFIFO ClusterQueue.
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-23T21:21:36Z

- **CHANGELOG/CHANGELOG-0.2.md:22** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:13:33Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
+- Fixed number of pending workloads in a BestEffortFIFO ClusterQueue.
 - Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
```

  Done

- **CHANGELOG/CHANGELOG-0.2.md:25** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:13:38Z
```diff
@@ -2,6 +2,23 @@
 
 Changes since `v0.1.0`:
 
+### Features
+- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Add webhooks to validate and add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
+
+### Bug fixes
+
+- Prevent Workloads that don't match the ClusterQueue's namespaceSelector from
+  blocking other Workloads in a StrictFIFO ClusterQueue.
+- Fixed number of pending workloads in a BestEffortFIFO ClusterQueue.
 - Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
   retried after a transient error.
-- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+- Fixed requeuing an out-of-date workload when failed to admit it.
+- Fixed bug in a BestEffortFIFO ClusterQueue where unadmissible workloads
```

  Done

- **README.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:15:19Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
```

  Done

- **docs/concepts/cluster_queue.md:201** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:19:47Z
```diff
@@ -132,9 +196,9 @@ steps:
    didn't specify them already.
 
    For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
-   Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
-   that the workload Pods run on the nodes associated to the flavor that Kueue
-   decided that the workload should use.
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the workload Pods run on the nodes associated to the flavor
+   that Kueue decided that the workload should use.
```

  nice, I was having a hard time with this one :)

- **docs/concepts/local_queue.md:7** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:21:01Z
```diff
@@ -0,0 +1,17 @@
+# Local Queue
+
+A `LocalQueue` is a namespaced object that groups closely related workloads
+belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
+from which resources are allocated to run its workloads.
+
+Users submit jobs to a `LocalQueue`, instead of directly to a `ClusterQueue`.
```

  Done

- **docs/concepts/local_queue.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-23T21:21:06Z
```diff
@@ -0,0 +1,17 @@
+# Local Queue
+
+A `LocalQueue` is a namespaced object that groups closely related workloads
+belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
+from which resources are allocated to run its workloads.
+
+Users submit jobs to a `LocalQueue`, instead of directly to a `ClusterQueue`.
+Tenants can discover which queues they can submit jobs to by listing the
+local queues in their namespace. The command looks similar to the following:
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-08-24T18:13:24Z

- **README.md:11** by [@ahg-g](https://github.com/ahg-g) — 2022-08-24T18:13:24Z
```diff
@@ -5,21 +5,26 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster. Kue does not replace any existing Kubernetes components. Kueue is
```

  ```suggestion
  cluster. Kueue does not replace any existing Kubernetes components. Kueue is
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-24T18:54:53Z

- **README.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T18:54:53Z
```diff
@@ -5,21 +5,26 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster. Kue does not replace any existing Kubernetes components. Kueue is
```

  Ah, how did that happen?
  Although not a bad name (it's basically pronounced the same).

### Review by [@cortespao](https://github.com/cortespao) (approved) — 2022-08-24T20:42:06Z

Just nits. 
Docs look awesome. Thanks all!

- **CHANGELOG/CHANGELOG-0.2.md:12** by [@cortespao](https://github.com/cortespao) — 2022-08-24T20:41:15Z
```diff
@@ -2,8 +2,27 @@
 
 Changes since `v0.1.0`:
 
-- Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
-  retried after a transient error.
-- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+### Features
+
+- Upgrade the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
+- Rename Queue to LocalQueue.
+- Add webhooks to validate and to add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
```

  ```suggestion
  - Define default requests to limits if requests are not set in a Workload pod set, to
  ```

- **CHANGELOG/CHANGELOG-0.2.md:14** by [@cortespao](https://github.com/cortespao) — 2022-08-24T20:41:22Z
```diff
@@ -2,8 +2,27 @@
 
 Changes since `v0.1.0`:
 
-- Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
-  retried after a transient error.
-- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+### Features
+
+- Upgrade the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
+- Rename Queue to LocalQueue.
+- Add webhooks to validate and to add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
 - Added [prometheus metrics](/docs/reference/metrics.md) to monitor health of
```

  ```suggestion
  - Add [prometheus metrics](/docs/reference/metrics.md) to monitor health of
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-24T20:54:44Z

- **CHANGELOG/CHANGELOG-0.2.md:12** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T20:54:43Z
```diff
@@ -2,8 +2,27 @@
 
 Changes since `v0.1.0`:
 
-- Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
-  retried after a transient error.
-- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+### Features
+
+- Upgrade the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
+- Rename Queue to LocalQueue.
+- Add webhooks to validate and to add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
```

  Maybe Set is a more accurate verb:
  
  ```
  Set requests to limits
  ```
  
  wdyt?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-24T21:06:07Z

- **CHANGELOG/CHANGELOG-0.2.md:12** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T21:06:07Z
```diff
@@ -2,8 +2,27 @@
 
 Changes since `v0.1.0`:
 
-- Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
-  retried after a transient error.
-- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+### Features
+
+- Upgrade the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
+- Rename Queue to LocalQueue.
+- Add webhooks to validate and to add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
```

  Changed to set and added a link to k8s reference doc.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-08-24T21:06:12Z

- **CHANGELOG/CHANGELOG-0.2.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-24T21:06:11Z
```diff
@@ -2,8 +2,27 @@
 
 Changes since `v0.1.0`:
 
-- Fixed bug in a BestEffortFIFO ClusterQueue where a workload might not be
-  retried after a transient error.
-- Bumped the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported and Queue is now named LocalQueue.
+### Features
+
+- Upgrade the API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
+- Rename Queue to LocalQueue.
+- Add webhooks to validate and to add defaults to all kueue APIs.
+- Support [codependent resources](/docs/concepts/cluster_queue.md#codepedent-resources)
+  by assigning the same flavor to codependent resources in a pod set.
+- Support [pod overhead](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/)
+  in Workload pod sets.
+- Default requests to limits if requests are not set in a Workload pod set, to
+  match internal defaulting for k8s Pods.
 - Added [prometheus metrics](/docs/reference/metrics.md) to monitor health of
```

  Done

### Review by [@rafrafek](https://github.com/rafrafek) (commented) — 2023-08-03T13:08:23Z

- **README.md:14** by [@rafrafek](https://github.com/rafrafek) — 2023-08-03T13:08:22Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
```

  What does "heterogeneous" mean in this example?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-03T15:37:55Z

- **README.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-03T15:37:55Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
```

  they have different characteristics

### Review by [@rafrafek](https://github.com/rafrafek) (commented) — 2023-08-03T15:41:44Z

- **README.md:14** by [@rafrafek](https://github.com/rafrafek) — 2023-08-03T15:41:44Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
```

  Does it mean it is not compatible with environments where architecture is homogeneous?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-03T16:55:12Z

- **README.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-03T16:55:12Z
```diff
@@ -5,21 +5,27 @@ Kueue is a set of APIs and controller for [job](docs/concepts/workload.md)
 a job should be [admitted](docs/concepts#admission) to start (as in pods can be
 created) and when it should stop (as in active pods should be deleted).
 
+## Why use Kueue
+
+Kueue is a lean controller that you can install on top of a vanilla Kubernetes
+cluster without replacing any components. It is compatible with cloud
+environments where:
+- Nodes and other compute resources can be scaled up and down.
+- Compute resources are heterogeneous (in architecture, availability, price, etc.).
```

  It is compatible, as homogeneous is just simpler. In kueue terms, if your resources are homegeneous, you need only one ResourceFlavor.
