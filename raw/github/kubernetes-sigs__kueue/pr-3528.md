# PR #3528: Allow mutating queue name in Deployment Webhook.

**Summary**: Allow mutating queue name in Deployment Webhook.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3528

**Last updated**: 2024-11-25T11:55:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/manageJobsWithoutQueueName-deployment` → `main`
- **Created**: 2024-11-13T21:44:35Z
- **Updated**: 2024-11-25T11:55:24Z
- **Merged**: 2024-11-25T09:50:56Z
- **Closed**: 2024-11-25T09:50:56Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `needs-kind`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 7   **+233 / -22**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Allow mutating queue name in Deployment Webhook.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3552

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Allow mutating the queue-name label for non-running Deployments.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-13T21:44:38Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-13T21:44:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c2426ca4dba231f1777540ee54c5cbca2aff27b7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6740448adf05400008601d0c |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-15T19:05:42Z

/hold for #3555 #3554 #3553

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-18T13:35:22Z

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-21T08:23:15Z

@mbobrovskyi is it still WIP or ready for review - the tests don't pass so I'm not sure

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T08:24:43Z

> @mbobrovskyi is it still WIP or ready for review - the tests don't pass so I'm not sure

Yes, because we need to merge https://github.com/kubernetes-sigs/kueue/pull/3315 first to allow deletion on Default().

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-21T08:29:38Z

Can we restict this PR to mutating only, but don't support dropping yet? It would provide the functionality in easier to dijest PRs. I'm also not sure we want to merge that one since we will have a fix in controller-runtime.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T08:32:14Z

> Can we restict this PR to mutating only, but don't support dropping yet?

It will not work if user want to remove queue-name label.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T08:33:49Z

> It would provide the functionality in easier to dijest PRs. I'm also not sure we want to merge that one since we will have a fix in controller-runtime.

I can't see some problems to merge it. We will still need to delete our defaulter.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-21T08:47:26Z

> It will not work if user want to remove queue-name label.

I know, but mutating is probably a more common use-case than deleting, and would let us handle these two scenarios independently

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T13:38:14Z

> Can we also have e2e test for that? When renaming from non-existing to existing queue-name

Oh, I found another problem. When Pods have Scheduling Gates and we update them through the Deployment, both the Pods and the Workloads get stuck. I will try to fix it, but I’ll need a reconciler for that.

/hold to fix this issue.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-22T07:19:59Z

/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-25T08:31:07Z

LGTM label has been added.  <details>Git tree hash: 388d6c014f42f3a0a328bffaa964bda43b90453f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-25T08:31:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3528#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3528#pullrequestreview-2457640649" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-25T08:32:45Z

/release-note-edit
```release-note
Allow mutating the queue-name label for non-running Deployments.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-25T09:38:50Z

/retest
Issue opened: https://github.com/kubernetes-sigs/kueue/issues/3620

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-14T09:03:42Z

- **test/e2e/config/common/controller_manager_config.yaml:18** by [@mimowo](https://github.com/mimowo) — 2024-11-14T09:03:42Z
```diff
@@ -15,6 +15,7 @@ fairSharing:
 clientConnection:
   qps: 50
   burst: 100
+manageJobsWithoutQueueName: true
```

  I'm not a fan of that. It is not official, but generally `manageJobsWithoutQueueName: false` + ValidationAdmissionPolicy is the preferred configuration for prod for users, as explained here: https://kueue.sigs.k8s.io/docs/tasks/manage/setup_job_admission_policy/. Then admins have full control over the admission policy.
  
  Maybe we could have 2 suites? I know this means an extra overhead to run them, but I think both configurations should be tested. cc @tenzen-y @dgrove-oss

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-14T10:05:54Z

- **test/e2e/config/common/controller_manager_config.yaml:18** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-14T10:05:53Z
```diff
@@ -15,6 +15,7 @@ fairSharing:
 clientConnection:
   qps: 50
   burst: 100
+manageJobsWithoutQueueName: true
```

  I think we can move this logic to integration test. For this case I think it should be enough. The only one place where we using manageJobsWithoutQueueName on the webhooks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-14T10:29:20Z

- **test/e2e/config/common/controller_manager_config.yaml:18** by [@mimowo](https://github.com/mimowo) — 2024-11-14T10:29:20Z
```diff
@@ -15,6 +15,7 @@ fairSharing:
 clientConnection:
   qps: 50
   burst: 100
+manageJobsWithoutQueueName: true
```

  Maybe, but we need to make sure the integration tests have the webhooks enabled. 
  
  Also, I feel the integration may depend on the details of the Deployment or StatefulSet running. I remember some scenarios where the order of deletions by STS matters. 
  
  So, my preference is still to have a dedicated e2e suite, but I'm ok to rely on integration as long as they have the webhooks enabled. Still, I don't like changing `manageJobsWithoutQueueName` in the main e2e test suite.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-11-14T14:23:36Z

- **test/e2e/config/common/controller_manager_config.yaml:18** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-11-14T14:23:36Z
```diff
@@ -15,6 +15,7 @@ fairSharing:
 clientConnection:
   qps: 50
   burst: 100
+manageJobsWithoutQueueName: true
```

  > I'm not a fan of that. It is not official, but generally `manageJobsWithoutQueueName: false` + ValidationAdmissionPolicy is the preferred configuration for prod for users, as explained here: https://kueue.sigs.k8s.io/docs/tasks/manage/setup_job_admission_policy/. Then admins have full control over the admission policy.
  > 
  
  I think this is right.  Since Jobs don't get the namespaceSelector exemption from `manageJobsWithoutQueueName` its hard to see how to set this flag to true in production.  Instead we prefer to set to false and use VAP.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-15T16:09:42Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:67** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-15T16:09:42Z
```diff
@@ -52,21 +52,23 @@ func SetupWebhook(mgr ctrl.Manager, opts ...jobframework.Option) error {
 		Complete()
 }
 
-// +kubebuilder:webhook:path=/mutate-apps-v1-deployment,mutating=true,failurePolicy=fail,sideEffects=None,groups="apps",resources=deployments,verbs=create,versions=v1,name=mdeployment.kb.io,admissionReviewVersions=v1
+// +kubebuilder:webhook:path=/mutate-apps-v1-deployment,mutating=true,failurePolicy=fail,sideEffects=None,groups="apps",resources=deployments,verbs=create;update,versions=v1,name=mdeployment.kb.io,admissionReviewVersions=v1
 
 var _ admission.CustomDefaulter = &Webhook{}
 
 func (wh *Webhook) Default(ctx context.Context, obj runtime.Object) error {
-	d := fromObject(obj)
-	log := ctrl.LoggerFrom(ctx).WithName("deployment-webhook").WithValues("deployment", klog.KObj(d))
+	deployment := fromObject(obj)
+
+	log := ctrl.LoggerFrom(ctx).WithName("deployment-webhook").WithValues("deployment", klog.KObj(deployment))
 	log.V(5).Info("Applying defaults")
 
-	cqLabel, ok := d.Labels[constants.QueueLabel]
-	if ok {
-		if d.Spec.Template.Labels == nil {
-			d.Spec.Template.Labels = make(map[string]string, 1)
+	if queueName := jobframework.QueueNameForObject(deployment.Object()); queueName != "" {
+		if deployment.Spec.Template.Labels == nil {
+			deployment.Spec.Template.Labels = make(map[string]string, 1)
 		}
-		d.Spec.Template.Labels[constants.QueueLabel] = cqLabel
+		deployment.Spec.Template.Labels[constants.QueueLabel] = queueName
+	} else if wh.manageJobsWithoutQueueName {
+		delete(deployment.Spec.Template.Labels, constants.QueueLabel)
```

  @mimowo can we merge https://github.com/kubernetes-sigs/kueue/pull/3315 to allow deletion on Webhook?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-21T12:25:59Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:102** by [@mimowo](https://github.com/mimowo) — 2024-11-21T12:25:59Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
```

  Add assert to check that queue-name cannot be removed, because this remains broken in the PR, and add unit test to check that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-21T12:26:44Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:105** by [@mimowo](https://github.com/mimowo) — 2024-11-21T12:26:44Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
+
+	if oldDeployment.Status.ReadyReplicas > 0 {
```

  add a comment why needed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-21T12:27:22Z

Can we also have e2e test for that? When renaming from non-existing to existing queue-name

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-21T13:35:07Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:105** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T13:35:07Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
+
+	if oldDeployment.Status.ReadyReplicas > 0 {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-21T13:35:26Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T13:35:26Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-21T13:37:37Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:102** by [@mimowo](https://github.com/mimowo) — 2024-11-21T13:37:37Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
```

  which test case shows that the queue-name cannot be removed?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-21T13:40:20Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T13:40:20Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
```

  Integration test 
  
  https://github.com/kubernetes-sigs/kueue/blob/bb5e82fce0102c72ef2aa4dab1b1d9042f171dfd/test/integration/webhook/jobs/deployment_webhook_test.go#L113-L128

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-21T13:42:44Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-21T13:42:44Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
```

  Unit test
  
  https://github.com/kubernetes-sigs/kueue/blob/bb5e82fce0102c72ef2aa4dab1b1d9042f171dfd/pkg/controller/jobs/deployment/deployment_webhook_test.go#L61-L64

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-22T08:01:16Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:102** by [@mimowo](https://github.com/mimowo) — 2024-11-22T08:01:16Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
```

  Sorry, I meant that we should still block dropping the queue-name on the Deployment. So oldQueueName!= nil, newQueueName=nil, should be blocked regardless of Status.ReadyReplicas

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-22T08:34:33Z

- **pkg/controller/jobs/deployment/deployment_webhook.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-22T08:34:33Z
```diff
@@ -100,12 +98,12 @@ func (wh *Webhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Ob
 	oldQueueName := jobframework.QueueNameForObject(oldDeployment.Object())
 	newQueueName := jobframework.QueueNameForObject(newDeployment.Object())
 
-	allErrs := apivalidation.ValidateImmutableField(oldQueueName, newQueueName, queueNameLabelPath)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(
-		newDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		oldDeployment.Spec.Template.GetLabels()[constants.QueueLabel],
-		podSpecQueueNameLabelPath,
-	)...)
+	allErrs := field.ErrorList{}
+	allErrs = append(allErrs, jobframework.ValidateQueueName(newDeployment.Object())...)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-25T08:31:01Z

/lgtm
/approve
Thanks!
