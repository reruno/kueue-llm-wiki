# PR #3560: Add wrapper for `ProvisioningRequestConfig` to util/testing package

**Summary**: Add wrapper for `ProvisioningRequestConfig` to util/testing package

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3560

**Last updated**: 2024-12-02T09:37:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@TusharMohapatra07](https://github.com/TusharMohapatra07)
- **Branch**: `wrapper` → `main`
- **Created**: 2024-11-16T16:23:33Z
- **Updated**: 2024-12-02T09:37:01Z
- **Merged**: 2024-12-02T09:37:00Z
- **Closed**: 2024-12-02T09:37:00Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+144 / -189**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

Improve code quality

#### Which issue(s) this PR fixes:

Fixes #3472 

```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-16T16:23:44Z

Hi @TusharMohapatra07. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-16T16:23:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b59774953c9b17976059ba98e6fd73e0f2815b47 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6749668c0f36e800084e2e74 |

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-16T16:24:50Z

@PBundyra @mimowo What other operations(methods) are expected on the wrapper??

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-18T05:16:06Z

What is the purpose of this wrapper if it is never used?

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-18T06:31:20Z

@mbobrovskyi The issue says to create a wrapper for `ProvisioningRequestConfig`.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-18T06:50:47Z

> @mbobrovskyi The issue says to create a wrapper for `ProvisioningRequestConfig`.

I agree the issue description is unclear. However, we need to not only create the wrapper but also use it in the code.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-18T10:53:00Z

> @PBundyra @mimowo What other operations(methods) are expected on the wrapper??

We need methods to set:
- `.spec.provisioningClassName`
- `.spec.parameters`
- `.spec.managedResources`
- `.spec.retryStrategy`
- `.spec.retryStrategy.backoffLimitCount`
- `.spec.retryStrategy.backoffBaseSeconds`
- `.spec.retryStrategy.backoffMaxSeconds`

And to create an actual object from a wrapper.

The point of introducing the wrapper would be indeed to use as @mbobrovskyi mentioned. Sorry for the unclear description. This change should be reflected in files:
- `pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler_test.go`
- `pkg/controller/admissionchecks/provisioning/controller_test.go`
- `pkg/util/admissioncheck/admissioncheck_test.go`
- `test/integration/controller/admissionchecks/provisioning/provisioning_test.go`

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-26T05:08:15Z

@mbobrovskyi @PBundyra Sorry for being late. I've used the wrapper in `controller_test.go`. I shall proceed with further using the wrapper post approval.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T06:33:28Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T06:37:35Z

@TusharMohapatra07 please apply for other files.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T08:43:33Z

Please add methods for fields:
``` 
.spec.managedResources
.spec.retryStrategy.backoffLimitCount
.spec.retryStrategy.backoffBaseSeconds
.spec.retryStrategy.backoffMaxSeconds
```

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-26T13:03:35Z

@mbobrovskyi @PBundyra Is this what's intended ?

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-26T13:10:00Z

Also, can i remove this https://github.com/kubernetes-sigs/kueue/blob/998d17cf826497f4a17c883f32ff6eceebce172f/pkg/controller/admissionchecks/provisioning/controller_test.go#L92
and
https://github.com/kubernetes-sigs/kueue/blob/998d17cf826497f4a17c883f32ff6eceebce172f/pkg/controller/admissionchecks/provisioning/controller_test.go#L98
as these are no where used and redundant now that we're using wrapper

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T13:19:58Z

> Also, can i remove this
> 
> https://github.com/kubernetes-sigs/kueue/blob/998d17cf826497f4a17c883f32ff6eceebce172f/pkg/controller/admissionchecks/provisioning/controller_test.go#L92
> 
> 
> and
> https://github.com/kubernetes-sigs/kueue/blob/998d17cf826497f4a17c883f32ff6eceebce172f/pkg/controller/admissionchecks/provisioning/controller_test.go#L98
> 
> 
> as these are no where used and redundant now that we're using wrapper

Yes, please.

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-26T13:55:12Z

Please review for `controller_test.go` so that i can proceed to other files if this is done

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T07:09:18Z

https://github.com/kubernetes-sigs/kueue/blob/783f74b5dbbaa12e8d3368cd80b2f7153619c64b/pkg/controller/admissionchecks/provisioning/controller_test.go#L345

Here we can use MakeProvisioningRequestConfig("config1")

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T09:10:17Z

Great effort @TusharMohapatra07! This PR is getting better and better and is heading into the right direction

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-28T11:28:30Z

> Great effort @TusharMohapatra07! This PR is getting better and better and is heading into the right direction

Glad to hear that @PBundyra

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-28T13:32:00Z

@PBundyra @mbobrovskyi 
![image](https://github.com/user-attachments/assets/487ada9d-e6fc-4164-89e3-339c4b01c86e)
I'm not being able to use the value without dereferencing as suggested in https://github.com/kubernetes-sigs/kueue/pull/3560#discussion_r1861775679

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-28T13:34:41Z

> @PBundyra @mbobrovskyi ![image](https://private-user-images.githubusercontent.com/137442734/390801250-487ada9d-e6fc-4164-89e3-339c4b01c86e.png?jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJnaXRodWIuY29tIiwiYXVkIjoicmF3LmdpdGh1YnVzZXJjb250ZW50LmNvbSIsImtleSI6ImtleTUiLCJleHAiOjE3MzI4MDEwODEsIm5iZiI6MTczMjgwMDc4MSwicGF0aCI6Ii8xMzc0NDI3MzQvMzkwODAxMjUwLTQ4N2FkYTlkLWU2ZmMtNDE2NC04OWUzLTMzOWM0YjAxYzg2ZS5wbmc_.GInD6rv5sEmvnBIoaadEiQ43762FBkFI0COKYyaMEVI) I'm not being able to use the value without dereferencing as suggested in [#3560 (comment)](https://github.com/kubernetes-sigs/kueue/pull/3560#discussion_r1861775679)

It should work.

```
[]kueue.ProvisioningRequestConfig{*baseConfigWithRetryStrategy.Clone().RetryLimit(1).BaseBackoff(0).Obj()},
```

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-28T13:35:53Z


> It should work.
> 
> ```
> []kueue.ProvisioningRequestConfig{*baseConfigWithRetryStrategy.Clone().RetryLimit(1).BaseBackoff(0).Obj()},
> ```

Yaa that's what, i need to dereference it

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-28T13:39:24Z

> > It should work.
> > ```
> > []kueue.ProvisioningRequestConfig{*baseConfigWithRetryStrategy.Clone().RetryLimit(1).BaseBackoff(0).Obj()},
> > ```
> 
> Yaa that's what, i need to dereference it

The question not about references. But use DeepCopy() only if it's really need.

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-28T13:45:34Z

@mbobrovskyi just to confirm, i don't have to use Obj() here right ? 
![image](https://github.com/user-attachments/assets/d0fa21da-925c-4658-9f6a-37d71584a440)

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T14:13:04Z

> @mbobrovskyi just to confirm, i don't have to use Obj() here right ? ![image](https://private-user-images.githubusercontent.com/137442734/390805780-d0fa21da-925c-4658-9f6a-37d71584a440.png?jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJnaXRodWIuY29tIiwiYXVkIjoicmF3LmdpdGh1YnVzZXJjb250ZW50LmNvbSIsImtleSI6ImtleTUiLCJleHAiOjE3MzI4MDMyNTcsIm5iZiI6MTczMjgwMjk1NywicGF0aCI6Ii8xMzc0NDI3MzQvMzkwODA1NzgwLWQwZmEyMWRhLTkyNWMtNDY1OC05ZjZhLTM3ZDcxNTg0YTQ0MC5wbmc_.uFv0k2N_K1Emd3YIxrBCsh9bGYX5hFK0it4X55iFz5I)

You should `Clone()` the base wrapper and then use `Obj()` to create the actual underlying object from the wrapper

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-29T05:58:54Z

I've used the wrapper in `admissioncheck_reconciler_test.go`, `admissioncheck_test.go`(there was nothing much to do in these two) and `provisioning_test.go`. Please review @mbobrovskyi

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-29T06:15:07Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-29T07:01:26Z

/lgtm
Awesome! Thank you so much!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-29T07:01:32Z

LGTM label has been added.  <details>Git tree hash: 278ad740d7663cea89a8e17b0082eb6757fe3e2e</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-29T07:02:01Z

Please also open the PR.

### Comment by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-29T07:05:03Z

@mbobrovskyi done

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-29T07:17:48Z

/cc @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-29T09:44:13Z

LGTM from me as well, good job @TusharMohapatra07

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-02T08:53:06Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-02T09:20:17Z

/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-02T09:20:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3560#pullrequestreview-2469190560" title="Approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3560#issuecomment-2510984596" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3560#" title="Author self-approved">TusharMohapatra07</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [mimowo]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [mimowo]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T08:40:12Z

- **pkg/util/testing/wrappers.go:1307** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T08:40:11Z
```diff
@@ -1275,3 +1275,36 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = in
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) RetryStrategy(backoffLimitCount, backoffBaseSeconds, backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy = &kueue.ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To[int32](backoffLimitCount),
+		BackoffBaseSeconds: ptr.To[int32](backoffBaseSeconds),
+		BackoffMaxSeconds:  ptr.To[int32](backoffMaxSeconds),
```

  ```suggestion
  		BackoffLimitCount:  &backoffLimitCount,
  		BackoffBaseSeconds: &backoffBaseSeconds,
  		BackoffMaxSeconds:  &backoffMaxSeconds,
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T08:41:43Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:504** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T08:41:43Z
```diff
@@ -516,7 +501,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
-			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig, 2), 0)},
+			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig.DeepCopy(), 2), 0)},
```

  I imagine desired state would looks something like
  ```suggestion
  			configs:  []kueue.ProvisioningRequestConfig{*baseConfig.DeepCopy().BaseBackoff(2).RetryLimit(0)},
  ```

### Review by [@TusharMohapatra07](https://github.com/TusharMohapatra07) (commented) — 2024-11-26T12:35:18Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:504** by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-26T12:35:18Z
```diff
@@ -516,7 +501,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
-			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig, 2), 0)},
+			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig.DeepCopy(), 2), 0)},
```

  @PBundyra, So, the DeepCopy returns `ProvisioningRequestConfig` which doesnot have the `BaseBackoff` method on it. So, should i implement a `DeepCopy` for wrapper too ??
  ![image](https://github.com/user-attachments/assets/07ac3612-b3ff-472f-b335-6e1b7484d99e)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-26T12:44:22Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:504** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T12:44:21Z
```diff
@@ -516,7 +501,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
-			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig, 2), 0)},
+			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig.DeepCopy(), 2), 0)},
```

  Please provide Clone() method to duplicate wrapper.

### Review by [@TusharMohapatra07](https://github.com/TusharMohapatra07) (commented) — 2024-11-26T12:45:39Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:504** by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-26T12:45:39Z
```diff
@@ -516,7 +501,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
-			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig, 2), 0)},
+			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig.DeepCopy(), 2), 0)},
```

  @mbobrovskyi understood

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-26T12:46:06Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:504** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T12:46:06Z
```diff
@@ -516,7 +501,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
-			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig, 2), 0)},
+			configs:  []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig.DeepCopy(), 2), 0)},
```

  An example https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/testing/wrappers.go#L82-L84.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T06:46:34Z

- **pkg/util/testing/wrappers.go:1303** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T06:46:33Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = in
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResourses(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
```

  ```suggestion
  func (prc *ProvisioningRequestConfigWrapper) ManagedResources(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T06:49:54Z

- **pkg/util/testing/wrappers.go:1308** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T06:49:54Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = in
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResourses(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = r
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) RetryStrategy(backoffLimitCount, backoffBaseSeconds, backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
```

  ```suggestion
  func (prc *ProvisioningRequestConfigWrapper) RetryStrategy(retryStrategy *kueue.ProvisioningRequestRetryStrategy) *ProvisioningRequestConfigWrapper {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T06:52:17Z

- **pkg/util/testing/wrappers.go:1339** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T06:52:17Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = in
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResourses(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = r
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) RetryStrategy(backoffLimitCount, backoffBaseSeconds, backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy = &kueue.ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  &backoffLimitCount,
+		BackoffBaseSeconds: &backoffBaseSeconds,
+		BackoffMaxSeconds:  &backoffMaxSeconds,
+	}
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) BaseBackoff(backoffBaseSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy.BackoffBaseSeconds = &backoffBaseSeconds
```

  Check for nil `RetryStrategy` first. To avoid panic.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T06:53:06Z

- **pkg/util/testing/wrappers.go:1348** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T06:53:06Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = in
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResourses(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = r
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) RetryStrategy(backoffLimitCount, backoffBaseSeconds, backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy = &kueue.ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  &backoffLimitCount,
+		BackoffBaseSeconds: &backoffBaseSeconds,
+		BackoffMaxSeconds:  &backoffMaxSeconds,
+	}
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) BaseBackoff(backoffBaseSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy.BackoffBaseSeconds = &backoffBaseSeconds
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) MaxBackoff(backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy.BackoffMaxSeconds = &backoffMaxSeconds
```

  The same.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T06:53:16Z

- **pkg/util/testing/wrappers.go:1357** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T06:53:16Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = in
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResourses(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = r
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) RetryStrategy(backoffLimitCount, backoffBaseSeconds, backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy = &kueue.ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  &backoffLimitCount,
+		BackoffBaseSeconds: &backoffBaseSeconds,
+		BackoffMaxSeconds:  &backoffMaxSeconds,
+	}
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) BaseBackoff(backoffBaseSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy.BackoffBaseSeconds = &backoffBaseSeconds
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) MaxBackoff(backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy.BackoffMaxSeconds = &backoffMaxSeconds
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) RetryLimit(backoffLimitCount int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy.BackoffLimitCount = &backoffLimitCount
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T07:02:35Z

- **pkg/util/testing/wrappers.go:1298** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T06:58:20Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
```

  ```suggestion
  func (prc *ProvisioningRequestConfigWrapper) Parameters(parameters map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
  ```

- **pkg/util/testing/wrappers.go:1298** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T07:01:00Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
```

  Also I think it would be great to add method 
  
  ```go
  func (prc *ProvisioningRequestConfigWrapper) WithParameter(key string, value kueue.Parameter) *ProvisioningRequestConfigWrapper {
  ```
  
  and use it in places where we need only one parameter.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T07:03:49Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:257** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T07:03:49Z
```diff
@@ -264,22 +252,9 @@ func TestReconcile(t *testing.T) {
 		},
 	}
 
-	baseConfig := &kueue.ProvisioningRequestConfig{
-		ObjectMeta: metav1.ObjectMeta{
-			Name: "config1",
-		},
-		Spec: kueue.ProvisioningRequestConfigSpec{
-			ProvisioningClassName: "class1",
-			Parameters: map[string]kueue.Parameter{
-				"p1": "v1",
-			},
-			RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
-				BackoffLimitCount:  ptr.To[int32](3),
-				BackoffBaseSeconds: ptr.To[int32](60),
-				BackoffMaxSeconds:  ptr.To[int32](1800),
-			},
-		},
-	}
+	baseConfig := utiltesting.MakeProvisioningRequestConfig("config1").ProvisioningClass("class1").Parameters(map[string]kueue.Parameter{"p1": "v1"}).RetryStrategy(3, 60, 1800)
+
+	baseConfigWithoutRetryStrategy := utiltesting.MakeProvisioningRequestConfig("config1").ProvisioningClass("class1").Parameters(map[string]kueue.Parameter{"p1": "v1"})
```

  ```suggestion
  	baseConfig := utiltesting.MakeProvisioningRequestConfig("config1").ProvisioningClass("class1").Parameters(map[string]kueue.Parameter{"p1": "v1"})
  	baseConfigWithRetryStrategy := baseConfigWithoutRetryStrategy.Clone().RetryStrategy(3, 60, 1800)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T07:12:48Z

- **pkg/util/testing/wrappers.go:1339** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T07:12:48Z
```diff
@@ -1275,3 +1275,60 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(in map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = in
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResourses(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = r
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) RetryStrategy(backoffLimitCount, backoffBaseSeconds, backoffMaxSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy = &kueue.ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  &backoffLimitCount,
+		BackoffBaseSeconds: &backoffBaseSeconds,
+		BackoffMaxSeconds:  &backoffMaxSeconds,
+	}
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) BaseBackoff(backoffBaseSeconds int32) *ProvisioningRequestConfigWrapper {
+	prc.Spec.RetryStrategy.BackoffBaseSeconds = &backoffBaseSeconds
```

  If nil create empty RetryStrategy object.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T07:14:20Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:261** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T07:14:20Z
```diff
@@ -264,22 +252,13 @@ func TestReconcile(t *testing.T) {
 		},
 	}
 
-	baseConfig := &kueue.ProvisioningRequestConfig{
-		ObjectMeta: metav1.ObjectMeta{
-			Name: "config1",
-		},
-		Spec: kueue.ProvisioningRequestConfigSpec{
-			ProvisioningClassName: "class1",
-			Parameters: map[string]kueue.Parameter{
-				"p1": "v1",
-			},
-			RetryStrategy: &kueue.ProvisioningRequestRetryStrategy{
-				BackoffLimitCount:  ptr.To[int32](3),
-				BackoffBaseSeconds: ptr.To[int32](60),
-				BackoffMaxSeconds:  ptr.To[int32](1800),
-			},
-		},
-	}
+	baseConfigWithoutRetryStrategy := utiltesting.MakeProvisioningRequestConfig("config1").ProvisioningClass("class1").Parameters(map[string]kueue.Parameter{"p1": "v1"})
+
+	baseConfig := baseConfigWithoutRetryStrategy.Clone().RetryStrategy(&kueue.ProvisioningRequestRetryStrategy{
+		BackoffLimitCount:  ptr.To[int32](3),
+		BackoffBaseSeconds: ptr.To[int32](60),
+		BackoffMaxSeconds:  ptr.To[int32](1800),
+	})
```

  ```suggestion
  	baseConfig := utiltesting.MakeProvisioningRequestConfig("config1").ProvisioningClass("class1").Parameters(map[string]kueue.Parameter{"p1": "v1"})
  
  	baseConfigWithRetryStrategy := baseConfigWithoutRetryStrategy.Clone().RetryStrategy(&kueue.ProvisioningRequestRetryStrategy{
  		BackoffLimitCount:  ptr.To[int32](3),
  		BackoffBaseSeconds: ptr.To[int32](60),
  		BackoffMaxSeconds:  ptr.To[int32](1800),
  	})
  ```
  
  Sorry, I updated my comment. Maybe like this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T08:38:56Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:349** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T08:38:56Z
```diff
@@ -367,7 +346,7 @@ func TestReconcile(t *testing.T) {
 				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
 				Obj(),
 			checks:  []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
-			configs: []kueue.ProvisioningRequestConfig{{ObjectMeta: metav1.ObjectMeta{Name: "config1"}}},
+			configs: []kueue.ProvisioningRequestConfig{*utiltesting.MakeProvisioningRequestConfig("config1").DeepCopy()},
```

  It’s not necessary to DeepCopy the object here. We’re using this wrapper only once, so there’s no need to allocate another object in memory. I propose adding a method Obj() to the wrapper, like this:
  
  https://github.com/kubernetes-sigs/kueue/blob/37ba40854dedeabd498f7cc589336b33a434b51f/pkg/util/testingjobs/deployment/wrappers.go#L69-L71

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T10:27:56Z

- **pkg/util/testing/wrappers.go:1299** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T10:27:56Z
```diff
@@ -1275,3 +1275,77 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(parameters map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	prc.Spec.Parameters = parameters
```

  Check if the `prc.Spec.Parameters` are not nil and then iterate through `parameters` adding them one by one instead of replacing them

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T10:28:32Z

- **pkg/util/testing/wrappers.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T10:28:31Z
  Add `Obj()` method

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T10:29:53Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:1147** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T10:29:52Z
```diff
@@ -1201,7 +1144,7 @@ func TestReconcile(t *testing.T) {
 				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
 				Obj(),
 			checks:             []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
-			configs:            []kueue.ProvisioningRequestConfig{{ObjectMeta: metav1.ObjectMeta{Name: "config1"}}},
+			configs:            []kueue.ProvisioningRequestConfig{*utiltesting.MakeProvisioningRequestConfig("config1").DeepCopy()},
```

  ```suggestion
  			configs:            []kueue.ProvisioningRequestConfig{*utiltesting.MakeProvisioningRequestConfig("config1").Obj()},
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T08:53:21Z

- **pkg/util/testing/wrappers.go:1306** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T08:53:21Z
```diff
@@ -1296,7 +1296,14 @@ func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *Provi
 }
 
 func (prc *ProvisioningRequestConfigWrapper) Parameters(parameters map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
-	prc.Spec.Parameters = parameters
+	if prc.Spec.Parameters != nil {
+		for key, value := range parameters {
+			prc.Spec.Parameters[key] = value
+		}
+	} else {
+		prc.Spec.Parameters = parameters
+	}
+
```

  ```suggestion
  	if prc.Spec.Parameters == nil {
  		prc.Spec.Parameters = make(map[string]kueue.Parameter, len(parameters))
  	} 
  	for key, value := range parameters {
  		prc.Spec.Parameters[key] = value
  	}
  
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T09:02:48Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:631** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T09:02:47Z
```diff
@@ -661,19 +628,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
-			configs: []kueue.ProvisioningRequestConfig{
-				{ObjectMeta: metav1.ObjectMeta{
-					Name: "config1",
-				},
-					Spec: kueue.ProvisioningRequestConfigSpec{
-						ProvisioningClassName: "class1",
-						Parameters: map[string]kueue.Parameter{
-							"p1": "v1",
-						},
-						ManagedResources: []corev1.ResourceName{corev1.ResourceMemory},
-					},
-				},
-			},
+			configs:  []kueue.ProvisioningRequestConfig{*baseConfig.WithManagedResource(corev1.ResourceMemory).DeepCopy()},
```

  ```suggestion
  			configs:  []kueue.ProvisioningRequestConfig{baseConfig.Clone().WithManagedResource(corev1.ResourceMemory).Obj()},
  ```
  Please follow this pattern with the rest of changes

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T09:04:19Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:971** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T09:04:19Z
```diff
@@ -1025,7 +968,7 @@ func TestReconcile(t *testing.T) {
 				Obj(),
 			checks:      []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			flavors:     []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
-			configs:     []kueue.ProvisioningRequestConfig{*provReqConfigWithBaseBackoff(provReqConfigWithRetryLimit(baseConfig, 1), 0)},
+			configs:     []kueue.ProvisioningRequestConfig{*baseConfigWithRetryStrategy.Clone().RetryLimit(1).BaseBackoff(0).DeepCopy()},
```

  ```suggestion
  			configs:     []kueue.ProvisioningRequestConfig{baseConfigWithRetryStrategy.Clone().RetryLimit(1).BaseBackoff(0).Obj()},
  ```
  Please follow this patter with the rest of changes

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T09:07:28Z

- **pkg/util/testing/wrappers.go:1317** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T09:07:28Z
```diff
@@ -1275,3 +1275,93 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(parameters map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters != nil {
+		for key, value := range parameters {
+			prc.Spec.Parameters[key] = value
+		}
+	} else {
+		prc.Spec.Parameters = parameters
+	}
+
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) WithParameter(key string, value kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters == nil {
+		prc.Spec.Parameters = make(map[string]kueue.Parameter, 1)
+	}
+
+	prc.Spec.Parameters[key] = value
+	return prc
+}
```

  I believe one function to set Parameters would be enough. Can we stick with `Parameters()` only?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T09:09:31Z

- **pkg/util/testing/wrappers.go:1327** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T09:09:31Z
```diff
@@ -1275,3 +1275,93 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(parameters map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters != nil {
+		for key, value := range parameters {
+			prc.Spec.Parameters[key] = value
+		}
+	} else {
+		prc.Spec.Parameters = parameters
+	}
+
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) WithParameter(key string, value kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters == nil {
+		prc.Spec.Parameters = make(map[string]kueue.Parameter, 1)
+	}
+
+	prc.Spec.Parameters[key] = value
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResources(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = r
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) WithManagedResource(managedResource corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = append(prc.Spec.ManagedResources, managedResource)
+	return prc
+}
```

  I believe one function to set `ManagedResources` is enough. Can we have a function that creates ManagedResource if it's nil and then append all elements, similarly to `Parameters()`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-28T09:10:05Z

- **pkg/util/testing/wrappers.go:1317** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-28T09:10:05Z
```diff
@@ -1275,3 +1275,93 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(parameters map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters != nil {
+		for key, value := range parameters {
+			prc.Spec.Parameters[key] = value
+		}
+	} else {
+		prc.Spec.Parameters = parameters
+	}
+
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) WithParameter(key string, value kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters == nil {
+		prc.Spec.Parameters = make(map[string]kueue.Parameter, 1)
+	}
+
+	prc.Spec.Parameters[key] = value
+	return prc
+}
```

  I think it’s simpler to set a single key-value pair rather than creating a map with just one key-value.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-28T09:10:34Z

- **pkg/util/testing/wrappers.go:1327** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-28T09:10:34Z
```diff
@@ -1275,3 +1275,93 @@ func (c *ContainerWrapper) AsSidecar() *ContainerWrapper {
 
 	return c
 }
+
+// ProvisioningRequestConfigWrapper wraps a ProvisioningRequestConfig
+type ProvisioningRequestConfigWrapper struct {
+	kueue.ProvisioningRequestConfig
+}
+
+// MakeProvisioningRequestConfig creates a wrapper for a ProvisioningRequestConfig.
+func MakeProvisioningRequestConfig(name string) *ProvisioningRequestConfigWrapper {
+	return &ProvisioningRequestConfigWrapper{kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ProvisioningClass(pc string) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ProvisioningClassName = pc
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) Parameters(parameters map[string]kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters != nil {
+		for key, value := range parameters {
+			prc.Spec.Parameters[key] = value
+		}
+	} else {
+		prc.Spec.Parameters = parameters
+	}
+
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) WithParameter(key string, value kueue.Parameter) *ProvisioningRequestConfigWrapper {
+	if prc.Spec.Parameters == nil {
+		prc.Spec.Parameters = make(map[string]kueue.Parameter, 1)
+	}
+
+	prc.Spec.Parameters[key] = value
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) ManagedResources(r []corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = r
+	return prc
+}
+
+func (prc *ProvisioningRequestConfigWrapper) WithManagedResource(managedResource corev1.ResourceName) *ProvisioningRequestConfigWrapper {
+	prc.Spec.ManagedResources = append(prc.Spec.ManagedResources, managedResource)
+	return prc
+}
```

  Here the same

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2024-11-29T06:49:28Z

LGTM

- **pkg/util/admissioncheck/admissioncheck_test.go:35** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-29T06:40:07Z
```diff
@@ -34,18 +32,7 @@ import (
 )
 
 func TestConfigHelper(t *testing.T) {
-	testConfig := &kueue.ProvisioningRequestConfig{
-		ObjectMeta: metav1.ObjectMeta{
-			Name: "config",
-		},
-		Spec: kueue.ProvisioningRequestConfigSpec{
-			ProvisioningClassName: "className",
-			Parameters: map[string]kueue.Parameter{
-				"p1": "v1",
-			},
-			ManagedResources: []corev1.ResourceName{"cpu"},
-		},
-	}
+	testConfig := utiltesting.MakeProvisioningRequestConfig("config").ProvisioningClass("className").WithParameter("p1", "v1").WithManagedResource("cpu").Obj()
```

  ```suggestion
  	testConfig := utiltesting.MakeProvisioningRequestConfig("config").
  		ProvisioningClass("className").
  		WithParameter("p1", "v1").
  		WithManagedResource("cpu")
  ```
  
  nit: just for readability

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-29T06:57:33Z

- **pkg/util/admissioncheck/admissioncheck_test.go:39** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-29T06:57:33Z
```diff
@@ -34,18 +32,11 @@ import (
 )
 
 func TestConfigHelper(t *testing.T) {
-	testConfig := &kueue.ProvisioningRequestConfig{
-		ObjectMeta: metav1.ObjectMeta{
-			Name: "config",
-		},
-		Spec: kueue.ProvisioningRequestConfigSpec{
-			ProvisioningClassName: "className",
-			Parameters: map[string]kueue.Parameter{
-				"p1": "v1",
-			},
-			ManagedResources: []corev1.ResourceName{"cpu"},
-		},
-	}
+	testConfig := utiltesting.MakeProvisioningRequestConfig("config").
+		ProvisioningClass("className").
+		WithParameter("p1", "v1").
+		WithManagedResource("cpu").
+		Obj()
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-29T06:57:45Z

- **pkg/util/admissioncheck/admissioncheck_test.go:39** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-29T06:57:45Z
```diff
@@ -34,18 +32,11 @@ import (
 )
 
 func TestConfigHelper(t *testing.T) {
-	testConfig := &kueue.ProvisioningRequestConfig{
-		ObjectMeta: metav1.ObjectMeta{
-			Name: "config",
-		},
-		Spec: kueue.ProvisioningRequestConfigSpec{
-			ProvisioningClassName: "className",
-			Parameters: map[string]kueue.Parameter{
-				"p1": "v1",
-			},
-			ManagedResources: []corev1.ResourceName{"cpu"},
-		},
-	}
+	testConfig := utiltesting.MakeProvisioningRequestConfig("config").
+		ProvisioningClass("className").
+		WithParameter("p1", "v1").
+		WithManagedResource("cpu").
+		Obj()
```

  I think this is no need, correct?

### Review by [@TusharMohapatra07](https://github.com/TusharMohapatra07) (commented) — 2024-11-29T06:59:34Z

- **pkg/util/admissioncheck/admissioncheck_test.go:39** by [@TusharMohapatra07](https://github.com/TusharMohapatra07) — 2024-11-29T06:59:34Z
```diff
@@ -34,18 +32,11 @@ import (
 )
 
 func TestConfigHelper(t *testing.T) {
-	testConfig := &kueue.ProvisioningRequestConfig{
-		ObjectMeta: metav1.ObjectMeta{
-			Name: "config",
-		},
-		Spec: kueue.ProvisioningRequestConfigSpec{
-			ProvisioningClassName: "className",
-			Parameters: map[string]kueue.Parameter{
-				"p1": "v1",
-			},
-			ManagedResources: []corev1.ResourceName{"cpu"},
-		},
-	}
+	testConfig := utiltesting.MakeProvisioningRequestConfig("config").
+		ProvisioningClass("className").
+		WithParameter("p1", "v1").
+		WithManagedResource("cpu").
+		Obj()
```

  yaa true, my bad!
