# PR #36: Add queuedWorkload created event to batch/job

**Summary**: Add queuedWorkload created event to batch/job

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/36

**Last updated**: 2022-02-23T18:34:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@denkensk](https://github.com/denkensk)
- **Branch**: `add-create-event-qw` → `main`
- **Created**: 2022-02-19T03:32:14Z
- **Updated**: 2022-02-23T18:34:20Z
- **Merged**: 2022-02-23T18:34:20Z
- **Closed**: 2022-02-23T18:34:20Z
- **Labels**: `kind/feature`, `lgtm`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: _none_
- **Changed files**: 6   **+77 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature
#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9

#### Special notes for your reviewer:
```bash
Name:           sample-job-4q87f
Namespace:      default
Selector:       controller-uid=6576a66d-dd4a-48e9-ba92-97d7db1ea9cd
Labels:         controller-uid=6576a66d-dd4a-48e9-ba92-97d7db1ea9cd
                job-name=sample-job-4q87f
Annotations:    kueue.x-k8s.io/queue-name: main
Parallelism:    3
Completions:    3
Start Time:     Sat, 19 Feb 2022 11:23:46 +0800
Completed At:   Sat, 19 Feb 2022 11:23:57 +0800
Duration:       11s
Pods Statuses:  0 Running / 3 Succeeded / 0 Failed
Pod Template:
  Labels:  controller-uid=6576a66d-dd4a-48e9-ba92-97d7db1ea9cd
           job-name=sample-job-4q87f
  Containers:
   pi:
    Image:      perl
    Port:       <none>
    Host Port:  <none>
    Command:
      perl
      -Mbignum=bpi
      -wle
      print bpi(2000)
    Limits:
      cpu:     1
      memory:  200Mi
    Requests:
      cpu:        1
      memory:     200Mi
    Environment:  <none>
    Mounts:       <none>
  Volumes:        <none>
Events:
  Type    Reason                 Age   From                  Message
  ----    ------                 ----  ----                  -------
  Normal  Suspended              4s    job-controller        Job suspended
  Normal  CreatedQueuedWorkload  4s    kueue-job-controller  Created QueuedWorkload: default/sample-job-8bqnh
  Normal  Started                4s    kueue-job-controller  Assigned to capacity cluster-total
  Normal  SuccessfulCreate       4s    job-controller        Created pod: sample-job-8bqnh--1-7c8rc
  Normal  SuccessfulCreate       4s    job-controller        Created pod: sample-job-8bqnh--1-898nb
  Normal  SuccessfulCreate       4s    job-controller        Created pod: sample-job-8bqnh--1-vz4gp
  Normal  Resumed                4s    job-controller        Job resumed
```

## Discussion

### Comment by [@denkensk](https://github.com/denkensk) — 2022-02-19T13:56:51Z

> lets also add the following events:
> 
> 1. When starting the job
> 2. When stopping the job
> 3. When deleting queued workloads (line 350)

When starting job is the suspend action ? （If so, it is also in the job event）
When stopping the job is the unsuspend action?

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-19T14:10:02Z

Starting the job event would indicate what capacity it was assigned to, and so users looking only at the job events can see all related information, they don't really need to look at the corresponding QW events.

Stoping the job event should include the reason why it was stopped, there is more than one reason the controller is stopping the job.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-19T19:23:36Z

when you rebase, can you check if you can do `/ok-to-test` yourself?

### Comment by [@denkensk](https://github.com/denkensk) — 2022-02-20T11:56:02Z

/ok-to-test

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T17:59:41Z

> I need to refactor this a bit so that stopping a job for non-matching queued workload is done in one place.

https://github.com/kubernetes-sigs/kueue/pull/42

### Comment by [@denkensk](https://github.com/denkensk) — 2022-02-22T08:27:40Z

/retest

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-23T14:16:26Z

/approve

leaving lgtm to Aldo if he feel strongly about the message format.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-02-23T14:16:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/36#issuecomment-1048825964" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/36#" title="Author self-approved">denkensk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-23T14:31:02Z

BTW, I'm fine with the format. We can always re-iterate.

### Comment by [@denkensk](https://github.com/denkensk) — 2022-02-23T16:34:39Z

> Can you refactor into a function that can check any type of event?
> Maybe you can have it pkg/util/testing.

Done.

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-19T13:02:05Z

lets also add the following events:
1) When starting the job
2) When stopping the job
3) When deleting queued workloads (line 350)

- **pkg/controller/workload/job/job_controller.go:302** by [@ahg-g](https://github.com/ahg-g) — 2022-02-19T12:53:43Z
```diff
@@ -293,7 +297,11 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	if err != nil {
 		return err
 	}
-	return r.client.Create(ctx, wl)
+	err = r.client.Create(ctx, wl)
+	if err == nil {
+		r.record.Eventf(job, corev1.EventTypeNormal, "Created", fmt.Sprintf("Successfully created queuedWorkload %v", workload.Key(wl)))
```

  just to mimic the create pod events, lets do `Created queuedWorkload: %v`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-19T15:37:40Z

- **pkg/controller/workload/job/job_controller.go:302** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-19T15:34:17Z
```diff
@@ -293,7 +297,11 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	if err != nil {
 		return err
 	}
-	return r.client.Create(ctx, wl)
+	err = r.client.Create(ctx, wl)
+	if err == nil {
+		r.record.Eventf(job, corev1.EventTypeNormal, "Created", fmt.Sprintf("Successfully created queuedWorkload %v", workload.Key(wl)))
```

  No need Sprintf when using Eventf

- **test/integration/controller/job/job_controller_test.go:59** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-19T15:37:01Z
```diff
@@ -55,12 +56,14 @@ var (
 
 var _ = ginkgo.Describe("Job controller", func() {
 	ginkgo.BeforeEach(func() {
+		scheme := scheme.Scheme
```

  don't we need to Add the kueue scheme?

- **test/integration/controller/job/job_controller_test.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-19T15:37:33Z
```diff
@@ -55,12 +56,14 @@ var (
 
 var _ = ginkgo.Describe("Job controller", func() {
 	ginkgo.BeforeEach(func() {
+		scheme := scheme.Scheme
 		mgr, err := ctrl.NewManager(cfg, ctrl.Options{
-			Scheme: scheme.Scheme,
+			Scheme: scheme,
 		})
 		gomega.Expect(err).ToNot(gomega.HaveOccurred(), "failed to create manager")
-
-		err = workloadjob.NewReconciler(mgr.GetScheme(), mgr.GetClient()).SetupWithManager(mgr)
+		broadcaster := record.NewBroadcaster()
+		recorder := broadcaster.NewRecorder(scheme, corev1.EventSource{Component: constants.JobControllerName})
```

  Can you test that the event is broadcasted?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-21T12:04:28Z

- **pkg/controller/workload/job/job_controller.go:302** by [@denkensk](https://github.com/denkensk) — 2022-02-21T12:04:27Z
```diff
@@ -293,7 +297,11 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	if err != nil {
 		return err
 	}
-	return r.client.Create(ctx, wl)
+	err = r.client.Create(ctx, wl)
+	if err == nil {
+		r.record.Eventf(job, corev1.EventTypeNormal, "Created", fmt.Sprintf("Successfully created queuedWorkload %v", workload.Key(wl)))
```

  fixed

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-21T16:24:08Z

I need to refactor this a bit so that stopping a job for non-matching queued workload is done in one place.

- **pkg/controller/workload/job/job_controller.go:184** by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T14:59:36Z
```diff
@@ -176,6 +180,8 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		if err != nil {
 			log.Error(err, "Suspending job with unassigned workload")
 		}
+		r.record.Eventf(&job, corev1.EventTypeNormal, "Stopped",
+			"Stopped due to no capacity assigned")
```

  ```suggestion
  			"No capacity assigned")
  ```

- **pkg/controller/workload/job/job_controller.go:256** by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T15:02:25Z
```diff
@@ -242,7 +248,12 @@ func (r *JobReconciler) startJob(ctx context.Context, w *kueue.QueuedWorkload, j
 	}
 
 	job.Spec.Suspend = pointer.BoolPtr(false)
-	return r.client.Update(ctx, job)
+	err := r.client.Update(ctx, job)
+	if err == nil {
+		r.record.Eventf(job, corev1.EventTypeNormal, "Started",
+			"Assigned to capacity %v", workload.Key(w))
+	}
+	return err
```

  ```suggestion
  	if err := r.client.Update(ctx, job); err != nil {
  	  return err
  	}
  	r.record.Eventf(job, corev1.EventTypeNormal, "Started",
  			"Assigned to capacity %v", workload.Key(w))
  	return nil
  ```

- **pkg/controller/workload/job/job_controller.go:297** by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T15:03:23Z
```diff
@@ -279,7 +290,11 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	// If the job is running, suspend it.
 	if !jobSuspended(job) {
 		log.V(2).Info("Job running with no corresponding workload object, suspending")
-		return r.stopJob(ctx, nil, job)
+		if err := r.stopJob(ctx, nil, job); err != nil {
+			return err
+		}
+		r.record.Eventf(job, corev1.EventTypeNormal, "Stopped",
+			"Stopped due to no queuedWorkload set")
```

  ```suggestion
  			"No matching QueuedWorkload")
  ```

- **pkg/controller/workload/job/job_controller.go:316** by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T15:06:06Z
```diff
@@ -293,7 +308,12 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	if err != nil {
 		return err
 	}
-	return r.client.Create(ctx, wl)
+	err = r.client.Create(ctx, wl)
+	if err == nil {
+		r.record.Eventf(job, corev1.EventTypeNormal, "Created",
+			"Created queuedWorkload: %v", workload.Key(wl))
+	}
+	return err
```

  ```suggestion
  	if err = r.client.Create(ctx, wl); err != nil {
  	    return err
  	}
  	r.record.Eventf(job, corev1.EventTypeNormal, "Created",
  		"Created QueuedWorkload: %v", workload.Key(wl))
  	return nil
  ```

- **pkg/controller/workload/job/job_controller.go:369** by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T16:21:42Z
```diff
@@ -334,13 +354,19 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 		}
 		if err := r.stopJob(ctx, w, job); err != nil {
 			log.Error(err, "stopping job")
+		} else {
+			r.record.Eventf(job, corev1.EventTypeNormal, "Stopped",
+				"Stopped due to no queuedWorkload matched")
 		}
 	}
 
 	// Delete redundant workload instances.
 	for i := range toDelete {
 		if err := r.client.Delete(ctx, toDelete[i]); err != nil {
 			log.Error(err, "Failed to delete workload")
+		} else {
+			r.record.Eventf(job, corev1.EventTypeNormal, "Deleted",
+				"Deleted queuedWorkload: %v", workload.Key(toDelete[i]))
```

  ```suggestion
  				"Deleted not matching QueuedWorkload: %v", workload.Key(toDelete[i]))
  ```

- **pkg/controller/workload/job/job_controller.go:359** by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T16:23:21Z
```diff
@@ -334,13 +354,19 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 		}
 		if err := r.stopJob(ctx, w, job); err != nil {
 			log.Error(err, "stopping job")
+		} else {
+			r.record.Eventf(job, corev1.EventTypeNormal, "Stopped",
+				"Stopped due to no queuedWorkload matched")
```

  ```suggestion
  				"No matching QueuedWorkload")
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-21T18:01:26Z

- **pkg/controller/workload/job/job_controller.go:293** by [@ahg-g](https://github.com/ahg-g) — 2022-02-21T18:01:26Z
```diff
@@ -279,7 +290,11 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	// If the job is running, suspend it.
 	if !jobSuspended(job) {
 		log.V(2).Info("Job running with no corresponding workload object, suspending")
-		return r.stopJob(ctx, nil, job)
+		if err := r.stopJob(ctx, nil, job); err != nil {
```

  lets pass the event message to stopjob and have the record.Eventf called inside stopjob

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-22T03:21:13Z

- **pkg/controller/workload/job/job_controller.go:293** by [@denkensk](https://github.com/denkensk) — 2022-02-22T03:21:13Z
```diff
@@ -279,7 +290,11 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	// If the job is running, suspend it.
 	if !jobSuspended(job) {
 		log.V(2).Info("Job running with no corresponding workload object, suspending")
-		return r.stopJob(ctx, nil, job)
+		if err := r.stopJob(ctx, nil, job); err != nil {
```

  Rebased and addressed

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-22T03:34:29Z

- **test/integration/controller/job/job_controller_test.go:67** by [@ahg-g](https://github.com/ahg-g) — 2022-02-22T03:34:29Z
```diff
@@ -63,7 +64,7 @@ var _ = ginkgo.Describe("Job controller", func() {
 		})
 		gomega.Expect(err).NotTo(gomega.HaveOccurred(), "failed to create manager")
 
-		err = workloadjob.NewReconciler(mgr.GetScheme(), mgr.GetClient()).SetupWithManager(mgr)
+		err = workloadjob.NewReconciler(mgr.GetScheme(), mgr.GetClient(), &record.FakeRecorder{}).SetupWithManager(mgr)
```

  we can do: `mgr.GetEventRecorderFor(constants.JobControllerName)`
  
  if possible, can you check the events are produced throughout the test? not add a new test, just add a check that the event is produced, I think the test covers all cases where an event is supposed to be generated.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-22T04:23:43Z

- **test/integration/controller/job/job_controller_test.go:67** by [@denkensk](https://github.com/denkensk) — 2022-02-22T04:23:43Z
```diff
@@ -63,7 +64,7 @@ var _ = ginkgo.Describe("Job controller", func() {
 		})
 		gomega.Expect(err).NotTo(gomega.HaveOccurred(), "failed to create manager")
 
-		err = workloadjob.NewReconciler(mgr.GetScheme(), mgr.GetClient()).SetupWithManager(mgr)
+		err = workloadjob.NewReconciler(mgr.GetScheme(), mgr.GetClient(), &record.FakeRecorder{}).SetupWithManager(mgr)
```

  sg, I will add these later.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-22T18:48:45Z

- **pkg/controller/workload/job/job_controller.go:307** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-22T18:45:50Z
```diff
@@ -288,7 +300,13 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job *batchv
 	if err != nil {
 		return err
 	}
-	return r.client.Create(ctx, wl)
+	if err = r.client.Create(ctx, wl); err != nil {
+		return err
+	}
+
+	r.record.Eventf(job, corev1.EventTypeNormal, "Created",
```

  ```suggestion
  	r.record.Eventf(job, corev1.EventTypeNormal, "QueuedWorkloadCreated",
  ```

- **pkg/controller/workload/job/job_controller.go:344** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-22T18:47:16Z
```diff
@@ -323,7 +341,7 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 			// than one workload...
 			w = &workloads.Items[0]
 		}
-		if err := r.stopJob(ctx, w, job); err != nil {
+		if err := r.stopJob(ctx, w, job, "No matching QueuedWorkload"); err != nil {
```

  The messages should be a full sentence: Stopped Job because there was no matching QueuedWorkload

- **pkg/controller/workload/job/job_controller.go:360** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-22T18:47:50Z
```diff
@@ -338,6 +356,10 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 		if err != nil && !apierrors.IsNotFound(err) {
 			log.Error(err, "Failed to delete workload")
 		}
+		if err == nil {
+			r.record.Eventf(job, corev1.EventTypeNormal, "Deleted",
```

  ```suggestion
  			r.record.Eventf(job, corev1.EventTypeNormal, "DeletedQueuedWorkload",
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-22T18:54:26Z

- **pkg/controller/workload/job/job_controller.go:344** by [@ahg-g](https://github.com/ahg-g) — 2022-02-22T18:54:26Z
```diff
@@ -323,7 +341,7 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 			// than one workload...
 			w = &workloads.Items[0]
 		}
-		if err := r.stopJob(ctx, w, job); err != nil {
+		if err := r.stopJob(ctx, w, job, "No matching QueuedWorkload"); err != nil {
```

  Sounds redundant, the reason is "Stopped", why replicate that again if the context is clear?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-22T18:58:00Z

- **pkg/controller/workload/job/job_controller.go:344** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-22T18:58:00Z
```diff
@@ -323,7 +341,7 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 			// than one workload...
 			w = &workloads.Items[0]
 		}
-		if err := r.stopJob(ctx, w, job); err != nil {
+		if err := r.stopJob(ctx, w, job, "No matching QueuedWorkload"); err != nil {
```

  For consistency with how we do events in k8s

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-22T18:59:21Z

- **pkg/controller/workload/job/job_controller.go:344** by [@ahg-g](https://github.com/ahg-g) — 2022-02-22T18:59:21Z
```diff
@@ -323,7 +341,7 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 			// than one workload...
 			w = &workloads.Items[0]
 		}
-		if err := r.stopJob(ctx, w, job); err != nil {
+		if err := r.stopJob(ctx, w, job, "No matching QueuedWorkload"); err != nil {
```

  messages in k8s seems quite adhoc to me :)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-22T19:01:12Z

- **pkg/controller/workload/job/job_controller.go:344** by [@ahg-g](https://github.com/ahg-g) — 2022-02-22T19:01:12Z
```diff
@@ -323,7 +341,7 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 			// than one workload...
 			w = &workloads.Items[0]
 		}
-		if err := r.stopJob(ctx, w, job); err != nil {
+		if err := r.stopJob(ctx, w, job, "No matching QueuedWorkload"); err != nil {
```

  feels easier to read when we have concise and to the point messages.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-23T07:25:55Z

- **test/integration/controller/job/job_controller_test.go:67** by [@denkensk](https://github.com/denkensk) — 2022-02-23T07:25:55Z
```diff
@@ -63,7 +64,7 @@ var _ = ginkgo.Describe("Job controller", func() {
 		})
 		gomega.Expect(err).NotTo(gomega.HaveOccurred(), "failed to create manager")
 
-		err = workloadjob.NewReconciler(mgr.GetScheme(), mgr.GetClient()).SetupWithManager(mgr)
+		err = workloadjob.NewReconciler(mgr.GetScheme(), mgr.GetClient(), &record.FakeRecorder{}).SetupWithManager(mgr)
```

  Added. I separate them into two commits.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-23T14:14:53Z

- **test/integration/controller/job/job_controller_test.go:28** by [@ahg-g](https://github.com/ahg-g) — 2022-02-23T14:14:53Z
```diff
@@ -18,29 +18,33 @@ package job
 
 import (
 	"context"
+	"fmt"
 	"time"
 
 	"github.com/onsi/ginkgo"
 	"github.com/onsi/gomega"
 	batchv1 "k8s.io/api/batch/v1"
 	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/events/v1"
```

  nit: eventsv1

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-23T14:29:13Z

- **test/integration/controller/job/job_controller_test.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-23T14:29:13Z
```diff
@@ -127,6 +131,26 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		events := &v1.EventList{}
+		gomega.Eventually(func() bool {
```

  This doesn't seem correct.
  
  Is empty list a failure? Even if it's not, just waiting for List to succeed is not a guarantee that the list will have the event we are looking for. Specially on the 2nd check (line 171).
  
  The checking of the list should be inside the `gomega.Eventually` call.
  
  An alternate implementation is using watchers, similar to https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/scheduler_test.go#L650-L667

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-23T14:36:48Z

- **test/integration/controller/job/job_controller_test.go:132** by [@denkensk](https://github.com/denkensk) — 2022-02-23T14:36:48Z
```diff
@@ -127,6 +131,26 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		events := &v1.EventList{}
+		gomega.Eventually(func() bool {
```

  Sorry. I am not sure if I understand correctly. I check the event in L141 to make sure it’s what we want.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-23T15:15:04Z

- **test/integration/controller/job/job_controller_test.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-23T15:15:04Z
```diff
@@ -127,6 +131,26 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		events := &v1.EventList{}
+		gomega.Eventually(func() bool {
```

  Yes, but if it's not satisfied, you should reload list again. There is no guarantee that by the time you call List, the event that you want is already there.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-23T15:22:19Z

- **test/integration/controller/job/job_controller_test.go:132** by [@ahg-g](https://github.com/ahg-g) — 2022-02-23T15:22:19Z
```diff
@@ -127,6 +131,26 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		events := &v1.EventList{}
+		gomega.Eventually(func() bool {
```

  right, you need to move the logic in the Expect function below here.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-23T15:24:31Z

- **test/integration/controller/job/job_controller_test.go:173** by [@ahg-g](https://github.com/ahg-g) — 2022-02-23T15:24:31Z
```diff
@@ -144,6 +168,26 @@ var _ = ginkgo.Describe("Job controller", func() {
 			}
 			return !*createdJob.Spec.Suspend
 		}, timeout, interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+				return false
+			}
+			return true
+		}, timeout, interval).Should(gomega.BeTrue())
+		gomega.Expect(func() bool {
+			length := len(events.Items)
```

  same here

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-23T15:56:35Z

- **test/integration/controller/job/job_controller_test.go:28** by [@denkensk](https://github.com/denkensk) — 2022-02-23T15:56:35Z
```diff
@@ -18,29 +18,33 @@ package job
 
 import (
 	"context"
+	"fmt"
 	"time"
 
 	"github.com/onsi/ginkgo"
 	"github.com/onsi/gomega"
 	batchv1 "k8s.io/api/batch/v1"
 	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/events/v1"
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-23T15:56:53Z

- **test/integration/controller/job/job_controller_test.go:132** by [@denkensk](https://github.com/denkensk) — 2022-02-23T15:56:53Z
```diff
@@ -127,6 +131,26 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		events := &v1.EventList{}
+		gomega.Eventually(func() bool {
```

  Thanks. Got it. Fixed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-23T16:00:01Z

- **test/integration/controller/job/job_controller_test.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-23T15:58:04Z
```diff
@@ -127,6 +131,23 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			events := &eventsv1.EventList{}
+			if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+				return false
+			}
+			length := len(events.Items)
+			if length == 0 {
+				return false
+			}
+			item := events.Items[length-1]
+			if item.Reason == "DeletedQueuedWorkload" && item.Type == corev1.
+				EventTypeNormal && item.Note == fmt.Sprintf(
```

  nit: I'm fine not testing the Note. It's too specific.

- **test/integration/controller/job/job_controller_test.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-23T15:59:58Z
```diff
@@ -127,6 +131,26 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		events := &v1.EventList{}
+		gomega.Eventually(func() bool {
```

  Can you refactor into a function that can check any type of event?
  
  Maybe you can have it pkg/util/testing.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-23T16:28:30Z

- **test/integration/controller/job/job_controller_test.go:145** by [@denkensk](https://github.com/denkensk) — 2022-02-23T16:28:30Z
```diff
@@ -127,6 +131,23 @@ var _ = ginkgo.Describe("Job controller", func() {
 			err := k8sClient.Get(ctx, lookupKey, createdWorkload)
 			return err == nil
 		}, consistentDuration, interval).Should(gomega.BeTrue())
+		gomega.Eventually(func() bool {
+			events := &eventsv1.EventList{}
+			if err := k8sClient.List(ctx, events, &client.ListOptions{}); err != nil {
+				return false
+			}
+			length := len(events.Items)
+			if length == 0 {
+				return false
+			}
+			item := events.Items[length-1]
+			if item.Reason == "DeletedQueuedWorkload" && item.Type == corev1.
+				EventTypeNormal && item.Note == fmt.Sprintf(
```

  It is better to compare notes here to ensure the accuracy of the message.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-23T18:33:44Z

/lgtm
