# PR #3615: TAS: Update cache on delete Topology.

**Summary**: TAS: Update cache on delete Topology.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3615

**Last updated**: 2024-11-26T14:11:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/tas-updates-or-deleted-of-topology` → `main`
- **Created**: 2024-11-22T13:34:54Z
- **Updated**: 2024-11-26T14:11:35Z
- **Merged**: 2024-11-26T11:18:58Z
- **Closed**: 2024-11-26T11:18:58Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 4   **+126 / -9**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fixed bug that doesn't allow to update cache on delete Topology.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3614

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Fixed bug that doesn't allow to update cache on delete Topology.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-22T13:34:56Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-22T13:35:07Z

/test all

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-22T13:35:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3c1aa990df23948076b8e605d5580a06a81b08ba |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6745a7140fa41400086f6cb1 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-25T08:35:59Z

@mbobrovskyi please split this PR into two: for delete and update. I think for delete we are almost good to go, for update I need to rethink if we need this complication. And I'm not sure about race conditions for the workload usage, so maybe we just block updating.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-25T08:41:21Z

Then, we could cherry-pick probably handling of delete. 

As for updates I think it is better to block updates for now to reflect the status quo that updates are not supported. Then, we could support updates and relax validation when we are confident about the solution.

So I propose:
1. minimal PR fix for deletes (can be cherry-picked cause deleting is broken anyway)
2. PR to block topology updates for .spec.levels field by validation on the topology API (can be considered for cherry-picking, because updating is broken anyway)
3. PR to relax validation for the `spec.levels` field to allow validation when we are confident the solution works (would be a new feature)

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-25T09:09:11Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-25T10:26:12Z

/retest

Due to #3626

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T07:41:29Z

/test pull-kueue-test-integration-main 

1/5

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-26T08:20:19Z

/test pull-kueue-test-integration-main

2/5

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-26T08:43:34Z

/test pull-kueue-test-integration-main

3/5

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T09:00:40Z

/test pull-kueue-test-integration-main

4/5

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T09:15:45Z

/test pull-kueue-test-integration-main

5/5

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-26T10:51:50Z

LGTM label has been added.  <details>Git tree hash: 4a3c79fd9b4b895e4e23eea39130ce4ce232e5ee</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-26T10:51:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3615#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3615#pullrequestreview-2461105328" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:11:35Z

/cherry-pick release 0.9

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-11-26T11:11:37Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3615#issuecomment-2500330669):

>/cherry-pick release 0.9


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-11-26T11:19:34Z

@mimowo: cannot checkout `release`: error checking out "release": exit status 1 error: pathspec 'release' did not match any file(s) known to git


<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3615#issuecomment-2500330669):

>/cherry-pick release 0.9


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:21:42Z

/cherry-pick release-0.9

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-11-26T11:22:17Z

@mimowo: #3615 failed to apply on top of branch "release-0.9":
```
Applying: TAS: Update cache on delete Topology.
Using index info to reconstruct a base tree...
M	pkg/cache/tas_flavor.go
M	pkg/controller/tas/resource_flavor.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/tas/resource_flavor.go
CONFLICT (content): Merge conflict in pkg/controller/tas/resource_flavor.go
Auto-merging pkg/cache/tas_flavor.go
CONFLICT (content): Merge conflict in pkg/cache/tas_flavor.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 TAS: Update cache on delete Topology.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3615#issuecomment-2500370726):

>/cherry-pick release-0.9


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:37:46Z

@mbobrovskyi can you check what are the conflicts and prepare manual cherry-pick if nothing serious?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T14:11:33Z

> @mbobrovskyi can you check what are the conflicts and prepare manual cherry-pick if nothing serious?

We should cherry-pick https://github.com/kubernetes-sigs/kueue/pull/3509 first.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-22T13:38:42Z

- **pkg/controller/tas/resource_flavor.go:206** by [@mimowo](https://github.com/mimowo) — 2024-11-22T13:38:42Z
```diff
@@ -146,15 +186,13 @@ func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
 	}
 	if flv.Spec.TopologyName != nil {
-		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
-			topology := kueuealpha.Topology{}
-			if err := r.client.Get(ctx, types.NamespacedName{Name: string(*flv.Spec.TopologyName)}, &topology); err != nil {
-				return reconcile.Result{}, err
-			}
-			levels := utiltas.Levels(&topology)
-			tasInfo := r.tasCache.NewTASFlavorCache(kueue.TopologyReference(topology.Name), levels, flv.Spec.NodeLabels)
-			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		topology := kueuealpha.Topology{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: string(*flv.Spec.TopologyName)}, &topology); err != nil {
+			return reconcile.Result{}, err
 		}
+		levels := utiltas.Levels(&topology)
+		tasInfo := r.tasCache.NewTASFlavorCache(kueue.TopologyReference(topology.Name), levels, flv.Spec.NodeLabels)
+		r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
```

  Only set new if the nodeLabels, or levels are changed, to avoid throwing away the workload usage otherwise.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-22T16:02:19Z

- **pkg/controller/tas/resource_flavor.go:206** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-22T16:02:19Z
```diff
@@ -146,15 +186,13 @@ func (r *rfReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		r.tasCache.Delete(kueue.ResourceFlavorReference(req.NamespacedName.Name))
 	}
 	if flv.Spec.TopologyName != nil {
-		if r.tasCache.Get(kueue.ResourceFlavorReference(flv.Name)) == nil {
-			topology := kueuealpha.Topology{}
-			if err := r.client.Get(ctx, types.NamespacedName{Name: string(*flv.Spec.TopologyName)}, &topology); err != nil {
-				return reconcile.Result{}, err
-			}
-			levels := utiltas.Levels(&topology)
-			tasInfo := r.tasCache.NewTASFlavorCache(kueue.TopologyReference(topology.Name), levels, flv.Spec.NodeLabels)
-			r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
+		topology := kueuealpha.Topology{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: string(*flv.Spec.TopologyName)}, &topology); err != nil {
+			return reconcile.Result{}, err
 		}
+		levels := utiltas.Levels(&topology)
+		tasInfo := r.tasCache.NewTASFlavorCache(kueue.TopologyReference(topology.Name), levels, flv.Spec.NodeLabels)
+		r.tasCache.Set(kueue.ResourceFlavorReference(flv.Name), tasInfo)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-25T10:54:25Z

- **test/integration/tas/tas_test.go:532** by [@mimowo](https://github.com/mimowo) — 2024-11-25T10:54:25Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  I think this test could be racy - the workload might get admitted if it is scheduled before the topology is removed from cache. I'm wondering if we could trigger deactivation of the ClusterQueue due to TopologyNotFound, by analogy to [here](https://github.com/kubernetes-sigs/kueue/blob/f0d1ecdd60df7e0105b4eb1319ff43f67222460f/pkg/cache/clusterqueue.go#L268). Then, in the test we could simply wait until the queue becomes inactive.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-25T13:46:22Z

- **test/integration/tas/tas_test.go:532** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-25T13:46:22Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  Should this work? I tried checking without a topology, and it still appears to be active.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-25T14:34:26Z

- **test/integration/tas/tas_test.go:532** by [@mimowo](https://github.com/mimowo) — 2024-11-25T14:34:26Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  Yeah, but I think we should deactivate as we deactivate when there is no ResourceFlavor

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-25T14:38:42Z

- **test/integration/tas/tas_test.go:532** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-25T14:38:42Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  In this case, I think we should consider how to reconcile the ClusterQueue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T07:59:02Z

- **test/integration/tas/tas_test.go:532** by [@mimowo](https://github.com/mimowo) — 2024-11-26T07:59:02Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  I'm concerned this may flake, if the workload is created before the topology delete event is propagated. 
  
  To test if this is possible I suggest to:
  1. temporarily inject time.Sleep(2*time.Second) before `tasCache.Delete`, [here](https://github.com/kubernetes-sigs/kueue/pull/3615/files#diff-7f688e0706bb6b6775d35b320ee0d496f92ed367d87cb845979731898ea9d1eaR182)
  2. if we confirm the test fails, adjust the test to create the workloads in a loop using gomega.Eventually, until we see a workload which is pending; the previously admitted workloads we can delete or mark finished to make sure they are not blocking quota
  3. once the test is stable, remove the temporarily injected sleep

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T10:42:09Z

- **test/integration/tas/tas_test.go:532** by [@mimowo](https://github.com/mimowo) — 2024-11-26T10:42:09Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  I synced with @mbobrovskyi , the point (2.) of the proposal is more challanging as it seems, because we would need to wait for the workload to get admitted (to give it a chance).
  
  So I propose to add the sleep with a comment:
  ```
  // TODO(#3645): replace the sleep with waiting for CQ deactivation.
  // The sleep is a temporary solution to minimize the chance for the test flaking in case
  // the workload is created and admitted before the event is handled and the topology
  // is removed from the cache.
  time.Sleep(time.Second)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-26T10:47:04Z

- **test/integration/tas/tas_test.go:532** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T10:47:04Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-26T10:49:48Z

- **test/integration/tas/tas_test.go:532** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-26T10:49:48Z
```diff
@@ -514,6 +514,48 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					))
 				})
 			})
+
+			ginkgo.It("should not admit the workload after the topology is deleted but should admit it after the topology is created", func() {
+				ginkgo.By("delete topology", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
```

  I also tested it using a stress test.
  
  ```
  KUBEBUILDER_ASSETS=/Users/mykhailo_bobrovskyi/Library/Application\ Support/io.kubebuilder.envtest/k8s/1.31.0-darwin-arm64 stress ./tas.test -test.run TestAPIs
  10m55s: 301 runs so far, 0 failures
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T10:51:44Z

/lgtm
/approve
