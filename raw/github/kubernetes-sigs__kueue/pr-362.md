# PR #362: Add options for internal cert management to component config

**Summary**: Add options for internal cert management to component config

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/362

**Last updated**: 2022-09-09T15:22:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `add-cert-options-to-component-config` → `main`
- **Created**: 2022-08-29T20:04:51Z
- **Updated**: 2022-09-09T15:22:29Z
- **Merged**: 2022-09-09T14:47:23Z
- **Closed**: 2022-09-09T14:47:23Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: _none_
- **Changed files**: 15   **+409 / -76**

## Description

Signed-off-by: tenzen-y <yuki.iwai.tz@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
/kind feature
/kind api-change

#### What this PR does / why we need it:

I added options for internal cert management to component config and I have tested the operations in the below commands:

-  For default internal cert management options
```shell
kubectl apply -k github.com/tenzen-y/kueue/config/default?ref=add-cert-options-to-component-config-default-cert-opts
```

- For customize namespace, serviceName and secretName

```shell
kubectl apply -k github.com/tenzen-y/kueue/config/default?ref=add-cert-options-to-component-config-customize-cert-opts
```
Also, I fixed Makefile and added a changelog.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #270 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-29T20:04:58Z

Welcome @tenzen-y! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-08-29T20:04:59Z

Hi @tenzen-y. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-29T20:20:50Z

/assign @alculquicondor @kerthcet

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-08-29T20:42:34Z

/ok-to-test

I'll let @kerthcet do the first pass

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T18:50:02Z

@kerthcet I have addressed your comments. Please take another look.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T18:55:02Z

Also, it might be better to move [`setOptionDefaults(opt *ctrl.Options)`](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/main.go#L246-L259) to [`SetDefaults_Configuration(cfg *Configuration)`](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/apis/config/v1alpha1/defaults.go#L33) same as [`InternalCertManagement`](https://github.com/tenzen-y/kueue/blob/5413fdcd5a7d53797908884817b912967e17d316/apis/config/v1alpha2/defaults.go#L43-L56) in another PR.

What do you think?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T02:37:35Z

> Also, it might be better to move [`setOptionDefaults(opt *ctrl.Options)`](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/main.go#L246-L259) to [`SetDefaults_Configuration(cfg *Configuration)`](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/apis/config/v1alpha1/defaults.go#L33) same as [`InternalCertManagement`](https://github.com/tenzen-y/kueue/blob/5413fdcd5a7d53797908884817b912967e17d316/apis/config/v1alpha2/defaults.go#L43-L56) in another PR.
> 
> What do you think?

Actually, it was designed on purpose before, we want to apply defaults after loading the config file. The advantage I can think of is that we apply `AndFrom` above, it will also set defaults, but it's out of our control since it's an imported package, applying defaults after gives us more confident. But I'm open with this.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T09:33:11Z

> > Also, it might be better to move [`setOptionDefaults(opt *ctrl.Options)`](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/main.go#L246-L259) to [`SetDefaults_Configuration(cfg *Configuration)`](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/apis/config/v1alpha1/defaults.go#L33) same as [`InternalCertManagement`](https://github.com/tenzen-y/kueue/blob/5413fdcd5a7d53797908884817b912967e17d316/apis/config/v1alpha2/defaults.go#L43-L56) in another PR.
> > What do you think?
> 
> Actually, it was designed on purpose before, we want to apply defaults after loading the config file. The advantage I can think of is that we apply `AndFrom` above, it will also set defaults, but it's out of our control since it's an imported package, applying defaults after gives us more confident. But I'm open with this.

Thanks for clarifying! @kerthcet 

It seems strange that the different how to set defaults (use the SetDefaults function or apply defaults after loading config) for both InternalCertManagement and other parameters.  So, it might be better to do the same logic.

In my opinion, it might be better to set all default parameters to Configuration using the SetDefaults function.

What do you think about the current logic that uses different logic for InternalCertManagement and other parameters?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T10:36:20Z

> It seems strange that the different how to set defaults (use the SetDefaults function or apply defaults after loading config) for both InternalCertManagement and other parameters. So, it might be better to do the same logic.
> 
> In my opinion, it might be better to set all default parameters to Configuration using the SetDefaults function.
> 
> What do you think about the current logic that uses different logic for InternalCertManagement and other parameters?

I'm ok with this if we can add enough tests to improve our confidence. But we can leave it a follow up as you said.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T15:29:54Z

> I'm ok with this if we can add enough tests to improve our confidence. But we can leave it a follow up as you said.

I agree with you.
I will not work on it in this PR.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T15:57:17Z

Let me know when this is ready for a second pass from my side

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T16:42:18Z

> Let me know when this is ready for a second pass from my side

Sure.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T18:23:19Z

@kerthcet Could you please take another look since I have applied your suggestions?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:45:22Z

@alculquicondor @kerthcet Could you please take a look at this PR again?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T14:26:33Z

Some nits, general looks good to me. But I leave the kustomize configuration to @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:10:24Z

Thank you very much for your detailed review many times! @kerthcet

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T08:51:36Z

@alculquicondor I have addressed your suggestions. PTAL

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T19:47:16Z

@kerthcet What is your opinion about https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r965205940?

> *string is necessary when the empty string is a valid value. But this is not the case for these fields. Empty means not-set, and we can set the default.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-08T20:35:05Z

Probably, we need to merge https://github.com/kubernetes-sigs/kueue/pull/379 first.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-08T21:54:14Z

/test pull-kueue-build-image-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-08T21:58:06Z

Probably, this PR is ready to merge.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-09T06:07:02Z

Once this PR is merged, I will work on [this](https://github.com/kubernetes-sigs/kueue/pull/362#issuecomment-1232041979).

> Also, it might be better to move [setOptionDefaults(opt *ctrl.Options)](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/main.go#L246-L259) to [SetDefaults_Configuration(cfg *Configuration)](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/apis/config/v1alpha1/defaults.go#L33) same as [InternalCertManagement](https://github.com/tenzen-y/kueue/blob/5413fdcd5a7d53797908884817b912967e17d316/apis/config/v1alpha2/defaults.go#L43-L56) in another PR.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T14:01:44Z

And please squash :)

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-09T14:07:02Z

> And please squash :)

I have already squashed.

https://github.com/kubernetes-sigs/kueue/pull/362/commits

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-09T14:09:49Z

You mean git sqush, right?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-09-09T14:42:23Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/362#pullrequestreview-1102349059" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/362#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-09T14:50:37Z

@alculquicondor 
Hmm..? This PR seems to be merged before I apply your suggestion.

I will create another PR to apply your suggestion.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T14:54:08Z

oh yeah... I should have checked

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-09-09T15:19:04Z

Thanks @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-09T15:22:29Z

> Thanks @tenzen-y

@kerthcet Thanks for your review!

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-29T20:07:36Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-29T20:07:36Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e test -e apis) -coverprofile cover.out
```

  Previously, `main_test.go` could not be tested.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-29T20:15:32Z

- **Makefile:125** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-29T20:15:32Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e test -e apis) -coverprofile cover.out
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ginkgo ## Run tests.
-	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
+	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) --arch=amd64 use $(ENVTEST_K8S_VERSION) -p path)" \
```

  We need to specify CPU architecture to run tests on Apple Silicon machines since envtest only provides k8s components for amd64 binaries.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-29T20:19:09Z

- **apis/config/v1alpha2/configuration_types.go:56** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-29T20:19:08Z
```diff
@@ -39,8 +39,27 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// Namespace is the namespace where Service and Secret are deployed and
+	// is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  I added `Namespace` field to allow kueue-controller to be deployed to other namespaces (except kueue-system).
  What do you think?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-30T06:41:30Z

- **apis/config/v1alpha2/configuration_types.go:56** by [@kerthcet](https://github.com/kerthcet) — 2022-08-30T05:48:28Z
```diff
@@ -39,8 +39,27 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// Namespace is the namespace where Service and Secret are deployed and
+	// is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  If so, it should be placed outside the InternalCertManagment.

- **apis/config/v1alpha2/defaults.go:47** by [@kerthcet](https://github.com/kerthcet) — 2022-08-30T05:55:57Z
```diff
@@ -31,7 +31,7 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
-	if cfg.EnableInternalCertManagement == nil {
-		cfg.EnableInternalCertManagement = pointer.Bool(true)
+	if cfg.InternalCertManagement.Enable == nil {
+		cfg.InternalCertManagement.Enable = pointer.Bool(true)
 	}
```

  Let's move all the defaults of InternalCertManagement here.

- **apis/config/v1alpha2/configuration_types.go:43** by [@kerthcet](https://github.com/kerthcet) — 2022-08-30T06:17:31Z
```diff
@@ -39,8 +39,27 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement InternalCertManagement `json:"internalCertManagement,omitempty"`
```

  Make this a pointer, then we can know whether we set this or not.

- **main.go:265** by [@kerthcet](https://github.com/kerthcet) — 2022-08-30T06:20:55Z
```diff
@@ -257,3 +258,17 @@ func setOptionDefaults(opt *ctrl.Options) {
 		opt.LeaderElectionID = "c1f6bfd2.kueue.x-k8s.io"
 	}
 }
+
+func setInternalCertManagementDefaults(certOpts *configv1alpha2.InternalCertManagement) {
+	if *certOpts.Enable {
+		if certOpts.ServiceName == "" {
+			certOpts.ServiceName = "kueue-webhook-service"
```

  Let's make  default `ServiceName`... a block of constants.

- **pkg/util/cert/cert.go:42** by [@kerthcet](https://github.com/kerthcet) — 2022-08-30T06:40:33Z
```diff
@@ -22,32 +22,31 @@ import (
 	cert "github.com/open-policy-agent/cert-controller/pkg/rotator"
 	"k8s.io/apimachinery/pkg/types"
 	ctrl "sigs.k8s.io/controller-runtime"
+
+	configv1alpha2 "sigs.k8s.io/kueue/apis/config/v1alpha2"
 )
 
 const (
-	serviceName     = "kueue-webhook-service"
-	secretName      = "kueue-webhook-server-cert"
-	secretNamespace = "kueue-system"
-	certDir         = "/tmp/k8s-webhook-server/serving-certs"
-	vwcName         = "kueue-validating-webhook-configuration"
-	mwcName         = "kueue-mutating-webhook-configuration"
-	caName          = "kueue-ca"
-	caOrganization  = "kueue"
+	certDir        = "/tmp/k8s-webhook-server/serving-certs"
+	vwcName        = "kueue-validating-webhook-configuration"
+	mwcName        = "kueue-mutating-webhook-configuration"
+	caName         = "kueue-ca"
+	caOrganization = "kueue"
 )
 
-// DNSName is <service name>.<namespace>.svc
-var dnsName = fmt.Sprintf("%s.%s.svc", serviceName, secretNamespace)
-
 //+kubebuilder:rbac:groups="",resources=secrets,verbs=get;list;watch;update
 //+kubebuilder:rbac:groups="admissionregistration.k8s.io",resources=mutatingwebhookconfigurations,verbs=get;list;watch;update
 //+kubebuilder:rbac:groups="admissionregistration.k8s.io",resources=validatingwebhookconfigurations,verbs=get;list;watch;update
 
 // ManageCerts creates all certs for webhooks. This function is called from main.go.
-func ManageCerts(mgr ctrl.Manager, setupFinished chan struct{}) error {
+func ManageCerts(mgr ctrl.Manager, certOpts *configv1alpha2.InternalCertManagement, setupFinished chan struct{}) error {
```

  Make certOpts a value not a pointer, it's a simple struct.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T07:16:54Z

- **apis/config/v1alpha2/configuration_types.go:56** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T07:16:54Z
```diff
@@ -39,8 +39,27 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// Namespace is the namespace where Service and Secret are deployed and
+	// is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  It makes sense.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-30T07:20:03Z

- **Makefile:121** by [@kerthcet](https://github.com/kerthcet) — 2022-08-30T07:20:03Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e test -e apis) -coverprofile cover.out
```

  Good catch.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T07:37:53Z

- **apis/config/v1alpha2/defaults.go:47** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T07:37:53Z
```diff
@@ -31,7 +31,7 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
-	if cfg.EnableInternalCertManagement == nil {
-		cfg.EnableInternalCertManagement = pointer.Bool(true)
+	if cfg.InternalCertManagement.Enable == nil {
+		cfg.InternalCertManagement.Enable = pointer.Bool(true)
 	}
```

  SGTM.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T07:52:03Z

- **main.go:265** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T07:52:03Z
```diff
@@ -257,3 +258,17 @@ func setOptionDefaults(opt *ctrl.Options) {
 		opt.LeaderElectionID = "c1f6bfd2.kueue.x-k8s.io"
 	}
 }
+
+func setInternalCertManagementDefaults(certOpts *configv1alpha2.InternalCertManagement) {
+	if *certOpts.Enable {
+		if certOpts.ServiceName == "" {
+			certOpts.ServiceName = "kueue-webhook-service"
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T07:52:23Z

- **apis/config/v1alpha2/configuration_types.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T07:52:23Z
```diff
@@ -39,8 +39,27 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement InternalCertManagement `json:"internalCertManagement,omitempty"`
```

  SGTM.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T18:42:21Z

- **apis/config/v1alpha2/configuration_types.go:56** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T18:42:21Z
```diff
@@ -39,8 +39,27 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// Namespace is the namespace where Service and Secret are deployed and
+	// is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T18:42:34Z

- **apis/config/v1alpha2/defaults.go:47** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T18:42:34Z
```diff
@@ -31,7 +31,7 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
-	if cfg.EnableInternalCertManagement == nil {
-		cfg.EnableInternalCertManagement = pointer.Bool(true)
+	if cfg.InternalCertManagement.Enable == nil {
+		cfg.InternalCertManagement.Enable = pointer.Bool(true)
 	}
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T18:42:41Z

- **apis/config/v1alpha2/configuration_types.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T18:42:41Z
```diff
@@ -39,8 +39,27 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement InternalCertManagement `json:"internalCertManagement,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T18:42:46Z

- **main.go:265** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T18:42:45Z
```diff
@@ -257,3 +258,17 @@ func setOptionDefaults(opt *ctrl.Options) {
 		opt.LeaderElectionID = "c1f6bfd2.kueue.x-k8s.io"
 	}
 }
+
+func setInternalCertManagementDefaults(certOpts *configv1alpha2.InternalCertManagement) {
+	if *certOpts.Enable {
+		if certOpts.ServiceName == "" {
+			certOpts.ServiceName = "kueue-webhook-service"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-30T18:42:53Z

- **pkg/util/cert/cert.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-30T18:42:53Z
```diff
@@ -22,32 +22,31 @@ import (
 	cert "github.com/open-policy-agent/cert-controller/pkg/rotator"
 	"k8s.io/apimachinery/pkg/types"
 	ctrl "sigs.k8s.io/controller-runtime"
+
+	configv1alpha2 "sigs.k8s.io/kueue/apis/config/v1alpha2"
 )
 
 const (
-	serviceName     = "kueue-webhook-service"
-	secretName      = "kueue-webhook-server-cert"
-	secretNamespace = "kueue-system"
-	certDir         = "/tmp/k8s-webhook-server/serving-certs"
-	vwcName         = "kueue-validating-webhook-configuration"
-	mwcName         = "kueue-mutating-webhook-configuration"
-	caName          = "kueue-ca"
-	caOrganization  = "kueue"
+	certDir        = "/tmp/k8s-webhook-server/serving-certs"
+	vwcName        = "kueue-validating-webhook-configuration"
+	mwcName        = "kueue-mutating-webhook-configuration"
+	caName         = "kueue-ca"
+	caOrganization = "kueue"
 )
 
-// DNSName is <service name>.<namespace>.svc
-var dnsName = fmt.Sprintf("%s.%s.svc", serviceName, secretNamespace)
-
 //+kubebuilder:rbac:groups="",resources=secrets,verbs=get;list;watch;update
 //+kubebuilder:rbac:groups="admissionregistration.k8s.io",resources=mutatingwebhookconfigurations,verbs=get;list;watch;update
 //+kubebuilder:rbac:groups="admissionregistration.k8s.io",resources=validatingwebhookconfigurations,verbs=get;list;watch;update
 
 // ManageCerts creates all certs for webhooks. This function is called from main.go.
-func ManageCerts(mgr ctrl.Manager, setupFinished chan struct{}) error {
+func ManageCerts(mgr ctrl.Manager, certOpts *configv1alpha2.InternalCertManagement, setupFinished chan struct{}) error {
```

  Done.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-08-31T10:30:51Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T07:15:18Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  Since this is optional, maybe *string will be better.

- **apis/config/v1alpha2/configuration_types.go:59** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T08:33:29Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName string `json:"serviceName,omitempty"`
```

  ditto.

- **apis/config/v1alpha2/defaults_test.go:17** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T09:23:08Z
```diff
@@ -0,0 +1,71 @@
+package v1alpha2
```

  Add the license here. You can copy that from other files.

- **docs/setup/install.md:21** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T09:27:34Z
```diff
@@ -15,7 +15,7 @@ if you don't have your own monitoring system.
 
 The webhook server in kueue uses an internal cert management for provisioning certificates. If you want to use
   a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
-  1. Set `enableInternalCertManagement` to `false` in [config file](#install-a-custom-configured-released-version).
+  1. Set `internalCertManagement.Enable` to `false` in [config file](#install-a-custom-configured-released-version).
   2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
   3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
 
```

  Also add the documents about serviceName and secretName.

- **apis/config/v1alpha2/defaults_test.go:18** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T09:31:05Z
```diff
@@ -0,0 +1,71 @@
+package v1alpha2
+
```

  Thanks for adding the tests.

- **Makefile:121** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T09:45:24Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue') -coverprofile cover.out
```

  Plz fix this shell since we have tests in `apis/kueue/webhooks`

- **main_test.go:149** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T09:58:06Z
```diff
@@ -42,54 +42,29 @@ webhook:
 		t.Fatal(err)
 	}
 
-	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
-	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
-kind: Configuration
-leaderElection:
-  leaderElect: true
-enableInternalCertManagement: false
-`), os.FileMode(0600)); err != nil {
-		t.Fatal(err)
-	}
-
 	testcases := []struct {
```

  We have more fields now so my suggestion is refactor the testcase like:
  ```
  	testcases := []struct {
  		name              string
  		configFile        string
  		wantConfiguration configv1alpha2.Configuration
        }{
        ...
        }
  ```
  
  Then we can use cmp.Diff to diff the config.

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-08-31T10:30:20Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  Besides, have you tried this, I have no time to test it up to now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T15:21:46Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T15:21:46Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  > Since this is optional, maybe *string will be better.
  
  SGTM.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T15:25:01Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T15:25:01Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  > Besides, have you tried this, I have no time to test it up to now.
  
  Yes, I have tested using the below command:
  
  ```shell
  kubectl apply -k github.com/tenzen-y/kueue/config/default?ref=add-cert-options-to-component-config-customize-cert-opts
  ```
  
  The above manifests are customized namespace, serviceName, and secretName.
  
  Detail of Configuration: https://github.com/tenzen-y/kueue/blob/add-cert-options-to-component-config-customize-cert-opts/config/components/manager/controller_manager_config.yaml

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T15:26:22Z

- **apis/config/v1alpha2/defaults_test.go:17** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T15:26:21Z
```diff
@@ -0,0 +1,71 @@
+package v1alpha2
```

  Thanks for pointing this out!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T15:26:34Z

- **docs/setup/install.md:21** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T15:26:34Z
```diff
@@ -15,7 +15,7 @@ if you don't have your own monitoring system.
 
 The webhook server in kueue uses an internal cert management for provisioning certificates. If you want to use
   a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
-  1. Set `enableInternalCertManagement` to `false` in [config file](#install-a-custom-configured-released-version).
+  1. Set `internalCertManagement.Enable` to `false` in [config file](#install-a-custom-configured-released-version).
   2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
   3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
 
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T15:27:08Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T15:27:08Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue') -coverprofile cover.out
```

  Sure.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T16:16:51Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T16:16:50Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T16:18:31Z

- **apis/config/v1alpha2/configuration_types.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T16:18:31Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName string `json:"serviceName,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T16:21:58Z

- **apis/config/v1alpha2/defaults_test.go:17** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T16:21:58Z
```diff
@@ -0,0 +1,71 @@
+package v1alpha2
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T16:36:04Z

- **docs/setup/install.md:21** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T16:36:04Z
```diff
@@ -15,7 +15,7 @@ if you don't have your own monitoring system.
 
 The webhook server in kueue uses an internal cert management for provisioning certificates. If you want to use
   a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
-  1. Set `enableInternalCertManagement` to `false` in [config file](#install-a-custom-configured-released-version).
+  1. Set `internalCertManagement.Enable` to `false` in [config file](#install-a-custom-configured-released-version).
   2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
   3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T16:41:06Z

- **main_test.go:149** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T16:41:06Z
```diff
@@ -42,54 +42,29 @@ webhook:
 		t.Fatal(err)
 	}
 
-	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
-	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
-kind: Configuration
-leaderElection:
-  leaderElect: true
-enableInternalCertManagement: false
-`), os.FileMode(0600)); err != nil {
-		t.Fatal(err)
-	}
-
 	testcases := []struct {
```

  SGTM.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T17:36:30Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T17:36:30Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue') -coverprofile cover.out
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-08-31T17:39:07Z

- **apis/kueue/webhooks/workload_webhook_test.go:304** by [@tenzen-y](https://github.com/tenzen-y) — 2022-08-31T17:39:07Z
```diff
@@ -301,7 +301,6 @@ func TestValidateWorkload(t *testing.T) {
 				Admit(testingutil.MakeAdmission("cluster-queue", "@invalid").Obj()).
 				Obj(),
 			wantErr: field.ErrorList{
-				field.Invalid(specField.Child("admission", "podSetFlavors").Index(0).Child("name"), nil, ""),
```

  There are no validating webhooks to verify this item.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-01T02:54:22Z

- **pkg/util/pointer/pointer.go:31** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T01:53:11Z
```diff
@@ -26,7 +26,8 @@ func Quantity(q resource.Quantity) *resource.Quantity {
 }
 
 var (
-	Int32 = pointer.Int32
-	Int64 = pointer.Int64
-	Bool  = pointer.Bool
```

  Why we wrap the pointer here? @alculquicondor

- **apis/config/v1alpha2/defaults_test.go:48** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T01:55:58Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
+					SecretName:  pointer.String("kueue-webhook-server-cert"),
 				},
 			},
 		},
 		"should not defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(false),
-					ServiceName: "",
-					SecretName:  "",
+					ServiceName: nil,
```

  This can be removed, also apply to other places.

- **apis/config/v1alpha2/defaults_test.go:38** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T01:59:03Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
```

  Let's make constants  etc. `defaultNamespace`  private in package not only in function(That means move them to the head of the file), then we can directly import here.

- **docs/setup/install.md:67** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T02:52:44Z
```diff
@@ -64,13 +64,15 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
-2. With an editor of your preference, open `manifests.yaml`.
-3. In the `kueue-manager-config` ConfigMap manifest, edit the
+1. With an editor of your preference, open `manifests.yaml`.
```

  It's actually 2.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T05:01:36Z

- **apis/config/v1alpha2/defaults_test.go:38** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T05:01:36Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
```

  It makes sense.
  Since we will many times create struct `InternalCertManagement` in `defaults_test.go` and `main_test.go`, I will add a Wrapper for `InternalCertManagement` in pkg `testing`, same as other wrappers.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T05:08:29Z

- **apis/config/v1alpha2/defaults_test.go:48** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T05:08:29Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
+					SecretName:  pointer.String("kueue-webhook-server-cert"),
 				},
 			},
 		},
 		"should not defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(false),
-					ServiceName: "",
-					SecretName:  "",
+					ServiceName: nil,
```

  Does this mean that It is redundant to specify `nil`? @kerthcet

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-01T05:10:51Z

- **apis/config/v1alpha2/defaults_test.go:48** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T05:10:51Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
+					SecretName:  pointer.String("kueue-webhook-server-cert"),
 				},
 			},
 		},
 		"should not defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(false),
-					ServiceName: "",
-					SecretName:  "",
+					ServiceName: nil,
```

  Yes, nil is the default value.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T05:23:41Z

- **apis/config/v1alpha2/defaults_test.go:48** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T05:23:41Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
+					SecretName:  pointer.String("kueue-webhook-server-cert"),
 				},
 			},
 		},
 		"should not defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(false),
-					ServiceName: "",
-					SecretName:  "",
+					ServiceName: nil,
```

  Thanks, that makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T05:28:09Z

- **docs/setup/install.md:67** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T05:28:08Z
```diff
@@ -64,13 +64,15 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
-2. With an editor of your preference, open `manifests.yaml`.
-3. In the `kueue-manager-config` ConfigMap manifest, edit the
+1. With an editor of your preference, open `manifests.yaml`.
```

  Oops...
  Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-01T14:11:29Z

- **CHANGELOG/CHANGELOG-0.3.md:7** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:46:27Z
```diff
@@ -0,0 +1,13 @@
+## v0.3.0
+
+Changes since `v0.2.1`:
+
+### Features
+
+- Upgrade the config.kueue.x-k8s.io API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
```

  ```suggestion
  - Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
  ```

- **Makefile:121** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:47:22Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
```

  why is this change necessary?

- **Makefile:125** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:47:43Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ginkgo ## Run tests.
-	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
+	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) --arch=amd64 use $(ENVTEST_K8S_VERSION) -p path)" \
```

  why is this necessary?

- **apis/config/v1alpha2/configuration_types.go:58** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:49:09Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
```

  ```suggestion
  	// Defaults to kueue-webhook-service.
  ```

- **apis/config/v1alpha2/defaults.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:53:01Z
```diff
@@ -31,7 +31,27 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
-	if cfg.EnableInternalCertManagement == nil {
-		cfg.EnableInternalCertManagement = pointer.Bool(true)
+	const (
+		defaultNamespace   = "kueue-system"
+		defaultServiceName = "kueue-webhook-service"
+		defaultSecretName  = "kueue-webhook-server-cert"
```

  put them outside of the function

- **apis/config/v1alpha2/configuration_types.go:59** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:54:01Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName *string `json:"serviceName,omitempty"`
```

  ```suggestion
  	WebhookServiceName *string `json:"webhookServiceName,omitempty"`
  ```

- **apis/config/v1alpha2/configuration_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:54:59Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName *string `json:"serviceName,omitempty"`
+
+	// SecretName is used to store CA and server certs.
+	// Default to kueue-webhook-server-cert.
+	SecretName *string `json:"secretName,omitempty"`
```

  ```suggestion
  	// WebhookSecretName is the name of the Secret used to store CA and server certs.
  	// Defaults to kueue-webhook-server-cert.
  	WebhookSecretName *string `json:"webhookSecretName,omitempty"`
  ```

- **pkg/util/pointer/pointer.go:31** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:56:53Z
```diff
@@ -26,7 +26,8 @@ func Quantity(q resource.Quantity) *resource.Quantity {
 }
 
 var (
-	Int32 = pointer.Int32
-	Int64 = pointer.Int64
-	Bool  = pointer.Bool
```

  Just so that you only need to import one package.

- **apis/config/v1alpha2/defaults_test.go:24** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:57:34Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha2
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
```

  If you don't need an special pointer, you could just import `k8s.io/utils/pointer`

- **apis/config/v1alpha2/defaults_test.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T13:59:47Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
```

  Creating a wrapper sounds like overkill. Don't

- **apis/kueue/webhooks/workload_webhook_test.go:304** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T14:01:36Z
```diff
@@ -301,7 +301,6 @@ func TestValidateWorkload(t *testing.T) {
 				Admit(testingutil.MakeAdmission("cluster-queue", "@invalid").Obj()).
 				Obj(),
 			wantErr: field.ErrorList{
-				field.Invalid(specField.Child("admission", "podSetFlavors").Index(0).Child("name"), nil, ""),
```

  what changed in the code that made this test change?

- **docs/setup/install.md:67** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T14:03:17Z
```diff
@@ -64,13 +64,15 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
-2. With an editor of your preference, open `manifests.yaml`.
-3. In the `kueue-manager-config` ConfigMap manifest, edit the
+1. With an editor of your preference, open `manifests.yaml`.
```

  the one above is 2.

- **docs/setup/install.md:74** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T14:03:47Z
```diff
@@ -64,13 +64,15 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
-2. With an editor of your preference, open `manifests.yaml`.
-3. In the `kueue-manager-config` ConfigMap manifest, edit the
+1. With an editor of your preference, open `manifests.yaml`.
+2. In the `kueue-manager-config` ConfigMap manifest, edit the
 `controller_manager_config.yaml` data entry. The entry represents
 the default Kueue Configuration
 struct ([v1alpha1@v0.2.1](https://pkg.go.dev/sigs.k8s.io/kueue@v0.2.1/apis/config/v1alpha1#Configuration)).
 The contents of the ConfigMap are similar to the following:
 
+__namespace and internalCertManagement fields are available in Kueue v0.3.0 and later__
```

  ```suggestion
  __`namespace` and `internalCertManagement` fields are available in Kueue v0.3.0 and later__
  ```

- **main.go:105** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T14:04:40Z
```diff
@@ -102,8 +102,8 @@ func main() {
 
 	certsReady := make(chan struct{})
 
-	if *config.EnableInternalCertManagement {
-		if err = cert.ManageCerts(mgr, certsReady); err != nil {
+	if *config.InternalCertManagement.Enable {
```

  ```suggestion
  	if config.InternalCertManagement != nil && *config.InternalCertManagement.Enable {
  ```

- **main_test.go:149** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T14:07:29Z
```diff
@@ -42,54 +42,29 @@ webhook:
 		t.Fatal(err)
 	}
 
-	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
-	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
-kind: Configuration
-leaderElection:
-  leaderElect: true
-enableInternalCertManagement: false
-`), os.FileMode(0600)); err != nil {
-		t.Fatal(err)
-	}
-
 	testcases := []struct {
```

  Please do as suggested.

- **apis/config/v1alpha2/configuration_types.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T14:10:12Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  I would put it inside the internalCertManagement struct

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-01T14:18:06Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T14:18:06Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  ~Forget to comment here, at the very beginning I was tricked by the annotation, actually we should name this serviceNamespace. What's more, I think just modify the api is not enough, I'm making some experiments  right now, I think we should also update the kustomize configuration.~

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T14:48:02Z

- **apis/kueue/webhooks/workload_webhook_test.go:304** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T14:48:01Z
```diff
@@ -301,7 +301,6 @@ func TestValidateWorkload(t *testing.T) {
 				Admit(testingutil.MakeAdmission("cluster-queue", "@invalid").Obj()).
 				Obj(),
 			wantErr: field.ErrorList{
-				field.Invalid(specField.Child("admission", "podSetFlavors").Index(0).Child("name"), nil, ""),
```

  This test change has nothing to do with my feature addition. However, as mentioned in https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r957749549, the bug in the test code was missed because pkg apis/kueue/webhook was not tested.
  
  You can reproduce it in the main branch with `go test ./apis/kueue/webhooks/...` command.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T14:53:28Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T14:53:27Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
```

  There are Go unit tests in pkg `main`, `./pkg`, `./apis/config/v1alpha1`and `./apis/kueue/webhooks`. However, the command before the change only runs the test on pkg `./pkg`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T14:58:22Z

- **Makefile:125** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T14:58:22Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ginkgo ## Run tests.
-	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
+	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) --arch=amd64 use $(ENVTEST_K8S_VERSION) -p path)" \
```

  As mentioned in https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r957756617, we can not run integration tests on Apple Silicon Mac.
  
  > We need to specify CPU architecture to run tests on Apple Silicon machines since envtest only provides k8s components for amd64 binaries.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T14:59:15Z

- **apis/config/v1alpha2/defaults.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T14:59:15Z
```diff
@@ -31,7 +31,27 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
-	if cfg.EnableInternalCertManagement == nil {
-		cfg.EnableInternalCertManagement = pointer.Bool(true)
+	const (
+		defaultNamespace   = "kueue-system"
+		defaultServiceName = "kueue-webhook-service"
+		defaultSecretName  = "kueue-webhook-server-cert"
```

  Sure.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-01T15:03:02Z

- **apis/kueue/webhooks/workload_webhook_test.go:304** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T15:03:02Z
```diff
@@ -301,7 +301,6 @@ func TestValidateWorkload(t *testing.T) {
 				Admit(testingutil.MakeAdmission("cluster-queue", "@invalid").Obj()).
 				Obj(),
 			wantErr: field.ErrorList{
-				field.Invalid(specField.Child("admission", "podSetFlavors").Index(0).Child("name"), nil, ""),
```

  Yes, exactly...

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T15:07:43Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T15:07:42Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
```

  Same reason as https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r960750058.
  
  > This test change has nothing to do with my feature addition. However, as mentioned in https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r957749549, the bug in the test code was missed because pkg apis/kueue/webhook was not tested.
  > 
  > You can reproduce it in the main branch with go test ./apis/kueue/webhooks/... command.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T15:14:19Z

- **apis/config/v1alpha2/defaults_test.go:24** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T15:14:19Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha2
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
```

  I see with your [this comment](https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r960689351). That makes sense.
  
  > Just so that you only need to import one package.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T15:27:05Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T15:27:05Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  > Forget to comment here, at the very beginning I was cheated by the annotation,
  
  I don't mind, thanks for your detailed review! @kerthcet 
  
  >  actually we should name this serviceNamespace.
  
  That makes sense. I will rename `Namespace` to `ServiceNamespace` and move to struct  `InternalCertManagement`.
  
  > What's more, I think just modify the api is not enough, I'm making some experiments right now, I think we should also update the kustomize configuration.
  
  Which kustomize configurations should we modify? [`namePrefix`](https://github.com/kubernetes-sigs/kueue/blob/6eeb73b1c6b9338f101ab095a39b1434a2f926ba/config/default/kustomization.yaml#L9)? @kerthcet

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T15:28:15Z

- **apis/config/v1alpha2/defaults_test.go:38** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T15:28:15Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
```

  Sure.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-01T15:49:25Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T15:49:25Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  Maybe you can try with a different service rather than `webhook-service`, and apply an unqualified object. Then you will get an error, maybe like `failed to call webhook: Post "https://webhook-service.kueue-system.svc:443/mutate-kueue-x-k8s-io-v1alpha2-resourceflavor?timeout=10s": service "webhook-service" not found`.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-01T15:52:07Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-01T15:52:06Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  Because we still need to update the manifests.yaml. IIUC.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-01T15:55:40Z

- **Makefile:121** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-01T15:55:39Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e test -e apis) -coverprofile cover.out
```

  would `-e /test` work?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-01T16:59:31Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-01T16:59:30Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  What manifest did you test with? @kerthcet 
  On my local test, it works fine.
  
  - `tmp/tmp.yaml`
  
  ```yaml
  apiVersion: kueue.x-k8s.io/v1alpha2
  kind: ResourceFlavor
  metadata:
    name: default
  ---
  apiVersion: kueue.x-k8s.io/v1alpha2
  kind: ClusterQueue
  metadata:
    name: cluster-total
  spec:
    namespaceSelector: {}
    resources:
    - name: "cpu"
      flavors:
      - name: default
        quota:
  # invalid
          invalid: 9
    - name: "memory"
      flavors:
      - name: default
        quota:
          min: 36Gi
  ---
  apiVersion: kueue.x-k8s.io/v1alpha2
  kind: LocalQueue
  metadata:
    namespace: default
    name: main
  spec:
    clusterQueue: cluster-total
  ```
  
  ```shell
  $ kubectl apply -k github.com/tenzen-y/kueue/config/default?ref=add-cert-options-to-component-config-customize-cert-opts
  namespace/kueue-tenant-a created
  customresourcedefinition.apiextensions.k8s.io/clusterqueues.kueue.x-k8s.io created
  customresourcedefinition.apiextensions.k8s.io/localqueues.kueue.x-k8s.io created
  customresourcedefinition.apiextensions.k8s.io/resourceflavors.kueue.x-k8s.io created
  customresourcedefinition.apiextensions.k8s.io/workloads.kueue.x-k8s.io created
  serviceaccount/kueue-controller-manager created
  role.rbac.authorization.k8s.io/kueue-leader-election-role created
  clusterrole.rbac.authorization.k8s.io/kueue-batch-admin-role created
  clusterrole.rbac.authorization.k8s.io/kueue-batch-user-role created
  clusterrole.rbac.authorization.k8s.io/kueue-clusterqueue-editor-role created
  clusterrole.rbac.authorization.k8s.io/kueue-clusterqueue-viewer-role created
  clusterrole.rbac.authorization.k8s.io/kueue-job-editor-role created
  clusterrole.rbac.authorization.k8s.io/kueue-job-viewer-role created
  clusterrole.rbac.authorization.k8s.io/kueue-localqueue-editor-role created
  clusterrole.rbac.authorization.k8s.io/kueue-localqueue-viewer-role created
  clusterrole.rbac.authorization.k8s.io/kueue-manager-role created
  clusterrole.rbac.authorization.k8s.io/kueue-metrics-reader created
  clusterrole.rbac.authorization.k8s.io/kueue-proxy-role created
  clusterrole.rbac.authorization.k8s.io/kueue-resourceflavor-editor-role created
  clusterrole.rbac.authorization.k8s.io/kueue-resourceflavor-viewer-role created
  clusterrole.rbac.authorization.k8s.io/kueue-workload-editor-role created
  clusterrole.rbac.authorization.k8s.io/kueue-workload-viewer-role created
  rolebinding.rbac.authorization.k8s.io/kueue-leader-election-rolebinding created
  clusterrolebinding.rbac.authorization.k8s.io/kueue-manager-rolebinding created
  clusterrolebinding.rbac.authorization.k8s.io/kueue-proxy-rolebinding created
  configmap/kueue-manager-config created
  secret/kueue-tenant-a-secret created
  service/kueue-controller-manager-metrics-service created
  service/kueue-tenant-a-service created
  deployment.apps/kueue-controller-manager created
  mutatingwebhookconfiguration.admissionregistration.k8s.io/kueue-mutating-webhook-configuration created
  validatingwebhookconfiguration.admissionregistration.k8s.io/kueue-validating-webhook-configuration created
  $ k get pods -n kueue-tenant-a 
  NAME                                        READY   STATUS    RESTARTS   AGE
  kueue-controller-manager-698469f7c5-tx4nm   2/2     Running   0          58s
  $ k get service -n kueue-tenant-a 
  NAME                                       TYPE        CLUSTER-IP     EXTERNAL-IP   PORT(S)    AGE
  kueue-controller-manager-metrics-service   ClusterIP   10.96.131.29   <none>        8443/TCP   2m2s
  kueue-tenant-a-service                     ClusterIP   10.96.122.75   <none>        443/TCP    2m2s
  $ k apply -f tmp/tmp.yaml 
  resourceflavor.kueue.x-k8s.io/default created
  error: error validating "tmp/tmp.yaml": error validating data: ValidationError(ClusterQueue.spec.resources[0].flavors[0].quota): unknown field "invalid" in io.x-k8s.kueue.v1alpha2.ClusterQueue.spec.resources.flavors.quota; if you choose to ignore these errors, turn validation off with --validate=false
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T04:10:18Z

- **main_test.go:149** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T04:10:18Z
```diff
@@ -42,54 +42,29 @@ webhook:
 		t.Fatal(err)
 	}
 
-	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
-	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
-kind: Configuration
-leaderElection:
-  leaderElect: true
-enableInternalCertManagement: false
-`), os.FileMode(0600)); err != nil {
-		t.Fatal(err)
-	}
-
 	testcases := []struct {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:28:06Z

- **apis/config/v1alpha2/defaults_test.go:48** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:28:06Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
+					SecretName:  pointer.String("kueue-webhook-server-cert"),
 				},
 			},
 		},
 		"should not defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(false),
-					ServiceName: "",
-					SecretName:  "",
+					ServiceName: nil,
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:35:57Z

- **apis/config/v1alpha2/defaults_test.go:38** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:35:57Z
```diff
@@ -15,46 +15,46 @@ func TestSetDefaults_Configuration(t *testing.T) {
 	}{
 		"defaulting namespace": {
 			original: &Configuration{
-				Namespace: "",
+				Namespace: nil,
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 			want: &Configuration{
-				Namespace: "kueue-system",
+				Namespace: pointer.String("kueue-system"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable: pointer.Bool(false),
 				},
 			},
 		},
 		"defaulting InternalCertManagement": {
 			original: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 			},
 			want: &Configuration{
-				Namespace: "kueue-tenant-a",
+				Namespace: pointer.String("kueue-tenant-a"),
 				InternalCertManagement: &InternalCertManagement{
 					Enable:      pointer.Bool(true),
-					ServiceName: "kueue-webhook-service",
-					SecretName:  "kueue-webhook-server-cert",
+					ServiceName: pointer.String("kueue-webhook-service"),
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:40:16Z

- **CHANGELOG/CHANGELOG-0.3.md:7** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:40:16Z
```diff
@@ -0,0 +1,13 @@
+## v0.3.0
+
+Changes since `v0.2.1`:
+
+### Features
+
+- Upgrade the config.kueue.x-k8s.io API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
```

  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:40:26Z

- **apis/config/v1alpha2/configuration_types.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:40:26Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
```

  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:40:33Z

- **apis/config/v1alpha2/configuration_types.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:40:33Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName *string `json:"serviceName,omitempty"`
```

  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:40:39Z

- **apis/config/v1alpha2/configuration_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:40:38Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName *string `json:"serviceName,omitempty"`
+
+	// SecretName is used to store CA and server certs.
+	// Default to kueue-webhook-server-cert.
+	SecretName *string `json:"secretName,omitempty"`
```

  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:42:23Z

- **docs/setup/install.md:67** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:42:23Z
```diff
@@ -64,13 +64,15 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
-2. With an editor of your preference, open `manifests.yaml`.
-3. In the `kueue-manager-config` ConfigMap manifest, edit the
+1. With an editor of your preference, open `manifests.yaml`.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:43:42Z

- **CHANGELOG/CHANGELOG-0.3.md:7** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:43:41Z
```diff
@@ -0,0 +1,13 @@
+## v0.3.0
+
+Changes since `v0.2.1`:
+
+### Features
+
+- Upgrade the config.kueue.x-k8s.io API version from v1alpha1 to v1alpha2. v1alpha1 is no longer supported.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:45:09Z

- **docs/setup/install.md:74** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:45:09Z
```diff
@@ -64,13 +64,15 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
-2. With an editor of your preference, open `manifests.yaml`.
-3. In the `kueue-manager-config` ConfigMap manifest, edit the
+1. With an editor of your preference, open `manifests.yaml`.
+2. In the `kueue-manager-config` ConfigMap manifest, edit the
 `controller_manager_config.yaml` data entry. The entry represents
 the default Kueue Configuration
 struct ([v1alpha1@v0.2.1](https://pkg.go.dev/sigs.k8s.io/kueue@v0.2.1/apis/config/v1alpha1#Configuration)).
 The contents of the ConfigMap are similar to the following:
 
+__namespace and internalCertManagement fields are available in Kueue v0.3.0 and later__
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T10:47:43Z

- **apis/config/v1alpha2/configuration_types.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T10:47:42Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
```

  Done.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-02T10:57:41Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-02T10:57:41Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  Some conclusions:
  1. When set the namespace, we should also update `config/default/kustomization.yaml`
  
  2. When set serviceName, we should update `config/components/webhook/service.yaml` and `config/components/webhook/manifests.yaml` manually. I didn't find how to auto-generate the serviceName actually. I think there should be ways.
  
  3. When set secretName, we should update `config/components/internalcert/secret.yaml` and `config/default/manager_webhook_patch.yaml`
  
  Better to document them or we can find ways to automate it.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-02T11:01:23Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-02T11:01:23Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  > I would put it inside the internalCertManagement struct
  
  Actually the `namespace` helps to control where to deploy the kueue components, besides configure this field, we should also update the `config/default/kustomization.yaml`, to change the value of `namespace`. 
  
  So I think we can leave it outside the internalCertManagement

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T17:36:31Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T17:36:30Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  > Some conclusions:
  >
  > 1. When set the namespace, we should also update config/default/kustomization.yaml
  >
  > 2. When set serviceName, we should update config/components/webhook/service.yaml and config/components/webhook/manifests.yaml manually. I didn't find how to auto-generate the serviceName actually. I think there should be ways.
  >
  > 3. When set secretName, we should update config/components/internalcert/secret.yaml and > config/default/manager_webhook_patch.yaml
  >
  > Better to document them or we can find ways to automate it.
  
  Thanks for clarifying! @kerthcet 
  After some research, it seems that we just need to update `config/default/kustomization.yaml` with the kustomize [`replacements`](https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/replacements/) feature in cases 2 and 3 as I show [this commit](https://github.com/kubernetes-sigs/kueue/pull/362/commits/20872ca4e8e7c09247e13ec5014a82e880c459f9). This method is convenient for users because they only need to update one file.
  
  ```diff
  +replacements:
  +  - source:
  +      version: v1
  +      kind: Secret
  +      # If you change `metadata.name` in `config/components/secret.yaml` and
  +      # `internalCertManagement.secretName` in `config/components/manager/controller_manager_config.yaml`,
  +      # you should update the below name.
  +      name: webhook-server-cert
  +      fieldPath: metadata.name
  +    targets:
  +      - select:
  +          group: apps
  +          kind: Deployment
  +          name: controller-manager
  +        fieldPaths:
  +          - spec.template.spec.volumes.0.secret.secretName
  +        options:
  +          create: true
  +  - source:
  +      version: v1
  +      kind: Service
  +      # If you change `metadata.name` in `config/components/secret.yaml` and
  +      # `internalCertManagement.serviceName` in `config/components/manager/controller_manager_config.yaml`,
  +      # you should update the below name.
  +      name: webhook-service
  +      fieldPath: metadata.name
  +    targets:
  +      - select:
  +          group: admissionregistration.k8s.io
  +          kind: MutatingWebhookConfiguration
  +        fieldPaths:
  +          - webhooks.0.clientConfig.service.name
  +          - webhooks.1.clientConfig.service.name
  +          - webhooks.2.clientConfig.service.name
  +        options:
  +          create: true
  +      - select:
  +          group: admissionregistration.k8s.io
  +          kind: ValidatingWebhookConfiguration
  +        fieldPaths:
  +          - webhooks.0.clientConfig.service.name
  +          - webhooks.1.clientConfig.service.name
  +          - webhooks.2.clientConfig.service.name
  +          - webhooks.3.clientConfig.service.name
  +        options:
  +          create: true
  
   # Adds namespace to all resources.
   namespace: kueue-system
  ...
  ```
  
  In case 1, we can not overwrite structured data in ConfigMap using the kustomize configurations yet, but KEP has been submitted in [this PR](https://github.com/kubernetes-sigs/kustomize/pull/4558) and is under discussion.
  
  So, as you say, it might be a good idea to mention in the documentation to update `config/default/kustomization.yaml` when setting namespace, secretName, and serviceName.
  (I have not added them to the docs yet.)
  
  What do you think about using kustomize [`replacements`](https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/replacements/) feature? @kerthcet

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T17:40:29Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T17:40:28Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  > Actually the namespace helps to control where to deploy the kueue components, besides configure this field, we should also update the config/default/kustomization.yaml, to change the value of namespace.
  >
  > So I think we can leave it outside the internalCertManagement
  
  Thanks for your suggestions. I agree with you.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T17:44:48Z

- **main.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T17:44:47Z
```diff
@@ -102,8 +102,8 @@ func main() {
 
 	certsReady := make(chan struct{})
 
-	if *config.EnableInternalCertManagement {
-		if err = cert.ManageCerts(mgr, certsReady); err != nil {
+	if *config.InternalCertManagement.Enable {
```

  Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T17:49:44Z

- **apis/config/v1alpha2/defaults.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T17:49:43Z
```diff
@@ -31,7 +31,27 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
-	if cfg.EnableInternalCertManagement == nil {
-		cfg.EnableInternalCertManagement = pointer.Bool(true)
+	const (
+		defaultNamespace   = "kueue-system"
+		defaultServiceName = "kueue-webhook-service"
+		defaultSecretName  = "kueue-webhook-server-cert"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T18:00:58Z

- **apis/config/v1alpha2/configuration_types.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T18:00:58Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName *string `json:"serviceName,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T18:01:02Z

- **apis/config/v1alpha2/configuration_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T18:01:02Z
```diff
@@ -39,8 +43,22 @@ type Configuration struct {
 	// unsuspended, they will start immediately.
 	ManageJobsWithoutQueueName bool `json:"manageJobsWithoutQueueName"`
 
-	// EnableInternalCertManagement controls whether to enable internal cert management or not.
+	// InternalCertManagement is configuration for internalCertManagement
+	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+}
+
+type InternalCertManagement struct {
+
+	// Enable controls whether to enable internal cert management or not.
 	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
 	// set it to false. See the user guide for more information.
-	EnableInternalCertManagement *bool `json:"enableInternalCertManagement,omitempty"`
+	Enable *bool `json:"enable,omitempty"`
+
+	// ServiceName is used as part of the DNSName.
+	// Default to kueue-webhook-service.
+	ServiceName *string `json:"serviceName,omitempty"`
+
+	// SecretName is used to store CA and server certs.
+	// Default to kueue-webhook-server-cert.
+	SecretName *string `json:"secretName,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T18:04:30Z

- **main.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T18:04:30Z
```diff
@@ -102,8 +102,8 @@ func main() {
 
 	certsReady := make(chan struct{})
 
-	if *config.EnableInternalCertManagement {
-		if err = cert.ManageCerts(mgr, certsReady); err != nil {
+	if *config.InternalCertManagement.Enable {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-02T18:09:32Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-02T18:09:31Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e test -e apis) -coverprofile cover.out
```

  Yes, It works fine.
  Do you prefer to use `go list ./... | grep -v -e '/test'`?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-03T02:38:24Z

- **Makefile:121** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T01:18:56Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
```

  This is great for fine-gained control, but considering we'll bump versions periodically, maybe we can remove `-e 'apis/kueue/v1alhpa2'`, then no need to update `Makefile` each time when bumping.  cc @alculquicondor

- **main.go:41** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T01:25:50Z
```diff
@@ -38,7 +38,7 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/healthz"
 	"sigs.k8s.io/controller-runtime/pkg/log/zap"
 
-	configv1alpha1 "sigs.k8s.io/kueue/apis/config/v1alpha1"
```

  We should avoid update this each time when bumping versions, but it's out of the scope of this PR, I'll follow this.

- **main_test.go:41** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T01:37:12Z
```diff
@@ -20,6 +20,27 @@ import (
 	"os"
 	"path/filepath"
 	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	componentconfigv1alpha1 "k8s.io/component-base/config/v1alpha1"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	cfg "sigs.k8s.io/controller-runtime/pkg/config/v1alpha1"
+	ctrlconfigv1alpha1 "sigs.k8s.io/controller-runtime/pkg/config/v1alpha1"
+
+	configv1alpha2 "sigs.k8s.io/kueue/apis/config/v1alpha2"
+)
+
+const (
+	defaultHealthProbeAddress = ":8081"
+	defaultMetricsAddress     = ":8080"
+	defaultWebhookPort        = 9443
+	defaultLeaderElectionID   = "c1f6bfd2.kueue.x-k8s.io"
+	defaultNamespace          = "kueue-system"
```

  Or we can directly import defaultNamespace, defaultServiceName, defaultSecretName from `apis/config/v1alpha2`. Like `defaultNamespace = v1alpha2.DefaultNamespace`

- **main_test.go:133** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T01:40:13Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
```

  We defined above.

- **main_test.go:168** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T01:41:07Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
```

  Can this be replaced with `enableDefaultInternalCertManagement`?

- **main_test.go:271** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T01:55:56Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String(defaultServiceName),
+					WebhookSecretName:  pointer.String(defaultSecretName),
+				},
+			},
+			wantOptions: ctrl.Options{
+				Port:                   9443,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(defaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: ":38081",
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: ":38080",
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: "test-id",
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(9444),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				HealthProbeBindAddress: ":38081",
+				MetricsBindAddress:     ":38080",
+				Port:                   9444,
+				LeaderElection:         true,
+				LeaderElectionID:       "test-id",
+			},
 		},
 		{
-			name:                             "cert overwrite config",
-			configFile:                       certOverWriteConfig,
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: false,
+			name:       "cert options overwrite config",
+			configFile: certOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(defaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
```

  If we change the cert, why we still get default ones.

- **main_test.go:192** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T02:14:12Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String(defaultServiceName),
+					WebhookSecretName:  pointer.String(defaultSecretName),
+				},
+			},
+			wantOptions: ctrl.Options{
+				Port:                   9443,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  We can ignore the field when calling diff for this is already checked in `wantOptions`. Code like
  ```
  if diff := cmp.Diff(tc.wantConfiguration, config, cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec")); diff != "" {
  }
  ```

- **main_test.go:106** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T02:21:32Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
```

  It should be webhookServiceName then. Also secretName.

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T02:33:19Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  I'm not quite familiar with Kustomize grammars, so I'd like to leave it to @alculquicondor , in case giving some bad advices. But I'm always for automation.

- **CHANGELOG/CHANGELOG-0.3.md:9** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T02:36:53Z
```diff
@@ -0,0 +1,13 @@
+## v0.3.0
+
+Changes since `v0.2.1`:
+
+### Features
+
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
+  `v1alpha2` includes the following changes:
+  - Add Namespace. 
```

  More details, or it's hard to understand, add namespace for what?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T03:26:26Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T03:26:25Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
```

  That makes sense. I will remove `-e 'apis/kueue/v1alpha2'`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T03:31:41Z

- **main_test.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T03:31:41Z
```diff
@@ -20,6 +20,27 @@ import (
 	"os"
 	"path/filepath"
 	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	componentconfigv1alpha1 "k8s.io/component-base/config/v1alpha1"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	cfg "sigs.k8s.io/controller-runtime/pkg/config/v1alpha1"
+	ctrlconfigv1alpha1 "sigs.k8s.io/controller-runtime/pkg/config/v1alpha1"
+
+	configv1alpha2 "sigs.k8s.io/kueue/apis/config/v1alpha2"
+)
+
+const (
+	defaultHealthProbeAddress = ":8081"
+	defaultMetricsAddress     = ":8080"
+	defaultWebhookPort        = 9443
+	defaultLeaderElectionID   = "c1f6bfd2.kueue.x-k8s.io"
+	defaultNamespace          = "kueue-system"
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T03:32:19Z

- **main_test.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T03:32:19Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
```

  Sure. Thanks for pointing that out!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T03:56:07Z

- **main_test.go:192** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T03:56:07Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String(defaultServiceName),
+					WebhookSecretName:  pointer.String(defaultSecretName),
+				},
+			},
+			wantOptions: ctrl.Options{
+				Port:                   9443,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T04:03:32Z

- **main_test.go:271** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T04:03:32Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String(defaultServiceName),
+					WebhookSecretName:  pointer.String(defaultSecretName),
+				},
+			},
+			wantOptions: ctrl.Options{
+				Port:                   9443,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(defaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: ":38081",
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: ":38080",
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: "test-id",
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(9444),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				HealthProbeBindAddress: ":38081",
+				MetricsBindAddress:     ":38080",
+				Port:                   9444,
+				LeaderElection:         true,
+				LeaderElectionID:       "test-id",
+			},
 		},
 		{
-			name:                             "cert overwrite config",
-			configFile:                       certOverWriteConfig,
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: false,
+			name:       "cert options overwrite config",
+			configFile: certOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(defaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
```

  You're correct.
  As you say in https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r962088288, I missed this bug because I entered the wrong test data.
  
  > It should be webhookServiceName then. Also secretName.
  
  Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T04:04:17Z

- **main_test.go:106** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T04:04:16Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
```

  Yes, that's right.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T04:30:08Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T04:30:08Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test' -e 'apis/kueue/v1alpha2') -coverprofile cover.out
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T04:30:26Z

- **main_test.go:106** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T04:30:26Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T04:30:36Z

- **main_test.go:271** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T04:30:36Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String(defaultServiceName),
+					WebhookSecretName:  pointer.String(defaultSecretName),
+				},
+			},
+			wantOptions: ctrl.Options{
+				Port:                   9443,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(defaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: ":38081",
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: ":38080",
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: "test-id",
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(9444),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				HealthProbeBindAddress: ":38081",
+				MetricsBindAddress:     ":38080",
+				Port:                   9444,
+				LeaderElection:         true,
+				LeaderElectionID:       "test-id",
+			},
 		},
 		{
-			name:                             "cert overwrite config",
-			configFile:                       certOverWriteConfig,
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: false,
+			name:       "cert options overwrite config",
+			configFile: certOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(defaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T08:07:59Z

- **main_test.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:07:59Z
```diff
@@ -20,6 +20,27 @@ import (
 	"os"
 	"path/filepath"
 	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	componentconfigv1alpha1 "k8s.io/component-base/config/v1alpha1"
+	"k8s.io/utils/pointer"
+	ctrl "sigs.k8s.io/controller-runtime"
+	cfg "sigs.k8s.io/controller-runtime/pkg/config/v1alpha1"
+	ctrlconfigv1alpha1 "sigs.k8s.io/controller-runtime/pkg/config/v1alpha1"
+
+	configv1alpha2 "sigs.k8s.io/kueue/apis/config/v1alpha2"
+)
+
+const (
+	defaultHealthProbeAddress = ":8081"
+	defaultMetricsAddress     = ":8080"
+	defaultWebhookPort        = 9443
+	defaultLeaderElectionID   = "c1f6bfd2.kueue.x-k8s.io"
+	defaultNamespace          = "kueue-system"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T08:08:07Z

- **main_test.go:133** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:08:07Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T08:13:41Z

- **main_test.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:13:40Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T08:21:23Z

- **main_test.go:192** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:21:23Z
```diff
@@ -44,51 +89,230 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  serviceName: kueue-tenant-a-webhook-service
+  secretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   9443,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(defaultServiceName),
+		WebhookSecretName:  pointer.String(defaultSecretName),
+	}
+
+	cmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace: pointer.String(defaultNamespace),
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String(defaultServiceName),
+					WebhookSecretName:  pointer.String(defaultSecretName),
+				},
+			},
+			wantOptions: ctrl.Options{
+				Port:                   9443,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T08:24:06Z

- **apis/config/v1alpha2/defaults_test.go:24** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:24:06Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1alpha2
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T08:28:26Z

- **CHANGELOG/CHANGELOG-0.3.md:9** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:28:26Z
```diff
@@ -0,0 +1,13 @@
+## v0.3.0
+
+Changes since `v0.2.1`:
+
+### Features
+
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
+  `v1alpha2` includes the following changes:
+  - Add Namespace. 
```

  Sure, I will add more detailed explanations.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T08:40:57Z

- **CHANGELOG/CHANGELOG-0.3.md:9** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T08:40:57Z
```diff
@@ -0,0 +1,13 @@
+## v0.3.0
+
+Changes since `v0.2.1`:
+
+### Features
+
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
+  `v1alpha2` includes the following changes:
+  - Add Namespace. 
```

  Done.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-03T14:23:12Z

- **docs/setup/install.md:67** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T14:19:13Z
```diff
@@ -64,13 +64,16 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
+
```

  revert this plz.

- **main_test.go:189** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T14:21:48Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  Remove this since we reached a consensus.

- **main_test.go:218** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T14:22:10Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  ditto.

- **main_test.go:253** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T14:22:30Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: ":38081",
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: ":38080",
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: "test-id",
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(9444),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				HealthProbeBindAddress: ":38081",
+				MetricsBindAddress:     ":38080",
+				Port:                   9444,
+				LeaderElection:         true,
+				LeaderElectionID:       "test-id",
+			},
 		},
 		{
-			name:                             "cert overwrite config",
-			configFile:                       certOverWriteConfig,
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: false,
+			name:       "cert options overwrite config",
+			configFile: certOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  ditto

- **main_test.go:286** by [@kerthcet](https://github.com/kerthcet) — 2022-09-03T14:22:42Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: ":38081",
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: ":38080",
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: "test-id",
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(9444),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				HealthProbeBindAddress: ":38081",
+				MetricsBindAddress:     ":38080",
+				Port:                   9444,
+				LeaderElection:         true,
+				LeaderElectionID:       "test-id",
+			},
 		},
 		{
-			name:                             "cert overwrite config",
-			configFile:                       certOverWriteConfig,
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: false,
+			name:       "cert options overwrite config",
+			configFile: certOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String("kueue-tenant-a-webhook-service"),
+					WebhookSecretName:  pointer.String("kueue-tenant-a-webhook-server-cert"),
+				},
+			},
+			wantOptions: defaultControlOptions,
+		},
+		{
+			name:       "disable cert overwrite config",
+			configFile: disableCertOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  ditto

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T15:04:39Z

- **docs/setup/install.md:67** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:04:39Z
```diff
@@ -64,13 +64,16 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
+
```

  Sure.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T15:04:52Z

- **main_test.go:189** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:04:52Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T15:07:44Z

- **docs/setup/install.md:67** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:07:44Z
```diff
@@ -64,13 +64,16 @@ To install a custom-configured released version of Kueue in your cluster, execut
 VERSION=v0.2.1
 wget https://github.com/kubernetes-sigs/kueue/releases/download/$VERSION/manifests.yaml
 ```
+
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T15:07:48Z

- **main_test.go:189** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:07:48Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T15:07:51Z

- **main_test.go:218** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:07:51Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T15:07:55Z

- **main_test.go:253** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:07:54Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: ":38081",
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: ":38080",
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: "test-id",
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(9444),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				HealthProbeBindAddress: ":38081",
+				MetricsBindAddress:     ":38080",
+				Port:                   9444,
+				LeaderElection:         true,
+				LeaderElectionID:       "test-id",
+			},
 		},
 		{
-			name:                             "cert overwrite config",
-			configFile:                       certOverWriteConfig,
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: false,
+			name:       "cert options overwrite config",
+			configFile: certOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-03T15:07:58Z

- **main_test.go:286** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-03T15:07:58Z
```diff
@@ -44,51 +86,234 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
+		),
+	}
+
+	configCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(configv1alpha2.Configuration{}, "ControllerManagerConfigurationSpec"),
+	}
+
 	testcases := []struct {
-		name                             string
-		configFile                       string
-		wantPort                         int
-		wantEnableInternalCertManagement bool
+		name              string
+		configFile        string
+		wantConfiguration configv1alpha2.Configuration
+		wantOptions       ctrl.Options
 	}{
 		{
-			name:                             "default config",
-			configFile:                       "",
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: true,
+			name:       "default config",
+			configFile: "",
+			wantConfiguration: configv1alpha2.Configuration{
+				Namespace:              pointer.String(configv1alpha2.DefaultNamespace),
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				Port:                   defaultWebhookPort,
+				HealthProbeBindAddress: defaultHealthProbeAddress,
+				MetricsBindAddress:     defaultMetricsAddress,
+				LeaderElectionID:       defaultLeaderElectionID,
+				LeaderElection:         false,
+			},
+		},
+		{
+			name:       "namespace overwrite config",
+			configFile: namespaceOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String("kueue-tenant-a"),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: defaultControlOptions,
 		},
 		{
-			name:                             "port overwrite config",
-			configFile:                       portOverWriteConfig,
-			wantPort:                         9444,
-			wantEnableInternalCertManagement: true,
+			name:       "ControllerManagerConfigurationSpec overwrite config",
+			configFile: ctrlManagerConfigSpecOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: ":38081",
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: ":38080",
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: "test-id",
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(9444),
+					},
+				},
+				InternalCertManagement: enableDefaultInternalCertManagement,
+			},
+			wantOptions: ctrl.Options{
+				HealthProbeBindAddress: ":38081",
+				MetricsBindAddress:     ":38080",
+				Port:                   9444,
+				LeaderElection:         true,
+				LeaderElectionID:       "test-id",
+			},
 		},
 		{
-			name:                             "cert overwrite config",
-			configFile:                       certOverWriteConfig,
-			wantPort:                         9443,
-			wantEnableInternalCertManagement: false,
+			name:       "cert options overwrite config",
+			configFile: certOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: defaultHealthProbeAddress,
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: defaultMetricsAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: defaultLeaderElectionID,
+					},
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(defaultWebhookPort),
+					},
+				},
+				InternalCertManagement: &configv1alpha2.InternalCertManagement{
+					Enable:             pointer.Bool(true),
+					WebhookServiceName: pointer.String("kueue-tenant-a-webhook-service"),
+					WebhookSecretName:  pointer.String("kueue-tenant-a-webhook-server-cert"),
+				},
+			},
+			wantOptions: defaultControlOptions,
+		},
+		{
+			name:       "disable cert overwrite config",
+			configFile: disableCertOverWriteConfig,
+			wantConfiguration: configv1alpha2.Configuration{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: configv1alpha2.GroupVersion.String(),
+					Kind:       "Configuration",
+				},
+				Namespace:                  pointer.String(configv1alpha2.DefaultNamespace),
+				ManageJobsWithoutQueueName: false,
+				ControllerManagerConfigurationSpec: cfg.ControllerManagerConfigurationSpec{
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-06T19:59:12Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-06T19:59:12Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  This conversation has derailed in ways I don't understand :)
  
  I don't think it's a primary use case to change the namespace name or service name, so I think we can leave it out of the documentation. IMO, it suffices to leave the hooks in the configuration to let users change the parameters without having to rebuild the image, **if they really need to**.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T03:10:21Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T03:10:21Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  > I don't think it's a primary use case to change the namespace name or service name, so I think we can leave it out of the documentation. IMO, it suffices to leave the hooks in the configuration to let users change the parameters without having to rebuild the image, if they really need to.
  
  @alculquicondor That makes sense.
  I will move [these configurations](https://github.com/kubernetes-sigs/kueue/blob/20872ca4e8e7c09247e13ec5014a82e880c459f9/config/default/kustomization.yaml#L1-L46) to comments.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T08:51:22Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T08:51:21Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-07T13:59:08Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T13:59:08Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  The initial conclusion of this thread was to move Namespace into `InternalCertManagement` as `WebhookServiceNamespace`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T17:37:45Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T17:37:45Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  @alculquicondor 
  As described in [this](https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r961557630) comment by @kerthcet, I and @kerthcet  updated the conclusion that we should include the namespace in struct `Configuration` because it represents the namespace where kueue components are deployed.
  
  > Actually the namespace helps to control where to deploy the kueue components, besides configure this field, we should also update the config/default/kustomization.yaml, to change the value of namespace.
  > 
  > So I think we can leave it outside the internalCertManagement

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-07T17:41:38Z

- **apis/config/v1alpha2/configuration_types.go:30** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T17:41:37Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
```

  ```suggestion
  	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T17:46:37Z

- **apis/config/v1alpha2/configuration_types.go:30** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T17:46:37Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
```

  Thanks for this suggestion!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-07T17:59:26Z

- **config/default/kustomization.yaml:77** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T17:59:26Z
```diff
@@ -73,3 +73,46 @@ patchesStrategicMerge:
 #     kind: Service
 #     version: v1
 #     name: webhook-service
+
+# If you change `metadata.name` in `config/components/secret.yaml` and `internalCertManagement.secretName` in `config/components/manager/controller_manager_config.yaml`,
```

  Just remove these instructions. It's not a primary use case.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T18:05:57Z

- **config/default/kustomization.yaml:77** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T18:05:57Z
```diff
@@ -73,3 +73,46 @@ patchesStrategicMerge:
 #     kind: Service
 #     version: v1
 #     name: webhook-service
+
+# If you change `metadata.name` in `config/components/secret.yaml` and `internalCertManagement.secretName` in `config/components/manager/controller_manager_config.yaml`,
```

  Sure.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-07T18:06:00Z

- **docs/setup/install.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T17:59:44Z
```diff
@@ -15,7 +15,7 @@ if you don't have your own monitoring system.
 
 The webhook server in kueue uses an internal cert management for provisioning certificates. If you want to use
   a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
-  1. Set `enableInternalCertManagement` to `false` in [config file](#install-a-custom-configured-released-version).
+  1. Set `internalCertManagement.Enable` to `false` in [config file](#install-a-custom-configured-released-version).
```

  ```suggestion
    1. Set `internalCertManagement.enable` to `false` in [config file](#install-a-custom-configured-released-version).
  ```

- **docs/setup/install.md:74** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T18:00:04Z
```diff
@@ -71,6 +71,8 @@ the default Kueue Configuration
 struct ([v1alpha1@v0.2.1](https://pkg.go.dev/sigs.k8s.io/kueue@v0.2.1/apis/config/v1alpha1#Configuration)).
 The contents of the ConfigMap are similar to the following:
 
+__ `namespace` and `internalCertManagement` fields are available in Kueue v0.3.0 and later__
```

  ```suggestion
  __The `namespace` and `internalCertManagement` fields are available in Kueue v0.3.0 and later__
  ```

- **docs/setup/install.md:86** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T18:00:34Z
```diff
@@ -81,13 +83,18 @@ data:
   controller_manager_config.yaml: |
     apiVersion: config.kueue.x-k8s.io/v1alpha1
     kind: Configuration
+    namespace: kueue-tenant-a
```

  ```suggestion
      namespace: kueue-system
  ```

- **docs/setup/install.md:96** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T18:00:47Z
```diff
@@ -81,13 +83,18 @@ data:
   controller_manager_config.yaml: |
     apiVersion: config.kueue.x-k8s.io/v1alpha1
     kind: Configuration
+    namespace: kueue-tenant-a
     health:
       healthProbeBindAddress: :8081
     metrics:
       bindAddress: :8080
     webhook:
       port: 9443
     manageJobsWithoutQueueName: true
+    internalCertManagement:
+      enable: true
+      webhookServiceName: kueue-tenant-a-service
```

  leave the default names here

- **main_test.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T18:04:47Z
```diff
@@ -44,51 +83,174 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
```

  use `ignoreUnexportedFields` instead

- **apis/config/v1alpha2/configuration_types.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T18:05:57Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  I think we can avoid the pointers.
  
  If the field is empty `len(field)==0`, you can set the default.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T18:09:47Z

- **config/default/kustomization.yaml:77** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T18:09:46Z
```diff
@@ -73,3 +73,46 @@ patchesStrategicMerge:
 #     kind: Service
 #     version: v1
 #     name: webhook-service
+
+# If you change `metadata.name` in `config/components/secret.yaml` and `internalCertManagement.secretName` in `config/components/manager/controller_manager_config.yaml`,
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T18:19:38Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T18:19:38Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  As described in [this](https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r959236593) by @kerthcet, I changed it to *string since this field is optional. Should I change it to string?
  
  > Since this is optional, maybe *string will be better.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-07T19:37:51Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-07T19:37:51Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed and is also used as part of DNSName.
+	// Defaults to kueue-system.
+	Namespace string `json:"namespace,omitempty"`
```

  `*string` is necessary when the empty string is a valid value. But this is not the case for these fields. Empty means not-set, and we can set the default.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:38:24Z

- **docs/setup/install.md:18** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:38:24Z
```diff
@@ -15,7 +15,7 @@ if you don't have your own monitoring system.
 
 The webhook server in kueue uses an internal cert management for provisioning certificates. If you want to use
   a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
-  1. Set `enableInternalCertManagement` to `false` in [config file](#install-a-custom-configured-released-version).
+  1. Set `internalCertManagement.Enable` to `false` in [config file](#install-a-custom-configured-released-version).
```

  Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:38:49Z

- **docs/setup/install.md:74** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:38:49Z
```diff
@@ -71,6 +71,8 @@ the default Kueue Configuration
 struct ([v1alpha1@v0.2.1](https://pkg.go.dev/sigs.k8s.io/kueue@v0.2.1/apis/config/v1alpha1#Configuration)).
 The contents of the ConfigMap are similar to the following:
 
+__ `namespace` and `internalCertManagement` fields are available in Kueue v0.3.0 and later__
```

  Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:39:09Z

- **docs/setup/install.md:96** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:39:09Z
```diff
@@ -81,13 +83,18 @@ data:
   controller_manager_config.yaml: |
     apiVersion: config.kueue.x-k8s.io/v1alpha1
     kind: Configuration
+    namespace: kueue-tenant-a
     health:
       healthProbeBindAddress: :8081
     metrics:
       bindAddress: :8080
     webhook:
       port: 9443
     manageJobsWithoutQueueName: true
+    internalCertManagement:
+      enable: true
+      webhookServiceName: kueue-tenant-a-service
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:40:10Z

- **docs/setup/install.md:18** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:40:10Z
```diff
@@ -15,7 +15,7 @@ if you don't have your own monitoring system.
 
 The webhook server in kueue uses an internal cert management for provisioning certificates. If you want to use
   a third-party one, e.g. [cert-manager](https://github.com/cert-manager/cert-manager), follow these steps:
-  1. Set `enableInternalCertManagement` to `false` in [config file](#install-a-custom-configured-released-version).
+  1. Set `internalCertManagement.Enable` to `false` in [config file](#install-a-custom-configured-released-version).
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:40:35Z

- **docs/setup/install.md:74** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:40:34Z
```diff
@@ -71,6 +71,8 @@ the default Kueue Configuration
 struct ([v1alpha1@v0.2.1](https://pkg.go.dev/sigs.k8s.io/kueue@v0.2.1/apis/config/v1alpha1#Configuration)).
 The contents of the ConfigMap are similar to the following:
 
+__ `namespace` and `internalCertManagement` fields are available in Kueue v0.3.0 and later__
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:41:13Z

- **docs/setup/install.md:86** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:41:13Z
```diff
@@ -81,13 +83,18 @@ data:
   controller_manager_config.yaml: |
     apiVersion: config.kueue.x-k8s.io/v1alpha1
     kind: Configuration
+    namespace: kueue-tenant-a
```

  Thanks. 
  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:41:41Z

- **docs/setup/install.md:96** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:41:41Z
```diff
@@ -81,13 +83,18 @@ data:
   controller_manager_config.yaml: |
     apiVersion: config.kueue.x-k8s.io/v1alpha1
     kind: Configuration
+    namespace: kueue-tenant-a
     health:
       healthProbeBindAddress: :8081
     metrics:
       bindAddress: :8080
     webhook:
       port: 9443
     manageJobsWithoutQueueName: true
+    internalCertManagement:
+      enable: true
+      webhookServiceName: kueue-tenant-a-service
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:55:17Z

- **main_test.go:147** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:55:17Z
```diff
@@ -44,51 +83,174 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
```

  That makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-07T21:59:27Z

- **main_test.go:147** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-07T21:59:27Z
```diff
@@ -44,51 +83,174 @@ webhook:
 
 	certOverWriteConfig := filepath.Join(tmpDir, "cert-overwrite.yaml")
 	if err := os.WriteFile(certOverWriteConfig, []byte(`
-apiVersion: config.kueue.x-k8s.io/v1alpha1
+apiVersion: config.kueue.x-k8s.io/v1alpha2
 kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
 leaderElection:
   leaderElect: true
-enableInternalCertManagement: false
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: true
+  webhookServiceName: kueue-tenant-a-webhook-service
+  webhookSecretName: kueue-tenant-a-webhook-server-cert
 `), os.FileMode(0600)); err != nil {
 		t.Fatal(err)
 	}
 
+	disableCertOverWriteConfig := filepath.Join(tmpDir, "disable-cert-overwrite.yaml")
+	if err := os.WriteFile(disableCertOverWriteConfig, []byte(`
+apiVersion: config.kueue.x-k8s.io/v1alpha2
+kind: Configuration
+namespace: kueue-system
+health:
+  healthProbeBindAddress: :8081
+metrics:
+  bindAddress: :8080
+leaderElection:
+  leaderElect: true
+  resourceName: c1f6bfd2.kueue.x-k8s.io
+webhook:
+  port: 9443
+internalCertManagement:
+  enable: false
+`), os.FileMode(0600)); err != nil {
+		t.Fatal(err)
+	}
+
+	defaultControlOptions := ctrl.Options{
+		Port:                   defaultWebhookPort,
+		HealthProbeBindAddress: defaultHealthProbeAddress,
+		MetricsBindAddress:     defaultMetricsAddress,
+		LeaderElectionID:       defaultLeaderElectionID,
+		LeaderElection:         true,
+	}
+
+	enableDefaultInternalCertManagement := &configv1alpha2.InternalCertManagement{
+		Enable:             pointer.Bool(true),
+		WebhookServiceName: pointer.String(configv1alpha2.DefaultWebhookServiceName),
+		WebhookSecretName:  pointer.String(configv1alpha2.DefaultWebhookSecretName),
+	}
+
+	ctrlOptsCmpOpts := []cmp.Option{
+		cmpopts.IgnoreFields(ctrl.Options{}, "Scheme",
+			"makeBroadcaster",
+			"Logger",
+			"newRecorderProvider",
+			"newResourceLock",
+			"newMetricsListener",
+			"newHealthProbeListener",
```

  Done.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-08T02:26:13Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-08T02:26:12Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  Still think pointer is a better choice since this is optional, if we set to string, `len(field) == 0` means both unset or misconfiguration of empty string. We can't tell the difference.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-08T02:38:50Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@kerthcet](https://github.com/kerthcet) — 2022-09-08T02:38:50Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  Do we have any obviously disadvantages by using pointer?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-08T04:11:10Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-08T04:11:10Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-08T16:13:06Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T16:13:06Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  empty string is not a valid value, so we can override it during defaulting. Using pointer just adds extra code that we can avoid. But I'm ok with it, if you feel strongly about it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-08T19:42:52Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-08T19:42:52Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  I agree with @kerthcet. So, I'd like to use `*string`.
  Do you have any concerns? @kerthcet

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-08T19:50:31Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:50:31Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  sg, please squash.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-08T20:20:10Z

- **apis/config/v1alpha2/configuration_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-08T20:20:10Z
```diff
@@ -27,6 +27,10 @@ import (
 type Configuration struct {
 	metav1.TypeMeta `json:",inline"`
 
+	// Namespace is the namespace in which kueue is deployed. It is used as part of DNSName of the webhook Service.
+	// Defaults to kueue-system.
+	Namespace *string `json:"namespace,omitempty"`
```

  I have squashed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-09T14:42:04Z

Awesome

/lgtm
/approve

- **Makefile:121** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T13:47:42Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test') -coverprofile cover.out
```

  nit:
  
  ```suggestion
  	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v '/test/') -coverprofile cover.out
  ```
  
  Just in case we have another package somewhere else that has `test` in the name.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-09T14:47:52Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-09T14:47:52Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test') -coverprofile cover.out
```

  Nice catch! Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-09T15:11:37Z

- **Makefile:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-09T15:11:37Z
```diff
@@ -118,11 +118,11 @@ vet: ## Run go vet against code.
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	$(GO_CMD) test $(GO_TEST_FLAGS) ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test $(GO_TEST_FLAGS) $(shell go list ./... | grep -v -e 'test') -coverprofile cover.out
```

  Followed up in #383.
