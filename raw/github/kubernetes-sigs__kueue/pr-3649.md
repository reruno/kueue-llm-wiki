# PR #3649: TAS: Rank-ordering support for Pod groups

**Summary**: TAS: Rank-ordering support for Pod groups

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3649

**Last updated**: 2024-11-28T15:59:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `pod-group-label` → `main`
- **Created**: 2024-11-26T09:57:51Z
- **Updated**: 2024-11-28T15:59:19Z
- **Merged**: 2024-11-28T09:08:57Z
- **Closed**: 2024-11-28T09:08:57Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Changed files**: 8   **+338 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
- Adds a new label for Pod groups `"kueue.x-k8s.io/pod-group-index"`
- Adds TAS rank-ordering support for Pod groups 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3533 


#### Special notes for your reviewer:
NONE

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support rank-based ordering of Pods with Topology-Aware Scheduling. 
The Pod indexes are determined based on the "kueue.x-k8s.io/pod-group-index" label which
can be set by an external controller managing the group.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-26T09:57:53Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T09:58:02Z

/assign @mimowo

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-26T09:58:12Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2974e1c4042ae68eb7670b30bba984096d08c173 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67482eaae6f22100082ccb8d |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3649--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3649--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTM2NDktLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.C0U3dsbdWsQMJ_UYn2WBJC-6tNIe2uSN09cbMtrCbsI)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3649--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-26T15:29:48Z

LGTM label has been added.  <details>Git tree hash: ab5296513f436fa3b3f495365c4fb3fade76154e</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-26T15:36:27Z

Actually, the PodGroups as such were already supported by TAS, just not rank-based ordering.

/release-note-edit
```release-note
Support rank-based ordering of Pods with Topology-Aware Scheduling. 
The Pod indexes are determined based on the "kueue.x-k8s.io/pod-group-index" label which
can be set by an external controller managing the group.
```

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T16:27:57Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-27T16:52:57Z

The build is failing after the other PR: https://github.com/kubernetes-sigs/kueue/pull/3656. Please rebase and use `corev1.LabelHostname`

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-28T09:08:08Z

/lgtm
/approve
@tenzen-y I believe all your comments since https://github.com/kubernetes-sigs/kueue/pull/3649#pullrequestreview-2464833242 are addressed, so merging to move forward, and unblock the follow up work on https://github.com/kubernetes-sigs/kueue/issues/3663. If you have some extra last minute comments feel free to still add them even after merged.
/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T09:08:14Z

LGTM label has been added.  <details>Git tree hash: fa53e63ff952ffaa065e789e81bc284e4e1bee84</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T09:08:16Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3649#issuecomment-2505603928" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3649#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T10:55:25Z

- **pkg/controller/jobs/pod/pod_controller.go:564** by [@mimowo](https://github.com/mimowo) — 2024-11-26T10:55:24Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
```

  This is something we could pass down from the context to avoid parsing again

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T10:59:46Z

- **pkg/controller/jobs/pod/pod_controller.go:575** by [@mimowo](https://github.com/mimowo) — 2024-11-26T10:59:46Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return nil, fmt.Errorf("pod group total count is invalid: %v", err)
+	}
+	groupIndex, ok := p.Object().GetAnnotations()[GroupIndexLabel]
+	if !ok {
+		return nil, nil
+	}
+	groupIndexValue, err := strconv.Atoi(groupIndex)
+	if err != nil {
+		return nil, err
+	}
+	if groupIndexValue <= podGroupTotalCount {
+		return nil, fmt.Errorf("incorrect annotation value '%s=%s': group index should be less than group total count",
+			GroupIndexLabel, groupIndex)
+	}
+	if groupIndexValue < 0 {
```

  A similar pattern repeats also in topology_ungater (and I just realize we don't check this is >=0 there) and probably other places. What about creating a set of helper functions for parsing labels and annotations. I think this could be `ReadUIntFromLabel()`, or even `ReadUIntFromLabelWithMax`.
  
  I think a follow up might be better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T11:01:28Z

- **pkg/util/testingjobs/pod/wrappers.go:128** by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:01:28Z
```diff
@@ -109,6 +123,11 @@ func (p *PodWrapper) GroupTotalCount(gtc string) *PodWrapper {
 	return p.Annotation("kueue.x-k8s.io/pod-group-total-count", gtc)
 }
 
+// GroupIndex updates the pod.GroupIndexLabel of the Pod
+func (p *PodWrapper) GroupIndex(index string) *PodWrapper {
+	return p.Label("kueue.x-k8s.io/pod-group-index", index)
```

  Can we use the constant?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T11:02:37Z

- **pkg/controller/jobs/pod/pod_controller.go:568** by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:02:36Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return nil, fmt.Errorf("pod group total count is invalid: %v", err)
+	}
+	groupIndex, ok := p.Object().GetAnnotations()[GroupIndexLabel]
```

  Why reading from annotations actually, not labels? I suspect this is actually a bug. Please add some form of test for it.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T11:08:19Z

- **pkg/util/testingjobs/pod/wrappers.go:128** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T11:08:19Z
```diff
@@ -109,6 +123,11 @@ func (p *PodWrapper) GroupTotalCount(gtc string) *PodWrapper {
 	return p.Annotation("kueue.x-k8s.io/pod-group-total-count", gtc)
 }
 
+// GroupIndex updates the pod.GroupIndexLabel of the Pod
+func (p *PodWrapper) GroupIndex(index string) *PodWrapper {
+	return p.Label("kueue.x-k8s.io/pod-group-index", index)
```

  Unfortunately there is a dependency cycle that prevents from doing that, and I wanted to avoid bigger refactor in this PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T11:09:03Z

- **pkg/controller/jobs/pod/pod_webhook.go:51** by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:09:03Z
```diff
@@ -48,6 +48,7 @@ const (
 	PodFinalizer                 = ManagedLabelKey
 	GroupNameLabel               = "kueue.x-k8s.io/pod-group-name"
 	GroupTotalCountAnnotation    = "kueue.x-k8s.io/pod-group-total-count"
+	GroupIndexLabel              = "kueue.x-k8s.io/pod-group-index"
```

  no strong view here, but imo `pod-group-index` and the var names `PodGroupIndex` are misleadingly suggesting this is index of the pod group. 
  
  I'm thinking about alternatives such as `pod-group-pod-index` or `kueue.x-k8s.io/pod-index`. Any preference here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T11:10:18Z

- **pkg/util/testingjobs/pod/wrappers.go:128** by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:10:18Z
```diff
@@ -109,6 +123,11 @@ func (p *PodWrapper) GroupTotalCount(gtc string) *PodWrapper {
 	return p.Annotation("kueue.x-k8s.io/pod-group-total-count", gtc)
 }
 
+// GroupIndex updates the pod.GroupIndexLabel of the Pod
+func (p *PodWrapper) GroupIndex(index string) *PodWrapper {
+	return p.Label("kueue.x-k8s.io/pod-group-index", index)
```

  sgtm, I think we could follow up on this (e.g. move all the consts to the API package)
  
  EDIT: this is what we do in the core k8s; https://github.com/kubernetes/kubernetes/blob/8770bd58d04555303a3a15b30c245a58723d0f4a/pkg/apis/batch/types.go#L25-L57

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T11:14:11Z

- **pkg/controller/jobs/pod/pod_controller.go:575** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T11:14:10Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return nil, fmt.Errorf("pod group total count is invalid: %v", err)
+	}
+	groupIndex, ok := p.Object().GetAnnotations()[GroupIndexLabel]
+	if !ok {
+		return nil, nil
+	}
+	groupIndexValue, err := strconv.Atoi(groupIndex)
+	if err != nil {
+		return nil, err
+	}
+	if groupIndexValue <= podGroupTotalCount {
+		return nil, fmt.Errorf("incorrect annotation value '%s=%s': group index should be less than group total count",
+			GroupIndexLabel, groupIndex)
+	}
+	if groupIndexValue < 0 {
```

  Good point, I've created an issue for that: https://github.com/kubernetes-sigs/kueue/issues/3651

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T11:21:14Z

- **pkg/controller/jobs/pod/pod_controller.go:575** by [@mimowo](https://github.com/mimowo) — 2024-11-26T11:21:14Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return nil, fmt.Errorf("pod group total count is invalid: %v", err)
+	}
+	groupIndex, ok := p.Object().GetAnnotations()[GroupIndexLabel]
+	if !ok {
+		return nil, nil
+	}
+	groupIndexValue, err := strconv.Atoi(groupIndex)
+	if err != nil {
+		return nil, err
+	}
+	if groupIndexValue <= podGroupTotalCount {
+		return nil, fmt.Errorf("incorrect annotation value '%s=%s': group index should be less than group total count",
+			GroupIndexLabel, groupIndex)
+	}
+	if groupIndexValue < 0 {
```

  Ok, when we have `ReadUIntFromLabelWithMax` I think it could also be used here.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T11:23:20Z

- **pkg/controller/jobs/pod/pod_controller.go:568** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T11:23:20Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return nil, fmt.Errorf("pod group total count is invalid: %v", err)
+	}
+	groupIndex, ok := p.Object().GetAnnotations()[GroupIndexLabel]
```

  Good catch, fixed. Test is in progress

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T13:44:31Z

- **pkg/controller/jobs/pod/pod_controller.go:568** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T13:44:31Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return nil, fmt.Errorf("pod group total count is invalid: %v", err)
+	}
+	groupIndex, ok := p.Object().GetAnnotations()[GroupIndexLabel]
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T13:44:37Z

- **pkg/controller/jobs/pod/pod_webhook.go:51** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T13:44:37Z
```diff
@@ -48,6 +48,7 @@ const (
 	PodFinalizer                 = ManagedLabelKey
 	GroupNameLabel               = "kueue.x-k8s.io/pod-group-name"
 	GroupTotalCountAnnotation    = "kueue.x-k8s.io/pod-group-total-count"
+	GroupIndexLabel              = "kueue.x-k8s.io/pod-group-index"
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T13:44:53Z

- **pkg/controller/jobs/pod/pod_controller.go:564** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T13:44:53Z
```diff
@@ -556,6 +556,34 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex() (*int, error) {
+	if podGroupName(p.pod) == "" {
+		return nil, fmt.Errorf("pod doesn't have a '%s' label", GroupNameLabel)
+	}
+	podGroupTotalCount, err := p.groupTotalCount()
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T13:49:49Z

- **pkg/util/testingjobs/pod/wrappers.go:128** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T13:49:49Z
```diff
@@ -109,6 +123,11 @@ func (p *PodWrapper) GroupTotalCount(gtc string) *PodWrapper {
 	return p.Annotation("kueue.x-k8s.io/pod-group-total-count", gtc)
 }
 
+// GroupIndex updates the pod.GroupIndexLabel of the Pod
+func (p *PodWrapper) GroupIndex(index string) *PodWrapper {
+	return p.Label("kueue.x-k8s.io/pod-group-index", index)
```

  Good point, created an issue for that:
  https://github.com/kubernetes-sigs/kueue/issues/3653

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T14:05:37Z

- **pkg/controller/jobs/pod/pod_controller.go:564** by [@mimowo](https://github.com/mimowo) — 2024-11-26T14:05:37Z
```diff
@@ -556,6 +559,25 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex(podGroupTotalCount int) (*int, error) {
+	groupIndex, ok := p.Object().GetLabels()[kueuealpha.PodGroupPodIndexLabel]
```

  ```suggestion
  	podIndex, ok := p.Object().GetLabels()[kueuealpha.PodGroupPodIndexLabel]
  ```
  nit: I think the "group" is already clear from the context

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T14:08:03Z

- **apis/kueue/v1alpha1/tas_types.go:65** by [@mimowo](https://github.com/mimowo) — 2024-11-26T14:08:02Z
```diff
@@ -59,6 +59,10 @@ const (
 	// from the Job's PodTemplate also have the label. For the Pod-based
 	// integrations the label is added in webhook during the Pod creation.
 	TASLabel = "kueue.x-k8s.io/tas"
+
+	// PodGroupPodIndexLabel is a label set on the Pod's metadata belonging
+	// to a Pod group. It indicates the Pod's index within the group.
+	PodGroupPodIndexLabel = "kueue.x-k8s.io/pod-group-pod-index"
```

  nit: please also update the KEP with the new label

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T14:13:27Z

- **pkg/controller/jobs/pod/pod_controller.go:564** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T14:13:26Z
```diff
@@ -556,6 +559,25 @@ func (p *Pod) groupTotalCount() (int, error) {
 	return gtc, nil
 }
 
+// podGroupIndex returns the value of GroupIndexLabel for the pod being reconciled at the moment.
+func (p *Pod) podGroupIndex(podGroupTotalCount int) (*int, error) {
+	groupIndex, ok := p.Object().GetLabels()[kueuealpha.PodGroupPodIndexLabel]
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T14:13:33Z

- **apis/kueue/v1alpha1/tas_types.go:65** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T14:13:33Z
```diff
@@ -59,6 +59,10 @@ const (
 	// from the Job's PodTemplate also have the label. For the Pod-based
 	// integrations the label is added in webhook during the Pod creation.
 	TASLabel = "kueue.x-k8s.io/tas"
+
+	// PodGroupPodIndexLabel is a label set on the Pod's metadata belonging
+	// to a Pod group. It indicates the Pod's index within the group.
+	PodGroupPodIndexLabel = "kueue.x-k8s.io/pod-group-pod-index"
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T15:29:43Z

/lgtm
/approve
/hold
in case @tenzen-y has some more comments

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T12:24:01Z

Basically, lgtm

- **keps/976-plain-pods/README.md:428** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:07:12Z
```diff
@@ -425,6 +425,7 @@ order to create a Workload object.
 To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
+- The label `kueue.x-k8s.io/pod-group-pod-index`, to indicate an index of a pod within the group
```

  Is this a required label for PodGroup? Or optional label?

- **keps/976-plain-pods/README.md:428** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:08:04Z
```diff
@@ -425,6 +425,7 @@ order to create a Workload object.
 To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
+- The label `kueue.x-k8s.io/pod-group-pod-index`, to indicate an index of a pod within the group
```

  Additionally, could you update the reference doc?
  https://kueue.sigs.k8s.io/docs/reference/labels-and-annotations/

- **pkg/controller/jobs/pod/pod_controller_test.go:96** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:18:54Z
```diff
@@ -93,6 +93,53 @@ func TestPodsReady(t *testing.T) {
 	}
 }
 
+func TestPodIndex(t *testing.T) {
```

  Basically, we should add unit tests against top-level functions. In this case, we should add these tests to the `TestReconciler`.
  
  If we continue to add unit testings against all unexported functions, maintenance costs for unit testings will significantly grow.

- **pkg/controller/tas/topology_ungater.go:413** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:23:50Z
```diff
@@ -430,6 +430,10 @@ func determineRanksLookup(pod *corev1.Pod) (*string, *replicatedJobsInfo) {
 			replicasCount: *jobCount,
 		}
 	}
+	// Check if this is Pod group
+	if jobCount, _ := readIntFromLabel(pod, kueuealpha.PodGroupPodIndexLabel); jobCount != nil {
```

  What if an incoming Pod has both a JobSet replica label (or Job Index annotation) and a Pod Index label?
  I can imagine a situation where the custom Jobs relied on the JobSet, but the Kueue integration for the custom Jobs relied on the Pod Group.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T12:55:46Z

- **pkg/controller/tas/topology_ungater.go:413** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T12:55:46Z
```diff
@@ -430,6 +430,10 @@ func determineRanksLookup(pod *corev1.Pod) (*string, *replicatedJobsInfo) {
 			replicasCount: *jobCount,
 		}
 	}
+	// Check if this is Pod group
+	if jobCount, _ := readIntFromLabel(pod, kueuealpha.PodGroupPodIndexLabel); jobCount != nil {
```

  Can you elaborate on the example? How does one job use both JobSet and Pod Group?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T12:56:13Z

- **keps/976-plain-pods/README.md:428** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T12:56:13Z
```diff
@@ -425,6 +425,7 @@ order to create a Workload object.
 To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
+- The label `kueue.x-k8s.io/pod-group-pod-index`, to indicate an index of a pod within the group
```

  Optional, I've clarified that and updated the reference doc

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T13:03:34Z

- **pkg/controller/tas/topology_ungater.go:413** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T13:03:34Z
```diff
@@ -430,6 +430,10 @@ func determineRanksLookup(pod *corev1.Pod) (*string, *replicatedJobsInfo) {
 			replicasCount: *jobCount,
 		}
 	}
+	// Check if this is Pod group
+	if jobCount, _ := readIntFromLabel(pod, kueuealpha.PodGroupPodIndexLabel); jobCount != nil {
```

  Let's say that we have XXXCustomJob which directly creates only batch/v1 Job. However, we do not have Kueue integration controllers for the XXXCustomJob, and batch users enqueue the XXXCustomJob by using PodGroup integration.
  
  In that case, the Pod has both batch/v1 Job specific annotation and PodGroup indexed labels. 
  What happens for this XXXCustomJob with TAS?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T13:20:29Z

- **pkg/controller/jobs/pod/pod_controller_test.go:96** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T13:20:29Z
```diff
@@ -93,6 +93,53 @@ func TestPodsReady(t *testing.T) {
 	}
 }
 
+func TestPodIndex(t *testing.T) {
```

  Good point, fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T13:30:06Z

- **pkg/controller/tas/topology_ungater.go:413** by [@mimowo](https://github.com/mimowo) — 2024-11-27T13:30:06Z
```diff
@@ -430,6 +430,10 @@ func determineRanksLookup(pod *corev1.Pod) (*string, *replicatedJobsInfo) {
 			replicasCount: *jobCount,
 		}
 	}
+	// Check if this is Pod group
+	if jobCount, _ := readIntFromLabel(pod, kueuealpha.PodGroupPodIndexLabel); jobCount != nil {
```

  I think I know what you mean, but this is a problem not specific to PodGroups, but in general with the support for external Jobs, for which we have a dedicated issue: https://github.com/kubernetes-sigs/kueue/issues/3663. 
  
  To directly answer the question, if we have `XXXCustomJob` which creates multiple groups (or replicated job instances) as JobSet does, there will be conflicts in the indexes (multiple pods with index 0). TopologyUngater will notice this situation [here](https://github.com/kubernetes-sigs/kueue/blob/72e44deaa5565a09f118e841b7f3c22a16ebebc2/pkg/controller/tas/topology_ungater.go#L478-L481) and will disable rank-based ordering for the Job entirely. 
  
  This is not ideal, and thus the issue. I think it would be great to solve it before 0.10 so that we have the APIs needed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-28T15:54:03Z

- **pkg/controller/tas/topology_ungater.go:413** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-28T15:54:03Z
```diff
@@ -430,6 +430,10 @@ func determineRanksLookup(pod *corev1.Pod) (*string, *replicatedJobsInfo) {
 			replicasCount: *jobCount,
 		}
 	}
+	// Check if this is Pod group
+	if jobCount, _ := readIntFromLabel(pod, kueuealpha.PodGroupPodIndexLabel); jobCount != nil {
```

  Perfect. That is my looking answer. Thank you.
  Let's address those problems in #3663.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-28T15:59:19Z

- **site/content/en/docs/reference/labels-and-annotations.md:123** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-28T15:59:18Z
```diff
@@ -116,6 +116,15 @@ Used on: [Plain Pods](/docs/tasks/run/plain_pods/).
 
 The annotation key is used to indicate how many Pods to expect in the group.
 
+### `kueue.x-k8s.io/pod-group-pod-index
+
+Type: Label
+
+Example: ``kueue.x-k8s.io/pod-group-pod-index: "0"`
```

  The code format does not seem to be correct.
  
  ```suggestion
  Example: `kueue.x-k8s.io/pod-group-pod-index: "0"`
  ```
  
  I fixed it in https://github.com/kubernetes-sigs/kueue/pull/3682
