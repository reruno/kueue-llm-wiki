# PR #3652: KEP-2936: LocalQueue defaulting

**Summary**: KEP-2936: LocalQueue defaulting

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3652

**Last updated**: 2024-12-03T14:57:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Branch**: `lq-default-kep` → `main`
- **Created**: 2024-11-26T12:00:48Z
- **Updated**: 2024-12-03T14:57:03Z
- **Merged**: 2024-12-03T14:57:01Z
- **Closed**: 2024-12-03T14:57:01Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+124 / -0**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

issue #2936 

#### Does this PR introduce a user-facing change?
```release-note
NONE
```
cc: @mimowo @mwielgus @PBundyra

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-26T12:01:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e7d7ee60ec452843f25d896d92eeff494104e937 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/674d82330945750008bfde43 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-29T11:19:10Z

/approve
/hold
for the alternative section update and lgtm from @tenzen-y or @PBundyra once ready

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-29T11:19:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3652#issuecomment-2507608479" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3652#" title="Author self-approved">yaroslava-serdiuk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-29T13:11:20Z

/lgtm
/hold cancel
Thank you @yaroslava-serdiuk

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-29T13:11:27Z

LGTM label has been added.  <details>Git tree hash: 53400e0bdd987d4ccf650c20b6c8b575f33f4359</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-03T08:28:10Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-03T08:28:16Z

LGTM label has been added.  <details>Git tree hash: 320f1c7fdc150270daafa7c8a53010cf9ca99cbe</details>

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-12-03T14:41:07Z

/unhold

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T14:59:52Z

- **keps/2936-local-queue-defaulting/README.md:37** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T14:59:52Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
```

  Can we clarify what CQ does the default LQ refer to?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-26T15:02:00Z

- **keps/2936-local-queue-defaulting/README.md:37** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-26T15:02:00Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
```

  The admin creates a default LQ manually, so they will specify CQ. 
  The feature only add `default` LQ to the job if it exists in the namespace.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-26T15:03:02Z

- **keps/2936-local-queue-defaulting/README.md:57** by [@PBundyra](https://github.com/PBundyra) — 2024-11-26T15:03:01Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
```

  Does this proposal results in every Job created in a namespace will be managed by Kueue?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-26T15:10:58Z

- **keps/2936-local-queue-defaulting/README.md:57** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-26T15:10:58Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
```

  If the feature gate is enabled and `default` LQ is created in a namespace, then yes.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T10:06:35Z

- **keps/2936-local-queue-defaulting/README.md:37** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T10:06:35Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
```

  Does it only apply if there is only one LQ in namespace named `default` or there can be more

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-27T10:34:03Z

- **keps/2936-local-queue-defaulting/README.md:37** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-27T10:34:03Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
```

  Could we have multiple LQs with the same name in the namespace? I'm not sure wether we through an error during creation, but internally queues stores in the map, so we can't have multiple queues with the same name+namespace

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T10:54:08Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@mimowo](https://github.com/mimowo) — 2024-11-27T10:54:08Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  Could you expand on the proposed approach? The options I see:
  1. default the `queue-name` label on the Job object in a webhook
  2. default only at the workload `spec.queueName` level
  
  The webhook-based approach will not work OOTB for external Jobs. Also, with the MAP effort we will want to move away from webhooks over time. On the plus side we have compatibility with tooling.
  
  (2.) would work OOTB for external Jobs. Also, (2.) is less committal, we could always do (1.) in a follow up if needed. 
  
  Maybe the issue for (2.) is the compatibility with other tooling like kueuectl (not sure where it looks now), but we could probably adjust them to lookup the workload.
  
  If we go with (1.) we should provide some utility functions so that it is easy for other developers to follow the semantics for the external jobs.
  
  All in all, I'm not sure which one is better, so would be good to put list the pros and cons, and put the rejected one to alternatives describing the reasons for rejecting.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-27T11:00:21Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-27T11:00:21Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  >The webhook-based approach will not work OOTB for external Jobs.
  
  Could you elaborate why it won't work?
  
  >Also, with the MAP effort we will want to move away from webhooks over time.
  
  What is MAP?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T11:32:34Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@mimowo](https://github.com/mimowo) — 2024-11-27T11:32:34Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  > Could you elaborate why it won't work?
  
  It will work, but not OOTB - the users need to implement the same semantics in their webhooks. AppWrapper is one of the frameworks, but there are also other in-house Jobs.
  
  > What is MAP?
  
  [Mutating Admission Policies](https://github.com/kubernetes/enhancements/issues/3962). I think we could actually move the approach from webhook to MAP when it is released.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T12:18:25Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@mimowo](https://github.com/mimowo) — 2024-11-27T12:18:25Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  Actually, (2.) may not work OOTB either - because if the webhook is not aware of the logic, then it would suspend the job without queue-name when manageJobsWithoutName: true (IIUC)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T12:39:30Z

This is interesting feature.

- **Makefile-deps.mk:100** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:31:11Z
```diff
@@ -97,7 +97,7 @@ hugo: ## Download hugo locally if necessary.
 MDTOC = $(PROJECT_DIR)/bin/mdtoc
 .PHONY: mdtoc
 mdtoc: ## Download mdtoc locally if necessary.
-	@GOBIN=$(PROJECT_DIR)/bin CGO_ENABLED=1 $(GO_CMD) install sigs.k8s.io/mdtoc@$(MDTOC_VERSION)
+	@GOBIN=$(PROJECT_DIR)/bin CGO_ENABLED=1 $(GO_CMD) install sigs.k8s.io/mdtoc@latest
```

  What is this reason?

- **keps/2936-local-queue-defaulting/README.md:20** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:33:02Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
```

  ```suggestion
  LocalQueue name label specified.
  ```
  
  The queue-name annotation has already been deprecated and will be removed in the future version.
  So, could you revise the annotation to label it in all places?

- **keps/2936-local-queue-defaulting/README.md:59** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:38:56Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
```

  Instead of using the reserved `default` name for the default localQueue, could we scrape the default queue-name from the cluster?
  
  For example, we might want to prepare the dedicated `.spec.default: true|false` field in the LocalQueue, and then the webhook looks into the default queue-name when the webhook insert the queue-name to Jobs.
  
  This idea is similar to storageClass.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-27T12:44:36Z

- **Makefile-deps.mk:100** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-27T12:44:20Z
```diff
@@ -97,7 +97,7 @@ hugo: ## Download hugo locally if necessary.
 MDTOC = $(PROJECT_DIR)/bin/mdtoc
 .PHONY: mdtoc
 mdtoc: ## Download mdtoc locally if necessary.
-	@GOBIN=$(PROJECT_DIR)/bin CGO_ENABLED=1 $(GO_CMD) install sigs.k8s.io/mdtoc@$(MDTOC_VERSION)
+	@GOBIN=$(PROJECT_DIR)/bin CGO_ENABLED=1 $(GO_CMD) install sigs.k8s.io/mdtoc@latest
```

  Sorry, this is leftover, removed.

- **keps/2936-local-queue-defaulting/README.md:20** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-27T12:44:28Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
```

  Thanks, done

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-27T12:57:21Z

- **keps/2936-local-queue-defaulting/README.md:59** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-27T12:57:21Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
```

  >.spec.default: true|false field in the LocalQueue
  
  Having default field in the API brings it owns challenge, I mentioned it earlier in the issue: https://github.com/kubernetes-sigs/kueue/issues/2936#issuecomment-2478698423, i.e. validating that only one LQ is default.
  
  Since the goal is to simplify workflow, it seems having `default` LQ is simple and explicit. 
  However I'm not sure about having a defaulting under the feature gate. In case the feature goes to GA and users are not familiar with the defaulting mechanism, they may create the LQ with default name and jobs without queue label and expecting a different behaviour. 
  Maybe instead of feature gate it should be a part of kueue config.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-27T14:52:53Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-27T14:52:53Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  >Actually, (2.) may not work OOTB either - because if the webhook is not aware of the logic, then it would suspend the job without queue-name when manageJobsWithoutName: true (IIUC)
  
  yes, this is my understanding

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-27T15:07:27Z

- **keps/2936-local-queue-defaulting/README.md:59** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-27T15:07:26Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
```

  >This idea is similar to storageClass.
  
  StorageClass allows multiple defaults and the latest default is an actual default. I also considered this as an alternative (will add it to alternatives section).
  
  I'm thinking about use case. 1) In the case described in the issue, when admin creates only one LQ that is supposed to use, then both solutions (`default` LQ and spec.default=true) would work fine. 
  2) In case when there many LQs and one of them is default, it seems having `default` LQ is better, because it easier to find a `default` LQ among hundreds of LQs in namespace and modify (not sure if it's possible, maybe recreate) in case of misconfiguration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T17:49:24Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@mimowo](https://github.com/mimowo) — 2024-11-27T17:49:24Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  I seem in that case I'm leaning to (1.) as IIUC both approaches don't work OOTB for external Jobs. It would be great to make it easy for developers of custom Jobs to easily support the semantics, which becomes non-trivial, as the webhooks needs to know if the LQ exists (so needs access to cache or have LQ informer).

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-11-27T19:56:56Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-11-27T19:56:56Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  It's going to be important that the webhook only do this for Jobs defined in namespaces where a designated default `LocalQueue` already exists.  If the webhook acts  without namespace awareness, then we still need the namespace-level filtering mechanism from[ KEP-3589](https://github.com/kubernetes-sigs/kueue/pull/3595)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T08:06:10Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@mimowo](https://github.com/mimowo) — 2024-11-28T08:06:10Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  I think the webhook should be namespace aware, but the awareness would come from  KEP-3589, so that we don't duplicate namespace filtering effort.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T08:10:17Z

- **keps/2936-local-queue-defaulting/kep.yaml:31** by [@mimowo](https://github.com/mimowo) — 2024-11-28T08:10:17Z
```diff
@@ -0,0 +1,32 @@
+title: LocalQueue defaulting
+kep-number: 2936
+authors:
+  - "@yaroslava-serdiuk"
+status: implementable
+creation-date: 2024-11-25
+reviewers:
+  - "@pbundyra"
+  - "@mwielgus"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDefaulting
```

  ```suggestion
    - name: LocalQueueDefaulting
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T08:11:48Z

Basically LGTM, would be just nice to add the short alternatives section to capture the main discussion points.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-28T08:28:29Z

- **keps/2936-local-queue-defaulting/kep.yaml:31** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-28T08:28:28Z
```diff
@@ -0,0 +1,32 @@
+title: LocalQueue defaulting
+kep-number: 2936
+authors:
+  - "@yaroslava-serdiuk"
+status: implementable
+creation-date: 2024-11-25
+reviewers:
+  - "@pbundyra"
+  - "@mwielgus"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDefaulting
```

  I was thinking that the feature could be grown later to have a default CQ, so the default LQ could be created by Kueue. That the reason why the feature gate name is generic enough, to not limit the defaulting functionality to LQ only.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T08:35:28Z

- **keps/2936-local-queue-defaulting/kep.yaml:31** by [@mimowo](https://github.com/mimowo) — 2024-11-28T08:35:28Z
```diff
@@ -0,0 +1,32 @@
+title: LocalQueue defaulting
+kep-number: 2936
+authors:
+  - "@yaroslava-serdiuk"
+status: implementable
+creation-date: 2024-11-25
+reviewers:
+  - "@pbundyra"
+  - "@mwielgus"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDefaulting
```

  I see, but (1) the KEP title is LocalQueue defaulting, (2) Kueue is redundant in the name, because this is the name of the project. So I think CQ defaulting would be a separate feature not under this KEP, and could have a dedicated feature gate, like ClusterQueueDefaulting.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T08:45:27Z

- **keps/2936-local-queue-defaulting/README.md:37** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T08:45:27Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
```

  What I meant is if we intend to default LQ if there is more than one LQ in the namespace (with different names)

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-28T08:51:29Z

- **keps/2936-local-queue-defaulting/README.md:37** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-28T08:51:29Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
```

  There is no limitation for the number of LQs in the namespace, the `default` LQ is applied only to jobs that haven't specified queue label.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-28T11:06:55Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-28T11:06:54Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  I check whether the `default`queue exists in the namespace: https://github.com/kubernetes-sigs/kueue/pull/3610

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T11:14:31Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@mimowo](https://github.com/mimowo) — 2024-11-28T11:14:30Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  Yes, but this code does not filter based on the namespaces. If we have filtering, then Kueue would stay away from namespaces which don't pass the filter, even if "default" LQ exists.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-11-28T11:17:47Z

- **keps/2936-local-queue-defaulting/kep.yaml:31** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-11-28T11:17:47Z
```diff
@@ -0,0 +1,32 @@
+title: LocalQueue defaulting
+kep-number: 2936
+authors:
+  - "@yaroslava-serdiuk"
+status: implementable
+creation-date: 2024-11-25
+reviewers:
+  - "@pbundyra"
+  - "@mwielgus"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDefaulting
```

  Updated

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-28T16:36:08Z

- **keps/2936-local-queue-defaulting/README.md:59** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-28T16:36:08Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
```

  That makes sense.
  I think that the custom default LQ name is still valid request, but it seems that the customization will require an additional mechanism like comparing creationTimestamp between multiple defaults LQs.
  
  So, I agree with the fixed default LQ name, "default" so that we can start this feature as a minimum.
  After we obtain some feedbacks for the custom default LQ name, we can revisit here, and consider the customization.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-28T16:39:50Z

- **keps/2936-local-queue-defaulting/README.md:61** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-28T16:39:50Z
```diff
@@ -0,0 +1,63 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name annotation specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name annotation.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have annotation for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and KueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+### User Stories
+
+#### Story 1
+
+Administrator createds a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue annotation in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+annotation.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
```

  > If we go with (1.) we should provide some utility functions so that it is easy for other developers to follow the semantics for the external jobs.
  
  I think that this should be enough for the in-house jobs. Additionally, we might want to expand the customJob documentation (https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/).
  
  Basically, I think that natively supporting custom Jobs is not necessary in here's upstream repository, and we can focus on the upstream supported Jobs like batch/v1 Job, RayJob, and KFJobs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-28T16:41:48Z

Basically, lgtm

- **keps/2936-local-queue-defaulting/README.md:71** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-28T16:41:42Z
```diff
@@ -0,0 +1,79 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name label specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name label.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have label for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and LocalQueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+The `default` LocalQueue itself should be created manually by administrator.
+
+### User Stories
+
+#### Story 1
+
+Administrator created a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue label in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+label.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
+label if the label is not present and the feature gate is enabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+## Alternatives
+
```

  Could you add one more alternative, "default only at the workload spec.queueName level"?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-29T13:13:55Z

- **keps/2936-local-queue-defaulting/README.md:71** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-29T13:13:55Z
```diff
@@ -0,0 +1,79 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name label specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name label.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have label for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and LocalQueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+The `default` LocalQueue itself should be created manually by administrator.
+
+### User Stories
+
+#### Story 1
+
+Administrator created a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue label in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+label.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
+label if the label is not present and the feature gate is enabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+## Alternatives
+
```

  /hold
  This is still not addressed, yet.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-12-02T09:49:59Z

- **keps/2936-local-queue-defaulting/README.md:71** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-12-02T09:49:59Z
```diff
@@ -0,0 +1,79 @@
+# KEP-2936: LocalQueue defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a defaulting LocalQueue mechanism for the jobs that don't have
+LocalQueue name label specified.
+
+## Motivation
+
+Simplify user experience with Kueue.
+
+### Goals
+
+- default LocalQueue for jobs that don't specify LocalQueue name label.
+
+### Non-Goals
+
+- create a default LocalQueue.
+
+## Proposal
+
+If the Job doesn't have label for LocalQueue in its Spec and the LocalQueue
+with name `default` is present in Job's namespace and LocalQueueDefaulting feature gate
+is enabled, the `kueue.x-k8s.io/queue-name:default` anotation will be added to the Job.
+
+The `default` LocalQueue itself should be created manually by administrator.
+
+### User Stories
+
+#### Story 1
+
+Administrator created a single `default` LocalQueue per namespace. Users doesn't need to specify
+the LocalQueue label in order for workload to be scheduled.
+
+### Notes/Constraints/Caveats (Optional)
+
+manageJobsWithoutQueueName doesn't work with this feature, since Kueue add the LocalQueue
+label.
+
+### Risks and Mitigations
+
+## Design Details
+
+Update defautling webhook for all types of job to add `kueue.x-k8s.io/queue-name:default`
+label if the label is not present and the feature gate is enabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+## Alternatives
+
```

  Updated
