# PR #3657: Implement Topology CRD validations testing

**Summary**: Implement Topology CRD validations testing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3657

**Last updated**: 2024-11-27T12:44:00Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `implement-immutable-tas-validation` → `main`
- **Created**: 2024-11-26T15:12:41Z
- **Updated**: 2024-11-27T12:44:00Z
- **Merged**: 2024-11-27T12:42:56Z
- **Closed**: 2024-11-27T12:42:56Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@trasc](https://github.com/trasc)
- **Changed files**: 1   **+54 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:
We used to implement validation and mutating testing based on webhooks and markers in the `/test/integration/webhook/core`. 

However, for Topology, we do not have any testing for those.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-26T15:12:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 190ddf9ac783a900897c123b3a923bdcda27e6ee |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6746d672448c0a0008679d53 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T07:51:16Z

/lgtm

Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-27T07:51:22Z

LGTM label has been added.  <details>Git tree hash: ec7fe0f5ed5f6d345996d9a8ecf1546a9da3476d</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T08:15:29Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:59:33Z

/lgtm
/approve
I think the PR is ready. We can think how to improve readability of the tests in a follow up more holistically. Feel free to release the hold.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-27T08:59:41Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3657#issuecomment-2503291987" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3657#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-27T08:59:41Z

LGTM label has been added.  <details>Git tree hash: c7ceb2d84092bea3853f7e8d2549f527bfe259e6</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:26:01Z

/hold cancel

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T15:28:09Z

- **test/integration/webhook/core/tas_test.go:26** by [@mimowo](https://github.com/mimowo) — 2024-11-26T15:28:09Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var _ = ginkgo.Describe("Topology validations", func() {
```

  We don't have a webhook for topology. The validations are don't by CRD schema so far, so I would suggest moving the tests to  `test/integration/tas`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-26T15:42:53Z

- **test/integration/webhook/core/tas_test.go:26** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-26T15:42:53Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var _ = ginkgo.Describe("Topology validations", func() {
```

  Yeah, I considered the same things. But other marker based validation testings are implemented in the webhook package. So, if we move those to `test/integration/tas`, only TAS validation testings are specialized.
  
  So, I'm on the fence about whether or not we should move this to `/test/integration/tas`.
  Do you still think moving this to dedicated directory even though there are all of other marker testings in the webhook package?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-26T15:54:28Z

- **test/integration/webhook/core/tas_test.go:26** by [@mimowo](https://github.com/mimowo) — 2024-11-26T15:54:28Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var _ = ginkgo.Describe("Topology validations", func() {
```

  I see, I think keeping them in webhook folder is misleading. I see that many validation asserts are in the package, but I think this is mostly for historical reasons when almost all validation was done in webhooks. As we move gradually the validations out of webhooks, we can also move the testing out.
  
  Unless, we rename the folder as test/integration/validation/core`, but I think it is better to keep them feature per feature in the long run.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T04:46:15Z

- **test/integration/webhook/core/tas_test.go:26** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T04:46:15Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var _ = ginkgo.Describe("Topology validations", func() {
```

  > Unless, we rename the folder as test/integration/validation/core`, but I think it is better to keep them feature per feature in the long run.
  
  The current `/test/integration/webhook/core` package has mutating and validating testings. So I think that we can rename it `test/integration/validation/core`. For now, let's move tas validation testings to tas package.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T04:49:53Z

- **test/integration/webhook/core/tas_test.go:26** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T04:49:53Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var _ = ginkgo.Describe("Topology validations", func() {
```

  Done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T07:47:08Z

- **test/integration/tas/tas_test.go:845** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T07:47:07Z
```diff
@@ -839,3 +839,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher gomega.OmegaMatcher) {
```

  ```suggestion
  		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher gomega.GomegaMatcherr) {
  ```
  
  Could you fix it here too?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T07:48:52Z

- **test/integration/tas/tas_test.go:845** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T07:48:52Z
```diff
@@ -839,3 +839,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher gomega.OmegaMatcher) {
```

  Good call :)
  
  related: https://github.com/kubernetes-sigs/kueue/pull/3660#discussion_r1860070171

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T07:49:45Z

- **test/integration/tas/tas_test.go:845** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T07:49:45Z
```diff
@@ -839,3 +839,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher gomega.OmegaMatcher) {
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T08:11:39Z

LGTM, just readability nits

- **test/integration/tas/tas_test.go:870** by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:09:55Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				}()
+			}
+			gomega.Expect(err).Should(matcher)
+		},
+			ginkgo.Entry("valid Topology",
+				testing.MakeTopology("valid").Levels([]string{corev1.LabelHostname}).Obj(),
+				gomega.Succeed()),
+			ginkgo.Entry("invalid levels",
+				testing.MakeTopology("invalid-level").Levels([]string{"@invalid"}).Obj(),
+				testing.BeInvalidError()),
+		)
+	})
+
+	ginkgo.When("Updating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on update", func(topology *kueuealpha.Topology, updateTopology func(topology *kueuealpha.Topology), matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				}()
```

  I think it will be more readable if we move the defer block below `gomega.Expect(err).Should(gomega.Succeed())`

- **test/integration/tas/tas_test.go:849** by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:10:25Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  Similarly here, we can move defer after we check there is no error

- **test/integration/tas/tas_test.go:885** by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:10:39Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				}()
+			}
+			gomega.Expect(err).Should(matcher)
+		},
+			ginkgo.Entry("valid Topology",
+				testing.MakeTopology("valid").Levels([]string{corev1.LabelHostname}).Obj(),
+				gomega.Succeed()),
+			ginkgo.Entry("invalid levels",
+				testing.MakeTopology("invalid-level").Levels([]string{"@invalid"}).Obj(),
+				testing.BeInvalidError()),
+		)
+	})
+
+	ginkgo.When("Updating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on update", func(topology *kueuealpha.Topology, updateTopology func(topology *kueuealpha.Topology), matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				}()
+			}
+			gomega.Expect(err).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(topology), topology)).Should(gomega.Succeed())
+			updateTopology(topology)
+			gomega.Expect(k8sClient.Update(ctx, topology)).Should(matcher)
+		},
+			ginkgo.Entry("succeed to update topology",
+				testing.MakeTopology("valid").Levels([]string{corev1.LabelHostname}).Obj(),
+				func(topology *kueuealpha.Topology) {
+					topology.ObjectMeta.Labels = map[string]string{
+						"alpha": "beta",
+					}
+				},
+				gomega.Succeed()),
+			ginkgo.Entry("updating levels are prohibited",
```

  ```suggestion
  			ginkgo.Entry("updating levels is prohibited",
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T08:14:26Z

- **test/integration/tas/tas_test.go:849** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T08:14:25Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  Do you request all defer block moving?
  
  https://github.com/search?q=repo%3Akubernetes-sigs%2Fkueue+defer+func%28%29+%7B+path%3A%2F%5Etest%5C%2Fintegration%5C%2F%2F&type=code

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T08:19:37Z

- **test/integration/tas/tas_test.go:849** by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:19:37Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  I see, I didn't see that, let's do it in a follow up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T08:19:38Z

- **test/integration/tas/tas_test.go:849** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T08:19:38Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  Additionally, I think the current defer place would be better. Because if there are any panic in the `gomega.Expect`, the defer will never performed, right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T08:21:10Z

- **test/integration/tas/tas_test.go:885** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T08:21:10Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				}()
+			}
+			gomega.Expect(err).Should(matcher)
+		},
+			ginkgo.Entry("valid Topology",
+				testing.MakeTopology("valid").Levels([]string{corev1.LabelHostname}).Obj(),
+				gomega.Succeed()),
+			ginkgo.Entry("invalid levels",
+				testing.MakeTopology("invalid-level").Levels([]string{"@invalid"}).Obj(),
+				testing.BeInvalidError()),
+		)
+	})
+
+	ginkgo.When("Updating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on update", func(topology *kueuealpha.Topology, updateTopology func(topology *kueuealpha.Topology), matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				}()
+			}
+			gomega.Expect(err).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(topology), topology)).Should(gomega.Succeed())
+			updateTopology(topology)
+			gomega.Expect(k8sClient.Update(ctx, topology)).Should(matcher)
+		},
+			ginkgo.Entry("succeed to update topology",
+				testing.MakeTopology("valid").Levels([]string{corev1.LabelHostname}).Obj(),
+				func(topology *kueuealpha.Topology) {
+					topology.ObjectMeta.Labels = map[string]string{
+						"alpha": "beta",
+					}
+				},
+				gomega.Succeed()),
+			ginkgo.Entry("updating levels are prohibited",
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T08:29:01Z

- **test/integration/tas/tas_test.go:849** by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:29:01Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  I see, yes, I didn't think about it. However, it seems this pattern is quite verbose. Also, if there is a panic we probably don't want to continue tests anyway, so cleanup might not be necessary I think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T08:30:37Z

- **test/integration/tas/tas_test.go:849** by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:30:37Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  Another pattern would be to use AfterEach as in most tests, and cleanup there, so I think could re-write this verbose pattern, but I'm ok as is since this is consitent with other tests in this file.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T08:44:09Z

- **test/integration/tas/tas_test.go:849** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T08:44:09Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  > Another pattern would be to use AfterEach as in most tests, and cleanup there, so I think could re-write this verbose pattern,
  
  IIUC, the solution is not available based on the DecribeTable specifications. If we use the AfterEach, the cleanup will be performed when all Entry completed. But we want to perform the cleanup when each Entry.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T08:58:46Z

- **test/integration/tas/tas_test.go:849** by [@mimowo](https://github.com/mimowo) — 2024-11-27T08:58:46Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  Hm, in that case I would suggest to not use `DecribeTable` or just not support cleanup on panics. I think in case of panic any method will not fully ok. Imagine you have After each with two cleanup statements. If first fails we don't cleanup the second anyway, so I wouldn't worry about them too much.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-11-27T12:25:56Z

- **test/integration/tas/tas_test.go:849** by [@tenzen-y](https://github.com/tenzen-y) — 2024-11-27T12:25:55Z
```diff
@@ -839,3 +840,56 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Topology validations", func() {
+	ginkgo.When("Creating a Topology", func() {
+		ginkgo.DescribeTable("Validate Topology on creation", func(topology *kueuealpha.Topology, matcher types.GomegaMatcher) {
+			err := k8sClient.Create(ctx, topology)
+			if err == nil {
+				defer func() {
```

  > Hm, in that case I would suggest to not use DecribeTable or just not support cleanup on panics.
  
  If we migrate to the `ginkgo.It`, it will bring us huge testing codes...
  So, the current `defer` solution is not bad one. But I agree with not good solution.
