# PR #3666: Use ReadUIntFromLabel to validate index label.

**Summary**: Use ReadUIntFromLabel to validate index label.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3666

**Last updated**: 2024-11-28T13:23:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/read-uint-from-label` → `main`
- **Created**: 2024-11-27T09:45:45Z
- **Updated**: 2024-11-28T13:23:25Z
- **Merged**: 2024-11-28T10:20:59Z
- **Closed**: 2024-11-28T10:20:59Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 3   **+142 / -19**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use ReadUIntFromLabel to validate index label.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #3651

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-27T09:46:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6a98c65c7f9ab2167856caf4abcf3d1c352d3254 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/674838db9064100008fda943 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3666--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3666--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTM2NjYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.QgFrtNbpcME-u6yOnhdH9UTpbTJoH4h0p1y1lHmXllY)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3666--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T13:30:23Z

/cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T09:14:41Z

LGTM label has been added.  <details>Git tree hash: 7c97d290d75ef846f8314c29d54c3d8574c52a0b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T09:14:43Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3666#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3666#pullrequestreview-2467282541" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [mimowo]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-28T10:08:04Z

@mimowo Resolved conflicts

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T10:19:15Z

LGTM label has been added.  <details>Git tree hash: 801ffcbe3efe77f014d11e96d281bd229df270dd</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T10:45:27Z

Thanks @mbobrovskyi!

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T10:13:55Z

- **pkg/util/pod/pod.go:70** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T10:13:55Z
```diff
@@ -56,3 +62,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(pod *corev1.Pod, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMinMax(pod, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMinMax(obj client.Object, labelKey string, max int) (*int, error) {
```

  Could we align the name of the function with the parameters? Should there be `min` parameter?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-27T10:16:52Z

- **pkg/util/pod/pod.go:92** by [@PBundyra](https://github.com/PBundyra) — 2024-11-27T10:16:52Z
```diff
@@ -56,3 +62,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(pod *corev1.Pod, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMinMax(pod, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMinMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, obj.GetObjectKind(), klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, obj.GetObjectKind(), klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	return readIntFromStringWithMinMax(value, 0, max)
+}
+
+func readIntFromStringWithMinMax(value string, min, max int) (*int, error) {
+	i, err := strconv.Atoi(value)
+	if err != nil {
+		return nil, err
+	}
+	if i < min {
+		return nil, fmt.Errorf("value should be greater than or equal to %d", min)
```

  Could we reduce this code and use `strconv.ParseUInt`? Since we don't export those function I believe `readIntFromStringWithMax` is enough

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T10:49:25Z

- **pkg/util/pod/pod.go:70** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T10:49:24Z
```diff
@@ -56,3 +62,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(pod *corev1.Pod, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMinMax(pod, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMinMax(obj client.Object, labelKey string, max int) (*int, error) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T11:02:52Z

- **pkg/util/pod/pod.go:92** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T11:02:52Z
```diff
@@ -56,3 +62,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(pod *corev1.Pod, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMinMax(pod, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMinMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, obj.GetObjectKind(), klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, obj.GetObjectKind(), klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	return readIntFromStringWithMinMax(value, 0, max)
+}
+
+func readIntFromStringWithMinMax(value string, min, max int) (*int, error) {
+	i, err := strconv.Atoi(value)
+	if err != nil {
+		return nil, err
+	}
+	if i < min {
+		return nil, fmt.Errorf("value should be greater than or equal to %d", min)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T11:09:15Z

- **pkg/util/pod/pod.go:92** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T11:09:15Z
```diff
@@ -56,3 +62,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(pod *corev1.Pod, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMinMax(pod, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMinMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, obj.GetObjectKind(), klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, obj.GetObjectKind(), klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	return readIntFromStringWithMinMax(value, 0, max)
+}
+
+func readIntFromStringWithMinMax(value string, min, max int) (*int, error) {
+	i, err := strconv.Atoi(value)
+	if err != nil {
+		return nil, err
+	}
+	if i < min {
+		return nil, fmt.Errorf("value should be greater than or equal to %d", min)
```

  Ah, missed ParseUint

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T11:12:26Z

- **pkg/util/pod/pod.go:92** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T11:12:26Z
```diff
@@ -56,3 +62,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(pod *corev1.Pod, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMinMax(pod, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMinMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, obj.GetObjectKind(), klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, obj.GetObjectKind(), klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	return readIntFromStringWithMinMax(value, 0, max)
+}
+
+func readIntFromStringWithMinMax(value string, min, max int) (*int, error) {
+	i, err := strconv.Atoi(value)
+	if err != nil {
+		return nil, err
+	}
+	if i < min {
+		return nil, fmt.Errorf("value should be greater than or equal to %d", min)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T13:36:10Z

- **pkg/util/pod/pod.go:86** by [@mimowo](https://github.com/mimowo) — 2024-11-27T13:36:10Z
```diff
@@ -56,3 +62,32 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, err
```

  Hm, maybe we could re-package the error into `errInvalidInt` so that it is easier to assert?
  
  IIUC this could work: `return nil, fmt.Sprintf("%w, message: %v", errInvalidInt, err.Error())`, then we could assert on  `errInvalidInt` rather than strings.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T13:36:31Z

- **pkg/util/pod/pod.go:86** by [@mimowo](https://github.com/mimowo) — 2024-11-27T13:36:31Z
```diff
@@ -56,3 +62,32 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, err
```

  cc @PBundyra as it may impact also your approach to testing

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T16:56:15Z

- **pkg/util/pod/pod.go:86** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T16:56:14Z
```diff
@@ -56,3 +62,32 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, err
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T17:10:14Z

- **pkg/util/pod/pod.go:86** by [@mimowo](https://github.com/mimowo) — 2024-11-27T17:10:13Z
```diff
@@ -56,3 +62,32 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, err
```

  Can you check if `return nil, fmt.Sprintf("%w, message: %s", errInvalidInt, err.Error())` works? I think it might be useful to get contextual information.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T17:33:03Z

Overall lgtm, proposing some improvements to usability of the new functions

- **pkg/controller/tas/topology_ungater.go:452** by [@mimowo](https://github.com/mimowo) — 2024-11-27T17:30:48Z
```diff
@@ -447,17 +446,17 @@ func readRanksForLabels(
 	podIndexLabel string,
 	rjInfo *replicatedJobsInfo,
 ) (map[int]*corev1.Pod, error) {
-	result := make(map[int]*corev1.Pod, 0)
+	result := make(map[int]*corev1.Pod)
 	podSetSize := int(*psa.Count)
 	for _, pod := range pods {
-		podIndex, err := readIntFromLabel(pod, podIndexLabel)
+		podIndex, err := utilpod.ReadUIntFromLabel(pod, podIndexLabel)
```

  Let's use `ReadUIntFromLabelBelowBound(pod, podIndexLabel, singleJobSize)` here. The bound value is `singleJobSize`. Let's compute on L451 
  ```golang
  singleJobSize := podSetSize
  if rjInfo != nil {
    singleJobSize := podSetSize / rjInfo.replicasCount
  }
  ```
  then drop L464.

- **pkg/controller/tas/topology_ungater.go:459** by [@mimowo](https://github.com/mimowo) — 2024-11-27T17:31:25Z
```diff
@@ -447,17 +446,17 @@ func readRanksForLabels(
 	podIndexLabel string,
 	rjInfo *replicatedJobsInfo,
 ) (map[int]*corev1.Pod, error) {
-	result := make(map[int]*corev1.Pod, 0)
+	result := make(map[int]*corev1.Pod)
 	podSetSize := int(*psa.Count)
 	for _, pod := range pods {
-		podIndex, err := readIntFromLabel(pod, podIndexLabel)
+		podIndex, err := utilpod.ReadUIntFromLabel(pod, podIndexLabel)
 		if err != nil {
 			// the Pod has no rank information - ranks cannot be used
 			return nil, err
 		}
 		rank := *podIndex
 		if rjInfo != nil {
-			jobIndex, err := readIntFromLabel(pod, rjInfo.jobIndexLabel)
+			jobIndex, err := utilpod.ReadUIntFromLabel(pod, rjInfo.jobIndexLabel)
```

  Here we can use `ReadUIntFromLabelBelowBound(pod, rjInfo.jobIndexLabel, rjInfo.replicasCount)`

- **pkg/util/pod/pod.go:71** by [@mimowo](https://github.com/mimowo) — 2024-11-27T17:32:34Z
```diff
@@ -56,3 +63,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
```

  I suggest to name to `ReadUIntFromLabelBelowBound` to make the inequality < bound. This way we don't need to remember to "-1" when calling the function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-27T17:41:05Z

- **pkg/util/pod/pod.go:95** by [@mimowo](https://github.com/mimowo) — 2024-11-27T17:41:04Z
```diff
@@ -56,3 +63,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+var (
+	errInvalidUInt = errors.New("invalid unsigned integer")
+)
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+	}
+	intValue := int(uintValue)
+	if intValue > max {
+		return nil, fmt.Errorf("value should be less than or equal to %d", max)
```

  nit: I'm wondering if we can also have here `return nil, fmt.Errorf("%w: value should be less or equal: %d", errInvalidValue, max)`. Then, in the testing we could compare errors rather than strings, IIUC.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T18:20:19Z

- **pkg/util/pod/pod.go:95** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T18:20:19Z
```diff
@@ -56,3 +63,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("no label %q for %s %q", labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringWithMax(value, max)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+var (
+	errInvalidUInt = errors.New("invalid unsigned integer")
+)
+
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+	}
+	intValue := int(uintValue)
+	if intValue > max {
+		return nil, fmt.Errorf("value should be less than or equal to %d", max)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T18:20:28Z

- **pkg/util/pod/pod.go:71** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T18:20:28Z
```diff
@@ -56,3 +63,36 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T18:23:42Z

- **pkg/controller/tas/topology_ungater.go:459** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T18:23:42Z
```diff
@@ -447,17 +446,17 @@ func readRanksForLabels(
 	podIndexLabel string,
 	rjInfo *replicatedJobsInfo,
 ) (map[int]*corev1.Pod, error) {
-	result := make(map[int]*corev1.Pod, 0)
+	result := make(map[int]*corev1.Pod)
 	podSetSize := int(*psa.Count)
 	for _, pod := range pods {
-		podIndex, err := readIntFromLabel(pod, podIndexLabel)
+		podIndex, err := utilpod.ReadUIntFromLabel(pod, podIndexLabel)
 		if err != nil {
 			// the Pod has no rank information - ranks cannot be used
 			return nil, err
 		}
 		rank := *podIndex
 		if rjInfo != nil {
-			jobIndex, err := readIntFromLabel(pod, rjInfo.jobIndexLabel)
+			jobIndex, err := utilpod.ReadUIntFromLabel(pod, rjInfo.jobIndexLabel)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-27T18:23:48Z

- **pkg/controller/tas/topology_ungater.go:452** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-27T18:23:48Z
```diff
@@ -447,17 +446,17 @@ func readRanksForLabels(
 	podIndexLabel string,
 	rjInfo *replicatedJobsInfo,
 ) (map[int]*corev1.Pod, error) {
-	result := make(map[int]*corev1.Pod, 0)
+	result := make(map[int]*corev1.Pod)
 	podSetSize := int(*psa.Count)
 	for _, pod := range pods {
-		podIndex, err := readIntFromLabel(pod, podIndexLabel)
+		podIndex, err := utilpod.ReadUIntFromLabel(pod, podIndexLabel)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T08:36:29Z

- **pkg/util/pod/pod.go:99** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T08:27:26Z
```diff
@@ -56,3 +63,38 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+var (
+	errLabelNotFound = errors.New("label not found")
+	errInvalidUInt   = errors.New("invalid unsigned integer")
+	errValidation    = errors.New("validation error")
+)
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringBelowBound(value, bound)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+	}
+	intValue := int(uintValue)
+	if intValue > bound {
+		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	}
+	return ptr.To(intValue), nil
```

  ```suggestion
  	return &intValue, nil
  ```

- **pkg/util/pod/pod.go:96** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T08:34:58Z
```diff
@@ -56,3 +63,38 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+var (
+	errLabelNotFound = errors.New("label not found")
+	errInvalidUInt   = errors.New("invalid unsigned integer")
+	errValidation    = errors.New("validation error")
+)
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringBelowBound(value, bound)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+	}
+	intValue := int(uintValue)
+	if intValue > bound {
```

  ```suggestion
  	if uintValue > uint64(bound) {
  ```
  This is to prevent overflow if uintValue was bigger than maxInt

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-11-28T08:48:48Z

- **pkg/util/pod/pod.go:96** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-11-28T08:48:48Z
```diff
@@ -56,3 +63,38 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 		return g.Name == gateName
 	})
 }
+
+var (
+	errLabelNotFound = errors.New("label not found")
+	errInvalidUInt   = errors.New("invalid unsigned integer")
+	errValidation    = errors.New("validation error")
+)
+
+func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
+	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+}
+
+func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+	value, found := obj.GetLabels()[labelKey]
+	kind := obj.GetObjectKind().GroupVersionKind().Kind
+	if !found {
+		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+	}
+	intValue, err := readUIntFromStringBelowBound(value, bound)
+	if err != nil {
+		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
+	}
+	return intValue, nil
+}
+
+func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+	uintValue, err := strconv.ParseUint(value, 10, 0)
+	if err != nil {
+		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+	}
+	intValue := int(uintValue)
+	if intValue > bound {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T09:14:34Z

/lgtm
/approve
Thanks for the contribution!

PTAL if there are some places which could reuse these new functions, and prepare follow up PR(s). I know about the PodGroup validation for pod-group-pod-index label. cc @PBundyra

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T10:19:00Z

- **pkg/controller/tas/topology_ungater.go:412** by [@mimowo](https://github.com/mimowo) — 2024-11-28T10:18:59Z
```diff
@@ -403,14 +402,14 @@ func readRanksIfAvailable(log logr.Logger,
 
 func determineRanksLookup(pod *corev1.Pod) (*string, *replicatedJobsInfo) {
 	// Check if this is JobSet
-	if jobCount, _ := readIntFromLabel(pod, jobset.ReplicatedJobReplicas); jobCount != nil {
+	if jobCount, _ := utilpod.ReadUIntFromLabel(pod, jobset.ReplicatedJobReplicas); jobCount != nil {
 		return ptr.To(batchv1.JobCompletionIndexAnnotation), &replicatedJobsInfo{
 			jobIndexLabel: jobset.JobIndexKey,
 			replicasCount: *jobCount,
 		}
 	}
 	// Check if this is Pod group
-	if jobCount, _ := readIntFromLabel(pod, kueuealpha.PodGroupPodIndexLabel); jobCount != nil {
+	if jobCount, _ := utilpod.ReadUIntFromLabel(pod, kueuealpha.PodGroupPodIndexLabel); jobCount != nil {
```

  Oh, I just realized this. I think it is better to follow the pattern as below 
  `if _, found := pod.Labels[batchv1.PodGroupPodIndexLabel]; found {` - the `jobCount` does not make sense in this context. The lookup only aims to check if the PodGroup is used. We will parse later on anyway.
  
  It is not related to this PR, so please follow up.  cc @PBundyra

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T10:19:08Z

/lgtm
