# PR #3678: TAS: Respect node taints if the lowest level is node

**Summary**: TAS: Respect node taints if the lowest level is node

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3678

**Last updated**: 2025-02-19T16:03:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-respect-taints` → `main`
- **Created**: 2024-11-28T10:45:11Z
- **Updated**: 2025-02-19T16:03:56Z
- **Merged**: 2024-11-28T15:34:58Z
- **Closed**: 2024-11-28T15:34:58Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 4   **+271 / -12**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #3658 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Respect node taints in Topology-Aware Scheduling when the lowest topology level is kubernetes.io/hostname.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T10:45:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3678#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-28T10:45:32Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e345655cf4f833cc2feba7bcbbb37d81b4d900c9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67488075c393d2000746d9df |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-28T10:45:53Z

cc @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-28T11:09:30Z

rebased after https://github.com/kubernetes-sigs/kueue/pull/3676

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T13:45:51Z

Thank you
/lgtm
/hold to address nits

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T13:45:58Z

LGTM label has been added.  <details>Git tree hash: 9dca769ed8fda7ba5eddfebb81c2e15e5a5c5a8e</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T14:40:33Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-11-28T14:40:38Z

LGTM label has been added.  <details>Git tree hash: d2233ab5b468a118929be7f68bf51f7bd2d82efa</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-11-28T15:34:35Z

/hold cancel
as the nits are addressed

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-19T16:03:18Z

/cherry-pick release-0.9

Prepare for https://github.com/kubernetes-sigs/kueue/pull/4322

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-02-19T16:03:53Z

@tenzen-y: #3678 failed to apply on top of branch "release-0.9":
```
Applying: TAS: Respect node taints if the lowest level is node
Using index info to reconstruct a base tree...
M	pkg/cache/tas_cache_test.go
M	pkg/cache/tas_flavor_snapshot.go
M	test/integration/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/tas/tas_test.go
Auto-merging pkg/cache/tas_flavor_snapshot.go
CONFLICT (content): Merge conflict in pkg/cache/tas_flavor_snapshot.go
Auto-merging pkg/cache/tas_cache_test.go
CONFLICT (content): Merge conflict in pkg/cache/tas_cache_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 TAS: Respect node taints if the lowest level is node

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3678#issuecomment-2669085900):

>/cherry-pick release-0.9
>
>Prepare for https://github.com/kubernetes-sigs/kueue/pull/4322


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T10:45:46Z

/assign @PBundyra

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T13:23:26Z

- **pkg/cache/tas_flavor_snapshot.go:377** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T13:23:25Z
```diff
@@ -342,13 +363,20 @@ func (s *TASFlavorSnapshot) sortedDomains(domains []*domain) []*domain {
 	return result
 }
 
-func (s *TASFlavorSnapshot) fillInCounts(requests resources.Requests) {
+func (s *TASFlavorSnapshot) fillInCounts(requests resources.Requests, tolerations []corev1.Toleration) {
 	for _, domain := range s.domains {
 		// cleanup the state in case some remaining values are present from computing
 		// assignments for previous PodSets.
 		domain.state = 0
 	}
 	for _, leaf := range s.leaves {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(leaf.nodeTaints, tolerations, func(t *corev1.Taint) bool {
+			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
+		})
+		if untolerated {
+			s.log.Info("excluding node with untolerated taint", "domainID", leaf.id, "taint", taint)
```

  Can we add specific log level?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T13:30:53Z

- **pkg/cache/tas_flavor_snapshot.go:377** by [@mimowo](https://github.com/mimowo) — 2024-11-28T13:30:53Z
```diff
@@ -342,13 +363,20 @@ func (s *TASFlavorSnapshot) sortedDomains(domains []*domain) []*domain {
 	return result
 }
 
-func (s *TASFlavorSnapshot) fillInCounts(requests resources.Requests) {
+func (s *TASFlavorSnapshot) fillInCounts(requests resources.Requests, tolerations []corev1.Toleration) {
 	for _, domain := range s.domains {
 		// cleanup the state in case some remaining values are present from computing
 		// assignments for previous PodSets.
 		domain.state = 0
 	}
 	for _, leaf := range s.leaves {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(leaf.nodeTaints, tolerations, func(t *corev1.Taint) bool {
+			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
+		})
+		if untolerated {
+			s.log.Info("excluding node with untolerated taint", "domainID", leaf.id, "taint", taint)
```

  for sure, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-11-28T13:44:13Z

- **test/integration/tas/tas_test.go:719** by [@PBundyra](https://github.com/PBundyra) — 2024-11-28T13:44:13Z
```diff
@@ -669,6 +669,174 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 		})
+
+		ginkgo.When("Node structure is mutated during test cases", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").
+					NodeLabel("node-group", "tas").
+					TopologyName("default").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+		})
+
```

  ```suggestion
  ```
  
  Removing duplicate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-11-28T14:39:10Z

- **test/integration/tas/tas_test.go:719** by [@mimowo](https://github.com/mimowo) — 2024-11-28T14:39:10Z
```diff
@@ -669,6 +669,174 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 		})
+
+		ginkgo.When("Node structure is mutated during test cases", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				ns = &corev1.Namespace{
+					ObjectMeta: metav1.ObjectMeta{
+						GenerateName: "tas-",
+					},
+				}
+				gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+				topology = testing.MakeTopology("default").Levels([]string{
+					tasBlockLabel,
+					tasRackLabel,
+				}).Obj()
+				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+
+				tasFlavor = testing.MakeResourceFlavor("tas-flavor").
+					NodeLabel("node-group", "tas").
+					TopologyName("default").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, tasFlavor)).Should(gomega.Succeed())
+
+				clusterQueue = testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(*testing.MakeFlavorQuotas(tasFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+				localQueue = testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, topology)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+		})
+
```

  thx, fixed
