# PR #3685: Fix live-updates for kubernetes Job getting stuck occasionally

**Summary**: Fix live-updates for kubernetes Job getting stuck occasionally

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3685

**Last updated**: 2024-12-04T10:32:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `fix/flaky-creating-multikueue-admission-check` → `main`
- **Created**: 2024-11-28T17:31:13Z
- **Updated**: 2024-12-04T10:32:19Z
- **Merged**: 2024-12-04T10:19:02Z
- **Closed**: 2024-12-04T10:19:02Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `kind/flake`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mszadkow](https://github.com/mszadkow), [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+11 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind bug
/kind flake

#### What this PR does / why we need it:
Adds a check when syncing jobs on multikueue to avoid trying to patch the spec before resuming the job.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3581 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix a bug which occasionally prevented updates to the PodTemplate of the Job on the management cluster
when starting a Job (e.g. updating nodeSelectors), when using `MultiKueueBatchJobWithManagedBy` enabled.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-11-28T17:31:30Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 05200ea41b4c8713a7ec0ae34522d93a5b09506e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67501d61592cff000815e0cf |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-02T08:52:38Z

Please describe what is the scenario when the test fails, could this impact end-to-end users of MultiKueue?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-02T15:14:31Z

/assign

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:08:21Z

Proposal:
/release-note-edit
```release-note
Fix a bug which occasionally prevented updates to the PodTemplate of the Job on the management cluster
when starting a Job (e.g. updating nodeSelectors), when using `MultiKueueBatchJobWithManagedBy` enabled.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:10:44Z

IIUC this is not just a fix for flaky tests, but an important bugfix for the issue which could affect production systems.
I agree it would be great to also apply the same approach for other Job CRDs  - they may or may not have similar
validation, but we also care about consistency. For other Jobs I'm ok with a follow up, if this makes this PR land before 0.9.2.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T11:15:50Z

@IrvingMg please update description with

```Fixes #3581```

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-12-04T09:32:15Z

> IIUC this is not just a fix for flaky tests, but an important bugfix for the issue which could affect production systems. I agree it would be great to also apply the same approach for other Job CRDs - they may or may not have similar validation, but we also care about consistency. For other Jobs I'm ok with a follow up, if this makes this PR land before 0.9.2.

Open #3730

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-04T10:18:14Z

/lgtm
/approve
/retitle Fix live-updates for kubernetes Job getting stuck occasionally

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-04T10:18:21Z

LGTM label has been added.  <details>Git tree hash: cc29b808ec1b919c9c640a1820f6ad354dfee0d6</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-04T10:18:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3685#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3685#issuecomment-2516852322" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-04T10:31:40Z

/cherry-pick release-0.9

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-12-04T10:32:18Z

@mimowo: new pull request created: #3731

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3685#issuecomment-2516896234):

>/cherry-pick release-0.9


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-02T08:51:11Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mimowo](https://github.com/mimowo) — 2024-12-02T08:51:11Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  This changes the implementation - is this actually a bug? If so, we should add a release note describing the scenario that is fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-02T16:24:58Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-02T16:24:57Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  The problem here is that remote job was unsuspended before local job. That's why after update status https://github.com/kubernetes-sigs/kueue/blob/c3abf9c77f5325b839cf52c27f5e8f5835f0c946/pkg/controller/jobs/job/job_multikueue_adapter.go#L67-L72 we can't update spec on jobframework https://github.com/kubernetes-sigs/kueue/blob/c3abf9c77f5325b839cf52c27f5e8f5835f0c946/pkg/controller/jobframework/reconciler.go#L478-L480.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-02T16:54:40Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mimowo](https://github.com/mimowo) — 2024-12-02T16:54:40Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  Could you maybe explain a bit more what is the update that is failing trying to update in the spec?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-02T17:01:59Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mimowo](https://github.com/mimowo) — 2024-12-02T17:01:59Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  Ah I think I know what you mean, k8s core blocks update of pod template if the job is running. And it determines that the job is running because status.startTime Is not bil

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T08:40:42Z

/cc @mszadkow

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T08:37:31Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  I think we should check it after the remote is created.

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T08:38:57Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  The same issue can occur in other adapters. It would be great to implement this in them as well.

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T08:40:21Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  It would be great to add a test case to verify skipping.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T09:02:27Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:02:27Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  FYI: [here](https://github.com/kubernetes/kubernetes/blob/810e9e212ec5372d16b655f57b9231d8654a2179/pkg/registry/batch/job/strategy.go#L192-L194) and [here](https://github.com/kubernetes/kubernetes/blob/810e9e212ec5372d16b655f57b9231d8654a2179/pkg/apis/batch/validation/validation.go#L641-L662) are the relevant places in the core k8s which reject the spec update if the status is already updated (status.startTime != nil).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T09:03:58Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:03:58Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  nit: Move the comment below, we don't inline comments with code

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-04T07:30:53Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mszadkow](https://github.com/mszadkow) — 2024-12-04T07:30:53Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  exactly, changing it here is blind change, let's only modify if remote has started

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-04T07:31:09Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mszadkow](https://github.com/mszadkow) — 2024-12-04T07:31:09Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  more tests! agree

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-12-04T09:14:56Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@IrvingMg](https://github.com/IrvingMg) — 2024-12-04T09:14:56Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-12-04T09:32:34Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@IrvingMg](https://github.com/IrvingMg) — 2024-12-04T09:32:34Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  Open #3730

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-04T09:35:50Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mimowo](https://github.com/mimowo) — 2024-12-04T09:35:50Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  This could be covered in integration test probably. right?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-12-04T09:43:36Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@IrvingMg](https://github.com/IrvingMg) — 2024-12-04T09:43:36Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  This bug seems hard to reproduce in a test case as it requires causing a failure in the reconciler of the local job while sync it with the remote one. 
  
  However, this bug was originally caught in multikueue e2e tests and after running them several times with this fix, the error didn't occur anymore.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-04T10:16:12Z

- **pkg/controller/jobs/job/job_multikueue_adapter.go:54** by [@mimowo](https://github.com/mimowo) — 2024-12-04T10:16:12Z
```diff
@@ -49,6 +50,12 @@ func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Clie
 		return err
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	if ptr.Deref(localJob.Spec.Suspend, false) { // Ensure the job is resumed before updating its status; otherwise, it will fail when patching the spec.
```

  > This bug seems hard to reproduce in a test case as it requires causing a failure in the reconciler of the local job while sync it with the remote one.
  
  I see, in that case I don't have a better test idea, maybe a unit test.
  
  > However, this bug was originally caught in multikueue e2e tests and after running them several times with this fix, the error didn't occur anymore.
  
  Yes, this is covered, just flaky, so I'm ok to accept as is. Feel free to follow up with unit tests or extra tests when working on https://github.com/kubernetes-sigs/kueue/issues/3730
