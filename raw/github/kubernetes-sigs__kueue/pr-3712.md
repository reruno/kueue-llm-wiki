# PR #3712: Implementation of KEP-3589: uniformly filter manageJobsWithoutQueueNames by namespace

**Summary**: Implementation of KEP-3589: uniformly filter manageJobsWithoutQueueNames by namespace

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3712

**Last updated**: 2024-12-11T17:01:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@dgrove-oss](https://github.com/dgrove-oss)
- **Branch**: `3589-impl` → `main`
- **Created**: 2024-12-03T03:28:36Z
- **Updated**: 2024-12-11T17:01:51Z
- **Merged**: 2024-12-05T09:20:00Z
- **Closed**: 2024-12-05T09:20:00Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@trasc](https://github.com/trasc), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 48   **+987 / -292**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Implements core feature of KEP-3589 by filtering default suspension of newly created jobs by a namespace selector.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #3589

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Adds a managedJobsNamespaceSelector to the Kueue configuration that enables namespace-based control of whether Jobs submitted without a `kueue.x-k8s.io/queue-name` label are managed by Kueue for all supported Job Kinds.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-03T03:28:53Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c2f149a6dc554998000c85972454a86d33a4d8d1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6750e6169af3480008d5ff68 |

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-04T03:11:12Z

unit and existing integration tests now passing.  

I don't quite understand the `kueue-verify-main` error is trying to tell me.  Any hints?
```/home/prow/go/src/sigs.k8s.io/kueue/bin/golangci-lint run --timeout 15m0s
pkg/controller/jobframework/defaults.go:26: File is not `gci`-ed with --skip-generated -s standard -s default -s prefix(sigs.k8s.io/kueue) -s blank -s dot (gci)
	"sigs.k8s.io/controller-runtime/pkg/client"
```

ideally, we'd add additional integration tests that verified the new feature across all the integrations, but I ran out of time today.  Could wait for a followup PR if necessary due to 0.10 deadline.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-04T14:48:52Z

> ideally, we'd add additional integration tests that verified the new feature across all the integrations, but I ran out of time today. Could wait for a followup PR if necessary due to 0.10 deadline.

I think we are good to move the release for tomorrow or EOW.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-04T23:19:33Z

/cc @mimowo @tenzen-y  I've added integration tests for the new functionality across most of the job integrations.  PR should be in good shape for hopefully final review.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-05T09:18:43Z

/lgtm
/approve
Please add "Part of #3589" in the description. As for a follow up, I think we should respect the new selector in webhooks for deployment and StatefulSet, before we set the PodGroup labels and annotations. Feel free to follow up within the issue. We also need a docs update.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-05T09:18:49Z

LGTM label has been added.  <details>Git tree hash: a7b213485338e5debf15a13714c587eca8336d90</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-05T09:18:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3712#" title="Author self-approved">dgrove-oss</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3712#issuecomment-2519718280" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-11T16:59:13Z

/kind feature

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-11T17:01:45Z

/release-note-edit
```release-note
Adds a managedJobsNamespaceSelector to the Kueue configuration that enables namespace-based control of whether Jobs submitted without a `kueue.x-k8s.io/queue-name` label are managed by Kueue for all supported Job Kinds.
```

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T12:12:57Z

- **pkg/features/kube_features.go:190** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T12:06:47Z
```diff
@@ -180,6 +187,7 @@ var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
 	ExposeFlavorsInLocalQueue:           {Default: true, PreRelease: featuregate.Beta},
 	AdmissionCheckValidationRules:       {Default: false, PreRelease: featuregate.Deprecated},
 	KeepQuotaForProvReqRetry:            {Default: false, PreRelease: featuregate.Deprecated},
+	ManagedJobsNamespaceSelector:        {Default: true, PreRelease: featuregate.Beta},
```

  Please also update [The currently supported features table](https://github.com/kubernetes-sigs/kueue/blob/dff92c9bcb5411d2b12bbf3240c977361d0ef87d/site/content/en/docs/installation/_index.md?plain=1#L234)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T12:14:14Z

- **keps/3589-manage-jobs-selectively/kep.yaml:5** by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:44:48Z
```diff
@@ -2,7 +2,7 @@ title: Uniformly filter manageJobsWithoutQueueNames by namespace
 kep-number: 3589
 authors:
   - "@dgrove-oss"
-status: provisional
+status: implemented
```

  implementable
  ```suggestion
  status: implementable
  ```
  nit: implemented: "The KEP has been implemented and is no longer actively changed.", which effectively means stable, from https://github.com/kubernetes/enhancements/blob/master/keps/sig-architecture/0000-kep-process/README.md

- **pkg/config/validation.go:228** by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:47:36Z
```diff
@@ -205,31 +206,42 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 		return allErrs
 	}
 
-	if c.Integrations.PodOptions == nil {
-		return field.ErrorList{field.Required(podOptionsPath, "cannot be empty when pod integration is enabled")}
-	}
-	if c.Integrations.PodOptions.NamespaceSelector == nil {
-		return field.ErrorList{field.Required(namespaceSelectorPath, "a namespace selector is required")}
-	}
-
+	// If the pod integration is enabled, there must be a namespace selector that exempts every prohibitedNamespace
 	prohibitedNamespaces := []labels.Set{{corev1.LabelMetadataName: metav1.NamespaceSystem}}
-
 	if c.Namespace != nil && *c.Namespace != "" {
 		prohibitedNamespaces = append(prohibitedNamespaces, labels.Set{corev1.LabelMetadataName: *c.Namespace})
 	}
 
-	allErrs = append(allErrs, validation.ValidateLabelSelector(c.Integrations.PodOptions.NamespaceSelector, validation.LabelSelectorValidationOptions{}, namespaceSelectorPath)...)
+	if c.ManagedJobsNamespaceSelector != nil {
+		allErrs = append(allErrs, validation.ValidateLabelSelector(c.ManagedJobsNamespaceSelector, validation.LabelSelectorValidationOptions{}, managedJobsNamespaceSelectorPath)...)
+		selector, err := metav1.LabelSelectorAsSelector(c.ManagedJobsNamespaceSelector)
+		if err != nil {
+			return allErrs
+		}
 
-	selector, err := metav1.LabelSelectorAsSelector(c.Integrations.PodOptions.NamespaceSelector)
-	if err != nil {
-		return allErrs
-	}
+		for _, pn := range prohibitedNamespaces {
+			if selector.Matches(pn) {
+				allErrs = append(allErrs, field.Invalid(managedJobsNamespaceSelectorPath, c.ManagedJobsNamespaceSelector,
+					fmt.Sprintf("should not match the %q namespace", pn[corev1.LabelMetadataName])))
+			}
+		}
+	} else if c.Integrations.PodOptions != nil && c.Integrations.PodOptions.NamespaceSelector != nil {
```

  why "else" - it seems it makes sense to validate PodOptions regardless of `ManagedJobsNamespaceSelector`?

- **pkg/controller/jobframework/defaults.go:38** by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:53:30Z
```diff
@@ -17,18 +17,43 @@ limitations under the License.
 package jobframework
 
 import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
-func ApplyDefaultForSuspend(job GenericJob, manageJobsWithoutQueueName bool) {
+func ApplyDefaultForSuspend(job GenericJob, k8sClient client.Client, ctx context.Context,
+	manageJobsWithoutQueueName bool, managedJobsNamespaceSelector *metav1.LabelSelector) error {
+
 	// Do not default suspend a job whose owner is already managed by Kueue
 	if owner := metav1.GetControllerOf(job.Object()); owner != nil && IsOwnerManagedByKueue(owner) {
-		return
+		return nil
+	}
+
+	// Do not default suspend a job without a queue name unless the namespace selector also matches
+	if QueueName(job) == "" && manageJobsWithoutQueueName && managedJobsNamespaceSelector != nil {
```

  IIUC this should be guarded by the feature-gate.

- **pkg/controller/jobframework/defaults.go:47** by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:54:00Z
```diff
@@ -17,18 +17,43 @@ limitations under the License.
 package jobframework
 
 import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
-func ApplyDefaultForSuspend(job GenericJob, manageJobsWithoutQueueName bool) {
+func ApplyDefaultForSuspend(job GenericJob, k8sClient client.Client, ctx context.Context,
+	manageJobsWithoutQueueName bool, managedJobsNamespaceSelector *metav1.LabelSelector) error {
+
 	// Do not default suspend a job whose owner is already managed by Kueue
 	if owner := metav1.GetControllerOf(job.Object()); owner != nil && IsOwnerManagedByKueue(owner) {
-		return
+		return nil
+	}
+
+	// Do not default suspend a job without a queue name unless the namespace selector also matches
+	if QueueName(job) == "" && manageJobsWithoutQueueName && managedJobsNamespaceSelector != nil {
+		ns := corev1.Namespace{}
+		err := k8sClient.Get(ctx, client.ObjectKey{Name: job.Object().GetNamespace()}, &ns)
+		if err != nil {
+			return fmt.Errorf("failed to get namespace: %w", err)
+		}
+		nsSelector, err := metav1.LabelSelectorAsSelector(managedJobsNamespaceSelector)
+		if err != nil {
+			return fmt.Errorf("failed to parse namespace selector: %w", err)
+		}
```

  Can we pass the already passed selector to improve performance?

- **pkg/controller/jobframework/defaults.go:41** by [@mimowo](https://github.com/mimowo) — 2024-12-03T09:55:08Z
```diff
@@ -17,18 +17,43 @@ limitations under the License.
 package jobframework
 
 import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
-func ApplyDefaultForSuspend(job GenericJob, manageJobsWithoutQueueName bool) {
+func ApplyDefaultForSuspend(job GenericJob, k8sClient client.Client, ctx context.Context,
+	manageJobsWithoutQueueName bool, managedJobsNamespaceSelector *metav1.LabelSelector) error {
+
 	// Do not default suspend a job whose owner is already managed by Kueue
 	if owner := metav1.GetControllerOf(job.Object()); owner != nil && IsOwnerManagedByKueue(owner) {
-		return
+		return nil
+	}
+
+	// Do not default suspend a job without a queue name unless the namespace selector also matches
+	if QueueName(job) == "" && manageJobsWithoutQueueName && managedJobsNamespaceSelector != nil {
+		ns := corev1.Namespace{}
+		err := k8sClient.Get(ctx, client.ObjectKey{Name: job.Object().GetNamespace()}, &ns)
```

  It would be good to ensure the Get is using controller-runtime informer cache (I'm not sure). You could check this with debugger in integration tests. Doing an actual Get request from webhook would not be good, but if this hits cache, then it is ok.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-12-03T14:42:37Z

- **pkg/config/validation.go:228** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-03T14:42:37Z
```diff
@@ -205,31 +206,42 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 		return allErrs
 	}
 
-	if c.Integrations.PodOptions == nil {
-		return field.ErrorList{field.Required(podOptionsPath, "cannot be empty when pod integration is enabled")}
-	}
-	if c.Integrations.PodOptions.NamespaceSelector == nil {
-		return field.ErrorList{field.Required(namespaceSelectorPath, "a namespace selector is required")}
-	}
-
+	// If the pod integration is enabled, there must be a namespace selector that exempts every prohibitedNamespace
 	prohibitedNamespaces := []labels.Set{{corev1.LabelMetadataName: metav1.NamespaceSystem}}
-
 	if c.Namespace != nil && *c.Namespace != "" {
 		prohibitedNamespaces = append(prohibitedNamespaces, labels.Set{corev1.LabelMetadataName: *c.Namespace})
 	}
 
-	allErrs = append(allErrs, validation.ValidateLabelSelector(c.Integrations.PodOptions.NamespaceSelector, validation.LabelSelectorValidationOptions{}, namespaceSelectorPath)...)
+	if c.ManagedJobsNamespaceSelector != nil {
+		allErrs = append(allErrs, validation.ValidateLabelSelector(c.ManagedJobsNamespaceSelector, validation.LabelSelectorValidationOptions{}, managedJobsNamespaceSelectorPath)...)
+		selector, err := metav1.LabelSelectorAsSelector(c.ManagedJobsNamespaceSelector)
+		if err != nil {
+			return allErrs
+		}
 
-	selector, err := metav1.LabelSelectorAsSelector(c.Integrations.PodOptions.NamespaceSelector)
-	if err != nil {
-		return allErrs
-	}
+		for _, pn := range prohibitedNamespaces {
+			if selector.Matches(pn) {
+				allErrs = append(allErrs, field.Invalid(managedJobsNamespaceSelectorPath, c.ManagedJobsNamespaceSelector,
+					fmt.Sprintf("should not match the %q namespace", pn[corev1.LabelMetadataName])))
+			}
+		}
+	} else if c.Integrations.PodOptions != nil && c.Integrations.PodOptions.NamespaceSelector != nil {
```

  This was a first attempt to try to support a transition from `podOptions.namespaceSelector` to `managedJobsNamespaceSelector` and not require both to be set without making the logic overly complicated.
  
  If `managedJobsNamespaceSelector` is non-nil, then we can allow `podOptions.namespaceSelector` to be nil.
  
  But you are right that there is still some validation of a non-nil ``podOptions.namespaceSelector` that should be done if both are non-nil.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-12-03T14:43:48Z

- **pkg/controller/jobframework/defaults.go:41** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-03T14:43:47Z
```diff
@@ -17,18 +17,43 @@ limitations under the License.
 package jobframework
 
 import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
-func ApplyDefaultForSuspend(job GenericJob, manageJobsWithoutQueueName bool) {
+func ApplyDefaultForSuspend(job GenericJob, k8sClient client.Client, ctx context.Context,
+	manageJobsWithoutQueueName bool, managedJobsNamespaceSelector *metav1.LabelSelector) error {
+
 	// Do not default suspend a job whose owner is already managed by Kueue
 	if owner := metav1.GetControllerOf(job.Object()); owner != nil && IsOwnerManagedByKueue(owner) {
-		return
+		return nil
+	}
+
+	// Do not default suspend a job without a queue name unless the namespace selector also matches
+	if QueueName(job) == "" && manageJobsWithoutQueueName && managedJobsNamespaceSelector != nil {
+		ns := corev1.Namespace{}
+		err := k8sClient.Get(ctx, client.ObjectKey{Name: job.Object().GetNamespace()}, &ns)
```

  Should be using the cache.  Similar get is already in the pod webhook.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-12-03T14:47:58Z

- **keps/3589-manage-jobs-selectively/kep.yaml:5** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-03T14:47:58Z
```diff
@@ -2,7 +2,7 @@ title: Uniformly filter manageJobsWithoutQueueNames by namespace
 kep-number: 3589
 authors:
   - "@dgrove-oss"
-status: provisional
+status: implemented
```

  Thanks.  I will change to implementable.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-12-03T20:30:49Z

- **pkg/features/kube_features.go:190** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-03T20:30:49Z
```diff
@@ -180,6 +187,7 @@ var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
 	ExposeFlavorsInLocalQueue:           {Default: true, PreRelease: featuregate.Beta},
 	AdmissionCheckValidationRules:       {Default: false, PreRelease: featuregate.Deprecated},
 	KeepQuotaForProvReqRetry:            {Default: false, PreRelease: featuregate.Deprecated},
+	ManagedJobsNamespaceSelector:        {Default: true, PreRelease: featuregate.Beta},
```

  Table updated.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-12-03T20:31:58Z

- **pkg/controller/jobframework/defaults.go:47** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-03T20:31:57Z
```diff
@@ -17,18 +17,43 @@ limitations under the License.
 package jobframework
 
 import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
-func ApplyDefaultForSuspend(job GenericJob, manageJobsWithoutQueueName bool) {
+func ApplyDefaultForSuspend(job GenericJob, k8sClient client.Client, ctx context.Context,
+	manageJobsWithoutQueueName bool, managedJobsNamespaceSelector *metav1.LabelSelector) error {
+
 	// Do not default suspend a job whose owner is already managed by Kueue
 	if owner := metav1.GetControllerOf(job.Object()); owner != nil && IsOwnerManagedByKueue(owner) {
-		return
+		return nil
+	}
+
+	// Do not default suspend a job without a queue name unless the namespace selector also matches
+	if QueueName(job) == "" && manageJobsWithoutQueueName && managedJobsNamespaceSelector != nil {
+		ns := corev1.Namespace{}
+		err := k8sClient.Get(ctx, client.ObjectKey{Name: job.Object().GetNamespace()}, &ns)
+		if err != nil {
+			return fmt.Errorf("failed to get namespace: %w", err)
+		}
+		nsSelector, err := metav1.LabelSelectorAsSelector(managedJobsNamespaceSelector)
+		if err != nil {
+			return fmt.Errorf("failed to parse namespace selector: %w", err)
+		}
```

  Yes.  I reworked to parse selector during startup and store the resulting `labels.Selector` in the webhooks/job reconciler.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-12-04T03:06:34Z

- **pkg/config/validation.go:228** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-12-04T03:06:34Z
```diff
@@ -205,31 +206,42 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 		return allErrs
 	}
 
-	if c.Integrations.PodOptions == nil {
-		return field.ErrorList{field.Required(podOptionsPath, "cannot be empty when pod integration is enabled")}
-	}
-	if c.Integrations.PodOptions.NamespaceSelector == nil {
-		return field.ErrorList{field.Required(namespaceSelectorPath, "a namespace selector is required")}
-	}
-
+	// If the pod integration is enabled, there must be a namespace selector that exempts every prohibitedNamespace
 	prohibitedNamespaces := []labels.Set{{corev1.LabelMetadataName: metav1.NamespaceSystem}}
-
 	if c.Namespace != nil && *c.Namespace != "" {
 		prohibitedNamespaces = append(prohibitedNamespaces, labels.Set{corev1.LabelMetadataName: *c.Namespace})
 	}
 
-	allErrs = append(allErrs, validation.ValidateLabelSelector(c.Integrations.PodOptions.NamespaceSelector, validation.LabelSelectorValidationOptions{}, namespaceSelectorPath)...)
+	if c.ManagedJobsNamespaceSelector != nil {
+		allErrs = append(allErrs, validation.ValidateLabelSelector(c.ManagedJobsNamespaceSelector, validation.LabelSelectorValidationOptions{}, managedJobsNamespaceSelectorPath)...)
+		selector, err := metav1.LabelSelectorAsSelector(c.ManagedJobsNamespaceSelector)
+		if err != nil {
+			return allErrs
+		}
 
-	selector, err := metav1.LabelSelectorAsSelector(c.Integrations.PodOptions.NamespaceSelector)
-	if err != nil {
-		return allErrs
-	}
+		for _, pn := range prohibitedNamespaces {
+			if selector.Matches(pn) {
+				allErrs = append(allErrs, field.Invalid(managedJobsNamespaceSelectorPath, c.ManagedJobsNamespaceSelector,
+					fmt.Sprintf("should not match the %q namespace", pn[corev1.LabelMetadataName])))
+			}
+		}
+	} else if c.Integrations.PodOptions != nil && c.Integrations.PodOptions.NamespaceSelector != nil {
```

  reworked this.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-04T06:48:34Z

I did not review the entire codes. I just left a comment how to resolve CI errors.

- **pkg/controller/jobframework/defaults.go:28** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-04T06:47:55Z
```diff
@@ -17,18 +17,39 @@ limitations under the License.
 package jobframework
 
 import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/kueue/pkg/features"
```

  ```suggestion
  	"sigs.k8s.io/controller-runtime/pkg/client"
  
  	"sigs.k8s.io/kueue/pkg/features"
  ```
  
  This should resolve CI errors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-05T09:09:58Z

- **pkg/controller/jobframework/defaults.go:41** by [@mimowo](https://github.com/mimowo) — 2024-12-05T09:09:58Z
```diff
@@ -17,18 +17,43 @@ limitations under the License.
 package jobframework
 
 import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 )
 
-func ApplyDefaultForSuspend(job GenericJob, manageJobsWithoutQueueName bool) {
+func ApplyDefaultForSuspend(job GenericJob, k8sClient client.Client, ctx context.Context,
+	manageJobsWithoutQueueName bool, managedJobsNamespaceSelector *metav1.LabelSelector) error {
+
 	// Do not default suspend a job whose owner is already managed by Kueue
 	if owner := metav1.GetControllerOf(job.Object()); owner != nil && IsOwnerManagedByKueue(owner) {
-		return
+		return nil
+	}
+
+	// Do not default suspend a job without a queue name unless the namespace selector also matches
+	if QueueName(job) == "" && manageJobsWithoutQueueName && managedJobsNamespaceSelector != nil {
+		ns := corev1.Namespace{}
+		err := k8sClient.Get(ctx, client.ObjectKey{Name: job.Object().GetNamespace()}, &ns)
```

  sgtm
