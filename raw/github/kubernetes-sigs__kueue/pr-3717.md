# PR #3717: Use ReadUIntFromLabelWithMax to get and validate pod index.

**Summary**: Use ReadUIntFromLabelWithMax to get and validate pod index.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3717

**Last updated**: 2024-12-03T15:23:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/validate-pod-index` → `main`
- **Created**: 2024-12-03T10:40:29Z
- **Updated**: 2024-12-03T15:23:02Z
- **Merged**: 2024-12-03T15:23:01Z
- **Closed**: 2024-12-03T15:23:01Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 4   **+31 / -41**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use ReadUIntFromLabelWithMax to get and validate pod index.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3651

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-03T10:40:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4d2f38bab64ce47746f41140a9a457159c51cfa4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/674f17395d2a1b000877d65e |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-12-03T14:47:02Z

/lgtm
Thank you!
/cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-03T14:47:10Z

LGTM label has been added.  <details>Git tree hash: a4c2e67e0ea07c374dd197847801d29eba9e0aa9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-03T15:05:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3717#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3717#pullrequestreview-2475974701" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [mimowo]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T10:41:40Z

/assign @PBundyra

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-03T12:33:48Z

- **pkg/util/pod/pod.go:102** by [@PBundyra](https://github.com/PBundyra) — 2024-12-03T12:33:47Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  Why do we want to return error if `uintValue == max`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T12:42:13Z

- **pkg/util/pod/pod.go:102** by [@mimowo](https://github.com/mimowo) — 2024-12-03T12:42:13Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  +1, also why is the PR changing the function, I think for pod-group validation we can reuse `ReadUIntFromLabelBelowBound` without adding the +1

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T12:44:36Z

- **pkg/util/pod/pod.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T12:44:36Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  > Why do we want to return error if `uintValue == max`?
  
  In the previous implementation, we were checking for equality.
  
  https://github.com/kubernetes-sigs/kueue/pull/3717/files#diff-dfb49586a8522fa91d733051fd3b7e4b3ff174907898cf249ab16e2620976a5dL572-L574

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T12:48:30Z

- **pkg/util/pod/pod.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T12:48:30Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  > +1, also why is the PR changing the function, I think for pod-group validation we can reuse `ReadUIntFromLabelBelowBound` without adding the +1
  
  In that case on pod controller we need to set -1.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-03T12:54:25Z

- **pkg/util/pod/pod.go:102** by [@PBundyra](https://github.com/PBundyra) — 2024-12-03T12:54:25Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  The previous implementation was pod specific and this is more generic function. Agree, that in pod controller we should call this function with `GroupCount - 1`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T12:57:24Z

- **pkg/util/pod/pod.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T12:57:24Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  Not sure that it is correct way. Imagine situation that you have podGroupTotalCount=1. Minus 1 it would be 0. And we can't compare with -1 due to we expect uint.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T13:03:51Z

- **pkg/util/pod/pod.go:102** by [@mimowo](https://github.com/mimowo) — 2024-12-03T13:03:51Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  I don't get it, pod group pod index can be only from 0 to group size -1, so using `ReadUIntFromLabelBelowBound` should be ok as is. No need to change the code for existing helper functions

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T13:14:05Z

- **pkg/util/pod/pod.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T13:14:04Z
```diff
@@ -65,35 +65,42 @@ func gateIndex(p *corev1.Pod, gateName string) int {
 }
 
 var (
-	errLabelNotFound = errors.New("label not found")
-	errInvalidUInt   = errors.New("invalid unsigned integer")
-	errValidation    = errors.New("validation error")
+	ErrLabelNotFound = errors.New("label not found")
+	ErrInvalidUInt   = errors.New("invalid unsigned integer")
+	ErrValidation    = errors.New("validation error")
 )
 
+func IgnoreLabelNotFoundError(err error) error {
+	if errors.Is(err, ErrLabelNotFound) {
+		return nil
+	}
+	return err
+}
+
 func ReadUIntFromLabel(obj client.Object, labelKey string) (*int, error) {
-	return ReadUIntFromLabelBelowBound(obj, labelKey, math.MaxInt)
+	return ReadUIntFromLabelWithMax(obj, labelKey, math.MaxInt)
 }
 
-func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int) (*int, error) {
+func ReadUIntFromLabelWithMax(obj client.Object, labelKey string, max int) (*int, error) {
 	value, found := obj.GetLabels()[labelKey]
 	kind := obj.GetObjectKind().GroupVersionKind().Kind
 	if !found {
-		return nil, fmt.Errorf("%w: no label %q for %s %q", errLabelNotFound, labelKey, kind, klog.KObj(obj))
+		return nil, fmt.Errorf("%w: no label %q for %s %q", ErrLabelNotFound, labelKey, kind, klog.KObj(obj))
 	}
-	intValue, err := readUIntFromStringBelowBound(value, bound)
+	intValue, err := readUIntFromStringWithMax(value, max)
 	if err != nil {
 		return nil, fmt.Errorf("incorrect label value %q for %s %q: %w", value, kind, klog.KObj(obj), err)
 	}
 	return intValue, nil
 }
 
-func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
+func readUIntFromStringWithMax(value string, max int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
-	if uintValue > uint64(bound) {
-		return nil, fmt.Errorf("%w: value should be less than %d", errValidation, bound)
+	if uintValue >= uint64(max) {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T13:18:33Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mimowo](https://github.com/mimowo) — 2024-12-03T13:18:33Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  Why the `-1`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T13:22:39Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T13:22:39Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  > Why do we want to return error if `uintValue == max`?
  
  In the previous implementation, we were checking for equality.
  
  https://github.com/kubernetes-sigs/kueue/pull/3717/files#diff-dfb49586a8522fa91d733051fd3b7e4b3ff174907898cf249ab16e2620976a5dL572-L574

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T13:22:57Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T13:22:56Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  So here it should be -1

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-03T13:45:00Z

- **pkg/util/pod/pod.go:102** by [@PBundyra](https://github.com/PBundyra) — 2024-12-03T13:44:59Z
```diff
@@ -90,10 +97,10 @@ func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int)
 func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
 	if uintValue > uint64(bound) {
```

  Can we set here `>=` so it aligns with the function name and the error we return?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-03T13:45:29Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@PBundyra](https://github.com/PBundyra) — 2024-12-03T13:45:29Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  After this fix: https://github.com/kubernetes-sigs/kueue/pull/3717/files#r1867747724 there should not be a need to pass `-1`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T13:46:51Z

- **pkg/util/pod/pod.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T13:46:51Z
```diff
@@ -90,10 +97,10 @@ func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int)
 func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
 	if uintValue > uint64(bound) {
```

  I already reverted changes. Or what do you mean?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T13:51:14Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T13:51:14Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  It will not work due to https://github.com/kubernetes-sigs/kueue/pull/3717#discussion_r1867688322.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T14:04:44Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mimowo](https://github.com/mimowo) — 2024-12-03T14:04:43Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  Sorry, in my comment I missed the bug in the `ReadUIntFromLabelBelowBound` function, as indicated by @PBundyra

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T14:05:22Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mimowo](https://github.com/mimowo) — 2024-12-03T14:05:22Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  I think if we fix `ReadUIntFromLabelBelowBound` then we can just call it without any -1 / +1

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T14:06:19Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T14:06:19Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  In that case on topology we need to set +1

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-03T14:11:20Z

- **pkg/util/pod/pod.go:102** by [@PBundyra](https://github.com/PBundyra) — 2024-12-03T14:11:08Z
```diff
@@ -90,10 +97,10 @@ func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int)
 func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
 	if uintValue > uint64(bound) {
```

  My suggestion is like that
  ```suggestion
  	if uintValue >= uint64(bound) {
  ```
  
  I believe checking if the uintValue is strictly greater is a bug

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T14:20:20Z

- **pkg/util/pod/pod.go:102** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T14:20:20Z
```diff
@@ -90,10 +97,10 @@ func ReadUIntFromLabelBelowBound(obj client.Object, labelKey string, bound int)
 func readUIntFromStringBelowBound(value string, bound int) (*int, error) {
 	uintValue, err := strconv.ParseUint(value, 10, 0)
 	if err != nil {
-		return nil, fmt.Errorf("%w: %s", errInvalidUInt, err.Error())
+		return nil, fmt.Errorf("%w: %s", ErrInvalidUInt, err.Error())
 	}
 	if uintValue > uint64(bound) {
```

  Ah, my bad. Thank you so much!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T14:20:39Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T14:20:39Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-03T14:35:49Z

- **pkg/controller/jobs/pod/pod_controller.go:708** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-03T14:35:49Z
```diff
@@ -725,9 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	if err != nil {
 		return err
 	}
-	if _, err = p.podGroupIndex(groupTotalCount); err != nil {
+
+	_, err = utilpod.ReadUIntFromLabelBelowBound(p.Object(), kueuealpha.PodGroupPodIndexLabel, groupTotalCount-1)
```

  FIxed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-03T15:05:11Z

/approve
