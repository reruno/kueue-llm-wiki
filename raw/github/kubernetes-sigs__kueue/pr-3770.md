# PR #3770: Deactivate ClusterQueue if there is no Topology

**Summary**: Deactivate ClusterQueue if there is no Topology

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3770

**Last updated**: 2025-01-21T10:54:00Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-deactivate-cq` → `main`
- **Created**: 2024-12-09T11:53:48Z
- **Updated**: 2025-01-21T10:54:00Z
- **Merged**: 2024-12-11T16:48:04Z
- **Closed**: 2024-12-11T16:48:04Z
- **Labels**: `kind/bug`, `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 9   **+265 / -36**

## Description

#### What type of PR is this?

/kind feature
/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3645 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: The CQ referencing a Topology is deactivated if the topology does not exist.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-09T11:54:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a5d3065d3658f8d6bb7e85f0a6fbda0031e4b4f5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6759be238b2c4700085e772f |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-10T13:31:50Z

FYI: I repro the data race issue locally, will investigate and push the update, most likely tomorrow

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-11T12:35:50Z

/assign @PBundyra

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-12-11T16:31:17Z

/hold
/lgtm
We agreed with @mimowo on running integration tests several dozen times to ensure they're not flaky. Thanks for the contriubtion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-11T16:31:24Z

LGTM label has been added.  <details>Git tree hash: 4eddf9b9cb51308f398e52a35684e3c78f1cd5b6</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-11T16:46:59Z

/lgtm
/approve
/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-11T16:47:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3770#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3770#issuecomment-2536527108" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-11T16:47:40Z

> We agreed with @mimowo on running integration tests several dozen times to ensure they're not flaky. Thanks for the contriubtion

That's right, I have done over 50 repeats already of the two newly added integration tests and they passed every time, using the command with `--race`:
```
GINKGO_ARGS="--timeout=5h --repeat=100 --race" INTEGRATION_NPROCS=1 INTEGRATION_TARGET=./test/integration/tas/ make test-integration
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-11T16:47:49Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-12T06:11:41Z

/cherry-pick release-0.9

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-12-12T06:12:17Z

@tenzen-y: #3770 failed to apply on top of branch "release-0.9":
```
Applying: Deactivate ClusterQueue if there is no Topology
Using index info to reconstruct a base tree...
M	pkg/cache/cache.go
M	pkg/cache/clusterqueue.go
M	pkg/cache/clusterqueue_test.go
M	pkg/controller/core/clusterqueue_controller.go
M	pkg/controller/core/core.go
M	pkg/controller/tas/resource_flavor.go
M	pkg/queue/manager.go
M	test/integration/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/tas/tas_test.go
CONFLICT (content): Merge conflict in test/integration/tas/tas_test.go
Auto-merging pkg/queue/manager.go
Auto-merging pkg/controller/tas/resource_flavor.go
CONFLICT (content): Merge conflict in pkg/controller/tas/resource_flavor.go
Auto-merging pkg/controller/core/core.go
Auto-merging pkg/controller/core/clusterqueue_controller.go
Auto-merging pkg/cache/clusterqueue_test.go
CONFLICT (content): Merge conflict in pkg/cache/clusterqueue_test.go
Auto-merging pkg/cache/clusterqueue.go
Auto-merging pkg/cache/cache.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Deactivate ClusterQueue if there is no Topology

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3770#issuecomment-2537902711):

>/cherry-pick release-0.9
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-12T06:12:40Z

@mimowo Could you manually create the cherry-pick PR against release-0.9?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-12T06:20:08Z

Yes, I will try

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-12T06:49:57Z

Prepared: https://github.com/kubernetes-sigs/kueue/pull/3819

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-11T14:16:32Z

- **test/integration/tas/tas_test.go:610** by [@PBundyra](https://github.com/PBundyra) — 2024-12-11T14:16:31Z
```diff
@@ -539,8 +546,82 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
 				})
 
-				topology = testing.MakeDefaultTwoLevelTopology("default")
-				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+				ginkgo.By("recreate the Topology and wait for the queue to become active", func() {
+					topology = testing.MakeDefaultTwoLevelTopology("default")
+					gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+					util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{testing.DefaultBlockTopologyLevel, testing.DefaultRackTopologyLevel},
+							Domains: []kueue.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"b1", "r1"}},
+								{Count: 1, Values: []string{"b1", "r2"}},
+							},
+						},
+					))
+				})
+			})
+
+			ginkgo.It("should not admit the workload after the TAS RF is deleted and admit it after the RF is re-created", func() {
+				ginkgo.By("remove TAS RF finalizers to allow deletion", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						var updatedFlavor kueue.ResourceFlavor
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(tasFlavor), &updatedFlavor)).To(gomega.Succeed())
+						updatedFlavor.Finalizers = nil
+						g.Expect(k8sClient.Update(ctx, &updatedFlavor)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("delete TAS RF", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				})
+
+				ginkgo.By("await for the CQ to become inactive", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						var updatedCq kueue.ClusterQueue
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCq)).To(gomega.Succeed())
+						g.Expect(updatedCq.Status.Conditions).Should(gomega.BeComparableTo([]metav1.Condition{
+							{
+								Type:    kueue.ClusterQueueActive,
+								Status:  metav1.ConditionFalse,
+								Reason:  "FlavorNotFound",
+								Message: `Can't admit new workloads: references missing ResourceFlavor(s): [tas-flavor].`,
+							},
+						}, util.IgnoreConditionTimestampsAndObservedGeneration))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl = testing.MakeWorkload("wl", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl.Spec.PodSets[0].Count = 2
+					wl.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultBlockTopologyLevel),
+					}
```

  Can we use `PodSets()` function here and above?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T14:24:07Z

- **test/integration/tas/tas_test.go:610** by [@mimowo](https://github.com/mimowo) — 2024-12-11T14:24:06Z
```diff
@@ -539,8 +546,82 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
 				})
 
-				topology = testing.MakeDefaultTwoLevelTopology("default")
-				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+				ginkgo.By("recreate the Topology and wait for the queue to become active", func() {
+					topology = testing.MakeDefaultTwoLevelTopology("default")
+					gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+					util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{testing.DefaultBlockTopologyLevel, testing.DefaultRackTopologyLevel},
+							Domains: []kueue.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"b1", "r1"}},
+								{Count: 1, Values: []string{"b1", "r2"}},
+							},
+						},
+					))
+				})
+			})
+
+			ginkgo.It("should not admit the workload after the TAS RF is deleted and admit it after the RF is re-created", func() {
+				ginkgo.By("remove TAS RF finalizers to allow deletion", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						var updatedFlavor kueue.ResourceFlavor
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(tasFlavor), &updatedFlavor)).To(gomega.Succeed())
+						updatedFlavor.Finalizers = nil
+						g.Expect(k8sClient.Update(ctx, &updatedFlavor)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("delete TAS RF", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				})
+
+				ginkgo.By("await for the CQ to become inactive", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						var updatedCq kueue.ClusterQueue
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCq)).To(gomega.Succeed())
+						g.Expect(updatedCq.Status.Conditions).Should(gomega.BeComparableTo([]metav1.Condition{
+							{
+								Type:    kueue.ClusterQueueActive,
+								Status:  metav1.ConditionFalse,
+								Reason:  "FlavorNotFound",
+								Message: `Can't admit new workloads: references missing ResourceFlavor(s): [tas-flavor].`,
+							},
+						}, util.IgnoreConditionTimestampsAndObservedGeneration))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl = testing.MakeWorkload("wl", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl.Spec.PodSets[0].Count = 2
+					wl.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultBlockTopologyLevel),
+					}
```

  sure, that was just a quick copy-paste, let me clean it up

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-11T14:29:01Z

- **pkg/controller/tas/resource_flavor.go:143** by [@PBundyra](https://github.com/PBundyra) — 2024-12-11T14:29:00Z
```diff
@@ -142,6 +143,8 @@ var _ handler.EventHandler = (*topologyHandler)(nil)
 // topologyHandler handles node update events.
```

  Can we update this comment?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-11T14:33:23Z

- **pkg/controller/tas/resource_flavor.go:166** by [@PBundyra](https://github.com/PBundyra) — 2024-12-11T14:33:23Z
```diff
@@ -158,13 +161,16 @@ func (h *topologyHandler) Create(ctx context.Context, e event.CreateEvent, q wor
 		return
 	}
 
+	defer h.notifyTopologyWatchers(nil, topology)
+
 	// trigger reconcile for TAS flavors affected by the node being created or updated
```

  Is the reconcile loop triggered if the nodes are created/updated or topology?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T14:42:54Z

- **test/integration/tas/tas_test.go:610** by [@mimowo](https://github.com/mimowo) — 2024-12-11T14:42:54Z
```diff
@@ -539,8 +546,82 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
 				})
 
-				topology = testing.MakeDefaultTwoLevelTopology("default")
-				gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+				ginkgo.By("recreate the Topology and wait for the queue to become active", func() {
+					topology = testing.MakeDefaultTwoLevelTopology("default")
+					gomega.Expect(k8sClient.Create(ctx, topology)).Should(gomega.Succeed())
+					util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+				})
+
+				ginkgo.By("verify the workload is admitted", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+				})
+
+				ginkgo.By("verify admission for the workload", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						&kueue.TopologyAssignment{
+							Levels: []string{testing.DefaultBlockTopologyLevel, testing.DefaultRackTopologyLevel},
+							Domains: []kueue.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"b1", "r1"}},
+								{Count: 1, Values: []string{"b1", "r2"}},
+							},
+						},
+					))
+				})
+			})
+
+			ginkgo.It("should not admit the workload after the TAS RF is deleted and admit it after the RF is re-created", func() {
+				ginkgo.By("remove TAS RF finalizers to allow deletion", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						var updatedFlavor kueue.ResourceFlavor
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(tasFlavor), &updatedFlavor)).To(gomega.Succeed())
+						updatedFlavor.Finalizers = nil
+						g.Expect(k8sClient.Update(ctx, &updatedFlavor)).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("delete TAS RF", func() {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavor, true)
+				})
+
+				ginkgo.By("await for the CQ to become inactive", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						var updatedCq kueue.ClusterQueue
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCq)).To(gomega.Succeed())
+						g.Expect(updatedCq.Status.Conditions).Should(gomega.BeComparableTo([]metav1.Condition{
+							{
+								Type:    kueue.ClusterQueueActive,
+								Status:  metav1.ConditionFalse,
+								Reason:  "FlavorNotFound",
+								Message: `Can't admit new workloads: references missing ResourceFlavor(s): [tas-flavor].`,
+							},
+						}, util.IgnoreConditionTimestampsAndObservedGeneration))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				var wl *kueue.Workload
+				ginkgo.By("creating a workload which requires block and can fit", func() {
+					wl = testing.MakeWorkload("wl", ns.Name).
+						Queue(localQueue.Name).Request(corev1.ResourceCPU, "1").Obj()
+					wl.Spec.PodSets[0].Count = 2
+					wl.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultBlockTopologyLevel),
+					}
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T14:47:31Z

- **pkg/controller/tas/resource_flavor.go:143** by [@mimowo](https://github.com/mimowo) — 2024-12-11T14:47:31Z
```diff
@@ -142,6 +143,8 @@ var _ handler.EventHandler = (*topologyHandler)(nil)
 // topologyHandler handles node update events.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T14:47:55Z

- **pkg/controller/tas/resource_flavor.go:166** by [@mimowo](https://github.com/mimowo) — 2024-12-11T14:47:55Z
```diff
@@ -158,13 +161,16 @@ func (h *topologyHandler) Create(ctx context.Context, e event.CreateEvent, q wor
 		return
 	}
 
+	defer h.notifyTopologyWatchers(nil, topology)
+
 	// trigger reconcile for TAS flavors affected by the node being created or updated
```

  topology, that was a stale comment. Fixed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-11T15:39:58Z

- **pkg/controller/core/clusterqueue_controller.go:640** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-11T15:33:29Z
```diff
@@ -594,6 +613,34 @@ func (h *cqAdmissionCheckHandler) Generic(_ context.Context, e event.GenericEven
 	}
 }
 
+type cqTopologyHandler struct {
+	cache *cache.Cache
+}
+
+func (h *cqTopologyHandler) Create(context.Context, event.CreateEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Update(context.Context, event.UpdateEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Delete(context.Context, event.DeleteEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Generic(_ context.Context, e event.GenericEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	topology, isTopology := e.Object.(*kueuealpha.Topology)
+	if !isTopology {
+		return
+	}
+	cqs := h.cache.ClusterQueuesUsingTopology(kueue.TopologyReference(topology.Name))
+	for _, cq := range cqs {
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name: cq,
+			}}
+		q.AddAfter(req, time.Second)
```

  Why do we want to delay a second?
  We usually set delay only when we can imagine Object operation frequently.
  IIUC, the topology resources will not be modified frequently, right?

- **pkg/controller/core/core.go:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-11T15:37:56Z
```diff
@@ -83,6 +83,7 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	).SetupWithManager(mgr, cfg); err != nil {
 		return "Workload", err
 	}
+	qManager.AddTopologyUpdateWatcher(cqRec)
```

  Can we add topology watcher in the resonciler so that we can avoid to take the qManager lock?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T15:48:26Z

- **pkg/controller/core/clusterqueue_controller.go:640** by [@mimowo](https://github.com/mimowo) — 2024-12-11T15:48:26Z
```diff
@@ -594,6 +613,34 @@ func (h *cqAdmissionCheckHandler) Generic(_ context.Context, e event.GenericEven
 	}
 }
 
+type cqTopologyHandler struct {
+	cache *cache.Cache
+}
+
+func (h *cqTopologyHandler) Create(context.Context, event.CreateEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Update(context.Context, event.UpdateEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Delete(context.Context, event.DeleteEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Generic(_ context.Context, e event.GenericEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	topology, isTopology := e.Object.(*kueuealpha.Topology)
+	if !isTopology {
+		return
+	}
+	cqs := h.cache.ClusterQueuesUsingTopology(kueue.TopologyReference(topology.Name))
+	for _, cq := range cqs {
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name: cq,
+			}}
+		q.AddAfter(req, time.Second)
```

  Right, topology creations / deletes are rare. OTOH, 1s delay will not be a problem in most cases. I'm indifferent about the delay. I can delete it tentatively.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T15:52:30Z

- **pkg/controller/core/core.go:86** by [@mimowo](https://github.com/mimowo) — 2024-12-11T15:52:30Z
```diff
@@ -83,6 +83,7 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	).SetupWithManager(mgr, cfg); err != nil {
 		return "Workload", err
 	}
+	qManager.AddTopologyUpdateWatcher(cqRec)
```

  We don't need to the qManager lock, because the watchers are only added during startup.
  
  I tried initially in reconciler, but this is problematic, because it would mean registering TAS RF reconciler in the CQ core reconciler, but this breaks the dependency - because currently TAS controllers are in a dedicated package. TAS reconcilers depend on core reconcilers, but not the other way round. So, placing the watchers in qManager breaks the dependency issue. I think we could use this pattern for other watchers. I think it works conceptually.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-11T15:52:58Z

- **pkg/cache/clusterqueue_test.go:1073** by [@PBundyra](https://github.com/PBundyra) — 2024-12-11T15:52:58Z
```diff
@@ -1067,20 +1069,41 @@ func TestClusterQueueReadinessWithTAS(t *testing.T) {
 			wantReason:  kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling,
 			wantMessage: "Can't admit new workloads: TAS is not supported with ProvisioningRequest admission check.",
 		},
+		{
+			name:         "TAS do not support ProvisioningRequest AdmissionCheck",
```

  Can we update the name of this scenario?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T16:03:14Z

- **pkg/controller/core/clusterqueue_controller.go:640** by [@mimowo](https://github.com/mimowo) — 2024-12-11T16:03:14Z
```diff
@@ -594,6 +613,34 @@ func (h *cqAdmissionCheckHandler) Generic(_ context.Context, e event.GenericEven
 	}
 }
 
+type cqTopologyHandler struct {
+	cache *cache.Cache
+}
+
+func (h *cqTopologyHandler) Create(context.Context, event.CreateEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Update(context.Context, event.UpdateEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Delete(context.Context, event.DeleteEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *cqTopologyHandler) Generic(_ context.Context, e event.GenericEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	topology, isTopology := e.Object.(*kueuealpha.Topology)
+	if !isTopology {
+		return
+	}
+	cqs := h.cache.ClusterQueuesUsingTopology(kueue.TopologyReference(topology.Name))
+	for _, cq := range cqs {
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name: cq,
+			}}
+		q.AddAfter(req, time.Second)
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T16:30:51Z

- **pkg/cache/clusterqueue_test.go:1073** by [@mimowo](https://github.com/mimowo) — 2024-12-11T16:30:50Z
```diff
@@ -1067,20 +1069,41 @@ func TestClusterQueueReadinessWithTAS(t *testing.T) {
 			wantReason:  kueue.ClusterQueueActiveReasonNotSupportedWithTopologyAwareScheduling,
 			wantMessage: "Can't admit new workloads: TAS is not supported with ProvisioningRequest admission check.",
 		},
+		{
+			name:         "TAS do not support ProvisioningRequest AdmissionCheck",
```

  sure, fixed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-11T16:46:00Z

- **pkg/controller/core/core.go:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-11T16:46:00Z
```diff
@@ -83,6 +83,7 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	).SetupWithManager(mgr, cfg); err != nil {
 		return "Workload", err
 	}
+	qManager.AddTopologyUpdateWatcher(cqRec)
```

  Uhm, I see. In that case, I think that we should restructure the package for the TAS so that we can distinguish the responsibility between each component.
  
  But based on the release schedule, I'm ok with this implementation for now.
