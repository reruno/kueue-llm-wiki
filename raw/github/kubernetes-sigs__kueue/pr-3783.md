# PR #3783: Documentation for LocalQueueDefaulting

**Summary**: Documentation for LocalQueueDefaulting

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3783

**Last updated**: 2025-01-02T02:27:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Branch**: `default-queue` → `main`
- **Created**: 2024-12-10T11:59:28Z
- **Updated**: 2025-01-02T02:27:59Z
- **Merged**: 2024-12-13T07:22:28Z
- **Closed**: 2024-12-13T07:22:28Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+27 / -18**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
Fixes #2936

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-10T11:59:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9c13153bc55cc083a62942c8534deeedada3b1db |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/675b46c340bae90008f601ab |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3783--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3783--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTM3ODMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.cmcCe5cCgVx-fu3LRfZ3NSAYoplNk7ajT7pExfQNjZg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3783--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-10T12:36:50Z

/assign @PBundyra 
For the first pass

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-12-10T14:04:26Z

Can we add a sentence or two that as a result batch users can create easily a job without adding the label?

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-12-12T15:29:03Z

/hold

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-12-12T15:32:27Z

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-13T07:21:39Z

/lgtm
/approve
Merging to make sure the feature is documented for release, but please follow up with https://github.com/kubernetes-sigs/kueue/pull/3783#discussion_r1883460153

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-13T07:21:45Z

LGTM label has been added.  <details>Git tree hash: e99a551c074ec2e816c3e5a58f90705f7b7d7068</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-13T07:21:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3783#issuecomment-2540751130" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3783#" title="Author self-approved">yaroslava-serdiuk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-10T13:55:52Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:18** by [@PBundyra](https://github.com/PBundyra) — 2024-12-10T13:55:51Z
```diff
@@ -13,7 +13,16 @@ intended for use by _batch users_ will be managed by Kueue even if they lack a `
 
 Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
 
-## Configuration
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature gate (disabled by default) that allows to use LocalQueue with `default` name as a default LocalQueue for the workloads in the same namespace without queue `kueue.x-k8s.io/queue-name` label. To use a feature:
```

  ```suggestion
  LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue for workloads in the same namespace that do not have the `kueue.x-k8s.io/queue-name` label. The feature is gated by a feature the `LocalQueueDefaulting` feature gate, and is disabled by default. To use this feature:
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-10T13:57:31Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:20** by [@PBundyra](https://github.com/PBundyra) — 2024-12-10T13:57:31Z
```diff
@@ -13,7 +13,16 @@ intended for use by _batch users_ will be managed by Kueue even if they lack a `
 
 Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
 
-## Configuration
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature gate (disabled by default) that allows to use LocalQueue with `default` name as a default LocalQueue for the workloads in the same namespace without queue `kueue.x-k8s.io/queue-name` label. To use a feature:
+
+- Enable LocalQueueDefaulting feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
```

  ```suggestion
  - Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-10T13:57:50Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:21** by [@PBundyra](https://github.com/PBundyra) — 2024-12-10T13:57:49Z
```diff
@@ -13,7 +13,16 @@ intended for use by _batch users_ will be managed by Kueue even if they lack a `
 
 Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
 
-## Configuration
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature gate (disabled by default) that allows to use LocalQueue with `default` name as a default LocalQueue for the workloads in the same namespace without queue `kueue.x-k8s.io/queue-name` label. To use a feature:
+
+- Enable LocalQueueDefaulting feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+- create LocalQueue with the `default` name.
```

  ```suggestion
  - create a LocalQueue with the name `default`.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-10T13:58:23Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:22** by [@PBundyra](https://github.com/PBundyra) — 2024-12-10T13:58:22Z
```diff
@@ -13,7 +13,16 @@ intended for use by _batch users_ will be managed by Kueue even if they lack a `
 
 Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
 
-## Configuration
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature gate (disabled by default) that allows to use LocalQueue with `default` name as a default LocalQueue for the workloads in the same namespace without queue `kueue.x-k8s.io/queue-name` label. To use a feature:
+
+- Enable LocalQueueDefaulting feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+- create LocalQueue with the `default` name.
+- create workload in the same namespace and observe that workload is updated with `kueue.x-k8s.io/queue-name:default` label.
```

  ```suggestion
  - create a workload in the same namespace. Observe that workload is updated with the `kueue.x-k8s.io/queue-name:default` label.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-10T13:59:01Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:23** by [@PBundyra](https://github.com/PBundyra) — 2024-12-10T13:59:01Z
```diff
@@ -13,7 +13,16 @@ intended for use by _batch users_ will be managed by Kueue even if they lack a `
 
 Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
 
-## Configuration
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature gate (disabled by default) that allows to use LocalQueue with `default` name as a default LocalQueue for the workloads in the same namespace without queue `kueue.x-k8s.io/queue-name` label. To use a feature:
+
+- Enable LocalQueueDefaulting feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+- create LocalQueue with the `default` name.
+- create workload in the same namespace and observe that workload is updated with `kueue.x-k8s.io/queue-name:default` label.
+- workloads that created in different namespace or workloads that has `kueue.x-k8s.io/queue-name` label won't be modified.
```

  ```suggestion
  - workloads created in a different namespace or workloads that already have the `kueue.x-k8s.io/queue-name` label won't be modified.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-12-10T14:03:47Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:18** by [@PBundyra](https://github.com/PBundyra) — 2024-12-10T14:03:47Z
```diff
@@ -13,7 +13,16 @@ intended for use by _batch users_ will be managed by Kueue even if they lack a `
 
 Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version).
 
-## Configuration
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue for workloads in the same namespace that do not have the `kueue.x-k8s.io/queue-name` label. The feature is gated by a feature the `LocalQueueDefaulting` feature gate, and is disabled by default. To use this feature:
```

  ```suggestion
  LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue for workloads in the same namespace that do not have the `kueue.x-k8s.io/queue-name` label. The feature is gated by the `LocalQueueDefaulting` feature gate, and is disabled by default. To use this feature:
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T06:43:27Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:2** by [@mimowo](https://github.com/mimowo) — 2024-12-11T06:43:21Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Setup manageJobsWithoutQueueName"
+title: "Manage Jobs without queue name"
```

  Actually, I would suggest moving the content about defaulting LocalQueues out to a separate subpage. Sibling to this one. The reason being that the defaulting is also relevant for VAP, so there is no reason to couple it to manageJobsWithoutQueueName . The title could be "Setup LocalQueue defaulting".

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-12-11T09:44:40Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:2** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-12-11T09:44:40Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Setup manageJobsWithoutQueueName"
+title: "Manage Jobs without queue name"
```

  I think currently default local queue feature related to how manage jobs without queue name and it would make sense to have all settings in one place, so users will understand how those feature work together and what the difference between them. 
  
  However what is VAP? Maybe it worth to add the link to default LocalQueue section in other places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T09:54:27Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:2** by [@mimowo](https://github.com/mimowo) — 2024-12-11T09:54:27Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Setup manageJobsWithoutQueueName"
+title: "Manage Jobs without queue name"
```

  https://github.com/kubernetes-sigs/kueue/blob/main/site/content/en/docs/tasks/manage/enforce_job_management/setup_job_admission_policy.md

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T09:56:41Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:2** by [@mimowo](https://github.com/mimowo) — 2024-12-11T09:56:41Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Setup manageJobsWithoutQueueName"
+title: "Manage Jobs without queue name"
```

  > I think currently default local queue feature related to how manage jobs without queue name and it would 
  > make sense to have all settings in one place, so users will understand how those feature work together and 
  > what the difference between them.
  
  I would rather link the dedicated page from both subpages for manageJobsWithoutQueueName and VAP. Since the defaulting happens in webhook it applies to both features. "have all settings in one place" - yeah in the recent update of docs on main the three related features would be under the common folder `enforce_job_management`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-11T09:58:01Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:2** by [@mimowo](https://github.com/mimowo) — 2024-12-11T09:58:01Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Setup manageJobsWithoutQueueName"
+title: "Manage Jobs without queue name"
```

  For better visibility of the feature we may also link it from https://kueue.sigs.k8s.io/docs/reference/labels-and-annotations/#kueuex-k8sioqueue-name

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-12-12T14:10:56Z

- **site/content/en/docs/tasks/manage/enforce_job_management/manage_jobs_without_queue_name.md:2** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-12-12T14:10:56Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Setup manageJobsWithoutQueueName"
+title: "Manage Jobs without queue name"
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-12T15:21:31Z

- **site/content/en/docs/reference/labels-and-annotations.md:186** by [@mimowo](https://github.com/mimowo) — 2024-12-12T15:08:16Z
```diff
@@ -197,6 +182,8 @@ Used on: Kueue-managed Jobs.
 
 The label key in the workload holds the queue name.
 
+Note: Read more about [Setup LocalQueueDefauling](/docs/tasks/manage/enforce_job_management/setup_default_local_queue/)
+in order to automatically fullfill `kueue.x-k8s.io/queue-name` label.
```

  ```suggestion
  in order to automatically set the `kueue.x-k8s.io/queue-name` label.
  ```
  nit

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:10** by [@mimowo](https://github.com/mimowo) — 2024-12-12T15:14:49Z
```diff
@@ -0,0 +1,19 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to specific namespace
+intended for use by _batch users_ will be managed by Kueue even if they lack a `kueue.x-k8s.io/queue-name` label.
```

  ```suggestion
  This page describes how to setup default LocalQueue to ensure that all Workloads submitted to a specific namespace, intended for Kueue users, are managed by Kueue, even if the `kueue.x-k8s.io/queue-name` label is not specified explicitly. 
  ```

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:19** by [@mimowo](https://github.com/mimowo) — 2024-12-12T15:18:45Z
```diff
@@ -0,0 +1,19 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to specific namespace
+intended for use by _batch users_ will be managed by Kueue even if they lack a `kueue.x-k8s.io/queue-name` label.
+
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue for workloads in the same namespace that do not have the `kueue.x-k8s.io/queue-name` label. The feature is gated by the `LocalQueueDefaulting` feature gate, and is disabled by default. To use this feature:
+
+- Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
+- create a LocalQueue with the name `default`.
+- create a workload in the same namespace. Observe that workload is updated with the `kueue.x-k8s.io/queue-name:default` label.
+- workloads created in a different namespace or workloads that already have the `kueue.x-k8s.io/queue-name` label won't be modified.
```

  ```suggestion
  - Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
  - create a LocalQueue with the name `default` in a namespace. 
  
  That's all! Now, to test the feature, create a Job in the same namespace. Observe that the Job is updated with the `kueue.x-k8s.io/queue-name: default` label.
  
  Note that workloads created in a different namespace or workloads that already have the `kueue.x-k8s.io/queue-name` label won't be modified.
  ```

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:19** by [@mimowo](https://github.com/mimowo) — 2024-12-12T15:20:55Z
```diff
@@ -0,0 +1,19 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to specific namespace
+intended for use by _batch users_ will be managed by Kueue even if they lack a `kueue.x-k8s.io/queue-name` label.
+
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue for workloads in the same namespace that do not have the `kueue.x-k8s.io/queue-name` label. The feature is gated by the `LocalQueueDefaulting` feature gate, and is disabled by default. To use this feature:
+
+- Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
+- create a LocalQueue with the name `default`.
+- create a workload in the same namespace. Observe that workload is updated with the `kueue.x-k8s.io/queue-name:default` label.
+- workloads created in a different namespace or workloads that already have the `kueue.x-k8s.io/queue-name` label won't be modified.
```

  nit proposal. As the 2 last points aren't really the setup, but testing and a note

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-12-12T15:25:52Z

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:19** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-12-12T15:25:52Z
```diff
@@ -0,0 +1,19 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to specific namespace
+intended for use by _batch users_ will be managed by Kueue even if they lack a `kueue.x-k8s.io/queue-name` label.
+
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue for workloads in the same namespace that do not have the `kueue.x-k8s.io/queue-name` label. The feature is gated by the `LocalQueueDefaulting` feature gate, and is disabled by default. To use this feature:
+
+- Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
+- create a LocalQueue with the name `default`.
+- create a workload in the same namespace. Observe that workload is updated with the `kueue.x-k8s.io/queue-name:default` label.
+- workloads created in a different namespace or workloads that already have the `kueue.x-k8s.io/queue-name` label won't be modified.
```

  Thank you!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-12T17:43:42Z

LGTM, two remaining nits

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:16** by [@mimowo](https://github.com/mimowo) — 2024-12-12T17:39:27Z
```diff
@@ -0,0 +1,21 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to a specific namespace, intended for Kueue users, are managed by Kueue, even if the `kueue.x-k8s.io/queue-name` label is not specified explicitly. 
+
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue for workloads in the same namespace that do not have the `kueue.x-k8s.io/queue-name` label. The feature is gated by the `LocalQueueDefaulting` feature gate, and is disabled by default. To use this feature:
+
+- Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
+- Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
```

  ```suggestion
  - Enable the LocalQueueDefaulting feature gate. Refer to the [feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) guide for details.
  ```
  it is duplicated

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:9** by [@mimowo](https://github.com/mimowo) — 2024-12-12T17:43:29Z
```diff
@@ -0,0 +1,21 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to a specific namespace, intended for Kueue users, are managed by Kueue, even if the `kueue.x-k8s.io/queue-name` label is not specified explicitly. 
```

  ```suggestion
  This page describes how to setup default LocalQueue to ensure that all Workloads submitted to a specific namespace are managed by Kueue, even if the `kueue.x-k8s.io/queue-name` label is not specified explicitly. 
  ```
  maybe it is not that relevent that it is "indended for Kueue users". And dropping this part makes it more readable imo.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T07:20:33Z

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:14** by [@mimowo](https://github.com/mimowo) — 2024-12-13T07:20:33Z
```diff
@@ -0,0 +1,24 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to a specific namespace are managed by Kueue,
+even if the `kueue.x-k8s.io/queue-name` label is not specified explicitly.
+
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue
```

  I would prefer to add the feature banner as here: https://github.com/kubernetes-sigs/kueue/blob/e544dc8a447abfafb471886f9390af0a303b24d5/site/content/en/docs/concepts/topology_aware_scheduling.md?plain=1#L9. Can be a follow up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-01-02T02:27:59Z

- **site/content/en/docs/tasks/manage/enforce_job_management/setup_default_local_queue.md:14** by [@tenzen-y](https://github.com/tenzen-y) — 2025-01-02T02:26:52Z
```diff
@@ -0,0 +1,24 @@
+---
+title: "Setup default LocalQueue"
+date: 2024-12-12
+weight: 10
+description: >
+  Setup default LocalQueue to fullfil a queue label on jobs that submited without queue label.
+---
+
+This page describes how to setup default LocalQueue to ensure that all Workloads submitted to a specific namespace are managed by Kueue,
+even if the `kueue.x-k8s.io/queue-name` label is not specified explicitly.
+
+## Setup default LocalQueue
+
+LocalQueueDefaulting is an Alpha feature that allows the use of a LocalQueue with name `default` as the default LocalQueue
```

  This was addressed in the https://github.com/kubernetes-sigs/kueue/pull/3867
