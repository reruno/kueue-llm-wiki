# PR #382: Add Admitted Workloads Field to LocalQueueStatus

**Summary**: Add Admitted Workloads Field to LocalQueueStatus

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/382

**Last updated**: 2022-09-09T17:31:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `issue-259-clean-history` → `main`
- **Created**: 2022-09-07T22:03:29Z
- **Updated**: 2022-09-09T17:31:24Z
- **Merged**: 2022-09-09T17:31:24Z
- **Closed**: 2022-09-09T17:31:24Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: _none_
- **Changed files**: 6   **+66 / -77**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind api-change

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add AdmittedWorkloads to LocalQueueStatus API and update it in the LocalQueueController.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #259 

#### Special notes for your reviewer:

Sorry for the silliness with opening up multiple repos.  I have my laptop setup with CLA and github commits but I committed from another instance.  I didn't realize that if 1 commit doesn't have my email, it will fail the check.

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-09-07T22:03:37Z

Hi @kannon92. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T18:27:24Z

/assign

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T14:41:19Z

Please fix the latest comment and squash

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-09-09T15:16:52Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-09-09T17:30:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/382#pullrequestreview-1102679941" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/382#" title="Author self-approved">kannon92</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-08T19:45:17Z

- **apis/kueue/v1alpha2/localqueue_types.go:35** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:26:15Z
```diff
@@ -35,6 +35,11 @@ type LocalQueueStatus struct {
 	// localQueue not yet admitted to a ClusterQueue.
```

  not sure why it says admitted.
  
  It should say:
  
  ```
  pendingWorkloads is the number of Workloads in the LocalQueue not yet admitted to a ClusterQueue
  ```

- **apis/kueue/v1alpha2/localqueue_types.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:28:02Z
```diff
@@ -35,6 +35,11 @@ type LocalQueueStatus struct {
 	// localQueue not yet admitted to a ClusterQueue.
 	// +optional
 	PendingWorkloads int32 `json:"pendingWorkloads"`
+
+	// AdmittedWorkloads is the number of workloads currently admitted to this
+	// localQueue not yet admitted to a ClusterQueue.
```

  ```suggestion
  	// admittedWorkloads is the number of workloads in this LocalQueue
  	// admitted to a ClusterQueue and that haven't finished yet.
  ```

- **pkg/cache/cache.go:158** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:29:53Z
```diff
@@ -148,6 +148,20 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) (int32, error) {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0, nil
+	}
+	localKey := queueKey(localQueue)
```

  ```suggestion
  	qKey := queueKey(localQueue)
  ```
  
  or `localQueueKey` if you want to be more explicit.

- **pkg/cache/cache.go:151** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:30:30Z
```diff
@@ -148,6 +148,20 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) (int32, error) {
```

  it looks like we never return error, so just return the value

- **pkg/cache/cache.go:163** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:31:08Z
```diff
@@ -148,6 +148,20 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) (int32, error) {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0, nil
+	}
+	localKey := queueKey(localQueue)
+	localWorkloadsAdmitted, localExists := cq.admittedWorkloadsPerQueue[localKey]
+	if !localExists {
+		return 0, nil
+	}
+	return int32(localWorkloadsAdmitted), nil
```

  ```suggestion
  	return int32(cq.admittedWorkloadsPerQueue[qKey])
  ```

- **pkg/cache/cache.go:160** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:36:51Z
```diff
@@ -148,6 +148,20 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) (int32, error) {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0, nil
+	}
+	localKey := queueKey(localQueue)
+	localWorkloadsAdmitted, localExists := cq.admittedWorkloadsPerQueue[localKey]
+	if !localExists {
+		return 0, nil
+	}
+	return int32(localWorkloadsAdmitted), nil
+}
```

  add empty line

- **pkg/cache/cache_test.go:1533** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:41:10Z
```diff
@@ -1496,6 +1529,20 @@ func TestCacheQueueOperations(t *testing.T) {
 			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
 				t.Errorf("Wrong active workloads counters for queues (-want,+got):\n%s", diff)
 			}
+			admittedCountsCounts := make(map[string]int)
+			for _, cq := range queues {
```

  ```suggestion
  			for _, lq := range queues {
  ```
  
  for local queues. `cq` is short for ClusterQueue

- **pkg/cache/cache_test.go:1307** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:42:04Z
```diff
@@ -1303,8 +1303,9 @@ func TestCacheQueueOperations(t *testing.T) {
 		return nil
 	}
 	cases := map[string]struct {
-		ops             []func(context.Context, client.Client, *Cache) error
-		wantQueueCounts map[string]map[string]int
+		ops                  []func(context.Context, client.Client, *Cache) error
+		wantQueueCounts      map[string]map[string]int
```

  Remove this map, and rename `queueCountsFlattened` to `wantQueueCounts`.

- **pkg/cache/cache_test.go:1529** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:42:50Z
```diff
@@ -1496,6 +1529,20 @@ func TestCacheQueueOperations(t *testing.T) {
 			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
```

  get rid of lines 1525 to 1531.

- **pkg/controller/core/localqueue_controller.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:45:14Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  Looks good, but this needs to be tested.
  
  You can add checks to the existing integration tests in `test/integration`

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-08T19:54:46Z

- **pkg/controller/core/localqueue_controller.go:87** by [@kannon92](https://github.com/kannon92) — 2022-09-08T19:54:46Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  Thank you!  I was having some trouble running those on my setup.  
  
  I had some failures in unrelated code and some stuff to do with certs.  I'll look more into it and add some tests in test/integration also.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-08T19:56:48Z

- **pkg/controller/core/localqueue_controller.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T19:56:48Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  Did you try `make test-integration`?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-08T20:21:32Z

- **pkg/cache/cache_test.go:1307** by [@kannon92](https://github.com/kannon92) — 2022-09-08T20:21:32Z
```diff
@@ -1303,8 +1303,9 @@ func TestCacheQueueOperations(t *testing.T) {
 		return nil
 	}
 	cases := map[string]struct {
-		ops             []func(context.Context, client.Client, *Cache) error
-		wantQueueCounts map[string]map[string]int
+		ops                  []func(context.Context, client.Client, *Cache) error
+		wantQueueCounts      map[string]map[string]int
```

  I think you just want me to remove queueCountsFlattened.  We already had wantQueueCounts in the cache_test.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-08T20:23:33Z

- **pkg/cache/cache_test.go:1529** by [@kannon92](https://github.com/kannon92) — 2022-09-08T20:23:33Z
```diff
@@ -1496,6 +1529,20 @@ func TestCacheQueueOperations(t *testing.T) {
 			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
```

  Can you be more explicit?  I'm not entirely sure what those line numbers correspond to anymore
  
  So what I am planning on doing:
  
  I'd like to keep the call to cache.AdmittedWorkloadsLocalQueue because that is a public function of cache.  But I will remove the flattenedQueueCounts and just test the values of the map in wantQueueCounts with the values of AdmittedWorkloadsLocalQueue.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-08T20:24:23Z

- **pkg/controller/core/localqueue_controller.go:87** by [@kannon92](https://github.com/kannon92) — 2022-09-08T20:24:23Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  Yea, I'll try it again.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-08T20:50:56Z

- **pkg/controller/core/localqueue_controller.go:87** by [@kannon92](https://github.com/kannon92) — 2022-09-08T20:50:56Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  Alright.  There is a regression caused by this code so I'll look into it, fix it and add some tests here.    
  
  I am unable to run `make test-integration` on my Mac M1.  it says architecture not supported and then fails every test.  
  
  I get failures when I get a test failure but it seems to be caused by the controller test detecting an error.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-08T20:55:44Z

- **pkg/controller/core/localqueue_controller.go:87** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-08T20:55:43Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  @kannon92 Hi.
  We will fix `make test-integration` for M1 Mac in #362. If you want to run `test-integration` on your local now, please check [this comment](https://github.com/kubernetes-sigs/kueue/pull/362#discussion_r957756617).

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-08T21:22:20Z

- **pkg/controller/core/localqueue_controller.go:87** by [@kannon92](https://github.com/kannon92) — 2022-09-08T21:22:20Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  @tenzen-y Thank you for that suggestion!  Applied that fix locally and I was able to deduce where my test failure was.  Pushed up a fix for that.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-08T21:36:42Z

- **apis/kueue/v1alpha2/localqueue_types.go:35** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T21:30:00Z
```diff
@@ -35,6 +35,11 @@ type LocalQueueStatus struct {
 	// localQueue not yet admitted to a ClusterQueue.
```

  please fix the comment for `pendingWorkloads`

- **pkg/cache/cache.go:163** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T21:31:45Z
```diff
@@ -148,6 +148,21 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) int32 {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0
+	}
+	qKey := queueKey(localQueue)
+	localWorkloadsAdmitted, localExists := cq.admittedWorkloadsPerQueue[qKey]
+	if !localExists {
+		return 0
+	}
+	return int32(localWorkloadsAdmitted)
```

  ```suggestion
  	return int32(cq.admittedWorkloadsPerQueue[qKey])
  ```

- **pkg/cache/cache_test.go:1529** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-08T21:34:03Z
```diff
@@ -1496,6 +1529,20 @@ func TestCacheQueueOperations(t *testing.T) {
 			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
```

  Now that we have a public method `AdmittedWorkloadsLocalQueue`, there is no need to check the values of the internal map `cq.admittedWorkloadsPerQueue`, so we can remove these checks.
  
  And you can flatten `wantQueueCounts` as `map[string]int32`

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-08T21:54:15Z

- **apis/kueue/v1alpha2/localqueue_types.go:35** by [@kannon92](https://github.com/kannon92) — 2022-09-08T21:54:15Z
```diff
@@ -35,6 +35,11 @@ type LocalQueueStatus struct {
 	// localQueue not yet admitted to a ClusterQueue.
```

  Sorry about that.  Missed this earlier!

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-09T13:11:21Z

- **apis/kueue/v1alpha2/localqueue_types.go:35** by [@kannon92](https://github.com/kannon92) — 2022-09-09T13:11:21Z
```diff
@@ -35,6 +35,11 @@ type LocalQueueStatus struct {
 	// localQueue not yet admitted to a ClusterQueue.
```

  Updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-09T13:11:52Z

- **pkg/cache/cache_test.go:1529** by [@kannon92](https://github.com/kannon92) — 2022-09-09T13:11:51Z
```diff
@@ -1496,6 +1529,20 @@ func TestCacheQueueOperations(t *testing.T) {
 			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
```

  Alright.  I added what you suggested.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-09T13:12:48Z

- **pkg/controller/core/localqueue_controller.go:87** by [@kannon92](https://github.com/kannon92) — 2022-09-09T13:12:48Z
```diff
@@ -84,6 +84,13 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	queueObj.Status.PendingWorkloads = pending
+	admitted, errAdmitted := r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
```

  It looks like existing tests in the controller do verify status so I just added a new field for AdmittedWorkloads in the test.  I think this is fine.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-09T13:18:41Z

- **pkg/cache/cache_test.go:1316** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T13:17:14Z
```diff
@@ -1304,55 +1304,44 @@ func TestCacheQueueOperations(t *testing.T) {
 	}
 	cases := map[string]struct {
 		ops             []func(context.Context, client.Client, *Cache) error
-		wantQueueCounts map[string]map[string]int
+		wantQueueCounts map[string]int32
 	}{
 		"insert cqs, queues, workloads": {
 			ops: []func(ctx context.Context, cl client.Client, cache *Cache) error{
 				insertAllClusterQueues,
 				insertAllQueues,
 				insertAllWorkloads,
 			},
-			wantQueueCounts: map[string]map[string]int{
-				"foo": {
-					"ns1/alpha": 1,
-					"ns2/beta":  2,
-				},
-				"bar": {
-					"ns1/gamma": 1,
-				},
+			wantQueueCounts: map[string]int32{
+				"foo/ns1/alpha": 1,
```

  I think it would have been fine to just have "ns1/alpha"

- **pkg/cache/cache_test.go:1461** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T13:18:23Z
```diff
@@ -1489,12 +1456,12 @@ func TestCacheQueueOperations(t *testing.T) {
 					t.Fatalf("Running op %d: %v", i, err)
 				}
 			}
-			qCounts := make(map[string]map[string]int)
-			for _, cq := range cache.clusterQueues {
-				qCounts[cq.Name] = cq.admittedWorkloadsPerQueue
-			}
-			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
-				t.Errorf("Wrong active workloads counters for queues (-want,+got):\n%s", diff)
+			for _, lq := range queues {
+				queueAdmitted := cache.AdmittedWorkloadsLocalQueue(lq)
+				key := fmt.Sprintf("%s/%s/%s", lq.Spec.ClusterQueue, lq.Namespace, lq.Name)
```

  I'm fine not checking `lg.Spec.ClusterQueue`. `AdmittedWorkloadsLocalQueue` should handle that already.
  
  There are separate tests for ClusterQueue operations, so we should already have all the coverage.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-09T13:57:20Z

- **pkg/cache/cache_test.go:1316** by [@kannon92](https://github.com/kannon92) — 2022-09-09T13:57:20Z
```diff
@@ -1304,55 +1304,44 @@ func TestCacheQueueOperations(t *testing.T) {
 	}
 	cases := map[string]struct {
 		ops             []func(context.Context, client.Client, *Cache) error
-		wantQueueCounts map[string]map[string]int
+		wantQueueCounts map[string]int32
 	}{
 		"insert cqs, queues, workloads": {
 			ops: []func(ctx context.Context, cl client.Client, cache *Cache) error{
 				insertAllClusterQueues,
 				insertAllQueues,
 				insertAllWorkloads,
 			},
-			wantQueueCounts: map[string]map[string]int{
-				"foo": {
-					"ns1/alpha": 1,
-					"ns2/beta":  2,
-				},
-				"bar": {
-					"ns1/gamma": 1,
-				},
+			wantQueueCounts: map[string]int32{
+				"foo/ns1/alpha": 1,
```

  You are correct.  Added.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2022-09-09T13:57:33Z

- **pkg/cache/cache_test.go:1461** by [@kannon92](https://github.com/kannon92) — 2022-09-09T13:57:33Z
```diff
@@ -1489,12 +1456,12 @@ func TestCacheQueueOperations(t *testing.T) {
 					t.Fatalf("Running op %d: %v", i, err)
 				}
 			}
-			qCounts := make(map[string]map[string]int)
-			for _, cq := range cache.clusterQueues {
-				qCounts[cq.Name] = cq.admittedWorkloadsPerQueue
-			}
-			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
-				t.Errorf("Wrong active workloads counters for queues (-want,+got):\n%s", diff)
+			for _, lq := range queues {
+				queueAdmitted := cache.AdmittedWorkloadsLocalQueue(lq)
+				key := fmt.Sprintf("%s/%s/%s", lq.Spec.ClusterQueue, lq.Namespace, lq.Name)
```

  Good point.  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-09T14:41:00Z

- **pkg/cache/cache_test.go:1464** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T14:41:00Z
```diff
@@ -1489,12 +1456,12 @@ func TestCacheQueueOperations(t *testing.T) {
 					t.Fatalf("Running op %d: %v", i, err)
 				}
 			}
-			qCounts := make(map[string]map[string]int)
-			for _, cq := range cache.clusterQueues {
-				qCounts[cq.Name] = cq.admittedWorkloadsPerQueue
-			}
-			if diff := cmp.Diff(tc.wantQueueCounts, qCounts); diff != "" {
-				t.Errorf("Wrong active workloads counters for queues (-want,+got):\n%s", diff)
+			for _, lq := range queues {
+				queueAdmitted := cache.AdmittedWorkloadsLocalQueue(lq)
+				key := fmt.Sprintf("%s/%s", lq.Namespace, lq.Name)
+				if diff := cmp.Diff(tc.wantQueueCounts[key], queueAdmitted); diff != "" {
+					t.Errorf("Wrong active workload counters for queues from AdmittedWorkloadsLocalQueue:\n%s", diff)
+				}
```

  You can either do a single comparison after the loop ends, or you can add the key somewhere in the Error message. Otherwise we can't tell which queue counters didn't match

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-09T15:16:17Z

- **pkg/cache/cache.go:151** by [@kerthcet](https://github.com/kerthcet) — 2022-09-09T15:11:39Z
```diff
@@ -148,6 +148,17 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) int32 {
```

  AdmittedWorkloadsInLocalQueue?

- **pkg/cache/cache.go:156** by [@kerthcet](https://github.com/kerthcet) — 2022-09-09T15:13:17Z
```diff
@@ -148,6 +148,17 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) int32 {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0
```

  Maybe we should also return error here.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-09T15:23:53Z

- **pkg/cache/cache.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T15:23:53Z
```diff
@@ -148,6 +148,17 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) int32 {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0
```

  Actually, the ClusterQueue might be created after the LocalQueue.
  
  During that time, there can't be any admitted workloads. So 0 is the right value.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-09-09T15:38:17Z

- **pkg/cache/cache.go:156** by [@kerthcet](https://github.com/kerthcet) — 2022-09-09T15:38:04Z
```diff
@@ -148,6 +148,17 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) int32 {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0
```

  Ah yes, that's the case. Another approach is 
  ```
  queueObj.Status.AdmittedWorkloads, _ = r.cache.AdmittedWorkloadsLocalQueue(&queueObj)
  ```
  
  But remaining the same also makes sense to me.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-09T15:42:23Z

- **pkg/cache/cache.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-09T15:42:23Z
```diff
@@ -148,6 +148,17 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) AdmittedWorkloadsLocalQueue(localQueue *kueue.LocalQueue) int32 {
+	c.Lock()
+	defer c.Unlock()
+	cq, ok := c.clusterQueues[string(localQueue.Spec.ClusterQueue)]
+	if !ok {
+		return 0
```

  if there are no cases of error, I would not add error to the return values

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-09-09T17:30:12Z

/lgtm
/approve
