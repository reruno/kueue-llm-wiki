# PR #3835: Fix memory leak due to workload entries left in MultiKueue cache

**Summary**: Fix memory leak due to workload entries left in MultiKueue cache

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3835

**Last updated**: 2025-01-21T10:53:52Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-memory-leak` → `main`
- **Created**: 2024-12-13T13:55:00Z
- **Updated**: 2025-01-21T10:53:52Z
- **Merged**: 2024-12-13T15:54:26Z
- **Closed**: 2024-12-13T15:54:26Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `do-not-merge/hold`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+53 / -1**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3818 

#### Special notes for your reviewer:

I have unit tests added and they fail without the change:
```
--- FAIL: TestWlReconcile (0.22s)
    --- FAIL: TestWlReconcile/deleted_MultiKueue_workload_is_deleted_from_cache_-_the_worker_will_be_deleted_by_GC (0.01s)
        /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/pkg/controller/admissionchecks/multikueue/workload_test.go:1166: unexpected deletedWlCache len 1 expecting 0
    --- FAIL: TestWlReconcile/deleted_regular_workload_is_removed_from_the_cache (0.01s)
        /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/pkg/controller/admissionchecks/multikueue/workload_test.go:1166: unexpected deletedWlCache len 1 expecting 0
```

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix memory leak due to workload entries left in MultiKueue cache. The leak affects the 0.9.0 and 0.9.1 
releases which enable MultiKueue by default, even if MultiKueue is not explicitly used on the cluster.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-13T13:55:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3835#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-13T13:55:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | abb75a6b703cab3c86ccb5640090796cae9ec78e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/675c55012322110008158443 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T14:15:35Z

I think that this is still work on unit testings.
/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-13T15:36:45Z

/hold cancel
The tests are added (updated the PR description to show how they fail when the line deleting the entry from cache is commented out). 

I also updated the comments with TODO follow ups, as this is still not ideal, but the changes are far from trivial, and I could break something. The most important thing is that the workloads are eventually removed from memory.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-12-13T15:42:47Z

/lgtm
Thanks @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-13T15:42:55Z

LGTM label has been added.  <details>Git tree hash: 48d87bfef68af84969ddbeddfcd109618cc1bcd1</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-13T16:00:43Z

/cherry-pick release-0.9

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-12-13T16:01:21Z

@mimowo: new pull request created: #3842

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3835#issuecomment-2541762413):

>/cherry-pick release-0.9


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T15:55:30Z

Basically, lgtm
/hold

- **pkg/controller/admissionchecks/multikueue/workload_test.go:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T15:54:36Z
```diff
@@ -95,6 +95,41 @@ func TestWlReconcile(t *testing.T) {
 		wantWorker2Workloads []kueue.Workload
 		wantWorker2Jobs      []batchv1.Job
 	}{
+		"deleted regular workload is removed from the cache": {
+			reconcileFor: "wl1",
+			managersJobs: []batchv1.Job{*baseJobBuilder.Clone().Obj()},
+			managersDeletedWorkloads: []*kueue.Workload{
+				baseWorkloadBuilder.Clone().
+					DeletionTimestamp(now).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{*baseJobBuilder.Clone().Obj()},
+		},
+		"deleted MultiKueue workload is deleted from cache - the worker will be deleted by GC": {
+			reconcileFor: "wl1",
+			managersJobs: []batchv1.Job{*baseJobManagedByKueueBuilder.Clone().Obj()},
+			managersDeletedWorkloads: []*kueue.Workload{
+				baseWorkloadBuilder.Clone().
+					DeletionTimestamp(now).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStateRejected}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					Obj(),
+			},
+			worker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{*baseJobManagedByKueueBuilder.Clone().Obj()},
+			wantWorker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+		},
```

  Could we add a case where the checkState has Retry or Pending?
  We want to verify if the workloads are not removed from the cache.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T16:00:22Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:132** by [@mimowo](https://github.com/mimowo) — 2024-12-13T16:00:22Z
```diff
@@ -95,6 +95,41 @@ func TestWlReconcile(t *testing.T) {
 		wantWorker2Workloads []kueue.Workload
 		wantWorker2Jobs      []batchv1.Job
 	}{
+		"deleted regular workload is removed from the cache": {
+			reconcileFor: "wl1",
+			managersJobs: []batchv1.Job{*baseJobBuilder.Clone().Obj()},
+			managersDeletedWorkloads: []*kueue.Workload{
+				baseWorkloadBuilder.Clone().
+					DeletionTimestamp(now).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{*baseJobBuilder.Clone().Obj()},
+		},
+		"deleted MultiKueue workload is deleted from cache - the worker will be deleted by GC": {
+			reconcileFor: "wl1",
+			managersJobs: []batchv1.Job{*baseJobManagedByKueueBuilder.Clone().Obj()},
+			managersDeletedWorkloads: []*kueue.Workload{
+				baseWorkloadBuilder.Clone().
+					DeletionTimestamp(now).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStateRejected}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					Obj(),
+			},
+			worker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{*baseJobManagedByKueueBuilder.Clone().Obj()},
+			wantWorker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+		},
```

  We can in a follow up cleanup PR (I will try on Monday). This is not directly related to the PR. Actually, such workloads will be deleted from cache, but after first removing the workloads on the worker clusters.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T16:05:19Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:05:19Z
```diff
@@ -95,6 +95,41 @@ func TestWlReconcile(t *testing.T) {
 		wantWorker2Workloads []kueue.Workload
 		wantWorker2Jobs      []batchv1.Job
 	}{
+		"deleted regular workload is removed from the cache": {
+			reconcileFor: "wl1",
+			managersJobs: []batchv1.Job{*baseJobBuilder.Clone().Obj()},
+			managersDeletedWorkloads: []*kueue.Workload{
+				baseWorkloadBuilder.Clone().
+					DeletionTimestamp(now).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{*baseJobBuilder.Clone().Obj()},
+		},
+		"deleted MultiKueue workload is deleted from cache - the worker will be deleted by GC": {
+			reconcileFor: "wl1",
+			managersJobs: []batchv1.Job{*baseJobManagedByKueueBuilder.Clone().Obj()},
+			managersDeletedWorkloads: []*kueue.Workload{
+				baseWorkloadBuilder.Clone().
+					DeletionTimestamp(now).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStateRejected}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					Obj(),
+			},
+			worker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{*baseJobManagedByKueueBuilder.Clone().Obj()},
+			wantWorker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+		},
```

  Yes, this is not in a hurry.
  I'm ok with followup.
