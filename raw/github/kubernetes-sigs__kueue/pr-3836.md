# PR #3836: Distinguish errors for non-existing LocalQueue and ClusterQueue in the workload-controller

**Summary**: Distinguish errors for non-existing LocalQueue and ClusterQueue in the workload-controller

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3836

**Last updated**: 2024-12-13T17:13:16Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `cleanup-queue-manageer-logging` → `main`
- **Created**: 2024-12-13T14:11:35Z
- **Updated**: 2024-12-13T17:13:16Z
- **Merged**: 2024-12-13T17:04:26Z
- **Closed**: 2024-12-13T17:04:26Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 3   **+25 / -30**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:
In the https://github.com/kubernetes-sigs/kueue/pull/3605, we dropped the inactive LocalQueue and ClusterQueue reason.
But we still face errors in situations where those are inactive.

Additionally, we should not omit errors verifications in testing.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-13T14:11:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5832abf078dc7d40334963c32878ee670d4229a9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/675c656bc4c9370008e89e57 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T14:13:04Z

/assign @mbobrovskyi

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:21:22Z

@mbobrovskyi  I addressed all your comments.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-13T16:27:44Z

LGTM. Thank you so much!

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-13T16:28:05Z

Left lgtm for @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-12-13T16:59:40Z

/lgtm
/approve
please also cherry-pick this one as a follow up to the previous fix

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-13T16:59:48Z

LGTM label has been added.  <details>Git tree hash: 57897cd51ebd14966875355b47451b81f94a7ac7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-13T16:59:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3836#issuecomment-2541868407" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3836#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T17:12:34Z

/cherry-pick release-0.9

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-12-13T17:13:14Z

@tenzen-y: new pull request created: #3844

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3836#issuecomment-2541890856):

>/cherry-pick release-0.9
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T14:22:22Z

- **pkg/controller/core/workload_controller.go:620** by [@mimowo](https://github.com/mimowo) — 2024-12-13T14:22:22Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  Hm `ErrClusterQueueDoesNotExist` already contains the information this is about "clusterQueue":  https://github.com/kubernetes-sigs/kueue/blob/cc65f7e7701df31e77959ef664384616abd54f9e/pkg/queue/manager.go#L42-L43. So why to duplicate the information?
  
  Also, this is a bulk of duplicated code, please commonize.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T14:23:13Z

- **pkg/queue/manager_test.go:539** by [@mimowo](https://github.com/mimowo) — 2024-12-13T14:23:13Z
```diff
@@ -533,7 +532,10 @@ func TestStatus(t *testing.T) {
 		}
 	}
 	for _, wl := range workloads {
-		_ = manager.AddOrUpdateWorkload(&wl)
+		// We ignore the ErrClusterQueueDoesNotExist since we never set up ClusterQueue in this test, and the error should be occurred.
+		if err := manager.AddOrUpdateWorkload(&wl); err != nil && !errors.Is(err, ErrClusterQueueDoesNotExist) {
+			t.Fatalf("Failed to add or update workloads: %v", err)
+		}
```

  I think this change is very good and super safe, if you extract to separate PR we can merge immediately

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T14:49:30Z

- **pkg/controller/core/workload_controller.go:620** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T14:49:30Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  That makes sense. Here, key problem is dropped the "inactive" reason from logging message and mention "queue" instead of localQueue.
  
  https://github.com/kubernetes-sigs/kueue/pull/3605/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffL733
  
  So, I will remove these error-typed checkings and then add an "inactive" reason.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T14:49:47Z

- **pkg/queue/manager_test.go:539** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T14:49:46Z
```diff
@@ -533,7 +532,10 @@ func TestStatus(t *testing.T) {
 		}
 	}
 	for _, wl := range workloads {
-		_ = manager.AddOrUpdateWorkload(&wl)
+		// We ignore the ErrClusterQueueDoesNotExist since we never set up ClusterQueue in this test, and the error should be occurred.
+		if err := manager.AddOrUpdateWorkload(&wl); err != nil && !errors.Is(err, ErrClusterQueueDoesNotExist) {
+			t.Fatalf("Failed to add or update workloads: %v", err)
+		}
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T14:54:50Z

- **pkg/queue/manager_test.go:539** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T14:54:50Z
```diff
@@ -533,7 +532,10 @@ func TestStatus(t *testing.T) {
 		}
 	}
 	for _, wl := range workloads {
-		_ = manager.AddOrUpdateWorkload(&wl)
+		// We ignore the ErrClusterQueueDoesNotExist since we never set up ClusterQueue in this test, and the error should be occurred.
+		if err := manager.AddOrUpdateWorkload(&wl); err != nil && !errors.Is(err, ErrClusterQueueDoesNotExist) {
+			t.Fatalf("Failed to add or update workloads: %v", err)
+		}
```

  Opened: https://github.com/kubernetes-sigs/kueue/pull/3838

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T15:42:00Z

- **pkg/controller/core/workload_controller.go:611** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T15:42:00Z
```diff
@@ -607,9 +607,8 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			log.V(2).Info("ignored an error for now", "error", err)
```

  It would be better to use structured logging instead of wrapping values inside the message.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T15:42:47Z

- **pkg/controller/core/workload_controller.go:620** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T15:42:46Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  Done.
  Please let me know if you have more recommendations.
  Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-13T15:57:36Z

- **pkg/queue/manager.go:42** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-13T15:56:22Z
```diff
@@ -39,8 +39,8 @@ import (
 )
 
 var (
-	ErrQueueDoesNotExist         = errors.New("queue doesn't exist")
-	ErrClusterQueueDoesNotExist  = errors.New("clusterQueue doesn't exist")
+	ErrQueueDoesNotExist         = errors.New("localQueue doesn't exist or inactive")
```

  ```suggestion
  	ErrLocalQueueDoesNotExistOrInactive         = errors.New("localQueue doesn't exist or inactive")
  ```

- **pkg/queue/manager.go:43** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-13T15:56:35Z
```diff
@@ -39,8 +39,8 @@ import (
 )
 
 var (
-	ErrQueueDoesNotExist         = errors.New("queue doesn't exist")
-	ErrClusterQueueDoesNotExist  = errors.New("clusterQueue doesn't exist")
+	ErrQueueDoesNotExist         = errors.New("localQueue doesn't exist or inactive")
+	ErrClusterQueueDoesNotExist  = errors.New("clusterQueue doesn't exist or inactive")
```

  ```suggestion
  	ErrClusterQueueDoesNotExistOrInactive  = errors.New("clusterQueue doesn't exist or inactive")
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T16:14:51Z

- **pkg/queue/manager.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:14:51Z
```diff
@@ -39,8 +39,8 @@ import (
 )
 
 var (
-	ErrQueueDoesNotExist         = errors.New("queue doesn't exist")
-	ErrClusterQueueDoesNotExist  = errors.New("clusterQueue doesn't exist")
+	ErrQueueDoesNotExist         = errors.New("localQueue doesn't exist or inactive")
```

  SGTM, thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T16:21:04Z

- **pkg/queue/manager.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:21:04Z
```diff
@@ -39,8 +39,8 @@ import (
 )
 
 var (
-	ErrQueueDoesNotExist         = errors.New("queue doesn't exist")
-	ErrClusterQueueDoesNotExist  = errors.New("clusterQueue doesn't exist")
+	ErrQueueDoesNotExist         = errors.New("localQueue doesn't exist or inactive")
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T16:21:07Z

- **pkg/queue/manager.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:21:07Z
```diff
@@ -39,8 +39,8 @@ import (
 )
 
 var (
-	ErrQueueDoesNotExist         = errors.New("queue doesn't exist")
-	ErrClusterQueueDoesNotExist  = errors.New("clusterQueue doesn't exist")
+	ErrQueueDoesNotExist         = errors.New("localQueue doesn't exist or inactive")
+	ErrClusterQueueDoesNotExist  = errors.New("clusterQueue doesn't exist or inactive")
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T16:26:10Z

- **pkg/controller/core/workload_controller.go:620** by [@mimowo](https://github.com/mimowo) — 2024-12-13T16:26:10Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  sgtm, looks much better. Actually, how do you tell the error is also returned for inactive?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T16:27:52Z

- **pkg/controller/core/workload_controller.go:620** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:27:52Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  Currently, we do not have distinguished ways so that we can tell that to users...

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T16:30:47Z

- **pkg/controller/core/workload_controller.go:620** by [@mimowo](https://github.com/mimowo) — 2024-12-13T16:30:47Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  I see, but do you have a code reference (could be a unit test) indicating that the error is returned for inactive queues (this is something I'm not sure about).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T16:32:02Z

The code LGTM, but it remains unclear to me if the errors can be returned for inactive queues.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-13T16:40:51Z

- **pkg/controller/core/workload_controller.go:620** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-13T16:40:51Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  We add LocalQueue to the queue manager only if it is active.
  
  https://github.com/kubernetes-sigs/kueue/blob/5ad7e23fdeed50d90d90629c28623f592063ac13/pkg/controller/core/localqueue_controller.go#L141-L146

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-12-13T16:44:21Z

- **pkg/controller/core/workload_controller.go:620** by [@mimowo](https://github.com/mimowo) — 2024-12-13T16:44:20Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  I see, so the message "does not exist or inactive" is accurate.  Do we have any unit tests for this scenario?
  
  How about ClusterQueue? In case it is inactive we still expect it in cache here: https://github.com/kubernetes-sigs/kueue/blob/5ad7e23fdeed50d90d90629c28623f592063ac13/pkg/cache/cache.go#L333-L340

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-13T16:45:59Z

- **pkg/controller/core/workload_controller.go:620** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-13T16:45:59Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  Agree. For CQ we need to revert.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T16:47:21Z

- **pkg/controller/core/workload_controller.go:620** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:47:21Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  As I discussed with @mimowo, for LocalQueue, the "inactive" reason is valid. But for ClusterQueue, we always insert that to queueManager, and then we identify the active CQs by `Active` function: https://github.com/kubernetes-sigs/kueue/blob/5ad7e23fdeed50d90d90629c28623f592063ac13/pkg/queue/cluster_queue.go#L58
  
  So, we decided to remove the "inactive" reason only from ClusterQueue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-13T16:49:00Z

- **pkg/controller/core/workload_controller.go:620** by [@tenzen-y](https://github.com/tenzen-y) — 2024-12-13T16:48:59Z
```diff
@@ -607,9 +608,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	workload.AdjustResources(ctx, r.client, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
-		err := r.queues.AddOrUpdateWorkload(wlCopy)
-		if err != nil {
-			log.V(2).Info(fmt.Sprintf("%s; ignored for now", err))
+		if err := r.queues.AddOrUpdateWorkload(wlCopy); err != nil {
+			var msg string
+			baseMsg := "for workload didn't exist or not active; ignored for now"
+			switch {
+			case errors.Is(err, queue.ErrClusterQueueDoesNotExist):
+				msg = fmt.Sprintf("ClusterQueue %s", baseMsg)
+			case errors.Is(err, queue.ErrQueueDoesNotExist):
+				msg = fmt.Sprintf("LocalQueue %s", baseMsg)
+			}
+			log.V(2).Info(msg)
```

  Done
