# PR #3892: [Feature] Kuberay RayJob MultiKueue adapter

**Summary**: [Feature] Kuberay RayJob MultiKueue adapter

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3892

**Last updated**: 2025-10-24T11:15:26Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/kuberay-multikueue-adapter` → `main`
- **Created**: 2024-12-19T10:53:06Z
- **Updated**: 2025-10-24T11:15:26Z
- **Merged**: 2025-01-17T10:28:08Z
- **Closed**: 2025-01-17T10:28:08Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 18   **+581 / -33**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
We want to be able to run Kuberay workloads RayJob in MultiKueue.
RayCluster will come as separate PR.

Testing: 
Unit and multikueue e2e tests are provided.
Multikueue integration tests will be provided in separate PR - agreed with @mimowo.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to  #3822

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support RayJob in MultiKueue, assuming only Ray CRDs are installed on the management cluster.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-19T10:53:09Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:53:18Z

/ok-to-test

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-19T10:53:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8f174bb7ff4c29ea579c39bddd64769a5b083739 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/678900beccfe650008c92bb0 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-12-30T12:39:39Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2024-12-30T13:38:59Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-01-07T09:02:29Z

Although the PR for Kuberay RayJob was ready for review before New Year, I couldn’t finish to work on it due to peculiar issue. 
Only CI integration tests failed (problem not spotted locally), but before running any test. 
IT failed on “Running Suite: Multikueue” and stuck forever. 
Further investigation and enabling ginkgo output gave little more details. 

Namely Envtest failed on setup and with couple of different yet relating errors: 
```
 unable to install CRDs onto control plane: unable to create CRD instances: unable to create CRD "pytorchjobs.kubeflow.org": Post "https://127.0.0.1:44817/apis/apiextensions.k8s.io/v1/customresourcedefinitions": http2: client connection lost
 
unable to install CRDs onto control plane: unable to create CRD instances: unable to create CRD "rayjobs.ray.io": Post "https://127.0.0.1:33909/apis/apiextensions.k8s.io/v1/customresourcedefinitions": http2: client connection lost

 unable to install CRDs onto control plane: unable to create CRD instances: unable to create CRD "rayjobs.ray.io": Post "https://127.0.0.1:37813/apis/apiextensions.k8s.io/v1/customresourcedefinitions": http2: client connection lost
 
unable to install CRDs onto control plane: unable to create CRD instances: unable to create CRD "workloads.kueue.x-k8s.io": Post "https://127.0.0.1:32965/apis/apiextensions.k8s.io/v1/customresourcedefinitions": http2: client connection lost
```

Those errors were distributed over all cluster routines (as clusters run in separate go routines).  

During the investigation with Mykhailo we were able to boil down the issue to simple addition of “ray-operator” CRDS to the suite.
https://github.com/kubernetes-sigs/kueue/pull/3920 - was our testing ground 

We also checked: 
* extended CRD installation times for testEnv 
* extended WorkerLostTimeout option in SetupControllers 
* increase INTEGRATION_API_LOG_LEVEL verbosity - this gave some insight but basically we saw that ETCD lost connection without learning the cause: 
`
  E0103 11:50:24.628853   28403 finisher.go:175] "Unhandled Error" err="FinishRequest: post-timeout activity - time-elapsed: 1.969052ms, panicked: false, err: context canceled, panic-reason: <nil>" logger="UnhandledError"
`

 Finally what worked was to reduce the `INTEGRATION_NPROCS`.

Which is still not understood why, but we assume that maybe apiserver of envtest got overwhelmed and possibly test-infra requires resource update.

We don’t have any proof that this will work, but some people reportedly so similar issues (not the exact ones): 
https://github.com/kubernetes-sigs/controller-runtime/issues/1246#issuecomment-722668315

 Maybe any of you guys encounter similar issue in the past?
 /cc @mimowo @PBundyra @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-07T13:03:16Z

To check if the tests are stable:
/test pull-kueue-test-integration-main
/test pull-kueue-test-multikueue-e2e-main

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-01-08T09:41:47Z

> Given that you had to reduce INTEGRATION_NPROCS I suspect the cluster running the integration tests is resource constrained. However, I'm wondering if `INTEGRATION_API_LOG_LEVEL=1` or `--output-interceptor-mode=none` could be making a difference here?

Those were added later to observe what is happening (after the behaviour started), but good point I will remove them.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-08T09:44:40Z

/test pull-kueue-test-integration-main
/test pull-kueue-test-multikueue-e2e-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-09T09:14:47Z

Actually, could you maybe split the PR into smaller parts:
1. integration tests (which could be helpful to prove this fix works as expected: https://github.com/kubernetes-sigs/kueue/pull/3949 (IIUC these changes in test/integration are not MultiKueue-specific anyway)
2. actual adapter + e2e tests (this PR)

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-01-09T16:23:04Z

> Actually, could you maybe split the PR into smaller parts:
> 
> 1. integration tests (which could be helpful to prove this fix works as expected: [[WIP] Update Jobeframework's IsActive() for RayJob #3949](https://github.com/kubernetes-sigs/kueue/pull/3949) (IIUC these changes in test/integration are not MultiKueue-specific anyway)
> 2. actual adapter + e2e tests (this PR)

I guess it's no longer valid, as there is no fix, rather extension for MK

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-13T12:41:10Z

/release-note-edit
```release-note
Support RayJob in MultiKueue, assuming only Ray CRDs are installed on the management cluster.
```
We may revisit the release note when the support relies on managedBy.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-13T12:49:32Z

@mszadkow  do you know why `pull-kueue-test-integration-main` is failing?

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-01-13T13:44:12Z

> @mszadkow do you know why `pull-kueue-test-integration-main` is failing?

It won't anymore, I have reverted one change to get logs for the fail - https://github.com/kubernetes-sigs/kueue/pull/3892#discussion_r1910129910

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-17T10:08:25Z

LGTM label has been added.  <details>Git tree hash: e24a7a5c8fb7053c6d7dd5c6c84ccba7bcb3821c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-17T10:08:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3892#pullrequestreview-2558457662" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3892#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T10:53:58Z

- **Makefile-deps.mk:136** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:53:58Z
```diff
@@ -127,9 +127,17 @@ kf-training-operator-manifests: ## Copy whole manifests folder from the training
 
 RAY_ROOT = $(shell $(GO_CMD) list -m -mod=readonly -f "{{.Dir}}" github.com/ray-project/kuberay/ray-operator)
 .PHONY: ray-operator-crd
-ray-operator-crd: ## Copy the CRDs from the ray-operator to the dep-crds directory.
-	mkdir -p $(EXTERNAL_CRDS_DIR)/ray-operator/
-	cp -f $(RAY_ROOT)/config/crd/bases/* $(EXTERNAL_CRDS_DIR)/ray-operator/
+ray-operator-crd: ## Copy the whole manifests from the ray-operator to the dep-crds directory.
+	## Full version of the manifest is required for e2e multikueue tests.
+	if [ -d "${EXTERNAL_CRDS_DIR}/ray-operator" ]; then \
+		chmod -R u+w "$(EXTERNAL_CRDS_DIR)/ray-operator" && \
+		rm -rf "$(EXTERNAL_CRDS_DIR)/ray-operator"; \
+	fi
+	mkdir -p "${EXTERNAL_CRDS_DIR}/ray-operator"; \
```

  Yes, I need all of those folders to be able to properly install it.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T10:55:36Z

- **Makefile-test.mk:74** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:55:36Z
```diff
@@ -70,6 +70,7 @@ IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
 JOBSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/jobset)
 KUBEFLOW_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/training-operator)
 KUBEFLOW_MPI_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/mpi-operator)
+KUBERAY_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/ray-project/kuberay/ray-operator)
```

  So far it's the version without managedBy.
  We don't have a ray-operator image that supports managedBy, that would have to be a custom one...
  latest -> 1.2.2, which is the last release

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T10:55:52Z

- **config/components/manager/kustomization.yaml:17** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:55:52Z
```diff
@@ -14,4 +14,4 @@ kind: Kustomization
 images:
 - name: controller
   newName: us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue
-  newTag: main
+  newTag: v0.10.0-rc.3-53-g215d32ad-dirty
```

  ups...will clean it

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T10:58:00Z

- **hack/e2e-common.sh:52** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:58:00Z
```diff
@@ -40,6 +40,14 @@ if [[ -n ${KUBEFLOW_MPI_VERSION:-} ]]; then
     export KUBEFLOW_MPI_IMAGE=mpioperator/mpi-operator:${KUBEFLOW_MPI_VERSION/#v}
 fi
 
+if [[ -n ${KUBERAY_VERSION:-} ]]; then
+    export KUBERAY_MANIFEST="${ROOT_DIR}/dep-crds/ray-operator/default/"
+    export KUBERAY_IMAGE=bitnami/kuberay-operator:${KUBERAY_VERSION/#v}
+    export KUBERAY_RAY_IMAGE=rayproject/ray:2.9.0
+    export KUBERAY_RAY_IMAGE_ARM=rayproject/ray:2.9.0-aarch64
```

  this one is for us people working on macOS, it's vital for development, not so much for prod - I think it should stay

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T10:58:48Z

- **hack/e2e-common.sh:160** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:58:48Z
```diff
@@ -136,6 +155,22 @@ function install_mpi {
     kubectl apply --server-side -f "${KUBEFLOW_MPI_MANIFEST}"
 }
 
+#$1 - cluster name
+function install_kuberay {
+    # Extra e2e images required for Kuberay, 
```

  that one gives us the speed on test execution, but takes time here...
  I am about to measure how much

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T10:59:08Z

- **pkg/controller/jobs/raycluster/raycluster_multikueue_adapter.go:76** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:59:08Z
```diff
@@ -0,0 +1,127 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type multikueueAdapter struct{}
+
+var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
+
+func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
+	localJob := rayv1.RayCluster{}
+	err := localClient.Get(ctx, key, &localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := rayv1.RayCluster{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// if the remote exists, just copy the status
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, &localJob, func() (bool, error) {
+			localJob.Status = remoteJob.Status
+			return true, nil
+		})
+	}
+
+	remoteJob = rayv1.RayCluster{
+		ObjectMeta: api.CloneObjectMetaForCreation(&localJob.ObjectMeta),
+		Spec:       *localJob.Spec.DeepCopy(),
+	}
+
+	// add the prebuilt workload
+	if remoteJob.Labels == nil {
+		remoteJob.Labels = make(map[string]string, 2)
+	}
+	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[kueue.MultiKueueOriginLabel] = origin
+
+	// clear the managedBy enables the controller to take over
+	// remoteJob.Spec.ManagedBy = nil
```

  This can be removed np

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T10:59:13Z

- **pkg/controller/jobs/raycluster/raycluster_multikueue_adapter.go:93** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T10:59:12Z
```diff
@@ -0,0 +1,127 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type multikueueAdapter struct{}
+
+var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
+
+func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
+	localJob := rayv1.RayCluster{}
+	err := localClient.Get(ctx, key, &localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := rayv1.RayCluster{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// if the remote exists, just copy the status
+	if err == nil {
+		return clientutil.PatchStatus(ctx, localClient, &localJob, func() (bool, error) {
+			localJob.Status = remoteJob.Status
+			return true, nil
+		})
+	}
+
+	remoteJob = rayv1.RayCluster{
+		ObjectMeta: api.CloneObjectMetaForCreation(&localJob.ObjectMeta),
+		Spec:       *localJob.Spec.DeepCopy(),
+	}
+
+	// add the prebuilt workload
+	if remoteJob.Labels == nil {
+		remoteJob.Labels = make(map[string]string, 2)
+	}
+	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[kueue.MultiKueueOriginLabel] = origin
+
+	// clear the managedBy enables the controller to take over
+	// remoteJob.Spec.ManagedBy = nil
+
+	return remoteClient.Create(ctx, &remoteJob)
+}
+
+func (b *multikueueAdapter) DeleteRemoteObject(ctx context.Context, remoteClient client.Client, key types.NamespacedName) error {
+	job := rayv1.RayCluster{}
+	job.SetName(key.Name)
+	job.SetNamespace(key.Namespace)
+	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
+}
+
+func (b *multikueueAdapter) KeepAdmissionCheckPending() bool {
+	return false
+}
+
+func (b *multikueueAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
+	// job := rayv1.RayCluster{}
```

  This can be removed np

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-19T11:20:57Z

- **pkg/controller/jobs/rayjob/rayjob_multikueue_adapter.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-19T11:20:57Z
```diff
@@ -0,0 +1,126 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type multikueueAdapter struct{}
+
+var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
+
+func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
+	localJob := rayv1.RayJob{}
+	err := localClient.Get(ctx, key, &localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := rayv1.RayJob{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// if the remote exists, just copy the status
+	if err == nil {
```

  Please also wait for unsuspend. See https://github.com/kubernetes-sigs/kueue/issues/3730.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-19T11:21:52Z

- **pkg/controller/jobs/raycluster/raycluster_multikueue_adapter.go:56** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-19T11:21:51Z
```diff
@@ -0,0 +1,127 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type multikueueAdapter struct{}
+
+var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
+
+func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
+	localJob := rayv1.RayCluster{}
+	err := localClient.Get(ctx, key, &localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := rayv1.RayCluster{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// if the remote exists, just copy the status
+	if err == nil {
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-19T11:45:01Z

- **test/e2e/multikueue/e2e_test.go:504** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-19T11:36:37Z
```diff
@@ -491,6 +495,56 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("Should run a RayJob on worker if admitted", func() {
+			E2eKuberayTestImage := "rayproject/ray:2.9.0"
+			if runtime.GOOS == "darwin" {
+				E2eKuberayTestImage = "rayproject/ray:2.9.0-py39-aarch64"
+			}
+			fmt.Println(runtime.GOOS, E2eKuberayTestImage)
```

  ```suggestion
  			ginkgo.GinkgoLogr.Info(runtime.GOOS, E2eKuberayTestImage)
  ```
  Or maybe remove it?

- **test/integration/multikueue/multikueue_test.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-19T11:43:32Z
```diff
@@ -66,9 +69,10 @@ import (
 var defaultEnabledIntegrations sets.Set[string] = sets.New(
 	"batch/job", "kubeflow.org/mpijob", "ray.io/rayjob", "ray.io/raycluster",
 	"jobset.x-k8s.io/jobset", "kubeflow.org/mxjob", "kubeflow.org/paddlejob",
-	"kubeflow.org/pytorchjob", "kubeflow.org/tfjob", "kubeflow.org/xgboostjob")
+	"kubeflow.org/pytorchjob", "kubeflow.org/tfjob", "kubeflow.org/xgboostjob",
+	"ray.io/rayjob", "ray.io/raycluster")
 
-var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+var _ = ginkgo.FDescribe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  ```suggestion
  var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-19T12:02:13Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:91** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-19T12:02:13Z
```diff
@@ -87,7 +88,7 @@ func (j *RayJob) IsSuspended() bool {
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew
```

  Do we need this changes?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T12:21:29Z

- **pkg/controller/jobs/rayjob/rayjob_multikueue_adapter.go:59** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T12:21:29Z
```diff
@@ -0,0 +1,126 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package rayjob
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/api"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+)
+
+type multikueueAdapter struct{}
+
+var _ jobframework.MultiKueueAdapter = (*multikueueAdapter)(nil)
+
+func (b *multikueueAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
+	localJob := rayv1.RayJob{}
+	err := localClient.Get(ctx, key, &localJob)
+	if err != nil {
+		return err
+	}
+
+	remoteJob := rayv1.RayJob{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// if the remote exists, just copy the status
+	if err == nil {
```

  thx

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T12:21:55Z

- **test/e2e/multikueue/e2e_test.go:504** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T12:21:55Z
```diff
@@ -491,6 +495,56 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("Should run a RayJob on worker if admitted", func() {
+			E2eKuberayTestImage := "rayproject/ray:2.9.0"
+			if runtime.GOOS == "darwin" {
+				E2eKuberayTestImage = "rayproject/ray:2.9.0-py39-aarch64"
+			}
+			fmt.Println(runtime.GOOS, E2eKuberayTestImage)
```

  yes, I will remove it, it was for debugging purposes

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T12:22:02Z

- **test/integration/multikueue/multikueue_test.go:75** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T12:22:01Z
```diff
@@ -66,9 +69,10 @@ import (
 var defaultEnabledIntegrations sets.Set[string] = sets.New(
 	"batch/job", "kubeflow.org/mpijob", "ray.io/rayjob", "ray.io/raycluster",
 	"jobset.x-k8s.io/jobset", "kubeflow.org/mxjob", "kubeflow.org/paddlejob",
-	"kubeflow.org/pytorchjob", "kubeflow.org/tfjob", "kubeflow.org/xgboostjob")
+	"kubeflow.org/pytorchjob", "kubeflow.org/tfjob", "kubeflow.org/xgboostjob",
+	"ray.io/rayjob", "ray.io/raycluster")
 
-var _ = ginkgo.Describe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+var _ = ginkgo.FDescribe("Multikueue", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  correct

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T12:24:22Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:91** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T12:24:22Z
```diff
@@ -87,7 +88,7 @@ func (j *RayJob) IsSuspended() bool {
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew
```

  well, that was the most important change actually
  Because `JobDeploymentStatusSuspended` is not a initial status and the workload couldn't be created at all
  Maybe it should be both like not new and not suspended, but definitively not the suspended only

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2024-12-19T12:51:52Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:91** by [@mszadkow](https://github.com/mszadkow) — 2024-12-19T12:51:52Z
```diff
@@ -87,7 +88,7 @@ func (j *RayJob) IsSuspended() bool {
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew
```

  I am not even entirely sure if this should watch the JobDeploymentStatus, as the Job has its own Status.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-19T12:53:15Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:91** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-19T12:53:15Z
```diff
@@ -87,7 +88,7 @@ func (j *RayJob) IsSuspended() bool {
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew
```

  I think you right, it should be both.
  
  https://github.com/kubernetes-sigs/kueue/blob/cea42f23253a4a3221ed529faef539cb61ec742f/pkg/controller/jobframework/interface.go#L57-L58

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-12-20T08:10:02Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:91** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-20T08:10:02Z
```diff
@@ -87,7 +88,7 @@ func (j *RayJob) IsSuspended() bool {
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew
```

  cc: @andrewsykim

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-07T12:56:20Z

Given that you had to reduce INTEGRATION_NPROCS I suspect the cluster running the integration tests is resource constrained. However, I'm wondering if `INTEGRATION_API_LOG_LEVEL=1` or `--output-interceptor-mode=none` could be making a difference here?

- **pkg/controller/jobs/rayjob/rayjob_controller.go:96** by [@mimowo](https://github.com/mimowo) — 2025-01-07T12:47:40Z
```diff
@@ -82,12 +84,16 @@ func (j *RayJob) Object() client.Object {
 	return (*rayv1.RayJob)(j)
 }
 
+func fromObject(obj runtime.Object) *RayJob {
+	return (*RayJob)(obj.(*rayv1.RayJob))
+}
+
 func (j *RayJob) IsSuspended() bool {
 	return j.Spec.Suspend
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended) && (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew)
```

  Why is this change needed? Maybe this is a fix for regular RayJobs too? In that case we should do it in a separate PR so that it can be cherry-picked.

- **test/e2e/multikueue/e2e_test.go:507** by [@mimowo](https://github.com/mimowo) — 2025-01-07T12:48:06Z
```diff
@@ -491,6 +495,55 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("Should run a RayJob on worker if admitted", func() {
+			E2eKuberayTestImage := "rayproject/ray:2.9.0"
+			if runtime.GOOS == "darwin" {
+				E2eKuberayTestImage = "rayproject/ray:2.9.0-aarch64"
+			}
```

  Can we read the image from the KUBERAY_RAY_IMAGE

- **pkg/util/testingjobs/rayjob/wrappers.go:51** by [@mimowo](https://github.com/mimowo) — 2025-01-07T12:52:14Z
```diff
@@ -40,40 +40,59 @@ func MakeJob(name, ns string) *JobWrapper {
 		Spec: rayv1.RayJobSpec{
 			ShutdownAfterJobFinishes: true,
 			RayClusterSpec: &rayv1.RayClusterSpec{
-				HeadGroupSpec: rayv1.HeadGroupSpec{
-					RayStartParams: map[string]string{"p1": "v1"},
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							Containers: []corev1.Container{
-								{
-									Name: "head-container",
-								},
-							},
+				HeadGroupSpec:    rayv1.HeadGroupSpec{},
+				WorkerGroupSpecs: make([]rayv1.WorkerGroupSpec, 0, 1),
+			},
+			Suspend: true,
+		},
+	}}
+}
+
+func (j *JobWrapper) RayJobSpecsDefault() *JobWrapper {
```

  Why to introduce it? Could the default be already set with MakeJob as so far?
  
  Adding this to most (or all) tests makes the diff larger unnecesarily.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-08T09:45:17Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:96** by [@mszadkow](https://github.com/mszadkow) — 2025-01-08T09:45:17Z
```diff
@@ -82,12 +84,16 @@ func (j *RayJob) Object() client.Object {
 	return (*rayv1.RayJob)(j)
 }
 
+func fromObject(obj runtime.Object) *RayJob {
+	return (*RayJob)(obj.(*rayv1.RayJob))
+}
+
 func (j *RayJob) IsSuspended() bool {
 	return j.Spec.Suspend
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended) && (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew)
```

  Yes, I believe it's needed fix.
  Otherwise RayJob is stuck in `handleJobWithNoWorkload()` with `Job is suspended but still has active pods, waiting` and workload is never created.
  I can create separate PR

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-08T09:45:33Z

- **test/e2e/multikueue/e2e_test.go:507** by [@mszadkow](https://github.com/mszadkow) — 2025-01-08T09:45:33Z
```diff
@@ -491,6 +495,55 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("Should run a RayJob on worker if admitted", func() {
+			E2eKuberayTestImage := "rayproject/ray:2.9.0"
+			if runtime.GOOS == "darwin" {
+				E2eKuberayTestImage = "rayproject/ray:2.9.0-aarch64"
+			}
```

  I think so, yes

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-08T09:47:44Z

- **pkg/util/testingjobs/rayjob/wrappers.go:51** by [@mszadkow](https://github.com/mszadkow) — 2025-01-08T09:47:44Z
```diff
@@ -40,40 +40,59 @@ func MakeJob(name, ns string) *JobWrapper {
 		Spec: rayv1.RayJobSpec{
 			ShutdownAfterJobFinishes: true,
 			RayClusterSpec: &rayv1.RayClusterSpec{
-				HeadGroupSpec: rayv1.HeadGroupSpec{
-					RayStartParams: map[string]string{"p1": "v1"},
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							Containers: []corev1.Container{
-								{
-									Name: "head-container",
-								},
-							},
+				HeadGroupSpec:    rayv1.HeadGroupSpec{},
+				WorkerGroupSpecs: make([]rayv1.WorkerGroupSpec, 0, 1),
+			},
+			Suspend: true,
+		},
+	}}
+}
+
+func (j *JobWrapper) RayJobSpecsDefault() *JobWrapper {
```

  Sure, I will strive to make it work without that

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-08T09:49:35Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:96** by [@mimowo](https://github.com/mimowo) — 2025-01-08T09:49:35Z
```diff
@@ -82,12 +84,16 @@ func (j *RayJob) Object() client.Object {
 	return (*rayv1.RayJob)(j)
 }
 
+func fromObject(obj runtime.Object) *RayJob {
+	return (*RayJob)(obj.(*rayv1.RayJob))
+}
+
 func (j *RayJob) IsSuspended() bool {
 	return j.Spec.Suspend
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended) && (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew)
```

  Interesting, maybe it is a race condition? Please open a separate PR, we may consider cherry-picking the fix then.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-09T08:27:04Z

- **Makefile-test.mk:40** by [@mimowo](https://github.com/mimowo) — 2025-01-09T08:27:03Z
```diff
@@ -37,12 +37,12 @@ ENVTEST_K8S_VERSION ?= 1.31
 # Number of processes to use during integration tests to run specs within a
 # suite in parallel. Suites still run sequentially. User may set this value to 1
 # to run without parallelism.
-INTEGRATION_NPROCS ?= 4
+INTEGRATION_NPROCS ?= 3
```

  Can you try to revert it and post a link to the failure log?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-09T08:37:35Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:96** by [@mszadkow](https://github.com/mszadkow) — 2025-01-09T08:37:35Z
```diff
@@ -82,12 +84,16 @@ func (j *RayJob) Object() client.Object {
 	return (*rayv1.RayJob)(j)
 }
 
+func fromObject(obj runtime.Object) *RayJob {
+	return (*RayJob)(obj.(*rayv1.RayJob))
+}
+
 func (j *RayJob) IsSuspended() bool {
 	return j.Spec.Suspend
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended) && (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew)
```

  https://github.com/kubernetes-sigs/kueue/pull/3949

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-09T16:22:04Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:96** by [@mszadkow](https://github.com/mszadkow) — 2025-01-09T16:22:04Z
```diff
@@ -82,12 +84,16 @@ func (j *RayJob) Object() client.Object {
 	return (*rayv1.RayJob)(j)
 }
 
+func fromObject(obj runtime.Object) *RayJob {
+	return (*RayJob)(obj.(*rayv1.RayJob))
+}
+
 func (j *RayJob) IsSuspended() bool {
 	return j.Spec.Suspend
 }
 
 func (j *RayJob) IsActive() bool {
-	return j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended
+	return (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusSuspended) && (j.Status.JobDeploymentStatus != rayv1.JobDeploymentStatusNew)
```

  As agreed with @mimowo, this will stay here, just updated

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-10T10:00:52Z

- **Makefile-test.mk:40** by [@mszadkow](https://github.com/mszadkow) — 2025-01-10T10:00:51Z
```diff
@@ -37,12 +37,12 @@ ENVTEST_K8S_VERSION ?= 1.31
 # Number of processes to use during integration tests to run specs within a
 # suite in parallel. Suites still run sequentially. User may set this value to 1
 # to run without parallelism.
-INTEGRATION_NPROCS ?= 4
+INTEGRATION_NPROCS ?= 3
```

  SO when I did revert [it](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/3892/pull-kueue-test-integration-main/1877634987231023104)
  
  There is no apparent reason to fail the tests, just this message:
  `There were failures detected in the following suites:
    [38;5;9mmultikueue [38;5;243m./test/integration/multikueue[0m`
  
  Below is more, but still nothing to figure out what can be wrong, the way it all started.
  
  ```
  [1m[38;5;9mGinkgo timed out waiting for all parallel procs to report back[0m
  [38;5;243mTest suite:[0m multikueue (./test/integration/multikueue)
  
  This occurs if a parallel process exits before it reports its results to the
  Ginkgo CLI.  The CLI will now print out all the stdout/stderr output it's
  collected from the running processes.  However you may not see anything useful
  in these logs because the individual test processes usually intercept output to
  stdout/stderr in order to capture it in the spec reports.
  
  You may want to try rerunning your test suite with
  [38;5;246m--output-interceptor-mode=none[0m to see additional output here and debug your
  suite.
    
  [1mOutput from proc 1:[0m
  
  [1mOutput from proc 2:[0m
  
  [1mOutput from proc 3:[0m
  
  [1mOutput from proc 4:[0m
    PASS
  
  ** End **Could not open /logs/artifacts/test_integration_multikueue_integration.json:
  open /logs/artifacts/test_integration_multikueue_integration.json: no such file or directory
  Could not open /logs/artifacts/test_integration_multikueue_junit.xml:
  open /logs/artifacts/test_integration_multikueue_junit.xml: no such file or directory
  
  Ginkgo ran 23 suites in 12m16.64715849s
  
  There were failures detected in the following suites:
    [38;5;9mmultikueue [38;5;243m./test/integration/multikueue[0m
  
  Test Suite Failed
  make: *** [Makefile-test.mk:83: test-integration] Error 1
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-13T12:42:37Z

- **Makefile-test.mk:74** by [@mimowo](https://github.com/mimowo) — 2025-01-13T12:42:36Z
```diff
@@ -70,6 +70,7 @@ IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
 JOBSET_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" sigs.k8s.io/jobset)
 KUBEFLOW_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/training-operator)
 KUBEFLOW_MPI_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/kubeflow/mpi-operator)
+KUBERAY_VERSION = $(shell $(GO_CMD) list -m -f "{{.Version}}" github.com/ray-project/kuberay/ray-operator)
```

  Sure, hopefully 1.3 of KubeRay is released before 0.11 of Kueue and the actually released support will rely on managedBy. @andrewsykim do you know the the KubeRay release is planned?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-13T12:46:19Z

- **test/e2e/multikueue/e2e_test.go:517** by [@mimowo](https://github.com/mimowo) — 2025-01-13T12:46:18Z
```diff
@@ -491,6 +496,58 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("Should run a RayJob on worker if admitted", func() {
+			var found bool
+			E2eKuberayTestImage, found := os.LookupEnv("KUBERAY_RAY_IMAGE")
+			gomega.Expect(found).To(gomega.BeTrue())
+			if runtime.GOOS == "darwin" {
+				E2eKuberayTestImage, found = os.LookupEnv("KUBERAY_RAY_IMAGE_ARM")
+				gomega.Expect(found).To(gomega.BeTrue())
+			}
+
+			// Since it requires 1.5 CPU, this job can only be admitted in worker 1.
+			rayjob := testingrayjob.MakeJob("rayjob1", managerNs.Name).
+				Suspend(true).
+				Queue(managerLq.Name).
+				WithSubmissionMode(rayv1.K8sJobMode).
+				Request(rayv1.HeadNode, corev1.ResourceCPU, "1").
+				Request(rayv1.WorkerNode, corev1.ResourceCPU, "0.5").
+				Entrypoint("python -c \"import ray; ray.init(); print(ray.cluster_resources())\"").
+				RayVersion("2.9.0").
```

  Is there a way to read it from the image rather than hard-code here? Alternatively we could introduce a dedicated env var like `KUBERAY_RAY_VERSION` to put all the consts in one place.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-13T12:47:23Z

- **test/integration/controller/jobs/rayjob/rayjob_controller_test.go:374** by [@mimowo](https://github.com/mimowo) — 2025-01-13T12:47:23Z
```diff
@@ -371,7 +371,9 @@ var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", ginkgo.O
 	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
 		func(podsReadyTestSpec podsReadyTestSpec) {
 			ginkgo.By("Create a job")
-			job := testingrayjob.MakeJob(jobName, ns.Name).WithSubmissionMode(rayv1.K8sJobMode).Obj()
```

  nit: Revert the unnecessary changes (and below in this file).

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-13T13:37:55Z

- **test/e2e/multikueue/e2e_test.go:517** by [@mszadkow](https://github.com/mszadkow) — 2025-01-13T13:37:55Z
```diff
@@ -491,6 +496,58 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("Should run a RayJob on worker if admitted", func() {
+			var found bool
+			E2eKuberayTestImage, found := os.LookupEnv("KUBERAY_RAY_IMAGE")
+			gomega.Expect(found).To(gomega.BeTrue())
+			if runtime.GOOS == "darwin" {
+				E2eKuberayTestImage, found = os.LookupEnv("KUBERAY_RAY_IMAGE_ARM")
+				gomega.Expect(found).To(gomega.BeTrue())
+			}
+
+			// Since it requires 1.5 CPU, this job can only be admitted in worker 1.
+			rayjob := testingrayjob.MakeJob("rayjob1", managerNs.Name).
+				Suspend(true).
+				Queue(managerLq.Name).
+				WithSubmissionMode(rayv1.K8sJobMode).
+				Request(rayv1.HeadNode, corev1.ResourceCPU, "1").
+				Request(rayv1.WorkerNode, corev1.ResourceCPU, "0.5").
+				Entrypoint("python -c \"import ray; ray.init(); print(ray.cluster_resources())\"").
+				RayVersion("2.9.0").
```

  that's different one it's from ray-project, we are not going to change it that much
  I think if we have const in e2e tests that should be fine for now

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-13T13:38:37Z

- **test/e2e/multikueue/e2e_test.go:517** by [@mszadkow](https://github.com/mszadkow) — 2025-01-13T13:38:37Z
```diff
@@ -491,6 +496,58 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("Should run a RayJob on worker if admitted", func() {
+			var found bool
+			E2eKuberayTestImage, found := os.LookupEnv("KUBERAY_RAY_IMAGE")
+			gomega.Expect(found).To(gomega.BeTrue())
+			if runtime.GOOS == "darwin" {
+				E2eKuberayTestImage, found = os.LookupEnv("KUBERAY_RAY_IMAGE_ARM")
+				gomega.Expect(found).To(gomega.BeTrue())
+			}
+
+			// Since it requires 1.5 CPU, this job can only be admitted in worker 1.
+			rayjob := testingrayjob.MakeJob("rayjob1", managerNs.Name).
+				Suspend(true).
+				Queue(managerLq.Name).
+				WithSubmissionMode(rayv1.K8sJobMode).
+				Request(rayv1.HeadNode, corev1.ResourceCPU, "1").
+				Request(rayv1.WorkerNode, corev1.ResourceCPU, "0.5").
+				Entrypoint("python -c \"import ray; ray.init(); print(ray.cluster_resources())\"").
+				RayVersion("2.9.0").
```

  or even better let's have it as default value in wrappers

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-13T14:33:10Z

- **Makefile-test.mk:40** by [@mimowo](https://github.com/mimowo) — 2025-01-13T14:33:10Z
```diff
@@ -37,7 +37,7 @@ ENVTEST_K8S_VERSION ?= 1.31
 # Number of processes to use during integration tests to run specs within a
 # suite in parallel. Suites still run sequentially. User may set this value to 1
 # to run without parallelism.
-INTEGRATION_NPROCS ?= 4
+INTEGRATION_NPROCS ?= 3
```

  TBH changing this because the test fails when this is 4 isn't very satisfying. 
  
  I don't want to block the PR on that OTOH I'm worried it may hit us when adding a new test later on, completely unrelated, and then the new contributor will need to fix two problems. 
  
  Could you take a closer look before we merge, specifically:
  1. can the issue be reproduced locally
  2. what is the content of the `test_integration_multikueue_integration.json` file normally, is it an example like [this](https://storage.googleapis.com/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/3892/pull-kueue-test-integration-main/1878801137641984000/artifacts/integration.json)?
  3. where is the code which fails when this is raised:
  ```
  ** End **Could not open /logs/artifacts/test_integration_multikueue_integration.json:
  open /logs/artifacts/test_integration_multikueue_integration.json: no such file or directory
  ```?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-01-13T15:11:31Z

- **Makefile-test.mk:40** by [@mszadkow](https://github.com/mszadkow) — 2025-01-13T15:11:31Z
```diff
@@ -37,7 +37,7 @@ ENVTEST_K8S_VERSION ?= 1.31
 # Number of processes to use during integration tests to run specs within a
 # suite in parallel. Suites still run sequentially. User may set this value to 1
 # to run without parallelism.
-INTEGRATION_NPROCS ?= 4
+INTEGRATION_NPROCS ?= 3
```

  I agree, that could be the case.
  We were able to distil the root cause to simple addition of extra CRDs in Multikueue setup -  https://github.com/kubernetes-sigs/kueue/pull/3920
  
  1. Locally we never had such issue, works everytime
  2. Locally it never appears with this name and even in CI there is np such file, it appears to be temporary file to produce this: https://storage.googleapis.com/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/3892/pull-kueue-test-integration-main/1878801137641984000/artifacts/integration.json
  3. There is no log we could point to that is indirectly telling `this is the problem`.
  
  I understand the concern and I will continue to investigate the issue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-17T10:08:19Z

/lgtm
/approve
Thanks!
Please follow up with integration tests. For now I believe the e2e tests + unit tests are providing enough coverage to merge, but going forward I want to make sure integration tests are added, so that we can test some bugfixes or features without extending the heavy e2e tests.
