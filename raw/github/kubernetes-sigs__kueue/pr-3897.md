# PR #3897: add argo integration based on pod integration

**Summary**: add argo integration based on pod integration

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3897

**Last updated**: 2025-01-30T15:01:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `argo-integration` → `main`
- **Created**: 2024-12-20T17:00:30Z
- **Updated**: 2025-01-30T15:01:49Z
- **Merged**: 2025-01-29T14:51:25Z
- **Closed**: 2025-01-29T14:51:25Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 9   **+163 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add an integration with Kueue and Argo Workflows
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
Credit goes to [sam-leitch-oxb](https://github.com/sam-leitch-oxb). 
Nice work!
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-20T17:00:47Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 30558fc276ce69fd671cd06e6f555876eca6c8bb |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/679931db4dc18000087cbc54 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-3897--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-3897--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTM4OTctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.5D1BuxDl8yi60Wi2fsWqQTnFFS0omVHkvRga2047dUw)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-3897--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@trasc](https://github.com/trasc) — 2025-01-14T11:55:49Z

/uncc

### Comment by [@kannon92](https://github.com/kannon92) — 2025-01-23T01:34:24Z

@mimowo PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-23T08:37:20Z

/approve
/hold
LGTM overall, but would like to get a pass from @PBundyra who is recently looking at the ways to improve our documentation.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-01-23T09:39:55Z

Other than the one comment I left, LGTM! Feel free to unhold your PR after addressing it.
Thanks!
/lgtm

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-01-23T09:41:28Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-23T09:41:35Z

LGTM label has been added.  <details>Git tree hash: c034e48c52ccc8eb568493ca2b67a9b23abdb2fe</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-01-23T15:23:54Z

/hold

Addressing https://github.com/kubernetes-sigs/kueue/pull/3897#discussion_r1926706720.

Need to confirm that this worked.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-01-23T20:25:29Z

@PBundyra @mimowo PTAL when you get a chance.

I moved around the files as you suggested.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-29T07:45:54Z

LGTM label has been added.  <details>Git tree hash: be39cfda7dec117ccaf20956e1f0ece28e98896f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-29T07:45:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3897#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3897#pullrequestreview-2580225304" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kannon92](https://github.com/kannon92) — 2025-01-29T14:33:57Z

/hold cancel

### Comment by [@kannon92](https://github.com/kannon92) — 2025-01-30T13:58:24Z

Should this be cherry-picked to website also?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-30T14:11:11Z

We could cherry pick, but i assume it would have conflicts due to introducing AppWrapper in the meanwhile.

I guess it depends on you. if you want to prepare the cherry pick and resolve the conflicts, then i will welcome the PR, but waiting another 1.5month for the docs update along with 0.11 is also fine for me.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-01-30T14:13:15Z

I would suggest waiting until 0.11 so we get a unified update of the website.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-01-30T15:01:47Z

SGTM.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-15T10:58:23Z

Thanks for the docs!

- **site/content/en/docs/tasks/run/argo_workload.md:27** by [@mimowo](https://github.com/mimowo) — 2025-01-15T10:50:57Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   The pod integration for Argo Workflows looks like:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
```

  Instead of podOptions we are migrating to use `managedJobsNamespaceSelector` as a more generic solution, also for plain pods. Could you update as currently in https://kueue.sigs.k8s.io/docs/tasks/run/plain_pods/
  
  cc @dgrove-oss

- **site/content/en/docs/tasks/run/argo_workload.md:64** by [@mimowo](https://github.com/mimowo) — 2025-01-15T10:51:56Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   The pod integration for Argo Workflows looks like:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which
+       # namespaces kueue will manage the Workflow pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Argo Workflows uses the workflows.argoproj.io/completed label to
+       # keep track of pods it manages. We will use that as a hint for Kueue
+       # to find Argo Workflow pods.
+       podSelector:
+         matchExpressions:
+         - key: workflows.argoproj.io/completed
+           operator: In
+           values: [ "false" ]
+   ```
+
+2. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration.
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Workflow definition
+
+When creating [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/),
+[WorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/workflow-templates/),
+[ClusterWorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/cluster-workflow-templates/),
+or [CronWorkflow](https://argo-workflows.readthedocs.io/en/latest/cron-workflows/) resources for
+Kueue, take into consideration the following aspects:
+
+### a. Targeting a single LocalQueue
+
+If you want the entire workflow to target a single [local queue](/docs/concepts/local_queue),
+it should be specified in the `spec.podMetadata` section of the Workflow configuration.
+
+```yaml
+apiVersion: argoproj.io/v1alpha1
```

  please more the examples to https://github.com/kubernetes-sigs/kueue/tree/main/site/static/examples/jobs for consistency as a single source of examples easy to run.

- **site/content/en/docs/tasks/run/argo_workload.md:145** by [@mimowo](https://github.com/mimowo) — 2025-01-15T10:52:33Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   The pod integration for Argo Workflows looks like:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which
+       # namespaces kueue will manage the Workflow pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Argo Workflows uses the workflows.argoproj.io/completed label to
+       # keep track of pods it manages. We will use that as a hint for Kueue
+       # to find Argo Workflow pods.
+       podSelector:
+         matchExpressions:
+         - key: workflows.argoproj.io/completed
+           operator: In
+           values: [ "false" ]
+   ```
+
+2. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration.
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Workflow definition
+
+When creating [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/),
+[WorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/workflow-templates/),
+[ClusterWorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/cluster-workflow-templates/),
+or [CronWorkflow](https://argo-workflows.readthedocs.io/en/latest/cron-workflows/) resources for
+Kueue, take into consideration the following aspects:
+
+### a. Targeting a single LocalQueue
+
+If you want the entire workflow to target a single [local queue](/docs/concepts/local_queue),
+it should be specified in the `spec.podMetadata` section of the Workflow configuration.
+
+```yaml
+apiVersion: argoproj.io/v1alpha1
+kind: Workflow
+metadata:
+  generateName: hello-world-
+spec:
+  entrypoint: whalesay
+  podMetadata:
+    labels:
+      kueue.x-k8s.io/queue-name: user-queue # All pods will target user-queue
+  templates:
+    - name: whalesay
+      container:
+        image: docker/whalesay
+        command: [ cowsay ]
+        args: [ "hello world" ]
+        resources:
+          limits:
+            memory: 32Mi
+            cpu: 100m
+```
+
+### b. Targeting a different LocalQueue per template
+
+If prefer to target a different [local queue](/docs/concepts/local_queue) for each step of your Workflow,
+you can define the queue in the `spec.templates[].metadata` section of the Workflow configuration.
+
+In this example `hello1` and `hello2a` will target `user-queue` and `hello2b` will
+target `user-queue-2`.
+
+```yaml
+apiVersion: argoproj.io/v1alpha1
+kind: Workflow
+metadata:
+  generateName: steps-
+spec:
+  entrypoint: hello-hello-hello
+
+  templates:
+  - name: hello-hello-hello
+    steps:
+    - - name: hello1            # hello1 is run before the following steps
+        template: whalesay
+        arguments:
+          parameters:
+          - name: message
+            value: "hello1"
+    - - name: hello2a           # double dash => run after previous step
+        template: whalesay
+        arguments:
+          parameters:
+          - name: message
+            value: "hello2a"
+      - name: hello2b           # single dash => run in parallel with previous step
+        template: whalesay-queue-2
+        arguments:
+          parameters:
+          - name: message
+            value: "hello2b"
+
+  - name: whalesay
+    metadata:
+      labels:
+        kueue.x-k8s.io/queue-name: user-queue # Pods from this template will target user-queue
+    inputs:
+      parameters:
+      - name: message
+    container:
+      image: docker/whalesay
+      command: [cowsay]
+      args: ["{{inputs.parameters.message}}"]
+
+  - name: whalesay-queue-2
+    metadata:
+      labels:
+        kueue.x-k8s.io/queue-name: user-queue-2 # Pods from this template will target user-queue-2
+    inputs:
+      parameters:
+      - name: message
+    container:
+      image: docker/whalesay
+      command: [cowsay]
+      args: ["{{inputs.parameters.message}}"]
```

  does it need to be expanded to make the example runnable?

- **site/content/en/docs/tasks/run/argo_workload.md:13** by [@mimowo](https://github.com/mimowo) — 2025-01-15T10:54:06Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
```

  ```suggestion
  Currently Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
  ```
  nit

- **site/content/en/docs/tasks/run/argo_workload.md:16** by [@mimowo](https://github.com/mimowo) — 2025-01-15T10:55:16Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
```

  Please wrap the step in "Before you begin" for consistency, example: https://kueue.sigs.k8s.io/docs/tasks/run/jobsets/. And please add a point about installing the Argo Workflow, along with a link indicating how to do it.

- **site/content/en/docs/tasks/_index.md:47** by [@mimowo](https://github.com/mimowo) — 2025-01-15T10:58:10Z
```diff
@@ -44,6 +44,7 @@ As a batch user, you can learn how to:
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 - [Submit jobs to MultiKueue](run/multikueue).
+- [Run an argo workflow](run/argo_workload)
```

  This is great. We also have a similar way of support for the Notebook integration: https://github.com/kubernetes-sigs/kueue/issues/3878. I think we could have a dedicated section for them like "Run workloads with Pods managed by Kueue", with this doc being the first of this kind, but Notebook a potential follow up. WDYT @kannon92 @varshaprasad96 @tenzen-y ?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-16T01:17:04Z

- **site/content/en/docs/tasks/run/argo_workload.md:145** by [@kannon92](https://github.com/kannon92) — 2025-01-16T01:17:04Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   The pod integration for Argo Workflows looks like:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which
+       # namespaces kueue will manage the Workflow pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Argo Workflows uses the workflows.argoproj.io/completed label to
+       # keep track of pods it manages. We will use that as a hint for Kueue
+       # to find Argo Workflow pods.
+       podSelector:
+         matchExpressions:
+         - key: workflows.argoproj.io/completed
+           operator: In
+           values: [ "false" ]
+   ```
+
+2. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration.
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Workflow definition
+
+When creating [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/),
+[WorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/workflow-templates/),
+[ClusterWorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/cluster-workflow-templates/),
+or [CronWorkflow](https://argo-workflows.readthedocs.io/en/latest/cron-workflows/) resources for
+Kueue, take into consideration the following aspects:
+
+### a. Targeting a single LocalQueue
+
+If you want the entire workflow to target a single [local queue](/docs/concepts/local_queue),
+it should be specified in the `spec.podMetadata` section of the Workflow configuration.
+
+```yaml
+apiVersion: argoproj.io/v1alpha1
+kind: Workflow
+metadata:
+  generateName: hello-world-
+spec:
+  entrypoint: whalesay
+  podMetadata:
+    labels:
+      kueue.x-k8s.io/queue-name: user-queue # All pods will target user-queue
+  templates:
+    - name: whalesay
+      container:
+        image: docker/whalesay
+        command: [ cowsay ]
+        args: [ "hello world" ]
+        resources:
+          limits:
+            memory: 32Mi
+            cpu: 100m
+```
+
+### b. Targeting a different LocalQueue per template
+
+If prefer to target a different [local queue](/docs/concepts/local_queue) for each step of your Workflow,
+you can define the queue in the `spec.templates[].metadata` section of the Workflow configuration.
+
+In this example `hello1` and `hello2a` will target `user-queue` and `hello2b` will
+target `user-queue-2`.
+
+```yaml
+apiVersion: argoproj.io/v1alpha1
+kind: Workflow
+metadata:
+  generateName: steps-
+spec:
+  entrypoint: hello-hello-hello
+
+  templates:
+  - name: hello-hello-hello
+    steps:
+    - - name: hello1            # hello1 is run before the following steps
+        template: whalesay
+        arguments:
+          parameters:
+          - name: message
+            value: "hello1"
+    - - name: hello2a           # double dash => run after previous step
+        template: whalesay
+        arguments:
+          parameters:
+          - name: message
+            value: "hello2a"
+      - name: hello2b           # single dash => run in parallel with previous step
+        template: whalesay-queue-2
+        arguments:
+          parameters:
+          - name: message
+            value: "hello2b"
+
+  - name: whalesay
+    metadata:
+      labels:
+        kueue.x-k8s.io/queue-name: user-queue # Pods from this template will target user-queue
+    inputs:
+      parameters:
+      - name: message
+    container:
+      image: docker/whalesay
+      command: [cowsay]
+      args: ["{{inputs.parameters.message}}"]
+
+  - name: whalesay-queue-2
+    metadata:
+      labels:
+        kueue.x-k8s.io/queue-name: user-queue-2 # Pods from this template will target user-queue-2
+    inputs:
+      parameters:
+      - name: message
+    container:
+      image: docker/whalesay
+      command: [cowsay]
+      args: ["{{inputs.parameters.message}}"]
```

  I think this is runnable.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-16T01:18:25Z

- **site/content/en/docs/tasks/_index.md:47** by [@kannon92](https://github.com/kannon92) — 2025-01-16T01:18:25Z
```diff
@@ -44,6 +44,7 @@ As a batch user, you can learn how to:
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 - [Submit jobs to MultiKueue](run/multikueue).
+- [Run an argo workflow](run/argo_workload)
```

  I also have #3898. So I think this is a good idea to document examples that use pod integration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-16T09:01:23Z

- **site/content/en/docs/tasks/_index.md:47** by [@mimowo](https://github.com/mimowo) — 2025-01-16T09:01:23Z
```diff
@@ -44,6 +44,7 @@ As a batch user, you can learn how to:
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 - [Submit jobs to MultiKueue](run/multikueue).
+- [Run an argo workflow](run/argo_workload)
```

  great, I missed that, I would put them together in one sub-folder. We can maybe first merge this one, then align the other.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-17T18:21:47Z

- **site/content/en/docs/tasks/run/argo_workload.md:27** by [@kannon92](https://github.com/kannon92) — 2025-01-17T18:21:47Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   The pod integration for Argo Workflows looks like:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
```

  I'll just link to that section.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-17T18:28:51Z

- **site/content/en/docs/tasks/run/argo_workload.md:64** by [@kannon92](https://github.com/kannon92) — 2025-01-17T18:28:50Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
+   Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+   and enable the `pod` integration.
+
+   The pod integration for Argo Workflows looks like:
+   ```yaml
+   apiVersion: config.kueue.x-k8s.io/v1beta1
+   kind: Configuration
+   integrations:
+     frameworks:
+      - "pod"
+     podOptions:
+       # You can change namespaceSelector to define in which
+       # namespaces kueue will manage the Workflow pods.
+       namespaceSelector:
+         matchExpressions:
+         - key: kubernetes.io/metadata.name
+           operator: NotIn
+           values: [ kube-system, kueue-system ]
+       # Argo Workflows uses the workflows.argoproj.io/completed label to
+       # keep track of pods it manages. We will use that as a hint for Kueue
+       # to find Argo Workflow pods.
+       podSelector:
+         matchExpressions:
+         - key: workflows.argoproj.io/completed
+           operator: In
+           values: [ "false" ]
+   ```
+
+2. Pods that belong to other API resources managed by Kueue are excluded from being queued by `pod` integration.
+   For example, pods managed by `batch/v1.Job` won't be managed by `pod` integration.
+
+4. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+## Workflow definition
+
+When creating [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/),
+[WorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/workflow-templates/),
+[ClusterWorkflowTemplate](https://argo-workflows.readthedocs.io/en/latest/cluster-workflow-templates/),
+or [CronWorkflow](https://argo-workflows.readthedocs.io/en/latest/cron-workflows/) resources for
+Kueue, take into consideration the following aspects:
+
+### a. Targeting a single LocalQueue
+
+If you want the entire workflow to target a single [local queue](/docs/concepts/local_queue),
+it should be specified in the `spec.podMetadata` section of the Workflow configuration.
+
+```yaml
+apiVersion: argoproj.io/v1alpha1
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-17T18:29:54Z

- **site/content/en/docs/tasks/run/argo_workload.md:13** by [@kannon92](https://github.com/kannon92) — 2025-01-17T18:29:54Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-17T18:30:04Z

- **site/content/en/docs/tasks/run/argo_workload.md:16** by [@kannon92](https://github.com/kannon92) — 2025-01-17T18:30:04Z
```diff
@@ -0,0 +1,167 @@
+---
+title: "Run An Argo Workflow"
+date: 2024-01-03
+weight: 7
+description: >
+  Integrate Kueue with Argo Workflows.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Argo Workflows](https://argo-workflows.readthedocs.io/en/latest/).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+As of the writing of this doc, Kueue doesn't support Argo Workflows [Workflow](https://argo-workflows.readthedocs.io/en/latest/workflow-concepts/) resources directly,
+but you can take advantage of the ability for Kueue to [manage plain pods](/docs/tasks/run_plain_pods) to integrate them.
+
+1. By default, the integration for `v1/pod` is not enabled.
```

  done.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-01-23T09:38:54Z

- **site/content/en/docs/tasks/run/pod-based-workloads/_index.md:2** by [@PBundyra](https://github.com/PBundyra) — 2025-01-23T09:38:54Z
```diff
@@ -0,0 +1,10 @@
+---
+title: "Pod Based Workloads"
```

  Maybe we could change the title to: "External integrations", or something similar? `Pod Based Workloads` can be a little misleading, since we already have `Plain Pods` section

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-23T09:50:12Z

- **site/content/en/docs/tasks/run/pod-based-workloads/_index.md:10** by [@mimowo](https://github.com/mimowo) — 2025-01-23T09:50:12Z
```diff
@@ -0,0 +1,10 @@
+---
+title: "Pod Based Workloads"
+linkTitle: "Pod-based-workloads"
+weight: 8
+date: 2025-01-17
+description: >
+  Using Pod Integration to support external integrations
+---
+
+This page explains how to use pod integration to support external workloads.
```

  ```suggestion
  This page explains how to use the [Plain Pods](https://kueue.sigs.k8s.io/docs/tasks/run/plain_pods/) integration to support external workloads.
  ```
  WDYT, to make the linking more explicit?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-23T09:50:23Z

- **site/content/en/docs/tasks/run/pod-based-workloads/_index.md:7** by [@mimowo](https://github.com/mimowo) — 2025-01-23T09:50:23Z
```diff
@@ -0,0 +1,10 @@
+---
+title: "Pod Based Workloads"
+linkTitle: "Pod-based-workloads"
+weight: 8
+date: 2025-01-17
+description: >
+  Using Pod Integration to support external integrations
```

  ```suggestion
    Using Plain Pods Integration to support external integrations
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-23T09:52:32Z

- **site/content/en/docs/tasks/run/pod-based-workloads/_index.md:2** by [@mimowo](https://github.com/mimowo) — 2025-01-23T09:52:32Z
```diff
@@ -0,0 +1,10 @@
+---
+title: "Pod Based Workloads"
```

  I agree it is worth thinking about a good name, but we already have a concept of "external integrations" / custom jobs: https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/
  So, it could be misleading too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-23T09:54:36Z

LGTM overall, please additionally extend the list of options in https://kueue.sigs.k8s.io/docs/tasks/dev/integrate_a_custom_job/ by adding a point like "Leverage the Plain Pods integration", linking to the new tasks page.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-23T10:06:29Z

- **site/content/en/docs/tasks/run/pod-based-workloads/_index.md:2** by [@mimowo](https://github.com/mimowo) — 2025-01-23T10:06:28Z
```diff
@@ -0,0 +1,10 @@
+---
+title: "Pod Based Workloads"
```

  OTOH, now thinking more about this, Plain Pods is one of the mechanisms for supporting external frameworks, as suggested here: https://github.com/kubernetes-sigs/kueue/pull/3897#pullrequestreview-2569382461. 
  
  So, I'm ok with the more generic section like "External integrations", then it should also probably group https://kueue.sigs.k8s.io/docs/tasks/run/flux_miniclusters/ and https://kueue.sigs.k8s.io/docs/tasks/run/wrapped_custom_workload/, and refactor the structure. WDYT @PBundyra @kannon92 ?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-23T15:00:04Z

- **site/content/en/docs/tasks/run/pod-based-workloads/_index.md:2** by [@kannon92](https://github.com/kannon92) — 2025-01-23T15:00:03Z
```diff
@@ -0,0 +1,10 @@
+---
+title: "Pod Based Workloads"
```

  SGTM.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-23T20:02:58Z

- **site/content/en/docs/tasks/run/external_workloads/index.md:15** by [@kannon92](https://github.com/kannon92) — 2025-01-23T20:02:57Z
```diff
@@ -0,0 +1,23 @@
+---
+
+title: "Supporting External Frameworks"
+linkTitle: "External Frameworks"
+weight: 6
+date: 2025-01-23
+description: >
+  How to run Kueue with external frameworks
+no_list: true
+---
+
+The tasks below show you how to build a custom integration.
+You can use AppWrapper, job-based workloads and pod-based workloads.
+
+### [AppWrapper](https://project-codeflare.github.io/appwrapper/) Integration
```

  These links don't seem to resolve correctly.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-23T20:25:09Z

- **site/content/en/docs/tasks/run/external_workloads/index.md:15** by [@kannon92](https://github.com/kannon92) — 2025-01-23T20:25:09Z
```diff
@@ -0,0 +1,23 @@
+---
+
+title: "Supporting External Frameworks"
+linkTitle: "External Frameworks"
+weight: 6
+date: 2025-01-23
+description: >
+  How to run Kueue with external frameworks
+no_list: true
+---
+
+The tasks below show you how to build a custom integration.
+You can use AppWrapper, job-based workloads and pod-based workloads.
+
+### [AppWrapper](https://project-codeflare.github.io/appwrapper/) Integration
```

  _index.md is super important apparently.
  
  I had index.md and none of the links would resolve. Once I changed it to _index.md everything resolved correctly.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-28T07:36:35Z

Actually, I would like to consider the following sturcture:
```
Run Workloads:
- Kubernetes Jobs
- Kubernetes CronJobs
- AppWrappers
- RayClusters
- RayJobs
- Deployment
- StatefulSet
- Plain Pods
- Kubeflow Jobs
- Python
- Jobsets
- External Frameworks
  * Using AppWrapper (renamed from Custom Workload)
  * Flux MiniClusters
  * Argo Workflows 
- Multi-Cluster
```
I don't think we need more structure under "External frameworks" at this point, because there are only three items underneath, and extra layers mean more clicking to expand.

- **site/content/en/docs/tasks/_index.md:66** by [@mimowo](https://github.com/mimowo) — 2025-01-28T07:19:26Z
```diff
@@ -63,6 +63,7 @@ As a platform developer, you can learn how to:
 - [Integrate a custom Job with Kueue](dev/integrate_a_custom_job).
 - [Enable pprof endpoints](dev/enabling_pprof_endpoints).
 - [Develop a custom AdmissionCheck Controller](dev/develop-acc).
+- [Integration a custom workload with existing frameworks](dev/external_frameworks)
```

  ```suggestion
  - [Integrate a custom workload with Kueue using built-in frameworks](dev/external_frameworks)
  ```
  Also, move it up just below "Integrate a custom Job with Kueue.

- **site/content/en/docs/tasks/_index.md:46** by [@mimowo](https://github.com/mimowo) — 2025-01-28T07:19:38Z
```diff
@@ -35,16 +35,16 @@ batch user is a researcher, AI/ML engineer, data scientist, among others.
 
 As a batch user, you can learn how to:
 - [Run a Kueue managed batch/Job](run/jobs).
-- [Run a Kueue managed Flux MiniCluster](run/flux_miniclusters).
 - [Run a Kueue managed Kubeflow Job](run/kubeflow).
   Kueue supports MPIJob v2beta1, PyTorchJob, TFJob, XGBoostJob, PaddleJob, and MXJob.
 - [Run a Kueue managed KubeRay RayJob](run/rayjobs).
 - [Run a Kueue managed KubeRay RayCluster](run/rayclusters).
-- [Run a Kueue managed AppWrapper](run/appwrappers).
 - [Submit Kueue jobs from Python](run/python_jobs).
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 - [Submit jobs to MultiKueue](run/multikueue).
+- [Support external workloads](run/external_workloads).
```

  ```suggestion
  - [Run external workloads](run/external_workloads).
  ```
  nit, for consistency

- **site/content/en/docs/tasks/_index.md:47** by [@mimowo](https://github.com/mimowo) — 2025-01-28T07:20:16Z
```diff
@@ -35,16 +35,16 @@ batch user is a researcher, AI/ML engineer, data scientist, among others.
 
 As a batch user, you can learn how to:
 - [Run a Kueue managed batch/Job](run/jobs).
-- [Run a Kueue managed Flux MiniCluster](run/flux_miniclusters).
 - [Run a Kueue managed Kubeflow Job](run/kubeflow).
   Kueue supports MPIJob v2beta1, PyTorchJob, TFJob, XGBoostJob, PaddleJob, and MXJob.
 - [Run a Kueue managed KubeRay RayJob](run/rayjobs).
 - [Run a Kueue managed KubeRay RayCluster](run/rayclusters).
-- [Run a Kueue managed AppWrapper](run/appwrappers).
 - [Submit Kueue jobs from Python](run/python_jobs).
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 - [Submit jobs to MultiKueue](run/multikueue).
+- [Support external workloads](run/external_workloads).
+  Kueue allows one to use existing integrations to support external workloads.
```

  ```suggestion
    Kueue allows one to use built-in integrations (such as Pods or Jobs) to run external workloads.
  ```

- **site/content/en/docs/tasks/run/external_workloads/_index.md:19** by [@mimowo](https://github.com/mimowo) — 2025-01-28T07:22:37Z
```diff
@@ -0,0 +1,23 @@
+---
+
+title: "Supporting External Frameworks"
+linkTitle: "External Frameworks"
+weight: 6
+date: 2025-01-23
+description: >
+  How to run Kueue with external frameworks
+no_list: true
+---
+
+The tasks below show you how to build a custom integration.
+You can use AppWrapper, job-based workloads and pod-based workloads.
+
+### [AppWrapper](https://project-codeflare.github.io/appwrapper/) Integration
+- [Run a workload using Appwrappers](/docs/tasks/run/external_workloads/appwrappers).
+- [Run a custom workload using Appwrappers](/docs/tasks/run/external_workloads/wrapped_custom_workload).
+
+### Job-based Workloads Integration
+- [Run a Flux Miniclusters using job integration](/docs/tasks/run/external_workloads/flux_miniclusters).
+
+### Pod-Based Workloads Integration
+- [Run an Argo Workflow using pod integration](/docs/tasks/run/external_workloads/pod_based_workloads/argo_workflow).
```

  I would suggest to combine the two, because conceptually this is the same mechanism:
  ```suggestion
  ### Integrations based on built-in frameworks
  - [Run a Flux Miniclusters using job integration](/docs/tasks/run/external_workloads/flux_miniclusters).
  - [Run an Argo Workflow using pod integration](/docs/tasks/run/external_workloads/pod_based_workloads/argo_workflow).
  ```

- **site/content/en/docs/tasks/run/external_workloads/appwrappers.md:3** by [@mimowo](https://github.com/mimowo) — 2025-01-28T07:28:54Z
```diff
@@ -1,6 +1,5 @@
 ---
 title: "Run An AppWrapper"
-linkTitle: "AppWrappers"
```

  No need to remove. Let's also keep AppWrapper where it was as it is a built-in framework currently. We have another page for showing how to use AppWrapper for custom Jobs.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-01-28T14:12:12Z

- **site/content/en/docs/tasks/run/external_workloads/appwrappers.md:3** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-01-28T14:12:12Z
```diff
@@ -1,6 +1,5 @@
 ---
 title: "Run An AppWrapper"
-linkTitle: "AppWrappers"
```

  > No need to remove. Let's also keep AppWrapper where it was as it is a built-in framework currently. We have another page for showing how to use AppWrapper for custom Jobs.
  
  I think this makes sense.  
  
  Once Kueue 0.11 comes out, we plan to simplify the AppWrapper controller, remove the code that is no longer needed since it is now a built-in framework, and make a new release of AppWrapper. As part of doing that, I will revise the Kueue documention and make sure it refers to the right historic pieces of external-framework code in the AppWrapper 1.0 git.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-28T18:28:41Z

- **site/content/en/docs/tasks/_index.md:46** by [@kannon92](https://github.com/kannon92) — 2025-01-28T18:28:40Z
```diff
@@ -35,16 +35,16 @@ batch user is a researcher, AI/ML engineer, data scientist, among others.
 
 As a batch user, you can learn how to:
 - [Run a Kueue managed batch/Job](run/jobs).
-- [Run a Kueue managed Flux MiniCluster](run/flux_miniclusters).
 - [Run a Kueue managed Kubeflow Job](run/kubeflow).
   Kueue supports MPIJob v2beta1, PyTorchJob, TFJob, XGBoostJob, PaddleJob, and MXJob.
 - [Run a Kueue managed KubeRay RayJob](run/rayjobs).
 - [Run a Kueue managed KubeRay RayCluster](run/rayclusters).
-- [Run a Kueue managed AppWrapper](run/appwrappers).
 - [Submit Kueue jobs from Python](run/python_jobs).
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 - [Submit jobs to MultiKueue](run/multikueue).
+- [Support external workloads](run/external_workloads).
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-28T19:37:22Z

- **site/content/en/docs/tasks/run/external_workloads/_index.md:19** by [@kannon92](https://github.com/kannon92) — 2025-01-28T19:37:22Z
```diff
@@ -0,0 +1,23 @@
+---
+
+title: "Supporting External Frameworks"
+linkTitle: "External Frameworks"
+weight: 6
+date: 2025-01-23
+description: >
+  How to run Kueue with external frameworks
+no_list: true
+---
+
+The tasks below show you how to build a custom integration.
+You can use AppWrapper, job-based workloads and pod-based workloads.
+
+### [AppWrapper](https://project-codeflare.github.io/appwrapper/) Integration
+- [Run a workload using Appwrappers](/docs/tasks/run/external_workloads/appwrappers).
+- [Run a custom workload using Appwrappers](/docs/tasks/run/external_workloads/wrapped_custom_workload).
+
+### Job-based Workloads Integration
+- [Run a Flux Miniclusters using job integration](/docs/tasks/run/external_workloads/flux_miniclusters).
+
+### Pod-Based Workloads Integration
+- [Run an Argo Workflow using pod integration](/docs/tasks/run/external_workloads/pod_based_workloads/argo_workflow).
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-28T19:37:28Z

- **site/content/en/docs/tasks/_index.md:47** by [@kannon92](https://github.com/kannon92) — 2025-01-28T19:37:28Z
```diff
@@ -35,16 +35,16 @@ batch user is a researcher, AI/ML engineer, data scientist, among others.
 
 As a batch user, you can learn how to:
 - [Run a Kueue managed batch/Job](run/jobs).
-- [Run a Kueue managed Flux MiniCluster](run/flux_miniclusters).
 - [Run a Kueue managed Kubeflow Job](run/kubeflow).
   Kueue supports MPIJob v2beta1, PyTorchJob, TFJob, XGBoostJob, PaddleJob, and MXJob.
 - [Run a Kueue managed KubeRay RayJob](run/rayjobs).
 - [Run a Kueue managed KubeRay RayCluster](run/rayclusters).
-- [Run a Kueue managed AppWrapper](run/appwrappers).
 - [Submit Kueue jobs from Python](run/python_jobs).
 - [Run a Kueue managed plain Pod](run/plain_pods).
 - [Run a Kueue managed JobSet](run/jobsets).
 - [Submit jobs to MultiKueue](run/multikueue).
+- [Support external workloads](run/external_workloads).
+  Kueue allows one to use existing integrations to support external workloads.
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-28T19:37:34Z

- **site/content/en/docs/tasks/_index.md:66** by [@kannon92](https://github.com/kannon92) — 2025-01-28T19:37:34Z
```diff
@@ -63,6 +63,7 @@ As a platform developer, you can learn how to:
 - [Integrate a custom Job with Kueue](dev/integrate_a_custom_job).
 - [Enable pprof endpoints](dev/enabling_pprof_endpoints).
 - [Develop a custom AdmissionCheck Controller](dev/develop-acc).
+- [Integration a custom workload with existing frameworks](dev/external_frameworks)
```

  done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-29T07:45:48Z

/lgtm
/approve
Feel free to address the nit comment in a follow up.

- **site/content/en/docs/tasks/run/external_workloads/argo_workflow.md:2** by [@mimowo](https://github.com/mimowo) — 2025-01-29T07:43:38Z
```diff
@@ -0,0 +1,51 @@
+---
+title: "Run An Argo Workflow"
```

  nit: add `linkTitle: Argo Workflow` to make the display consistent.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-01-29T14:33:39Z

- **site/content/en/docs/tasks/run/external_workloads/appwrappers.md:3** by [@kannon92](https://github.com/kannon92) — 2025-01-29T14:33:39Z
```diff
@@ -1,6 +1,5 @@
 ---
 title: "Run An AppWrapper"
-linkTitle: "AppWrappers"
```

  updated.
