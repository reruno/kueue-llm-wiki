# PR #3912: Add RemoveFinalizersWithStrictPatch feature gate

**Summary**: Add RemoveFinalizersWithStrictPatch feature gate

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/3912

**Last updated**: 2026-01-30T12:13:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `fix/concurrent-patch` → `main`
- **Created**: 2024-12-27T13:44:32Z
- **Updated**: 2026-01-30T12:13:40Z
- **Merged**: 2026-01-30T10:33:44Z
- **Closed**: 2026-01-30T10:33:44Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 6   **+125 / -30**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a RemoveFinalizersWithStrictPatch feature gate to ensure that the finalizer deletion does not result in a race condition removing simultaneous finalizer updates.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3899

#### Special notes for your reviewer:
Manual performance testing on a scale of 10000 pods was conducted using https://gist.github.com/mykysha/bd8ed4ae3609e67a1542557628f1b0c1
Results are the following:
Feature-gate disabled:
Total pod test runtime - 1h27m16s
Reconciliation time - 2h31m26s
Number of "lost" finalizer removals - 7297/10000

Feature-gate enabled:
Total pod test runtime - 1h27m6s
Reconciliation time - 3h8m38s

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
PodIntegration: Fix the bug that Kueue would occasionally remove the custom finalizers when
removing the `kueue.x-k8s.io/managed` finalizer.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-27T13:44:34Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-12-27T13:44:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 30421d1c37ad41ce2c5757e02f5c9f081634e995 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/697c8116c7a0890007162f20 |

### Comment by [@mykysha](https://github.com/mykysha) — 2024-12-27T13:45:36Z

/test all

### Comment by [@mykysha](https://github.com/mykysha) — 2024-12-27T13:45:44Z

/cc @mbobrovskyi

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-12-27T13:47:49Z

LGTM label has been added.  <details>Git tree hash: 8952d4f81acb656dc70e2e9ee998c643ab0a4228</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-12-27T13:50:03Z

cc: @troychiu

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-07T11:23:08Z

> AFAIK the nonstrict patch is the intended behavior.

I'm not sure about that actually, dropping finalizers successfully added by other controllers is not ideal.

> Could you add test cases to prove the situation where the nonstrict patch removes non-owned finalizers (kueue.x-k8s. io/managed) from the Pod?

Race conditions like this might be tricky to test. How would you imagine the test? e2e test which repeats the operation multiple times, or rather a unit test?

> Additionally, what is the reason for enforcing the strict patch everywhere?

RemoveFinalizer was the only use of non-strict mode, so it makes sense to drop the flag if no other place wants to use it.

However, I think there is some risk to use strict patches - it is the possible performance implication due to necessary re-tries. I don't expect it, because we anyway manage scheduling gates, and it wasn't a problem. However, for safety I would suggest to add a feature gate `RemoveFinalizersWithStrictPatch` which is beta and will graduate to GA in the future, but it will give us a safety mechanism for users who use Kueue at scale and the change could impact them. WDYT @tenzen-y ?

### Comment by [@trasc](https://github.com/trasc) — 2025-01-14T11:55:33Z

/uncc

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-01-16T06:30:30Z

> > AFAIK the nonstrict patch is the intended behavior.
> 
> I'm not sure about that actually, dropping finalizers successfully added by other controllers is not ideal.
> 
> > Could you add test cases to prove the situation where the nonstrict patch removes non-owned finalizers (kueue.x-k8s. io/managed) from the Pod?
> 
> Race conditions like this might be tricky to test. How would you imagine the test? e2e test which repeats the operation multiple times, or rather a unit test?
> 
> > Additionally, what is the reason for enforcing the strict patch everywhere?
> 
> RemoveFinalizer was the only use of non-strict mode, so it makes sense to drop the flag if no other place wants to use it.
> 
> However, I think there is some risk to use strict patches - it is the possible performance implication due to necessary re-tries. I don't expect it, because we anyway manage scheduling gates, and it wasn't a problem. However, for safety I would suggest to add a feature gate `RemoveFinalizersWithStrictPatch` which is beta and will graduate to GA in the future, but it will give us a safety mechanism for users who use Kueue at scale and the change could impact them. WDYT @tenzen-y ?

Yes, performance was my primary concern due to requeue objects to reconcilers.
So, guarding the strict patch by the `RemoveFinalizersWithStrictPatch` would be worth it.
Additionally, I would propose adding [the kueue performance testings](https://github.com/kubernetes-sigs/kueue/tree/main/test/performance) metrics to the feature graduation criteria. For example, we want to restrict the graduation like If the performance metrics are not decreased by 5% or something else, we can graduate it to GA.

@mimowo WDYT?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-16T09:17:40Z

> For example, we want to restrict the graduation like If the performance metrics are not decreased by 5% or something else, we can graduate it to GA.

Sounds reasonable, I'm just wondering how much insight we get by artificial testing, but the scale requirement wouldn't be clear to me. Some users will use it at a large scale, and not sure how small scale testing will be representative. I suppose we would need to have at least 10000 pods.  WDYT @tenzen-y @mykysha ?

Alternative (or along with that) we could enable it by default and wait 3 releases with graduation for the feedback from users.

### Comment by [@mykysha](https://github.com/mykysha) — 2025-01-16T13:28:43Z

Both approaches sound good to me. I believe the user-feedback approach would be more informative, however making some performance metering alongside definitely wouldn't hurt either

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-16T13:46:57Z

I think we will not need a KEP process for that, but let me check if @tenzen-y is ok with the plan:
* introduce the feature-gate in Beta, and add TODO with the graduation criteria to stable with a link to the issue "Performance testing for the impact of RemoveFinalizersWithStrictPatch". In the issue propose to test it manually.
* also add a comment to graduate in 0.13 or later to await for the user feedback while the gate is enabled

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-01-16T15:24:39Z

> Some users will use it at a large scale, and not sure how small scale testing will be representative. I suppose we would need to have at least 10000 pods. WDYT @tenzen-y @mykysha ?

That makes sense. We should verify the performance issue in the large env to represent issue obviously.
So, if it's challenging to simulate the situation by our performance testings, I guess that we can simulate it by other tools like KWOK manually.

> I think we will not need a KEP process for that, but let me check if @tenzen-y is ok with the plan:
> 
> introduce the feature-gate in Beta, and add TODO with the graduation criteria to stable with a link to the issue "Performance testing for the impact of RemoveFinalizersWithStrictPatch". In the issue propose to test it manually.
> also add a comment to graduate in 0.13 or later to await for the user feedback while the gate is enabled

I'm ok without KEP. But at least, could we summarize graduation criteria and background in the dedicated issue?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-17T18:02:42Z

> So, if it's challenging to simulate the situation by our performance testings, I guess that we can simulate it by other tools like KWOK manually.

Right, I think it might be tricky for automated testing, and will consume our build time. So, I'm leaning to just a manual experiment, either in a real cluster k8s or kwok. I think the scale of 10000 pods would be enough, they don't need to be running at the same time. Please also confirm how many of the errors we got in that case.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-17T18:03:52Z

> But at least, could we summarize graduation criteria and background in the dedicated issue?

SGTM, We can add TODO(# issue number) comment. @mykysha please open the issue and update the PR accordingly.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-14T10:56:40Z

@mykysha Do you have enough bandwidth to address our comments?

### Comment by [@mykysha](https://github.com/mykysha) — 2025-02-14T10:58:16Z

Working on this currently, running performance tests on Kwok

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-14T10:59:12Z

> Working on this currently, running performance tests on Kwok

Thank you!

### Comment by [@mykysha](https://github.com/mykysha) — 2025-02-26T14:21:06Z

/hold for #4416

### Comment by [@mykysha](https://github.com/mykysha) — 2025-03-06T10:44:45Z

@mimowo @tenzen-y Based on the performance testing on a real k8s cluster with 10000 pods, the performance hit with the feature-gate enabled is not as significant as the number of failures when it is disabled.
With the feature-gate enabled: took 41m57s
With the feature-gate disabled: took 37m52s and resulted in 3804 failures

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-06T11:24:27Z

Thank you for running the experiment!

Ok, but there is a trade off clearly at 10% as 2272s / 2517s = 0.9.

Maybe a more relevant metric of the hit averaged per pod: (2517s-2272s)/10000 = 25ms.

So, on average this is 25ms added to the lifetime of a pod.

I see two strategies:
1. (as planned) introduce the feature gate as a bailout option and change the default. If no-one complains for a couple of releases we graduate
2. introduce a config change

TBH I'm leaning to (1.) for simplicity, as the config switch would be irrelevant to most admins. 

Any other view @tenzen-y @dgrove-oss ?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-06T11:29:24Z

Some more questions on the experiment setup to help us better assess it:

- do you wait for all pods to be running start to delete all 10k pods at the same time?
- do you measure full experiment or just the delete phase?
- do you inert the "custom finalizers"? 
- by "resulted in 3804 failures" do you mean that the custom finalizer was deleted by Kueue?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-06T11:36:35Z

> @mimowo @tenzen-y Based on the performance testing on a real k8s cluster with 10000 pods, the performance hit with the feature-gate enabled is not as significant as the number of failures when it is disabled. With the feature-gate enabled: took 41m57s With the feature-gate disabled: took 37m52s and resulted in 3804 failures

Thank you for sharing it.
What about increasing the number of Pod reconcilers to 10 or 20 (default is 5): https://github.com/kubernetes-sigs/kueue/blob/92e194db4f47e54bddd05149175ef9052ac96c2d/config/components/manager/controller_manager_config.yaml#L16

If we can mitigate this performance disadvantage by this, it would be great.

### Comment by [@mykysha](https://github.com/mykysha) — 2025-03-06T13:10:01Z

> * do you wait for all pods to be running start to delete all 10k pods at the same time?

I run 100 of cases simultaneously until a total number of 10000 is reached.

> * do you measure full experiment or just the delete phase?

It measures the time between the pod creation until we get a result, complete pod deletion is untracked

> * do you inert the "custom finalizers"?

Test logic is basically the following: create pod - set custom finalizer - wait for pod status completed - remove custom finalizer - wait for kueue-managed finalizer to be deleted - check whether the custom finalizer was removed or this change was overwritten by the reconciler

> * by "resulted in 3804 failures" do you mean that the custom finalizer was deleted by Kueue?

It is the number of cases where our custom finalizer was not deleted, requiring manual interference in order for the pod to be deleted

### Comment by [@mykysha](https://github.com/mykysha) — 2025-03-07T15:42:42Z

@mimowo @tenzen-y Have modified tests a bit by increasing the number of concurrent kueue reconciliations to 20, as well as have added a second time metric calculating the time it took for the finalizer changes to be reconciled after the pod has finished running. These changes have resulted in some interested results:

Feature-gate disabled:
Total pod test runtime - 1h27m16s
Reconciliation time - 2h31m26s
Number of "lost" finalizer removals - 7297/10000

Feature-gate enabled:
Total pod test runtime - 1h27m6s
Reconciliation time - 3h8m38s

Reconciliation time was calculated by every runner and then summed up, so it is larger than the test run itself. Increased total runtime might be attributed to the system throttling with the amount of operations, going to look into running the experiment externally.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-07T16:23:03Z

@mykysha thank you for the experiments. I think this is valuable, but I'm still not 100% sure I understand what is measured. Would you mind sharing the code of the experiment code? For example as a github gist.

EDIT: and attach it to PR description. Ideal if with some comments or annotations.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-07T18:12:35Z

> @mykysha thank you for the experiments. I think this is valuable, but I'm still not 100% sure I understand what is measured. Would you mind sharing the code of the experiment code? For example as a github gist.
> 
> EDIT: and attach it to PR description. Ideal if with some comments or annotations.

I am also not sure what is measured by your tests. So, I also want to see your performance measuring codes.

### Comment by [@mykysha](https://github.com/mykysha) — 2025-03-10T10:15:58Z

@mimowo @tenzen-y have updated the description with the link to the test gist as well as the results

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-12T10:54:46Z

> @mimowo @tenzen-y have updated the description with the link to the test gist as well as the results

Thank you for the summary. @troychiu as the issue author could you please review if the problem reflected in the test is what you are hitting. Also, let us know if this is a blocker for you, or you have a workaround.

I will try to review before release, but not sure, so such hints will help us to prioritize.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-27T10:07:21Z

/lgtm
Awesome! Thank you so much!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-27T10:07:27Z

LGTM label has been added.  <details>Git tree hash: da12494a964647ef472859ce866842a86c5a58d0</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-27T10:08:51Z

> /hold for #4416

/unhold

Due to #4416 was merged.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-27T10:12:54Z

@mimowo @tenzen-y PTAL

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T12:54:42Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-09T12:54:48Z

LGTM label has been added.  <details>Git tree hash: 157589c0dcfb7a66238b56383cc54dbcb99eeff0</details>

### Comment by [@troychiu](https://github.com/troychiu) — 2025-04-09T17:27:21Z

@mimowo sorry for the late reply. This looks good to me. Thank you so much.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-11T07:34:38Z

Thanks for the feedback. I will try to return to the KEP and PR before the 0.12 release.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-10T11:45:31Z

@mimowo @tenzen-y PTAL

### Comment by [@mykysha](https://github.com/mykysha) — 2025-06-10T11:52:21Z

Changed the milestone version to 0.13

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-03T08:42:44Z

@mimowo @tenzen-y PTAL

### Comment by [@mykysha](https://github.com/mykysha) — 2025-07-29T07:28:39Z

Changed the milestone version to 0.14

### Comment by [@mykysha](https://github.com/mykysha) — 2025-09-29T11:18:44Z

Strict patch was enabled by default in #6792 , hence no need for this PR.
/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T11:18:50Z

@mykysha: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3912#issuecomment-3346390503):

>Strict patch was enabled by default in #6792 , hence no need for this PR.
>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mykysha](https://github.com/mykysha) — 2025-09-29T11:21:08Z

/reopen
Based on the closer inspection, there may be the need for this, will rework the PR now to reflect the changes

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-29T11:21:15Z

@mykysha: Reopened this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/3912#issuecomment-3346399924):

>/reopen
>Based on the closer inspection, there may be the need for this, will rework the PR now to reflect the changes


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-12-16T17:25:38Z

/cc @mimowo

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T08:32:54Z

/cc @mimowo

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-12T08:33:06Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-28T15:49:38Z

The PR looks mostly sane, I will include in planning for 0.17

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-28T15:50:06Z

@mykysha  please rebase

### Comment by [@pajakd](https://github.com/pajakd) — 2026-01-29T08:04:54Z

the release-note says "Add RemoveFinalizersWithStrictMode feature gate" but the feature gate is called "RemoveFinalizersWithStrictPatch"

### Comment by [@mykysha](https://github.com/mykysha) — 2026-01-29T12:54:19Z

I also want to rerun the experiment that measures the time impact, will share the results afterwards

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-29T12:56:06Z

LGTM label has been added.  <details>Git tree hash: 9cdec1cbb1bb6b72b50e96491c4e6b372ca87c4b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-29T12:56:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/3912#pullrequestreview-2524085689" title="LGTM">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3912#pullrequestreview-3722533583" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/3912#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mykysha](https://github.com/mykysha) — 2026-01-30T09:22:07Z

/retest-required
Looks like another instance of #8809

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-30T09:29:10Z

Thank you so much!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-30T09:29:17Z

LGTM label has been added.  <details>Git tree hash: 2ccc4430e9f24e2b60abc9633bee64b3d7dc3d91</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-30T09:29:29Z

/unhold

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-30T10:05:22Z

Thank you!

/lgtm
/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-30T10:05:29Z

LGTM label has been added.  <details>Git tree hash: d03291d593da75d33bc3a61691ba60ba03e46609</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-01-30T10:08:10Z

@mykysha: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-multikueue-integration-main | 7589f1427336fc3c33dd8b9b1a60d7b61032e1dd | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/3912/pull-kueue-test-multikueue-integration-main/1883921195963781120) | true | `/test pull-kueue-test-multikueue-integration-main`
pull-kueue-test-customconfigs-e2e-main | 7589f1427336fc3c33dd8b9b1a60d7b61032e1dd | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/3912/pull-kueue-test-customconfigs-e2e-main/1889321398363492352) | true | `/test pull-kueue-test-customconfigs-e2e-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=3912). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Amykysha). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-30T10:12:40Z

/test pull-kueue-test-integration-extended-main

Due to know issue

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-30T10:48:02Z

I think it is handle this as a bugfix. The issue was raised as a bug, and indeed removal of "custom" finalizers from Pod is bad. We have a feature gate so it should be safe.

/release-note-edit
```release-note
PodIntegration: Fix the bug that Kueue would occasionally remove the custom finalizers when
removing the `kueue.x-k8s.io/managed` finalizer. 
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-30T10:49:06Z

@mykysha could you please prepare the cherrypicks?

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2024-12-27T13:47:43Z

/lgtm
Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-12-28T19:22:16Z

AFAIK the nonstrict patch is the intended behavior.
Could you add test cases to prove the situation where the nonstrict patch removes non-owned finalizers (`kueue.x-k8s. io/managed`) from the Pod?

Additionally, what is the reason for enforcing the strict patch everywhere?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-25T13:19:36Z

Almost LGTM

- **pkg/features/kube_features.go:262** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-25T13:06:34Z
```diff
@@ -252,6 +258,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	TASProfileMixed: {
 		{Version: version.MustParse("0.11"), Default: false, PreRelease: featuregate.Deprecated},
 	},
+	RemoveFinalizersWithStrictPatch: {
+		{Version: version.MustParse("0.11"), Default: false, PreRelease: featuregate.Beta},
```

  ```suggestion
  		{Version: version.MustParse("0.12"), Default: false, PreRelease: featuregate.Beta},
  ```

- **pkg/features/kube_features.go:262** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-25T13:07:57Z
```diff
@@ -252,6 +258,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	TASProfileMixed: {
 		{Version: version.MustParse("0.11"), Default: false, PreRelease: featuregate.Deprecated},
 	},
+	RemoveFinalizersWithStrictPatch: {
+		{Version: version.MustParse("0.11"), Default: false, PreRelease: featuregate.Beta},
```

  Please also update [Feature gates for alpha and beta features](https://github.com/kubernetes-sigs/kueue/blob/b6793cffac91b081a7728985b255ef847fc53525/site/content/en/docs/installation/_index.md?plain=1#L262) and KEP.

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:326** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-25T13:16:33Z
```diff
@@ -259,6 +259,80 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.RemoveFinalizersWithStrictPatch, true)
+
+				pod := testingpod.MakePod(podName, ns.Name).Queue("test-queue").Obj()
+				gomega.Expect(k8sClient.Create(ctx, pod)).Should(gomega.Succeed())
+
+				createdPod := &corev1.Pod{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, lookupKey, createdPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Expect(createdPod.Finalizers).
+					To(gomega.ContainElement(constants.ManagedByKueueLabelKey), "Pod should have finalizer")
+
+				ginkgo.By("checking that workload is created for pod with the queue name")
+				createdWorkload := &kueue.Workload{}
+				wlLookupKey := types.NamespacedName{
+					Name:      podcontroller.GetWorkloadNameForPod(pod.Name, pod.UID),
+					Namespace: ns.Name,
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("checking the pod is unsuspended when workload is assigned")
+				clusterQueue := testing.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj(),
+					).Obj()
+				admission := testing.MakeAdmission(clusterQueue.Name).
+					Assignment(corev1.ResourceCPU, "default", "1").
+					AssignmentPodCount(createdWorkload.Spec.PodSets[0].Count).
+					Obj()
+				gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).
+					Should(gomega.Succeed())
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, lookupKey, createdPod)).Should(gomega.Succeed())
+					g.Expect(createdPod.Spec.SchedulingGates).Should(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				customFinalizer := "my.custom/finalizer"
+
+				updatedPod := pod.DeepCopy()
+
+				updatedPod.Finalizers = append(updatedPod.Finalizers, customFinalizer)
+
+				util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, pod)
+				gomega.Expect(k8sClient.Patch(ctx, updatedPod, client.MergeFrom(pod))).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+
+					var finished bool
+
+					for _, cond := range createdWorkload.Status.Conditions {
+						if cond.Type == kueue.WorkloadFinished && cond.Status == metav1.ConditionTrue {
+							finished = true
+
+							break
+						}
+					}
+
+					g.Expect(finished).Should(gomega.BeTrue())
```

  ```suggestion
  
  					g.Expect(createdWorkload.Status.Conditions).Should(testing.HaveConditionStatusTrue(kueue.WorkloadFinished))
  ```

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-25T13:17:43Z
  nit: 
  
  Please add more ginkgo.By() to explain steps.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-03-27T09:26:10Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:1** by [@mykysha](https://github.com/mykysha) — 2025-03-27T09:26:10Z
  Great idea, done!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-27T09:33:15Z

- **site/content/en/docs/installation/_index.md:290** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-27T09:32:25Z
```diff
@@ -287,6 +287,7 @@ spec:
 | `ManagedJobsNamespaceSelector`        | `true`  | Beta       | 0.10  |       |
 | `LocalQueueDefaulting`                | `false` | Alpha      | 0.10  |       |
 | `LocalQueueMetrics`                   | `false` | Alpha      | 0.10  |       |
+| `RemoveFinalizersWithStrictPatch`     | `false` | Beta       | 0.12  |       |
```

  ```suggestion
  | `RemoveFinalizersWithStrictPatch`     | `true` | Beta       | 0.12  |       |
  ```
  I think it should be true by default for Beta.

- **pkg/features/kube_features.go:262** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-27T09:32:58Z
```diff
@@ -252,6 +258,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	TASProfileMixed: {
 		{Version: version.MustParse("0.11"), Default: false, PreRelease: featuregate.Deprecated},
 	},
+	RemoveFinalizersWithStrictPatch: {
+		{Version: version.MustParse("0.12"), Default: false, PreRelease: featuregate.Beta},
```

  ```suggestion
  		{Version: version.MustParse("0.12"), Default: true, PreRelease: featuregate.Beta},
  ```
  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-27T09:58:25Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:263** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-27T09:58:25Z
```diff
@@ -259,6 +259,72 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.RemoveFinalizersWithStrictPatch, true)
```

  ```suggestion
  ```
  I think this is not needed. It should be true by default.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-03-27T10:01:36Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:263** by [@mykysha](https://github.com/mykysha) — 2025-03-27T10:01:36Z
```diff
@@ -259,6 +259,72 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.RemoveFinalizersWithStrictPatch, true)
```

  You are right, removed

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-01-29T05:01:30Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:315** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-01-29T05:01:30Z
```diff
@@ -263,6 +263,71 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Label("job:pod", "area:jobs"),
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
+				pod := testingpod.MakePod(podName, ns.Name).Queue("test-queue").Obj()
+				gomega.Expect(k8sClient.Create(ctx, pod)).Should(gomega.Succeed())
+
+				createdPod := &corev1.Pod{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, lookupKey, createdPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Expect(createdPod.Finalizers).
+					To(gomega.ContainElement(constants.ManagedByKueueLabelKey), "Pod should have finalizer")
+
+				ginkgo.By("checking that workload is created for pod with the queue name")
+				createdWorkload := &kueue.Workload{}
+				wlLookupKey := types.NamespacedName{
+					Name:      podcontroller.GetWorkloadNameForPod(pod.Name, pod.UID),
+					Namespace: ns.Name,
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("checking the pod is unsuspended when workload is assigned")
+				clusterQueue := utiltestingapi.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj(),
+					).Obj()
+				admission := utiltestingapi.MakeAdmission(clusterQueue.Name).
+					PodSets(utiltestingapi.MakePodSetAssignment(kueue.DefaultPodSetName).
+						Assignment(corev1.ResourceCPU, "default", "1").
+						Count(createdWorkload.Spec.PodSets[0].Count).
+						Obj(),
+					).Obj()
+				util.SetQuotaReservation(ctx, k8sClient, wlLookupKey, admission)
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, lookupKey, createdPod)).Should(gomega.Succeed())
+					g.Expect(createdPod.Spec.SchedulingGates).Should(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("adding custom finalizer to the pod and setting the pod as succeeded")
+				customFinalizer := "my.custom/finalizer"
+
+				updatedPod := pod.DeepCopy()
+
+				updatedPod.Finalizers = append(updatedPod.Finalizers, customFinalizer)
+
+				util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, pod)
+				gomega.Expect(k8sClient.Patch(ctx, updatedPod, client.MergeFrom(pod))).Should(gomega.Succeed())
```

  Shouldn't it be refetched to get prev Finalizers?
  
  ```suggestion
  				// Re-fetch current state before patching
  				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdPod)).Should(gomega.Succeed())
  				
  				updatedPod := createdPod.DeepCopy()
  				updatedPod.Finalizers = append(updatedPod.Finalizers, customFinalizer)
  				
  				gomega.Expect(k8sClient.Patch(ctx, updatedPod, client.MergeFrom(createdPod))).Should(gomega.Succeed())
  
  util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, updatedPod)
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-01-29T08:20:48Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:266** by [@pajakd](https://github.com/pajakd) — 2026-01-29T08:20:48Z
```diff
@@ -263,6 +263,71 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Label("job:pod", "area:jobs"),
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
```

  nit: Perhaps call this test "Should preserve custom finalizers when workload terminates" to describe in which situation the custom finalizers were lost. And add a comment saying that without RemoveFinalizersWithStrictPatch feature gate this test would be flaky.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T08:56:05Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T08:56:05Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  ```suggestion
  		patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
  ```
  I'm wondering if we can use clientutil.WithRetryOnConflict().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T08:57:11Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T08:57:11Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  Probably on that case we don't need feature gate at all.
  
  @mimowo WDYT?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T09:14:24Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mykysha](https://github.com/mykysha) — 2026-01-29T09:14:24Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  Let me test this theory

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T09:32:57Z

- **pkg/controller/jobs/pod/pod_controller.go:560** by [@mimowo](https://github.com/mimowo) — 2026-01-29T09:32:57Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
```

  I would lie to comminize the code. Probably we can extract a function like `removePodFinalizer`, and it would check the FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T09:34:01Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:266** by [@mimowo](https://github.com/mimowo) — 2026-01-29T09:34:01Z
```diff
@@ -263,6 +263,71 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Label("job:pod", "area:jobs"),
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
```

  The test looks reasonable, but can you confirm it replicates the problem without the changes (no FG enabled)?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T09:35:56Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mimowo](https://github.com/mimowo) — 2026-01-29T09:35:56Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  I like using WithRetryOnConflict, but this is still "strict" mode, which could result in a little bit of perf degradation. I don't expect the perf degradation to be important, but I think adding the feature-gate for safety is better.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T09:47:29Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:266** by [@mykysha](https://github.com/mykysha) — 2026-01-29T09:47:28Z
```diff
@@ -263,6 +263,71 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Label("job:pod", "area:jobs"),
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
```

  Yes, if the feature gate is disabled, it fails very often - locally on the first try. With the featuregate enabled, as well as with the clientutil.WithRetryOnConflict(), it can reach hundreds of fail-less runs and potentially more

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T09:49:50Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mykysha](https://github.com/mykysha) — 2026-01-29T09:49:50Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  200+ runs in and no issues. Seems like this addresses the problem as well, without the need for the featuregate. Looks like a cleaner approach to me

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T10:11:09Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:266** by [@mimowo](https://github.com/mimowo) — 2026-01-29T10:11:09Z
```diff
@@ -263,6 +263,71 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Label("job:pod", "area:jobs"),
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
```

  Nice, yes the feature gate just means "strict" with retry at the controller level, while WithRetryOnConflict means strict with retry at the request level + controller level. The important part is about using "strict" rather than loose.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T10:13:21Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mimowo](https://github.com/mimowo) — 2026-01-29T10:13:21Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  Wait, I think there is some misunderstanding here. `WithRetryOnConflict` also means "strict", so the feature gate RemoveFinalizersWithStrictPatch still names correctly what we do. It is just a "technical detail" that we would also do retry at the request level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T10:15:22Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mimowo](https://github.com/mimowo) — 2026-01-29T10:15:22Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  The decision about introducing the gate isn't based on correctness of the solution, but about performance. Both are correct in terms of "they will not remove custom finalizers"
  
  As your experiments show the reconcile time is increased by using "strict" mode. I don't think this will be a blocker for users, but having the feature-gate will give us a tool to "bailout" if a production environment suffers performance degradation.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T10:17:07Z

- **pkg/controller/jobs/pod/pod_controller.go:563** by [@mykysha](https://github.com/mykysha) — 2026-01-29T10:17:07Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
```

  Understandable. Will extract the common finalizer removals to a separate func and check the featuregate there, whether we use Loose or RetryOnConflict

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T12:01:50Z

- **pkg/controller/jobs/pod/pod_controller.go:921** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T12:01:50Z
```diff
@@ -913,6 +912,27 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
```

  ```suggestion
  	if features.Enabled(features.RemoveFinalizersWithStrictPatch) {
  ```
  ?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T12:01:57Z

- **pkg/controller/jobs/pod/pod_controller.go:560** by [@mykysha](https://github.com/mykysha) — 2026-01-29T12:01:57Z
```diff
@@ -557,11 +557,17 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 		}
 	}
 
+	var patchOptions []clientutil.PatchOption
```

  Moved to a separate function

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T12:02:36Z

- **pkg/controller/jobs/pod/pod_controller.go:921** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T12:02:35Z
```diff
@@ -913,6 +912,27 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
```

  Or should we set WithLoose()?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T12:03:50Z

- **pkg/controller/jobs/pod/pod_controller.go:921** by [@mykysha](https://github.com/mykysha) — 2026-01-29T12:03:50Z
```diff
@@ -913,6 +912,27 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
```

  Oops, fixed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T12:04:23Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:266** by [@mykysha](https://github.com/mykysha) — 2026-01-29T12:04:23Z
```diff
@@ -263,6 +263,71 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Label("job:pod", "area:jobs"),
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
```

  Updated the test name

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T12:04:50Z

- **test/integration/singlecluster/controller/jobs/pod/pod_controller_test.go:315** by [@mykysha](https://github.com/mykysha) — 2026-01-29T12:04:50Z
```diff
@@ -263,6 +263,71 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Label("job:pod", "area:jobs"),
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.It("Should preserve custom finalizers when using RemoveFinalizersWithStrictPatch featuregate", func() {
+				pod := testingpod.MakePod(podName, ns.Name).Queue("test-queue").Obj()
+				gomega.Expect(k8sClient.Create(ctx, pod)).Should(gomega.Succeed())
+
+				createdPod := &corev1.Pod{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, lookupKey, createdPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Expect(createdPod.Finalizers).
+					To(gomega.ContainElement(constants.ManagedByKueueLabelKey), "Pod should have finalizer")
+
+				ginkgo.By("checking that workload is created for pod with the queue name")
+				createdWorkload := &kueue.Workload{}
+				wlLookupKey := types.NamespacedName{
+					Name:      podcontroller.GetWorkloadNameForPod(pod.Name, pod.UID),
+					Namespace: ns.Name,
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("checking the pod is unsuspended when workload is assigned")
+				clusterQueue := utiltestingapi.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj(),
+					).Obj()
+				admission := utiltestingapi.MakeAdmission(clusterQueue.Name).
+					PodSets(utiltestingapi.MakePodSetAssignment(kueue.DefaultPodSetName).
+						Assignment(corev1.ResourceCPU, "default", "1").
+						Count(createdWorkload.Spec.PodSets[0].Count).
+						Obj(),
+					).Obj()
+				util.SetQuotaReservation(ctx, k8sClient, wlLookupKey, admission)
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, lookupKey, createdPod)).Should(gomega.Succeed())
+					g.Expect(createdPod.Spec.SchedulingGates).Should(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				ginkgo.By("adding custom finalizer to the pod and setting the pod as succeeded")
+				customFinalizer := "my.custom/finalizer"
+
+				updatedPod := pod.DeepCopy()
+
+				updatedPod.Finalizers = append(updatedPod.Finalizers, customFinalizer)
+
+				util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, pod)
+				gomega.Expect(k8sClient.Patch(ctx, updatedPod, client.MergeFrom(pod))).Should(gomega.Succeed())
```

  Thanks! Updated to re-fetch the pod

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T12:05:01Z

- **pkg/controller/jobs/pod/pod_controller.go:921** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T12:05:01Z
```diff
@@ -913,6 +912,27 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if !features.Enabled(features.RemoveFinalizersWithStrictPatch) {
```

  But still need WithLoose(), right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T12:55:59Z

Thank you 👍 
/lgtm
/approve
Putting on hold in case @mbobrovskyi has some extra comments
/hold

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T13:29:28Z

- **pkg/controller/jobs/pod/pod_controller.go:931** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T13:29:28Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
+	} else {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
+	}
+
+	err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
+		removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
+		if removed {
+			log.V(3).Info("Removed pod finalizers", "Pod", klog.KObj(&pod))
+		}
```

  I think this should be outside Patch() function, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T13:31:06Z

- **pkg/controller/jobs/pod/pod_controller.go:930** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T13:31:06Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
+	} else {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
+	}
+
+	err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
+		removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
+		if removed {
+			log.V(3).Info("Removed pod finalizers", "Pod", klog.KObj(&pod))
```

  ```suggestion
  			log.V(3).Info("Removed pod finalizers", "pod", klog.KObj(&pod))
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-01-29T13:31:34Z

- **pkg/controller/jobs/pod/pod_controller.go:931** by [@mimowo](https://github.com/mimowo) — 2026-01-29T13:31:34Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
+	} else {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
+	}
+
+	err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
+		removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
+		if removed {
+			log.V(3).Info("Removed pod finalizers", "Pod", klog.KObj(&pod))
+		}
```

  Great catch!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T13:33:15Z

- **site/content/zh-CN/docs/installation/_index.md:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T13:33:15Z
```diff
@@ -270,4 +270,4 @@ spec:
 
 ## 接下来是什么 {#whats-next}
 
-- 阅读 [`Configuration` 的 API 参考](/docs/reference/kueue-config.v1beta1/#Configuration)
\ No newline at end of file
+- 阅读 [`Configuration` 的 API 参考](/docs/reference/kueue-config.v1beta1/#Configuration)
```

  What the changes here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T13:34:49Z

- **pkg/controller/jobs/pod/pod_controller.go:919** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T13:34:49Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
```

  ```suggestion
  	patchOptions := make([]clientutil.PatchOption, 1)
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T13:35:45Z

- **site/content/zh-CN/docs/installation/_index.md:273** by [@mykysha](https://github.com/mykysha) — 2026-01-29T13:35:45Z
```diff
@@ -270,4 +270,4 @@ spec:
 
 ## 接下来是什么 {#whats-next}
 
-- 阅读 [`Configuration` 的 API 参考](/docs/reference/kueue-config.v1beta1/#Configuration)
\ No newline at end of file
+- 阅读 [`Configuration` 的 API 参考](/docs/reference/kueue-config.v1beta1/#Configuration)
```

  Not sure honestly, will revert. Copying the previous version and pasting over "new" shows no changes so thats interesting

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T13:37:13Z

- **pkg/controller/jobs/pod/pod_controller.go:562** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T13:37:13Z
```diff
@@ -558,10 +558,9 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 	}
 
 	return parallelize.Until(ctx, len(podsInGroup.Items), func(i int) error {
-		pod := &podsInGroup.Items[i]
-		return clientutil.Patch(ctx, c, pod, func() (bool, error) {
-			return controllerutil.RemoveFinalizer(pod, podconstants.PodFinalizer), nil
-		}, clientutil.WithLoose())
+		pod := podsInGroup.Items[i]
+		_, err := removePodFinalizers(ctx, c, pod)
```

  ```suggestion
  		_, err := removePodFinalizers(ctx, c, &podsInGroup.Items[i])
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T13:37:22Z

- **pkg/controller/jobs/pod/pod_controller.go:915** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T13:37:22Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
```

  ```suggestion
  func removePodFinalizers(ctx context.Context, c client.Client, pod *corev1.Pod) (bool, error) {
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T14:38:00Z

- **pkg/controller/jobs/pod/pod_controller.go:915** by [@mykysha](https://github.com/mykysha) — 2026-01-29T14:38:00Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
```

  Updated here and in calls accordingy

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T14:38:12Z

- **pkg/controller/jobs/pod/pod_controller.go:562** by [@mykysha](https://github.com/mykysha) — 2026-01-29T14:38:12Z
```diff
@@ -558,10 +558,9 @@ func (p *Pod) Finalize(ctx context.Context, c client.Client) error {
 	}
 
 	return parallelize.Until(ctx, len(podsInGroup.Items), func(i int) error {
-		pod := &podsInGroup.Items[i]
-		return clientutil.Patch(ctx, c, pod, func() (bool, error) {
-			return controllerutil.RemoveFinalizer(pod, podconstants.PodFinalizer), nil
-		}, clientutil.WithLoose())
+		pod := podsInGroup.Items[i]
+		_, err := removePodFinalizers(ctx, c, pod)
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T14:38:36Z

- **pkg/controller/jobs/pod/pod_controller.go:919** by [@mykysha](https://github.com/mykysha) — 2026-01-29T14:38:36Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
```

  Updated, but with len 0 and cap 1

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T14:39:33Z

- **site/content/zh-CN/docs/installation/_index.md:273** by [@mykysha](https://github.com/mykysha) — 2026-01-29T14:39:33Z
```diff
@@ -270,4 +270,4 @@ spec:
 
 ## 接下来是什么 {#whats-next}
 
-- 阅读 [`Configuration` 的 API 参考](/docs/reference/kueue-config.v1beta1/#Configuration)
\ No newline at end of file
+- 阅读 [`Configuration` 的 API 参考](/docs/reference/kueue-config.v1beta1/#Configuration)
```

  Ok, it was the EOF newline. Reverted

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T14:39:48Z

- **pkg/controller/jobs/pod/pod_controller.go:930** by [@mykysha](https://github.com/mykysha) — 2026-01-29T14:39:48Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
+	} else {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
+	}
+
+	err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
+		removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
+		if removed {
+			log.V(3).Info("Removed pod finalizers", "Pod", klog.KObj(&pod))
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-29T14:40:05Z

- **pkg/controller/jobs/pod/pod_controller.go:931** by [@mykysha](https://github.com/mykysha) — 2026-01-29T14:40:05Z
```diff
@@ -913,6 +912,29 @@ func sortActivePods(activePods []corev1.Pod) {
 	})
 }
 
+func removePodFinalizers(ctx context.Context, c client.Client, pod corev1.Pod) (bool, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	var removed bool
+	var patchOptions []clientutil.PatchOption
+
+	if features.Enabled(features.RemoveFinalizersWithStrictPatch) {
+		patchOptions = append(patchOptions, clientutil.WithRetryOnConflict())
+	} else {
+		patchOptions = append(patchOptions, clientutil.WithLoose())
+	}
+
+	err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
+		removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
+		if removed {
+			log.V(3).Info("Removed pod finalizers", "Pod", klog.KObj(&pod))
+		}
```

  Moved outside the Patch

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T14:53:34Z

- **pkg/controller/jobs/pod/pod_controller.go:988** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T14:53:35Z
```diff
@@ -967,15 +984,10 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
 
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		removed, err := removePodFinalizers(ctx, c, pod)
```

  ```suggestion
  		removed, err := removePodFinalizers(ctx, c, &extraPods[i])
  ```
  I think no need extra variable for extra pod :)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-29T14:55:05Z

- **pkg/controller/jobs/pod/pod_controller.go:953** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-29T14:55:06Z
```diff
@@ -926,26 +948,21 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		if _, err := removePodFinalizers(ctx, c, pod); err != nil {
```

  ```suggestion
  		if _, err := removePodFinalizers(ctx, c, &extraPods[i]); err != nil {
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-30T09:20:32Z

- **pkg/controller/jobs/pod/pod_controller.go:988** by [@mykysha](https://github.com/mykysha) — 2026-01-30T09:20:32Z
```diff
@@ -967,15 +984,10 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
 
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		removed, err := removePodFinalizers(ctx, c, pod)
```

  We reuse this variable afterwards, so I believe we should leave it

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-30T09:20:57Z

- **pkg/controller/jobs/pod/pod_controller.go:953** by [@mykysha](https://github.com/mykysha) — 2026-01-30T09:20:57Z
```diff
@@ -926,26 +948,21 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		if _, err := removePodFinalizers(ctx, c, pod); err != nil {
```

  Same, this var is used in other calls

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-30T09:35:15Z

- **pkg/controller/jobs/pod/pod_controller.go:998** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-30T09:35:15Z
```diff
@@ -967,22 +983,19 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
 
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		removed, err := removePodFinalizers(ctx, c, pod)
+		if err != nil {
 			// We won't observe this cleanup in the event handler.
 			p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 			return err
 		}
 		if !removed {
 			// We don't expect an event in this case.
 			p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
+		} else {
+			log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(pod))
```

  /hold
  
  ```suggestion
  			log.V(3).Info("Finalized pod in group", "pod", klog.KObj(pod))
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-01-30T09:39:09Z

- **pkg/controller/jobs/pod/pod_controller.go:998** by [@mykysha](https://github.com/mykysha) — 2026-01-30T09:39:09Z
```diff
@@ -967,22 +983,19 @@ func (p *Pod) finalizePods(ctx context.Context, c client.Client, extraPods []cor
 	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
 
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		var removed bool
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed = controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		removed, err := removePodFinalizers(ctx, c, pod)
+		if err != nil {
 			// We won't observe this cleanup in the event handler.
 			p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 			return err
 		}
 		if !removed {
 			// We don't expect an event in this case.
 			p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
+		} else {
+			log.V(3).Info("Finalizing pod in group", "Pod", klog.KObj(pod))
```

  Updated

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-30T09:42:46Z

- **pkg/controller/jobs/pod/pod_controller.go:954** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-30T09:42:46Z
```diff
@@ -926,26 +942,26 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		removed, err := removePodFinalizers(ctx, c, pod)
+		if err != nil {
 			// We won't observe this cleanup in the event handler.
 			p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 			return err
 		}
+		if removed {
+			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(pod))
```

  Also here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-01-30T09:45:45Z

- **pkg/controller/jobs/pod/pod_controller.go:957** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-01-30T09:45:45Z
```diff
@@ -926,26 +942,26 @@ func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.Ev
 
 	// Finalize and delete the active pods created last
 	err := parallelize.Until(ctx, len(extraPods), func(i int) error {
-		pod := extraPods[i]
-		if err := clientutil.Patch(ctx, c, &pod, func() (bool, error) {
-			removed := controllerutil.RemoveFinalizer(&pod, podconstants.PodFinalizer)
-			if removed {
-				log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
-			}
-			return removed, nil
-		}, clientutil.WithLoose()); err != nil {
+		pod := &extraPods[i]
+
+		removed, err := removePodFinalizers(ctx, c, pod)
+		if err != nil {
 			// We won't observe this cleanup in the event handler.
 			p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 			return err
 		}
+		if removed {
+			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(pod))
+		}
+
 		if pod.DeletionTimestamp.IsZero() {
```

  I’m also wondering if we should delete first and then finalize, but this for a follow-up.
