# PR #399: Consolidate defaulting for ControllerManagerConfigurationSpec

**Summary**: Consolidate defaulting for ControllerManagerConfigurationSpec

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/399

**Last updated**: 2022-10-13T21:39:18Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `defaulting-config-in-webook` → `main`
- **Created**: 2022-09-16T20:21:52Z
- **Updated**: 2022-10-13T21:39:18Z
- **Merged**: 2022-10-13T21:35:04Z
- **Closed**: 2022-10-13T21:35:04Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: _none_
- **Changed files**: 4   **+209 / -84**

## Description

Signed-off-by: tenzen-y <yuki.iwai.tz@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:
As discussed in [this](https://github.com/kubernetes-sigs/kueue/pull/362#issuecomment-1232041979), I implemented the defaulting webhooks to set the default values for `ControllerManagerConfigurationSpec`.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-16T20:53:45Z

/assign @kerthcet

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-09-20T06:56:22Z

I'll review this later.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-10-09T04:02:41Z

Sorry for let this sit for such a long time, just back from holiday. @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-09T05:55:35Z

@kerthcet 
Thanks for the review!
I addressed your review comments.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T16:24:55Z

/retitle Consolidate defaulting for ControllerManagerConfigurationSpec

Note that there is no webhook involved in this PR, just API defaulting.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T06:46:04Z

@tenzen-y can you rebase and address the comments, totally looks good to me.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T07:35:37Z

> @tenzen-y can you rebase and address the comments, totally looks good to me.

@kerthcet @alculquicondor I have rebased this PR and addressed comments by @alculquicondor.
PTAL.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T07:46:52Z

/lgtm

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T09:01:36Z

@kerthcet I found forgotten corrections in the below codes and fixed that. Could you please re-check this PR?

- Incorrect

https://github.com/kubernetes-sigs/kueue/blob/1b94fa5d8621b5f008cc66f67044642bd54f758f/main_test.go#L272-L274

- Correct

https://github.com/kubernetes-sigs/kueue/blob/1e1dfc11ac5ea057627f9645923a90f0b0d83221/main_test.go#L272-L274

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T09:04:21Z

/lgtm

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T16:41:43Z

@alculquicondor I have addressed your suggestions.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-10-13T21:34:26Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/399#pullrequestreview-1141538225" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/399#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-09-16T20:26:12Z

- **main.go:223** by [@tenzen-y](https://github.com/tenzen-y) — 2022-09-16T20:26:12Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	configuration := config.Configuration{}
```

  Since `config` conflicts with `config "sigs.k8s.io/kueue/apis/config/v1alpha2`, I replaced `config` with `configuration`.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-09T04:01:50Z

- **main.go:229** by [@kerthcet](https://github.com/kerthcet) — 2022-10-08T16:17:42Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	configuration := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&configuration)
+		options = ctrl.Options{
+			Port:                   config.DefaultWebhookPort,
```

  Missed scheme here

- **apis/config/v1alpha2/defaults.go:60** by [@kerthcet](https://github.com/kerthcet) — 2022-10-08T16:27:25Z
```diff
@@ -36,9 +41,31 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
+
+	// Defaulting the Namespace.
 	if cfg.Namespace == nil {
 		cfg.Namespace = pointer.String(DefaultNamespace)
 	}
+
+	// Defaulting the ControllerManagerConfigurationSpec.
+	if cfg.Webhook.Port == nil {
+		cfg.Webhook.Port = pointer.Int(DefaultWebhookPort)
+	}
+	if len(cfg.Metrics.BindAddress) == 0 {
+		cfg.Metrics.BindAddress = DefaultMetricsBindAddress
+	}
+	if len(cfg.Health.HealthProbeBindAddress) == 0 {
+		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
+	}
+	if cfg.LeaderElection == nil {
```

  Can we remove it and make `if cfg.LeaderElection.LeaderElect != nil && *cfg.LeaderElection.LeaderElect &&
  		len(cfg.LeaderElection.ResourceName) == 0` 
  to 
  `if cfg.LeaderElection != nil && cfg.LeaderElection.LeaderElect != nil && *cfg.LeaderElection.LeaderElect &&
  		len(cfg.LeaderElection.ResourceName) == 0`
  ?

- **apis/config/v1alpha2/defaults.go:21** by [@kerthcet](https://github.com/kerthcet) — 2022-10-08T16:38:24Z
```diff
@@ -18,13 +18,18 @@ package v1alpha2
 
 import (
 	"k8s.io/apimachinery/pkg/runtime"
+	componentconfigv1alpha1 "k8s.io/component-base/config/v1alpha1"
```

  alias to config.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-09T04:09:37Z

- **main.go:231** by [@kerthcet](https://github.com/kerthcet) — 2022-10-09T04:09:24Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	configuration := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&configuration)
+		options = ctrl.Options{
+			Port:                   config.DefaultWebhookPort,
+			HealthProbeBindAddress: config.DefaultHealthProbeBindAddress,
+			MetricsBindAddress:     config.DefaultMetricsBindAddress,
+			LeaderElectionID:       config.DefaultLeaderElectionID,
```

  Just created a PR here https://github.com/kubernetes-sigs/kueue/pull/413, if leaderElection is false, leaderElectionID should be empty since we have no config file here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-09T05:28:36Z

- **main.go:229** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-09T05:28:36Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	configuration := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&configuration)
+		options = ctrl.Options{
+			Port:                   config.DefaultWebhookPort,
```

  Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-09T05:42:02Z

- **apis/config/v1alpha2/defaults.go:60** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-09T05:42:02Z
```diff
@@ -36,9 +41,31 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
+
+	// Defaulting the Namespace.
 	if cfg.Namespace == nil {
 		cfg.Namespace = pointer.String(DefaultNamespace)
 	}
+
+	// Defaulting the ControllerManagerConfigurationSpec.
+	if cfg.Webhook.Port == nil {
+		cfg.Webhook.Port = pointer.Int(DefaultWebhookPort)
+	}
+	if len(cfg.Metrics.BindAddress) == 0 {
+		cfg.Metrics.BindAddress = DefaultMetricsBindAddress
+	}
+	if len(cfg.Health.HealthProbeBindAddress) == 0 {
+		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
+	}
+	if cfg.LeaderElection == nil {
```

  looks good!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-09T05:46:14Z

- **main.go:231** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-09T05:46:14Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	configuration := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&configuration)
+		options = ctrl.Options{
+			Port:                   config.DefaultWebhookPort,
+			HealthProbeBindAddress: config.DefaultHealthProbeBindAddress,
+			MetricsBindAddress:     config.DefaultMetricsBindAddress,
+			LeaderElectionID:       config.DefaultLeaderElectionID,
```

  It makes sense.
  I removed this leaderElectionID.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-11T17:24:13Z

Just nits

- **apis/config/v1alpha2/defaults.go:43** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T16:25:30Z
```diff
@@ -36,9 +40,28 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
+
```

  nit: remove the empty line and comments. They are not very useful.

- **main.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T16:28:19Z
```diff
@@ -85,7 +85,7 @@ func main() {
 
 	ctrl.SetLogger(zap.New(zap.UseFlagOptions(&opts)))
 
-	options, config := apply(configFile)
+	options, configuration := apply(configFile)
```

  Nit
  ```suggestion
  	options, cfg := apply(configFile)
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T02:11:28Z

- **apis/config/v1alpha2/defaults.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T02:11:28Z
```diff
@@ -36,9 +40,28 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
+
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T07:34:01Z

- **main.go:88** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T07:34:01Z
```diff
@@ -85,7 +85,7 @@ func main() {
 
 	ctrl.SetLogger(zap.New(zap.UseFlagOptions(&opts)))
 
-	options, config := apply(configFile)
+	options, configuration := apply(configFile)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T07:34:10Z

- **apis/config/v1alpha2/defaults.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T07:34:10Z
```diff
@@ -36,9 +40,28 @@ func addDefaultingFuncs(scheme *runtime.Scheme) error {
 
 // SetDefaults_Configuration sets default values for ComponentConfig.
 func SetDefaults_Configuration(cfg *Configuration) {
+
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-12T13:51:13Z

- **apis/config/v1alpha2/defaults_test.go:100** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-12T13:43:51Z
```diff
@@ -36,8 +56,117 @@ func TestSetDefaults_Configuration(t *testing.T) {
 					Enable: pointer.Bool(false),
 				},
 			},
+			want: &Configuration{
+				Namespace:                          pointer.String(DefaultNamespace),
+				ControllerManagerConfigurationSpec: defaultCtrlManagerConfigurationSpec,
+				InternalCertManagement: &InternalCertManagement{
+					Enable: pointer.Bool(false),
+				},
+			},
+		},
+		"defaulting ControllerManagerConfigurationSpec": {
+			original: &Configuration{
+				ControllerManagerConfigurationSpec: ctrlconfigv1alpha1.ControllerManagerConfigurationSpec{
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect: pointer.Bool(true),
+					},
+				},
+				InternalCertManagement: &InternalCertManagement{
+					Enable: pointer.Bool(false),
+				},
+			},
 			want: &Configuration{
 				Namespace: pointer.String(DefaultNamespace),
+				ControllerManagerConfigurationSpec: ctrlconfigv1alpha1.ControllerManagerConfigurationSpec{
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(DefaultWebhookPort),
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: DefaultMetricsBindAddress,
+					},
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: DefaultHealthProbeBindAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: DefaultLeaderElectionID,
+					},
+				},
+				InternalCertManagement: &InternalCertManagement{
+					Enable: pointer.Bool(false),
+				},
+			},
+		},
+		"should not defaulting ControllerManagerConfigurationSpec": {
```

  ```suggestion
  		"should not default ControllerManagerConfigurationSpec": {
  ```

- **main.go:237** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-12T13:49:25Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options = ctrl.Options{
+			Scheme:                 scheme,
+			Port:                   config.DefaultWebhookPort,
+			HealthProbeBindAddress: config.DefaultHealthProbeBindAddress,
+			MetricsBindAddress:     config.DefaultMetricsBindAddress,
+			LeaderElection:         false,
+		}
```

  I wonder if this works
  ```suggestion
  		options, err = options.AndFrom(cfg)
  ```

- **main.go:251** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-12T13:50:37Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options = ctrl.Options{
+			Scheme:                 scheme,
+			Port:                   config.DefaultWebhookPort,
+			HealthProbeBindAddress: config.DefaultHealthProbeBindAddress,
+			MetricsBindAddress:     config.DefaultMetricsBindAddress,
+			LeaderElection:         false,
+		}
 	} else {
-		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
+		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&cfg))
 		if err != nil {
 			setupLog.Error(err, "unable to load the config file")
 			os.Exit(1)
 		}
 
-		cfgStr, err := encodeConfig(&config)
+		cfgStr, err := encodeConfig(&cfg)
 		if err != nil {
 			setupLog.Error(err, "unable to encode config file")
 			os.Exit(1)
 		}
 		setupLog.Info("Successfully loaded config file", "config", cfgStr)
```

  We should also log when the configuration is just the default. It's useful for debugging.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T16:19:51Z

- **main.go:237** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T16:19:51Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options = ctrl.Options{
+			Scheme:                 scheme,
+			Port:                   config.DefaultWebhookPort,
+			HealthProbeBindAddress: config.DefaultHealthProbeBindAddress,
+			MetricsBindAddress:     config.DefaultMetricsBindAddress,
+			LeaderElection:         false,
+		}
```

  That works fine on my local. Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T16:20:21Z

- **main.go:251** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T16:20:21Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options = ctrl.Options{
+			Scheme:                 scheme,
+			Port:                   config.DefaultWebhookPort,
+			HealthProbeBindAddress: config.DefaultHealthProbeBindAddress,
+			MetricsBindAddress:     config.DefaultMetricsBindAddress,
+			LeaderElection:         false,
+		}
 	} else {
-		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
+		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&cfg))
 		if err != nil {
 			setupLog.Error(err, "unable to load the config file")
 			os.Exit(1)
 		}
 
-		cfgStr, err := encodeConfig(&config)
+		cfgStr, err := encodeConfig(&cfg)
 		if err != nil {
 			setupLog.Error(err, "unable to encode config file")
 			os.Exit(1)
 		}
 		setupLog.Info("Successfully loaded config file", "config", cfgStr)
```

  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T16:22:48Z

- **apis/config/v1alpha2/defaults_test.go:100** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T16:22:48Z
```diff
@@ -36,8 +56,117 @@ func TestSetDefaults_Configuration(t *testing.T) {
 					Enable: pointer.Bool(false),
 				},
 			},
+			want: &Configuration{
+				Namespace:                          pointer.String(DefaultNamespace),
+				ControllerManagerConfigurationSpec: defaultCtrlManagerConfigurationSpec,
+				InternalCertManagement: &InternalCertManagement{
+					Enable: pointer.Bool(false),
+				},
+			},
+		},
+		"defaulting ControllerManagerConfigurationSpec": {
+			original: &Configuration{
+				ControllerManagerConfigurationSpec: ctrlconfigv1alpha1.ControllerManagerConfigurationSpec{
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect: pointer.Bool(true),
+					},
+				},
+				InternalCertManagement: &InternalCertManagement{
+					Enable: pointer.Bool(false),
+				},
+			},
 			want: &Configuration{
 				Namespace: pointer.String(DefaultNamespace),
+				ControllerManagerConfigurationSpec: ctrlconfigv1alpha1.ControllerManagerConfigurationSpec{
+					Webhook: ctrlconfigv1alpha1.ControllerWebhook{
+						Port: pointer.Int(DefaultWebhookPort),
+					},
+					Metrics: ctrlconfigv1alpha1.ControllerMetrics{
+						BindAddress: DefaultMetricsBindAddress,
+					},
+					Health: ctrlconfigv1alpha1.ControllerHealth{
+						HealthProbeBindAddress: DefaultHealthProbeBindAddress,
+					},
+					LeaderElection: &componentconfigv1alpha1.LeaderElectionConfiguration{
+						LeaderElect:  pointer.Bool(true),
+						ResourceName: DefaultLeaderElectionID,
+					},
+				},
+				InternalCertManagement: &InternalCertManagement{
+					Enable: pointer.Bool(false),
+				},
+			},
+		},
+		"should not defaulting ControllerManagerConfigurationSpec": {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T16:38:40Z

- **main.go:251** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T16:38:40Z
```diff
@@ -220,40 +220,31 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options = ctrl.Options{
+			Scheme:                 scheme,
+			Port:                   config.DefaultWebhookPort,
+			HealthProbeBindAddress: config.DefaultHealthProbeBindAddress,
+			MetricsBindAddress:     config.DefaultMetricsBindAddress,
+			LeaderElection:         false,
+		}
 	} else {
-		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&config))
+		options, err = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(&cfg))
 		if err != nil {
 			setupLog.Error(err, "unable to load the config file")
 			os.Exit(1)
 		}
 
-		cfgStr, err := encodeConfig(&config)
+		cfgStr, err := encodeConfig(&cfg)
 		if err != nil {
 			setupLog.Error(err, "unable to encode config file")
 			os.Exit(1)
 		}
 		setupLog.Info("Successfully loaded config file", "config", cfgStr)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-12T16:39:59Z

- **main.go:238** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-12T16:39:59Z
```diff
@@ -220,40 +220,36 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options, err = options.AndFrom(&cfg)
+		if err != nil {
+			setupLog.Error(err, "unable to load the default config")
+			os.Exit(1)
+		}
+
+		cfgStr, err := encodeConfig(&cfg)
+		if err != nil {
+			setupLog.Error(err, "unable to encode the default config")
+			os.Exit(1)
+		}
+		setupLog.Info("Successfully loaded the default config", "config", cfgStr)
```

  This logger output the below:
  
  ```shell
  {"level":"info","ts":"2022-10-12T16:30:30.00241243Z","logger":"setup","caller":"logr@v1.2.3/logr.go:261","msg":"Successfully loaded the default config","config":"apiVersion: config.kueue.x-k8s.io/v1alpha2\nhealth:\n  healthProbeBindAddress: :8081\ninternalCertManagement:\n  enable: true\n  webhookSecretName: kueue-webhook-server-cert\n  webhookServiceName: kueue-webhook-service\nkind: Configuration\nmanageJobsWithoutQueueName: false\nmetrics:\n  bindAddress: :8080\nnamespace: kueue-system\nwebhook:\n  port: 9443\n"}
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-12T17:49:30Z

Final nit

- **main.go:238** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-12T17:46:45Z
```diff
@@ -220,40 +220,36 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options, err = options.AndFrom(&cfg)
+		if err != nil {
+			setupLog.Error(err, "unable to load the default config")
+			os.Exit(1)
+		}
+
+		cfgStr, err := encodeConfig(&cfg)
+		if err != nil {
+			setupLog.Error(err, "unable to encode the default config")
+			os.Exit(1)
+		}
+		setupLog.Info("Successfully loaded the default config", "config", cfgStr)
```

  we could de-duplicate this code by putting it outside of the conditional.
  
  A message such as `Successfully loaded configuration` would cover both codepaths.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-13T02:27:13Z

- **main.go:238** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-13T02:27:12Z
```diff
@@ -220,40 +220,36 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 		Scheme: scheme,
 	}
 
-	config := config.Configuration{}
+	cfg := config.Configuration{}
 
 	if configFile == "" {
-		scheme.Default(&config)
+		scheme.Default(&cfg)
+		options, err = options.AndFrom(&cfg)
+		if err != nil {
+			setupLog.Error(err, "unable to load the default config")
+			os.Exit(1)
+		}
+
+		cfgStr, err := encodeConfig(&cfg)
+		if err != nil {
+			setupLog.Error(err, "unable to encode the default config")
+			os.Exit(1)
+		}
+		setupLog.Info("Successfully loaded the default config", "config", cfgStr)
```

  Thanks for the suggestion!
  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-13T12:50:28Z

- **main.go:224** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-13T12:50:28Z
```diff
@@ -219,41 +219,37 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	options := ctrl.Options{
 		Scheme: scheme,
 	}
+	cfg := config.Configuration{}
 
-	config := config.Configuration{}
-
+	successLoadConfigurationMessage := "Successfully loaded configuration"
 	if configFile == "" {
```

  I meant something more like this (take it as pseudocode)
  
  ```
  if configFile == "" {
    scheme.Default(cfg)
    options = options.AndFrom(cfg)
  } else {
    options = options.AndFrom(ctrl.ConfigFile().AtPath(configFile).OfKind(cfg))
  }
  cfgStr = encodeConfig(cfg)
  setupLog("Successfully loaded configuration", "config", cfgStr))
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-13T12:53:42Z

- **main.go:224** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-13T12:53:41Z
```diff
@@ -219,41 +219,37 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	options := ctrl.Options{
 		Scheme: scheme,
 	}
+	cfg := config.Configuration{}
 
-	config := config.Configuration{}
-
+	successLoadConfigurationMessage := "Successfully loaded configuration"
 	if configFile == "" {
```

  Thanks for clarifying!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-13T18:58:29Z

- **main.go:224** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-13T18:58:29Z
```diff
@@ -219,41 +219,37 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 	options := ctrl.Options{
 		Scheme: scheme,
 	}
+	cfg := config.Configuration{}
 
-	config := config.Configuration{}
-
+	successLoadConfigurationMessage := "Successfully loaded configuration"
 	if configFile == "" {
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-13T21:34:20Z

Thanks!

/lgtm
/approve
