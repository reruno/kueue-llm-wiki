# PR #4023: [LeaderWorkerSet] Support LeaderReadyStartupPolicy

**Summary**: [LeaderWorkerSet] Support LeaderReadyStartupPolicy

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4023

**Last updated**: 2025-01-28T15:21:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/lws-support-leader-ready-startup-policy` → `main`
- **Created**: 2025-01-20T17:53:50Z
- **Updated**: 2025-01-28T15:21:03Z
- **Merged**: 2025-01-28T15:19:24Z
- **Closed**: 2025-01-28T15:19:24Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 20   **+1351 / -257**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Support LeaderReadyStartupPolicy on LeaderWorkerSet.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3232

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-20T17:53:53Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-01-20T17:54:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d6d56fbffc023ad800b99cf4fec8ed071c032529 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6798f0707100a100084d681d |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-28T15:15:06Z

/lgtm 
/approve
Awesome! thanks! please follow up with the TAS integration, which is rank based ordering in particular.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-28T15:15:16Z

LGTM label has been added.  <details>Git tree hash: 0815e77f84fc910ae813bab7078f84d1bcde0085</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-28T15:15:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4023#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4023#issuecomment-2619293763" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-27T09:55:33Z

First pass, overall looks like a good direction. Thanks!

- **pkg/controller/jobframework/integrationmanager.go:65** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:28:39Z
```diff
@@ -61,6 +61,9 @@ type IntegrationCallbacks struct {
 	GVK schema.GroupVersionKind
 	// NewReconciler creates a new reconciler
 	NewReconciler ReconcilerFactory
+	// NewAdditionalReconcilers creates a new additional reconcilers
+	// (this callback is optional)
```

  ```suggestion
  	// NewAdditionalReconcilers creates additional reconcilers (optional)
  ```
  nit

- **pkg/controller/jobframework/reconciler.go:431** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:31:58Z
```diff
@@ -428,8 +428,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				log.V(3).Info("Handling job with no workload found an existing workload")
 				return ctrl.Result{Requeue: true}, nil
 			}
+			// else if errors.Is(err, ErrPrebuildWorkloadNotFound) {
```

  I'm not sure what is the meaning of the comments and TODO. Do we need the changes? What they do from the user-perspective?

- **pkg/controller/jobframework/reconciler.go:438** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:32:27Z
```diff
@@ -428,8 +428,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				log.V(3).Info("Handling job with no workload found an existing workload")
 				return ctrl.Result{Requeue: true}, nil
 			}
+			// else if errors.Is(err, ErrPrebuildWorkloadNotFound) {
+			// TODO: ???
+			// log.V(3).Info("Handling job with no prebuild workload. Waiting one second...")
+			// return ctrl.Result{RequeueAfter: time.Second}, nil
+			// }
 			if IsUnretryableError(err) {
 				log.V(3).Info("Handling job with no workload", "unretryableError", err)
+				// TODO: If the error is non-retryable, should we return without an error?
```

  Same here, I don't understand why / if we need these TODO changes.

- **pkg/controller/jobframework/reconciler.go:360** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:33:29Z
```diff
@@ -357,9 +357,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 1. make sure there is only a single existing instance of the workload.
 	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := r.ensureOneWorkload(ctx, job, object)
-	if err != nil {
-		return ctrl.Result{}, err
+	wl, requeue, err := r.ensureOneWorkload(ctx, job, object)
```

  Why do we need the requeue? If there is an error then "requeue" is implied. Also, I suppose whenever a new pod is created it triggers the reconciler so, not sure why we need to force the requeue. It would also make the diff smaller.

- **pkg/controller/jobframework/reconciler.go:741** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:40:46Z
```diff
@@ -731,29 +751,36 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+func EnsurePrebuiltWorkloadOwnership(ctx context.Context, c client.Client, wl *kueue.Workload, object client.Object) (bool, error) {
 	if !metav1.IsControlledBy(wl, object) {
-		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
-			// don't return an error here, since a retry cannot give a different result,
-			// log the error.
-			log := ctrl.LoggerFrom(ctx)
-			log.Error(err, "Cannot take ownership of the workload")
-			return false, nil
```

  Actually, I like the fact we don't silence the error, but it seems a little bit disconnected from the PR. Could you send a separatory / preparatory PR which changes that? I think it might actually be a bugfix. Can you think of a user-facing scenario? If so, I believe we could even consider a cherry-pick.

- **pkg/controller/jobframework/reconciler.go:239** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:43:48Z
```diff
@@ -236,7 +236,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	ctx = ctrl.LoggerInto(ctx, log)
 
 	defer func() {
-		err = r.ignoreUnretryableError(log, err)
+		err = IgnoreUnretryableError(log, err)
```

  Is this change necessary, or just a temporary refactoring? If not necessary I prefer revert it, the diff is already large.

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:118** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:45:23Z
```diff
@@ -0,0 +1,227 @@
+/*
+Copyright 2025 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package leaderworkerset
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	leaderworkersetv1 "sigs.k8s.io/lws/api/leaderworkerset/v1"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	leaderPodSetName = "leader"
+	workerPodSetName = "worker"
+)
+
+type Reconciler struct {
+	client          client.Client
+	record          record.EventRecorder
+	labelKeysToCopy []string
+}
+
+func NewReconciler(client client.Client, eventRecorder record.EventRecorder, opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	options := jobframework.ProcessOptions(opts...)
+
+	return &Reconciler{
+		client:          client,
+		record:          eventRecorder,
+		labelKeysToCopy: options.LabelKeysToCopy,
+	}
+}
+
+var _ jobframework.JobReconcilerInterface = (*Reconciler)(nil)
+
+func (r *Reconciler) SetupWithManager(mgr ctrl.Manager) error {
+	ctrl.Log.V(3).Info("Setting up LeaderWorkerSet reconciler")
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&leaderworkersetv1.LeaderWorkerSet{}).
+		Named("leaderworkerset").
+		WithEventFilter(r).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=leaderworkerset.x-k8s.io,resources=leaderworkersets,verbs=get;list;watch
+
+func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	lws := &leaderworkersetv1.LeaderWorkerSet{}
+	err := r.client.Get(ctx, req.NamespacedName, lws)
+	if err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("leaderworkerset", klog.KObj(lws))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling LeaderWorkerSet")
+
+	err = r.createPrebuildWorkloadsIfNotExist(ctx, lws)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadsIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet) error {
+	replicas := ptr.Deref(lws.Spec.Replicas, 1)
+	for i := int32(0); i < replicas; i++ {
+		if err := r.createPrebuildWorkloadIfNotExist(ctx, lws, i); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, client.ObjectKey{Name: GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)), Namespace: lws.Namespace}, wl)
+	// Ignore if the Workload already exists or an error occurs.
+	if err == nil || client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err := r.createPrebuildWorkload(ctx, lws, index); err != nil {
+		return err
+	}
+	return nil
```

  ```suggestion
  	return r.createPrebuildWorkload(ctx, lws, index)
  ```
  nit

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:144** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:46:46Z
```diff
@@ -0,0 +1,227 @@
+/*
+Copyright 2025 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package leaderworkerset
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	leaderworkersetv1 "sigs.k8s.io/lws/api/leaderworkerset/v1"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	leaderPodSetName = "leader"
+	workerPodSetName = "worker"
+)
+
+type Reconciler struct {
+	client          client.Client
+	record          record.EventRecorder
+	labelKeysToCopy []string
+}
+
+func NewReconciler(client client.Client, eventRecorder record.EventRecorder, opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	options := jobframework.ProcessOptions(opts...)
+
+	return &Reconciler{
+		client:          client,
+		record:          eventRecorder,
+		labelKeysToCopy: options.LabelKeysToCopy,
+	}
+}
+
+var _ jobframework.JobReconcilerInterface = (*Reconciler)(nil)
+
+func (r *Reconciler) SetupWithManager(mgr ctrl.Manager) error {
+	ctrl.Log.V(3).Info("Setting up LeaderWorkerSet reconciler")
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&leaderworkersetv1.LeaderWorkerSet{}).
+		Named("leaderworkerset").
+		WithEventFilter(r).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=leaderworkerset.x-k8s.io,resources=leaderworkersets,verbs=get;list;watch
+
+func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	lws := &leaderworkersetv1.LeaderWorkerSet{}
+	err := r.client.Get(ctx, req.NamespacedName, lws)
+	if err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("leaderworkerset", klog.KObj(lws))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling LeaderWorkerSet")
+
+	err = r.createPrebuildWorkloadsIfNotExist(ctx, lws)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadsIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet) error {
+	replicas := ptr.Deref(lws.Spec.Replicas, 1)
+	for i := int32(0); i < replicas; i++ {
+		if err := r.createPrebuildWorkloadIfNotExist(ctx, lws, i); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, client.ObjectKey{Name: GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)), Namespace: lws.Namespace}, wl)
+	// Ignore if the Workload already exists or an error occurs.
+	if err == nil || client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err := r.createPrebuildWorkload(ctx, lws, index); err != nil {
+		return err
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkload(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	createdWorkload := r.constructWorkload(lws, index)
+	err := r.client.Create(ctx, createdWorkload)
+	if err != nil {
+		return err
+	}
+	r.record.Eventf(
+		lws, corev1.EventTypeNormal, jobframework.ReasonCreatedWorkload,
+		"Created Workload: %v", workload.Key(createdWorkload),
+	)
+	return nil
+}
+
+func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, index int32) *kueue.Workload {
+	wl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)),
+			Namespace:   lws.Namespace,
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: admissioncheck.FilterProvReqAnnotations(lws.GetAnnotations()),
+		},
+		Spec: kueue.WorkloadSpec{
+			QueueName:                   jobframework.QueueNameForObject(lws),
+			PodSets:                     r.podSets(lws),
+			MaximumExecutionTimeSeconds: jobframework.MaximumExecutionTimeSecondsForObject(lws),
+		},
+	}
```

  Can we commonize this more? Note that this requires duplication for `MaximumExecutionTimeSeconds`, but it might also require duplication for other fields in the future, and we are likely to forget about this function here.

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:163** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:50:11Z
```diff
@@ -0,0 +1,227 @@
+/*
+Copyright 2025 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package leaderworkerset
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	leaderworkersetv1 "sigs.k8s.io/lws/api/leaderworkerset/v1"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	leaderPodSetName = "leader"
+	workerPodSetName = "worker"
+)
+
+type Reconciler struct {
+	client          client.Client
+	record          record.EventRecorder
+	labelKeysToCopy []string
+}
+
+func NewReconciler(client client.Client, eventRecorder record.EventRecorder, opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	options := jobframework.ProcessOptions(opts...)
+
+	return &Reconciler{
+		client:          client,
+		record:          eventRecorder,
+		labelKeysToCopy: options.LabelKeysToCopy,
+	}
+}
+
+var _ jobframework.JobReconcilerInterface = (*Reconciler)(nil)
+
+func (r *Reconciler) SetupWithManager(mgr ctrl.Manager) error {
+	ctrl.Log.V(3).Info("Setting up LeaderWorkerSet reconciler")
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&leaderworkersetv1.LeaderWorkerSet{}).
+		Named("leaderworkerset").
+		WithEventFilter(r).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=leaderworkerset.x-k8s.io,resources=leaderworkersets,verbs=get;list;watch
+
+func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	lws := &leaderworkersetv1.LeaderWorkerSet{}
+	err := r.client.Get(ctx, req.NamespacedName, lws)
+	if err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("leaderworkerset", klog.KObj(lws))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling LeaderWorkerSet")
+
+	err = r.createPrebuildWorkloadsIfNotExist(ctx, lws)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadsIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet) error {
+	replicas := ptr.Deref(lws.Spec.Replicas, 1)
+	for i := int32(0); i < replicas; i++ {
+		if err := r.createPrebuildWorkloadIfNotExist(ctx, lws, i); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, client.ObjectKey{Name: GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)), Namespace: lws.Namespace}, wl)
+	// Ignore if the Workload already exists or an error occurs.
+	if err == nil || client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err := r.createPrebuildWorkload(ctx, lws, index); err != nil {
+		return err
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkload(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	createdWorkload := r.constructWorkload(lws, index)
+	err := r.client.Create(ctx, createdWorkload)
+	if err != nil {
+		return err
+	}
+	r.record.Eventf(
+		lws, corev1.EventTypeNormal, jobframework.ReasonCreatedWorkload,
+		"Created Workload: %v", workload.Key(createdWorkload),
+	)
+	return nil
+}
+
+func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, index int32) *kueue.Workload {
+	wl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)),
+			Namespace:   lws.Namespace,
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: admissioncheck.FilterProvReqAnnotations(lws.GetAnnotations()),
+		},
+		Spec: kueue.WorkloadSpec{
+			QueueName:                   jobframework.QueueNameForObject(lws),
+			PodSets:                     r.podSets(lws),
+			MaximumExecutionTimeSeconds: jobframework.MaximumExecutionTimeSecondsForObject(lws),
+		},
+	}
+
+	wl.Annotations[podcontroller.IsGroupWorkloadAnnotationKey] = podcontroller.IsGroupWorkloadAnnotationValue
+
+	return wl
+}
+
+func (r *Reconciler) podSets(lws *leaderworkersetv1.LeaderWorkerSet) []kueue.PodSet {
+	podSets := make([]kueue.PodSet, 0, 2)
+
+	if lws.Spec.LeaderWorkerTemplate.LeaderTemplate != nil {
+		podSets = append(podSets, kueue.PodSet{
+			Name:  leaderPodSetName,
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: *lws.Spec.LeaderWorkerTemplate.LeaderTemplate.Spec.DeepCopy(),
+			},
+			TopologyRequest: jobframework.PodSetTopologyRequest(
+				&lws.Spec.LeaderWorkerTemplate.LeaderTemplate.ObjectMeta,
+				// TODO: ???
+				ptr.To(kueuealpha.PodGroupPodIndexLabel),
```

  IIUC this is for TAS rank-based ordering, but does it work? IIUC STS will set indexes for workers, so we should use this label. The rank-based ordering for leaders does not matter, because there is only one leader. Please make sure there is an e2e test added ( could be a follow up). 
  
  Either support TAS in this PR with the label, or leave the TAS support with e2e test for a follow up.

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:188** by [@mimowo](https://github.com/mimowo) — 2025-01-27T09:51:54Z
```diff
@@ -0,0 +1,227 @@
+/*
+Copyright 2025 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package leaderworkerset
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	leaderworkersetv1 "sigs.k8s.io/lws/api/leaderworkerset/v1"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	leaderPodSetName = "leader"
+	workerPodSetName = "worker"
+)
+
+type Reconciler struct {
+	client          client.Client
+	record          record.EventRecorder
+	labelKeysToCopy []string
+}
+
+func NewReconciler(client client.Client, eventRecorder record.EventRecorder, opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	options := jobframework.ProcessOptions(opts...)
+
+	return &Reconciler{
+		client:          client,
+		record:          eventRecorder,
+		labelKeysToCopy: options.LabelKeysToCopy,
+	}
+}
+
+var _ jobframework.JobReconcilerInterface = (*Reconciler)(nil)
+
+func (r *Reconciler) SetupWithManager(mgr ctrl.Manager) error {
+	ctrl.Log.V(3).Info("Setting up LeaderWorkerSet reconciler")
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&leaderworkersetv1.LeaderWorkerSet{}).
+		Named("leaderworkerset").
+		WithEventFilter(r).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=leaderworkerset.x-k8s.io,resources=leaderworkersets,verbs=get;list;watch
+
+func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	lws := &leaderworkersetv1.LeaderWorkerSet{}
+	err := r.client.Get(ctx, req.NamespacedName, lws)
+	if err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("leaderworkerset", klog.KObj(lws))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling LeaderWorkerSet")
+
+	err = r.createPrebuildWorkloadsIfNotExist(ctx, lws)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadsIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet) error {
+	replicas := ptr.Deref(lws.Spec.Replicas, 1)
+	for i := int32(0); i < replicas; i++ {
+		if err := r.createPrebuildWorkloadIfNotExist(ctx, lws, i); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, client.ObjectKey{Name: GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)), Namespace: lws.Namespace}, wl)
+	// Ignore if the Workload already exists or an error occurs.
+	if err == nil || client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err := r.createPrebuildWorkload(ctx, lws, index); err != nil {
+		return err
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkload(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	createdWorkload := r.constructWorkload(lws, index)
+	err := r.client.Create(ctx, createdWorkload)
+	if err != nil {
+		return err
+	}
+	r.record.Eventf(
+		lws, corev1.EventTypeNormal, jobframework.ReasonCreatedWorkload,
+		"Created Workload: %v", workload.Key(createdWorkload),
+	)
+	return nil
+}
+
+func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, index int32) *kueue.Workload {
+	wl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)),
+			Namespace:   lws.Namespace,
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: admissioncheck.FilterProvReqAnnotations(lws.GetAnnotations()),
+		},
+		Spec: kueue.WorkloadSpec{
+			QueueName:                   jobframework.QueueNameForObject(lws),
+			PodSets:                     r.podSets(lws),
+			MaximumExecutionTimeSeconds: jobframework.MaximumExecutionTimeSecondsForObject(lws),
+		},
+	}
+
+	wl.Annotations[podcontroller.IsGroupWorkloadAnnotationKey] = podcontroller.IsGroupWorkloadAnnotationValue
+
+	return wl
+}
+
+func (r *Reconciler) podSets(lws *leaderworkersetv1.LeaderWorkerSet) []kueue.PodSet {
+	podSets := make([]kueue.PodSet, 0, 2)
+
+	if lws.Spec.LeaderWorkerTemplate.LeaderTemplate != nil {
+		podSets = append(podSets, kueue.PodSet{
+			Name:  leaderPodSetName,
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: *lws.Spec.LeaderWorkerTemplate.LeaderTemplate.Spec.DeepCopy(),
+			},
+			TopologyRequest: jobframework.PodSetTopologyRequest(
+				&lws.Spec.LeaderWorkerTemplate.LeaderTemplate.ObjectMeta,
+				// TODO: ???
+				ptr.To(kueuealpha.PodGroupPodIndexLabel),
+				nil,
+				nil,
+			),
+		})
+	}
+
+	defaultPodSetName := kueue.DefaultPodSetName
+	if len(podSets) > 0 {
+		defaultPodSetName = workerPodSetName
+	}
+
+	defaultPodSetCount := ptr.Deref(lws.Spec.LeaderWorkerTemplate.Size, 1)
+	if len(podSets) > 0 {
+		defaultPodSetCount--
+	}
+
+	podSets = append(podSets, kueue.PodSet{
+		Name:  defaultPodSetName,
+		Count: defaultPodSetCount,
+		Template: corev1.PodTemplateSpec{
+			Spec: *lws.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.DeepCopy(),
+		},
+		TopologyRequest: jobframework.PodSetTopologyRequest(
+			&lws.Spec.LeaderWorkerTemplate.WorkerTemplate.ObjectMeta,
+			// TODO: ???
+			ptr.To(kueuealpha.PodGroupPodIndexLabel),
```

  Same about TAS here, I don't see the need for rank-ordering for leader at all, it seems confusing.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T10:11:35Z

- **pkg/controller/jobframework/integrationmanager.go:65** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T10:11:34Z
```diff
@@ -61,6 +61,9 @@ type IntegrationCallbacks struct {
 	GVK schema.GroupVersionKind
 	// NewReconciler creates a new reconciler
 	NewReconciler ReconcilerFactory
+	// NewAdditionalReconcilers creates a new additional reconcilers
+	// (this callback is optional)
```

  In other places we start from a new line:
  
  https://github.com/kubernetes-sigs/kueue/blob/787758bfa570fd7972ded660f60b2a743cc12963/pkg/controller/jobframework/integrationmanager.go#L59-L60
  
  I would prefer to have the same to consistency. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-27T10:18:21Z

- **pkg/controller/jobframework/integrationmanager.go:65** by [@mimowo](https://github.com/mimowo) — 2025-01-27T10:18:21Z
```diff
@@ -61,6 +61,9 @@ type IntegrationCallbacks struct {
 	GVK schema.GroupVersionKind
 	// NewReconciler creates a new reconciler
 	NewReconciler ReconcilerFactory
+	// NewAdditionalReconcilers creates a new additional reconcilers
+	// (this callback is optional)
```

  Ah, sure, I didn't notice that, let's keep them consistent. Still, I would prefer to say just "NewAdditionalReconcilers creates additional reconcilers" (without "new", as it seems obvious as we create new objects).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T10:28:09Z

- **pkg/controller/jobframework/reconciler.go:438** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T10:28:09Z
```diff
@@ -428,8 +428,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				log.V(3).Info("Handling job with no workload found an existing workload")
 				return ctrl.Result{Requeue: true}, nil
 			}
+			// else if errors.Is(err, ErrPrebuildWorkloadNotFound) {
+			// TODO: ???
+			// log.V(3).Info("Handling job with no prebuild workload. Waiting one second...")
+			// return ctrl.Result{RequeueAfter: time.Second}, nil
+			// }
 			if IsUnretryableError(err) {
 				log.V(3).Info("Handling job with no workload", "unretryableError", err)
+				// TODO: If the error is non-retryable, should we return without an error?
```

  You're right. This is not related to this PR. I'll revert it.
  
  My only question is, shouldn't we ignore unretryable errors?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T10:31:52Z

- **pkg/controller/jobframework/integrationmanager.go:65** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T10:31:52Z
```diff
@@ -61,6 +61,9 @@ type IntegrationCallbacks struct {
 	GVK schema.GroupVersionKind
 	// NewReconciler creates a new reconciler
 	NewReconciler ReconcilerFactory
+	// NewAdditionalReconcilers creates a new additional reconcilers
+	// (this callback is optional)
```

  Done. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T10:36:25Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:188** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T10:36:24Z
```diff
@@ -0,0 +1,227 @@
+/*
+Copyright 2025 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package leaderworkerset
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	leaderworkersetv1 "sigs.k8s.io/lws/api/leaderworkerset/v1"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	leaderPodSetName = "leader"
+	workerPodSetName = "worker"
+)
+
+type Reconciler struct {
+	client          client.Client
+	record          record.EventRecorder
+	labelKeysToCopy []string
+}
+
+func NewReconciler(client client.Client, eventRecorder record.EventRecorder, opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	options := jobframework.ProcessOptions(opts...)
+
+	return &Reconciler{
+		client:          client,
+		record:          eventRecorder,
+		labelKeysToCopy: options.LabelKeysToCopy,
+	}
+}
+
+var _ jobframework.JobReconcilerInterface = (*Reconciler)(nil)
+
+func (r *Reconciler) SetupWithManager(mgr ctrl.Manager) error {
+	ctrl.Log.V(3).Info("Setting up LeaderWorkerSet reconciler")
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&leaderworkersetv1.LeaderWorkerSet{}).
+		Named("leaderworkerset").
+		WithEventFilter(r).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=leaderworkerset.x-k8s.io,resources=leaderworkersets,verbs=get;list;watch
+
+func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	lws := &leaderworkersetv1.LeaderWorkerSet{}
+	err := r.client.Get(ctx, req.NamespacedName, lws)
+	if err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("leaderworkerset", klog.KObj(lws))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling LeaderWorkerSet")
+
+	err = r.createPrebuildWorkloadsIfNotExist(ctx, lws)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadsIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet) error {
+	replicas := ptr.Deref(lws.Spec.Replicas, 1)
+	for i := int32(0); i < replicas; i++ {
+		if err := r.createPrebuildWorkloadIfNotExist(ctx, lws, i); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, client.ObjectKey{Name: GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)), Namespace: lws.Namespace}, wl)
+	// Ignore if the Workload already exists or an error occurs.
+	if err == nil || client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err := r.createPrebuildWorkload(ctx, lws, index); err != nil {
+		return err
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkload(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	createdWorkload := r.constructWorkload(lws, index)
+	err := r.client.Create(ctx, createdWorkload)
+	if err != nil {
+		return err
+	}
+	r.record.Eventf(
+		lws, corev1.EventTypeNormal, jobframework.ReasonCreatedWorkload,
+		"Created Workload: %v", workload.Key(createdWorkload),
+	)
+	return nil
+}
+
+func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, index int32) *kueue.Workload {
+	wl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)),
+			Namespace:   lws.Namespace,
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: admissioncheck.FilterProvReqAnnotations(lws.GetAnnotations()),
+		},
+		Spec: kueue.WorkloadSpec{
+			QueueName:                   jobframework.QueueNameForObject(lws),
+			PodSets:                     r.podSets(lws),
+			MaximumExecutionTimeSeconds: jobframework.MaximumExecutionTimeSecondsForObject(lws),
+		},
+	}
+
+	wl.Annotations[podcontroller.IsGroupWorkloadAnnotationKey] = podcontroller.IsGroupWorkloadAnnotationValue
+
+	return wl
+}
+
+func (r *Reconciler) podSets(lws *leaderworkersetv1.LeaderWorkerSet) []kueue.PodSet {
+	podSets := make([]kueue.PodSet, 0, 2)
+
+	if lws.Spec.LeaderWorkerTemplate.LeaderTemplate != nil {
+		podSets = append(podSets, kueue.PodSet{
+			Name:  leaderPodSetName,
+			Count: 1,
+			Template: corev1.PodTemplateSpec{
+				Spec: *lws.Spec.LeaderWorkerTemplate.LeaderTemplate.Spec.DeepCopy(),
+			},
+			TopologyRequest: jobframework.PodSetTopologyRequest(
+				&lws.Spec.LeaderWorkerTemplate.LeaderTemplate.ObjectMeta,
+				// TODO: ???
+				ptr.To(kueuealpha.PodGroupPodIndexLabel),
+				nil,
+				nil,
+			),
+		})
+	}
+
+	defaultPodSetName := kueue.DefaultPodSetName
+	if len(podSets) > 0 {
+		defaultPodSetName = workerPodSetName
+	}
+
+	defaultPodSetCount := ptr.Deref(lws.Spec.LeaderWorkerTemplate.Size, 1)
+	if len(podSets) > 0 {
+		defaultPodSetCount--
+	}
+
+	podSets = append(podSets, kueue.PodSet{
+		Name:  defaultPodSetName,
+		Count: defaultPodSetCount,
+		Template: corev1.PodTemplateSpec{
+			Spec: *lws.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.DeepCopy(),
+		},
+		TopologyRequest: jobframework.PodSetTopologyRequest(
+			&lws.Spec.LeaderWorkerTemplate.WorkerTemplate.ObjectMeta,
+			// TODO: ???
+			ptr.To(kueuealpha.PodGroupPodIndexLabel),
```

  Yeah, that was my questions. Reverted it for now. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T10:38:09Z

- **pkg/controller/jobframework/reconciler.go:239** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T10:38:09Z
```diff
@@ -236,7 +236,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	ctx = ctrl.LoggerInto(ctx, log)
 
 	defer func() {
-		err = r.ignoreUnretryableError(log, err)
+		err = IgnoreUnretryableError(log, err)
```

  Reverted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-27T10:38:58Z

- **pkg/controller/jobframework/reconciler.go:438** by [@mimowo](https://github.com/mimowo) — 2025-01-27T10:38:58Z
```diff
@@ -428,8 +428,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				log.V(3).Info("Handling job with no workload found an existing workload")
 				return ctrl.Result{Requeue: true}, nil
 			}
+			// else if errors.Is(err, ErrPrebuildWorkloadNotFound) {
+			// TODO: ???
+			// log.V(3).Info("Handling job with no prebuild workload. Waiting one second...")
+			// return ctrl.Result{RequeueAfter: time.Second}, nil
+			// }
 			if IsUnretryableError(err) {
 				log.V(3).Info("Handling job with no workload", "unretryableError", err)
+				// TODO: If the error is non-retryable, should we return without an error?
```

  I'm not sure - my question is - what is the user-facing or admin-facing difference? In other words: what is the issue the code aims to solve. Otherwise, it is really hard for me to answer. I'm not principal in the context -we should ignore or not, I'm putting the UX  (user experience) first.
  
  If there is no user-facing difference, then I prefer simplicity. If there is a user facing difference, then we can discuss what is the best way to improve the UX.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T13:12:43Z

- **pkg/controller/jobframework/reconciler.go:360** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T13:12:43Z
```diff
@@ -357,9 +357,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 1. make sure there is only a single existing instance of the workload.
 	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := r.ensureOneWorkload(ctx, job, object)
-	if err != nil {
-		return ctrl.Result{}, err
+	wl, requeue, err := r.ensureOneWorkload(ctx, job, object)
```

  You right. We can avoid requeue. Reverted. Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-27T13:15:38Z

- **pkg/controller/jobframework/reconciler.go:774** by [@mimowo](https://github.com/mimowo) — 2025-01-27T13:15:38Z
```diff
@@ -731,29 +742,36 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+func EnsurePrebuiltWorkloadOwnership(ctx context.Context, c client.Client, wl *kueue.Workload, object client.Object) error {
 	if !metav1.IsControlledBy(wl, object) {
-		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
-			// don't return an error here, since a retry cannot give a different result,
-			// log the error.
-			log := ctrl.LoggerFrom(ctx)
-			log.Error(err, "Cannot take ownership of the workload")
-			return false, nil
+		if err := ctrl.SetControllerReference(object, wl, c.Scheme()); err != nil {
+			return err
 		}
 
 		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
 			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
 		}
 
-		if err := r.client.Update(ctx, wl); err != nil {
-			return false, err
+		if err := c.Update(ctx, wl); err != nil {
+			return err
 		}
 	}
-	return true, nil
+	return nil
 }
 
 func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *kueue.Workload, job GenericJob) (bool, error) {
-	if equivalent, err := equivalentToWorkload(ctx, r.client, job, wl); !equivalent || err != nil {
+	var (
+		equivalent bool
+		err        error
+	)
+
+	if cj, implements := job.(ComposableJob); implements {
+		equivalent, err = cj.EquivalentToWorkload(ctx, r.client, wl)
+	} else {
+		equivalent, err = EquivalentToWorkload(ctx, r.client, job, wl)
+	}
```

  This looks a little bit weird, can we re-use the same interface? or at least wrap the logic in a function a before, say `equivalentToWorkload`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T13:22:37Z

- **pkg/controller/jobframework/reconciler.go:741** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T13:22:37Z
```diff
@@ -731,29 +751,36 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+func EnsurePrebuiltWorkloadOwnership(ctx context.Context, c client.Client, wl *kueue.Workload, object client.Object) (bool, error) {
 	if !metav1.IsControlledBy(wl, object) {
-		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
-			// don't return an error here, since a retry cannot give a different result,
-			// log the error.
-			log := ctrl.LoggerFrom(ctx)
-			log.Error(err, "Cannot take ownership of the workload")
-			return false, nil
```

  Done on https://github.com/kubernetes-sigs/kueue/pull/4069.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T16:48:51Z

- **pkg/controller/jobframework/reconciler.go:438** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T16:48:51Z
```diff
@@ -428,8 +428,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 				log.V(3).Info("Handling job with no workload found an existing workload")
 				return ctrl.Result{Requeue: true}, nil
 			}
+			// else if errors.Is(err, ErrPrebuildWorkloadNotFound) {
+			// TODO: ???
+			// log.V(3).Info("Handling job with no prebuild workload. Waiting one second...")
+			// return ctrl.Result{RequeueAfter: time.Second}, nil
+			// }
 			if IsUnretryableError(err) {
 				log.V(3).Info("Handling job with no workload", "unretryableError", err)
+				// TODO: If the error is non-retryable, should we return without an error?
```

  Ah, I see. It handles it on:
  
  https://github.com/kubernetes-sigs/kueue/blob/2d52e6e07648baf0ae1d247877b15f6ad00f5205/pkg/controller/jobframework/reconciler.go#L239-L241

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-28T08:46:46Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:144** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-28T08:46:46Z
```diff
@@ -0,0 +1,227 @@
+/*
+Copyright 2025 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package leaderworkerset
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	leaderworkersetv1 "sigs.k8s.io/lws/api/leaderworkerset/v1"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	podcontroller "sigs.k8s.io/kueue/pkg/controller/jobs/pod"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	leaderPodSetName = "leader"
+	workerPodSetName = "worker"
+)
+
+type Reconciler struct {
+	client          client.Client
+	record          record.EventRecorder
+	labelKeysToCopy []string
+}
+
+func NewReconciler(client client.Client, eventRecorder record.EventRecorder, opts ...jobframework.Option) jobframework.JobReconcilerInterface {
+	options := jobframework.ProcessOptions(opts...)
+
+	return &Reconciler{
+		client:          client,
+		record:          eventRecorder,
+		labelKeysToCopy: options.LabelKeysToCopy,
+	}
+}
+
+var _ jobframework.JobReconcilerInterface = (*Reconciler)(nil)
+
+func (r *Reconciler) SetupWithManager(mgr ctrl.Manager) error {
+	ctrl.Log.V(3).Info("Setting up LeaderWorkerSet reconciler")
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&leaderworkersetv1.LeaderWorkerSet{}).
+		Named("leaderworkerset").
+		WithEventFilter(r).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=leaderworkerset.x-k8s.io,resources=leaderworkersets,verbs=get;list;watch
+
+func (r *Reconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	lws := &leaderworkersetv1.LeaderWorkerSet{}
+	err := r.client.Get(ctx, req.NamespacedName, lws)
+	if err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("leaderworkerset", klog.KObj(lws))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling LeaderWorkerSet")
+
+	err = r.createPrebuildWorkloadsIfNotExist(ctx, lws)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+
+	return ctrl.Result{}, nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadsIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet) error {
+	replicas := ptr.Deref(lws.Spec.Replicas, 1)
+	for i := int32(0); i < replicas; i++ {
+		if err := r.createPrebuildWorkloadIfNotExist(ctx, lws, i); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkloadIfNotExist(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, client.ObjectKey{Name: GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)), Namespace: lws.Namespace}, wl)
+	// Ignore if the Workload already exists or an error occurs.
+	if err == nil || client.IgnoreNotFound(err) != nil {
+		return err
+	}
+	if err := r.createPrebuildWorkload(ctx, lws, index); err != nil {
+		return err
+	}
+	return nil
+}
+
+func (r *Reconciler) createPrebuildWorkload(ctx context.Context, lws *leaderworkersetv1.LeaderWorkerSet, index int32) error {
+	createdWorkload := r.constructWorkload(lws, index)
+	err := r.client.Create(ctx, createdWorkload)
+	if err != nil {
+		return err
+	}
+	r.record.Eventf(
+		lws, corev1.EventTypeNormal, jobframework.ReasonCreatedWorkload,
+		"Created Workload: %v", workload.Key(createdWorkload),
+	)
+	return nil
+}
+
+func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, index int32) *kueue.Workload {
+	wl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        GetWorkloadName(lws.UID, lws.Name, fmt.Sprint(index)),
+			Namespace:   lws.Namespace,
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: admissioncheck.FilterProvReqAnnotations(lws.GetAnnotations()),
+		},
+		Spec: kueue.WorkloadSpec{
+			QueueName:                   jobframework.QueueNameForObject(lws),
+			PodSets:                     r.podSets(lws),
+			MaximumExecutionTimeSeconds: jobframework.MaximumExecutionTimeSecondsForObject(lws),
+		},
+	}
```

  Done on a351825.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-28T08:53:41Z

- **pkg/controller/jobframework/reconciler.go:774** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-28T08:53:41Z
```diff
@@ -731,29 +742,36 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+func EnsurePrebuiltWorkloadOwnership(ctx context.Context, c client.Client, wl *kueue.Workload, object client.Object) error {
 	if !metav1.IsControlledBy(wl, object) {
-		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
-			// don't return an error here, since a retry cannot give a different result,
-			// log the error.
-			log := ctrl.LoggerFrom(ctx)
-			log.Error(err, "Cannot take ownership of the workload")
-			return false, nil
+		if err := ctrl.SetControllerReference(object, wl, c.Scheme()); err != nil {
+			return err
 		}
 
 		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
 			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
 		}
 
-		if err := r.client.Update(ctx, wl); err != nil {
-			return false, err
+		if err := c.Update(ctx, wl); err != nil {
+			return err
 		}
 	}
-	return true, nil
+	return nil
 }
 
 func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *kueue.Workload, job GenericJob) (bool, error) {
-	if equivalent, err := equivalentToWorkload(ctx, r.client, job, wl); !equivalent || err != nil {
+	var (
+		equivalent bool
+		err        error
+	)
+
+	if cj, implements := job.(ComposableJob); implements {
+		equivalent, err = cj.EquivalentToWorkload(ctx, r.client, wl)
+	} else {
+		equivalent, err = EquivalentToWorkload(ctx, r.client, job, wl)
+	}
```

  > This looks a little bit weird, can we re-use the same interface?
  
  We need to reuse this function for single Pod:
  
  https://github.com/kubernetes-sigs/kueue/blob/a351825cb959a697ca3751d274009579249557af/pkg/controller/jobs/pod/pod_controller.go#L1292-L1295

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-28T14:17:00Z

- **pkg/controller/jobframework/reconciler.go:627** by [@mimowo](https://github.com/mimowo) — 2025-01-28T14:17:00Z
```diff
@@ -622,7 +622,16 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			return nil, nil
 		}
 
-		if err := r.ensurePrebuiltWorkloadOwnership(ctx, wl, object); err != nil {
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
+		}
```

  I remember the analogous check in https://github.com/kubernetes-sigs/kueue/pull/4069, and we added a warning log line. Should we do it here too?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-28T14:17:25Z

- **pkg/controller/jobframework/reconciler.go:722** by [@mimowo](https://github.com/mimowo) — 2025-01-28T14:17:25Z
```diff
@@ -709,7 +718,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	}
 
 	if toUpdate != nil {
-		return r.updateWorkloadToMatchJob(ctx, job, object, toUpdate)
+		wl, err := r.updateWorkloadToMatchJob(ctx, job, object, toUpdate)
+		return wl, err
```

  ```suggestion
  		return r.updateWorkloadToMatchJob(ctx, job, object, toUpdate)
  ```
  ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-28T14:19:26Z

- **pkg/controller/jobframework/reconciler.go:774** by [@mimowo](https://github.com/mimowo) — 2025-01-28T14:19:26Z
```diff
@@ -731,29 +742,36 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+func EnsurePrebuiltWorkloadOwnership(ctx context.Context, c client.Client, wl *kueue.Workload, object client.Object) error {
 	if !metav1.IsControlledBy(wl, object) {
-		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
-			// don't return an error here, since a retry cannot give a different result,
-			// log the error.
-			log := ctrl.LoggerFrom(ctx)
-			log.Error(err, "Cannot take ownership of the workload")
-			return false, nil
+		if err := ctrl.SetControllerReference(object, wl, c.Scheme()); err != nil {
+			return err
 		}
 
 		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
 			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
 		}
 
-		if err := r.client.Update(ctx, wl); err != nil {
-			return false, err
+		if err := c.Update(ctx, wl); err != nil {
+			return err
 		}
 	}
-	return true, nil
+	return nil
 }
 
 func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *kueue.Workload, job GenericJob) (bool, error) {
-	if equivalent, err := equivalentToWorkload(ctx, r.client, job, wl); !equivalent || err != nil {
+	var (
+		equivalent bool
+		err        error
+	)
+
+	if cj, implements := job.(ComposableJob); implements {
+		equivalent, err = cj.EquivalentToWorkload(ctx, r.client, wl)
+	} else {
+		equivalent, err = EquivalentToWorkload(ctx, r.client, job, wl)
+	}
```

  What I mean is, can we keep a local function `equivalentToWorkload`, but do the dispatching 
  ```
  if cj, implements := job.(ComposableJob); implements {
  		equivalent, err = cj.EquivalentToWorkload(ctx, r.client, wl)
  	} else {
  		equivalent, err = EquivalentToWorkload(ctx, r.client, job, wl)
  	}
  ```
  inside that function?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-28T14:21:08Z

- **pkg/controller/jobs/pod/pod_controller.go:629** by [@mimowo](https://github.com/mimowo) — 2025-01-28T14:21:08Z
```diff
@@ -616,24 +608,38 @@ func (p *Pod) constructGroupPodSets() ([]kueue.PodSet, error) {
 		if err != nil {
 			return nil, err
 		}
-		return constructGroupPodSetsFast(p, tc)
+		return constructGroupPodSetsFast(p.list.Items, tc)
 	}
 	return constructGroupPodSets(p.list.Items)
 }
 
-func constructGroupPodSetsFast(p *Pod, groupTotalCount int) ([]kueue.PodSet, error) {
-	for _, podInGroup := range p.list.Items {
+func constructPodSets(p *corev1.Pod) []kueue.PodSet {
+	return []kueue.PodSet{
+		constructPodSet(p),
+	}
+}
+
+func constructPodSet(p *corev1.Pod) kueue.PodSet {
+	return kueue.PodSet{
+		Name:  kueue.DefaultPodSetName,
+		Count: 1,
+		Template: corev1.PodTemplateSpec{
+			Spec: *p.Spec.DeepCopy(),
+		},
+		TopologyRequest: jobframework.PodSetTopologyRequest(&p.ObjectMeta, ptr.To(kueuealpha.PodGroupPodIndexLabel), nil, nil),
```

  As mentioned , I don't think `PodGroupPodIndexLabel` would work well for rank-based ordering. I suggest to drop it for now, and do properly in a follow up along with e2e tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-28T14:26:04Z

- **pkg/controller/jobframework/reconciler.go:627** by [@mimowo](https://github.com/mimowo) — 2025-01-28T14:26:04Z
```diff
@@ -622,7 +622,16 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			return nil, nil
 		}
 
-		if err := r.ensurePrebuiltWorkloadOwnership(ctx, wl, object); err != nil {
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
+		}
```

  But ideally, the code is not duplicated at all - we have it wrapped in a function which is called from both places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-28T14:38:03Z

- **pkg/controller/jobframework/reconciler.go:774** by [@mimowo](https://github.com/mimowo) — 2025-01-28T14:38:03Z
```diff
@@ -731,29 +742,36 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+func EnsurePrebuiltWorkloadOwnership(ctx context.Context, c client.Client, wl *kueue.Workload, object client.Object) error {
 	if !metav1.IsControlledBy(wl, object) {
-		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
-			// don't return an error here, since a retry cannot give a different result,
-			// log the error.
-			log := ctrl.LoggerFrom(ctx)
-			log.Error(err, "Cannot take ownership of the workload")
-			return false, nil
+		if err := ctrl.SetControllerReference(object, wl, c.Scheme()); err != nil {
+			return err
 		}
 
 		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
 			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
 		}
 
-		if err := r.client.Update(ctx, wl); err != nil {
-			return false, err
+		if err := c.Update(ctx, wl); err != nil {
+			return err
 		}
 	}
-	return true, nil
+	return nil
 }
 
 func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *kueue.Workload, job GenericJob) (bool, error) {
-	if equivalent, err := equivalentToWorkload(ctx, r.client, job, wl); !equivalent || err != nil {
+	var (
+		equivalent bool
+		err        error
+	)
+
+	if cj, implements := job.(ComposableJob); implements {
+		equivalent, err = cj.EquivalentToWorkload(ctx, r.client, wl)
+	} else {
+		equivalent, err = EquivalentToWorkload(ctx, r.client, job, wl)
+	}
```

  After the second reading I think it is ok to keep as is. I was by both `EquivalentToWorkload` and `EquivalentToWorkload`, but with different arguments. I think something could be improved here, but I don't have a specific call to action. Feel free to mark as resolved.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-28T14:49:51Z

- **pkg/controller/jobframework/reconciler.go:627** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-28T14:49:51Z
```diff
@@ -622,7 +622,16 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			return nil, nil
 		}
 
-		if err := r.ensurePrebuiltWorkloadOwnership(ctx, wl, object); err != nil {
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
+		}
```

  Good catch! It was missed during rebasing. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-28T14:58:28Z

- **pkg/controller/jobs/pod/pod_controller.go:629** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-28T14:58:28Z
```diff
@@ -616,24 +608,38 @@ func (p *Pod) constructGroupPodSets() ([]kueue.PodSet, error) {
 		if err != nil {
 			return nil, err
 		}
-		return constructGroupPodSetsFast(p, tc)
+		return constructGroupPodSetsFast(p.list.Items, tc)
 	}
 	return constructGroupPodSets(p.list.Items)
 }
 
-func constructGroupPodSetsFast(p *Pod, groupTotalCount int) ([]kueue.PodSet, error) {
-	for _, podInGroup := range p.list.Items {
+func constructPodSets(p *corev1.Pod) []kueue.PodSet {
+	return []kueue.PodSet{
+		constructPodSet(p),
+	}
+}
+
+func constructPodSet(p *corev1.Pod) kueue.PodSet {
+	return kueue.PodSet{
+		Name:  kueue.DefaultPodSetName,
+		Count: 1,
+		Template: corev1.PodTemplateSpec{
+			Spec: *p.Spec.DeepCopy(),
+		},
+		TopologyRequest: jobframework.PodSetTopologyRequest(&p.ObjectMeta, ptr.To(kueuealpha.PodGroupPodIndexLabel), nil, nil),
```

  This is the pod controller. But you're right, I haven't removed it from the leaderworkerset yet.
