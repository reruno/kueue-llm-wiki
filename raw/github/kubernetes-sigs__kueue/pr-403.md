# PR #403: WIP Fix recover lent quota from cohort

**Summary**: WIP Fix recover lent quota from cohort

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/403

**Last updated**: 2022-12-13T21:07:45Z

---

## Metadata

- **State**: closed
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `recover-borrowed` → `main`
- **Created**: 2022-09-20T21:06:42Z
- **Updated**: 2022-12-13T21:07:45Z
- **Merged**: —
- **Closed**: 2022-12-13T21:07:44Z
- **Labels**: `kind/bug`, `size/L`, `approved`, `needs-rebase`, `do-not-merge/work-in-progress`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+79 / -72**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

When there is a ClusterQueue under its min quota with pending workloads, workloads from other ClusterQueues in the cohort can't continue borrowing.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

I plan to cherry pick this to 0.2

WIP because I need to add a release note and unit test.

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-09-20T21:06:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/403#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-09-20T21:13:49Z

Actually, the fix might be wrong, as `e.borrows` is always empty when the workload doesn't fit.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-02T02:51:51Z

@alculquicondor: PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-02T13:32:49Z

This implementation is wrong. I'll reuse the integration test though.

> is there currently a mechanism to prevent a situation in which a non-borrowing workload which is non-admissible blocks borrowing in the entire cohort indefinitely?

Yes, BestEffortFIFO does that. The queue is only blocked while there are workloads in the active queue.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-13T21:07:40Z

/close
in favor of #475

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-13T21:07:44Z

@alculquicondor: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/403#issuecomment-1349690403):

>/close
>in favor of #475 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-09-20T21:38:32Z

In the future, we can allow borrowing in this case based on when the job is expected to run when we add support for workload runtime and run by durations. 

But, in the meantime, I wonder if we can make this a configurable option; for example, we could expose a configuration to specify a minimum percentage of the quota to be available whenever there is a pending workload. Just trying to brainstorm here.

- **pkg/scheduler/scheduler.go:112** by [@ahg-g](https://github.com/ahg-g) — 2022-09-20T21:27:09Z
```diff
@@ -107,19 +107,20 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	// 4. Sort entries based on borrowing and timestamps.
 	sort.Sort(entryOrdering(entries))
 
-	// 5. Admit entries, ensuring that no more than one workload gets
-	// admitted by a cohort (if borrowing).
-	// This is because there can be other workloads deeper in a clusterQueue whose
-	// head got admitted that should be scheduled in the cohort before the heads
-	// of other clusterQueues.
-	usedCohorts := sets.NewString()
+	// 5. Admit entries, but disallow borrowing in the cohort there are
+	// workloads that don't require borrowing in a ClusterQueue of if other
+	// ClusterQueues borrow in this cycle.
```

  ```suggestion
  	// 5. Admit entries, but disallow borrowing in the cohort if there are
  	// workloads that don't require borrowing in a ClusterQueue or if other
  	// ClusterQueues borrow in this cycle.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-02T10:20:59Z

One nit and two higher-level questions:
- the Issue description says "When there is a ClusterQueue under its min quota with pending workloads", I wonder how (and if) this condition, about being under min quota, is checked in the current implementation.
- is there currently a mechanism to prevent a situation in which a non-borrowing workload which is non-admissible blocks borrowing in the entire cohort indefinitely?

- **pkg/scheduler/scheduler.go:113** by [@mimowo](https://github.com/mimowo) — 2022-12-02T10:13:30Z
```diff
@@ -107,19 +107,20 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	// 4. Sort entries based on borrowing and timestamps.
 	sort.Sort(entryOrdering(entries))
 
-	// 5. Admit entries, ensuring that no more than one workload gets
-	// admitted by a cohort (if borrowing).
-	// This is because there can be other workloads deeper in a clusterQueue whose
-	// head got admitted that should be scheduled in the cohort before the heads
-	// of other clusterQueues.
-	usedCohorts := sets.NewString()
+	// 5. Admit entries, but disallow borrowing in the cohort there are
+	// workloads that don't require borrowing in a ClusterQueue of if other
+	// ClusterQueues borrow in this cycle.
+	noBorrowingCohorts := sets.NewString()
```

  nit: Cohorts in this set may actually be borrowing already or start to borrow in this cycle. I suggest renaming it to indicate that further borrowing is suspended to reclaim the quota, maybe: `noMoreBorrowingCohorts`, or `suspendBorrowingCohorts`?
