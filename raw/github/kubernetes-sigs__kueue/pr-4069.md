# PR #4069: Ignore the workload is controlled by another object.

**Summary**: Ignore the workload is controlled by another object.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4069

**Last updated**: 2025-01-27T16:01:00Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/ignore-workload-is-controlled-by-another-object` → `main`
- **Created**: 2025-01-27T13:22:05Z
- **Updated**: 2025-01-27T16:01:00Z
- **Merged**: 2025-01-27T15:17:24Z
- **Closed**: 2025-01-27T15:17:24Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+14 / -9**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
We should ignore workloads controlled by another object but return an error if it occurs with something else.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
Prepare for https://github.com/kubernetes-sigs/kueue/pull/4023

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-01-27T13:22:25Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cc5a6f6d427915bfd70ea2cc9b8798d7d1ab067e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67979e5cac162300081bbd69 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-27T13:29:24Z

As in the other PR. could you please clarify what is the user observable impact so that ai can better assess if this is a bugfix / feature/ cleanup refactoring without any user facing changes?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T13:31:04Z

/kind cleanup

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-27T14:29:52Z

I have looked at the proposed change deeper and synced with @mbobrovskyi . I think the change is mostly just a cleanup, but there might be some corner cases when it fixes the behavior. Eg. the comment "don't return an error here, since a retry cannot give a different result, log the error" might not have been accurate, as technically the other owner reference might have been removed manually by an admin. However, since the difference is only in marginal corner cases I'm ok to handle it as a cleanup for now.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-01-27T15:00:44Z

/lgtm 
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-27T15:00:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4069#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4069#issuecomment-2615996156" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-27T15:00:52Z

LGTM label has been added.  <details>Git tree hash: acfda424c26ea1f5fceb60a534ca28dc60f25b18</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-27T14:19:12Z

- **pkg/controller/jobframework/reconciler.go:622** by [@mimowo](https://github.com/mimowo) — 2025-01-27T14:19:12Z
```diff
@@ -612,8 +613,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		if err != nil {
 			return nil, client.IgnoreNotFound(err)
 		}
+		// Ignore the workload is controlled by another object.
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
```

  IIUC this situation should not really happen normally, right? Or can this be created with normal using of Kueue?
  Previously we logged an error in that case, but now we ignore completely. 
  
  I would suggest to log some message like: `The workload is already controlled by another object`, "objectRef", klog.KObj(object))`, maybe even prepend it with "WARNING: The workload is already controlled by another object...`, WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T14:20:50Z

- **pkg/controller/jobframework/reconciler.go:622** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T14:20:50Z
```diff
@@ -612,8 +613,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		if err != nil {
 			return nil, client.IgnoreNotFound(err)
 		}
+		// Ignore the workload is controlled by another object.
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
```

  > I would suggest to log some message like: The workload is already controlled by another object, "objectRef", klog.KObj(object)), maybe even prepend it with "WARNING: The workload is already controlled by another object..., WDYT?
  
  Agree. This is a good point. I will add it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T14:26:59Z

- **pkg/controller/jobframework/reconciler.go:622** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T14:26:59Z
```diff
@@ -612,8 +613,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		if err != nil {
 			return nil, client.IgnoreNotFound(err)
 		}
+		// Ignore the workload is controlled by another object.
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
```

  > IIUC this situation should not really happen normally, right? Or can this be created with normal using of Kueue?
  
  Yes, exactly, it shouldn't happens. But I think it can happens with `PrebuiltWorkload` when user set invalid workload name.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-27T14:43:37Z

- **pkg/controller/jobframework/reconciler.go:622** by [@mimowo](https://github.com/mimowo) — 2025-01-27T14:43:37Z
```diff
@@ -612,8 +613,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		if err != nil {
 			return nil, client.IgnoreNotFound(err)
 		}
+		// Ignore the workload is controlled by another object.
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
```

  I see , but in any case this is not desired. let's prefix it with warning

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-01-27T14:57:00Z

- **pkg/controller/jobframework/reconciler.go:622** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-27T14:57:00Z
```diff
@@ -612,8 +613,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		if err != nil {
 			return nil, client.IgnoreNotFound(err)
 		}
+		// Ignore the workload is controlled by another object.
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
```

  Done. 
  
  I also tested it and got the following logs:
  
  ```
      reconciler.go:356: "level"=2 "msg"="Reconciling Job" "job"="ns/job" "gvk"="batch/v1, Kind=Job"
      reconciler.go:617: "level"=2 "msg"="WARNING: The workload is already controlled by another object" "job"="ns/job" "gvk"="batch/v1, Kind=Job" "workload"={"name"="prebuilt-workload" "namespace"="ns"} "controlledBy"="&OwnerReference{Kind:Job,Name:other-job,UID:other-uid,APIVersion:batch/v1,Controller:*true,BlockOwnerDeletion:*true,}"
      reconciler.go:434: "msg"="Handling job with no workload" "error"="prebuild workload not found" "job"="ns/job" "gvk"="batch/v1, Kind=Job"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-01-27T15:00:04Z

- **pkg/controller/jobframework/reconciler.go:622** by [@mimowo](https://github.com/mimowo) — 2025-01-27T15:00:04Z
```diff
@@ -612,8 +613,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		if err != nil {
 			return nil, client.IgnoreNotFound(err)
 		}
+		// Ignore the workload is controlled by another object.
+		if controllerutil.HasControllerReference(wl) && !metav1.IsControlledBy(wl, object) {
+			return nil, nil
```

  lgtm
