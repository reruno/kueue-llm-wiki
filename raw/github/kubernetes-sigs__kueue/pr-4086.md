# PR #4086: Create PodTemplate before ProvisioningRequest

**Summary**: Create PodTemplate before ProvisioningRequest

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4086

**Last updated**: 2025-02-03T16:38:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/create-pod-template-before-provision-request` → `main`
- **Created**: 2025-01-29T11:34:13Z
- **Updated**: 2025-02-03T16:38:54Z
- **Merged**: 2025-02-03T16:16:57Z
- **Closed**: 2025-02-03T16:16:57Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 3   **+359 / -223**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Create `PodTemplate` before `ProvisioningRequest`.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3957

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix a bug that occurs when a PodTemplate has not been created yet, but the Cluster Autoscaler attempts to process the ProvisioningRequest and marks it as failed.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-01-29T11:34:16Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-01-29T11:34:30Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cba64f408c49419ddbad1bd1fdc49eaee4069f85 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67a0e6a925e45c000823b508 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-01-29T13:45:08Z

/cc @PBundyra

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T12:47:02Z

> LGTM overall, but makes me wonder - what happens if the PodTemplate creation succeeds, but the ProvisioingRequest creation fails. Are we garbage collecting / deleting PodTemplates in some way?

That's a good question - I've been thinking about it too. I'm not sure how we can garbage collect it effectively. Maybe we should temporarily set ControllerReference to the Workload before we set it to ProvisionRequest? WDYT?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-03T12:59:51Z

sounds reasonable. if we do so they get deleted when the workload is deleted.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T14:54:46Z

> sounds reasonable. if we do so they get deleted when the workload is deleted.

Done

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-03T15:48:34Z

@mbobrovskyi please squash the commits, I find the manual cherry-picking works better in that case

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-03T16:12:54Z

LGTM label has been added.  <details>Git tree hash: fea2566b7e3ad48d2a686b0556af8884834b8243</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-03T16:12:55Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4086#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4086#pullrequestreview-2590401112" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-01-29T15:56:19Z

- **pkg/controller/admissionchecks/provisioning/controller.go:312** by [@PBundyra](https://github.com/PBundyra) — 2025-01-29T15:56:18Z
```diff
@@ -293,16 +297,62 @@ func (c *Controller) syncOwnedProvisionRequest(
 				if !psFound || !psaFound {
 					return nil, errInconsistentPodSetAssignments
 				}
-				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+
+				pt := &corev1.PodTemplate{}
+				ptKey := types.NamespacedName{
+					Namespace: wl.Namespace,
+					Name:      getProvisioningRequestPodTemplateName(requestName, psName),
+				}
+
+				podSet := autoscaling.PodSet{
 					PodTemplateRef: autoscaling.Reference{
-						Name: getProvisioningRequestPodTemplateName(requestName, psName),
+						Name: ptKey.Name,
 					},
 					Count: ptr.Deref(psa.Count, ps.Count),
-				})
-			}
+				}
 
-			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
-				return nil, err
+				err := c.client.Get(ctx, ptKey, pt)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					// it's a not found, so create it
+					newPt := &corev1.PodTemplate{
+						ObjectMeta: metav1.ObjectMeta{
+							Name:      ptKey.Name,
+							Namespace: ptKey.Namespace,
+							Labels: map[string]string{
+								constants.ManagedByKueueLabel: "true",
+							},
+						},
+						Template: ps.Template,
+					}
+
+					// apply the admission node selectors to the Template
+					psi, err := podset.FromAssignment(ctx, c.client, psaMap[psName], podSet.Count)
+					if err != nil {
+						return nil, err
+					}
+
+					err = podset.Merge(&newPt.Template.ObjectMeta, &newPt.Template.Spec, psi)
+					if err != nil {
+						return nil, err
+					}
+
+					// copy limits to requests if needed
+					workload.UseLimitsAsMissingRequestsInPod(&newPt.Template.Spec)
+
+					if err = c.client.Create(ctx, newPt); err != nil {
+						msg := fmt.Sprintf("Error creating PodTemplate %q: %v", newPt.Name, err)
+						ac.Message = api.TruncateConditionMessage(msg)
+						workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *ac, c.clock)
+
+						c.record.Eventf(wl, corev1.EventTypeWarning, "FailedCreate", api.TruncateEventMessage(msg))
+						return nil, err
+					}
```

  Could we make a separate function for PodTemplate creation?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-01-29T15:57:28Z

- **pkg/controller/admissionchecks/provisioning/controller.go:429** by [@PBundyra](https://github.com/PBundyra) — 2025-01-29T15:57:28Z
```diff
@@ -390,38 +440,12 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *
 			return err
 		}
 
-		if err != nil {
-			// it's a not found, so create it
-			newPt := &corev1.PodTemplate{
-				ObjectMeta: metav1.ObjectMeta{
-					Name:      ptKey.Name,
-					Namespace: ptKey.Namespace,
-					Labels: map[string]string{
-						constants.ManagedByKueueLabel: "true",
-					},
-				},
-				Template: ps.Template,
-			}
-
-			// apply the admission node selectors to the Template
-			psi, err := podset.FromAssignment(ctx, c.client, psaMap[psName], reqPS.Count)
-			if err != nil {
-				return err
-			}
-
-			err = podset.Merge(&newPt.Template.ObjectMeta, &newPt.Template.Spec, psi)
-			if err != nil {
-				return err
-			}
-
-			// copy limits to requests if needed
-			workload.UseLimitsAsMissingRequestsInPod(&newPt.Template.Spec)
```

  Since we moved this part to different place, can we name the function accordingly?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-01-30T13:31:51Z

- **pkg/controller/admissionchecks/provisioning/controller.go:440** by [@PBundyra](https://github.com/PBundyra) — 2025-01-30T13:31:51Z
```diff
@@ -371,7 +421,7 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *
 	for i := range request.Spec.PodSets {
 		reqPS := &request.Spec.PodSets[i]
 		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
```

  Do we need those check? Is there any risk of race condition? We are already checking that in the `syncOwnedProvisionRequest`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-01-30T13:33:05Z

- **pkg/controller/admissionchecks/provisioning/controller.go:440** by [@PBundyra](https://github.com/PBundyra) — 2025-01-30T13:33:05Z
```diff
@@ -371,7 +421,7 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *
 	for i := range request.Spec.PodSets {
 		reqPS := &request.Spec.PodSets[i]
 		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
```

  I also wonder if we need to check if the PodSets are the same in Workload and ProvReq:
  https://github.com/kubernetes-sigs/kueue/pull/4086/files#diff-d5b88e9a8af6b97ce61788f1307ec9ba1f4e3581a9c0634a151ce310c9ca3d91R412-R416
  Since we set them a few lines above. Could you evaluate if there are any scenarios where such a need indeed occurs?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T06:56:48Z

- **pkg/controller/admissionchecks/provisioning/controller.go:312** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T06:56:48Z
```diff
@@ -293,16 +297,62 @@ func (c *Controller) syncOwnedProvisionRequest(
 				if !psFound || !psaFound {
 					return nil, errInconsistentPodSetAssignments
 				}
-				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+
+				pt := &corev1.PodTemplate{}
+				ptKey := types.NamespacedName{
+					Namespace: wl.Namespace,
+					Name:      getProvisioningRequestPodTemplateName(requestName, psName),
+				}
+
+				podSet := autoscaling.PodSet{
 					PodTemplateRef: autoscaling.Reference{
-						Name: getProvisioningRequestPodTemplateName(requestName, psName),
+						Name: ptKey.Name,
 					},
 					Count: ptr.Deref(psa.Count, ps.Count),
-				})
-			}
+				}
 
-			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
-				return nil, err
+				err := c.client.Get(ctx, ptKey, pt)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					// it's a not found, so create it
+					newPt := &corev1.PodTemplate{
+						ObjectMeta: metav1.ObjectMeta{
+							Name:      ptKey.Name,
+							Namespace: ptKey.Namespace,
+							Labels: map[string]string{
+								constants.ManagedByKueueLabel: "true",
+							},
+						},
+						Template: ps.Template,
+					}
+
+					// apply the admission node selectors to the Template
+					psi, err := podset.FromAssignment(ctx, c.client, psaMap[psName], podSet.Count)
+					if err != nil {
+						return nil, err
+					}
+
+					err = podset.Merge(&newPt.Template.ObjectMeta, &newPt.Template.Spec, psi)
+					if err != nil {
+						return nil, err
+					}
+
+					// copy limits to requests if needed
+					workload.UseLimitsAsMissingRequestsInPod(&newPt.Template.Spec)
+
+					if err = c.client.Create(ctx, newPt); err != nil {
+						msg := fmt.Sprintf("Error creating PodTemplate %q: %v", newPt.Name, err)
+						ac.Message = api.TruncateConditionMessage(msg)
+						workload.SetAdmissionCheckState(&wl.Status.AdmissionChecks, *ac, c.clock)
+
+						c.record.Eventf(wl, corev1.EventTypeWarning, "FailedCreate", api.TruncateEventMessage(msg))
+						return nil, err
+					}
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T09:23:51Z

- **pkg/controller/admissionchecks/provisioning/controller.go:429** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T09:23:50Z
```diff
@@ -390,38 +440,12 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *
 			return err
 		}
 
-		if err != nil {
-			// it's a not found, so create it
-			newPt := &corev1.PodTemplate{
-				ObjectMeta: metav1.ObjectMeta{
-					Name:      ptKey.Name,
-					Namespace: ptKey.Namespace,
-					Labels: map[string]string{
-						constants.ManagedByKueueLabel: "true",
-					},
-				},
-				Template: ps.Template,
-			}
-
-			// apply the admission node selectors to the Template
-			psi, err := podset.FromAssignment(ctx, c.client, psaMap[psName], reqPS.Count)
-			if err != nil {
-				return err
-			}
-
-			err = podset.Merge(&newPt.Template.ObjectMeta, &newPt.Template.Spec, psi)
-			if err != nil {
-				return err
-			}
-
-			// copy limits to requests if needed
-			workload.UseLimitsAsMissingRequestsInPod(&newPt.Template.Spec)
```

  I believe the function name is correct because we still need to sync controllerReference in PodTemplate. Do you have a different name in mind?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T09:26:19Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:274** by [@mimowo](https://github.com/mimowo) — 2025-02-03T09:26:18Z
```diff
@@ -303,7 +305,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			wantWorkloads: map[string]*kueue.Workload{
-				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				baseWorkload.GetName(): (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
```

  Is this change needed? I consider the PR for cherry-picking so would prefer the diff be as small as possible. It is just a test code, but still seems a bit of distraction, isn't it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T09:33:26Z

- **pkg/controller/admissionchecks/provisioning/controller.go:440** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T09:33:26Z
```diff
@@ -371,7 +421,7 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *
 	for i := range request.Spec.PodSets {
 		reqPS := &request.Spec.PodSets[i]
 		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T09:36:03Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:274** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T09:36:03Z
```diff
@@ -303,7 +305,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			wantWorkloads: map[string]*kueue.Workload{
-				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				baseWorkload.GetName(): (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
```

  Yeah, I need to clone the baseWorkload wrapper object somehow on the new test case, but the Name function is part of `WorkloadWrapper`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T09:37:47Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:274** by [@mimowo](https://github.com/mimowo) — 2025-02-03T09:37:46Z
```diff
@@ -303,7 +305,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			wantWorkloads: map[string]*kueue.Workload{
-				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				baseWorkload.GetName(): (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
```

  why not just use `DeepCopy`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T09:41:44Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:274** by [@mimowo](https://github.com/mimowo) — 2025-02-03T09:41:44Z
```diff
@@ -303,7 +305,7 @@ func TestReconcile(t *testing.T) {
 			workload: baseWorkload.DeepCopy(),
 			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
 			wantWorkloads: map[string]*kueue.Workload{
-				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				baseWorkload.GetName(): (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
```

  Ah, I see, because you use the `AdmissionChecks()`. You could probably use this trick:
  ```golang
  baseWorkload.Name: func() *kueue.Workload {
    x := baseWorkload.DeepCopy()
    x.Status = ... // some processing of the AdmissionChecks
    return x
  }(),
  ```
  but I agree Cloning the wrapper is easier, keep it as is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T12:20:52Z

- **pkg/controller/admissionchecks/provisioning/controller.go:245** by [@mimowo](https://github.com/mimowo) — 2025-02-03T12:20:52Z
```diff
@@ -241,17 +241,17 @@ func (c *Controller) syncOwnedProvisionRequest(
 			continue
 		}
 
-		oldPr, exists := activeOrLastPRForChecks[checkName]
+		req, exists := activeOrLastPRForChecks[checkName]
```

  why is this renamed? I think the previous name makes sense as this is a pre-existing provreq. Also, it feels like extending the diff unnecessarily for a bugfix PR which is going to be cherry-picked.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T12:21:04Z

- **pkg/controller/admissionchecks/provisioning/controller.go:269** by [@mimowo](https://github.com/mimowo) — 2025-02-03T12:21:04Z
```diff
@@ -266,10 +266,11 @@ func (c *Controller) syncOwnedProvisionRequest(
 		} else {
 			shouldCreatePr = true
 		}
+
```

  nit, revert

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T12:21:50Z

- **pkg/controller/admissionchecks/provisioning/controller.go:340** by [@mimowo](https://github.com/mimowo) — 2025-02-03T12:21:50Z
```diff
@@ -313,7 +337,8 @@ func (c *Controller) syncOwnedProvisionRequest(
 			c.record.Eventf(wl, corev1.EventTypeNormal, "ProvisioningRequestCreated", "Created ProvisioningRequest: %q", req.Name)
 			activeOrLastPRForChecks[checkName] = req
 		}
-		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, requestName, prc); err != nil {
+
```

  nit, revert empty line changes since this is to be cherry-picked, so to minimize changes of unnecessary conflicts.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T12:25:06Z

- **pkg/controller/admissionchecks/provisioning/controller.go:302** by [@mimowo](https://github.com/mimowo) — 2025-02-03T12:25:06Z
```diff
@@ -293,16 +298,35 @@ func (c *Controller) syncOwnedProvisionRequest(
 				if !psFound || !psaFound {
 					return nil, errInconsistentPodSetAssignments
 				}
-				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+
+				pt := &corev1.PodTemplate{}
```

  Should we declare the pr lower, just above the `Get`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T12:32:22Z

- **pkg/controller/admissionchecks/provisioning/controller.go:245** by [@mimowo](https://github.com/mimowo) — 2025-02-03T12:32:21Z
```diff
@@ -241,17 +241,17 @@ func (c *Controller) syncOwnedProvisionRequest(
 			continue
 		}
 
-		oldPr, exists := activeOrLastPRForChecks[checkName]
+		req, exists := activeOrLastPRForChecks[checkName]
```

  Ah, I see you use it later for `syncProvisionRequestsPodTemplates`, this makes sense to reuse the same name for the new and old provReq. Feel free to keep as is then.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T12:33:52Z

LGTM overall, but makes me wonder - what happens if the PodTemplate creation succeeds, but the ProvisioingRequest creation fails. Are we garbage collecting / deleting PodTemplates in some way?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T13:01:55Z

- **pkg/controller/admissionchecks/provisioning/controller.go:269** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T13:01:55Z
```diff
@@ -266,10 +266,11 @@ func (c *Controller) syncOwnedProvisionRequest(
 		} else {
 			shouldCreatePr = true
 		}
+
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T13:02:02Z

- **pkg/controller/admissionchecks/provisioning/controller.go:340** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T13:02:02Z
```diff
@@ -313,7 +337,8 @@ func (c *Controller) syncOwnedProvisionRequest(
 			c.record.Eventf(wl, corev1.EventTypeNormal, "ProvisioningRequestCreated", "Created ProvisioningRequest: %q", req.Name)
 			activeOrLastPRForChecks[checkName] = req
 		}
-		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, requestName, prc); err != nil {
+
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T13:04:47Z

- **pkg/controller/admissionchecks/provisioning/controller.go:429** by [@mimowo](https://github.com/mimowo) — 2025-02-03T13:04:47Z
```diff
@@ -390,38 +440,12 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *
 			return err
 		}
 
-		if err != nil {
-			// it's a not found, so create it
-			newPt := &corev1.PodTemplate{
-				ObjectMeta: metav1.ObjectMeta{
-					Name:      ptKey.Name,
-					Namespace: ptKey.Namespace,
-					Labels: map[string]string{
-						constants.ManagedByKueueLabel: "true",
-					},
-				},
-				Template: ps.Template,
-			}
-
-			// apply the admission node selectors to the Template
-			psi, err := podset.FromAssignment(ctx, c.client, psaMap[psName], reqPS.Count)
-			if err != nil {
-				return err
-			}
-
-			err = podset.Merge(&newPt.Template.ObjectMeta, &newPt.Template.Spec, psi)
-			if err != nil {
-				return err
-			}
-
-			// copy limits to requests if needed
-			workload.UseLimitsAsMissingRequestsInPod(&newPt.Template.Spec)
```

  I think both functions `createPodTemplate` and `UseLimitsAsMissingRequestsInPod` are named ok. 
  
  I'm not sure these names are best possible, but they don't strike me as misleading.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T15:16:11Z

- **pkg/controller/admissionchecks/provisioning/controller.go:302** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T15:16:11Z
```diff
@@ -293,16 +298,35 @@ func (c *Controller) syncOwnedProvisionRequest(
 				if !psFound || !psaFound {
 					return nil, errInconsistentPodSetAssignments
 				}
-				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+
+				pt := &corev1.PodTemplate{}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T15:17:07Z

LGTM, small comments only to make it more clear why we own the PodTemplate by workload and then transfer the ownership.

- **pkg/controller/admissionchecks/provisioning/controller.go:430** by [@mimowo](https://github.com/mimowo) — 2025-02-03T15:04:36Z
```diff
@@ -423,11 +427,25 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, requ
 			return err
 		}
 
+		var shouldUpdate bool
```

  Can we move the `shouldUpdate` under the `err == nil` case?

- **pkg/controller/admissionchecks/provisioning/controller.go:391** by [@mimowo](https://github.com/mimowo) — 2025-02-03T15:05:55Z
```diff
@@ -375,17 +376,20 @@ func (c *Controller) handleError(ctx context.Context, wl *kueue.Workload, ac *ku
 	return errors.Join(err, patchErr)
 }
 
-func (c *Controller) createPodTemplate(ctx context.Context, ptKey types.NamespacedName, ps *kueue.PodSet, psa *kueue.PodSetAssignment) (*corev1.PodTemplate, error) {
+func (c *Controller) createPodTemplate(ctx context.Context, wl *kueue.Workload, name string, ps *kueue.PodSet, psa *kueue.PodSetAssignment) (*corev1.PodTemplate, error) {
 	newPt := &corev1.PodTemplate{
 		ObjectMeta: metav1.ObjectMeta{
-			Name:      ptKey.Name,
-			Namespace: ptKey.Namespace,
+			Name:      name,
+			Namespace: wl.Namespace,
 			Labels: map[string]string{
 				constants.ManagedByKueueLabel: "true",
 			},
 		},
 		Template: ps.Template,
 	}
+	if err := ctrl.SetControllerReference(wl, newPt, c.client.Scheme()); err != nil {
```

  Add a comment to make it clear:
  ```
  // set the controller reference to workload so that the template is not left orphaned
  // if the ProvisioningRequest creation fails. The ownership is later transferred to the
  // ProvisioningRequest.
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:435** by [@mimowo](https://github.com/mimowo) — 2025-02-03T15:08:17Z
```diff
@@ -423,11 +427,25 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, requ
 			return err
 		}
 
+		var shouldUpdate bool
+
 		if err == nil {
-			if err := ctrl.SetControllerReference(request, pt, c.client.Scheme()); err != nil {
-				return err
+			if metav1.IsControlledBy(pt, wl) {
```

  Add a comment
  ```
  // transfer the ownership of the template to the ProvisioningRequest
  ```
  Actually, it seems we could keep the templates as owned by the workload, but since prior to the PR they were owned by ProvisioningRequests I think it makes sense to keep that behavior.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T15:23:38Z

- **pkg/controller/admissionchecks/provisioning/controller.go:435** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T15:23:38Z
```diff
@@ -423,11 +427,25 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, requ
 			return err
 		}
 
+		var shouldUpdate bool
+
 		if err == nil {
-			if err := ctrl.SetControllerReference(request, pt, c.client.Scheme()); err != nil {
-				return err
+			if metav1.IsControlledBy(pt, wl) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T15:23:45Z

- **pkg/controller/admissionchecks/provisioning/controller.go:391** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T15:23:45Z
```diff
@@ -375,17 +376,20 @@ func (c *Controller) handleError(ctx context.Context, wl *kueue.Workload, ac *ku
 	return errors.Join(err, patchErr)
 }
 
-func (c *Controller) createPodTemplate(ctx context.Context, ptKey types.NamespacedName, ps *kueue.PodSet, psa *kueue.PodSetAssignment) (*corev1.PodTemplate, error) {
+func (c *Controller) createPodTemplate(ctx context.Context, wl *kueue.Workload, name string, ps *kueue.PodSet, psa *kueue.PodSetAssignment) (*corev1.PodTemplate, error) {
 	newPt := &corev1.PodTemplate{
 		ObjectMeta: metav1.ObjectMeta{
-			Name:      ptKey.Name,
-			Namespace: ptKey.Namespace,
+			Name:      name,
+			Namespace: wl.Namespace,
 			Labels: map[string]string{
 				constants.ManagedByKueueLabel: "true",
 			},
 		},
 		Template: ps.Template,
 	}
+	if err := ctrl.SetControllerReference(wl, newPt, c.client.Scheme()); err != nil {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T15:23:53Z

- **pkg/controller/admissionchecks/provisioning/controller.go:430** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T15:23:52Z
```diff
@@ -423,11 +427,25 @@ func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, requ
 			return err
 		}
 
+		var shouldUpdate bool
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T15:50:44Z

- **pkg/controller/admissionchecks/provisioning/controller.go:309** by [@mimowo](https://github.com/mimowo) — 2025-02-03T15:50:40Z
```diff
@@ -293,9 +294,27 @@ func (c *Controller) syncOwnedProvisionRequest(
 				if !psFound || !psaFound {
 					return nil, errInconsistentPodSetAssignments
 				}
+
+				ptName := getProvisioningRequestPodTemplateName(requestName, psName)
+
+				pt := &corev1.PodTemplate{}
+				err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: ptName}, pt)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					// it's a not found, so create it
+					_, err := c.createPodTemplate(ctx, wl, ptName, ps, psa)
+					if err != nil {
+						msg := fmt.Sprintf("Error creating PodTemplate %q: %v", ptName, err)
```

  wrap it also in `TruncateEventMessage`, since we are exposing the `err` it might be arbitrarily long (may depend on the user webhook messages)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T15:53:49Z

- **pkg/controller/admissionchecks/provisioning/controller.go:309** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T15:53:49Z
```diff
@@ -293,9 +294,27 @@ func (c *Controller) syncOwnedProvisionRequest(
 				if !psFound || !psaFound {
 					return nil, errInconsistentPodSetAssignments
 				}
+
+				ptName := getProvisioningRequestPodTemplateName(requestName, psName)
+
+				pt := &corev1.PodTemplate{}
+				err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: ptName}, pt)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					// it's a not found, so create it
+					_, err := c.createPodTemplate(ctx, wl, ptName, ps, psa)
+					if err != nil {
+						msg := fmt.Sprintf("Error creating PodTemplate %q: %v", ptName, err)
```

  Good catch! Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-02-03T15:54:25Z

- **pkg/controller/admissionchecks/provisioning/controller.go:309** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-02-03T15:54:25Z
```diff
@@ -293,9 +294,27 @@ func (c *Controller) syncOwnedProvisionRequest(
 				if !psFound || !psaFound {
 					return nil, errInconsistentPodSetAssignments
 				}
+
+				ptName := getProvisioningRequestPodTemplateName(requestName, psName)
+
+				pt := &corev1.PodTemplate{}
+				err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: ptName}, pt)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					// it's a not found, so create it
+					_, err := c.createPodTemplate(ctx, wl, ptName, ps, psa)
+					if err != nil {
+						msg := fmt.Sprintf("Error creating PodTemplate %q: %v", ptName, err)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-03T16:12:48Z

/lgtm
/approve
/cherry-pick release-0.10 release-0.9
