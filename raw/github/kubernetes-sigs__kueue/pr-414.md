# PR #414: Add webhook to Job controller

**Summary**: Add webhook to Job controller

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/414

**Last updated**: 2022-11-24T23:57:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feat/add-webhook-to-job` → `main`
- **Created**: 2022-10-09T10:35:41Z
- **Updated**: 2022-11-24T23:57:07Z
- **Merged**: 2022-11-24T19:20:04Z
- **Closed**: 2022-11-24T19:20:04Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 11   **+421 / -38**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/412

#### Special notes for your reviewer:

## Discussion

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-11-23T11:23:15Z

Try to dig the error.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-11-23T15:36:02Z

> Try to dig the error.

Test error might because of I didn't rebase the main branch.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-23T16:13:25Z

It looks like the E2E test is working as intended.

Somehow it seems that the new webhook is not starting up.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-11-24T03:20:40Z

/test pull-kueue-test-e2e-main

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-11-24T04:45:46Z

hold for https://github.com/kubernetes-sigs/kueue/pull/437
/hold

### Comment by [@kannon92](https://github.com/kannon92) — 2022-11-24T13:06:19Z

Now that we have e2e tests, should we have a test in e2e that covers this feature?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-11-24T14:03:28Z

> Now that we have e2e tests, should we have a test in e2e that covers this feature?

E2e test usually plays a part in integration between different components, like job controller vs kueue controller, and it wastes more time. This webhook only impacts the job controller, so integration tests might be enough.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-24T16:28:34Z

Also, the test was already failing initially :)

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-24T16:29:28Z

Can you rebase and squash?

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-11-24T16:29:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/414#issuecomment-1326659493" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/414#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-11-24T16:50:01Z

Squashed.

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-11T17:48:00Z

- **pkg/controller/workload/job/job_webhook.go:33** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T17:41:12Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
```

  ```suggestion
  var webhookLog = ctrl.Log.WithName("job-webhook")
  ```
  
  given that this is in the job package, this has less chances of collision in the future.

- **pkg/controller/workload/job/job_webhook.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T17:42:48Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
```

  wouldn't this cause `manageJobsWithoutQueueName` to be ignored?

- **test/integration/controller/job/job_controller_test.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T17:44:48Z
```diff
@@ -52,20 +47,13 @@ const (
 	priorityValue     = 10
 )
 
-var (
-	cfg       *rest.Config
-	k8sClient client.Client
-	ctx       context.Context
-	fwk       *framework.Framework
-	crdPath   = filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases")
-)
-
 // +kubebuilder:docs-gen:collapse=Imports
 
 var _ = ginkgo.Describe("Job controller", func() {
+
 	ginkgo.BeforeEach(func() {
 		fwk = &framework.Framework{
-			ManagerSetup: managerSetup(job.WithManageJobsWithoutQueueName(true)),
```

  We should have a test for false

- **pkg/controller/workload/job/job_webhook.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-11T17:47:58Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	return nil
+}
```

  Can you think of any validation we can do during update?
  
  If it involves querying the client to see the status of the Workload, perhaps it's not appropriate, as the cache could be out-of-date.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-12T11:10:37Z

- **pkg/controller/workload/job/job_webhook.go:70** by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T11:04:04Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
```

  manageJobsWithoutQueueName only takes effect when it's true and queue name is empty, we already handled this above.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-12T11:30:18Z

- **pkg/controller/workload/job/job_webhook.go:71** by [@kerthcet](https://github.com/kerthcet) — 2022-10-12T11:30:18Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	return nil
+}
```

  Good point, we should handler job suspend first in job reconciler.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-12T13:57:57Z

- **pkg/controller/workload/job/job_webhook.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-12T13:57:57Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
```

  if manageJobWithoutQueueName is false, the above condition would be false, but this condition would be true.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-27T02:57:48Z

- **pkg/controller/workload/job/job_webhook.go:70** by [@kerthcet](https://github.com/kerthcet) — 2022-10-27T02:57:48Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
```

  ```
  if manageJobWithoutQueueName == False && queueName(job) != "" && *job.Spec.Suspend == False {
      		job.Spec.Suspend = pointer.Bool(true)
  }
  ```
  Isn't this right?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-27T07:05:08Z

- **test/integration/controller/job/job_controller_test.go:68** by [@kerthcet](https://github.com/kerthcet) — 2022-10-27T07:05:08Z
```diff
@@ -52,20 +47,13 @@ const (
 	priorityValue     = 10
 )
 
-var (
-	cfg       *rest.Config
-	k8sClient client.Client
-	ctx       context.Context
-	fwk       *framework.Framework
-	crdPath   = filepath.Join("..", "..", "..", "..", "config", "components", "crd", "bases")
-)
-
 // +kubebuilder:docs-gen:collapse=Imports
 
 var _ = ginkgo.Describe("Job controller", func() {
+
 	ginkgo.BeforeEach(func() {
 		fwk = &framework.Framework{
-			ManagerSetup: managerSetup(job.WithManageJobsWithoutQueueName(true)),
```

  We have tests for false below. No need to add more I think.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-27T07:18:48Z

- **pkg/controller/workload/job/job_webhook.go:71** by [@kerthcet](https://github.com/kerthcet) — 2022-10-27T07:18:48Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	return nil
+}
```

  updated

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-16T20:39:41Z

- **pkg/controller/workload/job/job_webhook.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T20:21:53Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
```

  I think it might be?
  
  But it's simpler to say something like this at the beginning:
  
  ```golang
  if queueName(job) == "" && !manageJobWithoutQueue {
    return nil
  }
  ```
  
  Then you can apply suspend to any job

- **pkg/controller/workload/job/job_webhook.go:62** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T20:35:55Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var webhookLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	webhookLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
```

  I don't think we did this in the other webhooks, but did you try getting a logger from the context?
  
  In general, using a logger from the context allows to include, well, contextual information that might be coming from the callers of the function.

- **pkg/controller/workload/job/job_webhook.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T20:38:26Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var webhookLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	webhookLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-batch-v1-job,mutating=false,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=update,versions=v1,name=vjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &JobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *JobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	return nil
+}
+
+// ValidateUpdate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Object) error {
+	oldJob := oldObj.(*batchv1.Job)
+	newJob := newObj.(*batchv1.Job)
+	webhookLog.V(5).Info("Validating update", "job", klog.KObj(newJob))
+
+	return validateUpdate(oldJob, newJob, w.manageJobsWithoutQueueName)
+}
+
+func validateUpdate(oldJob, newJob *batchv1.Job, manageJobsWithoutQueueName bool) error {
+	suspendPath := field.NewPath("job", "spec", "suspend")
+
+	if !manageJobsWithoutQueueName && queueName(oldJob) == "" && queueName(newJob) != "" && !*newJob.Spec.Suspend {
+		return field.Required(suspendPath, "suspend should be true when adding the queue name with manageJobsWithoutQueueName set to false")
```

  I guess it shouldn't matter here what's the value of `manageJobsWithoutQueueName`?
  
  Because you are adding a queue, so it will be managed now.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-11-23T08:46:23Z

- **pkg/controller/workload/job/job_webhook.go:70** by [@kerthcet](https://github.com/kerthcet) — 2022-11-23T08:46:23Z
```diff
@@ -0,0 +1,76 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var jobLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+	client                     client.Client
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	webhook := JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		client:                     mgr.GetClient(),
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(&webhook).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	jobLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
```

  Make sense

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-11-23T08:50:59Z

- **pkg/controller/workload/job/job_webhook.go:62** by [@kerthcet](https://github.com/kerthcet) — 2022-11-23T08:50:58Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var webhookLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	webhookLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
```

  This is the general pattern we follow in other places, see https://github.com/kubernetes-sigs/kueue/blob/f24c63accaad461dfe582b21819dbf3a5d75dd60/apis/kueue/webhooks/workload_webhook.go#L37-L56

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-11-23T09:21:03Z

- **pkg/controller/workload/job/job_webhook.go:99** by [@kerthcet](https://github.com/kerthcet) — 2022-11-23T09:21:03Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var webhookLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	webhookLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
+
+	if queueName(job) == "" && w.manageJobsWithoutQueueName && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	if queueName(job) != "" && !(*job.Spec.Suspend) {
+		job.Spec.Suspend = pointer.Bool(true)
+		return nil
+	}
+
+	return nil
+}
+
+// +kubebuilder:webhook:path=/validate-batch-v1-job,mutating=false,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=update,versions=v1,name=vjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomValidator = &JobWebhook{}
+
+// ValidateCreate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *JobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) error {
+	return nil
+}
+
+// ValidateUpdate implements webhook.CustomValidator so a webhook will be registered for the type
+func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.Object) error {
+	oldJob := oldObj.(*batchv1.Job)
+	newJob := newObj.(*batchv1.Job)
+	webhookLog.V(5).Info("Validating update", "job", klog.KObj(newJob))
+
+	return validateUpdate(oldJob, newJob, w.manageJobsWithoutQueueName)
+}
+
+func validateUpdate(oldJob, newJob *batchv1.Job, manageJobsWithoutQueueName bool) error {
+	suspendPath := field.NewPath("job", "spec", "suspend")
+
+	if !manageJobsWithoutQueueName && queueName(oldJob) == "" && queueName(newJob) != "" && !*newJob.Spec.Suspend {
+		return field.Required(suspendPath, "suspend should be true when adding the queue name with manageJobsWithoutQueueName set to false")
```

  This refers to the specified scenario that, manageJobsWithoutQueueName is not enabled, user create a job without a queueName, then he wants to manage this by kueue, so he adds a queueName. At that moment, suspend is not allowed. When `manageJobsWithoutQueueName` enabled, it will be captured by below `queueName(oldJob) != queueName(newJob)`.
  
  But what you said also makes sense, updated as advised.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-23T13:34:43Z

- **pkg/controller/workload/job/job_webhook.go:62** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-23T13:34:42Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var webhookLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	webhookLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
```

  I know.... but I think that was by mistake.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-23T13:55:05Z

- **pkg/controller/workload/job/job_webhook_test.go:30** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-23T13:46:03Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+
+	testingutil "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestValidateUpdate(t *testing.T) {
```

  Also add a test for create

- **test/integration/controller/job/job_controller_test.go:267** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-23T13:50:54Z
```diff
@@ -275,6 +263,28 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 			return k8sClient.Get(ctx, lookupKey, createdWorkload)
 		}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should not reconcile suspend-in-creation jobs even queue is set", func() {
```

  What's the difference with the previous test?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-11-24T04:34:57Z

- **pkg/controller/workload/job/job_webhook_test.go:30** by [@kerthcet](https://github.com/kerthcet) — 2022-11-24T04:34:57Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+
+	testingutil "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestValidateUpdate(t *testing.T) {
```

  We didn't have validation on creation now. All jobs will be suspend in defaulting if met the requirements.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-11-24T04:42:49Z

- **test/integration/controller/job/job_controller_test.go:267** by [@kerthcet](https://github.com/kerthcet) — 2022-11-24T04:42:49Z
```diff
@@ -275,6 +263,28 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 			return k8sClient.Get(ctx, lookupKey, createdWorkload)
 		}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should not reconcile suspend-in-creation jobs even queue is set", func() {
```

  Seems useless, removed.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-11-24T06:22:38Z

- **pkg/controller/workload/job/job_webhook.go:62** by [@kerthcet](https://github.com/kerthcet) — 2022-11-24T06:22:38Z
```diff
@@ -0,0 +1,112 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"context"
+
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/webhook"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// log is for logging in this package.
+var webhookLog = ctrl.Log.WithName("job-webhook")
+
+type JobWebhook struct {
+	manageJobsWithoutQueueName bool
+}
+
+// SetupWebhook configures the webhook for batchJob.
+func SetupWebhook(mgr ctrl.Manager, opts ...Option) error {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	wh := &JobWebhook{
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+	}
+	return ctrl.NewWebhookManagedBy(mgr).
+		For(&batchv1.Job{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
+		Complete()
+}
+
+// +kubebuilder:webhook:path=/mutate-batch-v1-job,mutating=true,failurePolicy=fail,sideEffects=None,groups=batch,resources=jobs,verbs=create,versions=v1,name=mjob.kb.io,admissionReviewVersions=v1
+
+var _ webhook.CustomDefaulter = &JobWebhook{}
+
+// Default implements webhook.CustomDefaulter so a webhook will be registered for the type
+func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
+	job := obj.(*batchv1.Job)
+	webhookLog.V(5).Info("Applying defaults", "job", klog.KObj(job))
```

  updated

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-24T19:18:48Z

- **pkg/controller/workload/job/job_webhook_test.go:30** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-24T19:18:48Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	batchv1 "k8s.io/api/batch/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+
+	testingutil "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestValidateUpdate(t *testing.T) {
```

  But we can still test that suspend is set to true

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-24T19:19:10Z

/lgtm
/hold cancel
