# PR #415: Add condition field to ClusterQueueStatus

**Summary**: Add condition field to ClusterQueueStatus

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/415

**Last updated**: 2022-12-22T20:00:10Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `add-condition-to-clusterqueue` → `main`
- **Created**: 2022-10-10T08:39:18Z
- **Updated**: 2022-12-22T20:00:10Z
- **Merged**: 2022-12-22T19:59:27Z
- **Closed**: 2022-12-22T19:59:27Z
- **Labels**: `kind/feature`, `lgtm`, `size/XL`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: _none_
- **Changed files**: 12   **+614 / -75**

## Description

Signed-off-by: tenzen-y <yuki.iwai.tz@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:
I added conditions field to ClusterQueue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #250 

#### Special notes for your reviewer:
~~[API design](https://github.com/kubernetes-sigs/kueue/issues/250#issuecomment-1126866851)~~

Determined API design:

```go
type ClusterQueueStatus struct {
	// usedResources are the resources (by flavor) currently in use by the
	// workloads assigned to this clusterQueue.
	// +optional
	UsedResources UsedResources `json:"usedResources"`

	// PendingWorkloads is the number of workloads currently waiting to be
	// admitted to this clusterQueue.
	// +optional
	PendingWorkloads int32 `json:"pendingWorkloads"`

	// AdmittedWorkloads is the number of workloads currently admitted to this
	// clusterQueue and haven't finished yet.
	// +optional
	AdmittedWorkloads int32 `json:"admittedWorkloads"`

	// conditions hold the latest available observations of the ClusterQueue
	// current state.
	// +optional
	// +listType=map
	// +listMapKey=type
	Conditions []metav1.Condition `json:"conditions,omitempty"`
}
```

## Discussion

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T18:57:28Z

This PR is ready to review.
Please take a look.

/assign @alculquicondor @ahg-g @kerthcet

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-17T14:45:57Z

Let me know when this PR is ready for another pass :)

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-11-18T02:19:41Z

> Let me know when this PR is ready for another pass :)

Sure.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-05T20:56:00Z

@alculquicondor I have addressed your suggestions and re-implemented the `ClusterQueueConditions` using a new API design. PTAL

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T18:51:08Z

> It looks like you are still working on this.
Please let me know when it's ready. I'll ignore it for now, as I have a lot of review burden at the moment.

Sure.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-10T19:00:49Z

@alculquicondor @denkensk I have addressed your suggestions. Please take another look.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T13:33:24Z

Still waiting for an update here

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-14T13:43:37Z

> Still waiting for an update here

Thanks. I will get back to you when this PR is ready for review again.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:21:53Z

/retitle WIP Add condition field to ClusterQueueStatus

Please remove WIP from the title when it's ready for another pass.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T09:15:11Z

/retitle Add condition field to ClusterQueueStatus

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T09:16:56Z

@alculquicondor I have completed implementing a custom watcher for ResourceFlavors to the clusterqueue-controller.
PTAL

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T18:24:26Z

rebased.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T18:31:23Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-21T18:56:46Z

Before retrying a test suite, please make sure if there is an issue filed for the flaky test.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T19:02:58Z

> Before retrying a test suite, please make sure if there is an issue filed for the flaky test.

@alculquicondor I checked failed CI job, it didn't seem to be related to this PR. Maybe, related to the `WaitForPodsReady` feature.

> Summarizing 1 Failure:
  [FAIL] SchedulerWithWaitForPodsReady when Scheduling workloads on clusterQueues [It] Should block admission of one new workload if two are considered in the same scheduling cycle
  /home/prow/go/src/sigs.k8s.io/kueue/test/integration/scheduler/podsready/scheduler_test.go:147
Ran 3 of 3 Specs in 15.462 seconds
FAIL! -- 2 Passed | 1 Failed | 0 Pending | 0 Skipped
--- FAIL: TestSchedulerWithWaitForPodsReady (15.47s)
FAIL

https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/415/pull-kueue-test-integration-main/1605630317664669696

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-21T19:36:58Z

Please do rebase instead of merge.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T20:08:32Z

> Please do rebase instead of merge.

Does that mean using `git pull --rebase` instead of `git rebase`?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-21T20:26:16Z

You can do `git pull` on main (it should fast-forward) and then a `git rebase` on your branch.

Maybe this is what you did? I just see a lot of commits in this PR that don't seem related, but please correct me if I'm wrong.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T20:57:34Z

> You can do `git pull` on main (it should fast-forward) and then a `git rebase` on your branch.
> 
> Maybe this is what you did? I just see a lot of commits in this PR that don't seem related, but please correct me if I'm wrong.

Yes, I did `git pull` on main and then `git rebase` on my branch.
Although, all commits seem to be created by me.

```sh
$ git log --oneline
4318a54 (HEAD -> add-condition-to-clusterqueue, origin/add-condition-to-clusterqueue) Implement a custom watcher for ResourceFlavors to the cq-controller
0ffb49b Use testing utils to create kueue reated custom resources in unit tests for cq-controller
acd8199 Create ResourceFlavors before CQ admits workloads
d1f1895 Add test cases for ClusterQueueStatus
ac548d1 Remove unnecessary flavors from workload-controller integration tests
07037fd Remove unnecessary helper functions in integration testing
e3c1419 Ignore ClusterQueueStatus.Conditions in integration tests for the workload-controller
deea85e Merge UpdateCqStatusIfChanged func and UpdateCqStatus func into updateCqStatusIfChagned func
81dccf8 Add tests to verify the bool value returned by UpdateCqStatus func
23448cd Fix a goimports error
eaa4874 Add the Copyright to the unit test for clusterqueue_controller.go
da2b9c1 Add suite tests for cq status conditions
834ec27 Remove the logic to update cq condition just before cq-controller removes finalizers
b904d13 Add a unit test for the UpdateCqStatus function
64282b1 Add a status message when CQ is terminating
687e947 Fix error messages
f8a7ec3 Replace the kueue.ClusterQueueCondition with the metav1.Condition
f49858c Update apis/kueue/v1alpha2/clusterqueue_types.go
29483df Addd condition field to ClusterQueueStatus
9f27bc3 (origin/main, origin/HEAD) Merge pull request #473 from mimowo/block-admission-until-podsready
424aa0a Block admission until PodsReady for all admitted workloads if WaitForPodsReady is enabled
7b8316f Merge pull request #475 from alculquicondor/flavor-modes
ac88466 Merge pull request #466 from cmssczy/use_markers_validate
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T04:19:33Z

blocked by #489

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T18:18:30Z

Rebased to take #489

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T18:28:01Z

Our E2E seems to be so heavy... Maybe, the kube-apiserver is hung up...

```sh
{Timed out after 30.001s.
Expected success, but got an error:
    <*errors.StatusError | 0xc000322b40>: {
        ErrStatus: {
            TypeMeta: {Kind: "", APIVersion: ""},
            ListMeta: {
                SelfLink: "",
                ResourceVersion: "",
                Continue: "",
                RemainingItemCount: nil,
            },
            Status: "Failure",
            Message: "Internal error occurred: failed calling webhook \"mresourceflavor.kb.io\": failed to call webhook: Post \"https://kueue-webhook-service.kueue-system.svc:443/mutate-kueue-x-k8s-io-v1alpha2-resourceflavor?timeout=10s\": dial tcp 10.96.171.218:443: connect: connection refused",
            Reason: "InternalError",
            Details: {
                Name: "",
                Group: "",
                Kind: "",
                UID: "",
                Causes: [
                    {
                        Type: "",
                        Message: "failed calling webhook \"mresourceflavor.kb.io\": failed to call webhook: Post \"https://kueue-webhook-service.kueue-system.svc:443/mutate-kueue-x-k8s-io-v1alpha2-resourceflavor?timeout=10s\": dial tcp 10.96.171.218:443: connect: connection refused",
                        Field: "",
                    },
                ],
                RetryAfterSeconds: 0,
            },
            Code: 500,
        },
    }
    Internal error occurred: failed calling webhook "mresourceflavor.kb.io": failed to call webhook: Post "https://kueue-webhook-service.kueue-system.svc:443/mutate-kueue-x-k8s-io-v1alpha2-resourceflavor?timeout=10s": dial tcp 10.96.171.218:443: connect: connection refused failed [FAILED] Timed out after 30.001s.
Expected success, but got an error:
    <*errors.StatusError | 0xc000322b40>: {
        ErrStatus: {
            TypeMeta: {Kind: "", APIVersion: ""},
            ListMeta: {
                SelfLink: "",
                ResourceVersion: "",
                Continue: "",
                RemainingItemCount: nil,
            },
            Status: "Failure",
            Message: "Internal error occurred: failed calling webhook \"mresourceflavor.kb.io\": failed to call webhook: Post \"https://kueue-webhook-service.kueue-system.svc:443/mutate-kueue-x-k8s-io-v1alpha2-resourceflavor?timeout=10s\": dial tcp 10.96.171.218:443: connect: connection refused",
            Reason: "InternalError",
            Details: {
                Name: "",
                Group: "",
                Kind: "",
                UID: "",
                Causes: [
                    {
                        Type: "",
                        Message: "failed calling webhook \"mresourceflavor.kb.io\": failed to call webhook: Post \"https://kueue-webhook-service.kueue-system.svc:443/mutate-kueue-x-k8s-io-v1alpha2-resourceflavor?timeout=10s\": dial tcp 10.96.171.218:443: connect: connection refused",
                        Field: "",
                    },
                ],
                RetryAfterSeconds: 0,
            },
            Code: 500,
        },
    }
    Internal error occurred: failed calling webhook "mresourceflavor.kb.io": failed to call webhook: Post "https://kueue-webhook-service.kueue-system.svc:443/mutate-kueue-x-k8s-io-v1alpha2-resourceflavor?timeout=10s": dial tcp 10.96.171.218:443: connect: connection refused
In [BeforeSuite] at: /home/prow/go/src/sigs.k8s.io/kueue/test/e2e/suite_test.go:71 @ 12/22/22 18:22:04.417
}
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T18:37:04Z

/retest

Uhm.... it looks like kueue is particularly slow to start in 1.23. We have a timeout of 30s. We can easily lift this to 1 min or more.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T18:44:30Z

> /retest
> 
> Uhm.... it looks like kueue is particularly slow to start in 1.23. We have a timeout of 30s. We can easily lift this to 1 min or more.

I created an issue to track this:

#495

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T19:11:24Z

@alculquicondor, I addressed your suggestions. Thanks for the many reviews.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T19:58:27Z

This turned out not trivial.

Kudos for all your effort.

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-22T19:58:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/415#issuecomment-1363289774" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/415#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-19T19:01:55Z

- **test/integration/controller/core/workload_controller_test.go:186** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T19:01:55Z
```diff
@@ -180,7 +183,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 				Queue(localQueue.Name).
 				Request(corev1.ResourceCPU, "1").
 				Admit(testing.MakeAdmission(clusterQueue.Name).
-					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+					Flavor(corev1.ResourceCPU, flavorSpot).Obj()).
```

  We need to use other resourceFlavors, not `flavorOnDemand`, to avoid conflict with different test cases on workload_controller_test.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-19T19:06:49Z

- **test/integration/controller/core/workload_controller_test.go:177** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T19:06:48Z
```diff
@@ -171,6 +173,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 		ginkgo.AfterEach(func() {
 			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, flavor)).To(gomega.Succeed())
 			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
```

  Maybe, we want to use `the framework.ExpectFooToBeDeleted(ctx, k8sClient, foo, true)` for all cleanup processes in all suite tests.
  
  Probably, we can take up this in another PR.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-19T20:37:53Z

Also add validation via webhook

- **apis/kueue/v1alpha2/clusterqueue_types.go:252** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-19T20:31:58Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
```

  ```suggestion
  	Conditions []metav1.Condition `json:"conditions,omitempty"`
  ```
  
  We are using that struct everywhere else already.

- **apis/kueue/v1alpha2/clusterqueue_types.go:296** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-19T20:33:58Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
```

  No need to have 2 conditions
  
  Just `Active` with status=True/False should be enough.

- **apis/kueue/v1alpha2/clusterqueue_types.go:262** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-19T20:34:59Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
+
+	// ClusterQueueActive indicates clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
```

  @kerthcet did we implement the behavior that an inactive ClusterQueue's quota can't be borrowed?

- **pkg/controller/core/clusterqueue_controller.go:129** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-19T20:36:45Z
```diff
@@ -107,18 +109,38 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(cqObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", cqObj.Name)
+		reason := "Ready"
+
+		// ClusterQueueActive=True
+		updatedActiveCondition := r.UpdateClusterQueueCondition(&newCQObj.Status,
+			kueue.ClusterQueueActive, corev1.ConditionTrue, reason, msg)
+		// ClusterQueuePending=False
+		updatedPendingCondition := r.UpdateClusterQueueCondition(&newCQObj.Status,
+			kueue.ClusterQueuePending, corev1.ConditionFalse, reason, msg)
+
+		updatedConditions := updatedActiveCondition || updatedPendingCondition
+		if err := r.UpdateClusterQueueStatus(ctx, newCQObj, updatedConditions); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else if r.cache.ClusterQueuePending(cqObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads", cqObj.Name)
```

  It would be useful to know why: For example: there are missing ResourceFlavors or it's being deleted

- **pkg/controller/core/clusterqueue_controller.go:316** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-19T20:37:15Z
```diff
@@ -291,18 +313,79 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateClusterQueueCondition(
```

  Can we reuse the logic that we use for other objects that have metav1.Conditions?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-19T20:48:14Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:252** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T20:48:14Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
```

  We need to use `ClusterQueueConditions` instead of `metav1.Condition` to hold the `ClusterQueueConditionType`.
  Or, is it better to hold `ClusterQueueConditionType` in `ClusterQueueStatus` like the following?
  
  ```go
  type ClusterQueueStatus struct {
  	// usedResources are the resources (by flavor) currently in use by the
  	// workloads assigned to this clusterQueue.
  	// +optional
  	UsedResources UsedResources `json:"usedResources"`
  
  	// PendingWorkloads is the number of workloads currently waiting to be
  	// admitted to this clusterQueue.
  	// +optional
  	PendingWorkloads int32 `json:"pendingWorkloads"`
  
  	// AdmittedWorkloads is the number of workloads currently admitted to this
  	// clusterQueue and haven't finished yet.
  	// +optional
  	AdmittedWorkloads int32 `json:"admittedWorkloads"`
  
  	// type of condition could be:
  	//
  	// ClusterQueuePending: clusterQueue is accepted but not yet active.
  	// In this state, it can't admit new workloads and its quota can't be borrowed
  	// by other active ClusterQueues in the same cohort.
  	//
  	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
  	// can be borrowed by other ClusterQueues in the same cohort.
  	Type ClusterQueueConditionType `json:"type"`
  
  	// conditions hold the latest available observations of the ClusterQueue
  	// current state.
  	// +optional
  	// +listType=map
  	// +listMapKey=type
  	Conditions []metav1.Condition `json:"conditions,omitempty"`
  }
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-10-19T20:49:19Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:252** by [@alculquicondor](https://github.com/alculquicondor) — 2022-10-19T20:49:18Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
```

  Yeah, it's not worth the extra maintenance effort just to have the type. strings are fine.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-19T20:55:59Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:252** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T20:55:59Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
```

  It makes sense. 
  I will change to the following API. 
  
  ```go
  type ClusterQueueStatus struct {
  	// usedResources are the resources (by flavor) currently in use by the
  	// workloads assigned to this clusterQueue.
  	// +optional
  	UsedResources UsedResources `json:"usedResources"`
  
  	// PendingWorkloads is the number of workloads currently waiting to be
  	// admitted to this clusterQueue.
  	// +optional
  	PendingWorkloads int32 `json:"pendingWorkloads"`
  
  	// AdmittedWorkloads is the number of workloads currently admitted to this
  	// clusterQueue and haven't finished yet.
  	// +optional
  	AdmittedWorkloads int32 `json:"admittedWorkloads"`
  
  	// conditions hold the latest available observations of the ClusterQueue
  	// current state.
  	// +optional
  	// +listType=map
  	// +listMapKey=type
  	Conditions []metav1.Condition `json:"conditions,omitempty"`
  }
  ```
  
  Also, In cq-controller, I will add `Active` to `metav1.Condition.Type`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-19T20:57:06Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:296** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T20:57:06Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
```

  Sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-19T20:57:30Z

- **pkg/controller/core/clusterqueue_controller.go:129** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T20:57:29Z
```diff
@@ -107,18 +109,38 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(cqObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", cqObj.Name)
+		reason := "Ready"
+
+		// ClusterQueueActive=True
+		updatedActiveCondition := r.UpdateClusterQueueCondition(&newCQObj.Status,
+			kueue.ClusterQueueActive, corev1.ConditionTrue, reason, msg)
+		// ClusterQueuePending=False
+		updatedPendingCondition := r.UpdateClusterQueueCondition(&newCQObj.Status,
+			kueue.ClusterQueuePending, corev1.ConditionFalse, reason, msg)
+
+		updatedConditions := updatedActiveCondition || updatedPendingCondition
+		if err := r.UpdateClusterQueueStatus(ctx, newCQObj, updatedConditions); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else if r.cache.ClusterQueuePending(cqObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads", cqObj.Name)
```

  That makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-19T20:58:28Z

- **pkg/controller/core/clusterqueue_controller.go:316** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-19T20:58:28Z
```diff
@@ -291,18 +313,79 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateClusterQueueCondition(
```

  Sure.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-20T09:41:26Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:262** by [@kerthcet](https://github.com/kerthcet) — 2022-10-20T09:39:03Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
+
+	// ClusterQueueActive indicates clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
```

  Yes, we'll filter out the inactive cq in snapshotting https://github.com/kubernetes-sigs/kueue/blob/d136275b81b9592e7a8c888d6f5ed8af2551ce59/pkg/cache/snapshot.go#L54-L61

- **apis/kueue/v1alpha2/clusterqueue_types.go:296** by [@kerthcet](https://github.com/kerthcet) — 2022-10-20T09:41:17Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
```

  Won't enum type more extensible?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-20T10:32:54Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:296** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-20T10:32:53Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
```

  If we make `Active` enum type, we need to pay more costs to maintain API.
  WDYT? @alculquicondor @kerthcet 
  https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r999926158

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-10-20T10:41:36Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:296** by [@tenzen-y](https://github.com/tenzen-y) — 2022-10-20T10:41:36Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
```

  I agree with the suggestions by @alculquicondor.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-10-20T10:55:40Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:296** by [@kerthcet](https://github.com/kerthcet) — 2022-10-20T10:55:35Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
```

  Misunderstanding the intension, we should use `metav1.Condition`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-16T21:05:11Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:252** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T21:03:35Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
```

  that should work, thanks

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T21:04:11Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
+
+	// ClusterQueueActive indicates clusterQueue can admit new workloads and its quota
```

  ```suggestion
  	// ClusterQueueActive indicates that the ClusterQueue can admit new workloads and its quota
  ```

- **pkg/cache/cache.go:371** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-16T21:04:47Z
```diff
@@ -368,6 +368,10 @@ func (c *Cache) ClusterQueueTerminating(name string) bool {
 	return c.clusterQueueInStatus(name, terminating)
 }
 
+func (c *Cache) ClusterQueuePending(name string) bool {
```

  Probably just using ClusterQueueActive is enough?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-11-17T05:57:43Z

- **pkg/cache/cache.go:371** by [@tenzen-y](https://github.com/tenzen-y) — 2022-11-17T05:57:43Z
```diff
@@ -368,6 +368,10 @@ func (c *Cache) ClusterQueueTerminating(name string) bool {
 	return c.clusterQueueInStatus(name, terminating)
 }
 
+func (c *Cache) ClusterQueuePending(name string) bool {
```

  Yes, that's right.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-05T18:16:25Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-05T18:16:25Z
```diff
@@ -243,10 +243,63 @@ type ClusterQueueStatus struct {
 	// clusterQueue and haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// conditions hold the latest available observations of the ClusterQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []ClusterQueueCondition `json:"conditions,omitempty"`
 }
 
 type UsedResources map[corev1.ResourceName]map[string]Usage
 
+type ClusterQueueCondition struct {
+	// type of condition could be:
+	//
+	// ClusterQueuePending: clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	//
+	// ClusterQueueActive: clusterQueue can admit new workloads and its quota
+	// can be borrowed by other ClusterQueues in the same cohort.
+	Type ClusterQueueConditionType `json:"type"`
+
+	// status could be True, False or Unknown.
+	Status corev1.ConditionStatus `json:"status"`
+
+	// lastProbeTime is the last time the condition was checked.
+	// +optional
+	LastProbeTime metav1.Time `json:"lastProbeTime,omitempty"`
+
+	// lastTransitionTime is the last time the condition transit from one status
+	// to another.
+	// +optional
+	LastTransitionTime metav1.Time `json:"lastTransitionTime,omitempty"`
+
+	// reason is a brief reason for the condition's last transition.
+	// +optional
+	Reason string `json:"reason,omitempty"`
+
+	// message is a human-readable message indicating details about last
+	// transition.
+	// +optional
+	Message string `json:"message,omitempty"`
+}
+
+type ClusterQueueConditionType string
+
+const (
+	// ClusterQueuePending indicates clusterQueue is accepted but not yet active.
+	// In this state, it can't admit new workloads and its quota can't be borrowed
+	// by other active ClusterQueues in the same cohort.
+	ClusterQueuePending ClusterQueueConditionType = "Pending"
+
+	// ClusterQueueActive indicates clusterQueue can admit new workloads and its quota
```

  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-05T20:48:09Z

- **pkg/cache/cache.go:371** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-05T20:48:09Z
```diff
@@ -368,6 +368,10 @@ func (c *Cache) ClusterQueueTerminating(name string) bool {
 	return c.clusterQueueInStatus(name, terminating)
 }
 
+func (c *Cache) ClusterQueuePending(name string) bool {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-05T20:48:20Z

- **pkg/controller/core/clusterqueue_controller.go:316** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-05T20:48:20Z
```diff
@@ -291,18 +313,79 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateClusterQueueCondition(
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-05T20:48:29Z

- **pkg/controller/core/clusterqueue_controller.go:129** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-05T20:48:29Z
```diff
@@ -107,18 +109,38 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(cqObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", cqObj.Name)
+		reason := "Ready"
+
+		// ClusterQueueActive=True
+		updatedActiveCondition := r.UpdateClusterQueueCondition(&newCQObj.Status,
+			kueue.ClusterQueueActive, corev1.ConditionTrue, reason, msg)
+		// ClusterQueuePending=False
+		updatedPendingCondition := r.UpdateClusterQueueCondition(&newCQObj.Status,
+			kueue.ClusterQueuePending, corev1.ConditionFalse, reason, msg)
+
+		updatedConditions := updatedActiveCondition || updatedPendingCondition
+		if err := r.UpdateClusterQueueStatus(ctx, newCQObj, updatedConditions); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else if r.cache.ClusterQueuePending(cqObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads", cqObj.Name)
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-06T16:29:38Z

- **pkg/controller/core/clusterqueue_controller.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T16:14:28Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
```

  No need to complicate the message if this is a condition of the ClusterQueue itself.
  
  ```suggestion
  		msg := "Can admit new workloads"
  ```

- **pkg/controller/core/clusterqueue_controller.go:120** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T16:16:17Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionTrue, "Ready", msg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads; The specified ResourceFlavors are not found", newCQObj.Name)
```

  ```suggestion
  		msg := "Can't admit new workloads; some flavors are not found"
  ```
  
  But, IIRC, there are other possibilities, such as terminating.

- **pkg/controller/core/clusterqueue_controller.go:121** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T16:16:31Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionTrue, "Ready", msg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads; The specified ResourceFlavors are not found", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionFalse, "NotReady", msg); err != nil {
```

  ```suggestion
  		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionFalse, "FlavorNotFound", msg); err != nil {
  ```

- **pkg/controller/core/clusterqueue_controller.go:338** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T16:19:08Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
```

  Does this ignore changes in the LastTransitionTime?

- **pkg/controller/core/clusterqueue_controller.go:324** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T16:19:50Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
```

  wouldn't a single DeepEqual on the entire Status would be enough?

- **pkg/controller/core/clusterqueue_controller.go:340** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T16:21:06Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
+	newCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	return !equality.Semantic.DeepEqual(newCondition, oldCondition)
```

  I think this is not necessary, given the comment about having a single DeepEqual

- **test/integration/controller/core/clusterqueue_controller_test.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T16:28:55Z
```diff
@@ -72,8 +77,12 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 
 	ginkgo.When("Reconciling clusterQueue status", func() {
```

  ```suggestion
  	ginkgo.When("Reconciling clusterQueue usage in status", func() {
  ```
  
  Then create a separate case just for the condition, with less flavors and test that the CQ is initially Active=false, then Active=true, and Active=false when you remove 1 flavor.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-06T18:47:33Z

- **pkg/controller/core/clusterqueue_controller.go:340** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-06T18:47:33Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
+	newCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	return !equality.Semantic.DeepEqual(newCondition, oldCondition)
```

  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-06T18:49:30Z

- **pkg/controller/core/clusterqueue_controller.go:324** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-06T18:49:30Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
```

  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-06T19:05:57Z

- **pkg/controller/core/clusterqueue_controller.go:120** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-06T19:05:56Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionTrue, "Ready", msg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads; The specified ResourceFlavors are not found", newCQObj.Name)
```

  > such as terminating
  
  @alculquicondor Does that mean `cache.ClusterQueueActive` is false when CQ is terminating in the following?
  
  https://github.com/kubernetes-sigs/kueue/blob/d1d445f16478ba2740cf4cd810acfd27e3366d20/pkg/cache/cache.go#L221-L223

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-06T19:17:09Z

- **pkg/controller/core/clusterqueue_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-06T19:17:08Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
```

  IIUC, this function skips updating `statusCondition` (True or False) and `LastTransitionTime` when only LastTransitionTime is changed.
  
  https://github.com/kubernetes/apimachinery/blob/fd8a60496be5e4ce19d586738adb48ac6fa66ef9/pkg/api/meta/conditions.go#L25-L55
  
  Although, I guess that conditions don't exist for `statusCondition` (True or False) is not updated and only updated `LastTransitionTime`.
  
  @alculquicondor wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-06T19:18:15Z

- **test/integration/controller/core/clusterqueue_controller_test.go:78** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-06T19:18:15Z
```diff
@@ -72,8 +77,12 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 
 	ginkgo.When("Reconciling clusterQueue status", func() {
```

  Sure.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-06T19:44:13Z

- **pkg/controller/core/clusterqueue_controller.go:120** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T19:44:13Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionTrue, "Ready", msg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads; The specified ResourceFlavors are not found", newCQObj.Name)
```

  yes

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-06T19:49:50Z

- **pkg/controller/core/clusterqueue_controller.go:338** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T19:49:49Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
```

  It looks like the transition time is overridden if you set it and the condition already existed:
  
  https://github.com/kubernetes/apimachinery/blob/fd8a60496be5e4ce19d586738adb48ac6fa66ef9/pkg/api/meta/conditions.go#L45-L46
  
  So, do not set a transition time.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-06T19:50:14Z

- **pkg/controller/core/clusterqueue_controller.go:338** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T19:50:14Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
```

  Maybe a unit test is worth :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-06T20:15:07Z

- **pkg/controller/core/clusterqueue_controller.go:120** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-06T20:15:06Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionTrue, "Ready", msg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads; The specified ResourceFlavors are not found", newCQObj.Name)
```

  ok.
  I will add the case for CQ is terminating.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-07T16:12:46Z

- **pkg/controller/core/clusterqueue_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-07T16:12:46Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
```

  > So, do not set a transition time.
  Maybe a unit test is worth :)
  
  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-07T16:15:53Z

- **pkg/controller/core/clusterqueue_controller.go:115** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-07T16:15:53Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-07T16:16:02Z

- **pkg/controller/core/clusterqueue_controller.go:121** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-07T16:16:02Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionTrue, "Ready", msg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads; The specified ResourceFlavors are not found", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionFalse, "NotReady", msg); err != nil {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T03:54:09Z

- **pkg/controller/core/clusterqueue_controller.go:120** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T03:54:09Z
```diff
@@ -107,16 +110,17 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		}
 	}
 
-	status, err := r.Status(&cqObj)
-	if err != nil {
-		log.Error(err, "Failed getting status from cache")
-		return ctrl.Result{}, err
-	}
-
-	if !equality.Semantic.DeepEqual(status, cqObj.Status) {
-		cqObj.Status = status
-		err := r.client.Status().Update(ctx, &cqObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+	newCQObj := cqObj.DeepCopy()
+	if r.cache.ClusterQueueActive(newCQObj.Name) {
+		msg := fmt.Sprintf("ClusterQueue %q can admit new workloads", newCQObj.Name)
+		if err := r.UpdateCQStatusIfChanged(ctx, newCQObj, metav1.ConditionTrue, "Ready", msg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		msg := fmt.Sprintf("ClusterQueue %q can't admit new workloads; The specified ResourceFlavors are not found", newCQObj.Name)
```

  Done.
  https://github.com/kubernetes-sigs/kueue/pull/415/commits/db3cb1315a731d8cc9974d09b92c2a41e68ea508

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-08T18:47:14Z

It looks like you are still working on this.
Please let me know when it's ready. I'll ignore it for now, as I have a lot of review burden at the moment.

- **pkg/controller/core/clusterqueue_controller.go:21** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-07T16:46:07Z
```diff
@@ -18,10 +18,11 @@ package core
 
 import (
 	"context"
-
```

  keep this empty line

- **pkg/controller/core/clusterqueue_controller.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-08T18:38:17Z
```diff
@@ -80,6 +82,7 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	const cqTerminatingStatusMsg = "Can't admit new workloads; clusterQueue is terminating"
```

  put the constant as a package variable on top of the file

- **pkg/controller/core/clusterqueue_controller.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-08T18:42:15Z
```diff
@@ -93,6 +96,10 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		if !r.cache.ClusterQueueTerminating(cqObj.Name) {
 			r.cache.TerminateClusterQueue(cqObj.Name)
 		}
+		err := r.UpdateCQStatusIfChanged(ctx, &cqObj, metav1.ConditionFalse, "Terminating", cqTerminatingStatusMsg)
```

  We don't need the logic here.
  
  If we are able to remove the finalizer below, then this object will be deleted. Then adding the condition is just a waste.

- **pkg/controller/core/clusterqueue_controller.go:324** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-08T18:45:09Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
```

  this is still pending.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T18:50:12Z

- **pkg/controller/core/clusterqueue_controller.go:99** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T18:50:12Z
```diff
@@ -93,6 +96,10 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		if !r.cache.ClusterQueueTerminating(cqObj.Name) {
 			r.cache.TerminateClusterQueue(cqObj.Name)
 		}
+		err := r.UpdateCQStatusIfChanged(ctx, &cqObj, metav1.ConditionFalse, "Terminating", cqTerminatingStatusMsg)
```

  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T18:50:31Z

- **pkg/controller/core/clusterqueue_controller.go:85** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T18:50:31Z
```diff
@@ -80,6 +82,7 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	const cqTerminatingStatusMsg = "Can't admit new workloads; clusterQueue is terminating"
```

  Ok.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T20:16:10Z

- **pkg/controller/core/clusterqueue_controller.go:340** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T20:16:10Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
+	newCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	return !equality.Semantic.DeepEqual(newCondition, oldCondition)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T20:16:31Z

- **pkg/controller/core/clusterqueue_controller.go:324** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T20:16:30Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T20:16:41Z

- **pkg/controller/core/clusterqueue_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T20:16:41Z
```diff
@@ -291,18 +295,47 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCQStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	pendingWorkloads := r.qManager.Pending(cq)
+	oldStatus := cq.Status.DeepCopy()
+	cq.Status.UsedResources = usage
+	cq.Status.AdmittedWorkloads = int32(workloads)
+	cq.Status.PendingWorkloads = int32(pendingWorkloads)
+
+	activeConditionIsChanged := UpdateCQConditionsIfChanged(&cq.Status.Conditions, metav1.Condition{
+		Type:               kueue.ClusterQueueActive,
+		Status:             conditionStatus,
+		LastTransitionTime: metav1.Now(),
+		Reason:             reason,
+		Message:            msg,
+	})
+	if activeConditionIsChanged ||
+		!equality.Semantic.DeepEqual(cq.Status.UsedResources, oldStatus.UsedResources) ||
+		cq.Status.AdmittedWorkloads != oldStatus.AdmittedWorkloads ||
+		cq.Status.PendingWorkloads != oldStatus.PendingWorkloads {
+		return r.client.Status().Update(ctx, cq)
+	}
+	return nil
+}
 
-	return kueue.ClusterQueueStatus{
-		UsedResources:     usage,
-		AdmittedWorkloads: int32(workloads),
-		PendingWorkloads:  int32(r.qManager.Pending(cq)),
-	}, nil
+func UpdateCQConditionsIfChanged(
+	cqConditions *[]metav1.Condition,
+	condition metav1.Condition,
+) bool {
+	oldCondition := meta.FindStatusCondition(*cqConditions, kueue.ClusterQueueActive).DeepCopy()
+	meta.SetStatusCondition(cqConditions, condition)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T20:24:08Z

- **pkg/controller/core/clusterqueue_controller.go:85** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T20:24:08Z
```diff
@@ -80,6 +82,7 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	const cqTerminatingStatusMsg = "Can't admit new workloads; clusterQueue is terminating"
```

  I removed this constant since I removed this logic for the following reason.
  
  > We don't need the logic here.
  >
  > If we are able to remove the finalizer below, then this object will be deleted. Then adding the condition is just a waste.
  
  https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1043701123

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-08T20:29:04Z

- **pkg/controller/core/clusterqueue_controller.go:99** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-08T20:29:04Z
```diff
@@ -93,6 +96,10 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 		if !r.cache.ClusterQueueTerminating(cqObj.Name) {
 			r.cache.TerminateClusterQueue(cqObj.Name)
 		}
+		err := r.UpdateCQStatusIfChanged(ctx, &cqObj, metav1.ConditionFalse, "Terminating", cqTerminatingStatusMsg)
```

  Done.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-12-09T09:57:32Z

Thanks @tenzen-y

- **pkg/controller/core/clusterqueue_controller_test.go:17** by [@denkensk](https://github.com/denkensk) — 2022-12-09T09:40:07Z
```diff
@@ -0,0 +1,96 @@
+package core
```

  You forgot to add Copyright ^_^

- **pkg/controller/core/clusterqueue_controller_test.go:89** by [@denkensk](https://github.com/denkensk) — 2022-12-09T09:56:56Z
```diff
@@ -0,0 +1,96 @@
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCQStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
+		reason, msg      string
+		wantCqConditions []metav1.Condition
+	}{
+		"empty conditions": {
+			cqConditions:    nil,
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "FlavorNotFound",
+			msg:             "Can't admit new workloads; some flavors are not found",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+		},
+		"same condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			},
+			},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+		},
+		"same condition status with different reason and message": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "Terminating",
+			msg:             "Can't admit new workloads; clusterQueue is terminating",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "Terminating",
+				Message: "Can't admit new workloads; clusterQueue is terminating",
+			}},
+		},
+		"different condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			cqStatus := &kueue.ClusterQueueStatus{Conditions: tc.cqConditions}
+			UpdateCqStatus(cqStatus, 0, 0, kueue.UsedResources{}, tc.conditionStatus, tc.reason, tc.msg)
```

  I think you also need to check the bool value returned by `UpdateCqStatus` to help to test the `DeepEqual` is available?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-09T17:09:33Z

- **pkg/controller/core/clusterqueue_controller_test.go:17** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-09T17:09:33Z
```diff
@@ -0,0 +1,96 @@
+package core
```

  Oops...
  Thanks for mentioning this :)))

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-09T17:15:48Z

- **pkg/controller/core/clusterqueue_controller_test.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-09T17:15:48Z
```diff
@@ -0,0 +1,96 @@
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCQStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
+		reason, msg      string
+		wantCqConditions []metav1.Condition
+	}{
+		"empty conditions": {
+			cqConditions:    nil,
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "FlavorNotFound",
+			msg:             "Can't admit new workloads; some flavors are not found",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+		},
+		"same condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			},
+			},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+		},
+		"same condition status with different reason and message": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "Terminating",
+			msg:             "Can't admit new workloads; clusterQueue is terminating",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "Terminating",
+				Message: "Can't admit new workloads; clusterQueue is terminating",
+			}},
+		},
+		"different condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			cqStatus := &kueue.ClusterQueueStatus{Conditions: tc.cqConditions}
+			UpdateCqStatus(cqStatus, 0, 0, kueue.UsedResources{}, tc.conditionStatus, tc.reason, tc.msg)
```

  Makes sense.
  I will add the logic to verify that value returned by `UpdateCqStatus`.
  Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T16:56:42Z

- **pkg/controller/core/clusterqueue_controller.go:302** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:33:40Z
```diff
@@ -291,18 +299,43 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCqStatusIfChanged(
```

  ```suggestion
  func (r *ClusterQueueReconciler) updateCqStatusIfChanged(
  ```

- **pkg/controller/core/clusterqueue_controller.go:316** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:34:38Z
```diff
@@ -291,18 +299,43 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCqStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
+	pendingWorkloads := r.qManager.Pending(cq)
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	if UpdateCqStatus(&cq.Status, pendingWorkloads, workloads, usage, conditionStatus, reason, msg) {
```

  why split this in two functions?

- **pkg/controller/core/clusterqueue_controller_test.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:41:42Z
```diff
@@ -0,0 +1,120 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCqStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
```

  more generally, this could have ClusterQueueStatus

- **pkg/controller/core/clusterqueue_controller_test.go:110** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:43:22Z
```diff
@@ -0,0 +1,120 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCqStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
+		reason, msg      string
+		wantCqConditions []metav1.Condition
+		wantUpdated      bool
+	}{
+		"empty conditions": {
+			cqConditions:    nil,
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "FlavorNotFound",
+			msg:             "Can't admit new workloads; some flavors are not found",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			wantUpdated: true,
+		},
+		"same condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			},
+			},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+			wantUpdated: false,
+		},
+		"same condition status with different reason and message": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "Terminating",
+			msg:             "Can't admit new workloads; clusterQueue is terminating",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "Terminating",
+				Message: "Can't admit new workloads; clusterQueue is terminating",
+			}},
+			wantUpdated: true,
+		},
+		"different condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+			wantUpdated: true,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			cqStatus := &kueue.ClusterQueueStatus{Conditions: tc.cqConditions}
+			actualUpdated := UpdateCqStatus(cqStatus, 0, 0, kueue.UsedResources{}, tc.conditionStatus, tc.reason, tc.msg)
```

  I'm generally prefer not to test such small functions that are not exported. You could try to test the entire `Reconcile` or maybe even `UpdateCqStatusIfChanged`

- **test/integration/controller/core/clusterqueue_controller_test.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:48:29Z
```diff
@@ -185,12 +225,99 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 				return updatedCq.Status
 			}, framework.Timeout, framework.Interval).Should(gomega.BeComparableTo(kueue.ClusterQueueStatus{
 				UsedResources: emptyUsedResources,
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
 			framework.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 			framework.ExpectAdmittedActiveWorkloadsMetric(clusterQueue, 0)
 		})
 	})
 
+	ginkgo.When("Reconciling clusterQueue status condition", func() {
+		var (
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			wl             *kueue.Workload
+			cpuArchAFlavor *kueue.ResourceFlavor
+			cpuArchBFlavor *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("bar-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorCPUArchA, "5").Max("10").Obj()).
+					Flavor(testing.MakeFlavor(flavorCPUArchB, "5").Max("10").Obj()).Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+			lq = testing.MakeLocalQueue("bar-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+			wl = testing.MakeWorkload("bar-wl", ns.Name).Queue(lq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			framework.ExpectWorkloadToBeDeleted(ctx, k8sClient, wl, true)
+			framework.ExpectLocalQueueToBeDeleted(ctx, k8sClient, lq, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchAFlavor, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchBFlavor, true)
+		})
+
+		ginkgo.It("Should update status conditions when flavors are created", func() {
```

  I would do this:
  1. no flavors
  2. create all flavors
  3. remove one flavor

- **test/integration/controller/core/workload_controller_test.go:66** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:51:27Z
```diff
@@ -63,6 +64,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 	ginkgo.AfterEach(func() {
```

  Not sure why we were doing this. It doesn't seem necessary to set everything to nil.

- **test/integration/controller/core/workload_controller_test.go:176** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:53:55Z
```diff
@@ -171,6 +173,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 		ginkgo.AfterEach(func() {
 			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, flavor)).To(gomega.Succeed())
```

  why not reuse the same flavor in all tests cases? without ever deleting it?

- **test/integration/controller/core/workload_controller_test.go:214** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:55:17Z
```diff
@@ -194,18 +197,28 @@ var _ = ginkgo.Describe("Workload controller", func() {
 				AdmittedWorkloads: 1,
 				UsedResources: kueue.UsedResources{
 					corev1.ResourceCPU: {
-						flavorOnDemand: {
+						flavorSpot: {
 							Total:    pointer.Quantity(resource.MustParse("2")),
 							Borrowed: nil,
 						},
 					},
 				},
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
```

  just ignore the conditions as well. This is not where we should be testing that.

- **test/integration/controller/core/workload_controller_test.go:270** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:55:25Z
```diff
@@ -245,7 +259,15 @@ var _ = ginkgo.Describe("Workload controller", func() {
 						},
 					},
 				},
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
```

  same here.

- **test/integration/framework/framework.go:355** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:56:32Z
```diff
@@ -347,3 +347,23 @@ func ExpectResourceFlavorToBeDeleted(ctx context.Context, k8sClient client.Clien
 		return k8sClient.Get(ctx, client.ObjectKeyFromObject(rf), &newRF)
 	}, Timeout, Interval).Should(testing.BeNotFoundError())
 }
+
+func ExpectLocalQueueToBeDeleted(ctx context.Context, k8sClient client.Client, lq *kueue.LocalQueue, deleteLq bool) {
+	if deleteLq {
+		gomega.Expect(DeleteLocalQueue(ctx, k8sClient, lq)).To(gomega.Succeed())
+	}
+	gomega.Eventually(func() error {
```

  Are you having issues if you don't do wait?
  
  We have this wait in objects that can be blocked during deletion, like ClusterQueue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-13T22:04:12Z

- **pkg/controller/core/clusterqueue_controller_test.go:110** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-13T22:04:11Z
```diff
@@ -0,0 +1,120 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCqStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
+		reason, msg      string
+		wantCqConditions []metav1.Condition
+		wantUpdated      bool
+	}{
+		"empty conditions": {
+			cqConditions:    nil,
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "FlavorNotFound",
+			msg:             "Can't admit new workloads; some flavors are not found",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			wantUpdated: true,
+		},
+		"same condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			},
+			},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+			wantUpdated: false,
+		},
+		"same condition status with different reason and message": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "Terminating",
+			msg:             "Can't admit new workloads; clusterQueue is terminating",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "Terminating",
+				Message: "Can't admit new workloads; clusterQueue is terminating",
+			}},
+			wantUpdated: true,
+		},
+		"different condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+			wantUpdated: true,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			cqStatus := &kueue.ClusterQueueStatus{Conditions: tc.cqConditions}
+			actualUpdated := UpdateCqStatus(cqStatus, 0, 0, kueue.UsedResources{}, tc.conditionStatus, tc.reason, tc.msg)
```

  SGTM
  I would change this test to one using `UpdateCqStatusIfChanged`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-13T22:11:24Z

- **pkg/controller/core/clusterqueue_controller.go:316** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-13T22:11:24Z
```diff
@@ -291,18 +299,43 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCqStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
+	pendingWorkloads := r.qManager.Pending(cq)
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	if UpdateCqStatus(&cq.Status, pendingWorkloads, workloads, usage, conditionStatus, reason, msg) {
```

  I split this to simplify unit tests for the `UpdateCqStatus` function.
  
  Although, there is no longer any reason to split that function since I would change the test as I mentioned at https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1047798382.
  
  ```
  @[alculquicondor](https://github.com/alculquicondor) alculquicondor [yesterday](https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1046079081)
  I'm generally prefer not to test such small functions that are not exported. You could try to test the entire Reconcile or maybe even UpdateCqStatusIfChanged
  
  Member
  Author
  @[tenzen-y](https://github.com/tenzen-y) tenzen-y [4 minutes ago](https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1047798382)
  SGTM
  I would change this test to one using UpdateCqStatusIfChanged.
  ```
  
  So I would move the processes in the `UpdateCqStatus` func to the `UpdateCqStatusIfChanged` func.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-14T04:14:52Z

- **test/integration/controller/core/workload_controller_test.go:214** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-14T04:14:52Z
```diff
@@ -194,18 +197,28 @@ var _ = ginkgo.Describe("Workload controller", func() {
 				AdmittedWorkloads: 1,
 				UsedResources: kueue.UsedResources{
 					corev1.ResourceCPU: {
-						flavorOnDemand: {
+						flavorSpot: {
 							Total:    pointer.Quantity(resource.MustParse("2")),
 							Borrowed: nil,
 						},
 					},
 				},
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
```

  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T16:08:56Z

- **pkg/controller/core/clusterqueue_controller_test.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T16:08:55Z
```diff
@@ -0,0 +1,120 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCqStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
```

  Make sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T16:15:17Z

- **pkg/controller/core/clusterqueue_controller.go:302** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T16:15:16Z
```diff
@@ -291,18 +299,43 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCqStatusIfChanged(
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T16:15:23Z

- **pkg/controller/core/clusterqueue_controller.go:316** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T16:15:23Z
```diff
@@ -291,18 +299,43 @@ func (r *ClusterQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func (r *ClusterQueueReconciler) Status(cq *kueue.ClusterQueue) (kueue.ClusterQueueStatus, error) {
+func (r *ClusterQueueReconciler) UpdateCqStatusIfChanged(
+	ctx context.Context,
+	cq *kueue.ClusterQueue,
+	conditionStatus metav1.ConditionStatus,
+	reason, msg string,
+) error {
+	pendingWorkloads := r.qManager.Pending(cq)
 	usage, workloads, err := r.cache.Usage(cq)
 	if err != nil {
 		r.log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
-		return kueue.ClusterQueueStatus{}, err
+		return err
 	}
+	if UpdateCqStatus(&cq.Status, pendingWorkloads, workloads, usage, conditionStatus, reason, msg) {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T16:15:33Z

- **pkg/controller/core/clusterqueue_controller_test.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T16:15:33Z
```diff
@@ -0,0 +1,120 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCqStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T16:15:41Z

- **pkg/controller/core/clusterqueue_controller_test.go:110** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T16:15:41Z
```diff
@@ -0,0 +1,120 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+)
+
+func TestUpdateCqStatus(t *testing.T) {
+	testCases := map[string]struct {
+		cqConditions     []metav1.Condition
+		conditionStatus  metav1.ConditionStatus
+		reason, msg      string
+		wantCqConditions []metav1.Condition
+		wantUpdated      bool
+	}{
+		"empty conditions": {
+			cqConditions:    nil,
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "FlavorNotFound",
+			msg:             "Can't admit new workloads; some flavors are not found",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			wantUpdated: true,
+		},
+		"same condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			},
+			},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+			wantUpdated: false,
+		},
+		"same condition status with different reason and message": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionFalse,
+			reason:          "Terminating",
+			msg:             "Can't admit new workloads; clusterQueue is terminating",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "Terminating",
+				Message: "Can't admit new workloads; clusterQueue is terminating",
+			}},
+			wantUpdated: true,
+		},
+		"different condition status": {
+			cqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionFalse,
+				Reason:  "FlavorNotFound",
+				Message: "Can't admit new workloads; some flavors are not found",
+			}},
+			conditionStatus: metav1.ConditionTrue,
+			reason:          "Ready",
+			msg:             "Can admit new workloads",
+			wantCqConditions: []metav1.Condition{{
+				Type:    kueue.ClusterQueueActive,
+				Status:  metav1.ConditionTrue,
+				Reason:  "Ready",
+				Message: "Can admit new workloads",
+			}},
+			wantUpdated: true,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			cqStatus := &kueue.ClusterQueueStatus{Conditions: tc.cqConditions}
+			actualUpdated := UpdateCqStatus(cqStatus, 0, 0, kueue.UsedResources{}, tc.conditionStatus, tc.reason, tc.msg)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T16:29:59Z

- **test/integration/controller/core/workload_controller_test.go:214** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T16:29:59Z
```diff
@@ -194,18 +197,28 @@ var _ = ginkgo.Describe("Workload controller", func() {
 				AdmittedWorkloads: 1,
 				UsedResources: kueue.UsedResources{
 					corev1.ResourceCPU: {
-						flavorOnDemand: {
+						flavorSpot: {
 							Total:    pointer.Quantity(resource.MustParse("2")),
 							Borrowed: nil,
 						},
 					},
 				},
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T16:30:05Z

- **test/integration/controller/core/workload_controller_test.go:270** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T16:30:05Z
```diff
@@ -245,7 +259,15 @@ var _ = ginkgo.Describe("Workload controller", func() {
 						},
 					},
 				},
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T17:48:00Z

- **test/integration/controller/core/workload_controller_test.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T17:48:00Z
```diff
@@ -171,6 +173,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 		ginkgo.AfterEach(func() {
 			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, flavor)).To(gomega.Succeed())
```

  @alculquicondor If this flavor is not removed here, we sometimes face the error in `test/integration/controller/core/clusterqueue_controller_test.go` like the following since the 'spot' flavor is also used in integration testing for the clusterqueue-controller.
  
  ```sh
  ClusterQueue controller when Reconciling clusterQueue usage status [It] Should update status when workloads are assigned and finish
  /Users/tenzen-y/go/src/github.com/kubernetes-sigs/kueue/test/integration/controller/core/clusterqueue_controller_test.go:109
  
    [FAILED] Expected success, but got an error:
        <*errors.StatusError | 0x140004ad4a0>: {
            ErrStatus: {
                TypeMeta: {Kind: "", APIVersion: ""},
                ListMeta: {
                    SelfLink: "",
                    ResourceVersion: "",
                    Continue: "",
                    RemainingItemCount: nil,
                },
                Status: "Failure",
                Message: "resourceflavors.kueue.x-k8s.io \"spot\" already exists",
                Reason: "AlreadyExists",
                Details: {
                    Name: "spot",
                    Group: "kueue.x-k8s.io",
                    Kind: "resourceflavors",
                    UID: "",
                    Causes: nil,
                    RetryAfterSeconds: 0,
                },
                Code: 409,
            },
        }
        resourceflavors.kueue.x-k8s.io "spot" already exists
    In [It] at: /Users/tenzen-y/go/src/github.com/kubernetes-sigs/kueue/test/integration/controller/core/clusterqueue_controller_test.go:176 @ 12/19/22 02:39:00.155
  
    There were additional failures detected.  To view them in detail run ginkgo -vv
  ```
  
  This does not mean we always face the above error. We face sometimes face that error.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T17:59:54Z

- **test/integration/framework/framework.go:355** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T17:59:54Z
```diff
@@ -347,3 +347,23 @@ func ExpectResourceFlavorToBeDeleted(ctx context.Context, k8sClient client.Clien
 		return k8sClient.Get(ctx, client.ObjectKeyFromObject(rf), &newRF)
 	}, Timeout, Interval).Should(testing.BeNotFoundError())
 }
+
+func ExpectLocalQueueToBeDeleted(ctx context.Context, k8sClient client.Client, lq *kueue.LocalQueue, deleteLq bool) {
+	if deleteLq {
+		gomega.Expect(DeleteLocalQueue(ctx, k8sClient, lq)).To(gomega.Succeed())
+	}
+	gomega.Eventually(func() error {
```

  > Are you having issues if you don't do wait?
  
  I have no problems worth mentioning.
  
  > We have this wait in objects that can be blocked during deletion, like ClusterQueue.
  
  I see. I would remove the `ExpectLocalQueueToBeDeleted` function and the `ExpectWorkloadToBeDeleted ` function.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T18:05:09Z

- **test/integration/framework/framework.go:355** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T18:05:09Z
```diff
@@ -347,3 +347,23 @@ func ExpectResourceFlavorToBeDeleted(ctx context.Context, k8sClient client.Clien
 		return k8sClient.Get(ctx, client.ObjectKeyFromObject(rf), &newRF)
 	}, Timeout, Interval).Should(testing.BeNotFoundError())
 }
+
+func ExpectLocalQueueToBeDeleted(ctx context.Context, k8sClient client.Client, lq *kueue.LocalQueue, deleteLq bool) {
+	if deleteLq {
+		gomega.Expect(DeleteLocalQueue(ctx, k8sClient, lq)).To(gomega.Succeed())
+	}
+	gomega.Eventually(func() error {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T18:22:23Z

- **test/integration/controller/core/workload_controller_test.go:66** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T18:22:23Z
```diff
@@ -63,6 +64,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 	ginkgo.AfterEach(func() {
```

  I added flavor to the top of the ginkgo spec since I tested the `ClusterQueueStatus.Conditions` in `workload_controller_test.go`.
  
  Although we can remove these flavors since we can ignore the `ClusterQueueStatus.Conditions` as you mentioned in the following:
  
  https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1046096792
  
  ```
  [test/integration/controller/core/workload_controller_test.go](https://github.com/kubernetes-sigs/kueue/pull/415/files/0a7de4beee27755ac5815e234a03dd02acbeb5c1#diff-3c7d6a020caf54db44c9cf146bce6c049e9124d717c6ec38827c108591ac3c23)
  Outdated
  						Message: "Can admit new workloads",
  					},
  				},
  			}, ignoreCQConditionTimestamps))
  Member
  @[alculquicondor](https://github.com/alculquicondor) alculquicondor [last week](https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1046096792)
  just ignore the conditions as well. This is not where we should be testing that.
  
  Member
  Author
  @[tenzen-y](https://github.com/tenzen-y) tenzen-y [4 days ago](https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1047997766)
  Makes sense.
  
  Member
  Author
  @[tenzen-y](https://github.com/tenzen-y) tenzen-y [1 hour ago](https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1051629530)
  Done.
  ```
  I will remove this flavor from the top of the ginkgo spec.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T18:28:38Z

- **test/integration/controller/core/workload_controller_test.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T18:28:38Z
```diff
@@ -171,6 +173,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 		ginkgo.AfterEach(func() {
 			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, flavor)).To(gomega.Succeed())
```

  I will remove this process to delete flavor for the same reason as https://github.com/kubernetes-sigs/kueue/pull/415/files#r1051645138.
  
  ```
  I added flavor to the top of the ginkgo spec since I tested the ClusterQueueStatus.Conditions in workload_controller_test.go.
  
  Although we can remove these flavors since we can ignore the ClusterQueueStatus.Conditions as you mentioned in the following:
  
  https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1046096792
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T18:35:52Z

- **test/integration/controller/core/workload_controller_test.go:66** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T18:35:52Z
```diff
@@ -63,6 +64,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 	ginkgo.AfterEach(func() {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T18:36:00Z

- **test/integration/controller/core/workload_controller_test.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T18:36:00Z
```diff
@@ -171,6 +173,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 		ginkgo.AfterEach(func() {
 			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, flavor)).To(gomega.Succeed())
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T18:37:14Z

- **test/integration/controller/core/workload_controller_test.go:186** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T18:37:14Z
```diff
@@ -180,7 +183,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 				Queue(localQueue.Name).
 				Request(corev1.ResourceCPU, "1").
 				Admit(testing.MakeAdmission(clusterQueue.Name).
-					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+					Flavor(corev1.ResourceCPU, flavorSpot).Obj()).
```

  No needed for the reasons same as https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1051645138.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-18T18:39:36Z

- **test/integration/controller/core/workload_controller_test.go:177** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-18T18:39:36Z
```diff
@@ -171,6 +173,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 		ginkgo.AfterEach(func() {
 			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, flavor)).To(gomega.Succeed())
 			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
```

  No needed for the reasons same as https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1046098577.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T16:52:40Z

- **test/integration/controller/core/workload_controller_test.go:176** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T16:52:39Z
```diff
@@ -171,6 +173,7 @@ var _ = ginkgo.Describe("Workload controller", func() {
 		ginkgo.AfterEach(func() {
 			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, flavor)).To(gomega.Succeed())
```

  Ah, right. We can't create an object for a set of tests... just for the entire suite with `BeforeSuite`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T17:03:35Z

couple of nits

- **test/integration/controller/core/clusterqueue_controller_test.go:172** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T17:01:27Z
```diff
@@ -147,6 +168,17 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 					return k8sClient.Update(ctx, &newWL)
 				}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 			}
+
+			ginkgo.By("Creating ResourceFlavors")
```

  nit:
  while this works... it makes more sense to create the flavors before admitting workloads.

- **test/integration/controller/core/clusterqueue_controller_test.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T17:02:29Z
```diff
@@ -185,12 +225,99 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 				return updatedCq.Status
 			}, framework.Timeout, framework.Interval).Should(gomega.BeComparableTo(kueue.ClusterQueueStatus{
 				UsedResources: emptyUsedResources,
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
 			framework.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 			framework.ExpectAdmittedActiveWorkloadsMetric(clusterQueue, 0)
 		})
 	})
 
+	ginkgo.When("Reconciling clusterQueue status condition", func() {
+		var (
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			wl             *kueue.Workload
+			cpuArchAFlavor *kueue.ResourceFlavor
+			cpuArchBFlavor *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("bar-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorCPUArchA, "5").Max("10").Obj()).
+					Flavor(testing.MakeFlavor(flavorCPUArchB, "5").Max("10").Obj()).Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+			lq = testing.MakeLocalQueue("bar-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+			wl = testing.MakeWorkload("bar-wl", ns.Name).Queue(lq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			framework.ExpectWorkloadToBeDeleted(ctx, k8sClient, wl, true)
+			framework.ExpectLocalQueueToBeDeleted(ctx, k8sClient, lq, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchAFlavor, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchBFlavor, true)
+		})
+
+		ginkgo.It("Should update status conditions when flavors are created", func() {
```

  Address this. It has the advantage that the condition flips from false to true and back to false.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-19T17:15:30Z

- **test/integration/controller/core/clusterqueue_controller_test.go:271** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-19T17:15:29Z
```diff
@@ -185,12 +225,99 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 				return updatedCq.Status
 			}, framework.Timeout, framework.Interval).Should(gomega.BeComparableTo(kueue.ClusterQueueStatus{
 				UsedResources: emptyUsedResources,
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
 			framework.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 			framework.ExpectAdmittedActiveWorkloadsMetric(clusterQueue, 0)
 		})
 	})
 
+	ginkgo.When("Reconciling clusterQueue status condition", func() {
+		var (
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			wl             *kueue.Workload
+			cpuArchAFlavor *kueue.ResourceFlavor
+			cpuArchBFlavor *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("bar-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorCPUArchA, "5").Max("10").Obj()).
+					Flavor(testing.MakeFlavor(flavorCPUArchB, "5").Max("10").Obj()).Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+			lq = testing.MakeLocalQueue("bar-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+			wl = testing.MakeWorkload("bar-wl", ns.Name).Queue(lq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			framework.ExpectWorkloadToBeDeleted(ctx, k8sClient, wl, true)
+			framework.ExpectLocalQueueToBeDeleted(ctx, k8sClient, lq, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchAFlavor, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchBFlavor, true)
+		})
+
+		ginkgo.It("Should update status conditions when flavors are created", func() {
```

  @alculquicondor, I tried to add test cases, as you said. Although, `3.remove one flavor` did not work fine for the following reasons.
  
  1. Could not remove the flavor since removing flavor in use was blocked by finalizers.
  2. To avoid the first issue, I forcefully removed finalizers from the flavor and then successfully removed the flavor. Although, the clusterqueue-controller did not start reconciling and did not update the CQ status since the clusterqueue-controller does not watch ResourceFlavor in the following:
  
  https://github.com/kubernetes-sigs/kueue/blob/ed57453794e53f6a74e7363be83c663873729f03/pkg/controller/core/clusterqueue_controller.go#L286-L291
  
  I guess that clusterqueue-controller needs to watch ResourceFlavor resources. Do you have any other ideas to resolve this?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-19T18:05:28Z

- **test/integration/controller/core/clusterqueue_controller_test.go:172** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-19T18:05:27Z
```diff
@@ -147,6 +168,17 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 					return k8sClient.Update(ctx, &newWL)
 				}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 			}
+
+			ginkgo.By("Creating ResourceFlavors")
```

  Makes sense.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T18:21:42Z

- **test/integration/controller/core/clusterqueue_controller_test.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T18:21:42Z
```diff
@@ -185,12 +225,99 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 				return updatedCq.Status
 			}, framework.Timeout, framework.Interval).Should(gomega.BeComparableTo(kueue.ClusterQueueStatus{
 				UsedResources: emptyUsedResources,
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
 			framework.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 			framework.ExpectAdmittedActiveWorkloadsMetric(clusterQueue, 0)
 		})
 	})
 
+	ginkgo.When("Reconciling clusterQueue status condition", func() {
+		var (
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			wl             *kueue.Workload
+			cpuArchAFlavor *kueue.ResourceFlavor
+			cpuArchBFlavor *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("bar-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorCPUArchA, "5").Max("10").Obj()).
+					Flavor(testing.MakeFlavor(flavorCPUArchB, "5").Max("10").Obj()).Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+			lq = testing.MakeLocalQueue("bar-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+			wl = testing.MakeWorkload("bar-wl", ns.Name).Queue(lq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			framework.ExpectWorkloadToBeDeleted(ctx, k8sClient, wl, true)
+			framework.ExpectLocalQueueToBeDeleted(ctx, k8sClient, lq, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchAFlavor, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchBFlavor, true)
+		})
+
+		ginkgo.It("Should update status conditions when flavors are created", func() {
```

  > Could not remove the flavor since removing flavor in use was blocked by finalizers.
  
  Uhm...... actually, this is working as intended. The resource flavor is in use, so it's not removed immediately thanks to the finalizer. We can leave this test as is.
  
  However, how is the controller updating the status when the ResourceFlavors show up if the ClusterQueue controller is  not watching ResourceFlavors? Let's understand this first. And then, we might need to add a custom watcher, similar to https://github.com/kubernetes-sigs/kueue/blob/ed57453794e53f6a74e7363be83c663873729f03/pkg/controller/core/clusterqueue_controller.go#L289

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-19T19:47:29Z

- **test/integration/controller/core/clusterqueue_controller_test.go:271** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-19T19:47:29Z
```diff
@@ -185,12 +225,99 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 				return updatedCq.Status
 			}, framework.Timeout, framework.Interval).Should(gomega.BeComparableTo(kueue.ClusterQueueStatus{
 				UsedResources: emptyUsedResources,
-			}))
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
 			framework.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 			framework.ExpectAdmittedActiveWorkloadsMetric(clusterQueue, 0)
 		})
 	})
 
+	ginkgo.When("Reconciling clusterQueue status condition", func() {
+		var (
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			wl             *kueue.Workload
+			cpuArchAFlavor *kueue.ResourceFlavor
+			cpuArchBFlavor *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("bar-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorCPUArchA, "5").Max("10").Obj()).
+					Flavor(testing.MakeFlavor(flavorCPUArchB, "5").Max("10").Obj()).Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+			lq = testing.MakeLocalQueue("bar-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+			wl = testing.MakeWorkload("bar-wl", ns.Name).Queue(lq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			framework.ExpectWorkloadToBeDeleted(ctx, k8sClient, wl, true)
+			framework.ExpectLocalQueueToBeDeleted(ctx, k8sClient, lq, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchAFlavor, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, cpuArchBFlavor, true)
+		})
+
+		ginkgo.It("Should update status conditions when flavors are created", func() {
```

  > Uhm...... actually, this is working as intended. The resource flavor is in use, so it's not removed immediately thanks to the finalizer. We can leave this test as is.
  
  Sounds good.
  
  > However, how is the controller updating the status when the ResourceFlavors show up if the ClusterQueue controller is not watching ResourceFlavors? Let's understand this first. And then, we might need to add a custom watcher, similar to
  
  @alculquicondor Yes, I agree with your idea. I try to dive into implementations to understand that. And then, If we need to add a custom watcher, add one.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T21:09:23Z

- **test/integration/controller/core/clusterqueue_controller_test.go:172** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T21:09:23Z
```diff
@@ -147,6 +168,17 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 					return k8sClient.Update(ctx, &newWL)
 				}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 			}
+
+			ginkgo.By("Creating ResourceFlavors")
```

  I have the theory that if you move this up as suggested, the ClusterQueue status might not get updated.
  
  Currently some updates in the workloads might be causing the ClusterQueue to get a notification to update.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-20T15:13:10Z

- **test/integration/controller/core/clusterqueue_controller_test.go:172** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-20T15:13:10Z
```diff
@@ -147,6 +168,17 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 					return k8sClient.Update(ctx, &newWL)
 				}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 			}
+
+			ginkgo.By("Creating ResourceFlavors")
```

  @alculquicondor Yes, your guess is right. The CQ Status could not be updated after I moved this up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-20T15:15:53Z

- **test/integration/controller/core/clusterqueue_controller_test.go:172** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-20T15:15:53Z
```diff
@@ -147,6 +168,17 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 					return k8sClient.Update(ctx, &newWL)
 				}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 			}
+
+			ginkgo.By("Creating ResourceFlavors")
```

  Maybe, we need to add a custom watcher to the cq-controller, as you said at https://github.com/kubernetes-sigs/kueue/pull/415#discussion_r1052503608.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T15:18:48Z

- **test/integration/controller/core/clusterqueue_controller_test.go:172** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T15:18:47Z
```diff
@@ -147,6 +168,17 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 					return k8sClient.Update(ctx, &newWL)
 				}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
 			}
+
+			ginkgo.By("Creating ResourceFlavors")
```

  Yes, please do so.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-21T09:14:03Z

- **test/integration/controller/core/clusterqueue_controller_test.go:217** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T09:14:03Z
```diff
@@ -173,8 +205,16 @@ var _ = ginkgo.Describe("ClusterQueue controller", func() {
 						},
 					},
 				},
-			}))
-			framework.ExpectPendingWorkloadsMetric(clusterQueue, 0, 1)
+				Conditions: []metav1.Condition{
+					{
+						Type:    kueue.ClusterQueueActive,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Ready",
+						Message: "Can admit new workloads",
+					},
+				},
+			}, ignoreCQConditionTimestamps))
+			framework.ExpectPendingWorkloadsMetric(clusterQueue, 1, 0)
```

  Since ResourceFlavors are created above, the inadmissible is expected to be `0`, and the active is expected to be `1`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-21T20:01:04Z

- **pkg/controller/core/clusterqueue_controller.go:321** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-21T19:49:14Z
```diff
@@ -274,6 +290,35 @@ func (h *cqNamespaceHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInt
 func (h *cqNamespaceHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
 }
 
+type cqResourceFlavorHandler struct {
+	cache *cache.Cache
+}
+
+func (h *cqResourceFlavorHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+	rf, ok := e.Object.(*kueue.ResourceFlavor)
+	if !ok {
+		return
+	}
+
+	cqName, _ := h.cache.FlavorInUse(rf.Name)
```

  uhm... we should be getting a list of ClusterQueues that are using the flavor and add all of them to the workqueue.
  
  Can you create a PR to update FlavorInUse separately?
  
  I would change the function to be: `func (c *Cache) ClusterQueuesUsingFlavor(flavor string) []string`

- **pkg/controller/core/core.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-21T19:57:40Z
```diff
@@ -28,15 +29,16 @@ const updateChBuffer = 10
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
 func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
-	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc)
+	rfUpdateCh := make(chan event.GenericEvent, updateChBuffer)
+	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh)
 	if err := rfRec.SetupWithManager(mgr); err != nil {
 		return "ResourceFlavor", err
 	}
 	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
 	if err := qRec.SetupWithManager(mgr); err != nil {
 		return "LocalQueue", err
 	}
-	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfRec)
+	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh, rfRec)
```

  Use the same pattern we use for other co-dependent controllers. See the interface `WorkloadUpdateWatcher` and how it's used in `NewWorkloadReconciler`. Although, you won't be able to have it `NewResourceFlavorRenconciler`, because there is a cross-dependency with `NewClusterQueueReconciler`.
  
  But you can add a function `AddUpdateWatcher` to the `ResourceFlavorReconciler` and pass it the `ClusterQueueReconciler`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-21T20:15:30Z

- **pkg/controller/core/clusterqueue_controller.go:321** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T20:15:30Z
```diff
@@ -274,6 +290,35 @@ func (h *cqNamespaceHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInt
 func (h *cqNamespaceHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
 }
 
+type cqResourceFlavorHandler struct {
+	cache *cache.Cache
+}
+
+func (h *cqResourceFlavorHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+	rf, ok := e.Object.(*kueue.ResourceFlavor)
+	if !ok {
+		return
+	}
+
+	cqName, _ := h.cache.FlavorInUse(rf.Name)
```

  > we should be getting a list of ClusterQueues that are using the flavor and add all of them to the workqueue.
  
  Makes sense.
  
  > I would change the function to be: func (c *Cache) ClusterQueuesUsingFlavor(flavor string) []string
  
  Looks good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-21T20:24:51Z

- **pkg/controller/core/core.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T20:24:51Z
```diff
@@ -28,15 +29,16 @@ const updateChBuffer = 10
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
 func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
-	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc)
+	rfUpdateCh := make(chan event.GenericEvent, updateChBuffer)
+	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh)
 	if err := rfRec.SetupWithManager(mgr); err != nil {
 		return "ResourceFlavor", err
 	}
 	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
 	if err := qRec.SetupWithManager(mgr); err != nil {
 		return "LocalQueue", err
 	}
-	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfRec)
+	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh, rfRec)
```

  That looks more clean architecture than my implementation.
  Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-21T21:17:17Z

- **pkg/controller/core/core.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-21T21:17:16Z
```diff
@@ -28,15 +29,16 @@ const updateChBuffer = 10
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
 func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
-	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc)
+	rfUpdateCh := make(chan event.GenericEvent, updateChBuffer)
+	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh)
 	if err := rfRec.SetupWithManager(mgr); err != nil {
 		return "ResourceFlavor", err
 	}
 	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
 	if err := qRec.SetupWithManager(mgr); err != nil {
 		return "LocalQueue", err
 	}
-	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfRec)
+	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh, rfRec)
```

  When you fix this, feel free to squash the older commits.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-21T21:24:30Z

- **pkg/controller/core/core.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-21T21:24:30Z
```diff
@@ -28,15 +29,16 @@ const updateChBuffer = 10
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
 func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
-	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc)
+	rfUpdateCh := make(chan event.GenericEvent, updateChBuffer)
+	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh)
 	if err := rfRec.SetupWithManager(mgr); err != nil {
 		return "ResourceFlavor", err
 	}
 	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
 	if err := qRec.SetupWithManager(mgr); err != nil {
 		return "LocalQueue", err
 	}
-	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfRec)
+	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh, rfRec)
```

  Sure.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-22T11:47:10Z

- **pkg/controller/core/clusterqueue_controller.go:78** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T11:47:10Z
```diff
@@ -50,18 +52,31 @@ type ClusterQueueReconciler struct {
 	qManager   *queue.Manager
 	cache      *cache.Cache
 	wlUpdateCh chan event.GenericEvent
+	rfUpdateCh chan event.GenericEvent
 	watchers   []ClusterQueueUpdateWatcher
 }
 
-func NewClusterQueueReconciler(client client.Client, qMgr *queue.Manager, cache *cache.Cache, watchers ...ClusterQueueUpdateWatcher) *ClusterQueueReconciler {
-	return &ClusterQueueReconciler{
+func NewClusterQueueReconciler(
+	client client.Client,
+	qMgr *queue.Manager,
+	cache *cache.Cache,
+	watchers ...ClusterQueueUpdateWatcher,
+) *ClusterQueueReconciler {
+	cqRec := &ClusterQueueReconciler{
 		client:     client,
 		log:        ctrl.Log.WithName("cluster-queue-reconciler"),
 		qManager:   qMgr,
 		cache:      cache,
 		wlUpdateCh: make(chan event.GenericEvent, updateChBuffer),
+		rfUpdateCh: make(chan event.GenericEvent, updateChBuffer),
 		watchers:   watchers,
 	}
+	for _, w := range watchers {
+		if rfRec, match := w.(*ResourceFlavorReconciler); match {
+			rfRec.AddUpdateWatcher(cqRec)
+		}
+	}
```

  @alculquicondor Does it match your expected implementation?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-22T18:17:36Z

- **pkg/controller/core/clusterqueue_controller.go:321** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T18:17:36Z
```diff
@@ -274,6 +290,35 @@ func (h *cqNamespaceHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInt
 func (h *cqNamespaceHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
 }
 
+type cqResourceFlavorHandler struct {
+	cache *cache.Cache
+}
+
+func (h *cqResourceFlavorHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqResourceFlavorHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+	rf, ok := e.Object.(*kueue.ResourceFlavor)
+	if !ok {
+		return
+	}
+
+	cqName, _ := h.cache.FlavorInUse(rf.Name)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-22T18:21:51Z

- **pkg/controller/core/core.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T18:21:51Z
```diff
@@ -28,15 +29,16 @@ const updateChBuffer = 10
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
 func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
-	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc)
+	rfUpdateCh := make(chan event.GenericEvent, updateChBuffer)
+	rfRec := NewResourceFlavorReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh)
 	if err := rfRec.SetupWithManager(mgr); err != nil {
 		return "ResourceFlavor", err
 	}
 	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
 	if err := qRec.SetupWithManager(mgr); err != nil {
 		return "LocalQueue", err
 	}
-	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfRec)
+	cqRec := NewClusterQueueReconciler(mgr.GetClient(), qManager, cc, rfUpdateCh, rfRec)
```

  Squashed the older commits.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-22T18:37:24Z

- **pkg/controller/core/clusterqueue_controller.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T18:23:50Z
```diff
@@ -50,18 +52,31 @@ type ClusterQueueReconciler struct {
 	qManager   *queue.Manager
 	cache      *cache.Cache
 	wlUpdateCh chan event.GenericEvent
+	rfUpdateCh chan event.GenericEvent
 	watchers   []ClusterQueueUpdateWatcher
 }
 
-func NewClusterQueueReconciler(client client.Client, qMgr *queue.Manager, cache *cache.Cache, watchers ...ClusterQueueUpdateWatcher) *ClusterQueueReconciler {
-	return &ClusterQueueReconciler{
+func NewClusterQueueReconciler(
+	client client.Client,
+	qMgr *queue.Manager,
+	cache *cache.Cache,
+	watchers ...ClusterQueueUpdateWatcher,
+) *ClusterQueueReconciler {
+	cqRec := &ClusterQueueReconciler{
 		client:     client,
 		log:        ctrl.Log.WithName("cluster-queue-reconciler"),
 		qManager:   qMgr,
 		cache:      cache,
 		wlUpdateCh: make(chan event.GenericEvent, updateChBuffer),
+		rfUpdateCh: make(chan event.GenericEvent, updateChBuffer),
 		watchers:   watchers,
 	}
+	for _, w := range watchers {
+		if rfRec, match := w.(*ResourceFlavorReconciler); match {
+			rfRec.AddUpdateWatcher(cqRec)
+		}
+	}
```

  I don't think this loop should be inside the constructor. In theory, not all watchers to ClusterQueue updates need to send updates back to the ClusterQueue reconciler.
  
  I think you can simply do `rfRec.AddUpdateWatcher(cqRec)` in `SetupManagers`

- **pkg/controller/core/resourceflavor_controller.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T18:33:01Z
```diff
@@ -97,11 +106,22 @@ func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Reque
 	return ctrl.Result{}, nil
 }
 
+func (r *ResourceFlavorReconciler) AddUpdateWatcher(watchers ...ResourceFlavorUpdateWatcher) {
+	r.watchers = watchers
+}
+
+func (r *ResourceFlavorReconciler) notifyWatchers(rf *kueue.ResourceFlavor) {
+	for _, w := range r.watchers {
+		w.NotifyResourceFlavorUpdate(rf)
```

  nit:
  We can make this function accept the old and new flavor objects. See `NotifyClusterQueueUpdate` below as an example.
  
  This way, the ClusterQueue controller can filter only the creation and deletion of a RF. It doesn't really need to react to every ResourceFlavor update.
  
  But I don't expect ResourceFlavor updates to be common, so this would be an optimization that can be left for the future.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-22T18:53:22Z

- **pkg/controller/core/clusterqueue_controller.go:78** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T18:53:22Z
```diff
@@ -50,18 +52,31 @@ type ClusterQueueReconciler struct {
 	qManager   *queue.Manager
 	cache      *cache.Cache
 	wlUpdateCh chan event.GenericEvent
+	rfUpdateCh chan event.GenericEvent
 	watchers   []ClusterQueueUpdateWatcher
 }
 
-func NewClusterQueueReconciler(client client.Client, qMgr *queue.Manager, cache *cache.Cache, watchers ...ClusterQueueUpdateWatcher) *ClusterQueueReconciler {
-	return &ClusterQueueReconciler{
+func NewClusterQueueReconciler(
+	client client.Client,
+	qMgr *queue.Manager,
+	cache *cache.Cache,
+	watchers ...ClusterQueueUpdateWatcher,
+) *ClusterQueueReconciler {
+	cqRec := &ClusterQueueReconciler{
 		client:     client,
 		log:        ctrl.Log.WithName("cluster-queue-reconciler"),
 		qManager:   qMgr,
 		cache:      cache,
 		wlUpdateCh: make(chan event.GenericEvent, updateChBuffer),
+		rfUpdateCh: make(chan event.GenericEvent, updateChBuffer),
 		watchers:   watchers,
 	}
+	for _, w := range watchers {
+		if rfRec, match := w.(*ResourceFlavorReconciler); match {
+			rfRec.AddUpdateWatcher(cqRec)
+		}
+	}
```

  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-22T19:00:11Z

- **pkg/controller/core/resourceflavor_controller.go:115** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T19:00:10Z
```diff
@@ -97,11 +106,22 @@ func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Reque
 	return ctrl.Result{}, nil
 }
 
+func (r *ResourceFlavorReconciler) AddUpdateWatcher(watchers ...ResourceFlavorUpdateWatcher) {
+	r.watchers = watchers
+}
+
+func (r *ResourceFlavorReconciler) notifyWatchers(rf *kueue.ResourceFlavor) {
+	for _, w := range r.watchers {
+		w.NotifyResourceFlavorUpdate(rf)
```

  Sounds good. I would create an issue to raise this.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-22T19:09:05Z

- **pkg/controller/core/clusterqueue_controller.go:78** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T19:09:05Z
```diff
@@ -50,18 +52,31 @@ type ClusterQueueReconciler struct {
 	qManager   *queue.Manager
 	cache      *cache.Cache
 	wlUpdateCh chan event.GenericEvent
+	rfUpdateCh chan event.GenericEvent
 	watchers   []ClusterQueueUpdateWatcher
 }
 
-func NewClusterQueueReconciler(client client.Client, qMgr *queue.Manager, cache *cache.Cache, watchers ...ClusterQueueUpdateWatcher) *ClusterQueueReconciler {
-	return &ClusterQueueReconciler{
+func NewClusterQueueReconciler(
+	client client.Client,
+	qMgr *queue.Manager,
+	cache *cache.Cache,
+	watchers ...ClusterQueueUpdateWatcher,
+) *ClusterQueueReconciler {
+	cqRec := &ClusterQueueReconciler{
 		client:     client,
 		log:        ctrl.Log.WithName("cluster-queue-reconciler"),
 		qManager:   qMgr,
 		cache:      cache,
 		wlUpdateCh: make(chan event.GenericEvent, updateChBuffer),
+		rfUpdateCh: make(chan event.GenericEvent, updateChBuffer),
 		watchers:   watchers,
 	}
+	for _, w := range watchers {
+		if rfRec, match := w.(*ResourceFlavorReconciler); match {
+			rfRec.AddUpdateWatcher(cqRec)
+		}
+	}
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-22T19:23:17Z

- **pkg/controller/core/resourceflavor_controller.go:115** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-22T19:23:17Z
```diff
@@ -97,11 +106,22 @@ func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Reque
 	return ctrl.Result{}, nil
 }
 
+func (r *ResourceFlavorReconciler) AddUpdateWatcher(watchers ...ResourceFlavorUpdateWatcher) {
+	r.watchers = watchers
+}
+
+func (r *ResourceFlavorReconciler) notifyWatchers(rf *kueue.ResourceFlavor) {
+	for _, w := range r.watchers {
+		w.NotifyResourceFlavorUpdate(rf)
```

  Created #497.
