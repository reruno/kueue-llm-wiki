# PR #4172: Introduce preemption context to reduce passing of parameters

**Summary**: Introduce preemption context to reduce passing of parameters

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4172

**Last updated**: 2025-07-10T12:06:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-prepare-refactor` → `main`
- **Created**: 2025-02-07T15:49:42Z
- **Updated**: 2025-07-10T12:06:50Z
- **Merged**: 2025-02-07T17:23:56Z
- **Closed**: 2025-02-07T17:23:56Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+82 / -64**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Preparatory PR for https://github.com/kubernetes-sigs/kueue/pull/4171

While working on that PR I needed to pass a new parameter representing "tasRequests" and it becomes a chore to pump the parameter through all interfaces (such as the `minimalPreemptions` and `fairSharingPreemptions`  functions) down to the `workloadFits` function (such as currently `requests are passed). 

#### Special notes for your reviewer:

This concept is analogous to what we do in the core k8s Job controller: https://github.com/kubernetes/kubernetes/blob/39f1c90ac014828803a156b40d49f0c58fb7384a/pkg/controller/job/job_controller.go#L128 for the very same reason.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-07T15:49:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4172#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-07T15:49:57Z

/assign @gabesaba

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-07T15:50:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 00abb3b59b11904199a6a31f84a01ba1cec2feac |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67a63c55226a680008cd8df8 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-07T17:09:35Z

LGTM label has been added.  <details>Git tree hash: 9ab54894db31f95d93ea135c889c550548c636c3</details>

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-07T16:04:33Z

I like this cleanup :)

- **pkg/scheduler/preemption/preemption.go:68** by [@gabesaba](https://github.com/gabesaba) — 2025-02-07T16:00:41Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
```

  how about adding `cq *cache.ClusterQueueSnapshot`, so we don't have to fish it out of workload/cache later? also reduces parameters to `minimalPreemptions`, `fairPreemptions`, `fillbackWorkloads`, and `workloadFits`

- **pkg/scheduler/preemption/preemption.go:71** by [@gabesaba](https://github.com/gabesaba) — 2025-02-07T16:03:15Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
+	snapshot          *cache.Snapshot
+	frsNeedPreemption sets.Set[resources.FlavorResource]
+	quantities        resources.FlavorResourceQuantities
```

  a less ambiguous name. perhaps workload requests?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-07T16:08:00Z

- **pkg/scheduler/preemption/preemption.go:68** by [@gabesaba](https://github.com/gabesaba) — 2025-02-07T16:08:00Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
```

  or `preemptingClusterQueue`, to make it even more obvious which CQ it is
  
  consider `preemptingWorkload` as well?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-07T16:09:07Z

- **pkg/scheduler/preemption/preemption.go:71** by [@mimowo](https://github.com/mimowo) — 2025-02-07T16:09:07Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
+	snapshot          *cache.Snapshot
+	frsNeedPreemption sets.Set[resources.FlavorResource]
+	quantities        resources.FlavorResourceQuantities
```

  Right, the name is ambiguous, but workload requests might be misleading, because in some contexts (like the oracle) the requests are scoped to only one flavor, resource pair. 
  
  Maybe `requestsNeedPreemption` by analogy to `frsNeedPreemption`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-07T16:12:30Z

- **pkg/scheduler/preemption/preemption.go:68** by [@mimowo](https://github.com/mimowo) — 2025-02-07T16:12:30Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
```

  > how about adding `cq *cache.ClusterQueueSnapshot`, so we don't have to fish it out of workload/cache later?
  
  I thought about it, but [here](https://github.com/kubernetes-sigs/kueue/blob/577474373fafcd953faae122c7797a5ca2a7dad4/pkg/scheduler/preemption/preemption.go#L246) we need the entire snapshot, or at least the map from CQ at which point it does not matter much I think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-07T16:13:21Z

- **pkg/scheduler/preemption/preemption.go:68** by [@mimowo](https://github.com/mimowo) — 2025-02-07T16:13:21Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
```

  > consider `preemptingWorkload` as well?
  
  Maybe just `preemptor` as shorter?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-07T16:22:21Z

- **pkg/scheduler/preemption/preemption.go:68** by [@gabesaba](https://github.com/gabesaba) — 2025-02-07T16:22:21Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
```

  > > how about adding `cq *cache.ClusterQueueSnapshot`, so we don't have to fish it out of workload/cache later?
  > 
  > I thought about it, but [here](https://github.com/kubernetes-sigs/kueue/blob/577474373fafcd953faae122c7797a5ca2a7dad4/pkg/scheduler/preemption/preemption.go#L246) we need the entire snapshot, or at least the map from CQ at which point it does not matter much I think.
  
  I'm not suggesting to get rid of the cache, but to also have the CQ
  
  >Maybe just preemptor as shorter?
  
  This sounds good

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-07T16:23:15Z

- **pkg/scheduler/preemption/preemption.go:71** by [@gabesaba](https://github.com/gabesaba) — 2025-02-07T16:23:15Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
+	snapshot          *cache.Snapshot
+	frsNeedPreemption sets.Set[resources.FlavorResource]
+	quantities        resources.FlavorResourceQuantities
```

  Is ```requestsNeedPreemption``` accurate? Doesn't this include non-preempt FRs?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-07T16:26:59Z

- **pkg/scheduler/preemption/preemption.go:71** by [@mimowo](https://github.com/mimowo) — 2025-02-07T16:26:59Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
+	snapshot          *cache.Snapshot
+	frsNeedPreemption sets.Set[resources.FlavorResource]
+	quantities        resources.FlavorResourceQuantities
```

  You are right actually, it probably does not need to include the resources non-requiring preemption.
  So maybe `requests` isn't bad?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-07T16:31:27Z

- **pkg/scheduler/preemption/preemption.go:68** by [@mimowo](https://github.com/mimowo) — 2025-02-07T16:31:27Z
```diff
@@ -63,6 +63,14 @@ type Preemptor struct {
 	applyPreemption func(ctx context.Context, w *kueue.Workload, reason, message string) error
 }
 
+type preemptionCtx struct {
+	log               logr.Logger
+	wl                workload.Info
```

  > I'm not suggesting to get rid of the cache, but to also have the CQ
  
  I see, some duplication of the information should be fine. I will name it `preemptorCQ`

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-07T17:09:29Z

/lgtm
