# PR #4199: Add unit test for TAS to show it works with partial admission

**Summary**: Add unit test for TAS to show it works with partial admission

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4199

**Last updated**: 2025-07-10T12:06:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-partial-admission-ut` → `main`
- **Created**: 2025-02-10T12:14:03Z
- **Updated**: 2025-07-10T12:06:51Z
- **Merged**: 2025-02-10T15:51:59Z
- **Closed**: 2025-02-10T15:51:59Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+45 / -0**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

To make sure TAS works with partial admission as this scenario is easy to break when working on preemptions.

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-10T12:14:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | add855003ac96ab71476719bf5b9923bebd2274c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67aa1bba6d5f500008e5b7c8 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-10T12:14:30Z

/cc @gabesaba

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-10T15:08:15Z

LGTM label has been added.  <details>Git tree hash: b021a9e8bb2c46f14ed6d1dea2122bd39bdcbbc5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-10T15:08:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4199#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4199#pullrequestreview-2606145084" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-10T15:32:36Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-10T15:32:44Z

LGTM label has been added.  <details>Git tree hash: e3121fd0cb6e7b46e986ad7ee225a7974c05ac52</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-10T15:32:53Z

> /hold for @gabesaba comments

/hold cancel 
I believe https://github.com/kubernetes-sigs/kueue/pull/4199#discussion_r1949244820 is addressed, and https://github.com/kubernetes-sigs/kueue/pull/4199#discussion_r1949244094 is an exploratory question non blocking this PR.

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-10T14:57:10Z

- **pkg/scheduler/scheduler_test.go:4836** by [@gabesaba](https://github.com/gabesaba) — 2025-02-10T14:56:39Z
```diff
@@ -4797,6 +4797,51 @@ func TestScheduleForTAS(t *testing.T) {
 				},
 			},
 		},
+		"TAS workload gets scheduled as trimmed by partial admission": {
+			nodes:           defaultSingleNode,
+			topologies:      []kueuealpha.Topology{defaultSingleLevelTopology},
+			resourceFlavors: []kueue.ResourceFlavor{defaultTASFlavor},
+			clusterQueues:   []kueue.ClusterQueue{defaultClusterQueue},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("foo", "default").
+					Queue("tas-main").
+					PodSets(*utiltesting.MakePodSet("one", 5).
+						SetMinimumCount(1).
+						RequiredTopologyRequest(corev1.LabelHostname).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+			},
+			wantNewAssignments: map[string]kueue.Admission{
+				"default/foo": *utiltesting.MakeAdmission("tas-main", "one").
+					Assignment(corev1.ResourceCPU, "tas-default", "1000m").
+					AssignmentPodCount(1).
+					TopologyAssignment(&kueue.TopologyAssignment{
+						Levels: utiltas.Levels(&defaultSingleLevelTopology),
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count: 1,
+								Values: []string{
+									"x1",
+								},
+							},
+						},
+					}).Obj(),
+			},
+			eventCmpOpts: []cmp.Option{eventIgnoreMessage},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Namespace: "default", Name: "foo"},
+					Reason:    "QuotaReserved",
+					EventType: corev1.EventTypeNormal,
```

  not related to this additional test, but a general comment/question about this suite: are events something reliable enough that we want to assert against them in this test suite?

- **pkg/scheduler/scheduler_test.go:4811** by [@gabesaba](https://github.com/gabesaba) — 2025-02-10T14:56:50Z
```diff
@@ -4797,6 +4797,51 @@ func TestScheduleForTAS(t *testing.T) {
 				},
 			},
 		},
+		"TAS workload gets scheduled as trimmed by partial admission": {
+			nodes:           defaultSingleNode,
+			topologies:      []kueuealpha.Topology{defaultSingleLevelTopology},
+			resourceFlavors: []kueue.ResourceFlavor{defaultTASFlavor},
+			clusterQueues:   []kueue.ClusterQueue{defaultClusterQueue},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("foo", "default").
+					Queue("tas-main").
+					PodSets(*utiltesting.MakePodSet("one", 5).
+						SetMinimumCount(1).
+						RequiredTopologyRequest(corev1.LabelHostname).
+						Request(corev1.ResourceCPU, "1").
```

  nit: 1000m for consistency with below

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-10T15:06:52Z

- **pkg/scheduler/scheduler_test.go:4836** by [@mimowo](https://github.com/mimowo) — 2025-02-10T15:06:52Z
```diff
@@ -4797,6 +4797,51 @@ func TestScheduleForTAS(t *testing.T) {
 				},
 			},
 		},
+		"TAS workload gets scheduled as trimmed by partial admission": {
+			nodes:           defaultSingleNode,
+			topologies:      []kueuealpha.Topology{defaultSingleLevelTopology},
+			resourceFlavors: []kueue.ResourceFlavor{defaultTASFlavor},
+			clusterQueues:   []kueue.ClusterQueue{defaultClusterQueue},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("foo", "default").
+					Queue("tas-main").
+					PodSets(*utiltesting.MakePodSet("one", 5).
+						SetMinimumCount(1).
+						RequiredTopologyRequest(corev1.LabelHostname).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+			},
+			wantNewAssignments: map[string]kueue.Admission{
+				"default/foo": *utiltesting.MakeAdmission("tas-main", "one").
+					Assignment(corev1.ResourceCPU, "tas-default", "1000m").
+					AssignmentPodCount(1).
+					TopologyAssignment(&kueue.TopologyAssignment{
+						Levels: utiltas.Levels(&defaultSingleLevelTopology),
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count: 1,
+								Values: []string{
+									"x1",
+								},
+							},
+						},
+					}).Obj(),
+			},
+			eventCmpOpts: []cmp.Option{eventIgnoreMessage},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Namespace: "default", Name: "foo"},
+					Reason:    "QuotaReserved",
+					EventType: corev1.EventTypeNormal,
```

  Events are user-facing part of the API, so this way we can test what a user would see without employing integration tests. Similar asserts are done in the `TestSchedule` test. 
  
  EDIT: I guess the events are mostly interesting in case the scheduling fails, when it succeeds it is not that interesting, because it is always the same, so I would be ok to restrict the assert on events to only failed cases.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-10T15:08:08Z

/lgtm
/approve

/hold for @gabesaba comments

- **pkg/scheduler/scheduler_test.go:4836** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-10T15:07:44Z
```diff
@@ -4797,6 +4797,51 @@ func TestScheduleForTAS(t *testing.T) {
 				},
 			},
 		},
+		"TAS workload gets scheduled as trimmed by partial admission": {
+			nodes:           defaultSingleNode,
+			topologies:      []kueuealpha.Topology{defaultSingleLevelTopology},
+			resourceFlavors: []kueue.ResourceFlavor{defaultTASFlavor},
+			clusterQueues:   []kueue.ClusterQueue{defaultClusterQueue},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("foo", "default").
+					Queue("tas-main").
+					PodSets(*utiltesting.MakePodSet("one", 5).
+						SetMinimumCount(1).
+						RequiredTopologyRequest(corev1.LabelHostname).
+						Request(corev1.ResourceCPU, "1").
+						Obj()).
+					Obj(),
+			},
+			wantNewAssignments: map[string]kueue.Admission{
+				"default/foo": *utiltesting.MakeAdmission("tas-main", "one").
+					Assignment(corev1.ResourceCPU, "tas-default", "1000m").
+					AssignmentPodCount(1).
+					TopologyAssignment(&kueue.TopologyAssignment{
+						Levels: utiltas.Levels(&defaultSingleLevelTopology),
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count: 1,
+								Values: []string{
+									"x1",
+								},
+							},
+						},
+					}).Obj(),
+			},
+			eventCmpOpts: []cmp.Option{eventIgnoreMessage},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Namespace: "default", Name: "foo"},
+					Reason:    "QuotaReserved",
+					EventType: corev1.EventTypeNormal,
```

  > not related to this additional test, but a general comment/question about this suite: are events something reliable enough that we want to assert against them in this test suite?
  
  Does that mean whether or not mock event recorder (`recorder := &utiltesting.EventRecorder{}`) are reliable?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-10T15:31:31Z

- **pkg/scheduler/scheduler_test.go:4811** by [@mimowo](https://github.com/mimowo) — 2025-02-10T15:31:31Z
```diff
@@ -4797,6 +4797,51 @@ func TestScheduleForTAS(t *testing.T) {
 				},
 			},
 		},
+		"TAS workload gets scheduled as trimmed by partial admission": {
+			nodes:           defaultSingleNode,
+			topologies:      []kueuealpha.Topology{defaultSingleLevelTopology},
+			resourceFlavors: []kueue.ResourceFlavor{defaultTASFlavor},
+			clusterQueues:   []kueue.ClusterQueue{defaultClusterQueue},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("foo", "default").
+					Queue("tas-main").
+					PodSets(*utiltesting.MakePodSet("one", 5).
+						SetMinimumCount(1).
+						RequiredTopologyRequest(corev1.LabelHostname).
+						Request(corev1.ResourceCPU, "1").
```

  Actually, I updated the other places as "1" to reduce the character count :)
