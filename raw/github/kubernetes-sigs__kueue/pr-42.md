# PR #42: refactor job suspension due to multiple or non-matching workloads in job-controller

**Summary**: refactor job suspension due to multiple or non-matching workloads in job-controller

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/42

**Last updated**: 2022-02-22T02:25:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-refactor` → `main`
- **Created**: 2022-02-21T17:59:02Z
- **Updated**: 2022-02-22T02:25:47Z
- **Merged**: 2022-02-22T02:25:46Z
- **Closed**: 2022-02-22T02:25:46Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@denkensk](https://github.com/denkensk)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor)
- **Changed files**: 1   **+13 / -18**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

Refactor job-controller so that stopping a job due to non-matching workloads is done in one place

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-02-21T17:59:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/42#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@denkensk](https://github.com/denkensk) — 2022-02-22T02:23:50Z

/lgtm
Thanks.

## Reviews

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-22T01:06:49Z

- **pkg/controller/workload/job/job_controller.go:335** by [@denkensk](https://github.com/denkensk) — 2022-02-22T01:04:24Z
```diff
@@ -337,18 +328,22 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 		}
 	}
 
-	// Delete redundant workload instances.
+	// Delete duplicate workload instances.
+	existedWls := 0
 	for i := range toDelete {
-		if err := r.client.Delete(ctx, toDelete[i]); err != nil {
+		err := r.client.Delete(ctx, toDelete[i])
+		if err == nil || !apierrors.IsNotFound(err) {
```

  IIUC，is  it  `if err == nil || apierrors.IsNotFound(err) {` here ?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-22T01:17:05Z

- **pkg/controller/workload/job/job_controller.go:335** by [@ahg-g](https://github.com/ahg-g) — 2022-02-22T01:17:05Z
```diff
@@ -337,18 +328,22 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 		}
 	}
 
-	// Delete redundant workload instances.
+	// Delete duplicate workload instances.
+	existedWls := 0
 	for i := range toDelete {
-		if err := r.client.Delete(ctx, toDelete[i]); err != nil {
+		err := r.client.Delete(ctx, toDelete[i])
+		if err == nil || !apierrors.IsNotFound(err) {
```

  This should be correct as is, what we want is to count the number of workloads that actually existed as of this moment. If the error is nil, we successful deleted the extra workload (so this workload existed and counts), if there was an error on deleting the workload, then we count only if the error was not NotFound.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-22T01:46:08Z

- **pkg/controller/workload/job/job_controller.go:335** by [@denkensk](https://github.com/denkensk) — 2022-02-22T01:46:08Z
```diff
@@ -337,18 +328,22 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 		}
 	}
 
-	// Delete redundant workload instances.
+	// Delete duplicate workload instances.
+	existedWls := 0
 	for i := range toDelete {
-		if err := r.client.Delete(ctx, toDelete[i]); err != nil {
+		err := r.client.Delete(ctx, toDelete[i])
+		if err == nil || !apierrors.IsNotFound(err) {
```

  Thank you for the detailed explanation. 
  A small suggestion but not strong: If we includes the `deleting error without NotFound` workload in `existedWls`, we maybe not say `deleted existedWls existing workload(s)` in error. Because some of them may be deleted failed and still existed. This can be confusing in the process of troubleshooting through the logs.
  
  Can we change to `try to delete`or record success or failure separately ?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-22T02:18:45Z

- **pkg/controller/workload/job/job_controller.go:335** by [@ahg-g](https://github.com/ahg-g) — 2022-02-22T02:18:45Z
```diff
@@ -337,18 +328,22 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 		}
 	}
 
-	// Delete redundant workload instances.
+	// Delete duplicate workload instances.
+	existedWls := 0
 	for i := range toDelete {
-		if err := r.client.Delete(ctx, toDelete[i]); err != nil {
+		err := r.client.Delete(ctx, toDelete[i])
+		if err == nil || !apierrors.IsNotFound(err) {
```

  changed to "tried deleting ..."
