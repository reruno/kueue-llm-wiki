# PR #4200: Refactor computation of TAS assignments

**Summary**: Refactor computation of TAS assignments

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4200

**Last updated**: 2025-07-10T12:06:52Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-refactor-podset-usage` → `main`
- **Created**: 2025-02-10T12:26:17Z
- **Updated**: 2025-07-10T12:06:52Z
- **Merged**: 2025-02-12T16:22:07Z
- **Closed**: 2025-02-12T16:22:07Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 6   **+207 / -66**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #3761

Preparatory for https://github.com/kubernetes-sigs/kueue/pull/4171

#### Special notes for your reviewer:

The main change in this PR is to refactor how the TAS assignment is computed when there are multiple PodSets. Previously the usage from previous podsets was already accounted regardless of the final scheduling result, see [here](https://github.com/kubernetes-sigs/kueue/compare/main...mimowo:tas-refactor-podset-usage?expand=1#diff-5414dcb324187d1d853329b419691cde79632a6d77e1a7c2b503dc1a3688c081L351). This works well only assuming the FindTopologyAssignment function is called once. 

However, to support preemptions I will need to run this function multiple times for a different set of target workloads. Similarly, this assumption is also invalid for cohorts.

So I introduce a concept of `assumedUsage` which is tracked within one FindTopologyAssignment called across all PodSets targeting a flavor, see [here](https://github.com/kubernetes-sigs/kueue/compare/main...mimowo:tas-refactor-podset-usage?expand=1#diff-5414dcb324187d1d853329b419691cde79632a6d77e1a7c2b503dc1a3688c081R243-R263).

This change also improves the error message, which I extracted to yet another preparatory PR: https://github.com/kubernetes-sigs/kueue/pull/4204, so that we can decide about cherry-picking independently.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-10T12:26:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 70517e20ab5bc3b19125e535d588f7e91eed1203 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67ac961458d6c600087338b3 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-10T12:26:45Z

/cc @gabesaba @PBundyra PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-10T12:26:49Z

@mimowo: GitHub didn't allow me to request PR reviews from the following users: PTAL.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4200#issuecomment-2647842005):

>/cc @gabesaba @PBundyra PTAL


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-11T08:48:31Z

Rebased after https://github.com/kubernetes-sigs/kueue/pull/4204 was merged

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T13:01:52Z

Could we add more tests that cover the change?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-11T13:26:05Z

> Could we add more tests that cover the change?

In principle the change should result exactly the same schedule / no schedule decisions as before, but I admit the change is big so I'm happy to add some, but wondering where to focus attention, any candidates?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T14:23:43Z

> In principle the change should result exactly the same schedule / no schedule decisions as before, but I admit the change is big so I'm happy to add some, but wondering where to focus attention, any candidates?

Oh I see now, I had a scenario in mind with hierarchical cohorts, but currently focus in on preemption. In that case I believe we already have coverage for workloads with multiple PodSets

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-12T13:12:46Z

LGTM label has been added.  <details>Git tree hash: dbe54bd4c15a267e6666b7a591c9f1eb48a7a9ea</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-12T13:12:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4200#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4200#pullrequestreview-2611868698" title="LGTM">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-12T13:54:36Z

> Thank you! /approve /lgtm
> 
> /hold for @PBundyra

I believe all comments are either addressed on added to the tracking [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU). I will work on them in follow ups unless other contributors want to help.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T14:00:03Z

> > Thank you! /approve /lgtm
> > /hold for @PBundyra
> 
> I believe all comments are either addressed on added to the tracking [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU). I will work on them in follow ups unless other contributors want to help.

In case of having an agreement with @PBundyra, I'm ok with upholding it. Idk the relationship between you and Patryk in your team, though

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-12T14:02:44Z

> In case of having an agreement with @PBundyra, I'm ok with upholding it.

Thanks, let's wait for Patryk :) 

> Idk the relationship between you and Patryk in your team, though

We are team mates, desk to desk :)

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T14:03:25Z

> In case of having an agreement with @PBundyra, I'm ok with upholding it. Idk the relationship between you and Patryk in your team, though

Sounds great :)

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T15:57:34Z

/unhold

Thanks for waiting for me :)

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-19T16:11:39Z

/cherry-pick release-0.10

Preparing for #4322

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-02-19T16:12:16Z

@tenzen-y: #4200 failed to apply on top of branch "release-0.10":
```
Applying: Refactor computation of TAS assignments
Using index info to reconstruct a base tree...
M	pkg/cache/clusterqueue_snapshot.go
M	pkg/cache/tas_flavor_snapshot.go
M	pkg/scheduler/flavorassigner/tas_flavorassigner.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/flavorassigner/tas_flavorassigner.go
CONFLICT (content): Merge conflict in pkg/scheduler/flavorassigner/tas_flavorassigner.go
Auto-merging pkg/cache/tas_flavor_snapshot.go
Auto-merging pkg/cache/clusterqueue_snapshot.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Refactor computation of TAS assignments

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4200#issuecomment-2669109129):

>/cherry-pick release-0.10
>
>Preparing for #4322 
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-19T16:58:30Z

/cherry-pick release-0.10

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-02-19T16:59:10Z

@tenzen-y: new pull request created: #4327

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4200#issuecomment-2669229478):

>/cherry-pick release-0.10


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T08:47:26Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:36** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T08:47:26Z
```diff
@@ -20,61 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
```

  Is this necessary, as we already check this before calling this function?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T09:15:40Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:50** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T09:15:40Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for i, podSet := range wl.Obj.Spec.PodSets {
+			if podSet.TopologyRequest != nil {
+				psAssignment := a.podSetByName(podSet.Name)
+				if psAssignment.Status.IsError() {
+					// There is no resource quota assignment for the PodSet - no need to check TAS.
+					continue
+				}
+				psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+				if err != nil {
+					psAssignment.error(err)
+				} else if psTASRequest != nil {
+					if tasRequests == nil {
+						tasRequests = make(map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests)
+					}
```

  Can we avoid such an initialization

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T10:54:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:60** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T10:54:34Z
```diff
@@ -57,6 +57,15 @@ func (a *Assignment) Borrows() bool {
 	return a.Borrowing
 }
 
+func (a *Assignment) podSetByName(psName string) *PodSetAssignment {
```

  Can we adjust naming since we return `PodSetAssignment`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T11:00:51Z

- **pkg/cache/tas_flavor_snapshot.go:235** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T11:00:51Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
```

  Can we be more explicit that it's reason of failure?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T11:01:41Z

- **pkg/cache/tas_flavor_snapshot.go:243** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T11:01:40Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
+}
+
+type TASAssignmentsResult map[string]TASPodSetAssignmentResult
```

  Can we add a comment so we know what is the key of this map?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T12:36:05Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:31** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:36:04Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
```

  ```suggestion
  func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T12:38:16Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:47** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:38:16Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for i, podSet := range wl.Obj.Spec.PodSets {
+			if podSet.TopologyRequest != nil {
+				psAssignment := a.podSetByName(podSet.Name)
+				if psAssignment.Status.IsError() {
+					// There is no resource quota assignment for the PodSet - no need to check TAS.
+					continue
+				}
+				psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+				if err != nil {
+					psAssignment.error(err)
+				} else if psTASRequest != nil {
```

  Is there a case when the `podSetTopologyRequest()` returns `nil` as both `psTASRequest` and `err`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T12:39:13Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:57** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:39:13Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for i, podSet := range wl.Obj.Spec.PodSets {
+			if podSet.TopologyRequest != nil {
+				psAssignment := a.podSetByName(podSet.Name)
+				if psAssignment.Status.IsError() {
+					// There is no resource quota assignment for the PodSet - no need to check TAS.
+					continue
+				}
+				psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+				if err != nil {
+					psAssignment.error(err)
+				} else if psTASRequest != nil {
+					if tasRequests == nil {
+						tasRequests = make(map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests)
+					}
+					tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
+				}
+			}
+		}
+	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
 	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
 	switch {
```

  Do we need a `switch` statement here if there are only two cases?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T12:44:26Z

- **pkg/cache/tas_flavor_snapshot.go:226** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:44:26Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  Is there a need to introduce a new type if we already have a similar one `PodSetResources`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T12:47:36Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:36** by [@mimowo](https://github.com/mimowo) — 2025-02-11T12:47:36Z
```diff
@@ -20,61 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
```

  Not necessary, in the main preemption PR I'm calling it without the outer feature-gate check, but I will revisit this in that PR. Dropping the double check for now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T12:48:56Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:50** by [@mimowo](https://github.com/mimowo) — 2025-02-11T12:48:55Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for i, podSet := range wl.Obj.Spec.PodSets {
+			if podSet.TopologyRequest != nil {
+				psAssignment := a.podSetByName(podSet.Name)
+				if psAssignment.Status.IsError() {
+					// There is no resource quota assignment for the PodSet - no need to check TAS.
+					continue
+				}
+				psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+				if err != nil {
+					psAssignment.error(err)
+				} else if psTASRequest != nil {
+					if tasRequests == nil {
+						tasRequests = make(map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests)
+					}
```

  Yes, I did it to avoid allocation for the map, but you can say it is premature optimization. Alternatively we could iterate over the PodSets and just check if any of them requires TAS. before the main loop.
  
  EDIT: for now dropped the lazy initialization in favor or eager allocation. Let me know what is your preference.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T12:51:19Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:60** by [@mimowo](https://github.com/mimowo) — 2025-02-11T12:51:19Z
```diff
@@ -57,6 +57,15 @@ func (a *Assignment) Borrows() bool {
 	return a.Borrowing
 }
 
+func (a *Assignment) podSetByName(psName string) *PodSetAssignment {
```

  Since this is a function on Assignment I wanted to avoid repeating "Assignment", but I'm ok with some alternative, maybe:
  -  `podSetAssignment`
  - `podSetAssignmentByName`
  wdyt?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T12:56:56Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:401** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:51:09Z
```diff
@@ -363,17 +386,37 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		tasRequests := assignment.TopologyRequestsFor(a.wl, requests, a.cq)
+		if assignment.RepresentativeMode() == Fit {
```

  ```suggestion
  	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
  		tasRequests := assignment.TopologyRequestsFor(a.wl, requests, a.cq)
  
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:400** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:53:38Z
```diff
@@ -363,17 +386,37 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		tasRequests := assignment.TopologyRequestsFor(a.wl, requests, a.cq)
```

  WDYT about calling this function inside `a.cq.FindTopologyAssignments`

- **pkg/scheduler/flavorassigner/flavorassigner.go:60** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:56:11Z
```diff
@@ -57,6 +57,15 @@ func (a *Assignment) Borrows() bool {
 	return a.Borrowing
 }
 
+func (a *Assignment) podSetByName(psName string) *PodSetAssignment {
```

  I'm fine with `podSetAssignmentByName`, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T12:58:59Z

- **pkg/cache/clusterqueue_snapshot.go:159** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T12:58:58Z
```diff
@@ -149,3 +149,17 @@ func (c *ClusterQueueSnapshot) DominantResourceShareWithout(wlReq resources.Flav
 	}
 	return dominantResourceShare(c, without)
 }
+
+func (c *ClusterQueueSnapshot) FindTopologyAssignments(
+	tasRequestsByFlavor map[kueue.ResourceFlavorReference][]TASPodSetRequests) TASAssignmentsResult {
+	result := make(TASAssignmentsResult)
+	for tasFlavor, tasRequestsForFlavor := range tasRequestsByFlavor {
+		if tasFlavorCache := c.TASFlavors[tasFlavor]; tasFlavorCache != nil {
```

  Are we fine with ignoring TAS flavors that are not in cache?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T13:00:15Z

- **pkg/cache/clusterqueue_snapshot.go:153** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T13:00:15Z
```diff
@@ -149,3 +149,17 @@ func (c *ClusterQueueSnapshot) DominantResourceShareWithout(wlReq resources.Flav
 	}
 	return dominantResourceShare(c, without)
 }
+
+func (c *ClusterQueueSnapshot) FindTopologyAssignments(
```

  Can we maybe rename it to `FindTopologyAssignmentsForWorkload` or something similar to avoid repeating the name of the method?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T13:08:00Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:408** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T13:08:00Z
```diff
@@ -363,17 +386,37 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		tasRequests := assignment.TopologyRequestsFor(a.wl, requests, a.cq)
+		if assignment.RepresentativeMode() == Fit {
+			result := a.cq.FindTopologyAssignments(tasRequests)
+			if failure := result.Failure(); failure != nil {
+				// There is at least one PodSet which does not fit
+				psAssignment := assignment.podSetByName(failure.PodSetName)
+				psAssignment.reason(failure.Reason)
+				// clear flavors to make the PodSet assignment as NoFit
+				psAssignment.Flavors = nil
```

  Should we set `nil` here instead of `NoFit` given that, in `RepresentativeMode()` method we compare different modes?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T14:20:10Z

- **pkg/cache/tas_cache_test.go:1099** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T14:20:10Z
```diff
@@ -1083,7 +1083,20 @@ func TestFindTopologyAssignment(t *testing.T) {
 			if err != nil {
 				t.Fatalf("failed to build the snapshot: %v", err)
 			}
-			gotAssignment, reason := snapshot.FindTopologyAssignment(&tc.request, tc.requests, tc.count, tc.tolerations)
+			tasInput := TASPodSetRequests{
+				PodSet: &kueue.PodSet{
+					Name:            "main",
+					TopologyRequest: &tc.request,
+					Template: corev1.PodTemplateSpec{
+						Spec: corev1.PodSpec{
+							Tolerations: tc.tolerations,
+						},
+					},
+				},
+				SinglePodRequests: tc.requests,
+				Count:             tc.count,
+			}
+			gotAssignment, reason := snapshot.findTopologyAssignment(tasInput, nil)
```

  How about testing exported `FindTopologyAssignment` method?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T14:42:30Z

- **pkg/cache/tas_flavor_snapshot.go:235** by [@mimowo](https://github.com/mimowo) — 2025-02-11T14:42:30Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
```

  The field is on the FailureInfo struct, and you get to it by `assignment.Failure().Reason`, so I don't see a better name :). I will add a comment for now, but I'm open to other ideas.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T14:44:03Z

- **pkg/cache/tas_flavor_snapshot.go:243** by [@mimowo](https://github.com/mimowo) — 2025-02-11T14:44:02Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
+}
+
+type TASAssignmentsResult map[string]TASPodSetAssignmentResult
```

  Sure, what about introducing the podSetReference instead? Then it will be self-explanatory. 
  As we have FlavorReference and TopologyReference. The only trouble is that I will need to make conversion calling `string()` in a number of places, in order to keep the PR contained.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:00:43Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:47** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:00:43Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for i, podSet := range wl.Obj.Spec.PodSets {
+			if podSet.TopologyRequest != nil {
+				psAssignment := a.podSetByName(podSet.Name)
+				if psAssignment.Status.IsError() {
+					// There is no resource quota assignment for the PodSet - no need to check TAS.
+					continue
+				}
+				psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+				if err != nil {
+					psAssignment.error(err)
+				} else if psTASRequest != nil {
```

  Nope, it is safe to drop this check. Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:00:59Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:31** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:00:59Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:01:37Z

- **pkg/cache/tas_flavor_snapshot.go:243** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:01:37Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
+}
+
+type TASAssignmentsResult map[string]TASPodSetAssignmentResult
```

  Resolving as addressed. Feel free to re-open, or leave another comment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:02:05Z

- **pkg/cache/tas_flavor_snapshot.go:235** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:02:05Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
```

  Added a comment and resolving as addressed. Feel free to re-open or add another comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:02:13Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:60** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:02:13Z
```diff
@@ -57,6 +57,15 @@ func (a *Assignment) Borrows() bool {
 	return a.Borrowing
 }
 
+func (a *Assignment) podSetByName(psName string) *PodSetAssignment {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:17:10Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:57** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:17:10Z
```diff
@@ -20,63 +20,69 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
+// TopologyRequestsFor - returns the TopologyRequests of the workload
+func (a *Assignment) TopologyRequestsFor(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests {
+	var tasRequests map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for i, podSet := range wl.Obj.Spec.PodSets {
+			if podSet.TopologyRequest != nil {
+				psAssignment := a.podSetByName(podSet.Name)
+				if psAssignment.Status.IsError() {
+					// There is no resource quota assignment for the PodSet - no need to check TAS.
+					continue
+				}
+				psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+				if err != nil {
+					psAssignment.error(err)
+				} else if psTASRequest != nil {
+					if tasRequests == nil {
+						tasRequests = make(map[kueue.ResourceFlavorReference][]cache.TASPodSetRequests)
+					}
+					tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
+				}
+			}
+		}
+	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
 	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
 	switch {
```

  Nope, dropped it in favor of if, which also reduced indentation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:28:04Z

- **pkg/cache/tas_flavor_snapshot.go:226** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:28:04Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  Good point, there is no strict need, we could think about consolidating them, but there are subtle differences:
  - PodSetResources.Requests is the aggregated requests while TASPodSetRequests.SinglePodRequests is per pod
  - PodSetResources.Flavors is a resource -> Flavor map while for TAS we can have only one flavor per PodSet
  
  So the question, how would we reconcile that? Should `PodSetResources`, some ideas for (1.):
  - make the `PodSetResources` to internally keep SinglePodRequests and have a function with TotalRequests
  - keep both `SinglePodRequests` and `TotalRequets` redudantly
  - make `PodSetResources` keep total requests internally and derive `SinglePodRequests` in a function
  
  I think for (2.) it is quite straighforward that we would fit into the "Flavors" field, and just have a helper function OnlyFlavor which would be called in the TAS context.
  
  WDYT? I'm ok to make the changes in this PR or in a follow up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:29:36Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:401** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:29:36Z
```diff
@@ -363,17 +386,37 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		tasRequests := assignment.TopologyRequestsFor(a.wl, requests, a.cq)
+		if assignment.RepresentativeMode() == Fit {
```

  done, it reduces the indentation too which is always a win :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:32:15Z

- **pkg/cache/clusterqueue_snapshot.go:159** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:32:15Z
```diff
@@ -149,3 +149,17 @@ func (c *ClusterQueueSnapshot) DominantResourceShareWithout(wlReq resources.Flav
 	}
 	return dominantResourceShare(c, without)
 }
+
+func (c *ClusterQueueSnapshot) FindTopologyAssignments(
+	tasRequestsByFlavor map[kueue.ResourceFlavorReference][]TASPodSetRequests) TASAssignmentsResult {
+	result := make(TASAssignmentsResult)
+	for tasFlavor, tasRequestsForFlavor := range tasRequestsByFlavor {
+		if tasFlavorCache := c.TASFlavors[tasFlavor]; tasFlavorCache != nil {
```

  Definitely not, but `c` is ClusterQueueSnapshot and as all snapshots it is modified in only one goroutine.
  We already checked that `tasFlavor` is in `c.TASFlavors` in `WorkloadsTopologyRequests`. 
  We could repeat the check here and output an error, but then we need to think what to make with it :) .

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:34:07Z

- **pkg/cache/clusterqueue_snapshot.go:159** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:34:06Z
```diff
@@ -149,3 +149,17 @@ func (c *ClusterQueueSnapshot) DominantResourceShareWithout(wlReq resources.Flav
 	}
 	return dominantResourceShare(c, without)
 }
+
+func (c *ClusterQueueSnapshot) FindTopologyAssignments(
+	tasRequestsByFlavor map[kueue.ResourceFlavorReference][]TASPodSetRequests) TASAssignmentsResult {
+	result := make(TASAssignmentsResult)
+	for tasFlavor, tasRequestsForFlavor := range tasRequestsByFlavor {
+		if tasFlavorCache := c.TASFlavors[tasFlavor]; tasFlavorCache != nil {
```

  I agree it is not ideal, and I rely on the external context. Let me know if you would like this improved and if you have something specific in mind.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:49:39Z

- **pkg/cache/clusterqueue_snapshot.go:153** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:49:39Z
```diff
@@ -149,3 +149,17 @@ func (c *ClusterQueueSnapshot) DominantResourceShareWithout(wlReq resources.Flav
 	}
 	return dominantResourceShare(c, without)
 }
+
+func (c *ClusterQueueSnapshot) FindTopologyAssignments(
```

  Done, and I wrapped the scary map[Flavor][]TASPodSetRequests into WorkloadTASRequests to encapsulate the idea,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:50:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:400** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:50:34Z
```diff
@@ -363,17 +386,37 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		tasRequests := assignment.TopologyRequestsFor(a.wl, requests, a.cq)
```

  not so simple, because it needs access to the `assignment` to update it which is the receiver. Unless we refactor stuff somehow, but I'm not sure how.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T15:57:53Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:408** by [@mimowo](https://github.com/mimowo) — 2025-02-11T15:57:53Z
```diff
@@ -363,17 +386,37 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		tasRequests := assignment.TopologyRequestsFor(a.wl, requests, a.cq)
+		if assignment.RepresentativeMode() == Fit {
+			result := a.cq.FindTopologyAssignments(tasRequests)
+			if failure := result.Failure(); failure != nil {
+				// There is at least one PodSet which does not fit
+				psAssignment := assignment.podSetByName(failure.PodSetName)
+				psAssignment.reason(failure.Reason)
+				// clear flavors to make the PodSet assignment as NoFit
+				psAssignment.Flavors = nil
```

  Actually, in this line is for assigning the Flavors. Clearing the flavors as nil will actually result in `NoFit` mode for the PodSetAssignment. The trick to re-compute the assignment is to clear the cached represenativeMode .
  
  There are alternative approaches, such as setting the `representativeMode=NoFit` without clearing the flavors. This would work too, but I'm not sure which one is better. I would propose to revisit this in a follow up PR where we have 2 possible options Preempt or NoFit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-11T16:09:10Z

- **pkg/cache/tas_cache_test.go:1099** by [@mimowo](https://github.com/mimowo) — 2025-02-11T16:09:10Z
```diff
@@ -1083,7 +1083,20 @@ func TestFindTopologyAssignment(t *testing.T) {
 			if err != nil {
 				t.Fatalf("failed to build the snapshot: %v", err)
 			}
-			gotAssignment, reason := snapshot.FindTopologyAssignment(&tc.request, tc.requests, tc.count, tc.tolerations)
+			tasInput := TASPodSetRequests{
+				PodSet: &kueue.PodSet{
+					Name:            "main",
+					TopologyRequest: &tc.request,
+					Template: corev1.PodTemplateSpec{
+						Spec: corev1.PodSpec{
+							Tolerations: tc.tolerations,
+						},
+					},
+				},
+				SinglePodRequests: tc.requests,
+				Count:             tc.count,
+			}
+			gotAssignment, reason := snapshot.findTopologyAssignment(tasInput, nil)
```

  +1, alternatively we could move the tests completely to TestScheduleForTAS, but let me move them to the exported function for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-11T22:49:36Z

- **pkg/resources/requests.go:52** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T21:34:46Z
```diff
@@ -49,6 +49,12 @@ func (r Requests) Divide(f int64) {
 	}
 }
 
+func (r Requests) Multiply(f int64) {
```

  ```suggestion
  func (r Requests) Mul(f int64) {
  ```
  
  We might want to align with `resources.Quantity` function name: https://pkg.go.dev/k8s.io/apimachinery/pkg/api/resource#Quantity.Mul
  
  Additionally, what if computation face the overflow or underflow? But, I guess that overflow will not happen since 1 core equals 1000 (mcore), which means int64 max value is about 9,223,372,036,854,77 cores. So, I think there are not  such a massive commuting cluster in the world.

- **pkg/scheduler/flavorassigner/flavorassigner.go:65** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T21:55:03Z
```diff
@@ -57,6 +57,15 @@ func (a *Assignment) Borrows() bool {
 	return a.Borrowing
 }
 
+func (a *Assignment) podSetAssignmentByName(psName string) *PodSetAssignment {
+	for index := range a.PodSets {
+		if a.PodSets[index].Name == psName {
+			return &a.PodSets[index]
+		}
+	}
+	return nil
```

  ```suggestion
  	if idx := slices.IndexFunc(a.PodSets, func(ps PodSetAssignment) bool { return ps.Name == psName }); idx != -1 {
  		return &a.PodSets[idx]
  	}
  	return nil
  ```
  
  Thanks to the standard `slices` library.

- **pkg/cache/tas_flavor_snapshot.go:238** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:02:58Z
```diff
@@ -223,6 +225,62 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	// PodSet indicates the name of the PodSet for which computing the
+	// TAS assignment failed.
+	PodSet podSetReference
```

  ```suggestion
  	PodSetName podSetReference
  ```
  
  If this is a short living name, a shorter name would be fine. But this is an exposed and long-living name. So, saying `name` would be better, IMO.

- **pkg/scheduler/flavorassigner/flavorassigner.go:407** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:13:29Z
```diff
@@ -363,17 +386,35 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
+		tasRequests := assignment.WorkloadsTopologyRequests(a.wl, requests, a.cq)
+		result := a.cq.FindTopologyAssignmentsForWorkload(tasRequests)
+		if failure := result.Failure(); failure != nil {
+			// There is at least one PodSet which does not fit
+			psAssignment := assignment.podSetAssignmentByName(string(failure.PodSet))
+			psAssignment.reason(failure.Reason)
+			// clear flavors to make the PodSet assignment as NoFit
+			psAssignment.Flavors = nil
+			// clear the representative mode as
```

  What is following comments...?

- **pkg/scheduler/flavorassigner/flavorassigner.go:408** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:14:13Z
```diff
@@ -363,17 +386,35 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
+		tasRequests := assignment.WorkloadsTopologyRequests(a.wl, requests, a.cq)
+		result := a.cq.FindTopologyAssignmentsForWorkload(tasRequests)
+		if failure := result.Failure(); failure != nil {
+			// There is at least one PodSet which does not fit
+			psAssignment := assignment.podSetAssignmentByName(string(failure.PodSet))
+			psAssignment.reason(failure.Reason)
+			// clear flavors to make the PodSet assignment as NoFit
+			psAssignment.Flavors = nil
+			// clear the representative mode as
+			assignment.representativeMode = nil
```

  ```suggestion
  			assignment.representativeMode = noFit
  ```
  
  Is there any reason why we do not obviously set `noFit`?

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:23:07Z
```diff
@@ -20,64 +20,62 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
```

  Exposing this error and then adding UT for this error would be great.

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:69** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:33:50Z
```diff
@@ -20,64 +20,62 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
+	}
+	psResources := wl.TotalRequests[podSetIndex]
+	singlePodRequests := psResources.Requests.Clone()
+	singlePodRequests.Divide(int64(psResources.Count))
+	podCount := psAssignment.Count
+	tasFlvr, err := onlyFlavor(psAssignment.Flavors)
+	if err != nil {
+		return nil, err
+	}
+	snapshot := cq.TASFlavors[*tasFlvr]
+	if snapshot == nil {
```

  ```suggestion
  	if cq.TASFlavors[*tasFlvr] == nil {
  ```
  
  I think this can avoid allocation.

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:69** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:35:38Z
```diff
@@ -20,64 +20,62 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
+	}
+	psResources := wl.TotalRequests[podSetIndex]
+	singlePodRequests := psResources.Requests.Clone()
+	singlePodRequests.Divide(int64(psResources.Count))
+	podCount := psAssignment.Count
+	tasFlvr, err := onlyFlavor(psAssignment.Flavors)
+	if err != nil {
+		return nil, err
+	}
+	snapshot := cq.TASFlavors[*tasFlvr]
+	if snapshot == nil {
```

  Or, we might be able to just check if `cq.TASFlavors` has `tasFlvr` key like `if _, ok := ...; !ok`.
  Is there any situation where the TASFlavors has key, but value is null?

- **pkg/cache/tas_flavor_snapshot.go:34** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:37:48Z
```diff
@@ -31,6 +31,8 @@ import (
 	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
 )
 
+type podSetReference string
```

  Is there any motivation the reason why we want to use this reference?
  This seems to cause casting (`string()`) in some places across packages.

- **pkg/cache/tas_flavor_snapshot.go:226** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T22:46:19Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  > keep both SinglePodRequests and TotalRequets redudantly
  
  Do we really need this? I guess single pod requests can be represented as `TotalRequest[idx].Requests / TotalRequest[idx].Count`?
  
  Is there a reason why we prepare just the helper function? I can imagine the number of `TotalRequest` is smaller since it indicates the number of PodSet. At most, it is 4 like (pre-processing, driver, worker, post-processing).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:19:52Z

- **pkg/cache/tas_flavor_snapshot.go:226** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:19:52Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  > Do we really need this?
  
  I don't think so, it is one of the options in my previous comment. Other options I mentioned is to keep only one field and derive the other (by dividing or multiplication) in a function. I'm ok with either approach.
  
  I can give it a try today in this PR, but if I encounter issues I would prefer to leave it for follow up to unblock the functional PRs (which is still preemption within ClusterQueue and cohort support).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:21:05Z

- **pkg/resources/requests.go:52** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:21:05Z
```diff
@@ -49,6 +49,12 @@ func (r Requests) Divide(f int64) {
 	}
 }
 
+func (r Requests) Multiply(f int64) {
```

  Yeah, I would not worry about that. I think we already do multiplication in some places, even if it is not wrapped in a function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:24:17Z

- **pkg/resources/requests.go:52** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:24:17Z
```diff
@@ -49,6 +49,12 @@ func (r Requests) Divide(f int64) {
 	}
 }
 
+func (r Requests) Multiply(f int64) {
```

  Yeah, we already have this https://github.com/kubernetes-sigs/kueue/blob/51206f8efc42613837fa5b4a98d9ab664932e855/pkg/workload/workload.go#L435-L439 to multiple so I don't think this new function introduces more risk of overflow. I would like to commonize the functions in a follow up. I think the new "Mul" function is in a better place as the function is more genereric than just for workloads.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:29:00Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:65** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:29:00Z
```diff
@@ -57,6 +57,15 @@ func (a *Assignment) Borrows() bool {
 	return a.Borrowing
 }
 
+func (a *Assignment) podSetAssignmentByName(psName string) *PodSetAssignment {
+	for index := range a.PodSets {
+		if a.PodSets[index].Name == psName {
+			return &a.PodSets[index]
+		}
+	}
+	return nil
```

  Right, I knew about it, but reverted when realized the simple version has 95 characters, but using the library 126 :). 
  
  In any case I'm updating the code tentatively.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:30:23Z

- **pkg/cache/tas_flavor_snapshot.go:238** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:30:23Z
```diff
@@ -223,6 +225,62 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	// PodSet indicates the name of the PodSet for which computing the
+	// TAS assignment failed.
+	PodSet podSetReference
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:32:33Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:407** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:32:33Z
```diff
@@ -363,17 +386,35 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
+		tasRequests := assignment.WorkloadsTopologyRequests(a.wl, requests, a.cq)
+		result := a.cq.FindTopologyAssignmentsForWorkload(tasRequests)
+		if failure := result.Failure(); failure != nil {
+			// There is at least one PodSet which does not fit
+			psAssignment := assignment.podSetAssignmentByName(string(failure.PodSet))
+			psAssignment.reason(failure.Reason)
+			// clear flavors to make the PodSet assignment as NoFit
+			psAssignment.Flavors = nil
+			// clear the representative mode as
```

  Updated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:33:07Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:408** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:33:07Z
```diff
@@ -363,17 +386,35 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
+		tasRequests := assignment.WorkloadsTopologyRequests(a.wl, requests, a.cq)
+		result := a.cq.FindTopologyAssignmentsForWorkload(tasRequests)
+		if failure := result.Failure(); failure != nil {
+			// There is at least one PodSet which does not fit
+			psAssignment := assignment.podSetAssignmentByName(string(failure.PodSet))
+			psAssignment.reason(failure.Reason)
+			// clear flavors to make the PodSet assignment as NoFit
+			psAssignment.Flavors = nil
+			// clear the representative mode as
+			assignment.representativeMode = nil
```

  NoFit works equally well. I just wanted to assign nil to make sure it recomputes as NoFit, but since the result is guaranteed assigning directly is faster.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T08:34:18Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:69** by [@mimowo](https://github.com/mimowo) — 2025-02-12T08:34:18Z
```diff
@@ -20,64 +20,62 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
+	}
+	psResources := wl.TotalRequests[podSetIndex]
+	singlePodRequests := psResources.Requests.Clone()
+	singlePodRequests.Divide(int64(psResources.Count))
+	podCount := psAssignment.Count
+	tasFlvr, err := onlyFlavor(psAssignment.Flavors)
+	if err != nil {
+		return nil, err
+	}
+	snapshot := cq.TASFlavors[*tasFlvr]
+	if snapshot == nil {
```

  Updated.
  > Is there any situation where the TASFlavors has key, but value is null?
  
  This should not happen,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T09:20:49Z

- **pkg/cache/tas_flavor_snapshot.go:34** by [@mimowo](https://github.com/mimowo) — 2025-02-12T09:20:49Z
```diff
@@ -31,6 +31,8 @@ import (
 	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
 )
 
+type podSetReference string
```

  The motivation is to have a compile-time verification the value put as a key in map is a PodSet name. 
  
  Basically, the same motivation as for introducing the FlavorReference and TopologyReference types.
  
  > This seems to cause casting (string()) in some places across packages.
  
  In this PR yes, but I would like to follow up, when we can drop it all together. WDYT?
  
  Alternatively I use string in this PR and we open issue to do it throught.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T09:46:48Z

- **pkg/cache/tas_flavor_snapshot.go:34** by [@mimowo](https://github.com/mimowo) — 2025-02-12T09:46:47Z
```diff
@@ -31,6 +31,8 @@ import (
 	utiltas "sigs.k8s.io/kueue/pkg/util/tas"
 )
 
+type podSetReference string
```

  Ok, going with string and just added a comment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T09:50:58Z

- **pkg/cache/clusterqueue_snapshot.go:159** by [@mimowo](https://github.com/mimowo) — 2025-02-12T09:50:58Z
```diff
@@ -149,3 +149,17 @@ func (c *ClusterQueueSnapshot) DominantResourceShareWithout(wlReq resources.Flav
 	}
 	return dominantResourceShare(c, without)
 }
+
+func (c *ClusterQueueSnapshot) FindTopologyAssignments(
+	tasRequestsByFlavor map[kueue.ResourceFlavorReference][]TASPodSetRequests) TASAssignmentsResult {
+	result := make(TASAssignmentsResult)
+	for tasFlavor, tasRequestsForFlavor := range tasRequestsByFlavor {
+		if tasFlavorCache := c.TASFlavors[tasFlavor]; tasFlavorCache != nil {
```

  Added a comment along the lines, and removed the safety check. If the assumption is incorrect the code will blow with panic rather than hide the issue and make invalid assinment. PTAL. Marking as resolved.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T09:57:13Z

- **pkg/cache/tas_flavor_snapshot.go:243** by [@mimowo](https://github.com/mimowo) — 2025-02-12T09:57:12Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
+}
+
+type TASAssignmentsResult map[string]TASPodSetAssignmentResult
```

  Eventually went with the comment and string. Opened issue for future cleanup: https://github.com/kubernetes-sigs/kueue/issues/4242

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T09:58:33Z

- **pkg/cache/tas_flavor_snapshot.go:235** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T09:58:33Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
```

  Oh sorry about that, I meant `Reason` field in `type TASPodSetAssignmentResult`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T10:01:35Z

- **pkg/cache/tas_flavor_snapshot.go:235** by [@mimowo](https://github.com/mimowo) — 2025-02-12T10:01:34Z
```diff
@@ -212,6 +212,56 @@ func (s *TASFlavorSnapshot) addUsage(domainID utiltas.TopologyDomainID, usage re
 	s.leaves[domainID].freeCapacity.Sub(usage)
 }
 
+type TASPodSetRequests struct {
+	PodSet            *kueue.PodSet
+	SinglePodRequests resources.Requests
+	Count             int32
+	Flavor            kueue.ResourceFlavorReference
+}
+
+type FailureInfo struct {
+	PodSetName string
+	Reason     string
```

  Ahh, good point, renamed to FailureReason in `TASPodSetAssignmentResult` + I renamed the type to `tasPodSetAssignmentResult` as it is not externally used.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T10:02:04Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:58** by [@mimowo](https://github.com/mimowo) — 2025-02-12T10:02:04Z
```diff
@@ -20,64 +20,62 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
```

  Right, but it wasn't exposed prior to the PR, I would suggest a follow up to add the unit test. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T10:05:46Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:58** by [@mimowo](https://github.com/mimowo) — 2025-02-12T10:05:46Z
```diff
@@ -20,64 +20,62 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
```

  https://github.com/kubernetes-sigs/kueue/issues/3761#issuecomment-2653237909

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T11:10:36Z

- **pkg/cache/tas_flavor_snapshot.go:226** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T11:10:36Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  SGTM

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T12:40:39Z

- **pkg/cache/tas_cache_test.go:1099** by [@mimowo](https://github.com/mimowo) — 2025-02-12T12:40:38Z
```diff
@@ -1083,7 +1083,20 @@ func TestFindTopologyAssignment(t *testing.T) {
 			if err != nil {
 				t.Fatalf("failed to build the snapshot: %v", err)
 			}
-			gotAssignment, reason := snapshot.FindTopologyAssignment(&tc.request, tc.requests, tc.count, tc.tolerations)
+			tasInput := TASPodSetRequests{
+				PodSet: &kueue.PodSet{
+					Name:            "main",
+					TopologyRequest: &tc.request,
+					Template: corev1.PodTemplateSpec{
+						Spec: corev1.PodSpec{
+							Tolerations: tc.tolerations,
+						},
+					},
+				},
+				SinglePodRequests: tc.requests,
+				Count:             tc.count,
+			}
+			gotAssignment, reason := snapshot.findTopologyAssignment(tasInput, nil)
```

  Done. However currently the unit test only adjusts the input and output using the single "main" PodSet. I would prefer to extend the coverage for multiple PodSets in a follow up. Multiple PodSets are already covered at the higher level TestSchedule test: https://github.com/kubernetes-sigs/kueue/blob/79e9141322be2562f8b661aa0bf782f731bf1cdc/pkg/scheduler/scheduler_test.go#L4476

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-12T13:01:56Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:65** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T13:01:56Z
```diff
@@ -57,6 +57,15 @@ func (a *Assignment) Borrows() bool {
 	return a.Borrowing
 }
 
+func (a *Assignment) podSetAssignmentByName(psName string) *PodSetAssignment {
+	for index := range a.PodSets {
+		if a.PodSets[index].Name == psName {
+			return &a.PodSets[index]
+		}
+	}
+	return nil
```

  Oh, I see. That sounds good, thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-12T13:05:56Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:408** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T13:05:56Z
```diff
@@ -363,17 +386,35 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
+		tasRequests := assignment.WorkloadsTopologyRequests(a.wl, requests, a.cq)
+		result := a.cq.FindTopologyAssignmentsForWorkload(tasRequests)
+		if failure := result.Failure(); failure != nil {
+			// There is at least one PodSet which does not fit
+			psAssignment := assignment.podSetAssignmentByName(string(failure.PodSet))
+			psAssignment.reason(failure.Reason)
+			// clear flavors to make the PodSet assignment as NoFit
+			psAssignment.Flavors = nil
+			// clear the representative mode as
+			assignment.representativeMode = nil
```

  My point is different behavior for `noFit` situation in flavorassigner. In other places, assigned says obviously `noFit` 
  
  - https://github.com/kubernetes-sigs/kueue/blob/70517e20ab5bc3b19125e535d588f7e91eed1203/pkg/scheduler/flavorassigner/flavorassigner.go#L650-L655
  - https://github.com/kubernetes-sigs/kueue/blob/70517e20ab5bc3b19125e535d588f7e91eed1203/pkg/scheduler/flavorassigner/flavorassigner.go#L470
  
  So, I would offer to keep consistency behavior for the NoFit situation.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-12T13:06:47Z

- **pkg/cache/tas_flavor_snapshot.go:226** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T13:06:47Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  > I can give it a try today in this PR, but if I encounter issues I would prefer to leave it for follow up to unblock the functional PRs (which is still preemption within ClusterQueue and cohort support).
  
  Separate PR sounds good to me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T13:10:39Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:408** by [@mimowo](https://github.com/mimowo) — 2025-02-12T13:10:38Z
```diff
@@ -363,17 +386,35 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
+		tasRequests := assignment.WorkloadsTopologyRequests(a.wl, requests, a.cq)
+		result := a.cq.FindTopologyAssignmentsForWorkload(tasRequests)
+		if failure := result.Failure(); failure != nil {
+			// There is at least one PodSet which does not fit
+			psAssignment := assignment.podSetAssignmentByName(string(failure.PodSet))
+			psAssignment.reason(failure.Reason)
+			// clear flavors to make the PodSet assignment as NoFit
+			psAssignment.Flavors = nil
+			// clear the representative mode as
+			assignment.representativeMode = nil
```

  If we set to nil it would be recomputed to NoFit anyway. This is because representativeMode is just a cache.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-12T13:12:39Z

Thank you!
/approve
/lgtm

/hold for @PBundyra

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T13:09:45Z
```diff
@@ -20,64 +20,62 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
```

  SGTM

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:69** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T13:10:23Z
```diff
@@ -20,64 +20,61 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
+	}
+	psResources := wl.TotalRequests[podSetIndex]
+	singlePodRequests := psResources.Requests.Clone()
+	singlePodRequests.Divide(int64(psResources.Count))
+	podCount := psAssignment.Count
+	tasFlvr, err := onlyFlavor(psAssignment.Flavors)
+	if err != nil {
+		return nil, err
+	}
+	if cq.TASFlavors[*tasFlvr] == nil {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information for the assigned flavor")
```

  Here, let us implement UTs similar to above. I will add this followup list issue.

- **pkg/scheduler/flavorassigner/flavorassigner.go:408** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T13:11:44Z
```diff
@@ -363,17 +385,35 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 			psAssignment.append(flavors, status)
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
-				assignTopology(log, &psAssignment, a.cq, a.wl.TotalRequests[i], &a.wl.Obj.Spec.PodSets[i])
-			}
-		}
 
 		assignment.append(podSet.Requests, &psAssignment)
 		if psAssignment.Status.IsError() || (len(podSet.Requests) > 0 && len(psAssignment.Flavors) == 0) {
 			return assignment
 		}
 	}
+	if assignment.RepresentativeMode() == NoFit {
+		return assignment
+	}
+
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == Fit {
+		tasRequests := assignment.WorkloadsTopologyRequests(a.wl, requests, a.cq)
+		result := a.cq.FindTopologyAssignmentsForWorkload(tasRequests)
+		if failure := result.Failure(); failure != nil {
+			// There is at least one PodSet which does not fit
+			psAssignment := assignment.podSetAssignmentByName(failure.PodSetName)
+			psAssignment.reason(failure.Reason)
+			// clear flavors to make the PodSet assignment as NoFit
+			psAssignment.Flavors = nil
+			// update the representative mode accordingly
+			assignment.representativeMode = ptr.To(NoFit)
```

  Oh, you already have addressed, thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T13:21:41Z

- **pkg/cache/tas_flavor_snapshot.go:226** by [@mimowo](https://github.com/mimowo) — 2025-02-12T13:21:41Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  I just realized there is one more complication: `PodSetResources` does not reference `kueue.PodSet`, I add it in `TASPodSetRequests` as ptr. This is needed for example as input with tolerations for the algorithm. So, I think we would need to also extend `PodSetResources` with `*kueue.PodSet`. 
  
  I'm for it, but indeed prefer a follow up,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T13:50:52Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:69** by [@mimowo](https://github.com/mimowo) — 2025-02-12T13:50:52Z
```diff
@@ -20,64 +20,61 @@ import (
 	"errors"
 	"fmt"
 
-	"github.com/go-logr/logr"
 	"k8s.io/utils/ptr"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-func assignTopology(log logr.Logger,
-	psAssignment *PodSetAssignment,
-	cq *cache.ClusterQueueSnapshot,
-	psResources workload.PodSetResources,
-	podSet *kueue.PodSet) {
-	switch {
-	case psAssignment.Status.IsError():
-		log.V(2).Info("There is no resource quota assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(psAssignment.Flavors) == 0:
-		log.V(2).Info("There is no flavor assignment for the workload. No need to check TAS.", "message", psAssignment.Status.Message())
-	case len(cq.TASFlavors) == 0:
-		if psAssignment.Status == nil {
-			psAssignment.Status = &Status{}
-		}
-		psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information")
-		psAssignment.Flavors = nil
-	default:
-		singlePodRequests := psResources.Requests.Clone()
-		singlePodRequests.Divide(int64(psResources.Count))
-		podCount := psAssignment.Count
-		tasFlvr, err := onlyFlavor(psAssignment.Flavors)
-		if err != nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
-			}
-			psAssignment.Status.err = err
-			psAssignment.Flavors = nil
-			return
-		}
-		snapshot := cq.TASFlavors[*tasFlvr]
-		if snapshot == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+// WorkloadsTopologyRequests - returns the TopologyRequests of the workload
+func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info,
+	requests []workload.PodSetResources,
+	cq *cache.ClusterQueueSnapshot) cache.WorkloadTASRequests {
+	tasRequests := make(cache.WorkloadTASRequests)
+	for i, podSet := range wl.Obj.Spec.PodSets {
+		if podSet.TopologyRequest != nil {
+			psAssignment := a.podSetAssignmentByName(podSet.Name)
+			if psAssignment.Status.IsError() {
+				// There is no resource quota assignment for the PodSet - no need to check TAS.
+				continue
 			}
-			psAssignment.Status.append("Workload requires Topology, but there is no TAS cache information for the assigned flavor")
-			psAssignment.Flavors = nil
-			return
-		}
-		var reason string
-		psAssignment.TopologyAssignment, reason = snapshot.FindTopologyAssignment(podSet.TopologyRequest,
-			singlePodRequests, podCount, podSet.Template.Spec.Tolerations)
-		if psAssignment.TopologyAssignment == nil {
-			if psAssignment.Status == nil {
-				psAssignment.Status = &Status{}
+			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, i)
+			if err != nil {
+				psAssignment.error(err)
+			} else {
+				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
-			psAssignment.Status.append(reason)
-			psAssignment.Flavors = nil
 		}
-		log.Info("TAS PodSet assignment", "tasAssignment", psAssignment.TopologyAssignment)
 	}
+	return tasRequests
+}
+
+func podSetTopologyRequest(psAssignment *PodSetAssignment,
+	wl *workload.Info,
+	cq *cache.ClusterQueueSnapshot,
+	podSetIndex int) (*cache.TASPodSetRequests, error) {
+	if len(cq.TASFlavors) == 0 {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information")
+	}
+	psResources := wl.TotalRequests[podSetIndex]
+	singlePodRequests := psResources.Requests.Clone()
+	singlePodRequests.Divide(int64(psResources.Count))
+	podCount := psAssignment.Count
+	tasFlvr, err := onlyFlavor(psAssignment.Flavors)
+	if err != nil {
+		return nil, err
+	}
+	if cq.TASFlavors[*tasFlvr] == nil {
+		return nil, errors.New("workload requires Topology, but there is no TAS cache information for the assigned flavor")
```

  Added to the tracking [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T13:51:41Z

- **pkg/cache/tas_flavor_snapshot.go:226** by [@mimowo](https://github.com/mimowo) — 2025-02-12T13:51:41Z
```diff
@@ -223,6 +223,56 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+type TASPodSetRequests struct {
```

  Added the refactoring to the tracking [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU) as "commonize the structures for PodSetResources and TASPodSetRequests"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T13:53:30Z

- **pkg/cache/tas_cache_test.go:1099** by [@mimowo](https://github.com/mimowo) — 2025-02-12T13:53:30Z
```diff
@@ -1083,7 +1083,20 @@ func TestFindTopologyAssignment(t *testing.T) {
 			if err != nil {
 				t.Fatalf("failed to build the snapshot: %v", err)
 			}
-			gotAssignment, reason := snapshot.FindTopologyAssignment(&tc.request, tc.requests, tc.count, tc.tolerations)
+			tasInput := TASPodSetRequests{
+				PodSet: &kueue.PodSet{
+					Name:            "main",
+					TopologyRequest: &tc.request,
+					Template: corev1.PodTemplateSpec{
+						Spec: corev1.PodSpec{
+							Tolerations: tc.tolerations,
+						},
+					},
+				},
+				SinglePodRequests: tc.requests,
+				Count:             tc.count,
+			}
+			gotAssignment, reason := snapshot.findTopologyAssignment(tasInput, nil)
```

  I added the refactoring of the unit test to the tracking [spreadsheet](https://docs.google.com/spreadsheets/d/1MXCjKZtAfqBTb61bJo46u7jIUqRIlu1NrYj1L8Xz-UU) as "refactor the unit test in pkg/cache/tas_cache_test.go to test multiple PodSets"
