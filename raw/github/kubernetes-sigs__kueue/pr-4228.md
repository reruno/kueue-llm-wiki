# PR #4228: TAS: Add MostAllocated algorithm

**Summary**: TAS: Add MostAllocated algorithm

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4228

**Last updated**: 2025-08-12T10:15:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `bestFit-TAS` → `main`
- **Created**: 2025-02-11T14:07:08Z
- **Updated**: 2025-08-12T10:15:50Z
- **Merged**: 2025-02-14T11:02:22Z
- **Closed**: 2025-02-14T11:02:22Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+370 / -61**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR introduces a new default algorithm to TAS (old one gated by the `LargestFitTAS` feature flag). This algorithm selects the domain that can accommodate all pods, and has the least amount of free resources, instead of the most amount of free resources, as it's done currently. This helps mitigate the resource fragmentation. The KEP has been updated to reflect these changes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3756

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Add a new default algorithm to minimize resource fragmentation. The old algorithm is gated behind the `TASLargestFit` feature gate.
```

## Discussion

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T14:07:16Z

/assign @mimowo

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-11T14:07:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 007bd35cbde1fd807b7d4b619318277490b9526e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67af1d8b9f3a440008ba4289 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-11T14:09:24Z

I skimmed through it and it looks great! 
cc @gabesaba @tenzen-y would you like to give it a proper pass too / first?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T14:24:41Z

cc @mwysokin for visibility

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T14:30:12Z

I'm adding more optimizations to `updateCountsToMinimum()`

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T15:29:42Z

@gabesaba PTAL

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-11T17:03:48Z

> I skimmed through it and it looks great! cc @gabesaba @tenzen-y would you like to give it a proper pass too / first?

let me see.

> I'm adding more optimizations to updateCountsToMinimum()

Is this working in progress?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T09:48:25Z

> Is this working in progress?

It's ready for review

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T11:51:24Z

> > Is this working in progress?
> 
> It's ready for review

ACK

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-13T18:19:41Z

LGTM label has been added.  <details>Git tree hash: 90e72e884adf7d1716f8ca3562f09b7fc2110c10</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T18:20:32Z

/release-note-edit
```release-note
TAS: Add a new default algorithm to minimize resource fragmentation. The old algorithm is gated behind the `LargestFitTAS` feature gate. 
```

Let us use the same release note form for the TAS.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:49:48Z

/hold cancel
I will add LGTM when the nits are addressed

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-14T10:45:06Z

/lgtm
/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-14T10:45:13Z

LGTM label has been added.  <details>Git tree hash: ab6cd17d4dc295d83fb4220b1468aa291290f564</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-14T10:45:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4228#issuecomment-2658957211" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4228#" title="Author self-approved">PBundyra</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4228#pullrequestreview-2615883362" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-11T14:32:28Z

- **pkg/cache/tas_flavor_snapshot.go:303** by [@gabesaba](https://github.com/gabesaba) — 2025-02-11T14:31:43Z
```diff
@@ -296,6 +297,25 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomain(domains []*domain, count int32) *domain {
```

  how many domains do we expect at most - and does this number justify using binary search versus linear search? if yes, can we reuse https://go.dev/src/sort/search.go?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T14:37:59Z

- **pkg/cache/tas_flavor_snapshot.go:303** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T14:37:58Z
```diff
@@ -296,6 +297,25 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomain(domains []*domain, count int32) *domain {
```

  I believe we should support clusters that can have dozens of thousands of nodes so this is the number of domains we should support. Thanks for pointing to the `search.go`, I'm on it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-11T15:29:51Z

- **pkg/cache/tas_flavor_snapshot.go:303** by [@PBundyra](https://github.com/PBundyra) — 2025-02-11T15:29:50Z
```diff
@@ -296,6 +297,25 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomain(domains []*domain, count int32) *domain {
```

  Done, ptal

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T07:39:24Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@mimowo](https://github.com/mimowo) — 2025-02-12T07:39:24Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+
+By default Kueue uses the `LargestFit` algorithm. To use `BestFit` algorithm, a user needs to set the feature gate `BestFitTAS` to `true`
```

  Actually, we will want to change the default, right? I think the feature gate is just to provide the ability to restore the behavior in 0.11 when a user hits issues, but I would prefer not need to maintain two modes long term.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-12T07:40:54Z

- **pkg/features/kube_features.go:213** by [@mimowo](https://github.com/mimowo) — 2025-02-12T07:40:54Z
```diff
@@ -204,6 +210,7 @@ var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
 	ManagedJobsNamespaceSelector:        {Default: true, PreRelease: featuregate.Beta},
 	LocalQueueMetrics:                   {Default: false, PreRelease: featuregate.Alpha},
 	LocalQueueDefaulting:                {Default: false, PreRelease: featuregate.Alpha},
+	BestFitTAS:                          {Default: false, PreRelease: featuregate.Alpha},
```

  I would actually suggest to make it directly to Beta in 0.11, as it seems much more sensible than the current behavior for most users. Then we would graduate to GA and drop the gate in 0.13 probably.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T09:48:34Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T09:48:34Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+
+By default Kueue uses the `LargestFit` algorithm. To use `BestFit` algorithm, a user needs to set the feature gate `BestFitTAS` to `true`
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T09:49:04Z

- **pkg/features/kube_features.go:213** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T09:49:04Z
```diff
@@ -204,6 +210,7 @@ var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
 	ManagedJobsNamespaceSelector:        {Default: true, PreRelease: featuregate.Beta},
 	LocalQueueMetrics:                   {Default: false, PreRelease: featuregate.Alpha},
 	LocalQueueDefaulting:                {Default: false, PreRelease: featuregate.Alpha},
+	BestFitTAS:                          {Default: false, PreRelease: featuregate.Alpha},
```

  Discussed offline, and we decided to keep it alpha but set to deprecated already and drop in v0.13

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-12T10:16:29Z

- **pkg/cache/tas_flavor_snapshot.go:306** by [@gabesaba](https://github.com/gabesaba) — 2025-02-12T10:09:21Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
+		return domains[i].state < count
```

  How about `state <= count`? I think we can get rid of the conditional logic/subtraction below
  
  the only difference is that suppose we have the following setup:
  
  [10, 5, 5, 1], 5
  
  Current logic will choose index 2, while my suggestion will choose index 1. Does this matter?

- **pkg/cache/tas_flavor_snapshot.go:305** by [@gabesaba](https://github.com/gabesaba) — 2025-02-12T10:12:45Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
```

  when we don't find a fit, search returns index n. Can we explicitly handle this case (perhaps with a comment, saying no fit found)? In this case, do we want to return index 0 or index n-1?

- **pkg/cache/tas_cache_test.go:1327** by [@gabesaba](https://github.com/gabesaba) — 2025-02-12T10:16:09Z
```diff
@@ -1058,11 +1232,13 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			largestFit: true,
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			ctx := context.Background()
+			features.SetFeatureGateDuringTest(t, features.LargestFitTAS, tc.largestFit)
 
```

  rather than update all test names, could have done:
  
  ```
  if tc.largestFit {
    name = name + "; LargestFit"
  } else [
   ... 
  }
  t.Run(name, func(t *testing.T) {
  ```
  
  I'll leave this up to you if you want to do this way.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T10:47:45Z

- **pkg/cache/tas_flavor_snapshot.go:305** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T10:47:45Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
```

  If this function returns n it means that all domains have state >= count which is okay and we want to choose the last one as it's the one with the lowest state, so we return n-1 in this case. More problematic case is when `Search` returns 0 - it means that there is no domain that can fit the count - in that case I return 0 as it's the largest one, and it's gracefully handled afterwards

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T10:55:10Z

- **pkg/cache/tas_flavor_snapshot.go:306** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T10:55:10Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
+		return domains[i].state < count
```

  Can you expand on how we can get rid of the conditional subtraction? I think for case [10, 5, 5, 1] 6 the result without subtraction would be index 1 as this is the first index with state <= 6, which would be incorrect

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T11:04:41Z

- **pkg/cache/tas_cache_test.go:1327** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T11:04:41Z
```diff
@@ -1058,11 +1232,13 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			largestFit: true,
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			ctx := context.Background()
+			features.SetFeatureGateDuringTest(t, features.LargestFitTAS, tc.largestFit)
 
```

  Thanks! Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T11:17:13Z

- **pkg/cache/tas_cache_test.go:1327** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T11:17:13Z
```diff
@@ -1058,11 +1232,13 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			largestFit: true,
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			ctx := context.Background()
+			features.SetFeatureGateDuringTest(t, features.LargestFitTAS, tc.largestFit)
 
```

  Oh, if the featureGate is the only change in the two scenarios I wanted to keep the same name of the test, which is impossible as it's a duplicated key in a map. Hence, I needed to add some difference, which was BestFit/LargestFit suffix. Alternatively I could change the test cases type from map to list to allow duplicated names, but I would prefer to keep this PR as simple as possible + changes in tests' names are easily revertible

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-12T11:53:43Z

- **pkg/cache/tas_flavor_snapshot.go:306** by [@gabesaba](https://github.com/gabesaba) — 2025-02-12T11:53:43Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
+		return domains[i].state < count
```

  Yup I missed this, sorry.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-12T13:00:36Z

Pretty cool feature

- **keps/2724-topology-aware-scheduling/README.md:655** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T11:50:24Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  This seems to be the similar concept of the `LeastAllocated` and `MostAllocated` nodeResoucePlugin strategy.
  So, could we rename those
  
  - `LargestFit` -> `LeastAllocated`
  - `BestFit` -> `MostAllocated`
  
  Those can easily image the behavior since those are used in the kube-scheduler concept.
  
  Additionally, could we add knob to Configuration API when we graduate this featureGate to Beta.
  During alpha stage, I agree with keeping as feature gate. But after the Beta, I believe we want to improve the UX by knob in Configuration API.

- **pkg/cache/tas_flavor_snapshot.go:351** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T12:08:30Z
```diff
@@ -328,14 +346,24 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
-		if domain.state >= remainingCount {
-			domain.state = remainingCount
-			result = append(result, domain)
+	for i, domain := range domains {
+		// bestDomain is either domain with the most capacity or one that has the least capacity accommodating all pods
+		bestDomain := domain
```

  ```suggestion
  	// bestDomain is either domain with the most capacity or one that has the least capacity accommodating all pods
  	for i, bestDomain := range domains {
  ```
  
  Isn't this enough?

- **pkg/cache/tas_flavor_snapshot.go:351** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T12:11:34Z
```diff
@@ -328,14 +346,24 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
-		if domain.state >= remainingCount {
-			domain.state = remainingCount
-			result = append(result, domain)
+	for i, domain := range domains {
+		// bestDomain is either domain with the most capacity or one that has the least capacity accommodating all pods
+		bestDomain := domain
```

  Additionally, I think `best` term could bring confusing since we have 2 best here, 1 is `BestFit strategy`, 2 is `best domain to accommodate pods`.
  
  So, I'm wondering if we should revert this name to just `domain`.

- **pkg/cache/tas_flavor_snapshot.go:354** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T12:15:16Z
```diff
@@ -328,14 +346,24 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
-		if domain.state >= remainingCount {
-			domain.state = remainingCount
-			result = append(result, domain)
+	for i, domain := range domains {
+		// bestDomain is either domain with the most capacity or one that has the least capacity accommodating all pods
+		bestDomain := domain
+		if !features.Enabled(features.LargestFitTAS) {
+			bestFitIdx := findBestFitDomainIdx(domains[i:], remainingCount)
+			// if bestFitDomain is different than the one with the most capacity, assign it
```

  Here, which variable does indicate `bestFitDomain`? `domains[bestFitIdx]`?

- **pkg/cache/tas_flavor_snapshot.go:305** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T12:46:46Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
```

  Couldn't we handle the edge cases by `slices.BinarySearchFunc`, better? It allows us to know if searching can find the bestFitIdx.

- **keps/2724-topology-aware-scheduling/README.md:655** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-12T13:00:25Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  Actually, the expected `Best` is different based on user environments.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T15:26:32Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T15:26:32Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  > Additionally, could we add knob to Configuration API when we graduate this featureGate to Beta.
  During alpha stage, I agree with keeping as feature gate. But after the Beta, I believe we want to improve the UX by knob in Configuration API.
  
  I believe `BestFit` algorithm should be the default behavior and we shouldn't support `LargestFit` long term - we would like to drop the feature gate in v0.13
  
  However, I am aware that users should be allowed to have more impact on the strategy and we need some additional knobs/API to express that. At the same time I think we should gravitate towards more sophisticated solution of some kind of scoring nodes, maybe similarly to the one in scheduler.
  
  Having said that, I believe this is out of scope of this PR as we just wanted to quickly adjust current algorithm, and the feature gate is just for safety reasons/mitigating unpredicted issues.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T15:29:36Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T15:29:36Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  > This seems to be the similar concept of the LeastAllocated and MostAllocated nodeResoucePlugin strategy.
  > So, could we rename those
  >
  > LargestFit -> LeastAllocated
  > BestFit -> MostAllocated
  > Those can easily image the behavior since those are used in the kube-scheduler concept.
  
  Thanks, will do

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T15:36:38Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T15:36:38Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  > Actually, the expected `Best` is different based on user environments.
  
  Not sure if I understand this correctly, does it change anything with what you said above?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T15:39:47Z

- **pkg/cache/tas_flavor_snapshot.go:354** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T15:39:47Z
```diff
@@ -328,14 +346,24 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
-		if domain.state >= remainingCount {
-			domain.state = remainingCount
-			result = append(result, domain)
+	for i, domain := range domains {
+		// bestDomain is either domain with the most capacity or one that has the least capacity accommodating all pods
+		bestDomain := domain
+		if !features.Enabled(features.LargestFitTAS) {
+			bestFitIdx := findBestFitDomainIdx(domains[i:], remainingCount)
+			// if bestFitDomain is different than the one with the most capacity, assign it
```

  Index of the bestFitDomain for slice domains[i:] would be `i+bestFitIdx`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T15:43:12Z

- **pkg/cache/tas_flavor_snapshot.go:305** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T15:43:12Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
```

  I don't see clear benefits of using `slices.BinarySearchFunc`. Using `sort.Search()` also gives us information if searching found the best element, if it doesn't find one it returns 0, as all domains have state < count meaning none of them can accommodate all pods. In that case we return 0, as the index of the largest domain in the slice. 
  
  I've added a comment clarifying that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T16:22:39Z

- **pkg/cache/tas_flavor_snapshot.go:351** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T16:22:39Z
```diff
@@ -328,14 +346,24 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
-		if domain.state >= remainingCount {
-			domain.state = remainingCount
-			result = append(result, domain)
+	for i, domain := range domains {
+		// bestDomain is either domain with the most capacity or one that has the least capacity accommodating all pods
+		bestDomain := domain
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-12T16:22:52Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-12T16:22:52Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  Changed naming

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T08:26:29Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T08:26:29Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  > I believe BestFit algorithm should be the default behavior and we shouldn't support LargestFit long term - we would like to drop the feature gate in v0.13
  
  > > Actually, the expected Best is different based on user environments.
  > 
  > Not sure if I understand this correctly, does it change anything with what you said above?
  
  In that case, we will not support TAS for inference like StatefulSet, Deployment, LWS?
  For batch jobs, they should be allocated as packing (MostAllocated). For inference, they should be allocated as the spread (LeastAllocated), right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T08:37:14Z

- **pkg/cache/tas_flavor_snapshot.go:305** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T08:37:14Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
```

  My point was wondering if we can explain mechanism as codes instead of comments since the current impl complements the logic by comments (`// there is no domain that can accommodate all pods so we return the largest one`).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T08:37:27Z

- **pkg/cache/tas_flavor_snapshot.go:354** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T08:37:27Z
```diff
@@ -328,14 +346,24 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
-		if domain.state >= remainingCount {
-			domain.state = remainingCount
-			result = append(result, domain)
+	for i, domain := range domains {
+		// bestDomain is either domain with the most capacity or one that has the least capacity accommodating all pods
+		bestDomain := domain
+		if !features.Enabled(features.LargestFitTAS) {
+			bestFitIdx := findBestFitDomainIdx(domains[i:], remainingCount)
+			// if bestFitDomain is different than the one with the most capacity, assign it
```

  sgtm, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T10:00:37Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T10:00:36Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  I don't think users should rely on LeastAllocated mode when it comes to spreading PodSets. I believe we should address it in a separate feature that clearly exposes this possibility, as with LeastAllocated it works only for some cases

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-13T10:05:39Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@mimowo](https://github.com/mimowo) — 2025-02-13T10:05:39Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  I agree,support for "spreading" makes sense in the context of LWS and StatefulSet. However, we don't support it yet. The aim of the PR is to optimize the fragmentation in the context of "packing". 
  
  When supporting "spreading" I think we will need a dedicated API rather than a transient feature gate.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T10:20:59Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T10:20:59Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  That sounds reasonable. I could imagine leveraging existing scheduling directive like `podTopologySpreadConstraints` and `podAntiAffinity`. In that case, we may need to provide label whether or not we want to spread pods like `kueue.x-k8s.io/tas-spread: true`.  But that is different feature.
  
  Btw, is `LeastAllocatedTAS` actually deprecated featureGate instead of alpha, right?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T11:58:23Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T11:58:23Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  Oh yes, it's deprecated - I'll add a change for that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T15:11:25Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T15:11:25Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
+- `BestFit` - Kueue chooses the domain with the least resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LargestFit` - Kueue chooses the domain that has the most resources, providing better nodes utilization
+
+By default Kueue uses the `BestFit` algorithm. To use `LargestFit` algorithm, a user needs to set the feature gate `LargestFitTAS` to `true`
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-13T15:34:23Z

- **pkg/cache/tas_flavor_snapshot.go:374** by [@mimowo](https://github.com/mimowo) — 2025-02-13T15:34:22Z
```diff
@@ -296,6 +298,36 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx binsearches a sorted array of domains and finds index of
+// the first domain with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := sort.Search(len(domains), func(i int) bool {
+		return domains[i].state < count
+	})
+	if mostAllocatedFitIdx == 0 {
+		// all domains have state < count, so we return the largest one
+		return 0
+	}
+	// find the first occurrence of the most allocated domain
+	mostAllocatedFitState := domains[mostAllocatedFitIdx-1].state
+	left, right := 0, len(domains)-1
+	for left <= right {
+		mid := left + (right-left)/2
+		switch {
+		case domains[mid].state == mostAllocatedFitState:
+			mostAllocatedFitIdx = mid
+			right = mid - 1
+		case domains[mid].state > mostAllocatedFitState:
+			left = mid + 1
+		default:
+			right = mid - 1
+		}
+	}
+	return mostAllocatedFitIdx
```

  I must say this looks like premature optimization.  This search only fires once per list domains, right? The algorithm is already over-linear O(nlogn) with the number of domains. as we already sort them per level. We will never get below linear because we need to construct the snapshot in the first place (unless I'm missing something).
  
  If we really need binary search let's use library rather than re-implement it :)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T15:44:00Z

- **pkg/cache/tas_flavor_snapshot.go:374** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T15:44:00Z
```diff
@@ -296,6 +298,36 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx binsearches a sorted array of domains and finds index of
+// the first domain with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := sort.Search(len(domains), func(i int) bool {
+		return domains[i].state < count
+	})
+	if mostAllocatedFitIdx == 0 {
+		// all domains have state < count, so we return the largest one
+		return 0
+	}
+	// find the first occurrence of the most allocated domain
+	mostAllocatedFitState := domains[mostAllocatedFitIdx-1].state
+	left, right := 0, len(domains)-1
+	for left <= right {
+		mid := left + (right-left)/2
+		switch {
+		case domains[mid].state == mostAllocatedFitState:
+			mostAllocatedFitIdx = mid
+			right = mid - 1
+		case domains[mid].state > mostAllocatedFitState:
+			left = mid + 1
+		default:
+			right = mid - 1
+		}
+	}
+	return mostAllocatedFitIdx
```

  That's a good point, I've simplified the logic

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T15:47:12Z

- **pkg/cache/tas_flavor_snapshot.go:305** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T15:47:12Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
```

  Simplified the logic as indeed there's no need for O(log n) search, since we sort O(nlogn) the array before calling this function

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T15:49:44Z

- **pkg/cache/tas_flavor_snapshot.go:372** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T15:45:46Z
```diff
@@ -296,6 +297,22 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx binsearches a sorted array of domains and finds index of
+// the first domain with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := 0
+	for i, domain := range domains {
+		if domain.state < count {
+			break
+		} else if domain.state != domains[mostAllocatedFitIdx].state {
+			mostAllocatedFitIdx = i
+		}
```

  ```suggestion
  		}
  		if domain.state != domains[mostAllocatedFitIdx].state {
  			mostAllocatedFitIdx = i
  		}
  ```
  
  It just simplified the logic.

- **pkg/cache/tas_flavor_snapshot.go:363** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T15:46:39Z
```diff
@@ -296,6 +297,22 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx binsearches a sorted array of domains and finds index of
+// the first domain with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
```

  could you align with the current impl? IIUC, this is not binary search.

- **pkg/cache/tas_cache_test.go:229** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T15:49:21Z
```diff
@@ -221,8 +222,9 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		leastAllocated bool
 	}{
-		"minimize the number of used racks before optimizing the number of nodes": {
+		"minimize the number of used racks before optimizing the number of nodes; LeastAllocated": {
```

  Just confirmation. After some release, we will remove the deprecated `LeastAllocatedTAS` feature gates?
  In that case, could you add comments like `TODO: remove after removed LeastAllocatedTAS feature gates` to all removal test cases? Or Instead of that, we can just mention the removal cases in KEP graduation criteria.
  
  @mimowo wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T15:54:47Z

- **pkg/cache/tas_flavor_snapshot.go:305** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T15:54:47Z
```diff
@@ -296,6 +298,19 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findBestFitDomain binsearches a sorted array of domains and finds a domain
+// with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns the first element of the array
+func findBestFitDomainIdx(domains []*domain, count int32) int {
+	bestFitIdx := sort.Search(len(domains), func(i int) bool {
```

  Awesome!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-13T16:01:47Z

- **pkg/cache/tas_cache_test.go:229** by [@mimowo](https://github.com/mimowo) — 2025-02-13T16:01:47Z
```diff
@@ -221,8 +222,9 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		leastAllocated bool
 	}{
-		"minimize the number of used racks before optimizing the number of nodes": {
+		"minimize the number of used racks before optimizing the number of nodes; LeastAllocated": {
```

  We introduce the feature gate as a bailout mechanism as we already have users relying on TAS so we don't want to break them. 
  
  If we don't hear complains from users for one or two releases then Im happy to drop it. I would add it to graduation criteria for GA.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T16:14:08Z

- **pkg/cache/tas_cache_test.go:229** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T16:14:08Z
```diff
@@ -221,8 +222,9 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		leastAllocated bool
 	}{
-		"minimize the number of used racks before optimizing the number of nodes": {
+		"minimize the number of used racks before optimizing the number of nodes; LeastAllocated": {
```

  I'll mention that in KEP and create an issue on gh

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T16:15:09Z

- **pkg/cache/tas_cache_test.go:229** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-13T16:15:09Z
```diff
@@ -221,8 +222,9 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		leastAllocated bool
 	}{
-		"minimize the number of used racks before optimizing the number of nodes": {
+		"minimize the number of used racks before optimizing the number of nodes; LeastAllocated": {
```

  SGTM, thanks!

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T16:15:53Z

- **pkg/cache/tas_cache_test.go:229** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T16:15:52Z
```diff
@@ -221,8 +222,9 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		leastAllocated bool
 	}{
-		"minimize the number of used racks before optimizing the number of nodes": {
+		"minimize the number of used racks before optimizing the number of nodes; LeastAllocated": {
```

  > Just confirmation. After some release, we will remove the deprecated `LeastAllocatedTAS` feature gates?
  
  Correct

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T16:38:47Z

- **pkg/cache/tas_flavor_snapshot.go:363** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T16:38:47Z
```diff
@@ -296,6 +297,22 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx binsearches a sorted array of domains and finds index of
+// the first domain with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T16:38:55Z

- **pkg/cache/tas_flavor_snapshot.go:372** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T16:38:55Z
```diff
@@ -296,6 +297,22 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx binsearches a sorted array of domains and finds index of
+// the first domain with the lowest value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := 0
+	for i, domain := range domains {
+		if domain.state < count {
+			break
+		} else if domain.state != domains[mostAllocatedFitIdx].state {
+			mostAllocatedFitIdx = i
+		}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-13T16:43:25Z

- **pkg/cache/tas_cache_test.go:229** by [@PBundyra](https://github.com/PBundyra) — 2025-02-13T16:43:25Z
```diff
@@ -221,8 +222,9 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		leastAllocated bool
 	}{
-		"minimize the number of used racks before optimizing the number of nodes": {
+		"minimize the number of used racks before optimizing the number of nodes; LeastAllocated": {
```

  Added needed TODOs comments, mentioned dropping the flag in the KEP

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-13T18:19:34Z

Thank you!
/lgtm
/approve

/hold for @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T07:32:42Z

- **pkg/cache/tas_flavor_snapshot.go:368** by [@mimowo](https://github.com/mimowo) — 2025-02-14T07:23:15Z
```diff
@@ -356,6 +357,23 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx finds an index of the first domain with the lowest
+// value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := 0
+	for i, domain := range domains {
+		if domain.state < count {
+			break
```

  ```suggestion
  			return mostAllocatedFitIdx
  ```
  nit

- **pkg/cache/tas_flavor_snapshot.go:419** by [@mimowo](https://github.com/mimowo) — 2025-02-14T07:23:23Z
```diff
@@ -388,7 +409,12 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
+	for i, domain := range domains {
+		if !features.Enabled(features.LeastAllocatedTAS) {
+			mostAllocatedIdx := findMostAllocatedDomainIdx(domains[i:], remainingCount)
```

  I might be missing something here. I think this loop above `for i, domain := range domains` combined with linear `findMostAllocatedDomainIdx` makes me think again about complexity. Imagine you have 1000 nodes, all empty, and you have a big job spanning all of them, it would be quadratic, no?
  
  I was assuming in my other comment that the `findMostAllocatedDomainIdx` is run only to find a replacement for the last domain in the chain (when remainingCount drops to 0, or would drop to 0 when accounting for the domain).

- **pkg/cache/tas_flavor_snapshot.go:372** by [@mimowo](https://github.com/mimowo) — 2025-02-14T07:30:38Z
```diff
@@ -356,6 +357,23 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx finds an index of the first domain with the lowest
+// value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := 0
+	for i, domain := range domains {
+		if domain.state < count {
+			break
+		}
+		if domain.state != domains[mostAllocatedFitIdx].state {
+			mostAllocatedFitIdx = i
+		}
```

  IIUC this check is just to choose the first domain when there is a tie. 
  
  I guess this check chooses the first domain, while without it the last would be chosen in the island of the same "state". 
  
  Choosing the domain in case of a tie should not matter, but choosing the first will affect fewer tests for sure.

- **pkg/cache/tas_cache_test.go:375** by [@mimowo](https://github.com/mimowo) — 2025-02-14T07:32:37Z
```diff
@@ -365,8 +370,51 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			leastAllocated: true,
 		},
-		"host required; single Pod fits in the host": {
+		"block required; 4 pods fit into one host each; MostAllocated": {
```

  Do you have a test case demonstrating situation when the choice affect "mid" level in 3 levels of hierarch? In other words we choose "MostAllocated" rack, and then the choice translates to choosing nodes under this rack.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T07:47:21Z

- **pkg/cache/tas_flavor_snapshot.go:372** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T07:47:21Z
```diff
@@ -356,6 +357,23 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx finds an index of the first domain with the lowest
+// value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := 0
+	for i, domain := range domains {
+		if domain.state < count {
+			break
+		}
+		if domain.state != domains[mostAllocatedFitIdx].state {
+			mostAllocatedFitIdx = i
+		}
```

  I believe choosing the first domain among domains with the same state actually simplifies testing and debugging. Imagine you have 4 hosts spread between 2 racks, and you want to schedule 3 pods, then if we didn't choose the first domain we would schedule on `x1,x2` and `x4` hosts. It doesn't impact actual performance of the job, or general fragmentation but I think it's would be easier to reason about consecutive hosts `x1, x2, x3`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T07:48:44Z

- **pkg/cache/tas_flavor_snapshot.go:419** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T07:48:43Z
```diff
@@ -388,7 +409,12 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
+	for i, domain := range domains {
+		if !features.Enabled(features.LeastAllocatedTAS) {
+			mostAllocatedIdx := findMostAllocatedDomainIdx(domains[i:], remainingCount)
```

  Yes, that's true. We can prevent that by calling the `findMostAllocatedDomainIdx` only for the last domain in the chain. I'm on it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T07:58:47Z

- **pkg/cache/tas_cache_test.go:375** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T07:58:47Z
```diff
@@ -365,8 +370,51 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			leastAllocated: true,
 		},
-		"host required; single Pod fits in the host": {
+		"block required; 4 pods fit into one host each; MostAllocated": {
```

  Do you mean something like that? Firstly we choose the block with the mostAllocated, then rack and host 
  
  https://github.com/kubernetes-sigs/kueue/pull/4228/files#diff-85a93b2977c9efa55ca4846fdd219e22733e2b736c4fa3dba06f70739c1bbdfbR601-R627

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:04:30Z

- **pkg/cache/tas_flavor_snapshot.go:372** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:04:30Z
```diff
@@ -356,6 +357,23 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx finds an index of the first domain with the lowest
+// value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := 0
+	for i, domain := range domains {
+		if domain.state < count {
+			break
+		}
+		if domain.state != domains[mostAllocatedFitIdx].state {
+			mostAllocatedFitIdx = i
+		}
```

  yeah, makes sense. I'm ok with that. For the most part the comment was to check my understanding and if we are on the same page. You could maybe add a comment describing the purpose of the check as it might not be immediately obvious imo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:06:53Z

- **pkg/cache/tas_cache_test.go:375** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:06:53Z
```diff
@@ -365,8 +370,51 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			leastAllocated: true,
 		},
-		"host required; single Pod fits in the host": {
+		"block required; 4 pods fit into one host each; MostAllocated": {
```

  Yeah, and do you have a test case when the new strategy matters for the top level?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T09:14:38Z

- **pkg/cache/tas_flavor_snapshot.go:419** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T09:14:38Z
```diff
@@ -388,7 +409,12 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
+	for i, domain := range domains {
+		if !features.Enabled(features.LeastAllocatedTAS) {
+			mostAllocatedIdx := findMostAllocatedDomainIdx(domains[i:], remainingCount)
```

  > Imagine you have 1000 nodes, all empty, and you have a big job spanning all of them, it would be quadratic, no?
  
  Actually, it wouldn't be quadratic because if job spans across all nodes then the count would be big enough to brake in the if:
  
  ```
  if domain.state < count {
  	return mostAllocatedFitIdx
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:20:16Z

- **pkg/cache/tas_flavor_snapshot.go:419** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:20:16Z
```diff
@@ -388,7 +409,12 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
+	for i, domain := range domains {
+		if !features.Enabled(features.LeastAllocatedTAS) {
+			mostAllocatedIdx := findMostAllocatedDomainIdx(domains[i:], remainingCount)
```

  I see, you are right, because remainingCoubt is still large for all the domains except last. 
  
  I think this is awfully tricky calling the function and checking the condition inside the inner loop. Maybe it is enough to move the condition outside the inner loop or add comment. 
  
  I still think if we called this function only for the last domain it would be self explanatory why the complexity is linear and reflecting the intuition.
  
  Proving complexity is linear when you have two nested loops going to N it tricky in general.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T09:23:19Z

- **pkg/cache/tas_flavor_snapshot.go:372** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T09:23:18Z
```diff
@@ -356,6 +357,23 @@ func levelKey(topologyRequest *kueue.PodSetTopologyRequest) *string {
 	return nil
 }
 
+// findMostAllocatedDomainIdx finds an index of the first domain with the lowest
+// value of state, higher or equal than count.
+// If such a domain doesn't exist, it returns 0 as it's an index of the domain with the
+// most available resources
+func findMostAllocatedDomainIdx(domains []*domain, count int32) int {
+	mostAllocatedFitIdx := 0
+	for i, domain := range domains {
+		if domain.state < count {
+			break
+		}
+		if domain.state != domains[mostAllocatedFitIdx].state {
+			mostAllocatedFitIdx = i
+		}
```

  I see, I've added the comment

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T09:23:32Z

- **pkg/cache/tas_cache_test.go:375** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T09:23:32Z
```diff
@@ -365,8 +370,51 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			leastAllocated: true,
 		},
-		"host required; single Pod fits in the host": {
+		"block required; 4 pods fit into one host each; MostAllocated": {
```

  Is this what you had in mind?
  https://github.com/kubernetes-sigs/kueue/pull/4228/files#diff-85a93b2977c9efa55ca4846fdd219e22733e2b736c4fa3dba06f70739c1bbdfbR536-R586

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T09:25:57Z

- **pkg/cache/tas_flavor_snapshot.go:419** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T09:25:57Z
```diff
@@ -388,7 +409,12 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
+	for i, domain := range domains {
+		if !features.Enabled(features.LeastAllocatedTAS) {
+			mostAllocatedIdx := findMostAllocatedDomainIdx(domains[i:], remainingCount)
```

  I agree, updated the logic

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:34:28Z

- **pkg/cache/tas_flavor_snapshot.go:419** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:34:28Z
```diff
@@ -388,7 +409,12 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(levelIdx int, required bool,
 func (s *TASFlavorSnapshot) updateCountsToMinimum(domains []*domain, count int32) []*domain {
 	result := make([]*domain, 0)
 	remainingCount := count
-	for _, domain := range domains {
+	for i, domain := range domains {
+		if !features.Enabled(features.LeastAllocatedTAS) {
+			mostAllocatedIdx := findMostAllocatedDomainIdx(domains[i:], remainingCount)
```

  Actually, it might be a brain teaser at this point, but I think when the function used binary-search the complexity of choosing the domains after sorting was O(nlogn), while now it is O(n). (overall the algorithm complexity is still O(nlogn) because we sort the domains)..

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:37:20Z

- **pkg/cache/tas_cache_test.go:375** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:37:20Z
```diff
@@ -365,8 +370,51 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
+			leastAllocated: true,
 		},
-		"host required; single Pod fits in the host": {
+		"block required; 4 pods fit into one host each; MostAllocated": {
```

  Exactly, thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:39:12Z

- **keps/2724-topology-aware-scheduling/kep.yaml:34** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:39:12Z
```diff
@@ -31,6 +31,7 @@ milestone:
 # List the feature gate name and the components for which it must be enabled
 feature-gates:
   - name: TopologyAwareScheduling
+  - name: LeastAllocatedTAS
```

  ```suggestion
    - name: TASLeastAllocated
  ```
  nit, maybe we have more feature gates like this, and prefixing them with TAS will make them sort nicely.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:41:27Z

- **keps/2724-topology-aware-scheduling/README.md:651** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:41:27Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue can operate in two modes when it comes to choosing the best domain if there is more than one capable of accommodating all the pods:
```

  ```suggestion
  Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:42:24Z

- **pkg/cache/tas_cache_test.go:226** by [@mimowo](https://github.com/mimowo) — 2025-02-14T09:42:24Z
```diff
@@ -221,8 +222,11 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		// TODO: remove after dropping the LeastAllocatedTAS feature gate
+		leastAllocated bool
```

  ```suggestion
  		enableLeastAllocated bool
  ```
  nit, so that it is clear we are dealing with feature gate, and move it to the top.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T09:48:25Z

Overall LGTM, left some nits at this point

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T10:04:58Z

- **keps/2724-topology-aware-scheduling/kep.yaml:34** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T10:04:57Z
```diff
@@ -31,6 +31,7 @@ milestone:
 # List the feature gate name and the components for which it must be enabled
 feature-gates:
   - name: TopologyAwareScheduling
+  - name: LeastAllocatedTAS
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T10:05:04Z

- **pkg/cache/tas_cache_test.go:226** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T10:05:04Z
```diff
@@ -221,8 +222,11 @@ func TestFindTopologyAssignment(t *testing.T) {
 		tolerations    []corev1.Toleration
 		wantAssignment *kueue.TopologyAssignment
 		wantReason     string
+		// TODO: remove after dropping the LeastAllocatedTAS feature gate
+		leastAllocated bool
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T10:18:39Z

- **keps/2724-topology-aware-scheduling/kep.yaml:34** by [@mimowo](https://github.com/mimowo) — 2025-02-14T10:18:39Z
```diff
@@ -31,6 +31,7 @@ milestone:
 # List the feature gate name and the components for which it must be enabled
 feature-gates:
   - name: TopologyAwareScheduling
+  - name: LeastAllocatedTAS
```

  This seems not committed. PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-14T10:19:14Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@mimowo](https://github.com/mimowo) — 2025-02-14T10:19:14Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+
+By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `LeastAllocatedTAS` to `true`
```

  ```suggestion
  By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`
  ```
  check other places as well

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-14T10:40:21Z

- **keps/2724-topology-aware-scheduling/README.md:655** by [@PBundyra](https://github.com/PBundyra) — 2025-02-14T10:40:21Z
```diff
@@ -648,6 +648,12 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
+- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+
+By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `LeastAllocatedTAS` to `true`
```

  Done
