# PR #428: Increase coverage of pkg/queue

**Summary**: Increase coverage of pkg/queue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/428

**Last updated**: 2022-11-22T14:46:15Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `coverage-queue` → `main`
- **Created**: 2022-11-18T21:09:21Z
- **Updated**: 2022-11-22T14:46:15Z
- **Merged**: 2022-11-22T14:46:14Z
- **Closed**: 2022-11-22T14:46:14Z
- **Labels**: `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 10   **+213 / -80**

## Description

#### What type of PR is this?

/kind cleanup


#### What this PR does / why we need it:

Increases the unit coverage of pkg/queue from 62% to 79%

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

Note that codepaths were covered by unit tests in other packages and integration tests. But this should still help with developer speed when doing localized changes in pkg/queue.

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-11-18T21:09:25Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/428#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-11-21T22:45:10Z

/label tide/merge-method-squash
/lgtm
/hold

if you want to add the integration test in this PR

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-22T14:45:12Z

/hold cancel

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-11-21T20:41:05Z

- **pkg/queue/cluster_queue_impl_test.go:296** by [@ahg-g](https://github.com/ahg-g) — 2022-11-21T20:18:46Z
```diff
@@ -305,10 +289,10 @@ func TestClusterQueueImpl(t *testing.T) {
 			}
 
 			for _, w := range test.inadmissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, RequeueReasonNamespaceMismatch)
+				cq.requeueIfNotPresent(w, false)
 			}
 			for _, w := range test.admissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, RequeueReasonGeneric)
+				cq.requeueIfNotPresent(w, true)
```

  aren't we missing coverage for RequeueIfNotPresent by doing this? It would be good to test that we don't requeue immediately when `RequeueReasonNamespaceMismatch` is passed, may be a separate small test then?

- **pkg/queue/manager.go:462** by [@ahg-g](https://github.com/ahg-g) — 2022-11-21T20:22:48Z
```diff
@@ -459,10 +459,10 @@ func (m *Manager) Heads(ctx context.Context) []workload.Info {
 func (m *Manager) Dump() map[string]sets.String {
 	m.Lock()
 	defer m.Unlock()
-	if len(m.localQueues) == 0 {
+	if len(m.clusterQueues) == 0 {
```

  wow, I guess the tests always created the same number of cluster and local queues :)

- **pkg/queue/manager_test.go:160** by [@ahg-g](https://github.com/ahg-g) — 2022-11-21T20:33:25Z
```diff
@@ -87,25 +88,107 @@ func TestAddClusterQueueOrphans(t *testing.T) {
 	).Build()
 	ctx := context.Background()
 	manager := NewManager(kClient, nil)
+	cq := utiltesting.MakeClusterQueue("cq").Obj()
+	if err := manager.AddClusterQueue(ctx, cq); err != nil {
+		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+	}
 	for _, q := range queues {
 		if err := manager.AddLocalQueue(ctx, q); err != nil {
 			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
 		}
 	}
-	cq := utiltesting.MakeClusterQueue("cq").Obj()
+
+	wantActiveWorkloads := map[string]sets.String{
+		"cq": sets.NewString("/a", "/b"),
+	}
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after creating all objects (-want,+got):\n%s", diff)
+	}
+
+	// Recreating the ClusterQueue.
+	manager.DeleteClusterQueue(cq)
+	wantActiveWorkloads = nil
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after deleting ClusterQueue (-want,+got):\n%s", diff)
+	}
+
 	if err := manager.AddClusterQueue(ctx, cq); err != nil {
-		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+		t.Fatalf("Could not re-add ClusterQueue: %v", err)
 	}
-	workloads := popNamesFromCQ(manager.clusterQueues[cq.Name])
+	workloads := popNamesFromCQ(manager.clusterQueues["cq"])
 	wantWorkloads := []string{"/b", "/a"}
 	if diff := cmp.Diff(wantWorkloads, workloads); diff != "" {
 		t.Errorf("Workloads popped in the wrong order from clusterQueue:\n%s", diff)
 	}
 }
 
-// TestUpdateQueue tests that workloads are transferred between clusterQueues
+// TestUpdateClusterQueue tests that a ClusterQueue transfers cohorts on update.
+func TestUpdateClusterQueue(t *testing.T) {
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("cq1").Cohort("alpha").Obj(),
+		utiltesting.MakeClusterQueue("cq2").Cohort("beta").Obj(),
+	}
+	queues := []*kueue.LocalQueue{
+		utiltesting.MakeLocalQueue("foo", defaultNamespace).ClusterQueue("cq1").Obj(),
+		utiltesting.MakeLocalQueue("bar", defaultNamespace).ClusterQueue("cq2").Obj(),
+	}
+	now := time.Now()
+	workloads := []*kueue.Workload{
+		utiltesting.MakeWorkload("a", defaultNamespace).Queue("foo").Creation(now.Add(time.Second)).Obj(),
+		utiltesting.MakeWorkload("b", defaultNamespace).Queue("bar").Creation(now).Obj(),
+	}
+	// Setup.
+	scheme := utiltesting.MustGetScheme(t)
+	ctx := context.Background()
+	cl := fake.NewClientBuilder().WithScheme(scheme).WithObjects(
+		&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: defaultNamespace}},
+	).Build()
+	manager := NewManager(cl, nil)
+	for _, cq := range clusterQueues {
+		if err := manager.AddClusterQueue(ctx, cq); err != nil {
+			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
+		}
+	}
+	for _, q := range queues {
+		if err := manager.AddLocalQueue(ctx, q); err != nil {
+			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
+		}
+	}
+	// Add inadmissible workloads.
+	for _, w := range workloads {
+		if err := cl.Create(ctx, w); err != nil {
```

  Can we use WithObjects to add them to the cache directly when building the client above?

- **pkg/queue/manager_test.go:181** by [@ahg-g](https://github.com/ahg-g) — 2022-11-21T20:39:56Z
```diff
@@ -87,25 +88,107 @@ func TestAddClusterQueueOrphans(t *testing.T) {
 	).Build()
 	ctx := context.Background()
 	manager := NewManager(kClient, nil)
+	cq := utiltesting.MakeClusterQueue("cq").Obj()
+	if err := manager.AddClusterQueue(ctx, cq); err != nil {
+		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+	}
 	for _, q := range queues {
 		if err := manager.AddLocalQueue(ctx, q); err != nil {
 			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
 		}
 	}
-	cq := utiltesting.MakeClusterQueue("cq").Obj()
+
+	wantActiveWorkloads := map[string]sets.String{
+		"cq": sets.NewString("/a", "/b"),
+	}
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after creating all objects (-want,+got):\n%s", diff)
+	}
+
+	// Recreating the ClusterQueue.
+	manager.DeleteClusterQueue(cq)
+	wantActiveWorkloads = nil
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after deleting ClusterQueue (-want,+got):\n%s", diff)
+	}
+
 	if err := manager.AddClusterQueue(ctx, cq); err != nil {
-		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+		t.Fatalf("Could not re-add ClusterQueue: %v", err)
 	}
-	workloads := popNamesFromCQ(manager.clusterQueues[cq.Name])
+	workloads := popNamesFromCQ(manager.clusterQueues["cq"])
 	wantWorkloads := []string{"/b", "/a"}
 	if diff := cmp.Diff(wantWorkloads, workloads); diff != "" {
 		t.Errorf("Workloads popped in the wrong order from clusterQueue:\n%s", diff)
 	}
 }
 
-// TestUpdateQueue tests that workloads are transferred between clusterQueues
+// TestUpdateClusterQueue tests that a ClusterQueue transfers cohorts on update.
+func TestUpdateClusterQueue(t *testing.T) {
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("cq1").Cohort("alpha").Obj(),
+		utiltesting.MakeClusterQueue("cq2").Cohort("beta").Obj(),
+	}
+	queues := []*kueue.LocalQueue{
+		utiltesting.MakeLocalQueue("foo", defaultNamespace).ClusterQueue("cq1").Obj(),
+		utiltesting.MakeLocalQueue("bar", defaultNamespace).ClusterQueue("cq2").Obj(),
+	}
+	now := time.Now()
+	workloads := []*kueue.Workload{
+		utiltesting.MakeWorkload("a", defaultNamespace).Queue("foo").Creation(now.Add(time.Second)).Obj(),
+		utiltesting.MakeWorkload("b", defaultNamespace).Queue("bar").Creation(now).Obj(),
+	}
+	// Setup.
+	scheme := utiltesting.MustGetScheme(t)
+	ctx := context.Background()
+	cl := fake.NewClientBuilder().WithScheme(scheme).WithObjects(
+		&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: defaultNamespace}},
+	).Build()
+	manager := NewManager(cl, nil)
+	for _, cq := range clusterQueues {
+		if err := manager.AddClusterQueue(ctx, cq); err != nil {
+			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
+		}
+	}
+	for _, q := range queues {
+		if err := manager.AddLocalQueue(ctx, q); err != nil {
+			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
+		}
+	}
+	// Add inadmissible workloads.
+	for _, w := range workloads {
+		if err := cl.Create(ctx, w); err != nil {
+			t.Fatalf("Failed adding workload to client: %v", err)
+		}
+		manager.RequeueWorkload(ctx, workload.NewInfo(w), RequeueReasonGeneric)
+	}
+
+	// Put cq2 in the same cohort as cq1.
+	clusterQueues[1].Spec.Cohort = clusterQueues[0].Spec.Cohort
+	if err := manager.UpdateClusterQueue(ctx, clusterQueues[1]); err != nil {
+		t.Fatalf("Failed to update ClusterQueue: %v", err)
+	}
+
+	wantCohorts := map[string]sets.String{
+		"alpha": sets.NewString("cq1", "cq2"),
+	}
+	if diff := cmp.Diff(manager.cohorts, wantCohorts); diff != "" {
+		t.Errorf("Unexpected ClusterQueues in cohorts (-want,+got):\n%s", diff)
+	}
+
+	// Verify all workloads are active after the update.
+	activeWorkloads := manager.Dump()
+	wantActiveWorkloads := map[string]sets.String{
```

  it would be nice to have a scheduler test to test that an inadmissible workload becomes admissible when its CQ changes to a cohort with enough quota.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-21T21:39:48Z

- **pkg/queue/manager_test.go:181** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T21:05:33Z
```diff
@@ -87,25 +88,107 @@ func TestAddClusterQueueOrphans(t *testing.T) {
 	).Build()
 	ctx := context.Background()
 	manager := NewManager(kClient, nil)
+	cq := utiltesting.MakeClusterQueue("cq").Obj()
+	if err := manager.AddClusterQueue(ctx, cq); err != nil {
+		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+	}
 	for _, q := range queues {
 		if err := manager.AddLocalQueue(ctx, q); err != nil {
 			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
 		}
 	}
-	cq := utiltesting.MakeClusterQueue("cq").Obj()
+
+	wantActiveWorkloads := map[string]sets.String{
+		"cq": sets.NewString("/a", "/b"),
+	}
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after creating all objects (-want,+got):\n%s", diff)
+	}
+
+	// Recreating the ClusterQueue.
+	manager.DeleteClusterQueue(cq)
+	wantActiveWorkloads = nil
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after deleting ClusterQueue (-want,+got):\n%s", diff)
+	}
+
 	if err := manager.AddClusterQueue(ctx, cq); err != nil {
-		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+		t.Fatalf("Could not re-add ClusterQueue: %v", err)
 	}
-	workloads := popNamesFromCQ(manager.clusterQueues[cq.Name])
+	workloads := popNamesFromCQ(manager.clusterQueues["cq"])
 	wantWorkloads := []string{"/b", "/a"}
 	if diff := cmp.Diff(wantWorkloads, workloads); diff != "" {
 		t.Errorf("Workloads popped in the wrong order from clusterQueue:\n%s", diff)
 	}
 }
 
-// TestUpdateQueue tests that workloads are transferred between clusterQueues
+// TestUpdateClusterQueue tests that a ClusterQueue transfers cohorts on update.
+func TestUpdateClusterQueue(t *testing.T) {
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("cq1").Cohort("alpha").Obj(),
+		utiltesting.MakeClusterQueue("cq2").Cohort("beta").Obj(),
+	}
+	queues := []*kueue.LocalQueue{
+		utiltesting.MakeLocalQueue("foo", defaultNamespace).ClusterQueue("cq1").Obj(),
+		utiltesting.MakeLocalQueue("bar", defaultNamespace).ClusterQueue("cq2").Obj(),
+	}
+	now := time.Now()
+	workloads := []*kueue.Workload{
+		utiltesting.MakeWorkload("a", defaultNamespace).Queue("foo").Creation(now.Add(time.Second)).Obj(),
+		utiltesting.MakeWorkload("b", defaultNamespace).Queue("bar").Creation(now).Obj(),
+	}
+	// Setup.
+	scheme := utiltesting.MustGetScheme(t)
+	ctx := context.Background()
+	cl := fake.NewClientBuilder().WithScheme(scheme).WithObjects(
+		&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: defaultNamespace}},
+	).Build()
+	manager := NewManager(cl, nil)
+	for _, cq := range clusterQueues {
+		if err := manager.AddClusterQueue(ctx, cq); err != nil {
+			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
+		}
+	}
+	for _, q := range queues {
+		if err := manager.AddLocalQueue(ctx, q); err != nil {
+			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
+		}
+	}
+	// Add inadmissible workloads.
+	for _, w := range workloads {
+		if err := cl.Create(ctx, w); err != nil {
+			t.Fatalf("Failed adding workload to client: %v", err)
+		}
+		manager.RequeueWorkload(ctx, workload.NewInfo(w), RequeueReasonGeneric)
+	}
+
+	// Put cq2 in the same cohort as cq1.
+	clusterQueues[1].Spec.Cohort = clusterQueues[0].Spec.Cohort
+	if err := manager.UpdateClusterQueue(ctx, clusterQueues[1]); err != nil {
+		t.Fatalf("Failed to update ClusterQueue: %v", err)
+	}
+
+	wantCohorts := map[string]sets.String{
+		"alpha": sets.NewString("cq1", "cq2"),
+	}
+	if diff := cmp.Diff(manager.cohorts, wantCohorts); diff != "" {
+		t.Errorf("Unexpected ClusterQueues in cohorts (-want,+got):\n%s", diff)
+	}
+
+	// Verify all workloads are active after the update.
+	activeWorkloads := manager.Dump()
+	wantActiveWorkloads := map[string]sets.String{
```

  integration?

- **pkg/queue/cluster_queue_impl_test.go:296** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T21:28:04Z
```diff
@@ -305,10 +289,10 @@ func TestClusterQueueImpl(t *testing.T) {
 			}
 
 			for _, w := range test.inadmissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, RequeueReasonNamespaceMismatch)
+				cq.requeueIfNotPresent(w, false)
 			}
 			for _, w := range test.admissibleWorkloadsToRequeue {
-				cq.RequeueIfNotPresent(w, RequeueReasonGeneric)
+				cq.requeueIfNotPresent(w, true)
```

  I decided to test that as part of the suite for ClusterQueueStrictFIFO

- **pkg/queue/manager_test.go:160** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-21T21:32:38Z
```diff
@@ -87,25 +88,107 @@ func TestAddClusterQueueOrphans(t *testing.T) {
 	).Build()
 	ctx := context.Background()
 	manager := NewManager(kClient, nil)
+	cq := utiltesting.MakeClusterQueue("cq").Obj()
+	if err := manager.AddClusterQueue(ctx, cq); err != nil {
+		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+	}
 	for _, q := range queues {
 		if err := manager.AddLocalQueue(ctx, q); err != nil {
 			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
 		}
 	}
-	cq := utiltesting.MakeClusterQueue("cq").Obj()
+
+	wantActiveWorkloads := map[string]sets.String{
+		"cq": sets.NewString("/a", "/b"),
+	}
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after creating all objects (-want,+got):\n%s", diff)
+	}
+
+	// Recreating the ClusterQueue.
+	manager.DeleteClusterQueue(cq)
+	wantActiveWorkloads = nil
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after deleting ClusterQueue (-want,+got):\n%s", diff)
+	}
+
 	if err := manager.AddClusterQueue(ctx, cq); err != nil {
-		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+		t.Fatalf("Could not re-add ClusterQueue: %v", err)
 	}
-	workloads := popNamesFromCQ(manager.clusterQueues[cq.Name])
+	workloads := popNamesFromCQ(manager.clusterQueues["cq"])
 	wantWorkloads := []string{"/b", "/a"}
 	if diff := cmp.Diff(wantWorkloads, workloads); diff != "" {
 		t.Errorf("Workloads popped in the wrong order from clusterQueue:\n%s", diff)
 	}
 }
 
-// TestUpdateQueue tests that workloads are transferred between clusterQueues
+// TestUpdateClusterQueue tests that a ClusterQueue transfers cohorts on update.
+func TestUpdateClusterQueue(t *testing.T) {
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("cq1").Cohort("alpha").Obj(),
+		utiltesting.MakeClusterQueue("cq2").Cohort("beta").Obj(),
+	}
+	queues := []*kueue.LocalQueue{
+		utiltesting.MakeLocalQueue("foo", defaultNamespace).ClusterQueue("cq1").Obj(),
+		utiltesting.MakeLocalQueue("bar", defaultNamespace).ClusterQueue("cq2").Obj(),
+	}
+	now := time.Now()
+	workloads := []*kueue.Workload{
+		utiltesting.MakeWorkload("a", defaultNamespace).Queue("foo").Creation(now.Add(time.Second)).Obj(),
+		utiltesting.MakeWorkload("b", defaultNamespace).Queue("bar").Creation(now).Obj(),
+	}
+	// Setup.
+	scheme := utiltesting.MustGetScheme(t)
+	ctx := context.Background()
+	cl := fake.NewClientBuilder().WithScheme(scheme).WithObjects(
+		&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: defaultNamespace}},
+	).Build()
+	manager := NewManager(cl, nil)
+	for _, cq := range clusterQueues {
+		if err := manager.AddClusterQueue(ctx, cq); err != nil {
+			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
+		}
+	}
+	for _, q := range queues {
+		if err := manager.AddLocalQueue(ctx, q); err != nil {
+			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
+		}
+	}
+	// Add inadmissible workloads.
+	for _, w := range workloads {
+		if err := cl.Create(ctx, w); err != nil {
```

  No, otherwise the workloads would be active.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-11-21T22:41:12Z

- **pkg/queue/manager_test.go:181** by [@ahg-g](https://github.com/ahg-g) — 2022-11-21T22:41:12Z
```diff
@@ -87,25 +88,107 @@ func TestAddClusterQueueOrphans(t *testing.T) {
 	).Build()
 	ctx := context.Background()
 	manager := NewManager(kClient, nil)
+	cq := utiltesting.MakeClusterQueue("cq").Obj()
+	if err := manager.AddClusterQueue(ctx, cq); err != nil {
+		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+	}
 	for _, q := range queues {
 		if err := manager.AddLocalQueue(ctx, q); err != nil {
 			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
 		}
 	}
-	cq := utiltesting.MakeClusterQueue("cq").Obj()
+
+	wantActiveWorkloads := map[string]sets.String{
+		"cq": sets.NewString("/a", "/b"),
+	}
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after creating all objects (-want,+got):\n%s", diff)
+	}
+
+	// Recreating the ClusterQueue.
+	manager.DeleteClusterQueue(cq)
+	wantActiveWorkloads = nil
+	if diff := cmp.Diff(wantActiveWorkloads, manager.Dump()); diff != "" {
+		t.Errorf("Unexpected active workloads after deleting ClusterQueue (-want,+got):\n%s", diff)
+	}
+
 	if err := manager.AddClusterQueue(ctx, cq); err != nil {
-		t.Fatalf("Failed adding cluster queue %s: %v", cq.Name, err)
+		t.Fatalf("Could not re-add ClusterQueue: %v", err)
 	}
-	workloads := popNamesFromCQ(manager.clusterQueues[cq.Name])
+	workloads := popNamesFromCQ(manager.clusterQueues["cq"])
 	wantWorkloads := []string{"/b", "/a"}
 	if diff := cmp.Diff(wantWorkloads, workloads); diff != "" {
 		t.Errorf("Workloads popped in the wrong order from clusterQueue:\n%s", diff)
 	}
 }
 
-// TestUpdateQueue tests that workloads are transferred between clusterQueues
+// TestUpdateClusterQueue tests that a ClusterQueue transfers cohorts on update.
+func TestUpdateClusterQueue(t *testing.T) {
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("cq1").Cohort("alpha").Obj(),
+		utiltesting.MakeClusterQueue("cq2").Cohort("beta").Obj(),
+	}
+	queues := []*kueue.LocalQueue{
+		utiltesting.MakeLocalQueue("foo", defaultNamespace).ClusterQueue("cq1").Obj(),
+		utiltesting.MakeLocalQueue("bar", defaultNamespace).ClusterQueue("cq2").Obj(),
+	}
+	now := time.Now()
+	workloads := []*kueue.Workload{
+		utiltesting.MakeWorkload("a", defaultNamespace).Queue("foo").Creation(now.Add(time.Second)).Obj(),
+		utiltesting.MakeWorkload("b", defaultNamespace).Queue("bar").Creation(now).Obj(),
+	}
+	// Setup.
+	scheme := utiltesting.MustGetScheme(t)
+	ctx := context.Background()
+	cl := fake.NewClientBuilder().WithScheme(scheme).WithObjects(
+		&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: defaultNamespace}},
+	).Build()
+	manager := NewManager(cl, nil)
+	for _, cq := range clusterQueues {
+		if err := manager.AddClusterQueue(ctx, cq); err != nil {
+			t.Fatalf("Failed adding clusterQueue %s: %v", cq.Name, err)
+		}
+	}
+	for _, q := range queues {
+		if err := manager.AddLocalQueue(ctx, q); err != nil {
+			t.Fatalf("Failed adding queue %s: %v", q.Name, err)
+		}
+	}
+	// Add inadmissible workloads.
+	for _, w := range workloads {
+		if err := cl.Create(ctx, w); err != nil {
+			t.Fatalf("Failed adding workload to client: %v", err)
+		}
+		manager.RequeueWorkload(ctx, workload.NewInfo(w), RequeueReasonGeneric)
+	}
+
+	// Put cq2 in the same cohort as cq1.
+	clusterQueues[1].Spec.Cohort = clusterQueues[0].Spec.Cohort
+	if err := manager.UpdateClusterQueue(ctx, clusterQueues[1]); err != nil {
+		t.Fatalf("Failed to update ClusterQueue: %v", err)
+	}
+
+	wantCohorts := map[string]sets.String{
+		"alpha": sets.NewString("cq1", "cq2"),
+	}
+	if diff := cmp.Diff(manager.cohorts, wantCohorts); diff != "" {
+		t.Errorf("Unexpected ClusterQueues in cohorts (-want,+got):\n%s", diff)
+	}
+
+	// Verify all workloads are active after the update.
+	activeWorkloads := manager.Dump()
+	wantActiveWorkloads := map[string]sets.String{
```

  yes, I guess that is the only option

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-11-22T11:59:40Z

- **pkg/queue/cluster_queue_impl.go:63** by [@kerthcet](https://github.com/kerthcet) — 2022-11-22T09:37:40Z
```diff
@@ -60,8 +60,6 @@ func newClusterQueueImpl(keyFunc func(obj interface{}) string, lessFunc func(a,
 	}
 }
 
-var _ ClusterQueue = &ClusterQueueImpl{}
```

  Then we should rename the `ClusterQueueImpl`, it's unfit now I think.

- **pkg/queue/cluster_queue_impl_test.go:105** by [@kerthcet](https://github.com/kerthcet) — 2022-11-22T10:35:06Z
```diff
@@ -102,20 +102,6 @@ func Test_Delete(t *testing.T) {
 	}
 }
 
-func Test_Dump(t *testing.T) {
```

  I think it's better to keep this, verify the correctness here and use Dump() directly in other tests.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-22T14:44:23Z

- **pkg/queue/cluster_queue_impl_test.go:105** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-22T14:44:23Z
```diff
@@ -102,20 +102,6 @@ func Test_Delete(t *testing.T) {
 	}
 }
 
-func Test_Dump(t *testing.T) {
```

  But this test itself is not very comprehensive. Since we use Dump in a lot of the unit tests here, I think it's just repetitive to have a test exclusive for dump.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-11-22T14:45:03Z

- **pkg/queue/cluster_queue_impl.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2022-11-22T14:45:03Z
```diff
@@ -60,8 +60,6 @@ func newClusterQueueImpl(keyFunc func(obj interface{}) string, lessFunc func(a,
 	}
 }
 
-var _ ClusterQueue = &ClusterQueueImpl{}
```

  I think it's fine, although it should be unexported. I'll do it in a separate PR.
