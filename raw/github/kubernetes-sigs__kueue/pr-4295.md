# PR #4295: TAS: allow to dump the usage of domains for TAS ResourceFlavors

**Summary**: TAS: allow to dump the usage of domains for TAS ResourceFlavors

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4295

**Last updated**: 2025-03-10T14:21:00Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `dump-the-usage-of-domains` → `main`
- **Created**: 2025-02-18T08:59:27Z
- **Updated**: 2025-03-10T14:21:00Z
- **Merged**: 2025-03-10T14:01:48Z
- **Closed**: 2025-03-10T14:01:48Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+151 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Allows to dump the free capacity of TAS snapshots per domain.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #3493

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-18T08:59:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 27b8bce4b5655c807c34bfa0f7ea2326355a69ee |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67cedaf17f24ca0008319edb |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-02-18T08:59:58Z

/cc @mimowo

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-02-20T10:18:38Z

/test all

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-02-25T10:01:38Z

/ok-to-test

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-02-25T13:41:28Z

/test pull-kueue-verify-main

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-02-28T08:12:38Z

@gabesaba @mimowo @PBundyra Does this format look ok?
```
domain1={cpu: 2;memory: 4Gi;nvidia.com/gpu: 1}, domain2={cpu: 1;memory: 2Gi}
```

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-28T10:42:19Z

> @gabesaba @mimowo @PBundyra Does this format look ok?
> 
> ```
> domain1={cpu: 2;memory: 4Gi;nvidia.com/gpu: 1}, domain2={cpu: 1;memory: 2Gi}
> ```

Can we add a newline between domains? So it aligns nicely with me previous comment https://github.com/kubernetes-sigs/kueue/pull/4295#discussion_r1973859208

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-28T10:52:48Z

> Does this format look ok?

As mentioned in https://github.com/kubernetes-sigs/kueue/pull/4295#discussion_r1973652217 I would like the format to also include tasUsage.

Something like: 
```
domain1={
   freeCapacity: cpu: 2; memory: 4Gi; nvidia.com/gpu: 1
   tasUsage:  cpu: 1; memory: 2Gi; nvidia.com/gpu: 1 
},
domain2={
      ....
}
 ```
 
 > Can we add a newline between domains? 
 
 Yes, pretty print like above would be great for human readability.

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-03-04T13:44:37Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T13:33:06Z

/lgtm 
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-10T13:33:14Z

LGTM label has been added.  <details>Git tree hash: 361dde99a33fff071dc2ead816483b9846ebd79e</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-10T13:37:10Z

/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-10T13:37:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4295#issuecomment-2710625189" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4295#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-18T10:34:29Z

- **pkg/cache/tas_flavor_snapshot.go:236** by [@gabesaba](https://github.com/gabesaba) — 2025-02-18T10:30:58Z
```diff
@@ -226,6 +226,22 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+func (s *TASFlavorSnapshot) FreeCapacityPerDomain() map[string]int {
+	freeCapacityPerDomain := make(map[string]int)
+
+	for domainID, leaf := range s.leaves {
+		// Calculate the total free capacity for the domain
+		totalFreeCapacity := 0
+		for _, quantity := range leaf.freeCapacity {
+			totalFreeCapacity += int(quantity)
```

  What does this number mean? IIUC we are flattening heterogenous resources (cpu/gpu/memory) into a single quantity. I don't think we want to lose this granularity.

- **pkg/cache/tas_flavor_snapshot.go:229** by [@gabesaba](https://github.com/gabesaba) — 2025-02-18T10:33:46Z
```diff
@@ -226,6 +226,22 @@ func (s *TASFlavorSnapshot) addTASUsage(domainID utiltas.TopologyDomainID, usage
 	s.leaves[domainID].tasUsage.Add(usage)
 }
 
+func (s *TASFlavorSnapshot) FreeCapacityPerDomain() map[string]int {
```

  can we add a test for this function?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-26T07:25:48Z

- **pkg/cache/snapshot.go:90** by [@gabesaba](https://github.com/gabesaba) — 2025-02-26T07:25:48Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  Please verify that this log line works properly when running integration test, and that is not affected by https://github.com/kubernetes-sigs/kueue/issues/4137. There is no need to write a new integration test, unless this path is not covered. We only want to make sure that this line is output properly

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-02-26T08:39:17Z

- **pkg/cache/snapshot.go:90** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-02-26T08:39:17Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  This is how it looks now: `2025/02/26 10:38:20 INFO TAS Snapshot Free Capacity freeCapacityPerDomain="map[domain1:map[cpu:2000 memory:4294967296 nvidia.com/gpu:1] domain2:map[cpu:1000 memory:2147483648]]"`

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-26T13:55:08Z

- **pkg/cache/snapshot.go:90** by [@gabesaba](https://github.com/gabesaba) — 2025-02-26T13:55:08Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  Can we pretty print these? 4294967296, 2147483648 is hard for humans to read
  
  Try this function https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/pkg/resources/requests.go#L101-L104

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-27T14:08:29Z

- **pkg/cache/snapshot.go:90** by [@mimowo](https://github.com/mimowo) — 2025-02-27T14:08:29Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  I would suggest the key is just "leafDomains". Then for each leaf domain we report (under sub-keys):
  - freeCapacity
  - tasUsage
  
  I think reporting nodeTaints would not hurt, but I', not sure it would also help much, so IMO we can skip it for now. cc @PBundyra

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-27T14:44:51Z

- **pkg/cache/snapshot.go:90** by [@PBundyra](https://github.com/PBundyra) — 2025-02-27T14:44:51Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  WDYT about leaving freeCapacityPerDomain but then in the method `FreeCapacityPerDomain` iterate over all domains, not only leaves. I think it could be useful to have more comprehensive view into usage distribution.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-27T14:58:32Z

- **pkg/cache/snapshot.go:90** by [@mimowo](https://github.com/mimowo) — 2025-02-27T14:58:32Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  but we only compute usage for the leaf domains: https://github.com/kubernetes-sigs/kueue/blame/398c74d105743af8a3ad0d660323e5fdbfaf6e30/pkg/cache/tas_flavor_snapshot.go#L72-L78
  
  so not sure what you would like to report for the higher-up domains. We could aggregate it, but it seems like extra work, I'm not convinced about, becuase that aggregated usage isn't really used by Kueue to make decisions.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-27T15:45:45Z

- **pkg/cache/snapshot.go:90** by [@PBundyra](https://github.com/PBundyra) — 2025-02-27T15:45:44Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  Yeah I meant the aggregated usage but I see it can be misleading because of resource fragmentation. In that case could we sort domains lexicographically so one can easily aggregate them themselves, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-27T16:08:09Z

- **pkg/cache/snapshot.go:90** by [@mimowo](https://github.com/mimowo) — 2025-02-27T16:08:09Z
```diff
@@ -79,6 +79,19 @@ func (s *Snapshot) Log(log logr.Logger) {
 			"usage", cohort.ResourceNode.Usage,
 		)
 	}
+
+	// Dump TAS snapshots if the feature is enabled
+	if features.Enabled(features.TopologyAwareScheduling) {
+		for cqName, cq := range s.ClusterQueues {
+			for tasFlavor, tasSnapshot := range cq.TASFlavors {
+				log.Info("TAS Snapshot Free Capacity",
+					"clusterQueue", cqName,
+					"resourceFlavor", tasFlavor,
+					"freeCapacityPerDomain", tasSnapshot.FreeCapacityPerDomain(),
```

  sgtm

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-28T10:40:31Z

- **pkg/cache/tas_flavor_snapshot_test.go:49** by [@PBundyra](https://github.com/PBundyra) — 2025-02-28T10:40:31Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func TestFreeCapacityPerDomain(t *testing.T) {
+	snapshot := &TASFlavorSnapshot{
+		leaves: leafDomainByID{
+			"domain2": &leafDomain{
+				freeCapacity: resources.Requests{
+					corev1.ResourceCPU:    1000,
+					corev1.ResourceMemory: 2 * 1024 * 1024 * 1024, // 2 GiB
+				},
+			},
+			"domain1": &leafDomain{
+				freeCapacity: resources.Requests{
+					corev1.ResourceCPU:    2000,
+					corev1.ResourceMemory: 4 * 1024 * 1024 * 1024, // 4 GiB
+					"nvidia.com/gpu":      1,
+				},
+			},
+		},
+	}
+
+	expected := "domain1={cpu: 2;memory: 4Gi;nvidia.com/gpu: 1}, domain2={cpu: 1;memory: 2Gi}"
+
+	actual := snapshot.PrettyPrintFreeCapacityPerDomain()
```

  ```suggestion
  	got := snapshot.PrettyPrintFreeCapacityPerDomain()
  ```
  For consistency with other tests

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-28T10:41:03Z

- **pkg/cache/tas_flavor_snapshot_test.go:51** by [@PBundyra](https://github.com/PBundyra) — 2025-02-28T10:41:03Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func TestFreeCapacityPerDomain(t *testing.T) {
+	snapshot := &TASFlavorSnapshot{
+		leaves: leafDomainByID{
+			"domain2": &leafDomain{
+				freeCapacity: resources.Requests{
+					corev1.ResourceCPU:    1000,
+					corev1.ResourceMemory: 2 * 1024 * 1024 * 1024, // 2 GiB
+				},
+			},
+			"domain1": &leafDomain{
+				freeCapacity: resources.Requests{
+					corev1.ResourceCPU:    2000,
+					corev1.ResourceMemory: 4 * 1024 * 1024 * 1024, // 4 GiB
+					"nvidia.com/gpu":      1,
+				},
+			},
+		},
+	}
+
+	expected := "domain1={cpu: 2;memory: 4Gi;nvidia.com/gpu: 1}, domain2={cpu: 1;memory: 2Gi}"
+
+	actual := snapshot.PrettyPrintFreeCapacityPerDomain()
+	if diff := cmp.Diff(expected, actual); diff != "" {
+		t.Errorf("FreeCapacityPerDomain() mismatch (-expected +actual):\n%s", diff)
```

  ```suggestion
  	if diff := cmp.Diff(expected, got); diff != "" {
  		t.Errorf("FreeCapacityPerDomain() mismatch (-expected +got):\n%s", diff)
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-07T19:38:14Z

- **pkg/cache/tas_flavor_snapshot.go:249** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-07T19:20:20Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
```

  ```suggestion
  	freeCapacityPerDomain := make(map[string]resources.Requests, len(s.leaves))
  ```
  
  It would be good to pre-alloc space.

- **pkg/cache/tas_flavor_snapshot.go:259** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-07T19:22:00Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
```

  ```suggestion
  	tasUsagePerDomain := make(map[utiltas.TopologyDomainID]resources.Requests, len(s.leaves))
  ```
  
  Using dedicated typed would be better.

- **pkg/cache/tas_flavor_snapshot.go:262** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-07T19:22:27Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[string(domainID)] = leaf.tasUsage.Clone()
```

  ```suggestion
  		tasUsagePerDomain[domainID] = leaf.tasUsage.Clone()
  ```

- **pkg/cache/tas_flavor_snapshot.go:300** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-07T19:28:02Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[string(domainID)] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+func (s *TASFlavorSnapshot) PrettyPrintFreeCapacityPerDomain() string {
+	freeCapacityPerDomain := s.freeCapacityPerDomain()
+	tasUsagePerDomain := s.tasUsagePerDomain()
+
+	// Pretty-print the free capacities
+	var prettyDomains []string
+
+	// Sort domain keys lexicographically
+	for _, domain := range slices.Sorted(maps.Keys(freeCapacityPerDomain)) {
+		freeCapacity := freeCapacityPerDomain[domain]
+		tasUsage := tasUsagePerDomain[domain]
+
+		// Pretty-print freeCapacity
+		var freeCapacityDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(freeCapacity)) {
+			value := freeCapacity[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			freeCapacityDetails = append(freeCapacityDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Pretty-print tasUsage
+		var tasUsageDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(tasUsage)) {
+			value := tasUsage[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			tasUsageDetails = append(tasUsageDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Format freeCapacity and tasUsage into the output string for this domain
+		prettyDomains = append(prettyDomains, fmt.Sprintf("%s={\nfreeCapacity: %s\ntasUsage: %s\n}",
+			domain,
+			strings.Join(freeCapacityDetails, "; "),
+			strings.Join(tasUsageDetails, "; "),
```

  ```suggestion
  			strings.Join(freeCapacityDetails, ", "),
  			strings.Join(tasUsageDetails, ", "),
  ```
  
  I would recommend using JSON like logging form for better log parsing. @mimowo @PBundyra Do you have any motivation for this special log form instead of JSON?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-03-08T12:53:25Z

- **pkg/cache/tas_flavor_snapshot.go:300** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-03-08T12:53:25Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[string(domainID)] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+func (s *TASFlavorSnapshot) PrettyPrintFreeCapacityPerDomain() string {
+	freeCapacityPerDomain := s.freeCapacityPerDomain()
+	tasUsagePerDomain := s.tasUsagePerDomain()
+
+	// Pretty-print the free capacities
+	var prettyDomains []string
+
+	// Sort domain keys lexicographically
+	for _, domain := range slices.Sorted(maps.Keys(freeCapacityPerDomain)) {
+		freeCapacity := freeCapacityPerDomain[domain]
+		tasUsage := tasUsagePerDomain[domain]
+
+		// Pretty-print freeCapacity
+		var freeCapacityDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(freeCapacity)) {
+			value := freeCapacity[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			freeCapacityDetails = append(freeCapacityDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Pretty-print tasUsage
+		var tasUsageDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(tasUsage)) {
+			value := tasUsage[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			tasUsageDetails = append(tasUsageDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Format freeCapacity and tasUsage into the output string for this domain
+		prettyDomains = append(prettyDomains, fmt.Sprintf("%s={\nfreeCapacity: %s\ntasUsage: %s\n}",
+			domain,
+			strings.Join(freeCapacityDetails, "; "),
+			strings.Join(tasUsageDetails, "; "),
```

  smth like this?
  ```json
  "domain1": {
     "freeCapacity": { "cpu": "2", "memory": "4Gi", "nvidia.com/gpu": "1" },
     "tasUsage":  {"cpu": "1", "memory": "2Gi", "nvidia.com/gpu": "1" }
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T09:53:40Z

- **pkg/cache/tas_flavor_snapshot.go:300** by [@mimowo](https://github.com/mimowo) — 2025-03-10T09:53:40Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[string(domainID)] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+func (s *TASFlavorSnapshot) PrettyPrintFreeCapacityPerDomain() string {
+	freeCapacityPerDomain := s.freeCapacityPerDomain()
+	tasUsagePerDomain := s.tasUsagePerDomain()
+
+	// Pretty-print the free capacities
+	var prettyDomains []string
+
+	// Sort domain keys lexicographically
+	for _, domain := range slices.Sorted(maps.Keys(freeCapacityPerDomain)) {
+		freeCapacity := freeCapacityPerDomain[domain]
+		tasUsage := tasUsagePerDomain[domain]
+
+		// Pretty-print freeCapacity
+		var freeCapacityDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(freeCapacity)) {
+			value := freeCapacity[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			freeCapacityDetails = append(freeCapacityDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Pretty-print tasUsage
+		var tasUsageDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(tasUsage)) {
+			value := tasUsage[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			tasUsageDetails = append(tasUsageDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Format freeCapacity and tasUsage into the output string for this domain
+		prettyDomains = append(prettyDomains, fmt.Sprintf("%s={\nfreeCapacity: %s\ntasUsage: %s\n}",
+			domain,
+			strings.Join(freeCapacityDetails, "; "),
+			strings.Join(tasUsageDetails, "; "),
```

  I prefer to use JSON, yes, the snippet in https://github.com/kubernetes-sigs/kueue/pull/4295#discussion_r1986072254 lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T09:55:55Z

- **pkg/cache/tas_flavor_snapshot.go:297** by [@mimowo](https://github.com/mimowo) — 2025-03-10T09:55:55Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[string(domainID)] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+func (s *TASFlavorSnapshot) PrettyPrintFreeCapacityPerDomain() string {
+	freeCapacityPerDomain := s.freeCapacityPerDomain()
+	tasUsagePerDomain := s.tasUsagePerDomain()
+
+	// Pretty-print the free capacities
+	var prettyDomains []string
+
+	// Sort domain keys lexicographically
+	for _, domain := range slices.Sorted(maps.Keys(freeCapacityPerDomain)) {
+		freeCapacity := freeCapacityPerDomain[domain]
+		tasUsage := tasUsagePerDomain[domain]
+
+		// Pretty-print freeCapacity
+		var freeCapacityDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(freeCapacity)) {
+			value := freeCapacity[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			freeCapacityDetails = append(freeCapacityDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Pretty-print tasUsage
+		var tasUsageDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(tasUsage)) {
+			value := tasUsage[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			tasUsageDetails = append(tasUsageDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Format freeCapacity and tasUsage into the output string for this domain
+		prettyDomains = append(prettyDomains, fmt.Sprintf("%s={\nfreeCapacity: %s\ntasUsage: %s\n}",
```

  Instead of creating the JSON manually I would prefer to create the structure with the fields, and use general JSON marshaling.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T09:56:02Z

- **pkg/cache/tas_flavor_snapshot.go:297** by [@mimowo](https://github.com/mimowo) — 2025-03-10T09:56:02Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[string(domainID)] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+func (s *TASFlavorSnapshot) PrettyPrintFreeCapacityPerDomain() string {
+	freeCapacityPerDomain := s.freeCapacityPerDomain()
+	tasUsagePerDomain := s.tasUsagePerDomain()
+
+	// Pretty-print the free capacities
+	var prettyDomains []string
+
+	// Sort domain keys lexicographically
+	for _, domain := range slices.Sorted(maps.Keys(freeCapacityPerDomain)) {
+		freeCapacity := freeCapacityPerDomain[domain]
+		tasUsage := tasUsagePerDomain[domain]
+
+		// Pretty-print freeCapacity
+		var freeCapacityDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(freeCapacity)) {
+			value := freeCapacity[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			freeCapacityDetails = append(freeCapacityDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Pretty-print tasUsage
+		var tasUsageDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(tasUsage)) {
+			value := tasUsage[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			tasUsageDetails = append(tasUsageDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Format freeCapacity and tasUsage into the output string for this domain
+		prettyDomains = append(prettyDomains, fmt.Sprintf("%s={\nfreeCapacity: %s\ntasUsage: %s\n}",
```

  WDYT @tenzen-y ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T12:33:34Z

- **pkg/cache/tas_flavor_snapshot.go:273** by [@mimowo](https://github.com/mimowo) — 2025-03-10T12:33:23Z
```diff
@@ -244,6 +245,67 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[utiltas.TopologyDomainID]resources.Requests {
+	freeCapacityPerDomain := make(map[utiltas.TopologyDomainID]resources.Requests, len(s.leaves))
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[domainID] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[utiltas.TopologyDomainID]resources.Requests {
+	tasUsagePerDomain := make(map[utiltas.TopologyDomainID]resources.Requests, len(s.leaves))
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[domainID] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+type domainCapacityDetails struct {
+	FreeCapacity map[corev1.ResourceName]string `json:"freeCapacity"`
+	TasUsage     map[corev1.ResourceName]string `json:"tasUsage"`
+}
+
+func (s *TASFlavorSnapshot) SerializeFreeCapacityPerDomain() (string, error) {
```

  I think this is quite useful for logging, as part of the https://github.com/kubernetes-sigs/kueue/issues/4091 (second point).

- **pkg/cache/tas_flavor_snapshot.go:273** by [@mimowo](https://github.com/mimowo) — 2025-03-10T12:33:28Z
```diff
@@ -244,6 +245,67 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[utiltas.TopologyDomainID]resources.Requests {
+	freeCapacityPerDomain := make(map[utiltas.TopologyDomainID]resources.Requests, len(s.leaves))
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[domainID] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[utiltas.TopologyDomainID]resources.Requests {
+	tasUsagePerDomain := make(map[utiltas.TopologyDomainID]resources.Requests, len(s.leaves))
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[domainID] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+type domainCapacityDetails struct {
+	FreeCapacity map[corev1.ResourceName]string `json:"freeCapacity"`
+	TasUsage     map[corev1.ResourceName]string `json:"tasUsage"`
+}
+
+func (s *TASFlavorSnapshot) SerializeFreeCapacityPerDomain() (string, error) {
```

  cc @PBundyra

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-10T13:40:35Z

- **pkg/cache/tas_flavor_snapshot.go:297** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-10T13:40:35Z
```diff
@@ -244,6 +245,66 @@ func (s *TASFlavorSnapshot) removeTASUsage(domainID utiltas.TopologyDomainID, us
 	s.leaves[domainID].tasUsage.Sub(usage)
 }
 
+func (s *TASFlavorSnapshot) freeCapacityPerDomain() map[string]resources.Requests {
+	freeCapacityPerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		freeCapacityPerDomain[string(domainID)] = leaf.freeCapacity.Clone()
+	}
+
+	return freeCapacityPerDomain
+}
+
+func (s *TASFlavorSnapshot) tasUsagePerDomain() map[string]resources.Requests {
+	tasUsagePerDomain := make(map[string]resources.Requests)
+
+	for domainID, leaf := range s.leaves {
+		tasUsagePerDomain[string(domainID)] = leaf.tasUsage.Clone()
+	}
+
+	return tasUsagePerDomain
+}
+
+func (s *TASFlavorSnapshot) PrettyPrintFreeCapacityPerDomain() string {
+	freeCapacityPerDomain := s.freeCapacityPerDomain()
+	tasUsagePerDomain := s.tasUsagePerDomain()
+
+	// Pretty-print the free capacities
+	var prettyDomains []string
+
+	// Sort domain keys lexicographically
+	for _, domain := range slices.Sorted(maps.Keys(freeCapacityPerDomain)) {
+		freeCapacity := freeCapacityPerDomain[domain]
+		tasUsage := tasUsagePerDomain[domain]
+
+		// Pretty-print freeCapacity
+		var freeCapacityDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(freeCapacity)) {
+			value := freeCapacity[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			freeCapacityDetails = append(freeCapacityDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Pretty-print tasUsage
+		var tasUsageDetails []string
+		for _, resourceName := range slices.Sorted(maps.Keys(tasUsage)) {
+			value := tasUsage[resourceName]
+			prettyValue := resources.ResourceQuantityString(resourceName, value)
+			tasUsageDetails = append(tasUsageDetails, fmt.Sprintf("%s: %s", resourceName, prettyValue))
+		}
+
+		// Format freeCapacity and tasUsage into the output string for this domain
+		prettyDomains = append(prettyDomains, fmt.Sprintf("%s={\nfreeCapacity: %s\ntasUsage: %s\n}",
```

  I think so too, thanks.
