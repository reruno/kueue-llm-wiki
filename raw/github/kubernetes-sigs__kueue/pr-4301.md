# PR #4301: WaitForPodsReady.recoveryTimeout implementation

**Summary**: WaitForPodsReady.recoveryTimeout implementation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4301

**Last updated**: 2025-08-12T10:15:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `WFPR-recoveryTimeout-implementation` → `main`
- **Created**: 2025-02-18T16:44:16Z
- **Updated**: 2025-08-12T10:15:48Z
- **Merged**: 2025-02-26T16:34:36Z
- **Closed**: 2025-02-26T16:34:36Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo)
- **Changed files**: 19   **+1024 / -164**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
It introduces a new logic to WaitForPodsReady feature, to support new `.recoveryTimeout` API, according to [this KEP](https://github.com/kubernetes-sigs/kueue/pull/2737)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2732

#### Special notes for your reviewer:
This is one of two PRs for this feature. The [first one](https://github.com/kubernetes-sigs/kueue/pull/4302) introduces API only, and this one adds logic.
Please wait until the PR with API is merged

The integration test in this PR should be enough for the Kueue part, but I'd like to have an additional e2e test in the follow up, which covers full journey with the batchv1.Job controller 

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Kueue exposes a new recovery mechanism as part of the WaitForPodsReady API. This evicts jobs which surpasses configured threshold for pod's recovery during runtime
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-18T16:44:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e006d466eb28a0d2dca3c7b3b4c8bb6b320ba21a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67bf3d7502667a000868e552 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-21T14:21:38Z

@mimowo @tenzen-y 
Can you PTAL? The API PR has been merged, I rebased this one and it's ready for review

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-21T16:51:01Z

> @mimowo @tenzen-y Can you PTAL? The API PR has been merged, I rebased this one and it's ready for review

ACK
/assign

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:54:57Z

> Could we leave test cases to verify compatibility for older `PodsReady` reason?
> 
> > We introduce new WorkloadWaitForPodsStart and WorkloadWaitForPodsRecovery reasons to distinguish the reasons of setting the PodsReady=false condition.
> 
> https://github.com/PBundyra/kueue/blob/daad59ea89bc520b9af7d3ed58c4117bf03ec337/keps/349-all-or-nothing/README.md#timeout-on-reaching-the-podsready-condition

Do you mean something different than this one?

https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-4691f767e133e2011fb5255af68af22a19112c49cad441049aa4a0ef65ba5ebdR634-R660

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T13:51:06Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-24T14:05:53Z

LGTM overall, what I'm missing yet is an integration test for the baseline scenario.

I think it would be valuable to ensure the mechanism works well spanning multiple reconciles. I'm ok to leave it for a follow up, especially if you tested this locally on a running cluster.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T14:10:38Z

> > Could we leave test cases to verify compatibility for older `PodsReady` reason?
> > > We introduce new WorkloadWaitForPodsStart and WorkloadWaitForPodsRecovery reasons to distinguish the reasons of setting the PodsReady=false condition.
> > 
> > 
> > https://github.com/PBundyra/kueue/blob/daad59ea89bc520b9af7d3ed58c4117bf03ec337/keps/349-all-or-nothing/README.md#timeout-on-reaching-the-podsready-condition
> 
> Do you mean something different than this one?
> 
> https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-4691f767e133e2011fb5255af68af22a19112c49cad441049aa4a0ef65ba5ebdR634-R660

I meant could we keep holding cases where input Workload has PodsReady condition with PodsReady reason?
For example, this PR replaces those cases like [this](https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-fbea57b850eb71f650b095afe623b9ed3e9ce3eb5cc35ab965e3498a010a17e0R1114). But, It would be better to keep holding original case as long as the behavior is supported.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-24T14:17:53Z

/hold
for the https://github.com/kubernetes-sigs/kueue/pull/4301#discussion_r1967483299 comment which I consider blocking.
The integration tests are block-ish, but to me could be added in a quick follow up if it was tested manually end-to-end on a local cluster.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T15:01:29Z

> I meant could we keep holding cases where input Workload has PodsReady condition with PodsReady reason? For example, this PR replaces those cases like [this](https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-fbea57b850eb71f650b095afe623b9ed3e9ce3eb5cc35ab965e3498a010a17e0R1114). But, It would be better to keep holding original case as long as the behavior is supported.

Would you like to leave all the old test cases and duplicate all of them with the only difference being the new reason? Or maybe just leave them as they were without duplicating?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T15:15:19Z

@mimowo @tenzen-y 
Here's an integration test that checks the baseline scenario with a failing pod: https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-f1db0eb3182b0e49163aaa7fc1fcc2ee21f01eda279797cc07d01fd16da41ce6R2182-R2239

Would you like to have another one that tests anything particular that is not covered by this one?
I've chosen to put the test into batchv1.Job tests as it's easy to controller the `PodsReady()` method from the framework interface

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T15:42:10Z

> @mimowo @tenzen-y Here's an integration test that checks the baseline scenario with a failing pod: https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-f1db0eb3182b0e49163aaa7fc1fcc2ee21f01eda279797cc07d01fd16da41ce6R2182-R2239
> 
> Would you like to have another one that tests anything particular that is not covered by this one? I've chosen to put the test into batchv1.Job tests as it's easy to controller the `PodsReady()` method from the framework interface

I could not open the link. Could you re-point what you want to specify?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T15:53:16Z

> > @mimowo @tenzen-y Here's an integration test that checks the baseline scenario with a failing pod: https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-f1db0eb3182b0e49163aaa7fc1fcc2ee21f01eda279797cc07d01fd16da41ce6R2182-R2239
> > Would you like to have another one that tests anything particular that is not covered by this one? I've chosen to put the test into batchv1.Job tests as it's easy to controller the `PodsReady()` method from the framework interface
> 
> I could not open the link. Could you re-point what you want to specify?

Sorry, it should point to `test/integration/singlecluster/controller/jobs/job/job_controller_test.go`  L2182

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-25T07:37:06Z

> Sorry, it should point to `test/integration/singlecluster/controller/jobs/job/job_controller_test.go` L2182

Yeah, this looks like the scenario I was looking for, but somehow missed it in the amount of changes for integration tests. I posted comments to de-couple the integration tests, and have a new dedicated config. Then, use the old config to confirm that a failed pod does not trigger eviction (as before).

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T09:42:11Z

I believe I addressed all the comments, to sum up the changes since yesterday:
- Removed the `NotAdmitted` reason;
- Added two more integration tests:

Besides unit tests, currently we have 3 integration tests:
1. Checks that if there's no `.recoveryTimeout` a workload doesn't get evicted if a Pod fails (as it was before)
2. Checks that if there's `.recoveryTimeout` and the timeout is surpassed a workload gets evicted if a Pod fails
3. Checks that if there's `.recoveryTimeout` and a workload recovers before the timeout is surpassed, it doesn't get evicted

@mimowo @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-25T13:01:17Z

LGTM seeing the most recent updates. I will return here when the tests complete.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-25T14:41:01Z

/lgtm
/approve
/hold
Leaving final pass to @tenzen-y since there are some changes compared to the KEP

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-25T14:41:09Z

LGTM label has been added.  <details>Git tree hash: d1ec224b21ad6352f09c9cb4f87932bc6093ac92</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-25T14:46:32Z

> /lgtm /approve /hold Leaving final pass to @tenzen-y since there are some changes compared to the KEP

ACK, I will take a look after patch release

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T15:07:09Z

> Additionally, could you open KEP update PR as a follow-up?

Absolutely, will do

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-26T16:18:46Z

LGTM label has been added.  <details>Git tree hash: 6d78caa611650348c4b2aa6c1bdf1289b320c5e7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-26T16:18:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4301#issuecomment-2682221569" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4301#" title="Author self-approved">PBundyra</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4301#pullrequestreview-2645086928" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T16:33:27Z

> > Additionally, could you open KEP update PR as a follow-up?
> 
> Absolutely, will do

Follow up with KEP update:
https://github.com/kubernetes-sigs/kueue/pull/4419

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T16:33:44Z

/hold cancel
As I got two approvals

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-21T17:06:41Z

ack, initiall pass, I will continue next week

- **pkg/controller/core/workload_controller.go:830** by [@mimowo](https://github.com/mimowo) — 2025-02-21T16:58:11Z
```diff
@@ -826,8 +826,21 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+
+	var elapsedTime time.Duration
```

  I think the flow in this block of code is quite complex since one branch is setting the `elapsedTime` while the other returning. Since the last "return" is only used in the first branch I think it will be more readable to return from the first branch too.

- **pkg/controller/jobframework/reconciler.go:464** by [@mimowo](https://github.com/mimowo) — 2025-02-21T17:02:19Z
```diff
@@ -460,15 +460,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	// handle a job when waitForPodsReady is enabled, and it is the main job
 	if r.waitForPodsReady {
 		log.V(3).Info("Handling a job when waitForPodsReady is enabled")
-		condition := generatePodsReadyCondition(job, wl)
-		// optimization to avoid sending the update request if the status didn't change
-		if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
+		condition := generatePodsReadyCondition(log, job, wl)
+		if !workload.HasCondition(wl, &condition) {
```

  I think the semantics of the HasCondition is quite unclear just by looking at its name. In particular, should we check timestamps, message, reason etc. are the obvious questions. 
  
  I think it would be better to have HasConditionWithTypeAndReason (I believe we should already have something like this). 
  
  I did a quick check and the HasCondition function checks the message, I think we should avoid building any logic on the messages. They are only for humans.

- **pkg/controller/jobframework/reconciler.go:1171** by [@mimowo](https://github.com/mimowo) — 2025-02-21T17:04:12Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
```

  Revisit the log. Surely remove PATRYK, but also probably this is V(3). I also think the ":" aren't necessary. Isn't the log folded as json?

- **pkg/controller/jobframework/reconciler.go:1207** by [@mimowo](https://github.com/mimowo) — 2025-02-21T17:06:08Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionFalse
+		message = "Not all pods are ready or succeeded"
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case job.PodsReady() && podsReadyStartCond != nil:
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !job.PodsReady() && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		reason = kueue.WorkloadWaitForPodsRecovery
+		status = metav1.ConditionFalse
+		message = "One of pods has failed and is waiting for recovery"
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case job.PodsReady() && podsReadyRecoveryCond != nil:
+		reason = kueue.WorkloadWaitForPodsRecovery
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
+	default:
+		log.V(2).Info("Unexpected PodsReady condition",
```

  Here probably v(2) is justified, but clear the ":". Also, maybe add "WARNING" if this is unexpected.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T08:05:43Z

- **pkg/controller/jobframework/reconciler.go:1179** by [@mimowo](https://github.com/mimowo) — 2025-02-24T08:05:43Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
```

  Make sure PodsReady is computed only once in this function. While it is fast for batch/Job it is slower for JobSet (where it depends on the number of PodSets), but in general it depends on the integration code, so could be non-constant.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T08:08:46Z

- **pkg/controller/jobframework/reconciler.go:1198** by [@mimowo](https://github.com/mimowo) — 2025-02-24T08:08:46Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionFalse
+		message = "Not all pods are ready or succeeded"
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case job.PodsReady() && podsReadyStartCond != nil:
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !job.PodsReady() && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		reason = kueue.WorkloadWaitForPodsRecovery
+		status = metav1.ConditionFalse
+		message = "One of pods has failed and is waiting for recovery"
```

  ```suggestion
  		message = "At least one pod has failed, waiting for recovery"
  ```
  proposal, because the previous message suggested it is the pod that is waiting for recovery, whilst the pod will never recover :). What can recover is the workload/job (decide on one).  We could maybe say "At least one pod has failed, workload/job waiting for recovery", but up to you. I'm also open to other proposals.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T08:11:29Z

- **pkg/controller/jobframework/reconciler.go:1190** by [@mimowo](https://github.com/mimowo) — 2025-02-24T08:11:29Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionFalse
+		message = "Not all pods are ready or succeeded"
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case job.PodsReady() && podsReadyStartCond != nil:
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
```

  Maybe it is safer not to say "job", but rather workload? The reason is that Kueue already supports serving workloads such as Deployments, StatefulSet or LWS. For them "Job" might be misleading. I'm aware we use "job" in many places like that for historical reasons, but I want to migrate from it over time. PTAL https://github.com/kubernetes-sigs/kueue/issues/4286

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T08:15:26Z

- **pkg/controller/jobframework/reconciler.go:1166** by [@mimowo](https://github.com/mimowo) — 2025-02-24T08:15:26Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
```

  This is duplicated with the last return. Can we maybe create a function such as `createPodsReadyCondition` and call it from here, but also from the switch cases, instead of the final return. This would also eliminate the need for the "stateful" status, reason and message vars.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T08:17:36Z

- **pkg/controller/jobs/job/job_controller_test.go:497** by [@mimowo](https://github.com/mimowo) — 2025-02-24T08:17:36Z
```diff
@@ -491,6 +494,143 @@ func TestReconciler(t *testing.T) {
 		wantEvents        []utiltesting.EventRecord
 		wantErr           error
 	}{
+		"PodsReady is set to False before Workload is Admitted": {
```

  Do the test cases cover 100% of the code in `generatePodsReadyCondition`? You can generate the coverage data by adding `-coverprofile=` while running unit tests, then report with `go tool cover -html`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T08:19:40Z

Completed the first pass. I will revisit when the remarks are addressed.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T09:32:53Z

- **pkg/controller/jobframework/reconciler.go:464** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T09:32:53Z
```diff
@@ -460,15 +460,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	// handle a job when waitForPodsReady is enabled, and it is the main job
 	if r.waitForPodsReady {
 		log.V(3).Info("Handling a job when waitForPodsReady is enabled")
-		condition := generatePodsReadyCondition(job, wl)
-		// optimization to avoid sending the update request if the status didn't change
-		if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
+		condition := generatePodsReadyCondition(log, job, wl)
+		if !workload.HasCondition(wl, &condition) {
```

  There are two cases in which the message is the only differentiator between conditions:
  1. Before the workload is admitted we set `Workload isn't admitted`
  https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bL1154-R1156
  2. After the workload is admitted but the pods are not ready yet we set `Not all pods are ready or succeeded`
  https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR1181-R1183
  
  Are you suggesting merging those two and setting more generic message?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T09:48:11Z

- **pkg/controller/jobframework/reconciler.go:464** by [@mimowo](https://github.com/mimowo) — 2025-02-24T09:48:11Z
```diff
@@ -460,15 +460,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	// handle a job when waitForPodsReady is enabled, and it is the main job
 	if r.waitForPodsReady {
 		log.V(3).Info("Handling a job when waitForPodsReady is enabled")
-		condition := generatePodsReadyCondition(job, wl)
-		// optimization to avoid sending the update request if the status didn't change
-		if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
+		condition := generatePodsReadyCondition(log, job, wl)
+		if !workload.HasCondition(wl, &condition) {
```

  > There are two cases in which the message is the only differentiator between conditions:
  
  Thanks for explaining, I see the motivation.
  
  > Are you suggesting merging those two and setting more generic message?
  
  Possibly, but not necessarily. I can see at least two options:
  1. make the message generic IIUC this is how it was done before 
  2. discriminate against the two states by the reason field, e.g. `WaitForPodsStart` and `NotAdmitted`
  
  Basically, we should not build any logic based on messages being equal or different, because messages are free to change at any point. We have the analogous policy in the code k8s.
  
  My preference is for (2.), but since it was done like in (1.) I'm ok to leave it for follow up.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T09:54:05Z

- **pkg/controller/jobs/job/job_controller_test.go:497** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T09:54:05Z
```diff
@@ -491,6 +494,143 @@ func TestReconciler(t *testing.T) {
 		wantEvents        []utiltesting.EventRecord
 		wantErr           error
 	}{
+		"PodsReady is set to False before Workload is Admitted": {
```

  It covers 95.8% of the code, the remaining ~4% is because we don't test the `default` code of block when the conditions are invalid

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T10:01:25Z

- **pkg/controller/jobs/job/job_controller_test.go:497** by [@mimowo](https://github.com/mimowo) — 2025-02-24T10:01:25Z
```diff
@@ -491,6 +494,143 @@ func TestReconciler(t *testing.T) {
 		wantEvents        []utiltesting.EventRecord
 		wantErr           error
 	}{
+		"PodsReady is set to False before Workload is Admitted": {
```

  Thanks for checking. 
  
  > we don't test the default code of block when the conditions are invalid
  
  What does it mean the conditions are invalid? Does it mean the "reason" field is unrecognized? Is this the scenario which will happen to every workload on upgrade? If so then I believe it is worth covering. 
  
  Actually, I believe we should have some form of a test demonstrating the behavior on upgrade. We have two options: keep the old reason, but make sure it is handled as WaitForPodsStart, or convert it to "WaitForPodsStart" on the first iteration. I would be leaning to convert it actually.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:12:10Z

- **pkg/controller/jobframework/reconciler.go:1166** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:12:10Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
```

  PTAL if it aligns with your suggestion

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:12:18Z

- **pkg/controller/jobframework/reconciler.go:1171** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:12:18Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:12:33Z

- **pkg/controller/jobframework/reconciler.go:1179** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:12:33Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
```

  Good point, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:12:42Z

- **pkg/controller/jobframework/reconciler.go:1190** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:12:41Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionFalse
+		message = "Not all pods are ready or succeeded"
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case job.PodsReady() && podsReadyStartCond != nil:
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
```

  Thanks, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:13:07Z

- **pkg/controller/jobframework/reconciler.go:1198** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:13:07Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionFalse
+		message = "Not all pods are ready or succeeded"
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case job.PodsReady() && podsReadyStartCond != nil:
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !job.PodsReady() && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		reason = kueue.WorkloadWaitForPodsRecovery
+		status = metav1.ConditionFalse
+		message = "One of pods has failed and is waiting for recovery"
```

  I agree it was misleading, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:13:13Z

- **pkg/controller/jobframework/reconciler.go:1207** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:13:13Z
```diff
@@ -1148,22 +1150,72 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	status := metav1.ConditionFalse
+	message := "Workload isn't admitted"
+	reason := kueue.WorkloadWaitForPodsStart
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return metav1.Condition{
+			Type:    kueue.WorkloadPodsReady,
+			Status:  status,
+			Reason:  reason,
+			Message: message,
+			// ObservedGeneration is added via workload.UpdateStatus
+		}
+	}
+
+	podsReadyStartCond := workload.IsPodsReadyStartCond(wl)
+	podsReadyRecoveryCond := workload.IsPodsReadyRecoveryCond(wl)
+	log.V(2).Info("PATRYK before switch",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason:", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason:", podsReadyRecoveryCond,
+		"Workload admitted:", workload.IsAdmitted(wl),
+		"Pods are ready:", job.PodsReady())
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !job.PodsReady() && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionFalse
+		message = "Not all pods are ready or succeeded"
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case job.PodsReady() && podsReadyStartCond != nil:
+		reason = kueue.WorkloadWaitForPodsStart
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !job.PodsReady() && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		reason = kueue.WorkloadWaitForPodsRecovery
+		status = metav1.ConditionFalse
+		message = "One of pods has failed and is waiting for recovery"
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case job.PodsReady() && podsReadyRecoveryCond != nil:
+		reason = kueue.WorkloadWaitForPodsRecovery
+		status = metav1.ConditionTrue
+		message = "All pods reached readiness and the job is running"
+	default:
+		log.V(2).Info("Unexpected PodsReady condition",
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:13:35Z

- **pkg/controller/core/workload_controller.go:830** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:13:35Z
```diff
@@ -826,8 +826,21 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+
+	var elapsedTime time.Duration
```

  PTAL if this is what you had in mind

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:17:03Z

- **pkg/controller/jobs/job/job_controller_test.go:497** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:17:03Z
```diff
@@ -491,6 +494,143 @@ func TestReconciler(t *testing.T) {
 		wantEvents        []utiltesting.EventRecord
 		wantErr           error
 	}{
+		"PodsReady is set to False before Workload is Admitted": {
```

  We do convert it on the first iteration, I'll cover that in tests

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:23:05Z

- **pkg/controller/jobs/job/job_controller_test.go:497** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:23:04Z
```diff
@@ -491,6 +494,143 @@ func TestReconciler(t *testing.T) {
 		wantEvents        []utiltesting.EventRecord
 		wantErr           error
 	}{
+		"PodsReady is set to False before Workload is Admitted": {
```

  Added the test, covering 100% now

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T10:33:04Z

- **pkg/controller/core/workload_controller.go:830** by [@mimowo](https://github.com/mimowo) — 2025-02-24T10:33:04Z
```diff
@@ -826,8 +826,21 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+
+	var elapsedTime time.Duration
```

  Pretty much, and not you don't need to declare elapsed time outside of the switch, right?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T10:39:05Z

- **pkg/controller/jobframework/reconciler.go:464** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T10:39:05Z
```diff
@@ -460,15 +460,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	// handle a job when waitForPodsReady is enabled, and it is the main job
 	if r.waitForPodsReady {
 		log.V(3).Info("Handling a job when waitForPodsReady is enabled")
-		condition := generatePodsReadyCondition(job, wl)
-		// optimization to avoid sending the update request if the status didn't change
-		if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
+		condition := generatePodsReadyCondition(log, job, wl)
+		if !workload.HasCondition(wl, &condition) {
```

  I also like the second approach better. I've added `NotAdmitted` reason and removed message comparison from the `HasConditionWithTypeAndReason` function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T10:48:04Z

- **pkg/workload/workload.go:774** by [@mimowo](https://github.com/mimowo) — 2025-02-24T10:39:49Z
```diff
@@ -771,6 +771,22 @@ func IsActive(w *kueue.Workload) bool {
 	return ptr.Deref(w.Spec.Active, true)
 }
 
+func IsPodsReadyStartCond(w *kueue.Workload) *metav1.Condition {
```

  A function with a name starting with `Is` should return bool, so something is off here. 
  
  Maybe we can introduce a helper function `FindConditionWithReason`? It could already replace places like this in a follow up: https://github.com/kubernetes-sigs/kueue/blob/a70fd62ad7649877147a150afa92725e366a01f5/pkg/workload/workload.go#L725-L727

- **pkg/workload/workload.go:780** by [@mimowo](https://github.com/mimowo) — 2025-02-24T10:41:14Z
```diff
@@ -771,6 +771,22 @@ func IsActive(w *kueue.Workload) bool {
 	return ptr.Deref(w.Spec.Active, true)
 }
 
+func IsPodsReadyStartCond(w *kueue.Workload) *metav1.Condition {
+	cond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadPodsReady)
+	if cond == nil || cond.Reason != kueue.WorkloadWaitForPodsStart {
+		return nil
+	}
+	return cond
+}
```

  ```suggestion
  	if cond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadPodsReady); 
  	   cond != nil && cond.Reason == kueue.WorkloadWaitForPodsStart {
  		return cond
  	}
  	return nil
  }
  ```
  nit, to avoid negating the reason equality which makes it harder to imo. Same in the function below, but maybe we can have the more generic helper as suggested in the other comment.

- **apis/kueue/v1beta1/workload_types.go:591** by [@mimowo](https://github.com/mimowo) — 2025-02-24T10:47:44Z
```diff
@@ -587,6 +587,9 @@ const (
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
 	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
+
+	// WorkloadNotAdmitted is a reason that indicates the Workload has not been admitted
```

  Is this reason used for workload which get evicted and are waiting to be re-admitted?
  If no, then maybe say "the Workload has never been admitted yet" to make it more explicit, wdyt?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:02:05Z

- **apis/kueue/v1beta1/workload_types.go:591** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:02:05Z
```diff
@@ -587,6 +587,9 @@ const (
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
 	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
+
+	// WorkloadNotAdmitted is a reason that indicates the Workload has not been admitted
```

  It is used for both cases - first admission and readmission

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:08:36Z

- **pkg/workload/workload.go:774** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:08:36Z
```diff
@@ -771,6 +771,22 @@ func IsActive(w *kueue.Workload) bool {
 	return ptr.Deref(w.Spec.Active, true)
 }
 
+func IsPodsReadyStartCond(w *kueue.Workload) *metav1.Condition {
```

  Added the function

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:08:41Z

- **pkg/workload/workload.go:780** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:08:41Z
```diff
@@ -771,6 +771,22 @@ func IsActive(w *kueue.Workload) bool {
 	return ptr.Deref(w.Spec.Active, true)
 }
 
+func IsPodsReadyStartCond(w *kueue.Workload) *metav1.Condition {
+	cond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadPodsReady)
+	if cond == nil || cond.Reason != kueue.WorkloadWaitForPodsStart {
+		return nil
+	}
+	return cond
+}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:10:41Z

- **pkg/controller/core/workload_controller.go:830** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:10:41Z
```diff
@@ -826,8 +826,21 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+
+	var elapsedTime time.Duration
```

  Right, I've cleaned this up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T11:15:24Z

- **apis/kueue/v1beta1/workload_types.go:591** by [@mimowo](https://github.com/mimowo) — 2025-02-24T11:15:24Z
```diff
@@ -587,6 +587,9 @@ const (
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
 	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
+
+	// WorkloadNotAdmitted is a reason that indicates the Workload has not been admitted
```

  So, then maybe say "... the Workload is not admitted". I'm not an English expert, but " has not been admitted" sounds ambiguous to me, and I actually assumed you mean "has never been admitted yet".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T11:20:51Z

- **pkg/controller/jobframework/reconciler.go:1163** by [@mimowo](https://github.com/mimowo) — 2025-02-24T11:20:51Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
```

  Actually, these two lines are actually finding the same condition. I think this is misleading. 
  
  I feel the code could be easier to follow if we just returned `podsReadyCond := workload.FindStatusCondition(wl, kueue.WorkloadPodsReady)`, and then checked the reason in specific places. The first "if" could be to rule out there is no such condititon - nil, and then we just check by the reason. I think it will also be easier to follow. Can you experiment with that?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:29:59Z

- **pkg/controller/jobframework/reconciler.go:1163** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:29:59Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
```

  Do you mean something along the lines:
  
  ```golang
  var podsReadyStartCond, podsReadyRecoveryCond *metav1.Condition
  podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
  if podsReadyCond != nil && podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
  	podsReadyStartCond = podsReadyCond
  } else if podsReadyCond != nil && podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery {
  	podsReadyRecoveryCond = podsReadyCond
  }
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:30:50Z

- **apis/kueue/v1beta1/workload_types.go:591** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:30:50Z
```diff
@@ -587,6 +587,9 @@ const (
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
 	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
+
+	// WorkloadNotAdmitted is a reason that indicates the Workload has not been admitted
```

  Oh yes, done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T11:34:06Z

- **pkg/controller/jobframework/reconciler.go:1163** by [@mimowo](https://github.com/mimowo) — 2025-02-24T11:34:06Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
```

  no, I mean not to have the `podsReadyStartCond, podsReadyRecoveryCond ` variables at all.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T11:39:55Z

- **pkg/controller/jobframework/reconciler.go:1163** by [@mimowo](https://github.com/mimowo) — 2025-02-24T11:39:55Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
```

  In order words to have something like:
  
  ```golang
  podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
  if podsReadyCond == nil {
   // handle case without the condition
  }
  reason := podsReadyCond.Reason
  status := podsReadyCond.Status
  switch {
    case reason == XYZ && status == ABC {
       ...
    }
    ...
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T11:48:39Z

Could we leave test cases to verify compatibility for older `PodsReady` reason?

> We introduce new WorkloadWaitForPodsStart and WorkloadWaitForPodsRecovery reasons to distinguish the reasons of setting the PodsReady=false condition.

https://github.com/PBundyra/kueue/blob/daad59ea89bc520b9af7d3ed58c4117bf03ec337/keps/349-all-or-nothing/README.md#timeout-on-reaching-the-podsready-condition

- **pkg/controller/jobframework/reconciler.go:1160** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T11:26:54Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  I do not think that this is needed since we will do as following, IIUC:
  
  - `it was admitted in the past but it's evicted/requeued`: kueue turns the QuotaReserved condition to False and removes Admitted condition from Workload.
  - `The workload has not been admitted yet`: We tell by the `Admitted` and `QuotaReserved` conditions whether or not the Workload is admitted. What is the value of an additional admitted state rather than `Admitted` and `QuotaReserved`.

- **pkg/controller/jobframework/reconciler.go:1201** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T11:29:04Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason", podsReadyRecoveryCond,
+		"Workload admitted", workload.IsAdmitted(wl),
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case podsReady && podsReadyStartCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !podsReady && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case podsReady && podsReadyRecoveryCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
+			"All pods reached readiness and the workload is running")
+	default:
+		log.V(2).Info("WARNING Unexpected PodsReady condition",
```

  What is the assumed situations where we face here in this flow?
  https://github.com/PBundyra/kueue/blob/daad59ea89bc520b9af7d3ed58c4117bf03ec337/keps/349-all-or-nothing/README.md

- **pkg/controller/core/workload_controller.go:834** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T11:34:56Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
+		admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
+		elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+		return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+	} else {
```

  ```suggestion
  	}
  ```
  
  This `else` is redundant.

- **pkg/controller/core/workload_controller.go:836** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T11:40:30Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
+		admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
+		elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+		return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+	} else {
+		// podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery
+		// A pod has failed and the workload is waiting for recovery
```

  What if `podReadyCond` has `reason: PodsReady` and `status: False`?

- **pkg/controller/core/workload_controller.go:830** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T11:42:35Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
```

  ```suggestion
  	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady" {
  ```
  
  IIUC, the `WaitForPodsStart` is compatible with the old `PodsReady` reason.
  
  > We introduce new WorkloadWaitForPodsStart and WorkloadWaitForPodsRecovery reasons to distinguish the reasons of setting the PodsReady=false condition.

- **pkg/workload/workload.go:810** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T11:47:28Z
```diff
@@ -798,6 +807,28 @@ func IsEvicted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionPresentAndEqual(w.Status.Conditions, kueue.WorkloadEvicted, metav1.ConditionTrue)
 }
 
+// HasConditionWithTypeAndReason checks if there is a condition in Workload's status
+// with exactly the same Type, Status and Reason
+func HasConditionWithTypeAndReason(w *kueue.Workload, cond *metav1.Condition) bool {
+	for _, statusCond := range w.Status.Conditions {
+		if statusCond.Type == cond.Type && statusCond.Reason == cond.Reason &&
+			statusCond.Status == cond.Status {
+			return true
+		}
+	}
+	return false
```

  ```suggestion
  	condition := FindStatusConditionWithReason(w, cond.Type, cond.reason)
  	return condition != nil && condition.Status == cond.Status
  ```
  
  I think this could leverage `FindStatusConditionWithReason`.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T12:35:09Z

- **pkg/controller/jobframework/reconciler.go:1163** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:35:08Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
```

  Updated, ptal if this aligns with what you had in mind

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T12:40:20Z

- **pkg/controller/jobframework/reconciler.go:1160** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:40:20Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  My primary motivation was that after creating a Workload we set the PodsReady condition to False, even tho it hasn't been admitted yet, and is not counting down. I thought it can be misleading so I wanted to differentiate the case when the workload is not admitted yet, and when it's admitted and counting down

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T12:44:27Z

- **pkg/controller/core/workload_controller.go:830** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:44:27Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
```

  We don't necessarily need to cover that case as we convert the old reason into a new one in the `generatePodsReadyCondition` function. However if you think it would be safer to take into account the old reason in both places I'm happy to update that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T12:47:13Z

- **pkg/workload/workload.go:810** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:47:13Z
```diff
@@ -798,6 +807,28 @@ func IsEvicted(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionPresentAndEqual(w.Status.Conditions, kueue.WorkloadEvicted, metav1.ConditionTrue)
 }
 
+// HasConditionWithTypeAndReason checks if there is a condition in Workload's status
+// with exactly the same Type, Status and Reason
+func HasConditionWithTypeAndReason(w *kueue.Workload, cond *metav1.Condition) bool {
+	for _, statusCond := range w.Status.Conditions {
+		if statusCond.Type == cond.Type && statusCond.Reason == cond.Reason &&
+			statusCond.Status == cond.Status {
+			return true
+		}
+	}
+	return false
```

  I've removed the function since it was no longer needed with the refactored logic in `generatePodsReadyCondition` function

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T12:49:12Z

- **pkg/controller/jobframework/reconciler.go:1201** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:49:12Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason", podsReadyRecoveryCond,
+		"Workload admitted", workload.IsAdmitted(wl),
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case podsReady && podsReadyStartCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !podsReady && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case podsReady && podsReadyRecoveryCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
+			"All pods reached readiness and the workload is running")
+	default:
+		log.V(2).Info("WARNING Unexpected PodsReady condition",
```

  Should the link redirect to the whole KEP or did you mean any specific section?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T12:50:35Z

- **pkg/controller/core/workload_controller.go:836** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:50:35Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
+		admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
+		elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+		return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+	} else {
+		// podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery
+		// A pod has failed and the workload is waiting for recovery
```

  With the current flow `podsReadyCond` with `PodsReady` reason will be converted into `WaitForPodsStart` here:
  https://github.com/kubernetes-sigs/kueue/pull/4301/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR1196-R1202

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T12:52:29Z

- **pkg/controller/jobframework/reconciler.go:1163** by [@mimowo](https://github.com/mimowo) — 2025-02-24T12:52:29Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
```

  Yeah,  this looks better. 
  
  However, I still have a hard time to see if the switch cases are complementary or not, as each of them has a couple of entry checks. 
  
  Maybe it could be helpful to at least check podsReadyCond == nil as a preliminary check so that it does not need to be checked in every case down the line?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T12:54:52Z

- **pkg/controller/jobframework/reconciler.go:1170** by [@mimowo](https://github.com/mimowo) — 2025-02-24T12:54:52Z
```diff
@@ -1148,24 +1150,56 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart && podsReadyCond.Status == metav1.ConditionFalse):
```

  Actually, is this even correct, what happens if `podsReady=true`, but podsReadyCond == nil. IIUC the code will enter the default, which will set the condition to False. This does not seem accurate since `podsReady=True`.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T12:59:46Z

- **pkg/controller/jobframework/reconciler.go:1170** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T12:59:45Z
```diff
@@ -1148,24 +1150,56 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart && podsReadyCond.Status == metav1.ConditionFalse):
```

  Fixed, added a test scenario covering that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T13:16:11Z

- **pkg/controller/jobframework/reconciler.go:1163** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T13:16:11Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
```

  I see, ptal

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T13:47:51Z

- **pkg/controller/jobframework/reconciler.go:1160** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T13:47:51Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  In that case could we clear the `PodsReady` condition if the Workload has it since `{Admitted: nil, QuotaReserved: false, PodsReady: false}` is weird.
  
  The `{QuotaReserved: False}` covers all cases for `WorkloadNotAdmitted`. I mean we represent non admitted workloads by `{QuotaRerserved: False}` and `WorkloadNonAdmitted` reason. I think we should represent the state by only one semantics.
  
  Do you have any logic to verify something by this `WorkloadNonAdmitted` reason?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T13:48:57Z

- **pkg/controller/jobframework/reconciler.go:1201** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T13:48:57Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason", podsReadyRecoveryCond,
+		"Workload admitted", workload.IsAdmitted(wl),
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case podsReady && podsReadyStartCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !podsReady && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case podsReady && podsReadyRecoveryCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
+			"All pods reached readiness and the workload is running")
+	default:
+		log.V(2).Info("WARNING Unexpected PodsReady condition",
```

  Sorry for your confusion. This is just a question to clarify when we face this path (`default`).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T13:58:59Z

- **pkg/controller/core/workload_controller.go:836** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T13:58:59Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
+		admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
+		elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
+		return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+	} else {
+		// podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery
+		// A pod has failed and the workload is waiting for recovery
```

  That sounds great, thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T14:06:04Z

- **pkg/controller/core/workload_controller.go:830** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T14:06:04Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
```

  It would be better to confirm both places (JobFramework and WorkloadController (here)) since they usually deploy kueue-external-manager to support non-upstream Jobs. In that case, the kueue-external-manager might be older than kueue-manger, which means kueue-external-manager JobFramework reconciler keeps repling on PodsReady reason.
  
  It might be better to do following steps:
  
  v0.11 (upcoming latest release): Check if podsReady reason is PodsReady in the workload-controller as well, which means verifying PodsReady with PodsReady reason case both in JobFramwork and WrokloadController. And add `[ACTION REQUIRED]` release note to upgrade JobFramwrork version in kueue-external-manager.
  v0.12: Remove the redundant verification from WorkloadController, and then keep it only in JobFramework.
  
  @mimowo Any thoughts?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T14:07:22Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T14:07:22Z
  I think that we want to add integration test cases, at least.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T14:14:39Z

- **pkg/controller/core/workload_controller.go:830** by [@mimowo](https://github.com/mimowo) — 2025-02-24T14:14:39Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
```

  Oh yes, thanks @tenzen-y for bringing this up.
  
  workload_controller and job_controller may be looking at the Workload independently, and thus it might happen that workload_controller looks at the Workload before the job_controller adjusts the reason. This problem is more visible in case of external controllers, but is also present within build-in Kueue controllers, which is also why I'm thinking some integration test would be great.
  
  So, I thnk the code should handle this scenario gracefully and fall into the first if, as proposed in the comment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T14:29:59Z

- **pkg/controller/core/workload_controller.go:830** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T14:29:59Z
```diff
@@ -826,9 +826,20 @@ func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool,
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
 		return false, 0
 	}
-	admittedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted)
-	elapsedTime := r.clock.Since(admittedCond.LastTransitionTime.Time)
-	return true, max(r.waitForPodsReady.timeout-elapsedTime, 0)
+
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart {
```

  > This problem is more visible in case of external controllers, but is also present within build-in Kueue controllers, which is also why I'm thinking some integration test would be great.
  
  SGTM, in that case, let's do 2 things in this PR:
  
  1. Keep previous cases for PodsReady with `PodsReady` reason
  2. Add Integration testings.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T14:49:44Z

- **pkg/controller/jobframework/reconciler.go:1201** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T14:49:44Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason", podsReadyRecoveryCond,
+		"Workload admitted", workload.IsAdmitted(wl),
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case podsReady && podsReadyStartCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !podsReady && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case podsReady && podsReadyRecoveryCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
+			"All pods reached readiness and the workload is running")
+	default:
+		log.V(2).Info("WARNING Unexpected PodsReady condition",
```

  We face this path when handling the old reason

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T14:59:23Z

- **pkg/controller/jobframework/reconciler.go:1160** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T14:59:23Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  > In that case could we clear the `PodsReady` condition if the Workload has it since `{Admitted: nil, QuotaReserved: false, PodsReady: false}` is weird.
  
  Just so you know, this is the current Kueue's behavior. 
  
  Could you elaborate on what you mean by "clearing" the condition? Do you mean to set empty strings in Condition's Status, Reason and Message?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T15:28:38Z

- **pkg/controller/jobframework/reconciler.go:1160** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T15:28:37Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  > Just so you know, this is the current Kueue's behavior.
  
  Yes, that's correct. My PoV, we do not want to have multiple ways to represent Admitted or NotAdmitted.
  We want to have only one `Admitted: True`. After this PR, `Admitted: True` or `PodsReady: False, Reason: NonAdmitted` can represent admission status.
  
  > Could you elaborate on what you mean by "clearing" the condition? Do you mean to set empty strings in Condition's Status, Reason and Message?
  
  Uhm, As revisiting clearing way, it looks like it is not compatible with the current behavior.
  In that case, what about return nil in the case of `!workload.IsAdmitted(wl)`.
  
  In conclusion, basically, I think we should represent Admission state only by Admitted and QuotaReserved.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T15:40:10Z

- **pkg/controller/core/workload_controller_test.go:1154** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T15:39:48Z
```diff
@@ -1081,7 +1151,7 @@ func TestReconcile(t *testing.T) {
 				Condition(metav1.Condition{
 					Type:    kueue.WorkloadPodsReady,
 					Status:  metav1.ConditionFalse,
-					Reason:  "PodsReady",
+					Reason:  kueue.WorkloadWaitForPodsStart,
```

  Could you keep those cases with `PodsReady` reason.
  And, could you add tests with `WaitForPodsStart`, which means we have 2 tests every for existing tests with `PodsReady` with `PodsReady` reason.
  
  For example, we have 
  
  - `"With reason PodsReady: should set the Evicted condition with Deactivated reason, exceeded the maximum number of requeue retries when the .spec.active is False, Admitted, the Workload has Evicted=False and DeactivationTarget=True condition"`
  - `"With reason WaitForPodsStart: should set the Evicted condition with Deactivated reason, exceeded the maximum number of requeue retries when the .spec.active is False, Admitted, the Workload has Evicted=False and DeactivationTarget=True condition"`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T16:07:57Z

- **pkg/controller/core/workload_controller_test.go:1154** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T16:07:57Z
```diff
@@ -1081,7 +1151,7 @@ func TestReconcile(t *testing.T) {
 				Condition(metav1.Condition{
 					Type:    kueue.WorkloadPodsReady,
 					Status:  metav1.ConditionFalse,
-					Reason:  "PodsReady",
+					Reason:  kueue.WorkloadWaitForPodsStart,
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T16:09:31Z

- **pkg/controller/jobframework/reconciler.go:1201** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T16:09:31Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason", podsReadyRecoveryCond,
+		"Workload admitted", workload.IsAdmitted(wl),
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case podsReady && podsReadyStartCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !podsReady && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case podsReady && podsReadyRecoveryCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
+			"All pods reached readiness and the workload is running")
+	default:
+		log.V(2).Info("WARNING Unexpected PodsReady condition",
```

  I updated the logic so now we explicitly handle the old reason gracefully

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T16:14:48Z

- **pkg/controller/jobframework/reconciler.go:1160** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T16:14:48Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  > After this PR, `Admitted: True` or `PodsReady: False, Reason: NonAdmitted` can represent admission status.
  
  I see, that's a good catch.
  
  > In conclusion, basically, I think we should represent Admission state only by Admitted and QuotaReserved.
  
  @mimowo are you fine with getting rid of the `NotAdmitted` reason and sticking with the 1st approach discussed [here](https://github.com/kubernetes-sigs/kueue/pull/4301#discussion_r1967304906)?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T16:19:19Z

- **pkg/controller/jobframework/reconciler.go:1201** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T16:19:19Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
+
+	podsReadyStartCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsStart)
+	podsReadyRecoveryCond := workload.FindStatusConditionWithReason(wl, kueue.WorkloadPodsReady, kueue.WorkloadWaitForPodsRecovery)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"WaitForPodsReady with WorkloadWaitForPodsStart reason", podsReadyStartCond,
+		"WaitForPodsReady with WorkloadWaitForPodsRecovery reason", podsReadyRecoveryCond,
+		"Workload admitted", workload.IsAdmitted(wl),
+		"Pods are ready", podsReady)
+	switch {
+	// 1a. The workload has just been admitted and doesn't have PodsReady cond yet
+	// 1b. The workload has just been admitted, and not all pods are ready
+	case !podsReady && (apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady) == nil ||
+		podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionFalse):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	// 2a. Pods have reached readiness; before job runtime
+	// 2b. Pods continue to be ready
+	case podsReady && podsReadyStartCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// 3a. The workload was running and a pod has failed
+	// 3b. A pod has failed and the Workload has already recovered once before
+	case !podsReady && (podsReadyStartCond != nil && podsReadyStartCond.Status == metav1.ConditionTrue ||
+		podsReadyRecoveryCond != nil):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	// 4a. A failed pod has recovered during workload's runtime
+	// 4b. Pods continue to be ready after recovery
+	case podsReady && podsReadyRecoveryCond != nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
+			"All pods reached readiness and the workload is running")
+	default:
+		log.V(2).Info("WARNING Unexpected PodsReady condition",
```

  Now the `default` path is only for unexpected circumstances. Also, we need to return something if non of the above `cases` are true

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T16:21:21Z

- **pkg/controller/jobframework/reconciler.go:1160** by [@mimowo](https://github.com/mimowo) — 2025-02-24T16:21:21Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  The NotAdmitted reason is not a new way to represent not admitted workload. This remains solely dependent on the Admitted condition type.
  
  The reason is only used within the context of the PodsReady condition to indicate why PodsReady is False.
  
  That being said I'm ok to not introduce the reason as part of this PR because it is a bit orthogonal and it wasn't there prior to the PR, so it is like a new feature on its own. I still think it makes sense as a follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T07:29:12Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2094** by [@mimowo](https://github.com/mimowo) — 2025-02-25T07:29:12Z
```diff
@@ -2068,24 +2069,29 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 
 var _ = ginkgo.Describe("Job controller interacting with Workload controller when waitForPodsReady with requeuing strategy is enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 	var (
-		backoffBaseSeconds int32
-		backoffLimitCount  *int32
-		ns                 *corev1.Namespace
-		fl                 *kueue.ResourceFlavor
-		cq                 *kueue.ClusterQueue
-		lq                 *kueue.LocalQueue
+		backoffBaseSeconds      int32
+		backoffLimitCount       *int32
+		waitForPodsReadyTimeout *metav1.Duration
+		ns                      *corev1.Namespace
+		fl                      *kueue.ResourceFlavor
+		cq                      *kueue.ClusterQueue
+		lq                      *kueue.LocalQueue
+		wl                      kueue.Workload
+		wlKey                   types.NamespacedName
+		job                     *batchv1.Job
 	)
 
 	ginkgo.JustBeforeEach(func() {
 		waitForPodsReady := &configapi.WaitForPodsReady{
 			Enable:         true,
 			BlockAdmission: ptr.To(true),
-			Timeout:        &metav1.Duration{Duration: util.TinyTimeout},
+			Timeout:        waitForPodsReadyTimeout,
 			RequeuingStrategy: &configapi.RequeuingStrategy{
 				Timestamp:          ptr.To(configapi.EvictionTimestamp),
 				BackoffBaseSeconds: ptr.To(backoffBaseSeconds),
 				BackoffLimitCount:  backoffLimitCount,
 			},
+			RecoveryTimeout: &metav1.Duration{Duration: util.TinyTimeout},
```

  I would suggest a separate config (probably Describe?) for that. Yes, this is more code, but:
  - testing without `RecoveryTimeout` makes sense as this remains a valid configuration
  - it is hard for reviewers to decouple which changes in the files are due to logic changes and which due to the introduction of the timeout.
  
  Plus, if you don't modify configuration for the existing ones you can test what happens if the pod fails, but there is no recovertyTimeoutSet, which actually is quite important to check.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T07:31:56Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2094** by [@mimowo](https://github.com/mimowo) — 2025-02-25T07:31:56Z
```diff
@@ -2068,24 +2069,29 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 
 var _ = ginkgo.Describe("Job controller interacting with Workload controller when waitForPodsReady with requeuing strategy is enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 	var (
-		backoffBaseSeconds int32
-		backoffLimitCount  *int32
-		ns                 *corev1.Namespace
-		fl                 *kueue.ResourceFlavor
-		cq                 *kueue.ClusterQueue
-		lq                 *kueue.LocalQueue
+		backoffBaseSeconds      int32
+		backoffLimitCount       *int32
+		waitForPodsReadyTimeout *metav1.Duration
+		ns                      *corev1.Namespace
+		fl                      *kueue.ResourceFlavor
+		cq                      *kueue.ClusterQueue
+		lq                      *kueue.LocalQueue
+		wl                      kueue.Workload
+		wlKey                   types.NamespacedName
+		job                     *batchv1.Job
 	)
 
 	ginkgo.JustBeforeEach(func() {
 		waitForPodsReady := &configapi.WaitForPodsReady{
 			Enable:         true,
 			BlockAdmission: ptr.To(true),
-			Timeout:        &metav1.Duration{Duration: util.TinyTimeout},
+			Timeout:        waitForPodsReadyTimeout,
 			RequeuingStrategy: &configapi.RequeuingStrategy{
 				Timestamp:          ptr.To(configapi.EvictionTimestamp),
 				BackoffBaseSeconds: ptr.To(backoffBaseSeconds),
 				BackoffLimitCount:  backoffLimitCount,
 			},
+			RecoveryTimeout: &metav1.Duration{Duration: util.TinyTimeout},
```

  cc @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T07:34:17Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2133** by [@mimowo](https://github.com/mimowo) — 2025-02-25T07:34:17Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
```

  What is the purpose of this change? It is completely non-obvious to me how this is related to the recovery timeout. Please revert if possible. As posted in the other comment, I think it is better to have a bit more of extra code but have a dedicated configuraiton.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T07:34:46Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2421** by [@mimowo](https://github.com/mimowo) — 2025-02-25T07:34:46Z
```diff
@@ -2177,40 +2271,40 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 1
 			backoffLimitCount = ptr.To[int32](1)
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
```

  Simiarly here, please revert if not needed.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T08:52:00Z

- **pkg/controller/jobframework/reconciler.go:1160** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T08:52:00Z
```diff
@@ -1148,24 +1150,62 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadNotAdmitted,
+			"Workload isn't admitted")
+	}
```

  SGTM, I'll revert the changes related to NotAdmitted, and will create a new issue once this PR is merged

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T08:56:00Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2133** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T08:56:00Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
```

  Those changes were actually needed, as previously for every `When` scenario the waitForPodsReadyTimeout was hardcoded to tiny, as those test check if the Workload is evicted. Given that I wanted to test eviction because of `.recoveryTimeout` and not `.timeout` I had to prolong it, and this is where this change is coming from

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T08:57:42Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2133** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T08:57:42Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
```

  > Plus, if you don't modify configuration for the existing ones you can test what happens if the pod fails, but there is no recovertyTimeoutSet, which actually is quite important to check.
  
  If we want to test this then the changes need to stay

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T09:13:55Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2176** by [@mimowo](https://github.com/mimowo) — 2025-02-25T09:13:55Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
 		})
 
-		ginkgo.It("should evict workload due pods ready timeout", func() {
+		ginkgo.It("should evict workload due waitForPodsReady.timeout", func() {
 			ginkgo.By("creating job")
 			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
 			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
 
-			wl := &kueue.Workload{}
-			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
+			wlKey = types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
 
 			ginkgo.By("setting quota reservation")
 			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
-				g.Expect(util.SetQuotaReservation(ctx, k8sClient, wl, testing.MakeAdmission(cq.Name).Obj())).To(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlKey, &wl)).Should(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(ctx, k8sClient, &wl, testing.MakeAdmission(cq.Name).Obj())).To(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 
 			ginkgo.By("checking the workload is evicted")
 			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlKey, &wl)).Should(gomega.Succeed())
 				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
 					gomega.BeComparableTo(metav1.Condition{
 						Type:    kueue.WorkloadPodsReady,
 						Status:  metav1.ConditionFalse,
-						Reason:  kueue.WorkloadPodsReady,
-						Message: "Not all pods are ready or succeeded",
+						Reason:  kueue.WorkloadNotAdmitted,
+						Message: "Workload isn't admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
```

  Please revert to assert only these which were asserted. I think we use ContainElements for the reason - there used to be flakes as some of the other conditions might be transitioning.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T09:14:36Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2133** by [@mimowo](https://github.com/mimowo) — 2025-02-25T09:14:36Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
```

  Ah, I got it, so you are not changing the configuration. Just setting it to tiny as it was hardcoded before, fine.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T09:16:30Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2129** by [@mimowo](https://github.com/mimowo) — 2025-02-25T09:16:30Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
 		})
 
-		ginkgo.It("should evict workload due pods ready timeout", func() {
+		ginkgo.It("should evict workload due waitForPodsReady.timeout", func() {
 			ginkgo.By("creating job")
 			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
 			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
 
-			wl := &kueue.Workload{}
-			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
```

  Can you revert? Sorry, I know the cleanup looks ok, but the PR is already large and so the drive-by cleanups add distractions IMO.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T09:34:46Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2129** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T09:34:46Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
 		})
 
-		ginkgo.It("should evict workload due pods ready timeout", func() {
+		ginkgo.It("should evict workload due waitForPodsReady.timeout", func() {
 			ginkgo.By("creating job")
 			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
 			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
 
-			wl := &kueue.Workload{}
-			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
```

  Done, sorry for polluting the PR

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T09:37:27Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2094** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T09:37:27Z
```diff
@@ -2068,24 +2069,29 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 
 var _ = ginkgo.Describe("Job controller interacting with Workload controller when waitForPodsReady with requeuing strategy is enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 	var (
-		backoffBaseSeconds int32
-		backoffLimitCount  *int32
-		ns                 *corev1.Namespace
-		fl                 *kueue.ResourceFlavor
-		cq                 *kueue.ClusterQueue
-		lq                 *kueue.LocalQueue
+		backoffBaseSeconds      int32
+		backoffLimitCount       *int32
+		waitForPodsReadyTimeout *metav1.Duration
+		ns                      *corev1.Namespace
+		fl                      *kueue.ResourceFlavor
+		cq                      *kueue.ClusterQueue
+		lq                      *kueue.LocalQueue
+		wl                      kueue.Workload
+		wlKey                   types.NamespacedName
+		job                     *batchv1.Job
 	)
 
 	ginkgo.JustBeforeEach(func() {
 		waitForPodsReady := &configapi.WaitForPodsReady{
 			Enable:         true,
 			BlockAdmission: ptr.To(true),
-			Timeout:        &metav1.Duration{Duration: util.TinyTimeout},
+			Timeout:        waitForPodsReadyTimeout,
 			RequeuingStrategy: &configapi.RequeuingStrategy{
 				Timestamp:          ptr.To(configapi.EvictionTimestamp),
 				BackoffBaseSeconds: ptr.To(backoffBaseSeconds),
 				BackoffLimitCount:  backoffLimitCount,
 			},
+			RecoveryTimeout: &metav1.Duration{Duration: util.TinyTimeout},
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T09:37:43Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2176** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T09:37:43Z
```diff
@@ -2115,34 +2121,122 @@ var _ = ginkgo.Describe("Job controller interacting with Workload controller whe
 		fwk.StopManager(ctx)
 	})
 
-	ginkgo.When("long backoffBaseSeconds", func() {
+	ginkgo.When("long backoffBaseSeconds and tiny waitForPodsReady.timeout", func() {
 		ginkgo.BeforeEach(func() {
 			backoffBaseSeconds = 10
+			waitForPodsReadyTimeout = &metav1.Duration{Duration: util.TinyTimeout}
 		})
 
-		ginkgo.It("should evict workload due pods ready timeout", func() {
+		ginkgo.It("should evict workload due waitForPodsReady.timeout", func() {
 			ginkgo.By("creating job")
 			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
 			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
 
-			wl := &kueue.Workload{}
-			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
+			wlKey = types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
 
 			ginkgo.By("setting quota reservation")
 			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
-				g.Expect(util.SetQuotaReservation(ctx, k8sClient, wl, testing.MakeAdmission(cq.Name).Obj())).To(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlKey, &wl)).Should(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(ctx, k8sClient, &wl, testing.MakeAdmission(cq.Name).Obj())).To(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 
 			ginkgo.By("checking the workload is evicted")
 			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlKey, &wl)).Should(gomega.Succeed())
 				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
 					gomega.BeComparableTo(metav1.Condition{
 						Type:    kueue.WorkloadPodsReady,
 						Status:  metav1.ConditionFalse,
-						Reason:  kueue.WorkloadPodsReady,
-						Message: "Not all pods are ready or succeeded",
+						Reason:  kueue.WorkloadNotAdmitted,
+						Message: "Workload isn't admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T09:45:59Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2091** by [@mimowo](https://github.com/mimowo) — 2025-02-25T09:45:59Z
```diff
@@ -2068,24 +2069,26 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 
 var _ = ginkgo.Describe("Job controller interacting with Workload controller when waitForPodsReady with requeuing strategy is enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 	var (
-		backoffBaseSeconds int32
-		backoffLimitCount  *int32
-		ns                 *corev1.Namespace
-		fl                 *kueue.ResourceFlavor
-		cq                 *kueue.ClusterQueue
-		lq                 *kueue.LocalQueue
+		backoffBaseSeconds      int32
+		backoffLimitCount       *int32
+		waitForPodsReadyTimeout *metav1.Duration
+		ns                      *corev1.Namespace
+		fl                      *kueue.ResourceFlavor
+		cq                      *kueue.ClusterQueue
+		lq                      *kueue.LocalQueue
 	)
 
 	ginkgo.JustBeforeEach(func() {
 		waitForPodsReady := &configapi.WaitForPodsReady{
 			Enable:         true,
 			BlockAdmission: ptr.To(true),
-			Timeout:        &metav1.Duration{Duration: util.TinyTimeout},
+			Timeout:        waitForPodsReadyTimeout,
 			RequeuingStrategy: &configapi.RequeuingStrategy{
 				Timestamp:          ptr.To(configapi.EvictionTimestamp),
 				BackoffBaseSeconds: ptr.To(backoffBaseSeconds),
 				BackoffLimitCount:  backoffLimitCount,
 			},
+			RecoveryTimeout: nil,
```

  Sorry, for back and forth, but seeing how the test is structured, maybe we move the new "When" under this describe, but just make sure only the new when specifies the `RecoveryTimeout`.
  
  This way the configuration for the pre-existing tests will not be affected, and we will avoid duplicating the JustBeforeEach. WDYT @PBundyra ? My main concern initially was just changing config for the pre-existing tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T09:53:37Z

- **pkg/controller/jobframework/reconciler.go:1198** by [@mimowo](https://github.com/mimowo) — 2025-02-25T09:53:37Z
```diff
@@ -1148,24 +1150,61 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	case !podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	case !podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionFalse:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady"):
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// a) The workload was running and a pod has failed
+	// b) A pod has failed and the Workload has already recovered once before
+	case !podsReady && ((podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionTrue ||
+		podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	case podsReady && podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
```

  @PBundyra @tenzen-y I find these reasons `WaitForPodsRecovery` and `WaitForPodsStart` suboptimal / misleading for the positive scenarios (when pods are ready). 
  
  I know this was in the previous PR, but shouldn't we have dedicated "PodsRecovered" and "PodsStarted" reasons? Or just "Started" and "Recovered", something feels off here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T09:58:00Z

- **pkg/controller/jobframework/reconciler.go:1191** by [@mimowo](https://github.com/mimowo) — 2025-02-25T09:58:00Z
```diff
@@ -1148,24 +1150,61 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	case !podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	case !podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionFalse:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady"):
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// a) The workload was running and a pod has failed
+	// b) A pod has failed and the Workload has already recovered once before
+	case !podsReady && ((podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionTrue ||
+		podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery):
```

  TBH, I'm still sceptical about the spagetti entry condtitions, and still wasn't able to verify they are complementary, shouldn't it be just simply:
  
  ```
  case !podsReady && podsReadyCond.Status == metav1.ConditionTrue:
  ```
  meaning we wait for recovery because it was running before and is no longer running?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T09:59:00Z

- **pkg/controller/jobframework/reconciler.go:1178** by [@mimowo](https://github.com/mimowo) — 2025-02-25T09:59:00Z
```diff
@@ -1148,24 +1150,61 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	case !podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	case !podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionFalse:
```

  ```suggestion
  	case !podsReady && podsReadyCond.Status == metav1.ConditionFalse:
  ```
  why not just like this?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T11:57:13Z

- **pkg/controller/jobframework/reconciler.go:1179** by [@mimowo](https://github.com/mimowo) — 2025-02-25T11:57:12Z
```diff
@@ -1148,24 +1150,60 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+
+	if podsReady {
+		var reason string
+		switch {
+		case podsReadyCond == nil:
+			reason = kueue.WorkloadStarted
+
+		case podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery:
+			reason = kueue.WorkloadRecovered
+
+		default:
+			reason = kueue.WorkloadStarted
+		}
```

  ```suggestion
                  reason := kueue.WorkloadStarted
  		if podsReadyCond != nil && podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery {
  			reason = kueue.WorkloadRecovered
                  }
  ```
  Seems equivalent and much shorter

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T12:14:03Z

- **pkg/controller/jobframework/reconciler.go:1178** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T12:14:03Z
```diff
@@ -1148,24 +1150,61 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	case !podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	case !podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionFalse:
```

  Simplified

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T12:14:12Z

- **pkg/controller/jobframework/reconciler.go:1191** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T12:14:12Z
```diff
@@ -1148,24 +1150,61 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	case !podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	case !podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionFalse:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady"):
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// a) The workload was running and a pod has failed
+	// b) A pod has failed and the Workload has already recovered once before
+	case !podsReady && ((podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionTrue ||
+		podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery):
```

  Simplified

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T12:14:22Z

- **pkg/controller/jobframework/reconciler.go:1198** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T12:14:21Z
```diff
@@ -1148,24 +1150,61 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+	switch {
+	case !podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && podsReadyCond == nil:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	case !podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionFalse:
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+
+	case podsReady && (podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady"):
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsStart,
+			"All pods reached readiness and the workload is running")
+
+	// a) The workload was running and a pod has failed
+	// b) A pod has failed and the Workload has already recovered once before
+	case !podsReady && ((podsReadyCond.Reason == kueue.WorkloadWaitForPodsStart || podsReadyCond.Reason == "PodsReady") && podsReadyCond.Status == metav1.ConditionTrue ||
+		podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery):
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsRecovery,
+			"At least one pod has failed, waiting for recovery")
+
+	case podsReady && podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery:
+		return workload.CreatePodsReadyCondition(metav1.ConditionTrue,
+			kueue.WorkloadWaitForPodsRecovery,
```

  I see, added

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T12:38:21Z

- **apis/kueue/v1beta1/workload_types.go:589** by [@mimowo](https://github.com/mimowo) — 2025-02-25T12:38:21Z
```diff
@@ -587,6 +587,12 @@ const (
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
 	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
```

  Maybe for symmetry we have 
  "WaitForRecovery" and "Recovered"
  "WaitForStart" and "Started"
  WDYT @tenzen-y @PBundyra ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T12:39:14Z

- **apis/kueue/v1beta1/workload_types.go:589** by [@mimowo](https://github.com/mimowo) — 2025-02-25T12:39:14Z
```diff
@@ -587,6 +587,12 @@ const (
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
 	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
```

  I think, because "Pods" is already in the condition type name, so no need to repeat it in the reason. 
  
  The symmetry will make it easier to read.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T12:42:40Z

- **pkg/controller/jobframework/reconciler.go:1179** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T12:42:40Z
```diff
@@ -1148,24 +1150,60 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
+		// The workload has not been admitted yet
+		// or it was admitted in the past but it's evicted/requeued
+		return workload.CreatePodsReadyCondition(metav1.ConditionFalse,
+			kueue.WorkloadWaitForPodsStart,
+			"Not all pods are ready or succeeded")
+	}
+
+	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
+	podsReady := job.PodsReady()
+	log.V(3).Info("Generating PodsReady condition",
+		"Current PodsReady condition", podsReadyCond,
+		"Pods are ready", podsReady)
+
+	if podsReady {
+		var reason string
+		switch {
+		case podsReadyCond == nil:
+			reason = kueue.WorkloadStarted
+
+		case podsReadyCond.Reason == kueue.WorkloadWaitForPodsRecovery:
+			reason = kueue.WorkloadRecovered
+
+		default:
+			reason = kueue.WorkloadStarted
+		}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T12:48:44Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2091** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T12:48:44Z
```diff
@@ -2068,24 +2069,26 @@ var _ = ginkgo.Describe("Interacting with scheduler", ginkgo.Ordered, ginkgo.Con
 
 var _ = ginkgo.Describe("Job controller interacting with Workload controller when waitForPodsReady with requeuing strategy is enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
 	var (
-		backoffBaseSeconds int32
-		backoffLimitCount  *int32
-		ns                 *corev1.Namespace
-		fl                 *kueue.ResourceFlavor
-		cq                 *kueue.ClusterQueue
-		lq                 *kueue.LocalQueue
+		backoffBaseSeconds      int32
+		backoffLimitCount       *int32
+		waitForPodsReadyTimeout *metav1.Duration
+		ns                      *corev1.Namespace
+		fl                      *kueue.ResourceFlavor
+		cq                      *kueue.ClusterQueue
+		lq                      *kueue.LocalQueue
 	)
 
 	ginkgo.JustBeforeEach(func() {
 		waitForPodsReady := &configapi.WaitForPodsReady{
 			Enable:         true,
 			BlockAdmission: ptr.To(true),
-			Timeout:        &metav1.Duration{Duration: util.TinyTimeout},
+			Timeout:        waitForPodsReadyTimeout,
 			RequeuingStrategy: &configapi.RequeuingStrategy{
 				Timestamp:          ptr.To(configapi.EvictionTimestamp),
 				BackoffBaseSeconds: ptr.To(backoffBaseSeconds),
 				BackoffLimitCount:  backoffLimitCount,
 			},
+			RecoveryTimeout: nil,
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T12:53:49Z

- **apis/kueue/v1beta1/workload_types.go:589** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T12:53:48Z
```diff
@@ -587,6 +587,12 @@ const (
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
 	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
```

  I like this, added

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T13:28:15Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1032** by [@mimowo](https://github.com/mimowo) — 2025-02-25T13:28:14Z
```diff
@@ -1028,8 +1029,16 @@ var _ = ginkgo.Describe("When waitForPodsReady enabled", ginkgo.Ordered, ginkgo.
 					// also updated due to setting of the job status.
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
 					g.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, nil)).Should(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Conditions).To(gomega.ContainElements(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadAdmitted,
+							Status:  metav1.ConditionFalse,
+							Reason:  "NoReservation",
+							Message: "The workload has no reservation",
+						}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
```

  @PBundyra why is this line removed?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T13:41:21Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1032** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T13:41:21Z
```diff
@@ -1028,8 +1029,16 @@ var _ = ginkgo.Describe("When waitForPodsReady enabled", ginkgo.Ordered, ginkgo.
 					// also updated due to setting of the job status.
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
 					g.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, nil)).Should(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Conditions).To(gomega.ContainElements(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadAdmitted,
+							Status:  metav1.ConditionFalse,
+							Reason:  "NoReservation",
+							Message: "The workload has no reservation",
+						}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
```

  I added the check above which ensures the condition is synced

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T13:54:07Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1032** by [@mimowo](https://github.com/mimowo) — 2025-02-25T13:54:07Z
```diff
@@ -1028,8 +1029,16 @@ var _ = ginkgo.Describe("When waitForPodsReady enabled", ginkgo.Ordered, ginkgo.
 					// also updated due to setting of the job status.
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
 					g.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, nil)).Should(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Conditions).To(gomega.ContainElements(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadAdmitted,
+							Status:  metav1.ConditionFalse,
+							Reason:  "NoReservation",
+							Message: "The workload has no reservation",
+						}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
```

  Is it effectively doing the same? I'm wondering this could be separate cleanup PR, but it is not too bad. up to you.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T13:56:07Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1032** by [@mimowo](https://github.com/mimowo) — 2025-02-25T13:56:07Z
```diff
@@ -1028,8 +1029,16 @@ var _ = ginkgo.Describe("When waitForPodsReady enabled", ginkgo.Ordered, ginkgo.
 					// also updated due to setting of the job status.
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
 					g.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, nil)).Should(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Conditions).To(gomega.ContainElements(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadAdmitted,
+							Status:  metav1.ConditionFalse,
+							Reason:  "NoReservation",
+							Message: "The workload has no reservation",
+						}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
```

  Maybe separate is better, not because the change is bad, but because the role of the change is non-obvious in the PR with so many other changes, so it may distract future readers.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-25T13:58:12Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1032** by [@PBundyra](https://github.com/PBundyra) — 2025-02-25T13:58:12Z
```diff
@@ -1028,8 +1029,16 @@ var _ = ginkgo.Describe("When waitForPodsReady enabled", ginkgo.Ordered, ginkgo.
 					// also updated due to setting of the job status.
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
 					g.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, nil)).Should(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Conditions).To(gomega.ContainElements(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadAdmitted,
+							Status:  metav1.ConditionFalse,
+							Reason:  "NoReservation",
+							Message: "The workload has no reservation",
+						}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
```

  I added this because one of the integration test was flaky because a Workload sometimes was still admitted (after deleting admission) when generating a new PodsReady condition. So this change is directly related to the whole PR

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-26T14:22:25Z

Basically, lgtm
Additionally, could you open KEP update PR as a follow-up?

- **apis/kueue/v1beta1/workload_types.go:595** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-26T14:09:36Z
```diff
@@ -579,14 +579,20 @@ const (
 	// maximum execution time.
 	WorkloadMaximumExecutionTimeExceeded = "MaximumExecutionTimeExceeded"
 
-	// WorkloadWaitForPodsStart indicates the reason for PodsReady=False condition
+	// WorkloadWaitForStart indicates the reason for PodsReady=False condition
 	// when the pods have not been ready since admission, or the workload is not admitted.
-	WorkloadWaitForPodsStart = "WaitForPodsStart"
+	WorkloadWaitForStart = "WaitForStart"
 
-	// WorkloadWaitForPodsStart indicates the reason for the PodsReady=False condition
+	// WorkloadWaitForRecovery indicates the reason for the PodsReady=False condition
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
-	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
+	WorkloadWaitForRecovery = "WaitForRecovery"
+
+	// WorkloadStarted indicates that all Pods are ready and the Workload has successfully started
+	WorkloadStarted = "Started"
+
+	// WorkloadRecovered indicates that after at least one Pod has failed, the Workload has recovered and is running
+	WorkloadRecovered = "Recovered"
```

  Basically, LGTM
  My only concern is many reasons for Workload objects. I'm not sure if cluster admin can distinguish those reasons when they debug behavior.
  
  It might be better to explain in the documentation each reason for which step of the entire admission process.

- **pkg/controller/jobframework/reconciler.go:465** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-26T14:11:43Z
```diff
@@ -460,15 +460,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	// handle a job when waitForPodsReady is enabled, and it is the main job
 	if r.waitForPodsReady {
 		log.V(3).Info("Handling a job when waitForPodsReady is enabled")
-		condition := generatePodsReadyCondition(job, wl)
-		// optimization to avoid sending the update request if the status didn't change
-		if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
+		condition := generatePodsReadyCondition(log, job, wl)
+		if !workload.HasConditionWithTypeAndReason(wl, &condition) {
+			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with reason: %v and status: %v", condition.Reason, condition.Status))
```

  ```suggestion
  			log.V(3).Info("Updating the PodsReady condition", "reason", condition.Reason, "status", condition.Status)
  ```
  
  The logger should embed parameters as structured logging args.

- **pkg/controller/jobframework/reconciler.go:1159** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-26T14:15:12Z
```diff
@@ -1148,24 +1150,52 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
```

  Could you make common messages here?
  
  ```suggestion
  	constants (
  		notReadyMsg := "Not all pods are ready or succeeded"
  		waitingForRecoveryMsg := "At least one pod has failed, waiting for recovery"
  	)
  	if !workload.IsAdmitted(wl) {
  ```

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-26T14:19:29Z
  Could you keep only one test case for old `PodsReady` reason separate from `Started` reason?

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-26T14:21:59Z
  I'm ok with the removal of the old `PodsReady` reason tests from other types of Job integration tests like RayJob.
  However, It would be worth keeping one test case for the old `PodsReady` reason in this Job integration test since Job integration tests are responsible for verifying JobFramework availability.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-26T15:30:29Z

- **apis/kueue/v1beta1/workload_types.go:595** by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T15:30:29Z
```diff
@@ -579,14 +579,20 @@ const (
 	// maximum execution time.
 	WorkloadMaximumExecutionTimeExceeded = "MaximumExecutionTimeExceeded"
 
-	// WorkloadWaitForPodsStart indicates the reason for PodsReady=False condition
+	// WorkloadWaitForStart indicates the reason for PodsReady=False condition
 	// when the pods have not been ready since admission, or the workload is not admitted.
-	WorkloadWaitForPodsStart = "WaitForPodsStart"
+	WorkloadWaitForStart = "WaitForStart"
 
-	// WorkloadWaitForPodsStart indicates the reason for the PodsReady=False condition
+	// WorkloadWaitForRecovery indicates the reason for the PodsReady=False condition
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
-	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
+	WorkloadWaitForRecovery = "WaitForRecovery"
+
+	// WorkloadStarted indicates that all Pods are ready and the Workload has successfully started
+	WorkloadStarted = "Started"
+
+	// WorkloadRecovered indicates that after at least one Pod has failed, the Workload has recovered and is running
+	WorkloadRecovered = "Recovered"
```

  I'm happy to improve the documentation in general so this could be another thing to consider. However, please note that the additional two new reasons ("Started"/"Recovered") were introduce to actually ease the debugging progress

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-26T16:12:31Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T16:12:31Z
  It's a little tricky to test those as we don't set Workload's conditions directly in those tests, and the old reason is immediately switched to new reasons - we have unit tests for that. Now there's also an integration test for scheduler as well that showcase it works correctly

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-26T16:12:38Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T16:12:38Z
  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-26T16:12:45Z

- **pkg/controller/jobframework/reconciler.go:1159** by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T16:12:45Z
```diff
@@ -1148,24 +1150,52 @@ func (r *JobReconciler) ignoreUnretryableError(log logr.Logger, err error) error
 	return err
 }
 
-func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the condition when the pods transition
-	// from Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-		// ObservedGeneration is added via workload.UpdateStatus
+func generatePodsReadyCondition(log logr.Logger, job GenericJob, wl *kueue.Workload) metav1.Condition {
+	if !workload.IsAdmitted(wl) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-26T16:13:28Z

- **pkg/controller/jobframework/reconciler.go:465** by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T16:13:28Z
```diff
@@ -460,15 +460,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	// handle a job when waitForPodsReady is enabled, and it is the main job
 	if r.waitForPodsReady {
 		log.V(3).Info("Handling a job when waitForPodsReady is enabled")
-		condition := generatePodsReadyCondition(job, wl)
-		// optimization to avoid sending the update request if the status didn't change
-		if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
+		condition := generatePodsReadyCondition(log, job, wl)
+		if !workload.HasConditionWithTypeAndReason(wl, &condition) {
+			log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with reason: %v and status: %v", condition.Reason, condition.Status))
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-26T16:14:15Z

- **apis/kueue/v1beta1/workload_types.go:595** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-26T16:14:14Z
```diff
@@ -579,14 +579,20 @@ const (
 	// maximum execution time.
 	WorkloadMaximumExecutionTimeExceeded = "MaximumExecutionTimeExceeded"
 
-	// WorkloadWaitForPodsStart indicates the reason for PodsReady=False condition
+	// WorkloadWaitForStart indicates the reason for PodsReady=False condition
 	// when the pods have not been ready since admission, or the workload is not admitted.
-	WorkloadWaitForPodsStart = "WaitForPodsStart"
+	WorkloadWaitForStart = "WaitForStart"
 
-	// WorkloadWaitForPodsStart indicates the reason for the PodsReady=False condition
+	// WorkloadWaitForRecovery indicates the reason for the PodsReady=False condition
 	// when the Pods were ready since the workload admission, but some pod has failed,
 	// and workload waits for recovering.
-	WorkloadWaitForPodsRecovery = "WaitForPodsRecovery"
+	WorkloadWaitForRecovery = "WaitForRecovery"
+
+	// WorkloadStarted indicates that all Pods are ready and the Workload has successfully started
+	WorkloadStarted = "Started"
+
+	// WorkloadRecovered indicates that after at least one Pod has failed, the Workload has recovered and is running
+	WorkloadRecovered = "Recovered"
```

  Yeah, here I do not have a strong offering. Just nice to have :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-26T16:15:35Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-26T16:15:35Z
  That makes sense. I'm ok without those tests for JobController, thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-26T16:18:41Z

Thank you!
/lgtm
/approve
