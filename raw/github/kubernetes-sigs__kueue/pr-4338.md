# PR #4338: AppWrapper TAS: full support for PodSetTopologyRequest

**Summary**: AppWrapper TAS: full support for PodSetTopologyRequest

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4338

**Last updated**: 2025-02-25T02:25:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@dgrove-oss](https://github.com/dgrove-oss)
- **Branch**: `aw-tas` → `main`
- **Created**: 2025-02-20T22:01:38Z
- **Updated**: 2025-02-25T02:25:50Z
- **Merged**: 2025-02-24T23:20:28Z
- **Closed**: 2025-02-24T23:20:28Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 4   **+135 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind feature
#### What this PR does / why we need it:

We enhanced the AppWrapper controller in v1.0.6 to propagate Kind-specific TAS information via annotations on the AppWrapperPodSetInfo.  This PR updates the Kueue appwrapper integration to process these annotations.  Also minor updates to the integration and test cases to accommodate small changes in the go-level APIs of the AppWrapper utils package that is used to implement the Kueue integration.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-20T22:01:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9e8b09b0a9b3bc77ea7ab0f8e0ec66bce70be514 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67bcf9572d309600087b4699 |

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-20T22:02:25Z

/hold
Should not be merged until I update to use a released version of AppWrapper

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-21T00:35:44Z

/hold cancel
appwrapper 1.0.6 now released

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-02-21T10:23:15Z

That's cool to have, maybe we could use it in test/e2e/tas/appwrapper_test.go like in e.g. jobsets extra step `verify the assignment of pods are as expected with rank-based ordering`.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-02-21T10:23:27Z

other than that lgtm

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-21T10:24:27Z

/assign @gabesaba 
for the first pass

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-21T11:16:57Z

LGTM label has been added.  <details>Git tree hash: 70ef8e21e32873a8c0ab4bb2ce88549a59ff2cea</details>

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-24T15:31:07Z

/hold
We want to merge #4380 first, which just does the AppWrapper 1.0.4 to 1.0.6 bump.  Then I'll rebase and this PR will be only the TAS enhancements.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-24T17:31:48Z

/unhold

#4380 is merged.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-24T23:09:50Z

LGTM label has been added.  <details>Git tree hash: 446c31409581af8ac9f949641bae13f40ac3e5d4</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-24T23:09:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4338#" title="Author self-approved">dgrove-oss</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4338#pullrequestreview-2638658668" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-02-21T11:16:51Z

/lgtm

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:143** by [@gabesaba](https://github.com/gabesaba) — 2025-02-21T11:12:49Z
```diff
@@ -120,20 +122,50 @@ func (aw *AppWrapper) GVK() schema.GroupVersionKind {
 }
 
 func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
-	podSets, err := awutils.GetPodSets((*awv1beta2.AppWrapper)(aw))
+	podSpecTemplates, awPodSets, err := awutils.GetComponentPodSpecs((*awv1beta2.AppWrapper)(aw))
 	if err != nil {
-		ctrl.Log.Error(err, "Error returned from awutils.GetPodSets", "appwrapper", aw)
+		ctrl.Log.Error(err, "Error returned from awutils.GetComponentPodSpecs", "appwrapper", aw)
 		return nil, err
 	}
-	for idx := range podSets {
-		podSets[idx].TopologyRequest = jobframework.PodSetTopologyRequest(&podSets[idx].Template.ObjectMeta, nil, nil, nil)
+	podSets := []kueue.PodSet{}
+	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASPodIndexLabel]; ok {
+			podIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupIndexLabel]; ok {
+			subGroupIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupCount]; ok {
+			if count, err := strconv.Atoi(annotation); err == nil {
```

  Do we want to silently swallow this error?

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:135** by [@gabesaba](https://github.com/gabesaba) — 2025-02-21T11:14:25Z
```diff
@@ -120,20 +122,50 @@ func (aw *AppWrapper) GVK() schema.GroupVersionKind {
 }
 
 func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
-	podSets, err := awutils.GetPodSets((*awv1beta2.AppWrapper)(aw))
+	podSpecTemplates, awPodSets, err := awutils.GetComponentPodSpecs((*awv1beta2.AppWrapper)(aw))
 	if err != nil {
-		ctrl.Log.Error(err, "Error returned from awutils.GetPodSets", "appwrapper", aw)
+		ctrl.Log.Error(err, "Error returned from awutils.GetComponentPodSpecs", "appwrapper", aw)
 		return nil, err
 	}
-	for idx := range podSets {
-		podSets[idx].TopologyRequest = jobframework.PodSetTopologyRequest(&podSets[idx].Template.ObjectMeta, nil, nil, nil)
+	podSets := []kueue.PodSet{}
+	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
```

  all combinations of nil/non-nil are valid here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-21T12:01:13Z

I would suggest, for completeness to add an e2e test for AppWrapper wrapping batch/Job and showing the rank-based ordering, extending https://github.com/kubernetes-sigs/kueue/tree/main/test/e2e/tas.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-02-24T14:40:25Z

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:143** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-24T14:40:25Z
```diff
@@ -120,20 +122,50 @@ func (aw *AppWrapper) GVK() schema.GroupVersionKind {
 }
 
 func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
-	podSets, err := awutils.GetPodSets((*awv1beta2.AppWrapper)(aw))
+	podSpecTemplates, awPodSets, err := awutils.GetComponentPodSpecs((*awv1beta2.AppWrapper)(aw))
 	if err != nil {
-		ctrl.Log.Error(err, "Error returned from awutils.GetPodSets", "appwrapper", aw)
+		ctrl.Log.Error(err, "Error returned from awutils.GetComponentPodSpecs", "appwrapper", aw)
 		return nil, err
 	}
-	for idx := range podSets {
-		podSets[idx].TopologyRequest = jobframework.PodSetTopologyRequest(&podSets[idx].Template.ObjectMeta, nil, nil, nil)
+	podSets := []kueue.PodSet{}
+	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASPodIndexLabel]; ok {
+			podIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupIndexLabel]; ok {
+			subGroupIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupCount]; ok {
+			if count, err := strconv.Atoi(annotation); err == nil {
```

  I can add a warning log.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-02-24T15:01:01Z

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:135** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-24T15:01:01Z
```diff
@@ -120,20 +122,50 @@ func (aw *AppWrapper) GVK() schema.GroupVersionKind {
 }
 
 func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
-	podSets, err := awutils.GetPodSets((*awv1beta2.AppWrapper)(aw))
+	podSpecTemplates, awPodSets, err := awutils.GetComponentPodSpecs((*awv1beta2.AppWrapper)(aw))
 	if err != nil {
-		ctrl.Log.Error(err, "Error returned from awutils.GetPodSets", "appwrapper", aw)
+		ctrl.Log.Error(err, "Error returned from awutils.GetComponentPodSpecs", "appwrapper", aw)
 		return nil, err
 	}
-	for idx := range podSets {
-		podSets[idx].TopologyRequest = jobframework.PodSetTopologyRequest(&podSets[idx].Template.ObjectMeta, nil, nil, nil)
+	podSets := []kueue.PodSet{}
+	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
```

  I'd expect validation of the combinations (if any is needed) to be done inside `jobframework.PodSetTopologyRequest`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-24T17:35:28Z

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:144** by [@mimowo](https://github.com/mimowo) — 2025-02-24T17:35:27Z
```diff
@@ -128,11 +129,29 @@ func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
 	}
 	podSets := make([]kueue.PodSet, len(podSpecTemplates))
 	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASPodIndexLabel]; ok {
+			podIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupIndexLabel]; ok {
+			subGroupIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupCount]; ok {
+			if count, err := strconv.Atoi(annotation); err == nil {
+				count32 := int32(count)
+				subGroupCount = &count32
```

  ```suggestion
  				subGroupCount = ptr.To[int32](count)
  ```
  nit :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T21:16:58Z

Otherwise lgtm

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:133** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T21:11:16Z
```diff
@@ -128,11 +130,28 @@ func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
 	}
 	podSets := make([]kueue.PodSet, len(podSpecTemplates))
 	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
```

  Could you expand UTs? https://github.com/kubernetes-sigs/kueue/blob/6d1ccd92f0ff088303e9cbf18d1d1565e2f40795/pkg/controller/jobs/appwrapper/appwrapper_controller_test.go#L50
  
  I think that PodSets UTs for other jobs are helpful in understanding what we should add.

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:146** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T21:14:40Z
```diff
@@ -128,11 +130,28 @@ func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
 	}
 	podSets := make([]kueue.PodSet, len(podSpecTemplates))
 	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASPodIndexLabel]; ok {
+			podIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupIndexLabel]; ok {
+			subGroupIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupCount]; ok {
+			if count, err := strconv.Atoi(annotation); err == nil {
+				subGroupCount = ptr.To[int32](int32(count))
+			} else {
+				ctrl.Log.Error(err, fmt.Sprintf("Malformed %v annotation ignored", awutils.PodSetAnnotationTASSubGroupCount),
```

  ```suggestion
  				ctrl.Log.Error(err, "Malformed annotation ignored", "annotationKey", awutils.PodSetAnnotationTASSubGroupCount, "annotationValue", count),
  ```
  
  The `ctrl.Log.Error()` has `keyAndValues`. So, it would be better to rely on it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T21:17:38Z

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-24T21:17:38Z
```diff
@@ -128,11 +130,28 @@ func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
 	}
 	podSets := make([]kueue.PodSet, len(podSpecTemplates))
 	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASPodIndexLabel]; ok {
+			podIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupIndexLabel]; ok {
+			subGroupIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupCount]; ok {
+			if count, err := strconv.Atoi(annotation); err == nil {
+				subGroupCount = ptr.To[int32](int32(count))
```

  ```suggestion
  				subGroupCount = ptr.To(int32(count))
  ```
  
  This type parameter does not do anything.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-02-24T21:55:33Z

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:144** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-24T21:55:33Z
```diff
@@ -128,11 +130,28 @@ func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
 	}
 	podSets := make([]kueue.PodSet, len(podSpecTemplates))
 	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASPodIndexLabel]; ok {
+			podIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupIndexLabel]; ok {
+			subGroupIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupCount]; ok {
+			if count, err := strconv.Atoi(annotation); err == nil {
+				subGroupCount = ptr.To[int32](int32(count))
```

  removed

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-02-24T21:55:46Z

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:146** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-24T21:55:46Z
```diff
@@ -128,11 +130,28 @@ func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
 	}
 	podSets := make([]kueue.PodSet, len(podSpecTemplates))
 	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
+		var subGroupIndexLabel *string
+		var subGroupCount *int32
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASPodIndexLabel]; ok {
+			podIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupIndexLabel]; ok {
+			subGroupIndexLabel = &annotation
+		}
+		if annotation, ok := awPodSets[psIndex].Annotations[awutils.PodSetAnnotationTASSubGroupCount]; ok {
+			if count, err := strconv.Atoi(annotation); err == nil {
+				subGroupCount = ptr.To[int32](int32(count))
+			} else {
+				ctrl.Log.Error(err, fmt.Sprintf("Malformed %v annotation ignored", awutils.PodSetAnnotationTASSubGroupCount),
```

  done

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-02-24T22:57:52Z

- **pkg/controller/jobs/appwrapper/appwrapper_controller.go:133** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-02-24T22:57:51Z
```diff
@@ -128,11 +130,28 @@ func (aw *AppWrapper) PodSets() ([]kueue.PodSet, error) {
 	}
 	podSets := make([]kueue.PodSet, len(podSpecTemplates))
 	for psIndex := range podSpecTemplates {
+		var podIndexLabel *string
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-02-24T23:09:44Z

Thank you!
/lgtm
/approve
