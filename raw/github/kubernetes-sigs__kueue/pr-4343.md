# PR #4343: Deploy: Add e2e tests for the manageJobsWithoutQueueName

**Summary**: Deploy: Add e2e tests for the manageJobsWithoutQueueName

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4343

**Last updated**: 2025-06-10T21:08:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `add-deploy-withoutqueue-e2e-test` → `main`
- **Created**: 2025-02-21T11:38:01Z
- **Updated**: 2025-06-10T21:08:07Z
- **Merged**: 2025-02-26T10:06:30Z
- **Closed**: 2025-02-26T10:06:30Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 1   **+67 / -2**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

This PR adds e2e tests for the deployment integration when `manageJobsWithoutQueueName=true`. This is part of #3767  

#### Which issue(s) this PR fixes:
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-21T11:38:30Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f7aa0f01740cf7687733b6ff58e5a7a9a36fa493 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67bcdb8f309d3400089b1809 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-26T07:11:50Z

/assign @PBundyra

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-02-26T09:37:22Z

/lgtm
Thanks @kaisoz!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-26T09:37:29Z

LGTM label has been added.  <details>Git tree hash: a3b684bae2c263c775ababfff5dde98cb60c4912</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-26T09:38:54Z

/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-02-26T09:39:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4343#" title="Author self-approved">kaisoz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4343#issuecomment-2684429718" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:16:21Z

- **test/e2e/customconfigs/skipjobswithoutqueuename_test.go:246** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:16:21Z
```diff
@@ -237,7 +239,11 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 			var testPod, createdPod *corev1.Pod
 			var podLookupKey types.NamespacedName
 			ginkgo.By("creating a pod without a queue name", func() {
-				testPod = testingpod.MakePod("test-pod", ns.Name).Obj()
+				testPod = testingpod.MakePod("test-pod", ns.Name).
+					Image(util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+					Request(corev1.ResourceCPU, "1").
+					Request(corev1.ResourceMemory, "2Gi").
+					Obj()
```

  Is this necessary for the test to work?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:19:03Z

- **test/e2e/customconfigs/skipjobswithoutqueuename_test.go:305** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:19:03Z
```diff
@@ -283,5 +289,68 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should suspend the pods of a deployment created in the test namespace", func() {
+			var testDeploy *appsv1.Deployment
+			ginkgo.By("creating a deployment without a queue name", func() {
+				testDeploy = testingdeploy.MakeDeployment("test-deploy", ns.Name).
+					Image(util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+					Request(corev1.ResourceCPU, "1").
+					Request(corev1.ResourceMemory, "2Gi").
+					Replicas(2).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, testDeploy)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the pods the deployment created are gated", func() {
```

  ```suggestion
  			ginkgo.By("verifying that the pods of the deployment are gated", func() {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:20:31Z

- **test/e2e/customconfigs/skipjobswithoutqueuename_test.go:321** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:20:31Z
```diff
@@ -283,5 +289,68 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should suspend the pods of a deployment created in the test namespace", func() {
+			var testDeploy *appsv1.Deployment
+			ginkgo.By("creating a deployment without a queue name", func() {
+				testDeploy = testingdeploy.MakeDeployment("test-deploy", ns.Name).
+					Image(util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+					Request(corev1.ResourceCPU, "1").
+					Request(corev1.ResourceMemory, "2Gi").
+					Replicas(2).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, testDeploy)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the pods the deployment created are gated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Namespace),
+						client.MatchingLabels(testDeploy.Spec.Selector.MatchLabels))).To(gomega.Succeed())
+					for _, pod := range pods.Items {
+						g.Expect(pod.Spec.SchedulingGates).Should(gomega.BeComparableTo([]corev1.PodSchedulingGate{
+							{Name: "kueue.x-k8s.io/admission"},
+						}))
+						g.Expect(pod.ObjectMeta.Labels).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabel: "true"}))
+						g.Expect(pod.Finalizers).Should(gomega.ContainElement(constants.ManagedByKueueLabel))
+					}
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("should NOT suspend a the pods created by a deployment in the kube-system namespace", func() {
```

  ```suggestion
  		ginkgo.It("should not suspend the pods created by a deployment in the kube-system namespace", func() {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:21:03Z

- **test/e2e/customconfigs/skipjobswithoutqueuename_test.go:323** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:21:02Z
```diff
@@ -283,5 +289,68 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should suspend the pods of a deployment created in the test namespace", func() {
+			var testDeploy *appsv1.Deployment
+			ginkgo.By("creating a deployment without a queue name", func() {
+				testDeploy = testingdeploy.MakeDeployment("test-deploy", ns.Name).
+					Image(util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+					Request(corev1.ResourceCPU, "1").
+					Request(corev1.ResourceMemory, "2Gi").
+					Replicas(2).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, testDeploy)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the pods the deployment created are gated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Namespace),
+						client.MatchingLabels(testDeploy.Spec.Selector.MatchLabels))).To(gomega.Succeed())
+					for _, pod := range pods.Items {
+						g.Expect(pod.Spec.SchedulingGates).Should(gomega.BeComparableTo([]corev1.PodSchedulingGate{
+							{Name: "kueue.x-k8s.io/admission"},
+						}))
+						g.Expect(pod.ObjectMeta.Labels).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabel: "true"}))
+						g.Expect(pod.Finalizers).Should(gomega.ContainElement(constants.ManagedByKueueLabel))
+					}
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("should NOT suspend a the pods created by a deployment in the kube-system namespace", func() {
+			var testDeploy *appsv1.Deployment
+			ginkgo.By("creating a deployment without a queue name", func() {
```

  ```suggestion
  			ginkgo.By("creating a deployment without a queue name in the kube-system namespace", func() {
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-02-24T11:21:29Z

- **test/e2e/customconfigs/skipjobswithoutqueuename_test.go:333** by [@PBundyra](https://github.com/PBundyra) — 2025-02-24T11:21:29Z
```diff
@@ -283,5 +289,68 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 		})
+
+		ginkgo.It("should suspend the pods of a deployment created in the test namespace", func() {
+			var testDeploy *appsv1.Deployment
+			ginkgo.By("creating a deployment without a queue name", func() {
+				testDeploy = testingdeploy.MakeDeployment("test-deploy", ns.Name).
+					Image(util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+					Request(corev1.ResourceCPU, "1").
+					Request(corev1.ResourceMemory, "2Gi").
+					Replicas(2).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, testDeploy)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the pods the deployment created are gated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					pods := &corev1.PodList{}
+					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(ns.Namespace),
+						client.MatchingLabels(testDeploy.Spec.Selector.MatchLabels))).To(gomega.Succeed())
+					for _, pod := range pods.Items {
+						g.Expect(pod.Spec.SchedulingGates).Should(gomega.BeComparableTo([]corev1.PodSchedulingGate{
+							{Name: "kueue.x-k8s.io/admission"},
+						}))
+						g.Expect(pod.ObjectMeta.Labels).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabel: "true"}))
+						g.Expect(pod.Finalizers).Should(gomega.ContainElement(constants.ManagedByKueueLabel))
+					}
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("should NOT suspend a the pods created by a deployment in the kube-system namespace", func() {
+			var testDeploy *appsv1.Deployment
+			ginkgo.By("creating a deployment without a queue name", func() {
+				testDeploy = testingdeploy.MakeDeployment("test-deploy", "kube-system").
+					Image(util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+					Request(corev1.ResourceCPU, "1").
+					Request(corev1.ResourceMemory, "2Gi").
+					Replicas(2).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, testDeploy)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the pods the deployment created are running", func() {
```

  ```suggestion
  			ginkgo.By("verifying that the pods of the deployment are running", func() {
  ```

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-02-24T13:58:21Z

- **test/e2e/customconfigs/skipjobswithoutqueuename_test.go:246** by [@kaisoz](https://github.com/kaisoz) — 2025-02-24T13:58:20Z
```diff
@@ -237,7 +239,11 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 			var testPod, createdPod *corev1.Pod
 			var podLookupKey types.NamespacedName
 			ginkgo.By("creating a pod without a queue name", func() {
-				testPod = testingpod.MakePod("test-pod", ns.Name).Obj()
+				testPod = testingpod.MakePod("test-pod", ns.Name).
+					Image(util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+					Request(corev1.ResourceCPU, "1").
+					Request(corev1.ResourceMemory, "2Gi").
+					Obj()
```

  I added this for consistency with the following tests and because I understood that the decision of gating the pod could be influenced by the resources it requests.  
  
  However, having re-read the code, I see that the evaluation of `manageJobsWithoutQueueName` is taken at webhook time and therefore the pod spec is not considered at all, so I can see that this change is not necessary at all 😊 
  
  P.S: Please let me know if I misunderstood anything
