# PR #4385: add ability to give tls certificates to metrics server

**Summary**: add ability to give tls certificates to metrics server

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4385

**Last updated**: 2025-03-13T19:03:18Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `test-prometheus-cert` → `main`
- **Created**: 2025-02-24T22:13:25Z
- **Updated**: 2025-03-13T19:03:18Z
- **Merged**: 2025-03-13T17:19:49Z
- **Closed**: 2025-03-13T17:19:49Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 13   **+163 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a way to give tls certificates to metrics server
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4377 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Allow one to configure certificates for metrics
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-24T22:13:47Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ae3f8e6410aa0d05953a6b7c23beb1aac2c0b70d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d304d33911340007047700 |

### Comment by [@kannon92](https://github.com/kannon92) — 2025-02-24T22:14:48Z

/hold

Need to update helm chart and get consensus on this.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-26T07:12:48Z

/assign

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-28T10:16:51Z

/retitle WIP: add ability to give tls certificates to metrics server 

It seems like WIP since it is not aligned with KEP.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-02-28T11:34:23Z

> /retitle WIP: add ability to give tls certificates to metrics server
> 
> It seems like WIP since it is not aligned with KEP.

I updated it to match what we suggested in the KEP. It is still WIP as I need to test and verify helm changes.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-01T12:26:23Z

/hold cancel

this ready for review pending acceptance of the KEP.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-01T12:27:51Z

https://github.com/kubernetes-sigs/kueue/pull/4443 was pulled into this PR so maybe review that one first.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-05T04:32:25Z

/hold

Let's get the dependent PRs in first and then this one will be ready again.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-06T15:46:40Z

WAiting for KEP approval and I still need to do some testing to confirm this works.

It is ready for reviews if someone wants to take a look.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-11T20:16:50Z

> I would also like to update the API comment for the "internalCertmanagement.enable" field, as proposed in the KEP: https://github.com/kubernetes-sigs/kueue/pull/4404/files#r1989424928

Updated.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-11T21:02:45Z

/hold cancel

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-11T21:03:01Z

/assign @mimowo @tenzen-y 

This is ready for review again.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-12T06:22:12Z

LGTM label has been added.  <details>Git tree hash: ef830703894fbd08f33086ccf17ecf359a97d301</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-12T06:22:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4385#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4385#pullrequestreview-2677104788" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-13T16:22:23Z

/lgtm
Thanks

> /hold
> in case @tenzen-y has comments

I believe the last outstanding [comment](https://github.com/kubernetes-sigs/kueue/pull/4385#discussion_r1990778704) from Yuki is addressed by commenting out the patch. 

/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-13T16:22:30Z

LGTM label has been added.  <details>Git tree hash: 20c7fbf76d49ebaafb2ddcc11fe29b204638d263</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-13T16:40:57Z

/test pull-kueue-test-e2e-main-1-30
Looks like a known flake

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T07:00:12Z

- **apis/config/v1beta1/configuration_types.go:177** by [@mimowo](https://github.com/mimowo) — 2025-02-25T07:00:12Z
```diff
@@ -169,6 +169,12 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CertDir is the directory that contains the server key and certificate.
+	// The server key and certificate
+	// must be named tls.key and tls.crt, respectively.
+	// +optional
+	CertDir string `json:"certDir,omitempty"`
```

  Do we need this API at all? @tenzen-y was proposing we could use the default CertDir location whenever the internal cert manager is disabled. I like this approach as light-weight on the API: https://github.com/kubernetes-sigs/kueue/issues/3259#issuecomment-2578652710.
  
  I'm ok with the CertDir as a configuration, but it seems counter-intuitive that it enables the feature.
  wdyt @tenzen-y ?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-02-25T12:36:54Z

- **apis/config/v1beta1/configuration_types.go:177** by [@kannon92](https://github.com/kannon92) — 2025-02-25T12:36:54Z
```diff
@@ -169,6 +169,12 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CertDir is the directory that contains the server key and certificate.
+	// The server key and certificate
+	// must be named tls.key and tls.crt, respectively.
+	// +optional
+	CertDir string `json:"certDir,omitempty"`
```

  This is the recommended approach from kubebilder. We use CertDir for webhooks but I think reusing the same certificates may not be the best thing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-02-25T12:43:19Z

- **apis/config/v1beta1/configuration_types.go:177** by [@mimowo](https://github.com/mimowo) — 2025-02-25T12:43:19Z
```diff
@@ -169,6 +169,12 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CertDir is the directory that contains the server key and certificate.
+	// The server key and certificate
+	// must be named tls.key and tls.crt, respectively.
+	// +optional
+	CertDir string `json:"certDir,omitempty"`
```

  > This is the recommended approach from kubebilder. 
  
  What do you mean here specifically, any reference? 
  
  > We use CertDir for webhooks but I think reusing the same certificates may not be the best thing.
  
  Yeah, I'm not proposing it. I just think using "CertDir" as enable seems weird for the Kueue API. For example ControllerWebhook.CertDir does not play the role enabling the feature.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-02-25T12:52:08Z

- **apis/config/v1beta1/configuration_types.go:177** by [@kannon92](https://github.com/kannon92) — 2025-02-25T12:52:08Z
```diff
@@ -169,6 +169,12 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CertDir is the directory that contains the server key and certificate.
+	// The server key and certificate
+	// must be named tls.key and tls.crt, respectively.
+	// +optional
+	CertDir string `json:"certDir,omitempty"`
```

  https://github.com/kubernetes-sigs/kubebuilder/blob/d72fa069e3190adcf53a64a4e4daddc89cb3653e/testdata/project-v4/cmd/main.go#L87

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-02-25T12:53:45Z

- **apis/config/v1beta1/configuration_types.go:177** by [@kannon92](https://github.com/kannon92) — 2025-02-25T12:53:45Z
```diff
@@ -169,6 +169,12 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CertDir is the directory that contains the server key and certificate.
+	// The server key and certificate
+	// must be named tls.key and tls.crt, respectively.
+	// +optional
+	CertDir string `json:"certDir,omitempty"`
```

  I see what you mean.
  
  I could add an enum and hardcode cert path in case of cert manager. And for self serving I could use the other path.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-02-28T11:35:09Z

- **apis/config/v1beta1/configuration_types.go:176** by [@kannon92](https://github.com/kannon92) — 2025-02-28T11:35:09Z
```diff
@@ -169,6 +169,12 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// UseTLS, if true, will provide tls validation for the prometheus endpoint
+	// False means that we will allow access to metrics to whoever has access to the ServiceAccount
+	// Default will be false
+	// +optional
```

  Have to remove this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-01T00:10:03Z

- **apis/config/v1beta1/configuration_types.go:177** by [@kannon92](https://github.com/kannon92) — 2025-03-01T00:10:03Z
```diff
@@ -169,6 +169,12 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CertDir is the directory that contains the server key and certificate.
+	// The server key and certificate
+	// must be named tls.key and tls.crt, respectively.
+	// +optional
+	CertDir string `json:"certDir,omitempty"`
```

  Resolving as our API changed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:34:03Z

- **charts/kueue/prometheus_values.yaml:1** by [@mimowo](https://github.com/mimowo) — 2025-03-11T14:34:03Z
```diff
@@ -0,0 +1,172 @@
+# Default values for kueue.
```

  Do we need this file? Could we parametrize the default Kueue values instead? Asking cause it looks like a signifficant duplication to maintain.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:35:39Z

I would also like to update the API comment for the "internalCertmanagement.enable" field, as proposed in the KEP: https://github.com/kubernetes-sigs/kueue/pull/4404/files#r1989424928

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-11T14:42:24Z

- **charts/kueue/prometheus_values.yaml:1** by [@kannon92](https://github.com/kannon92) — 2025-03-11T14:42:24Z
```diff
@@ -0,0 +1,172 @@
+# Default values for kueue.
```

  Yea I'm not sure the best approach here.
  
  We can provide custom TLS Configs and this provides an example. Or we maintain the TLS Configs for ServiceMonitors?
  
  As of now the default Kueue values don't have anything close to this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T18:49:15Z

- **charts/kueue/prometheus_values.yaml:1** by [@mimowo](https://github.com/mimowo) — 2025-03-11T18:49:15Z
```diff
@@ -0,0 +1,172 @@
+# Default values for kueue.
```

  I don't think we want to afford the duplcation. 
  
  Could we just add commented-out section for the ServiceMonitor. And add a comment uncomment when using external certs? We do similarly for webhooks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T18:50:35Z

- **charts/kueue/prometheus_values.yaml:1** by [@mimowo](https://github.com/mimowo) — 2025-03-11T18:50:34Z
```diff
@@ -0,0 +1,172 @@
+# Default values for kueue.
```

  Also, the name of the file is misleading `charts/kueue/prometheus_values.yaml`. It suggests enabling prometheus, but in fact it enables external certs for webhooks and metrics.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-11T18:51:22Z

- **charts/kueue/prometheus_values.yaml:1** by [@kannon92](https://github.com/kannon92) — 2025-03-11T18:51:21Z
```diff
@@ -0,0 +1,172 @@
+# Default values for kueue.
```

  Thats a pattern we follow for kustomize but for helm we aim to programmatic handle that.
  
  I was thinking `prometheus_values` could be used as an example in documentation if one wanted this?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T18:56:22Z

- **charts/kueue/prometheus_values.yaml:1** by [@mimowo](https://github.com/mimowo) — 2025-03-11T18:56:22Z
```diff
@@ -0,0 +1,172 @@
+# Default values for kueue.
```

  > Thats a pattern we follow for kustomize but for helm we aim to programmatic handle that.
  
  Hm, I see for helm we also use the pattern with comments, for example [here](https://github.com/kubernetes-sigs/kueue/blob/main/charts/kueue/values.yaml#L117-L133).
  
  > I was thinking prometheus_values could be used as an example in documentation if one wanted this?
  
  I would prefer to avoid the copy-paste duplication. I'm open to other suggestions how to do it than comments, but copy-pasting values.yaml is not maintainable imo.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T19:18:11Z

- **charts/kueue/values.yaml:157** by [@mimowo](https://github.com/mimowo) — 2025-03-11T19:18:11Z
```diff
@@ -153,6 +153,3 @@ webhookService:
 
 # kueue-viz dashboard
 enableKueueViz: false
-
-metrics:
```

  I would suggest to keep the default values as they are, since the change could be breaking to some workflows. I would suggest to focus this PR only on the mechanics, and the configs adjust in a follow up. Then I would be open to include the PR in RC1.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T19:18:41Z

- **charts/kueue/templates/prometheus/monitor.yaml:15** by [@mimowo](https://github.com/mimowo) — 2025-03-11T19:18:41Z
```diff
@@ -12,8 +12,7 @@ spec:
     path: /metrics
     port: https
     scheme: https
-    tlsConfig:
```

  Similarly, I would prefer not to change the default behavior in the PR.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-11T19:40:26Z

- **charts/kueue/templates/prometheus/monitor.yaml:15** by [@kannon92](https://github.com/kannon92) — 2025-03-11T19:40:26Z
```diff
@@ -12,8 +12,7 @@ spec:
     path: /metrics
     port: https
     scheme: https
-    tlsConfig:
```

  This is difficult to decouple as I need to inject custom tlsConfig for this to actually work.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-11T20:16:39Z

- **charts/kueue/prometheus_values.yaml:1** by [@kannon92](https://github.com/kannon92) — 2025-03-11T20:16:39Z
```diff
@@ -0,0 +1,172 @@
+# Default values for kueue.
```

  I removed this file.
  
  We followed off offline and decided to go with UX as a followup. In this case we would maybe consider a user guide or a documentation update mentioning how one could enable the use case I want.
  
  For now I will get this PR functionally without changing the defaults.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-11T20:17:25Z

- **charts/kueue/templates/prometheus/monitor.yaml:15** by [@kannon92](https://github.com/kannon92) — 2025-03-11T20:17:25Z
```diff
@@ -12,8 +12,7 @@ spec:
     path: /metrics
     port: https
     scheme: https
-    tlsConfig:
```

  We followed off offline on this and we decided to set the defaults the same but we are introducing a config to change tlsConfigs in special cases.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T06:20:44Z

- **charts/kueue/values.yaml:157** by [@mimowo](https://github.com/mimowo) — 2025-03-12T06:20:44Z
```diff
@@ -153,6 +153,3 @@ webhookService:
 
 # kueue-viz dashboard
 enableKueueViz: false
-
-metrics:
```

  I think this is actually addressed with the update to set 
  
   serviceMonitor:
      tlsConfig:
        insecureSkipVerify: true

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T06:22:07Z

/lgtm
/approve
Thanks!
/hold 
in case @tenzen-y has comments

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-12T07:18:17Z

Thank you for this.

- **config/components/prometheus/kustomization.yaml:11** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T07:14:15Z
```diff
@@ -1,3 +1,11 @@
 resources:
 - monitor.yaml
 - role.yaml
+# [PROMETHEUS-WITH-CERTS] The following patch configures the ServiceMonitor in ../prometheus
+# to securely reference certificates created and managed by cert-manager.
+# Additionally, ensure that you uncomment the [METRICS WITH CERTMANAGER] patch under config/default/kustomization.yaml
+# to mount the "metrics-server-cert" secret in the Manager Deployment.
+patches:
+- path: monitor_tls_patch.yaml
+  target:
+    kind: ServiceMonitor
```

  Why do we need to apply TLS patch by default?

- **cmd/kueue/main.go:161** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T07:15:32Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
```

  ```suggestion
  		metricsCertPath := "/etc/kueue/metrics/certs"
  ```
  
  As I mentioned in KEP, we might want to use additional subdirectory to make room for future configuration.
  WDYT?

- **cmd/kueue/main.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T07:16:39Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
+		setupLog.Info("Initializing metrics certificate watcher using provided certificates",
+			"metrics-cert-path", metricsCertPath)
+
+		var err error
+		metricsCertWatcher, err := certwatcher.New(
+			filepath.Join(metricsCertPath, "tls.crt"),
+			filepath.Join(metricsCertPath, "tls.key"),
```

  It would be better to add this fixed named certs name in the installation guide.

- **cmd/kueue/main.go:171** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T07:17:20Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
+		setupLog.Info("Initializing metrics certificate watcher using provided certificates",
+			"metrics-cert-path", metricsCertPath)
+
+		var err error
+		metricsCertWatcher, err := certwatcher.New(
+			filepath.Join(metricsCertPath, "tls.crt"),
+			filepath.Join(metricsCertPath, "tls.key"),
+		)
+		if err != nil {
+			setupLog.Error(err, "to initialize metrics certificate watcher", "error", err)
```

  ```suggestion
  			setupLog.Error(err, "Unable to initialize metrics certificate watcher")
  ```
  
  The err has already been recorded.

- **apis/config/v1beta1/configuration_types.go:329** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T07:18:12Z
```diff
@@ -318,9 +318,15 @@ const (
 )
 
 type InternalCertManagement struct {
-	// Enable controls whether to enable internal cert management or not.
-	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
-	// set it to false. See the user guide for more information.
+	// Enable controls the use of internal cert management for the webhook
+	// and metrics endpoints.
+	// When enabled Kueue is using libraries to generate and
+	// self-sign the certificates.
+	// When disabled, you need to provide the certificates for
+	// the webhooks and metrics through a third party certificate
+	// This secret is mounted to the kueue controller manager pod. The mount
+	// path for webhooks is /tmp/k8s-webhook-server/serving-certs, whereas for
+	// metrics endpoint the expected path is `/etc/kueue/metrics-certs`.
```

  Could we mention fixed name certification names, `tls.crt` and `tls.key`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T07:26:57Z

- **config/components/prometheus/kustomization.yaml:11** by [@mimowo](https://github.com/mimowo) — 2025-03-12T07:26:57Z
```diff
@@ -1,3 +1,11 @@
 resources:
 - monitor.yaml
 - role.yaml
+# [PROMETHEUS-WITH-CERTS] The following patch configures the ServiceMonitor in ../prometheus
+# to securely reference certificates created and managed by cert-manager.
+# Additionally, ensure that you uncomment the [METRICS WITH CERTMANAGER] patch under config/default/kustomization.yaml
+# to mount the "metrics-server-cert" secret in the Manager Deployment.
+patches:
+- path: monitor_tls_patch.yaml
+  target:
+    kind: ServiceMonitor
```

  yeah, we want to make sure the Prometheus config works as before by default.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-12T12:49:09Z

- **cmd/kueue/main.go:168** by [@kannon92](https://github.com/kannon92) — 2025-03-12T12:49:09Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
+		setupLog.Info("Initializing metrics certificate watcher using provided certificates",
+			"metrics-cert-path", metricsCertPath)
+
+		var err error
+		metricsCertWatcher, err := certwatcher.New(
+			filepath.Join(metricsCertPath, "tls.crt"),
+			filepath.Join(metricsCertPath, "tls.key"),
```

  Yea, let me file an issue for documentation on this. @mimowo and I are thinking we would do a follow up PR with documentation showing an example of how to enable this.
  
  My first draft of this I included a values file to enable this but that is probably not the way we want to go.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T13:04:42Z

- **cmd/kueue/main.go:168** by [@mimowo](https://github.com/mimowo) — 2025-03-12T13:04:42Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
+		setupLog.Info("Initializing metrics certificate watcher using provided certificates",
+			"metrics-cert-path", metricsCertPath)
+
+		var err error
+		metricsCertWatcher, err := certwatcher.New(
+			filepath.Join(metricsCertPath, "tls.crt"),
+			filepath.Join(metricsCertPath, "tls.key"),
```

  I think it would be ok for me to just say a sentence about it in the API comment for the Enable API field, as suggested in the other comment: https://github.com/kubernetes-sigs/kueue/pull/4385#discussion_r1990791102. 
  
  Updating the installation guide is also fine as a follow up PR or Issue.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-12T16:23:31Z

- **apis/config/v1beta1/configuration_types.go:329** by [@kannon92](https://github.com/kannon92) — 2025-03-12T16:23:31Z
```diff
@@ -318,9 +318,15 @@ const (
 )
 
 type InternalCertManagement struct {
-	// Enable controls whether to enable internal cert management or not.
-	// Defaults to true. If you want to use a third-party management, e.g. cert-manager,
-	// set it to false. See the user guide for more information.
+	// Enable controls the use of internal cert management for the webhook
+	// and metrics endpoints.
+	// When enabled Kueue is using libraries to generate and
+	// self-sign the certificates.
+	// When disabled, you need to provide the certificates for
+	// the webhooks and metrics through a third party certificate
+	// This secret is mounted to the kueue controller manager pod. The mount
+	// path for webhooks is /tmp/k8s-webhook-server/serving-certs, whereas for
+	// metrics endpoint the expected path is `/etc/kueue/metrics-certs`.
```

  fixed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-12T16:24:24Z

- **cmd/kueue/main.go:168** by [@kannon92](https://github.com/kannon92) — 2025-03-12T16:24:24Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
+		setupLog.Info("Initializing metrics certificate watcher using provided certificates",
+			"metrics-cert-path", metricsCertPath)
+
+		var err error
+		metricsCertWatcher, err := certwatcher.New(
+			filepath.Join(metricsCertPath, "tls.crt"),
+			filepath.Join(metricsCertPath, "tls.key"),
```

  Sounds good. I will file a future issue for this as I think the documentation of this is important.
  
  Addressed the enable api field in the latest commit.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T00:20:31Z

- **cmd/kueue/main.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T00:20:31Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
+		setupLog.Info("Initializing metrics certificate watcher using provided certificates",
+			"metrics-cert-path", metricsCertPath)
+
+		var err error
+		metricsCertWatcher, err := certwatcher.New(
+			filepath.Join(metricsCertPath, "tls.crt"),
+			filepath.Join(metricsCertPath, "tls.key"),
```

  SGTM

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-13T00:30:50Z

- **cmd/kueue/main.go:161** by [@kannon92](https://github.com/kannon92) — 2025-03-13T00:30:50Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
```

  Done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-13T01:11:52Z

- **cmd/kueue/main.go:171** by [@kannon92](https://github.com/kannon92) — 2025-03-13T01:11:52Z
```diff
@@ -153,6 +156,26 @@ func main() {
 		SecureServing:  true,
 		FilterProvider: filters.WithAuthenticationAndAuthorization,
 	}
+
+	if cfg.InternalCertManagement == nil || !*cfg.InternalCertManagement.Enable {
+		metricsCertPath := "/etc/kueue/metrics-certs"
+		setupLog.Info("Initializing metrics certificate watcher using provided certificates",
+			"metrics-cert-path", metricsCertPath)
+
+		var err error
+		metricsCertWatcher, err := certwatcher.New(
+			filepath.Join(metricsCertPath, "tls.crt"),
+			filepath.Join(metricsCertPath, "tls.key"),
+		)
+		if err != nil {
+			setupLog.Error(err, "to initialize metrics certificate watcher", "error", err)
```

  addressed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T06:02:06Z

- **config/components/prometheus/kustomization.yaml:11** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T06:02:06Z
```diff
@@ -1,3 +1,11 @@
 resources:
 - monitor.yaml
 - role.yaml
+# [PROMETHEUS-WITH-CERTS] The following patch configures the ServiceMonitor in ../prometheus
+# to securely reference certificates created and managed by cert-manager.
+# Additionally, ensure that you uncomment the [METRICS WITH CERTMANAGER] patch under config/default/kustomization.yaml
+# to mount the "metrics-server-cert" secret in the Manager Deployment.
+patches:
+- path: monitor_tls_patch.yaml
+  target:
+    kind: ServiceMonitor
```

  This is still remaining.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-13T10:41:14Z

- **config/components/prometheus/kustomization.yaml:11** by [@kannon92](https://github.com/kannon92) — 2025-03-13T10:41:14Z
```diff
@@ -1,3 +1,11 @@
 resources:
 - monitor.yaml
 - role.yaml
+# [PROMETHEUS-WITH-CERTS] The following patch configures the ServiceMonitor in ../prometheus
+# to securely reference certificates created and managed by cert-manager.
+# Additionally, ensure that you uncomment the [METRICS WITH CERTMANAGER] patch under config/default/kustomization.yaml
+# to mount the "metrics-server-cert" secret in the Manager Deployment.
+patches:
+- path: monitor_tls_patch.yaml
+  target:
+    kind: ServiceMonitor
```

  I think we can still kept this based on @mimowo comments.
  
  if I remove this it breaks the ability to configure tlsConfigs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T16:03:50Z

- **config/components/prometheus/kustomization.yaml:11** by [@mimowo](https://github.com/mimowo) — 2025-03-13T16:03:50Z
```diff
@@ -1,3 +1,11 @@
 resources:
 - monitor.yaml
 - role.yaml
+# [PROMETHEUS-WITH-CERTS] The following patch configures the ServiceMonitor in ../prometheus
+# to securely reference certificates created and managed by cert-manager.
+# Additionally, ensure that you uncomment the [METRICS WITH CERTMANAGER] patch under config/default/kustomization.yaml
+# to mount the "metrics-server-cert" secret in the Manager Deployment.
+patches:
+- path: monitor_tls_patch.yaml
+  target:
+    kind: ServiceMonitor
```

  Actually, I agree with @tenzen-y  - i missed this when reviewing. We should not change the default configuration, so I suggest the patch to be opt-in.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T16:17:17Z

- **config/default/kustomization.yaml:32** by [@mimowo](https://github.com/mimowo) — 2025-03-13T16:17:17Z
```diff
@@ -29,7 +29,7 @@ resources:
 # [CERTMANAGER] To enable cert-manager, uncomment all sections with 'CERTMANAGER'. 'WEBHOOK' components are required.
 # - ../components/certmanager
 # [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
-#- ../components/prometheus
+# - ../components/prometheus
```

  nit: revert the formatting change

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-13T16:19:58Z

- **config/default/kustomization.yaml:32** by [@kannon92](https://github.com/kannon92) — 2025-03-13T16:19:57Z
```diff
@@ -29,7 +29,7 @@ resources:
 # [CERTMANAGER] To enable cert-manager, uncomment all sections with 'CERTMANAGER'. 'WEBHOOK' components are required.
 # - ../components/certmanager
 # [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
-#- ../components/prometheus
+# - ../components/prometheus
```

  That matches cert manager above.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T16:21:11Z

- **config/default/kustomization.yaml:32** by [@mimowo](https://github.com/mimowo) — 2025-03-13T16:21:11Z
```diff
@@ -29,7 +29,7 @@ resources:
 # [CERTMANAGER] To enable cert-manager, uncomment all sections with 'CERTMANAGER'. 'WEBHOOK' components are required.
 # - ../components/certmanager
 # [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
-#- ../components/prometheus
+# - ../components/prometheus
```

  ok

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T19:03:18Z

Thank you!
My concerns seemed to be gone!

/approve
