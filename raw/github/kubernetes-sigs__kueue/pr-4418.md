# PR #4418: TAS: support for cohorts and preemptions within them

**Summary**: TAS: support for cohorts and preemptions within them

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4418

**Last updated**: 2025-07-10T12:06:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `tas-cohort-preemption` → `main`
- **Created**: 2025-02-26T15:37:40Z
- **Updated**: 2025-07-10T12:06:37Z
- **Merged**: 2025-03-05T16:19:50Z
- **Closed**: 2025-03-05T16:19:50Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 9   **+1408 / -41**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of  #3761

#### Special notes for your reviewer:

Preparatory PRs (in the preferred order of merging):
- introduction of the helper struct for preemptedWorkloads: [PR](https://github.com/kubernetes-sigs/kueue/pull/4447) (done) 
- refactoring of the fits function in scheduler: [PR](https://github.com/kubernetes-sigs/kueue/pull/4449)

I consider two follow ups:
1. Update the [Usage](https://github.com/kubernetes-sigs/kueue/blob/f17779dc3de5cfc4232b0f4151358199d4936a7d/pkg/scheduler/flavorassigner/flavorassigner.go#L48-L50) field in assignment to the `workload.Usage` type to also encapsulate TAS usage - the benefit is more consistent handling of Quota usage and TAS usage, and avoids the need to re-compute
2. Improve the handling of situation when two workloads with "Fit" target the same TAS domains and cannot be assigned in the same cycle. We could then update the TAS assignment for the second workload (then !fits).

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Support cohorts and preemption within them.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-02-26T15:38:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bcce96f6f3ddc2db26386bcee1df6dad50f802d4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67c85d157efc1000082848f5 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-27T09:21:13Z

/test pull-kueue-test-unit-main
Opened: https://github.com/kubernetes-sigs/kueue/issues/4425

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-03T08:05:13Z

/test pull-kueue-test-e2e-main-1-30

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-03T08:39:25Z

cc @gabesaba @PBundyra @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-03T12:09:34Z

/hold
To avoid premature merging, I would like to get passes from @tenzen-y @gabesaba and @PBundyra, if possible.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-03T12:12:50Z

Rebased after https://github.com/kubernetes-sigs/kueue/pull/4449

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-04T09:59:16Z

/test pull-kueue-test-e2e-main-1-29
Due to  https://github.com/kubernetes-sigs/kueue/issues/4434

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-05T14:30:11Z

@tenzen-y I believe all comments are addressed. PTAL.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-05T15:47:23Z

LGTM label has been added.  <details>Git tree hash: 7c5355ba834a2bb4f91d2d1c6126422ca1051626</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-05T15:47:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4418#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4418#discussion_r1981692426" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-05T15:55:50Z

/hold cancel
I believe I can address @gabesaba comments in a follow up, if any.

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-03T15:55:40Z

- **pkg/scheduler/scheduler.go:392** by [@PBundyra](https://github.com/PBundyra) — 2025-03-03T15:55:40Z
```diff
@@ -383,8 +386,14 @@ func fits(cq *cache.ClusterQueueSnapshot, usage *workload.Usage, preemptedWorklo
 
 // resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
 func resourcesToReserve(e *entry, cq *cache.ClusterQueueSnapshot) workload.Usage {
+	if features.Enabled(features.TopologyAwareScheduling) && e.IsRequestingTAS() {
+		tasRequests := e.assignment.WorkloadsTopologyRequests(&e.Info, cq)
+		tasResult := cq.FindTopologyAssignmentsForWorkload(tasRequests, true)
```

  Why do we want to simulate empty snapshot here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-03T16:14:37Z

- **pkg/scheduler/scheduler.go:392** by [@mimowo](https://github.com/mimowo) — 2025-03-03T16:14:37Z
```diff
@@ -383,8 +386,14 @@ func fits(cq *cache.ClusterQueueSnapshot, usage *workload.Usage, preemptedWorklo
 
 // resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
 func resourcesToReserve(e *entry, cq *cache.ClusterQueueSnapshot) workload.Usage {
+	if features.Enabled(features.TopologyAwareScheduling) && e.IsRequestingTAS() {
+		tasRequests := e.assignment.WorkloadsTopologyRequests(&e.Info, cq)
+		tasResult := cq.FindTopologyAssignmentsForWorkload(tasRequests, true)
```

  This is the scenario when the mode is Preempt but there are no targets, for example, because the running workloads are of higher priority. Still we need to reserve the quota and TAS domains so that other workloads don't use it. Simulating empty is the best bet I think. I will add a comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-04T09:34:09Z

- **pkg/scheduler/scheduler.go:392** by [@mimowo](https://github.com/mimowo) — 2025-03-04T09:34:09Z
```diff
@@ -383,8 +386,14 @@ func fits(cq *cache.ClusterQueueSnapshot, usage *workload.Usage, preemptedWorklo
 
 // resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
 func resourcesToReserve(e *entry, cq *cache.ClusterQueueSnapshot) workload.Usage {
+	if features.Enabled(features.TopologyAwareScheduling) && e.IsRequestingTAS() {
+		tasRequests := e.assignment.WorkloadsTopologyRequests(&e.Info, cq)
+		tasResult := cq.FindTopologyAssignmentsForWorkload(tasRequests, true)
```

  Add the comment. PTAL. Also we already have a test case "preempting workload with targets reserves capacity so that lower priority workload cannot use it" demonstrating the need for reserving the resources.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-05T02:34:06Z

- **pkg/workload/workload.go:289** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T01:16:40Z
```diff
@@ -281,17 +281,17 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
-// IsUsingTAS returns information if the workload is using TAS
-func (i *Info) IsUsingTAS() bool {
-	return slices.ContainsFunc(i.TotalRequests,
-		func(ps PodSetResources) bool {
+// IsRequestingTAS returns information if the workload is requesting TAS
+func (i *Info) IsRequestingTAS() bool {
+	return slices.ContainsFunc(i.Obj.Spec.PodSets,
+		func(ps kueue.PodSet) bool {
 			return ps.TopologyRequest != nil
 		})
```

  Does this indicate that we forget updating `i.TotalRequests` somewhere?

- **pkg/scheduler/scheduler.go:434** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T02:14:38Z
```diff
@@ -424,9 +439,9 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	}
 
 	if arm == flavorassigner.Preempt {
-		faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
+		preemptionAssignment, faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
 		if len(faPreemptionTargets) > 0 {
```

  It seems this will update `fullAssignment` as well, right? Because preemptor does not clone `fullAssignment` and just update it.
  So, even if `len(faPreemptionTargets)` is `0`, the `fullAssignment` looks like updated in `GetTargets()` function.
  
  I meant even if there are no preemption targets, the assignment will be updated.
  Is this intended behavior?

- **pkg/scheduler/preemption/preemption.go:78** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T02:17:29Z
```diff
@@ -71,6 +72,10 @@ type preemptionCtx struct {
 	requests          resources.FlavorResourceQuantities
 	tasRequests       cache.WorkloadTASRequests
 	frsNeedPreemption sets.Set[resources.FlavorResource]
+
+	// tasResult is used to propagate the result from the successful
+	// workloadFits up, so that it can be used to update the assignment.
+	tasResult *cache.TASAssignmentsResult
```

  ```suggestion
  
  	tasRequests cache.WorkloadTASRequests
  	// tasResult is used to propagate the result from the successful
  	// workloadFits up, so that it can be used to update the assignment.
  	tasResult *cache.TASAssignmentsResult
  ```
  
  If this new line indicates TAS related grouping, it would be better to move `tasRequests` to here.

- **pkg/scheduler/preemption/preemption_test.go:1556** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T02:26:11Z
```diff
@@ -1553,7 +1553,7 @@ func TestPreemption(t *testing.T) {
 			}
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
-			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
+			_, targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
```

  Could you verify if newAssignment is expected?
  For now, we can just confirm this equal to `tc.assignment` since we do not enable TAS here.
  
  As as follow-up, it would be better TAS case to this UT, and then verify newAssignment.

- **pkg/scheduler/preemption/preemption_test.go:2070** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T02:26:49Z
```diff
@@ -2067,7 +2067,7 @@ func TestFairPreemptions(t *testing.T) {
 			}
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
-			targets := preemptor.GetTargets(log, *wlInfo, singlePodSetAssignment(
+			_, targets := preemptor.GetTargets(log, *wlInfo, singlePodSetAssignment(
```

  Regarding newAssignment verification.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T10:25:02Z

- **pkg/workload/workload.go:289** by [@mimowo](https://github.com/mimowo) — 2025-03-05T10:25:01Z
```diff
@@ -281,17 +281,17 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
-// IsUsingTAS returns information if the workload is using TAS
-func (i *Info) IsUsingTAS() bool {
-	return slices.ContainsFunc(i.TotalRequests,
-		func(ps PodSetResources) bool {
+// IsRequestingTAS returns information if the workload is requesting TAS
+func (i *Info) IsRequestingTAS() bool {
+	return slices.ContainsFunc(i.Obj.Spec.PodSets,
+		func(ps kueue.PodSet) bool {
 			return ps.TopologyRequest != nil
 		})
```

  This is needed for one place in scheduler: https://github.com/kubernetes-sigs/kueue/pull/4418/files#diff-fb3d6b36d3cd727e1dd2353fb86c0aee0bfaae1c900e0f005ab913620021d551R389
  
  In this scenario we have the TotalRequests, but they don't have the TopologyRequest set. I think this is ok, it could be solved by other ways, but I'm not sure better (and more involving):
  1. Always set TopologyRequest under TotalRequests, just make it "empty" (basically new state)
  2. Set the fallback TAS assignment in the preemption.go
  
  Out of the two, (2.) would be my preference, but changing`IsUsingTAS` to  `IsRequestingTAS` seems like least work, and the new function is more generic.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T10:48:58Z

- **pkg/scheduler/scheduler.go:434** by [@mimowo](https://github.com/mimowo) — 2025-03-05T10:48:57Z
```diff
@@ -424,9 +439,9 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	}
 
 	if arm == flavorassigner.Preempt {
-		faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
+		preemptionAssignment, faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
 		if len(faPreemptionTargets) > 0 {
```

  Good point, actually the `fullAssignment` is modified by GetTargets and the new value is returned. So, we have two options:
  1. don't return the new value and rename the function to something reflecting it may mutate assignment, like "Preempt"
  2. clone the assignment and return the updated version
  
  I would be leaning to 2, but it is more involving as we need to implement Clone for Assignment and its substructure. WDYT?
  
  > So, even if len(faPreemptionTargets) is 0, the fullAssignment looks like updated in GetTargets() function.
  
  It should not be updated, because if len(targets) is zero we wouldn't get successful tasResult, but we can prevent that explicitly in code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T10:50:37Z

- **pkg/scheduler/preemption/preemption.go:78** by [@mimowo](https://github.com/mimowo) — 2025-03-05T10:50:37Z
```diff
@@ -71,6 +72,10 @@ type preemptionCtx struct {
 	requests          resources.FlavorResourceQuantities
 	tasRequests       cache.WorkloadTASRequests
 	frsNeedPreemption sets.Set[resources.FlavorResource]
+
+	// tasResult is used to propagate the result from the successful
+	// workloadFits up, so that it can be used to update the assignment.
+	tasResult *cache.TASAssignmentsResult
```

  We could move it next to tasRequests because both are related to TAS. OTOH, I was thinking about putting it separately, because this is the only field which is set during the algorithm, which makes is special. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T10:53:41Z

- **pkg/scheduler/preemption/preemption_test.go:1556** by [@mimowo](https://github.com/mimowo) — 2025-03-05T10:53:40Z
```diff
@@ -1553,7 +1553,7 @@ func TestPreemption(t *testing.T) {
 			}
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
-			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
+			_, targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
```

  This is already unit tested by the tests in scheduler_test.go, in particular: https://github.com/kubernetes-sigs/kueue/pull/4418/files#diff-130e36b28b6a84d355879caa6b7927c98ec18c63adad6ab4fc486458763080bfR6570
  
  I agree having dedicated unit tests in `pkg/scheduler/preemption/preemption_test.go` could be better, but I would prefer to call it a follow up, but as you prefer.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T10:53:51Z

- **pkg/scheduler/preemption/preemption_test.go:2070** by [@mimowo](https://github.com/mimowo) — 2025-03-05T10:53:51Z
```diff
@@ -2067,7 +2067,7 @@ func TestFairPreemptions(t *testing.T) {
 			}
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
-			targets := preemptor.GetTargets(log, *wlInfo, singlePodSetAssignment(
+			_, targets := preemptor.GetTargets(log, *wlInfo, singlePodSetAssignment(
```

  Same as there: https://github.com/kubernetes-sigs/kueue/pull/4418#discussion_r1981161902

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T12:50:24Z

- **pkg/scheduler/scheduler.go:434** by [@mimowo](https://github.com/mimowo) — 2025-03-05T12:50:24Z
```diff
@@ -424,9 +439,9 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	}
 
 	if arm == flavorassigner.Preempt {
-		faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
+		preemptionAssignment, faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
 		if len(faPreemptionTargets) > 0 {
```

  Ok, I took the "in the middle" approach. I no longer mutate the assignment in GetTargets, but outside in the getAssignments function, which makes sense IMO. The GetTargets just returns extra the tasResult. 
  
  WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T13:22:06Z

- **pkg/workload/workload.go:289** by [@mimowo](https://github.com/mimowo) — 2025-03-05T13:22:06Z
```diff
@@ -281,17 +281,17 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
-// IsUsingTAS returns information if the workload is using TAS
-func (i *Info) IsUsingTAS() bool {
-	return slices.ContainsFunc(i.TotalRequests,
-		func(ps PodSetResources) bool {
+// IsRequestingTAS returns information if the workload is requesting TAS
+func (i *Info) IsRequestingTAS() bool {
+	return slices.ContainsFunc(i.Obj.Spec.PodSets,
+		func(ps kueue.PodSet) bool {
 			return ps.TopologyRequest != nil
 		})
```

  ~/hold~
  ~I have a better idea - just return the targets and re-compute the assignment. This will also eliminate other~ ~comments. The drawback is sligtly more computation, but I think we can optimize when needed.~
  
  I wanted to post it to another comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T13:23:27Z

- **pkg/scheduler/scheduler.go:434** by [@mimowo](https://github.com/mimowo) — 2025-03-05T13:23:27Z
```diff
@@ -424,9 +439,9 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	}
 
 	if arm == flavorassigner.Preempt {
-		faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
+		preemptionAssignment, faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
 		if len(faPreemptionTargets) > 0 {
```

  I have a better idea - just return the targets and re-compute the assignment. This will also eliminate other comments. The drawback is slightly more computation, but I think we can optimize when needed. On it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T14:10:20Z

- **pkg/scheduler/preemption/preemption_test.go:2070** by [@mimowo](https://github.com/mimowo) — 2025-03-05T14:10:20Z
```diff
@@ -2067,7 +2067,7 @@ func TestFairPreemptions(t *testing.T) {
 			}
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
-			targets := preemptor.GetTargets(log, *wlInfo, singlePodSetAssignment(
+			_, targets := preemptor.GetTargets(log, *wlInfo, singlePodSetAssignment(
```

  refactored in https://github.com/kubernetes-sigs/kueue/pull/4418/commits/f1a3f568c6a062fc5c4491e918957574fb032fa6 which keeps the interface of GetTargets the same

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T14:10:43Z

- **pkg/scheduler/preemption/preemption_test.go:1556** by [@mimowo](https://github.com/mimowo) — 2025-03-05T14:10:42Z
```diff
@@ -1553,7 +1553,7 @@ func TestPreemption(t *testing.T) {
 			}
 			wlInfo := workload.NewInfo(tc.incoming)
 			wlInfo.ClusterQueue = tc.targetCQ
-			targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
+			_, targets := preemptor.GetTargets(log, *wlInfo, tc.assignment, snapshotWorkingCopy)
```

  Refactored in https://github.com/kubernetes-sigs/kueue/commit/f1a3f568c6a062fc5c4491e918957574fb032fa6 which keeps the GetTargets interface the same

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T14:11:06Z

- **pkg/scheduler/preemption/preemption.go:78** by [@mimowo](https://github.com/mimowo) — 2025-03-05T14:11:06Z
```diff
@@ -71,6 +72,10 @@ type preemptionCtx struct {
 	requests          resources.FlavorResourceQuantities
 	tasRequests       cache.WorkloadTASRequests
 	frsNeedPreemption sets.Set[resources.FlavorResource]
+
+	// tasResult is used to propagate the result from the successful
+	// workloadFits up, so that it can be used to update the assignment.
+	tasResult *cache.TASAssignmentsResult
```

  Rafactored in https://github.com/kubernetes-sigs/kueue/commit/f1a3f568c6a062fc5c4491e918957574fb032fa6 which drops the field entirely.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-05T14:11:16Z

- **pkg/scheduler/scheduler.go:434** by [@mimowo](https://github.com/mimowo) — 2025-03-05T14:11:16Z
```diff
@@ -424,9 +439,9 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	}
 
 	if arm == flavorassigner.Preempt {
-		faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
+		preemptionAssignment, faPreemptionTargets := s.preemptor.GetTargets(log, *wl, fullAssignment, snap)
 		if len(faPreemptionTargets) > 0 {
```

  Done, PTAL https://github.com/kubernetes-sigs/kueue/commit/f1a3f568c6a062fc5c4491e918957574fb032fa6

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-05T15:45:00Z

The refactored solution looks a pretty better solution rather than the initial one.
Thank you!

I added a nit comment.

- **pkg/scheduler/scheduler.go:473** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T15:43:36Z
```diff
@@ -448,6 +459,31 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return fullAssignment, nil
 }
 
+func updateAssignmentForTAS(cq *cache.ClusterQueueSnapshot, wl *workload.Info, assignment flavorassigner.Assignment, targets []*preemption.Target) {
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == flavorassigner.Preempt && wl.IsRequestingTAS() {
+		tasRequests := assignment.WorkloadsTopologyRequests(wl, cq)
+		var tasResult cache.TASAssignmentsResult
+		if len(targets) > 0 {
+			var targetWorkloads []*workload.Info
+			for _, target := range targets {
+				targetWorkloads = append(targetWorkloads, target.WorkloadInfo)
+			}
+			revertUsage := cq.SimulateUsageRemoval(targetWorkloads)
+			tasResult = cq.FindTopologyAssignmentsForWorkload(tasRequests, false)
+			revertUsage()
```

  ```suggestion
  			revertUsage := cq.SimulateUsageRemoval(targetWorkloads)
  			defer revertUsage()
  			tasResult = cq.FindTopologyAssignmentsForWorkload(tasRequests, false)
  ```
  
  Should we use `defer` statement to guarantee it?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-05T15:46:48Z

- **pkg/scheduler/scheduler.go:473** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T15:46:48Z
```diff
@@ -448,6 +459,31 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return fullAssignment, nil
 }
 
+func updateAssignmentForTAS(cq *cache.ClusterQueueSnapshot, wl *workload.Info, assignment flavorassigner.Assignment, targets []*preemption.Target) {
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == flavorassigner.Preempt && wl.IsRequestingTAS() {
+		tasRequests := assignment.WorkloadsTopologyRequests(wl, cq)
+		var tasResult cache.TASAssignmentsResult
+		if len(targets) > 0 {
+			var targetWorkloads []*workload.Info
+			for _, target := range targets {
+				targetWorkloads = append(targetWorkloads, target.WorkloadInfo)
+			}
+			revertUsage := cq.SimulateUsageRemoval(targetWorkloads)
+			tasResult = cq.FindTopologyAssignmentsForWorkload(tasRequests, false)
+			revertUsage()
```

  Oh, this seems not to work. NVM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-05T15:47:17Z

- **pkg/scheduler/scheduler.go:473** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-05T15:47:17Z
```diff
@@ -448,6 +459,31 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return fullAssignment, nil
 }
 
+func updateAssignmentForTAS(cq *cache.ClusterQueueSnapshot, wl *workload.Info, assignment flavorassigner.Assignment, targets []*preemption.Target) {
+	if features.Enabled(features.TopologyAwareScheduling) && assignment.RepresentativeMode() == flavorassigner.Preempt && wl.IsRequestingTAS() {
+		tasRequests := assignment.WorkloadsTopologyRequests(wl, cq)
+		var tasResult cache.TASAssignmentsResult
+		if len(targets) > 0 {
+			var targetWorkloads []*workload.Info
+			for _, target := range targets {
+				targetWorkloads = append(targetWorkloads, target.WorkloadInfo)
+			}
+			revertUsage := cq.SimulateUsageRemoval(targetWorkloads)
+			tasResult = cq.FindTopologyAssignmentsForWorkload(tasRequests, false)
+			revertUsage()
```

  Thank you!
  
  /lgtm
  /approve
