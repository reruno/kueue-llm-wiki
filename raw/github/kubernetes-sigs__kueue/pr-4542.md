# PR #4542: TAS: KEP update: Add a new TAS algorithm and TAS profiles

**Summary**: TAS: KEP update: Add a new TAS algorithm and TAS profiles

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4542

**Last updated**: 2025-08-12T10:15:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `TAS-KEP-relaxed-fit` → `main`
- **Created**: 2025-03-10T11:16:32Z
- **Updated**: 2025-08-12T10:15:47Z
- **Merged**: 2025-03-13T10:03:46Z
- **Closed**: 2025-03-13T10:03:46Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 2   **+61 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
- Adds a new annotation that signalizes PodSet doesnt have any topological requirements
- Cleans up and renames current TAS algorithms and introduces a new one called `LeastFreeCapacity`
- Introduces TAS profiles

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #4474
Part of #4570 

#### Special notes for your reviewer:
Please note that names of the algorithms are not referenced in the code and they're only used for describing the behaviour

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-10T11:16:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 44c62ff795ea22d6594bc1eefdb18ea6765aef1e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d1b5ae3b50d90008afea4d |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T11:16:57Z

cc @mwielgus @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T12:39:58Z

@mimowo @mwielgus ptal

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:13:28Z

landing right now

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-12T14:18:06Z

/lgtm
/approve
Leaving final tagging to @tenzen-y 
/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-12T14:18:13Z

LGTM label has been added.  <details>Git tree hash: aa48e97004d3671f3050fdd3ae0be981d9568d9e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-13T09:44:15Z

LGTM label has been added.  <details>Git tree hash: addcce7fdcb3513dc1ea08cbfb018f7a9d734d5e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-13T09:44:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4542#issuecomment-2718053951" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4542#" title="Author self-approved">PBundyra</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4542#discussion_r1993139660" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-10T11:41:59Z

- **keps/2724-topology-aware-scheduling/README.md:437** by [@mwielgus](https://github.com/mwielgus) — 2025-03-10T11:41:59Z
```diff
@@ -429,6 +429,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetRelaxedTopologyAnnotation indicates that a PodSet requires
+  // Topology Aware Scheduling, but it promotes filling up nodes to maximum over
+  // compact placement. The pods have lower chance of being scheduled on the same
+  // node, but it mitigates resource fragmentation, and can lead to better
+  // node utilization. Recommended for PodSets that don't require heave inter pod
```

  Fix wording around "don't require heave".

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-10T11:46:56Z

- **keps/2724-topology-aware-scheduling/README.md:433** by [@mwielgus](https://github.com/mwielgus) — 2025-03-10T11:46:56Z
```diff
@@ -429,6 +429,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetRelaxedTopologyAnnotation indicates that a PodSet requires
```

  Maybe something more direct like "No topology required"?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-10T11:49:24Z

- **keps/2724-topology-aware-scheduling/README.md:434** by [@mwielgus](https://github.com/mwielgus) — 2025-03-10T11:49:24Z
```diff
@@ -429,6 +429,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetRelaxedTopologyAnnotation indicates that a PodSet requires
+  // Topology Aware Scheduling, but it promotes filling up nodes to maximum over
```

  It is mixing up pod needs with cluster efficiency. Can we have these things separated. Annotation on a podset should mean that Kueue is free to place podset as it see fit in a given moment, and the current implementation is to fill the already utilozed nodes (but may change in the future).

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-10T11:51:51Z

- **keps/2724-topology-aware-scheduling/README.md:662** by [@mwielgus](https://github.com/mwielgus) — 2025-03-10T11:51:51Z
```diff
@@ -648,11 +656,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `Relaxed` - Kueue chooses the domain with least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This is signalized by the `kueue.x-k8s.io/podset-relaxed-topology: true` PodSet annotation.
```

  To be honest, `Relaxed` does't indicate what is really going on here. This mixes up podset declared needs with scheduling implementation.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-10T13:29:09Z

- **keps/2724-topology-aware-scheduling/README.md:437** by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T13:29:08Z
```diff
@@ -429,6 +429,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetRelaxedTopologyAnnotation indicates that a PodSet requires
+  // Topology Aware Scheduling, but it promotes filling up nodes to maximum over
+  // compact placement. The pods have lower chance of being scheduled on the same
+  // node, but it mitigates resource fragmentation, and can lead to better
+  // node utilization. Recommended for PodSets that don't require heave inter pod
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-10T13:29:15Z

- **keps/2724-topology-aware-scheduling/README.md:433** by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T13:29:15Z
```diff
@@ -429,6 +429,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetRelaxedTopologyAnnotation indicates that a PodSet requires
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-10T13:29:23Z

- **keps/2724-topology-aware-scheduling/README.md:434** by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T13:29:23Z
```diff
@@ -429,6 +429,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetRelaxedTopologyAnnotation indicates that a PodSet requires
+  // Topology Aware Scheduling, but it promotes filling up nodes to maximum over
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-10T13:29:30Z

- **keps/2724-topology-aware-scheduling/README.md:662** by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T13:29:30Z
```diff
@@ -648,11 +656,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `Relaxed` - Kueue chooses the domain with least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This is signalized by the `kueue.x-k8s.io/podset-relaxed-topology: true` PodSet annotation.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T14:11:20Z

- **keps/2724-topology-aware-scheduling/README.md:659** by [@mimowo](https://github.com/mimowo) — 2025-03-10T14:09:35Z
```diff
@@ -648,11 +653,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `ConditionalBestFit` - Kueue chooses the domain with the least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This algorithm is used for PodSet with the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set.
```

  tbh I dont get neither the Conditional not BestFit parts of the name :) . Maybe it should be called 
  LeastFreeCapacityFirst?
  
  and to be fair the current names are not good either. I would call them 'MostFreeCapacityFirst' and 'BestFit'.

- **keps/2724-topology-aware-scheduling/README.md:662** by [@mimowo](https://github.com/mimowo) — 2025-03-10T14:11:15Z
```diff
@@ -648,11 +653,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `ConditionalBestFit` - Kueue chooses the domain with the least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This algorithm is used for PodSet with the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set.
 
-By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`
+By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`.
+To use `ConditionalBestFit` algorithm, a PodSet needs to have the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set. To default this annotation for all Workloads in the cluster a user can set the feature gate `TASImplicitDefaultRelaxed` to `true`.
```

  Regarding the feature gate, it does not really default the annotation. It just makes kueue assume it as an implicit default when explicit annotation is not specified.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-10T16:15:43Z

- **keps/2724-topology-aware-scheduling/README.md:662** by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T16:15:43Z
```diff
@@ -648,11 +653,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `ConditionalBestFit` - Kueue chooses the domain with the least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This algorithm is used for PodSet with the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set.
 
-By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`
+By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`.
+To use `ConditionalBestFit` algorithm, a PodSet needs to have the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set. To default this annotation for all Workloads in the cluster a user can set the feature gate `TASImplicitDefaultRelaxed` to `true`.
```

  Oh yes, clarified it, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-10T16:20:54Z

- **keps/2724-topology-aware-scheduling/README.md:659** by [@PBundyra](https://github.com/PBundyra) — 2025-03-10T16:20:54Z
```diff
@@ -648,11 +653,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `ConditionalBestFit` - Kueue chooses the domain with the least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This algorithm is used for PodSet with the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set.
```

  Are you proposing:
  
  `MostAllocated` -> `BestFit`
  `LeastAllocated` -> `MostFreeCapacityFirst`
  `ConditionalBestFit` -> `LeastFreeCapacityFirst`
  ?
  
  I remember the origin name for `MostAllocated` was actually `BestFit` but I've changed after discussion with @tenzen-y  https://github.com/kubernetes-sigs/kueue/pull/4228#discussion_r1952501465
  
  @mwielgus wdyt Michal's proposal

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T16:23:54Z

- **keps/2724-topology-aware-scheduling/README.md:659** by [@mimowo](https://github.com/mimowo) — 2025-03-10T16:23:54Z
```diff
@@ -648,11 +653,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `ConditionalBestFit` - Kueue chooses the domain with the least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This algorithm is used for PodSet with the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set.
```

  > Are you proposing:
  > MostAllocated -> BestFit
  > LeastAllocated -> MostFreeCapacityFirst
  > ConditionalBestFit -> LeastFreeCapacityFirst
  
  That's right, but I'm also ok to scope the comment to only renaming the new algo.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T12:38:17Z

- **keps/2724-topology-aware-scheduling/README.md:659** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T12:38:17Z
```diff
@@ -648,11 +653,13 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
+Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in three modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
 - `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
 - `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+- `ConditionalBestFit` - Kueue chooses the domain with the least available resources. It promotes minimizing resource fragmentation, and higher node utilization over compact placement. This algorithm is used for PodSet with the `kueue.x-k8s.io/podset-relaxed-topology: true` annotation set.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:43:24Z

- **keps/2724-topology-aware-scheduling/README.md:434** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:43:23Z
```diff
@@ -430,9 +430,16 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires no topology.
```

  ```suggestion
    // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any
    // topology requirements.
  ```
  proposal to make it less ambiguous

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:44:57Z

- **keps/2724-topology-aware-scheduling/README.md:436** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:44:57Z
```diff
@@ -430,9 +430,16 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires no topology.
+  // Kueue admits the PodSet if there's enough free capacity anywhere in the cluster.
+  // Recommended for PodSets that don't require inter pod communication
```

  ```suggestion
    // Recommended for PodSets that don't require pod-to-pod communication
  ```
  nit: maybe makes it more clear

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:45:51Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:45:50Z
```diff
@@ -430,9 +430,16 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires no topology.
+  // Kueue admits the PodSet if there's enough free capacity anywhere in the cluster.
+  // Recommended for PodSets that don't require inter pod communication
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
 )
 ```
 
+The annotations are mutually exclusive and shouldn't be combined.
```

  I would suggest to move this note to the Validation section, as we already validate against it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:49:02Z

- **keps/2724-topology-aware-scheduling/README.md:488** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:49:02Z
```diff
@@ -478,6 +485,14 @@ type PodSetTopologyRequest struct {
   // +optional
   Preferred *string `json:"preferred,omitempty"`
 
+  // Unconstrained indicates the topology assignment for the PodSet should be,
```

  ```suggestion
    // Unconstrained indicates the topology assignment for the PodSet should be,
  ```
  Should be sounds like it is not necessarily. Can we rephrase as  "is" or "is by default"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:49:23Z

- **keps/2724-topology-aware-scheduling/README.md:489** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:49:23Z
```diff
@@ -478,6 +485,14 @@ type PodSetTopologyRequest struct {
   // +optional
   Preferred *string `json:"preferred,omitempty"`
 
+  // Unconstrained indicates the topology assignment for the PodSet should be,
+  // computed using the Unconstrained algorithm. Indicated by the
```

  IIUC we don't have "Unconstrained algorithm", but in any case, I think we can assume the algorithm is not tightly coupled to the annotation definition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:50:30Z

- **keps/2724-topology-aware-scheduling/README.md:661** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:50:30Z
```diff
@@ -636,6 +651,9 @@ if it had the `podset-preferred-topology` annotation pointing to the lowest
 topology level defined in the Topology referenced by the selected TAS flavor.
 We call it "implicit default" as the annotation isn't persisted.
 
+To change the implicit default from `podset-preferred-topology` to `podset-unconstrained-topology`
+a user can switch on the `TASImplicitDefaultUnconstrained` feature gate.
```

  Please add somewhere a note that the feature gate is "deprecated" and only for experimentation to collect user feedback. If users find this configuration valuable we will introduce a dedicated TAS configuration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:51:09Z

- **keps/2724-topology-aware-scheduling/README.md:684** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:51:09Z
```diff
@@ -663,11 +681,21 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue packs pods on domains with different algorithms, depending on the annotation and chosen profile:
```

  ```suggestion
  Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
  ```
  Since we have `LeastFreeCapacity` maybe "packs" is misleading.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T12:52:27Z

- **keps/2724-topology-aware-scheduling/README.md:434** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T12:52:27Z
```diff
@@ -430,9 +430,16 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires no topology.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T12:52:34Z

- **keps/2724-topology-aware-scheduling/README.md:436** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T12:52:34Z
```diff
@@ -430,9 +430,16 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires no topology.
+  // Kueue admits the PodSet if there's enough free capacity anywhere in the cluster.
+  // Recommended for PodSets that don't require inter pod communication
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T12:52:39Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T12:52:39Z
```diff
@@ -430,9 +430,16 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires no topology.
+  // Kueue admits the PodSet if there's enough free capacity anywhere in the cluster.
+  // Recommended for PodSets that don't require inter pod communication
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
 )
 ```
 
+The annotations are mutually exclusive and shouldn't be combined.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T12:54:50Z

- **keps/2724-topology-aware-scheduling/README.md:698** by [@mimowo](https://github.com/mimowo) — 2025-03-11T12:54:50Z
```diff
@@ -663,11 +683,21 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue packs pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                  | preferred         | required          | unconstrained     |
+| --------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                    | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated) | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed                         | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity             | LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
```

  I would suggest to make these two feature gates deprecated too. The purpose of introducing them varies:
  - `TASProfileMostFreeCapacity` - introduced as a bailout.
  - `TASProfileMixed` - introduced for experimentation and collecting feedback
  -  `TASProfileLeastFreeCapacity` - introduced for experimentation and collecting feedback
  
  But I think all of them are "deprecated", because none of them is graduable in the current form.
  I would suggest to:
  - open an issue to introduce TAS configuration
  - add a graduation criteria for TAS for beta to introduce the configuration

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T13:01:00Z

- **keps/2724-topology-aware-scheduling/README.md:488** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T13:01:00Z
```diff
@@ -478,6 +485,14 @@ type PodSetTopologyRequest struct {
   // +optional
   Preferred *string `json:"preferred,omitempty"`
 
+  // Unconstrained indicates the topology assignment for the PodSet should be,
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T13:01:07Z

- **keps/2724-topology-aware-scheduling/README.md:489** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T13:01:07Z
```diff
@@ -478,6 +485,14 @@ type PodSetTopologyRequest struct {
   // +optional
   Preferred *string `json:"preferred,omitempty"`
 
+  // Unconstrained indicates the topology assignment for the PodSet should be,
+  // computed using the Unconstrained algorithm. Indicated by the
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T13:01:14Z

- **keps/2724-topology-aware-scheduling/README.md:661** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T13:01:14Z
```diff
@@ -636,6 +651,9 @@ if it had the `podset-preferred-topology` annotation pointing to the lowest
 topology level defined in the Topology referenced by the selected TAS flavor.
 We call it "implicit default" as the annotation isn't persisted.
 
+To change the implicit default from `podset-preferred-topology` to `podset-unconstrained-topology`
+a user can switch on the `TASImplicitDefaultUnconstrained` feature gate.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T13:01:21Z

- **keps/2724-topology-aware-scheduling/README.md:684** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T13:01:21Z
```diff
@@ -663,11 +681,21 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue packs pods on domains with different algorithms, depending on the annotation and chosen profile:
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T13:01:29Z

- **keps/2724-topology-aware-scheduling/README.md:698** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T13:01:29Z
```diff
@@ -663,11 +683,21 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue packs pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                  | preferred         | required          | unconstrained     |
+| --------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                    | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated) | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed                         | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity             | LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:08:12Z

- **keps/2724-topology-aware-scheduling/README.md:434** by [@mimowo](https://github.com/mimowo) — 2025-03-11T14:08:11Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires does not have any topology requirements.
```

  ```suggestion
    // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:09:29Z

- **keps/2724-topology-aware-scheduling/README.md:435** by [@mimowo](https://github.com/mimowo) — 2025-03-11T14:09:29Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity anywhere in the cluster.
```

  ```suggestion
    // Kueue admits the PodSet if there's enough free capacity available.
  ```
  not necessarily in the whole cluster. This is still constrained by the RF.spec.nodeLabels.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:10:23Z

- **keps/2724-topology-aware-scheduling/README.md:458** by [@mimowo](https://github.com/mimowo) — 2025-03-11T14:10:23Z
```diff
@@ -448,6 +453,10 @@ the rules is deactivated):
 - if "podset-required-topology" or "podset-preferred-topology" is specified for
   one PodTemplate, then one of them is specified for every PodTemplate in the
   Job spec.
+- the annotations `kueue.x-k8s.io/podset-required-topology`,
+  `kueue.x-k8s.io/podset-preferred-topology`, and `kueue.x-k8s.io/podset-unconstrained-topology`
+  are mutually exclusive and shouldn't be combined.
```

  ```suggestion
    are mutually exclusive.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:13:40Z

- **keps/2724-topology-aware-scheduling/README.md:491** by [@mimowo](https://github.com/mimowo) — 2025-03-11T14:13:39Z
```diff
@@ -478,6 +487,14 @@ type PodSetTopologyRequest struct {
   // +optional
   Preferred *string `json:"preferred,omitempty"`
 
+  // Unconstrained indicates that Kueue attempts to fit the PodSet within the entire available capacity,
+  // disregarding whether the nodes belong to the same domain.
```

  ```suggestion
    // unconstrained indicates that Kueue has freedom to schedule the PodSet within
    // the entire available capacity, regardless of domain placement.
  ```
  nit, proposal to rephrase.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:15:22Z

- **keps/2724-topology-aware-scheduling/README.md:718** by [@mimowo](https://github.com/mimowo) — 2025-03-11T14:15:21Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                  | preferred         | required          | unconstrained     |
+| --------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                    | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated) | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed (deprecated)            | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity (deprecated)| LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
 
-By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`
+Feature gates: `TASProfileLeastAllocated`, `TASProfileMixed` and `TASProfileLeastFreeCapacity` are mutually exclusive.
```

  Are they? Would we validate they are, or rather prioritize them?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:15:50Z

LGTM, just small-ish nits to clarify some aspects or improve comments

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T14:57:42Z

- **keps/2724-topology-aware-scheduling/README.md:718** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T14:57:42Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                  | preferred         | required          | unconstrained     |
+| --------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                    | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated) | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed (deprecated)            | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity (deprecated)| LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
 
-By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`
+Feature gates: `TASProfileLeastAllocated`, `TASProfileMixed` and `TASProfileLeastFreeCapacity` are mutually exclusive.
```

  I would lean towards validation and excluding them

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T15:02:37Z

- **keps/2724-topology-aware-scheduling/README.md:718** by [@mimowo](https://github.com/mimowo) — 2025-03-11T15:02:37Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                  | preferred         | required          | unconstrained     |
+| --------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                    | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated) | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed (deprecated)            | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity (deprecated)| LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
 
-By default Kueue uses the `MostAllocated` algorithm. To use `LeastAllocated` algorithm, a user needs to set the feature gate `TASLeastAllocated` to `true`
+Feature gates: `TASProfileLeastAllocated`, `TASProfileMixed` and `TASProfileLeastFreeCapacity` are mutually exclusive.
```

  sgtm, I don't have a strong view, just wasn't sure what it means in this context.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T17:44:01Z

- **keps/2724-topology-aware-scheduling/README.md:434** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T17:44:01Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires does not have any topology requirements.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T17:44:14Z

- **keps/2724-topology-aware-scheduling/README.md:435** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T17:44:13Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity anywhere in the cluster.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T17:44:22Z

- **keps/2724-topology-aware-scheduling/README.md:458** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T17:44:22Z
```diff
@@ -448,6 +453,10 @@ the rules is deactivated):
 - if "podset-required-topology" or "podset-preferred-topology" is specified for
   one PodTemplate, then one of them is specified for every PodTemplate in the
   Job spec.
+- the annotations `kueue.x-k8s.io/podset-required-topology`,
+  `kueue.x-k8s.io/podset-preferred-topology`, and `kueue.x-k8s.io/podset-unconstrained-topology`
+  are mutually exclusive and shouldn't be combined.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-11T17:44:31Z

- **keps/2724-topology-aware-scheduling/README.md:491** by [@PBundyra](https://github.com/PBundyra) — 2025-03-11T17:44:31Z
```diff
@@ -478,6 +487,14 @@ type PodSetTopologyRequest struct {
   // +optional
   Preferred *string `json:"preferred,omitempty"`
 
+  // Unconstrained indicates that Kueue attempts to fit the PodSet within the entire available capacity,
+  // disregarding whether the nodes belong to the same domain.
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-12T09:24:23Z

- **keps/2724-topology-aware-scheduling/README.md:716** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:21:42Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                   | preferred         | required          | unconstrained     |
+| ---------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                     | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated)  | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed (deprecated)             | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity (deprecated) | LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
```

  So, eventually, do we want to supply only BestFit, right?

- **keps/2724-topology-aware-scheduling/README.md:691** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:22:28Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
```

  If the topology is flat (only nodeLabel) for BestFit, what happens?

- **keps/2724-topology-aware-scheduling/README.md:441** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:23:13Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't require pod-to-pod communication
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  What are the differences between this annotation and one without any annotations?

- **keps/2724-topology-aware-scheduling/README.md:441** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:24:20Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't require pod-to-pod communication
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  Additionally, what is expected value? Bool?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T09:28:09Z

- **keps/2724-topology-aware-scheduling/README.md:691** by [@mimowo](https://github.com/mimowo) — 2025-03-12T09:28:08Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
```

  We don't know yet, but only BestFit sounds like a reasonable default.
  
  We may still keep other algorithms, depending on user feedback, under some dedicated configuration (not feature gates), and the user feedback will help us to design the configuration. 
  
  I think we could already open an issue for revisiting the TAS configuration regarding the placement algorithms.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T09:30:13Z

- **keps/2724-topology-aware-scheduling/README.md:716** by [@mimowo](https://github.com/mimowo) — 2025-03-12T09:30:13Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                   | preferred         | required          | unconstrained     |
+| ---------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                     | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated)  | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed (deprecated)             | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity (deprecated) | LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
```

  @PBundyra may want to add something, but my understanding is summarized in https://github.com/kubernetes-sigs/kueue/pull/4542#discussion_r1991039901. Basically BestFit is the only algorithm which works sensibly as a default. However, other algorithms may find some niche applicability, so we keep the feature gates for experimentation. We don't plan to graduate the feature gates, but remove. We either remove the feature gates along with the algorithms, or introduce a dedicated configuration for them (TBD).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-12T09:44:39Z

- **keps/2724-topology-aware-scheduling/README.md:716** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:44:38Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                   | preferred         | required          | unconstrained     |
+| ---------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                     | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated)  | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed (deprecated)             | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity (deprecated) | LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
```

  Uhm, I see. That makes sense. I just want to leave a note in the KEP that we currently expect the BestFit works well in most situations, but we might make the possibility for other deprecated scheduling based on user feedback.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-12T09:47:01Z

- **keps/2724-topology-aware-scheduling/README.md:691** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:47:01Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
```

  Sorry what I wanted to ask is about "but tries to optimize the last domain to leave as few free resources as possible."
  
  Does this "last domain" mean lowest level, right? In that case, this BesiFit algorithm behavior is mostly LeastFreeCapacity in the case of flat topology (only nodeLabel)?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T10:00:43Z

- **keps/2724-topology-aware-scheduling/README.md:716** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T10:00:43Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
+
+Selection of the algorithm depends on TAS profiles expressed by feature gates, and PodSet's annotation:
+
+| featuregate/annotation                   | preferred         | required          | unconstrained     |
+| ---------------------------------------- | ----------------- | ----------------- | ----------------- |
+| None                                     | BestFit           | BestFit           | BestFit           |
+| TASProfileMostFreeCapacity (deprecated)  | MostFreeCapacity  | MostFreeCapacity  | MostFreeCapacity  |
+| TASProfileMixed (deprecated)             | BestFit           | BestFit           | LeastFreeCapacity |
+| TASProfileLeastFreeCapacity (deprecated) | LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
```

  +1 to what mimowo said. I'll add clarification

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T10:03:39Z

- **keps/2724-topology-aware-scheduling/README.md:691** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T10:03:39Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
```

  No, the last domain means the last domain to choose at any given level. Imagine you want to schedule 7 pods and nodes with free capacity that can fit 3,3,2 and 1 pod. In that case both `BestFit` algorithm and `MostFreeCapacity` algorithm choose first two nodes with 3 available pods. The difference occurs when selecting the last domain:
  - `BestFit` will choose a node that can fit 1 pod, as it's enough
  - `MostFreeCapacity` will choose a node that can fit 2 pods, as it's the one with most free capacity

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T10:10:02Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T10:10:02Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't require pod-to-pod communication
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  Yes, bool is expected value. The difference between this annotation and no annotation is that with TAS we mitigate the risk of admitting a job when capacity is fragmented. Imagine you have 2 GPUs scattered across 2 nodes, and you want to schedule a single pod that requires. Without TAS (and this annotation) a job would be falsely admitted even it cannot run.
  
  A natural question may appear: why not setting just `preferred:hostname`? The difference between those two is that with preferred TAS try to pack pods as tighly as possible. While it's valuable for workloads that require heave pod-to-pod communication, that's not always the case, and a user might prefer to place pods on nodes scattered across the whole datacenter e.g. to reduce fragmentation

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T10:45:21Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T10:45:21Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't require pod-to-pod communication
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  Added clarification

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T10:54:08Z

- **keps/2724-topology-aware-scheduling/README.md:691** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T10:54:08Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
```

  Clarified, and added example as well

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T05:14:24Z

Mostly, lgtm
just question

- **keps/2724-topology-aware-scheduling/README.md:441** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T05:14:16Z
```diff
@@ -430,6 +431,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't need low-latency or high-throughput pod-to-pod communication,
+  // but want to leverage TAS capabilities improve accuracy of admitting jobs
+  //
+  // +kubebuilder:validation:Type=boolean
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  Does this mean that all Pods need to have either annotations (`podset-required-topology`, `podset-preferred-topology` or `PodSetUnconstrainedTopologyAnnotation`)?
  
  My previous question is what are differences no TAS annotation and `PodSetUnconstrainedTopologyAnnotation `.
  It looks like both (no TAS annotation and PodSetUnconstrainedTopologyAnnotation) are same.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T05:15:07Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T05:15:06Z
```diff
@@ -430,6 +430,11 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't require pod-to-pod communication
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  Thank you. I added a clarified question to another thread.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T05:15:18Z

- **keps/2724-topology-aware-scheduling/README.md:691** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T05:15:18Z
```diff
@@ -663,11 +685,23 @@ For a given PodSet Kueue:
   level. Kueue starts the search from the specified level, but if the PodSet
   does not fit, then it tries higher levels in the hierarchy.
 
-Kueue packs pods on domains starting from the domains with the most free capacity. However, Kueue can operate in two modes when it comes to choosing the last domain if there is more than one capable of accommodating the remaining pods:
-- `MostAllocated` - Kueue chooses the domain with the least available resource that is capable of accommodating all the pods to mitigate resource fragmentation
-- `LeastAllocated` - Kueue chooses the domain that has the most available resources, providing better nodes utilization
+Kueue places pods on domains with different algorithms, depending on the annotation and chosen profile:
+- `MostFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity;
+- `LeastFreeCapacity` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the least free capacity;
+- `BestFit` algorithm - Kueue selects as many domains as needed (if it meets user's requirement) starting from the one with the most free capacity, but tries to optimize the last domain to leave as few free resources as possible.
```

  Thank you, looks great.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-13T08:46:18Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@PBundyra](https://github.com/PBundyra) — 2025-03-13T08:46:18Z
```diff
@@ -430,6 +431,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't need low-latency or high-throughput pod-to-pod communication,
+  // but want to leverage TAS capabilities improve accuracy of admitting jobs
+  //
+  // +kubebuilder:validation:Type=boolean
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  In Kueue v0.10.0 PodSet needs to have one of annotation (required/preferred) in order to use TAS. The same goes with the unconstrained annotation, if a PodSet wants to leverage TAS it needs to have some annotation defines. So the difference between no annotation and the unconstrained annotation would be to whether or not use TAS. Using TAS even if there are no heavy pod-to-pod communication might be beneficial to mitigate resource fragmentation.
  
  Please note that in Kueue v0.11.0 mimowo introduces a mechanism to implicitly default the annotation (to `preferred`: [lowest level]) if all RFs in a CQ has Topology defined. This KEP takes it into account, as we allow a user to change the implicit default (to `unconstrained`)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T09:44:08Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T09:44:07Z
```diff
@@ -430,6 +431,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't need low-latency or high-throughput pod-to-pod communication,
+  // but want to leverage TAS capabilities improve accuracy of admitting jobs
+  //
+  // +kubebuilder:validation:Type=boolean
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  > In Kueue v0.10.0 PodSet needs to have one of annotation (required/preferred) in order to use TAS. The same goes with the unconstrained annotation, if a PodSet wants to leverage TAS it needs to have some annotation defines. So the difference between no annotation and the unconstrained annotation would be to whether or not use TAS. Using TAS even if there are no heavy pod-to-pod communication might be beneficial to mitigate resource fragmentation.
  
  Thank you for clarifying. So, `unconstrained` will just check node allocatable resources, but not consider any topology (including nodeLabel), right? It sounds like TAS without topology. haha
  
  The `unconstrained` looks like just scheduling a simulation. It sounds like a first step of https://github.com/kubernetes-sigs/kueue/issues/3755.
  
  Thank you!
  /lgtm
  /approve

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-13T09:48:28Z

- **keps/2724-topology-aware-scheduling/README.md:441** by [@PBundyra](https://github.com/PBundyra) — 2025-03-13T09:48:27Z
```diff
@@ -430,6 +431,14 @@ const (
   // at the highest topology level, then it gets admitted as distributed
   // among multiple topology domains.
   PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
+
+  // PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+  // Kueue admits the PodSet if there's enough free capacity available.
+  // Recommended for PodSets that don't need low-latency or high-throughput pod-to-pod communication,
+  // but want to leverage TAS capabilities improve accuracy of admitting jobs
+  //
+  // +kubebuilder:validation:Type=boolean
+  PodSetUnconstrainedTopologyAnnotation = "kueue.x-k8s.io/podset-unconstrained-topology"
```

  /hold cancel
  Thank you!
