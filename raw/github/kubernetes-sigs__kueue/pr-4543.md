# PR #4543: Expose the TopologyName in LocalQueue status

**Summary**: Expose the TopologyName in LocalQueue status

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4543

**Last updated**: 2025-03-13T07:15:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/expose-topology-name-in-localqueue-status` → `main`
- **Created**: 2025-03-10T14:21:08Z
- **Updated**: 2025-03-13T07:15:24Z
- **Merged**: 2025-03-13T06:57:47Z
- **Closed**: 2025-03-13T06:57:47Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 13   **+310 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Expose the TopologyName in LocalQueue status

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4534

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Expose the TopologyName in LocalQueue status
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-10T14:21:25Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 7378507ab25b1f261cf6855a9ef21cb93a70a061 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d26ee8e9765b0008d6ebb7 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-11T08:54:59Z

/retest

Due to #4551

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-11T08:58:25Z

/lgtm
/approve
Thanks! I think the ability for users which LQs use TAS will be quite valuable.
/hold
leaving final tagging to @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-11T08:58:31Z

LGTM label has been added.  <details>Git tree hash: 1df1e04e7685a5f52064043d91a89ae5754ba858</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T05:05:29Z

@tenzen-y, do you have any comments here?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-13T05:45:20Z

LGTM label has been added.  <details>Git tree hash: f4efd0e93b9aa07a4768b90ca48fae4493944483</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-13T05:45:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4543#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4543#issuecomment-2713218258" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4543#pullrequestreview-2680620837" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T05:48:57Z

/unhold

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T06:44:16Z

/retest

Due to #4569

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-10T15:26:20Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@kannon92](https://github.com/kannon92) — 2025-03-10T15:26:20Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  This seems to be mixing alpha and beta apis, no?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-10T15:44:31Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-10T15:44:31Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  We already used TopologyReference on ResourceFlavor:
  
  https://github.com/kubernetes-sigs/kueue/blob/2bea78f84cd7430fcb104f07d447148094a4afd9/apis/kueue/v1beta1/resourceflavor_types.go#L98-L103

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-10T15:48:12Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@kannon92](https://github.com/kannon92) — 2025-03-10T15:48:11Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  :crying_cat_face: 
  
  So we are planning on dropping alpha APIs from Kueue in our Kueue managed project. We were thinking that we would drop TAS CRDs and not set the feature gate.
  
  What would happen if we add these references in the beta APIs? Are these no-opts since TAS is not defined?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-10T16:00:27Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-10T16:00:27Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  > 😿
  > 
  > So we are planning on dropping alpha APIs from Kueue in our Kueue managed project. We were thinking that we would drop TAS CRDs and not set the feature gate.
  > 
  > What would happen if we add these references in the beta APIs? Are these no-opts since TAS is not defined?
  
  Not sure I understand the issue. We will move TAS CRDs to v1beta1 - nothing changes. Is there another way to link ResourceFlavor with the v1alpha1 Topology resource?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-10T16:02:51Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@kannon92](https://github.com/kannon92) — 2025-03-10T16:02:51Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  I am trying to understand if one does not install the TAS CRDs, will there still be a dependency on resourceflavors/LocalQueues to an alpha feature?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-10T16:03:26Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@kannon92](https://github.com/kannon92) — 2025-03-10T16:03:26Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  We want to be able to drop all alpha CRDs from Kueue and still expect the v1beta1 APIs to be functional and not reference the alpha APIs?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T16:16:44Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@mimowo](https://github.com/mimowo) — 2025-03-10T16:16:43Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  We don't reference the alpha API in any strong way. The "TopologyReference" is just an alias to string. If you delete Topology CRD the system will continue to run.
  Sometimes, we need to extend more mature API (like LQ or RF) with a field referencing alpha API. The same happens for the core k8s. For example, the Pod's ResourceRequirements is extended with alpha (now beta) ResourceClaims (see [here](https://github.com/kubernetes/kubernetes/blob/b82260f00370bb77814d9c1f0a88d7a5724984bc/pkg/apis/core/types.go#L2497)). What we could add a comment on the field to say this is an "// +featureGate=TopologyAwareScheduling", analogously as done for ResourceClaim? wdyt @kannon92 ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-10T16:17:32Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-10T16:17:32Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  I don't think we'll encounter any problems. If the feature gate is disabled, this field should be nil.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-10T16:26:07Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-10T16:26:07Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  > What we could add a comment on the field to say this is an "// +featureGate=TopologyAwareScheduling", analogously as done for ResourceClaim?
  
  This is a good point.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-10T16:42:51Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@kannon92](https://github.com/kannon92) — 2025-03-10T16:42:51Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  I'm happy with a feature gate flag but I think there may be code in Kueue required to handle that marker?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-10T16:43:08Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@kannon92](https://github.com/kannon92) — 2025-03-10T16:43:08Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  I don't really know what +featureGate=TopologyAwareScheduling does actually..

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T16:47:37Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@mimowo](https://github.com/mimowo) — 2025-03-10T16:47:37Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  > I'm happy with a feature gate flag but I think there may be code in Kueue required to handle that marker?
  
  Not based on LQ, this is purely informational API field at this point.
  
  Note that all fhe fields here (NodeLabels and NodeTaints) also play informational role for users who don't have access to read cluster-scoped objects only available to admin, such as RF or CQ.
  
  For context, it may help to check: https://github.com/kubernetes-sigs/kueue/issues/3122

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T16:51:49Z

- **pkg/cache/cache.go:769** by [@mimowo](https://github.com/mimowo) — 2025-03-10T16:51:49Z
```diff
@@ -766,6 +766,7 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 				if rf, ok := c.resourceFlavors[rgFlavor]; ok {
 					flavor.NodeLabels = rf.Spec.NodeLabels
 					flavor.NodeTaints = rf.Spec.NodeTaints
+					flavor.TopologyName = rf.Spec.TopologyName
```

  Please copy it conditionally, depending on TopologyAwareScheduling feature gate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T16:55:20Z

- **apis/kueue/v1beta1/localqueue_types.go:77** by [@mimowo](https://github.com/mimowo) — 2025-03-10T16:55:20Z
```diff
@@ -73,6 +73,10 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topologyName is the name of the topology associated with this ResourceFlavor.
```

  > I don't really know what +featureGate=TopologyAwareScheduling does actually..
  
  We can add more details like:
  ```
  // topologyName is the name of the Topology referenced by the ResourceFlavor.
  // The field is populated when the TopologyAwareScheduling feature gate is enabled.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-10T17:01:48Z

- **pkg/cache/cache.go:769** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-10T17:01:48Z
```diff
@@ -766,6 +766,7 @@ func (c *Cache) LocalQueueUsage(qObj *kueue.LocalQueue) (*LocalQueueUsageStats,
 				if rf, ok := c.resourceFlavors[rgFlavor]; ok {
 					flavor.NodeLabels = rf.Spec.NodeLabels
 					flavor.NodeTaints = rf.Spec.NodeTaints
+					flavor.TopologyName = rf.Spec.TopologyName
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T17:52:40Z

- **apis/kueue/v1beta1/localqueue_types.go:92** by [@mimowo](https://github.com/mimowo) — 2025-03-10T17:52:40Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
+}
+
+type Topology struct {
+	// name is the name of the topology.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	Name *TopologyReference `json:"name,omitempty"`
```

  ```suggestion
  	Name TopologyReference `json:"name"`
  ```
  No need for the ptr as we introduced the Topology struct.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-10T17:55:08Z

- **test/integration/singlecluster/tas/tas_test.go:363** by [@mimowo](https://github.com/mimowo) — 2025-03-10T17:55:08Z
```diff
@@ -356,6 +357,20 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("should expose the TopologyName in LocalQueue status", func() {
+				levels := slices.Map(topology.Spec.Levels, func(level *kueuealpha.TopologyLevel) string {
+					return level.NodeLabel
+				})
```

  IIUC we can use utiltas.Levels instead? Here and in the other test.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-11T08:01:38Z

- **test/integration/singlecluster/tas/tas_test.go:363** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-11T08:01:38Z
```diff
@@ -356,6 +357,20 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("should expose the TopologyName in LocalQueue status", func() {
+				levels := slices.Map(topology.Spec.Levels, func(level *kueuealpha.TopologyLevel) string {
+					return level.NodeLabel
+				})
```

  Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-11T08:34:00Z

- **apis/kueue/v1beta1/localqueue_types.go:92** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-11T08:34:00Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
+}
+
+type Topology struct {
+	// name is the name of the topology.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	Name *TopologyReference `json:"name,omitempty"`
```

  Thanks! Done.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-11T08:34:07Z

- **test/integration/singlecluster/tas/tas_test.go:363** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-11T08:34:07Z
```diff
@@ -356,6 +357,20 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("should expose the TopologyName in LocalQueue status", func() {
+				levels := slices.Map(topology.Spec.Levels, func(level *kueuealpha.TopologyLevel) string {
+					return level.NodeLabel
+				})
```

  Done

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-11T18:26:37Z

- **apis/kueue/v1beta1/localqueue_types.go:83** by [@mwielgus](https://github.com/mwielgus) — 2025-03-11T18:26:36Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
```

  Why is this needed? The info is available in ResourceFlavor which is referenced couple fields above.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T18:35:28Z

- **apis/kueue/v1beta1/localqueue_types.go:83** by [@mimowo](https://github.com/mimowo) — 2025-03-11T18:35:27Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
```

  ResourceFlavors and Topologies are cluster-scoped resources (rather than namespace-scoped), and are not visible to end-users. 
  
  This is basically the same motivation as for the KEP which introduced LocalQueueFlavorStatus: https://github.com/kubernetes-sigs/kueue/issues/3122, [KEP](https://github.com/kubernetes-sigs/kueue/tree/main/keps/3122-expose-flavors-in-localqueue-status)

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-12T14:27:42Z

- **apis/kueue/v1beta1/localqueue_types.go:83** by [@mwielgus](https://github.com/mwielgus) — 2025-03-12T14:27:42Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
```

  I understand that part. While I understand why ClusterQueues may not be readable for users, i don't see any point in hiding ResourceFlavors. That builds more features in this weird design where we dump spec of one object into the status of the other one. 
  
  While, for the sake of completeness and consistency with the previous decisions, we may accept the change,  I would propose to take a really close look at this part of the api when working on v1beta2 or GA.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T14:32:30Z

- **apis/kueue/v1beta1/localqueue_types.go:83** by [@mimowo](https://github.com/mimowo) — 2025-03-12T14:32:30Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
```

  I agree it is worth re-visiting the topic and alternative approaches might be possible, like a dedicated LQ subresource. However, I'm leaning to accept the change as is for the sake of completeness and providing the information to the end-users in 0.11.
  
  Added an item to the v1beta2 wishlist: https://github.com/kubernetes-sigs/kueue/issues/768#issuecomment-2718095633

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T05:29:20Z

Mostly, lgtm
Thank you

- **apis/kueue/v1beta1/localqueue_types.go:83** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T05:20:24Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
```

  > I understand that part. While I understand why ClusterQueues may not be readable for users, i don't see any point in hiding ResourceFlavors. That builds more features in this weird design where we dump spec of one object into the status of the other one.
  > 
  > While, for the sake of completeness and consistency with the previous decisions, we may accept the change, I would propose to take a really close look at this part of the api when working on v1beta2 or GA.
  
  I agree with you. Basically, LocalQueue is not a place where we expose cluster-scoped information.
  I guess we need to consider more abstraction in the next API version.

- **apis/kueue/v1beta1/localqueue_types.go:82** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T05:23:30Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
```

  I'm not sure of the reason why we need this one.
  Previously, Aldo and I discussed this API feature gate. But this actually does not do anything, which means that even if we add this marker, the removal of these fields will bring an API break.

- **apis/kueue/v1beta1/localqueue_types.go:101** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-13T05:25:12Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
+}
+
+type Topology struct {
+	// name is the name of the topology.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	Name TopologyReference `json:"name"`
+
+	// levels define the levels of topology.
+	//
+	// +required
+	// +listType=atomic
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=8
+	Levels []string `json:"levels,omitempty"`
```

  ```suggestion
  	Levels []string `json:"levels"`
  ```
  
  Why do we make this omitempty? It looks weird since this is a required field, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T05:41:30Z

- **apis/kueue/v1beta1/localqueue_types.go:82** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T05:41:30Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
```

  Removed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T05:41:46Z

- **apis/kueue/v1beta1/localqueue_types.go:101** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T05:41:46Z
```diff
@@ -73,6 +73,32 @@ type LocalQueueFlavorStatus struct {
 	// +kubebuilder:validation:MaxItems=8
 	// +optional
 	NodeTaints []corev1.Taint `json:"nodeTaints,omitempty"`
+
+	// topology is the topology that associated with this ResourceFlavor.
+	//
+	// This is an alpha field and requires enabling the TopologyAwareScheduling
+	// feature gate.
+	//
+	// +featureGate=TopologyAwareScheduling
+	// +optional
+	Topology *Topology `json:"topology,omitempty"`
+}
+
+type Topology struct {
+	// name is the name of the topology.
+	//
+	// +required
+	// +kubebuilder:validation:Required
+	Name TopologyReference `json:"name"`
+
+	// levels define the levels of topology.
+	//
+	// +required
+	// +listType=atomic
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=8
+	Levels []string `json:"levels,omitempty"`
```

  Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-13T05:45:15Z

Thank you
/lgtm
/approve
