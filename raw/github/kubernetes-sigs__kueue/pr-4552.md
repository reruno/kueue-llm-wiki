# PR #4552: Add development docs regarding testing

**Summary**: Add development docs regarding testing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4552

**Last updated**: 2025-03-14T08:30:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@nasedil](https://github.com/nasedil)
- **Branch**: `docs-test` → `main`
- **Created**: 2025-03-11T09:33:29Z
- **Updated**: 2025-03-14T08:30:27Z
- **Merged**: 2025-03-14T08:29:47Z
- **Closed**: 2025-03-14T08:29:47Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mszadkow](https://github.com/mszadkow), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: _none_
- **Changed files**: 2   **+186 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Adds docs regarding running and debugging unit, integration and e2e tests in Kueue.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4432

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-11T09:33:39Z

Hi @nasedil. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-11T09:33:53Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 43df6b07ef9abb0874c02ea962c8e0806b3b3f71 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d2f77f824b770008913fea |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-4552--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-4552--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTQ1NTItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.G5d5O6QTt-sxHgjpYm275t9ITuAnxx6wwVGXZGyq3cU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-4552--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-11T11:29:18Z

/ok-to-test

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-12T08:21:20Z

btw great work so far @nasedil

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:50:00Z

> cc @mbobrovskyi @kannon92 @tenzen-y could you give it a pass too?

Landing here; thanks for the reminder.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-14T07:58:18Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-14T07:58:25Z

LGTM label has been added.  <details>Git tree hash: 7f998346c8378d3c849b9a4d84446be95eb7a200</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-14T08:05:43Z

/approve
I believe this is just the first iteration. As we have a starting point contributors will expand it with their "tips & tricks". Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-14T08:05:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4552#issuecomment-2723927510" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4552#" title="Author self-approved">nasedil</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-14T08:06:05Z

/cherry-pick website
Since all of this is applicable to previous releases.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-03-14T08:06:07Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4552#issuecomment-2723928115):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-14T08:09:54Z

@nasedil Thank you for this contribution!
Could you address https://github.com/kubernetes-sigs/kueue/pull/4552#discussion_r1991107257 in a follow-up PR?

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-03-14T08:30:26Z

@mimowo: new pull request created: #4604

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4552#issuecomment-2723928115):

>/cherry-pick website
>Since all of this is applicable to previous releases.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T09:51:20Z

- **site/content/en/docs/contribution guidelines/testing.md:26** by [@mimowo](https://github.com/mimowo) — 2025-03-11T09:51:20Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```
+make test
+```
+
+To run unit tests for webhooks:
+```
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests many times
```

  ```suggestion
  #### Running unit tests with race detection
  ```
  running many times could be achieved without stress by adding `-c 100` to `go test`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T10:00:27Z

- **site/content/en/docs/contribution guidelines/testing.md:61** by [@mimowo](https://github.com/mimowo) — 2025-03-11T09:57:06Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```
+make test
+```
+
+To run unit tests for webhooks:
+```
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
```

  Actually this is something we may want to read from env. variable like `INTEGRATION_VLEVEL`, but we can leave it for follow up.

- **site/content/en/docs/contribution guidelines/testing.md:116** by [@mimowo](https://github.com/mimowo) — 2025-03-11T10:00:08Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```
+make test
+```
+
+To run unit tests for webhooks:
+```
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+### Running e2e tests
+```
+# build an image
+export IMAGE_REGISTRY=<image registry to which you have push access>
+make image-local-push
+# in the output you can image path that looks like the following:
+# <image registry>/kueue:<tag>
+# or, you can get it by combining $IMAGE_REGISTRY:`git describe --tags --dirty --always`
+# pull that image:
+docker pull <image registry>/kueue:<tag>
+# run e2e test:
+make test-e2e
+# make test-tas-e2e
+# make test-multikueue-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+### Running subset of integration or e2e tests
+#### Use Ginkgo --focus arg
+```
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+#### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+#### Use INTEGRATION_TARGET
+```
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+### Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```
```

  Adding "bash" or "sh" after the three apostrophes would probably add dedicated syntax coloring (same for all other bash commands), no?

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-11T10:39:59Z

- **site/content/en/docs/contribution guidelines/testing.md:61** by [@nasedil](https://github.com/nasedil) — 2025-03-11T10:39:59Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```
+make test
+```
+
+To run unit tests for webhooks:
+```
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
```

  @mimowo  should I make an issue for that?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T11:05:50Z

- **site/content/en/docs/contribution guidelines/testing.md:61** by [@mimowo](https://github.com/mimowo) — 2025-03-11T11:05:50Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```
+make test
+```
+
+To run unit tests for webhooks:
+```
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
```

  Yes please

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T11:33:10Z

- **site/content/en/docs/contribution guidelines/testing.md:2** by [@mimowo](https://github.com/mimowo) — 2025-03-11T11:33:09Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
```

  I would suggest moving this page out of Contribution Guidelines to "Tasks -> Developer Tools -> Running tests". I think we can safely skip "Kueue" since this is the name of the project.

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-11T11:34:45Z

- **site/content/en/docs/contribution guidelines/testing.md:61** by [@nasedil](https://github.com/nasedil) — 2025-03-11T11:34:45Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```
+make test
+```
+
+To run unit tests for webhooks:
+```
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
```

  added #4555

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T11:34:45Z

cc @mbobrovskyi @kannon92 @tenzen-y could you give it a pass too?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T11:44:41Z

- **site/content/en/docs/contribution guidelines/testing.md:50** by [@mimowo](https://github.com/mimowo) — 2025-03-11T11:44:41Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+### Running e2e tests
```

  ```suggestion
  ### Running e2e tests using custom build
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-11T11:44:54Z

- **site/content/en/docs/contribution guidelines/testing.md:141** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-11T11:44:54Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+### Running e2e tests
+```shell
+# build an image
+export IMAGE_REGISTRY=<image registry to which you have push access>
+make image-local-push
+# in the output you can image path that looks like the following:
+# <image registry>/kueue:<tag>
+# or, you can get it by combining $IMAGE_REGISTRY:`git describe --tags --dirty --always`
+# pull that image:
+docker pull <image registry>/kueue:<tag>
+# run e2e test:
+make test-e2e
+# make test-tas-e2e
+# make test-multikueue-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+### Running subset of integration or e2e tests
+#### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+#### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+#### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+### Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--race --focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+#### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments:
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-integration
```

  We already have race detection on integration tests

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-11T11:50:18Z

- **site/content/en/docs/tasks/dev/testing.md:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-11T11:50:18Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+### Running e2e tests
+```shell
+# build an image
+export IMAGE_REGISTRY=<image registry to which you have push access>
+make image-local-push
+# in the output you can image path that looks like the following:
+# <image registry>/kueue:<tag>
+# or, you can get it by combining $IMAGE_REGISTRY:`git describe --tags --dirty --always`
+# pull that image:
+docker pull <image registry>/kueue:<tag>
+# run e2e test:
+make test-e2e
+# make test-tas-e2e
+# make test-multikueue-e2e
+```
```

  Instead of this it's much easier to use `make kind-image-build test-e2e`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T11:52:10Z

- **site/content/en/docs/tasks/dev/testing.md:77** by [@mimowo](https://github.com/mimowo) — 2025-03-11T11:52:06Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+### Running e2e tests
+```shell
+# build an image
+export IMAGE_REGISTRY=<image registry to which you have push access>
+make image-local-push
+# in the output you can image path that looks like the following:
+# <image registry>/kueue:<tag>
+# or, you can get it by combining $IMAGE_REGISTRY:`git describe --tags --dirty --always`
+# pull that image:
+docker pull <image registry>/kueue:<tag>
+# run e2e test:
+make test-e2e
+# make test-tas-e2e
+# make test-multikueue-e2e
+```
```

  Or we may add a bit more details:
  
  Step 1:
  ```sh
  make kind-image-build
  ```
  Step 2 against a new cluster:
  ```sh
  IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:<image version> CREATE_KIND_CLUSTER=true E2E_K8S_VERSIONS=1.32.0 make test-e2e
  ```
  Or, step 2 against a pre-existing cluster:
  ```sh
  kind load docker-image us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:<image version> --name kind
  IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:<image version> CREATE_KIND_CLUSTER=true E2E_K8S_VERSIONS=1.32.0 make test-e2e
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-11T11:52:38Z

- **site/content/en/docs/tasks/dev/testing.md:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-11T11:52:35Z
  Also it would be great to mention `make verify`.

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-11T11:56:26Z

- **site/content/en/docs/contribution guidelines/testing.md:141** by [@nasedil](https://github.com/nasedil) — 2025-03-11T11:56:26Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+### Running e2e tests
+```shell
+# build an image
+export IMAGE_REGISTRY=<image registry to which you have push access>
+make image-local-push
+# in the output you can image path that looks like the following:
+# <image registry>/kueue:<tag>
+# or, you can get it by combining $IMAGE_REGISTRY:`git describe --tags --dirty --always`
+# pull that image:
+docker pull <image registry>/kueue:<tag>
+# run e2e test:
+make test-e2e
+# make test-tas-e2e
+# make test-multikueue-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+### Running subset of integration or e2e tests
+#### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+#### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+#### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+### Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--race --focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+#### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments:
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-integration
```

  does it make sense to run with --race for e2e tests? Or it's not applicable and I should delete the section?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-11T14:17:15Z

- **site/content/en/docs/contribution guidelines/testing.md:141** by [@mimowo](https://github.com/mimowo) — 2025-03-11T14:17:14Z
```diff
@@ -0,0 +1,157 @@
+---
+title: "Running tests in Kueue"
+linkTitle: "Running tests in Kueue"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running tests in Kueue
+
+### Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+#### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+### Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+#### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+### Running e2e tests
+```shell
+# build an image
+export IMAGE_REGISTRY=<image registry to which you have push access>
+make image-local-push
+# in the output you can image path that looks like the following:
+# <image registry>/kueue:<tag>
+# or, you can get it by combining $IMAGE_REGISTRY:`git describe --tags --dirty --always`
+# pull that image:
+docker pull <image registry>/kueue:<tag>
+# run e2e test:
+make test-e2e
+# make test-tas-e2e
+# make test-multikueue-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+### Running subset of integration or e2e tests
+#### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+#### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+#### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+### Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--race --focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+#### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments:
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-integration
```

  I'm fine either way, it is a detail.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T06:26:20Z

- **site/content/en/docs/tasks/dev/testing.md:2** by [@mimowo](https://github.com/mimowo) — 2025-03-12T06:26:20Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
```

  ```suggestion
  title: "Running and debugging tests"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T06:26:34Z

- **site/content/en/docs/tasks/dev/testing.md:6** by [@mimowo](https://github.com/mimowo) — 2025-03-12T06:26:34Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
```

  ```suggestion
    Running and debugging tests
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T06:26:52Z

LGTM overall

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T07:00:47Z

- **site/content/en/docs/tasks/dev/testing.md:49** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T06:39:38Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
```

  ```suggestion
  \`\`\`shell
  make test-integration
  \`\`\`
  
  ```

- **site/content/en/docs/tasks/dev/testing.md:46** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T06:54:39Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
```

  Can we split this block into two parts: one with race detection and one with stress?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T07:20:07Z

- **site/content/en/docs/tasks/dev/testing.md:69** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T07:12:23Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
```

  ```suggestion
  For integration tests, an additional step is needed.  Run `ENVTEST_K8S_VERSION=1.32 make envtest && ./bin/setup-envtest use $ENVTEST_K8S_VERSION -p path` and add the path to the variable in settings.json:
  ```

- **site/content/en/docs/tasks/dev/testing.md:84** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T07:17:28Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
```

  ```suggestion
  `ginkgo.It` to `ginkgo.FIt` for these tests. 
  For more details, see [here](https://onsi.github.io/ginkgo/#focused-specs).
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T07:21:45Z

- **site/content/en/docs/tasks/dev/testing.md:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T07:21:45Z
  It would be great to mention how to change `E2E_KIND_VERSION`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T07:26:27Z

- **site/content/en/docs/tasks/dev/testing.md:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T07:26:27Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments for e2e tests (on integration tests it's already used):
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-e2e
+```
```

  I'm not sure how it is useful for e2e tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T07:29:34Z

- **site/content/en/docs/tasks/dev/testing.md:134** by [@mimowo](https://github.com/mimowo) — 2025-03-12T07:29:34Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments for e2e tests (on integration tests it's already used):
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-e2e
+```
```

  yeah, I haven't actually used it for E2e tests in the past. let's drop until proven useful.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T07:35:19Z

- **site/content/en/docs/tasks/dev/testing.md:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T07:35:18Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
```

  Also we need to mention how to test other e2e tests
  
  ```suggestion
  make kind-image-build 
  make test-e2e 
  make test-tas-e2e 
  make test-e2e-customconfigs 
  make test-multikueue-e2e
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T07:38:00Z

- **site/content/en/docs/tasks/dev/testing.md:95** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T07:38:00Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
```

  If we are testing kueuectl tests, we need to mention that the `KUEUE_BIN` variable must be set.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T07:41:05Z

- **site/content/en/docs/tasks/dev/testing.md:146** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T07:41:05Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
```

  I think this command won't work.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-12T08:09:56Z

- **site/content/en/docs/tasks/dev/testing.md:69** by [@mszadkow](https://github.com/mszadkow) — 2025-03-12T08:09:56Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
```

  this way it's faster :)

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-12T08:16:00Z

- **site/content/en/docs/tasks/dev/testing.md:135** by [@mszadkow](https://github.com/mszadkow) — 2025-03-12T08:16:00Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
```

  I don't think the script is needed, as we can use ginkgo related decorators and flags e.g:
  * MustPassRepeatedly
  * FlakeAttempts
  More here: https://onsi.github.io/ginkgo/#repeating-spec-runs-and-managing-flaky-specs

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-12T08:18:58Z

- **site/content/en/docs/tasks/dev/testing.md:134** by [@mszadkow](https://github.com/mszadkow) — 2025-03-12T08:18:58Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments for e2e tests (on integration tests it's already used):
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-e2e
+```
```

  Really useful is actually to attach to existing kind cluster, usually what I do is to comment the last line of hack/*e2e-tst.sh (run make test-*e2e) and later run tests with Ginkgo Tests plugin in VS code.
  We might want to help ourselves in the future and maybe provide a flag to do it without modifying script every time ;)

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-12T09:03:14Z

- **site/content/en/docs/tasks/dev/testing.md:134** by [@nasedil](https://github.com/nasedil) — 2025-03-12T09:03:14Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments for e2e tests (on integration tests it's already used):
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-e2e
+```
```

  > Really useful is actually to attach to existing kind cluster, usually what I do is to comment the last line of hack/*e2e-tst.sh (run make test-*e2e) and later run tests with Ginkgo Tests plugin in VS code. We might want to help ourselves in the future and maybe provide a flag to do it without modifying script every time ;)
  
  I'm not sure I understood.  Can you provide an example?  I've tried to run Ginkgo tests from VSCode, but then I got errors about `etcd` missing.

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-12T09:24:40Z

- **site/content/en/docs/tasks/dev/testing.md:146** by [@nasedil](https://github.com/nasedil) — 2025-03-12T09:24:40Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
```

  @mbobrovskyi  do you mean it's useless in context of stress-testing integration/e2e tests?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T09:40:08Z

- **site/content/en/docs/tasks/dev/testing.md:146** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T09:40:08Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
```

  Oh, I see. It's another utility. Please ignore my comment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-12T10:05:15Z

Thanks for that

- **site/content/en/docs/tasks/dev/testing.md:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:52:02Z
  This documentation is for Kueue developper one. So, could we move this to https://kueue.sigs.k8s.io/docs/contribution-guidelines/?
  
  I guess we can create `ContributionGuide` folder, and then move this `testing` to there.
  @mimowo Any thoughts?

- **site/content/en/docs/tasks/dev/testing.md:54** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:56:31Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
```

  ```suggestion
  For running a subset of tests, see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
  ```
  
  nit

- **site/content/en/docs/tasks/dev/testing.md:79** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T09:57:33Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
```

  ```suggestion
  For running a subset of tests, see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
  ```
  
  nit

- **site/content/en/docs/tasks/dev/testing.md:144** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-12T10:03:51Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
```

  Does this indicate https://packages.ubuntu.com/noble/stress (https://github.com/resurrecting-open-source-projects/stress)?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T06:17:02Z

- **site/content/en/docs/tasks/dev/testing.md:46** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T06:17:02Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
```

  I think we missed this?

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-13T10:00:15Z

- **site/content/en/docs/tasks/dev/testing.md:46** by [@nasedil](https://github.com/nasedil) — 2025-03-13T10:00:15Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
```

  @mbobrovskyi I pushed the updated version

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-13T10:17:56Z

- **site/content/en/docs/tasks/dev/testing.md:144** by [@nasedil](https://github.com/nasedil) — 2025-03-13T10:17:56Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
```

  @tenzen-y yes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T10:19:36Z

- **site/content/en/docs/tasks/dev/testing.md:1** by [@mimowo](https://github.com/mimowo) — 2025-03-13T10:19:36Z
  Actually, initially it was put under "Contribution guidelines", but I thought "Developer Tools" make sense. 
  
  However, I'm ok either way. Contribution guide seems ok to me too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T10:20:10Z

- **site/content/en/docs/tasks/dev/testing.md:1** by [@mimowo](https://github.com/mimowo) — 2025-03-13T10:20:10Z
  @tenzen-y I'm ok to move the new page under contribution guidelines, are you ok with that?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-13T10:47:53Z

- **site/content/en/docs/tasks/dev/testing.md:134** by [@mszadkow](https://github.com/mszadkow) — 2025-03-13T10:47:52Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments for e2e tests (on integration tests it's already used):
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-e2e
+```
```

  Ok, let's do an example.
  We want to run MultiKueue tests, but we don't want to do it by hand.
  Let's use our `hack/multikueue-e2e-test.sh` script, so all dependencies are installed.
  First, go to `hack/multikueue-e2e-test.sh` and comment out last line - `$GINKGO $GINKGO_ARGS --junit-report=junit.xml --json-report=e2e.json --output-dir="$ARTIFACTS" -v ./test/e2e/multikueue/...
  `
  and instead put there some kind of a stop e.g. `read -p "run your tests now"`.
  Make sure your vs-code `.vscode/settings.json` has at least those envs:
  ```
   "ginkgotestexplorer.testEnvVars": {
          "KIND_CLUSTER_NAME": "kind",
          "WORKER1_KIND_CLUSTER_NAME": "kind-worker1",
          "MANAGER_KIND_CLUSTER_NAME": "kind-manager",
          "WORKER2_KIND_CLUSTER_NAME": "kind-worker2",
          "KIND": "<kueue_path>/bin/kind",
      },
  ```
  Now run `make kind-image-build test-multikueue-e2e` and wait until `"run your tests now"` appears.
  Then you have running multi cluster kind, spawn another terminal.
  Now you can run you tests here like this:
  ```
  <kueue_path>/bin/ginkgo --json-report ./ginkgo.report -focus "MultiKueue when Creating a multikueue admission check Should run a jobSet on worker if admitted"   -r
  ```
  
  Or use vs-code ginkgo plugin [Test Explorer](https://marketplace.visualstudio.com/items?itemName=joselitofilho.ginkgotestexplorer).
  ![image](https://github.com/user-attachments/assets/9d229419-f386-4680-9c3a-3802e1b762f5)

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-13T10:48:15Z

- **site/content/en/docs/tasks/dev/testing.md:134** by [@mszadkow](https://github.com/mszadkow) — 2025-03-13T10:48:15Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments for e2e tests (on integration tests it's already used):
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-e2e
+```
```

  Let me know if that worked for you

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-13T12:57:54Z

- **site/content/en/docs/tasks/dev/testing.md:134** by [@nasedil](https://github.com/nasedil) — 2025-03-13T12:57:54Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
+# run stress alongside tests
+/usr/bin/stress --cpu 80
+```
+
+And you can add --race to Ginkgo arguments for e2e tests (on integration tests it's already used):
+```shell
+GINKGO_ARGS="--race --focus=Scheduler" make test-e2e
+```
```

  Thanks, it worked, at least from commandline, updated the docs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T14:32:52Z

- **site/content/en/docs/contribution guidelines/_index.md:16** by [@mimowo](https://github.com/mimowo) — 2025-03-13T14:32:52Z
```diff
@@ -13,7 +13,7 @@ _As contributors and maintainers of this project, and in the interest of fosteri
 ## Getting Started
 
 We have full documentation on how to get started contributing here:
-
+- [Running and debugging tests](/docs/contribution\ guidelines/testing.md)
```

  I would suggest it as the last point

### Review by [@nasedil](https://github.com/nasedil) (commented) — 2025-03-13T15:20:21Z

- **site/content/en/docs/tasks/dev/testing.md:1** by [@nasedil](https://github.com/nasedil) — 2025-03-13T15:20:21Z
  I moved it back to contribution guidelines

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-14T08:07:13Z

- **site/content/en/docs/tasks/dev/testing.md:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-14T08:07:12Z
  Thank you. I'm ok with that!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-14T08:09:35Z

- **site/content/en/docs/tasks/dev/testing.md:144** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-14T08:09:35Z
```diff
@@ -0,0 +1,149 @@
+---
+title: "Running tests"
+linkTitle: "Running tests"
+weight: 25
+description: >
+  Running tests in Kueue
+---
+
+## Running presubmission verification tests
+```shell
+make verify
+```
+
+## Running unit tests
+To run all unit tests:
+```shell
+make test
+```
+
+To run unit tests for webhooks:
+```shell
+go test ./pkg/webhooks
+```
+To run tests that match `TestValidateClusterQueue` regular expression [ref](https://pkg.go.dev/cmd/go#hdr-Testing_flags):
+```shell
+go test ./pkg/webhooks -run TestValidateClusterQueue
+```
+
+### Running unit tests with race detection
+
+```shell
+# install go stress
+go install golang.org/x/tools/cmd/stress@latest
+# recompile with race instrumentation
+go test ./pkg/scheduler/preemption/ -race -c
+# it loops and reports failures
+stress ./preemption.test -test.run TestPreemption
+```
+
+## Running integration tests
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+### Increase logging verbosity
+You can change log level in the framework (for example, change -3 to -5 to increase verbosity):
+https://github.com/kubernetes-sigs/kueue/blob/f8015cb273f9115c34f9be32b35f7e1308c16459/test/integration/framework/framework.go#L72:
+```go
+var setupLogger = sync.OnceFunc(func() {
+	ctrl.SetLogger(util.NewTestingLogger(ginkgo.GinkgoWriter, -3))
+})
+```
+
+## Running e2e tests using custom build
+```shell
+make kind-image-build test-e2e
+```
+
+For running subset of tests see [Running subset of tests](#running-subset-of-integration-or-e2e-tests).
+
+## Debug tests in VSCode
+It is possible to debug unit and integration tests in VSCode.
+You need to have the [Go extension](https://marketplace.visualstudio.com/items?itemName=golang.Go) installed.
+Now you will have `run test | debug test` text buttons above lines like
+```go
+func TestValidateClusterQueue(t *testing.T) {
+```
+You can click on the `debug test` to debug a specific test.
+
+For integration tests, an additional step is needed.  Run `make test-integration | grep KUBEBUILDER_ASSETS` and add the path to the variable in settings.json:
+```json
+"go.testEnvVars": {
+    "KUBEBUILDER_ASSETS": "<path from output above>",
+  },
+```
+
+## Running subset of integration or e2e tests
+### Use Ginkgo --focus arg
+```shell
+GINKGO_ARGS="--focus=Scheduler" make test-integration
+GINKGO_ARGS="--focus=Creating a Pod requesting TAS" make test-e2e
+```
+### Use ginkgo.FIt
+If you want to focus on a specific tests, you can change
+`ginkgo.It` to `ginkgo.FIt` for these tests [ref][https://onsi.github.io/ginkgo/#focused-specs].
+Then the other tests will be skipped.
+For example, you can change
+```go
+ginkgo.It("Should place pods based on the ranks-ordering", func() {
+```
+to
+```go
+ginkgo.FIt("Should place pods based on the ranks-ordering", func() {
+```
+and then run
+```shell
+# build and pull image
+make test-tas-e2e
+```
+to test a particular TAS e2e test.
+
+### Use INTEGRATION_TARGET
+```shell
+INTEGRATION_TARGET='test/integration/singlecluster/scheduler' make test-integration
+```
+
+## Flaky integration/e2e tests
+You can use a script to repeatedly test:
+```shell
+echo "going to test $NUM_TEST_ITERATIONS times"
+for i in $(seq 1 $NUM_TEST_ITERATIONS); do
+  GINKGO_ARGS="--focus=Scheduler" make test-integration >> loop-test.log 2>&1
+  echo iteration $i
+  grep -q "FAIL!" loop-test.log
+  if [ $? -eq 0 ]; then
+    echo "Test run contains FAILED tests"
+    exit 1
+  fi
+done
+echo "looped the test $NUM_TEST_ITERATIONS times"
+```
+
+### Adding stress
+You can run `stress` to increase CPU load during tests.
+```shell
+# install stress, for example with
+sudo apt install stress
```

  Not all developers use Linux machines (as an alternative to MacOS). So, it would be nice to mention the tool link.
  Could you add the tool link here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T08:11:54Z

/lgtm
Thanks!
