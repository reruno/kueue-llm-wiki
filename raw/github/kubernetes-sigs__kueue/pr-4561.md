# PR #4561: Cohort Fair Sharing Status and Metrics.

**Summary**: Cohort Fair Sharing Status and Metrics.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4561

**Last updated**: 2025-03-19T13:17:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/cohort-fair-sharing-status-and-metrics` → `main`
- **Created**: 2025-03-12T06:21:36Z
- **Updated**: 2025-03-19T13:17:39Z
- **Merged**: 2025-03-17T13:07:50Z
- **Closed**: 2025-03-17T13:07:50Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 22   **+581 / -67**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Cohort Fair Sharing Status and Metrics

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4554

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
[FSxHC] Add Cohort Fair Sharing Status and Metrics
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-12T06:21:39Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-12T06:22:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 190f414fe85edc69ef86455949584d367bf0da57 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d80eab6055c100081db57f |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-4561--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-4561--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTQ1NjEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.L8rU38AAC3kdHTo6qt_SWgOXlJaWAcRdt4nPCPk2cO8)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-4561--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T12:12:59Z

/retest

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T12:22:36Z

Great work :)

/lgtm
/assign @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-14T12:22:43Z

LGTM label has been added.  <details>Git tree hash: 5b934726f16a0701f2b045901913dbd95a3d8eee</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T12:13:29Z

> Great work :)
> 
> /lgtm /assign @mimowo

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-17T12:13:34Z

LGTM label has been added.  <details>Git tree hash: f31444450f0d8a229102912fb079e6538f7b9cc9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-17T12:43:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4561#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4561#discussion_r1998651516" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-19T13:17:37Z

/release-note-edit
```release-note
[FSxHC] Add Cohort Fair Sharing Status and Metrics
```

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-12T08:43:41Z

- **apis/kueue/v1alpha1/cohort_types.go:73** by [@gabesaba](https://github.com/gabesaba) — 2025-03-12T08:39:41Z
```diff
@@ -64,8 +64,26 @@ type CohortSpec struct {
 	FairSharing *kueuebeta.FairSharing `json:"fairSharing,omitempty"`
 }
 
+type CohortStatus struct {
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharing *FairSharingStatus `json:"fairSharing,omitempty"`
+}
+
+type FairSharingStatus struct {
```

  Reuse the beta type?

- **pkg/cache/cache.go:701** by [@gabesaba](https://github.com/gabesaba) — 2025-03-12T08:41:07Z
```diff
@@ -688,6 +688,25 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
```

  is there any chance this will return nil? if so, check and return err. If not, remove error from return value

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T10:20:09Z

- **apis/kueue/v1alpha1/cohort_types.go:73** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T10:20:09Z
```diff
@@ -64,8 +64,26 @@ type CohortSpec struct {
 	FairSharing *kueuebeta.FairSharing `json:"fairSharing,omitempty"`
 }
 
+type CohortStatus struct {
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharing *FairSharingStatus `json:"fairSharing,omitempty"`
+}
+
+type FairSharingStatus struct {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-12T10:20:18Z

- **pkg/cache/cache.go:701** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-12T10:20:18Z
```diff
@@ -688,6 +688,25 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
```

  Done

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T09:20:41Z

- **pkg/cache/cache.go:741** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:12:51Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
+	c.RLock()
+	defer c.RUnlock()
+
+	return c.cohortAncestorsWithoutLock(cohortName)
+}
+
+func (c *Cache) cohortAncestorsWithoutLock(cohortName kueue.CohortReference) []kueue.CohortReference {
+	cohort := c.hm.Cohort(cohortName)
+	if cohort == nil || !cohort.HasParent() {
+		return nil
+	}
+
+	parent := cohort.Parent()
+
+	var ancestors []kueue.CohortReference
+	if !slices.Contains(ancestors, parent.Name) {
+		ancestors = append(ancestors, parent.Name)
+		ancestors = append(ancestors, c.cohortAncestorsWithoutLock(parent.Name)...)
+	}
+
+	return ancestors
+}
```

  Needs a cycle check. What do you think of this?
  
  ```suggestion
  func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) ([]kueue.CohortReference, error) {
  	c.RLock()
  	defer c.RUnlock()
  	cohort := c.hm.Cohort(cohortName)
  	if cohort == nil {
  		return nil, nil
  	} else if c.hm.CycleChecker.HasCycle(cohort) {
  		return nil, ERR
  	}
  
  	var ancestors []kueue.CohortReference
  	for cohort != nil {
  		ancestors = append(ancestors, cohort.Name)
  		cohort = cohort.Parent()
  	}
  	return ancestors, nil
  }
  ```

- **pkg/cache/cache.go:715** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:13:40Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
```

  this should also return the Cohort it was called with. the interface/name may be a little clearer if we call it ClusterQueueAncestors, and call it with a CQ?

- **pkg/cache/cache.go:715** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:13:47Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
```

  add a comment please

- **pkg/controller/core/cohort_controller.go:134** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:15:48Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  what is the motivation for the move? My understanding of Reconciler is these functions (Create/Update/Delete) is just a filter, and we shouldn't be executing logic in it (I know we do this for other reconcilers, maybe can fix at some point)

- **pkg/controller/core/cohort_controller.go:198** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:16:42Z
```diff
@@ -92,23 +146,87 @@ func (r *CohortReconciler) Generic(e event.GenericEvent) bool {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=cohorts,verbs=get;list;watch;create;update;patch;delete
 
 func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	log := ctrl.LoggerFrom(ctx)
-	log.V(2).Info("Reconcile Cohort")
-
 	var cohort kueue.Cohort
 	if err := r.client.Get(ctx, req.NamespacedName, &cohort); err != nil {
-		if apierrors.IsNotFound(err) {
-			log.V(2).Info("Cohort is being deleted")
-			r.cache.DeleteCohort(v1beta1.CohortReference(req.NamespacedName.Name))
-			r.qManager.DeleteCohort(v1beta1.CohortReference(req.NamespacedName.Name))
-		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Cohort")
+
 	log.V(2).Info("Cohort is being created or updated", "resources", cohort.Spec.ResourceGroups)
 	if err := r.cache.AddOrUpdateCohort(&cohort); err != nil {
 		log.V(2).Error(err, "Error adding or updating cohort in the cache")
 	}
 	r.qManager.AddOrUpdateCohort(ctx, &cohort)
-	return ctrl.Result{}, nil
+
+	err := r.updateCohortStatusIfChanged(ctx, &cohort)
+	return ctrl.Result{}, err
+}
+
+func (r *CohortReconciler) updateCohortStatusIfChanged(ctx context.Context, cohort *kueue.Cohort) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	oldStatus := cohort.Status.DeepCopy()
+
+	stats, err := r.cache.CohortUsage(cohort)
+	if err != nil {
+		log.Error(err, "Failed getting cohort usage from cache")
+		// This is likely because the cohort was recently removed,
+		// but we didn't process that event yet.
+		return err
+	}
+
+	if r.fairSharingEnabled {
+		metrics.ReportCohortWeightedShare(cohort.Name, stats.WeightedShare)
+		if cohort.Status.FairSharing == nil {
+			cohort.Status.FairSharing = &v1beta1.FairSharingStatus{}
+		}
+		cohort.Status.FairSharing.WeightedShare = stats.WeightedShare
+	} else {
+		cohort.Status.FairSharing = nil
+	}
+
+	if !equality.Semantic.DeepEqual(cohort.Status, oldStatus) {
+		return r.client.Status().Update(ctx, cohort)
+	}
+
+	return nil
+}
+
+func (r *CohortReconciler) NotifyClusterQueueUpdate(oldCQ, newCQ *v1beta1.ClusterQueue) {
+	// if newTopology is nil, it's a delete event.
```

  newTopology?

- **pkg/cache/cache.go:697** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:17:40Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
```

  CohortStats?

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:20:17Z
```diff
@@ -295,8 +296,140 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqLeft            *kueue.ClusterQueue
+			cqRight           *kueue.ClusterQueue
+			lqLeft            *kueue.LocalQueue
+			lqRight           *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqLeft = testing.MakeClusterQueue("left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqLeft)).To(gomega.Succeed())
+
+			cqRight = testing.MakeClusterQueue("right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqRight)).To(gomega.Succeed())
+
+			lqLeft = testing.MakeLocalQueue("left", ns.Name).ClusterQueue(cqLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqLeft)).To(gomega.Succeed())
+
+			lqRight = testing.MakeLocalQueue("right", ns.Name).ClusterQueue(cqRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("Admits workloads respecting fair share", func() {
```

  how about adding workloads to each CQ in a round robin fashion? Then, we should see even fair sharing scores, at each level of the hierarchy

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T09:42:50Z

- **pkg/cache/cache.go:715** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T09:42:50Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
```

  > this should also return the Cohort it was called with.
  
  Not sure I got why we need to return current cohort?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T09:58:23Z

- **pkg/controller/core/cohort_controller.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T09:58:23Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  > what is the motivation for the move?
  
  When executing CohortAncestors() we can return a cohort without an object, and in this case, we’ll remove it from the cache since it’s unnecessary.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T10:01:22Z

- **pkg/controller/core/cohort_controller.go:198** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T10:01:22Z
```diff
@@ -92,23 +146,87 @@ func (r *CohortReconciler) Generic(e event.GenericEvent) bool {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=cohorts,verbs=get;list;watch;create;update;patch;delete
 
 func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	log := ctrl.LoggerFrom(ctx)
-	log.V(2).Info("Reconcile Cohort")
-
 	var cohort kueue.Cohort
 	if err := r.client.Get(ctx, req.NamespacedName, &cohort); err != nil {
-		if apierrors.IsNotFound(err) {
-			log.V(2).Info("Cohort is being deleted")
-			r.cache.DeleteCohort(v1beta1.CohortReference(req.NamespacedName.Name))
-			r.qManager.DeleteCohort(v1beta1.CohortReference(req.NamespacedName.Name))
-		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Cohort")
+
 	log.V(2).Info("Cohort is being created or updated", "resources", cohort.Spec.ResourceGroups)
 	if err := r.cache.AddOrUpdateCohort(&cohort); err != nil {
 		log.V(2).Error(err, "Error adding or updating cohort in the cache")
 	}
 	r.qManager.AddOrUpdateCohort(ctx, &cohort)
-	return ctrl.Result{}, nil
+
+	err := r.updateCohortStatusIfChanged(ctx, &cohort)
+	return ctrl.Result{}, err
+}
+
+func (r *CohortReconciler) updateCohortStatusIfChanged(ctx context.Context, cohort *kueue.Cohort) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	oldStatus := cohort.Status.DeepCopy()
+
+	stats, err := r.cache.CohortUsage(cohort)
+	if err != nil {
+		log.Error(err, "Failed getting cohort usage from cache")
+		// This is likely because the cohort was recently removed,
+		// but we didn't process that event yet.
+		return err
+	}
+
+	if r.fairSharingEnabled {
+		metrics.ReportCohortWeightedShare(cohort.Name, stats.WeightedShare)
+		if cohort.Status.FairSharing == nil {
+			cohort.Status.FairSharing = &v1beta1.FairSharingStatus{}
+		}
+		cohort.Status.FairSharing.WeightedShare = stats.WeightedShare
+	} else {
+		cohort.Status.FairSharing = nil
+	}
+
+	if !equality.Semantic.DeepEqual(cohort.Status, oldStatus) {
+		return r.client.Status().Update(ctx, cohort)
+	}
+
+	return nil
+}
+
+func (r *CohortReconciler) NotifyClusterQueueUpdate(oldCQ, newCQ *v1beta1.ClusterQueue) {
+	// if newTopology is nil, it's a delete event.
```

  Oops, copy-paste :)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T10:05:09Z

- **pkg/cache/cache.go:741** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T10:05:09Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
+	c.RLock()
+	defer c.RUnlock()
+
+	return c.cohortAncestorsWithoutLock(cohortName)
+}
+
+func (c *Cache) cohortAncestorsWithoutLock(cohortName kueue.CohortReference) []kueue.CohortReference {
+	cohort := c.hm.Cohort(cohortName)
+	if cohort == nil || !cohort.HasParent() {
+		return nil
+	}
+
+	parent := cohort.Parent()
+
+	var ancestors []kueue.CohortReference
+	if !slices.Contains(ancestors, parent.Name) {
+		ancestors = append(ancestors, parent.Name)
+		ancestors = append(ancestors, c.cohortAncestorsWithoutLock(parent.Name)...)
+	}
+
+	return ancestors
+}
```

  This is good optimization. Thanks!

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T10:06:09Z

- **pkg/controller/core/cohort_controller.go:134** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T10:06:09Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  A race condition? Can we just ignore this Cohort, since another reconcile should cleanup that Cohort anyway from Cache (right?)?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T10:07:53Z

- **pkg/cache/cache.go:715** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T10:07:53Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
```

  ahh, well in the callsite you can get rid of reconcile request for cq.Cohort (which could be empty string anyway), and just process all parent Cohorts of the CQ in one loop?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T10:35:25Z

- **pkg/cache/cache.go:715** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T10:35:25Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
```

  In that case we need to change name. Something like CohortAndAncestors(). WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T10:48:40Z

- **pkg/controller/core/cohort_controller.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T10:48:40Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  Not exactly. We have a `kueuealpha.Cohort` called "cohort" with a parent "root", which is just a string and doesn’t have any resources. CohortAncestors() returns `[]string{"root"}`, but now the Reconciler deletes it from the cache, which isn’t entirely correct.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T11:17:18Z

- **pkg/cache/cache.go:715** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T11:17:17Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
```

  > the interface/name may be a little clearer if we call it ClusterQueueAncestors, and call it with a CQ?
  
  what about this?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T11:30:33Z

- **pkg/controller/core/cohort_controller.go:134** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T11:30:32Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  Ahh, this is the implicit cohort mechanism? In this case, what if we don't return the root from ancestors (and document this in the function comment)
  
  As the root will not have a FairSharing value anyway: https://github.com/kubernetes-sigs/kueue/blob/a454f0e133682887c8043b10cc360bc24f7d3924/pkg/cache/fair_sharing.go#L48-L50

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T11:38:03Z

- **pkg/controller/core/cohort_controller.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T11:38:03Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  > Ahh, this is the implicit cohort mechanism? In this case, what if we don't return the root from ancestors (and document this in the function comment)
  
  But what if root is really `kueuealpha.Cohort` object? Not sure how to check it on cache.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-13T11:41:49Z

- **pkg/cache/cache.go:715** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-13T11:41:49Z
```diff
@@ -688,6 +690,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortUsage(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+func (c *Cache) CohortAncestors(cohortName kueue.CohortReference) []kueue.CohortReference {
```

  Ah, I got what you mean. Thanks for explanation!

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T13:13:31Z

- **pkg/controller/core/cohort_controller.go:134** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T13:13:31Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  In both cases, the FairSharing score isn't relevant for this Cohort = it is always 0 - so I think we can skip triggering the reconcile
  
  At some point this Cohort will be reconciled, and it will be given a score of 0 - so that should be consistent from the UI perspective

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T05:38:22Z

- **pkg/controller/core/cohort_controller.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T05:38:22Z
```diff
@@ -82,6 +126,16 @@ func (r *CohortReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *CohortReconciler) Delete(e event.DeleteEvent) bool {
+	cohort, ok := e.Object.(*kueue.Cohort)
+	if !ok {
+		return false
+	}
+
+	log := r.log.WithValues("cohort", klog.KObj(cohort))
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T06:23:55Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T06:23:55Z
```diff
@@ -295,8 +296,140 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqLeft            *kueue.ClusterQueue
+			cqRight           *kueue.ClusterQueue
+			lqLeft            *kueue.LocalQueue
+			lqRight           *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqLeft = testing.MakeClusterQueue("left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqLeft)).To(gomega.Succeed())
+
+			cqRight = testing.MakeClusterQueue("right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqRight)).To(gomega.Succeed())
+
+			lqLeft = testing.MakeLocalQueue("left", ns.Name).ClusterQueue(cqLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqLeft)).To(gomega.Succeed())
+
+			lqRight = testing.MakeLocalQueue("right", ns.Name).ClusterQueue(cqRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("Admits workloads respecting fair share", func() {
```

  Done

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T10:46:22Z

only some minor nits left. looks good overall :)

- **pkg/cache/cache.go:722** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T10:37:35Z
```diff
@@ -688,6 +689,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortStats(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+// ClusterQueueAncestors returns all ancestors (Cohorts), excluding the root,
+// for a given ClusterQueue.
+func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) []kueue.CohortReference {
+	c.RLock()
+	defer c.RUnlock()
+
+	if len(cqObj.Spec.Cohort) == 0 {
```

  ```suggestion
  	if cqObj.Spec.Cohort == "" {
  ```

- **pkg/cache/cache.go:738** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T10:38:13Z
```diff
@@ -688,6 +689,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortStats(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+// ClusterQueueAncestors returns all ancestors (Cohorts), excluding the root,
+// for a given ClusterQueue.
+func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) []kueue.CohortReference {
+	c.RLock()
+	defer c.RUnlock()
+
+	if len(cqObj.Spec.Cohort) == 0 {
+		return nil
+	}
+
+	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
+	if cohort == nil {
+		return nil
+	}
+
+	var ancestors []kueue.CohortReference
+	for cohort != nil && cohort.HasParent() {
+		ancestors = append(ancestors, cohort.Name)
+		cohort = cohort.Parent()
+	}
```

  ```suggestion
  	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
  	var ancestors []kueue.CohortReference
  	for cohort != nil && cohort.HasParent() {
  		ancestors = append(ancestors, cohort.Name)
  		cohort = cohort.Parent()
  	}
  ```

- **pkg/controller/core/cohort_controller.go:167** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T10:41:46Z
```diff
@@ -110,5 +150,71 @@ func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctr
 		log.V(2).Error(err, "Error adding or updating cohort in the cache")
 	}
 	r.qManager.AddOrUpdateCohort(ctx, &cohort)
-	return ctrl.Result{}, nil
+
+	err := r.updateCohortStatusIfChanged(ctx, &cohort)
+	return ctrl.Result{}, err
+}
+
+func (r *CohortReconciler) updateCohortStatusIfChanged(ctx context.Context, cohort *kueue.Cohort) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	oldStatus := cohort.Status.DeepCopy()
+
+	stats, err := r.cache.CohortStats(cohort)
+	if err != nil {
+		log.Error(err, "Failed getting cohort usage from cache")
+		// This is likely because the cohort was recently removed,
+		// but we didn't process that event yet.
```

  I don't think this is accurate, as we will only delete Cohort in Reconcile loop, which we are currently in

- **apis/kueue/v1alpha1/cohort_types.go:68** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T10:45:20Z
```diff
@@ -64,8 +64,15 @@ type CohortSpec struct {
 	FairSharing *kueuebeta.FairSharing `json:"fairSharing,omitempty"`
 }
 
+type CohortStatus struct {
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
```

  delete please; then the doc will be generated using v1beta1.FairSharing's comment, I believe

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T11:19:02Z

- **apis/kueue/v1beta1/fairsharing_types.go:38** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T11:19:02Z
```diff
@@ -35,6 +35,7 @@ type FairSharing struct {
 	Weight *resource.Quantity `json:"weight,omitempty"`
 }
 
+// fairSharing contains the information about the current status of fair sharing.
```

  I'm not sure about the naming here - I understand why you did this but maybe it should be consistent with the type.
  
  wdyt @mimowo @tenzen-y

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T11:32:15Z

- **pkg/cache/cache.go:722** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T11:32:14Z
```diff
@@ -688,6 +689,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortStats(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+// ClusterQueueAncestors returns all ancestors (Cohorts), excluding the root,
+// for a given ClusterQueue.
+func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) []kueue.CohortReference {
+	c.RLock()
+	defer c.RUnlock()
+
+	if len(cqObj.Spec.Cohort) == 0 {
```

  this reverted

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T11:36:57Z

- **apis/kueue/v1beta1/fairsharing_types.go:38** by [@mimowo](https://github.com/mimowo) — 2025-03-14T11:36:57Z
```diff
@@ -35,6 +35,7 @@ type FairSharing struct {
 	Weight *resource.Quantity `json:"weight,omitempty"`
 }
 
+// fairSharing contains the information about the current status of fair sharing.
```

  looking at other comments we should probably align with the type name indeed: https://github.com/kubernetes-sigs/kueue/pull/4561/files#diff-fa7799c26041d219ab624bc2069198280537236008ff41d199593618b900dcb9L255.
  
  Also, this is named like this in the core k8s: https://github.com/kubernetes/kubernetes/blob/af3b4cd57a5e3a1dbdf9115b7ac71fcc309c7722/pkg/apis/core/types.go#L2956-L2957, even though the type is referenecd by "Conditions" field. You never know where someone references from your type.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T11:43:00Z

- **pkg/controller/core/cohort_controller.go:167** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T11:43:00Z
```diff
@@ -110,5 +150,71 @@ func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctr
 		log.V(2).Error(err, "Error adding or updating cohort in the cache")
 	}
 	r.qManager.AddOrUpdateCohort(ctx, &cohort)
-	return ctrl.Result{}, nil
+
+	err := r.updateCohortStatusIfChanged(ctx, &cohort)
+	return ctrl.Result{}, err
+}
+
+func (r *CohortReconciler) updateCohortStatusIfChanged(ctx context.Context, cohort *kueue.Cohort) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	oldStatus := cohort.Status.DeepCopy()
+
+	stats, err := r.cache.CohortStats(cohort)
+	if err != nil {
+		log.Error(err, "Failed getting cohort usage from cache")
+		// This is likely because the cohort was recently removed,
+		// but we didn't process that event yet.
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T11:43:04Z

- **apis/kueue/v1alpha1/cohort_types.go:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T11:43:04Z
```diff
@@ -64,8 +64,15 @@ type CohortSpec struct {
 	FairSharing *kueuebeta.FairSharing `json:"fairSharing,omitempty"`
 }
 
+type CohortStatus struct {
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T11:52:53Z

- **apis/kueue/v1beta1/fairsharing_types.go:38** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T11:52:52Z
```diff
@@ -35,6 +35,7 @@ type FairSharing struct {
 	Weight *resource.Quantity `json:"weight,omitempty"`
 }
 
+// fairSharing contains the information about the current status of fair sharing.
```

  Not sure about it. I think we need to set with lower case due to on generation we should see with lower case.
  
  https://github.com/kubernetes-sigs/kueue/blob/f56055462247e124b5de5d9e7104d826b0ed7069/charts/kueue/templates/crd/kueue.x-k8s.io_cohorts.yaml#L244-L248

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T12:21:57Z

- **pkg/cache/cache.go:722** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T12:21:57Z
```diff
@@ -688,6 +689,52 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortStats(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+// ClusterQueueAncestors returns all ancestors (Cohorts), excluding the root,
+// for a given ClusterQueue.
+func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) []kueue.CohortReference {
+	c.RLock()
+	defer c.RUnlock()
+
+	if len(cqObj.Spec.Cohort) == 0 {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T12:46:32Z

- **pkg/cache/cache.go:738** by [@mimowo](https://github.com/mimowo) — 2025-03-14T12:46:32Z
```diff
@@ -688,6 +690,56 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortStats(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+// ClusterQueueAncestors returns all ancestors (Cohorts), excluding the root,
+// for a given ClusterQueue. If the ClusterQueue contains a Cohort cycle, it
+// returns ErrCohortHasCycle.
+func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.CohortReference, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	if cqObj.Spec.Cohort == "" {
+		return nil, nil
+	}
+
+	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
+	if cohort == nil {
+		return nil, nil
+	}
+	if c.hm.CycleChecker.HasCycle(cohort) {
+		return nil, ErrCohortHasCycle
+	}
+
+	var ancestors []kueue.CohortReference
+	for cohort != nil && cohort.HasParent() {
+		ancestors = append(ancestors, cohort.Name)
+		cohort = cohort.Parent()
+	}
```

  non-blocking comment: It is one of the things we could have a helper function for, but it could be a follow up.
  Similar loop was done in the FS scheduling algorithm.
  
  cc @gabesaba

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T12:50:02Z

- **pkg/controller/core/cohort_controller.go:163** by [@mimowo](https://github.com/mimowo) — 2025-03-14T12:50:02Z
```diff
@@ -110,5 +150,74 @@ func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctr
 		log.V(2).Error(err, "Error adding or updating cohort in the cache")
 	}
 	r.qManager.AddOrUpdateCohort(ctx, &cohort)
-	return ctrl.Result{}, nil
+
+	err := r.updateCohortStatusIfChanged(ctx, &cohort)
+	return ctrl.Result{}, err
+}
+
+func (r *CohortReconciler) updateCohortStatusIfChanged(ctx context.Context, cohort *kueue.Cohort) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	oldStatus := cohort.Status.DeepCopy()
+
+	stats, err := r.cache.CohortStats(cohort)
+	if err != nil {
+		log.Error(err, "Failed getting cohort usage from cache")
```

  Would it log the cohort key (name / ns) in question? Probably worth adding this to the logger values.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T12:52:40Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mimowo](https://github.com/mimowo) — 2025-03-14T12:52:40Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  Can you please run this test locally in a loop say 50 times, just to ensure it is not flaky? We have recently many flakes, and I want to be extra cautious about adding more before release.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T12:53:08Z

lgtm, left minor comments

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T13:05:51Z

- **pkg/controller/core/cohort_controller.go:163** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T13:05:51Z
```diff
@@ -110,5 +150,74 @@ func (r *CohortReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctr
 		log.V(2).Error(err, "Error adding or updating cohort in the cache")
 	}
 	r.qManager.AddOrUpdateCohort(ctx, &cohort)
-	return ctrl.Result{}, nil
+
+	err := r.updateCohortStatusIfChanged(ctx, &cohort)
+	return ctrl.Result{}, err
+}
+
+func (r *CohortReconciler) updateCohortStatusIfChanged(ctx context.Context, cohort *kueue.Cohort) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	oldStatus := cohort.Status.DeepCopy()
+
+	stats, err := r.cache.CohortStats(cohort)
+	if err != nil {
+		log.Error(err, "Failed getting cohort usage from cache")
```

  Yes, we have this information. This log from context.
  
  ```
  2025-03-14T15:04:35.173342+02:00	ERROR	core/cohort_controller.go:164	Failed getting cohort usage from cache	{"controller": "cohort", "controllerGroup": "kueue.x-k8s.io", "controllerKind": "Cohort", "Cohort": {"name":"root"}, "namespace": "", "name": "root", "reconcileID": "e80cf601-8791-4bec-ad4e-0db5abb6ead0"}
  
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T14:53:30Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T14:53:30Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  Caught a race condition while running the stress test:
  
  ```
  WARNING: DATA RACE
  Read at 0x00c000f9c240 by goroutine 331:
    runtime.mapdelete_faststr()
        /Users/mykhailo_bobrovskyi/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.24.0.darwin-arm64/src/internal/runtime/maps/runtime_faststr_swiss.go:396 +0x8c
    sigs.k8s.io/kueue/pkg/hierarchy.(*CycleChecker).HasCycle()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/hierarchy/cycle.go:34 +0x68
    sigs.k8s.io/kueue/pkg/cache.(*Cache).ClusterQueueAncestors()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/cache/cache.go:730 +0x130
    sigs.k8s.io/kueue/pkg/controller/core.(*cohortCqHandler).Generic()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/controller/core/cohort_controller.go:215 +0x74
    sigs.k8s.io/controller-runtime/pkg/source.(*channel[go.shape.b7c155c91576830ee655d804b5e7b1fc9d1b717385cca83dc56c659142a3fa38,go.shape.struct { k8s.io/apimachinery/pkg/types.NamespacedName }]).Start.func2.1()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/vendor/sigs.k8s.io/controller-runtime/pkg/source/source.go:213 +0xac
    sigs.k8s.io/controller-runtime/pkg/source.(*channel[go.shape.b7c155c91576830ee655d804b5e7b1fc9d1b717385cca83dc56c659142a3fa38,go.shape.struct { k8s.io/apimachinery/pkg/types.NamespacedName }]).Start.func2()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/vendor/sigs.k8s.io/controller-runtime/pkg/source/source.go:214 +0xc0
  
  Previous write at 0x00c000f9c240 by goroutine 223:
    runtime.mapaccess2()
        /Users/mykhailo_bobrovskyi/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.24.0.darwin-arm64/src/internal/runtime/maps/runtime_swiss.go:117 +0x2dc
    sigs.k8s.io/kueue/pkg/hierarchy.(*CycleChecker).HasCycle()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/hierarchy/cycle.go:41 +0xcc
    sigs.k8s.io/kueue/pkg/cache.(*Cache).Snapshot()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/cache/snapshot.go:114 +0x2ac
    sigs.k8s.io/kueue/pkg/scheduler.(*Scheduler).schedule()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/scheduler/scheduler.go:191 +0x280
    sigs.k8s.io/kueue/pkg/scheduler.(*Scheduler).schedule-fm()
        <autogenerated>:1 +0x44
    sigs.k8s.io/kueue/pkg/util/wait.untilWithBackoff.func1()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/util/wait/backoff.go:43 +0x54
    k8s.io/apimachinery/pkg/util/wait.BackoffUntil.func1()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/vendor/k8s.io/apimachinery/pkg/util/wait/backoff.go:226 +0x48
    k8s.io/apimachinery/pkg/util/wait.BackoffUntil()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/vendor/k8s.io/apimachinery/pkg/util/wait/backoff.go:227 +0x94
    sigs.k8s.io/kueue/pkg/util/wait.untilWithBackoff()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/util/wait/backoff.go:42 +0x10c
    sigs.k8s.io/kueue/pkg/util/wait.UntilWithBackoff()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/util/wait/backoff.go:34 +0xc8
    sigs.k8s.io/kueue/pkg/scheduler.(*Scheduler).Start.gowrap1()
        /Users/mykhailo_bobrovskyi/Projects/epam/kueue/pkg/scheduler/scheduler.go:146 +0x4c
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T14:54:42Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mimowo](https://github.com/mimowo) — 2025-03-14T14:54:42Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  cc @gabesaba 
  is it a preexisting failure or new?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T14:59:53Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T14:59:53Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  No, this is a new one. The problem is that we are using a regular map in CycleChecker (see [here](https://github.com/kubernetes-sigs/kueue/pull/4561/files#diff-209289cb88a27598728c1fd07d7e264a6e59c94db4da02eeae47e547d9262fccL29-L31)). I've changed it to a SyncMap.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T15:03:35Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mimowo](https://github.com/mimowo) — 2025-03-14T15:03:35Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  Gabe can you double check this part?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T15:04:13Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mimowo](https://github.com/mimowo) — 2025-03-14T15:04:13Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  cc @gabesaba

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-14T15:18:40Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-14T15:18:40Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  @mimowo 
  
  `21m35s: 600 runs so far, 0 failures`

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T15:20:19Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T15:20:19Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  I think it is a harmless data race. Before calling cycleChecker, we are using a read lock, which prevents mutations to the Cohort structure. But, several Cohorts within the same root Cohort tree may be reconciling at the same time, and cycleChecker caches the results. Since the algorithm is deterministic, there will not be a problem.
  
  This overall makes sense to me. Let me think a bit more Monday morning about this - maybe there is a better way to solve than SyncMap

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T15:27:42Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mimowo](https://github.com/mimowo) — 2025-03-14T15:27:42Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  I see, in any case the code should not be panicing. 
  
  Yeah, the downside of sync map is that it penalizes performance of code which is single-threaded like scheduler, but I think this should be fine as the operations are very fast. Anyway I will wait for green light.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-17T09:33:00Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T09:33:00Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  https://github.com/kubernetes-sigs/kueue/pull/4637

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T12:09:08Z

- **pkg/cache/cache.go:738** by [@mimowo](https://github.com/mimowo) — 2025-03-17T12:09:08Z
```diff
@@ -688,6 +690,56 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 	return stats, nil
 }
 
+type CohortUsageStats struct {
+	WeightedShare int64
+}
+
+func (c *Cache) CohortStats(cohortObj *kueuealpha.Cohort) (*CohortUsageStats, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cohort := c.hm.Cohort(kueue.CohortReference(cohortObj.Name))
+	if cohort == nil {
+		return nil, ErrCohortNotFound
+	}
+
+	stats := &CohortUsageStats{}
+	if c.fairSharingEnabled {
+		weightedShare, _ := dominantResourceShare(cohort, nil)
+		stats.WeightedShare = int64(weightedShare)
+	}
+
+	return stats, nil
+}
+
+// ClusterQueueAncestors returns all ancestors (Cohorts), excluding the root,
+// for a given ClusterQueue. If the ClusterQueue contains a Cohort cycle, it
+// returns ErrCohortHasCycle.
+func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.CohortReference, error) {
+	c.RLock()
+	defer c.RUnlock()
+
+	if cqObj.Spec.Cohort == "" {
+		return nil, nil
+	}
+
+	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
+	if cohort == nil {
+		return nil, nil
+	}
+	if c.hm.CycleChecker.HasCycle(cohort) {
+		return nil, ErrCohortHasCycle
+	}
+
+	var ancestors []kueue.CohortReference
+	for cohort != nil && cohort.HasParent() {
+		ancestors = append(ancestors, cohort.Name)
+		cohort = cohort.Parent()
+	}
```

  This is ticketed already: https://github.com/kubernetes-sigs/kueue/issues/4644

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T12:09:37Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mimowo](https://github.com/mimowo) — 2025-03-17T12:09:36Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  @mbobrovskyi can you confirm the test is no longer flaky after rebase?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-17T12:42:14Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-17T12:42:14Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  ```
  17m40s: 500 runs so far, 0 failures
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T12:43:06Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:372** by [@mimowo](https://github.com/mimowo) — 2025-03-17T12:43:05Z
```diff
@@ -295,8 +296,129 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("using hierarchical cohorts", func() {
+		var (
+			cohortFirstLeft   *kueuealpha.Cohort
+			cohortFirstRight  *kueuealpha.Cohort
+			cohortSecondLeft  *kueuealpha.Cohort
+			cohortSecondRight *kueuealpha.Cohort
+			cohortBank        *kueuealpha.Cohort
+			cqSecondLeft      *kueue.ClusterQueue
+			cqSecondRight     *kueue.ClusterQueue
+			lqSecondLeft      *kueue.LocalQueue
+			lqSecondRight     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			//         root
+			//        /    \
+			//       fl     fr
+			//     /   \     \
+			//    sl   sr    bank
+			//    /     \
+			//   cqL    cqR
+
+			rootCohort := testing.MakeCohort("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rootCohort)).To(gomega.Succeed())
+
+			cohortFirstLeft = testing.MakeCohort("first-left").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstLeft)).To(gomega.Succeed())
+
+			cohortFirstRight = testing.MakeCohort("first-right").Parent("root").Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortFirstRight)).To(gomega.Succeed())
+
+			cohortSecondLeft = testing.MakeCohort("second-left").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondLeft)).To(gomega.Succeed())
+
+			cohortSecondRight = testing.MakeCohort("second-right").Parent(kueue.CohortReference(cohortFirstLeft.Name)).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortSecondRight)).To(gomega.Succeed())
+
+			cohortBank = testing.MakeCohort("bank").Parent(kueue.CohortReference(cohortFirstRight.Name)).
+				ResourceGroup(
+					testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "10").FlavorQuotas,
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cohortBank)).To(gomega.Succeed())
+
+			cqSecondLeft = testing.MakeClusterQueue("second-left").
+				Cohort(kueue.CohortReference(cohortSecondLeft.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondLeft)).To(gomega.Succeed())
+
+			cqSecondRight = testing.MakeClusterQueue("second-right").
+				Cohort(kueue.CohortReference(cohortSecondRight.Name)).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "2").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSecondRight)).To(gomega.Succeed())
+
+			lqSecondLeft = testing.MakeLocalQueue("second-left", ns.Name).ClusterQueue(cqSecondLeft.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondLeft)).To(gomega.Succeed())
+
+			lqSecondRight = testing.MakeLocalQueue("second-right", ns.Name).ClusterQueue(cqSecondRight.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqSecondRight)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cqSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortFirstRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondLeft, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortSecondRight, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cohortBank, true)
+		})
+
+		ginkgo.It("admits workloads respecting fair share", func() {
```

  /lgtm
  /approve
  thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-17T13:16:31Z

- **apis/kueue/v1beta1/fairsharing_types.go:38** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-17T13:16:31Z
```diff
@@ -35,6 +35,7 @@ type FairSharing struct {
 	Weight *resource.Quantity `json:"weight,omitempty"`
 }
 
+// fairSharing contains the information about the current status of fair sharing.
```

  Yeah, @mbobrovskyi is right. The generated documentation is the reason. API documentation is referred by users and they use YAML instead of code.
