# PR #4567: TAS: Add the `kueue.x-k8s.io/podset-unconstrained-topology` annotation

**Summary**: TAS: Add the `kueue.x-k8s.io/podset-unconstrained-topology` annotation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4567

**Last updated**: 2025-08-12T10:15:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `TAS-unconstrained` → `main`
- **Created**: 2025-03-12T12:47:26Z
- **Updated**: 2025-08-12T10:15:48Z
- **Merged**: 2025-03-13T12:25:47Z
- **Closed**: 2025-03-13T12:25:47Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 22   **+436 / -127**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR introduces changes described in this KEP #4542 :
- Adds a new annotation
- Extends PodSetTopologyRequest API with .unconstrained field

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #4474 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TopologyAwareScheduling can now be used with `kueue.x-k8s.io/podset-unconstrained-topology` annotation
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-12T12:47:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | eb33d6faebbc05c47868006d2e9e015a61647df7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d2b25ac906b100089192c3 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T12:50:09Z

/assign @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T13:22:34Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-13T09:17:39Z

> 2\. it is not clear to me if the asserts are functionalliy different than "preferred" would have returned. Adding unit tests which show the difference as discussed under the issue would be great, and add a comment why we observe the difference.

Do you mean other than those?
https://github.com/kubernetes-sigs/kueue/pull/4567/files#diff-85a93b2977c9efa55ca4846fdd219e22733e2b736c4fa3dba06f70739c1bbdfbR417-R470
In these pods are assigned on nodes scattered across the whole cluster, while with preferred they would fit into a single rack

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-13T09:38:57Z

> Do you mean other than those?

No, these tests is what I was looking for (I somehow missed them), just not sure about the semantics still, but asked dedicated question: https://github.com/kubernetes-sigs/kueue/pull/4567/files#r1993128268

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-13T12:24:12Z

LGTM label has been added.  <details>Git tree hash: c329b55c9336c1415569166fd21b5cd8b206a88a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-13T12:24:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4567#pullrequestreview-2681693172" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4567#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-13T12:25:08Z

cc @tenzen-y feel free to add some comments post-merge. 

I'm merging as we need to revisit the defaulting in the context of rank-based ordering anyway.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T14:04:49Z

- **charts/kueue/values.yaml:153** by [@mimowo](https://github.com/mimowo) — 2025-03-12T14:04:49Z
```diff
@@ -150,9 +150,7 @@ webhookService:
       protocol: TCP
       targetPort: 9443
   type: ClusterIP
-
```

  nit, revert formatting changes of unrelated files

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T14:05:51Z

- **apis/kueue/v1alpha1/tas_types.go:45** by [@mimowo](https://github.com/mimowo) — 2025-03-12T14:05:51Z
```diff
@@ -38,6 +38,16 @@ const (
 	// among multiple topology domains.
 	PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
 
+	// PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires
+	// Topology Aware Scheduling, but it promotes filling up nodes in use over
+	// compact placement. The pods have lower chance of being scheduled on the same
+	// node, but it mitigates resource fragmentation, and can lead to better
+	// node utilization. Recommended for PodSets that don't require heave inter pod
```

  This seems old, with typos like " require heave inter pod". Please align with KEP.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T14:14:09Z

- **charts/kueue/values.yaml:153** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T14:14:09Z
```diff
@@ -150,9 +150,7 @@ webhookService:
       protocol: TCP
       targetPort: 9443
   type: ClusterIP
-
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T14:14:14Z

- **apis/kueue/v1alpha1/tas_types.go:45** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T14:14:14Z
```diff
@@ -38,6 +38,16 @@ const (
 	// among multiple topology domains.
 	PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
 
+	// PodSetUnconstrainedTopologyAnnotation indicates that a PodSet requires
+	// Topology Aware Scheduling, but it promotes filling up nodes in use over
+	// compact placement. The pods have lower chance of being scheduled on the same
+	// node, but it mitigates resource fragmentation, and can lead to better
+	// node utilization. Recommended for PodSets that don't require heave inter pod
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T14:14:27Z

- **pkg/controller/jobframework/tas.go:50** by [@mimowo](https://github.com/mimowo) — 2025-03-12T14:14:27Z
```diff
@@ -17,27 +17,38 @@ limitations under the License.
 package jobframework
 
 import (
+	"strconv"
+
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
 )
 
 func PodSetTopologyRequest(meta *metav1.ObjectMeta, podIndexLabel *string, subGroupIndexLabel *string, subGroupCount *int32) *kueue.PodSetTopologyRequest {
 	requiredValue, requiredFound := meta.Annotations[kueuealpha.PodSetRequiredTopologyAnnotation]
 	preferredValue, preferredFound := meta.Annotations[kueuealpha.PodSetPreferredTopologyAnnotation]
+	unconstrained, unconstrainedFound := meta.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]
 
-	if requiredFound || preferredFound {
+	if requiredFound || preferredFound || unconstrainedFound || features.Enabled(features.TASImplicitDefaultUnconstrained) {
 		psTopologyReq := &kueue.PodSetTopologyRequest{
 			PodIndexLabel:      podIndexLabel,
 			SubGroupIndexLabel: subGroupIndexLabel,
 			SubGroupCount:      subGroupCount,
 		}
 		if requiredFound {
 			psTopologyReq.Required = &requiredValue
-		} else {
+		} else if preferredFound {
 			psTopologyReq.Preferred = &preferredValue
 		}
+		if unconstrainedFound {
+			unconstrained, _ := strconv.ParseBool(unconstrained)
+			psTopologyReq.Unconstrained = &unconstrained
+		} else if features.Enabled(features.TASImplicitDefaultUnconstrained) {
+			psTopologyReq.Unconstrained = ptr.To(true)
```

  Actually, this makes me wonder that https://github.com/kubernetes-sigs/kueue/pull/4556 has a bug, that the rank-based ordering would not work.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T14:26:38Z

- **pkg/controller/jobframework/tas.go:50** by [@mimowo](https://github.com/mimowo) — 2025-03-12T14:26:38Z
```diff
@@ -17,27 +17,38 @@ limitations under the License.
 package jobframework
 
 import (
+	"strconv"
+
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
 )
 
 func PodSetTopologyRequest(meta *metav1.ObjectMeta, podIndexLabel *string, subGroupIndexLabel *string, subGroupCount *int32) *kueue.PodSetTopologyRequest {
 	requiredValue, requiredFound := meta.Annotations[kueuealpha.PodSetRequiredTopologyAnnotation]
 	preferredValue, preferredFound := meta.Annotations[kueuealpha.PodSetPreferredTopologyAnnotation]
+	unconstrained, unconstrainedFound := meta.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]
 
-	if requiredFound || preferredFound {
+	if requiredFound || preferredFound || unconstrainedFound || features.Enabled(features.TASImplicitDefaultUnconstrained) {
 		psTopologyReq := &kueue.PodSetTopologyRequest{
 			PodIndexLabel:      podIndexLabel,
 			SubGroupIndexLabel: subGroupIndexLabel,
 			SubGroupCount:      subGroupCount,
 		}
 		if requiredFound {
 			psTopologyReq.Required = &requiredValue
-		} else {
+		} else if preferredFound {
 			psTopologyReq.Preferred = &preferredValue
 		}
+		if unconstrainedFound {
+			unconstrained, _ := strconv.ParseBool(unconstrained)
+			psTopologyReq.Unconstrained = &unconstrained
+		} else if features.Enabled(features.TASImplicitDefaultUnconstrained) {
+			psTopologyReq.Unconstrained = ptr.To(true)
```

  However, when the problem is fixed, I wouldn't default it here in a persistent fashion probably since the default is implicit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-12T15:46:07Z

- **pkg/controller/jobframework/tas.go:50** by [@mimowo](https://github.com/mimowo) — 2025-03-12T15:46:07Z
```diff
@@ -17,27 +17,38 @@ limitations under the License.
 package jobframework
 
 import (
+	"strconv"
+
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
 )
 
 func PodSetTopologyRequest(meta *metav1.ObjectMeta, podIndexLabel *string, subGroupIndexLabel *string, subGroupCount *int32) *kueue.PodSetTopologyRequest {
 	requiredValue, requiredFound := meta.Annotations[kueuealpha.PodSetRequiredTopologyAnnotation]
 	preferredValue, preferredFound := meta.Annotations[kueuealpha.PodSetPreferredTopologyAnnotation]
+	unconstrained, unconstrainedFound := meta.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]
 
-	if requiredFound || preferredFound {
+	if requiredFound || preferredFound || unconstrainedFound || features.Enabled(features.TASImplicitDefaultUnconstrained) {
 		psTopologyReq := &kueue.PodSetTopologyRequest{
 			PodIndexLabel:      podIndexLabel,
 			SubGroupIndexLabel: subGroupIndexLabel,
 			SubGroupCount:      subGroupCount,
 		}
 		if requiredFound {
 			psTopologyReq.Required = &requiredValue
-		} else {
+		} else if preferredFound {
 			psTopologyReq.Preferred = &preferredValue
 		}
+		if unconstrainedFound {
+			unconstrained, _ := strconv.ParseBool(unconstrained)
+			psTopologyReq.Unconstrained = &unconstrained
+		} else if features.Enabled(features.TASImplicitDefaultUnconstrained) {
+			psTopologyReq.Unconstrained = ptr.To(true)
```

  Ticketed: https://github.com/kubernetes-sigs/kueue/issues/4573

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T15:54:54Z

- **pkg/controller/jobframework/tas.go:50** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T15:54:54Z
```diff
@@ -17,27 +17,38 @@ limitations under the License.
 package jobframework
 
 import (
+	"strconv"
+
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
 
 	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
 )
 
 func PodSetTopologyRequest(meta *metav1.ObjectMeta, podIndexLabel *string, subGroupIndexLabel *string, subGroupCount *int32) *kueue.PodSetTopologyRequest {
 	requiredValue, requiredFound := meta.Annotations[kueuealpha.PodSetRequiredTopologyAnnotation]
 	preferredValue, preferredFound := meta.Annotations[kueuealpha.PodSetPreferredTopologyAnnotation]
+	unconstrained, unconstrainedFound := meta.Annotations[kueuealpha.PodSetUnconstrainedTopologyAnnotation]
 
-	if requiredFound || preferredFound {
+	if requiredFound || preferredFound || unconstrainedFound || features.Enabled(features.TASImplicitDefaultUnconstrained) {
 		psTopologyReq := &kueue.PodSetTopologyRequest{
 			PodIndexLabel:      podIndexLabel,
 			SubGroupIndexLabel: subGroupIndexLabel,
 			SubGroupCount:      subGroupCount,
 		}
 		if requiredFound {
 			psTopologyReq.Required = &requiredValue
-		} else {
+		} else if preferredFound {
 			psTopologyReq.Preferred = &preferredValue
 		}
+		if unconstrainedFound {
+			unconstrained, _ := strconv.ParseBool(unconstrained)
+			psTopologyReq.Unconstrained = &unconstrained
+		} else if features.Enabled(features.TASImplicitDefaultUnconstrained) {
+			psTopologyReq.Unconstrained = ptr.To(true)
```

  Made defaulting actually implict, done

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-12T15:57:12Z

- **apis/kueue/v1alpha1/tas_types.go:43** by [@mwielgus](https://github.com/mwielgus) — 2025-03-12T15:57:12Z
```diff
@@ -38,6 +38,14 @@ const (
 	// among multiple topology domains.
 	PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
 
+	// PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+	// Kueue admits the PodSet if there's enough free capacity available.
+	// Recommended for PodSets that don't require pod-to-pod communication, but want
```

  s/don't require/don't need low-latency or high-throughput/

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-12T15:59:09Z

- **apis/kueue/v1beta1/workload_types.go:107** by [@mwielgus](https://github.com/mwielgus) — 2025-03-12T15:59:09Z
```diff
@@ -104,6 +104,14 @@ type PodSetTopologyRequest struct {
 	// +optional
 	Preferred *string `json:"preferred,omitempty"`
 
+	// unconstrained indicates that Kueue has freedom to schedule the PodSet within
```

  With preferred Kueue also has this freedom.  Pivot the description more toward lack of need or preference for any specific placement.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T16:17:57Z

- **apis/kueue/v1alpha1/tas_types.go:43** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T16:17:56Z
```diff
@@ -38,6 +38,14 @@ const (
 	// among multiple topology domains.
 	PodSetPreferredTopologyAnnotation = "kueue.x-k8s.io/podset-preferred-topology"
 
+	// PodSetUnconstrainedTopologyAnnotation indicates that a PodSet does not have any topology requirements.
+	// Kueue admits the PodSet if there's enough free capacity available.
+	// Recommended for PodSets that don't require pod-to-pod communication, but want
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-12T16:18:03Z

- **apis/kueue/v1beta1/workload_types.go:107** by [@PBundyra](https://github.com/PBundyra) — 2025-03-12T16:18:02Z
```diff
@@ -104,6 +104,14 @@ type PodSetTopologyRequest struct {
 	// +optional
 	Preferred *string `json:"preferred,omitempty"`
 
+	// unconstrained indicates that Kueue has freedom to schedule the PodSet within
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T08:13:22Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2713** by [@mimowo](https://github.com/mimowo) — 2025-03-13T08:13:22Z
```diff
@@ -2655,4 +2655,105 @@ var _ = ginkgo.Describe("Job controller when TopologyAwareScheduling enabled", g
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("should admit workload with topology unconstrained annotation which fits", func() {
+		job := testingjob.MakeJob("job", ns.Name).
+			Queue(localQueue.Name).
+			PodAnnotation(kueuealpha.PodSetUnconstrainedTopologyAnnotation, "true").
+			Request(corev1.ResourceCPU, "1").
+			Obj()
+		ginkgo.By("creating a job", func() {
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		})
+
+		wl := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+		ginkgo.By("verify the workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets).Should(gomega.BeComparableTo([]kueue.PodSet{{
+					Name:  kueue.DefaultPodSetName,
+					Count: 1,
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+						PodIndexLabel: ptr.To(batchv1.JobCompletionIndexAnnotation),
+					},
+				}}, cmpopts.IgnoreFields(kueue.PodSet{}, "Template")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify the workload is admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+		})
+
+		ginkgo.By("verify admission for the workload", func() {
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(wl.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+					&kueue.TopologyAssignment{
+						Levels:  []string{tasBlockLabel},
+						Domains: []kueue.TopologyDomainAssignment{{Count: 1, Values: []string{"b1"}}},
+					},
+				))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("should implicitly default the .unconstrained field if the TASImplicitDefaultUnconstrained gate is set", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASImplicitDefaultUnconstrained, true)
```

  Make sure the feature gate is reverted to false, so that it does not affect other tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T08:20:10Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2660** by [@mimowo](https://github.com/mimowo) — 2025-03-13T08:20:10Z
```diff
@@ -2655,4 +2655,105 @@ var _ = ginkgo.Describe("Job controller when TopologyAwareScheduling enabled", g
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("should admit workload with topology unconstrained annotation which fits", func() {
```

  Actually, I would consider moving these tests as unite tests in scheduler_test.go, under `TestScheduleForTAS`. 
  Mainly, because the unit tests are much faster. I would be ok to probably keep one of them as 
  integration, but it would be healthier to have a proper "testing pyramide".
  
  And I would suggest to add a unit test which shows the difference between "preferred" and "unconstrained", as discussed under the issue: https://github.com/kubernetes-sigs/kueue/issues/4474

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T08:22:21Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2705** by [@mimowo](https://github.com/mimowo) — 2025-03-13T08:22:21Z
```diff
@@ -2655,4 +2655,105 @@ var _ = ginkgo.Describe("Job controller when TopologyAwareScheduling enabled", g
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("should admit workload with topology unconstrained annotation which fits", func() {
+		job := testingjob.MakeJob("job", ns.Name).
+			Queue(localQueue.Name).
+			PodAnnotation(kueuealpha.PodSetUnconstrainedTopologyAnnotation, "true").
+			Request(corev1.ResourceCPU, "1").
+			Obj()
+		ginkgo.By("creating a job", func() {
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		})
+
+		wl := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+		ginkgo.By("verify the workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets).Should(gomega.BeComparableTo([]kueue.PodSet{{
+					Name:  kueue.DefaultPodSetName,
+					Count: 1,
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+						PodIndexLabel: ptr.To(batchv1.JobCompletionIndexAnnotation),
+					},
+				}}, cmpopts.IgnoreFields(kueue.PodSet{}, "Template")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify the workload is admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+		})
+
+		ginkgo.By("verify admission for the workload", func() {
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(wl.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+					&kueue.TopologyAssignment{
+						Levels:  []string{tasBlockLabel},
+						Domains: []kueue.TopologyDomainAssignment{{Count: 1, Values: []string{"b1"}}},
```

  It is not clear to me if the assignment was different if "preferred" was used and why.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T08:25:21Z

The implementation LGTM, but I would like to improve the testing:
1. adding integration tests for the feature gate seems too much, I would keep the "proper" pyramid and move as unit tests to "scheduler_test.go". Feel free to keep one integration test for the baseline scenario.
2. it is not clear to me if the asserts are functionalliy different than "preferred" would have returned. Adding unit tests which show the difference as discussed under the issue would be great, and add a comment why we observe the difference.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T09:38:10Z

- **pkg/cache/tas_cache_test.go:417** by [@mimowo](https://github.com/mimowo) — 2025-03-13T09:38:10Z
```diff
@@ -348,7 +412,64 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
-			enableTASMostFreeCapacity: true,
+			enableFeatureGates: []featuregate.Feature{features.TASMostFreeCapacity},
+		},
+		"unconstrained; 5 pods fit into hosts scattered across the whole datacenter even they could fit into single rack; BestFit": {
```

  I don't understand the test intention and description: "unconstrained; 5 pods fit into hosts scattered across the whole datacenter even they could fit into single rack", since x1 and x2 belong to the same rack (b1-r1), or I'm missing something?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-13T10:18:05Z

- **pkg/cache/tas_cache_test.go:417** by [@PBundyra](https://github.com/PBundyra) — 2025-03-13T10:18:05Z
```diff
@@ -348,7 +412,64 @@ func TestFindTopologyAssignment(t *testing.T) {
 					},
 				},
 			},
-			enableTASMostFreeCapacity: true,
+			enableFeatureGates: []featuregate.Feature{features.TASMostFreeCapacity},
+		},
+		"unconstrained; 5 pods fit into hosts scattered across the whole datacenter even they could fit into single rack; BestFit": {
```

  Oh I see, indeed that doesn't show the intentions very well, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-13T10:19:00Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2660** by [@PBundyra](https://github.com/PBundyra) — 2025-03-13T10:19:00Z
```diff
@@ -2655,4 +2655,105 @@ var _ = ginkgo.Describe("Job controller when TopologyAwareScheduling enabled", g
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("should admit workload with topology unconstrained annotation which fits", func() {
```

  Done, discussed in a separate thread

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-13T10:20:19Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2705** by [@PBundyra](https://github.com/PBundyra) — 2025-03-13T10:20:19Z
```diff
@@ -2655,4 +2655,105 @@ var _ = ginkgo.Describe("Job controller when TopologyAwareScheduling enabled", g
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("should admit workload with topology unconstrained annotation which fits", func() {
+		job := testingjob.MakeJob("job", ns.Name).
+			Queue(localQueue.Name).
+			PodAnnotation(kueuealpha.PodSetUnconstrainedTopologyAnnotation, "true").
+			Request(corev1.ResourceCPU, "1").
+			Obj()
+		ginkgo.By("creating a job", func() {
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		})
+
+		wl := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+		ginkgo.By("verify the workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets).Should(gomega.BeComparableTo([]kueue.PodSet{{
+					Name:  kueue.DefaultPodSetName,
+					Count: 1,
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+						PodIndexLabel: ptr.To(batchv1.JobCompletionIndexAnnotation),
+					},
+				}}, cmpopts.IgnoreFields(kueue.PodSet{}, "Template")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify the workload is admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+		})
+
+		ginkgo.By("verify admission for the workload", func() {
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(wl.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+					&kueue.TopologyAssignment{
+						Levels:  []string{tasBlockLabel},
+						Domains: []kueue.TopologyDomainAssignment{{Count: 1, Values: []string{"b1"}}},
```

  My primary goal for this integration test wasn't to show the difference in assignment as I show it in unit tests. I just wanted to make sure workload is admitted to show the new annotation and implicit defaulting works as intended

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-13T10:24:35Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2713** by [@PBundyra](https://github.com/PBundyra) — 2025-03-13T10:24:35Z
```diff
@@ -2655,4 +2655,105 @@ var _ = ginkgo.Describe("Job controller when TopologyAwareScheduling enabled", g
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("should admit workload with topology unconstrained annotation which fits", func() {
+		job := testingjob.MakeJob("job", ns.Name).
+			Queue(localQueue.Name).
+			PodAnnotation(kueuealpha.PodSetUnconstrainedTopologyAnnotation, "true").
+			Request(corev1.ResourceCPU, "1").
+			Obj()
+		ginkgo.By("creating a job", func() {
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		})
+
+		wl := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+		ginkgo.By("verify the workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets).Should(gomega.BeComparableTo([]kueue.PodSet{{
+					Name:  kueue.DefaultPodSetName,
+					Count: 1,
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+						PodIndexLabel: ptr.To(batchv1.JobCompletionIndexAnnotation),
+					},
+				}}, cmpopts.IgnoreFields(kueue.PodSet{}, "Template")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify the workload is admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+		})
+
+		ginkgo.By("verify admission for the workload", func() {
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(wl.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+					&kueue.TopologyAssignment{
+						Levels:  []string{tasBlockLabel},
+						Domains: []kueue.TopologyDomainAssignment{{Count: 1, Values: []string{"b1"}}},
+					},
+				))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("should implicitly default the .unconstrained field if the TASImplicitDefaultUnconstrained gate is set", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASImplicitDefaultUnconstrained, true)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T12:23:54Z

- **test/integration/singlecluster/controller/jobs/job/job_controller_test.go:2705** by [@mimowo](https://github.com/mimowo) — 2025-03-13T12:23:54Z
```diff
@@ -2655,4 +2655,105 @@ var _ = ginkgo.Describe("Job controller when TopologyAwareScheduling enabled", g
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.It("should admit workload with topology unconstrained annotation which fits", func() {
+		job := testingjob.MakeJob("job", ns.Name).
+			Queue(localQueue.Name).
+			PodAnnotation(kueuealpha.PodSetUnconstrainedTopologyAnnotation, "true").
+			Request(corev1.ResourceCPU, "1").
+			Obj()
+		ginkgo.By("creating a job", func() {
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		})
+
+		wl := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+		ginkgo.By("verify the workload is created", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets).Should(gomega.BeComparableTo([]kueue.PodSet{{
+					Name:  kueue.DefaultPodSetName,
+					Count: 1,
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+						PodIndexLabel: ptr.To(batchv1.JobCompletionIndexAnnotation),
+					},
+				}}, cmpopts.IgnoreFields(kueue.PodSet{}, "Template")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("verify the workload is admitted", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
+		})
+
+		ginkgo.By("verify admission for the workload", func() {
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Admission).ShouldNot(gomega.BeNil())
+				g.Expect(wl.Status.Admission.PodSetAssignments).Should(gomega.HaveLen(1))
+				g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+					&kueue.TopologyAssignment{
+						Levels:  []string{tasBlockLabel},
+						Domains: []kueue.TopologyDomainAssignment{{Count: 1, Values: []string{"b1"}}},
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T12:24:06Z

/lgtm
/approve
Thanks!
