# PR #4572: Fair Sharing Preemption for Hierarchical Cohorts

**Summary**: Fair Sharing Preemption for Hierarchical Cohorts

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4572

**Last updated**: 2025-03-19T13:16:32Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `fs_preemption` → `main`
- **Created**: 2025-03-12T15:09:51Z
- **Updated**: 2025-03-19T13:16:32Z
- **Merged**: 2025-03-17T11:19:49Z
- **Closed**: 2025-03-17T11:19:50Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 6   **+620 / -75**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
Fair Sharing Preemption support in Hierarchical Cohorts #3759

#### Which issue(s) this PR fixes:

#### Special notes for your reviewer:
Feature is complete after this change. We will add integration tests in a follow-up PR before closing the tracking issue.

#### Does this PR introduce a user-facing change?
No release note for this change, as we will do a single release note for the entire feature

```release-note
[FSxHC] Make Fair Sharing compatible with Hierarchical Cohorts during preemption
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-12T15:10:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 691567dc83d8731a7b40766ed7ef1c7e1e11abef |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d80015e526880008df0626 |

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T13:24:14Z

> @gabesaba I skimmed through the changes and I like them, however, would it be possible for the ease of reviewing to extract a preparatory PR with the refactorings?

Which refactorings did you have in mind? Most of the changes here are adding new code - and updating existing flow to use this new code

I did a major refactor in #4564 to reduce the diff

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-13T13:49:14Z

I see, I meant changes in pkg/scheduler/preemption/preemption.go, such as:
- replacing DominantResourceShareWith and DominantResourceShareWithout with manipulation of the cq snapshot
- introduction of ` ordering.Iter()`

wdyt?

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T08:05:12Z

> I see, I meant changes in pkg/scheduler/preemption/preemption.go, such as:
> 
> * replacing DominantResourceShareWith and DominantResourceShareWithout with manipulation of the cq snapshot
> * introduction of ` ordering.Iter()`
> 
> wdyt?

I will get rid of `DominantResourceShareWith`

deleting `DominantResourceShareWithout` will require additional intermediate code, which we'd just delete in this PR, so I will avoid this change. Same with introducing `ordering.Iter` - we are just moving to Iter pattern, before deleting the `CqHeap` in this PR.

See #4603

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-14T08:16:16Z

> deleting DominantResourceShareWithout will require additional intermediate code, which we'd just delete in this PR, so I will avoid this change. 

sure, I'm fine with that. This was just an exploratory question / suggestion to know your view and possibly reduce the diff a little bit based on things which caught my attention - not an extensive analysis.

Thank you for the couple of cleanups towards that goal.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T09:56:25Z

> > deleting DominantResourceShareWithout will require additional intermediate code, which we'd just delete in this PR, so I will avoid this change.
> 
> sure, I'm fine with that. This was just an exploratory question / suggestion to know your view and possibly reduce the diff a little bit based on things which caught my attention - not an extensive analysis.
> 
> Thank you for the couple of cleanups towards that goal.

The diff is a lot easier to read now. Thanks for the suggestions :)

### Comment by [@pajakd](https://github.com/pajakd) — 2025-03-14T15:44:18Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-14T15:44:23Z

@pajakd: changing LGTM is restricted to collaborators

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4572#issuecomment-2725074543):

>/lgtm


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-17T08:58:38Z

/lgtm
Thanks
cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-17T08:58:45Z

LGTM label has been added.  <details>Git tree hash: b4ef028a1284467123a7ac0f64f95910e94a2b25</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-17T10:59:31Z

/lgtm
/approve
Awesome!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-17T10:59:37Z

LGTM label has been added.  <details>Git tree hash: f2afb7dedc5db54ea0364f290aabdfb028379310</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-17T10:59:38Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4572#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4572#issuecomment-2729071871" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4572#pullrequestreview-2685964180" title="Approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-19T13:16:27Z

/release-note-edit
```release-note
[FSxHC] Make Fair Sharing compatible with Hierarchical Cohorts during preemption
```

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T09:45:03Z

- **pkg/cache/clusterqueue_snapshot.go:93** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T09:45:03Z
```diff
@@ -88,16 +86,24 @@ func (c *ClusterQueueSnapshot) SimulateUsageRemoval(workloads []*workload.Info)
 }
 
 func (c *ClusterQueueSnapshot) AddUsage(usage workload.Usage) {
-	for fr, q := range usage.Quota {
+	c.AddFRQUsage(usage.Quota)
+	c.updateTASUsage(usage.TAS, add)
+}
+
+func (c *ClusterQueueSnapshot) AddFRQUsage(frq resources.FlavorResourceQuantities) {
```

  I suppose this is incorrect for TAS? We may need to solve at least part of https://github.com/kubernetes-sigs/kueue/issues/4545
  
  @mimowo

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-13T10:00:49Z

- **pkg/scheduler/fairsharing/preemption.go:171** by [@gabesaba](https://github.com/gabesaba) — 2025-03-13T10:00:49Z
```diff
@@ -0,0 +1,257 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueOrdering defines an ordering over ClusterQueues to
+// consider during preemption.  This is done by traversing from root
+// Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload,
+// or ClusterQueueOrdering.DropQueue must be called between
+// each entry returned.
+type ClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// used to find leastCommonAncestor (LCA) and almostLCA.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	cqToPreemptionCandidate map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption candidates.
+	prunedCqs     sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) ClusterQueueOrdering {
+	fp := ClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		cqToPreemptionCandidate: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedCqs:     sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts: sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		fp.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		fp.cqToPreemptionCandidate[candidate.ClusterQueue] = append(fp.cqToPreemptionCandidate[candidate.ClusterQueue], candidate)
+	}
+
+	return fp
+}
+
+func (ci *ClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		root := ci.preemptorCq.Parent().Root()
+
+		// we stop once we have marked the root as pruned.
+		for !ci.prunedCohorts.Has(root) {
+			targetCq := ci.search(root)
+
+			// an iteration which just pruned some node
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (ci *ClusterQueueOrdering) DropQueue(t *TargetClusterQueue) {
+	ci.prunedCqs.Insert(t.targetCq)
+}
+
+func (ci *ClusterQueueOrdering) onPathToCandidateCq(cohort *cache.CohortSnapshot) bool {
+	return ci.preemptorAncestors.Has(cohort)
+}
+
+func (ci *ClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(ci.cqToPreemptionCandidate[cq.GetName()]) > 0
+}
+
+// search is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (ci *ClusterQueueOrdering) search(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := 0
+	for _, cq := range cohort.ChildCQs() {
+		if ci.prunedCqs.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preempting ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != ci.preemptorCq) || !ci.hasWorkload(cq) {
+			ci.prunedCqs.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := 0
+	for _, cohort := range cohort.ChildCohorts() {
+		if ci.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preempting ClusterQueue to
+		// root, as there may be imbalance within some
+		// subtree, or preemption within Preempting CQ itself
+		// which is possible.  We will only prune such Cohorts
+		// if all if its children have been pruned.
+		if drs == 0 && !ci.onPathToCandidateCq(cohort) {
+			ci.prunedCohorts.Insert(cohort)
+		} else if drs >= highestCohortDrs {
+			highestCohortDrs = drs
+			highestCohort = cohort
+		}
+	}
+
+	// None of the children are valid candidates (i.e. all
+	// children pruned), so this Cohort is pruned.
+	if highestCohort == nil && highestCq == nil {
+		ci.prunedCohorts.Insert(cohort)
+		return nil
+	}
+
+	// we use >= because, as a tiebreak, choosing the Cohort seems
+	// slightly more fair, as we can choose the most unfair node
+	// within that Cohort.
+	if highestCohortDrs >= highestCqDrs {
```

  Null pointer bug here: when CQ has score of 0, and Cohort is nil

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T11:09:49Z

- **pkg/cache/clusterqueue_snapshot.go:93** by [@mimowo](https://github.com/mimowo) — 2025-03-13T11:09:49Z
```diff
@@ -88,16 +86,24 @@ func (c *ClusterQueueSnapshot) SimulateUsageRemoval(workloads []*workload.Info)
 }
 
 func (c *ClusterQueueSnapshot) AddUsage(usage workload.Usage) {
-	for fr, q := range usage.Quota {
+	c.AddFRQUsage(usage.Quota)
+	c.updateTASUsage(usage.TAS, add)
+}
+
+func (c *ClusterQueueSnapshot) AddFRQUsage(frq resources.FlavorResourceQuantities) {
```

  yeah, seem incorrect. We need to fix or at least ticket. IIUC https://github.com/kubernetes-sigs/kueue/issues/4545 is just a cleanup, so doesn't belong there imo.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-13T11:17:11Z

@gabesaba I skimmed through the changes and I like them, however, would it be possible for the ease of reviewing to extract a preparatory PR with the refactorings?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T08:00:23Z

- **pkg/cache/clusterqueue_snapshot.go:93** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T08:00:22Z
```diff
@@ -88,16 +86,24 @@ func (c *ClusterQueueSnapshot) SimulateUsageRemoval(workloads []*workload.Info)
 }
 
 func (c *ClusterQueueSnapshot) AddUsage(usage workload.Usage) {
-	for fr, q := range usage.Quota {
+	c.AddFRQUsage(usage.Quota)
+	c.updateTASUsage(usage.TAS, add)
+}
+
+func (c *ClusterQueueSnapshot) AddFRQUsage(frq resources.FlavorResourceQuantities) {
```

  https://github.com/kubernetes-sigs/kueue/pull/4603

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-14T11:41:35Z

- **pkg/scheduler/preemption/fairsharing/target.go:40** by [@PBundyra](https://github.com/PBundyra) — 2025-03-14T11:41:35Z
```diff
@@ -0,0 +1,69 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import (
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueue is a ClusterQueue which yields candidate
+// workloads for preemption.
+type TargetClusterQueue struct {
+	ordering *TargetClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the TargetClusterQueue is
+// the preemptor ClusterQueue; i.e. the preemptor ClusterQueue is
+// considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.ordering.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.ordering.clusterQueueToTarget[t.targetCq.GetName()][0]
+	t.ordering.clusterQueueToTarget[t.targetCq.GetName()] = t.ordering.clusterQueueToTarget[t.targetCq.GetName()][1:]
+	return head
+}
```

  Can we extract `t.targetCq.GetName()` to a variable?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-14T11:58:20Z

- **pkg/scheduler/preemption/fairsharing/target.go:61** by [@PBundyra](https://github.com/PBundyra) — 2025-03-14T11:58:20Z
```diff
@@ -0,0 +1,69 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import (
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueue is a ClusterQueue which yields candidate
+// workloads for preemption.
+type TargetClusterQueue struct {
+	ordering *TargetClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the TargetClusterQueue is
+// the preemptor ClusterQueue; i.e. the preemptor ClusterQueue is
+// considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.ordering.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.ordering.clusterQueueToTarget[t.targetCq.GetName()][0]
+	t.ordering.clusterQueueToTarget[t.targetCq.GetName()] = t.ordering.clusterQueueToTarget[t.targetCq.GetName()][1:]
+	return head
+}
+
+func (t *TargetClusterQueue) HasWorkload() bool {
+	return t.ordering.hasWorkload(t.targetCq)
+}
+
+// ComputeShares computes the DominantResourceShares of the premptor
+// and target ClusterQueues' AlmostLeastCommonAncestors. These shares
+// do not depend on the removal of the workload being considered for
+// preemption.
+func (t *TargetClusterQueue) ComputeShares() (PreemptorNewShare, TargetOldShare) {
+	preemptorAlmostLca, targetAlmostLca := getAlmostLcas(t)
+	return PreemptorNewShare(preemptorAlmostLca.DominantResourceShare()), TargetOldShare(targetAlmostLca.DominantResourceShare())
+}
+
+// ComputeTargetNewShare returns DominantResourceShare of the
+// TargetClusterQueue's AlmostLeastCommonAncestor, after removing
+// provided workload.
+//
+// This simulation is required, as we need to update usage while
+// respecting lending and borrowing limits, which the cache/snapshot
+// implements.
```

  Not sure if I understand this right. If we only remove the workload, is there a risk of invalidating lending/borrowing limits? How is it better then deleted `DominantResourceShareWithout`
  
  Also could me maybe rename the function so it express intention of deletion? e.g `ComputeTargetShareAfterRemoval`/`ComputeDRSAfterRemoval`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-14T12:18:01Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:95** by [@PBundyra](https://github.com/PBundyra) — 2025-03-14T12:18:01Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
```

  Can't this lead to infinite loops?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-14T13:19:56Z

- **pkg/scheduler/fairsharing/preemption.go:229** by [@pajakd](https://github.com/pajakd) — 2025-03-14T09:27:58Z
```diff
@@ -0,0 +1,257 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueOrdering defines an ordering over ClusterQueues to
+// consider during preemption.  This is done by traversing from root
+// Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload,
+// or ClusterQueueOrdering.DropQueue must be called between
+// each entry returned.
+type ClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// used to find leastCommonAncestor (LCA) and almostLCA.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	cqToPreemptionCandidate map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption candidates.
+	prunedCqs     sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) ClusterQueueOrdering {
+	fp := ClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		cqToPreemptionCandidate: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedCqs:     sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts: sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		fp.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		fp.cqToPreemptionCandidate[candidate.ClusterQueue] = append(fp.cqToPreemptionCandidate[candidate.ClusterQueue], candidate)
+	}
+
+	return fp
+}
+
+func (ci *ClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		root := ci.preemptorCq.Parent().Root()
+
+		// we stop once we have marked the root as pruned.
+		for !ci.prunedCohorts.Has(root) {
+			targetCq := ci.search(root)
+
+			// an iteration which just pruned some node
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (ci *ClusterQueueOrdering) DropQueue(t *TargetClusterQueue) {
+	ci.prunedCqs.Insert(t.targetCq)
+}
+
+func (ci *ClusterQueueOrdering) onPathToCandidateCq(cohort *cache.CohortSnapshot) bool {
+	return ci.preemptorAncestors.Has(cohort)
+}
+
+func (ci *ClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(ci.cqToPreemptionCandidate[cq.GetName()]) > 0
+}
+
+// search is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (ci *ClusterQueueOrdering) search(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := 0
+	for _, cq := range cohort.ChildCQs() {
+		if ci.prunedCqs.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preempting ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != ci.preemptorCq) || !ci.hasWorkload(cq) {
+			ci.prunedCqs.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := 0
+	for _, cohort := range cohort.ChildCohorts() {
+		if ci.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preempting ClusterQueue to
+		// root, as there may be imbalance within some
+		// subtree, or preemption within Preempting CQ itself
+		// which is possible.  We will only prune such Cohorts
+		// if all if its children have been pruned.
+		if drs == 0 && !ci.onPathToCandidateCq(cohort) {
+			ci.prunedCohorts.Insert(cohort)
+		} else if drs >= highestCohortDrs {
+			highestCohortDrs = drs
+			highestCohort = cohort
+		}
+	}
+
+	// None of the children are valid candidates (i.e. all
+	// children pruned), so this Cohort is pruned.
+	if highestCohort == nil && highestCq == nil {
+		ci.prunedCohorts.Insert(cohort)
+		return nil
+	}
+
+	// we use >= because, as a tiebreak, choosing the Cohort seems
+	// slightly more fair, as we can choose the most unfair node
+	// within that Cohort.
+	if highestCohortDrs >= highestCqDrs {
+		return ci.search(highestCohort)
+	}
+	return &TargetClusterQueue{
+		cqIter:   ci,
+		targetCq: highestCq,
+	}
+}
+
+// ClusterQueue that we're targeting for preemption.
+type TargetClusterQueue struct {
+	cqIter   *ClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the ClusterQueue
+// is considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.cqIter.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][0]
+	t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()] = t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][1:]
+	return head
+}
+
+func (t *TargetClusterQueue) HasWorkload() bool {
+	return t.cqIter.hasWorkload(t.targetCq)
+}
+
+// AlmostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node the leastCommonAncestor (LCA). While LCA is
+// always a Cohort, AlmostLCA may be a ClusterQueue or a Cohort.
+type AlmostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAS returns almostLCA of (preemptorCq, targetCq).
+func (t *TargetClusterQueue) GetAlmostLCAs() (AlmostLCA, AlmostLCA) {
+	lca := t.getLca(t.targetCq)
+	return t.getAlmostLca(t.cqIter.preemptorCq, lca), t.getAlmostLca(t.targetCq, lca)
+}
+
+// GetLca iterates from the targetCq, the CQ which we are considering
+// to preempt in, up to the root. We return the first Cohort we
+// encounter which contains the preempting CQ.
+func (t *TargetClusterQueue) getLca(targetCq *cache.ClusterQueueSnapshot) *cache.CohortSnapshot {
+	cohort := targetCq.Parent()
+	for {
+		if t.cqIter.onPathToCandidateCq(cohort) {
+			return cohort
+		}
+		cohort = cohort.Parent()
+	}
+}
+
+// getAlmostLca iterates from a CQ towards the root, returning the
+// first node that has LCA as its direct child.
```

  direct child -> direct parent?

- **pkg/scheduler/fairsharing/preemption.go:218** by [@pajakd](https://github.com/pajakd) — 2025-03-14T09:32:42Z
```diff
@@ -0,0 +1,257 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueOrdering defines an ordering over ClusterQueues to
+// consider during preemption.  This is done by traversing from root
+// Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload,
+// or ClusterQueueOrdering.DropQueue must be called between
+// each entry returned.
+type ClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// used to find leastCommonAncestor (LCA) and almostLCA.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	cqToPreemptionCandidate map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption candidates.
+	prunedCqs     sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) ClusterQueueOrdering {
+	fp := ClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		cqToPreemptionCandidate: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedCqs:     sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts: sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		fp.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		fp.cqToPreemptionCandidate[candidate.ClusterQueue] = append(fp.cqToPreemptionCandidate[candidate.ClusterQueue], candidate)
+	}
+
+	return fp
+}
+
+func (ci *ClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		root := ci.preemptorCq.Parent().Root()
+
+		// we stop once we have marked the root as pruned.
+		for !ci.prunedCohorts.Has(root) {
+			targetCq := ci.search(root)
+
+			// an iteration which just pruned some node
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (ci *ClusterQueueOrdering) DropQueue(t *TargetClusterQueue) {
+	ci.prunedCqs.Insert(t.targetCq)
+}
+
+func (ci *ClusterQueueOrdering) onPathToCandidateCq(cohort *cache.CohortSnapshot) bool {
+	return ci.preemptorAncestors.Has(cohort)
+}
+
+func (ci *ClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(ci.cqToPreemptionCandidate[cq.GetName()]) > 0
+}
+
+// search is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (ci *ClusterQueueOrdering) search(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := 0
+	for _, cq := range cohort.ChildCQs() {
+		if ci.prunedCqs.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preempting ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != ci.preemptorCq) || !ci.hasWorkload(cq) {
+			ci.prunedCqs.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := 0
+	for _, cohort := range cohort.ChildCohorts() {
+		if ci.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preempting ClusterQueue to
+		// root, as there may be imbalance within some
+		// subtree, or preemption within Preempting CQ itself
+		// which is possible.  We will only prune such Cohorts
+		// if all if its children have been pruned.
+		if drs == 0 && !ci.onPathToCandidateCq(cohort) {
+			ci.prunedCohorts.Insert(cohort)
+		} else if drs >= highestCohortDrs {
+			highestCohortDrs = drs
+			highestCohort = cohort
+		}
+	}
+
+	// None of the children are valid candidates (i.e. all
+	// children pruned), so this Cohort is pruned.
+	if highestCohort == nil && highestCq == nil {
+		ci.prunedCohorts.Insert(cohort)
+		return nil
+	}
+
+	// we use >= because, as a tiebreak, choosing the Cohort seems
+	// slightly more fair, as we can choose the most unfair node
+	// within that Cohort.
+	if highestCohortDrs >= highestCqDrs {
+		return ci.search(highestCohort)
+	}
+	return &TargetClusterQueue{
+		cqIter:   ci,
+		targetCq: highestCq,
+	}
+}
+
+// ClusterQueue that we're targeting for preemption.
+type TargetClusterQueue struct {
+	cqIter   *ClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the ClusterQueue
+// is considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.cqIter.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][0]
+	t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()] = t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][1:]
+	return head
+}
+
+func (t *TargetClusterQueue) HasWorkload() bool {
+	return t.cqIter.hasWorkload(t.targetCq)
+}
+
+// AlmostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node the leastCommonAncestor (LCA). While LCA is
+// always a Cohort, AlmostLCA may be a ClusterQueue or a Cohort.
+type AlmostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAS returns almostLCA of (preemptorCq, targetCq).
+func (t *TargetClusterQueue) GetAlmostLCAs() (AlmostLCA, AlmostLCA) {
+	lca := t.getLca(t.targetCq)
+	return t.getAlmostLca(t.cqIter.preemptorCq, lca), t.getAlmostLca(t.targetCq, lca)
+}
+
+// GetLca iterates from the targetCq, the CQ which we are considering
+// to preempt in, up to the root. We return the first Cohort we
+// encounter which contains the preempting CQ.
+func (t *TargetClusterQueue) getLca(targetCq *cache.ClusterQueueSnapshot) *cache.CohortSnapshot {
```

  nit: lets write everywhere either Lca or LCA

- **pkg/scheduler/preemption/fairsharing/ordering.go:169** by [@pajakd](https://github.com/pajakd) — 2025-03-14T11:13:49Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
+
+			// an iteration which just pruned some node(s).
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (t *TargetClusterQueueOrdering) DropQueue(cq *TargetClusterQueue) {
+	t.prunedClusterQueues.Insert(cq.targetCq)
+}
+
+func (t *TargetClusterQueueOrdering) onPathToPreemptorCQ(cohort *cache.CohortSnapshot) bool {
+	return t.preemptorAncestors.Has(cohort)
+}
+
+func (t *TargetClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(t.clusterQueueToTarget[cq.GetName()]) > 0
+}
+
+// nextTarget is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (t *TargetClusterQueueOrdering) nextTarget(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := -1
+	for _, cq := range cohort.ChildCQs() {
+		if t.prunedClusterQueues.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preemptor ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != t.preemptorCq) || !t.hasWorkload(cq) {
+			t.prunedClusterQueues.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := -1
+	for _, cohort := range cohort.ChildCohorts() {
+		if t.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preemptor ClusterQueue to
+		// root, as there may be imbalance within some
+		// subtree, or a possible preemption within Preemptor
+		// CQ itself.  We will only prune such a Cohort if all
+		// if its children have been pruned.
```

  nit: if all if its -> if all of its

- **pkg/scheduler/preemption/fairsharing/ordering.go:77** by [@pajakd](https://github.com/pajakd) — 2025-03-14T11:48:55Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
```

  Just to clarify. We don't care about the priorities here, right? In #1773 there is a sentence "Super high priority workloads overrule fair sharing..." -- this is just not implemented yet at this stage? With Fair Sharing, we don't look at MaxPriorityThreshold parameter?

- **pkg/scheduler/preemption/fairsharing/ordering.go:102** by [@pajakd](https://github.com/pajakd) — 2025-03-14T13:19:24Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
```

  Greedy algorithm walking from the root is a heuristic here, right? 
  
  One case that we are slightly missing here is the following. Lets say my sibling cohort is unfairly using my quota by a lot, but my parent cohort is not borrowing at all. Even if my sibling has the highest drs globally we would reach to them rather late, right? We would first consider all cohorts that have drs > 0 on the whole path from the root. We will eventually reach to the sibling because we don't prune the ancestors path, which is correct but the order might be counter-intuitive sometimes.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T14:53:56Z

- **pkg/scheduler/preemption/fairsharing/target.go:61** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T14:53:56Z
```diff
@@ -0,0 +1,69 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import (
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueue is a ClusterQueue which yields candidate
+// workloads for preemption.
+type TargetClusterQueue struct {
+	ordering *TargetClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the TargetClusterQueue is
+// the preemptor ClusterQueue; i.e. the preemptor ClusterQueue is
+// considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.ordering.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.ordering.clusterQueueToTarget[t.targetCq.GetName()][0]
+	t.ordering.clusterQueueToTarget[t.targetCq.GetName()] = t.ordering.clusterQueueToTarget[t.targetCq.GetName()][1:]
+	return head
+}
+
+func (t *TargetClusterQueue) HasWorkload() bool {
+	return t.ordering.hasWorkload(t.targetCq)
+}
+
+// ComputeShares computes the DominantResourceShares of the premptor
+// and target ClusterQueues' AlmostLeastCommonAncestors. These shares
+// do not depend on the removal of the workload being considered for
+// preemption.
+func (t *TargetClusterQueue) ComputeShares() (PreemptorNewShare, TargetOldShare) {
+	preemptorAlmostLca, targetAlmostLca := getAlmostLcas(t)
+	return PreemptorNewShare(preemptorAlmostLca.DominantResourceShare()), TargetOldShare(targetAlmostLca.DominantResourceShare())
+}
+
+// ComputeTargetNewShare returns DominantResourceShare of the
+// TargetClusterQueue's AlmostLeastCommonAncestor, after removing
+// provided workload.
+//
+// This simulation is required, as we need to update usage while
+// respecting lending and borrowing limits, which the cache/snapshot
+// implements.
```

  I updated the comment to make it more clear.
  
  TLDR: we need to have accurate usage at arbitrary node, and the easiest way to simulate this is to add the usage to the ClusterQueue, and let our libraries (which we trust are implemented correctly..) propagate it up
  
  updated the function name

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T14:55:38Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:95** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T14:55:38Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
```

  As long as the user follows the API contract - see TargetClusterQueueOrdering doc - it won't.
  
  In general, this code worries me as an infinite loop risk. What do you think of having a max iterations breakout, so at least we'll print a debugging error message, so users can file an issue if there are any branches we miss? Rather than going into a livelock

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T14:56:54Z

- **pkg/scheduler/fairsharing/preemption.go:229** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T14:56:54Z
```diff
@@ -0,0 +1,257 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueOrdering defines an ordering over ClusterQueues to
+// consider during preemption.  This is done by traversing from root
+// Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload,
+// or ClusterQueueOrdering.DropQueue must be called between
+// each entry returned.
+type ClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// used to find leastCommonAncestor (LCA) and almostLCA.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	cqToPreemptionCandidate map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption candidates.
+	prunedCqs     sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) ClusterQueueOrdering {
+	fp := ClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		cqToPreemptionCandidate: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedCqs:     sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts: sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		fp.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		fp.cqToPreemptionCandidate[candidate.ClusterQueue] = append(fp.cqToPreemptionCandidate[candidate.ClusterQueue], candidate)
+	}
+
+	return fp
+}
+
+func (ci *ClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		root := ci.preemptorCq.Parent().Root()
+
+		// we stop once we have marked the root as pruned.
+		for !ci.prunedCohorts.Has(root) {
+			targetCq := ci.search(root)
+
+			// an iteration which just pruned some node
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (ci *ClusterQueueOrdering) DropQueue(t *TargetClusterQueue) {
+	ci.prunedCqs.Insert(t.targetCq)
+}
+
+func (ci *ClusterQueueOrdering) onPathToCandidateCq(cohort *cache.CohortSnapshot) bool {
+	return ci.preemptorAncestors.Has(cohort)
+}
+
+func (ci *ClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(ci.cqToPreemptionCandidate[cq.GetName()]) > 0
+}
+
+// search is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (ci *ClusterQueueOrdering) search(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := 0
+	for _, cq := range cohort.ChildCQs() {
+		if ci.prunedCqs.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preempting ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != ci.preemptorCq) || !ci.hasWorkload(cq) {
+			ci.prunedCqs.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := 0
+	for _, cohort := range cohort.ChildCohorts() {
+		if ci.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preempting ClusterQueue to
+		// root, as there may be imbalance within some
+		// subtree, or preemption within Preempting CQ itself
+		// which is possible.  We will only prune such Cohorts
+		// if all if its children have been pruned.
+		if drs == 0 && !ci.onPathToCandidateCq(cohort) {
+			ci.prunedCohorts.Insert(cohort)
+		} else if drs >= highestCohortDrs {
+			highestCohortDrs = drs
+			highestCohort = cohort
+		}
+	}
+
+	// None of the children are valid candidates (i.e. all
+	// children pruned), so this Cohort is pruned.
+	if highestCohort == nil && highestCq == nil {
+		ci.prunedCohorts.Insert(cohort)
+		return nil
+	}
+
+	// we use >= because, as a tiebreak, choosing the Cohort seems
+	// slightly more fair, as we can choose the most unfair node
+	// within that Cohort.
+	if highestCohortDrs >= highestCqDrs {
+		return ci.search(highestCohort)
+	}
+	return &TargetClusterQueue{
+		cqIter:   ci,
+		targetCq: highestCq,
+	}
+}
+
+// ClusterQueue that we're targeting for preemption.
+type TargetClusterQueue struct {
+	cqIter   *ClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the ClusterQueue
+// is considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.cqIter.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][0]
+	t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()] = t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][1:]
+	return head
+}
+
+func (t *TargetClusterQueue) HasWorkload() bool {
+	return t.cqIter.hasWorkload(t.targetCq)
+}
+
+// AlmostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node the leastCommonAncestor (LCA). While LCA is
+// always a Cohort, AlmostLCA may be a ClusterQueue or a Cohort.
+type AlmostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAS returns almostLCA of (preemptorCq, targetCq).
+func (t *TargetClusterQueue) GetAlmostLCAs() (AlmostLCA, AlmostLCA) {
+	lca := t.getLca(t.targetCq)
+	return t.getAlmostLca(t.cqIter.preemptorCq, lca), t.getAlmostLca(t.targetCq, lca)
+}
+
+// GetLca iterates from the targetCq, the CQ which we are considering
+// to preempt in, up to the root. We return the first Cohort we
+// encounter which contains the preempting CQ.
+func (t *TargetClusterQueue) getLca(targetCq *cache.ClusterQueueSnapshot) *cache.CohortSnapshot {
+	cohort := targetCq.Parent()
+	for {
+		if t.cqIter.onPathToCandidateCq(cohort) {
+			return cohort
+		}
+		cohort = cohort.Parent()
+	}
+}
+
+// getAlmostLca iterates from a CQ towards the root, returning the
+// first node that has LCA as its direct child.
```

  Outdated comment :)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T14:58:44Z

- **pkg/scheduler/fairsharing/preemption.go:218** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T14:58:44Z
```diff
@@ -0,0 +1,257 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueOrdering defines an ordering over ClusterQueues to
+// consider during preemption.  This is done by traversing from root
+// Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload,
+// or ClusterQueueOrdering.DropQueue must be called between
+// each entry returned.
+type ClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// used to find leastCommonAncestor (LCA) and almostLCA.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	cqToPreemptionCandidate map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption candidates.
+	prunedCqs     sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) ClusterQueueOrdering {
+	fp := ClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		cqToPreemptionCandidate: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedCqs:     sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts: sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		fp.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		fp.cqToPreemptionCandidate[candidate.ClusterQueue] = append(fp.cqToPreemptionCandidate[candidate.ClusterQueue], candidate)
+	}
+
+	return fp
+}
+
+func (ci *ClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		root := ci.preemptorCq.Parent().Root()
+
+		// we stop once we have marked the root as pruned.
+		for !ci.prunedCohorts.Has(root) {
+			targetCq := ci.search(root)
+
+			// an iteration which just pruned some node
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (ci *ClusterQueueOrdering) DropQueue(t *TargetClusterQueue) {
+	ci.prunedCqs.Insert(t.targetCq)
+}
+
+func (ci *ClusterQueueOrdering) onPathToCandidateCq(cohort *cache.CohortSnapshot) bool {
+	return ci.preemptorAncestors.Has(cohort)
+}
+
+func (ci *ClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(ci.cqToPreemptionCandidate[cq.GetName()]) > 0
+}
+
+// search is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (ci *ClusterQueueOrdering) search(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := 0
+	for _, cq := range cohort.ChildCQs() {
+		if ci.prunedCqs.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preempting ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != ci.preemptorCq) || !ci.hasWorkload(cq) {
+			ci.prunedCqs.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := 0
+	for _, cohort := range cohort.ChildCohorts() {
+		if ci.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preempting ClusterQueue to
+		// root, as there may be imbalance within some
+		// subtree, or preemption within Preempting CQ itself
+		// which is possible.  We will only prune such Cohorts
+		// if all if its children have been pruned.
+		if drs == 0 && !ci.onPathToCandidateCq(cohort) {
+			ci.prunedCohorts.Insert(cohort)
+		} else if drs >= highestCohortDrs {
+			highestCohortDrs = drs
+			highestCohort = cohort
+		}
+	}
+
+	// None of the children are valid candidates (i.e. all
+	// children pruned), so this Cohort is pruned.
+	if highestCohort == nil && highestCq == nil {
+		ci.prunedCohorts.Insert(cohort)
+		return nil
+	}
+
+	// we use >= because, as a tiebreak, choosing the Cohort seems
+	// slightly more fair, as we can choose the most unfair node
+	// within that Cohort.
+	if highestCohortDrs >= highestCqDrs {
+		return ci.search(highestCohort)
+	}
+	return &TargetClusterQueue{
+		cqIter:   ci,
+		targetCq: highestCq,
+	}
+}
+
+// ClusterQueue that we're targeting for preemption.
+type TargetClusterQueue struct {
+	cqIter   *ClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the ClusterQueue
+// is considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.cqIter.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][0]
+	t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()] = t.cqIter.cqToPreemptionCandidate[t.targetCq.GetName()][1:]
+	return head
+}
+
+func (t *TargetClusterQueue) HasWorkload() bool {
+	return t.cqIter.hasWorkload(t.targetCq)
+}
+
+// AlmostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node the leastCommonAncestor (LCA). While LCA is
+// always a Cohort, AlmostLCA may be a ClusterQueue or a Cohort.
+type AlmostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAS returns almostLCA of (preemptorCq, targetCq).
+func (t *TargetClusterQueue) GetAlmostLCAs() (AlmostLCA, AlmostLCA) {
+	lca := t.getLca(t.targetCq)
+	return t.getAlmostLca(t.cqIter.preemptorCq, lca), t.getAlmostLca(t.targetCq, lca)
+}
+
+// GetLca iterates from the targetCq, the CQ which we are considering
+// to preempt in, up to the root. We return the first Cohort we
+// encounter which contains the preempting CQ.
+func (t *TargetClusterQueue) getLca(targetCq *cache.ClusterQueueSnapshot) *cache.CohortSnapshot {
```

  Done.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T14:59:12Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:169** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T14:59:12Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
+
+			// an iteration which just pruned some node(s).
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (t *TargetClusterQueueOrdering) DropQueue(cq *TargetClusterQueue) {
+	t.prunedClusterQueues.Insert(cq.targetCq)
+}
+
+func (t *TargetClusterQueueOrdering) onPathToPreemptorCQ(cohort *cache.CohortSnapshot) bool {
+	return t.preemptorAncestors.Has(cohort)
+}
+
+func (t *TargetClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(t.clusterQueueToTarget[cq.GetName()]) > 0
+}
+
+// nextTarget is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (t *TargetClusterQueueOrdering) nextTarget(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := -1
+	for _, cq := range cohort.ChildCQs() {
+		if t.prunedClusterQueues.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preemptor ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != t.preemptorCq) || !t.hasWorkload(cq) {
+			t.prunedClusterQueues.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := -1
+	for _, cohort := range cohort.ChildCohorts() {
+		if t.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preemptor ClusterQueue to
+		// root, as there may be imbalance within some
+		// subtree, or a possible preemption within Preemptor
+		// CQ itself.  We will only prune such a Cohort if all
+		// if its children have been pruned.
```

  Done.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T15:01:29Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:77** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T15:01:28Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
```

  Correct. I don't think that we will ever have such a rule, as it resulted in buggy behavior #3779. I may update the kep at some point
  
  We stopped considering MaxPriorityThreshold in FairSharing here as it resulted in the aforementioned bug: https://github.com/kubernetes-sigs/kueue/pull/4165

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T15:04:37Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:102** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T15:04:37Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
```

  That is correct. The intuition here is that we would rather preempt workloads in other Cohorts first, as they are using more shared capacity than we are- shared capacity which is in the more common shared pool.
  
  This might be configured intentionally too: imagine the preempting cohort has a super high fair sharing value, so it does deserve more of the shared resource
  
  But this is a heuristic after all.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-14T15:04:46Z

- **pkg/scheduler/preemption/fairsharing/target.go:40** by [@gabesaba](https://github.com/gabesaba) — 2025-03-14T15:04:46Z
```diff
@@ -0,0 +1,69 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import (
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueue is a ClusterQueue which yields candidate
+// workloads for preemption.
+type TargetClusterQueue struct {
+	ordering *TargetClusterQueueOrdering
+	targetCq *cache.ClusterQueueSnapshot
+}
+
+// InClusterQueuePreemption indicates that the TargetClusterQueue is
+// the preemptor ClusterQueue; i.e. the preemptor ClusterQueue is
+// considering its own workloads for priority based preemption.
+func (t *TargetClusterQueue) InClusterQueuePreemption() bool {
+	return t.targetCq == t.ordering.preemptorCq
+}
+
+func (t *TargetClusterQueue) PopWorkload() *workload.Info {
+	head := t.ordering.clusterQueueToTarget[t.targetCq.GetName()][0]
+	t.ordering.clusterQueueToTarget[t.targetCq.GetName()] = t.ordering.clusterQueueToTarget[t.targetCq.GetName()][1:]
+	return head
+}
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (approved) — 2025-03-14T15:41:38Z

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-17T08:52:07Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:95** by [@PBundyra](https://github.com/PBundyra) — 2025-03-17T08:52:07Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
```

  Yeah, it makes sense to add some safety mechanism maybe, cap it by the number of workloads?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T09:05:43Z

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:42** by [@mimowo](https://github.com/mimowo) — 2025-03-17T09:05:43Z
```diff
@@ -0,0 +1,59 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import "sigs.k8s.io/kueue/pkg/cache"
+
+// almostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node - the LeastCommonAncestor (LCA). While LCA
+// is always a Cohort, almostLCA may be a ClusterQueue or a Cohort.
+type almostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAs returns almostLCAs of (preemptor, target).
+func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
+	lca := getLCA(t)
+	return getAlmostLCA(t.ordering.preemptorCq, lca), getAlmostLCA(t.targetCq, lca)
+}
+
+// getLCA traverses from a ClusterQueue towards the root Cohort,
+// returning the first Cohort which contains the preemptor
+// ClusterQueue in its subtree.
+func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
+	cohort := t.targetCq.Parent()
+	for {
+		if t.ordering.onPathToPreemptorCQ(cohort) {
+			return cohort
+		}
+		cohort = cohort.Parent()
+	}
```

  non-blocking comment: Seeing the pattern repeated in a few places already  I would really like to have an iterator, like say `(c *ClusterQueue) pathToRoot() seq.Iter[]`, over the path to the root. Then, we could just for-range in here IIUC.
  
  Still, it might be a follow up cleanup issue / pr.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T09:16:39Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:166** by [@mimowo](https://github.com/mimowo) — 2025-03-17T09:16:38Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
+
+			// an iteration which just pruned some node(s).
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (t *TargetClusterQueueOrdering) DropQueue(cq *TargetClusterQueue) {
+	t.prunedClusterQueues.Insert(cq.targetCq)
+}
+
+func (t *TargetClusterQueueOrdering) onPathToPreemptorCQ(cohort *cache.CohortSnapshot) bool {
+	return t.preemptorAncestors.Has(cohort)
+}
+
+func (t *TargetClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(t.clusterQueueToTarget[cq.GetName()]) > 0
+}
+
+// nextTarget is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (t *TargetClusterQueueOrdering) nextTarget(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := -1
+	for _, cq := range cohort.ChildCQs() {
+		if t.prunedClusterQueues.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preemptor ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != t.preemptorCq) || !t.hasWorkload(cq) {
+			t.prunedClusterQueues.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := -1
+	for _, cohort := range cohort.ChildCohorts() {
+		if t.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preemptor ClusterQueue to
+		// root, as there may be imbalance within some
```

  non-blocking question: if the cohort is not borrowing (DRS=0), but has some internal imbalance, are we in position to reclaim quota from the children. IIUC the picture is like this:
  
  ```
         ROOT
     /            \
    L              R
   /  \           /   \
  a   b       c     d
  ```
  Now, we are trying to admit into "a", and we see that R has DRS=0, but "d" has DRS > 0. I would expect we can already prune, no?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-17T09:40:58Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:166** by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T09:40:58Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
+
+			// an iteration which just pruned some node(s).
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (t *TargetClusterQueueOrdering) DropQueue(cq *TargetClusterQueue) {
+	t.prunedClusterQueues.Insert(cq.targetCq)
+}
+
+func (t *TargetClusterQueueOrdering) onPathToPreemptorCQ(cohort *cache.CohortSnapshot) bool {
+	return t.preemptorAncestors.Has(cohort)
+}
+
+func (t *TargetClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(t.clusterQueueToTarget[cq.GetName()]) > 0
+}
+
+// nextTarget is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (t *TargetClusterQueueOrdering) nextTarget(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := -1
+	for _, cq := range cohort.ChildCQs() {
+		if t.prunedClusterQueues.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preemptor ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != t.preemptorCq) || !t.hasWorkload(cq) {
+			t.prunedClusterQueues.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := -1
+	for _, cohort := range cohort.ChildCohorts() {
+		if t.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preemptor ClusterQueue to
+		// root, as there may be imbalance within some
```

  > non-blocking question: if the cohort is not borrowing (DRS=0), but has some internal imbalance, are we in position to reclaim quota from the children. IIUC the picture is like this:
  
  No, we cannot reclaim from any of R's children in this case, since R's DRS=0
  
  > Now, we are trying to admit into "a", and we see that R has DRS=0, but "d" has DRS > 0. I would expect we can already prune, no?
  
  Correct - we will prune R

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T09:58:01Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:166** by [@mimowo](https://github.com/mimowo) — 2025-03-17T09:58:01Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
+
+			// an iteration which just pruned some node(s).
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (t *TargetClusterQueueOrdering) DropQueue(cq *TargetClusterQueue) {
+	t.prunedClusterQueues.Insert(cq.targetCq)
+}
+
+func (t *TargetClusterQueueOrdering) onPathToPreemptorCQ(cohort *cache.CohortSnapshot) bool {
+	return t.preemptorAncestors.Has(cohort)
+}
+
+func (t *TargetClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(t.clusterQueueToTarget[cq.GetName()]) > 0
+}
+
+// nextTarget is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (t *TargetClusterQueueOrdering) nextTarget(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := -1
+	for _, cq := range cohort.ChildCQs() {
+		if t.prunedClusterQueues.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preemptor ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != t.preemptorCq) || !t.hasWorkload(cq) {
+			t.prunedClusterQueues.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := -1
+	for _, cohort := range cohort.ChildCohorts() {
+		if t.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preemptor ClusterQueue to
+		// root, as there may be imbalance within some
```

  so isn't the comment misleading?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T10:08:39Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:2** by [@mimowo](https://github.com/mimowo) — 2025-03-17T10:08:39Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
```

  Please summarize what is the test coverage of the code in this file, using the unit tests we have.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-17T10:09:10Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:166** by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T10:09:09Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
+
+			// an iteration which just pruned some node(s).
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (t *TargetClusterQueueOrdering) DropQueue(cq *TargetClusterQueue) {
+	t.prunedClusterQueues.Insert(cq.targetCq)
+}
+
+func (t *TargetClusterQueueOrdering) onPathToPreemptorCQ(cohort *cache.CohortSnapshot) bool {
+	return t.preemptorAncestors.Has(cohort)
+}
+
+func (t *TargetClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(t.clusterQueueToTarget[cq.GetName()]) > 0
+}
+
+// nextTarget is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (t *TargetClusterQueueOrdering) nextTarget(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := -1
+	for _, cq := range cohort.ChildCQs() {
+		if t.prunedClusterQueues.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preemptor ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != t.preemptorCq) || !t.hasWorkload(cq) {
+			t.prunedClusterQueues.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := -1
+	for _, cohort := range cohort.ChildCohorts() {
+		if t.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preemptor ClusterQueue to
+		// root, as there may be imbalance within some
```

  This comment is talking about the path from A to root - so L is not pruned just because its DRS is 0 - since there is still potential to preempt from either a or b. It is only pruned once all of its children - a and b - are will no longer yield candidates

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T10:14:49Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:166** by [@mimowo](https://github.com/mimowo) — 2025-03-17T10:14:48Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
+	// yield more preemption target candidates. We use this set to
+	// determine our stopping condition: once the rootCohort is in
+	// the prunedCohorts list, we will not find any more
+	// preemption target candidates.
+	prunedClusterQueues sets.Set[*cache.ClusterQueueSnapshot]
+	prunedCohorts       sets.Set[*cache.CohortSnapshot]
+}
+
+func MakeClusterQueueOrdering(cq *cache.ClusterQueueSnapshot, candidates []*workload.Info) TargetClusterQueueOrdering {
+	t := TargetClusterQueueOrdering{
+		preemptorCq:        cq,
+		preemptorAncestors: sets.New[*cache.CohortSnapshot](),
+
+		clusterQueueToTarget: make(map[kueue.ClusterQueueReference][]*workload.Info),
+
+		prunedClusterQueues: sets.New[*cache.ClusterQueueSnapshot](),
+		prunedCohorts:       sets.New[*cache.CohortSnapshot](),
+	}
+
+	cohort := cq.Parent()
+	for cohort != nil {
+		t.preemptorAncestors.Insert(cohort)
+		cohort = cohort.Parent()
+	}
+
+	for _, candidate := range candidates {
+		t.clusterQueueToTarget[candidate.ClusterQueue] = append(t.clusterQueueToTarget[candidate.ClusterQueue], candidate)
+	}
+
+	return t
+}
+
+func (t *TargetClusterQueueOrdering) Iter() iter.Seq[*TargetClusterQueue] {
+	return func(yield func(v *TargetClusterQueue) bool) {
+		// handle CQ without Cohort case.
+		if !t.preemptorCq.HasParent() {
+			targetCq := &TargetClusterQueue{
+				ordering: t,
+				targetCq: t.preemptorCq,
+			}
+
+			for targetCq.HasWorkload() {
+				if !yield(targetCq) {
+					return
+				}
+			}
+		}
+
+		root := t.preemptorCq.Parent().Root()
+		// we stop once we have marked the root as pruned.
+		for !t.prunedCohorts.Has(root) {
+			targetCq := t.nextTarget(root)
+
+			// an iteration which just pruned some node(s).
+			if targetCq == nil {
+				continue
+			}
+			if !yield(targetCq) {
+				return
+			}
+		}
+	}
+}
+
+// DropQueue indicates that we should no longer
+// consider workloads from this Queue.
+func (t *TargetClusterQueueOrdering) DropQueue(cq *TargetClusterQueue) {
+	t.prunedClusterQueues.Insert(cq.targetCq)
+}
+
+func (t *TargetClusterQueueOrdering) onPathToPreemptorCQ(cohort *cache.CohortSnapshot) bool {
+	return t.preemptorAncestors.Has(cohort)
+}
+
+func (t *TargetClusterQueueOrdering) hasWorkload(cq *cache.ClusterQueueSnapshot) bool {
+	return len(t.clusterQueueToTarget[cq.GetName()]) > 0
+}
+
+// nextTarget is a recursive algorithm for finding the next
+// TargetClusterQueue.  It finds the child with the highest DRS,
+// returning it if it is a ClusterQueue, or entering the recursive
+// call if it is a Cohort.  The return of nil doesn't mean that there
+// are no more candidate ClusterQueues; an iteration may have only
+// pruned nodes from the tree.
+func (t *TargetClusterQueueOrdering) nextTarget(cohort *cache.CohortSnapshot) *TargetClusterQueue {
+	var highestCq *cache.ClusterQueueSnapshot = nil
+	highestCqDrs := -1
+	for _, cq := range cohort.ChildCQs() {
+		if t.prunedClusterQueues.Has(cq) {
+			continue
+		}
+
+		drs := cq.DominantResourceShare()
+		// we can't prune the preemptor ClusterQueue itself,
+		// until it runs out of candidates.
+		if (drs == 0 && cq != t.preemptorCq) || !t.hasWorkload(cq) {
+			t.prunedClusterQueues.Insert(cq)
+		} else if drs >= highestCqDrs {
+			highestCqDrs = drs
+			highestCq = cq
+		}
+	}
+
+	var highestCohort *cache.CohortSnapshot = nil
+	highestCohortDrs := -1
+	for _, cohort := range cohort.ChildCohorts() {
+		if t.prunedCohorts.Has(cohort) {
+			continue
+		}
+
+		drs := cohort.DominantResourceShare()
+
+		// we prune a Cohort when it is no longer borrowing
+		// (DRS=0). Even when not borrowing, we can't prune a
+		// Cohort on path from preemptor ClusterQueue to
+		// root, as there may be imbalance within some
```

  I see, in that case I would just suggest to rename `onPathToPreemptorCQ` -> `onPathFromRootToPreemptorCQ`. Yes, it is longer, but the "root" seems important to make the function name "readable" without looking into implementation. wdyt?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-17T10:16:48Z

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:42** by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T10:16:48Z
```diff
@@ -0,0 +1,59 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import "sigs.k8s.io/kueue/pkg/cache"
+
+// almostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node - the LeastCommonAncestor (LCA). While LCA
+// is always a Cohort, almostLCA may be a ClusterQueue or a Cohort.
+type almostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAs returns almostLCAs of (preemptor, target).
+func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
+	lca := getLCA(t)
+	return getAlmostLCA(t.ordering.preemptorCq, lca), getAlmostLCA(t.targetCq, lca)
+}
+
+// getLCA traverses from a ClusterQueue towards the root Cohort,
+// returning the first Cohort which contains the preemptor
+// ClusterQueue in its subtree.
+func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
+	cohort := t.targetCq.Parent()
+	for {
+		if t.ordering.onPathToPreemptorCQ(cohort) {
+			return cohort
+		}
+		cohort = cohort.Parent()
+	}
```

  Created #4644, as I think it would be best to solve all the cases at once

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T10:16:49Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:50** by [@mimowo](https://github.com/mimowo) — 2025-03-17T10:16:49Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"iter"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// TargetClusterQueueOrdering defines an ordering over ClusterQueues
+// to consider during preemption.  This is done by traversing from
+// root Cohort, selecting the child which satisfies the 3 conditions:
+// 1. has candidate workloads
+// 2. has DRS >= 0
+// 3. has the highest DominantResourceShare of nodes passing filters 1 and 2.
+//
+// The same TargetClusterQueue may be returned multiple times in a
+// row, if its AlmostLeastCommonAncestor's DominantResourceShare is
+// still the highest.
+//
+// To guarantee progression, TargetClusterQueue.PopWorkload, or
+// TargetClusterQueueOrdering.DropQueue must be called between each
+// entry returned.
+type TargetClusterQueueOrdering struct {
+	preemptorCq *cache.ClusterQueueSnapshot
+	// ancestor Cohorts of the preemptor ClusterQueue.
+	preemptorAncestors sets.Set[*cache.CohortSnapshot]
+
+	clusterQueueToTarget map[kueue.ClusterQueueReference][]*workload.Info
+
+	// pruned nodes are nodes which we are certain will never
```

  Thanks for the comment - this is quite useful as "pruned" by itself could be interpreted in various ways.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T10:23:19Z

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:42** by [@mimowo](https://github.com/mimowo) — 2025-03-17T10:23:19Z
```diff
@@ -0,0 +1,59 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import "sigs.k8s.io/kueue/pkg/cache"
+
+// almostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node - the LeastCommonAncestor (LCA). While LCA
+// is always a Cohort, almostLCA may be a ClusterQueue or a Cohort.
+type almostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAs returns almostLCAs of (preemptor, target).
+func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
+	lca := getLCA(t)
+	return getAlmostLCA(t.ordering.preemptorCq, lca), getAlmostLCA(t.targetCq, lca)
+}
+
+// getLCA traverses from a ClusterQueue towards the root Cohort,
+// returning the first Cohort which contains the preemptor
+// ClusterQueue in its subtree.
+func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
+	cohort := t.targetCq.Parent()
+	for {
+		if t.ordering.onPathToPreemptorCQ(cohort) {
+			return cohort
+		}
+		cohort = cohort.Parent()
+	}
```

  Oh I also opened: https://github.com/kubernetes-sigs/kueue/issues/4645 :) such a race condition :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T10:24:11Z

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:42** by [@mimowo](https://github.com/mimowo) — 2025-03-17T10:24:11Z
```diff
@@ -0,0 +1,59 @@
+// Copyright The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package fairsharing
+
+import "sigs.k8s.io/kueue/pkg/cache"
+
+// almostLCA is defined on two ClusterQueues, as the two nodes before
+// the lowest shared node - the LeastCommonAncestor (LCA). While LCA
+// is always a Cohort, almostLCA may be a ClusterQueue or a Cohort.
+type almostLCA interface {
+	DominantResourceShare() int
+}
+
+// getAlmostLCAs returns almostLCAs of (preemptor, target).
+func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
+	lca := getLCA(t)
+	return getAlmostLCA(t.ordering.preemptorCq, lca), getAlmostLCA(t.targetCq, lca)
+}
+
+// getLCA traverses from a ClusterQueue towards the root Cohort,
+// returning the first Cohort which contains the preemptor
+// ClusterQueue in its subtree.
+func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
+	cohort := t.targetCq.Parent()
+	for {
+		if t.ordering.onPathToPreemptorCQ(cohort) {
+			return cohort
+		}
+		cohort = cohort.Parent()
+	}
```

  closed mine as you were first

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-17T10:30:13Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:2** by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T10:30:13Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
```

  ```
  go test --cover -coverpkg=./pkg/scheduler/preemption/fairsharing  ./pkg/scheduler/preemption  -coverprofile=cover.out
  ok      sigs.k8s.io/kueue/pkg/scheduler/preemption      0.509s  coverage: 98.8% of statements in ./pkg/scheduler/preemption/fairsharing
  ```
  
  We are missing coverage only here: https://github.com/kubernetes-sigs/kueue/blob/4d15dbdc9fa9330ee302a77d524877f563c5cbef/pkg/scheduler/preemption/fairsharing/least_common_ancestor.go#L57
  
  We need a deeper cohort-tree to get this coverage. I will add this test now.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-17T10:55:27Z

- **pkg/scheduler/preemption/fairsharing/ordering.go:2** by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T10:55:27Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
```

  ```
  ok      sigs.k8s.io/kueue/pkg/scheduler/preemption      0.509s  coverage: 100.0% of statements in ./pkg/scheduler/preemption/fairsharing
  ```
