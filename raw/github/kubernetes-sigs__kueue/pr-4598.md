# PR #4598: document tls metrics

**Summary**: document tls metrics

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4598

**Last updated**: 2025-03-19T16:24:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `document-secure-metrics-enablement` → `main`
- **Created**: 2025-03-13T20:32:15Z
- **Updated**: 2025-03-19T16:24:33Z
- **Merged**: 2025-03-19T16:24:31Z
- **Closed**: 2025-03-19T16:24:31Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 7   **+145 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a walkthrough on how to enable helm chart for TLS metrics.
#### Which issue(s) this PR fixes:
Fixes: #4575
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->


#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-13T20:32:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 34b43ca7a15625bee7352b6a2737bbdca050226f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d9dd4a68ab900008c8e929 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-4598--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-4598--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTQ1OTgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.JCcaOi7PocgBDP1rTHVD5tobBJHm-J9Vj0oq-EFTGRM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-4598--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-14T07:46:32Z

/hold 
for moving the content as proposed in https://github.com/kubernetes-sigs/kueue/pull/4598#pullrequestreview-2684598959

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-14T20:21:41Z

Pushed a big update to this.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-19T00:22:40Z

/hold cancel

I moved these docs so this should be ready for review again.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-19T15:57:32Z

As we have a release tomorrow morning (Polish time) I will merge this as is, because the comments are only nits, and we want to make sure users can see it. @kannon92 please follow up with a PR for addressing the remaining comments though in a follow up (which may be cherry-picked after 0.11).
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-19T15:57:39Z

LGTM label has been added.  <details>Git tree hash: f9a87d52dc2015085e938eadde255bfc139c0e7d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-19T15:57:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4598#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4598#issuecomment-2737185999" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T07:45:55Z

Thank you for the PR! I think setting up cert-manager is super important.

I would just suggest to keep the installation guide tidy, it conceptually belongs to "Manage Kueue" tasks IMO. 

cc @tenzen-y

- **charts/kueue/README.md:51** by [@mimowo](https://github.com/mimowo) — 2025-03-14T07:44:37Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  I prefer to keep the installation guide short and clean and focused on installation, not optional configuration. I suggest to move it to https://kueue.sigs.k8s.io/docs/tasks/manage/, probably under "Setup using cert-manager". Then describe how to do it for both webhooks and metrics.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-14T13:18:29Z

- **charts/kueue/README.md:51** by [@kannon92](https://github.com/kannon92) — 2025-03-14T13:18:29Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  That section is all about managing kueue objects and not installation.
  
  I thought about putting in our install guide but the only section on helm links to this README.
  
  So I thought I should put it here as this is the only doc where we actually mention our helm installation in any detail.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T13:24:28Z

- **charts/kueue/README.md:51** by [@mimowo](https://github.com/mimowo) — 2025-03-14T13:24:28Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  I think some short link to the tasks page from here is ok, but it seems more appropriate to have a proper page on setting up TLS. Also, cert-manager is for webhooks, not just metrics so this page would need to be expanded, and I think too long. We don't document other features here, just the installation steps and the params.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-14T13:27:14Z

- **charts/kueue/README.md:51** by [@kannon92](https://github.com/kannon92) — 2025-03-14T13:27:14Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  So should I add a new task page for configuration installation?
  
  The persona for ManageKueue is really the batch admistration and not the installation person.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T13:33:23Z

- **charts/kueue/README.md:51** by [@mimowo](https://github.com/mimowo) — 2025-03-14T13:33:23Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  We don't distinguish these persons currently. Similarly, in the tasks for managing Kueue we instruct admins to install external frameworks such as MPIOperator, RayOperator, etc.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-14T13:33:34Z

- **charts/kueue/README.md:51** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-14T13:33:34Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  Batch admin indicates cluster administrator in our documentations.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-14T13:35:46Z

- **charts/kueue/README.md:51** by [@mimowo](https://github.com/mimowo) — 2025-03-14T13:35:46Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  Yes, I see it as a new tasks page under https://kueue.sigs.k8s.io/docs/tasks/manage/, some title ideas, might be "Setup TLS" or "Setup cert manager". I would envision the page to detail the setup for webhooks and metrics. Then, we can x-reference the pages for installation and the setup. IMO, but let me check with @tenzen-y ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-14T13:39:07Z

- **charts/kueue/README.md:51** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-14T13:39:06Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  I think we want to have 2 things
  
  1. Add CertManager documentation to https://kueue.sigs.k8s.io/docs/tasks/manage/
  2. Add a link to lead the detailed CertManger installation documentation to https://kueue.sigs.k8s.io/docs/installation/

### Review by [@rphillips](https://github.com/rphillips) (commented) — 2025-03-14T20:53:15Z

- **charts/kueue/README.md:75** by [@rphillips](https://github.com/rphillips) — 2025-03-14T20:53:15Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
+
+Kueue has support for third-party certificates.
+One can enable this by setting `enableCertManager` to true.
+This will use certManager to generate a secret, inject the CABundles and set up the tls.
+
+##### Protecting metrics endpoints with certificates
+
+Assuming one has set up cert manager and prometheus, Kueue's helm chart provides
+support for third-party certificates for metrics also.
+
+One can enable this by setting both `enablePrometheus` and `enableCertManager` to true.
+If one wants to require TLS certificates for metrics endpoints, one needs to provide a tlsConfig to the serviceMonitor.
+
+An example values file could be as follows:
+
+```yaml
+...
+metrics:
+  prometheusNamespace: monitoring
+# tls configs for serviceMonitor
+  serviceMonitor:
+    tlsConfig:
+      serverName: kueue-controller-manager-metrics-service.kueue-system.svc
+      insecureSkipVerify: false
```

  `insecureSkipVerify: false` is the default. This line could be deleted.

### Review by [@rphillips](https://github.com/rphillips) (commented) — 2025-03-14T20:53:42Z

- **site/content/en/docs/tasks/manage/installation/prometheus.md:65** by [@rphillips](https://github.com/rphillips) — 2025-03-14T20:53:42Z
```diff
@@ -0,0 +1,79 @@
+---
+title: "Installation with Prometheus"
+date: 2025-03-14
+weight: 2
+description: >
+  Prometheus Support
+---
+
+
+This page shows how you configure Kueue to use prometheus metrics.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure you the following conditions are set:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- Prometheus is [installed](https://prometheus-operator.dev/docs/getting-started/installation/)
+- Cert Manager can be optionally [installed](https://cert-manager.io/docs/installation/)
+
+Kueue supports either Kustomize or installation via a Helm chart.
+
+### Kustomize Installation
+
+1. Enable `prometheus` in `config/default/kustomization.yaml` and uncomment all sections with 'PROMETHEUS'.
+
+#### Kustomize Prometheus with certificates
+
+If one wants to require TLS verification for the metrics endpoint, follow the directions below.
+
+  1. Set `internalCertManagement.enable` to `false` in the kueue configuration.
+  2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
+  3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
+  4. To enable secure metrics with TLS protection, uncomment all sections with 'PROMETHEUS-WITH-CERTS'.
+
+### Helm Installation
+
+#### Prometheus installation
+
+Kueue can also supports helm deployment for Prometheus.
+
+1. Set `enablePrometheus` in your values.yaml file to true.
+
+#### Helm Prometheus with certificates
+
+If one wants to enable secure metrics,
+
+1. Provide values for the tlsConfig. See below for an example.
+2. Disable internal cert management in the kueue configuration.
+3. Set both `enableCertManager` and `enablePrometheus` to true.
+
+An example for your tlsConfig in the helm chart could be as follows:
+
+```yaml
+...
+metrics:
+  prometheusNamespace: monitoring
+# tls configs for serviceMonitor
+  serviceMonitor:
+    tlsConfig:
+      serverName: kueue-controller-manager-metrics-service.kueue-system.svc
+      insecureSkipVerify: false
```

  ditto

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-14T21:14:25Z

- **charts/kueue/README.md:75** by [@kannon92](https://github.com/kannon92) — 2025-03-14T21:14:25Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
+
+Kueue has support for third-party certificates.
+One can enable this by setting `enableCertManager` to true.
+This will use certManager to generate a secret, inject the CABundles and set up the tls.
+
+##### Protecting metrics endpoints with certificates
+
+Assuming one has set up cert manager and prometheus, Kueue's helm chart provides
+support for third-party certificates for metrics also.
+
+One can enable this by setting both `enablePrometheus` and `enableCertManager` to true.
+If one wants to require TLS certificates for metrics endpoints, one needs to provide a tlsConfig to the serviceMonitor.
+
+An example values file could be as follows:
+
+```yaml
+...
+metrics:
+  prometheusNamespace: monitoring
+# tls configs for serviceMonitor
+  serviceMonitor:
+    tlsConfig:
+      serverName: kueue-controller-manager-metrics-service.kueue-system.svc
+      insecureSkipVerify: false
```

  I actually wanted to drop this entirely.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-14T21:14:32Z

- **site/content/en/docs/tasks/manage/installation/prometheus.md:65** by [@kannon92](https://github.com/kannon92) — 2025-03-14T21:14:32Z
```diff
@@ -0,0 +1,79 @@
+---
+title: "Installation with Prometheus"
+date: 2025-03-14
+weight: 2
+description: >
+  Prometheus Support
+---
+
+
+This page shows how you configure Kueue to use prometheus metrics.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure you the following conditions are set:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- Prometheus is [installed](https://prometheus-operator.dev/docs/getting-started/installation/)
+- Cert Manager can be optionally [installed](https://cert-manager.io/docs/installation/)
+
+Kueue supports either Kustomize or installation via a Helm chart.
+
+### Kustomize Installation
+
+1. Enable `prometheus` in `config/default/kustomization.yaml` and uncomment all sections with 'PROMETHEUS'.
+
+#### Kustomize Prometheus with certificates
+
+If one wants to require TLS verification for the metrics endpoint, follow the directions below.
+
+  1. Set `internalCertManagement.enable` to `false` in the kueue configuration.
+  2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
+  3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
+  4. To enable secure metrics with TLS protection, uncomment all sections with 'PROMETHEUS-WITH-CERTS'.
+
+### Helm Installation
+
+#### Prometheus installation
+
+Kueue can also supports helm deployment for Prometheus.
+
+1. Set `enablePrometheus` in your values.yaml file to true.
+
+#### Helm Prometheus with certificates
+
+If one wants to enable secure metrics,
+
+1. Provide values for the tlsConfig. See below for an example.
+2. Disable internal cert management in the kueue configuration.
+3. Set both `enableCertManager` and `enablePrometheus` to true.
+
+An example for your tlsConfig in the helm chart could be as follows:
+
+```yaml
+...
+metrics:
+  prometheusNamespace: monitoring
+# tls configs for serviceMonitor
+  serviceMonitor:
+    tlsConfig:
+      serverName: kueue-controller-manager-metrics-service.kueue-system.svc
+      insecureSkipVerify: false
```

  ah. good call. Dropped.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T14:30:07Z

- **site/content/en/docs/tasks/manage/installation/_index.md:2** by [@mimowo](https://github.com/mimowo) — 2025-03-17T14:30:06Z
```diff
@@ -0,0 +1,7 @@
+---
+title: "Configuration Installation of Kueue"
```

  
  ```suggestion
  title: "Configure external cert-manager"
  ```
  I suggest to focus the new pare on cert-management only. We can later group some of the pages under 
  "Productizing aspects of Kueue" as proposed in https://github.com/kubernetes-sigs/kueue/issues/3087

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T14:31:58Z

- **site/content/en/docs/tasks/manage/installation/_index.md:2** by [@mimowo](https://github.com/mimowo) — 2025-03-17T14:31:58Z
```diff
@@ -0,0 +1,7 @@
+---
+title: "Configuration Installation of Kueue"
```

  Or alternatively, feel free to rename it as "Setup for productization" (grouping the new two sections), wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T14:32:56Z

- **charts/kueue/README.md:55** by [@mimowo](https://github.com/mimowo) — 2025-03-17T14:32:56Z
```diff
@@ -48,6 +48,12 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
+
+Kueue has support for third-party certificates.
+One can enable this by setting `enableCertManager` to true.
+This will use certManager to generate a secret, inject the CABundles and set up the tls.
```

  Add a link from this section to the docs which explain how to configure it.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-18T20:18:29Z

- **site/content/en/docs/tasks/manage/installation/_index.md:2** by [@kannon92](https://github.com/kannon92) — 2025-03-18T20:18:29Z
```diff
@@ -0,0 +1,7 @@
+---
+title: "Configuration Installation of Kueue"
```

  Is that under manage?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-18T20:42:27Z

- **charts/kueue/README.md:55** by [@kannon92](https://github.com/kannon92) — 2025-03-18T20:42:27Z
```diff
@@ -48,6 +48,12 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
+
+Kueue has support for third-party certificates.
+One can enable this by setting `enableCertManager` to true.
+This will use certManager to generate a secret, inject the CABundles and set up the tls.
```

  I went ahead and removed it.
  
  Linking to the site from the readme seems odd.. I'm not sure the links would work out.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-18T20:54:06Z

- **charts/kueue/README.md:51** by [@kannon92](https://github.com/kannon92) — 2025-03-18T20:54:06Z
```diff
@@ -48,6 +48,46 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
```

  done! Let me know what you think.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-18T20:54:25Z

- **site/content/en/docs/tasks/manage/installation/_index.md:2** by [@kannon92](https://github.com/kannon92) — 2025-03-18T20:54:24Z
```diff
@@ -0,0 +1,7 @@
+---
+title: "Configuration Installation of Kueue"
```

  I put this under tasks/manage/producization.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-18T20:54:54Z

- **charts/kueue/README.md:55** by [@kannon92](https://github.com/kannon92) — 2025-03-18T20:54:54Z
```diff
@@ -48,6 +48,12 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
+
+Kueue has support for third-party certificates.
+One can enable this by setting `enableCertManager` to true.
+This will use certManager to generate a secret, inject the CABundles and set up the tls.
```

  I tried to link it but I'm not sure the links will resolve until the PR merges.. As I link to the site which does not yet have these.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-19T13:39:22Z

- **site/content/en/docs/tasks/manage/installation/_index.md:2** by [@mimowo](https://github.com/mimowo) — 2025-03-19T13:39:22Z
```diff
@@ -0,0 +1,7 @@
+---
+title: "Configuration Installation of Kueue"
```

  sgtm, this is what I was thinking about

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-19T13:49:48Z

LGTM overall, just nits

- **site/content/en/docs/tasks/manage/productization/prometheus.md:49** by [@mimowo](https://github.com/mimowo) — 2025-03-19T13:45:46Z
```diff
@@ -0,0 +1,78 @@
+---
+title: "Configure Prometheus"
+date: 2025-03-14
+weight: 2
+description: >
+  Prometheus Support
+---
+
+
+This page shows how you configure Kueue to use prometheus metrics.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure you the following conditions are set:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- Prometheus is [installed](https://prometheus-operator.dev/docs/getting-started/installation/)
+- Cert Manager can be optionally [installed](https://cert-manager.io/docs/installation/)
+
+Kueue supports either Kustomize or installation via a Helm chart.
+
+### Kustomize Installation
+
+1. Enable `prometheus` in `config/default/kustomization.yaml` and uncomment all sections with 'PROMETHEUS'.
+
+#### Kustomize Prometheus with certificates
+
+If one wants to require TLS verification for the metrics endpoint, follow the directions below.
+
+  1. Set `internalCertManagement.enable` to `false` in the kueue configuration.
+  2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
+  3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
+  4. To enable secure metrics with TLS protection, uncomment all sections with 'PROMETHEUS-WITH-CERTS'.
+
+### Helm Installation
+
+#### Prometheus installation
+
+Kueue can also supports helm deployment for Prometheus.
+
+1. Set `enablePrometheus` in your values.yaml file to true.
+
+#### Helm Prometheus with certificates
+
+If one wants to enable secure metrics,
```

  ```suggestion
  If you want to secure the metrics endpoints with external certificates:
  ```

- **site/content/en/docs/tasks/manage/productization/prometheus.md:32** by [@mimowo](https://github.com/mimowo) — 2025-03-19T13:46:45Z
```diff
@@ -0,0 +1,78 @@
+---
+title: "Configure Prometheus"
+date: 2025-03-14
+weight: 2
+description: >
+  Prometheus Support
+---
+
+
+This page shows how you configure Kueue to use prometheus metrics.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure you the following conditions are set:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- Prometheus is [installed](https://prometheus-operator.dev/docs/getting-started/installation/)
+- Cert Manager can be optionally [installed](https://cert-manager.io/docs/installation/)
+
+Kueue supports either Kustomize or installation via a Helm chart.
+
+### Kustomize Installation
+
+1. Enable `prometheus` in `config/default/kustomization.yaml` and uncomment all sections with 'PROMETHEUS'.
+
+#### Kustomize Prometheus with certificates
+
+If one wants to require TLS verification for the metrics endpoint, follow the directions below.
```

  ```suggestion
  If you want to enable TLS verification for the metrics endpoint, follow the directions below.
  ```
  nit

- **site/content/en/docs/tasks/manage/productization/prometheus.md:52** by [@mimowo](https://github.com/mimowo) — 2025-03-19T13:47:54Z
```diff
@@ -0,0 +1,78 @@
+---
+title: "Configure Prometheus"
+date: 2025-03-14
+weight: 2
+description: >
+  Prometheus Support
+---
+
+
+This page shows how you configure Kueue to use prometheus metrics.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure you the following conditions are set:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- Prometheus is [installed](https://prometheus-operator.dev/docs/getting-started/installation/)
+- Cert Manager can be optionally [installed](https://cert-manager.io/docs/installation/)
+
+Kueue supports either Kustomize or installation via a Helm chart.
+
+### Kustomize Installation
+
+1. Enable `prometheus` in `config/default/kustomization.yaml` and uncomment all sections with 'PROMETHEUS'.
+
+#### Kustomize Prometheus with certificates
+
+If one wants to require TLS verification for the metrics endpoint, follow the directions below.
+
+  1. Set `internalCertManagement.enable` to `false` in the kueue configuration.
+  2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
+  3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
+  4. To enable secure metrics with TLS protection, uncomment all sections with 'PROMETHEUS-WITH-CERTS'.
+
+### Helm Installation
+
+#### Prometheus installation
+
+Kueue can also supports helm deployment for Prometheus.
+
+1. Set `enablePrometheus` in your values.yaml file to true.
+
+#### Helm Prometheus with certificates
+
+If one wants to enable secure metrics,
+
+1. Provide values for the tlsConfig. See below for an example.
+2. Disable internal cert management in the kueue configuration.
```

  ```suggestion
  2. Disable internal cert management in the kueue configuration (see [custom-configuration](https://kueue.sigs.k8s.io/docs/installation/#install-a-custom-configured-released-version) for more details).
  ```

- **site/content/en/docs/tasks/manage/productization/prometheus.md:51** by [@mimowo](https://github.com/mimowo) — 2025-03-19T13:48:39Z
```diff
@@ -0,0 +1,78 @@
+---
+title: "Configure Prometheus"
+date: 2025-03-14
+weight: 2
+description: >
+  Prometheus Support
+---
+
+
+This page shows how you configure Kueue to use prometheus metrics.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure you the following conditions are set:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- Prometheus is [installed](https://prometheus-operator.dev/docs/getting-started/installation/)
+- Cert Manager can be optionally [installed](https://cert-manager.io/docs/installation/)
+
+Kueue supports either Kustomize or installation via a Helm chart.
+
+### Kustomize Installation
+
+1. Enable `prometheus` in `config/default/kustomization.yaml` and uncomment all sections with 'PROMETHEUS'.
+
+#### Kustomize Prometheus with certificates
+
+If one wants to require TLS verification for the metrics endpoint, follow the directions below.
+
+  1. Set `internalCertManagement.enable` to `false` in the kueue configuration.
+  2. Comment out the `internalcert` folder in `config/default/kustomization.yaml`.
+  3. Enable `cert-manager` in `config/default/kustomization.yaml` and uncomment all sections with 'CERTMANAGER'.
+  4. To enable secure metrics with TLS protection, uncomment all sections with 'PROMETHEUS-WITH-CERTS'.
+
+### Helm Installation
+
+#### Prometheus installation
+
+Kueue can also supports helm deployment for Prometheus.
+
+1. Set `enablePrometheus` in your values.yaml file to true.
+
+#### Helm Prometheus with certificates
+
+If one wants to enable secure metrics,
+
+1. Provide values for the tlsConfig. See below for an example.
```

  ```suggestion
  1. Provide values for the tlsConfig, see the example below:
  ```
  And make the point as last, then it will naturally flow to the example

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-19T13:50:13Z

- **charts/kueue/README.md:55** by [@mimowo](https://github.com/mimowo) — 2025-03-19T13:50:13Z
```diff
@@ -48,6 +48,12 @@ NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
 kueue-controller-manager       1/1     1            1           7s
 ```
 
+##### Cert Manager
+
+Kueue has support for third-party certificates.
+One can enable this by setting `enableCertManager` to true.
+This will use certManager to generate a secret, inject the CABundles and set up the tls.
```

  I see, ok
