# PR #460: Add PodsReady condition if WaitForPodsReady is enabled

**Summary**: Add PodsReady condition if WaitForPodsReady is enabled

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/460

**Last updated**: 2023-03-18T18:45:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `pods-ready-condition` → `main`
- **Created**: 2022-12-05T09:59:41Z
- **Updated**: 2023-03-18T18:45:40Z
- **Merged**: 2022-12-13T19:59:36Z
- **Closed**: 2022-12-13T19:59:36Z
- **Labels**: `kind/feature`, `lgtm`, `tide/merge-method-squash`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: _none_
- **Changed files**: 9   **+554 / -29**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

This is the first part of implementing the solution designed in: https://github.com/kubernetes-sigs/kueue/pull/433. 
Admission of queued workloads will be blocked until the currently starting workload has the `PodsReady` condition (when `WaitForPodsReady` is configured at the Kueue level).

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of: https://github.com/kubernetes-sigs/kueue/issues/349

#### Special notes for your reviewer:

- the current description of the `WaitForPodsReady` field is not fully implemented - there is going to be a follow up
PR to implement blocking of admission

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-05T09:59:42Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@mimowo](https://github.com/mimowo) — 2022-12-08T17:40:27Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2022-12-09T11:23:44Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2022-12-09T14:51:46Z

@alculquicondor @ahg-g thanks for reviewing. Addressed all comments, but one question remains open about setting the condition back to false when suspending the job back. Do we currently have a scenario requiring it? PTAL.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T18:11:22Z

/lgtm
/label tide/merge-method-squash

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-12-12T18:12:54Z

/hold

I would like to take a look

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-13T18:23:31Z

/lgtm

Leaving the hold to @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-12-13T19:59:04Z

/hold cancel
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-13T19:59:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/460#issuecomment-1349611072" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/460#pullrequestreview-1213741086" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/460#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g,alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-06T20:36:30Z

- **Makefile:44** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T18:59:46Z
```diff
@@ -41,6 +41,7 @@ BUILDER_IMAGE ?= golang:1.18
 ENVTEST_K8S_VERSION = 1.24
 
 INTEGRATION_TARGET ?= ./test/integration/...
+INTEGRATION_EXTRA_OPTS ?=
```

  what are you using this for? To target a specific package, I usually set the INTEGRATION_TARGET variable in combination with `FIt` to target a specific test.

- **apis/kueue/v1alpha2/workload_types.go:117** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:05:50Z
```diff
@@ -113,6 +113,10 @@ const (
 	// WorkloadFinished means that the workload associated to the
 	// ResourceClaim finished running (failed or succeeded).
 	WorkloadFinished = "Finished"
+
+	// WorkloadPodsReady means that the first batch of Job pods (up to the
+	// specified parallelism and completions) is either ready or succeeded.
```

  That's specific to batch.Job, whereas Workload is supposed to be generic.
  
  Maybe you can say that at least `.spec.podSets[*].replicas` Pods are ready or have succeeded.

- **main.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:09:20Z
```diff
@@ -120,7 +120,7 @@ func main() {
 	// Cert won't be ready until manager starts, so start a goroutine here which
 	// will block until the cert is ready before setting up the controllers.
 	// Controllers who register after manager starts will start directly.
-	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName)
+	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName, waitForPodsReady(&cfg))
```

  Before this line grows too much:
  
  ```suggestion
  	go setupControllers(mgr, cCache, queues, certsReady, cfg)
  ```

- **apis/config/v1alpha2/configuration_types.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:09:48Z
```diff
@@ -45,6 +45,19 @@ type Configuration struct {
 
 	// InternalCertManagement is configuration for internalCertManagement
 	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+
+	// WaitForPodsReady is configuration for waitForPodsReady
+	WaitForPodsReady *WaitForPodsReady `json:"waitForPodsReady,omitempty"`
+}
+
+type WaitForPodsReady struct {
+	// Enable when true, indicates that each admitted workload
+	// blocks admission of other workloads in the cluster, until it is in the
+	// `PodsReady` condition. If false, all workloads start as soon as they are
+	// admitted and do not block admission of other workloads. The PodsReady
+	// condition is only added if this setting is enabled. If unspecified,
+	// it defaults to false.
+	Enable *bool `json:"enable,omitempty"`
```

  Because it defaults to false, this should be enough:
  
  ```suggestion
  	Enable bool `json:"enable,omitempty"`
  ```

- **pkg/controller/workload/job/job_controller.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:13:20Z
```diff
@@ -207,10 +219,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		log.Info("Job is running", "enabled", r.waitForPodsReady, "ready", jobPodsReady(&job))
+		// handle an unsuspended job when waitForPodsReady is enabled
+		if r.waitForPodsReady && jobPodsReady(&job) {
```

  Maybe this logic should be in line 249

- **pkg/controller/workload/job/job_controller.go:223** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:13:58Z
```diff
@@ -207,10 +219,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		log.Info("Job is running", "enabled", r.waitForPodsReady, "ready", jobPodsReady(&job))
```

  only log when the value changed? Also, at least V(3)

- **pkg/controller/workload/job/job_controller.go:269** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:15:13Z
```diff
@@ -219,11 +246,33 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
+	parallelism := *(jobSpec.Parallelism) // parallelism is always set as it is otherwise defaulted by k8s to 1
+	if jobSpec.Completions != nil {
+		if parallelism <= *jobSpec.Completions {
+			return parallelism
+		}
+		return *jobSpec.Completions
+	}
+	return parallelism
+}
+
+func podsReadyCount(jobStatus *batchv1.JobStatus) int32 {
```

  You can replace this with `pointer.Int32Deref` https://github.com/kubernetes/utils/blob/master/pointer/pointer.go#L86

- **pkg/controller/workload/job/job_controller.go:261** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:17:48Z
```diff
@@ -219,11 +246,33 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
+	parallelism := *(jobSpec.Parallelism) // parallelism is always set as it is otherwise defaulted by k8s to 1
+	if jobSpec.Completions != nil {
+		if parallelism <= *jobSpec.Completions {
```

  Is this possible? If so, we should fix `ConstructWorkloadFor`.

- **pkg/controller/workload/job/job_controller.go:212** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:20:42Z
```diff
@@ -184,7 +196,7 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 4. Handle a not finished job
```

  FYI, if a Job finishes fast, we might never add the `PodsReady` condition. Which is fine, but we need to consider that in the rest of the implementation.

- **pkg/controller/workload/job/job_controller.go:492** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:24:23Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
```

  If this function is not used anywhere else, just make it part of appendFinishedConditionIfNotExists

- **pkg/controller/workload/job/job_controller.go:505** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:24:31Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
+	message := "Job finished successfully"
+	if jobStatus == batchv1.JobFailed {
+		message = "Job failed"
+	}
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  "JobFinished",
+		Message: message,
+	}
+}
+
+func appendPodsReadyConditionIfNotExists(conds []metav1.Condition, jobStatus *batchv1.JobStatus) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generatePodsReadyCondition(jobStatus))
+}
+
+func generatePodsReadyCondition(jobStatus *batchv1.JobStatus) metav1.Condition {
```

  same suggestion for this condition

- **pkg/controller/workload/job/job_controller.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:25:18Z
```diff
@@ -207,10 +219,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		log.Info("Job is running", "enabled", r.waitForPodsReady, "ready", jobPodsReady(&job))
+		// handle an unsuspended job when waitForPodsReady is enabled
+		if r.waitForPodsReady && jobPodsReady(&job) {
```

  Also, we should add the condition with Status=false if we are still waiting.

- **pkg/controller/workload/job/job_controller.go:516** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:25:52Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
+	message := "Job finished successfully"
+	if jobStatus == batchv1.JobFailed {
+		message = "Job failed"
+	}
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  "JobFinished",
+		Message: message,
+	}
+}
+
+func appendPodsReadyConditionIfNotExists(conds []metav1.Condition, jobStatus *batchv1.JobStatus) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generatePodsReadyCondition(jobStatus))
+}
+
+func generatePodsReadyCondition(jobStatus *batchv1.JobStatus) metav1.Condition {
+	podsReadyCount := podsReadyCount(jobStatus)
+	message := fmt.Sprintf("All pods are ready or succeeded. Ready: %v, succeeded: %v.", podsReadyCount, jobStatus.Succeeded)
+	return metav1.Condition{
+		Type:    kueue.WorkloadPodsReady,
+		Status:  metav1.ConditionTrue,
+		Reason:  "PodsReady",
+		Message: message,
+	}
+}
+
+func appendConditionIfNotExists(conds []metav1.Condition, condition metav1.Condition) ([]metav1.Condition, bool) {
```

  you can use `k8s.io/apimachinery/pkg/api/meta.SetStatusCondition`

- **pkg/controller/workload/job/job_controller_test.go:24** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:28:14Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
```

  one block

- **pkg/controller/workload/job/job_controller_test.go:143** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:30:17Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+func TestJobPodsReady(t *testing.T) {
+	testcases := map[string]struct {
+		job  *batchv1.Job
+		want bool
+	}{
+		"parallelism = completions; no progress": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{},
+			},
+			want: false,
+		},
+		"parallelism = completions; not enough progress": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready:     pointer.Int32(1),
+					Succeeded: 1,
+				},
+			},
+			want: false,
+		},
+		"parallelism = completions; all ready": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready:     pointer.Int32(3),
+					Succeeded: 0,
+				},
+			},
+			want: true,
+		},
+		"parallelism = completions; some ready, some succeeded": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready:     pointer.Int32(2),
+					Succeeded: 1,
+				},
+			},
+			want: true,
+		},
+		"parallelism = completions; all succeeded": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Succeeded: 3,
+				},
+			},
+			want: true,
+		},
+		"parallelism < completions; reaching parallelism is enough": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(2),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(2),
+				},
+			},
+			want: true,
+		},
+		"parallelism > completions; reaching completions is enough": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(2),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(2),
+				},
+			},
+			want: true,
+		},
+		"parallelism specified only; not enough progress": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(2),
+				},
+			},
+			want: false,
+		},
+		"parallelism specified only; all ready": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(3),
+				},
+			},
+			want: true,
+		},
+	}
+
+	for name, tc := range testcases {
+		t.Run(name, func(t *testing.T) {
+			got := jobPodsReady(tc.job)
```

  We already have all the coverage in integration.

- **test/integration/controller/job/job_controller_test.go:293** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:31:31Z
```diff
@@ -265,6 +269,104 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(jobStatus *batchv1.JobStatus, wantCondition *metav1.Condition) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
```

  this shouldn't be needed

- **test/integration/controller/job/job_controller_test.go:323** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:33:31Z
```diff
@@ -265,6 +269,104 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(jobStatus *batchv1.JobStatus, wantCondition *metav1.Condition) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Create a job")
+			job := testing.MakeJob(jobName, jobNamespace).Parallelism(2).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{constants.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, lookupKey, createdWorkload)
+			}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
+			createdWorkload.Spec.Admission = &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+				PodSetFlavors: []kueue.PodSetFlavors{{
+					Flavors: map[corev1.ResourceName]string{
+						corev1.ResourceCPU: defaultFlavor.Name,
+					},
+				}},
+			}
+			gomega.Expect(k8sClient.Update(ctx, createdWorkload)).Should(gomega.Succeed())
+
+			ginkgo.By("Update the job status to similate its progress towards completion")
+			createdJob.Status = *jobStatus
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Verify the PodsReady condition is added if exepected")
```

  ```suggestion
  			ginkgo.By("Verify the PodsReady condition is added")
  ```

- **pkg/controller/workload/job/job_controller.go:507** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-06T20:36:26Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
+	message := "Job finished successfully"
+	if jobStatus == batchv1.JobFailed {
+		message = "Job failed"
+	}
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  "JobFinished",
+		Message: message,
+	}
+}
+
+func appendPodsReadyConditionIfNotExists(conds []metav1.Condition, jobStatus *batchv1.JobStatus) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generatePodsReadyCondition(jobStatus))
+}
+
+func generatePodsReadyCondition(jobStatus *batchv1.JobStatus) metav1.Condition {
+	podsReadyCount := podsReadyCount(jobStatus)
+	message := fmt.Sprintf("All pods are ready or succeeded. Ready: %v, succeeded: %v.", podsReadyCount, jobStatus.Succeeded)
```

  Use a simple message `Pods are ready or succeeded`. We don't want to generate a Workload update for each change in the Job.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T14:15:39Z

- **Makefile:44** by [@mimowo](https://github.com/mimowo) — 2022-12-08T14:15:39Z
```diff
@@ -41,6 +41,7 @@ BUILDER_IMAGE ?= golang:1.18
 ENVTEST_K8S_VERSION = 1.24
 
 INTEGRATION_TARGET ?= ./test/integration/...
+INTEGRATION_EXTRA_OPTS ?=
```

  I use it to pass `--focus` to also target a specific test, for example: `INTEGRATION_EXTRA_OPTS=--focus "Single job at different stages of progress towards completion"`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T14:25:57Z

- **apis/kueue/v1alpha2/workload_types.go:117** by [@mimowo](https://github.com/mimowo) — 2022-12-08T14:25:56Z
```diff
@@ -113,6 +113,10 @@ const (
 	// WorkloadFinished means that the workload associated to the
 	// ResourceClaim finished running (failed or succeeded).
 	WorkloadFinished = "Finished"
+
+	// WorkloadPodsReady means that the first batch of Job pods (up to the
+	// specified parallelism and completions) is either ready or succeeded.
```

  Adjusted

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T16:27:18Z

- **apis/config/v1alpha2/configuration_types.go:60** by [@mimowo](https://github.com/mimowo) — 2022-12-08T16:27:18Z
```diff
@@ -45,6 +45,19 @@ type Configuration struct {
 
 	// InternalCertManagement is configuration for internalCertManagement
 	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+
+	// WaitForPodsReady is configuration for waitForPodsReady
+	WaitForPodsReady *WaitForPodsReady `json:"waitForPodsReady,omitempty"`
+}
+
+type WaitForPodsReady struct {
+	// Enable when true, indicates that each admitted workload
+	// blocks admission of other workloads in the cluster, until it is in the
+	// `PodsReady` condition. If false, all workloads start as soon as they are
+	// admitted and do not block admission of other workloads. The PodsReady
+	// condition is only added if this setting is enabled. If unspecified,
+	// it defaults to false.
+	Enable *bool `json:"enable,omitempty"`
```

  Adjusted

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T16:35:44Z

- **pkg/controller/workload/job/job_controller.go:223** by [@mimowo](https://github.com/mimowo) — 2022-12-08T16:35:44Z
```diff
@@ -207,10 +219,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		log.Info("Job is running", "enabled", r.waitForPodsReady, "ready", jobPodsReady(&job))
```

  Dropped it entirely, it was left over from debugging

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T16:51:37Z

- **pkg/controller/workload/job/job_controller.go:225** by [@mimowo](https://github.com/mimowo) — 2022-12-08T16:51:37Z
```diff
@@ -207,10 +219,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		log.Info("Job is running", "enabled", r.waitForPodsReady, "ready", jobPodsReady(&job))
+		// handle an unsuspended job when waitForPodsReady is enabled
+		if r.waitForPodsReady && jobPodsReady(&job) {
```

  Good idea. It allows to also distinguish two situations: (1) the job controller has not inspected the Job, (2) the job controller already inspected the job but the pods are not ready.
  
  Is it possible that the workload is in PodsReady condition already, but a user increases the level of parallelism. If so, should we keep the condition or recompute it? Currently I recompute.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T16:54:01Z

- **pkg/controller/workload/job/job_controller.go:269** by [@mimowo](https://github.com/mimowo) — 2022-12-08T16:54:01Z
```diff
@@ -219,11 +246,33 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
+	parallelism := *(jobSpec.Parallelism) // parallelism is always set as it is otherwise defaulted by k8s to 1
+	if jobSpec.Completions != nil {
+		if parallelism <= *jobSpec.Completions {
+			return parallelism
+		}
+		return *jobSpec.Completions
+	}
+	return parallelism
+}
+
+func podsReadyCount(jobStatus *batchv1.JobStatus) int32 {
```

  I see, nice

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T16:54:32Z

- **pkg/controller/workload/job/job_controller.go:492** by [@mimowo](https://github.com/mimowo) — 2022-12-08T16:54:32Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T16:54:41Z

- **pkg/controller/workload/job/job_controller.go:505** by [@mimowo](https://github.com/mimowo) — 2022-12-08T16:54:40Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
+	message := "Job finished successfully"
+	if jobStatus == batchv1.JobFailed {
+		message = "Job failed"
+	}
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  "JobFinished",
+		Message: message,
+	}
+}
+
+func appendPodsReadyConditionIfNotExists(conds []metav1.Condition, jobStatus *batchv1.JobStatus) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generatePodsReadyCondition(jobStatus))
+}
+
+func generatePodsReadyCondition(jobStatus *batchv1.JobStatus) metav1.Condition {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T17:04:11Z

- **pkg/controller/workload/job/job_controller_test.go:143** by [@mimowo](https://github.com/mimowo) — 2022-12-08T17:04:10Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+func TestJobPodsReady(t *testing.T) {
+	testcases := map[string]struct {
+		job  *batchv1.Job
+		want bool
+	}{
+		"parallelism = completions; no progress": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{},
+			},
+			want: false,
+		},
+		"parallelism = completions; not enough progress": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready:     pointer.Int32(1),
+					Succeeded: 1,
+				},
+			},
+			want: false,
+		},
+		"parallelism = completions; all ready": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready:     pointer.Int32(3),
+					Succeeded: 0,
+				},
+			},
+			want: true,
+		},
+		"parallelism = completions; some ready, some succeeded": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready:     pointer.Int32(2),
+					Succeeded: 1,
+				},
+			},
+			want: true,
+		},
+		"parallelism = completions; all succeeded": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Succeeded: 3,
+				},
+			},
+			want: true,
+		},
+		"parallelism < completions; reaching parallelism is enough": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(2),
+					Completions: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(2),
+				},
+			},
+			want: true,
+		},
+		"parallelism > completions; reaching completions is enough": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+					Completions: pointer.Int32(2),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(2),
+				},
+			},
+			want: true,
+		},
+		"parallelism specified only; not enough progress": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(2),
+				},
+			},
+			want: false,
+		},
+		"parallelism specified only; all ready": {
+			job: &batchv1.Job{
+				Spec: batchv1.JobSpec{
+					Parallelism: pointer.Int32(3),
+				},
+				Status: batchv1.JobStatus{
+					Ready: pointer.Int32(3),
+				},
+			},
+			want: true,
+		},
+	}
+
+	for name, tc := range testcases {
+		t.Run(name, func(t *testing.T) {
+			got := jobPodsReady(tc.job)
```

  Not everything, for example in integration tests we only test parallelism=completions. Further, the integration tests are much slower, so I would rather decrease the number of scenarios in integration tests than deleting unit tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T17:04:37Z

- **pkg/controller/workload/job/job_controller.go:507** by [@mimowo](https://github.com/mimowo) — 2022-12-08T17:04:37Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
+	message := "Job finished successfully"
+	if jobStatus == batchv1.JobFailed {
+		message = "Job failed"
+	}
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  "JobFinished",
+		Message: message,
+	}
+}
+
+func appendPodsReadyConditionIfNotExists(conds []metav1.Condition, jobStatus *batchv1.JobStatus) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generatePodsReadyCondition(jobStatus))
+}
+
+func generatePodsReadyCondition(jobStatus *batchv1.JobStatus) metav1.Condition {
+	podsReadyCount := podsReadyCount(jobStatus)
+	message := fmt.Sprintf("All pods are ready or succeeded. Ready: %v, succeeded: %v.", podsReadyCount, jobStatus.Succeeded)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T17:07:47Z

- **test/integration/controller/job/job_controller_test.go:323** by [@mimowo](https://github.com/mimowo) — 2022-12-08T17:07:47Z
```diff
@@ -265,6 +269,104 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(jobStatus *batchv1.JobStatus, wantCondition *metav1.Condition) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+
+			ginkgo.By("Create a job")
+			job := testing.MakeJob(jobName, jobNamespace).Parallelism(2).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{constants.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, lookupKey, createdWorkload)
+			}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
+			createdWorkload.Spec.Admission = &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+				PodSetFlavors: []kueue.PodSetFlavors{{
+					Flavors: map[corev1.ResourceName]string{
+						corev1.ResourceCPU: defaultFlavor.Name,
+					},
+				}},
+			}
+			gomega.Expect(k8sClient.Update(ctx, createdWorkload)).Should(gomega.Succeed())
+
+			ginkgo.By("Update the job status to similate its progress towards completion")
+			createdJob.Status = *jobStatus
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Verify the PodsReady condition is added if exepected")
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T17:09:29Z

- **pkg/controller/workload/job/job_controller_test.go:24** by [@mimowo](https://github.com/mimowo) — 2022-12-08T17:09:28Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-08T17:38:36Z

- **test/integration/controller/job/job_controller_test.go:293** by [@mimowo](https://github.com/mimowo) — 2022-12-08T17:38:36Z
```diff
@@ -265,6 +269,104 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(jobStatus *batchv1.JobStatus, wantCondition *metav1.Condition) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
```

  indeed

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-12-08T20:34:10Z

- **pkg/controller/workload/job/job_controller.go:223** by [@ahg-g](https://github.com/ahg-g) — 2022-12-08T19:18:52Z
```diff
@@ -207,10 +219,23 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		if r.waitForPodsReady { // handle an unsuspended job when waitForPodsReady is enabled
```

  nit: I don't think we use this format in the code, please put comments on their own lines

- **pkg/controller/workload/job/job_controller.go:222** by [@ahg-g](https://github.com/ahg-g) — 2022-12-08T19:41:14Z
```diff
@@ -207,10 +219,23 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
```

  In the preemption case, the job is stopped after it was started, and we may start it again in the future. We need to set the condition back to false, if it exists, every time we stop the job so that it gets correctly set the next time we start the job again.

- **pkg/controller/workload/job/job_controller.go:252** by [@ahg-g](https://github.com/ahg-g) — 2022-12-08T20:28:28Z
```diff
@@ -219,11 +244,30 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
```

  just "podsReady"

- **pkg/controller/workload/job/job_controller.go:256** by [@ahg-g](https://github.com/ahg-g) — 2022-12-08T20:28:53Z
```diff
@@ -219,11 +244,30 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
```

  ditto, remove the job prefix

- **pkg/controller/workload/job/job_controller.go:269** by [@ahg-g](https://github.com/ahg-g) — 2022-12-08T20:32:46Z
```diff
@@ -219,11 +246,33 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
+	parallelism := *(jobSpec.Parallelism) // parallelism is always set as it is otherwise defaulted by k8s to 1
+	if jobSpec.Completions != nil {
+		if parallelism <= *jobSpec.Completions {
+			return parallelism
+		}
+		return *jobSpec.Completions
+	}
+	return parallelism
+}
+
+func podsReadyCount(jobStatus *batchv1.JobStatus) int32 {
```

  too many short functions :) I would remove this function and just use `Int32Deref` directly in jobsPodReady

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T11:03:34Z

- **pkg/controller/workload/job/job_controller.go:223** by [@mimowo](https://github.com/mimowo) — 2022-12-09T11:03:34Z
```diff
@@ -207,10 +219,23 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		if r.waitForPodsReady { // handle an unsuspended job when waitForPodsReady is enabled
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T11:03:41Z

- **pkg/controller/workload/job/job_controller.go:252** by [@mimowo](https://github.com/mimowo) — 2022-12-09T11:03:41Z
```diff
@@ -219,11 +244,30 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T11:04:37Z

- **pkg/controller/workload/job/job_controller.go:256** by [@mimowo](https://github.com/mimowo) — 2022-12-09T11:04:37Z
```diff
@@ -219,11 +244,30 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
```

  Inlined the entire function as it wasn't called from other places

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T11:37:52Z

- **pkg/controller/workload/job/job_controller.go:269** by [@mimowo](https://github.com/mimowo) — 2022-12-09T11:37:52Z
```diff
@@ -219,11 +246,33 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
+	parallelism := *(jobSpec.Parallelism) // parallelism is always set as it is otherwise defaulted by k8s to 1
+	if jobSpec.Completions != nil {
+		if parallelism <= *jobSpec.Completions {
+			return parallelism
+		}
+		return *jobSpec.Completions
+	}
+	return parallelism
+}
+
+func podsReadyCount(jobStatus *batchv1.JobStatus) int32 {
```

  Indeed, inlined this function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T11:38:44Z

- **pkg/controller/workload/job/job_controller.go:261** by [@mimowo](https://github.com/mimowo) — 2022-12-09T11:38:44Z
```diff
@@ -219,11 +246,33 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+func jobPodsReady(job *batchv1.Job) bool {
+	return job.Status.Succeeded+podsReadyCount(&job.Status) >= jobPodsReadyExpectedCount(&job.Spec)
+}
+
+func jobPodsReadyExpectedCount(jobSpec *batchv1.JobSpec) int32 {
+	parallelism := *(jobSpec.Parallelism) // parallelism is always set as it is otherwise defaulted by k8s to 1
+	if jobSpec.Completions != nil {
+		if parallelism <= *jobSpec.Completions {
```

  Yes it is, fixed `ConstructWorkloadFor` to reuse the `podsCount` function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T11:49:05Z

- **Makefile:44** by [@mimowo](https://github.com/mimowo) — 2022-12-09T11:49:05Z
```diff
@@ -41,6 +41,7 @@ BUILDER_IMAGE ?= golang:1.18
 ENVTEST_K8S_VERSION = 1.24
 
 INTEGRATION_TARGET ?= ./test/integration/...
+INTEGRATION_EXTRA_OPTS ?=
```

  Withdrawn from the change for now as I can also use FIt of FDescribeTable . Let me know what you think, we could also have INTEGRATION_FOCUS dedicated param for focus.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T14:04:02Z

- **pkg/controller/workload/job/job_controller.go:516** by [@mimowo](https://github.com/mimowo) — 2022-12-09T14:04:01Z
```diff
@@ -433,27 +482,49 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 }
 
 func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generateFinishedCondition(jobStatus))
+}
+
+func generateFinishedCondition(jobStatus batchv1.JobConditionType) metav1.Condition {
+	message := "Job finished successfully"
+	if jobStatus == batchv1.JobFailed {
+		message = "Job failed"
+	}
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  "JobFinished",
+		Message: message,
+	}
+}
+
+func appendPodsReadyConditionIfNotExists(conds []metav1.Condition, jobStatus *batchv1.JobStatus) ([]metav1.Condition, bool) {
+	return appendConditionIfNotExists(conds, generatePodsReadyCondition(jobStatus))
+}
+
+func generatePodsReadyCondition(jobStatus *batchv1.JobStatus) metav1.Condition {
+	podsReadyCount := podsReadyCount(jobStatus)
+	message := fmt.Sprintf("All pods are ready or succeeded. Ready: %v, succeeded: %v.", podsReadyCount, jobStatus.Succeeded)
+	return metav1.Condition{
+		Type:    kueue.WorkloadPodsReady,
+		Status:  metav1.ConditionTrue,
+		Reason:  "PodsReady",
+		Message: message,
+	}
+}
+
+func appendConditionIfNotExists(conds []metav1.Condition, condition metav1.Condition) ([]metav1.Condition, bool) {
```

  Good idea, used. Also applied it to add the `WorkloadFinished` condition for consistency.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T14:40:53Z

- **pkg/controller/workload/job/job_controller.go:222** by [@mimowo](https://github.com/mimowo) — 2022-12-09T14:40:53Z
```diff
@@ -207,10 +219,23 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
```

  I see. I'm wondering if this is an issue for now - before we have preemption? Maybe this could be a dedicated PR once we have preemption.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T14:46:17Z

- **pkg/controller/workload/job/job_controller_test.go:24** by [@mimowo](https://github.com/mimowo) — 2022-12-09T14:46:17Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
```

  Reverted. Actually, verification started to fail once I joined. It seems the newline separates the k8s and kueue packages. An analogous line is present in other go files.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-09T15:48:44Z

- **main.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T15:48:43Z
```diff
@@ -120,7 +120,7 @@ func main() {
 	// Cert won't be ready until manager starts, so start a goroutine here which
 	// will block until the cert is ready before setting up the controllers.
 	// Controllers who register after manager starts will start directly.
-	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName)
+	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName, waitForPodsReady(&cfg))
```

  This was not addressed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T15:52:46Z

- **main.go:123** by [@mimowo](https://github.com/mimowo) — 2022-12-09T15:52:45Z
```diff
@@ -120,7 +120,7 @@ func main() {
 	// Cert won't be ready until manager starts, so start a goroutine here which
 	// will block until the cert is ready before setting up the controllers.
 	// Controllers who register after manager starts will start directly.
-	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName)
+	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName, waitForPodsReady(&cfg))
```

  Ups, going to address now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T16:06:05Z

- **main.go:123** by [@mimowo](https://github.com/mimowo) — 2022-12-09T16:06:05Z
```diff
@@ -120,7 +120,7 @@ func main() {
 	// Cert won't be ready until manager starts, so start a goroutine here which
 	// will block until the cert is ready before setting up the controllers.
 	// Controllers who register after manager starts will start directly.
-	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName)
+	go setupControllers(mgr, cCache, queues, certsReady, cfg.ManageJobsWithoutQueueName, waitForPodsReady(&cfg))
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-09T16:12:50Z

- **main.go:252** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T15:49:41Z
```diff
@@ -245,3 +247,7 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 
 	return options, cfg
 }
+
+func waitForPodsReady(cfg *config.Configuration) bool {
+	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable
```

  put this in `setupControllers` instead, to avoid the one line function

- **pkg/controller/workload/job/job_controller.go:222** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T15:51:56Z
```diff
@@ -207,10 +219,23 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
```

  I would add it already. It's coming soon.
  Also, we concluded that it's better to do this after checking admission.

- **pkg/controller/workload/job/job_controller.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T15:54:14Z
```diff
@@ -207,10 +219,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		log.Info("Job is running", "enabled", r.waitForPodsReady, "ready", jobPodsReady(&job))
+		// handle an unsuspended job when waitForPodsReady is enabled
+		if r.waitForPodsReady && jobPodsReady(&job) {
```

  yes, that's possible.
  
  Recomputing is fine.
  
  Also, opened #467 to think about this long term

- **pkg/controller/workload/job/job_controller.go:257** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T15:55:16Z
```diff
@@ -219,11 +247,17 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+// podsReady checks if all pods are ready or succeeded
+func podsReady(job *batchv1.Job) bool {
+	podsReadyCount := pointer.Int32Deref(job.Status.Ready, 0)
```

  nit
  ```suggestion
  	ready := pointer.Int32Deref(job.Status.Ready, 0)
  ```

- **pkg/controller/workload/job/job_controller_test.go:24** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T16:04:01Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package job
+
+import (
+	"testing"
+
+	batchv1 "k8s.io/api/batch/v1"
+
+	"sigs.k8s.io/kueue/pkg/util/pointer"
```

  ah, yes, this is a kueue package.

- **test/integration/controller/job/job_controller_test.go:337** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T16:11:36Z
```diff
@@ -265,6 +269,186 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		jobStatus       batchv1.JobStatus
+		beforeCondition *metav1.Condition
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+
+			ginkgo.By("Create a job")
+			job := testing.MakeJob(jobName, jobNamespace).Parallelism(2).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{constants.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, lookupKey, createdWorkload)
+			}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
+			createdWorkload.Spec.Admission = &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+				PodSetFlavors: []kueue.PodSetFlavors{{
+					Flavors: map[corev1.ResourceName]string{
+						corev1.ResourceCPU: defaultFlavor.Name,
+					},
+				}},
+			}
+			gomega.Expect(k8sClient.Update(ctx, createdWorkload)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdWorkload)).Should(gomega.Succeed())
+
+			if podsReadyTestSpec.beforeCondition != nil {
+				ginkgo.By("Update the workload status")
+				apimeta.SetStatusCondition(&createdWorkload.Status.Conditions, *podsReadyTestSpec.beforeCondition)
+				gomega.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdWorkload)).Should(gomega.Succeed())
+			}
+
+			ginkgo.By("Update the job status to simulate its progress towards completion")
+			createdJob.Status = podsReadyTestSpec.jobStatus
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Verify the PodsReady condition is added")
+			gomega.Eventually(func() error {
```

  ```suggestion
  			gomega.Eventually(func() metav1.Condition {
  ```
  
  then you can use `Should(gomega.BeComparableTo(wantCondition))`. This uses `cmp.Diff` underneath

- **test/integration/controller/job/job_controller_test.go:315** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T16:12:46Z
```diff
@@ -265,6 +269,186 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		jobStatus       batchv1.JobStatus
+		beforeCondition *metav1.Condition
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+
+			ginkgo.By("Create a job")
+			job := testing.MakeJob(jobName, jobNamespace).Parallelism(2).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{constants.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, lookupKey, createdWorkload)
+			}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
+			createdWorkload.Spec.Admission = &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
```

  ```suggestion
  				ClusterQueue: kueue.ClusterQueueReference("foo"),
  ```
  
  No need for an actual CQ object.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T16:33:35Z

- **pkg/controller/workload/job/job_controller.go:257** by [@mimowo](https://github.com/mimowo) — 2022-12-09T16:33:35Z
```diff
@@ -219,11 +247,17 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	// 4.4 workload is admitted and job is running, nothing to do.
+	// workload is admitted and job is running, nothing to do.
 	log.V(3).Info("Job running with admitted workload, nothing to do")
 	return ctrl.Result{}, nil
 }
 
+// podsReady checks if all pods are ready or succeeded
+func podsReady(job *batchv1.Job) bool {
+	podsReadyCount := pointer.Int32Deref(job.Status.Ready, 0)
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T16:33:41Z

- **main.go:252** by [@mimowo](https://github.com/mimowo) — 2022-12-09T16:33:41Z
```diff
@@ -245,3 +247,7 @@ func apply(configFile string) (ctrl.Options, config.Configuration) {
 
 	return options, cfg
 }
+
+func waitForPodsReady(cfg *config.Configuration) bool {
+	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T16:44:31Z

- **test/integration/controller/job/job_controller_test.go:315** by [@mimowo](https://github.com/mimowo) — 2022-12-09T16:44:31Z
```diff
@@ -265,6 +269,186 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		jobStatus       batchv1.JobStatus
+		beforeCondition *metav1.Condition
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+
+			ginkgo.By("Create a job")
+			job := testing.MakeJob(jobName, jobNamespace).Parallelism(2).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{constants.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, lookupKey, createdWorkload)
+			}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
+			createdWorkload.Spec.Admission = &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T16:44:39Z

- **test/integration/controller/job/job_controller_test.go:337** by [@mimowo](https://github.com/mimowo) — 2022-12-09T16:44:39Z
```diff
@@ -265,6 +269,186 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
+	type podsReadyTestSpec struct {
+		jobStatus       batchv1.JobStatus
+		beforeCondition *metav1.Condition
+		wantCondition   *metav1.Condition
+	}
+
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(workloadjob.WithWaitForPodsReady(true)),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.DescribeTable("Single job at different stages of progress towards completion",
+		func(podsReadyTestSpec podsReadyTestSpec) {
+			ginkgo.By("Create a cluster queue")
+			defaultFlavor := testing.MakeResourceFlavor("default").Label(labelKey, "default").Obj()
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).Should(gomega.Succeed())
+			clusterQueue := testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).Obj()
+
+			ginkgo.By("Create a job")
+			job := testing.MakeJob(jobName, jobNamespace).Parallelism(2).Obj()
+			jobQueueName := "test-queue"
+			job.Annotations = map[string]string{constants.QueueAnnotation: jobQueueName}
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			lookupKey := types.NamespacedName{Name: jobName, Namespace: jobNamespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Admit the workload created for the job")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func() error {
+				return k8sClient.Get(ctx, lookupKey, createdWorkload)
+			}, framework.Timeout, framework.Interval).Should(gomega.Succeed())
+			createdWorkload.Spec.Admission = &kueue.Admission{
+				ClusterQueue: kueue.ClusterQueueReference(clusterQueue.Name),
+				PodSetFlavors: []kueue.PodSetFlavors{{
+					Flavors: map[corev1.ResourceName]string{
+						corev1.ResourceCPU: defaultFlavor.Name,
+					},
+				}},
+			}
+			gomega.Expect(k8sClient.Update(ctx, createdWorkload)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdWorkload)).Should(gomega.Succeed())
+
+			if podsReadyTestSpec.beforeCondition != nil {
+				ginkgo.By("Update the workload status")
+				apimeta.SetStatusCondition(&createdWorkload.Status.Conditions, *podsReadyTestSpec.beforeCondition)
+				gomega.Expect(k8sClient.Status().Update(ctx, createdWorkload)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdWorkload)).Should(gomega.Succeed())
+			}
+
+			ginkgo.By("Update the job status to simulate its progress towards completion")
+			createdJob.Status = podsReadyTestSpec.jobStatus
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("Verify the PodsReady condition is added")
+			gomega.Eventually(func() error {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-09T16:54:54Z

- **pkg/controller/workload/job/job_controller.go:225** by [@mimowo](https://github.com/mimowo) — 2022-12-09T16:54:54Z
```diff
@@ -207,10 +219,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
+		log.Info("Job is running", "enabled", r.waitForPodsReady, "ready", jobPodsReady(&job))
+		// handle an unsuspended job when waitForPodsReady is enabled
+		if r.waitForPodsReady && jobPodsReady(&job) {
```

  Got it. Pushed recomputing. Also moved the logic out of the unsuspended job branch. Now a suspended job will also go through the update.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-09T18:34:45Z

- **test/integration/framework/framework.go:149** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T18:23:45Z
```diff
@@ -146,6 +146,15 @@ func DeleteWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) er
 	return nil
 }
 
+func GetConditionByType(list []metav1.Condition, cType string) *metav1.Condition {
```

  Just use `apimeta.FindStatusCondition`

- **pkg/workload/workload.go:243** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T18:24:39Z
```diff
@@ -239,6 +239,10 @@ func UpdateStatusIfChanged(ctx context.Context,
 }
 
 func InCondition(w *kueue.Workload, condition string) bool {
+	return InConditionWithStatus(w, condition, metav1.ConditionTrue)
```

  followup clean up: use api/meta

- **pkg/controller/workload/job/job_controller.go:200** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T18:31:28Z
```diff
@@ -170,11 +183,27 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
+			return ctrl.Result{}, nil
+		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(3).Info("Handling an unsuspended job when waitForPodsReady is enabled")
```

  V(5) at least.

- **pkg/controller/workload/job/job_controller.go:199** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T18:33:59Z
```diff
@@ -170,11 +183,27 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
+			return ctrl.Result{}, nil
+		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
```

  you are always returning if you enter this branch. We should continue doing the other checks on the Job

- **pkg/controller/workload/job/job_controller.go:200** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-09T18:34:43Z
```diff
@@ -170,11 +183,27 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
+			return ctrl.Result{}, nil
+		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(3).Info("Handling an unsuspended job when waitForPodsReady is enabled")
```

  But also, you are not actually checking if the job is suspended.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T08:01:36Z

- **test/integration/framework/framework.go:149** by [@mimowo](https://github.com/mimowo) — 2022-12-12T08:01:35Z
```diff
@@ -146,6 +146,15 @@ func DeleteWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) er
 	return nil
 }
 
+func GetConditionByType(list []metav1.Condition, cType string) *metav1.Condition {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T08:01:45Z

- **pkg/workload/workload.go:243** by [@mimowo](https://github.com/mimowo) — 2022-12-12T08:01:45Z
```diff
@@ -239,6 +239,10 @@ func UpdateStatusIfChanged(ctx context.Context,
 }
 
 func InCondition(w *kueue.Workload, condition string) bool {
+	return InConditionWithStatus(w, condition, metav1.ConditionTrue)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T08:02:10Z

- **pkg/controller/workload/job/job_controller.go:200** by [@mimowo](https://github.com/mimowo) — 2022-12-12T08:02:09Z
```diff
@@ -170,11 +183,27 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
+			return ctrl.Result{}, nil
+		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(3).Info("Handling an unsuspended job when waitForPodsReady is enabled")
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T08:02:22Z

- **pkg/controller/workload/job/job_controller.go:199** by [@mimowo](https://github.com/mimowo) — 2022-12-12T08:02:22Z
```diff
@@ -170,11 +183,27 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
+			return ctrl.Result{}, nil
+		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T08:04:02Z

- **pkg/controller/workload/job/job_controller.go:222** by [@mimowo](https://github.com/mimowo) — 2022-12-12T08:04:01Z
```diff
@@ -207,10 +219,23 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		}
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
+	} else {
```

  Done, also added an integration test case in job_controller "Job suspended; PodsReady=True before" which simulates the situation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T15:01:20Z

/approve

- **pkg/controller/workload/job/job_controller.go:480** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T14:58:14Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  `&& !jobSuspended(job)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T15:46:55Z

- **pkg/controller/workload/job/job_controller.go:480** by [@mimowo](https://github.com/mimowo) — 2022-12-12T15:46:55Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  I was thinking about it. However, a suspended job does not need to block admission of new workloads as its pods are terminating, so will not deadlock. Also, a suspended job will eventually reach the PodsReady=False condition as its pods terminate (unless unsuspended quickly, but then we can save the two requests). 
  
  Also, there might be a slight penalty to throuthput by an additional request to change the condition to `PodsReady=False` when a new workload is about to get admitted. WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T16:25:15Z

- **pkg/controller/workload/job/job_controller.go:480** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:25:15Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  >  a suspended job does not need to block admission of new workloads 
  
  That is true, but for that, we should be looking at `Workload.spec.admission != nil`.
  
  > Also, a suspended job will eventually reach the PodsReady=False condition as its pods terminate (unless unsuspended quickly, but then we can save the two requests).
  
  Since we are unsuspending after updating the jobs ready condition, I think what you have is fine.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T16:28:06Z

- **pkg/controller/workload/job/job_controller.go:480** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T16:28:05Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  Or maybe not? When the job has `suspend=true`, the Job status might still be out-of-date. If we unsuspend at that point, we might mark a Job as ready and `suspend=false`, when in reality there might be pods finishing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T17:05:45Z

- **pkg/controller/workload/job/job_controller.go:480** by [@mimowo](https://github.com/mimowo) — 2022-12-12T17:05:45Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  When the pods are finishing, then the Kueue Job reconciler will set `PodsReady=False` in response do decreased Ready count, but indeed it might be lagging a little bit. So what the scenario would be?
  - workload A is admitted and the job A is unsuspended and gets all pods running (`PodsReady=True`)
  - workload B preempts A, A gets suspended and removed from admitted workloads
  - workload B completes very quickly, so that A is still in `PodsReady=True` ready to be re-admitted
  - workload A gets readmitted with `PodsReady=True` even though it has a terminating pod now
  - workload C gets admitted even though workload A still needs to restart some of its pods - both A and C are starting their pods at the same time.
  
  However, I think the same scenario may theoretically still happen even if we set `PodsReady=False` when suspending. Still, it is unlikely, but it could happen that workload A gets re-admitted within the Job controller single syncJob pass thus, the pods will start to be terminated by invoking https://github.com/kubernetes/kubernetes/blob/0e19bbb91644885a6db38a77ea3d697730269802/pkg/controller/job/job_controller.go#L1416.
  
  Both scenarios are very unlikely and with this feature we don't aim to give a full guarantee of no concurrent pods scheduling - this may still happen in more likely case of pod restart.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T17:07:23Z

- **pkg/controller/workload/job/job_controller.go:480** by [@mimowo](https://github.com/mimowo) — 2022-12-12T17:07:23Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  Btw. I have just realized we don't add the Pod Disruption condition to pods deleted by the Job controller in reaction to suspending a workload. It seems like an omission / bug? I will create a bug if this is the case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T17:17:57Z

- **pkg/controller/workload/job/job_controller.go:480** by [@mimowo](https://github.com/mimowo) — 2022-12-12T17:17:57Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  Actually, after a second thought, I see that by setting `PodsReady=False` I would eliminate the risk of the scenario described above by being more conservative.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T17:20:07Z

- **pkg/controller/workload/job/job_controller.go:480** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T17:20:07Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  It's not a "disruption". The user intention (setting `.spec.suspend=True`) is to terminate those pods.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T17:29:53Z

- **pkg/controller/workload/job/job_controller.go:480** by [@mimowo](https://github.com/mimowo) — 2022-12-12T17:29:53Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  Hm, but two points: 
  - this mechanism will be used for workload preemption which is not necessarily user-driven. It is conceptually very similar to pod preemption (for which we add disruption)
  - when the user sends the eviction API we also add the disruption condition (for example to drain a node). Deliberate cluster administrator actions is one of the reasons of pod disruptions: https://kubernetes.io/docs/concepts/workloads/pods/disruptions/

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-12T17:36:12Z

- **pkg/controller/workload/job/job_controller.go:480** by [@mimowo](https://github.com/mimowo) — 2022-12-12T17:36:12Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  Pushed the suggested change PTAL.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T18:09:57Z

- **pkg/controller/workload/job/job_controller.go:480** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T18:09:57Z
```diff
@@ -432,29 +465,41 @@ func ConstructWorkloadFor(ctx context.Context, client client.Client,
 	return w, nil
 }
 
-func appendFinishedConditionIfNotExists(conds []metav1.Condition, jobStatus batchv1.JobConditionType) ([]metav1.Condition, bool) {
-	for i, c := range conds {
-		if c.Type == kueue.WorkloadFinished {
-			if c.Status == metav1.ConditionTrue {
-				return conds, false
-			}
-			conds = append(conds[:i], conds[i+1:]...)
-			break
-		}
+func podsCount(jobSpec *batchv1.JobSpec) int32 {
+	// parallelism is always set as it is otherwise defaulted by k8s to 1
+	podsCount := *(jobSpec.Parallelism)
+	if jobSpec.Completions != nil && *jobSpec.Completions < podsCount {
+		podsCount = *jobSpec.Completions
+	}
+	return podsCount
+}
+
+func generatePodsReadyCondition(job *batchv1.Job) metav1.Condition {
+	conditionStatus := metav1.ConditionFalse
+	message := "Not all pods are ready or succeeded"
+	if podsReady(job) {
```

  Maybe worth creating an issue for the discussion around job suspension and the disruption condition. Although, one reason why I thought is not a priority for Jobs is because these pods would get the finalizer removed before a condition is added and they wouldn't affect the failure policy.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-12-12T23:13:37Z

- **apis/config/v1alpha2/configuration_types.go:49** by [@ahg-g](https://github.com/ahg-g) — 2022-12-12T18:21:14Z
```diff
@@ -45,6 +45,18 @@ type Configuration struct {
 
 	// InternalCertManagement is configuration for internalCertManagement
 	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+
+	// WaitForPodsReady is configuration for waitForPodsReady
```

  not useful docs :)

- **apis/config/v1alpha2/configuration_types.go:56** by [@ahg-g](https://github.com/ahg-g) — 2022-12-12T18:22:45Z
```diff
@@ -45,6 +45,18 @@ type Configuration struct {
 
 	// InternalCertManagement is configuration for internalCertManagement
 	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+
+	// WaitForPodsReady is configuration for waitForPodsReady
+	WaitForPodsReady *WaitForPodsReady `json:"waitForPodsReady,omitempty"`
+}
+
+type WaitForPodsReady struct {
+	// Enable when true, indicates that each admitted workload
+	// blocks admission of other workloads in the cluster, until it is in the
+	// `PodsReady` condition. If false, all workloads start as soon as they are
```

  ```suggestion
  	// blocks the admission of all other workloads from all queues until it is in the
  	// `PodsReady` condition. If false, all workloads start as soon as they are
  ```

- **apis/config/v1alpha2/configuration_types.go:61** by [@ahg-g](https://github.com/ahg-g) — 2022-12-12T18:24:37Z
```diff
@@ -45,6 +45,18 @@ type Configuration struct {
 
 	// InternalCertManagement is configuration for internalCertManagement
 	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+
+	// WaitForPodsReady is configuration for waitForPodsReady
+	WaitForPodsReady *WaitForPodsReady `json:"waitForPodsReady,omitempty"`
+}
+
+type WaitForPodsReady struct {
+	// Enable when true, indicates that each admitted workload
+	// blocks admission of other workloads in the cluster, until it is in the
+	// `PodsReady` condition. If false, all workloads start as soon as they are
+	// admitted and do not block admission of other workloads. The PodsReady
+	// condition is only added if this setting is enabled. It defaults to false.
```

  document the purpose of this option, something along the lines: the purpose of this option is to provide simple all-or-nothing semantics ...

- **pkg/controller/workload/job/job_controller.go:201** by [@ahg-g](https://github.com/ahg-g) — 2022-12-12T20:53:53Z
```diff
@@ -170,21 +183,35 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
 		err := r.client.Status().Update(ctx, wl)
 		if err != nil {
 			log.Error(err, "Updating workload status")
 		}
 		return ctrl.Result{}, err
 	}
 
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(5).Info("Handling a job when waitForPodsReady is enabled")
+		condition := generatePodsReadyCondition(&job)
```

  Don't we need to update the condition when workload.admitted is set to nil? Or we want to wait until the job is actually suspended and we get the job update event?

- **test/integration/controller/job/job_controller_test.go:271** by [@ahg-g](https://github.com/ahg-g) — 2022-12-12T23:13:35Z
```diff
@@ -265,6 +268,217 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
```

  Can we add a test to verify that the condition gets updated for a workload that initially has `spec.Admission` set and then unset (to simulate preemption).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-13T08:11:54Z

- **apis/config/v1alpha2/configuration_types.go:49** by [@mimowo](https://github.com/mimowo) — 2022-12-13T08:11:53Z
```diff
@@ -45,6 +45,18 @@ type Configuration struct {
 
 	// InternalCertManagement is configuration for internalCertManagement
 	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+
+	// WaitForPodsReady is configuration for waitForPodsReady
```

  Extended

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-13T08:20:49Z

- **apis/config/v1alpha2/configuration_types.go:61** by [@mimowo](https://github.com/mimowo) — 2022-12-13T08:20:49Z
```diff
@@ -45,6 +45,18 @@ type Configuration struct {
 
 	// InternalCertManagement is configuration for internalCertManagement
 	InternalCertManagement *InternalCertManagement `json:"internalCertManagement,omitempty"`
+
+	// WaitForPodsReady is configuration for waitForPodsReady
+	WaitForPodsReady *WaitForPodsReady `json:"waitForPodsReady,omitempty"`
+}
+
+type WaitForPodsReady struct {
+	// Enable when true, indicates that each admitted workload
+	// blocks admission of other workloads in the cluster, until it is in the
+	// `PodsReady` condition. If false, all workloads start as soon as they are
+	// admitted and do not block admission of other workloads. The PodsReady
+	// condition is only added if this setting is enabled. It defaults to false.
```

  Added now on the field declaration in Configuration (pointed in the previous comment).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-13T09:54:47Z

- **test/integration/controller/job/job_controller_test.go:271** by [@mimowo](https://github.com/mimowo) — 2022-12-13T09:54:46Z
```diff
@@ -265,6 +268,217 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
```

  Modified the tests, but I reuse the previously added test cases (with suspended=true), but in these cases the job was never unsuspended. Now I unconditionally admit the workload at the beginning, but if a test specifies "suspended=true" then Admission is unset, the job is suspended, and then we verify the condition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-13T10:11:00Z

- **pkg/controller/workload/job/job_controller.go:201** by [@mimowo](https://github.com/mimowo) — 2022-12-13T10:10:59Z
```diff
@@ -170,21 +183,35 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
 		err := r.client.Status().Update(ctx, wl)
 		if err != nil {
 			log.Error(err, "Updating workload status")
 		}
 		return ctrl.Result{}, err
 	}
 
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(5).Info("Handling a job when waitForPodsReady is enabled")
+		condition := generatePodsReadyCondition(&job)
```

  I don't see a practical scenario when it matters. Maybe if the request to set admission=nil, but the request to set the suspend=true fails (and is not executed as the job gets quickly re-admitted), but then it seems to make sense to react to the change of the `suspend` field.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-13T14:20:43Z

- **pkg/controller/workload/job/job_controller.go:201** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-13T14:20:43Z
```diff
@@ -170,21 +183,35 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
 		err := r.client.Status().Update(ctx, wl)
 		if err != nil {
 			log.Error(err, "Updating workload status")
 		}
 		return ctrl.Result{}, err
 	}
 
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(5).Info("Handling a job when waitForPodsReady is enabled")
+		condition := generatePodsReadyCondition(&job)
```

  The only scenario where it could matter is when the workload is immediately admitted again and we didn't have time to update the PodsReady condition to false.
  
  But that could still happen in any case, because we can't update the status and spec at the same time. Unless we can in a mutating webhook?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-13T14:26:34Z

- **pkg/controller/workload/job/job_controller.go:201** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-13T14:26:33Z
```diff
@@ -170,21 +183,35 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
 		err := r.client.Status().Update(ctx, wl)
 		if err != nil {
 			log.Error(err, "Updating workload status")
 		}
 		return ctrl.Result{}, err
 	}
 
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(5).Info("Handling a job when waitForPodsReady is enabled")
+		condition := generatePodsReadyCondition(&job)
```

  We can't reset the condition in a mutating webhook either. To be on the safer side, maybe we can set the condition to false as soon as we see `admission=nil`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-13T16:05:12Z

- **pkg/controller/workload/job/job_controller.go:201** by [@mimowo](https://github.com/mimowo) — 2022-12-13T16:05:11Z
```diff
@@ -170,21 +183,35 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	// 3. handle a finished job
 	if jobFinished {
-		added := false
-		wl.Status.Conditions, added = appendFinishedConditionIfNotExists(wl.Status.Conditions, jobFinishedCond)
-		if !added {
+		if workload.InCondition(wl, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
 		err := r.client.Status().Update(ctx, wl)
 		if err != nil {
 			log.Error(err, "Updating workload status")
 		}
 		return ctrl.Result{}, err
 	}
 
+	// handle a job when waitForPodsReady is enabled
+	if r.waitForPodsReady {
+		log.V(5).Info("Handling a job when waitForPodsReady is enabled")
+		condition := generatePodsReadyCondition(&job)
```

  I have updated the code now to check for workload admission to be on the safer side.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-13T16:32:53Z

- **pkg/workload/workload.go:246** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-13T16:32:53Z
```diff
@@ -239,6 +240,9 @@ func UpdateStatusIfChanged(ctx context.Context,
 }
 
 func InCondition(w *kueue.Workload, condition string) bool {
-	i := FindConditionIndex(&w.Status, condition)
-	return i != -1 && w.Status.Conditions[i].Status == metav1.ConditionTrue
+	return InConditionWithStatus(w, condition, metav1.ConditionTrue)
+}
+
+func InConditionWithStatus(w *kueue.Workload, condition string, status metav1.ConditionStatus) bool {
```

  Don't add this function.
  
  As a follow up: remove `InCondition`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-13T17:22:56Z

- **pkg/workload/workload.go:246** by [@mimowo](https://github.com/mimowo) — 2022-12-13T17:22:56Z
```diff
@@ -239,6 +240,9 @@ func UpdateStatusIfChanged(ctx context.Context,
 }
 
 func InCondition(w *kueue.Workload, condition string) bool {
-	i := FindConditionIndex(&w.Status, condition)
-	return i != -1 && w.Status.Conditions[i].Status == metav1.ConditionTrue
+	return InConditionWithStatus(w, condition, metav1.ConditionTrue)
+}
+
+func InConditionWithStatus(w *kueue.Workload, condition string, status metav1.ConditionStatus) bool {
```

  Cleaned up unnecessary changes, will create the follow up PR to remove the function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-13T17:27:40Z

- **pkg/workload/workload.go:246** by [@mimowo](https://github.com/mimowo) — 2022-12-13T17:27:40Z
```diff
@@ -239,6 +240,9 @@ func UpdateStatusIfChanged(ctx context.Context,
 }
 
 func InCondition(w *kueue.Workload, condition string) bool {
-	i := FindConditionIndex(&w.Status, condition)
-	return i != -1 && w.Status.Conditions[i].Status == metav1.ConditionTrue
+	return InConditionWithStatus(w, condition, metav1.ConditionTrue)
+}
+
+func InConditionWithStatus(w *kueue.Workload, condition string, status metav1.ConditionStatus) bool {
```

  https://github.com/kubernetes-sigs/kueue/pull/474

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-12-13T18:00:42Z

- **test/integration/controller/job/job_controller_test.go:271** by [@ahg-g](https://github.com/ahg-g) — 2022-12-13T18:00:41Z
```diff
@@ -265,6 +268,217 @@ var _ = ginkgo.Describe("Job controller for workloads with no queue set", func()
 	})
 })
 
+var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", func() {
```

  sg, we can have a test in the scheduler once preemption is implemented.
