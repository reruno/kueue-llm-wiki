# PR #4606: Namespace wrapper.

**Summary**: Namespace wrapper.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4606

**Last updated**: 2025-03-18T17:51:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/namespace-wrapper` → `main`
- **Created**: 2025-03-14T08:57:08Z
- **Updated**: 2025-03-18T17:51:51Z
- **Merged**: 2025-03-18T17:51:49Z
- **Closed**: 2025-03-18T17:51:50Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 92   **+222 / -840**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Introduce namespace wrapper and namespace defaults.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-14T08:57:11Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-14T08:57:30Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ec8ff024af1ab5caa7183cfd737e41d0f5aa207d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d99eae8941a800081fc0a6 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-18T16:48:14Z

/retest

Due to #2738.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-18T17:15:07Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-18T17:15:14Z

LGTM label has been added.  <details>Git tree hash: 3dfc43e04a6d72e298d76d13884ce240025db436</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-18T17:15:16Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4606#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4606#issuecomment-2734090421" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-14T13:52:17Z

- **pkg/util/testing/defaults.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-14T13:52:17Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  I think it would be better to avoid this since this hides actual test data (namespace).
  This seems to be a bit programmable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T13:05:12Z

- **pkg/util/testing/defaults.go:59** by [@mimowo](https://github.com/mimowo) — 2025-03-18T13:05:12Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  Can you clarify why this hides the actual test data?
  
  It is similar pattern as for other APIs, so I'm not sure why would it be different. The prefix is passed as a parameter, so it is visible to the developer without going into the function. 
  
  Maybe I would adjust the function names, as maybe `MakeDefaultNamespace` suggests this is "default" namespace. WDYT about:
  - `MakeNamespace`
  - `MakeNamespaceWithPrefix`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-18T13:29:18Z

- **pkg/util/testing/defaults.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-18T13:29:18Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  The problem is `MakeNamespace("")`. This hides to treat an empty namespace "" as a default.
  For testing, it should be dumped.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T13:46:01Z

- **pkg/util/testing/defaults.go:59** by [@mimowo](https://github.com/mimowo) — 2025-03-18T13:46:01Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  I see, I agree with that

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-18T15:53:58Z

- **pkg/util/testing/defaults.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-18T15:53:58Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  ```
  MakeNamespaceWithGenerateName(name)
  ```
  
  So, I think we can use this one something. However, this is mostly same as `MakeNamespace().GenerateName(name).Obj()`, I guess. Is this really needed?
  
  Additionally, for `MakeDefaultNamespace(name)`, it is also almost same as `MakeNamespace(name).Obj()`.
  Any reason to prepare this dedicated function?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T15:57:28Z

- **pkg/util/testing/defaults.go:59** by [@mimowo](https://github.com/mimowo) — 2025-03-18T15:57:28Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  I guess having two functions eliminates the need for a little awkward optional parameter in golang that is the name. wdyt?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-18T15:59:23Z

- **pkg/util/testing/defaults.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-18T15:59:23Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  > Any reason to prepare this dedicated function?
  
  Just to reduce the amount of code. But yeah, you're right - it's almost the same.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-18T15:59:44Z

- **pkg/util/testing/defaults.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-18T15:59:44Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  For `MakeNamespaceWithGenerateName(name)` sounds good to me. However, `MakeDefaultNamespace(name)` seems not to do almost... isn't it?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-18T16:02:18Z

- **pkg/util/testing/defaults.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-18T16:02:18Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  > > Any reason to prepare this dedicated function?
  > 
  > Just to reduce the amount of code.
  
  In testing, redundant can be justified.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T16:07:37Z

- **pkg/util/testing/defaults.go:59** by [@mimowo](https://github.com/mimowo) — 2025-03-18T16:07:37Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  Ok, so
  - `MakeNamespaceWithGenerateName(prefix)`, and 
  - `MakeNamespace(name)`
  
  wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-18T16:10:16Z

- **pkg/util/testing/defaults.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-18T16:10:16Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  SGTM, thank you

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-18T16:27:27Z

- **pkg/util/testing/defaults.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-18T16:27:27Z
```diff
@@ -47,3 +47,13 @@ func MakeDefaultThreeLevelTopology(name string) *kueuealpha.Topology {
 		Levels(DefaultBlockTopologyLevel, DefaultRackTopologyLevel, corev1.LabelHostname).
 		Obj()
 }
+
+// MakeDefaultNamespace creates a default namespace with name.
+func MakeDefaultNamespace(name string) *corev1.Namespace {
+	return MakeNamespace(name).Obj()
+}
+
+// MakeDefaultNamespaceGenerate creates a default namespace with generate name.
+func MakeDefaultNamespaceGenerate(generateName string) *corev1.Namespace {
+	return MakeNamespace("").GenerateName(generateName).Obj()
+}
```

  Done
