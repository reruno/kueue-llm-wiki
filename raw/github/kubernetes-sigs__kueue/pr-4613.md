# PR #4613: Support JAX in Kueue using training-operator 1.9

**Summary**: Support JAX in Kueue using training-operator 1.9

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4613

**Last updated**: 2025-05-15T15:34:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `jax-support` → `main`
- **Created**: 2025-03-14T11:59:38Z
- **Updated**: 2025-05-15T15:34:27Z
- **Merged**: 2025-05-15T06:28:50Z
- **Closed**: 2025-05-15T06:28:50Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow), [@mwielgus](https://github.com/mwielgus), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 43   **+2188 / -57**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
For completeness of the support in Kueue. Also, the support for JAX was long-awaited in the training-operator, and there will be a community of people willing to use it with Kueue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4073

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support for JAX in the training-operator
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-14T11:59:40Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-03-14T11:59:45Z

/assign

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-14T11:59:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9aa61ba789762cd1e04f55b9ddf07eccaeac03fe |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68250491a2d2060008e557c9 |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-03-21T10:23:24Z

/ok-to-test
/cc @mimowo @mszadkow

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-04-08T10:39:13Z

/retest-required

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T08:26:24Z

Could you please resolve comments that have been addressed? Also could we have an integration test similarly to other kubeflow integrations

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-04-28T07:24:45Z

I see the tests are still falling...
Can you dump MK related objects statuses before the Active check?
Namely AdmissionCheck, MultiKueueCluster, MultiKueueConfig from the manager to determine what happens.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-01T06:39:37Z

@vladikkuzn Please address CI failures, thanks

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-05-05T08:51:43Z

Investigating CI issues right now

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:49:34Z

Thank you!
/approve

Leaving lgtm on @mimowo for https://github.com/kubernetes-sigs/kueue/pull/4613#discussion_r2086671836

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-13T12:49:43Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4613#issuecomment-2876381627" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4613#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-13T13:17:25Z

> Leaving lgtm on @mimowo for [#4613 (comment)](https://github.com/kubernetes-sigs/kueue/pull/4613#discussion_r2086671836)

ack, @vladikkuzn please fix the remaining verify issues

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-15T06:27:10Z

LGTM label has been added.  <details>Git tree hash: 6fb56b50e47133f7df04c5b332fb86d12305dd79</details>

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-14T15:16:28Z

- **pkg/controller/jobs/kubeflow/jobs/jaxjob/jaxjob_controller.go:1** by [@mwielgus](https://github.com/mwielgus) — 2025-03-14T15:16:28Z
```diff
@@ -0,0 +1,133 @@
+/*
```

  Can you expand the documentation as well?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-21T11:17:39Z

- **pkg/controller/jobs/kubeflow/jobs/jaxjob/jaxjob_controller.go:82** by [@mszadkow](https://github.com/mszadkow) — 2025-03-21T11:17:39Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jaxjob
+
+import (
+	"context"
+	"fmt"
+	"strings"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.JAXJobKind)
+	FrameworkName = "kubeflow.org/jaxjob"
+
+	SetupJaxJobWebhook = jobframework.BaseWebhookFactory(
+		NewJob(),
+		func(o runtime.Object) jobframework.GenericJob {
+			return fromObject(o)
+		},
+	)
+)
+
+// +kubebuilder:webhook:path=/mutate-kubeflow-org-v1-jaxjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=kubeflow.org,resources=jaxjobs,verbs=create,versions=v1,name=mjaxjob.kb.io,admissionReviewVersions=v1
+// +kubebuilder:webhook:path=/validate-kubeflow-org-v1-jaxjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=kubeflow.org,resources=jaxjobs,verbs=create;update,versions=v1,name=vjaxjob.kb.io,admissionReviewVersions=v1
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:           SetupIndexes,
+		NewJob:                 NewJob,
+		NewReconciler:          NewReconciler,
+		SetupWebhook:           SetupJaxJobWebhook,
+		JobType:                &kftraining.JAXJob{},
+		AddToScheme:            kftraining.AddToScheme,
+		IsManagingObjectsOwner: isJAXJob,
+		MultiKueueAdapter:      kubeflowjob.NewMKAdapter(copyJobSpec, copyJobStatus, getEmptyList, gvk, fromObject),
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=jaxjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=jaxjobs/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=jaxjobs/finalizers,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloadpriorityclasses,verbs=get;list;watch
+
+func NewJob() jobframework.GenericJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: &JobControl{}}
+}
+
+var NewReconciler = jobframework.NewGenericReconcilerFactory(func() jobframework.GenericJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: &JobControl{}}
+})
+
+func isJAXJob(owner *metav1.OwnerReference) bool {
+	return owner.Kind == kftraining.JAXJobKind && strings.HasPrefix(owner.APIVersion, kftraining.SchemeGroupVersion.Group)
+}
+
+type JobControl kftraining.JAXJob
+
+var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
```

  Add ManageBy interface and its implementation too, look at other kubeflow jobs

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-03-23T12:42:49Z

- **pkg/controller/jobs/kubeflow/jobs/jaxjob/jaxjob_controller.go:82** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-03-23T12:42:49Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jaxjob
+
+import (
+	"context"
+	"fmt"
+	"strings"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.JAXJobKind)
+	FrameworkName = "kubeflow.org/jaxjob"
+
+	SetupJaxJobWebhook = jobframework.BaseWebhookFactory(
+		NewJob(),
+		func(o runtime.Object) jobframework.GenericJob {
+			return fromObject(o)
+		},
+	)
+)
+
+// +kubebuilder:webhook:path=/mutate-kubeflow-org-v1-jaxjob,mutating=true,failurePolicy=fail,sideEffects=None,groups=kubeflow.org,resources=jaxjobs,verbs=create,versions=v1,name=mjaxjob.kb.io,admissionReviewVersions=v1
+// +kubebuilder:webhook:path=/validate-kubeflow-org-v1-jaxjob,mutating=false,failurePolicy=fail,sideEffects=None,groups=kubeflow.org,resources=jaxjobs,verbs=create;update,versions=v1,name=vjaxjob.kb.io,admissionReviewVersions=v1
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:           SetupIndexes,
+		NewJob:                 NewJob,
+		NewReconciler:          NewReconciler,
+		SetupWebhook:           SetupJaxJobWebhook,
+		JobType:                &kftraining.JAXJob{},
+		AddToScheme:            kftraining.AddToScheme,
+		IsManagingObjectsOwner: isJAXJob,
+		MultiKueueAdapter:      kubeflowjob.NewMKAdapter(copyJobSpec, copyJobStatus, getEmptyList, gvk, fromObject),
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=jaxjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=jaxjobs/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=jaxjobs/finalizers,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloadpriorityclasses,verbs=get;list;watch
+
+func NewJob() jobframework.GenericJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: &JobControl{}}
+}
+
+var NewReconciler = jobframework.NewGenericReconcilerFactory(func() jobframework.GenericJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: &JobControl{}}
+})
+
+func isJAXJob(owner *metav1.OwnerReference) bool {
+	return owner.Kind == kftraining.JAXJobKind && strings.HasPrefix(owner.APIVersion, kftraining.SchemeGroupVersion.Group)
+}
+
+type JobControl kftraining.JAXJob
+
+var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
```

  It's already implemented by KubeflowJob, it uses KFJobControl to extract ManagedBy
  https://github.com/kubernetes-sigs/kueue/blob/9a5b7aba6bc0365e701c6566644ab6ac196118ca/pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go#L201-L213

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-24T15:14:47Z

I think you should update the config in the chart repo also. https://github.com/kubernetes-sigs/kueue/blob/main/charts/kueue/values.yaml#L115

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-26T11:15:31Z

- **site/static/examples/jobs/sample-jaxjob.yaml:20** by [@mszadkow](https://github.com/mszadkow) — 2025-03-26T11:15:30Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: kubeflow.org/v1
+kind: JAXJob
+metadata:
+  name: jax-simple
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  jaxReplicaSpecs:
+    Worker:
+      replicas: 2
+      restartPolicy: OnFailure
+      template:
+        spec:
+          containers:
+            - name: jax
+              image: docker.io/kubeflow/jaxjob-simple:latest
+              command:
+                - "python3"
+                - "train.py"
```

  where is the `train.py` located?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-26T11:27:26Z

- **test/e2e/config/default/controller_manager_config.yaml:31** by [@mszadkow](https://github.com/mszadkow) — 2025-03-26T11:27:26Z
```diff
@@ -28,6 +28,7 @@ integrations:
   - "kubeflow.org/pytorchjob"
   - "kubeflow.org/tfjob"
   - "kubeflow.org/xgboostjob"
+  - "kubeflow.org/jaxjob"
```

  do you plan to add e2e tests for JAX too?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-26T11:46:15Z

- **site/content/en/docs/tasks/run/kubeflow/jaxjobs.md:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-26T11:43:37Z
  Please also update this table
  
  https://github.com/kubernetes-sigs/kueue/blob/919b0301e4f66b6df36da0cc54e2116047307319/site/content/en/docs/overview/_index.md?plain=1#L39

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-03-26T19:22:39Z

- **site/static/examples/jobs/sample-jaxjob.yaml:20** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-03-26T19:22:39Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: kubeflow.org/v1
+kind: JAXJob
+metadata:
+  name: jax-simple
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  jaxReplicaSpecs:
+    Worker:
+      replicas: 2
+      restartPolicy: OnFailure
+      template:
+        spec:
+          containers:
+            - name: jax
+              image: docker.io/kubeflow/jaxjob-simple:latest
+              command:
+                - "python3"
+                - "train.py"
```

  https://github.com/kubeflow/trainer/blob/17077e33f21cd3aacf92e0470afd2de1ef465c9b/examples/jax/cpu-demo/Dockerfile#L26

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-03-26T19:22:54Z

- **site/static/examples/jobs/sample-jaxjob.yaml:20** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-03-26T19:22:54Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: kubeflow.org/v1
+kind: JAXJob
+metadata:
+  name: jax-simple
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  jaxReplicaSpecs:
+    Worker:
+      replicas: 2
+      restartPolicy: OnFailure
+      template:
+        spec:
+          containers:
+            - name: jax
+              image: docker.io/kubeflow/jaxjob-simple:latest
+              command:
+                - "python3"
+                - "train.py"
```

  It's inside an image

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-10T10:03:05Z

- **site/content/en/docs/tasks/run/kubeflow/jaxjobs.md:25** by [@PBundyra](https://github.com/PBundyra) — 2025-04-10T08:45:29Z
```diff
@@ -0,0 +1,54 @@
+---
+title: "Run a JAXJob"
+date: 2025-04-23
+weight: 6
+description: >
+  Run a Kueue scheduled JAXJob
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Training Operator](https://www.kubeflow.org/docs/components/training/jax/) JAXJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [the Training Operator installation guide](https://github.com/kubeflow/training-operator#installation).
+
+Note that the minimum requirement training-operator version is v1.9.0.
+
+You can [modify kueue configurations from installed releases](/docs/installation#install-a-custom-configured-released-version) to include JAXJobs as an allowed workload.
+
+{{% alert title="Note" color="primary" %}}
+In order to use Training Operator, prior to v0.8.1, you need to restart Kueue after the installation.
+You can do it by running: `kubectl delete pods -lcontrol-plane=controller-manager -nkueue-system`.
```

  ```suggestion
  You can do it by running: `kubectl delete pods -l control-plane=controller-manager -n kueue-system`.
  ```

- **test/e2e/singlecluster/jaxjob_test.go:119** by [@PBundyra](https://github.com/PBundyra) — 2025-04-10T10:02:08Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue(clusterQueueName).
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(resourceFlavorName).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteAllJAXJobsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("JAX created", func() {
+		ginkgo.It("should admit group with leader only", func() {
+			jax := jaxjobtesting.MakeJAXJob("jax-simple", ns.Name).
+				Label("kueue.x-k8s.io/queue-name", localQueueName).
+				Suspend(false).
+				SetTypeMeta().
+				JAXReplicaSpecsDefault().
+				Parallelism(kftraining.JAXJobReplicaTypeWorker, 2).
+				Image(kftraining.JAXJobReplicaTypeWorker, util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceCPU, "1").
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceMemory, "200Mi").
+				Obj()
+
+			ginkgo.By("Create a JAX", func() {
+				gomega.Expect(k8sClient.Create(ctx, jax)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Waiting for replicas is ready", func() {
+				createdJAX := &kftraining.JAXJob{}
+
+				gomega.Eventually(func() bool {
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(jax), createdJAX)
+					if err != nil {
+						ginkgo.GinkgoLogr.Info("Failed to fetch JAX", "error", err)
+						return false
+					}
+
+					workerReplicaStatus, ok := createdJAX.Status.ReplicaStatuses[kftraining.JAXJobReplicaTypeWorker]
+					if !ok {
+						return false
+					}
+
+					if workerReplicaStatus.Active != int32(2) {
+						return false
+					}
+
+					for _, condition := range createdJAX.Status.Conditions {
+						if condition.Type == kftraining.JobRunning && condition.Status == corev1.ConditionTrue {
+							return true
+						}
+					}
+					return false
```

  Please use g.Expect and follow standard convention for our tests

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-04-10T11:50:32Z

- **test/e2e/singlecluster/suite_test.go:74** by [@mszadkow](https://github.com/mszadkow) — 2025-04-10T11:50:32Z
```diff
@@ -70,5 +70,6 @@ var _ = ginkgo.BeforeSuite(func() {
 	util.WaitForJobSetAvailability(ctx, k8sClient)
 	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
 	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	ginkgo.GinkgoLogr.Info("Kueue, JobSet, LeaderWorkerSet and AppWrapper operators are available in the cluster", "waitingTime", time.Since(waitForAvailableStart))
+	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	ginkgo.GinkgoLogr.Info("Kueue, JobSet, LeaderWorkerSet, KubeFlow Training and AppWrapper operators are available in the cluster", "waitingTime", time.Since(waitForAvailableStart))
```

  We could change this to "All integration operators are available in the cluster", the list is getting too big but brings no value I think.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-04-11T10:02:48Z

- **test/e2e/singlecluster/suite_test.go:74** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-04-11T10:02:48Z
```diff
@@ -70,5 +70,6 @@ var _ = ginkgo.BeforeSuite(func() {
 	util.WaitForJobSetAvailability(ctx, k8sClient)
 	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
 	util.WaitForAppWrapperAvailability(ctx, k8sClient)
-	ginkgo.GinkgoLogr.Info("Kueue, JobSet, LeaderWorkerSet and AppWrapper operators are available in the cluster", "waitingTime", time.Since(waitForAvailableStart))
+	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
+	ginkgo.GinkgoLogr.Info("Kueue, JobSet, LeaderWorkerSet, KubeFlow Training and AppWrapper operators are available in the cluster", "waitingTime", time.Since(waitForAvailableStart))
```

  It's not a good option because not all operators are always installed in some tests
  Possible option is to move this kind of logs inside `util.WaitForAppWrapperAvailability`. WDYT? But still this log would be needed since it logs time it takes to install all operators, on the other hand log which is inside `util.WaitForAppWrapperAvailability` could track time for each installation, not total time

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-14T10:21:28Z

- **test/e2e/singlecluster/jaxjob_test.go:51** by [@PBundyra](https://github.com/PBundyra) — 2025-04-14T10:21:28Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
```

  Do we need NodeLabel here?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-14T10:29:58Z

- **test/e2e/singlecluster/jaxjob_test.go:101** by [@PBundyra](https://github.com/PBundyra) — 2025-04-14T10:29:58Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue(clusterQueueName).
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(resourceFlavorName).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteAllJAXJobsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("JAX created", func() {
+		ginkgo.It("should admit group with leader only", func() {
+			jax := jaxjobtesting.MakeJAXJob("jax-simple", ns.Name).
+				Label("kueue.x-k8s.io/queue-name", localQueueName).
+				Suspend(false).
+				SetTypeMeta().
+				JAXReplicaSpecsDefault().
+				Parallelism(kftraining.JAXJobReplicaTypeWorker, 2).
+				Image(kftraining.JAXJobReplicaTypeWorker, util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceCPU, "1").
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceMemory, "200Mi").
+				Obj()
+
+			ginkgo.By("Create a JAX", func() {
+				gomega.Expect(k8sClient.Create(ctx, jax)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Waiting for replicas is ready", func() {
+				createdJAX := &kftraining.JAXJob{}
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					// Fetch the JAX object
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(jax), createdJAX)
+					ginkgo.GinkgoLogr.Info("Fetching JAX object", "error", err)
```

  Why do we log an error here if we expect it to not occur?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-14T10:32:46Z

- **test/e2e/singlecluster/jaxjob_test.go:52** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-14T10:32:46Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
```

  ```suggestion
  		util.MustCreate(ctx, k8sClient, rf)
  ```
  nit: use MustCreate for consistency.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-14T10:33:34Z

- **test/e2e/singlecluster/jaxjob_test.go:118** by [@PBundyra](https://github.com/PBundyra) — 2025-04-14T10:33:34Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue(clusterQueueName).
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(resourceFlavorName).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteAllJAXJobsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("JAX created", func() {
+		ginkgo.It("should admit group with leader only", func() {
+			jax := jaxjobtesting.MakeJAXJob("jax-simple", ns.Name).
+				Label("kueue.x-k8s.io/queue-name", localQueueName).
+				Suspend(false).
+				SetTypeMeta().
+				JAXReplicaSpecsDefault().
+				Parallelism(kftraining.JAXJobReplicaTypeWorker, 2).
+				Image(kftraining.JAXJobReplicaTypeWorker, util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceCPU, "1").
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceMemory, "200Mi").
+				Obj()
+
+			ginkgo.By("Create a JAX", func() {
+				gomega.Expect(k8sClient.Create(ctx, jax)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Waiting for replicas is ready", func() {
+				createdJAX := &kftraining.JAXJob{}
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					// Fetch the JAX object
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(jax), createdJAX)
+					ginkgo.GinkgoLogr.Info("Fetching JAX object", "error", err)
+					g.Expect(err).ToNot(gomega.HaveOccurred(), "Failed to fetch JAX")
+
+					// Check the worker replica status exists
+					workerReplicaStatus, ok := createdJAX.Status.ReplicaStatuses[kftraining.JAXJobReplicaTypeWorker]
+					g.Expect(ok).To(gomega.BeTrue(), "Worker replica status not found in JAX status")
+
+					// Check the number of active worker replicas
+					g.Expect(workerReplicaStatus.Active).To(gomega.Equal(int32(2)), "Unexpected number of active worker replicas")
+
+					// Ensure JAX job has "Running" condition with status "True"
+					runningConditionFound := false
+					for _, condition := range createdJAX.Status.Conditions {
+						if condition.Type == kftraining.JobRunning && condition.Status == corev1.ConditionTrue {
+							runningConditionFound = true
+							break
+						}
+					}
+					g.Expect(runningConditionFound).To(gomega.BeTrue(), "Running condition with status True not found")
```

  Maybe we could make it a little more concise using `IsStatusConditionTrue`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-14T10:33:56Z

- **test/e2e/singlecluster/jaxjob_test.go:130** by [@PBundyra](https://github.com/PBundyra) — 2025-04-14T10:33:56Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue(clusterQueueName).
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(resourceFlavorName).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteAllJAXJobsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("JAX created", func() {
+		ginkgo.It("should admit group with leader only", func() {
+			jax := jaxjobtesting.MakeJAXJob("jax-simple", ns.Name).
+				Label("kueue.x-k8s.io/queue-name", localQueueName).
+				Suspend(false).
+				SetTypeMeta().
+				JAXReplicaSpecsDefault().
+				Parallelism(kftraining.JAXJobReplicaTypeWorker, 2).
+				Image(kftraining.JAXJobReplicaTypeWorker, util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceCPU, "1").
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceMemory, "200Mi").
+				Obj()
+
+			ginkgo.By("Create a JAX", func() {
+				gomega.Expect(k8sClient.Create(ctx, jax)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Waiting for replicas is ready", func() {
+				createdJAX := &kftraining.JAXJob{}
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					// Fetch the JAX object
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(jax), createdJAX)
+					ginkgo.GinkgoLogr.Info("Fetching JAX object", "error", err)
+					g.Expect(err).ToNot(gomega.HaveOccurred(), "Failed to fetch JAX")
+
+					// Check the worker replica status exists
+					workerReplicaStatus, ok := createdJAX.Status.ReplicaStatuses[kftraining.JAXJobReplicaTypeWorker]
+					g.Expect(ok).To(gomega.BeTrue(), "Worker replica status not found in JAX status")
+
+					// Check the number of active worker replicas
+					g.Expect(workerReplicaStatus.Active).To(gomega.Equal(int32(2)), "Unexpected number of active worker replicas")
+
+					// Ensure JAX job has "Running" condition with status "True"
+					runningConditionFound := false
+					for _, condition := range createdJAX.Status.Conditions {
+						if condition.Type == kftraining.JobRunning && condition.Status == corev1.ConditionTrue {
+							runningConditionFound = true
+							break
+						}
+					}
+					g.Expect(runningConditionFound).To(gomega.BeTrue(), "Running condition with status True not found")
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			wlLookupKey := types.NamespacedName{
+				Name:      jaxjob.GetWorkloadNameForJAXJob(jax.Name, jax.UID),
+				Namespace: ns.Name,
+			}
+			createdWorkload := &kueue.Workload{}
+			ginkgo.By("Check workload is created", func() {
+				gomega.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+			})
+
```

  Can we check if the workload is admitted

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-14T10:34:26Z

- **test/e2e/singlecluster/jaxjob_test.go:95** by [@PBundyra](https://github.com/PBundyra) — 2025-04-14T10:34:25Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue(clusterQueueName).
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(resourceFlavorName).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteAllJAXJobsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("JAX created", func() {
+		ginkgo.It("should admit group with leader only", func() {
+			jax := jaxjobtesting.MakeJAXJob("jax-simple", ns.Name).
+				Label("kueue.x-k8s.io/queue-name", localQueueName).
+				Suspend(false).
+				SetTypeMeta().
+				JAXReplicaSpecsDefault().
+				Parallelism(kftraining.JAXJobReplicaTypeWorker, 2).
+				Image(kftraining.JAXJobReplicaTypeWorker, util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceCPU, "1").
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceMemory, "200Mi").
+				Obj()
+
+			ginkgo.By("Create a JAX", func() {
+				gomega.Expect(k8sClient.Create(ctx, jax)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Waiting for replicas is ready", func() {
```

  ```suggestion
  			ginkgo.By("Waiting for replicas to be ready", func() {
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-04-17T17:05:07Z

- **test/e2e/singlecluster/jaxjob_test.go:118** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-04-17T17:05:07Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue(clusterQueueName).
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(resourceFlavorName).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteAllJAXJobsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("JAX created", func() {
+		ginkgo.It("should admit group with leader only", func() {
+			jax := jaxjobtesting.MakeJAXJob("jax-simple", ns.Name).
+				Label("kueue.x-k8s.io/queue-name", localQueueName).
+				Suspend(false).
+				SetTypeMeta().
+				JAXReplicaSpecsDefault().
+				Parallelism(kftraining.JAXJobReplicaTypeWorker, 2).
+				Image(kftraining.JAXJobReplicaTypeWorker, util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceCPU, "1").
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceMemory, "200Mi").
+				Obj()
+
+			ginkgo.By("Create a JAX", func() {
+				gomega.Expect(k8sClient.Create(ctx, jax)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Waiting for replicas is ready", func() {
+				createdJAX := &kftraining.JAXJob{}
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					// Fetch the JAX object
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(jax), createdJAX)
+					ginkgo.GinkgoLogr.Info("Fetching JAX object", "error", err)
+					g.Expect(err).ToNot(gomega.HaveOccurred(), "Failed to fetch JAX")
+
+					// Check the worker replica status exists
+					workerReplicaStatus, ok := createdJAX.Status.ReplicaStatuses[kftraining.JAXJobReplicaTypeWorker]
+					g.Expect(ok).To(gomega.BeTrue(), "Worker replica status not found in JAX status")
+
+					// Check the number of active worker replicas
+					g.Expect(workerReplicaStatus.Active).To(gomega.Equal(int32(2)), "Unexpected number of active worker replicas")
+
+					// Ensure JAX job has "Running" condition with status "True"
+					runningConditionFound := false
+					for _, condition := range createdJAX.Status.Conditions {
+						if condition.Type == kftraining.JobRunning && condition.Status == corev1.ConditionTrue {
+							runningConditionFound = true
+							break
+						}
+					}
+					g.Expect(runningConditionFound).To(gomega.BeTrue(), "Running condition with status True not found")
```

  The problem is that it's of type [JobCondition](https://github.com/kubernetes-sigs/kueue/blob/fbc9017f56b20484346beab559f3949089e58465/vendor/github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1/common_types.go#L113), and not [Condition](https://github.com/kubernetes-sigs/kueue/blob/cf5ef9fdc8046791caa709e591032e34123abea3/vendor/k8s.io/apimachinery/pkg/apis/meta/v1/types.go#L1604)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-29T10:06:43Z

- **test/e2e/singlecluster/jaxjob_test.go:118** by [@PBundyra](https://github.com/PBundyra) — 2025-04-29T10:06:43Z
```diff
@@ -0,0 +1,141 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package e2e
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/jaxjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	jaxjobtesting "sigs.k8s.io/kueue/pkg/util/testingjobs/jaxjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("JAX integration", func() {
+	const (
+		resourceFlavorName = "jax-rf"
+		clusterQueueName   = "jax-cq"
+		localQueueName     = "jax-lq"
+	)
+
+	var (
+		ns *corev1.Namespace
+		rf *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "jax-e2e-")
+
+		rf = testing.MakeResourceFlavor(resourceFlavorName).NodeLabel("instance-type", "on-demand").Obj()
+		gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue(clusterQueueName).
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(resourceFlavorName).
+					Resource(corev1.ResourceCPU, "5").
+					Resource(corev1.ResourceMemory, "10Gi").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).To(gomega.Succeed())
+	})
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteAllJAXJobsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("JAX created", func() {
+		ginkgo.It("should admit group with leader only", func() {
+			jax := jaxjobtesting.MakeJAXJob("jax-simple", ns.Name).
+				Label("kueue.x-k8s.io/queue-name", localQueueName).
+				Suspend(false).
+				SetTypeMeta().
+				JAXReplicaSpecsDefault().
+				Parallelism(kftraining.JAXJobReplicaTypeWorker, 2).
+				Image(kftraining.JAXJobReplicaTypeWorker, util.E2eTestAgnHostImage, util.BehaviorWaitForDeletion).
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceCPU, "1").
+				Request(kftraining.JAXJobReplicaTypeWorker, corev1.ResourceMemory, "200Mi").
+				Obj()
+
+			ginkgo.By("Create a JAX", func() {
+				gomega.Expect(k8sClient.Create(ctx, jax)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("Waiting for replicas is ready", func() {
+				createdJAX := &kftraining.JAXJob{}
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					// Fetch the JAX object
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(jax), createdJAX)
+					ginkgo.GinkgoLogr.Info("Fetching JAX object", "error", err)
+					g.Expect(err).ToNot(gomega.HaveOccurred(), "Failed to fetch JAX")
+
+					// Check the worker replica status exists
+					workerReplicaStatus, ok := createdJAX.Status.ReplicaStatuses[kftraining.JAXJobReplicaTypeWorker]
+					g.Expect(ok).To(gomega.BeTrue(), "Worker replica status not found in JAX status")
+
+					// Check the number of active worker replicas
+					g.Expect(workerReplicaStatus.Active).To(gomega.Equal(int32(2)), "Unexpected number of active worker replicas")
+
+					// Ensure JAX job has "Running" condition with status "True"
+					runningConditionFound := false
+					for _, condition := range createdJAX.Status.Conditions {
+						if condition.Type == kftraining.JobRunning && condition.Status == corev1.ConditionTrue {
+							runningConditionFound = true
+							break
+						}
+					}
+					g.Expect(runningConditionFound).To(gomega.BeTrue(), "Running condition with status True not found")
```

  I see, I'm good with the current state then

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-02T09:52:27Z

- **config/components/rbac/jaxjob_editor_role.yaml:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-02T09:45:42Z
  Please also add it to:
  
  https://github.com/kubernetes-sigs/kueue/blob/858b75aeb841bbfbc50780f0105248e4cfd51b64/config/components/rbac/kustomization.yaml#L37-L55

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-12T14:27:36Z

Thank you
Basically, lgtm

- **test/e2e/singlecluster/suite_test.go:73** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T14:25:38Z
```diff
@@ -70,6 +70,7 @@ var _ = ginkgo.BeforeSuite(func() {
 	util.WaitForJobSetAvailability(ctx, k8sClient)
 	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
 	util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
```

  Why do we need this checking? Previously, we do not need this one.

- **site/content/en/docs/tasks/run/kubeflow/jaxjobs.md:9** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T14:27:31Z
```diff
@@ -0,0 +1,54 @@
+---
+title: "Run a JAXJob"
+date: 2025-04-23
+weight: 6
+description: >
+  Run a Kueue scheduled JAXJob
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Training Operator](https://www.kubeflow.org/docs/components/training/jax/) JAXJobs.
```

  ```suggestion
  This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Trainer](https://www.kubeflow.org/docs/components/training/jax/) JAXJobs.
  ```
  
  The training-operator was rebranded as `trainer`. So, could you replace all with `trainer` in this doc?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2025-05-12T16:54:25Z

- **test/e2e/singlecluster/suite_test.go:73** by [@vladikkuzn](https://github.com/vladikkuzn) — 2025-05-12T16:54:24Z
```diff
@@ -70,6 +70,7 @@ var _ = ginkgo.BeforeSuite(func() {
 	util.WaitForJobSetAvailability(ctx, k8sClient)
 	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
 	util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
```

  because without this check kubeflow is not available in cluster, also kubeflow was not installed for e2e tests

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T12:13:26Z

Otherwise, lgtm
Thank you

- **test/e2e/singlecluster/jaxjob_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:12:10Z
  I don't have any opinions to adding E2E tests for kf trainer jobs.
  @mimowo Are you ok with adding those?

- **site/content/en/docs/tasks/run/kubeflow/jaxjobs.md:19** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:12:52Z
```diff
@@ -0,0 +1,54 @@
+---
+title: "Run a JAXJob"
+date: 2025-04-23
+weight: 6
+description: >
+  Run a Kueue scheduled JAXJob
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [Trainer](https://www.kubeflow.org/docs/components/training/jax/) JAXJobs.
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Check [administer cluster quotas](/docs/tasks/manage/administer_cluster_quotas) for details on the initial cluster setup.
+
+Check [the Trainer installation guide](https://github.com/kubeflow/training-operator#installation).
+
+Note that the minimum requirement training-operator version is v1.9.0.
```

  ```suggestion
  Note that the minimum requirement trainer version is v1.9.0.
  ```
  
  nit

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T12:14:00Z

- **test/e2e/singlecluster/suite_test.go:73** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:13:59Z
```diff
@@ -70,6 +70,7 @@ var _ = ginkgo.BeforeSuite(func() {
 	util.WaitForJobSetAvailability(ctx, k8sClient)
 	util.WaitForLeaderWorkerSetAvailability(ctx, k8sClient)
 	util.WaitForAppWrapperAvailability(ctx, k8sClient)
+	util.WaitForKubeFlowTrainingOperatorAvailability(ctx, k8sClient)
```

  Thank you for the clarification.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T06:27:04Z

/lgtm
Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-15T08:42:37Z

- **test/e2e/singlecluster/jaxjob_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-15T08:42:37Z
  @mimowo Do we want to add E2E tests for all KF jobs?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T08:48:53Z

- **test/e2e/singlecluster/jaxjob_test.go:1** by [@mimowo](https://github.com/mimowo) — 2025-05-15T08:48:53Z
  > @mimowo Are you ok with adding those?
  
  Yes, I'm ok with that. I think some sanity tests are valuable to make sure the integration works e2e.
  
  > Do we want to add E2E tests for all KF jobs?
  
  I would add them incrementally when we encounter an issue, or suspect there might be some issue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-15T09:05:15Z

- **test/e2e/singlecluster/jaxjob_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-15T09:05:15Z
  > > Do we want to add E2E tests for all KF jobs?
  > 
  > I would add them incrementally when we encounter an issue, or suspect there might be some issue.
  
  If we add one KF job E2E tests, we should use PyTorchJob since we have every KF job test cases for PyTorchJob in UTs and integration tests and other KF job types do not have every case. It's weird to have only JaxJob E2E tests. So, my recommendation is 1 or 2.
  
  1. Remove JaxJob E2E and Implement PyTorchJob E2E.
  2. Implement E2E all for KF jobs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T09:08:07Z

- **test/e2e/singlecluster/jaxjob_test.go:1** by [@mimowo](https://github.com/mimowo) — 2025-05-15T09:08:07Z
  out of the two I prefer 1. But I'm also ok with just Jax + PyTorch for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-15T15:31:56Z

- **test/e2e/singlecluster/jaxjob_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-15T15:31:55Z
  Alright, let's open an issue to track adding PyTorchJob E2E.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-15T15:34:27Z

- **test/e2e/singlecluster/jaxjob_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-15T15:34:27Z
  Opened: http://github.com/kubernetes-sigs/kueue/issues/5258
