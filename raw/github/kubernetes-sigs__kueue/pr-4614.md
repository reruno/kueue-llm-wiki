# PR #4614: [Feature] [Tool] Script to download logs/artifacts from the CI job.

**Summary**: [Feature] [Tool] Script to download logs/artifacts from the CI job.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4614

**Last updated**: 2025-05-06T06:54:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/ci-logs-downloader-script` → `main`
- **Created**: 2025-03-14T12:17:17Z
- **Updated**: 2025-05-06T06:54:11Z
- **Merged**: 2025-05-06T06:45:13Z
- **Closed**: 2025-05-06T06:45:13Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+60 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Our CI produces a lot of logs, sometimes in vast structures of folders.
It is cumbersome to click on each to do the search for specific strings.
This script allows you to download all the artefacts from the CI job
 and let you search required strings in the comfort of your IDE.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-14T12:17:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6f3ab460a2465d448dba4bb6c0e49d55bc495e61 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67f6b1f3ae57230008972600 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-14T16:26:17Z

/test pull-kueue-test-e2e-main-1-31

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T07:53:46Z

> leverage the fact that its a GCS Web bucket that way web crawling isn't required?
> 
> [](https://cloud.google.com/storage/docs/downloading-objects#downloading-an-object)

I wasn't aware of it and I couldn't find it, that's beautiful!
Yes, definitively I would rather use this, if I have enough access rights etc. 
I will check it.
Thank you!

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-18T12:39:23Z

/test pull-kueue-test-e2e-main-1-29

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-04-08T07:16:56Z

Actually, I think the final results would be an instruction how to use either `gsutil` or `gcloud storage` as they do exactly what this script does.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T07:30:54Z

> Actually, I think the final results would be an instruction how to use either `gsutil` or `gcloud storage` as they do exactly what this script does.

Can we prepare small script for this?

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-04-08T11:27:48Z

> > Actually, I think the final results would be an instruction how to use either `gsutil` or `gcloud storage` as they do exactly what this script does.
> 
> Can we prepare small script for this?

Yes, I have just produced it

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T17:54:20Z

/lgtm

Thank you so much! This is awesome feature!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-09T17:54:26Z

LGTM label has been added.  <details>Git tree hash: 936111d785cd99aa1af0064911612b552a828bcf</details>

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-04-28T06:51:39Z

@mimowo  @kannon92  @KPostOffice 
Now it's a wrapper script on top of the existing tool, as suggested by #sigs-k8s-infra.
PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-06T05:56:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4614#" title="Author self-approved">mszadkow</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4614#discussion_r2074774548" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-14T13:17:10Z

- **hack/get_ci_logs.py:2** by [@kannon92](https://github.com/kannon92) — 2025-03-14T13:17:10Z
```diff
@@ -0,0 +1,102 @@
+import os
+import requests
+from bs4 import BeautifulSoup
```

  I think you need some requirements file as I don't think bs4 is default python libs.
  
  I am not a pythonista though so I may be mistaken.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-14T13:25:20Z

- **hack/get_ci_logs.py:2** by [@mszadkow](https://github.com/mszadkow) — 2025-03-14T13:25:20Z
```diff
@@ -0,0 +1,102 @@
+import os
+import requests
+from bs4 import BeautifulSoup
```

  good point, thanks

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-03-14T18:36:51Z

- **hack/get_ci_logs.py:8** by [@kannon92](https://github.com/kannon92) — 2025-03-14T18:36:51Z
```diff
@@ -0,0 +1,118 @@
+import argparse
+from bs4 import BeautifulSoup
+import requests
+import os
+from urllib.parse import urljoin, urlparse
+
+
+def extract_timestamp_from_url(base_url):
```

  Could we use pytyping here at least?
  
  Looks like your comments specify types by the function parameters do not.

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-03-14T21:02:19Z

Couldn't you leverage the fact that its a GCS Web bucket that way web crawling isn't required?

https://cloud.google.com/storage/docs/downloading-objects#downloading-an-object

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-17T07:13:47Z

I would propose to move this changes to separate directory with requirement file. Also can we add some docs how to install and use it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-04T02:41:20Z

- **hack/get_ci_logs/README.md:4** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T02:24:04Z
```diff
@@ -0,0 +1,17 @@
+The script is designed to recursively download files and directories from a specified HTTP URL, saving them locally while maintaining the original directory structure. It is particularly useful for downloading artifacts of CI/CD, build logs.
+
+Before running the script, ensure you have Python installed on your system. The script is compatible with Python 3.6 and above.
+
```

  I think we missed step for about activation venv:

- **hack/get_ci_logs/requirements.txt:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T02:25:09Z
  Could you please freeze all dependencies and lock the versions using the following command?
  ```
  pip3 freeze > requirements.txt
  ```

- **hack/get_ci_logs/get_ci_logs.py:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T02:26:12Z
  Please add license prefix.

- **hack/get_ci_logs/README.md:4** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T02:37:42Z
```diff
@@ -0,0 +1,17 @@
+The script is designed to recursively download files and directories from a specified HTTP URL, saving them locally while maintaining the original directory structure. It is particularly useful for downloading artifacts of CI/CD, build logs.
+
+Before running the script, ensure you have Python installed on your system. The script is compatible with Python 3.6 and above.
+
```

  If you see an error saying: `This environment is externally managed`, please use a virtual environment.
  
  ```
  ## One time step of creating the venv:
  VENV_DIR=~/venvp3
  python3 -m venv $VENV_DIR
  ## Enter your venv.
  source $VENV_DIR/bin/activate
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T10:48:47Z

Overall LGTM.

- **hack/get_build_logs.sh:56** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T10:46:41Z
```diff
@@ -0,0 +1,57 @@
+#!/usr/bin/env bash
+
+# Copyright 2025 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+# This shell script is designed to facilitate the build logs from a Kueue CI/CD (GCS) bucket using the gcloud CLI.
+# It transforms a given URL copied from the CI into a format compatible with the gcloud storage cp command.
+# It ensures that the gcloud CLI is installed and that given URL is in fact pointing to Kueue related GCS bucket.
+# Second command line argument is optional and allows to define output directory, which by default is located in build_logs at project direcotry level.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+
+transform_url() {
+    local url="$1"
+    local prefix="https://prow.k8s.io/view/gs/"  # Prefix to remove
+    local replacement="gs://"  # Replacement prefix
+
+    # Check if the URL starts with the prefix
+    if [[ "$url" == "$prefix"* ]]; then
+        # Replace the prefix with 'gs://'
+        local transformed_url="${url/$prefix/$replacement}"
+        echo "$transformed_url"
+    else
+        echo "Error: URL does not start with the expected prefix: $prefix"
+        return 1
+    fi
+}
+
+if ! command -v gcloud &> /dev/null; then
+    echo "Can't find Google Cloud SDK (gcloud CLI) in PATH."
+    echo "Please install it by following the instructions at: https://cloud.google.com/sdk/docs/install"
+    exit 1
+fi
+
+if [[ "$1" != *"kubernetes-sigs_kueue"* ]]; then
+    echo "The URL does not point to Kueue build logs bucket."
+fi
+
+transformed_url=$(transform_url "$1")
+root_dir="$(cd "$(dirname "$0")/.." && pwd)"
+output_dir=${2:-"$root_dir/build_logs"}
+gcloud storage cp --recursive "$transformed_url" "$output_dir"
```

  ```suggestion
  gsutil -m cp -r "$transformed_url" "$output_dir"
  ```
  This one working without auth.
  
  -m - multithread

- **hack/get_build_logs.sh:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T10:47:34Z
```diff
@@ -0,0 +1,57 @@
+#!/usr/bin/env bash
+
+# Copyright 2025 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+# This shell script is designed to facilitate the build logs from a Kueue CI/CD (GCS) bucket using the gcloud CLI.
+# It transforms a given URL copied from the CI into a format compatible with the gcloud storage cp command.
+# It ensures that the gcloud CLI is installed and that given URL is in fact pointing to Kueue related GCS bucket.
+# Second command line argument is optional and allows to define output directory, which by default is located in build_logs at project direcotry level.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+
+transform_url() {
+    local url="$1"
+    local prefix="https://prow.k8s.io/view/gs/"  # Prefix to remove
+    local replacement="gs://"  # Replacement prefix
+
+    # Check if the URL starts with the prefix
+    if [[ "$url" == "$prefix"* ]]; then
+        # Replace the prefix with 'gs://'
+        local transformed_url="${url/$prefix/$replacement}"
+        echo "$transformed_url"
+    else
+        echo "Error: URL does not start with the expected prefix: $prefix"
+        return 1
+    fi
+}
+
+if ! command -v gcloud &> /dev/null; then
+    echo "Can't find Google Cloud SDK (gcloud CLI) in PATH."
+    echo "Please install it by following the instructions at: https://cloud.google.com/sdk/docs/install"
+    exit 1
+fi
+
+if [[ "$1" != *"kubernetes-sigs_kueue"* ]]; then
+    echo "The URL does not point to Kueue build logs bucket."
+fi
+
+transformed_url=$(transform_url "$1")
+root_dir="$(cd "$(dirname "$0")/.." && pwd)"
+output_dir=${2:-"$root_dir/build_logs"}
```

  ```suggestion
  output_dir=${2:-"$root_dir/build_logs"}
  mkdir -p $output_dir
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-04-09T14:00:44Z

- **hack/get_build_logs.sh:56** by [@mszadkow](https://github.com/mszadkow) — 2025-04-09T14:00:44Z
```diff
@@ -0,0 +1,57 @@
+#!/usr/bin/env bash
+
+# Copyright 2025 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+# This shell script is designed to facilitate the build logs from a Kueue CI/CD (GCS) bucket using the gcloud CLI.
+# It transforms a given URL copied from the CI into a format compatible with the gcloud storage cp command.
+# It ensures that the gcloud CLI is installed and that given URL is in fact pointing to Kueue related GCS bucket.
+# Second command line argument is optional and allows to define output directory, which by default is located in build_logs at project direcotry level.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+
+transform_url() {
+    local url="$1"
+    local prefix="https://prow.k8s.io/view/gs/"  # Prefix to remove
+    local replacement="gs://"  # Replacement prefix
+
+    # Check if the URL starts with the prefix
+    if [[ "$url" == "$prefix"* ]]; then
+        # Replace the prefix with 'gs://'
+        local transformed_url="${url/$prefix/$replacement}"
+        echo "$transformed_url"
+    else
+        echo "Error: URL does not start with the expected prefix: $prefix"
+        return 1
+    fi
+}
+
+if ! command -v gcloud &> /dev/null; then
+    echo "Can't find Google Cloud SDK (gcloud CLI) in PATH."
+    echo "Please install it by following the instructions at: https://cloud.google.com/sdk/docs/install"
+    exit 1
+fi
+
+if [[ "$1" != *"kubernetes-sigs_kueue"* ]]; then
+    echo "The URL does not point to Kueue build logs bucket."
+fi
+
+transformed_url=$(transform_url "$1")
+root_dir="$(cd "$(dirname "$0")/.." && pwd)"
+output_dir=${2:-"$root_dir/build_logs"}
+gcloud storage cp --recursive "$transformed_url" "$output_dir"
```

  thanks that's nice simplification

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T14:20:13Z

- **hack/get_build_logs.sh:56** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T14:20:12Z
```diff
@@ -0,0 +1,58 @@
+#!/usr/bin/env bash
+
+# Copyright 2025 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+# This shell script is designed to facilitate the build logs from a Kueue CI/CD (GCS) bucket using the gcloud CLI.
+# It transforms a given URL copied from the CI into a format compatible with the gcloud storage cp command.
+# It ensures that the gcloud CLI is installed and that given URL is in fact pointing to Kueue related GCS bucket.
+# Second command line argument is optional and allows to define output directory, which by default is located in build_logs at project direcotry level.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+
+transform_url() {
+    local url="$1"
+    local prefix="https://prow.k8s.io/view/gs/"  # Prefix to remove
+    local replacement="gs://"  # Replacement prefix
+
+    # Check if the URL starts with the prefix
+    if [[ "$url" == "$prefix"* ]]; then
+        # Replace the prefix with 'gs://'
+        local transformed_url="${url/$prefix/$replacement}"
+        echo "$transformed_url"
+    else
+        echo "Error: URL does not start with the expected prefix: $prefix"
+        return 1
+    fi
+}
+
+if ! command -v gcloud &> /dev/null; then
+    echo "Can't find Google Cloud SDK (gcloud CLI) in PATH."
+    echo "Please install it by following the instructions at: https://cloud.google.com/sdk/docs/install"
+    exit 1
+fi
+
+if [[ "$1" != *"kubernetes-sigs_kueue"* ]]; then
+    echo "The URL does not point to Kueue build logs bucket."
+fi
+
+transformed_url=$(transform_url "$1")
+root_dir="$(cd "$(dirname "$0")/.." && pwd)"
+output_dir=${2:-"$root_dir/build_logs"}
+mkdir -p $output_dir
```

  ```suggestion
  mkdir -p "$output_dir"
  ```
  
  Sorry my bad. 
  
  It is failing if we have space in path .
  ```
  /Users/mykhailo_bobrovskyi/Projects/epam/kueue copy/build_logs
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-04-09T14:31:15Z

- **hack/get_build_logs.sh:56** by [@mszadkow](https://github.com/mszadkow) — 2025-04-09T14:31:15Z
```diff
@@ -0,0 +1,58 @@
+#!/usr/bin/env bash
+
+# Copyright 2025 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+# This shell script is designed to facilitate the build logs from a Kueue CI/CD (GCS) bucket using the gcloud CLI.
+# It transforms a given URL copied from the CI into a format compatible with the gcloud storage cp command.
+# It ensures that the gcloud CLI is installed and that given URL is in fact pointing to Kueue related GCS bucket.
+# Second command line argument is optional and allows to define output directory, which by default is located in build_logs at project direcotry level.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+
+transform_url() {
+    local url="$1"
+    local prefix="https://prow.k8s.io/view/gs/"  # Prefix to remove
+    local replacement="gs://"  # Replacement prefix
+
+    # Check if the URL starts with the prefix
+    if [[ "$url" == "$prefix"* ]]; then
+        # Replace the prefix with 'gs://'
+        local transformed_url="${url/$prefix/$replacement}"
+        echo "$transformed_url"
+    else
+        echo "Error: URL does not start with the expected prefix: $prefix"
+        return 1
+    fi
+}
+
+if ! command -v gcloud &> /dev/null; then
+    echo "Can't find Google Cloud SDK (gcloud CLI) in PATH."
+    echo "Please install it by following the instructions at: https://cloud.google.com/sdk/docs/install"
+    exit 1
+fi
+
+if [[ "$1" != *"kubernetes-sigs_kueue"* ]]; then
+    echo "The URL does not point to Kueue build logs bucket."
+fi
+
+transformed_url=$(transform_url "$1")
+root_dir="$(cd "$(dirname "$0")/.." && pwd)"
+output_dir=${2:-"$root_dir/build_logs"}
+mkdir -p $output_dir
```

  yeah, correct, forgot to run verify before push

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-02T20:53:34Z

Otherwise lgtm
thanks.

- **.gitignore:19** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-02T20:53:12Z
```diff
@@ -15,6 +15,8 @@ testbin/*
 # Output of the go coverage tool, specifically when used with LiteIDE
 *.out
 
+# build_logs directory, where by default are downloaded artifacts from the CI job
+build_logs
```

  Could we put the downloaded logs inside of `artifacts`?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-05T16:48:19Z

- **.gitignore:19** by [@mszadkow](https://github.com/mszadkow) — 2025-05-05T16:48:19Z
```diff
@@ -15,6 +15,8 @@ testbin/*
 # Output of the go coverage tool, specifically when used with LiteIDE
 *.out
 
+# build_logs directory, where by default are downloaded artifacts from the CI job
+build_logs
```

  In artifacts we also keep outcome of local tests, it's really up to us.
  Right now local build and CI build logs are separated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-05T17:09:48Z

- **.gitignore:19** by [@mimowo](https://github.com/mimowo) — 2025-05-05T17:09:48Z
```diff
@@ -15,6 +15,8 @@ testbin/*
 # Output of the go coverage tool, specifically when used with LiteIDE
 *.out
 
+# build_logs directory, where by default are downloaded artifacts from the CI job
+build_logs
```

  I would prefer to use the artifacts folder only for the released artifacts, nothing else. Otherwise we may risk that we download some files into the dir and upload during the release process. 
  
  It is not very likely, but if we can easily avoid, I would prefer to keep logs and artifacts separate. wdyt @tenzen-y ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-06T05:56:30Z

- **.gitignore:19** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-06T05:56:30Z
```diff
@@ -15,6 +15,8 @@ testbin/*
 # Output of the go coverage tool, specifically when used with LiteIDE
 *.out
 
+# build_logs directory, where by default are downloaded artifacts from the CI job
+build_logs
```

  The original motivation to align with https://gcsweb.k8s.io/gcs/kubernetes-ci-logs/logs/post-kueue-push-images/1919378548565479424/artifacts/ structure. 
  However, as @mimowo mentioned, the artifact directory was probably shipped as a release artifact for us (@mimowo and I). So, that's fair.
  Thank you for the good call.
  
  /lgtm
  /approve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T06:19:01Z

- **.gitignore:19** by [@mimowo](https://github.com/mimowo) — 2025-05-06T06:19:01Z
```diff
@@ -15,6 +15,8 @@ testbin/*
 # Output of the go coverage tool, specifically when used with LiteIDE
 *.out
 
+# build_logs directory, where by default are downloaded artifacts from the CI job
+build_logs
```

  > However, as @mimowo mentioned, the artifact directory was probably shipped as a release artifact for us
  
  To avoid the confusion we can consider introducing `release_artifacts` dir, but I think it can wait for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-06T06:26:09Z

- **.gitignore:19** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-06T06:26:09Z
```diff
@@ -15,6 +15,8 @@ testbin/*
 # Output of the go coverage tool, specifically when used with LiteIDE
 *.out
 
+# build_logs directory, where by default are downloaded artifacts from the CI job
+build_logs
```

  Yes, if we find another one to store in the local dir, we can consider the `release_artifacts` dir.
