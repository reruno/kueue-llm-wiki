# PR #462: Kueue performance tests

**Summary**: Kueue performance tests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/462

**Last updated**: 2023-03-20T19:18:36Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@oginskis](https://github.com/oginskis)
- **Branch**: `performance_tests` → `main`
- **Created**: 2022-12-05T16:40:26Z
- **Updated**: 2023-03-20T19:18:36Z
- **Merged**: 2022-12-19T16:49:45Z
- **Closed**: 2022-12-19T16:49:45Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 9   **+443 / -0**

## Description

This is an attempt to solve https://github.com/kubernetes-sigs/kueue/issues/45

The PR contains a [Clusterloader](https://github.com/kubernetes/perf-tests/tree/master/clusterloader2) configuration to test job admission latency and throughput. It also includes a wrapper script that automates test scenario execution and captures the results.

Please see more details in the README file.

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-05T16:40:34Z

Welcome @oginskis! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-05T16:40:35Z

Hi @oginskis. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@oginskis](https://github.com/oginskis) — 2022-12-05T16:40:58Z

/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:00:47Z

ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-07T17:00:28Z

/ok-to-test

### Comment by [@oginskis](https://github.com/oginskis) — 2022-12-12T11:51:02Z

@alculquicondor all the changes are squashed and ready for the final review. Please have a look. 
This does not include any additional Prometheus-based measurements for Kueue. Let's merge this first.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-19T16:42:23Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/462#pullrequestreview-1223206734" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/462#" title="Author self-approved">oginskis</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-20T19:18:34Z

/kind feature

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-05T19:36:28Z

- **test/performance/README.md:8** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:02:29Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
```

  Add that a high latency in Kueue is expected when the total quota is not enough to immediately schedule all jobs, because the jobs need to queue.

- **test/performance/README.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:06:22Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
```

  Can you clarify?

- **test/performance/README.md:17** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:07:15Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
```

  Explain that if all jobs fit within the cluster quota, this returns 0, as there is no rate change

- **test/performance/README.md:22** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:08:29Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
```

  link to `jq` installation.
  
  Although we generally have all dependencies installed through the Makefile.

- **test/performance/README.md:36** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:10:52Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Test run
+
+* Configure header of `run-test.sh` script:
+
+```
+#!/bin/bash
+
+# Clusterloader home directory (check out https://github.com/kubernetes/perf-tests)
+CL2_HOME_DIR=/Users/johny/perf-tests/clusterloader2
```

  users should be able to set this variable without editing the file, probably through environment variables.

- **test/performance/README.md:48** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:11:17Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Test run
+
+* Configure header of `run-test.sh` script:
+
+```
+#!/bin/bash
+
+# Clusterloader home directory (check out https://github.com/kubernetes/perf-tests)
+CL2_HOME_DIR=/Users/johny/perf-tests/clusterloader2
+CL2_BINARY_NAME=clusterloader
+
+# Clusterloader test config directory:
+#
+# kueue        Kueue load test configuration  
+# k8s-job-api  Configuration to test vanilla Kubernetes Job API
+#  
+TEST_CONFIG_DIR=k8s-job-api
+
+# Test iterations
+# number-of-small-jobs number-of-large-jobs job-replica-running-time test-timeout cluster-queue-CPU-quota cluster-queue-memory-quota
+experiments=(          
```

  Ideally, users should be able to provide another file to configure this.

- **test/performance/README.md:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:11:39Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Test run
+
+* Configure header of `run-test.sh` script:
+
+```
+#!/bin/bash
+
+# Clusterloader home directory (check out https://github.com/kubernetes/perf-tests)
+CL2_HOME_DIR=/Users/johny/perf-tests/clusterloader2
+CL2_BINARY_NAME=clusterloader
+
+# Clusterloader test config directory:
+#
+# kueue        Kueue load test configuration  
+# k8s-job-api  Configuration to test vanilla Kubernetes Job API
+#  
+TEST_CONFIG_DIR=k8s-job-api
+
+# Test iterations
+# number-of-small-jobs number-of-large-jobs job-replica-running-time test-timeout cluster-queue-CPU-quota cluster-queue-memory-quota
+experiments=(          
+    "10 2 0 2s 3m 100 100Gi"
+    "30 6 0 2s 3m 100 100Gi"    
+    "50 10 0 2s 5m 100 100Gi"
+    "100 15 0 2s 10m 100 100Gi" 
+    "150 20 0 2s 15m 100 100Gi"    
+)
+
+# Kubernetes config location, the default context is used
+export KUBECONFIG="$HOME/.kube/config"
+
+# Kubernetes cluster type
+PROVIDER=gke
+
+...
+```
+
+* `chmod +x run-test.sh` and execute the script
```

  the script should be committed with `+x` already

- **test/performance/README.md:60** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:15:13Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Test run
+
+* Configure header of `run-test.sh` script:
+
+```
+#!/bin/bash
+
+# Clusterloader home directory (check out https://github.com/kubernetes/perf-tests)
+CL2_HOME_DIR=/Users/johny/perf-tests/clusterloader2
+CL2_BINARY_NAME=clusterloader
+
+# Clusterloader test config directory:
+#
+# kueue        Kueue load test configuration  
+# k8s-job-api  Configuration to test vanilla Kubernetes Job API
+#  
+TEST_CONFIG_DIR=k8s-job-api
+
+# Test iterations
+# number-of-small-jobs number-of-large-jobs job-replica-running-time test-timeout cluster-queue-CPU-quota cluster-queue-memory-quota
+experiments=(          
+    "10 2 0 2s 3m 100 100Gi"
+    "30 6 0 2s 3m 100 100Gi"    
+    "50 10 0 2s 5m 100 100Gi"
+    "100 15 0 2s 10m 100 100Gi" 
+    "150 20 0 2s 15m 100 100Gi"    
+)
+
+# Kubernetes config location, the default context is used
+export KUBECONFIG="$HOME/.kube/config"
+
+# Kubernetes cluster type
+PROVIDER=gke
+
+...
+```
+
+* `chmod +x run-test.sh` and execute the script
+
+### Test results
+
+Every test execution creates a `report_<timestamp>` directory inside `TEST_CONFIG_DIR` with `summary.csv` file, where the following metrics are available:
+
+* P50 Job Create to start latency (ms)
+* P90 Job Create to start latency (ms)
+* P50 Job Start to complete latency (ms)
+* P90 Job Start to complete latency (ms)
+* Max Job Throughput (max jobs/s)
+* Total Jobs
+* Total Pods
+* Duration (s)
```

  Put the remaining ones after a paragraph that explains that these measure are only shared for reference. Kueue doesn't influence these times.

- **test/performance/k8s-job-api/config.yaml:60** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:18:21Z
```diff
@@ -0,0 +1,153 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+name: batch
+
+namespace:
+  number: {{$namespaces}}
+  prefix: {{$namespacePrefix}}  
+
+tuningSets:
+- name: UniformQPS
+  qpsLoad:
+    qps: {{$LOAD_TEST_THROUGHPUT}}
+
+steps:
+- name: Start measurements
+  measurements:
+  - Identifier: Timer
+    Method: Timer
+    Params:
+      action: start
+      label: job_performance
+  - Identifier: WaitForFinishedJobs
+    Method: WaitForFinishedJobs
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: JobLifecycleLatency
+    Method: JobLifecycleLatency
+    Params:
+      action: start
+      labelSelector: group = test-job  
+  - Identifier: GenericPrometheusQuery
+    Method: GenericPrometheusQuery
+    Params:
+      action: start
+      metricName: API request latency
```

  this is not accurate

- **test/performance/kueue/config.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:23:31Z
```diff
@@ -0,0 +1,175 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
```

  Is the only difference that this file creates a LocalQueue?
  I suppose there is no way to do inheritance in cluster loader, is there?
  
  But maybe you can embed? We should avoid having so much duplicated configuration.

- **test/performance/kueue/job.yaml:8** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:26:13Z
```diff
@@ -0,0 +1,28 @@
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: {{.Name}}
+  labels:
+    group: test-job
+  annotations:
+    kueue.x-k8s.io/queue-name: {{.LocalQueue}}  
```

  If possible, share the file with the other configuration. If kueue is not installed, this annotation is ignored, so it's not bad to have it in both configurations.

- **test/performance/kueue/config.yaml:95** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:27:32Z
```diff
@@ -0,0 +1,175 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+name: batch
+
+namespace:
+  number: {{$namespaces}}
+  prefix: {{$namespacePrefix}}
+
+tuningSets:
+- name: UniformQPS
+  qpsLoad:
+    qps: {{$LOAD_TEST_THROUGHPUT}}
+
+steps:
+- name: Start measurements
+  measurements:
+  - Identifier: Timer
+    Method: Timer
+    Params:
+      action: start
+      label: job_performance  
+  - Identifier: WaitForFinishedJobs
+    Method: WaitForFinishedJobs
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: JobLifecycleLatency
+    Method: JobLifecycleLatency
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: GenericPrometheusQuery
+    Method: GenericPrometheusQuery
+    Params:
+      action: start
+      metricName: API request latency
+      metricVersion: v1
+      unit: s
+      queries:
+        - name: total_jobs_scheduled
+          query: kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"} 
+        - name: total_pods_scheduled
+          query: count(kube_pod_info{namespace=~"{{$namespacePrefix}}.*"})
+        - name: avg_pod_running_time
+          query: (avg(kube_pod_completion_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"}))
+        - name: perc_90_pod_completion_time
+          query: quantile(0.90, kube_pod_completion_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"})
+        - name: avg_pod_waiting_time
+          query: (avg(kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_created{namespace=~"{{$namespacePrefix}}.*"})) 
+        - name: perc_90_pod_waiting_time
+          query: quantile(0.90, kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_created{namespace=~"{{$namespacePrefix}}.*"})                
+        - name: max_job_throughput
+          query: max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])                               
+- name: Sleep
+  measurements:
+  - Identifier: sleep
+    Method: Sleep
+    Params:
+      duration: 10s            
+- name: Create local queue
+  phases: 
+  - namespaceRange:
+      min: 1
+      max: {{$namespaces}}
+    replicasPerNamespace: 1
+    tuningSet: UniformQPS
+    objectBundle:
+    - basename: local-queue
+      objectTemplatePath: "local-queue.yaml"
+      templateFillMap:
+        LocalQueue: {{$localQueue}}
```

  do you need to set this? isn't the name set from `basename`?

- **test/performance/kueue/prerequisites/prometheus-sm-crd.yaml:3** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:29:37Z
```diff
@@ -0,0 +1,685 @@
+# https://raw.githubusercontent.com/prometheus-operator/prometheus-operator/v0.60.1/example/prometheus-operator-crd/monitoring.coreos.com_servicemonitors.yaml
+---
+apiVersion: apiextensions.k8s.io/v1
```

  Can you put this as installation instructions? We shouldn't need to copy this file into the kueue repo.

- **test/performance/kueue/prerequisites/prometheus-sm.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-05T19:31:27Z
```diff
@@ -0,0 +1,20 @@
+apiVersion: monitoring.coreos.com/v1
```

  Isn't this the same as https://github.com/kubernetes-sigs/kueue/blob/main/config/components/prometheus/monitor.yaml?
  
  We have installation instructions here: https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md#add-metrics-scraping-for-prometheus-operator

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-07T14:04:19Z

- **test/performance/kueue/prerequisites/prometheus-sm.yaml:1** by [@oginskis](https://github.com/oginskis) — 2022-12-07T14:04:18Z
```diff
@@ -0,0 +1,20 @@
+apiVersion: monitoring.coreos.com/v1
```

  I propose a slightly better approach: 
  
  generating an effective Service monitor manifest for Kueue (based on the existing files in the repo) and injecting it into Prometheus manifests path of Clusterloader, so the Clusterloader can deploy it along with other Prometheus configurations when the test starts, please have a look:
   
  https://github.com/kubernetes-sigs/kueue/pull/462/commits/ca3bf8ff9fd4731ba17b1de87ed57b7bdf60ce63

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-07T14:07:08Z

- **test/performance/kueue/prerequisites/prometheus-sm-crd.yaml:3** by [@oginskis](https://github.com/oginskis) — 2022-12-07T14:07:08Z
```diff
@@ -0,0 +1,685 @@
+# https://raw.githubusercontent.com/prometheus-operator/prometheus-operator/v0.60.1/example/prometheus-operator-crd/monitoring.coreos.com_servicemonitors.yaml
+---
+apiVersion: apiextensions.k8s.io/v1
```

  We do not need to pre-provision this CRD if we use https://github.com/kubernetes-sigs/kueue/pull/462/commits/ca3bf8ff9fd4731ba17b1de87ed57b7bdf60ce63
  
  Please see my comment in the next message.

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-07T14:27:05Z

- **test/performance/kueue/config.yaml:95** by [@oginskis](https://github.com/oginskis) — 2022-12-07T14:27:05Z
```diff
@@ -0,0 +1,175 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+name: batch
+
+namespace:
+  number: {{$namespaces}}
+  prefix: {{$namespacePrefix}}
+
+tuningSets:
+- name: UniformQPS
+  qpsLoad:
+    qps: {{$LOAD_TEST_THROUGHPUT}}
+
+steps:
+- name: Start measurements
+  measurements:
+  - Identifier: Timer
+    Method: Timer
+    Params:
+      action: start
+      label: job_performance  
+  - Identifier: WaitForFinishedJobs
+    Method: WaitForFinishedJobs
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: JobLifecycleLatency
+    Method: JobLifecycleLatency
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: GenericPrometheusQuery
+    Method: GenericPrometheusQuery
+    Params:
+      action: start
+      metricName: API request latency
+      metricVersion: v1
+      unit: s
+      queries:
+        - name: total_jobs_scheduled
+          query: kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"} 
+        - name: total_pods_scheduled
+          query: count(kube_pod_info{namespace=~"{{$namespacePrefix}}.*"})
+        - name: avg_pod_running_time
+          query: (avg(kube_pod_completion_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"}))
+        - name: perc_90_pod_completion_time
+          query: quantile(0.90, kube_pod_completion_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"})
+        - name: avg_pod_waiting_time
+          query: (avg(kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_created{namespace=~"{{$namespacePrefix}}.*"})) 
+        - name: perc_90_pod_waiting_time
+          query: quantile(0.90, kube_pod_start_time{namespace=~"{{$namespacePrefix}}.*"} - kube_pod_created{namespace=~"{{$namespacePrefix}}.*"})                
+        - name: max_job_throughput
+          query: max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])                               
+- name: Sleep
+  measurements:
+  - Identifier: sleep
+    Method: Sleep
+    Params:
+      duration: 10s            
+- name: Create local queue
+  phases: 
+  - namespaceRange:
+      min: 1
+      max: {{$namespaces}}
+    replicasPerNamespace: 1
+    tuningSet: UniformQPS
+    objectBundle:
+    - basename: local-queue
+      objectTemplatePath: "local-queue.yaml"
+      templateFillMap:
+        LocalQueue: {{$localQueue}}
```

  Fixed: https://github.com/kubernetes-sigs/kueue/pull/462/commits/4c4ccc622a94448bfe1495281a96585cfc587111

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-07T16:44:22Z

- **test/performance/kueue/config.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-07T15:29:19Z
```diff
@@ -0,0 +1,175 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
```

  This scenario has a concept of "modules" https://github.com/kubernetes/perf-tests/tree/master/clusterloader2/testing/load/modules

- **test/performance/kueue/prerequisites/prometheus-sm.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-07T16:44:06Z
```diff
@@ -0,0 +1,20 @@
+apiVersion: monitoring.coreos.com/v1
```

  But then you have a dirty perf-tests repo. Maybe we could have the script first copy all the manifests from the clusterloader repo and the monitor file into an ignored folder in this repo.

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-07T17:04:39Z

- **test/performance/kueue/config.yaml:1** by [@oginskis](https://github.com/oginskis) — 2022-12-07T17:04:39Z
```diff
@@ -0,0 +1,175 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
```

  Parametrized the `config.yaml` to support with and without kueue scenarios:
  https://github.com/kubernetes-sigs/kueue/pull/462/commits/febfe07584a73e03832dff42685d67e396bf7c1d

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-07T21:01:01Z

- **test/performance/config.yaml:67** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-07T20:47:21Z
```diff
@@ -0,0 +1,185 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+{{$suspendJob := DefaultParam .CL2_SUSPEND_JOB false}}
+
+name: batch
+
+namespace:
+  number: {{$namespaces}}
+  prefix: {{$namespacePrefix}}
+
+tuningSets:
+- name: UniformQPS
+  qpsLoad:
+    qps: {{$LOAD_TEST_THROUGHPUT}}
+
+steps:
+- name: Start measurements
+  measurements:
+  - Identifier: Timer
+    Method: Timer
+    Params:
+      action: start
+      label: job_performance  
+  - Identifier: WaitForFinishedJobs
+    Method: WaitForFinishedJobs
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: JobLifecycleLatency
+    Method: JobLifecycleLatency
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: GenericPrometheusQuery
+    Method: GenericPrometheusQuery
+    Params:
+      action: start
+      metricName: API request latency
+      metricVersion: v1
+      unit: s
+      queries:
+        - name: total_jobs_scheduled
+        {{if $suspendJob}}
+          query: kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}
+        {{else}}
+          query: count(kube_job_info{namespace=~"{{$namespacePrefix}}.*"})
```

  we could use this in both cases, there shouldn't be a difference, right?

- **test/performance/config.yaml:27** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-07T20:48:17Z
```diff
@@ -0,0 +1,185 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+{{$suspendJob := DefaultParam .CL2_SUSPEND_JOB false}}
```

  ```suggestion
  {{$useKueue := DefaultParam .CL2_USE_KUEUE false}}
  ```

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-08T11:53:23Z

- **test/performance/config.yaml:27** by [@oginskis](https://github.com/oginskis) — 2022-12-08T11:53:23Z
```diff
@@ -0,0 +1,185 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+{{$suspendJob := DefaultParam .CL2_SUSPEND_JOB false}}
```

  Ok

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-08T11:54:24Z

- **test/performance/config.yaml:67** by [@oginskis](https://github.com/oginskis) — 2022-12-08T11:54:24Z
```diff
@@ -0,0 +1,185 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+{{$suspendJob := DefaultParam .CL2_SUSPEND_JOB false}}
+
+name: batch
+
+namespace:
+  number: {{$namespaces}}
+  prefix: {{$namespacePrefix}}
+
+tuningSets:
+- name: UniformQPS
+  qpsLoad:
+    qps: {{$LOAD_TEST_THROUGHPUT}}
+
+steps:
+- name: Start measurements
+  measurements:
+  - Identifier: Timer
+    Method: Timer
+    Params:
+      action: start
+      label: job_performance  
+  - Identifier: WaitForFinishedJobs
+    Method: WaitForFinishedJobs
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: JobLifecycleLatency
+    Method: JobLifecycleLatency
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: GenericPrometheusQuery
+    Method: GenericPrometheusQuery
+    Params:
+      action: start
+      metricName: API request latency
+      metricVersion: v1
+      unit: s
+      queries:
+        - name: total_jobs_scheduled
+        {{if $suspendJob}}
+          query: kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}
+        {{else}}
+          query: count(kube_job_info{namespace=~"{{$namespacePrefix}}.*"})
```

  Yes, the results from these 2 queries should be the same.

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-08T11:55:27Z

- **test/performance/kueue/prerequisites/prometheus-sm.yaml:1** by [@oginskis](https://github.com/oginskis) — 2022-12-08T11:55:26Z
```diff
@@ -0,0 +1,20 @@
+apiVersion: monitoring.coreos.com/v1
```

  Okay, makes sense

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-08T12:10:14Z

- **test/performance/k8s-job-api/config.yaml:60** by [@oginskis](https://github.com/oginskis) — 2022-12-08T12:10:14Z
```diff
@@ -0,0 +1,153 @@
+{{$MODE := DefaultParam .MODE "Indexed"}}
+{{$LOAD_TEST_THROUGHPUT := DefaultParam .CL2_LOAD_TEST_THROUGHPUT 10}}
+
+{{$smallJobs := DefaultParam .CL2_SMALL_JOBS 10}}
+{{$mediumJobs := DefaultParam .CL2_MEDIUM_JOBS 2}}
+{{$largeJobs := DefaultParam .CL2_LARGE_JOBS 0}}
+
+{{$namespaces := DefaultParam .CL2_NAMESPACES 1}}
+
+{{$smallJobsPerNamespace := DivideInt $smallJobs $namespaces}}
+{{$mediumJobsPerNamespace := DivideInt $mediumJobs $namespaces}}
+{{$largeJobsPerNamespace := DivideInt $largeJobs $namespaces}}
+
+{{$smallJobSize := 5}}
+{{$mediumJobSize := 20}}
+{{$largeJobSize := 100}}
+
+{{$jobRunningTime := DefaultParam .CL2_JOB_RUNNING_TIME "30s"}}
+
+{{$clusterQueue := "default-cluster-queue"}}
+{{$localQueue := "local-queue"}}
+
+{{$testTimeout := DefaultParam .CL2_TEST_TIMEOUT "5m"}}
+
+{{$namespacePrefix := "queue-test"}}
+
+name: batch
+
+namespace:
+  number: {{$namespaces}}
+  prefix: {{$namespacePrefix}}  
+
+tuningSets:
+- name: UniformQPS
+  qpsLoad:
+    qps: {{$LOAD_TEST_THROUGHPUT}}
+
+steps:
+- name: Start measurements
+  measurements:
+  - Identifier: Timer
+    Method: Timer
+    Params:
+      action: start
+      label: job_performance
+  - Identifier: WaitForFinishedJobs
+    Method: WaitForFinishedJobs
+    Params:
+      action: start
+      labelSelector: group = test-job
+  - Identifier: JobLifecycleLatency
+    Method: JobLifecycleLatency
+    Params:
+      action: start
+      labelSelector: group = test-job  
+  - Identifier: GenericPrometheusQuery
+    Method: GenericPrometheusQuery
+    Params:
+      action: start
+      metricName: API request latency
```

  Changed

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-08T12:26:55Z

- **test/performance/kueue/job.yaml:8** by [@oginskis](https://github.com/oginskis) — 2022-12-08T12:26:54Z
```diff
@@ -0,0 +1,28 @@
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: {{.Name}}
+  labels:
+    group: test-job
+  annotations:
+    kueue.x-k8s.io/queue-name: {{.LocalQueue}}  
```

  We still need to remove this annotation if Kueue IS installed, but we still want to test regular Kubernetes Job API performance. If not, Kueue suspends the Job and complains that the local queue is not there. So, I added:
  ```  
  {{if .UseKueue}}
    annotations:
      kueue.x-k8s.io/queue-name: {{.LocalQueue}}
  {{end}}
  ```

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-08T12:28:33Z

- **test/performance/README.md:65** by [@oginskis](https://github.com/oginskis) — 2022-12-08T12:28:32Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Test run
+
+* Configure header of `run-test.sh` script:
+
+```
+#!/bin/bash
+
+# Clusterloader home directory (check out https://github.com/kubernetes/perf-tests)
+CL2_HOME_DIR=/Users/johny/perf-tests/clusterloader2
+CL2_BINARY_NAME=clusterloader
+
+# Clusterloader test config directory:
+#
+# kueue        Kueue load test configuration  
+# k8s-job-api  Configuration to test vanilla Kubernetes Job API
+#  
+TEST_CONFIG_DIR=k8s-job-api
+
+# Test iterations
+# number-of-small-jobs number-of-large-jobs job-replica-running-time test-timeout cluster-queue-CPU-quota cluster-queue-memory-quota
+experiments=(          
+    "10 2 0 2s 3m 100 100Gi"
+    "30 6 0 2s 3m 100 100Gi"    
+    "50 10 0 2s 5m 100 100Gi"
+    "100 15 0 2s 10m 100 100Gi" 
+    "150 20 0 2s 15m 100 100Gi"    
+)
+
+# Kubernetes config location, the default context is used
+export KUBECONFIG="$HOME/.kube/config"
+
+# Kubernetes cluster type
+PROVIDER=gke
+
+...
+```
+
+* `chmod +x run-test.sh` and execute the script
```

  It is, just to double check :)

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-12T10:22:02Z

- **test/performance/README.md:48** by [@oginskis](https://github.com/oginskis) — 2022-12-12T10:22:02Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Test run
+
+* Configure header of `run-test.sh` script:
+
+```
+#!/bin/bash
+
+# Clusterloader home directory (check out https://github.com/kubernetes/perf-tests)
+CL2_HOME_DIR=/Users/johny/perf-tests/clusterloader2
+CL2_BINARY_NAME=clusterloader
+
+# Clusterloader test config directory:
+#
+# kueue        Kueue load test configuration  
+# k8s-job-api  Configuration to test vanilla Kubernetes Job API
+#  
+TEST_CONFIG_DIR=k8s-job-api
+
+# Test iterations
+# number-of-small-jobs number-of-large-jobs job-replica-running-time test-timeout cluster-queue-CPU-quota cluster-queue-memory-quota
+experiments=(          
```

  Added a way to override the default values through .env file

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-12T10:22:08Z

- **test/performance/README.md:36** by [@oginskis](https://github.com/oginskis) — 2022-12-12T10:22:08Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Test run
+
+* Configure header of `run-test.sh` script:
+
+```
+#!/bin/bash
+
+# Clusterloader home directory (check out https://github.com/kubernetes/perf-tests)
+CL2_HOME_DIR=/Users/johny/perf-tests/clusterloader2
```

  Added a way to override the default values through .env file

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-12T10:51:36Z

- **test/performance/README.md:8** by [@oginskis](https://github.com/oginskis) — 2022-12-12T10:51:35Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
```

  +

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-12T11:03:26Z

- **test/performance/README.md:14** by [@oginskis](https://github.com/oginskis) — 2022-12-12T11:03:25Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
```

  Added more details explaining the query as well as reference to PromQL docs

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-12T11:14:43Z

- **test/performance/README.md:17** by [@oginskis](https://github.com/oginskis) — 2022-12-12T11:14:43Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
```

  Explained

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-12T11:25:06Z

- **test/performance/README.md:22** by [@oginskis](https://github.com/oginskis) — 2022-12-12T11:25:06Z
```diff
@@ -0,0 +1,82 @@
+# Kueue performance testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+### Job startup throughput
+
+Workload admission [rate per second] over minute intervals, Prometheus query:
+
+[To be fine-tuned]
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, `jq`, [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
```

  Added the link

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T19:21:39Z

/approve

- **test/performance/README.md:19** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:05:29Z
```diff
@@ -0,0 +1,82 @@
+# Kueue Performance Testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+High Job startup latency in Kueue is expected when the total quota is not enough to schedule all jobs immediately, because the jobs need to queue.
+
+### Job startup throughput
+
+The best workload admission rate per second within 1 minute intervals. 
+The rate is measured every 5 seconds (see more details in [PromQL examples](https://prometheus.io/docs/prometheus/latest/querying/examples/#subquery)):
+
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+This measurement is not efficient if the cluster quota is big enough to schedule all workloads of the test immediately, because Kueue immediately admits all the workloads and the `kueue_admitted_workloads_total` never increases. In this case, the PromQL query returns 0.
```

  ```suggestion
  This measurement is not accurate if the cluster quota is big enough to schedule all workloads of the test immediately, because Kueue immediately admits all the workloads and the `kueue_admitted_workloads_total` never increases. In this case, the PromQL query returns 0.
  ```

- **test/performance/README.md:60** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:06:26Z
```diff
@@ -0,0 +1,82 @@
+# Kueue Performance Testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+High Job startup latency in Kueue is expected when the total quota is not enough to schedule all jobs immediately, because the jobs need to queue.
+
+### Job startup throughput
+
+The best workload admission rate per second within 1 minute intervals. 
+The rate is measured every 5 seconds (see more details in [PromQL examples](https://prometheus.io/docs/prometheus/latest/querying/examples/#subquery)):
+
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+This measurement is not efficient if the cluster quota is big enough to schedule all workloads of the test immediately, because Kueue immediately admits all the workloads and the `kueue_admitted_workloads_total` never increases. In this case, the PromQL query returns 0.
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, [jq](https://stedolan.github.io/jq/download/), [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Run the test
+
+1. Copy an environment file example to `.env` file: 
+
+    * `cp .env.example .env`
+    
+2. Configure the environment variable file:
+
+```
+# Clusterloader home directory (checkout https://github.com/kubernetes/perf-tests)
+export CL2_HOME_DIR="/Users/johny/perf-tests/clusterloader2"
+
+# Run the performance test with Kueue (this requires Kueue to be pre-deployed to the cluster)
+# or without Kueue
+export USE_KUEUE=false
+
+# Test iterations:
+# number-of-small-jobs number-of-large-jobs job-replica-running-time test-timeout cluster-queue-CPU-quota cluster-queue-memory-quota
+export EXPERIMENTS=(
+    "10 2 0 2s 3m 100 100Gi"
+    "20 2 0 2s 5m 100 100Gi"
+)
+
+# Kubeconfig file location
+export KUBECONFIG="$HOME/.kube/config"
+
+# Kubernetes kind
+export PROVIDER="gke"
+
+```
```

  let's not copy this here to avoid duplication

- **test/performance/run-test.sh:29** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:10:44Z
```diff
@@ -0,0 +1,111 @@
+#!/bin/bash
+
+# shellcheck disable=SC1091
+if test -f .env; then
+  source .env
+fi
+
+CL2_HOME_DIR=${CL2_HOME_DIR:=/Users/johny/perf-tests/clusterloader2} 
+CL2_BINARY_NAME=${CL2_BINARY_NAME:=clusterloader}
+
+USE_KUEUE=${USE_KUEUE:=true}
+
+DEFAULT_EXPERIMENTS=(
+    "10 2 0 2s 3m 100 100Gi"
+)
+
+EXPERIMENTS=("${EXPERIMENTS[@]:=${DEFAULT_EXPERIMENTS[@]}}")
+
+KUBECONFIG=${KUBECONFIG:="$HOME/.kube/config"}
+export KUBECONFIG
+
+PROVIDER=${PROVIDER:=gke}
+
+export EXEC_DEPLOYMENT_YAML="$CL2_HOME_DIR/pkg/execservice/manifest/exec_deployment.yaml"
+
+cp -r "$CL2_HOME_DIR/pkg/prometheus/manifests/" tmp_manifests
+trap 'rm -r tmp_manifests' EXIT
+PROMETHEUS_MANIFEST_PATH=$(pwd)/tmp_manifests
+export PROMETHEUS_MANIFEST_PATH
```

  ```suggestion
  export PROMETHEUS_MANIFEST_PATH=$(pwd)/tmp_manifests
  ```

- **test/performance/run-test.sh:39** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:12:45Z
```diff
@@ -0,0 +1,111 @@
+#!/bin/bash
+
+# shellcheck disable=SC1091
+if test -f .env; then
+  source .env
+fi
+
+CL2_HOME_DIR=${CL2_HOME_DIR:=/Users/johny/perf-tests/clusterloader2} 
+CL2_BINARY_NAME=${CL2_BINARY_NAME:=clusterloader}
+
+USE_KUEUE=${USE_KUEUE:=true}
+
+DEFAULT_EXPERIMENTS=(
+    "10 2 0 2s 3m 100 100Gi"
+)
+
+EXPERIMENTS=("${EXPERIMENTS[@]:=${DEFAULT_EXPERIMENTS[@]}}")
+
+KUBECONFIG=${KUBECONFIG:="$HOME/.kube/config"}
+export KUBECONFIG
+
+PROVIDER=${PROVIDER:=gke}
+
+export EXEC_DEPLOYMENT_YAML="$CL2_HOME_DIR/pkg/execservice/manifest/exec_deployment.yaml"
+
+cp -r "$CL2_HOME_DIR/pkg/prometheus/manifests/" tmp_manifests
+trap 'rm -r tmp_manifests' EXIT
+PROMETHEUS_MANIFEST_PATH=$(pwd)/tmp_manifests
+export PROMETHEUS_MANIFEST_PATH
+
+now=$(date +%Y-%m-%d-%H.%M.%S)
+
+if [[ "$USE_KUEUE" == true ]]; then
+    export CL2_USE_KUEUE=true
+    kubectl kustomize ../../config/prometheus | yq -s '.kind' -o yaml
```

  maybe `kubectl kustomize -o` would allow you to write directly into `$PROMETHEUS_MANIFEST_PATH`?

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-16T11:34:43Z

- **test/performance/run-test.sh:39** by [@oginskis](https://github.com/oginskis) — 2022-12-16T11:34:43Z
```diff
@@ -0,0 +1,111 @@
+#!/bin/bash
+
+# shellcheck disable=SC1091
+if test -f .env; then
+  source .env
+fi
+
+CL2_HOME_DIR=${CL2_HOME_DIR:=/Users/johny/perf-tests/clusterloader2} 
+CL2_BINARY_NAME=${CL2_BINARY_NAME:=clusterloader}
+
+USE_KUEUE=${USE_KUEUE:=true}
+
+DEFAULT_EXPERIMENTS=(
+    "10 2 0 2s 3m 100 100Gi"
+)
+
+EXPERIMENTS=("${EXPERIMENTS[@]:=${DEFAULT_EXPERIMENTS[@]}}")
+
+KUBECONFIG=${KUBECONFIG:="$HOME/.kube/config"}
+export KUBECONFIG
+
+PROVIDER=${PROVIDER:=gke}
+
+export EXEC_DEPLOYMENT_YAML="$CL2_HOME_DIR/pkg/execservice/manifest/exec_deployment.yaml"
+
+cp -r "$CL2_HOME_DIR/pkg/prometheus/manifests/" tmp_manifests
+trap 'rm -r tmp_manifests' EXIT
+PROMETHEUS_MANIFEST_PATH=$(pwd)/tmp_manifests
+export PROMETHEUS_MANIFEST_PATH
+
+now=$(date +%Y-%m-%d-%H.%M.%S)
+
+if [[ "$USE_KUEUE" == true ]]; then
+    export CL2_USE_KUEUE=true
+    kubectl kustomize ../../config/prometheus | yq -s '.kind' -o yaml
```

  Kustomize places all Kubernetes object manifests (`role`, `rolebinding` and `servicemonitor`) in the same file, however, Clusterloader expects, that there is one manifest per file, otherwise it does not create all the objects from the file. That's why I added `yq` expression, which splits produced manifest into 3 files and then moves to temporary `$PROMETHEUS_MANIFEST_PATH`

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-16T13:31:40Z

- **test/performance/README.md:60** by [@oginskis](https://github.com/oginskis) — 2022-12-16T13:31:40Z
```diff
@@ -0,0 +1,82 @@
+# Kueue Performance Testing
+
+## Measurements
+
+### Job startup latency
+
+How fast do jobs transition from `created` to `started` state?
+Time spent between the transition from `job.CreationTimestamp.Time` to `job.Status.StartTime.Time` state.
+
+High Job startup latency in Kueue is expected when the total quota is not enough to schedule all jobs immediately, because the jobs need to queue.
+
+### Job startup throughput
+
+The best workload admission rate per second within 1 minute intervals. 
+The rate is measured every 5 seconds (see more details in [PromQL examples](https://prometheus.io/docs/prometheus/latest/querying/examples/#subquery)):
+
+`max_over_time(sum(rate(kueue_admitted_workloads_total{cluster_queue="{{$clusterQueue}}"}[1m]))[{{$testTimeout}}:5s])`
+
+This measurement is not efficient if the cluster quota is big enough to schedule all workloads of the test immediately, because Kueue immediately admits all the workloads and the `kueue_admitted_workloads_total` never increases. In this case, the PromQL query returns 0.
+## How to run the test?
+
+### Prerequisites
+
+1. Deploy [Kueue](https://github.com/kubernetes-sigs/kueue/blob/main/docs/setup/install.md)
+2. Make sure you have `kubectl`, [jq](https://stedolan.github.io/jq/download/), [golang version](https://github.com/mikefarah/yq) of `yq` and `go` 
+3. Checkout `Clusterloader2` framework: https://github.com/kubernetes/perf-tests and build `clusterloader` binary:
+
+    * change to `clusterloader2` directory
+    * run `go build -o clusterloader './cmd/'` 
+
+### Run the test
+
+1. Copy an environment file example to `.env` file: 
+
+    * `cp .env.example .env`
+    
+2. Configure the environment variable file:
+
+```
+# Clusterloader home directory (checkout https://github.com/kubernetes/perf-tests)
+export CL2_HOME_DIR="/Users/johny/perf-tests/clusterloader2"
+
+# Run the performance test with Kueue (this requires Kueue to be pre-deployed to the cluster)
+# or without Kueue
+export USE_KUEUE=false
+
+# Test iterations:
+# number-of-small-jobs number-of-large-jobs job-replica-running-time test-timeout cluster-queue-CPU-quota cluster-queue-memory-quota
+export EXPERIMENTS=(
+    "10 2 0 2s 3m 100 100Gi"
+    "20 2 0 2s 5m 100 100Gi"
+)
+
+# Kubeconfig file location
+export KUBECONFIG="$HOME/.kube/config"
+
+# Kubernetes kind
+export PROVIDER="gke"
+
+```
```

  Now it's a table with variables and corresponding descriptions.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T15:17:28Z

- **test/performance/run-test.sh:39** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T15:17:28Z
```diff
@@ -0,0 +1,111 @@
+#!/bin/bash
+
+# shellcheck disable=SC1091
+if test -f .env; then
+  source .env
+fi
+
+CL2_HOME_DIR=${CL2_HOME_DIR:=/Users/johny/perf-tests/clusterloader2} 
+CL2_BINARY_NAME=${CL2_BINARY_NAME:=clusterloader}
+
+USE_KUEUE=${USE_KUEUE:=true}
+
+DEFAULT_EXPERIMENTS=(
+    "10 2 0 2s 3m 100 100Gi"
+)
+
+EXPERIMENTS=("${EXPERIMENTS[@]:=${DEFAULT_EXPERIMENTS[@]}}")
+
+KUBECONFIG=${KUBECONFIG:="$HOME/.kube/config"}
+export KUBECONFIG
+
+PROVIDER=${PROVIDER:=gke}
+
+export EXEC_DEPLOYMENT_YAML="$CL2_HOME_DIR/pkg/execservice/manifest/exec_deployment.yaml"
+
+cp -r "$CL2_HOME_DIR/pkg/prometheus/manifests/" tmp_manifests
+trap 'rm -r tmp_manifests' EXIT
+PROMETHEUS_MANIFEST_PATH=$(pwd)/tmp_manifests
+export PROMETHEUS_MANIFEST_PATH
+
+now=$(date +%Y-%m-%d-%H.%M.%S)
+
+if [[ "$USE_KUEUE" == true ]]; then
+    export CL2_USE_KUEUE=true
+    kubectl kustomize ../../config/prometheus | yq -s '.kind' -o yaml
```

  sounds good. Please add a comment in the code for what you just said.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T15:20:28Z

minor nit

### Review by [@oginskis](https://github.com/oginskis) (commented) — 2022-12-19T16:01:13Z

- **test/performance/run-test.sh:39** by [@oginskis](https://github.com/oginskis) — 2022-12-19T16:01:13Z
```diff
@@ -0,0 +1,111 @@
+#!/bin/bash
+
+# shellcheck disable=SC1091
+if test -f .env; then
+  source .env
+fi
+
+CL2_HOME_DIR=${CL2_HOME_DIR:=/Users/johny/perf-tests/clusterloader2} 
+CL2_BINARY_NAME=${CL2_BINARY_NAME:=clusterloader}
+
+USE_KUEUE=${USE_KUEUE:=true}
+
+DEFAULT_EXPERIMENTS=(
+    "10 2 0 2s 3m 100 100Gi"
+)
+
+EXPERIMENTS=("${EXPERIMENTS[@]:=${DEFAULT_EXPERIMENTS[@]}}")
+
+KUBECONFIG=${KUBECONFIG:="$HOME/.kube/config"}
+export KUBECONFIG
+
+PROVIDER=${PROVIDER:=gke}
+
+export EXEC_DEPLOYMENT_YAML="$CL2_HOME_DIR/pkg/execservice/manifest/exec_deployment.yaml"
+
+cp -r "$CL2_HOME_DIR/pkg/prometheus/manifests/" tmp_manifests
+trap 'rm -r tmp_manifests' EXIT
+PROMETHEUS_MANIFEST_PATH=$(pwd)/tmp_manifests
+export PROMETHEUS_MANIFEST_PATH
+
+now=$(date +%Y-%m-%d-%H.%M.%S)
+
+if [[ "$USE_KUEUE" == true ]]; then
+    export CL2_USE_KUEUE=true
+    kubectl kustomize ../../config/prometheus | yq -s '.kind' -o yaml
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T16:42:15Z

/lgtm
/approve
