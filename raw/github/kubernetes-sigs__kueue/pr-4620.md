# PR #4620: [Flaky] Revisit RayJob TAS e2e tests to not count submitter job pod

**Summary**: [Flaky] Revisit RayJob TAS e2e tests to not count submitter job pod

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4620

**Last updated**: 2025-03-18T10:14:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `flaky/rayjob-tas-test` → `main`
- **Created**: 2025-03-14T16:24:59Z
- **Updated**: 2025-03-18T10:14:27Z
- **Merged**: 2025-03-18T09:55:50Z
- **Closed**: 2025-03-18T09:55:50Z
- **Labels**: `lgtm`, `kind/flake`, `release-note-none`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 1   **+11 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind flake
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
We temporarily don't assert on the exact number of Pods as this flakes on Ray 1.2.2
 due to the submitter Pod and Job not being created occasionally 
(See https://github.com/kubernetes-sigs/kueue/issues/4508#issuecomment-2724257298 for more details).

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4508 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-14T16:25:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b82d8ebdedc4ef6615e630a4dda3d6d5eeb5c976 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67d935a06d737b000893687e |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T09:00:23Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T14:19:59Z

LWS is failing the CI

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-18T08:36:54Z

```
Deleting cluster "kind" ...
ERROR: failed to delete cluster "kind": failed to delete nodes: command "docker rm -f -v kind-control-plane kind-worker kind-worker2" failed with error: exit status 1
Command Output: Error response from daemon: cannot remove container "/kind-control-plane": could not kill: tried to kill container, but did not receive an exit event
Error response from daemon: cannot remove container "/kind-worker": could not kill: tried to kill container, but did not receive an exit event
Error response from daemon: cannot remove container "/kind-worker2": could not kill: tried to kill container, but did not receive an exit event
make: *** [Makefile-test.mk:170: run-test-e2e-customconfigs-1.31.1] Error 1
+ EXIT_VALUE=2
```
infra failure not related to the pr
/test pull-kueue-test-e2e-customconfigs-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-18T08:37:15Z

LGTM label has been added.  <details>Git tree hash: c1da89744b45a1b730d87bc1fa97070e2b3cad9e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-18T08:37:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4620#pullrequestreview-2693471200" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4620#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-18T09:00:24Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-18T09:00:33Z

LGTM label has been added.  <details>Git tree hash: a57057db4afaf50ea48255c04639b2e18a3fc2c7</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-18T09:59:41Z

/cherry-pick release-0.10
To make the release branch more stable

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-03-18T10:00:19Z

@mimowo: new pull request created: #4667

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4620#issuecomment-2732446542):

>/cherry-pick release-0.10
>To make the release branch more stable


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T09:28:47Z

- **test/e2e/tas/rayjob_test.go:95** by [@mimowo](https://github.com/mimowo) — 2025-03-17T09:28:47Z
```diff
@@ -89,11 +105,10 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 	ginkgo.When("Creating a RayJob", func() {
 		ginkgo.It("Should place pods based on the ranks-ordering", func() {
 			const (
-				headReplicas     = 1
-				workerReplicas   = 3
-				submitterReplica = 1
+				headReplicas   = 1
+				workerReplicas = 3
 			)
-			numPods := headReplicas + workerReplicas + submitterReplica
+			numPods := headReplicas + workerReplicas
```

  IIUC you need to check the actual number of pods is >= numPods now (or more precisely numPods, or numPods+ 1), right?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T09:52:21Z

- **test/e2e/tas/rayjob_test.go:95** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T09:52:21Z
```diff
@@ -89,11 +105,10 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 	ginkgo.When("Creating a RayJob", func() {
 		ginkgo.It("Should place pods based on the ranks-ordering", func() {
 			const (
-				headReplicas     = 1
-				workerReplicas   = 3
-				submitterReplica = 1
+				headReplicas   = 1
+				workerReplicas = 3
 			)
-			numPods := headReplicas + workerReplicas + submitterReplica
+			numPods := headReplicas + workerReplicas
```

  yes, good point

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T10:22:21Z

- **test/e2e/tas/rayjob_test.go:95** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T10:22:21Z
```diff
@@ -89,11 +105,10 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 	ginkgo.When("Creating a RayJob", func() {
 		ginkgo.It("Should place pods based on the ranks-ordering", func() {
 			const (
-				headReplicas     = 1
-				workerReplicas   = 3
-				submitterReplica = 1
+				headReplicas   = 1
+				workerReplicas = 3
 			)
-			numPods := headReplicas + workerReplicas + submitterReplica
+			numPods := headReplicas + workerReplicas
```

  Changed to:
  `g.Expect(len(pods.Items)).Should(gomega.BeNumerically(">=", numPods))`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T11:29:32Z

- **test/e2e/tas/rayjob_test.go:197** by [@mimowo](https://github.com/mimowo) — 2025-03-17T11:29:32Z
```diff
@@ -179,7 +194,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 					},
 					ObjectMeta: metav1.ObjectMeta{
 						Annotations: map[string]string{
-							kueuealpha.PodSetRequiredTopologyAnnotation: testing.DefaultBlockTopologyLevel,
+							kueuealpha.PodSetPreferredTopologyAnnotation: testing.DefaultBlockTopologyLevel,
```

  Why is this changed?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T11:41:21Z

- **test/e2e/tas/rayjob_test.go:197** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T11:41:21Z
```diff
@@ -179,7 +194,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 					},
 					ObjectMeta: metav1.ObjectMeta{
 						Annotations: map[string]string{
-							kueuealpha.PodSetRequiredTopologyAnnotation: testing.DefaultBlockTopologyLevel,
+							kueuealpha.PodSetPreferredTopologyAnnotation: testing.DefaultBlockTopologyLevel,
```

  This was something I tested, but it shouldn't end up here, sorry.
  Reverting this ofc

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T12:07:54Z

- **test/e2e/tas/rayjob_test.go:62** by [@mimowo](https://github.com/mimowo) — 2025-03-17T12:07:53Z
```diff
@@ -45,6 +59,8 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 	)
 
 	ginkgo.BeforeEach(func() {
+		format.RegisterCustomFormatter(podsFormatter)
```

  It feels like this should be an orthogonal change. Maybe we want to use this (or another) formatter for other tests.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T12:43:23Z

- **test/e2e/tas/rayjob_test.go:62** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T12:43:23Z
```diff
@@ -45,6 +59,8 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 	)
 
 	ginkgo.BeforeEach(func() {
+		format.RegisterCustomFormatter(podsFormatter)
```

  Let me split it then

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T13:12:27Z

- **test/e2e/tas/rayjob_test.go:211** by [@mimowo](https://github.com/mimowo) — 2025-03-17T13:12:22Z
```diff
@@ -197,13 +201,16 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 			})
 
 			pods := &corev1.PodList{}
-			ginkgo.By("ensure all pods are created", func() {
+			ginkgo.By("ensure all pods are created and raycluster is ready", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(rayjob.Namespace))).To(gomega.Succeed())
-					g.Expect(pods.Items).Should(gomega.HaveLen(numPods))
+					g.Expect(len(pods.Items)).Should(gomega.BeNumerically(">=", numPods))
 					// The timeout is long to ensure all cluster pods are up and running.
 					// This is because the Ray image takes long time (around 170s on the CI)
 					// to load and then to sync with head.
+					createdRayJob := &rayv1.RayJob{}
```

  actually, o don't see a need to check the pods are running. checking that they get scheduled should be enough imo. I suggest to make the fix minimal. no need to add extra asserts. so that it is clear the assets are relaxed actually

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T13:39:09Z

- **test/e2e/tas/rayjob_test.go:225** by [@mimowo](https://github.com/mimowo) — 2025-03-17T13:39:09Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  Will this actually succeed in cases when we don't have the submitter Job and Pod created?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T13:40:59Z

- **test/e2e/tas/rayjob_test.go:236** by [@mimowo](https://github.com/mimowo) — 2025-03-17T13:40:58Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdRayJob := &rayv1.RayJob{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(rayjob), createdRayJob)).To(gomega.Succeed())
+					g.Expect(createdRayJob.Status.JobDeploymentStatus).To(gomega.Equal(rayv1.JobDeploymentStatusComplete))
+					g.Expect(createdRayJob.Status.JobStatus).To(gomega.Equal(rayv1.JobStatusSucceeded))
+					finishReasonMessage := "Job finished successfully."
```

  Actually, I would prefer not o test the message in e2e tests. Message is free to change at any point.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T13:47:56Z

- **test/e2e/tas/rayjob_test.go:225** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T13:47:56Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  No, but the benefit is we don't have to rely on the specific point in time when job submitter pod is created, only what it does.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T13:48:09Z

- **test/e2e/tas/rayjob_test.go:236** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T13:48:09Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdRayJob := &rayv1.RayJob{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(rayjob), createdRayJob)).To(gomega.Succeed())
+					g.Expect(createdRayJob.Status.JobDeploymentStatus).To(gomega.Equal(rayv1.JobDeploymentStatusComplete))
+					g.Expect(createdRayJob.Status.JobStatus).To(gomega.Equal(rayv1.JobStatusSucceeded))
+					finishReasonMessage := "Job finished successfully."
```

  sure, make sense, checking job status should be enough

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-17T14:04:36Z

- **test/e2e/tas/rayjob_test.go:225** by [@mimowo](https://github.com/mimowo) — 2025-03-17T14:04:36Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  Hm, so actually this PR would not fix the flake?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T14:20:41Z

- **test/e2e/tas/rayjob_test.go:225** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T14:20:41Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  If there is deeper issue with RayJob submitter pod then not

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-17T14:21:14Z

- **test/e2e/tas/rayjob_test.go:225** by [@mszadkow](https://github.com/mszadkow) — 2025-03-17T14:21:14Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  If there is no issue with submitter pod, just our test couldn't catch him, then yes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T07:37:40Z

- **test/e2e/tas/rayjob_test.go:225** by [@mimowo](https://github.com/mimowo) — 2025-03-18T07:37:40Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  Maybe just drop the expectation about the workload finishing for now? From TAS PoV the most important thing is that the Pods get scheduled, and the lend on the consecutive kind-worker nodes. I know we don't have rank based ordering for Ray, but we could just check the set of workers, as done here: https://github.com/kubernetes-sigs/kueue/blob/69a9538f234aaeed55d825ac04e8c1338e62c5a7/test/e2e/tas/pod_group_test.go#L169-L183

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-18T07:51:12Z

- **test/e2e/tas/rayjob_test.go:225** by [@mszadkow](https://github.com/mszadkow) — 2025-03-18T07:51:12Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  We do the similar thing anyway, in line: 222, just before this.
  We verify that each RayCluster worker node was placed on different node.
  You think it's enough?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T08:00:27Z

- **test/e2e/tas/rayjob_test.go:225** by [@mimowo](https://github.com/mimowo) — 2025-03-18T08:00:27Z
```diff
@@ -222,6 +223,20 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 				gotAssignment := readAssignedNodes(pods.Items)
 				gomega.Expect(gotAssignment).Should(gomega.HaveLen(workerReplicas))
 			})
+
+			wlLookupKey := types.NamespacedName{
+				Name: workloadrayjob.GetWorkloadNameForRayJob(rayjob.Name, rayjob.UID), Namespace: ns.Name,
+			}
+			ginkgo.By("Waiting for the RayJob to finish", func() {
```

  I would be ok-ish with that yes. However, this only asserts on the number of scheduled workers, but they could still be scattered. If they are scheduled by TAS they would not be scattered, so the assert on the set of workers is a bit stronger, confirming that TAS was used indeed, but this is not a strong view.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T08:04:08Z

- **test/e2e/tas/rayjob_test.go:207** by [@mimowo](https://github.com/mimowo) — 2025-03-18T08:04:08Z
```diff
@@ -200,7 +199,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 			ginkgo.By("ensure all pods are created", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(rayjob.Namespace))).To(gomega.Succeed())
-					g.Expect(pods.Items).Should(gomega.HaveLen(numPods))
+					g.Expect(len(pods.Items)).Should(gomega.BeNumerically(">=", numPods))
```

  Add a comment like:
  ```
  // TODO(#4665): strengthen the assert after moving to Ray >=1.3.1.
  // We temporarily don't assert on the exact number of Pods as this flakes on Ray
  // 1.2.2 due to the submitter Pod and Job not being created occasionally (See 
  // https://github.com/kubernetes-sigs/kueue/issues/4508#issuecomment-2724257298
  // for more details).
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-18T08:12:08Z

- **test/e2e/tas/rayjob_test.go:207** by [@mszadkow](https://github.com/mszadkow) — 2025-03-18T08:12:07Z
```diff
@@ -200,7 +199,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 			ginkgo.By("ensure all pods are created", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(rayjob.Namespace))).To(gomega.Succeed())
-					g.Expect(pods.Items).Should(gomega.HaveLen(numPods))
+					g.Expect(len(pods.Items)).Should(gomega.BeNumerically(">=", numPods))
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T08:21:51Z

- **test/e2e/tas/rayjob_test.go:207** by [@mimowo](https://github.com/mimowo) — 2025-03-18T08:21:51Z
```diff
@@ -200,7 +199,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 			ginkgo.By("ensure all pods are created", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(rayjob.Namespace))).To(gomega.Succeed())
-					g.Expect(pods.Items).Should(gomega.HaveLen(numPods))
+					g.Expect(len(pods.Items)).Should(gomega.BeNumerically(">=", numPods))
```

  Update `TODO(#4665)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-18T08:37:09Z

/lgtm
/approve
Thanks!

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-18T08:58:24Z

- **test/e2e/tas/rayjob_test.go:207** by [@mszadkow](https://github.com/mszadkow) — 2025-03-18T08:58:24Z
```diff
@@ -200,7 +199,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling for RayJob", ginkgo.Ordered, fu
 			ginkgo.By("ensure all pods are created", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.List(ctx, pods, client.InNamespace(rayjob.Namespace))).To(gomega.Succeed())
-					g.Expect(pods.Items).Should(gomega.HaveLen(numPods))
+					g.Expect(len(pods.Items)).Should(gomega.BeNumerically(">=", numPods))
```

  done
