# PR #4635: Assert Snapshot Unmodified in Preemption Tests

**Summary**: Assert Snapshot Unmodified in Preemption Tests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4635

**Last updated**: 2025-04-09T14:30:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Horiodino](https://github.com/Horiodino)
- **Branch**: `Assert_Snapshot_Unmodified` → `main`
- **Created**: 2025-03-17T08:44:16Z
- **Updated**: 2025-04-09T14:30:46Z
- **Merged**: 2025-04-09T14:30:43Z
- **Closed**: 2025-04-09T14:30:44Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 1   **+14 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind flake
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
It ensures the assertion catches modifications to mutable state ,that could indicate a bug in preemption logic and specificity ignoring irrelevant differences.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #
https://github.com/kubernetes-sigs/kueue/issues/4553#event-16794070155

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-17T08:44:28Z

Hi @Horiodino. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-17T08:44:37Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bbf316a3e076962cd34c77affe7ccfac97b89b4f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67f653d679d07c0008dda969 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-17T08:45:45Z

cc @gabesaba

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T09:00:04Z

/ok-to-test

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-03-17T09:30:54Z

/retest

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-17T09:38:01Z

Test failure looks related to the change. please test locally with `make test`, or `go test ./pkg/scheduler/preemption` for fast feedback loop

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-19T10:41:11Z

PTAL @gabesaba , is it ready to merge?

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-19T10:52:59Z

> PTAL @gabesaba , is it ready to merge?

No. We need to compare the Snapshot again in both places where we are currently comparing ResourceNodes. That entails reverting part of https://github.com/kubernetes-sigs/kueue/pull/4658/files

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-19T10:56:50Z

/hold
to address https://github.com/kubernetes-sigs/kueue/pull/4635#issuecomment-2736187842

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-03-19T11:49:49Z

> > PTAL @gabesaba , is it ready to merge?
> 
> No. We need to compare the Snapshot again in both places where we are currently comparing ResourceNodes. That entails reverting part of https://github.com/kubernetes-sigs/kueue/pull/4658/files

 Isn't it essentially the same? Here, `beforeResourceNodes := resourceNodes(beforeSnapshot)` is indirectly capturing a snapshot, and then it is being compared with `afterResourceNodes` using:  
 
> ```go
> if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
> ```
>  
> So, isn't `beforeResourceNodes` just a transformed version of `beforeSnapshot`, making it an implicit snapshot?

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-03-19T11:51:59Z

> I assume you're referring to something like this:  
>  
> ```go
> if diff := cmp.Diff(beforeSnapshot, snapshotWorkingCopy, snapCmpOpts); diff != "" {
>     t.Errorf("Snapshot was modified (-initial, +end):\n%s", diff)
> }
> ```  
>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T13:02:06Z

> > > PTAL @gabesaba , is it ready to merge?
> > 
> > 
> > No. We need to compare the Snapshot again in both places where we are currently comparing ResourceNodes. That entails reverting part of https://github.com/kubernetes-sigs/kueue/pull/4658/files
> 
> Isn't it essentially the same? Here, `beforeResourceNodes := resourceNodes(beforeSnapshot)` is indirectly capturing a snapshot, and then it is being compared with `afterResourceNodes` using:
> 
> > ```go
> > if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
> > ```
> > 
> > 
> >     
> >       
> >     
> > 
> >       
> >     
> > 
> >     
> >   
> > So, isn't `beforeResourceNodes` just a transformed version of `beforeSnapshot`, making it an implicit snapshot?

We need to check those to see if the preemptor does not modify those.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-20T15:12:04Z

> > I assume you're referring to something like this:
> > ```go
> > if diff := cmp.Diff(beforeSnapshot, snapshotWorkingCopy, snapCmpOpts); diff != "" {
> >     t.Errorf("Snapshot was modified (-initial, +end):\n%s", diff)
> > }
> > ```

Exactly, something like this. Comparing ResourceNodes was just to get something minimal working, as ^ seems complicated due to the generics in `hierarchy` package

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-03-22T16:40:54Z

Any thoughts on this?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-23T01:46:11Z

/release-note-edit
```release-note
NONE
```

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T14:36:25Z

/retitle Assert Snapshot Unmodified in Preemption Tests

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-05T12:37:02Z

LGTM label has been added.  <details>Git tree hash: 2aad849fdbb9a8e1f54e53370217e196d1e48f4d</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-04-07T09:48:07Z

/hold

Please test whether the suggestion in https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2014314069 is catching regressions properly [1]. All things considered, it would be better to have the simplest possible test code (as we are not testing our tests...). If that suggestion works, please update this PR to use it, rather than implementing custom diffing logic.

[1] You can do this by manipulating fields in either the before or after snapshot, and see if the diff catches it. Or introduce a bug in preemption.go, as suggested [here](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2011783859), and see if that is caught.

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-04-07T11:24:10Z

> /hold
> 
> Please test whether the suggestion in [#4635 (comment)](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2014314069) is catching regressions properly [1]. All things considered, it would be better to have the simplest possible test code (as we are not testing our tests...). If that suggestion works, please update this PR to use it, rather than implementing custom diffing logic.
> 
> [1] You can do this by manipulating fields in either the before or after snapshot, and see if the diff catches it. Or introduce a bug in preemption.go, as suggested [here](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2011783859), and see if that is caught.


```
@@ -454,7 +454,7 @@ func fairPreemptions(preemptionCtx *preemptionCtx, candidates []*workload.Info,
 
        revertSimulation()
        if !fits {
-               restoreSnapshot(preemptionCtx.snapshot, targets)
+               // restoreSnapshot(preemptionCtx.snapshot, targets)
                return nil
        }
        targets = fillBackWorkloads(preemptionCtx, targets, true)

```

its failing -> === FAIL: pkg/scheduler/preemption TestFairPreemptions (3.82s)

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-04-08T11:53:07Z

> > /hold
> > Please test whether the suggestion in [#4635 (comment)](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2014314069) is catching regressions properly [1]. All things considered, it would be better to have the simplest possible test code (as we are not testing our tests...). If that suggestion works, please update this PR to use it, rather than implementing custom diffing logic.
> > [1] You can do this by manipulating fields in either the before or after snapshot, and see if the diff catches it. Or introduce a bug in preemption.go, as suggested [here](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2011783859), and see if that is caught.
> 
> ```
> @@ -454,7 +454,7 @@ func fairPreemptions(preemptionCtx *preemptionCtx, candidates []*workload.Info,
>  
>         revertSimulation()
>         if !fits {
> -               restoreSnapshot(preemptionCtx.snapshot, targets)
> +               // restoreSnapshot(preemptionCtx.snapshot, targets)
>                 return nil
>         }
>         targets = fillBackWorkloads(preemptionCtx, targets, true)
> ```
> 
> its failing -> === FAIL: pkg/scheduler/preemption TestFairPreemptions (3.82s)

Please update the PR to use the simpler logic, then

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-04-08T16:00:24Z

> > > /hold
> > > Please test whether the suggestion in [#4635 (comment)](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2014314069) is catching regressions properly [1]. All things considered, it would be better to have the simplest possible test code (as we are not testing our tests...). If that suggestion works, please update this PR to use it, rather than implementing custom diffing logic.
> > > [1] You can do this by manipulating fields in either the before or after snapshot, and see if the diff catches it. Or introduce a bug in preemption.go, as suggested [here](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2011783859), and see if that is caught.
> > 
> > 
> > ```
> > @@ -454,7 +454,7 @@ func fairPreemptions(preemptionCtx *preemptionCtx, candidates []*workload.Info,
> >  
> >         revertSimulation()
> >         if !fits {
> > -               restoreSnapshot(preemptionCtx.snapshot, targets)
> > +               // restoreSnapshot(preemptionCtx.snapshot, targets)
> >                 return nil
> >         }
> >         targets = fillBackWorkloads(preemptionCtx, targets, true)
> > ```
> > 
> > 
> >     
> >       
> >     
> > 
> >       
> >     
> > 
> >     
> >   
> > its failing -> === FAIL: pkg/scheduler/preemption TestFairPreemptions (3.82s)
> 
> Please update the PR to use the simpler logic, then

But wasn't that the whole point — to comment out that specific piece of code, and if it throws an error (without ignoring the entire snapshot), then it's actually working as intended?
I'm just not sure what you're referring to.
Are you saying the logic is incorrect, or just that it should be implemented in a more minimal way?

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-04-09T07:08:35Z

> But wasn't that the whole point — to comment out that specific piece of code, and if it throws an error (without ignoring the entire snapshot), then it's actually working as intended? 

I was suggesting to test whether the minimal piece of code is catching error as expected, as I thought that you were concerned ([comment](https://github.com/kubernetes-sigs/kueue/pull/4635#discussion_r2014433716)) that it didn't work as intended

> Are you saying the logic is incorrect, or just that it should be implemented in a more minimal way?

The second - that we want to minimize code whenever possible, as I wrote here:

> All things considered, it would be better to have the simplest possible test code (as we are not testing our tests...). If that suggestion works, please update this PR to use it, rather than implementing custom diffing logic.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-04-09T11:35:06Z

Thanks for making the changes and for your patience, @Horiodino. This logic is still catching regressions, right?

/lgtm
/unhold
/remove-label flake
/label cleanup

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-09T11:35:08Z

@gabesaba: The label(s) `/label cleanup, /remove-label flake` cannot be applied. These labels are supported: `api-review, tide/merge-method-merge, tide/merge-method-rebase, tide/merge-method-squash, team/katacoda, refactor, ci-short, ci-extended, ci-full`. Is this label configured under `labels -> additional_labels` or `labels -> restricted_labels` in `plugin.yaml`?

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4635#issuecomment-2789383439):

>Thanks for making the changes and for your patience, @Horiodino. This logic is still catching regressions, right?
>
>/lgtm
>/unhold
>/remove-label flake
>/label cleanup


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-09T11:35:13Z

LGTM label has been added.  <details>Git tree hash: 64ff81514329998ae44ff829480ce02ed1cb1dfe</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-04-09T11:36:01Z

/remove-label kind-flake
/label kind-cleanup

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-09T11:36:04Z

@gabesaba: The label(s) `/label kind-cleanup, /remove-label kind-flake` cannot be applied. These labels are supported: `api-review, tide/merge-method-merge, tide/merge-method-rebase, tide/merge-method-squash, team/katacoda, refactor, ci-short, ci-extended, ci-full`. Is this label configured under `labels -> additional_labels` or `labels -> restricted_labels` in `plugin.yaml`?

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4635#issuecomment-2789385499):

>/remove-label kind-flake
>/label kind-cleanup


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-04-09T11:36:34Z

/remove-kind flake
/kind cleanup

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-04-09T11:40:48Z

> Thanks for making the changes and for your patience, @Horiodino. This logic is still catching regressions, right?
> 
> /lgtm /unhold /remove-label flake /label cleanup

yep its catching .

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-09T14:28:55Z

/approve
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-09T14:29:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4635#" title="Author self-approved">Horiodino</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4635#pullrequestreview-2744880465" title="LGTM">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4635#issuecomment-2789935197" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-18T08:25:21Z

- **pkg/scheduler/preemption/preemption_test.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-18T08:24:28Z
```diff
@@ -51,18 +51,17 @@ import (
 )
 
 var snapCmpOpts = []cmp.Option{
+	cmpopts.IgnoreFields(cache.Snapshot{}, "ResourceFlavors"),
+	cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{},
+		"Preemption",
+		"FairWeight",
+	),
+	cmpopts.IgnoreFields(cache.CohortSnapshot{},
+		"Name",
+	),
 	cmpopts.EquateEmpty(),
-	cmpopts.IgnoreUnexported(hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
-	cmpopts.IgnoreUnexported(hierarchy.ClusterQueue[*cache.CohortSnapshot]{}),
+	cmpopts.IgnoreUnexported(cache.Snapshot{}),
```

  ```suggestion
  ```
  
  We should not ignore the whole of Snapshot. We should verify this if it is expected.
  @gabesaba Did you indicate ignoring a whole of snapshot field when you create tracking issue?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-18T09:33:04Z

- **pkg/scheduler/preemption/preemption_test.go:63** by [@gabesaba](https://github.com/gabesaba) — 2025-03-18T09:33:04Z
```diff
@@ -51,18 +51,17 @@ import (
 )
 
 var snapCmpOpts = []cmp.Option{
+	cmpopts.IgnoreFields(cache.Snapshot{}, "ResourceFlavors"),
+	cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{},
+		"Preemption",
+		"FairWeight",
+	),
+	cmpopts.IgnoreFields(cache.CohortSnapshot{},
+		"Name",
+	),
 	cmpopts.EquateEmpty(),
-	cmpopts.IgnoreUnexported(hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
-	cmpopts.IgnoreUnexported(hierarchy.ClusterQueue[*cache.CohortSnapshot]{}),
+	cmpopts.IgnoreUnexported(cache.Snapshot{}),
```

  No, we do not want to ignore the entire Snapshot. I am merging https://github.com/kubernetes-sigs/kueue/pull/4658 as a starting point @Horiodino, as those are the most critical fields to compare. After that merges, you can expand and try to get Snapshot working again

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-25T10:26:05Z

- **pkg/scheduler/preemption/preemption_test.go:59** by [@gabesaba](https://github.com/gabesaba) — 2025-03-25T10:15:46Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  I think this will ignore too much, as most everything we care about in the snapshot is in the private fields - for Cohorts and ClusterQueues - within this data structure

- **pkg/scheduler/preemption/preemption_test.go:1892** by [@gabesaba](https://github.com/gabesaba) — 2025-03-25T10:20:49Z
```diff
@@ -1879,6 +1881,10 @@ func TestPreemption(t *testing.T) {
 				t.Errorf("Reported %d preemptions, want %d", preempted, tc.wantPreempted.Len())
 			}
 
+			if diff := cmp.Diff(beforeSnapshot, snapshotWorkingCopy, snapCmpOpts); diff != "" {
+				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
+			}
+
 			beforeResourceNodes := resourceNodes(beforeSnapshot)
```

  Please revert #4658. These diffs should be done as part of the snapshot comparison

- **pkg/scheduler/preemption/preemption_test.go:59** by [@gabesaba](https://github.com/gabesaba) — 2025-03-25T10:25:20Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  When testing, I would recommend commenting out some relevant line in preemption.go. Such as line 457:
  https://github.com/kubernetes-sigs/kueue/blob/3ec058c12f4e77cb01646fee0a405dc0bc2970fd/pkg/scheduler/preemption/preemption.go#L455-L459
  
  or introducing a bug here:
  https://github.com/kubernetes-sigs/kueue/blob/3ec058c12f4e77cb01646fee0a405dc0bc2970fd/pkg/scheduler/preemption/preemption.go#L344-L348
  
  The snapshot diff should fail in this case, since we missed reverting the snapshot. Testing like this ensures that the logic you're writing is acting as a safety net, and not just passing because it ignores everything
  
  Right now, if you remove this line, it will fail due to the ResourceNode diff. Make sure that it still fails when only diffing the Snapshot, once you delete the ResourceNode specific logic (see my other comment)

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-03-25T14:16:21Z

- **pkg/scheduler/preemption/preemption_test.go:59** by [@Horiodino](https://github.com/Horiodino) — 2025-03-25T14:16:21Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  > I think this will ignore too much, as most everything we care about in the snapshot is in the private fields - for Cohorts and ClusterQueues - within this data structure
  
  I believe using a custom `Comparer` would be a good option. I had implemented it previously but later removed later, it was using the default resource values causing err.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-25T15:10:21Z

- **pkg/scheduler/preemption/preemption_test.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-25T15:10:21Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  > Right now, if you remove this line, it will fail due to the ResourceNode diff.
  
  Without it we have an error:
  
  ```
  panic: cannot handle unexported field at {*cache.Snapshot}.Manager.cohorts:
  		"sigs.k8s.io/kueue/pkg/hierarchy".Manager[*sigs.k8s.io/kueue/pkg/cache.ClusterQueueSnapshot,*sigs.k8s.io/kueue/pkg/cache.CohortSnapshot]
  	consider using a custom Comparer; if you control the implementation of type, you can also consider using an Exporter, AllowUnexported, or cmpopts.IgnoreUnexported [recovered]
  	panic: cannot handle unexported field at {*cache.Snapshot}.Manager.cohorts:
  		"sigs.k8s.io/kueue/pkg/hierarchy".Manager[*sigs.k8s.io/kueue/pkg/cache.ClusterQueueSnapshot,*sigs.k8s.io/kueue/pkg/cache.CohortSnapshot]
  	consider using a custom Comparer; if you control the implementation of type, you can also consider using an Exporter, AllowUnexported, or cmpopts.IgnoreUnexported
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-25T15:27:26Z

- **pkg/scheduler/preemption/preemption_test.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-25T15:27:26Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  I think we can keep it as it is and create separate comparer(s) for Cohorts and ClusterQueues.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-26T14:32:09Z

- **pkg/scheduler/preemption/preemption_test.go:59** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T14:32:08Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  Sorry, when I said removing this line, I meant deleting one of the lines in preemption.go to help with development, to make sure that the test code is catching the introduced bug.
  
  @Horiodino, I think that this should be possible without so much custom code, by using cmp.diff on Snapshot, and changing the diff options. How about something like this? https://github.com/gabesaba/kueue/commit/ed2527a255666b29db01e6398d1743fdc4d3923b

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-03-26T15:27:15Z

- **pkg/scheduler/preemption/preemption_test.go:59** by [@Horiodino](https://github.com/Horiodino) — 2025-03-26T15:27:15Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  https://github.com/gabesaba/kueue/commit/ed2527a255666b29db01e6398d1743fdc4d3923b But isn't this ignoring Cohort? What I'm saying is that we can compare Cohort values in a way similar to beforeParent := before.Parent(), allowing us to retrieve the value from the Cohort struct,Rather than ignoring .

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-04T04:38:25Z

- **pkg/scheduler/preemption/preemption_test.go:2718** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T04:37:41Z
```diff
@@ -2697,6 +2704,10 @@ func TestFairPreemptions(t *testing.T) {
 				t.Errorf("Issued preemptions (-want,+got):\n%s", diff)
 			}
 
+			if equal, got := compareSnapshots(beforeSnapshot, snapshotWorkingCopy); !equal {
+				t.Errorf("Snapshot was modified: %v", got)
+			}
```

  I would propose to go on this way:
  
  ```go
  var snapCmpOpts = cmp.Options{
  	cmp.AllowUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
  	cmp.Comparer(hierarchyManagerComparer),
  }
  
  if diff := cmp.Diff(beforeSnapshot, snapshotWorkingCopy, snapCmpOpts); diff != "" {
  	t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
  }
  
  func hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
  	if !clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
  		return false
  	}
  	if !cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
  		return false
  	}
  	return true
  }
  
  func clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
  	if len(x) != len(y) {
  		return false
  	}
  	for name, xSnapshot := range x {
  		ySnapshot, ok := y[name]
  		if !ok {
  			return false
  		}
  		if !clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
  			return false
  		}
  	}
  	return true
  }
  
  func clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
  	diff := cmp.Diff(x, y, cmp.Options{
  		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"),
  	})
  	if diff != "" {
  		return false
  	}
  	if x == nil {
  		return true
  	}
  	if !cohortSnapshotComparer(x.ClusterQueue.Parent(), y.ClusterQueue.Parent()) {
  		return false
  	}
  	return true
  }
  
  func cohortSnapshotsComparer(x, y map[kueue.CohortReference]*cache.CohortSnapshot) bool {
  	if len(x) != len(y) {
  		return false
  	}
  	for name, xSnapshot := range x {
  		ySnapshot, ok := y[name]
  		if !ok {
  			return false
  		}
  		if !cohortSnapshotComparer(xSnapshot, ySnapshot) {
  			return false
  		}
  	}
  	return true
  }
  
  func cohortSnapshotComparer(x, y *cache.CohortSnapshot) bool {
  	diff := cmp.Diff(x, y, cmp.Options{
  		cmpopts.IgnoreFields(cache.CohortSnapshot{}, "Cohort"),
  	})
  	if diff != "" {
  		return false
  	}
  	if x == nil {
  		return true
  	}
  	if !cohortSnapshotComparer(x.Cohort.Parent(), y.Cohort.Parent()) {
  		return false
  	}
  	// TODO: Compare x.Cohort.ChildCohorts() and x.Cohort.ChildCQs()
  	return true
  }
  ```

- **pkg/scheduler/preemption/preemption_test.go:2716** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T04:38:02Z
```diff
@@ -2697,6 +2704,10 @@ func TestFairPreemptions(t *testing.T) {
 				t.Errorf("Issued preemptions (-want,+got):\n%s", diff)
 			}
 
+			if equal, got := compareSnapshots(beforeSnapshot, snapshotWorkingCopy); !equal {
+				t.Errorf("Snapshot was modified: %v", got)
+			}
+
 			beforeResourceNodes := resourceNodes(beforeSnapshot)
 			afterResourceNodes := resourceNodes(snapshotWorkingCopy)
 			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
```

  I think this no need if we have custom comparer.

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-04-05T06:03:22Z

- **pkg/scheduler/preemption/preemption_test.go:2718** by [@Horiodino](https://github.com/Horiodino) — 2025-04-05T06:03:22Z
```diff
@@ -2697,6 +2704,10 @@ func TestFairPreemptions(t *testing.T) {
 				t.Errorf("Issued preemptions (-want,+got):\n%s", diff)
 			}
 
+			if equal, got := compareSnapshots(beforeSnapshot, snapshotWorkingCopy); !equal {
+				t.Errorf("Snapshot was modified: %v", got)
+			}
```

  i just added a small change as it needs to be  evaluate at every level of the hierarchy

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T07:54:32Z

- **pkg/scheduler/preemption/preemption_test.go:1897** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T06:10:03Z
```diff
@@ -1879,9 +1887,13 @@ func TestPreemption(t *testing.T) {
 				t.Errorf("Reported %d preemptions, want %d", preempted, tc.wantPreempted.Len())
 			}
 
+			if diff := cmp.Diff(beforeSnapshot, snapshotWorkingCopy, SnapCmpOpts()); diff != "" {
+				t.Errorf("Snapshot was modified by preemption (-initial,+end):\n%s", diff)
+			}
+
 			beforeResourceNodes := resourceNodes(beforeSnapshot)
 			afterResourceNodes := resourceNodes(snapshotWorkingCopy)
-			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
+			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, SnapCmpOpts()); diff != "" {
 				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
 			}
 		})
```

  I think if doesn't need now.

- **pkg/scheduler/preemption/preemption_test.go:2718** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T06:11:43Z
```diff
@@ -2697,9 +2710,13 @@ func TestFairPreemptions(t *testing.T) {
 				t.Errorf("Issued preemptions (-want,+got):\n%s", diff)
 			}
 
+			if diff := cmp.Diff(beforeSnapshot, snapshotWorkingCopy, SnapCmpOpts()); diff != "" {
+				t.Errorf("Snapshot was modified by preemption (-initial,+end):\n%s", diff)
+			}
+
 			beforeResourceNodes := resourceNodes(beforeSnapshot)
 			afterResourceNodes := resourceNodes(snapshotWorkingCopy)
-			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
+			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, SnapCmpOpts()); diff != "" {
 				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
 			}
```

  Also here.

- **pkg/scheduler/preemption/preemption_test.go:2956** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:09:11Z
```diff
@@ -2796,3 +2813,83 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+func hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
+	if !clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
+		return false
+	}
+	if !cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
+		return false
+	}
+	return true
+}
+
+func clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
+	// need to evaluate at every level of the hierarchy
+	opt := append(SnapCmpOpts(),
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"),
+		cmpopts.IgnoreFields(hierarchy.ClusterQueue[*cache.CohortSnapshot]{}, "cohort"),
+	)
+
+	diff := cmp.Diff(x, y, opt...)
+	if diff != "" {
+		return false
+	}
+	if x == nil {
+		return true
+	}
+	if !cohortSnapshotComparer(x.ClusterQueue.Parent(), y.ClusterQueue.Parent()) {
+		return false
+	}
+	return true
+}
+
+func cohortSnapshotsComparer(x, y map[kueue.CohortReference]*cache.CohortSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !cohortSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func cohortSnapshotComparer(x, y *cache.CohortSnapshot) bool {
+	opt := append(SnapCmpOpts(),
+		cmpopts.IgnoreFields(cache.CohortSnapshot{}, "Cohort"),
+	)
+
+	diff := cmp.Diff(x, y, opt...)
+	if diff != "" {
+		return false
+	}
+	if x == nil {
+		return true
+	}
+	if !cohortSnapshotComparer(x.Cohort.Parent(), y.Cohort.Parent()) {
+		return false
+	}
+	return true
```

  Shouldn't we be comparing x.Cohort.ChildCohorts() and x.Cohort.ChildCQs()?

- **pkg/scheduler/preemption/preemption_test.go:2845** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:13:04Z
```diff
@@ -2796,3 +2813,83 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+func hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
+	if !clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
+		return false
+	}
+	if !cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
+		return false
+	}
+	return true
+}
+
+func clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
+	// need to evaluate at every level of the hierarchy
+	opt := append(SnapCmpOpts(),
```

  Please don't use SnapCmpOpts() here.

- **pkg/scheduler/preemption/preemption_test.go:2851** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:14:26Z
```diff
@@ -2796,3 +2813,83 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+func hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
+	if !clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
+		return false
+	}
+	if !cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
+		return false
+	}
+	return true
+}
+
+func clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
+	// need to evaluate at every level of the hierarchy
+	opt := append(SnapCmpOpts(),
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"),
+		cmpopts.IgnoreFields(hierarchy.ClusterQueue[*cache.CohortSnapshot]{}, "cohort"),
+	)
+
+	diff := cmp.Diff(x, y, opt...)
+	if diff != "" {
```

  ```suggestion
  	if !cmp.Equal(x, y, opt...) {
  ```
  
  You used cmp.Equal() in your custom Comparer. It is good point. I forgot about it. You right, it's much better to use it instead of cmp.Diff().

- **pkg/scheduler/preemption/preemption_test.go:2880** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:15:31Z
```diff
@@ -2796,3 +2813,83 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+func hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
+	if !clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
+		return false
+	}
+	if !cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
+		return false
+	}
+	return true
+}
+
+func clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
+	// need to evaluate at every level of the hierarchy
+	opt := append(SnapCmpOpts(),
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"),
+		cmpopts.IgnoreFields(hierarchy.ClusterQueue[*cache.CohortSnapshot]{}, "cohort"),
+	)
+
+	diff := cmp.Diff(x, y, opt...)
+	if diff != "" {
+		return false
+	}
+	if x == nil {
+		return true
+	}
+	if !cohortSnapshotComparer(x.ClusterQueue.Parent(), y.ClusterQueue.Parent()) {
+		return false
+	}
+	return true
+}
+
+func cohortSnapshotsComparer(x, y map[kueue.CohortReference]*cache.CohortSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !cohortSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func cohortSnapshotComparer(x, y *cache.CohortSnapshot) bool {
+	opt := append(SnapCmpOpts(),
```

  Here the same. Don't use this SnapCmpOpts() here. If I correct understood you need just:
  
  ```
  cmp.Comparer(func(x, y cache.ResourceNode) bool {
  	return cmp.Equal(x.Quotas, y.Quotas) && cmp.Equal(x.SubtreeQuota, y.SubtreeQuota)
  })
  ```

- **pkg/scheduler/preemption/preemption_test.go:2885** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:16:42Z
```diff
@@ -2796,3 +2813,83 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+func hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
+	if !clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
+		return false
+	}
+	if !cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
+		return false
+	}
+	return true
+}
+
+func clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
+	// need to evaluate at every level of the hierarchy
+	opt := append(SnapCmpOpts(),
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"),
+		cmpopts.IgnoreFields(hierarchy.ClusterQueue[*cache.CohortSnapshot]{}, "cohort"),
+	)
+
+	diff := cmp.Diff(x, y, opt...)
+	if diff != "" {
+		return false
+	}
+	if x == nil {
+		return true
+	}
+	if !cohortSnapshotComparer(x.ClusterQueue.Parent(), y.ClusterQueue.Parent()) {
+		return false
+	}
+	return true
+}
+
+func cohortSnapshotsComparer(x, y map[kueue.CohortReference]*cache.CohortSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !cohortSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func cohortSnapshotComparer(x, y *cache.CohortSnapshot) bool {
+	opt := append(SnapCmpOpts(),
+		cmpopts.IgnoreFields(cache.CohortSnapshot{}, "Cohort"),
+	)
+
+	diff := cmp.Diff(x, y, opt...)
+	if diff != "" {
```

  ```suggestion
  	if !cmp.Equal(x, y, opt...) {
  ```
  And here.

- **pkg/scheduler/preemption/preemption_test.go:58** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:17:38Z
```diff
@@ -43,17 +43,25 @@ import (
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/features"
-	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/hierarchy"
 	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
 	"sigs.k8s.io/kueue/pkg/util/slices"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-var snapCmpOpts = cmp.Options{
-	// ignore zero values during comparison, as we consider
-	// zero FlavorResource usage to be same as no map entry.
-	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+func SnapCmpOpts() cmp.Options {
+	return cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
```

  ```suggestion
  ```
  
  I think we don't need this. We already ignored it by `hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},`.

- **pkg/scheduler/preemption/preemption_test.go:63** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:34:00Z
```diff
@@ -43,17 +43,25 @@ import (
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/features"
-	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/hierarchy"
 	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
 	"sigs.k8s.io/kueue/pkg/util/slices"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-var snapCmpOpts = cmp.Options{
-	// ignore zero values during comparison, as we consider
-	// zero FlavorResource usage to be same as no map entry.
-	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+func SnapCmpOpts() cmp.Options {
+	return cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+		),
+		cmp.Comparer(hierarchyManagerComparer),
+		cmp.Comparer(func(x, y cache.ResourceNode) bool {
+			return cmp.Equal(x.Quotas, y.Quotas) && cmp.Equal(x.SubtreeQuota, y.SubtreeQuota)
+		}),
```

  IIUC, you're ignoring the Usage field, right? Try using IgnoreFields:
  
  ```
  cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"),
  ```
  
  And please use it on custom comparers or separate cmpopts.

- **pkg/scheduler/preemption/preemption_test.go:63** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T07:37:07Z
```diff
@@ -43,17 +43,25 @@ import (
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/features"
-	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/hierarchy"
 	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
 	"sigs.k8s.io/kueue/pkg/util/slices"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-var snapCmpOpts = cmp.Options{
-	// ignore zero values during comparison, as we consider
-	// zero FlavorResource usage to be same as no map entry.
-	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+func SnapCmpOpts() cmp.Options {
+	return cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+		),
+		cmp.Comparer(hierarchyManagerComparer),
+		cmp.Comparer(func(x, y cache.ResourceNode) bool {
+			return cmp.Equal(x.Quotas, y.Quotas) && cmp.Equal(x.SubtreeQuota, y.SubtreeQuota)
+		}),
```

  Actually, here's a problematic part: should we ignore it globally? Or would it be better to propagate cmpopts into the custom comparer to conditionally ignore this field?
  
  From what I see, we don’t need to ignore it in TestPreemption, and maybe not in all cases within TestFairPreemptions either.
  
  So it would be great if we could change these options dynamically.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T08:19:34Z

- **pkg/scheduler/preemption/preemption_test.go:63** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T08:19:34Z
```diff
@@ -43,17 +43,25 @@ import (
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/features"
-	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/hierarchy"
 	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
 	"sigs.k8s.io/kueue/pkg/util/slices"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-var snapCmpOpts = cmp.Options{
-	// ignore zero values during comparison, as we consider
-	// zero FlavorResource usage to be same as no map entry.
-	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+func SnapCmpOpts() cmp.Options {
+	return cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+		),
+		cmp.Comparer(hierarchyManagerComparer),
+		cmp.Comparer(func(x, y cache.ResourceNode) bool {
+			return cmp.Equal(x.Quotas, y.Quotas) && cmp.Equal(x.SubtreeQuota, y.SubtreeQuota)
+		}),
```

  I think it would be great to use struct for this. Maybe something like:
  ```go
  type SnapshotComparer struct {
  	opts cmp.Options
  }
  
  func NewSnapshotComparer(opts ...cmp.Option) *SnapshotComparer {
  	return &SnapshotComparer{opts: opts}
  }
  
  func (c *SnapshotComparer) Diff(x, y *cache.Snapshot) string {
  	return cmp.Diff(x, y, append(c.opts, cmp.Options{
  		cmp.AllowUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
  		cmp.Comparer(c.hierarchyManagerComparer),
  	}))
  }
  
  func (c *SnapshotComparer) hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
  	if !c.clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
  		return false
  	}
  	if !c.cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
  		return false
  	}
  	return true
  }
  
  func (c *SnapshotComparer) clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
  	if len(x) != len(y) {
  		return false
  	}
  	for name, xSnapshot := range x {
  		ySnapshot, ok := y[name]
  		if !ok {
  			return false
  		}
  		if !c.clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
  			return false
  		}
  	}
  	return true
  }
  
  func (c *SnapshotComparer) clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
  	if !cmp.Equal(x, y, append(c.opts, cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"))) {
  		return false
  	}
  	if x == nil {
  		return true
  	}
  	if !c.cohortSnapshotComparer(x.ClusterQueue.Parent(), y.ClusterQueue.Parent()) {
  		return false
  	}
  	return true
  }
  
  func (c *SnapshotComparer) cohortSnapshotsComparer(x, y map[kueue.CohortReference]*cache.CohortSnapshot) bool {
  	if len(x) != len(y) {
  		return false
  	}
  	for name, xSnapshot := range x {
  		ySnapshot, ok := y[name]
  		if !ok {
  			return false
  		}
  		if !c.cohortSnapshotComparer(xSnapshot, ySnapshot) {
  			return false
  		}
  	}
  	return true
  }
  
  func (c *SnapshotComparer) cohortSnapshotComparer(x, y *cache.CohortSnapshot) bool {
  	if !cmp.Equal(x, y, append(c.opts, cmpopts.IgnoreFields(cache.CohortSnapshot{}, "Cohort"))) {
  		return false
  	}
  	if x == nil {
  		return true
  	}
  	if !c.cohortSnapshotComparer(x.Cohort.Parent(), y.Cohort.Parent()) {
  		return false
  	}
  	// TODO: Compare x.Cohort.ChildCohorts() and x.Cohort.ChildCQs()
  	return true
  }
  ```
  
  And get diff with
  ```go
  snapshotComparer := NewSnapshotComparer(cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"))
  if diff := snapshotComparer.Diff(beforeSnapshot, snapshotWorkingCopy); diff != "" {
  	t.Errorf("Snapshot was modified by preemption (-initial,+end):\n%s", diff)
  }
  ```
  
  
  WDYT?

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-04-05T08:46:22Z

- **pkg/scheduler/preemption/preemption_test.go:63** by [@Horiodino](https://github.com/Horiodino) — 2025-04-05T08:46:22Z
```diff
@@ -43,17 +43,25 @@ import (
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/features"
-	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/hierarchy"
 	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
 	"sigs.k8s.io/kueue/pkg/util/slices"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-var snapCmpOpts = cmp.Options{
-	// ignore zero values during comparison, as we consider
-	// zero FlavorResource usage to be same as no map entry.
-	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+func SnapCmpOpts() cmp.Options {
+	return cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+		),
+		cmp.Comparer(hierarchyManagerComparer),
+		cmp.Comparer(func(x, y cache.ResourceNode) bool {
+			return cmp.Equal(x.Quotas, y.Quotas) && cmp.Equal(x.SubtreeQuota, y.SubtreeQuota)
+		}),
```

  Yeah, it's LGTM. Just one thing  in `cohortSnapshotComparer`, when comparing `x.Cohort.ChildCohorts()` and `x.Cohort.ChildCQs()` using `cmp.Equal(..., c.opts)`, I think the issue might be that `cmp.AllowUnexported` isn't set for all the necessary types. Let me take a closer look. Otherwise, the structure looks really solid.

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-04-05T10:37:24Z

- **pkg/scheduler/preemption/preemption_test.go:63** by [@Horiodino](https://github.com/Horiodino) — 2025-04-05T10:37:24Z
```diff
@@ -43,17 +43,25 @@ import (
 	"sigs.k8s.io/kueue/pkg/constants"
 	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
 	"sigs.k8s.io/kueue/pkg/features"
-	"sigs.k8s.io/kueue/pkg/resources"
+	"sigs.k8s.io/kueue/pkg/hierarchy"
 	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
 	"sigs.k8s.io/kueue/pkg/util/slices"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-var snapCmpOpts = cmp.Options{
-	// ignore zero values during comparison, as we consider
-	// zero FlavorResource usage to be same as no map entry.
-	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+func SnapCmpOpts() cmp.Options {
+	return cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+		),
+		cmp.Comparer(hierarchyManagerComparer),
+		cmp.Comparer(func(x, y cache.ResourceNode) bool {
+			return cmp.Equal(x.Quotas, y.Quotas) && cmp.Equal(x.SubtreeQuota, y.SubtreeQuota)
+		}),
```

  Hey, I’ve made some changes and added comments explaining the reasoning behind them. Let me know if there’s anything else you’d like me to look into.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T11:00:14Z

- **pkg/scheduler/preemption/preemption_test.go:2848** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:00:04Z
```diff
@@ -2796,3 +2773,185 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+var baseOpts cmp.Options
+
+func init() {
+	baseOpts = cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+			hierarchy.ClusterQueue[*cache.CohortSnapshot]{},
+			hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+		),
+		// comparer for sets based on content, not pointers else causing false negatives
+		cmp.Comparer(func(x, y sets.Set[*cache.ClusterQueueSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			// sort by name for  consistent comparison,  else its unordered causing false negatives
+			sortByName(xList)
+			sortByName(yList)
+			for i := range xList {
+				if !clusterQueueSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+		cmp.Comparer(func(x, y sets.Set[*cache.CohortSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			sortCohortByName(xList)
+			sortCohortByName(yList)
+			for i := range xList {
+				if !cohortSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+	}
+}
+
+func sortByName(slice []*cache.ClusterQueueSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func sortCohortByName(slice []*cache.CohortSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func clusterQueueSnapshotContentEqual(x, y *cache.ClusterQueueSnapshot) bool {
+	if x == nil && y == nil {
+		return true
+	}
+	if x == nil || y == nil {
+		return false
+	}
+	opts := append(baseOpts,
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue"),
+		cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"),
```

  Please move this to 
  
  ```
  snapshotComparer := NewSnapshotComparer(cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"))
  ```
  
  where this really need.

- **pkg/scheduler/preemption/preemption_test.go:2867** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:00:11Z
```diff
@@ -2796,3 +2773,185 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+var baseOpts cmp.Options
+
+func init() {
+	baseOpts = cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+			hierarchy.ClusterQueue[*cache.CohortSnapshot]{},
+			hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+		),
+		// comparer for sets based on content, not pointers else causing false negatives
+		cmp.Comparer(func(x, y sets.Set[*cache.ClusterQueueSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			// sort by name for  consistent comparison,  else its unordered causing false negatives
+			sortByName(xList)
+			sortByName(yList)
+			for i := range xList {
+				if !clusterQueueSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+		cmp.Comparer(func(x, y sets.Set[*cache.CohortSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			sortCohortByName(xList)
+			sortCohortByName(yList)
+			for i := range xList {
+				if !cohortSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+	}
+}
+
+func sortByName(slice []*cache.ClusterQueueSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func sortCohortByName(slice []*cache.CohortSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func clusterQueueSnapshotContentEqual(x, y *cache.ClusterQueueSnapshot) bool {
+	if x == nil && y == nil {
+		return true
+	}
+	if x == nil || y == nil {
+		return false
+	}
+	opts := append(baseOpts,
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue"),
+		cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"),
+	)
+	if !cmp.Equal(x, y, opts) {
+		return false
+	}
+	xCohort := x.ClusterQueue.Parent()
+	yCohort := y.ClusterQueue.Parent()
+	return cohortSnapshotContentEqual(xCohort, yCohort)
+}
+
+func cohortSnapshotContentEqual(x, y *cache.CohortSnapshot) bool {
+	if x == nil && y == nil {
+		return true
+	}
+	if x == nil || y == nil {
+		return false
+	}
+	opts := append(baseOpts,
+		cmpopts.IgnoreFields(cache.CohortSnapshot{}, "Cohort"),
+		cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"),
```

  And this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T11:03:30Z

- **pkg/scheduler/preemption/preemption_test.go:2839** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:02:14Z
```diff
@@ -2796,3 +2773,185 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+var baseOpts cmp.Options
+
+func init() {
+	baseOpts = cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+			hierarchy.ClusterQueue[*cache.CohortSnapshot]{},
+			hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+		),
+		// comparer for sets based on content, not pointers else causing false negatives
+		cmp.Comparer(func(x, y sets.Set[*cache.ClusterQueueSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			// sort by name for  consistent comparison,  else its unordered causing false negatives
+			sortByName(xList)
+			sortByName(yList)
+			for i := range xList {
+				if !clusterQueueSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+		cmp.Comparer(func(x, y sets.Set[*cache.CohortSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			sortCohortByName(xList)
+			sortCohortByName(yList)
+			for i := range xList {
+				if !cohortSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+	}
+}
+
+func sortByName(slice []*cache.ClusterQueueSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func sortCohortByName(slice []*cache.CohortSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func clusterQueueSnapshotContentEqual(x, y *cache.ClusterQueueSnapshot) bool {
```

  ```suggestion
  func (c *SnapshotComparer) clusterQueueSnapshotContentEqual(x, y *cache.ClusterQueueSnapshot) bool {
  ```

- **pkg/scheduler/preemption/preemption_test.go:2858** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:02:22Z
```diff
@@ -2796,3 +2773,185 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+var baseOpts cmp.Options
+
+func init() {
+	baseOpts = cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+			hierarchy.ClusterQueue[*cache.CohortSnapshot]{},
+			hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+		),
+		// comparer for sets based on content, not pointers else causing false negatives
+		cmp.Comparer(func(x, y sets.Set[*cache.ClusterQueueSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			// sort by name for  consistent comparison,  else its unordered causing false negatives
+			sortByName(xList)
+			sortByName(yList)
+			for i := range xList {
+				if !clusterQueueSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+		cmp.Comparer(func(x, y sets.Set[*cache.CohortSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			sortCohortByName(xList)
+			sortCohortByName(yList)
+			for i := range xList {
+				if !cohortSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+	}
+}
+
+func sortByName(slice []*cache.ClusterQueueSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func sortCohortByName(slice []*cache.CohortSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func clusterQueueSnapshotContentEqual(x, y *cache.ClusterQueueSnapshot) bool {
+	if x == nil && y == nil {
+		return true
+	}
+	if x == nil || y == nil {
+		return false
+	}
+	opts := append(baseOpts,
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue"),
+		cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"),
+	)
+	if !cmp.Equal(x, y, opts) {
+		return false
+	}
+	xCohort := x.ClusterQueue.Parent()
+	yCohort := y.ClusterQueue.Parent()
+	return cohortSnapshotContentEqual(xCohort, yCohort)
+}
+
+func cohortSnapshotContentEqual(x, y *cache.CohortSnapshot) bool {
```

  ```suggestion
  func (c *SnapshotComparer) cohortSnapshotContentEqual(x, y *cache.CohortSnapshot) bool {
  ```

- **pkg/scheduler/preemption/preemption_test.go:2865** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:03:03Z
```diff
@@ -2796,3 +2773,185 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+var baseOpts cmp.Options
+
+func init() {
+	baseOpts = cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+			hierarchy.ClusterQueue[*cache.CohortSnapshot]{},
+			hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+		),
+		// comparer for sets based on content, not pointers else causing false negatives
+		cmp.Comparer(func(x, y sets.Set[*cache.ClusterQueueSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			// sort by name for  consistent comparison,  else its unordered causing false negatives
+			sortByName(xList)
+			sortByName(yList)
+			for i := range xList {
+				if !clusterQueueSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+		cmp.Comparer(func(x, y sets.Set[*cache.CohortSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			sortCohortByName(xList)
+			sortCohortByName(yList)
+			for i := range xList {
+				if !cohortSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+	}
+}
+
+func sortByName(slice []*cache.ClusterQueueSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func sortCohortByName(slice []*cache.CohortSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func clusterQueueSnapshotContentEqual(x, y *cache.ClusterQueueSnapshot) bool {
+	if x == nil && y == nil {
+		return true
+	}
+	if x == nil || y == nil {
+		return false
+	}
+	opts := append(baseOpts,
+		cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue"),
+		cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"),
+	)
+	if !cmp.Equal(x, y, opts) {
+		return false
+	}
+	xCohort := x.ClusterQueue.Parent()
+	yCohort := y.ClusterQueue.Parent()
+	return cohortSnapshotContentEqual(xCohort, yCohort)
+}
+
+func cohortSnapshotContentEqual(x, y *cache.CohortSnapshot) bool {
+	if x == nil && y == nil {
+		return true
+	}
+	if x == nil || y == nil {
+		return false
+	}
+	opts := append(baseOpts,
```

  ```suggestion
  	opts := append(c.opts,
  ```

- **pkg/scheduler/preemption/preemption_test.go:2846** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:03:28Z
```diff
@@ -2796,3 +2773,185 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+var baseOpts cmp.Options
+
+func init() {
+	baseOpts = cmp.Options{
+		cmp.AllowUnexported(
+			hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+			cache.ClusterQueueSnapshot{},
+			cache.CohortSnapshot{},
+			hierarchy.ClusterQueue[*cache.CohortSnapshot]{},
+			hierarchy.Cohort[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{},
+		),
+		// comparer for sets based on content, not pointers else causing false negatives
+		cmp.Comparer(func(x, y sets.Set[*cache.ClusterQueueSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			// sort by name for  consistent comparison,  else its unordered causing false negatives
+			sortByName(xList)
+			sortByName(yList)
+			for i := range xList {
+				if !clusterQueueSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+		cmp.Comparer(func(x, y sets.Set[*cache.CohortSnapshot]) bool {
+			if x.Len() != y.Len() {
+				return false
+			}
+			xList := x.UnsortedList()
+			yList := y.UnsortedList()
+			sortCohortByName(xList)
+			sortCohortByName(yList)
+			for i := range xList {
+				if !cohortSnapshotContentEqual(xList[i], yList[i]) {
+					return false
+				}
+			}
+			return true
+		}),
+	}
+}
+
+func sortByName(slice []*cache.ClusterQueueSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func sortCohortByName(slice []*cache.CohortSnapshot) {
+	sort.Slice(slice, func(i, j int) bool {
+		return slice[i].Name < slice[j].Name
+	})
+}
+
+func clusterQueueSnapshotContentEqual(x, y *cache.ClusterQueueSnapshot) bool {
+	if x == nil && y == nil {
+		return true
+	}
+	if x == nil || y == nil {
+		return false
+	}
+	opts := append(baseOpts,
```

  ```suggestion
  	opts := append(c.opts,
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T11:03:51Z

- **pkg/scheduler/preemption/preemption_test.go:2783** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:03:48Z
```diff
@@ -2796,3 +2773,185 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+var baseOpts cmp.Options
+
+func init() {
```

  Don't use init().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T11:49:59Z

- **pkg/scheduler/preemption/preemption_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T11:49:59Z
  I would propose to use something like this:
  
  ```go
  type SnapshotComparer struct {
  	opts cmp.Options
  }
  
  func NewSnapshotComparer(opts ...cmp.Option) *SnapshotComparer {
  	return &SnapshotComparer{opts: opts}
  }
  
  func (c *SnapshotComparer) Diff(x, y *cache.Snapshot) string {
  	return cmp.Diff(x, y, append(c.opts, cmp.Options{
  		cmp.AllowUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
  		cmp.Comparer(c.hierarchyManagerComparer),
  	}))
  }
  
  func (c *SnapshotComparer) hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
  	if !c.clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
  		return false
  	}
  	if !c.cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
  		return false
  	}
  	return true
  }
  
  func (c *SnapshotComparer) clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
  	if len(x) != len(y) {
  		return false
  	}
  	for name, xSnapshot := range x {
  		ySnapshot, ok := y[name]
  		if !ok {
  			return false
  		}
  		if !c.clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
  			return false
  		}
  	}
  	return true
  }
  
  func (c *SnapshotComparer) clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
  	if !cmp.Equal(x, y, append(c.opts, cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"))) {
  		return false
  	}
  	if x == nil {
  		return true
  	}
  	if !c.cohortSnapshotComparer(x.ClusterQueue.Parent(), y.ClusterQueue.Parent()) {
  		return false
  	}
  	return true
  }
  
  func (c *SnapshotComparer) cohortSnapshotsComparer(x, y map[kueue.CohortReference]*cache.CohortSnapshot) bool {
  	if len(x) != len(y) {
  		return false
  	}
  	for name, xSnapshot := range x {
  		ySnapshot, ok := y[name]
  		if !ok {
  			return false
  		}
  		if !c.cohortSnapshotComparer(xSnapshot, ySnapshot) {
  			return false
  		}
  	}
  	return true
  }
  
  func (c *SnapshotComparer) cohortSnapshotComparer(x, y *cache.CohortSnapshot) bool {
  	c.compareCohortSnapshotContent(x, y)
  	if x == nil {
  		return true
  	}
  
  	if !c.compareCohortSnapshotContent(x.Parent(), y.Parent()) {
  		return false
  	}
  
  	xChildCohorts := x.Cohort.ChildCohorts()
  	yChildCohorts := y.Cohort.ChildCohorts()
  	if len(xChildCohorts) != len(yChildCohorts) {
  		return false
  	}
  	sortCohortSnapshotsByName(xChildCohorts)
  	sortCohortSnapshotsByName(yChildCohorts)
  	for index := range xChildCohorts {
  		if !c.compareCohortSnapshotContent(xChildCohorts[index], yChildCohorts[index]) {
  			return false
  		}
  	}
  
  	xChildCQs := x.Cohort.ChildCQs()
  	yChildCQs := y.Cohort.ChildCQs()
  	if len(xChildCQs) != len(yChildCQs) {
  		return false
  	}
  	sortCQSnapshotsByName(xChildCQs)
  	sortCQSnapshotsByName(yChildCQs)
  	for index := range xChildCQs {
  		if !c.compareCQSnapshotContent(xChildCQs[index], yChildCQs[index]) {
  			return false
  		}
  	}
  
  	return true
  }
  
  func (c *SnapshotComparer) compareCohortSnapshotContent(x, y *cache.CohortSnapshot) bool {
  	if !cmp.Equal(x, y, append(c.opts, cmpopts.IgnoreFields(cache.CohortSnapshot{}, "Cohort"))) {
  		return false
  	}
  	return true
  }
  
  func (c *SnapshotComparer) compareCQSnapshotContent(x, y *cache.ClusterQueueSnapshot) bool {
  	if !cmp.Equal(x, y, append(c.opts, cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"))) {
  		return false
  	}
  	return true
  }
  
  func sortCQSnapshotsByName(slice []*cache.ClusterQueueSnapshot) {
  	sort.Slice(slice, func(i, j int) bool {
  		return slice[i].Name < slice[j].Name
  	})
  }
  
  func sortCohortSnapshotsByName(slice []*cache.CohortSnapshot) {
  	sort.Slice(slice, func(i, j int) bool {
  		return slice[i].Name < slice[j].Name
  	})
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T12:19:37Z

- **pkg/scheduler/preemption/preemption_test.go:2848** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T12:12:58Z
```diff
@@ -2796,3 +2773,132 @@ func TestPreemptionMessage(t *testing.T) {
 		}
 	}
 }
+
+type SnapshotComparer struct {
+	opts cmp.Options
+}
+
+func NewSnapshotComparer(opts ...cmp.Option) *SnapshotComparer {
+	return &SnapshotComparer{opts: opts}
+}
+
+func (c *SnapshotComparer) Diff(x, y *cache.Snapshot) string {
+	return cmp.Diff(x, y, append(c.opts, cmp.Options{
+		cmp.AllowUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
+		cmp.Comparer(c.hierarchyManagerComparer),
+	}))
+}
+
+func (c *SnapshotComparer) hierarchyManagerComparer(x, y hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]) bool {
+	if !c.clusterQueuesSnapshotsComparer(x.ClusterQueues(), y.ClusterQueues()) {
+		return false
+	}
+	if !c.cohortSnapshotsComparer(x.Cohorts(), y.Cohorts()) {
+		return false
+	}
+	return true
+}
+
+func (c *SnapshotComparer) clusterQueuesSnapshotsComparer(x, y map[kueue.ClusterQueueReference]*cache.ClusterQueueSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !c.clusterQueueSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *SnapshotComparer) clusterQueueSnapshotComparer(x, y *cache.ClusterQueueSnapshot) bool {
+	if !cmp.Equal(x, y, append(c.opts, cmpopts.IgnoreFields(cache.ClusterQueueSnapshot{}, "ClusterQueue", "tasOnly"))) {
+		return false
+	}
+	if x == nil {
+		return true
+	}
+	if !c.cohortSnapshotComparer(x.ClusterQueue.Parent(), y.ClusterQueue.Parent()) {
+		return false
+	}
+	return true
+}
+
+func (c *SnapshotComparer) cohortSnapshotsComparer(x, y map[kueue.CohortReference]*cache.CohortSnapshot) bool {
+	if len(x) != len(y) {
+		return false
+	}
+	for name, xSnapshot := range x {
+		ySnapshot, ok := y[name]
+		if !ok {
+			return false
+		}
+		if !c.cohortSnapshotComparer(xSnapshot, ySnapshot) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *SnapshotComparer) cohortSnapshotComparer(x, y *cache.CohortSnapshot) bool {
+	c.compareCohortSnapshotContent(x, y)
```

  ```suggestion
  	if !c.compareCohortSnapshotContent(x, y) {
  		return false
  	}
  ```
  Oops. Missed it.

- **pkg/scheduler/preemption/preemption_test.go:1860** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T12:13:45Z
```diff
@@ -1879,10 +1857,9 @@ func TestPreemption(t *testing.T) {
 				t.Errorf("Reported %d preemptions, want %d", preempted, tc.wantPreempted.Len())
 			}
 
-			beforeResourceNodes := resourceNodes(beforeSnapshot)
-			afterResourceNodes := resourceNodes(snapshotWorkingCopy)
-			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
-				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
+			snapshotComparer := NewSnapshotComparer(cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"))
```

  ```suggestion
  			snapshotComparer := NewSnapshotComparer()
  ```
  Is it working here without `IgnoreFields `?

- **pkg/scheduler/preemption/preemption_test.go:1862** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T12:15:04Z
```diff
@@ -1879,10 +1857,9 @@ func TestPreemption(t *testing.T) {
 				t.Errorf("Reported %d preemptions, want %d", preempted, tc.wantPreempted.Len())
 			}
 
-			beforeResourceNodes := resourceNodes(beforeSnapshot)
-			afterResourceNodes := resourceNodes(snapshotWorkingCopy)
-			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
-				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
+			snapshotComparer := NewSnapshotComparer(cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"))
+			if diff := snapshotComparer.Diff(beforeSnapshot, snapshotWorkingCopy); diff != "" {
+				t.Errorf("Snapshot was modified by preemption (-initial,+end):\n%s", diff)
```

  ```suggestion
  				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
  ```
  
  I think we can revert here.

- **pkg/scheduler/preemption/preemption_test.go:2680** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T12:15:18Z
```diff
@@ -2697,10 +2675,9 @@ func TestFairPreemptions(t *testing.T) {
 				t.Errorf("Issued preemptions (-want,+got):\n%s", diff)
 			}
 
-			beforeResourceNodes := resourceNodes(beforeSnapshot)
-			afterResourceNodes := resourceNodes(snapshotWorkingCopy)
-			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
-				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
+			snapshotComparer := NewSnapshotComparer(cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"))
+			if diff := snapshotComparer.Diff(beforeSnapshot, snapshotWorkingCopy); diff != "" {
+				t.Errorf("Snapshot was modified by preemption (-initial,+end):\n%s", diff)
```

  ```suggestion
  				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
  ```
  
  I think we can revert here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-05T12:20:23Z

- **pkg/scheduler/preemption/preemption_test.go:2661** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-05T12:20:23Z
```diff
@@ -2681,6 +2658,7 @@ func TestFairPreemptions(t *testing.T) {
 			if err != nil {
 				t.Fatalf("unexpected error while building snapshot: %v", err)
 			}
+
```

  Also could you please revert this.

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-04-05T12:33:21Z

- **pkg/scheduler/preemption/preemption_test.go:1860** by [@Horiodino](https://github.com/Horiodino) — 2025-04-05T12:33:21Z
```diff
@@ -1879,10 +1857,9 @@ func TestPreemption(t *testing.T) {
 				t.Errorf("Reported %d preemptions, want %d", preempted, tc.wantPreempted.Len())
 			}
 
-			beforeResourceNodes := resourceNodes(beforeSnapshot)
-			afterResourceNodes := resourceNodes(snapshotWorkingCopy)
-			if diff := cmp.Diff(beforeResourceNodes, afterResourceNodes, snapCmpOpts); diff != "" {
-				t.Errorf("Snapshot was modified (-initial,+end):\n%s", diff)
+			snapshotComparer := NewSnapshotComparer(cmpopts.IgnoreFields(cache.ResourceNode{}, "Usage"))
```

  yep

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2025-04-05T12:36:56Z

/lgtm

Awesome! Thank you so much!

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-07T09:38:59Z

- **pkg/scheduler/preemption/preemption_test.go:59** by [@gabesaba](https://github.com/gabesaba) — 2025-04-07T09:38:59Z
```diff
@@ -54,6 +55,7 @@ var snapCmpOpts = cmp.Options{
 	// ignore zero values during comparison, as we consider
 	// zero FlavorResource usage to be same as no map entry.
 	cmpopts.IgnoreMapEntries(func(_ resources.FlavorResource, v int64) bool { return v == 0 }),
+	cmpopts.IgnoreUnexported(hierarchy.Manager[*cache.ClusterQueueSnapshot, *cache.CohortSnapshot]{}),
```

  Sorry for the slow reply, I was OOO last week.
  
  This is not ignoring the Cohort - we are comparing all the Cohorts in the hierarchy.Manager (as well as all the ClusterQueues). You can verify this by modifying the Cohort in the after snapshot, and see that the test fails.
  
  This is only ignoring the edges (allowing traversal), defined by hierarchy.Manager.
