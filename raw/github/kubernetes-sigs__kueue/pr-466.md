# PR #466: Validate using open API where possible

**Summary**: Validate using open API where possible

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/466

**Last updated**: 2022-12-21T13:37:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@nayihz](https://github.com/nayihz)
- **Branch**: `use_markers_validate` → `main`
- **Created**: 2022-12-08T13:39:31Z
- **Updated**: 2022-12-21T13:37:58Z
- **Merged**: 2022-12-20T19:37:19Z
- **Closed**: 2022-12-20T19:37:19Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: _none_
- **Changed files**: 15   **+188 / -223**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Validate using open API where possible

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes # https://github.com/kubernetes-sigs/kueue/issues/465

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-08T13:39:41Z

Hi @cmssczy. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-08T18:20:23Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-15T16:27:55Z

Implementation looks good. The rest of the suggestions are just trying to simplify the tests.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-20T08:22:45Z

@cmssczy: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-23 | 7472d44af9f4b470c66e0f0e33a63fef81622ebe | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/466/pull-kueue-test-e2e-main-1-23/1605115464738934784) | false | `/test pull-kueue-test-e2e-main-1-23`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=466). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Acmssczy). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T15:29:06Z

Opened #482 for the flaky.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T19:35:22Z

/lgmt
/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T19:35:28Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T19:35:43Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-20T19:35:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/466#issuecomment-1360060880" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/466#" title="Author self-approved">cmssczy</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-12-12T04:33:16Z

- **apis/kueue/webhooks/clusterqueue_webhook.go:108** by [@kerthcet](https://github.com/kerthcet) — 2022-12-12T04:23:25Z
```diff
@@ -105,7 +101,6 @@ func ValidateClusterQueue(cq *kueue.ClusterQueue) field.ErrorList {
 		allErrs = append(allErrs, validateNameReference(cq.Spec.Cohort, path.Child("cohort"))...)
 	}
 	allErrs = append(allErrs, validateResources(cq.Spec.Resources, path.Child("resources"))...)
-	allErrs = append(allErrs, validateQueueingStrategy(string(cq.Spec.QueueingStrategy), path.Child("queueingStrategy"))...)
```

  How? I didn't see any API maker about this.

- **apis/kueue/v1alpha2/clusterqueue_types.go:199** by [@kerthcet](https://github.com/kerthcet) — 2022-12-12T04:30:09Z
```diff
@@ -195,6 +196,7 @@ type Resource struct {
 	//
 	// +listType=map
 	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
```

  Should we also add MinItems=1 @alculquicondor , if we want to add resources, we should provide at least one flavor.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-12T06:07:10Z

- **apis/kueue/webhooks/clusterqueue_webhook.go:108** by [@nayihz](https://github.com/nayihz) — 2022-12-12T06:07:10Z
```diff
@@ -105,7 +101,6 @@ func ValidateClusterQueue(cq *kueue.ClusterQueue) field.ErrorList {
 		allErrs = append(allErrs, validateNameReference(cq.Spec.Cohort, path.Child("cohort"))...)
 	}
 	allErrs = append(allErrs, validateResources(cq.Spec.Resources, path.Child("resources"))...)
-	allErrs = append(allErrs, validateQueueingStrategy(string(cq.Spec.QueueingStrategy), path.Child("queueingStrategy"))...)
```

  `// +kubebuilder:validation:Enum=StrictFIFO;BestEffortFIFO` could do this.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-12-12T07:53:43Z

- **apis/kueue/webhooks/clusterqueue_webhook.go:108** by [@kerthcet](https://github.com/kerthcet) — 2022-12-12T07:53:42Z
```diff
@@ -105,7 +101,6 @@ func ValidateClusterQueue(cq *kueue.ClusterQueue) field.ErrorList {
 		allErrs = append(allErrs, validateNameReference(cq.Spec.Cohort, path.Child("cohort"))...)
 	}
 	allErrs = append(allErrs, validateResources(cq.Spec.Resources, path.Child("resources"))...)
-	allErrs = append(allErrs, validateQueueingStrategy(string(cq.Spec.QueueingStrategy), path.Child("queueingStrategy"))...)
```

  Yes, I just mean we didn't add this marker.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-12T08:13:59Z

- **apis/kueue/webhooks/clusterqueue_webhook.go:108** by [@nayihz](https://github.com/nayihz) — 2022-12-12T08:13:59Z
```diff
@@ -105,7 +101,6 @@ func ValidateClusterQueue(cq *kueue.ClusterQueue) field.ErrorList {
 		allErrs = append(allErrs, validateNameReference(cq.Spec.Cohort, path.Child("cohort"))...)
 	}
 	allErrs = append(allErrs, validateResources(cq.Spec.Resources, path.Child("resources"))...)
-	allErrs = append(allErrs, validateQueueingStrategy(string(cq.Spec.QueueingStrategy), path.Child("queueingStrategy"))...)
```

  Actually we already have this marker
  https://github.com/kubernetes-sigs/kueue/blob/d1d445f16478ba2740cf4cd810acfd27e3366d20/apis/kueue/v1alpha2/clusterqueue_types.go#L137

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-12-12T08:42:19Z

- **apis/kueue/webhooks/clusterqueue_webhook.go:108** by [@kerthcet](https://github.com/kerthcet) — 2022-12-12T08:42:18Z
```diff
@@ -105,7 +101,6 @@ func ValidateClusterQueue(cq *kueue.ClusterQueue) field.ErrorList {
 		allErrs = append(allErrs, validateNameReference(cq.Spec.Cohort, path.Child("cohort"))...)
 	}
 	allErrs = append(allErrs, validateResources(cq.Spec.Resources, path.Child("resources"))...)
-	allErrs = append(allErrs, validateQueueingStrategy(string(cq.Spec.QueueingStrategy), path.Child("queueingStrategy"))...)
```

  Got that, it's a kind of cleanup. Thanks.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-12-12T10:27:27Z

Thanks @cmssczy

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:172** by [@denkensk](https://github.com/denkensk) — 2022-12-12T10:17:37Z
```diff
@@ -148,6 +165,22 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
+		ginkgo.It("Should have valid flavor quantity when creating", func() {
+			ginkgo.By("Creating a new clusterQueue")
+			cq := func() *kueue.ClusterQueue {
+				res := testing.MakeResource("cpu")
+				for i := 0; i < 17; i++ {
```

  Maybe you put the numbers like `16` `8` in test/integration/framework/constants.go, change 17 to `FlavorMaxItems` + 1?
  It will help to improve the readability of code.

- **apis/kueue/v1alpha2/clusterqueue_types.go:199** by [@denkensk](https://github.com/denkensk) — 2022-12-12T10:26:56Z
```diff
@@ -195,6 +196,7 @@ type Resource struct {
 	//
 	// +listType=map
 	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
```

  Yes, we need set `MinItems=1`. It need at least one flavor.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-12T13:06:11Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:199** by [@nayihz](https://github.com/nayihz) — 2022-12-12T13:06:11Z
```diff
@@ -195,6 +196,7 @@ type Resource struct {
 	//
 	// +listType=map
 	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
```

  If we set `MinItems=1`, we should update `MakeResource` function in `wrapper.go` simultaneously.  Maybe it will lead to a lot of code refactoring in unit tests and integration tests where use this function. Do we have a more elegant way to implement this?😂

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T18:56:28Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:199** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T18:56:28Z
```diff
@@ -195,6 +196,7 @@ type Resource struct {
 	//
 	// +listType=map
 	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
```

  I think leaving `MakeResource` as-is is perfectly acceptable.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-13T01:54:23Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:97** by [@nayihz](https://github.com/nayihz) — 2022-12-13T01:54:22Z
```diff
@@ -64,7 +66,7 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 		ginkgo.It("Should have qualified resource names when creating", func() {
 			ginkgo.By("Creating a new clusterQueue")
 			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("@cpu").Obj(),
+				testing.MakeResource("@cpu").Flavor(testing.MakeFlavor("x86", "5").Obj()).Obj(),
```

  we should do this if we don't update `MakeResource` function.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-14T16:54:55Z

- **config/components/crd/bases/kueue.x-k8s.io_workloads.yaml:7465** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T16:43:03Z
```diff
@@ -7461,6 +7462,8 @@ spec:
                   - name
                   - spec
                   type: object
+                maxItems: 8
```

  What happens when you do `kubectl explain workloads`? Do these limits show in the help? if so, we can get rid of the comments of the form "this field must have at most 8 items"

- **apis/kueue/webhooks/clusterqueue_webhook.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T16:45:05Z
```diff
@@ -35,10 +35,6 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-var (
-	queueingStrategies = sets.NewString(string(kueue.StrictFIFO), string(kueue.BestEffortFIFO))
```

  add an integration test to make sure that using a different value or empty fails.

- **test/integration/framework/constants.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T16:49:20Z
```diff
@@ -24,4 +24,12 @@ const (
 	Timeout            = time.Second * 10
 	ConsistentDuration = time.Second * 3
 	Interval           = time.Millisecond * 250
+
+	ResourcesMaxItems         = 16
+	FlavorsMaxItems           = 16
+	NodeSelectorMaxProperties = 8
+	TaintsMaxItems            = 8
+	PodSetsMaxItems           = 8
+	PodSetsMinItems           = 1
+	PodSetsCountMinItems      = 1
```

  just keep them in their respective tests packages. They serve no value as "global" variables.

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:97** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T16:50:14Z
```diff
@@ -64,7 +66,7 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 		ginkgo.It("Should have qualified resource names when creating", func() {
 			ginkgo.By("Creating a new clusterQueue")
 			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("@cpu").Obj(),
+				testing.MakeResource("@cpu").Flavor(testing.MakeFlavor("x86", "5").Obj()).Obj(),
```

  make sure there is an integration test that fails on empty flavors

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:128** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T16:51:59Z
```diff
@@ -87,14 +89,29 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			var updateCQ kueue.ClusterQueue
 			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updateCQ)).Should(gomega.Succeed())
 			updateCQ.Spec.Resources = []kueue.Resource{
-				*testing.MakeResource("@cpu").Obj(),
+				*testing.MakeResource("@cpu").Flavor(testing.MakeFlavor("x86", "5").Obj()).Obj(),
 			}
 
 			err := k8sClient.Update(ctx, &updateCQ)
 			gomega.Expect(err).Should(gomega.HaveOccurred())
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
+		ginkgo.It("Should have valid resources quantity when creating", func() {
```

  consider using a single `ginkgo.DescribeTable` for all the validation errors to reduce code.
  
  where necessary, you can use an anonymous functions to do the loops.

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:81** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T16:54:35Z
```diff
@@ -76,6 +78,39 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.When("Creating a ResourceFlavor with invalid number of Taint", func() {
```

  ginkgo.DescribeTable

- **test/integration/webhook/v1alpha2/workload_test.go:80** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T16:54:50Z
```diff
@@ -75,31 +77,52 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 
 var _ = ginkgo.Describe("Workload validating webhook", func() {
 	ginkgo.Context("When creating a Workload", func() {
-		ginkgo.It("Should validate Workload", func() {
+		ginkgo.It("Should have valid PodSet.Count when creating", func() {
```

  ginkgo.DescribeTable

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-15T13:27:36Z

- **config/components/crd/bases/kueue.x-k8s.io_workloads.yaml:7465** by [@nayihz](https://github.com/nayihz) — 2022-12-15T13:27:36Z
```diff
@@ -7461,6 +7462,8 @@ spec:
                   - name
                   - spec
                   type: object
+                maxItems: 8
```

  ```bash
  # kubectl explain workloads.spec.podSets
  KIND:     Workload
  VERSION:  kueue.x-k8s.io/v1alpha2
  
  RESOURCE: podSets <[]Object>
  
  DESCRIPTION:
       podSets is a list of sets of homogeneous pods, each described by a Pod spec
       and a count. There must be at least one element and at most 8. podSets
       cannot be changed.
  
  FIELDS:
     count	<integer> -required-
       count is the number of pods for the spec.
  
     name	<string> -required-
       name is the PodSet name.
  
     spec	<Object> -required-
       spec is the Pod spec. If requests are omitted for a container or
       initContainer, they default to the limits if they are explicitly specified
       for the container or initcontainer.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-15T15:20:07Z

- **config/components/crd/bases/kueue.x-k8s.io_workloads.yaml:7465** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-15T15:20:07Z
```diff
@@ -7461,6 +7462,8 @@ spec:
                   - name
                   - spec
                   type: object
+                maxItems: 8
```

  I take that the answer is: they don't show up, so we should keep the description as-is.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-15T16:27:30Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-15T16:16:42Z
```diff
@@ -61,10 +68,33 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(created.GetFinalizers()).Should(gomega.Equal([]string{kueue.ResourceInUseFinalizerName}))
 		})
 
+		ginkgo.It("Should have qualified clusterQueueingStrategy when creating", func() {
```

  Can you make it a `DescribeTable` too? along with other creation validations.
  
  
  And perhaps a separate `DescribeTable` for all update validtions, if more than one.

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-15T16:17:46Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
```

  this anonymous function is unnecessary.
  
  Anonymous functions might be necessary when the table accepts a spec, instead of parameters.
  https://github.com/kubernetes/kubernetes/blob/c1c0e4fe0bb4e7c0145d45a010577ed64619903a/pkg/apis/batch/validation/validation_test.go#L285-L305

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-15T16:25:10Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
+			rf := testing.MakeResourceFlavor("resource-flavor")
+			for i := 0; i < taintsCount; i++ {
+				rf.Taint(corev1.Taint{
+					Key:    fmt.Sprintf("t%d", i),
+					Effect: corev1.TaintEffectNoExecute,
+				})
+			}
+
+			m := make(map[string]string)
+			for i := 0; i < nodeSelectorCount; i++ {
+				m[fmt.Sprintf("l%d", i)] = ""
+			}
+
+			return rf.MultiLabels(m).Obj()
+		}()
+		err := k8sClient.Create(ctx, resourceFlavor)
+		if isInvalid {
+			gomega.Expect(err).To(gomega.HaveOccurred())
+			gomega.Expect(errors.IsInvalid(err)).To(gomega.BeTrue(), "error: %v", err)
+		} else {
+			gomega.Expect(err).To(gomega.Succeed())
+			defer func() {
+				var rf kueue.ResourceFlavor
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(resourceFlavor), &rf)).To(gomega.Succeed())
+				controllerutil.RemoveFinalizer(&rf, kueue.ResourceInUseFinalizerName)
+				gomega.Expect(k8sClient.Update(ctx, &rf)).Should(gomega.Succeed())
+				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
```

  I think this should be enough. The controller should remove the finalizer, given that the RF is not in use.
  
  ```suggestion
  				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
  ```

- **test/integration/webhook/v1alpha2/workload_test.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-15T16:26:52Z
```diff
@@ -75,31 +80,43 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 
 var _ = ginkgo.Describe("Workload validating webhook", func() {
 	ginkgo.Context("When creating a Workload", func() {
-		ginkgo.It("Should validate Workload", func() {
+
+		ginkgo.It("Should have valid PriorityClassName when creating", func() {
 			ginkgo.By("Creating a new Workload")
 			workload := testing.MakeWorkload(workloadName, ns.Name).
-				PodSets([]kueue.PodSet{
-					{
-						Name:  "main",
-						Count: -1,
-						Spec: corev1.PodSpec{
-							Containers: []corev1.Container{
-								{
-									Name: "c",
-									Resources: corev1.ResourceRequirements{
-										Requests: make(corev1.ResourceList),
-									},
-								},
-							},
-						},
-					},
-				}).
 				PriorityClass("invalid_class").
 				Obj()
 			err := k8sClient.Create(ctx, workload)
 			gomega.Expect(err).Should(gomega.HaveOccurred())
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
+
+		ginkgo.DescribeTable("Should have valid PodSet when creating", func(podSetsCapacity int, podSetCount int, isInvalid bool) {
+			podSet := func() []kueue.PodSet {
```

  don't make this an anonymous function

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-16T01:21:35Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:71** by [@nayihz](https://github.com/nayihz) — 2022-12-16T01:21:35Z
```diff
@@ -61,10 +68,33 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(created.GetFinalizers()).Should(gomega.Equal([]string{kueue.ResourceInUseFinalizerName}))
 		})
 
+		ginkgo.It("Should have qualified clusterQueueingStrategy when creating", func() {
```

  Wouldn't it be better to fix this in a new PR?

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-16T02:12:23Z

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:112** by [@nayihz](https://github.com/nayihz) — 2022-12-16T02:12:23Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
+			rf := testing.MakeResourceFlavor("resource-flavor")
+			for i := 0; i < taintsCount; i++ {
+				rf.Taint(corev1.Taint{
+					Key:    fmt.Sprintf("t%d", i),
+					Effect: corev1.TaintEffectNoExecute,
+				})
+			}
+
+			m := make(map[string]string)
+			for i := 0; i < nodeSelectorCount; i++ {
+				m[fmt.Sprintf("l%d", i)] = ""
+			}
+
+			return rf.MultiLabels(m).Obj()
+		}()
+		err := k8sClient.Create(ctx, resourceFlavor)
+		if isInvalid {
+			gomega.Expect(err).To(gomega.HaveOccurred())
+			gomega.Expect(errors.IsInvalid(err)).To(gomega.BeTrue(), "error: %v", err)
+		} else {
+			gomega.Expect(err).To(gomega.Succeed())
+			defer func() {
+				var rf kueue.ResourceFlavor
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(resourceFlavor), &rf)).To(gomega.Succeed())
+				controllerutil.RemoveFinalizer(&rf, kueue.ResourceInUseFinalizerName)
+				gomega.Expect(k8sClient.Update(ctx, &rf)).Should(gomega.Succeed())
+				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
```

  The resourceFlavor created before cannot be deleted if we delete this code.
  ```bash
  • [FAILED] [10.025 seconds]
  ResourceFlavor Webhook invalid number of properties [It] valid number of nodeSelector and taint
  /go/src/kueue/test/integration/webhook/v1alpha2/resourceflavor_test.go:118
  
    [FAILED] Timed out after 10.001s.
    Error: NotFoundError expects an error
        <*errors.errorString | 0x14000833c40>: {
            s: "NotFoundError expects an error",
        }
  ```
  
  The reason of timeout may be the exist of finailizer.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-17T04:40:42Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:71** by [@nayihz](https://github.com/nayihz) — 2022-12-17T04:40:41Z
```diff
@@ -61,10 +68,33 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(created.GetFinalizers()).Should(gomega.Equal([]string{kueue.ResourceInUseFinalizerName}))
 		})
 
+		ginkgo.It("Should have qualified clusterQueueingStrategy when creating", func() {
```

  Have done in this pr.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T14:32:04Z

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:32:03Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
+			rf := testing.MakeResourceFlavor("resource-flavor")
+			for i := 0; i < taintsCount; i++ {
+				rf.Taint(corev1.Taint{
+					Key:    fmt.Sprintf("t%d", i),
+					Effect: corev1.TaintEffectNoExecute,
+				})
+			}
+
+			m := make(map[string]string)
+			for i := 0; i < nodeSelectorCount; i++ {
+				m[fmt.Sprintf("l%d", i)] = ""
+			}
+
+			return rf.MultiLabels(m).Obj()
+		}()
+		err := k8sClient.Create(ctx, resourceFlavor)
+		if isInvalid {
+			gomega.Expect(err).To(gomega.HaveOccurred())
+			gomega.Expect(errors.IsInvalid(err)).To(gomega.BeTrue(), "error: %v", err)
+		} else {
+			gomega.Expect(err).To(gomega.Succeed())
+			defer func() {
+				var rf kueue.ResourceFlavor
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(resourceFlavor), &rf)).To(gomega.Succeed())
+				controllerutil.RemoveFinalizer(&rf, kueue.ResourceInUseFinalizerName)
+				gomega.Expect(k8sClient.Update(ctx, &rf)).Should(gomega.Succeed())
+				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
```

  That's because the controller is not running in this test.
  Enable it please: `core.SetupControllers`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T14:52:00Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:42:25Z
```diff
@@ -128,42 +142,70 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
-		ginkgo.It("Should have non-negative quota value when creating", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("cpu").Flavor(testing.MakeFlavor("x86", "-1").Obj()).Obj(),
-			).Obj()
+		ginkgo.DescribeTable("ClusterQueue", func(cq *kueue.ClusterQueue, isForbidden, isInvalid bool) {
```

  ```suggestion
  		ginkgo.DescribeTable("Validate ClusterQueue on creation", func(cq *kueue.ClusterQueue, isForbidden, isInvalid bool) {
  ```

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:139** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:43:01Z
```diff
@@ -128,42 +142,70 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
-		ginkgo.It("Should have non-negative quota value when creating", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("cpu").Flavor(testing.MakeFlavor("x86", "-1").Obj()).Obj(),
-			).Obj()
+		ginkgo.DescribeTable("ClusterQueue", func(cq *kueue.ClusterQueue, isForbidden, isInvalid bool) {
 			err := k8sClient.Create(ctx, cq)
-			gomega.Expect(err).Should(gomega.HaveOccurred())
-			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
-		})
-
-		ginkgo.It("Should have quota whose max value is greater than min", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
+			switch {
+			case isForbidden:
+				gomega.Expect(err).Should(gomega.HaveOccurred())
+				gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
+			case isInvalid:
+				gomega.Expect(err).Should(gomega.HaveOccurred())
+				gomega.Expect(errors.IsInvalid(err)).Should(gomega.BeTrue(), "error: %v", err)
+			default:
+				gomega.Expect(err).Should(gomega.Succeed())
+				defer func() {
+					var deleteCQ kueue.ClusterQueue
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &deleteCQ)).Should(gomega.Succeed())
+					controllerutil.RemoveFinalizer(&deleteCQ, kueue.ResourceInUseFinalizerName)
+					gomega.Expect(k8sClient.Update(ctx, &deleteCQ)).Should(gomega.Succeed())
```

  run the controller instead.

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:43:56Z
```diff
@@ -61,15 +68,32 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(created.GetFinalizers()).Should(gomega.Equal([]string{kueue.ResourceInUseFinalizerName}))
 		})
 
-		ginkgo.It("Should have qualified resource names when creating", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("@cpu").Obj(),
-			).Obj()
+		ginkgo.DescribeTable("Should have qualified clusterQueueingStrategy when creating", func(qs string, isInvalid bool) {
+			cq := testing.MakeClusterQueue("cluster-queue").QueueingStrategy(kueue.QueueingStrategy(qs)).Obj()
 			err := k8sClient.Create(ctx, cq)
-			gomega.Expect(err).Should(gomega.HaveOccurred())
-			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
-		})
+			if isInvalid {
+				gomega.Expect(err).Should(gomega.HaveOccurred())
+				gomega.Expect(errors.IsInvalid(err)).Should(gomega.BeTrue(), "error: %v", err)
+			} else {
+				gomega.Expect(err).Should(gomega.Succeed())
+				var created kueue.ClusterQueue
+				defer func() {
+					controllerutil.RemoveFinalizer(&created, kueue.ResourceInUseFinalizerName)
+					gomega.Expect(k8sClient.Update(ctx, &created)).Should(gomega.Succeed())
+					framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, &created, true)
+				}()
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &created)).Should(gomega.Succeed())
+				if len(qs) == 0 {
+					gomega.Expect(created.Spec.QueueingStrategy).To(gomega.Equal(kueue.BestEffortFIFO))
+				} else {
+					gomega.Expect(created.Spec.QueueingStrategy).To(gomega.Equal(kueue.QueueingStrategy(qs)))
+				}
+			}
+		},
+			ginkgo.Entry("unknown clusterQueueingStrategy", "unknown", true),
+			ginkgo.Entry("empty clusterQueueingStrategy", "", false),
+			ginkgo.Entry("BestEffortFIFO clusterQueueingStrategy", "BestEffortFIFO", false),
```

  They can all be part of the DescribeTable below (line 145)

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:165** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:45:50Z
```diff
@@ -128,42 +142,70 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
-		ginkgo.It("Should have non-negative quota value when creating", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("cpu").Flavor(testing.MakeFlavor("x86", "-1").Obj()).Obj(),
-			).Obj()
+		ginkgo.DescribeTable("ClusterQueue", func(cq *kueue.ClusterQueue, isForbidden, isInvalid bool) {
 			err := k8sClient.Create(ctx, cq)
-			gomega.Expect(err).Should(gomega.HaveOccurred())
-			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
-		})
-
-		ginkgo.It("Should have quota whose max value is greater than min", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
+			switch {
+			case isForbidden:
+				gomega.Expect(err).Should(gomega.HaveOccurred())
+				gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
+			case isInvalid:
+				gomega.Expect(err).Should(gomega.HaveOccurred())
+				gomega.Expect(errors.IsInvalid(err)).Should(gomega.BeTrue(), "error: %v", err)
+			default:
+				gomega.Expect(err).Should(gomega.Succeed())
+				defer func() {
+					var deleteCQ kueue.ClusterQueue
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &deleteCQ)).Should(gomega.Succeed())
+					controllerutil.RemoveFinalizer(&deleteCQ, kueue.ResourceInUseFinalizerName)
+					gomega.Expect(k8sClient.Update(ctx, &deleteCQ)).Should(gomega.Succeed())
+					framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, &deleteCQ, true)
+				}()
+			}
+		},
+			ginkgo.Entry("Should have quota whose max value is greater than min", testing.MakeClusterQueue("cluster-queue").Resource(
```

  Same for other items
  
  ```suggestion
  			ginkgo.Entry("Should have quota whose max value is greater than min",
  				testing.MakeClusterQueue("cluster-queue").Resource(
  ```

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:47:45Z
```diff
@@ -128,42 +142,70 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
-		ginkgo.It("Should have non-negative quota value when creating", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("cpu").Flavor(testing.MakeFlavor("x86", "-1").Obj()).Obj(),
-			).Obj()
+		ginkgo.DescribeTable("ClusterQueue", func(cq *kueue.ClusterQueue, isForbidden, isInvalid bool) {
```

  Maybe also something like this?
  
  ```suggestion
  		ginkgo.DescribeTable("ClusterQueue", func(cq *kueue.ClusterQueue, errorType string) {
  ```
  
  and then:
  ```golang
  switch errorType {
  	case isForbidden:
  ```

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T14:48:52Z
```diff
@@ -128,42 +142,70 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
-		ginkgo.It("Should have non-negative quota value when creating", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("cpu").Flavor(testing.MakeFlavor("x86", "-1").Obj()).Obj(),
-			).Obj()
+		ginkgo.DescribeTable("ClusterQueue", func(cq *kueue.ClusterQueue, isForbidden, isInvalid bool) {
```

  The logic and constants can even be part of `test/integration/framework`

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-20T07:12:24Z

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:112** by [@nayihz](https://github.com/nayihz) — 2022-12-20T07:12:24Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
+			rf := testing.MakeResourceFlavor("resource-flavor")
+			for i := 0; i < taintsCount; i++ {
+				rf.Taint(corev1.Taint{
+					Key:    fmt.Sprintf("t%d", i),
+					Effect: corev1.TaintEffectNoExecute,
+				})
+			}
+
+			m := make(map[string]string)
+			for i := 0; i < nodeSelectorCount; i++ {
+				m[fmt.Sprintf("l%d", i)] = ""
+			}
+
+			return rf.MultiLabels(m).Obj()
+		}()
+		err := k8sClient.Create(ctx, resourceFlavor)
+		if isInvalid {
+			gomega.Expect(err).To(gomega.HaveOccurred())
+			gomega.Expect(errors.IsInvalid(err)).To(gomega.BeTrue(), "error: %v", err)
+		} else {
+			gomega.Expect(err).To(gomega.Succeed())
+			defer func() {
+				var rf kueue.ResourceFlavor
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(resourceFlavor), &rf)).To(gomega.Succeed())
+				controllerutil.RemoveFinalizer(&rf, kueue.ResourceInUseFinalizerName)
+				gomega.Expect(k8sClient.Update(ctx, &rf)).Should(gomega.Succeed())
+				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
```

  If we enable `core.SetupControllers` in `test/integration/webhook/v1alpha2/suite_test.go`, we got many update conflict event in `clusterqueue_test.go` and `workload_test.go`, like
  ```
  [FAILED] error: Operation cannot be fulfilled on clusterqueues.kueue.x-k8s.io "cluster-queue": the object has been modified; please apply your changes to the latest version and try again
  Expected
        <bool>: false
    to be true
    In [It] at: /go/src/kueue/test/integration/webhook/v1alpha2/clusterqueue_test.go:120
  ```
  
  ```
  [FAILED] error: Operation cannot be fulfilled on workloads.kueue.x-k8s.io "workload-test": the object has been modified; please apply your changes to the latest version and try again
    Expected
        <bool>: false
    to be true
    In [It] at: /go/src/kueue/test/integration/webhook/v1alpha2/workload_test.go:169
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T15:32:25Z

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T15:32:25Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
+			rf := testing.MakeResourceFlavor("resource-flavor")
+			for i := 0; i < taintsCount; i++ {
+				rf.Taint(corev1.Taint{
+					Key:    fmt.Sprintf("t%d", i),
+					Effect: corev1.TaintEffectNoExecute,
+				})
+			}
+
+			m := make(map[string]string)
+			for i := 0; i < nodeSelectorCount; i++ {
+				m[fmt.Sprintf("l%d", i)] = ""
+			}
+
+			return rf.MultiLabels(m).Obj()
+		}()
+		err := k8sClient.Create(ctx, resourceFlavor)
+		if isInvalid {
+			gomega.Expect(err).To(gomega.HaveOccurred())
+			gomega.Expect(errors.IsInvalid(err)).To(gomega.BeTrue(), "error: %v", err)
+		} else {
+			gomega.Expect(err).To(gomega.Succeed())
+			defer func() {
+				var rf kueue.ResourceFlavor
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(resourceFlavor), &rf)).To(gomega.Succeed())
+				controllerutil.RemoveFinalizer(&rf, kueue.ResourceInUseFinalizerName)
+				gomega.Expect(k8sClient.Update(ctx, &rf)).Should(gomega.Succeed())
+				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
```

  No idea what those failing lines are trying to do, but, once you enable the controllers:
  
  - you shouldn't be trying to delete the finalizers in the test code.
  - any updates to objects should have retries. We do this for other integration tests.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T19:35:08Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:139** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T19:35:07Z
```diff
@@ -128,42 +142,70 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
 
-		ginkgo.It("Should have non-negative quota value when creating", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
-				testing.MakeResource("cpu").Flavor(testing.MakeFlavor("x86", "-1").Obj()).Obj(),
-			).Obj()
+		ginkgo.DescribeTable("ClusterQueue", func(cq *kueue.ClusterQueue, isForbidden, isInvalid bool) {
 			err := k8sClient.Create(ctx, cq)
-			gomega.Expect(err).Should(gomega.HaveOccurred())
-			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
-		})
-
-		ginkgo.It("Should have quota whose max value is greater than min", func() {
-			ginkgo.By("Creating a new clusterQueue")
-			cq := testing.MakeClusterQueue("cluster-queue").Resource(
+			switch {
+			case isForbidden:
+				gomega.Expect(err).Should(gomega.HaveOccurred())
+				gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
+			case isInvalid:
+				gomega.Expect(err).Should(gomega.HaveOccurred())
+				gomega.Expect(errors.IsInvalid(err)).Should(gomega.BeTrue(), "error: %v", err)
+			default:
+				gomega.Expect(err).Should(gomega.Succeed())
+				defer func() {
+					var deleteCQ kueue.ClusterQueue
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &deleteCQ)).Should(gomega.Succeed())
+					controllerutil.RemoveFinalizer(&deleteCQ, kueue.ResourceInUseFinalizerName)
+					gomega.Expect(k8sClient.Update(ctx, &deleteCQ)).Should(gomega.Succeed())
```

  Please do this in a follow up.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-21T01:53:04Z

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:112** by [@nayihz](https://github.com/nayihz) — 2022-12-21T01:53:04Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
+			rf := testing.MakeResourceFlavor("resource-flavor")
+			for i := 0; i < taintsCount; i++ {
+				rf.Taint(corev1.Taint{
+					Key:    fmt.Sprintf("t%d", i),
+					Effect: corev1.TaintEffectNoExecute,
+				})
+			}
+
+			m := make(map[string]string)
+			for i := 0; i < nodeSelectorCount; i++ {
+				m[fmt.Sprintf("l%d", i)] = ""
+			}
+
+			return rf.MultiLabels(m).Obj()
+		}()
+		err := k8sClient.Create(ctx, resourceFlavor)
+		if isInvalid {
+			gomega.Expect(err).To(gomega.HaveOccurred())
+			gomega.Expect(errors.IsInvalid(err)).To(gomega.BeTrue(), "error: %v", err)
+		} else {
+			gomega.Expect(err).To(gomega.Succeed())
+			defer func() {
+				var rf kueue.ResourceFlavor
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(resourceFlavor), &rf)).To(gomega.Succeed())
+				controllerutil.RemoveFinalizer(&rf, kueue.ResourceInUseFinalizerName)
+				gomega.Expect(k8sClient.Update(ctx, &rf)).Should(gomega.Succeed())
+				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
```

  I had tried to do so but failed as mentioned above. I will try to finish this in a follow up. Thank you for your patience and guidance!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-21T13:37:57Z

- **test/integration/webhook/v1alpha2/resourceflavor_test.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-21T13:37:57Z
```diff
@@ -76,6 +83,43 @@ var _ = ginkgo.Describe("ResourceFlavor Webhook", func() {
 		})
 	})
 
+	ginkgo.DescribeTable("invalid number of properties", func(taintsCount int, nodeSelectorCount int, isInvalid bool) {
+		resourceFlavor := func() *kueue.ResourceFlavor {
+			rf := testing.MakeResourceFlavor("resource-flavor")
+			for i := 0; i < taintsCount; i++ {
+				rf.Taint(corev1.Taint{
+					Key:    fmt.Sprintf("t%d", i),
+					Effect: corev1.TaintEffectNoExecute,
+				})
+			}
+
+			m := make(map[string]string)
+			for i := 0; i < nodeSelectorCount; i++ {
+				m[fmt.Sprintf("l%d", i)] = ""
+			}
+
+			return rf.MultiLabels(m).Obj()
+		}()
+		err := k8sClient.Create(ctx, resourceFlavor)
+		if isInvalid {
+			gomega.Expect(err).To(gomega.HaveOccurred())
+			gomega.Expect(errors.IsInvalid(err)).To(gomega.BeTrue(), "error: %v", err)
+		} else {
+			gomega.Expect(err).To(gomega.Succeed())
+			defer func() {
+				var rf kueue.ResourceFlavor
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(resourceFlavor), &rf)).To(gomega.Succeed())
+				controllerutil.RemoveFinalizer(&rf, kueue.ResourceInUseFinalizerName)
+				gomega.Expect(k8sClient.Update(ctx, &rf)).Should(gomega.Succeed())
+				framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, resourceFlavor, true)
```

  example of retrying https://github.com/kubernetes-sigs/kueue/blob/7b8316fa811f4afc75e22803e33b3061a030afb8/test/integration/controller/core/localqueue_controller_test.go#L89-L95
