# PR #4687: Introduce FS at admission time

**Summary**: Introduce FS at admission time

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4687

**Last updated**: 2025-08-12T10:15:45Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `FS-AdmissionTime-CQ` → `main`
- **Created**: 2025-03-19T12:51:30Z
- **Updated**: 2025-08-12T10:15:45Z
- **Merged**: 2025-05-07T17:47:27Z
- **Closed**: 2025-05-07T17:47:27Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 44   **+1813 / -57**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Changes described in the first phase of https://github.com/kubernetes-sigs/kueue/pull/4252

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4136 

#### Special notes for your reviewer:
I still need to comment some API fields and integration tests

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support a new fair sharing alpha feature `Admission Fair Sharing`, along with the new API. It orders workloads based on the recent usage coming from a LocalQueue the workload was submitted to. The recent usage is more important than the priority of workloads  
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-19T12:51:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 118ac5c9d83014c230f9af8579c443555522447d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/681b97748d89a5000795c375 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-19T13:45:06Z

/assign @gabesaba @pajakd 
cc @mimowo @mwielgus

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-19T13:45:09Z

@PBundyra: GitHub didn't allow me to assign the following users: pajakd.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) with read permissions, repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4687#issuecomment-2736705405):

>/assign @gabesaba @pajakd 
>cc @mimowo @mwielgus 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-19T16:40:10Z

/retest

### Comment by [@pajakd](https://github.com/pajakd) — 2025-03-21T06:22:24Z

Just to be sure about the scope here -- by "Changes described in the first phase of https://github.com/kubernetes-sigs/kueue/pull/4252" you mean CQ + LQ level support, right (so no cohorts yet)?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T09:09:49Z

> Just to be sure about the scope here -- by "Changes described in the first phase of https://github.com/kubernetes-sigs/kueue/pull/4252" you mean CQ + LQ level support, right (so no cohorts yet)? 

Exactly

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T14:45:03Z

Will rebase after https://github.com/kubernetes-sigs/kueue/pull/4933 is merged to reduce the diff

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-04-17T13:11:50Z

Still working on integration/e2e tests and `ResetInactivityPeriod` logic but I believe this PR is ready for another pass @gabesaba @pajakd

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-04-29T14:20:06Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-06T14:39:19Z

/release-note-edit
```release-note
NONE
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-06T14:39:41Z

@PBundyra please add a proper release note

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-06T14:46:37Z

LGTM label has been added.  <details>Git tree hash: 3ecc1c52c9a0d3ef397a5e7b00601cb03d634d17</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:33:23Z

> @PBundyra please add a proper release note

Sure, done

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-06T15:37:25Z

From the release note: 

> Support a new fair sharing alpha feature `Admission Fair Sharing`

Actually in the current version the feature is straight beta, because the API is beta and there is no feature gate.
If we go via alpha I would suggest to add a feature gate. If we have good reasons not to, I would say it is beta directly.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-05-07T09:07:10Z

/lgtm

nice work :)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-07T09:07:17Z

LGTM label has been added.  <details>Git tree hash: 04487552e74cbdf9639f081fce58e3bded618900</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T10:31:34Z

> From the release note:
> 
> > Support a new fair sharing alpha feature `Admission Fair Sharing`
> 
> Actually in the current version the feature is straight beta, because the API is beta and there is no feature gate. If we go via alpha I would suggest to add a feature gate. If we have good reasons not to, I would say it is beta directly.


> From the release note:
> 
> > Support a new fair sharing alpha feature `Admission Fair Sharing`
> 
> Actually in the current version the feature is straight beta, because the API is beta and there is no feature gate. If we go via alpha I would suggest to add a feature gate. If we have good reasons not to, I would say it is beta directly.

Yes that's a good point, added a feature gate

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-07T17:12:54Z

@PBundyra: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-integration-main | 88778ecca3ffa323b870218af4ba0bff7a72f004 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/4687/pull-kueue-test-integration-main/1913285260632657920) | true | `/test pull-kueue-test-integration-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=4687). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3APBundyra). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-07T17:46:34Z

/lgtm
/approve
Thank you, this is an important feature.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-07T17:46:41Z

LGTM label has been added.  <details>Git tree hash: 33fd40967855828f0da439e29de8d1485591560e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-07T17:46:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4687#issuecomment-2859590598" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4687#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-08T06:26:25Z

These are good comments, I guess the most important is to address the API remarks before the release, the other ones could probably be fixed as follow up issue(s). wdyt @tenzen-y ?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-08T06:27:26Z

@PBundyra please address the comments, I think the API updates have priority.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T06:49:26Z

Sure, will do in a follow-up

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-08T09:38:31Z

> These are good comments, I guess the most important is to address the API remarks before the release, the other ones could probably be fixed as follow up issue(s). wdyt @tenzen-y ?

I think API markers and validations for ConfigAPIs are higher priority.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:51:27Z

> @PBundyra please address the comments, I think the API updates have priority.

Addressed here: https://github.com/kubernetes-sigs/kueue/pull/5203

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-19T16:43:52Z

- **apis/config/v1beta1/configuration_types.go:494** by [@mwielgus](https://github.com/mwielgus) — 2025-03-19T16:43:51Z
```diff
@@ -471,4 +478,12 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
+
+	UsageHalfDecayTime metav1.Duration `json:"usageHalfDecayTime,omitempty"`
```

  Can we group these admission-fs specific stuff into a dedicated struct?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-03-19T16:45:39Z

- **apis/kueue/v1beta1/fairsharing_types.go:56** by [@mwielgus](https://github.com/mwielgus) — 2025-03-19T16:45:39Z
```diff
@@ -49,4 +53,12 @@ type FairSharingStatus struct {
 	// weight of zero, this will return 9223372036854775807, the
 	// maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
```

  Same as with config, what about putting this in context of admission-usage-based-fs?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-20T10:02:42Z

- **pkg/controller/core/localqueue_controller.go:270** by [@pajakd](https://github.com/pajakd) — 2025-03-20T10:02:41Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
```

  I'm missing something here. Why aren't we looking at the time since the last update? So, alpha is always the same value, regardless of how much time has passed?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-20T11:51:19Z

- **pkg/util/resource/resource.go:111** by [@pajakd](https://github.com/pajakd) — 2025-03-20T11:51:19Z
```diff
@@ -103,3 +107,32 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+func MulResources(res map[corev1.ResourceName]resource.Quantity, multiplier float64) map[corev1.ResourceName]resource.Quantity {
```

  Can this be expressed using "MapValues"?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-20T12:02:43Z

- **pkg/controller/core/localqueue_controller.go:270** by [@pajakd](https://github.com/pajakd) — 2025-03-20T12:02:43Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
```

  Looking at the call site of this func -- we are guaranteeing that between two calls of this func at least UsageSamplingFrequency.Duration has passed, right? But in this function we are assuming that exactly this much has passed? Not sure how frequently we check so perhaps irrelevant.
  
  But, I think there probably should be 0.5 ^{samplingFreqSeconds/halfDecaySeconds}

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-20T12:28:58Z

- **pkg/controller/core/localqueue_controller.go:280** by [@pajakd](https://github.com/pajakd) — 2025-03-20T12:28:58Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	err, cachedLq := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+
+	if len(oldUsage) == 0 {
+		// first loop
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = oldUsage
+		return r.cache.UpdatedLQUsage(cqName, cachedLq)
```

  This is always nil? (It looks like UpdatedLQUsage returns always nil)

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-20T12:34:03Z

- **pkg/cache/cache.go:514** by [@pajakd](https://github.com/pajakd) — 2025-03-20T12:34:02Z
```diff
@@ -503,6 +503,22 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
+	cq := c.hm.ClusterQueue(kueue.ClusterQueueReference(cqName))
+	if cacheLq, ok := cq.localQueues[queueKey(lq)]; ok {
+		return nil, cacheLq
+	}
+	return errQNotFound, nil
+}
+
+func (c *Cache) UpdatedLQUsage(cqName string, lq *LocalQueue) error {
```

  nit: Perhaps consider renaming (StoreUpdatedLQUsage ?) as this func is updating something and the name suggests it returns the updated usage :). Also, do we want to keep the return type "error"?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-20T13:54:30Z

- **apis/config/v1beta1/configuration_types.go:497** by [@pajakd](https://github.com/pajakd) — 2025-03-20T13:54:29Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
+
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
+	AdmissionFairSharing *AdmissionFairSharing `json:"admissionFairSharing,omitempty"`
+}
+
+type AdmissionFairSharing struct {
+	// usageHalfDecayTime indicates the time after which the current usage will decay by a half
+	UsageHalfDecayTime metav1.Duration `json:"usageHalfDecayTime,omitempty"`
+
+	// usageSamplingFrequency indicates how often Kueue updates consumedResources in FairSharingStatus
+	UsageSamplingFrequency metav1.Duration `json:"usageSamplingFrequency,omitempty"`
```

  Shouldn't we have some minimum value of this parameter? This cannot be for example smaller than the duration of our single reconciling loop. 
  
  Also (at least in physics) frequency is the number of occurrences of sth in a unit of time. 
  
  This should (probably) be called sampling period or sampling interval. (unless I misunderstood this)

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-21T07:00:06Z

- **pkg/workload/workload.go:298** by [@pajakd](https://github.com/pajakd) — 2025-03-21T07:00:06Z
```diff
@@ -281,6 +281,24 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, fsConfig *config.FairSharing) (error, float64) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: i.Obj.Spec.QueueName}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return err, 0
+	}
+	usage := float64(0)
+	for resName, resVal := range lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources {
+		weight, found := fsConfig.AdmissionFairSharing.ResourceWeights[resName]
+		if !found {
+			weight = 1
+		}
+		usage += weight * resVal.AsApproximateFloat64()
+	}
+	usage *= lq.Spec.FairSharing.Weight.AsApproximateFloat64()
```

  I don't understand why we don't divide here. If some LQ has bigger weight, it should be prioritized so it should get smaller usage bc later LQs with smaller usage are prioritized in sorting, right?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-21T08:18:10Z

- **pkg/controller/core/localqueue_controller.go:285** by [@pajakd](https://github.com/pajakd) — 2025-03-21T08:18:09Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	err, cachedLq := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+
+	if len(oldUsage) == 0 {
+		// first loop
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = oldUsage
+		return r.cache.UpdatedLQUsage(cqName, cachedLq)
+	}
+	newUsage := utilmaps.MapValues(cachedLq.GetAdmittedUsage().FlattenFlavors(),
+		func(val int64) resource.Quantity { return resource.MustParse(strconv.FormatInt(val, 10)) },
+	)
+	scaledOld := utilresource.MulResources(oldUsage, alpha)
```

  Is this intentional? In the kep its the other way around ($new = (1-\alpha) \cdot old + \alpha \cdot current$). 
  
  Assuming current = 0 we want to have the exponential decay of the old usage, right?
  so we should have $new = old \cdot 2^{-t/t_{1/2}}$, but as we defined $\alpha = 1-2^{- \dots }$,so we should have $new = (1-\alpha)\cdot old + \alpha \cdot current$

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-03-21T08:39:11Z

- **apis/config/v1beta1/configuration_types.go:494** by [@pajakd](https://github.com/pajakd) — 2025-03-21T08:39:11Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
+
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
+	AdmissionFairSharing *AdmissionFairSharing `json:"admissionFairSharing,omitempty"`
+}
+
+type AdmissionFairSharing struct {
+	// usageHalfDecayTime indicates the time after which the current usage will decay by a half
+	UsageHalfDecayTime metav1.Duration `json:"usageHalfDecayTime,omitempty"`
```

  If this is really inspired by natural sciences then half-life is the term we might want to use.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T09:10:12Z

- **pkg/workload/workload.go:298** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T09:10:12Z
```diff
@@ -281,6 +281,24 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, fsConfig *config.FairSharing) (error, float64) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: i.Obj.Spec.QueueName}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return err, 0
+	}
+	usage := float64(0)
+	for resName, resVal := range lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources {
+		weight, found := fsConfig.AdmissionFairSharing.ResourceWeights[resName]
+		if !found {
+			weight = 1
+		}
+		usage += weight * resVal.AsApproximateFloat64()
+	}
+	usage *= lq.Spec.FairSharing.Weight.AsApproximateFloat64()
```

  Good catch, will fix

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T09:14:19Z

- **pkg/controller/core/localqueue_controller.go:285** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T09:14:19Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	err, cachedLq := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+
+	if len(oldUsage) == 0 {
+		// first loop
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = oldUsage
+		return r.cache.UpdatedLQUsage(cqName, cachedLq)
+	}
+	newUsage := utilmaps.MapValues(cachedLq.GetAdmittedUsage().FlattenFlavors(),
+		func(val int64) resource.Quantity { return resource.MustParse(strconv.FormatInt(val, 10)) },
+	)
+	scaledOld := utilresource.MulResources(oldUsage, alpha)
```

  Yes, this is intentional as I also calculated alpha slightly different from the KEP. In KEP we divide sampling/half-life, and here I do half-life/sampling to mitigate float inaccuracy

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T10:23:20Z

- **pkg/controller/core/localqueue_controller.go:270** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T10:23:20Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
```

  > Why aren't we looking at the time since the last update
  
  We look at it in the caller of this function
  
  > So, alpha is always the same value, regardless of how much time has passed
  
  Correct, this is wai. `halfDecayTimeSeconds` and `samplingFrequencySeconds` are parameters set by the user in the config API to control how fast usage decays, and the frequency is also how often Kueue should update the usage. This function is called regularly, every `samplingFrequencySeconds` seconds
  
  > But, I think there probably should be 0.5 ^{samplingFreqSeconds/halfDecaySeconds}
  
  As commented below, it is intentional to mitigate float inaccuracy

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T10:27:11Z

- **pkg/cache/cache.go:514** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T10:27:11Z
```diff
@@ -503,6 +503,22 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
+	cq := c.hm.ClusterQueue(kueue.ClusterQueueReference(cqName))
+	if cacheLq, ok := cq.localQueues[queueKey(lq)]; ok {
+		return nil, cacheLq
+	}
+	return errQNotFound, nil
+}
+
+func (c *Cache) UpdatedLQUsage(cqName string, lq *LocalQueue) error {
```

  Oh yes, this was a typo probably. How about simply`UpdateLQUsage`? This was my primary idea

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T10:27:21Z

- **pkg/controller/core/localqueue_controller.go:280** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T10:27:21Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	err, cachedLq := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+
+	if len(oldUsage) == 0 {
+		// first loop
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = oldUsage
+		return r.cache.UpdatedLQUsage(cqName, cachedLq)
```

  Will fix, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T13:41:44Z

- **pkg/workload/workload.go:298** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T13:41:43Z
```diff
@@ -281,6 +281,24 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, fsConfig *config.FairSharing) (error, float64) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: i.Obj.Spec.QueueName}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return err, 0
+	}
+	usage := float64(0)
+	for resName, resVal := range lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources {
+		weight, found := fsConfig.AdmissionFairSharing.ResourceWeights[resName]
+		if !found {
+			weight = 1
+		}
+		usage += weight * resVal.AsApproximateFloat64()
+	}
+	usage *= lq.Spec.FairSharing.Weight.AsApproximateFloat64()
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T13:42:37Z

- **pkg/controller/core/localqueue_controller.go:285** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T13:42:37Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	err, cachedLq := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+
+	if len(oldUsage) == 0 {
+		// first loop
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = oldUsage
+		return r.cache.UpdatedLQUsage(cqName, cachedLq)
+	}
+	newUsage := utilmaps.MapValues(cachedLq.GetAdmittedUsage().FlattenFlavors(),
+		func(val int64) resource.Quantity { return resource.MustParse(strconv.FormatInt(val, 10)) },
+	)
+	scaledOld := utilresource.MulResources(oldUsage, alpha)
```

  After revisiting this, and syncing with @pajakd I reverted those changes to align with the KEP

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T13:42:53Z

- **pkg/controller/core/localqueue_controller.go:280** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T13:42:53Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	err, cachedLq := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+
+	if len(oldUsage) == 0 {
+		// first loop
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = oldUsage
+		return r.cache.UpdatedLQUsage(cqName, cachedLq)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T13:44:01Z

- **pkg/util/resource/resource.go:111** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T13:44:01Z
```diff
@@ -103,3 +107,32 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+func MulResources(res map[corev1.ResourceName]resource.Quantity, multiplier float64) map[corev1.ResourceName]resource.Quantity {
```

  Absolutely, but I don't think it would be any simpler/cleaner? WDYT?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T13:44:57Z

- **pkg/controller/core/localqueue_controller.go:270** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T13:44:57Z
```diff
@@ -204,6 +264,32 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(lq *kueue.LocalQueue, cqName string) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingFrequency.Seconds())
+	alpha := 1.0 - math.Pow(0.5, halfDecayTimeSeconds/samplingFrequencySeconds)
```

  > But, I think there probably should be 0.5 ^{samplingFreqSeconds/halfDecaySeconds}
  
  After revisiting this, and syncing with @pajakd I reverted those changes to align with the KEP

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-03-21T13:46:36Z

- **apis/kueue/v1beta1/fairsharing_types.go:56** by [@PBundyra](https://github.com/PBundyra) — 2025-03-21T13:46:35Z
```diff
@@ -49,4 +53,12 @@ type FairSharingStatus struct {
 	// weight of zero, this will return 9223372036854775807, the
 	// maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
```

  Done

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-26T11:11:11Z

First pass. Let's iron out the API, and interaction with other FS mode, as this will be difficult to change later

- **pkg/cache/cache.go:506** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:07:06Z
```diff
@@ -503,6 +503,21 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
```

  nit: return values transposed compared to go convention?

- **pkg/cache/cache.go:506** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:07:24Z
```diff
@@ -503,6 +503,21 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
```

  can we use cqref as type instead of string, or does this make function too verbose to call?

- **apis/config/v1beta1/defaults.go:217** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:18:50Z
```diff
@@ -210,6 +210,18 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.PreemptionStrategies) == 0 {
 		fs.PreemptionStrategies = []PreemptionStrategy{LessThanOrEqualToFinalShare, LessThanInitialShare}
 	}
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.Modes) == 0 {
+		fs.Modes = []FairSharingMode{PreemptionBasedMode}
+	}
+
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && fs.AdmissionFairSharing != nil {
+		if fs.AdmissionFairSharing.UsageHalfDecayTime.Duration == 0 {
+			fs.AdmissionFairSharing.UsageHalfDecayTime = metav1.Duration{Duration: 24 * time.Hour}
+		}
+		if fs.AdmissionFairSharing.UsageSamplingFrequency.Duration == 0 {
+			fs.AdmissionFairSharing.UsageSamplingFrequency = metav1.Duration{Duration: 5 * time.Minute}
+		}
+	}
```

  can we break out FS to its own function?

- **apis/config/v1beta1/defaults.go:217** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:19:23Z
```diff
@@ -210,6 +210,18 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.PreemptionStrategies) == 0 {
 		fs.PreemptionStrategies = []PreemptionStrategy{LessThanOrEqualToFinalShare, LessThanInitialShare}
 	}
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.Modes) == 0 {
+		fs.Modes = []FairSharingMode{PreemptionBasedMode}
+	}
+
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && fs.AdmissionFairSharing != nil {
+		if fs.AdmissionFairSharing.UsageHalfDecayTime.Duration == 0 {
+			fs.AdmissionFairSharing.UsageHalfDecayTime = metav1.Duration{Duration: 24 * time.Hour}
+		}
+		if fs.AdmissionFairSharing.UsageSamplingFrequency.Duration == 0 {
+			fs.AdmissionFairSharing.UsageSamplingFrequency = metav1.Duration{Duration: 5 * time.Minute}
+		}
+	}
```

  then, starting the function with a guard clause, returning if nil || disabled, should make the rest of the logic less repetitive

- **pkg/resources/resource.go:52** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:26:57Z
```diff
@@ -43,3 +44,15 @@ func (q FlavorResourceQuantities) MarshalJSON() ([]byte, error) {
 	}
 	return json.Marshal(temp)
 }
+
+func (frq FlavorResourceQuantities) FlattenFlavors() map[corev1.ResourceName]int64 {
+	resources := sets.New[corev1.ResourceName]()
+	for key := range frq {
+		resources.Insert(key.Resource)
+	}
```

  Delete this please. This looks premature optimization, especially since the resources set may be resizing, just so we can provide a sizing _hint_ to result map

- **apis/config/v1beta1/configuration_types.go:455** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:39:10Z
```diff
@@ -449,6 +449,13 @@ const (
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
 )
 
+type FairSharingMode string
+
+const (
+	PreemptionBasedMode FairSharingMode = "PreemptionBased"
+	AdmissionTimeMode   FairSharingMode = "AdmissionTime"
+)
+
 type FairSharing struct {
 	// enable indicates whether to enable Fair Sharing for all cohorts.
 	// Defaults to false.
```

  We need to specify `Enable=True` for this Admission Based, right? It seems like this will result in enabling both modes at once, as this flag is wired throughout codebase to enable logic for the Preemption FS mode.

- **apis/config/v1beta1/configuration_types.go:485** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:53:38Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
```

  What is the compatibility of these two modes?

- **apis/config/v1beta1/configuration_types.go:485** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:57:22Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
```

  Depending on the answer to this - can we make invalid states impossible to represent - and generally simplify the configuration?
  
  This could look like the following:
  
  - struct/field for PreemptionBased
  - struct/field for AdmissionBased
  
  We get rid of enabled bool - it is inferred by one of above being non-nil. Depending on the answer to compatibility, we can disallow both being set at once.
  
  The advantage of this is we don't have some repetition of fields, which we need to validate for consistency.  Unfortunately, I think this will need to wait for v1beta2. Can we implement something now which makes this simplification later easier?

- **pkg/cache/cache.go:514** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T10:59:07Z
```diff
@@ -503,6 +503,21 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
+	cq := c.hm.ClusterQueue(kueue.ClusterQueueReference(cqName))
+	if cacheLq, ok := cq.localQueues[queueKey(lq)]; ok {
+		return nil, cacheLq
+	}
+	return errQNotFound, nil
+}
+
+func (c *Cache) UpdateLQUsage(cqName string, lq *LocalQueue) {
```

  here too, depending on answer to comment above

- **pkg/cache/localqueue.go:28** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T11:10:09Z
```diff
@@ -0,0 +1,41 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type LocalQueue struct {
```

  consider moving LQ definition out to a new PR, to reduce diff of this one

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-26T11:28:50Z

- **pkg/util/resource/resource.go:121** by [@gabesaba](https://github.com/gabesaba) — 2025-03-26T11:23:46Z
```diff
@@ -103,3 +107,32 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+func MulResources(res map[corev1.ResourceName]resource.Quantity, multiplier float64) map[corev1.ResourceName]resource.Quantity {
+	result := make(map[corev1.ResourceName]resource.Quantity, len(res))
+	for k, v := range res {
+		sum := float64(resources.ResourceValue(k, v))
+		sum *= multiplier
+		result[k] = resources.ResourceQuantity(k, int64(sum))
+	}
+	return result
+}
+
+func AddResources(a, b map[corev1.ResourceName]resource.Quantity) map[corev1.ResourceName]resource.Quantity {
```

  I think `AddResources` and `MulResources` can be simplified (and reduce risk of bugs) by reusing the `Requests` type.
  
  Consider: define a new constructor for `Requests`, `FromResourceList(corev1.ResourceList) Requests`. Then use respective Add/Mul functions. Then convert back with already defined `ToResourceList`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T11:58:03Z

- **pkg/cache/cache.go:506** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T11:58:03Z
```diff
@@ -503,6 +503,21 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T11:58:11Z

- **pkg/cache/cache.go:506** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T11:58:11Z
```diff
@@ -503,6 +503,21 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
```

  Absolutely, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T11:58:23Z

- **pkg/cache/cache.go:514** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T11:58:23Z
```diff
@@ -503,6 +503,21 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
+	cq := c.hm.ClusterQueue(kueue.ClusterQueueReference(cqName))
+	if cacheLq, ok := cq.localQueues[queueKey(lq)]; ok {
+		return nil, cacheLq
+	}
+	return errQNotFound, nil
+}
+
+func (c *Cache) UpdateLQUsage(cqName string, lq *LocalQueue) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:03:49Z

- **pkg/resources/resource.go:52** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:03:49Z
```diff
@@ -43,3 +44,15 @@ func (q FlavorResourceQuantities) MarshalJSON() ([]byte, error) {
 	}
 	return json.Marshal(temp)
 }
+
+func (frq FlavorResourceQuantities) FlattenFlavors() map[corev1.ResourceName]int64 {
+	resources := sets.New[corev1.ResourceName]()
+	for key := range frq {
+		resources.Insert(key.Resource)
+	}
```

  Agree, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:17:04Z

- **apis/config/v1beta1/configuration_types.go:485** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:17:04Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
```

  I like the idea of making invalid states impossible. Maybe we could continue this discussion in the KEP PR https://github.com/kubernetes-sigs/kueue/pull/4252

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:22:19Z

- **apis/config/v1beta1/defaults.go:217** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:22:19Z
```diff
@@ -210,6 +210,18 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.PreemptionStrategies) == 0 {
 		fs.PreemptionStrategies = []PreemptionStrategy{LessThanOrEqualToFinalShare, LessThanInitialShare}
 	}
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.Modes) == 0 {
+		fs.Modes = []FairSharingMode{PreemptionBasedMode}
+	}
+
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && fs.AdmissionFairSharing != nil {
+		if fs.AdmissionFairSharing.UsageHalfDecayTime.Duration == 0 {
+			fs.AdmissionFairSharing.UsageHalfDecayTime = metav1.Duration{Duration: 24 * time.Hour}
+		}
+		if fs.AdmissionFairSharing.UsageSamplingFrequency.Duration == 0 {
+			fs.AdmissionFairSharing.UsageSamplingFrequency = metav1.Duration{Duration: 5 * time.Minute}
+		}
+	}
```

  I'll hold on with those changes, until we have an agreement on how the API should look like

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:24:32Z

- **apis/config/v1beta1/configuration_types.go:455** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:24:31Z
```diff
@@ -449,6 +449,13 @@ const (
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
 )
 
+type FairSharingMode string
+
+const (
+	PreemptionBasedMode FairSharingMode = "PreemptionBased"
+	AdmissionTimeMode   FairSharingMode = "AdmissionTime"
+)
+
 type FairSharing struct {
 	// enable indicates whether to enable Fair Sharing for all cohorts.
 	// Defaults to false.
```

  Well, not necessarily. We introduce FairSharingModes to have control over which features should be enabled

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:27:00Z

- **apis/config/v1beta1/configuration_types.go:455** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:27:00Z
```diff
@@ -449,6 +449,13 @@ const (
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
 )
 
+type FairSharingMode string
+
+const (
+	PreemptionBasedMode FairSharingMode = "PreemptionBased"
+	AdmissionTimeMode   FairSharingMode = "AdmissionTime"
+)
+
 type FairSharing struct {
 	// enable indicates whether to enable Fair Sharing for all cohorts.
 	// Defaults to false.
```

  But indeed, there's some logic missing that should disable Preemption FS if the PreemptionBased mode is not set

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:28:40Z

- **apis/config/v1beta1/configuration_types.go:494** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:28:40Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
+
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
+	AdmissionFairSharing *AdmissionFairSharing `json:"admissionFairSharing,omitempty"`
+}
+
+type AdmissionFairSharing struct {
+	// usageHalfDecayTime indicates the time after which the current usage will decay by a half
+	UsageHalfDecayTime metav1.Duration `json:"usageHalfDecayTime,omitempty"`
```

  Let's continue conversation about the API naming in the KEP PR https://github.com/kubernetes-sigs/kueue/pull/4252

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:29:02Z

- **apis/config/v1beta1/configuration_types.go:497** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:29:01Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
+
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
+	AdmissionFairSharing *AdmissionFairSharing `json:"admissionFairSharing,omitempty"`
+}
+
+type AdmissionFairSharing struct {
+	// usageHalfDecayTime indicates the time after which the current usage will decay by a half
+	UsageHalfDecayTime metav1.Duration `json:"usageHalfDecayTime,omitempty"`
+
+	// usageSamplingFrequency indicates how often Kueue updates consumedResources in FairSharingStatus
+	UsageSamplingFrequency metav1.Duration `json:"usageSamplingFrequency,omitempty"`
```

  Let's continue conversation about API naming in the KEP PR https://github.com/kubernetes-sigs/kueue/pull/4252

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T12:31:52Z

- **pkg/cache/cache.go:514** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T12:31:52Z
```diff
@@ -503,6 +503,22 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName string, lq *kueue.LocalQueue) (error, *LocalQueue) {
+	cq := c.hm.ClusterQueue(kueue.ClusterQueueReference(cqName))
+	if cacheLq, ok := cq.localQueues[queueKey(lq)]; ok {
+		return nil, cacheLq
+	}
+	return errQNotFound, nil
+}
+
+func (c *Cache) UpdatedLQUsage(cqName string, lq *LocalQueue) error {
```

  Change to `UpdateLQUsage`. Feel free to reopen this if you like a different name better

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T14:23:06Z

- **pkg/util/resource/resource.go:121** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T14:23:06Z
```diff
@@ -103,3 +107,32 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+func MulResources(res map[corev1.ResourceName]resource.Quantity, multiplier float64) map[corev1.ResourceName]resource.Quantity {
+	result := make(map[corev1.ResourceName]resource.Quantity, len(res))
+	for k, v := range res {
+		sum := float64(resources.ResourceValue(k, v))
+		sum *= multiplier
+		result[k] = resources.ResourceQuantity(k, int64(sum))
+	}
+	return result
+}
+
+func AddResources(a, b map[corev1.ResourceName]resource.Quantity) map[corev1.ResourceName]resource.Quantity {
```

  Changed the types to simplify the logic and use some of the existing functions, ptal

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-11T14:44:41Z

- **pkg/cache/localqueue.go:28** by [@PBundyra](https://github.com/PBundyra) — 2025-04-11T14:44:41Z
```diff
@@ -0,0 +1,41 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+type LocalQueue struct {
```

  Done: https://github.com/kubernetes-sigs/kueue/pull/4933

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-15T17:00:49Z

- **apis/config/v1beta1/configuration_types.go:485** by [@PBundyra](https://github.com/PBundyra) — 2025-04-15T17:00:49Z
```diff
@@ -471,4 +478,26 @@ type FairSharing struct {
 	//   newest start time first.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// modes indicates which modes are enabled in FairSharing is turned on.
+	// By default it's': [“PreemptionBased”].
+	// Allowed values: “PreemptionBase”, “AdmissionTime”.
+	Modes []FairSharingMode `json:"fairSharingMode,omitempty"`
```

  Moved the discussion there, so I'll resolve this conversation

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-17T13:04:48Z

- **apis/config/v1beta1/defaults.go:217** by [@PBundyra](https://github.com/PBundyra) — 2025-04-17T13:04:48Z
```diff
@@ -210,6 +210,18 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.PreemptionStrategies) == 0 {
 		fs.PreemptionStrategies = []PreemptionStrategy{LessThanOrEqualToFinalShare, LessThanInitialShare}
 	}
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && len(fs.Modes) == 0 {
+		fs.Modes = []FairSharingMode{PreemptionBasedMode}
+	}
+
+	if fs := cfg.FairSharing; fs != nil && fs.Enable && fs.AdmissionFairSharing != nil {
+		if fs.AdmissionFairSharing.UsageHalfDecayTime.Duration == 0 {
+			fs.AdmissionFairSharing.UsageHalfDecayTime = metav1.Duration{Duration: 24 * time.Hour}
+		}
+		if fs.AdmissionFairSharing.UsageSamplingFrequency.Duration == 0 {
+			fs.AdmissionFairSharing.UsageSamplingFrequency = metav1.Duration{Duration: 5 * time.Minute}
+		}
+	}
```

  Simplified a bit, based on the discussion in KEP PR

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-18T13:42:12Z

- **apis/kueue/v1beta1/clusterqueue_types.go:168** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T12:55:58Z
```diff
@@ -149,6 +153,19 @@ type AdmissionCheckStrategyRule struct {
 	OnFlavors []ResourceFlavorReference `json:"onFlavors,omitempty"`
 }
 
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
+}
+
+type AdmissionMode string
+
+const (
+	// FairSharing based on usage, with QueuingStrategy as defined in CQ.
+	UsageBasedFairSharing AdmissionMode = "UsageBasedFairSharing"
+
+	NoFairSharing AdmissionMode = "NoFairSharing"
+)
+
```

  can these go in fairsharing_types.go?

- **pkg/cache/cache.go:507** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T12:58:52Z
```diff
@@ -500,6 +500,14 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName kueue.ClusterQueueReference, lq *kueue.LocalQueue) (*LocalQueue, error) {
+	cq := c.hm.ClusterQueue(cqName)
```

  do we need to make sure cq nonnil?

- **pkg/controller/core/localqueue_controller.go:81** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:02:58Z
```diff
@@ -60,13 +63,45 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	fsConfig *config.FairSharing
+	clock    clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithFairSharingConfig(cfg *config.FairSharing) LocalQueueReconcilerOption {
+	var result *config.FairSharing
+	if cfg == nil || !cfg.Enable || cfg.AdmissionFairSharing == nil {
+		result = nil
+	} else {
+		result = cfg
+	}
+	return func(o *LocalQueueReconcilerOptions) {
+		o.fsConfig = result
+	}
```

  ```suggestion
  	return func(o *LocalQueueReconcilerOptions) {
  		if cfg != nil && cfg.Enable && cfg.AdmissionFairSharing != nil {
  			o.fsConfig = cfg
  		} else {
  			o.fsConfig = nil
  		}
  	}
  ```

- **pkg/controller/core/localqueue_controller.go:103** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:10:32Z
```diff
@@ -60,13 +63,45 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	fsConfig *config.FairSharing
+	clock    clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithFairSharingConfig(cfg *config.FairSharing) LocalQueueReconcilerOption {
+	var result *config.FairSharing
+	if cfg == nil || !cfg.Enable || cfg.AdmissionFairSharing == nil {
+		result = nil
+	} else {
+		result = cfg
+	}
+	return func(o *LocalQueueReconcilerOptions) {
+		o.fsConfig = result
+	}
+}
+
+func WithClock(c clock.Clock) LocalQueueReconcilerOption {
+	return func(o *LocalQueueReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultLQOptions = LocalQueueReconcilerOptions{
+	clock: realClock,
+}
+
 // LocalQueueReconciler reconciles a LocalQueue object
 type LocalQueueReconciler struct {
 	client     client.Client
 	log        logr.Logger
 	queues     *queue.Manager
 	cache      *cache.Cache
 	wlUpdateCh chan event.GenericEvent
+	fsConfig   *config.FairSharing
```

  why not *config.AdmissionFairSharing? this matches the validation we did in withFairSharingConfig

- **pkg/controller/core/localqueue_controller.go:263** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:16:28Z
```diff
@@ -204,6 +257,27 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfLifeDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingInterval.Seconds())
+	alpha := 1.0 - math.Pow(0.5, samplingFrequencySeconds/halfDecayTimeSeconds)
```

  Shouldn't we use the actual time between checks? Imagine that the kueue controller goes down for a minute. or an hour. or the localqueue reconciler is slow/behind

- **pkg/controller/core/localqueue_controller.go:175** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:16:52Z
```diff
@@ -129,6 +171,17 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
 		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+		if r.fsConfig != nil {
+			recheckAfter := r.clock.Now().Sub(queueObj.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time)
```

  what happens when last update is nil?

- **pkg/queue/cluster_queue.go:97** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:21:58Z
```diff
@@ -89,17 +91,24 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.FairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs := false
+	fsResWeights := make(map[corev1.ResourceName]float64)
+	if fsConfig != nil && fsConfig.Enable && fsConfig.AdmissionFairSharing != nil &&
```

  Consider defining (in util?) function, IsAdmissionBasedFairSharingEnabled. Use here, and elsewhere where we do this exact check

- **pkg/workload/workload.go:290** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:30:33Z
```diff
@@ -281,6 +281,24 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: i.Obj.Spec.QueueName}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := float64(0)
```

  ```suggestion
  	usage := 0.0
  ```

- **pkg/workload/workload.go:297** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:31:03Z
```diff
@@ -281,6 +281,24 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: i.Obj.Spec.QueueName}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := float64(0)
+	for resName, resVal := range lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources {
+		weight, found := resWeights[resName]
+		if !found {
+			weight = 1
+		}
+		usage += weight * resVal.AsApproximateFloat64()
```

  ```suggestion
  		if weight, found := resWeights[resName]; found {
  			usage += resVal.AsApproximateFloat64() * weight
  		} else {
  			usage += resVal.AsApproximateFloat64() * 1.0
  		}
  ```

- **pkg/resources/requests.go:71** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:35:04Z
```diff
@@ -68,6 +68,12 @@ func (r Requests) Mul(f int64) {
 	}
 }
 
+func (r Requests) MulByFloat(f float64) {
```

  please document precision loss, as we are casting back to int64

- **apis/kueue/v1beta1/fairsharing_types.go:65** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:38:31Z
```diff
@@ -49,4 +53,17 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
+}
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources map[corev1.ResourceName]resource.Quantity `json:"consumedResources,omitempty"`
```

  I'm now wondering whether resource.Quantity is the right type here - as we are doing multiplications by floats - then rounding - with rather small integers (GPUs, for example)
  
  Do you think this will be a problem? If so, how else may we represent?

- **pkg/controller/core/localqueue_controller.go:81** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:39:39Z
```diff
@@ -60,13 +63,45 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	fsConfig *config.FairSharing
+	clock    clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithFairSharingConfig(cfg *config.FairSharing) LocalQueueReconcilerOption {
+	var result *config.FairSharing
+	if cfg == nil || !cfg.Enable || cfg.AdmissionFairSharing == nil {
+		result = nil
+	} else {
+		result = cfg
+	}
+	return func(o *LocalQueueReconcilerOptions) {
+		o.fsConfig = result
+	}
```

  or 
  
  o.fsConfig = cfg.AdmissionFairSharing, as suggested below

- **pkg/controller/core/localqueue_controller_test.go:196** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:41:04Z
```diff
@@ -112,6 +121,195 @@ func TestLocalQueueReconcile(t *testing.T) {
 				Obj(),
 			wantError: nil,
 		},
+		"local queue decaying usage decays if there is no running workloads": {
+			clusterQueue: utiltesting.MakeClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				Obj(),
+			localQueue: utiltesting.MakeLocalQueue("test-queue", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							LastUpdate: metav1.NewTime(now.Add(-6 * time.Minute)),
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("8")},
+						},
+					}).
+				Obj(),
+			wantLocalQueue: utiltesting.MakeLocalQueue("test-queue", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("4"),
+							},
+						},
+					}).
+				Obj(),
+			fsConfig: &config.FairSharing{
+				Enable: true,
+				AdmissionFairSharing: &config.AdmissionFairSharing{
+					UsageHalfLifeDecayTime: metav1.Duration{Duration: 5 * time.Minute},
+					UsageSamplingInterval:  metav1.Duration{Duration: 5 * time.Minute},
+				},
+			},
+		},
+		"local queue decaying usage sums the previous state and running workloads with A=1/2": {
+			clusterQueue: utiltesting.MakeClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				Obj(),
+			localQueue: utiltesting.MakeLocalQueue("lq", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							LastUpdate: metav1.NewTime(now.Add(-6 * time.Minute)),
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("8")},
+						},
+					}).
+				Obj(),
+			wantLocalQueue: utiltesting.MakeLocalQueue("lq", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				ReservingWorkloads(1).
+				AdmittedWorkloads(1).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("6"),
```

  Related to my precision comment(s): try also testing with GPUs, as CPUs are represented in millis

- **apis/config/v1beta1/configuration_types.go:455** by [@gabesaba](https://github.com/gabesaba) — 2025-04-18T13:42:10Z
```diff
@@ -449,6 +449,13 @@ const (
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
 )
 
+type FairSharingMode string
+
+const (
+	PreemptionBasedMode FairSharingMode = "PreemptionBased"
```

  I'm not convinced about this name - this mode can also work without preemption. Let me think about it a bit more (and if you have any ideas, please feel free to share too)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T14:40:46Z

- **apis/config/v1beta1/configuration_types.go:455** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T14:40:46Z
```diff
@@ -449,6 +449,13 @@ const (
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
 )
 
+type FairSharingMode string
+
+const (
+	PreemptionBasedMode FairSharingMode = "PreemptionBased"
```

  Oh this is idle, as we don't have modes anymore in this API

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T17:31:52Z

- **apis/config/v1beta1/configuration_types.go:455** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T17:31:52Z
```diff
@@ -449,6 +449,13 @@ const (
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
 )
 
+type FairSharingMode string
+
+const (
+	PreemptionBasedMode FairSharingMode = "PreemptionBased"
```

  Removed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T17:32:01Z

- **apis/kueue/v1beta1/clusterqueue_types.go:168** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T17:32:01Z
```diff
@@ -149,6 +153,19 @@ type AdmissionCheckStrategyRule struct {
 	OnFlavors []ResourceFlavorReference `json:"onFlavors,omitempty"`
 }
 
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
+}
+
+type AdmissionMode string
+
+const (
+	// FairSharing based on usage, with QueuingStrategy as defined in CQ.
+	UsageBasedFairSharing AdmissionMode = "UsageBasedFairSharing"
+
+	NoFairSharing AdmissionMode = "NoFairSharing"
+)
+
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T17:32:32Z

- **pkg/controller/core/localqueue_controller.go:81** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T17:32:32Z
```diff
@@ -60,13 +63,45 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	fsConfig *config.FairSharing
+	clock    clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithFairSharingConfig(cfg *config.FairSharing) LocalQueueReconcilerOption {
+	var result *config.FairSharing
+	if cfg == nil || !cfg.Enable || cfg.AdmissionFairSharing == nil {
+		result = nil
+	} else {
+		result = cfg
+	}
+	return func(o *LocalQueueReconcilerOptions) {
+		o.fsConfig = result
+	}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T17:32:40Z

- **pkg/controller/core/localqueue_controller.go:103** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T17:32:40Z
```diff
@@ -60,13 +63,45 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	fsConfig *config.FairSharing
+	clock    clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithFairSharingConfig(cfg *config.FairSharing) LocalQueueReconcilerOption {
+	var result *config.FairSharing
+	if cfg == nil || !cfg.Enable || cfg.AdmissionFairSharing == nil {
+		result = nil
+	} else {
+		result = cfg
+	}
+	return func(o *LocalQueueReconcilerOptions) {
+		o.fsConfig = result
+	}
+}
+
+func WithClock(c clock.Clock) LocalQueueReconcilerOption {
+	return func(o *LocalQueueReconcilerOptions) {
+		o.clock = c
+	}
+}
+
+var defaultLQOptions = LocalQueueReconcilerOptions{
+	clock: realClock,
+}
+
 // LocalQueueReconciler reconciles a LocalQueue object
 type LocalQueueReconciler struct {
 	client     client.Client
 	log        logr.Logger
 	queues     *queue.Manager
 	cache      *cache.Cache
 	wlUpdateCh chan event.GenericEvent
+	fsConfig   *config.FairSharing
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T17:33:08Z

- **pkg/controller/core/localqueue_controller.go:175** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T17:33:07Z
```diff
@@ -129,6 +171,17 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
 		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+		if r.fsConfig != nil {
+			recheckAfter := r.clock.Now().Sub(queueObj.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T17:35:33Z

- **pkg/workload/workload.go:297** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T17:35:33Z
```diff
@@ -281,6 +281,24 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: i.Obj.Spec.QueueName}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := float64(0)
+	for resName, resVal := range lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources {
+		weight, found := resWeights[resName]
+		if !found {
+			weight = 1
+		}
+		usage += weight * resVal.AsApproximateFloat64()
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-18T17:35:39Z

- **pkg/workload/workload.go:290** by [@PBundyra](https://github.com/PBundyra) — 2025-04-18T17:35:39Z
```diff
@@ -281,6 +281,24 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: i.Obj.Spec.QueueName}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := float64(0)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-24T16:19:51Z

- **pkg/controller/core/localqueue_controller.go:263** by [@PBundyra](https://github.com/PBundyra) — 2025-04-24T16:19:51Z
```diff
@@ -204,6 +257,27 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfDecayTimeSeconds := float64(r.fsConfig.AdmissionFairSharing.UsageHalfLifeDecayTime.Seconds())
+	samplingFrequencySeconds := float64(r.fsConfig.AdmissionFairSharing.UsageSamplingInterval.Seconds())
+	alpha := 1.0 - math.Pow(0.5, samplingFrequencySeconds/halfDecayTimeSeconds)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-24T16:20:26Z

- **pkg/resources/requests.go:71** by [@PBundyra](https://github.com/PBundyra) — 2025-04-24T16:20:25Z
```diff
@@ -68,6 +68,12 @@ func (r Requests) Mul(f int64) {
 	}
 }
 
+func (r Requests) MulByFloat(f float64) {
```

  Addressed, created a different function that doesn't rely on `Requests` type

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-24T16:21:35Z

- **apis/kueue/v1beta1/fairsharing_types.go:65** by [@PBundyra](https://github.com/PBundyra) — 2025-04-24T16:21:35Z
```diff
@@ -49,4 +53,17 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
+}
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources map[corev1.ResourceName]resource.Quantity `json:"consumedResources,omitempty"`
```

  I don't think this is necessarily a problem. With resource quantity we get a precision of 2 decimal places (even with GPUs) that should be sufficient for this feature.
  
  Are you worried that 2 decimal places is not enough?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-24T16:21:45Z

- **pkg/controller/core/localqueue_controller_test.go:196** by [@PBundyra](https://github.com/PBundyra) — 2025-04-24T16:21:45Z
```diff
@@ -112,6 +121,195 @@ func TestLocalQueueReconcile(t *testing.T) {
 				Obj(),
 			wantError: nil,
 		},
+		"local queue decaying usage decays if there is no running workloads": {
+			clusterQueue: utiltesting.MakeClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				Obj(),
+			localQueue: utiltesting.MakeLocalQueue("test-queue", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							LastUpdate: metav1.NewTime(now.Add(-6 * time.Minute)),
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("8")},
+						},
+					}).
+				Obj(),
+			wantLocalQueue: utiltesting.MakeLocalQueue("test-queue", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("4"),
+							},
+						},
+					}).
+				Obj(),
+			fsConfig: &config.FairSharing{
+				Enable: true,
+				AdmissionFairSharing: &config.AdmissionFairSharing{
+					UsageHalfLifeDecayTime: metav1.Duration{Duration: 5 * time.Minute},
+					UsageSamplingInterval:  metav1.Duration{Duration: 5 * time.Minute},
+				},
+			},
+		},
+		"local queue decaying usage sums the previous state and running workloads with A=1/2": {
+			clusterQueue: utiltesting.MakeClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				Obj(),
+			localQueue: utiltesting.MakeLocalQueue("lq", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							LastUpdate: metav1.NewTime(now.Add(-6 * time.Minute)),
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("8")},
+						},
+					}).
+				Obj(),
+			wantLocalQueue: utiltesting.MakeLocalQueue("lq", "default").
+				ClusterQueue("cq").
+				Active(metav1.ConditionTrue).
+				ReservingWorkloads(1).
+				AdmittedWorkloads(1).
+				FairSharing(&kueue.FairSharing{
+					Weight: ptr.To(resource.MustParse("1")),
+				}).
+				FairSharingStatus(
+					&kueue.FairSharingStatus{
+						AdmissionFairSharingStatus: &kueue.AdmissionFairSharingStatus{
+							ConsumedResources: map[corev1.ResourceName]resource.Quantity{
+								corev1.ResourceCPU: resource.MustParse("6"),
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-24T16:33:03Z

- **pkg/queue/cluster_queue.go:97** by [@PBundyra](https://github.com/PBundyra) — 2025-04-24T16:33:03Z
```diff
@@ -89,17 +91,24 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.FairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs := false
+	fsResWeights := make(map[corev1.ResourceName]float64)
+	if fsConfig != nil && fsConfig.Enable && fsConfig.AdmissionFairSharing != nil &&
```

  We do a similar check only in defaults so I'd rather keep it simple and don't introduce util func

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-24T16:58:43Z

- **pkg/cache/cache.go:507** by [@PBundyra](https://github.com/PBundyra) — 2025-04-24T16:58:42Z
```diff
@@ -500,6 +500,14 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName kueue.ClusterQueueReference, lq *kueue.LocalQueue) (*LocalQueue, error) {
+	cq := c.hm.ClusterQueue(cqName)
```

  Thanks for catching that, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-25T10:24:57Z

- **apis/kueue/v1beta1/fairsharing_types.go:65** by [@PBundyra](https://github.com/PBundyra) — 2025-04-25T10:24:57Z
```diff
@@ -49,4 +53,17 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
+}
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources map[corev1.ResourceName]resource.Quantity `json:"consumedResources,omitempty"`
```

  Discussed offline and agreed this is sufficient

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-25T11:24:25Z

- **pkg/controller/core/core.go:54** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T10:53:22Z
```diff
@@ -43,14 +43,15 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	if err := acRec.SetupWithManager(mgr, cfg); err != nil {
 		return "AdmissionCheck", err
 	}
-	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
+	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc,
+		WithFairSharingConfig(cfg.FairSharing))
 	if err := qRec.SetupWithManager(mgr, cfg); err != nil {
 		return "LocalQueue", err
 	}
 
 	var fairSharingEnabled bool
 	if cfg.FairSharing != nil {
-		fairSharingEnabled = cfg.FairSharing.Enable
+		fairSharingEnabled = cfg.FairSharing.Enable && len(cfg.FairSharing.PreemptionStrategies) > 0
```

  I think that this will always be true, due to defaulting logic. Is there a way to disable classical fair sharing, while enabling Admission fair sharing?

- **pkg/controller/core/localqueue_controller.go:169** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T10:58:39Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
```

  is this semantically equivalent? 
  ```suggestion
  	if meta.IsStatusConditionFalse(cq.Status.Conditions, kueue.ClusterQueueActive) {
  ```

- **pkg/controller/core/localqueue_controller.go:177** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:00:58Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+		err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
+	if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
```

  nit: consider making this an else (or else if) clause, as it is the compliment to the ClusterQueueInactive condition

- **pkg/controller/core/localqueue_controller.go:182** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:03:46Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+		err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
+	if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	if r.admissionFSConfig != nil {
+		if lq.Status.FairSharingStatus.AdmissionFairSharingStatus == nil {
+			lq.Status.FairSharingStatus.AdmissionFairSharingStatus = &kueue.AdmissionFairSharingStatus{
+				LastUpdate: metav1.NewTime(r.clock.Now()),
+			}
+			err = r.client.Status().Update(ctx, &lq)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
```

  why do we skip reconciler first iteration?

- **pkg/controller/core/localqueue_controller.go:273** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:07:07Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+		return r.client.Status().Update(ctx, lq)
+	}
```

  During this time, workloads will still be admitted, but their usage won't be counted, right? Is this something we need to handle (disabling scheduling)?

- **pkg/controller/core/localqueue_controller.go:179** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:10:35Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+		err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
+	if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	if r.admissionFSConfig != nil {
```

  nit: how about splitting this logically into 3 parts, a function for each:
  
  1) initialize status if nil
  2) determine recheckAfter
  3) (conditionally) Reconcile
  
  Right now, part 1/2 are combined.

- **pkg/controller/core/localqueue_controller.go:277** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:11:29Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+		return r.client.Status().Update(ctx, lq)
+	}
+	samplingFrequencySeconds := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time).Seconds()
+	alpha := 1.0 - math.Pow(0.5, samplingFrequencySeconds/halfLifeTimeSeconds)
```

  nit: please define these more locally (right before usage)

- **pkg/controller/core/localqueue_controller.go:272** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:13:45Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
```

  can we just set to nil (or default resource list), and get oldUsage later (right before it is used?)

- **pkg/controller/core/localqueue_controller.go:304** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:15:01Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+		return r.client.Status().Update(ctx, lq)
+	}
+	samplingFrequencySeconds := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time).Seconds()
+	alpha := 1.0 - math.Pow(0.5, samplingFrequencySeconds/halfLifeTimeSeconds)
+	cachedLq, err := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+	newUsage := cachedLq.GetAdmittedUsage().FlattenFlavors().ToResourceList()
+	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
+	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
+	lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
+	lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+	return r.client.Status().Update(ctx, lq)
```

  nit: add comments before each logical section (calculate usage, update status)

- **pkg/controller/core/localqueue_controller.go:169** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:18:35Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
```

  no it is not. but how about ordering like this (related to my comment below)
  
  ```
  if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
   
  } else {
  
  }
  ```

- **pkg/queue/cluster_queue.go:101** by [@gabesaba](https://github.com/gabesaba) — 2025-04-25T11:21:18Z
```diff
@@ -89,17 +91,24 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.FairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs := false
+	fsResWeights := make(map[corev1.ResourceName]float64)
+	if fsConfig != nil && fsConfig.Enable && fsConfig.AdmissionFairSharing != nil &&
+		cq.Spec.AdmissionScope != nil && cq.Spec.AdmissionScope.AdmissionMode == kueue.UsageBasedFairSharing {
+		enableAdmissionFs = true
+		fsResWeights = fsConfig.AdmissionFairSharing.ResourceWeights
+	}
```

  nit: func(fsConfig) -> bool, map

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-25T13:19:19Z

- **pkg/controller/core/localqueue_controller.go:273** by [@PBundyra](https://github.com/PBundyra) — 2025-04-25T13:19:19Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+		return r.client.Status().Update(ctx, lq)
+	}
```

  > During this time, workloads will still be admitted, but their usage won't be counted, right?
  
  Exactly
  
  > Is this something we need to handle (disabling scheduling)?
  
  I don't think so, all LQs will have the same usage, so the admission will be based on priorities, and timestamps

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-25T13:57:04Z

- **pkg/controller/core/core.go:54** by [@PBundyra](https://github.com/PBundyra) — 2025-04-25T13:57:04Z
```diff
@@ -43,14 +43,15 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	if err := acRec.SetupWithManager(mgr, cfg); err != nil {
 		return "AdmissionCheck", err
 	}
-	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
+	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc,
+		WithFairSharingConfig(cfg.FairSharing))
 	if err := qRec.SetupWithManager(mgr, cfg); err != nil {
 		return "LocalQueue", err
 	}
 
 	var fairSharingEnabled bool
 	if cfg.FairSharing != nil {
-		fairSharingEnabled = cfg.FairSharing.Enable
+		fairSharingEnabled = cfg.FairSharing.Enable && len(cfg.FairSharing.PreemptionStrategies) > 0
```

  Good point, I believe we should allow user to disable the classical fair sharing and use admission fs on its own. I think we need changes to defaulting logic

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-28T14:42:19Z

- **pkg/controller/core/localqueue_controller.go:169** by [@PBundyra](https://github.com/PBundyra) — 2025-04-28T14:42:19Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-28T14:42:25Z

- **pkg/controller/core/localqueue_controller.go:182** by [@PBundyra](https://github.com/PBundyra) — 2025-04-28T14:42:25Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+		err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
+	if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	if r.admissionFSConfig != nil {
+		if lq.Status.FairSharingStatus.AdmissionFairSharingStatus == nil {
+			lq.Status.FairSharingStatus.AdmissionFairSharingStatus = &kueue.AdmissionFairSharingStatus{
+				LastUpdate: metav1.NewTime(r.clock.Now()),
+			}
+			err = r.client.Status().Update(ctx, &lq)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-28T14:42:31Z

- **pkg/controller/core/localqueue_controller.go:177** by [@PBundyra](https://github.com/PBundyra) — 2025-04-28T14:42:31Z
```diff
@@ -105,33 +144,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq)
 	if err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
+	if !meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+		err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
+	if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-28T14:42:50Z

- **pkg/controller/core/localqueue_controller.go:277** by [@PBundyra](https://github.com/PBundyra) — 2025-04-28T14:42:50Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+		return r.client.Status().Update(ctx, lq)
+	}
+	samplingFrequencySeconds := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time).Seconds()
+	alpha := 1.0 - math.Pow(0.5, samplingFrequencySeconds/halfLifeTimeSeconds)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-28T14:42:59Z

- **pkg/controller/core/localqueue_controller.go:304** by [@PBundyra](https://github.com/PBundyra) — 2025-04-28T14:42:59Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+		return r.client.Status().Update(ctx, lq)
+	}
+	samplingFrequencySeconds := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time).Seconds()
+	alpha := 1.0 - math.Pow(0.5, samplingFrequencySeconds/halfLifeTimeSeconds)
+	cachedLq, err := r.cache.GetCacheLocalQueue(cqName, lq)
+	if err != nil {
+		return err
+	}
+	newUsage := cachedLq.GetAdmittedUsage().FlattenFlavors().ToResourceList()
+	scaledNewUsage := resource.MulByFloat(newUsage, alpha)
+	scaledOldUsage := resource.MulByFloat(oldUsage, 1-alpha)
+	lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MergeResourceListKeepSum(scaledOldUsage, scaledNewUsage)
+	lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate = metav1.NewTime(r.clock.Now())
+	return r.client.Status().Update(ctx, lq)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-28T14:43:06Z

- **pkg/queue/cluster_queue.go:101** by [@PBundyra](https://github.com/PBundyra) — 2025-04-28T14:43:06Z
```diff
@@ -89,17 +91,24 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.FairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs := false
+	fsResWeights := make(map[corev1.ResourceName]float64)
+	if fsConfig != nil && fsConfig.Enable && fsConfig.AdmissionFairSharing != nil &&
+		cq.Spec.AdmissionScope != nil && cq.Spec.AdmissionScope.AdmissionMode == kueue.UsageBasedFairSharing {
+		enableAdmissionFs = true
+		fsResWeights = fsConfig.AdmissionFairSharing.ResourceWeights
+	}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-28T14:43:23Z

- **pkg/controller/core/localqueue_controller.go:272** by [@PBundyra](https://github.com/PBundyra) — 2025-04-28T14:43:23Z
```diff
@@ -204,6 +264,29 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	return true
 }
 
+func (r *LocalQueueReconciler) ReconcileConsumedUsage(ctx context.Context, lq *kueue.LocalQueue, cqName kueue.ClusterQueueReference) error {
+	halfLifeTimeSeconds := r.admissionFSConfig.UsageHalfLifeTime.Seconds()
+	oldUsage := lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources
+	// reset usage to 0 if halfLife is 0
+	if halfLifeTimeSeconds == 0 {
+		lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources = resource.MulByFloat(oldUsage, 0)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-29T13:30:29Z

- **pkg/controller/core/core.go:54** by [@PBundyra](https://github.com/PBundyra) — 2025-04-29T13:30:29Z
```diff
@@ -43,14 +43,15 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	if err := acRec.SetupWithManager(mgr, cfg); err != nil {
 		return "AdmissionCheck", err
 	}
-	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
+	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc,
+		WithFairSharingConfig(cfg.FairSharing))
 	if err := qRec.SetupWithManager(mgr, cfg); err != nil {
 		return "LocalQueue", err
 	}
 
 	var fairSharingEnabled bool
 	if cfg.FairSharing != nil {
-		fairSharingEnabled = cfg.FairSharing.Enable
+		fairSharingEnabled = cfg.FairSharing.Enable && len(cfg.FairSharing.PreemptionStrategies) > 0
```

  Added another PreemptionStrategy for classical fs that disables it

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-05T07:45:38Z

- **pkg/controller/core/core.go:54** by [@gabesaba](https://github.com/gabesaba) — 2025-05-05T07:45:38Z
```diff
@@ -43,14 +43,15 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	if err := acRec.SetupWithManager(mgr, cfg); err != nil {
 		return "AdmissionCheck", err
 	}
-	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc)
+	qRec := NewLocalQueueReconciler(mgr.GetClient(), qManager, cc,
+		WithFairSharingConfig(cfg.FairSharing))
 	if err := qRec.SetupWithManager(mgr, cfg); err != nil {
 		return "LocalQueue", err
 	}
 
 	var fairSharingEnabled bool
 	if cfg.FairSharing != nil {
-		fairSharingEnabled = cfg.FairSharing.Enable
+		fairSharingEnabled = cfg.FairSharing.Enable && len(cfg.FairSharing.PreemptionStrategies) > 0
```

  Though this new parameter only concerns preemption - Classical Fair Sharing also works during admission - determining the order in which we choose candidates

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-05T13:28:49Z

- **apis/config/v1beta1/configuration_types.go:450** by [@gabesaba](https://github.com/gabesaba) — 2025-05-05T13:17:23Z
```diff
@@ -447,6 +447,7 @@ type PreemptionStrategy string
 const (
 	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+	NoPreemption                PreemptionStrategy = "NoPreemption"
```

  I'm not convinced that this is the right solution (even if it is, it should be part of a separate PR as it is introducing a new API). I think this will make classical fair sharing harder to understand - as now disabling preemption can be controlled by both knobs in CQs and here, in configuration

- **apis/config/v1beta1/configuration_types.go:479** by [@gabesaba](https://github.com/gabesaba) — 2025-05-05T13:18:26Z
```diff
@@ -469,6 +470,25 @@ type FairSharing struct {
 	//   This strategy doesn't depend on the share usage of the workload being preempted.
 	//   As a result, the strategy chooses to preempt workloads with the lowest priority and
 	//   newest start time first.
+	// - NoPreemption: Never preempt a workload.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
+	AdmissionFairSharing *AdmissionFairSharing `json:"admissionFairSharing,omitempty"`
```

  Until we can put ClassicalFairSharing into its own proto, I think we need to do one of the following to allow the 2 types of Fair Sharing to be enabled/disabled independently:
  
  1) Add a second boolean, something like `enableAdmissionFairSharing`
  2) (preferred) Use `AdmissionFairSharing` as a top level struct, not nested in FairSharing. This makes it clear to users that these mechanisms are independent and compatible. For consistency with classical, you may consider adding `enable bool` in here, and deprecate later as part of #5032

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-05T14:48:12Z

- **apis/config/v1beta1/configuration_types.go:479** by [@PBundyra](https://github.com/PBundyra) — 2025-05-05T14:48:12Z
```diff
@@ -469,6 +470,25 @@ type FairSharing struct {
 	//   This strategy doesn't depend on the share usage of the workload being preempted.
 	//   As a result, the strategy chooses to preempt workloads with the lowest priority and
 	//   newest start time first.
+	// - NoPreemption: Never preempt a workload.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
+	AdmissionFairSharing *AdmissionFairSharing `json:"admissionFairSharing,omitempty"`
```

  Discussed offline and came to conclusion the second approach seems the cleanest, so this is what I'm gonna do

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-05T14:48:56Z

- **apis/config/v1beta1/configuration_types.go:450** by [@PBundyra](https://github.com/PBundyra) — 2025-05-05T14:48:56Z
```diff
@@ -447,6 +447,7 @@ type PreemptionStrategy string
 const (
 	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+	NoPreemption                PreemptionStrategy = "NoPreemption"
```

  Will drop it as a result of the discussion here: https://github.com/kubernetes-sigs/kueue/pull/4687/files#r2073433984

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-05T15:10:02Z

- **pkg/queue/cluster_queue.go:442** by [@gabesaba](https://github.com/gabesaba) — 2025-05-05T14:40:10Z
```diff
@@ -410,8 +431,24 @@ func (c *ClusterQueue) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueR
 // to sort workloads. The function sorts workloads based on their priority.
 // When priorities are equal, it uses the workload's creation or eviction
 // time.
-func queueOrderingFunc(wo workload.Ordering) func(a, b *workload.Info) bool {
+func queueOrderingFunc(ctx context.Context, c client.Client, wo workload.Ordering, fsResWeights map[corev1.ResourceName]float64, enableAdmissionFs bool) func(a, b *workload.Info) bool {
+	log := ctrl.LoggerFrom(ctx)
+
 	return func(a, b *workload.Info) bool {
+		if enableAdmissionFs {
+			lqAUsage, errA := a.LqUsage(ctx, c, fsResWeights)
+			lqBUsage, errB := b.LqUsage(ctx, c, fsResWeights)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", a.Obj.Spec.QueueName, "Usage", lqAUsage)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", b.Obj.Spec.QueueName, "Usage", lqBUsage)
```

  should we error check before logging?

- **pkg/queue/cluster_queue.go:445** by [@gabesaba](https://github.com/gabesaba) — 2025-05-05T14:40:52Z
```diff
@@ -410,8 +431,24 @@ func (c *ClusterQueue) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueR
 // to sort workloads. The function sorts workloads based on their priority.
 // When priorities are equal, it uses the workload's creation or eviction
 // time.
-func queueOrderingFunc(wo workload.Ordering) func(a, b *workload.Info) bool {
+func queueOrderingFunc(ctx context.Context, c client.Client, wo workload.Ordering, fsResWeights map[corev1.ResourceName]float64, enableAdmissionFs bool) func(a, b *workload.Info) bool {
+	log := ctrl.LoggerFrom(ctx)
+
 	return func(a, b *workload.Info) bool {
+		if enableAdmissionFs {
+			lqAUsage, errA := a.LqUsage(ctx, c, fsResWeights)
+			lqBUsage, errB := b.LqUsage(ctx, c, fsResWeights)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", a.Obj.Spec.QueueName, "Usage", lqAUsage)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", b.Obj.Spec.QueueName, "Usage", lqBUsage)
+			switch {
+			case errA != nil:
+				log.Error(errA, "Error fetching LocalQueue from informer")
```

  nit: should we specify a log level?

- **pkg/util/resource/resource.go:112** by [@gabesaba](https://github.com/gabesaba) — 2025-05-05T14:59:27Z
```diff
@@ -103,3 +103,17 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+// MulByFloat multiplies every element in q by f.
+// It first mutiplies f by 1 000 to mitigate further precision loss, and then leverages
+// k8s.io/apimachinery/pkg/api/resource package to provide precision to 3 decimal places
+func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
+	ret := q.DeepCopy()
+	scaleFact := f * 1_000
```

  I don't think this is necessary. float64 uses 53 of its bits for precision, allowing at least 15 significant decimal digit precision - [wiki](https://en.wikipedia.org/wiki/Double-precision_floating-point_format)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-05T17:42:27Z

- **apis/config/v1beta1/configuration_types.go:450** by [@PBundyra](https://github.com/PBundyra) — 2025-05-05T17:42:27Z
```diff
@@ -447,6 +447,7 @@ type PreemptionStrategy string
 const (
 	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
 	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+	NoPreemption                PreemptionStrategy = "NoPreemption"
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T07:53:34Z

- **pkg/queue/cluster_queue.go:445** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T07:53:34Z
```diff
@@ -410,8 +431,24 @@ func (c *ClusterQueue) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueR
 // to sort workloads. The function sorts workloads based on their priority.
 // When priorities are equal, it uses the workload's creation or eviction
 // time.
-func queueOrderingFunc(wo workload.Ordering) func(a, b *workload.Info) bool {
+func queueOrderingFunc(ctx context.Context, c client.Client, wo workload.Ordering, fsResWeights map[corev1.ResourceName]float64, enableAdmissionFs bool) func(a, b *workload.Info) bool {
+	log := ctrl.LoggerFrom(ctx)
+
 	return func(a, b *workload.Info) bool {
+		if enableAdmissionFs {
+			lqAUsage, errA := a.LqUsage(ctx, c, fsResWeights)
+			lqBUsage, errB := b.LqUsage(ctx, c, fsResWeights)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", a.Obj.Spec.QueueName, "Usage", lqAUsage)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", b.Obj.Spec.QueueName, "Usage", lqBUsage)
+			switch {
+			case errA != nil:
+				log.Error(errA, "Error fetching LocalQueue from informer")
```

  Sure, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T07:53:44Z

- **pkg/queue/cluster_queue.go:442** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T07:53:44Z
```diff
@@ -410,8 +431,24 @@ func (c *ClusterQueue) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueR
 // to sort workloads. The function sorts workloads based on their priority.
 // When priorities are equal, it uses the workload's creation or eviction
 // time.
-func queueOrderingFunc(wo workload.Ordering) func(a, b *workload.Info) bool {
+func queueOrderingFunc(ctx context.Context, c client.Client, wo workload.Ordering, fsResWeights map[corev1.ResourceName]float64, enableAdmissionFs bool) func(a, b *workload.Info) bool {
+	log := ctrl.LoggerFrom(ctx)
+
 	return func(a, b *workload.Info) bool {
+		if enableAdmissionFs {
+			lqAUsage, errA := a.LqUsage(ctx, c, fsResWeights)
+			lqBUsage, errB := b.LqUsage(ctx, c, fsResWeights)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", a.Obj.Spec.QueueName, "Usage", lqAUsage)
+			log.V(3).Info("Resource usage from LocalQueue", "LocalQueue", b.Obj.Spec.QueueName, "Usage", lqBUsage)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T07:54:01Z

- **pkg/util/resource/resource.go:112** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T07:54:01Z
```diff
@@ -103,3 +103,17 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+// MulByFloat multiplies every element in q by f.
+// It first mutiplies f by 1 000 to mitigate further precision loss, and then leverages
+// k8s.io/apimachinery/pkg/api/resource package to provide precision to 3 decimal places
+func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
+	ret := q.DeepCopy()
+	scaleFact := f * 1_000
```

  Addressed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T07:54:12Z

- **apis/config/v1beta1/configuration_types.go:479** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T07:54:12Z
```diff
@@ -469,6 +470,25 @@ type FairSharing struct {
 	//   This strategy doesn't depend on the share usage of the workload being preempted.
 	//   As a result, the strategy chooses to preempt workloads with the lowest priority and
 	//   newest start time first.
+	// - NoPreemption: Never preempt a workload.
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
+
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
+	AdmissionFairSharing *AdmissionFairSharing `json:"admissionFairSharing,omitempty"`
```

  Done

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-06T09:30:24Z

- **cmd/kueue/main.go:218** by [@gabesaba](https://github.com/gabesaba) — 2025-05-06T09:15:52Z
```diff
@@ -217,6 +217,7 @@ func main() {
 	}
 	if cfg.FairSharing != nil {
```

  we need to handle FairSharing == nil, AdmissionFairSharing != nil, right?

- **pkg/controller/core/localqueue_controller.go:80** by [@gabesaba](https://github.com/gabesaba) — 2025-05-06T09:16:19Z
```diff
@@ -60,13 +63,43 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	admissionFSConfig *config.AdmissionFairSharing
+	clock             clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithAdmissionFairSharingConfig(cfg *config.AdmissionFairSharing) LocalQueueReconcilerOption {
+	return func(o *LocalQueueReconcilerOptions) {
+		if cfg != nil {
+			o.admissionFSConfig = cfg
+		} else {
+			o.admissionFSConfig = nil
+		}
```

  ```suggestion
  			o.admissionFSConfig = cfg
  
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T09:42:37Z

Initial pass

- **apis/config/v1beta1/configuration_types.go:482** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:12:31Z
```diff
@@ -472,3 +476,17 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0.
```

  Do you mean it is reset to 0 immediately? 
  
  0 is the default? Also, do we have use-cases for 0?

- **apis/config/v1beta1/configuration_types.go:486** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:13:07Z
```diff
@@ -472,3 +476,17 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0.
+	UsageHalfLifeTime metav1.Duration `json:"usageHalfLifeTime,omitempty"`
+
+	// usageSamplingInterval indicates how often Kueue updates consumedResources in FairSharingStatus
+	UsageSamplingInterval metav1.Duration `json:"usageSamplingInterval,omitempty"`
```

  What is the default, or 0? If 0 is the default, does it just run in a hot loop?

- **apis/kueue/v1beta1/fairsharing_types.go:81** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:13:44Z
```diff
@@ -49,4 +53,30 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
+}
+
+type AdmissionMode string
+
+const (
+	// FairSharing based on usage, with QueuingStrategy as defined in CQ.
+	UsageBasedFairSharing AdmissionMode = "UsageBasedFairSharing"
+
+	NoFairSharing AdmissionMode = "NoFairSharing"
```

  Add a comment

- **apis/kueue/v1beta1/localqueue_types.go:53** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:14:37Z
```diff
@@ -48,6 +48,12 @@ type LocalQueueSpec struct {
 	// +kubebuilder:validation:Enum=None;Hold;HoldAndDrain
 	// +kubebuilder:default="None"
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
+
+	// fairSharing defines the properties of the LocalQueue when
+	// participating in FairSharing.  The values are only relevant
```

  `participating in FairSharing.` -> maybe participating in Admission Fair Sharing to reduce confusion?

- **pkg/queue/cluster_queue_test.go:158** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:17:41Z
```diff
@@ -149,7 +155,7 @@ func Test_PushOrUpdate(t *testing.T) {
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			cq := newClusterQueueImpl(defaultOrdering, fakeClock)
+			cq := newClusterQueueImpl(context.TODO(), nil, defaultOrdering, fakeClock, nil, false)
```

  Don't use TODO context, even in tests, there should be t.Context() exposed, iirc.

- **pkg/controller/core/localqueue_controller.go:170** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:19:57Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
```

  Is the refactoring needed? The PR is large and does not have any feature gate, so we should minimize drive-by refactoring. 
  
  Or Is it just refactoring or potentially changing the logic?

- **pkg/cache/clusterqueue.go:569** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:24:34Z
```diff
@@ -566,6 +566,8 @@ func (c *clusterQueue) flavorInUse(flavor kueue.ResourceFlavorReference) bool {
 
 func (q *LocalQueue) resetFlavorsAndResources(cqUsage resources.FlavorResourceQuantities, cqAdmittedUsage resources.FlavorResourceQuantities) {
 	// Clean up removed flavors or resources.
+	q.Lock()
```

  Was this a drive-by fix, needed fix only for this PR?

- **pkg/workload/workload.go:297** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:30:46Z
```diff
@@ -282,6 +282,28 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: string(i.Obj.Spec.QueueName)}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := 0.0
+	for resName, resVal := range lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources {
+		if weight, found := resWeights[resName]; found {
+			usage += weight * resVal.AsApproximateFloat64()
+		} else {
+			// if no weight for resource was defined, use default weight of 1
+			usage += resVal.AsApproximateFloat64()
```

  maybe this is more self explanatory that weight is defaulted
  ```golang
  weight, found := resWeights[resName]
  if !found {
     weight = 1
  } 
  usage += weight * resVal.AsApproximateFloat64()
  ```

- **pkg/controller/core/localqueue_controller.go:108** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:33:07Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
```

  Please don't do drive-by var naming, the PR is already big, I'm ok with a follow up

- **pkg/controller/core/localqueue_controller.go:185** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:37:30Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
+		if err := r.initializeAdmissionFsStatus(ctx, &lq); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		recheckAfter := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time)
+		if recheckAfter < r.admissionFSConfig.UsageSamplingInterval.Duration {
+			return ctrl.Result{RequeueAfter: recheckAfter}, nil
```

  Is this correct?
  Suppose duration=5min, now is after 3min from the last, so 2min to the next.
  Then, Now().Sub(Last) is 3min, so we should requeue after 2min?

- **pkg/controller/core/localqueue_controller.go:179** by [@mimowo](https://github.com/mimowo) — 2025-05-06T09:39:28Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
```

  IIUC both `initializeAdmissionFsStatus` and `reconcileConsumedUsage` update the LQ status. Can we combine them? If this is non trivial, I think a follow up issue is ok.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T10:29:15Z

- **pkg/controller/core/localqueue_controller.go:80** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T10:29:15Z
```diff
@@ -60,13 +63,43 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	admissionFSConfig *config.AdmissionFairSharing
+	clock             clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithAdmissionFairSharingConfig(cfg *config.AdmissionFairSharing) LocalQueueReconcilerOption {
+	return func(o *LocalQueueReconcilerOptions) {
+		if cfg != nil {
+			o.admissionFSConfig = cfg
+		} else {
+			o.admissionFSConfig = nil
+		}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T10:29:37Z

- **cmd/kueue/main.go:218** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T10:29:37Z
```diff
@@ -217,6 +217,7 @@ func main() {
 	}
 	if cfg.FairSharing != nil {
```

  Right, good catch! Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T10:31:23Z

- **apis/config/v1beta1/configuration_types.go:482** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T10:31:23Z
```diff
@@ -472,3 +476,17 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0.
```

  > Do you mean it is reset to 0 immediately?
  
  Exactly
  
  > 0 is the default?
  
  No, we don't have a default for HalfTime
  
  > Also, do we have use-cases for 0?
  
  Yes, we would like to allow user to quickly reset values in FairSharingStatus if they're not happy with the current state

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T10:34:44Z

- **pkg/cache/clusterqueue.go:569** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T10:34:44Z
```diff
@@ -566,6 +566,8 @@ func (c *clusterQueue) flavorInUse(flavor kueue.ResourceFlavorReference) bool {
 
 func (q *LocalQueue) resetFlavorsAndResources(cqUsage resources.FlavorResourceQuantities, cqAdmittedUsage resources.FlavorResourceQuantities) {
 	// Clean up removed flavors or resources.
+	q.Lock()
```

  It's part of this feature, as I introduce mutex in the LQ object, before it wasn't the case

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T11:36:26Z

- **pkg/cache/clusterqueue.go:569** by [@mimowo](https://github.com/mimowo) — 2025-05-06T11:36:26Z
```diff
@@ -566,6 +566,8 @@ func (c *clusterQueue) flavorInUse(flavor kueue.ResourceFlavorReference) bool {
 
 func (q *LocalQueue) resetFlavorsAndResources(cqUsage resources.FlavorResourceQuantities, cqAdmittedUsage resources.FlavorResourceQuantities) {
 	// Clean up removed flavors or resources.
+	q.Lock()
```

  so before it wasn't necessary? Why is it necessary now?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:10:53Z

- **pkg/controller/core/localqueue_controller.go:185** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:10:53Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
+		if err := r.initializeAdmissionFsStatus(ctx, &lq); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		recheckAfter := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time)
+		if recheckAfter < r.admissionFSConfig.UsageSamplingInterval.Duration {
+			return ctrl.Result{RequeueAfter: recheckAfter}, nil
```

  That's a good catch, it should be `interval-recheckAfter`, I'll fix

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:22:48Z

- **pkg/controller/core/localqueue_controller.go:179** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:22:48Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
```

  I'd rather do it in a follow-up. We need to have it initialized before we compare it with the interval

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T12:27:34Z

- **pkg/controller/core/localqueue_controller.go:179** by [@mimowo](https://github.com/mimowo) — 2025-05-06T12:27:34Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
```

  sgtm, it is just a performance improvement. Though it is typically a good practice / idiomatic to collect all updates to a resource, and send the patch at the end of reconcile

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:34:33Z

- **pkg/controller/core/localqueue_controller.go:170** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:34:33Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
```

  It's necessary, because otherwise the new logic would be never executed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:35:45Z

- **apis/config/v1beta1/configuration_types.go:486** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:35:45Z
```diff
@@ -472,3 +476,17 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0.
+	UsageHalfLifeTime metav1.Duration `json:"usageHalfLifeTime,omitempty"`
+
+	// usageSamplingInterval indicates how often Kueue updates consumedResources in FairSharingStatus
+	UsageSamplingInterval metav1.Duration `json:"usageSamplingInterval,omitempty"`
```

  The default is 5mins, added a comment

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:36:34Z

- **apis/kueue/v1beta1/fairsharing_types.go:81** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:36:34Z
```diff
@@ -49,4 +53,30 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
+}
+
+type AdmissionMode string
+
+const (
+	// FairSharing based on usage, with QueuingStrategy as defined in CQ.
+	UsageBasedFairSharing AdmissionMode = "UsageBasedFairSharing"
+
+	NoFairSharing AdmissionMode = "NoFairSharing"
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:36:45Z

- **apis/kueue/v1beta1/localqueue_types.go:53** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:36:45Z
```diff
@@ -48,6 +48,12 @@ type LocalQueueSpec struct {
 	// +kubebuilder:validation:Enum=None;Hold;HoldAndDrain
 	// +kubebuilder:default="None"
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
+
+	// fairSharing defines the properties of the LocalQueue when
+	// participating in FairSharing.  The values are only relevant
```

  Improved

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:36:53Z

- **pkg/queue/cluster_queue_test.go:158** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:36:53Z
```diff
@@ -149,7 +155,7 @@ func Test_PushOrUpdate(t *testing.T) {
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			cq := newClusterQueueImpl(defaultOrdering, fakeClock)
+			cq := newClusterQueueImpl(context.TODO(), nil, defaultOrdering, fakeClock, nil, false)
```

  Done, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:39:20Z

- **pkg/cache/clusterqueue.go:569** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:39:19Z
```diff
@@ -566,6 +566,8 @@ func (c *clusterQueue) flavorInUse(flavor kueue.ResourceFlavorReference) bool {
 
 func (q *LocalQueue) resetFlavorsAndResources(cqUsage resources.FlavorResourceQuantities, cqAdmittedUsage resources.FlavorResourceQuantities) {
 	// Clean up removed flavors or resources.
+	q.Lock()
```

  Because now we rely on cached usage per LQ to calculate `ConsumedUsage` - we use `LocalQueue.admittedUsage` in LQ controller. Hence we need synchronization mechanism to prevent data races

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:39:25Z

- **pkg/workload/workload.go:297** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:39:25Z
```diff
@@ -282,6 +282,28 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: string(i.Obj.Spec.QueueName)}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := 0.0
+	for resName, resVal := range lq.Status.FairSharingStatus.AdmissionFairSharingStatus.ConsumedResources {
+		if weight, found := resWeights[resName]; found {
+			usage += weight * resVal.AsApproximateFloat64()
+		} else {
+			// if no weight for resource was defined, use default weight of 1
+			usage += resVal.AsApproximateFloat64()
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:39:33Z

- **pkg/controller/core/localqueue_controller.go:108** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:39:33Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
```

  Reverted

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:39:39Z

- **pkg/controller/core/localqueue_controller.go:185** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:39:39Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
+		if err := r.initializeAdmissionFsStatus(ctx, &lq); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		recheckAfter := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time)
+		if recheckAfter < r.admissionFSConfig.UsageSamplingInterval.Duration {
+			return ctrl.Result{RequeueAfter: recheckAfter}, nil
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T12:40:09Z

- **pkg/controller/core/localqueue_controller.go:179** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T12:40:09Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
```

  Will do in a follow-up then

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T12:48:38Z

- **pkg/cache/clusterqueue.go:569** by [@mimowo](https://github.com/mimowo) — 2025-05-06T12:48:38Z
```diff
@@ -566,6 +566,8 @@ func (c *clusterQueue) flavorInUse(flavor kueue.ResourceFlavorReference) bool {
 
 func (q *LocalQueue) resetFlavorsAndResources(cqUsage resources.FlavorResourceQuantities, cqAdmittedUsage resources.FlavorResourceQuantities) {
 	// Clean up removed flavors or resources.
+	q.Lock()
```

  is this function called often? Asking cause I'm wondering if this could result in perf regression and if should be don conditionally (only if the  AdmissionFairSharing) used.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T13:16:22Z

- **pkg/cache/clusterqueue.go:569** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T13:16:22Z
```diff
@@ -566,6 +566,8 @@ func (c *clusterQueue) flavorInUse(flavor kueue.ResourceFlavorReference) bool {
 
 func (q *LocalQueue) resetFlavorsAndResources(cqUsage resources.FlavorResourceQuantities, cqAdmittedUsage resources.FlavorResourceQuantities) {
 	// Clean up removed flavors or resources.
+	q.Lock()
```

  `resetFlavorsAndResources` is called only on Create/Update for LQ and related CQ so I wouldn't anticipate much regression

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T13:28:02Z

- **pkg/cache/clusterqueue.go:569** by [@mimowo](https://github.com/mimowo) — 2025-05-06T13:28:02Z
```diff
@@ -566,6 +566,8 @@ func (c *clusterQueue) flavorInUse(flavor kueue.ResourceFlavorReference) bool {
 
 func (q *LocalQueue) resetFlavorsAndResources(cqUsage resources.FlavorResourceQuantities, cqAdmittedUsage resources.FlavorResourceQuantities) {
 	// Clean up removed flavors or resources.
+	q.Lock()
```

  sgtm, thanks, this is totally acceptable

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T14:08:21Z

- **pkg/controller/core/localqueue_controller.go:185** by [@mimowo](https://github.com/mimowo) — 2025-05-06T14:08:21Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
+		if err := r.initializeAdmissionFsStatus(ctx, &lq); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		recheckAfter := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time)
+		if recheckAfter < r.admissionFSConfig.UsageSamplingInterval.Duration {
+			return ctrl.Result{RequeueAfter: recheckAfter}, nil
```

  is there a test added to catch that bug? I would like to add such a test case, but it can be a follow up if tested manually

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T14:43:39Z

- **apis/config/v1beta1/configuration_types.go:482** by [@mimowo](https://github.com/mimowo) — 2025-05-06T14:43:38Z
```diff
@@ -472,3 +476,17 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0.
```

  > Yes, we would like to allow user to quickly reset values in FairSharingStatus if they're not happy with the current state
  
  I don't understand this use case actually, because if they set to 0, then it will be reset all the time, without user intervention, so I'm wondering how this can be useful.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-06T14:46:29Z

/lgtm

minus one last comment

- **cmd/kueue/main.go:220** by [@gabesaba](https://github.com/gabesaba) — 2025-05-06T14:45:17Z
```diff
@@ -217,6 +217,10 @@ func main() {
 	}
 	if cfg.FairSharing != nil {
 		cacheOptions = append(cacheOptions, cache.WithFairSharing(cfg.FairSharing.Enable))
+		queueOptions = append(queueOptions, queue.WithFairSharing(cfg.FairSharing))
```

  is this used?

- **pkg/queue/manager.go:50** by [@gabesaba](https://github.com/gabesaba) — 2025-05-06T14:45:54Z
```diff
@@ -47,6 +47,8 @@ var (
 type options struct {
 	podsReadyRequeuingTimestamp config.RequeuingTimestamp
 	workloadInfoOptions         []workload.InfoOption
+	fairSharing                 *config.FairSharing
```

  same as above comment - is this used?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T14:51:03Z

- **apis/config/v1beta1/configuration_types.go:99** by [@mimowo](https://github.com/mimowo) — 2025-05-06T14:51:02Z
```diff
@@ -95,6 +95,10 @@ type Configuration struct {
 	// FairSharing controls the Fair Sharing semantics across the cluster.
 	FairSharing *FairSharing `json:"fairSharing,omitempty"`
 
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
```

  ```suggestion
  ```
  Drop the optional, I think it is no-op for the global configuration, we don't set it elsewhere.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T14:51:23Z

- **apis/config/v1beta1/configuration_types.go:482** by [@mimowo](https://github.com/mimowo) — 2025-05-06T14:51:23Z
```diff
@@ -472,3 +476,18 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0.
```

  ```suggestion
  	// If set to 0, usage will be reset to 0 immediately.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T14:52:24Z

- **apis/kueue/v1beta1/fairsharing_types.go:82** by [@mimowo](https://github.com/mimowo) — 2025-05-06T14:52:24Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
+}
+
+type AdmissionMode string
+
+const (
+	// AdmissionFairSharing based on usage, with QueuingStrategy as defined in CQ.
+	UsageBasedAdmissionFairSharing AdmissionMode = "UsageBasedAdmissionFairSharing"
+
+	// AdmissionFairSharing is disabled for this CQ
+	NoAdmissionFairSharing AdmissionMode = "NoFairSharing"
```

  ```suggestion
  	NoAdmissionFairSharing AdmissionMode = "NoAdmissionFairSharing"
  ```
  Suggestion  for consistency of of the const name and the string

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T14:59:38Z

- **cmd/kueue/main.go:220** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T14:59:38Z
```diff
@@ -217,6 +217,10 @@ func main() {
 	}
 	if cfg.FairSharing != nil {
 		cacheOptions = append(cacheOptions, cache.WithFairSharing(cfg.FairSharing.Enable))
+		queueOptions = append(queueOptions, queue.WithFairSharing(cfg.FairSharing))
```

  Good point, after changes to the configuration API this is no longer used, will fix

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T14:59:42Z

- **pkg/queue/manager.go:50** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T14:59:42Z
```diff
@@ -47,6 +47,8 @@ var (
 type options struct {
 	podsReadyRequeuingTimestamp config.RequeuingTimestamp
 	workloadInfoOptions         []workload.InfoOption
+	fairSharing                 *config.FairSharing
```

  ditto

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T15:06:38Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-05-06T15:06:37Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  ```suggestion
  	FairSharing FairSharingStatus `json:"fairSharing,omitempty"`
  ```
  To make it consistent with CQ: https://github.com/kubernetes-sigs/kueue/pull/4687/files#diff-fa7799c26041d219ab624bc2069198280537236008ff41d199593618b900dcb9L314
  Wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T15:07:17Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-05-06T15:07:17Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  Also, I think this should be *FairSharingStatus, we only add new objects to the existing API which via pointers.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T15:10:09Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-05-06T15:10:08Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  For reference: https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api_changes.md#new-field-in-existing-api-version

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:23:07Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:23:07Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  Should the link redirect me to something specific? Either way, I'm fine with changing that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:27:44Z

- **cmd/kueue/main.go:220** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:27:44Z
```diff
@@ -217,6 +217,10 @@ func main() {
 	}
 	if cfg.FairSharing != nil {
 		cacheOptions = append(cacheOptions, cache.WithFairSharing(cfg.FairSharing.Enable))
+		queueOptions = append(queueOptions, queue.WithFairSharing(cfg.FairSharing))
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:27:51Z

- **pkg/queue/manager.go:50** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:27:51Z
```diff
@@ -47,6 +47,8 @@ var (
 type options struct {
 	podsReadyRequeuingTimestamp config.RequeuingTimestamp
 	workloadInfoOptions         []workload.InfoOption
+	fairSharing                 *config.FairSharing
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:27:58Z

- **apis/config/v1beta1/configuration_types.go:99** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:27:58Z
```diff
@@ -95,6 +95,10 @@ type Configuration struct {
 	// FairSharing controls the Fair Sharing semantics across the cluster.
 	FairSharing *FairSharing `json:"fairSharing,omitempty"`
 
+	// admissionFairSharing indicates configuration of FairSharing with the `AdmissionTime` mode on
+	// +optional
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:28:06Z

- **apis/config/v1beta1/configuration_types.go:482** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:28:06Z
```diff
@@ -472,3 +476,18 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:28:14Z

- **apis/kueue/v1beta1/fairsharing_types.go:82** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:28:14Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
+}
+
+type AdmissionMode string
+
+const (
+	// AdmissionFairSharing based on usage, with QueuingStrategy as defined in CQ.
+	UsageBasedAdmissionFairSharing AdmissionMode = "UsageBasedAdmissionFairSharing"
+
+	// AdmissionFairSharing is disabled for this CQ
+	NoAdmissionFairSharing AdmissionMode = "NoFairSharing"
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:28:23Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:28:22Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:29:28Z

- **pkg/controller/core/localqueue_controller.go:185** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:29:28Z
```diff
@@ -105,34 +145,54 @@ func (r *LocalQueueReconciler) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=localqueues/finalizers,verbs=update
 
 func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var queueObj kueue.LocalQueue
-	if err := r.client.Get(ctx, req.NamespacedName, &queueObj); err != nil {
+	var lq kueue.LocalQueue
+	if err := r.client.Get(ctx, req.NamespacedName, &lq); err != nil {
 		// we'll ignore not-found errors, since there is nothing to do.
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile LocalQueue")
 
-	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
-		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
+	if ptr.Deref(lq.Spec.StopPolicy, kueue.None) != kueue.None {
+		err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, StoppedReason, localQueueIsInactiveMsg)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(lq.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
-			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
+			err = r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &lq, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-	err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg)
-	return ctrl.Result{}, client.IgnoreNotFound(err)
+
+	if r.admissionFSConfig != nil {
+		if err := r.initializeAdmissionFsStatus(ctx, &lq); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		recheckAfter := r.clock.Now().Sub(lq.Status.FairSharingStatus.AdmissionFairSharingStatus.LastUpdate.Time)
+		if recheckAfter < r.admissionFSConfig.UsageSamplingInterval.Duration {
+			return ctrl.Result{RequeueAfter: recheckAfter}, nil
```

  Yes, a unit test has been modified here: https://github.com/kubernetes-sigs/kueue/pull/4687/commits/33618a237db1e37910eb13ae1116fe0a688612f9

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T15:31:30Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-05-06T15:31:30Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  I think this is not adressed fully

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T15:32:06Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-05-06T15:32:06Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  ```
  Add the field to the API type:
  
  ensure the field is optional
  add the omitempty struct tag
  add the // +optional comment tag
  add the // +featureGate=<gate-name> comment tag
  ensure the field is entirely absent from API responses when empty (optional fields must be pointers)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T15:32:35Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@mimowo](https://github.com/mimowo) — 2025-05-06T15:32:35Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  So, IIUC, new fields should always be ptrs, with omitempty. Otherwise I think a newly generated go-client may have issues reading the old status, which is the same version of the API (v1beta1)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:34:29Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:34:29Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  Oh I had a stale version of your comment, will fix

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-06T15:42:53Z

- **apis/kueue/v1beta1/localqueue_types.go:161** by [@PBundyra](https://github.com/PBundyra) — 2025-05-06T15:42:53Z
```diff
@@ -149,6 +155,10 @@ type LocalQueueStatus struct {
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
 	Flavors []LocalQueueFlavorStatus `json:"flavors,omitempty"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharingStatus FairSharingStatus `json:"fairSharingStatus,omitempty"`
```

  Addressed

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-07T08:27:10Z

- **pkg/controller/core/localqueue_controller.go:174** by [@gabesaba](https://github.com/gabesaba) — 2025-05-07T08:18:14Z
```diff
@@ -120,19 +158,39 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
 			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
```

  Double checking: do we want to handle admissionFS logic when !Active? I thought (may be mistaken) that a previous version of this PR exited from function in this case, regardless of whether an error was returned here

- **apis/config/v1beta1/configuration_types.go:488** by [@gabesaba](https://github.com/gabesaba) — 2025-05-07T08:19:20Z
```diff
@@ -472,3 +475,18 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0 immediately.
+	UsageHalfLifeTime metav1.Duration `json:"usageHalfLifeTime,omitempty"`
+
+	// usageSamplingInterval indicates how often Kueue updates consumedResources in FairSharingStatus
+	// Defaults to 5min.
+	UsageSamplingInterval metav1.Duration `json:"usageSamplingInterval,omitempty"`
+
+	// resourceWeights assigns weights to resources which then are used to calculate LocalQueue/ClusterQueue/Cohort's
```

  does this only apply to LQ for now? if so, I would wait until we extend this so that the documentation doesn't confuse user

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T08:53:10Z

- **pkg/controller/core/localqueue_controller.go:174** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T08:53:10Z
```diff
@@ -120,19 +158,39 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
 			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
```

  Good catch, we probably don't want to update the usage value it the LQ is inactive, will fix

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T08:56:38Z

- **apis/config/v1beta1/configuration_types.go:488** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T08:56:38Z
```diff
@@ -472,3 +475,18 @@ type FairSharing struct {
 	// The default strategy is ["LessThanOrEqualToFinalShare", "LessThanInitialShare"].
 	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
 }
+
+type AdmissionFairSharing struct {
+	// usageHalfLifeTime indicates the time after which the current usage will decay by a half
+	// If set to 0, usage will be reset to 0 immediately.
+	UsageHalfLifeTime metav1.Duration `json:"usageHalfLifeTime,omitempty"`
+
+	// usageSamplingInterval indicates how often Kueue updates consumedResources in FairSharingStatus
+	// Defaults to 5min.
+	UsageSamplingInterval metav1.Duration `json:"usageSamplingInterval,omitempty"`
+
+	// resourceWeights assigns weights to resources which then are used to calculate LocalQueue/ClusterQueue/Cohort's
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T08:56:50Z

- **pkg/controller/core/localqueue_controller.go:174** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T08:56:50Z
```diff
@@ -120,19 +158,39 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	}
 
 	var cq kueue.ClusterQueue
-	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
-	if err != nil {
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq); err != nil {
 		if apierrors.IsNotFound(err) {
 			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", clusterQueueIsInactiveMsg)
 		}
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
-		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue")
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to localQueue"); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	} else {
+		if err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, clusterQueueIsInactiveReason, clusterQueueIsInactiveMsg); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T13:18:34Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:446** by [@mimowo](https://github.com/mimowo) — 2025-05-07T13:18:33Z
```diff
@@ -392,6 +393,78 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectReservingActiveWorkloadsMetric(llmQueue, 4)
 		})
 	})
+
+	ginkgo.When("Using AdmissionFairSharing", func() {
+		var (
+			cq  *kueue.ClusterQueue
+			lqA *kueue.LocalQueue
+			lqB *kueue.LocalQueue
+			lqC *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "32").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{WithinClusterQueue: kueue.PreemptionPolicyNever}).
+				QueueingStrategy(kueue.StrictFIFO).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA = testing.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB = testing.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqC = testing.MakeLocalQueue("lq-c", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			lqs = append(lqs, lqB)
+			lqs = append(lqs, lqC)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+			util.MustCreate(ctx, k8sClient, lqC)
+		})
+
+		ginkgo.It("should promote a workload from LQ with lower recent usage", func() {
+			ginkgo.By("Creating a workload")
+			wl := createWorkload("lq-a", "32")
+
+			ginkgo.By("Admitting the workload")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(cq, 1)
+
+			ginkgo.By("Checking that LQ's resource usage is updated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), lqA)).Should(gomega.Succeed())
+					g.Expect(lqA.Status.FairSharing).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources).Should(gomega.HaveLen(1))
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources[corev1.ResourceCPU]).
```

  It seems this has a potential to flake, as the feature is time-based, and we check exact=32. Does it only pass if the check happens within 250ms? If so, I would suggest checking it is >0. This will prove it is updated, we can check exact values in the unit tests where we control the clock.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T13:28:40Z

- **pkg/queue/cluster_queue.go:104** by [@mimowo](https://github.com/mimowo) — 2025-05-07T13:28:16Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  ```suggestion
  func afsResourceWeights(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
  ```
  if is a bit weird to call function "is" when returning more than bool. The bool probably should be the second argument, as more idiomatic (as golang returns "ok" bool as second when checking maps).

- **pkg/queue/cluster_queue.go:104** by [@mimowo](https://github.com/mimowo) — 2025-05-07T13:28:40Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  ```suggestion
  func afsResourceWeights(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (map[corev1.ResourceName]float64, bool) {
  ```
  if is a bit weird to call function "is" when returning more than bool. The bool probably should be the second argument, as more idiomatic (as golang returns "ok" bool as second when checking maps).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T13:31:05Z

- **pkg/queue/cluster_queue.go:190** by [@mimowo](https://github.com/mimowo) — 2025-05-07T13:31:05Z
```diff
@@ -171,6 +183,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 	c.heap.PushOrUpdate(wInfo)
 }
 
+func (c *ClusterQueue) Heapify(lqName string) {
+	for _, wl := range c.heap.List() {
```

  Hm, this should probably take a lock on the CQ, otherwise elements may be added / returned while iterating over the heap, no?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T14:40:59Z

- **pkg/queue/cluster_queue.go:104** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T14:40:59Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T14:42:07Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:446** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T14:42:07Z
```diff
@@ -392,6 +393,78 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectReservingActiveWorkloadsMetric(llmQueue, 4)
 		})
 	})
+
+	ginkgo.When("Using AdmissionFairSharing", func() {
+		var (
+			cq  *kueue.ClusterQueue
+			lqA *kueue.LocalQueue
+			lqB *kueue.LocalQueue
+			lqC *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "32").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{WithinClusterQueue: kueue.PreemptionPolicyNever}).
+				QueueingStrategy(kueue.StrictFIFO).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA = testing.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB = testing.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqC = testing.MakeLocalQueue("lq-c", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			lqs = append(lqs, lqB)
+			lqs = append(lqs, lqC)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+			util.MustCreate(ctx, k8sClient, lqC)
+		})
+
+		ginkgo.It("should promote a workload from LQ with lower recent usage", func() {
+			ginkgo.By("Creating a workload")
+			wl := createWorkload("lq-a", "32")
+
+			ginkgo.By("Admitting the workload")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(cq, 1)
+
+			ginkgo.By("Checking that LQ's resource usage is updated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), lqA)).Should(gomega.Succeed())
+					g.Expect(lqA.Status.FairSharing).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources).Should(gomega.HaveLen(1))
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources[corev1.ResourceCPU]).
```

  Given the short halflife and interval the usage should reach 32 in ~1.5s, but I agree it still might be flaky if the controller chokes. Addressed according to your suggestion

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T14:42:32Z

- **pkg/queue/cluster_queue.go:104** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T14:42:32Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  Ditto

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T14:45:23Z

- **pkg/queue/cluster_queue.go:190** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T14:45:23Z
```diff
@@ -171,6 +183,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 	c.heap.PushOrUpdate(wInfo)
 }
 
+func (c *ClusterQueue) Heapify(lqName string) {
+	for _, wl := range c.heap.List() {
```

  Added a lock, however this function already locks the mutex on Manager before it's called. I agree with your suggestion, however let's consider in the future if we aren't a bit too conservative about locks given we have them both in Manager and CQ objects. We can maybe decrease the number of locks in the whole file in a follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T14:52:49Z

- **pkg/queue/cluster_queue.go:190** by [@mimowo](https://github.com/mimowo) — 2025-05-07T14:52:49Z
```diff
@@ -171,6 +183,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 	c.heap.PushOrUpdate(wInfo)
 }
 
+func (c *ClusterQueue) Heapify(lqName string) {
+	for _, wl := range c.heap.List() {
```

  > Added a lock, however this function already locks the mutex on Manager before it's called. 
  
  The question is if every access to the CQ goes via the manager, I remember analyzing the code in the past, and there were accesses bypassing the manager, but I'm not fully sure at the moment.
  
  > We can maybe decrease the number of locks in the whole file in a follow up
  
  maybe there is room for improvement, I cannot recall exactly the reason we introduced 2 levels of locking, most likely there were (maybe still are) some functions bypassing the manager.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T14:56:52Z

- **pkg/queue/cluster_queue.go:190** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T14:56:52Z
```diff
@@ -171,6 +183,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 	c.heap.PushOrUpdate(wInfo)
 }
 
+func (c *ClusterQueue) Heapify(lqName string) {
+	for _, wl := range c.heap.List() {
```

  I agree, it requires some little investigation. I believe it's out of scope for this PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T16:03:50Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:461** by [@mimowo](https://github.com/mimowo) — 2025-05-07T16:03:50Z
```diff
@@ -392,6 +393,78 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectReservingActiveWorkloadsMetric(llmQueue, 4)
 		})
 	})
+
+	ginkgo.When("Using AdmissionFairSharing", func() {
+		var (
+			cq  *kueue.ClusterQueue
+			lqA *kueue.LocalQueue
+			lqB *kueue.LocalQueue
+			lqC *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "32").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{WithinClusterQueue: kueue.PreemptionPolicyNever}).
+				QueueingStrategy(kueue.StrictFIFO).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA = testing.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB = testing.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqC = testing.MakeLocalQueue("lq-c", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			lqs = append(lqs, lqB)
+			lqs = append(lqs, lqC)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+			util.MustCreate(ctx, k8sClient, lqC)
+		})
+
+		ginkgo.It("should promote a workload from LQ with lower recent usage", func() {
+			ginkgo.By("Creating a workload")
+			wl := createWorkload("lq-a", "32")
+
+			ginkgo.By("Admitting the workload")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(cq, 1)
+
+			ginkgo.By("Checking that LQ's resource usage is updated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), lqA)).Should(gomega.Succeed())
+					g.Expect(lqA.Status.FairSharing).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources).Should(gomega.HaveLen(1))
+					usage := lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources[corev1.ResourceCPU]
+					g.Expect(usage.MilliValue()).To(gomega.BeNumerically(">=", 0))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Creating two pending workloads")
+			wlA := createWorkload("lq-a", "32")
+			wlB := createWorkload("lq-b", "32")
+
+			ginkgo.By("Finish the previous workload", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					util.FinishWorkloads(ctx, k8sClient, wl)
+					updatedCQ := &kueue.ClusterQueue{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), updatedCQ)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
```

  I don't understand the intention of the assert. The FinishWorkloads inside Consistently looks weird, does it actually make sense? Please add some comment or improve the step description to clarify the intention.
  
  EDIT: isn't it enough to just call `util.FinishWorkloads(ctx, k8sClient, wl)`, then it would match the description, but again not sure what is the intention here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T16:07:28Z

- **pkg/queue/cluster_queue.go:104** by [@mimowo](https://github.com/mimowo) — 2025-05-07T16:07:27Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  > The bool probably should be the second argument, as more idiomatic (as golang returns "ok" bool as second when checking maps).
  
  this is not addressed I think

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T16:09:30Z

- **pkg/controller/core/localqueue_controller.go:76** by [@mimowo](https://github.com/mimowo) — 2025-05-07T16:09:30Z
```diff
@@ -60,13 +63,41 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	admissionFSConfig *config.AdmissionFairSharing
+	clock             clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithAdmissionFairSharingConfig(cfg *config.AdmissionFairSharing) LocalQueueReconcilerOption {
+	return func(o *LocalQueueReconcilerOptions) {
+		if cfg != nil {
```

  why the `if`? This looks suspicious

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T16:09:49Z

- **pkg/queue/cluster_queue.go:104** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T16:09:49Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  Can you double check if you looking at the latest changes?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T16:11:11Z

- **pkg/queue/cluster_queue.go:104** by [@mimowo](https://github.com/mimowo) — 2025-05-07T16:11:11Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  here: https://github.com/kubernetes-sigs/kueue/pull/4687/files#diff-996d1d16eaefed60407545849893ea41fac4b85c04b78bb396bb93fcf6004bc8R104

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T16:12:37Z

- **pkg/queue/cluster_queue.go:104** by [@mimowo](https://github.com/mimowo) — 2025-05-07T16:12:34Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := isAdmissionFsEnabled(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func isAdmissionFsEnabled(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  ![image](https://github.com/user-attachments/assets/012018d5-ce2f-42d8-8d74-c2dfdf5e6673)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T16:14:58Z

- **pkg/queue/cluster_queue.go:106** by [@mimowo](https://github.com/mimowo) — 2025-05-07T16:14:58Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := afsResourceWeights(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func afsResourceWeights(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
+	enableAdmissionFs, fsResWeights := false, make(map[corev1.ResourceName]float64)
+	if fsConfig != nil && cq.Spec.AdmissionScope != nil && cq.Spec.AdmissionScope.AdmissionMode == kueue.UsageBasedAdmissionFairSharing {
```

  I would suggest to check the feature gate also here, otherwise it may get triggered, and the consequences aren't clear to me

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T16:24:46Z

- **pkg/queue/cluster_queue.go:444** by [@mimowo](https://github.com/mimowo) — 2025-05-07T16:24:46Z
```diff
@@ -410,8 +432,26 @@ func (c *ClusterQueue) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueR
 // to sort workloads. The function sorts workloads based on their priority.
 // When priorities are equal, it uses the workload's creation or eviction
 // time.
-func queueOrderingFunc(wo workload.Ordering) func(a, b *workload.Info) bool {
+func queueOrderingFunc(ctx context.Context, c client.Client, wo workload.Ordering, fsResWeights map[corev1.ResourceName]float64, enableAdmissionFs bool) func(a, b *workload.Info) bool {
+	log := ctrl.LoggerFrom(ctx)
+
 	return func(a, b *workload.Info) bool {
+		if enableAdmissionFs {
+			lqAUsage, errA := a.LqUsage(ctx, c, fsResWeights)
+			lqBUsage, errB := b.LqUsage(ctx, c, fsResWeights)
+			switch {
+			case errA != nil:
+				log.V(3).Error(errA, "Error fetching LocalQueue from informer")
```

  Actually this code does not know this comes from informer. For example we could change the impl of LqUsage to use cache, but we wouldn't remember to change the log. I would suggest to have a local function which logs and call it.
  
  something like:
  ```golang
  lqUsageOrLogError := func(...) usage {
     usage, err := a.LqUsage(ctx, c, fsResWeights)
     if err != nil {
        log.V(2).Error("Error determining LocalQueue usage", "localQueue", lq.Name)
     }
     return usage
  }
  ```
  also, errors should probably not be V(3).

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T16:56:18Z

- **pkg/queue/cluster_queue.go:444** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T16:56:17Z
```diff
@@ -410,8 +432,26 @@ func (c *ClusterQueue) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueR
 // to sort workloads. The function sorts workloads based on their priority.
 // When priorities are equal, it uses the workload's creation or eviction
 // time.
-func queueOrderingFunc(wo workload.Ordering) func(a, b *workload.Info) bool {
+func queueOrderingFunc(ctx context.Context, c client.Client, wo workload.Ordering, fsResWeights map[corev1.ResourceName]float64, enableAdmissionFs bool) func(a, b *workload.Info) bool {
+	log := ctrl.LoggerFrom(ctx)
+
 	return func(a, b *workload.Info) bool {
+		if enableAdmissionFs {
+			lqAUsage, errA := a.LqUsage(ctx, c, fsResWeights)
+			lqBUsage, errB := b.LqUsage(ctx, c, fsResWeights)
+			switch {
+			case errA != nil:
+				log.V(3).Error(errA, "Error fetching LocalQueue from informer")
```

  I'll fix the message and the depth of the log but the local functions seems like a little overkill to me. I need to have information about the error not only for logging but also to decide if I can compare usages

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T16:58:21Z

- **pkg/queue/cluster_queue.go:106** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T16:58:21Z
```diff
@@ -89,17 +91,27 @@ func workloadKey(i *workload.Info) string {
 	return workload.Key(i.Obj)
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueue, error) {
-	cqImpl := newClusterQueueImpl(wo, realClock)
+func newClusterQueue(ctx context.Context, client client.Client, cq *kueue.ClusterQueue, wo workload.Ordering, fsConfig *config.AdmissionFairSharing) (*ClusterQueue, error) {
+	enableAdmissionFs, fsResWeights := afsResourceWeights(cq, fsConfig)
+	cqImpl := newClusterQueueImpl(ctx, client, wo, realClock, fsResWeights, enableAdmissionFs)
 	err := cqImpl.Update(cq)
 	if err != nil {
 		return nil, err
 	}
 	return cqImpl, nil
 }
 
-func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
-	lessFunc := queueOrderingFunc(wo)
+func afsResourceWeights(cq *kueue.ClusterQueue, fsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
+	enableAdmissionFs, fsResWeights := false, make(map[corev1.ResourceName]float64)
+	if fsConfig != nil && cq.Spec.AdmissionScope != nil && cq.Spec.AdmissionScope.AdmissionMode == kueue.UsageBasedAdmissionFairSharing {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T16:58:34Z

- **pkg/controller/core/localqueue_controller.go:76** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T16:58:34Z
```diff
@@ -60,13 +63,41 @@ const (
 	clusterQueueIsInactiveReason = "ClusterQueueIsInactive"
 )
 
+type LocalQueueReconcilerOptions struct {
+	admissionFSConfig *config.AdmissionFairSharing
+	clock             clock.Clock
+}
+
+// LocalQueueReconcilerOption configures the reconciler.
+type LocalQueueReconcilerOption func(*LocalQueueReconcilerOptions)
+
+func WithAdmissionFairSharingConfig(cfg *config.AdmissionFairSharing) LocalQueueReconcilerOption {
+	return func(o *LocalQueueReconcilerOptions) {
+		if cfg != nil {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-07T16:59:00Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:461** by [@PBundyra](https://github.com/PBundyra) — 2025-05-07T16:59:00Z
```diff
@@ -392,6 +393,78 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectReservingActiveWorkloadsMetric(llmQueue, 4)
 		})
 	})
+
+	ginkgo.When("Using AdmissionFairSharing", func() {
+		var (
+			cq  *kueue.ClusterQueue
+			lqA *kueue.LocalQueue
+			lqB *kueue.LocalQueue
+			lqC *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas(defaultFlavor.Name).Resource(corev1.ResourceCPU, "32").Obj()).
+				Preemption(kueue.ClusterQueuePreemption{WithinClusterQueue: kueue.PreemptionPolicyNever}).
+				QueueingStrategy(kueue.StrictFIFO).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA = testing.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB = testing.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqC = testing.MakeLocalQueue("lq-c", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			lqs = append(lqs, lqB)
+			lqs = append(lqs, lqC)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+			util.MustCreate(ctx, k8sClient, lqC)
+		})
+
+		ginkgo.It("should promote a workload from LQ with lower recent usage", func() {
+			ginkgo.By("Creating a workload")
+			wl := createWorkload("lq-a", "32")
+
+			ginkgo.By("Admitting the workload")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+			util.ExpectReservingActiveWorkloadsMetric(cq, 1)
+
+			ginkgo.By("Checking that LQ's resource usage is updated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), lqA)).Should(gomega.Succeed())
+					g.Expect(lqA.Status.FairSharing).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus).ShouldNot(gomega.BeNil())
+					g.Expect(lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources).Should(gomega.HaveLen(1))
+					usage := lqA.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources[corev1.ResourceCPU]
+					g.Expect(usage.MilliValue()).To(gomega.BeNumerically(">=", 0))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Creating two pending workloads")
+			wlA := createWorkload("lq-a", "32")
+			wlB := createWorkload("lq-b", "32")
+
+			ginkgo.By("Finish the previous workload", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					util.FinishWorkloads(ctx, k8sClient, wl)
+					updatedCQ := &kueue.ClusterQueue{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), updatedCQ)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
```

  Absolutely, I think this is some legacy from the previous version of the test, thanks for catching. Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T17:11:58Z

- **pkg/queue/cluster_queue.go:444** by [@mimowo](https://github.com/mimowo) — 2025-05-07T17:11:58Z
```diff
@@ -410,8 +432,26 @@ func (c *ClusterQueue) RequeueIfNotPresent(wInfo *workload.Info, reason RequeueR
 // to sort workloads. The function sorts workloads based on their priority.
 // When priorities are equal, it uses the workload's creation or eviction
 // time.
-func queueOrderingFunc(wo workload.Ordering) func(a, b *workload.Info) bool {
+func queueOrderingFunc(ctx context.Context, c client.Client, wo workload.Ordering, fsResWeights map[corev1.ResourceName]float64, enableAdmissionFs bool) func(a, b *workload.Info) bool {
+	log := ctrl.LoggerFrom(ctx)
+
 	return func(a, b *workload.Info) bool {
+		if enableAdmissionFs {
+			lqAUsage, errA := a.LqUsage(ctx, c, fsResWeights)
+			lqBUsage, errB := b.LqUsage(ctx, c, fsResWeights)
+			switch {
+			case errA != nil:
+				log.V(3).Error(errA, "Error fetching LocalQueue from informer")
```

  Ah, got it, so in case of errors you don't compare them at all, sounds reasonable. Ideally, the functions used in comparators should not return errors at all. 
  
  For example, we could retrieve the LqUsage before calling comparators and just store in memory for quick access.  We can leave it for a follow up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-07T22:49:44Z

- **apis/config/v1beta1/configuration_types.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:28:20Z
  @PBundyra sorry for the delayed review.
  Could we drop `omitempty` tags for required fields?

- **pkg/cache/cache.go:504** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:31:09Z
```diff
@@ -501,6 +501,19 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName kueue.ClusterQueueReference, lq *kueue.LocalQueue) (*LocalQueue, error) {
```

  @PBundyra can we add `TestCacheLocalQueueOperations` UTs in cache_test.go?

- **apis/kueue/v1beta1/fairsharing_types.go:68** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:32:58Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
```

  ```suggestion
  	ConsumedResources corev1.ResourceList `json:"consumedResources"`
  
  	// LastUpdate is the time when share and consumed resources were updated.
  	LastUpdate metav1.Time `json:"lastUpdate"`
  ```
  
  Those look like required fields.

- **apis/kueue/v1beta1/fairsharing_types.go:72** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:33:37Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
```

  ```suggestion
  	AdmissionMode AdmissionMode `json:"admissionMode"`
  ```
  
  Could you add API documentation here?

- **pkg/workload/workload.go:285** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:38:12Z
```diff
@@ -282,6 +282,27 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
```

  ```suggestion
  func (i *Info) LocalQueueUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
  ```
  
  This is the wide-scope function. So let's use fully sentence.

- **pkg/workload/workload.go:291** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:41:43Z
```diff
@@ -282,6 +282,27 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: string(i.Obj.Spec.QueueName)}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := 0.0
```

  ```suggestion
  	var usage float64
  ```
  
  Is this `usage` given to one significant figure?
  If not, we do not want to specify exactly one.

- **pkg/util/resource/resource.go:116** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:47:42Z
```diff
@@ -103,3 +103,14 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+// MulByFloat multiplies every element in q by f.
+// Leverages k8s.io/apimachinery/pkg/api/resource package to provide precision to 3 decimal places
+func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
+	ret := q.DeepCopy()
+	for k, v := range ret {
+		scaledV := float64(v.MilliValue()) * f
+		ret[k] = *resource.NewMilliQuantity(int64(scaledV), resource.DecimalSI)
+	}
+	return ret
+}
```

  Could we detect overflow and underflow?
  The k/k resource `Mul` accommodates only int values since we can avoid unexpected overflow and underflow.
  However, this does not have any guards.

- **apis/config/v1beta1/configuration_types.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T22:49:37Z
  Could you implement validations in https://github.com/PBundyra/kueue/blob/118ac5c9d83014c230f9af8579c443555522447d/pkg/config/validation.go?
  
  I guess that we need to add required validations.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-08T06:20:29Z

- **apis/kueue/v1beta1/fairsharing_types.go:72** by [@mimowo](https://github.com/mimowo) — 2025-05-08T06:20:29Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
```

  And we should probably add the +required tag, missed that.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T07:07:26Z

- **pkg/util/resource/resource.go:116** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T07:07:26Z
```diff
@@ -103,3 +103,14 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+// MulByFloat multiplies every element in q by f.
+// Leverages k8s.io/apimachinery/pkg/api/resource package to provide precision to 3 decimal places
+func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
+	ret := q.DeepCopy()
+	for k, v := range ret {
+		scaledV := float64(v.MilliValue()) * f
+		ret[k] = *resource.NewMilliQuantity(int64(scaledV), resource.DecimalSI)
+	}
+	return ret
+}
```

  The `f` multiplier is always in range [0,1] so the overflow won't occur. When it comes to underflow, go `float64` provides ~15 decimal digits precision, and this is definitely sufficient for our use case. It the underflow happens and the result will be rounded to 0, I believe it's WAI. Given that we have to convert it to `int64` eventually, we will still loose some precision, but I think we're good with it, as the feature itself doesn't need to be really precise, but rather present some notion of LQ's usage

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-08T09:37:24Z

- **apis/kueue/v1beta1/fairsharing_types.go:72** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-08T09:37:24Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
```

  Yes, that makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-08T09:42:51Z

- **pkg/util/resource/resource.go:116** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-08T09:42:51Z
```diff
@@ -103,3 +103,14 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+// MulByFloat multiplies every element in q by f.
+// Leverages k8s.io/apimachinery/pkg/api/resource package to provide precision to 3 decimal places
+func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
+	ret := q.DeepCopy()
+	for k, v := range ret {
+		scaledV := float64(v.MilliValue()) * f
+		ret[k] = *resource.NewMilliQuantity(int64(scaledV), resource.DecimalSI)
+	}
+	return ret
+}
```

  > The f multiplier is always in range [0,1] so the overflow won't occur.
  
  Yes, you're right. The key problem is this is exposed function and generic one. Actually, this function name does not imply that the input range should be [0, 1]. So, we can consider 1. adding validations for  input `f` or 2. adding just comments that this expects [0, 1], otherwise, it will occur unexpected overflow.
  
  I think 2 would be enough since 1 will decrease usability.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:51:56Z

- **apis/config/v1beta1/configuration_types.go:1** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:51:56Z
  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:52:01Z

- **pkg/cache/cache.go:504** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:52:01Z
```diff
@@ -501,6 +501,19 @@ func (c *Cache) DeleteLocalQueue(q *kueue.LocalQueue) {
 	cq.deleteLocalQueue(q)
 }
 
+func (c *Cache) GetCacheLocalQueue(cqName kueue.ClusterQueueReference, lq *kueue.LocalQueue) (*LocalQueue, error) {
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:52:10Z

- **apis/kueue/v1beta1/fairsharing_types.go:68** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:52:10Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:52:22Z

- **apis/kueue/v1beta1/fairsharing_types.go:72** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:52:22Z
```diff
@@ -49,4 +53,31 @@ type FairSharingStatus struct {
 	// weight of zero and is borrowing, this will return
 	// 9223372036854775807, the maximum possible share value.
 	WeightedShare int64 `json:"weightedShare"`
+
+	// admissionFairSharingStatus represents information relevant to the Admission Fair Sharing
+	AdmissionFairSharingStatus *AdmissionFairSharingStatus `json:"admissionFairSharingStatus,omitempty"`
 }
+
+type AdmissionFairSharingStatus struct {
+	// ConsumedResources represents the aggregated usage of resources over time,
+	// with decaying function applied.
+	// The value is populated if usage consumption functionality is enabled in Kueue config.
+	ConsumedResources corev1.ResourceList `json:"consumedResources,omitempty"`
+
+	// LastUpdate is the time when share and consumed resources were updated.
+	LastUpdate metav1.Time `json:"lastUpdate,omitempty"`
+}
+
+type AdmissionScope struct {
+	AdmissionMode AdmissionMode `json:"admissionMode,omitempty"`
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:52:30Z

- **pkg/workload/workload.go:291** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:52:30Z
```diff
@@ -282,6 +282,27 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+	var lq kueue.LocalQueue
+	lqKey := client.ObjectKey{Namespace: i.Obj.Namespace, Name: string(i.Obj.Spec.QueueName)}
+	if err := c.Get(ctx, lqKey, &lq); err != nil {
+		return 0, err
+	}
+	usage := 0.0
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:53:10Z

- **pkg/util/resource/resource.go:116** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:53:10Z
```diff
@@ -103,3 +103,14 @@ func QuantityToFloat(q *resource.Quantity) float64 {
 	}
 	return float64(q.MilliValue()) / 1000
 }
+
+// MulByFloat multiplies every element in q by f.
+// Leverages k8s.io/apimachinery/pkg/api/resource package to provide precision to 3 decimal places
+func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
+	ret := q.DeepCopy()
+	for k, v := range ret {
+		scaledV := float64(v.MilliValue()) * f
+		ret[k] = *resource.NewMilliQuantity(int64(scaledV), resource.DecimalSI)
+	}
+	return ret
+}
```

  Agree, added the comment

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:54:18Z

- **apis/config/v1beta1/configuration_types.go:1** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:54:18Z
  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-08T10:54:26Z

- **pkg/workload/workload.go:285** by [@PBundyra](https://github.com/PBundyra) — 2025-05-08T10:54:26Z
```diff
@@ -282,6 +282,27 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
+func (i *Info) LqUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
```

  Done
