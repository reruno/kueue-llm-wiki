# PR #4690: [Cleanup] Print created namespace in tests

**Summary**: [Cleanup] Print created namespace in tests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4690

**Last updated**: 2025-03-20T14:08:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/log-namespace-in-tests` → `main`
- **Created**: 2025-03-19T13:45:58Z
- **Updated**: 2025-03-20T14:08:02Z
- **Merged**: 2025-03-19T16:24:40Z
- **Closed**: 2025-03-19T16:24:40Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 72   **+158 / -278**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Seeing in what namespace is operating the given test, brings a debuggability advantage.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4654

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-19T13:46:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f8134fb382a784a1a53d2399013532bf252b98cc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67dad5be785a9e00072a3bca |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-03-19T13:46:59Z

@mbobrovskyi it's kind of a follow up on the MakeNamespace wrapper ;)

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-19T15:34:33Z

I will merge it after the 0.10.3 release is done

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-19T15:50:56Z

That is a fragment from the build-log.txt artifact: https://storage.googleapis.com/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/4690/pull-kueue-test-e2e-main-1-31/1902367847221301248/build-log.txt

```
[0mMetrics [38;5;243mwhen workload is admitted with admission checks [0m[1mshould ensure the admission check metrics are available[0m
[38;5;243m/home/prow/go/src/sigs.k8s.io/kueue/test/e2e/singlecluster/metrics_test.go:298[0m
  "level"=0 "msg"="Created namespace: e2e-metrics-75hqd"
  [1mSTEP:[0m Waiting for the curl-metrics pod to run. [38;5;243m@ 03/19/25 14:48:07.002[0m
  [1mSTEP:[0m setting the check as successful [38;5;243m@ 03/19/25 14:48:08.564[0m
  [1mSTEP:[0m checking that admission check metrics are available [38;5;243m@ 03/19/25 14:48:08.58[0m
  [1mSTEP:[0m deleting the cluster queue [38;5;243m@ 03/19/25 14:48:08.739[0m
  [1mSTEP:[0m checking that admission check metrics are no longer available [38;5;243m@ 03/19/25 14:48:09.273[0m
[38;5;10m• [5.090 seconds][0m
```
I think seeing the namespace for grepping will save some time.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-19T15:51:10Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-19T15:51:19Z

LGTM label has been added.  <details>Git tree hash: 9795287396b89c90343ff7d6a7254f7f630cf9b2</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-19T15:51:26Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4690#issuecomment-2737158163" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4690#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-19T13:59:48Z

- **test/util/e2e.go:325** by [@mimowo](https://github.com/mimowo) — 2025-03-19T13:59:48Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: nsName}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s", ns.Name))
+	return ns
+}
+
+func CreateNamespaceFromPrefixWithLog(ctx context.Context, k8sClient client.Client, nsPrefix string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: nsPrefix}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s (from prefix %s)", ns.Name, nsPrefix))
```

  Maybe use `ginkgo.GinkgoLogr.Info` (same for the other)?  We typically use ginkgo.By at the beginning of a step. This is just a summary. I think these lines would still be part of the build-log.log artifact (but please double check).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-19T14:03:35Z

- **test/util/e2e.go:325** by [@mimowo](https://github.com/mimowo) — 2025-03-19T14:03:35Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: nsName}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s", ns.Name))
+	return ns
+}
+
+func CreateNamespaceFromPrefixWithLog(ctx context.Context, k8sClient client.Client, nsPrefix string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: nsPrefix}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s (from prefix %s)", ns.Name, nsPrefix))
```

  ```suggestion
  	ginkgo.By(fmt.Sprintf("Created namespace: %s", ns.Name))
  ```
  I think logging the passed prefix feels like too much detail TBH

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-19T14:21:54Z

- **test/util/e2e.go:325** by [@mszadkow](https://github.com/mszadkow) — 2025-03-19T14:21:53Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: nsName}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s", ns.Name))
+	return ns
+}
+
+func CreateNamespaceFromPrefixWithLog(ctx context.Context, k8sClient client.Client, nsPrefix string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: nsPrefix}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s (from prefix %s)", ns.Name, nsPrefix))
```

  sure

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-19T14:34:57Z

- **test/util/e2e.go:325** by [@mszadkow](https://github.com/mszadkow) — 2025-03-19T14:34:57Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: nsName}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s", ns.Name))
+	return ns
+}
+
+func CreateNamespaceFromPrefixWithLog(ctx context.Context, k8sClient client.Client, nsPrefix string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: nsPrefix}}
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.By(fmt.Sprintf("Created namespace: %s (from prefix %s)", ns.Name, nsPrefix))
```

  Yes, we don't want it to be separate step I guess

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T13:20:22Z

- **test/util/e2e.go:316** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T13:19:39Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespace(nsName)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
```

  ```suggestion
  	gomega.ExpectWithOffset(k8sClient.Create(ctx, ns), 1).To(gomega.Succeed())
  ```
  
  @mszadkow Could you use offset?

- **test/util/e2e.go:324** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T13:20:02Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespace(nsName)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.GinkgoLogr.Info(fmt.Sprintf("Created namespace: %s", ns.Name))
+	return ns
+}
+
+func CreateNamespaceFromPrefixWithLog(ctx context.Context, k8sClient client.Client, nsPrefix string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespaceWithGenerateName(nsPrefix)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
```

  ```suggestion
  	gomega.ExpectWithOffset(k8sClient.Create(ctx, ns), 1).To(gomega.Succeed())
  ```
  
  ditto

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-20T13:28:17Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:1679** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-20T13:28:17Z
```diff
@@ -1673,7 +1671,13 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				Cohort(chName).
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
-			matchingNS = testing.MakeNamespaceWrapper("").GenerateName("foo-").Label("dep", "eng").Obj()
+			matchingNS = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "foo-",
+					Labels:       map[string]string{"dep": "eng"},
+				},
+			}
```

  Why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-20T13:30:47Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:1680** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-20T13:30:47Z
```diff
@@ -1673,7 +1671,13 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				Cohort(chName).
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
-			matchingNS = testing.MakeNamespaceWrapper("").GenerateName("foo-").Label("dep", "eng").Obj()
+			matchingNS = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "foo-",
+					Labels:       map[string]string{"dep": "eng"},
+				},
+			}
+			ginkgo.By(fmt.Sprintf("Creating namespace: %s", matchingNS.Name))
```

  This is namespace with generate name.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-03-20T13:34:38Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:1680** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-03-20T13:34:38Z
```diff
@@ -1673,7 +1671,13 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				Cohort(chName).
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
-			matchingNS = testing.MakeNamespaceWrapper("").GenerateName("foo-").Label("dep", "eng").Obj()
+			matchingNS = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "foo-",
+					Labels:       map[string]string{"dep": "eng"},
+				},
+			}
+			ginkgo.By(fmt.Sprintf("Creating namespace: %s", matchingNS.Name))
```

  For this case, can we create a similar function that propagates a namespace wrapper instead of a name?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-20T13:49:50Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:1680** by [@mimowo](https://github.com/mimowo) — 2025-03-20T13:49:50Z
```diff
@@ -1673,7 +1671,13 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				Cohort(chName).
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
-			matchingNS = testing.MakeNamespaceWrapper("").GenerateName("foo-").Label("dep", "eng").Obj()
+			matchingNS = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "foo-",
+					Labels:       map[string]string{"dep": "eng"},
+				},
+			}
+			ginkgo.By(fmt.Sprintf("Creating namespace: %s", matchingNS.Name))
```

  Yeah, we should either drop this step or adjust the name of step

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-20T13:51:43Z

- **test/util/e2e.go:324** by [@mszadkow](https://github.com/mszadkow) — 2025-03-20T13:51:42Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespace(nsName)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.GinkgoLogr.Info(fmt.Sprintf("Created namespace: %s", ns.Name))
+	return ns
+}
+
+func CreateNamespaceFromPrefixWithLog(ctx context.Context, k8sClient client.Client, nsPrefix string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespaceWithGenerateName(nsPrefix)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
```

  that's already done with ginkgo.GinkgoHelper()

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T13:55:44Z

- **test/util/e2e.go:324** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T13:55:43Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespace(nsName)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+	ginkgo.GinkgoLogr.Info(fmt.Sprintf("Created namespace: %s", ns.Name))
+	return ns
+}
+
+func CreateNamespaceFromPrefixWithLog(ctx context.Context, k8sClient client.Client, nsPrefix string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespaceWithGenerateName(nsPrefix)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
```

  Oh, I didn't know the `ginkgo.GinkgoHelper()` works on it.
  Thank you for describing it.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-20T14:00:31Z

- **test/util/e2e.go:316** by [@mszadkow](https://github.com/mszadkow) — 2025-03-20T14:00:31Z
```diff
@@ -308,3 +309,19 @@ func GetKuberayTestImage() string {
 	gomega.Expect(found).To(gomega.BeTrue())
 	return kuberayTestImage
 }
+
+func CreateNamespaceWithLog(ctx context.Context, k8sClient client.Client, nsName string) *corev1.Namespace {
+	ginkgo.GinkgoHelper()
+	ns := utiltesting.MakeNamespace(nsName)
+	gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
```

  ginkgo.GinkgoHelper() does that, at least that's how I understand it

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-20T14:07:48Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:1680** by [@mszadkow](https://github.com/mszadkow) — 2025-03-20T14:07:48Z
```diff
@@ -1673,7 +1671,13 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				Cohort(chName).
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
-			matchingNS = testing.MakeNamespaceWrapper("").GenerateName("foo-").Label("dep", "eng").Obj()
+			matchingNS = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "foo-",
+					Labels:       map[string]string{"dep": "eng"},
+				},
+			}
+			ginkgo.By(fmt.Sprintf("Creating namespace: %s", matchingNS.Name))
```

  Sure, that one slipped under my radar, after I have changed that in helper func

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-03-20T14:08:02Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:1680** by [@mszadkow](https://github.com/mszadkow) — 2025-03-20T14:08:02Z
```diff
@@ -1673,7 +1671,13 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				Cohort(chName).
 				Obj()
 			gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
-			matchingNS = testing.MakeNamespaceWrapper("").GenerateName("foo-").Label("dep", "eng").Obj()
+			matchingNS = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "foo-",
+					Labels:       map[string]string{"dep": "eng"},
+				},
+			}
+			ginkgo.By(fmt.Sprintf("Creating namespace: %s", matchingNS.Name))
```

  good idea
