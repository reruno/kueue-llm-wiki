# PR #4702: [FSxHC] Add Integration Test

**Summary**: [FSxHC] Add Integration Test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4702

**Last updated**: 2025-03-25T09:16:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `fs_integ` → `main`
- **Created**: 2025-03-20T10:09:18Z
- **Updated**: 2025-03-25T09:16:19Z
- **Merged**: 2025-03-21T09:36:32Z
- **Closed**: 2025-03-21T09:36:32Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo)
- **Changed files**: 1   **+71 / -1**

## Description

#### What type of PR is this?
Additional testing

#### What this PR does / why we need it:
Integration test for FairSharing x Hierarchical Cohorts, featuring scheduling and preemption

#### Which issue(s) this PR fixes:
Fixes #3759

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-20T10:10:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 973a92e581f05db88cf26131b0525a7be7d78bd8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67dc5b2fe1b7260008abb28d |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-21T09:01:02Z

LGTM label has been added.  <details>Git tree hash: 2f551bbb8f9cd4d5321d5bbf9cf80626bd48ff90</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-21T09:01:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4702#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4702#pullrequestreview-2705192597" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-21T09:19:53Z

Let's cherry-pick this to release-0.11
/cherry-pick release-0.11

cc: @mimowo

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-03-21T09:19:55Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `release-0.11` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4702#issuecomment-2742780936):

>Let's cherry-pick this to release-0.11
>/cherry-pick release-0.11
>
>cc: @mimowo 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-03-21T09:37:11Z

@tenzen-y: new pull request created: #4741

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4702#issuecomment-2742780936):

>Let's cherry-pick this to release-0.11
>/cherry-pick release-0.11
>
>cc: @mimowo 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T11:37:52Z

Just one nit.
Otherwise lgtm

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T11:16:05Z
```diff
@@ -66,15 +67,20 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		return cq
 	}
 
-	var createWorkload = func(queue string, cpuRequests string) *kueue.Workload {
+	var createWorkloadWithPriority = func(queue string, cpuRequests string, priority int32) *kueue.Workload {
 		wl := testing.MakeWorkloadWithGeneratedName("workload-", ns.Name).
+			Priority(priority).
 			Queue(queue).
 			Request(corev1.ResourceCPU, cpuRequests).Obj()
 		wls = append(wls, wl)
 		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 		return wl
 	}
 
+	var createWorkload = func(queue string, cpuRequests string) *kueue.Workload {
+		return createWorkloadWithPriority(queue, cpuRequests, 0)
```

  ```suggestion
  		return createWorkloadWithPriority(queue, cpuRequests, nil)
  ```
  
  Acutally, the Workload priority field is a pointer. Should we distinguish 0 and null?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T11:40:17Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:339** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T11:40:17Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
```

  ```suggestion
  			// weights. 12 CPUs are provided by the root
  ```
  
  Isn't this actually CPUs?
  
  > Request(corev1.ResourceCPU, cpuRequests).Obj()

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T11:44:58Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:351** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T11:44:58Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
+			// Cohort
+			//
+			//            root
+			//          /      \
+			//        /          \
+			// best-effort(0.5)  research
+			//   |               /   |    \
+			//   |              /    |     \
+			//   cq     chemistry  physics   llm(2.0)
+			//               |       |         |
+			//               cq
+			createCohort(testing.MakeCohort("root").ResourceGroup(*testing.MakeFlavorQuotas("default").Resource("cpu", "12").Obj()).Obj())
```

  ```suggestion
  			createCohort(testing.MakeCohort("root").ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "12").Obj()).Obj())
  ```
  
  nits

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T11:46:48Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:361** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T11:46:47Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
+			// Cohort
+			//
+			//            root
+			//          /      \
+			//        /          \
+			// best-effort(0.5)  research
+			//   |               /   |    \
+			//   |              /    |     \
+			//   cq     chemistry  physics   llm(2.0)
+			//               |       |         |
+			//               cq
+			createCohort(testing.MakeCohort("root").ResourceGroup(*testing.MakeFlavorQuotas("default").Resource("cpu", "12").Obj()).Obj())
+			createCohort(testing.MakeCohort("research").Parent("root").Obj())
+			createCohort(testing.MakeCohort("chemistry").Parent("research").Obj())
+			createCohort(testing.MakeCohort("physics").Parent("research").Obj())
+			createCohort(testing.MakeCohort("llm").FairWeight(resource.MustParse("2.0")).Parent("research").Obj())
+			createCohort(testing.MakeCohort("best-effort").FairWeight(resource.MustParse("0.5")).Parent("root").Obj())
+
+			preemptionPolicy := kueue.ClusterQueuePreemption{
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}
+			zeroQuota := *testing.MakeFlavorQuotas("default").Resource("cpu", "0").Obj()
```

  ```suggestion
  			zeroQuota := *testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "0").Obj()
  ```
  
  nits

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T11:46:57Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:361** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T11:46:56Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
+			// Cohort
+			//
+			//            root
+			//          /      \
+			//        /          \
+			// best-effort(0.5)  research
+			//   |               /   |    \
+			//   |              /    |     \
+			//   cq     chemistry  physics   llm(2.0)
+			//               |       |         |
+			//               cq
+			createCohort(testing.MakeCohort("root").ResourceGroup(*testing.MakeFlavorQuotas("default").Resource("cpu", "12").Obj()).Obj())
+			createCohort(testing.MakeCohort("research").Parent("root").Obj())
+			createCohort(testing.MakeCohort("chemistry").Parent("research").Obj())
+			createCohort(testing.MakeCohort("physics").Parent("research").Obj())
+			createCohort(testing.MakeCohort("llm").FairWeight(resource.MustParse("2.0")).Parent("research").Obj())
+			createCohort(testing.MakeCohort("best-effort").FairWeight(resource.MustParse("0.5")).Parent("root").Obj())
+
+			preemptionPolicy := kueue.ClusterQueuePreemption{
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}
+			zeroQuota := *testing.MakeFlavorQuotas("default").Resource("cpu", "0").Obj()
```

  ```suggestion
  			zeroQuota := *testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "0").Obj()
  ```
  
  nits

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-20T11:48:09Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:361** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-20T11:48:09Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
+			// Cohort
+			//
+			//            root
+			//          /      \
+			//        /          \
+			// best-effort(0.5)  research
+			//   |               /   |    \
+			//   |              /    |     \
+			//   cq     chemistry  physics   llm(2.0)
+			//               |       |         |
+			//               cq
+			createCohort(testing.MakeCohort("root").ResourceGroup(*testing.MakeFlavorQuotas("default").Resource("cpu", "12").Obj()).Obj())
+			createCohort(testing.MakeCohort("research").Parent("root").Obj())
+			createCohort(testing.MakeCohort("chemistry").Parent("research").Obj())
+			createCohort(testing.MakeCohort("physics").Parent("research").Obj())
+			createCohort(testing.MakeCohort("llm").FairWeight(resource.MustParse("2.0")).Parent("research").Obj())
+			createCohort(testing.MakeCohort("best-effort").FairWeight(resource.MustParse("0.5")).Parent("root").Obj())
+
+			preemptionPolicy := kueue.ClusterQueuePreemption{
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}
+			zeroQuota := *testing.MakeFlavorQuotas("default").Resource("cpu", "0").Obj()
```

  I'm not sure the reason why these are submitted twise...

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-20T15:08:17Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:81** by [@gabesaba](https://github.com/gabesaba) — 2025-03-20T15:06:33Z
```diff
@@ -66,15 +67,20 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		return cq
 	}
 
-	var createWorkload = func(queue string, cpuRequests string) *kueue.Workload {
+	var createWorkloadWithPriority = func(queue string, cpuRequests string, priority int32) *kueue.Workload {
 		wl := testing.MakeWorkloadWithGeneratedName("workload-", ns.Name).
+			Priority(priority).
 			Queue(queue).
 			Request(corev1.ResourceCPU, cpuRequests).Obj()
 		wls = append(wls, wl)
 		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 		return wl
 	}
 
+	var createWorkload = func(queue string, cpuRequests string) *kueue.Workload {
+		return createWorkloadWithPriority(queue, cpuRequests, 0)
```

  I don't think it matters for this case. I only wanted to provide it so that there's the ability to specify different priorities.

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:339** by [@gabesaba](https://github.com/gabesaba) — 2025-03-20T15:06:45Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
```

  Done.

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:351** by [@gabesaba](https://github.com/gabesaba) — 2025-03-20T15:07:07Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
+			// Cohort
+			//
+			//            root
+			//          /      \
+			//        /          \
+			// best-effort(0.5)  research
+			//   |               /   |    \
+			//   |              /    |     \
+			//   cq     chemistry  physics   llm(2.0)
+			//               |       |         |
+			//               cq
+			createCohort(testing.MakeCohort("root").ResourceGroup(*testing.MakeFlavorQuotas("default").Resource("cpu", "12").Obj()).Obj())
```

  Done.

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:361** by [@gabesaba](https://github.com/gabesaba) — 2025-03-20T15:07:11Z
```diff
@@ -328,6 +334,73 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			expectCohortWeightedShare(cohortSecondRight.Name, 214)
 			expectCohortWeightedShare(cohortBank.Name, 0)
 		})
+		ginkgo.It("preempts workloads to enforce fair share", func() {
+			// below are Cohorts, queues, and their fair
+			// weights. 12 GPUs are provided by the root
+			// Cohort
+			//
+			//            root
+			//          /      \
+			//        /          \
+			// best-effort(0.5)  research
+			//   |               /   |    \
+			//   |              /    |     \
+			//   cq     chemistry  physics   llm(2.0)
+			//               |       |         |
+			//               cq
+			createCohort(testing.MakeCohort("root").ResourceGroup(*testing.MakeFlavorQuotas("default").Resource("cpu", "12").Obj()).Obj())
+			createCohort(testing.MakeCohort("research").Parent("root").Obj())
+			createCohort(testing.MakeCohort("chemistry").Parent("research").Obj())
+			createCohort(testing.MakeCohort("physics").Parent("research").Obj())
+			createCohort(testing.MakeCohort("llm").FairWeight(resource.MustParse("2.0")).Parent("research").Obj())
+			createCohort(testing.MakeCohort("best-effort").FairWeight(resource.MustParse("0.5")).Parent("root").Obj())
+
+			preemptionPolicy := kueue.ClusterQueuePreemption{
+				ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+			}
+			zeroQuota := *testing.MakeFlavorQuotas("default").Resource("cpu", "0").Obj()
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-21T09:00:42Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2025-03-21T09:00:42Z
```diff
@@ -66,15 +67,20 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		return cq
 	}
 
-	var createWorkload = func(queue string, cpuRequests string) *kueue.Workload {
+	var createWorkloadWithPriority = func(queue string, cpuRequests string, priority int32) *kueue.Workload {
 		wl := testing.MakeWorkloadWithGeneratedName("workload-", ns.Name).
+			Priority(priority).
 			Queue(queue).
 			Request(corev1.ResourceCPU, cpuRequests).Obj()
 		wls = append(wls, wl)
 		gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 		return wl
 	}
 
+	var createWorkload = func(queue string, cpuRequests string) *kueue.Workload {
+		return createWorkloadWithPriority(queue, cpuRequests, 0)
```

  That makes sense. Indeed this `createWorkloadWithPriority` function is closed within fair_sharing testings file.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-03-21T09:00:56Z

Thank you
/lgtm
/approve
