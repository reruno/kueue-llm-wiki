# PR #4710: Add test cases for multiple podsets

**Summary**: Add test cases for multiple podsets

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4710

**Last updated**: 2025-08-13T13:27:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Horiodino](https://github.com/Horiodino)
- **Branch**: `add_test_cases_for_multiple_podsets` → `main`
- **Created**: 2025-03-20T11:36:32Z
- **Updated**: 2025-08-13T13:27:13Z
- **Merged**: 2025-08-13T13:27:10Z
- **Closed**: 2025-08-13T13:27:10Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+279 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Adds the test for multiple podsets in TestFindTopologyAssignment to improve coverage and ensure comprehensive validation of FindTopologyAssignment.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/4549#event-16802169139

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-20T11:36:42Z

Hi @Horiodino. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-20T11:36:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f18a022227edab8db09bba74f51c076a9d6fd579 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6895f03512ea510008e152ae |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-4710--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-4710--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTQ3MTAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.vSpxDo3nHsYVw_wHshKrF-bD5ztgxZA7YUx4CC-EkB0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-4710--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-20T15:03:04Z

/milestone v0.12

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-21T05:30:36Z

/ok-to-test
Thank you for driving this!

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-28T09:50:01Z

Hi @Horiodino, could you rebase the branch so we can proceed with the PR?

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-06-16T16:31:37Z

> Hi @Horiodino, could you rebase the branch so we can proceed with the PR?

Hey, sorry for the delay , I had exams and then went on vacation. I'll get it fixed  soon .

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-07-29T14:09:27Z

Thanks @Horiodino,
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-29T14:09:35Z

LGTM label has been added.  <details>Git tree hash: 38d914066d4743c1bf66502a50de27b718cb6a44</details>

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-08-08T13:07:28Z

Hey, could anyone  please review this PR soon? It’s been stale for a while, and with so many changes, it’s starting to develop conflicts.

### Comment by [@Horiodino](https://github.com/Horiodino) — 2025-08-11T16:13:26Z

cc @PBundyra

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-08-12T07:14:05Z

/lgtm
Thanks @Horiodino

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-12T07:14:11Z

LGTM label has been added.  <details>Git tree hash: 9f2bb4ef6269508cd1369500716902011ab827a1</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-08-12T07:14:20Z

cc @mimowo @tenzen-y

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-08-13T13:01:01Z

/lgtm
/approve

thanks!!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-13T13:01:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4710#issuecomment-3183828526" title="Approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4710#" title="Author self-approved">Horiodino</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-03-20T11:39:10Z

- **pkg/cache/tas_cache_test.go:3235** by [@Horiodino](https://github.com/Horiodino) — 2025-03-20T11:39:09Z
```diff
@@ -1944,22 +1946,336 @@ func TestFindTopologyAssignment(t *testing.T) {
 			wantReason:         `topology "default" doesn't allow to fit any of 1 pod(s)`,
 			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
 		},
+		"multiple podsets; block required for one, unconstrained for another; MostFreeCapacity": {
+			nodes:              defaultNodes,
+			levels:             defaultThreeLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasBlockLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Unconstrained: ptr.To(true),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{"kubernetes.io/hostname"},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x2"}},
+							{Count: 1, Values: []string{"x3"}},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{"kubernetes.io/hostname"},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x6"}},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; rack required for both, different resource requests; BestFit": {
+			nodes:  defaultNodes,
+			levels: defaultTwoLevels,
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"b2", "r2"},
+							},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b1", "r1"},
+							},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; one fits, one doesn't due to capacity; MostFreeCapacity": {
+			nodes:              defaultNodes,
+			levels:             defaultTwoLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 3,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 3,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 3, Values: []string{"b1", "r2"}},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					FailureReason: `topology "default" allows to fit only 2 out of 3 pod(s)`,
+				},
+			},
+		},
+		"multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity": {
```

  @mimowo Could you take another look at the test for `TASProfileLeastFreeCapacity`? It should pass since it’s expected to allocate to any, but it's returning an error. This behavior seems consistent with the other `TASProfileLeastFreeCapacity` tests below. What do you think?

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-03-20T11:40:38Z

- **pkg/cache/tas_cache_test.go:3235** by [@Horiodino](https://github.com/Horiodino) — 2025-03-20T11:40:38Z
```diff
@@ -1944,22 +1946,336 @@ func TestFindTopologyAssignment(t *testing.T) {
 			wantReason:         `topology "default" doesn't allow to fit any of 1 pod(s)`,
 			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
 		},
+		"multiple podsets; block required for one, unconstrained for another; MostFreeCapacity": {
+			nodes:              defaultNodes,
+			levels:             defaultThreeLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasBlockLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Unconstrained: ptr.To(true),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{"kubernetes.io/hostname"},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x2"}},
+							{Count: 1, Values: []string{"x3"}},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{"kubernetes.io/hostname"},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x6"}},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; rack required for both, different resource requests; BestFit": {
+			nodes:  defaultNodes,
+			levels: defaultTwoLevels,
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"b2", "r2"},
+							},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b1", "r1"},
+							},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; one fits, one doesn't due to capacity; MostFreeCapacity": {
+			nodes:              defaultNodes,
+			levels:             defaultTwoLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 3,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 3,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 3, Values: []string{"b1", "r2"}},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					FailureReason: `topology "default" allows to fit only 2 out of 3 pod(s)`,
+				},
+			},
+		},
+		"multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity": {
```

  By the way, I also added some logging to verify that resources  freed properly.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-03-21T05:33:15Z

- **pkg/cache/tas_cache_test.go:3235** by [@mimowo](https://github.com/mimowo) — 2025-03-21T05:33:15Z
```diff
@@ -1944,22 +1946,336 @@ func TestFindTopologyAssignment(t *testing.T) {
 			wantReason:         `topology "default" doesn't allow to fit any of 1 pod(s)`,
 			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
 		},
+		"multiple podsets; block required for one, unconstrained for another; MostFreeCapacity": {
+			nodes:              defaultNodes,
+			levels:             defaultThreeLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasBlockLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Unconstrained: ptr.To(true),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{"kubernetes.io/hostname"},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x2"}},
+							{Count: 1, Values: []string{"x3"}},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{"kubernetes.io/hostname"},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x6"}},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; rack required for both, different resource requests; BestFit": {
+			nodes:  defaultNodes,
+			levels: defaultTwoLevels,
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"b2", "r2"},
+							},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b1", "r1"},
+							},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; one fits, one doesn't due to capacity; MostFreeCapacity": {
+			nodes:              defaultNodes,
+			levels:             defaultTwoLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileMostFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 3,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 3,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": tasPodSetAssignmentResult{
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 3, Values: []string{"b1", "r2"}},
+						},
+					},
+				},
+				"podset2": tasPodSetAssignmentResult{
+					FailureReason: `topology "default" allows to fit only 2 out of 3 pod(s)`,
+				},
+			},
+		},
+		"multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity": {
```

  cc @PBundyra PTAL

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:40:09Z

- **pkg/cache/tas_cache_test.go:3334** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:40:09Z
```diff
@@ -2970,38 +3284,59 @@ func TestFindTopologyAssignment(t *testing.T) {
 			if err != nil {
 				t.Fatalf("failed to build the snapshot: %v", err)
 			}
-			tasInput := TASPodSetRequests{
-				PodSet: &kueue.PodSet{
-					Name:            kueue.DefaultPodSetName,
-					TopologyRequest: tc.topologyRequest,
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							Tolerations:  tc.tolerations,
-							NodeSelector: tc.nodeSelector,
-						},
-					},
-				},
-				SinglePodRequests: tc.requests,
-				Count:             tc.count,
-			}
-			if tc.topologyRequest == nil {
-				tasInput.Implied = true
-			}
-			flavorTASRequests := []TASPodSetRequests{tasInput}
-			wantResult := make(TASAssignmentsResult)
-			wantMainPodSetResult := tasPodSetAssignmentResult{
-				FailureReason: tc.wantReason,
+			var flavorTASRequests []TASPodSetRequests
+			var wantResult TASAssignmentsResult
+			if len(tc.podSets) > 0 {
+				flavorTASRequests = tc.podSets
+				wantResult = tc.wantResult
+				for _, r := range wantResult {
+					if r.TopologyAssignment != nil {
+						sort.Slice(r.TopologyAssignment.Domains, func(i, j int) bool {
+							return utiltas.DomainID(r.TopologyAssignment.Domains[i].Values) < utiltas.DomainID(r.TopologyAssignment.Domains[j].Values)
+						})
+					}
+				}
+			} else {
+				tasInput := TASPodSetRequests{
+					PodSet: &kueue.PodSet{
+						Name:            kueue.DefaultPodSetName,
+						TopologyRequest: tc.topologyRequest,
+						Template: corev1.PodTemplateSpec{
+							Spec: corev1.PodSpec{
+								Tolerations:  tc.tolerations,
+								NodeSelector: tc.nodeSelector,
+							},
+						},
+					},
+					SinglePodRequests: tc.requests,
+					Count:             tc.count,
+				}
+				if tc.topologyRequest == nil {
+					tasInput.Implied = true
+				}
+				flavorTASRequests = []TASPodSetRequests{tasInput}
+				wantResult = make(TASAssignmentsResult)
+				wantMainPodSetResult := tasPodSetAssignmentResult{
+					FailureReason: tc.wantReason,
+				}
+				if tc.wantAssignment != nil {
+					sort.Slice(tc.wantAssignment.Domains, func(i, j int) bool {
+						return utiltas.DomainID(tc.wantAssignment.Domains[i].Values) < utiltas.DomainID(tc.wantAssignment.Domains[j].Values)
+					})
+					wantMainPodSetResult.TopologyAssignment = tc.wantAssignment
+				}
+				wantResult[kueue.DefaultPodSetName] = wantMainPodSetResult
 			}
-			if tc.wantAssignment != nil {
-				sort.Slice(tc.wantAssignment.Domains, func(i, j int) bool {
-					return utiltas.DomainID(tc.wantAssignment.Domains[i].Values) < utiltas.DomainID(tc.wantAssignment.Domains[j].Values)
-				})
-				wantMainPodSetResult.TopologyAssignment = tc.wantAssignment
-			}
-			wantResult[kueue.DefaultPodSetName] = wantMainPodSetResult
 			gotResult := snapshot.FindTopologyAssignmentsForFlavor(flavorTASRequests)
+			for _, r := range gotResult {
+				if r.TopologyAssignment != nil {
+					sort.Slice(r.TopologyAssignment.Domains, func(i, j int) bool {
+						return utiltas.DomainID(r.TopologyAssignment.Domains[i].Values) < utiltas.DomainID(r.TopologyAssignment.Domains[j].Values)
```

  Let's not sort the domains in tests, as this could lead to false positive test outcomes. As a general good practice, we should avoid adding any kind of additional logic to tests if possible

### Review by [@PBundyra](https://github.com/PBundyra) (changes_requested) — 2025-06-23T08:57:18Z

- **pkg/cache/tas_cache_test.go:3339** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:40:37Z
```diff
@@ -2970,38 +3284,59 @@ func TestFindTopologyAssignment(t *testing.T) {
 			if err != nil {
 				t.Fatalf("failed to build the snapshot: %v", err)
 			}
-			tasInput := TASPodSetRequests{
-				PodSet: &kueue.PodSet{
-					Name:            kueue.DefaultPodSetName,
-					TopologyRequest: tc.topologyRequest,
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							Tolerations:  tc.tolerations,
-							NodeSelector: tc.nodeSelector,
-						},
-					},
-				},
-				SinglePodRequests: tc.requests,
-				Count:             tc.count,
-			}
-			if tc.topologyRequest == nil {
-				tasInput.Implied = true
-			}
-			flavorTASRequests := []TASPodSetRequests{tasInput}
-			wantResult := make(TASAssignmentsResult)
-			wantMainPodSetResult := tasPodSetAssignmentResult{
-				FailureReason: tc.wantReason,
+			var flavorTASRequests []TASPodSetRequests
+			var wantResult TASAssignmentsResult
+			if len(tc.podSets) > 0 {
+				flavorTASRequests = tc.podSets
+				wantResult = tc.wantResult
+				for _, r := range wantResult {
+					if r.TopologyAssignment != nil {
+						sort.Slice(r.TopologyAssignment.Domains, func(i, j int) bool {
+							return utiltas.DomainID(r.TopologyAssignment.Domains[i].Values) < utiltas.DomainID(r.TopologyAssignment.Domains[j].Values)
+						})
+					}
+				}
+			} else {
+				tasInput := TASPodSetRequests{
+					PodSet: &kueue.PodSet{
+						Name:            kueue.DefaultPodSetName,
+						TopologyRequest: tc.topologyRequest,
+						Template: corev1.PodTemplateSpec{
+							Spec: corev1.PodSpec{
+								Tolerations:  tc.tolerations,
+								NodeSelector: tc.nodeSelector,
+							},
+						},
+					},
+					SinglePodRequests: tc.requests,
+					Count:             tc.count,
+				}
+				if tc.topologyRequest == nil {
+					tasInput.Implied = true
+				}
+				flavorTASRequests = []TASPodSetRequests{tasInput}
+				wantResult = make(TASAssignmentsResult)
+				wantMainPodSetResult := tasPodSetAssignmentResult{
+					FailureReason: tc.wantReason,
+				}
+				if tc.wantAssignment != nil {
+					sort.Slice(tc.wantAssignment.Domains, func(i, j int) bool {
+						return utiltas.DomainID(tc.wantAssignment.Domains[i].Values) < utiltas.DomainID(tc.wantAssignment.Domains[j].Values)
+					})
+					wantMainPodSetResult.TopologyAssignment = tc.wantAssignment
+				}
+				wantResult[kueue.DefaultPodSetName] = wantMainPodSetResult
 			}
-			if tc.wantAssignment != nil {
-				sort.Slice(tc.wantAssignment.Domains, func(i, j int) bool {
-					return utiltas.DomainID(tc.wantAssignment.Domains[i].Values) < utiltas.DomainID(tc.wantAssignment.Domains[j].Values)
-				})
-				wantMainPodSetResult.TopologyAssignment = tc.wantAssignment
-			}
-			wantResult[kueue.DefaultPodSetName] = wantMainPodSetResult
 			gotResult := snapshot.FindTopologyAssignmentsForFlavor(flavorTASRequests)
+			for _, r := range gotResult {
+				if r.TopologyAssignment != nil {
+					sort.Slice(r.TopologyAssignment.Domains, func(i, j int) bool {
+						return utiltas.DomainID(r.TopologyAssignment.Domains[i].Values) < utiltas.DomainID(r.TopologyAssignment.Domains[j].Values)
+					})
+				}
+			}
 			if diff := cmp.Diff(wantResult, gotResult); diff != "" {
-				t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+				t.Errorf("test case %q: unexpected topology assignment (-want,+got): %s", name, diff)
```

  I believe the name is already part of the log, so there's no need to add that

- **pkg/cache/tas_cache_test.go:3296** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:47:56Z
```diff
@@ -2970,38 +3284,59 @@ func TestFindTopologyAssignment(t *testing.T) {
 			if err != nil {
 				t.Fatalf("failed to build the snapshot: %v", err)
 			}
-			tasInput := TASPodSetRequests{
-				PodSet: &kueue.PodSet{
-					Name:            kueue.DefaultPodSetName,
-					TopologyRequest: tc.topologyRequest,
-					Template: corev1.PodTemplateSpec{
-						Spec: corev1.PodSpec{
-							Tolerations:  tc.tolerations,
-							NodeSelector: tc.nodeSelector,
-						},
-					},
-				},
-				SinglePodRequests: tc.requests,
-				Count:             tc.count,
-			}
-			if tc.topologyRequest == nil {
-				tasInput.Implied = true
-			}
-			flavorTASRequests := []TASPodSetRequests{tasInput}
-			wantResult := make(TASAssignmentsResult)
-			wantMainPodSetResult := tasPodSetAssignmentResult{
-				FailureReason: tc.wantReason,
+			var flavorTASRequests []TASPodSetRequests
+			var wantResult TASAssignmentsResult
+			if len(tc.podSets) > 0 {
+				flavorTASRequests = tc.podSets
+				wantResult = tc.wantResult
+				for _, r := range wantResult {
+					if r.TopologyAssignment != nil {
+						sort.Slice(r.TopologyAssignment.Domains, func(i, j int) bool {
+							return utiltas.DomainID(r.TopologyAssignment.Domains[i].Values) < utiltas.DomainID(r.TopologyAssignment.Domains[j].Values)
+						})
```

  Same as below, let's not add additional logic to tests

- **pkg/cache/tas_cache_test.go:376** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:56:47Z
```diff
@@ -302,6 +372,8 @@ func TestFindTopologyAssignment(t *testing.T) {
 		count              int32
 		tolerations        []corev1.Toleration
 		wantAssignment     *kueue.TopologyAssignment
+		podSets            []TASPodSetRequests
+		wantResult         TASAssignmentsResult
```

  We need to avoid having two ways of setting input and expected output. Since there are plenty of test scenarios for only one podSet that don't require additional boilerplate, I suggest moving new scenarios to test suite `TestFindTopologyAssignmentForTwoPodSets`. This will also simplify testing logic below

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-26T12:28:43Z

- **pkg/cache/tas_cache_test.go:3127** by [@PBundyra](https://github.com/PBundyra) — 2025-06-26T12:28:43Z
```diff
@@ -3010,94 +3010,398 @@ func TestFindTopologyAssignment(t *testing.T) {
 func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 	const (
 		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
 	)
 
-	t.Run("find topology assignment for two podsets with overlapping domain", func(t *testing.T) {
-		ctx, _ := utiltesting.ContextWithLog(t)
-		nodes := []corev1.Node{
-			*testingnode.MakeNode("b1").
-				Label(tasBlockLabel, "b1").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b2").
-				Label(tasBlockLabel, "b2").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b3").
-				Label(tasBlockLabel, "b3").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-		}
-		levels := []string{tasBlockLabel}
-		requests := resources.Requests{
-			corev1.ResourceCPU: 1000,
-		}
-		topologyRequest := &kueue.PodSetTopologyRequest{
-			Preferred: ptr.To(tasBlockLabel),
-		}
-		wantAssignment1 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+	defaultTwoLevels := []string{tasBlockLabel, tasRackLabel}
+	defaultThreeLevels := []string{tasBlockLabel, tasRackLabel, corev1.LabelHostname}
+
+	multiPodSetsNodes := []corev1.Node{
+		*testingnode.MakeNode("b1-r1-x1").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x1").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x2").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x2").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x3").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x3").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x4").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x4").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r1-x5").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x5").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r2-x6").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x6").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("20"),
+				corev1.ResourceMemory: resource.MustParse("4Gi"),
+				corev1.ResourcePods:   resource.MustParse("40"),
+			}).
+			Ready().
+			Obj(),
+	}
+
+	tests := map[string]struct {
+		nodes              []corev1.Node
+		levels             []string
+		enableFeatureGates []featuregate.Feature
+		podSets            []TASPodSetRequests
+		wantResult         TASAssignmentsResult
+	}{
+		"find topology assignment for two podsets with overlapping domain": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []TASPodSetRequests{
+				buildTASInput("podset1", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+				buildTASInput("podset2", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+			},
+			wantResult: func() TASAssignmentsResult {
```

  Can we use `TASAssignmentsResult` instead of a function?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-26T12:34:05Z

- **pkg/cache/tas_cache_test.go:3017** by [@PBundyra](https://github.com/PBundyra) — 2025-06-26T12:34:05Z
```diff
@@ -3010,94 +3010,398 @@ func TestFindTopologyAssignment(t *testing.T) {
 func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 	const (
 		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
 	)
 
-	t.Run("find topology assignment for two podsets with overlapping domain", func(t *testing.T) {
-		ctx, _ := utiltesting.ContextWithLog(t)
-		nodes := []corev1.Node{
-			*testingnode.MakeNode("b1").
-				Label(tasBlockLabel, "b1").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b2").
-				Label(tasBlockLabel, "b2").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b3").
-				Label(tasBlockLabel, "b3").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-		}
-		levels := []string{tasBlockLabel}
-		requests := resources.Requests{
-			corev1.ResourceCPU: 1000,
-		}
-		topologyRequest := &kueue.PodSetTopologyRequest{
-			Preferred: ptr.To(tasBlockLabel),
-		}
-		wantAssignment1 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+	defaultTwoLevels := []string{tasBlockLabel, tasRackLabel}
+	defaultThreeLevels := []string{tasBlockLabel, tasRackLabel, corev1.LabelHostname}
```

  Can we commonize this with variables from `TestFindTopologyAssignment`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-26T12:34:51Z

- **pkg/cache/tas_cache_test.go:3334** by [@PBundyra](https://github.com/PBundyra) — 2025-06-26T12:34:51Z
```diff
@@ -3010,94 +3010,398 @@ func TestFindTopologyAssignment(t *testing.T) {
 func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 	const (
 		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
 	)
 
-	t.Run("find topology assignment for two podsets with overlapping domain", func(t *testing.T) {
-		ctx, _ := utiltesting.ContextWithLog(t)
-		nodes := []corev1.Node{
-			*testingnode.MakeNode("b1").
-				Label(tasBlockLabel, "b1").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b2").
-				Label(tasBlockLabel, "b2").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b3").
-				Label(tasBlockLabel, "b3").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-		}
-		levels := []string{tasBlockLabel}
-		requests := resources.Requests{
-			corev1.ResourceCPU: 1000,
-		}
-		topologyRequest := &kueue.PodSetTopologyRequest{
-			Preferred: ptr.To(tasBlockLabel),
-		}
-		wantAssignment1 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+	defaultTwoLevels := []string{tasBlockLabel, tasRackLabel}
+	defaultThreeLevels := []string{tasBlockLabel, tasRackLabel, corev1.LabelHostname}
+
+	multiPodSetsNodes := []corev1.Node{
+		*testingnode.MakeNode("b1-r1-x1").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x1").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x2").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x2").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x3").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x3").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x4").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x4").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r1-x5").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x5").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r2-x6").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x6").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("20"),
+				corev1.ResourceMemory: resource.MustParse("4Gi"),
+				corev1.ResourcePods:   resource.MustParse("40"),
+			}).
+			Ready().
+			Obj(),
+	}
+
+	tests := map[string]struct {
+		nodes              []corev1.Node
+		levels             []string
+		enableFeatureGates []featuregate.Feature
+		podSets            []TASPodSetRequests
+		wantResult         TASAssignmentsResult
+	}{
+		"find topology assignment for two podsets with overlapping domain": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []TASPodSetRequests{
+				buildTASInput("podset1", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+				buildTASInput("podset2", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+			},
+			wantResult: func() TASAssignmentsResult {
+				wantAssignment1 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 2, Values: []string{"b1"}},
+						{Count: 1, Values: []string{"b2"}},
+					},
+				}
+				wantAssignment2 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b2"}},
+						{Count: 2, Values: []string{"b3"}},
+					},
+				}
+				res := make(TASAssignmentsResult)
+				res["podset1"] = buildWantedResult(wantAssignment1)
+				res["podset2"] = buildWantedResult(wantAssignment2)
+				return res
+			}(),
+		},
+		"multiple podsets; rack required for both, different resource requests; BestFit": {
+			nodes:  multiPodSetsNodes,
+			levels: defaultTwoLevels,
+			podSets: []TASPodSetRequests{
 				{
-					Count: 2,
-					Values: []string{
-						"b1",
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
 					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
 				},
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"b1", "r1"},
+							},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b1", "r1"},
+							},
+						},
 					},
 				},
 			},
-		}
-		wantAssignment2 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+		},
+		"multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultThreeLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasBlockLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 9,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Unconstrained: ptr.To(true),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 9, Values: []string{"x5"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x1"}},
+							{Count: 1, Values: []string{"x5"}},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; rack required for both, different resource requests; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultTwoLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+						corev1.ResourceCPU:    1000,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
 					},
 				},
+			},
+		},
+		"multiple podsets; one fits, one doesn't due to capacity; LeastFreeCapacity": {
```

  Why the second podset doesnt fit?

### Review by [@Horiodino](https://github.com/Horiodino) (commented) — 2025-06-26T12:52:02Z

- **pkg/cache/tas_cache_test.go:3334** by [@Horiodino](https://github.com/Horiodino) — 2025-06-26T12:52:01Z
```diff
@@ -3010,94 +3010,398 @@ func TestFindTopologyAssignment(t *testing.T) {
 func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 	const (
 		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
 	)
 
-	t.Run("find topology assignment for two podsets with overlapping domain", func(t *testing.T) {
-		ctx, _ := utiltesting.ContextWithLog(t)
-		nodes := []corev1.Node{
-			*testingnode.MakeNode("b1").
-				Label(tasBlockLabel, "b1").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b2").
-				Label(tasBlockLabel, "b2").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b3").
-				Label(tasBlockLabel, "b3").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-		}
-		levels := []string{tasBlockLabel}
-		requests := resources.Requests{
-			corev1.ResourceCPU: 1000,
-		}
-		topologyRequest := &kueue.PodSetTopologyRequest{
-			Preferred: ptr.To(tasBlockLabel),
-		}
-		wantAssignment1 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+	defaultTwoLevels := []string{tasBlockLabel, tasRackLabel}
+	defaultThreeLevels := []string{tasBlockLabel, tasRackLabel, corev1.LabelHostname}
+
+	multiPodSetsNodes := []corev1.Node{
+		*testingnode.MakeNode("b1-r1-x1").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x1").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x2").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x2").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x3").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x3").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x4").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x4").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r1-x5").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x5").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r2-x6").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x6").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("20"),
+				corev1.ResourceMemory: resource.MustParse("4Gi"),
+				corev1.ResourcePods:   resource.MustParse("40"),
+			}).
+			Ready().
+			Obj(),
+	}
+
+	tests := map[string]struct {
+		nodes              []corev1.Node
+		levels             []string
+		enableFeatureGates []featuregate.Feature
+		podSets            []TASPodSetRequests
+		wantResult         TASAssignmentsResult
+	}{
+		"find topology assignment for two podsets with overlapping domain": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []TASPodSetRequests{
+				buildTASInput("podset1", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+				buildTASInput("podset2", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+			},
+			wantResult: func() TASAssignmentsResult {
+				wantAssignment1 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 2, Values: []string{"b1"}},
+						{Count: 1, Values: []string{"b2"}},
+					},
+				}
+				wantAssignment2 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b2"}},
+						{Count: 2, Values: []string{"b3"}},
+					},
+				}
+				res := make(TASAssignmentsResult)
+				res["podset1"] = buildWantedResult(wantAssignment1)
+				res["podset2"] = buildWantedResult(wantAssignment2)
+				return res
+			}(),
+		},
+		"multiple podsets; rack required for both, different resource requests; BestFit": {
+			nodes:  multiPodSetsNodes,
+			levels: defaultTwoLevels,
+			podSets: []TASPodSetRequests{
 				{
-					Count: 2,
-					Values: []string{
-						"b1",
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
 					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
 				},
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"b1", "r1"},
+							},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b1", "r1"},
+							},
+						},
 					},
 				},
 			},
-		}
-		wantAssignment2 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+		},
+		"multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultThreeLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasBlockLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 9,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Unconstrained: ptr.To(true),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 9, Values: []string{"x5"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x1"}},
+							{Count: 1, Values: []string{"x5"}},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; rack required for both, different resource requests; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultTwoLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+						corev1.ResourceCPU:    1000,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
 					},
 				},
+			},
+		},
+		"multiple podsets; one fits, one doesn't due to capacity; LeastFreeCapacity": {
```

  In this case Podset 1 ,Gets assigned to b1/r1 (which has exactly 10 CPU cores)
  This completely fills rack r1 in block b1, leaving 0 free capacity .
  
  The LeastFreeCapacity algorithm is trying to find the most efficient placement, but the constraint that both podsets must be scheduled together means if one optimal assignment prevents the other from being placed efficiently, the entire assignment fails.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-30T08:45:02Z

- **pkg/cache/tas_cache_test.go:3334** by [@PBundyra](https://github.com/PBundyra) — 2025-06-30T08:45:02Z
```diff
@@ -3010,94 +3010,398 @@ func TestFindTopologyAssignment(t *testing.T) {
 func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 	const (
 		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
 	)
 
-	t.Run("find topology assignment for two podsets with overlapping domain", func(t *testing.T) {
-		ctx, _ := utiltesting.ContextWithLog(t)
-		nodes := []corev1.Node{
-			*testingnode.MakeNode("b1").
-				Label(tasBlockLabel, "b1").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b2").
-				Label(tasBlockLabel, "b2").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b3").
-				Label(tasBlockLabel, "b3").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-		}
-		levels := []string{tasBlockLabel}
-		requests := resources.Requests{
-			corev1.ResourceCPU: 1000,
-		}
-		topologyRequest := &kueue.PodSetTopologyRequest{
-			Preferred: ptr.To(tasBlockLabel),
-		}
-		wantAssignment1 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+	defaultTwoLevels := []string{tasBlockLabel, tasRackLabel}
+	defaultThreeLevels := []string{tasBlockLabel, tasRackLabel, corev1.LabelHostname}
+
+	multiPodSetsNodes := []corev1.Node{
+		*testingnode.MakeNode("b1-r1-x1").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x1").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x2").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x2").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x3").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x3").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x4").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x4").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r1-x5").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x5").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r2-x6").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x6").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("20"),
+				corev1.ResourceMemory: resource.MustParse("4Gi"),
+				corev1.ResourcePods:   resource.MustParse("40"),
+			}).
+			Ready().
+			Obj(),
+	}
+
+	tests := map[string]struct {
+		nodes              []corev1.Node
+		levels             []string
+		enableFeatureGates []featuregate.Feature
+		podSets            []TASPodSetRequests
+		wantResult         TASAssignmentsResult
+	}{
+		"find topology assignment for two podsets with overlapping domain": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []TASPodSetRequests{
+				buildTASInput("podset1", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+				buildTASInput("podset2", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+			},
+			wantResult: func() TASAssignmentsResult {
+				wantAssignment1 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 2, Values: []string{"b1"}},
+						{Count: 1, Values: []string{"b2"}},
+					},
+				}
+				wantAssignment2 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b2"}},
+						{Count: 2, Values: []string{"b3"}},
+					},
+				}
+				res := make(TASAssignmentsResult)
+				res["podset1"] = buildWantedResult(wantAssignment1)
+				res["podset2"] = buildWantedResult(wantAssignment2)
+				return res
+			}(),
+		},
+		"multiple podsets; rack required for both, different resource requests; BestFit": {
+			nodes:  multiPodSetsNodes,
+			levels: defaultTwoLevels,
+			podSets: []TASPodSetRequests{
 				{
-					Count: 2,
-					Values: []string{
-						"b1",
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
 					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
 				},
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"b1", "r1"},
+							},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b1", "r1"},
+							},
+						},
 					},
 				},
 			},
-		}
-		wantAssignment2 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+		},
+		"multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultThreeLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasBlockLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 9,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Unconstrained: ptr.To(true),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 9, Values: []string{"x5"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x1"}},
+							{Count: 1, Values: []string{"x5"}},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; rack required for both, different resource requests; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultTwoLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+						corev1.ResourceCPU:    1000,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
 					},
 				},
+			},
+		},
+		"multiple podsets; one fits, one doesn't due to capacity; LeastFreeCapacity": {
```

  What enforces that both podsets must be scheduled together in the same rack? When it comes to the `topologyRequest.required` field it enforces that Pod within a PodSet must be scheduled and doesnt indicate colocation of other podsets.
  
  I believe this is a bug and it's already being worked on here: https://github.com/kubernetes-sigs/kueue/pull/5450

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-30T08:45:23Z

- **pkg/cache/tas_cache_test.go:3056** by [@PBundyra](https://github.com/PBundyra) — 2025-06-30T08:45:23Z
```diff
@@ -3010,94 +3010,398 @@ func TestFindTopologyAssignment(t *testing.T) {
 func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 	const (
 		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
 	)
 
-	t.Run("find topology assignment for two podsets with overlapping domain", func(t *testing.T) {
-		ctx, _ := utiltesting.ContextWithLog(t)
-		nodes := []corev1.Node{
-			*testingnode.MakeNode("b1").
-				Label(tasBlockLabel, "b1").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b2").
-				Label(tasBlockLabel, "b2").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b3").
-				Label(tasBlockLabel, "b3").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-		}
-		levels := []string{tasBlockLabel}
-		requests := resources.Requests{
-			corev1.ResourceCPU: 1000,
-		}
-		topologyRequest := &kueue.PodSetTopologyRequest{
-			Preferred: ptr.To(tasBlockLabel),
-		}
-		wantAssignment1 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+	defaultTwoLevels := []string{tasBlockLabel, tasRackLabel}
+	defaultThreeLevels := []string{tasBlockLabel, tasRackLabel, corev1.LabelHostname}
+
+	multiPodSetsNodes := []corev1.Node{
```

  Can you also add a comment that visualize how the tree looks like?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-30T11:43:35Z

- **pkg/cache/tas_cache_test.go:3334** by [@PBundyra](https://github.com/PBundyra) — 2025-06-30T11:43:35Z
```diff
@@ -3010,94 +3010,398 @@ func TestFindTopologyAssignment(t *testing.T) {
 func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 	const (
 		tasBlockLabel = "cloud.com/topology-block"
+		tasRackLabel  = "cloud.com/topology-rack"
 	)
 
-	t.Run("find topology assignment for two podsets with overlapping domain", func(t *testing.T) {
-		ctx, _ := utiltesting.ContextWithLog(t)
-		nodes := []corev1.Node{
-			*testingnode.MakeNode("b1").
-				Label(tasBlockLabel, "b1").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b2").
-				Label(tasBlockLabel, "b2").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-			*testingnode.MakeNode("b3").
-				Label(tasBlockLabel, "b3").
-				StatusAllocatable(corev1.ResourceList{
-					corev1.ResourceCPU:  resource.MustParse("2"),
-					corev1.ResourcePods: resource.MustParse("10"),
-				}).
-				Ready().
-				Obj(),
-		}
-		levels := []string{tasBlockLabel}
-		requests := resources.Requests{
-			corev1.ResourceCPU: 1000,
-		}
-		topologyRequest := &kueue.PodSetTopologyRequest{
-			Preferred: ptr.To(tasBlockLabel),
-		}
-		wantAssignment1 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+	defaultTwoLevels := []string{tasBlockLabel, tasRackLabel}
+	defaultThreeLevels := []string{tasBlockLabel, tasRackLabel, corev1.LabelHostname}
+
+	multiPodSetsNodes := []corev1.Node{
+		*testingnode.MakeNode("b1-r1-x1").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x1").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x2").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x2").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x3").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x3").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b1-r2-x4").
+			Label(tasBlockLabel, "b1").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x4").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r1-x5").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r1").
+			Label(corev1.LabelHostname, "x5").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("10"),
+				corev1.ResourceMemory: resource.MustParse("1Gi"),
+				corev1.ResourcePods:   resource.MustParse("10"),
+			}).
+			Ready().
+			Obj(),
+		*testingnode.MakeNode("b2-r2-x6").
+			Label(tasBlockLabel, "b2").
+			Label(tasRackLabel, "r2").
+			Label(corev1.LabelHostname, "x6").
+			StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("20"),
+				corev1.ResourceMemory: resource.MustParse("4Gi"),
+				corev1.ResourcePods:   resource.MustParse("40"),
+			}).
+			Ready().
+			Obj(),
+	}
+
+	tests := map[string]struct {
+		nodes              []corev1.Node
+		levels             []string
+		enableFeatureGates []featuregate.Feature
+		podSets            []TASPodSetRequests
+		wantResult         TASAssignmentsResult
+	}{
+		"find topology assignment for two podsets with overlapping domain": {
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1").
+					Label(tasBlockLabel, "b1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b2").
+					Label(tasBlockLabel, "b2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b3").
+					Label(tasBlockLabel, "b3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("2"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: []string{tasBlockLabel},
+			podSets: []TASPodSetRequests{
+				buildTASInput("podset1", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+				buildTASInput("podset2", &kueue.PodSetTopologyRequest{Preferred: ptr.To(tasBlockLabel)}, resources.Requests{corev1.ResourceCPU: 1000}, 3),
+			},
+			wantResult: func() TASAssignmentsResult {
+				wantAssignment1 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 2, Values: []string{"b1"}},
+						{Count: 1, Values: []string{"b2"}},
+					},
+				}
+				wantAssignment2 := &kueue.TopologyAssignment{
+					Levels: []string{tasBlockLabel},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b2"}},
+						{Count: 2, Values: []string{"b3"}},
+					},
+				}
+				res := make(TASAssignmentsResult)
+				res["podset1"] = buildWantedResult(wantAssignment1)
+				res["podset2"] = buildWantedResult(wantAssignment2)
+				return res
+			}(),
+		},
+		"multiple podsets; rack required for both, different resource requests; BestFit": {
+			nodes:  multiPodSetsNodes,
+			levels: defaultTwoLevels,
+			podSets: []TASPodSetRequests{
 				{
-					Count: 2,
-					Values: []string{
-						"b1",
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
 					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
 				},
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  2,
+								Values: []string{"b1", "r1"},
+							},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{
+								Count:  1,
+								Values: []string{"b1", "r1"},
+							},
+						},
 					},
 				},
 			},
-		}
-		wantAssignment2 := &kueue.TopologyAssignment{
-			Levels: []string{tasBlockLabel},
-			Domains: []kueue.TopologyDomainAssignment{
+		},
+		"multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultThreeLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
 				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasBlockLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 9,
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Unconstrained: ptr.To(true),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
+					Count: 2,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 9, Values: []string{"x5"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: []string{corev1.LabelHostname},
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"x1"}},
+							{Count: 1, Values: []string{"x5"}},
+						},
+					},
+				},
+			},
+		},
+		"multiple podsets; rack required for both, different resource requests; LeastFreeCapacity": {
+			nodes:              multiPodSetsNodes,
+			levels:             defaultTwoLevels,
+			enableFeatureGates: []featuregate.Feature{features.TASProfileLeastFreeCapacity},
+			podSets: []TASPodSetRequests{
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset1",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceCPU: 1000,
+					},
 					Count: 1,
-					Values: []string{
-						"b2",
+				},
+				{
+					PodSet: &kueue.PodSet{
+						Name: "podset2",
+						TopologyRequest: &kueue.PodSetTopologyRequest{
+							Required: ptr.To(tasRackLabel),
+						},
+					},
+					SinglePodRequests: resources.Requests{
+						corev1.ResourceMemory: 1024,
+						corev1.ResourceCPU:    1000,
+					},
+					Count: 1,
+				},
+			},
+			wantResult: TASAssignmentsResult{
+				"podset1": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
+					},
+				},
+				"podset2": {
+					TopologyAssignment: &kueue.TopologyAssignment{
+						Levels: defaultTwoLevels,
+						Domains: []kueue.TopologyDomainAssignment{
+							{Count: 1, Values: []string{"b1", "r1"}},
+						},
 					},
 				},
+			},
+		},
+		"multiple podsets; one fits, one doesn't due to capacity; LeastFreeCapacity": {
```

  Since this issue was addressed today, please rebase once again and adjust this scenario

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-28T09:17:04Z

- **pkg/cache/tas_cache_test.go:4097** by [@PBundyra](https://github.com/PBundyra) — 2025-07-28T09:17:03Z
```diff
@@ -3849,6 +3916,297 @@ func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
 		}
 	})
+
+	t.Run("multiple podsets; rack required for both, different resource requests; BestFit", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultTwoLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 2,
+			},
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset2",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceMemory: 1024,
+				},
+				Count: 1,
+			},
+		}
+
+		wantResult := TASAssignmentsResult{
+			"podset1": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{
+							Count:  2,
+							Values: []string{"b1", "r1"},
+						},
+					},
+				},
+			},
+			"podset2": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{
+							Count:  1,
+							Values: []string{"b1", "r1"},
+						},
+					},
+				},
+			},
+		}
+
+		gotResult := snapshot.FindTopologyAssignmentsForFlavor(podSets)
+		if diff := cmp.Diff(wantResult, gotResult); diff != "" {
+			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+		}
+	})
+
+	t.Run("multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		features.SetFeatureGateDuringTest(t, features.TASProfileLeastFreeCapacity, true)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultThreeLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 9,
+			},
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset2",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 2,
+			},
+		}
+
+		wantResult := TASAssignmentsResult{
+			"podset1": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: []string{corev1.LabelHostname},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 9, Values: []string{"x5"}},
+					},
+				},
+			},
+			"podset2": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: []string{corev1.LabelHostname},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 2, Values: []string{"x1"}},
+					},
+				},
+			},
+		}
+
+		gotResult := snapshot.FindTopologyAssignmentsForFlavor(podSets)
+		if diff := cmp.Diff(wantResult, gotResult); diff != "" {
+			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+		}
+	})
+
+	t.Run("multiple podsets; rack required for both, different resource requests; LeastFreeCapacity", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		features.SetFeatureGateDuringTest(t, features.TASProfileLeastFreeCapacity, true)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultTwoLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 1,
+			},
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset2",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceMemory: 1024,
+					corev1.ResourceCPU:    1000,
+				},
+				Count: 1,
+			},
+		}
+
+		wantResult := TASAssignmentsResult{
+			"podset1": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b1", "r1"}},
+					},
+				},
+			},
+			"podset2": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b1", "r1"}},
+					},
+				},
+			},
+		}
+
+		gotResult := snapshot.FindTopologyAssignmentsForFlavor(podSets)
+		if diff := cmp.Diff(wantResult, gotResult); diff != "" {
+			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+		}
+	})
+
+	t.Run("multiple podsets; one fits, one doesn't due to capacity; LeastFreeCapacity", func(t *testing.T) {
```

  Please align the description

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-28T09:19:53Z

- **pkg/cache/tas_cache_test.go:4170** by [@PBundyra](https://github.com/PBundyra) — 2025-07-28T09:19:53Z
```diff
@@ -3849,6 +3916,297 @@ func TestFindTopologyAssignmentForTwoPodSets(t *testing.T) {
 			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
 		}
 	})
+
+	t.Run("multiple podsets; rack required for both, different resource requests; BestFit", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultTwoLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 2,
+			},
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset2",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceMemory: 1024,
+				},
+				Count: 1,
+			},
+		}
+
+		wantResult := TASAssignmentsResult{
+			"podset1": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{
+							Count:  2,
+							Values: []string{"b1", "r1"},
+						},
+					},
+				},
+			},
+			"podset2": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{
+							Count:  1,
+							Values: []string{"b1", "r1"},
+						},
+					},
+				},
+			},
+		}
+
+		gotResult := snapshot.FindTopologyAssignmentsForFlavor(podSets)
+		if diff := cmp.Diff(wantResult, gotResult); diff != "" {
+			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+		}
+	})
+
+	t.Run("multiple podsets; block required for one, unconstrained for another; LeastFreeCapacity", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		features.SetFeatureGateDuringTest(t, features.TASProfileLeastFreeCapacity, true)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultThreeLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 9,
+			},
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset2",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Unconstrained: ptr.To(true),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 2,
+			},
+		}
+
+		wantResult := TASAssignmentsResult{
+			"podset1": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: []string{corev1.LabelHostname},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 9, Values: []string{"x5"}},
+					},
+				},
+			},
+			"podset2": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: []string{corev1.LabelHostname},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 2, Values: []string{"x1"}},
+					},
+				},
+			},
+		}
+
+		gotResult := snapshot.FindTopologyAssignmentsForFlavor(podSets)
+		if diff := cmp.Diff(wantResult, gotResult); diff != "" {
+			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+		}
+	})
+
+	t.Run("multiple podsets; rack required for both, different resource requests; LeastFreeCapacity", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		features.SetFeatureGateDuringTest(t, features.TASProfileLeastFreeCapacity, true)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultTwoLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 1,
+			},
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset2",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceMemory: 1024,
+					corev1.ResourceCPU:    1000,
+				},
+				Count: 1,
+			},
+		}
+
+		wantResult := TASAssignmentsResult{
+			"podset1": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b1", "r1"}},
+					},
+				},
+			},
+			"podset2": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"b1", "r1"}},
+					},
+				},
+			},
+		}
+
+		gotResult := snapshot.FindTopologyAssignmentsForFlavor(podSets)
+		if diff := cmp.Diff(wantResult, gotResult); diff != "" {
+			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+		}
+	})
+
+	t.Run("multiple podsets; one fits, one doesn't due to capacity; LeastFreeCapacity", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		features.SetFeatureGateDuringTest(t, features.TASProfileLeastFreeCapacity, true)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultTwoLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 10,
+			},
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset2",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasRackLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 2,
+			},
+		}
+
+		wantResult := TASAssignmentsResult{
+			"podset1": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 10, Values: []string{"b1", "r1"}},
+					},
+				},
+			},
+			"podset2": {
+				TopologyAssignment: &kueue.TopologyAssignment{
+					Levels: defaultTwoLevels,
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 2, Values: []string{"b2", "r1"}},
+					},
+				},
+			},
+		}
+
+		gotResult := snapshot.FindTopologyAssignmentsForFlavor(podSets)
+		if diff := cmp.Diff(wantResult, gotResult); diff != "" {
+			t.Errorf("unexpected topology assignment (-want,+got): %s", diff)
+		}
+	})
+
+	t.Run("multiple podsets; block required for one, unconstrained for another; TASProfileMixed", func(t *testing.T) {
+		ctx, _ := utiltesting.ContextWithLog(t)
+		features.SetFeatureGateDuringTest(t, features.TASProfileMixed, true)
+		snapshot := buildSnapshot(ctx, t, multiPodSetsNodes, defaultThreeLevels)
+
+		podSets := []TASPodSetRequests{
+			{
+				PodSet: &kueue.PodSet{
+					Name: "podset1",
+					TopologyRequest: &kueue.PodSetTopologyRequest{
+						Required: ptr.To(tasBlockLabel),
+					},
+				},
+				SinglePodRequests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				Count: 10,
```

  Can we set it to 8 and check it both podsets land on the same node?
