# PR #473: Block admission until PodsReady for all admitted workloads if WaitForPodsReady is enabled

**Summary**: Block admission until PodsReady for all admitted workloads if WaitForPodsReady is enabled

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/473

**Last updated**: 2023-03-18T18:45:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `block-admission-until-podsready` → `main`
- **Created**: 2022-12-12T09:56:01Z
- **Updated**: 2023-03-18T18:45:58Z
- **Merged**: 2022-12-21T17:23:25Z
- **Closed**: 2022-12-21T17:23:26Z
- **Labels**: `kind/feature`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 8   **+740 / -18**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Introduces sequencing of job startups to prevent deadlocks in case of resource provisioning issues.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of: https://github.com/kubernetes-sigs/kueue/issues/349

#### Special notes for your reviewer:

- built on top of the current version of https://github.com/kubernetes-sigs/kueue/pull/460, will rebase once the first PR is merged

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-12T09:56:03Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-20T18:17:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/473#pullrequestreview-1224918600" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/473#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2022-12-21T08:27:18Z

Applied the remarks and squashed the commits. PTAL

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-12-21T17:23:04Z

/lgtm

Great!

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T20:26:47Z

- **main.go:114** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:31:36Z
```diff
@@ -111,7 +111,7 @@ func main() {
 		close(certsReady)
 	}
 
-	cCache := cache.New(mgr.GetClient())
+	cCache := cache.New(mgr.GetClient(), cache.WithWaitForPodsReady(waitForPodsReady(&cfg)))
```

  without looking at the rest of the PR, it seems odd that the cache needs to know about this. Maybe it's just a matter of naming (the cache shouldn't be waiting, but it can track "something").

- **pkg/cache/cache.go:208** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:42:35Z
```diff
@@ -148,6 +177,66 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads() {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.cond.Wait()
+		}
+	}
```

  It looks like this could block when trying to terminate kueue.
  
  The scenario would be that we enter `c.cond.Wait` and then kueue gets a termination signal. Nothing is broadcasting `c.cond`.
  
  See how in `pkg/queue`, `Broadcast` is called from `CleanUpOnContext`.

- **pkg/cache/cache.go:61** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:43:12Z
```diff
@@ -45,25 +47,52 @@ var (
 	errWorkloadNotAdmitted = errors.New("workload not admitted by a ClusterQueue")
 )
 
+type options struct {
+	waitForPodsReady bool
+}
+
+// Option configures the reconciler.
+type Option func(*options)
+
+// WithWaitForPodsReady indicates if the controller should add the PodsReady
+// condition to the workload when the corresponding job has all pods ready
+// or succeeded.
+func WithWaitForPodsReady(f bool) Option {
```

  maybe `WithTrackPodsReady`?

- **pkg/cache/cache.go:227** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:47:51Z
```diff
@@ -148,6 +177,66 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads() {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.cond.Wait()
+		}
+	}
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads() bool {
+	workloadsToCheck := []*kueue.Workload{}
+	for _, cq := range c.clusterQueues {
+		for _, wl := range cq.Workloads {
+			workloadsToCheck = append(workloadsToCheck, wl.Obj)
+		}
+	}
+	klog.V(5).InfoS("Workloads to check for PodsReady=true condition", "count", len(workloadsToCheck))
+	for _, wl := range workloadsToCheck {
+		klog.V(5).InfoS("Checking workload", "workload", klog.KObj(wl))
+		if !workload.InCondition(wl, kueue.WorkloadPodsReady) {
+			klog.InfoS("There is a workload which is not in PodsReady condition", "workload", klog.KObj(wl))
+			return false
+		}
+	}
+	klog.V(5).InfoS("All workloads are in the PodsReady condition")
+	return true
+}
+
+func (c *Cache) broadcastToRecheckPodsReady() {
+	c.cond.Broadcast()
+}
+
+func (c *Cache) broadcastToRecheckPodsReadyForDeletedWorkload(w *kueue.Workload) {
+	if c.waitForPodsReady {
+		// deleting a PodsReady=false workload
+		if !workload.InCondition(w, kueue.WorkloadPodsReady) {
+			c.broadcastToRecheckPodsReady()
```

  just inline `c.cond.Broadcast()`

- **pkg/cache/cache.go:204** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:49:26Z
```diff
@@ -148,6 +177,66 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads() {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.cond.Wait()
+		}
+	}
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads() bool {
+	workloadsToCheck := []*kueue.Workload{}
+	for _, cq := range c.clusterQueues {
+		for _, wl := range cq.Workloads {
+			workloadsToCheck = append(workloadsToCheck, wl.Obj)
```

  why not check here directly?
  
  building a slice could be expensive. It could also be tracked ias a counter in the ClusterQueue.

- **pkg/cache/cache.go:655** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:51:31Z
```diff
@@ -562,6 +651,7 @@ func (c *Cache) UpdateWorkload(oldWl, newWl *kueue.Workload) error {
 	if !ok {
 		return fmt.Errorf("new ClusterQueue doesn't exist")
 	}
+	c.broadcastToRecheckPodsReadyForUpdatedWorkload(oldWl, newWl)
```

  inline

- **pkg/cache/cache.go:655** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T19:54:16Z
```diff
@@ -562,6 +651,7 @@ func (c *Cache) UpdateWorkload(oldWl, newWl *kueue.Workload) error {
 	if !ok {
 		return fmt.Errorf("new ClusterQueue doesn't exist")
 	}
+	c.broadcastToRecheckPodsReadyForUpdatedWorkload(oldWl, newWl)
```

  Also you probably need to check when the Workload is first added. There is no guarantee from apiserver that all edits come as a separate notification.

- **pkg/scheduler/scheduler.go:158** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T20:26:01Z
```diff
@@ -124,6 +154,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
 			e.inadmissibleMsg = fmt.Sprintf("Failed to admit workload: %v", err)
+		} else if s.waitForPodsReady {
+			klog.InfoS("Workload successfully admitted when waitForPodsReady - do admit more in this cycle", "workload", e.Obj.Name)
```

  halting more admissions in this cycle.
  
  Admit should already log the name of the workload, so I don't think we should include it again.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-12T20:30:07Z

- **pkg/scheduler/scheduler.go:170** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-12T20:30:07Z
```diff
@@ -124,6 +154,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
```

  An alternative implementation would be to wait somewhere inside this function.
  
  This has the advantage that if the workload doesn't fit in the existing quota, the user would get an event or status update immediately.
  
  Also, it might be useful to produce a Workload status update if the Workload satisfies the quota, but is blocked waiting on others Workloads to be ready.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-15T13:56:42Z

- **pkg/scheduler/scheduler.go:158** by [@mimowo](https://github.com/mimowo) — 2022-12-15T13:56:42Z
```diff
@@ -124,6 +154,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
 			e.inadmissibleMsg = fmt.Sprintf("Failed to admit workload: %v", err)
+		} else if s.waitForPodsReady {
+			klog.InfoS("Workload successfully admitted when waitForPodsReady - do admit more in this cycle", "workload", e.Obj.Name)
```

  That's right, cleaned up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-15T14:00:47Z

- **pkg/cache/cache.go:655** by [@mimowo](https://github.com/mimowo) — 2022-12-15T14:00:47Z
```diff
@@ -562,6 +651,7 @@ func (c *Cache) UpdateWorkload(oldWl, newWl *kueue.Workload) error {
 	if !ok {
 		return fmt.Errorf("new ClusterQueue doesn't exist")
 	}
+	c.broadcastToRecheckPodsReadyForUpdatedWorkload(oldWl, newWl)
```

  I want to broadcast only if it makes sense to recheck the condition - meaning that there was an admitted workload with PodsReady=False and now the workload has PodsReady=True or is gone. Rechecking the condition on adding a new workload does not seem necessary.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-15T14:01:43Z

- **pkg/cache/cache.go:204** by [@mimowo](https://github.com/mimowo) — 2022-12-15T14:01:42Z
```diff
@@ -148,6 +177,66 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads() {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.cond.Wait()
+		}
+	}
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads() bool {
+	workloadsToCheck := []*kueue.Workload{}
+	for _, cq := range c.clusterQueues {
+		for _, wl := range cq.Workloads {
+			workloadsToCheck = append(workloadsToCheck, wl.Obj)
```

  It was written this way for debugging purposes, I will follow up with a clean up break early if there is a workload with PodsReady=False.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-15T14:08:58Z

- **pkg/scheduler/scheduler.go:170** by [@mimowo](https://github.com/mimowo) — 2022-12-15T14:08:58Z
```diff
@@ -124,6 +154,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
```

  The reasoning to block early in this function was that the blocking may take a while (say a couple of minutes) and after the time the nominated workloads could be different. However, this seems a minor concern. 
  
  I think that another advantage of the suggested approach is that this way it will be easier to implement the more granular version of the feature with blocking based on overlapping resource flavors. Going to follow up with a commit along these lines.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-15T22:08:52Z

- **pkg/scheduler/scheduler.go:170** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-15T22:08:52Z
```diff
@@ -124,6 +154,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
```

  You make a good point. Workloads could have finished and freed resources. But at least we know that this workload would fit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-19T15:02:11Z

- **main.go:114** by [@mimowo](https://github.com/mimowo) — 2022-12-19T15:02:11Z
```diff
@@ -111,7 +111,7 @@ func main() {
 		close(certsReady)
 	}
 
-	cCache := cache.New(mgr.GetClient())
+	cCache := cache.New(mgr.GetClient(), cache.WithWaitForPodsReady(waitForPodsReady(&cfg)))
```

  renamed the option as `podsReadyTracking`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-19T15:02:41Z

- **pkg/cache/cache.go:208** by [@mimowo](https://github.com/mimowo) — 2022-12-19T15:02:41Z
```diff
@@ -148,6 +177,66 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads() {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.cond.Wait()
+		}
+	}
```

  Applied the same pattern as for the manager.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-19T15:03:02Z

- **pkg/cache/cache.go:61** by [@mimowo](https://github.com/mimowo) — 2022-12-19T15:03:02Z
```diff
@@ -45,25 +47,52 @@ var (
 	errWorkloadNotAdmitted = errors.New("workload not admitted by a ClusterQueue")
 )
 
+type options struct {
+	waitForPodsReady bool
+}
+
+// Option configures the reconciler.
+type Option func(*options)
+
+// WithWaitForPodsReady indicates if the controller should add the PodsReady
+// condition to the workload when the corresponding job has all pods ready
+// or succeeded.
+func WithWaitForPodsReady(f bool) Option {
```

  Renamed to `WithPodsReadyTracking`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-19T15:05:22Z

- **pkg/cache/cache.go:227** by [@mimowo](https://github.com/mimowo) — 2022-12-19T15:05:21Z
```diff
@@ -148,6 +177,66 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads() {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.cond.Wait()
+		}
+	}
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads() bool {
+	workloadsToCheck := []*kueue.Workload{}
+	for _, cq := range c.clusterQueues {
+		for _, wl := range cq.Workloads {
+			workloadsToCheck = append(workloadsToCheck, wl.Obj)
+		}
+	}
+	klog.V(5).InfoS("Workloads to check for PodsReady=true condition", "count", len(workloadsToCheck))
+	for _, wl := range workloadsToCheck {
+		klog.V(5).InfoS("Checking workload", "workload", klog.KObj(wl))
+		if !workload.InCondition(wl, kueue.WorkloadPodsReady) {
+			klog.InfoS("There is a workload which is not in PodsReady condition", "workload", klog.KObj(wl))
+			return false
+		}
+	}
+	klog.V(5).InfoS("All workloads are in the PodsReady condition")
+	return true
+}
+
+func (c *Cache) broadcastToRecheckPodsReady() {
+	c.cond.Broadcast()
+}
+
+func (c *Cache) broadcastToRecheckPodsReadyForDeletedWorkload(w *kueue.Workload) {
+	if c.waitForPodsReady {
+		// deleting a PodsReady=false workload
+		if !workload.InCondition(w, kueue.WorkloadPodsReady) {
+			c.broadcastToRecheckPodsReady()
```

  Inlined

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-12-19T21:08:40Z

- **pkg/cache/cache.go:206** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T19:33:21Z
```diff
@@ -148,6 +178,65 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.podsReadyCond.Wait()
```

  I know this is basic condition mechanics, but I would document here that wait releases the lock while waiting and then acquires again when awaken

- **pkg/cache/cache.go:671** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T21:01:28Z
```diff
@@ -578,6 +668,7 @@ func (c *Cache) DeleteWorkload(w *kueue.Workload) error {
 	}
 
 	c.cleanupAssumedState(w)
+	c.broadcastToRecheckPodsReadyForDeletedWorkload(w)
```

  the function is short and its name is not too friendly, I would inline it:
  
  ```
  c.podsReadyTracking && !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
    c.podsReadyCond.Broadcast()
  }
  ```

- **pkg/cache/cache.go:721** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T21:02:52Z
```diff
@@ -627,6 +718,7 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 		return errCqNotFound
 	}
 	cq.deleteWorkload(w)
+	c.broadcastToRecheckPodsReadyForDeletedWorkload(w)
```

  ditto

- **pkg/cache/cache.go:655** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T21:03:01Z
```diff
@@ -562,6 +651,7 @@ func (c *Cache) UpdateWorkload(oldWl, newWl *kueue.Workload) error {
 	if !ok {
 		return fmt.Errorf("new ClusterQueue doesn't exist")
 	}
+	c.broadcastToRecheckPodsReadyForUpdatedWorkload(oldWl, newWl)
```

  +1 to inlining here

- **pkg/cache/cache.go:203** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T21:08:28Z
```diff
@@ -148,6 +178,65 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.podsReadyCond.Wait()
+		}
+	}
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	log := ctrl.LoggerFrom(ctx)
+	for _, cq := range c.clusterQueues {
+		for _, wl := range cq.Workloads {
```

  Something to think about: what if we track "not ready" workloads in each ClusterQueue, and so instead of iterating over all workloads every time, we just check if that list is empty? This will also make it not expensive to call `podsReadyForAllAdmittedWorkloads`, and so we can remove the checks that we have around broadcast.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T21:29:37Z

- **pkg/cache/cache.go:203** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T21:19:37Z
```diff
@@ -148,6 +178,65 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.podsReadyCond.Wait()
+		}
+	}
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	log := ctrl.LoggerFrom(ctx)
+	for _, cq := range c.clusterQueues {
+		for _, wl := range cq.Workloads {
```

  +1, although I'm fine leaving this as a follow up, to focus on the semantics first.

- **pkg/cache/cache.go:655** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T21:22:53Z
```diff
@@ -562,6 +651,7 @@ func (c *Cache) UpdateWorkload(oldWl, newWl *kueue.Workload) error {
 	if !ok {
 		return fmt.Errorf("new ClusterQueue doesn't exist")
 	}
+	c.broadcastToRecheckPodsReadyForUpdatedWorkload(oldWl, newWl)
```

  Back to this:
  
  > Also you probably need to check when the Workload is first added
  
  when we admit a workload, it could immediately start running and become ready before kueue gets the event that runs `AddOrUpdateWorkload`. So you need to check if the pods are ready and broadcast in that function too.

- **pkg/cache/cache_test.go:1991** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T21:26:13Z
```diff
@@ -1733,6 +1733,44 @@ func TestClusterQueueUpdateCodependentResources(t *testing.T) {
 	}
 }
 
+// TestHeadsCancelled ensures that the Heads call returns when the context is closed.
+func TestHeadsCancelled(t *testing.T) {
```

  ```suggestion
  func TestWaitForPodsReadyCancelled(t *testing.T) {
  ```
  
  Although the test looks just copied from the next one?

- **pkg/scheduler/scheduler.go:158** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T21:29:13Z
```diff
@@ -121,9 +144,19 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			e.inadmissibleMsg = "cohort used in this cycle"
 			continue
 		}
+
+		if s.waitForPodsReady {
+			// Block admission until all currently admitted workloads are in
+			// PodsReady condition if the waitForPodsReady is enabled
+			s.cache.WaitForPodsReady(ctx)
+		}
+
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
 			e.inadmissibleMsg = fmt.Sprintf("Failed to admit workload: %v", err)
+		} else if s.waitForPodsReady {
+			klog.InfoS("Workload successfully admitted when waitForPodsReady - do admit more in this cycle")
```

  do not?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T10:24:07Z

- **pkg/scheduler/scheduler.go:158** by [@mimowo](https://github.com/mimowo) — 2022-12-20T10:24:07Z
```diff
@@ -121,9 +144,19 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			e.inadmissibleMsg = "cohort used in this cycle"
 			continue
 		}
+
+		if s.waitForPodsReady {
+			// Block admission until all currently admitted workloads are in
+			// PodsReady condition if the waitForPodsReady is enabled
+			s.cache.WaitForPodsReady(ctx)
+		}
+
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
 			e.inadmissibleMsg = fmt.Sprintf("Failed to admit workload: %v", err)
+		} else if s.waitForPodsReady {
+			klog.InfoS("Workload successfully admitted when waitForPodsReady - do admit more in this cycle")
```

  Yes, but I think this if is no longer necessary once we moved the waiting inside the loop over heads. Removed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T10:52:32Z

- **pkg/cache/cache_test.go:1991** by [@mimowo](https://github.com/mimowo) — 2022-12-20T10:52:32Z
```diff
@@ -1733,6 +1733,44 @@ func TestClusterQueueUpdateCodependentResources(t *testing.T) {
 	}
 }
 
+// TestHeadsCancelled ensures that the Heads call returns when the context is closed.
+func TestHeadsCancelled(t *testing.T) {
```

  That's right, now fixed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T11:50:34Z

- **pkg/cache/cache.go:721** by [@mimowo](https://github.com/mimowo) — 2022-12-20T11:50:34Z
```diff
@@ -627,6 +718,7 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 		return errCqNotFound
 	}
 	cq.deleteWorkload(w)
+	c.broadcastToRecheckPodsReadyForDeletedWorkload(w)
```

  cleaned up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T11:55:50Z

- **pkg/cache/cache.go:671** by [@mimowo](https://github.com/mimowo) — 2022-12-20T11:55:50Z
```diff
@@ -578,6 +668,7 @@ func (c *Cache) DeleteWorkload(w *kueue.Workload) error {
 	}
 
 	c.cleanupAssumedState(w)
+	c.broadcastToRecheckPodsReadyForDeletedWorkload(w)
```

  I have inlined and just check `podsReadyTracking` since checking the condition in `podsReadyForAllAdmittedWorkloads`  should now be fast after caching the WorkloadsWithPodsReady at the ClusterQueue level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T11:57:26Z

- **pkg/cache/cache.go:206** by [@mimowo](https://github.com/mimowo) — 2022-12-20T11:57:26Z
```diff
@@ -148,6 +178,65 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.podsReadyCond.Wait()
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T11:58:05Z

- **pkg/cache/cache.go:655** by [@mimowo](https://github.com/mimowo) — 2022-12-20T11:58:05Z
```diff
@@ -562,6 +651,7 @@ func (c *Cache) UpdateWorkload(oldWl, newWl *kueue.Workload) error {
 	if !ok {
 		return fmt.Errorf("new ClusterQueue doesn't exist")
 	}
+	c.broadcastToRecheckPodsReadyForUpdatedWorkload(oldWl, newWl)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T11:59:27Z

- **pkg/scheduler/scheduler.go:170** by [@mimowo](https://github.com/mimowo) — 2022-12-20T11:59:27Z
```diff
@@ -124,6 +154,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
```

  Moved the waiting just before admit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T13:00:13Z

- **pkg/cache/cache.go:203** by [@mimowo](https://github.com/mimowo) — 2022-12-20T13:00:13Z
```diff
@@ -148,6 +178,65 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			return
+		default:
+			c.podsReadyCond.Wait()
+		}
+	}
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	log := ctrl.LoggerFrom(ctx)
+	for _, cq := range c.clusterQueues {
+		for _, wl := range cq.Workloads {
```

  Done, I think it also simplifies the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T15:10:37Z

- **pkg/scheduler/scheduler.go:170** by [@mimowo](https://github.com/mimowo) — 2022-12-20T15:10:37Z
```diff
@@ -124,6 +154,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
 		if err := s.admit(ctrl.LoggerInto(ctx, log), e); err != nil {
```

  > Also, it might be useful to produce a Workload status update if the Workload satisfies the quota, but is blocked waiting on others Workloads to be ready.
  
  Now, I set the `WorkloadAdmitted=False` condition with `Reason=Waiting`. It allows me to wait for the situation in the integration test.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T16:27:19Z

- **pkg/cache/cache.go:186** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T15:57:06Z
```diff
@@ -148,6 +179,53 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
```

  ```golang
  if !c.podsReadyTracking { panic }
  ```
  
  or just `return`?

- **pkg/cache/cache.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T15:59:37Z
```diff
@@ -148,6 +179,53 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled, returning for the waiting")
+			return
+		default:
+			// wait releases the lock and acquires again when awaken
+			c.podsReadyCond.Wait()
+		}
+	}
+}
+
+func (c *Cache) PodsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	c.Lock()
+	defer c.Unlock()
+	return c.podsReadyForAllAdmittedWorkloads(ctx)
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	log := ctrl.LoggerFrom(ctx)
+	for _, cq := range c.clusterQueues {
+		if len(cq.WorkloadsWithoutPodsReady) > 0 {
+			log.V(3).Info("There is a cluster queue with not ready workloads", "clusterQueue", cq.Name, "workloads", cq.WorkloadsWithoutPodsReady.List())
```

  ```suggestion
  			log.V(3).Info("There is a ClusterQueue with not ready workloads", "clusterQueue", cq.Name, "workloads", cq.WorkloadsWithoutPodsReady.List())
  ```

- **pkg/cache/cache.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:00:46Z
```diff
@@ -148,6 +179,53 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled, returning for the waiting")
+			return
+		default:
+			// wait releases the lock and acquires again when awaken
+			c.podsReadyCond.Wait()
+		}
+	}
+}
+
+func (c *Cache) PodsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	c.Lock()
+	defer c.Unlock()
+	return c.podsReadyForAllAdmittedWorkloads(ctx)
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	log := ctrl.LoggerFrom(ctx)
+	for _, cq := range c.clusterQueues {
+		if len(cq.WorkloadsWithoutPodsReady) > 0 {
+			log.V(3).Info("There is a cluster queue with not ready workloads", "clusterQueue", cq.Name, "workloads", cq.WorkloadsWithoutPodsReady.List())
```

  also, wrap it with `if logV := log.V(3); logV.Enabled() { }`, otherwise we pay the cost of calling `List` and serializing every time.
  Or just remove the list... I don't think it will be that useful.

- **pkg/cache/cache.go:194** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:03:25Z
```diff
@@ -148,6 +179,53 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled, returning for the waiting")
```

  ```suggestion
  			log.V(5).Info("Context cancelled when waiting for pods to be ready; returning")
  ```

- **pkg/cache/cache.go:344** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:06:12Z
```diff
@@ -263,6 +341,9 @@ func (c *ClusterQueue) addWorkload(w *kueue.Workload) error {
 	wi := workload.NewInfo(w)
 	c.Workloads[k] = wi
 	c.updateWorkloadUsage(wi, 1)
+	if !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
```

  also, if `c.podsReadyTracking`

- **pkg/cache/cache.go:723** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:09:16Z
```diff
@@ -627,6 +720,7 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 		return errCqNotFound
 	}
 	cq.deleteWorkload(w)
+	c.podsReadyCond.Broadcast()
```

  `if c.podsReadyTracking`

- **pkg/cache/cache_test.go:1807** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:10:59Z
```diff
@@ -1770,6 +1771,303 @@ func TestMatchingClusterQueues(t *testing.T) {
 	}
 }
 
+// TestWaitForPodsReadyCancelled ensures that the WaitForPodsReady call does not block when the context is closed.
+func TestWaitForPodsReadyCancelled(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	cache := New(cl, WithPodsReadyTracking(true))
+	ctx, cancel := context.WithCancel(context.Background())
+
+	cq := kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{Name: "one"},
+	}
+	if err := cache.AddClusterQueue(ctx, &cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue: %v", err)
+	}
+
+	wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+		ClusterQueue: "one",
+	}).Obj()
+	if err := cache.AssumeWorkload(wl); err != nil {
+		t.Fatalf("Failed assuming the workload to block the further admission: %v", err)
+	}
+
+	if cache.podsReadyForAllAdmittedWorkloads(ctx) {
+		t.Fatalf("Unexpected that all admitted workloads are in PodsReady condition")
+	}
+
+	// cancel the context so that the WaitForPodsReady is returns
+	go func() {
+		cancel()
+		cache.CleanUpOnContext(ctx)
+	}()
```

  ```suggestion
  	go cancel()
  ```
  
  `CleanUpOnContext` could be called before.

- **pkg/cache/cache.go:367** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:14:09Z
```diff
@@ -274,6 +355,9 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 		return
 	}
 	c.updateWorkloadUsage(wi, -1)
+	if !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
+		c.WorkloadsWithoutPodsReady.Delete(k)
```

  what about calling broadcast here?

- **test/integration/scheduler/podsready/scheduler_test.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:19:44Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
```

  To simplify the test:
  
  ```suggestion
  		defaultFlavor = testing.MakeResourceFlavor("default").Obj()
  ```

- **test/integration/scheduler/podsready/scheduler_test.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:23:48Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
+	})
+
+	ginkgo.When("Scheduling workloads on clusterQueues", func() {
+		var (
+			prodClusterQ *kueue.ClusterQueue
+			devClusterQ  *kueue.ClusterQueue
+			prodQueue    *kueue.LocalQueue
+			devQueue     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+			prodClusterQ = testing.MakeClusterQueue("prod-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+			devClusterQ = testing.MakeClusterQueue("dev-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+			prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		})
+
+		ginkgo.It("Should unblock admission of new workloads once the admitted workload is in PodsReady condition", func() {
+			ginkgo.By("checking the first prod workload gets admitted")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, prodWl)
+
+			ginkgo.By("verifying that the second workload is waiting as the first one has PodsReady=False")
+			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeWaiting(ctx, k8sClient, devWl)
```

  I think this test is too gentle:
  
  ```suggestion
  			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
  			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
  			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, prodWl)
  			framework.ExpectWorkloadsToBeWaiting(ctx, k8sClient, devWl)
  ```
  
  Basically, create the workloads in rapid succession to increase the chances of race conditions.
  
  In particular, this would ensure that `AssumeWorkload` also puts the workload in the list of unready workloads (which I think it does already).

- **test/integration/scheduler/podsready/scheduler_test.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:26:52Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
```

  Also, you can create the RF once for the suite in `BeforeSuite`. We don't really need to be recreating it, making our tests cases a bit slower.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T16:29:41Z

- **test/integration/scheduler/podsready/scheduler_test.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T16:29:41Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
```

  Although.... creating a suite is expensive. I think it's perfectly fine to have it `test/integration/scheduler` with a separate `ginkgo.Context`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T16:45:26Z

- **test/integration/scheduler/podsready/scheduler_test.go:95** by [@mimowo](https://github.com/mimowo) — 2022-12-20T16:45:25Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
+	})
+
+	ginkgo.When("Scheduling workloads on clusterQueues", func() {
+		var (
+			prodClusterQ *kueue.ClusterQueue
+			devClusterQ  *kueue.ClusterQueue
+			prodQueue    *kueue.LocalQueue
+			devQueue     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+			prodClusterQ = testing.MakeClusterQueue("prod-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+			devClusterQ = testing.MakeClusterQueue("dev-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+			prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		})
+
+		ginkgo.It("Should unblock admission of new workloads once the admitted workload is in PodsReady condition", func() {
+			ginkgo.By("checking the first prod workload gets admitted")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, prodWl)
+
+			ginkgo.By("verifying that the second workload is waiting as the first one has PodsReady=False")
+			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeWaiting(ctx, k8sClient, devWl)
```

  ack, also just added a new test which makes sure both workloads are considered in the same scheduling cycle

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T16:47:29Z

- **pkg/cache/cache.go:367** by [@mimowo](https://github.com/mimowo) — 2022-12-20T16:47:29Z
```diff
@@ -274,6 +355,9 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 		return
 	}
 	c.updateWorkloadUsage(wi, -1)
+	if !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
+		c.WorkloadsWithoutPodsReady.Delete(k)
```

  I was thinking about it, it would work. The only reason I haven't done this is performance consideration. For example, when `UpdateWorkload` is called it would result in two broadcasts - one after delete and one after add. But maybe this is not a concern, especially as now checking the condition is faster, what do you think?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T16:53:21Z

- **pkg/cache/cache.go:723** by [@mimowo](https://github.com/mimowo) — 2022-12-20T16:53:20Z
```diff
@@ -627,6 +720,7 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 		return errCqNotFound
 	}
 	cq.deleteWorkload(w)
+	c.podsReadyCond.Broadcast()
```

  fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T16:57:45Z

- **pkg/cache/cache.go:344** by [@mimowo](https://github.com/mimowo) — 2022-12-20T16:57:45Z
```diff
@@ -263,6 +341,9 @@ func (c *ClusterQueue) addWorkload(w *kueue.Workload) error {
 	wi := workload.NewInfo(w)
 	c.Workloads[k] = wi
 	c.updateWorkloadUsage(wi, 1)
+	if !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
```

  we are here in cluster queue, so also needed to pass the `podsReadyTracking` down from cache.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T17:02:41Z

- **pkg/cache/cache_test.go:1807** by [@mimowo](https://github.com/mimowo) — 2022-12-20T17:02:41Z
```diff
@@ -1770,6 +1771,303 @@ func TestMatchingClusterQueues(t *testing.T) {
 	}
 }
 
+// TestWaitForPodsReadyCancelled ensures that the WaitForPodsReady call does not block when the context is closed.
+func TestWaitForPodsReadyCancelled(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	cache := New(cl, WithPodsReadyTracking(true))
+	ctx, cancel := context.WithCancel(context.Background())
+
+	cq := kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{Name: "one"},
+	}
+	if err := cache.AddClusterQueue(ctx, &cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue: %v", err)
+	}
+
+	wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+		ClusterQueue: "one",
+	}).Obj()
+	if err := cache.AssumeWorkload(wl); err != nil {
+		t.Fatalf("Failed assuming the workload to block the further admission: %v", err)
+	}
+
+	if cache.podsReadyForAllAdmittedWorkloads(ctx) {
+		t.Fatalf("Unexpected that all admitted workloads are in PodsReady condition")
+	}
+
+	// cancel the context so that the WaitForPodsReady is returns
+	go func() {
+		cancel()
+		cache.CleanUpOnContext(ctx)
+	}()
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T17:04:54Z

- **pkg/cache/cache.go:213** by [@mimowo](https://github.com/mimowo) — 2022-12-20T17:04:54Z
```diff
@@ -148,6 +179,53 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
+	c.Lock()
+	defer c.Unlock()
+
+	log := ctrl.LoggerFrom(ctx)
+	for {
+		if c.podsReadyForAllAdmittedWorkloads(ctx) {
+			return
+		}
+		log.V(3).Info("Blocking admission as not all workloads are in the PodsReady condition")
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled, returning for the waiting")
+			return
+		default:
+			// wait releases the lock and acquires again when awaken
+			c.podsReadyCond.Wait()
+		}
+	}
+}
+
+func (c *Cache) PodsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	c.Lock()
+	defer c.Unlock()
+	return c.podsReadyForAllAdmittedWorkloads(ctx)
+}
+
+func (c *Cache) podsReadyForAllAdmittedWorkloads(ctx context.Context) bool {
+	log := ctrl.LoggerFrom(ctx)
+	for _, cq := range c.clusterQueues {
+		if len(cq.WorkloadsWithoutPodsReady) > 0 {
+			log.V(3).Info("There is a cluster queue with not ready workloads", "clusterQueue", cq.Name, "workloads", cq.WorkloadsWithoutPodsReady.List())
```

  yup, no longer logging it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T17:08:03Z

- **pkg/cache/cache.go:186** by [@mimowo](https://github.com/mimowo) — 2022-12-20T17:08:03Z
```diff
@@ -148,6 +179,53 @@ func (c *Cache) newClusterQueue(cq *kueue.ClusterQueue) (*ClusterQueue, error) {
 	return cqImpl, nil
 }
 
+func (c *Cache) WaitForPodsReady(ctx context.Context) {
```

  just returning. Returning makes it possible to test what happens

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T17:44:47Z

- **test/integration/scheduler/podsready/scheduler_test.go:51** by [@mimowo](https://github.com/mimowo) — 2022-12-20T17:44:47Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
```

  Hm, I'm wondering about the new context instead of suite though, I have two concerns:
  - the framework is currently setup in BeforeSuite, so it would require refactoring to move the configuration from `suite_test.go` to `scheduler_test.go`, then it no longer can be BeforeSuite but rather BeforeAll or BeforeEach.
  - new context means that tests within the suite can no longer execute in parallel (do we support [Serial] tag in Kueue?), it may actually result in worse performance.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T17:53:35Z

- **test/integration/scheduler/podsready/scheduler_test.go:95** by [@mimowo](https://github.com/mimowo) — 2022-12-20T17:53:35Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
+	})
+
+	ginkgo.When("Scheduling workloads on clusterQueues", func() {
+		var (
+			prodClusterQ *kueue.ClusterQueue
+			devClusterQ  *kueue.ClusterQueue
+			prodQueue    *kueue.LocalQueue
+			devQueue     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+			prodClusterQ = testing.MakeClusterQueue("prod-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+			devClusterQ = testing.MakeClusterQueue("dev-cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+			prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		})
+
+		ginkgo.It("Should unblock admission of new workloads once the admitted workload is in PodsReady condition", func() {
+			ginkgo.By("checking the first prod workload gets admitted")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, prodWl)
+
+			ginkgo.By("verifying that the second workload is waiting as the first one has PodsReady=False")
+			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeWaiting(ctx, k8sClient, devWl)
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T18:17:40Z

/approve
Just nits left

You can squash as you address them.

- **pkg/cache/cache.go:367** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T17:55:54Z
```diff
@@ -274,6 +355,9 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 		return
 	}
 	c.updateWorkloadUsage(wi, -1)
+	if !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
+		c.WorkloadsWithoutPodsReady.Delete(k)
```

  I think calling broadcast more than once might be fine... because the this function still has the lock, so those two broadcasts would be "collapsed" into one.
  
  However, this is a function of ClusterQueue, which doesn't have access to the lock. So leaving it outside makes more sense.

- **pkg/cache/cache_test.go:2058** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:00:21Z
```diff
@@ -1770,6 +1771,302 @@ func TestMatchingClusterQueues(t *testing.T) {
 	}
 }
 
+// TestWaitForPodsReadyCancelled ensures that the WaitForPodsReady call does not block when the context is closed.
+func TestWaitForPodsReadyCancelled(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	cache := New(cl, WithPodsReadyTracking(true))
+	ctx, cancel := context.WithCancel(context.Background())
+
+	go cache.CleanUpOnContext(ctx)
+
+	cq := kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{Name: "one"},
+	}
+	if err := cache.AddClusterQueue(ctx, &cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue: %v", err)
+	}
+
+	wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+		ClusterQueue: "one",
+	}).Obj()
+	if err := cache.AssumeWorkload(wl); err != nil {
+		t.Fatalf("Failed assuming the workload to block the further admission: %v", err)
+	}
+
+	if cache.podsReadyForAllAdmittedWorkloads(ctx) {
+		t.Fatalf("Unexpected that all admitted workloads are in PodsReady condition")
+	}
+
+	// cancel the context so that the WaitForPodsReady is returns
+	go cancel()
+
+	cache.WaitForPodsReady(ctx)
+}
+
+// TestCachePodsReadyForAllAdmittedWorkloads verifies the condition used to determine whether to wait
+func TestCachePodsReadyForAllAdmittedWorkloads(t *testing.T) {
+	clusterQueues := []kueue.ClusterQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+		},
+	}
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	tests := []struct {
+		name                                 string
+		setup                                func(cache *Cache) error
+		operation                            func(cache *Cache) error
+		wantPodsReadyForAllAdmittedWorkloads bool
+	}{
+		{
+			name:                                 "empty cache",
+			operation:                            func(cache *Cache) error { return nil },
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "add Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "assume Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume second workload without PodsReady",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				return cache.AssumeWorkload(wl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "update second workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl2)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := cache.clusterQueues["two"].Workloads["/b"].Obj
+				newWl2 := wl2.DeepCopy()
+				apimeta.SetStatusCondition(&newWl2.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl2, newWl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "delete workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.DeleteWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "forget workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.ForgetWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(cl, WithPodsReadyTracking(true))
+			ctx := context.Background()
+
+			for _, c := range clusterQueues {
+				if err := cache.AddClusterQueue(ctx, &c); err != nil {
+					t.Fatalf("Failed adding clusterQueue: %v", err)
+				}
+			}
+			if tc.setup != nil {
+				if err := tc.setup(cache); err != nil {
+					t.Errorf("Unexpected error during setup: %q", err)
+				}
+			}
+			if err := tc.operation(cache); err != nil {
+				t.Errorf("Unexpected error during operation: %q", err)
+			}
+			gotPodsReadyForAllAdmittedWorkloads := cache.podsReadyForAllAdmittedWorkloads(ctx)
```

  ```suggestion
  			gotPodsReadyForAllAdmittedWorkloads := cache.PodsReadyForAllAdmittedWorkloads(ctx)
  ```

- **pkg/cache/cache_test.go:2059** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:02:08Z
```diff
@@ -1770,6 +1771,302 @@ func TestMatchingClusterQueues(t *testing.T) {
 	}
 }
 
+// TestWaitForPodsReadyCancelled ensures that the WaitForPodsReady call does not block when the context is closed.
+func TestWaitForPodsReadyCancelled(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	cache := New(cl, WithPodsReadyTracking(true))
+	ctx, cancel := context.WithCancel(context.Background())
+
+	go cache.CleanUpOnContext(ctx)
+
+	cq := kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{Name: "one"},
+	}
+	if err := cache.AddClusterQueue(ctx, &cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue: %v", err)
+	}
+
+	wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+		ClusterQueue: "one",
+	}).Obj()
+	if err := cache.AssumeWorkload(wl); err != nil {
+		t.Fatalf("Failed assuming the workload to block the further admission: %v", err)
+	}
+
+	if cache.podsReadyForAllAdmittedWorkloads(ctx) {
+		t.Fatalf("Unexpected that all admitted workloads are in PodsReady condition")
+	}
+
+	// cancel the context so that the WaitForPodsReady is returns
+	go cancel()
+
+	cache.WaitForPodsReady(ctx)
+}
+
+// TestCachePodsReadyForAllAdmittedWorkloads verifies the condition used to determine whether to wait
+func TestCachePodsReadyForAllAdmittedWorkloads(t *testing.T) {
+	clusterQueues := []kueue.ClusterQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+		},
+	}
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	tests := []struct {
+		name                                 string
+		setup                                func(cache *Cache) error
+		operation                            func(cache *Cache) error
+		wantPodsReadyForAllAdmittedWorkloads bool
+	}{
+		{
+			name:                                 "empty cache",
+			operation:                            func(cache *Cache) error { return nil },
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "add Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "assume Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume second workload without PodsReady",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				return cache.AssumeWorkload(wl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "update second workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl2)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := cache.clusterQueues["two"].Workloads["/b"].Obj
+				newWl2 := wl2.DeepCopy()
+				apimeta.SetStatusCondition(&newWl2.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl2, newWl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "delete workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.DeleteWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "forget workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.ForgetWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(cl, WithPodsReadyTracking(true))
+			ctx := context.Background()
+
+			for _, c := range clusterQueues {
+				if err := cache.AddClusterQueue(ctx, &c); err != nil {
+					t.Fatalf("Failed adding clusterQueue: %v", err)
+				}
+			}
+			if tc.setup != nil {
+				if err := tc.setup(cache); err != nil {
+					t.Errorf("Unexpected error during setup: %q", err)
+				}
+			}
+			if err := tc.operation(cache); err != nil {
+				t.Errorf("Unexpected error during operation: %q", err)
+			}
+			gotPodsReadyForAllAdmittedWorkloads := cache.podsReadyForAllAdmittedWorkloads(ctx)
+			if diff := cmp.Diff(tc.wantPodsReadyForAllAdmittedWorkloads, gotPodsReadyForAllAdmittedWorkloads); diff != "" {
```

  nit:
  
  ```suggestion
  			if diff := cmp.Diff(tc.wantReady, gotReady); diff != "" {
  ```
  
  the rest can be inferred from the name of the Test, no need to make this line so long :)

- **test/integration/scheduler/podsready/scheduler_test.go:131** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:04:40Z
```diff
@@ -0,0 +1,156 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Obj()
+	})
+
+	ginkgo.When("Scheduling workloads on clusterQueues", func() {
+		var (
+			prodClusterQ *kueue.ClusterQueue
+			devClusterQ  *kueue.ClusterQueue
+			prodQueue    *kueue.LocalQueue
+			devQueue     *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+			prodClusterQ = testing.MakeClusterQueue("prod-cq").
+				Cohort("all").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodClusterQ)).Should(gomega.Succeed())
+
+			devClusterQ = testing.MakeClusterQueue("dev-cq").
+				Cohort("all").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(defaultFlavor.Name, "5").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, devClusterQ)).Should(gomega.Succeed())
+
+			prodQueue = testing.MakeLocalQueue("prod-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodQueue)).Should(gomega.Succeed())
+
+			devQueue = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, devQueue)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, prodClusterQ, true)
+			framework.ExpectClusterQueueToBeDeleted(ctx, k8sClient, devClusterQ, true)
+			framework.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		})
+
+		ginkgo.It("Should unblock admission of new workloads once the admitted workload is in PodsReady condition", func() {
+			ginkgo.By("checking the first workload gets admitted while the second is waiting")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, prodWl)
+			framework.ExpectWorkloadsToBeWaiting(ctx, k8sClient, devWl)
+
+			ginkgo.By("update the first workload to be in the PodsReady condition and verify the second workload is admitted")
+			prodKey := types.NamespacedName{Name: prodWl.Name, Namespace: prodWl.Namespace}
+			gomega.Expect(k8sClient.Get(ctx, prodKey, prodWl)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&prodWl.Status.Conditions, metav1.Condition{
+				Type:   kueue.WorkloadPodsReady,
+				Status: metav1.ConditionTrue,
+				Reason: "PodsReady",
+			})
+			gomega.Expect(k8sClient.Status().Update(ctx, prodWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devClusterQ.Name, devWl)
+		})
+
+		ginkgo.It("Should unblock admission of new workloads once the admitted workload is deleted", func() {
+			ginkgo.By("checking the first workload gets admitted while the second is waiting")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, prodWl)
+			framework.ExpectWorkloadsToBeWaiting(ctx, k8sClient, devWl)
+
+			ginkgo.By("delete the first workload and verify the second workload is admitted")
+			gomega.Expect(k8sClient.Delete(ctx, prodWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devClusterQ.Name, devWl)
+		})
+
+		ginkgo.It("Should block admission of one new workload if two are considered in the same scheduling cycle", func() {
+			ginkgo.By("creating two workloads but delaying cluster queue creation which has enough capacity")
+			prodWl := testing.MakeWorkload("prod-wl", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
+			devWl := testing.MakeWorkload("dev-wl", ns.Name).Queue(devQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+			gomega.Expect(k8sClient.Create(ctx, devWl)).Should(gomega.Succeed())
+			framework.ExpectWorkloadsToBePending(ctx, k8sClient, prodWl, devWl)
+
+			ginkgo.By("creating the cluster queue to ensure the two workloads are considered in the same scheduling cycle")
```

  The rest is already explained in the first `By` and in the comment
  
  ```suggestion
  			ginkgo.By("creating the cluster queue")
  ```

- **test/integration/scheduler/podsready/scheduler_test.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:16:49Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
```

  Ah right... we share the same controllers across the suite.
  
  Separate suite makes sense. I didn't know about `BeforeAll`, we should be using it in other tests. And we should create the common RF either `BeforeSuite` or `BeforeAll`. But that's a nit.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-12-20T18:51:05Z

- **pkg/scheduler/scheduler.go:163** by [@ahg-g](https://github.com/ahg-g) — 2022-12-20T18:39:50Z
```diff
@@ -121,6 +144,21 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			e.inadmissibleMsg = "cohort used in this cycle"
 			continue
 		}
+
+		if s.waitForPodsReady {
+			if !s.cache.PodsReadyForAllAdmittedWorkloads(ctx) {
+				log.V(5).Info("Waiting for all admitted workloads to be in the PodsReady condition")
+				// Block admission until all currently admitted workloads are in
+				// PodsReady condition if the waitForPodsReady is enabled
+				if err := workload.UpdateStatus(ctx, s.client, e.Obj, kueue.WorkloadAdmitted, metav1.ConditionFalse, "Waiting", "waiting for all admitted workloads to be in PodsReady condition"); err != nil {
+					log.Error(err, "Could not update Workload status")
+					continue
```

  why `continue` here? failing to update the status here isn't critical, we shouldn't give up on admitting the workload here.

- **pkg/cache/cache.go:131** by [@ahg-g](https://github.com/ahg-g) — 2022-12-20T18:45:25Z
```diff
@@ -94,12 +123,13 @@ const (
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds admitted workloads.
 type ClusterQueue struct {
-	Name                 string
-	Cohort               *Cohort
-	RequestableResources map[corev1.ResourceName]*Resource
-	UsedResources        ResourceQuantities
-	Workloads            map[string]*workload.Info
-	NamespaceSelector    labels.Selector
+	Name                      string
+	Cohort                    *Cohort
+	RequestableResources      map[corev1.ResourceName]*Resource
+	UsedResources             ResourceQuantities
+	Workloads                 map[string]*workload.Info
+	WorkloadsWithoutPodsReady sets.String
```

  I am trying to find a better name, this is hard to parse, may be `NotReadyWorkloads` or `WorkloadsNotReady` to keep Workloads are the prefix?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T07:56:18Z

- **pkg/cache/cache.go:131** by [@mimowo](https://github.com/mimowo) — 2022-12-21T07:56:18Z
```diff
@@ -94,12 +123,13 @@ const (
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds admitted workloads.
 type ClusterQueue struct {
-	Name                 string
-	Cohort               *Cohort
-	RequestableResources map[corev1.ResourceName]*Resource
-	UsedResources        ResourceQuantities
-	Workloads            map[string]*workload.Info
-	NamespaceSelector    labels.Selector
+	Name                      string
+	Cohort                    *Cohort
+	RequestableResources      map[corev1.ResourceName]*Resource
+	UsedResources             ResourceQuantities
+	Workloads                 map[string]*workload.Info
+	WorkloadsWithoutPodsReady sets.String
```

  I like `WorkloadsNotReady`, renamed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T08:03:19Z

- **pkg/scheduler/scheduler.go:163** by [@mimowo](https://github.com/mimowo) — 2022-12-21T08:03:19Z
```diff
@@ -121,6 +144,21 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			e.inadmissibleMsg = "cohort used in this cycle"
 			continue
 		}
+
+		if s.waitForPodsReady {
+			if !s.cache.PodsReadyForAllAdmittedWorkloads(ctx) {
+				log.V(5).Info("Waiting for all admitted workloads to be in the PodsReady condition")
+				// Block admission until all currently admitted workloads are in
+				// PodsReady condition if the waitForPodsReady is enabled
+				if err := workload.UpdateStatus(ctx, s.client, e.Obj, kueue.WorkloadAdmitted, metav1.ConditionFalse, "Waiting", "waiting for all admitted workloads to be in PodsReady condition"); err != nil {
+					log.Error(err, "Could not update Workload status")
+					continue
```

  My reasoning was that if the update fails it is likely due to conflicts as `e` holds an outdated workload state, so the actual admission is also likely to fail.
  
  However, we need to be prepared anyway to handle outdated workload state during admission anyway, so we should be ok with trying admission (and it may actually succeed.  Reverting the `continue`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T08:06:54Z

- **pkg/cache/cache_test.go:2059** by [@mimowo](https://github.com/mimowo) — 2022-12-21T08:06:54Z
```diff
@@ -1770,6 +1771,302 @@ func TestMatchingClusterQueues(t *testing.T) {
 	}
 }
 
+// TestWaitForPodsReadyCancelled ensures that the WaitForPodsReady call does not block when the context is closed.
+func TestWaitForPodsReadyCancelled(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	cache := New(cl, WithPodsReadyTracking(true))
+	ctx, cancel := context.WithCancel(context.Background())
+
+	go cache.CleanUpOnContext(ctx)
+
+	cq := kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{Name: "one"},
+	}
+	if err := cache.AddClusterQueue(ctx, &cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue: %v", err)
+	}
+
+	wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+		ClusterQueue: "one",
+	}).Obj()
+	if err := cache.AssumeWorkload(wl); err != nil {
+		t.Fatalf("Failed assuming the workload to block the further admission: %v", err)
+	}
+
+	if cache.podsReadyForAllAdmittedWorkloads(ctx) {
+		t.Fatalf("Unexpected that all admitted workloads are in PodsReady condition")
+	}
+
+	// cancel the context so that the WaitForPodsReady is returns
+	go cancel()
+
+	cache.WaitForPodsReady(ctx)
+}
+
+// TestCachePodsReadyForAllAdmittedWorkloads verifies the condition used to determine whether to wait
+func TestCachePodsReadyForAllAdmittedWorkloads(t *testing.T) {
+	clusterQueues := []kueue.ClusterQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+		},
+	}
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	tests := []struct {
+		name                                 string
+		setup                                func(cache *Cache) error
+		operation                            func(cache *Cache) error
+		wantPodsReadyForAllAdmittedWorkloads bool
+	}{
+		{
+			name:                                 "empty cache",
+			operation:                            func(cache *Cache) error { return nil },
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "add Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "assume Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume second workload without PodsReady",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				return cache.AssumeWorkload(wl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "update second workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl2)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := cache.clusterQueues["two"].Workloads["/b"].Obj
+				newWl2 := wl2.DeepCopy()
+				apimeta.SetStatusCondition(&newWl2.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl2, newWl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "delete workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.DeleteWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "forget workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.ForgetWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(cl, WithPodsReadyTracking(true))
+			ctx := context.Background()
+
+			for _, c := range clusterQueues {
+				if err := cache.AddClusterQueue(ctx, &c); err != nil {
+					t.Fatalf("Failed adding clusterQueue: %v", err)
+				}
+			}
+			if tc.setup != nil {
+				if err := tc.setup(cache); err != nil {
+					t.Errorf("Unexpected error during setup: %q", err)
+				}
+			}
+			if err := tc.operation(cache); err != nil {
+				t.Errorf("Unexpected error during operation: %q", err)
+			}
+			gotPodsReadyForAllAdmittedWorkloads := cache.podsReadyForAllAdmittedWorkloads(ctx)
+			if diff := cmp.Diff(tc.wantPodsReadyForAllAdmittedWorkloads, gotPodsReadyForAllAdmittedWorkloads); diff != "" {
```

  Right, renamed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T08:06:58Z

- **pkg/cache/cache_test.go:2058** by [@mimowo](https://github.com/mimowo) — 2022-12-21T08:06:58Z
```diff
@@ -1770,6 +1771,302 @@ func TestMatchingClusterQueues(t *testing.T) {
 	}
 }
 
+// TestWaitForPodsReadyCancelled ensures that the WaitForPodsReady call does not block when the context is closed.
+func TestWaitForPodsReadyCancelled(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	cache := New(cl, WithPodsReadyTracking(true))
+	ctx, cancel := context.WithCancel(context.Background())
+
+	go cache.CleanUpOnContext(ctx)
+
+	cq := kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{Name: "one"},
+	}
+	if err := cache.AddClusterQueue(ctx, &cq); err != nil {
+		t.Fatalf("Failed adding clusterQueue: %v", err)
+	}
+
+	wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+		ClusterQueue: "one",
+	}).Obj()
+	if err := cache.AssumeWorkload(wl); err != nil {
+		t.Fatalf("Failed assuming the workload to block the further admission: %v", err)
+	}
+
+	if cache.podsReadyForAllAdmittedWorkloads(ctx) {
+		t.Fatalf("Unexpected that all admitted workloads are in PodsReady condition")
+	}
+
+	// cancel the context so that the WaitForPodsReady is returns
+	go cancel()
+
+	cache.WaitForPodsReady(ctx)
+}
+
+// TestCachePodsReadyForAllAdmittedWorkloads verifies the condition used to determine whether to wait
+func TestCachePodsReadyForAllAdmittedWorkloads(t *testing.T) {
+	clusterQueues := []kueue.ClusterQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+		},
+	}
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+	cl := fake.NewClientBuilder().WithScheme(scheme).Build()
+
+	tests := []struct {
+		name                                 string
+		setup                                func(cache *Cache) error
+		operation                            func(cache *Cache) error
+		wantPodsReadyForAllAdmittedWorkloads bool
+	}{
+		{
+			name:                                 "empty cache",
+			operation:                            func(cache *Cache) error { return nil },
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "add Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "add Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "assume Workload without PodsReady condition",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=False",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume Workload with PodsReady=True",
+			operation: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "update workload to have PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				newWl := wl.DeepCopy()
+				apimeta.SetStatusCondition(&newWl.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				})
+				return cache.UpdateWorkload(wl, newWl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "assume second workload without PodsReady",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				return cache.AssumeWorkload(wl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: false,
+		},
+		{
+			name: "update second workload to have PodsReady=True",
+			setup: func(cache *Cache) error {
+				wl1 := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl1)
+				wl2 := utiltesting.MakeWorkload("b", "").Admit(&kueue.Admission{
+					ClusterQueue: "two",
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl2)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl2 := cache.clusterQueues["two"].Workloads["/b"].Obj
+				newWl2 := wl2.DeepCopy()
+				apimeta.SetStatusCondition(&newWl2.Status.Conditions, metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionTrue,
+				})
+				return cache.UpdateWorkload(wl2, newWl2)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "delete workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				cache.AddOrUpdateWorkload(wl)
+				return nil
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.DeleteWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+		{
+			name: "forget workload with PodsReady=False",
+			setup: func(cache *Cache) error {
+				wl := utiltesting.MakeWorkload("a", "").Admit(&kueue.Admission{
+					ClusterQueue: "one",
+				}).Condition(metav1.Condition{
+					Type:   kueue.WorkloadPodsReady,
+					Status: metav1.ConditionFalse,
+				}).Obj()
+				return cache.AssumeWorkload(wl)
+			},
+			operation: func(cache *Cache) error {
+				wl := cache.clusterQueues["one"].Workloads["/a"].Obj
+				return cache.ForgetWorkload(wl)
+			},
+			wantPodsReadyForAllAdmittedWorkloads: true,
+		},
+	}
+	for _, tc := range tests {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(cl, WithPodsReadyTracking(true))
+			ctx := context.Background()
+
+			for _, c := range clusterQueues {
+				if err := cache.AddClusterQueue(ctx, &c); err != nil {
+					t.Fatalf("Failed adding clusterQueue: %v", err)
+				}
+			}
+			if tc.setup != nil {
+				if err := tc.setup(cache); err != nil {
+					t.Errorf("Unexpected error during setup: %q", err)
+				}
+			}
+			if err := tc.operation(cache); err != nil {
+				t.Errorf("Unexpected error during operation: %q", err)
+			}
+			gotPodsReadyForAllAdmittedWorkloads := cache.podsReadyForAllAdmittedWorkloads(ctx)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T08:13:31Z

- **test/integration/scheduler/podsready/scheduler_test.go:51** by [@mimowo](https://github.com/mimowo) — 2022-12-21T08:13:31Z
```diff
@@ -0,0 +1,133 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package podsready
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/integration/framework"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
+	const (
+		instanceKey = "cloud.provider.com/instance"
+	)
+
+	var (
+		ns            *corev1.Namespace
+		defaultFlavor *kueue.ResourceFlavor
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		defaultFlavor = testing.MakeResourceFlavor("default").Label(instanceKey, "default").Obj()
```

  Moved the RF setup to `BeforeSuite`, but it does not make any noticeable speedup on my local machine.
  I think `BeforeAll` might be tricky as it works, but requires the container to be ordered, then all tests within the container are executed sequentially, which in our case might not be necessary.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T08:14:32Z

- **pkg/cache/cache.go:367** by [@mimowo](https://github.com/mimowo) — 2022-12-21T08:14:31Z
```diff
@@ -274,6 +355,9 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 		return
 	}
 	c.updateWorkloadUsage(wi, -1)
+	if !apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadPodsReady) {
+		c.WorkloadsWithoutPodsReady.Delete(k)
```

  we could pass the broadcast function down, but not sure it is the right approach indeed. Keeping as is for now.
