# PR #475: Prevent borrowing when preemption could help

**Summary**: Prevent borrowing when preemption could help

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/475

**Last updated**: 2022-12-21T07:25:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `flavor-modes` → `main`
- **Created**: 2022-12-13T21:06:23Z
- **Updated**: 2022-12-21T07:25:25Z
- **Merged**: 2022-12-21T07:25:25Z
- **Closed**: 2022-12-21T07:25:25Z
- **Labels**: `kind/feature`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 5   **+597 / -152**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Calculate flavor assignments that could be satisfied with preemption.

If we find such assignment, prevent borrowing in the cohort. Actual preemption will be implemented in a follow up.

#### Which issue(s) this PR fixes:

Part of #83 

This also solves what #403 was trying to solve.

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-13T21:06:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/475#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-14T20:42:52Z

/assign @ahg-g

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:59:52Z

squashed into two commits

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-12-20T19:48:30Z

/lgtm
/hold

in case Michal has other comments

### Comment by [@mimowo](https://github.com/mimowo) — 2022-12-21T07:18:40Z

LGTM
/hold cancel

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-16T17:26:53Z

Major comments:
- IIUC when there is a workload requesting more than `>flavor.Min`, but `<flavor.Max` then we don't attempt preemption (neither passive nor active) to make room for it (either `Fit` or `NoFit` mode is returned). IIUC, I suggest to make it clear in the comments and PR description or support.
- I find the description of the modes hard to understand, suggested rephrasing
- I find the logic in `fitsFlavorLimits` hard to follow. Suggested avoiding mutation of `mode` to improve it.

- **pkg/scheduler/flavorassigner/flavorassigner.go:58** by [@mimowo](https://github.com/mimowo) — 2022-12-15T15:27:24Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
 }
 
 func (a *Assignment) Borrows() bool {
 	return len(a.TotalBorrow) > 0
 }
 
+func (a *Assignment) RepresentativeMode() FlavorAssignmentMode {
+	if a.repMode != nil {
+		return *a.repMode
+	}
+	if len(a.PodSets) == 0 {
```

  Wondering if we need this special case - do we in general allow for workloads with empty PodSets? It seems that for jobs we always have one PodSet. I think if we aim to support empty PodSets then should also write tests for it.

- **pkg/scheduler/flavorassigner/flavorassigner.go:129** by [@mimowo](https://github.com/mimowo) — 2022-12-15T15:45:59Z
```diff
@@ -55,38 +102,62 @@ func (a *Assignment) ToAPI() []kueue.PodSetFlavors {
 }
 
 type Status struct {
-	podSet  string
 	reasons []string
 	err     error
 }
 
-func (s *Status) IsSuccess() bool {
-	return s == nil
-}
-
 func (s *Status) IsError() bool {
 	return s != nil && s.err != nil
 }
 
-func (s *Status) append(r string) {
-	s.reasons = append(s.reasons, r)
+func (s *Status) append(r ...string) *Status {
+	s.reasons = append(s.reasons, r...)
+	return s
 }
 
 func (s *Status) Message() string {
-	if s.IsSuccess() {
+	if s == nil {
 		return ""
 	}
 	if s.err != nil {
-		return fmt.Sprintf("Couldn't assign flavors for podSet %s: %v", s.podSet, s.err)
+		return s.err.Error()
 	}
 	sort.Strings(s.reasons)
-	msg := strings.Join(s.reasons, "; ")
-	return fmt.Sprintf("Workload's %q podSet didn't fit: %s", s.podSet, msg)
+	return strings.Join(s.reasons, ", ")
+}
+
+func (s *Status) Equal(o *Status) bool {
```

  It does not seem to be used.

- **pkg/scheduler/flavorassigner/flavorassigner.go:190** by [@mimowo](https://github.com/mimowo) — 2022-12-16T08:52:09Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
+	NoFit FlavorAssignmentMode = iota
+	// ClusterQueuePreempt means that there is not enough min quota in the
+	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
+	// with lower priority.
+	ClusterQueuePreempt
+	// CohortReclaim means that there is enough unused min quota in the
+	// ClusterQueue, but it needs to be reclaimed from the cohort.
+	CohortReclaim
+	// Fit means that there is not enough unused quota in the cohort to
```

  ```suggestion
  	// Fit means that there is enough unused quota in the cohort to
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:184** by [@mimowo](https://github.com/mimowo) — 2022-12-16T09:00:51Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
+	NoFit FlavorAssignmentMode = iota
+	// ClusterQueuePreempt means that there is not enough min quota in the
+	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
```

  Maybe just say `... ClusterQueue. We preempt other workloads in the ClusterQueue` - without implying that  preemption follows directly from the mode. It seems that, preemption is one of the possible solutions,  but we could as well just reclaim passively (similarly as for `CohortReclaim`).

- **pkg/scheduler/flavorassigner/flavorassigner.go:181** by [@mimowo](https://github.com/mimowo) — 2022-12-16T15:23:32Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
```

  ```suggestion
  	// NoFit means that there is not enough quota left to assign this flavor.
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:67** by [@mimowo](https://github.com/mimowo) — 2022-12-16T16:02:18Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
 }
 
 func (a *Assignment) Borrows() bool {
 	return len(a.TotalBorrow) > 0
 }
 
+func (a *Assignment) RepresentativeMode() FlavorAssignmentMode {
+	if a.repMode != nil {
+		return *a.repMode
+	}
+	if len(a.PodSets) == 0 {
+		return NoFit
+	}
+	mode := Fit
+	for _, ps := range a.PodSets {
+		psMode := ps.RepresentativeMode()
+		if psMode < mode {
+			mode = psMode
+			if mode == NoFit {
```

  It is not clear to me why the optimization is applied here but not in: `PodSetAssignment.RepresentativeMode()`. Maybe just drop it?

- **pkg/scheduler/flavorassigner/flavorassigner.go:445** by [@mimowo](https://github.com/mimowo) — 2022-12-16T16:33:56Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
```

  when `val > flavor.Min` either `Fit` or `NoFit` is returned - meaning that we don't attempt to trigger preemption, but preemption could help in that case as long as `val <= flavor.Max` by using freed cohort resources. I think we should make it clear or support.

- **pkg/scheduler/flavorassigner/flavorassigner.go:188** by [@mimowo](https://github.com/mimowo) — 2022-12-16T16:50:59Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
+	NoFit FlavorAssignmentMode = iota
+	// ClusterQueuePreempt means that there is not enough min quota in the
+	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
+	// with lower priority.
+	ClusterQueuePreempt
+	// CohortReclaim means that there is enough unused min quota in the
+	// ClusterQueue, but it needs to be reclaimed from the cohort.
```

  Use this as a suggestion and validation if my understanding is correct. Feel free to rephrase: 
  
  ```
  ClusterQueuePreempt means that there is not enough unused min quota in the ClusterQueue.
  Additionally, freeing only resources used by workloads admitted to other cluster queues in the 
  cohort  is not enough to assign this flavor. We reclaim the quota by passively waiting for the 
  borrowing workloads in the cluster queue as well as preemption of lower-priority workloads 
  admitted to the cluster queue.
  
  CohortReclaim means that there is not enough unused min quota in the ClusterQueue, but 
  freeing only resources used by workloads admitted to other cluster queues in the cohort 
  is enough to assign this flavor. We reclaim the quota from the cohort by passively waiting 
  for the borrowing workloads in the cluster queue.
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:435** by [@mimowo](https://github.com/mimowo) — 2022-12-16T17:10:28Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
```

  It seems to me that readability could be improved by moving this `if` into the `if val <= flavor.Min {` branch to avoid mutating `mode` and to make it clear that this happens only if `val <= flavor.Min`. For example, due to the mutability of `mode` it is not obvious if the comment under `val <= flavor.Min`  describes the entering of the branch or the semantics of the mode `ClusterQueuePreempt`  - because the semantics of the mode depends on what happens later.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-12-16T18:38:31Z

We need to document the placement order, something like:

1) If it fits within quota
2) borrowing
3) reclaiming lent quota
4) preempting within the CQ based on priority

and discuss cases that we don't currently support:
1) preempting to borrow
what else?

- **pkg/scheduler/flavorassigner/flavorassigner.go:48** by [@ahg-g](https://github.com/ahg-g) — 2022-12-15T22:50:20Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
```

  assignmentMode so that it is aligned with the type name?

- **pkg/scheduler/flavorassigner/flavorassigner.go:164** by [@ahg-g](https://github.com/ahg-g) — 2022-12-15T23:15:47Z
```diff
@@ -55,38 +102,62 @@ func (a *Assignment) ToAPI() []kueue.PodSetFlavors {
 }
 
 type Status struct {
-	podSet  string
 	reasons []string
 	err     error
 }
 
-func (s *Status) IsSuccess() bool {
-	return s == nil
-}
-
 func (s *Status) IsError() bool {
 	return s != nil && s.err != nil
 }
 
-func (s *Status) append(r string) {
-	s.reasons = append(s.reasons, r)
+func (s *Status) append(r ...string) *Status {
+	s.reasons = append(s.reasons, r...)
+	return s
 }
 
 func (s *Status) Message() string {
-	if s.IsSuccess() {
+	if s == nil {
 		return ""
 	}
 	if s.err != nil {
-		return fmt.Sprintf("Couldn't assign flavors for podSet %s: %v", s.podSet, s.err)
+		return s.err.Error()
 	}
 	sort.Strings(s.reasons)
-	msg := strings.Join(s.reasons, "; ")
-	return fmt.Sprintf("Workload's %q podSet didn't fit: %s", s.podSet, msg)
+	return strings.Join(s.reasons, ", ")
+}
+
+func (s *Status) Equal(o *Status) bool {
+	if s == nil || o == nil {
+		return s == o
+	}
+	if s.err != nil {
+		return errors.Is(s.err, o.err)
+	}
+	return cmp.Equal(s.reasons, o.reasons, cmpopts.SortSlices(func(a, b string) bool {
+		return a < b
+	}))
 }
 
 type PodSetAssignment struct {
 	Name    string
 	Flavors ResourceAssignment
+	Status  *Status
+}
+
+func (psa *PodSetAssignment) RepresentativeMode() FlavorAssignmentMode {
+	if psa.Status == nil {
+		return Fit
+	}
+	if psa.Flavors == nil {
+		return NoFit
+	}
+	mode := Fit
+	for _, flvAssignment := range psa.Flavors {
```

  describe what we are trying to find in this iteration: the flavor with the worst fit mode represents the fit mode for the whole pod assignment

- **pkg/scheduler/flavorassigner/flavorassigner.go:66** by [@ahg-g](https://github.com/ahg-g) — 2022-12-15T23:20:07Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
 }
 
 func (a *Assignment) Borrows() bool {
 	return len(a.TotalBorrow) > 0
 }
 
+func (a *Assignment) RepresentativeMode() FlavorAssignmentMode {
+	if a.repMode != nil {
+		return *a.repMode
+	}
+	if len(a.PodSets) == 0 {
+		return NoFit
+	}
+	mode := Fit
+	for _, ps := range a.PodSets {
```

  describe what we are trying to find: the podSet with the worst fit assignment represent the fit mode of the whole workload

- **pkg/scheduler/flavorassigner/flavorassigner.go:430** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T17:52:23Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
```

  ```suggestion
  		// The request can be satisfied by the min quota, assuming all active workloads
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:437** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T17:57:20Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
```

  ```suggestion
  		// The request can be satisfied by the min quota, assuming all active workloads
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:435** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T17:58:49Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
```

  I think the mode `ClusterQueuePreempt` still applies as we move down the logic, but the logic below tries to find a "better" one, and hence keeps changing.

- **pkg/scheduler/flavorassigner/flavorassigner.go:456** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T18:04:51Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
+		// from other ClusterQueues in the cohort are preempted.
+		mode = CohortReclaim
+	}
 	if flavor.Max != nil && used+val > *flavor.Max {
 		status.append(fmt.Sprintf("borrowing limit for %s flavor %s exceeded", rName, flavor.Name))
-		return 0, &status
+		return mode, 0, &status
```

  this if statement will never be entered if we entered the previous one, perhaps hook them via an else to emphasize that we are now looking at whether we can borrow resources:
  
  `} else if flavor.Max != nil && used+val > *flavor.Max {`

- **pkg/scheduler/flavorassigner/flavorassigner.go:446** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T18:07:25Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
+		// from other ClusterQueues in the cohort are preempted.
+		mode = CohortReclaim
+	}
 	if flavor.Max != nil && used+val > *flavor.Max {
 		status.append(fmt.Sprintf("borrowing limit for %s flavor %s exceeded", rName, flavor.Name))
-		return 0, &status
+		return mode, 0, &status
 	}
 	cohortUsed := used
 	cohortTotal := flavor.Min
```

  can we name it `cohortAvailable`

- **pkg/scheduler/flavorassigner/flavorassigner.go:478** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T18:10:51Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
+		// from other ClusterQueues in the cohort are preempted.
+		mode = CohortReclaim
+	}
 	if flavor.Max != nil && used+val > *flavor.Max {
 		status.append(fmt.Sprintf("borrowing limit for %s flavor %s exceeded", rName, flavor.Name))
-		return 0, &status
+		return mode, 0, &status
 	}
 	cohortUsed := used
 	cohortTotal := flavor.Min
 	if cq.Cohort != nil {
 		cohortUsed = cq.Cohort.UsedResources[rName][flavor.Name]
 		cohortTotal = cq.Cohort.RequestableResources[rName][flavor.Name]
 	}
-	borrow := used + val - flavor.Min
-	if borrow < 0 {
-		borrow = 0
-	}
 
 	lack := cohortUsed + val - cohortTotal
-	if lack > 0 {
-		lackQuantity := workload.ResourceQuantity(rName, lack)
-		if cq.Cohort == nil {
-			status.append(fmt.Sprintf("insufficient quota for %s flavor %s, %s more needed", rName, flavor.Name, &lackQuantity))
+	if lack <= 0 {
+		return Fit, borrow, nil
+	}
```

  I feel the function will be more readable if we move this check and early exit to be done first thing in the function (first, we check if we fit without preemption).

- **pkg/scheduler/flavorassigner/flavorassigner.go:445** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T18:21:37Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
```

  That is a good point, I think we want to support this for higher priority jobs.

- **pkg/scheduler/flavorassigner/flavorassigner.go:366** by [@ahg-g](https://github.com/ahg-g) — 2022-12-16T18:24:52Z
```diff
@@ -219,35 +331,46 @@ func (a *Assignment) findFlavorForCodepResources(
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.NodeSelector}}); !match || err != nil {
 			if err != nil {
 				status.err = err
-				return nil, &status
+				return nil, status
 			}
 			status.append(fmt.Sprintf("flavor %s doesn't match with node affinity", flvLimit.Name))
 			continue
 		}
 
 		assignments := make(ResourceAssignment, len(requests))
+		worstFlavorMode := Fit
 		for name, val := range requests {
 			codepFlvLimit := cq.RequestableResources[name].Flavors[i]
 			// Check considering the flavor usage by previous pod sets.
-			borrow, s := fitsFlavorLimits(name, val+a.usage[name][flavor.Name], cq, &codepFlvLimit)
-			if s.IsError() {
-				return nil, s
-			}
-			if !s.IsSuccess() {
+			mode, borrow, s := fitsFlavorLimits(name, val+a.usage[name][flavor.Name], cq, &codepFlvLimit)
+			if s != nil {
 				status.reasons = append(status.reasons, s.reasons...)
+			}
+			if mode < worstFlavorMode {
+				worstFlavorMode = mode
+			}
+			if worstFlavorMode == NoFit {
+				// The flavor doesn't fit, no need to check other resources.
 				break
 			}
+
 			assignments[name] = &FlavorAssignment{
 				Name:   flavor.Name,
-				Mode:   CohortFit,
+				Mode:   mode,
 				borrow: borrow,
 			}
 		}
-		if len(assignments) == len(requests) {
-			return assignments, nil
+
+		if worstFlavorMode > bestAssignmentMode {
+			bestAssignment = assignments
+			bestAssignmentMode = worstFlavorMode
```

  this is a unintuitive, assigning the worst as the best :); perhaps rename `bestAssignmentMode` to `selectedMode`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T21:02:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T19:25:31Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
```

  I'll use the full word "representative". This field is already part of the struct "Assignment", so I think that would be repetitive.

- **pkg/scheduler/flavorassigner/flavorassigner.go:58** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T19:26:50Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
 }
 
 func (a *Assignment) Borrows() bool {
 	return len(a.TotalBorrow) > 0
 }
 
+func (a *Assignment) RepresentativeMode() FlavorAssignmentMode {
+	if a.repMode != nil {
+		return *a.repMode
+	}
+	if len(a.PodSets) == 0 {
```

  We don't allow for workloads with no podsets (in validation).
  At some point I was thinking that if the assigment has empty podsets, it means that this assigment is impossible, then `NoFit` is the answer. But I still append a PodSetAssignment that has a NoFit mode, so removing this.

- **pkg/scheduler/flavorassigner/flavorassigner.go:66** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T19:31:02Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
 }
 
 func (a *Assignment) Borrows() bool {
 	return len(a.TotalBorrow) > 0
 }
 
+func (a *Assignment) RepresentativeMode() FlavorAssignmentMode {
+	if a.repMode != nil {
+		return *a.repMode
+	}
+	if len(a.PodSets) == 0 {
+		return NoFit
+	}
+	mode := Fit
+	for _, ps := range a.PodSets {
```

  Added as method comment.

- **pkg/scheduler/flavorassigner/flavorassigner.go:67** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T19:37:32Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
 }
 
 func (a *Assignment) Borrows() bool {
 	return len(a.TotalBorrow) > 0
 }
 
+func (a *Assignment) RepresentativeMode() FlavorAssignmentMode {
+	if a.repMode != nil {
+		return *a.repMode
+	}
+	if len(a.PodSets) == 0 {
+		return NoFit
+	}
+	mode := Fit
+	for _, ps := range a.PodSets {
+		psMode := ps.RepresentativeMode()
+		if psMode < mode {
+			mode = psMode
+			if mode == NoFit {
```

  because `PodSetAssignment` has empty `Flavors` if it's `NoFit`.
  But sure, we can drop it, as the expected number of podsets is 1.

- **pkg/scheduler/flavorassigner/flavorassigner.go:129** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T19:49:46Z
```diff
@@ -55,38 +102,62 @@ func (a *Assignment) ToAPI() []kueue.PodSetFlavors {
 }
 
 type Status struct {
-	podSet  string
 	reasons []string
 	err     error
 }
 
-func (s *Status) IsSuccess() bool {
-	return s == nil
-}
-
 func (s *Status) IsError() bool {
 	return s != nil && s.err != nil
 }
 
-func (s *Status) append(r string) {
-	s.reasons = append(s.reasons, r)
+func (s *Status) append(r ...string) *Status {
+	s.reasons = append(s.reasons, r...)
+	return s
 }
 
 func (s *Status) Message() string {
-	if s.IsSuccess() {
+	if s == nil {
 		return ""
 	}
 	if s.err != nil {
-		return fmt.Sprintf("Couldn't assign flavors for podSet %s: %v", s.podSet, s.err)
+		return s.err.Error()
 	}
 	sort.Strings(s.reasons)
-	msg := strings.Join(s.reasons, "; ")
-	return fmt.Sprintf("Workload's %q podSet didn't fit: %s", s.podSet, msg)
+	return strings.Join(s.reasons, ", ")
+}
+
+func (s *Status) Equal(o *Status) bool {
```

  It's necessary for `cmp.Diff`

- **pkg/scheduler/flavorassigner/flavorassigner.go:164** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T19:50:49Z
```diff
@@ -55,38 +102,62 @@ func (a *Assignment) ToAPI() []kueue.PodSetFlavors {
 }
 
 type Status struct {
-	podSet  string
 	reasons []string
 	err     error
 }
 
-func (s *Status) IsSuccess() bool {
-	return s == nil
-}
-
 func (s *Status) IsError() bool {
 	return s != nil && s.err != nil
 }
 
-func (s *Status) append(r string) {
-	s.reasons = append(s.reasons, r)
+func (s *Status) append(r ...string) *Status {
+	s.reasons = append(s.reasons, r...)
+	return s
 }
 
 func (s *Status) Message() string {
-	if s.IsSuccess() {
+	if s == nil {
 		return ""
 	}
 	if s.err != nil {
-		return fmt.Sprintf("Couldn't assign flavors for podSet %s: %v", s.podSet, s.err)
+		return s.err.Error()
 	}
 	sort.Strings(s.reasons)
-	msg := strings.Join(s.reasons, "; ")
-	return fmt.Sprintf("Workload's %q podSet didn't fit: %s", s.podSet, msg)
+	return strings.Join(s.reasons, ", ")
+}
+
+func (s *Status) Equal(o *Status) bool {
+	if s == nil || o == nil {
+		return s == o
+	}
+	if s.err != nil {
+		return errors.Is(s.err, o.err)
+	}
+	return cmp.Equal(s.reasons, o.reasons, cmpopts.SortSlices(func(a, b string) bool {
+		return a < b
+	}))
 }
 
 type PodSetAssignment struct {
 	Name    string
 	Flavors ResourceAssignment
+	Status  *Status
+}
+
+func (psa *PodSetAssignment) RepresentativeMode() FlavorAssignmentMode {
+	if psa.Status == nil {
+		return Fit
+	}
+	if psa.Flavors == nil {
+		return NoFit
+	}
+	mode := Fit
+	for _, flvAssignment := range psa.Flavors {
```

  Done

- **pkg/scheduler/flavorassigner/flavorassigner.go:184** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T19:54:21Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
+	NoFit FlavorAssignmentMode = iota
+	// ClusterQueuePreempt means that there is not enough min quota in the
+	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
```

  rephrased

- **pkg/scheduler/flavorassigner/flavorassigner.go:190** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:01:08Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
+	NoFit FlavorAssignmentMode = iota
+	// ClusterQueuePreempt means that there is not enough min quota in the
+	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
+	// with lower priority.
+	ClusterQueuePreempt
+	// CohortReclaim means that there is enough unused min quota in the
+	// ClusterQueue, but it needs to be reclaimed from the cohort.
+	CohortReclaim
+	// Fit means that there is not enough unused quota in the cohort to
```

  oops :)

- **pkg/scheduler/flavorassigner/flavorassigner.go:188** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:01:24Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
+	NoFit FlavorAssignmentMode = iota
+	// ClusterQueuePreempt means that there is not enough min quota in the
+	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
+	// with lower priority.
+	ClusterQueuePreempt
+	// CohortReclaim means that there is enough unused min quota in the
+	// ClusterQueue, but it needs to be reclaimed from the cohort.
```

  I rephrased but trying to use less words.

- **pkg/scheduler/flavorassigner/flavorassigner.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:06:08Z
```diff
@@ -219,35 +331,46 @@ func (a *Assignment) findFlavorForCodepResources(
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.NodeSelector}}); !match || err != nil {
 			if err != nil {
 				status.err = err
-				return nil, &status
+				return nil, status
 			}
 			status.append(fmt.Sprintf("flavor %s doesn't match with node affinity", flvLimit.Name))
 			continue
 		}
 
 		assignments := make(ResourceAssignment, len(requests))
+		worstFlavorMode := Fit
 		for name, val := range requests {
 			codepFlvLimit := cq.RequestableResources[name].Flavors[i]
 			// Check considering the flavor usage by previous pod sets.
-			borrow, s := fitsFlavorLimits(name, val+a.usage[name][flavor.Name], cq, &codepFlvLimit)
-			if s.IsError() {
-				return nil, s
-			}
-			if !s.IsSuccess() {
+			mode, borrow, s := fitsFlavorLimits(name, val+a.usage[name][flavor.Name], cq, &codepFlvLimit)
+			if s != nil {
 				status.reasons = append(status.reasons, s.reasons...)
+			}
+			if mode < worstFlavorMode {
+				worstFlavorMode = mode
+			}
+			if worstFlavorMode == NoFit {
+				// The flavor doesn't fit, no need to check other resources.
 				break
 			}
+
 			assignments[name] = &FlavorAssignment{
 				Name:   flavor.Name,
-				Mode:   CohortFit,
+				Mode:   mode,
 				borrow: borrow,
 			}
 		}
-		if len(assignments) == len(requests) {
-			return assignments, nil
+
+		if worstFlavorMode > bestAssignmentMode {
+			bestAssignment = assignments
+			bestAssignmentMode = worstFlavorMode
```

  changed `worstFlavorMode` to `representativeMode`.

- **pkg/scheduler/flavorassigner/flavorassigner.go:445** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:12:03Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
```

  This is out-of-scope for the MVP https://github.com/kubernetes-sigs/kueue/tree/main/keps/83-workload-preemption#allow-high-priority-jobs-to-borrow-quota-while-preempting

- **pkg/scheduler/flavorassigner/flavorassigner.go:435** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:17:28Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
```

  Yes, this function is attempting to find a better mode as it goes down.
  
  And we need to calculate `borrow` in all branches.
  
  But I moved the calculation of `borrow` to where borrowing is needed (when it fits in the unused quota)

- **pkg/scheduler/flavorassigner/flavorassigner.go:430** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:20:15Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
```

  Done

- **pkg/scheduler/flavorassigner/flavorassigner.go:437** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:20:22Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
```

  Done

- **pkg/scheduler/flavorassigner/flavorassigner.go:456** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:25:57Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
+		// from other ClusterQueues in the cohort are preempted.
+		mode = CohortReclaim
+	}
 	if flavor.Max != nil && used+val > *flavor.Max {
 		status.append(fmt.Sprintf("borrowing limit for %s flavor %s exceeded", rName, flavor.Name))
-		return 0, &status
+		return mode, 0, &status
```

  It can also be thought the other way: if we are past Max, we can stop and we don't need to check how much borrowing we need. Moved it up.

- **pkg/scheduler/flavorassigner/flavorassigner.go:446** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:27:38Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
+		// from other ClusterQueues in the cohort are preempted.
+		mode = CohortReclaim
+	}
 	if flavor.Max != nil && used+val > *flavor.Max {
 		status.append(fmt.Sprintf("borrowing limit for %s flavor %s exceeded", rName, flavor.Name))
-		return 0, &status
+		return mode, 0, &status
 	}
 	cohortUsed := used
 	cohortTotal := flavor.Min
```

  yes, better.

- **pkg/scheduler/flavorassigner/flavorassigner.go:478** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T20:49:28Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
+		borrow = 0
+		// The request can be satisfied by the min quota, if all active workloads
+		// from other ClusterQueues in the cohort are preempted.
+		mode = CohortReclaim
+	}
 	if flavor.Max != nil && used+val > *flavor.Max {
 		status.append(fmt.Sprintf("borrowing limit for %s flavor %s exceeded", rName, flavor.Name))
-		return 0, &status
+		return mode, 0, &status
 	}
 	cohortUsed := used
 	cohortTotal := flavor.Min
 	if cq.Cohort != nil {
 		cohortUsed = cq.Cohort.UsedResources[rName][flavor.Name]
 		cohortTotal = cq.Cohort.RequestableResources[rName][flavor.Name]
 	}
-	borrow := used + val - flavor.Min
-	if borrow < 0 {
-		borrow = 0
-	}
 
 	lack := cohortUsed + val - cohortTotal
-	if lack > 0 {
-		lackQuantity := workload.ResourceQuantity(rName, lack)
-		if cq.Cohort == nil {
-			status.append(fmt.Sprintf("insufficient quota for %s flavor %s, %s more needed", rName, flavor.Name, &lackQuantity))
+	if lack <= 0 {
+		return Fit, borrow, nil
+	}
```

  I tried, but we actually need to check if we are past the Max first. Also, this fits the narrative that we are trying to find a better mode as we go down.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T21:03:02Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:181** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T21:03:02Z
```diff
@@ -104,19 +175,44 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 
 type FlavorAssignmentMode int
 
+// The flavor assigment modes below are ordered from lowest to highest
+// preference.
 const (
-	// CohortFit means that there are enough unused resources in the cohort to
+	// NoFit means that there is no enough quota left to assign this flavor.
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-19T21:38:19Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:58** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-19T21:38:19Z
```diff
@@ -40,12 +43,56 @@ type Assignment struct {
 	// usedResources is the accumulated usage of resources as podSets get
 	// flavors assigned.
 	usage cache.ResourceQuantities
+
+	// repMode is the cached representative mode for this assignment.
+	repMode *FlavorAssignmentMode
 }
 
 func (a *Assignment) Borrows() bool {
 	return len(a.TotalBorrow) > 0
 }
 
+func (a *Assignment) RepresentativeMode() FlavorAssignmentMode {
+	if a.repMode != nil {
+		return *a.repMode
+	}
+	if len(a.PodSets) == 0 {
```

  Actually, this was needed to identify when there was no assignment calculated. This is covered by scheduler tests.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-12-19T22:07:36Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:145** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T21:48:03Z
```diff
@@ -138,17 +138,24 @@ func (s *Status) Equal(o *Status) bool {
 	}))
 }
 
+// PodSetAssignment holds the assigned flavors and status messages for each of
+// the resources that the podset requests. Each assigned flavor is accompanied
+// with an AssignmentMode.
+// Empty flavors can be interpreted as NoFit mode for all the resources.
+// Empty status can be interpreted as Fit mode for all the resources.
```

  clarify that Status should not be nil if Flavors is empty and vice versa.

- **pkg/scheduler/flavorassigner/flavorassigner.go:194** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T21:51:30Z
```diff
@@ -173,22 +180,25 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 	}
 }
 
+// FlavorAssignmentMode describes whether the flavor can be assigned immediately
+// or what needs to happen so it can be assigned.
 type FlavorAssignmentMode int
 
 // The flavor assigment modes below are ordered from lowest to highest
 // preference.
 const (
-	// NoFit means that there is no enough quota left to assign this flavor.
+	// NoFit means that there is not enough quota to assign this flavor.
 	NoFit FlavorAssignmentMode = iota
-	// ClusterQueuePreempt means that there is not enough min quota in the
-	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
-	// with lower priority.
+	// ClusterQueuePreempt means that there is not enough unused min quota in the
+	// ClusterQueue. Preempting other workloads in the ClusterQueue or waiting for
+	// them to finish might allow to assign this flavor.
```

  "may make it possible to assign this flavor"?

- **pkg/scheduler/flavorassigner/flavorassigner.go:142** by [@ahg-g](https://github.com/ahg-g) — 2022-12-19T21:53:20Z
```diff
@@ -138,17 +138,24 @@ func (s *Status) Equal(o *Status) bool {
 	}))
 }
 
+// PodSetAssignment holds the assigned flavors and status messages for each of
+// the resources that the podset requests. Each assigned flavor is accompanied
```

  nit: in some comments it is podset and other it is pod sets...

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T11:15:33Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:435** by [@mimowo](https://github.com/mimowo) — 2022-12-20T11:15:33Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
```

  I see, I was thinking about something like this:
  
  ```golang
  mode := NoFit
  if val <= flavor.Min {
  	if val <= flavor.Min - used {
  		// The request can be satisfied by the min quota, if all active workloads
  		// from other ClusterQueues in the cohort are preempted.
  		mode = CohortReclaim
  	} else {
  		// The request can be satisfied by the min quota, if all active workloads
  		// in the ClusterQueue are preempted.
  		mode = ClusterQueuePreempt
  	}
  }
  ...
  if lack <= 0 {
    borrow := used + val - flavor.Min
    if borrow <= 0 {
  	  borrow = 0
    }
    return Fit, borrow, nil
  }
  ...
  ```
  However, this recomputes the value for `used + val - flavor.Min`, so I'm ok with keeping as is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-20T11:35:14Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:445** by [@mimowo](https://github.com/mimowo) — 2022-12-20T11:35:14Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
```

  I see, this sentence matches my case "The proposed policies for preemption within cohort require that the Workload fits within the min quota of the ClusterQueue.". I'm fine with deferring the implementation then.
  
  Although I'm a bit confused by the next sentence: "In other words, we don't try to borrow quota when preempting.". IIUC, it is possible (but maybe unlikely) that a big high priority workload gets starved and cannot borrow while the smaller workloads don't trigger preemption cause they are constantly fitting inside their ClusterQueues.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T18:42:23Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:445** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:42:23Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
```

  yes, but if a single high priority workload uses more than the entire min quota for the ClusterQueue, you are probably asking for trouble :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-20T18:59:03Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:142** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:48:09Z
```diff
@@ -138,17 +138,24 @@ func (s *Status) Equal(o *Status) bool {
 	}))
 }
 
+// PodSetAssignment holds the assigned flavors and status messages for each of
+// the resources that the podset requests. Each assigned flavor is accompanied
```

  :sweat_smile: 
  
  using "pod set" now.

- **pkg/scheduler/flavorassigner/flavorassigner.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:51:16Z
```diff
@@ -138,17 +138,24 @@ func (s *Status) Equal(o *Status) bool {
 	}))
 }
 
+// PodSetAssignment holds the assigned flavors and status messages for each of
+// the resources that the podset requests. Each assigned flavor is accompanied
+// with an AssignmentMode.
+// Empty flavors can be interpreted as NoFit mode for all the resources.
+// Empty status can be interpreted as Fit mode for all the resources.
```

  Done

- **pkg/scheduler/flavorassigner/flavorassigner.go:194** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:52:14Z
```diff
@@ -173,22 +180,25 @@ func (psa *PodSetAssignment) toAPI() kueue.PodSetFlavors {
 	}
 }
 
+// FlavorAssignmentMode describes whether the flavor can be assigned immediately
+// or what needs to happen so it can be assigned.
 type FlavorAssignmentMode int
 
 // The flavor assigment modes below are ordered from lowest to highest
 // preference.
 const (
-	// NoFit means that there is no enough quota left to assign this flavor.
+	// NoFit means that there is not enough quota to assign this flavor.
 	NoFit FlavorAssignmentMode = iota
-	// ClusterQueuePreempt means that there is not enough min quota in the
-	// ClusterQueue, so we need to preempt other workloads in the ClusterQueue
-	// with lower priority.
+	// ClusterQueuePreempt means that there is not enough unused min quota in the
+	// ClusterQueue. Preempting other workloads in the ClusterQueue or waiting for
+	// them to finish might allow to assign this flavor.
```

  Done

- **pkg/scheduler/flavorassigner/flavorassigner.go:435** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-20T18:57:19Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
```

  I'm happy with the current flow. It might make it easier to split each mode into it's own independent function in the future, if necessary.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T07:14:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:445** by [@mimowo](https://github.com/mimowo) — 2022-12-21T07:14:35Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
```

  agree, wondering if there is a good place in the docs to explain that, but maybe it can wait for feedback from users if it is a real scenario

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2022-12-21T07:15:11Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:435** by [@mimowo](https://github.com/mimowo) — 2022-12-21T07:15:10Z
```diff
@@ -296,38 +419,52 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 	return nodeaffinity.GetRequiredNodeAffinity(&corev1.Pod{Spec: specCopy})
 }
 
-// fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
+// fitsFlavorLimits returns how could this flavor be assigned, according to the
+// remaining quota in the ClusterQueue and cohort.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (int64, *Status) {
+func fitsFlavorLimits(rName corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (FlavorAssignmentMode, int64, *Status) {
 	var status Status
 	used := cq.UsedResources[rName][flavor.Name]
+	mode := NoFit
+	if val <= flavor.Min {
+		// The request can be satisfied by the min quota, if all active workloads
+		// in the ClusterQueue are preempted.
+		mode = ClusterQueuePreempt
+	}
+	borrow := used + val - flavor.Min
+	if borrow <= 0 {
```

  I'm also happy with that approach
