# PR #4763: Add yaml file processor for Helm chart template generation

**Summary**: Add yaml file processor for Helm chart template generation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4763

**Last updated**: 2025-05-26T09:23:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/improve-helm-charts-sync` → `main`
- **Created**: 2025-03-24T07:34:15Z
- **Updated**: 2025-05-26T09:23:07Z
- **Merged**: 2025-05-12T05:53:14Z
- **Closed**: 2025-05-12T05:53:14Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 24   **+11192 / -10814**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind cleanup

#### What this PR does / why we need it:
Replace `hack/update-helm.sh` to improving maintainability.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1798

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-24T07:34:24Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-24T07:34:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 84d280539684f4e218c50046519d01ef6a977371 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/681e1b6277d56b0008f1438e |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-04-10T11:51:26Z

This is wonderful work @IrvingMg! Are there any other changes in the freshly generated files besides quotation and identation?

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-04-23T17:36:57Z

> This is wonderful work @IrvingMg! Are there any other changes in the freshly generated files besides quotation and identation?

There are a couple more:

1. The separators '---' are removed when they appear at the beginning of the file.
2. In some strings where a sentence is split across multiple lines, it has been reformatted into a single line.

In general, there should only be formatting changes in the generated files, as the input files are formatted with YQ before being processed. Ref: [FormatYAML](https://github.com/kubernetes-sigs/kueue/pull/4763/files#diff-0d60626640a9b6b3dd92f168107e512accf62c108c0445e794c4cb96229d0fdaR152) function.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-05T09:07:44Z

/lgtm

Awesome! Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-05T09:07:51Z

LGTM label has been added.  <details>Git tree hash: 17b2e203f123e489f99c39ea0392a66eed2c0c23</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-05T11:52:26Z

Thanks @IrvingMg!
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-05T11:52:33Z

LGTM label has been added.  <details>Git tree hash: 131e5095eee190f20d2f40babb0d3fb2fbe50ee6</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-05T11:52:41Z

/assign @tenzen-y @mimowo

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-05-06T15:32:48Z

> This LGTM, and if I didn't miss anything this is just whitespace changes.
> 
> Still, for sanity check, could you make an experiment and install Kueue from this manifest, and check basic functionality works ok?

Yes, it seems to be working properly, as the files should only contain formatting changes. I manually tested it by running the following:

```bash
#!/bin/bash

make kind-image-build
REPOSITORY=$(docker images --format '{{.Repository}}' | head -n 1)
TAG=$(docker images --format '{{.Tag}}' | head -n 1)

kind create cluster
kind load docker-image $REPOSITORY:$TAG

./bin/helm install kueue charts/kueue/ --create-namespace --namespace kueue-system \
    --set controllerManager.manager.image.tag=$TAG \
    --set controllerManager.manager.image.pullPolicy='Never' 

kubectl wait deploy/kueue-controller-manager -nkueue-system --for=condition=available --timeout=5m

kubectl apply -f examples/admin/single-clusterqueue-setup.yaml
cat >sample-job.yaml <<EOL
apiVersion: batch/v1
kind: Job
metadata:
  generateName: sample-job-
  namespace: default
  labels:
    kueue.x-k8s.io/queue-name: user-queue
spec:
  parallelism: 3
  completions: 3
  suspend: true
  template:
    spec:
      containers:
      - name: dummy-job
        image: registry.k8s.io/e2e-test-images/agnhost:2.53
        args: ["entrypoint-tester", "hello", "world"]
        resources:
          requests:
            cpu: 1
            memory: "200Mi"
      restartPolicy: Never
EOL
kubectl create -f sample-job.yaml
```

And got:

```
kubectl get deploy -n kueue-system
NAME                       READY   UP-TO-DATE   AVAILABLE   AGE
kueue-controller-manager   1/1     1            1           4m47s

---

kubectl get jobs
NAME               STATUS     COMPLETIONS   DURATION   AGE
sample-job-5pchv   Complete   3/3           10s        3m18s

---

kubectl get workloads
NAME                         QUEUE        RESERVED IN     ADMITTED   FINISHED   AGE
job-sample-job-5pchv-beed7   user-queue   cluster-queue   True       True       3m21s

---

kubectl get pods
NAME                     READY   STATUS      RESTARTS   AGE
sample-job-5pchv-6dftg   0/1     Completed   0          3m25s
sample-job-5pchv-bdqwq   0/1     Completed   0          3m25s
sample-job-5pchv-tslkb   0/1     Completed   0          3m25s
```

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-08T11:27:39Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-08T11:27:45Z

LGTM label has been added.  <details>Git tree hash: b15b717143a61f8153fbcd93e95f4904273b5bca</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-09T14:28:09Z

/approve 
Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-09T14:28:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4763#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4763#issuecomment-2866766147" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:30:57Z

/lgtm
I think this is a valid approach 👍 

Actually, I was initially imagining this a bit different - using golang to process the yamls directly, rather than via the abstraction layer of the processing-plan.yaml.

The abstraction layer makes it more readable, but we may sometimes have complications when the syntax needs to be extended. OTOH we support Helm in kueue already for quite a while, so such syntax requirements have likely already surfaced.

Let's see how it works in practice.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-12T05:31:02Z

LGTM label has been added.  <details>Git tree hash: 8077cb01458bf3dc2ab018f16fc5d5af2e0356d1</details>

### Comment by [@mtparet](https://github.com/mtparet) — 2025-05-26T08:03:51Z

There are weird behabior where multiple `INSERT_TEXT` (targetting same yaml block/key) leads to removing completly parts of the YAML or injecting at the wrong place.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2025-05-26T08:20:11Z

> There are weird behabior where multiple `INSERT_TEXT` (targetting same yaml block/key) leads to removing completly parts of the YAML or injecting at the wrong place.

Could you provide an example of where you're observing the weird behavior?

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-04T13:35:35Z

- **hack/update-helm.sh:30** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T13:35:35Z
```diff
@@ -19,452 +19,32 @@ set -o nounset
 set -o pipefail
 
 # Set the source and destination directories
-SRC_CRD_DIR=config/components/crd/bases
-SRC_RBAC_DIR=config/components/rbac
 SRC_WEBHOOK_DIR=config/components/webhook
-SRC_VISIBILITY_DIR=config/components/visibility
-SRC_VISIBILITY_APF_DIR=config/components/visibility-apf
-SRC_KUEUEVIZ_DIR=config/components/kueueviz
-
-DEST_CRD_DIR=charts/kueue/templates/crd
-DEST_RBAC_DIR=charts/kueue/templates/rbac
 DEST_WEBHOOK_DIR=charts/kueue/templates/webhook
-DEST_VISIBILITY_DIR=charts/kueue/templates/visibility
-DEST_VISIBILITY_APF_DIR=charts/kueue/templates/visibility-apf
-DEST_KUEUEVIZ_DIR=charts/kueue/templates/kueueviz
 
+# Define tools
 YQ=./bin/yq
-SED=${SED:-/usr/bin/sed}
-
-# Create the destination directory if it doesn't exist
-mkdir -p ${DEST_CRD_DIR} "${DEST_RBAC_DIR}" ${DEST_WEBHOOK_DIR} ${DEST_VISIBILITY_DIR}
+YAML_PROCESSOR=./bin/yaml-processor
 
-# Add more excluded files separated by spaces
-EXCLUDE_FILES='kustomization.yaml kustomizeconfig.yaml'
-# shellcheck disable=SC2086
-EXCLUDE_FILES_ARGS=$(printf "! -name %s " $EXCLUDE_FILES)
+# Process the manifests.yaml file using yq
+$YQ -N -s '.kind' ${SRC_WEBHOOK_DIR}/manifests.yaml
```

  I'm wondering if we can avoid it and process multiple elements in yaml processor.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-04T13:40:04Z

- **hack/processing-plan.yaml:232** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T13:40:03Z
```diff
@@ -0,0 +1,383 @@
+files:
+  - path: ./config/components/crd/bases/*.yaml
+    output_dir: ./charts/kueue/templates/crd/
+    excludes:
+      - kustomization.yaml
+    operations:
+      - type: "INSERT_TEXT"
+        key: ".metadata.annotations"
+        value: |
+          {{- if .Values.enableCertManager }}
+          cert-manager.io/inject-ca-from: '{{ .Release.Namespace }}/{{ include "kueue.fullname" . }}-serving-cert'
+          {{- end }}
+        indentation: 2
+      - type: "INSERT_OBJECT"
+        key: ".spec"
+        value: |
+          conversion:
+              strategy: Webhook
+              webhook:
+                clientConfig:
+                  service:
+                    name: '{{ include "kueue.fullname" . }}-webhook-service'
+                    namespace: '{{ .Release.Namespace }}'
+                    path: /convert
+                conversionReviewVersions:
+                  - v1
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+  - path: ./config/components/rbac/*.yaml
+    output_dir: ./charts/kueue/templates/rbac
+    excludes:
+      - kustomization.yaml
+    remove_separators: true
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "APPEND"
+        key: ".roleRef.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "APPEND"
+        key: ".subjects.[].name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".subjects.[].namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata.labels"
+        value: |
+          {{- include "kueue.labels" . | nindent 4 }}
+        on_condition: ".metadata.labels != null"
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+        on_condition: ".metadata.labels == null"
+  - path: ./config/components/visibility/*.yaml
+    output_dir: ./charts/kueue/templates/visibility
+    excludes:
+      - kustomization.yaml
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+        on_condition: ".metadata.name != \"v1beta*\""
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+        on_condition: ".metadata.namespace != \"kube-system\""
+      - type: "APPEND"
+        key: ".spec.service.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".spec.service.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "UPDATE"
+        key: ".subjects.[].namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "INSERT_TEXT"
+        key: ".spec"
+        value: |
+          selector:
+          {{- include "kueue.selectorLabels" . | nindent 4 }}
+        indentation: 2
+        on_condition: ".kind == \"Service\""
+  - path: ./config/components/visibility-apf/*.yaml
+    output_dir: ./charts/kueue/templates/visibility-apf
+    excludes:
+      - kustomization.yaml
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "INSERT_TEXT"
+        position: "START"
+        value: |
+          {{- if .Values.enableVisibilityAPF }}
+      - type: "INSERT_TEXT"
+        position: "END"
+        value: |
+          {{- end }}
+  - path: ./config/components/webhook/service.yaml
+    output_dir: ./charts/kueue/templates/webhook
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "INSERT_OBJECT"
+        key: ".spec.type"
+        value: '"{{ .Values.webhookService.type }}"'
+        add_key_if_missing: true
+      - type: "DELETE"
+        key: ".spec.ports"
+      - type: "INSERT_TEXT"
+        key: ".spec.type"
+        value: |
+          {{- if .Values.webhookService.ipDualStack.enabled }}
+          ipFamilies: {{ toYaml .Values.webhookService.ipDualStack.ipFamilies | nindent 4 }}
+          ipFamilyPolicy: {{ .Values.webhookService.ipDualStack.ipFamilyPolicy }}
+          {{- end }}
+          selector:
+          {{- include "kueue.selectorLabels" . | nindent 4 }}
+          ports:
+          {{- .Values.webhookService.ports | toYaml | nindent 2 -}}
+  - path: ./config/components/webhook/MutatingWebhookConfiguration.yml
+    output_dir: ./charts/kueue/templates/webhook
+    operations:
+      - type: "INSERT_TEXT"
+        position: "START"
+        value: |
+          {{- $integrationsConfig := (fromYaml .Values.managerConfig.controllerManagerConfigYaml).integrations }}
+          {{- $managerConfig := (fromYaml .Values.managerConfig.controllerManagerConfigYaml) }}
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "INSERT_TEXT"
+        key: ".metadata.name"
+        value: |
+          {{- if .Values.enableCertManager }}
+          annotations:
+            cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "kueue.fullname" . }}-serving-cert
+          {{- end }}
+          namespace: '{{ .Release.Namespace }}'
+      - type: "APPEND"
+        key: ".webhooks.[].clientConfig.service.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".webhooks.[].clientConfig.service.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "DELETE"
+        key: ".webhooks.[8].failurePolicy"
+      - type: "INSERT_TEXT"
+        key: ".webhooks.[8].name"
+        value: |
+          {{- if has "pod" $integrationsConfig.frameworks }}
+          failurePolicy: Fail
+          {{- else }}
+          failurePolicy: Ignore
+          {{- end }}
+          namespaceSelector:
+            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") -}}
+              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 -}}
+            {{- else }}
+            matchExpressions:
+              - key: kubernetes.io/metadata.name
+                operator: NotIn
+                values:
+                  - kube-system
+                  - '{{ .Release.Namespace }}'
+            {{- end }}
+      - type: "DELETE"
+        key: ".webhooks.[1].failurePolicy"
+      - type: "INSERT_TEXT"
+        key: ".webhooks.[1].name"
+        value: |
+          {{- if has "deployment" $integrationsConfig.frameworks }}
+          failurePolicy: Fail
+          {{- else }}
+          failurePolicy: Ignore
+          {{- end }}
+          namespaceSelector:
+            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") -}}
+              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 -}}
+            {{- else }}
+            matchExpressions:
+              - key: kubernetes.io/metadata.name
+                operator: NotIn
+                values:
+                  - kube-system
+                  - '{{ .Release.Namespace }}'
+            {{- end }}
+      - type: "DELETE"
+        key: ".webhooks.[13].failurePolicy"
```

  I think we'll lose it after the changes in `manifests.yaml`. It would be great to find the element by name or path like we did it before.
  
  Something like:
  ```
  on_condition: '.webhooks.[].clientConfig.service.path == "/validate--v1-pod"'
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-04T13:42:54Z

- **hack/internal/tools/yaml-processor/go.mod:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-04T13:42:54Z
```diff
@@ -0,0 +1,29 @@
+module sigs.k8s.io/kueue/internal/tools/yaml-processor
```

  Can we use go.mod from sigs.k8s.io/kueue/internal/tools?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-04-08T15:56:15Z

- **hack/update-helm.sh:30** by [@IrvingMg](https://github.com/IrvingMg) — 2025-04-08T15:56:15Z
```diff
@@ -19,452 +19,32 @@ set -o nounset
 set -o pipefail
 
 # Set the source and destination directories
-SRC_CRD_DIR=config/components/crd/bases
-SRC_RBAC_DIR=config/components/rbac
 SRC_WEBHOOK_DIR=config/components/webhook
-SRC_VISIBILITY_DIR=config/components/visibility
-SRC_VISIBILITY_APF_DIR=config/components/visibility-apf
-SRC_KUEUEVIZ_DIR=config/components/kueueviz
-
-DEST_CRD_DIR=charts/kueue/templates/crd
-DEST_RBAC_DIR=charts/kueue/templates/rbac
 DEST_WEBHOOK_DIR=charts/kueue/templates/webhook
-DEST_VISIBILITY_DIR=charts/kueue/templates/visibility
-DEST_VISIBILITY_APF_DIR=charts/kueue/templates/visibility-apf
-DEST_KUEUEVIZ_DIR=charts/kueue/templates/kueueviz
 
+# Define tools
 YQ=./bin/yq
-SED=${SED:-/usr/bin/sed}
-
-# Create the destination directory if it doesn't exist
-mkdir -p ${DEST_CRD_DIR} "${DEST_RBAC_DIR}" ${DEST_WEBHOOK_DIR} ${DEST_VISIBILITY_DIR}
+YAML_PROCESSOR=./bin/yaml-processor
 
-# Add more excluded files separated by spaces
-EXCLUDE_FILES='kustomization.yaml kustomizeconfig.yaml'
-# shellcheck disable=SC2086
-EXCLUDE_FILES_ARGS=$(printf "! -name %s " $EXCLUDE_FILES)
+# Process the manifests.yaml file using yq
+$YQ -N -s '.kind' ${SRC_WEBHOOK_DIR}/manifests.yaml
```

  Done. Added the flag `multidoc` to handle these cases.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-04-09T15:49:31Z

- **hack/update-helm.sh:30** by [@IrvingMg](https://github.com/IrvingMg) — 2025-04-09T15:49:30Z
```diff
@@ -19,452 +19,32 @@ set -o nounset
 set -o pipefail
 
 # Set the source and destination directories
-SRC_CRD_DIR=config/components/crd/bases
-SRC_RBAC_DIR=config/components/rbac
 SRC_WEBHOOK_DIR=config/components/webhook
-SRC_VISIBILITY_DIR=config/components/visibility
-SRC_VISIBILITY_APF_DIR=config/components/visibility-apf
-SRC_KUEUEVIZ_DIR=config/components/kueueviz
-
-DEST_CRD_DIR=charts/kueue/templates/crd
-DEST_RBAC_DIR=charts/kueue/templates/rbac
 DEST_WEBHOOK_DIR=charts/kueue/templates/webhook
-DEST_VISIBILITY_DIR=charts/kueue/templates/visibility
-DEST_VISIBILITY_APF_DIR=charts/kueue/templates/visibility-apf
-DEST_KUEUEVIZ_DIR=charts/kueue/templates/kueueviz
 
+# Define tools
 YQ=./bin/yq
-SED=${SED:-/usr/bin/sed}
-
-# Create the destination directory if it doesn't exist
-mkdir -p ${DEST_CRD_DIR} "${DEST_RBAC_DIR}" ${DEST_WEBHOOK_DIR} ${DEST_VISIBILITY_DIR}
+YAML_PROCESSOR=./bin/yaml-processor
 
-# Add more excluded files separated by spaces
-EXCLUDE_FILES='kustomization.yaml kustomizeconfig.yaml'
-# shellcheck disable=SC2086
-EXCLUDE_FILES_ARGS=$(printf "! -name %s " $EXCLUDE_FILES)
+# Process the manifests.yaml file using yq
+$YQ -N -s '.kind' ${SRC_WEBHOOK_DIR}/manifests.yaml
```

  Flag was removed and now this is detected automatically.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-04-23T17:23:15Z

- **hack/processing-plan.yaml:232** by [@IrvingMg](https://github.com/IrvingMg) — 2025-04-23T17:23:15Z
```diff
@@ -0,0 +1,383 @@
+files:
+  - path: ./config/components/crd/bases/*.yaml
+    output_dir: ./charts/kueue/templates/crd/
+    excludes:
+      - kustomization.yaml
+    operations:
+      - type: "INSERT_TEXT"
+        key: ".metadata.annotations"
+        value: |
+          {{- if .Values.enableCertManager }}
+          cert-manager.io/inject-ca-from: '{{ .Release.Namespace }}/{{ include "kueue.fullname" . }}-serving-cert'
+          {{- end }}
+        indentation: 2
+      - type: "INSERT_OBJECT"
+        key: ".spec"
+        value: |
+          conversion:
+              strategy: Webhook
+              webhook:
+                clientConfig:
+                  service:
+                    name: '{{ include "kueue.fullname" . }}-webhook-service'
+                    namespace: '{{ .Release.Namespace }}'
+                    path: /convert
+                conversionReviewVersions:
+                  - v1
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+  - path: ./config/components/rbac/*.yaml
+    output_dir: ./charts/kueue/templates/rbac
+    excludes:
+      - kustomization.yaml
+    remove_separators: true
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "APPEND"
+        key: ".roleRef.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "APPEND"
+        key: ".subjects.[].name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".subjects.[].namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata.labels"
+        value: |
+          {{- include "kueue.labels" . | nindent 4 }}
+        on_condition: ".metadata.labels != null"
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+        on_condition: ".metadata.labels == null"
+  - path: ./config/components/visibility/*.yaml
+    output_dir: ./charts/kueue/templates/visibility
+    excludes:
+      - kustomization.yaml
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+        on_condition: ".metadata.name != \"v1beta*\""
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+        on_condition: ".metadata.namespace != \"kube-system\""
+      - type: "APPEND"
+        key: ".spec.service.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".spec.service.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "UPDATE"
+        key: ".subjects.[].namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "INSERT_TEXT"
+        key: ".spec"
+        value: |
+          selector:
+          {{- include "kueue.selectorLabels" . | nindent 4 }}
+        indentation: 2
+        on_condition: ".kind == \"Service\""
+  - path: ./config/components/visibility-apf/*.yaml
+    output_dir: ./charts/kueue/templates/visibility-apf
+    excludes:
+      - kustomization.yaml
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "INSERT_TEXT"
+        position: "START"
+        value: |
+          {{- if .Values.enableVisibilityAPF }}
+      - type: "INSERT_TEXT"
+        position: "END"
+        value: |
+          {{- end }}
+  - path: ./config/components/webhook/service.yaml
+    output_dir: ./charts/kueue/templates/webhook
+    operations:
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".metadata.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "INSERT_OBJECT"
+        key: ".spec.type"
+        value: '"{{ .Values.webhookService.type }}"'
+        add_key_if_missing: true
+      - type: "DELETE"
+        key: ".spec.ports"
+      - type: "INSERT_TEXT"
+        key: ".spec.type"
+        value: |
+          {{- if .Values.webhookService.ipDualStack.enabled }}
+          ipFamilies: {{ toYaml .Values.webhookService.ipDualStack.ipFamilies | nindent 4 }}
+          ipFamilyPolicy: {{ .Values.webhookService.ipDualStack.ipFamilyPolicy }}
+          {{- end }}
+          selector:
+          {{- include "kueue.selectorLabels" . | nindent 4 }}
+          ports:
+          {{- .Values.webhookService.ports | toYaml | nindent 2 -}}
+  - path: ./config/components/webhook/MutatingWebhookConfiguration.yml
+    output_dir: ./charts/kueue/templates/webhook
+    operations:
+      - type: "INSERT_TEXT"
+        position: "START"
+        value: |
+          {{- $integrationsConfig := (fromYaml .Values.managerConfig.controllerManagerConfigYaml).integrations }}
+          {{- $managerConfig := (fromYaml .Values.managerConfig.controllerManagerConfigYaml) }}
+      - type: "INSERT_TEXT"
+        key: ".metadata"
+        value: |
+          labels:
+          {{- include "kueue.labels" . | nindent 4 }}
+        indentation: 2
+      - type: "APPEND"
+        key: ".metadata.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "INSERT_TEXT"
+        key: ".metadata.name"
+        value: |
+          {{- if .Values.enableCertManager }}
+          annotations:
+            cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "kueue.fullname" . }}-serving-cert
+          {{- end }}
+          namespace: '{{ .Release.Namespace }}'
+      - type: "APPEND"
+        key: ".webhooks.[].clientConfig.service.name"
+        value: '"{{ include \"kueue.fullname\" . }}-"'
+      - type: "UPDATE"
+        key: ".webhooks.[].clientConfig.service.namespace"
+        value: '"{{ .Release.Namespace }}"'
+      - type: "DELETE"
+        key: ".webhooks.[8].failurePolicy"
+      - type: "INSERT_TEXT"
+        key: ".webhooks.[8].name"
+        value: |
+          {{- if has "pod" $integrationsConfig.frameworks }}
+          failurePolicy: Fail
+          {{- else }}
+          failurePolicy: Ignore
+          {{- end }}
+          namespaceSelector:
+            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") -}}
+              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 -}}
+            {{- else }}
+            matchExpressions:
+              - key: kubernetes.io/metadata.name
+                operator: NotIn
+                values:
+                  - kube-system
+                  - '{{ .Release.Namespace }}'
+            {{- end }}
+      - type: "DELETE"
+        key: ".webhooks.[1].failurePolicy"
+      - type: "INSERT_TEXT"
+        key: ".webhooks.[1].name"
+        value: |
+          {{- if has "deployment" $integrationsConfig.frameworks }}
+          failurePolicy: Fail
+          {{- else }}
+          failurePolicy: Ignore
+          {{- end }}
+          namespaceSelector:
+            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") -}}
+              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 -}}
+            {{- else }}
+            matchExpressions:
+              - key: kubernetes.io/metadata.name
+                operator: NotIn
+                values:
+                  - kube-system
+                  - '{{ .Release.Namespace }}'
+            {{- end }}
+      - type: "DELETE"
+        key: ".webhooks.[13].failurePolicy"
```

  It was added `on_item_condition` param to filter arrays.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-29T07:53:26Z

- **charts/kueue/templates/webhook/manifests.yaml:386** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-29T07:53:25Z
```diff
@@ -383,6 +383,7 @@ webhooks:
         resources:
           - workloads
     sideEffects: None
+
```

  nit: can we remove this space?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-29T08:02:03Z

- **hack/internal/tools/yaml-processor/yamlproc/processor.go:154** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-29T08:02:03Z
```diff
@@ -0,0 +1,303 @@
+package yamlproc
+
+import (
+	"errors"
+	"fmt"
+	"os"
+	"path/filepath"
+
+	"go.uber.org/zap"
+	"gopkg.in/yaml.v3"
+)
+
+type ProcessingPlan struct {
+	Files []FileOperations `yaml:"files"`
+}
+
+type FileOperations struct {
+	Path            string      `yaml:"path"`
+	Excludes        []string    `yaml:"excludes,omitempty"`
+	Operations      []Operation `yaml:"operations"`
+	PostOperations  []Operation `yaml:"post_operations,omitempty"`
+	OutputDir       string      `yaml:"output_dir,omitempty"`
+	ContinueOnError bool        `yaml:"continue_on_error,omitempty"`
+}
+
+type Operation struct {
+	Type            string `yaml:"type"`
+	Key             string `yaml:"key,omitempty"` // Key is optional for INSERT_TEXT
+	Value           string `yaml:"value"`
+	Indentation     int    `yaml:"indentation,omitempty"`
+	AddKeyIfMissing bool   `yaml:"add_key_if_missing,omitempty"`
+	Position        string `yaml:"position,omitempty"` // Position is valid only for INSERT_TEXT
+	OnFileCondition string `yaml:"on_file_condition,omitempty"`
+	OnItemCondition string `yaml:"on_item_condition,omitempty"`
+}
+
+const (
+	InsertObject = "INSERT_OBJECT"
+	InsertText   = "INSERT_TEXT"
+	Append       = "APPEND"
+	Update       = "UPDATE"
+	Delete       = "DELETE"
+)
+
+type FileProcessor struct {
+	yq           *YQClient
+	textInserter *TextInserter
+}
+
+var logger *zap.Logger
+
+func SetLogger(l *zap.Logger) {
+	logger = l
+}
+
+func NewProcessor(yq *YQClient, textInserter *TextInserter) *FileProcessor {
+	return &FileProcessor{
+		yq:           yq,
+		textInserter: textInserter,
+	}
+}
+
+func (fp *FileProcessor) ProcessPlan(plan ProcessingPlan) {
+	for _, file := range plan.Files {
+		outputDir := file.OutputDir
+		if outputDir == "" {
+			logger.Fatal("Missing outputDir for file", zap.String("file", file.Path))
+		}
+
+		matchedFiles, err := filepath.Glob(file.Path)
+		if err != nil {
+			logger.Fatal("Failed to expand glob pattern", zap.String("pattern", file.Path), zap.Error(err))
+		}
+
+		if len(matchedFiles) == 0 {
+			logger.Warn("No files matched for pattern", zap.String("pattern", file.Path))
+		}
+
+		for _, filePath := range filterFiles(matchedFiles, file.Excludes) {
+			file.Path = filePath
+			err := fp.ProcessFile(file)
+			if err != nil && !file.ContinueOnError {
+				logger.Fatal("Failed to process file", zap.String("file", file.Path), zap.Error(err))
+			}
+		}
+	}
+}
+
+func filterFiles(files, excludes []string) []string {
+	var filtered []string
+	for _, file := range files {
+		if !isExcluded(file, excludes) {
+			filtered = append(filtered, file)
+		}
+	}
+	return filtered
+}
+
+func isExcluded(file string, excludes []string) bool {
+	base := filepath.Base(file)
+	for _, pattern := range excludes {
+		matched, err := filepath.Match(pattern, base)
+		if err != nil {
+			logger.Warn("Invalid exclude pattern", zap.String("pattern", file), zap.Error(err))
+			continue
+		}
+
+		if matched {
+			return true
+		}
+	}
+	return false
+}
+
+func (fp *FileProcessor) ProcessFile(fileOps FileOperations) error {
+	if fileOps.OutputDir != "" {
+		if err := os.MkdirAll(fileOps.OutputDir, 0755); err != nil {
+			logger.Error("Failed to create output directory", zap.String("outputDir", fileOps.OutputDir), zap.Error(err))
+			return err
+		}
+	}
+
+	var opErrors []error
+	data, err := fp.prepareFile(fileOps)
+	if err != nil {
+		logger.Error("Skipping file due to preparation error", zap.String("file", fileOps.Path), zap.Error(err))
+		return err
+	}
+
+	if isMultiDocumentYAML(data) {
+		docs, err := SplitYAMLDocuments(data)
+		if err != nil {
+			logger.Error("Skipping file due to YAML splitting error", zap.String("file", fileOps.Path), zap.Error(err))
+			return err
+		}
+
+		var modifiedDocs [][]byte
+		for _, doc := range docs {
+			modifiedDoc, errs := fp.ProcessFileOperations(doc, fileOps)
+			modifiedDocs = append(modifiedDocs, modifiedDoc)
+			opErrors = append(opErrors, errs...)
+		}
+
+		data = JoinYAMLDocuments(modifiedDocs)
+	} else {
+		data, opErrors = fp.ProcessFileOperations(data, fileOps)
+	}
+
+	if len(opErrors) > 0 && !fileOps.ContinueOnError {
+		return errors.Join(opErrors...)
+	}
+
+	outputPath := filepath.Join(fileOps.OutputDir, filepath.Base(fileOps.Path))
+	if err := os.WriteFile(outputPath, []byte(data), 0644); err != nil {
```

  ```suggestion
  	if err := os.WriteFile(outputPath, data, 0644); err != nil {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-29T08:05:56Z

- **hack/internal/tools/yaml-processor/yamlproc/yaml.go:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-29T08:05:55Z
```diff
@@ -0,0 +1,112 @@
+package yamlproc
+
+import (
+	"bytes"
+	"errors"
+	"io"
+	"strings"
+
+	"gopkg.in/yaml.v3"
+)
+
+func SplitYAMLDocuments(data []byte) ([][]byte, error) {
+	var result [][]byte
+
+	reader := bytes.NewReader(data)
+	dec := yaml.NewDecoder(reader)
+	for {
+		var node yaml.Node
+		err := dec.Decode(&node)
+		if errors.Is(err, io.EOF) {
+			return result, nil
+		}
+
+		if err != nil {
+			return nil, err
+		}
+
+		doc, err := yaml.Marshal(&node)
+		if err != nil {
+			return nil, err
+		}
+		result = append(result, []byte(doc))
```

  ```suggestion
  		result = append(result, doc)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T07:01:44Z

- **hack/internal/tools/yaml-processor/main.go:17** by [@mimowo](https://github.com/mimowo) — 2025-05-06T07:01:43Z
```diff
@@ -0,0 +1,43 @@
+package main
```

  is the code of the processor written from scratch or it vendors some code?
  
  if written from scratch please add the  "Copyright The Kubernetes Authors." preamble to all go files.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T07:05:02Z

This LGTM, and if I didn't miss anything this is just whitespace changes. 

Still, for sanity check, could you make an experiment and install Kueue from this manifest, and check basic functionality works ok?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2025-05-06T15:28:13Z

- **hack/internal/tools/yaml-processor/main.go:17** by [@IrvingMg](https://github.com/IrvingMg) — 2025-05-06T15:28:13Z
```diff
@@ -0,0 +1,43 @@
+package main
```

  It's written from scratch. Added copyright disclaimer.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-06T15:30:14Z

- **hack/internal/tools/yaml-processor/main.go:17** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-06T15:30:14Z
```diff
@@ -0,0 +1,43 @@
+package main
```

  I'm wondering why the linter didn't update these files.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-12T12:49:31Z

Awesome! Thank you!
