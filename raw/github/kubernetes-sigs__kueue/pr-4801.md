# PR #4801: make iterable function for traversing to root

**Summary**: make iterable function for traversing to root

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4801

**Last updated**: 2025-04-14T09:16:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@KPostOffice](https://github.com/KPostOffice)
- **Branch**: `iterable-path-to-root` → `main`
- **Created**: 2025-03-26T15:44:11Z
- **Updated**: 2025-04-14T09:16:56Z
- **Merged**: 2025-04-14T09:16:55Z
- **Closed**: 2025-04-14T09:16:55Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: _none_
- **Changed files**: 7   **+51 / -28**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4644 

#### Special notes for your reviewer:
~Let me know if this was the approach that was expected or if there's a way to make this generic so that the iterator doesn't have to be implemented twice~

**Solved** I was able to implement this in the generic ClusterQueue and Cohort structs

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-26T15:44:22Z

Hi @KPostOffice. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-26T15:44:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2cd169ffd6afe2acfb545ee112cefd58e258db54 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67f961106153130008a88ec2 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-26T15:54:05Z

/assign @gabesaba

### Comment by [@mimowo](https://github.com/mimowo) — 2025-03-28T19:28:33Z

/ok-to-test

### Comment by [@KPostOffice](https://github.com/KPostOffice) — 2025-03-28T21:14:59Z

/test pull-kueue-test-integration-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-14T08:41:05Z

/approve
Leaving final lgtm to @gabesaba

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-14T08:41:13Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4801#" title="Author self-approved">KPostOffice</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4801#issuecomment-2800899878" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-14T08:41:57Z

somehow explicit "NONE" is required by the robot in release-note
/release-note-edit
```release-note
NONE
```

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-04-14T08:52:01Z

/lgtm

Thanks for the cleanup :)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-14T08:52:08Z

LGTM label has been added.  <details>Git tree hash: 154120bf5d2157dd7d97044aace42e485527c8a7</details>

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-03-27T09:58:47Z

can we make this more general, such that it returns all nodes? then have a helper function to chop off root (or have the caller ignore the root, as that seems like a very specific case)

Then, we could use in other locations, such as:
https://github.com/kubernetes-sigs/kueue/blob/1a09780b09cf210c9df2e9be9151585d361fec1d/pkg/scheduler/preemption/fairsharing/ordering.go#L70-L74
https://github.com/kubernetes-sigs/kueue/blob/1a09780b09cf210c9df2e9be9151585d361fec1d/pkg/scheduler/preemption/fairsharing/least_common_ancestor.go#L32-L59

- **pkg/scheduler/fair_sharing_iterator.go:214** by [@gabesaba](https://github.com/gabesaba) — 2025-03-27T09:07:03Z
```diff
@@ -210,10 +210,9 @@ func (ec *entryComparer) computeDRS(rootCohort *cache.CohortSnapshot, cqToEntry
 		// calculate DRS, with workload, for all Cohorts on
 		// path to root.
 		cohort := cq.Parent()
-		for cohort.HasParent() {
-			dominantResourceShare := cohort.DominantResourceShare()
-			ec.drsValues[drsKey{parentCohort: cohort.Parent().GetName(), workloadKey: workload.Key(entry.Obj)}] = dominantResourceShare
-			cohort = cohort.Parent()
+		for c := range cohort.PathToRoot() {
+			dominantResourceShare := c.DominantResourceShare()
+			ec.drsValues[drsKey{parentCohort: c.Parent().GetName(), workloadKey: workload.Key(entry.Obj)}] = dominantResourceShare
 		}
```

  to reduce diff, and can delete cohort variable above
  ```suggestion
  		for cohort := range cq.Parent().PathToRoot() {
  			dominantResourceShare := cohort.DominantResourceShare()
  			ec.drsValues[drsKey{parentCohort: cohort.Parent().GetName(), workloadKey: workload.Key(entry.Obj)}] = dominantResourceShare
  		}
  ```

- **pkg/cache/cache.go:738** by [@gabesaba](https://github.com/gabesaba) — 2025-03-27T09:46:53Z
```diff
@@ -732,8 +732,9 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	}
 
 	var ancestors []kueue.CohortReference
-	for cohort != nil && cohort.HasParent() {
-		ancestors = append(ancestors, cohort.Name)
+
+	for c := range cohort.PathToRoot() {
+		ancestors = append(ancestors, c.Name)
 		cohort = cohort.Parent()
```

  delete

- **pkg/cache/cache.go:736** by [@gabesaba](https://github.com/gabesaba) — 2025-03-27T09:47:08Z
```diff
@@ -732,8 +732,9 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	}
 
 	var ancestors []kueue.CohortReference
-	for cohort != nil && cohort.HasParent() {
-		ancestors = append(ancestors, cohort.Name)
+
+	for c := range cohort.PathToRoot() {
```

  nit: s/c/ancestor

- **pkg/cache/clusterqueue_snapshot.go:228** by [@gabesaba](https://github.com/gabesaba) — 2025-03-27T09:47:32Z
```diff
@@ -221,3 +223,13 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 func (c *ClusterQueueSnapshot) IsTASOnly() bool {
 	return c.tasOnly
 }
+
+func (c *ClusterQueueSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
```

  please document these functions, that they return all Cohorts, except for the root

- **pkg/cache/cohort_snapshot.go:94** by [@gabesaba](https://github.com/gabesaba) — 2025-03-27T09:48:50Z
```diff
@@ -88,3 +90,15 @@ func (c *CohortSnapshot) parentHRN() hierarchicalResourceNode {
 func (c *CohortSnapshot) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+func (c *CohortSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
```

  can this be generic? probably a function, rather than a method in that case

- **pkg/cache/clusterqueue_snapshot.go:228** by [@gabesaba](https://github.com/gabesaba) — 2025-03-27T09:49:43Z
```diff
@@ -221,3 +223,13 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 func (c *ClusterQueueSnapshot) IsTASOnly() bool {
 	return c.tasOnly
 }
+
+func (c *ClusterQueueSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
```

  how about making these all functions, and putting in a single file?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-01T22:08:51Z

- **pkg/cache/cache.go:736** by [@gabesaba](https://github.com/gabesaba) — 2025-04-01T21:52:58Z
```diff
@@ -724,18 +724,17 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	}
 
 	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
-	if cohort == nil {
+	if cohort == nil || !cohort.HasParent() {
 		return nil, nil
 	}
 	if hierarchy.HasCycle(cohort) {
 		return nil, ErrCohortHasCycle
 	}
-
-	var ancestors []kueue.CohortReference
-	for cohort != nil && cohort.HasParent() {
-		ancestors = append(ancestors, cohort.Name)
-		cohort = cohort.Parent()
+	ancestors := []kueue.CohortReference{cohort.Name} // PathToRoot does not include self
+	for ancestor := range cohort.PathToRoot() {
```

  this is only caller of PathToRoot. Therefore, let's have it return the calling node too
  
  (or just leave inlined, since it is the only caller. you can decide)

- **pkg/hierarchy/clusterqueue.go:1** by [@gabesaba](https://github.com/gabesaba) — 2025-04-01T21:55:10Z
  After seeing the result, I think the generics was the wrong call, as it just makes the code too complex. My apologies for suggesting that.
  
  It looks a lot simpler just using concrete types:
  
  ```go
  func (c *cohort) PathToRoot() iter.Seq[*cohort] {
  	return func(yield func(*cohort) bool) {
  		currentCohort := c
  		for currentCohort != nil {
  			if !yield(currentCohort) {
  				return
  			}
  			currentCohort = currentCohort.Parent()
  		}
  	}
  }
  
  func (c *ClusterQueueSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
  	return func(yield func(*CohortSnapshot) bool) {
  		currentCohort := c.Parent()
  		for currentCohort != nil {
  			if !yield(currentCohort) {
  				return
  			}
  			currentCohort = currentCohort.Parent()
  		}
  	}
  }
  ```
  
  Please revert the generic logic and just use concrete types (defined in `cache` package), as it only needs to be implemented for ClusterQueueSnapshot and cohort anyhow.

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:41** by [@gabesaba](https://github.com/gabesaba) — 2025-04-01T21:58:43Z
```diff
@@ -33,13 +33,12 @@ func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
 // returning the first Cohort which contains the preemptor
 // ClusterQueue in its subtree.
 func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
-	cohort := t.targetCq.Parent()
-	for {
+	for cohort := range t.targetCq.PathToRoot() {
 		if t.ordering.onPathFromRootToPreemptorCQ(cohort) {
 			return cohort
 		}
-		cohort = cohort.Parent()
 	}
+	return nil
```

  If we hit this branch (return nil), it implies a serious bug in the code. Should we log here and then panic, or is doing nothing fine (expecting that we will never hit this branch)? @tenzen-y 
  
  I suppose if we did return nil, we will panic shortly thereafter

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:50** by [@gabesaba](https://github.com/gabesaba) — 2025-04-01T22:02:24Z
```diff
@@ -49,11 +48,12 @@ func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) alm
 	if cq.Parent() == lca {
 		return cq
 	}
```

  ```suggestion
  ```

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:51** by [@gabesaba](https://github.com/gabesaba) — 2025-04-01T22:02:42Z
```diff
@@ -49,11 +48,12 @@ func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) alm
 	if cq.Parent() == lca {
 		return cq
 	}
-	cohort := cq.Parent()
-	for {
-		if cohort.Parent() == lca {
-			return cohort
+	var toReturn almostLCA
```

  ```suggestion
  	var candidateAlmostLCA almostLCA = cq
  ```

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:51** by [@gabesaba](https://github.com/gabesaba) — 2025-04-01T22:03:17Z
```diff
@@ -49,11 +48,12 @@ func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) alm
 	if cq.Parent() == lca {
 		return cq
 	}
-	cohort := cq.Parent()
-	for {
-		if cohort.Parent() == lca {
-			return cohort
+	var toReturn almostLCA
```

  Does this work, combined with previous suggestion?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-10T10:02:22Z

- **pkg/cache/cohort.go:92** by [@gabesaba](https://github.com/gabesaba) — 2025-04-10T07:24:47Z
```diff
@@ -85,3 +87,13 @@ func (c *cohort) CCParent() hierarchy.CycleCheckable {
 func (c *cohort) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+func (c *cohort) PathToRoot() iter.Seq[*cohort] {
+	// Returns all ancestors starting with self and ending with root
```

  This comment is not accurate - we don't yield the root

- **pkg/cache/cohort.go:92** by [@gabesaba](https://github.com/gabesaba) — 2025-04-10T07:25:17Z
```diff
@@ -85,3 +87,13 @@ func (c *cohort) CCParent() hierarchy.CycleCheckable {
 func (c *cohort) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+func (c *cohort) PathToRoot() iter.Seq[*cohort] {
```

  please move comments above the functions (here and elsewhere), so we have docstrings

- **pkg/cache/cohort_snapshot.go:94** by [@gabesaba](https://github.com/gabesaba) — 2025-04-10T07:34:10Z
```diff
@@ -88,3 +90,15 @@ func (c *CohortSnapshot) parentHRN() hierarchicalResourceNode {
 func (c *CohortSnapshot) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+func (c *CohortSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
```

  there are still other places which can use the new code:
  
  ```
  scheduler/preemption/fairsharing/ordering.go
  scheduler/preemption/fairsharing/least_common_ancestor.go (2x)
  ```

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:41** by [@gabesaba](https://github.com/gabesaba) — 2025-04-10T07:36:18Z
```diff
@@ -33,13 +33,12 @@ func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
 // returning the first Cohort which contains the preemptor
 // ClusterQueue in its subtree.
 func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
-	cohort := t.targetCq.Parent()
-	for {
+	for cohort := range t.targetCq.PathToRoot() {
 		if t.ordering.onPathFromRootToPreemptorCQ(cohort) {
 			return cohort
 		}
-		cohort = cohort.Parent()
 	}
+	return nil
```

  I wasn't suggesting the change was wrong @KPostOffice, it was more about reconciling the go compiler with our business logic

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-10T14:31:15Z

- **pkg/cache/cache.go:736** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-10T14:31:15Z
```diff
@@ -724,18 +724,17 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	}
 
 	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
-	if cohort == nil {
+	if cohort == nil || !cohort.HasParent() {
 		return nil, nil
 	}
 	if hierarchy.HasCycle(cohort) {
 		return nil, ErrCohortHasCycle
 	}
-
-	var ancestors []kueue.CohortReference
-	for cohort != nil && cohort.HasParent() {
-		ancestors = append(ancestors, cohort.Name)
-		cohort = cohort.Parent()
+	ancestors := []kueue.CohortReference{cohort.Name} // PathToRoot does not include self
+	for ancestor := range cohort.PathToRoot() {
```

  I think I'll keep it refactored into a function which returns the calling node. I'll be sure to document that as well

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-10T14:34:43Z

- **pkg/cache/clusterqueue_snapshot.go:228** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-10T14:34:43Z
```diff
@@ -221,3 +223,13 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 func (c *ClusterQueueSnapshot) IsTASOnly() bool {
 	return c.tasOnly
 }
+
+func (c *ClusterQueueSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
```

  I think that making them functions is a bit clunky since it requires a naming convention like:
  ```
  CQSnapshotPathToRoot(c *ClusterQueueSnapshot)
  CohortPathToRoot(c *cohort)
  ```
  I think leaving them as methods on the object makes more sense

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-10T14:35:36Z

- **pkg/cache/cohort_snapshot.go:94** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-10T14:35:36Z
```diff
@@ -88,3 +90,15 @@ func (c *CohortSnapshot) parentHRN() hierarchicalResourceNode {
 func (c *CohortSnapshot) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+func (c *CohortSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
```

  Ack

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-10T18:00:57Z

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:41** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-10T18:00:57Z
```diff
@@ -33,13 +33,12 @@ func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
 // returning the first Cohort which contains the preemptor
 // ClusterQueue in its subtree.
 func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
-	cohort := t.targetCq.Parent()
-	for {
+	for cohort := range t.targetCq.PathToRoot() {
 		if t.ordering.onPathFromRootToPreemptorCQ(cohort) {
 			return cohort
 		}
-		cohort = cohort.Parent()
 	}
+	return nil
```

  I've left the `nil` for now

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-10T18:38:04Z

- **pkg/cache/cohort.go:92** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-10T18:38:04Z
```diff
@@ -85,3 +87,13 @@ func (c *cohort) CCParent() hierarchy.CycleCheckable {
 func (c *cohort) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+func (c *cohort) PathToRoot() iter.Seq[*cohort] {
+	// Returns all ancestors starting with self and ending with root
```

  I've changed the function to yield root now just so that both PathToRoot methods behave the same

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-10T18:39:37Z

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:51** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-10T18:39:37Z
```diff
@@ -49,11 +48,12 @@ func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) alm
 	if cq.Parent() == lca {
 		return cq
 	}
-	cohort := cq.Parent()
-	for {
-		if cohort.Parent() == lca {
-			return cohort
+	var toReturn almostLCA
```

  I don't quite understand the suggestion, but I couldn't find a way to get rid of the `if cq.Parent() == lca` conditional

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-11T06:56:23Z

This is looking good. just two small nits left :)

- **pkg/cache/cache.go:727** by [@gabesaba](https://github.com/gabesaba) — 2025-04-11T06:47:23Z
```diff
@@ -724,20 +724,20 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	}
 
 	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
-	if cohort == nil {
+	if cohort == nil || !cohort.HasParent() {
```

  nit - code below hadles this case well, by returning empty list when singleton Cohort
  
  ```suggestion
  	if cohort == nil {
  ```
  
  wil just have to update unit test to use cmp opt, EquateEmpty

- **pkg/cache/clusterqueue_snapshot.go:228** by [@gabesaba](https://github.com/gabesaba) — 2025-04-11T06:47:46Z
```diff
@@ -221,3 +223,13 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 func (c *ClusterQueueSnapshot) IsTASOnly() bool {
 	return c.tasOnly
 }
+
+func (c *ClusterQueueSnapshot) PathToRoot() iter.Seq[*CohortSnapshot] {
```

  Sounds good to me

- **pkg/scheduler/preemption/fairsharing/least_common_ancestor.go:47** by [@gabesaba](https://github.com/gabesaba) — 2025-04-11T06:53:19Z
```diff
@@ -33,27 +33,25 @@ func getAlmostLCAs(t *TargetClusterQueue) (almostLCA, almostLCA) {
 // returning the first Cohort which contains the preemptor
 // ClusterQueue in its subtree.
 func getLCA(t *TargetClusterQueue) *cache.CohortSnapshot {
-	cohort := t.targetCq.Parent()
-	for {
-		if t.ordering.onPathFromRootToPreemptorCQ(cohort) {
-			return cohort
+	for ancestor := range t.targetCq.PathToRoot() {
+		if t.ordering.onPathFromRootToPreemptorCQ(ancestor) {
+			return ancestor
 		}
-		cohort = cohort.Parent()
 	}
+	return nil
 }
 
 // getAlmostLCA traverses from a ClusterQueue towards the root,
 // returning the first Cohort or ClusterQueue that has the
 // LeastCommonAncestor as its parent.
-func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) almostLCA {
+func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) (toRet almostLCA) {
```

  nit: how about this? it unifies the cq/cohort handling case, we don't keep looping even after we found almostLca, and we don't do a check nil  == lca on the very last iteration
  
  ```go
  func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) almostLCA {
  	var aLca almostLCA = cq
  	for ancestor := range cq.PathToRoot() {
  		if ancestor == lca {
  			break
  		}
  		aLca = ancestor
  	}
  	return aLca
  }
  ```
  
  ```go
  func getAlmostLCA(cq *cache.ClusterQueueSnapshot, lca *cache.CohortSnapshot) almostLCA {
  	var aLca almostLCA = cq
  	for ancestor := range cq.PathToRoot() {
  		if ancestor == lca {
  			return aLca
  		}
  		aLca = ancestor
  	}
  	panic("invalid state")
  }
  ```

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-11T16:17:41Z

- **pkg/cache/cache.go:727** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-11T16:17:41Z
```diff
@@ -724,20 +724,20 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	}
 
 	cohort := c.hm.Cohort(cqObj.Spec.Cohort)
-	if cohort == nil {
+	if cohort == nil || !cohort.HasParent() {
```

  Ack, I wasn't sure if empty list would be acceptable

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-11T16:24:23Z

- **pkg/scheduler/fair_sharing_iterator.go:215** by [@mimowo](https://github.com/mimowo) — 2025-04-11T16:24:23Z
```diff
@@ -205,15 +205,12 @@ func (e *entryComparer) computeDRS(rootCohort *cache.CohortSnapshot, cqToEntry m
 
 		// calculate DRS, with workload, for CQ.
 		dominantResourceShare := cq.DominantResourceShare()
-		e.drsValues[drsKey{parentCohort: cq.Parent().GetName(), workloadKey: workload.Key(entry.Obj)}] = dominantResourceShare
 
 		// calculate DRS, with workload, for all Cohorts on
 		// path to root.
-		cohort := cq.Parent()
-		for cohort.HasParent() {
-			dominantResourceShare := cohort.DominantResourceShare()
-			e.drsValues[drsKey{parentCohort: cohort.Parent().GetName(), workloadKey: workload.Key(entry.Obj)}] = dominantResourceShare
```

  It is so lovely to see this cleaned up !

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-11T16:32:44Z

- **pkg/cache/cohort.go:92** by [@mimowo](https://github.com/mimowo) — 2025-04-11T16:32:44Z
```diff
@@ -85,3 +87,16 @@ func (c *cohort) CCParent() hierarchy.CycleCheckable {
 func (c *cohort) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+// Returns all ancestors starting with self and ending with root
+func (c *cohort) PathToRoot() iter.Seq[*cohort] {
```

  I see there is a small semantic difference between cohort.PathToRoot and cq.PathToRoot - as CQ does not start with self. I'm ok to keep this detail reflected just in comment. 
  
  Just exploring alternative, more explicit, naming options:
  - PathFromParentToRoot and PathFromSelfToRoot.
  - or just: FromParentToRoot and FromSelfToRoot, assuming the "Path" is implied
  
  Wdyt @KPostOffice @gabesaba ?

### Review by [@KPostOffice](https://github.com/KPostOffice) (commented) — 2025-04-11T18:30:34Z

- **pkg/cache/cohort.go:92** by [@KPostOffice](https://github.com/KPostOffice) — 2025-04-11T18:30:33Z
```diff
@@ -85,3 +87,16 @@ func (c *cohort) CCParent() hierarchy.CycleCheckable {
 func (c *cohort) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+// Returns all ancestors starting with self and ending with root
+func (c *cohort) PathToRoot() iter.Seq[*cohort] {
```

  wdyt about omitting `From`. So, `PathSelfToRoot` and `PathParentToRoot`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T08:40:53Z

- **pkg/cache/cohort.go:92** by [@mimowo](https://github.com/mimowo) — 2025-04-14T08:40:53Z
```diff
@@ -85,3 +87,16 @@ func (c *cohort) CCParent() hierarchy.CycleCheckable {
 func (c *cohort) fairWeight() *resource.Quantity {
 	return &c.FairWeight
 }
+
+// Returns all ancestors starting with self and ending with root
+func (c *cohort) PathToRoot() iter.Seq[*cohort] {
```

  sgtm, but let's hear from @gabesaba
