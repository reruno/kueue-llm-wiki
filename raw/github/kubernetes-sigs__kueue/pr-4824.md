# PR #4824: Fix getAncestorWorkload if integration disabled.

**Summary**: Fix getAncestorWorkload if integration disabled.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4824

**Last updated**: 2025-04-10T15:15:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/manageJobsWithoutQueueName-if-integration-disabled` → `main`
- **Created**: 2025-03-28T16:29:08Z
- **Updated**: 2025-04-10T15:15:41Z
- **Merged**: 2025-04-10T15:12:48Z
- **Closed**: 2025-04-10T15:12:48Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@dgrove-oss](https://github.com/dgrove-oss), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 8   **+554 / -108**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fix `getAncestorWorkload` when one of middle owner object disabled or not managed by Kueue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #4860

#### Special notes for your reviewer:
In case

```
Job -> JobSet(disabled) -> AppWrapper (with queue name)
```
or

```
Job -> CustomResource -> AppWrapper (with queue name)
```

we should traverse to root object and get Workload for AppWrapper even if one of middle resources not managed by Kueue.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fixed a bug that caused Kueue to create redundant workloads for each Job when manageJobsWithoutQueueName was enabled, JobSet integration was disabled, and AppWrapper was used for JobSet.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-03-28T16:29:10Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-03-28T16:29:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 59a298b65fc9aab03f3b729960d5f3ec4cba3ef4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67f7d9e043a2b40008ab7563 |

### Comment by [@kannon92](https://github.com/kannon92) — 2025-03-28T16:33:33Z

/cc @dgrove-oss

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-02T10:23:43Z

/cc @mimowo @dgrove-oss

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-02T10:37:57Z

Can you please create a dedicated issue with the yamls reproducing the problem. I think this PR is only loosely related to the issue, as the issue causes the problem even if all integrations are enabled.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-02T11:07:21Z

Im at KubeCon and Dave so will probably be able to review next week.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-07T14:37:13Z

The changes to the job reconciler make sense to me.  I did a careful read through the ancestor chain computations and it is a nice cleanup/generalization.

I was expecting changes in webhooks as well.   In particular, we're still just doing a [parent check](https://github.com/kubernetes-sigs/kueue/blob/498c6fab0c74f2d4fe4490875378e9319477e813/pkg/controller/jobframework/integrationmanager.go#L317-L328) in the function that is being used from the various webhook defaulters.  Don't we need to make the same fixes to skip over disabled integrations here?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-07T16:34:03Z

> I was expecting changes in webhooks as well. In particular, we're still just doing a [parent check](https://github.com/kubernetes-sigs/kueue/blob/498c6fab0c74f2d4fe4490875378e9319477e813/pkg/controller/jobframework/integrationmanager.go#L317-L328) in the function that is being used from the various webhook defaulters. Don't we need to make the same fixes to skip over disabled integrations here?

Yeah, I was planning to add it in a separate PR to keep the changes minimal.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-07T16:43:43Z

Yes, I believe the changes are going in the right direction, and I would like to commonize the logic as much as possible.

otoh I find this is quite complex to reason about by static code analysis during review - and I missed something multiple times in this area of code, so smaller PRs are generally better.

If there is no natural split of the fix Im ok with bigger PR, but think if the change to webhooks is supported by some e2e test  demonstraiting the need.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-07T16:48:11Z

> Yeah, I was planning to add it in a separate PR to keep the changes minimal

i think the right question is - are there some issues still in the code if we dont make the changes to webhook we could demonstrate with e2e?

 If we dont think anything is broken - why make the change, is it code deduplication? This would hell me to drive decision. about cherry picking also.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-07T17:06:52Z

> If we dont think anything is broken - why make the change, is it code deduplication? This would hell me to drive decision. about cherry picking also.

This PR focuses on fixing the issue where Kueue creates redundant workloads for each Job when JobSet is disabled. However, we also need to fix the webhooks - specifically for StatefulSets and LeaderWorkerSets.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-07T17:09:04Z

Multiple small PRs makes sense.  Just wanted to make sure we hadn't lost track of the parallel issue in the webhooks.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-10T14:44:40Z

/approve

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-10T14:46:43Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-10T14:46:50Z

LGTM label has been added.  <details>Git tree hash: 7bd6b53dc7f927df425afc95e8b3273a9f8a86c1</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T14:47:41Z

> /lgtm

Thank you! Just added small cleanup.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-10T14:49:26Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-10T14:49:37Z

LGTM label has been added.  <details>Git tree hash: 1904f61ba569b92771345329321ce5e094ede61f</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-10T15:01:49Z

/lgtm
/approve
Thanks! I think this is an awesome work!
/cherry-pick release-0.11

Regarding discussion in https://github.com/kubernetes-sigs/kueue/pull/4824#discussion_r2032813446, I still think we should have a better policy about introducing new interfaces, but the PR is consistent with our current approach, so let's keep it outside of the scope, opened: https://github.com/kubernetes-sigs/kueue/issues/4921.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-04-10T15:01:51Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.11` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4824#issuecomment-2794110313):

>/lgtm
>/approve
>Thanks! I think this is an awesome work!
>/cherry-pick release-0.11
>
>Regarding discussion in https://github.com/kubernetes-sigs/kueue/pull/4824#discussion_r2032813446, I still think we should have a better policy about introducing new interfaces, but the PR is consistent with our current approach, so let's keep it outside of the scope, opened: https://github.com/kubernetes-sigs/kueue/issues/4921.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-10T15:01:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4824#pullrequestreview-2757152612" title="Approved">dgrove-oss</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4824#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4824#issuecomment-2794110313" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-04-10T15:13:28Z

@mimowo: new pull request created: #4922

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4824#issuecomment-2794110313):

>/lgtm
>/approve
>Thanks! I think this is an awesome work!
>/cherry-pick release-0.11
>
>Regarding discussion in https://github.com/kubernetes-sigs/kueue/pull/4824#discussion_r2032813446, I still think we should have a better policy about introducing new interfaces, but the PR is consistent with our current approach, so let's keep it outside of the scope, opened: https://github.com/kubernetes-sigs/kueue/issues/4921.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-07T07:13:06Z

Thank you for the work. I think it would be great to also have a pass from @dgrove-oss

- **charts/kueue/templates/rbac/role.yaml:85** by [@mimowo](https://github.com/mimowo) — 2025-04-07T07:06:37Z
```diff
@@ -82,6 +82,7 @@ rules:
   - apiGroups:
       - apps
     resources:
+      - deployments
```

  @mbobrovskyi can you please explain in more detail in the PR description why this is needed? It does not show up in the issue description (only AppWrapper , JobSet and Job) so, I'm surprised seeing this, but haven't read the PR in detail yet.

- **pkg/controller/jobframework/reconciler.go:315** by [@mimowo](https://github.com/mimowo) — 2025-04-07T07:06:49Z
```diff
@@ -292,27 +291,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	isTopLevelJob := true
-	objectOwner := metav1.GetControllerOf(object)
-	if objectOwner != nil && IsOwnerManagedByKueue(objectOwner) {
-		isTopLevelJob = false
+	managedOwnersChain, err := r.getManagedOwnersChain(ctx, job)
+	if err != nil {
+		return ctrl.Result{}, err
 	}
+	isTopLevelJob := len(managedOwnersChain) == 0
+	ancestorJob := FindAncestorJobManagedByKueue(managedOwnersChain)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that either
 	// have a queue-name label or have a kueue-managed ancestor that has a queue-name label.
 	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
 		if isTopLevelJob {
-			log.V(3).Info("queue-name label is not set, ignoring the job", "queueName", QueueName(job))
+			log.V(3).Info("queue-name label is not set, ignoring the job")
```

  Why is this changed?

- **pkg/controller/jobframework/reconciler.go:573** by [@mimowo](https://github.com/mimowo) — 2025-04-07T07:07:23Z
```diff
@@ -572,68 +566,104 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil {
-		return false, err
+func (r *JobReconciler) getManagedOwnersChain(ctx context.Context, job GenericJob) ([]client.Object, error) {
+	if jobWithCustomManagedOwnersChain, ok := job.(JobWithCustomManagedOwnersChain); ok {
+		return jobWithCustomManagedOwnersChain.ManagedOwnersChain(ctx, r.client, r.record)
+	} else {
+		return ManagedOwnersChain(ctx, r.client, r.record, job.Object())
 	}
-	return ancestor != nil, nil
 }
 
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object, namespace string) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
```

  Why is this changed? If this is just a "drive-by" cleanup, then I suggest to revert - we can do the clean up in a dedicated PR, because maybe not everything needs to be cherry-picked.

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:564** by [@mimowo](https://github.com/mimowo) — 2025-04-07T07:12:45Z
```diff
@@ -410,3 +413,80 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ManageJobsWithoutQueueName without JobSet integration", ginkgo.Ordered, func() {
+	var (
+		ns           *corev1.Namespace
+		defaultRf    *kueue.ResourceFlavor
+		localQueue   *kueue.LocalQueue
+		clusterQueue *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		updateKueueConfiguration(func(cfg *config.Configuration) {
+			cfg.ManageJobsWithoutQueueName = true
+			cfg.Integrations.Frameworks = slices.Filter(nil, cfg.Integrations.Frameworks, func(framework string) bool {
+				return framework != jobset.FrameworkName
+			})
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-")
+		defaultRf = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultRf)).Should(gomega.Succeed())
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(defaultRf.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "2G").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true", func() {
+		ginkgo.It("should create only one workload for parent job", func() {
+			aw := awtesting.MakeAppWrapper("aw", ns.Name).
+				Queue(localQueue.Name).
+				Component(testingjobset.MakeJobSet("job-set", ns.Name).
+					ReplicatedJobs(
+						testingjobset.ReplicatedJobRequirements{
+							Name:        "replicated-job-1",
+							Replicas:    2,
+							Parallelism: 2,
+							Completions: 2,
+							Image:       util.E2eTestAgnHostImage,
+							Args:        util.BehaviorWaitForDeletion,
+						},
+					).
+					SetTypeMeta().
+					Suspend(false).
+					RequestAndLimit("replicated-job-1", corev1.ResourceCPU, "200m").
+					Obj()).
+				Suspend(false).
+				Obj()
+
+			ginkgo.By("creating an unsuspended appwrapper without a queue name", func() {
+				gomega.Expect(k8sClient.Create(ctx, aw)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("check that only one workload is created and admitted", func() {
```

  Would this test actually fail without the fix? The issue description actually says the `appwrapper-app-wrapper-2deaa` workload is created and admitted. Maybe we could instead lower the number of Pods, and check the Job gets finished? I suppose it would never get finished before the fixes, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-07T08:21:36Z

- **charts/kueue/templates/rbac/role.yaml:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-07T08:21:36Z
```diff
@@ -82,6 +82,7 @@ rules:
   - apiGroups:
       - apps
     resources:
+      - deployments
```

  Yes, it happens in the case of Pod -> ReplicaSet -> Deployment. Currently, we traverse to the top-level job even if ReplicaSet is not an integrated resource. This fix simply allows us to retrieve the Deployment resource.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-07T08:23:15Z

- **pkg/controller/jobframework/reconciler.go:315** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-07T08:23:14Z
```diff
@@ -292,27 +291,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	isTopLevelJob := true
-	objectOwner := metav1.GetControllerOf(object)
-	if objectOwner != nil && IsOwnerManagedByKueue(objectOwner) {
-		isTopLevelJob = false
+	managedOwnersChain, err := r.getManagedOwnersChain(ctx, job)
+	if err != nil {
+		return ctrl.Result{}, err
 	}
+	isTopLevelJob := len(managedOwnersChain) == 0
+	ancestorJob := FindAncestorJobManagedByKueue(managedOwnersChain)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that either
 	// have a queue-name label or have a kueue-managed ancestor that has a queue-name label.
 	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
 		if isTopLevelJob {
-			log.V(3).Info("queue-name label is not set, ignoring the job", "queueName", QueueName(job))
+			log.V(3).Info("queue-name label is not set, ignoring the job")
```

  The queue name is an empty string, so this isn't needed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-07T08:25:24Z

- **pkg/controller/jobframework/reconciler.go:573** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-07T08:25:24Z
```diff
@@ -572,68 +566,104 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil {
-		return false, err
+func (r *JobReconciler) getManagedOwnersChain(ctx context.Context, job GenericJob) ([]client.Object, error) {
+	if jobWithCustomManagedOwnersChain, ok := job.(JobWithCustomManagedOwnersChain); ok {
+		return jobWithCustomManagedOwnersChain.ManagedOwnersChain(ctx, r.client, r.record)
+	} else {
+		return ManagedOwnersChain(ctx, r.client, r.record, job.Object())
 	}
-	return ancestor != nil, nil
 }
 
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object, namespace string) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
```

  I didn't get why we need to propagate the namespace, but OK — I’ll create a separate PR for it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-07T08:25:41Z

- **charts/kueue/templates/rbac/role.yaml:85** by [@mimowo](https://github.com/mimowo) — 2025-04-07T08:25:41Z
```diff
@@ -82,6 +82,7 @@ rules:
   - apiGroups:
       - apps
     resources:
+      - deployments
```

  Yeah, but we supported Deployments in the past without it, so I'm wondering how it worked - or maybe it didn't work and this is yet another issue?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-07T08:25:56Z

- **pkg/controller/jobframework/reconciler.go:315** by [@mimowo](https://github.com/mimowo) — 2025-04-07T08:25:56Z
```diff
@@ -292,27 +291,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	isTopLevelJob := true
-	objectOwner := metav1.GetControllerOf(object)
-	if objectOwner != nil && IsOwnerManagedByKueue(objectOwner) {
-		isTopLevelJob = false
+	managedOwnersChain, err := r.getManagedOwnersChain(ctx, job)
+	if err != nil {
+		return ctrl.Result{}, err
 	}
+	isTopLevelJob := len(managedOwnersChain) == 0
+	ancestorJob := FindAncestorJobManagedByKueue(managedOwnersChain)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that either
 	// have a queue-name label or have a kueue-managed ancestor that has a queue-name label.
 	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
 		if isTopLevelJob {
-			log.V(3).Info("queue-name label is not set, ignoring the job", "queueName", QueueName(job))
+			log.V(3).Info("queue-name label is not set, ignoring the job")
```

  ack

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-07T08:27:45Z

- **charts/kueue/templates/rbac/role.yaml:85** by [@mimowo](https://github.com/mimowo) — 2025-04-07T08:27:45Z
```diff
@@ -82,6 +82,7 @@ rules:
   - apiGroups:
       - apps
     resources:
+      - deployments
```

  Also, if this is a bugfix, it would be good to have a test which fails otherwise - do we have such an e2e test?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-07T08:29:50Z

- **charts/kueue/templates/rbac/role.yaml:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-07T08:29:50Z
```diff
@@ -82,6 +82,7 @@ rules:
   - apiGroups:
       - apps
     resources:
+      - deployments
```

  Deployment doesn’t have a workload, so previously it was just ignored at the ReplicaSet level. Since ReplicaSet is not an integration, the Pod is treated as the top-level job. Now we traverse to top level job even if it is not integration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-07T08:39:15Z

- **pkg/controller/jobframework/reconciler.go:573** by [@mimowo](https://github.com/mimowo) — 2025-04-07T08:39:15Z
```diff
@@ -572,68 +566,104 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil {
-		return false, err
+func (r *JobReconciler) getManagedOwnersChain(ctx context.Context, job GenericJob) ([]client.Object, error) {
+	if jobWithCustomManagedOwnersChain, ok := job.(JobWithCustomManagedOwnersChain); ok {
+		return jobWithCustomManagedOwnersChain.ManagedOwnersChain(ctx, r.client, r.record)
+	} else {
+		return ManagedOwnersChain(ctx, r.client, r.record, job.Object())
 	}
-	return ancestor != nil, nil
 }
 
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object, namespace string) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
```

  I think your changes look good, but I'm trying to minimize the distractions. Please submit separate PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-07T08:41:05Z

- **charts/kueue/templates/rbac/role.yaml:85** by [@mimowo](https://github.com/mimowo) — 2025-04-07T08:41:05Z
```diff
@@ -82,6 +82,7 @@ rules:
   - apiGroups:
       - apps
     resources:
+      - deployments
```

  I'm still a bit confused about the rbac changes - do we have an e2e test to demonstrate the need?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-07T09:31:18Z

- **pkg/controller/jobframework/reconciler.go:573** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-07T09:31:18Z
```diff
@@ -572,68 +566,104 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil {
-		return false, err
+func (r *JobReconciler) getManagedOwnersChain(ctx context.Context, job GenericJob) ([]client.Object, error) {
+	if jobWithCustomManagedOwnersChain, ok := job.(JobWithCustomManagedOwnersChain); ok {
+		return jobWithCustomManagedOwnersChain.ManagedOwnersChain(ctx, r.client, r.record)
+	} else {
+		return ManagedOwnersChain(ctx, r.client, r.record, job.Object())
 	}
-	return ancestor != nil, nil
 }
 
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object, namespace string) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj, namespace)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
```

  Done #4876

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T06:40:14Z

- **pkg/controller/jobframework/interface.go:167** by [@mimowo](https://github.com/mimowo) — 2025-04-08T06:40:14Z
```diff
@@ -164,6 +164,10 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+type JobWithCustomManagedOwnersChain interface {
```

  Hm, this feels weird workaround.
  
  I think the new interface, does not belong conceptually to a workload integration. In particular, it indicates the Job object knows how to resolve the entire chain up to AppWrapper (or beyond), but I think it should know nothing about AppWrapper. 
  
  Ideally, the algorithm for determining the chain is just technical detail of Kueue, without any interface exposed. Maybe a small interface like "FindParent" could be justified, because most Jobs are aware of the immediate owner by the ownerReferences.
  
  Please think about restructuring the code to avoid this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T09:21:58Z

- **pkg/controller/jobframework/interface.go:167** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T09:21:57Z
```diff
@@ -164,6 +164,10 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+type JobWithCustomManagedOwnersChain interface {
```

  > I think the new interface, does not belong conceptually to a workload integration. In particular, it indicates the Job object knows how to resolve the entire chain up to AppWrapper (or beyond), but I think it should know nothing about AppWrapper.
  
  Yeah, you're right. I think `OwnsWorkload() bool` is sufficient to determine whether it's a top-level job. If it owns the workload, we can safely skip the suspend-by-parent logic.
  
  > Ideally, the algorithm for determining the chain is just technical detail of Kueue, without any interface exposed. Maybe a small interface like "FindParent" could be justified, because most Jobs are aware of the immediate owner by the ownerReferences.
  
  Unfortunately, I don’t see a solution without introducing a new interface. We still need to skip the suspend-by-parent logic - especially in the case of `Pod (queue-name) -> STS -> Pod (queue-name) -> STS -> LWS (queue-name)`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T09:37:15Z

- **pkg/controller/jobframework/reconciler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-04-08T08:51:26Z
```diff
@@ -572,68 +569,96 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
+
+		parentObj := GetEmptyOwnerObject(owner)
+		if parentObj == nil {
+			managed = false
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+
+		// Ignore unmanaged resources and disabled jobs.
+		if managed {
+			chain = append(chain, parentObj)
 		}
-		currentJob = parentJob
+
+		currentObj = parentObj
 		if len(seen) > managedOwnersChainLimit {
-			r.record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit)
+			if record != nil {
```

  Why the recording is conditional now? I would prefer to keep as is.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T09:40:00Z

- **pkg/controller/jobframework/reconciler.go:647** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T09:40:00Z
```diff
@@ -572,68 +569,96 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
+
+		parentObj := GetEmptyOwnerObject(owner)
+		if parentObj == nil {
+			managed = false
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+
+		// Ignore unmanaged resources and disabled jobs.
+		if managed {
+			chain = append(chain, parentObj)
 		}
-		currentJob = parentJob
+
+		currentObj = parentObj
 		if len(seen) > managedOwnersChainLimit {
-			r.record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit)
+			if record != nil {
```

  Reverted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T09:42:33Z

- **pkg/controller/jobframework/interface.go:167** by [@mimowo](https://github.com/mimowo) — 2025-04-08T09:42:33Z
```diff
@@ -164,6 +164,10 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+type JobWithCustomManagedOwnersChain interface {
```

  I see, it still feels off with the OwnsWorkload. For example, I don't understand why we are checking isServing there - I will post another comment. 
  
  I would think that what matters is "is it pod integration"? Possibly, I'm missing something, I will try to review in more detail by tomorrow, but some explanation on the isServing could be useful.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T09:43:29Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-08T09:43:28Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  Yeah, I don't understand that. What if you returned true unconditionally here for Pods? I assume it wouldn't work, but I don't understand why.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T09:45:38Z

- **pkg/controller/jobframework/interface.go:167** by [@mimowo](https://github.com/mimowo) — 2025-04-08T09:45:38Z
```diff
@@ -164,6 +164,10 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+type JobWithCustomManagedOwnersChain interface {
```

  Also, the new interface makes it harder to cherry-pick, and this is a bug we would like to cherry-pick, so that we can fix LWS.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T09:58:52Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mimowo](https://github.com/mimowo) — 2025-04-08T09:58:52Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  Does it need to be exported?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T09:59:09Z

- **pkg/controller/jobframework/reconciler.go:605** by [@mimowo](https://github.com/mimowo) — 2025-04-08T09:59:09Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
```

  Does it need to be exported?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T10:01:30Z

- **pkg/controller/jobframework/reconciler.go:614** by [@mimowo](https://github.com/mimowo) — 2025-04-08T10:01:29Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
```

  Why break, and not just `return nil nil`? If there is a loop I don't think we want to return the chain. I think ideally, it is an error, but it wasn't an error before, so I would be ok to just have nil.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T10:02:00Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:564** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T10:02:00Z
```diff
@@ -410,3 +413,80 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ManageJobsWithoutQueueName without JobSet integration", ginkgo.Ordered, func() {
+	var (
+		ns           *corev1.Namespace
+		defaultRf    *kueue.ResourceFlavor
+		localQueue   *kueue.LocalQueue
+		clusterQueue *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		updateKueueConfiguration(func(cfg *config.Configuration) {
+			cfg.ManageJobsWithoutQueueName = true
+			cfg.Integrations.Frameworks = slices.Filter(nil, cfg.Integrations.Frameworks, func(framework string) bool {
+				return framework != jobset.FrameworkName
+			})
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-")
+		defaultRf = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultRf)).Should(gomega.Succeed())
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(defaultRf.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "2G").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true", func() {
+		ginkgo.It("should create only one workload for parent job", func() {
+			aw := awtesting.MakeAppWrapper("aw", ns.Name).
+				Queue(localQueue.Name).
+				Component(testingjobset.MakeJobSet("job-set", ns.Name).
+					ReplicatedJobs(
+						testingjobset.ReplicatedJobRequirements{
+							Name:        "replicated-job-1",
+							Replicas:    2,
+							Parallelism: 2,
+							Completions: 2,
+							Image:       util.E2eTestAgnHostImage,
+							Args:        util.BehaviorWaitForDeletion,
+						},
+					).
+					SetTypeMeta().
+					Suspend(false).
+					RequestAndLimit("replicated-job-1", corev1.ResourceCPU, "200m").
+					Obj()).
+				Suspend(false).
+				Obj()
+
+			ginkgo.By("creating an unsuspended appwrapper without a queue name", func() {
+				gomega.Expect(k8sClient.Create(ctx, aw)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("check that only one workload is created and admitted", func() {
```

  > Would this test actually fail without the fix? 
  
  Yeah, it is failing without changes. Due to we have 
  
  ```
  kubectl get wl -A
  NAMESPACE   NAME                                   QUEUE   RESERVED IN     ADMITTED   FINISHED   AGE
  e2e-44sms   appwrapper-aw-dba9f                    main    cluster-queue   True                  44s
  e2e-44sms   job-job-set-replicated-job-1-0-c0d8f                                                 44s
  e2e-44sms   job-job-set-replicated-job-1-1-ca9b1                                                 44s
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T10:02:55Z

- **pkg/controller/jobframework/reconciler.go:620** by [@mimowo](https://github.com/mimowo) — 2025-04-08T10:02:55Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
```

  Maybe just `return chain, nil` for the ease of readability - no need to jump reading the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T10:03:58Z

- **pkg/controller/jobframework/reconciler.go:617** by [@mimowo](https://github.com/mimowo) — 2025-04-08T10:03:58Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
```

  Why introducing the var since it can be derived from `parentObj != nil`, unless I'm missing something?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T10:04:45Z

- **pkg/controller/jobframework/reconciler.go:655** by [@mimowo](https://github.com/mimowo) — 2025-04-08T10:04:44Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
+
+		parentObj := GetEmptyOwnerObject(owner)
+		if parentObj == nil {
+			managed = false
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+
+		// Ignore unmanaged resources and disabled jobs.
+		if managed {
+			chain = append(chain, parentObj)
 		}
-		currentJob = parentJob
+
+		currentObj = parentObj
 		if len(seen) > managedOwnersChainLimit {
-			r.record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit)
+			record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
+				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit,
+			)
 			ctrl.LoggerFrom(ctx).V(2).Info(
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded managedOwnersChainlimit",
+				"WARNING: Terminated search for Kueue-managed Job because ancestor depth exceeded managedOwnersChainLimit",
 				"limit ", managedOwnersChainLimit,
-				"lastParentReached", parentJob,
+				"lastParentReached", parentObj,
 			)
-			return nil, nil
+			break
```

  I would suggest to just keep the `return` for the readability, and smaller diff.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T10:07:15Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:564** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T10:07:15Z
```diff
@@ -410,3 +413,80 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ManageJobsWithoutQueueName without JobSet integration", ginkgo.Ordered, func() {
+	var (
+		ns           *corev1.Namespace
+		defaultRf    *kueue.ResourceFlavor
+		localQueue   *kueue.LocalQueue
+		clusterQueue *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		updateKueueConfiguration(func(cfg *config.Configuration) {
+			cfg.ManageJobsWithoutQueueName = true
+			cfg.Integrations.Frameworks = slices.Filter(nil, cfg.Integrations.Frameworks, func(framework string) bool {
+				return framework != jobset.FrameworkName
+			})
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-")
+		defaultRf = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultRf)).Should(gomega.Succeed())
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(defaultRf.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "2G").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true", func() {
+		ginkgo.It("should create only one workload for parent job", func() {
+			aw := awtesting.MakeAppWrapper("aw", ns.Name).
+				Queue(localQueue.Name).
+				Component(testingjobset.MakeJobSet("job-set", ns.Name).
+					ReplicatedJobs(
+						testingjobset.ReplicatedJobRequirements{
+							Name:        "replicated-job-1",
+							Replicas:    2,
+							Parallelism: 2,
+							Completions: 2,
+							Image:       util.E2eTestAgnHostImage,
+							Args:        util.BehaviorWaitForDeletion,
+						},
+					).
+					SetTypeMeta().
+					Suspend(false).
+					RequestAndLimit("replicated-job-1", corev1.ResourceCPU, "200m").
+					Obj()).
+				Suspend(false).
+				Obj()
+
+			ginkgo.By("creating an unsuspended appwrapper without a queue name", func() {
+				gomega.Expect(k8sClient.Create(ctx, aw)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("check that only one workload is created and admitted", func() {
```

  But you right. This is not stable test need to optimize it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T10:07:23Z

- **pkg/controller/jobframework/reconciler.go:605** by [@mimowo](https://github.com/mimowo) — 2025-04-08T10:07:23Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
```

  Actually, we don't need to return the entire chain, just the top-level managed job. So, I would not prefer not to need to keep the entire chain in memory. We just need to return the top-level one.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T10:50:06Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:564** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T10:50:06Z
```diff
@@ -410,3 +413,80 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ManageJobsWithoutQueueName without JobSet integration", ginkgo.Ordered, func() {
+	var (
+		ns           *corev1.Namespace
+		defaultRf    *kueue.ResourceFlavor
+		localQueue   *kueue.LocalQueue
+		clusterQueue *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		updateKueueConfiguration(func(cfg *config.Configuration) {
+			cfg.ManageJobsWithoutQueueName = true
+			cfg.Integrations.Frameworks = slices.Filter(nil, cfg.Integrations.Frameworks, func(framework string) bool {
+				return framework != jobset.FrameworkName
+			})
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-")
+		defaultRf = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultRf)).Should(gomega.Succeed())
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(defaultRf.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "2G").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true", func() {
+		ginkgo.It("should create only one workload for parent job", func() {
+			aw := awtesting.MakeAppWrapper("aw", ns.Name).
+				Queue(localQueue.Name).
+				Component(testingjobset.MakeJobSet("job-set", ns.Name).
+					ReplicatedJobs(
+						testingjobset.ReplicatedJobRequirements{
+							Name:        "replicated-job-1",
+							Replicas:    2,
+							Parallelism: 2,
+							Completions: 2,
+							Image:       util.E2eTestAgnHostImage,
+							Args:        util.BehaviorWaitForDeletion,
+						},
+					).
+					SetTypeMeta().
+					Suspend(false).
+					RequestAndLimit("replicated-job-1", corev1.ResourceCPU, "200m").
+					Obj()).
+				Suspend(false).
+				Obj()
+
+			ginkgo.By("creating an unsuspended appwrapper without a queue name", func() {
+				gomega.Expect(k8sClient.Create(ctx, aw)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("check that only one workload is created and admitted", func() {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T11:16:22Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T11:16:21Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  Yes, it should be exported due to circular dependencies in the unit tests.
  
  https://github.com/kubernetes-sigs/kueue/blob/be02d7a12d71e444a03fe38172eca6096de7b7a5/pkg/controller/jobframework/reconciler_test.go#L17
  
  https://github.com/kubernetes-sigs/kueue/blob/be02d7a12d71e444a03fe38172eca6096de7b7a5/pkg/controller/jobframework/reconciler_test.go#L215
  
  We'll also reuse this function in the webhooks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T11:17:57Z

- **pkg/controller/jobframework/reconciler.go:605** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T11:17:57Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
```

  The same problem. See https://github.com/kubernetes-sigs/kueue/pull/4824#discussion_r2032967182.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T11:28:07Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mimowo](https://github.com/mimowo) — 2025-04-08T11:28:07Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  Hopefully we don't need it, I imagine rather than finding the entire chain we could just update the implementation of the getAncestorJobManagedByKueue function which is not exported.
  
  > We'll also reuse this function in the webhooks.
  
  we can export when proven to be needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T11:28:40Z

- **pkg/controller/jobframework/reconciler.go:605** by [@mimowo](https://github.com/mimowo) — 2025-04-08T11:28:40Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
```

  Hopefully we don't need the function which returns the entire chain at all, also asked here: https://github.com/kubernetes-sigs/kueue/pull/4824#discussion_r2032855258.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T11:38:54Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T11:38:54Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  > Hopefully we don't need it, I imagine rather than finding the entire chain we could just update the implementation of the getAncestorJobManagedByKueue function which is not exported.
  
  We still need this function to be exported due to we are using it in test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T11:44:04Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mimowo](https://github.com/mimowo) — 2025-04-08T11:44:04Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  Maybe, but maybe we don't need a test for a function which is not exported. 
  
  Actually, we don't have a test for getAncestorJobManagedByKueue today. 
  
  Ideally, we make a decision if we need to export a function or not based on our needs in code, not based on tests. Then, we cover functions in tests which are exported. For example I imagine we could cover this at the `ReconcileGenericJob` level. This is ideal, but we may take a pragmatic shortcut, if really needed. I'm not convinced at the moment.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T13:00:35Z

- **pkg/controller/jobframework/reconciler.go:617** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T13:00:35Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
```

  Yes, but we are replacing it on next steps
  https://github.com/kubernetes-sigs/kueue/blob/88bdf345160e7f19c96be31c38906bba541ad0a1/pkg/controller/jobframework/reconciler.go#L617-L627

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T13:00:58Z

- **pkg/controller/jobframework/reconciler.go:655** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T13:00:58Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
+
+		parentObj := GetEmptyOwnerObject(owner)
+		if parentObj == nil {
+			managed = false
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+
+		// Ignore unmanaged resources and disabled jobs.
+		if managed {
+			chain = append(chain, parentObj)
 		}
-		currentJob = parentJob
+
+		currentObj = parentObj
 		if len(seen) > managedOwnersChainLimit {
-			r.record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit)
+			record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
+				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit,
+			)
 			ctrl.LoggerFrom(ctx).V(2).Info(
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded managedOwnersChainlimit",
+				"WARNING: Terminated search for Kueue-managed Job because ancestor depth exceeded managedOwnersChainLimit",
 				"limit ", managedOwnersChainLimit,
-				"lastParentReached", parentJob,
+				"lastParentReached", parentObj,
 			)
-			return nil, nil
+			break
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T13:01:14Z

- **pkg/controller/jobframework/reconciler.go:620** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T13:01:13Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T13:02:07Z

- **pkg/controller/jobframework/reconciler.go:614** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T13:02:07Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
```

  Yeah, this is good call to return error here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T13:02:58Z

- **pkg/controller/jobframework/reconciler.go:605** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T13:02:57Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
```

  Yeah, you right, we can return just one managed object. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T13:04:16Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T13:04:15Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  Currently we are managing only managed by kueue pod and pod groups. So you right we can return true here in all cases.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T14:06:26Z

- **pkg/controller/jobframework/reconciler.go:617** by [@mimowo](https://github.com/mimowo) — 2025-04-08T14:06:26Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
```

  ah, got it, so we could just say `managed := parantObj != nil` before `if parentObj == nil { `

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T14:07:19Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-08T14:07:19Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  Yeah, so basically we need to check "is this a Pod integration", so we can check if this implements the ComposableJob interface - no need for new interface

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-04-08T14:42:23Z

- **pkg/controller/jobframework/reconciler.go:647** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-08T14:42:22Z
```diff
@@ -572,68 +569,96 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
+
+		parentObj := GetEmptyOwnerObject(owner)
+		if parentObj == nil {
+			managed = false
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+
+		// Ignore unmanaged resources and disabled jobs.
+		if managed {
+			chain = append(chain, parentObj)
 		}
-		currentJob = parentJob
+
+		currentObj = parentObj
 		if len(seen) > managedOwnersChainLimit {
-			r.record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit)
+			if record != nil {
```

  Fine to revert it to keep this PR minimal; but I think the reason @mbobrovskyi changed it to became conditional is that if (when) we call this helper from the webhook we either have to "plumb" a recorder through all the webhooks or handle the recorder being nil here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T14:52:41Z

- **pkg/controller/jobframework/reconciler.go:647** by [@mimowo](https://github.com/mimowo) — 2025-04-08T14:52:41Z
```diff
@@ -572,68 +569,96 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
+
+		parentObj := GetEmptyOwnerObject(owner)
+		if parentObj == nil {
+			managed = false
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+
+		// Ignore unmanaged resources and disabled jobs.
+		if managed {
+			chain = append(chain, parentObj)
 		}
-		currentJob = parentJob
+
+		currentObj = parentObj
 		if len(seen) > managedOwnersChainLimit {
-			r.record.Eventf(jobObj, corev1.EventTypeWarning, ReasonJobNestingTooDeep,
-				"Terminated search for Kueue-managed Job because ancestor depth exceeded limit of %d", managedOwnersChainLimit)
+			if record != nil {
```

  I see, I would still suggest avoiding dead code. We can revisit when we follow up with the webhook fix to support LWS.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T15:36:24Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T15:36:24Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  > Yeah, so basically we need to check "is this a Pod integration", so we can check if this implements the ComposableJob interface - no need for new interface
  
  Not sure this is the correct approach. Right now, we only have one ComposableJob integration and this is a Pod, but in the future, other implementations might be possible.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-08T15:42:40Z

- **pkg/controller/jobframework/reconciler.go:617** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-08T15:42:40Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
+	for i := len(managedOwnersChain) - 1; i >= 0; i-- {
+		owner := managedOwnersChain[i]
+		if QueueNameForObject(owner) != "" {
+			return owner
+		}
+	}
+	return nil
+}
+
+func ManagedOwnersChain(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) ([]client.Object, error) {
+	var (
+		chain      = make([]client.Object, 0, managedOwnersChainLimit)
+		seen       = sets.New[types.UID]()
+		currentObj = jobObj
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			break
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			break
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		managed := true
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-08T16:02:31Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-08T16:02:31Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  I know where you are coming from, but I would rather just check that, rather than over-engineer with some weird interface like OwnsWorkload. The problem with interfaces is that users (developers) can start using them, and then it is harder to avoid breaking changes, so I like the conservative approach. We can always refactor if there is such a need. Or, we can also check specifically if the integration is Pod (not just CompostableJob).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T06:37:50Z

- **charts/kueue/templates/rbac/role.yaml:85** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T06:37:50Z
```diff
@@ -82,6 +82,7 @@ rules:
   - apiGroups:
       - apps
     resources:
+      - deployments
```

  After last changes we don't need it. Due to we are skipping suspend by parent logic for Pod integration at all.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T06:39:17Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T06:39:16Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  > Actually, we don't have a test for getAncestorJobManagedByKueue today.
  
  Yes, but we have for IsAncestorJobManaged that using this function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T06:40:47Z

- **pkg/controller/jobframework/interface.go:167** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T06:40:46Z
```diff
@@ -164,6 +164,10 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+type JobWithCustomManagedOwnersChain interface {
```

  > Also, the new interface makes it harder to cherry-pick, and this is a bug we would like to cherry-pick, so that we can fix LWS.
  
  I agree but we still need to skip Pod integration somehow.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T06:48:05Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T06:48:05Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  > I know where you are coming from, but I would rather just check that, rather than over-engineer with some weird interface like OwnsWorkload. The problem with interfaces is that users (developers) can start using them, and then it is harder to avoid breaking changes, so I like the conservative approach. We can always refactor if there is such a need. 
  
  I still believe we need a new interface. ComposableJob doesn’t suit this logic, and users might implement it for entirely different purposes. Relying on it here could result in incorrect behavior.
  
  > Or, we can also check specifically if the integration is Pod (not just CompostableJob).
  
  I'm not so sure about that. Introducing a new interface specifically for skipping the "suspend-by-parent" logic seems like a cleaner and more future-proof approach. If we hardcode this logic to check whether it’s a Pod, we lose the abstraction provided by the JobFramework interface.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T07:01:03Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T07:01:02Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  > Ideally, we make a decision if we need to export a function or not based on our needs in code, not based on tests. Then, we cover functions in tests which are exported. For example I imagine we could cover this at the ReconcileGenericJob level. This is ideal, but we may take a pragmatic shortcut, if really needed. I'm not convinced at the moment.
  
  I'm thinking of this function more as a library, since we’ll also use it in the webhooks. We decided to split this PR, but the webhook logic still needs to be fixed as well. Unfortunately, we can’t carefully test this function through the Reconcile function, so I’d propose testing it separately.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T07:16:29Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-09T07:16:29Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  The abstraction of jobframework was designed and introduced to support writing custom in-house integrations. This value stays I think, the users don't need to implement this interface. The only type which would implement it is Pod integration, and it is built-in, so I don't see what we are losing. 
  
  Is there any use case you see where users (developers of in-house Jobs) would benefit from the new OwnsWorkload interface? I think it would just confuse them. Every new interface is a question to the developer - how do you implement it? TBH, I would have no idea as an external developer how to implement it. It requires some thinking about the good name, and documenting the interface. We didn't do any of that.
  
  I think for now we could have just a local helper function `isPodIntegration` (or ownsWorkload), which detects only that. If we have use cases of users asking for something like the OwnsWorkload interface, then we can always introduce it in the future releases, properly documented.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T07:19:32Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-09T07:19:32Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  Finally, our users already know that po integration is special, and this is reflected in our documentation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T07:21:34Z

- **pkg/controller/jobframework/reconciler.go:580** by [@mimowo](https://github.com/mimowo) — 2025-04-09T07:21:34Z
```diff
@@ -572,66 +569,80 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
```

  Is this changed, or just re-formatted? If re-formatted, please revert.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T07:27:14Z

- **pkg/util/testingjobs/jobset/wrappers.go:206** by [@mimowo](https://github.com/mimowo) — 2025-04-09T07:27:14Z
```diff
@@ -193,3 +200,17 @@ func (j *JobSetWrapper) ManagedBy(c string) *JobSetWrapper {
 	j.Spec.ManagedBy = &c
 	return j
 }
+
+// OwnerReference adds a ownerReference to the default container.
+func (j *JobSetWrapper) OwnerReference(ownerName string, ownerGVK schema.GroupVersionKind) *JobSetWrapper {
+	j.ObjectMeta.OwnerReferences = []metav1.OwnerReference{
```

  Please make it append rather than set, for semantical consistency with Workload: https://github.com/kubernetes-sigs/kueue/blob/d57b40c9d5596f10c86de98feed5bb3a47bc219e/pkg/util/testing/wrappers.go#L328-L331
  
  Even better if we can somehow commonize it, but I would be a follow up probably to avoid increasing the diff size.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T07:31:13Z

- **pkg/controller/jobframework/reconciler.go:631** by [@mimowo](https://github.com/mimowo) — 2025-04-09T07:31:13Z
```diff
@@ -572,66 +569,80 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	var (
+		log         = ctrl.LoggerFrom(ctx)
+		seen        = sets.New[types.UID]()
+		currentObj  = jobObj
+		topLevelJob client.Object
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			log.Error(ErrCyclicOwnership,
+				"Terminated search for Kueue-managed Job because of cyclic ownership",
+				"owner", currentObj,
+			)
+			return nil, ErrCyclicOwnership
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			return topLevelJob, nil
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		parentObj := GetEmptyOwnerObject(owner)
+		managed := parentObj != nil
+		if parentObj == nil {
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+		// Ignore unmanaged resources and disabled jobs.
```

  I don't understand the comment, and it does not match the checks, unless I'm missing something. It says "Ignore unmanaged", but checks "managed". Also, I don't understand what is "resources" in this context.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T07:40:41Z

- **pkg/controller/jobframework/reconciler.go:595** by [@mimowo](https://github.com/mimowo) — 2025-04-09T07:40:41Z
```diff
@@ -572,68 +569,94 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if ancestorJob == nil {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func FindAncestorJobManagedByKueue(managedOwnersChain []client.Object) client.Object {
```

  Yeah, sometimes we export just for testing, as IsAncestorJobManaged, it is not ideal and may clean it up in a follow up. Sometimes it is omission during review, sometimes "pragmatic" shortcut just before release, but I don't think it is "best practice" we should cultivate when we have other choices.
  
  However, in this context as you say we will need some function to be used in webhooks (not sure if this or another), so some function will need to be exported. So, I'm ok to export this function along with `IsAncestorJobManaged`, we can then consolidate, and export one of them.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T07:42:12Z

- **pkg/controller/jobframework/reconciler.go:631** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T07:42:12Z
```diff
@@ -572,66 +569,80 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	var (
+		log         = ctrl.LoggerFrom(ctx)
+		seen        = sets.New[types.UID]()
+		currentObj  = jobObj
+		topLevelJob client.Object
+	)
+
 	for {
-		if seen.Has(currentJob.GetUID()) {
-			return nil, nil
+		if seen.Has(currentObj.GetUID()) {
+			log.Error(ErrCyclicOwnership,
+				"Terminated search for Kueue-managed Job because of cyclic ownership",
+				"owner", currentObj,
+			)
+			return nil, ErrCyclicOwnership
 		}
-		seen.Insert(currentJob.GetUID())
+		seen.Insert(currentObj.GetUID())
 
-		owner := metav1.GetControllerOf(currentJob)
-		if owner == nil || !IsOwnerManagedByKueue(owner) {
-			return nil, nil
+		owner := metav1.GetControllerOf(currentObj)
+		if owner == nil {
+			return topLevelJob, nil
 		}
-		parentJob := GetEmptyOwnerObject(owner)
-		if parentJob == nil {
-			return nil, fmt.Errorf("workload owner %v: %w", owner, ErrUnknownWorkloadOwner)
+
+		parentObj := GetEmptyOwnerObject(owner)
+		managed := parentObj != nil
+		if parentObj == nil {
+			parentObj = &metav1.PartialObjectMetadata{
+				TypeMeta: metav1.TypeMeta{
+					APIVersion: owner.APIVersion,
+					Kind:       owner.Kind,
+				},
+			}
 		}
-		if err := r.client.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentJob); err != nil {
+		if err := c.Get(ctx, client.ObjectKey{Name: owner.Name, Namespace: jobObj.GetNamespace()}, parentObj); err != nil {
 			return nil, errors.Join(ErrWorkloadOwnerNotFound, err)
 		}
-		if QueueNameForObject(parentJob) != "" {
-			return parentJob, nil
+		// Ignore unmanaged resources and disabled jobs.
```

  I think this was missing in the previous logic. I've removed the comment.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T07:45:44Z

- **pkg/util/testingjobs/jobset/wrappers.go:206** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T07:45:44Z
```diff
@@ -193,3 +200,17 @@ func (j *JobSetWrapper) ManagedBy(c string) *JobSetWrapper {
 	j.Spec.ManagedBy = &c
 	return j
 }
+
+// OwnerReference adds a ownerReference to the default container.
+func (j *JobSetWrapper) OwnerReference(ownerName string, ownerGVK schema.GroupVersionKind) *JobSetWrapper {
+	j.ObjectMeta.OwnerReferences = []metav1.OwnerReference{
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T07:45:54Z

- **pkg/controller/jobframework/reconciler.go:580** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T07:45:54Z
```diff
@@ -572,66 +569,80 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()),
+		client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())},
+	); client.IgnoreNotFound(err) != nil {
```

  Reverted

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T07:53:27Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T07:53:27Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  > Is there any use case you see where users (developers of in-house Jobs) would benefit from the new OwnsWorkload interface?
  
  Yes, if user wants to skip the suspend-by-parent logic in a custom integration, I agree that the interface name might not be fully accurate, but the idea behind this interface seems useful.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T08:45:34Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-09T08:45:34Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  > Yes, if user wants to skip the suspend-by-parent logic in a custom integration
  
  Why would they want to do it? I don't understand the use case yet. 
  
  Again, maybe this is useful, but we can handle this as a new feature, ideally as a KEP, where we discuss the use cases properly. Then, we properly name and document the interface.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T09:24:24Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-09T09:24:24Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  Seems like we are looping the same arguments. I would just do something like:
  ```golang
  func isPodIntegration(job GenericJob) bool {
    _, ok := job.(Pod)
    return ok
  ```
  for the bugfix (which we aim to cherry-pick), and consider the new interface as a new feature in the future, when requested by users, because I don't like to bloat our interfaces. However, if there is support among other maintainers, I can be ok with the OwnsWorkload interface.
  
  wdyt @tenzen-y @gabesaba ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T15:45:00Z

- **pkg/controller/jobframework/reconciler.go:585** by [@mimowo](https://github.com/mimowo) — 2025-04-09T15:45:00Z
```diff
@@ -572,66 +569,77 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())}); client.IgnoreNotFound(err) != nil {
```

  nit: keep the ancestor name to minimize the diff.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T15:46:33Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mimowo](https://github.com/mimowo) — 2025-04-09T15:46:33Z
```diff
@@ -572,66 +569,77 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  ```suggestion
  func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
  ```
  I think we don't need to say "TopLevelAncestor". I think the semantic of the function didn't really change wrt to what we return. IIUC the main difference is that we skip over disabled Jobs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T15:49:02Z

- **pkg/controller/jobframework/reconciler.go:606** by [@mimowo](https://github.com/mimowo) — 2025-04-09T15:49:02Z
```diff
@@ -572,66 +569,77 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	var (
+		log         = ctrl.LoggerFrom(ctx)
+		seen        = sets.New[types.UID]()
+		currentObj  = jobObj
+		topLevelJob client.Object
+	)
```

  nit, can we return to the previous form 
  ```
  seen := sets.New[types.UID]()
  currentJob := jobObj
  ```
  just so that we minimize the diff with distractions.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:03:01Z

- **pkg/controller/jobframework/interface.go:170** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:02:57Z
```diff
@@ -164,6 +164,15 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+// TopLevelJob interface should be implemented by generic jobs
+// that are considered top-level and should skip traversal to
+// find an ancestor job managed by Kueue.
+// This interface is optional.
```

  Maybe  "considered top-level" is a bit unclear, because the Job (Pod) will most often have an owner. So, considered top-level de-facto means owns/manages workload.
  
  Maybe something like:
  
  // JobOwnsWorkload interface is an optional interface used to indicate
  // that the Job owns/manages the Workload object, regardless of the Job
  // owner references.
  // (this interface is optional)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:09:42Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:571** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:09:42Z
```diff
@@ -492,3 +495,97 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ManageJobsWithoutQueueName without JobSet integration", ginkgo.Ordered, func() {
+	var (
+		ns           *corev1.Namespace
+		defaultRf    *kueue.ResourceFlavor
+		localQueue   *kueue.LocalQueue
+		clusterQueue *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		updateKueueConfiguration(func(cfg *config.Configuration) {
+			cfg.ManageJobsWithoutQueueName = true
+			cfg.Integrations.Frameworks = slices.Filter(nil, cfg.Integrations.Frameworks, func(framework string) bool {
+				return framework != jobset.FrameworkName
+			})
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-")
+		defaultRf = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultRf)).Should(gomega.Succeed())
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(defaultRf.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "2G").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true", func() {
+		ginkgo.It("should create only one workload for parent job", func() {
+			jobSetKey := client.ObjectKey{Name: "job-set", Namespace: ns.Name}
+
+			aw := awtesting.MakeAppWrapper("aw", ns.Name).
+				Queue(localQueue.Name).
+				Component(awtesting.Component{
+					Template: testingjobset.MakeJobSet(jobSetKey.Name, jobSetKey.Namespace).
+						ReplicatedJobs(
+							testingjobset.ReplicatedJobRequirements{
+								Name:        "replicated-job-1",
+								Replicas:    2,
+								Parallelism: 2,
+								Completions: 2,
+								Image:       util.E2eTestAgnHostImage,
+								Args:        util.BehaviorExitFast,
+							},
+						).
+						SetTypeMeta().
+						Suspend(false).
+						RequestAndLimit("replicated-job-1", corev1.ResourceCPU, "200m").
+						Obj(),
+				}).
+				Suspend(false).
+				Obj()
+
+			ginkgo.By("creating an unsuspended AppWrapper without a queue name", func() {
+				gomega.Expect(k8sClient.Create(ctx, aw)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("AppWrapper is unsuspended", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(aw), aw)).To(gomega.Succeed())
+					g.Expect(aw.Spec.Suspend).Should(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
  ```
  nit, it is probably safer to use LongTimeout, we had a similar case causing flakes, see: https://github.com/kubernetes-sigs/kueue/pull/4785

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:10:16Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:10:16Z
```diff
@@ -572,66 +569,77 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  The logic has changed. Previously, we searched for the next ancestor job with a queue-name; now, we look for the top-level ancestor job with a queue-name.
  
  For example:
  Job (with queue-name) → JobSet (with queue-name) → AppWrapper (with queue-name)
  
  Previously, we returned JobSet.
  Now, we return AppWrapper.
  
  So I believe `GetTopLevelAncestorJobManagedByKueue` is an appropriate name for this function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:11:47Z

- **pkg/controller/jobframework/interface.go:170** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:11:46Z
```diff
@@ -164,6 +164,15 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+// TopLevelJob interface should be implemented by generic jobs
+// that are considered top-level and should skip traversal to
+// find an ancestor job managed by Kueue.
+// This interface is optional.
```

  Ah, I see, the IsTopLevel probably reads better with the pre-existing code in pkg/controller/jobframework/reconciler.go, ok.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:12:55Z

- **pkg/controller/jobframework/interface.go:170** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:12:55Z
```diff
@@ -164,6 +164,15 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+// TopLevelJob interface should be implemented by generic jobs
+// that are considered top-level and should skip traversal to
+// find an ancestor job managed by Kueue.
+// This interface is optional.
```

  Just nit proposal then
  ```suggestion
  // TopLevelJob interface is an optional interface used to indicate
  // that the Job owns/manages the Workload object, regardless of the Job
  // owner references.
  // This interface is optional.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:13:41Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:571** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:13:41Z
```diff
@@ -492,3 +495,97 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ManageJobsWithoutQueueName without JobSet integration", ginkgo.Ordered, func() {
+	var (
+		ns           *corev1.Namespace
+		defaultRf    *kueue.ResourceFlavor
+		localQueue   *kueue.LocalQueue
+		clusterQueue *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		updateKueueConfiguration(func(cfg *config.Configuration) {
+			cfg.ManageJobsWithoutQueueName = true
+			cfg.Integrations.Frameworks = slices.Filter(nil, cfg.Integrations.Frameworks, func(framework string) bool {
+				return framework != jobset.FrameworkName
+			})
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-")
+		defaultRf = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultRf)).Should(gomega.Succeed())
+		clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas(defaultRf.Name).
+					Resource(corev1.ResourceCPU, "2").
+					Resource(corev1.ResourceMemory, "2G").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+		localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.LongTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true", func() {
+		ginkgo.It("should create only one workload for parent job", func() {
+			jobSetKey := client.ObjectKey{Name: "job-set", Namespace: ns.Name}
+
+			aw := awtesting.MakeAppWrapper("aw", ns.Name).
+				Queue(localQueue.Name).
+				Component(awtesting.Component{
+					Template: testingjobset.MakeJobSet(jobSetKey.Name, jobSetKey.Namespace).
+						ReplicatedJobs(
+							testingjobset.ReplicatedJobRequirements{
+								Name:        "replicated-job-1",
+								Replicas:    2,
+								Parallelism: 2,
+								Completions: 2,
+								Image:       util.E2eTestAgnHostImage,
+								Args:        util.BehaviorExitFast,
+							},
+						).
+						SetTypeMeta().
+						Suspend(false).
+						RequestAndLimit("replicated-job-1", corev1.ResourceCPU, "200m").
+						Obj(),
+				}).
+				Suspend(false).
+				Obj()
+
+			ginkgo.By("creating an unsuspended AppWrapper without a queue name", func() {
+				gomega.Expect(k8sClient.Create(ctx, aw)).To(gomega.Succeed())
+			})
+
+			ginkgo.By("AppWrapper is unsuspended", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(aw), aw)).To(gomega.Succeed())
+					g.Expect(aw.Spec.Suspend).Should(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  Hmm, interesting.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:18:34Z

- **pkg/controller/jobframework/interface.go:170** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:18:33Z
```diff
@@ -164,6 +164,15 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+// TopLevelJob interface should be implemented by generic jobs
+// that are considered top-level and should skip traversal to
+// find an ancestor job managed by Kueue.
+// This interface is optional.
```

  Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:21:05Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:21:05Z
```diff
@@ -572,66 +569,77 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  Yeah, I was thinking we can consider it a bugfix and technical detail which does not need to be reflected in name. So, we still return "AncestorJobManagedByKueue", just top-level, rather than lowest-level. In other words - previously we didn't say "GetLowestLevelAncestorJobManagedByKueue". 
  
  In any case, it is a nit, feel free to keep if you still think we should add TopLevel to the name, I'm ok either way.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:23:05Z

- **pkg/controller/jobframework/reconciler.go:619** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:23:05Z
```diff
@@ -572,66 +576,75 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	log := ctrl.LoggerFrom(ctx)
 	seen := sets.New[types.UID]()
-	currentJob := jobObj
+	currentObj := jobObj
```

  Can we keep it as currentJob? Just to minimize the diff, and thus distractions.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:25:33Z

- **pkg/controller/jobframework/reconciler.go:619** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:25:33Z
```diff
@@ -572,66 +576,75 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	log := ctrl.LoggerFrom(ctx)
 	seen := sets.New[types.UID]()
-	currentJob := jobObj
+	currentObj := jobObj
```

  I think this is not fully correct, because the owner may not necessarily be a Job.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:31:52Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:31:52Z
```diff
@@ -572,66 +576,75 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  Please add a comment to the function as it is exported now. Maybe :
  get... traverses controllerRefs to find the top-level ancestor job that is manged by Kueue (ie, it has a queue-name label).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:33:21Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:33:21Z
```diff
@@ -572,66 +569,77 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestorJob client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestorJob) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
-	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
+	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestorJob.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestorJob.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestorJob.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
-	seen := sets.New[types.UID]()
-	currentJob := jobObj
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  Renamed and added comment for this function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:40:06Z

- **pkg/controller/jobframework/reconciler.go:619** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:40:06Z
```diff
@@ -572,66 +576,75 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	log := ctrl.LoggerFrom(ctx)
 	seen := sets.New[types.UID]()
-	currentJob := jobObj
+	currentObj := jobObj
```

  What could this be otherwise? IIUC this needs to be a managed and enabled integration? We generally call all integrations in Kueue Jobs. Yes, I know we now support serving workloads etc, but it sounds ok-ish given the historical naming we probably don't need to change it now.
  
  EDIT: and the input for the function may also not be considered best as "jobObj" since this can be a Pod.
  Similarly, we say topLevelJob, which could be any integration, not necessarily "Job".

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:42:45Z

- **pkg/controller/jobframework/reconciler.go:619** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:42:45Z
```diff
@@ -572,66 +576,75 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	log := ctrl.LoggerFrom(ctx)
 	seen := sets.New[types.UID]()
-	currentJob := jobObj
+	currentObj := jobObj
```

  > What could this be otherwise? 
  
  Now, we are looking for all owners and traversing even if the integration is not supported.
  
  For example:
  
  Job -> Custom CRD -> AppWrapper

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:42:58Z

- **pkg/controller/jobframework/reconciler.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:42:58Z
```diff
@@ -572,66 +576,75 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:43:11Z

- **pkg/controller/jobframework/reconciler.go:582** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:43:11Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
```

  Is this check actually needed?  I suppose GetAncestorJobManagedByKueue would never return a Job without queue-name, right? but maybe this is needed for the Pod integration (instance of the TopLevelJob). If this is the case, do we have it covered in tests? 
  
  EDIT: actually, no, for an instance of TopLevelJob interace we would not call getAncestorWorkload because we would not pass this check: https://github.com/kubernetes-sigs/kueue/pull/4824/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR325.
  
  So this seems dead, isn't it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:46:17Z

- **pkg/controller/jobframework/reconciler_test.go:49** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:46:16Z
```diff
@@ -31,47 +31,53 @@ import (
 	"k8s.io/utils/clock"
 	testingclock "k8s.io/utils/clock/testing"
 	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/jobset/api/jobset/v1alpha2"
 
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta1"
 	"sigs.k8s.io/kueue/pkg/util/kubeversion"
 	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
 	testingaw "sigs.k8s.io/kueue/pkg/util/testingjobs/appwrapper"
 	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	"sigs.k8s.io/kueue/pkg/util/testingjobs/jobset"
 	testingmpijob "sigs.k8s.io/kueue/pkg/util/testingjobs/mpijob"
 
 	_ "sigs.k8s.io/kueue/pkg/controller/jobs"
 
 	. "sigs.k8s.io/kueue/pkg/controller/jobframework"
 )
 
-func TestIsAncestorJobManaged(t *testing.T) {
+func TestGetTopLevelAncestorJobManagedByKueue(t *testing.T) {
```

  ```suggestion
  func TestGetAncestorJobManagedByKueue(t *testing.T) {
  ```
  nit, to align the naming

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:46:45Z

- **pkg/controller/jobframework/reconciler.go:582** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:46:45Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
```

  No, it can be possible that we are returning Job without queue-name if Jobs with queue-name not found. This is need for manageJobsWithoutQueueName=true logic.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-09T16:49:13Z

- **pkg/controller/jobframework/reconciler.go:582** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-09T16:49:13Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
```

  Also following original logic
  
  https://github.com/kubernetes-sigs/kueue/blob/db2620111045588de49a2f850d79060793f344b7/pkg/controller/jobframework/reconciler.go#L295-L299
  
  The `IsOwnerManagedByKueue(objectOwner)` returns true event if we don't have queue-name.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T16:52:24Z

- **pkg/controller/jobframework/reconciler.go:619** by [@mimowo](https://github.com/mimowo) — 2025-04-09T16:52:24Z
```diff
@@ -572,66 +576,75 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+func GetTopLevelAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
+	log := ctrl.LoggerFrom(ctx)
 	seen := sets.New[types.UID]()
-	currentJob := jobObj
+	currentObj := jobObj
```

  aha, got it, so we may return AppWrapper as top-level Job, but we don't consider Custom CRD as a Job. Makes sense.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-04-09T17:16:15Z

- **pkg/controller/jobframework/reconciler.go:582** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-09T17:16:15Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
```

  Right.  If `manageJobsWithoutQueueName=true` then we need to find the highest ancestor that can be managed by Kueue, independent of whether or not it actually has a queue-name label.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T17:21:40Z

- **pkg/controller/jobframework/reconciler.go:603** by [@mimowo](https://github.com/mimowo) — 2025-04-09T17:21:40Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  I'm thinking if the `GetAncestorJobManagedByKueue` function should depend on the manageJobsWithoutQueueName. 
  
  Let's consider a couple of examples, I mark the cases I consider "questionable" currently with stars:
  
  `manageJobsWithoutQueueName: false` I expect:
  ```
  * Job -> JobSet -> AppWrapper => nil; in the current PR we return AppWrapper, and we rely on checks later in the reconciler
  Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
  Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
  ```
  * does not matter yet, because we have external protection, but can complicate reuse for webhooks
  
  `manageJobsWithoutQueueName: true` I expect:
  ```
  Job -> JobSet -> AppWrapper => AppWrapper
  ** Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper; currently we return JobSet
  Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
  ```
  ** I think the current logic is surprising, because if we skip regardless of "queue-name", I would assume to return "top-level" known integration, which is AppWrapper.
  
  wdyt @mbobrovskyi @dgrove-oss ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T17:22:24Z

- **pkg/controller/jobframework/reconciler.go:582** by [@mimowo](https://github.com/mimowo) — 2025-04-09T17:22:24Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
```

  Yeah, this is my point also here: https://github.com/kubernetes-sigs/kueue/pull/4824#discussion_r2035811140

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-04-09T17:32:39Z

- **pkg/controller/jobframework/reconciler.go:603** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-09T17:32:39Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  There's another case which is important:
  ```
  Job (queue-name) -> JobSet-> AppWrapper (queue-name) => AppWrapper
  ```
  I believe we ignore (and should ignore) the `queue-name` label on `Job` in this chain because the AppWrapper is where Kueue made the admission decision and is the owner of the Workload object.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-09T17:34:22Z

- **pkg/controller/jobframework/reconciler.go:603** by [@mimowo](https://github.com/mimowo) — 2025-04-09T17:34:22Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  Yes, but I believe this case is independent of `manageJobsWithoutQueueName`, and already covered well => AppWrapper returned (IIUC)

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-04-09T17:41:27Z

- **pkg/controller/jobframework/reconciler.go:603** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-09T17:41:27Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  I think you are right that passing in `manageJobsWithoutQueueName` lets us define the utility function that is the right primitive operation.  Namely "get the top-level job that Kueue _should_ be managing starting from a given object".  Given that primitive, the logic in its various callers should become simpler.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T05:43:44Z

- **pkg/controller/jobframework/reconciler.go:603** by [@mimowo](https://github.com/mimowo) — 2025-04-10T05:43:44Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  Yeah, but not exactly. One complication is that when `manageJobsWithoutQueueName: true`, then we also need the selector to know if managed or not. 
  
  So, maybe better if `GetAncestorJobManagedByKueue` accepts a more generic `func isManaged(object) bool`. IIUC The semantics of the function would be:
  ```golang
  func isManaged(object) bool {
    if QueueName(object) != nil {
      return true
    }
    if manageJobsWithoutQueueName == false { 
      return false
    }
    return manageJobsSelectorMatches() // here for Pods we also need to account for both selectors (Job level and Pod level), which we already do in webhooks but not in Reconcilers
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T06:25:25Z

- **pkg/controller/jobframework/reconciler.go:603** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T06:25:25Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  The `manageJobsSelectorMatches()` function only checks the namespace, so we can move it after `GetAncestorJobManagedByKueue()` to avoid retrieving the namespace on each iteration.
  
  https://github.com/kubernetes-sigs/kueue/blob/db2620111045588de49a2f850d79060793f344b7/pkg/controller/jobframework/reconciler.go#L341-L354

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T06:33:36Z

- **pkg/controller/jobframework/reconciler.go:603** by [@mimowo](https://github.com/mimowo) — 2025-04-10T06:33:36Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  I just realized we probably don't need to consider the selector for Pods because they are skipped in reconcilers, so maybe the isManaged function would slightly differ when calling from webhooks and reconcilers. We can figure this out in a follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T06:34:58Z

- **pkg/controller/jobframework/reconciler.go:603** by [@mimowo](https://github.com/mimowo) — 2025-04-10T06:34:58Z
```diff
@@ -572,66 +576,78 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
+	if QueueNameForObject(ancestor) == "" {
+		return nil, nil
 	}
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find a top-level ancestor Job
+// that is managed by Kueue (i.e., it has a queue-name label). If a Job with a queue-name
+// is not found, it will return the top-level ancestor with an enabled integration.
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object) (client.Object, error) {
```

  Regarding the namespace, yeah it would be better to retrieve it only once at the beginning of the GetAncestorJob function for better performance.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-10T09:00:36Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@gabesaba](https://github.com/gabesaba) — 2025-04-10T09:00:36Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  Without much further context:
  
  1) How understandable is this interface to users? Do any users currently want this?
  2) If this interface turns out to be the wrong fit, how impactful is it to change it/delete it?
  
  I'd suggest: doing something pragmatic for the cherry-pick (as michał suggests above), even if it goes against our principles a bit, and move the interface definition to a new PR so we can discuss it further

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T09:40:10Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T09:40:10Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  > How understandable is this interface to users? 
  
  I think we added good enough and understandable comment for this interface:
  
  https://github.com/kubernetes-sigs/kueue/blob/6b6fc101ecce71399dbf0d2d77651cb09e6a11f8/pkg/controller/jobframework/interface.go#L167-L173
  
  If it's still unclear, we can expand the explanation further.
  
  > Do any users currently want this?
  
  This interface can be useful for users who want to implement behavior similar to what we currently have for Pods. It allows skipping the ownership traversal and treating the current job as the top-level one. This means we won’t search for an ancestor job or base suspension behavior on whether one is found.
  
  > If this interface turns out to be the wrong fit, how impactful is it to change it/delete it?
  
  I see only one possible case where this interface might be removed: if we remove the traversal logic it supports. In that case, the interface becomes unnecessary, and users can safely remove it as well.
  
  > I'd suggest: doing something pragmatic for the cherry-pick (as michał suggests above), even if it goes against our principles a bit, and move the interface definition to a new PR so we can discuss it further
  
  I agree with implementing the logic described by Michał for the cherry-pick, but I think we can create a manual cherry-pick and make the changes within this PR.
  
  
  My primary concern is that we are attempting to manipulate implementation details within an abstraction layer, which is far from ideal. The JobFramework serves as an abstraction, and it should remain agnostic to specific implementation details - such as the current Pod integration. This mixing of concerns risks undermining the integrity of the abstraction, making the system less modular, harder to maintain, and more prone to errors. Ideally, the JobFramework should define a clear interface and leave the concrete implementation to separate, interchangeable components, ensuring flexibility and adherence to proper design principles.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T10:20:56Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-10T10:20:55Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  My point of view:
  
  > How understandable is this interface to users?
  
  It took me something like 1h to understand what we are doing and how. Then it took me another 1h to come up with naming etc, probably another 30min to discuss the documentation. Despite the effort we did, I'm still not convinced the API would be understood by users. I think this generates a lot of overhead. 
  
  >  Do any users currently want this?
  
  Not to my knowledge - we don't have any GH issue open, and I didn't hear about the need by any other channels.
  
  > If this interface turns out to be the wrong fit, how impactful is it to change it/delete it?
  
  Removing any user-facing API is hard, and this is my major concern. I'm not confident about the abstraction we are creating. There is no way to know if we are breaking a user or not, we will probably need to go through deprecation process. Maybe we could mitigate it by adding a comment like "// This interface is subject to change or remove in future releases without any deprecation notice".
  
  --- 
  
  > My primary concern is that we are attempting to manipulate implementation details within an abstraction layer, which is far from ideal.
  
  I agree, but we need to choose a "less evil" solution, and both solutions are evil in some ways. I argue making an internal hack is better than paying the review and maintenance cost of a new interface. Especially, if we introduce a couple interfaces for the "Pod" integration.
  
  OTOH - I agree this is the path we were taking so far, so I would be ok to accept as is, but I don't think this is a healthy approach in the long-run.
  
  I would like yet to hear an opinion from @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T10:24:38Z

- **pkg/controller/jobframework/interface.go:172** by [@mimowo](https://github.com/mimowo) — 2025-04-10T10:24:37Z
```diff
@@ -164,6 +164,14 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+// TopLevelJob interface is an optional interface used to indicate
+// that the Job owns/manages the Workload object, regardless of the Job
+// owner references.
+type TopLevelJob interface {
+	// IsTopLevelJob returns true if the Job owns/manages the Workload.
+	IsTopLevelJob() bool
```

  ```suggestion
  	IsTopLevel() bool
  ```
  nit, "Job" looks redundant here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T10:28:31Z

- **pkg/controller/jobframework/interface.go:172** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T10:28:31Z
```diff
@@ -164,6 +164,14 @@ type JobWithManagedBy interface {
 	SetManagedBy(*string)
 }
 
+// TopLevelJob interface is an optional interface used to indicate
+// that the Job owns/manages the Workload object, regardless of the Job
+// owner references.
+type TopLevelJob interface {
+	// IsTopLevelJob returns true if the Job owns/manages the Workload.
+	IsTopLevelJob() bool
```

  Renamed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T10:45:27Z

- **pkg/controller/jobframework/reconciler.go:616** by [@mimowo](https://github.com/mimowo) — 2025-04-10T10:45:27Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
+// Job -> JobSet -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper => AppWrapper
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object, manageJobsWithoutQueueName bool) (client.Object, error) {
```

  ```suggestion
  func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object, manageJobsWithoutQueueName bool) (client.Object, error) {
  ```
  nit, to emphasize this is actually complex :) Get is usually used as a prefix for very simple retrival.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T10:47:13Z

- **pkg/controller/jobframework/reconciler.go:603** by [@mimowo](https://github.com/mimowo) — 2025-04-10T10:47:13Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
```

  Shouldn't this be "false"?`Job -> JobSet -> AppWrapper => nil` suggests this is "false"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T10:47:32Z

- **pkg/controller/jobframework/reconciler.go:610** by [@mimowo](https://github.com/mimowo) — 2025-04-10T10:47:32Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
```

  ```suggestion
  // With manageJobsWithoutQueueName=true:
  ```
  IIUC the examples below

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T10:49:45Z

- **pkg/controller/jobframework/reconciler.go:612** by [@mimowo](https://github.com/mimowo) — 2025-04-10T10:49:45Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
+// Job -> JobSet -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper
```

  Do we have an e2e test for that? If not, please add.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T10:51:00Z

- **pkg/controller/jobframework/reconciler.go:612** by [@mimowo](https://github.com/mimowo) — 2025-04-10T10:51:00Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
+// Job -> JobSet -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper
```

  Please check what happens on the last release when `manageJobsWithoutQueueName=true`, and
  `Job (queue-name) -> JobSet`. I'm wondering we might be changing the behavior here with the recent change. Please also add e2e test for this scenario.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T11:19:12Z

- **pkg/controller/jobframework/reconciler.go:616** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T11:19:12Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
+// Job -> JobSet -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper => AppWrapper
+func GetAncestorJobManagedByKueue(ctx context.Context, c client.Client, record record.EventRecorder, jobObj client.Object, manageJobsWithoutQueueName bool) (client.Object, error) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T11:19:31Z

- **pkg/controller/jobframework/reconciler.go:610** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T11:19:31Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
```

  Ah, my bad. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T12:52:11Z

- **pkg/controller/jobframework/reconciler.go:612** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T12:52:11Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
+// Job -> JobSet -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T13:12:40Z

- **pkg/controller/jobframework/reconciler.go:612** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T13:12:39Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
+// Job -> JobSet -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper
```

  Yeah, `Job (queue-name) -> JobSet (queue-name) -> AppWrapper` test failing on main branch. Good catch!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T14:27:47Z

- **pkg/controller/jobframework/reconciler.go:612** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T14:27:46Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
 	wlList := kueue.WorkloadList{}
 	if err := r.client.List(ctx, &wlList, client.InNamespace(ancestor.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(ancestor.GetUID())}); client.IgnoreNotFound(err) != nil {
 		return nil, err
 	}
 	if len(wlList.Items) > 0 {
 		// In theory the job can own multiple Workloads, we cannot do too much about it, maybe log it.
+		ctrl.LoggerFrom(ctx).V(2).Info(
+			"WARNING: The job has multiple associated Workloads.",
+			"job", ancestor.GetName(),
+			"workloads", klog.KObjSlice(wlList.Items),
+		)
 		return &wlList.Items[0], nil
 	}
 	return nil, nil
 }
 
-// getAncestorJobManagedByKueue traverses controllerRefs to find an ancestor job that is manged by Kueue (ie, it has a queue-name label).
-func (r *JobReconciler) getAncestorJobManagedByKueue(ctx context.Context, jobObj client.Object) (client.Object, error) {
+// GetAncestorJobManagedByKueue traverses controllerRefs to find the top-level ancestor Job managed by Kueue.
+// If manageJobsWithoutQueueName is set to false, it returns only Jobs with a queue-name.
+// If manageJobsWithoutQueueName is true, it may return a Job even if it doesn't have a queue-name.
+//
+// Examples:
+//
+// With manageJobsWithoutQueueName=true:
+// Job -> JobSet -> AppWrapper => nil
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => JobSet
+// Job (queue-name) -> JobSet -> AppWrapper (queue-name) => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper (queue-name) => AppWrapper
+// Job -> JobSet (disabled) -> AppWrapper (queue-name) => AppWrapper
+//
+// With manageJobsWithoutQueueName=false:
+// Job -> JobSet -> AppWrapper => AppWrapper
+// Job (queue-name) -> JobSet (queue-name) -> AppWrapper => AppWrapper
```

  > Please check what happens on the last release when manageJobsWithoutQueueName=true, and
  Job (queue-name) -> JobSet. I'm wondering we might be changing the behavior here with the recent change. Please also add e2e test for this scenario.
  
  The `Job (queue-name) -> JobSet` working due to we are suspending JobSet on the webhook side.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (approved) — 2025-04-10T14:35:21Z

lgtm;  just one optional suggestion for a function name change

- **pkg/controller/jobframework/reconciler.go:580** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-10T14:20:52Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
```

  nit:  now that we are finding the ancestor externally and passing it in, this function simply tries to get the Workload of the provided object.  Maybe rename to `getWorkloadForObject` or similar?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-10T14:43:22Z

- **pkg/controller/jobframework/reconciler.go:580** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-10T14:43:21Z
```diff
@@ -572,66 +576,91 @@ func (r *JobReconciler) recordAdmissionCheckUpdate(wl *kueue.Workload, job Gener
 	}
 }
 
-// IsAncestorJobManaged checks whether an ancestor job is managed by kueue.
-func (r *JobReconciler) IsAncestorJobManaged(ctx context.Context, jobObj client.Object) (bool, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil {
-		return false, err
-	}
-	return ancestor != nil, nil
-}
-
 // getAncestorWorkload returns the Workload object of the Kueue-managed ancestor job.
-func (r *JobReconciler) getAncestorWorkload(ctx context.Context, jobObj client.Object) (*kueue.Workload, error) {
-	ancestor, err := r.getAncestorJobManagedByKueue(ctx, jobObj)
-	if err != nil || ancestor == nil {
-		return nil, err
-	}
+func (r *JobReconciler) getAncestorWorkload(ctx context.Context, ancestor client.Object) (*kueue.Workload, error) {
```

  Done

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (approved) — 2025-04-10T14:47:54Z

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-10T15:02:53Z

- **pkg/controller/jobs/pod/pod_controller.go:325** by [@mimowo](https://github.com/mimowo) — 2025-04-10T15:02:53Z
```diff
@@ -320,6 +321,10 @@ func (p *Pod) Run(ctx context.Context, c client.Client, podSetsInfo []podset.Pod
 	})
 }
 
+func (p *Pod) OwnsWorkload() bool {
+	return p.isServing()
```

  As summarized in https://github.com/kubernetes-sigs/kueue/pull/4824#issuecomment-2794110313, I think the concerns are valid, but not blocking for the PR which is fully consitent with our current appoach. Opened dedicated issue to discuss / reconsider our approach: https://github.com/kubernetes-sigs/kueue/issues/4921
