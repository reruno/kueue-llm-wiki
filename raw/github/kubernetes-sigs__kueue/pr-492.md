# PR #492: Clean up webhook integration test

**Summary**: Clean up webhook integration test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/492

**Last updated**: 2022-12-23T06:34:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@nayihz](https://github.com/nayihz)
- **Branch**: `clean_up_integration_test` → `main`
- **Created**: 2022-12-22T08:13:28Z
- **Updated**: 2022-12-23T06:34:35Z
- **Merged**: 2022-12-23T06:29:26Z
- **Closed**: 2022-12-23T06:29:26Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 4   **+77 / -52**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Enable `core.SetupControllers` to replace deleting finalizers manually.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes # https://github.com/kubernetes-sigs/kueue/pull/466#discussion_r1049860993

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-22T08:13:37Z

Hi @cmssczy. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-12-22T08:24:58Z

/ok-to-test

### Comment by [@nayihz](https://github.com/nayihz) — 2022-12-22T12:12:31Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T16:55:09Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-12-22T16:55:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/492#issuecomment-1363097490" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/492#" title="Author self-approved">cmssczy</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T21:00:59Z

@kerthcet could you take on this review?

### Comment by [@nayihz](https://github.com/nayihz) — 2022-12-23T02:09:07Z

```
/home/prow/go/src/sigs.k8s.io/kueue/bin/golangci-lint run --timeout 7m0s
test/integration/webhook/v1alpha2/workload_test.go:26: File is not `goimports`-ed with -local sigs.k8s.io/kueue (goimports)
	"sigs.k8s.io/controller-runtime/pkg/client"
```
Is this issue relevant to `gofmt`? There is no change after run `make fmt` and `gofmt -s -w xxxx.go`.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-12-23T02:10:09Z

/assign

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-12-23T06:28:19Z

Thanks @cmssczy for your efforts.
/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-12-22T16:55:00Z

- **test/integration/webhook/v1alpha2/workload_test.go:26** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T16:47:54Z
```diff
@@ -23,6 +23,7 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
 
+	"sigs.k8s.io/controller-runtime/pkg/client"
```

  move one line below (alphabetical order)

- **pkg/util/testing/not_found_error_cmp.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T16:49:42Z
```diff
@@ -47,3 +47,27 @@ func (matcher *notFoundErrorMatch) FailureMessage(actual interface{}) (message s
 func (matcher *notFoundErrorMatch) NegatedFailureMessage(actual interface{}) (message string) {
 	return format.Message(actual, "not to be NotFound error")
 }
+
+func IsForbiddenError() types.GomegaMatcher {
```

  ```suggestion
  func BeForbiddenError() types.GomegaMatcher {
  ```
  
  to match ginkgo's language

- **pkg/util/testing/not_found_error_cmp.go:61** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T16:51:29Z
```diff
@@ -47,3 +47,27 @@ func (matcher *notFoundErrorMatch) FailureMessage(actual interface{}) (message s
 func (matcher *notFoundErrorMatch) NegatedFailureMessage(actual interface{}) (message string) {
 	return format.Message(actual, "not to be NotFound error")
 }
+
+func IsForbiddenError() types.GomegaMatcher {
+	return &isForbiddenErrorMatch{}
+}
+
+type isForbiddenErrorMatch struct {
+}
+
+func (matcher *isForbiddenErrorMatch) Match(actual interface{}) (success bool, err error) {
+	err, ok := actual.(error)
+	if !ok {
+		return false, fmt.Errorf("NotFoundError expects an error")
```

  ```suggestion
  		return false, fmt.Errorf("ForbiddenError expects an error")
  ```

- **pkg/util/testing/not_found_error_cmp.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T16:51:50Z
```diff
@@ -47,3 +47,27 @@ func (matcher *notFoundErrorMatch) FailureMessage(actual interface{}) (message s
 func (matcher *notFoundErrorMatch) NegatedFailureMessage(actual interface{}) (message string) {
 	return format.Message(actual, "not to be NotFound error")
 }
+
+func IsForbiddenError() types.GomegaMatcher {
+	return &isForbiddenErrorMatch{}
+}
+
+type isForbiddenErrorMatch struct {
+}
+
+func (matcher *isForbiddenErrorMatch) Match(actual interface{}) (success bool, err error) {
+	err, ok := actual.(error)
+	if !ok {
+		return false, fmt.Errorf("NotFoundError expects an error")
+	}
+
+	return err != nil && apierrors.IsForbidden(err), nil
+}
+
+func (matcher *isForbiddenErrorMatch) FailureMessage(actual interface{}) (message string) {
+	return format.Message(actual, "to be a IsForbidden error")
```

  ```suggestion
  	return format.Message(actual, "to be a Forbidden error")
  ```

- **pkg/util/testing/not_found_error_cmp.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2022-12-22T16:51:59Z
```diff
@@ -47,3 +47,27 @@ func (matcher *notFoundErrorMatch) FailureMessage(actual interface{}) (message s
 func (matcher *notFoundErrorMatch) NegatedFailureMessage(actual interface{}) (message string) {
 	return format.Message(actual, "not to be NotFound error")
 }
+
+func IsForbiddenError() types.GomegaMatcher {
+	return &isForbiddenErrorMatch{}
+}
+
+type isForbiddenErrorMatch struct {
+}
+
+func (matcher *isForbiddenErrorMatch) Match(actual interface{}) (success bool, err error) {
+	err, ok := actual.(error)
+	if !ok {
+		return false, fmt.Errorf("NotFoundError expects an error")
+	}
+
+	return err != nil && apierrors.IsForbidden(err), nil
+}
+
+func (matcher *isForbiddenErrorMatch) FailureMessage(actual interface{}) (message string) {
+	return format.Message(actual, "to be a IsForbidden error")
+}
+
+func (matcher *isForbiddenErrorMatch) NegatedFailureMessage(actual interface{}) (message string) {
+	return format.Message(actual, "not to be IsForbidden error")
```

  ```suggestion
  	return format.Message(actual, "not to be a Forbidden error")
  ```

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-23T01:42:26Z

- **test/integration/webhook/v1alpha2/workload_test.go:26** by [@nayihz](https://github.com/nayihz) — 2022-12-23T01:42:26Z
```diff
@@ -23,6 +23,7 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
 
+	"sigs.k8s.io/controller-runtime/pkg/client"
```

  this line is actually before `kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"` after `make fmt`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2022-12-23T02:15:41Z

- **test/integration/webhook/v1alpha2/workload_test.go:30** by [@tenzen-y](https://github.com/tenzen-y) — 2022-12-23T02:15:41Z
```diff
@@ -23,6 +23,7 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
 
+	"sigs.k8s.io/controller-runtime/pkg/client"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 	"sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/test/util"
```

  ```suggestion
  	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
  	"k8s.io/apimachinery/pkg/types"
  	"sigs.k8s.io/controller-runtime/pkg/client"
  
  	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
  	"sigs.k8s.io/kueue/pkg/util/testing"
  	"sigs.k8s.io/kueue/test/util"
  ```
  
  @cmssczy Maybe, you need to remove a new line of L25 and add a new line to L26.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-23T02:29:11Z

- **test/integration/webhook/v1alpha2/workload_test.go:30** by [@nayihz](https://github.com/nayihz) — 2022-12-23T02:29:11Z
```diff
@@ -23,6 +23,7 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
 
+	"sigs.k8s.io/controller-runtime/pkg/client"
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 	"sigs.k8s.io/kueue/pkg/util/testing"
 	"sigs.k8s.io/kueue/test/util"
```

  Indeed. Thanks.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-12-23T03:32:27Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:81** by [@kerthcet](https://github.com/kerthcet) — 2022-12-23T03:31:40Z
```diff
@@ -80,22 +75,17 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(k8sClient.Create(ctx, cq)).Should(gomega.Succeed())
 
 			defer func() {
-				var deleteCQ kueue.ClusterQueue
-				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &deleteCQ)).Should(gomega.Succeed())
-				controllerutil.RemoveFinalizer(&deleteCQ, kueue.ResourceInUseFinalizerName)
-				gomega.Expect(k8sClient.Update(ctx, &deleteCQ)).Should(gomega.Succeed())
-				util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, &deleteCQ, true)
+				util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
 			}()
 
-			var updateCQ kueue.ClusterQueue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updateCQ)).Should(gomega.Succeed())
-			updateCQ.Spec.Resources = []kueue.Resource{
-				*testing.MakeResource("@cpu").Flavor(testing.MakeFlavor("x86", "5").Obj()).Obj(),
-			}
-
-			err := k8sClient.Update(ctx, &updateCQ)
-			gomega.Expect(err).Should(gomega.HaveOccurred())
-			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
+			gomega.Eventually(func() error {
```

  No need to use `Eventually` everywhere, no? Same to other places.

### Review by [@nayihz](https://github.com/nayihz) (commented) — 2022-12-23T03:44:50Z

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:81** by [@nayihz](https://github.com/nayihz) — 2022-12-23T03:44:50Z
```diff
@@ -80,22 +75,17 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			gomega.Expect(k8sClient.Create(ctx, cq)).Should(gomega.Succeed())
 
 			defer func() {
-				var deleteCQ kueue.ClusterQueue
-				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &deleteCQ)).Should(gomega.Succeed())
-				controllerutil.RemoveFinalizer(&deleteCQ, kueue.ResourceInUseFinalizerName)
-				gomega.Expect(k8sClient.Update(ctx, &deleteCQ)).Should(gomega.Succeed())
-				util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, &deleteCQ, true)
+				util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
 			}()
 
-			var updateCQ kueue.ClusterQueue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updateCQ)).Should(gomega.Succeed())
-			updateCQ.Spec.Resources = []kueue.Resource{
-				*testing.MakeResource("@cpu").Flavor(testing.MakeFlavor("x86", "5").Obj()).Obj(),
-			}
-
-			err := k8sClient.Update(ctx, &updateCQ)
-			gomega.Expect(err).Should(gomega.HaveOccurred())
-			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
+			gomega.Eventually(func() error {
```

  We will occasionally get an error here If don't use `Eventually`. I think it's safe to do so.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-12-23T05:09:40Z

Totally LGTM

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:105** by [@kerthcet](https://github.com/kerthcet) — 2022-12-23T05:06:23Z
```diff
@@ -105,20 +95,16 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 			).Obj()
 			gomega.Expect(k8sClient.Create(ctx, cq)).Should(gomega.Succeed())
 			defer func() {
-				var deleteCQ kueue.ClusterQueue
-				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &deleteCQ)).Should(gomega.Succeed())
-				controllerutil.RemoveFinalizer(&deleteCQ, kueue.ResourceInUseFinalizerName)
-				gomega.Expect(k8sClient.Update(ctx, &deleteCQ)).Should(gomega.Succeed())
-				util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, &deleteCQ, true)
+				util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
 			}()
 
-			var updateCQ kueue.ClusterQueue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updateCQ)).Should(gomega.Succeed())
-			updateCQ.Spec.Resources[0].Flavors[0].Name = "invalid_name"
+			gomega.Eventually(func() error {
+				var updateCQ kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updateCQ)).Should(gomega.Succeed())
+				updateCQ.Spec.Resources[0].Flavors[0].Name = "invalid_name"
 
```

  Remove the blank line.

- **test/integration/webhook/v1alpha2/workload_test.go:128** by [@kerthcet](https://github.com/kerthcet) — 2022-12-23T05:07:21Z
```diff
@@ -125,7 +126,12 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 			ginkgo.By("Updating the priority")
 			priority := int32(10)
 			workload.Spec.Priority = &priority
```

  Remove this line.

- **test/integration/webhook/v1alpha2/workload_test.go:132** by [@kerthcet](https://github.com/kerthcet) — 2022-12-23T05:08:49Z
```diff
@@ -125,7 +126,12 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 			ginkgo.By("Updating the priority")
 			priority := int32(10)
 			workload.Spec.Priority = &priority
-			gomega.Expect(k8sClient.Update(ctx, workload)).Should(gomega.Succeed())
+			gomega.Eventually(func() error {
+				var newWL kueue.Workload
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(workload), &newWL)).To(gomega.Succeed())
+				newWL.Spec.Priority = &priority
```

  ```suggestion
  				newWL.Spec.Priority = pointer.Int32(10)
  ```
  
  Also remove the priority.
