# PR #4950: Set the E2E_KIND_VERSION mk variable based on the E2E_K8S_VERSION

**Summary**: Set the E2E_KIND_VERSION mk variable based on the E2E_K8S_VERSION

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4950

**Last updated**: 2025-06-10T21:07:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `set-e2e-k8s-img-version` → `main`
- **Created**: 2025-04-14T08:42:14Z
- **Updated**: 2025-06-10T21:07:24Z
- **Merged**: 2025-04-15T09:21:07Z
- **Closed**: 2025-04-15T09:21:07Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+8 / -5**

## Description

#### What type of PR is this?
/kind cleanup


#### What this PR does / why we need it:
This PR sets the `E2E_KIND_VERSION` Makefile variable based on the `E2E_K8S_VERSION` variable. This is needed to make `test-infra` jobs pass only the `E2E_K8S_VERSION` variable


#### Which issue(s) this PR fixes:
Part of #4729

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-04-14T08:42:32Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 27846e4dd426dd978989a23a4df0cee206ae6137 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67fe1e92d54dc600082f6eea |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-14T09:58:03Z

/approve
/cherry-pick release-0.11 release-0.10
I prefer to leave lgtm from @mbobrovskyi or @gabesaba

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-04-14T09:58:07Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.11` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4950#issuecomment-2801152928):

>/approve
>/cherry-pick release-0.11 release-0.10
>I prefer to leave lgtm from @mbobrovskyi or @gabesaba 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-15T08:52:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4950#" title="Author self-approved">kaisoz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4950#pullrequestreview-2767427185" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-15T09:01:03Z

/lgtm
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-15T09:01:11Z

LGTM label has been added.  <details>Git tree hash: cb273d0859fd2f37939ba4675a6d784976d32dc5</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-04-15T09:21:45Z

@mimowo: #4950 failed to apply on top of branch "release-0.11":
```
Applying: Set the E2E_KIND_VERSION mk variable based on the E2E_K8S_VERSION
Using index info to reconstruct a base tree...
M	Makefile-test.mk
M	site/content/en/docs/contribution_guidelines/testing.md
Falling back to patching base and 3-way merge...
Auto-merging site/content/en/docs/contribution_guidelines/testing.md
Auto-merging Makefile-test.mk
CONFLICT (content): Merge conflict in Makefile-test.mk
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Set the E2E_KIND_VERSION mk variable based on the E2E_K8S_VERSION

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4950#issuecomment-2801152928):

>/approve
>/cherry-pick release-0.11 release-0.10
>I prefer to leave lgtm from @mbobrovskyi or @gabesaba 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T08:57:13Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T08:57:13Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  This looks great to me, but I'm wondering what we should do with `E2E_K8S_VERSIONS`. Is it still necessary? 
  
  It seems not even aligned with the block above. Maybe @mbobrovskyi knows what is `E2E_K8S_VERSIONS` used for as recently updating. I think the intention is to have one test per minor version, so properly no need to add patch to the versions.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-04-14T09:11:57Z

- **Makefile-test.mk:68** by [@kaisoz](https://github.com/kaisoz) — 2025-04-14T09:11:57Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  Agree, I wanted to start this discussion once I got an ok from you guys on these changes.
  
  I realized that, right now, `E2E_K8S_VERSIONS` can get unaligned with what `test-infra` tests very easily. 
  We could limit this variable to contain just the `<major>.<version>` of K8S and **move** the k8s version patch calculation (the code this PR introduces) to a Makefile function. This function could be called both when defining the `E2E_KIND_VERSION` and when generating the `E2E_TARGETS` variable (for example [here](https://github.com/kubernetes-sigs/kueue/blob/6345f5d42dde10a86b364f23ed94c793552bb518/Makefile-test.mk#L127)). In this way we don't need to change much code. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T09:31:06Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T09:31:06Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  > can get unaligned with what test-infra tests very easily.
  
  It is not aligned in the PR even :) , what are the consequences of merging unaligned?
  
  > We could limit this variable to contain just the <major>.<version> of K8S and move the k8s version patch calculation (the code this PR introduces) to a Makefile function. 
  
  sounds reasonable, but I would like to see the prototype to assess.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-14T09:36:55Z

- **Makefile-test.mk:70** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-14T09:36:55Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
 # E2E_K8S_VERSIONS sets the list of k8s versions included in test-e2e-all
 E2E_K8S_VERSIONS ?= 1.30.9 1.31.5 1.32.1
```

  We're using it in the test-e2e-all target, but I don’t think we actually use the `test-e2e-all` target anywhere.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-04-14T09:54:43Z

- **Makefile-test.mk:68** by [@kaisoz](https://github.com/kaisoz) — 2025-04-14T09:54:42Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  > It is not aligned in the PR even :) , what are the consequences of merging unaligned?
  
  yes :D I didn't touch it because it was already unaligned with what `test-infra` sets (which is what this PR sets)
  
  > sounds reasonable, but I would like to see the prototype to assess.
  
  I could have another PR with this if it's finally needed, although @mbobrovskyi suggests is not? https://github.com/kubernetes-sigs/kueue/pull/4950#discussion_r2041785759

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T09:55:39Z

- **Makefile-test.mk:70** by [@mimowo](https://github.com/mimowo) — 2025-04-14T09:55:39Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
 # E2E_K8S_VERSIONS sets the list of k8s versions included in test-e2e-all
 E2E_K8S_VERSIONS ?= 1.30.9 1.31.5 1.32.1
```

  If we don't use it I would just drop it. We can drop in a follow up either.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T09:56:55Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T09:56:55Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  sgtm, let's drop it then in a follow up

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-14T09:58:07Z

- **Makefile-test.mk:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-14T09:58:07Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  ```suggestion
  E2E_K8S_VERSIONS ?= 1.30.9 1.31.5 1.32.1
  E2E_K8S_VERSION ?= 1.32
  E2E_K8S_PATCH_VERSION ?= $(filter $(E2E_K8S_VERSION).%,$(E2E_K8S_VERSIONS))
  # Default to E2E_K8S_VERSION.0 if no match is found
  E2E_K8S_PATCH_VERSION := $(or $(E2E_K8S_PATCH_VERSION),$(E2E_K8S_VERSION).0)
  E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_PATCH_VERSION)
  ```
  I'm wondering if we can go on this way?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T10:00:29Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T10:00:29Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  Why do we need E2E_K8S_VERSIONS? I thought this is not used based on https://github.com/kubernetes-sigs/kueue/pull/4950#discussion_r2041785759. 
  
  Maybe we drop it in a preparatory PR, so that it does not complicate the view here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-14T10:02:21Z

- **Makefile-test.mk:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-14T10:02:21Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  I think this approach is easier to maintain than using multiple if-else cases.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T10:07:34Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T10:07:34Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  I see, make sense. But the versions in E2E_K8S_VERSIONS should correspond to the versions used in test-infra so far, so probably 1.30.10, etc.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T10:08:09Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T10:08:09Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  > I think this approach is easier to maintain than using multiple if-else cases.
  
  Regardless of that I would just drop the unsused make targets in a follow up to avoid future confusion.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-14T10:14:32Z

- **Makefile-test.mk:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-14T10:14:32Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  > I see, make sense. But the versions in E2E_K8S_VERSIONS should correspond to the versions used in test-infra so far, so probably 1.30.10, etc.
  
  To be more clear
  
  E2E_K8S_VERSIONS ?= 1.30.10 1.31.6 1.32.3

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T10:29:22Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T10:29:22Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  sgtm

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-04-14T10:33:57Z

- **Makefile-test.mk:68** by [@kaisoz](https://github.com/kaisoz) — 2025-04-14T10:33:57Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  @mbobrovskyi 
  
  I like it! But I'd would still use `E2E_K8S_PATCH_VERSION` to build the `E2E_KIND_VERSION`, because the user might want to choose the k8s patch to test things out (think about running the e2e tests in a newly released k8s patch version)
  
  ```
  E2E_K8S_VERSIONS ?= 1.30.10 1.31.6 1.32.3
  E2E_K8S_VERSION ?= 1.32
  E2E_K8S_PATCH_VERSION ?= $(filter $(E2E_K8S_VERSION).%,$(E2E_K8S_VERSIONS))
  # Default to patch v0 if no match is found
  E2E_K8S_PATCH_VERSION := $(or $(E2E_K8S_PATCH_VERSION:$(E2E_K8S_VERSION).%=%), 0)
  E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_PATCH_VERSION)
  ```
  
  what do you think of this? I've tested it out and it correctly handles all cases

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-14T10:55:31Z

- **Makefile-test.mk:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-14T10:55:31Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  User can use `E2E_K8S_PATCH_VERSION=1.32.3 make test-e2e` and it should work, no? I think it is easier than `E2E_K8S_VERSION=1.32 E2E_K8S_PATCH_VERSION=3 make test-e2e`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T10:56:45Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T10:56:44Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  yeah

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-14T10:58:28Z

- **Makefile-test.mk:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-14T10:58:28Z
  Also it would be great to update docs https://github.com/kubernetes-sigs/kueue/blob/3a36022b1ba9c2ae8355a39e005c5a8afae74a96/site/content/en/docs/contribution_guidelines/testing.md?plain=1#L69.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T11:05:53Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T11:05:53Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  yeah

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2025-04-14T12:24:40Z

- **Makefile-test.mk:68** by [@kaisoz](https://github.com/kaisoz) — 2025-04-14T12:24:40Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  I didn't consider that option because when I read `E2E_K8S_PATCH_VERSION`, I understand that the variable holds the Kubernetes **patch version** and not the **full version** (combination of major.minor.patch). So as a user, `E2E_K8S_PATCH_VERSION=1.32.3 make test-e2e` is confusing for me...
  
  What do you think about renaming the variable to `E2E_K8S_FULL_VERSION`? Then the snippet would be something like
  
  ```
  E2E_K8S_VERSIONS ?= 1.30.10 1.31.6 1.32.3
  E2E_K8S_VERSION ?= 1.32
  E2E_K8S_FULL_VERSION ?= $(filter $(E2E_K8S_VERSION).%,$(E2E_K8S_VERSIONS))
  # Default to E2E_K8S_VERSION.0 if no match is found
  E2E_K8S_FULL_VERSION := $(or $(E2E_K8S_FULL_VERSION),$(E2E_K8S_VERSION).0)
  E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_FULL_VERSION)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-14T13:05:24Z

- **Makefile-test.mk:68** by [@mimowo](https://github.com/mimowo) — 2025-04-14T13:05:24Z
```diff
@@ -55,7 +55,17 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
+E2E_K8S_VERSION ?= 1.32
+ifeq ($(E2E_K8S_VERSION),1.30)
+	E2E_K8S_VERSION_PATCH ?= 10
+else ifeq ($(E2E_K8S_VERSION),1.31)
+	E2E_K8S_VERSION_PATCH ?= 6
+else ifeq ($(E2E_K8S_VERSION),1.32)
+	E2E_K8S_VERSION_PATCH ?= 3
+else
+	E2E_K8S_VERSION_PATCH ?= 0
+endif
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_VERSION).$(E2E_K8S_VERSION_PATCH)
```

  So we can then call `E2E_K8S_VERSION=1.32 make test-e2e` or `E2E_K8S_FULL_VERSION=1.32.3 make test-e2e`? sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-15T08:51:39Z

- **Makefile-test.mk:64** by [@mimowo](https://github.com/mimowo) — 2025-04-15T08:51:39Z
```diff
@@ -55,9 +55,13 @@ endif
 
 # Folder where the e2e tests are located.
 E2E_TARGET ?= ./test/e2e/...
-E2E_KIND_VERSION ?= kindest/node:v1.32.0
-# E2E_K8S_VERSIONS sets the list of k8s versions included in test-e2e-all
-E2E_K8S_VERSIONS ?= 1.30.9 1.31.5 1.32.1
+E2E_K8S_VERSIONS ?= 1.30.10 1.31.6 1.32.3
+E2E_K8S_VERSION ?= 1.32
+E2E_K8S_FULL_VERSION ?= $(filter $(E2E_K8S_VERSION).%,$(E2E_K8S_VERSIONS))
+# Default to E2E_K8S_VERSION.0 if no match is found
+E2E_K8S_FULL_VERSION := $(or $(E2E_K8S_FULL_VERSION),$(E2E_K8S_VERSION).0)
+E2E_KIND_VERSION ?= kindest/node:v$(E2E_K8S_FULL_VERSION)
+
```

  nit, drop extra empty line

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-15T08:52:16Z

Thank you!
/approve
LGTM, leaving final lgtm to @mbobrovskyi 

/cherry-pick release-0.11 release-0.10
