# PR #4962: Protect GuaranteedQuota When FairSharing Weight=0

**Summary**: Protect GuaranteedQuota When FairSharing Weight=0

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/4962

**Last updated**: 2025-05-06T09:45:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `protect_nominal` → `main`
- **Created**: 2025-04-14T14:47:01Z
- **Updated**: 2025-05-06T09:45:35Z
- **Merged**: 2025-04-15T11:53:09Z
- **Closed**: 2025-04-15T11:53:09Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 10   **+62 / -24**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
We protect workloads running under nominal quota from being preempted, even when Cohort has FairWeight of 0. We do this by only returning math.MaxInt score when the Cohort is borrowing. When a Cohort is not borrowing, it gets a FairShare score of 0.

#### Which issue(s) this PR fixes:
Fixes #4961

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
Fix bug where Cohorts with FairWeight set to 0 could have workloads running within Nominal Quota preempted
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-04-14T14:47:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cf19e381ef579e31f9afe451786d54816e49ba76 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/67fd1fe72d2e52000993814d |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-4962--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-4962--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTQ5NjItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.HQHMeOy9edJLzVXMViro-tnbOQ8hJkrjbR2L5r0U-h4)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-4962--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-04-15T10:54:53Z

/lgtm
Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-15T10:55:01Z

LGTM label has been added.  <details>Git tree hash: 30ed13ff96e0276b7aa6f9a4a5fde1cc07250b7c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-15T11:25:58Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/4962#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/4962#discussion_r2044304299" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-04-15T11:27:15Z

/cherry-pick release-0.11

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-04-15T11:27:18Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `release-0.11` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4962#issuecomment-2804725685):

>/cherry-pick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-04-15T11:53:49Z

@tenzen-y: new pull request created: #4980

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/4962#issuecomment-2804725685):

>/cherry-pick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-04-14T16:14:45Z

- **pkg/cache/fair_sharing.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2025-04-14T16:14:34Z
```diff
@@ -38,19 +38,18 @@ type dominantResourceShareNode interface {
 	hierarchicalResourceNode
 }
 
-// dominantResourceShare returns a value from 0 to 1,000,000 representing the maximum of the ratios
-// of usage above nominal quota to the lendable resources in the cohort, among all the resources
-// provided by the ClusterQueue, and divided by the weight.
-// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
-// The function also returns the resource name that yielded this value.
-// Also for a weight of zero, this will return 9223372036854775807.
+// dominantResourceShare returns a value from 0 to 1,000,000
+// representing the maximum of the ratios of usage above nominal quota
+// to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue, and divided by the weight.  If zero,
+// it means that the usage of the ClusterQueue is below the nominal
+// quota.  The function also returns the resource name that yielded
+// this value.  When the FairSharing weight is 0, and the ClusterQueue
+// or Cohort is borrowing, we return math.MaxInt.
 func dominantResourceShare(node dominantResourceShareNode, wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
 	if !node.HasParent() {
 		return 0, ""
 	}
-	if node.fairWeight().IsZero() {
-		return math.MaxInt, ""
-	}
 
 	borrowing := make(map[corev1.ResourceName]int64, len(node.getResourceNode().SubtreeQuota))
```

  what if the number of`borrowing` is 0?
  Are there still the same problems?
  
  I meant I guess lines 61-62 still have problems.
  
  ```
  	if len(borrowing) == 0 {
  		return 0, ""
  	}
  ```

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-04-15T07:33:31Z

- **pkg/cache/fair_sharing.go:54** by [@gabesaba](https://github.com/gabesaba) — 2025-04-15T07:33:31Z
```diff
@@ -38,19 +38,18 @@ type dominantResourceShareNode interface {
 	hierarchicalResourceNode
 }
 
-// dominantResourceShare returns a value from 0 to 1,000,000 representing the maximum of the ratios
-// of usage above nominal quota to the lendable resources in the cohort, among all the resources
-// provided by the ClusterQueue, and divided by the weight.
-// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
-// The function also returns the resource name that yielded this value.
-// Also for a weight of zero, this will return 9223372036854775807.
+// dominantResourceShare returns a value from 0 to 1,000,000
+// representing the maximum of the ratios of usage above nominal quota
+// to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue, and divided by the weight.  If zero,
+// it means that the usage of the ClusterQueue is below the nominal
+// quota.  The function also returns the resource name that yielded
+// this value.  When the FairSharing weight is 0, and the ClusterQueue
+// or Cohort is borrowing, we return math.MaxInt.
 func dominantResourceShare(node dominantResourceShareNode, wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
 	if !node.HasParent() {
 		return 0, ""
 	}
-	if node.fairWeight().IsZero() {
-		return math.MaxInt, ""
-	}
 
 	borrowing := make(map[corev1.ResourceName]int64, len(node.getResourceNode().SubtreeQuota))
```

  What is the problem with those lines? Moving the lines that I did ensures that we give a DRS of 0 when there is no borrowing (at lines 61-63), and only MaxInt if the CQ/Cohort (with weight 0) is borrowing

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-04-15T11:01:09Z

- **pkg/cache/fair_sharing.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2025-04-15T11:01:09Z
```diff
@@ -38,19 +38,18 @@ type dominantResourceShareNode interface {
 	hierarchicalResourceNode
 }
 
-// dominantResourceShare returns a value from 0 to 1,000,000 representing the maximum of the ratios
-// of usage above nominal quota to the lendable resources in the cohort, among all the resources
-// provided by the ClusterQueue, and divided by the weight.
-// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
-// The function also returns the resource name that yielded this value.
-// Also for a weight of zero, this will return 9223372036854775807.
+// dominantResourceShare returns a value from 0 to 1,000,000
+// representing the maximum of the ratios of usage above nominal quota
+// to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue, and divided by the weight.  If zero,
+// it means that the usage of the ClusterQueue is below the nominal
+// quota.  The function also returns the resource name that yielded
+// this value.  When the FairSharing weight is 0, and the ClusterQueue
+// or Cohort is borrowing, we return math.MaxInt.
 func dominantResourceShare(node dominantResourceShareNode, wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
 	if !node.HasParent() {
 		return 0, ""
 	}
-	if node.fairWeight().IsZero() {
-		return math.MaxInt, ""
-	}
 
 	borrowing := make(map[corev1.ResourceName]int64, len(node.getResourceNode().SubtreeQuota))
```

  Based on this comment:
  
  > When the FairSharing weight is 0, and the ClusterQueue
  // or Cohort is borrowing, we return math.MaxInt.
  
  IIUC, we want to return match.MaxInt only when weight is 0 and there is borrowing, right?
  However, in lines 61-62, we return `0` regardless of weight value.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-04-15T11:25:51Z

- **pkg/cache/fair_sharing.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2025-04-15T11:25:51Z
```diff
@@ -38,19 +38,18 @@ type dominantResourceShareNode interface {
 	hierarchicalResourceNode
 }
 
-// dominantResourceShare returns a value from 0 to 1,000,000 representing the maximum of the ratios
-// of usage above nominal quota to the lendable resources in the cohort, among all the resources
-// provided by the ClusterQueue, and divided by the weight.
-// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
-// The function also returns the resource name that yielded this value.
-// Also for a weight of zero, this will return 9223372036854775807.
+// dominantResourceShare returns a value from 0 to 1,000,000
+// representing the maximum of the ratios of usage above nominal quota
+// to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue, and divided by the weight.  If zero,
+// it means that the usage of the ClusterQueue is below the nominal
+// quota.  The function also returns the resource name that yielded
+// this value.  When the FairSharing weight is 0, and the ClusterQueue
+// or Cohort is borrowing, we return math.MaxInt.
 func dominantResourceShare(node dominantResourceShareNode, wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
 	if !node.HasParent() {
 		return 0, ""
 	}
-	if node.fairWeight().IsZero() {
-		return math.MaxInt, ""
-	}
 
 	borrowing := make(map[corev1.ResourceName]int64, len(node.getResourceNode().SubtreeQuota))
```

  Ah, I misunderstand something. Line 61-62 indicates there is no borrowing. So, we do not need to case the weight value.
  
  /approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-04-15T11:26:47Z

- **pkg/cache/fair_sharing.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2025-04-15T11:26:46Z
```diff
@@ -38,19 +38,18 @@ type dominantResourceShareNode interface {
 	hierarchicalResourceNode
 }
 
-// dominantResourceShare returns a value from 0 to 1,000,000 representing the maximum of the ratios
-// of usage above nominal quota to the lendable resources in the cohort, among all the resources
-// provided by the ClusterQueue, and divided by the weight.
-// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
-// The function also returns the resource name that yielded this value.
-// Also for a weight of zero, this will return 9223372036854775807.
+// dominantResourceShare returns a value from 0 to 1,000,000
+// representing the maximum of the ratios of usage above nominal quota
+// to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue, and divided by the weight.  If zero,
+// it means that the usage of the ClusterQueue is below the nominal
+// quota.  The function also returns the resource name that yielded
+// this value.  When the FairSharing weight is 0, and the ClusterQueue
+// or Cohort is borrowing, we return math.MaxInt.
 func dominantResourceShare(node dominantResourceShareNode, wlReq resources.FlavorResourceQuantities) (int, corev1.ResourceName) {
 	if !node.HasParent() {
 		return 0, ""
 	}
-	if node.fairWeight().IsZero() {
-		return math.MaxInt, ""
-	}
 
 	borrowing := make(map[corev1.ResourceName]int64, len(node.getResourceNode().SubtreeQuota))
```

  /cherry-pick release-0.11
