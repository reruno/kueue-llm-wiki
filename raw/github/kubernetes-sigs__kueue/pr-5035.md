# PR #5035: Add git hub action to automatically bump hugo version on dependabot commit

**Summary**: Add git hub action to automatically bump hugo version on dependabot commit

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5035

**Last updated**: 2025-04-24T15:20:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@2000krysztof](https://github.com/2000krysztof)
- **Branch**: `VersionBumpingWorkflow` → `main`
- **Created**: 2025-04-17T09:53:45Z
- **Updated**: 2025-04-24T15:20:33Z
- **Merged**: 2025-04-24T15:20:32Z
- **Closed**: 2025-04-24T15:20:32Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+35 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR will automate the hugo version bumping and it will alter dependabot's commits to bump the version where dependabot can't.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #4811

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-17T09:53:56Z

Hi @2000krysztof. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-04-17T09:54:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 097d10efc8859f22cd10fbbc1c0b2e09fb39114d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/68076eaba88e330008c7faa4 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-18T07:39:39Z

Please also close the comments that have already been resolved. It's hard to understand which ones are still under discussion.

### Comment by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-22T09:28:54Z

I'm still testing this on my end to see if it all works.

### Comment by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-22T10:44:00Z

I think this version should work

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-22T11:30:54Z

> I think this version should work

Yeah, I can confirm it.

PR: https://github.com/mbobrovskyi/kueue/pull/15.
Action: https://github.com/mbobrovskyi/kueue/actions/runs/14593602466/job/40934204207.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-22T11:33:57Z

LGTM label has been added.  <details>Git tree hash: 8badaad304f2098b987ca0c5278db9aac16f85e1</details>

### Comment by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-22T13:53:07Z

@tenzen-y Could you have a look at this PR and see if there is anything else that might be needed.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-24T06:57:49Z

/cc @mimowo @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-24T15:12:36Z

@2000krysztof @mbobrovskyi once this merges what are the next steps? Will this start to update the Hugo PRs, or yet requires some extra configuration in Kueue settings?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-24T15:12:51Z

Thank you for the effort!

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-24T15:16:45Z

> @2000krysztof @mbobrovskyi once this merges what are the next steps? Will this start to update the Hugo PRs, or yet requires some extra configuration in Kueue settings?

It should start to update the Hugo PRs without additional configuration.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-04-24T15:19:26Z

/lgtm
/approve
Thank you 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-24T15:19:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5035#" title="Author self-approved">2000krysztof</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5035#pullrequestreview-2783796230" title="LGTM">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5035#issuecomment-2828015719" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-17T10:56:37Z

/ok-to-test

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-17T10:59:01Z

Just a quick comment, will wait with approval for a pass from another contributor, maybe  @gabesaba or @mbobrovskyi

- **.github/workflows/sync-hugo.yaml:10** by [@mimowo](https://github.com/mimowo) — 2025-04-17T10:57:28Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
```

  This seems targeting all PRs by dependabot, can we somehow restrict to only the PR which aims to bump hugo specifially?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-17T11:23:00Z

- **.github/workflows/sync-hugo.yaml:27** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-17T11:23:00Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - run: |
+                  make sync-hugo-version
+                  git config user.name "github-actions[bot]"
+                  git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
+                  git add .
+                  git commit -m "Bump Hugo version"
```

  What if we don't have changes? Can we check it and commit only if we have changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-17T11:28:46Z

- **.github/workflows/sync-hugo.yaml:27** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-17T11:28:45Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - run: |
+                  make sync-hugo-version
+                  git config user.name "github-actions[bot]"
+                  git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
+                  git add .
+                  git commit -m "Bump Hugo version"
```

  ```suggestion
                    git commit -m "Sync Hugo version"
  ```

### Review by [@2000krysztof](https://github.com/2000krysztof) (commented) — 2025-04-17T11:42:05Z

- **.github/workflows/sync-hugo.yaml:10** by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-17T11:42:05Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
```

  I can look into that

### Review by [@2000krysztof](https://github.com/2000krysztof) (commented) — 2025-04-17T11:44:12Z

- **.github/workflows/sync-hugo.yaml:27** by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-17T11:44:12Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - run: |
+                  make sync-hugo-version
+                  git config user.name "github-actions[bot]"
+                  git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
+                  git add .
+                  git commit -m "Bump Hugo version"
```

  I can try to cancel it if there are no diffs the commit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-17T11:46:26Z

- **.github/workflows/sync-hugo.yaml:25** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-17T11:33:48Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - run: |
+                  make sync-hugo-version
+                  git config user.name "github-actions[bot]"
+                  git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
```

  ```suggestion
                    git config user.name "$(git log -1 --pretty=%an)"
                    git config user.email "$(git log -1 --pretty=%ae)"
  ```

- **.github/workflows/sync-hugo.yaml:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-17T11:38:05Z
  I think it would be great to commonize this workflow. We have other cases where our Dependabot updates require manual intervention – for example, this one: https://github.com/kubernetes-sigs/kueue/pull/4459.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-17T11:54:48Z

- **.github/workflows/sync-hugo.yaml:10** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-17T11:54:48Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
```

  ```suggestion
          if: ${{ github.actor == 'dependabot[bot]' }}
  ```
  
  Are you sure we have this label?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-17T12:20:58Z

- **.github/workflows/sync-hugo.yaml:15** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-17T12:20:58Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
```

  I don't think this is necessary – we're not making any changes to the PR, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-17T12:23:25Z

I tested it in my local fork and it seems to be working fine. Here's my configuration: https://github.com/mbobrovskyi/kueue/blob/main/.github/workflows/sync-hugo.yaml.

### Review by [@2000krysztof](https://github.com/2000krysztof) (commented) — 2025-04-17T14:11:27Z

- **.github/workflows/sync-hugo.yaml:15** by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-17T14:11:26Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
```

  We are making changes to the pr. The action edits the pr and edits the version in the toml file

### Review by [@2000krysztof](https://github.com/2000krysztof) (commented) — 2025-04-17T14:13:16Z

- **.github/workflows/sync-hugo.yaml:1** by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-17T14:13:16Z
  For this we could have a generic yaml action instead of a hugo specific one where we can run all the synchronisation commands.

### Review by [@2000krysztof](https://github.com/2000krysztof) (commented) — 2025-04-17T14:14:00Z

- **.github/workflows/sync-hugo.yaml:25** by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-17T14:14:00Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - run: |
+                  make sync-hugo-version
+                  git config user.name "github-actions[bot]"
+                  git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
```

  I will do that I was just working of what was in the github example

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-17T14:17:42Z

- **.github/workflows/sync-hugo.yaml:15** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-17T14:17:42Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
```

  According to the documentation (https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/controlling-permissions-for-github_token#overview), `pull-requests` permissions are only required if we need to add labels or modify PR details. However, for pushing changes, I believe `contents` permissions should be enough.

### Review by [@2000krysztof](https://github.com/2000krysztof) (commented) — 2025-04-17T14:24:15Z

- **.github/workflows/sync-hugo.yaml:1** by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-17T14:24:15Z
  Since this is an opportunity to commonize the way of helping dependabot out, we could also make some room for other things to be put in there. I think that this could instead be broken into separate steps and we could make one commit after all the changes are done to keep it clean.

### Review by [@2000krysztof](https://github.com/2000krysztof) (commented) — 2025-04-17T14:27:19Z

- **.github/workflows/sync-hugo.yaml:15** by [@2000krysztof](https://github.com/2000krysztof) — 2025-04-17T14:27:19Z
```diff
@@ -0,0 +1,28 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependabot') }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          pull-requests: write
```

  I see what you mean now. We only need the contents: write permission.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-18T07:38:13Z

- **.github/workflows/sync-dependabot.yaml:26** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-18T07:19:24Z
```diff
@@ -0,0 +1,36 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - name: sync hugo version
+
+            # Space for other changes to supplement dependabot
+
+            - run: |
+                  make sync-hugo-version
+
+
+
+            - name: commit and push the changes
+            - run: |
```

  ```suggestion
              - name: Sync hugo version
                if: ${{ contains(github.event.pull_request.title, 'hugo') }}
                run: |
                    make sync-hugo-version
  ```
  
  For now it is ok to have just a one step.

- **.github/workflows/sync-dependabot.yaml:2** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-18T07:28:28Z
```diff
@@ -0,0 +1,36 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
```

  ```suggestion
  # This workflow syncs dependency versions for Dependabot PRs
  name: Dependabot Version Sync
  ```
  Maybe like this?

- **.github/workflows/sync-dependabot.yaml:36** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-18T07:37:18Z
```diff
@@ -0,0 +1,36 @@
+# This workflow runs the make sync-hugo-version command and alters the dependabot pr
+name: Hugo Version Synchronizer
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - name: sync hugo version
+
+            # Space for other changes to supplement dependabot
+
+            - run: |
+                  make sync-hugo-version
+
+
+
+            - name: commit and push the changes
+            - run: |
+                  git config user.name "$(git log -1 --pretty=%an)"
+                  git config user.email "$(git log -1 --pretty=%ae)"
+                  git add .
+                  git commit -m "Sync versions for dependabot"
+                  git push
```

  ```suggestion
                    if ! git diff --exit-code; then
                          git config user.name "$(git log -1 --pretty=%an)"
                          git config user.email "$(git log -1 --pretty=%ae)"
                          git add .
                          git commit -m "Sync hugo version"
                          git push
                    else
                          echo "No changes to commit"
                    fi
  ```
  Maybe like this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-22T09:46:17Z

- **.github/workflows/sync-dependabot.yaml:26** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-22T09:45:44Z
```diff
@@ -0,0 +1,35 @@
+# This workflow syncs dependency versions for Dependabot PRs
+name: Dependabot Version Sync
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - name: sync hugo version
+              if: ${{ contains(github.event.pull_request.title, 'hugo') }}
+            - run: |
+                  make sync-hugo-version
+            - name: commit and push the changes
+            - run: |
```

  ```suggestion
              - name: commit and push the changes
                run: |
  ```

- **.github/workflows/sync-dependabot.yaml:23** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-22T09:45:59Z
```diff
@@ -0,0 +1,35 @@
+# This workflow syncs dependency versions for Dependabot PRs
+name: Dependabot Version Sync
+
+on:
+  pull_request_target:
+    branches: [ main ]
+
+jobs:
+    run-sync-hugo-version:
+        if: ${{ github.actor == 'dependabot[bot]' }}
+        runs-on: ubuntu-latest
+
+        # Permission required to edit a PR
+        permissions:
+          contents: write
+
+        steps:
+            - uses: actions/checkout@v4
+              with: 
+                ref: ${{github.head_ref}}
+            - name: sync hugo version
+              if: ${{ contains(github.event.pull_request.title, 'hugo') }}
+            - run: |
```

  ```suggestion
              - name: sync hugo version
                if: ${{ contains(github.event.pull_request.title, 'hugo') }}
                run: |
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2025-04-22T11:33:51Z

/lgtm
This is a great start toward automating dependency bumps without requiring manual fixes.
Thank you so much!
