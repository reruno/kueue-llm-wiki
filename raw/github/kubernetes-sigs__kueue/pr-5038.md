# PR #5038: Promote the LocalQueueDefaulting feature to Beta

**Summary**: Promote the LocalQueueDefaulting feature to Beta

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5038

**Last updated**: 2025-05-06T18:25:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@MaysaMacedo](https://github.com/MaysaMacedo)
- **Branch**: `promote-local-queue-defaulting` → `main`
- **Created**: 2025-04-17T19:50:45Z
- **Updated**: 2025-05-06T18:25:19Z
- **Merged**: 2025-05-06T18:25:17Z
- **Closed**: 2025-05-06T18:25:17Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: _none_
- **Changed files**: 6   **+114 / -9**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

This commit promotes LocalQueueDefaulting to Beta
(enabled by default).

#### Which issue(s) this PR fixes:

Fixes #4942

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Promoted LocalQueueDefaulting to Beta and enabled by default.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-04-17T19:52:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ad4b0390f3033d1268081133db21a32cef6d0f45 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/681a44413a0d830008c3ee4f |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5038--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5038--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTUwMzgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.NfxW1oWqJ7Fhr4nJZCxx-jJ2lHT3dGHStzEDGsyhbPg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5038--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/notifications#deploy-webhooks)._

### Comment by [@kannon92](https://github.com/kannon92) — 2025-04-17T20:06:09Z

/hold

I have some minor nits but the main gap here is lack of e2e tests.

https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/customconfigs/managejobswithoutqueuename_test.go

I think maybe we could consider a similar test for customconfigs that runs with LocalQueueDefaulting enabled.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-04-23T11:17:08Z

LGTM overall, will add a label once comments to tests are addressed

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-30T17:22:00Z

Try looking here: https://github.com/kubernetes-sigs/kueue/tree/main/test/e2e/customconfigs   I think something similar to https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/customconfigs/managejobswithoutqueuename_test.go (but perhaps not as extensive) is what @tenzen-y is suggesting.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-04-30T17:31:41Z

> Try looking here: https://github.com/kubernetes-sigs/kueue/tree/main/test/e2e/customconfigs I think something similar to https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/customconfigs/managejobswithoutqueuename_test.go (but perhaps not as extensive) is what @tenzen-y is suggesting.

@dgrove-oss right, thanks for the suggestion. But that `manageJobsWithoutQueueName` is enabling a configuration setting, not a feature gate as `LocalQueueDefaulting`. So, I'm not sure how could I disable the `LocalQueueDefaulting` feature gate in the test, without using something [like this](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6).

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-04-30T17:38:03Z

> > Try looking here: https://github.com/kubernetes-sigs/kueue/tree/main/test/e2e/customconfigs I think something similar to https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/customconfigs/managejobswithoutqueuename_test.go (but perhaps not as extensive) is what @tenzen-y is suggesting.
> 
> @dgrove-oss right, thanks for the suggestion. But that `manageJobsWithoutQueueName` is enabling a configuration setting, not a feature gate as `LocalQueueDefaulting`. So, I'm not sure how could I disable the `LocalQueueDefaulting` feature gate in the test, without using something [like this](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6).

You are correct.  Sorry, I should have read the entire conversation before responding.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-04-30T17:39:41Z

> > > Try looking here: https://github.com/kubernetes-sigs/kueue/tree/main/test/e2e/customconfigs I think something similar to https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/customconfigs/managejobswithoutqueuename_test.go (but perhaps not as extensive) is what @tenzen-y is suggesting.
> > 
> > 
> > @dgrove-oss right, thanks for the suggestion. But that `manageJobsWithoutQueueName` is enabling a configuration setting, not a feature gate as `LocalQueueDefaulting`. So, I'm not sure how could I disable the `LocalQueueDefaulting` feature gate in the test, without using something [like this](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6).
> 
> You are correct. Sorry, I should have read the entire conversation before responding.

@dgrove-oss is on the right track. Late last release, I folded FeatureGates into the configuration also to help make things like this easier.

https://github.com/kubernetes-sigs/kueue/blob/main/apis/config/v1beta1/configuration_types.go#L101

So if we are able to programatticaly change feature gates we should be able to add these tests.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-04-30T18:44:52Z

> > > > Try looking here: https://github.com/kubernetes-sigs/kueue/tree/main/test/e2e/customconfigs I think something similar to https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/customconfigs/managejobswithoutqueuename_test.go (but perhaps not as extensive) is what @tenzen-y is suggesting.
> > > 
> > > 
> > > @dgrove-oss right, thanks for the suggestion. But that `manageJobsWithoutQueueName` is enabling a configuration setting, not a feature gate as `LocalQueueDefaulting`. So, I'm not sure how could I disable the `LocalQueueDefaulting` feature gate in the test, without using something [like this](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6).
> > 
> > 
> > You are correct. Sorry, I should have read the entire conversation before responding.
> 
> @dgrove-oss is on the right track. Late last release, I folded FeatureGates into the configuration also to help make things like this easier.
> 
> https://github.com/kubernetes-sigs/kueue/blob/main/apis/config/v1beta1/configuration_types.go#L101
> 
> So if we are able to programatticaly change feature gates we should be able to add these tests.

@kannon92 great point, I wasn't aware about it! I gave it a try, but unfortunately it doesn't work due to:

`{"error": "feature gates for CLI and configuration cannot both specified"}`

There are some feature gates enabled in [the deployment](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6).

### Comment by [@kannon92](https://github.com/kannon92) — 2025-04-30T18:55:03Z


> @kannon92 great point, I wasn't aware about it! I gave it a try, but unfortunately it doesn't work due to:
> 
> `{"error": "feature gates for CLI and configuration cannot both specified"}`
> 
> There are some feature gates enabled in [the deployment](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6).

Maybe we can consider moving those feature gates into https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/config/default/controller_manager_config.yaml?

Remove the CLI arguments so its easier to toggle this going forward?

WDYT @tenzen-y?

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-05-05T22:52:35Z

> > @kannon92 great point, I wasn't aware about it! I gave it a try, but unfortunately it doesn't work due to:
> > `{"error": "feature gates for CLI and configuration cannot both specified"}`
> > There are some feature gates enabled in [the deployment](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6).
> 
> Maybe we can consider moving those feature gates into https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/config/default/controller_manager_config.yaml?
> 
> Remove the CLI arguments so its easier to toggle this going forward?
> 
> WDYT @tenzen-y?

I will proceed with trying to implement this. @tenzen-y Let us know what you think, thanks

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-06T17:06:54Z

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-06T17:08:04Z

please rebase

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-06T18:23:53Z

LGTM label has been added.  <details>Git tree hash: be06af1d40764553c2920f38582fcc77ef51dc43</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-06T18:23:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5038#" title="Author self-approved">MaysaMacedo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5038#pullrequestreview-2819238649" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-04-17T20:01:55Z

- **pkg/features/kube_features.go:242** by [@kannon92](https://github.com/kannon92) — 2025-04-17T19:59:14Z
```diff
@@ -239,6 +239,7 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	},
 	LocalQueueDefaulting: {
 		{Version: version.MustParse("0.10"), Default: false, PreRelease: featuregate.Alpha},
+		{Version: version.MustParse("0.12"), Default: false, PreRelease: featuregate.Beta},
```

  ```suggestion
  		{Version: version.MustParse("0.12"), Default: true, PreRelease: featuregate.Beta},
  ```

- **pkg/controller/jobs/statefulset/statefulset_webhook_test.go:158** by [@kannon92](https://github.com/kannon92) — 2025-04-17T20:00:20Z
```diff
@@ -155,7 +155,9 @@ func TestDefault(t *testing.T) {
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
-			features.SetFeatureGateDuringTest(t, features.LocalQueueDefaulting, tc.localQueueDefaulting)
+			if !tc.localQueueDefaulting {
```

  I'd revert this change and keep the old change.
  
  You can always set a feature gate to false/true in the tests.

- **pkg/controller/jobframework/base_webhook_test.go:249** by [@kannon92](https://github.com/kannon92) — 2025-04-17T20:01:41Z
```diff
@@ -246,7 +246,9 @@ func TestBaseWebhookDefault(t *testing.T) {
 	for name, tc := range testcases {
 		t.Run(name, func(t *testing.T) {
 			ctx, _ := utiltesting.ContextWithLog(t)
-			features.SetFeatureGateDuringTest(t, features.LocalQueueDefaulting, tc.localQueueDefaulting)
+			if !tc.localQueueDefaulting {
```

  I would prefer to revert all these changes and keep the old code. 
  
  ie
  
  ```suggestion
  			features.SetFeatureGateDuringTest(t, features.LocalQueueDefaulting, tc.localQueueDefaulting)
  ```
  
  Apply that to all the tests below also.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-23T11:11:39Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:177** by [@PBundyra](https://github.com/PBundyra) — 2025-04-23T11:11:39Z
```diff
@@ -84,6 +84,44 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the job has been admitted", func() {
+				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(testJob.Name, testJob.UID), Namespace: ns.Name}
+				createdWorkload := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
```

  Can we use `util.ExpectWorkloadsToBeAdmitted` function here?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-23T11:15:54Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:158** by [@PBundyra](https://github.com/PBundyra) — 2025-04-23T11:15:54Z
```diff
@@ -84,6 +84,44 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
```

  ```suggestion
  				gomega.Consistently(func(g gomega.Gomega) {
  ```
  To ensure that there are no data races

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-23T11:25:36Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:105** by [@mimowo](https://github.com/mimowo) — 2025-04-23T11:25:36Z
```diff
@@ -101,7 +101,8 @@ func (w *RayJobWebhook) validateCreate(job *rayv1.RayJob) field.ErrorList {
 	var allErrors field.ErrorList
 	kueueJob := (*RayJob)(job)
 
-	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" || features.Enabled(features.LocalQueueDefaulting) {
+	queueName := jobframework.QueueName(kueueJob)
+	if w.manageJobsWithoutQueueName || queueName != "" || (features.Enabled(features.LocalQueueDefaulting) && queueName == "default") {
```

  This seems like a bugfix unrelated to the graduation itself. 
  
  Would it be feasible (ideally with unit test) to extract to separate PR we may cherry-pick?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-04-23T13:43:05Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:105** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-04-23T13:43:05Z
```diff
@@ -101,7 +101,8 @@ func (w *RayJobWebhook) validateCreate(job *rayv1.RayJob) field.ErrorList {
 	var allErrors field.ErrorList
 	kueueJob := (*RayJob)(job)
 
-	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" || features.Enabled(features.LocalQueueDefaulting) {
+	queueName := jobframework.QueueName(kueueJob)
+	if w.manageJobsWithoutQueueName || queueName != "" || (features.Enabled(features.LocalQueueDefaulting) && queueName == "default") {
```

  Thanks for the review!
  I just created this PR https://github.com/kubernetes-sigs/kueue/pull/5073

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-23T13:51:30Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:158** by [@mimowo](https://github.com/mimowo) — 2025-04-23T13:51:30Z
```diff
@@ -84,6 +84,44 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
```

  Actually, I'm not sure what data races you mean which could be related to the change. I would prefer to use Consistently rarely because it makes tests longer without apparent benefit. WDYT ?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-23T14:30:08Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:158** by [@PBundyra](https://github.com/PBundyra) — 2025-04-23T14:30:08Z
```diff
@@ -84,6 +84,44 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
```

  After second consideration I agree that the data race I had in mind wouldn't occur here, so I'm good with leaving it as it is

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-04-23T14:30:43Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:163** by [@PBundyra](https://github.com/PBundyra) — 2025-04-23T14:30:42Z
```diff
@@ -84,6 +84,44 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
```

  Could we also check if there is annotation to ensure defaulting worked

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-23T14:40:21Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:105** by [@mimowo](https://github.com/mimowo) — 2025-04-23T14:40:21Z
```diff
@@ -101,7 +101,8 @@ func (w *RayJobWebhook) validateCreate(job *rayv1.RayJob) field.ErrorList {
 	var allErrors field.ErrorList
 	kueueJob := (*RayJob)(job)
 
-	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" || features.Enabled(features.LocalQueueDefaulting) {
+	queueName := jobframework.QueueName(kueueJob)
+	if w.manageJobsWithoutQueueName || queueName != "" || (features.Enabled(features.LocalQueueDefaulting) && queueName == "default") {
```

  Thanks, let's drop from this PR then.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-04-23T15:24:54Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:105** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-04-23T15:24:54Z
```diff
@@ -101,7 +101,8 @@ func (w *RayJobWebhook) validateCreate(job *rayv1.RayJob) field.ErrorList {
 	var allErrors field.ErrorList
 	kueueJob := (*RayJob)(job)
 
-	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" || features.Enabled(features.LocalQueueDefaulting) {
+	queueName := jobframework.QueueName(kueueJob)
+	if w.manageJobsWithoutQueueName || queueName != "" || (features.Enabled(features.LocalQueueDefaulting) && queueName == "default") {
```

  Note that if I drop this change from this PR I will get some unit tests failures. So, I believe we would need to have the other PR merged first.
  Like:
  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5038/pull-kueue-test-unit-main/1914772229044834304

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-04-25T15:06:23Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:163** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-04-25T15:06:23Z
```diff
@@ -84,6 +84,44 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-04-25T18:13:49Z

- **pkg/controller/jobs/rayjob/rayjob_webhook.go:103** by [@mimowo](https://github.com/mimowo) — 2025-04-25T18:13:49Z
```diff
@@ -101,7 +100,7 @@ func (w *RayJobWebhook) validateCreate(job *rayv1.RayJob) field.ErrorList {
 	var allErrors field.ErrorList
 	kueueJob := (*RayJob)(job)
 
-	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" || features.Enabled(features.LocalQueueDefaulting) {
+	if w.manageJobsWithoutQueueName || jobframework.QueueName(kueueJob) != "" {
```

  I would suggest that https://github.com/kubernetes-sigs/kueue/pull/5073 goes first

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-04-25T20:18:16Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:179** by [@tenzen-y](https://github.com/tenzen-y) — 2025-04-25T20:16:42Z
```diff
@@ -84,6 +84,53 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).Should(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the job has been admitted", func() {
+				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(testJob.Name, testJob.UID), Namespace: ns.Name}
+				createdWorkload := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  Why do we want to use `LondTimeout`?

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:183** by [@tenzen-y](https://github.com/tenzen-y) — 2025-04-25T20:18:06Z
```diff
@@ -84,6 +84,53 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).Should(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the job has been admitted", func() {
+				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(testJob.Name, testJob.UID), Namespace: ns.Name}
+				createdWorkload := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+	})
+
```

  Additionally, could you add a case where the LocalQueueDefaulting is false. Because by this PR, there are not E2E testings for the situation where the LocalQueueDefaulting is default.
  
  However, this feature is still Beta, we should verify the case in E2E.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-04-28T13:56:04Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:179** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-04-28T13:56:04Z
```diff
@@ -84,6 +84,53 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).Should(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the job has been admitted", func() {
+				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(testJob.Name, testJob.UID), Namespace: ns.Name}
+				createdWorkload := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  no specific reason, I'm updating it

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-04-30T14:50:17Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:183** by [@kannon92](https://github.com/kannon92) — 2025-04-30T14:50:17Z
```diff
@@ -84,6 +84,53 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).Should(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the job has been admitted", func() {
+				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(testJob.Name, testJob.UID), Namespace: ns.Name}
+				createdWorkload := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+	})
+
```

  This seems to be a bit challenging to solve from Kueue side.
  
  So the way I see it, is that we have a test where this LocalQueueDefaulting is true.
  
  Do you think we should create a new test-infra job to run e2es with this feature gate turned off? And once this feature is promoted to GA, we then remove that feature gate off job.
  
  For unit tests it is trivial to set feature gates but for e2e it would require deploying Kueue with the gate turned off and running the tests?
  
  Do you have any examples of feature disable/enable in e2es for Kueue?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2025-04-30T15:48:58Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:183** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2025-04-30T15:48:58Z
```diff
@@ -84,6 +84,53 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).Should(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the job has been admitted", func() {
+				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(testJob.Name, testJob.UID), Namespace: ns.Name}
+				createdWorkload := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+	})
+
```

  Thanks for the input @kannon92 
  It's indeed not simple to disable the feature gate on the fly with e2e.
  The only place I found where the feature gates is enabled/disabled in e2e is [here](https://github.com/kubernetes-sigs/kueue/blob/55019ff9c1d12a4bd3e5991535db8f8d0b74c1b3/test/e2e/config/default/manager_e2e_patch.yaml#L6). But that would require creating a hole set of separate tests for localQueueDefault disabled. @tenzen-y Is that the goal? or would you prefer to implement some unit tests somewhere?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-06T05:52:27Z

Thank you, basically lgtm

- **test/e2e/config/default/controller_manager_config.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-06T05:50:14Z
  Thank you for this update!

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:137** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-06T05:51:51Z
```diff
@@ -84,6 +84,103 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=false", func() {
+		ginkgo.It("should suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("setting local queue defulting as false", func() {
+				updateKueueConfiguration(func(cfg *config.Configuration) {
+					cfg.FeatureGates = map[string]bool{"LocalQueueDefaulting": false}
+					cfg.ManageJobsWithoutQueueName = true
+				})
+			})
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying no existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).ShouldNot(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+			ginkgo.By("setting feature gates back to its original state", func() {
+				updateKueueConfiguration(func(cfg *config.Configuration) {
+					cfg.ManageJobsWithoutQueueName = true
+				})
+			})
+		})
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
```

  TBH, I'm wondering if we should add LocalQueue defaulting to dedicated E2E testing.
  However, we can reconsider it as a follow-up issue.

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:179** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-06T05:52:16Z
```diff
@@ -84,6 +84,53 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
+		ginkgo.It("should not suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does not get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeFalse())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).Should(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying that the job has been admitted", func() {
+				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(testJob.Name, testJob.UID), Namespace: ns.Name}
+				createdWorkload := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
+					g.Expect(createdWorkload.Status.Admission).ShouldNot(gomega.BeNil())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, createdWorkload)
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  Is this still `LongTimeout` needed?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-06T06:03:55Z

- **test/e2e/customconfigs/managejobswithoutqueuename_test.go:137** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-06T06:03:55Z
```diff
@@ -84,6 +84,103 @@ var _ = ginkgo.Describe("ManageJobsWithoutQueueName", ginkgo.Ordered, func() {
 		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, defaultRf, true, util.LongTimeout)
 		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=false", func() {
+		ginkgo.It("should suspend a job", func() {
+			var testJob, createdJob *batchv1.Job
+			var jobLookupKey types.NamespacedName
+
+			ginkgo.By("setting local queue defulting as false", func() {
+				updateKueueConfiguration(func(cfg *config.Configuration) {
+					cfg.FeatureGates = map[string]bool{"LocalQueueDefaulting": false}
+					cfg.ManageJobsWithoutQueueName = true
+				})
+			})
+
+			ginkgo.By("creating a default LocalQueue", func() {
+				localQueue = testing.MakeLocalQueue("default", ns.Name).ClusterQueue("cluster-queue").Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.By("creating an unsuspended job without a queue name", func() {
+				testJob = testingjob.MakeJob("test-job", ns.Name).
+					Suspend(false).
+					Image(util.E2eTestAgnHostImage, util.BehaviorExitFast).
+					Obj()
+				util.MustCreate(ctx, k8sClient, testJob)
+			})
+
+			ginkgo.By("verifying that the job does get suspended", func() {
+				createdJob = &batchv1.Job{}
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("verifying no existence of kueue labels", func() {
+				jobLookupKey = types.NamespacedName{Name: testJob.Name, Namespace: ns.Name}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).Should(gomega.Succeed())
+					g.Expect(createdJob.Labels).ShouldNot(gomega.HaveKeyWithValue(controllerconstants.QueueLabel, "default"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+			ginkgo.By("setting feature gates back to its original state", func() {
+				updateKueueConfiguration(func(cfg *config.Configuration) {
+					cfg.ManageJobsWithoutQueueName = true
+				})
+			})
+		})
+	})
+
+	ginkgo.When("manageJobsWithoutQueueName=true and LocalQueueDefaulting=true", func() {
```

  NVM, this will be removed when both graduate to GA.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-06T18:23:47Z

Thank you!
/lgtm
/approve
