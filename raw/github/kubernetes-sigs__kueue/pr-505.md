# PR #505: Delete workload when job changed in reconciling

**Summary**: Delete workload when job changed in reconciling

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/505

**Last updated**: 2023-01-17T02:27:58Z

---

## Metadata

- **State**: closed
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `refactor/job-reconcile` → `main`
- **Created**: 2023-01-10T08:37:20Z
- **Updated**: 2023-01-17T02:27:58Z
- **Merged**: —
- **Closed**: 2023-01-17T02:27:20Z
- **Labels**: `size/L`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 6   **+63 / -129**

## Description

Signed-off-by: Kante Yin <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

1. Confused with `ensureAtMostOneWorkload()` since we construct workload with Job's namespacedName, there should not be two different workloads for one Job. So I will find the workload with the Job's namespacedName.
2. When we changed the job's spec, like the `parallelism`, we'll delete the workload and create a new one. If job is unsuspend, we'll stop the job at first.
 
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-10T19:06:06Z

/assign @ahg-g 

I agree with Kante: ensuring that there is only one Workload is unnecessary code, given how its name matches the job name, which is unique.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-01-11T03:34:46Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/505#" title="Author self-approved">kerthcet</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from ahg-g by writing `/assign @ahg-g` in a comment. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["ahg-g"]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2023-01-11T15:34:45Z

> Confused with ensureAtMostOneWorkload() since we construct workload with Job's namespacedName, there should not be two different workloads for one Job. So I will find the workload with the Job's namespacedName.

The source of truth is the ownerReference on the job, not the names, hence the logic to ensure a single Job.

> When we changed the job's spec, like the parallelism, we'll delete the workload and create a new one. If job is unsuspend, we'll stop the job at first.

What difference does this make? Why add another call to the api-server?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-01-13T10:54:52Z

 > When we changed the job's spec, like the parallelism, we'll delete the workload and create a new one. If job is unsuspend, we'll stop the job at first.

We'll set the owner while constructing the workload in `ConstructWorkloadFor`, unless we create a workload with the same owner but a different namespaced obj than `jobNamespace/jobName`, or this will not happen.

> What difference does this make? Why add another call to the api-server?

I think there's no difference, we have the same logics in `ensureAtMostOneWorkload`.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-01-16T04:10:53Z

@ahg-g can you confirm this? I want to add a Job Interface{} https://github.com/kubernetes-sigs/kueue/issues/369 here for other workloads easy to write the controllers, if this is necessary for Job, then same to other workloads.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2023-01-16T07:53:09Z

> We'll set the owner while constructing the workload in ConstructWorkloadFor, unless we create a workload with the same owner but a different namespaced obj than jobNamespace/jobName, or this will not happen.

Nothing prevents another controller or a manual creation of another workload with the same ownerReference but different name.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-01-16T10:07:50Z

> Nothing prevents another controller or a manual creation of another workload with the same ownerReference but different name.

Avoid bad influence of other controllers makes sense although this is rare but we should ban, then I'd like to close this PR and make `EnsureOneWorkload` a method of the Job Interface.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-16T14:56:14Z

> make EnsureOneWorkload a method of the Job Interface.

I don't think it should be a method of the interface. We can probably provide it as a utility method. All we need is the Group, Version, Kind of the custom Job API.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2023-01-16T18:28:09Z

>  this is rare 

Just a quick feedback, rare is not an argument we can use when building production ready and reliable systems, it either happens or it doesn't :)

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-01-17T02:27:17Z

> I don't think it should be a method of the interface. We can probably provide it as a utility method. All we need is the Group, Version, Kind of the custom Job API.

I'll reconsider this, currently, we use `ensureAtMostOneWorkload` to find the corresponding workload. 

> Just a quick feedback, rare is not an argument we can use when building production ready and reliable systems, it either happens or it doesn't :)

Good suggestion, thanks. Let me close the PR.
/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-01-17T02:27:21Z

@kerthcet: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/505#issuecomment-1384758866):

>> I don't think it should be a method of the interface. We can probably provide it as a utility method. All we need is the Group, Version, Kind of the custom Job API.
>
>I'll reconsider this, currently, we use `ensureAtMostOneWorkload` to find the corresponding workload. 
>
>> Just a quick feedback, rare is not an argument we can use when building production ready and reliable systems, it either happens or it doesn't :)
>
>Good suggestion, thanks. Let me close the PR.
>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-01-10T09:10:40Z

- **pkg/controller/workload/job/job_controller.go:191** by [@kerthcet](https://github.com/kerthcet) — 2023-01-10T09:10:20Z
```diff
@@ -195,6 +177,25 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
+	// 4. Handle a not finished job
+
+	if !jobAndWorkloadEqual(&job, wl) {
+		if !jobSuspended(&job) {
+			log.V(2).Info("Running job is changed, suspending")
+			if err := r.stopJob(ctx, wl, &job, "Running job is changed, suspending"); err != nil {
+				log.Error(err, "Suspending job")
+				return ctrl.Result{}, err
+			}
+		}
+
+		if err := r.client.Delete(ctx, wl); err != nil && !apierrors.IsNotFound(err) {
```

  This worths discussing, I choose to delete the workload directly because it's hard to know what changed in Job unless we list them out and seems like all the important messages we can all get from Job again. Admission can be ignored here because the Job is suspend again unless we want to reuse the admission.
