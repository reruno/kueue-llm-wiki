# PR #5127: Create a metric to track the number of workloads evicted due to PodsReady timeouts.

**Summary**: Create a metric to track the number of workloads evicted due to PodsReady timeouts.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5127

**Last updated**: 2025-05-26T08:49:56Z

---

## Metadata

- **State**: closed
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/metric-to-track-the-number-of-workloads-evicted-due-to-PodsReady-timeouts` → `main`
- **Created**: 2025-04-25T15:41:38Z
- **Updated**: 2025-05-26T08:49:56Z
- **Merged**: —
- **Closed**: 2025-05-26T08:49:34Z
- **Labels**: `kind/feature`, `release-note`, `size/XL`, `kind/api-change`, `needs-rebase`, `cncf-cla: yes`
- **Assignees**: [@mszadkow](https://github.com/mszadkow), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow)
- **Changed files**: 19   **+544 / -101**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind api-change

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Create a metric to track the number of workloads evicted due to PodsReady timeouts.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5071

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Added kueue_wait_for_pods_ready_workload_eviction_total metric – counts workloads evicted due to hitting a WaitForPodsReady timeout at least once.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-25T15:41:41Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-25T15:41:44Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5127#" title="Author self-approved">mbobrovskyi</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-04-25T15:41:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c42b079a61974e6502c0f8f2bd685e3ce5de8290 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/681b4dec17a1370008ce660e |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-25T16:32:24Z

/cc @mszadkow

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-25T16:33:03Z

/cc @mimowo

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-04-28T07:20:30Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-04-28T07:20:39Z

LGTM label has been added.  <details>Git tree hash: f8ec4b6619b7f939eb510e1daa8cd74dcfa73eb6</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-28T08:21:36Z

/cc @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-01T06:51:53Z

New changes are detected. LGTM label has been removed.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-08T16:09:17Z

/kind api-change

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-09T06:58:32Z

@mbobrovskyi IIUC, this is not must-have feature for closely 0.12 release. So, could you update the KEP, https://github.com/kubernetes-sigs/kueue/tree/main/keps/1282-pods-ready-requeue-strategy

@mimowo is my understanding correct for Priority?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-09T08:41:10Z

> @mbobrovskyi IIUC, this is not must-have feature for closely 0.12 release. So, could you update the KEP, https://github.com/kubernetes-sigs/kueue/tree/main/keps/1282-pods-ready-requeue-strategy

Created https://github.com/kubernetes-sigs/kueue/pull/5211.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-09T08:58:58Z

> @mimowo is my understanding correct for Priority?

I would suggest to still prefer to include it in 0.12, but if we fail then we update the kep for 0.13.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-09T12:06:26Z

/assign
Let's agree on API first, and then I'll make a pass on the implementation

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-12T13:31:33Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-26T08:48:51Z

/close 

Due to fixed by https://github.com/kubernetes-sigs/kueue/pull/5259.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-26T08:49:29Z

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-26T08:49:37Z

@mbobrovskyi: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5127#issuecomment-2908992470):

>/close 
>
>Due to fixed by https://github.com/kubernetes-sigs/kueue/pull/5259.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-26T08:49:55Z

@mbobrovskyi: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5127#issuecomment-2908993954):

>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-04-25T15:46:39Z

- **site/content/en/docs/reference/kueue.v1beta1.md:2544** by [@kannon92](https://github.com/kannon92) — 2025-04-25T15:46:39Z
```diff
@@ -2525,6 +2525,49 @@ domain indicated by the values field.</p>
 
 
 
+## `WaitForPodsReadyRequeue`     {#kueue-x-k8s-io-v1beta1-WaitForPodsReadyRequeue}
```

  I'm not sure if our metrics are autogenerated but you are missing a site update for metrics.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-04-25T15:49:02Z

- **pkg/metrics/metrics.go:66** by [@kannon92](https://github.com/kannon92) — 2025-04-25T15:48:58Z
```diff
@@ -64,6 +64,14 @@ var (
 
 	// Metrics tied to the scheduler
 
+	WaitForPodsReadyWorkloadEvictionTotal = prometheus.NewCounterVec(
```

  Would having a counter per namespace or workload type be also useful here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-25T15:50:04Z

- **site/content/en/docs/reference/kueue.v1beta1.md:2544** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-25T15:50:04Z
```diff
@@ -2525,6 +2525,49 @@ domain indicated by the values field.</p>
 
 
 
+## `WaitForPodsReadyRequeue`     {#kueue-x-k8s-io-v1beta1-WaitForPodsReadyRequeue}
```

  Unfortunately, no. Thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-04-25T16:25:00Z

- **pkg/metrics/metrics.go:66** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-04-25T16:25:00Z
```diff
@@ -64,6 +64,14 @@ var (
 
 	// Metrics tied to the scheduler
 
+	WaitForPodsReadyWorkloadEvictionTotal = prometheus.NewCounterVec(
```

  What do you mean by 'workload type'?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-04-25T19:36:55Z

- **pkg/metrics/metrics.go:66** by [@kannon92](https://github.com/kannon92) — 2025-04-25T19:36:55Z
```diff
@@ -64,6 +64,14 @@ var (
 
 	// Metrics tied to the scheduler
 
+	WaitForPodsReadyWorkloadEvictionTotal = prometheus.NewCounterVec(
```

  Sorry I got confused. I more meant if this was a RayJob, RayCluster, etc. But I am not actually sure if that is easy to get or if we want that information.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-04-28T07:18:52Z

- **pkg/metrics/metrics.go:66** by [@mszadkow](https://github.com/mszadkow) — 2025-04-28T07:18:51Z
```diff
@@ -64,6 +64,14 @@ var (
 
 	// Metrics tied to the scheduler
 
+	WaitForPodsReadyWorkloadEvictionTotal = prometheus.NewCounterVec(
```

  If it was not `Total` that could be per clusterQueue. 
  Not sure which one is desired, in this form looks good.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-04-28T07:19:43Z

- **test/e2e/customconfigs/waitforpodsready_test.go:57** by [@mszadkow](https://github.com/mszadkow) — 2025-04-28T07:19:43Z
```diff
@@ -44,8 +51,45 @@ var _ = ginkgo.Describe("WaitForPodsReady Job Controller E2E", func() {
 		job   *batchv1.Job
 		wl    kueue.Workload
 		wlKey types.NamespacedName
+
+		metricsReaderClusterRoleBinding *rbacv1.ClusterRoleBinding
+
+		curlContainerName string
+		curlPod           *corev1.Pod
```

  Great test update, I am definitely using that one if your PR gets merged first :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-01T06:18:43Z

- **apis/kueue/v1beta1/workload_types.go:376** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-01T06:18:43Z
```diff
@@ -367,6 +367,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
```

  Why do we need to preserve this information in Workload? Why do we not just increment the Prometheus metrics count?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-01T06:43:39Z

- **apis/kueue/v1beta1/workload_types.go:376** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-01T06:43:38Z
```diff
@@ -367,6 +367,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
```

  According to the description ("The number of workloads evicted due to hitting one of the WaitForPodsReady timeouts at least once"), we need to track whether this is the first time the timeout has occurred for a given workload. If it’s the first occurrence, we can safely increment the metric.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-06T12:39:28Z

- **apis/kueue/v1beta1/workload_types.go:376** by [@mimowo](https://github.com/mimowo) — 2025-05-06T12:39:28Z
```diff
@@ -367,6 +367,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
```

  Yes, the idea is to count workloads with the timeout, not necessarily how many times a timeout was hit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T11:47:55Z

- **pkg/controller/core/workload_controller.go:590** by [@mimowo](https://github.com/mimowo) — 2025-05-07T11:47:55Z
```diff
@@ -566,14 +566,36 @@ func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req c
 	message := fmt.Sprintf("Exceeded the PodsReady timeout %s", req.String())
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, message)
 	workload.ResetChecksOnEviction(wl, r.clock.Now())
+	reportWaitForPodsReadyEvicted := r.waitForPodsReadyRequeueInc(wl, reason)
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
 	if err == nil {
 		cqName, _ := r.queues.ClusterQueueForWorkload(wl)
 		workload.ReportEvictedWorkload(r.recorder, wl, cqName, kueue.WorkloadEvictedByPodsReadyTimeout, message)
+		if reportWaitForPodsReadyEvicted {
+			metrics.ReportWaitForPodsReadyEvictedWorkload(wl.Namespace, reason)
+		}
 	}
 	return 0, client.IgnoreNotFound(err)
 }
 
+func (r *WorkloadReconciler) waitForPodsReadyRequeueInc(wl *kueue.Workload, reason kueue.WaitForPodsReadyRequeueReason) bool {
+	requeue := workload.FindWaitForPodsReadyRequeue(wl, reason)
+	if requeue == nil {
+		requeue = &kueue.WaitForPodsReadyRequeue{Reason: reason}
+	}
+
+	var report bool
+	if requeue.Count == 0 {
+		report = true
+	}
```

  ```suggestion
  	report := requeue.Count == 0
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T11:52:16Z

- **pkg/metrics/metrics.go:66** by [@mimowo](https://github.com/mimowo) — 2025-05-07T11:52:16Z
```diff
@@ -64,6 +64,14 @@ var (
 
 	// Metrics tied to the scheduler
 
+	WaitForPodsReadyWorkloadEvictionTotal = prometheus.NewCounterVec(
```

  I think split by "framworkName" makes sense, but not sure how easy it is to add. Also, we don't track the frameworkName for other workload metrics at the moment. Maybe we can say 
  `StabilityLevel: metrics.Beta` to make such a proposed adjustment easier in the future, wdyt?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-07T14:13:26Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mwielgus](https://github.com/mwielgus) — 2025-05-07T14:13:26Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  This structure is generic enough to hold any workload-related counters. Yet its name (and the name of the other types) artificially reduces the scope to wait for pods ready. I would recommend changing it to one of the directions:
  
  * Make it truly generic. Rename stuff to WorkloadStats, StatName, etc.
  * Change it to WaitForPodsReady specific. Introduce dedicated fields like startupFailureCount, runtimeFailureCount, or so.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-07T14:24:02Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mimowo](https://github.com/mimowo) — 2025-05-07T14:24:01Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  Exploring (2.), maybe
  ```yaml
  WaitForPodsReadyStats:
    startupTimeoutCount:
    recoveryTimeoutCount:
  ```
  wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-07T14:45:14Z

- **apis/kueue/v1beta1/workload_types.go:376** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-07T14:45:06Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
```

  I understand what is the objective for this field. In that case, why can we not reuse `requeueState` field?
  Why can we not just add reason field to `requeueState` field?
  Why do we need to store both `StartupTimeout` and `RecoveryTimeout` reasons?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-08T16:08:21Z

- **apis/kueue/v1beta1/workload_types.go:376** by [@mimowo](https://github.com/mimowo) — 2025-05-08T16:08:21Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
```

  We probably could, but we would need to extend it, because currently `requeueState` does not hold the reason at all. Moreover, `requeueState` is incremented for other reasons too, such as Failed ProvisiningRequest.
  
  I opened the issue on behalf of a user, and in the user request they want a metric only specifically for WaitForPodsReady.
  
  Let us know if you have some specific idea how to extend `requeueState` to include the necessary information to build the metric.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-08T16:09:28Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mimowo](https://github.com/mimowo) — 2025-05-08T16:09:27Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  Actually, I'm not in favor of dedicated fields per reason, because it means we need to add new field per reason. 
  Maybe just:
  ```
  WaitForPodsReadyStats:
    evictionByReasonCount
  ```
  where evictionByReasonCount is map[string]int32.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-09T07:25:19Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-09T07:25:19Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  > where evictionByReasonCount is map[string]int32.
  
  The idea to keep separate struct for this is to allow extend it in future. 
  
  https://github.com/kubernetes-sigs/kueue/blob/c42b079a61974e6502c0f8f2bd685e3ce5de8290/apis/kueue/v1beta1/workload_types.go#L386-L400
  
  For example, to add `LastTrasitionTime` to know when it was happens last time.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T07:48:59Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mimowo](https://github.com/mimowo) — 2025-05-09T07:48:59Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  I see the intention/
  
  However, it is not clear to me if we need it, the use case of determining the last eviction time can be taken from the Eviction condition. The name `WaitForPodsReadyStats` also suggests it is for stats, so LastTransitionTime does not fit this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-09T07:58:42Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-09T07:58:42Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  >  the last eviction time can be taken from the Eviction condition.
  
  Yes, but it can be overwritten with other reasons.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T08:58:21Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mimowo](https://github.com/mimowo) — 2025-05-09T08:58:20Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  Indeed, but in any case I'm not clear this is a likely feature request. Also, I'm not sure how this would be useful for metrics. I would suggest to keep `WaitForPodsReadyStats` as stats useful to build metrics. Otherwise we may overengineer the api.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-09T11:57:31Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@PBundyra](https://github.com/PBundyra) — 2025-05-09T11:57:31Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  > Actually, I'm not in favor of dedicated fields per reason, because it means we need to add new field per reason. Maybe just:
  > 
  > ```
  > WaitForPodsReadyStats:
  >   evictionByReasonCount
  > ```
  > 
  > where evictionByReasonCount is map[string]int32.
  
  +1 to what @mimowo has said. I wonder if we could have a rather generic field in Workload's Status as @mwielgus  suggested, e.g. `evictionStats`. Then it could be a list of struct that holds: `Reason` (eviction/startup waitForPodsReady/recovery waitForPodsReady/AC), and the counter. I don't see the need for the timestamp of the last transition. I think this could be a generally useful field for observability, not only to track waitForPodsReady, but preemptions as well

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-09T12:02:25Z

- **apis/kueue/v1beta1/workload_types.go:376** by [@PBundyra](https://github.com/PBundyra) — 2025-05-09T12:02:25Z
```diff
@@ -367,6 +367,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
```

  It makes sense for a Workload to be counted only once, since if it failed once it's likely to fail again, and then it adds noise to the metric. I believe the main goal of this metric should be to track how many workloads have failed, but we shouldn't double/triple count it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-09T12:04:56Z

- **apis/kueue/v1beta1/workload_types.go:376** by [@PBundyra](https://github.com/PBundyra) — 2025-05-09T12:04:56Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
```

  +1 to what @mimowo has said, I think it would be useful to track the reason behind eviction/requeueing as well, not only the fact it was requeued

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T12:13:57Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@mimowo](https://github.com/mimowo) — 2025-05-09T12:13:57Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  any concrete proposal?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-09T14:36:10Z

- **apis/kueue/v1beta1/workload_types.go:386** by [@PBundyra](https://github.com/PBundyra) — 2025-05-09T14:36:10Z
```diff
@@ -365,6 +365,38 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// waitForPodsReadyRequeuesByReason holds the re-queue state for WaitForPodsReady.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	WaitForPodsReadyRequeuesByReason []WaitForPodsReadyRequeue `json:"waitForPodsReadyRequeuesByReason,omitempty"`
+}
+
+type WaitForPodsReadyRequeueReason string
+
+const (
+	StartupTimeoutReason  WaitForPodsReadyRequeueReason = "StartupTimeout"
+	RecoveryTimeoutReason WaitForPodsReadyRequeueReason = "RecoveryTimeout"
+)
+
+type WaitForPodsReadyRequeue struct {
```

  I thought about something like:
  ```
  WorkloadStatus {
          ...
  	// +optional
  	// +listType=map
  	// +listMapKey=name
  	EvictionStats []EvictionStat `json:"evictionStats,omitempty"`
  }
  
  type EvictionStat struct {
  	// Reason the Workload has been evicted
  	Reason string
  
  	// How many times the Workload has been evicted with this Reason
  	Count  int64
  }
  ```
