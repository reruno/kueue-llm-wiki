# PR #514: Preemption within ClusterQueue and cohort

**Summary**: Preemption within ClusterQueue and cohort

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/514

**Last updated**: 2023-01-30T16:34:53Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `preemption` → `main`
- **Created**: 2023-01-12T21:16:28Z
- **Updated**: 2023-01-30T16:34:53Z
- **Merged**: 2023-01-30T16:34:53Z
- **Closed**: 2023-01-30T16:34:53Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 20   **+2005 / -49**

## Description

#### What type of PR is this?

/kind feature

/kind api-change

#### What this PR does / why we need it:

Implements https://github.com/kubernetes-sigs/kueue/tree/05da320/keps/83-workload-preemption

#### Which issue(s) this PR fixes:

Fixes #83

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-01-12T21:16:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/514#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-18T20:52:39Z

/assign @ahg-g

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T13:23:13Z

squashed into 2 commits

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T13:23:28Z

/hold cancel

### Comment by [@ahg-g](https://github.com/ahg-g) — 2023-01-30T16:33:39Z

/lgtm

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-01-22T06:15:00Z

The suggestions for optimizations can be done on followup PRs.

- **apis/kueue/v1alpha2/clusterqueue_types.go:301** by [@ahg-g](https://github.com/ahg-g) — 2023-01-20T22:13:39Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
+)
+
+// ClusterQueuePreemption constains policies to preempt Workloads from this
```

  ```suggestion
  // ClusterQueuePreemption contains policies to preempt Workloads from this
  ```

- **apis/kueue/v1alpha2/clusterqueue_types.go:307** by [@ahg-g](https://github.com/ahg-g) — 2023-01-20T22:14:25Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
+)
+
+// ClusterQueuePreemption constains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+	// withinCohort determines whether a pending Workload can preempt Workloads
+	// from other ClusterQueues in the cohort that are using more than their min
+	// quota.
+	// Possible values are:
```

  nit: move this up on the same line

- **apis/kueue/v1alpha2/clusterqueue_types.go:314** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T01:21:23Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
+)
+
+// ClusterQueuePreemption constains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+	// withinCohort determines whether a pending Workload can preempt Workloads
+	// from other ClusterQueues in the cohort that are using more than their min
+	// quota.
+	// Possible values are:
+	//
+	// - `Never` (default): do not preempt workloads in the cohort.
+	// - `ReclaimFromLowerPriority`: if the pending workload fits within the min
+	//   quota of its ClusterQueue, only preempt workloads in the cohort that have
+	//   lower priority than the pending Workload.
+	// - `ReclaimFromAny`: if the pending workload fits within the min quota of its
+	//   ClusterQueue, preempt any workload in the cohort.
```

  Just to emphasize the difference from the other option.
  
  ```suggestion
  	//   ClusterQueue, preempt any workload in the cohort irrespective of priority.
  ```

- **apis/kueue/v1alpha2/clusterqueue_types.go:323** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T01:21:55Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
+)
+
+// ClusterQueuePreemption constains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+	// withinCohort determines whether a pending Workload can preempt Workloads
+	// from other ClusterQueues in the cohort that are using more than their min
+	// quota.
+	// Possible values are:
+	//
+	// - `Never` (default): do not preempt workloads in the cohort.
+	// - `ReclaimFromLowerPriority`: if the pending workload fits within the min
+	//   quota of its ClusterQueue, only preempt workloads in the cohort that have
+	//   lower priority than the pending Workload.
+	// - `ReclaimFromAny`: if the pending workload fits within the min quota of its
+	//   ClusterQueue, preempt any workload in the cohort.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;ReclaimFromLowerPriority;ReclaimFromAny
+	WithinCohort PreemptionPolicy `json:"withinCohort,omitempty"`
+
+	// withinClusterQueue determines whether a pending workload that doesn't fit
+	// within the min quota for its ClusterQueue, can preempt active Workloads in
+	// the ClusterQueue.
+	// Possible values are:
```

  ditto, looks odd

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T01:24:24Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  do we need both `ReclaimFromLowerPriority` and `LowerPriority`? we can do the following as well:
  
  `Never`, `LowerPriority` and `Any`

- **pkg/cache/cache.go:284** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T01:45:41Z
```diff
@@ -274,6 +280,13 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[string
 	}
 	c.UsedResources = usedResources
 	c.UpdateWithFlavors(resourceFlavors)
+
+	if in.Spec.Preemption != nil {
```

  why here and not in defaulting webhook?

- **pkg/workload/workload.go:253** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T02:53:06Z
```diff
@@ -237,3 +237,32 @@ func UpdateStatusIfChanged(ctx context.Context,
 	// Updating an existing condition
 	return UpdateStatus(ctx, c, wl, conditionType, conditionStatus, reason, message)
 }
+
+// ClearAdmissionPatch creates a new object based on the input workload that
+// doesn't contain admission. The object can be used in Server-Side-Apply.
+func ClearAdmissionPatch(w *kueue.Workload) *kueue.Workload {
+	wlCopy := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			UID:        w.UID,
+			Name:       w.Name,
+			Namespace:  w.Namespace,
+			Generation: w.Generation, // Produce a conflict if there was a change in the spec.
+		},
+		TypeMeta: w.TypeMeta,
+	}
+	if wlCopy.APIVersion == "" {
```

  is this and the one below for tests?

- **pkg/workload/workload.go:243** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T02:55:39Z
```diff
@@ -237,3 +237,32 @@ func UpdateStatusIfChanged(ctx context.Context,
 	// Updating an existing condition
 	return UpdateStatus(ctx, c, wl, conditionType, conditionStatus, reason, message)
 }
+
+// ClearAdmissionPatch creates a new object based on the input workload that
+// doesn't contain admission. The object can be used in Server-Side-Apply.
+func ClearAdmissionPatch(w *kueue.Workload) *kueue.Workload {
```

  this is more of a "base" than a "clear admission" patch, no?

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:119** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T22:06:52Z
```diff
@@ -116,50 +186,76 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 				gomega.Expect(err).Should(gomega.HaveOccurred())
 				gomega.Expect(errors.IsInvalid(err)).Should(gomega.BeTrue(), "error: %v", err)
 			default:
-				gomega.Expect(err).Should(gomega.Succeed())
```

  we don't need to check for success anymore?

- **test/integration/scheduler/preemption_test.go:39** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T22:11:45Z
```diff
@@ -0,0 +1,238 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	lowPriority int32 = iota - 1
+	midPriority
+	highPriority
+)
+
+var _ = ginkgo.Describe("Preemption", func() {
+	var (
+		alphaFlavor *kueue.ResourceFlavor
+
```

  empty line

- **test/integration/scheduler/preemption_test.go:136** by [@ahg-g](https://github.com/ahg-g) — 2023-01-21T22:30:32Z
```diff
@@ -0,0 +1,238 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	lowPriority int32 = iota - 1
+	midPriority
+	highPriority
+)
+
+var _ = ginkgo.Describe("Preemption", func() {
+	var (
+		alphaFlavor *kueue.ResourceFlavor
+
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "preemption-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		alphaFlavor = testing.MakeResourceFlavor("alpha").Obj()
+		gomega.Expect(k8sClient.Create(ctx, alphaFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, alphaFlavor, true)
+	})
+
+	ginkgo.Context("In a single ClusterQueue", func() {
+		var (
+			cq *kueue.ClusterQueue
+			q  *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(alphaFlavor.Name, "4").Obj()).
+					Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+			q = testing.MakeLocalQueue("q", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, q)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		})
+
+		ginkgo.It("Should preempt Workloads with lower priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			lowWl1 := testing.MakeWorkload("low-wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			lowWl2 := testing.MakeWorkload("low-wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			midWl := testing.MakeWorkload("mid-wl", ns.Name).
+				Queue(q.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			highWl1 := testing.MakeWorkload("high-wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, lowWl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowWl2)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, midWl)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, highWl1)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl1, lowWl2, midWl, highWl1)
+
+			ginkgo.By("Creating a low priority Workload")
+			lowWl3 := testing.MakeWorkload("low-wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, lowWl3)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl3)
+
+			ginkgo.By("Creating a high priority Workload")
+			highWl2 := testing.MakeWorkload("high-wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, highWl2)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
+		})
+	})
+
+	ginkgo.Context("In a ClusterQueue that is part of a cohort", func() {
```

  Can we have a case where a high priority workload submitted to alphaCQ preempts two admitted workloads: the first is a lower priority from alphaCQ and the second from betaCQ that is borrowing from alphaCQ1?

- **pkg/scheduler/scheduler_test.go:691** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T03:19:19Z
```diff
@@ -682,6 +688,44 @@ func TestSchedule(t *testing.T) {
 				"eng-beta/user-on-demand": *utiltesting.MakeAdmission("eng-beta").Flavor(corev1.ResourceCPU, "on-demand").Obj(),
 			},
 		},
+		"preempt workloads in ClusterQueue and cohort": {
```

  does this single test provide enough coverage? It seems we should have more cases here.

- **pkg/scheduler/scheduler.go:158** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T03:30:42Z
```diff
@@ -138,19 +141,23 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		if e.assignment.RepresentativeMode() == flavorassigner.NoFit {
 			continue
 		}
-		c := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
+		cq := snapshot.ClusterQueues[e.ClusterQueue]
+		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
 			e.status = skipped
 			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
 			continue
 		}
 		// Even if there was a failure, we shouldn't admit other workloads to this
 		// cohort.
-		if c.Cohort != nil {
-			usedCohorts.Insert(c.Cohort.Name)
+		if cq.Cohort != nil {
+			usedCohorts.Insert(cq.Cohort.Name)
 		}
+		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
+		ctx := ctrl.LoggerInto(ctx, log)
 		if e.assignment.RepresentativeMode() != flavorassigner.Fit {
-			// TODO(#43): Implement preemption.
+			if err := s.preemptor.Do(ctx, e.Info, e.assignment, &snapshot); err != nil {
```

  I think it will be useful if in the event message (and may be in the pending status message) we indicate if this workload preempted others, WDYT?

- **pkg/scheduler/preemption/preemption.go:168** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T03:39:01Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
```

  what if one was NoFit? shouldn't we abort preemption completely?

- **pkg/scheduler/preemption/preemption.go:168** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T03:42:39Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
```

  ok, so because of `RepresentativeMode`, we will not be here if at least one flavor is NoFit, I would either put a comment to clarify that or update the code to take NoFit into account (test RepresentativeMode again in this func?).

- **pkg/scheduler/preemption/preemption.go:235** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T03:57:25Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
+			}
+			if !workloadUsesFlavors(candidateWl, flavors) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
+	}
+	return candidates
+}
+
+func cqIsBorrowing(cq *cache.ClusterQueue, flavors flavorsPerResource) bool {
+	for res, rFlavors := range flavors {
+		fUsage := cq.UsedResources[res]
+		requestable := cq.RequestableResources[res]
+		if requestable == nil {
+			// Cant' be borrowing if this resource is not defined in the ClusterQueue.
+			continue
+		}
+		for _, flvLimits := range requestable.Flavors {
+			if rFlavors.Has(flvLimits.Name) && fUsage[flvLimits.Name] > flvLimits.Min {
+				return true
+			}
+		}
+	}
+	return false
+}
+
+func workloadUsesFlavors(wl *workload.Info, flavors flavorsPerResource) bool {
+	for _, ps := range wl.TotalRequests {
```

  perhaps we can have a set of used flavors in workload.Info to avoid this iteration.

- **pkg/scheduler/preemption/preemption.go:217** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T03:59:51Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
+			}
+			if !workloadUsesFlavors(candidateWl, flavors) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
+	}
+	return candidates
+}
+
+func cqIsBorrowing(cq *cache.ClusterQueue, flavors flavorsPerResource) bool {
```

  we can calculate a set of flavors that each CQ is borrowing when creating the snapshot?

- **pkg/scheduler/preemption/preemption.go:206** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T04:09:01Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
```

  In a large cluster, with many workloads, it will be worth exiting early. We should consider keeping a list of the workloads names of each CQ ordered by priority.

- **pkg/scheduler/preemption/preemption.go:303** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T04:18:49Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
+			}
+			if !workloadUsesFlavors(candidateWl, flavors) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
+	}
+	return candidates
+}
+
+func cqIsBorrowing(cq *cache.ClusterQueue, flavors flavorsPerResource) bool {
+	for res, rFlavors := range flavors {
+		fUsage := cq.UsedResources[res]
+		requestable := cq.RequestableResources[res]
+		if requestable == nil {
+			// Cant' be borrowing if this resource is not defined in the ClusterQueue.
+			continue
+		}
+		for _, flvLimits := range requestable.Flavors {
+			if rFlavors.Has(flvLimits.Name) && fUsage[flvLimits.Name] > flvLimits.Min {
+				return true
+			}
+		}
+	}
+	return false
+}
+
+func workloadUsesFlavors(wl *workload.Info, flavors flavorsPerResource) bool {
+	for _, ps := range wl.TotalRequests {
+		for res, flv := range ps.Flavors {
+			if flavors[res].Has(flv) {
+				return true
+			}
+		}
+	}
+	return false
+}
+
+func totalRequestsForAssignment(wl *workload.Info, assignment flavorassigner.Assignment) cache.ResourceQuantities {
+	usage := make(cache.ResourceQuantities)
+	for i, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			resUsage := usage[res]
+			if resUsage == nil {
+				resUsage = make(map[string]int64)
+				usage[res] = resUsage
+			}
+			resUsage[assignment.PodSets[i].Flavors[res].Name] += q
+		}
+	}
+	return usage
+}
+
+// workloadFits determines if the workload requests would fits given the
+// requestable resources and simulated usage of the ClusterQueue and its cohort,
+// if it belongs to one.
+// These two checks are a simplification compared to flavorassigner.fitsFlavorsLimits,
+// because there is no borrowing when doing preemptions.
+func workloadFits(wlReq cache.ResourceQuantities, cq *cache.ClusterQueue) bool {
+	for res, flvReqs := range wlReq {
+		cqFlvsUsage := cq.UsedResources[res]
+		cqResRequestable := cq.RequestableResources[res]
+		var cohortFlvsUsage, cohortFlvsRequestable map[string]int64
+		if cq.Cohort != nil {
+			cohortFlvsUsage = cq.Cohort.UsedResources[res]
+			cohortFlvsRequestable = cq.Cohort.RequestableResources[res]
+		}
+		for _, flvLimits := range cqResRequestable.Flavors {
+			flvReq, ok := flvReqs[flvLimits.Name]
+			if !ok {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			if cqFlvsUsage[flvLimits.Name]+flvReq > flvLimits.Min {
+				return false
+			}
+			if len(cohortFlvsRequestable) == 0 {
+				continue
+			}
+			if cohortFlvsUsage[flvLimits.Name]+flvReq > cohortFlvsRequestable[flvLimits.Name] {
+				return false
+			}
+		}
+	}
+	return true
+}
+
+func candidatesOrdering(candidates []*workload.Info, cq string, now time.Time) func(int, int) bool {
+	return func(i, j int) bool {
+		a := candidates[i]
+		b := candidates[j]
+		aInCQ := a.ClusterQueue == cq
```

  add a comment that order is first based on within cohort vs within CQ, then priority

- **pkg/scheduler/preemption/preemption.go:126** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T05:08:48Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
```

  add a comment describing the criteria to calculate the "minimal preemption"

- **pkg/scheduler/preemption/preemption.go:134** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T05:11:29Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
```

  why are we checking `cqIsBorrowing` again?

- **pkg/scheduler/preemption/preemption_test.go:44** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T05:59:57Z
```diff
@@ -0,0 +1,587 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"sync"
+	"testing"
+	"time"
+
+	"github.com/go-logr/logr/testr"
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func TestPreemption(t *testing.T) {
```

  what is the test coverage for preemption.go?

- **pkg/scheduler/preemption/preemption_test.go:90** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T06:12:47Z
```diff
@@ -0,0 +1,587 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"sync"
+	"testing"
+	"time"
+
+	"github.com/go-logr/logr/testr"
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func TestPreemption(t *testing.T) {
+	flavors := []*kueue.ResourceFlavor{
+		utiltesting.MakeResourceFlavor("default").Obj(),
+		utiltesting.MakeResourceFlavor("alpha").Obj(),
+		utiltesting.MakeResourceFlavor("beta").Obj(),
+	}
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("standalone").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Resource(utiltesting.MakeResource(corev1.ResourceMemory).
+				Flavor(utiltesting.MakeFlavor("alpha", "3Gi").Obj()).
+				Flavor(utiltesting.MakeFlavor("beta", "3Gi").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj(),
+		utiltesting.MakeClusterQueue("c1").
+			Cohort("cohort").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				WithinCohort:       kueue.PreemptionPolicyReclaimFromLowerPriority,
+			}).
+			Obj(),
+		utiltesting.MakeClusterQueue("c2").
+			Cohort("cohort").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyNever,
+				WithinCohort:       kueue.PreemptionPolicyReclaimFromAny,
+			}).
+			Obj(),
+	}
+	cases := map[string]struct {
+		admitted      []kueue.Workload
+		incoming      *kueue.Workload
+		targetCQ      string
+		assignment    flavorassigner.Assignment
+		wantPreempted sets.Set[string]
+	}{
```

  A suggestion for a weird test: Is it worth it to have a test where a workload has two podSets, each one require preemption for the same resource, but they get different flavors after preemption?

- **test/integration/scheduler/preemption_test.go:84** by [@ahg-g](https://github.com/ahg-g) — 2023-01-22T06:14:40Z
```diff
@@ -0,0 +1,238 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	lowPriority int32 = iota - 1
+	midPriority
+	highPriority
+)
+
+var _ = ginkgo.Describe("Preemption", func() {
+	var (
+		alphaFlavor *kueue.ResourceFlavor
+
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "preemption-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		alphaFlavor = testing.MakeResourceFlavor("alpha").Obj()
+		gomega.Expect(k8sClient.Create(ctx, alphaFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, alphaFlavor, true)
+	})
+
+	ginkgo.Context("In a single ClusterQueue", func() {
+		var (
+			cq *kueue.ClusterQueue
+			q  *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(alphaFlavor.Name, "4").Obj()).
+					Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+			q = testing.MakeLocalQueue("q", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, q)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		})
+
+		ginkgo.It("Should preempt Workloads with lower priority when there is not enough quota", func() {
```

  a test with node affinity and/or tolerations to verify that those are taken into account when selecting the flavors on preemption.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-01-23T11:17:11Z

- **pkg/cache/snapshot.go:45** by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-23T10:39:38Z
```diff
@@ -29,6 +29,28 @@ type Snapshot struct {
 	InactiveClusterQueueSets sets.Set[string]
 }
 
+// RemoveWorkload removes a workload from its corresponding ClusterQueue and
+// updates resources usage.
+func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
+	cq := s.ClusterQueues[wl.ClusterQueue]
+	delete(cq.Workloads, workload.Key(wl.Obj))
+	updateUsage(wl, cq.UsedResources, -1)
+	if cq.Cohort != nil {
+		updateUsage(wl, cq.Cohort.UsedResources, -1)
+	}
+}
+
+// AddWorkloads removes a workload from its corresponding ClusterQueue and
+// updates resources usage.
+func (s *Snapshot) AddWorkload(wl *workload.Info) {
```

  ```suggestion
  // AddWorkload removes a workload from its corresponding ClusterQueue and
  // updates resources usage.
  func (s *Snapshot) AddWorkload(wl *workload.Info) {
  ```

- **pkg/scheduler/preemption/preemption.go:44** by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-23T11:04:25Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
```

  Do you have reasons why this is hardcoded?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-24T18:59:22Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T18:59:22Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  The word "Reclaim" might be necessary if we ever add "stealing": when a ClusterQueue is able to preempt while borrowing quota for high priority workloads.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-24T19:04:29Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T19:04:29Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  An alternative:
  
  ```yaml
  preemption:
    reclaimWithinCohort: Never|LowerPriority|Any
    withinClusterQueue: Never|LowerPriority
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-01-24T19:13:01Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@ahg-g](https://github.com/ahg-g) — 2023-01-24T19:13:00Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  Shouldn't we support that under both `LowerPriority` (preempt lower priority and borrow the remaining) and `Any` (preempt any and borrow the remaining)?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-24T19:26:18Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T19:26:18Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  Are you saying "preempt any|low-priority from ClusterQueue and borrow the remaining"? This doesn't affect any workload in the cohort. We might be able to support this in the near future without changing the API. It's a bit of an optimization and I wouldn't call this "stealing".
  
  These two cases, that I call "stealing", require an explicit API IMO, where the action leads to the preempting ClusterQueue to be in a borrowing status:
  - preempt low priority workloads in the cohort that are currently borrowing
  - (a bit more extreme) preempt low priority workloads in the cohort from ClusterQueues that are not currently borrowing

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-01-25T18:21:15Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@mwielgus](https://github.com/mwielgus) — 2023-01-25T18:21:15Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  If I may suggest, I think the constants should be the same for both preemption on borrowing and preemption on reclaiming so I like the proposal @alculquicondor made 3 comments above.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-01-25T20:06:31Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@ahg-g](https://github.com/ahg-g) — 2023-01-25T20:06:31Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  > An alternative:
  > 
  > ```yaml
  > preemption:
  >   reclaimWithinCohort: Never|LowerPriority|Any
  >   withinClusterQueue: Never|LowerPriority
  > ```
  
  isn't that what I suggested?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-01-25T20:11:01Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@ahg-g](https://github.com/ahg-g) — 2023-01-25T20:11:01Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  ok, you added reclaim prefix, sure.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-25T20:45:45Z

- **pkg/scheduler/scheduler_test.go:691** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T19:42:00Z
```diff
@@ -682,6 +688,44 @@ func TestSchedule(t *testing.T) {
 				"eng-beta/user-on-demand": *utiltesting.MakeAdmission("eng-beta").Flavor(corev1.ResourceCPU, "on-demand").Obj(),
 			},
 		},
+		"preempt workloads in ClusterQueue and cohort": {
```

  Most of the tests are in the preemption package though.

- **pkg/cache/cache.go:284** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T19:43:26Z
```diff
@@ -274,6 +280,13 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[string
 	}
 	c.UsedResources = usedResources
 	c.UpdateWithFlavors(resourceFlavors)
+
+	if in.Spec.Preemption != nil {
```

  Added to the webhook as well.
  But we might need this for old ClusterQueues and unit tests that are not using the wrappers yet.

- **pkg/scheduler/preemption/preemption.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T20:28:24Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
```

  Just simplicity. We can add a configuration option later. But it might just be a general parallelism parameter, like in scheduler.

- **pkg/scheduler/preemption/preemption.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:09:46Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
```

  Done

- **pkg/scheduler/preemption/preemption.go:134** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:11:31Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
```

  because as you remove workloads, the CQ might not be borrowing anymore.

- **pkg/scheduler/preemption/preemption.go:168** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:14:14Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
```

  Yes, added a comment here.

- **pkg/scheduler/preemption/preemption.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:14:59Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
```

  Yes, but I'll leave that as a follow up.

- **pkg/scheduler/preemption/preemption.go:217** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:15:32Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
+			}
+			if !workloadUsesFlavors(candidateWl, flavors) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
+	}
+	return candidates
+}
+
+func cqIsBorrowing(cq *cache.ClusterQueue, flavors flavorsPerResource) bool {
```

  this changes as we simulate the removal of workloads.

- **pkg/scheduler/preemption/preemption.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:17:12Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
+			}
+			if !workloadUsesFlavors(candidateWl, flavors) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
+	}
+	return candidates
+}
+
+func cqIsBorrowing(cq *cache.ClusterQueue, flavors flavorsPerResource) bool {
+	for res, rFlavors := range flavors {
+		fUsage := cq.UsedResources[res]
+		requestable := cq.RequestableResources[res]
+		if requestable == nil {
+			// Cant' be borrowing if this resource is not defined in the ClusterQueue.
+			continue
+		}
+		for _, flvLimits := range requestable.Flavors {
+			if rFlavors.Has(flvLimits.Name) && fUsage[flvLimits.Name] > flvLimits.Min {
+				return true
+			}
+		}
+	}
+	return false
+}
+
+func workloadUsesFlavors(wl *workload.Info, flavors flavorsPerResource) bool {
+	for _, ps := range wl.TotalRequests {
```

  Probably more worth doing the optimization of tracking priorities. This function is only used once per workload and it's not particularly heavy.

- **pkg/scheduler/preemption/preemption.go:303** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:21:54Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
+
+type Preemptor struct {
+	client   client.Client
+	recorder record.EventRecorder
+
+	// stubs
+	applyPreemption func(context.Context, *kueue.Workload) error
+}
+
+func New(cl client.Client, recorder record.EventRecorder) *Preemptor {
+	p := &Preemptor{
+		client:   cl,
+		recorder: recorder,
+	}
+	p.applyPreemption = p.applyPreemptionWithSSA
+	return p
+}
+
+func (p *Preemptor) OverrideApply(f func(context.Context, *kueue.Workload) error) {
+	p.applyPreemption = f
+}
+
+func (p *Preemptor) Do(ctx context.Context, wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	flavors := flavorsRequiringPreemption(assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+
+	candidates := findCandidates(wl.Obj, cq, flavors)
+	if len(candidates) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionWithinCohort", cq.Preemption.WithinCohort)
+		return nil
+	}
+	sort.Slice(candidates, candidatesOrdering(candidates, cq.Name, time.Now()))
+
+	targets := minimalPreemptions(&wl, assignment, snapshot, flavors, candidates)
+
+	if len(targets) == 0 {
+		log.V(2).Info("Workload requires preemption, but there are not enough candidate workloads allowed for preemption")
+	}
+
+	return p.issuePreemptions(ctx, targets, cq)
+}
+
+func (p *Preemptor) issuePreemptions(ctx context.Context, targets []*workload.Info, cq *cache.ClusterQueue) error {
+	log := ctrl.LoggerFrom(ctx)
+	errCh := routine.NewErrorChannel()
+	ctx, cancel := context.WithCancel(ctx)
+	defer cancel()
+	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
+		target := targets[i]
+		err := p.applyPreemption(ctx, workload.ClearAdmissionPatch(target.Obj))
+		if err != nil {
+			errCh.SendErrorWithCancel(err, cancel)
+			return
+		}
+		origin := "ClusterQueue"
+		if cq.Name != target.ClusterQueue {
+			origin = "cohort"
+		}
+		log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
+		p.recorder.Eventf(target.Obj, corev1.EventTypeNormal, "Preempted", "Preempted by another workload in the %s", origin)
+	})
+	return errCh.ReceiveError()
+}
+
+func (p *Preemptor) applyPreemptionWithSSA(ctx context.Context, w *kueue.Workload) error {
+	return p.client.Patch(ctx, w, client.Apply, client.FieldOwner(constants.AdmissionName))
+}
+
+func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, flavors flavorsPerResource, candidates []*workload.Info) []*workload.Info {
+	wlReq := totalRequestsForAssignment(wl, assignment)
+	cq := snapshot.ClusterQueues[wl.ClusterQueue]
+	// Simulate removing all candidates from the ClusterQueue and cohort.
+	var targets []*workload.Info
+	fits := false
+	for _, candWl := range candidates {
+		candCQ := snapshot.ClusterQueues[candWl.ClusterQueue]
+		if cq != candCQ && !cqIsBorrowing(candCQ, flavors) {
+			continue
+		}
+		snapshot.RemoveWorkload(candWl)
+		targets = append(targets, candWl)
+		if workloadFits(wlReq, cq) {
+			fits = true
+			break
+		}
+	}
+	if !fits {
+		return nil
+	}
+	// In the reverse order, check if any of the workloads can be added back.
+	for i := len(targets) - 2; i >= 0; i-- {
+		snapshot.AddWorkload(targets[i])
+		if workloadFits(wlReq, cq) {
+			// O(1) deletion: copy the last element into index i and reduce size.
+			targets[i] = targets[len(targets)-1]
+			targets = targets[:len(targets)-1]
+		} else {
+			snapshot.RemoveWorkload(targets[i])
+		}
+	}
+	return targets
+}
+
+type flavorsPerResource map[corev1.ResourceName]sets.Set[string]
+
+func flavorsRequiringPreemption(assignment flavorassigner.Assignment) flavorsPerResource {
+	flavors := make(flavorsPerResource)
+	for _, ps := range assignment.PodSets {
+		for res, flvAssignment := range ps.Flavors {
+			if flvAssignment.Mode != flavorassigner.Preempt {
+				continue
+			}
+			if flavors[res] == nil {
+				flavors[res] = sets.New(flvAssignment.Name)
+			} else {
+				flavors[res].Insert(flvAssignment.Name)
+			}
+		}
+	}
+	return flavors
+}
+
+// findCandidates obtains candidates for preemption within the ClusterQueue and
+// cohort that respect the preemption policy and are using a flavor that the
+// preempting workload needs.
+func findCandidates(wl *kueue.Workload, cq *cache.ClusterQueue, flavors flavorsPerResource) []*workload.Info {
+	var candidates []*workload.Info
+	cqs := sets.New(cq)
+	if cq.Cohort != nil && cq.Preemption.WithinCohort != kueue.PreemptionPolicyNever {
+		cqs = cq.Cohort.Members
+	}
+	if cq.Preemption.WithinClusterQueue == kueue.PreemptionPolicyNever {
+		cqs.Delete(cq)
+	}
+	for cohortCQ := range cqs {
+		onlyLowerPrio := true
+		if cq != cohortCQ {
+			if !cqIsBorrowing(cohortCQ, flavors) {
+				// Can't reclaim quota from ClusterQueues that are not borrowing.
+				continue
+			}
+			if cq.Preemption.WithinCohort == kueue.PreemptionPolicyReclaimFromAny {
+				onlyLowerPrio = false
+			}
+		}
+		for _, candidateWl := range cohortCQ.Workloads {
+			if onlyLowerPrio && priority.Priority(candidateWl.Obj) >= priority.Priority(wl) {
+				continue
+			}
+			if !workloadUsesFlavors(candidateWl, flavors) {
+				continue
+			}
+			candidates = append(candidates, candidateWl)
+		}
+	}
+	return candidates
+}
+
+func cqIsBorrowing(cq *cache.ClusterQueue, flavors flavorsPerResource) bool {
+	for res, rFlavors := range flavors {
+		fUsage := cq.UsedResources[res]
+		requestable := cq.RequestableResources[res]
+		if requestable == nil {
+			// Cant' be borrowing if this resource is not defined in the ClusterQueue.
+			continue
+		}
+		for _, flvLimits := range requestable.Flavors {
+			if rFlavors.Has(flvLimits.Name) && fUsage[flvLimits.Name] > flvLimits.Min {
+				return true
+			}
+		}
+	}
+	return false
+}
+
+func workloadUsesFlavors(wl *workload.Info, flavors flavorsPerResource) bool {
+	for _, ps := range wl.TotalRequests {
+		for res, flv := range ps.Flavors {
+			if flavors[res].Has(flv) {
+				return true
+			}
+		}
+	}
+	return false
+}
+
+func totalRequestsForAssignment(wl *workload.Info, assignment flavorassigner.Assignment) cache.ResourceQuantities {
+	usage := make(cache.ResourceQuantities)
+	for i, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			resUsage := usage[res]
+			if resUsage == nil {
+				resUsage = make(map[string]int64)
+				usage[res] = resUsage
+			}
+			resUsage[assignment.PodSets[i].Flavors[res].Name] += q
+		}
+	}
+	return usage
+}
+
+// workloadFits determines if the workload requests would fits given the
+// requestable resources and simulated usage of the ClusterQueue and its cohort,
+// if it belongs to one.
+// These two checks are a simplification compared to flavorassigner.fitsFlavorsLimits,
+// because there is no borrowing when doing preemptions.
+func workloadFits(wlReq cache.ResourceQuantities, cq *cache.ClusterQueue) bool {
+	for res, flvReqs := range wlReq {
+		cqFlvsUsage := cq.UsedResources[res]
+		cqResRequestable := cq.RequestableResources[res]
+		var cohortFlvsUsage, cohortFlvsRequestable map[string]int64
+		if cq.Cohort != nil {
+			cohortFlvsUsage = cq.Cohort.UsedResources[res]
+			cohortFlvsRequestable = cq.Cohort.RequestableResources[res]
+		}
+		for _, flvLimits := range cqResRequestable.Flavors {
+			flvReq, ok := flvReqs[flvLimits.Name]
+			if !ok {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			if cqFlvsUsage[flvLimits.Name]+flvReq > flvLimits.Min {
+				return false
+			}
+			if len(cohortFlvsRequestable) == 0 {
+				continue
+			}
+			if cohortFlvsUsage[flvLimits.Name]+flvReq > cohortFlvsRequestable[flvLimits.Name] {
+				return false
+			}
+		}
+	}
+	return true
+}
+
+func candidatesOrdering(candidates []*workload.Info, cq string, now time.Time) func(int, int) bool {
+	return func(i, j int) bool {
+		a := candidates[i]
+		b := candidates[j]
+		aInCQ := a.ClusterQueue == cq
```

  Added

- **pkg/scheduler/scheduler.go:158** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:24:32Z
```diff
@@ -138,19 +141,23 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		if e.assignment.RepresentativeMode() == flavorassigner.NoFit {
 			continue
 		}
-		c := snapshot.ClusterQueues[e.ClusterQueue]
-		if e.assignment.Borrows() && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
+		cq := snapshot.ClusterQueues[e.ClusterQueue]
+		if e.assignment.Borrows() && cq.Cohort != nil && usedCohorts.Has(cq.Cohort.Name) {
 			e.status = skipped
 			e.inadmissibleMsg = "workloads in the cohort that don't require borrowing were prioritized and admitted first"
 			continue
 		}
 		// Even if there was a failure, we shouldn't admit other workloads to this
 		// cohort.
-		if c.Cohort != nil {
-			usedCohorts.Insert(c.Cohort.Name)
+		if cq.Cohort != nil {
+			usedCohorts.Insert(cq.Cohort.Name)
 		}
+		log := log.WithValues("workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", e.ClusterQueue))
+		ctx := ctrl.LoggerInto(ctx, log)
 		if e.assignment.RepresentativeMode() != flavorassigner.Fit {
-			// TODO(#43): Implement preemption.
+			if err := s.preemptor.Do(ctx, e.Info, e.assignment, &snapshot); err != nil {
```

  Yes, Added to the event. I prefer not to change the condition, to avoid spamming.

- **pkg/workload/workload.go:253** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:25:57Z
```diff
@@ -237,3 +237,32 @@ func UpdateStatusIfChanged(ctx context.Context,
 	// Updating an existing condition
 	return UpdateStatus(ctx, c, wl, conditionType, conditionStatus, reason, message)
 }
+
+// ClearAdmissionPatch creates a new object based on the input workload that
+// doesn't contain admission. The object can be used in Server-Side-Apply.
+func ClearAdmissionPatch(w *kueue.Workload) *kueue.Workload {
+	wlCopy := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			UID:        w.UID,
+			Name:       w.Name,
+			Namespace:  w.Namespace,
+			Generation: w.Generation, // Produce a conflict if there was a change in the spec.
+		},
+		TypeMeta: w.TypeMeta,
+	}
+	if wlCopy.APIVersion == "" {
```

  No, depending on where you grab the object from, the field might be missing https://github.com/kubernetes-sigs/controller-runtime/issues/2016

- **pkg/workload/workload.go:243** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:27:46Z
```diff
@@ -237,3 +237,32 @@ func UpdateStatusIfChanged(ctx context.Context,
 	// Updating an existing condition
 	return UpdateStatus(ctx, c, wl, conditionType, conditionStatus, reason, message)
 }
+
+// ClearAdmissionPatch creates a new object based on the input workload that
+// doesn't contain admission. The object can be used in Server-Side-Apply.
+func ClearAdmissionPatch(w *kueue.Workload) *kueue.Workload {
```

  If you look at it from the perspective of `AdmissionPatch`, yes. But see that as an implementation detail.
  From a user of just the exported functions, this is about getting a patch for deleting the Admission.

- **test/integration/scheduler/preemption_test.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:28:14Z
```diff
@@ -0,0 +1,238 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	lowPriority int32 = iota - 1
+	midPriority
+	highPriority
+)
+
+var _ = ginkgo.Describe("Preemption", func() {
+	var (
+		alphaFlavor *kueue.ResourceFlavor
+
```

  Removed

- **test/integration/scheduler/preemption_test.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-24T21:29:05Z
```diff
@@ -0,0 +1,238 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	lowPriority int32 = iota - 1
+	midPriority
+	highPriority
+)
+
+var _ = ginkgo.Describe("Preemption", func() {
+	var (
+		alphaFlavor *kueue.ResourceFlavor
+
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "preemption-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		alphaFlavor = testing.MakeResourceFlavor("alpha").Obj()
+		gomega.Expect(k8sClient.Create(ctx, alphaFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, alphaFlavor, true)
+	})
+
+	ginkgo.Context("In a single ClusterQueue", func() {
+		var (
+			cq *kueue.ClusterQueue
+			q  *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(alphaFlavor.Name, "4").Obj()).
+					Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+			q = testing.MakeLocalQueue("q", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, q)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		})
+
+		ginkgo.It("Should preempt Workloads with lower priority when there is not enough quota", func() {
```

  Uhm... I would leave it for the flavorassigner unit tests. I'll do it in a separate PR.

- **test/integration/scheduler/preemption_test.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-25T19:26:15Z
```diff
@@ -0,0 +1,238 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	lowPriority int32 = iota - 1
+	midPriority
+	highPriority
+)
+
+var _ = ginkgo.Describe("Preemption", func() {
+	var (
+		alphaFlavor *kueue.ResourceFlavor
+
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "preemption-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		alphaFlavor = testing.MakeResourceFlavor("alpha").Obj()
+		gomega.Expect(k8sClient.Create(ctx, alphaFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, alphaFlavor, true)
+	})
+
+	ginkgo.Context("In a single ClusterQueue", func() {
+		var (
+			cq *kueue.ClusterQueue
+			q  *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(alphaFlavor.Name, "4").Obj()).
+					Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+			q = testing.MakeLocalQueue("q", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, q)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		})
+
+		ginkgo.It("Should preempt Workloads with lower priority when there is not enough quota", func() {
+			ginkgo.By("Creating initial Workloads with different priorities")
+			lowWl1 := testing.MakeWorkload("low-wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			lowWl2 := testing.MakeWorkload("low-wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			midWl := testing.MakeWorkload("mid-wl", ns.Name).
+				Queue(q.Name).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			highWl1 := testing.MakeWorkload("high-wl-1", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, lowWl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowWl2)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, midWl)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, highWl1)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl1, lowWl2, midWl, highWl1)
+
+			ginkgo.By("Creating a low priority Workload")
+			lowWl3 := testing.MakeWorkload("low-wl-3", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, lowWl3)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl3)
+
+			ginkgo.By("Creating a high priority Workload")
+			highWl2 := testing.MakeWorkload("high-wl-2", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, highWl2)).To(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl2)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, lowWl1, lowWl2)
+		})
+	})
+
+	ginkgo.Context("In a ClusterQueue that is part of a cohort", func() {
```

  I just modified the existing case. I don't see much value on having two cases, as more detailed ones are tested at unit level.

- **test/integration/webhook/v1alpha2/clusterqueue_test.go:119** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-25T20:12:50Z
```diff
@@ -116,50 +186,76 @@ var _ = ginkgo.Describe("ClusterQueue Webhook", func() {
 				gomega.Expect(err).Should(gomega.HaveOccurred())
 				gomega.Expect(errors.IsInvalid(err)).Should(gomega.BeTrue(), "error: %v", err)
 			default:
-				gomega.Expect(err).Should(gomega.Succeed())
```

  This is the alternative writing: `gomega.Expect(err).ShouldNot(gomega.HaveOccurred())`
  Just being idiomatic.

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-25T20:45:34Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  Updated

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-25T21:09:29Z

- **pkg/scheduler/preemption/preemption_test.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-25T21:09:28Z
```diff
@@ -0,0 +1,587 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"sync"
+	"testing"
+	"time"
+
+	"github.com/go-logr/logr/testr"
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func TestPreemption(t *testing.T) {
```

  95%

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-25T21:09:58Z

- **pkg/scheduler/preemption/preemption_test.go:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-25T21:09:58Z
```diff
@@ -0,0 +1,587 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"sync"
+	"testing"
+	"time"
+
+	"github.com/go-logr/logr/testr"
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func TestPreemption(t *testing.T) {
+	flavors := []*kueue.ResourceFlavor{
+		utiltesting.MakeResourceFlavor("default").Obj(),
+		utiltesting.MakeResourceFlavor("alpha").Obj(),
+		utiltesting.MakeResourceFlavor("beta").Obj(),
+	}
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("standalone").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Resource(utiltesting.MakeResource(corev1.ResourceMemory).
+				Flavor(utiltesting.MakeFlavor("alpha", "3Gi").Obj()).
+				Flavor(utiltesting.MakeFlavor("beta", "3Gi").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj(),
+		utiltesting.MakeClusterQueue("c1").
+			Cohort("cohort").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				WithinCohort:       kueue.PreemptionPolicyReclaimFromLowerPriority,
+			}).
+			Obj(),
+		utiltesting.MakeClusterQueue("c2").
+			Cohort("cohort").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyNever,
+				WithinCohort:       kueue.PreemptionPolicyReclaimFromAny,
+			}).
+			Obj(),
+	}
+	cases := map[string]struct {
+		admitted      []kueue.Workload
+		incoming      *kueue.Workload
+		targetCQ      string
+		assignment    flavorassigner.Assignment
+		wantPreempted sets.Set[string]
+	}{
```

  Oh, I missed this comment. That's a good test case, I'll add one.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-25T21:10:41Z

- **test/integration/scheduler/preemption_test.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-25T21:10:40Z
```diff
@@ -0,0 +1,238 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	lowPriority int32 = iota - 1
+	midPriority
+	highPriority
+)
+
+var _ = ginkgo.Describe("Preemption", func() {
+	var (
+		alphaFlavor *kueue.ResourceFlavor
+
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "preemption-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+		alphaFlavor = testing.MakeResourceFlavor("alpha").Obj()
+		gomega.Expect(k8sClient.Create(ctx, alphaFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, alphaFlavor, true)
+	})
+
+	ginkgo.Context("In a single ClusterQueue", func() {
+		var (
+			cq *kueue.ClusterQueue
+			q  *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			cq = testing.MakeClusterQueue("cq").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(alphaFlavor.Name, "4").Obj()).
+					Obj()).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cq)).To(gomega.Succeed())
+
+			q = testing.MakeLocalQueue("q", ns.Name).ClusterQueue(cq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, q)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		})
+
+		ginkgo.It("Should preempt Workloads with lower priority when there is not enough quota", func() {
```

  #530

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-01-26T02:13:28Z

- **pkg/scheduler/preemption/preemption.go:44** by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-26T02:13:28Z
```diff
@@ -0,0 +1,306 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	"sigs.k8s.io/kueue/pkg/util/priority"
+	"sigs.k8s.io/kueue/pkg/util/routine"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const parallelPreemptions = 8
```

  Sounds good.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-27T15:44:11Z

- **pkg/scheduler/preemption/preemption_test.go:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-27T15:44:10Z
```diff
@@ -0,0 +1,587 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"sort"
+	"sync"
+	"testing"
+	"time"
+
+	"github.com/go-logr/logr/testr"
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/flavorassigner"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func TestPreemption(t *testing.T) {
+	flavors := []*kueue.ResourceFlavor{
+		utiltesting.MakeResourceFlavor("default").Obj(),
+		utiltesting.MakeResourceFlavor("alpha").Obj(),
+		utiltesting.MakeResourceFlavor("beta").Obj(),
+	}
+	clusterQueues := []*kueue.ClusterQueue{
+		utiltesting.MakeClusterQueue("standalone").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Resource(utiltesting.MakeResource(corev1.ResourceMemory).
+				Flavor(utiltesting.MakeFlavor("alpha", "3Gi").Obj()).
+				Flavor(utiltesting.MakeFlavor("beta", "3Gi").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj(),
+		utiltesting.MakeClusterQueue("c1").
+			Cohort("cohort").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				WithinCohort:       kueue.PreemptionPolicyReclaimFromLowerPriority,
+			}).
+			Obj(),
+		utiltesting.MakeClusterQueue("c2").
+			Cohort("cohort").
+			Resource(utiltesting.MakeResource(corev1.ResourceCPU).
+				Flavor(utiltesting.MakeFlavor("default", "6").Obj()).
+				Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyNever,
+				WithinCohort:       kueue.PreemptionPolicyReclaimFromAny,
+			}).
+			Obj(),
+	}
+	cases := map[string]struct {
+		admitted      []kueue.Workload
+		incoming      *kueue.Workload
+		targetCQ      string
+		assignment    flavorassigner.Assignment
+		wantPreempted sets.Set[string]
+	}{
```

  Added in the last commit

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-27T16:02:36Z

- **apis/kueue/v1alpha2/clusterqueue_types.go:298** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-27T16:02:36Z
```diff
@@ -272,6 +289,48 @@ type Usage struct {
 	Borrowed *resource.Quantity `json:"borrowing,omitempty"`
 }
 
+type PreemptionPolicy string
+
+const (
+	PreemptionPolicyNever                    PreemptionPolicy = "Never"
+	PreemptionPolicyReclaimFromLowerPriority PreemptionPolicy = "ReclaimFromLowerPriority"
+	PreemptionPolicyReclaimFromAny           PreemptionPolicy = "ReclaimFromAny"
+	PreemptionPolicyLowerPriority            PreemptionPolicy = "LowerPriority"
```

  > Are you saying "preempt any|low-priority from ClusterQueue and borrow the remaining"? This doesn't affect any workload in the cohort. We might be able to support this in the near future without changing the API. It's a bit of an optimization and I wouldn't call this "stealing".
  
  tracking issue #533

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-01-28T01:54:55Z

/lgtm
/hold

for one nit

- **apis/kueue/webhooks/clusterqueue_webhook.go:42** by [@ahg-g](https://github.com/ahg-g) — 2023-01-28T01:45:24Z
```diff
@@ -38,13 +38,16 @@ const (
 	isNegativeErrorMsg string = `must be greater than or equal to 0`
 )
 
-type ClusterQueueWebhook struct{}
+type ClusterQueueWebhook struct {
+	scheme *runtime.Scheme
```

  where is this used?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-30T13:23:01Z

- **apis/kueue/webhooks/clusterqueue_webhook.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T13:23:01Z
```diff
@@ -38,13 +38,16 @@ const (
 	isNegativeErrorMsg string = `must be greater than or equal to 0`
 )
 
-type ClusterQueueWebhook struct{}
+type ClusterQueueWebhook struct {
+	scheme *runtime.Scheme
```

  Left over from another approach I tried. Removed
